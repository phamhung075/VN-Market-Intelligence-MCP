{
  "name": "vn-market-intelligence",
  "version": "1.0.0",
  "description": "VN Market Intelligence — DDD monorepo (MCP server + microservices)",
  "private": true,
  "scripts": {
    "test": "pnpm --filter mcp-server test",
    "test:all": "pnpm -r test",
    "build": "pnpm -r build",
    "check": "pnpm --filter mcp-server check"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
