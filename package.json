{
  "name": "vn-market-intelligence",
  "version": "1.0.0",
  "description": "VN Market Intelligence — DDD monorepo (MCP server + microservices)",
  "private": true,
  "scripts": {
    "test": "pnpm --filter vn-market test",
    "test:all": "pnpm -r test",
    "build": "pnpm -r build",
    "check": "pnpm --filter vn-market check"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18",
    "bun": ">=1.3.13"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
