## Task Report TASK-BCTC-INSPECT-UI-FILTERS
changed: [apps/mcp-server/src/interface/bctc-inspector.html:92 (.filter-select CSS)+870-881 (#quarter-filter/#ticker-filter selects)+1074-1077 (allDocs cache)+1091-1093 (DOM refs)+1137-1258 (normalizeQuarter/renderDocOptions/populateFilterOptions/applyFilters + loadDocList refactor), apps/mcp-server/src/__tests__/FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER.test.ts (new, 18 assertions), docs/architecture/microservice/mcp-server/financial-reports.md (new subsection)]
commit: 2e66153fd (direct to main — no task branch; project-wide CLAUDE.md "NO branches" policy); depends on paired commit 237fa6e26 (TASK-BCTC-INSPECT-LABEL-FIX, AC9 server-side label fix, separately QA'd DONE_VERIFIED)
tests: 18 pass / 0 fail (new file, 30 expect) + 109 pass / 0 fail (5 named regression files, 257 expect) | tsc: 0 errors | ddd: PASS | security: PASS | mock-guard: N/A (.html not in scan scope — hand-verified diff instead, see below)
verdict: APPROVED — DONE_VERIFIED

### Verification method
Direct-Commit Verify, adapted lane (row arrived in `task_board.review[]`/`REVIEW`, not `qa[]`/`QA` — same precedent as TASK-BCTC-INSPECT-LABEL-FIX cycle-809/sibling report). `git merge-base --is-ancestor 2e66153fd main` / `cab92d7c5 main` both confirm ancestry to local `main` (this repo's "already on main" reference for the no-branch model; local `main` is currently 59 commits ahead of `origin/main`, an unrelated push-cadence gap out of QA scope — noted, not blocking). Zero uncommitted changes under `apps/mcp-server/` at verify time, so all runs below reflect exactly the committed content, unaffected by concurrent peer edits elsewhere in the shared working tree.

Independently re-ran every check rather than trusting the developer's prose:
- `bun test src/__tests__/FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER.test.ts` → 18/18, 30 expect().
- `bun test` on the 5 named regression files (`1976-bctc-inspector-page-nav`, `1273-bctc-inspect-overlay`, `PI3-bctc-inspect`, `PI3-bctc-inspect-reopen2`, `1271-bctc-inspect-md`) → 109/109, 257 expect() — exact match to AC8's "unchanged" requirement.
- `bun tsc --noEmit` (apps/mcp-server) — clean.
- `bash scripts/audits/mock-guard.sh --files apps/mcp-server/src/interface/bctc-inspector.html` → "No production source files to scan" (the guard only scans `.ts/.tsx/.py/.go` — a real scope gap, not evidence of a pass). Compensated by hand-reading the full `git show 2e66153fd` diff: every rendered value is sourced from the already-fetched `items[]` payload (`item.doc_id`, `item.label`, `item.action_code`, `item.period_year`/`period_quarter`, `item.has_pdf`/`has_ocr`/`anomaly_decimal_shift`) — zero hardcoded/fabricated data.
- DDD scan: file has no `infrastructure`/`application` imports (pure client-side vanilla JS embedded in a static HTML page, no module imports at all).
- Security scan: no `process.env`, no hardcoded secrets/credentials.

### Live dual-origin browser verification (real Chromium via Playwright — not curl/API-only)
Ran identical interaction scripts against BOTH `http://localhost:3000/api/bctc-inspect` (direct mcp-server) and `http://localhost:3001/dashboard/bctc-inspect` (frontend proxy, the user's actual entry point) — results identical on both origins:
- **AC1/AC3**: quarter facet renders 11 real `<option>`s + placeholder `"— tất cả (11) —"` (live count). NOT 13 — the 2 live HUT rows with `period_quarter: "Q1"` (string) are correctly coerced by `normalizeQuarter()` into the existing `(2024,1)`/`(2026,1)` numeric buckets (cross-checked against the raw `/api/bctc-inspect/docs` payload: 257 items, 11 distinct `(year,quarter)` keys total including the 2 HUT string rows).
- **AC1/AC4**: ticker facet renders 50 real options + placeholder `"— tất cả (50) —"`.
- **AC5**: AND-compose — selecting ticker=HUT + quarter=2024-1 narrows `#doc-select` to exactly 1 matching document (`HUT Q1 2024`), zero network requests fired during the filter change (confirms AC2 — zero additional network calls).
- **AC7**: zero-match — ticker=HUT + quarter=2026-2 (no intersection) renders a single `<option disabled selected>— no document matches —</option>` (both attrs present, per D-4), status bar reads `"Quý 2 2026 × HUT: không có tài liệu khớp"` (matches D-3 pseudocode format), zero console/page errors. Screenshotted both origins — pixel-identical.
- **AC9**: `#doc-select` option labels for HUT's 2024 docs render as `"HUT Q1 2024"` / `"HUT Q2 2024"` / `"HUT Q3 2024"` — not duplicated/malformed (`"HUT Q1 QQ1 2024"` was the pre-fix bug, resolved by paired commit `237fa6e26`).
- **FR-7/AC6 (selection preservation)**: selected a real document, applied a ticker filter that still matches it — `#doc-select.value` unchanged after the filter AND **zero** network requests fired during the change (the existing `change` handler fetches PDF/OCR/table/markdown for the selected `doc_id` — had a synthetic `change` event dispatched, this would have re-fired those fetches). Total `/api/bctc-inspect/docs` fetch count across the entire multi-filter interaction sequence = 1 (only the initial page load) — confirms `select.value` is mutated directly, never via `.dispatchEvent`.
- **AC8**: no regression — confirmed both by the 109/109 green regression suite and by `git show --stat` (doc-select `change` handler, header page-nav, overlay toggle, `resetPanes()` untouched by this diff).
- **AC10**: both `:3000` and `:3001` verified live and interactively, not just curl.

### Blast-radius smoke (rebuild carried 8 unrelated commits)
`/health` → `toolCount: 183` (matches handoff claim). No `scheduler` field exists in `/health` at all — confirmed via source read (`apps/mcp-server/src/interface/mcp/server.ts:505-518`), this is structural (this task touched zero scheduler files), not a regression; nearest available proxy `/api/cron-status` shows `layer_a_count: 89` (off-by-one from the router's stated 88, unrelated to this diff). `/api/bctc-inspect` → 200 on both origins. `/dashboards/news-fetch/` → 200 on `:3000`; 404 on `:3001` — confirmed structural (the frontend app has no route registered under that path prefix at all, `grep` of `apps/frontend/app/routes/` found zero matches — pre-existing, not caused by this rebuild).

BCTC Eval Gate: N/A (UI-only client-side filter, no `report_id`-scoped data mutation). OOM-Class Durability Gate: N/A (no crash/memory-durability claim).

### Process note (routing deviation, documented for traceability)
Row was dispatched to QA sitting in `task_board.review[]` (`status: REVIEW`, `next_agent: qa`), not `task_board.qa[]` — the flow's `verify-committed` JUMP-TO hardcodes source `qa[]`/`status: QA` as precondition, and its `vc-approved` actuator (per own notebook cycle-810/qa-S159 finding) both hard-guards to the wrong source lane for rows shaped like this AND never writes `verification.raw_probe`. Adapted the lane-move mechanism to the row's actual source lane (`review[] → done_verified[]`, guard on `status=="REVIEW"`) AND added a schema-conformant `verification.raw_probe` object on the same write. Self-verified the write persisted: `jq -e '.task_board.done_verified[] | select(.id=="TASK-BCTC-INSPECT-UI-FILTERS") | .status=="DONE_VERIFIED"'` → true; confirmed absent from `review[]`. `orch-apply.sh` Stage 0/1 PASS, conservation OK (task_total 779→780). Decision journal: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-26.md` STEP qa-S161.
