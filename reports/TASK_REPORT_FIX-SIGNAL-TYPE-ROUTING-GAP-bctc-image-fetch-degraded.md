## Task Report FIX-SIGNAL-TYPE-ROUTING-GAP-bctc-image-fetch-degraded
changed: docs/agents/po/flow/triage-signals-longtail.md (1 file, 3 insertions/2 deletions — routing table row for `bctc_image_fetch_degraded`, Pipeline-B)
commit: a309c9334 (docs, direct commit to main, no branch)
tests: `guard-signal-type-coverage.sh --check` re-run live — Pipeline-B check passes for this type (no "unrouted Pipeline-B" line) | `.test.sh` 23/24 (TEST10 fails on an unrelated pre-existing Pipeline-A gap, see below) | tsc --noEmit (apps/mcp-server): 0 errors | ddd: N/A (doc-only) | security: N/A (doc-only) | mock-guard: PASS (no production source touched)
verdict: APPROVED (direct-commit verify) — DONE_VERIFIED

### Verification method
Direct-Commit Verify (dev-team Review-Lane QA-Drain, `branch:null`). Commit `a309c9334` confirmed on `main` ancestry via `git merge-base --is-ancestor`; `git show --stat` confirms exactly the 1 claimed file changed.

**Scoping — attribution by pipeline + type, not by "guard was green/now isn't":** the dispatcher flagged that the guard is red again right now, but on a *different* pipeline/type (`flow_actuator_fix`/`system-issue`, Pipeline-A) that is NOT this row's scope. Independently reproduced this exactly: `bash scripts/audits/guard-signal-type-coverage.sh --check` prints only a Pipeline-A `FAIL` line (both types already separately backlog-tracked, own `dedup_key` rows — `FIX-SIGNAL-TYPE-ROUTING-GAP-flow-actuator-fix`, `FIX-SIGNAL-TYPE-ROUTING-GAP-system-issue`, confirmed live on the board); no `unrouted Pipeline-B` line is emitted, which per the script's own control flow (prints both FAIL blocks if both fail) means Pipeline-B — this row's pipeline — genuinely PASSES. `.test.sh` TEST10 (live-integration smoke test) fails for the identical, unrelated Pipeline-A reason, matching its own inline comment ("the guard doing its job, do not silence"). Cross-checked commit boundary: current HEAD sits many commits ahead of `a309c9334`; the 2 Pipeline-A gaps trace to a later, unrelated commit (`4e7aa7eaf`, dev-mcp-server's dual-pipeline guard rewrite — already separately reviewed this session, `vc-changes`, see decision journal STEP qa-S47), not to this row.

**CI (independently re-verified, not taken on dispatch-note claim):** `gh run view 32630904807 --json headSha,jobs` — `headSha=a309c933...` (this exact commit), job `signal-type-coverage-guard` `conclusion=success`.

`mock-guard.sh --files docs/agents/po/flow/triage-signals-longtail.md` → PASS (no production source). `bun tsc --noEmit` clean. Not OOM/crash-durability-class (doc-only routing-table row, no memory/crash claim) — OOM-Class Durability Gate not applicable.

### Board actuation
`.task_board.qa[]` → `.task_board.done_verified[]` in one `orch-apply.sh` write (status `QA`→`DONE_VERIFIED`, `status_note` appended with `[QA] Review Record (direct-commit verify)`, `verification.raw_probe` attached same write per schema gate — `gh run view` probe on run `32630904807`). Measured directly before/after my own snapshot: `qa[]` 7→4 (my own row minus 1, net drop of 3 total — see shared-workdir note below), `done_verified[]` 32→34. Row confirmed absent from `qa[]`, present in `done_verified[]` post-write with `status_note`/`verification` intact.

**Shared-working-directory note:** this repo has no branch isolation for direct-commit rows, so multiple QA-drain sub-sessions from the same batch-of-8 dispatch write to the same live `orch-state.json` concurrently. My `jq ... | orch-apply.sh` pipe reads the file fresh at execution time, so it correctly picked up 2 peer sub-sessions' already-applied, schema-valid moves (`TE-T23`, `UNBLOCK-FLEETPUSH-SIZELINT-ORCHSTATESCHEMA-NEW-OFFENDER-BLOCKS-ALL-PUSHES`, both → `done_verified`, `updated_by:qa`) — that is why the net `qa[]` depth dropped by 3, not 1, on this write; those 2 rows are peer work, independently verified and reported by that TE-T23 sub-session's own Task Report (`reports/TASK_REPORT_TE-T23.md`), not fabricated or double-counted here. My commit `55f100b2b` therefore bundles all 3 moves (single atomic file, single commit) — matches the established shared-workdir commit-collision precedent (cycle-799 QA notebook). Content independently verified correct for my own row regardless of commit-message attribution. Committed with explicit pathspec (`-- docs/data/orch/orch-state.json`), pushed clean to `origin/main`.

Decision journal: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-25.md` STEP qa-S48.
