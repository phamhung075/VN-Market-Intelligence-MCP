## Task Report FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT

**Commit (main, RAW-verified):** `ad26f2f95` — `git show --stat` confirms exactly 5 files: `pushBctcExtraction.ts`, `bctcVpsIngestHandler.ts`, `FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT.test.ts` (new), `FIX-CTG-3-STEP-C.test.ts` + `FIX-EXTRACT-CONSUMER-PDFPATH.test.ts` (updated). Design contract: `docs/architecture-briefs/2026-07-13-pdfextractor-ocr-timeout-async-reroute.md`.

### 1. Targeted + cross-reference tests (RAW re-run, not trusted from dispatcher)
- `bun tsc --noEmit` (apps/mcp-server) → clean, 0 errors.
- 3 dev-cited files → **29 pass / 0 fail** (matches dispatcher's claim).
- Every OTHER file in the repo importing the 2 changed modules (`grep -rl "pushBctcExtraction\|bctcVpsIngestHandler" src/`): `1945d-reparse-pipeline-gap.test.ts`, `FIX-BCTC-D3A-PEK-TRIGGER-HELPER.test.ts`, `bctc-pdf-pull-job.test.ts` → **18 + 26 pass / 0 fail** (44 additional tests). Total 5 test files / 47 tests directly touching the changed modules — **47/47 pass**.

### 2. Full suite (RAW, own run — not the dispatcher's)
`bun test` (apps/mcp-server root, 1203 files): **14583 pass / 40 skip / 69 fail / 10 errors**, 661s, followed by the known Bun 1.3.13 post-summary C++ panic (runner artifact, judged on the printed pass/fail summary line, not the crash).

Dispatcher/dev claimed 14590/62/7 (this run: 14583/69/10) — **numbers differ**, but this sprint's own decision journal (`sprint-FLOW-PRICE-ALPHA-LOOP-qa.md`, prior STEPs) already documents this exact suite's baseline bouncing between 63-67 fail / 4-10 errors run-to-run under full 1203-file parallel load — a known flake class, not new. Verified by exhaustive grep, not assumption:
- Extracted all 69 `(fail)` test names + all 10 `# Unhandled error between tests` stack contexts.
- Grepped every failing describe/test name and every failing test FILE's source for `pushBctcExtraction|bctcVpsIngestHandler|ensureFinancialReportShellRow|pekExtractTrigger|overwritePdfPathUnconditional|applyPushBctcExtractionOutcome` → **zero matches**.
- Failures cluster in: news-poll infra (`pollNews`/`push-news`/9-VPS-sources, ~24 tests, all the 10 unhandled-errors trace here to `titleFingerprint`/`rag_analyses`), `get_insider_transactions` (9), `logVpsPush`/`VPS Proxy Health` (16 — a shared helper used by both bctc AND news handlers, but the helper file itself, `vpsPushLogStore.ts`, is untouched by this commit and its own failures are `TypeError: null is not an object (evaluating 'row.status')` — a pre-existing schema/row-shape bug unrelated to outcome routing), `get_market_cap`/`get_company_profile`/`search_similar_context`/`get_foreign_flow` (12), `record_signal_outcome` tool-registration (1, unrelated tool), SSE endpoint timeout (2), `docs/analysis-briefs/BSR.md` sector-line lint (1), deprecated `1302-technical-indicators.test.ts` (2). Nearly all failures show exactly 5000-5012ms durations = timeout-clustering from full-suite CPU/resource contention, not assertion regressions.
- **Conclusion: none of the 69 fail + 10 errors reference or exercise the changed code.** Dev's unrelated-failure claim is confirmed in substance even though the exact count differs run-to-run.

### 3. AC1 — FAIL-LOUD (code review, `pushBctcExtraction.ts` / `bctcVpsIngestHandler.ts`)
`triggerPushBctcExtraction()` returns `PushBctcExtractionOutcome = {done|async_routed|failed}` (no `Promise<void>` path left). Traced every exit: all-3-tiers-exhausted → `failed`; `runPipeline()` null → `failed`; `runPipeline()` throws → `failed`; async-route missing deps → `failed`. `applyPushBctcExtractionOutcome()` (bctcVpsIngestHandler.ts:75-134) switches exhaustively on the 3 outcomes — `done` only sets `status='done'`, `failed` sets `status='failed'` + fires `sendBugFn` (default `sendTelegramBug`) inside its own try/catch (notify failure never blocks the status write). No path silently defaults to `done`. Verified by test (not just read): the "returns 'failed' when async-route deps are missing" + "runPipeline returns null/throws → failed" cases are all asserted.

### 4. AC2 — LARGE-PDF PATH (code review + test)
Confidence pre-gate (`extractText`/`readFile` on the just-saved buffer) runs BEFORE Tier 1. `confidence < 1.0` (0 or 0.3, both tested) routes to `routeToAsyncPekExtraction()` and **never reaches Tier 1/2/3** (`tier1Called`/`runPipelineCalled` asserted false in the test). `confidence === 1.0` keeps the sync gauntlet and **never calls** ensureShellRow/updatePdfPath/triggerPekExtraction (asserted false). Both directions test-proven, not just read.

### 5. MUST-FIX (Risk 1) — `overwritePdfPathUnconditional`
`apps/mcp-server/src/scheduler/financial-reports/pushBctcExtraction.ts:201-208` — parameterized `UPDATE financial_reports SET pdf_path = ? WHERE action_code = ? AND sort_key = ?`, no `IS NULL` guard. Proof is a REAL in-memory DB test (`initDatabase()`/`getDb()`, not a mock): test 1 shows `ensureFinancialReportShellRow` ALONE leaves the STALE path unchanged (sibling contract sanity check); test 2 shows the explicit unconditional call DOES overwrite it after the same `ensureShellRow` call sequence the production code uses. Call order in `routeToAsyncPekExtraction()` matches the brief exactly: `ensureShellRow → updatePdfPath (unconditional) → triggerPekExtraction`.

### 6. Dev-flagged side-effect — bctcPdfPullJob.ts Step5/Step6 double-fire (scrutinized at source, not waved through)
Confirmed the double-fire is real: `bctcPdfPullJob.ts` Step 5 (`deps.triggerExtraction`, untouched call site) now transitively calls the updated `triggerPushBctcExtraction`, which for a scanned pull-path PDF ALSO fires `/pek-extract` via the new `routeToAsyncPekExtraction` branch — a genuinely new side-effect this file's own code didn't have before, in addition to Step 6's pre-existing unconditional `triggerPekExtraction` call.
Read `apps/pdf-extractor/infrastructure/pek_engine_adapter.py:87-107,656-687` and `apps/pdf-extractor/interface/handlers.py:420-477` directly:
- `_extraction_semaphore = threading.Semaphore(1)` is process-global; `Dockerfile:117` runs `uvicorn main:app` with no `--workers` flag → single process, guard is effective (not defeated by multi-worker).
- `extract_layout_and_tables()` does `_extraction_semaphore.acquire(blocking=False)`; on contention raises `SemaphoreContendedError` immediately; `acquire`/`release` wrapped in `try/finally` (no permanent-stuck-lock risk).
- The loser's `SemaphoreContendedError` is caught by `_run_pek_extract`'s generic `except Exception` and logged via `_log.error(..., exc_info=True)` — no retry, no duplicate model inference, no HTTP 429 actually reaches the TS caller (contention is detected inside the fire-and-forget background task, after the 202 was already sent — the handler's docstring claim of "Contention → HTTP 429" is stale/inaccurate for this specific race window, but that inaccuracy is pre-existing documentation, not introduced by this fix).
- On the TS side: Step 5's outcome is discarded (`bctcPdfPullJob.ts`'s `triggerExtraction` dep has return type `Promise<void>`, awaits but does not return the outcome) so it cannot affect `bctc_vps_queue`'s state transition; Step 6's `updatePekTriggered.run(row.id)` still runs unconditionally exactly as before. **No queue-state corruption results.**
- **Verdict: dev's claim is CONFIRMED accurate** — the only effect is one wasted `/pek-extract` HTTP round-trip + a spurious ERROR-level log line (`_run_pek_extract: FAILED ...`) + a redundant (idempotent) `ensureShellRow`/`overwritePdfPathUnconditional` call. Recording as a **known-benign follow-up** (worth a small cleanup ticket later: dedupe Step5/Step6 in `bctcPdfPullJob.ts`, or refresh the stale "429" docstring) — not blocking this task.

### 7. Security / DDD sanity
All SQL in both changed files is parameterized (`?` placeholders via `db.prepare(...).run/get(...)`, no string interpolation). No `process.env` / `Bun.env` misuse, no hardcoded secrets. `actionCode` validated `^[A-Z0-9]{2,10}$`, `periodQuarter` enum-checked before use; `filePath`/`pdfPath` values flowing into `overwritePdfPathUnconditional` are server-computed (`normaliseFilename` + `resolve(pdfDir, ...)`), never raw user input — no new path-traversal surface. Every failure path is explicitly logged and returned as a discriminated outcome — no swallowed errors reintroduced (this is precisely what the fix corrects). Layering unchanged: scheduler/interface files call infrastructure via injected deps exactly as the pre-existing pattern in this codebase.

### Verdict
**APPROVED.**

AC1 + AC2 + MUST-FIX Risk-1 all verified by direct code read AND test (not just claim). Full-suite failures (69/10, differs numerically from dev's 62/7 but matches this sprint's own documented flaky baseline) confirmed by exhaustive grep to have zero overlap with the changed modules. The dev-flagged bctcPdfPullJob.ts double-fire side-effect was independently verified at the Python source (`pek_engine_adapter.py`) and is genuinely benign (Semaphore(1) guard real, single-process, try/finally-safe) — recorded as a known non-blocking follow-up, not a defect.

QA did not deploy/rebuild, did not reset `bctc_vps_queue` row 223, did not touch `docs/data/orch/orch-state.json`. Dispatcher to move the row to `deploy_pending` and dispatch ops for the gated rebuild.
