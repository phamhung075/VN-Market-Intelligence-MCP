## Task Report FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER

changed: scripts/git-hooks/pre-push (new run_doc_shaped_checks(), lines 59-69), scripts/git-hooks/pre-push.test.sh (new), docs/policies/dev-standards.md (new CANONICAL entry) — all by developer, commits `23c97bbb3`/`553496834`/`1804f9cc3`
tests: pre-push.test.sh 4 pass / 0 fail (re-run by QA, not trusted from prose) | tsc: N/A (docs/script-only change) | ddd: N/A | security: N/A
verdict: APPROVED (Direct-Commit Verify — no branch/handoff, row already committed straight to main per FIX row convention)

### Verification performed (independent, not trusted from developer's status_note)

1. **Diff read** — `scripts/git-hooks/pre-push` lines 59-69: `run_doc_shaped_checks()` runs `size-lint-justification.sh --check`, `task-claim-owner-session-lint.sh --check`, `bun test tool-registry-parity` UNCONDITIONALLY (not `CODE_TOUCHING_REGEX`-gated), and is invoked at line 69 — genuinely BEFORE the `PRE_PUSH_SKIP_TSC` early-exit at line 71. tsc + `rebuild-raw-verify-check.sh` confirmed unchanged, still on the pre-existing `CODE_TOUCHING_REGEX` gate (lines 81-128). Matches AC3 ("no silent fail-open on a docs-only push") and the architect brief's ordering-correction exactly.
2. **Hook installation** — `.git/hooks/pre-push` is a live symlink to `scripts/git-hooks/pre-push`; `diff` confirms byte-identical (installed, not stale).
3. **Test re-run** — `bash scripts/git-hooks/pre-push.test.sh` executed myself: `Results: 4 passed, 0 failed`, rc=0. Did not trust the developer's reported "4/4 pass".
4. **Push** — 5 pending commits (`cb83b20bf` peer po notebook, `23c97bbb3` fix, `553496834` WORK.md, `1804f9cc3` developer notebook, `7ff1cf1e6` board row) were unpushed, ahead of `origin/main`. Pushed via plain `git push origin main` (no `--force`, no `--no-verify`). This QA session has no MCP gateway/task_claim binding — confirmed live (`mcp__gateway__call_tool` → "No such tool available"), consistent with INV-GATEWAY-1 and the `commit-mutex` skill's own DISPATCHER-ONLY banner (specialist sub-agents must not invoke it). No commit-mutex ceremony was applicable since no new commit was authored — only a push of already-committed work, matching this flow's own `jump:approved` direct-push pattern. The hook ran live during the push: size-lint PASS (0 unjustified offenders, 1409 scanned), task-claim-lint PASS (0 new offenders), `bun test` 17 pass/0 fail (tool-registry-parity), tsc correctly skipped (no code-touching paths in the pushed range).
5. **CI confirmation on the fix-delivering head** — new head SHA `7ff1cf1e68057ba6594f32b8d584ab7db324a784`. `gh run view` on run `32594523529` (workflow "CI"): overall `conclusion=success`. Job-level (per the explicit ask, not just overall green): `size-lint`=success, `task-claim-owner-session-lint`=success, `bun test` (the job containing `tool-registry-parity.test.ts` — this is a test file inside the full per-file-isolation suite, not a standalone CI job; confirmed via `.github/workflows/ci.yml` — size-lint/task-claim-lint are separate jobs, tool-registry-parity is not)=success, `15130 pass / 40 skip / 0 fail`.
6. **Board write** — moved `.task_board.review[FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER]` → `.task_board.done_verified[]` via `scripts/orch-apply.sh` (re-read fresh immediately before the write — unchanged since the last read, no concurrent edit collision). Appended QA verification note + `verification.raw_probe{tool,args,live_value_observed,observed_at}` in the same write. `orch-apply.sh` Stage 0/1 PASS, conservation OK (task_total live=717/candidate=717), only pre-existing non-fatal prose-ceiling WARNs. Committed directly (`git commit -- docs/data/orch/orch-state.json` only, explicit pathspec) as `3962d6f3a`, then pushed.
7. **Side observation (not fabricated as a pass)** — the CI run triggered by my own board-write push (`3962d6f3a`) hit a `go-lint` failure: `golangci-lint config verify` failed with `context deadline exceeded` fetching `https://golangci-lint.run/jsonschema/golangci.v2.0.jsonschema.json` — an external network timeout, unrelated to `docs/data/orch/orch-state.json` (the only file that commit touched) and unrelated to any of the 3 gates this row's `verification_gate` covers. Confirmed transient, not a real regression, via `gh run rerun 32594771832 --failed` — passed clean on retry; final head fully green across every job.

### AC gate (`verification_gate: dry_run_measured_then_ci_green`)
- AC1 (measure before widening): satisfied pre-implementation by architect (~19.5-21s combined, ~4.5x margin under the 90s commit-mutex TTL).
- AC2 (cheap-vs-tsc): confirmed — 3 checks total <1s wall-clock observed live during this push, tsc (~94s) unaffected.
- AC3 (fail loud, no silent fail-open on docs-only push): confirmed via direct diff read — unconditional block, not regex-gated.
- AC4 (dry-run + subsequent green CI, not self-assertion): dry-run was already recorded by developer/architect; the "subsequent green CI" half is what this QA cycle supplied — confirmed on head `7ff1cf1e6` with job-level detail above.

**verification_gate: MET.** No blocking issues.
