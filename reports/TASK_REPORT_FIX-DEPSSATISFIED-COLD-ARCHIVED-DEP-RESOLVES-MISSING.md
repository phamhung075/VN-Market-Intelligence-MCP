## Task Report FIX-DEPSSATISFIED-COLD-ARCHIVED-DEP-RESOLVES-MISSING

**Commit (main, RAW-verified):** `0a56c46ae` — `git merge-base --is-ancestor` confirms it is on `main`'s ancestry. `git show --stat` confirms exactly the claimed file set: `scripts/lib/devteam-eligibility.jq`, new `scripts/lib/archive-glob-cat.sh`, `scripts/orch-cold-evict.sh`, `scripts/devteam-backlog-promote-bounded1.jq`, `scripts/devteam-backlog-promote-supervised-lane-sweep.jq`, `scripts/devteam-backlog-claim-ready-lane-consumer.jq`, `scripts/audits/devteam-dispatch-gate-satisfiability.sh`, `scripts/audits/devteam-bounded1-detail-disposition-gate-verify.sh`, `scripts/audits/devteam-bounded1-prose-sequencing-gate-verify.sh`, `docs/agents/dev-team/flow/main.md`, `docs/policies/dev-standards.md`, `docs/WORK.md`. No `apps/mcp-server/` files touched (confirmed via diff-stat) — `bun test`/`bun tsc --noEmit` correctly not run, not applicable.

### 1. Code read (not trusted from review_note prose)
- `dep_status_map($archive)` (`scripts/lib/devteam-eligibility.jq`): seeds an id→status map from `archive_status_map($archive)` (reduces over every monthly `docs/data/orch/archive/YYYY-MM.json`'s `.done_tasks[]`), then hot-lane entries (`backlog/ready/in_progress/qa/review/done/done_verified`) overwrite the archive seed — hot stays authoritative for a still-live id, archive is fallback-only for ids evicted entirely out of hot. `normalize_archive_status` matches `DONE_VERIFIED` case/separator-insensitively ONLY; every other status (`DONE`, `IN_PROGRESS`, `CANCELLED`, ...) passes through **unnormalized**, so it can never accidentally string-equal `"DONE_VERIFIED"` in `deps_satisfied`'s `all(. == "DONE_VERIFIED")` check — confirms the negative-control design intent (non-terminal cold status must stay UNSATISFIED).
- 0-arg `dep_status_map` is a literal proxy for `dep_status_map([])` — verified byte-for-byte zero behavior change for every pre-existing hot-only caller (jq arity overload, one merged implementation, not forked logic).
- `scripts/lib/archive-glob-cat.sh` (new): `find -maxdepth 1 -name '[0-9][0-9][0-9][0-9]-[0-9][0-9].json' | sort` + a `for` loop `cat`, deliberately avoiding the naive `<(cat docs/data/orch/archive/2026-*.json)` glob (documented failure mode: silent empty output under zsh's default no-word-split). Zero-match case outputs nothing → `$archive` becomes `[]` → `dep_status_map([])` behavior, confirmed safe.
- `scripts/orch-cold-evict.sh`'s referential eviction guard (`compute_id_maps`): computes `$referenced_dep_ids` as the union of `effective_depends_on(...)` across every row in every live `TASK_LANES` lane, then applies `select($referenced_dep_ids[.] != true)` as a real filter on `$all_terminal_done_ids_raw`, `$all_terminal_dv_ids_raw`, AND the flat-lane (`backlog/review/qa/in_progress/ready`) terminal-status candidates — read the exact lines (396-473, 533-551); this is genuine `select()`-based exclusion, not a report-only annotation. `guard_ref_held_ids`/`n_guard_ref_held` in the output are informational counters, separate from the actual removal maps.
- The 3 real `dep_status_map($archive)` callers (`devteam-backlog-promote-bounded1.jq`, `devteam-backlog-promote-supervised-lane-sweep.jq`, `devteam-backlog-claim-ready-lane-consumer.jq`) all thread `--slurpfile archive` the same documented way `--slurpfile detail` already is — grep-confirmed, not assumed.

### 2. Independent live re-run (RAW, not trusted from "re-run GREEN" prose)
Ran `scripts/audits/devteam-dispatch-gate-satisfiability.sh` live against the current board (not a fixture I built):
- **PASS** — AC-DEP-1 (live, dynamic): 0 live `effective_depends_on` ids resolve MISSING via `dep_status_map($archive)` despite being `DONE_VERIFIED` in cold archive (this row's own acceptance criterion, satisfied on today's live data, not just the 2026-07-28 measurement).
- **PASS** — AC-DEP-MECH (control + positive): archive-blind run (same real promote script, empty archive) does NOT promote a row depending on a real cold-archived id; archive-fed run DOES — proves the mechanism actually FIRES, not just that the predicate resolves in isolation (the exact failure class the row's own acceptance text warns against, citing `bounded1-supervised-lane-report.sh` shipping green while dead).
- **PASS** — AC-DEP-NEG-A: a dep-id existing NOWHERE (hot or cold) stays UNSATISFIED.
- **PASS** — AC-DEP-NEG-B: a dep-id cold-archived with a NON-terminal status (`IN_PROGRESS`) stays UNSATISFIED.
- **PASS** — AC-EVICT-1/2/3: a real `orch-cold-evict.sh` run against a scratch `ORCH_STATE`/`ARCHIVE_DIR` fixture (never touches the live file) completes cleanly; a still-referenced row is HELD in hot (not evicted); an unreferenced sibling row IS evicted normally — guard proven both directions.

Also re-ran 2 of the claimed "4 sibling audit scripts re-run GREEN": `devteam-bounded1-prose-sequencing-gate-verify.sh` (exit 0) and `devteam-bounded1-detail-disposition-gate-verify.sh` (exit 0) — both GREEN, matching claim.

### 3. Unrelated pre-existing drift found (NOT this row's defect — reported, not blocking)
- `devteam-dispatch-gate-satisfiability.sh`: 1 FAIL — `"BOUNDED-1 correctly no-ops when in_progress>=1"`. Root cause: the LIVE `ready[]` lane currently carries 2 rows already stamped `promoted_by:"dev-team (bounded-1 auto-pickup)"` from earlier promote cycles that were never claimed (`FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT`, `FIX-JANITOR-HEALTHRECHECK-IDEMPOTENCY-GUARD-MISSES-PROCESSED-DIR`) — `scripts/devteam-backlog-claim-bounded1.jq` claims the first one it finds unconditionally, without independently re-checking current WIP. This file is **not in this commit's diff-stat** (confirmed via `git show --stat`), so this commit cannot have caused it — it is board-state drift accumulated since the commit's 2026-07-28 landing.
- `bounded1-supervised-lane-report.sh`: 1 FAIL — 4 backlog rows (`FIX-ORCHSTATE-SIGNALQUEUE-UNCOMMITTED-ROWS-LOST-TO-PEER-FULLDOC-WRITE`, `FIX-PUSH-DELIVERY-ERROR-RATE-ALERT`, `FIX-RAG-COMPACTION-DISK-AMPLIFICATION`, `FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE`) now genuinely lack a resolvable dispatch lane. This script is also absent from this commit's diff-stat.

Both findings are noted in the row's own `status_note` and reported to the `bug` Telegram channel; neither touches `dep_status_map`/archive/eviction-guard logic and neither blocks this row's own AC.

### Verdict
**APPROVED, DONE_VERIFIED.**

Every AC this row's own acceptance criteria states (zero live leaks, archive-blind-vs-fed mechanism-fires proof, both negative controls, eviction-guard refusal proven both directions, extended the existing instrument rather than minting a new one) independently reproduced live, not trusted from the developer's self-report. `apps/mcp-server/` untouched, so `bun test`/`tsc`/`mock-guard` are structurally not applicable — confirmed, not assumed.

`task_board.qa[]` → `task_board.done_verified[]` via `jq` + `scripts/orch-apply.sh` (qa 9→8, done_verified 11→12, conservation OK: task_total 791→791). `.head` untouched (pointed at unrelated `DEFLAKE-VNSTOCK-3STATEMENT`). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S46.
