# TECH-1408: fix(tool-diacritics) — MCP tool Vietnamese diacritics

status: APPROVED_BY_ARCHITECT
req_ref: REQ-1408
sprint: 144

## Brownfield Impact

- Files modified: `src/interface/mcp/tools/kinhDichTools.ts`, `src/interface/mcp/tools/technicalIndicatorTools.ts`, `src/interface/mcp/tools/supplyChainTools.ts`
- Files created: `src/__tests__/1408-tool-diacritics.test.ts`
- Files deleted: none
- Breaking changes: no — only string content and export additions; existing call sites unaffected

## Architecture Decision

All three files are in the `interface` layer and contain user-facing output strings only. The fix is a string-only replacement with two export additions (`formatKinhDichTradingContext`, `formatTaIndicatorReport`) needed solely for testability. No domain logic, no DB queries, no cross-layer concerns. Pattern matches Sprint 142 task 1405 (convictionScorer diacritics fix).

## DDD Layer Plan

| Component | Layer | File Path | New/Modify |
|---|---|---|---|
| `formatKinhDichTradingContext` | interface | `src/interface/mcp/tools/kinhDichTools.ts` | MODIFY (extract + export) |
| `formatTaIndicatorReport` | interface | `src/interface/mcp/tools/technicalIndicatorTools.ts` | MODIFY (re-export alias + string fix) |
| `buildSupplyChainExposureOutput` strings | interface | `src/interface/mcp/tools/supplyChainTools.ts` | MODIFY (string fix only) |
| TDD RED test | test | `src/__tests__/1408-tool-diacritics.test.ts` | NEW |

## Interface Contracts

### New export — kinhDichTools.ts

Extract lines 1031-1040 (trading context block) into a standalone exported function placed **before** `registerKinhDichTools` (i.e. at file top-level, after existing exports). The inline call site is replaced with a call to the new function.

```typescript
// src/interface/mcp/tools/kinhDichTools.ts  (new export, ~line 515)
export function formatKinhDichTradingContext(trend: string): string {
  const t = trend.toUpperCase();
  if (t.includes("THUAN LOI") || t.includes("THUẬN LỢI")) {
    return "Nhận định giao dịch: Thuận lợi cho giao dịch — xu hướng tích cực";
  } else if (t.includes("BAT LOI") || t.includes("BẤT LỢI")) {
    return "Nhận định giao dịch: Bất lợi cho giao dịch — cẩn thận";
  } else {
    return "Nhận định giao dịch: Trung tính — cần xem thêm tín hiệu khác";
  }
}
```

Call site replacement (lines 1030-1040 in original):

```typescript
// replace inline block with:
lines.push(formatKinhDichTradingContext(data.state.trend));
```

### New export — technicalIndicatorTools.ts

Add an exported alias after the existing `formatReport` function definition (line 125, ends at line 245):

```typescript
// directly after the closing brace of formatReport (~line 245)
export const formatTaIndicatorReport = formatReport;
```

String fixes inside `formatReport` (no signature change):

| Line | Old | New |
|---|---|---|
| 150 | `"MA50=N/A (can 50 nen)"` | `"MA50=N/A (cần 50 nến)"` |
| 155 | `"sap xep hon hop"` | `"sắp xếp hỗn hợp"` |
| 156 | `` `→ Xu huong ${maSig}` `` | `` `→ Xu hướng ${maSig}` `` |
| 164 | `"RSI(14): N/A (can toi thieu 15 nen)"` | `"RSI(14): N/A (cần tối thiểu 15 nến)"` |

### String fixes — supplyChainTools.ts

`buildSupplyChainExposureOutput` is already exported (line 43). No structural change.

| Line | Old | New |
|---|---|---|
| 106 | `"Tin hieu co phieu: Khong co tin hieu dang ke (chi so vận tai trong ngưỡng binh thuong)"` | `"Tín hiệu cổ phiếu: Không có tín hiệu đáng kể (chỉ số vận tải trong ngưỡng bình thường)"` |
| 115 | `"TONG KET: Co tin hieu QUAN TRONG - theo doi chat chat HPG/GMD/VNM"` | `"TỔNG KẾT: Có tín hiệu QUAN TRỌNG — theo dõi chặt chẽ HPG/GMD/VNM"` |
| 117 | `"TONG KET: Phat hien tin hieu nhe - theo doi them"` | `"TỔNG KẾT: Phát hiện tín hiệu nhẹ — theo dõi thêm"` |
| 119 | `"TONG KET: Chuoi cung ung on dinh - khong co tin hieu bat thuong"` | `"TỔNG KẾT: Chuỗi cung ứng ổn định — không có tín hiệu bất thường"` |

## Task Breakdown

| Task | Description | Dependency |
|---|---|---|
| 1408 | Write `src/__tests__/1408-tool-diacritics.test.ts` — 5 RED test cases | none |
| 1409 | Extract `formatKinhDichTradingContext`, alias `formatTaIndicatorReport`, fix all strings in three files | 1408 must be RED first |

## Confirmed Line Numbers (verified by Architect brownfield scan 2026-04-18)

### kinhDichTools.ts

| Lines | Content |
|---|---|
| 1031-1038 | trading-context if/else block (inline, to be extracted) |
| 1040 | `lines.push(\`Nhan dinh giao dich: ${tradingContext}\`)` — replaced by call to new function |

### technicalIndicatorTools.ts

| Line | Content |
|---|---|
| 125 | `function formatReport(...)` — unexported, to gain alias |
| 150 | `"MA50=N/A (can 50 nen)"` |
| 155 | `"sap xep hon hop"` |
| 156 | `` `→ Xu huong ${maSig}` `` |
| 164 | `"RSI(14): N/A (can toi thieu 15 nen)"` |
| 245 | closing `}` of `formatReport` — alias export added after this line |

### supplyChainTools.ts

| Line | Content |
|---|---|
| 43 | `export function buildSupplyChainExposureOutput(...)` — already exported |
| 106 | mixed-accent "Tin hieu co phieu..." |
| 115 | "TONG KET: Co tin hieu..." |
| 117 | "TONG KET: Phat hien..." |
| 119 | "TONG KET: Chuoi cung ung..." |

## Test File Specification — src/__tests__/1408-tool-diacritics.test.ts

```
process.env["DB_PATH"] = ":memory:"; // isolation — must be first line

imports:
  - formatKinhDichTradingContext from "../interface/mcp/tools/kinhDichTools.js"
  - formatTaIndicatorReport from "../interface/mcp/tools/technicalIndicatorTools.js"
  - buildSupplyChainExposureOutput from "../interface/mcp/tools/supplyChainTools.js"
  - DailyCandle from "../domain/services/technicalIndicators.js"

helpers (inline in test file):
  - flatCandles(n, price=100): DailyCandle[] — same pattern as 1302 test
  - risingCandles(n, start=100, step=1): DailyCandle[] — same pattern

test cases (5):
  1. formatKinhDichTradingContext("THUAN LOI")
       contains: "Thuận lợi", "tích cực"
       not contains: "THUAN LOI", "tich cuc"

  2. formatKinhDichTradingContext("BAT LOI")
       contains: "Bất lợi", "cẩn thận"
       not contains: "BAT LOI", "can than"

  3. formatKinhDichTradingContext("NEUTRAL")
       contains: "Trung tính", "xem thêm"
       not contains: "TRUNG TINH", "xem them"

  4a. formatTaIndicatorReport("VCB", flatCandles(60), 60)
        contains: "sắp xếp hỗn hợp"
        not contains: "sap xep hon hop", "Xu huong"

  4b. formatTaIndicatorReport("VCB", flatCandles(49), 60)
        contains: "cần 50 nến"
        not contains: "can 50 nen"

  4c. formatTaIndicatorReport("VCB", flatCandles(60), 60)
        contains: "Xu hướng"
        not contains: "Xu huong"

  4d. formatTaIndicatorReport("VCB", flatCandles(14), 60)  — hits RSI N/A via short set (< 35 → guard fires, out of scope; use 40 candles for RSI N/A with MA valid)
      NOTE: use flatCandles(40) to pass MA (needs 50) but fail RSI (needs 15) — wait: RSI needs 15, flatCandles(40) has 40 so RSI is valid.
      Use flatCandles(14) → hits insufficient-data guard (< 35), does NOT reach RSI block.
      CORRECT approach for "cần tối thiểu 15 nến": pass exactly 35 candles (passes guard), but RSI needs 15; 35 candles → RSI is valid.
      Use flatCandles(36) — passes guard (≥35), but ma50 is null (needs 50), RSI is valid (≥15).
      For RSI N/A: need candles >= 35 (guard) AND candles < 15 for RSI — impossible simultaneously.
      RESOLUTION per REQ note: "When called with 14 candles (below 35 guard): output does not reach MA block (guard fires first — out of scope)".
      RSI N/A branch (line 164) is reachable only when candles ≥ 35 AND < 15 RSI lookback — contradiction.
      ARCHITECT FINDING: line 164 ("cần tối thiểu 15 nến") is unreachable in practice because the guard at line 130 requires ≥35 candles which always satisfies the RSI 15-period requirement.
      → Test case 4d: skip RSI N/A path. Assert line 164 string fix via code review only (no test case); cover via 4a–4c.

  5a. buildSupplyChainExposureOutput([], [], null)
        contains: "TỔNG KẾT", "ổn định"
        not contains: "TONG KET", "on dinh"

  5b. buildSupplyChainExposureOutput([], [{severity:"high", ...}], null)
        contains: "QUAN TRỌNG", "theo dõi chặt chẽ"
        not contains: "TONG KET", "theo doi chat chat"

  5c. buildSupplyChainExposureOutput([], [], null) — also confirms line 106
        contains: "Tín hiệu cổ phiếu: Không có tín hiệu đáng kể"
        not contains: "Tin hieu co phieu: Khong"
```

**Note on SupplyChainSignal shape:** Dev must inspect `SupplyChainSignal` type in `src/domain/services/supplyChainAnalyzer.ts` to build a minimal valid stub for test case 5b.

## Risk Assessment

| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| RSI N/A branch (line 164) unreachable in test | High | Low | Document as code-review-only; string fix still ships in 1409 |
| `formatKinhDichTradingContext` extraction breaks `explain_hexagram` | Low | Medium | Inline call replaced exactly; `data.state.trend` passed as-is |
| `toUpperCase()` in extracted function vs. original `toUpperCase()` at line 1030 | Low | Medium | Extracted function does its own `.toUpperCase()` — remove the inline call site's `.toUpperCase()` at line 1030 OR pass the already-uppercased trend; Dev must ensure one `.toUpperCase()` call, not double |
| `formatTaIndicatorReport` alias export causes duplicate symbol | None | Low | Alias `const`, not re-declaration; no conflict |

## Security Review

- [ ] SQL parameterized? N/A — no new DB queries
- [ ] File paths validated? N/A
- [ ] External HTTP rate-limited? N/A
- [ ] Secrets via Bun.env only? N/A — string-only change
