# TECH-1412: fix(diacritics-wave3) — Scheduler, Domain, Application Layer

status: APPROVED_BY_ARCHITECT
req_ref: REQ-1412
sprint: 146

## Brownfield Impact

- Files modified: 8 source files (scheduler × 2, application × 1, domain × 5)
- Files created: `src/__tests__/1412-diacritics-wave3.test.ts`
- Files deleted: none
- Breaking changes: no — pure string replacement; all function signatures and return types unchanged

## Architecture Decision

Same TDD pattern as Sprint 144–145. All 8 files already export the functions under test — no new exports needed. `buildTelegramMessage` and `sendCalibrationDigest` are private scheduler functions; test via public entry-point exports (`runPredictionMarketPoll` is not testable without DB, so `buildTelegramMessage` must be exported). `formatCrisisWarningOutput`, `computeSentimentTrend`, `formatReading`, `computeReading`, `classifyNguHanh`, `buildPositionLine`, `buildActionNote` are already exported. Minimal invasiveness: only string literals touched, no extraction or refactor.

## DDD Layer Plan

| Component | Layer | File Path | New/Modify |
|---|---|---|---|
| TDD RED test | test | `src/__tests__/1412-diacritics-wave3.test.ts` | NEW |
| buildTelegramMessage | interface/scheduler | `src/scheduler/predictionMarketJob.ts` | MODIFY (add export + string fix) |
| sendCalibrationDigest inline block | interface/scheduler | `src/scheduler/calibrationReportJob.ts` | MODIFY (string fix only) |
| formatCrisisWarningOutput | application | `src/application/usecases/getCrisisEarlyWarning.ts` | MODIFY (string fix only) |
| buildVietnameseSummary (via computeSentimentTrend) | domain | `src/domain/services/sentimentTrend.ts` | MODIFY (string fix only) |
| formatReading + HAO_STATE_LABELS + HAO_POSITION_LABELS | domain | `src/domain/services/kinhDich/kinhDichFormatter.ts` | MODIFY (string fix only) |
| computeReading — tradingSignalDisplay + actionNote + overallReading | domain | `src/domain/services/kinhDich/kinhDichReading.ts` | MODIFY (string fix only — display fields only) |
| classifyNguHanh — interpretation strings | domain | `src/domain/services/kinhDich/nguHanhClassifier.ts` | MODIFY (string fix only) |
| buildPositionLine + buildActionNote | domain | `src/domain/services/decisionNoteSynthesizer.ts` | MODIFY (string fix only + JSDoc comment) |

## Export Gap — Task 1412 Testability

`buildTelegramMessage` is currently `function` (not exported) in `predictionMarketJob.ts`.
Task 1412 must add `export` keyword before writing RED test. This is the only structural change required.

```typescript
// predictionMarketJob.ts line 235 — change:
function buildTelegramMessage(signal: PredictionSignal): string {
// to:
export function buildTelegramMessage(signal: PredictionSignal): string {
```

All other 7 functions are already exported — no structural change needed before RED test.

## Interface Contracts (unchanged — signatures reference only)

```typescript
// scheduler — needs export added
export function buildTelegramMessage(signal: PredictionSignal): string

// application — already exported
export function formatCrisisWarningOutput(
  result: Omit<CrisisEarlyWarningResult, "generatedAt">
): McpContentItem[]

// domain — already exported
export function computeSentimentTrend(
  db: Database, code: string, windowDays?: number
): SentimentTrend        // .summary field carries accented strings

export function formatReading(reading: KinhDichReading): string

export function computeReading(
  stockCode: string, queChinhNumber: number,
  queData: QueData, trendRaw: string,
  haoStates: HaoState[], markovData?: MarkovData | null
): KinhDichReading       // .actionNote + .overallReading + .queChiNh.tradingSignal

export function classifyNguHanh(
  lowerElement: NguHanh, upperElement: NguHanh
): NguHanhResult         // .interpretation field

export function buildPositionLine(input: PositionLineInput | null): string
export function buildActionNote(input: ActionNoteInput): string
```

## String Inventory by File (exact replacements for Task 1413)

### FILE 1: `src/scheduler/predictionMarketJob.ts` — `buildTelegramMessage()`

| Current | Correct |
|---------|---------|
| `"NGHIEM TRONG"` | `"NGHIÊM TRỌNG"` |
| `"QUAN TRONG"` | `"QUAN TRỌNG"` |
| `"LUU Y"` | `"LƯU Ý"` |
| `` `dich chuyen ${...}%` `` | `` `dịch chuyển ${...}%` `` |
| `` `Thi truong: ` `` | `` `Thị trường: ` `` |
| `` `Loai tin hieu: ` `` | `` `Loại tín hiệu: ` `` |
| `` `Xac suat YES: ` `` | `` `Xác suất YES: ` `` |
| `` `Khoi luong 24h: ` `` | `` `Khối lượng 24h: ` `` |
| `` `Do tin cay: ` `` | `` `Độ tin cậy: ` `` |
| `` `Phan tich: ` `` | `` `Phân tích: ` `` |

### FILE 2: `src/scheduler/calibrationReportJob.ts` — `sendCalibrationDigest()` MARKET block

| Current | Correct |
|---------|---------|
| `"Brier Score trung binh:"` | `"Brier Score trung bình:"` |
| `` `Xu huong: ${trend}` `` | `` `Xu hướng: ${trend}` `` |

Do NOT touch lines 301–338 (WORK channel) or already-accented lines 343–344, 357–367.

### FILE 3: `src/application/usecases/getCrisisEarlyWarning.ts` — `formatCrisisWarningOutput()`

| Current | Correct |
|---------|---------|
| `"=== RADAR KHUNG HOANG — Crisis Early Warning ==="` | `"=== RADAR KHỦNG HOẢNG — Crisis Early Warning ==="` |
| `` `Thoi gian: ` `` | `` `Thời gian: ` `` |
| `"--- CHI SO CANH BAO KHUNG HOANG ---"` | `"--- CHỈ SỐ CẢNH BÁO KHỦNG HOẢNG ---"` |
| `"Khong co tin hieu khung hoang nao duoc phat hien."` | `"Không có tín hiệu khủng hoảng nào được phát hiện."` |
| `` `[CANH BAO] ` `` | `` `[CẢNH BÁO] ` `` |
| `` `nguon tin` `` | `` `nguồn tin` `` |
| `"--- UY TIN DOANH NGHIEP (score < 50) ---"` | `"--- UY TÍN DOANH NGHIỆP (score < 50) ---"` |
| `"Tat ca co phieu co diem uy tin an toan."` | `"Tất cả cổ phiếu có điểm uy tín an toàn."` |
| `` `xu huong=` `` | `` `xu hướng=` `` |

### FILE 4: `src/domain/services/sentimentTrend.ts` — `buildVietnameseSummary()`

| Current | Correct |
|---------|---------|
| `"khong co du lieu cam tinh trong"` + `"ngay qua."` | `"không có dữ liệu cảm tính trong"` + `"ngày qua."` |
| `"co xu huong TICH CUC"` | `"có xu hướng TÍCH CỰC"` |
| `"co xu huong TIEU CUC"` | `"có xu hướng TIÊU CỰC"` |
| `"ON DINH"` | `"ỔN ĐỊNH"` |
| `"ngay qua "` (template line 208) | `"ngày qua "` |

### FILE 5: `src/domain/services/kinhDich/kinhDichFormatter.ts`

HAO_STATE_LABELS:

| Key | Current | Correct |
|-----|---------|---------|
| `LAO_DUONG` | `"Lao Duong ⚡ (qua mua, sap dao chieu)"` | `"Lão Dương ⚡ (quá mua, sắp đảo chiều)"` |
| `THIEU_DUONG` | `"Thieu Duong (tang on dinh)"` | `"Thiếu Dương (tăng ổn định)"` |
| `THIEU_AM` | `"Thieu Am (giam on dinh)"` | `"Thiếu Âm (giảm ổn định)"` |
| `LAO_AM` | `"Lao Am ⚡ (qua ban, sap hoi phuc)"` | `"Lão Âm ⚡ (quá bán, sắp hồi phục)"` |

HAO_POSITION_LABELS (preserve trailing spaces for column alignment):

| Key | Current | Correct |
|-----|---------|---------|
| `1` | `"So hao   (Tam ly)"` | `"Sơ hào   (Tâm lý)"` |
| `2` | `"Nhi hao  (Co ban)"` | `"Nhị hào  (Cơ bản)"` |
| `3` | `"Tam hao  (Gia)   "` | `"Tam hào  (Giá)   "` |
| `4` | `"Tu hao   (Ngoai) "` | `"Tứ hào   (Ngoại) "` |
| `5` | `"Ngu hao  (Nganh) "` | `"Ngũ hào  (Ngành) "` |
| `6` | `"Thuong hao (Vi mo)"` | `"Thượng hào (Vĩ mô)"` |

`formatReading()` body:

| Current | Correct |
|---------|---------|
| `` `Que chinh: ` `` | `` `Quẻ chính: ` `` |
| `` `  Xu huong: ` `` | `` `  Xu hướng: ` `` |
| `` `  Tin hieu: ` `` + `` `Do tin cay: ` `` | `` `  Tín hiệu: ` `` + `` `Độ tin cậy: ` `` |
| `"6 Hao:"` | `"6 Hào:"` |
| `"bien chuyen, su thay doi dang den"` | `"biến chuyển, sự thay đổi đang đến"` |
| `"Hao dong: Khong co hao dong — que on dinh, xu huong hien tai ben vung"` | `"Hào động: Không có hào động — quẻ ổn định, xu hướng hiện tại bền vững"` |
| `` `Ho que (an sau): ` `` | `` `Hộ quẻ (ẩn sâu): ` `` |
| `` `Bien que (tuong lai): ` `` | `` `Biến quẻ (tương lai): ` `` |
| `` `Ngu Hanh: ` `` | `` `Ngũ Hành: ` `` |
| `` `  Diem ngu hanh: ` `` | `` `  Điểm ngũ hành: ` `` |
| `` `Markov: Xac suat chuyen sang ` `` | `` `Markov: Xác suất chuyển sang ` `` |
| `"Hao dong: "` (prefix in changing lines block) | `"Hào động: "` |

### FILE 6: `src/domain/services/kinhDich/kinhDichReading.ts` — `computeReading()` display fields ONLY

**DO NOT TOUCH:** `OUTCOME_SCORES_ASCII` keys, `TREND_SCORE_MAP` keys, `TIEN_KEYWORDS` / `LUI_KEYWORDS` / `GIU_KEYWORDS` / `CHO_KEYWORDS` / `THAN_KEYWORDS`, `extractAction()` return values.

| Current | Correct |
|---------|---------|
| `"tich cuc"` (in tradingSignalDisplay) | `"tích cực"` |
| `"tieu cuc"` (in tradingSignalDisplay) | `"tiêu cực"` |
| `"tich cuc"` (scoreSign) | `"tích cực"` |
| `"tieu cuc"` (scoreSign) | `"tiêu cực"` |
| `` `KHUYEN NGHI: ` `` | `` `KHUYẾN NGHỊ: ` `` |
| `` `Que ` `` (in actionNote template) | `` `Quẻ ` `` |
| `` `, xu huong: ` `` | `` `, xu hướng: ` `` |
| `` `. Do tin cay: ` `` | `` `. Độ tin cậy: ` `` |
| `"bien chuyen sang"` (changingDesc) | `"biến chuyển sang"` |
| `"Khong co hao dong — que on dinh."` | `"Không có hào động — quẻ ổn định."` |
| `"Xu huong: "` (overallReading) | `"Xu hướng: "` |
| `"Ho que (an sau): "` (overallReading) | `"Hộ quẻ (ẩn sâu): "` |
| `"Ngu Hanh: "` (overallReading) | `"Ngũ Hành: "` |
| `"Hao dong: "` (changingDesc prefix) | `"Hào động: "` |

### FILE 7: `src/domain/services/kinhDich/nguHanhClassifier.ts` — `classifyNguHanh()`

| Current | Correct |
|---------|---------|
| `"Cung hanh — tang cuong xu huong hien tai"` | `"Cùng hành — tăng cường xu hướng hiện tại"` |
| `"Noi luc sinh ngoai luc — xu huong ben vung"` | `"Nội lực sinh ngoại lực — xu hướng bền vững"` |
| `"Ngoai luc ho tro noi luc — moi truong thuan loi"` | `"Ngoại lực hỗ trợ nội lực — môi trường thuận lợi"` |
| `"Noi luc khac ngoai luc — mau thuan noi tai"` | `"Nội lực khắc ngoại lực — mâu thuẫn nội tại"` |
| `"Ngoai luc triet tieu noi luc — ap luc ben ngoai"` | `"Ngoại lực triệt tiêu nội lực — áp lực bên ngoài"` |
| `"Trung tinh — khong tuong tac dac biet"` | `"Trung tính — không tương tác đặc biệt"` |

### FILE 8: `src/domain/services/decisionNoteSynthesizer.ts`

`buildPositionLine()`:

| Current | Correct |
|---------|---------|
| `"CHUA CO VI THE"` (return line 63) | `"CHƯA CÓ VỊ THẾ"` |
| `` `Vi the: ${sharesStr} co phieu @ ...` `` | `` `Vị thế: ${sharesStr} cổ phiếu @ ... \| Giá hiện tại: ... \| Lãi/Lỗ: ` `` |
| JSDoc comment line 57 example string | Fix to match corrected output string |

`buildActionNote()`:

| Current | Correct |
|---------|---------|
| `"Khuyen nghi: CHUA CO VI THE"` | `"Khuyến nghị: CHƯA CÓ VỊ THẾ"` |
| `"Khuyen nghi: GIAM BOT"` (×2) | `"Khuyến nghị: GIẢM BỚT"` |
| `"Khuyen nghi: THEM VAO"` | `"Khuyến nghị: THÊM VÀO"` |
| `"Khuyen nghi: XEM XET GIAM"` | `"Khuyến nghị: XEM XÉT GIẢM"` |
| `"Khuyen nghi: GIU NGUYEN"` | `"Khuyến nghị: GIỮ NGUYÊN"` |

## Task Breakdown

| Task | Title | Layer | Depends on |
|------|-------|-------|-----------|
| 1412 | RED test — 8 file assertions | test | add `buildTelegramMessage` export first |
| 1413 | Fix 8 files per inventory above | scheduler + application + domain | 1412 RED + merged |

## Risk Assessment

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|-----------|
| `buildTelegramMessage` not exported → test can't import | High | High | Export added in task 1412 before test written |
| ASCII lookup guards in `kinhDichReading.ts` accidentally touched | Medium | High | REQ explicit: lines 67–83 and `extractAction()` returns are frozen |
| HAO_POSITION_LABELS trailing spaces removed → column misalignment | Low | Medium | REQ explicit: preserve exact trailing space padding |
| `calibrationReportJob.ts` WORK channel lines accidentally touched | Low | Medium | Only lines 342–370 in scope; lines 301–338 explicitly excluded |
| Full suite regression from prior sprints | Low | High | AC-3 requires Sprint 144+145 test files remain GREEN |

## Security Review

- SQL parameterized: N/A — no DB queries in changed files
- File paths validated: N/A
- External HTTP rate-limited: N/A
- Secrets via Bun.env only: N/A
