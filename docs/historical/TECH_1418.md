# TECH-1418: fix(diacritics-wave6)

status: APPROVED_BY_ARCHITECT
req_ref: REQ-1418

## Brownfield Impact

- Files modified: 4
  - `src/interface/mcp/tools/customAlertTools.ts`
  - `src/interface/mcp/tools/changelogTools.ts`
  - `src/interface/mcp/tools/targetAllocationTools.ts`
  - `src/application/usecases/exportPortfolioSnapshot.ts`
- Files created: 1
  - `src/__tests__/1418-diacritics-wave6.test.ts`
- Files deleted: none
- Breaking changes: no

## Architecture Decision

Same pattern as waves 1-5 (tasks 1408–1417): runtime output strings only, no logic change. `customAlertTools`, `changelogTools`, `targetAllocationTools` export no pure functions for the affected strings — they are embedded in MCP handler closures or private helpers — so Group B source-scan (readFileSync) is the correct test strategy. `exportPortfolioSnapshot.ts` exposes `exportPortfolioSnapshot()` directly, enabling Group A direct-call testing. test-190 line 254 asserts the old sentinel `"khong the ghi file"` — it must be updated atomically in task 1419 alongside the fix to line 211.

## DDD Layer Plan

| Component | Layer | File Path | New/Modify |
|-----------|-------|-----------|------------|
| RED test suite | test | `src/__tests__/1418-diacritics-wave6.test.ts` | NEW |
| buildRuleListText strings | interface | `src/interface/mcp/tools/customAlertTools.ts` | MODIFY (task 1419) |
| list_alert_rules handler error string | interface | `src/interface/mcp/tools/customAlertTools.ts` | MODIFY (task 1419) |
| log_fix handler error string | interface | `src/interface/mcp/tools/changelogTools.ts` | MODIFY (task 1419) |
| get_recent_fixes handler strings | interface | `src/interface/mcp/tools/changelogTools.ts` | MODIFY (task 1419) |
| get_target_allocation handler strings | interface | `src/interface/mcp/tools/targetAllocationTools.ts` | MODIFY (task 1419) |
| SnapshotResult.error sentinel | application | `src/application/usecases/exportPortfolioSnapshot.ts` | MODIFY (task 1419) |
| Existing snapshot test sentinel | test | `src/__tests__/190-export-snapshot.test.ts` | MODIFY (task 1419) |

## Interface Contracts

No interface signature changes. `SnapshotResult.error` type stays `string | undefined`. Only the string value changes:

```
"(khong the ghi file): ${msg}"  →  "(không thể ghi file): ${msg}"
```

Callers checking `.error` by presence (`.toBeTruthy()`) are unaffected. Only callers checking `.toContain("khong the ghi file")` break — exactly test-190 line 254.

## Test Design: 1418-diacritics-wave6.test.ts

### Group A — direct-call (exportPortfolioSnapshot only)

| Test | Method | Assertion |
|------|--------|-----------|
| error sentinel correct | `exportPortfolioSnapshot({ db, exportDir: "/root/no-permission" })` | `result.error` starts with `"(không thể ghi file):"` |
| old sentinel absent | same result | `result.error` does NOT contain `"khong the ghi file"` |

### Group B — source-scan (readFileSync on 3 handler files)

**customAlertTools.ts** (3 strings):

| Positive assert | Negative guard |
|-----------------|----------------|
| contains `"Chưa có quy tắc cảnh báo tùy chỉnh nào."` | not contains `"Chua co quy tac canh bao tuy chinh nao."` |
| contains `` "Quy tắc cảnh báo tùy chỉnh (${active} đang hoạt động / ${total} tổng cộng)" `` | not contains `"Quy tac canh bao tuy chinh"` |
| contains `` "Lỗi khi lấy danh sách quy tắc:" `` | not contains `"Loi khi lay danh sach quy tac:"` |

**changelogTools.ts** (4 strings):

| Positive assert | Negative guard |
|-----------------|----------------|
| contains `` "Lỗi khi ghi sửa lỗi:" `` | not contains `"Loi khi ghi sua loi:"` |
| contains `"Chưa có sửa lỗi nào được ghi lại."` | not contains `"Chua co sua loi nao duoc ghi lai."` |
| contains `` "sửa lỗi gần nhất (mới nhất trước)" `` | not contains `"sua loi gan nhat (moi nhat truoc)"` |
| contains `` "Lỗi khi đọc lịch sử sửa lỗi:" `` | not contains `"Loi khi doc lich su sua loi:"` |

**targetAllocationTools.ts** (9 strings):

| Positive assert | Negative guard |
|-----------------|----------------|
| contains `"Chưa có mục tiêu phân bổ."` | not contains `"Chua co muc tieu phan bo."` |
| contains `"Dùng set_target_allocation để thiết lập mục tiêu."` | not contains `"Dung set_target_allocation de thiet lap muc tieu."` |
| contains `"Phân bổ mục tiêu"` | not contains `"Phan bo muc tieu"` |
| contains `` `pad("Mã",` `` | not contains `` `pad("Ma",` `` |
| contains `"(đúng)"` | not contains `"(dung)"` |
| contains `"(thừa)"` | not contains `"(thua)"` |
| contains `"(thiếu)"` | not contains `"(thieu)"` |
| contains `"(không có mục tiêu)"` | not contains `"(khong co muc tieu)"` |
| contains `` "Tổng danh mục:" `` | not contains `"Tong danh muc:"` |
| contains `` "Lưu ý: Giá ước tính (giá vốn) cho:" `` | not contains `"Luu y: Gia uoc tinh"` |
| contains `"Lưu ý: Chưa có vị trí nào trong danh mục"` | not contains `"Luu y: Chua co vi tri nao"` |
| contains `` "Lỗi khi đọc phân bổ mục tiêu:" `` | not contains `"Loi khi doc phan bo muc tieu:"` |

## Atomic Coupling: exportPortfolioSnapshot + test-190

Task 1419 must change both in one commit:

| File | Line | Old | New |
|------|------|-----|-----|
| `exportPortfolioSnapshot.ts` | 63 (JSDoc) | `"(khong the ghi file)"` | `"(không thể ghi file)"` |
| `exportPortfolioSnapshot.ts` | 211 | `(khong the ghi file): ${msg}` | `(không thể ghi file): ${msg}` |
| `190-export-snapshot.test.ts` | 254 | `.toContain("khong the ghi file")` | `.toContain("không thể ghi file")` |

## Task Breakdown

| Task | Depends on | Action |
|------|-----------|--------|
| 1418 | — | Write `src/__tests__/1418-diacritics-wave6.test.ts`. All assertions RED before fix. |
| 1419 | 1418 RED | Fix 4 source files + update test-190 line 254. All 1418 assertions go GREEN. |

## Risk Assessment

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|------------|
| test-190 line 254 not updated in task 1419 | Medium | High | Architect spec mandates atomic coupling; Dev must update both files in same commit |
| Negative guards fire on already-fixed strings | Low | Low | Source scan confirms all 4 files still carry broken strings before fix |
| `pad()` column alignment broken by diacritic chars | Very Low | Low | JS `.length` counts code points, not bytes; diacritics do not change length |

## Security Review

- SQL parameterized? N/A (no SQL changes)
- File paths validated? N/A (no path changes)
- External HTTP rate-limited? N/A
- Secrets via Bun.env only? N/A
