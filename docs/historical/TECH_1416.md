# TECH-1416: fix(diacritics-wave5) — Vietnamese diacritics in 12 interface tools + 1 domain service

status: APPROVED_BY_ARCHITECT
req_ref: REQ-1416

## Brownfield Impact

- Files modified: 13 (see DDD Layer Plan)
- Files created: 1 (`src/__tests__/1416-diacritics-wave5.test.ts`)
- Files deleted: none
- Breaking changes: no — string literal replacements only; existing tests that assert unaccented strings MUST be updated in same task 1416 (RED→GREEN after 1417 fix)

## Architecture Decision

Wave 5 follows identical pattern to waves 1–4: all changes are confined to string literals inside `content[].text`-bound paths at the interface layer (and one domain service helper). No interfaces, schemas, or DB queries are touched. The RED test uses a mixed strategy — source-scan (`readFileSync`) for files with no callable formatter, direct-call for files that export testable pure functions.

## DDD Layer Plan

| Component | Layer | File Path | New/Modify |
|---|---|---|---|
| alertMuteTools strings (6) | interface | `src/interface/mcp/tools/alertMuteTools.ts` | MODIFY |
| climateTools strings (1) | interface | `src/interface/mcp/tools/climateTools.ts` | MODIFY |
| compareTools strings (9) | interface | `src/interface/mcp/tools/compareTools.ts` | MODIFY |
| dataFreshnessTools strings (22) | interface | `src/interface/mcp/tools/dataFreshnessTools.ts` | MODIFY |
| earningsCalendarTools strings (10) | interface | `src/interface/mcp/tools/earningsCalendarTools.ts` | MODIFY |
| kinhDichTools strings (28) | interface | `src/interface/mcp/tools/kinhDichTools.ts` | MODIFY |
| pharmaTools strings (1) | interface | `src/interface/mcp/tools/pharmaTools.ts` | MODIFY |
| portfolioTools strings (2) | interface | `src/interface/mcp/tools/portfolioTools.ts` | MODIFY |
| sourceHealthTools strings (11) | interface | `src/interface/mcp/tools/sourceHealthTools.ts` | MODIFY |
| supplyChainTools strings (10) | interface | `src/interface/mcp/tools/supplyChainTools.ts` | MODIFY |
| technicalIndicatorTools strings (15) | interface | `src/interface/mcp/tools/technicalIndicatorTools.ts` | MODIFY |
| stockSearch strings (3) | domain | `src/domain/services/stockSearch.ts` | MODIFY |
| Wave5 RED test | test | `src/__tests__/1416-diacritics-wave5.test.ts` | NEW |

## Interface Contracts

No interface changes. All modifications are string literal values inside existing exported functions.

### Shared-function risk: dataFreshnessTools → systemTools

`classifyFreshness()` and `formatAge()` are exported from `dataFreshnessTools.ts` and imported by `systemTools.ts`. Their return values are asserted in:

| Test file | Assertions that break after wave5 fix |
|---|---|
| `src/__tests__/185-data-freshness.test.ts` | `toBe("Tot")`, `toBe("Binh thuong")`, `toBe("Cu")`, `toBe("Rat cu")`, `toBe("Chua co du lieu")` (9 assertions); `toContain("phut")`, `toContain("ngay")` (2); `toContain("Do tuoi du lieu")`, `toContain("Gia co phieu")`, `toContain("Hang hoa")`, `toContain("Ty gia SBV")`, `toContain("Du doan")`, `toContain("Chua co du lieu")`, `toContain("Tot")`, `toContain("Cu")`, `toContain("Rat cu")`, `toContain("Kiem tra luc")` |
| `src/__tests__/1208-price-freshness-vps-push.test.ts` | `toBe("Cu")` line 79, `toBe("Binh thuong")` line 80, `toContain("Chua co du lieu")` line 104 |
| `src/__tests__/1293-data-freshness-label.test.ts` | `toContain("Cu")` lines 96, 108; `toContain("Chua co du lieu")` line 117; `toContain("Tot")` line 126 |

**Dev rule**: Task 1416 RED test file must assert NEW accented forms. Task 1417 fixes source + updates all three legacy test files above so they pass.

### Shared-function risk: sourceHealthTools

`formatSourceHealthTable` and `STATUS_LABEL` are asserted in `210-source-health.test.ts`:

| Line | Current assertion | Must become |
|---|---|---|
| 230 | `toContain("Tinh trang nguon du lieu")` | `toContain("Tình trạng nguồn dữ liệu")` |
| 236 | `toContain("Suy giam")` | `toContain("Suy giảm")` |
| 237 | `toContain("Ngung")` | `toContain("Ngưng")` |
| 247 | `toContain("Chua co du lieu")` | `toContain("Chưa có dữ liệu")` |

### Shared-function risk: technicalIndicatorTools

`formatTaIndicatorReport` assertions in `1302-technical-indicators.test.ts`:

| Line | Current assertion | Must become |
|---|---|---|
| 398 | `toContain("Chi bao ky thuat")` | `toContain("Chỉ báo kỹ thuật")` |
| 435 | `toContain("[HPG] Khong du du lieu ky thuat")` | `toContain("[HPG] Không đủ dữ liệu kỹ thuật")` |
| 437 | `toContain("can toi thieu 35 cho MACD")` | `toContain("cần tối thiểu 35 cho MACD")` |
| 461 | `toContain("Khong du du lieu ky thuat")` | `toContain("Không đủ dữ liệu kỹ thuật")` |

Note: `1410-tool-diacritics-sweep.test.ts` line 141 asserts `.not.toContain("Khong du du lieu")` — this already passes and will continue to pass after fix (double-negative guard). No change needed there.

### earningsCalendarTools — column padding audit

`statusLabel()` returns: `ĐÃ NỘP` (6 chars), `QUÁ HẠN` (7), `SẮP ĐẾN` (7), `(ước tính)` (10). Column header line 194 uses fixed-width string `TRẠNG THÁI` (10 chars). Vietnamese diacritics are single-byte-display-width in terminal/MCP output — column padding is unchanged. **No padding adjustment needed.**

### kinhDichTools — 28 strings, confirmed scope

Waves 1–4 already fixed lines 524–529 (`formatKinhDichTradingContext`), 734, 808–809. Wave 5 targets 28 strings across lines: 568, 621, 676, 713, 723–724, 740, 754, 796, 806, 810, 819, 821, 841, 880, 922, 924–926, 935, 945, 951, 966, 996, 1018, 1021, 1024, 1028–1029, 1033, 1043, 1065, 1093. Do NOT touch the already-fixed lines.

## Test Strategy for Task 1416 (RED test)

Group files by callable export availability:

### Group A — direct-call (pure function exported, no DB needed)

| File | Function to call | Key assertion |
|---|---|---|
| `dataFreshnessTools.ts` | `classifyFreshness(0)` | `toBe("Tốt")` |
| `dataFreshnessTools.ts` | `classifyFreshness(null)` | `toBe("Chưa có dữ liệu")` |
| `dataFreshnessTools.ts` | `classifyFreshness(1)` | `toBe("Bình thường")` |
| `dataFreshnessTools.ts` | `classifyFreshness(6)` | `toBe("Cũ")` |
| `dataFreshnessTools.ts` | `classifyFreshness(24)` | `toBe("Rất cũ")` |
| `dataFreshnessTools.ts` | `formatAge(0.25)` | `toContain("phút")` |
| `dataFreshnessTools.ts` | `formatAge(48)` | `toContain("ngày")` |
| `sourceHealthTools.ts` | `formatSourceHealthTable([])` | `toContain("Chưa có dữ liệu")` |
| `stockSearch.ts` | `formatSearchResults("x", [])` | `toContain("Không tìm thấy kết quả nào")` |
| `technicalIndicatorTools.ts` | `formatTaIndicatorReport("VCB", flatCandles(10), 60)` | `toContain("Không đủ dữ liệu kỹ thuật")` |

### Group B — source-scan (`readFileSync` on `.ts` source)

Files with no easily-isolated pure formatter, or where the output requires heavy DB/MCP setup:

| File | Sample strings to scan |
|---|---|
| `alertMuteTools.ts` | `đã được tắt tiếng`, `Lý do:`, `Để bật lại cảnh báo` |
| `climateTools.ts` | `Lọc: ` |
| `compareTools.ts` | `Lỗi: Cần ít nhất 2 mã`, `So sánh cổ phiếu`, `Chỉ tiêu`, `Xác tín`, `Cập nhật:` |
| `earningsCalendarTools.ts` | `ĐÃ NỘP`, `QUÁ HẠN`, `SẮP ĐẾN`, `(ước tính)`, `LỊCH NỘP BCTC`, `CỔ PHIẾU` |
| `kinhDichTools.ts` | `không có trong watchlist`, `Lỗi khi đọc Kinh Dịch`, `LỊCH SỬ KINH DỊCH`, `XÁC SUẤT CHUYỂN QUẺ`, `BACKTEST KINH DỊCH`, `Độ chính xác`, `Lợi nhuận TB`, `Tỷ lệ thắng`, `Lưu ý: Backtest` |
| `pharmaTools.ts` | `Lọc theo cổ phiếu:` |
| `portfolioTools.ts` | `Watchlist trống`, `Ngành ` |
| `sourceHealthTools.ts` | `Tình trạng nguồn dữ liệu`, `Suy giảm`, `Ngưng`, `Chưa bao giờ`, `giây trước`, `phút trước`, `giờ trước`, `ngày trước` |
| `supplyChainTools.ts` | `PHÂN TÍCH CHUỖI CUNG ỨNG`, `CHỈ SỐ VẬN TẢI BIỂN`, `NGHIÊM TRỌNG`, `QUAN TRỌNG`, `LƯU Ý`, `THẤP`, `TÍN HIỆU TÁC ĐỘNG`, `Sai lệch:`, `TĂNG`, `GIẢM`, `TRUNG LẬP` |
| `technicalIndicatorTools.ts` (source-scan supplement) | `Quá mua`, `Quá bán`, `Trung tính`, `histogram dương`, `histogram âm`, `cần tối thiểu 34 nến`, `cần tối thiểu 20 nến`, `Lỗi tính chỉ báo`, `giá > MA5`, `giá < MA5`, `trung bình-thấp`, `thấp (cảnh oversold)`, `cao (cảnh overbought)` |

**Pattern**: source-scan tests use `expect(src).toContain("accented")` + `expect(src).not.toContain("unaccented")` pairs. Direct-call tests call the exported function and assert the return value.

## Task Breakdown (for PM)

| Task | Title | Type | Depends on |
|---|---|---|---|
| 1416 | `test(diacritics-wave5)`: RED test — 12 interface tools + 1 domain service | RED test | REQ-1416, TECH-1416 |
| 1417 | `fix(diacritics-wave5)`: replace all 118 unaccented strings + update legacy test assertions | Fix | 1416 |

**1417 must also update these legacy test files** (they assert old unaccented strings):
- `src/__tests__/185-data-freshness.test.ts` — 12+ assertions
- `src/__tests__/1208-price-freshness-vps-push.test.ts` — 3 assertions
- `src/__tests__/1293-data-freshness-label.test.ts` — 4 assertions
- `src/__tests__/210-source-health.test.ts` — 4 assertions
- `src/__tests__/1302-technical-indicators.test.ts` — 4 assertions

## Risk Assessment

| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| 185/1208/1293 tests break after wave5 fix (classifyFreshness return value changes) | High | High | Explicitly list all 23 assertion lines in TASK_1417.md; Dev updates them atomically |
| 210-source-health tests break after sourceHealthTools fix | High | Medium | List 4 assertion lines; Dev updates in same commit |
| 1302-technical-indicators tests break after technicalIndicatorTools fix | High | Medium | List 4 assertion lines; Dev updates in same commit |
| earningsCalendarTools column misalignment after diacritic addition | Low | Low | Vietnamese chars are single-display-width; no width change needed |
| kinhDichTools: wave4 already-fixed lines accidentally re-touched | Low | High | RED test must assert already-correct lines still present (do-not-break guard) |
| 1410-tool-diacritics-sweep.test.ts line 141 `.not.toContain("Khong du du lieu")` broken | None | None | Already asserts absence of unaccented; will still pass after fix |

## Security Review

- SQL parameterized? N/A — string literal changes only
- File paths validated? N/A
- External HTTP rate-limited? N/A
- Secrets via Bun.env only? N/A
