### REQ 1303i (cascade-gaps) — 2026-04-23

- **Feature**: Close 3 cascade gaps: Taiwan geo routing, BCTC overdue chain trigger, trade map 4 sectors
- **Blockers**: 0 — all decisions resolved from code inspection
- **Domain findings**:
  - `cascadeEngine.ts` has Hormuz supply-shock rules (line ~1194) but zero Taiwan/semiconductor entries. `COUNTRY_KEYWORDS` in `tradeRelationships.ts` has no `taiwan` key — `detectCountries()` blind to Taiwan events.
  - `bctcOverdueCheckJob.ts` inserts alert then returns `RunResult` — never calls `runImpactChain`. Application layer import is permitted from scheduler (precedented in `intelligenceCycleJob.ts`).
  - `TRADE_PROFILES` covers only 5 tickers (VNM/FPT/VCB/HPG/VEA). Four sectors (pharma/logistics/construction/steel) have no export seeds. DHG has China API import exposure (~40% cost) — high confidence china→pharma cascade.
  - DomainType has both `'pharma'` (legacy) and `'pharmaceutical'` (sprint 044). `SECTOR_RULES` uses both. Leave as-is — unification is separate task.
  - First-match-wins per domain in cascadeEngine means Taiwan de-escalation rules must be inserted BEFORE escalation block (~line 1398).
- **Status**: READY_FOR_ARCHITECT
