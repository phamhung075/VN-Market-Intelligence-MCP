# Fixer Session — 2026-04-25

## Task 1328e Fix (02:59–03:05)

**Issue**: notifyTelegramAlert was routing HIGH/CRITICAL alerts to MARKET channel instead of BUG channel, violating Alert Commander exclusivity rule in CLAUDE.md.

**Files**: `apps/mcp-server/src/infrastructure/notifiers/telegram.ts`
- Line 525: Comment said `sendTelegramMarket()`, corrected to `sendTelegramBug()`
- Line 552: Call changed from `sendTelegramMarket(text, sendOpts)` to `sendTelegramBug(text, sendOpts)`
- Line 553: Return type conversion added (number → boolean)

**Root Cause**: Server infrastructure schedulers (like intelligenceCycleJob) must never send to MARKET. Only Alert Commander (05-alert-commander.md) has exclusivity to MARKET channel.

**Pattern**: Channel routing violation — infrastructure layer breaking domain service boundaries

**Status**: Ready for QA re-review
- Test: `bun test 1328e-conviction-display.test.ts` → 12/12 pass
- TypeScript: `bun tsc --noEmit` → 0 errors
- Commit: `fix(1328e): notifyTelegramAlert routes to BUG channel, not MARKET`
- Branch: `task/fix-1328e-telegram`
