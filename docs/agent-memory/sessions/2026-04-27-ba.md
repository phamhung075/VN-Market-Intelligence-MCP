# BA Session — 2026-04-27

**Task:** BA-1345
**Output:** docs/REQ_1345.md
**Status:** DONE — 1 blocker open

---

## What Was Done

Read sprint goal, handoff, all 4 issue descriptions, and relevant source files:
- `circuitBreakerRegistry.ts` — confirmed reuters + tradingEconomics breakers exist, no fallback activation
- `pollNews.ts` — confirmed SourceFetchers interface has Reuters/TE slots but no fallback chain
- `predictionMarketJob.ts` — confirmed 30min cron, enabled-flag guard, stale-snapshot fallback (silent)
- `polymarket.ts` — confirmed CLOB 403 geo-block handling; Gamma is real source of truth
- `cascadeEngine.ts` (lines 2412–2477) — confirmed `isMarketWide()` logic is correct
- `162-market-wide-broadcast.test.ts` — confirmed domain AC-9 to AC-11 all pass; VIC issue is downstream in intelligenceCycleJob routing
- `pdf-extractor/domain/models.py` — confirmed `ocr_confidence` field exists on ExtractedContent but no financial validation layer

## Key Findings

1. **Reuters/TE:** No fallback source in SourceFetchers. Zero graceful degradation when both breakers open.
2. **BCTC:** `ocr_confidence` exists in model but (a) not propagated to MCP server DB, (b) no financial-figure validation. VNM Assets < Equity and VEA margin 330% are clear accounting identity violations.
3. **Polymarket:** 30min cron correct. Root cause is either `enabled=false` in config OR Gamma API failure causing silent stale-snapshot fallback. No staleness guard exists.
4. **VN-Index cascade:** Domain logic (`buildCausalChain`) is correct and tested. Bug is in `intelligenceCycleJob.ts` alert dispatch — only VIC gets routed. No domain change needed.

## Blocker Open

BLOCKER-1345a-1: PO must confirm if NewsAPI.org key is available. If not, MarketWatch RSS becomes default first fallback (no key required).

## DDD Layer Assignments

| Task | Primary Change Layer |
|------|---------------------|
| 1345a | Infrastructure (fetchers + CB registry) + Application (pollNews fallback chain) |
| 1345b | Domain (validation rules, confidence model) + Infrastructure (schema migration) |
| 1345c | Interface/scheduler (staleness guard) + Config |
| 1345d | Interface/scheduler (intelligenceCycleJob routing fix only) |
| 1345e | Test only |
