### Task: Task 1300b: QA Review

---

### Task: Task 1778 — QA Round 2 (2026-04-30)

**Status:** APPROVED — merged to main

**Tests:**
- 1343b regression: 4/4 pass
- 1778 task suite: 7/7 pass
- Full suite: 8265 pass / 18 fail (18 pre-existing in 308-tool-registry — identical on main, not a regression)
- TypeScript: 0 errors

**DDD:** PASS — bctcDiscovery.ts in domain/services, zero infrastructure imports, Bun.env only, no `any`

**Security:** PASS — no hardcoded secrets, no process.env, no SQL injection surface

**Fixer fix verified:** stripAnsiJunk guard correctly scoped to Strategy A (JSON.parse) only. Strategy B HTML scraping runs unconditionally on raw string. 28 stuck tickers unblocked.

**Merge:** `task/1778-bctc-vnstock-junk-response` merged to main (1c5ec294), branch deleted.

---

### Task: Task 1787 — GVR Sector Fix (2026-04-30)

**Status:** APPROVED — merged to main

**Tests:**
- 1787 task suite: 4/4 pass
- 1343a regression: 15/15 pass
- Full suite: 8329 pass / 30 fail (identical to main baseline — zero regressions)
- TypeScript: 0 errors

**DDD:** PASS — seedWatchlist.ts in infrastructure/db, no cross-layer violations

**Security:** PASS — no process.env, no hardcoded secrets

**Change verified:** GVR domain `oil_gas` → `agriculture` in seedWatchlist.ts. GAS remains `oil_gas` (no collateral damage). Distinct domains: 9 → 10 (agriculture restored).

**Logged:** JANITOR-012 — DAG misclassified as pharma, should be machinery.

**Merge:** `task/1787-gvr-sector-fix` committed to main (f35aa9be), branch deleted.

---

### Task: Tasks 1790 + 1791 — Alert Digest Dedup (2026-04-30)

**Status:** BOTH APPROVED — merged to main

**Task 1790 — alertDigestJob dedup guard fix:**
- 5/5 tests pass (1790-alert-digest-dedup.test.ts)
- Old `if (db && alreadySentToday(db))` pattern confirmed absent
- New `const effectiveDb = db ?? getDb()` pattern confirmed present
- Full suite on 1793 branch: 8342/30 (+12 vs baseline, 0 new failures)
- TypeScript: 0 errors

**Task 1791 — intra-digest identical message dedup:**
- 26/26 tests pass (188-alert-digest.test.ts — 3 new for 1791)
- Set<string> dedup on message string before top-3 slice confirmed
- Overflow reflects unique count, raw count preserved
- Full suite on 1790 branch: 8345/30 (+15 vs baseline, 0 new failures)
- TypeScript: 0 errors (after QA fix)

**QA Fix Applied — Task 1791 (blocking):**
- `bun tsc --noEmit` failed: `msgs[0]`/`msgs[j]` typed `string | undefined` due to `noUncheckedIndexedAccess: true`; `seedAlert` requires `message: string`
- Fix: const destructuring + for-of loop (commit df550ba6)
- Test semantics unchanged

**Branch chaos noted:** 1790 fix was on `task/1793-pollnews-cooldown`; 1791+1792 fix on `task/1790-alert-digest-dedup`; `task/1791-alert-digest-intra-dedup` was empty.

**DDD:** PASS — assembleAlertDigest.ts in application/usecases, alertDigestJob.ts in interface/scheduler; no cross-layer violations

**Security:** PASS — no process.env, no hardcoded secrets, no SQL injection surface

**Merge:**
- `task/1793-pollnews-cooldown` → main: c18fd46f (1790+1793 fixes)
- `task/1790-alert-digest-dedup` → main: b76c3c19 (1791+1792 fixes + TS fix)
- Branches deleted: task/1790*, task/1791*, task/1793*
- TASKS.md: 1790+1791 → Done
- Reports: TASK_REPORT_1790.md, TASK_REPORT_1791.md

---

### Task: Tasks 1792 + 1793 — BCTC Conviction Debounce + pollNews Cooldown (2026-04-30)

**Status:** BOTH APPROVED — already merged to main (via 1790/1791 wave), TASKS.md updated

**Task 1792 — BCTC conviction signal debounce:**
- 5/5 tests pass (1792-conviction-debounce.test.ts)
- `bctcSignalDebounce.ts` in `infrastructure/db/` — DDD layer correct
- 10 rapid fires for same ticker+quarter → 1 Telegram bug message (debounce gate confirmed)
- DB-backed: survives restarts; cross-ticker isolation verified; cooldown expiry verified
- Parameterized SQL throughout; no process.env; no hardcoded secrets

**Task 1793 — pollNews cooldown persist across restarts:**
- 4/4 tests pass (1793-pollnews-cooldown-persist.test.ts)
- 2/2 regression tests pass (1398-pollnews-all-dark-cooldown.test.ts)
- Root cause fixed: INSERT now stores `new Date(now).toISOString()` (injected clock) instead of `strftime('now')` (real wall clock)
- Restart simulation: second call within 4h suppressed; after 4h fires again
- Far-future fake nowMs clock-mismatch regression test (d) passing

**Full suite (main):** 8342 pass / 30 fail — all failures pre-existing, 0 regressions
**TypeScript:** 0 errors

**Branching note:** 1792 implementation was committed to `task/1790-alert-digest-dedup` (wrong branch). `task/1792-conviction-debounce` was empty pointer. Both merged correctly via `b76c3c19`. Empty 1792 branch deleted.

**TSC fix applied:** `as string` cast for `msgs[i]` in 188-alert-digest.test.ts (1791 test introduced `exactOptionalPropertyTypes` TS error — fixed in QA commit).

**Merge:** Already in main via b76c3c19 (1792) and c18fd46f (1793). TASKS.md updated 2026-04-30.
Reports: TASK_REPORT_1792.md, TASK_REPORT_1793.md

---

### Wave 3: Tasks 1786 + 1788 + 1794 (2026-04-30)

**Branch structure:** All three tasks stacked on `task/1786-earnings-conflict-detection` (1788 branch was already at main tip; 1794 commit was included in stack). Merged to main as single merge commit.

**Baseline:** 8418 tests / 744 files on main. Post-merge: 8463 tests / 747 files (+45 net, includes 16+10+19 from new test files).

**Task 1786 — Earnings conflict detection:**
- 16/16 tests pass (1786-earnings-conflict-detection.test.ts)
- `earningsConflictDetector.ts` in `domain/services/` — zero infra imports (DDD clean)
- `agentSignalStore.ts` wired at postSignal: fires only for `chain_catalyst` + `event_type="earnings"`
- Parameterized SQL (`db.prepare(...).all(stockCode)`); no process.env; no hardcoded secrets
- Conflict warning format: `[WARNING] Conflict: prior signal shows X%, this signal shows Y% — verify source`
- Vietnamese number normalization: "tăng 17 lần (1.700%)" → 1700 (parenthesised value preferred)

**Task 1788 — HCM geographic false positive fix:**
- 10/10 tests pass (1788-hcm-geographic-false-positive.test.ts)
- `GEOGRAPHIC_CONTEXT_MAP` + look-behind guard (10-char window) added to `newsNormalizer.ts`
- "TP.HCM", "TP HCM", "TPHCM", "thành phố Hồ Chí Minh" all blocked from extracting HCM ticker
- Pattern 1 (parenthetical "(HCM)") unguarded — company ticker still extracted correctly
- Alias route (stockAliases.ts detectStocksInText) unaffected — no geographic aliases present
- DDD clean: newsNormalizer.ts imports only domain models and other domain services

**Task 1794 — EOD Vol+RSI populated:**
- 19/19 tests pass (1794-eod-vol-rsi.test.ts)
- `WatchlistMover` interface extended: `volume?: number`, `rsi14?: number | null`
- SQL extended: `COALESCE(mp.volume, oc.today_volume) AS volume` in both moverSql branches
- RSI threaded from `taSummary` via `rsiMap` after TA step; defaults to `rsi14: null`
- `fmtMoverLine()` renders: `CODE: +X.XX% | Vol: X.XM | RSI: X.X`; N/A when absent
- `fmtVolume()`: >= 1M → "X.XM", >= 1K → "X.XK"
- Backward compat confirmed: `{ code, changePct }` only callers unaffected
- Non-blocking warn: `globalSnapshot step failed — no such table: commodity_prices` (pre-existing in test env)

**TypeScript:** 0 errors (all 3 tasks).
**Merge:** Merged to main 2026-04-30. Branches 1786/1788/1794 deleted.
Reports: TASK_REPORT_1786.md, TASK_REPORT_1788.md, TASK_REPORT_1794.md