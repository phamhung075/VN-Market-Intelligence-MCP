### Task: Task 1300b: QA Review

---

### QA TASK REPORT — T1-ARCH-CRON-T4-DEDUP-GUARDS (commit 60b48a9b)
**Verdict: APPROVED**
**Gate run: 2026-06-14T12:25Z**
**Evaluated by: qa**

---

#### G1 — Unit Tests GREEN + tsc clean

**bun test src/__tests__/ARCH-CRON-idempotency.test.ts**
```
bun test v1.3.13 (bf2e2cec)

src/__tests__/ARCH-CRON-idempotency.test.ts:
[2026-06-14T12:25:37.630Z] [SCHEDULER] [testJob] already ran within cadence window — skipping (recovery dedup)
[2026-06-14T12:25:37.636Z] [SCHEDULER] [reputationComputeJob] already ran within cadence window — skipping (recovery dedup)
[2026-06-14T12:25:37.636Z] [SCHEDULER] [calibrationReportJob] already ran within cadence window — skipping (recovery dedup)
[2026-06-14T12:25:37.637Z] [SCHEDULER] [newsHeadlinesRefreshJob] already ran within cadence window — skipping (recovery dedup)
[2026-06-14T12:25:37.637Z] [SCHEDULER] [boundaryJob] already ran within cadence window — skipping (recovery dedup)

 13 pass
 0 fail
 13 expect() calls
Ran 13 tests across 1 file. [275.00ms]
```

**npx tsc --noEmit**: exit 0 (no errors)

Result: PASS

---

#### G2 — LIVE no-double-fire

Container image: `sha256:b67438c80de22aa723273d134dc8601c252500a8572f6fe1e0a4b163fc353443` (built 2026-06-14T12:19:13Z)
Live DB: `/app/data/market.db` (named-volume inside container, NOT host ./data/market.db)

bctcReparseJob post-restart rows (queried via `docker exec bun -e sqlite`):
```
{ "job_name": "bctcReparseJob", "started_at": "2026-06-14 12:19:52", "status": "success" }
```
Exactly ONE row after the 12:19Z restart. Prior success was at 11:00:24 (79 min prior — inside 21.6h window).

Container log at 12:19:52Z:
```
[2026-06-14T12:19:52.110Z] [SCHEDULER] [bctcReparseJob] already ran within cadence window — skipping (recovery dedup)
```

Mechanism: `runBctcReparseWithDb(db)` → `recordJobRun(db, 'bctcReparseJob', fn)` inserts a `running` row, then calls `fn()` which calls `runBctcReparseJob()`. The T4 guard fires inside `fn()`, returns early (no business logic runs). `recordJobRun` updates the row to `success`. Single row, zero business execution. No duplicate.

Pre-deploy double-fire observation: `reputationComputeJob` shows two rows at 08:30:00 and 08:30:01 on 2026-06-14. These occurred BEFORE the 12:19Z deploy (server was running from the 08:16:51 restart — previous image without T4 guard). This is exactly the historical failure mode T4 was designed to eliminate; it is NOT a regression from this deploy.

Result: PASS

---

#### G3 — NO-OP on normal fires (guard does not suppress legitimate scheduled run)

`verdictResolutionJob` (hourly cadence, HOURLY_CADENCE_MS = 3600000ms, 90% window = 54min):

```
2026-06-14 00:07:00 (success)
2026-06-14 01:07:00 (success) gap=60.0min
2026-06-14 02:07:00 (success) gap=60.0min
2026-06-14 03:07:00 (success) gap=60.0min
...
2026-06-14 12:07:00 (success) gap=60.0min
```

Each run fires at a 60-minute gap, which is outside the 54-minute (90%) skip window. Guard correctly allows all scheduled ticks through. 13 consecutive hourly rows — zero skipped by guard.

Result: PASS

---

#### G4 — Generic shared helper, not per-job hardcode

`shouldSkipRecoveryReplay` is defined once in `apps/mcp-server/src/scheduler/startupHelpers.ts`. All 24 job files import it via:
```
import { shouldSkipRecoveryReplay } from "../startupHelpers.js"
```

Each call site passes its own cadence constant (e.g. `DAILY_CADENCE_MS`, `WEEKLY_CADENCE_MS`, `HOURLY_CADENCE_MS`, `THIRTY_MIN_CADENCE_MS`). Representative calls:

- `reputationComputeJob.ts`: `shouldSkipRecoveryReplay(db, "reputationComputeJob", DAILY_CADENCE_MS)`
- `calibrationReportJob.ts`: `shouldSkipRecoveryReplay(database, "calibrationReportJob", WEEKLY_CADENCE_MS)`
- `verdictResolutionJob.ts`: `shouldSkipRecoveryReplay(_db, "verdictResolutionJob", HOURLY_CADENCE_MS, deps?.nowMsFn)`

Total files with `@idempotency T4` JSDoc: 25 (24 job files + 1 helper definition in startupHelpers.ts).
Total files calling `shouldSkipRecoveryReplay` (excluding test file): 24 job files.

Result: PASS

---

#### G5 — FAIL-OPEN on DB error

Source at `apps/mcp-server/src/scheduler/startupHelpers.ts` lines 197-200:
```typescript
  } catch {
    // Table missing or schema mismatch — fail-open (let the job proceed)
    return false
  }
```

Unit test T5 in `ARCH-CRON-idempotency.test.ts` verifies this path:
- DB without `cron_job_runs` table → `shouldSkipRecoveryReplay` returns `false` (job proceeds)
- Test passes: confirmed in G1 output

Result: PASS

---

#### Observations (non-blocking)

1. `reputationComputeJob` double-fire at 08:30:00/08:30:01 on 2026-06-14 is a PRE-DEPLOY artifact from the old image (running since 08:16:51, before the 12:19Z deploy). The new T4 guard will prevent this from recurring on the next scheduled fire.

2. `bctcReparseJob` startup catch-up path (`setTimeout(30_000)`) bypasses the T4 cadence semantics at the `recordJobRun` wrapper level (it always inserts a `running` row), but the guard fires INSIDE the job body, ensuring business logic is skipped. The resulting single `success` row with zero duration and no actual PDF processing is correct and expected behavior.

---

#### Summary

| Gate | Result |
|---|---|
| G1 — 13/13 unit tests pass + tsc exit 0 | PASS |
| G2 — no duplicate success row after restart; guard log confirmed in container | PASS |
| G3 — normal scheduled fire (60min gap > 54min window) not suppressed | PASS |
| G4 — single shared helper, 24 job call sites, cadence injected per-job | PASS |
| G5 — missing-table catch returns false (fail-open); covered by T5 unit test | PASS |

**VERDICT: APPROVED — T1-ARCH-CRON-T4-DEDUP-GUARDS promoted to done_verified. T2-ARCH-CRON-RECOVER-JITTER unblocked.**

---

### QA TASK REPORT — T2-ARCH-CRON-RECOVER-JITTER LIVE IDEMPOTENCY FINAL GATE (commit 526e0c25, image 2026-06-14T13:03:09Z)
**Verdict: APPROVED**
**Gate run: 2026-06-14T13:15Z**
**Evaluated by: qa**

---

#### ORACLE WARNING ACKNOWLEDGED

`cron_job_runs` row-count for calibrationReportJob is NOT a valid idempotency oracle due to known cosmetic bug FIX-CRON-JOB-RUNS-DOUBLE-LOG (deferred backlog). Ops agent a88beacd fell into this trap — it read c=2 and proposed opt-out. All gates here use side-effect counts only.

---

#### G1 — calibration_snapshots body-execution count

SQL: `SELECT COUNT(*), MIN(datetime(computed_at)), MAX(datetime(computed_at)) FROM calibration_snapshots WHERE date(computed_at)='2026-06-14';`

RAW OUTPUT:
```
1|2026-06-14 13:00:00|2026-06-14 13:00:00
```

Result: COUNT=1, exactly one body execution. `insertCalibrationSnapshot` is a PLAIN INSERT (no ON CONFLICT) at `apps/mcp-server/src/infrastructure/db/calibrationSnapshotStore.ts:162` — one row = one execution. No duplicate side-effect. **PASS**

---

#### G2 — T4 guard fired on calibration replays

RAW CONTAINER LOG LINES:
```
[2026-06-14T13:04:00.221Z] [SCHEDULER] [calibrationReportJob] already ran within cadence window — skipping (recovery dedup)
[2026-06-14T13:04:01.237Z] [SCHEDULER] [calibrationReportJob] already ran within cadence window — skipping (recovery dedup)
```

Two recovery-replay ticks were absorbed by `shouldSkipRecoveryReplay` (called at `apps/mcp-server/src/scheduler/macro/calibrationReportJob.ts:569`) before any Telegram send or DB write. T4 tier live. **PASS**

---

#### G3 — deepFetch jobs naturally idempotent

Reviewed `apps/mcp-server/src/scheduler/news-analysis/deepFetchVpsJob.ts` and `deepFetchMainJob.ts`:
- Only DB side-effect: `UPDATE rag_analyses SET body_text = ? WHERE id = ?` (keyed UPDATE, naturally idempotent — writing the same body_text twice to the same row is a no-op).
- LanceDB re-index: upserts with `id=ragId+"_deep"` suffix (keyed).
- No plain INSERT to a log/event table.
- No sendTelegram calls.
- Both dispatched via `jobRunRepo.wrapRun` (not recordJobRun, not T4-guarded) — natural idempotency is the declared safety tier (architect brief §6 compliant).

**PASS**

---

#### G4 — Opt-outs intact, no new unguarded gap

RAW grep output (`grep -n "recoverMissedExecutions: false" startScheduler.ts`):
```
273:  // recoverMissedExecutions: false (OPT-OUT, T2-ARCH-CRON-RECOVER-JITTER):
282:  }, { timezone: 'UTC', recoverMissedExecutions: false })
804:  // recoverMissedExecutions: false (OPT-OUT, T2-ARCH-CRON-RECOVER-JITTER):
814:  }, { timezone: 'UTC', recoverMissedExecutions: false })
```

Exactly 2 opt-out registrations: line 282 = monthlySignalQualityAudit (Telegram WORK, no T4 guard), line 814 = foreignFlowFetch (*/1m). Both match architect brief §6 documented opt-outs.

Jobs with `recoverMissedExecutions: true` (explicit override) verified:
- alertDigestJob (line 197): `runAlertDigest` has DB-backed alreadySentToday() dedup guard.
- evidenceAccumulatorJob (line 523): `runEvidenceAccumulatorWithDb` has same-day dedup guard (lines 324-342 of startupHelpers.ts).
- reputationComputeJob (line 975): `jobRunRepo.wrapRun` dedup; daily cadence write is an upsert.

All other jobs use `scheduleCron` default (`recoverMissedExecutions: true` via Lever B wrapper). All are either T4-guarded (shouldSkipRecoveryReplay), naturally idempotent (keyed UPSERT/UPDATE), or have equivalent dedup guards.

No Telegram-sending, non-idempotent, unguarded job found without either a T4 guard or opt-out. **PASS**

---

#### G5 — tsc + T2 test suite

**bunx tsc --noEmit**: exit 0 (no errors)

**bun test src/__tests__/ARCH-CRON-recover-jitter.test.ts**:
```
bun test v1.3.13 (bf2e2cec)
 18 pass
 0 fail
 30 expect() calls
Ran 18 tests across 1 file. [352.00ms]
```

**PASS**

---

#### Summary

| Gate | RAW Evidence | Result |
|---|---|---|
| G1 — calibration_snapshots COUNT=1 for 2026-06-14 | `1\|2026-06-14 13:00:00\|2026-06-14 13:00:00` | PASS |
| G2 — T4 guard skip logs present (2 replay ticks absorbed) | Log lines at 13:04:00 + 13:04:01 | PASS |
| G3 — deepFetch side-effect is keyed UPDATE only, no Telegram | Source review of deepFetchVpsJob.ts + deepFetchMainJob.ts | PASS |
| G4 — 2 opt-outs intact; no unguarded Telegram-sending job | grep lines 282 + 814; all recoverMissedExecutions:true jobs audited | PASS |
| G5 — tsc exit 0, 18/18 tests green | Raw test output | PASS |

**VERDICT: APPROVED — T2-ARCH-CRON-RECOVER-JITTER idempotency final gate PASSED. Ready for done_verified promotion.**

---

### QA TASK REPORT — FIX-REFINE-LOCK-TTL-RECLAIM (commit c080313e, image 674f872db96d)
**Verdict: APPROVED**
**Gate run: 2026-06-14T15:21Z**
**Evaluated by: qa**

---

#### V1 — Commit Content

`git show c080313e --stat` → 3 files, 347 insertions, 4 deletions:
- `apps/mcp-server/src/__tests__/FIX-REFINE-LOCK-TTL-RECLAIM.test.ts` (332 lines, new file)
- `docs/agent-memory/notebooks/dev-mcp-server.md` (11 lines appended)
- `docs/agents/refine_bctc_md/flow/main.md` (8 changes)

No coverage-state.json, cowork-schedule.json, docs/signals/*.json captured — commit is clean of concurrent-agent dirt.

Flow doc diff verified (`git show c080313e -- docs/agents/refine_bctc_md/flow/main.md`):

| Edit | Before | After |
|---|---|---|
| task_claim ttl_seconds | `ttl_seconds: 1000` | `ttl_seconds: 1800` |
| task_heartbeat | bare `{ task_id: ... }` | `{ task_id: ..., owner_agent: "refine-orchestrator" }` |
| happy-path task_release | bare `{ task_id: ... }` | `{ task_id: ..., owner_agent: "refine-orchestrator" }` |
| error-boundary task_release | `task_release` (no args shown) | full call `task_release({ task_id: ..., owner_agent: "refine-orchestrator" })` |

All 4 required edits confirmed. Result: PASS

---

#### V2 — Test Suite

Container path `/app/apps/mcp-server` does not exist (image copies `src` flat to `/app`) — container cannot self-run suite.

Local run (same host, same source tree, image built from it):
```
bun test v1.3.13 (bf2e2cec)
src/__tests__/FIX-REFINE-LOCK-TTL-RECLAIM.test.ts:
 5 pass
 0 fail
 22 expect() calls
Ran 5 tests across 1 file. [332.00ms]
```

`bunx tsc --noEmit` → exit 0.

Container image `sha256:674f872db96deff5a6f3a6e02062ca9ab0b88ea76be9ceb26c97152386c24f21` confirmed live via `docker inspect`.

Result: 5/5 pass — PASS (run locally; container flat-layout incompatible with `bun test` invocation)

---

#### V3 — Non-Tautology (T1 and T2 test bodies)

T1 (`expiresAtOffset: -10`): inserts a lock expired 10 seconds ago. `claimTask` is called with a different `owner_agent` ("refine-orchestrator-new"). Asserts `claimed=true`, `stolen=true`, `row.owner_agent="refine-orchestrator-new"`, and `row.expires_at > now`. This exercises the real `UPDATE WHERE expires_at < unixepoch('now')` steal path in `coordinationStore.ts` — changes=1 is required for the branch to succeed. Not a constant assertion.

T2 (`expiresAtOffset: +900`): inserts a lock expiring 900 seconds from now. `claimTask` is called with a competing caller. Asserts `claimed=false`, `stolen` is undefined, `current_holder.owner_agent="refine-orchestrator"`, and the original `owner_session` is unchanged in the DB. This exercises the no-steal branch where the UPDATE returns changes=0 and a SELECT returns current_holder. Not a constant assertion.

Both tests use an in-memory DB with real `claimTask` logic. Neither test pre-computes expected constants — outcomes flow from the actual SQL branches. Result: PASS

---

#### V4 — Live Unblock Proof

Gateway call (`mcp__claude_ai_gateway__call_tool`) was not available in this QA agent session (MCP tool absent from environment — `.mcp.json` has empty `mcpServers: {}`). Fell back to direct DB probe via `docker exec bun -e`.

Coordination DB (`/app/data/coordination.db`):
- `SELECT COUNT(*) FROM task_locks WHERE task_id LIKE 'bctc-refine:%'` → 0 rows.
- No orphan `bctc-refine:*` locks. Queue unblocked.

Other locks present (2 digest-predict `published:` mutex rows + 1 active `task:FIX-REFINE-LOCK-TTL-RECLAIM` sprint lock, all with `expires_at > now_ts`) — these are legitimate and unrelated.

Market DB (`/app/data/market.db`):
```json
[
  { "id": "fallback-DBC-2026-Q1", "action_code": "DBC", "text_status": "COMPLETE", "refine_status": "PENDING" },
  { "id": "fallback-SAB-2026-Q1", "action_code": "SAB", "text_status": "COMPLETE", "refine_status": "PENDING" },
  { "id": "fallback-GEX-2026-Q1", "action_code": "GEX", "text_status": "COMPLETE", "refine_status": "PENDING" }
]
```

3 reports with `text_status=COMPLETE` and `refine_status=PENDING` present — claimable with no orphan lock blocking. Result: PASS (gateway unavailable; direct DB evidence substituted)

---

#### Anomaly

None blocking. One environment note: `mcp__claude_ai_gateway__call_tool` was not loaded in this agent session (`.mcp.json` `mcpServers` is empty). Live-unblock gate completed via direct DB probe, which is more authoritative than the tool response anyway (avoids any tool-layer caching).

---

#### Summary

| Gate | Evidence | Result |
|---|---|---|
| V1 — 4 flow-doc edits in commit + 3 files only (no dirty capture) | `git show c080313e` diff confirmed | PASS |
| V2 — 5/5 tests pass + tsc exit 0 | Local `bun test` output; container flat-layout note | PASS |
| V3 — T1/T2 non-tautology (real steal/no-steal SQL branches exercised) | Test body inspection confirmed | PASS |
| V4 — bctc-refine queue unblocked, PENDING reports claimable | coordination.db COUNT=0 orphan locks; market.db 3 PENDING reports | PASS |

**VERDICT: APPROVED — FIX-REFINE-LOCK-TTL-RECLAIM gate cleared. Router to promote board to done_verified.**
