### Task: 10-Pass Audit — Sprint 1814 post-merge (2026-05-02)

- **Finding**: agentMemoryUpdateTools.ts + agentMemoryTools.ts used `process.cwd()` → `apps/mcp-server/docs/agent-memory/` (wrong). Root cause: MCP server CWD is `apps/mcp-server/`, not project root.
- **Fix**: Changed path to `../../docs/agent-memory` (local) with `AGENT_MEMORY_DIR` env var override for Docker. Added volume mount `./docs/agent-memory:/app/docs/agent-memory` + env var to docker-compose.yml.
- **Files moved**: 4 session files + 1 issues + 1 patterns + 1 modules + 1 manifests from `apps/mcp-server/docs/agent-memory/` to canonical `docs/agent-memory/`. Removed empty dirs.
- **Other fixes**: agent-roster.md "7 agents" hardcoded → pointer to project-stats.json. SPRINT_GOAL.md trimmed 34→25 lines. docs-organization.md updated with manifests/ subdir rule.
- **Status**: All passes complete. No critical escalation needed.
