# PM Session Log — 2026-05-02

## Cycle: SPRINT-S 1822d task decomposition

### Input
User request: plan Sprint 1822d — migrate BCTC Playwright discovery from VPS to
mcp-server Docker, then remove VPS Playwright scripts.

### Analysis

**Source files read:**
- `apps/mcp-server/src/application/usecases/discoverBctcPdfUrlBrowser.ts` —
  `defaultBrowserFetcher` is plain `fetch` to a VPS URL (but the current code
  actually does plain HTTP fetch, not the VPS endpoint — the file description in
  the briefing was the intent, not current state). The `discoverBctcPdfUrlWithBrowser`
  function has an injected `browserFetcher` parameter pattern — testability is
  already wired correctly.
- `apps/mcp-server/src/infrastructure/fetchers/tradingEconomicsChromium.ts` —
  reference Puppeteer pattern: `buildChromiumLaunchConfig()`, `playwrightScrape()`,
  retry-on-Target-closed, `TE_USER_AGENT`. DI via `deps.scrape`.
- `vps-scripts/discover-bctc-urls-browser.py` — full SSC NewsSearch Playwright
  workflow: selectors, wait times, download via `expect_download`, filename
  sanitisation, proxy URL construction.
- `vps-scripts/vps-proxy-server.js` — `/proxy/bctc-discover/` handler at lines
  281–325, `BCTC_DISCOVER_PREFIX` const, `runBctcDiscoverScript` helper. The
  `/bctc-files/` handler is separate and must be preserved.
- `apps/mcp-server/src/__tests__/1289f-bctc-browser-discovery.test.ts` — 8 existing
  tests, all using injected mock fetcher. Existing tests need zero changes.

**DDD violation identified:** `application/usecases` importing from
`infrastructure/fetchers` directly. Resolved by requiring a new
`infrastructure/fetchers/chromiumPageFetcher.ts` helper so the use case only imports
from infrastructure (valid cross-layer direction per dev-standards).

### Tasks created

| ID | Title | Estimate | Files |
|----|-------|----------|-------|
| 1822d-a | Migrate BCTC Playwright discovery to local Docker | ~2h | `discoverBctcPdfUrlBrowser.ts`, new `chromiumPageFetcher.ts`, test file |
| 1822d-b | Remove VPS Playwright scripts | ~1h | `discover-bctc-urls-browser.py` (delete), `vps-proxy-server.js` |

### Dependency enforced
1822d-b blocked by 1822d-a. VPS code must not be removed until local replacement
is merged and green.

### WIP check
Both tasks added to Todo (not In Progress). WIP = 0 at time of writing. Limit = 2.

### Handoff files written
- `docs/handoffs/TASK_1822d-a.md`
- `docs/handoffs/TASK_1822d-b.md`

### TASKS.md updated
Both tasks added to Todo table.
