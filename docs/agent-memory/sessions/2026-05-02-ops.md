# Ops Session — 2026-05-02

## Task
Deploy updated VPS scripts (Playwright removal + StartLimitIntervalSec fix) to Vinahost VPS.

## What Happened

### Deploy attempt 1: scripts/deploy-vps-proxy.sh
- FAILED immediately: hardcodes VULTR_IP variable (Vultr decommissioned, env uses VINAHOST_IP).
- Correct script is scripts/deploy-vinahost.sh.

### Deploy attempt 2: scripts/deploy-vinahost.sh
- Step 1 (price proxy): uploaded and restarted OK, but verification script
  failed — it checks for live price data within 30s. Market is closed on Saturday 16:18 VT.
  Service itself showed active (running). Script exited at set -e due to verification failure.
- Steps 2-9: needed to be completed manually.

### Manual deploy of remaining services
- Uploaded all scripts via SCP (bash subprocess to bypass zsh sshpass issue).
- All 9 services restarted remotely. All showed active state.
- VPS proxy HTTP health: 200.

### Bug found during verification
- StartLimitIntervalSec=0 was in [Service] section of vn-news-fetch.service,
  vn-reuters-fetch.service, vn-tradingeconomics-fetch.service.
- systemd logs Unknown key warning on every reload/restart (spamming journal).
- Fix: moved to [Unit] section in all 3 files.
- Redeployed, restarted, confirmed no warnings.

### Stale deploy script references
- deploy-vinahost.sh tried to SCP fetch-browser.py and discover-bctc-urls-browser.py
  (both deleted as part of Playwright removal). Fixed by removing those lines.
- Also removed Playwright/Chromium install block from news RSS deploy step.

## Final State

| Service | Status |
|---|---|
| vn-price-fetch | active (market closed = idle) |
| vn-bctc-fetch | healthy |
| vn-news-fetch | healthy |
| vn-sbv-fetch | healthy |
| vn-foreign-flow | active (market closed = idle) |
| vn-tradingeconomics-fetch | active |
| vn-vps-proxy | active, HTTP 200 |

News SLA: age=17min, threshold=30min, OK.

## Commits
- 3dbc92a1 — fix(vps): correct StartLimitIntervalSec placement and remove stale Playwright deploy steps

## Known Issues / Follow-up
- deploy-vinahost.sh verify-deploy-price-fetch.sh check is too strict for off-hours deploys.
  It should skip data verification when market is closed (weekend/outside 9:00-15:15 VT).
  Leaving as-is for now — services deployed correctly, verification logic is cosmetic.


### Task: Task 1540: WAL Checkpoint Fix (11:40–11:52 UTC)
- **Finding**: Signal handler registration order matters
- **Fix**: Moved shutdown hook to early bootstrap
- **Status**: Merged to main