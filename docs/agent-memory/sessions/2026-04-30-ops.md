# Ops Session — 2026-04-30

## Session Goal
Fix 2 VPS infrastructure issues on Vinahost (125.212.251.27).

## Issue 1: vn-news-fetch restart loop (CRITICAL)

### Diagnosis
- Journal showed: OOM kill (`Failed with result 'oom-kill'`) on Apr 29 02:25:09
- Playwright/Chromium peak usage: 385 MB on 961 MB VPS with no swap
- Loop script called `/root/fetch-vn-news.sh` with no timeout — hung runs also possible
- RAM at fix time: 463 MB available

### Fix
1. `fetch-vn-news-loop.sh`: added `timeout 600` wrapper around main script call
2. `fetch-vn-news.sh`: added RAM guard inside `fetch_browser()` — reads `/proc/meminfo`, falls back to `fetch_rss()` curl when MemAvailable < 400 MB

### Deploy
- Both files SCP'd to VPS, `systemctl restart vn-news-fetch`, verified CGroup shows `timeout 600` wrapper active

## Issue 2: vn-reuters-fetch decommission (LOW)

### Diagnosis
- `fetch-reuters.sh` uses dead `feeds.reuters.com` URLs (return empty)
- MCP server fetches Reuters/Google News directly — VPS proxy redundant
- `deploy-vinahost.sh` already had no Reuters section

### Fix
- `systemctl stop vn-reuters-fetch && systemctl disable vn-reuters-fetch` on VPS
- Local files kept for reference, not modified

## Fix Log
- id=206 logged via log_fix MCP tool
- BUG channel notified (message_id=1789)

## Files Changed
- `vps-scripts/fetch-vn-news-loop.sh`
- `vps-scripts/fetch-vn-news.sh`
- `docs/handoffs/TASK_ops_news_fix.md` (new)


### Task: Task 1540: WAL Checkpoint Fix (11:40–11:52 UTC)
- **Finding**: Signal handler registration order matters
- **Fix**: Moved shutdown hook to early bootstrap
- **Status**: Merged to main