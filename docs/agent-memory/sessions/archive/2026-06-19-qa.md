### Task: FIX-OHLCV-SCALE-X1000-AUTO-REPAIR QA sign-off (commit df4d4b34)
date: 2026-06-19
outcome: APPROVED

## Gate 1 — Write-time reject guard (code + container): PASS

`ohlcvUnitGuard.ts` `detectAndNormalizeScaleFromPrevClose` (lines 322–335):
- `upRatioClose = v.close / prevClose`
- `upRatioHigh  = v.high  / prevClose`
- `upRatioMax   = Math.max(upRatioClose, upRatioHigh)`
- If `upRatioMax >= SCALE_DETECTION_RATIO (50)` → `{ rejected: true }` returned with reason `implausible_magnitude_reject`.
- High checked INDEPENDENTLY of close (catches VNM shape: open/low normal, high/close inflated).
- `ohlcvWriteService.ts` Step 3 (lines 366–372): `scaleResult.rejected` → `console.error BUG` + `result.rejected.push(msg)` + `continue` — no DB row written.
- Container probe: `docker exec vn-market-intelligence-mcp-mcp-server-1 grep -c implausible_magnitude_reject /app/src/domain/services/market-data/ohlcvUnitGuard.ts` → `1`. Deployed image carries the guard.

## Gate 2 — Regression tests exist + pass: PASS

`bun test src/__tests__/FIX-OHLCV-SCALE-X1000-AUTO-REPAIR.test.ts src/__tests__/FIX-OHLCV-SEED-CANDLE-UNIT-SCALE.test.ts` → **47 pass, 0 fail** (398ms).

REPAIR-T2 (T2-a) tests the EXACT VNM 2026-06-01 incident shape:
- `open=59200, high=72500000, low=59200, close=72500000, volume=0, dataEnv=null`
- Result: `written=0, rejected.length=1`, reason contains `implausible_magnitude_reject`.
- `getRow(db, 'TICKER_VNM', '2026-06-01')` → null (honest gap confirmed, no DB row written).

T7-c tests the boundary case at ratio == 50 exactly (`prevClose=1000, close=50000`) → `rejected=true`.

T7-h tests high-inflated-but-close-normal (mixed-field inflation) → `rejected=true` via `max(upRatioClose, upRatioHigh)` governing.

## Gate 3 — Writer hardening / no unguarded path: PASS WITH NOTED FOLLOW-ON

Production source INSERTs into `daily_ohlcv` outside `writeOhlcvBatch` (i.e., bypass writers):
- Writer E: `infrastructure/fetchers/ohlcvBackfill.ts:154` — INSERT OR IGNORE; historical backfill only.
- Writer F: `domain/services/priceBackfillService.ts:113` — INSERT OR IGNORE; seed/mock only.
- Writer H: `interface/mcp/server.ts:1247` — ON CONFLICT DO UPDATE; has C=0/vol=0 guard at L1264.

None of these carry the `OHLCV-WRITE-BYPASS-ALLOWED` sentinel comment documented in `ohlcvWriteService.ts` (lines 57–67). However:
1. The sentinel absence predates df4d4b34 (none of these files were changed in this commit).
2. The dev's claim that the original bad row "predates CONTAM-3 hardening" holds — E/F/H are bypass writers that existed before the new guard; the guard is in `writeOhlcvBatch` which covers Writers A, C, D.
3. The follow-on lint task `LINT-OHLCV-WRITE-BYPASS` is explicitly documented in `ohlcvWriteService.ts` lines 74–78 as a backlog item.
4. This is a non-blocking pre-existing gap, not a regression introduced by df4d4b34.

The root-cause fix is in `writeOhlcvBatch` (the SSOT for live-market writers). Writers E/F/H are historical-only and were already in-scope bypasses before this fix.

## Gate 4 — Typecheck: PASS

`pnpm check` (`bun tsc --noEmit`) → exit 0, no errors.

## Summary

All 4 DONE gates satisfied. Sentinel absence on bypass writers E/F/H is a pre-existing non-blocking gap tracked in the codebase (LINT-OHLCV-WRITE-BYPASS backlog) — not introduced by this commit.


### Task: Task 1300b: QA Review