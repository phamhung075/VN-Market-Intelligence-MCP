# Ops Session — 2026-05-01

## Incident: MCP Server Complete Outage (CRITICAL)

**Reported by**: QA Responder (07) — /ask queue blocked
**Time**: 2026-05-01 (current session)
**Severity**: CRITICAL — entire MCP tool layer unreachable

---

## Diagnosis Summary

### Root Cause: Docker Daemon Not Running

1. **Port 3000 not listening** — curl to http://localhost:3000/health returns exit code 7 (connection refused)
2. **Docker socket missing** — `/var/run/docker.sock` and `/run/docker.sock` both absent
3. **Docker daemon not running** — `docker` binary present at `/usr/bin/docker` but daemon fails to connect to socket
4. **Systemd not available** — this environment uses a non-systemd init (PID 1 is environment-manager). `systemctl status docker` fails with "Host is down"
5. **docker-compose binary absent** — `docker-compose: command not found` (v1 compose plugin not installed)
6. **All 9 microservices down** — mcp-server (3000), api-gateway (4000), stock-price (5000), pdf-extractor (5001), rag-service (5002), technical-analysis (5003), macro-indicators (5004), kinh-dich-service (5005), alert-engine (5006) — none listening

### Self-Referential Impact
MCP tools `send_telegram`, `log_fix`, `get_system_status`, `get_vps_service_health`, `get_recent_fixes` ALL live inside the MCP server at localhost:3000. They are also unreachable. Tool calls cannot be executed.

### .mcp.json Transport
- Type: SSE
- URL: http://localhost:3000/sse
- Status: UNREACHABLE

### Last Known Good State
- market-watcher session 2026-04-30 22:00 UTC ran successfully — server was operational
- Last ops fix: id=206 logged 2026-04-30 (vn-news-fetch OOM fix, Reuters decommission)
- Health-check log (2026-04-23 13:32): 983 unclaimed reports, 0 fixes in 48h

### Environment Constraints
- No systemd available (container/sandbox environment)
- Docker binary present but daemon socket absent — Docker not running
- `docker-compose` v1 CLI not installed (v2 plugin form: `docker compose` may exist but daemon is still down)
- `ss`, `netstat`, `lsof` not available in this environment

---

## Recommended Fix Steps (Requires Host Intervention)

1. **Start Docker daemon on host machine** — the container/VPS hosting this environment needs Docker started
   - On host: `sudo systemctl start docker` (if systemd host)
   - Or: `sudo service docker start` (SysV init)
   - Or: restart the Docker Desktop application (if macOS/Windows host)

2. **Verify Docker socket** — confirm `/var/run/docker.sock` exists after daemon start

3. **Start all 9 microservices** using docker-compose:
   ```
   cd /home/user/VN-Market-Intelligence-MCP
   docker compose down
   docker compose up -d
   sleep 5
   curl http://localhost:3000/health
   ```
   Expected response: `{"status":"ok","tools":112,"jobs":50}`

4. **Verify all 9 services healthy**:
   ```
   docker compose ps
   ```
   All services should show "Up ... (healthy)"

5. **Resume /ask queue** — QA Responder (07) can reconnect once localhost:3000/sse is responsive

---

## Escalation Status

MCP tools unavailable — cannot send Telegram via `send_telegram()` or log via `log_fix()`.
This report is the session log. Human operator must start Docker daemon to restore service.

