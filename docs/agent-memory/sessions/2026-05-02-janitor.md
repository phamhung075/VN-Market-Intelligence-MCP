# Code Janitor Session — 2026-05-02

## Scan 021 (00:00–00:30 VN)

- Scope: diff HEAD~3..HEAD — 21 production TS files
- Checks: 5 of 5 completed
- Findings: 1 new | 0 recurrent
- Action: 0 shipped (no single-file mechanical candidates) | 1 backlog task added (JANITOR-020)

### Finding: JANITOR-020 [MEDIUM]

MACRO_CODES array (13 elements), AnalysisRow/AlertCountRow/LastCycleRow interfaces, and all 5 section-builder functions duplicated between:
- `apps/mcp-server/src/domain/services/marketContextBuilder.ts` (canonical, Task 1563)
- `apps/mcp-server/src/interface/mcp/tools/market-data/marketContextTools.ts` (stale parallel impl)

marketContextTools.ts was never updated to delegate to the domain builder after Sprint 226. Two-file change required — added to Backlog.

### Clean areas

- Check 1 (classification maps): no duplication in 21 changed files
- Check 3 (magic numbers/cron): predictionMarketJob.ts catch-branch 50000 is safe defensive pattern; no cron duplication
- Check 4 (schema): zero inline DDL; infrastructure/db/sqlHelpers.ts confirmed re-export barrel
- Check 5 (config drift): all ?? fallbacks are runtime sentinels, not config key fallbacks

---

## Scan 022 (09:00–09:15 VN)

- Scope: diff HEAD~15..HEAD — 2 new production TS files (discoverBctcPdfUrlBrowser.ts, chromiumPageFetcher.ts)
- Checks: 5 of 5 completed
- Findings: 1 new | 0 recurrent
- Action: 1 shipped (JANITOR-021) | 0 backlog tasks

### Finding shipped: JANITOR-021 [LOW]

`30000` ms browser fetch timeout inlined 4x in `discoverBctcPdfUrlBrowser.ts` (lines 52, 100, 147, 217 across tryHoseBrowser, tryHnxBrowser, tryUpcomBrowser, defaultBrowserFetcher). Extracted to `BROWSER_FETCH_TIMEOUT_MS = 30_000` constant at file top. Single-file mechanical. 8 tests pass. Commit b33d6856.

### Clean areas

- Check 1 (classification maps): no Record<string,> ticker maps in 2 new files
- Check 2 (hardcoded ticker arrays): no uppercase ticker literals; `code` is a parameter
- Check 4 (schema): zero inline DDL in either file
- Check 5 (config drift): no ?? fallback patterns in either file

---

## Scan 023 (10:30–10:45 VN)

- Scope: diff HEAD~15..HEAD — 2 production TS files (macroIndicatorFetcher.ts, smartCompactSpawner.ts)
- Checks: 5 of 5 completed
- Findings: 2 new | 0 recurrent
- Action: 1 shipped (JANITOR-022) | 1 backlog task added (JANITOR-023)

### Finding shipped: JANITOR-022 [LOW]

`500` (max chars per turn) inlined twice (lines 65, 73) and `60` (max turns retained) inlined once (line 81) in `smartCompactSpawner.ts`. Extracted to `MAX_TURN_CHARS = 500` and `MAX_TURNS = 60` constants at file top. Single-file mechanical. 2 tests pass. Commit f34852b0.

### Finding proposed: JANITOR-023 [LOW]

`CLAUDE_BIN = "/Users/admin/.local/bin/claude"` defined independently in:
- `apps/mcp-server/src/infrastructure/agents/smartCompactSpawner.ts` (line 22)
- `apps/mcp-server/src/infrastructure/agents/qaResponderSpawner.ts` (line 27)

Two-file change; `qaResponderSpawner.ts` has no test coverage. Added to Backlog as JANITOR-023.

### Clean areas

- Check 1 (classification maps): no ticker-keyed Record<string,> in either file
- Check 2 (hardcoded ticker arrays): no uppercase ticker literals in either file
- Check 4 (schema): zero CREATE TABLE or inline DDL in either file
- Check 5 (config drift): `GSO_VPS_ENDPOINT ?? 'https://www.gso.gov.vn/'` is env-var not config key — not a drift
