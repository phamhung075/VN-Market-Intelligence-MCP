# QA Session — 2026-05-11 — Tier-2 Cycle 20

**Agent:** qa | **Sprint:** 1871/1872 | **Cycle:** Tier-2 / Cycle 20

## Authoritative Baseline

Main HEAD `67d99029` (before merges): **9168 pass / 12 fail / 38 skip**
TSC: **23 pre-existing errors**

Reconciliation: Tier-1 QA reported 9169/11 (1-test delta = Bun flakiness, not regression). 1871f developer's 9046/117 caused by broken `data/` symlink in worktrees — see below.

## 1871b — APPROVED

- Files: docs/ARCHITECTURE.md + notebook + session (doc-only)
- All 11 infrastructure/ subdirs present: adapters/agents/cache/db/fetchers/fileStore/microservices/notifiers/observability/rag/vps
- fileStore/ entry explicitly mentions alertVerdictStore.ts and Sprint 1863
- Cross-link to alert-policy.md updated in 1871g present
- TSC delta: 0 (no code)
- Merge SHA: `6f161a4b`

## 1871d — APPROVED

- Files: docs/data/cron-registry.json (doc-only)
- 21 new entries added: 41→62 total jobs
- cronConfig.ts keys: 59 (exact match to schedulerFileCount=59 metadata)
- 62 vs 59 delta: 3 pre-existing non-job entries (walCheckpointAlert helper, macroIndicatorRefreshJob old-format, ohlcvStartupProbe)
- Existing 41 entries: unchanged (diff shows additions only)
- New entries schema: name/schedule/desc/file (consistent with existing schema — brief spec said purpose/source-file but actual file uses desc/file; new entries match existing convention)
- TSC delta: 0 (no code)
- Merge SHA: `2bcae2e5`

## 1871f — APPROVED

- Files: NEW domain/models/vnstockTypes.ts + IVnstockRepository.ts + vnstockBridge.ts + SqliteVnstockRepository.ts + vnstockStore.ts
- DDD critical check: `grep -rn "^import.*from.*infrastructure/" src/domain/` = 0 hits (grep of comments returned matches; import-statement-only grep = ZERO)
- vnstockTypes.ts: ZERO imports, 6 canonical interfaces exported
- vnstockBridge.ts re-exports: import type from domain + export type — correct backward compat, no shadowing
- TSC delta: 0 (still 23 errors, all pre-existing)
- Vnstock tests: 37/48/6 identical on main and worktree
- Full suite delta (9168 main vs 9050 worktree): caused by broken symlink `data/ → ../../data` in worktrees. `readlink -f` confirms symlink resolves to `agent-XXXX/data` which does not exist. Tests using `process.cwd()/data/` fail with ENOENT. Pre-existing infrastructure issue, not code regression.
- Security: no process.env, no hardcoded secrets, no SQL in changed files (type-only)
- Merge SHA: `30030baa`

## Push + Cleanup

- `git push origin main` — pre-push hook tsc OK — pushed `8583f8c3..30030baa`
- Remote branch `worktree-agent-a9c4d75195f2e9f7c` (1871b) deleted
- Remote branch `worktree-agent-a73eac1c2e606be38` (1871d) — was not on remote (local-only branch)
- Remote branch `worktree-agent-a6fc4a10e62e71722` (1871f) deleted

## TASKS.md

1871b, 1871d, 1871f moved to Done. Commit: `d5bdabad`
