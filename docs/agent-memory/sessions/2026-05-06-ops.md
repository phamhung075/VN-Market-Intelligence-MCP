# Ops Session — 2026-05-06

## Task 1847c — RSS Sources STOPPED (6 sources, 36+ consecutive failures)

### Cycle 21:23 — RESOLVED

**Duration:** ~30 min  
**Diagnosis:** Cloudflare tunnel routing misconfiguration — 29h outage

**Root Cause:**
VPS scripts push to `https://zenmidi.com/api/*` but Cloudflare tunnel (running via `--token` mode, reads ingress from Cloudflare API not local config.yml) only has routes for:
- `/vn-market/*` → localhost:3000 (MCP server)
- `/gateway/*` → localhost:4040 (API Gateway)
- default `/` → localhost:4040

No route matched `/api/*` → all push requests returned HTTP 404 from Cloudflare edge.

**Timeline:**
- Last successful push: 2026-05-05T16:28:30Z
- Outage start: ~2026-05-05T16:45Z (next push cycle)
- Duration: ~29 hours (208 news cycles missed, ~6032 news items not ingested)
- Detected: Quality audit via Task 1847c

**Sources status (actual):**
- CafeF RSS: HEALTHY on VPS (20+20 items per cycle) — push broken
- VnExpress RSS: BLOCKED by RSS site (0 items) — separate issue
- VnEconomy RSS: HEALTHY on VPS (20+20 items per cycle) — push broken
- Reuters RSS: Intentionally disabled (Sprint 1833g) — EXPECTED
- Trading Economics (legacy stream): Intentionally disabled (Sprint 1833g) — EXPECTED
- Trading Economics News (Chromium): Not in VPS script — handled by VPS separately
- Vietstock, Vietnambiz, VnBusiness, TuoiTre, NhanDan, NLD: All healthy on VPS — push broken
- nhandan-economy RSS: BLOCKED by site (0 items) — separate issue

**Fix Applied:**
Updated all 7 VPS push scripts via SSH `sed -i` to use `/vn-market/api/` prefix:
- /root/fetch-vn-news.sh: `https://zenmidi.com/api/push-news` → `https://zenmidi.com/vn-market/api/push-news`
- /root/fetch-prices.sh: push-prices
- /root/fetch-sbv.sh: push-sbv-rates
- /root/fetch-bctc.sh: push-bctc-pdf
- /root/fetch-tradingeconomics.sh: push-tradingeconomics
- /root/fetch-reuters.sh: REUTERS_PUSH_URL default
- /root/fetch-foreign-flow.sh: FOREIGN_FLOW_API_URL default

**Verification:**
- `https://zenmidi.com/vn-market/api/push-news` → HTTP 200, `{"ok":true,"received":208}`
- VPS proxy health: prices=ok (112 items), news=ok (208 items), sbv=ok, foreign-flow=ok
- Source health tracker will reset on next pollNews cycle (in-memory only)

**Outstanding issues:**
1. VnExpress RSS permanently blocked on VPS after 3 UA rotation attempts
2. nhandan-economy RSS blocked
3. BCTC still stale since 2026-04-27 (separate incident)
4. Source health tracker shows 37 consecutive failures (in-memory, resets next cycle)
5. Local config.yml edit at ~/.cloudflared/config.yml was a no-op (--token mode ignores it)
6. VnEconomy timeout at 21:10 was transient (recovered at 21:24)

**Escalation needed:**
- BCTC stale since 2026-04-27 — dev team needs to investigate BCTC fetch scripts
- VnExpress PERMANENTLY_BLOCKED on VPS — VPS IP may be rate-limited; UA rotation not sufficient
