# Ops Session — 2026-04-29

## Task
BCTC contamination cleanup for VCB, HPG, FPT (identical financials).

## Key Findings

### Container vs Host DB Mismatch
The container uses Docker named volume `/var/lib/docker/volumes/vn-market_market_data/_data` — NOT the local `data/` directory on the host. When running diagnostics, ALWAYS use `docker exec ... bun -e "const { getDb } = await import('./src/infrastructure/db/schema.js'); ..."` to query the correct DB.

### Contamination Scope
5 rows deleted from container DB:
- HPG Q1, MBB Q1, TCB Q1, VCB Q1 — all parsed_at 2026-04-24T05:21:50 with identical values (net_profit=6880000, total_assets=80000000) — fan-out contamination from single extract
- VCB year=130 Q4 — wrong year from parseYearQuarterFromFilename bug

### Bug Fixed: parseYearQuarterFromFilename (year=130)
File: `apps/mcp-server/src/scheduler/financial-reports/bctcReparseJob.ts`
- Old regex `/(\d{4})[^\d]*Q([1-4])/` matched `0130` from `20260130-VCB-CBTT-...Q4.2025.pdf`
- Fix: restrict to `(19|20)\d{2}`, add `Quy-N` Vietnamese pattern, add YYYYMMDD prefix tier
- Commit: 79156d6e

### poppler-utils Missing from Dockerfile
- `pdftoppm` was absent → OCR silently failed for all scanned PDFs
- Runtime fix: `docker exec ... apt-get install -y poppler-utils`
- Permanent fix: added to `apps/mcp-server/Dockerfile`
- Commit: 79156d6e

## Re-Extractions Done
All 4 PDFs now in financial_reports with year=2025:
- FPT Q4 2025: low_confidence (0.875)
- HPG Q4 2025: low_confidence (0.25) — manually triggered (not in watchlist)
- VCB Q4 2025: failed (0.625) — bank number parsing bug
- VCB Q1 2025: failed (0.6875) — bank number parsing bug

## Open Bugs Reported (Telegram bug msg_id 1757)
- BUG-A HIGH: VCB bank balance sheet numbers parse incorrectly (liabilities=2, equity=15 instead of billions)
- BUG-B MEDIUM: HPG not in watchlist, disk-scan silently skips it
- BUG-C LOW: Root cause of 2026-04-24 fan-out contamination unknown

## Fix Log
Fix ID: 203


### Task: Task 1540: WAL Checkpoint Fix (11:40–11:52 UTC)
- **Finding**: Signal handler registration order matters
- **Fix**: Moved shutdown hook to early bootstrap
- **Status**: Merged to main
---

## Task 1777a — VPS Price Pipeline Incident (21:07 UTC)

### Findings

**VPS Service Status:**
- vn-price-fetch: IDLE (not pushing — dark since 08:59:31 today)
- vn-foreign-flow: IDLE (dark since 2026-04-24 15:53 — 5 days)
- vn-news-fetch: UNHEALTHY (uptime 1h 40m — VPS restarted ~19:27 UTC)
- vn-bctc-fetch: healthy
- vn-sbv-fetch: healthy

**Root Cause Hypothesis:**
VPS was restarted approximately 1h 40m ago (inferred from vn-news-fetch uptime). vn-price-fetch and vn-foreign-flow systemd services did NOT auto-restart after VPS reboot. Both remain stopped/failed.

**SLA Breaches:**
- price: 37min age (SLA 10min) — CRITICAL
- foreign_flow: 367min age (SLA 10min) — CRITICAL
- news: 103min age (SLA 30min) — CRITICAL

**Pipeline Health:**
- All 31 tickers: rows=0-5, all TA not ready (need >=8)
- 5 trading days of daily_ohlcv missing (since 2026-04-24)
- vn_index_cache: 0 rows

**Secondary Issue — vnstock API broken:**
- JSON Parse error: "Unrecognized token '╭'" across all 30 tickers
- Affects: finance, balance_sheet, cash_flow, stats endpoints
- vnstock library returning ANSI/CLI escape codes instead of JSON
- Likely a vnstock version change or API format change

### Actions Taken
- SSH debug trigger fired: vn-price-fetch (fire-and-forget, /tmp/price-debug-*.log on VPS)
- SSH debug trigger fired: vn-foreign-flow (fire-and-forget, /tmp/foreign-flow-debug-*.log on VPS)
- BUG channel report sent (message_id: 1777)
- WORK channel status sent

### Escalation Required
Manual VPS admin check needed:
- ssh root@125.212.251.27
- systemctl status vn-price-fetch vn-foreign-flow vn-news-fetch
- journalctl -u vn-price-fetch --since "2h ago" | tail -50
- If stopped: systemctl restart vn-price-fetch vn-foreign-flow

### For 1777b Developer
Three code bugs identified:
1. UNIQUE constraint fail on push-foreign-flow endpoint — missing duplicate guard
2. CB reset missing for foreignFlow circuit after market-hours OPEN state
3. vnstock API: "Unrecognized token '╭'" — ANSI escape code in JSON response, needs strip/parse fix
