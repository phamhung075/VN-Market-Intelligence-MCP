# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect

**Sprint goal:** Make cowork `guaranteed:true` an honored contract — bounded catch-up/look-back for slots whose window elapsed during host standby / session-down, or correct the label.
**Agent:** architect
**Started:** 2026-07-22T22:03:07Z

---

### STEP architect-S1 · architect · 2026-07-22T22:06:54Z
**task-id:** BA-COWORK-GUARANTEED-SLOT-CATCHUP
**what-done:** Designed the shared catch-up module extension (new pure domain sibling `cowork-catchup-predicate.js`, mirroring `cadence-policy.js`; `task_list_held` delivery-check kept per-caller/infrastructure per DDD golden rule); ruled explicitly on FR-8 (raise `FIRE_TIMEOUT_SECONDS` per dish_type + accept bounded residual) and Track-B (document residual, no keep-awake daemon); reassigned owner=developer/next_agent=pm on all 5 consolidated rows + the umbrella task.
**what-considered:**
- FR-3/FR-7 delivery-check inside the pure matcher (rejected — breaks domain/infrastructure DDD rule, no I/O in a sync CLI) vs per-caller conditional `task_list_held` call reusing the SAME response for both catch-up-eligibility AND `last_fired` reconciliation (chosen — zero extra gateway calls, NFR-3 preserved).
- FR-7 Option (a) per-flow self-write vs Option (b) reconciler (chosen — (a) reintroduces the exact lost-update race class `last-fired.md`'s batched-write was built to avoid, and contradicts live prior art already on `FIX-COWORK-LASTFIRED-DECOUPLE-FROM-DELIVERY`'s own board note requiring a path-agnostic delivery-proof source).
- FR-6 "which plane stands down" vs "ratify published-marker as sole symmetric arbiter" (chosen the latter — a stand-down mechanism would be a NEW derived signal, repeating this exact sprint's own root-cause class of staleness bug).
- FR-8 raise-timeout vs diagnose-flow-duration vs accept-risk (chose raise + accept-residual — shortening the TNB 6-layer methodology is a PO/BA product call, out of architect's boundary; diagnosis is ops/developer's job, not mine).
**why-decision:** Each choice was grounded in a live-verified brownfield finding (grep-confirmed zero MCP access in the firer script, zero `last_fired` write call-sites, `claimed_at` field existing in `listHeldTasks()`, chef.md's 812-line sequential-no-subagent-fanout structure) rather than the BA spec's prose alone — matches `never_design_without_reading`.
**why-change:** New brownfield finding not in BA spec: `digest-daily` keys its published marker on UTC-date, not VN-date (unlike the other 3 flows) — ruled to mirror, not correct, in this sprint (scope discipline, avoids a duplicate-post regression at the day-boundary).

### STEP architect-S2 · architect · 2026-07-24T16:58:00Z
**task-id:** FACTORY-GUARD-CI-size-lint-justification
**what-done:** Verified zero existing CI/lint mechanism covers `apps/**/*.ts|py|go` file-size (only doc-plane `context-bloat-backstop.sh` hook exists, explicitly "code NOT governed"); live-counted 748 files >120L, 733 unjustified (ticket's "600+" stale); designed baseline/ratchet CI gate (grandfather today's debt, fail only new/regrown offenders); minted child dev row `FACTORY-GUARD-CI-SIZELINT-IMPL`.
**what-considered:**
- Blanket hard-fail on all 733 offenders now (rejected — would red every push instantly; ticket intent is "stop regrowing", not "fix today").
- Diff-only (touched-files) CI check vs baseline-manifest ratchet (chose baseline manifest — a full-tree scan at CI time also catches non-Claude-tool edits the doc-plane hook structurally misses, which is the whole point of a real CI gate vs a session hook).
- Routing child task to `agent-father` (dispatcher's suggestion, rejected — live-verified `agent-father` init.md disclaims production code, and sibling row UC-ASL-P6 is `supervised:true` for exactly this un-routable next_agent+zone combo) vs `developer` (chosen — zone-detect Tier-3 for `cross-service/`).
**why-decision:** Ticket prose ("<!-- --> for TS/MD") was syntactically wrong (HTML comments invalid in `.ts`) and the offender count was stale — both corrected against live grep/wc evidence, not assumed.
**why-change:** Backlog note said "scope via spike FACTORY-GUARD-CI-REGRESSION-SPIKE first" — that spike is still unclaimed; scoped this row standalone per direct dispatch since design is self-contained and creates no rework risk for the spike later.

### STEP architect-S3 · architect · 2026-07-24T17:26:07Z
**task-id:** FACTORY-GUARD-CI-metric-mask-lint
**what-done:** Corrected dispatch prompt's premise (backlog row NOT stub/no-prose — `detail_ref` carries a full spec: ban non-zero `?? <literal>`/`|| <literal>`/py `or <literal>`/destructuring defaults on `confidence*/score*/impact*/magnitude*/probability*`, the `confidence_score=50` bug class, not composite-masks-dead-detector as inferred). Verified zero CI/lint coverage (3 eslint configs, 7 golangci.yml, 2 pyproject.toml — all only fence/import rules). Live-counted real offenders: 4 non-zero-literal masks in 2 files (`cascadeEngine.ts` x3 `?? 0.6`, `marketSentimentCalculator.ts` x1 `?? 1.0`), 1 correctly-excludable config default (`watchlist.ts` threshold `?? 7`). Confirmed all 5 audit fast-track fixes DONE_VERIFIED (allowlist basis is honest). Minted child dev row `FACTORY-GUARD-CI-METRICMASK-IMPL`.
**what-considered:**
- Mirror sibling's baseline/ratchet gate (dispatch-suggested) vs zero-tolerance (chosen) — sibling had 733 pre-existing offenders (grandfather-only-option); this ticket has 4, small enough to fix in the same child task before the gate activates, so no baseline manifest needed and the gate is stronger (fails on ANY match).
- 3 separate native-linter integrations (ESLint rule + ruff AST + go-vet/staticcheck, per ticket's literal text) vs 1 cross-language regex script (chosen — pattern is lexically detectable, debt is near-zero, zero live Go offenders at all; matches size-lint sibling's cheap-CI-job precedent). Native-AST migration documented as the fallback if false-positive rate proves too high.
- `?? 0`/`?? null` always-allow vs flagging all literals (chosen allow-zero — the 5 prior fixes established `?? 0` as the honest-absence idiom now used ~34x; a blanket ban would false-positive-flood the very fix pattern the audit mandated).
**why-decision:** Each finding (ticket prose exists, offender count, fast-track-done status, zero Go debt) is grep/jq-verified against live state, not asserted from the dispatch prompt's inferred intent.
**why-change:** Dispatch prompt's inferred intent (composite/aggregate masking a dead detector) does not match the actual ticket — corrected to the real spec (numeric-literal fallback fabrication) found via `detail_ref`.

### STEP architect-S4 · architect · 2026-07-24T18:29:15Z
**task-id:** FACTORY-GUARD-CI-depguard-tier-boundaries
**what-done:** Verify-live found the ticket wrong on 2 of 3 clauses: (1) TS — both named leaks (pollNews interface-import, telegramCommands application-import) already `DONE_VERIFIED`/committed live, AND the two fence directions asked for (application->interface, domain->infrastructure) already forbidden in all 3 live `eslint.config.mjs` — real gap is ESLint never running in CI at all (0 eslint steps in ci.yml); (2) Python — the "missing" import-linter contract already lives at `pdf-extractor/pyproject.toml:69-76`, `DONE_VERIFIED` 2026-07-09, already running in `py-lint` — zero work; (3) Go — depguard genuinely fine for 6/7 services but `news-fetch` (7th `.golangci.yml`) has ZERO CI job wired (ticket didn't name this). The ticket's 3rd ask (composition-root contains no pkg/module-logic) is genuinely new — depguard can't express it; live-scanned all 7 services' `cmd/server/**/*.go` at receiver-method granularity and found exactly 2 real offenders (both macro-indicators: `policyRatesAdapter.FetchPolicyRates`, `omoAdapter.FetchOMO` — real fallback+`IsEstimate`-flag decision logic), zero false positives against 10 other checked shims. Minted 2 child dev rows (different toolchains): `FACTORY-GUARD-CI-TSBOUNDARIES-IMPL`, `FACTORY-GUARD-CI-COMPROOT-LOGIC-IMPL`.
**what-considered:**
- 1 combined child task (sibling precedent) vs 2 separate (chosen — TS is pure CI-wiring+3-line-fix, Go composition-root check is a net-new `go/ast` mechanism; different toolchains, independently testable, avoids one task mixing unrelated stacks).
- Naive if/for branch-count over ALL functions (rejected — over-fires on legitimate `main()` DI-gate branching + free helper funcs like `envInt`/`splitCSV`) vs scoping to receiver methods only (chosen — empirically zero false positives against the live 12-shim surface, cleanly isolates the 2 real offenders).
- Report-only/non-blocking launch (considered, given the semantic fuzziness of "business logic") vs zero-tolerance (chosen — same size-driven call as metric-mask: debt is 2 functions/1 service, fixable in the same child task before the gate opens, and the receiver-method scoping already proved zero-noise on today's repo).
**why-decision:** Every "gap" claim is grep/read-verified against live config + live source (eslint.config.mjs content, ci.yml job list, pyproject.toml contract text, cmd/server source), not the ticket's prose.
**why-change:** Ticket's 2 named TS leaks and the Python contract are stale/already-fixed — corrected scope to the real gap (CI wiring + a genuinely new Go mechanism) rather than designing around premises that no longer hold.

### STEP architect-S5 · architect · 2026-07-24T19:00:00Z
**task-id:** FACTORY-GUARD-CI-dead-code-gate
**what-done:** Corrected ticket's "4 currently committed .bak" — already 0 (`FACTORY-INTERFACE-delete-bak-files` self-closed same-day, commit `2a146ecdd`, .gitignore already has `*.bak`); found 2 uncovered survivors (`docker-compose.yml.backup/.patch`, different extension). Live-counted real `_deprecated/` debt: 4 dirs/1,373L across mcp-server/pdf-extractor/stock-price, all carrying stale "delete after G5" headers from a stalled migration phase. Found a BIGGER, unlabeled instance the naming grep alone misses: `apps/technical-analysis/src/` (697L incl. tests) — a dead TS DDD stack from a PO-decided-FINAL 2026-05-22 Go rewrite that shipped (Dockerfile/CI/compose all Go-only) but never swept its pre-pivot TS tree; verified zero live wiring, last instance of this shape (macro-indicators/kinh-dich-service already 0 files). Designed 4-check zero-tolerance `dead-code-gate.sh` (bak/backup/patch ban, `_deprecated/`-name ban, Go-service+stray-TS-scaffold ban, `//go:build ignore` ban); deferred full knip/ts-prune/vulture unused-export tooling as documented enhancement. Minted 1 child row `FACTORY-GUARD-CI-DEADCODE-IMPL`.
**what-considered:**
- Full knip/ts-prune/go-deadcode/vulture unused-EXPORT tooling (ticket's literal parenthetical suggestion) vs 4 cheap structural/path-based checks (chosen — zero live debt evidence beyond what the 4 checks already catch; native tooling needs per-project entry-point config to avoid false-positiving on intentional exports like MCP tool registrations; same "defer, not needed at current scale" call as metric-mask/depguard-Python).
- Baseline/ratchet (debt is 2,070L, bigger than metric-mask/depguard's single-digit-line fixes) vs zero-tolerance fix-now (chosen — unlike size-lint's 733-file refactor-risk debt, this is 100% pure subtraction of already zero-import-verified code, matching the audit brief's own "pure subtraction... must lead the program" framing).
- Deleting `apps/technical-analysis/package.json` wholesale (naive) vs trimming only TS-execution fields (chosen — caught live: `dashboard/build.sh` still uses its `esbuild`/`playwright-core` devDeps to build the still-live dashboard).
- Deleting `1081-sprint-054-smoke.test.ts` wholesale (naive, imports the deleted `kinhDichWrapper`) vs surgical removal of only its 3 `appendKinhDich` `it()` blocks (chosen — file is a 569L broad smoke suite; the wrapper is one of many still-live scenarios it covers).
**why-decision:** Every count/claim (bak=0 not 4, _deprecated=1,373L with stale removal triggers, technical-analysis/src/=697L unlabeled-but-dead, package.json partially-live, 1081 partially-live) is grep/read/git-log-verified against live state and the PO decision record, not assumed from ticket prose.
**why-change:** Ticket named only `.bak`/`.backup`/`.patch` + `_deprecated/`-labeled trees; live audit found a larger unlabeled orphan (`technical-analysis/src/`) the ticket didn't name — added to child scope since it's the same bug class and the audit brief's own "~10,000 LOC dead code" line item explicitly named it.

### STEP architect-S6 · architect · 2026-07-24T19:29:07Z
**task-id:** FACTORY-GUARD-CI-no-hardcode-allowlist-scan
**what-done:** Corrected the ticket's naive scope: a literal "ticker allowlist" grep hits hundreds of legitimate domain rule-table arrays (predictionCascadeMapper/policyImpactMapper/creditFlowAnalyzer/climateImpactMapper/cascade-rules — already correctly homed, the same shape `FACTORY-DOMAIN-split-cascade-engine` explicitly legitimizes). Real bug class = a ticker/date literal compared INSIDE a control-flow branch, not a named data table. Live-counted 5 real sites/5 files: the ticket's own named example verbatim at `newsChainFallback.ts:224` (already tracked `JANITOR-035`, unresolved since 2026-07-08); `cascadeExecutor.ts`/`priceSourceRouter.ts` diverging ticker arrays (already tracked `JANITOR-034`); 2 NEW untracked cosmetic per-ticker branches (`backfillBctcScalarsTool.ts` CTG, `pharmaEventMapper.ts` IMP). Zero CI/lint coverage confirmed (same pattern as all 4 siblings). Designed a 2-check zero-tolerance `no-hardcode-allowlist-scan.sh` (temporal-combo ban, ticker/code-literal-branch ban); explicitly deferred generic cross-file array-duplication detection (false-positive risk against dozens of legitimately-overlapping rule tables). Minted 1 child dev row `FACTORY-GUARD-CI-NOHARDCODE-IMPL`.
**what-considered:**
- Flagging all bare ticker-array literals repo-wide (naive ticket reading) vs literal-comparison-in-control-flow only (chosen — the former over-scopes ~100x against already-legitimate, already-tracked-elsewhere rule-table data; verified via full grep sweep before committing to the narrower reading).
- Fixing JANITOR-034/035 outright in the child task vs annotate-and-defer (chosen defer — both findings' own ledger entries explicitly state they require a human design decision on intended semantics/consolidation strategy that a CI-tooling task must not make unilaterally; annotating still closes the zero-tolerance gate honestly).
- Generic cross-file ticker-set-overlap detector (would also catch JANITOR-034 mechanically) vs 2 narrow checks only (chosen narrow — the repo has dozens of rule tables that INTENTIONALLY re-use the same tickers across unrelated cascade categories; an overlap-count heuristic would false-positive on all of them).
**why-decision:** Every count (5 sites not "ticket's assumed" unscoped hundreds) and every exclusion (HOSE/HNX/UPCOM enum, muasamcong.ts table, priceBackfillService.ts test-mock, ohlcvBackfill.ts magic-dates) is grep/read-verified against live source, not asserted from ticket prose.
**why-change:** Ticket's illustrative example implied a scan targeting broad "ticker/date/exchange-floor allowlists" as a class; live evidence shows almost all such arrays are legitimate reference data already in the right home, so the mechanism narrows to literal-in-control-flow only — same "verify-live corrects an over-broad ticket premise" pattern as all 4 siblings.

### STEP architect-S7 · architect · 2026-07-24T19:56:00Z
**task-id:** FACTORY-GUARD-CI-shared-package-import-check
**what-done:** Verify-live corrected the audit's 2 cited drift examples: "shared-db stale module list" already fixed (`FACTORY-SHARED-fix-shared-db-stale-list`, REVIEW, commit `ef62d2921` — 9/9 match confirmed live); "shared-types ComputeTAResponse nullability" is actually a 3-way divergence between two already-orphaned/dead shapes (shared-types scalar vs the dead TS shadow service's nullable-scalar vs the live Go array-shaped contract, whose own header comment documents the drift was investigated and intentional). Confirmed live debt: 3/3 `packages/shared-*` (shared-types/config/db) have ZERO real importers repo-wide (no import specifier, no package.json dependency, anywhere). Found additional undocumented name-collisions (Alert/Signal/McpConfig) between the orphaned shared package and independently-invented live app-side types of the same name. Critically: cross-checked against the Refactor Backlog and found `FACTORY-SHARED-wire-or-prune-shared-packages` (P2, effort L, risk med, rebuild true, BACKLOG) already owns the "deliberate keep-or-cut decision per package" work — this CI-guardrail task must NOT preempt/duplicate that. Designed baseline/ratchet guardrail (package-level orphan-importer check, blocking, baseline-seeded with the 3 current orphans; symbol-name-collision scan, advisory-only non-blocking). Minted 1 child row `FACTORY-GUARD-CI-SHAREDPKG-IMPL`.
**what-considered:**
- Zero-tolerance (fix the 3 orphans in the same child task, matching dead-code/metric-mask/no-hardcode precedent) vs baseline/ratchet (chosen — unlike those siblings, the fix here is a domain keep-or-cut judgment call already explicitly assigned to a separate, larger, riskier task; forcing it into a CI-tooling child task would preempt or rush that task's own field-superset-reconciliation DoD).
- Per-symbol unused-export granularity (knip-shaped) vs package-level granularity (chosen — today's debt is 100% package-level, 0/3 packages have ANY importer; finer analysis is the same class of tool dead-code-gate already declined to build).
- Blocking AST structural-diff for check 2 vs advisory-only name-collision scan (chosen advisory — no TS AST tool wired into any bash-only audit script in this repo; a regex field-diff would false-positive on reorder/JSDoc/optional-marker churn).
**why-decision:** Every count (0/3 importers, shared-db already-fixed, ComputeTAResponse 3-way not 2-way) and the baseline/ratchet call are grep/read/git-log-verified against live source and the live task_board, not asserted from ticket or audit prose.
**why-change:** Ticket/audit implied a single mechanical "orphan + drift" check; live evidence shows the drift half is entangled with a domain decision already owned elsewhere — scoped the guardrail to what's mechanically safe (orphan-block + advisory-surface) and explicitly deferred the rest to the owning task.

### STEP architect-S8 · architect · 2026-07-24T20:31:00Z
**task-id:** FACTORY-GUARD-CI-rebuild-raw-verify-hook
**what-done:** Corrected the ticket's "attached to the PR" framing — verify-live (`gh pr list --state all`, `gh api .../branches/main/protection`) confirms this repo has exactly 1 PR ever (closed, never merged) and no branch protection; the actual governing workflow is autonomous direct-push-to-main. Bigger finding: the DoD itself is not a gap — `dev-standards.md` `PUSH-AUTONOMY-1` §5 already mandates a mandatory post-push REAL-DATA verification task word-for-word; the gap is 0% mechanical enforcement, evidenced by only 2 `VERIFY-*-REALDATA` rows existing across all of orch-state history against 54 serving-code commits since the policy was pinned, and one concrete un-attested miss (`e3386bdfa`, a real confidence-mask fix, self-flagged `rebuild_required: true`, zero RAW-verify token anywhere). Cross-checked no other backlog row owns enforcing §5. Designed a zero-tolerance, no-baseline, forward-only attestation check composing the depguard-tier-boundaries layer fences with the metric-mask-lint field regex (rather than inventing a 3rd pattern), wired primarily into the existing `scripts/git-hooks/pre-push` (the repo's real merge gate) with a CI-job backstop for bypass visibility. Minted 1 child row `FACTORY-GUARD-CI-RAWVERIFY-IMPL`. This is the 7th and last of the 7 `ci-regression-prevention` guardrails.
**what-considered:**
- CI-only enforcement (literal reading of "PR" gate) vs pre-push-hook-primary + CI-backstop (chosen — this repo has no PR/branch-protection to gate a merge on; CI-post-push on `main` can't block an already-landed commit, so the hook is the only mechanism with actual blocking teeth; CI stays as a bypass-visibility backstop since red-CI-on-main is itself a monitored signal here).
- Maximal content-grep trigger scope (any file, any line matching "hardcode-metric or metric-serving") vs a curated trigger composing 2 already-designed sibling mechanisms (chosen — the maximal reading would flag nearly the entire FACTORY backlog, since almost every task's own DoD text already says "RAW-verify... after rebuild"; composing depguard's layer fences + metric-mask-lint's field regex reuses proven, narrower primitives instead of inventing a 3rd independent, unvalidated pattern).
- Literal automated verification of the actual DB probe (impossible — CI runners have no access to the named-volume Docker DB, which only exists on the local host/VPS) vs a textual-attestation presence check (chosen — matches the only mechanically-checkable proxy, and mirrors the already-organic ~650-occurrence RAW-verify-in-commit-message idiom this repo already uses informally).
- Building a board-state-aware check that a `VERIFY-*-REALDATA` task was actually minted vs a lighter commit-range-only textual check (chosen lighter — the REALDATA task is minted temporally AFTER CI green, i.e. in a later, separate push outside the triggering commit range; a cross-push board-state check is a heavier, different mechanism, deferred as documented enhancement).
**why-decision:** Every claim (1 PR ever/no branch protection, §5 exact policy text, 2/54 REALDATA-task compliance ratio, `e3386bdfa`'s zero-attestation shape, no double-owning backlog row) is `gh`/grep/jq-verified against live GitHub state and live orch-state, not asserted from ticket prose.
**why-change:** Ticket assumed a PR-attachment mechanism and implied inventing a new metric-serving-path detector from scratch; live evidence shows neither premise holds — the correct fix is mechanizing an already-written, already-mandatory policy via the repo's real merge gate (the pre-push hook), composed from 2 sibling primitives already designed this session.

### STEP architect-S9 · architect · 2026-07-28T15:48:34Z
**task-id:** FIX-PDFX-TESSERACT-CONCURRENCY-VIOLATES-SINGLE-WORKER-INVARIANT
**what-done:** Root-caused the 10-concurrent-tesseract violation to `POST /extract` → `extraction_engine.py:63 asyncio.to_thread` → `:173 pytesseract.image_to_string` (no `timeout=`), a path neither declared guard covers; the bound in force is CPython's asyncio DEFAULT executor `min(32, cpu_count+4)` = `min(32, 6+4)` = 10 (docker info NCPU=6), which is exactly the observed pin. Refuted TWO row premises with read-only probes: PID 1 IS uvicorn (Dockerfile:117 exec-form CMD) so PPID=1 carries zero orphan signal, and one atomic `ps -Lo tid,wchan -p 1` shows exactly 10 threads in `do_sys_poll` 1:1 with the 10 children — candidate (a)(iii) dead, (a)(ii) dead (`pek_engine_adapter.py:92` semaphore IS module-global, just never on this path). Found the ratchet: `pdfExtractorClient.ts:69 AbortSignal.timeout(120_000)` aborts client-side but `to_thread` is uncancellable, so each abandoned request permanently burns a slot; bctc-reparse-job re-arms one every 122s and Tier 1a fails 100%. Designed a single `infrastructure/ocr_gateway.py` funnel + static call-site fence + a 15-wide ASGI concurrency-watermark regression test. Brief written, row BACKLOG→READY/dev-pdf-extractor.
**what-considered:**
- Per-path semaphore in `extraction_engine.py` (rejected — acceptance (1) names it: a 3rd guard is a 4th thing to bypass; 6 call sites, 5 prose warnings, 2 real guards, zero composition = architectural failure, not a missing `if`).
- `loop.set_default_executor(ThreadPoolExecutor(1))` one-liner (rejected — that executor is shared by 6 push-client/repository `to_thread` users; would queue every HTTP round-trip behind a 40-min OCR, converting an OCR bug into service-wide head-of-line block, and still binds no subprocess lifetime).
- Non-blocking acquire → instant 429, copying PekEngineAdapter (rejected — against the MEASURED 15-wide burst it rejects 14/15 every time and the caller has no backoff, so it hot-loops; chose a bounded 5s queue wait).
- Delete `main.py:154 ProcessPoolExecutor` as the guard that "didn't work" (rejected — it is the ARCH-A20/PDF-AVAIL-02 fix keeping OCR CPU out of the event-loop PROCESS; subordinated it instead by acquiring the semaphore in the parent before `run_in_executor`, so the two bounds compose instead of living in different processes where they provably cannot).
- Cap mcp-server fan-out instead (rejected as THE fix, kept as companion — a server whose safety depends on caller behaviour is not safe; recommended a separate dev-mcp-server row to PO, did not mint, explicitly not a blocker).
**why-decision:** Every load-bearing claim is a raw read-only capture, not inference: `ps` for PID-1 identity and the 10:10 thread↔child pairing, `docker info` for NCPU=6 closing the `min(32,n+4)`=10 arithmetic, tesseract cmdline `txt` (⇒`image_to_string`, excludes the 3 `image_to_data`/tsv sites), absence of any python child (⇒ ProcessPool never fed, excludes `ocr_worker.py:412`), 0 `/extract-tables` in 6h of access log, and the 122s mcp-server⇄pdf-extractor log correlation showing `serviceStatus:null` = client abort with no server reply.
**why-change:** The row pre-committed to PPID=1 meaning re-parenting and pointed "hard at (iii)"; live evidence inverts it — nothing escaped the process, the guards were simply never on the hot path. Also answered rather than deferred the row's open "wedged or progressing" question: CPU pinned at exactly the 200% cgroup cap with per-process ~cap/10 is fair-share starvation, so they are progressing AND abandoned — which reframes the user-gated reap's real cost from "lose 10 in-flight OCRs" to "those results were already unreachable, but `extraction_engine.py:177-178` will silently store them as low-quality successes".

### STEP architect-S10 · architect · 2026-07-28T15:58:25Z
**task-id:** FIX-PDFX-TESSERACT-CONCURRENCY-VIOLATES-SINGLE-WORKER-INVARIANT
**what-done:** Coordinator asked me to make my prose sequencing constraint machine-readable. Checked the premise first and it collapsed: `FACTORY-PDF-extract-tesseract-config`'s code is ALREADY on main (`cfe0a78d7`), `git cat-file -e HEAD:.../tesseract_config.py` resolves, and all 6 importers already import it at HEAD — REVIEW on this board means landed-awaiting-QA (`verify-committed`), not pending-implementation. DECLINED the blocker and instead withdrew the false claim from all three places I had written it (brief §12, board `design_summary`, notebook). Separately established by EXECUTION that the coordinator's stated consequence is also false: nothing auto-picks this row.
**what-considered:**
- Set `depends: ["FACTORY-PDF-extract-tesseract-config"]` as asked (rejected — `deps_satisfied()` is satisfied ONLY by DONE_VERIFIED, so it would pin a live P0 memory-pressure fix behind a P2's QA queue to prevent a collision that cannot occur; the coordinator's own first guard names this harm).
- Invent `blocked_by` (rejected as unnecessary AND it would have worked — `effective_depends_on` unions `.depends_on`/`.depends`/`.blocked_by`, so it is a real alias, not a dead field; the objection was never the field name).
- Leave the prose as-is and just answer (rejected — the prose is what misled the coordinator and would mislead the implementer; a withdrawn claim must be withdrawn in the artifacts, not only in a reply).
- Clear `supervised:true` to unblock the P0's dispatch (rejected — not mine to change, and it is PO's deliberate mint intent, "the implementer does not self-certify"; surfaced it for the router instead).
**why-decision:** Both premises were tested by execution rather than reasoned about: `git log/cat-file` for the collision, and `jq include scripts/lib/devteam-eligibility` for the dispatch path — `is_bounded1_eligible`=false, and running `devteam-backlog-claim-ready-lane-consumer.jq` read-only shows RLC would claim `CCATO-MCP-T1-DOMAIN-ENGINE` with this row resolving to 0 candidates (supervised excluded by both, SLS needs supervised AND plan_only).
**why-change:** I wrote "land that row first" from the REVIEW status alone without a `git log -- <file>`. REVIEW on this board means the code is already merged; treating it as pending-work manufactured a collision that does not exist and nearly converted it into a real P0 blocker.

### STEP architect-S11 · architect · 2026-07-28T23:47:34Z
**task-id:** FIX-POLYMARKET-FETCH-DEAD-GEOBLOCK-ACTUATOR
**what-done:** Ruled RETIRE, not restore-via-VPS. WebSearch confirms the gamma-api `*.anj.fr` TLS-cert swap IS France's ANJ (national gambling regulator) ordering French ISPs to block Polymarket outright (2026-07-16), after ANJ's own investigation found rigged markets (hacked weather-sensor bets) and zero KYC — not a generic geoblock; mcp-server runs on France-hosted infra (`franceSummaryJob` in schedulerJobTable.ts). Wrote full retirement design to the board row's `architect_review_note` (kill acquisition via `config.ts` enabled-default flip; mandatory `fetchPolymarkets()` throw-on-transport-failure fix so `cron_job_runs` stops lying — this part required either way; drop 3 consumer refs + retire the now-purposeless `prediction.md` flow+dispatch-row entirely; deregister the MCP tool; retire 2 quality-checklist SLA rows). Handed to `dev-mcp-server` (`next_agent`), not implementing myself per zone convention.
**what-considered:**
- (a) restore via Vinahost VPS — technically would work (VN egress bypasses a France-only ISP block) but rejected: building infra specifically to evade a sovereign regulator's block against a source that regulator found fraudulent is not the same class of problem as the VN-source geoblocks (SSC/HNX/SBV/CafeF) the VPS pattern exists for; also scope creep on infra `system-map.json` documents as VN-ingress-only.
- (b) retire — chosen.
- partial retire (drop the 3 flow calls, leave the MCP tool registered) — rejected: PO's own "no code path still reads prediction_markets" + "no frozen-but-live-looking table" bars leaving a live-looking tool nobody's supposed to call.
**why-decision:** A regulator's block against a source it found fraudulent is new information PO's (a)/(b) framing didn't have (predates this WebSearch) — it converts a pure cost/architecture tradeoff into a should-not decision, not just a could-not one.
**why-change:** PO left the call open between two build options; the ANJ finding is what actually resolves which one is correct, not a coin-flip on novelty/effort.
