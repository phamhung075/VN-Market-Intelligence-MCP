# Decision Journal — Triage 2026-08-06T14:00Z · po

**Trigger:** dev-team Step 1 (S3 dispatcher-wrap) tick 2026-08-06T13:37Z continuation — 2 drained pendingSignals + 2 mid-task coordinator relays from Step 4.1 `list_unresolved_reports()`
**Agent:** po
**Started:** 2026-08-06T14:05:00Z

---

### STEP po-S1 · po · 2026-08-06T14:15:00Z
**task_id:** FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS
**what-done:** Folded the `[notebook-immutability-guard]` bug-escalation (2 sections named: c58, c56) onto the existing REVIEW row. No mint. Recorded a corrected characterisation plus a new consequence class and a new AC.
**what-considered:**
- Treat as noise (the guard says `mode=warn`, never blocks)
- Mint a fresh FIX for system-auditor notebook-write discipline
- Fold onto the row that already owns the mechanism, with measured discrimination per section
**why-decision:** The routing table mints only when the SAME agent+section pair fires on ≥2 cycles. system-auditor+c58 met that bar (13:37:56Z, 13:46:40Z) — but the owning row's own MINT-THRESHOLD NOTE pre-answers this exact case: don't re-mint, it owns the mechanism. Measured all three named sections in the git plane rather than accepting or dismissing the warn: c56 was byte-identical across all 3 commits (**false positive**), c58 fired twice for two structurally different true positives.
**why-change:** I initially recorded c58 as 30 lines of destroyed audit data and had to refute myself. Extracting bodies "heading → next heading" shows c58 with a 0-line body, identical in shape to the c41 gutting. Reading the file top-to-bottom shows no bytes were lost at all: the new c59 heading was inserted *between* c58's heading and c58's body, orphaning the heading and re-parenting the body. Both versions are in the row note so the wrong one cannot be inherited.

### STEP po-S2 · po · 2026-08-06T14:15:00Z
**task_id:** FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS
**what-done:** Filed a new consequence class + AC: the mechanical retained-bytes check must also assert heading/body adjacency.
**what-considered:**
- Byte-comparison of section bodies is sufficient (status quo AC-4)
- Byte-comparison can pass while attribution is corrupted
**why-decision:** Prior occurrences of this row were costed as *lost evidence*. This one lost nothing and instead **fabricated** evidence: c58's body under c59's heading makes one section simultaneously say "awaiting 768m→1g cap raise" and "post-cap-raise to 1GiB". I spent part of this tick adjudicating an auditor self-contradiction that does not exist. A pure body-hash AC would be green on this commit.
**why-change:** no change from plan.

### STEP po-S3 · po · 2026-08-06T14:15:00Z
**task_id:** FU-RAG-DEPLOY-MEMORY
**what-done:** Resolved the branch question I deliberately left open at 13:55Z. **Branch B: the 1GiB cap is live and did not fix the problem.**
**what-considered:**
- Keep refusing to rule until someone runs `docker inspect` (my 13:55Z position)
- Rule on evidence already sitting in the auditor notebook
**why-decision:** I had framed this as needing one `docker inspect`, because 97.06% of 768MiB is 745MiB and of 1024MiB is 994MiB. Arithmetic settles it without the probe: the container is **using 992.6 MiB**, which cannot fit under a 768 MiB cap at all. That holds regardless of whether the auditor's "(post-cap-raise to 1GiB)" label is trustworthy — and the label is exactly the kind of thing I refused to trust last tick. Two independent cycles (c57 13:18Z, c58 13:36Z) agree, and both uptimes put recreation at ~13:00Z, right after cap-raise commit `2f835ec63` (13:00:35Z).
**why-change:** Reversed my own "refuse to resolve either way". The decisive datum was already collected; what was missing was reading it, not probing for it. Kept the `docker inspect` as confirmatory and recorded that the A-30 dip verdict is still corroborating-at-best either way.

### STEP po-S4 · po · 2026-08-06T14:20:00Z
**task_id:** FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH
**what-done:** Promoted P2→P1 and retired the rationale behind its `depends` on FU-RAG-DEPLOY-MEMORY.
**what-considered:**
- Leave it deferred — the stated sequencing is reasonable on its face
- Dispatch it; the deferral condition is unreachable
**why-decision:** The dependency says: wait for the cap raise, because against a saturated cap an idle-unload's before/after is unreadable. The cap raise landed — and the service re-saturated the larger cap within ~17 minutes of a clean start. So the deferral condition is "wait until the service is not saturated", and the service saturates whatever it is given. **Waiting cannot clear it.** That is a deadlock, not a plan. The saturation is now the argument *for* the row.
**why-change:** Surfaced by the orch-apply Stage-1g report printing the dependency rationale verbatim — I would not have re-read it otherwise.

### STEP po-S5 · po · 2026-08-06T14:15:00Z
**task_id:** FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE
**what-done:** Folded 4 `E-3 write/read-back failure rc=3 (check_id=scheduler_locks-FAIL)` fires; raised P1; added a discriminator AC. Did **not** mint a scheduler_locks FIX.
**what-considered:**
- Take the escalation at face value (4 occurrences today → recurring-bug policy → mint)
- Check what actually emits that string first
**why-decision:** Four independent indicators say these are test artifacts reaching the live BUG channel: `_send_orphan_bug_telegram()` (emit-audit-signal.sh:337-343) calls `send_telegram` with no test-sink gate; `scheduler_locks`+`class:FAIL` is a hard-coded fixture in `db-integrity-history-append.test.sh` L124/147/167; three fires landed in 4m01s (test cadence, not cron); and rc=3 is the script's own documented **usage error**, not a write failure (rc=1/rc=2). Minting would have sent a developer hunting a stuck lock that probably does not exist.
**why-change:** Escalation's reading inverted. But I did **not** prove the suite ran at those timestamps, so I added the AC that matters: gating the telegram is necessary but insufficient — sends must carry an origin discriminator, or the next triager re-derives all four points from source. A silenced-but-ambiguous channel satisfies the row's title while leaving the triage defect intact.

### STEP po-S6 · po · 2026-08-06T14:15:00Z
**task_id:** FIX-AUDITOR-SBVFX-SLA-POSTMARKET-TOLERANCE
**what-done:** Folded the live CRITICAL `sbv_fx stale 1934min (threshold 30min)`; added a scope fence. Corrected a stale title on `FIX-SBV-FETCHER-ZERO-VALUE-EMIT`.
**what-considered:**
- Mint a fresh freshness row for the CRITICAL
- Fold per `data_stale` CONSOLIDATION and separate the two claims arithmetically
**why-decision:** "threshold 30min" against a once-daily source is exactly this row's defect — but 1934 min = 32h14m, i.e. **more than 24h**, so a correct threshold would still breach. The threshold defect explains the CRITICAL grade, not the gap. Fenced it: widening the tolerance would turn this green while the feed is a day stale.
**why-change:** I first read `FIX-SBV-FETCHER-ZERO-VALUE-EMIT` as a false-green trap — a REVIEW/qa row whose title says "fix NOT yet applied" — and nearly escalated that. Its `dev_mcp_server_review_note` documents the fix landing 2026-07-28; the parenthetical was a stale fragment frozen into the title at zone-correction time. Corrected the title. A status-bearing phrase inside a TITLE outlives the state it describes and no status-token check can reach it.
