# Decision Journal — PO triage tick 2026-08-25T16:03-16:24Z (router-direct) · po

**Scope:** Two caller-owed rulings (sweep-guard AC-3 scope; SECONDARY-drain AC-4/AC-6 + routing deadlock) + Step 0-SIG over 21 durable-inbox envelopes + 1 signal_queue NEW row + one mid-run SECONDARY-drain pick folded in by the router.
**Agent:** po
**Started:** 2026-08-25T16:03:30Z

---

### STEP po-S1 · po · 2026-08-25T16:05Z
**what-done:** Before ruling on the SECONDARY-drain row, ran the FULL claim pipeline read-only against a byte-copy of the live board (`ORCH_APPLY_LIVE_FILE_OVERRIDE`, live file untouched).
**what-considered:** QA's AC-4 finding was "ZERO `secondary_claimed_*` stamps of any origin since the fix landed 12:54:37Z", and it offered two readings: the lane is still broken, or the window is unelapsed. I did not want to rule on either without knowing whether the *mechanism* works at HEAD.
**why-decision:** Result: exit 0. Zod PASS, conservation OK (task_total 866=866), prose-ceiling OK, one row stamped (`FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER`, `_lane=done`, target `pm`). **The pipeline is green at HEAD.** That eliminates "nothing eligible" as an explanation for the live zero — 20 union candidates were eligible and the script demonstrably stamps one. Only two explanations survive: a swallowed non-zero `orch-apply` exit (the trailing `|| true`), or a block that was never executed. This is what upgraded AC-6 from QA's single clause to three.
**why-change:** No change to QA's verdict — it is corroborated and strengthened, not overturned.

### STEP po-S2 · po · 2026-08-25T16:12Z · RULING 1 — sweep-guard AC-3
**what-done:** NARROWED AC-3 to the cowork-tick class. Did not widen the row.
**what-considered:**
- **Widen to cover all four classes** — rejected. The three unfixed classes have structurally different remedies that cannot share a DoD: cowork = a missing instruction (fixed); system-auditor 27.1% = an execution-fidelity gap on an *already mandatory, already pathspec-scoped* script, which QA proved intermittent not deterministic (`0a7d11978` at 15:05:51Z did not fire; actor `c6581d0a` at 08:01:59Z did) so no further doc edit can close it; misc 64.4% = no named call-site at all. One AC-3 over three remedy shapes is an unbounded DoD — the exact mechanism that left this row 11 days at `occurrence_count` 32.
- **Close the row outright on AC-1/2/4** — rejected. AC-3 is a durability window, and closing before it elapses is the failure recorded in `feedback_ac3_durability_certified_on_window_that_ended_before_metric_settled`.
- **Narrow + carve the rest into named follow-ups** — chosen.
**why-decision:** The row's delivered value is AC-4, which three prior PO notes on this same row each called the binding blocker, and it landed and was independently re-verified by QA across all three surfaces. The misc class became *discoverable for the first time* because of it — that unlock belongs in a data-driven follow-up, not in this row's AC-3.
**why-change:** Added a **mandatory, non-waivable positive-exercise clause** QA did not ask for. The cowork-tick class base rate is only ~1.3 fires/day (4 fires / 3 days), so "zero fires in 24h" alone cannot distinguish *fixed* from *the path was never exercised*. AC-3 now also requires ≥2 pathspec-scoped commits touching the cowork tick artifacts inside the window. Same class as `feedback_probe_fails_on_dimension_the_spawned_auditor_cannot_see`.

### STEP po-S3 · po · 2026-08-25T16:12Z · RULING 2 — SECONDARY-drain
**what-done:** Accepted QA's AC-4 re-scope; made AC-6 a binding, three-clause blocker; kept `next_agent=agent-father` and declared a hand-hop.
**what-considered (routing):**
- **Widen the DRS allowlist to admit agent-father** — rejected. The 2026-07-30 exclusion (fleet-wide blast radius; it edits the files defining every other agent) is ratified, and the allowlist is threaded by the caller via `--argjson`, so widening leaks to every future DRS pick, not just this row.
- **Re-route to `architect`** (on-allowlist, would auto-dispatch) — rejected. Architect produces briefs; agent-father owns `docs/agents/*/flow/` and wrote this exact block at 12:54Z today. AC-6 is a ~15-line edit inside his own code.
- **Re-route to `.owner` = qa** — rejected, and QA was right to deviate from its flow's canonical actuator (`docs/agents/qa/flow/main.md:274` emits `next_agent: ($t.owner // …)`, `.owner` is `"qa"`): that self-loops to an agent forbidden from editing flow docs.
**why-decision:** One path remains — a router/PO hand-hop spawning agent-father. Stamped `po_manual_dispatch_required=true`; deliberately did **not** write `dispatch_lane` (`feedback_drs_claim_script_trusts_stale_dispatch_lane_field`).
**why-change:** I verified QA's AC-4 falsification myself rather than taking it on prose — four review[] rows *do* carry stamps (08-24T01:58:13Z, 08-24T18:18:12Z, 08-24T22:16:13Z, 08-25T04:16:23Z, the last with a confirmed matching dispatch). The row's own `desc` premise ("ZERO … has EVER carried a stamp") is false. Left the desc verbatim as historical record; the AC array now carries the corrected bar.

### STEP po-S4 · po · 2026-08-25T16:15Z · head-of-line clear
**what-done:** Signed `FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER` off to DONE_VERIFIED, `done[] → done_verified[]`.
**what-considered:** It was the strictly-oldest union candidate and, being an epic wrapper with no residual work, was re-picked every tick with nothing for `pm` to action. I checked whether it could legitimately close rather than evicting it for lane hygiene: its blocking `verify_note` required child `TASK-BCTC-INSPECT-UI-FILTERS` to clear its ops-rebuild → qa-live-verify chain. **Both** children resolve DONE_VERIFIED in `docs/data/orch/archive/2026-08.json`, and AC10 demanded verification through the frontend proxy, so I probed it: `GET http://localhost:3001/dashboard/bctc-inspect` → HTTP 200, 109331B, containing `id="quarter-filter"`, `id="ticker-filter"`, both Vietnamese labels and the AC7 zero-match string.
**why-decision:** Blocking condition genuinely satisfied. Closing it was correct on the merits *and* unblocked the lane — not a hygiene shortcut. Attached as `verification.raw_probe`.
**why-change:** none.

### STEP po-S5 · po · 2026-08-25T16:15:36Z · the ruling was confirmed and then immediately re-broken
**what-done:** Nine seconds after my write landed (16:15:27Z), the SECONDARY-Drain produced the **first post-fix `secondary_claimed_*` stamp on the board** — `RAG-FTS-BUILD-MEMORY-BOUND`, review[]-origin, target `po`, folded into this run by the router. I set it `REVIEW → BLOCKED`.
**what-considered:**
- **Treat it as AC-4 satisfied** — rejected, and the router flagged this correctly. That row is *review[]-origin*, so the OLD review[]-only readback would have found it too. It does not exercise the done[]-lane widening — the actual subject of the row — which remains proven only on QA's fixture. Recorded as an explicit `po_ac4_caveat` on the row: AC-4 closes only on a **done[]-origin** post-fix stamp with a matching dispatch.
- **Treat it as retiring AC-6** — rejected. The same block produced nothing on the 15:37Z tick with 20 eligible candidates and left no trace. One green tick is not an instrument.
- **Leave it REVIEW after a no-op disposition** — rejected, and this is the important one. The row carries `po_timegate_next_recheck_after=2026-09-20` and a standing PO hold, so a no-op changes neither `status` nor `next_agent` — and the claim script re-picks the strictly-oldest row *every tick until one of those changes*. Leaving it would have re-created, on a different row, the exact head-of-line block I had cleared nine seconds earlier, this time for 26 days.
**why-decision:** `BLOCKED` is the only eviction lever the claim script offers (it selects `status == "REVIEW"`, excluding BLOCKED by construction) and it is also the honest token — the row *is* blocked, on a date and on two uncertified sibling container windows. Same disposition PO already applied to `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM`. Un-block condition stated on the row.
**why-change:** This exposed a defect neither the caller nor QA had named: **the SECONDARY-Drain candidate set has no not-actionable predicate at all**, so *any* deliberately-held row is a permanent lane block, and `BLOCKED` is being overloaded to mean both "blocked on a dependency" and "held until a date". Its own flow doc calls the re-pick "expected throttle-by-design, not a bug" — true only for rows someone can action. Minted `FIX-DEVTEAM-SECONDARY-DRAIN-CANDIDATE-SET-NO-NOT-ACTIONABLE-PREDICATE` (P1 → architect, which *is* DRS-allowlisted), whose AC-6 requires reverting my hand containment once the predicate ships.

### STEP po-S6 · po · 2026-08-25T16:18Z · retraction
**what-done:** RETRACTED P0 `FIX-TASKRELEASE-ORIGINAL-OWNER-CLIENT-SESSION-INERT-RUNGB-NEVER-RELEASES` → `archive[]`, status CANCELLED.
**what-considered:** dev-team read `coordinationStore.ts releaseTask()` at source: Rung B gates on `task_kind=='orphan-signal' AND owner_client_session IS NULL AND owner_agent match AND payload.original_owner_client_session match`. The PO fixture that produced this row was minted with `task_claim` under a dead-but-**non-null** session, so `released:0` was *correct anti-theft behaviour*, and the step-4 control's `released:1` via Rung A was also correct. Independently corroborated by `feedback_orphan_signal_unreleasable_null_client_session_and_activesprints_only_flip`.
**why-decision:** The row's premise is a fixture artifact, not a defect. Keeping a P0 alive on a refuted premise costs a dispatch slot and, worse, this row was cited as blocking `FIX-ORPHAN-FR4-FR5-FLOW-DEVTEAM-ADOPTION-GUARD` (P0, review[]) — both premises fall together, so the retraction *releases* a block rather than creating one.
**why-change:** Recorded the genuine residual without re-minting a P0: Rung B is untestable-by-construction from the tool surface (nothing can mint a null-session fixture). That is a test-affordance gap, not a release defect.

### STEP po-S7 · po · 2026-08-25T16:24Z · 0a-D prune re-ruling
**what-done:** SUPERSEDED the 2026-07-21T18:11Z standing instruction. **0a-D signal_queue prune is re-enabled.**
**what-considered:** The deferral's *only* stated rationale was evidence preservation — "running the prune now would … resolve the open question by deleting its subject." I re-measured both premises rather than accepting dev-team's report: live `.signal_queue.rows` census is **NEW=1, READ=41, zero `triaged`, zero `RETRACTED`** — the vocabulary fragmentation is gone — and the cited live-probe row has been consumed into a DONE_VERIFIED fix.
**why-decision:** An evidence-preservation hold whose evidence no longer exists preserves nothing; it just accumulates 41 READ rows in the fleet's hottest file. Released early and on the record, noting explicitly that the instruction's own release condition (row closure) is **not** met and that whoever closes `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT` should not treat closure as the trigger.
**why-change:** Attached one condition to the *first* post-ruling prune only — it must list pruned row ids in its commit message, so the removed set stays recoverable from git. Cheap insurance against premise (a) being under-measured; not a standing requirement.

### STEP po-S8 · po · 2026-08-25T16:22Z · inbox CLEAR
**what-done:** Cleared all 21 durable-inbox envelopes by `envelope_id` with `ORCH_APPLY_DECLARED_INBOX_TRIAGED`. Inbox 21 → 0.
**what-considered:** The caller said 17; my own fresh read found 17 at start and **21** by CLEAR time — four more drained in mid-run, two of which were the very signal *files* the caller listed separately (they reached the inbox at 16:10:57Z), plus a Pipeline-A duplicate of the `qadrainskip` signal_queue row and a routine `cowork-fire`. All four were already covered by rulings/mints made earlier in the same run.
**why-decision:** Subtract-by-`envelope_id` (never `= []`) is exactly what this case needs — the four late arrivals were triaged, so all 21 clear honestly.
**why-change:** none. Confirms `triage-signals.md`'s self-read mandate: a caller-supplied count is a convenience copy, and it was stale by four within twenty minutes.
