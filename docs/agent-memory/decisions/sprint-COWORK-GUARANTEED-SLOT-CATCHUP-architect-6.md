# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect (cont. from -5.md)

**Sprint goal:** COWORK-GUARANTEED-SLOT-CATCHUP (active)
**Agent:** architect
**Started:** 2026-08-22T20:05:00Z (rolled from -5.md per its own CAP-REACHED note: lines=137 bytes=37800+ > 36000)

---

### STEP architect-S33 · architect · 2026-08-22T20:05:00Z
**task-id:** FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE
**what-done:** Bounded amendment pass on my own 2026-08-08 brief responding to `po_verdict: AMEND-THEN-RESUBMIT` (2026-08-14). Re-measured the resolver live (not the brief's or PO's stale numbers) two ways — broad union (identical to `decision-journal-archive.sh`'s own `KNOWN_IDS`, cross-checked against the real script: `known_ids=100` matched exactly) and a strict/corrected union dropping the weak `.done_tasks[].sprint` signal per §2.3. Broad-dangling=11 (set churned vs PO's 08-14 11), strict-dangling=22 (+11 hidden by the broad union). Designed A1 (Stage 1h known-id union corrected), A2 (new step-0 task-id-collision check, 2 live instances: `UC-RDL-P4`, `BA-IND-P1-MOMENTUM-FRONTEND`), A3 (branch-table contradiction resolved per PO's ELSE-branch ruling: exact `sprint_goal.status=="active"` only, `PLANNING` alone never a liveness signal), AC-0 (safety-valve design for `decision-journal-archive.sh --all`, non-blocking, not shipped). Folded Q1/Q2/Q3 rulings into an amended per-id table (22 ids); surfaced NEW Q4 (`sprint_goal.status=="OPEN"` on `PREDICTION-CLAIMS-DAILY-CADENCE`, a 3rd vocabulary word neither the brief nor PO's ruling anticipated).
**what-considered:**
- Re-measuring live vs. reusing PO's 08-14 or the brief's 08-08 numbers — chose re-measure: PO's own instruction (§2.2's churn, twice, already proved a static table goes stale) plus the row sat untouched 8 days, long enough for material drift (confirmed: `COWORK-GUARANTEED-SLOT-DURABILITY` resolved, `CHORE-COMMIT-OVERHEAD`/`UC-RDL-P4` newly dangling).
- Where to put the amended content: append a new numbered section (§11) to the SAME brief file, naming exactly which old paragraph each subsection supersedes, vs. writing a fresh v2 file — chose append-in-place: matches the repo's own changelog convention (`dev-standards.md`'s header block never rewrites prior entries) and keeps a single git-blameable artifact rather than forking the design across 2 files.
- Board-row update shape: the row was already 17715B against the 12000B `orch-row-prose-ceiling-check.mjs` ceiling — confirmed empirically (per the prior cycle's S30 finding on a different row) that ANY net-new inline growth on a non-structural field hard-rejects. Compressed `architect_review_note` from 2330B to a 1987B summary (net -343B) instead of appending, and used the (structural, zero-cost) `detail_ref` field to point at the new §11 anchor rather than inlining full content into JSON.
**why-decision:** The row's own consequence #1 (PO sign-off structurally impossible) is exactly what a stale/broad-union "clean" reading would silently perpetuate — re-measuring live and cross-checking against the real archiver script (not a reimplementation guess) is what makes A1's necessity demonstrable today, not just asserted from an 8-day-old finding.
**why-change:** §1–§10 of the 08-08 brief are NOT retracted — only the 3 blocking items PO named (A1/A2/A3) plus the 1 non-blocking item (AC-0) and the Q1/Q2/Q3 folding are amended, exactly the bounded scope the `po_goahead_20260814T143024` stamp authorized. No implementation shipped (plan_only unchanged); routed `next_agent=po` for re-ratification of the amendment + Q4.

### STEP architect-S34 · architect · 2026-08-23T00:00:00Z
**task-id:** FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES
**what-done:** Direct P0 router dispatch (10-day-starved ready[] row, CI RED on HEAD). Measured live: `SignalRowSchema.severity`/`type` both `z.string()` unenforced against the same file's own `SignalSeverityEnum`; `WARN` (non-canonical) = 65-69% of live signal rows right now, not a rare straggler. Confirmed `orch-apply.sh`'s Stage-1 Zod gate is whole-document all-or-nothing — a closed enum on either field would reject entire write transactions on first unlisted value, not just misroute one signal. Independently re-verified triage-signals.md:59's AC-2 cross-check claims as false via raw grep counts across cold archives: system_issue(underscore)=112, system-issue(hyphen)=109, both concurrently live through August (underscore fired 6x this month — not "historical"). Confirmed today's CI-red (`audit-handoff`) is a correct guard fire on a real cross-pipeline gap (Pipeline-A table has a rule, Pipeline-B doesn't), not a repeat of the already-fixed hand-array-drift defect (guard-signal-type-coverage.sh, 2026-08-22, already derives from the docs, no hand array).
**what-considered:**
- (a) closed `z.enum()` on `type`/`severity` vs (b) registry-derived routing + self-filing fallback — chose (b): (a)'s migration cost is not just the one-time ~101-token enumeration but an ongoing per-new-type governance gate the fleet has no process for, and the write-outage risk is measured, not hypothetical (WARN alone would reject 65-69% of today's severity traffic).
- Whether to propose "just add the missing audit-handoff Pipeline-B row" as the fix — rejected per router's explicit constraint; named it only as a same-pass tactical bridge that ships alongside the structural registry+self-filing mechanism, not as the deliverable.
- Board write shape for the sibling row (FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED, 35127B, already over the 12000B ceiling) — chose not to write to it inline (net-new growth on an over-ceiling row hard-rejects); disposition covered in the brief + RETURN prose instead, PM to fold at decomposition.
**why-decision:** Precedent inside the same schema (`TaskSchema.status: StatusEnum`) proves closed enums work in this exact write-gate when the vocabulary is small/centrally-ratified/slow-growing; signal type/severity fail all three properties, measured, so the same mechanism transfers into an availability risk rather than a corruption guard.
**why-change:** No change from PO's own diagnosis on the row's `detail_ref` (open producer namespace vs closed consumer allowlist, 2026-08-13) — this pass adds the concrete design (registry+self-filing), the severity measurement, the AC-2 correction, and rejects (a) with a mechanism-level reason rather than restating the problem.

### STEP architect-S35 · architect · 2026-08-23T09:32:22Z
**task-id:** FIX-CRON-REARM-STEP1B1-LIVENESS-ORACLE-BLIND-WINDOW-FALSE-LIVE
**what-done:** Re-read all 3 cron SKILL.md at source, measured the presence roster / process table / transcript mtime live, then designed one guard shape for all three families in `docs/architecture-briefs/2026-08-23-cron-rearm-liveness-oracle-process-observation.md`.
**what-considered:**
- Adopt the dispatch brief oracle ranking verbatim (transcript-mtime PRIMARY) — REJECTED on measurement: at the incident instant the dead session transcript was 16s old, so any mtime threshold above 16s also returns LIVE. Transcript-mtime has a DEAD-direction blind window that is structurally worst at a restart, for the same reason heartbeat_age is. Promoting it reproduces the outage with a smaller constant.
- Keep a LIVE/DEAD binary and just pick a better default — REJECTED: with two branches the spec must guess on ambiguity, and "conservatively LIVE" is the branch that cost 8h10m. Added an explicit third UNKNOWN branch instead.
- Resolve UNKNOWN with a per-family threshold (the status quo, retuned) vs a per-family declared property — chose the property (`has_fire_election_mutex`, verified: cowork/detect-loop hold `cron:<x>:<tick>`, standalone holds nothing), held in ONE table inside the probe. Three numbers in three docs is how this bug was born.
**why-decision:** Both existing oracles read bookkeeping the session wrote about ITSELF — presence is a participation signal (measured 2 roster rows vs 5 live claude PIDs), and force_release_orphan reads heartbeat_at on the SAME marker only the dead owner renews, so it is a lagging derivative, not the independent check §1.3 claims. Only process-absence is sound at t+1s after death, so O1 must be first; that single reordering is what makes one spec close both the T=7200 false-LIVE and the T=120 false-DEAD.
**why-change:** Stronger than the brief on two points — the oracle ranking is inverted (O1 before O2, with evidence), and the standalone false-DEAD was found FIRING LIVE during this pass (heartbeat_age 30948s, holder absent from roster but transcript 16s old), so it became mandatory fixture F2 rather than a cited memory.

### STEP architect-S36 · architect · 2026-08-23T09:32:22Z
**task-id:** FIX-COWORK-DAILY-SLOT-SILENT-SKIP-NO-CATCHUP-CATCHUPRAW-SCOPED-TO-GUARANTEED-ONLY
**what-done:** Answered PO question (b) empirically before designing — inventoried all 3 durability layers live, cross-read the launchd firer log against `last_fired`, pulled `pmset -g log`, and ran the live matcher — then wrote `docs/architecture-briefs/2026-08-23-cowork-slot-durability-layer-inventory-and-miss-detection.md`.
**what-considered:**
- Design on the guaranteed/non-guaranteed split (the 2026-07-22 brief premise) — REJECTED, disproved: staleness tracks HOUR OF DAY, not `guaranteed` (all 10 fresh slots fire UTC 17:30-00:10, all 11 stale fire UTC 01:30-16:35; 3/8 guaranteed fresh vs 7/13 non-guaranteed).
- Wire Step 4.55 / widen `catchup_raw` past `guaranteed:true` as the fix — REJECTED on a live run: `catchup_raw` returns 8 records, ZERO eligible. Its `rolled_past_vn_date` + 60-1440min bounds cap catch-up at one VN day against a measured 4-day outage, so a fully-wired 4.55 recovers nothing for anyone.
- Move Layer C off the laptop (VPS) — NOT reopened; re-read the 2026-07-07 §3 rejection and it still holds on its own terms (no LLM runtime, no credential, no flow-doc tree).
- Blanket-remove `cowork-catchup-predicate.js:209` vs a narrow `catchup_scope` opt-in — chose narrow: only `refine-bctc-*`/`bctc-analyst-*` lack publish-date semantics, so only they may legitimately run late.
**why-decision:** Layer A was retired 2026-06-22 (0 slots with `trigger_status:active`), so PO's either/or had no true branch; the real mechanism is that Layer C is AWAKE-scoped not host-scoped — `pmset` shows 96.5h continuous Standby 08-18T12:00Z→08-22T12:35Z and the firer log gap matches both edges exactly, so B and C are down simultaneously with no catch-up in either. Ordered `last_fired` write-back first because 4 of 8 guaranteed slots already show a Layer-C fire NEWER than `last_fired`, so any detector built on today's field reports 4 false misses.
**why-change:** Reframed the row from "build catch-up" to "detect misses" — for a 4-day outage the honest answer for publish-date-bound dishes is that no catch-up is possible, and the row's own verification_gate already allows "declared out of scope with a reason".

### STEP architect-S37 · architect · 2026-08-23T09:47:00Z
**task-id:** FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS
**what-done:** Designed a value-shape prose measure for `orch-row-prose-ceiling-check.mjs` (prose iff string >200B or non-empty container, UNION'd with the existing `STRUCTURAL_FIELDS` name set), widened the live-side baseline to all lanes with a `max()` duplicate-id rule, and specified a targeted `--over-ceiling-only` compaction mode for `orch-backlog-stub.sh`; brief `docs/architecture-briefs/2026-08-23-orch-row-prose-ceiling-value-shape-measure-and-frozen-cohort-paydown.md`.
**what-considered:**
- Add `occurrence_count` + `po_manual_dispatch_*` to `STRUCTURAL_FIELDS` (both sibling rows' own proposal) — rejected as the durable fix: it is turn #4 of a documented 3-turn loop whose instance #1 (2026-08-15) already carried an AC-5 ordering the enumeration and never got it; 846 field names live, 33 excluded, 813 on the prose side, `.passthrough()` keeps the namespace open forever.
- Raise / retune `ORCH_ROW_PROSE_CEILING_BYTES` — rejected: `project_context_bloat_governance` incidents are real, and arithmetically it fails anyway (12000→41000 to clear today's max would grandfather p99=21918, i.e. the whole distribution).
- Special-case integer increments — rejected on measurement: `occurrence_count: 1→2` ALREADY passes today (same digit count, zero byte delta), so that rule fixes neither of PO's two actual blocks (a +306B 4-field coordination stamp, and field-addition/digit-boundary cases).
- Bypass env var / split the write / fold onto the ceiling row — rejected, agreeing with the guard's own header and PO's two prior refusals.
**why-decision:** The property that makes a field a bloat risk is a property of its VALUE (unbounded text), not of its NAME — so deriving the classification from the value needs no registry and covers producers that do not exist yet, exactly the same closed-allowlist→derived-rule move that resolved the signal-type row yesterday. It is also monotone (exclusion set only grows ⇒ `prose_new ≤ prose_old`), so no write that passes today can start failing — the property that makes it landable against three concurrent writers. Verified on live data that it does not disarm the guard: 22 of the 23 frozen rows STAY over ceiling, and short strings hold 43,875B against 1,616,447B for long ones (2.6% of prose mass), so they are not the bloat channel.
**why-change:** Root cause is one layer above the row's own thesis. The 2026-08-09 brief chose (c) = 3 parts in dependency order and only part 3 (the gate) shipped — the §2.3 paydown never ran on `backlog[]`/`ready[]`, so the guard has run 14 days in the exact brick-mode its own author named and rejected; the grandfathered-WARN cohort was sized as a transient residue, not a permanent 23-row population. Also found and reproduced a third, unreported defect nobody had filed (D3): the live baseline is looked up only in the 3 guarded lanes, so a zero-change lane-move in from `in_progress[]` hard-rejects with a false `live=0B` (proven live on `UC-CCA-P3`), with 15 over-ceiling rows currently armed in unguarded lanes. And found a landmine in the paydown itself — today's `STUB_FIELDS` would strip `po_manual_dispatch_flagged_at`, which `flag_reentrant` reads inline with no `detail_ref` fallback.

### STEP architect-S38 · architect · 2026-08-23T10:05:00Z
**task-id:** FIX-SIGNAL-INBOX-NON-DRAINABLE-ENVELOPE-50-OF-51-FILES-SILENTLY-CLASSED-LITTER
**what-done:** Re-measured PO's inbox count live (drainable_count moved 1→2, inbox is live), then classified all 49 non-drainable files by hand against the script's own two predicates + `JSON.parse`. Designed an age-bounded one-shot escalation (ledger table in `signals.db` + a normal enveloped signal emitted by `drain-signals.js` itself) for the two genuinely-orphaned sub-populations, leaving the discriminator (`isDrainableShape`/`isByPathConsumerFamilies`) byte-for-byte unchanged. Brief: `docs/architecture-briefs/2026-08-23-signal-inbox-orphan-escalation-discriminator.md`.
**what-considered:**
- PO's option (ii), widen the reader to synthesize an envelope from filename+mtime — rejected: this is the exact move `mcp-tools.md`'s DUAL-PLANE CONTRACT records as already having produced 4 false fixes on `price_anomaly_*`.
- Fix only the known writers (part of option (i)) — rejected as the whole fix: the 26-file backlog shows a writer defect closing (telemetry.md envelope invariant, 06-05) does not un-stick its own wreckage, and a SECOND, currently-undetected writer bug (`notebook-auto-prune.sh:574-575`, `grep -c ... || echo 0` double-emit) is producing the identical failure class right now with zero test coverage.
- PO's option (iii), escalate loudly on non-drainable non-by-path age — chosen, with two floors derived from existing constants (30min = 2x fastest cowork cadence, matches D4's own "2x-cadence" convention this sprint; 7d = the drain's own existing DB/processed-prune cutoff, not a second hardcode).
**why-decision:** 49/51 non-drainable is not one population: 21 are the sanctioned by-path family (correctly excluded, PO's hard constraint), 26 are well-formed-but-unenveloped litter, and 2 are a THIRD failure mode PO's row didn't name — malformed JSON that never reaches `isDrainableShape()` at all. Only the escalation design (not reader-widening, not writer-only) closes all three without re-risking the by-path family.
**why-change:** Corrected the framing from "50 mis-shaped files" to "26 orphaned + 2 malformed + 21 correctly-exempt" — the exempt population must stay invisible to the fix, which is why the by-path check is verified to run BEFORE the new age logic (AC-4 in the brief). Flagged, not fixed here: `price_anomaly_*`'s own unbounded growth (no prune anywhere, oldest 52d) and the live `notebook-auto-prune.sh` bug (not in PO's `files[]` scope) — both named as candidate follow-up rows for PO/PM.

### STEP architect-S39 · architect · 2026-08-23T13:00:23Z
**task-id:** FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER
**what-done:** Brownfield-verified BA's spec against live code (ran `buildLabel()` in node before designing, read `.controls`/`loadDocList`/change-handler/`resetPanes` in full) and appended `[Architect] Brownfield Findings` to the BA spec handoff.
**what-considered:**
- BA's literal FR-3b prescription (parseInt-coerce `period_quarter` only) — REJECTED as insufficient: it fixes "QQ1"→"Q1" but the row still renders "HUT Q1 Q1 2024" (duplicate), not AC9's own target "HUT Q1 2024" — traced root cause to `buildLabel()` unconditionally appending a 2nd quarter token onto a `period_type` that's ALREADY `'Q1'..'Q4'` on every live row (255/257, not just the 2 HUT), a stale assumption from when the comment's example implied a `QUARTERLY`/`ANNUAL` literal.
- Leave `PI3-bctc-inspect.test.ts:361` (AC-14) untouched since NFR-2 said "stay green" — REJECTED: that assertion hardcodes the CURRENT BUGGY value `"VCB Q1 Q1 2025"`; the real AC9 fix necessarily changes it to `"VCB Q1 2025"` — called out explicitly as in-scope, not a regression.
- Server-side/client-side dedup module for the quarter normalizer — REJECTED per BA's own NFR-4 (no build step on the HTML page); kept as accepted, flagged duplication.
**why-decision:** Fixing only the type-mismatch (as literally spec'd) would ship a design that fails its own acceptance criterion on delivery — the wider duplicate-quarter bug had to be traced to design the fix BA's AC9 actually needs.
**why-change:** Widened AC9's fix surface from "2/257 rows" to "buildLabel() itself, all rows" (one function, `bctcInspectHandler.ts:167-171`, ~8L incl. new exported `normalizeQuarter()`) — no scope change to the 2 target files, PM/dev-mcp-server routing unaffected.
