# Decision Journal — PO triage tick 2026-08-26T06:51Z (router-direct, ad hoc) · po

**Scope:** Step 0-SIG over 4 router signals (1 retracted mid-tick by its producer) + rulings A/B/C + one REVIEW adjudication + one mid-tick router correction folded in.
**Agent:** po
**Started:** 2026-08-26T06:42:52Z
**Landed:** `docs/data/orch/orch-state.json` via one `orch-apply.sh` pipe at 06:51:41Z — 4 mints, 1 lane move, 5 row regates/unfreezes, 1 review adjudication, 4 signal dispositions.

---

### STEP po-S1 · po · 2026-08-26T06:43Z
**what-done:** Fresh self-read of `.dev_team_idle_chain.pending_triage_inbox` per Step 0-SIG. Length 0. Signal work for this tick lives entirely in `.signal_queue.rows[]` (Pipeline B), 4 rows `to: po` status NEW.
**what-considered:** only path: the durable inbox is SSOT and it is empty; the caller-supplied list was the live-queue rows, which I re-read myself rather than trusting.
**why-change:** One caller-supplied id was wrong — the spawn prompt cited `rtr-20260826T0650-ocrworkerloggingnotobservable`; the live id is `rtr-20260826T0639-...`. Disposed the real id. Also noted the prompt's "pre-gate at 06:52Z" is a hand-typed stamp ~9 min ahead of real `date -u` (06:43Z); the router self-corrected this mid-tick and I have not cited 06:52Z anywhere.

### STEP po-S2 · po · 2026-08-26T06:45Z — DECISION A, rebuild routing
**what-done:** APPROVED. Minted ONE row, `OPS-FLEET-REDEPLOY-STOCKPRICE-MACROINDICATORS-CONFIRMED-BINARY-DRIFT` (P1, `next_agent=ops`, `zone=cross-service/`), **prepended** to `ready[]`.
**what-considered:**
- Two per-service rows — rejected. Same failure, same remedy, same owner, same window; two rows double the dispatch cost of one ops visit for zero added information.
- Append to `ready[]` — rejected. DRS breaks ties by ARRAY INDEX, not age, and dispatches ~1 row/tick against a 106-row lane; appending ranks a fresh P1 last in band. Prepended instead.
- Prose-append the two confirmations onto `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` — rejected, and this was pre-flagged by the router: that row is 14205B, grandfathered over the 12000B ceiling, so net-new inline growth hard-rejects at orch-apply Stage 2.5. Folded BY REFERENCE via `.related`. (Confirmed in the apply output: it emitted a non-blocking grandfathered WARN, candidate=live=14205B, no growth.)
- Include technical-analysis — **rejected, deliberately.** 392h drift with no valid probe is UNPROVEN. Rebuilding it here would launder an inference into an action. Left with `FIX-FLEET-DEPLOYED-VS-MAIN-UNANSWERABLE-GIT-SHA-BUILD-ARG-IS-OMITTABLE` (backlog, P1), the row that makes the question answerable at all.
**why-decision:** Timing gate 09:00Z as the router asked. stock-price serves live session price fetches; a single-service recreate costs 1-3 min of failed fetches — real but bounded, and not worth paying with ~2h of session left. macro-indicators folded into the same window because it is on a 15s cron deadline at ~2.5s margin.
**why-change:** Encoded the time gate **three ways on purpose**, because `next_recheck_not_before` has exactly ONE consumer (`scripts/devteam-review-claim-qa-drain.jq:199`) and dispatch gates are known blind to prose `status_note`. So it is also the FIRST CLAUSE OF `.title` (picker-visible) and AC-0 (agent-visible). Also **verified the stock-price fix is real at source myself** rather than relaying it: `pkg/primitive/vndirect-quote-mapper/mapper.go` declares `Change/PctChange` as `*float64` (:30-31) and preserves nil through `MapPayload` (:113-131, comment cites DSI-INV-1), and BOTH served paths call it — `pkg/infrastructure/fetchers.go:75` (Tier1) and `:143` (Tier2). The redeploy is not speculative.

### STEP po-S3 · po · 2026-08-26T06:47Z — MID-TICK ROUTER CORRECTION: I falsified 2 of the 3 items handed to me
**what-done:** Accepted the producer's retraction of signal 4 (`status=RETRACTED`, no row minted). Then **falsified BOTH replacement findings** the router asked me to mint, and minted the one real defect hiding underneath them.
**what-considered:** The router reported 86x 503 + 45x 202 on `/pek-extract` and concluded (F1) a 66% failure rate from semaphore contention and (F2) 86 breaches of the VN market-hours prohibition, which it believed was enforced "only in agent docs". I ran the status-by-hour histogram it had not run (`docker logs -t`, bucketed by UTC hour):
```
00Z: 2 x 202    01Z: 43 x 202    02Z: 40 x 503
03Z: 17 x 503   04Z: 16 x 503    05Z: 11 x 503    06Z: 2 x 503
```
The partition is EXACT: every one of the 45 successes is OUTSIDE the block window, every one of the 86 503s is INSIDE it. 45 + 86 = 131 = the router's own totals.
- **F2 is INVERTED, not merely wrong.** The prohibition IS code-enforced: `apps/pdf-extractor/interface/routes_pek.py:62` runs a Layer-2 guard calling `is_vn_market_open_utc()` BEFORE any model check or adapter call and raises 503 with `detail.error=market_open`; primitive at `apps/pdf-extractor/domain/primitives/market_hours/primitive.py`, unit-tested at `apps/pdf-extractor/__tests__/test_market_hours_guard.py`. ZERO breaches occurred. All 86 attempts were correctly REFUSED.
- **F1 collapses with it.** If the 86 503s are the guard, there is no 66% failure rate; the off-window HTTP success rate is 45/45.
**why-decision:** The tell was that the router attributed 100% of the 503s to a `SemaphoreContendedError` traceback it had seen, without checking whether the 503s and the traceback occupy the same hours. They do not — the semaphore errors are 18 in 01Z, 26 in 02Z, 2 in 03Z, i.e. they overlap the *202* burst, not the 503 wall. This is the "probe FAILS on a dimension the agent cannot see" shape in reverse: a real artefact attributed to the wrong mechanism because only one axis was bucketed.
**why-change:** What survives is better than what was reported, so I minted it: `FIX-PDFX-PEK-EXTRACT-202-ACCEPTED-THEN-SILENTLY-DROPPED-SEMAPHORE-1800S` (P1, `dev-pdf-extractor`, prepended to `ready[]`). `/pek-extract` returns **HTTP 202 Accepted and then drops the work** — the background task dies with `SemaphoreContendedError` after a 1800s queue wait and the caller is never told. 13 distinct `report_id`s died this way out of 45 accepted extractions (~29% silent loss). The caller is `172.18.0.4` = `vn-market-intelligence-mcp-mcp-server-1`, identified by `docker inspect`, not guessed.
P1 not P0: bounded silent loss on a path idle 6h of every 24, no corruption, no outage. Made binding in the row: **do NOT close this by raising `PEK_SEMAPHORE_WAIT_SECONDS`** — a longer wait converts fast failures into slow ones and leaves the false-202 contract untouched. Folded the router's real-but-small residue as AC-4 (mcp-server has no client-side market-hours gate and ignores `detail.retry_after`, burning 86 pointless round-trips/session — P3 weight, not P0). Found in passing and filed as AC-5: `routes_pek.py:41` and `interface/schemas.py:80` document the window as `02:00-08:59` while the executing guard and its own message at `:72` say `02:00-07:59`. One of those lies to every reader; today's traffic cannot discriminate (no 07Z/08Z calls yet).

### STEP po-S4 · po · 2026-08-26T06:48Z — DECISION B, PaddleOCR sequencing: router's read OVERRULED
**what-done:** OVERRULED the router's proposed bundle. Ruled the order: **(1) fix the silent drop → (2) measure the tesseract-vie production baseline → (3) then and only then decide PaddleOCR.** Wrote the dependency into `MEASURE-PDFX-BCTC-QUALITY-TESSERACT-VIE-PRODUCTION-BASELINE.depends_on`.
**what-considered:**
- Router's read: "instrument `ocr_worker.py` and re-baseline/split the two size-lint offenders must be done TOGETHER in one change." **Rejected — its load-bearing half is void.** The instrumentation prerequisite rested entirely on signal 4, which its own producer retracted this tick: logging IS configured (`apps/pdf-extractor/infrastructure/lifespan.py:37`), and the module is IDLE not dark (`/extract`, its only route, was called twice since container start). A module that ran at most once emitting no INFO lines is expected. With that gone, the "together" argument (instrumenting adds lines to an already-over-bound file) has nothing to bind to.
- The size-lint half is REAL and I verified it independently against `docs/data/size-lint-baseline.json`: `ocr_backends.py` baseline 449 vs live **689**, `ocr_worker.py` baseline 462 vs live **578**. Both are genuine current offenders.
- Making the split a separate pre-task — rejected. Size-lint scans the WORKING TREE, not the push range, so a replacement whose FIRST commit splits both files passes on the final tree. Ordering it as a standalone row buys nothing and adds a dispatch.
**why-decision:** Measuring extraction quality over a corpus in which ~29% of accepted extractions silently evaporate produces a number with an unknown denominator and a survivor set selected by queue luck. That is not a baseline. The standing user goal ("replace tesseract with PaddleOCR to improve quality of extraction BCTC") is a MEASURED claim, so the measurement apparatus has to be trustworthy before the measurement, and the pipeline has to be lossless before the apparatus.
**why-change:** **DEFERRED-NOT-DECLINED** on the actual PaddleOCR-vs-tesseract choice, and on the `4 fires/document` cap in `DECIDE-PDFX-OCRWORKER-PAGE-RESCUE-LIVE-UNMEASURED-QUALITY-PATH`. Reason is a missing measurement, not a missing opinion. Inventing a number now would repeat exactly the defect that row was filed to protest — that `1db5f9f81` capped the rescue at 4 with no product ruling behind it.

### STEP po-S5 · po · 2026-08-26T06:50Z — DECISION C, the six 09:00Z-gated rows
**what-done:** Split 6 ways, not 1: **2 unfrozen now, 4 re-gated to 17:11Z.** The finding is that 09:00Z was set against the WRONG binding constraint for five of the six.
**what-considered:** 09:00Z is the VN market-close gate. But the constraint that actually binds the pdf-extractor rows is the **AC-7 cgroup-memory sampler on container `417febec1a03`, whose 12h window closes 2026-08-26T17:11Z** and which has already been destroyed four times. Any row that must RUN the extractor contaminates that series at 09:00Z.
- `FIX-MARKETDB-...-RESTORE-DROPPED-12205-FF5M-AND-54-EVIDENCE-ROWS-STILL-RECOVERABLE` — **UNFROZEN NOW, gate deleted, moved backlog → front of `ready[]`.** It touches market.db recovery and has NO market-hours dependency whatsoever; it was swept into a batch gate that never applied to it. It has the opposite property: a hard expiry — AC-2's 12,205 rows are recoverable ONLY while the preserved corrupt snapshot survives. P0 + irreversible-on-timeout ⇒ every frozen hour is pure loss.
- `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` — **UNFROZEN NOW, source-only.** Root cause already settled at source by the 01:16Z adjudication (`main.py:154` `ProcessPoolExecutor(max_workers=1)` with no `max_tasks_per_child`; both shipped `malloc_trim` mitigations wired only into PID 1). Writing that needs an editor, not a container. Substituted a BINDING constraint in its place: no rebuild/restart/recreate before 17:11Z.
- Orientation-corpus sweep (79 files/312 pages), the quality baseline, and the CTG balance-sheet refine — **gate MOVED 09:00Z → 17:11Z**, not relaxed. All three must run the extractor; 17:11Z satisfies both constraints at once.
- `DECIDE-PDFX-OCRWORKER-PAGE-RESCUE-...` — gate moved to 17:11Z and left BLOCKED deliberately, `depends_on` the baseline. See S4.
**why-decision:** "Unfreeze early?" is the wrong question when the gate encodes the wrong constraint. Answering it row-by-row surfaced a P0 with a decaying recovery window that had been parked behind an irrelevant market-hours bar.
**why-change:** Recorded on the DECIDE row that the retracted signal must NOT be carried forward as a live premise — a retraction that leaves stale dependants behind it is how a corrected finding re-enters the system as fact.

### STEP po-S6 · po · 2026-08-26T06:50Z — REVIEW adjudication (pre-check: rows waiting on PO)
**what-done:** `FACTORY-STOCK-extract-vndirect-mapper` (review, `next_agent=po`, QA CHANGES_REQUESTED) — **SPLIT, not bounced.** Both QA gaps accepted as accurate; ruled differently on each.
**what-considered:**
- Gap 1, Tier1/Tier2 not collapsed to `doFetch` (~44L of HTTP plumbing still duplicated, no `doFetch` exists): accurate, and **DESCOPED**. Carved to `CLEAN-STOCKPRICE-TIER1-TIER2-DOFETCH-HTTP-PLUMBING-DUPE` (P3, developer). Hygiene, not correctness; the DoD wording is mine to narrow. Carved rather than dropped so the debt stays visible.
- Gap 2, "RAW-verify the served Price after rebuild": accurate, **RETAINED**, and **not satisfiable by the developer** — it needs a rebuild, which is ops. Row re-routed `next_agent=qa`, `blocked_by` the new ops row, whose AC-2 discharges it.
**why-decision:** Holding a correctness fix (DSI-INV-1 null-vs-zero, verified live in source at S2) in REVIEW behind a refactor-completeness clause is the wrong trade. Bouncing to the developer would have been worse — there is no developer-side action left in the row.
**why-change:** none from plan.

### STEP po-S7 · po · 2026-08-26T06:51Z — terminal shape
**what-done:** Verified on disk after apply: `ready` 106→109, `backlog` 596→597, `in_progress` unchanged at 1 (pre-existing, not mine), `.head` untouched and still `idle` (last written by qa 06:18:29Z). Conservation check clean (task_total 884→888 = exactly my 4 mints; signal_total 74→74). Every touched row is in a real lane with a real status and an explicit `next_agent`. Inbox CLEAR executed (already 0, set to `[]` unconditionally per Step 0-SIG ownership).
**what-considered:** only path: this is the mandated terminal shape.
**why-change:** Two schema corrections during apply — `owner: null` is rejected (must be string or absent), and `next_recheck_not_before` unfreeze must be `del(...)` not `= null`. Recorded here because both are cheap traps for the next minter.

### NOT DONE THIS TICK — declared, not silently skipped
- **Push:** standing disarm in force. Step PUSH-BACKSTOP not run.
- **pdf-extractor container `417febec1a03`:** not rebuilt, restarted or recreated. Not touched at all beyond read-only `docker logs`.
- **Channel audit (Step 0):** skipped. Router-direct triage spawn with a non-empty signal set; No-Task Guard clause 3 satisfied by the signals, so the audit is not the gating input this tick.
- **Stale worktree noticed, not filed:** `.claude/worktrees/agent-ae9ed2cd6f04b3686/` holds `ocr_worker.py` at 462L and `ocr_backends.py` at 449L — exactly the size-lint baseline values, i.e. a pre-split snapshot. Flagged to the router in the RETURN rather than minted, since worktree hygiene is not a product decision and I did not verify whether it hides uncommitted work.
