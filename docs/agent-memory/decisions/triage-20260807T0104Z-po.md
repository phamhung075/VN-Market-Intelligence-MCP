# PO Decision Journal — triage 2026-08-07T01:04Z

Tick: dev-team Step 1 PO triage (dev-team-tick-preflight 2026-08-07T00:37Z), lock `task:po-triage-20260807`.
Inputs: 1 Pipeline-A `ci_red` signal, 4 Pipeline-B `.signal_queue` rows, WF-2 supervised hold, manual-dispatch sweep.

---

## D-1 — `ci_red` CI-RED-417573d7 → 5 file-scoped FIX rows + 1 fold

**task_id:** FIX-CI-SIZELINT-BCTCREFINED-PROJECTION-BASELINE (+4 siblings)

- **what-considered:** (a) dedup the whole signal into the existing root-cause row `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER` (ready[], P1); (b) one consolidated "CI paydown" row; (c) one row per distinct failing file per `triage-signals.md` § `ci_red`.
- **why-change:** chose (c). (a) is the ANTI-AMNESTY case the spec forbids — that row's ACs are about the pre-push hook, closing it does not make any of these 6 files pass. (b) breaks the file-scoped `dedup_key` machinery, which is the only thing that stops the next ci_red re-minting duplicates.
- **Pre-dedup failing-file read (mandatory, done before dedup was evaluated):** `gh run view 31136261063 --log-failed` returned no `FAILEDFILE:` block (neither failing job is the bun-test isolation runner), so FAILING_FILES came verbatim from each failing step's own error lines. Re-read against the CURRENT head too (`70ae0d9ae`, run `31136642246`) because the signal's head_sha was already 2 commits stale: **identical 6-file failure set, still RED** — signal is live, not historical.
  - size-lint: `apps/mcp-server/src/infrastructure/db/schema.ts` (336→372, upper 369), `.../financial-reports/getBctcRefinedTool.ts` (162→234, upper 178), `apps/rag-service/app_factory.py` (121→168, upper 133), `apps/rag-service/infrastructure/embedder.py` (167L **new-offender**, absent from `.entries` — verified directly against `docs/data/size-lint-baseline.json`).
  - task-claim-owner-session-lint: `docs/agents/market-watcher/flow/cycle.md:303`, `.../eod.md:78`, both labelled NEW call site (not the line-shift regrandfather class of the chef.md sibling).
- **Dedup outcome:** file-scoped `dedup_key` scan over the 5 non-terminal lanes. **1 hit** — `ci_job:size-lint|file:.../db/schema.ts` matches `FIX-MCP-MEMORY-CODE-LEAK` (review[]) → folded, no mint. **5 misses** → minted.
- **Method note worth keeping:** my first dedup scan returned `[]` and I nearly took it as evidence. It was a jq type bug (`lanes | select(type=="object")` tests the ARRAY, never the rows) — the predicate was vacuous. Caught it by cross-checking against an id I already knew existed from memory. `feedback_empty_read_is_not_evidence_confirm_tool_targets_store`.

## D-2 — Root cause behind D-1 (recorded, not separately minted)

- **only path:** all four size-lint offenders trace to **three `review[]`-lane rows whose own fix commits crossed their size baseline and never reconciled it**: `0308514f5` (→ FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH) grew both rag files; `ff36391dc` (→ FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM) grew getBctcRefinedTool.ts; schema.ts is drifting under multiple hands. Verified by `git show --stat`, not inferred from commit subjects.
- **why-change:** did not mint a 4th "process" row — `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER` (ready[], P1) already owns *why nothing local catches it* (pre-push excludes `docs/` and never runs size-lint). Instead wrote a **QA gate note onto each of the two live review rows**: do not sign off a row whose own commit leaves CI red. That converts a systemic observation into an enforceable checkpoint on rows already in flight.
- **AC discipline on every minted row:** fix = justification header or real shrink. Explicitly **forbade `--update`** — `size-lint-baseline.json` regenerates WHOLESALE, so a blanket `--update` silently grandfathers every unrelated current offender. Same shape as the ratified AC1 on `FIX-CI-TASKCLAIM-CHEF-MD-OWNER-SESSION-PAYDOWN`.

## D-3 — WF-2 supervised hold: ratified, `po_goahead` stamped

**task_id:** FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT

- `should_hold` re-run byte-identical to dev-team's own predicate → `true` before, `false` after. Ratified **at source per `supervised-goahead.md` Step 2, never on the relayed verdict**: read `cowork-schedule.json` directly, read live `/app/data/market.db` via `docker exec` + `bun:sqlite`, read `bctcSanityValidator.ts` directly.
- **what-considered (cadence):** (a) re-enable all 3 disabled refine slots → 4x drain, VHM ~08-08; (b) hold at slot-4 only, accept ~08-10; (c) re-enable exactly one slot → 2x.
- **why-change:** chose (c). The canary DID prove the Option-C fix generically (12 windows, correct DONE/FAILED discrimination, correct per-window flags). But the same run proved the **image plane is down** — so (a) would bulk-write image-blind ≤0.6-confidence units across HSG (18 windows) and VHM. Reliability before coverage; 2x is a real ETA win with bounded blast radius and is one boolean to revert.
- **what-considered (quality):** the relayed hypothesis was "flat confidence=0.55 is a hardcoded/fallback value introduced by the fix".
- **why-change: REFUTED, and this is the load-bearing finding of the tick.** `bctcSanityValidator.ts` can only pass confidence through (`:134`) or clamp to `0.4` (`:149`) / `0.1` (`:163`) — **it can never emit 0.55**, so the value is the agent's own. All three 0.55 units carry `flags:["image_unavailable:text_only_parse"]`, and `table-page.md:55` / `continuation-stitch.md:60` mandate `confidence ≤ 0.6` on image failure. **0.55 is correct behaviour under a degraded input.** The defect is one layer up: 0 image failures in the 14:07Z batch vs **3/3 in the 16:37Z batch on the same report**. Minted `FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE` (P1) with an explicit "do NOT close this by adjusting 0.55 or the ≤0.6 cap" clause, and an AC1 that forbids assuming a code regression before comparing pdf-extractor availability across the two windows (a transient outage routes to ops, not developer).

## D-4 — A-30 rag-service ×2 → fold, not mint

- **only path:** `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` (review[]) is the exact tracked root cause. Corroborated the premise independently rather than trusting the auditor's percentage — `docker stats` 983.8MiB/1GiB = 96.07% against a real `HostConfig.Memory=1073741824`, so **not** the known denominator-false-spike class.
- **why-change:** added a QA blocker to the target row. Its fix commit `0308514f5` is on main, but `RestartCount=0` / `StartedAt=2026-08-06T12:57:42Z` is **before** the 18:33Z fix commit — the idle-unload path is not running in the live process at all. Signing off on a code read would have been a textbook false-green (`feedback_passive_health_masks_dead_data`).

## D-5 — Manual-dispatch sweep: deliberate tie-break override

**task_id:** FIX-LEAF-AGENT-ANALYSIS-ONLY-EXIT-NARRATES-INSTEAD-OF-EXECUTING

- 42 eligible candidates; several at `rank=1`. The sweep's default tie-break is board index → `FIX-CHEF-MIDFLOW-BAIL-DETERMINISM` (idx 200) over this row (idx 353).
- **why-change:** overrode on `main.md`'s own stated priority order (*recurring bugs → UNBLOCK → FIX*). 5 defective occurrences across 7 spawns of the same leaf-agent narrate-instead-of-execute pattern, and it **invalidates the self-reports every other triage in this system consumes, including this one**. It was already folded into a BATCH at 2026-08-06T22:05Z and never left `backlog[]` — the exact TE-T12 strandedness the sweep's re-admission window exists for. Override logged on the row itself, not just here.

## D-6 — Signal-type coverage guard has re-drifted

- Ran `guard_signal_type_coverage` live: **FAIL**, 8 unrouted `to=po` types (`agent_flow_defect`, `bug-escalation`, `db_anomaly_illiquid_ohlcv`, `decision-needed`, `dispatch-race-observation`, `notebook_concurrency_gap`, `recurring-bug-corroboration`, `recurring_defect_unfixed`). Recorded PASS on 2026-08-01; **both** of this tick's own Pipeline-B signals were unrouted types I had to hand-route.
- **only path:** folded onto `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` (review[]) as a QA gate rather than minting a 7th row — that row IS this defect, and it is about to be signed off while the defect has already recurred. Recommendation on the fold: derive `$routed` from the tables themselves; a second hand-maintained literal will drift again.
