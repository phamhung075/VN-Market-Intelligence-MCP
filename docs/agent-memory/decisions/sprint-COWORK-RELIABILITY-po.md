# Decision Journal — Sprint COWORK-RELIABILITY · po

**Sprint goal:** Close recurring cowork signal/synthesis/publish-marker reliability defects (drain, filename collisions, marker key/lifecycle).
**Agent:** po
**Started:** 2026-08-07T05:45:00Z

---

### STEP po-1 · po · 2026-08-07T05:45:00Z
**task-id:** FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING
**what-done:** Read `docs/architecture-briefs/2026-08-07-cowork-signal-filename-cycleid-keying.md` in full and the live `task_board` row's complete `architect_review_note`. Adjudicated both § 10 open items. Advanced the row `architect → pm` with `po_goahead_20260807T054500`; `supervised`/`plan_only` preserved. Recorded three BINDING amendments in `po_architect_signoff_20260807T0545`. Synced `.head` (was stale at `next_agent=architect`, 04:33:33Z). Minted `FIX-CHEF-INTRADAY-MARKER-KEY-UTC-HOUR-BASIS-MIGRATION` (backlog/BLOCKED). Folded FR-4 onto `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` as occurrence-3 corroboration.

**what-considered:**
- **FR-4 (tnb write-serialization) — architect's option 1 "monitor, no action":** REJECTED. Its premise ("unreproduced since the 2026-07-29 fix") is a per-FILE null result, not a per-MECHANISM one. The architect's `git log` was scoped to `tran-ngoc-bau.md` alone; inside that same window the mechanism recurred on `qa.md` (2026-08-06T17:21:01Z, cycle-522/523 lost to a full-overwrite race — signal row `dev-20260806T172101-qanb`, triaged by me as occurrence 2). A "monitor" disposition also needed a named monitor: I checked `scripts/git-hooks/pre-commit:310-382` and the AC-2a immutability guard is fleet-scoped and does cover tnb — but it treats an authorized whole-section DROP as legal, which is exactly the shape this race produces. So the monitor would not have monitored.
- **FR-4 — architect's option 2 "commission a cheap `task_claim` read-lock now":** REJECTED as a duplicate. `FIX-NOTEBOOK-WRITE-AC7-SKILL` (BLOCKED, P1, agent-father) already specifies that exact mechanism verbatim, TTL carve-out included.
- **FR-4 — DESCOPE and fold (option 3, not offered):** CHOSEN. A three-row chain already owns it end-to-end — `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` (tracking) → `FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` (enabling Zod enum) → `FIX-NOTEBOOK-WRITE-AC7-SKILL` (the design). Zero tnb files in this row's handoff; brief § 9 already maps no AC to FR-4, so no acceptance criterion moves.
- **EC-2 Phase 2 — fold into this row's handoff:** REJECTED, agreeing with the architect. It reopens scope the sibling ANCHOR row deliberately excluded, it is gated on an unshipped dependency (folding it would block this row's ship on someone else's), and a live publish-mutex basis change carries double-publish risk that needs its own AC.
- **EC-2 Phase 2 — separate row, minted later "once Component A ships" (architect's timing):** REJECTED. That is a consumer with no producer — nobody watches for the landing. Same defect class that idled `FIX-WF2-SUPERVISED-HOLD-NO-PO-SIDE-GOAHEAD-PRODUCER` and left `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER`'s P0 dormant 6+ days.
- **EC-2 Phase 2 — separate row, minted NOW as BLOCKED with `depends[]`:** CHOSEN. The dependency lives on the board where the machinery can see it, not in a brief's § 10 prose.
- **Return the brief to architect over the FR-1 midnight-straddle gap:** REJECTED. The gap is real but precisely specifiable in one sentence; a full architect round-trip costs a cycle and would change nothing else. Attached as a binding amendment instead — the same handling the BA spec got (ratified with one recorded relay correction).

**why-decision:** The brief is sound; both flagged items were mis-framed rather than mis-designed. FR-4's two options both assumed the mechanism was unowned — the board says otherwise, and the deciding evidence (the qa.md recurrence) sits on a plane the architect never looked at. EC-2's split is right and only its trigger was unowned. The one substantive technical gap I found is in FR-1's fallback, which the brief itself declares the ACTIVE day-one path: `WINDOW_KEY = <date of live_mcp_fetched_at> + CRON_HOUR` re-creates this row's own collision class at the UTC day boundary — `bctc-analyst-slot-4`'s cron is exactly `0 0 * * *`, so a fire a few minutes early keys to the previous day's window and clobbers a 24h-old file, and a slot-3 run delayed or re-run past midnight keys to a future window the real fire will then collide with. Snapping to the NEAREST `CRON_HOUR:00Z` occurrence (not "most recent at-or-before" — only "nearest" is correct in both the early and late directions) closes it with no new I/O and no signature change.

**why-change:** Three changes from the architect's plan. (1) FR-4 removed from scope entirely rather than dispositioned within it. (2) EC-2's follow-on row minted this tick as BLOCKED rather than deferred to an unowned future trigger. (3) FR-1 fallback branch 2 gains a nearest-occurrence rule plus two unit cases (slot-4 @ D 23:57Z → (D+1)T0000Z; slot-3 @ (D+1) 08:00Z → (D)T2100Z).

### STEP po-2 · po · 2026-08-14T15:01:13Z
**task-id:** TASK-COWORK-SIGNAL-DERIVE-WINDOWKEY
**what-done:** Signed off the derive_window_key() spec DONE_VERIFIED with binding Amendment 4 (branch-2 cron-hour domain guard + §7 shared-consumer over-claim corrected); parent left BLOCKED/next_agent=po.
**what-considered:**
- Return for rework — rejected: defect is documentary + a 3-line guard on an unimplemented artifact; round-trip costs more than the fix.
- Sign off clean — rejected: §7's "serves either writer" is false at source; a future implementer would wire a range-hour cron in and re-ship the collision class.
- Sign off + binding amendment — CHOSEN.
- Escalate BLOCKED — rejected: nothing external is missing; the block is PO's own review debt.
**why-decision:** Same precedent as this row's parent at po_architect_signoff_20260807T0545 — approve in place with acceptance-bearing amendments rather than bounce. Algorithm verified correct: both PO-mandated test cases re-derived independently and reproduce to the second.
**why-change:** Amendment 4 was not in plan — surfaced only by checking the function's cron-field assumption against all 23 live slots instead of the 4 the spec cites.

### STEP po-3 · po · 2026-08-14T17:34:18Z
**task-id:** TASK-COWORK-SIGNAL-BCTC-REKEY
**what-done:** Signed off the bctc-analyst FR-2/FR-7 rekey spec DONE_VERIFIED (review[] → done_verified[], one write) with binding Amendment 5: (a) the four `derive_window_key()` arguments are unbound at the only call site in the decomposition, (b) §10's "PM never minted the handoff stubs" claim struck as false, (c) `cycle.md` line anchors stale +9. Parent left BLOCKED/next_agent=po at 2/4 children reviewed. Amendment 5 text appended to the spec doc as §11.
**what-considered:**
- **Sign off clean** — rejected. Amendment 5(a) is acceptance-bearing, not stylistic: task 1 §1 says all four inputs arrive from the caller's session state, Amendment 4(a) makes this row the *sole* consumer, so §2 here is the only place they ever bind — and it binds none. Live counts in `docs/agents/bctc-analyst/flow/`: `prompt_text` 0, `cowork_schedule_json` 0, `live_mcp_fetched_at` 0, `slot_id` 1 (itself an undefined placeholder). An implementer following §2 verbatim gets an empty WINDOW_KEY, the filename collapses to a constant across all 4 slots, and the row silently re-ships the very collision it exists to close.
- **Return for rework** — rejected. Every substantive element verified correct at source (line-114 anchor, the FR-7 gap, the §5d-1 token, the genuinely-stale `stage-consolidate.md:64` xref, the NFR-4 fingerprint no-op at `drain-signals.js:251-252`). The three defects are additive corrections to a document nobody has implemented; a round-trip costs a full dispatch cycle for ~15 lines, on a row already idle 6 days.
- **Adjudicate the parent gate / lift plan_only and dispatch implementation now** — rejected. The parent's `supervised_note` conditions shipping on parent re-adjudication, which cannot be honestly discharged on a partial child set; 2 of 4 children are still unreviewed, and Amendment 5(a)'s `live_mcp_fetched_at` residual may need a bootstrap-side change that task 4's naming-contract review could bear on.
- **Escalate BLOCKED** — rejected. Nothing external is missing; the only blocker is PO's own remaining review debt.
- **Sign off + binding amendment** — CHOSEN, same precedent as po-1 and po-2.
**why-decision:** The deliverable is sound and is not being changed. The one real hazard is *caller-side* and invisible from inside the document — it only appears when task 1's "already in session state" contract is checked against what bctc-analyst's flow actually defines, which is nothing. Amendment 4(b)'s fail-loud guard cannot catch it because the bad value arrives before the parse.
**why-change:** Amendment 5(b) was not in plan. It surfaced from re-checking a claim of *absence* rather than accepting it: the four PM stubs `docs/handoffs/TASK-00{1,2,3,4}-*.md` demonstrably exist (2026-08-07 07:59), and `TASK-002-bctc-analyst-rekey.md` carries both items the spec lacks — the argument sourcing (its AC-2) and NFR-2 no-Bash (its Implementation Note 2). That is the third inherited-unverified-claim defect on this one chain, so I recorded a standing rule on the parent for the remaining two reviews: treat every assertion of absence as unverified until re-checked at source.
