# PO Decision Journal — triage 2026-08-06T12:29Z (market.db WAL re-arm, 5th corruption)

### STEP po-S143 · po · 2026-08-06T12:29:36Z
**task-id:** FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH
**what-done:** Reopened REVIEW→READY, P1→P0, widened from 2 call sites to all 4 + tests. The premise I was handed ("a REVIEW ticket would make this worse if merged") was falsifiable and false: the code already merged 6 days ago (e370f5f51, 2026-07-31T00:45Z, on main). The fleet is already at 4 re-armers, not 2.
**what-considered:**
- (a) block the merge / hold at QA — REJECTED, nothing to block, it is in HEAD
- (b) mint a separate P0 row for the 2 "original" re-armers per brief §3, leaving this row in REVIEW — REJECTED
- (c) reopen + widen THIS row to all 4 call sites, one owner — ADOPTED
**why-decision:** (b) was the architecture brief's own recommendation, and I overrode it deliberately. It was right on 07-30 when the 4 sites were untouched; it is wrong now that a fix landed on 2 of them in the wrong direction. Two rows against 4 DSNs in the same Go package is exactly the geometry that produced the split — one row "corrected" 2 sites toward WAL while another would correct 2 sites away from it. Splitting again guarantees re-divergence. Also decisive independent of the corruption argument: 2 of this row's own 3 ACs were never implemented (fetchers.go:312/:325 still `//nolint:nilerr`; fetchers_test.go fixtures still open `?_journal_mode=WAL`, the exact masking fixture the row warned against). QA had to fail it on its own acceptance text.
**why-change:** PO owns the inversion. I minted this row at 2026-07-31T00:07Z citing foreign_flow_repository.go/room_event_repository.go as "the correction already applied to 2 of 4 call sites". The architect brief published 14h earlier names those same two files as "a live landmine ... the single most actionable finding in this brief" and prescribes the opposite fix shape. I read a landmine as prior art and told a developer to propagate it. Root-cause lesson: a sibling file's pattern is not prior art unless you check whether anything has condemned it — "matches the neighbours" is a consistency argument, not a correctness one.

### STEP po-S144 · po · 2026-08-06T12:29:36Z
**task-id:** FIX-MARKETDB-JOURNALMODE-GUARD-SHIPPED-BUT-NEVER-ARMED
**what-done:** Minted P0. `scripts/audits/verify-market-db-journal-mode.sh` is correct, self-tested, and wired to nothing — repo-wide grep returns only its own usage header and two decision journals. Zero call sites since 2026-07-30. Also folded the deferred DELETE-vs-WAL policy question in here as AC-4 rather than opening a spike.
**what-considered:**
- (a) open a fresh SPIKE to resolve SPIKE-...-HARDENING AC-B DELETE-vs-WAL policy — REJECTED
- (b) patch re-armers only, treat detection as follow-up — REJECTED
- (c) arm the existing guard + add a source-grep sibling + write the policy SSOT — ADOPTED
**why-decision:** (a) rests on a stale premise. AC-B is not unresolved: the architect answered AC-A..AC-D in §4 of the 2026-07-30 brief, and for market.db the verdict is settled (DELETE, sole owner schema.ts, §2.4 durability reasoning). The only genuinely open part of AC-B is coordination.db, where the verdict is "do NOT blanket-convert, measure lock-hold under DELETE in a fixture first" — separate row, not a blocker. Re-spiking would have burned a cycle re-deriving a week-old answer while the vector stayed live. The real gap was never the decision; it was that the decision had no enforcement and no discoverable home, which is why a developer re-derived the opposite conclusion 14h later.
**why-change:** Escalated to P0 alongside the code fix rather than filed as hygiene. 5 occurrences, 4 re-armers, 3 services, 2 languages, 1 week. Patching call sites is O(n) against a stream of new writers; an armed guard is O(1). Per feedback_recurring_bug_escalation (2+ recurrences ⇒ structural fix before further point patches), I recorded a no-further-one-off-patches hold on the stock-price row covering both planes.

### STEP po-S145 · po · 2026-08-06T12:29:36Z
**task-id:** FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT
**what-done:** Returned REVIEW/qa → READY/developer with 3 added ACs. Did NOT revert the named-volume migration. Proved the immutable=1 defect live rather than relying on two contradictory memory entries.
**what-considered:**
- (a) sign off as-is, file immutable=1 as follow-up — REJECTED
- (b) revert the whole row — REJECTED
- (c) keep the migration, block sign-off on a -wal guard AC — ADOPTED
**why-decision:** Measured it, did not infer it. Live market.db with -wal at 1,545,032 bytes pending: `immutable=1` → daily_ohlcv=775891, journal_mode='delete'; `mode=ro` → 775912, journal_mode='wal'. Two defects in one reading: 21 rows silently missing, and — far worse — the probe reports the SAFE journal mode at the moment the DB is in the DANGEROUS one. The integrity observer would have certified as clean the exact condition that has corrupted this DB five times. (b) rejected because the migration itself is a strict improvement that ended 21 days of fail-open. (a) rejected because signing it off stamps "verified" on a probe that is provably inverted on the one field that matters.
**why-change:** The two prior memory entries gave opposite unconditional verdicts on immutable=1 (06-25 "correct", 07-31 "silently stale"). Both are consistent under one conditional rule — safe iff -wal is empty at read time, silently wrong otherwise — and the 06-25 measurement simply happened during a checkpointed moment. The developer's AC-2 evidence ("byte-identical counts, 6/6") is the same single-snapshot shape and establishes nothing general. Hence AC-5's verification gate mandates evidence captured with -wal deliberately non-empty.

### STEP po-S146 · po · 2026-08-06T12:29:36Z
**task-id:** FIX-MARKETDB-BACKUP-NO-INTEGRITY-GATE-SINGLE-GENERATION
**what-done:** Minted P2 in backlog from ops's flag. Kept at P2, not bundled into either P0.
**what-considered:** (a) bundle into a P0 — REJECTED; (b) standalone P2 — ADOPTED
**why-decision:** It reduces blast radius, not recurrence; the two P0s reduce recurrence. Folding a defence-in-depth item into an active-incident fix is how P0s slip. Near-miss is real though (07-30 needed `.recover` salvage because the only backup was already overwritten — market.db.impoverished-backup, 16.9MB from a 400MB DB), so it is filed with a fixture-proof AC, not left as prose.
**why-change:** no change from plan.
