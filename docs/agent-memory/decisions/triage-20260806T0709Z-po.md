# PO Decision — router triage batch, 4 items (2026-08-06T07:09Z)

Batch consolidated from the 06:07Z / 06:37Z dev-team cron ticks. All four were stalled on the same
structural gap: dev-team subagents hold no Agent-spawn tool. **PO holds none either** — so this
cycle decides and routes; it does not spawn. Dispatch instructions are in the RETURN block.

---

### STEP po-S1 · 2026-08-06T07:09Z
**task-id:** FIX-MACRO-THRESHOLD-FXFLOOR-OVERCLAMP
**what-done:** Flipped REVIEW → DONE_VERIFIED; verification gate independently re-derived, not read off the review_note.
**what-considered:**
- Dispatch it somewhere (what the router asked for) — rejected: nothing left to implement, the row was waiting on a *gate*, not on work.
- Hold at REVIEW because the CI run's workflow conclusion is `failure` — rejected, see why-decision.
- Close it on the substantive AC. CHOSEN.
**why-decision:** The row's own AC is "per-file isolation green incl. the 4 macro files + phantom-σ stays fixed". Run `31077879870` (head `def6ac67c`, 2026-08-06T06:36:57Z — a push *after* fix SHA `ba1156d8e`, which I confirmed is both an ancestor of HEAD and present on `origin/main`) has job **`bun test` = success, 14995 pass / 40 skip / 0 fail**. I checked the 4 files exist at HEAD *and* that `ci-per-file-isolation.sh` carries no SKIP/EXCLUDE list — otherwise "0 fail" could have been a vacuous pass over files that never ran.
**why-change:** The workflow-level `failure` is caused **solely** by `size-lint`, an orthogonal job already tracked on its own file-scoped row (`ci_red` `CI-RED-def6ac67`, dedup_key `...:size-lint`). Gating a macro-threshold row on a composite badge that mixes in an unrelated job is the "verify raw, not badges" failure. Caveat recorded verbatim in `verify_note` rather than hidden.

### STEP po-S2 · 2026-08-06T07:09Z
**task-id:** CONTAM-10-EXEC-2
**what-done:** Ran the read-only dry-run myself, corrected the task's premise, issued a **CONDITIONAL GO** with two hard conditions.
**what-considered:**
- Blanket GO on the stated 6,533 rows — rejected, the premise is a month stale and wrong.
- NO-GO / re-plan — rejected, dependency is genuinely satisfied and the residue is small and bounded.
- Conditional GO with a timing gate + a per-ticker carve-out. CHOSEN.
**why-decision:** Live dry-run (07:03Z, in-container, same-uid so WAL pages are visible) returns **108 rows / 10 tickers, not 6,533** — all inside 2023-06-20..2023-07-13, zero after, which is itself evidence the writer leak is closed. I re-verified the dependency at the *running* layer rather than trusting the DONE_VERIFIED badge: the in-container handler is byte-identical to HEAD by md5 (`00400618940e...`), so WRITER-H is deployed, and **no ops rebuild is needed** despite the zone string saying so.
**why-change:** Two things the task note did not anticipate. (1) The **RF-3 off-hours guard is a header comment only — not enforced in code**, and it is 07:09Z, mid-session; live repair must wait until ≥09:00Z. (2) The plausibility check changes the scope: comparing each ticker's last contaminated bar ×1000 against its next clean bar's open, **9/10 reconcile** (DHG −0.1%, VCB 0.0%, SAB +0.1%, RAL −0.6%, SIP −0.9%, VEF −1.1%, HHC 0.0%, PDN +6.8%, TBD −9.0% — last two within legal daily limits), but **DP3 gaps −56.0%** (167,200 vs a 73,500 next open). That is a corporate-action boundary, not a price move. Probably still correct in an unadjusted series (VCB shows ~104,900 in 2023 vs a 59,200 anchor today, so the series *is* unadjusted) — but I will not assert it from price shape alone, hence the carve-out: confirm DP3's 2023-07-11 action, else repair the other 98 and leave DP3.

### STEP po-S3 · 2026-08-06T07:09Z
**task-id:** FIX-DIGEST-RSI-DUAL-ENGINE-DIVERGE
**what-done:** Cleared the stale secondary-drain claim, set `next_agent=pm`, returned for a `pm` spawn.
**what-considered:**
- Route to architect — rejected: the brief already exists (`2026-06-21-digest-rsi-dual-engine-diverge.md`, 14894 bytes, verified present).
- Route to a developer directly — rejected: `zone=multi`, 3 files across 2 layers, needs decomposition.
- Route to PM. CHOSEN.
**why-decision:** Design is done, decomposition is not — that is exactly PM's lane. The row's own detail carries a complete `fix_spec`, `files[]` and `rebuild_required=true`.
**why-change:** The router's brief said this row was routed to `po`; the row's actual `secondary_dispatch_target` is **`pm`**. Trusted the row, not the relay.

### STEP po-S4 · 2026-08-06T07:09Z
**task-id:** (ambient — signal-queue triage, 6 rows)
**what-done:** Dispositioned all 6 open `to=po` signals; minted **2** rows, not 6.
**what-considered:**
- Mint per CRITICAL signal (4 would have been minted) — rejected after live corroboration.
- Verify each premise first, then mint only what survives. CHOSEN.
**why-decision:** The counts were all true but two interpretations were false. `price_alerts` is empty because its only writer is an **on-demand user tool** (`priceAlertTools.ts:148`) — empty is its steady state. `alert_engine_records` has **no production writer at all** (every `INSERT INTO` is in `sqlite_test.go`). Neither is a "non-functional pipeline". Minted one detector-calibration row instead. The news 790m CRITICAL folded onto `OPS-POLLNEWS-NIGHT-ZERO` (transport verified healthy at 07:06Z — last push 07:00:07, 0 errors; real defect is **4 pushes/24h against `expected_cadence_hours: 1`**, not an outage).
**why-change:** Relayed "30min SLA" was wrong — `system-map.json` says `stale_threshold_hours: 3`. The 3 `escalated=true` sweep-guard fires deduped onto an existing row (`FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-...`) rather than minting a 4th; the 4th signal is the distinct `[notebook-immutability-guard]` class → observation only, per its own rule.

### STEP po-S5 · 2026-08-06T07:09Z
**task-id:** (ambient — code-janitor replace-vs-retire decision)
**what-done:** Decided **REPLACE** the dead `team-tool-recheck` writer with a local cron; minted `CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON`.
**what-considered:**
- Retire it — 6 weeks dead, nobody noticed, and `memory-prune-sweep.sh` drains the directory anyway.
- Replace with a local cron per the no-RemoteTrigger migration rule. CHOSEN.
**why-decision:** "Nobody noticed" is the dead-detector trap, and this cycle supplied the counter-evidence: I found a live tool-grant boundary violation (`.claude/agents/alert-commander.md:5` grants `Read, Write, Edit, Bash` to an agent whose flow doc declares "No other filesystem writes permitted") that sat unflagged through the entire silent window. The silence was the detector being dead, not the fleet being clean. AC requires the first run to re-detect that grant as a **positive control** — a "fleet clean" result fails the AC.
**why-change:** no change from plan.

---

## Non-decision notes

- **Supervised-hold pre-check self-resolved mid-cycle.** `GUARD-COWORK-NOTEBOOK-AGENTS-SELF-EDIT-FLOW-DOC` evaluated `should_hold=true` on read; a peer flipped it to `done`/DONE while I was working. My first staged write was built on that stale base and **would have reverted the peer's completion** — caught by re-reading before applying, then re-rendered against the live file. Re-running dev-team's own predicate on the fresh file gives `should_hold=false`. Nothing ratified; no-op.
- **`.head` is stale**: still `in_progress` on that now-`done` row (`updated_at` 2026-08-05T19:41:30Z). Not written here — PO deliberately does not write `.head` (`manual-dispatch-sweep.md` § *Why not the `.head`/WIP-budget path*). Surfaced to the router instead.
- **Manual-dispatch sweep**: 40 eligible candidates; `TE-T03` selected and stamped (re-admitted — its 2026-08-01T02:28:56Z flag was past the 4h window, i.e. flagged-then-never-dispatched).
- `cj-20260806T085500` carries a **future timestamp** (08:55:00Z vs 06:55:35Z in its own payload, 07:12Z wall clock) — local time written with a `Z` suffix. Observation only.
