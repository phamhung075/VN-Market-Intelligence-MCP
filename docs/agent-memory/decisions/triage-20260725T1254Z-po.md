# PO Triage — A-30 veto-gate defect 2026-07-25T12:54Z · mint-vs-fold ruling (ONE finding)

**Agent:** po · **Referred by:** router session `ad265f86` (`mode=triage-decision`, router mints nothing)
**Journal note:** standalone triage record (precedent: `triage-*Z-po.md` in this directory).
ONE board write this tick — a single atomic `orch-apply` transform carrying the mint plus one
back-reference. Recorded as one STEP.

---

### STEP po-S178 · po · 2026-07-25T12:56:44Z
**task-id:** FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE (mint)
**what-done:** Minted the row into `task_board.backlog` (P1/S/`cross-service/`, owner+next=`architect`,
`supervised:true`, `plan_only:true`, `recurring_bug_count:5`) and appended it to
`FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE.related` in the SAME transform.
**what-considered:**
- (a) fold into `FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE` — rejected, see why-decision.
- (b) fold into `FIX-VPS-HEALTH-OFFHOURS-MASK-FALSE-GREEN` — rejected: same *class*
  (veto masks a genuine condition → false green), unrelated *substance*. That row is
  `apps/mcp-server/` TypeScript (`vpsProxyTools.ts` `formatHealth()`, `vpsHealthPoller.ts`
  `checkServiceFreshness()`). One row cannot span that and a system-auditor flow doc + a bash
  audit script without becoming `zone:multi` and needing an architect split anyway — i.e. the
  fold buys nothing and costs traceability. Cross-referenced in `related` instead.
- (c) reopen `FIX-AUDITOR-A12A20A30-FP-REEMIT-CONVERGE` — rejected: it is genuinely DONE on its
  own axis (the FP churn it killed was real, `recurring_bug_count=4`, false CRITICAL 07-23T03:42Z),
  and its QA gate was independently executed over two passes. Reopening a correctly-closed row to
  carry a NEW defect its fix introduced destroys the audit trail of both.
- (d) MINT — CHOSEN.
**why-decision:** Against the scope row the split is structural, not cosmetic: different file,
different plane, *opposite mechanism*. Scope row = `scripts/agents-flow/auditor-tier1-probe.sh:209`
`_check_mem_creep()`, the CRON-side docker-stats loop, false negative BY BLINDNESS (detector cannot
see 12 of 13 containers). This row = `docs/agents/system-auditor/flow/tier1-probe.md:167-171`, the
AGENT-side interpretation layer over `probe.sh:92`'s deep-probe subprocess, false negative BY VETO
(detector sees the container, computes ESCALATE, discards it). Zero file overlap. Decisive: the
scope row's own `note` — which I wrote at 08:56Z today — already ruled the merge out in advance:
"OUT OF SCOPE (do NOT fold in): an absolute-headroom secondary predicate … adding a second
predicate in the same change would confound the FP evidence for the first; mint it separately
later". This IS that separate mint, arriving earlier than expected because the veto plane produced
its own demonstrated false negative today.
**what-i-verified-myself (did not inherit from the router):**
- Read clause 4 verbatim at `tier1-probe.md:167-171`. Confirmed the router's reading AND found a
  second defect the router did not report: **clause 4 is ordered BEFORE clause 5's verdict mapping,
  so it pre-empts `OOMKilled=true → CRITICAL` and `peak >97% → CRITICAL` too.** A container that was
  genuinely OOM-killed and restarted presents a fresh process with `VmHWM > VmRSS` almost at once,
  so the confirmed-OOM CRITICAL branch is reachable in practice only when VM data is UNAVAILABLE.
- Sharpened the root cause past the router's framing. Router said the predicate "is true of ANY
  process that has ever freed a page". Stronger and checkable: **VmHWM is a monotonically
  non-decreasing high-water mark, so `VmHWM >= VmRSS` holds by construction and `VmHWM > VmRSS` is
  FALSE only in the knife-edge instant where current RSS equals the all-time peak.** It is a
  tautology, not a weak heuristic. Confirmed 3/3 against the router's samples — including a
  3-minute-old container at 10.4% of cap.
- Read `scripts/audits/verify-a30-mcp-memory-reclamation.sh:83,93-97`: the upstream script is NOT
  at fault. Its three ESCALATE conditions already discriminate, and it already computes the
  intra-window reclamation-DEPTH signal (`dips`, `dip_detail`) that the veto layer throws away.
- Corroboration BY ABSENCE re-checked at 12:50Z, not taken from the agent's assertion:
  `signal_queue.rows` 125 / `archive` 0; `notebooks/system-auditor.md` 186L; zero system-auditor
  commits since 12:00Z. Control row `cowork-20260721T232634Z-a30-mcp-oom-escalate` present,
  `status=triaged`, untouched.
- Stale-duplicate guard (`feedback_bounded1_spawns_health_recheck_stale_duplicate_fix_rows`):
  `git log --since=2026-07-23` on all three root_cause files returns ONE commit, `b9484fa7a`,
  touching the launchd ack-ledger arm only. Clause 4 is live and untouched at HEAD.
- Independent prior-art sweep (not inherited): depth-agnostic id sweep over all 814 ids for
  `veto|hwm|reclam|A-30|A30|memcreep|mem-creep|offhours|mask|false-green` → 10 ids, all inspected;
  id+title sweep → 62 rows, all read. `veto`/`hwm`/`reclam`/`MEM-CREEP` return ZERO ids.
  Router's fold list confirmed correct and complete.
**why-priority-P1-not-P0:** The demonstrated harm was mitigated out-of-band (router escalation →
ops swap 12:45:09Z) and the underlying leak has its own open row. `ready[]` already carries 18
starving P0s; adding a 19th dilutes the signal rather than sharpening it. Recorded instead as the
*highest-severity P1 in the A-30 family*, to be sequenced ahead of the scope row.
**why-supervised+plan_only:** This is a RECURRING FAILED FIX, not a recurring bug — the previous
fix INTRODUCED this defect (`feedback_recurring_detection_vs_recurring_failed_fix`). The implementer
must not self-certify. `architect` (NOT `agents-architect`) is deliberate: the maintenance-lane
next_agent gate would withhold the row otherwise.
**why-i-did-not-prescribe-the-predicate:** Router named three candidates and explicitly withheld
prescription; I hold no data to choose between them (growth rate is unresolved and cadence-limited).
Instead I bounded the design with an acceptance test of *information content*: the replacement must
return DIFFERENT verdicts for the 12:42:26Z sample (98.1% of cap) and the 12:47:46Z sample (10.4%
of cap). Any predicate that answers both the same is rejected by construction, whatever its
threshold. That constrains the outcome without choosing the formula — and without re-introducing
the flat-threshold FP churn the converge row eliminated (rag-service's legitimate ~95-99% plateau).
**what-i-explicitly-did-not-do:** Deliverable (a) is a STOPGAP that is *precedence only* — no
threshold decision — restoring the unvetoable CRITICAL branches. I recorded in the row that it does
NOT fix today's miss (that was the `min_pct>93 → WARN` branch and would still be vetoed), rather
than letting a partial fix read as a complete one.
**why-change:** No change from the router's dispatch — it delegated the disposition and I exercised
it. The one addition beyond the brief is the clause-4 precedence defect, found while verifying.

---

### Constraints carried into the row (so they are not re-derived downstream)

1. Veto INTENT was correct; only the PREDICATE is wrong. No wholesale revert.
2. No flat threshold across containers — rag-service's healthy set-point is legitimately ~95-99%.
3. Growth rate UNRESOLVED (7.25 / ~3 / 1.6-RETRACTED / ~4.7 pp/h). No pp/h figure, no 85% ETA from
   docker-stats spot sampling, anywhere downstream.
4. The 12:45:09Z swap reset the clock — the fresh climb series from 12:47:46Z is the cleanest
   observation window available and degrades with every subsequent restart.
5. `docs/data/auditor-tier1-last-healthy.json` shows `mem_creep: PASS` at 12:20:51Z. Logged in the
   row as an UNVERIFIED side-observation only — other plane, one sample, no raw capture, and
   commit `c7afe73ab` already flags that heartbeat sample as confounded. Not evidence for this row.

### Invariants held
`task_total` 652→653 (+1, the mint) · `signal_total` 125→125 (no signal_queue mutation) ·
ids 815/815 unique · `.head` untouched (`FIX-PREDCLAIM-BACKFILL-NULL-CREATIONPRICE`, `in_progress`,
dev-team session `e3a3f331` owns it) · zero runtime calls, read-only throughout.
