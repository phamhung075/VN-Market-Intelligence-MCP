# PO triage decision journal — 2026-08-26T17:54Z

task_id: FIX-DISPATCH-GATES-BLIND-TO-PROSE-DISPOSITION
Also folds: FIX-CCATO-NULLMARKER-SET-INCOMPLETE-2ND-TOOL-GETTECHNICALINDICATORS-NOTFOUND-SCORED-AS-CONTRADICTION,
FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG, FIX-BCTCANALYST-CARRYOVER-ESCALATION-NEVER-REACHES-BOARD-11-DAYS,
FIX-BCTCANALYST-MISSING-TABLEROWS-BALANCECHECKS-TOOL-GRANT, FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK,
FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE
Mints: FIX-COWORK-MUTEX-DROPPED-SLOTS-DISCARDED-BY-FINISH-META-DIVERGES-FROM-SLOTS
Cancels: CLEAN-RETIRE-TEAM-TOOL-RECHECK-HEALTH-DOC-FAMILY-DEAD-REMOTETRIGGER-WRITER, CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER
Input: 10 durable-inbox envelopes (self-read from `.dev_team_idle_chain.pending_triage_inbox`, matched the
caller-supplied convenience copy exactly: same 10 ids). Dispatched by dev-team tick 2026-08-26T17:37Z.

what-considered: for each of the 10 envelopes, mint vs fold vs skip. Chose FOLD on 7 (all had a
path-resolved covering row), MINT on 1 (no covering row, mechanism diagnosed from source), SKIP on 2
(routine bctc_signal with no data-pipeline defect), plus 2 CANCELs that were not in the inbox at all but
were forced by verifying one envelope's premise.
why-change: the only deviation from plan is the 2 cancels. Verifying the code-janitor envelope's premise
(mandatory for `system-issue`) surfaced a dispatchable board row whose instruction was destructive and whose
premise was falsified. Leaving it open was not a neutral act.

This file is the cold detail store for the three rows whose inline fold was refused by
`orch-apply.sh`'s row-prose-ceiling check (ORCH_ROW_PROSE_CEILING_BYTES=12000). Those rows carry a short
inline pointer + `detail_ref` to this file; the authoritative fold text is below.

Predecessor detail store for the cycle-snapshot row (NOT superseded — still authoritative for everything
before this tick): `docs/agent-memory/decisions/triage-20260826T1228Z-po.md`.

---

## §1 — FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG : fold of the HPG Q1-2026 envelope

 || FOLD (po triage 2026-08-26T17:54Z, bctc_signal envelope 7291410c291a, HPG Q1-2026, generated_at 2026-08-26T15:20Z). Recurrence confirmation, no new row. bctc-analyst reports operating_profit=0 for HPG UNRESOLVED FOR 41+ CONSECUTIVE CYCLES and stamps data_quality_flag "operating_profit=0 unresolved 41+ cycles -- infra/extraction issue, not this agent's fix". Same cycle also shows the downstream consequence this row predicts: LNST 9.056 tỷ (17.1%) exceeds reported gross profit 8.365 tỷ, tripping the analyst's own "possible non-operating income" validation warning, and ESC-4 was DISCARDED as un-evaluable with reason "operating_profit_0_corrupt_data". So the corrupt zero is not merely cosmetic -- it is disabling an escalation gate. Cycle count is the load-bearing new datum: this row was minted against a single observation; it is now a 41+ cycle standing defect.

---

## §2 — FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-... : fold of the 15:00Z + 16:00Z cowork-fire envelopes

 || FOLD, 2nd + 3rd consecutive same-tick confirmations (po triage 2026-08-26T17:54Z, cowork-fire envelopes cc50b047702a and 8591ee13c5e5). 15:00Z tick: Step 4.7 wrote docs/data/cycle-snapshot-15:04.json (17464B, carry.regime=NEUTRAL) and emit_pressure_state returned cycle_snapshot_promoted=false 3 seconds later (produced as 15:04 by wall clock, looked up as nominal 15:00). 16:00Z tick: cycle-snapshot-16:06.json (17025B, carry.regime=NEUTRAL), promoted=false 5 seconds later. With the 12:00Z tick (12:05) that is THREE consecutive fan-out ticks, a fresh valid snapshot on disk every time, unpromoted every time. Two escalations from this: (1) the 16:00Z occurrence was PREDICTED IN ADVANCE by the dispatcher and then observed, which upgrades this from deterministic-on-inspection to deterministic-and-forecastable -- there is no intermittency left to explain and no further reproduction is needed before implementing; (2) staleness only ever resets on a real fan-out, so the 12:15Z-14:45Z silent stretch (11 consecutive correctly-SILENT ticks -- no enabled slot carries cadence_minutes, all 21 are cron-driven, none matched) let the promoted snapshot age ~179 minutes unbounded. SCOPE NOTE for whoever takes this: ready[106] UC-SDF-P2 ("cycle-snapshot promotion dead since 07-07, tickHHMM-vs-FILE_TICK filename divergence") is the SAME root cause sitting in the same lane. Reconcile or explicitly subsume it before implementing -- do not let two ready[] rows be dispatched separately against one defect.

---

## §3 — FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE : fold of the code-janitor system-issue envelope

 || FOLD (po triage 2026-08-26T17:54Z, code-janitor system-issue envelope bd1cd6a9f54c, LOW). The hook fired its hardcoded premise AGAIN, and this time code-janitor caught and corrected it in-payload. PO verified the correction independently before accepting it (disk + git, not the payload's say-so): docs/agent-memory/health/ holds 15 team-tool-recheck-*.md files, newest team-tool-recheck-2026-08-26-1257.md written 14:58Z TODAY, with commits landing 08-08, 08-11, 08-12, 08-13, 08-14, 08-15, 08-18, 08-22, 08-23, 08-25 and 08-26 (ffc3951b3) under agent-father's local keep-cycle. The writer is ACTIVE. The "silent since 2026-06-23 / killed by the 2026-06-22 no-RemoteTrigger directive" premise this hook hardcodes has been false since at least 2026-08-06, when CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON shipped the local replacement. COST NOW MEASURED, and it is no longer just noise: the hook's dead premise propagated onto a SECOND board row and made it destructive. CLEAN-RETIRE-TEAM-TOOL-RECHECK-HEALTH-DOC-FAMILY-DEAD-REMOTETRIGGER-WRITER sat in backlog[] as status=BACKLOG owner=developer next_agent=developer -- fully dispatchable -- instructing a developer to permanently retire a doc family that is written to daily. Worse, its own fold note (dated 2026-08-23T17:37Z) restated the dead premise verbatim about five hours AFTER a PO pass had already retracted the identical premise on its sibling CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER at 12:32Z the same day. That is the escalation path this row's missing liveness predicate actually produces: not a duplicate signal, but a same-day self-contradiction that arms a destructive instruction. Both rows were cancelled to archive[] by this triage (po_cancel_ruling_20260826T1754Z). This row is now the ONLY open artifact for the class, and its AC is unchanged and still correct: give memory-prune-sweep.sh a real liveness predicate (newest file mtime / last commit touching the family) instead of a hardcoded date, so it stops emitting a decision-needed escalation for a decision that shipped on 2026-08-06.

---

## §4 — Cancel ruling (both CLEAN-RETIRE-TEAM-TOOL-RECHECK-* rows)

PO CANCEL RULING 2026-08-26T17:54Z. Both CLEAN-RETIRE-TEAM-TOOL-RECHECK-* rows are cancelled to archive[] as CANCELLED / do_not_reopen. Grounds: their shared premise ("the team-tool-recheck writer is dead, killed by the 2026-06-22 no-RemoteTrigger directive") is falsified by direct disk and git evidence -- 15 live files, newest 2026-08-26T14:58Z, continuous commits since 08-08 under agent-father's local keep-cycle -- and the decision they existed to force ("replace with a local cron, or ratify retirement") was already resolved in favour of replacement when CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON shipped on 2026-08-06. A row whose premise is false AND whose decision has shipped has no correct open form. The urgency was that one of the two was status=BACKLOG owner=developer next_agent=developer, i.e. dispatchable, and instructed permanent deletion of a doc family written to daily. Cancelled, not corrected, and not parked at BLOCKED -- BLOCKED has no automatic release path and parking is what left the sibling stranded for three days after its own retraction. The root-cause row FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE stays OPEN and now carries this incident's evidence; it is the only open artifact for the class. Reversal path if this ruling is ever wrong: both rows are preserved verbatim in archive[] with full rationale.

---

## §1a — FIX-BCTC-NONBANK-...-HPG : pre-existing `status_note`, moved VERBATIM out of the hot row

Moved by po/triage-20260826T1737Z because the hot row sat at 11984B against
`ORCH_ROW_PROSE_CEILING_BYTES=12000` — 16 bytes of headroom, i.e. the row could not accept even a
`detail_ref` pointer, let alone this tick's fold. `orch-apply.sh`'s row-prose-ceiling check names this
exact remedy ("route this row's prose through its detail_ref cold store"). Nothing was edited, condensed
or dropped — the text below is the byte-for-byte prior value. The hot row now carries a pointer to here.

```
AC-1 get_bctc_full({code:'HPG'}) returns a NON-ZERO Operating Profit consistent with the served P&L (gross_profit 8.365,1 tỷ and profit_before_tax 10.762,2 tỷ bracket the plausible range) — verify through the RUNTIME tool via the gateway, never off a host-side SQL read (feedback_host_cli_integrity_check_false_ok_verify_through_runtime). AC-2 EBITDA and structured_data.operating_cf are no longer hard 0 for HPG, or are explicitly served as null/N-A if genuinely unextractable — a corrupt 0 that reads as a real number is the actual defect, an honest null is an acceptable outcome. AC-3 ESC-4 on a fresh HPG bctc_signal returns a real true/false verdict instead of result='discarded'. AC-4 a plausibility check runs on the mapping: reject/flag any extraction where net_profit > gross_profit AND operating_profit == 0 (feedback_nonzero_values_need_plausibility_check — the inverse case, a ZERO that should have been rejected). AC-5 regression covers at least one non-bank ticker beyond HPG so the fix is not validated on a single file (feedback_fleetwide_gate_validated_on_one_file_optout_allowlist). NOTE the reflow dependency: fixing the mapping alone will not repair the already-stored 2026-Q1 row. Triaged 2026-08-12T17:00:39Z from bctc_signal HPG Q1-2026, RAW-verified live via gateway get_bctc_full.

[po/triage-2026-08-13T17:30:59Z] STALE-CLAIM RECLAIM in_progress[] -> ready[]. EVIDENCE (all read live this tick, none inferred): (1) `task_list_held` returned 20 live locks and NONE of them is this row's `task:<id>` — the claim lock is long expired (sprint-task TTL is 3600s; this row was claimed >24h ago); (2) the row's own `updated_at` has not advanced since 2026-08-12; (3) `git log --since=2026-08-12 --grep=<id>` returns ZERO commits. WHY THIS MATTERED FLEET-WIDE: `wip_in_progress` (scripts/lib/devteam-eligibility.jq:115-118) counts EVERY in_progress[] row whose status is not TERMINAL and not BLOCKED — it has NO liveness/lock check. With 3 such rows parked, wip_in_progress=3, which fails BOUNDED-1's `WIP<1` AND the shared `<2` budget of SLS (WIP2), RLC (WIP3) and DRS (WIP4) SIMULTANEOUSLY — all four auto-dispatch lanes were shut, so NOTHING on the board could be dispatched at all, regardless of priority. TWO of the three rows were claimed by `dev-team (ready-lane consumer)`: RLC claimed them, the spawned specialist never completed, and because its own dead claims still counted against its own budget, RLC could never claim again. The lane starved itself, and CI stayed RED for ~2 days behind it. Reclaimed to ready[] (NOT backlog — these were legitimately promoted and should stay dispatchable), placed AFTER this tick's P0 incident expedites so the CI/inbox unblocks go first. Same class as the FU-CNYVND-DEAD-FIELD-REMOVE incident the WIP formula's own history documents, recurring through a different door: that fix excluded BLOCKED status, but status is not liveness.
[po/triage 2026-08-14T20:02:29Z] RECURRENCE CORROBORATION. bctc_signal (bctc-analyst-slot-2, cycle 20260814-1800, ts 2026-08-14T18:05:45Z) reports HPG Q1-2026 operating_profit=0 unfixed for the 22nd+ consecutive cycle, plus a second internally-inconsistent scalar the row's AC-1 range check should also catch: profit-after-tax 9.055,9 ty EXCEEDS gross profit 8.365,1 ty. Still READY/next_agent=ba after 22+ cycles — this row has never been dispatched. Not re-minted (dedup hit on this row). || FOLD +1 occurrence, po triage 2026-08-25T01:07Z: bctc_signal envelope 13ae0312 (HPG Q1-2026, routine cycle) reports esc4 DISCARDED with reason 'operating_profit=0 (corrupt extraction, recurring 36th+ cycle)', alongside net profit 9,055.9 ty VND EXCEEDING gross profit. Same corrupt-zero scalar this row already owns, now self-counted at 36+ consecutive cycles. No re-mint. || [po/triage 2026-08-26T02:28Z] RE-DISPOSED - prior tick's premise is FALSE. It parked this BLOCKED/IMPL-ALREADY-LANDED-28f8509fc on 'only qa verification remains'. I probed AC-1/AC-2 through the RUNTIME (get_bctc_full HPG via gateway, 02:2xZ, as the AC demands): STILL RED 33h after impl. Operating Profit 0, EBITDA 0, operating_cf 0, while profit_before_tax 10.762.183,84 and gross_profit 8.365.068,61 are populated. AC-3's ESC-4 gate stays disabled. AC-4 did not visibly fire: net_profit 9.055.918,20 > gross_profit IS its trigger, yet output says only 'passed_with_warnings'. MISSING LEG (not qa work): payload has fetchedAt=2026-06-07T11:12:22.298Z, refine_status=PARTIAL - a STORED artifact. 28f8509fc changed the EXTRACTION path, which cannot repair a row written 11 weeks earlier. No AC asks for re-extraction, so as written the ACs are UNSATISFIABLE whatever the code does. DISCRIMINATOR: (a) stored-artifact staleness - re-extract HPG 2026-Q1, re-probe; (b) stale image - telegram 5163 (A-32, 08-25T03:56Z) says mcp-server ran 13h-older code; check the image label first. SCOPE: fix the whole corrupt-zero cohort (FRT/KDH/GVR/SHB/GEX/VJC/DBC/PDR total_assets=0), not HPG only. Keep in backlog[] - the ready-lane consumer reads NO prose.
```

---

## §5 — FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS : PO ruling + lane fix

[po 2026-08-26T18:02Z] PO RULING + LANE FIX (see key po_ruling_20260826T1802Z). Raised by the dev-team tick as "will not self-heal, worth a ruling", and that reading is correct: WF-1's BLOCKED carve-out only fires when .head is in_progress, .head is idle, and this row had sat in in_progress[] across many ticks holding the only WIP slot on the board while nobody was working it. RULING, three parts. (1) THE BLOCK IS SUBSTANTIVELY VALID -- it is NOT cleared. blocked_by names TASK-COWORK-MUTEX-001, which resolves live to task_board.review[10], status=REVIEW, next_agent=developer -- genuinely non-terminal. The dependency is real, so flipping this row to BACKLOG or READY would make a P0 claimable ahead of its own blocker. (2) THE LANE WAS WRONG, AND THAT IS WHAT THIS WRITE FIXES. A row blocked on an unmet dependency is not in progress. Moved in_progress[] -> backlog[] with status BLOCKED unchanged (lane-coherent: LANE_ALLOWED_STATUSES.backlog = {BACKLOG, BLOCKED}). This frees the sole WIP slot and changes no gate semantics. Nothing was unblocked, re-prioritised or re-routed. (3) THE REAL DEFECT, MEASURED THIS TICK: THE BLOCK WAS NEVER MACHINE-READABLE. This row carried blocked_by but NO depends_on. Grepped the gates: scripts/lib/devteam-eligibility.jq and the BOUNDED-1/SLS/DRS promote+claim jq all key on depends_on, while blocked_by appears ONLY at write sites and in the STUB_FIELDS keep-list of scripts/orch-backlog-stub.sh -- no eligibility predicate anywhere in the repo reads it (the field is already named write-only-decorative by FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE). So the ONLY thing holding this P0 back was status=BLOCKED: a manual freeze with no automatic release, the same anti-pattern ruled against elsewhere in this same triage. depends_on:["TASK-COWORK-MUTEX-001"] is therefore ADDED here, mirroring blocked_by, so the dependency is enforced by a predicate that actually runs. RELEASE CONDITION, now automatic instead of needing a PO tick to notice it: when TASK-COWORK-MUTEX-001 reaches a terminal lane, flip this row BLOCKED -> BACKLOG and deps_satisfied will gate it correctly on its own. SAME CLASS as this tick's headline finding -- see FIX-DISPATCH-GATES-BLIND-TO-PROSE-DISPOSITION key po_ruling_20260826T1754Z: a disposition or a dependency recorded in a field no predicate reads is not a control, it is decoration.

---

## §5a — same row : pre-existing `note` field, moved VERBATIM out of the hot row

Moved by po/triage-20260826T1737Z. The hot row serialized to 20197B against
`ORCH_ROW_PROSE_CEILING_BYTES=12000`, so `orch-apply.sh` refused any write that grew it further —
including this tick's ruling. Its `note` field (8671B) is reproduced byte-for-byte below and the hot
row now carries a pointer here. Nothing was edited, condensed or dropped.

```
ENABLER (new — no existing row covers it; the two per-slot guard bugs are separate). ROOT CAUSE: router CLAUDE.md/dispatch-claim §2.5 PRE-CLAIM keys on intent:<agent>:<intent-key> while cowork keys on cowork-slot:<slot> / cron:cowork:<tick> / published:<slot>:<periodKey> — DIFFERENT namespaces, so task_claim cannot collide across the two dispatch paths (no-op mutex by construction) and neither path consults the other. 2 CONFIRMED instances same tick 2026-07-15 (feedback_recurring_bug_escalation 2+ => rank-1-band candidate): (a) chef-evening/unified-agent -> MARKET double-publish ids 932+933; (b) tnb-audit/tran-ngoc-bau -> 2 concurrent full c110 audits, clobber avoided ONLY by the Edit stale-read check (the Write path has none => silent-clobber risk). CHEAPEST CORRECT FIX (ba/architect own HOW): router §2.5 PRE-CLAIM for a cowork-slot agent MUST also probe cowork-slot:<slot_id> and/or published:<slot>:<periodKey> before dispatch, OR key both paths on ONE shared task_id namespace so they collide by construction. Rule: do NOT dispatch a cowork slot agent via the intent path while the cowork dispatcher owns that slot. Distinct from UC-CCA-P3 (marker lifecycle) and FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK (capability gap) — this is the cross-path coordination hole that ENABLED both. PLAN-ONLY.

[po 2026-07-21T20:37:51Z 3rd CONFIRMED OCCURRENCE — P1->P0 ESCALATION, NO NEW ROW MINTED] Router escalated this as a fresh P1 root-cause ask; PO checked prior art first and this row ALREADY owns it exactly (title + note already name the disjoint-namespace root cause and the cheapest-correct-fix). Minting a sibling would have duplicated it — declined.

OCCURRENCE 3 (2026-07-21, RAW-verified via task_list_held, not agent self-report): 20:24:33Z cowork dispatcher session 9f4a6bfc-b001-4349-8a44-545f24c1b0ac claimed published:tnb-audit:2026-07-20/2026-07-26 (owner_agent tran-ngoc-bau) and its tnb instance began running. 20:25:14Z a SECOND router session 8def3b83-8326-4a26-a663-2f2616dfc38e claimed intent:tran-ngoc-bau:tnb-audit -> claimed:TRUE, 41s later, while the peer instance was live. Exactly the predicted no-op mutex: two namespaces, one agent, zero collision. Same slot (tnb-audit) as occurrence 2. Occurrence count is now 3 (07-15 chef-evening MARKET 932+933; 07-15 tnb-audit dual c110; 07-21 tnb-audit). feedback_recurring_bug_escalation: 2+ => block and fix ROOT CAUSE not symptom — this is the root cause row, and it has been dormant in BACKLOG for 6 days while the class recurred.

WHY P0 (priority inversion correction, NOT damage-this-time): this occurrence caused ZERO damage — see POSITIVE below — so the escalation is NOT argued on impact. It is argued on (a) 3rd confirmed occurrence of a class whose worst instance was a user-visible MARKET double-publish, and (b) priority inversion: this row is the ENABLER that UC-CCA-P3 (P0) explicitly names as the hole that "ENABLED both" — an enabler ranked below its own downstream symptom will never be reached first.

OPERATIVE BLOCKER IS DISPATCH, NOT PRIORITY (PO directive): this row is plan_only:true + supervised:true + next_agent:ba, and supervised_note states "Deliberate router/ba/architect dispatch is UNAFFECTED" — the hold only stops BOUNDED-1 idle auto-pickup. So the reason nothing shipped in 6 days is that nobody deliberately dispatched it, and a priority bump ALONE will not change that. PO AUTHORIZES + DIRECTS deliberate ba dispatch now. Keep supervised:true (it is correctly preventing the plan_only/no-detail-entry auto-pickup leak it was installed for — do NOT clear it as a shortcut to auto-pickup). ba should produce the spec for the cheapest-correct-fix already stated in this note (router PRE-CLAIM must probe cowork-slot:<slot_id> / published:<slot>:<periodKey> before dispatching a cowork-slot agent, OR both paths key one shared namespace).

POSITIVE — 07-15 PRIMARY DEFECT IS FIXED AND PROVEN (partially closes detail_ref handoff 2026-07-15-tnb-audit-double-dispatch-unreachable-marker-gate.md): that handoff s headline finding was that the Step G gate was DEAD CODE — .claude/agents/tran-ngoc-bau.md frontmatter lacked mcp__gateway__call_tool, so the gate was unreachable on every cycle and both 07-15 instances ran two full audits. VERIFIED FIXED: frontmatter now reads "tools: Read, Edit, Write, Glob, Grep, mcp__gateway__call_tool", and c115 (2026-07-21) is the FIRST cycle with the gate confirmed live — it fired, claimed cleanly, and the redundant session was blocked with no duplicate audit and no notebook clobber. DEFENCE-IN-DEPTH HELD. Residual risk is unchanged though: the inner marker gate is a per-agent LAST line of defence that only protects cowork agents that both have a marker gate AND gateway tools; the cross-path mutex hole itself is still fully open, and any slot agent without that gate is still unprotected. Do NOT downgrade or close this row on the strength of the gate working.

NOTE ON THE OTHER HALF: the router also fixed the gate s missing owner_client_session in commit 164f836f7 (Step G task_claim was failing zod on its first-ever live invocation) — that is a tran-ngoc-bau-local fix, already shipped, not part of this row s scope.

[ba 2026-07-23T09:30:55Z SPEC COMPLETE — PLAN-ONLY, zero PO blockers] Confirmed the 4 literal task_id formats live (intent:<agent>:<intent-key> / cron:cowork:<TICK> / cowork-slot:<slot_id> / published:<slot_id>:<period>) — disjoint keyspaces, task_claim is a no-op mutex across paths by construction. 7 FRs + 6 edge cases in ba_handoff: cowork-slot-agent recognition (cowork-schedule.json SSOT), intent-key->slot_id resolution (ambiguous for 8/9 multi-slot agents), 2 named collision-check candidates (read-probe vs shared-namespace) narrowed to both needing to gate on published:<slot_id>:<period> since cowork-slot:<slot_id> alone is released seconds after spawn and cannot anchor the fix, symmetric log+telegram+EXIT reuse, CLAUDE.md/SKILL.md lockstep-update requirement (pattern duplicated inline in both), live reproduction-harness requirement. Did not mint child board rows -- updated this same parent row per ba/flow/main.md Step 5 anti-dup-row precedent. NEXT: architect -- rule on candidate A vs B + multi-slot resolution rule.

[architect 2026-07-29T14:31:31Z RULING — Candidate A REFINED (not literal BA candidate, not B). Candidate B (shared namespace) REJECTED: forces router to duplicate each cowork flow's date-basis marker-key computation (4 publish_date_basis values + chef.md's own CYCLE_DATE_UTC/per-window quirks), which changed 3x live in the last 2 weeks -- DDD layering violation + proven drift risk, not cheapest. BA's literal Candidate A (probe exact published:<slot>:<period> string) also unnecessary: read live coordinationStore.ts/coordinationTools.ts and confirmed cron:cowork:<TICK> / cowork-slot:<slot_id> / published:<slot_id>:<period> ALL share task_kind=cowork-slot, so ONE task_list_held(kind=cowork-slot, expired=false) call + client-side prefix match on published:<slot_id>: (no period computed) closes the gap with zero apps/mcp-server changes and zero per-agent date logic at the router layer. New Step 2.4 (Cowork-Slot Cross-Path Collision Probe) inserted between Phase A.5 and Phase B in dispatch-claim/SKILL.md; CLAUDE.md gets a 1-line phase-list diff only (already a thin pointer post aef457f38, no re-paste risk). Multi-slot resolution (FR-2/EC-1): intent-key-IS-slot_id required convention (mirrors existing trigger_prompt slot=<slot_id> pattern, already unambiguous for tnb-audit today) with an ALL-SLOTS conservative fallback (check every slot_id the agent owns) when intent-key does not name a real slot_id -- fail-safe-conservative asymmetry (occasional over-block on rare manual dispatch vs recreating the exact double-dispatch defect class). 2 residual race windows named + bounded, NOT fixed this row (per EC-3): (1) probe-to-spawn TOCTOU, sub-second, 1-2 orders of magnitude smaller than the 33s/41s windows in all 3 confirmed occurrences; (2) cowork spawn-to-first-gate window (pre-existing, not created by this fix, not observed in any confirmed occurrence -- flagged as optional future row, not this one's scope). Full design + file-level ownership + FR-7 test harness spec: docs/architecture-briefs/2026-07-29-fix-cowork-dispatch-router-intent-mutex-bypass-design.md. NEXT: pm -- decompose into atomic dev tasks (CLAUDE.md 1-line diff + SKILL.md Step 2.4 addition MUST land same commit per FR-6; new scripts/agents-flow/*.test.sh FR-7 harness). Do NOT close/downgrade this row or its siblings (FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE, FIX-ROUTER-COWORK-SLOT-DEMAND-DISPATCH-BLIND) on this ruling alone -- SUPERVISED HOLD unchanged.]
```

---

## §6 — UC-SDF-P2 : manual-dispatch-sweep selection + archive dedup

 || [po 2026-08-26T18:06Z] MANUAL-DISPATCH SWEEP -- SELECTED AND FOLDED INTO THIS TICK BATCH. This row is a ready[] plan_only-XOR-supervised candidate (plan_only=true, supervised=false, next_agent=developer, owner null), i.e. reachable only by manual/PO dispatch and invisible to the automatic pickers; 4 such rows exist and this one is selected. WHY THIS ONE: its design is already DELIVERED, so dispatching it commissions no new design work. Both artifacts verified on disk this tick, not inherited -- docs/architecture-briefs/2026-08-26-uc-sdf-p2-filename-contract-fix-and-regime-volatility-writer.md (19465B, mtime 10:28Z) and docs/handoffs/UC-SDF-P2-BA-step60-hardening-spec.md (22997B). zone=multi is therefore already split by that brief; do NOT re-commission an architect pass. DUPLICATE WARNING, MUST BE RESOLVED BY WHOEVER TAKES THIS: ready[] also holds FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK (P0), whose defect IS this row unit 1. Two ready[] rows, one root cause. Reconcile or explicitly subsume before implementing, and do not let both be dispatched separately. That row now carries three consecutive same-tick reproductions folded on 2026-08-26 (12:00Z, 15:00Z, 16:00Z ticks) -- use them as this work fixtures. ARCHIVE DEDUP DONE (mandatory pre-dispatch check, by SUBJECT not id): archive[] and the 2026-06/07/08 cold archives hold FIX-CYCLE-SNAPSHOT-STALE-PROMOTE-FAILSAFE (DONE, 2026-06), EMIT-DARK-RECURRING (DONE), FU-TICK-SNAPSHOT-EMIT-DARK (DONE) and SPIKE-TICK-SNAPSHOT-DEADCODE-OR-REGRESSED (CANCELLED). NONE of them is the tickHHMM-vs-FILE_TICK filename-divergence defect, so this is NOT re-shipping archived work -- but the 2026-06 stale-promote failsafe is close enough in subject that the implementer should confirm this is not a regression OF that fix before writing new code.

---

## §6a — UC-SDF-P2 : pre-existing `po_corroboration_20260725T0948`, moved VERBATIM out of the hot row

Moved by po/triage-20260826T1737Z. The hot row stood at 11937B against
`ORCH_ROW_PROSE_CEILING_BYTES=12000` — 63 bytes of headroom, so `orch-apply.sh` refused to record even
the manual-dispatch stamp. The 3279B field is reproduced byte-for-byte below and remains authoritative.
Nothing was edited, condensed or dropped.

```
THIRD independent detection — folded, no new row minted (signal cwk-20260725T092802-7c2e, cowork-team). Priority P2 -> P1: the DIAGNOSE half of this row is now COMPLETE, it is a pure FIX with a fully source-verified root cause, and the dead-adaptive-layer blast radius is confirmed downstream. Detection history: (1) 2026-07-15 signal cow-20260715T195340 deduped here; (2) 2026-07-21 memory feedback_cycle_snapshot_promote_conservative_default_refuses_every_input; (3) 2026-07-25 this signal. VERIFIED AT SOURCE by PO this tick, not relayed: emitPressureStateTool.ts L359-362 derives tickHHMM from the NOMINAL tick_id (always :00/:15/:30/:45); L232 looks up docs/data/cycle-snapshot-<that HH:MM>.json; but docs/agents/cowork-team/flow/tick-snapshot.md Step 4.7 names the file by FIRE time (FILE_TICK=$(date -u +%H:%M)). All six on-disk snapshots sit at nominal+8..11min (00:10, 08:10, 09:08, 20:09, 20:26, 21:09) — none on a nominal boundary, so existsSync fails every tick and L233 returns {promoted:false, stale:false}. Because stale is ALSO false, stale_warning never fires: the failure is completely silent, which is why it survived 18 days (cycle-snapshot-latest.json content tick '19:45', created_at 2026-07-07T19:47:26Z). This is a producer/consumer filename-contract mismatch that NEVER worked — not a race and not a regression of FU-TICK-SNAPSHOT-EMIT-DARK's bash-fence defect. SECOND independent failure mode still applies and must be fixed too (from the 07-21 memory): even on a name match, the on-grid file is the dispatcher's OWN Step 4.7 tick-snapshot, which carries none of fetchedAt/created_at/macro_snapshot.fetchedAt, so the freshness gate treats it as Infinity-stale and refuses. Fixing only the filename leaves the promoter still refusing. DOWNSTREAM CONFIRMED DEAD: docs/data/pressure-state.json currently has regime_status:null, volatility_level:null, cycle_snapshot_promoted:null. FIX DIRECTION IS AN OPEN DESIGN CALL, deliberately NOT decided by PO (rename in Step 4.7 vs glob/most-recent lookup) — route to architect with the sequencing guard below. ABSORBED FROM SPIKE-TICK-SNAPSHOT-DEADCODE-OR-REGRESSED (now CANCELLED/superseded, its question answered by the above): SEQUENCING GUARD — DO NOT land cycle-snapshot key-unification / residue-file prune ALONE. Pruning the ~110 gitignored cycle-snapshot-<HH:MM>.json residue files flips every cowork tick to stale_warning=false -> adaptive fleet-wide, which activates the 240-min dangling-policy gate and degrades the alert-commander-market path 16x (15min -> 240min). Per handoff docs/handoffs/2026-07-16-cycle-snapshot-promotion-dark-9-days-adaptive-cadence-never-runs.md the coupling is 3-way + a 4th unit, and must land as ONE change set: (1) unify the snapshot key + put a date in it; (2) author the missing cadence-policy rows (alert-commander-market 15min + alert-commander-critical 240min + gatherer open/half_day tiers); (3) WIRE calendar_status to the vnTradingCalendar oracle (isVnTradingDay) — see UC-CDC-P1, which now carries live weekend-suppression impact evidence; (4) restore a writer for regime_status/volatility_level (dropped in the 2026-06-05 refactor) + a test that fails when the reader's source field has no writer. Couples with UC-CDC-P1 and FIX-COWORK-CADENCE-DANGLING-POLICY-ID.
```

---

## §5b — CORRECTION to §5 (self-issued, po/triage-20260826T1825Z)

**§5 above contains a false factual claim. It is corrected here rather than edited in place, so the
error and its refutation both stay on the record.**

**The false claim (§5, part 3):** *"scripts/lib/devteam-eligibility.jq and the BOUNDED-1/SLS/DRS
promote+claim jq all key on depends_on, while blocked_by appears ONLY at write sites and in the
STUB_FIELDS keep-list of scripts/orch-backlog-stub.sh — no eligibility predicate anywhere in the
repo reads it (the field is already named write-only-decorative by
FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE)."*

**The fact, re-derived at source 2026-08-26T18:2xZ.** `scripts/lib/devteam-eligibility.jq:232`
defines `effective_depends_on` as a union of **all three** field names:

```
((.depends_on | as_dep_array) + (.depends | as_dep_array) + (.blocked_by | as_dep_array)) as $inline
```

Line 236 applies the identical union to the detail-side item. All four pickers (BOUNDED-1, SLS, RLC,
DRS) inherit that predicate. **`blocked_by` is a live dependency field, not decoration.**

**Root of the error — a field-name conflation, not a bad measurement.** `blocks` (the *reverse*
edge) is the write-only-decorative field, and `FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE` is
about `blocks`, not `blocked_by`. §5 borrowed that fix's conclusion and attached it to the wrong
field. The distinction is stated explicitly in that fix's own implementation journal
(`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md:345`): the validator hard-rejects a `blocks`
edge unless the named target carries the source id back in its own
`depends_on`/`depends`/`blocked_by` — *"the only fields `effective_depends_on()` reads"*. The grep
that produced §5's claim evidently did not cover line 232.

**Consequence for FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS: none.** The action §5 took —
adding `depends_on: ["TASK-COWORK-MUTEX-001"]` alongside the existing `blocked_by` — is a harmless
duplicate rather than the repair it was billed as. The dependency was already enforced. The block
remains correct on the merits: `TASK-COWORK-MUTEX-001` is live at `task_board.review[]`,
`status=REVIEW`, so the edge is genuinely unsatisfied by either field. The `in_progress[] →
backlog[]` lane move also stands and freed a real WIP slot.

**Why this is recorded rather than quietly dropped.** Acting on "`blocked_by` is inert" would invite
*stripping* the field from rows as dead weight, which would silently **release real dependencies**
across all four pickers. The wrong rationale is more dangerous than the (harmless) action it
justified.

**Second, separate defect found while correcting the first — narrated, never landed.** §5's opening
line reads *"(see key `po_ruling_20260826T1802Z`)"*. That key **does not exist anywhere in
`docs/data/orch/orch-state.json`** (`grep -c` → 0; the sibling `po_ruling_20260826T1754Z` from the
same tick landed fine, 2 occurrences). So the ruling's rationale was never written to the row it
governs — only the *actions* (lane move + `depends_on`) landed. A future reader of that row finds a
citation to a nonexistent key. Same failure shape as the AC-3 "narrated but never landed" class in
`po/flow/main.md`. The row now carries a short `po_ruling_correction_20260826T1825Z` pointer back to
this section; the full text is kept **here**, in the cold store, because the hot row is at 11865B
against `ORCH_ROW_PROSE_CEILING_BYTES=12000` and `orch-apply.sh` correctly refused the inline
version of this correction on the first attempt (that abort is what surfaced the ceiling).
