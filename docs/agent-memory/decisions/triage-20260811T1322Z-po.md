# PO Decision Journal — dev-team Step 1 triage, tick 2026-08-11T13:22Z

Task ids: board triage of 3 durable-inbox envelopes (1 `ci_red`, 2 `bug-escalation`), 2 new unresolved Telegram reports, manual-dispatch-sweep selection. `UC-RDL-P4` deliberately NOT re-adjudicated (architect in flight, ratified 13:02:45Z this same session).

Applied write: ONE `scripts/orch-apply.sh` pipe, 6 rows stamped, `pending_triage_inbox` 3 → 0. `[orch-conservation-check] OK — task_total live=770 candidate=770, signal_total live=16 candidate=13`.

---

## STEP po-1 — `ci_red` CI-RED-f75059f7: the failing-file read, not the job name, decided everything

**What was considered:** (a) dedup on `check_id`/`head_sha`; (b) dedup on `failing_jobs[].name`; (c) MANDATORY pre-dedup failing-file read, then file-scoped dedup.

**Why (c):** `triage-signals.md`'s `ci_red` row forbids (a) and (b) outright — job names ("bun test", "size-lint") carry zero discriminating information and SHA-derived keys never match across advancing SHAs on this repo. Ran `gh run view 31492326034 --log-failed` before evaluating any dedup key.

The signal named **two** failing jobs, and the two jobs needed **two different read procedures** — this is the part that is easy to get wrong:

| Job | Read procedure used | FAILING_FILES |
|---|---|---|
| `bun test` | `FAILEDFILE:` block parsed verbatim (`15093 pass / 40 skip / 3 fail`, `=== FAILED FILES (2) ===`) | `src/__tests__/ALLZERO-OHLCV-FETCH.test.ts`, `src/__tests__/FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` |
| `size-lint` | grep returns EMPTY by construction (not the isolation runner) → failing step's own error line, read verbatim from `--job 93781550499` | `apps/mcp-server/src/interface/mcp/transport.ts` |

Had I stopped at the first grep, size-lint would have silently contributed **zero** files and its offender would have gone untriaged this tick while still holding the job RED.

**Disposition: 3/3 files file-scoped dedup HIT → 0 mints, 3 folds.** Keys `ci_job:size-lint|file:apps/mcp-server/src/interface/mcp/transport.ts`, `ci_job:bun test|file:src/__tests__/ALLZERO-OHLCV-FETCH.test.ts`, `ci_job:bun test|file:src/__tests__/FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` all matched open `backlog[]` rows. New observed SHA `f75059f7` appended to each.

**Two findings the fold produced that a bare dedup would not have:**

1. **Independent local confirmation of the size-lint measurement.** `wc -l apps/mcp-server/src/interface/mcp/transport.ts` = **265** on the working tree at HEAD `2a58c8275`, matching CI's `actual=265L upper=138L` exactly. So it is not a CI-environment artifact, and the row id's `237L` is stale by 28 lines. Sole offender of **1376 files scanned**, on **6/6** sampled runs across 4 days.
2. **The two `bun test` failures are proven non-flake.** Both files failed on run `31489910331`/`f63b1cb7` AND run `31492326034`/`f75059f7` — two consecutive isolated runs on advancing SHAs. Because CI forks one bun process per test file, cross-file order pollution is structurally excluded, so a repeat is deterministic. This is the increment over the 13:03Z mint, which had one run and could not yet rule out flake.

## STEP po-2 — Two `bug-escalation` envelopes: distinct events, opposite dispositions

**Question posed by the caller:** same underlying event, or two trips? **Answer: two distinct trips**, on four independent axes — different actor UUIDs (`085954f2` vs `165f4245`), different `createdAt` (13:04:01Z vs 13:08:12Z), disjoint staged-file sets (6 files incl. `pm.md` + 4 `docs/handoffs/TASK_CHORE-COMMIT-*.md` vs 1 file `system-auditor.md`), different `escalated` flags.

**What was considered for both:** (a) `git show --stat` the resulting commits and close whichever came back clean; (b) parse the payload's own leading tag and `escalated=` key.

**Why (b), and why (a) is a forbidden disposition:** `triage-signals.md` names "`git show --stat <sha>` is clean" as an INVALID disposition. A clean `--stat` is OUTCOME evidence (no peer happened to collide in that race window); the claim under test is MECHANISM (whether a pathspec was on the `git commit` line). `pre-commit:453-454` `exit 0`s silently on `mode=SCOPED` **before** `write_signal` is reached, so the signal existing at all IS the mechanism proof. Both are true positives by construction; only the *response* differs.

- **Trip A** (`escalated=false`, `prior_warns=2`) → `pendingObservations[]` volume log, **no mint**. The hook's own escalation is meant to converge repeat offenders, so a per-occurrence row would be churn.
- **Trip B** (`escalated=true`, `prior_warns=28`) → REPEAT-OFFENDER-AFTER-BLOCK. Dedup-checked the `FIX-COMMIT-SWEEP-*`/`FIX-SWEEPGUARD-*` family across all five non-terminal lanes first; `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES` is the open owner → folded, `occurrence_count` 25 → 26.

**★ The escalated=false trip is the more alarming of the two.** `085954f2` is a **fourth distinct offending actor** (prior: `165f4245`, `0454e9d8`, `bc8e264c`), and it absorbed `docs/data/orch/orch-state.json` — the hot SSOT — on only its **2nd** strike. The defect is no longer a single-session pathology that the top offender's counter describes; it is fleet-wide behaviour. That is a scope fact the P0 row did not previously carry.

**★ The flow doc I was following is wrong, and I am recording it for the second consecutive tick.** `triage-signals.md`'s `escalated=true` branch asserts "the hook itself already blocked this attempt". The payload reads `mode=warn escalated=true`. `mode=warn` never blocks. The instruction describes a block that has never once happened in 28 strikes. Correcting that prose is now explicitly in the FIX row's scope — otherwise the next triager reads a nonexistent enforcement and under-prioritises accordingly.

## STEP po-3 — Telegram backlog: 88 of 90 were already dispositioned; only the 2-row delta was new work

**What was considered:** re-triage all 90 unresolved rows vs establish the delta against the 12:37Z tick's own record.

**Why the delta:** the 12:37Z journal records "88 Telegram reports triaged", and the live unresolved set is ids `4561..4650` = exactly 90. `4649`/`4650` are the only rows post-dating that pass. Re-triaging 88 already-dispositioned rows would have produced 88 duplicate folds and consumed the tick. Rows stay `status=new` in the DB because triage disposition and DB resolution are separate planes here — that is pre-existing and not this tick's finding.

Both new rows are `bctcExtractReconcile RECONCILE EXHAUSTED` → folded onto `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE`, `occurrence_count` 45 → 47.

**★ The fold surfaced a scope error waiting to happen in that P0.** Telegram 4649 (FRT 2024-Q1) carries `report_id: 268f4544-d66b-42cd-b37e-18ff7b17d55f` — a **real UUID**, not a `fallback-<TICKER>-<PERIOD>` shell. That makes **3** real-UUID EXHAUSTED reports in ~22h (HUT 2025-Q3, BSR 2024-Q1, now FRT 2024-Q1), up from the 2 the prior tick recorded. The subset is **growing, not static**. The row's premise — shells are unextractable *by construction* — cannot explain a report with a real id. Its proposed terminal-classification gate will remove ~87% of alert volume and **still leave the real-UUID defect standing**, behind a green AC that measures volume. Written into the row as an explicit instruction to split or justify, because "alert volume dropped 87%" is exactly the shape of a false green.

Live hypothesis recorded at one log-read's cost, not asserted: system-auditor reports `pdf-extractor` A-30 at 94.07% sustained-high (loss of reclamation) at 12:36:20Z, concurrent with the window. A memory-exhausted extractor emitting zero rows on genuinely-extractable PDFs would explain the real-UUID subset. Not checked — naming it so the implementer checks it rather than re-deriving it.

## STEP po-4 — Mandatory pre-check producers

- **Supervised-hold (WF-2):** re-ran dev-team's own `should_hold` jq against the live file. `.head` = `UC-RDL-P4`, `supervised=true`, `goahead=true` → `should_hold=false`. Nothing held. No-op, correct — `po_goahead_20260811T130245` from 13:02Z is doing its job.
- **Manual-dispatch sweep:** 86 live candidates. Top = `TASK-COWORK-MUTEX-001` (P0, rank 0, BACKLOG-XOR-GAP), re-admitted by `flag_reentrant` because its prior stamp (2026-08-08T21:00:06Z) is 64h stale against the 4h window. Stamped and folded per Step 2/Step 3.
- **Step 0-TNB:** `docs/handoffs/tnb-audit-latest.md` mtime 2026-08-08T21:01Z, already carries a `## PO ACK` at 2026-08-08T21:01:34Z. No new audit since → no-op, no duplicate ACK appended.
- **Signal dashboard:** 0 NEW `to=po` rows in `.signal_queue.rows[]` (13 rows, all previously triaged). Inbox empty.

## STEP po-5 — BATCH composition: why these five, in this order

**What was considered:** (a) return `NOTHING` (every finding deduped — zero mints this tick); (b) return only the manual-dispatch candidate; (c) promote the standing, never-dispatched rows the folds landed on.

**Why (c):** zero mints is not the same as zero work. Every one of this tick's four folds landed on a row that was **minted days ago and never dispatched**. The size-lint row's own `status_note` already reads "PROMOTED INTO PO BATCH 2026-08-08T20:54Z" and it is still in `backlog[]` 3 days later; the prior tick's notebook NEXT names it "the single cheapest CI-green win". A triage loop that keeps re-folding evidence onto rows nothing ever picks up is a detection loop, not a delivery loop — `feedback_po_notebook_mint_never_reaches_orchstate_board`. Returning `NOTHING` here would have been the fourth consecutive tick to do that.

Priority order per `main.md` (recurring bugs → UNBLOCK → FIX → CLEAN → SPRINT):

1. `FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L` — recurring bug, ~72h of continuous main-branch RED, 1 file, 127L over bound, sole offender of 1376. Cheapest green.
2. `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES` — P0, 26 occurrences, 4 distinct actors, top offender is the fleet's own dispatcher, absorbed file is the hot SSOT.
3. `FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH` — CI RED, proven non-flake this tick.
4. `FIX-CI-BUNTEST-FOREIGN-FLOW-MISSING-TRADING-DAY-NO-BACKFILL` — CI RED, proven non-flake, plus a live production SLA breach on the same subject.
5. `SPIKE-COWORK-MUTEX-001-PRIOR-ART-DIFF` — manual-dispatch-sweep obligation, emitted as SPIKE not implementation.

**★ Why #5 is a SPIKE and not the P0 build task its title describes.** The row is titled "Implement Cowork-Slot Collision Probe Core (dispatch-claim/SKILL.md + CLAUDE.md)". Both named targets **already carry** a collision path (`CLAUDE.md:14` peer-collision row; `dispatch-claim/CARD.md:35`; `SKILL.md:194/:288/:563`), and `SKILL.md` was last written 2026-08-07, well after this row's 2026-07-30 mint. The 08-08T16:06Z tick correctly refused to dispatch it as a build task on prior-art grounds. But it equally correctly refused to **close** it: the match is on file+behaviour, and the row carries no `files` and no AC beyond "Atomic implementation of Step 2.4 collision probe". Closing a P0 on a partial match is the mirror of the mistake being avoided. The SPIKE's only deliverable is the disambiguation — close-as-shipped, or name the missing sub-behaviour. This is the **third** pass through the sweep and the diff has still never been performed, so the timebox is deliberately tight.

`TASK-COWORK-MUTEX-002`/`003` were NOT pulled in: their scope was not examined this tick and the SPIKE's own output is the right input for deciding them.
