
## A-30 [system-auditor] "mcp-server MemPerc=97.80% (>=85% threshold)" + A-21 re-emit RestartCount=7 — BOTH FOLD, RAW-verified 6-probe mem TRAJECTORY = GC-sawtooth (NOT monotonic-climb), NO mint / NOT route-to-ops — DECISION (2026-07-19T08:24Z, PO Step-1 triage, coordination_session 69b0312e) [task_id: sys-20260719T081104-59eb]
author: po (Step-1 triage). TWO system-auditor signal_queue NEW rows (both UNCOMMITTED in orch-state `.signal_queue.rows` — router did NOT touch orch-state; PO owns+commits) + 1 Telegram report 3509 (msg 3574, analysis-agent @08:11:04Z), all mcp-server. Memory TRIPWIRE set last tick (band 85.63-90.32%); 97.80% is materially higher → did NOT trust the single MemPerc snapshot ([1 obs≠mechanism, probe 2-3×], [auditor-FP-RAW-probe]) — RAW-verified the memory TRAJECTORY across 6 probes:
- **6 probes / 65s @08:20-08:22Z**: 89.16 → 91.98 → 92.00 → 91.89(dip) → 93.01 → 92.05(dip) of 3GiB cap (MemUsage 2.675-2.79GiB/3GiB). Two intra-window GC-reclamation dips (samples 4 & 6) ⇒ OSCILLATING sawtooth, NOT monotonic.
- **GC ceiling NOT breached.** Report captured 97.80% @08:11 → first probe 89.16% @08:20 = GC reclaimed **8.6pt**; a monotonic climb-to-OOM does not drop. `OOMKilled=false`; container Running/Health=healthy; StartedAt 2026-07-18T15:56:50Z = **16h continuous uptime**; gateway served live router tool-calls this tick ⇒ not OOM'd.
- **Baseline creep = the KNOWN slow leak, already backlogged.** Sawtooth low rose 85.63→89.16% over ~30min (~7pt/h), ~16h into rebuild-headroom cycle. Same mechanism as `FIX-MCP-MEMORY-CODE-LEAK` corroboration_20260626T15Z (6pt/h on old 2GiB cap). NOT a new leak.
- **VERDICT A-30 = FOLD (oscillating-high-stable, GC reclaiming, no OOM). NOT route-to-ops** — GC still reclaims below cap, no live outage, 3GB cap mitigation already landed, interim rebuild-cron already user-surfaced. DEDUP to `FIX-MCP-MEMORY-CODE-LEAK` (BACKLOG/high/apps/mcp-server); recorded 97.80% HWM + full trajectory in backlog-detail `corroboration_20260719T08Z`. NO new mint.
- **A-21 re-emit (`sys-20260719T081102-4ff3`) = DEDUP/FOLD without re-investigation** — IDENTICAL to `sys-20260719T074117-22be` folded @07:54Z (commit a35c5c1f0), evidence unchanged: RestartCount=7 CUMULATIVE, 16h healthy uptime, OOMKilled=false, gateway serving. auditor-dedup-ledger tracks `microservice_degraded:mcp-server:A-21`. Just NEW→triaged.
- **2 PLAN-ONLY notes (NO mint):** (a) **A-30 threshold=85% too tight** for a GC-sawtooth-near-cap service (normal band 85-93%, peaks ~97.8% while GC reclaims fine → fires on nearly every sawtooth peak, A-21-class detector FP) — candidate auditor-predicate-tune: raise to ~95% AND/OR gate on loss-of-reclamation (N consecutive samples with no dip = monotonic climb) or OOMKilled, not a single snapshot. (b) A-21 windowed/crash-only predicate note carried unchanged from 07:54Z.
- **DISPOSITIONS:** report 3509 → `process_telegram_report(3509, resolution=duplicate, delete_telegram_message=true)` ⇒ processed:true, delete_success:true, msg 3574 deleted; post-verified cleared from BOTH sets (read_telegram_reports(new)=empty, list_unresolved_reports()=[]). Both signal_queue rows NEW→triaged via `jq --arg … | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS; conservation task_total 546↔546 / signal_total 5↔5; CAS clean; atomic). 0 NEW rows remain.
- **NEXT-TICK TRIPWIRE (STANDING):** escalate A-30 to ops ONLY if a future probe shows GC ceiling lost — baseline low >~93% with no reclamation dips across all samples, OR peak sustained >97% with no reclaim, OR OOMKilled=true. Until then = known parked leak, not an incident.
- **RETURN to router = FOLD** (terminal, no dispatchable BATCH; NOT escalated to ops). Commit scoped: orch-state.json + backlog-detail.json + po-decisions.md + po.md (explicit paths, no peer sweep, NO push — router sweeps).

## A-21 [system-auditor] "mcp-server RestartCount=7 (threshold: 2)" on 2 channels (tg 3508 + signal_queue sys-20260719T074117-22be) — FOLD false-positive, RAW-verified 3x, NO mint / NOT route-to-ops (recurring cumulative-counter-misread-as-crashloop class) — DECISION (2026-07-19T07:54Z, PO Step-1 triage, coordination_session 69b0312e) [task_id: sys-20260719T074117-22be]
author: po (Step-1 triage). ONE finding on TWO planes: Telegram report 3508 (msg 3573) + UNCOMMITTED signal_queue row sys-20260719T074117-22be (system-auditor atomic write, orch-state dirty). Did NOT trust auditor text (A-2x = frequent FP class per mem[auditor-FP-RAW-probe]) — RAW-verified container health 3x (obs @07:51/07:51/07:53Z, all consistent):
- **Container HEALTHY + SERVING.** `vn-market-intelligence-mcp-mcp-server-1` (NOTE: real name, not bare `mcp-server`) Running=true Health=healthy across 3 `docker inspect` probes; all `.State.Health.Log` entries Exit=0; gateway served live `list_unresolved_reports`/`process_telegram_report` this very tick ⇒ NOT a live outage.
- **RestartCount=7 is CUMULATIVE, not a live crashloop.** Container Created 2026-07-15 (~4d) → counter reset then; current instance StartedAt 2026-07-18T15:56:50Z = **16h continuous healthy uptime**, last restart 16h ago. 7 accreted over ~3.2d then STABILIZED (decelerating, not accelerating).
- **All restarts CLEAN — not OOM.** LastExitCode=0, OOMKilled=false. Contrast the GENUINE 2026-07-02 A-21 episode (RestartCount 3→4 WITH mem-collapse 95.28%→15.89% in-place OOM restart — that one was real and drove the 2g→3g cap bump). This time exits are graceful (dev-env deploy/rebuild churn), no OOM signature.
- **Memory near cap but stable, NOT climbing.** 2 samples 90.32%→85.63% of the 3GB limit (`2.71GiB→2.569GiB / 3GiB`) — oscillating, not monotonic ⇒ no runaway-leak trajectory in-window. The 3GB cap-bump mitigation (archive commit 4dbf7ef0) has ALREADY landed (limit renders /3GiB); mem now sawtooths against 3GB as designed.
- **Precedent = well-established FP.** rag-service RestartCount=100 (router 2026-06-23 RAW-verified "CUMULATIVE not active crashloop… counter misread as active crash, cf bctc-lastpush class") + multiple prior mcp-server A-21 (=3/=4) all folded. auditor-dedup-ledger.json holds `microservice_degraded:mcp-server:A-21` (ts=07-19T07:41:17 sev=2) — this is a fresh re-emit of a chronic class.
- **VERDICT: FOLD (false positive).** NO mint, NOT route-to-ops — container healthy, no live crashloop, no rebuild needed, cap-bump mitigation already in place. Not an ops incident.
- **2 PLAN-ONLY observations (recorded, NO mint):** (a) **auditor A-21 predicate-drift** — the system-auditor A-21 tier check reads cumulative `docker inspect .RestartCount` vs a tight threshold=2, while mcp-server's OWN `apps/mcp-server/src/scheduler/system/restartCadenceAlertJob.ts` already does it correctly (windowed WINDOW_HOURS + crash-filtered, returns {restartCount:0} for clean restarts). A-21 should adopt windowed/crash-only logic OR honor the auditor-dedup-ledger board-tracked DEDUP-ENFORCEMENT (exact recommendation from the rag=100 triage). Candidate auditor-predicate-tune — plan-only, NOT minted this tick (detector-FP, not infra). (b) **mem WATCH** — 85-90% of 3GB cap corresponds to the known parked `FIX-MCP-MEMORY-CODE-LEAK` (pm); already backlogged, not a new item. Recurring-bug threshold: the recurrence here is the DETECTOR FP + an ALREADY-tracked root, so no escalation-block mint ([recurring-detection ≠ recurring-failed-fix]).
- **DISPOSITIONS:** report 3508 → process_telegram_report(3508, wontfix, delete=true) ⇒ processed:true, delete_success:true, msg 3573 deleted. signal_queue row → NEW→triaged via `jq --arg … | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS; conservation CONSERVED task_total 546↔546 / signal_total 3↔3; CAS clean; atomic). 0 NEW rows remain. PO owns + commits the orch-state signal_queue triage (router did NOT touch orch-state — row was uncommitted waiting for PO).
- **RETURN to router = FOLD** (terminal, no dispatchable BATCH). Commit scoped: orch-state.json + po-decisions.md + po.md (explicit paths, no peer sweep, NO push — router sweeps on next push).

## report 3507 [OHLCV-BACKFILL] "no completion report / Manual VPS investigation required" — FOLD wontfix, 3-plane RAW FALSE ALARM, NO mint / NO route-to-ops (pre-deploy weekend FP) — DECISION (2026-07-18T22:53Z, PO Step-1 triage, coordination_session 69b0312e) [task_id: FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS]
author: po (Step-1 triage). ONE new unclaimed Telegram report 3507 (msg 3572, from analysis-agent, 2026-07-18T22:31:59Z), same recurring OHLCV-BACKFILL class as folded 3504/3505/3506. Claimed via claim_telegram_report(3507, claimant=po); did NOT trust report text — RAW-verified 3 independent planes:
- **PLANE 1 serving (get_price_history HPG/VCB) = PASS, no gap.** HPG freshest bar 2026-07-17 close 21,850, contiguous 07-13→07-17; VCB freshest bar 2026-07-17 close 58,500, contiguous. Report fired Sat 07-18 = weekend/market closed → no new bar expected; last trading day (Fri 07-17) present = freshest possible. daily_ohlcv NOT missing.
- **PLANE 2 pipeline (get_pipeline_health) = PASS, contradicts report.** Backfill queue pending=FALSE; last completed 2026-07-18 21:19:19 (~1h BEFORE report fired at 22:31 — backfill actually completed). HPG rows=766 TA-ready, VCB rows=762 TA-ready; 20 non-neutral TA signals. Report's "crashed/DNS-failed before reporting, poller force-closed the queue row" is directly falsified by pending=false + recent completion.
- **PLANE 3 VPS (get_vps_proxy_health + get_vps_service_health) = PASS for OHLCV.** prices proxy status=ok, stale=no (0×24h pushes = weekend market-closed, expected — last push Fri 07-17 08:59); vn-price-fetch idle (market closed) but polled 1m ago = alive. NOTE bctc-plane divergence (vn-bctc-fetch unhealthy + bctc proxy STALE) is a SEPARATE service (financial-statement pipeline), weekend-benign per mem[B-05], does NOT touch OHLCV backfill — not conflated into the verdict.
- **VERDICT: FALSE ALARM (expected pre-deploy weekend FP).** All 3 planes contradict the report. "Manual VPS investigation required" is exactly the false-escalation string the in-review `FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS` (REVIEW/qa, NOT yet deployed) suppresses. Tripwire (re-report AFTER deploy = genuine regression) NOT tripped: fix undeployed → this instance is the expected benign emission. Weekend / 22:31 UTC = classic non-trading FP window.
- **DISPOSITION: FOLD, zero escalation.** process_telegram_report(3507, resolution=wontfix, delete_telegram_message=true) → processed:true, delete_success:true, msg 3572 deleted. Post-verified: read_telegram_reports(new)=empty, list_unresolved_reports()=[] (3507 cleared from BOTH sets). NO mint (covering fix already tracked in REVIEW — grep-before-mint / churn-without-convergence), NOT route-to-ops (would be the exact false-escalation the fix removes). Did NOT re-triage 3506 (already terminal wontfix). NOT counted as obs#2 toward an ops-vps-fetch row (VPS verified healthy for the OHLCV concern).
- **RETURN to router = NOTHING** (terminal fold; no dispatchable BATCH). Commit scoped: po-decisions.md + po.md only (explicit paths, no peer sweep, no push — router sweeps on next push).

## ci_red CI-RED-77c07cc7 — FLAKY-DISMISSED (1299b, 2nd ci_red obs) + NO re-mint, bump existing root-cause row P2→P1 — DECISION (2026-07-17T19:33Z, dev-team Step 0-SIG triage, coordination_session 69b0312e) [task_id: CI-RED-77c07cc7]
author: po (dev-team Step 0-SIG triage). Router routed EXACTLY ONE pendingSignal = ci_red (ci-health-probe, `docs/signals/ci-red-77c07cc7-20260717192055.json`, UNTRACKED/post-drain). All other PO-inputs empty (0 Telegram, 0 unresolved, 0 signal_queue, 0 orphan). Head idle, WIP 0 (in_progress=SPIKE-BCTC ops-owned), backlog 386.
- **RAW-VERIFIED INDEPENDENTLY (did NOT trust router).** `git diff f845eb6fd..77c07cc71 --name-only` = ONLY `docs/agent-memory/notebooks/main.md` (77c07cc71's parent IS f845eb6fd) → tested tree BYTE-IDENTICAL to f845eb6fd. `gh run list --branch main`: f845eb6fd CI run 29603867657 SUCCESS + 4 prior main commits (ef15a17797/a6705cdd8e/162ec56c0a/ef9aab15ec) all SUCCESS. Run 29605330359 @ 77c07cc71 (==origin/main, non-stale) = 14466 pass / 40 skip / **1 fail**, sole FAILEDFILE via `gh run view --job 87967067273 --log-failed` = `src/__tests__/1299b-skill-gated-bootstrap.test.ts`. Same code green→red = FLAKY / CI-runner nondeterminism, NOT a regression. NO code-FIX minted (no product-code delta).
- **ROUTER PREMISE CORRECTED — 1299b is NOT 1st-obs / NOT a WATCH candidate.** Router framed this as "1st observed flake of 1299b → 1-obs WATCH". FALSE: 1299b was ALREADY flaky-dismissed as a ci_red once (CI-RED-571818c2, 2026-07-16T02:01Z) AND already has a tracked root-cause backlog row `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART` (SPIKE_CI-PERFILE-ISOLATION-FLAKE item #1: blocking `execSync('git rev-parse --show-toplevel')` in `agentBootstrap.ts:358` eager `buildToolNameMap()` singleton → per-test timeout under CPU starvation → TDZ-poisoned ES module cascade; reproduced 11/24 under contention; also a real prod cold-start risk). This is the 2nd ci_red obs of that SAME already-tracked bug. The router's WATCH list (167-prediction-market-job / 1255-alert-off-hours-send / 1404-cb-startup-reset) is for OTHER files' 1-obs flakes; 1299b graduated past WATCH long ago.
- **NO DEFLAKE MINT (prior-art dedup; feedback_file_prior_art_check_before_minting_row + recurring_bug_escalation).** Board-wide grep (1299b/agentBootstrap/projectRoot/execSync/DEFLAKE/perfile) → `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART` already covers the exact root cause. A new DEFLAKE-1299b row = duplicate/churn. Contrast: DEFLAKE-VNSTOCK-3STATEMENT was minted at the 00e3c4b1 tick precisely because THAT file had NO existing root-cause row (2nd obs, no prior-art) — 1299b does. Per feedback_recurring_detection_vs_recurring_failed_fix: this is a recurring DETECTION of an un-fixed known bug (fix never ATTEMPTED yet), NOT a recurring failed fix → NO P0 emergency escalation.
- **INSTEAD: annotate + one-notch bump on the EXISTING row.** `jq … | scripts/orch-apply.sh`: FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART priority **P2→P1** + added `ci_red_recurrence{obs_count:2, signals:[571818c2,77c07cc7], last_obs, churn-cap note}`. Rationale for P1: recurring per-tick triage cost now empirically demonstrated TWICE (each ci_red forces a full router→PO spawn) + it is the highest-value SPIKE fix (also prod cold-start). P0 REJECTED (CI completes green on subsequent pushes — not a reliability emergency). Annotation CAPS further bumps (do NOT re-bump on future recurrences → churn cap). Mirrors 571818c2 PO's "one notch above" posture with one more data point.
- **RE-DRAIN PREVENTION (feedback_ci_red_close_must_record_fingerprint_else_redrain — MANDATORY).** (a) Verified `sha256("ci_red:77c07cc71…:bun test")` == payload.fingerprint `63fba0b43e12…` (probe Layer-1 dedup key). INSERT OR IGNORE that fp into `signals_processed` (from=ci-health-probe, result=flaky-dismissed, processed_by=po, source_filename=ci-red-77c07cc7-…json; bound params, injection-safe; DB 197→198, row id 2921) + `PRAGMA wal_checkpoint(TRUNCATE)` → probe re-emit blocked while HEAD stays 77c07cc71. (b) appended `_disposition` audit block + `mv` UNTRACKED signal → `docs/signals/processed/` (drain glob non-recursive) → drain re-route blocked. Post-verified: fp present in DB, file GONE from docs/signals/.
- **CONSERVATION + boundary:** orch-validate Stage0+1 PASS; conservation task_total live=543 candidate=543 (pure field mutation, zero count change); backlog 386 unchanged; CAS clean. `.head` untouched (idle, active_task_id=null — signal triage, no lane-move → no head sync). Did NOT touch cowork/state/peer-dirty files. Commit scoped: po-decisions.md + po.md + processed signal (explicit paths, no peer sweep). signals.db untracked/gitignored (not committed).
- **RETURN to router = NOTHING.** Flaky dismissal + fingerprint-record + existing-row escalation is terminal; no dispatchable BATCH (the P1 row is plan-only, dev-team dispatcher schedules it by priority next).

## Recurring BCTC reconcile-exhausted storm — ESCALATE SPIKE high→P0 + FAST-TRACK circuit-breaker (not no-change) — DECISION (2026-07-16T22:07Z, dev-team Step-1 triage, coordination_session 69b0312e) [task_id: SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD]
author: po (dev-team Step-1 triage, tick 22:07Z). ONE new Telegram report 3482.
- **ARCHIVED report 3482 = duplicate (RAW-verified, zero re-mint).** read_telegram_reports(status="new") → id 3482 [bctcExtractReconcile] VIX **2024-Q2**, 0 rows across bctc_layout_units/bctc_table_rows/bctc_md_tables, 8 passes (cap 8), enrich_failed terminal (report_id 1d1b7e75-8e2f-4a5c-92f8-cda8dd7a5bad) — EXACT chronic-storm shape. Confirmed `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` present in backlog[] AND its AC-3 already = the emission circuit-breaker BEFORE archiving (grep-before-mint; churn-without-convergence). process_telegram_report(3482, resolution="duplicate", delete_telegram_message=true) → processed:true, Telegram msg 3543 deleted.
- **CONVERGENCE = ESCALATE, not no-change (why-change from prior tick's pure-archive posture).** This is the **3rd RECONCILE-EXHAUSTED duplicate in 2 ticks** (3480 VND 2024-Q2, 3481 BSR 2024-Q1, 3482 VIX 2024-Q2). Diagnostic tell: bctcExtractReconcileJob has now walked BACKWARD past the 2025 quarters the SPIKE originally catalogued and INTO 2024 quarters → the watchlist backlog it grinds is DEEP → the report flood is UNBOUNDED (~1 fail-loud per ticker*quarter, ~1/30-min tick), and each forces a full router→PO spawn just to archive one duplicate = the churn-without-convergence anti-pattern the system explicitly targets (mem: project_systemic_review_0704). A "no-change, already-converged" verdict was CONSIDERED and REJECTED: the SPIKE is only ~1h old, priority merely "high", plan_only, unscheduled on an idle board, and it BUNDLES slow exploratory diagnosis (AC-1 infra-rollback verify, AC-2 dormancy root-cause, 180min timebox) WITH the fast churn-stopper (AC-3 emission circuit-breaker). Bundled, the churn-stopper waits for the whole SPIKE → churn keeps running every tick meanwhile. That bundling IS the convergence gap.
- **WROTE orch-state — scoped single-row field mutation (`jq -f | scripts/orch-apply.sh`, NOT full-doc overwrite):** SPIKE row `.task_board.backlog[]` — **priority high→P0** (P0 is an in-use canonical value, 6 rows; priority is z.string().optional() so schema-valid); added `converge_note` (records 3480/3481/3482 evidence + the fast-track instruction) + `converge_by=po-report-triage-20260716T2207Z`. INSTRUCTION folded in: fast-track AC-3 (emission circuit-breaker) as the FIRST, independently-shippable deliverable AHEAD of AC-1/AC-2 — it is the ONLY lever that stops the per-tick churn and does NOT depend on the diagnosis outcome (only needs to detect systemic producer-dormancy → batch/summarize/suppress, not one fail-loud per ticker*quarter); split it into the follow-up FIX immediately (the SPIKE already says "MINT A FOLLOW-UP FIX, do not fix inline"). AC-1/AC-2 stay in-scope but MUST NOT gate the churn-stopper.
- **CONSERVATION + boundary:** orch-validate Stage0+1 PASS; conservation task_total 542=542 (field mutation, zero count change); backlog length 403 unchanged; dup-key PASS; CAS clean. `.head` untouched (idle→router, active_task_id=null — top-level authoritative, no lane-move so no head sync needed). NO re-mint, NO new row, NO full-doc overwrite. Did NOT touch cowork/state/signals.db/peer-dirty files. Router commits po.md + po-decisions.md sweep-proof; I did NOT git-add/commit/push.
- **RETURN to router:** archived 1 duplicate + escalated the storm SPIKE to P0 with a fast-track circuit-breaker directive. Board remains idle (no BATCH of dispatchable work this tick — the P0 SPIKE is plan_only, dev-team dispatcher schedules it next).

## Close CI-RED-b51fbe13-FIX (gate satisfied) + TNB c111 ACK zero-remint — DECISION (2026-07-16T21:27Z, dev-team Step-1 triage, coordination_session 69b0312e) [task_id: CI-RED-b51fbe13-FIX]
author: po (dev-team Step-1 triage, tick 21:07Z). 7 signals drained; 0 new Telegram reports; TNB c111 handoff.
- **CLOSED CI-RED-b51fbe13-FIX (review→done_verified).** DID NOT trust router summary — RAW-verified: `git merge-base --is-ancestor b51fbe13 HEAD`=YES (ancestor of 1f9fbf52b); FIX a757051e9 landed (drop forbidden **Sector** line BSR/VIX/DBC, BSD3 guard); `gh run list --branch main` → CI conclusion=success on 320cb1d76 (run 29532453232) + 7b13612e8 (run 29535011678) — both pushes AFTER b51fbe13 AND after the FIX ⇒ verification_gate=ci_green_on_subsequent_push SATISFIED (2 green). Current HEAD 1f9fbf52b CI still in_progress but 2 prior green already satisfy; did not wait. **only path: gate empirically met; no alternative.**
- **Fingerprint 8a0a02371423de8cc179b9a00e25bdea45cfc228dda2aab55afb012643c5ced0 recorded in close_note** — MANDATORY else the ci_red signal re-drains (mem: feedback_ci_red_close_must_record_fingerprint_else_redrain). Verified post-write: absent review[], present done_verified[] status=DONE_VERIFIED, close_note contains fp. orch-apply Stage0+1 PASS, conservation 543=543 (lane-move, no count change). `.head` untouched (idle→router, active_task_id=null — status-flip=lane-move needs no head sync here).
- **TNB c111 ACK — ZERO re-mint (why-change: no new work vs plan).** Grep board BEFORE minting (mem: feedback_file_prior_art_check_before_minting_row): every HIGH finding maps onto an existing BACKLOG row. F-CHEF-MORNING-EOD-NO-SYNTHESIS (monitoring half) = FIX-COWORK-LASTFIRED-DECOUPLE-FROM-DELIVERY exact-thesis match (last_fired≠delivery proof→misses masked); its root cause + F-MCP-SUBAGENT-SYSTEMIC = FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK (P1); F-VNINDEX = FIX-VNINDEX-ESTIMATE-IMPLAUSIBLE-DELTA-GATE; BCTC serve-layer + extraction dormancy = bctc-analyst-owned + SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD (prior tick). Minting a duplicate = churn-without-convergence anti-pattern → declined. F-CHEF-EVENING-DOUBLE-PASS UNCONFIRMED (single obs) → no mint (mem: single-observation≠mechanism); recurrence risk pointer = UC-CCA-P3. ACK block appended to docs/handoffs/tnb-audit-latest.md.
- **RETURN = idle EXIT (BATCH NOTHING).** One task closed, zero minted. Router owns commit/push of my files; I did not touch git.

## cowork preflight presence-claim signal vs UC-RDL-P4 withhold — standalone FIX, not subsumed — DECISION (2026-07-15T19:02Z, dev-team triage, coordination_session 69b0312e) [task_id: FIX-COWORK-PREFLIGHT-PRESENCE-CLAIM-GAP]
author: po (dev-team Step-1 triage). 1 NEW signal cow-20260715T184053 (cowork-team, system-issue, MED) + router-withheld BOUNDED-1 auto-promote of UC-RDL-P4 routed to PO for adjudication.
- **BUG (payload docs/handoffs/2026-07-15-cowork-preflight-presence-claim-gap.md, RAW-read):** scripts/agents-flow/cowork-tick-preflight.sh Step 2 (~L127-138) `task_heartbeat`s `session-presence:<session>` but never `task_claim`s it (claim appears only at L146 for the ELECTION lock `cron:cowork:<tick>`). Fresh session tick-1 (no presence row) → heartbeat ok=false → verdict=ERROR → return 1 → expensive LLM fallback. Inverts cowork-team/flow/main.md Step 0b.1 (presence NEVER a gate; claim-first, heartbeat only on re-entry) and reopens the exact per-session/per-30min-lapse cost TOKEN-ECONOMY-TICK-PREFLIGHT WU-1 removed. Deterministic, self-heals (ttl 1800s > 900s tick), non-blocking → cost+contract defect, not outage. Reproduces scars feedback_chain_mutex_ttl_lapse_during_long_hop_reclaim + feedback_task_claim_held_lock_noop_use_heartbeat.
- **ADJUDICATION = (ii) fast standalone FIX, NOT subsumed by UC-RDL-P4.** UC-RDL-P4 (SPRINT-M, size L, apps/mcp-server/, next_agent ba) = composite `dispatch_preflight` MCP tool collapsing the ROUTER dispatch preflight (Phase A/A.5/B). The signal bug is a DIFFERENT flow AND zone — the cowork TICK preflight bash (scripts/agents-flow/, cross-service). Even after UC-RDL-P4 ships, that bash retains its own presence logic unless a SEPARATE migration task moves it; and the cowork tick preflight is deliberately a single-bash-call token path (WU-1), so a heavier per-tick MCP round-trip may not be its right home → composite tool would NOT auto-obsolete this fix. FIX priority > SPRINT-M (flow priority order) and the bug bleeds tokens NOW. Avoided minting a fix UC-RDL-P4 would obsolete because the fix targets a file UC-RDL-P4's current scope never touches.
- **MINTED FIX-COWORK-PREFLIGHT-PRESENCE-CLAIM-GAP** (backlog, FIX, P1, size S, zone cross-service/, owner po, next_agent developer, **promotable** — BOUNDED-1-eligible: no supervised/children/depends_on, board next_agent non-empty defeats the non-dev-owner/next_agent gates). baseline_pass=true (ran scripts/agents-flow/cowork-tick-preflight.test.sh myself → 20/20 green pre-fix). origin_signal_id=cow-20260715T184053 (task-archive close-gate flips signal READ→RESOLVED on done_verified). Fix = mirror main.md Step 0b.1 (claim-first, heartbeat on re-entry, presence never emits ERROR — reserve ERROR for election/transport failures) + header L13. AC includes: dev/qa MUST update existing test T3 ("ERROR verdict (presence transport failure)") which asserts the OLD gating contract, + add fresh-session→SILENT/WORK and re-entrant→clean+TTL-renewed cases; no cowork-slot lock leak (task_list_held count:0).
- **UC-RDL-P4 held for deliberate sprint start:** stamped `supervised:true` (stops BOUNDED-1 re-promote→router-re-withhold churn every tick) + `po_adjudication` note; STAYS BACKLOG (a SPRINT-M is PO sprint-initiation authority, not idle auto-dispatch). Cross-ref recorded on the row: when the composite tool is designed, honor claim-first/presence-never-a-gate and evaluate cowork-tick migration (but weigh WU-1 token path); SYSREMAKE-P2 RC-CEREMONY "shared tick-preflight-lib" is the eventual re-home — the standalone contract fix is compatible with that later extraction. Coordinate UC-RDL-P1 (lock-prefix) landing first (kept).
- **WRITE:** single `jq | scripts/orch-apply.sh` — Zod Stage0+1 PASS, conservation task_total 580→581 (+1 mint), signal_total 1=1, CAS clean, dup-key PASS (exactly one UC-RDL-P4). Signal NEW→READ. `.head` untouched (idle→router; dev-team session 69b0312e owns it). No locks touched. RETURN to router = BATCH([FIX-COWORK-PREFLIGHT-PRESENCE-CLAIM-GAP]) for Step-3 direct dispatch.

## Phase-2 STRUCTURAL-REMAKE — FULL user authorization (RC-ORCHMONO/RC-GITSTATE/RC-CEREMONY) — DECISION (2026-07-15T04:26Z, router coordination_session e417ef1f) [task_id: SYSREMAKE-P2-STRUCTURAL-REMAKE-ROUTE]
author: po (router-directed execution — DIRECTED TASK, normal triage skipped). REAL user selection via router AskUserQuestion ~2026-07-15T02:2xZ: "Authorize now" for ALL THREE previously user-gated RC items — RC-ORCHMONO (orch-cold-evict.sh backlog-lane eviction + orch-apply.sh HOT CEILING ~600KB gate), RC-GITSTATE (per-ticker stamping fix + gitignore pure-derived counters), RC-CEREMONY (SF-1 re-entrancy + shared tick-preflight-lib). Combined with 2026-07-14 DECISION-3 (RC-VERIF+RC-CONVERGE authorized), the ENTIRE SYSREMAKE-P2-STRUCTURAL-REMAKE-ROUTE is now FULLY user-authorized. Design-first discipline UNCHANGED: supervised architect-led cascade, architect TECH doc FIRST, sequence RC-VERIF+RC-CONVERGE -> RC-ORCHMONO -> RC-GITSTATE -> RC-CEREMONY. NOT a gate-jump — explicit user selection. All writes via `jq | scripts/orch-apply.sh`; every transform PASSED Stage 0+1 + conservation (task_total 577=577, no mints). `.head` + FIX-DAILY-FF + ALPHA rows untouched (the concurrent dev-team ALPHA/head churn present in the hot file is pre-existing loop state, NOT my edits).
- **ROW SYSREMAKE-P2-STRUCTURAL-REMAKE-ROUTE (backlog):** authorization PARTIAL->FULL; note rewritten to REMOVE the "STILL USER-GATED — do NOT design/build/dispatch" text (no tick may read a stale gate) while preserving BOTH decision dates' provenance; stamped "[PO 2026-07-15 coordination_session e417ef1f] USER GO ... NOT a gate-jump". supervised:true KEPT (held OUT of BOUNDED-1 — deliberate router/PO dispatch; the RC cascade launch is a SEPARATE deliberate dispatch, NOT this authorization step). next_agent:architect KEPT.
- **TE-T15 (backlog):** DECISION-3 defer note said "DEFER to the still-gated RC-ORCHMONO decision" — that gate is now RESOLVED. ANNOTATION CHOICE = STAYS a distinct follow-up row sequenced AFTER/WITHIN RC-ORCHMONO (NOT presumptively folded): the overlap coordination rule STANDS (both touch orch-cold-evict.sh — do not proceed independently), and the fold-vs-follow-up decision is deferred to the architect's RC-ORCHMONO TECH doc under design-first (that doc doesn't exist yet, so pre-folding would presume its scope). supervised:true already holds it out of BOUNDED-1.
- **RECONCILED (3rd live marker):** `sprint_goal.entries[SYSTEMIC-REMAKE-P1].scope_out` said "USER-GATED: router gates with the owner before ANY write-path/verification change lands" -> updated to "NOW FULLY USER-AUTHORIZED (both dates), superseding the earlier USER-GATED state; tracked+sequenced on the ROUTE row". watch_items / narrative / signal_queue / docs/signals files scanned — CLEAN (no RC gate markers). UC-GCP-P2 / UC-GCP-P8 = pure coordination pointers to RC-GITSTATE, no gate status -> left unchanged (correct).
- **BOUNDARY honored:** did NOT dispatch the architect, did NOT flip supervised:false, did NOT touch `.head`/FIX-DAILY-FF/ALPHA. Authorization state-change + reconciliation ONLY.

## BOUNDED-1 rank-1 groom-promote on idle board — DECISION (2026-07-14T21:01Z, dev-team router holds SF-1, coordination_session 69b0312e) [task_id: ALPHA-S2-TICK-DOWNSAMPLE-5MIN]
author: po (router-delegated triage + grooming). Board IDLE after FIX-DAILY-FF CLOSED done_verified (CI GREEN on pushed head e9b9fba6b; freeze lifted); ready=0 → dev-team had no pickable work. Directive: promote exactly ONE rank-1, dependency-satisfied, pick-time-clean row.
- **METHOD — ran the CANONICAL gate script, not a hand-pick:** `jq --slurpfile detail … -f scripts/devteam-backlog-promote-bounded1.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (Zod Stage0+1 + conservation task_total 563=563 PASS; atomic CAS rename). Dry-ran first, then reconciled the pick against the LIVE decision-journal before applying. Idempotent (WIP>=1 → no-op) so safe alongside the router's own bounded-1 lane.
- **PICK = ALPHA-S2-TICK-DOWNSAMPLE-5MIN** (P1, SPRINT-S, zone=multi→architect-splits, owner=developer, depends:[], supervised:false). backlog 394→393, ready 0→1, status→READY.
- **WHY rank-1 = P1 (all 5 P0 correctly gated OUT — verified RAW, not by label):** 3× FACTORY-* (split-pollNews / split-assembleBriefing / split-agentSignalStore) carry detail-level `depends_on` on prerequisite refactors (…-layering-fix, …-dedup-date-freshness-helpers, …-confidence-50-default/…-sql-binding) that are NOT done_verified → depends-blind, correctly skipped; FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD = supervised:true+plan_only:true; SPRINT-CCATO-TRUTHGATE-MCP-NATIVE = supervised:true. Script fell through to the lowest-idx eligible P1.
- **CLEAN per pick-time pre-verify:** user-AUTHORIZED (this file's DECISION-1 20:20Z un-parked ALL ALPHA rows supervised true→false, "Do NOT re-park", "BOUNDED-1 may pick" — the global MEMORY "waves 2+ still parked supervised" is STALE/superseded by that user full-launch GO); git-clean (0 dirty files under apps/); not status-divergent (confirmed absent from review[]/in_progress[]); dev-owned (owner=developer, not po/architect/qa); time-CRITICAL (ARCHIVE-NOW: VN intraday ticks are non-purchasable + 24h-rolling, every day of delay lost permanently). zone=multi is by-design (row documents "architect splits") — a valid ready-lane pick that consumes the WIP=1 slot via the architect-decompose relay.
- **REJECTED alt FIX-BCTC-BANK-SUMMARY-MAPPING** despite its rich baseline_pass AC — board `.status`=BLOCKED (blocked on a user-gated ops rebuild+reingest, permission-classifier-denied); a P1 label + strong AC does NOT make a row pickable. Lesson: RAW-read `.status`/`.supervised`/detail `depends_on`, never trust the priority label.
- **CARRY-OVER SIGNALS (async, non-blocking):** (1) `bctc_signal_FPT_20260714_routine` (FAIR / no esc4-5 / byte-identical 8 cycles / esc3 DATA-COVERAGE-LIMITED) → routine investment-thesis, ACK'd → docs/signals/processed/; covered by ROUTE-BCTC-FPT-Q1-2026-ROUTINE, no mint. (2) digest-predict Bash-grant → already DENIED (DECISION-4), routed to SPRINT-CCATO (P0, supervised) — no re-address. (3) BCTC serve-layer gap → bctc-analyst-owned per tnb c109 ACK; FPT esc3 corroborates; no re-mint. (4) PEK PERF-PEK-PER-PAGE-LATENCY → subsumed by FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT (per-page OCR latency IS the timeout root cause). (5) tnb c109 → already fully ACK'd 20:41Z.
- **FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT left UNTOUCHED:** stays PLAN-ONLY, owner=po, in review[], NOT closed (reflow FAILED-PATHOLOGICAL); awaits eventual triage→dev-pdf-extractor for the root-cause perf fix — never close it on a pek report. Directive satisfied by inaction (do-no-harm).
- `.head` untouched (dev-team session 69b0312e owns it); router locks untouched; no new sprint started.

## FOUR user gate decisions executed — DECISION (2026-07-14 ~20:20Z, router coordination_session e417ef1f) [task_id: USER-GATE-BATCH-20260714]
author: po (router-directed execution). FOUR explicit USER decisions given directly to the router via interactive selection (router AskUserQuestion) 2026-07-14 ~20:20Z — a REAL user GO (not relayed/inferred). All orch-state writes via `jq | scripts/orch-apply.sh`; every transform PASSED Stage 0+1 validate + conservation. `.head` + FIX-DAILY-FF (now done_verified) + its chain untouched (dev-team session 69b0312e owns chain close).
- **DECISION 1 — flow-alpha: LAUNCH EVERYTHING incl. wave 6.** Un-parked ALL 8 remaining ALPHA rows (supervised true->false, appended the exact wave-1 07-13 USER-GO annotation style): ALPHA-S2-TICK-DOWNSAMPLE-5MIN / ALPHA-S2-FOREIGN-FLOW-WRITE-RACE / ALPHA-S2-OMO-LIQUIDITY-CRON / ALPHA-S2-RAG-FTS-REBUILD-CRON (P1), ALPHA-S3-DIVERGENCE-SCREEN-V1 (P2), ALPHA-S4-REGIME-GATE-V1 (P2), ALPHA-S5-TRUTH-LEDGER-BACKTEST (P2), ALPHA-S6-OPTIONB-DECISION-GATE (P3). S6 GATE CONVERTED from user-gate -> normal sequenced row (`gate_converted` field): still sits LAST via P3 + depends:[ALPHA-S5-TRUTH-LEDGER-BACKTEST]; the 'PLAN-ONLY GATE / Do NOT decompose until PO trips gate' text SUPERSEDED. Priority + depends preserved (NOT reordered); zone/depends/safety notes preserved. BOUNDED-1/pipeline-resume may now pick. Transforms 1a (7 rows) + 1b (S6): task_total conserved 561=561.
- **DECISION 2 — FU-DEV-CAFEF-1: GREENLIT.** Minted backlog row FU-DEV-CAFEF-1 (SPRINT-S, size S, P2, zone apps/news-fetch/ owner dev-news-fetch per system-map, next_agent architect, supervised:false, depends []): wire the LIVE VPS /proxy/article-body endpoint (article-body-fetcher.py; cafef 5000ch 200 OK per dev-vps-crawls 2026-06-01; recon docs/vps-sources/cafef-article-body/recon.md) into push-news ingestion so stored news rows carry full CafeF body text. verification_gate = RAW-live (PUSH-AUTONOMY-1): a really-ingested news row whose body column holds the full article text (>> RSS ~500ch excerpt), direct-DB-read verified post-deploy. Resolved the LIVE 'awaiting USER greenlight' marker in `.narrative.watch_items[]` (was there since 2026-06-01). The processed-signal historical copy (docs/signals/processed/po-20260601T111530Z.json `do_not_open`) left as archived history — not a live gate. task_total 561->562 (backlog 392->393).
- **DECISION 3 — Phase-2 STRUCTURAL: RC-VERIF+RC-CONVERGE ONLY authorized.** The RC work is bundled in ONE route row SYSREMAKE-P2-STRUCTURAL-REMAKE-ROUTE (no separate RC-* rows exist). Added `note` + `authorization=PARTIAL` + supervised:true + next_agent:architect: AUTHORIZED phase-1 = orch-apply completion gate (verification.raw_probe before DONE_VERIFIED) + DEGRADED 8th StatusEnum + recurring-bug re-arm (LIVE bug_class-fingerprint replacing dead recurringBugEscalationFlag) as a supervised architect-led cascade (design-first). EXPLICITLY STILL USER-GATED: RC-ORCHMONO (hot/cold split), RC-GITSTATE (gitignore migration), RC-CEREMONY — no future tick may read the partial as blanket. OVERLAP handled: TE-T15 (cold-evict) annotated to DEFER to the still-gated RC-ORCHMONO decision (both touch orch-cold-evict.sh — do NOT proceed independently). task_total 562=562.
- **DECISION 4 — digest-predict Bash-grant: DENIED.** Signal already drained at chain close (signal_queue.rows empty) — reconciled idempotently, did NOT resurrect. digest-predict stays SHELL-LESS (Read/Write/Edit + gateway only); no shell granted to any narrative cowork agent. Capability need routes to SPRINT-CCATO-TRUTHGATE-MCP-NATIVE (gateway-native narrative_truth_gate MCP tool = least-privilege alternative): priority RAISED high->P0 (`user_prioritized` + `prior_priority` fields), note records DENIED + the routing; supervised:true kept (design-first architect->pm), the flow-alpha architect-contention hold superseded. task_total 562=562.

## CI-RED-29f92c5b (bun test) triage — no-mint, link existing root-cause row — DECISION (2026-07-13, dev-team tick 20:07Z, coordination_session 69b0312e) [task_id: FIX-DAILY-FF-VIEW-JOIN-ANCHOR]
author: po (dev-team Step 1 triage). 7 pendingSignals drained. HIGH signal `ci_red` (CI-RED-29f92c5b, run 29279251504, job `bun test`) on origin/main HEAD 29f92c5b.
- REAL-vs-FLAKY (feedback_ci_red_can_be_flaky_confirm_before_blame): TWO runs on same SHA — 29279251395 SUCCESS is a *different* workflow (`rag-service-py-lint`); 29279251504 FAILURE is the `CI` workflow. Genuine failure: `14471 pass / 51 skip / 2 fail`, FAILEDFILE `apps/mcp-server/src/__tests__/daily-foreign-flow-integration.test.ts` (2 assertions). NOT flaky, NOT unmocked-live-fetch — the test file is DELIBERATELY RED by design (header: "do not paper over it with a passing test"), committed RED in d15eedbec, PO-accepted; the 2 failing assertions (T-3 view correctness + R-1 elimination gate) encode an AC the current `daily_ohlcv_with_flow` view (FROM daily_ohlcv o LEFT JOIN daily_foreign_flow f) cannot satisfy — an FF-only row (OHLCV bar not yet landed) is never surfaced.
- DEDUP → NO MINT: root-cause FIX `FIX-DAILY-FF-VIEW-JOIN-ANCHOR` (backlog, high, size M, next_agent=architect, supervised, zone apps/mcp-server/) ALREADY exists with candidate shapes A/B + a verification_gate == "the 2 assertions pass RAW". Minting CI-RED-29f92c5b-FIX would DUPLICATE it (churn, feedback_recurring_bug_escalation). Instead: additive orch-apply annotation adds `ci_red_gate{check_id,head_sha,run_id,failing_test_file,verification_gate:ci_green_on_subsequent_push,...}` + `origin_signal_id` to the existing row. task_total conserved 560=560, Zod+conservation PASS.
- Fleet impact: RED-on-main strands the pre-push merge gate (feedback_red_prepush_strands_fleet) for all queued work (49 ULTRACODE + TE rows). Resolution = LAND the view fix (flips the 2 gates GREEN). Did NOT authorize an interim it.skip: the fix is ready-to-impl with clear shapes, skip→unskip is churn, and it contradicts the accepted RED-by-design gate. Local HEAD 14c12e782 is 7 commits ahead of origin, NONE touch test code — pushing them will NOT fix CI.
- Other signals: #2/#3 context_bloat_breach (dispatch-claim + task-lock SKILL.md) fired on files just touched by sanctioned UC-RDL-P1 doc fix 18885ff50 (+1 net line) → DEFER-class (feedback_ctxbloat_breach_on_live_sprint_file_defer), route to claude-manager-helper (not dev-team chain); noted, no mint. #4 tnb gateway-blind = converged known issue, no CRITICAL re-raise. #5/#6 cowork-fire "spawned" = routine. #7 cowork-fire 4th-consecutive legacy-downgrade = standing observability, explicitly NOT a dispatcher action; noted, no mint.
- Telegram: all-new reports are analysis-agent BCTC — large `bctcExtractReconcile RECONCILE EXHAUSTED` cluster + report 3649/3651 requesting BCTC-EXTRACT-QUALITY sprint (serve-layer "PDF present but get_bctc_full returns none" cluster n=8, escalating). NOT minted this tick: merge gate is CI-frozen (new work can't merge) + needs architect diagnosis; carried as pendingObservation for the post-CI-green tick.

## agent_signals verified_decision empty-payload + null-stock_code triage — DECISION (2026-06-25, po-s117) [task_id: FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE]
author: po (dev-team Step 1 triage). 1 NEW router_verified repair_task_request signal (router-dbsweep-20260625T2332Z-r6-agent-signals-verified-decision-empty-payload-null-stockcode, MEDIUM-HIGH). LIVE RAW (named volume vn-market-intelligence-mcp_market_data, read-only, immutable=1, scan_ts 2026-06-25T23:32Z): 559/559 (100%) verified_decision rows from alert-engine carry payload='{}'; ZERO verified_decision rows have BOTH non-empty payload AND non-null stock_code; ongoing 12+ days (MIN 2026-06-13 → MAX 2026-06-25). PLAN-ONLY per project_anomaly_task_bridge — minted BACKLOG, NOT promoted to ready (WIP stays 0). Corroboration gate (feedback_false_infra_failure_corroboration_gate): mint carries a RAW-verify-root-LIVE-before-fix generic_mandate.
- TRIAGE DECISION — MINT DISTINCT, NOT fold: the named fold-candidate FIX-ALERT-ENGINE-VERIFIED-DECISION-ALERTID-UUID-MISMATCH is already DONE-LIVE-VERIFIED + closed (commit 57a781a1; taAlertScanJob.ts:258 + bbAlertScanJob.ts:245 semantic id replacing crypto.randomUUID). Its shipped fix scoped alert_id ONLY ("FIX TARGET: persist the SEMANTIC alerts.id"); it never touched payload or stock_code, and its verify gate (alert_ids match format AND orphans stop growing) is GREEN while THIS stays broken. You cannot fold open work into a closed/verified task, and the defect column-set differs (payload+stock_code vs alert_id) → genuinely distinct. The instruction's "EXISTING backlog task" premise was false (RAW-checked: it is in the done[] lane), so the fold path does not apply.
- CONVERGENCE THESIS (cross-ref anchored, NOT merged): THREE defects on the ONE alert-engine verified_decision emit construction site (apps/mcp-server scheduler scan jobs → storeAlerts → agent_signals): (a) COUNT/double-fire = FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION (backlog, r5); (b) LINKAGE/orphan-alert_id = FIX-ALERT-ENGINE-VERIFIED-DECISION-ALERTID-UUID-MISMATCH (DONE — alert_id fixed); (c) CONTENT/empty-payload+null-stock_code = THIS (r6). Fix (c) at the SAME site (b) already touched.
- ZONE: apps/mcp-server (NOT the Go alert-engine) — resolves the signal's CONFIRM-BEFORE-LOCK zone Q using the DONE task's RAW grep (Go has zero verified_decision code; real emitters are the TS scan jobs).
- MINTED (BACKLOG, PLAN-ONLY): FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE (P2/MEDIUM-HIGH, apps/mcp-server/) — mandate: RAW-verify root LIVE (re-run the empty-payload + zero-well-formed scans) before fix; locate the verified_decision construction site (start at the (b)-fixed sites) and populate payload + stock_code + keep correct semantic alert_id BEFORE emit; add a FAIL-LOUD emit-time guard refusing empty-payload verified_decision (feedback_silent_swallow_serial_bugs); verify-don't-claim severity by confirming consumers READ payload; ACCEPTANCE = LIVE scan post-fix returns 0 malformed (payload='{}'/NULL OR stock_code NULL/empty) over ≥2 cycles AND ≥1 well-formed verified_decision row exists.
- SIGNAL: NEW→READ (not RESOLVED — open until fix ships) with triaged_into=FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE anchor + po_decision note.
- script: scripts/po-s117-agent-signals-verified-decision-empty-payload-null-stockcode-mint-ack.jq (idempotent: mint id-guarded all-lanes, flip NEW-guarded; conservation backlog +1 / NEW −1 / ready,in_progress,review,done,done_verified byte-stable; CAS-mtime guard; re-run delta 0). backlog 301 → 302.

## agent_signals identical-dup double-emission triage — DECISION (2026-06-25, po-s116) [task_id: FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION]
author: po (dev-team Step 1 triage). 1 NEW router_verified repair_task_request signal (router-dbsweep-20260625T2231Z-r5-agent-signals-identical-dup-doublefire, MEDIUM). LIVE RAW evidence (named volume vn-market-intelligence-mcp_market_data, immutable=1, scan_ts 2026-06-25T22:31Z): agent_signals total=4266; 102 all-time truly-identical dup-groups; 43 active dup-groups last 24h. PLAN-ONLY per project_anomaly_task_bridge — minted BACKLOG, NOT promoted to ready (WIP stays 0). Corroboration gate (feedback_false_infra_failure_corroboration_gate): mint carries a "RAW-verify root LIVE before fix" generic_mandate.
- DEDUP: only path — net-new task, no existing lane covers a DATA-LAYER agent_signals idempotency backstop against identical-dup ROWS from double-emission. Closest near-dups confirmed DISTINCT (not merged, cross-ref anchored): r2 FIX-SCHEDULER-DOUBLE-REGISTRATION = mcp-server scheduler cron_job_runs symptom, ONE job (this = data-layer rows + 3 COWORK emitters outside r2 scope; do NOT fold into r2); FIX-ALERT-ENGINE-VERIFIED-DECISION-ALERTID-UUID-MISMATCH (done-live-verified) = same alert-engine verified_decision emit path but alert_id orphan-FK defect, coordinate roots; FACTORY-ALERT-dedup-window-config = alert dedup-window value not row idempotency; FIX-AGENTSIGNALS-EXPIRED-GC-CRON = retention/GC not duplicate-prevention.
- MINTED (BACKLOG, PLAN-ONLY): FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION (P2, apps/mcp-server/) — mandate: (A) data-layer backstop CREATE UNIQUE INDEX + INSERT OR IGNORE/REPLACE on (from_agent,signal_type,stock_code,payload,minute-bucket), NEVER ALTER ADD UNIQUE (feedback_sqlite_add_column_unique_silent_noop); (B) per-emitter roots for ALL 5 emitters incl. the 3 COWORK agents (alert-engine, news-scout, bctc-analyst) + scheduler path (freshness-sla-monitor, system-auditor — confirm shared root with r2); (C) verify-don't-claim — confirm verified_decision triggers a Telegram send BEFORE escalating to HIGH.
- SIGNAL: NEW→READ (not RESOLVED — open until fix ships) with triaged_into=FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION anchor + po_decision note.
- script: scripts/po-s116-agent-signals-identical-dup-emission-mint-ack.jq (idempotent: mint id-guarded all-lanes, flip NEW-guarded; conservation backlog +1 / NEW −1 / ready,in_progress,review,done,done_verified byte-stable; re-run delta 0). backlog 300 → 301.

## Frontend-freshness + conviction-history triage — DECISION (2026-06-25, po-s115)
author: po (dev-team Step 1 triage). 8 NEW plan_only signals (7 po-frontend-freshness-20260625-* from the frontend-data-coverage-map.json freshness audit + 1 router-dbsweep-20260625T2202Z-conviction-history-no-backfill, router_verified). All PLANNING findings, NOT runtime breaks. PLAN-ONLY per project_anomaly_task_bridge — minted BACKLOG, NONE promoted to ready (WIP stays 0). Corroboration gate (feedback_false_infra_failure_corroboration_gate): every mint carries a "RAW-verify root cause LIVE before fix" generic_mandate.

- DEDUP: all 8 target task_ids net-new (none present in any board lane). Cross-refs anchored, NOT merged: L2 ~ FACTORY-APP-dedup-date-freshness-helpers (helper home); L4 ~ DS-OBS-01-FIX + FW-FRESH-01-FIX (source-level stale_threshold ≠ frontend-element coverage) + FACTORY-DOMAIN-extract-sla-config; depthThin-b ~ FIX-TA-INDICATORS-TIER3-ROUTING (indicator-computation root) + FACTORY-TECHANALYSIS-reconcile-ta-contract — mandate carries RAW-verify routing-vs-computation root to avoid duplicating that work; conviction-history ~ FIX-VNINDEX-CACHE-STARTUP-PURGE (same off-hours-restart genre, DIFFERENT table/mechanism, DISTINCT).
- EPIC frontend-freshness-20260625 (7): GAP-CHEF-SYNTHESIS-A-FLOW-PERSIST (P1, chef.md via agent-md-factory) → blocks GAP-CHEF-SYNTHESIS-B-ENDPOINT-CARD (P1, multi, blocked_by A); FIX-L2-FRESHNESS-DATAASOF-FIELDS (P2, mcp-server, reuse sectorRotationHandler.ts); FIX-L3-FRONTEND-AUTOREFRESH-FRESHNESS-BADGE (P2, frontend, soft-depends L2); FIX-L4-FRESHNESS-SLA-MONITOR-SELF-POLICING (P2, multi, market-hours-aware per feedback_auditor_freshness_threshold_market_hours_blind); FIX-DEPTHTHIN-A-PRICE-HISTORY-RETENTION-10D (P3, stock-price, fix WRITER per feedback_ohlcv_startup_purge_defeated_by_backfill_seeder); FIX-DEPTHTHIN-B-GATEWAY-TA-PATH-REWRITE (P3, multi).
- STANDALONE data-integrity (1): FIX-CONVICTION-HISTORY-EOD-BACKFILL (P2, mcp-server) — EOD reconcile recomputes any trading day with daily_ohlcv but no conviction_history row + Step-5c zero-write observability signal. CONFIRM-BEFORE-BLAME carried (logs since 19:00Z don't cover 06-25 VN window; scan-never-ran vs Step-5c-short-circuit indistinguishable, durable fix identical).
- DEPENDENCY preserved: chef-synthesis-b blocked_by chef-synthesis-a (addBlockedBy); L3 soft-depends L2.
- SIGNALS: all 8 NEW→READ (not RESOLVED — open until fix ships) with triaged_into (minted task id) + router_dup_of (self-anchor) + po_decision_triage note. Priority map HIGH→P1, MEDIUM→P2, LOW→P3.
- script: scripts/po-s115-frontend-freshness-conviction-mint-ack.jq (idempotent: mint id-guarded all-lanes, flip NEW-guarded; conservation backlog +8 / NEW −8 / ready stays 0; ready/in_progress/review/done/done_verified byte-stable; re-run delta 0). backlog 292 → 300.

## DB-sweep 19:06Z R1-R4 triage — DECISION (2026-06-25, po-s114)
author: po (dev-team Step 1 triage). 4 NEW router_verified repair_task_request signals (router-dbsweep-20260625T1906Z-r1..r4), all DB-sweep findings. PLAN-ONLY per project_anomaly_task_bridge — minted BACKLOG, NONE promoted to ready (WIP stays 0). Corroboration gate (feedback_false_infra_failure_corroboration_gate): every mint carries a "RAW-verify root cause LIVE before fix" generic_mandate — DB-sweep can be false-positive.

- DEDUP: no existing task covers R2/R3/R4 (ARCH-CRON-SCHEDULER-RELIABILITY=missed-fire class ≠ double-register; FACTORY-SCHEDULER-job-table-registry=maintainability refactor; KD-FRESH-01=freshness test; macro-fetch-cluster=FRED/PMI item3 only). R1's prior fix FIX-VNINDEX-CACHE-EMPTY-REFRESH-PATH is DONE/never-done_verified and the signal proves it did NOT close the root → recurring-bug-escalation, NEW task (distinct root: startup-purge + market-hours-only window), NOT a dedup.
- MINTED (all BACKLOG, PLAN-ONLY):
  - R1 → FIX-VNINDEX-CACHE-STARTUP-PURGE (P2, apps/mcp-server/) — stop startup purge OR add startup backfill; supersedes_resolution_of prior DONE fix; memory feedback_ohlcv_startup_purge_defeated_by_backfill_seeder.
  - R2 → FIX-SCHEDULER-DOUBLE-REGISTRATION (P2, apps/mcp-server/) — idempotent single registration; root for R3.
  - R3 → FIX-KINHDICH-READINGS-DEDUP (P3, apps/mcp-server/, depends R2) — CREATE UNIQUE INDEX (NOT ALTER ADD UNIQUE per feedback_sqlite_add_column_unique_silent_noop) + INSERT OR IGNORE + one-time 1534-group dedup.
  - R4 → FIX-MACRO-INDICATORS-EMPTY-COLUMNS (P3, apps/macro-indicators/) — scope source per column; no fabricated fill (feedback_no_fake_data_real_fetch).
- SIGNALS: all 4 NEW→READ (not RESOLVED — open until fix ships) with po_decision note + minted task id.
- script: scripts/po-s114-dbsweep-1906z-r1r4-mint-ack.jq (idempotent: mint id-guarded all-lanes, flip NEW-guarded; conservation backlog +4 / NEW −4 / ready stays 0; re-run delta 0).

## CI-FLEET-PUSH-DEADLOCK reconcile — DECISION (2026-06-23T01:4xZ)
author: po (dev-team Step 1 triage). signal=docs/signals/fleet-push-abort-push-fail-20260622T194948Z.json (auto-push-abort, ahead=21 behind=104→111 growing). Router RAW-verified all git facts; PO decides reconcile path only.

### Situation (router-verified, do-not-re-derive)
- local HEAD 5ecae5f2, ahead=9 (1 real fix 37ac9a52 test-only + 8 chore/orch/notebook), behind=111 (GROWING) vs origin/main b1d32758.
- ONLY code file in behind-set = FIX-DIGEST-FOREIGN-FLOW-ZERO-PAD-TOPN.test.ts — same file 37ac9a52 touches. Fleet conflict-surface guard aborts because that code file is in origin's behind-set, not in HEAD.
- DEADLOCK: origin has prod 2ea45dd6 (formatForeignFlowSection→canonical "Dữ liệu không khả dụng (pipeline tạm dừng)") but origin's TEST still asserts OLD "dữ liệu không có" → origin CI RED on this exact test. 37ac9a52 is the fix (test→canonical), NOT on origin.
- 37ac9a52 pre-image == OLD string == origin's current file → rebase of local onto origin replays 37ac9a52 CLEANLY (no conflict on that file). Router did NOT rebase (standing constraint).

### Decision: OPTION (a) — clean-checkout reconcile (rebase 9-ahead onto origin/main, out-of-band push)
NOT (b) cherry-pick-only. Rationale: (b) puts origin AHEAD of local on a 10th commit while local stays 111-behind, so the fleet conflict-surface guard KEEPS ABORTING and the 8 local chores never reconcile — trading one deadlock for a slower-growing one. (a) is verified-safe: router confirmed the only code-file divergence replays cleanly, so the rebase is NOT a conflict gamble; it absorbs origin's 111, replays the green test-fix, and leaves local==origin so the fleet guard goes quiet permanently. The 8 chores are notebook/orch churn — replaying them on origin is harmless and keeps the agent-memory trail intact (cherry-pick would orphan them). Must run out-of-band (clean checkout) — NEVER router/this-tree rebase (strands bg agents: feedback_push_blocked_by_perpetual_dirty_tree).

### Exact out-of-band commands (operator runs from a CLEAN checkout, NOT this working tree)
```
git clone <repo-url> /tmp/vnm-reconcile && cd /tmp/vnm-reconcile   # or: git worktree add /tmp/vnm-reconcile main from a clean clone
git fetch origin
git checkout -B reconcile origin/main                              # land on origin tip (111 ahead of local)
git cherry 5ecae5f2 origin/main || true                            # sanity: shows the 9 local-ahead to replay
git rebase --onto origin/main b1d32758 5ecae5f2                    # replay local 9-ahead (incl 37ac9a52) onto origin
pnpm check && bun test apps/mcp-server/src/__tests__/FIX-DIGEST-FOREIGN-FLOW-ZERO-PAD-TOPN.test.ts   # gate: green BEFORE push
git push origin HEAD:main                                          # out-of-band push; clears origin CI red
```
(If b1d32758 is not the exact local merge-base, operator uses `git merge-base 5ecae5f2 origin/main` for the `--onto ... <base> 5ecae5f2` base arg. Push only after pnpm check EXIT:0 + the foreign-flow test green — do NOT push red, feedback_red_prepush_strands_fleet.)

### done_verified ruling
- FIX-MACRO (ba1156d8) + FIX-FOREIGN-FLOW (2ea45dd6): WITHHELD until origin CI re-runs GREEN on the pushed SHA. The prod code is already on origin (2ea45dd6); the ONLY thing red is the stale test, which 37ac9a52 fixes on push. Grant done_verified once: (1) push lands, (2) origin Actions run on the new HEAD = success, (3) the foreign-flow test green in CI (not just local). Until then, local-green ≠ origin-done (feedback_ci_green_gate_blocked_by_cloud_chore_divergence).
- Fleet conflict-surface guard: NO new follow-on needed — it is CORRECT to abort; it is a symptom, not a bug. Once (a) lands and local==origin, the test-file divergence clears and the guard goes quiet on its own. It will KEEP aborting (correctly) every fire until the out-of-band push completes — that is expected backpressure, not a defect. Do NOT lower the threshold or weaken the guard to silence it (that would let real divergence through). If the operator cannot run (a) promptly and the abort-signal noise needs damping, the ONLY acceptable follow-on is a one-line abort-cooldown/dedup so the same unactionable abort doesn't re-signal every tick — NOT a guard-logic change. Flag-only, P-low, not minting now.

### what-considered
(a) clean rebase vs (b) cherry-pick-only vs (c) weaken-guard. Chose (a): verified-safe replay, permanently clears the guard, preserves chore trail. Rejected (b): leaves local 111-behind → guard keeps aborting. Rejected (c): guard is doing its job; weakening it admits real divergence. No board mutation (this is a push/CI reconcile, not a task) — head router_note set, signal can be drained as triaged-decision-recorded.

## NEXT-LEVEL ROADMAP — REVISION 2 (2026-06-22, supersedes Rev-1 below)
author: po (autonomous, direction-setting, next-level cycle requested by router for "po + architect next level"). NOT minting task_board rows beyond normal flow — this is a roadmap; router routes to architect/dev-team after.

### Why a revision
Rev-1 (below, same date) correctly converged on PROVENANCE-AND-CALIBRATION as the value theme, but it predated TWO load-bearing facts now in force:
1. **STANDING DIRECTIVE 2026-06-22 (user, verbatim "no remote trigger all working on this server")** — the project must run ENTIRELY on this local server; NO cloud RemoteTrigger jobs/backstops. This OVERRIDES the 12-RemoteTrigger guaranteed-slot backstop strategy that cron-cowork-team/SKILL.md + docs/standards/cron-jobs.md still document as live.
2. The gateway-blind execution substrate is now understood as the GATE on the value theme, not a side-issue. Verified live this tick: `.mcp.json` = `{"mcpServers": {}}` (empty) → Agent-spawned cowork data subagents have NO `mcp__gateway__call_tool` → they fabricate/no-op (confirmed 06-18, 3/3 chef spawns blind). Today the only real vn-market access is the router (claude.ai connector, main-session-only) + the 12 cloud RemoteTriggers. Remove the cloud triggers per directive #1 and the ONLY remaining real-data path on this server is the router — every locally-spawned data agent fabricates.

**Consequence:** you cannot calibrate or attribute the provenance of data produced by agents that fabricate. PROVENANCE-AND-CALIBRATION sits ON TOP of an execution substrate that does not yet reliably fetch real data locally. Rev-1 had no foundation layer for that substrate. Rev-2 inserts it as Foundation-1 and sequences foundations-first.

### Foundation-1 — LOCAL-EXECUTION-SUBSTRATE (the gateway + no-cloud cutover) — DO THIS FIRST
This is more load-bearing than "just register .mcp.json." It is a two-part foundation that MUST land together, because doing either alone breaks the system:
- **Part A — register the gateway wrapper in local `.mcp.json`** so Agent-spawned cowork/data subagents inherit a REAL `mcp__gateway__call_tool` on this server. CRITICAL CONSTRAINT (architect must enforce): register ONLY the `gateway` server and KEEP the `call_tool` indirection — do NOT load the 146 vn-market tools directly (small tool surface is a hard requirement per CLAUDE.md; the whole reason vn-market was cleaned out of .mcp.json). Anchor task on board: `ARCH-HEADLESS-GATEWAY-COWORK-NOPOST` (P high, zone=agents, backlog).
- **Part B — migrate the 12 cloud RemoteTrigger guaranteed/hourly slots to LOCAL launchd timers** on this server (the pattern already proven by `com.vn-market.fleet-push.plist`), then DECOMMISSION the cloud RemoteTriggers and rewrite cron-cowork-team/SKILL.md + cron-jobs.md to drop the "12 RemoteTriggers are the session-independent backstop" guidance. After this, persistence for guaranteed slots comes from local launchd, not the cloud.
- **Why both together:** Part B without Part A = local-spawned guaranteed-slot agents fire but fabricate (no gateway) — strictly worse than today's cloud backstops which at least had real access. Part A without Part B leaves the system still depending on cloud triggers the user has forbidden. The foundation is "local agents can fetch real data AND local timers keep them firing — with the cloud entirely removed."
- **Gate / done_verified:** a locally-spawned cowork data agent (e.g. news-scout or market-watcher) calls `mcp__gateway__call_tool(server="vn-market", ...)` and returns LIVE varied data (not fabricated/no-op), AND a guaranteed slot fires from a local launchd timer with the cloud RemoteTrigger deleted, AND tool surface stays small (only `gateway` in .mcp.json, verified by listing loaded tools).
- **Architect brief REQUIRED before dev** — see "Architect briefs" below. This touches agent-spawn inheritance, MCP connection model, and launchd timer fan-out; it is not a single-zone code fix.

### Sequencing the next level (foundations-first — JUSTIFIED)
1. **Foundation-1: LOCAL-EXECUTION-SUBSTRATE** (gateway .mcp.json + cloud→local cutover). Everything downstream that relies on real fetched data depends on this. Highest priority.
2. **Foundation-2: HONEST-DATA-FLOOR HOLD** (keep daily_ohlcv writer-integrity P0 cluster green). Already mostly landed; runs underneath. Calibrating conviction on dirty candles scores noise — so the floor must hold while Foundation-1 ships. (Was Rev-1 "Supporting Epic 3" — promoted to foundation because it is a precondition for the value theme's accuracy.)
3. **PRIMARY VALUE EPIC: PROVENANCE-AND-CALIBRATION** — decomposes into THREE shippable epics (see below). Starts once Foundation-1's real-data path is verified, because provenance/calibration of fabricated data is meaningless.
4. **SUPPORTING: MACRO-SUBSTRATE-REAL** (close VN-MACRO-TOOLING) — feeds the calibration layer (cannot calibrate conviction built on estimate-flagged macro). Can run in parallel with the primary epic once Foundation-1 lands, since VN-MACRO-TOOLING is already IN_PROGRESS.
5. **SUPPORTING: SOURCE-LINK-FLEET-ROLLOUT** (goal-3 generalize) — sequenced WITH FE-PAGE-REORG Wave-2 so the source-link/expand contract bakes into shared `<DetailPanel>`/`<ContentCard>` components (near-zero rollout cost if co-timed, expensive if retrofitted). This is the "provenance" half of the theme made visible to the user.

### PROVENANCE-AND-CALIBRATION — decomposition into shippable epics
- **Epic P-1: CONFIDENCE-PROVENANCE (the confidence-mask sub-wave).** Every served confidence/score becomes real OR an honest-null — never a silent default. Folds: FACTORY-INTERFACE-confidence-score-50-mask (P0), FACTORY-INFRA-agentsignal-confidence-50-default (P0), FACTORY-INTERFACE-sequential-confidence-05-mask (P1), FACTORY-INTERFACE-source-confidence-10-mask (P1), FACTORY-PDF-paddleocr-score-07-mask (P1), FACTORY-RAG-confidence-impact-or-00-mask (P2). Needs the CONFIDENCE CONTRACT design (architect) so all six converge on ONE honest-null shape instead of diverging. This is the deepest goal-1 debt (a fake `50`/`0.5`/`1.0` confidence is fake data on a metric the user reads to decide).
- **Epic P-2: CALIBRATION-LOOP (the self-scoring sub-wave).** The FB poster's forward calls are logged and weekly auto-scored hit/miss/partial, with calibrated accuracy fed back into next-session conviction phrasing. Folds: PRED-RESOLVER-GAP-FIX (just-shipped trading-day producer — substrate), FIX-PREDICTION-SIGNALS-EMPTY (poll-writes-zero producer gap), FIX-FB-PREDICTION-CALIBRATION-LOOP (cross-agent ledger: fb-market-poster logs, digest-predict scores + feeds back). Needs the CALIBRATION LEDGER design (architect) — cross-agent contract, scoring rule reusing the neutral-band trading-day rule, and where calibrated accuracy re-enters Layer-5 conviction phrasing.
- **Epic P-3: SOURCE-PROVENANCE-FLEET (the visible-traceability sub-wave) = the SOURCE-LINK-FLEET-ROLLOUT supporting epic.** Every served info element links its source + expands to full fetched detail (goal-3 fleet-wide). Folds: AUDIT-FETCH-COMPLETE (P1) + the goal-3 expand contract on FE-PAGE-REORG Wave-2 shared components. This is the user-facing face of "provenance."

### Architect briefs REQUIRED before dev work starts
1. **LOCAL-EXECUTION-SUBSTRATE brief (Foundation-1)** — extends `ARCH-HEADLESS-GATEWAY-COWORK-NOPOST`. Must specify: (a) the minimal `.mcp.json` gateway-only registration that preserves the small surface + the call_tool indirection (do NOT load 146 tools); (b) whether Agent-spawned subagents actually inherit the router's .mcp.json server set or need a different mechanism (this is the open technical question — the router's claude.ai connector is main-session-only today); (c) the cloud-RemoteTrigger→local-launchd migration plan for the 12 guaranteed/hourly slots (fan-out model, dedup/mutex so a local timer + the */15 dispatcher don't double-fire — see the gatherer manual×cloud double-fire lesson); (d) decommission + doc-rewrite steps. HIGHEST priority brief.
2. **CONFIDENCE CONTRACT brief (Epic P-1)** — the one honest-null shape (null + is_estimate:true vs sentinel vs omission) applied consistently across agentSignalStore, /api/signals/stock, sequential-market-analysis, finalizeBctcRefine, PaddleOCR, RAG. One contract or per-layer. Folds the 6 confidence-mask FACTORY tasks under one design so they don't diverge.
3. **CALIBRATION LEDGER brief (Epic P-2)** — the cross-agent ledger contract (fb-market-poster logger ↔ digest-predict weekly scorer): schema, ownership, hit/miss/partial rule reusing the |move|<NEUTRAL_BAND_PCT trading-day rule, feedback path into Layer-5. Cross-agent .md edits route via agent-father; architect rules the DATA contract first. Resolve whether FIX-PREDICTION-SIGNALS-EMPTY is the same pipeline as the resolver leg or a distinct producer gap.
4. **SOURCE-LINK ENVELOPE brief (Epic P-3)** — decided BEFORE FE-PAGE-REORG Wave-2 extracts shared components: does source-link/detail metadata ride the existing FE loader-utils safeFetch<T> envelope or a sibling contract, so the expand contract is baked into `<DetailPanel>`/`<ContentCard>` (one-shot rollout).

### Non-goals (explicit — to keep WIP sane; WIP cap stays 2)
- **Do NOT start PROVENANCE-AND-CALIBRATION (any P-epic) before Foundation-1's real-data path is verified.** Calibrating/attributing fabricated data is wasted work; sequence discipline is the whole point of this revision.
- **Do NOT keep ANY new cloud RemoteTrigger and do NOT add new always-on cloud components** — directive #1. Local launchd only.
- **Do NOT register the 146 vn-market tools in .mcp.json** — only the `gateway` wrapper; small surface is a hard constraint.
- **Do NOT restart mcp-server to mask FIX-MCP-MEMORY-CODE-LEAK** — the user wants it really-fixed; 0 OOM-kills in 18h so it is tracked (urgency raised oscillating→sustained) but NOT an incident requiring a masking restart. Restart trigger stays RestartCount++ OR OOMKilled=true.
- **Do NOT batch the EPS-field-accuracy follow-on into the P0 work** — get_bctc_full VCB EPS≈15 (~100x low) is a real but SEPARATE minor accuracy issue from the already-done_verified 0-rows P0; mint it as its own low-priority follow-on, do not let it block or bloat the primary epics.
- **Do NOT promote all 102 FACTORY backlog rows at once** — only the 6 confidence-mask rows belong to Epic P-1 under the confidence contract; the FDA-5/6/7/10 hardcoded-figure rows are P3 and stay in backlog until a later wave.
- **Do NOT exceed WIP 2** across all this — foundations-first means at most Foundation-1 + the floor-hold concurrently, then one P-epic at a time.

---

## NEXT-LEVEL ROADMAP — REVISION 1 (2026-06-22, SUPERSEDED by Rev-2 above)
author: po (autonomous, direction-setting). NOT minting task_board rows — router routes to architect/dev-team after.
> NOTE: Rev-1 omitted the no-remote-trigger directive and the gateway-blind foundation gate. Rev-2 inserts Foundation-1 (LOCAL-EXECUTION-SUBSTRATE) ahead of the value theme and promotes the honest-data floor to a foundation. The PROVENANCE-AND-CALIBRATION decomposition, supporting epics, and architect-input list below remain valid and are carried into Rev-2.

### Where the product IS vs the 3 standing goals
- Maturity: multi-agent cowork + 17-row dev-team, 166 MCP tools (project-stats), FB poster with full TNB-6-layer synthesis flow, BCTC pipeline, 33-route dashboard, alert engine, 271-item backlog (po-s111 triaged 06-21). WIP=0, head idle.
- GOAL 3 (source-link + click-expand full detail): FIRST EPIC INFOCARD-EXPAND-FETCH is COMPLETE — FIX-SIGNALS-STOCK-FULL-DETAIL + FIX-CASCADE-CARD-INVALID-DATE + FIX-INFOCARD-DROPDOWN-EXPAND all DONE_VERIFIED. The dropdown-expand pattern is PROVEN on signal/cascade cards but NOT yet generic across ALL cards; the unclosed generalization is AUDIT-FETCH-COMPLETE (P1) + the per-card source-link wiring. Goal-3 is ~60% — pattern shipped, fleet-wide rollout pending.
- GOAL 2 (expert decision-grade FB post): the SYNTHESIS FLOW is built and mature — STEP 2b TNB 6-layer walk (macro→FX→regime→sector→per-ticker conviction→valuation), STEP 2c T-45 adversarial gate, conviction calls with entry/if-then/risk, anti-fabrication detail-floor. The genericness root ("synthesis gap") is structurally addressed. The UNCLOSED gap is now the ACCOUNTABILITY SUBSTRATE feeding/validating it: prediction_signals frozen 45d (FIX-PREDICTION-SIGNALS-EMPTY), no closed self-scoring loop (FIX-FB-PREDICTION-CALIBRATION-LOOP), macro layer still estimate-flagged (VN-MACRO-TOOLING IN_PROGRESS + FIX-MACRO-SNAPSHOT-DELTAS-NULL / FIX-MACRO-CARRY-YIELD-ESTIMATE-FLAG / FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE). Expert VALUE is composed; expert ACCURACY is not yet measured or fed back.
- GOAL 1 (no fake data): served metrics are real-SHAPED but not yet PROVABLE — a whole FACTORY wave (102/271 backlog) enumerates the live fabrication/masking residue: `confidence_score ?? 50` (FACTORY-INTERFACE-confidence-score-50-mask P0 + FACTORY-INFRA-agentsignal-confidence-50-default P0), `0.5` conviction (FACTORY-INTERFACE-sequential-confidence-05-mask P1), `1.0` source-confidence (FACTORY-INTERFACE-source-confidence-10-mask P1), PaddleOCR `0.7` (FACTORY-PDF-paddleocr-score-07-mask P1), RAG `or 0.0` mask, plus FDA-5/6/7/10 hardcoded grid/credit/shipping figures (P3). These are the NAMED goal-1 violations still on the served path.

### Highest-leverage NEXT-LEVEL theme
**From "REAL data" to "PROVABLE, SELF-SCORING, FULLY-ATTRIBUTED intelligence."**
Justification: all three standing goals have crossed the "is it real / is it composed / can it expand" threshold. What blocks the product from being a TRUSTWORTHY daily-use expert tool is the next axis — every served number can be traced to its source AND the system measures its own forecast accuracy and feeds it back. The board EVIDENCE converges here: (a) the largest single cluster (FACTORY confidence/score masks, P0/P1) fabricates CERTAINTY on top of real values — a confidence number that is silently `50`/`0.5`/`1.0` is itself fake data on a metric the user reads to decide; (b) the FB poster composes high-conviction calls but prediction_signals has been frozen 45 days and there is no loop scoring those calls hit/miss — the product asserts expertise it never verifies; (c) goal-3's expand-to-source pattern is proven but not fleet-wide, so most cards still cannot be re-checked. The unifying move is ACCOUNTABILITY: trust = traceability + self-measurement.

### Prioritized roadmap

PRIMARY EPIC — **PROVENANCE-AND-CALIBRATION** (theme: every served metric is honest about its certainty AND the system scores its own forecasts)
- Outcome: no served confidence/score is a silent default; the FB poster's forward calls are logged and weekly auto-scored hit/miss/partial, with calibrated accuracy fed back into next-session conviction phrasing.
- Why now: directly closes the two deepest goal-1+goal-2 gaps; WIP=0, head idle; the synthesis flow is already built so the only missing piece is the measurement loop around it; the confidence-mask cluster is the highest-priority (P0) honest-data debt on the board.
- Rough scope / folds in: confidence-mask sub-wave — FACTORY-INTERFACE-confidence-score-50-mask (P0), FACTORY-INFRA-agentsignal-confidence-50-default (P0), FACTORY-INTERFACE-sequential-confidence-05-mask (P1), FACTORY-INTERFACE-source-confidence-10-mask (P1), FACTORY-PDF-paddleocr-score-07-mask (P1), FACTORY-RAG-confidence-impact-or-00-mask (P2). Calibration sub-wave — FIX-PREDICTION-SIGNALS-EMPTY (poll-writes-zero root, builds on the just-shipped PRED-RESOLVER-GAP-FIX trading-day producer) + FIX-FB-PREDICTION-CALIBRATION-LOOP (cross-agent ledger: fb-market-poster logs forward calls, digest-predict weekly auto-scores + feeds back). Decision principle: a confidence/accuracy figure is a SERVED METRIC and must obey goal-1 (real or honest-null, never a masked default).

SUPPORTING EPIC 1 — **SOURCE-LINK-FLEET-ROLLOUT** (generalize goal-3 expand-detail to ALL cards)
- Outcome: every dashboard card links its source and click-expands to full fetched detail — the proven INFOCARD-EXPAND-FETCH pattern applied fleet-wide, not just signal/cascade cards.
- Why now: first epic is DONE_VERIFIED so the reusable pattern + backend detail-exposure approach already exist; FE-PAGE-REORG is actively extracting shared `<ScreenerTable>`/`<DetailPanel>`/`<ContentCard>` components — the source-link + expand contract should land ON those shared components so one change covers all pages (rollout cost is near-zero if sequenced WITH the reorg, expensive if after).
- Rough scope / folds in: AUDIT-FETCH-COMPLETE (P1, the completeness audit that defines what "full detail" means per card) + the goal-3 expand contract added to FE-PAGE-REORG Wave-2 `<DetailPanel>`/`<ContentCard>` extraction. Architect rules whether the source-link metadata rides the existing loader-utils safeFetch envelope.

SUPPORTING EPIC 2 — **MACRO-SUBSTRATE-REAL** (close VN-MACRO-TOOLING so the synthesis macro layer is fetched, not estimated)
- Outcome: the TNB Layer-1/2 macro inputs (trade balance, credit flow, carry, yield, FX deltas, macro calendar) are LIVE-fetched with is_estimate=false, removing the estimate-flag that currently caps the expert post's macro confidence.
- Why now: VN-MACRO-TOOLING is already IN_PROGRESS with BA-VN-MACRO-TOOLING SPEC-COMPLETE — it is the closest-to-done lever and it directly feeds the PRIMARY epic (you cannot calibrate conviction built on estimate-flagged macro). Finishing it unblocks honest Layer-1/2 synthesis.
- Rough scope / folds in: VN-MACRO-TOOLING (drive IN_PROGRESS→done), FIX-MACRO-SNAPSHOT-DELTAS-NULL, FIX-MACRO-CARRY-YIELD-ESTIMATE-FLAG, FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE, FU-MACRO-SNAPSHOT-TIER-WORSTOF, FDA-6 (creditFlow fabrication). The 5-new-tools-from-07-06-roundtable scope already lives here.

SUPPORTING EPIC 3 (foundational, runs underneath) — **HONEST-DATA-FLOOR HOLD** (keep the OHLCV/integrity floor green so the substrate the above three build on is trustworthy)
- Outcome: the daily_ohlcv writer-integrity P0 cluster lands and stays green so every downstream metric (TA, RSI, sector rotation, conviction) reads clean candles.
- Why now: FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0 + TASK-OHLCV-WIC-1/2 are P0 and gate the accuracy of everything the primary epic measures — calibrating conviction on dirty OHLCV would score noise. This is the load-bearing floor, not new product, but it must not regress while the three epics ship.
- Rough scope / folds in: FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0, TASK-OHLCV-WIC-1, TASK-OHLCV-WIC-2, CLEAN-OHLCV-INTEGRITY-RESIDUE-REPAIR, LINT-OHLCV-WRITE-BYPASS.

### Product decisions needing ARCHITECT technical input
1. CONFIDENCE CONTRACT (primary epic): when a real confidence/source/score value is genuinely unavailable, what is the served honest-null shape — `null` + `is_estimate:true`, an explicit sentinel, or omission — consistently across agentSignalStore, /api/signals/stock, sequential-market-analysis, finalizeBctcRefine, PaddleOCR, RAG? One contract, or per-layer? (folds the 6 confidence-mask FACTORY tasks under one design so they don't diverge.)
2. CALIBRATION LEDGER (primary epic): the cross-agent ledger contract between fb-market-poster (logger) and digest-predict (weekly scorer) — schema, ownership, hit/miss/partial scoring rule (reuse the |move|<NEUTRAL_BAND_PCT trading-day rule from PRED-RESOLVER-GAP-FIX), and where calibrated accuracy re-enters Layer-5 conviction phrasing. Cross-agent .md edits route via agent-father; architect rules the data contract first. Also resolve whether FIX-PREDICTION-SIGNALS-EMPTY (poll-writes-zero) is the SAME pipeline as the resolver leg or a distinct producer gap.
3. SOURCE-LINK ENVELOPE (supporting-1): does source-link/detail metadata ride the existing FE loader-utils safeFetch<T> envelope, or a sibling contract — decided BEFORE FE-PAGE-REORG Wave-2 extracts `<DetailPanel>`/`<ContentCard>`, so the expand contract is baked into the shared components (one-shot rollout) rather than retrofitted per-page.
4. (Systemic, flag-only) local-cowork gateway-blind (ARCH-HEADLESS-GATEWAY-COWORK-NOPOST, high) keeps Agent-spawned data subagents fabricating/no-op — this undercuts goal-1 at the agent layer. USER-side fix (register gateway in .mcp.json), but architect should confirm whether any primary-epic data agent depends on it before dispatch.

## PRED-RESOLVER-GAP-FIX (folded) — 2026-06-21
- task_id: PRED-RESOLVER-GAP-FIX
- trigger: router-verified live diagnosis of dashboard "Dự báo AI & Kết quả" reported not-working (named-vol /app/data/market.db via mcp-server bun:sqlite).
- dedup: board scan found 3 prediction tasks + the resolver one. FIX-DIGEST-PREDICT-ISO-WEEK-DEDUP (done_verified, double-post) / FIX-PREDICTION-SIGNALS-EMPTY (prediction_signals poll write-gap, DIFFERENT table) / FIX-FB-PREDICTION-CALIBRATION-LOOP (poster ledger) all DISTINCT. PRED-RESOLVER-GAP-FIX (backlog, P high) is the SAME producer<->resolver contract bug — FOLDED into it, did NOT mint a dup. Mentioned-test ids (1125/1154 prediction-resolution-job/loop) are test files, not board tasks.
- enrichment over prior framing: prior task said resolver "never fetches actual_price" for ids 6/7. Live diagnosis SUPERSEDES that — the OHLCV bar is MISSING on the exact resolution_date (weekend/holiday) because the resolver matches by EXACT-date equality while create_prediction_claim computes resolution_date in CALENDAR days (evidenceTools.ts:378-380, confirmed by reading code). Added the PRODUCER leg (calendar-day + neutral/null default), which the resolver-only task lacked. Added same-DB LIVE re-verify gate (self-confirming-test trap).
- product call on neutral predictions: KEEP producing neutral claims + ADD a neutral-band rule (|move from creation_price| < NEUTRAL_BAND_PCT default 2.0% over the trading-day window = HIT, else MISS); legacy neutral with creation_price=NULL -> explicit excluded status, never NULL. Rejecting neutral claims would bias hit-rate to directional-only and hide flat-call accuracy (no-fake / full-accountability standing goals).
- what-considered: fold-only vs fold+promote. Chose fold+promote+head-dispatch — WIP=0, head idle, spec now complete, single zone (apps/mcp-server) so dev direct (no architect split). priority P2 (dashboard quality, not safety) per router.
- script: scripts/po-s110-pred-resolver-gap-fold-tradingday-producer-promote.jq (idempotent, conservation-guarded, re-run delta 0).

## DRAIN-STATEFILE-DATALOSS — 2026-06-21
- task_id: FIX-DRAIN-STATEFILE-DATALOSS
- signal: repair_task_request from dev-team (router), HIGH, recurring manual workaround on >=2 ticks
- ground-truth verified by reading code:
  - drain-signals.js L33 globs ALL docs/signals/*.json, no state-file/shape exclusion; L67-69 fingerprint+move-to-processed+unlink ANY file.
  - db-integrity-history.json (33KB live, 7 entries) is sitting in docs/signals/ RIGHT NOW = armed trap.
  - db-integrity-history-append.sh: on MISSING file (drain removed it) -> else branch L30-34 writes `printf '[]'` with NO backup (backup only for non-empty non-array). Effect = silent wipe of all history. Signal effect-claim holds (exact branch wording in signal slightly off; outcome identical).
  - orphan processed/db-integrity-history.json now gone (cleaned since signal authored) but live file still in inbox.
- path readers to update for option (b): scripts/db-integrity-history-append.sh (HIST default L12), .claude/commands/crons/cron-system-auditor.md, .claude/commands/crons/cron-db-data-integrity.md (3 refs). orch-state.json payload_ref rows are historical signal_queue, not live readers — leave.
- what-considered: only path — classify FIX, drive to board, belt-and-suspenders (a)+(b) per router note. (b) relocate state file to docs/data/ is the durable root fix; (a) shape-guard in drain-signals.js prevents ANY future non-signal *.json sweep. Both cheap.
- why-change: no change from router recommendation. zone = cross-service (scripts/ + .claude/ cron docs; no apps code path). dev_agent = dev-mcp-server (owns agents-flow drain helper + scripts/). rebuild_required: false.
- dedup: board scan 0 hits. WIP=0.

## F1-LOCAL-EXECUTION-SUBSTRATE (Foundation-1 formalized) — 2026-06-22T20:13:35Z
- task_ids minted (5, dependency-ordered): F1-GATEWAY-TRANSPORT-PROBE → F1-WRITE-MCP-JSON-GATEWAY → F1-AGENT-FATHER-BLIND-GUARD-REMOVE; F1-LAUNCHD-COWORK-BACKSTOP → F1-CLOUD-TRIGGER-DECOMMISSION.
- source: po-decisions Rev-2 (approved) + architect brief docs/architecture-briefs/2026-06-22-provenance-calibration-local-arch.md (Pillar 1 + Pillar 3, Part-4 sequencing).
- scope discipline: minted ONLY Foundation-1's 5 pieces to .task_board.backlog[]. P-epics (P-1/P-2/P-3) NOT formalized — gated on F-1 real-data path verified (Rev-2 non-goal: do not start any P-epic before F-1 verified; WIP cap stays 2).
- dependency order: (chain A, gateway) PROBE has no deps → WRITE-MCP-JSON blocked_by PROBE → BLIND-GUARD-REMOVE blocked_by WRITE; (chain B, scheduling, parallel) LAUNCHD-COWORK-BACKSTOP has no deps → CLOUD-TRIGGER-DECOMMISSION blocked_by LAUNCHD. Chains A and B are parallelizable per brief Part-4.
- what-considered: only path — mint as backlog rows (status=backlog), do NOT promote to ready (router/dev-team dispatches; PO formalizes). Anchor ARCH-HEADLESS-GATEWAY-COWORK-NOPOST left in place (it is the design-rationale anchor; F1.1-F1.3 are its concrete decomposition).
- ground-truth corrections vs brief: brief Part-3 slot labels (chef-intraday / bctc-analyst-slot-1) are loose. Real active slots (cowork-schedule.json slot_ids) = chef-morning, chef-eod, chef-evening, digest-sunday, tnb-audit, with verbatim trigger_ids carried into F1.4/F1.5 desc. 5 active triggers, not the brief's loose count.
- gates encoded: F1.2 carries explicit USER-RESTART gate (.mcp.json only reaches subagents spawned after restart; done_verified WITHHELD). F1.3 owns the post-restart test-spawn gate that releases F1.2 done_verified AND must precede blind-guard removal. F1.5 carries the HARD sequential gate (2 successful launchd fires logged per slot before trigger_status flips; decommission=set decommissioned NOT delete — platform has no delete).
- why-change: none from approved Rev-2 + brief; pure formalization. zones: agents (PROBE/WRITE/BLIND-GUARD/DECOMM), cross-service (LAUNCHD scripts).
- script: scripts/po-s112-f1-local-execution-substrate-mint.jq (idempotent id-guard, conservation backlog+=5 / all other lanes byte-stable, .head untouched; re-run delta 0 verified). backlog 271 → 276.

## BCTC-SLA-ESCALATION (router signal triage) — 2026-06-25T02:16Z
- signal: router-bctc-sla-esc-20260625-0148 (router->po, priority_recommendation, INFO). Bump FIX-BCTC-SLA-THRESHOLD-360 P3->P1; bctc-discover stale false-CRITICAL recurred 06-19/06-22x3/06-25, each burns auditor+router+telegram + manual RAW-verify (VPS up, queue=0, off-season every time).
- STATE ON ARRIVAL: signal already RESOLVED by the prior pass (stamped po-s112) at 02:15:29Z — THRESHOLD-360 already bumped P3->P1 AND promoted backlog->ready (next_agent=dev-mcp-server, zone apps/mcp-server/). Scope was also corrected: router '120 vs 360' framing outdated (system-map SLA resolver already shipped); real roots are (a) mcp get_sla_status legacy 120/360 config diverges from the resolver, (b) 168h out-of-window too tight for the ~10-week inter-quarter quiet gap, (c) push-age->host-down inference wrong (gate on service-state+queue). I concur with that disposition — it is the durable root fix, not a symptom mute.
- MY DECISION (the open question po-s112 left): the SIBLING FIX-BCTC-SLA-FRESHNESS-EXCLUDE-TERMINAL (P3 backlog, same file freshnessSlaChecker.ts, same freshness-age logic) — router asked whether it should ride along / merge.
  - what-considered: (1) merge into THRESHOLD-360, (2) bump+promote it alongside, (3) sequence-after + leave P3 backlog.
  - chose (3). FRESHNESS is an ORTHOGONAL metric-semantics change (exclude url_not_found terminal rows from freshness age) with its OWN verification gate; its own spec says it "can be closed no-op" if THRESHOLD-360 done_verified shows SLA stable with terminal rows present. It is NOT one of the 3 recurrence roots. Merging would bloat a clean S FIX with an unrelated gate; promoting alongside would race the SAME FILE (contention). Its dependency FIX-BCTC-DISCOVER-CURRENT-QUARTER-ZERO-PUSH is already done_verified, so it's unblocked but deliberately serialized.
  - applied: in-place annotated FRESHNESS with sequence_after=FIX-BCTC-SLA-THRESHOLD-360 + po_sequence_note (may close no-op); annotated THRESHOLD-360 with po_sibling_decision. NO lane move, NO priority change, NO new task.
- PROMOTE-TO-READY: NO additional promotion. THRESHOLD-360 already occupies the ready slot (WIP=1, cap 2). Did NOT promote FRESHNESS (gate = after THRESHOLD-360 lands; same file). Board head is done/idle — router dispatches THRESHOLD-360 next cycle; keeping WIP=1 lets it dispatch cleanly without same-file contention.
- backlog-head scan: 283 backlog rows; head is F1-LOCAL-EXECUTION-SUBSTRATE chain (gated on F-1 verified, WIP cap stays 2 per Rev-2 — do not start P-epics). No unblocked, non-same-file, higher-value task warranted jumping the queue this idle tick. Left rest in backlog.
- signal status: already RESOLVED (po-s112). Left RESOLVED (correct terminal state) — added no churn.
- script: scripts/po-s113-bctc-sla-freshness-sibling-sequence.jq (idempotent marker-guarded, conservation all-lanes byte-stable, re-run delta 0).

## OUTAGE-ARTIFACT-7SIGNAL-SWEEP (router signal triage) — 2026-06-26T04:33Z [task_id: po-s118]
- context: Docker host outage (~22:11Z 06-25) recovered, but Claude session's MCP gateway client stayed disconnected ~4h until /mcp reconnect ~04:2xZ 06-26. system-auditor fired against a flapping/blind gateway -> 7 NEW signal_queue rows, MOST outage/disconnect artifacts now false. Triaged all 7 in ONE atomic pass (po-s118).
- RAW-verify-first (corroboration gate, router verify-not-badges): gateway+api-gateway LIVE 04:27Z (get_market_snapshot VN-Index 1856.14 -0.37%, breadth 76/211/55 written 04:27:20Z); agent_signals 7593/7594 fresh; task_list_held probed for both flagged locks.
- D1 sau-2026-06-26T00:10:15Z (HIGH A-12 api-gateway CURL_ERR) -> RESOLVED: outage/disconnect transient, gateway RAW-live. No task.
- D2 sau-...-c06 (WARN C-06 market_messages 0/3h) -> RESOLVED: fired 00:31Z pre-market(07:31 GMT+7)+outage window=expected 0; pipeline RAW-live post-reconnect (chef-intraday slot running 04:24Z); off-market false-positive class already tracked FIX-AUDITOR-C06-OFFMARKET-RECALIBRATE(ready). No new mint.
- D3 sau-...-c11 (WARN C-11 pdf_done 0/48h) -> RESOLVED: bctc off-season idle (Healthy-Idle Gate); predicate bug already tracked FIX-AUDITOR-C11-PDFX-STATUS-PREDICATE(backlog). No new mint.
- D4a sau-d4 esc-datacov:FPT:Q1-2026:ESC-3 (LOW orphan) -> RESOLVED, NOT RELEASED: DIVERGED from disposition premise — RAW task_list_held shows it ACTIVE (owner bctc-analyst, claimed 2026-06-24 PRE-outage, expires 2026-07-02, TTL 8d). It is an INTENTIONAL ESC-3 escalation lock with no task row BY DESIGN; releasing would drop a live ~6-day escalation. Recurring D4 blind-spot (po-s103/po-s76/po-s95 precedent); durable fix tracked FIX-D4-HELD-LOCK-NO-BOARD-ROW-RECONCILE + FU-AUDITOR-D4-SIGNAL-ID.
- D4b sau-d4 bctc-analyst-slot-2:2026-06-25 (LOW orphan) -> RESOLVED: stale blind-run lock; task_release(owner_agent=bctc-analyst) returned ok:false (already GC'd) -> LET-EXPIRE.
- D5 sau-...-c08 (CRITICAL orphaned_alerts:1) -> READ, router_dup_of FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID: 1 orphan = known alert_id orphan-FK class (record-and-leave); CRITICAL overstated for 1. No new mint.
- D6 sau-2026-06-26T00:31:41Z (CRITICAL task_board overflow 248/cap80) -> READ + MINT: RAW status-breakdown of the 248 active_sprints[].tasks[] = 242 (~97.6%) TERMINAL (163 DONE/40 DONE-LIVE-VERIFIED/...); active WIP(ready+in_progress+qa+review)=1. Predicate counts lifetime-accumulated CLOSED sprint tasks vs an active-WIP cap and emits CRITICAL where design action is the routine pm task-archive sub-flow => FALSE-CRITICAL. Minted FIX-AUDITOR-TASKBOARD-OVERFLOW-PREDICATE-WIP-ONLY (P3/LOW, backlog, PLAN-ONLY, zone docs/agents/system-auditor/, next_agent ba).
- what-considered: D4a only-divergence — disposition said release both; RAW proved esc-datacov is a live legitimate escalation, so verify-not-badge => resolve-not-release (no destructive lock drop). All else per disposition.
- why-change: none from disposition except D4a (RAW evidence overrode the stale-lock premise).
- script: scripts/po-s118-outage-artifact-7signal-triage.jq (idempotent: status==NEW + id-not-in-lane guards; conservation NEW 7->0, backlog 302->303, ready/in_progress/review/done/done_verified byte-stable, rows count unchanged; atomic temp->jq -e->python3 json.load->lane-type->conservation->CAS-mtime->mv). PLAN-ONLY, WIP stays 0.

## SPIKE-BCTC-2025Q4-PDFPULL-OCR-0ROW handoff triage — 2026-07-10T09:26Z [task_id: OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE]
- context: the developer SPIKE I armed round-6 completed (findings docs/spikes/SPIKE-BCTC-2025Q4-PDFPULL-OCR-0ROW.md commit 336a6ce11; journal developer-S13). Router handed it back for a fix-path decision (ops mitigation vs structural fix vs both).
- RAW-VERIFIED the SPIKE's root cause myself (not payload-trust): (a) `pdfExtractorClient.ts:65` POSTs ONLY `${BASE}/extract` — never a table endpoint; (b) `/pek-extract`'s only caller is manual `POST /api/trigger-pek-extract` (bctcVpsIngestHandler.ts:242); (c) `bctcBatchTableBackfillJob.ts` (sole `/extract-tables` caller) is imported ONLY by itself + its test = dead code; (d) `grep -ri pek apps/mcp-server/src/scheduler` = ZERO matches → PEK never wired into any cron. Confirms: bctcPdfPullJob's */30 cron populates scalar `financial_reports` only, so its own 0-row gate fires `enrich_failed` unconditionally batch-wide. NOT a Jul-8 regression (corroborated by architect SPIKE 2026-07-03-bctc-discover-pipeline-dead.md predating fdb424178).
- endpoint contract RAW-checked: trigger-pek-extract returns 202/async, 404-on-null-pdf_path, propagates pdf-extractor 503 verbatim as the VN-market-hours guard. Current UTC 09:26 is OUTSIDE the 02:00-08:59 guard → PEK triggers allowed NOW.
- dedup (churn-not-product ★07-04): scanned board — NO existing task wires table-extraction into the pull cron. `BA-FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT` = column-parse ACCURACY (post-extraction, different concern); `FIX-ERRAUDIT-W3-PEK-P2` = pdf-extractor error-handling P2; `PDF-TEST-01-FIX` = test coverage. Structural fix is NOT a duplicate.
- DECISION = BOTH tracks (this is the ONE tick where escalation-condition is VERIFIED post-fix-window, not pre-fix detection breadth — so it's convergence, not a churn round):
  - TRACK 1 (UNBLOCK, dispatch to ops NOW): continuation of the live IN_PROGRESS OPS row — NOT a new mint. Per report_id: (i) 8 tickers w/ pdf_path set (ACB/BID/EIB/HCM/HSG/NKG/POW/SSI) → `POST /api/trigger-pek-extract {report_id}` each; (ii) GVR/MBB/D2D (pdf_path IS NULL, file on disk) → sync pdf_path via backfillBctcPdfPaths (or UPDATE to /app/data/pdfs/<T>_2025_Q4.pdf) THEN trigger; (iii) GAS (no financial_reports row) → create minimal shell row (action_code/sort_key/pdf_path) THEN trigger. All outside 02:00-08:59 UTC. RAW re-probe bctc_table_rows per report_id after — do NOT trust status echoes (verification-taint discipline). Unblocks the current cohort; does NOT prevent recurrence.
  - TRACK 2 (SPRINT-S, mint → Step 2 planning): wire automatic table-extraction into bctcPdfPullJob, DECOUPLED from the synchronous 0-row gate (PEK is 202/async → gate must become deferred/next-cycle, else it still reads 0) + pdf_path populated at PDF-save time + financial_reports shell-row creation decoupled from the legacy OCR-confidence gate (_OCR_CONFIDENCE_THRESHOLD=0.5). Zone = apps/mcp-server/ — SPIKE RAW-verified NO apps/pdf-extractor code change needed (PEK endpoints + push-back path already exist/work); architect escalates to `multi` ONLY if the deferred-gate write-back contract genuinely needs a pdf-extractor callback change. SPRINT-S not FIX because the async-gate decoupling is a real design decision (architect) spanning 3 concerns (ba decompose).
- why BOTH not ops-only: ops-only leaves the */30 cron firing enrich_failed on EVERY future quarter/ticker forever (VCI/VHM already hit it live outside the cohort) — that IS the recurring-bug-escalation trigger. why BOTH not structural-only: 12-ticker cohort is blocked NOW and the fix path (manual PEK) is already-built + health-confirmed + market-window-open → immediate unblock has zero code risk.
- I hold NO lock (router holds task:po-triage; board mid 3-fire churn) → NO orch-state write this tick; disposition returned as BATCH for router to mint TRACK 2 + dispatch TRACK 1 on the live OPS row. Files: po-decisions.md + po.md notebook only.

## cowork guaranteed-slot recurring-miss SCOPE-EXPANSION triage — 2026-07-10T11:00Z [task_id: SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING]
- context: router handed a long-deferred HIGH signal (dev-team tick 08:37Z, task#27) with an explicit "triage now, not defer again" instruction. Signal `cowork-recurring-guaranteed-miss-scope-expansion-20260710T0917Z` (from cowork-dispatcher, to po) is a SCOPE EXPANSION of prior signal `cowork-recurring-guaranteed-miss-20260710T0847Z` (already READ/triaged into the DELIVERY-side OPS-COWORK-GUARANTEED-SLOT-INSTALL launchd-firer, now REVIEW).
- new evidence: a THIRD guaranteed slot **fb-daily (09:15Z, fb-poster parallel_group)** missed the same cron tick on BOTH 2026-07-09 and 07-10 — same `_superseded_by=cowork-dispatcher` deprecated-backstop pattern as chef-morning (05:15Z) + chef-eod (08:45Z). fb-daily is NOT in the chef parallel_group ⇒ the defect is **dispatcher-wide across ALL guaranteed slots**, not CHEF-specific. Session guaranteed-miss count = 9.
- DECISION = ONE SPIKE, NOT 3 per-slot FIX (matches the signal's own recommendation + ★07-04 churn-not-product + main.md SPIKE definition "exploratory question, no clear scope"): three non-overlapping slot groups sharing one symptom is ONE root cause; the WHY (why does supersede mark guaranteed slots undeliverable even when a backstop should fire) is exploratory → findings-doc SPIKE, not a scoped FIX. PLAN-ONLY per project_anomaly_task_bridge — minted BACKLOG, NOT promoted to ready (WIP stays 0), NO live dispatch, NO code change.
- dedup (board-wide, churn-not-product): NO duplicate. `OPS-COWORK-GUARANTEED-SLOT-INSTALL` (REVIEW) is a DELIVERY-side launchd-firer WORKAROUND (fires slots externally; complementary to but NOT a root-cause investigation of the dispatcher supersede wiring; first live fire 2026-07-11T05:15Z). `FIX-COWORK-LASTFIRED-DECOUPLE-FROM-DELIVERY` (BACKLOG) = last_fired-bumped-on-dispatch MASKS misses (adjacent detection defect, not this dispatch-path defect). `FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK` (BACKLOG P1) = curl-bypass retrofit. `SPIKE-GATEWAY-BLIND-CLI-HANDSHAKE` (DONE) = prior art (gateway-blind = CLI MCP-client handshake). All cross-referenced in the SPIKE `note`.
- deliberately NOT reconciled by PO (investigator's job, folded into the spike question): the two source signals disagree on trigger_id state — prior signal says chef slots have trigger_id NON-null, this one says fb-daily trigger_id=null. The spike must resolve whether trigger_id presence is even relevant when `_superseded_by` is set.
- MINTED (BACKLOG, PLAN-ONLY): `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` (high, cross-service/, size S, mode spike, timebox 120, owner po, origin_signal_id stamped). zone cross-service/ = spans docs/agents/cowork-team/flow/ + docs/data/cowork-schedule.json + scripts/agents-flow/. Route to architect after normal po→ba→pm chain; deliverable = findings doc + durable-fix recommendation.
- writes: single `scripts/orch-apply.sh` transform (append SPIKE to task_board.backlog + flip signal NEW→READ). RAW-verified both landed (backlog 391→392; signal status=READ). Gateway bridge unreachable this session (3rd tick) — proceeded local-only (orch-apply needs no gateway). Did NOT commit (router commits after report). No `.head`, no in_progress row, no lock touched.

## verify-prune SWEEP adjudicated dispatch + 21-report closeout — 2026-07-10T16:37Z [task_id: BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP]
- context: dev-team tick, pendingSignals EMPTY, CI GREEN, WIP=0/idle. Router surfaced 21 unresolved telegram reports + the SHG-2..SHG-5 stale-pickup finding (completion commits landed but rows still BACKLOG; 129 live coherence warns; orch-validate.mjs:375 hard-fail flip still gated).
- what-considered: (1) mint new FIX for the BCTC ENRICH 0-rows cluster; (2) mint new task for OHLCV-DEPTH stall; (3) close SHG-2..SHG-5 as stale-picks; (4) promote the supervised verify-prune sweep; (5) NOTHING.
- why-change from a naive "mint per cluster": REJECTED (1)+(2) — dedup board-wide proved BOTH clusters already tracked + converging (BCTC: OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE REVIEW + BCTC-REFINE-STALL-RETRIGGER ACTIVE + FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION done_verified, all from my own AM triage; OHLCV: FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS, honest per-code thin-code gap). New mint = dup churn (★07-04). REJECTED (3) — SHG-2..5 have genuine open work behind the landed commits (129 coherence violations + the gated hard-fail flip); rubber-stamp close would be a false-green.
- DECISION = (4) promote `BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` backlog→ready (next_agent=architect, zone=cross-service, size M, P1, supervised→false, adjudicated_dispatch). WIP=0 = the sanctioned window for an adjudicated supervised-row dispatch; this is the convergence vehicle for the SHG cluster, not more churn.
- ROOT found (RAW): scripts/orch-cold-evict.sh evicts ONLY from .done/.done_verified/.active_sprints — never scans backlog[]; so status-flip-without-lane-move strands terminal rows in backlog[] permanently. Concrete: 55 terminal (34 DONE + 12 CANCELLED + 9 DONE_VERIFIED) + 12 mislaned active (5 REVIEW/4 BLOCKED/3 IN_PROGRESS) + 1 review-lane DONE_VERIFIED + ~62 DEFERRED/TODO/HELD = the 129 coherence warns + the 385-bloat feeding recurring stale BOUNDED-1 picks. Sharpened D0-D5 scope onto the row; D4 = fix cold-evict to scan all lanes (durable root-fix); D5 = flip orch-validate.mjs:375 warn→hard-fail then close SHG-2..5.
- folded SHG-2/3/4/5 in-place (`folded_into=BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` + blocked_reason marker) — linked, NOT closed.
- reports: resolved ALL 21 via bridge `process_telegram_report resolution=monitoring` (honest "tracked, convergence pending" — NOT "fixed", reparse not QA-signed). status→processed clears both new + unresolved sets (query excludes status=processed). Verified unresolved 21→0. Learned: `resolution` is an ENUM {none,fixed,wontfix,duplicate,monitoring}; free-text → -32602.
- writes via `scripts/orch-apply.sh` (conservation OK 538=538; backlog 385→384, ready 0→1). Did NOT commit orch-state — router re-verifies + commits. No `.head`/in_progress/lock touched. PO does not spawn — router PRE-CLAIMs + spawns architect on the RETURN.

## CONTAM-11-REMEDIATE stale-row disposition — ARCHIVE (obsolete-by-other-lanes) — 2026-07-11T12:23Z [task_id: CONTAM-11-REMEDIATE]
- context: targeted single-objective pass (router directive, NOT full board triage). Disposition the stale backlog row CONTAM-11-REMEDIATE, whose title claims repair of "3023 contaminated-anchorless sub-1000 daily_ohlcv rows (9 tickers BMP/MCH/HGM/PMC/KSV/TOS/AGX/TBD/STS)". pm already raw-verified on 07-11 (commit 07cd7d848) that only 4 residual rows remained (BMP/HGM/KSV/MCH x1 each) and correctly SKIPPED it at pickup, dispatching the verified-fresh alternate WATCHLIST-DB-SYSMAP-DRIFT-FIX instead (now DONE, qa-APPROVED round 2, d5a922f45).
- LIVE RE-VERIFY (this tick, one bound query, read-only): the serving DB is the Docker NAMED VOLUME vn-market-intelligence-mcp_market_data at /app/data/market.db (confirmed via `docker inspect` mounts) — the host apps/mcp-server/data/market.db is a stale/separate copy and MUST NOT be probed (feedback_live_db_is_named_volume_not_host_data). No sqlite3 in-container → `docker cp` the live market.db + WAL + SHM out to scratchpad, queried WAL-inclusive with host sqlite3. Predicate: `code IN (9 tickers) AND close < 1000`. Result: **6 residual rows, ALL TBD** (close 100-135, dates 2023-06-28..2023-07-06); BMP/HGM/KSV/MCH/PMC/TOS/AGX/STS all 0. Total daily_ohlcv rows = 754,216.
- KEY EVIDENCE: pm's probe (~2h earlier, 4 rows: BMP/HGM/KSV/MCH) and mine (6 rows: TBD) have COMPLETELY DIFFERENT composition. That divergence is not an error — it proves the residual is a MOVING TARGET actively churned by ongoing OHLCV normalization lanes (backfill seeder + CONTAM-10 predicate + others). Both probes agree the "3023" claim is ~99.8% stale (bulk repaired elsewhere).
- DECISION = ARCHIVE (backlog→done_verified), NOT RESCOPE. Rationale (best reduces backlog staleness debt, the standing invariant): (1) a RESCOPE pinned to specific ticker rows would re-stale within hours precisely because the residual composition moves tick-to-tick — it would regenerate the exact stale-pick hazard pm already hit; (2) 6/754216 = 0.0008%, historical 2023 dates, is below any reasonable action threshold; (3) the row's own acceptance criteria mandate DESTRUCTIVE x1000 remediation gated by VPS external-anchor + mandatory BACKUP (407MB DB) + off-hours window + PO go/no-go — machinery wildly disproportionate to 6 stale rows.
- NO FRESH ROW MINTED. The residual is below threshold and I cannot even confirm from a count query whether the 6 TBD rows are bucket-(b) contamination or bucket-(a) legit-cheap (that anchor-logic determination is dev/ops' job, not PO's). The correct self-regenerating re-detection path already exists: the OHLCV contamination auditor / CONTAM-10 detector re-emits a signal → fresh backlog row via the anomaly bridge if the residual ever crosses threshold. A manually-pinned row would be pure churn (★07-04 churn-not-product).
- dedup (board-wide): NO duplicate. The bulk-repair lanes (CONTAM-10 family, OHLCV normalization) are the DONE convergence vehicles; this row is the last stale artifact of a completed campaign, not open work.
- WRITE: single `jq -f scripts/po-archive-contam11-obsolete-by-other-lanes.jq --arg NOW <ISO> | bash scripts/orch-apply.sh` (transform persisted, guarded to abort unless exactly 1 CONTAM-11 in backlog). orch-validate Stage-0/1 PASS + orch-conservation-check OK (task_total live=458 candidate=458 CONSERVED — net-zero lane move) + CAS-mtime + atomic rename. backlog 314→313, done_verified 0→1. Closure metadata (status DONE_VERIFIED + closed_at + verify_note full-provenance + status_note) stamped on the moved row; backlog-detail.json archive entry synced BACKLOG→DONE_VERIFIED with closure_note (secondary hygiene, non-hot file, temp+mv). Did NOT commit here if router owns commit — but this is a self-contained targeted pass, so PO commits its own explicit paths. No `.head`/in_progress/lock touched. PO does not spawn.

## GO/NO-GO Docker-daemon restart — mcp-server D-state wedge — 2026-07-11T~21:00 VN (market closed) [incident: mcp-server-1 wedged since 13:44Z]
- context: URGENT single-decision, no full triage. Router-verified raw + ops diagnostics. mcp-server-1 PID 33194 in uninterruptible kernel D-state inside Docker Desktop VM; logs frozen 13:29:28Z right after "[bctcPdfPull] PDF saved — PDR Q4". Mem 395MiB/3GiB, CPU 0.13% — NOT resource exhaustion. Healthcheck failing streak 33+. My own MCP call_tool is DOWN (that IS the incident) — decided from file evidence, no tool probes.
- container-level remediation EXHAUSTED (ops): `docker compose restart` FAILED ("did not receive an exit event"), `docker kill` SIGKILL FAILED. SIGKILL-immune is the definitive tell: a D-state (uninterruptible sleep) process cannot be reaped by any in-VM signal — the kernel task is blocked in a syscall and only a VM/kernel restart clears it. On macOS Docker Desktop there is no host-side cgroup/kernel surgery available (Linux kernel lives inside the LinuxKit VM). Ops did the right ladder and hit the architectural floor.
- BLAST RADIUS OF INACTION = TOTAL and open-ended: whole vn-market tool surface dead — task-lock mutexes, send_telegram (user comms DOWN), all cowork agent MCP calls, DB writes. Pipeline stays dead until host-level action. There is no lower-cost remediation left below daemon restart.
- window: Friday ~21:00 VN, market CLOSED — the lowest-impact window that exists; ~15 min of already-lost in-VM output (13:29Z→now), all off-market, minimal.
- fleet auto-recovery is DESIGNED-IN: router verified every key container (mcp-gateway, headroom-proxy, +~14) carries restart=unless-stopped → daemon restart brings the whole fleet back; ~1-3 min outage.
- rollback NOT on the table (correct): image is the QA-approved watchlist-fix, deployed 11:47Z, ran healthy ~2h before wedging — the wedge is a VM-runtime D-state event, NOT a code regression. Reverting a good image would add a false variable and lose the fix; nothing in evidence implicates the image.
- ONLY real risk weighed = SQLite mid-write freeze. bctcPdfPull was writing when it froze. WAL journaling is crash-safe BY DESIGN (that is precisely what WAL is for), BUT this project has a burned-in precedent: "SQLite corruption = Docker virt" (project_sqlite_corruption_fix.md) + FIX-MCP-CRASH-LOOP-WRITEWAL. So I do NOT wave it through on WAL theory alone — I gate GO on a pre-restart safety copy + post-restart integrity proof. Cheap insurance vs a 754k-row serving DB.
- DECISION = **GO**, conditioned. This is not a close call: remediation floor reached, blast radius total, window optimal, recovery automatic, one risk fully mitigable by a copy-first + verify-after gate. NO-GO would mean choosing indefinite total-pipeline death over a 1-3 min mitigated outage — indefensible.
- CONDITIONS (ordered, ops MUST follow):
  1. SAFETY-COPY FIRST — before touching the daemon, snapshot named volume `vn-market-intelligence-mcp_market_data` via a THROWAWAY alpine container mounting the volume read-only (`docker run --rm -v vn-market-intelligence-mcp_market_data:/src:ro -v <hostdir>:/dst alpine cp -a /src/. /dst/`). Copy ALL SQLite files together — market.db + market.db-wal + market.db-shm — so the snapshot is crash-consistent/recoverable. This container does NOT touch the wedged mcp-server. Verify the copy is non-empty + host `sqlite3 <copy> "PRAGMA quick_check;"` returns ok BEFORE proceeding. If the copy step itself hangs (shared volume contention with the wedged writer) → do NOT block the restart on it; log that the pre-copy was unobtainable and rely on WAL crash-safety + post-restart check.
  2. DAEMON RESTART via escalation ladder, least-destructive first: (a) `docker desktop restart` (graceful) → (b) full quit + relaunch Docker Desktop app → (c) ONLY if the VM refuses to shut down (D-state blocks VM teardown), host reboot — user-only. Comms are DOWN, so for (c) ops documents the state to file and is ready to retry send_telegram once partial recovery restores the tool.
  3. RESTORE COMMS FIRST post-restart — as soon as mcp-gateway + mcp-server return, verify send_telegram works and post an incident-status line to WORK before any further work. Comms-down is a first-class casualty here.
  4. POST-RESTART INTEGRITY GATE (mandatory, blocking) — against the LIVE named-volume serving DB (/app/data/market.db, WAL-inclusive; NOT the stale host copy): `PRAGMA quick_check;` MUST return ok, then serving probes (a representative get_* tool round-trip returning plausible non-zero rows). If quick_check is NOT ok OR probes fail → STOP, restore from the step-1 safety copy, re-verify, then re-probe. Do not declare recovery on container "healthy" alone (passive health masks dead data — feedback_passive_health_masks_dead_data).
  5. RECONCILE + REPORT — confirm all ~14 containers back up; note the ~15 min in-VM buffered-write loss (13:29Z→restart, off-market, accepted); file the incident closure referencing ops notebook 47075dafb. If mcp-server re-wedges within one pull cycle, that is a NEW signal (bctcPdfPull-adjacent hang) → BUG channel, do not silently re-restart.
- disposition = DECISION-ONLY, no board write, no dispatch, no `.head`/lock touch (my MCP is down anyway). Router hands to ops to execute. PIPELINE continue. NEXT ops.

## FR-FUNC-02-FIX disposition — DONE_VERIFIED (false-warn reclassify, no code) — 2026-07-13T17:28Z [task_id: FR-FUNC-02-FIX]
- context: dev-team BOUNDED-1 idle-pickup selected FR-FUNC-02-FIX (rank-1 eligible, priority normal) but pick-time pre-verify WITHHELD auto-launch — board (type=fix, owner/zone dev-mcp-server) diverges from ground truth. Asked to RAW-verify + disposition so BOUNDED-1 does not re-pick.
- RAW-VERIFY (this tick, read-only): gateway call_tool(vn-market, get_bctc_full, code=HPG) → fully-structured financials (net_revenue 52,900.8 tỷ, total_assets 259,327.5 tỷ, equity 139,781.8 tỷ, ROE 6.5%, structured_data JSON, source_tier=2, confidence 80%). Check "does get_bctc_full return structured data for a ticker with known BCTC" PASSES.
- EVIDENCE the WARN was an auditor probe-error (CLUSTER-G): sprint-QUALITY-BURNDOWN-phase4-verdict.md records "FR-FUNC-02 → PASS" (graceful degrade = expected); 2026-06-10-quality-burndown-strategy.md line25/69 classifies FR-FUNC-02 as CLUSTER-G RECLASSIFY, "Fix expected. No code", owner qa/system-auditor. The 2026-06-10 probe used VCB (no ingested BCTC → correct graceful-empty "Chưa có dữ liệu BCTC"), not a code defect.
- DECISION = DONE_VERIFIED (backlog→done_verified, no code, no data-ingest). Row was mis-scoped as a dev code-fix. Not a coverage gap (get_bctc_full works; VCB absence is a separate matter this check does not exercise). No depends_on referenced FR-FUNC-02 (safe move). WRITE via one jq | orch-apply.sh (validate+conservation PASS, task_total 509→511 net +2 incl. 2 sibling intakes). NO code dispatched.
- SYSTEMIC FLAG (not actioned this tick — first pick, churn ≤2): FR-FUNC-02-FIX is one of a ~17-row CLUSTER-G `*-FIX` cohort still on the backlog at priority normal (FR-DEGRADE-01-FIX, SEC-FUNC-01/03-FIX, SEC-TEST-01-FIX, MD-FRESH-01/02/03-FIX, NEWS-OBS-01-FIX, ALT-PERF-01-FIX, ALT-FUNC-03-FIX, KD-FRESH-01-FIX, ANA-TEST-01-FIX, CI-FRESH-01-FIX, FW-FRESH-01/OBS-03-FIX, CO-OBS-01-FIX, PA-CONSIST-01-FIX). All were already re-probed PASS/INFO in phase-4 verdict but the parallel `*-FIX` rows never got reconciled — each will churn through BOUNDED-1 one-by-one. RECOMMEND: batch-reclassify the cohort (owner→qa/system-auditor, no code) to converge instead of one-row-per-tick. Left as BATCH recommendation for dispatcher; NOT mass-mutated on a first-pick tick (proportionate).

## Signal intake this tick — 2 new backlog rows (PLAN-ONLY) — 2026-07-13T17:28Z [task_id: dev-team-triage-20260713]
- FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT (FIX, high, apps/mcp-server/, deploy-gated): durable fix for recurring SILENT-FAILURE class. PO call = NOT a bare timeout bump (evidence: 480s direct call still failed, tesseract 17+ min). Two-part AC: (1) fail-loud — push-bctc-pdf must not return ok:true / mark queue row done when OCR times out; keep row retryable + surface staleness; (2) route large scanned PDFs via async /api/trigger-pek-extract. Architect design touch: async-reroute vs sync-bump + threshold. Post-deploy: reset bctc_vps_queue row 223 (HPG Q4-2025) done→pending. Closes HPG consolidated coverage-gap (followup_of HPG-DISCOVER-CONSOLIDATED-PDF). Deploy = dispatch OPS to attempt rebuild once through front door, capture any classifier denial verbatim (2026-07-13 ruling), not user-wait.
- DEBT-SCRIPTS-MIGRATIONS-TSC-COVERAGE (CLEAN, low, cross-service/, plan_only): scripts/migrations/ outside all tsconfig include globs → tsc-clean claims false-clean-by-omission. Add tsc coverage (tsconfig include OR standalone `tsc --noEmit` in CI) then fix 3 QA-surfaced errors (discover-consolidated-bctc-pdf.ts:395 Blob/Uint8Array + reparse-bctc-reports.ts x2). Zero runtime impact (Bun strips types).

## Ultracode "fix all" audit mint — 2026-07-13T19:31Z [task_id: ultracode-audit-fixall-mint]
- CONTEXT: router-delegated (gateway-blind router). User directive "fix all" on docs/architecture-briefs/2026-07-12-ultracode-workflow-improvement-audit.md (verdicts FINAL 2026-07-13: 17 CONFIRMED / 23 RESCOPE / 8 REJECTED / 58 UNVERIFIED; 118 issues, 3 CRITICAL). PO TRIAGES+MINTS only — no implementation.
- GATEWAY OUTAGE (deviation, per task fallback): PRE-CLAIM `intent:po:ultracode-audit-fixall-mint` could NOT be acquired — BOTH `mcp__gateway__call_tool` AND `mcp__claude_ai_gateway__call_tool` return "no such tool available" (gateway server at zenmidi.com unregistered this session — consistent with 07-11 Docker/mcp-server incidents in git log). Task directive explicitly authorizes: proceed with mint anyway (idempotent-deduped by row id). No lock to release (never acquired). This outage IS live evidence for critic-gap (b) — minted UC-CRITIC-GATEWAY-CONTRACT-DRIFT.
- WHAT CONSIDERED: only path — mint per the 5 rules. CONFIRMED→fix rows (S-effort/critical first: UC-RDL-P1 lock-prefix P0 = sequence lead); RESCOPE→rows scoped per the verifier Rescope note NOT original proposal; UNVERIFIED→per-domain PLAN-ONLY umbrellas (pick-time pre-verify mandatory); REJECTED→0 rows; critic gaps→PLAN-ONLY (2 high-risk priority + 1 batch). DEDUP-FIRST board-wide.
- DECISION = 49 rows minted to backlog (16 CONFIRMED + 22 RESCOPE + 8 UNVERIFIED-umbrella + 3 CRITIC) under sprint ULTRACODE-AUDIT-FIXALL, all status:BACKLOG (NOT promoted; dev-team drains). 8 existing rows annotated (marker-guarded audit_ref) instead of double-minted: router-P3→FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD (folds AC1+AC3, AC2 residual, do NOT close), dev-team-loop-P3→FIX-DEVTEAM-EPIC-WRAPPER-AUTOCLOSE-SWEEP (ship vehicle), cowork-P6-Piece2+MDH-P2→TE-T05, cowork-P1-rider→TE-T11, MDH-P4-coord→TE-T33, ASL-P3-repoint→FIX-AUDITOR-C11+C06, git-P2/P8→SYSREMAKE-P2 RC-GITSTATE.
- REJECTED (8) = 0 rows. auditor-signal-loop-P4 (D4 janitor FP) special-case: verified code ALREADY LANDED (commit e109f49f8; all 3 markers live — expired:false @834, KNOWN_LEGIT_PREFIXES+applyR1bFilter, parsePriorD4Candidates+applyR4bDebounce; last FP batch 2026-07-08 pre-fix, zero since). NO open board row tracks FIX-D4-HELD-LOCK-NO-BOARD-ROW-RECONCILE in any lane → nothing to close (already evicted). Residual doc-hygiene (handlers.md:19 stale note) NOT minted per rule-2 (agent-father doc-only, out of scope).
- WRITE: one atomic `jq -f scripts/po-s143-ultracode-audit-fixall-mint.jq | bash scripts/orch-apply.sh` (Stage0+1 Zod PASS, conservation task_total 511→560 net +49, CAS clean). Dry-run pre-checks: JSON-valid + delta=49 + 0 rows missing zone + idempotency re-run delta 0. Script registered in po/flow/scripts-registry.md. NO code dispatched. NEXT = dev-team BOUNDED-1 drains; router re-arms hourly cron separately.

## ALPHA-S2-FF epic-close follow-up intake (PLAN-ONLY, WIP=0) — 2026-07-15T00:46Z [task_id: ALPHA-S2-FF-followup-triage]
- CONTEXT: router (dev-team) CLOSED epic ALPHA-S2-FOREIGN-FLOW-WRITE-RACE (parent+SUB1-5 done_verified; qa APPROVE 36c1e3148; CI-green 4491a1f2e; umbrella mutex released). head IDLE, WIP=0. Two PLAN-ONLY follow-ups to triage into backlog — NO sprint launch, NO WIP raise, NO dev dispatch, NO `.head` touch.
- (1) MINT `FIX-MCP-TEST-SUITE-INTERVAL-TIMER-LEAK-TEARDOWN` → backlog[] (P3, apps/mcp-server/, dev-mcp-server, sprint=TEST-HYGIENE, plan_only). Root cause = tests arm setInterval-style sla-monitor(periodic seed-check)/reaper('periodic reaper armed') timers with NO unref/clearInterval in afterAll → leaked intervals hold Bun event loop open + flood log ('not seeded yet (age=-1)') → full `bun test` never settles, hangs ~test22 034-telegram-notifier. One run reached summary (14599 pass/65 fail/4 err/14704, 605s) then Bun v1.3.13 panicked in post-run shutdown (SEPARATE known upstream instability AFTER results print). Priority=P3: CI UNAFFECTED (completes green), impact is LOCAL full-suite verification only; UNRELATED to ALPHA-S2-FF (its targeted+regression tests 100% green). AC = clearInterval+.unref in teardown; 2 consecutive clean full-suite runs exit 0; no timer flood.
- (2) SUB6 decision = **prioritized@P3** (NOT dropped). ALPHA-S2-FF-SUB6-BUCKETING-HELPER (XS DRY refactor: extract bucketBy5Min<T>(rows,tsField), refactor both intraday5mCompactorJob.ts + intradayForeignFlow5mCompactorJob.ts). Both target files now exist → the 5-min bucketing logic is genuinely duplicated → real drift risk = tracked debt worth keeping (CLAUDE.md "reduce debt"). In-place edit: priority P2→P3, optional/stretch=false, po_decision stamped, status stays BACKLOG. P3 not P2 — pure architecture/DRY tier, zero functional impact, below the reliability→coverage→UX ladder.
- WHAT CONSIDERED: SUB6 prioritize-vs-drop — dropping loses a legitimate low-cost debt-reduction row now that both compactors are live; keeping at P3 costs nothing and prevents future silent bucketing drift → prioritize. Mint priority P0/P1 rejected — local-only test-infra with green CI is not reliability-critical.
- WRITE: one atomic `jq -f <scratchpad>/alpha-s2-ff-followup-triage.jq | bash scripts/orch-apply.sh` (Stage0+1 Zod PASS; conservation task_total live=575 candidate=576 net +1 [SUB6 = in-place edit, no count change]; CAS clean). Dry-run pre-checks: JSON-valid, backlog objects 395→396, new row present ×1, SUB6 P3, total 576. `.head` left idle/active_task_id:null; ALL 6 ALPHA-S2-FF done_verified rows untouched. NO code dispatched. NEXT = dev-team tick drains both from backlog when WIP frees.

## ci_red CI-RED-571818c2 — FLAKY-DISMISSED (no code-FIX) + SPIKE-item-#1 gap mint — 2026-07-16T02:01Z [task_id: CI-RED-571818c2]
- CONTEXT: dev-team dispatcher tick 2026-07-16T01:37Z routed EXACTLY ONE pendingSignal = ci_red from ci-health-probe (docs/signals/ci-red-571818c2-20260716014951.json, UNTRACKED, written post-drain). Router pre-gathered RAW; PO to disposition + prevent re-drain. Head idle, WIP=0, backlog 402, review 25, signal_queue 0.
- RAW (router-supplied, PO accepted as decisive — not re-run): CI run 29464553988 @ head 571818c2d (== origin/main, non-stale) = 14498 pass / 51 skip / 1 fail; sole FAILED FILE = apps/mcp-server/src/__tests__/1299b-skill-gated-bootstrap.test.ts. Delta since last GREEN 25cb69031 = 4 commits, ALL docs/chore/orch-state/notebooks, ZERO product code. Local isolated repro = 9 pass / 0 fail 549ms — does NOT reproduce.
- DECISION = FLAKY / CI-runner nondeterminism, NOT a regression. NO code-FIX minted (no product-code delta; nothing to fix). CI-RED-FIX task deliberately NOT created.
- RE-DRAIN PREVENTION (ci-red-close-must-record-fingerprint rule): (a) INSERT OR IGNORE fingerprint e62ede572b03... into signals_processed (from_agent=ci-health-probe, result=flaky-dismissed, source_filename=ci-red-571818c2-20260716014951.json, row id 2837, 186->187) + PRAGMA wal_checkpoint(TRUNCATE); (b) plain mv the UNTRACKED signal -> docs/signals/processed/ (drain glob is non-recursive => definitive re-drain prevention).
- REPEAT-FLAKER = YES. docs/spikes/SPIKE_CI-PERFILE-ISOLATION-FLAKE.md already diagnosed 1299b (reproduced 11/24 under CPU contention; 6/9 in worst run) as SPIKE item #1: blocking execSync('git rev-parse --show-toplevel') in agentBootstrap.ts:358 eager buildToolNameMap() singleton -> per-test timeout under CPU starvation -> permanently TDZ-poisoned ES module ('Cannot access toolNameMap before initialization') cascading to the rest of the file. Also a real production cold-start risk (server.ts:40 boot path; ~30-file transitive-import blast radius = the 'rotating failing file' effect).
- GAP FOUND + MINTED: the 07-10 triage (po-triage-20260710T0650Z) minted SPIKE items #2/#3/#4 (FIX-CYCLEJOB-1294-FOLLOWUP-SWEEP, FIX-RAG-TEMPORAL-DECAY-TEST-JITTER, CI-PERFILE-STRUCTURAL-MITIGATION) but DROPPED item #1. Minted `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART` (FIX/M/apps/mcp-server/, BACKLOG, plan-only). Dup-guarded board-wide first (no AGENTBOOTSTRAP/EXECSYNC/PROJECTROOT/COLDSTART row existed). WHAT-CONSIDERED: router suggested P3 grooming; SPIKE rated this HIGH (production boot path + recurring per-tick triage cost). PO set P2 — one notch above router's P3, still plain status=BACKLOG so BOUNDED-1 dispatches by priority (no this-tick execute). P0/P1 rejected: this is a recurring DETECTION of an un-fixed known bug (no failed fix), CI still completes green, not a reliability emergency.
- STANDARD INPUTS (read_telegram_reports status=new + list_unresolved_reports): large recurring BCTC/OHLCV cluster — [BCTC-1345b] low-confidence conviction-skips, [bctcExtractReconcile] RECONCILE EXHAUSTED wave across ~20 Q3/Q4-2025 tickers (terminal enrich_failed), [BCTC-ZERO-URL-ALERT] 0 URLs all tickers x2 cycles, [OHLCV-BACKFILL] crash. ALL covered by existing backlog (28 BCTC rows; FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE explicitly documents these re-surface as status=new every tick because no ack tool exists -> re-triage risks dup mint). NO mint. ZERO-URL-ALERT + OHLCV-backfill crash = ops/VPS infra, out of dev-dispatcher scope -> surfaced to router, not minted as dev rows.
- WRITE: sqlite direct (signals.db, INSERT+checkpoint) + one `jq --slurpfile row ... --arg now ... | bash scripts/orch-apply.sh` (Stage0+1 Zod PASS, conservation task_total 540->541 +1, CAS clean). `.head` untouched (idle both top-level + task_board.head, active_task_id=null, next_agent=router). NO code dispatched. RETURN to router = NOTHING (flaky dismissal + fingerprint-record is terminal).

## CHEF-mutex recurring FIX + cycle-snapshot sequencing-guard — dev-team triage 06:37Z — 2026-07-16T07:08Z [task_id: FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT]
- CONTEXT: dev-team dispatcher tick 06:37Z drained 4 pendingSignals (2 cowork diagnosis, 1 bug-escalation HIGH, 4x cowork-fire telemetry) + I ran read_telegram_reports(new)/list_unresolved_reports + TNB c110 (mandatory). Head idle, WIP 0, backlog 403→406.
- DECISION cow-20260716T052700 → MINT `FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT` (P1, cross-service/) + LAUNCH-CANDIDATE. RECURRING 2nd occurrence (07-14 + 07-16T05:15Z), outward MARKET double-publish path → recurring-bug-escalation mandates a FIX row THIS triage. RAW-confirmed pressure-cadence.md Step 4.5c: `echo "$SCHEDULE"|jq` (L140-141) + `echo "$MATCHES"|jq --argjson` (L144-145,149); zsh echo expands escaped \n in cowork-schedule.json chef-intraday.trigger_prompt → jq control-char parse error → G_ARR/NG_ARR empty → CHEF_MUTEX_APPLIED=false → chef-morning+chef-intraday both spawn. Prior lesson feedback_cowork_chef_mutex_echo_jq_mangles_escaped_newline EXISTS yet the doc still carries the bug (fix never landed or regressed). Board-wide dup scan: no row covers this vector. WHAT-CONSIDERED: fold into UC-CCA-P3 rejected (marker-lifecycle mechanism, upstream-of-publish is different); defer rejected (recurring + user-facing). Fix = file-direct jq + `jq -n --argjson` bound params, extract to tested scripts/ helper.
- DECISION cow-20260716T043200 → PLAN-ONLY, NO new row (handoff explicitly says none) — annotated SEQUENCING GUARD onto SPIKE-TICK-SNAPSHOT-DEADCODE-OR-REGRESSED.note (the actual pickup surface, zone docs/agents/cowork-team/flow/). Coupling is 3-way+writer per handoff §4-NEW-2/§4-NEW-3 (residue-prune → adaptive fleet-wide → 240min dangling gate → alert-commander-market 16x degrade; + calendar_status latched 'unknown' needs wiring to vnTradingCalendar; + regime_status writer dropped 06-05). WHAT-CONSIDERED: mint fresh coupling row rejected — UC-SDF-P2 + FIX-COWORK-CADENCE-DANGLING-POLICY-ID (I minted 03:37Z, backlog idx 402) already exist + handoff is SSOT; competing row = double-triage churn.
- DECISION signal-1 (alert-commander no-Bash, HIGH) → NO mint, FOLD. Self-heals via peer notebook commits (e055f2be2 '5 uncommitted cycles', 1451a619b); same class as F-MCP-SUBAGENT-SYSTEMIC → folds to existing FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK (P1) with a refinement (gateway-present/Bash-absent variant, NOT gateway-blind). WHAT-CONSIDERED: grant Bash to cowork subagent rejected (agent-tool-profile = agent-father territory + host-starvation risk per project_overparallel/host_memory_panic); peer-pickup is a working compensating control.
- DECISION TNB c110 → MINT `FIX-VNINDEX-ESTIMATE-IMPLAUSIBLE-DELTA-GATE` (P2, apps/macro-indicators/, via BA spec) for F-VNINDEX (MED-HIGH: tier-4 fallback emits wrong-but-non-null vnIndex delta -526pp cited to MARKET, no plausibility gate). Narrowed FIX-CHEF-EVENING-DUP-DATE-MISLABEL to component-1 (component-2 double-publish confirmed+folded to UC-CCA-P3). Hygiene: MINT `CLEAN-COWORK-DISPATCHER-TELEMETRY-DRAIN-DIR` (low). BCTC/OHLCV telegram flood = KNOWN (ENRICHER-OLD-QUARTERS/HIST-VPS-BACKFILL/QUEUE-MAXAGE-GATE/1345B-REPORT-BATCH) → NO mint; recommend router prioritize QUEUE-MAXAGE-GATE (stops live alert-flood).
- WRITE: one atomic `jq --argjson newrows --arg ts --rawfile spikenote --rawfile chefappend ... | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS, conservation live=541 candidate=544 +3, CAS clean) — 3 backlog mints + 2 annotations, id-dedup guarded. `.head` untouched (idle, WIP 0). RETURN to router = BATCH([FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT]); BOUNDED-1 heavily-dirty peer tree → dispatcher gates actual launch.

## ULTRACODE rank-1 null-next_agent BAND groom — autolaunch_safety_hold adjudication — 2026-07-16T10:56Z [task_id: UC-ASL-P6-band-route]
- CONTEXT: dev-team tick 10:07Z BOUNDED-1 picked `UC-ASL-P6` (P1/CLEAN/M/cross-service, TRIPLE-NULL: board owner=null + next_agent=null + no backlog-detail entry) and WITHHELD auto-launch (orch-state untouched) → routed signal `autolaunch_safety_hold` (`docs/signals/processed/dev-team-20260716T102429Z-autolaunch-safety-hold-uc-asl-p6.json`) to PO. Its work = purge phantom `docs/handoffs/DASHBOARD.md` protocol refs from the system-auditor flow docs + align signal-dashboard SKILL hot-path write text to the orch-apply.sh CAS contract = agent-flow-doc + SKILL editing (agent-father factory rule), NOT generic-developer. Zone-detect Tier-3 would MISROUTE the un-annotated row to the `developer` placeholder. Class NOT isolated — the whole ULTRACODE rank-1 (P1) band re-surfaces the same question each idle tick. Head idle, WIP 0, backlog 405.
- DECISION = groom the WHOLE rank-1 (P1) band routing in ONE pass (signal recommendation (b); memory meta-lesson: churn>2 ticks → groom the band, not the surfaced row). Rejected (a) single-row assign (band re-churns), (c) bless developer (mis-owner: flow-doc+SKILL is agent-father), (d) supervise (blocks legit BOUNDED-1 advance). Rank-2 (P2) rows left untouched — not surfacing before P1 clears; grooming 15+ unverified rows = over-reach.
- ROUTING (verified each by RAW file-location, not row prose): `UC-ASL-P6`→**agent-father** (targets `docs/agents/system-auditor/flow/{main,tier1-probe}.md` + `.claude/skills/signal-dashboard/SKILL.md` = flow-doc+SKILL, dispatch SKILL L47). `UC-SDF-P4`→**developer** (`scripts/agents-flow/drain-signals.js` + purge script = genuine scripts, cross-service by-design route main.md L30). `UC-GCP-P2`→**developer** (git untrack + `.gitignore` = genuine root). `UC-GCP-P4`→**developer** (`scripts/git-hooks/pre-push` = genuine hooks/scripts). `UC-MDH-P1`→**dev-mcp-server** (`apps/mcp-server/` test sandbox, valid dev-svc agent). KEPT as-is: UC-RDL-P4 (ba, apps/mcp-server SPRINT-M), UC-CCA-P3 (ba, P0), UC-CRITIC-HOOKS-ENFORCEMENT + UC-CRITIC-GATEWAY-CONTRACT-DRIFT (ba-relay SPIKEs). Owner left null on all (matches existing backlog convention — owner set at claim; next_agent is the load-bearing routing field the gates read).
- WRITE: one atomic `jq '({map} as $route | .task_board.backlog |= map(if $route[.id]!=null then .next_agent=$route[.id] else . end))' | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS, conservation task_total 541↔541 UNCHANGED [pure annotation, no lane-move/mint], CAS clean). Dry-run pre-verify to scratchpad: EXACTLY 5 rows changed next_agent, backlog 405↔405, top-level keys outside backlog byte-identical. `.head` untouched (idle, WIP 0 — no status-flip/lane-move so no head sync needed). Post-check: 0 P1 ULTRACODE rows remain null-next_agent. NO code dispatched. RETURN = BATCH (band groomed; BOUNDED-1 now auto-advances each row to its declared owner).

## ci_red CI-RED-b51fbe13 — REAL regression (NOT flaky), MINT FIX — 2026-07-16T20:03Z [task_id: CI-RED-b51fbe13-FIX]
- CONTEXT: dev-team dispatcher tick routed ONE actionable pendingSignal = ci_red (ci-health-probe, `docs/signals/ci-red-b51fbe13-20260716195426.json`) + 1 informational cowork_tick_fire (to system-auditor, note+ignore). Router attribution GUESSED "likely the flaky bun-test class" — PO verified independently, attribution WRONG. Head idle, WIP 0, backlog 402→403, review 29 (stranded legacy lane, untouched).
- VERIFY (PO ran, did NOT trust router): `gh run view --job 87724682260 --log-failed` → sole FAILED FILE = `apps/mcp-server/src/__tests__/BSD3-brief-sector-drift.test.ts` (1 fail / 14452 pass). Local repro `bun test BSD3-brief-sector-drift.test.ts` = DETERMINISTIC fail (throws on BSR.md). NOT the router's named flaky class (vps_push_log/insider-tx/OCR-cache/foreign-flow), and NOT in b51fbe130's changed files — the attribution premise was false.
- ROOT CAUSE (code-grounded): the BSD3 guard forbids any `**Sector**:` line in `docs/analysis-briefs/*.md` (sector SSOT = WATCHLIST_SEED domain field, never the brief). 3 briefs added in commit ebbfda6a5 (report-analyzer, "refresh briefs + add BSR DBC VIX") — BSR.md, VIX.md, DBC.md — each carry a legacy `**Sector**:` line. All other briefs comply; canonical template `docs/references/analysis-ledger-template.md` has none. NO code generator emits the line (grep-confirmed across apps/scripts/agents/skills) → line-removal is DEFINITIVE, not a symptom patch; the BSD3 guard test IS the structural recurrence-prevention.
- DECISION = MINT `CI-RED-b51fbe13-FIX` (FIX / cross-service/ / status BACKLOG / verification_gate=ci_green_on_subsequent_push / fingerprint 8a0a0237… recorded in status_note per ci-red-close-must-record-fingerprint). Two-layer dedup PASS: no open row w/ check_id in title/id; no IN_PROGRESS/REVIEW FIX carrying head_sha (the 2 CI-RED rows in REVIEW are unrelated — FXFLOOR-OVERCLAMP + FOREIGN-FLOW-BULLETIN). NOT dismissed-as-flaky (unlike CI-RED-571818c2 02:01Z) precisely because repro is deterministic + product-adjacent (briefs consumed by analysisBriefReader).
- CARRYOVER debt candidates = NO mint (both ALREADY backlogged, grep-confirmed → re-mint = dupe): cowork-team-*.json telemetry (45 files in docs/signals/) → `CLEAN-COWORK-DISPATCHER-TELEMETRY-DRAIN-DIR`; price_anomaly envelope → `FIX-PRICE-ANOMALY-DISH-SIGNAL-ENVELOPE`. Telegram/list_unresolved (118, all normal-pri analysis-agent BCTC OCR/reconcile flood) = KNOWN cluster, ops/analysis scope → NO mint.
- WRITE: one atomic `jq --slurpfile row … '.task_board.backlog += $row' | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS; conservation task_total 541→542 +1; CAS clean; backlog 402→403). First apply rejected (status `TODO` not allowed in backlog lane → relabeled `BACKLOG`; triage-signals.md schema predates the lane-coherence Zod gate). `.head` untouched (idle, WIP 0). Did NOT touch/commit live peer CHEF files (unified-agent.md, cowork-schedule.json, synthesis json). NO push (main is RED + live peer writes present). RETURN = BATCH([CI-RED-b51fbe13-FIX]); dispatcher Step 2 FIX→Step 3 direct execution.

## report-triage — BCTC reconcile flood ROOT-CAUSED + archived; 1 PLAN-ONLY SPIKE — 2026-07-16T21:00Z [task_id: SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD]
- CONTEXT: router report-triage (dev-team Step 4.1.2 surfaced 20 re-surfacing analysis-agent reports, status=new, accrued 07-15). Prior-art from router honored (mock-guard Go FP / 1345b batch / ACK-resurface / OHLCV / enricher = covered, NOT re-minted). Prior ticks (07-10, 20:03Z) repeatedly logged this BCTC OCR/reconcile flood as "known cluster, no mint" — this tick CONVERGED (root-caused + cleared + minted the net-new gap).
- THE ONE DECISION (12-ticker Q4-2025 RECONCILE-EXHAUSTED cluster) = CHRONIC (systemic extraction dormancy), NOT a new acute source/VPS/regression incident. Discriminator = OTHER-PLANE RAW probe of live market.db (docker exec, readonly): all 12 (DBC/DXG/FRT/GEX/KDC/KDH/MSN/PDR/SAB/VIX/VJC/VND) present in financial_reports as Q4-2025 with text_status=COMPLETE + pdf_path SET (⇒ discovery/fetch/OCR-text HEALTHY ⇒ source-outage/VPS-down/DNS DEFINITIVELY RULED OUT) but LAYOUT=0/TABLE_ROWS=0/refine_status=PENDING, no queue row. The "12 one night" = leading edge; bctcExtractReconcileJob walked the WHOLE watchlist backlog backwards Q4→Q3→Q2→Q1-2025 overnight (07-15 20:05Z→07-16 07:15Z) mass-terminalizing all as enrich_failed → 76-report storm (bounded+complete). GLOBAL freshness frozen: bctc_layout_units MAX=2026-06-10, bctc_table_rows MAX=2026-06-30 — OLDER than SPIKE-BCTC-TABLEROWS-FROZEN-HOLLOW-DONE's 07-12 readings ⇒ implies DATA-LOSS/ROLLBACK 07-12..07-16.
- ARCHIVE: all 121 overnight reports 3358-3478 via process_telegram_report(resolution=duplicate, delete=true). ACK-path CONFIRMED working (code-read: markProcessed changes status; verified poll 0 new / 121 processed) — the FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE concern is about lacking a THIRD "acknowledged/tracked" state, NOT a broken resolve path; for DUPLICATES the tool is correct. Bounded one-time clear (flood complete since 07:15Z), not churn-cataloguing.
- MINT `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` (SPIKE/high/plan_only/zone=multi/owner=dev-mcp-server/180min). NET-NEW vs prior-art (dup-scanned board-wide): (AC-1 ops) verify 07-12..07-16 DB rollback/VM-rebuild/volume-swap; (AC-2) why extraction dormant since mid-June despite FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION DONE_VERIFIED 07-10; (AC-3) report-storm circuit-breaker → mint follow-up FIX. WHAT-CONSIDERED: fold into FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP (that's only the refine/table_rows half — misses layout-freeze + rollback + storm) or FIX-BCTC-D3C-FOLLOW-UP-RESET-ATTEMPTS (only per-row recycle counter) → both REJECTED as partial. NOT a recurring-FAILED-fix escalation-block: the covering fixes are BACKLOG (never shipped) so it's recurring DETECTION, not a fix that failed.
- WRITE: one atomic `jq --slurpfile row '.task_board.backlog += $row[0]' | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS; conservation 542→543 +1; CAS clean). `.head` untouched (idle, WIP 0 — triage-only, no lane-move). NO push (router owns push this tick; PO did not touch git). RETURN to router = triage summary + verdict (no BATCH — SPIKE is BACKLOG PLAN-ONLY, not dispatched inline; AC-1 to ops first).

## decompose AC-3 from the storm SPIKE — CLOSE the execution gap (directed-but-never-minted) — 2026-07-16T22:37Z [task_id: FIX-BCTC-RECONCILE-EMISSION-CIRCUIT-BREAKER]
- CONTEXT: router RAW-verified this tick that my 22:07Z "fast-track AC-3" directive was NEVER minted as an actionable row — I only escalated the SPIKE priority + wrote a `converge_note`. Board grep confirmed only the plan_only SPIKE existed. Consequence = a plan_only SPIKE is not dev-pickable and BOUNDED-1 cannot promote a nonexistent row, so the storm (5 reconcile-exhausted dups / 3 ticks; 2 this tick — 3483 DGC + 3484 DIG already archived by router) had NO execution path and was accelerating: perpetual per-tick archival with no shippable fix = the churn-without-convergence. META-LESSON (recorded): a "direct/escalate/fast-track" verdict is NOT a deliverable — a decomposition directive MUST land as a real actionable row in the SAME tick, else it evaporates into a note and the churn continues.
- PRIOR-ART FIRST (mandated): board-wide id+title grep (AC-3/circuit/emission/reconcile/dormant) + handoffs scan. All matches were false-positives: FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION (agent_signals dedup, different producer, explicit do-not-remint), FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T (stored-PDF ingest stall, earlier layer), FIX-POSTCYCLE-STEP45-NB-WRITE-AC3 (unrelated SPIKE's notebook-writepath AC-3), TASK_1404-cb-fix.md (foreign-flow CB). ZERO rows derived-from the SPIKE. => genuine gap, MINT.
- DECISION = MINT `FIX-BCTC-RECONCILE-EMISSION-CIRCUIT-BREAKER` (FIX/READY/P0/mcp-server-scheduler/dev-mcp-server/plan_only:false/size S). Scope = ONE file `apps/mcp-server/src/scheduler/financial-reports/bctcExtractReconcileJob.ts`; RAW-located the exact emission site (the `if (nextAttempt >= MAX_RECONCILE_ATTEMPTS)` terminal block ~L388-424 fires `effectiveSendBug` once per exhausted ticker*quarter inside the per-row loop). AC = run-level dormancy predicate (>=K exhausted rows default 3, OR MAX(bctc_layout_units.extracted_at) stale > N days default 2) → emit exactly ONE run-summary BUG; below threshold keep per-row fail-loud (no regression); enrich_failed transition still commits (no data-integrity change); unit test asserts sendBug call-count==1 on M>=K. WHAT-CONSIDERED: (a) BACKLOG rejected — emission site precisely located + AC single-file/self-contained + P0 churn-stopper => fully ready-to-work, put directly in dev's path (READY chosen); (b) fold into the SPIKE (leave plan_only) rejected — that is exactly the gap that let churn run; AC-3 is independently shippable and does NOT depend on AC-1 infra-verify / AC-2 diagnosis per the SPIKE's own converge_note; (c) zone=multi (SPIKE's) rejected — the FIX touches one scheduler file, so precise zone `mcp-server-scheduler` (in-use, cf FACTORY-SCHEDULER-split-bctcReparseJob). Owner=dev-mcp-server + depends=[] → clears BOUNDED-1 withhold predicates (dev-owner, not depends-blind, status-lane coherent).
- LINK: FIX `parent`+`derived_from` = SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD; reciprocally added `ac3_followup`=FIX id + `ac3_followup_by` onto the SPIKE row so AC-3's satisfaction is visible on the SPIKE face. AC-1/AC-2 remain open on the SPIKE (still plan_only, unchanged).
- WRITE: one atomic `jq --slurpfile nr '.task_board.ready += $nr | (SPIKE row |= + {ac3_followup,...})' | bash scripts/orch-apply.sh` (Zod Stage0+1 PASS; conservation task_total 542→543 +1 exactly; CAS clean). Post `orch-state-validate.sh` exit 0; ready 0→1, backlog 403 unchanged, review 29 unchanged; `.head` untouched (idle→router, no lane-move on the head-tracked WIP). NO commit/push (router sweeps po.md + po-decisions.md). RETURN to router = BATCH([FIX-BCTC-RECONCILE-EMISSION-CIRCUIT-BREAKER]); storm now ON a convergence path (a READY/P0 dev-actionable FIX exists for BOUNDED-1/dev pickup).

## CLUSTER-G *-FIX cohort BATCH convergence + lost-mint recovery + signal triage — dev-team tick 00:07Z — 2026-07-17T00:39Z [task_id: SEC-FUNC-01-FIX]
- CONTEXT: router BOUNDED-1 auto-picked SEC-FUNC-01-FIX into in_progress (head=in_progress/next_agent=developer) and WITHHELD the dev spawn — confirmed no-op. Proportionality now MET: SEC-FUNC-01-FIX is the 2nd BOUNDED-1 pick from the CLUSTER-G cohort (FR-FUNC-02-FIX was 1st, dispositioned 07-13) → churn corroborated ≥2 → batch convergence is proportionate (my 07-13 note deferred it as "not on a first-pick tick"). RAW across 4 planes: (1) LIVE get_sector_rotation this tick = 17 sectors ("Tổng số ngành phân tích: 17"), non-empty ≥1/sector → SEC-FUNC-01 acceptance PASSES; (2) sprint-QUALITY-BURNDOWN-phase4-verdict.md L84 SEC-FUNC-01→PASS (+ all cohort rows PASS/INFO L41-107); (3) my 07-13 po-decisions note; (4) no board row depends_on any cohort id (safe to close).
- DECISION A = BATCH-converge 16 live cohort rows → done_verified (status DONE_VERIFIED, no code), matching the FR-FUNC-02-FIX precedent (same cohort, same reasoning). Moved: SEC-FUNC-01-FIX (from in_progress) + 15 backlog (ALT-FUNC-03/ALT-PERF-01/ANA-TEST-01/CI-FRESH-01/CO-OBS-01/FW-FRESH-01/FW-OBS-03/KD-FRESH-01/MD-FRESH-01/02/03/NEWS-OBS-01/PA-CONSIST-01/SEC-FUNC-03/SEC-TEST-01)-FIX. FR-FUNC-02-FIX + FR-DEGRADE-01-FIX already gone (converged). WHAT-CONSIDERED: keep-in-backlog + supervised/plan_only gate REJECTED — leaves clutter + relies on promote-gate honoring; closing removes them from backlog entirely = strongest anti-churn (BOUNDED-1 cannot re-pick). CANCELLED vs DONE_VERIFIED — chose DONE_VERIFIED for cohort-wide consistency with FR-FUNC-02-FIX. Each row stamped closed_at/closed_by/completed_at/verify_note (per-row provenance; SEC-FUNC-01 carries the 17-sector live-probe evidence).
- DECISION B = RECOVER LOST MINT. While deduping WORKSTREAM-2 bctc signals I found the SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD row STILL carries ac3_followup="FIX-BCTC-RECONCILE-EMISSION-CIRCUIT-BREAKER" but that FIX row is ABSENT from every lane (ready empty) = dangling FK. Root: the 22:37Z mint (journal ea9872474) committed ONLY po.md+po-decisions.md — the orch-state ready-lane write lived in an uncommitted working tree that a later peer op reverted (documented "peer-push sweeps held data commits" hazard). Re-applied the FIX row to ready (status READY, P0, mcp-server-scheduler, owner/next_agent dev-mcp-server, depends=[]) faithfully from the committed spec — heals the dangling FK AND restores the storm's ONLY board convergence vehicle (the 3 bctc signals + 2 DBC/MSN reports this tick corroborate an actively-flooding storm). Idempotency-guarded (aborts if id already on board); CAS clean. NOT a new decision — mechanical restore of a committed one.
- DECISION C (systemic) = NO promote-time already-PASS guard row minted (task's optional). Over-engineering: acceptance criteria aren't generically machine-executable, and the specific churn source (mis-minted *-FIX parallels of phase-4-reclassified checks that were never reconciled) is now fully converged. Recorded here per the task's "else" branch.
- WORKSTREAM-2 signals: bctc_signal_{FPT,HPG,VCB}_20260717_routine (HPG data_quality=unreliable_extraction_defect esc4-skipped; FPT/VCB esc3 DATA-COVERAGE-LIMITED under guard-held locks) → dedup vs SPIKE (P0), NO mint. cowork-team 00:06 tick-fire heartbeat → noted, non-actionable, discard. read_telegram_reports(new) = 2 reports (3491 DBC 2024-Q1, 3492 MSN 2024-Q1 RECONCILE EXHAUSTED enrich_failed) → both dedup of the SPIKE → process_telegram_report resolution=monitoring (processed+telegram-deleted); new-set now empty.
- WRITE: TWO atomic orch-apply.sh writes (both fixture-validated Zod Stage0+1 PASS first). (1) CLUSTER-G: in_progress 1→0, backlog 402→387, done_verified 0→16, conservation task_total 542↔542 CONSERVED (pure lane moves); head reset idle (top-level authoritative + task_board.head mirror, next_agent=router, no next_action). (2) FIX-BCTC restore: ready 0→1, conservation 542→543 (+1 growth). Final: head idle/router, in_progress 0, ready 1, backlog 387, done_verified 16, review 29. PO owns the commit (scoped orch-state.json + po-decisions.md).
