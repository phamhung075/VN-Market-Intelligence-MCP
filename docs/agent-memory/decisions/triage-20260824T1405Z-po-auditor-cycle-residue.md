### STEP po-R14 · po · 2026-08-24T14:05Z
**task-id:** FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL
**what-done:** Ruled the tier-2 auditor's "CRITICAL cron fire-gaps" cluster (vpsProxyWatchdog / priceUpdateWatchdog / vnIndexRefresh / alertScanParallel / taAlertNotifier, all ~5h overdue) to be a schedule-blind FALSE CRITICAL owned by this row. Full measurement written to this row's cold detail entry (`docs/data/orch/archive/backlog-detail.json#FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL`), `detail_ref` pointer set in hot. Minted nothing for it.
**what-considered:**
- Accepting the auditor's CRITICAL at face value and minting a cron-death row — REJECTED on measurement. In `cron_job_runs` (live `market.db` via `docker exec` into the mcp-server container, NOT the host `data/` copy) all four resolvable jobs completed their FULL normal daily run count on 2026-08-24: vnIndexRefreshJob 84, vpsProxyWatchdogJob 42, alertScanParallelJob 28, taAlertNotifierJob 28 — identical to 2026-08-12/13/14. Nothing was missed.
- Accepting the escalation's own hypothesis that this is ONE host-suspension/outage window (five crons agreeing to within 0.1h) and folding it onto `FIX-A29-CRON-GAP-NO-OUTAGE-WINDOW-DISCRIMINATOR` as corroboration — REJECTED, and this is the substantive correction of this tick. An outage predicts a TRUNCATED daily complement. The complement is complete and byte-identical to three prior full sessions, and 2026-08-24 is a normal Monday (2098 fleet cron runs, 64 distinct jobs). There was no outage today.
- Treating the 5th name as a dead cron — REJECTED: `priceUpdateWatchdog` has ZERO rows under any `%priceUpdate%` match, i.e. the A-29b unresolved-join class already owned by `FIX-CRONSTATUS-STATICMAP-MISSING-9-PAIRS` (backlog[436]).
- Recording the ruling inline on the owning row — REJECTED mechanically: the row is 15,638 prose bytes, over `ORCH_ROW_PROSE_CEILING_BYTES=12000`; `orch-apply.sh` aborted the first write attempt naming `detail_ref` as the sanctioned destination. Re-routed to cold, hot touched with structural fields only (`detail_ref`/`updated_at`/`updated_by` are all in `STRUCTURAL_FIELDS`), so the write lands at zero prose growth.
**why-decision:** These five are market-hours-scoped jobs whose designed daily envelope is 02:00–08:55 UTC (09:00–15:55 ICT, the VN session). Between session close and next open they are ALWAYS 5h–17h "overdue" against a raw age-vs-cadence ladder. The condition is the designed daily state and it recurs every single day — so this is a permanent daily false-CRITICAL generator, not the weekend-only case the row currently documents. That is a widening of an existing row, not a new defect.
**why-change:** The escalation asked me to record this as outage corroboration on the discriminator row. I recorded the opposite finding there instead, because a two-class discriminator (outage vs dead cron) would still misclassify this cluster. Added a third class and, more usefully, the separator that actually worked: a completed-complement test (did each job finish its normal daily run COUNT before going quiet), not an age test. Age is identical for an outage and for a designed stand-down, which is exactly why A-29 cannot tell them apart today.

### STEP po-R15 · po · 2026-08-24T14:05Z
**task-id:** FIX-AUDIT-OUTPUT-CONTRACT-V1-NULL-AUDITCYCLETAG-JOIN-ALWAYS-ZERO
**what-done:** Minted the missing V1 row (P1, developer, `cross-service/`). V1 was the only output-contract check with no board row; V3 (backlog[307]) and V4/V5 (backlog[300]) both had one.
**what-considered:**
- Filing it as a lost-write / data-loss row, which is what V1's own message ("signal_queue_rows_written mismatch") suggests — REJECTED. The row is present AND committed; `sys-20260824T135127-56bd` carries `audit_cycle_tag: null`, and `audit-output-contract.sh:549-550` computes `independent_sqr` as `[.signal_queue.rows[] | select(.audit_cycle_tag == $tag)] | length`. `null == "<tag>"` is false, so the join returns 0 by construction. Null-field defect, not a lost write — and V1's wording actively misdirects.
- Asserting a root cause for the null tag — REFUSED deliberately. The obvious hypothesis (provenance predicts nullness) was tested and REJECTED: `provenance="detector"` is 28 rows of which only 5 are null (23 tagged), and the 3 rows with no provenance field are all null. The row is scoped to the observable and instructs the specialist to find the emit sites; AC-5 says so explicitly.
- Folding the violations-file duplication into a separate row — REJECTED as the same defect surface: `_record_violation_durable` (L333-342) appends `{ts,cycle_tag,from,detail}` unconditionally with `.[-200:]` and no dedup key. Carried as `secondary_defect` with AC-3/AC-4 on the same row.
**why-decision:** V1 has fired 12x since 2026-08-08 in three mutually contradictory directions (narrated=1/independent=0, =1/=2, =4/=2, =0/=2). A check that reports all four shapes for one condition is not measuring what its message claims, and it is the check most likely to be read as evidence of data loss.
**why-change:** Went beyond the brief to quantify the duplicate-emission side because it corrupts the evidence base itself: the 5-in-80-seconds V1 cluster on 2026-08-23T21:3xZ and V3 firing twice per cycle (04:12:18/04:12:33 and 13:52:46/13:53:28) mean every violation-frequency count read off that file is inflated. Anyone triaging from it would over-weight V1 and V3.

### STEP po-R16 · po · 2026-08-24T14:05Z
**task-id:** FIX-BROAD-GITADD-LEAVES-STAGED-RESIDUE-DESPITE-CORRECT-COMMIT-PATHSPEC
**what-done:** Minted the commit-discipline row (P1, developer). Also minted `CLEAN-GITIGNORE-MISSING-TICKVERDICT-REFINEPROBE-EPHEMERAL-PATTERNS` (P3) for the 38 untracked-but-not-ignored `docs/data/.tick-verdict-*` / `.refine-probe-*` scratch files.
**what-considered:**
- Treating this as already covered by the sweep-guard work — REJECTED after resolving each candidate by jq path. `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-HOOK` (done_verified[37]) and the hook itself fire only on a BARE commit, so a pathspec-scoped commit never trips them and the residue is left silently. `FIX-AUDITOR-COMMIT-NONEXPLICIT-PATHSPEC` (archive[13]) is CANCELLED and is the inverse shape (peers swept IN). `FIX-COMMIT-SWEEP-VICTIM-SELF-DETECT` (backlog[196]) is victim-side. `FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS` (ready[63], P0) is guard-side. The staging act itself is uncovered.
- Accepting the stated sweep-guard state of "2 warns against threshold 3" — CORRECTED at source. `.git/sweep-guard.log` has SIX warns for this coordination session since the baseline in `.git/sweep-guard.escalation-baseline` (2026-07-31T04:04:07Z), against `escalate_threshold` default 3 (`pre-commit:808`). The session is already PAST escalation: the next bare commit gets ESCALATED REJECT (`pre-commit:887`), not a warning. Recorded on the row as `correction_to_intake`.
**why-decision:** A correct explicit commit pathspec protects only its own commit; it does not unstage what a preceding broad `git add` put in the index. Nothing in the fleet catches that, because every existing control keys on bare-commit detection.
**why-change:** Added AC-4 asking for an explicit DECISION on whether the hook should also warn when a pathspec-scoped commit leaves a non-empty index — that is the only mechanical catch for this shape, and I did not want to mandate a hook change without the owner ruling on false-positive cost (legitimate multi-commit staging exists).

### STEP po-R17 · po · 2026-08-24T14:05Z
**task-id:** FIX-EMITSIGNAL-DEDUPKEY-GRAMMAR-UNVALIDATED-CALLER-FREETEXT-DEFEATS-7D-WINDOW
**what-done:** Recorded the `dedup_skipped=0` miss on this existing row's cold detail entry rather than minting a dedup row. Marked my own inbox row `sys-20260824T135127-56bd` triaged.
**what-considered:**
- Minting a new "auditor dedup_skipped under-reports" row — REJECTED, this row already owns the mechanism and the ledger proves it verbatim: ELEVEN distinct keys for the ONE A-29 fire-gap condition in THREE incompatible grammars (per-job bare, per-job `:A-29`-suffixed, and aggregate `:missed-crons`/`:stale-crons`).
- Attributing `dedup_skipped=0` to a broken dedup lookup — REJECTED: `morningBriefing` is present under TWO grammars simultaneously, so that one job provably cannot dedup against itself. The lookup is fine; the key is free-text, which is precisely this row's thesis.
**why-decision:** The escalation asked me to record on "whichever row already owns that behaviour" and not to mint without checking. Checked; this is the owner.
**why-change:** Also noted the aggregate buckets cannot dedup against ANY per-job key, so one cron gap is counted once per-job and once in aggregate — that is a second, independent multiplier on the same row that the existing write-up does not capture.
