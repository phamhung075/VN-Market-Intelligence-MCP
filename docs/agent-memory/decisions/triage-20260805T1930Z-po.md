# PO Triage Decision Journal — 2026-08-05T19:30Z (router hand-off, 3 dev-team-flagged items)

## STEP po-D1 — commit-sweep-guard occurrence 9, dedup-fold not new row

**task-id:** FIX-COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR-AND-NOTEBOOK-LONGTAIL
**what-done:** Appended occurrence 9 (escalated=true, prior_warns=9, actor f298ccf7) to the row; sent WORK telegram nudge.
**what-considered:**
- Mint a new FIX-COMMIT-SWEEP-GUARD-* row — rejected, dedup-checked against the 5-row family, this row already owns the exact defect (script-actuator pathspec migration) and is unshipped.
- Treat as benign since content landed intact — rejected per mandatory rule: attribution corruption is the tracked class, not a non-finding.
**why-decision:** Standing ruling `po_live_evidence_20260801T0158` already holds fresh occurrences are true-positive volume tracking, not new-row triggers; this occurrence's only new fact (first non-self-corrected sweep) strengthens the case to expedite the existing row, not fork it.
**why-change:** none — following established family disposition.

## STEP po-D2 — ci_red CI-RED-71969033 folded into causing row

**task-id:** FIX-MCP-MEMORY-CODE-LEAK
**what-done:** Ran mandatory pre-dedup failing-file read (gh run view --log-failed), traced size-lint FAIL to schema.ts, folded into the row the router had already flagged; stamped dedup_key for future auto-fold; drained the raw signal file via drain-signals.js.
**what-considered:**
- Mint `CI-RED-71969033-FIX` per the routing table's default template — rejected, would duplicate an already-tracked, already-next_agent=qa disposition on the row whose own commit caused the regression.
**why-decision:** File-scoped dedup found an exact match (same file, same job, same root commit); folding avoids double-tracking one regression under two ids.
**why-change:** none.

## STEP po-D3 — FIX-BCTC-FPT-BT5-BALANCE-GATE routed to qa + follow-up minted

**task-id:** FIX-BCTC-FPT-BT5-BALANCE-GATE
**what-done:** Flipped stale self-targeted next_agent (dev-team) to qa for merge-review; minted follow-up FIX-BCTC-FPT-PAGE8-EPS-BLEED-ROWCOUNT (backlog, P2) for the out-of-scope page-8 gap dev-pdf-extractor flagged but correctly did not fix inline.
**what-considered:**
- Close the row DONE directly — rejected, verification is QA's job not a self-report read.
- Leave the EPS-bleed gap as an unowned recommendation in the review_note — rejected, epic-tail-loss precedent (feedback_epic_wrapper_closeout_gap_no_auto_revisit).
**why-decision:** Root-cause fix is complete and well-evidenced (balance identity EXACT, 213/213 targeted tests, full non-regression clean); only remaining action is independent QA confirmation + not losing the flagged follow-up.
**why-change:** none.
