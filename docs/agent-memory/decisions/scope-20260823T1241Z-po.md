# Decision Journal — Scope 2026-08-23T12:41Z · po

**Scope request:** user — add quarter + ticker select dropdowns to `/dashboard/bctc-inspect`
**Agent:** po
**Started:** 2026-08-23T12:41:38Z

---

### STEP po-S1 · po · 2026-08-23T12:41:38Z
**task-id:** FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER
**what-done:** Minted one `backlog[]` row (SPRINT-S, P1, zone `apps/mcp-server/`, `next_agent=ba`) after probing the live page and the live `/docs` payload rather than trusting the request's stated stack.
**what-considered:**
- Route to `dev-frontend` as the request framed it (Remix + Tailwind + shadcn) — REJECTED. `apps/frontend/app/routes/dashboard.bctc-inspect.tsx` is a resource route returning a raw `Response`; it renders no JSX and only proxies `GET {MCP_SERVER_BASE_URL}/api/bctc-inspect`. The UI is `apps/mcp-server/src/interface/bctc-inspector.html`, 2692L of self-contained vanilla JS/CSS. A shadcn `<Select>` cannot be mounted into it. Wrong zone = wrong specialist = guaranteed bounce.
- Add a server-side filter param to `/api/bctc-inspect/docs` — REJECTED. `DocListItem` already carries `action_code` / `period_type` / `period_year` / `period_quarter`, and `loadDocList()` already fetches all 257 items in one call (112669B). A backend param would add an endpoint contract, a test surface and a second round-trip for a filter that is free client-side.
- Split the `period_quarter` type drift into its own row — REJECTED for the display half, ACCEPTED as a deferred follow-up for the write half. The 2 string-typed rows render as `HUT Q1 QQ1 2024` through `buildLabel()`; that is ~3 lines in the same file the developer is already in, and leaving it unfixed makes the new quarter facet emit 13 options instead of 11. The DB write path that stores a string is a genuinely separate root cause and is named OUT of scope in the row so it is not silently absorbed.
- Append a `.sprint_goal.entries[]` vision entry per `sprint-kickoff.md` step 3 — REJECTED. `entries=16` already breaches the documented cap of 15 (the 16th was admitted only as a user-originated P0 on a shipped surface). A two-dropdown facet filter is not vision-scale; deepening a breached governance cap buys nothing that the board row does not already carry.
**why-change:** The request's own premise was wrong about the implementation surface. Accepting it would have sent a Remix specialist to a file they do not own, in a language/stack the page does not use.
**evidence:** live probe `GET :3000/api/bctc-inspect/docs` 2026-08-23T12:38Z → HTTP 200, 257 docs / 50 tickers / 11 real quarters / 13 raw quarter combos; `period_quarter` type histogram `{number:255, string:2}`; `period_type` ∈ {Q1:81, Q2:65, Q3:33, Q4:78}, zero annual rows.
**risk-accepted:** ~80-100 net lines exceeds the nominal SPRINT-S 30L budget. Kept at S because the SPRINT-S→M escalation trigger is domain crossing, which is not met (single zone, 2 files). Architect may rescope.
