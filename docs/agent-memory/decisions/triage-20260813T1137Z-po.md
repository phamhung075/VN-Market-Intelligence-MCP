# PO Decision Journal — dev-team Step 1 Triage — 2026-08-13T11:37Z

Session (coordination param): `632721c2-41e4-4aff-8d06-a47cf80dc0d7`
Inputs: 147 durable-inbox envelopes · 22 unresolved telegram reports · 608 non-terminal board rows · TNB c127 handoff

---

## D-1 — `FIX-DEEPFETCH-PIPELINE-100PCT-UNFETCHED-PRODUCER-LIVE-CONSUMER-DEAD` (MINT)

- **what-considered:** (a) mint the auditor's two findings as two rows (DATA-FETCH-QUEUE-STALE CRITICAL + DATA-FETCH-STATS-EMPTY HIGH), as the signals were emitted; (b) fold into an existing BCTC/queue row; (c) mint ONE consolidated row.
- **why-change:** Chose (c). I queried the live DB rather than trusting the signal summaries, and the numbers reframed the finding. `deep_fetch_queue` has exactly two statuses — `expired` (2167) and `vps-failed` (5) — and `SUM(fetched_at IS NULL) = 2172/2172`. There is no success state in the table at all: **not one row has ever been fetched in 66 days**. `deep_fetch_stats` is empty *because* of that (its writer only fires on a successful fetch), so (a) would have split one root cause across two rows and invited two partial fixes. (b) was impossible — a scan of all 608 non-terminal rows for `deep.?fetch|fetch.?queue|fetch.?stats` returned **zero** matches.
- **contradicting evidence I looked for and did not find:** a `done`/`completed`/`pending` status anywhere in the tally that would have made this a partial-failure story instead of a total one.
- **escalation:** the auditor reported 2134 rows; live is **2172 and growing**, newest enqueued 4 minutes before triage. Producer live, consumer 100% dead.

## D-2 — `cron_job_runs` 205 crashed jobs — CRITICAL **DOWNGRADED**, no mint

- **what-considered:** (a) mint the CRITICAL as reported; (b) fold with a severity correction.
- **why-change:** Chose (b). The count is exactly right (205 crashed + 7 error, `bctcReparseJob` 72 / `intelligenceCycleJob` 42 / ...), but `MAX(started_at) WHERE status='crashed'` is **2026-08-11 14:00:02 — ~46h ago**, against 203,105 success rows (0.10% lifetime rate). This is a historical accumulation, not an active incident; the CRITICAL is an artifact of a detector with **no recency bound**. That is a known class — 2nd confirmed instance — so it folds onto `FIX-AUDITOR-EVAL-DELTA-RECENCY-BOUND` rather than minting.
- **note:** per-job causes are already owned (`FIX-BCTCREPARSEJOB-NOT-FIRING-40H...`, `FIX-BCTC-REPARSE-PERIOD-KEY-...`, `FIX-NEWS-REUTERS-DEAD-URL-DECOMMISSION`); the diagnosis blocker is `FIX-CRON-RUNS-NULL-ERRORMSG` (crash rows carry NULL `error_msg`).

## D-3 — A-30 memory cluster (9 signals/reports) — **live-refuted, but not the way I first read it**

- **what-considered:** (a) backfill the WARN the auditor narrated-but-never-wrote (telegram 4788's explicit ask #2); (b) refuse the backfill.
- **why-change:** Chose (b), and **reversed my own first conclusion mid-check.** My first reading was "rag-service 88.58% now vs 95.23% at 08:45Z, therefore reclamation happened, therefore false escalate." Then I ran the crash-cliff discriminator: `StartedAt=2026-08-13T09:20:09Z`, `FinishedAt=09:20:08.74`, `RestartCount=3`, `ExitCode=0`. **The drop is a restart artifact, not reclamation** — the exact misscoring trap on record. The 08:45Z reading was real and is simply unreproducible now, so a "current-state WARN" backfill would assert something false.
- **the finding this produced:** pdf-extractor tells the *opposite* story — 86.07%, **uptime 2 days, no restart** — so its drop IS a genuine reclamation dip. Same probe, same tick, two containers, two opposite verdicts. That is direct corroboration for `FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD`'s own thesis that the fix must be a **per-container window**, never a blanket threshold change. Promoted into the BATCH.

## D-4 — both "narrated-not-executed" auditor escalations (4788, 4795) — process defect real, **findings were false positives**

- **what-considered:** (a) treat the lost writes as lost findings and backfill both; (b) verify each underlying finding first.
- **why-change:** Chose (b) — and it inverted the disposition. 4795's `vn-bctc-fetch VPS UNHEALTHY` reproduces live (`get_vps_service_health` says unhealthy right now), which looks like confirmation until you read `FIX-AUDITOR-HEALTHCHECK-FALSE-UNHEALTHY-NONHTTP-SERVICES`: **vn-bctc-fetch is a pure-bash systemd timer with no HTTP port**, so that probe can never return healthy. Backfilling would have persisted a known permanent FP. Combined with D-3, **both** narrated-not-executed escalations had known-FP underlying findings — the lost writes cost nothing substantive. The execution gap is the whole defect, and it gets its own row (D-5).

## D-5 — three process-defect mints

| id | why it is not covered by an existing row |
|---|---|
| `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES` | The three adjacent `-NOTEBOOK-COMMIT-*` rows all **presuppose the commit fires** (trailer format, double-commit retro-rewrite, batching cadence). None covers fire-at-all. Verified across all 608 rows. AC-2 explicitly rejects a prose STOP/NEVER restatement as a fix — that shape already failed twice on `FIX-AUDITOR-TIER1-HEARTBEAT-HANDWRITE-...`. |
| `FIX-AUDITOR-DATATIER-REINVOKES-STATEFUL-PREGATE-SELF-DEFEATS-ROUTER-SPAWN` | The probe is **stateful** — reading it refreshes its own baseline — so it is not idempotent and must be called once per gate decision. 2nd agent after code-janitor with this shape, hence an AC for a fleet grep. |
| `FIX-DEVTEAM-SF1-GATE-BLIND-TO-INFLIGHT-DISPATCH-LIVENESS` | Carries an explicit **negative** AC-3: a long-running-but-healthy dispatch must never be killed. Without it this fix would trade a duplicate-spawn risk for a kill-good-work risk. |

## D-6 — routine (only path)

- **why-change:** no change from plan. 4 ci_red signals → all 3 checked runs name the identical 2 `FAILEDFILE`s, both already `ready[]`; folded, 0 mints, both promoted. 13 `bug-escalation` → folded onto `FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK` (agents are obeying the documented convention and being hard-blocked for it — that contradiction is the root cause of all 6 `escalated=true`). 32 notebook-prune signals, 4 context-bloat, 8 `bctc_signal`, 64 cowork telemetry → folded onto existing rows. DXG → `FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T`.
- **PO decision rendered (code-janitor asked, 4× duplicate signals):** **RETIRE `team-tool-recheck`** — reaffirming the 2026-08-12T17:00Z ruling. The re-fire is not a new ask; it is `FIX-JANITOR-HEALTHRECHECK-IDEMPOTENCY-GUARD-MISSES-PROCESSED-DIR` (`ready[]`) re-emitting every 6h because its idempotency guard never scans `docs/signals/processed/`.

## Residual gaps — flagged, deliberately not fixed here

1. `orch-validate` **Stage 1g false positive**: it reported `FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` as "MISSING in both hot board + cold archive", but it is `DONE_VERIFIED` at `archive/2026-08.json` `done_tasks[163]`. Stage 1g does not scan `archive/*.json` `done_tasks[]`. Non-fatal (REPORT stage only), so no mint against a 400-row backlog — recorded here.
2. Consequence of (1): `FIX-RAG-COMPACTION-DISK-AMPLIFICATION`'s stated blocker **is satisfied**; per its own note it should now be re-measured and re-sized.
3. **Possible regression, needs a 2nd data point:** a clean-exit (code 0) rag-service restart happened today at 09:20:08Z although `FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` is DONE_VERIFIED. `RestartCount=3`. Not minted on one observation.
