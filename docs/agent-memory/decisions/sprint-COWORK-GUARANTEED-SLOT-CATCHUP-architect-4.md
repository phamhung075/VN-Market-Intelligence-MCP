# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect

**Sprint goal:** Cowork guaranteed-slot catch-up (rolling sprint)
**Agent:** architect
**Started:** 2026-08-07T22:07:18Z (continuation of -3.md, which CAP-REACHED)

---

### STEP architect-S1 · architect · 2026-08-07T22:07:18Z
**task-id:** FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET
**what-done:** Wrote `docs/architecture-briefs/2026-08-07-fix-bctc-newschain-fallback-zeros-write-target-blueprint.md` — brownfield-reverified every BA-named file byte-exact (all 7 test line citations matched, zero material line-number drift), designed the new `bctc_news_fallback_hints` DDL (placed additively in `schema-financial-reports.ts`'s `initFinancialReportsTables()`, following the live 10+-table precedent, NOT the frozen `bctc-schema.ts` SQLITE_DDL string), the exact `newsChainFallback.ts` rewrite (keep signal-query/hints/confidence-calc/return-contract byte-identical, swap only the persistence target, drop the now-dead id-reuse-across-financial_reports-rows logic since FR-1 makes the PEK-FK concern structurally moot), the full 7-test rewrite plan, 2 new serving-plane regression tests (reusing the existing `callTool`/`_registeredTools` harness from `fix-bctc-identity-serve-guard.test.ts`), and RED-before/grep-verify/full-suite process steps.
**what-considered:**
- Table DDL placement: `bctc-schema.ts`'s `SQLITE_DDL` (BA left as architect's call) vs `schema-financial-reports.ts`'s `initFinancialReportsTables()` — chose the latter after confirming via read that zero new tables have landed in the frozen SQLITE_DDL string since Sprint 209 (10+ counter-examples all in the other file), and that string is flagged as covered by an equivalence test (higher risk to touch).
- Return-contract of `tryNewsChainFallback()`: keep the full in-memory `FinancialReport`-shaped zero object (chosen) vs redesign it to return a lean hints-only shape — chose keep-as-is because BA's own FR-3 explicitly says "no functional change required" on the resolvePdfText/fetchParseAndStoreBctc caller side, and a shape change would ripple into 3+ caller files' type contracts for zero benefit (nothing downstream persists/joins against the in-memory object once the DB write moves off financial_reports).
- id handling: keep the existing-row id-reuse dance (safer/smaller diff) vs remove it (chosen) — removed because FR-1 makes the original ID-ORPHAN/PEK-FK motivation structurally moot for this write path, and the project's standing rule is to reduce dead code, not preserve it out of caution once its purpose is gone.
**why-decision:** Live-verified every citation against source rather than trusting BA's line numbers (mandate from the spawn prompt) — found zero material drift, one genuine new finding (the PUB-1/PUB-2/PUB-3 gate gotcha in the extended AC-2 test, §7 task 7 of the blueprint) that would otherwise have cost a developer a debugging cycle discovering their naive `get_bctc_full` extension fails for the wrong reason (PUB-1 refusal, not a real arm-b1 regression proof).
**why-change:** EC-5 live DB probe (mandatory per the row) executed via a read-write `bun:sqlite` handle inside the running container (per `feedback_integrity_helper_readonly_wal_blinded` — readonly handles risk WAL blind-spots) — result 0 `news_inference` rows in live `market.db` today, reconfirming BA's historical-note claim at implementation-design time rather than trusting it blindly; no data-migration branch needed in the blueprint as a result.

### STEP architect-S2 · architect · 2026-08-07T22:37Z
**task-id:** FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE
**what-done:** Wrote `docs/architecture-briefs/2026-08-07-devteam-head-pin-stale-threshold-resume-bound.md` — cadence-proportional 2h stale threshold (basis: 30min cron cadence x4, 2x the existing 1h task-lock TTL backstop) replacing 24h, git-log-corroborated reset, new `.head.resume_attempts`/`last_resume_at` fields + 3-attempt circuit breaker (WF-3) that escalates to BLOCKED+`hold_reason` mirroring the existing WF-1 shape, 2 new BUG-channel signal shapes, full ready-to-apply diff spec for `docs/agents/dev-team/flow/main.md`.
**what-considered:**
- Age check field: `.head.updated_at` (matches current code) vs `row.claimed_at` (chosen) — live-read `scripts/orch-stamp-updated-at.mjs` and found it auto-refreshes `.head.updated_at` on ANY `.head` write, including the new resume_attempts bump this same task requires (AC-3) — using `updated_at` would make the age check self-defeating the moment resume attempts start recording; `claimed_at` (row-scoped, set by all 5 claim scripts, untouched by the head-only auto-stamp) is the only stable anchor.
- Chain ordering: age check evaluated in parallel with / before WF-2 (matches current code) vs strictly after WF-2 (chosen) — live-verified the CURRENT 24h check has no supervised-hold exemption at all (entry gate `L253` gates the whole WF-1..S2 chain including WF-2 on `updated_at<24h`); po_live_evidence_20260806 on this same row measured a 24.2h supervised-hold span, meaning shrinking to 2h without reordering would turn a rare edge case into a routine false-positive against legitimate PO-ratification waits. Restructured into one unified chain (WF-1/1b/1c/WF-2 unconditional on age, new WF-3/WF-4 age+attempt checks strictly after WF-2) instead of the naive in-place threshold swap.
- Escalation field naming: reuse `blocked_by` for the escalation actor (considered, rejected) — that field is ALREADY schema-meaningful (`orchStateSchema.ts:707`, reverse dependency-edge array feeding `effective_depends_on`) — would have silently corrupted dep resolution on the escalated row. Used `resume_attempt_bound_exceeded_at/_by` instead, matching the file's own established `<event>_at/_by` idiom.
- 08-05 adjacent write-coherence note (PO's own occurrence, folded into this row): fold into this fix's code (considered) vs scope out with a named follow-up (chosen) — different trigger class (instantaneous write-ordering vs this row's temporal-staleness ACs), directly matches project lesson `feedback_selfreport_conflates_two_dbadjacent_defects_by_topic`; recommended standalone follow-up `FIX-DEVTEAM-HEAD-NEXTAGENT-RESYNC-ON-REASSIGN` in the brief §6 instead of bundling two DoDs into one S-sized row.
- Handoff zone: developer (dispatch prompt's own guess, "your call") vs agent-father (chosen) — corrected using the standing `po_routing_ruling_20260721` artifact-class ruling, already applied twice to this exact file (TE-T02, 2026-08-06 qadrain brief §1c) — `docs/agents/dev-team/flow/main.md` is agent-instruction prose, `developer` carries no `docs/agents/**` commit-zone grant and its own init.md forbids `.ts`/`.py`; the ONLY code-zone piece (schema typing + regression verifier) is spec'd as an explicitly optional, non-blocking companion for PO to mint separately.
**why-decision:** Both `HeadSchema` and `TaskSchema` confirmed live `.passthrough()` — every new field validates with zero schema change, so the schema/verifier work is genuinely optional polish, not load-bearing for the 4 ACs, which let the handoff correction (agent-father) stand cleanly without an artificial code-vs-prose split inside one row.
**why-change:** Two findings not present in the row's own mechanism text (the auto-stamp interaction, the missing supervised-hold exemption on the existing 24h check) required restructuring beyond an in-place `24h→2h` edit — both are load-bearing for correctness (a naive edit would ship a self-defeating check and/or a new false-positive class), documented explicitly in the brief §1/§10 rather than left implicit for the implementing agent to rediscover.

### STEP architect-S3 · architect · 2026-08-07T23:57:37Z
**task-id:** FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM
**what-done:** Wrote `docs/architecture-briefs/2026-08-07-fix-pdfx-parent-process-memory-burst-headroom.md`. Named the mechanism with a live artifact: PEK pipeline (`POST /pek-extract`) runs torch/YOLOv10 + PaddleOCR CPU inference directly inside PID1 (thread-isolated only, unlike tesseract's real `ProcessPoolExecutor`). Isolated `docker run --rm` repro (own cgroup, same image+model volume, real 59p BCTC PDF, script saved to `scripts/audits/pdfx-pek-mem-arena-probe.py`): model load +845MiB, one job +236-485MiB, `gc.collect()` recovers 0, `malloc_trim(0)` recovers ~70-99% — discriminates AC5's 3 candidates decisively toward glibc-malloc-arena-fragmentation.
**what-considered:**
- Fire the row's literal AC2 burst (15-wide `POST /extract`) at production (rejected) vs isolated repro (chosen) — literal spec targets the wrong endpoint (tesseract path, already fixed) AND the live container already touched 98.5% of its cap this life; adding load risks the exact silent-death failure under investigation, and live load-testing is outside architect's charter (`not_my_job: Infrastructure diagnosis`). Corrected burst spec written for dev-pdf-extractor to run ops-supervised instead.
- AC5 discrimination via gdb-injected `malloc_trim` on the LIVE PID1 (rejected, too invasive/risky for a plan_only row) vs a separate throwaway container from the same image (chosen) — zero shared cgroup budget with production, same real code+weights+PDF, decisive result obtained safely.
**why-decision:** `malloc_trim(0)` recovering ~99.7% of job2's own growth while `gc.collect()` recovered 0 is a clean, actionable signal — glibc-arena retention, not Python-heap or (dominantly) deliberate-cache — directly motivates the AC3 fix recommendation (malloc_trim in `_run_pek_extract`'s finally) without landing code (plan_only).
**why-change:** Found the AC2 spec itself names the wrong endpoint (root_cause's own evidence names `PekEngineAdapter`/`/pek-extract`, not `/extract`) — corrected in the brief rather than executed as literally stated. next_agent set to dev-pdf-extractor (system-map.json Tier-1 zone match), not the dispatch prompt's implicit default.

### STEP architect-S4 · architect · 2026-08-08T00:38:00Z
**task-id:** FIX-AUDITOR-A29-UNEXECUTABLE-SPEC-SILENT-JOIN-DROP
**what-done:** Re-verified all 4 root_cause claims live (jq path, 32/38 grammar split, join counts). Found a 2nd, already-shipped, tested subsystem (`GET /api/cron-status`) that already solves A-29's predicate — rewrote A-29 to consume it instead of reinventing its own join/grammar. Landed the spec edit directly in `docs/agents/system-auditor/flow/main.md` (architect-authored doc edit, not a design left for another agent to type up) + full evidence brief.
**what-considered:**
- Hand-roll A-29's own normalization/grammar contract from scratch (my first draft, before finding cronStatusCompute.ts) vs discover-and-reuse the existing engine — chose reuse once `cron-parser` grep surfaced it: building a 2nd normalizer next to a tested one is exactly the defect class this row exists to close (`always_extend_not_duplicate`).
- Trust `/api/cron-status`'s own `status` field at face value vs add a discriminator — added one after live-testing found 9/90 rows are `NEVER_FIRED` via that engine's own honest-fallback join-miss (100% of its NEVER_FIRED population), not confirmed-dead crons — same defect class recurring in the reused code, would have silently reintroduced false-CRITICALs.
- Land the `STATIC_JOB_NAME_MAP` 8-pair alias fix myself (mechanical, ~8 lines) vs flag as follow-on — flagged: `apps/mcp-server` is explicitly out of scope per the dispatch instruction; A-29's own UNRESOLVED-JOIN bucket names all 8 every cycle so nothing is masked by leaving it unfixed.
**why-decision:** Live curl of the endpoint + live DB query (`cron_job_runs` DISTINCT job_name) gave ground truth for every claim rather than trusting the row's 2026-07-29 snapshot; AC4 explicitly required re-derived counts, not a re-assertion of the old ones.
**why-change:** AC2/AC3 effectively superseded rather than literally satisfied — A-29 no longer reads system-map's crons[] path at all (source switched to live `CRONS` via the endpoint), and no prose grammar was designed because the real schedule SSOT (`cronConfig.ts`) was never prose in the first place; both documented explicitly in the brief so the supersession is traceable, not silent.

### STEP architect-S5 · architect · 2026-08-08T01:41:00Z
**task-id:** FIX-ANALYSIS-ONLY-EXIT-DETECTOR-OR-VERDICT-BLIND-TO-PARTIAL-WRITE-CYCLE
**what-done:** Implemented directly (row arrived with RCA/ACs pre-written by PO). New `scripts/lib/output-contract-invariant.sh` sourced by both `detect-analysis-only-exit.sh` (AC-1 claim-vs-Plane-3 + AC-2 arithmetic gates, new `contract=` field) and `auditor-notebook-commit.sh` (AC-4 pre-commit refusal). 12/12 tests green (T8-T12 new, incl. RED=c80's real line, GREEN=c79's real line).
**what-considered:**
- Extract "the newest" `[OUTPUT-CONTRACT]` line via diff position (first draft: `tail -1`, assuming newest_first) vs check EVERY added claim line in-window (chosen) — live re-check against the actual c80 commit (569f79108) proved the first approach wrong: it bundles c80's fabricated line AND a legit stale c79 line (c79's own write never got its own commit — confirmed via `git log -S"## c79"`, string introduced only in 569f79108), and picking by physical position silently read the wrong (legit) line, staying falsely PASS. Redesigned to sum/check every claim found — sidesteps the per-notebook newest_first/oldest_first ambiguity entirely.
- AC-4 (pre-commit backstop): implement (chosen) vs evaluate-and-reject with a written reason — implemented; no `.test.sh` exists for `auditor-notebook-commit.sh` (MCP-coupled, predates this fix) so verified end-to-end via disposable scratch-repo + stubbed `mcp_call` (RED abort / GREEN commit) instead of a persisted regression test, flagged to QA.
- Shared lib vs duplicating the extraction/arithmetic in both call sites — shared lib chosen: a second hand-copy drifting from the first is the exact defect class this row exists to close (mirrors `scripts/agents-flow/lib/notebook-section-direction.sh` precedent).
**why-decision:** Live re-run of PO's own decisive command before AND after the fix (before: PASS rc=0; after: DETECTED contract=arithmetic-violation rc=1) plus spot-checks against real historical clean cycles (c76/c77, still PASS) and a wide 38-commit window (still isolates only c80) — verified against ground truth, not just synthetic fixtures.
**why-change:** No change from the row's 5 ACs; the bundled-commit bug above was found and fixed during this cycle's own validation, before landing, not left as a known gap.

### STEP architect-S6 · architect · 2026-08-08T02:33:00Z
**task-id:** FIX-PUSH-DELIVERY-ERROR-RATE-ALERT
**what-done:** Split zone=multi row into 2 single-zone children per po_ruling_20260730. Brownfield: mcp-server has NO `/metrics` route (zero prom-client usage); nearest reusable analog is `GET /api/vps-proxy-health` (vpsPushLogStore.getVpsProxyHealth, calendar-aware staleness) covering 3/4 RCA-scoped services (missing foreign-flow). alert-engine is 100% reactive today (GET /health, POST /evaluate only) — zero Ticker/scheduler; `cfg.DBPath`(market.db) is loaded but never opened in main.go. Minted FIX-PUSH-DELIVERY-METRICS-MCP-SERVER (extend vps-proxy-health) + FIX-PUSH-DELIVERY-ALERT-ENGINE-POLLER (new poller, depends_on child 1) into backlog[]; parent → review[] next_agent=po.
**what-considered:**
- RCA's literal in-handler 5xx-counter spec vs staleness-based signal — chose staleness as primary: CF-tunnel origin-unreachable (the actual 2026-07-04 failure mode) never reaches the mcp-server handler process, so an in-handler counter is structurally blind to it; flagged this correction explicitly in child 1's desc so dev-mcp-server doesn't implement the RCA table literally.
- Channel routing: let alert-engine's existing `Classify()` (high/critical→market) decide vs hardcode work — hardcoded work: market is stock-alert-exclusive per alert-policy.md (alert-commander), and the 2 in-repo precedents for this alert CLASS (vpsProxyWatchdogJob, cronHealthAlertJob) both use work.
- Child supervised/plan_only flags: copy parent's true/true vs reset to null/null — reset to null: supervised=true + dev-role next_agent is a documented zero-picker trap (BOUNDED-1 excludes supervised=true, DRS excludes dev-role next_agent, SLS needs both true) per live precedent BCTC-ENRICHER-OLD-QUARTERS and open row FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION.
**why-decision:** Read server.ts's full route table (zero /metrics), vpsPushLogStore.ts, vpsProxyHealthHandler.ts, alert-engine's pipeline.go/router.go/main.go/classifier.go/telegram.go/config.go, docker-compose.yml env wiring, and 2 existing mcp-server infra-alert precedents before designing — verified reuse targets and the CF-tunnel blind-spot from source, not from the RCA table's prose alone.
**why-change:** No change from the row's own scope (split it); the naive-counter blind-spot correction and the channel-routing fix are corrections to the RCA's literal instrumentation table, not scope changes to the task itself.

### STEP architect-S7 · architect · 2026-08-08T03:07:00Z
**task-id:** FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE
**what-done:** Re-measured live (34→14 dangling; structural defect unchanged). Wrote 2-axis reconciliation rule (AC-1) + full per-id table; wrote arming design for `orch-validate.mjs` Stage 1h (AC-2/AC-3); wrote third-state-branch design for `decision-journal-archive.sh` (AC-4); wrote AC-5 verification gate + AC-6 reaffirmation.
**what-considered:**
- Trust the archiver's existing `CLOSED_IDS_FILE` union at face value vs independently re-derive per-id open-task counts — chose re-derive after `--all --dry-run` (read-only, script's own contract) showed `TOKEN-ECONOMY-AUDIT`/`ULTRACODE-AUDIT-FIXALL`/`COWORK-GUARANTEED-SLOT-CATCHUP`/`FLOW-PRICE-ALPHA-LOOP` all `WOULD-ARCHIVE` today despite 9-23 open task rows each — the closed-id derivation (`.done_tasks[].sprint`, a per-task not per-sprint signal) is over-inclusive, not just the row's stated under-inclusive silent-skip.
- Ship AC-4's third-state branch standalone vs sequence it after the closed-id correction — sequenced after: flipping silent-skip to fail-loud on top of an already-unsound closed predicate fails loud on the safe case (no-orch-record) while staying blind to the dangerous one (wrongly-resolved-closed, still-open sprint).
- Auto-register all 14 dangling ids as either active or closed vs a 2-axis rule with a "pre-sprint label, exempt" branch — chose the exempt branch: 6 of 14 are backlog-only labels on not-yet-kicked-off work; forcing an `active_sprints[]` object for them would fabricate a sprint that never had a real kickoff.
**why-decision:** Every classification claim re-verified against live `orch-state.json` + live archive files + a live (dry-run-only, non-mutating) run of the actual script, not against the row's 2026-07-30 snapshot or its prose description of the mechanism.
**why-change:** Row asked for AC-1..AC-6 design; delivered that plus one CRITICAL-FINDING-flagged-not-fixed section (§2.3 of the brief) the row's own text did not anticipate — the archiver's closed-set being over-inclusive is a live, dated (2026-08-08T02:58:41Z), measured risk to 2 `sprint_goal`-active sprints' journals, surfaced for PO rather than silently left for AC-4's implementer to discover the hard way.

### STEP architect-S8 · architect · 2026-08-08T05:30:00Z
**task-id:** FIX-AUDITOR-DOCAUDIT-MEMORY-PATH-PREDICATE
**what-done:** Plan-only spec (supervised+plan_only — did not edit main.md). Re-verified row's stale `main.md:445` citation live (real section `:720-722`). Proposed verbatim diff repointing `## Existing Doc/Memory Audit § 1 Memory integrity` from phantom `memory/MEMORY.md` at `docs/agent-memory/INDEX.md` only. Swept DOC-AUDIT items 2-6 for sibling phantom paths (none found). Wrote `docs/handoffs/FIX-AUDITOR-DOCAUDIT-MEMORY-PATH-PREDICATE-spec.md`.
**what-considered:**
- Repoint at `docs/agent-memory/INDEX.md` only vs also add a 2nd sub-bullet validating the external Claude Code auto-memory file (present+healthy, proven live — this session's own context was auto-injected from exactly that per-project-hash path) — chose INDEX.md-only: the external file has no canonical repo-relative location (per-user/per-host absolute path), asserting it in a checked-in flow doc would either hardcode a host-specific path (violates this repo's no-hardcode convention) or stay unresolvable; every sibling DOC-AUDIT item (2-6, all live-verified) checks repo-tracked paths only — folding in one platform-external outlier check would be the one inconsistent sub-check, not a generalization.
- Negative-control design: synthesize a scratch fixture vs use naturally-occurring evidence — found `docs/agent-memory/INDEX.md` itself already has all 5 of its own listed session pointers dead (stale since commit `ace28b78d`, 2026-04-27, never updated as `sessions/` evolved) — used this as the PRIMARY negative control (first post-fix cycle proves both halves of the acceptance criterion in one real run) and kept the synthetic scratch-copy procedure only as a documented fallback for if a future cleanup makes the natural condition disappear.
- Whether to also fix INDEX.md's own broken pointers in this same task — explicitly out of scope (task boundary: read-only investigation for item 2, no fixes applied to any file besides the spec doc + board row); flagged for PO as a candidate follow-up row instead.
**why-decision:** Every citation re-derived live this cycle rather than trusted from the row/router snapshot — `sed -n '718,723p' | cat -e` for exact bytes, `git log --diff-filter=D` + repo-wide grep for the phantom-path-absence claim, `ls` against all 6 items' referenced paths (2-6) individually, `ls` against INDEX.md's 5 linked session files (found all 5 missing — the negative-control finding). Matches `never_design_without_reading`.
**why-change:** No change from PO's deliverable (repoint at INDEX.md OR remove; I chose repoint per PO's stated primary branch) — the "single sub-bullet, not two" call and the natural-negative-control substitution are interpretive refinements within the deliverable's own stated OR, documented with reasoning in spec §1/§3, not scope changes.

### STEP architect-S9 · architect · 2026-08-08T09:39:52Z
**task-id:** FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW
**what-done:** Plan-only spec choosing candidate (2) cross-window columnLayout threading (mirrors shipped statement_section pattern exactly) as primary fix + flag-only slice of (3) (real `truncated_continuation` field, closes a live comment≠code drift) over (1) raise-cap (rejected — doesn't fix the defect class, costs Haiku leaf-worker token budget). Wrote full diffs (4 TS files, 2 flow docs) + hand-traced regression fixture to `docs/handoffs/FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW-spec.md`.
**what-considered:**
- Adopting all 3 candidates vs a narrower combination — rejected "all 3": (1) has no correctness benefit once (2) lands, only cost; (3)'s on-demand-fetch half needs new leaf-worker tool-call machinery (2) makes unnecessary.
- Reverting to the original brief's literal lossy-truncation design (drop excess pages) — rejected: would regress the already-shipped, verified no-data-loss property (spike Finding 1) to fix a property (2) already fixes losslessly.
- Health-recheck WARN wording correction — traced its full lifecycle (dead RemoteTrigger writer, pruned log file, replacement flow explicitly out-of-scope) and found zero live doc still carries the misattribution; corrected the one still-live inaccuracy instead (windowPartitioner.ts:50 docstring) inside this same change.
**why-decision:** Hand-traced every regression-fixture assertion field-by-field against post-fix code (resolveColumnLayout branches, parseVnNumber format auto-detect, DT-2/DT-3 BLOCK predicates checked to confirm the fixture can't false-trip them) rather than asserting expected values on faith; caught and fixed my own negative-control bug (statement_section mismatch, not the layout param) before finalizing.
**why-change:** Task text said "implement" but row is supervised+plan_only — followed established precedent (S7/S8 same file) of plan-only spec + board→review, not code changes.

### STEP architect-S10 · architect · 2026-08-08T12:09:15Z
**task-id:** FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE
**what-done:** Gate-placement blueprint appended to BA's handoff (`docs/handoffs/FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE-BA-spec.md`): Rule AF-4 exact diff at chef-dish.md Step 6.7 (mirrors AF-3's structure, PO's sentence-scoped-strip on 2nd-pass FAIL swapped in for AF-3's honest-gap-and-leave-in half), Step 7.6 secondary-check diff, finalized `macro-threshold-registry.json` (added `aliases`/`historical_context_markers`/`number_format` — BA's shape had no way to locate a mention in text nor resolve EC-1/the VND-vs-USD separator collision), script+skill contracts (developer-authored, not implemented here — plan_only row).
**what-considered:**
- Trusted the relay's 2 corrected-anchor claims (macroTools.ts:125 not :118; 0 grep hits for 26500 vs 32 for 25500) vs independently re-deriving — re-derived both live (grep + Read), both confirmed correct.
- Stopped at the 2 claims the router asked me to check vs continuing the trace to see whether `currencySignal()` is actually wired to live output — continued (brownfield-scan mandate) and found it is NOT: dead code, never called; `get_macro_snapshot` routes through the fully-generic `buildMacroSnapshotText()` instead. Corrected the registry's `source` citation to point at the genuinely-live evidence (`macroAdjustments.ts`'s 8 cascade rules, `cascadeEngine.ts`-imported, live `usdVndMarket`) rather than ship an SSOT file with a load-bearing-but-wrong provenance note.
- Routing next_agent=pm (per architect's own flow template) vs next_agent=po (3x precedent this session on supervised FIX plan-only rows) — chose po: `supervised:true` exists specifically to keep PO as the human-facing gate on this exact task family (its own Q1 ruling shows why — a gameable-metric risk PO cares about), and PO has been sole adjudicator across this row's entire lifecycle (mint→BA dispatch→ratify), unlike a normal BA/PM/Architect/Developer sprint chain.
**why-decision:** SSOT value (25,500) unchanged by the dead-code finding — `macroAdjustments.ts` alone is sufficient live-code backing, and 26,500 still has zero code source anywhere (re-confirmed). The registry's `source` field is user-facing provenance an implementer/auditor will read later; shipping it with the (relay-inherited) wrong justification would plant a fresh false claim inside the very file meant to stop false claims — corrected before it lands as SSOT.
**why-change:** No change to BA's core mechanism (2-sub-check gate, primary+secondary placement) or PO's strip-not-block ruling — only additive: 3 registry fields BA flagged as "illustrative, for architect," a sequencing note (developer's artifacts must land before agent-father wires AF-4, or CHEF's next cycle hits a missing-file exit-2), and the oil-thresholds-already-disagree adjacent finding (flagged, not actioned).
