# Decision Journal — Sprint triage-20260729T0719Z · po

**Sprint goal:** ambient triage tick (router-dispatched: A-21 windowed crash detector predecessor-bound false negative; no active sprint_goal entry for this work)
**Agent:** po
**Started:** 2026-07-29T07:19:03Z

---

### STEP po-S1 · po · 2026-07-29T07:20Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Minted P1/architect/zone=multi row after prior-art grep found NO existing row for the window mechanics; verified both planes raw before minting.
**what-considered:**
- Attach to OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN (owns the crash restarts)
- Attach to FIX-AUDITOR-CALLER-PROSE-OVERRIDES-DOCUMENTED-DETECTOR-THRESHOLD (owns A-21 emit)
- Fold into EPIC-AUDITOR-DETECTOR-CORRECTNESS-DRAIN
- Mint standalone
**why-decision:** OPS row owns why shutdown is UNRECORDED; this owns why the counter drops records that DO exist. Caller-prose row owns PRECEDENCE, not the count. Epic is disposition-only over 18 enumerated rows and its AC(3) directs new work to PO to mint separately.
**why-change:** no change from plan.

### STEP po-S2 · po · 2026-07-29T07:20Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Re-measured both planes myself rather than accepting the supplied output: 8h window holds 1 row, loop runs 0 iterations, crashRestarts=0 while ground truth is 1; docker inspect RestartCount=4 Exit=0 OOM=false.
**what-considered:**
- Accept the router's numbers (they were detailed, internally consistent, and correct)
- Re-derive from the live DB and container independently
**why-decision:** internal consistency is not corroboration, and this row's whole claim is a measurement. Cost was two read-only calls; it also produced firstCleanShutdown/lastCleanShutdown, which is what exposed the cross-row interaction in S4.
**why-change:** no change from plan.

### STEP po-S3 · po · 2026-07-29T07:21Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Generalised the defect from "loop starts at i=1" to "a FIXED 2x lookback cannot bound an UNBOUNDED inter-startup gap", and wrote AC(1) to REJECT any widen-the-constant fix.
**what-considered:**
- File it as the reported off-by-one on the loop index
- File the structural form: predecessor fetched only if P >= now-8h, so gap > 8h is always dropped
**why-decision:** the off-by-one framing invites "widen 8h to 24h", which relocates the boundary instead of removing it and would re-recur. The structural form yields the real fix: unbounded predecessor lookup, candidates still window-bounded.
**why-change:** sharpened beyond the router's framing — the TS job at :171-173 states the 2x lookback's intent explicitly, so the guarantee is documented AND false, which is stronger evidence than the loop bound alone.

### STEP po-S4 · po · 2026-07-29T07:21Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Recorded a blocking cross-row interaction: with lastCleanShutdown frozen at 2026-07-22, an unbounded predecessor makes EVERY restart classify as a crash.
**what-considered:**
- Ship the window fix as a self-contained correction
- Force the architect to state an ordering position vs OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN
**why-decision:** the fix converts a false negative into a new false-positive class if the sentinel writer is genuinely broken. Discovering that in the production Telegram path is worse than deciding it now; hence plan_only.
**why-change:** not in the router's input — surfaced from the lastCleanShutdown value read in S2.
**SUPERSEDED 2026-07-29T08:12Z by STEP po-S8 below.** The word "new" was wrong: the false-positive class already exists at HEAD. Governing record = `po_ordering_reconciliation_20260729T0812` on the parent row. Do not read this step as a standing blocking ruling.

### STEP po-S5 · po · 2026-07-29T07:21Z
**task-id:** FIX-AUDITOR-CALLER-PROSE-OVERRIDES-DOCUMENTED-DETECTOR-THRESHOLD
**what-done:** Wrote a scope amendment onto that row confining its AC(5) ("DO NOT change the A-21 predicate") to the verdict mapping at :133-142, freeing the counting query at :112-131.
**what-considered:**
- Mint and leave AC(5) standing
- Amend AC(5)'s scope in place and name which row owns the count
**why-decision:** read literally AC(5) forbids the fix I just chartered; two P1 rows with contradictory instructions on one file strand both. A mint that leaves a live veto in place is not dispatchable work.
**why-change:** not in the router's input; found during prior-art read.

### STEP po-S6 · po · 2026-07-29T07:21Z
**task-id:** (ambient — disposition of my own prior ruling)
**what-done:** Amended signal sys-20260729T060929-39de: kept the retraction, but recorded that its safety argument ("1 is below threshold so A-21 stays ARMED") is FALSIFIED.
**what-considered:**
- Leave the 06:25Z disposition standing (it was mine, and the retraction verdict is still right)
- Amend it, since the same query now returns 0 on unchanged ground truth
**why-decision:** that disposition also says "do not open a row against A-21", which contradicts today's mint and would be read as standing policy. The count decays to 0 on its own rather than holding and firing on a second crash, so "retracting costs nothing" was wrong for a reason that matters.
**why-change:** my own prior cycle's premise was falsified by new measurement; correcting it beats letting it govern.

---

## Split-mint cycle — 2026-07-29T08:12Z (architect brief delivered)

### STEP po-S7 · po · 2026-07-29T08:12Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Re-derived the recurrence evidence from git instead of trusting `recurring_bug_count: 3`. Ruling: STRUCTURAL (de-duplication), not a fourth point patch.
**what-considered:**
- Take the count at face value ("three failed fixes") and re-architect the crash predicate
- Mint two point fixes, each hand-porting the corrected LAG SQL
- Re-derive the history first, then choose the axis the evidence actually supports
**why-decision:** git shows babe54c6c is a FEAT that *introduced* the 2x lookback; 2d494f77e and ff874c821 are same-day commits sharing ONE task id, and neither touched the bound. So it is one feature + one two-commit cycle, not three failed fixes — and the defect *multiplied* by hand-copy into the 2026-07-23 brief and then tier1-probe.md (2f956f1c5). The multiplier is REPLICATION, not re-breakage. Re-architecting the predicate would be scope inflation (the predicate is fine; the window mechanics are wrong and LAG fixes them). De-duplication is the move the evidence supports.
**why-change:** materially sharpens the router's and the row's own "three prior fix cycles" framing, which would have justified the wrong kind of structural change.

### STEP po-S8 · po · 2026-07-29T08:12Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Withdrew the S4 blocking framing; adopted ship-together; ELEVATED the architect's optional staleness caveat to a REQUIRED acceptance clause on child 1.
**what-considered:**
- Keep S4 standing (two contradictory records)
- Accept the architect's revision wholesale, caveat stays optional
- Accept the revision but fix the hole it leaves
**why-decision:** S4's "NEW false-positive class" is falsified by this row's own evidence — at HEAD the effective rule is "3 restarts in 8h", i.e. the FP path is already reachable; the fix lowers the accidental bar, changing likelihood not existence. But the brief adopted option (b) ship-with-a-guard and then made the guard declinable, which silently collapses (b) into the (c) it had just rejected. Lifting my block is conditional on the guard existing, so the guard is an AC.
**why-change:** my own prior premise falsified by re-checked arithmetic; the elevation is a correction *to the brief*, not to my earlier self.

### STEP po-S9 · po · 2026-07-29T08:12Z
**task-id:** FIX-A21-PREDBOUND-1-MCPSERVER-LAG-QUERY-AND-REGRESSION-TEST
**what-done:** Overrode architect brief §6 — the regression cases extend the EXISTING test file; the recommended new `2028-` file is rejected.
**what-considered:**
- Follow §6 as written (new test file, per "no test file exists, confirmed by search")
- Verify the claim at HEAD first
**why-decision:** the claim is false. `apps/mcp-server/src/__tests__/FIX-MCP-CRASH-LOOP-A-restart-cadence.test.ts` exists (332L, 10 cases, imports the exact function, already has the makeDb/insertStartup/insertCleanShutdown/nowMinus fixture convention the brief went looking for elsewhere). Minting a second test file for one module would replicate the exact duplication pathology this task exists to close — the same mistake, in the test plane.
**why-change:** direct read of HEAD contradicted a stated-as-verified premise in the brief; also found the real gap (no fixture puts a predecessor beyond the 8h bound) and the near-miss at :253 whose nowMinus(9) predecessor is currently invisible, so it passes partly for the wrong reason.

### STEP po-S10 · po · 2026-07-29T08:12Z
**task-id:** FIX-A21-PREDBOUND-2-TIER1-PROBE-DEDUPE-VIA-SHARED-ACTUATOR
**what-done:** Made the docker-exec-import MANDATORY (removed the implementer's hand-port fallback); added two ACs the brief did not have — import-failure must map to TOOL-UNAVAILABLE, and a deploy-lag honesty marker.
**what-considered:**
- Keep the brief's ranked "preferred / fallback" pair at implementer discretion
- Mandate the import, route the fallback to PO as a scope decision
**why-decision:** the fallback IS the recurrence mechanism. Leaving it as discretion means a fourth hand-port can be chosen quietly by whoever finds the import fiddly. Separately: a `catch → {crashRestarts:0}` would be a silent false negative of exactly the class this task removes, and after the SQL is deleted a reader greps a clean import and concludes A-21 is fixed while the un-redeployed image still under-counts. Both holes are cheap to close now and invisible later.
**why-change:** the deploy-lag trap is not in the brief — it surfaces from the interaction between the import shape and the user-gated, market-hours-fenced deploy.

### STEP po-S11 · po · 2026-07-29T08:12Z
**task-id:** FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE
**what-done:** Parent re-dispositioned to UMBRELLA — owner architect→po, next_agent architect→qa, plan_only true→false, depends=[both children]. Two children minted, both with explicit `next_agent`.
**what-considered:**
- Close the parent as superseded by the brief
- Collapse the parent into the dev-mcp-server row (fewer rows)
- Keep it as a dependency-gated umbrella
**why-decision:** closing loses AC(2) "must not diverge", which is only checkable across both diffs together and inside neither child. Collapsing it into the TS half loses the same thing. The parent is not a new mint, so keeping it costs no lane growth. Left `next_agent` explicit on all three — 11 backlog FIX-AUDITOR-* rows are stranded with `next_agent: null` and this split does not add a twelfth.
**why-change:** no change from plan.
