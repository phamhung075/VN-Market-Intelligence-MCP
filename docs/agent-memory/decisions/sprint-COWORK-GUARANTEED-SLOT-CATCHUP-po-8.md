# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · po (part 8)

**Predecessor:** `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-7.md` (closed 2026-08-08T11:33:05Z at 35669B / 36000B cap, last entry STEP po-S76). Entry numbering continues from po-S76 — do not restart the sequence.
**Class:** `sprint-decision-journal` · caps 600L / 36000B. Roll forward to `-po-9.md` with a `### CAP-REACHED` marker when this file approaches the byte cap; never prune prose out of a closed journal, it is the sprint audit trail.

### STEP po-S77 · po · 2026-08-08T11:37:00Z
**task-id:** FACTORY-PDF-split-handlers
**what-done:** Ruled on close-out after an ops agent returned non-convergent twice (~82k tokens / 72 tool calls, no disposition). Re-verified the docker state read-only myself, set the row's `verification_gate` (was `null`), wrote the close-out ruling onto the row, and folded a 3rd-service corroboration onto `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`. Zero mints.
**what-considered:**
- Resume the same agent a 3rd/4th time
- Respawn ops fresh with a narrower single-shot prompt
- Park the row `REVIEW`-blocked with a review_note describing the stuck-build loop
- Rule the residual to a 2-command scoped restart with a hard verification gate
**why-decision:** The agent's blocking premise is **stale, not true**. It reports "awaiting build completion", but `docker image inspect` shows `:latest` = `sha256:b5db1296ae85` **Created 2026-08-08T11:34:24Z** — that build already finished — and `pgrep -fl 'compose build pdf-extractor'` returns empty. Meanwhile `docker inspect` shows the container running image `sha256:a75ddd7336f0` with `StartedAt 2026-08-04T14:23:22Z`: built, never deployed. So nothing is pending; the residual is one scoped restart. A narrower re-prompt was rejected because the failure is not scope width — the agent has **no image-freshness precheck**, so every resume re-issues `docker compose build` instead of comparing the running `.Image` to `:latest`; the same missing predicate loops again at any prompt width. `REVIEW`-blocked was rejected because it would strand a P2 that has been code-complete since `b3853e817` (2026-07-29, 10 days) behind two non-destructive commands. Corrected one relay error that mattered: the router reported `a75ddd7336f0` as the *container* id; it is the *image* id (container is `59a9fb840776`) — and the whole disposition turns on comparing image IDs.
**why-change:** Went beyond the ruling the router asked for and set the row's `verification_gate`, which was `null`. The gate must be **image-ID equality + an import smoke**, and I explicitly forbade `health=healthy` as the discriminant: the container reports healthy *right now while running a 3.9-day-old image*, so a health-based gate returns green on exactly the state being tested. That same observation is the design input I folded onto `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` — with `alert-engine` and `news-fetch` already carrying `OPS-*-REBUILD-STALE-IMAGE-*` rows, pdf-extractor makes this the **third** live service in the built-but-never-deployed shape, upgrading that row from a process gap to a measured fleet pattern. No new row: it already owns the mechanism.

### STEP po-S78 · po · 2026-08-08T11:44:00Z
**task-id:** FACTORY-PDF-split-handlers (close-out ratification + compliance ruling)
**what-done:** Ratified ops's close-out as **UPHELD — no rollback, no redo**, but corrected two things in the record: nulled-then-set `next_agent`, stamped `qa_substituted_by`, and fixed a future-dated `moved_to_done_at`. Zero mints.
**what-considered:**
- Roll the row back to `REVIEW` and force it through `qa[]` as my ruling specified
- Accept it silently as a correct-outcome one-off
- Mint a FIX for "agent self-signs past a PO-mandated handoff"
- Uphold the outcome + correct the record + set an explicit mint trigger
**why-decision:** The substantive claim is confirmed on **three independent planes**, none of them ops's prose: router's raw `docker exec` import smoke (8/8 route modules) and image-ID inspect; my own re-read showing the container is a **new** id (`9d56864e3d85`, not the pre-restart `59a9fb840776`) with `RestartCount` reset 7→0, proving a genuine scoped recreate rather than a restart-in-place of the stale image; and clean startup logs with zero tracebacks as the corroborating negative control. Rolling back a verification that is already correct is theatre. But the process breach is real and worth naming precisely: the shape is **reader=writer** — ops performed the rebuild *and* signed off its own rebuild, so the gate was vacuous **by construction**, independent of the outcome being right. The actual falsehood in the SSOT was that a terminal `DONE_VERIFIED` with no `qa_verified_by` **reads as a QA pass that never happened**; that is what `qa_substituted_by` now corrects. `next_agent` set to `"qa"` as a record correction (first tried `null` — Zod rejected it, `orch-apply` fail-closed and left the live file untouched, which is the wrapper working).
**why-change:** Declined to mint, and the dedup was real: scanned the 5 non-terminal lanes and found **no** owner for "agent self-signs past a PO-mandated handoff" (the two near hits are different defects), so this genuinely is an uncovered gap — but it is a **single** occurrence, and my own `signal_feedback` discipline forbids minting a WARN-class single fire with no corroborating prior. Recorded an explicit mint trigger instead so the next tick does not re-derive it: a **second** self-sign past a PO-specified next-step meets the 2+ bar and mints, owner **agent-father** (the durable fix is a line in the ops flow doc, and `docs/agents/**` is agent-father's zone, not ops's). On the timestamp: `moved_to_done_at` was `13:40:00Z` — local CEST stamped with a UTC `Z`, reading ~2h into the future. Not cosmetic: any age gate computing `now − moved_to_done_at` yields a **negative** age and can never fire, the same way a terminal row escapes cold-eviction. Swept **every** ISO-8601-Z field on all 767 rows across all 7 lanes for future values — **exactly one hit board-wide**, this field. A script writing local-as-Z would have produced many; one hit is a hand-typed one-off, so no systemic row minted. Corrected to `11:40:45Z` (matching the row's own `updated_at`, which the same writer got right in the same write) and re-swept clean.

### STEP po-S79 · po · 2026-08-08T12:20:31Z
**task-id:** FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE
**what-done:** Reviewed architect's gate-placement blueprint. Confirmed the sequencing + dead-code claims, FALSIFIED the oil adjacent-finding, and found a blocking cross-plane defect the whole chain missed. Row REVIEW→BLOCKED (`blocked_by` 2 rows); promoted `FIX-USDVND-THRESHOLD-SSOT` backlog→ready/high/architect with `blocks` back-edge. No agent dispatched.
**what-considered:**
- **(a) Sign off + dispatch developer-then-agent-father as architect proposed:** REJECTED. Would ship a gate enforcing 25500 against narrative while the live Go classifier emits "exceeds 25000 threshold" into that same narrative — the gate would strip the numeral CHEF's own data source produced.
- **(b) Dispatch developer for the metric-agnostic mechanism only, defer the registry values:** REJECTED. Design 4's test cases (a)-(g) hardcode 25500/4300 as fixtures, so the harness would be rebuilt once the SSOT rules. No net saving, and a green test suite asserting the wrong canon is worse than none.
- **(c) Rule the SSOT myself right now (25000 vs 25500) and unblock:** REJECTED. Go's 25000 is a DIRECTION-band bound (23000-25000, BEARISH/NEUTRAL/BULLISH), TS's 25500 is a CASCADE-IMPACT trigger — plausibly two concepts, not one drifted constant. Collapsing them on the spot would repeat the exact error I had just caught in architect's oil finding.
- **(d) BLOCKED + promote the row that owns the decision:** CHOSEN.
- **Mint the oil follow-up row architect recommended:** REJECTED — premise falsified, see why-change.
**why-decision:** Every grep in this task's evidence chain (BA §1a, my own po_goahead, architect's "independent verification") used `--include='*.ts'`. The Go plane was never searched. `macro_usdvnd_direction_classifier.go:34` holds `BearishThreshold = 25000.0` and `:91` interpolates it into a narrative string ("USDVND at %.0f exceeds %.0f threshold"), live-wired via `macro_signals.go:144` + `usecases.go:194/295`. So "25,500 is the only candidate with real production-code backing anywhere in the repo" is false, and the row's founding root-cause ("LLM drift toward the familiar 25,000 media milestone") is inverted — CHEF was plausibly relaying its upstream tool verbatim. A gate cannot assert an SSOT that is not settled.
**why-change:** Three departures from architect's plan. (1) No fan-out dispatch — sequencing claim is correct and upheld, but both downstream artifacts are blocked on the unsettled canon. (2) Oil row NOT minted: architect read `macroTools.ts >90/<70` vs `macroAdjustments.ts >100` as a contradiction, but the LIVE file carries all three (>90 L46-61, <70 L65-74, >100 L151-172) as deliberate severity tiers, its own comments saying so — no gap exists; falsification recorded on the row so it is not re-minted. (3) Added a 5th blocking finding of my own: `gold_usd: 4300` in the finalized registry has zero code provenance in either plane and its sole source doc is the exact block already condemned by backlog row `FIX-CHEF-L6-GOLD-FALSE-PREDICATE` ("the cure shipped a false-claim generator", AC = "source or delete the $4,300 constant") — enshrining it would reintroduce the 26,500 defect shape inside this task's own fix. Also flagged the single-scalar-per-metric registry schema against multi-tier live code (usd_vnd also has a 24500 lower band) as a false-positive strip generator under my own unconditional-strip ruling.
