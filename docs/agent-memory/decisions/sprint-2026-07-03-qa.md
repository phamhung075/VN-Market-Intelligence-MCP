# Decision Journal — Sprint 2026-07-03 · qa

**Sprint goal:** ambient — CHORE-GITIGNORE-CLAUDE-TMP is a standalone cross-service chore, not scoped to the active MERGE-MONEY-RADAR-INTO-MOMENTUM sprint.
**Agent:** qa
**Started:** 2026-07-03T19:11:00Z

---

### STEP qa-S1 · qa · 2026-07-03T19:20:00Z
**task-id:** CHORE-GITIGNORE-CLAUDE-TMP
**what-done:** Added `.claude/tmp/` to `.gitignore`, untracked 111 tracked `orch-hook-proposal-*.json` files (git rm --cached, files kept on disk), one commit.
**what-considered:**
- only path: forward-fix (ignore + untrack), no history rewrite — task explicitly ruled out `git filter-repo` (token ephemeral, not warranted)
- verified `scripts/orch-apply.sh` has zero reference to `.claude/tmp` — its temp file lives under `docs/data/orch/` (same-fs atomic-rename contract); the `.claude/tmp/orch-hook-proposal-*` writer is a separate PreToolUse hook (`scripts/agents-flow/orch-state-hook-prewrite.mjs`), not orch-apply.sh itself
**why-decision:** round-trip `jq '.' orch-state.json | orch-apply.sh` still exits 0 ("OK — candidate applied", 104 pre-existing non-blocking coherence warnings) post-untracking — confirms no dependency broken.
**why-change:** first commit attempt used `git commit -m ... -- .gitignore .claude/tmp/` (pathspec-limited); this silently discarded the staged `git rm --cached` because path-limited commit re-reads working-tree-vs-HEAD for listed paths, not the index, and the untracked files were still present on disk with unchanged content. Fixed via `git reset --soft HEAD~2` + plain unscoped `git commit` to correctly capture the index deletions. Verified: no exact session UUID in any added/committed line (only in content of the files being removed, which is the leak being fixed).
