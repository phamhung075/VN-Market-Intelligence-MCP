# Decision Journal — Triage 2026-08-23T12:32Z · po

**Scope:** router-queued 5-observation triage (owner_client_session 7be6b4cd-057e-419b-a967-4810daf2b646). Live peer dev-team session 007e33e4 held `intent:dev-pdf-extractor:FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE` throughout — no row it owns was touched.
**Agent:** po
**Started:** 2026-08-23T12:32:29Z

---

### STEP po-T1 · po · 2026-08-23T12:42:46Z
**task-id:** FIX-RAG-COMPACTION-DISK-AMPLIFICATION, FIX-BCTC-CTG-BALANCE-SHEET-REFINE
**what-done:** Rewrote both rows' prose-glued `depends_on` strings to clean 1-element arrays; both now return `deps_satisfied=true`.
**what-considered:**
- Router reported ONE offender. Ran the predicate across all 7 lanes instead of trusting the count: 212 dep ids on 127 rows, THREE contain whitespace, TWO are pure formatting errors. FIX-BCTC-CTG carried `TASK-W5-...-CARRY-FORWARD (prior carry-forward work exposed this data-completeness gap)` — a parenthetical the router had classified as a legitimate single id.
- Preserve the discarded prose in `status_note` vs discard it.
**why-decision:** Discarded. `depends_on` is in STRUCTURAL_FIELDS (scripts/orch-row-prose-ceiling-check.mjs:128-165) so it is NOT prose-measured; `status_note` is. Moving the sentence would have converted 0 measured bytes into ~400 measured bytes on rows for which the rationale is already moot — both dep targets are DONE_VERIFIED in cold archive (2026-08.json, 2026-07.json), so the sequencing argument the prose makes has already been satisfied. Text preserved here instead.
**why-change:** Widened from 1 row to 2 vs the brief.

### STEP po-T2 · po · 2026-08-23T12:44:59Z
**task-id:** FIX-BCTC-CTG-BALANCE-SHEET-REFINE
**what-done:** Set `owner`/`next_agent` = dev-pdf-extractor, `zone` = `apps/pdf-extractor/`.
**what-considered:** only: unblocking the dep was half a fix — the row had `next_agent: null`, `supervised: false`, `plan_only: false`, which is Step A2's explicit "NO LANE" trap. `deps_satisfied=true` on a row no picker can see is not progress.
**why-decision:** system-map zone `apps/pdf-extractor/` maps to specialist dev-pdf-extractor by keyword (BCTC, financial report). Distinct task id from the peer session's held intent, so no collision.
**why-change:** no change from plan.

### STEP po-T3 · po · 2026-08-23T12:44:59Z
**task-id:** FIX-ORCHSTATE-DEPSFIELD-ACCEPTS-PROSE-SENTENCE-AS-DEP-ID
**what-done:** Minted the write-time guard (backlog, dev-mcp-server, P1).
**what-considered:**
- Reject dep ids failing an id-charset regex vs reject only on whitespace.
- Leave Stage 1g NON-FATAL vs promote the prose class to FATAL.
**why-decision:** Whitespace only — measured as a perfect discriminator on live data (3/212 hit, 0 false positives on the other 209). A charset regex would have to guess at `TASK_001`-style underscores and numeric ids and could reject legitimate rows. Promote to FATAL only for the whitespace class: genuine MISSING-but-well-formed ids are a legitimate transient state (12 live rows today) and must stay non-fatal.
**why-change:** Added AC3 to migrate HOLD-CRON-MARKETWATCHER-NEWSSCOUT-MARKETHOURS-MODES-PRODUCT-DECISION, whose `blocked_by` holds a deliberate 380-char DO-NOT-DISPATCH paragraph — the guard would otherwise reject the next write touching that row.

### STEP po-T4 · po · 2026-08-23T12:47:32Z
**task-id:** FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE, CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER
**what-done:** Issued a superseding ruling on team-tool-recheck; retracted CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER (status BLOCKED, `next_agent` removed); dispositioned signal cj-20260823T103003Z as triaged.
**what-considered:**
- Answer the janitor's `decision_needed` as framed (replace vs retire).
- Verify the premise first.
**why-decision:** Verified first, and the question was already settled AND shipped. `docs/agents/agent-father/flow/team-tool-recheck.md` (74L) records `CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON (2026-08-06, po triage, DECISION=REPLACE not retire)`, runs the static file-based subset on agent-father's existing `23 14 * * *` cron, and `docs/agent-memory/health/` holds 12 live reports (newest `team-tool-recheck-2026-08-22-1242.md`, yesterday). The writer is alive. Worse, the row carried TWO CONTRADICTORY PO rulings — 2026-08-12T17:00 and 2026-08-14T16:54 both said RETIRE PERMANENTLY and instructed the implementer to DELETE the escalation branch; 2026-08-14T13:37 and 2026-08-15T09:15 recorded the opposite. That contradiction is the reason no implementer has been able to act on a P1 row for 9 days. Both RETIRE rulings rested on premises ("writer is dead", "nothing consumed it in 7 weeks") that were already false when written, so both are RETRACTED, and the derived row CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER (backlog, next_agent=developer, promotable) would have instructed a developer to stop a running job. Kept BLOCKED on the board rather than deleted because two PO ticks and the 08:20Z signal triage all cited it as authority; deleting it silently would let that citation chain re-form.
**why-change:** No new row minted — this observation was reported as needing a fresh PO decision and needed a retraction instead.

### STEP po-T5 · po · 2026-08-23T12:44:59Z
**task-id:** FIX-NEWSSCOUT-OFFHOURS-SELFCOMMIT-PROSE-RECIPE-INTERMITTENT
**what-done:** Minted to `ready[]`, next_agent architect, zone `multi`.
**what-considered:**
- Reopen FIX-NEWSSCOUT-COMMIT-POLICY-NEVER-MECHANICALLY-WIRED.
- Mint a fresh row for a different failure shape.
- Route direct to agent-father (doc-only rewire) vs architect (split).
**why-decision:** Fresh row: the 2026-08-15 fix closed "no step executes a commit" and demonstrably worked on the 2026-08-22 off-hours cycle (cea834da2). What remains is probabilistic compliance with a 26-line conditional prose recipe — a different defect with a different fix. Architect, not agent-father: the durable fix is mechanization into `scripts/agents-flow/`, and `scripts/` is outside agent-father's commit zone while `docs/agents/` is outside a developer's, so it needs a two-owner split. Precedent is exact — `cowork-write-last-fired.js` replaced last-fired.md's inline version after two silent corruptions of the same class.
**why-change:** no change from plan.

### STEP po-T6 · po · 2026-08-23T12:44:59Z
**task-id:** FIX-COWORK-FLOWDOC-STALE-WEEKEND-SUPPRESSION-AND-BGFAN1-RETURN-PRESUMPTION
**what-done:** Minted both cowork flow-doc defects as ONE row to `ready[]`, next_agent agent-father.
**what-considered:** Two rows (one per defect, clean independent verification gates) vs one row with two ACs.
**why-decision:** One row. Same owner, same flow tree, both small doc edits, both found on the same tick. The board is under measured lane pressure (RLC starvation, qa cap-10 dam) and a second row buys a second dispatch cycle for no additional verification value — the two ACs are independently checkable by grep within one dispatch.
**why-change:** Independently re-verified all four staleness claims for Step 4.3 rather than accepting the brief: cadence-policy.json:20 (gatherer-standard weekend 480), cowork-match-slots.js:316-317 (`_cron_fallback` = cron governs, not suppress), evaluateCadence owns the calendar gate, gatherers fired 2026-08-22T20:08:12Z. Also confirmed the sibling SUPERSEDED markers live in `pressure-cadence.md:74,134,196` — a different file from `pressure-read.md`, which is why `grep -c SUPERSEDED` on the latter returns 0.

### STEP po-T7 · po · 2026-08-23T12:42:46Z
**task-id:** FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION
**what-done:** Corrected the stale `note` IN PLACE — it named TASK-COWORK-CATCHUP-2 as unverified after PO flipped that row to DONE_VERIFIED at 10:19:45Z today.
**what-considered:**
- Accept the brief's finding that the row cannot be corrected in place at 11732 B against a 12000 B ceiling.
- Read the enforcing script first.
**why-decision:** Read it first, and the premise is wrong. `scripts/orch-row-prose-ceiling-check.mjs` is GROWTH-ONLY: it rejects only when candidate prose exceeds the ceiling AND exceeds that same row's live prose. A byte-neutral or shrinking correction always passes. Rewrote the note 589 → 578 chars naming FACTORY-APP-split-assembleBriefing as the sole remaining unverified DONE row. Applied cleanly; the run logged 23 pre-existing grandfathered WARNs and 0 net-new-growth violations.
**why-change:** Brief said "cannot be corrected in place" — false as stated; true only for corrections that ADD text.

### STEP po-T8 · po · 2026-08-23T12:44:59Z
**task-id:** FIX-COLDEVICT-REFERENTIAL-GUARD-PINS-EVERY-TERMINAL-ROW-ZERO-RECLAIM
**what-done:** Minted (backlog, developer, P1) with the guard-held count measured, not asserted.
**what-considered:** only: the brief named the referential guard as the cause of the 0-byte preview; PO must verify a causal claim before minting an AC that tells a developer to weaken a safety guard.
**why-decision:** Verified by computing `$referenced_dep_ids` directly against the live file: 140 distinct dep ids across the 5 live lanes pin 23/30 `done_verified[]` rows and 3/14 `done[]` rows. Confirmed the guard is now redundant as well as binding — `dep_status_map($archive)` already resolves cold-archived deps (FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP resolves MISSING hot-only, DONE_VERIFIED with the archive layer threaded). Added AC2 from a second finding the brief did not have: `n_guard_ref_held`/`guard_ref_held_ids` are computed at scripts/orch-cold-evict.sh:692-701 and never printed, which is why the 0-byte preview reads as unexplained. AC1 deliberately forbids deleting the guard without a negative control.
**why-change:** no change from plan.

### STEP po-T9 · po · 2026-08-23T12:48:04Z
**task-id:** (ambient — Step 0-SIG durable-inbox CLEAR)
**what-done:** Triaged and cleared 4 `pending_triage_inbox` envelopes that accumulated after the 10:33Z clear; 0 mints, all DEDUP HIT.
**what-considered:** Skip the inbox (the router scoped this run to 5 named observations) vs run Step 0-SIG.
**why-decision:** Ran it. FIX-TRIAGE-INBOX-CLEAR-OWNERSHIP-PO-SELF-READ makes the CLEAR unconditionally PO-owned and self-read, and one of the 4 envelopes WAS the carrier for observation 4 — skipping would have left the item's own envelope undrained. Cleared by `envelope_id` subtraction with `ORCH_APPLY_DECLARED_INBOX_TRIAGED`, never a blind `= []`.
**why-change:** Re-recorded the 10:33Z caveat verbatim: this clear removes the cowork-fire envelope that `guard-signal-type-coverage.sh:198` reads, so CI can go green with the routing table still unfixed.
