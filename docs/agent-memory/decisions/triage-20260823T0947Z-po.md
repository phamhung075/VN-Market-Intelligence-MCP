# PO decision journal — adjudication 2026-08-23T09:40–09:52Z · three agent follow-ups deliberately withheld from their own rows

**task_id:** 3 rows minted + 1 dangling dep resolved + 1 signal drained. Router-direct, no dev-team tick.
**Spawned by:** router session `7be6b4cd`, three items adjudicated: (1) mint a policy row for the inert flow-file cap, (2) drain a 24-day-old signal, (3) rule on architect's two withheld flags.
**Concurrency respected:** `pm` decomposing the two cron-durability rows and `architect` on the prose-ceiling row — neither touched. Did not re-attempt the blocked `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` stamp.

## D1 — Verified every relayed premise at source. One came back FALSE and it changed the row's whole scope

- **what-considered:** (a) mint from the router's brief, which was detailed and internally consistent; (b) re-derive each claim from the scripts, plists and git HEAD first.
- **why-decision:** (b), per `feedback_flow_doc_veto_manufactures_pass_read_spec_before_blaming_script`. It paid on ITEM 3(a) — see D4.
- **what-changed:** ITEM 1 and ITEM 2 confirmed as relayed. ITEM 3(a)'s central premise was wrong. ITEM 3(b) resolved to the opposite of what "dangling" implied.

## D2 — ITEM 1: minted to `agents-architect`, and the blast radius is 62 files, not "many"

- **verified:** `case "docs/agents/system-auditor/flow/main.md" in docs/agents/*/flow/**/*.md)` → NO-MATCH; nested path → MATCH. 173 flow files, 0 nested. `context-bloat-backstop.sh:132` exits 0 on the non-governed hot path before any `wc`.
- **what-considered:** (a) treat it as a one-line glob patch to `developer`; (b) route the rollout decision to `agents-architect` as the file's declared `_maintained_by` policy owner.
- **why-decision:** (b). Measured the cohort rather than describing it: 98/173 within both caps, 63 over the 120L line cap, **59 over the 7200B byte cap**, 13 line-only breaches absorbed by an existing `size-justification` header, **62 would emit on their next edit**. The load-bearing number is 59, because the script's own TE-T24 note states `size-justification` honors a LINE count only and NEVER a byte breach. So 59 breaches have no existing escape hatch at all — including `dev-team/flow/main.md` at 26.7x the byte cap. That is a policy calibration, not a patch.
- **fence written into the row:** `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist` — validate the corrected glob against all 173 and diff the emit set; any allowlist opt-IN and dated.

## D3 — ITEM 2: closed, but verified against git HEAD rather than the escalating agent's fixture report

- **what-considered:** (a) accept the five-fixture before/after table as sufficient; (b) confirm the code itself is in HEAD.
- **why-decision:** (b), per `feedback_auditor_resumed_run_narrates_writes_never_executed` — a fixture table is a claim about a diff, not proof the diff landed. `git show HEAD:…spawn-fanout.md` returns `MARKER_HITS >= 2` (line 460-472, commit `b545a69cc`) and `SESSION_ID_LINE` (line 292, commit `0d16f28ce`). Both of the signal's two recommendations are shipped; the shipped comment block cites this signal by filename as its own evidence. Closeable.
- **fingerprint deviation, recorded not faked:** the canonical formula is `sha256(from + type + JSON.stringify(payload) + createdAt)` and this file has none of those four fields. Substituted `sha256(raw bytes)` = `dc12eb28…` and wrote `fingerprint_basis` into `_processed` stating why. DB INSERT skipped for the same reason; the move to `processed/` already makes re-drain impossible, so cross-cycle dedup is not weakened.

## D4 — ITEM 3(a): the relayed premise was FALSE, and correcting it is what made this a NEW row instead of a fold

- **relayed claim:** "the 2026-07-07 brief §3.8 ordered a Tier-1 loaded-assertion that was never shipped."
- **measured, and it falsifies that:** Check 6, `_check_launchd_agents()` at `auditor-tier1-probe.sh:583-687`, IS shipped. It reads each `launchd/*.plist`'s own `<key>Label</key>` (no hardcoded list), requires an exact field-3 label match AND field-2 status `== 0`, and honors an ack ledger with `tracked_by` liveness. Hardened twice on 2026-07-23. Architect's own brief §8 R6 says only "it is loaded *today*" — "never shipped" was added in relay.
- **the real defect, which is different and worse:** loadedness is **invariant under host Standby**. `launchctl list` prints `-\t0\tcom.vn-market.cowork-guaranteed-slot-firer` right now, byte-identical to what it printed throughout the 96.5 h Standby of 08-18→08-22. Check 6 was ALL_GREEN for the whole of a total Layer-C firing outage.
- **and there is no fallback oracle:** `log()` at `cowork-guaranteed-slot-firer.sh:97` is called only inside the fire path, so ~90% of the 900 s ticks emit nothing BY DESIGN. Log silence is the healthy steady state. **No observable anywhere distinguishes "ticking, nothing due" from "asleep, not ticking."**
- **what-considered:** (a) fold into `FIX-COWORK-DAILY-SLOT-SILENT-SKIP-…`; (b) new row.
- **why-decision:** (b), four reasons. Architect explicitly asked for a candidate row rather than a fold. `pm` is mid-decomposition on that exact row and folding races a live decomposition (`feedback_pm_decomposition_races_live_parallel_terminal`). The file surfaces are disjoint — Tier-1 probe vs. `cowork-missed-fire-audit.js`. And a fold would have carried the false "never shipped" premise straight into pm's decomposition.
- **anti-duplication fence written into the row:** brief §6 #2 builds a per-SLOT miss detector; this row is per-PROCESS tick liveness. Implementer must read §6 #1/#2 and §8 R3 first, and **close this row as a duplicate** if the two genuinely collapse — never build a second detector alongside theirs. Routed straight to `developer`, no architect hop, because the design is forced by the constraint (unconditional pre-gate heartbeat + 2x-cadence freshness assertion, retroactively evaluable).

## D5 — ITEM 3(b): the dependency was not missing-and-needed. It was already satisfied

- **what-considered:** (a) mint `OPS-COWORK-GUARANTEED-SLOT-INSTALL` so the dep resolves; (b) remove it.
- **measured before choosing:** located the only surviving record at `archive/backlog-detail.json items[375]`, status BACKLOG, never in any hot lane. Then checked its three declared acceptance criteria live: the LaunchAgent is loaded (`launchctl list`), the plist is symlinked into `~/Library/LaunchAgents/` (dated 10 jul), and the old `fb-daily-firer` is absent from both `launchctl list` and `launchd/`. All three met. Its parent gate `F1-LAUNCHD-COWORK-BACKSTOP` also landed — script, plist and real invocations in the log.
- **why-decision:** (b). Minting it would have created a no-op ops row and delayed a QA gate that has been held since 2026-07-28. Set `depends_on: []` and wrote the full verification into `po_dep_resolution_20260823` so the removal is auditable rather than a silent unblock. `QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL` is now dispatchable.
- **scoping note left for QA:** point 7 of the 7-point spec is testable today, but scope it to LOADEDNESS only — it must not be failed for the firing gap, nor passed by claiming firing coverage. That gap is D4's row.

## D6 — Process review: the 24-day sit was not a triage lapse, and the root cause is systemic

- **the router asked for "a line in the journal" about drain-coverage. The measurement turned out to be much worse than a coverage gap.** `drain-signals.js --count-drainable` prints **`drainable_count=1`** against **51** files in `docs/signals/`. Re-derived with the script's own predicate: 50 of 51 have `from`, `source`, `type` and `signal_type` all null.
- **mechanism:** `isDrainableShape()` (`drain-signals.js:84-88`) skips them, so they are never enveloped, never routed, never fingerprinted, never moved, and **never pruned** — the 7-day prune only walks `processed/`. No age check, no floor, no escalation. Oldest stuck file: 25 days.
- **why this is the root cause of ITEM 2:** both of that signal's recommendations shipped, 24 days apart, and it was never drained on either occasion. Both fixes reached production by routes that bypassed the inbox entirely. The inbox is not a queue; it is a write-only directory.
- **the writer is not obviously at fault:** PO's own `main.md` "Signal write rule" specifies only the FILENAME contract and says nothing about a required body envelope. The envelope is enforced solely by a reader-side predicate that no writer-side doc states. That asymmetry is the defect.
- **what-considered:** (a) note it and move on, as asked; (b) mint it.
- **why-decision:** (b), P1 to `architect`. Distinct from `project_signalrow_type_open_namespace_vs_closed_allowlist_20260813` — that is a type VALUE unmatched inside a valid envelope; this is envelope ABSENCE, which never reaches the type table. Row explicitly forbids a blanket reader-widening: `BY_PATH_CONSUMER_FAMILIES` (`drain-signals.js:102`) proves some non-envelope files are legitimate by-path dish inputs, and that family has already been misdiagnosed as stuck litter four times.

## D7 — What I deliberately did NOT do

- Did not re-open any of the 6 rows from the 09:04Z triage. Did not touch either row `pm` is decomposing or the row `architect` holds.
- Did not re-attempt the blocked stamp on `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE`, and did not fold the recurrence onto the ceiling row. Confirmed in passing that the wrapper reported **23** pre-existing over-ceiling WARNs and **0 net-new-growth violations** on my own write — the grandfathering mechanism works, and the narrow defect really is only the numeric-bump recognition, which is architect's row.
- Did not escalate anything to the user.
