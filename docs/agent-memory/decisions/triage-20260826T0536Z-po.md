# PO triage — 2026-08-26T05:36Z

Session `036ceaf1-bf34-46cd-92e4-8c6b213ff4bb`. Inputs: 23 envelopes in
`.dev_team_idle_chain.pending_triage_inbox`, 6 NEW `.signal_queue.rows[]`
addressed to `po`. Actuator: `scripts/po-triage-20260826T0536Z-blocked-freeze-and-langvi-ruling.jq`
piped through `scripts/orch-apply.sh`. ONE write.

---

## STEP po-1 — lang=vi BCTC quality (P1 priority item) — ASK REJECTED, REPLACED

**task_id:** `MEASURE-PDFX-BCTC-QUALITY-TESSERACT-VIE-PRODUCTION-BASELINE`

**what-considered:** (a) mint the before/after measurement exactly as the signal
asked; (b) reject outright; (c) establish the missing production baseline instead.
Chose (c).

**why-change:** The signal's premise — "production BCTC OCR quality has plausibly
moved for the FIRST time" — is FALSE. Verified read-only, no extractor run:

1. `docker exec` on the RUNNING container `417febec1a03` returns five env vars
   (`BCTC_PAGE_TEXT_BACKEND`, `PADDLE_OCR_BASE_DIR`, `PDFX_OCR_MAX_CONCURRENCY`,
   `PDFX_OCR_PAGE_TIMEOUT_S`, `PDFX_OCR_QUEUE_WAIT_S`). `OCR_TEXT_BACKEND` is
   **not among them** — unset.
2. Unset selects `TesseractVieBackend`
   (`apps/pdf-extractor/__tests__/test_ocr_backends.py:77-78`, restated at
   `apps/pdf-extractor/infrastructure/ocr_orientation.py:34`).
3. Two of the three `lang="vi"` sites — `ocr_adapter.py:609` and
   `ocr_worker.py:268` (the signal cited `:224`) — are on the PaddleOCR **text**
   path, reachable only under `OCR_TEXT_BACKEND=paddleocr|auto`. Both off.
4. The third, `pek_engine_adapter.py:420`, is disclaimed by its own adjacent
   comment at `:412-418`: *"select_ocr_backend() defaults to tesseract-vie and
   OCR_TEXT_BACKEND is unset, so this instance supplies the table GRID, not the
   cell TEXT."*
5. The one decision that would have switched the path on,
   `DECISION-PDFX-OCR-TEXT-BACKEND-DEFAULT-FLIP-TO-AUTO`, was REJECTED AND
   CANCELLED 2026-08-25T17:52Z (DBC 46% rescue fire vs its own 10% rollback
   trigger; 494 `memory.max` events vs a baseline of 0).

All three `lang="vi"` sites are therefore **dormant** for production BCTC text
quality. A before/after would measure a null change and would manufacture exactly
the unfounded improvement claim the signal's own `caution` field warns against.

**Two absence claims in the signal are also wrong.** Both
`OPS-PDFX-REDEPLOY-DEBT-LANG-VI-FIX-INERT-IN-PRODUCTION` and
`FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS` are
present at `DONE_VERIFIED` in `docs/data/orch/archive/2026-08.json`. Nothing is
lost; the redeploy discharge did leave a trace. See STEP po-3.

**What was minted instead:** the user's standing goal is served in production
today by tesseract-vie, and this fleet has never held a repeatable quality
baseline for the engine that actually runs — which is why every prior quality
claim here was contested and then retracted. AC-2 measures char count, near-zero-
text page count and diacritic-presence rate on DXG + FPT p9; AC-4 settles whether
`pek_engine_adapter.py:420` `lang='vi'` vs `'en'` changes the table GRID at all;
AC-5 forbids any config change; AC-6 forbids anyone asserting an improvement on
the back of it. Gated `next_recheck_not_before=2026-08-26T09:00:00Z`.

---

## STEP po-2 — stale-BLOCKED freeze: 5 rows unfrozen + class fix minted

**task_id:** `FIX-DEVTEAM-BLOCKED-STATUS-FREEZES-ROWS-NO-CONSUMER-ALLOWLIST-ADMITS-IT`

**what-considered:** only path — the router cannot self-actuate a status ruling.

**why-change / MECHANISM CORRECTION.** Signal `rtr-20260826T0429` named
`scripts/lib/devteam-eligibility.jq:117` and `:160` as the gate. **They are not.**
Those lines are `wip_in_progress` / `incident_wip_in_progress`, which exclude
BLOCKED from the WIP *budget* — that FREES dispatch capacity (deliberate, per
`FIX-DEVTEAM-WIP-BUDGET-COUNTS-BLOCKED-INPROGRESS-ROWS`). A fix aimed there
changes nothing.

The real gate is eight **positive status allowlists** in the consumer scripts,
none of which contains `BLOCKED`:

| script | line | allowlist |
|---|---|---|
| `devteam-backlog-promote-bounded1.jq` | 104 | `BACKLOG\|TODO` |
| `devteam-backlog-promote-design-router-sweep.jq` | 128 | `BACKLOG\|TODO` |
| `devteam-backlog-promote-supervised-lane-sweep.jq` | 107 | `BACKLOG\|TODO` |
| `devteam-backlog-claim-ready-lane-consumer.jq` | 137 | `READY\|TODO` |
| `devteam-backlog-claim-incident-lane-consumer.jq` | 121 | `READY\|TODO` |
| `devteam-backlog-claim-supervised-lane-sweep.jq` | 168 | `READY\|TODO` |
| `devteam-review-claim-qa-drain.jq` | 189/194 | `REVIEW` / `DONE` |
| `devteam-review-claim-secondary-drain.jq` | 134/139 | `REVIEW` / `DONE` |

So BLOCKED is terminal-in-practice in every lane and deps are never consulted.
55 live rows sit at BLOCKED. Five had every blocker resolvable to DONE_VERIFIED
(hot **or** cold archive) and were unfrozen by hand; hand-clearing does not scale.

Negative controls written into AC-4/AC-5 so the fix cannot be a blanket clear:
`CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER` (deliberate hold — now carries
`hold_reason`), `HOLD-CRON-MARKETWATCHER-NEWSSCOUT-MARKETHOURS-MODES-PRODUCT-DECISION`
(its `blocked_by` is a paragraph of English, not an id), and
`ALPHA-S2-RAG-FTS-REBUILD-CRON` (one blocker done, one genuinely still blocked).

**Market-hours gate re-encoded (strand 2).** `blocked_by:
"AC8-MARKET-HOURS-GATE-UNTIL-0900Z-WEEKDAY"` is a clock condition wearing a task
id — it resolves to nothing hot or cold, so no completion event can ever
discharge it. Replaced on 2 rows with `next_recheck_not_before`, the key already
defined in `devteam-eligibility.jq` `gate_not_before_keys` / `is_gated_not_before`.
Status stays BLOCKED for now because that key is honoured by the qa-drain
consumer ONLY — making it binding in all six backlog/ready consumers is AC-2 of
the class fix, after which all four gated rows self-release with no agent edit.

---

## STEP po-3 — dedup search blind to cold monthly archives

**task_id:** `FIX-PO-DEDUP-SEARCH-BLIND-TO-COLD-MONTHLY-ARCHIVE-FILES`

**what-considered:** only path — three false negatives from one blind spot in a
single day is a mechanism defect, not a run of bad luck.

**why-change:** `scripts/po-board-dedup-search.sh:30` expands `--all-lanes` to ten
lane names, every one of them a key **inside the hot** `orch-state.json`. The cold
monthly files `docs/data/orch/archive/2026-0*.json` (`.done_tasks[]`) are never
opened, yet `:18` advertises `--all-lanes` as the exhaustive mode. Today that
produced: two false "row is lost" claims (both rows are DONE_VERIFIED in
`2026-08.json`), one false "the discharge left no trace" claim, and it is why
`VERIFY-FIX-COVERAGE-SWEEP-BLANKET-STAMP-REALDATA` looked permanently blocked —
its blocker `FIX-BDI-SHIPPING-STALE-404-GUARD` is DONE_VERIFIED in `2026-07.json`.

---

## STEP po-4 — lane move ATTEMPTED and REFUSED (recorded, not narrated as done)

`FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` sits BLOCKED inside
`in_progress[]`. The first version of this write moved it to `backlog[]`;
`orch-apply.sh` aborted the **entire** write on
`[orch-row-prose-ceiling-check] id=... live=0B -> candidate=18774B`.
The `0B` is false: `PROSE_CEILING_LANES` omits `in_progress[]`, so the pre-move
baseline reads zero and the move looks like 18.7KB of brand-new prose. Owning row
already exists and is already dispatchable: `TASK-PROSECEILING-LIVE-BASELINE-ALL-LANES`
(`ready[]`, READY, P1, developer) — whose own spec note already records "two rows
are parked BLOCKED-in-place right now instead of lane-moved because of this".
This is the third. Move dropped from the write, refusal stamped on the row.
Harmless meanwhile: BLOCKED rows are excluded from `wip_in_progress`
(`devteam-eligibility.jq:117`) and `claimed_by`/`claimed_at`/`owner_client_session`
are all null, so it holds no claim and consumes no dispatch budget. Its BLOCKED
status is CORRECT, not stale — `TASK-COWORK-MUTEX-001` is genuinely still REVIEW.

---

## STEP po-5 — signal dispositions

| signal | disposition |
|---|---|
| `rtr-20260826T0429-staleblockedstatusfreezesrows` | READ — actioned, with the STEP po-2 mechanism correction |
| `rtr-20260826T0431-invarianttestmissedretainedgoliterals` | READ — DEFERRED, not declined; two named kinh-dich owner rows exist |
| `cj-20260826T043200` | RESOLVED — premise already falsified on the board; real owner `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE` already dispatchable |
| `rtr-20260826T0435-datatiercyclewrotenorecord` | READ — DEFERRED; third signal in the same auditor-DATA-tier family today, fold onto the existing owner rather than mint a fourth |
| `rtr-20260826T0505-dbintegrityhistorydoubleappend` | READ — DEFERRED; same family, corrupts a trail not production data |
| `rtr-20260826T0530-bugchannelnotifyonceabsent` | READ — DEFERRED; arrived mid-triage, needs one shared contract row not per-agent rows |

Durable inbox cleared to zero in the same write (23 envelope_ids declared via
`ORCH_APPLY_DECLARED_INBOX_TRIAGED`; conservation check reported
`inbox_row_identity=clean`).

---

## Carry-over

- **09:00Z today** — four rows need `status: BLOCKED -> BACKLOG` once the clock
  passes, highest first: `FIX-MARKETDB-20260826-RESTORE-DROPPED-12205-FF5M-AND-54-EVIDENCE-ROWS-STILL-RECOVERABLE`
  (P0, ops), `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` (P1),
  `FIX-PDFOCR-ORIENTATION-CORPUS-79-FILES-312-PAGES-SWEEP-REVERTED-BY-DB-RESTORE` (P1),
  `MEASURE-PDFX-BCTC-QUALITY-TESSERACT-VIE-PRODUCTION-BASELINE` (P1). This step
  disappears once AC-2 of the class fix lands.
- 50 rows remain at `BLOCKED` after this tick. Only the 5 with all-blockers-done
  were touched; the rest were not audited row-by-row.
- 4 deferred signals to mint next tick (STEP po-5).
- `docs/agent-memory/decisions/triage-20260824T2030Z-po.md` is an earlier PO
  journal still untracked in git — not written by this tick, left alone.
