# PO Triage — 2026-07-28T13:10Z · po

**Trigger:** router dispatch (session e3a3f331) — blocking: a 10-row epic with 9 unrouted rows; 9 NEW signal rows; 3 standing structural findings.
**Board writes:** 2, both via `scripts/orch-apply.sh` (validated + conservation-checked). `.head` untouched, still `idle`.

---

### STEP po-T1 · po · 2026-07-28T13:26:20Z
**task-id:** FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY (mint)
**what-done:** Source-verified the cowork-team headroom-gauge report to root cause and minted it P1 → dev-mcp-server; closed the reporter's own "not_yet_established" question.
**what-considered:**
- Fold into UC-CDC-P1 (pressure-state family) — rejected: that row is calendar_status, a CALLER-SUPPLIED circular field; this is a SERVER-COMPUTED field that runs and lies.
- Defer as "gauge oscillation, no incident" — rejected: it gates spawn-fanout Step 5.1 degraded mode at a hardcoded <1500 and reads 2284 now.
- Mint with root cause established (chosen).
**why-decision:** `computeHostHeadroomMb()` tries macOS `vm_stat` then Linux `free -m`; running inside the container only the second can succeed, so it measures the Docker VM under a field named `host_headroom_mb`. Live three-way proof: gauge=2284 (13:07:45Z) vs this host's `Pages free`=261 MB vs `free+inactive+speculative`=6427 MB — it matches neither, so it is not this machine. Second, latent defect: the macOS branch returns `Pages free` alone, which would sit permanently below the 1500 floor.
**why-change:** the reporter left "is it the Docker VM or genuine drift" open; PO closed it at source rather than minting the open question.

### STEP po-T2 · po · 2026-07-28T13:26:20Z
**task-id:** FIX-DEPSSATISFIED-COLD-ARCHIVED-DEP-RESOLVES-MISSING (mint)
**what-done:** Found and measured a systemic dispatch blocker while repairing one row: cold-evicted deps resolve MISSING and block dependents forever. Minted P1 → developer.
**what-considered:**
- Hand-repair only the one row I needed (QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL) — rejected after measuring the population.
- Mint + also hand-repair the acute instance (chosen).
- Route to architect as a design question — rejected: the fix is one predicate in one shared library plus an eviction-time guard.
**why-decision:** measured, not inferred: 34 live rows carry ≥1 dep resolving to MISSING; 29 of the 40 distinct blocking ids are DONE_VERIFIED in cold archive. `dep_status_map` scans only the hot board, and all four pickers share it, so one line of scope hides ~34 permanently-stuck rows.
**why-change:** no change from plan — prior-art grep found 4 cold-evict rows, all about eviction mechanics, none about the dep-resolution consequence.

### STEP po-T3 · po · 2026-07-28T13:26:20Z
**task-id:** FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX (converge) + 9-signal drain
**what-done:** Drained all 9 NEW signal rows to RESOLVED with explicit dispositions; folded the D4 `pipeline_mismatch` facet onto the existing row (P3→P2) instead of minting; minted nothing for rag-service or pdf-extractor.
**what-considered:**
- Mint a second D4 row for the pipeline_mismatch emission — rejected, same detector, same root cause, one row.
- Leave the 9 rows NEW as the reporter did — rejected: I acted on all 9, so NEW would misstate the state; the anti-pattern is flipping READ *without* acting, not recording an actual disposition.
- Mint for pdf-extractor 85.54% — rejected: stable plateau under cap, healthy, RestartCount=2; recorded a named re-escalation threshold instead.
**why-decision:** the pipeline_mismatch predicate compares `.head.active_task_id` against a sprint-task escalation lock owned by another agent in another plane — a category error that mismatches every tick for the whole 7-day TTL, so a whitelist entry alone only halves the FP. On the auditor's prose-only "458 PDF jobs stuck, 0 completed in 24h": unverifiable from the PO shell, but the "0 completed in 24h" half is refuted on two planes — `list_stored_pdfs` shows files dated today, `get_sla_status` reports bctc age 235 min vs a 19552 min SLA. Recorded as unsubstantiated, not minted.
**why-change:** no change from plan.

### STEP po-T4 · po · 2026-07-28T13:26:20Z
**task-id:** (ambient — router's 3 structural findings)
**what-done:** Split the router's 3 findings by prior art: 2 already owned (attached evidence, no mint), 1 genuinely unowned → source-verified, then minted.
**what-considered:**
- Mint a fresh row for review-lane QA-Drain starvation (review=116, qa=0).
- Attach to the existing owner.
- Take the spawn-fanout finding on the router's description alone.
**why-decision:** `FIX-DEVTEAM-IDLE-CHAIN-STEP1-TRIAGE-STARVATION` (P0, →pm) already carries the identical mechanism plus a PO ruling, an architect brief, and a `devteam_direction_correction_20260725` that inverts the premise exactly as the router describes; executed the SLS candidate list — it is rank-0 **#1**, so it dispatches to pm on the next tick with `in_progress==1`. `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT` owns the Step-0a `NEW→READ` root cause (its own consequence is the cross-team leg; the router's po-burial leg is a second consequence of the same line) — folded in, not minted. The spawn-fanout finding matched NO row, so I verified it at source before minting.
**why-change:** the router said the spawn-fanout item was "filed 07-25"; it is not on the board. Filing ≠ a row — the PO-mint≠board lesson, live again. Verified independently instead: `spawn-fanout.md:192` dispatches `slot.flow_path`; digest-daily's `flow_path` (`daily-predict.md`) and its own `trigger_prompt` (`main.md`) name different files; the `published:digest-daily:<UTC_DATE>` gate exists only in `main.md:15-45`; `daily-predict.md`'s lone `task_claim` is the commit-mutex. digest-sunday's two fields agree, so digest-daily is the only divergent slot. Minted P1 → developer.

### STEP po-T5 · po · 2026-07-28T13:30:55Z
**task-id:** FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER (converge) + 3 lane repairs
**what-done:** Measured the NO-LANE hole board-wide — **162 of 397 backlog rows (41%) match no dispatch lane** — and attached the census to the row whose own fix created the largest sub-class, instead of minting.
**what-considered:**
- Mint a new coverage row.
- Blanket-repair all 48 `plan_only`-without-`supervised` rows in one transform.
- Converge onto `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` + repair only the chain I need (chosen).
**why-decision:** the census is the residual of that row's own 2026-07-21 fix (SLS's select is `supervised AND plan_only`; a row with one flag is withheld by BOUNDED-1 and unmatched by SLS), so a second row would be churn against a still-open one. Blanket-repairing 48 rows fixes 48 symptoms and leaves the mechanism — and destroys the acceptance evidence: the fix is right when the census reaches 0 with nobody touching a row. Repaired exactly 3, each with a live consequence: the sweeper row (which its own lane could not pick up), `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT`, and its sole blocking predecessor `FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT`.
**why-change:** repaired the predecessor as well as the target — verified by execution that fixing the dependent alone would have been useless, since `deps_satisfied()` would have held it behind a predecessor that could itself never dispatch.
