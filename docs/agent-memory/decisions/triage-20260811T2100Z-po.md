# PO triage decision journal — 2026-08-11T21:00Z

**Inputs:** 34 `pendingSignals[]` · 15 unresolved Telegram reports (4684-4698) · `tnb-audit-latest.md` c126 · `.task_board` (763 rows) · `git log -30` · branch `main` only.
**Writes:** 1 `orch-apply.sh` pipe (`scripts/po-triage-20260811T2100Z-a29-refute-bizctx-p0-ci-fold.jq`) — 4 mints, 8 amendments, `task_total` 763→767, `.head` untouched.

---

## D-1 — `FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING` P1→P0, `occurrence_count` 2→3

- **task_id:** FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING
- **what-considered:** (a) bump to P0 per the standing rule; (b) hold at P1 pending a 4th instance; (c) re-scope to a data-availability row.
- **why-change:** (b) and (c) both fail on evidence. The threshold was set by PO itself across three prior ACKs (c123/c124/c125) as "3rd consecutive day / 3rd ticker → P0"; declining to honour a rule I authored, on the first tick where it is satisfiable, would make every future threshold I set unenforceable. (c) is refuted at source: `bctc_signal_{VCB,FPT}_20260811_routine.json` were `processedAt` 18:20:54Z, **1h32m before** the 19:53:18Z dish, both fully populated — and I re-confirmed the same four payloads independently in my own `pendingSignals[]` inbox at 18:08:03Z. The dish's `known_gaps[]` token `[gap:business_context_unavailable_signal_drain_archive]` is factually wrong; this is a wiring defect wearing a data-gap label.
- **not changed:** `next_agent` stays `ba`. Row is `supervised:true` — it needs a spec, and routing it straight to an implementer to chase a P0 would skip the gate that exists because this row's shape is a join, not a patch.

## D-2 — `FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL` P2→P1; all 8 A-29 findings refuted

- **task_id:** FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL
- **what-considered:** (a) treat the 6 CRITICALs as a real scheduler death; (b) attribute them to the 67h Anthropic-quota outage, as the dispatch brief suggested; (c) fold into the existing root-cause row.
- **why-change:** (a) and (b) are both **refuted live**, not argued away. I queried `cron_job_runs` inside `vn-market-intelligence-mcp-mcp-server-1` (`docker exec … bun bun:sqlite`, readonly) rather than trusting the alert text:
  - (b) dies first: the outage was on the `claude -p` CLI plane; these are mcp-server in-process node-cron jobs, and the in-process scheduler ran *continuously* through the whole outage window (`deepFetchMainJob` 16 452 runs, last 20:55:02 — i.e. right now). **No A-29 finding is explained by the outage.** Worth stating plainly because the brief proposed the opposite and it is a tempting, wrong join.
  - (a) dies on the day-profile: `vpsProxyWatchdogJob` / `price-update-watchdog` / `vnIndexRefreshJob` / `taAlertNotifierJob` are market-hours crons (`*/N 2-8 * * 1-5`). Their 2026-08-11 profile is **byte-identical** to 08-07 and 08-10 (n=42/42/84/28, first fire 02:00:0x, last at the window edge). A 9.4-9.6h gap at 18:22Z is the overnight window, graded against a flat 0.1-0.4h 24/7 threshold.
  - `taAlertScan`/`bbAlertScan` "MISSED 2625.6h": 27 runs each, 2026-04-23→04-24, then merged into `alertScanParallelJob` (first run 04-27, 1644 runs, healthy today). They survive only as `cronConfig.ts:89-92` config keys with no registration. The number is arithmetically exact and semantically empty.
  - `ragFtsRebuildCron` = deliberate env-gate; `brokerSanctionsSweep` = the dom/dow OR-vs-AND artefact. **8/8 false, 0 genuine gaps.**
- **why fold not mint:** all 8 map onto sub-defects the existing row already names, and **7 of the 8 job names appear verbatim in its own 2026-08-09 evidence field** — I minted that row myself 2 days ago. Minting again would have been me duplicating my own work.
- **why reprice anyway:** P2 was priced on blast radius (zero — every maligned job is firing correctly) and that stays true. P1 prices *recurrence cost*: two full PO triage cycles in 3 days, 6 CRITICAL rows on the human-facing BUG channel that are indistinguishable from a real scheduler death, and on 08-09 an entire ops investigation that reached a wrong conclusion before PO refuted it.

## D-3 — 4× `ci_red` → 0 new rows

- **task_id:** FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L (+2 sibling rows)
- **what-considered:** mint per the `ci_red` template; or dedup.
- **why-change:** the flow's **mandatory pre-dedup failing-file read** was executed before dedup was evaluated, on all four runs (`gh run view … --log-failed`), and the fail set is identical and stable across all 4 HEADs: `ALLZERO-OHLCV-FETCH.test.ts`, `FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts`, and `apps/mcp-server/src/interface/mcp/transport.ts`. All three hit **file-scoped `dedup_key`** rows already open in `backlog[]`. Appended the 4 observed SHAs to each; corrected the size-lint row's title 237L→265L (the file grew since the row was written).
- **note:** the CI problem here is not detection or triage — it is that three correctly-scoped rows have sat in `backlog[]`/`dev-mcp-server` across four red HEADs with nothing dispatching them. Surfaced as UNBLOCK, not as a new FIX.

## D-4 — 4 new mints

- **task_id:** FIX-PO-REFUTED-DETECTOR-FINDING-HAS-NO-SUPPRESSION-FEEDBACK-CHANNEL (P1) — the second-order finding behind D-2. My 08-09 refutation is durable **only as prose inside a `task_board` row that `emit-audit-signal.sh` never reads**; its ledger (`auditor-dedup-ledger.json`) keys on `(dedup_key, ts, sev)` with no verdict dimension, so there is nowhere an adjudication could live. The detector is not malfunctioning — it has no channel. Scope-fenced against the obvious wrong fixes (widen thresholds / extend TTL / patch A-29).
- **task_id:** FIX-AUDITOR-TIER1-HEARTBEAT-HANDWRITE-RECURS-SAME-DAY-AS-ITS-OWN-FLOW-FIX (P1) — `heartbeat-guard` REJECTed the hand-write twice (18:44:58Z, 18:47:34Z) on the same day `FIX-AUDITOR-TIER1-FLOW-HAND-WRITES-HEARTBEAT-FILE` shipped a STOP/NEVER restatement. Three successive fixes have all been text-in-a-flow-doc; the only layer with a 4/4 record is `scripts/git-hooks/pre-commit`. AC-3 explicitly forbids closing this with more prose. AC-1 requires giving the Tier-1 cycle a *positive* path to a genuine heartbeat — a guardrail that only forbids leaves an unmet need, and the agent keeps improvising.
- **task_id:** FIX-SBVOMO-LIQUIDITY-CRON-ERROR-STRING-CONFLATES-TIMEOUT-AND-UNREACHABLE (P3) — only path: `system-issue` from dev-team, no dedup hit, out-of-zone find. No change from plan.
- **task_id:** CLEAN-CTXBLOAT-CRON-COWORK-TEAM-SKILL-242L-OVER-200L-CAP (P3) — only path: uncovered file, `dedup_key` follows the established `context_bloat_breach|file:<path>` convention. No change from plan.

## D-5 — folds without mints

- **task_id:** FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE — report 4684's `report_id` is literally `fallback-VND-2023-Q4`, the exact shape this P0 owns. `occ` 47→48. Recorded that the report's own suggested action (manual `/api/trigger-pek-extract` re-fire) is **invalid for this class** — 0/66 shells ever produced a row DB-wide, so a re-fire cannot succeed.
- **task_id:** FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER — report 4685 is direct live evidence for this P0's premise. Second-order note attached: `verdict=ERROR` ≠ `verdict=FAIL`, so the guard degrades to *unobservable* rather than alarming — the WAL state is currently unmonitored.
- **task_id:** FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK — 5 fires, `escalated=true`, `prior_warns` 32→36, **`outcome=blocked`**. The triage rule's `escalated=true` branch says open-or-append against the sweep-guard family; this row is that home. Disposition changed materially though: the hook is now hard-rejecting real commits, i.e. the contradiction this row describes is realised, not hypothetical.
- `[sweep-guard] SAME-FILE DIVERGENCE` (1 fire): the tracked FP root-cause row is `DONE_VERIFIED` in `archive/2026-08.json`, so per the flow this is a *candidate genuine* divergence and the two-blob diff is mandatory. Ran it in the correct form (**no pathspec** — appending one is a git usage error that manufactures a false benign): 6 insertions / 5 deletions on `orch-state.json`, consistent with the actor's own `orch-apply.sh` write between staging and commit. Informational, no escalation.
- A-30 `signal_feedback` (pdf-extractor-1 85.12%): WARN, single fire, **below** the 93/97 gates, and covered by `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM`. No mint on a single tick, per the re-emit-churn rule.
- Outage post-mortem (4695): verified both tickets exist, are correctly priced P0, and are **not** duplicates of each other — one owns detection, the other owns catch-up recoverability. No action beyond confirming.

---

## Self-check — my own script was wrong and the validator caught it

First `orch-apply.sh` run **aborted at Stage 1e** with 7 dangling-`blocks` errors. Those were not pre-existing debt: my jq rebuilt `.task_board.backlog` as the concatenation of only the four new-mint terms, dropping all 385 existing rows, which made every `blocks` target unresolvable. Fixed by prepending `.`, then dry-ran to a scratch file and diffed lane counts (`backlog` 385→389, `ready`/`review`/`qa`/`in_progress` unchanged) **before** re-applying. Conservation check confirmed `task_total` 763→767.
**Keep:** a validator abort is a finding about my own write, not an obstacle to route around — and a lane-count dry-run is the cheap check that would have caught it first. Never pipe a hand-written `|=` array rebuild straight into `orch-apply.sh` without one.
