# PO decision journal — Review-Lane SECONDARY-Drain owner-triage, dev-team tick 2026-08-23T23:07Z

Written: 2026-08-23T23:44:49Z · agent: po · flow: docs/agents/po/flow/main.md (router-direct SECONDARY-drain dispatch) · session 007e33e4-b453-4bb3-8ab1-ef31495906a3

---

## STEP po-1 — [task_id: FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT] Refused the DONE_VERIFIED sign-off; made the row auto-closeable instead

**what-considered:** (a) DONE_VERIFIED sign-off — it is `plan_only:true` and pm's decomposition deliverable genuinely shipped; (b) escalate BLOCKED; (c) reassign next_agent and move it to a lane that has a reader.

**why-change:** Chose (c), rejected (a). Resolved all 4 children on the LIVE board rather than trusting the status_note: FIX-SIGNALQUEUE-PUSH-SPAWN-FANOUT-MECHANISM, -SIGNAL-DASHBOARD-DOCS, -UNIFIED-AGENT-CONSUMER, -ALERT-COMMANDER-CONSUMER are ALL `ready[]` / `status=TODO`, 0/4 complete, untouched since 2026-08-14T01:35Z. (a) would have been a false green with a concrete downstream victim: FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT carries `depends:["FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT"]`, and `deps_satisfied()` accepts only DONE_VERIFIED — signing off would have unblocked it on a contract that is 0/4 implemented. (b) is wrong because nothing is blocking; the children were simply undispatchable.

The real defect was that the row had NO READER. Parked in `done[]` it is invisible to every picker; the one lane that does scan `done[]` is structurally dead (STEP po-2). So: populated `children[]` with the 4 ids and moved it `done[] -> ready[]`. That combination is load-bearing, not cosmetic — `scripts/devteam-wrapper-autoclose.jq`'s candidate set is `ready[] UNION in_progress[]`, never `backlog[]`, and it fires on `is_epic_wrapper` (non-empty children) + `all_children_terminal`. So the row now closes itself out the moment the 4 children land, and BOUNDED-1's EPIC-WRAPPER GATE simultaneously guarantees it is never auto-claimed as atomic work.

**Children routing:** all 4 carried NO `next_agent` at all — that is why they sat 10 days. Their `zone` values are bare labels (`cowork-team`, `docs`, `unified-agent`, `alert-commander`), not `apps/<service>/` paths, so zone-detect's Tier-3 fallback would have mis-routed them to the generic `developer` placeholder. Every file they touch is under `docs/agents/` or `.claude/skills/` — agent-father's exclusive commit zone (docs/agents/agent-father/init.md:63). Stamped `next_agent=owner=agent-father` on all 4. One exception recorded on the rows rather than hidden: FIX-SIGNALQUEUE-PUSH-SPAWN-FANOUT-MECHANISM also touches `docs/data/cowork-schedule.json`, outside that grant.

Dropped the orphaned `secondary_claimed_*` stamps from the parent — this triage IS their resolution. Deliberately left the two OTHER stranded stamps in `done[]` untouched as live evidence for STEP po-2's row, which owns them via its AC-5.

## STEP po-2 — [task_id: FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY] Filed, not fixed — and not folded into the nearest neighbour

**what-considered:** (a) promote FIX-DONELANE-DEVTEAM-FLOWDOC-PROSE-SYNC (ready[], agent-father, same file, same section, already asks for a clarifying sentence there); (b) fold into FIX-DONELANE-SECONDARY-DRAIN-BLIND-TO-ACTIVE-SPRINTS-NESTED-DONE-TASKS; (c) mint a distinct P1 row.

**why-change:** Chose (c). (a) is the closest neighbour and looks like the dedup target, but it EXCLUDES this defect in its own words — "Non-blocking documentation/audit-trail sync ... zero bash/call-site changes in main.md, so the mechanism is fully live the instant that row lands." That premise is exactly what is false: the call site IS a bash block in main.md and it IS `review[]`-only, so the mechanism has never been live for review[] rows at all. Folding a P1 liveness bug into a P2 prose-sync would very likely have shipped as prose only. (b) is a SCRIPT-side gap (candidate set omits `active_sprints[].tasks[]`) and would not touch the caller. Both ids resolved live before the argument was written; so did the three cited precedents (FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND, FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE, FIX-DEVTEAM-WF1D-REVIEW-QA-LANE-HEAD-PIN-BLIND — all DONE_VERIFIED in archive/2026-08.json; note the spawn brief's "FIX-DEVTEAM-PIPELINE-RESUME-GATES-OMIT-READY-LANE" does NOT resolve, the real id has no PIPELINE- segment).

Re-measured everything at source rather than inheriting it: 36 eligible SECONDARY candidates (16 review[] REVIEW + 20 done[] DONE), ZERO review[] rows ever stamped, all 3 stamps done[]-origin, the 8 oldest candidates all done[]-origin. Added AC-5 so the two stranded stamps get re-driven, and AC-6 so "stamped but not dispatched" stops being byte-identical to "nothing eligible" — the silence is what let this run 11 days.

## STEP po-3 — [task_id: FIX-ORCHAPPLY-CAS-BASELINE-CAPTURED-AFTER-CALLER-JQ-READ] Acted on the CORRECTION envelope, not on the three qa notebooks it corrects

**what-considered:** (a) clear envelope 68531dca with the rest and log it; (b) mint the row the 3 qa sessions recommended; (c) mint the 2 rows the CORRECTION actually asks for.

**why-change:** Chose (c). (a) would have destroyed an explicit ACTION REQUESTED with no durable artifact — the exact silent-loss the CLEAR contract warns about. (b) is the trap the envelope exists to prevent: three independent qa sessions concluded the QA-Drain PRIMARY claim "never actuated" and recommended a dispatcher-side actuation row; their test ("no commit moves these ids into qa[]") cannot distinguish "never actuated" from "actuated, then reverted before commit", and the claim DID actuate. I minted no such row. Verified both root causes at source before minting: scripts/orch-apply.sh:116 captures MTIME_BEFORE at orch-apply start, after the caller's jq read (RC-1); main.md's qa-drain-headdecoupled pipe ends `|| true` with no retry while orch-apply.sh:68 documents exit 2 as "caller should retry" (RC-2). Deliberately did NOT dep-gate RC-2 on RC-1 — dropping `|| true` is independently correct today. AC-5 on RC-1 forbids reporting it fixed on a green run alone, because green IS this bug's symptom-free state.

## STEP po-4 — [task_id: FIX-TRIAGESIGNALS-SYSTEMISSUE-UNDERSCORE-FORM-UNROUTED] Minted a duplicate, caught it, corrected it net-zero

**what-considered:** after minting FIX-TRIAGE-SIGNALS-PIPELINE-A-UNROUTED-TYPES covering all 3 held types: (a) leave it; (b) delete it outright; (c) delete + re-mint scoped to the one genuinely uncovered type, in a single write.

**why-change:** Chose (c). While reading a prior tick's journal for the file-naming convention I found it cited FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS — a live P0 agent-father row already owning `recurring-bug` + `sprint_registry_dangling_ids`, and the row driving the current CI red. My dedup sweep missed it because the search regex keyed on SIGNAL-TYPE-ROUTING-GAP / routing-gap and that id contains neither token. **Lesson: id-token dedup is not dedup.** Only `system_issue` (underscore) was genuinely uncovered. (b) alone would leave a −1 task_total delta against the conservation floor for no benefit; (c) is net-zero by construction. Added a compact pointer field to the P0 row so the two land in one agent-father hop.

## STEP po-5 — [ambient] Inbox CLEAR: 32 of 36, and what the 4 holds cost

**what-considered:** (a) clear all 36; (b) clear 32, hold the 4 whose types have no Pipeline-A routing row.

**why-change:** Chose (b). Verified each type by grepping the routing table directly and by hand-replaying the guard's own read-only extractors (`pipeline_a_section` + `extract_type_column`) — the guard script itself was never executed, per its own MEASUREMENT WARNING: it is not read-only (mints via ORCH_APPLY_LIVE_FILE_OVERRIDE) and `--check` is an accepted-and-ignored no-op alias, not a dry-run. ROUTED_A=28; 10 of the inbox's 13 types route. `auto-push-abort` now has a real row at line 43 and was cleared (5 envelopes) — the earlier hold on it is correctly retired. `system-issue` hyphen routes at line 39 and was cleared; `system_issue` underscore does not and was held: its only appearance is a stats-table data row at line 72 inside the `**CORRECTION` block that the guard's parser skips BY CONSTRUCTION, with an in-file comment naming this exact false-pass risk. Held: 2x recurring-bug, 1x sprint_registry_dangling_ids, 1x system_issue. Their content was fully triaged; only inbox removal is withheld, so the guard stays honestly red.

**Folds, zero re-mints:** `auto-push-abort` x5 — re-measured the premise instead of trusting the envelopes' ahead snapshots (49/58/73/82/92, all stale): live `git rev-list --count origin/main..HEAD`=95, behind=0, and fleet-push.log shows the same gate on all 4 recent attempts (tsc passes, size-lint FAILs on pushBctcLayoutHandler.ts 252L vs upper 250L). Folded into FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH (P0, dedup_key auto-push-abort:push-fail); backlog has grown 46 -> 95 since that row was minted. `bug-escalation` x9 — 6 are `[notebook-immutability-guard]` (no `escalated=` key by construction, high FP base rate, root cause already tracked on FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS and FIX-AUDITOR-NOTEBOOK-SECOND-COMMIT-RETRO-REWRITES-PUBLISHED-OUTPUT-CONTRACT) -> volume tracking only; 1 is `[sweep-guard] BARE commit` with `escalated=true prior_warns=10` -> occurrence bump on FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS (27 -> 28); 2 are the dev-team defect report and CORRECTION handled in STEP po-2/po-3. That occurrence bump had to be count-only: the row is already 11968B against ORCH_ROW_PROSE_CEILING_BYTES=12000, and my first attempt at a full fold note aborted the whole write at 12552B. `notebook_*` x10 -> folded onto CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION (ba.md was covered by no row, added) with the design root on FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE. `context_bloat_breach` x1 -> DEFER, its file is sprint COWORK-GUARANTEED-SLOT-CATCHUP's own live journal and that sprint is `active`. `bctc_signal` x4 -> skip per the routing row. `cowork-fire` x1 -> `errors:[]`, `silent:false`, classification FIRE, spawned clean -> log only. `esc-deep-dive-request` x1 (ESC-5 / FPT / Q1-2026) -> REFLOW-FPT-Q1-2026 and FIX-ESC-KNOWNROOT-SUPPRESSION-CONSUMER-SIDE-ONLY are both already open on that subject -> ack, no dispatch.
