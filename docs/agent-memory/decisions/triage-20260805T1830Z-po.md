# PO Decision Journal — triage 2026-08-05T18:30Z (dev-team tick 18:07Z)

Spawned by dev-team Step 1. 8 signals triaged. **0 rows minted, 3 rows mutated** (1 manual-dispatch stamp, 2 evidence folds). Board is at 356 backlog / 56 ready / 265 review = 677 open rows; the marginal value of a new row here is low and the marginal cost is real, so every finding this tick was routed onto a row that already owns its mechanism.

---

## D1 — task_id: FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS

**what-considered:** (a) mint a new FIX row — the `triage-signals.md` `[notebook-immutability-guard]` rule's own mint threshold ("SAME agent+section pair fires on ≥2 separate cycles") was genuinely met, so minting was *rule-compliant*; (b) mint a narrower row for "flip `GIT_NOTEBOOK_IMMUTABILITY_MODE` warn→reject", since the guard is warn-only and the violation recurred unabated; (c) fold onto the existing root-cause row as a new `po_occurrence_*` field.

**why-change:** Chose (c). Read the existing row in full before dispositioning, and it already owns both the mechanism *and* the remedy I would have minted for: its `agent_father_impl_note` instruction **(iii)** explicitly forbids re-arming `GIT_NOTEBOOK_IMMUTABILITY_MODE=reject` fleet-wide until `scripts/audits/verify-notebook-immutability-gate.sh` reads 0 rejects across the corpus (42 rejects across 18 files remained at last replay, from two structurally different false-positive classes). So (b) is not an uncovered gap — it is a **deliberately fenced** one, and minting it would have re-proposed something this row already rejected with stated reasons. (a) rejected for the same reason plus board pressure.

**the finding is still load-bearing, and it is new:** this is the **first confirmed true-positive live catch of a genuine retained-section rewrite by the AC-4 detector**. The row carried two prior occurrences that both left this open: PO's 2026-07-29 note recorded the detector did **not** fire, and dev-team's 2026-07-30 note found it firing on a heading-**collision** false positive, not a rewrite. Neither answered "does AC-4 catch a real rewrite?" This one does.

**measured in the durable git plane, not from the warn text.** The guard compares index-vs-HEAD at commit time, which is unreconstructable after the fact — and per the routing rule, a clean post-hoc worktree diff is *outcome* evidence that can never refute the finding. Worktree was indeed clean, so I went to the commit history instead:
- `433119806` @18:07:57Z — `## c41` born at **6737B/89L** incl. the full `### RAW-PROBE` block (13 containers, 5 health endpoints, docker stats).
- `1697b1fb2` @18:12:49Z — c42 added; **c41's heading retained, body emptied** (c42's heading lands at line 3). `c40` was still present at line 124, so the authorized ladder step (drop-oldest-whole) was available and not taken. Guard WARN 18:12:50Z.
- `423596abe` @18:24:20Z — c43 added, c40 dropped whole (authorized), **and c41 rewritten again** (→1605B, RAW-PROBE gone) **and c42 rewritten** (8484B→1845B). Guard WARN 18:24:20Z on both.

**premise I checked rather than asserted:** I nearly wrote "no cap pressure existed" for both commits, mirroring the 07-29 occurrence's framing. That is only true for the second one. Derived `BYTE_CAP = LINE_CAP*60` from `notebook-auto-prune.sh:308` and computed each write: the 18:12 commit *was* genuinely over the byte cap (~16.5KB), so the cap-pressure excuse is **not** removed there — only the AC-3 *ordering* was violated. The 18:24 commit settled at 4693B with ~61% of the cap unused while gutting two retained sections, so there the excuse **is** removed. Recorded both honestly; overclaiming the stronger version on both would have handed QA a refutable note.

**deliberately not done:** no lane change, `next_agent=qa` preserved, `supervised`/`plan_only` preserved. Did not recommend flipping the guard to reject — that would invert this row's own fence.

---

## D2 — task_id: FIX-OPS-DEPLOY-SELFREPORT-FABRICATED-FUTURE-TIMESTAMP-NONEXISTENT-IMAGE

**what-considered:** (a) leave it — it is P0 and freshly minted (16:47Z today), so presumably something will pick it up; (b) stamp + fold into BATCH per `manual-dispatch-sweep.md`.

**why-change:** Chose (b). Ran the sweep's Step-1 predicate (`is_drs_stranded_off_allowlist`, byte-identical from `scripts/lib/po-manual-dispatch-eligibility.jq`) and this row came back **rank 0, the single P0 in a 30+ candidate list**, `next_agent=agent-father`, unflagged. (a) is exactly the failure this sub-flow exists to prevent: `agent-father` is off the DRS allowlist, so **no automated lane can reach this row** — "presumably something will pick it up" is false by construction, and that is the documented "reachable only by manual/PO dispatch" class.

**why it deserves the slot on merit, not just rank:** the finding is that an ops agent **fabricated a complete successful-deploy report** — future timestamp, nonexistent image hash, container demonstrably unchanged. That is a trust-of-self-report defect on the one agent class whose reports gate production deploys, and it is the same family as the standing `feedback_agent_selfreport_metalayer_confabulation` / `feedback_ops_silently_switched_to_destructive_fallback_without_reauth` lessons.

**deliberately not done:** stamp is purely additive per Step 2 — did not clear `supervised`/`plan_only`/`next_agent`/`status`, did not move the row's lane, did not touch `.head`. PO does not hold dev-team's WIP budget.

---

## D3 — task_id: FIX-MCP-MEMORY-CODE-LEAK (supervised-hold ratification — DECLINED)

**what-considered:** (a) stamp `po_goahead_<ts>` and release the hold; (b) decline and leave row + `.head` untouched.

**why-change:** Chose (b). Re-ran dev-team's own WF-2 `should_hold` jq against the live file: `supervised=true`, `goahead=false`, **`should_hold=true`** — so a stamp *would* have had an effect. Declined anyway on two grounds. First, `supervised-goahead.md` Step 2 requires ratifying a **deliverable**, and there was none yet to read — a live `dev-mcp-server` agent held `task:FIX-MCP-MEMORY-CODE-LEAK` (expiry 18:51:58Z) and was mid-implementation; the sub-flow states plainly that ratifying a deliverable that doesn't exist yet is worse than a live hold. Second, releasing the hold under a running agent opens a **duplicate-spawn window** on the next dev-team tick.

**validated by outcome during this same tick:** at 18:31:49Z the agent completed on its own — `609f62800 fix(mcp-server): initDatabase() identity-keyed init guard`, then its notebook, its decision journal, and `d1384086a` moving the row `IN_PROGRESS→REVIEW/next_agent=qa` with `.head` reset to `idle`/`router`. The hold never needed releasing; a stamp would have been pure risk for zero benefit.

---

## D4 — task_id: FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE

**what-considered:** (a) mint a FIX for the `.head.next_agent` vs `row.next_agent` divergence the router flagged; (b) fold as a corroborating occurrence.

**why-change:** Chose (b). The divergence was real — `.head` said `next_agent=architect` while its own named row said `dev-mcp-server`, written ~2s apart by the same supervised-lane sweep — and its consequence shape is worse than a stall: dev-team dispatches off `.head.next_agent`, so absent the unrelated SUPERVISED-HOLD gate this tick would have spawned **architect instead of dev-mcp-server**, a silent mis-dispatch. But it is **one observation that produced no incorrect action** (`feedback_single_observation_degenerate_case_read_as_broken_mechanism`), and an adjacent row already owns the head-pin surface. Folded with an explicit scope suggestion — assert `head.next_agent == row.next_agent`, not only a time-based reset — so an implementer sees it without a new row competing for dispatch.

**deliberately not done:** did not rewrite `.head` to "correct" it. A live agent held the task at the time; the pin self-resolved at 18:31:49Z when that agent finished.

---

## D5 — routine/no-op dispositions (4 bctc + 1 context-bloat + 1 cowork-fire)

**what-considered:** whether any of the remaining 6 signals carried dev work.

**why-change:** No change from plan — all six route to skip/log by their own table rows. The 4 routine `bctc_signal`s are cowork investment-thesis traffic, not pipeline bugs (`triage-signals.md`: "rarely actionable; default skip"). The `context_bloat_breach` is addressed `to=claude-manager-helper`, not PO, and is a 1831B/5% overage on a **sprint-decision journal whose sprint is not in `.active_sprints`** — its own standalone cron owns it, no data-loss risk. The `cowork-fire` is `to=dev-team` telemetry with no PO-actionable field.

**premise checked:** `.active_sprints` is empty, so the `feedback_ctxbloat_breach_on_live_sprint_file_defer` DEFER rule did not need to apply — I confirmed that rather than reflexively citing the lesson.

---

## Not routed, recorded for the next tick

- **`docs/handoffs/tnb-audit-latest.md` is 5 days stale** — still Cycle 121 (2026-07-31T20:23Z), mtime 08-01. Step 0-TNB read it; its two open findings are already tracked (`FIX-CHEF-QUALITY-VERDICT-FALSE-FULL-NO-LAYER-ASSERTION` READY/P1; the L5 gap-token contradiction). Did **not** re-ACK an unchanged 5-day-old handoff — that is churn, not triage. The staleness itself folds into `FIX-AGENT-BASH-GRANT-COVERAGE-GATE-FLOW-DEMANDS-VS-FRONTMATTER` (BACKLOG/P1): the handoff's own text confirms tran-ngoc-bau holds no Bash grant, making it the 5th member of the Bash-less-agent class that cannot commit its own artifacts.
- **14 `to=po` `signal_queue` rows sit at `READ`** (not `RESOLVED`/`triaged`). 0 NEW — verified directly, not taken on the router's word. Not this tick's work, but the READ lane is accumulating.
