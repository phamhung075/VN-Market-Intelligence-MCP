# Decision Journal — Sprint SPIKE-BCTC-TABLEROWS-FROZEN-HOLLOW-DONE · developer

**Started:** 2026-07-12T06:01Z
**Agent:** developer

---

## DEV-SPIKE-BCTC-TABLEROWS-FROZEN-HOLLOW-DONE [task_id: SPIKE-BCTC-TABLEROWS-FROZEN-HOLLOW-DONE]

- **what-considered:** (a) `bctcExtractReconcileJob`'s success check is a code defect (should require `bctc_table_rows` specifically, not accept `bctc_layout_units` alone); (b) a derivation/parser step from `bctc_layout_units.stitched_markdown` → `bctc_table_rows` exists somewhere but is silently failing/uncalled; (c) `bctc_table_rows` is produced by an entirely separate pipeline (BCTC-AGENTIC-REFINE) that has its own, independent, dormant trigger — this SPIKE's "hollow-done" symptom is a downstream masking effect, not the primary defect.
- **why-change (evidence chain):** RAW-verified live named-volume `market.db` (docker exec, not endpoint): `bctc_layout_units` MAX=2026-07-12 06:02 (growing, 1829 rows), `bctc_table_rows` MAX=2026-07-08 12:35 (frozen), `bctc_md_tables` MAX=2026-05-26 (dead, 1 row), `bctc_refined_units` MAX=2026-07-04 (frozen 8d, OLDER than table_rows freeze — proves the refine pipeline stopped before table_rows did), `financial_reports.refine_status` breakdown shows 151 PENDING vs 8 DONE/7 PARTIAL. Read `bctcExtractReconcileJob.ts` in full — its 3-table OR success check is a verbatim implementation of R-CRIT-2 in the architect's own `TASK_FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION.md` D3 design (QA-approved, commit `43f4c8a22`) — not a bug. Read `pushBctcLayoutHandler.ts` doc comment + the originating `2026-05-26-bctc-layout-first-pipeline.md` architecture brief — `bctc_layout_units` was explicitly scoped as a "geometric zone review overlay" (QA/pass-rate diagnostic), never as a `bctc_table_rows` source; grep-confirmed zero derivation consumer of `stitched_markdown` anywhere in `apps/mcp-server/`/`apps/pdf-extractor/` besides the human-review overlay handler. Read `bctcRefineJob.ts` — the ONLY live producer of `bctc_table_rows` (`refineOneReport` Phase 4) — its module doc confirms "Option-Y": the in-container automated trigger was deliberately DELETED; production dispatch depends solely on a session-scoped Claude-native `CronCreate` cron (`.claude/commands/crons/cron-refine-bctc.md`). Cross-referenced board history: `REFINE-CRON-ARM` (opened 2026-06-12, purpose = durably arm this exact cron) is still `status:TODO` 30 days later; sprint `BCTC-REFINE-STALL-RETRIGGER` (opened 2026-06-27) already diagnosed the identical root cause ("(a) cowork CronCreate not re-armed after session restart") and shipped throughput mitigations (T1/T2) but never the durable fix (T3-WATCHDOG still BACKLOG); `cowork-schedule.json`'s T2-added refine slots (commit `19764c0e2`) are no longer present in the live file today (0 `bctc` references, 23 slots total).
- **path chosen:** (c), confirmed by (a) being explicitly-designed/QA-approved behavior and (b) never having existed by design. Root cause is the F4/F5 chain in the findings doc: `bctc_table_rows`'s sole producer (BCTC-AGENTIC-REFINE) depends on a fragile session-scoped trigger that has gone dormant for a second/third time via an already-diagnosed defect class, with no durable backstop and no automated staleness watchdog ever built. Filed follow-on `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP` (backlog, high, owner=ops) rather than fixing inline — durable-cron-backstop design is genuinely infra-scoped work, not a small in-place patch, and out of SPIKE-mode's read-only mandate.
- **BUILD-STANDARD:** not-applicable (read-only diagnostic SPIKE, no code changed, no branch merged — throwaway `spike/bctc-tablerows-frozen-hollow-done` created and deleted per protocol).
- **brief:** `docs/spikes/SPIKE-BCTC-TABLEROWS-FROZEN-HOLLOW-DONE.md`
- **board:** flipped `task_board.in_progress[]` → `task_board.review[]` (status REVIEW, next_agent=router) via `orch-apply.sh`; added `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP` to `task_board.backlog[]` in the same atomic write (conservation check: task_total 452→453, +1 net new row, PASS).
