# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa

**Sprint goal:** Make cowork `guaranteed:true` an honored contract — bounded catch-up firing for elapsed guaranteed slots, or a structured (non-silent) miss.
**Agent:** qa
**Started:** 2026-08-14T12:58:53Z
**Continuation of:** sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md (CAP-REACHED 2026-08-14T09:34:30Z, 600L line cap)

---

### STEP qa-S126 · qa · 2026-08-14T12:58:53Z
**task-id:** FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK
**what-done:** Direct-Commit Verify (2nd pass; row stuck in qa[] since 2026-08-11 because 1st QA pass appended review-record prose but never lane-moved). Re-verified commits `e57c4b669` (AC-1/AC-2) + `4fbb3eb0d` (AC-3 docker-runbook + dev-standards fix + persisted script) at source.
**what-considered:**
- Both on main ancestry; `git show --stat` matches all 6 claimed files. Read live text of execute-tier.md:94, developer/main.md:102/104, dev-frontend/main.md:99/101, docker-runbook.md:148-153, commit-convention.md:38/108, dev-standards.md:1586/1598 — all now mandate trailing `-- <paths>`, none states the bare form as INVARIANT (PO's explicit 2026-08-14T04:11Z closure condition, discharged).
- Ran persisted `scripts/verify-fleet-commit-pathspec.sh`: 2 FAIL (dev-standards.md:909,1586) — but content-diffed against the original 4fbb3eb0d allowlist (lines 805/1465) and confirmed byte-identical prose, only line-shifted by unrelated later commits (9647c9c14 CANONICAL-block edit etc). Manually re-grepped full 64-hit corpus: zero unresolved live bare-commit-mandate sites.
**why-decision:** vc-approved, DONE_VERIFIED. Substantive fix (the actual policy/hook contradiction) verified correct at source; the script FAIL is an allowlist line-number fragility bug in the persisted proof, not a regression of this task's contradiction. Holding the row further re-costs throughput PO already flagged 27x (router sessions hard-blocked, prior_warns up to 60).
**why-change:** flagging script fragility (line-number-keyed allowlist drifts on any unrelated doc edit) as a lightweight follow-up in the review record — not in this task's own AC scope, not blocking.

### STEP qa-S127 · qa · 2026-08-14T13:00:00Z
**task-id:** FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH
**what-done:** Direct-Commit Verify. Re-verified commit `1b9425d6f` (fix+test) + `b1a05ab56` (notebook/DJ) at source, both on main ancestry, `git show --stat` matches sole claimed file. Own `bun test` on new regression file: 2/2 pass. Targeted 8-file OCR suite (dev's own file list, corrected my own typo'd BPE-DEV-3 filename): 89 pass/1 skip/0 fail, 90 tests — exact match to dev's 89/89 claim. `tsc --noEmit` clean, `mock-guard.sh` PASS on prod file, DDD/secret greps clean.
**what-considered:**
- Read diff at source: threshold now `Math.max(Math.min(expectedPages, OCR_MAX_PAGES)*0.5, 3)` (AC1 correct), blanket upfront DELETE removed in favor of existing per-page UPSERT + a post-loop prune that only deletes `page_number > maxPages` AFTER the write loop (AC2 write-before-delete correct).
- AC4 (live 3-file boot verify) was marked "NOT ATTEMPTED" by dev-mcp-server (honest — no restart in their session). Container `vn-market-intelligence-mcp-mcp-server-1` started 2026-08-13T19:18:35Z (after the fix commit), running fixed source (grep-confirmed `OCR_MAX_PAGES`/`cappedExpectedPages` present in-container). Queried live `market.db` via `bun -e` (bun:sqlite, readonly): all 3 named files stable at exactly 80 rows (maxp=80). Grepped container boot-window logs: all 3 fired only `"[pdfOcr] already extracted"` with `cappedExpected:80`, zero `"starting"`/`"incomplete extraction detected"`/DELETE entries — AC4 independently satisfied, going beyond the dev's own honest non-claim.
**why-decision:** vc-approved, DONE_VERIFIED. All 4 ACs verified true (AC1-3 at source+own test run, AC4 via live container/DB evidence QA obtained directly, not trusted from dev prose).
**why-change:** none — verdict matches expected outcome, no plan deviation.

### STEP qa-S128 · qa · 2026-08-14T12:59:12Z
**task-id:** UNBLOCK-OPS-RAG-REBUILD-DONEVERIFIED-FALSIFIED-BY-KERNEL
**what-done:** Direct-Commit Verify (board-hygiene row, no `.commit`/branch field). Derived write commit via `git log --oneline --all -- docs/data/orch/orch-state.json docs/data/orch/archive/2026-08.json` cross-checked against `completed_at` 2026-08-14T06:58:58Z: commit `3220f31bc` (committer-date 07:02:21Z, ~3min after completed_at). Confirmed on main ancestry; `git show --stat` matches exactly the row's own `files[]` (orch-state.json + archive/2026-08.json), no other production files touched.
**what-considered:**
- Re-ran all 4 checks the row's own status_note prescribed under "QA VERIFY", not trusted at face value: (a) `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` in hot `review[]` status == "BLOCKED" — confirmed; (b) archive `.done_tasks[]` index for that id == null AND `.restored_to_hot[0].id` == same id — confirmed; (c) row carries no un-prefixed `qa_verified_at` (only `_RETRACTED` suffix) — confirmed; (d) both files present in commit 3220f31bc's diff — confirmed, and read the diff at source (not stat alone): full OPS-RAG block deleted from archive `.done_tasks[]`, de-falsified BLOCKED version with FALSIFIED/RETRACTED banners + blocked_reason/verify_note/blocked_by/superseded_by present verbatim in orch-state.json.
- Original DONE_VERIFIED postmortem text retained verbatim below the FALSIFIED banner (no silent deletion); AC-1/AC-2 legs explicitly not disputed, only AC-3 (durability) leg retracted — matches the cited kernel dmesg evidence (3x OOM-kill at +1h00m/+1h14m/+20h34m past the 2026-08-12T12:46:40Z certification close). Pure board-data write (JSON only) — no production source touched, so `bun test`/`tsc`/`mock-guard`/DDD/security are correctly N/A (both files re-validated `jq empty` clean). `.head.active_task_id` is a different row (FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS, same QA-Drain batch) — left untouched.
**why-decision:** vc-approved, DONE_VERIFIED. All 4 row-prescribed checks independently re-verified true at source; commit content matches board state exactly; no ISSUE found.
**why-change:** none — verdict matches expected outcome, no plan deviation.

### STEP qa-S129 · qa · 2026-08-14T13:01:20Z
**task-id:** FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H
**what-done:** Direct-Commit Verify. Derived commit via `git log --all -- <new test file named in status_note>`: `88863a82d`, confirmed on main ancestry. `git show --stat` matches all 4 files the review_note claims (freshnessSlaChecker.ts, freshnessSlaMonitorJob.ts, new FIX test, 1920i-freshness-sla-extension.test.ts). Own re-run: new test file 8/8 pass (AC-1 bidirectional 20d-ok/40d-breach via checkSignalSla AND full checkDataFreshnessSla pipeline, config pin 43200min, fixed-not-calendar check, boundary 43200/43201, anti-false-green pinning real 76067min figure still CRITICAL); sibling 1920i suite 23/23 pass (no regression from TC-6f edit); `bun tsc --noEmit` 0 errors; `mock-guard.sh` PASS on both touched production files.
**what-considered:**
- Read diff at source (not trusted from review_note prose alone): `DEFAULT_SLA_CONFIG` signal_quality_audit.defaultThresholdMinutes 2880→43200 confirmed exactly; both misleading "tracks the monthly cron" comments corrected in-place in freshnessSlaChecker.ts + freshnessSlaMonitorJob.ts to state the real writer path (post_agent_signal price_confirmation/urgent_news+confidence via insertSignalQualityAudit, single call site).
- AC-3 (row explicitly required: file a follow-up rather than silently leave the confirmed-missed-July-cron defect unaddressed) — independently confirmed BOTH resulting rows live on the current board, not narration-only: `FIX-MONTHLYSIGNALQUALITYAUDITJOB-MISSED-JULY-RECOVER-GUARD` (backlog, P2, dev-mcp-server) and a PO-filed residual `FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT` (currently itself in qa[], P2, dev-mcp-server, different QA cycle).
- `.head.active_task_id` is a different row in the same QA-Drain batch (FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS) — left untouched per flow's conditional head-reset rule.
**why-decision:** vc-approved, DONE_VERIFIED. AC-1 (fixture bidirectional), AC-2 (confirm/refute at source — commit confirms genuinely-missed cron via idx_cron_job_runs_job_started workaround), AC-3 (follow-up filed and verified present) all satisfied; no ISSUE found.
**why-change:** none — verdict matches expected outcome, no plan deviation.

### STEP qa-S130 · qa · 2026-08-14T13:03:31Z
**task-id:** FIX-PO-TRIAGE-SIGNALS-CIRED-TEMPLATE-STATUS-TODO-REJECTED-BY-VALIDATOR
**what-done:** Direct-Commit Verify. Row's stored `.commit_sha` (`b164e37781f01...`) FAILED check-3 (real commit, main ancestry, but touches zero of the claimed files — an unrelated agent-father notebook-merge commit, same-cycle sibling per its own message). Derived the real commit via `git log --all --grep=<task-id>` (exactly 1 hit): `c1c97e340`, on main ancestry, `git show --stat` touches exactly all 3 claimed po/flow/*.md + new regression script.
**what-considered:**
- The prior dev-team review_note's own cross-check ("cb6ba9567 cherry-picked, sha above post-merge, confirmed, diff matches exactly") is unverifiable as stated against the stored hash — `cb6ba9567` itself is a parallel-worktree duplicate NOT on main ancestry; only `c1c97e340` actually landed. Corrected `.commit_sha` on the write rather than bouncing to owner for a "new commit" that isn't needed — the AC is genuinely satisfied on main already, only the row's pointer was stale/wrong.
- Live-verified on main HEAD directly, not trusting any single commit's diff: zero `status: "TODO"` tokens remain across all 15 `docs/agents/po/flow/*.md`; every `backlog[]` mint append uses `status: "BACKLOG"`; `orchStateSchema.ts` `LANE_ALLOWED_STATUSES.backlog` confirmed unchanged `{BACKLOG, BLOCKED}`. Re-ran the fix's own regression harness fresh: `scripts/audits/po-triage-mint-backlog-status-lane-coherence-verify.sh` 42/42 PASS, real live orch-state.json confirmed untouched by that run.
- No production TS/JS touched (pure prose + 1 new bash audit script) — `bun test`/`tsc`/`mock-guard`/DDD/security correctly N/A; the purpose-built script IS the targeted regression proof for this template-vs-schema artifact class.
**why-decision:** vc-approved, DONE_VERIFIED (with commit_sha correction persisted). AC fully satisfied on main via the real commit; blocking the row on a metadata-only pointer error would be pure wasted round-trip (no code change actually needed).
**why-change:** flagged the prior review_note's unverified-as-stated cross-check for fleet visibility (peer verification-discipline signal), not re-litigating — the underlying work stands.

### STEP qa-S131 · qa · 2026-08-14T13:08:00Z
**task-id:** FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT
**what-done:** Direct-Commit Verify. Row carried no `.commit`/`.files[]` — derived `43e0d7ddc` via `-G` pickaxe + review_started_at cross-check, on main ancestry, `git show --stat` matches all 5 production files + new test + 2 docs. `bun test` new file 13/13; sibling suite (1920f/1295c/1920i/1352c/1354b/FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-.../233-cowork-resilience-e2e) 73/73+28/28; `tsc --noEmit` 0 errors; `mock-guard.sh` PASS on 6 files.
**what-considered:**
- Row's own `po_verification_gate_20260730T2148` requires live BEHAVIOUR + NEGATIVE CONTROL, not source/unit-test trust — deploy leg was already RAW-verified 2026-07-31 (`rebuild_verification` field) but items (2)/(3) had no recorded evidence since. Queried live bind-mounted `data/live/market.db` directly: `signal_quality_audit` now 58 rows (was 6), most recent today 04:10:09Z, confidence values varying (70/80/90/100) — continuous real production writes since deploy, stronger than a synthetic single-post proof.
- NEGATIVE CONTROL derived independently: of 9 post-deploy price_confirmation/urgent_news `agent_signals` rows, exactly 2 (freshness-sla-monitor's own empty-`finding_data` urgent_news alerts) carry neither `confidence` nor `regime_adjusted_score` — zero matching `signal_quality_audit` rows for those 2 signal_ids, confirming no fabricated write.
- `233-cowork-resilience-e2e.test.ts:393` (AC's own named reconciliation target) is an `expect(true).toBe(true)` placeholder with no real table assertion — reconciles cleanly, not a stale false-pass. Flagged non-blocking: `news-scout.md:73` still repeats the commit's own inflated "9/9 regime_adjusted_score" figure a same-day peer RAW-verify (`223e3d4dd`) already found was really max 3/10 — decisive AC-1 finding (0 rows carried `confidence`) unaffected, not re-litigating a peer-flagged non-blocking miscount.
**why-decision:** vc-approved, DONE_VERIFIED. All 5 AC satisfied: mechanism stated, WIRE decision explicit+doc-aligned, swallows now `logger.warn`→`system_logs`-persisted (confirmed at source), bidirectional live proof obtained directly (not trusted from prose), regression test pins the decision.
**why-change:** none — verdict matches expected outcome; obtained the live BEHAVIOUR/NEGATIVE-CONTROL evidence the row's own gate demanded, which prior REVIEW rounds had not recorded.

### STEP qa-S132 · qa · 2026-08-14T13:20:00Z
**task-id:** FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS
**what-done:** Direct-Commit Verify on commit `0eb733b577a`. Code review PASS (independently re-verified in a clean `docker run` against the deployed base image, never trusted prose): httpx2 dep fix genuinely resolves the 2026-08-12 CHANGES_REQUESTED (12 TestClient failures → 0), 14 new tests pass incl. a real no-mock traffic replay, mock-guard/size-lint/import-linter all clean. Corrected `files[]` (stale since mint, disclosed once already 2026-08-12, never fixed).
**what-considered:**
- Test-count claim "215/215" vs reproduced 214/215 — isolated the 1 failure against the immediate parent commit in a scratch copy (untouched by this diff): identical failure present, so pre-existing async-lock race, not a regression, not in scope. mypy claim "no new error categories" vs diffed baseline: 1 genuinely new hit (`self._session`, same tolerated bare-None-attribute pattern as 17 other pre-existing hits in the file). Neither blocks — both are prose-precision corrections, not defects.
- The decisive question was NOT code quality but whether this row's own AC is met: PO ruling `po_RULING_CRITICAL_PATH_20260814T0927Z` ties closure explicitly to RAG-MEM-DURABILITY-BAR v2 D1-D5 (>=12h plateau). Live-checked: running container predates this commit — fix committed, not deployed, durability window never started. This exact chain already has one premature done_verified→kernel-OOM-falsified precedent (OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX) — declined to repeat it.
**why-decision:** NOT vc-approved. Lane-moved qa[]→review[], status BLOCKED (schema-valid, matches the 2 sibling BLOCKED rows on this same incident), next_agent=po (matches sibling routing convention + PO's own "not another dev-rag-service cycle" instruction — this is not a rework request).
**why-change:** flow's literal vc-changes template defaults to status REVIEW + owner-field routing; deviated to BLOCKED/next_agent=po because the code did NOT fail review — only the row's own durability AC is outstanding, and blind owner-field routing would wrongly imply dev-rag-service needs to write more code.

### STEP qa-S133 · qa · 2026-08-14T14:27:31Z
**task-id:** FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT
**what-done:** Direct-Commit Verify on commit `f9e5113534a`. Confirmed real, on main ancestry, `git show --stat` matches claimed files (new `.sh`+`.test.sh` under `scripts/audits/`, `docs/WORK.md`). Ran everything myself: `.test.sh` 7/7 (matches claim exactly), live fleet-wide run over 15 `docs/agents/po/flow/*.md` files 13 pass/2 fail (triage-signals.md + triage-signals-longtail.md, same defect class, matches claim), shellcheck clean both files, `bash -n` clean, security greps (process.env/secret/password/token) clean, mock-guard PASS (bash scripts out of its TS/JS scan scope), no `apps/` touched so bun/tsc N/A confirmed via diff stat.
**what-considered:**
- Live violation count is 6 lines today vs the script header's own documented "8 instances at authoring time" — traced via `git log --follow` on triage-signals.md: 2 unrelated commits (Aug 9, Aug 14) touched that file after this task's Aug 6 commit, naturally shifting/reducing matches. Not a script defect — this is exactly what a regression verifier is supposed to keep re-measuring live; row's own AC never froze the count.
- `docs/agents/po/flow/main.md:173` still literally reads "spec, unimplemented" even though the script now exists — flagged as non-blocking doc staleness, not part of this task's stated AC trailer (script-implements-spec/fleet-wide/test-proves-detection/scoped-pass-on-4-subflows), all 4 of which are independently verified true.
**why-decision:** vc-approved, DONE_VERIFIED. All 4 AC-trailer items independently reproduced from a clean run, not trusted from commit prose.
**why-change:** none — verdict matches expected outcome.

### STEP qa-S134 · qa · 2026-08-14T18:05:00Z
**task-id:** FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE
**what-done:** Direct-Commit Verify on commit `d89392752`. Confirmed real, on main ancestry, `git show --stat` matches claimed 4 doc files exactly (all `.md`, zero code touched — bun/tsc/mock-guard N/A, confirmed via diff stat not assumption). Read full diff of all 4 files, not trusted from commit prose.
**what-considered:**
- Ran the landed jq route-derivation live: `.project.infrastructure.vps.routes[].source_id` over system-map.json returns exactly the 8 source_ids the doc cites verbatim, length=8. Ran the landed OH-3.2 fleetwide regex sweep live over the 3 named doc files: 0 hits, matches commit's own "0 hits post-landing (was 3)" claim.
- Independently verified the doc's embedded code claims at source (not trusted): `vpsPushLogStore.ts:13/97` — `VpsService` union includes `foreign-flow`, hardcoded services array excludes it (dead branch, exactly as documented). `vpsHealthPoller.ts:27-31` — 5-service list matches doc's service-plane column exactly. `vn-sbv-fetch` 35-min threshold (`:124`) confirmed, corroborates the B12-crossref's 30<35 argument.
- No live gateway binding in this sub-session (`mcp__gateway__call_tool` unavailable) — could not directly re-invoke `get_vps_proxy_health`/`get_vps_service_health` to replay AC(2)/(3) end-to-end; substituted source-level verification of every factual premise the verdict logic depends on (service lists, dead branch, thresholds), all confirmed accurate, plus full manual trace of the verdict table logic (no-coverage→WARN never PASS; any service-plane unhealthy→cycle can't PASS) — internally consistent and correctly closes the cited gap.
- AC(5) cross-reference: confirmed field `sbv_fx_b12_rootcause_crossref_20260808` present on the (now-archived) `FIX-AUDITOR-B12-DOUBLE-INVOKE-EMIT-MARKER-LOSS` row (`docs/data/orch/archive/2026-08.json:5761`), additive only, does not alter that row's already-approved closeout.
**why-decision:** vc-approved, DONE_VERIFIED. All 5 acceptance criteria independently confirmed: (1) jq expression + live output shown, no literal count remains (fleetwide grep 0 hits); (2) no-coverage routes (muasamcong/vietstock-agm-plan) FLAG WARN by construction, never PASS; (3) cycle-verdict rule explicitly blocks PASS on any service-plane unhealthy entry; (4) OH-3.2 Leg-1 counts `routes|length` directly, geo_blocked leg retired with documented rationale; (5) crossref present and additive.
**why-change:** none — verdict matches expected outcome; noted the missing gateway binding as a scope limitation on live-tool replay, compensated with source-level verification since the change is doc-only (no code path to unit-test).

### STEP qa-S135 · qa · 2026-08-14T14:30:32Z
**task-id:** FIX-AUDITOR-EMPTYTABLE-CHECK-NO-WRITER-DISCRIMINATOR
**what-done:** Direct-Commit Verify, commit `d16b7c212` (row predates drain, derived via `git log -- <files[]>` + `completed_at` cross-check 08:30:10Z vs 10:40Z; commit trailer itself carries `Task:`/`AC:` matching this id). Ancestry + `git show --stat` confirmed against all 5 `files[]` exactly. `db-empty-table-classify.test.sh` 40/40 live (incl. T7 negative-control + T11 live-DB replay). Independently re-ran classifier live per PO's `po_QA_GATE_WARNING` gate order (positive control FIRST): daily_ohlcv/market_prices->a/may_stay_critical (fires), THEN price_alerts->c, alert_engine_records->b, pdf_documents->SCHEMA-MISSING (FPs quiet) — matches AC exactly.
**what-considered:** PO's fail-open-probe concern (sidecar named-volume) — verified `db-empty-table-classify.sh` reads host-bind directly (own doc'd pattern, unaffected); renamed JSON keys (`*_count`) confirmed live in `db-integrity-counts.sh`, zero stale non-historical refs. `register.md` re-sync claim: content now lives behind an SSOT pointer in `register-job-db-integrity-*.md` (later same-day dedup fix), supersedes not breaks. bash -n clean 3 files, mock-guard PASS (bash outside its scan scope).
**why-decision:** vc-approved, DONE_VERIFIED — AC fully met, PO's gate-ordering requirement honored literally.
**why-change:** none.

### STEP qa-S135 · qa · 2026-08-14T14:29:03Z
**task-id:** FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS
**what-done:** Direct-Commit Verify on `876f07874` (code+tests) + `2477cfba7` (docs). Both real, on main ancestry, `git show --stat` matches claimed files (`ohlcvBackfillHandler.ts`, `ohlcvHistoryBackfillJob.ts` + new `ohlcvGapClassifier.ts` + 3 test files).
**what-considered:**
- Re-ran everything myself, not trusted from prose: 9 new tests (`FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS-{handler,scheduler}.test.ts`) 9/9 pass; `1360-ohlcv-backfill-queue.test.ts` (TC-6 updated) + `ohlcv-backfill-done-subtask-b.test.ts` (BT-1..8) + `OHLCV-BACKFILL-P0.test.ts` all green (18 pass across the 2-file targeted run); `bun tsc --noEmit` 0 errors; `mock-guard.sh` PASS; DDD (domain→infra/app import) + secret/process.env greps clean on all 3 touched production files.
- Read the actual diff of `isHonestGapCode` (pure predicate, no I/O) + both call sites — confirmed VNINDEX is never routed through the predicate (handler pushes it to `shallowCodes` directly; scheduler gates on `code !== VNINDEX_CODE`), confirmed `bars_inserted` UPDATE uses `COALESCE(?, bars_inserted)` (real count never clobbered to NULL).
- Verified the row's own `verification_gate` literally, via test assertions not descriptions: (a) HG-1/HG-2 (handler) + scheduler DoD(a) test — honest-gap-only shortfall → 0 pending rows, no new row inserted (escalate branch never entered → no BUG send possible); (b) HG-3 — empty-body ack closing a 2nd row after a healthy count-POST close → 0 pending rows, no phantom 3rd row. HG-4/HG-5 + scheduler regression-control tests confirm genuine shortfalls and a stalled VNINDEX still fire (fix does not neuter real detection).
**why-decision:** vc-approved, DONE_VERIFIED. All commit/file/test/tsc/DDD/security checks independently reproduced green; predicate correctly excludes VNINDEX from honest-gap exemption per its own design comment.
**why-change:** none — verdict matches expected outcome.

### STEP qa-S136 · qa · 2026-08-14T14:29:49Z
**task-id:** FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM
**what-done:** Direct-Commit Verify on commit `ff36391dc`. Confirmed real, on main ancestry, `git show --stat` matches both claimed files (`getBctcRefinedTool.ts`, `refine_bctc_md/flow/main.md`) plus 2 more in-scope (`docs/agents/tools/list/get_bctc_refined.md`, new `FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM.test.ts`). Ran everything myself: new test 7/7 pass (50 expect(), exact match to claim); `tsc --noEmit` 0 errors; `mock-guard.sh --files getBctcRefinedTool.ts` PASS; DDD greps (interface→infrastructure import is this dir's established pattern, not domain layer — not a violation) + secret/process.env greps clean (only false-positive "tokens" in a doc comment).
**what-considered:**
- PO's own triage note on this row explicitly blocked sign-off while the fix's own commit left size-lint CI red (getBctcRefinedTool.ts 162→234L, baseline=162/upper=178, tracked as sibling row `FIX-CI-SIZELINT-BCTCREFINED-PROJECTION-BASELINE`). Checked that sibling row (found in `docs/data/orch/archive/2026-08.json`, not hot board): DONE_VERIFIED 2026-08-08 by a prior QA cycle, commit `ed0df780d` added a `size-justification: 242L` header (comment-only, 8 insertions). Confirmed live on the file today (lines 2-10) and re-ran `size-lint-justification.sh --check` full-repo myself: PASS, 0 unjustified offenders — the blocking condition is resolved, not just claimed.
- Read AC-1 at source: `fields:'ids'|'full'` zod enum, default `'full'`, `ids` branch SQLs only `unit_id, window_status` (no markdown/page_numbers_json/flags/confidence/refined_at at the SQL layer, not just response-shaping) → `{report_id, total_units, units:[{unit_id, window_status}]}`, empty `units:[]` on zero-unit reports (never the `full` path's `{error}` shape) — matches AC-1 exactly. Read AC-2 at source: `refine_bctc_md/flow/main.md:63-69` now calls `fields:"ids"` and documents the actual served shape verbatim (was previously `{units:[{unit_id}]}`, a shape the tool never produced) — doc/code now agree.
- AC-3 (test proves ids payload for a >=12-unit report <4KB): read the test file — real served-path invocation via `registerGetBctcRefinedTool` on a live `McpServer`'s `_registeredTools` registry (not a hand-copied helper), 12-unit fixture with realistic bulk markdown per row (so a regression back to the unbounded shape would clearly overflow), asserts `Buffer.byteLength < 4096` — genuinely proves the claim, not just asserts a trivial small number.
- Out-of-scope guard ("do not change the default response shape") independently confirmed: AC-1b test asserts default/`fields:'full'`-explicit both retain all 7 original columns including markdown, byte-for-byte equal to each other. Ran the 2 other test files that reference `get_bctc_refined`/`getBctcRefinedTool` (`FIX-BCTC-BANK-BS-COLUMN-ORDER.test.ts`, `FIX-BCTC-REPORT-ID-LOOKUP-TOOL.test.ts`) alongside the new one: 28/28 pass combined, zero regression in adjacent callers.
**why-decision:** vc-approved, DONE_VERIFIED. AC-1/AC-2/AC-3 all independently reproduced at source, out-of-scope constraint held, and the one explicit PO sign-off gate (size-lint CI red from this row's own commit) is confirmed genuinely resolved by an already-DONE_VERIFIED sibling row, not merely asserted.
**why-change:** none — verdict matches expected outcome; the only extra work was confirming the PO gate's resolution independently rather than trusting the archived sibling row's own DONE_VERIFIED status at face value.

### STEP qa-S137 · qa · 2026-08-14T14:31:12Z
**task-id:** FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH
**what-done:** Direct-Commit Verify. Row's own `commit_sha` (`3a9118e24`) is WRONG — it only touches `docs/WORK.md` (log entry). Real implementation: `28197b3d5` (+follow-up doc-fix `787703374`), both confirmed real, on `main` ancestry, `git show --stat` matches every claimed file across AC-1/AC-3/AC-4.
**what-considered:**
- Independently re-ran `cowork-tick-preflight.test.sh`: 73/73 green. Reverted the script to its parent (pre-fix) commit to reproduce RED-first myself — 2 new FAILs, exact match to claim (T1 SILENT + T6 TOMBSTONED) — then restored clean (verified via `git status`, zero diff).
- AC-1 placement read at source: heartbeat sits after Step 2 presence claim/renew, before Step 2.5's tombstone early-return — fires on all 5 verdicts, output discarded, rc never tested.
- AC-3/AC-4 verified at source: fallback-doc call kept verbatim + pointer note added; brief §1.4 annotated with dated correction, original not deleted. AC-6 sibling sweep independently reproduced: detect-loop renews via auditor Step 0d on its own healthy anti-stale-mask re-spawn (register.md Job 2, heartbeat-age>60min), not a broken-dependent path — correctly no follow-up filed.
- AC-5: no MCP gateway binding in this sub-session (INV-GATEWAY-1) to re-call `task_list_held` live; corroborated instead via the live gitignored telemetry file (real automatic ticks continuing every ~15min post-fix-commit: 13:35/13:59/14:07/14:25Z) and independently recomputed epoch math in `verification_note` (1786711974=12:52:54Z, 1786714502=13:35:02Z) — both check out exactly.
**why-decision:** vc-approved, DONE_VERIFIED. Corrected `commit_sha` field on the write (bookkeeping-only — pointed at the wrong commit); no substantive AC failure, all 6 ACs independently reproduced.
**why-change:** `commit_sha` corrected from `3a9118e24` (WORK.md-only) to `28197b3d5` (actual fix commit).
