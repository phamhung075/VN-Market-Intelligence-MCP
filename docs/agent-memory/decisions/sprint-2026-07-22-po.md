# Decision Journal — Sprint 2026-07-22 · po

**Sprint goal:** cross-plane cron audit sequencing — triage the unassigned remainder of the 2026-07-22 15:48Z read-only cron audit
**Agent:** po
**Started:** 2026-07-22T15:51:08Z

---

### STEP po-S1 · po · 2026-07-22T15:52:00Z
**task-id:** FIX-FLEET-PUSH-LAUNCHD-EXCONFIG-SILENT-DEAD
**what-done:** Verified the fleet-push "permanent abort loop" premise at source instead of accepting the log tails; found the premise stale and the real defect different.
**what-considered:**
- Accept audit framing → route a human-decision reconcile (what the brief asked for)
- Verify divergence first, then decide
**why-decision:** `git rev-list --left-right --count origin/main...HEAD` = 0 behind / 1 ahead; both cited abort triggers are in HEAD. Error log frozen 06-27, stdout 07-03. launchctl: 522 runs, EX_CONFIG(78), zero log bytes; hand-run of the identical command RC=0 with output. Script healthy, launchd spawn layer dead.
**why-change:** Reconcile task would have been work against a resolved premise; the live defect is a silent 19-day-dark backstop.

### STEP po-S2 · po · 2026-07-22T15:55:30Z
**task-id:** FIX-LAUNCHD-PROBE-PRESENCE-ONLY-FALSE-GREEN
**what-done:** Ran auditor-tier1-probe.sh live and read _check_launchd_agents; found presence-only assertion, and separately confirmed the socat-bridge exclusion is correct.
**what-considered:**
- Mint the socat-bridge omission as a probe hole
- Read the function first
**why-decision:** socat-bridge is explicitly allow-listed as obsolete with a documented reason — that would have been a false positive. The real hole is `grep -q "$label"` discarding the status column: probe said ALL_GREEN while `-  78  com.vn-market.fleet-push` was in the same captured string.
**why-change:** New finding, not in the source audit; sequenced FIRST because it is the detector for the fixes.

### STEP po-S3 · po · 2026-07-22T15:58:00Z
**task-id:** ARCH-SESSION-CRON-PLANE-LIVENESS-WATCHDOG
**what-done:** Ruled item 3 (CronCreate cross-session visibility/persistence) — the design call delegated to PO.
**what-considered:**
- (a) broader launchd coverage
- (b) persistence/registry layer under ~/.claude
- (c) accept with monitoring
**why-decision:** (a) scoped + (c). (b) rejected: re-implements a scheduler we do not own inside a surface whose contract can change, and adds a third cron host — the exact thing UC-CDC-P5 warns against. Wholesale (a) also rejected: `claude -p` per tick truncates fan-out (po-S4). launchd hosts re-arm + liveness only. (c) alone rejected: nothing to monitor against without a registry.
**why-change:** Split into 3 parts across 2 EXISTING rows + 1 new, rather than one new epic.

### STEP po-S4 · po · 2026-07-22T15:57:00Z
**task-id:** FIX-GUARANTEED-SLOT-FIRER-FANOUT-TRUNCATION
**what-done:** Separated two bounds the audit conflated, after checking today's log lines directly.
**what-considered:**
- One row for "firer truncates"
- Name both bounds explicitly in one row
**why-decision:** 600s is the inner headless-claude BG ceiling (errors dated 07-18); 1800s is the firer's own `_bounded_exec` SIGTERM (today's chef-morning, exit 143 at exactly 05:58:17Z). Fixing one leaves truncation at the other. `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` verified unset in both script and plist.
**why-change:** Folded item-7a log double-write here — same file, same owner, one pass.

### STEP po-S5 · po · 2026-07-22T16:00:00Z
**task-id:** CLEAN-LAUNCHAGENTS-ORPHANED-UNLOADED-PLISTS
**what-done:** Ruled item 6 — delete both, load neither.
**what-considered:**
- Load com.vn-market.mcp.plist
- Delete it
**why-decision:** Its ProgramArguments target `launchd/mcp-launch.sh`, deleted by f698e0f8b (Docker migration). RunAtLoad=1 + KeepAlive{Crashed:1} would crash-loop, and a restored script would race the live container on :3000/:4004. socat-bridge symlink: delete symlink, keep repo file as the rollback reference the probe already documents as obsolete.
**why-change:** no change from plan.

### STEP po-S6 · po · 2026-07-22T16:01:00Z
**task-id:** TRACK-CRON-AUDIT-SERVER-PLANE
**what-done:** Chose backlog+BLOCKED for the two tracking mirrors of live out-of-band agents.
**what-considered:**
- in_progress + IN_PROGRESS (truthful lane)
- backlog + BLOCKED (visible, undispatchable)
**why-decision:** in_progress is at the WIP<=2 cap; adding 2 would saturate BOUNDED-1/SLS/RLC and stall the dev-team loop, and would advertise the rows as dispatch targets — double-dispatch risk against agents already running. backlog+BLOCKED is the ratified "paused pending external precondition" class and is excluded from every promote gate.
**why-change:** no change from plan.

### STEP po-S7 · po · 2026-07-22T16:01:30Z
**task-id:** FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP
**what-done:** Declined to mint a bctcReparseJob 58.8%-success row; annotated the existing active row instead.
**what-considered:**
- Mint standalone row (audit called it a separate genuine concern)
- Annotate the ACTIVE+SPREADING reparse-corruption row
**why-decision:** A ~41% failure rate on the exact job an open row says is corrupting reports is far more likely the same root cause; two rows would split one cause across two owners. Annotation instructs the owner to test disjointness and mint only if the sets are disjoint.
**why-change:** Deviates from the audit's "worth a row" — prior-art check governs.
