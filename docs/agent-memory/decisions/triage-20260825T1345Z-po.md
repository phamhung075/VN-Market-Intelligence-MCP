# Decision Journal — PO triage tick 2026-08-25T13:45Z (dev-team Step 1, S3 task:po-triage-20260825) · po

**Scope:** PRIMARY RULING on the PaddleOCR migration + Step 0-SIG over 27 durable-inbox envelopes + mandatory pre-checks + 8 caller-surfaced candidates.
**Agent:** po
**Started:** 2026-08-25T13:45:50Z

---

### STEP po-R1 · po · 2026-08-25T13:55Z — PRIMARY RULING: PaddleOCR migration REJECTED
**what-done:** Ruled. Do NOT adopt `paddleocr`. Do NOT adopt `auto`. Keep `tesseract-vie`. The OCR-backend phase of the standing goal is CLOSED as *measured, not adopted, with evidence*; the goal's actual objective is re-targeted onto `FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS`.

**what-considered:**
- **Adopt `paddleocr`** — rejected on four independently sufficient grounds. (i) It REGRESSES the goal's own stated objective. The directive was "improve quality of extraction BCTC"; PaddleOCR strips/garbles Vietnamese diacritics on nearly every word (`Công ty Phần mềm` → `Cng ty Phan mém`). Diacritics are not cosmetic in BCTC label text — this board already carries `FIX-BCTC-REFINE-DIACRITIC-COLLAPSE-A-BREVE-ACUTE` (P-row, architect) for the *far milder* case of `ắ` collapsing to `ă` on two tickers. Wholesale stripping is strictly worse than a defect we already thought worth fixing. A backend cannot be adopted in service of a goal it measurably fails. (ii) MEMORY: peak 2790.0 MiB against a **2560 MiB** (2.5 GiB) cap. The specialist reported this as "98.5-100%"; it is in fact **over** the cap, sustained ~2 min. The same container is independently on record at 88–98.87% under ordinary traffic (`FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` in review[], `FIX-AUDITOR-EMIT-SEVERITY-LABEL-FLAT-ESCALATION-BYPASS-NEVER-FIRES`). This converts an intermittent OOM risk into a deterministic one on every table phase. (iii) 1.8x slower on a service that already carries `PERF-PEK-PER-PAGE-LATENCY` for 30–40 min/page. (iv) `FIX-PDFX-PRECURSOR-TELEMETRY-98PCT-VMHWM-PINNED-15MIN-BEFORE-THE-OOM-KILL` records a real CONSTRAINT_MEMCG OOM kill on this container two days ago.
- **Adopt `auto`** — rejected. Memory-safe (1511 MiB, ~60%) but delivers **zero measured quality upside**: byte-identical to `tesseract-vie` on 30/30 units *including page 9*. Paying 1.6x runtime for a rescue that never fires is a pure loss.
- **Keep `tesseract-vie` and stop there** — rejected as incomplete. It leaves the page-9 class of catastrophic per-page miss unowned, which is the real quality defect the whole exercise surfaced.
- **Keep `tesseract-vie` AND re-target the goal onto the confidence signal** — CHOSEN.

**why-decision:** The specialist's own data supports a conclusion it did not draw. PaddleOCR's single genuine win (page 9: tesseract lost 100% of revenue/profit figures, paddle got the full row) is *not* an argument for the backend — it is evidence that a catastrophic per-page miss exists and **nothing detects it**. The rescue mechanism already exists (`AutoFallbackOcrBackend`, tess-first, threshold 0.5) and would capture exactly that win *without* paying the diacritic regression fleet-wide, because it only fires on pages tesseract genuinely failed. The only broken part is the trigger. So the correct product question was never "which backend" — it was "why is the confidence signal blind". That reframing is the ruling's substance.

**Independent verification (not relayed — PO read the source, per supervised-goahead §Step 2 discipline):**
- `apps/pdf-extractor/infrastructure/ocr_backends.py` `TesseractVieBackend.recognize_text()`: `valid_rows = data[(data['conf'] > 0) & (data['text'].str.strip() != '')]` then `mean_conf = float(valid_rows['conf'].mean()) / 100.0`. **The denominator is the rows it FOUND, not the rows it should have found.** Recall is invisible to the score by construction. A page yielding 3 header words at conf 92 and losing 40 data rows returns 0.92. Confirms the specialist's root cause exactly.
- `_VALID_BACKENDS = {tesseract-vie, paddleocr, auto}`, `_DEFAULT_BACKEND = "tesseract-vie"`, selected via `OCR_TEXT_BACKEND`.
- **The ruling requires ZERO deploy action:** `grep -rn OCR_TEXT_BACKEND` across all compose/env/Dockerfile files → no match; `docker inspect` on the live `vn-market-intelligence-mcp-pdf-extractor-1` (Up 7 hours, healthy) → the var is unset. Live backend is already the default `tesseract-vie`. The ruling is the status quo; nothing to redeploy, nothing to roll back.

**What would reopen the backend question (asked for explicitly by the caller):** a measurement showing PaddleOCR renders diacritics correctly AND peaks under 2560 MiB. The stripping pattern (accents dropped, base letters kept) reads like a recognition-dictionary/charset problem — the default rec model and `use_space_char` are the obvious suspects — not an inherent PaddleOCR limitation. That is a SPIKE, not an adoption, and it is **not** worth opening now: even a diacritic-clean PaddleOCR would still be 1.8x slower and over the memory cap, so it could only ever be the *rescue* arm of `auto`, which is precisely what po-R2 unblocks anyway.

**why-change:** no change from plan.

### STEP po-R2 · po · 2026-08-25T13:58Z — the row that actually carries the user's goal
**what-done:** Minted `FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS` (P1, `apps/pdf-extractor/`, next_agent=dev-pdf-extractor).
**what-considered:** Mint at P0 — rejected; no user-facing surface is currently broken, and the pdf-extractor lane already has two open P0/P1s competing for the same specialist. AC-1 forbids the obvious wrong fix (lowering the 0.5 threshold — trading one arbitrary constant for another); AC-2/AC-3 are a matched positive/negative control pair on the *real* FPT Q4 2025 corpus, so a fix that rescues everything (and thus reintroduces the diacritic regression) fails as loudly as one that rescues nothing.
**why-change:** no change from plan.

### STEP po-R3 · po · 2026-08-25T13:58Z — the LARGER quality lever, verified at source
**what-done:** Minted `FIX-PDFX-PEK-TABLE-REGION-COLLAPSED-TO-ONE-CELL-DISCARDS-PER-LINE-BBOXES` (P1, next_agent=**architect**).
**what-considered:** Route to dev-pdf-extractor like its sibling — rejected: the fix is an `OcrBackendPort` **contract** decision (`(text, confidence)` vs a list of `(bbox, text, confidence)`) spanning three adapters and two downstream assemblers. The current signature literally cannot express what the caller needs; that *is* the root cause, so it must be decided before code.
**why-decision:** I read `apps/pdf-extractor/infrastructure/pek_engine_adapter.py` ~1146-1165 myself. When `self._ocr_backend is not None` — the live configuration for **every** backend value — the whole table-region crop becomes ONE cell whose `bbox` is the *region* bbox. The `else` branch it replaced iterated `paddle_table.ocr(crop)` per-line results. So PEK-IMPL-OCR silently traded table structure for backend pluggability, fleet-wide, tesseract included. **This is a strictly larger BCTC-quality lever than the backend choice ever was, and it explains why a backend swap could not have delivered the goal on its own.** It also independently explains the 30/30 byte-identical result: downstream assembly is insensitive to per-line geometry.
**why-change:** no change from plan.

### STEP po-R4 · po · 2026-08-25T13:52Z — RAN the acceptance probe the QA session could not, and it FAILED
**what-done:** Executed subtask-4 of `FIX-ORPHAN-FR4-FR5-FLOW-DEVTEAM-ADOPTION-GUARD` (P0, review[]) myself. Minted `FIX-TASKRELEASE-ORIGINAL-OWNER-CLIENT-SESSION-INERT-RUNGB-NEVER-RELEASES` (P0, next_agent=architect) and folded the result onto the review row.
**what-considered:** Accept the QA note's framing ("needs a gateway-capable owner") and re-dispatch — rejected. I *am* gateway-capable; deferring would have been a third cycle of narrating a probe instead of running it.
**why-decision:** 5-step probe with a positive control:
1. `task_claim(orphan-signal:PO-SYNTHETIC-FR2-RUNGB-PROBE-20260825, owner_client_session=dead-session-000…)` → `{claimed:true}`
2. TEST, the exact `orphan-adoption.md:61-66` shape (`owner_client_session`=CALLER, `owner_agent`, `original_owner_client_session`=DEAD) → **`{ok:true, released:0}`**
3. `task_list_held(kind="orphan-signal")` → count:1, **lock STILL HELD** — so `released:0` is a real non-release, not an already-gone no-op
4. CONTROL, release under the dead session's OWN id → **`{ok:true, released:1}`**
5. `task_list_held` → count:0, clean, no residue

`task_release` authorises **solely** on `owner_client_session`; `original_owner_client_session` is accepted by the schema and ignored. The doc half of subtask 4 DID land (params present at all 3 sites), so the row looks complete on inspection while being functionally dead — which is why 15 days of review never caught it. **This falsifies the standing note `feedback_orphan_signal_unreleasable_null_client_session_and_activesprints_only_flip`**, which recorded that orphan-signals ARE releasable via `owner_agent` + `original_owner_client_session`. They are not.

**Second finding, same probe:** subtask-4's acceptance is unrunnable *as written* — it demands "a real NULL-owner orphan-signal row", and I measured `task_list_held(kind="orphan-signal") = 0` live rows. As worded it can only be satisfied opportunistically **after a genuine mid-task agent death**. Holding a P0 on an AC that requires a production incident is a structural stall, not an acceptance criterion. Restated as the synthetic-fixture probe above (AC-4 on the new row).
**why-change:** Diagnosis changed from "QA session lacked tools" (the record's premise) to "the mechanism does not exist, and the AC cannot be run on demand". The cheap fix is caller-side — pass `dead_session` as `owner_client_session`; it is already in scope at all 3 sites — and the control proves it works today with zero server change.

### STEP po-R5 · po · 2026-08-25T13:47Z — CARD.md, with one caller claim CORRECTED
**what-done:** Minted `FIX-DISPATCHCLAIM-CARD-PAYLOAD-OBJECT-REJECTED-AND-PHASEA-OMITS-OWNER-AGENT` (P1, agent-father).
**why-decision:** Probed the live schema rather than trusting the report. Passing `payload` as an object returned a Zod error naming **three** problems at once, not one: `payload` expected string; **`owner_agent` Required, received undefined** (CARD.md:16-17 Phase A omits it — the caller did not report this); and the `task_kind` enum, which CARD.md never enumerates.
**CORRECTION recorded on the row so the implementer does not "fix" a non-bug:** I initially suspected `task_list_held`'s filter was inert after `task_kind="orphan-signal"` returned all 13 locks. Negative control `task_kind="zzz-nonexistent"` → also all 13; but `kind="cowork-slot"` → exactly 4. So **`kind` is the real parameter and `task_kind` is silently ignored** — CARD.md:11's `kind=` is CORRECT. My first read was wrong and I am recording that rather than shipping it as a finding. The input param is `kind` while the response field is `task_kind`; a real ergonomics trap, but the card is on the right side of it.
**why-change:** Row scope WIDENED to also own the `dispatch-claim/SKILL.md` FR-3 retrofit (caller item 6) — see po-R6.

### STEP po-R6 · po · 2026-08-25T13:59Z — answered QA's "file it or correct the claim"
**what-done:** Folded onto review[26]. Grep-confirmed the cited FR-3 retrofit row genuinely does not exist anywhere. Rather than mint a ninth thin row, put FR-3 in scope on the po-R5 row (AC-4) and pointed the broken reference at that id — it is the same file family, same owner (agent-father), same commit.
**what-considered:** Mint a standalone FR-3 row — rejected: two rows editing `dispatch-claim/{CARD,SKILL}.md` in the same window would race each other's diffs.
**why-change:** no change from plan.

### STEP po-R7 · po · 2026-08-25T14:00Z — the sweep filed the bug that blocked the sweep
**what-done:** manual-dispatch-sweep Step 1 → **146** candidates (up from 127 nine hours ago; the starvation set is still growing). Step 2 on rank 0 **hard-aborted**: `id=FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED live=34589B -> candidate=34897B`, live file untouched. Ranks 2 and 3 are 12249B/12156B — also over. Fell through to rank 4 `FIX-COWORK-DELPROOF-1-STEP53-TWOARM-GATE` (2890B), stamped clean, folded into BATCH.
**what-considered:** Fold onto the existing `FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS` (ready[]) — **impossible: that row measures 11967B, 33 bytes under the ceiling.** The row documenting "the ceiling blocks bumps" is itself one sentence from being unbumpable. Minted `FIX-PO-MANUALDISPATCH-STEP2-STAMP-BLOCKED-BY-PROSE-CEILING-STARVES-TOP-CANDIDATES` (P1) instead.
**why-decision:** This is a *starvation mechanism*, not an annoyance, and it is materially broader than the known occurrence-bump symptom: Step 2 is the **only** dispatch path for the DRS-STRANDED / BACKLOG-XOR-GAP classes, and the feedback loop runs the wrong way — the longer a row is stranded, the more triage prose it accretes, the more certainly it becomes permanently undispatchable. AC-1 forbids both wrong fixes (raise the ceiling → defeats the guard; skip over-ceiling rows → makes the starvation designed rather than accidental) and points at a named coordination-field allowlist. AC-4 keeps a 500-byte narrative append aborting, so the exemption cannot degrade into a bypass.
**why-change:** Selected rank 4 instead of rank 0, and prose-gated it first per `feedback_dispatch_gates_blind_to_prose_status_note_autoclaim_contraindicated_row` (`status_note` null, no contraindication).

### STEP po-R8 · po · 2026-08-25T13:45Z — inbox: 22 of 27 already covered
**what-done:** Fresh self-read (27 envelopes, SSOT). Dedup by SUBJECT via `scripts/po-board-dedup-search.sh` (never `grep`, per `feedback_auditor_dedup_citation_resolves_to_nonexistent_row_id`).
**why-decision:** Only 3 inbox envelopes needed new rows. The 5 cowork→architect cycle-snapshot envelopes (0,1,3,4,12) were **already fully folded by earlier PO ticks today** — `backlog[499]`'s own `po_fold_20260825` already names both divergent SKILL copies and the withdrawn framings, and `backlog[568]` was already minted from envelope 3. Envelope 16 (hangSeng) claimed "zero board row tracks it": **false**, `FIX-EVENING-GLOBALSNAPSHOT-ZERO-GUARD-DROPS-HANGSENG-ONLY-ROW` was minted at 12:51Z today. Envelope 17 (team-tool-recheck) asks for a decision **already made three times** (`CLEAN-RETIRE-TEAM-TOOL-RECHECK-*`, `DECIDE-TEAM-TOOL-RECHECK-WRITER-*`); the actual defect is `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE` — the emitter has no liveness predicate, so it will keep re-asking a settled question forever. Envelope 13 (same-tick producer/consumer race) is covered by `DESIGN-COWORK-FANOUT-PRODUCER-CONSUMER-ORDERING` + `DESIGN-COWORK-FANOUT-T4`, and `backlog[568]`'s `po_boundary_20260825T1245Z` already fences the two axes apart. New: envelopes 21 (bctc-discover 513 stale, **no board row ever**), 24/25 (CADRAT-3, one incident two transports), 26 (A-30 ephemeral discriminator).
**why-change:** Envelope 7 produced a **premise correction** rather than a mint: `FIX-COWORK-BATCH-COVERAGESTATE-CONTENTION-CORRELATES-WITH-MISSED-NOTEBOOK-COMMIT` blames mutex contention, but the same-tick controlled contrast (market-watcher committed `d41596771`; news-scout narrated a commit it never made, same second, same conditions) **falsifies** contention as the discriminator. Folded.

### STEP po-R9 · po · 2026-08-25T13:46Z — pre-checks
**what-done:** Supervised-hold: re-ran dev-team's own WF-2 `should_hold` against `.head` (`FIX-CCATO-NTG-…-FORGED-WRITER-ID`) → `supervised=false`, `should_hold=false`. No-op; `.head` and that row untouched as instructed. Step 0-TNB: `docs/handoffs/tnb-audit-latest.md` is c134 (2026-08-24T20:13–20:34Z), already ACKed 2026-08-24T23:04:02Z, file unmodified since — no-op. Signal dashboard (Pipeline B): 0 NEW `to=po` rows — inbox empty. INCIDENT_CAP slot 2 left free, per the prior cycle's reasoning; `in_progress[]` has one live row and the strand-fix still has only one live test, so nothing this tick justifies overriding that.
**why-change:** no change from plan.

---

**Writes actuated this tick** (all via `scripts/orch-apply.sh`, all confirmed by its `OK — candidate applied` line):
- `.task_board.backlog +=` 8 rows (`scripts/po-triage-20260825T1345Z-mint.jq`) — task_total 855→863
- 3 folds + 1 `.signal_queue.rows +=` (`scripts/po-triage-20260825T1345Z-folds.jq`) — signal_total 32→33
- 1 dispatch stamp + 1 row (`scripts/po-triage-20260825T1345Z-dispatch-stamp.jq`) — task_total 863→864
- `.dev_team_idle_chain.pending_triage_inbox` CLEAR, 27→0, `ORCH_APPLY_DECLARED_INBOX_TRIAGED` with all 27 envelope_ids
