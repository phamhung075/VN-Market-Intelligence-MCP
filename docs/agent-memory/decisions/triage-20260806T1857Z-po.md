# PO Triage — 2026-08-06T18:57Z (dev-team tick 18:37Z, Step 1)

task_id: `triage-20260806T1857Z-po`
Inputs: 4 pendingSignals[] · `read_telegram_reports(status=new)` (6, ids 4464-4469) · `list_unresolved_reports()` (same 6) · `.task_board` (backlog 361 / ready 62 / review 248 / qa 3 / in_progress 0) · `git log -30` · `git branch` (main only, 0 stale branches → no CLEAN-branch row).

---

## STEP po-1 — Signal dispositions (4/4 routed, 0 minted)

| Signal | Disposition | Target |
|---|---|---|
| context-bloat `register-job-db-integrity-offhours.md` 255L/21159B | FOLD (occ 3) | `CLEAN-SKILL-BLOAT-CRON-STANDALONE-DB-INTEGRITY-SPLIT-CHILDREN-BYTE-CAP` |
| context-bloat `register-job-db-integrity-weekday.md` 257L/21361B | FOLD (occ 3, same row) | ″ |
| context-bloat `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-agent-father.md` 532L/36224B | FOLD as evidence | `FIX-DECISION-JOURNAL-SKILL-CAPCHECK-LINE-ONLY-NO-BYTE-ROLLOVER` (review[]) |
| notebook_tiebreak_direction_defaulted `ba.md` | ACK, no action | payload's own `action_required: informational_no_action_needed` |

**what-considered / why-change:** the default path was "3 bloat breaches → 3 CLEAN rows". Rejected after a full-text board grep (not an id grep): all three already had owning rows. Minting would have produced three duplicates and, worse, would have hidden that two of the three owners are *structurally undispatchable* — which is the actual finding.

## STEP po-2 — The bloat recurrence is a dispatch defect, not a pruning defect

The CLEAN row was minted 09:29Z, escalated P2→P1 at 14:34Z, and re-fired at 18:44Z. Occurrence 3. Two things were measured rather than assumed:

**(a) The files are still growing, and by whose hand.** `git show --stat` on every commit touching the pair *after* the split (`3bfd388ea`):

| commit | net lines added across the pair | lane |
|---|---|---|
| `9d0ea50c2` | +146 | `fix(cross-service)` |
| `656a63821` | +32 | `fix(db-integrity)` |
| `74a1df441` | +26 | `fix(db-integrity)` |
| `36e109170` | +12 | `fix(cross-service)` (landed this same tick window) |

Growth vs this row's own title numbers (09:29Z): offhours 15967B→21159B (+32.5%), weekday 16158B→21361B (+32.2%), in 9.4h. Every growth commit is developer-lane cron-content work — *not* an agent misbehaving, but the ordinary cost of maintaining the db-integrity cron.

**(b) The two children duplicate one prompt body.** The split commit's own message records offhours as "same prompt as Job 1". So every db-integrity cron fix is applied **twice**. Any line-only re-prune therefore re-breaches within a few commits — which is exactly what happened once already.

→ Revised fix spec written into the row: extract the ONE shared CronCreate prompt body into a single lazily-loaded file both `register-job-db-integrity-{weekday,offhours}.md` reference, leaving only the schedule delta inline. Halves bytes and removes the double-append, so the cap holds under future cron fixes.

**(c) Why it never shipped.** `next_agent: claude-manager-helper` is off every dispatch allowlist → `DRS-STRANDED-OFF-ALLOWLIST`, reachable only by PO manual dispatch, where it ranked **15th of 44** and was never selected. claude-manager-helper itself ran at 18:06Z (`5024251f8`) and did not touch it — so this is empirical, not inferred: that agent is not an actuator for this row. Re-pointed `next_agent`/`owner` → `developer`, `zone` → `cross-service/`, matching the agent that demonstrably edits these files.

**Verified both directions** (not narrated): `is_bounded1_eligible` → `true`; `is_drs_stranded_off_allowlist` match count → `0`. Sanctioned remedy per `manual-dispatch-sweep.md` § Step 1 ("a row drops out of the stranded predicate once reassigned to a dev-role next_agent").

## STEP po-3 — Manual-dispatch sweep (MANDATORY pre-check)

44 candidates (43 DRS-stranded backlog + 1 ready-XOR), 3 at `reflag=true`. Top = **`FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT`**, the only P0 in the set. Prior stamp `2026-08-06T14:53:40Z`, age **14464s** vs the 14400s window — re-admitted by 64 seconds, having never dispatched. This is re-admission **#2**. Stamped per Step 2, folded per Step 3.

## STEP po-4 — Systemic: 84% of the stranded queue points at one agent

Re-ran the shared predicate and grouped by `effective_next_agent`: **37 of 44 (84%) = `agent-father`**; ops 3, ops-mainserver-fetch 2, code-janitor 1, ops-vps-fetch 1.

Throughput arithmetic: the sole mechanism reaching this class stamps **exactly one row per PO tick** by design. Against a 44-row queue still being minted into, drain ≤ mint, so the queue is non-decreasing *regardless of priority*. The P0 above is the proof — the single documented mechanism could not clear the only P0 in the set within 4h.

**Did NOT mint.** Prior-art grep found `FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION` (backlog, P1, owner=architect) already owns the class — but it had sat since 2026-07-30 with `desc: ""`, `files: null`, `baseline_pass: null`, i.e. an unactionable stub. Filled it with the live re-measurement, the 84% concentration, the throughput argument, and a narrow direction for the architect: *does agent-father's own cron consume its own board queue, or only freshly-spawned intents?* — answerable from agent-father's own flow docs, and it decides between "allowlist agent-father" and "give its cron a board-drain step". Explicitly flagged that `FIX-DEVTEAM-BOUNDED1-MAINTLANE-NEXTAGENT-GATE` pushes the opposite direction and would grow the stranded population if it ships alone.

## STEP po-5 — Supervised-hold ratification

`.head.status == "idle"` (`active_task_id: null`) → WF-2 evaluates nothing. No-op, correctly. No `po_goahead_*` stamped; stamping with nothing held would be a vacuous gate.

## STEP po-6 — Double-dispatch guard before folding the BCTC row

Router flagged `FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH` as eligible. Confirmed unclaimed (`status: BACKLOG`, no bounded1 stamp, `ready[]` carries zero bounded1-stamped rows). Rather than assume no race, **dry-ran `scripts/devteam-backlog-promote-bounded1.jq`** against the live board: its next auto-pick is `FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR` (earlier array index at equal rank 1), not the BCTC row — and once that promotes, WIP=1 makes BOUNDED-1 a no-op. So the BCTC row has no automatic path this tick and folding it into BATCH cannot collide.

## Telegram (6 unresolved, all `status: new`)

- **4464-4466** `[audit-output-contract]` — auditor self-contradiction (`signals_posted=1` vs `dashboard_rows=0` / "0 anomalies"). Known class, `feedback_auditor_dashboard_row_telegram_sent_text_ignores_dedup_skip`. Not re-minted this tick.
- **4467** `[qa]` — both halves already actioned by others: the bounded1 stale-stamp half shipped as `ab681bb7b`; the 4-stranded-rows half is subsumed by the STEP po-4 row, whose new desc is a superset (44 rows, measured).
- **4468** `[cowork-dispatcher]` push-rebase blocked by unrelated dirty tree — known standing condition (`feedback_push_blocked_by_perpetual_dirty_tree`).
- **4469** `[clean-obsolete-files]` 16 dry-run candidates — CLEAN class, deferred; no free slot and `CLEAN-STRANDED-REPO-STATE-20260806` already sits in the stranded set.

**Structural, unchanged and worth repeating:** PO's grant is `read_telegram_reports` only — there is no resolve/close verb, so these six stay `new` no matter how many ticks read them. `FIX-PO-MAINFLOW-ORPHANS-TELEGRAM-REPORTS-RESOLVER-SUBFLOW` owns it and is itself DRS-stranded on agent-father — i.e. the row that would let PO close reports is in the queue that cannot drain.

## Writes

Two `orch-apply.sh` pipes, both Stage 0+1 PASS, conservation `task_total 782=782` / `signal_total 200=200`, 4 rows stamped, **0 rows created** (no mint this tick — every finding had an owner). Stage 1g's 16 dangling refs are pre-existing/NON-FATAL; none are mine.
