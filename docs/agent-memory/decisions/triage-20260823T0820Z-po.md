# PO decision journal — triage 2026-08-23T08:03–08:22Z

**task_id:** `RAG-FTS-BUILD-MEMORY-BOUND` (review-lane SECONDARY-drain) + Step-1 triage of 19 durable-inbox envelopes.
**Spawned by:** router handoff after dev-team tick `2026-08-23T07:37Z` returned `NEXT: po` (developer has no `Agent` grant).

## D1 — The CRITICAL cron row: I was asked to judge the auditor, and the auditor was right about mechanism

- **what-considered:** (a) accept the auditor's "post-recovery backlog, recovering normally"; (b) accept the router's read that MCP-server-scheduler crons cannot be explained by a Claude-session outage and mint a scheduler-defect row; (c) measure before choosing.
- **why-change:** Chose (c). The router's reasoning was sound and I initially agreed with it — 12 crons stopping inside a 37-minute window while 52 peers kept firing looks exactly like a partial scheduler failure. It was falsified by one query against `cron_job_runs`: daily run totals `08-13=3146, 08-14=3147, 08-15=1584, 08-16=18, 08-17=42, 08-18=154, 08-19/20/21=NO ROWS, 08-22=1215, 08-23=889`, and `intelligenceCycle` (15-min cadence) with a clean hole `08-18 09:xx → 08-22 12:40` and exactly 4 fires/hour on either side. **Everything stopped and everything resumed** — a host/Docker suspension, not a defect. Had I minted on the "partial failure" hypothesis I would have sent a developer to hunt a scheduler bug that does not exist.
- **The auditor was still wrong about the disposition, which is the part that mattered.** "Recovering normally" is a claim that no action is owed. Two things it masked: (1) the outage is a **3rd** recurrence of a known unmitigated failure mode, ~99.7h of total silence plus a 3-day degraded shoulder; (2) `monthlySignalQualityAudit` missed **07-01 and 08-01 on days the scheduler ran 3146 jobs/day** — genuinely dead, and invisible inside a 28-row batch attributed wholesale to backlog.
- **Generalizable:** an aggregate attribution ("all of this is one event") is only safe if something checks the residue. A detector that cannot separate its own findings will always be paired with a narrator who papers over them.

## D2 — I minted 3 duplicate rows and retracted them in the same tick

- **what-considered:** leave them (they were already committed to disk and each was individually defensible), or retract.
- **why-change:** Retracted. My dedup scan used the token `notebook` and the board names those rows `CLEAN-NB-*`; the regex missed three live rows. I only caught it because `orch-apply.sh`'s prose-ceiling WARN list printed `CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` — an unrelated diagnostic that happened to name my duplicate. **A dedup scan keyed on my own naming instinct rather than the board's actual id vocabulary is not a dedup scan.** The pre-existing rows were also strictly better than mine: `CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION` already argued "fix the producer, the split is cleanup afterwards", which my row did not.
- The evidence was not wasted — it was folded onto the existing rows, where it **confirms their prediction**: both notebooks grew ~1.5x since those rows were written (`dev-rag-service` 15888→23244B, `digest-predict` 23477→35214B) while still `section_count=1`. That turns their hypothesis into measurement.

## D3 — RAG cluster: three rows, one subsystem, three different fixes — deliberately NOT merged

- **what-considered:** merge all three as one RAG-memory problem (the router flagged them as possibly the same); or adjudicate separately.
- **why-change:** Separate. `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` is a deploy action whose own desc says it is superseded and whose blocking action is verified done (image `2026-08-14T20:12:15Z` post-dates fix commit `4c8c601e6` @ `2026-08-12T06:16:02Z`) → CANCELLED. `FU-RAG-DEPLOY-MEMORY` is a sizing decision, deploy half verified (`MemLimit=2GiB`), durability half owed. `RAG-FTS-BUILD-MEMORY-BOUND` is an unbuilt code-level bound on `_build_fts_index()`. **Merging would have hidden the only one still unbuilt.**
- **Refused to self-certify `FU-RAG-DEPLOY-MEMORY` despite 7 days at RestartCount=0 / 38% of cap.** The row names a specific bar (RAG-MEM-DURABILITY-BAR v2 D1–D5) I did not evaluate, and ~4 of those 7 days the whole scheduler was dead, so the service was not under load. **A durability window that spans an outage is not a durability window.** Routed to qa.

## D4 — The escape clause that looked like it fired, and did not

- `RAG-FTS-BUILD-MEMORY-BOUND` carries an explicit instruction: if corpus growth collapses to ~0 before the time-gate, that IS a new defect and needs its own row. Wall-clock arithmetic said **7.96 rows/h vs a 47.7 baseline — a 6x collapse**. That is a mint on the row's own terms.
- **what-considered:** mint the stalled-ingest row as instructed; or test whether the denominator is real.
- **why-change:** Did not mint. The measurement window contains the 99.7h outage I had already proven in D1. Per **live** hour the rate is ~52–63 rows/h — at or above baseline. The pipeline is healthy. **Generalizable: a rate is a ratio, and an availability incident corrupts the denominator of every rate measured across it. Before acting on a rate collapse, check whether the clock was running.**
- Time-gate moved `2026-09-06 → 2026-09-20` and I **split the ACs**: AC#2 (peak-mem + wall-clock) is measurable today at 32184 rows and gates three other decisions; AC#1 (no-OOM at ~56k) stays gated. Hard guard recorded: an AC#2 green is not an AC#1 green — **this row already produced that exact false green once, certified at 116 rows where the test could not have failed.**

## D5 — Notes I could not write, and what I did instead

- `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` was 16824B against a 12000B growth-only ceiling, so the `ci_red` contract ("append the observed SHAs to that row") was physically unexecutable. Rather than skip the contract or split the write, I did the migration the ceiling check itself advertises: moved `po_review_verdict_20260813T1629Z` (6025B) verbatim to `backlog-detail.json`, set `detail_ref`, verified the text survived, then amended. Row is now **11509B, under ceiling and amendable again**; fleet over-ceiling count 24 → 23.
- **Did NOT close signal row `tra-20260822T203234`** (the `audit-handoff` row making CI red). Closing it would very likely turn the guard green. That is gaming the guard, not fixing the routing gap. CI stays red and the row stays P0.
