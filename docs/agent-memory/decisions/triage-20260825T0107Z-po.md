# PO Triage Decision Journal — 2026-08-25T01:07Z tick

task_id: `FIX-REAPER-ORPHAN-MINT-KEYS-ON-TTL-ONLY-NO-SESSION-LIVENESS-CHECK`
session: `036ceaf1-bf34-46cd-92e4-8c6b213ff4bb` · inbox: 11 envelopes → 1 mint, 5 folds, CLEAR to 0

## Decision 1 — mint the reaper minting-side row (the caller's coverage brief was right, but not for the reason given)

**what-considered:** (a) trust the spawn brief's "minting side has NO row" and mint; (b) treat
`FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD` (backlog, P0) as covering it — its title literally reads
*"stop false-orphaning long agents + clearable reaper orphans"*; (c) treat
`FIX-SPRINT-TASK-HEARTBEAT-LOCK` as covering it.

**why-change:** I was told to verify rather than trust, and (b) is a genuine trap — on title alone
it looks like exact coverage. It is not. That row's own `audit_ref.note` scopes the
"false-orphaning long agents" clause to `fix_spec(b)/AC2`, whose successor is (c). And (c)'s four
subtasks are all CLAIMANT-side: bind `owner_client_session`, raise the sprint-task TTL, add a
heartbeat loop in `execute-tier.md`, delete INV-GATEWAY-1 dead calls. That keeps the lock *fresh so
it never expires* — the opposite end of the same symptom. The reaper's own predicate is untouched,
so any task outliving even a raised TTL is still falsely orphaned. Confirmed at source rather than
inferred: `gcExpiredLocks()` Phase 1 in `coordinationStore.ts` selects on `expires_at + grace <
unixepoch('now')` plus kind/prefix filters only. It *does* read the row's `owner_client_session`,
but purely to copy it into the signal payload — there is no join, subquery or lookup against
`task_kind='session-presence'` anywhere in the function. Verdict: uncovered, mint.

**design ruling folded into the row:** the guard must be **suppress-only**. Presence PRESENT ⇒
suppress the mint; presence ABSENT ⇒ behave exactly as today. Rationale: presence registration is
opt-in and `SPIKE-SESSION-PRESENCE-ROSTER-UNDERCOUNTS-LIVE-SESSIONS` measured the roster returning
1 row against ≥3 live sessions. Under suppress-only polarity an undercounting roster can only make
the guard weaker, never wrong — so this row must NOT be blocked behind that SPIKE. Also recorded:
do not copy the cron Step 1b.1 oracle the envelope proposes mirroring; `FIX-CRON-REARM-STEP1B1-
LIVENESS-ORACLE-BLIND-WINDOW-FALSE-LIVE` documents it broken in both directions (8h10m outage).

## Decision 2 — falsify the "unreleasable" half instead of minting it

The correction envelope asserted the false orphan is *unreleasable* because `owner_client_session`
is null and `task_release` keys on it. **The tool-side half of that is false.** `releaseTask()`
carries a working FR-2 Rung B null-session ladder (`task_kind='orphan-signal' AND
owner_client_session IS NULL`) and `taskReleaseTool.ts` exposes both required params; it shipped
with `FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER`.

The real defect is **caller-side** and already owned by `ready[] FIX-ORPHAN-FR4-FR5-...` subtask 4:
all three `orphan-signal:*` release calls in `orphan-adoption.md` (:69, :72, :95) pass only
`owner_client_session`, so Rung A cannot match and Rung B is never attempted. That is the true
generator of `released:0` and the every-tick re-trip.

**Root cause of the non-implementation — line-reference drift.** Subtask 4 pointed at
`dev-team/flow/main.md :365-370 / :391-394`. Verified live: those lines now hold the WF-1c/WF-1d
head-pin blocks. The adoption body was extracted to `orphan-adoption.md` after the subtask was
written, so an implementer following it verbatim finds no release call and skips. Corrected the
subtask in place; folded rather than minted a second row.

**what-considered:** minting the release-side defect as its own row. Rejected — that is exactly the
searched-the-symptom-not-the-owner duplicate this row's own 22:30Z correction warns about.

**not actuated, deliberately:** left `dispatch_lane` null on that row. Its own
`po_triage_20260806T2230_rootcause_correction` already diagnosed the 33-day stranding as RLC
starvation, not a lane-field defect. Overwriting that analysis on thinner evidence during a triage
tick would be unjustified.

## Decision 3 — DXG got a labelled CANDIDATE note, not a row and not silent coverage

`get_bctc_full(DXG)` has returned no data for 40+ consecutive cycles despite Q1/Q2 PDFs on file
since 2026-08-01. `FIX-REFINE-PAGECOUNT-ZERO-COVERLETTER-MASK` predicts exactly that shape (PDFs
present, refine mis-skips as cover-letter). I could not verify DXG is actually among its measured
25/25 pending set. **only path:** annotate that row with the corroboration explicitly marked
*linkage not proven*, naming the check required before it may be treated as covering DXG, and
stating that if DXG is not in that set it needs its own row. Minting on an unverified linkage is
the duplicate-by-symptom trap; asserting coverage silently would bury a 40-cycle watchlist gap.

## Guard interaction worth keeping

`orch-row-prose-ceiling-check` aborted my first apply with **zero writes** — two target rows were
already near the 12000B ceiling. I did not use `orch-backlog-stub.sh` (a lane-wide bulk stripper,
far too broad for a triage tick) and did not split the write (explicitly forbidden as a dodge). I
rewrote both notes shorter. Note the irony: the row that blocked me is
`CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION` — a byte-cap row breaching a byte cap.

Every mutation was dry-run to scratch and asserted present **before** apply, then re-read from the
live file **after**, because `orch-apply.sh` returns OK on a transform whose selector matched
nothing (`FIX-ORCHAPPLY-SELECTOR-MISS-SILENT-NOOP`).
