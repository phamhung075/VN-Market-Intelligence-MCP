# PO Decision Journal — dev-team Step 1 triage, tick 2026-08-11T12:37Z

Task ids: `UC-RDL-P4` (supervised-hold ratification, primary) + board triage of 29 durable-inbox envelopes, 11 `to=po` signal_queue rows, 88 unresolved Telegram reports.

---

## STEP po-1 — UC-RDL-P4 supervised-hold ratification (WF-2 `po_goahead` producer)

**What was considered:** (a) stamp on the relayed BA verdict; (b) verify at source then stamp; (c) refuse to stamp and leave the hold.

**Why (b):** `supervised-goahead.md` §Step 2 forbids stamping on a relayed verdict — the whole point of `supervised` + `po_goahead` is that PO is ratifier of record. Independently re-derived, at source, five load-bearing claims from the BA spec:

| Spec claim | Verified at | Result |
|---|---|---|
| `coordinationTools.ts` split into per-tool files | `apps/mcp-server/src/interface/mcp/tools/system/coordination/` | CONFIRMED — 6 tool files + `index.ts` |
| `getCycleBootstrap.ts` layering precedent exists | `apps/mcp-server/src/application/usecases/getCycleBootstrap.ts` | CONFIRMED — 5744 bytes |
| `tool-registry.json` `totalCount: 183` | `docs/data/tool-registry.json` | CONFIRMED |
| EC-5: `ORPHAN_EMIT_ALLOW_LIST` excludes `"intent"` | `coordinationStore.ts:460-465` | CONFIRMED — 4 kinds, none is `intent` |
| §6 row 3: header comment reads "6 MCP tools" | `coordinationTools.ts:4` | CONFIRMED verbatim |

**One correction issued before release** (precedent: `ruling-20260730T0906Z-po-triage-po.md` STEP po-5 corrected the relay before releasing): §0/§6 omit that `coordination/index.ts` is a **P1-C sub-barrel re-exporting `registerCoordinationTools` from `../coordinationTools.js`** — it does NOT export per-tool registrars. `dispatchPreflightTool.ts` therefore needs **no** `index.ts` change. Without this note an architect would plausibly add a spurious barrel export.

**Verdict: RATIFIED.** `po_goahead_20260811T130245` stamped on the row (row-level, not `.head` — durable per-task audit trail per §Step 3 placement rule). `.head` repointed `ba` → `architect`.

### Q1 — cutover DoD sequencing. Ruled: **SPLIT. No same-wave doc cutover.**

DONE bar for UC-RDL-P4 = tool ships + registry synced (183→184) + live integration round-trip against the **running** container (all 3 branches: `claimed:true`, re-entrant self-held, peer collision). FR-7's file-6 edits (`CLAUDE.md`, `dispatch-claim/{CARD.md,SKILL.md}`) are **FORBIDDEN this wave**; they become linked follow-up `UC-RDL-P4B-DOC-CUTOVER`.

*Why not same-wave:* BA framed this as a blast-radius judgement. It is stronger than that — it is a **certainty, not a risk**. EC-10 makes the new tool inert until an ops-gated container rebuild. A same-wave doc flip would point always-loaded fleet-wide `CLAUDE.md` at a tool that is **absent from the running container**, breaking every agent's every spawn. The two constraints (doc-flip timing, rebuild gating) are not independent; BA analysed them separately in §5 Q1 and §5 Q2 and so did not surface that their conjunction is fatal.

### Q2 — rebuild sequencing vs `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS`. Ruled: **APPROVE BA's recommendation — coordinate the REBUILD, not the implementation.**

Build UC-RDL-P4 now in parallel; hold the `apps/mcp-server` rebuild until the sibling reaches `DONE_VERIFIED`, then one combined ops-gated rebuild covers both. EC-7 already proves zero functional dependency (verified live: this tool's only heartbeat call is the plain 2-arg renewal). Blocking implementation behind a `review[]`-lane row whose `next_agent` is `developer` would idle a P1 for no benefit.

---

## STEP po-2 — Inbox + report triage: dedup-first, 14 folds vs 6 mints

**What was considered:** mint per finding (29 envelopes + 11 rows + 88 reports ≈ 40 distinct findings) vs dedup-first against non-terminal lanes.

**Why dedup-first:** the board already carries 626 open rows; per-occurrence minting is the churn class `feedback_auditor_mcpserver_a21_a30_memory_fp_reemit_churn`. Scanned `backlog[]+ready[]+in_progress[]+review[]+qa[]` by **lane membership**, never a status-token enum (`FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY`: enum-based dedup was measured to miss 62.4% of open rows).

Result: **14 folds, 6 mints.** Findings that earned a mint, and why they were not folds:

1. **3 × CI bun-test rows.** MANDATORY pre-dedup failing-file read performed (`gh run view <id> --log-failed`, FAILEDFILE blocks parsed verbatim) — job names carry zero discriminating information. New files: `ALLZERO-OHLCV-FETCH.test.ts`, `FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts`, `1837a-pipeline-state.test.ts`. One row per distinct file. Anti-amnesty fence observed — **not** folded into `FIX-MCP-SUITE-HEALTH-BASELINE` (different plane; CI runs per-file isolation, green baseline 0 fail).
2. **`FIX-BCTC-ENRICHER-ZERO-URL-ALL-TICKERS-ACTIVE-WINDOW`.** `BCTC-ENRICHER-OLD-QUARTERS` covers pre-Q4-2025 only; this alert is ALL tickers in a *confirmed active* window. AC-0 is deliberately "re-verify it is still live first" — the alert has been silent >24h and may have self-resolved.
3. **`FIX-DEVTEAM-DRAIN-ROUTES-NON-PO-ENVELOPES-TO-PO-STEP0SIG`** — see po-3.
4. **`FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR`** — see po-4.

**All 5 size-lint ci_red signals folded, not minted:** every one of runs 31291780838 / 31293956930 / 31295786444 / 31298255179 / 31489910331 reports the identical single offender `apps/mcp-server/src/interface/mcp/transport.ts — baseline=126L actual=265L upper=138L`. File-scoped dedup hit on `FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L`. Recorded a title-drift correction on that row: it is **265L**, not the 237L in its own id.

---

## STEP po-3 — `routed_to` is a constant, not a routing decision

Not in any report; found by inspecting the envelope shape rather than trusting the summary. **All 29** durable-inbox envelopes carried `routed_to: "PO Step 0-SIG"` — including **12 (41%)** whose own `to` field named a different agent (claude-manager-helper ×6, agent-father ×2, dev-team ×4).

Consequence chain: none of those 5 types (`context_bloat_breach`, `notebook_single_section_overage_breach`, `notebook_tiebreak_direction_defaulted`, `cowork-fire`, `db_integrity_breach` to dev-team) appears in **either** `triage-signals.md` table, so they fall to the unknown-type catch-all *every tick*; PO logs-and-skips rather than clears; the inbox therefore **accumulates** — which is exactly how this batch reached 29 across several ticks. The "supervised-hold starved rotation" explanation given at spawn is real but is only half the cause.

Cross-check that made this decisive: ran `triage-signals.md`'s own `guard_signal_type_coverage` regression verifier live → **FAIL**, unrouted `to=po` types `["cron_fire_gap","system_issue"]`, both live right now. Folded onto `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` with a specific correction: that doc's Cold-archive paragraph explicitly dismisses underscore-`system_issue` as a "historical artifact, not a live routing gap" — **false as of today**, and left in place it will cause the next editor to re-close the gap as already-handled.

---

## STEP po-4 — Declined the offered bypass

The documented inbox-clear step aborted: `signal_total` counts `signal_queue.rows[]` + `pending_triage_inbox[]` jointly (42 = 13 + 29), so clearing a fully-consumed inbox drops it to 13 = 0.31, under the 0.5 floor.

**What was considered:** (a) `ORCH_APPLY_ALLOW_SHRINK=<reason>`, which the abort message itself recommends; (b) 4 sequential targeted subtractions; (c) leave the inbox uncleared.

**Why (b):** `scripts/orch-apply.sh:60-66` states that bypass is wired ONLY into `orch-cold-evict.sh` and `pm/flow/task-archive.md` and "NEVER set this from any other caller". PO is not an authorised caller. Taking the bypass because an error message suggested it is precisely `feedback_ops_silently_switched_to_destructive_fallback_without_reauth` and `feedback_telemetry_stale_sweep_must_use_documented_scoped_pattern`. Executed 42→28→21→14→13, **every write an enumerated `envelope_id` subtraction** (never `= []`, never a full-doc replace) — so the guard's actual invariant (no single write collapses the doc) held on all four.

(c) rejected: leaving it uncleared re-presents 29 dispositioned envelopes to every future tick.

**The workaround is not the fix.** Minted `FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR`: the failure mode is **self-reinforcing** (larger inbox → lower ratio → harder to clear → more pressure toward the forbidden bypass), and the abort message actively recommends a path most callers are forbidden to take. Proposed shape for architect to ratify: a **queue** whose purpose is to drain to zero is the wrong subject for a magnitude-conservation invariant — exclude `pending_triage_inbox` from the ratio and give it a row-identity guard instead, leaving `signal_queue.rows[]` (an accumulating log) unchanged.

---

## STEP po-5 — Refuted two live alert classes at source

- **`[audit-output-contract] BUG: signals_posted=N but dashboard_rows=0`** ×2 (12:13:20Z, 12:18:25Z). **Both false.** Read `.signal_queue.rows[]` live at 12:56Z: the rows those cycles posted are present — `sys-20260811T121235-33fd`, `sys-20260811T121737-2a33`, `sys-20260811T121748-5da0`. `dashboard_rows` was demonstrably not 0 in either window. Confirms `FIX-AUDIT-OUTPUT-CONTRACT-V3-DASHBOARDROWS-NO-INDEPENDENT-CROSSCHECK`.
- **D4 orch-state/lock diverge ×6.** Verified via `task_list_held` rather than assumed stale: all three `bctc-dataquality:*` locks genuinely exist (owner `bctc-analyst`, ttl 691200s, expiring 08-14/15). Two corrections folded onto `FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX`: (1) the live prefix is `bctc-dataquality:`, **not** the `data-quality-anomaly:` in that row's own title — a whitelist written to the titled prefix would not suppress these; add `esc-datacov:` too. (2) the `active=X held=Y` variant compares `.head.active_task_id` against *every* held lock and is a false positive **by construction**, a separate defect from the whitelist gap.

**Deliberately not re-escalated:** telegram 4561 (`[ops-diagnostics]` CRITICAL, 23/90 crons stopped 08-07). Today's A-29 reads 8 stale / 1 missed / 72 on-time — largely self-recovered. Noted instead that 72+8+1 = 81 ≠ 90, a 9-cron accounting gap that is itself evidence of `FIX-AUDITOR-A29-UNEXECUTABLE-SPEC-SILENT-JOIN-DROP`; neither number is trustworthy until that row lands.

---

## STEP po-6 — One cross-plane linkage worth more than either symptom

Newly-minted CI row `FIX-CI-BUNTEST-FOREIGN-FLOW-MISSING-TRADING-DAY-NO-BACKFILL` is a RED test about a missing foreign-flow trading day. In the same window sla-monitor reported `foreign_flow stale 3900min` (65h, 08-10T02:00Z) and `1020min` (17h, 08-11T02:00Z).

The sibling price/news breaches were folded into `FIX-SLA-MONITOR-MARKET-OPEN-BOUNDARY-DETERMINISTIC-DAILY-BREACH` (they fire at exactly 02:00:03Z, the pre-open boundary artifact that row characterises). The foreign_flow pair was **carved out and NOT folded**: a boundary artifact cannot manufacture a 65-hour gap. Cross-linked to the CI row instead — plausibly one defect with two symptoms across the CI and production planes, which is a different and cheaper investigation than treating them as two.

Same reasoning produced the pdf-extractor linkage: A-30 ESCALATE at 94.07% memory in the same window as 16 `RECONCILE EXHAUSTED` rows all reporting "0 rows" and all advising "inspect pdf-extractor logs". Folded onto `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` as a candidate single root cause. New sub-finding surfaced there: **the reconcile cap is not enforced** — three alerts report 9, 9 and 11 passes against their own stated "cap 8", burning a memory-starved service on rows that 14/16 report_ids suggest (`fallback-<TICKER>-<Y>-<Q>`, see `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE`) can never succeed.

---

## STEP po-7 — Manual-dispatch sweep

`FIX-PO-MANUAL-DISPATCH-SKIP-STAMP-FIELD-MISMATCH-STARVES-SWEEP` is still `BACKLOG` (unfixed since 08-08). Top unflagged candidate re-confirmed: `FIX-TNB-AUDIT-STEPS-ASSUME-NONEXISTENT-CHANNEL-PARAM` — `ready[]`, `next_agent: agent-father`, off the DRS ratified allowlist, no skip-stamp key on the row, now **21 days** unactioned. Folded into this tick's BATCH as an UNBLOCK rather than left to the starved sweep.

TNB audit itself: `docs/handoffs/tnb-audit-latest.md` is unchanged since 2026-08-08 and already carries a PO ACK from 2026-08-08T21:01:34Z. No new findings; 3 days stale, coincident with the 08-09→08-11 weekly-API-limit outage. No new ACK appended — appending one would falsely imply a fresh audit was read.
