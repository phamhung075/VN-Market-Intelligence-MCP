# Decision Journal — Sprint SYSTEMIC-REMAKE-P1 · dev-mcp-server

**Sprint goal:** Systemic remake P1 — root-cause fixes (idle-loops→verif→detector) per 07-04 systemic review.
**Agent:** dev-mcp-server
**Started:** 2026-07-04T00:00:00Z

---

### STEP dev-mcp-server-S1 · dev-mcp-server · 2026-07-04T00:00:00Z
**task-id:** FACTORY-INTERFACE-confidence-score-50-mask
**what-done:** Grepped apps/mcp-server for `confidence_score ?? 50` / `confidenceScore ?? 50` in the /api/signals/stock handler — zero matches. stockSignalsHandler.ts:224 already reads `row.confidence_score ?? null` (FIX-SIGNAL-CONFIDENCE-DEFAULT-50 marker present in comments).
**what-considered:**
- Assume stale detector output and implement anyway (rejected — would fabricate a diff on already-correct code)
- Search for a second/older stock-signals route that might still carry the mask (none found — server.ts:1359 has exactly one `/api/signals/stock/:code` route, delegating to this same handler)
- Verify via git history that the mask was already removed (confirmed: commit e3386bdfa "TASK-CONF-1 remove DEFAULT-50 confidence mask")
**why-decision:** Code, tests, and git history all converge on: this exact mask was fixed by a prior task (FIX-SIGNAL-CONFIDENCE-DEFAULT-50 / TASK-CONF-1). Editing would be a no-op diff or risk introducing churn on a correct file.
**why-change:** No implementation change made — reporting NO-CHANGE-NEEDED per detection-never-ran ≠ failed-fix policy. Ran scratch behavior-proof (absent confidence_score → null) + existing regression suite (47 pass) + `bun tsc --noEmit` (exit 0) to confirm current state is honest, not to fix a live bug.

### STEP dev-mcp-server-S2 · dev-mcp-server · 2026-07-07T17:50:00Z
**task-id:** CI-RED-c5b5f885-FIX
**what-done:** Pulled real GH Actions logs (run 28689707086 + 2 prior runs) — 1410-tool-diacritics-sweep.test.ts failed 3/3, 262-mcp-tools-042.test.ts 2/3. Both call getClimateRiskSignals/getEnergyGridStatus with no DI, hitting real network (weatherVn/hydrologicalData fetchers, 15s axios timeout > bun-test 5s default) under CI's 16-way parallel isolation. Mocked both fetcher modules (freeze-before-mock + afterAll-restore pattern, matched 1355b precedent); un-skipped 262's 2 previously-flaky energy tests. Verified isolation + together-with-siblings (257/258/259, both orders) + full-suite via ci-per-file-isolation.sh (no regression vs before-fix baseline). Committed 1efb6f918, pushed; CI run 28886901289 = success (bun test job = success).
**what-considered:**
- `.it.skip` both flaky cases (matches existing 262 precedent) — rejected as incomplete: doesn't cover 1410's real culprit, leaves 262 cases 1-3 (weatherVn) still real-network-flaky
- Add httpClient DI param to climateTools/energyTools (broader prod-code refactor) — rejected: out of "minimal targeted fix" scope, touches interface layer unnecessarily
- mock.module() the 2 fetcher modules in both test files, un-skip 262's dead tests — chosen: root-causes both files, matches repo's own established mock.module + afterAll-restore convention, zero prod-code touched
**why-decision:** Empirically probed (scratch reproduction) that mock.module() must precede the static climateTools/energyTools import and that afterAll-restore needs a value-copy (not a live import-binding alias) to actually protect sibling files — verified both hold before applying to the real files.
**why-change:** 183-alert-accuracy.test.ts (failed 1/3 runs) is unrelated (no network dep) — left untouched, out of scope. Local plain `bun test` (bare, no isolation) surfaced ~62 unrelated fails + a Bun-engine crash; disregarded as non-authoritative per the isolation script's own "NEVER bare bun test" comment and its scope leak into src/_deprecated/ — used ci-per-file-isolation.sh (CI's actual mechanism) as the real gate instead.

### STEP dev-mcp-server-S3 · dev-mcp-server · 2026-07-07T18:20:00Z
**task-id:** DATA-BACKFILL-PRICES-20260706-MONDAY-GAP
**what-done:** Backfilled all-ticker 2026-07-06 daily_ohlcv gap (Cloudflare Tunnel outage). Standard recovery (INSERT ohlcv_backfill_queue trigger, precedent c94b58da4, row id=559) confirmed BROKEN: VPS poller calls /api/ohlcv-backfill-done repeatedly with zero /api/push-ohlcv-history calls in container's entire uptime — VPS-side script not executing (separate infra defect, flagged to bug channel, out of zone to fix). Diagnostic probe found direct VNDirect fetch from inside the mcp-server container itself succeeds now (stale "geo-blocked" comment in ohlcvHistoryBackfillJob.ts no longer holds) — wrote scripts/migrations/backfill-ohlcv-gap-2026-07-06.ts, reusing writeOhlcvBatch SSOT path, targeting the 583 codes with real rows on 07-03+07-07 but missing 07-06, plus VNINDEX via vnmarket_prices. Ran --apply: 585 rows written, 0 fabricated, 6 transient SQLITE_BUSY retried clean.
**what-considered:**
- Wait for VPS systemd timer (30 min cadence) to self-heal — rejected: proved broken via log evidence (0 push-ohlcv-history lines), would spin forever
- Use priceBackfillService.backfillPrices() — rejected: fetchOhlcvData() inside it is an explicit Math.random() mock/stub, would fabricate data (NO FAKE DATA violation)
- Direct VNDirect fetch + SSOT write reusing ohlcvWriteService.writeOhlcvBatch (chosen) — real data, zero duplicated write/normalization logic, idempotent backfill conflictStrategy
**why-decision:** Live curl probe from the container proved VNDirect reachable + per-ticker + accurate before writing any code; verified against VCB/FPT/HPG/VNM/VIC (distinct, plausible, non-duplicate values) before declaring done.
**why-change:** Plan assumed VPS-queue mechanism would work (matches all prior precedent) — pivoted after log evidence showed it silently non-functional; this is a genuinely new, separate finding from INFRA-CLOUDFLARE-TUNNEL-OUTAGE-ROOT-CAUSE.

### STEP dev-mcp-server-S4 · dev-mcp-server · 2026-07-07T20:00:00Z
**task-id:** KD-OBS-01-FIX
**what-done:** Confirmed 5 kinhDichTools.ts catches + 3 kinh-dich route handlers (reading/signals/market) caught genuine DB/HTTP errors with logger-only, no Telegram. New `kinhDichErrorNotify.ts` (`notifyKinhDichError`, dynamic-import `sendTelegramBug`, non-fatal, 📋-category dedup reuses sendTelegramBug's built-in 4h window) wired via injectable `notifyError` param (defaults real) into all 8 catch blocks. Left `appendMarketHexagram`/`appendStockHexagram` (marketTools.ts) untouched — genuinely benign "service unreachable, omit block" by design, not silent-drop.
**what-considered:**
- mock.module() the telegram/HTTP-client modules for full integration coverage of all 5 tool catches — rejected: worker-scoped mock.module leak risk (flagged precedent in this repo); found a zero-mock deterministic trigger instead (DB_PATH→directory forces getDb() throw on the initDatabase() call every tool hits first)
- Thread deps through registerKinhDichTools/handlers via a broader deps object — rejected: single optional trailing param (matches accuracyDigestJob.ts/handleForeignFlow precedent) is minimal and non-breaking (2 real call sites, both grepped)
- Route to sendTelegramWork vs sendTelegramBug — chose BUG: every existing genuine-error precedent in this codebase (ohlcvBackfillHandler, tasksMdJanitorJob, bctcPdfPullJob, etc.) uses BUG; WORK is reserved for routine business content
**why-decision:** New KD-OBS-01-FIX-kinhdich-bug-notify.test.ts (11 tests, 43 expect) proves the wiring end-to-end per catch block via injected spy + deterministic DB-path-is-a-directory trigger — no reliance on live kinh-dich-service state.
**why-change:** No plan deviation. Targeted suite (kinh-dich + registry, 197/197) + full `bun test` (14290/14393, 63 pre-existing fails matching 2026-07-03 documented baseline, none kinh-dich-related) + tsc clean + server boot (toolCount=183 unchanged, health 200, dashboard route 200) all green.

### STEP dev-mcp-server-S5 · dev-mcp-server · 2026-07-08T01:35:00Z
**task-id:** CI-RED-0d28104a-FIX
**what-done:** Mocked hydrologicalData.js/fetchReservoirLevels in DSI-S3-sector-fin.test.ts (freeze-before-mock + afterAll-restore, copied verbatim from proven 1efb6f918 pattern) — 3rd recurrence of the same live-network-races-5s-timeout class, root cause confirmed via `gh run view 28910244855 --log-failed` on the exact commit 0d28104ac named in the task.
**what-considered:**
- Inject an HttpClient param through getEnergyGridStatus for full DI — rejected: larger surface change than task scope; precedent 1efb6f918 already established mock.module as the accepted fix shape for this exact class
- Mock at energyTools.js boundary instead of hydrologicalData.js — rejected: precedent mocks one level down (the fetcher), keeps getEnergyGridStatus's own reservoir-aggregation/estimate-labelling logic genuinely under test
- Fix 257/258 too (task's sweep instruction) — investigated first: both always pass an injected client to the fetcher, never reach the live-network branch, so mocking them would be a no-op; journaled as verified-out-of-scope, not silently skipped
**why-decision:** `gh run view --log-failed` on the actual failing CI run (not a guess) named DSI-S3-sector-fin.test.ts as the sole failed file; `scripts/ci-per-file-isolation.sh 16` (CI's real mechanism) re-run post-fix shows it absent from the 12 remaining (pre-existing, unrelated pollNews/RSS) FAILED FILES.
**why-change:** No plan deviation — task instructions matched the codebase's own established precedent exactly; only genuinely new finding was confirming 257/258 don't need the fix (DI already present), which the task asked to explicitly decide either way.

### STEP dev-mcp-server-S6 · dev-mcp-server · 2026-07-08T02:53:00Z
**task-id:** FACTORY-INTERFACE-sequential-confidence-05-mask
**what-done:** `sequential-market-analysis.ts:170` `result.confidence = input.confidence ?? 0.5` → only assigns when `input.confidence !== undefined`; init default changed `confidence: 0` → `undefined`; type `number` → `number | undefined`. Also fixed `generateRecommendations` mislabeling undefined confidence as "Low confidence" (same fabrication class one call deeper).
**what-considered:**
- Verified `AnalysisResult`/`confidence` is genuinely internal — `handle()` only returns `{status,thought,progress,nextSteps}`, never the result object; DoD's "served payload" language doesn't map to a live HTTP surface for this field today
- To make the fix testable added `_analysisState` (Map) as a test/introspection-only property on the returned tool object — additive, not consumed by `registerSequentialMarketAnalysisTools`/MCP SDK, zero risk to the real contract
- Leave prior real confidence intact if a later revision's hypothesis omits confidence (vs resetting to undefined) — matches "never fabricate," a real stated value shouldn't be nulled just because a later message didn't restate it
**why-decision:** DoD requires proof that omitted confidence stays undefined/null and unchanged-when-supplied; `_analysisState` introspection is the only way to observe this given the current return contract, so it's the minimal safe path to a real (non-vacuous) test.
**why-change:** Widened 1L beyond the literal "line ~170" pointer to also fix `generateRecommendations`'s undefined-confidence branch — same fabrication bug, same file/function, required for `number | undefined` to type-check cleanly anyway (exactOptionalPropertyTypes).

### STEP dev-mcp-server-S7 · dev-mcp-server · 2026-07-08T04:30:00Z
**task-id:** FACTORY-INTERFACE-source-confidence-10-mask
**what-done:** `finalizeBctcRefineTool.ts:398 row.source_confidence ?? 1.0` — investigated ground-truth first: `parseRefinedMarkdown` always computes a REAL per-row confidence (never absent), and existing `DV-HC-SC` suite + live DB (0.1×380, 0.4×2 rows found) already prove real values persist unchanged — the `?? 1.0` branch was provably unreachable dead code, not an active mask. Hardened anyway per required discipline: typed local row-shape `source_confidence` honestly as `number | undefined`, extracted INSERT-boundary resolution into exported `resolveSourceConfidence()` (propagates real value incl. edge-case 0/1.0 unchanged; falls to schema default 1.0 ONLY when genuinely undefined, NOT NULL preserved).
**what-considered:**
- Make the column nullable to let `undefined` flow straight to SQLite — rejected: explicit hard constraint in task, would relocate the mask into a different failure mode
- Skip the task as NO-CHANGE-NEEDED (mirrors S1 precedent) — rejected: unlike S1 (mask already provably removed by a NAMED prior commit), here the `??` literally still exists in the diff; the correct move is to hardstructurally close the anti-pattern + document the finding, not silently no-op
- Reproduce the "parser-absent" case through the real pipeline for an end-to-end test — rejected: parser genuinely never omits it (by design), so this would require fabricating an artificial parser defect; tested `resolveSourceConfidence`'s own honest `number|undefined` boundary directly instead
**why-decision:** DoD requires test coverage for BOTH "parser-provided" and "parser-absent → documented fallback" — only achievable by giving the resolver its own honest optional-typed unit boundary, since the full pipeline cannot produce the absent case today.
**why-change:** RAW-verify finding differs from the sibling's premise: current live DB rows already show non-1.0 confidence (0.1/0.4) even under the OLD pre-fix code — documented explicitly rather than claimed as "fixed a live bug," per no-fabrication policy. tsc clean; targeted+adjacent (HC-human-confirm/AR-parser-dv/W2-ROW-REPAIR/FU-5b/BANK-AWARE-1/FU-6f + new file) 167/167 pass; full `bun test` 14312 pass/58 fail/3 errors/1177 files (fail set = pre-existing VPS-push/RSS/insider/foreign-flow network-flaky class, zero overlap with changed files, matches S6's documented baseline). Image rebuilt (35c8117c1f85) but NOT swapped (still 180382145ee7) — ops-gated.

### STEP dev-mcp-server-S8 · dev-mcp-server · 2026-07-08T10:00:00Z
**task-id:** FACTORY-SCHEDULER-alert-confidence-literals
**what-done:** Replaced 4 frozen confidence literals (foreignFlow 0.75, insider 0.85, BB 0.65, TA 0.7) with deriveConfidenceFromStrength() (new domain/services/alertConfidenceScorer.ts) fed by each job's real in-scope magnitude; new base/ceiling consts in alertThresholds.ts per job.
**what-considered:**
- Per-job inline `base + magnitude*scale` (matches leadershipSignal.ts precedent) — rejected: duplicates formula 4x, harder to unit-test the mapping in isolation
- mcpConfig alertConfidence group — rejected: pure-function fit cleanly, no env-tunability need yet, alertThresholds.ts is already the canonical "alert scan job tunables" home
- Shared linear scorer in domain/services + per-job base/ceiling consts in alertThresholds.ts — CHOSEN
**why-decision:** BB/TA had no pre-existing magnitude, so new pure extractors were needed anyway (bbBreakoutStrength.ts: band-penetration ratio; rsiExtremityStrength.ts: distance past 70/30 threshold toward saturation) — TA deliberately uses RSI-extremity, NOT the job's unused `macd` field, to avoid fabricating reliance on an unrelated metric never otherwise read by this job.
**why-change:** no change from backlog note — used the "domain/services pure scorer" branch of the OR, not mcpConfig; foreignFlow/insider reused their pre-existing evidence-fragment magnitude (min(1,|totalNetVolume3d|/500k) / min(1,buyDays/10)) verbatim for confidence too, so the persisted magnitude and confidence now always agree for the same signal.

### STEP dev-mcp-server-S9 · dev-mcp-server · 2026-07-08T11:07:00Z
**task-id:** FACTORY-SCHEDULER-prediction-default-dedup
**what-done:** `predictionMarketJob.ts` Step 6 hardcoded volumeSpike=50000/probShift=5/minWallets=10 twice (try + catch). Extracted `DEFAULT_PREDICTION_SIGNAL_CONFIG` (named, provenance-commented) + `resolvePredictionSignalConfig(pm?)` — both branches now call the same function; catch branch calls it with no arg (all defaults), try branch passes the loaded `pm` slice.
**what-considered:**
- Literal `{ ...DEFAULT, ...(pm ?? {}) }` spread (backlog's suggested example) — rejected: unsafe if `pm` ever had an explicit-`undefined` field (spread would clobber default with undefined); prior `??`-per-field semantics wouldn't survive verbatim
- Per-field `pm?.field ?? DEFAULT.field` resolver function (chosen) — provably identical resolved values to the old duplicated code in all cases, and independently unit-testable
- config.ts:604 `PredictionMarketsConfig` already declares these 3 fields required/non-optional with matching defaults (50000/5/10) — nothing to change there, already "fits cleanly" per backlog's own hedge
**why-decision:** DoD requires "resolved threshold values UNCHANGED" as a hard pure-refactor constraint; per-field `??` merge is the only pattern that's mechanically provable identical to the two prior literal blocks, not just usually-equivalent.
**why-change:** no change from plan other than the merge-mechanics judgment call above (function documented in backlog as "guidance not rigid spec"). tsc clean; targeted+adjacent (8 prediction-related files) 116/117 pass (1 unrelated pre-existing flaky timeout in 1345e, passes in isolation); full `bun test` 14344 pass/40 skip/58 fail/4 errors/1179 files (568.93s) then Bun 1.3.13 crash-at-teardown (known engine bug) — zero overlap between the 58 fail files and predictionMarketJob.ts/config.ts/predictionSignalDetector.ts (grepped), matches the same pre-existing VPS-push/RSS/insider/foreign-flow network-flaky class documented in S7/S8. Server boot verified clean on PORT=3999 (port 3000 held by the live container) — health/bctc-inspect/news-fetch-dashboard all 200, toolCount=183 unchanged.

### STEP dev-mcp-server-S10 · dev-mcp-server · 2026-07-08T12:05:00Z
**task-id:** FACTORY-INTERFACE-extract-finalizeBctc-usecase
**what-done:** buildFinalizeBctcRefineHandler (~1100L, own header already said "DDD layer: application") lived in finalizeBctcRefineTool.ts (interface). Extracted BLOCK-1..5 to application/usecases/finalizeBctcRefine/{backfillScalarColumns,deriveRatioColumns,revalidateBalanceIdentity,recomputeExtractionConfidence,recomputeBctcEval}.ts + the rest of the orchestration (CONFIRMED guard, parse loop, DT-2/3/4, BEQ-7, single db.transaction, BAL-1c/1d) into finalizeBctcRefine.ts. Interface file now only Zod schema + validation + response-wrap + server.tool registration (1349L → 102L).
**what-considered:**
- Keep the whole ~1100L body as one application function, ignore the "<=120L per extracted step" DoD line — rejected: DoD explicit, BLOCK-1 alone is ~300L raw
- Decompose BLOCK-1..5 into private sub-helpers (one call-site each) inside their own file so every exported+private function is <=120L — CHOSEN
- Application-layer function returns the full MCP `{content:[...]}` shape itself (interface becomes a 1-line passthrough) vs. application returns a plain result object and interface wraps once — CHOSEN the latter: every original return point used the byte-identical wrapper, so this is a provably no-op formatting factor, and keeps MCP-response-shape ownership in the interface layer (matches DDD + the DoD's explicit "interface keeps the Zod schema... and a thin orchestration call" wording)
**why-decision:** DoD requires recomputeExtractionConfidence output IDENTICAL before/after — verbatim-copy + call-order-preserved + same-wrapper-shape is the only way to make that provable rather than assumed.
**why-change:** no change from plan; BAL-1c/BAL-1d aren't in the named-5 list but had to leave the interface file too (DoD: interface keeps ONLY schema+registration+thin call) — kept as 2 small private helpers inside finalizeBctcRefine.ts rather than their own files, since they're not part of the named seam list. tsc clean; targeted finalizeBctcRefine-related suite (22 files) 293/293 pass; eslint clean on all 8 touched files. Full `bun test` run in progress at journal-write time — see review_note for final tally.

### STEP dev-mcp-server-S11 · dev-mcp-server · 2026-07-08T13:41:00Z
**task-id:** FACTORY-DOMAIN-extract-bctc-parsing-lib
**what-done:** Created financial-reports/lib/lineScan.ts (canonical findValue/findValueByCode/scaleNumericFields/detectDivisor) + lib/vasPatterns.ts (shared P_TOTAL_ASSETS/LIABILITIES/EQUITY_TOTAL). Migrated balanceSheetExtractor/cashFlowExtractor/incomeStatementExtractor/bctcScalarAggregator ONE AT A TIME, each with tests green before the next.
**what-considered:**
- findValue 2-arg (balanceSheet, row-code guard, no ASCII fallback) vs 4-arg (income/cashFlow, ASCII fallback, no guard) — NOT the same function; reconciled via opt-in `{rowCodeGuard, fallback}` options object so each call site reproduces its exact pre-migration behavior (verified: per-extractor targeted suites all match pre-change baseline pass counts exactly)
- applyMultiplier: balanceSheet+cashFlow both hand-scale every leaf field unconditionally (verified against bctc-schema.ts: both types are 100% numeric, no exceptions) → generalized to recursive `scaleNumericFields`. incomeStatementExtractor has NO applyMultiplier (deliberately excludes eps/dilutedEps) — left untouched, not forced into the generic scaler
- bctcMagnitudeValidator's inline total-assets/liabilities/equity label regexes vs balanceSheetExtractor's named VAS patterns — REJECTED merging: different data shape (cleaned bctc_table_rows labels + English fallback vs raw-OCR fuzzy diacritic text); forcing a shared regex would silently WIDEN BLOCK-severity fraud-detection matching. Left local, documented with pointer comment only (mirrors S10's "pure relocation, not a dedup pass" precedent)
- bctcScalarAggregator's own findByCode/findByLabel/findTotalAssetsCorporate (row-based) — NOT touched; different data shape than extractors' findValue/findValueByCode (raw OCR lines), same name is a false-cognate not a duplicate
**why-decision:** DoD requires the 2-arg/4-arg reconciliation be "verified value-preserving, not a silent behavior change" — the opt-in options object is the only design where each of the 3 call sites is mechanically provably identical to its prior local implementation, not just usually-equivalent.
**why-change:** no change from plan; the "detectDivisor" named in DoD turned out to already exist verbatim in bctcScalarAggregator (row-based, distinct from the 3 extractors' own per-statement magnitude-inference blocks — those were deliberately NOT unified, same "reconciliation not a straight merge" principle). tsc clean; eslint clean (7 touched/created files); per-extractor targeted suites all match pre-change baselines exactly (186/186, 93/93, 121/121, 147/147, 17/17); combined 42-file BCTC suite 411 pass/4 skip/0 fail — IDENTICAL to the pre-change baseline run. Full `bun test` + server-boot/tool-count/scheduler-count gates run separately — see review_note for final tally.

### STEP dev-mcp-server-S12 · dev-mcp-server · 2026-07-08T14:35:00Z
**task-id:** FACTORY-DOMAIN-relocate-stock-catalog
**what-done:** stockAliases.ts (828L) embedded the 66-ticker STOCK_CATALOG reference table (lines 80-629, ~560L) intermixed with the detection functions. Cut STOCK_CATALOG+StockCatalogEntry verbatim via `sed` (diffed byte-identical against the original lines before deleting them) into a new pure-data module `domain/services/stockAliases/catalog.ts` (618L incl. size-justification header, zero imports). stockAliases.ts (828L → 265L) now imports the table and re-exports it unchanged for the existing consumers (stockSearch.ts, newsNormalizer.ts, domain/services/index.ts barrel) — zero import-path changes at any call site. normalizeText, both NORMALISED_ALIASES/NORMALISED_CONTEXT_GUARDS derived-map IIFEs, and all 5 detection functions stay in stockAliases.ts, which now carries its own size-justification header explaining the remaining 267L (maps + normaliser must stay beside the functions that close over them as private state).
**what-considered:**
- Directory `stockAliases/` coexisting with file `stockAliases.ts` in the same folder — verified no other file in the repo has this same-name file+dir pattern (checked via a find-loop first), then verified it is safe anyway: every import in this repo uses an explicit `.js`-suffixed specifier (bundler moduleResolution) — `./stockAliases.js` and `./stockAliases/catalog.js` are unambiguous, unlike a bare `./stockAliases` that Node would resolve LOAD_AS_FILE-first
- Split location suggested by the backlog note (`domain/services/stockAliases/catalog.ts`) vs `packages/shared-config` (the backlog's OR-alternative) — chose the former: STOCK_CATALOG has exactly one real consumer family (stockAliases.ts's own detection logic + stockSearch.ts), a cross-package shared-config module would be premature abstraction for a single-service concern
- Re-export STOCK_CATALOG/StockCatalogEntry from stockAliases.ts vs updating the 2 external consumers (stockSearch.ts, newsNormalizer.ts) to import from the new module directly — chose re-export: zero call-site churn outside the one file the task scoped, and the domain/services/index.ts barrel already does `export * from "./stockAliases.js"` so nothing else needed touching
**why-decision:** DoD requires "normalised maps build identically... behavior unchanged, verified (not just claimed)" — same class as the immediately-preceding FACTORY-DOMAIN-extract-bctc-parsing-lib sibling. Wrote a scratch script that imports BOTH the pre-move module (`git show HEAD:...stockAliases.ts` copied standalone, self-contained pre-change) and the post-move module side by side, and diffs STOCK_CATALOG itself plus getAliasesForCode/getCompanyName/detectStocksInText/tickerWholeWordMatch/stripSourceAttributionSuffix output across all 66 tickers + a battery of realistic/context-guard sentences (incl. the DPM "phu my" guarded-vs-unguarded case) → 0 mismatches. This is provably stronger than "tests pass" since NORMALISED_ALIASES/NORMALISED_CONTEXT_GUARDS are private (not directly comparable) — comparing their observable effects through every public function closes that gap.
**why-change:** no change from plan. tsc clean; targeted alias suite (14 files) 211/211 pass; consumer suites (news-normalizer/cascade-engine/stock-search/domain-services/legal-risk/deep-fetch-gate) 167+28 pass; eslint clean on both files. Full `bun test`: 14339 pass/40 skip/63 fail/5 errors/1179 files (566.88s) then Bun 1.3.13 crash-at-teardown (known engine bug, non-authoritative) — failing-file set 100% the documented pre-existing VPS-push/RSS/news-poll/insider/foreign-flow/chromium-timeout flaky class (grepped: zero "alias"/"catalog"/"STOCK_CATALOG" mentions anywhere in the failure output). Went one step further than S10/S11: temporarily restored the pre-change stockAliases.ts (git HEAD) and re-ran the 5 pollNews-adjacent files that appeared in the fail set, in isolation — the UNCHANGED baseline ALSO fails them (11/34 vs 7/34 post-change; the run-to-run count delta is itself the flakiness signature — chromium binary missing + 5s timeout budget under load — not alias-detection related), confirming pre-existing rather than a regression. Restored the real files after the probe and re-ran targeted suite (220/220) + tsc + the equivalence script + server boot — all green again. Server boot verified PORT=3999: health/bctc-inspect/news-fetch-dashboard all 200, toolCount=183 unchanged, scheduler cron.schedule grep=3 unchanged. Also updated docs/protocols/janitor-procedures.md's canonical-source pointer for STOCK_CATALOG to the new file path. Commit: cec87c726 (2-file relocation + doc pointer). Board: `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true`.

### STEP dev-mcp-server-S13 · dev-mcp-server · 2026-07-08T16:20:00Z
**task-id:** FACTORY-APP-dedup-date-freshness-helpers
**what-done:** midnightVietnamAsUtc/todayVietnam/parseAffectedCodes/isPriceFresh were duplicated verbatim in assembleBriefing.ts (406/429/590/613) and assembleEveningSummary.ts (238/259/271/316); todayVietnam duplicated a 3rd time in assembleAlertDigest.ts using a bare `7*3_600_000` literal instead of VN_OFFSET_MS. Created 3 new homes: `domain/services/timeHelpers.ts` (midnightVietnamAsUtc/todayVietnam — sibling to timeConstants.ts since that file's own header forbids functions), `domain/utils/affectedCodesParser.ts` (parseAffectedCodes — pure JSON parse, no I/O, same layer as sibling sqlHelpers.ts/safeQuery.ts), `application/utils/priceFreshnessGate.ts` (isPriceFresh(db, logLabel) — takes the per-caller log label as an arg instead of duplicating the whole function; may import infrastructure/logger.js same as sibling windowPartitioner.ts). Added named `PRICE_FRESHNESS_MS = 24*MS_PER_HOUR` to timeConstants.ts (mirrors the existing VN_INDEX_FRESHNESS_MS pattern), replacing the bare `<=24` literal in both isPriceFresh copies. All 3 usecase files' local copies deleted, imports wired in.
**what-considered:**
- isPriceFresh home: domain/utils (matches safeQuery.ts's db+I/O precedent) vs application/utils — chose application/utils because isPriceFresh calls `logger.warn/error` (infrastructure/), and domain/utils/safeQuery.ts deliberately avoids logger imports to preserve the domain-never-imports-infrastructure rule (uses console.error instead); application/utils/windowPartitioner.ts already imports infrastructure/logger.js, confirming that layer is the correct, precedent-matching home
- Behavior-preservation for the `<=24` → PRICE_FRESHNESS_MS swap: kept the exact pre-existing `ageHours` (rounded) comparison — `ageHours <= PRICE_FRESHNESS_MS / MS_PER_HOUR` — rather than switching to a raw `ageMs <= PRICE_FRESHNESS_MS` comparison, because the rounding changes edge-case behavior (e.g. 24h29min rounds to 24h and was previously "fresh"; a raw-ms comparison would flip that specific edge to stale) — DoD requires behavior unchanged, not just equivalent-in-the-common-case
- parseAffectedCodes destination "shared db/domain helper" (DoD wording) — chose domain/utils (not infrastructure/db) since the function does zero I/O/DB access, only JSON parsing of a value already read from a DB column; naming/layer matches the existing sqlHelpers.ts precedent in the same directory
**why-decision:** DoD requires "one home per helper (date fns in timeHelpers, not timeConstants)" and "behavior unchanged verified (not just claimed)" — matched each helper to the DDD layer its actual dependencies (I/O vs pure) require, and proved the freshness-threshold swap byte-for-byte behavior-identical via the derived-constant division rather than a raw-ms rewrite.
**why-change:** no change from plan. New test FACTORY-APP-dedup-date-freshness-helpers.test.ts (15 assertions: timeHelpers date format/VN-offset correctness, parseAffectedCodes both formats + null/invalid-JSON/non-array, isPriceFresh fresh/stale/no-rows + PRICE_FRESHNESS_MS constant + distinct per-caller labels). tsc clean. Targeted suite (14 files incl. all 3 usecase call-sites) 206/206 pass. Full `bun test`: 14346 pass/40 skip/71 fail/10 errors/1180 files (612s) then the same known Bun 1.3.13 crash-at-teardown as S10-S12 — grepped the full 22611-line log for every touched/created filename (assembleBriefing/assembleEveningSummary/assembleAlertDigest/timeConstants/timeHelpers/affectedCodesParser/priceFreshnessGate): zero mentions anywhere in fail/error output; all 71 fails are the same documented pre-existing pollNews/VPS-push/RSS-timeout + shared-DB-race flaky class (Task 125's one briefing-adjacent fail is a pollNews 5000ms network timeout, not an assembleBriefing assertion — same test passed 0-fail in the isolated targeted run). Server boot verified PORT=3998: /health 200, toolCount=183 unchanged. scheduler cron.schedule grep=3 unchanged (no scheduler files touched — gate run for evidence completeness only). Simplicity gate: PASS (Q1 clean — no extra flags/branches; Q2 clean — every new module has 2-3 real call-sites, none single-use; Q3 clean — flat extraction, no new indirection layers; Q4 clean — net -172L of duplication removed). Commit: dbb87db26. Board: `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (application-layer usecase change runs against live briefing/evening/alert-digest cron paths).

### STEP dev-mcp-server-S14 · dev-mcp-server · 2026-07-08T19:25:00Z
**task-id:** FACTORY-APP-split-fetchParseAndStoreBctc
**what-done:** fetchParseAndStoreBctc.ts (895L) split per backlog approach into `bctc/resolvePdfText.ts` (Step 2 PDF download + OCR-cache fallback + the news-chain-fallback terminal decision, returns a `{status:"text"|"final"}` discriminated union so the orchestrator either continues to Step 3 or returns immediately) and `bctc/newsChainFallback.ts` (tryNewsChainFallback + buildFiscalPeriod + buildAnalysisSummary). Additionally split out `bctc/types.ts` (QuarterString/InsertAnalysisFn/FetchParseAndStoreBctcParams, re-exported from the orchestrator for backward-compat imports in checkSscReports.ts/reports.ts/bctcVpsIngestHandler.ts/tests) and `bctc/insertBctcAnalysis.ts` (Step 4 sector-lookup + LanceDB embed + getDefaultInsertAnalysis) — needed beyond the 2 files the backlog named because the orchestrator DoD is a hard "<=120L", not "or justified", and the 2-file split alone left ~191L. Orchestrator is now 117L. Hoisted the 4 tuning literals into named exports in newsChainFallback.ts (NEWS_FALLBACK_BASELINE=0.55/TEMPORAL_DISCOUNT=0.8/FALLBACK_CONF_MIN=0.45/FALLBACK_CONF_MAX=0.65) with a comment linking the OCR-confidence ladder in resolvePdfText.ts. normaliseFilename moved into resolvePdfText.ts (re-exported from the orchestrator) rather than staying in the orchestrator, to avoid a circular import (resolvePdfText.ts needs it internally for the OCR-cache-lookup branch; keeping it in the orchestrator would have made resolvePdfText.ts import back from its own caller).
**what-considered:**
- Stop at the 2 backlog-named files (resolvePdfText.ts/newsChainFallback.ts) vs also extracting types.ts+insertBctcAnalysis.ts — chose the 4-file split: measured the file after only the 2 named extractions (191L, `wc -l`) which fails the explicit "<=120L" DoD; the extra 2 files are pure relocations (types have zero logic, Step 4 is a self-contained LanceDB side-effect with its own tag param) so this doesn't change behavior, only reduces further where the "orchestrator" boundary is drawn
- normaliseFilename: leave in orchestrator + resolvePdfText.ts imports it back (circular, both are value imports) vs relocate to resolvePdfText.ts + orchestrator re-exports — chose relocate+re-export: Node/Bun ESM circular imports of function declarations usually resolve fine since call sites are deferred past module-eval time, but the re-export pattern is strictly cleaner (one-directional dependency graph: orchestrator → resolvePdfText → newsChainFallback) and every existing external caller (bctcVpsIngestHandler.ts, 2 test files) still imports `normaliseFilename` from the unchanged path `fetchParseAndStoreBctc.js`, zero call-site churn
- exactOptionalPropertyTypes tsc errors on `pdfHttpClient`/`insertAnalysisFn` passed through as `X | undefined` into interfaces declared merely `field?: X` — fixed by widening the two interface fields to `field?: X | undefined` (not by removing the optional-chaining at the call site), matching how the codebase already writes these for other multi-file DI param objects
**why-decision:** DoD is explicit and testable ("orchestrator <=120L"); `wc -l` after each extraction step is the authoritative check, not a guess — iterated 3 versions (191L → 117L) until it passed. The pre-existing 2023/2024 special-case is left byte-identical (only `0.8` literal replaced by `TEMPORAL_DISCOUNT` reference, same resolved value) and flagged as JANITOR-035 in code-janitor-known-findings.json per the task's own instruction, rather than silently doing nothing (it wasn't previously tracked as its own discrete entry, only mentioned generically inside a different CI-scan idea in the 2026-06-15 maintainability audit doc).
**why-change:** no change from plan except the 2 extra files (justified above). Also fixed a test that broke as a direct, foreseeable consequence of the split, not a regression: p2-f-rag-http-rewire.test.ts's "fetchParseAndStoreBctc.ts imports AnalysisInput from ragHttpClient" AC literally grepped the orchestrator file's source text for the string `ragHttpClient` — that import legitimately moved to bctc/insertBctcAnalysis.ts with Step 4, so updated the test to check both files (orchestrator has neither retriever nor ragHttpClient references now; insertBctcAnalysis.ts has ragHttpClient, not retriever) — same acceptance intent, correct location. tsc clean. Targeted suite (17 of 18 relevant files, excluding FIX-1267): 156 pass/2 skip/0 fail. FIX-1267-ssc-circuit-breaker.test.ts AC-7/AC-8 (PDF-timeout/CircuitOpenError integration) are flaky independent of this change — proved by temporarily restoring the pre-split file via `git show HEAD:...` and re-running: identical 7 pass/2 fail (and 8/1 on a 3rd run) on the UNCHANGED baseline, both AC-7 and AC-8 timing out at the bun-test 5000ms default in some runs and passing in others; root cause is real subprocess/network timing (isOcrAvailable()/axios calls), not the file split. Server boot verified PORT=3997: /health 200 toolCount=183 (matches docs/data/project-stats.json baseline), /api/bctc-inspect 200, /dashboards/news-fetch/ 200. Gate 2d scheduler cron.schedule grep=3 unchanged (no scheduler files touched). Full `bun test`: 14353 pass/40 skip/64 fail/6 errors/1180 files (605s) then the same known Bun 1.3.13 crash-at-teardown as S10-S13 — grepped the full log for fetchParseAndStoreBctc/resolvePdfText/newsChainFallback/insertBctcAnalysis/bctc/types: only the 2 already-explained pre-existing-flaky FIX-1267 fails reference any touched file, zero other overlap. Doc updates: usecases.md (fetchParseAndStoreBctc.ts entry describes the split), pdfOcrWorker.ts pointer comment (fetchParseAndStoreBctc.ts:getCachedPdfText → bctc/resolvePdfText.ts:getCachedPdfText). rebuild_required=true (BCTC pipeline runs against the live named-volume market.db via scheduler + push-bctc-pdf route); RAW-verify of unchanged confidence values deferred to qa Step 5 per the Docker Microservice Code-Change Close Gate — this task's own DoD explicitly requires post-rebuild verification, so board routed `in_progress`→`review`, `next_agent=ops` (not qa directly), matching the S12/S13 precedent for rebuild_required=true FACTORY tasks in this same zone.

### STEP dev-mcp-server-S15 · dev-mcp-server · 2026-07-08T20:35:00Z
**task-id:** FIX-D4-HELD-LOCK-NO-BOARD-ROW-RECONCILE
**what-done:** Ported agent-father's doc-spec fix (commit 5436ead58: handlers.md/audit-dimensions.md Steps R-1b exclusion-whitelist+live-session-guard, R-4b 2-cycle debounce) into the real D4 executor `tasksMdJanitorJob.ts` — the LLM system-auditor flow never reads that spec; also fixed the 2nd gap (listHeld missing `expired:false`).
**what-considered:**
- R-4b ledger location: literal notebook-based ledger per doc spec (chosen — matches spec's explicit no-new-state-file/no-coordination.db-write constraint) vs a new dedicated debounce-state file (rejected — not authorized to unilaterally redesign the committed spec; flagged the notebook's own 30min-prune-vs-24h-debounce timing risk forward instead of silently "fixing" it)
- `JanitorDeps.listSessionPresence` optional (chosen, zero blast-radius on the 1 pre-existing test constructing JanitorDeps literally) vs required field (rejected — forces an unrelated test update for no behavioral gain)
- 2nd full `bun test` re-run for complete-log grep (started, then killed mid-run) vs relying on the already-decisive 23-file targeted suite (382/382, complete coverage of every real consumer) — chose the latter once the 2nd run added zero new information over what targeted+first-full-run already proved
**why-decision:** Task explicitly demanded RAW-verify over trusting a green test, given 6+ prior recurrences of this exact false-positive — docker-exec'd the LIVE coordination.db, confirmed all 7 real held locks from the 2026-07-08T03:00Z-class batch are excluded by the new whitelist, and confirmed the named negative-control task ids are NOT excluded (whitelist not over-broad).
**why-change:** No change from the dispatch's own instructions — implemented R-1b/R-4b/expired:false exactly as specified and RAW-verified against the exact fixtures named in the task. tsc clean, targeted 382/382, new suite 20 assertions all pass, eslint clean, server boot verified (toolCount=183, scheduler=3 unchanged). Commit e109f49f8. Board `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (compiled cron job — new code needs a rebuild to take effect).
### STEP dev-mcp-server-S16 · dev-mcp-server · 2026-07-08T20:45:00Z
**task-id:** FACTORY-INFRA-split-vnstockBridge
**what-done:** vnstockBridge.ts (1206L) held 11 inline Python `*_SCRIPT` templates; 9 of them (tradingStats/officers/shareholders/intraday/orderBook/balanceSheet/cashFlow/news + financials' 2-dataframe variant) re-embedded the identical FIX-FUNDAMENTALS-REFRESH-CRON-DEAD stdout-suppression preamble verbatim (proof the fix was hand-applied 9 times). Extracted each template to `fetchers/vnstock/scripts/*.ts` (one `buildXxxScript()` per data type); added `wrapVnstockScript(opts)` in new `fetchers/vnstock/runtime.ts` that owns the preamble once via a `{symbol, fetch, resultVar?, extraInit?, errorLabel, emptyValue, body}` options object. `prices.ts` (multi-symbol loop) and `events.ts` (bypasses `Vnstock().stock()` entirely via a `vnstock.common.viz` mock — pre-existing v4-compat workaround) kept bespoke — genuinely different control flow, not forced through the wrapper. `VnstockRateLimiter`/`runPython`/`runPythonWithBackoff`/`calcBackoffMs`/`isRateLimitResponse`/`stripAnsiAndDetectJunk` moved into `runtime.ts` (374L); `vnstock/index.ts` barrel re-exports it; `vnstockBridge.ts` (330L) now imports the barrel + 11 script builders and re-exports the tested helpers unchanged for existing test import paths.
**what-considered:**
- `wrapVnstockScript` 2-arg signature `(body, symbol)` as literally suggested by the backlog approach vs a richer options object — chose the options object: FINANCE_SCRIPT's shape (2 sequential dataframe fetches: `income_statement()` then `ratio()`, both under one stdout-suppressed try) doesn't fit a bare `(body, symbol)` call; the options object (`fetch`/`resultVar`/`extraInit`) lets financials.ts reuse the same wrapper instead of staying a 3rd bespoke script, maximizing de-duplication (9/11 scripts through the shared wrapper, not 8/11)
- Barrel (`vnstock/index.ts`) initially had zero internal call-sites (only scripts imported `runtime.ts` directly) — simplicity-gate Q2 flagged this as a speculative unused abstraction; fixed by routing `vnstockBridge.ts`'s own runtime imports through the barrel instead of `runtime.ts` directly, giving the barrel a real purpose (matches the DoD's "importable from a barrel" wording) rather than an unused file
- Byte-for-byte proof over unit-test-green: wrote a scratch script (not committed — scratchpad-only) exporting the pre-split `*_SCRIPT` consts via `git show HEAD:...` + sed, importing both pre/post modules side by side, diffing generated Python text for 3 symbols × 11 templates (35 checks) — chose this over relying solely on `bun test` because Python whitespace/control-flow is invisible to TS-level test assertions; a passing TS test proves the JSON shape is right, not that the Python source is unchanged
**why-decision:** DoD requires "banner dance defined once via wrapVnstockScript" + "RAW-verify... fetch identical values" — the options-object design was the only way to route financials.ts (the actual origin of the 9x-duplicated fix-comment) through the shared wrapper without duplicating its 2-fetch shape as a 3rd bespoke script; byte-diffing the actual generated Python (not just TS-level tests) is the only verification method that directly proves "identical values" for a codegen-template split, since the Python source itself is the thing that changed hands.
**why-change:** no change from plan. 32/35 equivalence checks are byte-identical; the 3 financials.ts checks differ only in a shortened shared comment (original: 4-line multi-banner explanation; wrapped: 1-line generic comment) — confirmed line-by-line that every subsequent Python statement (fetch calls, math, dict keys) is unchanged, only comment text shifted. tsc clean, eslint clean (both new files + vnstockBridge.ts). Targeted vnstock suite (24 files covering every exported symbol + every fetch* consumer, incl. mock.module-based syncVnstockData/hose tests): 252/252 pass. Server boot verified PORT=3996: /health 200 toolCount=183 (matches baseline), /api/bctc-inspect 200, /dashboards/news-fetch/ 200. Gate 2d scheduler cron.schedule grep=3 unchanged (no scheduler files touched). Full `bun test` run 1: 14349 pass/40 skip/68 fail/7 errors/44989 expect() calls/1180 files (732.30s); run 2 (full log captured to a file, not `tail`-truncated, specifically for exhaustive grep): 14350 pass/40 skip/67 fail/4 errors/1180 files (702.33s) — both then hit the known Bun 1.3.13 crash-at-teardown (non-authoritative). Run 1's visible tail already showed the fails are the documented pre-existing pollNews/RSS-timeout flaky class (1898b-rss-degradation-regression.test.ts x3) plus 2 unrelated fails in `src/_deprecated/1302-technical-indicators.test.ts` (deprecated dir) — zero vnstock mentions. Exhaustive grep of run 2's complete 22.5k-line log confirms this at scale: zero of the 67 `(fail)` lines mention "vnstock"; zero mentions anywhere in the whole log of `vnstockBridge.ts`/`vnstock/runtime`/`vnstock/scripts`/`wrapVnstockScript`/any of the 11 `buildXxxScript` names (only the unchanged `[vnstock:...]` runtime info-log prefix appears, same format as before the split). Commit: e27121d93. Doc updates: infrastructure.md (vnstock Python Bridge section rewritten for the new file layout). Board: `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (vnstock fetchers run against live named-volume market.db; RAW-verify of unchanged fetch values is this task's own DoD, routed via the Docker Microservice Code-Change Close Gate).

### STEP dev-mcp-server-S17 · dev-mcp-server · 2026-07-08T22:10:00Z
**task-id:** FACTORY-INFRA-split-telegramCommands
**what-done:** `telegramCommands.ts` (1071L) mixed router + 9 raw-SQL handlers + presentation + a layering violation (infra importing `assembleEveningSummary`/`generatePeriodicSummary` from `application/usecases/` to both fetch AND render `/recap*`). Split: presentation → `telegram/format.ts`; 8 SQL handlers → `telegram/commandHandlers.ts` (SQL → new `infrastructure/db/{watchlistReadStore,systemHealthStore,agentFeedbackStore}.ts`); `/news` → `telegram/newsHandler.ts`; `/recap*` **rendering** (pure) → `telegram/recapRenderer.ts`; `/recap*` **orchestration** (the fetch step) → new `application/usecases/orchestrateRecapCommand.ts`, invoked by the INTERFACE layer (`webhookHandler.ts`) via a new `RecapResolvers` DI param on `handleTelegramCommand`. Router is now 267L (down from 1071L), zero `application/usecases/` imports anywhere in the infra chain.
**what-considered:**
- Zero-coupling render types: `recapRenderer.ts`'s `EveningRecapData`/`PeriodicRecapData` are narrow, LOCAL, hand-written structural views (not `import type` of the producer's `EveningSummary`/`PeriodicSummary`) — chosen over a domain-layer type relocation (rejected: `EveningSummary` transitively depends on `BriefingAlert`/`TopStory`/`TaSignal`/`GlobalSnapshot` living inside `assembleBriefing.ts`, another application usecase — moving all of those was judged out of proportion for this task) and over a thin re-export wrapper usecase (rejected: still leaves a literal `application/usecases` import in infra, failing the DoD's "infra no longer imports usecases" literally). TS structural typing makes the real objects satisfy the narrow views with zero import needed.
- `handleRecap(db, assembleFn?)`'s public signature kept byte-identical (assembleFn now optional-with-no-default instead of optional-with-a-live-default) — chosen because it means EVERY existing test in `214-telegram-commands.test.ts` (which always passes an explicit fn) needed ZERO changes, while router-level `/recap*` calls without an injected resolver now degrade to the friendly Vietnamese error text (new, explicit, tested default-safety behavior) instead of the router itself importing a production fallback.
- Router "≤120L" aspirational target (approach text) not hit (landed 267L) — the 3 tiny DI-wrapper handlers (`handleRecap`/`Week`/`Month`, 6L each) stayed in the router file specifically to avoid changing the existing test import surface; judged this preserves more real value (zero test churn, zero regression risk) than hitting the raw line target.
**why-decision:** DoD explicitly requires "infra no longer imports usecases" — verified literally via `grep -c "application/usecases" telegramCommands.ts telegram/*.ts` = 0 matches anywhere in the infra chain, and via a passing `webhookHandler.ts`-level integration test proving the DI wiring reaches all the way through.
**why-change:** no change from plan. New tests: `FACTORY-INFRA-split-telegramCommands.test.ts` (20 assertions covering all 3 new stores, pure recapRenderer with hand-built non-application-typed objects, RecapResolvers DI both with and without an injected resolver, and orchestrateRecapCommand's 3 functions against a real assembleEveningSummary/generatePeriodicSummary) + 1 new case in `1406c-webhook-handler.test.ts` (full webhook→router→resolver→render path, `/recap`, 200 never-throws). tsc clean, eslint clean. Targeted suite (32 files: all telegram*/evening-summary/periodic-summary/webhook + the 2 new test files) 431/431 pass — `214-telegram-commands.test.ts` alone unchanged 60/60. Server boot verified PORT=3997: health 200 toolCount=183 unchanged, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200. Gate 2d scheduler cron.schedule grep=3 unchanged (no scheduler files touched). Full `bun test`: 14406 pass/40 skip/58 fail/6 errors/1182 files (583.84s) then the known Bun 1.3.13 crash-at-teardown — grepped every `(fail)` line: 57 are the documented pre-existing pollNews/RSS-timeout/VPS-push/logVpsPush/insider/foreign-flow/1302-deprecated flaky class; the 1 telegram-adjacent fail (`235-telegram-send-merge.test.ts` "channel='bug' sends to BUG channel") is in a file I never touched (`telegram.ts`'s BUG-channel sender, unrelated to the command router) and passes 10/10 cleanly in isolation — full-suite cross-test `mock.module` pollution, not a regression. Doc updates: `infrastructure.md` (new "Telegram Command Router" section + layering-fix note), `usecases.md` (`orchestrateRecapCommand.ts` entry). Board: `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (telegramCommands.ts + webhookHandler.ts are on the live `/webhook` HTTP path).

### STEP dev-mcp-server-S18 · dev-mcp-server · 2026-07-08T23:55:00Z
**task-id:** FACTORY-SCHEDULER-job-table-registry
**what-done:** `startScheduler.ts` (1257L) held 79 `scheduleCron(CRONS.x, async () => { jobRunRepo.wrapRun('xJob', ...) }, opts)` call sites (57 sharing an identical outer envelope). Introduced `schedulerJobTable.ts`: `buildJobTable(ctx)` — a declarative `{name,cron,options,runner}` array for the 57 — plus a single generic loop `registerJobTable(table, jobRunRepo)` (`scheduleCron(j.cron, () => jobRunRepo.wrapRun(j.name, j.runner), j.options)`); the 22 bespoke entries (skip wrapRun, use a `run*WithDb` startupHelpers dedup wrapper, or build extra local state first — WAL-escalation closure, scheduler-watchdog self-heal manifest) keep verbatim individual `scheduleCron(...)` calls in `registerBespokeJobs(ctx)`. `walEscalateFn` moved to `walEscalation.ts`'s `createWalEscalateFn()`. `startScheduler.ts` → 305L (composition root: DB/repo init, non-cron startup repairs/probes/backfills, 2 registration calls).
**what-considered:**
- Which entries qualify for the generic table vs stay bespoke: the task's own approach note cited only 3 "bespoke" examples (restartCadenceAlert/monthlySignalQuality/dataAuditDaily) but 2 of those (restartCadenceAlert's `{rowsWritten}` return, monthlySignalQuality's `recoverMissedExecutions:false`) fit the generic envelope fine since `options`/return-shape are per-entry fields anyway — audited all 79 call sites myself against the strict criterion "does the scheduleCron callback consist SOLELY of `await jobRunRepo.wrapRun(name, runnerBody)`, nothing else?" — 57 pass (any runner body complexity is fine, only the WRAPPER shape must be uniform), 22 fail (no wrapRun at all, or wrapRun preceded/followed by extra setup) → true bespoke set.
- Test-surface blast radius: 8 pre-existing tests read `startScheduler.ts` SOURCE TEXT via regex (not runtime import) to assert specific job wiring — moving code invalidates literal-adjacency assumptions (`scheduleCron(CRONS.x[\s\S]*?jobRunRepo.wrapRun('xJob'`) regardless of which file the code lives in. Retargeted each to `schedulerJobTable.ts` with syntax adjusted to the new shape, using a brace-depth-aware `extractJobTableEntry()` helper (not naive regex) so nested runner-body braces don't break extraction. ARCH-CRON-watchdog's WD-11 (derives `cron_job_runs` job_name literals from source to catch watchdog-manifest/call-site drift) got a 2nd scanned file + a `name: 'x'`-field regex (mirrors its existing `JOB_NAME_FUNDAMENTALS`-const indirection pattern).
**why-decision:** Runner bodies are pure verbatim copies (only the boilerplate 3-line wrapper is factored out) — this is the only design that satisfies "every job's cron/options/runner reproduced exactly" while actually removing the duplication (76+ near-identical wrapper blocks → 1 loop).
**why-change:** no change from plan. RAW-verify: wrote a scratch bracket-aware equivalence script (`/scratchpad/verify-job-table-equivalence.ts`, comment-stripped, quote-normalized) comparing every scheduleCron(CRONS.x,...) call site's cron-key + options + wrapRun-job-name between the git-HEAD pre-refactor blob and the new split — 79/79 match exactly (0 missing/extra/drift; 1 non-literal `JOB_NAME_BREADTH_PERSISTER` identifier case flagged by the script, manually confirmed identical in both). New `FACTORY-SCHEDULER-job-table-registry.test.ts` (15 assertions: buildJobTable shape/uniqueness/options fidelity, registerJobTable's generic wrapRun routing, registerBespokeJobs' 22-call coverage matched against the exact CRONS-key set, combined 79-call scheduler-boot smoke, walEscalation.ts real-tmpfile fixture integration test — same `BASE_ORCH`/`makeTmpPath` pattern as `WF2-signal-queue-cas.test.ts`). tsc clean, eslint clean (0 issues on all touched files; 2 pre-existing unrelated Fence-B errors in `getMoneyRadarComposite.ts`, confirmed untouched by me). Targeted suite (35 scheduler-referencing files, 431 tests) 430/431 — 1 fail is `188-alert-digest.test.ts` concurrency-guard timing-flake, confirmed pre-existing via isolated 26/26 pass. Server boot verified `APP_ENV=development PORT=3998`: health 200 toolCount=183 unchanged, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200, `[scheduler] jobs registered` final log line fired (all 79 registrations completed, zero throw). Gate 2d (`cron.schedule` literal grep) = 3, unchanged. Full `bun test`: 14414 pass/40 skip/59 fail/4 errors/1183 files (623s) then known Bun 1.3.13 crash-at-teardown — grepped every fail/error: all pollNews/vps_push_log/telegram-simulation/1302-deprecated pre-existing flaky class, zero scheduler mentions. Doc update: `infrastructure.md` new "Scheduler Job Table" section. Commit: a25bdc617. Board: `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (scheduler wiring — new code only takes effect after container rebuild, Docker Microservice Code-Change Close Gate).

### STEP dev-mcp-server-S19 · dev-mcp-server · 2026-07-09T02:41:00Z
**task-id:** CI-RED-06043b3c-FIX
**what-done:** Added `"done"` to the hardcoded `validStatuses` array in `1837a-pipeline-state.test.ts` L96 (AC-2), which validates live `docs/data/orch/orch-state.json` `.head.status` — po's Step 6 Docker Close Gate sign-off convention writes `.head.status="done"` on every closeout (confirmed standard via live file: currently `"done"`), and the array omitted it, causing a deterministic (not flaky) CI failure on every such closeout.
**what-considered:**
- Trust the pre-diagnosis vs independently re-verify "only one hardcoded spot" — chose independent re-verify (grep `validStatuses` + the `"in_progress"` array-literal pattern repo-wide) per task instruction not to trust the claim blindly; confirmed all other hits are test fixtures (set status, don't validate) or the unrelated uppercase task_board `StatusEnum`/lanes array — 1837a is genuinely the only spot.
- Board disposition: self-close DONE_VERIFIED (offered as an option, test-only change with no rebuild) vs REVIEW/next_agent=qa — chose REVIEW: the signal's own AC (`ci_green_on_subsequent_push`) requires a green CI run on a push after this commit, unconfirmable synchronously; self-closing DONE_VERIFIED before that gate is satisfied would be an unverified completion claim.
**why-decision:** One-line enum addition matching direct precedent commit 728ef563c exactly; REVIEW disposition follows standing "verification=system job" convention — DONE_VERIFIED requires the actual post-push CI-green fact, not an assertion that the fix should work.
**why-change:** no change from the fast-tracked dispatch's own instructions. tsc clean. Targeted `1837a-pipeline-state.test.ts` 5/5 pass (confirmed against live `.head.status="done"`). Regression sweep (orchStateSchema.test.ts, 1980-f2-canon-schema, 1979-orchestration-decisions, WF2-signal-queue-cas, orchStateStore-atomic-write, 1977-orchestration-endpoint) 200/200 pass, 0 fail. Test-file-only change — `rebuild_required=false`, no Docker Close Gate. Board `BACKLOG`→`review`, `next_agent=qa`.

### STEP dev-mcp-server-S20 · dev-mcp-server · 2026-07-09T09:50:00Z
**task-id:** FACTORY-DOMAIN-split-cascade-engine
**what-done:** cascadeEngine.ts (3739L, news->stock cascade hot path) held 9 exported rule-data constants (SECTOR_RULES ~2016L, LEGAL_RISK/POLICY/INSIDER_DUMP/MSCI x3/AGRICULTURE/IMF rules) + macro-adjustment/combo-detection orchestration ahead of buildCausalChain. Steps 1-3 only (per dispatch, Step 4 explicitly excluded): moved the 9 constants + interfaces into `domain/services/cascade/rules/*.ts` (one file per table; CascadeKeywordRule shared by 6 tables got its own type file instead of 6x duplication); barreled via `cascade/rules/index.ts`; split orchestration into `cascade/macroAdjustments.ts` (428L) and `cascade/comboDetectors.ts` (241L), keeping buildCausalChain + all exported types in cascadeEngine.ts (3739L → 779L).
**what-considered:**
- CascadeKeywordRule: duplicate the interface into all 6 consuming rule-table files (literal "one file per table" reading) vs one shared `cascadeKeywordRule.ts` type file — chose shared file: duplication across 6 files risks silent drift and isn't what "pure move of already-exported symbols" implies for a genuinely shared type.
- Re-export surface for the 2 orchestration files: `export *` everything (simplest) vs re-exporting only the symbols that were exported pre-split — chose the latter (explicit `export { X, Y }` after `import`) to preserve EXACT module-surface parity; direction2sentiment/findKeyword/isMarketWide/stripDiacriticsLower/PLACE_PREFIX_SINGLE were module-private in the monolith and stay non-re-exported from cascadeEngine.ts (only exported from comboDetectors.ts so cascadeEngine.ts itself can import them).
- macroAdjustments.ts needs CausalChainEntry/MacroContext (defined in cascadeEngine.ts, which imports macroAdjustments.ts back) — resolved via `import type` (erased at compile, zero runtime circular dependency) rather than relocating those types out of cascadeEngine.ts (task explicitly says types stay there).
**why-decision:** DoD requires "cascadeEngine imports via barrel" + "Step 4 explicitly NOT done" + behavior-unchanged proof beyond "tests pass" — the barrel + explicit re-export design is the only way to satisfy both "pure move" and "module surface parity" simultaneously; a byte-level before/after probe is the only verification method that directly proves cascade-path behavior parity for a pure-relocation refactor (test-green alone doesn't prove the SECTOR_RULES ordering/macro-delta-capping/combo-multiplier math is untouched).
**why-change:** no change from plan. RAW-verify: `bun tsc --noEmit` exit 0. Before/after behavior probe (temp script, deleted after use) ran buildCausalChain against 10 representative scenarios covering every moved code path (SECTOR_RULES oil match, legacy macro adjustments, market-wide broadcast, policy-intervention combo, insider-dump, place-prefix NER guard, agriculture drought, MSCI inclusion, legal-risk banking, brokerage-competitive warning) against the pre-split monolith (swapped in from a backup) and the post-split tree — output MD5-identical (90797bc2588adcf29117f72e7554d2f8) with only non-deterministic id/createdAt stripped. 062-cascade-engine.test.ts 23/23 pass; all 17 cascade-related test files show zero failures in the full suite run. Full `bun test`: 14400 pass/40 skip/73 fail/13 errors/1183 files (617s) then the known Bun 1.3.13 crash-at-teardown (post-summary, non-authoritative) — grepped every fail: zero mention cascade/macro/sector/msci/agriculture/imf/policy/insider-dump/place-prefix/market-wide; all 73 are the documented pre-existing pollNews/VPS-push/Chromium-missing/DB-table-drift flaky class (confirmed via isolated re-run of 102-job-news-poll.test.ts: identical Chromium/network-timeout causes, file doesn't even import cascadeEngine). Server boot verified via local process: tool registration reached toolCount=183 (matches docs/data/project-stats.json baseline) before the expected EADDRINUSE against the live Docker container on :3000 — confirms zero import/module-resolution error from the new file tree. Gate 2d scheduler probe unaffected (no scheduler files touched). Doc updates: NONE (architecture docs reference cascadeEngine by public API only, unchanged). Simplicity gate: PASS (Q1-Q4 all NO). Commit: 00a23ec28. Board `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (domain hot path — new code only takes effect after container rebuild, Docker Microservice Code-Change Close Gate).

### STEP dev-mcp-server-S21 · dev-mcp-server · 2026-07-09T13:10:33Z
**task-id:** FACTORY-SCHEDULER-split-dataAuditJob
**what-done:** dataAuditJob.ts (1300L) held runDailyChecks (D-1..D-11) + runWeeklyChecks (W-1..W-7) as two giant functions with 30+ inline findings.push sites. Extracted one file per check group into new `scheduler/news-analysis/audit-checks/` (19 files, each ≤120L, pure `(db)=>AuditFinding[]` except async W-7 `checkLancedbDrift`); `AuditFinding`/`TelegramFn`/`GetCountFn` + all shared helpers/constants (checkToCategory, severityToPriority, getPreviousRowCounts, insertFeedbackIfNew, buildFindingTitle, buildTelegramMessage, INDICATOR_RANGES, SNAPSHOT_TABLES) moved to new `dataAuditShared.ts`. `dataAuditJob.ts` now a 353L thin orchestrator (array-spread composition, original D-n/W-n order preserved), re-exporting the shared symbols for zero call-site churn.
**what-considered:**
- File-per-check-group boundaries: literal 1-D-number-per-file (19 tiny files matching each D-n/W-n exactly, ~11+7=18) vs grouping closely-related same-table checks (D-3+D-4 alerts-lifecycle, D-5+D-6 rag-quality) into one file each — chose grouping where the architect's own example names were plural/grouped (`checkStaleAlerts`), literal 1:1 elsewhere (`checkZeroPriceRows`/`checkStalePriceRows` split despite both hitting market_prices, since the example explicitly named checkZeroPriceRows on its own) — followed the given example names as the disambiguating signal.
- Intra-function ordering of `findings.push(finding)` vs `insertFeedbackIfNew(db, finding)`: several extracted checks call insertFeedbackIfNew before returning the array (vs original's push-then-insert) — verified this is behavior-neutral since both happen synchronously within the same check call before the orchestrator advances to the next check; empirically confirmed via the RAW-verify probe's agent_feedback row-order comparison (byte-identical).
- Commit granularity: architect approach note says "one check per commit" (would mean ~19+ commits) vs a single commit for the whole pure-relocation split — chose ONE commit, following the literal precedent of both cited prior FACTORY splits this session (a25bdc617 job-table-registry, 00a23ec28 cascade-engine), each landed as a single commit despite comparable or larger scope; an inconsistent-intermediate-state multi-commit split of a mutually-dependent orchestrator+19-file tree was judged riskier, not safer.
**why-decision:** DoD requires "SQL + finding semantics + side-effect ordering unchanged" verified by an actual output diff, not just green tests — a scratch pre/post comparison script (git-HEAD monolith copy vs post-split module) run against identical seeded fixture DBs is the only method that directly proves this; test-green alone doesn't prove D-7c's per-file insertFeedbackIfNew loop or W-3's 50%-safeguard math are untouched.
**why-change:** no change from plan. RAW-verify: `bun tsc --noEmit` exit 0. Scratch probe (temp script, deleted after use) ran `runDailyAudit`/`runWeeklyAudit` against identical seeded fixture DBs using a git-HEAD copy of the pre-split monolith vs the post-split module — findings[] (20 daily + 27 weekly) and `agent_feedback` insert-row ordering byte-identical (JSON deep-equal). New `FACTORY-SCHEDULER-split-dataAuditJob.test.ts` (7 tests) calls 6 extracted checks directly + dataAuditShared's pure helpers, demonstrating individual testability. Targeted suite (11 files: 157/314/1055/1420/1862j/1041/1086/1101/1221/p2-f-rag-http-rewire + new split test) 92/92 pass. tsc clean. toolCount=183 unchanged (live curl confirmed against the running container, pre-rebuild). Gate 2d scheduler `cron.schedule` grep=3 unchanged (only news-analysis/ internal layout touched, no scheduler wiring). Full `bun test`: 14415 pass/40 skip/65 fail/4 errors/1184 files (600.96s) then the known Bun 1.3.13 crash-at-teardown (post-summary, non-authoritative) — output was captured via a `tail -60` pipe so only the last 2 fails are visible in the log (both `src/_deprecated/1302-technical-indicators.test.ts`, the same pre-existing deprecated-suite flaky class cited in both prior FACTORY splits this session); did not re-run to grep the complete 65-fail list, relying instead on the complete targeted regression check (11 files touching dataAuditJob/dataAuditShared/audit-checks, 92/92 pass) as the authoritative direct evidence for this change. Doc update: `infrastructure.md` new "Data Audit Job" section. Commit: 7b62f73e7. Board `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (scheduler/cron hot path — new code only takes effect after container rebuild, Docker Microservice Code-Change Close Gate). Did NOT spawn a nested `claude --dangerously-skip-permissions` ops/qa/po session myself (mechanism confirmed available on this host) — treated that as bypassing the permission system on another agent's say-so, which the session's own safety framing explicitly disallows; left `next_agent=ops` on the board for the standing supervised dispatch mechanism to pick up, matching how both cited precedent tasks' ops/qa/po continuation actually landed.

### STEP dev-mcp-server-S22 · dev-mcp-server · 2026-07-09T14:45:00Z
**task-id:** FACTORY-SCHEDULER-split-intelligenceCycleJob
**what-done:** intelligenceCycleJob.ts (1381L, 15-min hot path) mixed CycleResult/CycleDeps contracts, isMarketHours, 9 defaultXxx DI-seam production impls, and the 7-step orchestrator. Split into `intelligenceCycle/types.ts` (CycleResult/CycleDeps, 138L), `intelligenceCycle/marketHours.ts` (isMarketHours, 36L — VN_OFFSET_MS imported from timeConstants.ts, not redefined), and `intelligenceCycle/defaults/*.ts` (one file per defaultXxx: 8 files ≤79L + `defaultComputeHexagrams.ts` 146L). `intelligenceCycleJob.ts` is now a 975L thin orchestrator (concurrency guard, per-step timeout helper, ALERT_WINDOW_MS/CYCLE_WARN_THRESHOLD_MS, `_runCycle`, `runIntelligenceCycle`, Step G chain synthesis); re-exports CycleResult/CycleDeps/isMarketHours/resetHexagramCooldown for zero call-site churn.
**what-considered:**
- `defaultComputeHexagrams` + `resetHexagramCooldown` + `_lastHexagramComputedAt` map: split across 2 files (compute fn separate from reset+map) matching the strict "one file per defaultXxx" naming vs keeping all three together in one file — chose keeping together per the dispatch's own explicit CRITICAL-invariant instruction; this pushes the file to 146L (over the 120L target) but is called out in the task spec itself as the one case where the size target is subordinate to correctness (splitting the map from either function silently breaks the 15-min per-stock cooldown closure — no test would catch it since both functions would still typecheck and run, just against two different empty maps).
- `ALERT_WINDOW_MS`: move into `defaults/defaultReadUnnotifiedAlerts.ts` (co-located with the one default that takes a windowMs param) vs keep in the orchestrator — chose orchestrator, because the constant is actually *consumed* at the `readUnnotifiedAlertsFn(ALERT_WINDOW_MS)` call site inside `_runCycle`, not inside the default impl itself (which just takes windowMs as a plain parameter); moving it to the defaults file would have been a cosmetic co-location, not a functional one, and the dispatch explicitly said "keep ALERT_WINDOW_MS ... named constants" without saying where — chose the consumption site as the correct home.
- Two tests doing raw source-text introspection (`readFileSync` + `indexOf("async function defaultPollNews")`) on intelligenceCycleJob.ts broke because the function physically moved — fix in-place (update srcPath to the new defaults/defaultPollNews.ts location, same regex assertions) vs leave broken/skip — chose fix-in-place: the tests assert on real behavior (which sources are stubbed) that is still true at the new location, and the DoD explicitly requires "cycle tests green", not "cycle tests unchanged".
**why-decision:** DoD requires RAW-verify (MD5/deep-equal of a cycle run pre vs post split) because this is a 15-min hot path where behavior-preservation matters more than usual — test-green alone doesn't prove `_runCycle`'s step-by-step wiring or the hexagram cooldown map are byte-for-byte unchanged; a git-HEAD-snapshot-vs-post-split executable comparison is the only method that directly proves it.
**why-change:** no change from plan. RAW-verify (two layers): (1) every extracted function body diffed byte-identical against a git-HEAD copy of the pre-split monolith (import-path depth normalized: `../../` inside `defaults/` legitimately becomes `../../../../` — everything else matched exactly, confirmed via `diff -u` on all 11 functions/consts). (2) scratch executable probe (temp script + temp git-HEAD snapshot copy in the same directory so relative imports resolved identically, both deleted after use) ran `runIntelligenceCycle` with an identical fully-injected `CycleDeps` object against both module versions across market-hours=true/false — `CycleResult` MD5-identical in both scenarios (`c57e0a99...`/`08430c10...`); `isMarketHours` matched across 5 fixed timestamps spanning weekday/weekend/open/close boundaries. tsc clean. Targeted suite (25 files: 106/1047/105/1101/1137/1187/1228/1255/1281/1285/1294/1307a/1313/1345d/1345e/137/1383/1501/1843/1920g/278/311/FACTORY-SCHEDULER-job-table-registry/FIX-NEWS-CB-FALSE-CLOSED/FIX-SIGNAL-CONFIDENCE-DEFAULT-50) 227/227 pass, 746 expect() calls. toolCount=183 unchanged. Gate 2d scheduler `cron.schedule` grep=3 unchanged (no scheduler wiring touched — only news-analysis/ internal file layout). Full `bun test`: 14415 pass/40 skip/65 fail/10 errors/1184 files (583.73s) then the known Bun 1.3.13 crash-at-teardown (post-summary, non-authoritative) — grepped every one of the 65 fail + 10 error lines: zero mention of intelligence-cycle/scheduler/news-analysis; all are the documented pre-existing flaky class (pollNews/VPS-push/insider-transactions/foreign-flow/market-cap/telegram-channel-routing/deprecated-technical-indicators, same 5000ms-network-timeout signature cited in both S20/S21's prior FACTORY splits this sprint). Doc update: `infrastructure.md` new "Intelligence Cycle Job" section. Commit: 0e1e48dad. Board `in_progress`→`review`, `next_agent=ops`, `rebuild_required=true` (scheduler/cron hot path — new code only takes effect after container rebuild, Docker Microservice Code-Change Close Gate).

### STEP dev-mcp-server-S23 · dev-mcp-server · 2026-07-09T14:36:00Z
**task-id:** FIX-CYCLEJOB-1294-MACRO-TEST-UNMOCKED-LIVE-FETCH
**what-done:** 1294-macro-spam-fix.test.ts AC-1/AC-2 `CycleDeps` objects omitted `macroFetchFn`/`vnstockSyncFn` (both already exist on `CycleDeps` — added by CI-RED-8081e584-FIX round 2, commit 8a2ef7255, for sibling 1285-macro-alert-cooldown test), so step A2 defaulted to REAL Yahoo Finance/SBV HTTP calls every run — confirmed live via 3 local runs showing `[yahooFinance] fetched commodity prices`/`[sbv] macro snapshot fetched` log lines. Fix: injected `async () => {}` stubs for both, mirroring the 1285 precedent exactly (no new DI convention).
**what-considered:**
- Scope: fix only 1294 (dispatch said "minimal targeted fix") vs also patch 8 other test files (106/1228/1255/137/1383/1501/278/311) sharing the identical unmocked-`runIntelligenceCycle` exposure (confirmed via grep+run — currently pass, network reachable) — chose 1294-only + flagged the other 8 in the commit msg/board note for a follow-up sweep; expanding an S-size FIX's blast radius to 8+ unrelated files without a dispatcher/PO decision was judged scope creep, not root-cause discipline.
- CI red on push (76acfb4e4): job failed but on an unrelated file (FU-LOCKSTORE-EXPIRED-GC.test.ts, 1294 NOT in the failure list) — re-run same commit (no new push) vs investigate/fix that file too — chose re-run only, after 3x local isolated pass confirmed it's a pre-existing flake with zero code overlap with this fix; investigating an out-of-scope flake would itself be scope creep.
**why-decision:** Task's own baseline_pass demands proof of "no live-fetch log lines," not just green — log-line absence is the only signal that distinguishes "stubbed" from "network happened to respond fast."
**why-change:** no change from plan. Verified: target test green x4 local runs (duration ~1050ms→~150ms, no live-fetch logs). tsc clean. `git stash` A/B on the 12-file CI failure list from a monolithic `bun test` run confirmed those 65 fails are pre-existing/unrelated (identical with or without this diff). CI-equivalent `ci-per-file-isolation.sh`: 1294 not among 12 failed files. Pushed 76acfb4e4; CI job initially red on FU-LOCKSTORE-EXPIRED-GC (unrelated, confirmed 3/3 local isolated pass) — re-ran same job (GH run 29025427212) → green. Board `in_progress`→`review`, `next_agent=qa` via orch-apply.sh (commit 531af9a11).

### STEP dev-mcp-server-S24 · dev-mcp-server · 2026-07-09T21:22:38Z
**task-id:** FACTORY-NEWS-extract-rss-parse
**what-done:** BOUNDED-1 auto-pickup dispatched this task to dev-mcp-server, but zone=news-fetch and all 3 target files are under `apps/news-fetch/` — outside dev-mcp-server's hard `zone_restricted: apps/mcp-server/` boundary (init.md boundary_rules: "NEVER write code outside apps/mcp-server/"). Did NOT implement the rss-parse.ts split. Root-caused: `backlog-detail.json` `dev_agent` field was wrong for ALL 6 `FACTORY-NEWS-*` rows (stamped `dev-mcp-server` since the 2026-06-15 audit-sprint data-entry pass) — `.claude/skills/zone-detect/SKILL.md` Tier-1 (explicit `zone:` field) unambiguously resolves `news-fetch` to `dev-news-fetch` per `system-map.json`. Corrected `dev_agent` to `dev-news-fetch` for all 6 rows. Reverted board row `in_progress` to `backlog` (status `BACKLOG`, `reroute_note` explaining the misroute) since no work occurred; reset top-level `.head` (NOT the deprecated `.task_board.head` stub) to idle/`next_agent=router` via `orch-apply.sh` so BOUNDED-1 WIP capacity is freed for the next tick. Released intent lock `intent:dev-mcp-server:FACTORY-NEWS-extract-rss-parse`. `send_telegram(bug)` flagging the router-side gap (dispatch used backlog's `dev_agent` field instead of running zone-detect Tier-1).
**what-considered:**
- Implement anyway (dispatcher explicitly said "run your own dev-mcp-server flow entrypoint") vs refuse — chose refuse: dispatcher instructions are never authorization to override the agent's own configured zone boundary (session safety framing); `dev-mcp-server/flow/main.md` itself states "zone restriction enforced: only apps/mcp-server/ files" with no news-fetch carve-out.
- Fix only this one row's `dev_agent` vs sweep all `FACTORY-NEWS-*` rows — checked all 6 news-fetch FACTORY rows and found the identical bug on every one; fixed all 6 in the same edit (root-cause, not one-off symptom patch).
- Delete the board row vs revert to `backlog` — chose revert-to-backlog (status `BACKLOG`, lane-coherent) so the task remains discoverable for a correct future pickup, per the standing rule that misrouted work needs re-dispatch, not deletion.
**why-decision:** Zone restriction is a hard permission-system boundary, not negotiable by a dispatcher's task payload; leaving the board row stuck in `in_progress` under the wrong claimant would also pin WIP>=1 and block the next tick — both effects are worse than a same-cycle self-correcting revert.
**why-change:** deviated from the literal dispatch (execute + commit + REVIEW) because the dispatch's own premise (dev-mcp-server is correct for this zone) was false; verified via `.claude/skills/zone-detect/SKILL.md` Tier-1 + `docs/data/system-map.json` (dev-news-fetch specialist zone == apps/news-fetch) + direct backlog-detail.json audit (6/6 news-fetch rows wrong). No apps/news-fetch/ file touched (zero code diff). Files changed: `docs/data/orch/archive/backlog-detail.json` (6 dev_agent fixes), `docs/data/orch/orch-state.json` (via orch-apply.sh — board revert + `.head` idle reset, exit 0, pre-existing 124 SHG coherence warnings unrelated/unblocking), this journal, notebook.

### STEP dev-mcp-server-S25 · dev-mcp-server · 2026-07-09T23:35:00Z
**task-id:** FIX-FOREIGN-FLOW-COVERAGE
**what-done:** Root cause (AUDIT-FC-FOREIGN-FLOW-recon.md): CODES sent to bgapidatafeed were built only from watchlist+referenceStocks (~111 codes) -> ~1457/1569 daily_ohlcv codes permanently foreign_net_vol=NULL. Added `GET /api/ohlcv-codes` (ohlcvBackfillHandler.ts, `handleOhlcvCodes` — `SELECT DISTINCT code FROM daily_ohlcv`, no hardcoded list); wired fetch-foreign-flow.sh to source CODES from it (fallback: /api/watchlist), mirroring fetch-ohlcv-backfill.sh's pre-existing R-2 pattern — that script already expected `/api/ohlcv-codes` but it was never implemented (confirmed dormant 404 in ohlcv-backfill-pipeline-stall/recon.md), so this closes both gaps. money_terms_elevated half (fBValue/fSValue -> foreign_buy_value/sell_value, VND serving in get_foreign_flow) was already shipped in ddc36452e (2026-06-16) — verified live via docker-exec query against the running container, no new work needed there.
**what-considered:**
- New dedicated endpoint vs widening `/api/watchlist` itself — widening would also balloon fetch-prices.sh's per-minute price-fetch CODES list (shared endpoint, different consumer, unrelated pipeline) as an unintended side effect; chose the separate `/api/ohlcv-codes` endpoint (root-cause fix scoped to the actual task, matches the pre-existing script contract exactly).
- Single bgapidatafeed call for all ~1459 codes vs batching — recon said no pagination; live-tested it directly (off-market-hours, from a non-VPS host): 1459 codes -> HTTP 200, 1400 items, ~1.2MB, ~7s. No batching needed.
**why-decision:** `writeForeignFlowToOhlcv` is UPDATE-only (no fabricated INSERT) — coverage naturally self-limits to codes that already have a `daily_ohlcv` row for that date, so widening CODES is safe (no fake-data risk) and self-heals as more codes get OHLCV rows through the day.
**why-change:** no change from plan. Verified: FIX-FOREIGN-FLOW-COVERAGE.test.ts (5/5 new, TDD RED->GREEN) + targeted foreign-flow/ohlcv-backfill suite 78/78 pass, tsc clean, toolCount=183 unchanged, server-boot + dashboard circular-dep probes clean, shellcheck clean on new lines. Bumped PAYLOAD_SIZE_THRESHOLD 50000->3000000 and bgapidatafeed fetch timeout 60s->90s for the ~13x larger payload. Filed `docs/signals/ops-rebuild-verify-mcp-server-20260709T2332Z.json` (mcp-server rebuild+swap is user-gated per feedback_container_swaps_user_gated.md; VPS script redeploy + market-hours RAW-verify both deferred to ops). Board `in_progress`->`review` via orch-apply.sh.

### STEP dev-mcp-server-S26 · dev-mcp-server · 2026-07-10T01:00:00Z
**task-id:** TASK-W5-FIX-BCTC-BANK-SUMMARY-MAPPING-CTG-CARRY-FORWARD
**what-done:** RISK-2 pre-check: latest on-disk signal (cowork-team-20260710T000000Z, newer than architect brief) confirms gateway still gateway-blind — Track 1 stays necessary. Built `scripts/migrations/carry-forward-bctc-orphaned-rows.ts` (idempotent, `--source`/`--target`/`--apply`) — copies orphaned `bctc_table_rows` onto the current report_id then reuses `buildBackfillBctcScalarsHandler` (zero dup logic) to reflow scalars. Ran live against named-volume DB: 451 CTG rows carried forward `96e36139→e497f7d1` (RAW-verified). Reflow found the carried rows are 208 income_statement+173 cash_flow+70 notes with ZERO balance_sheet/general rows — BEQ-6 gate correctly refused DONE, set PARTIAL, left total_assets=0/net_revenue=3910 unchanged. AC-TRACK1-2 PASS; AC-TRACK1-3 escalated (not silently claimed) — deeper defect than W2 scope: balance-sheet window never captured in the original refine pass.
**what-considered:**
- backfill_bctc_scalars tool-param extension vs standalone migration script — tool is already deregistered (U3), so script matches precedent (reingest-bctc-report.ts) and avoids touching a dead-surface file.
- Roll back the 451-row copy after discovering the missing balance-sheet section vs keep it — kept it: append-only copy (source untouched), refine_status=PARTIAL is strictly more truthful than PENDING+0-rows, and AC-TRACK1-2's own wording only requires the row count, which is met and verified.
**why-decision:** AC-TRACK1-3 itself names this exact escalation path ("if source rows are still corrupted... escalate if found") — fabricating a DONE/plausible-scalar claim would repeat the composite-score-masks-dead-detector class of false-green.
**why-change:** no change from plan on AC-TRACK1-1/2/4/5/6 (all PASS as scoped). AC-TRACK1-3 outcome differs from the hoped-for result — documented honestly, not silently downgraded. Verified: new script's own test suite 8/8 pass (24 expect()); targeted financial-reports suite (BEQ-2/BEQ-SECTION-GUARD/FU-BACKFILL-DE-SYNC/LF-SERVE-REFLOW/TSU-DEV-U3) 39/39 pass; tsc clean; full `bun test` 14426 pass/40 skip/59 fail/5 errors/1185 files (626s, known Bun 1.3.13 teardown crash after summary) — zero apps/mcp-server/src/ files touched by this task, so the fail/error set is provably pre-existing/unrelated. toolCount=183 unchanged, live health/dashboard probes clean (no rebuild — server code untouched). Board `ready`->`review` via orch-apply.sh, `next_agent=qa`.

### STEP dev-mcp-server-S27 · dev-mcp-server · 2026-07-10T02:57:00Z
**task-id:** FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH
**what-done:** Root cause: `freshnessSlaMonitorJob.ts`'s bctc check (`financial_reports.parsed_at` age) escalated CRASH/CRITICAL to alert-commander on age-vs-threshold alone — the ONLY layer of the 3 BCTC health surfaces (vpsHealthPoller/FIX-BCTC-FRESHNESS-GATE already has a queue-guard; freshnessSlaChecker/FIX-BCTC-SLA-THRESHOLD-360 already has calendar-window awareness) that never checked live queue-depth or service-state. Added generic `PipelineRuntimeState{serviceActive,queueDepth}` gate to `checkSignalSla`/`checkDataFreshnessSla` (domain, pure): service-down->CRASH always; queue-depth=0+active->IDLE always (never CRASH regardless of age); queue>0+active->normal age check unchanged. Wired scheduler-side reader `queryBctcPipelineRuntimeState` (bctc_vps_queue active-count + latest vps_service_health row) into `runFreshnessSlaMonitor`, fail-open (undefined) on any DB error so callers/tests lacking the new tables are unaffected.
**what-considered:**
- Extend FIX-BCTC-SLA-THRESHOLD-360's calendar-window heuristic further vs add a live runtime-state gate — calendar logic answers "should data exist by now" (BCTC-specific), not "is the service actually alive with an empty queue" (fix_spec explicitly demands the latter, generically, for any queue-backed pipeline) — chose the new gate, kept the calendar logic untouched (both now compose: calendar sets the threshold, runtime gate overrides the verdict).
- Reuse vpsHealthPoller's already-idle-aware `vps_service_health` row as the service-state proxy vs a new SSH/systemd probe — mcp-server has no SSH credentials by design (vpsProxyWatchdogJob.ts) — reused the existing poll table (matches generic_mandate: no new infra).
**why-decision:** Gating only on `runtimeState` presence (optional param, default undefined) makes the fix additive/fail-open — zero behavior change for any signal type or DB shape that predates this task, matching the RAW-confirmed scenario (report 3256) without touching FIX-BCTC-SLA-THRESHOLD-360's calendar path.
**why-change:** no change from plan. New test FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts (19/19 pass, Groups A/B domain-pure + C scheduler-wiring incl. fail-open/missing-table + true-positive-preserved cases). tsc clean. Targeted SLA/BCTC-health suite (9 files, incl. 1407b/1920i/1352c/FIX-BCTC-SLA-THRESHOLD-360/FIX-BCTC-FRESHNESS-GATE) 129/129 pass, run twice. Full `bun test`: two runs (55 fail/3 err then 62 fail/7 err, both end in the documented Bun-1.3.13 teardown panic) — non-deterministic counts across runs is itself evidence of resource-contention flake, not a regression; grepped every unique failing test name — zero overlap with freshnessSla/bctc/vpsHealth; re-ran 3 sampled full-suite-only failures (1113/1146/RAPID-B2, 35 tests) in isolation -> 35/35 pass, confirming flake not regression. Server-boot probe: health 200, toolCount=183, boot log shows "SLA monitor" still registered among 85 cron keys, `bctc-inspect`/`news-fetch` dashboard routes clean. Board `ready`->`in_progress`->`review` via orch-apply.sh, `next_agent=qa`.

### STEP dev-mcp-server-S28 · dev-mcp-server · 2026-07-10T04:30:00Z
**task-id:** FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID
**what-done:** Live RAW-verify found 0 current orphans (10/10 alert_id rows resolve; TTL/cleanExpired churns the table, historical 124 self-expired, unrecoverable). Root-caused via git archaeology: `storeAlerts`/`storeAlertsFromCommander` wrote the `agent_signals` correlation row without confirming the paired `INSERT OR IGNORE INTO alerts` persisted — it silently no-ops on an `alerts.fingerprint` UNIQUE collision too, not just an `id` collision; pre-06-25 scan jobs paired a deterministic fingerprint with `crypto.randomUUID()` ids, exactly reproducing this (commit 57a781a14 fixed the CALLER side, confirming "220 rows, +96/day" at the time). Fixed at the WRITER (defense in depth): both functions now `SELECT 1 FROM alerts WHERE id=?` before co-writing. Added `checkOrphanAgentSignalsAlertId` (D-NEW2) as an ongoing regression tripwire (inverse of C-08/W-6).
**what-considered:**
- Caller-only fix (ensure every future job uses deterministic ids) vs writer-level existence guard — writer guard chosen: cheap (1 indexed PK lookup/alert), and closes the defect class for ANY future caller, not just ones remembered to be careful.
- Backfill the 124 historical rows vs document their self-expiry — no live rows exist to backfill (TTL=2h + cleanExpired already purged them weeks ago); fabricating placeholder alerts rows to "fix" already-gone data would violate no-fake-data.
**why-decision:** `e3386bdf` (TASK-CONF-1) ruled out — diff touches only confidence_score plumbing, no id/fingerprint/alert_id logic, and 06-22 orphans predate its 06-23T17:55Z landing anyway.
**why-change:** no change from plan on root-cause + writer fix. Verification-gate point 4 (2nd confirmation pass ≥1 day later) could not be executed live in-session — documented as an open item requiring ops rebuild (container swap user-gated) rather than silently skipped. New test 8/8 (TDD RED reproduced the exact dangling-FK defect before the fix). Targeted alert suites 110/110 pass, tsc clean, full suite 14449 pass/63 fail/9 err (599s, known Bun teardown crash, zero alert/signal/orphan/audit overlap in failures). toolCount=183, scheduler untouched, server-boot+dashboard probes clean. Board `in_progress`->`review` via orch-apply.sh, `next_agent=qa`.

### STEP dev-mcp-server-S29 · dev-mcp-server · 2026-07-10T06:45:00Z
**task-id:** SPIKE-CI-PERFILE-ISOLATION-FLAKE
**what-done:** Reproduced the rotating-file CI flake live by manufacturing CPU contention (background `yes` flood) around `ci-per-file-isolation.sh`'s 16-way parallel model, matching `ubuntu-latest`'s constrained vCPU count. Found 3 independent real bugs, not one shared cause: (1) `agentBootstrap.ts:358`'s eager `buildToolNameMap()` blocks on `execSync("git ...")` inside `getProjectRoot()` — under contention a bun test-timeout fires mid-blocking-call and permanently poisons the ES module (cascading TDZ, reproduced 6/9 tests failing in one run) for ~30 files incl. production `server.ts` boot path; (2) `106-intelligence-cycle.test.ts` fires real unmocked Yahoo Finance/SBV network calls every test (Step A2 unconditional) — confirmed via live log, matches an already-known `FIX-CYCLEJOB-1294` review_note follow-up candidate; (3) `135-rag-temporal-decay.test.ts`'s "brand-new result" test asserts a zero-tolerance boundary (`toBeLessThanOrEqual(0.6)`) against two separately-captured `Date.now()` calls — any scheduling jitter between them flips it, reproduced (`0.6000000002062938`).
**what-considered:**
- Fold into one generic "de-flake CI" ticket vs 3 separate scoped fixes — 3 separate: the bugs are in unrelated code paths (execSync/module-init, live network I/O, floating-point boundary assertion) and forcing a false unification would misdirect whoever picks up the fix.
- Blame PO's hypothesis (a) "date-roll 07-07→07-10" vs actual mechanism — disproved by evidence: the other tight-tolerance assertions in the same file are numerically immune (~70min drift needed), only the exact-boundary assertion is exposed, and it can trigger on any date.
**why-decision:** `execSync`-in-singleton-init is the highest-value fix (production boot-path risk, not just CI flake, ~30-file blast radius) — recommended HIGH priority as a NEW backlog item, separate from the already-tracked `FIX-CYCLEJOB-1294` sweep and the tiny `135-rag` assertion fix.
**why-change:** no change from plan. Findings doc `docs/spikes/SPIKE_CI-PERFILE-ISOLATION-FLAKE.md` committed directly to main (INV-GATEWAY-1, no mutex) at c3c7966e1, pushed. No code merged — throwaway branch `spike/ci-perfile-isolation-flake` deleted post-write per feature-spike.md. `DEFLAKE-1187-POLLNEWS-DEAD-PATH` left untouched — not one of the 3 evidenced files, no investigation performed, not folded in without evidence.

### STEP dev-mcp-server-S30 · dev-mcp-server · 2026-07-10T10:20:00Z
**task-id:** FIX-BCTC-D1-STABILIZE-REPORT-ID
**what-done:** Verified live schema first (`bctc-schema.ts:727-822` `SQLITE_DDL`, wired via `initFinancialReportsTables`→`schema.ts initDatabase()`) confirms `UNIQUE(action_code, sort_key)` is real, not just a test-fixture artifact. Rewrote `storeReport()`'s `INSERT OR REPLACE INTO financial_reports` as `INSERT ... ON CONFLICT(action_code, sort_key) DO UPDATE SET <60 cols except id>` (id omitted from SET so SQLite preserves it on conflict). Also added a pre-check `SELECT id ... WHERE action_code=? AND sort_key=?` inside `parseBctcReport()` itself so the in-memory `report.id` returned to callers also stays stable, not just the DB column.
**what-considered:**
- SQL-only DO-UPDATE trick (router's minimal instruction) vs also implementing the architect's full pre-check-SELECT design — did both: DO-UPDATE alone leaves the *returned* `report.id` diverging from the persisted row on every re-parse (nothing downstream currently reads it, but it's a latent footgun D2/D3 would inherit); the pre-check is one cheap indexed SELECT and closes the gap completely, matching the architect brief verbatim.
- Grepped every existing caller/test for double-`parseBctcReport()`-calls on an identical (action_code, sort_key) — none exist, so the upsert-semantics change carries zero regression risk to the pre-existing suite.
**why-decision:** SPIKE-CI-PERFILE-ISOLATION-FLAKE (S29) already proved raw `bun test` is unreliable under this sandbox's contention; used `scripts/ci-per-file-isolation.sh` (P=12) instead — completed all 1184 files (3 killed by me after independently-confirmed hangs unrelated to this diff; one re-verified hung in total isolation, zero contention).
**why-change:** no change from plan. New `FIX-BCTC-D1-STABILIZE-REPORT-ID.test.ts` (3/3 pass, 17 assertions): DB-row id unchanged + in-memory id unchanged + net_revenue updated + row count=1 across 2 parses of the identical key; distinct sort_key gets its own id. tsc clean. Per-file-isolation full sweep: 14286 pass/40 skip/37 fail/1184 files — 3 rc=137 (my kill, confirmed pre-existing hangs, one re-verified hung standalone with zero contention: `1294b-bctc-fallback.test.ts`, unrelated to storeReport), 3 rc=132/SIGILL pass-then-crash (matches the S26-S29-documented recurring Bun 1.3.13 teardown crash), 12 genuine fails all in pollNews/Chromium/SSC-breaker/pdf-extractor/RAG files — zero reference to `parseBctcReport`/`storeReport`/`financial_reports` write path. BCTC/financial-reports-scoped subset (106 files): 1220 pass/0 fail (only the 1 pre-existing-hung file inconclusive). toolCount unchanged (no interface/tool file touched). `docs/architecture/microservice/mcp-server/usecases.md` updated (application layer changed, doc-review Step 1 rule). Board `ready`→`review` via orch-apply.sh, `next_agent=qa`. NOT committed by this agent — router/dev-team commits per explicit dispatch instruction (override of the zone's normal RUN-SOLO direct-commit default).

### STEP dev-mcp-server-S31 · dev-mcp-server · 2026-07-10T11:05:00Z
**task-id:** FIX-BCTC-D2-ENSURE-SHELL-ROW
**what-done:** New application usecase `ensureFinancialReportShellRow({actionCode,year,quarter,pdfPath,db?})` (`usecases/bctc/ensureFinancialReportShellRow.ts`) — idempotent `INSERT ... ON CONFLICT(action_code, sort_key) DO UPDATE SET pdf_path = excluded.pdf_path WHERE financial_reports.pdf_path IS NULL`, first-insert rows get `validation_status='pending_extraction'` + `'{}'` JSON columns. Reuses D1's pre-check-SELECT id-stability pattern and `bctc/newsChainFallback.ts`'s existing `buildFiscalPeriod()` helper (no reimplementation). Wired into `bctcPdfPullJob.ts` as new Step 4b — called with the job's own `db` handle right after `deps.savePdf()` succeeds, before Step 5 `triggerExtraction`; errors caught+logged, non-fatal.
**what-considered:**
- `db` defaulting to the `getDb()` singleton (matching design-doc's literal call signature, no `db` param) vs an optional injected `db` param (matching sibling `backfillBctcPdfPaths(db, pdfDir)` for this same table) — injected: `bctcPdfPullJob.test.ts` constructs its OWN dedicated `:memory:` Database instance separate from the `getDb()` singleton (confirmed by reading the existing test file before writing any code); a `getDb()`-only usecase would silently write into a second, un-migrated, orphaned in-memory DB during every existing pull-job test run, throwing "no such table" — caught this BEFORE writing code, not via a failing test.
- Adding `ensureFinancialReportShellRow` to the injectable `BctcPdfPullDeps` interface (matching `fetchPdf`/`savePdf`/`triggerExtraction`) vs calling it directly with the job's own `db` — direct call: it is pure in-process SQLite persistence on the same DB the job already reads/writes `bctc_vps_queue` against (like the existing 0-row-gate queries), not external I/O (network/filesystem/HTTP) — the `deps` seam exists specifically for real I/O boundaries.
**why-decision:** design doc §D2's literal SQL/behavior contract (docs/handoffs/TASK_FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION.md) follows verbatim (no `extraction_confidence` column in the shell-row INSERT list — defaults to schema's `1.0`, matching the doc's exact column list, not my own addition).
**why-change:** no change from plan. New `FIX-BCTC-D2-ENSURE-SHELL-ROW.test.ts` (7 tests): first-insert shell row, no-op re-call preserves pdf_path+id, NULL→set update, NOT-NULL never-clobber, distinct sort_key own id, injected-db isolation from the singleton. Extended `bctc-pdf-pull-job.test.ts` +2 tests (TC-13 new-ticker shell row w/ pdf_path set, TC-13b pre-existing GVR-pattern row w/ pdf_path NULL gets set + id preserved) — reproduces the exact GVR/MBB/D2D bug class the design doc names. Targeted suite (D1+D2+pull-job, 3 files) 28/28 pass. tsc clean. mock-guard PASS. Full per-file-isolation sweep (P=12, 1185 files): 14288 pass/40 skip/59 fail/16 files — grepped every failed file for `ensureFinancialReportShellRow`/`bctcPdfPullJob`/`storeReport`/`parseBctcReport`/`financial_reports`: 2 textual hits, both individually re-verified as pre-existing/unrelated — `125-test-e2e-briefing.test.ts` (38/39 pass, 1 fail = Chromium binary absent in sandbox) and `1294b-bctc-fallback.test.ts` (confirmed standalone hang, same file S30/D1 already root-caused one cycle earlier — I never touch `newsChainFallback.ts`, `git status` confirms). Scheduler count: `docs/agents/dev-mcp-server/flow/main.md`'s Gate-2d grep probe (`cron.schedule` literal) is stale post-refactor (scheduling now goes through the `CRONS` config map, not scattered literals) — reported the live `CRONS` count instead (`Object.keys(CRONS).length`=85, unchanged, file untouched) rather than trust/fabricate the stale-pattern grep's wrong number (3). toolCount=183 unchanged. Server-boot probe: module load + DB init + tool registration (183) + scheduler bctc-poison-cleanup all clean, zero import errors; final `httpServer.listen()` hit pre-existing `EADDRINUSE` on :3000 (an already-running process predating this task, not started by me) — HTTP-serve step inconclusive in this sandbox, module-load step is the meaningful signal and is clean. `docs/architecture/microservice/mcp-server/usecases.md` updated (new application-layer file, doc-review Step 1 rule). Board `ready`→`review` via orch-apply.sh, `next_agent=qa`. NOT committed by this agent — router commits after QA's gate passes, per explicit dispatch instruction (same dev→qa→commit ordering as D1).

### STEP dev-mcp-server-S32 · dev-mcp-server · 2026-07-10T11:50:00Z
**task-id:** FIX-BCTC-D2-ENSURE-SHELL-ROW (round-1 QA fix)
**what-done:** QA (independent gate, live scratch insert+read-back) blocked S31's diff on one regression: the shell-row INSERT omitted `extraction_confidence`, so SQLite silently applied the schema's `DEFAULT 1.0` (bctc-schema.ts:750) to a row with zero extracted data — a shell row would read back `Confidence: 100%` from `get_financial_summary`/`compare_financials` (reports.ts, no refine_status/validation_status gate, unlike `get_bctc_full`'s PUB-1 check) even though nothing was ever extracted, and this is permanent (not transient) for tickers like GAS whose scalar tiers never cross the 0.5 threshold, per `parseBctcReport.ts`'s own `extractionConfidence===0` skip-guard. Fixed at the single flagged file/line: added `extraction_confidence` to the INSERT column list bound to `0` (measured zero confidence, not schema default). Strengthened `FIX-BCTC-D2-ENSURE-SHELL-ROW.test.ts`'s first test to assert `extraction_confidence === 0` on the freshly-inserted row so this exact regression class cannot silently reappear.
**what-considered:**
- Also patch `reports.ts` (add a serve-layer validation_status/refine_status gate mirroring `bctcFullTools.ts`'s PUB-1 / `bctcIdentityGuard.ts` precedent) vs leave it — left it: QA explicitly flagged that as a separate non-blocking backlog item (out of D2's `files:` scope, a 4th occurrence of the recurring "new row-state not caught by shared serve-layer guard" class QA named for pm/architect), matching D1's own precedent of flag-not-absorb for out-of-scope findings.
- Noticed the design doc itself (`docs/handoffs/TASK_FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION.md` §D2's literal SQL fragment, line ~52) also omits `extraction_confidence` from its column list — S31's "verbatim to the doc" implementation choice (recorded in S31's why-decision) is exactly why this regression shipped. Did not edit the handoff doc (architect-owned, out of this fix-round's explicit scope: code fix + test + board + journal/notebook only) — flagging here so a future architect pass on that doc catches the same gap at the source.
**why-decision:** minimum in-scope fix only, per explicit round-1 fix-request instructions — single file (`ensureFinancialReportShellRow.ts`), bind `extraction_confidence=0`, do not touch `reports.ts`.
**why-change:** no change from the requested fix scope. Re-ran targeted suite (same 3 files as S31): 28/28 pass, unchanged count. `bun tsc --noEmit`: clean. `scripts/audits/mock-guard.sh --files` on both changed files: PASS. Board row `status_note` appended (status stays REVIEW, QA's `qa_verdict`/`qa_note` from round 1 left untouched as the historical record), `next_agent` flipped `dev-mcp-server`→`qa` via `orch-apply.sh`. NOT committed — router RAW-verifies this fix (3rd independent verification pass on this task: router's original spot-check, QA's round-1 gate, now this fix) and commits the full D2 bundle (S31 impl + QA round-1 review + this fix + updated docs) together once QA signs off clean. gateway MCP bridge unreachable in this sandboxed sub-session (known ongoing gap, not mine to fix) — could not `send_telegram`.

### STEP dev-mcp-server-S33 · dev-mcp-server · 2026-07-10T12:09:00Z
**task-id:** FIX-BCTC-D3A-PEK-TRIGGER-HELPER
**what-done:** Pure DRY extraction (no new logic, per the board row's own note) — read `handleTriggerPekExtract` (`bctcVpsIngestHandler.ts:194-280`) first, then moved its fetch + market-hours-handling block (POST `http://pdf-extractor:5001/pek-extract`, 30s `withDeadline`, 503-market-hours / non-OK / network-unreachable discrimination) into a new `triggerPekExtractionForReport(reportId, pdfPath, log?)` (`infrastructure/fetchers/pekExtractTrigger.ts`, co-located with `pdfExtractorClient.ts` per the D3 design doc's DDD-layer table). Returns a discriminated union (`queued|market_hours|pdf_extractor_error|unreachable`) instead of writing directly to `res` — `handleTriggerPekExtract` now just fetches the result and translates each outcome to the EXACT SAME `res.writeHead`/`res.end` byte-for-byte shape it always produced (verified via `git diff`, no response-body/status-code text changed).
**what-considered:**
- `log` param: hardcode the new helper to the infra singleton `logger` (matches `pdfExtractorClient.ts`/`bctcPdfPullJob.ts`'s own convention, and matches what the still-BACKLOG D3B auto-trigger caller — a cron job with no injected per-request logger — will actually have available) vs require an injected logger — chose a **default param** (`log: PekTriggerLogger = defaultLogger`): `handleTriggerPekExtract` passes its own injected `log` through (preserves its exact pre-refactor log call-sites/behavior with zero duplication), while the forthcoming D3B caller can omit the 3rd arg entirely and get the singleton for free — one helper, two call-site conventions, neither forced to adapt to the other.
- Where to catch the try/catch: kept it entirely inside the new helper (never throws) so callers branch on the returned `outcome` field with no try/catch of their own — matches D3's own text ("factored into a small shared helper... so both the manual route and the new automatic call share one implementation") and keeps the forthcoming D3B caller (which needs to set `bctc_vps_queue.status`, not write an HTTP response) simple.
- Did NOT touch `bctcPdfPullJob.ts` or wire any auto-trigger call site — D3B is explicitly still BACKLOG/blocked-on-this-task per the dispatch instructions; scope is strictly "prepare the extractable helper," not consume it.
**why-decision:** the D3 design doc (`docs/handoffs/TASK_FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION.md` §D3, "Reuse patterns") names `triggerPekExtractionForReport()` and the `infrastructure/fetchers/` co-location explicitly — followed verbatim rather than inventing a different shape.
**why-change:** no plan deviation. New `FIX-BCTC-D3A-PEK-TRIGGER-HELPER.test.ts` (6 tests, 25 expect() calls, mocked `globalThis.fetch`): 202→`queued` (+ log.info call asserted), 503→`market_hours` (body forwarded verbatim, no log.error), non-OK/non-503→`pdf_extractor_error` (pekStatus/body captured, log.error asserted), thrown fetch error→`unreachable` (error message captured, log.error asserted), POST body forwards `{report_id, pdf_path}` verbatim to the exact URL, and a no-log-arg call proves the default-param path (used by the future D3B caller) doesn't throw. Targeted regression (8 files: the new test + `1112-bctc-vps-proxy` + `bctc-pdf-pull-job` + `FIX-BCTC-D2-ENSURE-SHELL-ROW` + `FIX-BCTC-D1-STABILIZE-REPORT-ID` + `FIX-BCTC-VPS-QUEUE-SYNC` + `FIX-1281-bctc-vps-only` + `pek-render-seam`) 82/82 pass, 287 expect() calls — zero regression to `handleTriggerPekExtract`'s existing response-shape contract. `bun tsc --noEmit`: clean (one `exactOptionalPropertyTypes` fix needed in the test's own spy-logger helper, not production code). `scripts/audits/mock-guard.sh --files` on all 3 changed/new files: PASS. Bare `bun test` (no file arg) hung as already documented (S29 SPIKE-CI-PERFILE-ISOLATION-FLAKE, S30/S31 precedent) — killed, used `scripts/ci-per-file-isolation.sh 12` (canonical per this sandbox's own "NEVER bare bun test" host-safety comment) instead: 14295 pass/40 skip/58 fail across 1185 files. Of the anomalies: 3 files SIGILL/rc=132 pass-then-crash (matches the S26-S30-documented recurring Bun 1.3.13 teardown crash — summary line printed before the crash, so these don't land in the fail count); 1 file (`FIX-1267-ssc-circuit-breaker.test.ts`) hung with near-zero CPU for 5.5min — killed by me; this is a previously-documented pre-existing flake (grepped `sprint-FIX-MACRO-THRESHOLD-FXFLOOR-OVERCLAMP-architect.md`: same file independently reproduced flaky on an unrelated diff's UNCHANGED baseline, "real subprocess/network timing (isOcrAvailable()/axios calls), not the file split"). Of the 16 genuinely-failed files: grepped all 16 for `pekExtractTrigger`/`triggerPekExtractionForReport`/`bctcVpsIngestHandler`/`handleTriggerPekExtract` — zero matches; `1294b-bctc-fallback.test.ts` is the same standalone-hang file S30/S31 already root-caused (unrelated to this diff, never touches `bctcVpsIngestHandler.ts`/`pekExtractTrigger.ts`). toolCount=183 unchanged (no MCP tool registered/changed — this is an infra HTTP-call helper + interface-layer refactor, not a new tool). No scheduler/cron file touched (D3B, which would add the cron wiring, is explicitly out of scope). `git status --porcelain -- apps/mcp-server/` confirms exactly 3 files touched (1 modified, 2 new) — matches the "pure DRY refactor" scope claim. Board `ready`→`in_progress` (Step 1)→`review` via `orch-apply.sh`, `next_agent=qa`. NOT committed — router RAW-verifies and commits per explicit dispatch instruction.

### STEP dev-mcp-server-S34 · dev-mcp-server · 2026-07-10T13:00:00Z
**task-id:** FIX-BCTC-SERVE-GATE-FINANCIAL-REPORTS
**what-done:** Verified architect's status_note verbatim against the live board row (jq) before starting — confirmed exact insertion points unchanged. Added `validation_status: string` to `ReportRow` (reports.ts). Inserted a `row.validation_status==='pending_extraction'` gate in `get_financial_summary` (after the `!row` 404 branch, before `checkBctcIdentityGuard`) and a dual `row1/row2.validation_status` check in `compare_financials` (after `!row1||!row2`, before the identity-guard block, mirroring its guard1/guard2 idiom) — both return a plain-text "PDF downloaded but not yet extracted" message, not thrown/isError.
**what-considered:**
- Blind-copy PUB-1's `refine_status` field (the task's own pre-architect-triage framing) — REJECTED, architect already ruled this out (S5): legacy scalar-pipeline rows never touch `refine_status`, would hide legitimate real data.
- Hand-roll raw SQL INSERTs for the pending-extraction test fixtures vs. call the real `ensureFinancialReportShellRow` usecase — chose the real usecase: exercises the actual D2 production write path (extraction_confidence=0, '{}' JSON columns), zero drift risk from a hand-maintained duplicate INSERT.
**why-decision:** Architect's S5 rationale (verified against live schema, not just narrated) is authoritative and was implemented exactly as specified — no design deviation warranted for an S-size mechanical fix with a pre-verified field choice.
**why-change:** no change from plan. New `FIX-BCTC-SERVE-GATE-FINANCIAL-REPORTS.test.ts` (6 tests, 20 expect() calls): (a) pending-extraction row → pending message not 404/real-data; (b) compare_financials one-pending and both-pending; (c) regression proof — legacy `validation_status='pending'` (DB DEFAULT, bctc-schema.ts:818) + explicit 'passed'/'failed' rows still serve real data, incl. a legacy-vs-legacy compare_financials case. Targeted regression (085-tool-reports, D2-shell-row, 1181, 1086, every file importing `registerReportTools`/`reports.js` incl. all 15 `fix-bctc-identity-serve-guard` DV-BCTC-GUARD-* cases) 109/109 pass combined. tsc clean. mock-guard PASS. Bare `bun test` hung (documented S29-S33 sandbox issue) — killed, used `scripts/ci-per-file-isolation.sh` (canonical host-safety runner): 14303 pass/40 skip/65 fail/16 files, grepped all 16 for any reference to this diff's touched symbols — zero matches, same pre-existing pollNews/Chromium/SSC-breaker/pdf-extractor/RAG realm as S30/S31/S33 (2 already root-caused: `1294b-bctc-fallback.test.ts`, `FIX-1267-ssc-circuit-breaker.test.ts`). toolCount=183 unchanged (dry-run + live `:3000/health`). No scheduler file touched. Live server-boot: module load clean, final `listen()` hit a pre-existing process already on :3000 (EADDRINUSE, same S31/S32 precedent) — that live instance's health/bctc-inspect/news-fetch probes all clean. `git status --porcelain -- apps/mcp-server/` confirms exactly 2 files (1 modified, 1 new) — matches board `files:[]`. Board `READY`→`REVIEW` via `orch-apply.sh`, `next_agent=qa`. NOT committed — router RAW-verifies and commits per explicit dispatch instruction.
