# PO decision journal — WIP deadlock, CATCHUP chain, architect next_agent — 2026-08-23T10:17:06Z

**task-id:** UC-CCA-P3 · UC-CDC-P1 · FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58 · TASK-COWORK-CATCHUP-2 · FIX-SIGNAL-INBOX-NON-DRAINABLE-ENVELOPE-50-OF-51-FILES-SILENTLY-CLASSED-LITTER
**Session:** `7be6b4cd-057e-419b-a967-4810daf2b646`, router-direct, PRE-CLAIM `intent:po:wip-deadlock-and-catchup-disposition`
**Board at start:** HEAD `eceb60bba`, `in_progress[]=3`, `wip_in_progress=3`, limit 2
**Inputs:** `docs/data/orch/orch-state.json` · `docs/data/orch/archive/{2026-07,2026-08}.json` · live execution of `scripts/lib/devteam-eligibility.jq` predicates · `git cat-file`/`merge-base`/`show --stat` · `scripts/orch-row-prose-ceiling-check.mjs` · `docs/agents/developer/flow/main.md` · both 2026-08-23 architecture briefs
**Writes:** `scripts/po-triage-20260823T1017Z-wip-deadlock-and-catchup-disposition.jq`, `scripts/po-triage-20260823T1017Z-followup-mints.jq`, both piped through `scripts/orch-apply.sh`

---

## S1 — All three WIP rows were umbrella/tracking rows with zero live work

**what-considered:** (a) finish them; (b) demote all three to `ready[]`; (c) close as obsolete; (d) rule each on its own evidence.

**why-decision:** Took (d). Measuring first changed the answer for every row, so a uniform disposition would have been wrong three different ways.

| row | prose | children | live work of its own |
|---|---|---|---|
| `UC-CCA-P3` | 12161B | 9/9 `DONE_VERIFIED`, cold-archived | none — close condition already met |
| `UC-CDC-P1` | 13724B | WP-A 3/3 `DONE_VERIFIED` today; WP-B blocked | none — blocked on `UC-SDF-P2` |
| `FIX-SYSTEM-MAP-WATCHLIST-…` | 17859B | 3/3 minted and live on board | none — pm decomposition already done |

None of the three was "in progress" in any sense. Each was a stale tracking row holding a slot.

**why-change:** No change from plan.

## S2 — `UC-CCA-P3` closed because its own note said when to close it, and that moment was 8 days ago

Its `status_note` states the condition verbatim: *"Once all 7 pass QA (DONE_VERIFIED) and join FR1-FR2-SKILL + FR5-CODE-GATE (already DONE_VERIFIED), this umbrella can close DONE per PO's B1 Path-A ruling."* Probed the cold archive rather than trusting that note — the note is exactly the artefact that went stale:

`jq -c '[.done_tasks[] | select(.id|test("^UC-CCA-P3-")) | {id,status}]' docs/data/orch/archive/2026-08.json` → **9/9 `DONE_VERIFIED`**, last at `2026-08-15T00:00:28Z`. Zero `UC-CCA-P3-*` rows in any hot lane.

The validator initially rejected `DONE_VERIFIED` for want of `verification.raw_probe{tool,args,live_value_observed,observed_at}`. That gate is correct and I attached a real probe rather than downgrading to `DONE`.

## S3 — The two blocked rows: `status: BLOCKED` in place, because a lane move is defect-blocked

**what-considered:** (a) move to `backlog[]` per the write-side rule; (b) `status: BLOCKED` in place; (c) leave them and close something else.

**why-decision:** Attempted (a) first, on `UC-CDC-P1`, so the rejection would be measured rather than assumed. `orch-apply.sh` aborted, verbatim:

```
[orch-row-prose-ceiling-check] ABORTED — 1 row(s) with net new inline growth past ORCH_ROW_PROSE_CEILING_BYTES=12000:
  id=UC-CDC-P1 live=0B -> candidate=13724B
[orch-apply] ABORTED: row prose ceiling check exit 1 — live file untouched
```

`live=0B -> candidate=13724B` on a body where I changed only `status` and `blocked_by` — **both members of `STRUCTURAL_FIELDS`, both excluded from `proseBytes()`**. The row did not grow by one byte. The "growth" is an artefact of `PROSE_CEILING_LANES = ['backlog','ready','review']` (`orch-row-prose-ceiling-check.mjs:105`): leaving unguarded `in_progress[]` makes the live lookup miss, `liveBytes=0` at :267, and :269 rejects. That is defect D3 of `FIX-ORCH-PROSE-CEILING-…`, reproduced exactly as architect predicted.

Took (b). This is **not** a workaround. `wip_in_progress` (`devteam-eligibility.jq:115-118`) excludes any `in_progress[]` row normalizing to `BLOCKED`, and that file's own comment documents `BLOCKED`-inside-`in_progress[]` as a schema-**valid** "orthogonal sub-state". Both rows are genuinely blocked, so `BLOCKED` is the true status. The write-side companion rule (`execute-tier.md § CANONICAL:SSOT-STATUSFLIP-LANEMOVE(c)`) additionally wants the lane move; that half is recorded on each row as deferred-until-D3-ships, not silently dropped.

**Result:** `wip_in_progress` 3 → **0**. All four pickers open at full budget.

## S4 — CATCHUP-2 was never unverified. Only its `status` field lagged.

The dispatch framed Item 2 as "one unverified row dams nine". Measuring inverted the premise. `TASK-COWORK-CATCHUP-2` carries `qa_verified_at=2026-07-28T20:35:33Z`, `qa_verified_by=qa`, `updated_by=qa`, and a full `[QA] Review Record (direct-commit verify): APPROVED, DONE_VERIFIED` describing independent commit re-verification, an MD5 byte-identity check for NFR-2, a self-run 43/43 test pass, an independently reproduced RED, and a live fallback-path exercise. Only `.status` stayed `DONE`.

Did not take that record on trust — the "agent narrates unexecuted writes / fabricated PASS" class is well-documented here. Re-verified all four cited commits against live git: `c5e7c6747`, `fd5d4565e`, `64c41a6e0`, `d7330d539` are **all real, all ancestors of `HEAD`, all with `--stat` scopes matching their claims**. The QA record is genuine.

Ran the real predicate rather than reasoning about it. Before: `CATCHUP-3/4/5` → `deps_satisfied=false`, depmap `CATCHUP-1=DONE_VERIFIED, CATCHUP-2=DONE`. **CATCHUP-2 was the sole unsatisfied dep on all three.** After the status advance: `deps_satisfied=true` on all three. 6/7/8/9/10 remain blocked on 3/4/5/6/9 — a genuine dependency chain, not a dam.

`CATCHUP-1` was never missing: it resolves `DONE_VERIFIED` from cold archive `2026-07.json` via `archive_status_map()`. The cold-archive fallback works and is not implicated.

**why-change:** Advancing `status` ratifies QA's existing verdict. It does not manufacture a new one — that distinction is why this was PO-rulable at all rather than a QA re-dispatch.

## S5 — The desync is a class, and its second instance is still damming four rows

Scanned `done[]`: 3 of 15 rows carry `qa_verified_at` with `status=DONE`. Two are load-bearing — `TASK-COWORK-CATCHUP-2` (9 rows) and `TASK_RUNIDLE-1-AUDIT` (blocks `TASK_RUNIDLE-2`/`-3`, cascading to `-4`/`-5`).

**Deliberately did NOT flip the other two.** `TASK_RUNIDLE-1-AUDIT` and `FACTORY-APP-split-assembleBriefing` have `commit=none` and no `review_note` — materially weaker evidence than CATCHUP-2's. Curing a "status asserts more than the evidence supports" defect by asserting harder would be the same defect. Routed via mint instead.

## S6 — Item 3: the board field was wrong, but not for the reason given

Checked whether the two rows *should* be consistent before assuming it. Both briefs' own `§5`/`§6` name `developer` as the eventual zone owner for `cross-service/`, so at first the architect's `next_agent: developer` looked defensible and the router's framing looked inverted.

It is not, and the reason is sharper than "no handoff file". `execute-tier.md:123` documents a sanctioned direct-execute path explicitly having *"no task branch/handoff"* — so a missing handoff is not by itself disqualifying. The decisive measurements:

1. `docs/agents/developer/flow/main.md` Input line 12 + Step 0c + Step 3 read **only** `docs/handoffs/TASK_NNN.md`. None exists for this row.
2. `architect_handoff` has **zero readers fleet-wide** — grep across `docs/agents/`, `scripts/`, `.claude/` returns only the jq that *writes* it.
3. **The row's `note` is `null`.** Zero bytes. That is what closes the direct-execute escape hatch.

A developer dispatched today would receive four file paths and nothing else. The likely outcome is not a stall but a plausible *wrong* implementation: with only the file list visible, the obvious fix is exactly the reader-widening the brief's §2 explicitly rejects. Confirmed the misroute was armed, not theoretical — the live predicate returned `effective_next_agent=developer, deps_ok=true, supervised=false, plan_only=false`.

Set `next_agent: pm`. The two rows are now consistent at `pm`, but consistency was not the goal — they are consistent because both independently need a decomposition hop. "Who implements" and "what is the next hop" are different questions; architect answered the first.

Made the change myself rather than routing to pm: it is a one-field correction that disarms a live misroute, and leaving it armed to preserve field-ownership etiquette would have been the worse trade.

## S7 — Four mints, all from measurements taken while ruling

| id | pri | next | why |
|---|---|---|---|
| `FIX-ARCHITECTHANDOFF-DEAD-FIELD-ZERO-READERS-…` | P1 | architect | Root cause of S6. Three-way design choice, fleet-wide blast radius. |
| `FIX-NOTEBOOKAUTOPRUNE-GREPC-DOUBLE-EMIT-…` | P1 | developer | Reproduced directly. 5 sites. **100% of the inbox's malformed-JSON population.** |
| `FIX-QAVERIFIED-STATUS-DESYNC-…` | P1 | architect | S5's class. Needs an unknown second writer traced + an `orch-apply` guard. |
| `CLEAN-PRICEANOMALY-…-NO-AGE-CEILING-…` | P2 | developer | 21 files, oldest 52 days, no bound anywhere. |

On the prune bug I reproduced the mechanism rather than relaying it: `X=$(grep -c NOMATCH file 2>/dev/null || echo 0)` captures `"0\n0"`, because `grep -c` prints its own `0` **and** exits 1, firing the fallback. Wire evidence at `…2026-08-22T232534Z.json:13-14` — `"sentinel_section_count": 0` then a bare `0,`; `jq .` fails `Expected separator between values at line 14, column 2`. Scanning every `docs/signals/*.json`, exactly 2 files are invalid JSON and **both** come from this writer.

Routed #2 and #4 straight to `developer` with self-sufficient `desc`/`acceptance` blocks — legitimate per `execute-tier.md:123` precisely because their notes carry the spec, which is the test S6 says the signal-inbox row fails.

## S8 — What I did not do

- **Did not close `FIX-PROSECEILING-PO-MANUAL-DISPATCH-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET`.** The prose-ceiling brief §8 assigns PO this closure, but as **step 5 of 5** — after §3/§4 ship. The rule that supersedes it does not exist yet, so the name-list patch is still the only thing that would work today. Closing it now would be a silent drop dressed as a supersession.
- **Did not run `scripts/orch-cold-evict.sh`.** Terminal-lane bloat is real (`done_verified[]=30` vs threshold 0, `done[]=14` vs 10) and pm has skipped that gate three cycles, but a bulk hot-file eviction is not decision-only scope.
- **Did not stage the three untracked `docs/handoffs/TASK_00{1,2,3}-WATCHLIST-*.md`.** Side finding recorded on the parent row: pm authored the decomposition and never committed the artefacts, so the specs its three children point at are absent from `HEAD`.
- **Did not touch** `drain-signals.js`, `isDrainableShape()`, `isByPathConsumerFamilies()`, the `price_anomaly_*` files, or `dev-team/flow/main.md`.
