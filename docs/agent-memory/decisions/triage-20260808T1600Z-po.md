# PO decision journal — triage 2026-08-08T16:00Z

task_id: FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD
cycle: dev-team Step 1 PO Triage (S3 dispatcher-wrap, triage_key=task:po-triage-20260808)
coordination session: 165f4245-6173-4054-87fd-c55bb626265f

## STEP po-1 — rag-service A-30 WARN: ruled FALSE POSITIVE, not (a)/(b)/(c) as framed

**what-considered.** The auditor asked for one of three: (a) benign continuation of the
FU-RAG-DEPLOY-MEMORY steady state, (b) escalation beyond accepted bounds, (c) remediation
beyond the existing backlog. All three presuppose the reading is *correct*. I checked the
reading first.

**why-change (from the brief's premise).** Two of the brief's load-bearing facts are false:
- "VmHWM pinned at container cap (1.5GiB/1.5GiB)" — live cgroup limit is **1.0GiB**
  (`docker inspect ... {{.HostConfig.Memory}}` = 1073741824; last compose change to this
  service is `2f835ec63` "raise memory cap 768m->1g"). 1.5GiB is VmHWM's own value
  (1568064kB). The cap slot was filled with VmHWM, making "pinned at cap" self-referential.
- "zero reclamation dips / loss of reclamation" — `docker stats` 19 minutes after the
  sample window reads **82.00% (839.7MiB / 1GiB)**. 94.69% of 1GiB is 969.6MiB, so ~130MiB
  was reclaimed *inside* the interval declared reclamation-free.

Answer to the auditor's question is **(a) benign**, and the WARN itself is a detector FP.

**Root cause, read at source rather than inferred.** The branch that fired is
`verify-a30-mcp-memory-reclamation.sh:326` (`MINP > 93`), not the VmHWM branch (:325 — c373
itself records VmHWM "NOT advancing"). Its window is `PROBES=12 x INTERVAL=25s` (:124-125)
= 5 min max. rag-service's only reclaimer is the embedder idle-unload at
`app_factory.py:87`, threshold **15 minutes**, polled every 60s (:46, :108-109). A 5-minute
window cannot contain a 15-minute event, so `MINP > 93` is unfalsifiable for this container.

**Discriminator that stopped me generalising.** Same probe, same tick, mcp-server-1 is a
**true** positive: 96.37% live (2.891/3GiB, above its whole sampled band), VmRSS 3015024kB
≈ VmHWM 3034944kB, cap 3.0GiB verified correct. So the predicate is fine and the threshold
must not move — the *window* is uncalibrated. That pair is now the row's regression fixture
and AC-4 forbids touching the 93/97 constants.

## STEP po-2 — mcp-server A-30: true positive, FOLDED, and an explicit do-not-restart ruling

Folded to `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` (IN_PROGRESS, live worker) +
`FIX-MCP-MEMORY-CODE-LEAK` (REVIEW). The mid-triage `cow-20260808T155644` ESCALATE is the
same finding from a second detector — folded to the same row, not minted.

**why-change: none from plan** on the fold. Added ruling: **do not restart/rebuild
mcp-server** to relieve 96.37% while the reaper fix is in flight — transport.ts/server.ts +
2 test files are uncommitted worker state, and `feedback_restart_masks_bun_jit_corruption`
says a restart destroys the reproduction while resetting the meter.

## STEP po-3 — sweep-guard occ 28-31: folded, ruling re-affirmed, one new consequence

Only path: this row owns the family (28-31 continues 23-27). Payload tag parsed first per
`triage-signals.md`; never adjudicated off `git show --stat`. Threshold stays 3.

**New, not previously recorded:** the 13:37:32Z fire was **n=30** — six agents' notebooks,
6 analysis-briefs, 4 ledgers, orch-state.json. Blocked, correctly. But unlike occ 24-27
(4/4 converged on a scoped retry within 3-15s) **nobody retried it**: those 30 paths are
still uncommitted at 16:00Z with an empty index. The block is right; the gap is that a
multi-agent artifact set has no scoped committer. Cross-referenced to the existing
Bash-grant / stranded-state rows, deliberately not re-minted.

## STEP po-4 — the durable inbox is write-only (24 envelopes, 0 consumed)

`FIX-DEVTEAM-IDLE-CHAIN-P2A-DURABLE-DRAIN` (producer) went DONE_VERIFIED at 15:33Z and
`P1A-MAIN-ROTATION` at 15:37Z — both `depends_on` of
`FIX-DEVTEAM-IDLE-CHAIN-MAIN-COMPLETION` (P0), the **only** planned consumer (§3.2
read/clear). Until it lands, `.dev_team_idle_chain.pending_triage_inbox` accumulates with
no reader: 24 envelopes, oldest 13:08:59Z, including 2 unrouted `ci_red` that had sat 2.5h,
2 CRITICAL `microservice_degraded`, a 101h VPS staleness and a cron-failure signal. They
reached PO this tick only because I read the array by hand.

**what-considered:** stamping `consumed_at` myself to clear the backlog.
**why-change — rejected:** inventing a consume contract ahead of §3.2 would fork the schema
the row exists to define. Folded into BATCH instead, verbatim from the row's own fields.

## STEP po-5 — manual-dispatch sweep: top candidate skipped on prior art

83 candidates. #1 `TASK-COWORK-MUTEX-001` (P0) **skipped**: its deliverable
("Cowork-Slot Collision Probe Core (dispatch-claim/SKILL.md + CLAUDE.md)") already appears
live — `CLAUDE.md:14`, `CARD.md:35`, `SKILL.md:194/:288/:563`, SKILL.md last written
2026-08-07 vs the row's 2026-07-30 mint. Dispatching would have re-implemented shipped code.
**Not closed either** — the match is on behaviour, not on the row's ACs (it has none beyond
a one-line note) and it has two unexamined siblings. Flagged for AC-level verify.
#2 `UC-ASL-P5` skipped (supervised + `deploy_gate: user-approved-off-market` + zone `multi`
— folding it strands it, defeating the sweep's purpose); #3-#5 skipped (`reflag=true`,
already surfaced and undispatched). Selected #6
`FIX-AUDITOR-SBVFX-SLA-POSTMARKET-TOLERANCE` — the highest-ranked candidate that is
simultaneously unflagged, dev-role-routed, single-zone and not deploy-gated.

## STEP po-6 — two ci_red, one file, zero new rows

Both `CI-RED-efaae0d4` (run 31258403592) and `CI-RED-6cbc7836` (run 31262924171) resolve via
the mandatory pre-dedup `gh run view --log-failed` to the *same* offender:
`coordinationStore.ts — baseline-tolerance-exceeded (baseline=1241L actual=1388L
upper=1365L)`. File-scoped dedup hit on
`FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L`'s existing `dedup_key`. Folded both; the
23-line overage keeps CI red on every commit to main.

## Persistence

Board mutation via `scripts/po-triage-20260808T1600Z-a30-window-vs-reclamation-period.jq`
piped through `scripts/orch-apply.sh` (Stage 0+1 PASS, conservation task_total 766→767,
9 rows stamped). Per my own standing rule from the 15:13Z cycle, every mutation was
**re-read field-by-field on disk** after apply — not trusted from the wrapper's `OK` line.
