# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect (continuation -2)

**Sprint goal:** Make cowork `guaranteed:true` an honored contract — bounded catch-up/look-back for slots whose window elapsed during host standby / session-down, or correct the label.
**Agent:** architect
**Started:** 2026-07-31T02:41:00Z (rolled from base file, CAP-REACHED 2026-07-31T01:35:33Z)

---

### STEP architect-S22 · architect · 2026-07-31T02:41:00Z
**task-id:** FIX-SUBAGENT-BRANCH-CHECKOUT-HIJACKS-SHARED-WORKING-DIR
**what-done:** Verified git has NO `pre-checkout` hook (`git help githooks`, git 2.49.0 — only `post-checkout` exists; cannot block the checkout, only revert-after + force the caller's exit code). Designed + live-proved (disposable scratch repo, 5 scenarios) a new `scripts/git-hooks/post-checkout` hard-revert guard, `MODE=enforce` default. Wrote `docs/architecture-briefs/2026-07-31-fix-subagent-branch-checkout-hijacks-shared-working-dir.md`. Found (grep, not assumed) that 5 live flow docs still actively author/verify/honor a `task/NNN-*` checkout convention post-dating `UC-RDL-P7`'s 2026-07-17 ruling, one of which (`pm/flow/main.md:75`) fired hours before this brief on the row's own cited incident file. Moved board row `backlog[]→ready[]`, `next_agent: pm`, `architect_brief`/`architect_review_note` written via `orch-apply.sh`.
**what-considered:**
- AC-1(b) worktree-isolation-for-everyone (rejected as primary — blast radius is the whole 8-agent-type fleet vs `SPIKE-C44`'s own 2-developer-disjoint-zone scope, and that spike is itself still unstarted; chosen design exempts linked worktrees by construction so it does not foreclose (b) later — tested live, `SPIKE-C44` is therefore NOT a dependency of this row).
- AC-1(c) schema-level ban on `branch:` (rejected as this row's own primary layer — grepped `orchStateSchema.ts`, no `branch` field exists to ban; the trigger lives in free-form markdown handoffs, nothing for a schema to bind to) vs (a) post-checkout hook (chosen — reachable with zero MCP grant, same "beneath the specialist population" argument that justified the sweep-guard hook).
- `MODE=enforce` default vs mirroring the sweep-guard hook's own `warn` default (chosen enforce — deliberate divergence, justified: unlike the sweep-guard's dominant-legitimate bare-commit idiom, zero legitimate off-`main` state exists live today per `UC-RDL-P7`'s own re-confirmed ground truth, and a revert is self-healing, not blocking, so the "don't red the whole fleet day 1" risk that drove WARN-first there does not transfer here).
**why-decision:** Every claim is live-verified, not assumed: `git help githooks` for the no-pre-checkout fact, `git branch -a`/`git worktree list` for the zero-legitimate-branch ground truth, grep across 5 flow docs for the still-live `task/NNN-*` convention (found the literal handoff file from one of the row's own 4 cited incidents, dated hours before this brief), and 5 scratch-repo scenarios (control/guarded/non-destructive-conflict/worktree-exemption/re-entrancy) for AC-4's live positive control — asserting from hook source prose alone was explicitly named a rejection condition in the row's own AC-4.
**why-change:** No change from the row's own AC — this cycle's substantive addition beyond the router's framing is the co-requisite finding (§1.3 of the brief): the hook and `UC-RDL-P7` STEP2 are not independent siblings but a rollout pair (shipping the hook alone breaks `developer/main.md`'s own `VERIFY` line for every M/L task), handed to PM as a coordination recommendation, not implemented or absorbed into this row (AC-3 dedup boundary — did not mint a 3rd row, did not touch the 5 flow-doc files, which are `agent-father`'s declared `commit_zone`, not this row's or developer's).

### STEP architect-S23 · architect · 2026-07-31T09:27:37Z
**task-id:** FU-OCR-BOOT-LOOP-SEQUENTIAL
**what-done:** Read composition-root.ts §4b (the bootstrap OCR loop, FIX-CTG-3-STEP-D/21c467e8) + pdfOcrWorker.ts's `extractAndStorePdfPages` in full. Live-probed `vn-market-intelligence-mcp-mcp-server-1` (docker logs, 2 consecutive boots 2026-07-30T22:07Z + 2026-07-31T00:56Z; `bun -e` read-only query against `/app/data/market.db`) instead of assuming the row's "small in steady state" premise. Verdict: leave the sequential loop as-is — no code change. Flipped board row `in_progress[]→done_verified[]`, `.status=DONE_VERIFIED`, `.head` reset to idle, in the SAME `orch-apply.sh` write (CANONICAL:SSOT-STATUSFLIP-LANEMOVE). Minted new backlog row `FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH` (P2, zone `apps/mcp-server/`, owner+next_agent both explicitly `dev-mcp-server` — not `.route`-only) for a real, separate, currently-live bug found during the probe.
**what-considered:**
- Whether "cold-start latency" (the row's own framing) is even the right lens — traced the loop's call site: it fires via `setTimeout(10_000)` AFTER `createBunServer()` already made the MCP server live (composition-root.ts:62 vs :137), so sequential-vs-parallel cannot affect server-boot/health latency at all, only background Tier-3-cache warmup timing. This reframes the original dev-mcp-server note's premise, not just answers it.
- Reintroducing parallelism via the originally-floated `reparseSingleAfterOcr` helper (restoring `Promise.allSettled`) — rejected: cross-checked against my own prior sibling finding `docs/architecture-briefs/2026-07-28-pdfx-tesseract-concurrency-invariant.md` (apps/pdf-extractor, uncontrolled tesseract concurrency measured driving a container to 98.92% of its 2.5GiB cap with near-zero effective throughput) — direct, measured, same-codebase corroboration that OCR concurrency here is genuinely dangerous, not just a plausible-sounding rationale in a 2-month-old commit message.
- Whether the 80-page-cap bug I found belongs inside FU-OCR-BOOT-LOOP-SEQUENTIAL's own closeout (folded, not minted) vs a separate row — chose separate: root cause (pdfOcrWorker.ts completeness-threshold math) is orthogonal to sequential-vs-parallel and would reproduce identically either way; folding it in would have made a correct "no action" verdict on the ORIGINAL question look like it required a fix, and would have buried a `dev-mcp-server`-zone bug inside an architect-closed row that lane-moves to `done_verified[]` (a lane no dispatcher sweeps for pickup).
**why-decision:** Did not trust the row's "boot OCR backlog is small in steady state" claim — measured it: 271/279 PDFs on disk are fast-skip cache hits (true, backlog COUNT is small), but 3 large PDFs (204p/255p/285p) are pinned at the 80-page cap and re-OCR'd in full on every single boot forever (the "small backlog" framing is right on count, silent on the 3 outliers that actually dominate wall-time — ~10-12 real minutes each, ~33min total observed 22:07:03Z→22:40:10Z). DB query confirmed the pin (`c=80` for all 3, both boots, never advancing) rather than inferring it from log timestamps alone.
**why-change:** Dispatcher offered 3 verdict shapes (leave as-is / mint a `reparseSingleAfterOcr` FIX / something else) — landed on a combination: (a) for the literal question asked, plus a 4th shape not offered (mint an UNRELATED FIX for what the live probe actually surfaced), because the real backlog driver was a different bug than the one the row hypothesized. Also flagged for the record (not acted on beyond the note): this row's own cold-detail entry carried only `.route` (no `.owner`/`.next_agent`), which is exactly why BOUNDED-1's NON-DEV-OWNER/NON-DEV-NEXT_AGENT gates (`scripts/lib/devteam-eligibility.jq`) didn't exclude it despite `route:"architect"` being a non-dev target — dev-team had to router-correct before dispatch. A live sibling row, `HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING`, carries the identical `.route`-only shape. Not fixing the gate script myself (scope: this is a REVIEW-type task, not a FIX dispatch) — noting it here for po to decide whether it clears the recurring-bug bar (2nd confirmed instance of the same shape) independent of `FIX-DEVTEAM-BOUNDED1-NONDEV-OWNER-BOARD-FALLBACK-GATE`, which fixed the sibling `.owner`-present-but-non-dev case, not the `.route`-only/no-`.owner` case this row and `HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING` both have.

### STEP architect-S24 · architect · 2026-08-01T01:48:40Z
**task-id:** FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN
**what-done:** PLAN-ONLY. Read full row history first (per dispatch instruction), then found an UN-referenced but further-along prior-art brief (`SPIKE-DEVTEAM-QADRAIN-HEAD-SLOT-DECOUPLE`, agents-architect 2026-07-29) whose Part 1 (`FIX-DEVTEAM-QADRAIN-HEAD-WRITE-CONDITIONAL`) turned out to be DONE_VERIFIED the SAME tick as this dispatch, whose Part 2 (busy-tick reachability) is transitively blocked on a stalled P0 (`FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` ← `TASK-DEVTEAM-IDLE-CHAIN-1-SCHEMA-UTILITIES`, itself stuck in REVIEW since 07-29 — same disease this row exists to cure), and whose Part 3 (throughput cap) is exactly this row's remedy-(a) but was still unplanned. Wrote `docs/architecture-briefs/2026-08-01-review-lane-drain-throughput-and-secondary-sweep.md`: §1 supplies Part 3's design (batch N-oldest claim, `QA_CAP=10` named constant, BGFAN-1 fan-out spawn, reuses the SPIKE brief's own claimed_at/claimed_by correlation idiom) so it does not need a 2nd planning pass; §2 designs a brand-new SECONDARY-Drain mechanism (44 live rows, zero existing sweep of any kind, confirmed by grep across every agent flow doc) that deliberately never touches `.head` or a new board lane (TaskBoardSchema is `.strict()` with 9 enumerated lanes; TaskSchema rows are `.passthrough()`) so it can ship head-decoupled from day one, unlike PRIMARY's own retrofit.
**what-considered:**
- Re-deriving remedy (a) from scratch vs adopting the existing ratified SPIKE's Part 1/2/3 split — chose adopt: re-deriving would have ignored a PO-ratified design already covering the identical ground, risking the exact "prior-art blindness → phantom gap" failure this same row's own 2026-07-21 text was minted to correct in the router.
- Routing SECONDARY rows into a new `.task_board.triage[]` lane (mirrors PRIMARY's own `qa[]` shape) vs stamping in place — chose stamp-in-place after reading `orchStateSchema.ts` directly: `TaskBoardSchema.strict()` enumerates exactly 9 lanes, a 10th is a real TS/validator change, not a jq-only one; `TaskSchema.passthrough()` makes new stamped fields free. Also the task's own explicit constraint ("do NOT piggyback on qa[]") ruled out reusing the existing lane.
- SECONDARY null-`next_agent` fallback: `po` vs `architect` — chose `po`: live data shows `architect` already owns 10/44 SECONDARY rows, the largest non-null share; routing null rows there too concentrates load on the agent authoring this very mechanism.
**why-decision:** Verified every load-bearing claim live rather than trusting either the dispatch's own numbers or the SPIKE brief's now-3-day-old ones: re-ran `devteam-review-lane-drain-report.sh` (198 PRIMARY / 44 SECONDARY today, was 202/244-total at dispatch time — normal same-session churn), re-checked Part 1/2/3's actual current board status (Part 1 DONE_VERIFIED this tick, Part 2 still BACKLOG+blocked, Part 3 still BACKLOG+unplanned), grepped po/flow + dev-team/flow for any existing `review[]` scan (zero hits, confirms SECONDARY truly has no surface today), and traced Step 0b's actual control flow (S2 claim-fails-on-peer-held-lock is the steady-state busy case under hourly cron + 3600s TTL, not the rarer immediate-JUMP-TO-execute case) to confirm the Session-Gate/Step-1 boundary is genuinely reachable often, not just in theory.
**why-change:** Dispatch asked me to pick between (a)/(b)/a sequenced combination — picked (a)-first-then-(b) as asked, but the CONCRETE shape of both differs from a naive re-read of this row's own suggested-remedy prose: (a) is not a fresh design, it is "close out Part 3 with the design it already owed"; (b) deliberately does NOT mirror PRIMARY's `.head`/`qa[]` mechanics (schema + coordination-risk reasons above) even though the task text's own phrasing ("equivalent age-ordered sweep") could be read as asking for a structural mirror.

### STEP architect-S25 · architect · 2026-08-05T09:20:00Z
**task-id:** FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE
**what-done:** Direct-implemented (own file family, S24 precedent + row's own `plan-only:false` shape): `resolved_secondary_dispatch_target()` (`scripts/lib/devteam-eligibility.jq`) now treats `next_agent=="dev-team"` the same as null — falls back to `"po"` — instead of passing it through verbatim. Live-verified via read-only jq re-query (no write) that all 4 currently-live dev-team-targeted `review[]` rows (`FIX-BCTC-FPT-BT5-BALANCE-GATE`, `FIX-MACRO-THRESHOLD-FXFLOOR-OVERCLAMP`, `FIX-FOREIGN-FLOW-BULLETIN-UNAVAIL-STRING`, `FIX-DRAIN-TEST-HARNESS-ORCH-HELPER-COPY-LIST`) now resolve to `"po"` — matches PO's own triage enumeration exactly (AC-2/AC-3). New regression test `scripts/devteam-eligibility-resolved-secondary-dispatch-target.test.sh` (5/5 pass: null/absent fallback unchanged, dev-team→po THE FIX, architect/ops passthrough unchanged — no-regression controls). Re-ran `scripts/orch-backlog-stub.test.sh` (7/7 pass, unaffected sibling predicate). Updated both doc call sites (`scripts/devteam-review-claim-secondary-drain.jq` header, `docs/agents/dev-team/flow/main.md` § Review-Lane SECONDARY-Drain Claim paragraph + new Reusable Scripts bullet) so the fix isn't silently undocumented at its only 2 prose describers.
**what-considered:**
- Fix shape (a) resolver-level default vs (b) exclude self-targeted rows from SECONDARY selection entirely — chose (a), per the row's own explicit steer ("cheapest, consistent with the existing single defaulting rule, correct because a dev-team-targeted review row is by definition one nobody has triaged to a real owner") — same shape PO's own row-level workaround (`OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE` → `architect`) already used, generalized to the resolver instead of hand-applied per row.
- Case-sensitive exact match on `"dev-team"` vs regex/case-insensitive — chose exact match, matching this file's own precedent (`is_design_router_allowed`'s documented case-sensitive-exact-match convention against canonical lower-case `system-map.json` agent ids); `is_dev_role`'s `"i"`-flag regex is a DIFFERENT predicate serving a different purpose (dev-ROLE detection, would also match `dev-mcp-server` etc.) and is not reused here.
- Test placement: new standalone `scripts/*.test.sh` (chosen, mirrors `orch-backlog-stub.test.sh`'s exact pattern — isolated fixture, executes the real `include`-d def, never reads the field back) vs extending an existing suite — no existing suite executes this def (grep-confirmed the 2 `.test.ts` hits are comment-only).
**why-decision:** Grep-confirmed `resolved_secondary_dispatch_target` has exactly ONE caller repo-wide (`devteam-review-claim-secondary-drain.jq`) before editing, so the fix's blast radius is fully enumerable; re-ran both the new and the pre-existing sibling test suite after the edit (12/12 pass total) rather than trusting the jq syntax check alone.
**why-change:** No change from the router's own fix-shape steer — implemented (a) as suggested, took architect's own explicit judgment call to implement directly rather than brief-only (task text: "this is small enough you may implement directly... at your judgment").

### STEP architect-S26 · architect · 2026-08-05T09:25:00Z
**task-id:** FIX-BCTC-FULL-SERVING-EMPTY-NEWEST-PERIOD-HEAD-OF-LINE
**what-done:** Read `bctcFullTools.ts` in full (never-design-without-reading) plus the sibling `getBctcPendingRefineTool.ts` review_note and `reports.ts`'s `get_financial_summary`/`compare_financials`. Confirmed root cause: `get_bctc_full`'s `ORDER BY sort_key DESC LIMIT 1` (:954-958) selects the absolute-newest `financial_reports` row with no usability predicate; PUB-1 (checkPublishability, :616-627) correctly rejects an empty PENDING shell but the handler (:1190-1200) returns that rejection verbatim with zero fallback to an older DONE/PARTIAL row. Wrote `docs/architecture-briefs/2026-08-05-fix-bctc-full-serving-empty-newest-period-fallback.md`. Wrote findings to the row's own `architect_review_note` via `orch-apply.sh` (direct PO board-mint, no handoff file, per established convention) and set `next_agent: dev-mcp-server`.
**what-considered:**
- Shared-root-cause hypothesis (PO's explicit ask) — checked literally: grepped both files for cross-imports (none), compared query shape/selection-axis/fix-mechanics side by side (brief §2 table). REFUTED as a shared-helper claim (different files, different scope: one-ticker-newest-period vs all-tickers-refine-queue, different sort axis, sibling's exclusion-clause fix has nothing to "fall back to" since it operates on a multi-report queue). CONFIRMED as the same recurring bug CLASS — 3rd instance in the file family (reports.ts's `get_financial_summary` already carries a narrower version, `FIX-BCTC-SERVE-GATE-FINANCIAL-REPORTS`).
- Fix shape: unconditional fallback for ALL calls vs scoped to the no-explicit-`year`/`quarter` path only — chose scoped: an explicit `{year, quarter}` request silently answering a different period than asked would be a correctness regression, not a fix; mirrors the explicit-vs-implicit split `reports.ts` already established between `get_financial_summary` (implicit-latest) and `compare_financials` (always-explicit, confirmed NOT affected by this bug class at all).
- New shared domain-layer helper vs inline candidate-scan loop in the existing tool file — chose inline (extend the existing exported `checkPublishability` call, reuse not duplicate); flagged extraction as a future trigger IF a 4th instance turns up during the already-sequenced `SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS`, not now (YAGNI, one extra call site doesn't justify a new abstraction).
**why-decision:** Did not take PO's shared-helper hypothesis on word — read both implicated files' actual code (not just the sibling's review_note prose) before ruling, since the hypothesis was explicitly posed as a question to verify, not a conclusion to extend. Reused the existing `checkPublishability`/`isBankFormFromDb` exports rather than inlining a second PUB-1-only predicate, per `always_extend_not_duplicate`.
**why-change:** No change from PO's own AC (AC-1..3, fallback-to-most-recent-COMPLETE-period) — this cycle's interpretive addition is scoping the fallback to the no-filter path only (PO's note didn't explicitly address explicit-period calls) and the blast-radius table in brief §3 (found reports.ts already has a partial sibling fix, flagged not folded in).

### STEP architect-S27 · architect · 2026-08-05T09:32:00Z
**task-id:** FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE
**what-done:** CORRECTION to S25's own closeout. Initially self-closed the row `backlog[]→done_verified[]` after direct-implementing + self-verifying (unit test + live re-query). Re-examined the task text's own "hand off per your normal contract" phrasing and the standing system-wide practice (every other code-touching board row's own history goes review→qa_verified→done_verified, never self-closed by its own implementer) and re-routed `done_verified[]→review[]`, `status:REVIEW`, `next_agent:qa` instead, in the same `orch-apply.sh` write pattern, preserving the `architect_review_note` evidence + `commit:4a6d53151` for QA to independently re-run rather than re-derive.
**what-considered:**
- Leaving DONE_VERIFIED as-is (the task's "implement directly... at your judgment" phrasing could be read as also authorizing self-closeout) vs re-routing to `qa` — chose re-route: the authorization text only relaxes "brief-only, developer implements" into "architect implements," it does not say anything about skipping independent QA sign-off, and my own `init.md` boundary ("no direct implementation... developer executes") was explicitly waived for the IMPLEMENT step only, not for closing the loop myself.
- Live production dispatch-logic code (consumed by every SECONDARY-Drain tick, cross-service/) is exactly the class of change `trust=system` memory (verification is system's job, not the implementer's self-report) exists to gate — the fact my own AC-2/AC-3 checks are objectively re-runnable does not substitute for an independent run.
**why-decision:** No board-schema or lane-transition rule forbids a `done_verified[]→review[]` reversal (checked `LANE_ALLOWED_STATUSES`/orch-validate — status/lane coherence only, no forward-only-terminal enforcement); Review-Lane QA-Drain will auto-pick this row now that `next_agent=="qa"`.
**why-change:** Self-correction, same cycle, before any downstream agent acted on the DONE_VERIFIED state (git log confirms the done_verified commit and this correction commit are adjacent, no intervening board reads by another agent).

### STEP architect-S28 · architect · 2026-08-05T09:34:00Z
**task-id:** FIX-AUDIT-OUTPUT-CONTRACT-SIGNALQUEUE-ROWS-WRITTEN-SELFREPORT-MISMATCH
**what-done:** Read `scripts/audit-output-contract.sh` (255L) + `scripts/emit-audit-signal.sh` (551L) in full, plus all 6 `emit-audit-signal.sh` call sites and the 1 `audit-output-contract.sh` call site in `main.md`/`tier1-probe.md`. Reproduced BOTH mismatch directions against the real, unmodified script in an isolated scratch dir — single bash process, zero peers, zero cron — ruling out PO's named "ordinary concurrent-write timing skew" hypothesis by construction. Root-caused two independent structural bugs (not one): Bug A, `cycle_start_ts`=`$FIRE_TICK` is always minute-precision while row `.ts` is always second-precision, and V1's raw jq string `>=` silently drops any row in the tick's own clock-minute (explains 4412/4415); Bug B, no call site ever passes `--from-agent`, so every tier/session shares the identical default identity `"system-auditor"` and V1's `from`+ts-window query picks up ANY peer tier's concurrent row (explains 4420, the dangerous inflate-the-self-report direction). Wrote `docs/architecture-briefs/2026-08-05-fix-audit-output-contract-signalqueue-mismatch.md` with diffs (port the existing `to_epoch` helper already live in `emit-audit-signal.sh::_ledger_prune_and_lookup()`; add a `--cycle-tag` flag threading the already-computed, already-unique `FIRE_TASK_ID` through both scripts as a new passthrough `audit_cycle_tag` row field). Wrote `architect_review_note` via `orch-apply.sh`, moved `backlog[]→ready[]`, `next_agent:developer`.
**what-considered:**
- Replacing "trust the higher value" with a direction-locked/asymmetric rule (the row's own suggested framing) vs justifying it — chose justify: once Bug A/B are fixed, the only remaining divergence sources (post-write drain, or a dropped marker line) both genuinely favor the higher operand; the policy was fed corrupted inputs, it was never itself unsound. Replacing it would have papered over the real defects.
- Fix mechanism for Bug B: add an upper `ts` bound to narrow the cross-check window (cheap) vs a per-cycle identity tag — rejected the window-narrowing alone: Tier-1 fires far more frequently than Tier-2/3/5 so cross-tier overlap during any longer cycle is routine, not rare; only an exact per-cycle identity (`FIRE_TASK_ID`, already computed, already unique per tier+tick) removes the collision structurally.
- `next_agent`: `pm` (decomposition) vs `developer` directly — chose `developer`: S-size, single-behavior fix, atomic scope (two scripts + call-site threading), matches the closest live precedent `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` (same S/P2 shape, routed developer/cross-service/ directly, no BA/PM needed).
**why-decision:** PO's explicit instruction was to rule out concurrency BEFORE concluding the rule is wrong — did so with a live repro against the production script rather than reasoning about it, since "internal consistency is not corroboration" (both directions had to reproduce deterministically, single-process, to count as ruling out timing).
**why-change:** No change from PO's framing except the conclusion itself: PO's memo hypothesized either "benign concurrency, fix via snapshot-and-compare" or "the reconciliation rule is wrong" — neither was correct; the actual defects are upstream of the rule (input-format + input-scoping), and the rule survives unchanged once its inputs are fixed.


### STEP architect-S29 · architect · 2026-08-05T09:34:53Z
**task-id:** FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE
**what-done:** Root-caused (live-confirmed via docker logs, not inferred) why 66 fallback-<TICKER>-<YEAR>-<Q> financial_reports rows never produce a bctc_layout_units/bctc_table_rows entry: pushBctcLayoutHandler.ts/pushBctcTableHandler.ts/pushBctcMdTablesHandler.ts each gate report_id on isValidUuid() FORMAT (not existence), a check pre-dating SPRINT-1330's fallback-id minting path by weeks — pdf-extractor logs show real PEK extraction succeeding (67/67 pages etc.) then the callback push HTTP-400-rejected every time, 50x in 96h. Wrote brief (docs/architecture-briefs/2026-08-05-fix-bctc-fallback-shell-reports-structurally-unextractable.md) recommending the format check be replaced with an existence check against financial_reports.id in all 3 handlers (db already DI'd, zero new plumbing). Wrote architect_review_note via orch-apply.sh, moved backlog[]->ready[], next_agent=pm.
**what-considered:**
- PO's option (a) make-extractable vs (b) backfill-repair-and-abandon — chose (a): (b) permanently throws away 63 real stranded PDFs and leaves the id-shape assumption live for the next non-UUID scheme.
- existence-check vs a second fallback-id-shape regex alongside isValidUuid — chose existence-check: closes an orphan-write gap that exists TODAY (any syntactically-valid-but-unknown UUID is currently accepted) while ALSO fixing the bug, no second regex to keep in sync as new id schemes appear.
- fixing all 3 push handlers now vs only pushBctcLayoutHandler.ts (the one actually hit live today) — chose all 3: reconcile job's own success check already ORs across all 3 tables by design ("forward-compat"), same latent bug, near-zero incremental cost, avoids a guaranteed future recurrence.
**why-decision:** reconcile job's re-derive-by-(action_code,sort_key) lookup is format-agnostic and the trigger call forwards report_id verbatim — the ONLY gate in the whole round-trip is the 3 push handlers' isValidUuid check; fixing there closes AC-1/AC-2 without touching the reconcile job itself.
**why-change:** no change from PO's framing except precision — PO scoped "id-format assumption somewhere" as the hypothesis to verify; confirmed exactly that, pinned to a specific 5-line if-block x3 files via live docker logs, not pattern-matched.

### STEP architect-S30 · architect · 2026-08-05T16:33:51Z
**task-id:** SPIKE-BOUNDED1-ELIGIBILITY-CONTRACT-REVIEW
**what-done:** Independently re-verified (not the 07-21 self-report) that `scripts/lib/devteam-eligibility.jq` is live today: all 5 originally-cited call sites still `include` it (grep-confirmed), adoption expanded to 30+ callers (every new picker since 07-21 — DRS, wrapper-autoclose, SECONDARY-Drain, po-manual-dispatch-eligibility — extended this lib, none forked new predicates), 2 live regression suites pass (5/5, 7/7; one suite dated TODAY closed a real self-target bug through the shared fn, benefiting every caller at once — the exact payoff this SPIKE bet on). Flipped `review[]→done_verified[]` via `orch-apply.sh`.
**what-considered:**
- Trusting the 07-21 "ADOPTED AND IMPLEMENTED" note vs re-running the actual instruments live — chose live re-run (task's own mandate: code drifts). Ran `devteam-dispatch-gate-satisfiability.sh` + `bounded1-supervised-lane-report.sh` against real board data (scratch copies, never mutated live file); both exited non-zero.
- Traced both FAILs to root cause before deciding scope: (1) stale `ready[]` promoted_by stamp on `FIX-DEPTHTHIN-B-GATEWAY-TA-PATH-REWRITE` surviving a PO WIP-eviction, exposing that `devteam-backlog-claim-bounded1.jq` has no self-contained WIP recheck (relies entirely on `main.md:549`'s outer bash gate) — confirmed harmless in production today (WIP=1 live, gate never invoked) but a latent gap, NOT a devteam-eligibility.jq defect; (2) 5 backlog rows with owner+next_agent both null resolve dispatch-lane=none — `resolved_dispatch_lane()` correctly reports unresolvable, a mint-time hygiene gap already tracked at the PO-residual-census/DRS level, not a predicate bug.
- Whether either FAIL should block this SPIKE's sign-off — no: SPIKE's charter is "one shared contract vs hole-by-hole patches," not "every downstream dispatch lane is bug-free"; both FAILs are adjacent/out-of-scope, flagged separately (status_note + telegram) rather than folded into this row or silently dropped.
**why-decision:** The consolidation is not just intact, it is the ACTIVE norm — new pickers keep extending the one library instead of re-forking predicates, and a same-day regression suite proves "fix once, every caller benefits" in practice, not just in the original migration commit.
**why-change:** No change from the 07-21 recommendation; this cycle's addition is independent re-verification plus 2 newly-surfaced, explicitly out-of-scope findings for PO/dev-team triage.

### STEP architect-S31 · architect · 2026-08-05T16:48:15Z
**task-id:** FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST
**what-done:** Confirmed root cause at source (`ssc.ts:85-99` no quarter param, `fetchParseAndStoreBctc.ts:68` `docs[0]!` unconditional). Chose selection option (a) title/publishedAt-derived match + named not-found error over (b) content-probe selector. Reconciled family-B/DPM counter-example to a fully separate acquisition path (`bctcVpsIngestHandler.ts` external multipart push, shape-only validated, never touches `listSscDocuments`) — out of scope. Wrote full design to `docs/architecture-briefs/2026-08-05-fix-bctc-ssc-doc-selection-quarter-blind.md`.
**what-considered:**
- (a) title-parse selection vs (b) checkPeriodContentConsistency-as-selector — chose (a): `SscDocument.title` already carries "quý N năm YYYY" markers (proven by shipped `titleMatchesReportType` + 6 live test fixtures), plus `deriveQuarterFromPublishedAt` already shipped/tested as a 2nd free signal; (b) requires downloading+OCR N candidates before selection (collapses the Step1/Step2 boundary) and its `null`="cannot determine" safe-default (proceed) inverts to unsafe when repurposed as a selector.
- Narrowing `listSscDocuments()`'s own signature to take quarter — rejected: `checkSscReports.ts:defaultListDocs()` depends on getting ALL quarters back for its own new-report-detection loop; selection must be a new layer at the `fetchParseAndStoreBctc.ts` call site only, not inside the shared lister.
- Family-B/DPM: traced to `bctcVpsIngestHandler.ts:182-201` (receive-only endpoint, no in-repo caller) rather than assuming it shares this row's mechanism — mechanistically proven separate (never calls `listSscDocuments`), so out of scope; recommended (not minted) a follow-up row for PO.
**why-decision:** Reused two already-shipped, already-tested signals (title keyword convention, `deriveQuarterFromPublishedAt`) instead of inventing new I/O or repurposing the detector's conservative veto semantics into a selector role they were not designed for.
**why-change:** No change from the row's own framing — this cycle resolved the row's own open DESIGN QUESTION and its own flagged counter-example (DPM/family B), both explicitly deferred to architect.

### STEP architect-S32 · architect · 2026-08-05T17:20:00Z
**task-id:** FIX-MCP-MEMORY-CODE-LEAK
**what-done:** Re-verified the 07-02 recon's `initDatabase()` finding still live at HEAD (call-sites grew 68→117); confirmed cap-bump+rebuild (recon's cheap tracks) already shipped and sawtooth persisted as predicted. Designed `WeakSet<Database>` identity guard (schema.ts:156). Found + wrote up a 2nd, distinct, higher-volume leak mechanism (SSE per-connection `McpServer`+heartbeat, transport.ts, never explicitly closed on the documented "dial-and-drop" gateway pattern) as an explicit fast-follow, not folded in.
**what-considered:**
- Bare module boolean guard (recon's literal suggestion) vs identity-keyed `WeakSet<Database>` — chose WeakSet: grepped 15+ test files that pass a fresh per-test `Database` as `dbArg`; a boolean would silently skip DDL on every 2nd+ test in one `bun test` process (suite-wide false-red). WeakSet also self-GCs, so the guard itself can't become a leak.
- Fold Finding 2 (SSE session leak) into this same fix vs flag separately — flagged separately: different file/mechanism/risk profile, needs its own `/health` sessionCount-correlation confirmation step first (mirrors this row's own standing "instrument before speculative fix" constraint), would blow this row's "single-file, atomic" sizing.
- PM decomposition vs direct-to-dev-mcp-server — direct: single file, ~4 lines, next_agent/owner already dev-mcp-server (PO 07-29), matches this sprint's own precedent for FIX rows this size.
**why-decision:** Cheapest-sufficient-first, per the 07-02 recon's own decision matrix, which this row had never advanced past due to the plan_only/supervised dispatch gap SLS closed today.
**why-change:** No change from the 07-02 recon's own sequencing; extended it with a live re-verification + one new independently-found mechanism it did not attribute a leak to.

### STEP architect-S33 · architect · 2026-08-05T17:52:45Z
**task-id:** FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER
**what-done:** Ran AC1's mandatory measured dry-run (not skipped): `size-lint-justification.sh --check` ~13.3-14.0s + `task-claim-owner-session-lint.sh --check` ~5.5-6.1s + `bun test tool-registry-parity` ~0.1-0.3s, 3 runs each, real `time` output — combined ~19.5-20s, ~4.5x under the 90s commit-mutex TTL. Live side-effect: all 3 runs reproduced the exact 3 CI-red conditions already on GitHub Actions, proving local/CI detection parity would close if wired in. Designed the hook change: add the 3 checks UNCONDITIONALLY (not `CODE_TOUCHING_REGEX`-gated) before the existing `PRE_PUSH_SKIP_TSC` early-exit; leave tsc/rebuild-raw-verify on their existing gate untouched.
**what-considered:**
- Widen `CODE_TOUCHING_REGEX` to include `docs/` (the naive fix) — rejected: would make every doc-only push pay tsc's measured ~94s against the 90s TTL that regex exists to protect, re-opening the exact UC-GCP-P4 bug.
- Run the 3 checks only when `CODE_TOUCHING_REGEX` does NOT match (mutually exclusive with tsc) vs unconditionally on every push — chose unconditional, per the row's own AC2/AC3 steer and because CI already runs all 3 on every push regardless of path; local coverage should match that, not add a 2nd path-based skip condition.
- Leave `PRE_PUSH_SKIP_TSC=1` as a full-hook bypass (its current, mislabeled behavior) vs reorder so it only skips tsc — chose reorder: the var's own name and header doc already claim "skip tsc check"; leaving the new unconditional checks skippable by the same var would silently reopen AC3's "no fail-open on docs-only" concern via a different door.
**why-decision:** AC1 is explicit that this row cannot be closed by assumption — the number itself (not "these are probably cheap") is the design constraint; measuring first, then designing to the measured number, is the only way to honor that gate.
**why-change:** No change from the row's own framing; fence honored explicitly (this row does not touch the 3 symptom files/lines that are separately tracked).

### STEP architect-S34 · architect · 2026-08-05T17:58:00Z
**task-id:** FACTORY-STOCK-split-sandbox
**what-done:** Review-Lane SECONDARY-Drain triage of a 27-day-stale review[] row (dev-stock-price reviewed 2026-07-09). Read commit 0d93b3c95's own message (the review record) + live files; ran `git log 0d93b3c95..HEAD -- apps/stock-price/cmd/sandbox/` (EMPTY — zero drift since review) then independently re-executed the AC live: `go build`/`go vet`/`golangci-lint run` all clean, `CGO_ENABLED=0 go build` clean, built binary run against all 11 real scenario JSONs -> 11/11 PASS, matching the original review exactly. Closed DONE_VERIFIED via `scripts/devteam-close-task-done-verified.jq` -> `orch-apply.sh`.
**what-considered:**
- Request rework — rejected: no defect found; DoD fully met, review not stale (file byte-identical to reviewed state).
- Reassign next_agent (e.g. to qa) for a 2nd verification pass — rejected: I already performed a more rigorous re-verify than a fresh QA pass would (live build+vet+lint+scenario-run against the actual committed code, not chain-trust of the review note).
- Escalate BLOCKED — rejected: nothing blocking; the 27-day staleness was a review-lane drain-throughput gap (now closed by this sweep), not a work-quality defect.
**why-decision:** DONE_VERIFIED sign-off is the only option matching the evidence — commit message + git history + live re-run all corroborate the split is complete, correct, and undrifted; also confirmed (beyond dev-stock-price's original scope) that `cmd/sandbox/` is never baked into the Docker image (Dockerfile builds only `./cmd/server/`), so the backlog-detail.json `rebuild_required:true` flag was a generic audit-batch default, not applicable here — no ops/rebuild gate needed.
**why-change:** No change from plan; this agent lacked gateway MCP tool access this session (Read/Edit/Write/Bash only) so `task_release` for the PRE-CLAIM lock is deferred to the spawning dev-team session per its own fallback instruction.
