# PO Decision Journal — triage tick 2026-08-12T17:00Z

Session: dev-team Step 1 PO triage (tick 2026-08-12T16:37Z). Board write: 1 × `scripts/orch-apply.sh`, Stage 0+1 PASS, `[orch-stamp-updated-at] stamped 13 row(s)`, conservation OK (task_total 749→751, signal_total 91=91, signal_row_identity=clean).

Inputs: 30 `pending_triage_inbox` envelopes, 5 `signal_queue` `to=po` rows in READ, 2 unresolved telegram reports (4775, 4776). Disposition: **2 mints, 11 folds, 5 signal rows closed**.

---

## D-1 — REFUTED the rag-service memory incident; it is a probe false-positive, not a second thread

**task_id:** `FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD`

**Decision:** rag-service A-30 WARN "sustained memory 95.57% (loss of reclamation)" is REFUTED as a live degradation. No mint, no ops action. Folded as the strongest empirical confirmation yet of the A-30 window defect. The dispatch brief's framing — "two separate memory-pressure threads needing triage, not one" — is **wrong on the rag-service half**.

**What was considered:**
1. Accept telegram 4776's claim that rag-service is "ALSO still WARN ~95.5% unresolved since 13:30Z" and mint/escalate a second memory incident. Rejected — never re-measured.
2. Fold to the rag-service allocator row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-...`) as a real degradation. Rejected — the reading does not survive re-measurement.
3. Re-measure the live plane before dispositioning. Chosen.

**Evidence (measured directly, 17:00Z):**
- `docker stats --no-stream` → rag-service **284.3MiB / 1GiB = 27.76%**. Claimed value at 15:35Z was 95.57%.
- `docker inspect` → `StartedAt=2026-08-12T14:00:55Z`, `RestartCount=2`, `OOMKilled=false`. **The last start PRECEDES the 15:35Z sample**, so both readings fall inside one container lifetime.

**Why it decides the question:** the drop is therefore **in-process reclamation, not a restart artifact** — ~680MiB freed 85 minutes after A-30 declared "loss of reclamation". 67.8pp over a window 17× longer than the ≤5-min `PROBES=12 × INTERVAL=25s` deep probe. That is a larger, cleaner datum than the row's own founding evidence (94.69→82.00% at 19min). This is exactly the check `feedback_a30_discriminator_crash_cliff_misscored_as_reclamation_dip` demands, run in the honest direction: I tested whether the *alarm* was wrong, not only whether the service was.

**Why change:** the brief pre-classified this as a genuine second incident. Two agents (system-auditor, cowork-team) propagated "still 95.5%" as prose without re-measuring; PO is the last gate before that becomes a task.

---

## D-2 — pdf-extractor WARN→CRITICAL is a threshold artifact, not new physics — fold, do not re-open

**task_id:** `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM`

**Decision:** Fold all four pdf-extractor A-30 signals + telegram 4775/4776 onto the existing REVIEW row. **No severity-aware re-open**, despite the brief explicitly inviting one.

**What was considered:**
1. Mint a new CRITICAL row per the brief's "severity-aware re-open even though a WARN-level entry was already dispositioned". Rejected.
2. Fold with corroboration onto the open REVIEW row. Chosen.

**Evidence:** live **2.375GiB / 2.5GiB = 95.00%** at 17:00Z; Up 40h, `StartedAt=2026-08-11T01:19:14Z`, `RestartCount=1`, `OOMKilled=false`, healthy. Series across the drain: 95.36% (15:35Z) → 95.32% CRITICAL (16:05Z) → 94.52% (16:35Z) → 95.00% (17:00Z).

**Why:** that is a **flat oscillating band**, and 93% is the crossing threshold — a series wandering by ±0.5pp around 95% will label itself WARN or CRITICAL essentially at random. Neither the 16:05Z "escalation" nor the 16:30Z cowork note calling 94.52% "receding from the CRITICAL peak" is a real state change. The service has neither restarted nor been OOM-killed while sitting at ~95%, so this is steady-state occupancy — genuine, already owned, third consecutive PO tick folding here. Minting a second row would duplicate an open REVIEW item and split the evidence.

**Also recorded:** `sys-20260812T163525-0051` uses dedup_key `vn-market-intelligence-mcp-pdf-extractor-1:A-30` while `sys-20260812T160538-57ef` uses `pdf-extractor:A-30` — **dedup-key spelling drift on one physical service**, flagged on the row so it is never counted as two threads.

---

## D-3 — Minted the only ci_red failing file with zero dedup hit

**task_id:** `FIX-CI-BUNTEST-1108-AGENT-WORK-LOG-STORE`

**Decision:** Mint. The other 2 files + the lint job all dedup-hit existing rows and were folded with the new observed SHAs.

**What was considered:** (only path) the ci_red ANTI-AMNESTY BACKSTOP — CI-plane baseline is 0 fail under `scripts/ci-per-file-isolation.sh`, so "pre-existing" is never a valid disposition without a matched FILE-scoped row. Dedup across all 5 non-terminal lanes **and** done/done_verified returned 0 hits.

**Method note (process defect I caught in myself):** my first `gh run view --log-failed | grep FAILEDFILE` returned empty for all 3 runs and I nearly dispositioned on that. The cause was **my own broken command** — I had prefixed it with `timeout`, which does not exist on macOS, so the shell errored and the pipeline emitted nothing with `rc=0`. Re-run without it: the FAILEDFILE blocks were there all along. An empty grep is not evidence of an empty result set; a silent-failure `rc=0` is exactly the shape `feedback_silent_swallow_serial_bugs` warns about.

**Intermittency, recorded not hidden:** the file failed ONLY in run 31613681006 (0b3d494d, 6 fail) and passed in the bracketing runs b701e913 (14:53Z) and f954d591 (16:21Z). Deflake shape per the `DEFLAKE-VNSTOCK-3STATEMENT` precedent — scoped as deflake-or-fix, minted anyway per the backstop.

---

## D-4 — Minted a corrupt-zero that is silently disabling an escalation gate

**task_id:** `FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG`

**Decision:** Mint (P2/M, `next_agent=ba`). This is the one `bctc_signal` of four that is a data-pipeline bug rather than an investment-thesis note.

**What was considered:**
1. Skip all 4 `bctc_signal`s per the routing table's "rarely actionable; default skip with notebook log". Rejected — the table's own escape clause is "or data-pipeline bug (FIX task)", and this qualifies.
2. Fold into `FIX-BCTC-BANK-SUMMARY-MAPPING`. Rejected — that row owns B02-TCTD **bank** rows; HPG is non-bank, and FPT/VCB both serve a populated operating profit this same tick, so the failure is scoped to the non-bank path.
3. Mint. Chosen.

**Evidence — RAW-verified through the runtime, not read off the signal prose:** `call_tool(vn-market, get_bctc_full, {code:"HPG"})` returns `Operating Profit: 0 tỷ`, `EBITDA: 0`, `structured_data.operating_cf: 0`, while the same payload carries `gross_profit=8.365.068,6`, `profit_before_tax=10.762.183,8`, `net_profit=9.055.918,2`. The server's own validator already says `passed_with_warnings — Net profit exceeds gross profit ... possible non-operating income`. Published 2026-06-07 — served ~9 weeks. Dedup on `/operating.?profit|LN thuần|operatingProfit/i` across 5 lanes → 0 hits, after 14+ reported cycles.

**Why it matters beyond a wrong number:** ESC-4 self-reports `result="discarded", reason="operating_profit=0 corrupt — non_operating_share formula undefined on corrupt denominator basis"`. **A corrupt zero is switching an escalation gate OFF instead of failing loud** — and ESC-4 exists precisely to catch net>gross unseparated non-operating income, which is the condition actually present. AC-4 therefore requires a plausibility check on the inverse case (`feedback_nonzero_values_need_plausibility_check`), AC-5 requires a second non-bank ticker so the fix is not validated on one file, and the row records the reflow dependency (`feedback_derived_column_fix_needs_reflow`).

---

## D-5 — Rendered the product decision code-janitor asked for, instead of parking it

**task_id:** `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE`

**Decision:** **RETIRE `team-tool-recheck` permanently.** Do not rebuild it as a local cron. No new row — the signal *is* the hardcoded string this row exists to delete, so its recurrence is evidence for the row, not new information.

**What was considered:**
1. Replace with a local cron per the no-RemoteTrigger migration rule. Rejected.
2. Retire permanently and let the 30d sweep drain the directory. Chosen.
3. Defer to a user gate. Rejected — PO has full autonomy; deferring is what left this open since 06-23.

**Why:** its only writer was cloud RemoteTrigger `trig_019Q8D5xttjZn6iytx2Ld9dW`, killed by the 2026-06-22 standing directive; nothing has read `docs/agent-memory/health/team-tool-recheck-*.md` in the 7 weeks since, so there is no live consumer to serve; and tool-health is already covered by system-auditor Tier-1/Tier-2, so a replacement cron would duplicate an existing detector rather than close a gap. **Implementation consequence written onto the row:** delete the hardcoded escalation branch outright rather than add a liveness check to it.

---

## D-6 — Held the line on minting for four already-owned classes

**task_id:** ambient (no single row)

**Decision:** 0 mints across sweep-guard, notebook-immutability, notebook-overage, and routing-gap — all folded.

- **sweep-guard ×4, all `escalated=true`/`outcome=blocked`** → `FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK` (QA). Mechanism-confirmed off the `[sweep-guard] BARE` payload tag, **not** off `git show --stat` (which the table names an INVALID disposition). Escalation argument recorded: `prior_warns` hit **60** on one actor and **two distinct actors were hard-blocked inside a 4-minute window** — agents are following `commit-convention.md` and being rejected for it in a live loop.
- **`[notebook-immutability-guard]` on system-auditor.md `## c54`** → `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` (REVIEW), volume tracking only per the do-not-mint-per-occurrence rule. Explicitly did **not** call it benign from a clean after-the-fact worktree: the index state at fire time is unreconstructable, so absence of a reproducible diff is INCONCLUSIVE.
- **7 × `notebook_single_section_overage_breach` + 1 × `context_bloat_breach`** → `CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` (READY). New datum: code-janitor.md grew **230L/14026B → 247L/15307B in 96 seconds while the prune hook was actively firing**. Board-hygiene flag raised (not actioned): this row and `CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION` overlap on 2 of 3 files and can both be closed independently for the same work.
- **`[notebook-uuid-provenance-guard]` on developer.md** → **already remediated**; `git show HEAD:` finds no UUID, fixed in `725658b2e`. Verified against the persistence layer rather than assumed from the signal's age.
- **2 unrouted signal types** (`notebook_single_section_overage_breach`, and the `[notebook-uuid-provenance-guard]` payload class — a 5th `bug-escalation` sub-class the table does not name) → conceptually owned by `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE` (READY), whose fix is route-by-`to` fallback rather than per-type whack-a-mole. Logged, not minted as a 3rd routing row.

---

## D-7 — Two BCTC threads folded to their real owners

**task_id:** `REFLOW-FPT-Q1-2026`, `BCTC-HIST-VPS-BACKFILL`

- **FPT:** signal `esc5.report_id = e8ea3df5-3f32-413d-a3eb-c71634c0438d` is byte-identical to `REFLOW-FPT-Q1-2026`'s own target (15 units, 7 low-confidence — matches the row's "units 8-14 of 15"). Fold. **Timing flag raised:** the ESC-5 guard has been re-arming on a 24h TTL since 2026-08-11T15:10Z and is now ~26h, i.e. it has already rolled over at least once — unattended it will re-arm indefinitely and the deep-dive will never dispatch.
- **DXG:** 18th+ consecutive `Chưa có dữ liệu BCTC`; the signal names `BCTC-HIST-VPS-BACKFILL` as root_task and that attribution is correct. Fold. Recorded consequence: with zero company-specific BCTC, every DXG valuation figure served (PE 66,5x, ROE 1,9%) is peer-median-derived and all five ESC gates read "unevaluable" — the gap silently reduces DXG to a sector shell while still presenting as per-ticker analysis.
