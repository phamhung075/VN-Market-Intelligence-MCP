### STEP po-R12 · po · 2026-08-24T08:50Z
**task-id:** UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT
**what-done:** Ruled on ops' AC-4/AC-5/AC-6 hand-back. Withdrew ops' AC-5 conclusion (wrong container), corrected AC-4's arithmetic (88.37%, not 86.3%), rescinded my own 07:22Z P0 expedite back to P1, replaced the AC-4 bar on the sibling row with a 4-part anon/oom_kill/reclaim-health/idle-drift bar, and replaced the aborted burst test with a passive longitudinal sampler (AC-7/AC-8). 2 orch-apply writes, 4 rows mutated, 0 rows minted.
**what-considered:**
- Accepting ops' "malloc_trim cannot solve this" and minting a re-diagnosis row — REJECTED on evidence: `oom_memcg=/docker/6ec27ac85c8e` is not among the 13 containers in `docker ps -a --no-trunc`; it is the retired pdf-extractor created 2026-08-15T09:47:36Z (QA read it "Up 8 days" at 08-23T13:50Z). The live container started 77 min later.
- Accepting the router's sharper framing ("PRE-fix evidence, efficacy UNDETERMINED") verbatim — REJECTED as still imprecise in a load-bearing way. The retired container DID carry AC-3's malloc_trim (c3fd44766); that trim was wired only into `pek_run_helper`. The kills are POST-AC-3 and PRE-6f3577b9f, whose own commit body cites these exact two kills (14:27:11Z→14:58:52Z = 31m41s, matching its "~31 min apart") as its motivation. So they prove AC-3 was insufficient ALONE, and they are evidence FOR a shipped fix — not against the running one.
- Calling efficacy UNDETERMINED because the burst test aborted — REJECTED: the live container has served 563 POST /extract + 58 /pek-extract in 17.5h, all HTTP 200, `memory.events oom_kill=0`, zero dmesg OOM lines across a buffer that fully covers its life. The production workload already ran the test 621 times.
- Holding P0 on `memory.peak == memory.max` + 88.4% VmHWM alone (the router explicitly left this open) — REJECTED: `pgsteal/pgscan = 98.6%`, `workingset_refault_anon = 0`, and `memory.events.max` frozen across 8 samples over 2m21s. That is successful reclaim, not an impending kill. Held at P1, not lower, on peak==max plus the idle anon drift.
- Recording findings on the UNBLOCK row as the router's constraint mandated, and leaving the sibling untouched — REJECTED after measuring both with the guard's own algorithm (`orch-row-prose-ceiling-check.mjs` STRUCTURAL_FIELDS): the sibling is 8,006 prose bytes (3,994 UNDER ceiling, writable); the UNBLOCK row was the tight one at 10,424 with 1,576 headroom. The "33,898B / Stage 2.5 hard-rejects" premise is stale. Put the bar ruling on the sibling where it belongs and consolidated 3 superseded fields on the UNBLOCK row to make budget.
- Keeping VmHWM as AC-4's instrument with a raised threshold — REJECTED: VmHWM is a monotone lifetime high-water mark of PID 1 only. It can never register a PASS after the first burst (unfalsifiable in the pass direction), and both kernel victims were forked children whose RSS is not in PID 1's VmHWM at all.
- Mandating a container restart to re-arm the peak instruments — REJECTED: destroys 17.5h of cgroup evidence. Noted instead that `memory.peak` is write-resettable on kernel >= 6.12 (host verified 6.12.76-linuxkit), usable only after the pre-reset values are recorded.
**why-decision:** Ops got the AC-4 verdict right and the AC-5 inference backwards, and both errors trace to reading a metric off the wrong plane — decimal-kB for VmHWM, and a retired cgroup for dmesg. The durable fix is not another burst test; it is retiring the instrument that cannot answer the question (VmHWM/% of cap) in favour of the one the kernel actually charges (anon) plus the counter that survives child kills (memory.events.oom_kill).
**why-change:** Widened past the router's 3 questions to a 4th finding it did not ask about — `anon` rose 1,447,403,520 → 1,743,306,752 B (+282 MiB) between 03:56Z and 08:33Z while `docker logs --since 04:00Z` shows zero non-/health requests. Both malloc_trim fixes are wired to request finally-blocks, so with no requests neither can have run. That became bar B4 and the priority question of AC-7/AC-8. Two cross-session samples, not one series — flagged as such, not asserted.

### STEP po-R13 · po · 2026-08-24T08:52Z
**task-id:** FIX-MCP-OOMKILL-DETECT-VIA-CGROUP-MEMORY-EVENTS
**what-done:** P2 → P1. Also corrected `FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-1-PROBE-SCRIPT` next_agent developer → qa (review-lane row pointing at its own implementer, so it had no picker).
**what-considered:**
- Leaving it P2 on "no live outage" — REJECTED: priority here is about instrument uniqueness, not severity. `memory.events.oom_kill` is the only fleet-reachable counter that sees a memcg kill of a CHILD process (docker `.State.OOMKilled` reads false in exactly that case, now proven twice), AND the only DURABLE one — dmesg is a rolling ring buffer, and FU-RAG-DEPLOY-MEMORY documents earlier OOM lines that already aged out unprovable.
- Escalating to P0 — REJECTED: `oom_kill=0` on every current container; nothing is being lost right now.
**why-decision:** This tick alone burned an ops cycle, a router re-verification and a PO ruling on an OOM attribution that one `memory.events.oom_kill` read would have settled in a line. That is the cost case, and it recurs on every future OOM.
**why-change:** Flagged a scope question rather than silently rewriting it — the row is zoned `apps/mcp-server/` but the value is fleet-wide. Asked dev-mcp-server to say so on return if the design is mcp-server-local, and PO mints the fleet-probe half rather than pre-empting the implementer's read.
