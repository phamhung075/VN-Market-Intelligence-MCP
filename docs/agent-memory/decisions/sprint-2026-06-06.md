# Decision Journal — Sprint 2026-06-06

**Sprint goal:** dev-team triage ticks (no single named sprint)
**Started:** 2026-06-06T12:37:13Z

## STEP — rtr-refine-idem-test-lock-isolation-202606061226 (triage tick 123211Z)

**Decision:** Emit ONE FIX → dev-mcp-server (zone apps/mcp-server/). Channel reports surfaced no second actionable item.

**What considered:**
- Path A (chosen): route the test-isolation row as a FIX to dev-mcp-server. Root cause raw-verified — `refineOneReport` injects `deps.db` for report data but `claimTask`/`releaseTask` (coordinationStore.ts) bind to a module-level `_coordDb` singleton, NOT the per-test in-memory db. The test's `beforeEach` never calls the existing `_injectCoordinationDb(db)` seam (line 700) nor resets `_coordDb`, so a `refine-orchestrator` lock from scenario/run N survives into N+1 → "[bctcRefine] skip — task already claimed". 4 cases (A/B/C + no-accumulation) all fail because each re-runs the SAME reportId-derived taskId without an isolated coordination store.
- Path B (rejected): widen to refactor refineOneReport to take coordination store via deps injection. Deferred — the `_injectCoordinationDb` seam already exists; test-side injection + per-test reset is the minimal definitif fix. Job-signature refactor is a larger optional hardening, not needed to make tests isolated/green. Left as an open note for the dev to choose (seam reset vs deps param) — both close the class.

**Why change:** no change from plan — single FIX, single zone.

**Channel audit dispositions (cross-checked, NOT batched):**
- BUG/WORK 3052 (09:05Z) "[refine_bctc_md] Phase 0 blocked: get_bctc_pending_refine missing text_status/confirm_status/windows[]" → DEPLOY-GAP, not a live defect. Source already returns all 3 fields (commit 172999f0 FIX-REFINE-PENDING-SCHEMA, repo-confirmed, context-flagged RESOLVED+DEPLOYED). 09:05Z report predates today's tick; the in-flight ops scoped rebuild (d4d2e453+04123334) closes the stale-container gap. No new task per decision-matrix (git=yes/container=no → deploy-gap, already being closed). Do NOT re-open 172999f0.
- 3053 (11:17Z) outage RESTORED — router-handled (e1de9e1b), ops-footgun FORBIDDEN (bd41a6b3) + auditor confab follow-ups already queued. Informational, skip.

**Verification basis:** raw-read of AR-refined-units-idempotency.test.ts (no _injectCoordinationDb call), bctcRefineJob.ts L306/314/512 (claimTask has no db param), coordinationStore.ts L334 (claimTask(input: ClaimInput)) + L700 (_injectCoordinationDb seam present). Router-verify-raw, not badge.

### STEP po-S1 · po · 2026-06-06T18:32:39Z
**task-id:** BA-ORCH-TASK-CANON
**what-done:** Kicked off sprint ORCH-TASK-CANON (canonical task schema {id,title,owner,status,zone,created_at} + closed status enum + decision visibility); wrote .sprint_goal entry + canonical BA task; claimed umbrella lock.
**what-considered:**
- Accept router F1-F4+QA shape with F1(contract+66-row migration)||F4(journal format+skill .id-vs-.sprint_id resolver bug) -> F2(serving coalesce) -> F3(frontend filter) -> QA — chosen
- F2-only serving coalesce without F1 creation contract — rejected: leaves 9-shape proliferation alive (symptom, not class)
- Keep freeform DONE-* statuses as enum members — rejected: 16 strings already; closed enum + status_note kills proliferation while keeping nuance
- Skip migration, legacy-tolerate forever — rejected: derived-column-reflow lesson — coalesce code without corpus migration leaves the join starved for existing 66 done rows
**why-decision:** Operator demand is creation-time schema + visible decisions; only fixing CREATION (F1) + SERVING (F2) + RENDER (F3) + JOURNAL FORMAT (F4) together closes all 3 diagnosed layers definitively.
**why-change:** no change from router proposal except F4 widened with the skill resolver bug (.sprint_goal.entries[0].id never matches; field is sprint_id) found during this kickoff.
