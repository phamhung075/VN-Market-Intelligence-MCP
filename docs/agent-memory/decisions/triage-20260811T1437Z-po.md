# PO Decision Journal — triage tick 2026-08-11T14:37Z

**task_id:** `po-triage-20260811T1437Z` · dev-team Step 1 triage
**Session:** `165f4245-6173-4054-87fd-c55bb626265f`
**Board:** task_total 770 → 772 (2 mints, 2 folds, 4 dedup-notes, 3 signal dispositions, 1 sweep stamp). 4 `orch-apply.sh` pipes, all PASS, conservation clean.

---

## STEP po-1 — Pre-checks

| Check | Result |
|---|---|
| Signal dashboard (Pipeline B) | 0 NEW; **3 READ with `disposition:null`** — not empty, and the prior tick logged "inbox empty". Dispositioned all 3. |
| `.head` blocked-waiting-for-PO | `.head.status=idle`, `active_task_id=null` |
| WF-2 supervised-hold (`supervised-goahead.md`) | `should_hold` not evaluable (head idle) → no-op |
| Manual-dispatch sweep | candidates computed; top = `CLEAN-NOTEBOOK-AC2A-CYCLE-BOUNDARY-DEFINITION` (DRS-STRANDED-OFF-ALLOWLIST, `next_agent=agent-father`, `reflag=false`, priority high). Stamped + folded into BATCH. |
| Step 0-TNB | `docs/handoffs/tnb-audit-latest.md` unchanged since the 14:36:06Z ACK this same session → no new findings, no re-ACK |

**what-considered:** only path — the two MANDATORY pre-check sub-flows plus the dashboard read.
**why-change:** no change from plan.

---

## STEP po-2 — `pendingSignals[]` (Pipeline A, 3 envelopes)

### po-2a · `architecture_brief` from agents-architect → **MINTED** `FIX-CRON-CODEJANITOR-NO-PRESPAWN-GATE-BOOTS-FULL-SESSION-4X-DAILY` (P2)

**what-considered:**
1. Skip — envelope carries `pipeline: hold-for-user-confirmation`, so propagate the hold upward.
2. UNBLOCK `route_to: agent-father` — the `brief_complete` table row's default for `docs/agents/*` targets.
3. Ratify at source and mint a FIX row routed to `developer`.

**why-change (chose 3):** Two independent reasons rejected 1 and 2.

*Against 1:* CLAUDE.md is explicit — "Never ask user — spawn `po` for decisions, continue" — and `feedback_po_deploy_rebuild_full_autonomy_no_user_gate` is exactly this class. PO **is** the confirmation authority. Propagating the hold would have been the 3rd tick this brief sat undispatched.

*Against 2:* the brief's own §6 anticipates and rejects it: the change is **mixed-zone** (`scripts/` + `.claude/skills/` + `.claude/commands/`), which is outside agent-father-solo. Routing to agent-father would also have put the row **off the DRS allowlist** and structurally stranded it.

**RATIFIED AT SOURCE, not on the brief's prose** (`supervised-goahead.md` Step 2 discipline). Three load-bearing claims re-derived from the files themselves:

- `register-job-code-janitor.md:9` prompt is verbatim `"Launch subagent (subagent_type=code-janitor). Read and execute docs/agents/code-janitor/flow/main.md\nMCP: ..."` — no shell, no gate, unconditional spawn. **Gap confirmed.**
- `docs/agents/code-janitor/flow/main.md:19` heading is verbatim `## Pre-Check (gates ONLY the Decision-Tree DRY scan below — CADRAT-3)`, and `:26` states `The 3 every-scan sweeps ... stay UNCONDITIONAL on this gate`. **Post-boot + DRY-only confirmed.**
- `register-job-market-db-journal-guard.md` exists — the exemplar is real.

**Scope fence written onto the row:** the brief assessed and *rejected* any LLM/local-model pre-gate (3 documented live instances of safe-looking gates silently disabling their own mechanism). Rows 1–8 already gated, rows 9–10 need no change. Implement **row 11 only**.

**P2, deliberately not higher:** host-load/token efficiency, not correctness. Nothing is broken by the missing gate.

### po-2b/2c · 2× `bug-escalation` from commit-sweep-guard → **FOLD** (occurrence_count 26 → 28)

`prior_warns=29` (`system-auditor.md`) and `=30` (`auditor-dedup-ledger.json`), both `escalated=true`, both actor `165f4245`. Threshold 3.

**what-considered:** (1) mint a repeat-offender row per `triage-signals.md`'s `escalated=true` branch; (2) fold onto `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES`.

**why-change (chose 2):** the family dedup-check names that row as the open owner, and its `po_correction_20260811T1340Z` key already refuted the "no actuator" premise at source. Minting again would re-open a question answered yesterday.

**What this tick actually adds — the last open question is now closed, empirically.** The 13:40Z correction proved the block only in a **throwaway repo**. This tick observed it in the **live repo**: a bare `git commit -m` was ESCALATED REJECTed, and the same commit retried with an explicit trailing pathspec succeeded. Both files landed correctly seconds later — `01cfb57ce`, `5595bcfc9`. **Zero data loss from either fire.**

**That is what makes item (b) measurable rather than arguable.** Strikes 29 and 30 are *blocked attempts that then succeeded*, and their payloads are byte-identical to what a genuine sweep would emit. A triager holding these two envelopes has **no field** distinguishing "mechanism worked" from "mechanism failed". That is the entire reason this row accumulated 28 occurrences of a working guard reporting its own success.

---

## STEP po-3 — `ci_red` (CI-RED-1765317d) → **3/3 file-scoped dedup hits, 0 mints**

Ran the MANDATORY pre-dedup failing-file read **before** evaluating dedup. Two distinct read procedures, because one signal named two jobs:

- `bun test` → `gh run view 31502853310 --log-failed | grep FAILEDFILE` → `15093 pass / 40 skip / 3 fail`, `=== FAILED FILES (2) ===`: `src/__tests__/ALLZERO-OHLCV-FETCH.test.ts`, `src/__tests__/FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts`
- `size-lint` → emits **no** FAILEDFILE block by construction; read verbatim from the failing step: `apps/mcp-server/src/interface/mcp/transport.ts — baseline-tolerance-exceeded (baseline=126L actual=265L upper=138L)`

All three matched an existing `dedup_key` exactly. Appended the observed `head_sha` to each; no mint.

**One finding came out of the dedup rather than the failure:** the size-lint row is titled `...-237L`. Live actual is **265L** — **+28L since mint**, now 2.1× its baseline and 1.9× the tolerance ceiling. The offender is still growing, which is itself evidence nothing gates edits to that file. Wrote "re-measure, do not trust the title" onto the row.

**Not dispositioned as pre-existing.** Per the ANTI-AMNESTY BACKSTOP the CI plane's known-green baseline is 0 fail, so a `ci_red` is always actionable; "pre-existing" is only valid when it names specific already-open file-scoped rows — which it does here, all three.

---

## STEP po-4 — Pipeline B READ rows + **the guard that caught a real gap**

Ran `guard_signal_type_coverage` (byte-identical, from `triage-signals.md` § Regression verifier) against the live board: **FAIL, rc=1**, unrouted `to=po` types `["cron_fire_gap","system_issue"]`.

**why-change:** FOLD onto `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` rather than mint — same class, same two files, same guard. **But the fold rewrites the fix scope**, and that is the substantive call:

That row was minted 2026-08-06 naming `agent_flow_defect`, `db_anomaly_illiquid_ohlcv`, `cowork-fire`. **None of those three is in today's failing set.** `.signal_queue` is a rolling ~2h TTL window (`reference_agent_signals_is_a_rolling_2h_ttl_window_not_an_accumulating_log`), so offending types churn out and new ones churn in. **Shipping the row exactly as written would have left the guard RED anyway.** A one-time table patch cannot converge against a rotating namespace — the fix has to become a mechanism (run the guard in-flow; make the `any unknown type` catch-all produce a real disposition instead of log-and-skip).

Two further specifics recorded on the row:

- **`system_issue` (underscore) is a documented-wrong assumption, not drift.** `triage-signals.md` § Cold-archive cross-check reasons that the underscore form "predates the live `system-issue` hyphen form" and is "a historical artifact, not a live routing gap", and declines to add a row on that basis. **Falsified:** 6 live underscore rows, all `system-auditor`, all `2026-08-11T03:00Z`. Both spellings are live simultaneously. Correct the paragraph or the next author re-derives the same wrong conclusion.
- **`architecture_brief` is unrouted on Pipeline A and the guard cannot see it** — the guard only scans `.signal_queue`. Same producer and semantics as `brief_complete`, different token, so it hits the catch-all every time. (This is po-2a's own envelope.)

Three READ rows dispositioned → `triaged`: `cron_fire_gap` (bctcReparseJob LATE — content folds onto 3 existing owners incl. the known Layer-A false-MISSED classifier), `data_stale` (pipeline-health **endpoint** unreachable — the *tracker* is down, not a tracked feed; WARN single-fire → re-verify next tick before minting), `microservice_degraded` (rag-service 98.11% → folds onto the allocator root-cause row + the A-30 detector-window row).

---

## STEP po-5 — **MINTED** `FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK` (P1)

This is the tick's main find, and it came from asking *why* the strike counter reaches 30 at all.

**what-considered:** (1) fold into the sweep-guard row as more volume; (2) mint per-agent rows for the offending flow docs; (3) mint one row at the policy-SSOT level.

**why-change (chose 3):** CLAUDE.md — "Fix root cause definitif not recurrent symtom". Option 1 buries a supply-side defect inside a signal-noise row. Option 2 is what `FIX-AUDITOR-COMMIT-NONEXPLICIT-PATHSPEC` already tried; it is **CANCELLED** in archive. All three flow docs cite one policy file by name, so there is exactly one place to fix.

**Root cause, read at source — `docs/policies/commit-convention.md` contradicts itself:**

- `:27-35` canonical Shell Pattern — **correct**, ends `)" -- <explicit paths>`
- `:38` — `MANDATORY RULE — carried forward verbatim, non-negotiable: git commit -m (index-only) ONLY.` "index-only" + "ONLY" reads as **forbidding** the pathspec form
- `:39` — puts the explicitness requirement on **`git add`**, not on `git commit`. That is precisely the `git add` + bare `git commit` TOCTOU shape of `feedback_git_add_then_bare_commit_is_toctou_race_use_pathspec_commit`
- `:106-108` § Notebook Commits — the worked template is `git add <path>` then **bare** `git commit -m "..."`, no `--`. This is the **most-executed commit template in the fleet**: every agent, every cycle

**Why it is now breaking rather than untidy:** `scripts/git-hooks/pre-commit:850-877` sets `escalate_effective=reject` on `escalated=true` and `exit 1`s **independent of `GIT_SWEEP_GUARD_MODE`**. The hook's own message at `:875` says the actor is `$CLAUDE_CODE_SESSION_ID`, *"a per-COORDINATION-SESSION id shared by every agent spawned inside it — this budget is POOLED across the whole session"*. Threshold 3; this session is at 30. **Every agent spawned here is already hard-blocked on the exact form its own policy SSOT calls non-negotiable.**

Inheritors: `execute-tier.md:94` (states it as an **invariant** for all developer agents), `developer/flow/main.md:102`, `dev-frontend/flow/main.md:99`. None mentions the escalated-reject or the pathspec retry. Both dev files are also internally inconsistent — their *own* notebook-commit lines (`:147`, `:185`) **do** carry `-- <path>`.

**Failure mode if unfixed** — this is why it is P1 and not cosmetic: an agent hits `exit 1` at its commit step with **no documented recovery**. The documented outcomes of that are silent commit skip (`feedback_auditor_notebook_commit_step_silent_noop_c49`, `feedback_auditor_c11_cycle_completed_but_self_commit_skipped`) and self-reported-complete-but-unpersisted work (`feedback_auditor_tier2_self_report_no_persisted_writes`). Real work loss, not a wasted round-trip.

**Dispatch-stranding warning written onto the row:** `next_agent=agent-father` is **off** the DRS allowlist `[architect,ba,pm,po,agents-architect]`, so no automated lane reaches it. Folded into this tick's BATCH for exactly that reason. Still BACKLOG next tick = dispatch starvation, not a spec gap.

---

## STEP po-6 — Not minted (recorded so no one re-derives it)

- **20 `read_telegram_reports(status="new")`** — all dated 2026-08-09, two days stale. 13 BCTC period-mismatch quarantines (the known byte-identical daily zero-yield loop, owned by `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`), 3 `orch-cold-evict.sh` failures (self-healed — cold-evicts have landed since), 2 sla-monitor news-stale, 1 rag A-30 (folded above), 1 ops-diagnostics 23/90 cron (already upgraded to SUPPORTED against the Layer-A weekend-FP row last tick). **Zero new content.**
- **Why they keep resurfacing is itself already tracked:** `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` surfaced in this tick's own manual-dispatch candidate list. The queue is not growing because incidents are unaddressed; it is growing because ACK doesn't clear status.
