# PO Dispatch — 2026-08-06T19:03Z — cadence-reanalysis-v2 user greenlight (9 items)

task_id: `CLEAN-CRON-STANDALONE-DOCS-SUPERSEDED-BY-COWORK`
Inputs: router dispatch (session `085954f2-75fe-44ef-8fbe-c550db2da453`) carrying an explicit **user greenlight** for `docs/architecture-briefs/2026-08-06-cadence-reanalysis-v2.md` §5 — 5 DST fixes + 4 deprecations, "nothing more, nothing less" · `.task_board` (backlog 361 / ready 62 / review 248) · live `.claude/commands/crons/*.md` · `git log`/`git show` on the target files.

---

## Headline: 5 of the 9 greenlit items were already shipped before the greenlight arrived

**what-considered:** mint 9 fresh rows exactly as the dispatch enumerated them, vs. check prior art first.
**why-change:** prior art check first — `feedback_file_prior_art_check_before_minting_row` and `feedback_bounded1_spawns_health_recheck_stale_duplicate_fix_rows` both name this exact failure. It paid: **items 1-5 are already on `main`.**

The brief's §5 table states the *current* Job A expression is `15,45 2-9 * * 1-5`. The live file reads `15,45 4-11 * * 1-5` with a full CEST/CET dual-expression block. The brief is stale relative to the repo — it was authored before its own recommendations were implemented.

Verified at the persistence layer, not the worktree (`feedback_empty_read_is_not_evidence`, and a clean worktree alone proves nothing about HEAD):
- `git show HEAD:.claude/commands/crons/<f>` for all 3 files → all 5 literals present in **committed** content.
- `git merge-base --is-ancestor 36e109170 HEAD` → true. Follow-on `dd7a036b6` likewise.
- `git status --porcelain .claude/commands/crons/` → empty.

All 5 live literals re-read against the brief's exact proposed expressions — **5/5 exact match**:

| Item | Brief proposes | Live (committed) |
|---|---|---|
| db-integrity Job A | CEST `15,45 4-11 * * 1-5` / CET `15,45 3-10 * * 1-5` | match |
| db-integrity Job B | CEST `0 0 * * *` / CET `0 23 * * *` | match |
| system-auditor Tier-3 | CEST `0 4 * * *` / CET `0 3 * * *` | match |
| orch-sentinel FULL | CEST `18 5 * * 0` / CET `18 4 * * 0` | match |
| orch-sentinel LITE | CEST `48 3 * * *` / CET `48 2 * * *` | match |

Owning row `FIX-CRON-DST-LOCAL-EVAL-MOMENT-ANCHORED-EXPRESSIONS` already exists in `review[]` → `next_agent: qa`, minted by PO itself at 18:21Z under the same brief. **Zero new rows minted for items 1-5.** Annotated the row with the post-hoc user ratification only — no lane/status change, so QA's in-flight verification is not disturbed. The HIGH-severity Job A settlement-window regression the dispatch asked to prioritise is *already fixed*; what remains is QA verification, which is already correctly queued.

## The greenlight *reverses* a PO ruling that is already on the board

`CLEAN-CRON-STANDALONE-DOCS-SUPERSEDED-BY-COWORK` existed in `backlog[]` scoped to **6 files + retiring market-watcher's `mode=market`/`mode=prepost` code**, under `po_product_ruling_20260806T1821` (RETIRE, don't restore). The user greenlight scopes implementation to **4 files** and re-opens market-watcher/news-scout as an explicit HOLD: "Leave both files exactly as-is."

**what-considered:** (a) leave the row at 6 and trust the implementer to respect a prose fence; (b) narrow the row itself.
**why-change:** (b). A fence that lives only in a dispatch prompt does not survive to the implementer — `feedback_dispatch_prompt_inherits_stale_fence_prose_verify_live_config`. The row's own `files[]` and `ac[]` are what the implementer actually reads, so the fence has to be *in them*. Dropped AC-3/AC-4 (the market-watcher code retirement), cut `files[]` 7→4, added AC-4 as an explicit scope fence and a `qa_gate_note` making an untouched `cron-market-watcher.md`/`cron-news-scout.md`/`market-watcher/flow/main.md` a checkable QA predicate. User scope beats PO autonomous ruling.

**The product question is not dropped, it is parked.** Split into `HOLD-CRON-MARKETWATCHER-NEWSSCOUT-MARKETHOURS-MODES-PRODUCT-DECISION`, `status: BLOCKED`, `plan_only: true`, `next_agent: po`, with a `blocked_by` that explicitly forbids auto-promotion and backlog-sweep pickup. The superseded ruling's four-point reasoning (host memory pressure, 2mo zero restoration demand, correct-mechanism-is-a-new-cowork-slot) is preserved **verbatim** on it as a standing recommendation, flagged as *not* authority to act. Deleting that analysis to honour the HOLD would have thrown away the only evidence anyone has on the question.

## Routing — honoured the dispatch, but backstopped its one weak assumption

Dispatch said PO → PM → developer. Set `next_agent: pm` with a `pm_routing_note` forbidding re-decomposition (S-size, 4 files, exact ACs — the brief IS the spec, per the dispatch's own reasoning for skipping BA/architect).

**what-considered:** re-point straight to `agent-father` instead. **why-change:** did neither blindly. `feedback_brief_stated_implementer_not_authoritative_verify_commit_zone` says check the zone, so I did — and it is genuinely ambiguous: `.claude/commands/` is **not** in agent-father's declared `commit_zone.allowed` (`docs/agents/agent-father/init.md:63` lists `docs/agents/`, `docs/agent-memory/`, `.claude/skills/`, `.claude/agents/`), yet `git show --stat 36e109170` proves agent-father committed all three `.claude/commands/crons/*.md` files two hours ago. Declared zone and observed behaviour disagree; overriding the router on a contradiction I cannot resolve would be guessing. Kept the router's routing and added **AC-7**: land a real commit covering all 4 files, record the SHA, and if the implementer cannot commit `.claude/commands/`, hand to agent-father rather than leaving edits uncommitted. That closes `feedback_commit_zone_excluded_agent_ships_board_stays_stale` (occurrence 2) whichever way the ambiguity resolves.

## Hard constraint propagated into the row

The dispatch's "zero scheduler-tool calls by anyone in the chain" is now **AC-5** on the row (no `CronCreate`/`CronDelete`/`CronList`, no `/cron-*` skill), with the grep-confirmation duty written into `qa_gate_note`. Same reason as the scope fence: a constraint stated only in a spawn prompt does not reach the agent three hops down. PO itself made zero scheduler-tool calls this cycle.

## Writes

One `orch-apply.sh` pipe — Stage 0+1 **PASS**, conservation `task_total 783→784` / `signal_total 200=200`, 3 rows stamped, **1 row created** (the HOLD row; the +1 is exactly it). Candidate dry-run to scratchpad and diffed before applying. Live backlog moved 360→361 under me mid-cycle from a peer write; the candidate was regenerated from live at pipe time, so the CAS guard held. Stage 1g dangling refs: 17 listed, 16 pre-existing; the 17th is my HOLD row's prose `blocked_by`, matching the same prose-not-an-id convention 6 other backlog rows already use — NON-FATAL, Stage 1 PASS. Duplicate-id sweep run on the candidate: the 2 hits (`FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR`, `FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT`) are confirmed pre-existing in live, neither is mine.
