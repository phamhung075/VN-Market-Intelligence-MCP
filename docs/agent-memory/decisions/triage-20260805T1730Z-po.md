# PO Decision Journal — triage 2026-08-05T17:30Z (CI RED, HEAD f080fbc08)

Spawned by dev-team Step 1. 4 board rows minted, 1 review row disposed.

---

## D1 — task_id: FIX-CI-SIZELINT-BCTC-1345B-PARSE-VALIDATOR-PAIR

**what-considered:** (a) two separate rows, one per file, matching the 7-row `FIX-CI-SIZELINT-*` family precedent; (b) one row tying both files to the recent BCTC fallback/period-mismatch work of this session, as the relay suggested; (c) one row tying both files to a single earlier commit.

**why-change:** Chose (c). The relay's premise for (b) is refuted by raw git: `git log -- <each file>` shows the last commit touching either is `7ac55adc8`, dated 2026-08-01 — four days before this session. This session's BCTC rows touch different files. Chose one row over (a) because `git show --stat 7ac55adc8` is +116L/+153L across *exactly* these two files: one commit, one cause, one verification. Splitting would create two rows whose ACs can only be satisfied together.

**deliberately not done:** Did not treat the 7 parked `FIX-CI-SIZELINT-*` rows as evidence that the family shape fails. Checked instead: the lint now reports only these 2 files, so all 7 prior targets are green. They are parked at `next_agent=qa` — a lane-drain backlog, not a failed fix.

**remedy left open:** Extraction vs justified baseline bump is the implementer's call. Named the natural seam (the two reason/detail builders `7ac55adc8` added) and the two sibling precedents that both chose extraction, without pre-deciding.

---

## D2 — task_id: FIX-CI-PARITY-CLAUDEMD-CRON-LITERAL-EXEMPTION-SHAPE

**what-considered:** (a) fix CLAUDE.md wording to the `(N crons)` convention; (b) widen the test's exemption regex to accept un-parenthesised per-skill prose.

**why-change:** Chose (a) after reading `findCronCountLiterals` (`tool-registry-parity.test.ts:223-235`) rather than assuming which side was correct. The exemption predicate requires the *whole match* to be paren-wrapped; `CLAUDE.md:47` satisfies it, `:48` does not. The relay's claim that `cronJobCount` "may agree with 4" is false — it is 88 — so `4` genuinely reads as divergent global drift. The detector is behaving as designed; the defect is that `9af50bb26` (2026-08-04) added a per-skill annotation without the convention's token.

**deliberately not done:** (b) rejected. Widening the exemption to un-parenthesised prose creates a hole real global-count drift walks through, and gate-widening requires an actuator dry-run first (standing rule). Also declined to touch CLAUDE.md semantics: AC4 fences the edit to punctuation-only, zero instruction-meaning change, because CLAUDE.md is the user-owned project instruction file.

**second defect found and folded in:** `T-U3-7` asserts `hits.every(h => h.exempted)` — strictly stronger than its own docstring, and it would also fail on a *correct* un-parenthesised global literal matching the SSOT. Filed as AC2 so this cannot recur from the other direction.

---

## D3 — task_id: FIX-CI-TASKCLAIM-PO-FLOW-OWNER-SESSION-PAYDOWN

**what-considered:** (a) the parent fix never covered this file; (b) the file was edited after the fix and reintroduced the gap; (c) re-run `--update` to re-grandfather; (d) widen the baseline key from `(file,line,snippet)` to `(file,snippet)`; (e) pay the debt.

**why-change:** Chose (e). Both relayed hypotheses are refuted at source: `git blame -L 42,53` attributes the entire block to `dc430566c2`, 2026-05-27 — never edited after the parent fix, so (b) is false; and the baseline *does* carry this site as `sprint-kickoff.md:42`, so (a) is false. Actual trigger: `3ce726a6e` inserted 2 net lines above, shifting it to `:44`. The lint's own header documents the `(file,line,snippet)` key and that a moved line loses grandfathering — the guard did exactly what it was built to do.

**why this is not cosmetic:** Read `coordinationTools.ts` directly. `owner_client_session` is `z.string()` with no `.optional()` on both `task_claim` (:104-110) and `task_release` (:199-205). A doc-faithful PO never engages the sprint umbrella mutex at kickoff and can never release `task:<sprint_id>`, stranding it for the full 3600s TTL. This is a live defect in PO's own flow, carried since 2026-05-27.

**deliberately not done:** (c) and (d). Re-grandfathering hides a live defect; widening the key loosens a working ratchet whose boy-scout intent (touch a file, pay its debt) is correct. Scope instead widened to all 3 PO-owned sites (kickoff `task_claim`, signoff `task_release` ×2) so `docs/agents/po/flow/**` leaves the baseline entirely and no future line-shift can re-trigger this.

**ergonomics AC added:** the lint's FAIL message should distinguish "grandfathered site whose line moved" from "genuinely new call site". This exact ambiguity is what produced both wrong hypotheses upstream.

---

## D4 — task_id: FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER

**what-considered:** (a) mint only the three symptom rows and let CI go green; (b) add a process rule that agents check CI before pushing; (c) find and file the mechanism that let three independent breakages survive.

**why-change:** Chose (c). `gh run list --workflow=CI --limit 100` shows 30 consecutive failures, last green 2026-08-01T12:09. Read `.git/hooks/pre-push` in full: it runs `pnpm --filter vn-market check` (tsc) and `rebuild-raw-verify-check.sh` — neither size-lint, nor task-claim lint, nor bun test — and both are gated behind a `CODE_TOUCHING_REGEX` that explicitly excludes `docs/`. Two of the three breakages arrived on exactly the excluded paths. (a) alone would guarantee a 4th recurrence; (b) is unenforceable and is the class of "fix" that produces churn without convergence.

**deliberately not done:** did not prescribe the widening. The hook carries a hard measured constraint — UC-GCP-P4 recorded tsc at ~94s against a 90s commit-mutex TTL, which is *why* the docs-excluding filter exists. AC1 therefore requires a measured dry-run over the last ~30 pushes *before* any change is proposed, per the standing rule that gate-widening needs an actuator dry-run.

**fence:** the row states it must not be closed by the three symptom rows going green.

---

## D5 — task_id: FIX-TASKCLAIM-OWNER-CLIENT-SESSION-MISSING-FLEET-FLOW-DOCS (disposition)

**what-considered:** (a) leave parked at `next_agent=po`; (b) reopen it to absorb the `sprint-kickoff.md:44` hit; (c) route to qa.

**why-change:** Chose (c). Re-verified AC5/AC6 at source rather than trusting the row's own 07-30 "partial" note: `commit-mutex/SKILL.md:36,:69`, `refine_bctc_md/flow/main.md:24-36` (including the spawn-prompt `Coordination:` extraction channel the note said was missing), and `pm/flow/main.md` are all compliant; alert-commander was already compliant. AC4 is now *positively proven* rather than merely shipped — the guard it delivered caught a real regression today (run 31028825500). That is the anti-false-green evidence the row previously lacked.

**deliberately not done:** (b). Fenced explicitly in the row: `:44` is grandfathered in this row's own baseline as `:42` and surfaced only via a line shift. Absorbing it would let a closed row reopen on an unrelated commit.
