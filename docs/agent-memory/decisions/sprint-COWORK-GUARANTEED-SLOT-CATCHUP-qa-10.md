# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa

**Sprint goal:** Not reopened for this row — resolver self-target fix direct-commit verify riding the same dev-team Review-Lane QA-Drain batch; own new file (qa-8.md was mid-write/clobbered by a concurrent peer QA sub-agent per qa-9.md's own note — avoiding re-collision by starting fresh rather than appending to any in-flight sibling file).
**Agent:** qa
**Started:** 2026-08-07T00:07:40Z

---

### STEP qa-S1 · qa · 2026-08-07T00:07:40Z
**task-id:** FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE
**what-done:** Direct-commit verify (`qa[]` row, `branch:null`) of commit `4a6d531518b74182eb8b2f3217abe66977bc083f`. Row's own `status_note` explicitly flagged architect self-verified first, then self-corrected to route through independent QA before DONE_VERIFIED — router dispatch explicitly instructed re-deriving AC-2/AC-3 myself, not trusting the architect's own re-query.
**what-considered:**
- Dispatch prompt said the row lives in `task_board.review[]`; live query showed it's actually in `.task_board.qa[]` (matches this flow's Direct-Commit-Verify input spec exactly) — metadata inaccuracy only, not a blocker (same class noted by a sibling qa-9.md entry this cycle).
- Row has no `files[]` field — fell back to the architect's own `architect_review_note` prose (4 named files) + `git show --stat` on the commit, which matched exactly: `scripts/lib/devteam-eligibility.jq`, `scripts/devteam-review-claim-secondary-drain.jq`, new test `scripts/devteam-eligibility-resolved-secondary-dispatch-target.test.sh`, `docs/agents/dev-team/flow/main.md`.
- Read the actual diff, not the commit-message prose: `resolved_secondary_dispatch_target()` one-line change from `if ($na|length)>0 then $na else "po" end` to `if ($na|length)>0 and ($na != "dev-team") then $na else "po" end` — correct, minimal, matches the claimed fix shape (dev-team falls to po, same as null).
- AC-1 (exactly one caller repo-wide): grepped `resolved_secondary_dispatch_target` across the whole repo myself — only real invocation is `scripts/devteam-review-claim-secondary-drain.jq:123`; the 2 `.test.ts` hits mentioned in a sibling review are comment/label-only (not this row, a different sibling's dev-team review confirmed this same fact independently on 2026-08-05).
- Re-ran regression myself, did not trust "5/5"/"7/7" from prose: `scripts/devteam-eligibility-resolved-secondary-dispatch-target.test.sh` → 5/5 PASS exact match (T1 null, T2 absent, T3 dev-team→po THE FIX, T4 architect passthrough, T5 ops passthrough). `scripts/orch-backlog-stub.test.sh` → 7/7 PASS, sibling predicate unaffected.
- AC-2/AC-3 (live re-derivation, the router's specific ask): the architect's 4 named rows (FIX-BCTC-FPT-BT5-BALANCE-GATE / FIX-MACRO-THRESHOLD-FXFLOOR-OVERCLAMP / FIX-FOREIGN-FLOW-BULLETIN-UNAVAIL-STRING / FIX-DRAIN-TEST-HARNESS-ORCH-HELPER-COPY-LIST) are 2 days stale — board churn moved 3 of the 4 off `review[]` entirely (normal tick-to-tick churn, not a defect); the 4th now shows `next_agent=="qa"` with a pre-existing `secondary_dispatch_target="po"` stamp left over from before it was re-routed to qa — consistent, not contradictory. Rather than chase 3 vanished rows, re-derived the GENERAL invariant live: wrote a scratch jq script that `include`s the real `scripts/lib/devteam-eligibility.jq` and runs `effective_next_agent`/`resolved_secondary_dispatch_target` over every current `review[]` row with `status=="REVIEW"` (193 rows) — 0 have `effective_next_agent=="dev-team"`, 0 resolve to `"dev-team"`. Cross-checked with a full end-to-end dry run of the REAL production script `scripts/devteam-review-claim-secondary-drain.jq` against a scratch copy of `orch-state.json` (never wrote to the live file) — of the 11 rows carrying any `secondary_dispatch_target` stamp (old + 1 new claim), zero resolve to `"dev-team"`.
- `bun tsc --noEmit` (apps/mcp-server) → 0 errors, matches claim (zone `cross-service/`, zero `apps/` TS touched — confirmed via `git show --name-only`: only `.jq`/`.sh`/`.md`). `mock-guard.sh` N/A, no production TS/JS files in the diff.
- DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-2.md` STEP architect-S25, `task-id:` line present, matches.
**why-decision:** APPROVED, DONE_VERIFIED. Every AC independently re-derived from raw sources/live data, not accepted from the architect's own self-report — the exact independent-sign-off gap the row's own status_note flagged. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 753→753, signal_total 204→204). `next_agent: pm`. Verification text appended to the row's own `status_note` field (no handoff file, no `review_note` field on this row).
**why-change:** none from plan — direct-commit-verify jump ran as specced; the router's explicit instruction to re-derive rather than trust was honored via the general-invariant re-derivation above (the specific 4 named rows being stale on the board is an artifact of 2 days of churn, not a gap in the fix).
