# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · dev-mcp-server (continuation 5)

**Sprint goal:** see sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md header — this file is a byte-cap rollover continuation (dual-axis cap check, base file capped 2026-07-31, -2 capped 2026-08-01, -3 capped 2026-08-06, -4 capped 2026-08-09).
**Agent:** dev-mcp-server
**Started:** 2026-08-09T02:10:00Z

---

### STEP dev-mcp-server-S83 · dev-mcp-server · 2026-08-09T02:10:00Z
**task-id:** FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L
**what-done:** `coordinationTools.ts` was 457L (baseline=388L, upper=426L, 31L overage), CI-RED on size-lint (run 31285673936, head 39e1e161). Split all 6 registered tools (task_claim, task_heartbeat, task_release, task_list_held, task_force_release_orphan, get_week_period) verbatim into 6 new files under `interface/mcp/tools/system/coordination/` (54-119L each — all under the 120L size-lint threshold, so none needs a `size-justification` header). `coordinationTools.ts` is now a 54L thin entry point re-exporting `registerCoordinationTools(server)` unchanged. Dropped the dead `buildWeeklyPublishMarkerKey` import (unused in the file body, confirmed via grep before removal) and pruned the now-stale 388L `docs/data/size-lint-baseline.json` entry (surgical `jq del`, not a wholesale `--update` — a wholesale `--update` would have also silently rebaselined the separate, out-of-scope, already-failing `transport.ts` offender, which this row does not own).
**what-considered:**
- 1-2 grouped files (e.g. heartbeat+release together) — rejected: bundling would push each grouped file past 120L (own zod-schema-heavy tool descriptions are ~55-80L each), forcing a justification header per file anyway; one-file-per-tool keeps every file comfortably under threshold with zero headers needed.
- `--update` baseline bump — rejected outright per the row's own explicit mandate ("do NOT rebaseline silently") and CLAUDE.md's "reduce debt" directive; a real split was structurally available (each tool is already a self-contained `server.tool(...)` call with no cross-tool state except the `SERVER_SESSION_ID` const, which only `task_claim` uses).
- `size-justification` header on the untouched 457L file — rejected: a mechanically clean per-tool split existed (same precedent as the S82 `coordinationStore.ts` sibling row), so a header would paper over a cheap structural fix.
**why-decision:** Each of the 6 tools is already syntactically self-contained (own zod schema + own async handler, single store-function call) — a per-tool file split is the lowest-risk, zero-logic-change remedy and produces files well clear of the threshold with room to grow.
**why-change:** none — split chosen on its merits per the row's own split-OR-header remedy, same class of fix as the S82 sibling.
**verification:** `bash scripts/audits/size-lint-justification.sh --check` no longer lists `coordinationTools.ts` (sole remaining offender: pre-existing, untouched `transport.ts`, out of this row's scope). `bun tsc --noEmit` clean. `npx eslint` (fence config) clean on all 7 touched/new files. Targeted suite: `task-lock-coordination-tools.test.ts` + `1980-p1-final-required-flip.test.ts` + `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts` + `FIX-DIGEST-PREDICT-ISO-WEEK-DEDUP.test.ts` + `1982-quality-burndown-CHIJ.test.ts` = 96/96 pass, 0 fail (all 4 files that `import { registerCoordinationTools }` directly, plus the ISO-week dedup suite covering `get_week_period`'s domain dependency). Pushed commit `1653cea0aa56e43b4a84463a3dc2772bbde15705`; pre-push hook (`pnpm --filter vn-market check` / `tsc`) green, no `--no-verify`. Board → `review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`. CI-plane verification is a NEXT-TICK QA action (`gh run view <databaseId with headSha AFTER 1653cea0a> --json jobs -q '.jobs[]|select(.name=="size-lint")|.conclusion' == success`) — not observable from this session per the task's own `verification_gate: ci_green_on_subsequent_push`.

---

### STEP dev-mcp-server-S84 · dev-mcp-server · 2026-08-09T02:10:00Z
**task-id:** FIX-SSE-SOAK-VERIFY-DEPENDS-ON-SHARED-CONTAINER-UPTIME-3RD-RESET
**what-done:** Added an injectable `_now: () => number = Date.now` clock (8th `SseSessionManager` constructor param, same override idiom as `_heartbeatIntervalMs`/`_idleTimeoutMs`/`_maxAgeMs`/`_reaperIntervalMs`) to `apps/mcp-server/src/interface/mcp/transport.ts`, threaded through all 3 `Date.now()` call sites. New T13/T14 tests in `1862c-transport-session-eviction.test.ts` construct the manager with every real shipped default intact (the actual 4h max-age, not a shrunk override) and inject a fake clock to advance past the threshold instantly, proving the exact shipped max-age branch fires — no real elapsed wall-clock time, no dependency on any container's uptime. AC-2 companion: stamped `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER`'s `qa[]` row `status_note` (previously absent through all 3 resets) with the full incident history.
**what-considered:**
- Fake-timer/`setSystemTime` mocking (jest-style) — rejected: Bun's test runner (1.3.13, verified via bun-types) has no built-in fake-timer support; the injectable-clock pattern is the only mechanism available and matches the codebase's own existing override idiom.
- Real-timer + shrunk-threshold-only (extend T10's existing pattern) — rejected as insufficient alone: T10 already proves the branch fires, but only with a shrunk 20ms override, never the REAL shipped 4h default, and (per the CI-regression triage already on the sibling row) real-timer races under CPU contention are exactly the flake class that hit T9 — an injected clock sidesteps that entirely while still testing the true default value.
- Persisted session-age ledger (the row's other suggested option) — rejected as heavier: would add a new persistence surface for a value (`createdAt`) already held in-memory correctly; the actual defect was the EVIDENCE SOURCE for a unit-provable invariant, not a missing persistence layer.
**why-decision:** the injectable-clock design directly satisfies AC-1's own framing ("advance a mock clock past the threshold, assert the session is reaped") while testing the unmodified shipped 4h constant, is immune to the T9-class contention flake (verified 8/8 clean runs under manufactured 48x `yes` CPU load), and requires the smallest surface-area change (one new constructor param, three call-site substitutions).
**why-change:** no change from the row's own AC-1 suggestion — implemented the first listed option.
**verification:** `1862c-transport-session-eviction.test.ts` 14/14 pass (45 expect), 5 clean reruns, 8/8 clean under manufactured CPU contention (only pre-existing T9 flaked, separately tracked `FIX-CI-BUNTEST-1862C-TRANSPORT-SESSION-EVICTION`, not touched). `bun tsc --noEmit` clean. Full suite 15230 pass/40 skip/51 fail/48042 expect across 1269 files — consistent with the pre-existing flaky baseline already documented on the sibling row (none of the 51 in files this commit touches). Gate 2: `PORT=3099` clean boot, `/health` `{toolCount:183,sessions:0}`; `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88, both match baseline. Doc: `infrastructure.md` SSE Session Manager section updated. Pushed commits `a3d7ff35f` (code+test+doc) and `50a0fce44` (board: `FIX-SSE-SOAK-VERIFY-...` `backlog[]→review[]`, `status:REVIEW`, `next_agent:qa`, `.head` untouched — not `.head.active_task_id`; `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` `status_note` stamped, lane/status left unchanged per the row's own AC-2 scope). Pre-push hook (`pnpm --filter vn-market check`) green both pushes, no `--no-verify`.

---

### STEP dev-mcp-server-S85 · dev-mcp-server · 2026-08-12T00:24:00Z
**task-id:** FIX-CRON-ALERTSCAN-CONFIG-KEYS-ORPHANED-BY-PARALLEL-WRAPPER
**what-done:** `cronConfig.ts` still exported two independent CRONS keys (`taAlertScan`/`bbAlertScan`) from before the `alertScanParallelJob` refactor (task 1309c) that merged both scans into one scheduled job. `bbAlertScan` was grep-confirmed dead (zero registration call sites); both keys' `/api/cron-status` Layer-A rows resolved (tier-3 honest fallback in `resolveJobNameDb`) to their own literal key name, which matched frozen pre-refactor `cron_job_runs` rows (last real fire 2026-04-24) — an unfalsifiable permanent CRITICAL. Collapsed both keys into one `alertScanParallel` CRONS key (same cron string `*/15 2-8 * * 1-5`, new env var `CRON_ALERT_SCAN_PARALLEL`) in `cronConfig.ts`, repointed `schedulerJobTable.ts`'s sole `alertScanParallelJob` entry's `cron:` field to it.
**what-considered:**
- AC-1's 2nd listed option — make Layer-A enumerate the registered scheduler table instead of the CRONS config table — rejected: large blast radius across `cronStatusHandler.ts`/`cronStatusCompute.ts` touching ~89 other CRONS-keyed rows, disproportionate to a 2-key naming defect; the row's own AC-1 phrasing offers the key-collapse as an equally valid, narrower alternative.
- Adding `alertScanParallel` to `STATIC_JOB_NAME_MAP` (tier-1) for cache-safety — rejected: `FIX-CRON-SSCCHECKERJOB-DEAD-87D-classfix-registry-invariant.test.ts` locks that map 1:1 to `CANONICAL_WATCHDOG_JOB_NAMES` (`WATCHDOG_MANIFEST` keys); `alertScanParallelJob` is not watchdog-covered and adding it would need a `WATCHDOG_MANIFEST` entry too — out of this row's explicit OBSERVABILITY-only scope (NOTE: parallel-wrapper liveness itself is FIX-ALERT-OPEN-ZERO-PRICE-RACE/FIX-SCHEDULER-DOUBLE-REGISTRATION). Verified tier-2 normalized-fallback alone is sufficient: `normalizeJobToken('alertScanParallel')` === `normalizeJobToken('alertScanParallelJob')` === `"alertscanparallel"`.
**why-decision:** the collapse directly satisfies AC-1's first option, requires touching only the 2 files the row names, and the tier-2 normalizer proves the new single key self-resolves to the real `wrapRun('alertScanParallelJob', ...)` cron_job_runs rows with zero registry-map edits (AC-2 satisfied for free).
**why-change:** no change from the row's own AC-1 first option.
**verification:** controlled unit probe (`buildLayerARow` against an in-memory DB seeded with one real `alertScanParallelJob` row) confirms tier-2 resolves `job_name_db:"alertScanParallelJob"` and surfaces the real `last_fire`/`last_status` — AC-2 mechanism proven. Live local server boot (`PORT=3097`, cwd=project-root for `.claude/commands/crons`) `/api/cron-status`: `layer_a_count` down by 1, zero rows named `taAlertScan`/`bbAlertScan`, one `alertScanParallel` row present (this dev DB has no historical alert-scan rows at all, so it honestly reads `NEVER_FIRED` here — not the same DB as the row's live evidence, expected). Targeted 146/146 pass (0 fail) across cronStatusHandler/cronStatusCompute/STATIC_JOB_NAME_MAP-invariant/job-metrics/alert-scan-parallel/ta-alert-scan/bb-alert-scan/pipeline-watchdog/integration-observability/FACTORY-SCHEDULER-registry/1949-cron-rewiring. `bun tsc --noEmit` clean. Gate 2: `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88, both match baseline (job-table entry count unchanged — rename only, no add/remove). Full bare `bun test`: 15224 pass/40 skip/60 fail/2 errors/48030 expect across 1269 files (504s) — none of the 60 fail or 2 unhandled-error names touch `cronConfig`/`schedulerJobTable`/`cronStatus`/`alertScan`/CRONS (grep-confirmed), consistent with this repo's documented pre-existing flaky-churn baseline (S84 sibling entry: 51 fail/48042 expect; count drifts run-to-run per that entry's own note). Deliberately did NOT touch `docs/data/cron-registry.json`/`system-map.json`'s `taAlertScanJob`/`bbAlertScanJob` doc rows — those name the still-real, still-running job *functions* (unchanged), not the collapsed CRONS *key*, so they remain factually accurate; editing them was out of this row's explicit 2-file scope.

---

### STEP dev-mcp-server-S86 · dev-mcp-server · 2026-08-12T07:15:00Z
**task-id:** FACTORY-APP-split-pollNews
**what-done:** BOUNDED-1 auto-pickup, title-only spec (no detail_ref). `pollNews.ts` (1444L, no size-justification header, baselined at 1444L in `size-lint-baseline.json`) was 1 giant function + 5 clusters of unrelated helpers. Extracted 5 single-responsibility sibling files under new `application/usecases/pollNews/`: `types.ts` (5 public interfaces), `insiderDetectors.ts` (2 pure classifiers), `signalDedup.ts` (1 internal helper), `defaultFetchers.ts` (6 lazy network fetchers — dropped the grep-confirmed-dead `defaultTradingEconomicsFetcher`, not relocated), `dbHelpers.ts` (4 SQLite helpers). `pollNews.ts` is now a 923L orchestrator that imports + re-exports everything unchanged — zero external call-site changes (verified via grep: only `PollNewsResult`/`SourceFetchers`/`detectInsiderFamilyBuying`/`detectInsiderSelling`/`_resetAllDarkAlert`/`pollNews` are imported from outside, all still resolve from `pollNews.ts`).
**what-considered:**
- Convert `pollNews.ts` to a directory `index.ts` — rejected: 2 external non-test files import `pollNews.js` with an explicit `.js` extension; directory-index resolution under that literal filename is not guaranteed by Bun's bundler-mode resolver, and re-export-from-sibling-folder is zero-risk by comparison.
- Also decompose the ~790L `pollNews()` orchestrator body itself into pipeline-stage functions this same pass — deferred: its local state (db, watchlist, resolvedFetchers, macroContext/macroStats, nmCfg closures) is tightly threaded through every stage; a rushed split risks a silent behavior change the isolated-helper extractions above do not carry. Flagged as an explicit follow-up in the file's own banner + `usecases.md`.
**why-decision:** re-export-from-sibling-folder preserves 100% import-path back-compat (proven by tsc clean + the grep audit) while still shrinking the god-file 1444L→923L (36%) into 5 real single-responsibility modules — the safest "staged, verifiable" increment for a P0 file with 13 existing test files depending on it.
**why-change:** no change from the task title's own "split into single-responsibility modules (staged)" framing.
**verification:** `bun tsc --noEmit` clean (0 errors). `size-lint-justification.sh --check`: only pre-existing, untouched `transport.ts` fails (265L vs its own 126L baseline — not this row's file); scoped `jq` update of `size-lint-baseline.json`'s `pollNews.ts` entry 1444→923 (not a wholesale `--update`, which would have silently rebaselined `transport.ts` too). New files 126-173L each carry an in-file `size-justification:` header (within first 10 lines, verified by the script's own `head -10` gate). All 13 pollNews-referencing test files pass in isolation (109/109, 0 fail) when run without heavy suite-wide contention. Full `bun test`: 15221 pass/40 skip/63 fail/1 error/48017 expect (1104s, contended — concurrent server-boot+earlier runs) — 4 pollNews-touching names among the 63 (`FIX-POLLNEWS-COUNTER-CONSERVATION` x3, `push-news...9 sources` x1), ALL at the ~5000ms bun-test-timeout mark; re-verified NOT a regression by swapping the pre-split HEAD `pollNews.ts` back in and reproducing the IDENTICAL timeout signature on the identical test names (`102-job-news-poll`/`1324-push-news-all-sources`) under the same contention, then re-running `FIX-POLLNEWS-COUNTER-CONSERVATION.test.ts` alone post-suite on the split version: 3/3 pass, 0 fail — confirms pre-existing load-dependent network-timeout flake, not a split regression. Gate 2b: `PORT=3099` clean boot, `/health {toolCount:183,sessions:0}`. Gate 2c/2d: `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88, both match baseline. Dashboard circular-dep check (`/api/bctc-inspect`, `/dashboards/news-fetch/`): both return real HTML, not 500/empty. Doc: `usecases.md` `pollNews.ts` entry expanded with the new module map + follow-up note. Graphify: SKIPPED — this subagent has no Skill-tool binding (Task-tool subagent context, same INV-GATEWAY-1-class gap as the MCP dispatcher binding), so `/graphify docs --update --no-viz` cannot be actually invoked from here; noted honestly rather than claimed.

### STEP dev-mcp-server-S87 · dev-mcp-server · 2026-08-12T07:56:00Z
**task-id:** FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW
**what-done:** Implemented the PO-ratified architect spec verbatim — threaded `ColumnLayout` cross-window (`refinedMarkdownParser.ts`/`finalizeBctcRefine.ts`, mirroring the live `carrySection` thread exactly) and made `Window.truncated_continuation`/`RefineWindow.truncated_continuation` real (`windowPartitioner.ts` + `getBctcPendingRefineTool.ts` wire pass-through), plus 2 flow-doc updates.
**what-considered:**
- Implement verbatim vs re-derive from scratch — verbatim: spec was PO-ratified AT SOURCE (`po_goahead_20260812T035700`, independently confirmed live-code line-anchors in orch-state, not a relayed verdict), re-deriving would duplicate already-verified work with zero benefit.
- Also thread `columnLayout` through `bctcRefineJob.ts`'s own `parseRefinedMarkdown` call site (identical carrySection-only pattern) — declined: that file's own header states the production spawn path is DELETED (Option-Y ruling), `refineOneReport()` is a test-only DI helper, not a live defect path.
**why-decision:** the spec's hand-traced regression fixture (§5, 4 parts) matched post-implementation reality exactly — 7/7 GREEN with the identical predicted values (`rows_parsed=5`, tail row `code="3"` not swapped, BEQ-7 override to PARTIAL) — zero deviation needed.
**why-change:** none — spec followed verbatim. Added one thing the spec didn't anticipate: `windowPartitioner.ts` crossed the 120L size-lint threshold (115L→149L) as a "new-offender", so I added its own `size-justification:` header to keep the gate green.
**verification:** RED-first (wrote the spec's full test file before touching source — 5/7 failed pre-fix, confirming the fixture is decisive). `tsc --noEmit` clean. Targeted 412/412 pass (0 fail) across all 32 test files referencing the touched functions/tools. Full `bun test` x2 independent runs: run1 15234 pass/40 skip/57 fail/1 error/48061 expect (770s); run2's full fail-name list grep-confirmed ZERO overlap with `windowPartitioner`/`refinedMarkdownParser`/`finalizeBctcRefine`/`getBctcPendingRefineTool`/`resolveColumnLayout`/`BCTC-REFINE-WINDOWTRUNCATION` — consistent with this zone's documented pre-existing flaky-churn baseline (S84/S85/S86 siblings: 51/60/63 fail bands). Gate 2b/2c/2d: clean boot, toolCount=183/cronJobCount=88 both match baseline. Dashboard circular-dep check clean. `size-lint-justification.sh --check`: only pre-existing untouched `transport.ts` fails. Health-recheck WARN misattribution (spec §7 "no live doc to correct" finding): independently spot-checked via repo-wide grep for the misattributing phrase — zero live hits outside the spike doc itself, confirms closed. Commit `8b4f977fd` (code+test+docs), pushed clean, pre-push tsc hook green. Board: `2d7c6a7e4` (`in_progress[]`→`review[]`, `.head` idle-reset, same write, per CANONICAL:SSOT-STATUSFLIP-LANEMOVE).

### STEP dev-mcp-server-S88 · dev-mcp-server · 2026-08-12T08:31:00Z
**task-id:** FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST
**what-done:** Implemented the architect's ratified design (brief `2026-08-05-fix-bctc-ssc-doc-selection-quarter-blind.md`) verbatim, superseding the stale handoff's generic PM-planning-context prose (which proposed adding a quarter param to `listSscDocuments()` — explicitly the option the architect investigated and rejected). New `domain/services/financial-reports/documentTitlePeriodExtractor.ts` (pure `extractPeriodFromTitle`, parses Vietnamese "quý N/I..IV năm YYYY" + abbreviated "QN YYYY" title forms) + `application/usecases/bctc/selectSscDocument.ts` (`selectSscDocumentForPeriod` + named `SscDocumentPeriodNotFoundError`, title-first with `deriveQuarterFromPublishedAt` fallback). `fetchParseAndStoreBctc.ts` Step 1 now calls the selector instead of `docs[0]`; no-match is caught and converted to the function's existing `return null` contract with a debounce-gated Telegram bug (mirrors `parseBctcReport.ts:685-701`'s idiom exactly, distinct debounce key `doc-selection-not-found`).
**what-considered:**
- Option (b) (run `checkPeriodContentConsistency` as a selector, reusing shipped code) vs option (a) (title-parse + select) — architect already resolved this in the brief (chose (a): (b) collapses the Step1/Step2 boundary, needs N downloads before selection, and its null-on-inconclusive design has no safe selector-context default); implemented (a) as ratified, did not re-litigate.
- A private `derivePublishedAtSignal(publishedAt)` helper vs inlining the publishedAt-fallback branch into the selection loop — inlined (simplicity-gate Q2: a helper with exactly one call site is unjustified abstraction; the guard+adapt logic is 5 lines, equally readable inline).
- Trusting `deriveQuarterFromPublishedAt`'s own empty/unparseable-input fallback (Q1-of-current-year) as a selection signal vs guarding it — guarded (explicit empty-string check before delegating): that function's fallback exists for its original caller (`checkSscReports.ts`, always a real portal date) and repurposing an ungated fallback as a selector signal is exactly the class of defect this row exists to eliminate (mirrors the architect's own reasoning for rejecting option (b)'s null-handling).
**why-decision:** the brief is PO/architect-ratified with a fully-specified plan, regex sketches, exact call-site diff, and file list — implementing verbatim is both the correct process (no re-derivation of already-verified design work) and the fastest path to AC-1..AC-3 with zero re-litigation risk.
**why-change:** none from the brief's plan. One test-fixture repair not anticipated by the brief: 3 pre-existing tests in `293-ocr-fallback-pipeline.test.ts` requested Q2/Q3/Q4 against a mock SSC listing containing only a Q1-titled document (previously masked by Step 1 being quarter-blind) — corrected their `quarter` param to `Q1` to match the fixture; OCR-cache filename key is derived from the doc URL, independent of the requested quarter, so this is a pure fixture-honesty fix, not a behavior change.
**verification:** RED-first (`bun test` on the new file before implementation confirmed compile failure on the not-yet-created imports). New test `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST.test.ts`: 15/15 pass (35 expect) — domain `extractPeriodFromTitle` unit tests (every live fixture-title shape from `029-ssc-scraper`/`104-job-ssc-check`/`034-telegram-notifier`/`1289f-refinement-direct-api`), `selectSscDocumentForPeriod` unit tests (AC-1 match-among-several, AC-2 named-error-on-no-match + empty-candidate-list, publishedAt-fallback, empty-publishedAt-never-trusted), AC-3 family-A fingerprint regression (Q1 request vs Q4-first listing — Q4 doc NOT selected), and 3 `fetchParseAndStoreBctc` integration tests (AC-1 real `listSscDocuments()` path selects the Q1 doc not docs[0]; AC-2 no-match returns null with exactly 1 debounced Telegram bug across 2 calls; docs.length===0 case stays Telegram-silent, distinct from selection-not-found). `tsc --noEmit` clean. Targeted 376/376 pass (0 fail) across all 39 test files referencing `fetchParseAndStoreBctc`/`ssc.js`/`checkSscReports`/`newsChainFallback`. Full `bun test`: 15247 pass/40 skip/59 fail/1 error/48101 expect across 1271 files (546s) — grep-confirmed ZERO overlap between the 59 fail names and `bctc`/`ssc`/`fetchParseAndStoreBctc`/`selectSscDocument`/`documentTitlePeriod`/`293-ocr` (the one superficially-alarming hit, `1405b-bctc-vps-fixes.test.ts`, fails on unrelated `vps_push_log`/news-service assertions, imports none of this task's files); consistent with today's documented pre-existing flaky-churn band (S85/S86/S87 siblings: 60/63/57 fail). Gate 2: `PORT=3099` clean boot, `/health {toolCount:183,sessions:0}`; `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88 both match baseline (no tool/cron files touched). Dashboard circular-dep check: `/api/bctc-inspect` + `/dashboards/news-fetch/` both HTTP 200. `size-lint-justification.sh --check`: `fetchParseAndStoreBctc.ts` grew 125L→168L (touched, pre-existing baseline entry) — added its own `size-justification:` header (accurate, 168L); only pre-existing untouched `transport.ts` still fails. AC-4 (family-B/DPM scope answer) and AC-5 (backfill-replay gate) are architect-answered/QA-gated per the brief §5/§7 — not re-derived here. AC-5 explicitly BLOCKED on `BCTC-ENRICHER-OLD-QUARTERS` (still BACKLOG per the brief's live-check) — noting per the dispatch prompt's instruction, not skipping AC-1..AC-4.

### STEP dev-mcp-server-S89 · dev-mcp-server · 2026-08-12T10:36:00Z
**task-id:** FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L
**what-done:** Confirmed CI genuinely RED at current main HEAD (`gh run view 31583395162` on head `4424dd410`, an ancestor with zero subsequent commits touching this file — `git log -- transport.ts` confirms unchanged since `a3d7ff35f`): size-lint FAILs on exactly 1 offender of 1383 scanned, `transport.ts` at 265L (baseline=126L upper=138L). Trimmed the file's 57-line historical narrative header comment (pure prose already duplicated by git history + the linked design brief — zero code/behavior touched) down to ~22 lines, and added an accurate `size-justification: 241L — <reason>` header (within first 10 lines) explaining why the `SseSessionManager` class body is a single cohesive session-lifecycle unit that should not be mechanically split.
**what-considered:**
- Extract the reaper / route-handlers / session-map into separate files to get under the 138L upper bound — declined: every method (`evictSession`/`reapStaleSessions`/`handleSse`/`handleMessage`/`closeSession`) shares the same `this.sessions` map + `this.now` clock; the file's whole reason for existing (per its own header + the design brief) is that a prior version had 2 parallel Maps + 2 duplicated inline cleanup blocks, which is exactly the shape a naive extraction across files would reintroduce (>=2 call sites again mutating shared session state, real risk of a missed `evictSession()` path — the original bug). To close the ~127L gap to the upper bound would require fragmenting nearly half the file, not a clean single seam.
- Trim only comment bloat (no header) vs. trim + size-justification header — did both: trimming the 57L narrative (retrievable via `git log`/the design brief) is honest, risk-free size reduction on its own merits (not "extraction that reintroduces duplication"), but even a moderately aggressive trim only reaches ~228L, still far over 138L — the header is still required, and repo precedent (`financialFiguresRules.ts` 300L, `stockAliases.ts` 267L, `sectorRules.ts` ~2040L) explicitly endorses this pattern for cohesive, non-fragmentable files.
- Declare the header's line count as the exact current `wc -l` (241L) rather than a padded/approximate number — exact: makes the declared number self-evidently true today and leaves the full +10%/min-5L tolerance as real headroom for future legitimate growth, instead of already having spent it on padding.
**why-decision:** PO's triage note explicitly flagged this as a judgment call ("deliberate consolidation that removed duplication... don't default to a mechanical split") and the size/coupling analysis above confirms a mechanical split has no clean seam here — the size-justification-header route is the honest call, matching established repo precedent for exactly this shape of file.
**why-change:** none from PO's framing — PO asked for an honest judgment between extraction and a justification header; this is that judgment, decided in the header's favor with reasoning recorded in-file (not just in this journal).
**verification:** `bash scripts/audits/size-lint-justification.sh --check` → PASS (0 unjustified offenders, scanned 1383 files) — was the sole failing file before this change. `bun tsc --noEmit` clean (0 errors; comment-only change). Targeted `1862c-transport-session-eviction.test.ts`: 14/14 pass. `251-mcp-tools.test.ts` (includes the `GET /sse returns 200` assertion) in isolation: 16/16 pass. Full `bun test` (2nd run, clean — killed a leftover `PORT=3099` server from the Gate-2b probe that held `market.db` open and inflated fail count 68→57 on the contended 1st run): 15249 pass/40 skip/57 fail/48113 expect across 1271 files (502s) — grep-confirmed the only transport/SSE-adjacent name among the 57 (`Task 251 — MCP Tool Registration > GET /sse returns 200`, 5002ms) reproduces clean (0 fail) in isolation, i.e. a load-induced timeout not a regression; the 57-fail count itself matches today's already-documented pre-existing flaky-churn band (S85/S86/S87/S88 siblings: 60/63/57/59 fail, zero overlap with files those tasks touched either). Gate 2b: `PORT=3098` clean boot, `/health {toolCount:183,sessions:0}`, killed cleanly (no leftover process this time). Gate 2c/2d: `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88, both match baseline (no tool/cron files touched). Dashboard circular-dep check: `/api/bctc-inspect` + `/dashboards/news-fetch/` both HTTP 200. No `docs/architecture/microservice/mcp-server/*.md` update needed — `infrastructure.md`'s existing `SseSessionManager` section documents behavior/structure, which is unchanged; only comment prose moved.

### STEP dev-mcp-server-S90 · dev-mcp-server · 2026-08-12T15:35:00Z
**task-id:** UC-CCA-P3-FR5-CODE-GATE
**what-done:** Implemented architect brief `2026-08-08-uc-cca-p3-published-marker-gate-skill.md` §6 verbatim (PO B1 Path-A ruling). New `domain/services/publishedMarkerImmunity.ts` (`isPublishedMarkerTaskId`, pure prefix predicate). Re-verified all 4 line anchors live before editing (row's own note said unchanged at 1173L — confirmed exact: `releaseTask` :888, `releaseOrphanTask` :1000, `ReleaseResult` :391-393, `OrphanReleaseResult` :395-400). Added the UNCONDITIONAL guard as literally the first statement of both `releaseTask()`/`releaseOrphanTask()`, extended `ReleaseResult` with an additive optional `reason?: string`.
**what-considered:**
- coordinationTools.ts's ":208"/":320" anchors from the row's own note vs. live state — found the note's line numbers are stale (that file was split 457L→54L barrel + 6 per-tool files by a DIFFERENT, untracked-by-this-row sprint, `FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L`, 2026-08-09). Judged: NOT a spec-invalidating discovery — read both successor files (`taskReleaseTool.ts`, `taskForceReleaseOrphanTool.ts`) directly; both `JSON.stringify(result)` the store's return value unmodified, so the substantive "zero changes needed" claim holds true, only the row's cited line numbers are dead references to a now-deleted single-file shape. Documented the correction in system.md rather than silently trusting either the stale numbers or my own assumption.
- Where to place the release-refused test (b) fixture: reused the existing `insertLiveRow`+new local `ageHeartbeat` helper (idiom copied from `DWF-coordination-phase2.test.ts`) inside `coordinationStore.test.ts` rather than a new file — brief §6 explicitly said "extend existing suite if present".
**why-decision:** infrastructure importing a domain/services pure predicate is the correct DDD dependency direction (confirmed precedent: 10+ other infrastructure/fetchers/*.ts already do this) — the file's own stale doc-comment ("no domain imports") describes historical state, not an enforced fence (ESLint fence is explicitly Phase-2/not-yet-live per this zone's own flow doc).
**why-change:** none from the brief's exact code sketch — implemented verbatim. Only correction was documenting the coordinationTools.ts anchor drift (§ above), not a design change.
**verification:** RED-first (`publishedMarkerImmunity.test.ts` failed on missing module before the source file existed) → GREEN, 8/8 pass. `coordinationStore.test.ts` extended +4 cases (a/b/c per brief §6): 20/20 pass. All 20 test files referencing `coordinationStore`/`releaseTask`/`releaseOrphanTask` run together: 335/335 pass, 0 fail. `tsc --noEmit` clean. Full `bun test`: 15263 pass/40 skip/55 fail/48122 expect across 1272 files (488s) — grep-confirmed only 2 fail names touch coordination code at all (`task_release Zod schema ... S5/S6` inside `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts`), and that exact file reproduces 13/13 clean in isolation — load-induced flakiness, not a regression (matches today's documented pre-existing flaky-churn band, S85-S89 siblings 60/63/57/59/57 fail). Gate 2: `PORT=3099` clean boot `/health {toolCount:183,sessions:0}`, `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88 both match baseline. Dashboard circular-dep check: `/api/bctc-inspect` + `/dashboards/news-fetch/` both HTTP 200. `size-lint-justification.sh --check`: PASS, 0 unjustified offenders (`coordinationStore.ts` grew 1173L→1192L, well inside the 1365L upper bound — no helper-module factoring needed). Docs: `domain-model.md` (new Specialized-services row) + `system.md` (new Invariant 8) updated per doc-review flow. Commits: `47f987f50` (code+tests), `068a62d37` (docs).

### STEP dev-mcp-server-S91 · dev-mcp-server · 2026-08-12T17:33:23Z
**task-id:** FIX-CI-BUNTEST-1108-AGENT-WORK-LOG-STORE
**what-done:** Root-caused CI RED as a real-clock race in the test fixture, not a code regression: `purgeOldAgentWorkLogs keeps rows exactly at the boundary` computed `datetime('now','-30 days')` independently in the fixture INSERT and inside `purgeOldAgentWorkLogs`'s own DELETE query — a wall-clock second tick between the two statements flips `deleted` 0→1. Reproduced deterministically standalone (bun:sqlite script, 1.1s injected delay → deleted=1), then fixed by widening the boundary row to `-30 days +5 seconds` (still exercises "just inside window → kept", now immune to realistic scheduler jitter); re-ran the same repro against the new value → deleted=0.
**what-considered:**
- Bisect assuming a permanent regression — declined: bracketing commits 0b3d494d/b701e913/f954d591 are unrelated notebook/orch-state commits, matching the PO triage's deflake framing.
- Inject a mock clock into `purgeOldAgentWorkLogs` (production code) — declined: task scope is the test file only; the SUT's `datetime('now')` usage is correct (DEFAULT + filters both use SQLite's own format consistently — no ISO8601 strcompare bypass, cf. `feedback_sqlite_iso8601_datetime_strcompare_bypass`).
- Margin size — 5s chosen: real statement gap is microsecond-scale, so 5s eliminates the race with zero semantic loss to the "< not <=" assertion.
**why-decision:** empirical repro (not guesswork) proved the exact mechanism and confirmed the fix survives the same forced-delay condition that broke the original.
**why-change:** none from the dispatch brief — brief asked to determine flaky-vs-regression first; this is that determination (flaky) plus the test-scoped fix.
**verification:** isolated `bun test src/__tests__/1108-agent-work-log-store.test.ts`: 17/17 pass (41 expect). `bun tsc --noEmit` (after `bun install`, worktree had no node_modules): clean. Full non-isolated `bun test`: 15262 pass/56 fail/1 error/1272 files — pre-existing unrelated noise per `scripts/test-all.sh`'s documented singleton/env-leakage rationale, not gating evidence; the real CI runner `scripts/ci-per-file-isolation.sh` never invokes bare `bun test`. No production code touched, Gate 2b/2c/2d not triggered.

### STEP dev-mcp-server-S92 · dev-mcp-server · 2026-08-13T18:35:00Z
**task-id:** FACTORY-INFRA-split-agentSignalStore
**what-done:** Collapsed `_postSignalInner`'s 6-deep nested column-existence cascade (10 hand-written near-identical `INSERT OR IGNORE` variants, ~270L) into `detectSignalColumns(db)` (WeakMap-memoized 6-probe + a folded-in 7th `hasCreatedAtColumn` dedup-guard probe, `agentSignals/columnDetect.ts`) + `buildSignalInsert(flags, values)` (single dynamic builder, `agentSignals/insertBuilder.ts`). Split the 1774L file into 20 modules under `agentSignals/` by query seam (postSignal+dedupGuard+earningsConflictGuard, getSignals/getBroadcastSignals/getPriceAnomalySignals/causalRootGrouping, chainQueries+chainRowSerializer, recordOutcome/getSignalEffectiveness/cleanExpired, criticGate+criticGateTypes, shared types/helpers) — `agentSignalStore.ts` is now a 60L barrel re-exporting every pre-split name unchanged.
**what-considered:**
- Column/bind inclusion logic: independent per-flag checks vs. mirroring the original's exact nesting (context/critic columns only reachable when ALL ancestor flags true, not just their own flag) — chose mirroring: hand-derived all 10 reachable branches from the pre-refactor source line-by-line (branch names A-J), verified each one's column list + bind order against the original before writing `buildSignalInsert`, so it is provably byte-identical for every flag state, not just the realistic monotonic-migration-order ones.
- Module boundary for `PostSignalInput`/`SignalInsertValues`/`CausalRootGroup` etc. — split each into its own small `*Types.ts` sibling wherever combining with its owning function file would exceed 120L (`insertBuilder.ts` was 122L combined; split out `insertBuilderTypes.ts` to close under it), rather than shrinking comments to force-fit — precedent (`postSignalTypes.ts`, `criticGateTypes.ts`) already established the pattern.
- Task named exactly 5 module files (`postSignal,getSignals,chainQueries,effectiveness,criticGate`) but the file grew 1569L→1774L since the audit (new `getBroadcastSignals`/`getPriceAnomalySignals`/`getSignalsGroupedByCausalRoot`/`migrateUnknownStockCodes` landed after); split into the 5 named seams' natural siblings instead of force-cramming into exactly 5 files, since every module must independently satisfy the <=120L DoD line.
**why-decision:** the DoD's own explicit bar ("must reproduce the exact column/bind order of every branch, or existing callers break") makes byte-identical reproduction non-negotiable — mirroring nesting exactly, verified against hand-read pre-refactor source, is the only approach that proves this rather than assumes it from "realistic" input coverage alone.
**why-change:** module count (20, not 5) — organic scope growth since the 2026-06-15 audit wrote the task, not a plan deviation; DoD's <=120L-per-module bar is satisfied either way.
**verification:** RED-first is N/A (pure refactor, no new behavior) — wrote characterization/round-trip tests FIRST against the read (not-yet-edited) pre-refactor source, confirmed all 10 branches by hand-derivation, THEN refactored, THEN re-ran: `insertBuilder.test.ts` (pure-function, 10 branch cases, asserts exact SQL text + bind array) 10/10 pass; `columnDetect.test.ts` (memoization + isolation) 4/4 pass; `FACTORY-INFRA-split-agentSignalStore-roundtrip.test.ts` (drives real `postSignal()` against 10 distinct `:memory:` schema shapes, raw `SELECT *` readback) 11/11 pass, 189 expect. Full pre-existing agentSignal-adjacent suite (38 files: 242/1105/1862g/1786/1328c/tnb-critic-gate/SPIKE-BOOTSTRAP-BROADCAST/etc.) 402/402 pass, 0 fail — zero regression. `tsc --noEmit` clean. Gate 2b: `PORT=3099` clean boot `/health {toolCount:183}`. Gate 2c/2d: `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88, both match baseline. Dashboard circular-dep check: `/api/bctc-inspect` + `/dashboards/news-fetch/` both HTTP 200. Full `bun test`: 15288 pass/40 skip/55 fail/48291 expect across 1275 files (471.89s) — grep-confirmed all 55 fail names are pre-existing (VPS-push-log/foreign-flow/get_market_cap/task_heartbeat-Zod/insider-transactions/etc., zero overlap with agentSignalStore); the one signal-adjacent name (`1875c — record_signal_outcome dispatch regression guard`) reproduces 5/5 clean in isolation — matches today's documented flaky-churn band (S89-S91 siblings: 57/55/56 fail). `size-lint-justification.sh --check` PASS (`--update` regenerated `docs/data/size-lint-baseline.json`, pruning the now-obsolete `agentSignalStore.ts` 1754L entry — file dropped to 60L). Docs: `infrastructure.md` updated (FACTORY-INFRA-split-agentSignalStore module-map note, precedent-matched to the existing vnstockBridge/telegramCommands split notes). PRE-REBUILD live-DB baseline captured (`docker exec` read-only, `vn-market-intelligence-mcp-mcp-server-1`, still running pre-refactor code): 29-column full schema (branch-A shape, the only branch production ever exercises), 114 rows, latest id=10883 — snapshot saved for QA/ops post-rebuild diff. REBUILD_REQUIRED=true (rebuild is ops's job per this zone's `not_my_job` boundary, not self-executed) — byte-identity RAW-verify against the live named-volume DB is therefore a QA/ops follow-up gate, not closed by this cycle.

### CAP-REACHED · 2026-08-13T18:36:27Z
Byte-axis cap tripped post-S92 write (119L/600L line-cap fine, but 40939B > 36000B byte-cap = LINE_CAP*60). Continuation file: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md. New entries go there.
