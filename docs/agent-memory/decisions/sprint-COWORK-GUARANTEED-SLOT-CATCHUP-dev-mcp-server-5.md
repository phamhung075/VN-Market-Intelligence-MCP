# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · dev-mcp-server (continuation 5)

**Sprint goal:** see sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md header — this file is a byte-cap rollover continuation (dual-axis cap check, base file capped 2026-07-31, -2 capped 2026-08-01, -3 capped 2026-08-06, -4 capped 2026-08-09).
**Agent:** dev-mcp-server
**Started:** 2026-08-09T02:10:00Z

---

### STEP dev-mcp-server-S83 · dev-mcp-server · 2026-08-09T02:10:00Z
**task-id:** FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L
**what-done:** `coordinationTools.ts` was 457L (baseline=388L, upper=426L, 31L overage), CI-RED on size-lint (run 31285673936, head 39e1e161). Split all 6 registered tools (task_claim, task_heartbeat, task_release, task_list_held, task_force_release_orphan, get_week_period) verbatim into 6 new files under `interface/mcp/tools/system/coordination/` (54-119L each — all under the 120L size-lint threshold, so none needs a `size-justification` header). `coordinationTools.ts` is now a 54L thin entry point re-exporting `registerCoordinationTools(server)` unchanged. Dropped the dead `buildWeeklyPublishMarkerKey` import (unused in the file body, confirmed via grep before removal) and pruned the now-stale 388L `docs/data/size-lint-baseline.json` entry (surgical `jq del`, not a wholesale `--update` — a wholesale `--update` would have also silently rebaselined the separate, out-of-scope, already-failing `transport.ts` offender, which this row does not own).
**what-considered:**
- 1-2 grouped files (e.g. heartbeat+release together) — rejected: bundling would push each grouped file past 120L (own zod-schema-heavy tool descriptions are ~55-80L each), forcing a justification header per file anyway; one-file-per-tool keeps every file comfortably under threshold with zero headers needed.
- `--update` baseline bump — rejected outright per the row's own explicit mandate ("do NOT rebaseline silently") and CLAUDE.md's "reduce debt" directive; a real split was structurally available (each tool is already a self-contained `server.tool(...)` call with no cross-tool state except the `SERVER_SESSION_ID` const, which only `task_claim` uses).
- `size-justification` header on the untouched 457L file — rejected: a mechanically clean per-tool split existed (same precedent as the S82 `coordinationStore.ts` sibling row), so a header would paper over a cheap structural fix.
**why-decision:** Each of the 6 tools is already syntactically self-contained (own zod schema + own async handler, single store-function call) — a per-tool file split is the lowest-risk, zero-logic-change remedy and produces files well clear of the threshold with room to grow.
**why-change:** none — split chosen on its merits per the row's own split-OR-header remedy, same class of fix as the S82 sibling.
**verification:** `bash scripts/audits/size-lint-justification.sh --check` no longer lists `coordinationTools.ts` (sole remaining offender: pre-existing, untouched `transport.ts`, out of this row's scope). `bun tsc --noEmit` clean. `npx eslint` (fence config) clean on all 7 touched/new files. Targeted suite: `task-lock-coordination-tools.test.ts` + `1980-p1-final-required-flip.test.ts` + `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts` + `FIX-DIGEST-PREDICT-ISO-WEEK-DEDUP.test.ts` + `1982-quality-burndown-CHIJ.test.ts` = 96/96 pass, 0 fail (all 4 files that `import { registerCoordinationTools }` directly, plus the ISO-week dedup suite covering `get_week_period`'s domain dependency). Pushed commit `1653cea0aa56e43b4a84463a3dc2772bbde15705`; pre-push hook (`pnpm --filter vn-market check` / `tsc`) green, no `--no-verify`. Board → `review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`. CI-plane verification is a NEXT-TICK QA action (`gh run view <databaseId with headSha AFTER 1653cea0a> --json jobs -q '.jobs[]|select(.name=="size-lint")|.conclusion' == success`) — not observable from this session per the task's own `verification_gate: ci_green_on_subsequent_push`.
