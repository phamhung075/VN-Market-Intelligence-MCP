# Decision Journal — PO sign-off, SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD · po

**Scope:** Single review-lane sign-off, dispatched by the dev-team Review-Lane SECONDARY-Drain sweep (tick 2026-08-26T18:07Z). One row only. Plus one self-issued correction carried in from the prior PO tick.
**Agent:** po
**Started:** 2026-08-26T18:20:00Z
**task_id:** SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD

---

### STEP po-S1 · po · 2026-08-26T18:22:00Z
**task-id:** SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD
**what-done:** Re-derived the row's close conditions from the row + its `detail_ref` before reading anyone's recommendation, then probed the live serving DB independently.
**what-considered:**
- Accept `dev_mcp_server_20260825`'s DONE_VERIFIED recommendation on its stated evidence.
- Re-probe from scratch (CHOSEN).
**why-decision:** The row does not close on its ACs alone. Its own 2026-07-17 `close_caveat` installed a *no-false-green* rule: closing on "MAX(extracted_at) advanced + fresh PDFs land" is explicitly forbidden; the close must ALSO show the mass-terminalised `enrich_failed` backlog re-extracts. That is precisely the branch a recommendation is most likely to skip, so it had to be measured, not inherited.
**why-change:** No change from plan.

### STEP po-S2 · po · 2026-08-26T18:23:00Z
**task-id:** SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD
**what-done:** Probed `/app/data/market.db` readonly via `docker exec` on `vn-market-intelligence-mcp-mcp-server-1`; confirmed both halves of the close gate.
**what-considered:**
- Probe by the two `report_id`s the `close_caveat` names (SAB `1f6cbffe…`, VND `11d8b60f…`).
- Probe by `action_code` + period instead (CHOSEN, after the first returned zero).
**why-decision:** **Both named `report_id`s no longer exist in `financial_reports` at all.** A `bctc_layout_units` lookup keyed on them returns `count=0` — which reads exactly like "never re-extracted" but is **vacuous**: the shell rows themselves are gone, so the query cannot distinguish "not extracted" from "not asked". Re-probing by `action_code`+period gave the real answer: 18 of the 28 rows in the caveat's own named 2024 cluster (SAB/VND/VJC/DBC/VIX/SHB/BSR × Q1–Q4) are now `status=done`, SHB recovered all four quarters, queue-wide `done=220` vs `enrich_failed=39`. Producer alive: `bctc_layout_units` MAX `2026-08-26 14:53:11`, 1700 rows / 33 distinct reports in 7 days. Close predicate AND caveat both satisfied.
**why-change:** No change from plan. (Guards against `feedback_probe_aggregated_coarser_than_the_phenomenon` — a null from a key that no longer exists is blindness, not evidence.)

### STEP po-S3 · po · 2026-08-26T18:24:00Z
**task-id:** SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD
**what-done:** Verified AC-3 is not merely "done_verified on paper" — the shipped circuit-breaker is live code AND observed firing in production.
**what-considered:**
- Trust the archived `done_verified` record for `FIX-BCTC-RECONCILE-EMISSION-CIRCUIT-BREAKER`.
- Additionally prove it runs (CHOSEN).
**why-decision:** `DONE_VERIFIED ≠ deployed` is a standing failure class. Confirmed three ways: archived record present in `docs/data/orch/archive/2026-07.json`; code live at `apps/mcp-server/src/scheduler/financial-reports/bctcExtractReconcileJob.ts:576` with 4 assertions in its test file; and it **tripped in production** at `2026-08-26 06:05:03` (`telegram_reports` id 5185, "per-row BUGs suppressed, systemic dormancy detected", 3-row batch). The report-storm suppressor works.
**why-change:** No change from plan.

### STEP po-S4 · po · 2026-08-26T18:25:00Z
**task-id:** SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD
**what-done:** Falsified one claim in the 08-25 review note on this same row, and routed the live evidence to the sibling row that owns it rather than blocking the close or minting a duplicate.
**what-considered:**
- Treat the falsified claim as a close-blocker and send the row back for rework.
- Close, record the correction, hand the evidence to the owning sibling (CHOSEN).
**why-decision:** `dev_mcp_server_20260825` asserts "every post-fix `enrich_failed` termination lands at `reconcile_attempts` EXACTLY 8" and that >8 outliers "all have `last_attempt<=2026-08-22` … never reprocessed — benign". **FALSIFIED:** HUT 2025-Q2 is at `reconcile_attempts=11`, `last_attempt 2026-08-26 03:05:02`, and emitted a fresh per-row BUG at 03:05:05 reading "after 11 reconciliation passes (cap 8)" — an over-cap row re-attempted and re-terminalised *today*, two weeks after `FIX-BCTC-D3C-FOLLOW-UP-RESET-ATTEMPTS`. The 08-25 measurement was true when taken; the "benign / never reprocessed" *inference* is what failed. But cap enforcement is **not an AC of this SPIKE**, and `FIX-BCTC-VPSINGEST-REQUEUE-NO-RECONCILE-COUNTER-RESET` (backlog, P1, dev-mcp-server) names exactly the mechanism that yields 9/10/11 against a cap of 8. Rework would strand a SPIKE whose own deliverables are complete; a new row would duplicate a live one.
**why-change:** No change from plan.

### STEP po-S5 · po · 2026-08-26T18:28:00Z
**task-id:** SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD
**what-done:** Flipped `review[] REVIEW → done_verified[] DONE_VERIFIED` with a schema-valid `verification.raw_probe`, dropped `next_agent`, via `orch-apply.sh`.
**what-considered:**
- Status `DONE` (no probe needed).
- Status `DONE_VERIFIED` with a real `raw_probe` (CHOSEN).
**why-decision:** The row is not in `RC_VERIF_GRANDFATHERED_IDS`, so `checkVerificationGate` in `orchStateSchema.ts` hard-rejects `DONE_VERIFIED` without `verification.raw_probe{tool,args,live_value_observed,observed_at}` — and I had genuinely independent live values to put there, so downgrading to `DONE` would have been a weaker record than the evidence supports. Confirmed no `market.db-wal`/`-shm` existed at probe time, so the readonly open was not WAL-blinded (`feedback_integrity_helper_readonly_wal_blinded`). `.head` NOT written — it was already `idle`; head reset is the dispatcher's exclusive duty (INV-GATEWAY-1).
**why-change:** No change from plan.

### STEP po-S6 · po · 2026-08-26T18:26:00Z
**what-done:** Corrected PO's own false `blocked_by` ruling from the 18:02Z tick — see `triage-20260826T1737Z-po.md` §5b for the full text; only the row's `detail_ref` was re-pointed.
**what-considered:**
- Write the correction inline on `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS`.
- Put it in the cold store and re-point `detail_ref` (CHOSEN, forced).
**why-decision:** The inline attempt was **aborted by `orch-row-prose-ceiling-check`** (`live=11865B → candidate=13787B` vs ceiling 12000B; live file untouched). The guard's own instruction is to route new prose through `detail_ref`, and explicitly *not* to split the write to dodge the check — so the text went to the cold store whole. Convenient here: that row's `detail_ref` already pointed at `triage-20260826T1737Z-po.md#5`, the very section containing the error, so §5b sits directly beside it. Substance: `scripts/lib/devteam-eligibility.jq:232` unions `.depends_on + .depends + .blocked_by`; `blocked_by` is live to all four pickers. PO had conflated `blocks` (genuinely write-only-decorative) with `blocked_by`.
**why-change:** Differs from plan — I intended an inline row key; a guard I did not know applied to this row forced the cold-store route. Two extra facts fell out: (a) the key `po_ruling_20260826T1802Z` that §5 cites **was never written to orch-state at all** (`grep -c` → 0) — actions landed, rationale did not; (b) PO stated the *correct* `blocked_by` fact at 17:26Z in `release-20260826T1734Z-po.md` STEP po-S1 and then contradicted it at 18:02Z, so this was a same-day self-contradiction, not missing knowledge.
