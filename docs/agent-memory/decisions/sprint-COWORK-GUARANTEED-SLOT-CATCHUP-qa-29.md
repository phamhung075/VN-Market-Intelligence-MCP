# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa (continuation 29)

**Sprint goal:** COWORK-GUARANTEED-SLOT-CATCHUP
**Agent:** qa
**Started:** 2026-08-25T04:30:00Z (continuation — qa-28.md breached byte cap 46349/36000, rolled per decision-journal § Cap Check)

---

### STEP qa-S205 · qa · 2026-08-25T05:20:00Z
**task-id:** FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-MAINFLOW
**what-done:** Direct-Commit Verify of `94716bfe6` (main.md ancestor-confirmed, +90/-2, 1279->1367L exact match to size-justification claim). All 5 §5 MAINFLOW brief bullets present at source: ILC section at the Session-Gate anchor before SECONDARY-Drain (L935-1019), SECONDARY-Drain's intro corrected (L1023), new Reusable Scripts bullet (L1323), Invariants clause naming INCIDENT_CAP (L1339), regression coverage via the shared `devteam-dispatch-gate-satisfiability.sh` instrument (all 13 ILC assertions independently re-run and PASS). Found a real defect NOT disclosed by the row: L942 + L1339 both assert `wip_in_progress` "excludes" incident rows / INCIDENT_CAP "never competes" with BOUNDED-1/SLS/RLC/DRS for the shared WIP<=2 slot — false. Read `scripts/lib/devteam-eligibility.jq` at source: `wip_in_progress` (L115-118) has NO `claimed_by` filter; only `incident_wip_in_progress` (L158-162) does. Live-reproduced: a single synthetic in_progress row with `claimed_by:"dev-team (incident-lane consumer)"` scores `wip_in_progress:1` (not 0). Confirmed WIP/WIP2/WIP3/WIP4 (BOUNDED-1/SLS/RLC/DRS's own gates, main.md L622/658/746/805) all call the exact same unfiltered `wip_in_progress` — so 2 in-flight incident rows saturate the shared budget to 2/2 and block all four siblings, the opposite of the row's own claim. The jq file's own comment (L153-157) already documents this as a deliberate, scoped asymmetry from the SCRIPTS row (DONE_VERIFIED) — MAINFLOW's prose should have matched that, not repeated the architect brief's own (same, also-wrong) §4b framing verbatim.
**what-considered:**
- treat as out-of-scope follow-up, same as the row's own two disclosed SECONDARY-Drain deviations — rejected: those two are pre-existing defects in a NEIGHBOUR section this row never touched, properly flagged; this defect is IN the row's own new text (L942, L1339), the row's sole deliverable being prose — misdescribing the system it documents is this row's own correctness bar, not a neighbour's.
- whether the false claim is merely stylistic vs materially misleading — checked against the actual gate call sites (WIP/WIP2/WIP3/WIP4 all read the same unfiltered `wip_in_progress`): 2 incident rows genuinely can starve BOUNDED-1/SLS/RLC/DRS to zero, which is exactly the failure mode this whole architecture effort (and the row's own "must not become a 4th priority tier" framing) exists to prevent — material, not cosmetic.
- fix scope: only main.md prose needs correction (this row's own `files:[]`); no `scripts/lib/devteam-eligibility.jq` change required or in scope (that file belongs to the already-DONE_VERIFIED SCRIPTS row).
**why-decision:** AC-1/2/3/5 (structure, placement, cross-refs, shared regression instrument) all independently verified true at source. The false budget-independence claim is a genuine, file:line-backed, live-reproduced defect in the row's own shipped content, not a pre-flagged/out-of-scope item — `vc-changes`.
**why-change:** `vc-changes`, not `vc-approved`. Routed to row's own `owner` (`agent-father`) per Direct-Commit Verify convention (no branch/fixer). Required fix: correct L942/L1339 to state incident rows ARE counted by `wip_in_progress` (conservative-direction, deliberate per the jq's own note) rather than claiming full exclusion/no-competition.

### STEP qa-S206 · qa · 2026-08-25T05:20:00Z
**task-id:** TASK-DEV-MCP-SIGNAL-TYPE-REGISTRY
**what-done:** 2nd-pass Direct-Commit Verify of rework `796a2fc58` (exactly the 2 claimed files, main-ancestor confirmed). Diff shows `mint_routing_gap_row()` now literal `owner:"agent-father", next_agent:"agent-father", zone:"cross-service/", priority:"P1"` — my prior CHANGES_REQUESTED (owner=po/zone=docs/agents/po/flow//no next_agent, undispatchable) is closed. Re-ran `guard-signal-type-coverage.test.sh` fresh: 31/32 (sole fail TEST10, live-data smoke, already-accepted). Confirmed AC-5 (`orchStateSchema.ts` diff=0). Spot-checked minted rows live (not fixture): `signal-feedback` (dev-mcp-server's own pass) + `flow-defect`/`telemetry` (minted by MY OWN test execution moments ago, against real live data) all carry the 4 required fields — strongest possible evidence since these are freshly-detected types the corrected template had never seen before. Dedup held: 0 duplicates across 9 live unrouted types on repeat run.
**what-considered:**
- router's discrepancy (a): is verifying the mint path against live orch-state.json acceptable, given it mutates production data on every invocation? — Read `--check`'s own header (line 79-80, "same behaviour, --check optional" — not a real dry-run) and found this exact defect is ALREADY its own tracked row, `FIX-GUARD-SIGNAL-TYPE-COVERAGE-CHECK-FLAG-MISLEADING-NOT-DRYRUN` (po, 2026-08-23, owner=developer, BACKLOG) — pre-existing, out of THIS task's AC, not something the rework introduced or was required to fix. Live-verifying against real data is in fact mandated by this task's own `acceptance_notes` item 1. Ruled acceptable.
- router's discrepancy (b): do the 7 (now 9, after my own run) new P1 backlog rows need separate disposition? — All are well-formed/dispatchable (next_agent=agent-father) and represent genuinely-detected unrouted types — this is the AC (self-filing fallback) working exactly as designed, not an artifact needing cleanup.
- the 5 pre-existing stale zone=docs/agents/po/flow/ rows dev-mcp-server left untouched — read the umbrella row `FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS`'s own status_note (po, 2026-08-24T18:29:19Z) at source: it explicitly names these rows (by type) as umbrella members for ONE coordinated agent-father hop, still BACKLOG — confirms leaving them alone was correct scope discipline, not an omission.
**why-decision:** Every AC independently re-verified against artifacts (diff, test re-run, live board state), not narrative. The prior blocking defect is objectively closed and the two flagged discrepancies both resolve in the rework's favor on inspection.
**why-change:** `vc-approved` (2nd pass). Terminal shape written directly: `.task_board.qa[] -> .task_board.done_verified[]`, `status: DONE_VERIFIED`, `qa_verified_at` + `verification.raw_probe` stamped, `next_agent` deleted (schema optional-not-nullable) — single `orch-apply.sh` write, self-verify passed, committed `f086451f9` (docs/data/orch/orch-state.json only, explicit pathspec).

### STEP qa-S207 · qa · 2026-08-25T08:30:00Z
**task-id:** FACTORY-APP-split-assembleEveningSummary
**what-done:** Direct-Commit Verify of `ad0ff8268` (main-ancestor confirmed, 14 new `eveningSummary/` step modules + thin sequencer, `usecases.md` doc update). Adjudicated the flagged hangSeng question: (1) omission CONFIRMED present — `queryGlobalSnapshotStep.ts`'s truthiness guard checks `vix/dxy/sp500 !== 0` only, never `hang_seng`; (2) DELIBERATE not oversight — diffed against pre-split `assembleEveningSummary.ts@ad0ff8268^` and the guard is byte-identical (same 3-field check, no `hang_seng`), so the split preserved existing behavior verbatim; the commit message, in-file doc comment, AND dev-mcp-server's own decision-journal S-entry all independently disclose it as "a pre-existing latent inconsistency vs briefing's version, left unfixed per refactor-only scope" — a recorded, not accidental, decision; (3) tracking-row check: grepped `orch-state.json` + `backlog-detail.json` + `archive/` for `hangseng`/`hang_seng` — ZERO board rows. The underlying defect is disclosed in prose (commit + journal) but has NO dispatchable tracking row — this is the finding.
**what-considered:**
- fold the hangSeng defect into this row's own disposition (block or asterisk the approval) — rejected per dispatch brief's own instruction: it's a legitimate behavior-preserving refactor outcome, not this row's defect to own; folding it in would conflate "refactor is sound" with "underlying bug is tracked", which is exactly what let it go untracked in the first place.
- silently accept the prose disclosure as sufficient — rejected: prose in a commit message/journal is not dispatchable, has no owner/priority, and is exactly the "disappears" failure mode the brief named. A signal_queue row to po is the minimum durable pointer.
**why-decision:** Ran full Direct-Commit Verify: targeted 5-file evening-summary test bundle 35/35 pass, `bun tsc --noEmit` 0 errors, `mock-guard.sh` PASS on all 14 touched production files, DDD scan clean (no domain/ touched, application→infrastructure imports are permitted, not application→domain violations), security scan clean. Not OOM-class (no crash/memory claim in scope). All Direct-Commit Verify checks green → `vc-approved` on refactor grounds.
**why-change:** `vc-approved`, refactor signed off standalone. hangSeng defect surfaced SEPARATELY via `signal_queue` row (`from:qa`, `to:po`, type `preserved_bug_no_tracking_row`) rather than folded into this row's `status_note` — per dispatch brief's explicit instruction not to fold silently.

### STEP qa-S208 · qa · 2026-08-25T08:30:00Z
**task-id:** FIX-DEVTEAM-ELIGIBILITY-EFFECTIVE-NEXTAGENT-DETAIL-FIRST-NO-RECENCY-COMPARE
**what-done:** Direct-Commit Verify `6b94d0832` (ancestor-confirmed, sole claimed file `scripts/lib/devteam-eligibility.jq` touched). Ran ad-hoc jq fixtures directly against `effective_next_agent`/`effective_owner` (never trusting the row's own prose): board-newer, detail-newer, board-only, detail-only, legacy-no-timestamp, and BOTH unparseable-timestamp shapes all match AC-1 exactly. Explicitly tested the tie case the dispatcher flagged: identical `updated_at` on both sides -> detail wins on BOTH functions (unchanged tie-break, matches "detail-first retained ONLY as tie-break" claim). `effective_owner` byte-identical treatment confirmed (AC-3).
**what-considered:**
- AC-4 no-regression claim ("114 PASS / ONE failing assertion") — re-ran `devteam-dispatch-gate-satisfiability.sh` fresh: got 113 PASS / 2 failing (SLS gate, already-diagnosed pre-existing, PLUS a NEW-to-me "Review-Lane QA-Drain gate SATISFIABLE" fail). Did not accept prose at face value — A/B'd it: extracted the pre-fix jq lib via `git show 6b94d0832~1:...` into scratchpad (no git stash), computed `effective_next_agent` for every live `review[]` row under OLD vs NEW code on the SAME frozen board snapshot — both give zero qa-routed rows. Root cause: the fixture pads `review[]` with synthetic `next_agent:"qa"` rows only if live count `<20`; live review[]=25 (real rows, no padding) and the one row that used to mask this (`FIX-AUDITOR-TIER1-PROBE-SCORES...`) already got drained to qa[] by this very fix moments earlier this tick. Confirmed board-content-coupled, not a code regression — same class as the already-diagnosed SLS failure.
- whether to block sign-off on the discrepancy between developer's stated count and mine — rejected: I verified the actual mechanism (A/B replay), which is stronger evidence than either count; both real failures are provably pre-existing/live-board-shape artifacts of this audit instrument's own padding threshold, not something this commit introduced.
**why-decision:** All AC-1/2/3 confirmed via direct execution (not prose). AC-4's "no regression" holds under a stronger test (A/B on frozen fixture) than the row's own git-stash-based claim, despite the stale PASS/FAIL count. `mock-guard.sh` PASS (no production-source surface for `.jq`), no TS/tsc surface touched (grepped — only comment mentions in unrelated `.test.ts` files), no OOM-class scope.
**why-change:** `vc-approved`. Flagged the stale AC-4 count + the audit script's own `<20` padding-threshold brittleness in status_note for the record; non-blocking.

### STEP qa-S209 · qa · 2026-08-25T13:25:00Z
**task-id:** FU-RAG-DEPLOY-MEMORY
**what-done:** Re-derived D1-D5 on the 08-23T14:36:47Z->08-24T17:36:53Z instrumented window myself (python regression, peak/trough scan) instead of trusting the prior probe fields. D2/D4 PASS (identity unbroken 08-15->08-24, zero rag-service compose touch in-window). D3 mixed: literal final-reading (76.53%) + final-12h slope (-0.0278pp/min) clear the bar, but a 95.62% peak sits inside that same final-12h segment — flagged unresolved under this incident's own precedent (po_RULING_20260815T1002Z used sample-proportion/peak, not a bare endpoint). D1 is the real blocker: last confirmed rag-service dmesg pull covers only to 08-24T07:18:58Z (~10h18m short of window close); the fleet-wide Docker VM crash (~08-25T05:31-06:45Z) reset the kernel ring buffer before anyone re-queried it — buffer floor now 08-25T06:32:52Z, non-vacuous, nothing earlier recoverable.
**what-considered:**
- certify on the literal D3 text (final-reading+slope only) since it numerically passes — rejected: ignores the 95.62% peak inside the same segment and would repeat exactly the single-endpoint gaming this bar was written to close.
- treat the closed window as still usable for D1 by inference ("probably fine, no compose changes") — rejected: D1 requires a positive kernel-log absence-of-OOM read, not an absence of config changes; the evidence itself is gone, not merely unlikely-to-differ.
**why-decision:** A window can be D2/D4-valid on its own bounds yet evidentially dead for D1 once its only authoritative source rotates out from under it unread. Neither vc-approved (D1/D3 unmet) nor vc-changes (zero code/config defect in evidence) applies.
**why-change:** HELD, no lane move. Opened a fresh instrumented window on the current post-crash container (16c59b5e929f, StartedAt 2026-08-25T06:33:04Z) at 13:18:55Z, 27h, so the next cycle has real D3/D5 data and a live D1 chance before the buffer rolls again.

### STEP qa-S210 · qa · 2026-08-25T13:25:00Z
**task-id:** FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED
**what-done:** Re-confirmed commit 82216e291 unchanged/still sound (no new diff since prior review). This row's own blocker (FU-RAG-DEPLOY-MEMORY) remains unresolved this cycle for the reasons in qa-S209 — D1 evidence lost to the fleet-wide VM crash before anyone checked it, D3 mixed.
**what-considered:**
- close this row independently of its blocker since the code itself has no new defect — rejected: `blocked_by` is load-bearing per po's own ruling (po_WINDOW_VOIDED_20260814T0927Z), a code-sound verdict was already recorded in a prior cycle and is not the open question.
**why-decision:** No new evidence changes this row's disposition from prior cycles; the blocker chain is unchanged in substance (still unresolved, now for a different proximate reason than before).
**why-change:** HELD, no lane move, blocked_by unchanged. Pointed at qa-S209/FU-RAG-DEPLOY-MEMORY for full detail rather than duplicating it.

### STEP qa-S211 · qa · 2026-08-25T13:30:00Z
**task-id:** FIX-ORPHAN-FR4-FR5-FLOW-DEVTEAM-ADOPTION-GUARD
**what-done:** Direct-Commit Verify of `3212e2739` (main-ancestor confirmed, all claimed files match exactly). Structure/placement sound (guard before N_MAX+adopt-claim, board-flip lane-aware, cold-archive fallback+supervised-skip live-verified). Found subtask 4's own acceptance line ("verify released:1 against a real NULL-owner orphan-signal row") was never executed — Implementation Record shows unit tests + read-only jq lookups only. Attempted to run it myself; this session's tool grant is Read/Edit/Write/Bash only (no mcp__gateway__call_tool present in the tool schema) — structurally cannot invoke task_release, same class as this task-family's own Task-5 record (2026-08-09). Also grepped the whole board + archive for the "separate row" the record says will retrofit dispatch-claim/SKILL.md's FR-3 guard — zero hits, it was never filed.
**what-considered:**
- approve on structure/tests alone and treat the released:1 probe as a follow-up nit — rejected: it is the row's own stated acceptance criterion for subtask 4, not an optional nice-to-have, and per this fleet's own standing lesson a QA session must not sign off an unmet AC.
- run the probe by calling coordinationStore's releaseTask() directly, bypassing the MCP layer — rejected: that would prove the internal function works, not that the exposed tool interface the AC actually names does; also a live-state-mutating action outside a session that can safely observe/roll back MCP-layer effects.
**why-decision:** Two concrete, board-verifiable gaps against the row's own stated commitments (unrun AC + never-filed follow-up), independent of the router's own flags on this row (which I re-derived rather than took on trust).
**why-change:** `vc-changes`, routed to owner `pm` (row has no branch/fixer). Also flagged (non-blocking) that commit 3212e2739 omits the required Task:/AC: trailers per commit-convention.md.

### STEP qa-S212 · qa · 2026-08-25T13:40:00Z
**task-id:** FACTORY-APP-split-pollNews
**what-done:** Direct-Commit Verify of 3 stage commits (72bcaf940/1a7fca8b2/82029f65b, all main-ancestor). Read pollNews.ts in full: genuine thin orchestrator (269L, no undisclosed inline logic). tsc/mock-guard/size-lint/DDD/security all clean. Ran the full 61-file/562-test pollNews-touching set myself (broader than the claimed 26): 561 pass/1 fail, the 1 a documented chromium-binary-absent contention-timeout flake — reproduced the mechanism at source (resolveFetchers.ts:71/82 CI-gated no-op fetcher) and confirmed 6/6 pass in isolation both with and without CI. Closed the one DoD bullet nobody had addressed across any prior cycle: rebuilt mcp-server (single-service, peers unaffected) and RAW-verified against the real named-volume DB — a genuine live VPS-push poll cycle post-rebuild inserted 3 real rows, independently confirmed present in `rag_analyses`.
**what-considered:**
- hold for a stage-4 header-trim since pollNews.ts (269L) is still over the literal "<=120L" figure — rejected: all 5 named approach-field targets are done, remaining mass is header/imports/state-box/pure-delegation not business logic, and the task's own DoD sanctions staged delivery; a further trim would only cut line count, not complexity.
- treat the router's "1345a is non-reproducible, real trigger is CI env var" claim as sufficient without re-deriving it — rejected: verified independently (ran it in the 562-test batch, then isolated with/without CI) rather than taking either the developer's or the router's word for it.
**why-decision:** Every AC-bearing claim independently re-verified at source, including the one nobody had closed (real DB rebuild verify), not merely the ones already checked in prior cycles.
**why-change:** `vc-approved`, qa[] -> done_verified[], RC-VERIF raw_probe recorded in the same write.

### STEP qa-S213 · qa · 2026-08-25T15:52Z
**task-id:** FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS
**what-done:** Direct-commit verify of `e6adc6dfe` — AC-4 PASS (re-proved live end-to-end, not from prose), AC-1/AC-2 PASS-with-scope-caveat, AC-3 refused as unverifiable at 3.3% of its own 24h window; landed qa[] -> review[], next_agent=po.
**what-considered:**
- Close AC-3 on "zero fires since the commit" — REJECTED, quantified as worthless: median inter-fire gap over the last 40 fires is 0.68h and 48% of historical gaps already exceed the 47min elapsed, so the observed silence is the MODAL outcome under the unfixed regime.
- Absorb the two flagged out-of-scope observations (system-auditor execution-fidelity, verify-fleet-commit-pathspec.sh RED) into this row's verdict — REJECTED, both belong to other rows; recorded in verify_note as separately-flagged, not scored here.
- Trust the developer's "verified live in a sandboxed repo" AC-4 claim — REJECTED, rebuilt the probe myself.
**why-decision:** AC-4's real risk was a fix that lives in `scripts/git-hooks/pre-commit` but never reaches the LIVE hook — confirmed `.git/hooks/pre-commit` is a symlink to it, then fired a real BARE commit in a fresh sandbox and read `caller_chain=` back off all three surfaces (banner, `.git/sweep-guard.log`, signal payload), naming both the invoking command line and `claude -n detect-loop`. Re-ran `pre-commit.test.sh`: 15/15.
**why-change:** Terminal shape is `vc-changes` (qa[] -> review[], redispatch_count 1) NOT because the shipped work is defective — it is sound — but because AC-3 is a time-gated AC with 23h13m left. Deviation from the canonical actuator: the row's prose sat at 11997B against a 12000B ceiling, so the whole verdict went into `verify_note` (a STRUCTURAL field, excluded from `proseBytes`) and `qa_changes_requested_at` was dropped; deleting the now-stale `drain_source_lane` made prose SHRINK to 11989B. Ceiling-checked as a candidate before applying.
**AC-3 forward risk recorded on the row:** the fixed class (cowork-tick) is 4 of 59 fires since 08-22 (6.8%); 93.2% (system-auditor 27.1%, mega-sweep 1.7%, misc 64.4%) is untouched, so AC-3 as literally worded is unlikely to pass on this fix alone — earliest closable 2026-08-26T14:59:30Z, PO ruling needed on scope.

### STEP qa-S214 · qa · 2026-08-25T15:54Z
**task-id:** FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY
**what-done:** Direct-commit verify of `183e1ad8f` — AC-1/2/3/5/7 PASS (controls re-run myself on scratch fixtures), AC-4 and AC-6 refused; landed qa[] -> review[], next_agent=agent-father, redispatch_count 1.
**what-considered:**
- Accept the review_note's "readback widened, AC-7 fixed" as covering the row — REJECTED: the commit touches only the readback + spawn text; AC-6 has literally zero implementation (one `log` statement in the whole block, the peer-collision SKIP) and the commit message never claims it.
- Score AC-4 green off the 4 review[]-lane stamps already on the board — REJECTED as a laundering read: all 4 predate the fix (newest 08-25T04:16:23Z vs fix landed 12:54:37Z), so they prove the review[] half was never broken, not that this fix works.
- Score AC-4 "not yet elapsed, defer" — REJECTED as too generous: a post-fix tick DID traverse this block (15:37Z, commit 094c201a7 reached the head-decoupled QA-Drain, which is strictly downstream of SECONDARY-Drain in the ILC->SECONDARY->QA-Drain->Step-1 chain) with 20 eligible union candidates and left no stamp and no trace. That is a live negative, not an unelapsed window.
- Route back to `.owner` per the flow's own `($t.owner // $t.owner_agent // "po")` actuator — REJECTED: `.owner` is `qa` here, so the canonical chain emits a self-loop to an agent forbidden from editing flow docs. Used `agent-father` (implementer + docs/agents/*/flow owner) and said so on the row.
**why-decision:** The two controls that actually discriminate the fix were runnable and I ran them (ORCH_APPLY_LIVE_FILE_OVERRIDE, live file untouched): the same fixture yields `_lane=done` non-empty under the new readback and EMPTY under the old one, which is the whole defect — but a discriminating unit control is not the live-drain evidence AC-4 demands, and AC-6 is a deliverable that simply is not in the diff.
**why-change:** Also flagged for PO, not scored here: AC-4 is mis-scoped (its literal review[]-row bar is unreachable while the oldest union candidate is done[]-origin FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER, 2026-08-23T13:41:02Z, which the script re-picks every tick until that row moves) — re-scope to "any-origin post-fix stamp WITH matching dispatch". `scripts/audits/devteam-dispatch-gate-satisfiability.sh` is 113 PASS / 2 FAIL at HEAD, both live-board-state artifacts and provably unrelated (the script never reads main.md).

### STEP qa-S215 · qa · 2026-08-25T16:12Z
**task-id:** FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID
**what-done:** Direct-commit verify of `98968ed8b`+`a3a9491b3` — AC-1/3/4/5/6 PASS, AC-7 PASS on its sound half, AC-2 NOT IMPLEMENTED on a premise I verified FALSE at source, AC-8 met by live state; landed qa[] -> done_verified[] with RC-VERIF raw_probe, committed `86277d7d9`.
**what-considered:**
- Score AC-2 red and route back to review[] — REJECTED: AC-2's own premise ("every signal_queue write passes through orch-apply.sh") is false at `orchStateStore.ts:217-218`, and `SIGNAL_WRITER_ID="narrative-truth-gate"` (`narrativeContradictionSignalWriter.ts:56`) is a SECOND sanctioned producer, so the demanded check would have missed this leak AND rejected genuine rows. Re-filing would guarantee another round-trip on a provably wrong AC.
- Accept the developer's refutation from `verify_note` prose — REJECTED, rebuilt every load-bearing claim myself: causal repro (unstub T5, exactly 6 `NTG-GUARD-REFUSED-TEST-HARNESS-LIVE-WRITE`, live sha byte-identical), 8-row replay at per-row reconstructed wall clocks, `jq -f` on the shipped bash dedup program, python-vs-TS summary byte-diff.
- Trust "failing FILE SET pre-existing and disjoint" — REJECTED, proved disjointness structurally: all 17 failing files carry zero references to `orchStateStore|appendSignalQueueRow|narrativeContradiction|narrativeTruthGate|signal_queue`.
**why-decision:** The 42-row / 110-row populations the ACs were written against no longer exist; the surviving real population is 8 rows (6 leaked + 2 genuine), and the replay over ALL of them gave 6 quarantined / 2 accepted / 0 false rejects. That is the negative control `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist` demands, and it is what moved this from "trust the note" to DONE_VERIFIED.
**why-change:** AC-7's second clause ("reject a ts second-identical to an existing row") is CONTRAINDICATED, not merely unimplemented — `runNarrativeTruthGate.ts:110` stamps every finding in one gate run from a single `now`, so the literal check would reject legitimate multi-finding batches; recorded on the row so no future redispatch re-demands it.
**Two forward flags on the row for PO, deliberately NOT scored here:** (1) the residual orch-apply shape-not-provenance hole is real but its only evidence base is now refuted — needs a NEW row with a correct premise, not a re-hang on this one; (2) `collapseOrPrepend` bumps `.occurrences` in place without resetting `.status`, so a same-day re-emission of an already-READ finding never resurfaces as NEW (bounded to one calendar day by `dedup_key`'s `{cycle}`, and it is exactly what AC-6 mandates).

### STEP qa-S216 · qa · 2026-08-25T17:35:00Z
**task-id:** FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS
**what-done:** Direct-Commit Verify on e9144ea75. Rebuilt `pdf-extractor:latest` fresh from clean HEAD first (grepped the image for `RESCUE FIRED` + `_ink_coverage` to confirm the binary actually carries the instrumentation — the dev's own notebook flagged a prior stale-image miss). Reproduced AC-2 two ways (live `market.db` readonly read + my own fresh bench). Reproduced AC-3/AC-4 on FPT via my own `ocr-backend-bench.sh` run: 29/30 units byte-identical, rescue 1x page9, cgroup peak 50.2%/0 hard-limits. Then ran the router's mandated "highest-value check": `ocr-confidence-probe.sh` + a tesseract-vie/auto bench pair against a SECOND real document (DBC_2025_Q4.pdf, unrelated issuer, same corpus).
**what-considered:**
- Trust FPT-only evidence and approve — rejected: router's own dispatch explicitly demanded the second-document check as the deciding test, and PO independently minted `PROBE-PDFX-OCR-CONFIDENCE-SECOND-DOCUMENT-MARGIN` as a hard gate on the same question (I read `.head.next_action` citing it as open gate G2, unprompted — strong independent corroboration this was the right check).
- Treat low-ink-coverage regions as ambiguous without reading their actual OCR text — rejected: classifying "legitimate vs broken" by the score itself would be circular (the SPIKE row explicitly forbids this too); I read every flagged region's raw tesseract text and confirmed 6/6 are coherent, correctly-parsed VND figures, not misses.
**why-decision:** On DBC_2025_Q4.pdf the negative control collapses: rescue fires on 6/13 (46%) table regions, all confirmed legitimate reads, vs 1/30 (3.3%) on FPT. Ink-coverage floor for a confirmed-legitimate region measured as low as 0.329 — under the 0.5 gate AND under the FPT-derived "0.674 lowest-legit" floor the whole design argument rests on. Worse, the auto-mode container pinned at EXACTLY the 2.5GiB cgroup cap (peak==max, 494 memory.max events, 0 on FPT) — an undisclosed memory-safety regression, not just a diacritic-quality one. This independently reproduces `PROBE-PDFX-OCR-CONFIDENCE-SECOND-DOCUMENT-MARGIN`'s own AC-2 bar failing. CHANGES_REQUESTED, routed `review[]->in_progress[]` (this board's dispatcher never used a distinct `qa[]` lane for this row — it moved `in_progress[]->review[]` directly — so I mirrored that, not the flow-doc's `qa[]`-sourced template verbatim), `next_agent=dev-pdf-extractor`, `redispatch_count:1`, self-verified by re-read.
**why-change:** AC-2/AC-3/AC-4 as literally scoped to FPT Q4 2025 all independently PASS — this is not a revert, it is "not proven safe past n=1". Also flagged (non-blocking): the commit's claimed +3.8% table-phase overhead did not reproduce (I measured +54.9% on the same script) — magnitude discrepancy only, cap/headroom still holds on FPT either way.

### STEP qa-S218 · qa · 2026-08-25T20:35:00Z
**task-id:** FIX-NOTEBOOKAUTOPRUNE-GREPC-DOUBLE-EMIT-WRITES-MALFORMED-SIGNAL-JSON
**note:** renumbered S217->S218 post-write — a concurrent QA session's S217 (FIX-SWEEPGUARD..., committed `07074c440`) landed in the shared journal between my last read and this Edit call, producing a same-number collision at the git-tip; this entry is the later one by commit order.
**what-done:** Direct-Commit Verify. Fix landed incidentally under sibling commit `73d4ce109` (task FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE). Confirmed ancestry + all 5 sites touched via `git show --stat`. Rebuilt a pre-fix fixture from parent commit `091c1242318` (script+lib copied to scratch, live tree untouched, sha256-verified) and ran the shipped T15 regression against it: FAILS pre-fix (malformed JSON emitted, matches live victim signature), PASSES post-fix (valid JSON, sentinel_section_count=0) — independent must-fail-before/pass-after, not trusted from commit prose. Full suite 15/15 pass on live tree. Live inbox rescan: 51/51 `docs/signals/*.json` valid JSON (0 invalid), and the 4 specific victim files PO named are now sitting in `docs/signals/processed/` with valid `sentinel_section_count:0` — proof they cleared `isDrainableShape()` and actually drained.
**what-considered:**
- Trust PO's line-number citations and commit SHA as-is — REJECTED, re-derived independently via `git log --oneline --all -- <file>` (73d4ce109 is the only/most-recent touching commit) and re-read the diff myself rather than accepting the prose.
- Score AC-2 (regression test) FAIL because it doesn't literally drive all 5 call sites individually — CONSIDERED but not scored as a gap: T15 exercises the NON_SENTINEL/SENTINEL_SECTION_COUNT site end-to-end with the exact JSON-validity assertion AC-2 demands (not just "var==0"), and the other 4 sites got the identical textual fix verified structurally; no fixture proves those 4 independently emit a signal at all in the zero-count path (SECTION_COUNT/TIE_COUNT feed internal control-flow, not always a JSON emit), so demanding 5 separate JSON-emitting harnesses would over-fit the AC text beyond what the code path supports. Noted as PARTIAL, not blocking.
**why-decision:** Every load-bearing artifact check passed on direct re-execution (ancestry, diff content, isolated fixture repro, live inbox rescan, drain confirmation) — this is the "meaningful AC" the dispatch asked for (drain's own predicate accepts a real emission), proven by the 4 named victims actually reaching `processed/`, not by eyeballing JSON.
**why-change:** No change from plan — verify-committed path, `vc-approved` with AC-2 noted PARTIAL in status_note (textual fix confirmed at all 5 sites, full 5-site JSON-emission harness not built).

### STEP qa-S217 · qa · 2026-08-25T20:30:00Z
**task-id:** FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS
**what-done:** Direct-Commit Verify re-run on e6adc6dfe. AC-1/AC-2 confirmed present+committed (telemetry.md Step 6.2 + main.md dispatch table). AC-4 independently re-executed live in a fresh sandbox repo (not trusted from prose): caller_chain appeared on all 3 surfaces (stderr, `.git/sweep-guard.log`, `docs/signals/` payload), correctly named both the invoking command line and top-level `claude -n <family>` identity; re-ran `pre-commit.test.sh` myself, 15/15 PASS. Did NOT adjudicate AC-3.
**what-considered:**
- Adjudicate AC-3 now using the favorable early data (0 cowork-tick fires + 3+ pathspec-scoped tick commits touching only their own 2 files, confirmed via `git show --stat`) — REJECTED: the row's own PO ruling makes AC-3's 24h window/`qa_not_before` NON-WAIVABLE precisely to prevent certifying on an unsettled window (`feedback_ac3_durability_certified_on_window_that_ended_before_metric_settled`). Only 5.4h/24h (22.6%) elapsed at verify time, 18.6h remaining before `qa_not_before=2026-08-26T14:59:30Z`.
- Fail the row for an incomplete AC-3 — REJECTED: the row itself was mis-dispatched (Review-Lane QA-Drain claimed it at 2026-08-25T20:21:26Z, before its own `qa_not_before`), and its own routing note is explicit: an early pick "MUST read qa_not_before first and return HOLD without consuming a redispatch."
**why-decision:** HOLD, not PASS/FAIL — the row's own embedded gate governs over the generic dispatch instruction to "re-run the AC yourself" when the AC is explicitly time-gated and the gate has not elapsed. Did not write orch-state.json (dispatcher's explicit instruction) or touch `redispatch_count`.
**why-change:** No change from the row's own PO ruling; this cycle only adds independent re-verification evidence for AC-1/2/4 and confirms AC-3's early trend is favorable but not yet certifiable.

### STEP qa-S219 · qa · 2026-08-25T20:40:00Z
**task-id:** FIX-ORCHAPPLY-MKTEMP-SUFFIX-DEFEATS-RANDOMIZATION-BSD-CONCURRENT-WRITER-COLLISION
**what-done:** Direct-Commit Verify on 5a18d8726 (+b91a9aadc notebook). Independently reproduced BEFORE (2nd `mktemp ./.orch-apply-XXXXXXXX.json` fails `mkstemp failed: File exists`, literal unexpanded name on disk — real Darwin/BSD mktemp, confirmed `uname -a`) and AFTER (10x sequential + 6x truly concurrent full `orch-apply.sh` runs against an `ORCH_APPLY_LIVE_FILE_OVERRIDE` fixture: zero `mkstemp`/`File exists`, only expected CAS aborts on stale baselines, zero leftover temp files). Ran `orch-apply-wrapper-tests.sh` 3x: runs 1-2 showed 28 then 13 FAILs, ALL exclusively the "REAL live file CHANGED" meta-check — root-caused via `git log`/`git diff` to a live peer session's real `done_verified` lane-move landing mid-run (commit `d8530380b` / working-tree write at 20:26:5xZ), not this fix; zero non-meta logic failures in any run. Run 3 landed in a quiet window: clean 109/109, live orch-state.json sha256 unchanged throughout. CAS guard/atomic-rename/cleanup trap logic confirmed byte-identical pre/post diff except the TMP-creation line.
**what-considered:**
- Approve on the router's own verification note (AC-1/2/3 all independently confirmed correct by me too) — REJECTED as insufficient: the row's own AC-4 and AC-5 are separate, explicit, checkable bars the router's note never actually addresses.
- AC-4 ("regression test reproduces BEFORE/AFTER ... state explicitly how the test is made platform-honest"): diff confirms ONLY `scripts/orch-apply.sh` changed (34+/1-, 1 file) — `orch-apply-wrapper-tests.sh` untouched, and grep confirms zero mktemp/BSD/concurrent-collision assertions anywhere in it. The 109/109 cited is the PRE-EXISTING suite (CAS-mtime-mismatch tests, unrelated mechanism) — the developer's own notebook admits the BEFORE/AFTER proof was ad hoc manual session execution, never persisted as code. Scored AC-4 FAIL: nothing in the repo would catch a regression of this exact defect.
- AC-5 ("trap cleanup EXIT ... addressed at least by a log line naming the temp path on the failure branch"): `cleanup()` is byte-identical to pre-fix — still bare `rm -f "${TMP}"`, zero `printf`/log statement anywhere in the diff or function. Scored AC-5 FAIL: evidence-destruction-on-failure problem is unaddressed, not merely partially met.
- Route CHANGES_REQUESTED to `.owner` verbatim ("qa") — REJECTED: same self-loop hazard already caught live on `FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY` (cycle-840, this same journal) — `.owner=qa` here reflects "current handler," not the implementer. Routed to `developer` instead (matches the commit's own author identity per the developer notebook entry), and named the hazard in status_note so PO doesn't launder a future self-loop.
**why-decision:** AC-1/AC-2/AC-3 genuinely met (independently reproduced, not trusted from prose) but AC-4 and AC-5 are unmet as literally scoped — a P1 row whose blast radius is the single fleet-wide write path must be held to its full own checklist, not partial-credit-approved on the parts that happen to be easiest to verify.
**why-change:** No change from plan — verify-committed path; verdict is CHANGES_REQUESTED (2/5 AC unmet) rather than the router-implied APPROVE, `qa[]->review[]`, next_agent=developer (not owner="qa" — self-loop).

### STEP qa-S220 · qa · 2026-08-25T20:40:00Z
**task-id:** FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE
**note:** renumbered S219->S220 post-write — a concurrent QA session's S219 (FIX-ORCHAPPLY-MKTEMP..., commit `5a18d8726`) landed in the shared journal between my last read and this Edit call, same collision pattern as the prior S217/S218 note above.
**what-done:** Direct-Commit Verify on `73d4ce109`+`bd0d47bed`. Confirmed both on `main` ancestry, files-touched match claim (`notebook-auto-prune.sh`, `SKILL.md`, test file / notebook+journal). Ran real suites myself (not narration): `notebook-auto-prune.test.sh` 15/15, `notebook-compose.test.sh` 9/9, `notebook-linecap-sweep.test.sh` 11/11, `bash -n` clean, `shellcheck` shows the SAME 3 pre-existing info-level warnings pre/post (0 new). Built an isolated sandbox (own git repo, own file-size-caps.json fixture) and ran the ACTUAL committed hook against COPIES of 4 REAL live notebooks (`ba.md`, `digest-predict.md`, `tran-ngoc-bau.md`, `dev-pdf-extractor.md` — the exact file this row's dispatch cites as previously damaged), never the live paths. Also built a real-content-derived fixture (ba.md's own rolling section duplicated 6x to force undroppable-remainder > cap while non_sentinel_section_count stays 3, avoiding the pre-existing AC-5 cardinality==1 shortcut) and diffed pre-fix-script vs post-fix-script output byte-for-byte.
**what-considered:**
- Trust the row's own git-stash fail-before/pass-after claim — REJECTED, re-derived independently: checked out the parent commit's script into a scratch dir and ran it against real-notebook copies myself.
- Treat "file stayed byte-identical pre-fix too" as disproving the ticket — REJECTED after tracing the loop: the pre-existing AC-5 (non_sentinel==1 trap, shipped 2026-08-12/14) already blocks the worst-case write on any file with >=1 sentinel section, so pre-fix's real defect is the SIGNAL being misleading/wasteful (reports post-virtual-drop counts, e.g. ba.md reported section_count=3/non_sentinel=1 instead of true 5/3), not an un-gated destructive write — this matches the developer's own honest disclosure verbatim, confirmed by my own repro, not merely read.
**why-decision:** All 4 real-notebook byte comparisons: file bytes identical before/after in every run (sha256 + diff), retained dated sections byte-for-byte identical to source (checked 2 sections directly, e.g. dev-pdf-extractor.md's OCR-PADDLE and dispatch#2 sections), pre-fix vs post-fix produce IDENTICAL output on pure-dated files (tran-ngoc-bau.md, dev-pdf-extractor.md — AC-2 no-regression, confirmed on the actual previously-damaged file) and DIFFERENT-but-correct output on rolling-heavy files (ba.md/digest-predict.md: post-fix fires `notebook_undroppable_remainder_over_cap_breach` with TRUE original counts on iteration 1; pre-fix eventually falls through to the pre-existing `notebook_no_valid_drop_candidate_breach` with misleading post-virtual-drop counts). Grep-c fix confirmed at source: all 5 sites (587,630,699,701,751) now `2>/dev/null` + `[ -z ] && VAR=0`, zero `|| echo` idioms left. SKILL.md doc update present and accurate. Decision-journal entry present at source (`sprint-...-developer-9.md:45`). Noted, out-of-scope, not blocking: SKILL.md was already 15807B over its own cap before this edit (developer flagged, unrelated pre-existing debt).
**why-change:** No change from plan — verify-committed path, `vc-approved`.

### STEP qa-S221 · qa · 2026-08-25T20:33:05Z
**task-id:** FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY
**what-done:** Re-verify (redispatch) on `0d0284a9d` atop already-approved `183e1ad8f`. EXECUTED, not read, AC-6's 4 branches via `ORCH_APPLY_LIVE_FILE_OVERRIDE` scratch fixtures + a bash-logic harness cloned verbatim from the shipped block: SUCCESS/no-extra-line (real board, 18 candidates, review-origin pick), NOTHING-ELIGIBLE (fixture with all candidates' `next_agent` forced to "qa", count=0, correct line), CAS-ABORT exit=2 (forced via `ORCH_APPLY_CALLER_BASELINE_MTIME`, live-untouched confirmed by hash), REAL-ABORT exit=1 (malformed-candidate direct call to `orch-apply.sh`, confirmed distinct from exit=2/3) and exit=3 (missing-file case), STAMPED-BUT-NOT-READ-BACK (logic-level, since fixed code can't reproduce the anomaly live — mocked `apply_exit=0/CANDIDATE_COUNT=3/picked=""` against the exact extracted conditional). Also fresh-reran AC-3's positive control on the post-AC-6 code: forced `OCR-PADDLE-VI-LANG-FIX-AND-REBENCH` to global-oldest done[]-origin candidate — stamped correctly, `_lane=done`, `secondary_dispatch_target=po`, zero extra print lines.
**what-considered:**
- Approve to DONE_VERIFIED now that AC-6 (the binding blocker) ships — REJECTED: `verification_gate` requires AC-4 too, and it is still unmet — zero post-fix (after 12:54:37Z) done[]-origin `secondary_claimed_*` stamp exists anywhere on the live board; newest stamp overall remains the already-flagged review-origin `RAG-FTS-BUILD-MEMORY-BOUND` (16:15:36Z), and the current real oldest live candidate is review-origin again, so the done[]-widening this row exists to fix has still never fired in production.
- Route CHANGES_REQUESTED to agent-father for further code — REJECTED: AC-4 is an evidence gate, not a code gate; nothing left to commit satisfies it, only an organic tick or an explicit PO ruling accepting fixture-only proof (the row's own `po_ac4_caveat` text names this exact fork).
**why-decision:** 6 of 7 AC now hold (AC-1/2/3/5/6/7 PASS); AC-4 is the sole, structurally-different open item — PARTIAL verdict, route to `po` for an AC-4 disposition ruling, not back to the implementer.
**why-change:** Dispatching session instructed "do NOT write orch-state.json — router owns that write" for this cycle, so the lane-move/next_agent is reported in RETURN only, not self-actuated via `orch-apply.sh` (deviates from the qa flow's own default `vc-changes` executable block, honored as an explicit dispatch-time override).
