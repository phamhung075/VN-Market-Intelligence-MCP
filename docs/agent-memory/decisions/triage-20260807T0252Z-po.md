# Decision Journal — PO triage 2026-08-07T02:52Z

Session (coordination): f298ccf7-8cf4-452d-9a5a-57dcb47e65ac
Inputs: telegram reports 4477/4478/4479/4480 (re-fetched live), `.task_board` (755 tasks), `.signal_queue.rows[]` (209), `git log -30`, `git branch` (main only, no CLEAN candidates).

---

## task_id: FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL

- **what-considered**: (a) close 4480 as a known/tracked duplicate of one of the four existing foreign-flow rows; (b) close it as an sla-monitor threshold false positive, same class as the price alert in the same tick; (c) probe the live history table first.
- **why-change**: took (c) and it inverted the expected answer. The serving tool `get_market_foreign_flow` returns 2026-08-07 with 98 tickers — on that evidence alone this reads as a stale-detector FP. The readonly DB probe shows `daily_foreign_flow` has zero rows for 2026-08-06 (a full trading Thursday) and 08-05 truncated at 04:29:40Z against an ~08:59Z session-close pattern. 2730min before the 02:00:04Z alert lands exactly on 2026-08-05T04:30Z. Rejected (a): the four adjacent rows are ticker-breadth (`FIX-FOREIGN-FLOW-COVERAGE`), a dead 404 route (`FIX-FOREIGN-FLOW-DEAD-ENDPOINT`), a different table's columns (`TASK_2006`), and bulletin rendering (`FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION`) — none owns day-completeness. Rejected (b) on the arithmetic.
- **consequence**: P1, `apps/mcp-server/`. AC-3 (per-trading-day completeness detector) is the load-bearing one — the intraday freshness SLA caught this only incidentally and self-cleared silently at 02:47Z when today's fetch landed.

## task_id: FIX-EMITSIGNAL-E3-RC1-OPAQUE-FATAL-DROPS-DETECTOR-FINDING

- **what-considered**: (a) transient, resolve 4477/4478 with no row; (b) fold into `FIX-EMITSIGNAL-E3-RC3-FATAL-NORETRY-DROPS-DETECTOR-FINDING` (already `review[]`); (c) new row for the rc=1 arm.
- **why-change**: took (c). Transience is real but is the *defect*, not a reason to skip: both ids are absent from `.signal_queue.rows[]`, so two detector findings left the board plane. Rejected (b) because that row explicitly closed the rc=3/empty-candidate arm only and left every other non-zero rc fatal by design — rc=1 was never in its scope. Second arm of one root class, not a duplicate.
- **consequence**: P1, `cross-service/`. AC-1 propagate orch-apply's discarded stderr into the marker + BUG telegram; AC-2 route the validator-abort arm into the rc=2 CAS lane, keep the conservation-abort arm fatal; AC-3 RED-at-HEAD regression test alongside the existing T25/T26 fixtures.

## task_id: FIX-SLA-MONITOR-MARKET-OPEN-BOUNDARY-DETERMINISTIC-DAILY-BREACH

- **what-considered**: (a) resolve 4479 as noise, no row; (b) mint a plain "suppress the 02:00Z tick" fix; (c) mint with an explicit anti-suppression AC and a dependency.
- **why-change**: took (c). (a) is wrong because the FP is deterministic — `*/30` cron + a window opening at 02:00Z means it fires on the first tick of every trading day and lands in PO's own triage input plane. (b) is actively dangerous: that same 02:00Z tick is what surfaced the missing 2026-08-06 foreign-flow session, so a blanket warm-up would trade a daily FP for a blind spot on real multi-day gaps.
- **consequence**: P2, `apps/mcp-server/`, `depends: [FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL]` so the completeness detector exists before the accidental one is silenced. Breach must still be recorded in `sla_breach_audit`; only escalation is gated, matching the existing off-hours behaviour.

---

## Verification performed
- `bun scripts/orch-validate.mjs` exit 0 on live + on 792769d2f / 0426a1910 / 70fa2b2a6 (E-3 transience).
- Readonly probe inside `vn-market-intelligence-mcp-mcp-server-1`, `/app/data/market.db` (named volume), scan_ts 2026-08-07T02:49Z.
- Board grepped for id collisions before mint: 0.
- Post-mint re-read of the live file confirmed all 3 rows + both signal-row transitions on disk.

## Known defect in this cycle's own record
Report 4477 was processed with `resolution="fixed"`, which is false — the enum was over-specified before the sub-flow was read. 4478/4479/4480 carry `"none"`. The disposition of record for all four is the board rows above.
