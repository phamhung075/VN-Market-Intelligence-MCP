# PO Decision Journal — Sign-off 2026-08-14T09:08Z

**task_id:** `EPIC-AUDITOR-DETECTOR-CORRECTNESS-DRAIN` (REVIEW → DONE_VERIFIED)
**Inputs:** the row's own `architect_review_note_20260808T0110Z` · `docs/architecture-briefs/2026-08-08-epic-auditor-detector-correctness-drain-triage.md` · LIVE `.task_board` (all 9 lanes) + `docs/data/orch/archive/2026-{06,07,08}.json` · live `docs/agents/system-auditor/flow/main.md` · `git log --since=2026-08-08` on that file
**Writes:** 1 `orch-apply.sh` pipe, Stage 0+1 PASS, conservation-clean (task_total 740 → 740 — **zero mints**, matching the epic's own AC-3 which I was verifying)

---

## D-1 — Verified by probe, not by narrative — and the narrative held on every count

**What was considered:** (a) sign off on architect's own count (N=15/M=3/N+M=18 was internally consistent and the brief was unusually well-evidenced); (b) re-derive every claim from the live board before signing.

**Why (b):** this session's standing discipline, and the repo's own recorded failure class — agents that narrate writes they never executed. A self-consistent completion note is exactly what that failure mode produces, so internal consistency is not evidence.

**What the probe actually showed:** the crispest single check was a **stamp census** — count rows carrying `epic_drain_disposition_*` or `updated_by ~ EPIC-AUDITOR-…`. Result: **exactly 12**, exactly the 12 originally-unowned ids (10 assigned + 2 CANCELLED). The 5 pre-owned rows carry no stamp, which *corroborates* rather than contradicts "verified and left untouched", and the phantom has nothing to stamp. The 15/3/18 arithmetic reconciles against board state, not just against itself. AC-3 verified independently: zero rows anywhere carry `created_by` referencing this epic.

**The two ids that did not resolve on the hot board both resolved correctly in cold archive**, and both strengthened the case: `T1-PREGATE-MEMCREEP` reached DONE_VERIFIED and archived 2026-08-14T04:13Z; `A30-DISCRIMINATOR-CRASH-CLIFF` — the survivor architect named for the phantom — was real and reached DONE_VERIFIED 2026-08-08T10:32Z. **The lane architect made drainable actually drained.** That is outcome evidence the completion note could not have contained, because it postdates the note.

## D-2 — Spot-checking live source caught a false mismatch in my own probe, not in architect's work

Three live-source claims re-checked independently: C-11 `status = 'done'` (confirmed, `main.md:965`), C-06 `-3 hours` (confirmed, `:960` + `:584`), task_board size-cap `length > 80` (my first grep **missed** — the literal string does not exist).

**Why I did not treat that as a failed claim:** architect's own brief had pre-emptively flagged that this row's severity wording *had softened over time* to "alert pm to run task-archive sub-flow". `main.md:829` carries verbatim that phrasing plus `[.task_board.active_sprints[].tasks[]] | length` > 80 — the `> 80` sits outside the jq quote, which is why my pattern missed it. Semantics identical, claim correct. **A grep miss is a claim about my pattern until proven a claim about the source** — and a completion note that predicts the exact drift that breaks your pattern is a note written from a real read.

## D-3 — Ratified both judgement calls, declined both new-work mints

**Closing `NB-AUDITOR-MAIN-SPLIT` now rather than when `TE-T06` lands:** ratified. An open-ended "close later" leaves `next_agent` unset indefinitely — the precise structural undispatchability this epic existed to kill. `superseded_by` preserves the linkage; TE-T06 verified live (BACKLOG/agent-father).

**Assigning B-05 with a caution instead of closing it:** ratified, and this was the harder call to get right. A doc-read suggested `FIX-BCTC-SLA-THRESHOLD-360`'s shipped Healthy-Idle Gate may already cover it, but the row was minted *after* that gate landed, citing fresh RAW corroboration against machinery the gate does not literally match. Closing on that ambiguity risks burying a live bug; duplicating risks paying twice. Architect deferred to the epic's own confirm-don't-infer standard — correct.

**Neither §5 item minted.** Item 1 (HEALTHCHECK zone) was a metadata correction already applied — verified live, `zone=apps/mcp-server/`. Item 2 I converted into a **binding PO ruling attached to the B-05 row** (`po_ratified_binding_followup`) rather than a separate re-check row: agent-father must re-verify before implementing. **Minting a row to track a re-check would have grown the exact lane this epic spent its whole existence draining** — the epic would have ended by becoming the churn it was created to stop.

## D-4 — The real defect this closes is not architect's, it is the review lane's

Architect finished 2026-08-08T01:10:31Z. First PO look: 2026-08-14T09:04Z. **Six days, and the work was complete and correct on day one** — nothing was wrong except that no triage pass looked. My own 08:15Z carry-over this same morning recorded "21 rows sit in `review[]` with `next_agent:po` — that is a PO review backlog nobody is draining." This row was one of them, and it took dev-team's Review-Lane SECONDARY-Drain to surface it, not any PO pass. The `review[]`-with-`next_agent:po` starvation is now confirmed by a concrete 6-day instance with a measurable cost: 15 correctly-assigned rows sat undispatchable-in-practice for six days after being made dispatchable-in-principle.
