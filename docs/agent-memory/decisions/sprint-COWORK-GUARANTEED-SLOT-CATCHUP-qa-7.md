# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa

**Sprint goal:** Not reopened for this row — BCTC period-identity direct-commit verify riding the same dev-team Review-Lane QA-Drain batch; separate sub-agent instance (own file, mirrors qa-5/qa-6 pattern) to avoid a concurrent-write collision with sibling QA sub-agents drain-verifying other rows in parallel.
**Agent:** qa
**Started:** 2026-08-06T21:53:01Z

---

### STEP qa-S1 · qa · 2026-08-06T21:53:01Z
**task-id:** FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT
**what-done:** Direct-commit verify of `qa[]` row (`branch:null`; review_note cites `Commits: 526181a37 (code+tests+docs+scripts), da207aa82 (notebook+journal)`).
**what-considered:**
- Trust review_note's prose (16/16+14/14 tests, tsc clean, mock-guard clean, A/B regression 635/34/15 both sides) vs. RAW-re-derive every check from main HEAD myself.
- Cited commits 526181a37/da207aa82: `git merge-base --is-ancestor` = NO for both, `git fsck --unreachable` confirms both are dangling objects, not on any branch. Could have hard-failed here per the verify-committed script's literal step 2 ("commit not found in main ancestry" -> ISSUE -> vc-changes).
- Instead ran `git patch-id` on both dangling commits and searched main's same-day history for content-equivalent commits by subject+author-date: found `c6730ddc7` (code, same subject/author-date 18:50:29) and `2bb3d6665` (memory, same subject/author-date 18:50:36), both `git merge-base --is-ancestor`=YES. Patch-id comparison: 526181a37==c6730ddc7 (10123ac0...) and da207aa82==2bb3d6665 (53d14d48...) — byte-identical diffs, replayed onto a different parent chain (matches sibling commit `30f97f7f2`'s own note "(replay of e8945d047)" — known git-contention replay pattern this project has hit before, exact precedent at qa row for agent-father: `fd000eca4`/`92ba46360` cherry-pick, same resolution shape).
**why-decision:** APPROVED, DONE_VERIFIED. Verified the ACTUAL landed commits (c6730ddc7 + 2bb3d6665), not the stale dangling hashes in review_note's prose. `git show --stat c6730ddc7` matches exactly: new `periodContentExtractor.ts` domain module + `parseBctcReport.ts` Step-0 gate (read the code myself at lines 601-642 — throw precedes Step-1 extraction/storeReport entirely, matches AC-2/AC-5 "written under NEITHER key" claim by construction) + migration script `dedupe-mislabeled-bctc-period.ts` + tests + docs. Re-ran both new test files myself on current main: `FIX-BCTC-INGEST-...CONTENT.test.ts` 16/16 pass (43 expect), `dedupe-mislabeled-bctc-period.test.ts` 14/14 pass (32 expect) — exact match to claim, and by name every AC has a dedicated test (AC-2 x2, AC-3 negative-control x2, AC-4+AC-5 x2, plus 6 unit tests on the extractor itself). `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` on all 3 touched production files -> PASS. DDD: `periodContentExtractor.ts` (domain) has zero infrastructure/application imports; `parseBctcReport.ts` (application) importing domain+infra is the established pattern used fleet-wide, not a new violation. Security: zero `process.env`, zero secrets, migration script's SQL is 100% parameterized (`.run(reportId)`/`?` placeholders). AC-1 (data correction) independently corroborated live by PO on 2 separate occasions same-day (po_corroboration_20260728, po_corroboration_20260728T1731) plus 4 further live-production evidence rounds through 2026-08-01 confirming the Step-0 guard firing correctly on real traffic (19-24 real quarantines, zero false-negatives observed, acquisition-side skew root-caused and split into its own row `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` per PO's own `po_actuator_row_ref` note, which explicitly grants "this row may be signed off on its own ACs"). Files[] mismatch noted but not blocking: row's top-level `.files` (bctcVpsIngestHandler.ts/resolvePdfText.ts/getBctcReportIdTool.ts) are the PO's original diagnostic-trace files, not the fix's edit-set — the fix deliberately guards one layer upstream (parseBctcReport.ts Step-0), explicitly justified in the commit body ("zero plumbing changes" to the 3 unedited files) and verified live in production since 07-28.
**why-change:** Flagging the commit-hash discrepancy in `status_note` for audit-trail correction (dispatcher/board hygiene) rather than silently accepting or blocking on it — same handling precedent as the agent-father `fd000eca4`/`92ba46360` row this same drain cycle. Not re-litigating the acquisition-side skew (already split to its own row, out of this row's scope per PO).
