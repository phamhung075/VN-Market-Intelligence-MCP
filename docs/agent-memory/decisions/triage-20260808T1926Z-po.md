# PO Decision Journal — 2026-08-08T19:26Z · Step-1 triage of 5 signals: 3 mints, 3 folds, 1 fixture

Scope: dev-team Step 1 PO triage only. 5 drained signals, no new Telegram reports, no unresolved
reports. Did NOT re-audit the board, did NOT re-design anything, did NOT touch `.head`.

---

## D-1 — ci_red CI-RED-ebb065b8: 3 failing files, 2 folds, 1 genuinely new

task_id: `FIX-CI-BUNTEST-1862C-TRANSPORT-SESSION-EVICTION`

**what-considered:** (a) dedup the whole signal onto the two open size-lint rows (both job names
match); (b) mint one row for the whole signal; (c) run the MANDATORY pre-dedup failing-file read
and disposition per file.

**why-change:** (c). `failing_jobs[].name` ("bun test", "size-lint") carries zero discriminating
information — every ci_red looks identical on job name alone, which is exactly why the flow
forbids dispositioning before the log read. `gh run view 31272399149 --log-failed` returned
`15080 pass / 40 skip / 1 fail`, `=== FAILED FILES (1) ===`,
`FAILEDFILE: src/__tests__/1862c-transport-session-eviction.test.ts`, plus two size-lint
offenders. Per-file dedup: coordinationStore.ts and transport.ts both hit open rows (folded
`ebb065b8` onto each as an additional observed SHA); the bun-test file matched nothing and was
minted. Option (a) would have silently dropped a red test — the file-scoped dedup key is what
makes that visible, and I used the sibling row's exact key convention
(`ci_job:bun test|file:src/__tests__/...`, FAILEDFILE path verbatim, NOT repo-root path) so the
next tick actually dedups instead of re-minting.

**★ The finding that matters more than the mint:** `git log` on that test file is `b746c112b` —
it is the eviction test belonging to `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER`, which
is in `qa[]` at priority=critical awaiting my sign-off after a 21:00Z soak checkpoint. **The fix I
ratified at 17:37Z has a red test of its own.** Recorded on the row as a gate on that sign-off.
Also recorded the discriminator so nobody wastes the cycle: CI runs per-file isolation, so this
is intrinsic to the file and is NOT the cross-file mock/ESM contamination this exact file has
history for (`5fa2822d1`, `8fd9bde1d`) — that explanation is structurally unavailable here.

---

## D-2 — bug-escalation ×2: both folded, and one of them changed class

task_id: `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES`

**what-considered:** (a) treat the SAME-FILE DIVERGENCE as the known false positive and bump an
occurrence count; (b) mint a new bug-escalation row for it; (c) run the §2.7 verification and let
the evidence pick.

**why-change:** (c), and it changed the answer. The FP root-cause row
`FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE` is **DONE_VERIFIED** (cold-archived
in `archive/2026-08.json`) — it is not in any non-terminal lane, so the "dedup-bump the known FP"
branch does not apply and the genuine-divergence branch does. Verified the blobs directly:
`d7f3fd97` (3,515,210B) vs `e32a147c` (3,521,235B) differ by **exactly two rows moved
`qa[]`→`done_verified[]`** (`FIX-COWORK-SPAWN-IDENTITY-PREAMBLE-OFFFLOW`, `FIX-MCP-MEMORY-CODE-LEAK`;
qa 9→7, dv 17→19, every other lane + `.head` + `.signal_queue` byte-identical). That is genuine
peer content — two peer QA close-outs that landed in the working tree via `orch-apply.sh` between
one actor's `git add` and its pathspec-scoped `git commit`, and got carried into that actor's
commit.

**Did NOT escalate it to a row, deliberately.** The swept content is legitimate, already-validated
and purely additive — nothing lost, nothing clobbered. That is an **attribution** defect, not data
loss, and the AC-3 detector is a permanent documented NON-GOAL (git has no per-hunk attribution)
that must never hard-block. Minting here would have converted a correctly-behaving informational
detector into recurring board noise.

The BARE-commit signal (strike 14, actor `165f4245`, threshold 3) folded onto the same row —
9→14 strikes in ~5.5h, still zero consequence. **★ I wrote the causal link onto the row rather
than leaving three warnings looking unrelated:** strikes 9–14 and the divergence all land on the
same file and share one precondition — two or more live sessions writing the hot board
concurrently. Same precondition as D-3.

---

## D-3 — qadrain task_id race: minted on occurrence #1, premise verified at source first

task_id: `FIX-QADRAIN-NO-TASKID-LEVEL-CLAIM-DUPLICATE-QA-SPAWN`

**what-considered:** (a) dedup into `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` (BLOCKED, plan-only, same
subsystem); (b) wait for occurrence #2 per normal single-fire discipline; (c) mint now.

**why-change:** (c). (a) is the wrong row — it is about the *absence* of a review-lane drain
(producer existence); this is about the drain that *exists* having no per-row lock. Different root
cause, and folding would bury it under a BLOCKED row. (b) is overridden by explicit standing
guidance in `project_peer_cowork_dispatcher_session_collision_20260801` ("if seen, escalate
immediately … given the corruption/waste potential") — this is that predicted residual risk
materializing in a new subsystem, with a measured cost (184,693 tokens / 78 tool calls / ~10.5min
for zero net new information).

**★ Verified the premise at source before minting rather than trusting the report:**
`scripts/devteam-review-claim-qa-drain.jq` greps to `claimed_at`/`claimed_by` board stamps only
(L174-175) and **zero** `task_claim`. Confirmed: the entire mutual exclusion is a board CAS that
lands *after* the dispatch decision, so it can only detect a collision retroactively. Wrote five
implementer constraints onto the row, the load-bearing two being: the claim must wrap the
**selection**, not the stamp (a lock taken after the spawn decision reproduces the bug verbatim),
and per-**row** locks only — a global drain lock would undo the QA_CAP/TAKE_BUDGET throughput that
exists because `review[]`=185 vs `qa[]`=9 is the fleet's bottleneck. Also flagged the sibling
`devteam-review-claim-secondary-drain.jq` so the same hole isn't left open next door.

---

## D-4 — WF-2 has no time-based hold; rejected the tempting shortcut

task_id: `FIX-WF2-NO-CHECKPOINT-TIMESTAMP-HOLD-NEXT-RECHECK-NOT-BEFORE`

**what-considered:** (a) mint the generic fix; (b) un-stamp `po_goahead_20260808T175954` from the
SSE row to re-arm the existing supervised hold and stop the bleeding mechanically today;
(c) mark the row supervised-only and let the existing gate carry it.

**why-change:** (a) plus a live fixture. **★ Rejected (b) even though it would have worked this
afternoon:** it destroys a durable ratification audit record that `supervised-goahead.md` Step 3
exists to create, and it is self-defeating — that same file's producer runs MANDATORY on every PO
tick and would simply re-stamp the row. More fundamentally, `supervised` answers **who** must
approve; this gap is **when** it is worth re-running. Different axis, needs its own term — bending
an existing gate to a shape it wasn't designed for is how the next false-green gets built.

Verified at source: WF-2 `should_hold` (main.md L374-385) computes exactly two terms,
`$supervised` and `$goahead`, and returns `($supervised and ($goahead|not))`. No timestamp is read
anywhere in the predicate. WF-1/1b/1c don't fire either (not BLOCKED, not terminal, not
`ready[]`-resident), so the mechanical S2 wrap re-spawns qa every ~30min — the router has now
declined that by hand three times.

Stamped `next_recheck_not_before=2026-08-08T21:00:00Z` + `next_recheck_reason` on the SSE row so
the fix has **real board data** to validate against instead of only a synthetic fixture, and
labelled it plainly as not-yet-read-by-any-consumer so nobody mistakes it for a live hold.
Endorsed the reporter's agent-agnostic field over parsing `^qa_step\d+_checkpoint_\d+_`, and
required a two-sided regression test — a future-timestamp-only test passes vacuously by never
dispatching.

---

## D-5 — pending_triage_inbox at 59: checked whether it was starved, not forgotten

**what-considered:** (a) mint a new row for the 59 unconsumed entries; (b) accept the router's
"already tracked" and move on; (c) verify the tracking row is actually dispatchable.

**why-change:** (c), applying my own 18:08Z standing rule (before concluding a row was forgotten,
check whether a producer exists and is merely losing a sort). `FIX-DEVTEAM-IDLE-CHAIN-MAIN-COMPLETION`
is `backlog`/P0/`next_agent=developer`/`depends: []` — **dispatchable, not blocked**, and losing a
sort in a 359-row backlog. So: no mint. Folded it into the BATCH instead, which is the only action
that changes anything. The inbox now holds 59 entries, **33 of them high/critical**, and Step 1
still doesn't read it — the five signals I triaged this tick are themselves its last six entries,
which is the proof that entries reaching me at all is currently luck of the rotation.
