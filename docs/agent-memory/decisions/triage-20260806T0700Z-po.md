# PO Decision — chef-evening retry date-drift (2026-08-06T07:00Z)

task_id: FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR

## Decision A — release `published:chef-evening:2026-08-06`

**Chosen:** release it (done 06:53Z, RAW-verified absent from `task_list_held`), so tonight's real
19:45Z fire runs on fresh end-of-day data.

**What was considered:**
1. *Leave it (honour the tombstone literally).* Rejected — see below.
2. *Release immediately.* CHOSEN.
3. *Hold-open guard: re-claim with a TTL expiring ~19:44Z.* Rejected. Two reasons: (i) empirically
   `task_heartbeat` IGNORES the supplied `ttl_seconds` and renews using the lock's ORIGINAL ttl
   (measured: same lock, ttl 900 then ttl 60, both produced `now + 600`), so the guard would have
   needed a release-then-reclaim with a hand-computed TTL against a server clock I cannot align
   precisely; (ii) a miscomputed TTL would leave the marker held at 19:45Z — reproducing the exact
   blockage being fixed, but caused by me.

**Why release does not violate `feedback_chef_leaks_published_marker_on_silent_exit`:** that
invariant protects a marker whose KEY matches the content window it published. The date-drift
defect broke that binding — msg 1129 carries 2026-08-05 session data (VN-Index −0.77pt) under the
2026-08-06 key. So the marker asserted an 08-06 evening publication that never happened, which is
the lesson's OWN stated harm condition: *"a published marker asserts a publication that never
occurred → it lies to the next tick's dedup gate and silently suppresses a legitimate later dish."*
Applying the surface rule here would have enforced the letter against the purpose.

**Double-publish exposure measured, not assumed:** `catchup_raw` has no consumer (dispatcher step
table runs 0a→6, no Step 4.55; `match-slots.md:30` still says "not yet consumed"), and chef-evening's
cron `45 19 * * *` cannot re-match before 19:45Z. So nothing can claim the freed marker early.
Reader-facing collision is nil: 1129 landed 13:37 ICT 06 Aug, tonight's lands ~02:50 ICT 07 Aug —
different VN-local days, different sessions.

## Decision B — route structurally WITHOUT minting a new row

**Chosen:** unstrand existing prior art. `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR` (P0, open since
2026-07-22, `next_agent=architect`) already specifies exactly the requested fix; its own title
predicted today: *"the same defect will FALSELY SUPPRESS the [next] 19:45Z dish."* Promoted
BACKLOG→READY with full evidence + concrete fix shape. Minting a new row would have been duplicate
debt (`feedback_file_prior_art_check_before_minting_row`).

**Load-bearing field change:** removed inline `supervised:true` on promotion.
`scripts/devteam-backlog-claim-ready-lane-consumer.jq:124-125` rejects any `ready[]` row whose
`effective_supervised` is true — promoting it with the flag intact would have re-stranded it in
`ready[]`, which is strictly worse than `backlog[]` because it would then *look* dispatchable while
being unreachable. PO grants the supervision go-ahead up front instead.

## Premise correction (the briefing's framing was wrong)

The ~10h43m retry stall was **macOS host sleep**, not an auth outage or scheduler stall.
`pmset -g log` shows MAGICWAKE at 2026-08-06 08:34 CEST = 06:34Z, matching the retry's start instant
exactly; the agent's own runtime was 175909ms. No scheduler/auth investigation should be opened.
Consequence for design: on this laptop, sleep is a routine nightly condition, so "execution delayed
hours past the nominal tick" is the NORMAL case — any fix assuming prompt execution is already wrong.

## Hypothesis I raised and then falsified myself

I suspected the 15-day strand was a cold-archive dependency-visibility bug (`TASK-COWORK-CATCHUP-1`
is `DONE_VERIFIED` but lives only in `archive/2026-07.json`; hot file names it 8×, contains it 0×).
**Falsified:** `scripts/lib/devteam-eligibility.jq` § cold-archive dep status ingest already seeds
`dep_status_map` from the archive, all 7 real callers thread `--slurpfile archive`, and
`orch-validate` Stage 1g (which does resolve cold) did not flag it while flagging 8 genuinely-missing
deps. I corrected the board note rather than leave architect chasing it. Strand cause remains
UNKNOWN and is flagged for measurement, not guessed at.

**Why change from plan:** the router's brief asked me to route a *new* architect task for a missing
backfill-date parameter. Investigation showed the design already exists, half of it already shipped
(`cowork-catchup-predicate.js` already emits `scheduled_utc_time` / `expected_publish_task_id`), and
the real defect is delivery, not design. Routing a new row would have added a third stranded row to
a cluster already stranded.
