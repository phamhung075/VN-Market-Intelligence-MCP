# PO decision journal — AC-2 rescope ruling, FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58, 2026-08-22T17:30Z

**task-id:** FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58 (AC-2 rescope) + FIX-WATCHLIST-ROSTER-RESTORE-23-LOST-TO-CORRUPTION-RESEED (mint)
**Inputs:** parent row `pm_decomposition_note` + `architect_review_note` + `ba_note` read directly from `docs/data/orch/orch-state.json` · live `get_watchlist` RPC via `mcp__gateway__call_tool` · `docs/data/system-map.json` · `apps/frontend/app/domain/market.ts` · served page `http://localhost:3001/dashboard/analysis` · `docs/data/coverage-state.json`
**Scope of dispatch:** the AC-2 rescope call only. TASK_001/002/003 untouched.

## S1 — Verified the premise myself before ruling, through a plane neither prior agent could reach

**what-considered:** (a) accept pm's summary and rule on it; (b) re-read architect's 2026-08-14 note and rule on that; (c) independently re-measure all planes, including the served one AC-2 actually names.

**why-decision:** Took (c). Two prior agents (BA, architect) both reported they lacked the `mcp__gateway__call_tool` grant and fell back to `docker exec` + `bun:sqlite`. So every existing measurement of the "runtime roster" came from one method, run twice. A third repetition of that method would not have been independent confirmation. PO holds the gateway grant, so the decisive RPC was placeable here for the first time.

Measured this tick:

| plane | method | count | ACB/CTG/MBB/VPB |
|---|---|---|---|
| runtime `get_watchlist` | gateway RPC → vn-market (SQLite `watchlist`) | 34 | absent |
| `docs/data/system-map.json .project.watchlist` | jq | 34 | absent |
| `apps/frontend/app/domain/market.ts WATCHLIST_STOCKS` | source read | 34 | absent |
| SERVED page `/dashboard/analysis` | HTTP 200, 75388 bytes | "33 mã" | absent (0 occurrences each) |

`diff runtime sysmap` = EMPTY. `diff sysmap frontend` = EMPTY. Strictly identical, zero-diff every direction. Served-page controls all present (VCB=8, BID=4, FPT=14, HPG=8, PLX=4 occurrences); the four named tickers score exactly 0. The "33 mã" label is 34 minus VEA (`active: false`) — correct behaviour, not a divergence.

**why-change:** No change from plan. Architect's 2026-08-14 finding is CONFIRMED, now on four planes and by an independent method. There is no live 41% hole; the 58-ticker premise AC-2 was minted against is refuted.

## S2 — The ruling: BOTH of pm's options, not either/or

**what-considered:** (a) Option A alone — reword AC-2, drop the ticker names, close the question; (b) Option B alone — approve a content task and leave AC-2 as written; (c) both; (d) neither — accept 34 as the intended roster.

**why-decision:** Took (c).

**Option A is necessary on its own merits.** A hardcoded ticker list inside an acceptance criterion is the same anti-pattern the parent row exists to kill — it passes on the wrong roster the moment the roster legitimately changes. AC-2's load-bearing half was never the names; it was "verify in the SERVED page, not the source constant", which its own citations (`feedback_host_cli_integrity_check_false_ok`, `feedback_factory_testfirst_primitive_tests_copy_not_served_path`) make explicit. That half is retained verbatim in intent. Only the names are retired. Left as written, AC-2 is unsatisfiable by any sync or architecture fix and would have stranded a P1 row in review indefinitely.

**Option A alone is not sufficient, so (a) and (d) are both wrong.** The loss is real, mechanical, and still costing coverage today:

1. **Mechanical, not curatorial.** BA's forensics — all 34 surviving rows share one identical `added_at` (2026-07-31T18:25:37) — prove a single bulk reseed-from-empty following a `SQLITE_CORRUPT` event (5th since 04-25). `add_to_watchlist` issues a plain INSERT with zero file write-back, so any tool-added ticker never hand-mirrored into `system-map.json` is destroyed by every corruption recovery. There is no removal decision on record for any of the 23. Nobody chose to drop them.
2. **The fleet already pays to cover them.** `docs/data/coverage-state.json` (`_ssot: true`, last written by news-scout 2026-08-15T08:13:23Z — live, not an archive) holds 57 tickers and is a **strict superset** of the live roster: `live \ coverage` = EMPTY, `coverage \ live` = exactly 23. The system produces coverage for 57 tickers and serves 34. That produce/serve mismatch is a live cost, not a historical curiosity.
3. **Precedent prices the risk.** `SPIKE_1946-crisis-detection-plx-gap.md` records a missed crisis detection caused by exactly one absent ticker (PLX). The current hole is 23, including four of Vietnam's largest listed banks plus GAS, HVN, ACV, REE, POW, GVR, HCM.

Dropping the names and walking away would silently ratify a data-loss event as intended product scope. That is the failure mode (d) represents, and it is why the rescope could not be a pure deletion.

**why-change:** pm proposed A *or* B. Ruled A *and* B. pm's deferral to PO was correct — this is a coverage/product call, not a decomposition call.

## S3 — The restore list is derived, and MWG is deliberately excluded

**what-considered:** (a) restore the 4 tickers AC-2 names; (b) restore the 24 the parent's 2026-07-25 note lists; (c) derive the set from surviving evidence.

**why-decision:** Took (c). `comm -23` of `coverage-state.json .tickers` against the live runtime roster yields exactly 23:

```
ACB ACV BDI CTG D2D DHG DLC GAS GVR HCM HSG HVN JSH MBB NKG POW PPC REE SIS TCH VDC VNH VPB
```

Restoring only the 4 named would re-encode the same arbitrary hardcoded list this ruling is retiring. The parent's list of 24 includes **MWG**, which is absent from *both* coverage-state and the live roster today — it has no evidentiary basis for restoration. Adding it would be a fresh addition, not a restoration; excluded, mint separately if wanted.

I approve the restore **policy**, not the listing status of 23 tickers read off an orch-state row. AC-2 on the new row therefore mandates a listing-status screen at execution time, with skipped tickers recorded by name. The final restored set may legitimately be smaller than 23. Certifying tradeability from here would be exactly the kind of unverified assertion this fleet keeps paying for.

**why-change:** Scope widened from the 4 named to a derived 23, and narrowed from the parent's 24 by dropping MWG.

## S4 — The sequencing constraint is the most valuable part of the mint

**what-considered:** (a) mint the restore as READY so it can be picked up now; (b) mint it BLOCKED on TASK_001; (c) fold it into TASK_001.

**why-decision:** Took (b). Executing the restore **today**, before TASK_001's write-through lands, writes SQLite only — `system-map.json` stays at 34 and the compile-time frontend constant stays at 34. The restore would **manufacture the exact three-way divergence the parent row exists to eliminate.** Approving the content and executing it in the wrong order actively creates the defect. This is the single most likely way this ruling gets misapplied, so it is stated on both rows: do not "just add the four banks quickly".

Rejected (c) — folding content into TASK_001 would contaminate a clean mechanism fix with a data decision and make its QA ambiguous.

**why-change:** No change from plan.

## S5 — Routed to architect first, because this ruling falsifies architect's own deferral premise

**what-considered:** (a) route straight to a dev — 23 tool calls plus a constant edit; (b) route to architect for one bounded decision first.

**why-decision:** Took (b). Architect deferred FR-6 (frontend watchlist codegen) on 2026-08-14 with the stated reason *"zero live drift found, building it now would be scope-creep against a refuted premise."* That reasoning was sound **at the time and on the evidence then available**. Approving a 23-ticker restore falsifies it by construction: `add_to_watchlist` — even with TASK_001's write-through to `system-map.json` — cannot update a compile-time TypeScript constant, so frontend-plane drift becomes guaranteed rather than hypothetical.

Architect must pick one and record it on the row:
- **(a)** un-defer FR-6 — generate `WATCHLIST_STOCKS` from the SSOT so the frontend plane self-syncs; or
- **(b)** accept a documented manual mirror step in the *same commit* as the restore, **and** extend TASK_002's audit to cover the frontend plane so the next drift fails loud.

Either is acceptable to PO. Silently doing neither is not — that reproduces the parent defect on the one plane the parent's own AC-2 measures. The row is BLOCKED anyway, so this routing costs no latency.

**why-change:** Added an architect gate that neither pm nor the dispatch anticipated. Surfacing a falsified premise beats silently absorbing it.

## S6 — What I deliberately did not touch

**what-considered:** (a) also fix the parent's title, which still asserts the refuted "stale at 34 vs the live 58-ticker roster / 41% hole"; (b) leave it.

**why-decision:** Took (b) for the title, (a) for the note. The row `id` and `title` are reference keys quoted across handoffs, the BA spec, and three subtask rows; mutating them mid-flight risks breaking those references for a cosmetic gain. The correction is recorded in `po_rescope_note` and inside the rescoped AC-2 instead, which is where pm and QA actually read.

Also untouched, on purpose: AC-1/AC-3/AC-4 (unchanged and still correct — AC-1 already asserts set equality, not the number 58); TASK_001/002/003 (no PO input needed, explicitly out of dispatch scope); and `coverage-state.json`'s own blanket-stamp defect, which stays with `FIX-COVERAGE-SWEEP-BLANKET-STAMP-DEAD-TRIGGER`. I read that file purely as **evidence** of the pre-corruption roster and did not pull its defect into scope — the parent row's own out-of-scope clause forbids exactly that, and it is right to.

**why-change:** No change from plan.
