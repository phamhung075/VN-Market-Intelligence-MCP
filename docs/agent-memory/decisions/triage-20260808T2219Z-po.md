# Decision Journal — 2026-08-08T22:19Z · po

**Trigger:** router-directed re-mint of RC-IDLE-LOOPS (2026-07-04 systemic-remake brief Phase 1)
**Agent:** po
**Started:** 2026-08-08T22:15:40Z

---

### STEP po-S1 · po · 2026-08-08T22:17:00Z
**task-id:** (ambient — premise falsification before any mint)
**what-done:** Refused the instructed mint after live census showed all 5 RC-IDLE-LOOPS tasks already DONE_VERIFIED under the `P1-IDLE-*` id prefix.
**what-considered:**
- Mint `RC-IDLE-LOOPS` as instructed — router asserted zero board/archive matches
- Search the board by *brief-section id* only (what the router did) vs by *implementation target* (script/flow paths)
- Grep the repo for any id prefix carrying the RC name before trusting the "never minted" premise
**why-decision:** `grep -rhoE "P1-(IDLE|DETECTOR|DRIFT)-[A-Z0-9-]+"` returned 10 distinct ids, 484 references. `archive/2026-07.json` has 7 at DONE_VERIFIED. Minting would have re-commissioned shipped code.
**why-change:** Router's search key was the brief's section label; the board never used that label as an id. The premise was a search-key artifact, not a board fact.

### STEP po-S2 · po · 2026-08-08T22:18:00Z
**task-id:** FIX-RUNIDLE-PREDICATE-D-ACTIVE-SPRINTS-PERMANENT-FLOOR
**what-done:** Re-measured RC-IDLE-LOOPS' own AC-3 instead of re-minting it; it FAILS, so minted the failure not the feature.
**what-considered:**
- Report "already done, nothing to do" and exit
- Re-measure the acceptance criterion the brief itself specified
**why-decision:** The router's *symptom* (89% chore commits, unchanged) is real even though its *diagnosis* is wrong. AC-3 says drain/auditor commits/day must trend down: 07-19..07-25 = 18-49/day, 08-05..08-08 = 25-65/day. Flat-to-up. `consecutive_run_idle=0`. The gate shipped and never fired.
**why-change:** Task class changed from "mint missing work" to "mint an unmet acceptance criterion" — a different, sharper row.

### STEP po-S3 · po · 2026-08-08T22:19:00Z
**task-id:** FIX-RUNIDLE-PREDICATE-D-ACTIVE-SPRINTS-PERMANENT-FLOOR
**what-done:** Hand-evaluated all four `_step5_idle_check()` predicates against live state to name the blocker rather than let pm re-derive it.
**what-considered:**
- File the row on the observed metric alone ("RUN-IDLE never fires")
- Run the predicates and identify which one is saturated
**why-decision:** (a) drainable=1, (b) db fresh, (c) signal_queue NEW=1, (d) active_sprints=**8**. (d) demands zero; `active_sprints[]` is an accumulator with no closeout producer — 2 of 8 stamped 2026-07-17, 3 weeks stale. Structurally unreachable, not "rarely reached".
**why-change:** no change from plan.

### STEP po-S4 · po · 2026-08-08T22:20:00Z
**task-id:** FIX-RUNIDLE-PREDICATE-D-ACTIVE-SPRINTS-PERMANENT-FLOOR
**what-done:** Deliberately gave the new row `sprint: null` instead of creating/reviving a sprint object for it.
**what-considered:**
- Register `SYSTEMIC-REMAKE-P1` in `active_sprints[]` (2 rows already point at a sprint id with no object)
- Leave the row sprint-less in backlog
**why-decision:** Every new `active_sprints[]` member increments the exact counter this task exists to unblock. Registering a sprint to hold it would be self-defeating. Wrote that constraint onto the row as decomposition hint (iv) so pm does not undo it.
**why-change:** no change from plan.

### STEP po-S5 · po · 2026-08-08T22:20:55Z
**task-id:** P1-DETECTOR-CLOSURE-TRIAGE-SIGNALS · P1-DETECTOR-CLOSURE-TASK-ARCHIVE · P1-DRIFT-QUARANTINE-FREEZE-FLAG
**what-done:** Closed 3 stranded BACKLOG rows to DONE_VERIFIED on live source raw-probe, moving them `backlog[]`→`done_verified[]` (lane-status coherence).
**what-considered:**
- Promote them BACKLOG→READY per the brief's §1.2 instruction
- Verify the source first, then close whatever already shipped
**why-decision:** The brief's instruction was 5 weeks old. Live: `origin_signal_id` is real executable jq in `pm/flow/task-archive.md` L118-123 and a real spec block in `po/flow/triage-signals.md` L27; `recurringBugEscalationFlag`/`escalationReason` are gone from `project-stats.json` entirely. Promoting to READY would have dispatched three no-ops. `LANE_ALLOWED_STATUSES.backlog` is `{BACKLOG,BLOCKED}`, so closure required the lane move, not just a status edit.
**why-change:** Brief said "promote"; source said "already done". Source wins.

### STEP po-S6 · po · 2026-08-08T22:20:55Z
**task-id:** FIX-SIGNALQUEUE-DUP-ID-GUARD
**what-done:** Set `next_agent: developer` on a row that had `next_agent: null` — no lane could ever have picked it up.
**what-considered:**
- Close it like the other three
- Verify then unstrand
**why-decision:** Verified STILL OPEN: `scripts/orch-validate.mjs` has only a `payload_ref` existence check (L417, L754-761) on `signal_queue.rows[]`, no id-uniqueness guard. Brief §1.2 assigns it to developer. Left `supervised:true` untouched — that flag was a prior PO's deliberate call, not mine to reverse.
**why-change:** no change from plan.
