# PO triage decision journal — 2026-08-26T12:28Z

task_id: FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK
Also folds: UC-CDC-P1
Input: 1 durable-inbox envelope `b204d1f2bb96332a22ff82ffa63fec9572c14b7ed49c383e3f6785c85b3a5ae8`
(cowork-team `cowork-fire`, fire_time 2026-08-26T12:06:51Z, drained_at 12:12:14Z).

what-considered: mint a new row for the `calendar_status:"open"` falsehood vs fold it into an
existing tracked row. Chose FOLD — a tracked row (UC-CDC-P1) exists and was found by a
path-resolved board scan; the spawn prompt asserted no row existed, which was wrong.
why-change: no new rows minted this tick. `ready[]` is 114 deep at ~1 dispatch/6 idle ticks;
the measured behavioural harm (~6 avoidable agent spawns/day, §2.3) does not outrank what is
already starving there, and both items have owners.

Predecessor detail store for the cycle-snapshot row (NOT superseded, still authoritative for
everything before this tick): `docs/agent-memory/decisions/triage-20260826T0348Z-po.md#cycle-snapshot`
(holds `po_premise_amendment_20260824T2030Z`, `po_fold_20260825T0727Z`, `po_fold_20260825` verbatim at its §2.1-2.4).

---

## §1 — FIX-CYCLE-SNAPSHOT-... : fold of the 12:06Z envelope


[po/triage 2026-08-26T12:28Z] FOLD (1 cowork-fire envelope b204d1f2b, fire_time 12:06:51Z). NO NEW ROW. Four items, each verified on disk this tick, not relayed.
(a) RECURRENCE CONFIRMED, occurrence 42. Producer wrote docs/data/cycle-snapshot-12:05.json (17547B, mtime 12:05 UTC) at nominal tick 12:00; emit_pressure_state returned cycle_snapshot_promoted=false ~1 min later. Verified independently of the envelope: that file exists at that size; docs/data/cycle-snapshot-latest.json is STILL tick="00:00" created_at=2026-08-23T00:07:50Z, now ~84h stale (up from the ~75h in the previous fold). Directory now holds 45 cycle-snapshot-*.json, up from 36 at the 02:30Z census. Gap 09:20 -> 12:05 = 165 min, matching the envelope's staleness-reset claim.
(b) SCOPING HAZARD INSIDE THIS ROW'S OWN NOTE - READ BEFORE IMPLEMENTING. The "ELIMINATED: lookup-by-nominal-tick i.e. THIS ROW'S ID" line above was measured against alert-commander's cycle-bootstrap consumer (c281-c284), which is Copy B and keys off its OWN post-spawn wall clock. It does NOT apply to the emit_pressure_state PROMOTER, a different consumer that does key by the caller-supplied NOMINAL tick: apps/mcp-server/src/interface/mcp/tools/system/emitPressureStateTool.ts:398 derives tickHHMM via regex /T(dd:dd):/ off tick_id, passes it to promoteCycleSnapshot at :415, and the not-found branch at :263-265 returns {promoted:false,stale:false}. Today's 12:06Z evidence is PROMOTER-side and re-confirms nominal-tick lookup for that half. TWO consumers, TWO key derivations; eliminating one must not retire the other.
(c) files[] PATH WAS WRONG, CORRECTED IN THIS WRITE. The row listed apps/mcp-server/src/interface/mcp/tools/emitPressureStateTool.ts - that path does not exist. Real path is apps/mcp-server/src/interface/mcp/tools/system/emitPressureStateTool.ts; the :398/:415/:263 line numbers above are against the real file and are current.
(d) DUPLICATE - THIS ROW OVERLAPS UC-SDF-P2 (task_board.ready[109], READY, P1, next_agent=developer). UC-SDF-P2's title already names "cycle-snapshot promotion dead since 07-07 (tickHHMM-vs-FILE_TICK filename divergence)"; its unit 1 IS this row's defect. This row's po_not_a_duplicate field lists four inspected neighbours and UC-SDF-P2 is NOT among them - the 2026-08-24 mint missed it. UC-SDF-P2 is strictly further along: BA spec docs/handoffs/UC-SDF-P2-BA-step60-hardening-spec.md (2026-08-24), architect design pass 2026-08-26T08:30Z, and a DELIVERED brief docs/architecture-briefs/2026-08-26-uc-sdf-p2-filename-contract-fix-and-regime-volatility-writer.md (on disk, 19465B, mtime 10:28Z) whose Unit 1 is exactly this row's settled fix shape - drop the exact-filename join for a newest-mtime glob. DO NOT commission a second architect pass for this row: the design already exists and this row is P0 with next_agent=architect + dispatch_lane=architect, so a DRS dispatch would redo delivered work. PO deliberately did NOT alter status/next_agent/dispatch_lane (two live agents on the board this tick; dispatch_lane is the design-router sweep's field) - the merge/re-point decision is raised to the caller in this tick's RETURN.

---

## §2 — UC-CDC-P1 : fold of the `calendar_status:"open"` arm of the same envelope


[po/triage 2026-08-26T12:28Z] FOLD (envelope b204d1f2b, fire_time 12:06:51Z). NO NEW ROW. Recurrence + a CORRECTION to this row's own previous fold + a measured blast radius.
(a) RECURRENCE, independently verifiable WITHOUT the envelope: docs/data/pressure-state.json on disk carries calendar_status="open" at emitted_at 2026-08-26T12:18:55Z. HOSE/HNX trade 09:00-11:30 and 13:00-15:00 ICT (docs/architecture-briefs/2026-08-04-cadence-rationalization.md:236), so 12:18Z = 19:18 ICT, hours after close. CORRECTION TO THE ENVELOPE: its "VN market closed 08:59Z" figure is wrong by ~1h (15:00 ICT close = 08:00Z). Conclusion unaffected; do not inherit 08:59Z.
(b) CORRECTION TO THE 2026-08-26T03:48Z FOLD ON THIS ROW. That fold concluded the wrong value is "currently non-binding on suppression decisions" because pressure-read.md Step 4.3 is a SUPERSEDED no-op. THE PREMISE IS FALSE. Step 4.3's own SUPERSEDED banner states the logic MOVED into scripts/agents-flow/cadence-policy.js + docs/data/cadence-policy.json, and every one of that table's 29 rules branches on calendar_status (cadence-policy.js:58). The value is binding TODAY via the live owner; the superseded copy is the wrong place to read consumer-ness from.
(c) MEASURED BLAST RADIUS - SMALLER THAN (b) ALONE IMPLIES. Scope from this, not from the alarm. cowork-match-slots.js:328-330 makes cron "always the first gate" and the adaptive loop iterates ONLY cron-matched candidates, so cadence can only throttle a slot DOWN, never add a fire. Per policy: chef-intraday is cron-confined to market hours (13 2-8 * * 1-5); bctc-offmarket and both alert-commander policies carry _cron_fallback:true on open/half_day/unknown so cron governs. That leaves gatherer-standard as the ONLY policy where day-granular "open" changes off-hours behaviour: news-scout-offhours and market-watcher-offhours (cron 0 */4 * * *, 24/7) never receive the holiday/weekend 480-min throttle on weekday nights. Measured from git history of docs/data/cowork-schedule.json: news-scout-offhours fired 6x/day on both 08-25 and 08-26 (00:06, 04:05, 08:06, 12:06, 16:06, 20:06) - exactly its 240-min cron, NO adaptive speed-up. Order of magnitude ~6 avoidable agent spawns/day across two gatherer slots. NOT a fleet-wide cadence break. Compounding factor: signal_backlog sits at 31 against a ~30-file undrainable permanent floor, so signal_backlog_tier is pinned "high" and gatherer-standard resolves its 30-min branch unconditionally - the throttle is dead twice over.
(d) THE DEEPER DEFECT IS THE ENUM, AND IT HAS NO OWNER. apps/mcp-server/src/domain/services/vnTradingCalendar.ts:80 isVnTradingDay() takes a DATE and has NO time-of-day component; grep for isMarketOpen/getMarketSession/market_session across apps/mcp-server/src + scripts returns ZERO hits. SESSION_STATUSES (:30) = open|holiday|half_day|weekend|unknown - there is NO member meaning "trading day, session closed", so even a session-aware producer could not express the true state, and pressure-read.md's FR-A5 branch explicitly treats the historically-observed literal "closed" as OUT-OF-DOMAIN. Meanwhile the table's authoring brief documents session-level intent: 2026-08-04-cadence-rationalization.md:37 describes chef-intraday's open row as "60-120 min DURING MARKET HOURS". UC-SDF-P2 unit 3 ("wire calendar_status to the vnTradingCalendar oracle") is marked COMPLETE by both the BA spec and the 2026-08-26 architect brief, and NEITHER asks whether the oracle's granularity matches what the cadence table means by "open". BA's ba_spec_note_20260824(b) noticed that "closed" does not exist and routed around it rather than questioning the gap. If this row is ever unblocked, the enum gap is the thing to fix - not the wiring, which already shipped as TASK_2008a.
(e) blocked_reason CORRECTED IN THIS WRITE - it described a state that no longer exists. It said UC-SDF-P2 is "still BACKLOG/plan_only/next_agent=ba and unclaimed"; UC-SDF-P2 is in fact task_board.ready[109], status READY, next_agent=developer, secondary_claimed_at 2026-08-26T08:15:51Z by dev-team, dispatched to architect, brief delivered 10:28Z. The DEPENDENCY itself still genuinely holds (UC-SDF-P2 units 1 and 4 are unimplemented) so this row correctly STAYS BLOCKED and PO did not flip it; only the stale description is repaired.

---

## §3 — VERBATIM ARCHIVE of the cycle-snapshot row `status_note` as it stood at 2026-08-26T12:28Z

The hot row `status_note` was COMPACTED in the same write that appended §1, solely to stay under
`ORCH_ROW_PROSE_CEILING_BYTES=12000` (the first attempt was hard-rejected at live=11464B ->
candidate=14658B). NOTHING below was discarded — this is the complete pre-compaction text.

```
[po/triage 2026-08-26T03:48Z] FOLD x3 arms + PRIORITY BUMP P1->P0. No new row (one arm carried an explicit do-not-mint flag; honoured). FULL PROSE + ALL MIGRATED HISTORY: detail_ref -> docs/agent-memory/decisions/triage-20260826T0348Z-po.md#cycle-snapshot (that file also holds this row's former po_premise_amendment_20260824T2030Z, po_fold_20260825T0727Z and po_fold_20260825 keys verbatim at §2.1-2.4; they were removed from the hot row only to satisfy the row-prose ceiling, nothing was discarded).
(a) DIRECTORY CENSUS (envelope f9b11f827, amends the drained 02:15Z filing), measured 02:30Z: 36 cycle-snapshot-*.json total, 4 created TODAY, 32 created the PRIOR DAY, names spanning 02:20-21:05. Prior-day files are the MAJORITY of the directory, not prune residue at the window edge. Producer cadence is a fixed 15 min every day, so today's filename pool and yesterday's are nearly the same set: the directory is pre-seeded RIGHT NOW with a 24h-old decoy at nearly every tick name today will use for ~19 more hours. A stale HIT is the DEFAULT path; a clean miss is the exception. Confirmed hits 02:05Z and 02:20Z (alert-commander c261 read cycle-snapshot-02:20.json created 2026-08-25T02:20:38Z, exactly 24h old, while a ~1-min-old cycle-snapshot-02:19.json sat unconsulted in the same directory). BASIS FOR THE P0 BUMP: the only thing between this and a consumer acting on day-old macro data is each consumer's own inside-the-file date check -- per-consumer, contracted nowhere, and no future consumer is obliged to implement it.
(b) THIRD CLOCK (envelope cf74c03f9): .claude/skills/cycle-bootstrap/SKILL.md Step -1 derives its tick from its OWN POST-spawn wall clock. 4/4 consecutive alert-commander cycles today (c261-c264), fresh-hit rate 0/4. NAME-SPACE mismatch, not a +1min offset -- consumer rounds to 5-min multiples, producer names by unrounded date -u +%H:%M. SEQUENCING: landing the producer-side rename ALONE makes THIS arm permanently, deterministically dead. Both arms must land together.
(c) MARKER_KEY (envelope e7bd57fd4): alert-commander derives <nominal_tick> inconsistently between its published-marker key and its notebook header (c263/c264 correct, c265 wrote 03:20 where nominal was 03:15). MARKER_KEY is a cross-tick dedup mechanism, so a mis-derived key silently stops the gate from gating; failure mode is a DOUBLE PUBLISH to a real channel. No damage yet. Folded here ONLY because the envelope claims the same one-line consumer fix closes both (b) and (c) -- architect MUST verify that claim before scoping and split it out if it does not hold.
FIX SHAPE UNCHANGED (settled at the migrated po_fold_20260825): drop the key lookup, take the NEWEST cycle-snapshot-*.json inside the <=7min freshness gate both copies already specify.
STILL DO NOT: (i) round the producer's filename to the nominal tick -- already withdrawn; it makes the producer's name collide with the prior day's EXACTLY rather than approximately. (ii) Widen the prune age threshold -- the prune window (1440 min) EQUALS the filename namespace period (1440 min) by construction, so every HH:MM name has a same-named prior-day twin alive in its final minutes; widening makes it worse.
RECURRENCE: the 6 cowork-fire envelopes this tick report cycle_snapshot_promoted=false on the 36th-41st consecutive tick. cycle-snapshot-latest.json still tick=00:00 created_at=2026-08-23T00:07:50Z (~75h stale).
RUNTIME MEASUREMENT 2026-08-26 (router, samples #10+#11). RETRACTION: an earlier version of this paragraph, written on sample #10 alone, claimed runtime "takes the newest cycle-snapshot-*.json". WRONG -- #11 refuted it. Scope from the lines below, not from this row's id.
MEASURED (producer mtime -> consumer lookup key): c281 nom 07:05 | wrote 07:05@07:05:30 | key 07:05 HIT. c282 nom 07:15 | wrote 07:20@07:20:06 | key 07:20 HIT. c283 nom 07:30 | wrote 07:34@07:34:37 | key 07:34 HIT. c284 nom 07:45 | wrote 07:49@07:49:46 | key 07:51 MISS.
CONSUMER KEYS ON ITS OWN WALL-CLOCK MINUTE AT BOOTSTRAP, MATCHED EXACTLY -- no rounding, no nearest-file fallback. c284 is decisive: its notebook reads "no cycle-snapshot exact-match for tick 07:51 (nearest 07:49, treated as absent)" -- it SAW a 2-min-old file inside the <=7min gate and rejected it on key mismatch alone.
ELIMINATED: newest-file (killed by c284); round-to-nearest-5 i.e. Copy A at cycle-bootstrap/SKILL.md L57-59 (killed by c283 -- 07:34 is not a 5-multiple); lookup-by-nominal-tick i.e. THIS ROW'S ID (killed by c282/c283/c284, keys 07:20/07:34/07:51 vs nominals 07:15/07:30/07:45). Only Copy B (raw own-clock, exact) survives all four.
RESTATE AS: producer names by ITS wall clock, consumer looks up by ITS wall clock, separated by dispatch latency -- hit only when the spawn lands inside the producer write-minute. Measured 3/4 today across 07:05-07:51. A coin flip on latency, NOT a fixed offset; do not scope it as one.
FIX SHAPE UNCHANGED, NOW POSITIVELY SUPPORTED: newest inside the <=7min gate. NOT a no-op ratification (the retracted note wrongly said so) -- it converts the c284 miss class into a hit. Each miss burns the two redundant MCP calls (~20KB) tick-snapshot.md says the snapshot exists to avoid.
COROLLARY, same file: cycle-bootstrap/SKILL.md L72 still says the writer "is a future task ... Step -1 will always miss" -- false since it landed. Fix in the same edit.
```
