# PO decision journal — triage 2026-08-08T17:37Z

task_id: FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER
cycle: dev-team Step 1 PO Triage (router-dispatched, tick=2026-08-08T17:37Z, triage_key=task:po-triage-20260808 held by router)
coordination session: 165f4245-6173-4054-87fd-c55bb626265f

## STEP po-1 — WF-2 SUPERVISED-HOLD released on the CRITICAL row, after ratifying at source

**what-considered.** The router handed me a CRITICAL chain (Telegram 4552/4553/4554 + dashboard
signal) saying mcp-server-1 was pinned at its 3GiB cap with VmHWM advancing, and a row held at
WF-2 SUPERVISED-HOLD whose `next_agent` was `ops` for a Docker rebuild. The obvious move was to
stamp `po_goahead` on QA's APPROVED verdict and let ops rebuild. I did not do that, because
`supervised-goahead.md` Step 2 forbids ratifying on a relayed verdict — the whole point of the
`supervised` + `po_goahead` pair is that PO is the ratifier of record.

**why-change (from the plan).** Ratifying at source changed the disposition materially. Three
checks, in order of increasing value:

1. Commits are real: `git merge-base --is-ancestor` PASS for b746c112b / 925641bb9 / 8b7a34674 /
   d24ddf6b6. This only proves the code is on main.
2. The fix is in the RUNNING IMAGE, not just the repo. `docker exec ... grep /app/src/interface/
   mcp/transport.ts` shows `interface SessionRecord` (:48), `reapStaleSessions` (:120),
   `stopReaper` (:234) and 8 `evictSession` sites; `server.ts` has the DELETE `closeSession`
   route. This matters because `apps/mcp-server/src/` is baked at build (Dockerfile:62 COPY, not
   volume-mounted) — reading the repo would have told me nothing about what is actually serving.
3. **THE REBUILD HAD ALREADY HAPPENED.** Image `sha256:630fa5d26275` Created 16:59:29Z, which
   post-dates fix commit b746c112b (16:57:46Z); container recreated 16:59:50Z, RestartCount=0.

So the row was being held at `ops` for a rebuild that was already done, while the CRITICAL alerts
it was blocking on described a state that no longer existed.

**Root cause of the alert chain, and why it is now closed.** Live at 17:59Z: MemPerc **11.26%**
(345.8MiB / 3GiB) at 57.8 min uptime, against a pre-fix container pinned at 96-99%. `/proc/1/
status` VmHWM=662168kB **>** VmRSS=348028kB — high-water above current RSS means reclamation is
happening, the exact inverse of the "VmHWM advancing / loss of reclamation" signature in report
4554. Reaper observed firing in container logs (`[SseSessionManager] Session evicted ...
"reason":"idle_timeout"` at 17:55:55Z, 17:57:55Z, 17:58:55Z) — a code path that did not exist
before this fix.

**The control that makes the above non-degenerate.** A memory drop alone is consistent with a
traffic lull, which would be a degenerate reading (`feedback_single_observation_degenerate_case_
read_as_broken_mechanism`). So I measured the construction rate too: 661 McpServer constructions
since boot with 22 resident = **96.7% reaped**, against this row's own pre-fix control of 4018
constructions / 873 resident at 6h31m = **78.3% reaped** with the residual 21.7% never reaped at
all. Rate is essentially unchanged (686/h now vs 616/h pre-fix). The residency collapse is the
reaper, not idle traffic.

**Peer-preservation check, because a rebuild is the fleet's most destructive routine action.**
All 12 peer containers untouched: rag-service up since 08:11:45Z, pdf-extractor 11:40:07Z,
stock-price 08-06T15:59:05Z, macro-indicators 07-29T23:58:26Z, frontend 07-24T18:02:39Z,
mcp-gateway 07-15T18:13:12Z, and 6 more at 07-15T15:16:48Z. Single-service scope confirmed; no
destructive `down && up` occurred (`feedback_rebuild_recreate_destroys_peers`).

**Decision.** Stamped `po_goahead_20260808T175954`; recomputed dev-team's own `should_hold`
predicate before and after (true -> false). Flipped `next_agent` ops -> qa, which is exactly the
leg QA's own 17:08Z note specified once Steps 1-4b were met.

## STEP po-2 — flipped `rebuild_required` true -> false, deliberately, at the FIELD level

**what-considered.** I could have left `rebuild_required: true` and written "do not rebuild" in
prose. That is what the row already effectively had, and it is a trap.

**why-change.** A structured field saying `true` next to prose saying "do not" is precisely the
contradiction class recorded in `feedback_a30_prose_overrides_embedded_escalate_verdict`. A
second rebuild would reset the 57-minute post-fix trajectory that QA Step 5 now needs — and this
row's own history already records TWO observation windows destroyed by unrelated rebuilds
(2026-08-06T23:21:41Z for TASK_603, and the 08-05 image churn). Removing the trigger is the
definitive fix; adding prose next to a live trigger is the recurrent-symptom fix.

**Noted, not minted.** The rebuild landed with no board attribution and no runbook ledger entry —
no commit in the 16:30-18:00Z window records an ops deploy. Verified-good by content, so this is
record-keeping and not safety. Single occurrence; logged rather than minted.

## STEP po-3 — unstuck FIX-MCP-MEMORY-CODE-LEAK by re-scoping an AC it could never satisfy

**what-considered.** Report 4553 asked for an expedite/interim-mitigation ruling on this row,
REVIEW since 08-05. The tempting move is to expedite QA. That would not have worked: the row's
own 08-07 raw-verify already recommended exactly what needed doing and it still sat for three
days, because the recommendation was written in a note and never applied to the ACs.

**why-change.** The row's title AC ("memory does not climb to 87% in 12h from a 5% cold start")
is not satisfiable by its deliverable at all — the residual climb was entirely the /sse leak, a
mechanism the `initDatabase()` WeakSet guard does not touch. I formally **transferred** the 12h
memory-ceiling AC and gate (c) to the SSE row, leaving this row with its one real AC: the
`backfillOCFForWatchlist` bootstrap-sweep signature probe. Measured live on the current image:
**1 occurrence since boot** vs ~52/10min pre-fix. **AC PASSES.** Guard verified at source too
(`/app/src/infrastructure/db/schema.ts:76` `new WeakSet<Database>()`, guard site :177).

Interim-mitigation question is moot: the contemplated mitigation was a restart, and the durable
fix is already deployed.

## STEP po-4 — rag-service chronic pattern: folded, and the "zero reclamation" premise corrected

**what-considered.** 4 reports (4551/4557/4558/4559) + a dashboard signal + 2 inbox envelopes,
all "99.66-99.79% sustained, zero reclamation dips, chronic 11+ signals over 4 days". Escalating
a 4-day chronic P2 was on the table.

**why-change.** Same discipline as STEP po-1 of the 16:00Z journal — check the reading first. Two
planes, measured live: cgroup **99.79%** (1024MiB/1GiB, cap verified real at 1073741824) BUT
`/proc/1/status` VmHWM=1568064kB (1.50GiB) vs VmRSS=894036kB (874MiB). The process sits **674MB
below its own high-water** — reclamation has demonstrably occurred. cgroup `memory.current`
counts reclaimable page cache; VmRSS does not. "Zero reclamation dips" is an artifact of the
plane A-30 samples, not a process fact.

This is the same two-plane discriminator that separated TP from FP on the 16:00Z pair: mcp-server
had VmRSS AT its all-time high-water (honest no-reclamation), rag-service has VmRSS far below it.
Opposite signatures, same probe, same tick.

**Decision.** Folded onto `FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`. Held
at P2/architect, **not** escalated: up 9h48m, RestartCount=0, zero OOM across the whole 4-day
chain. A container at its cgroup cap for four days with no OOM is one whose kernel is reclaiming
fine. No new mint; explicit do-not-restart.

## STEP po-5 — CI RED: 3 failing files, 2 minted, 1 folded

Mandatory pre-dedup failing-file read performed on both runs before any dedup was evaluated.

- run 31268871450 (head c5d6ab5e), size-lint: `coordinationStore.ts` (1241->1388, upper 1365) and
  `transport.ts` (126->237, upper 138).
- run 31267224323 (head 384ce899), bun test per-file isolation: 15071 pass / 1 fail,
  `FAILEDFILE: src/__tests__/1255-alert-off-hours-send.test.ts`.

`coordinationStore.ts` exact-matched the `dedup_key` on `FIX-CI-SIZELINT-COORDINATIONSTORE-
BASELINE-1388L` (READY/P0) -> folded, both observed SHAs appended, no mint. The other two had no
match in any non-terminal lane -> one row each, per the one-row-per-distinct-file rule.

**Worth recording:** the `transport.ts` size-lint regression is caused by b746c112b — the very
fix I ratified in STEP po-1. Stated plainly on the row rather than quietly bundled. Size-lint is
a hygiene gate with zero runtime semantics and does not gate the reaper deploy. I also flagged
the judgement explicitly for the implementer: repo precedent prefers extraction over a
justification header, but this growth is a consolidation that *removed* duplication, so the
header may genuinely be right here — the one thing that must not happen is re-fragmenting the
single-eviction-path design the fix exists to establish. That design is why `mcpServer.close()`
went missing from both pre-existing cleanup paths in the first place.

## STEP po-6 — sweep-guard: minted the missing actuator; AC-2a folded with an evidence upgrade

**what-considered.** 6 `[sweep-guard] BARE commit` fires, all `escalated=true`, one actor
(165f4245 — the live router session) climbing prior_warns **9 -> 10 -> 11 -> 12 -> 13** against
threshold=3 in 4h02m, plus one at 9 from actor 0454e9d8. Per the routing table `escalated=true`
is repeat-offender-after-block and mints. The question was whether it was already tracked.

**why-change.** Dedup across the FIX-COMMIT-SWEEP-*/FIX-SWEEPGUARD-* family found one open row,
`FIX-COMMIT-SWEEP-VICTIM-SELF-DETECT` — the *victim*-side detector, a different concern. Nothing
owns offender-side enforcement. Minted `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-
OFFENDER-13-STRIKES` (high, developer, cross-service/).

The precise gap is worth stating: `FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-
SCOPED-ACTOR` went DONE_VERIFIED earlier today and is exactly what makes these per-session counts
trustworthy — it fixed the **measurement**. The residual defect is the **actuator**:
`escalated=true` is computed right, emitted right, and then has no consequence. A counter that is
accurate and ignored is not an improvement on a counter that is wrong. I explicitly told the
implementer *not* to default to hard-blocking — a blocked commit on shared main can strand a
worker's staged index (`feedback_coldevict_bare_commit_sweeps_worker_staged_index`); auto-minting
one dedup-keyed row per actor per session may be the safer actuator.

**AC-2a, folded not minted.** `[notebook-immutability-guard]` fired on the SAME agent+section
pair — `system-auditor.md` section `## c50 · 2026-08-08T13:30Z` — on **three** separate cycles
(14:23:23Z, 14:34:58Z, 15:14:43Z). The flow's mint bar is >=2 cycles, so the bar is met. But
`CLEAN-NOTEBOOK-AC2A-CYCLE-BOUNDARY-DEFINITION` already owns AC-2a semantics, so I folded the
evidence there and raised it to high rather than fragmenting one defect across two rows. That
row's abstract finding ("AC-2a never defines cycle") now carries the concrete harm it predicts:
real notebook data loss. `qa.md` fired once — below bar, volume-logged only. The 2 `SAME-FILE
DIVERGENCE` fires are on high-contention shared artifacts (orch-state.json, qa.md); their FP
root-cause row is absent from every hot lane and from cold detail, i.e. shipped, so per the
flow's own branch these are candidate-genuine — but that detector is permanently
NON-GOAL/informational and must never block. Volume-tracked, no escalation.

## STEP po-7 — closed the report queue with dispositions, not acknowledgements

All 9 unresolved Telegram reports processed (4551-4559; 4558/4559 arrived mid-tick). `fixed` for
the mcp-server chain (4552/4553/4554) and the stale cold-evict report (4556, self-corrected by
d3b216cca); `duplicate` for the four rag-service views (4551/4557/4558/4559); `wontfix` for the
qa byte-cap roll (4555, designed behaviour). Report 4553's expedite request is answered in full
on the report itself, not just on the board — it asked PO a direct question and deserved a direct
answer.

On 4559 I left a note for system-auditor: minting a **new dedup_key for an unchanged condition**
is what turned one rag-service finding into 11+ signals over 4 days. The underlying gap is that
A-30 reads only the cgroup plane.
