# Decision Journal — Triage 2026-08-23T13:40Z · po

**Scope:** router escalation, single question — fb-market-poster weekend deliverable ruling (router session cac137dd-2565-424d-a9bc-e724c626ccd7, holding `intent:po:main`). Router BLOCKED a `slot=fb-weekend` manual dispatch at dispatch-claim Step 2.4 and asked PO to rule whether Sunday WEEKLY_PREDICTION is a real deliverable or the weekend is intentionally one post.
**Agent:** po
**Started:** 2026-08-23T13:40:26Z

---

### STEP po-U1 · po · 2026-08-23T13:40:26Z
**task-id:** (ambient — ruling on today's dispatch)
**what-done:** Confirmed the router's Step 2.4 block was a TRUE positive and that no action was owed on today's fb-weekend dispatch.
**what-considered:**
- Accept the router's classification.
- Re-derive it.
**why-decision:** Re-derived. `published:fb-weekend:2026-08-22` is live (claimed 2026-08-22T13:33:44Z, ttl 100800s, expires 2026-08-23T17:33:44Z) and Sunday STEP 0a computes MARKER_KEY = that exact string via `PERIOD_SAT = VN_DATE - 1d`. The agent's own gate is EXACT-match, so a spawn would have self-exited no-op regardless of the router — this is not the TTL>cadence false-positive class (`feedback_step24_cowork_collision_probe_ttl_exceeds_cadence_daily_false_positive`), where the blocking key belongs to a PRIOR period. Here it is the current period key. Router was right on both counts.
**why-change:** no change from plan.

### STEP po-U2 · po · 2026-08-23T13:42:38Z
**task-id:** FIX-FB-WEEKEND-MARKER-KEY-SATURDAY-ANCHOR-SUPPRESSES-SUNDAY-PREDICTION
**what-done:** Ruled Sunday WEEKLY_PREDICTION a REAL deliverable; declined the cleanup branch; minted the fix row (backlog, agent-father, P1).
**what-considered:**
- Honour the flow-doc prose (`weekly-recap.md:45-47`, `weekly-prediction.md:48-50`, `daily.md:81`: shared Sat-anchored key is intentional) → the MODE ROUTER, the `13 13 * * 6,0` cron and the second sub-flow are dead weight → mint a CLEAN row.
- Overturn the prose → Sunday is a real deliverable → mint a FIX row.
- Treat it as a genuine product question needing a fresh ruling from first principles.
**why-decision:** Neither judgement call was needed — the archive settles it as a fact. The originating row `FIX-FB-WEEKEND-DEDUP-GATE` (archive/backlog-detail.json, created 2026-06-30T17:28:32Z by po) approved this fix_spec verbatim: `task_claim(task_id='published:fb-weekend:'+VN_DATE, ...)`, with verification_gate "a second weekend firing IN THE SAME VN-DATE no-ops". VN_DATE, not PERIOD_SAT. Its root_cause names the threat as the launchd firer and the cowork */15 dispatcher double-firing the SAME day — never Sat-vs-Sun. Commit 11e10fd20 (same day) shipped PERIOD_SAT instead, widening the dedup period 1 day → 2, and wrote the justifying prose into three flow docs as if it were the requirement. So the "documented intent" the router flagged is the defect self-documenting, not an approved product decision — nothing to overturn, only a deviation to revert. Corroborating, independently: (a) `weekly-recap.md:8` forbids the Sunday content outright ("EXPLICITLY NO prediction ... NO 'Du doan' section"), so Saturday cannot absorb the deliverable — suppression deletes it; (b) the catch-up reading is refuted by config, `_dish_type_catchup_config.fb_weekly_post.catchup_max_lateness_minutes = 120`, and Sunday's fire is ~24h late, so the Sunday cron leg is a second deliverable and not a Saturday retry; (c) the notebook at 3e08bf39d logs 07-11 WEEKLY_RECAP and 07-12 WEEKLY_PREDICTION both `Status: published`, two normal cycles in one weekend. The originating row never left REVIEW and its QA gate FB-LAUNCHD-QA-FIRE-VERIFY-DEDUP is still BACKLOG, which is why the deviation ran unchallenged for 8 weeks.
**why-change:** Router offered "fold into the P0 sibling" as the likely disposition; declined — see po-U3. Router also asked PO to pick the fix axis or route it to agents-architect for a ruling; neither is needed once the approved spec is in hand, so the row ships to agent-father as an S-sized revert with no design cycle.

### STEP po-U3 · po · 2026-08-23T13:42:38Z
**task-id:** FIX-FB-WEEKEND-MARKER-KEY-SATURDAY-ANCHOR-SUPPRESSES-SUNDAY-PREDICTION
**what-done:** Kept it a separate row rather than folding into FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE (P0, READY, agents-architect); cross-linked both ways.
**what-considered:**
- Fold — same published-marker family, same agent, same architect, one dispatch.
- Separate rows, cross-linked.
**why-decision:** Separate. Different surface, different blast path, different root, and the P0 row's own text excludes this case: its consumer_impact reads "bounded to router-MANUAL dispatch ... the cron path uses a separate fire-election and is unaffected". This defect is the AGENT's STEP 0a EXACT-match self-gate against a CURRENT-period marker and it kills the CRON path — the leg that actually fires every Sunday. None of the P0's three ratified axes repairs it: (A) exact-period-key is already what the agent does and leaves Sat/Sun sharing one key, a no-op here; (B) TTL<=cadence and (C) stale-owner presence check would unblock Sunday only incidentally, and only when the Saturday holder session happens to be dead. Folding an S-sized revert into a P0 carrying 6 ACs, 3 rejected axes and a standing PO rescope ruling would bury it behind a design cycle it does not need. Recorded the sequencing so the split does not read as duplication: once VN_DATE keys land, fb-weekend joins the exact 28h-TTL-over-24h-period class the P0 describes, so the P0 becomes the follow-on for fb-weekend MANUAL dispatch — a successor, not a prerequisite.
**why-change:** no change from plan.

### STEP po-U4 · po · 2026-08-23T13:42:38Z
**task-id:** FIX-FB-WEEKEND-MARKER-KEY-SATURDAY-ANCHOR-SUPPRESSES-SUNDAY-PREDICTION
**what-done:** Rejected the per-mode key axis in AC-6 before any implementer could reach for it, and added AC-5 for a third defect site the router had not found.
**what-considered:**
- Per-mode keys `published:fb-weekend-recap:` / `published:fb-weekend-prediction:` (the router's first-listed candidate).
- Plain VN_DATE per the approved spec.
**why-decision:** Per-mode keys silently blind the router's collision guard. `.claude/skills/dispatch-claim/SKILL.md` Step 2.4 prefix-matches `"published:" + slot_id + ":"` — with the trailing colon. `published:fb-weekend-recap:2026-08-22` does NOT start with `published:fb-weekend:`, so BOTH weekend markers would become invisible to Step 2.4 and the double-publish guard would fail open with no error. VN_DATE preserves the prefix exactly. If mode granularity is ever genuinely wanted it must be a SUFFIX (`published:fb-weekend:<date>:recap`), and AC-6 records that so the next reader does not rediscover it the hard way. AC-5: `cowork-schedule.json .slots[fb-weekend].publish_date_basis = "vn_date_saturday_anchor"` is a third site of the same Saturday-anchor conflation and would keep writing the wrong date onto the Sunday file even after both flow docs are fixed; flagged with the routing caveat that that file's own `_maintained_by` says "agent-father (via architect brief only)", so agent-father escalates rather than editing in place if that constraint still binds.
**why-change:** Added AC-3 forbidding conversion of the agent-side gate to a prefix match — that is the sibling row's surface and importing it here would trade this bug for that one.

### STEP po-U5 · po · 2026-08-23T13:42:38Z
**task-id:** (ambient — cycle scoping)
**what-done:** Ran a SCOPED tick: ruling + mint + journal + notebook + commit only. No TNB audit, no channel audit, no signal sweep, no supervised-goahead, no manual-dispatch sweep.
**what-considered:**
- Full `po/flow/main.md` pre-flight chain as written.
- Scoped to the router's single question.
**why-decision:** Scoped. The router spawned for one bounded decision and holds `intent:po:main` for this spawn only; the pre-flight chain is the cron-tick path and re-running it risks the `feedback_datatier_subagent_rereads_stateful_pregate_after_router_spawn_gate` class plus a race with a concurrent cron PO (`feedback_phaseb_intent_key_is_freeform_so_two_sessions_run_same_agent_concurrently`, live 2026-08-23). A prior PO tick already drained the durable inbox at 12:48Z. The scoping is recorded in the minted row's `status_note` so the next tick does not read this cycle as a completed full triage.
**why-change:** no change from plan.
