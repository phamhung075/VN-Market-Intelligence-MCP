# PO Decision Journal — review[] SECONDARY-drain sign-off triage — 2026-08-13T16:29Z

Session (coordination param): `632721c2-41e4-4aff-8d06-a47cf80dc0d7`
Row: `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` (review[], REVIEW, branch:null, redispatch_count=1)
Inputs: live `.signal_queue.rows[]` (30 rows / 25 `to=po`) · 3 cold archives · `orchStateSchema.ts` · DRS allowlist · successor row

---

## D-1 — Verdict: **CHANGES_REQUESTED**, next_agent `po` → `architect`

- **task-id:** FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES
- **what-done:** Re-ran the row's own AC-5 guard (`triage-signals.md § guard_signal_type_coverage`) verbatim against the CURRENT live board, re-derived the cold-archive counts the row's AC-2 rests on, and read the emitter-side schema. Rejected sign-off; reassigned to `architect` with a narrowed 2-item mechanism-only residual.
- **what-considered:**
  - (a) DONE_VERIFIED — the row's *headline* defect (0/16 match) is genuinely fixed.
  - (b) CHANGES_REQUESTED back to `agent-father` (last implementer, artifact owner of `docs/agents/*`).
  - (c) CHANGES_REQUESTED reassigned to `architect` (mechanism/design, on the DRS allowlist).
  - (d) BLOCKED escalation.
- **why-decision:** (c). Two of the row's own ACs fail on live evidence I measured this tick, so (a) is dishonest. But (b) reproduces the exact loop that has already run four times — and worse, it *strands* the row: DRS's ratified allowlist is `{architect,ba,pm,po,agents-architect}` (`bounded1-supervised-lane-report.sh:171`) and `agent-father` is deliberately off it, which is why the successor row has sat BACKLOG for 7 days with no picker. `architect` is on the allowlist AND artifact-correct — the residual is no longer a table edit, it is a mechanism design spanning schema + guard + flow doc, which `po/init.md` `not_my_job` assigns to architect.
- **why-change:** Row was dispatched to me for sign-off; I am returning it as rework with a *different* owner and a *narrower* scope than it carried in.

## D-2 — The measurement: guard FAILS today, with a fourth completely different type set

- Ran `guard_signal_type_coverage` byte-identical → **FAIL, 9 unrouted `to=po` types**: `audit-handoff`, `bug-escalation`, `db_data_integrity`, `db_data_missing`, `db_data_stale`, `defect`, `flow-doc-review`, `improvement_proposal`, `memory_pressure`.
- **3 are guard false positives** (routed in the Pipeline-A table, absent from the hand-maintained `$routed` literal): `audit-handoff`, `bug-escalation`, `improvement_proposal`.
- **6 have no rule in ANY table** — `db_data_integrity` (CRITICAL), `db_data_stale` (CRITICAL), `db_data_missing` (HIGH), `defect` (HIGH + MED), `memory_pressure` (WARN ×2), `flow-doc-review` (LOW). 8 of 25 live `to=po` rows = **32%**, and it includes both of today's CRITICALs.
- **The 08-07 regression list has fully rotated out.** Of the 8 types that recheck named, only `bug-escalation` is still present. `.signal_queue.rows[]` holds a ~24h window (min ts `2026-08-12T16:31:25Z`). Counting the prior recorded runs, the unrouted set has now been measured **different on every one of 5+ independent ticks** (08-06, 08-07, 08-08, 08-11, 08-12, 08-13). No one-time table patch can converge against that.
- 23 of 25 live `to=po` rows are `READ`/`OPEN` with `disposition == null` right now.

## D-3 — Root cause, newly established this tick (not previously recorded on either row)

- **The producer namespace is unconstrained free text.** `apps/mcp-server/src/infrastructure/orchStateSchema.ts` `SignalRowSchema` declares `type: z.string().optional()` — no enum, no registry, `.passthrough()`. Any agent invents any token at emit time. Confirmed: all 6 genuinely-unrouted types appear **nowhere** in `docs/agents/`, `.claude/skills/`, or `apps/mcp-server/src/` — they are authored per-emission.
- **Measured scale of the mismatch: 101 distinct `to=po` type tokens have existed (hot + 3 cold archives); the guard's hand-maintained `$routed` covers 16 = 15.8%.**
- Therefore an open producer namespace is being policed by a closed, hand-maintained consumer allowlist. That is structurally non-convergent, and it is why this row is on its 4th pass. **The fix must invert: bound the producer, or make the unknown-type fallback a real disposition instead of `log and skip`.**

## D-4 — AC-2 is genuinely unmet: two prose claims in the delivered artifact are falsified

`triage-signals.md:35` (Cold-archive cross-check) makes two load-bearing claims I checked directly against `docs/data/orch/archive/2026-0{6,7,8}.json`:
1. *"`system_issue` underscore predates the live `system-issue` hyphen form … historical artifact, not a live routing gap"* — **inverted.** Underscore fired `2026-08-11T03:00Z` (6 rows, 2026-08 archive); hyphen's own range is `2026-07-23 → 2026-08-12`. Both spellings are concurrent and the underscore is the *later* fire. 111 lifetime `to=po` fires.
2. *"the remaining ~40 cold-archive types are ≤1-2 fires each"* — **false for 16 types**, incl. `system_issue` 111, `repair_task_request` 22, `cron_fire_gap` 19, `agent_flow_defect` 9, `audit-handoff` 7, `defect` 5, `recurring-bug-corroboration` 5.
- This is not stale prose: it is an active instruction that tells the next editor to *skip* the largest live class. Two prior PO ticks (08-11T1237Z, 08-11T1437Z) already asked for this correction; it is still in the file 2 days later. **Cheapest, most certain item in the residual.**

## D-5 — What I am NOT re-litigating (credit where the work landed)

- **AC-3 MET** — `system-issue` first-class rule present (`triage-signals.md:46`).
- **AC-4 MET** — signal-dashboard SKILL Receivers + `triaged`/`RETRACTED` status enum reconciled (QA-verified 08-07).
- **AC-6 MET** — 77L + 22L, well under cap.
- **AC-5 mechanism sound** — the predicate correctly FAILs live and QA verified its synthetic negative control. What is unmet is AC-5's *purpose* ("so the drift cannot silently return"): nothing invokes it. A repo-wide grep finds the guard referenced only in its own doc and in PO decision journals where an agent ran it by hand. Today's earlier PO tick (11:37Z) hand-routed the two `db_data_*` CRITICALs off-table without running it at all.
- **AC-1 met only against the frozen 2026-07-31 snapshot** it names; all 16 of those types are routed today. As a standing invariant it fails.

## D-6 — Un-stranding the successor instead of duplicating it

- `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` (backlog[], P1) already carries ~10 independent measurements and the correct AC-4 ("nothing schedules the guard"). It is the evidence dossier; I am not re-minting its content.
- But its `next_agent=agent-father` is off the DRS allowlist, so **no automated lane has ever been able to pick it up** — 7 days BACKLOG, its own `po_dispatch_blocker_note` says exactly this. Repointing it to `architect` puts it in DRS's auto-dispatch set.
- Both rows now resolve to `architect` so they land in one pass. I did **not** move either lane: the review[] row stays in `review[]`, where `devteam-review-claim-secondary-drain.jq` selects `status=="REVIEW" AND effective_next_agent != "qa"` age-ordered — the same picker that delivered it to me. No `.head` write, no lane move, no dup-key risk.
