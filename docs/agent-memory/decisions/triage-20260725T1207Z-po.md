# PO Triage — dev-team tick 2026-07-25T12:07Z · head-wedge break + 6-row BATCH

**Agent:** po · **Referred by:** dev-team Step 1 (cron tick), router session `e3a3f331`
**Journal note:** standalone triage record (precedent: `triage-*Z-po.md` in this directory).
Three board writes were made this tick; each is recorded as its own STEP below.

---

### STEP po-S174 · po · 2026-07-25T12:30:31Z
**task-id:** FIX-EXECTIER-HEADSYNC-BRANCHNULL-REVIEW-IDLE (contract restoration, not a flip)
**what-done:** Reset `.head` from `{status:"review", active_task_id:"FIX-PREDCLAIM-CREATIONPRICE-UNGATE-ZOD-CONTRACT", next_agent:"qa"}` to `{status:"idle", active_task_id:null, next_agent:"router"}` via `scripts/orch-apply.sh`.
**what-considered:**
- (a) leave head as-is and let the next tick handle it — rejected: `status=="review"` matches NO
  lane in dev-team Step 0b (it handles in_progress / idle / done / missing / v1). The whole
  BOUNDED-1 → SLS → RLC → QA-Drain chain is head-idle-only, so "the next tick" is the same dead tick.
- (b) route it as a batch row and wait for a dev fix — rejected: that is the state the board has
  been in since 07-23. `in_progress=0`, `qa=0`, `ready=44`, `review=107`. Zero throughput.
- (c) reset head to idle — CHOSEN.
**why-decision:** This is not a judgement call, it is a contract violation being repaired.
`FIX-EXECTIER-HEADSYNC-BRANCHNULL-REVIEW-IDLE` SHIPPED on 2026-07-23 (commits `020b9eeeeb`,
`7b6d878ab`) and amended `execute-tier.md § MUST (b)` to require exactly
`head={status:idle, active_task_id:null, next_agent:router}` for a `branch:null` REVIEW flip.
I verified the predclaim row is `branch:null` (all 104 REVIEW rows are). So the live head value
was already non-conformant with a shipped rule.
**what-broke:** Nothing is lost by the reset — the predclaim row remains in `review[]` with
`next_agent=="qa"` and is reachable through QA-Drain. Head was a duplicate pointer, not the
only pointer.
**why-change:** The router explicitly delegated this disposition ("Router did NOT write to
`.head` — that disposition is yours").

---

### STEP po-S175 · po · 2026-07-25T12:31:47Z
**task-id:** TE-T27
**what-done:** Annotated with `po_triage_20260725` — sharpened the root cause of the
signal_queue PRUNE defect from "stale procedure" to "status-vocabulary mismatch". Did NOT mint
a new row.
**what-considered:**
- (a) mint a new FIX for "archive[] is empty" — rejected. Prior art exists (TE-T27 +
  `P1-DETECTOR-CLOSURE-TASK-ARCHIVE`) and `feedback_file_prior_art_check_before_minting_row`
  applies. Also `archive==0` alone is AMBIGUOUS evidence: the SKILL.md frontmatter states
  HSC-7 removed the `archive[]` lane in favour of cold-file eviction, so an empty archive is
  partly expected by design.
- (b) annotate the existing row with the decisive evidence — CHOSEN.
**why-decision:** The decisive datum is not the empty archive, it is set-disjointness.
`SKILL.md:90` prunes `status IN (READ, RESOLVED, SUPERSEDED)`; the live queue holds only
`triaged` (124) and `RETRACTED` (1). The predicate matches zero rows *by construction*, not by
timing. That materially changes the fix: naively widening the prune set to include `triaged`
would archive rows that were never actually consumed. The real question — which no existing row
asks — is whether any writer ever executes the ACK/CLOSE transitions at `SKILL.md:80-81` at all.

---

### STEP po-S176 · po · 2026-07-25T12:32:05Z
**task-id:** FIX-PREDCLAIM-BACKFILL-NULL-CREATIONPRICE
**what-done:** Removed the `depends` field, raised P1→P0, narrowed the deadline-bearing scope to
deliverable (a) only, recorded `po_ruling_20260725`.
**what-considered:**
- (a) honour the declared dependency on `FIX-PREDCLAIM-CREATIONPRICE-UNGATE-ZOD-CONTRACT` —
  rejected. That row's live acceptance is `REBUILD_REQUIRED`; rebuilds are user-gated and no
  authorization exists in this session. The dependency is not slow, it is **unsatisfiable**
  before the deadline.
- (b) wait until 07-27 and invoke the row's own escape clause then — rejected. Waiting is not
  free: the resolver runs daily at 16:35Z and burns one pending claim per day (id 13 on 07-28,
  id 14 on 07-29, ids 15/16/17 on 07-30 and 07-31). Deferring to T-1 buys nothing and risks the
  tick that authorizes it never arriving.
- (c) break the dependency now — CHOSEN.
**why-decision:** The row's own `note` already authored this exact clause ("the 2026-07-28
deadline outranks the dependency"). I am invoking it early, not inventing it. `depends` is
deleted rather than overridden so BOUNDED-1/RLC cannot silently re-block on it.
**what-broke:** Scope narrowing is deliberate — deliverables (b) reconstruct-the-6-excluded and
(c) machine-readable exclusion reasons are NOT deadline-bearing. Deliverables (d) persist as a
`scripts/` script and (e) never fabricate a price REMAIN BINDING on the standalone run.

---

### STEP po-S177 · po · 2026-07-25T12:33Z
**task-id:** (batch authoring — 6 new rows returned to dev-team, not written to the board)
**what-done:** Returned a 6-entry BATCH. Ranked #1 = write-time enforcement of `head.status`.
**what-considered:**
- (a) treat the head wedge as a one-off and just reset it — rejected. It was already "fixed"
  on 07-23 and the fix was violated 2 days later by `dev-mcp-server`. Per
  `feedback_recurring_detection_vs_recurring_failed_fix`, a re-occurrence after a shipped fix is
  a DIFFERENT bug from the original.
- (b) re-open the 07-23 row — rejected: it is stranded in `review[]` and its shipped artifact
  (a flow-doc paragraph + a synthetic-fixture verify script) is precisely the artifact class
  that `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` AC(3) already warned is not acceptance.
- (c) mint a new row that moves enforcement from PROSE to the WRITE GATE — CHOSEN.
**why-decision:** I verified `HeadSchema.status` is `z.string()` with `.passthrough()` — there is
no enum, no validator, no gate anywhere. The only thing standing between the board and a wedge is
whether the writing agent happened to read `execute-tier.md`. `dev-mcp-server` did not. Since
CLAUDE.md already makes `scripts/orch-apply.sh` the mandatory single door for every orch-state
write, a guard there is unbypassable and cannot be forgotten.
**second finding of the same class:** `scripts/audits/devteam-review-lane-drain-report.sh`
printed `[PASS]` live this tick with 75 undrained PRIMARY rows and `qa[]==0`. Two independent
defects in one predicate: (i) it is a disjunction, so one fresh arrival green-lights the lane;
(ii) it ages rows by `updated_at`, a field bumped by ANY edit — including this very triage —
so it measures *edit recency*, never drainage. Both must be fixed or the instrument keeps
certifying the failure it exists to catch.
