# Decision Journal — Sprint FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK · dev-mcp-server

**Sprint goal:** Stop legal_risk CRITICAL alerts re-firing every alert-commander cycle for up to 30 days (qa a522cb30f finding from FIX-ALERT-COMMANDER-DEAD-NO-SLOT gate)
**Agent:** dev-mcp-server
**Started:** 2026-07-03T17:20:00Z

---

### STEP dev-mcp-server-S1 · dev-mcp-server · 2026-07-03T18:10:00Z
**task-id:** FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK
**what-done:** Two-tier fix. QUICK (no deploy): `docs/agents/alert-commander/flow/stage-bootstrap.md` call site now bounds `get_legal_risk_signals(days=1, hours_back=6)` (was bare, no args). ROBUST (deploy-gated): (1) `legalRiskTools.ts` gained an additive, opt-in `hours_back` param overriding the shared `days` cutoff with hour-granularity, applied to both the `alerts` table and `agent_signals` sources via a shared `computeCutoffIso()` helper; (2) `alertVerdictTools.ts` `writeAlertVerdict()` gained a dedup guard — skips appending a new pending row when one already exists for the same `(ticker, alertSource)` pair with `verdict==='pending'`, returning the existing row with `duplicate:true` instead.
**what-considered:**
- Lower the shared `days` DEFAULT (30→1) globally in `legalRiskTools.ts`: REJECTED. Verified via grep that bctc-analyst, digest-predict, unified-agent (x2) and fb-market-poster (x2) all call `get_legal_risk_signals()` bare, relying on the 30-day default breadth for evidence-generation / summary purposes unrelated to alert-firing (fb-market-poster explicitly documents "T-45 adversarial governance checks"). Narrowing the shared default to 1 day would silently starve their visibility — a new regression traded for fixing this one.
- Global read-state / "mark as read" dedup on `agent_signals` rows (reusing the existing `status` column the way `get_agent_signals` already does): REJECTED. `get_legal_risk_signals` is called by 6 different agent flows (grepped `docs/agents/**/flow/*.md`), several of which read the SAME underlying agent_signals rows for non-firing purposes. A global read-mark side effect would mean whichever agent calls the tool FIRST in a cycle "consumes" the signal and starves the others — a genuine cross-agent visibility bug, not a fix.
- Scoped already-alerted tracking keyed by `(caller_agent, row_id)`: investigated but requires exposing the underlying row id through `get_legal_risk_signals`'s plain-Vietnamese-text response AND wiring `record_signal_outcome(signal_id, "fired")` into stage-bootstrap.md's Step 2 (currently only documented generically at Step 4a in stage-dispatch-log.md, with no id plumbing from Step 2's legal_risk hit). This is real follow-up work but is a flow-level restructuring beyond a surgical FIX task — flagged as a candidate follow-up, not implemented here.
- `hours_back` naming/value: chose to mirror the ALREADY-ESTABLISHED codebase convention — `agentSignalTools.ts` `get_agent_signals` hours_back doc explicitly states "hours_back=6 covers the 360-min legal_risk dedup window"; `__tests__/1948e-b-legal-risk-dispatch.test.ts` uses `windowMinutes=360` for the news-scout-side legal_risk POST dedup. Reusing `hours_back=6` for the READ side keeps the whole legal_risk dedup story consistent at one canonical number.
- QUICK-tier flow edit written to pass BOTH `days=1` and `hours_back=6` in the SAME edit (forward-compat): verified MCP SDK's zod-compat layer (`node_modules/@modelcontextprotocol/sdk .../zod-compat.js` → `objectFromShape` → non-strict `z.object(shape)`) silently strips unrecognized keys — so the CURRENTLY deployed server (pre-`hours_back`) safely ignores `hours_back` and only honors `days=1` today; once the ROBUST tier deploys, `hours_back=6` takes precedence automatically with no second flow edit needed.
**why-decision:** The shared `get_legal_risk_signals` tool is a genuinely multi-consumer bus-read tool, not alert-commander's private API — any fix that changes default/shared behaviour risks starving the other 5 legitimate consumers. Scoping the fix to an additive, opt-in param that ONLY the caller with a re-fire problem uses is the safest surgical change. `write_alert_verdict` dedup is fully self-contained (only alert-commander calls it, ticker+alertSource are always available at fire time) and directly satisfies the explicit "add dedup to write_alert_verdict" ask.
**why-change:** No change from PO/QA-dictated scope — this is exactly the two-tier fix requested. One scope reduction: did NOT implement full row-level "fires exactly once" semantics inside `get_legal_risk_signals` itself (would require flow-level id-plumbing beyond this FIX task's surgical scope); documented as a known limitation. The `hours_back=6` bound + `write_alert_verdict` pending-dedup together reduce the re-fire window from "up to 30 days, unbounded repeats" to "at most a handful of repeats within a 6h window, with duplicate pending-verdict bookkeeping suppressed" — a large, honest improvement, not a claimed-perfect fix. Repeat-fire-suppression test added at the `write_alert_verdict` layer (TC4 in `FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK.test.ts`) — the layer where "fires once" is actually, fully guaranteed by this change.
**verification:** `bun tsc --noEmit` clean. New test file 7/7 pass. Adjacent legalRisk/alertVerdict test files (1940a, 245-mcp-tools-039, 1863d, c220, 1967-01, 1903a, 1948e-b, 1968b1, 1872b, TSU-DEV-U3) — 67/67 pass, 0 regressions. Tool count unchanged (183, matches `docs/data/project-stats.json` baseline — no `server.tool()` call added/removed).
