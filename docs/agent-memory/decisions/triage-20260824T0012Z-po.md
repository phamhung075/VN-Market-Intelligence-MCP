# PO decision journal — Step 0-SIG triage, dev-team tick 2026-08-23T23:37Z (router-direct)

Written: 2026-08-24T00:12:36Z · agent: po · flow: docs/agents/po/flow/main.md → docs/agents/po/flow/triage-signals.md § Step 0-SIG · session 007e33e4-b453-4bb3-8ab1-ef31495906a3

Inbox read fresh at source (12 envelopes, 6 types). ONE `orch-apply.sh` write: 2 mints, 2 folds, 1 lane-move + retarget, 7 signal-row retractions, partial CLEAR (8 of 12). `task_total` 809→811, `signal_total` 73→73, `inbox_row_identity=clean`.

---

## STEP po-1 — [task_id: FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID] Minted; refused the dedup target that looks right; refused to name the actor

**what-considered:** (a) fold into `FIX-SIGNALQUEUE-DUP-ID-GUARD` (backlog, developer, cross-service/, already owns an orch-apply.sh guard for `signal_queue.rows[]`); (b) fold into `FIX-EMITSIGNAL-DEDUPKEY-GRAMMAR-UNVALIDATED-CALLER-FREETEXT-DEFEATS-7D-WINDOW`; (c) let `CCATO-MCP-T6/T7/T8` absorb it, since the whole gate is mid-migration into mcp-server; (d) mint a distinct P1 row at the `orch-apply.sh` choke point.

**why-change:** Chose (d). (a) is the nearest neighbour by subject and is **structurally unable** to catch this family, which is the whole reason it needed its own row: the ids are `ntg-{compact_ts}-{dimension}-{ticker}-{uuid4()[:6]}`, so all 42 live rows are id-unique **by construction** and an id-uniqueness guard accepts every one of them; and its second dimension, "normalize auditor ts to full ISO8601", cannot help either — the `ts` here is already well-formed ISO8601, it is simply **false**. A format normaliser cannot detect a wrong value. (b) is the same dedup disease in a different organ (`scripts/emit-audit-signal.sh` + the auditor dedup ledger); this path has no `dedup_key` at all to validate. I cited it on the row anyway because its AC-3 fail-mode analysis and its `po_retarget` argument ("a fourth prose instruction to an LLM caller is a known-failed fix") both transfer verbatim. (c) fails because T6 registers a tool, it does not authenticate a writer — and the TS writer it exposes reproduces both the missing-`dedup_key` and the hardcoded-summary defects **faithfully**, because CCATO-MCP-T4's own ACs demanded byte-faithfulness to the bash row and QA certified that equivalence explicitly.

**Everything on the row was re-measured at source this tick, not inherited from the spawn brief.** The brief said "6 rows → 4 distinct fingerprints"; the live queue is worse — **42 rows → 4 fingerprints**, multiplicities 14/14/7/7, `has("dedup_key")` false on all 42.

**Two independent proofs that neither sanctioned engine produced these rows.** This is the load-bearing part of the row and it is what separates it from every neighbour.

1. **The `ts` is not a clock read.** All 42 carry `2026-08-24T00:00:00Z`, and so does `.signal_queue._updated_at`. Traced the count through git: `b0695c074` = 0 rows (committed 00:32:24+02:00 = 22:32Z), `4145766ac` = 18 (00:49:52+02:00 = 22:49Z), `1e476e889` = 36 (01:46:52+02:00 = 23:46Z), `b039090c3` = 42 (01:58:32+02:00 = 23:58Z). **Three append batches spread over 86 minutes, every row second-identical.** `scripts/narrative-truth-gate.sh:409` uses python `datetime.datetime.now(datetime.timezone.utc)`; `narrativeContradictionSignalWriter.ts:41-66` uses `new Date().toISOString()`. Neither can emit the same second three times an hour apart. The host is CEST (+02:00), so that string is the **local date's midnight wearing a `Z` suffix**.

2. **Seven rows are unreachable through either engine's code path.** They carry `payload.returned_value = "not found in database"` — a **verbatim member** of `docs/data/claim-tool-map.json .tool_null_markers` (10 entries; it is the 5th). `scripts/narrative-truth-gate.sh:252`/`:270` classify any `tool_null_markers` substring hit as NULL (an honest data gap), and the emit loop at `:414` skips every verdict whose `result != "FAIL"`. `verdictClassifier.ts:63-69` ports the same SSOT check. A FAIL row carrying that value cannot be produced by the code.

**Deliberately did NOT name the actor.** I proved the rows did not come from either engine's code path; I did **not** establish who composed them, and the row says so in AC-1 in writing. `_updated_by` is a `--arg who "narrative-truth-gate"` literal at `narrative-truth-gate.sh:446`, so anything that can run `jq | orch-apply.sh` can wear any producer's name — that unauthenticated-provenance property **is** the finding, not an incidental detail of it. Guessing "an agent hand-composed it" would have been the `feedback_auditor_dedup_citation_resolves_to_nonexistent_row_id` shape: right conclusion, invented reasoning. AC-1 is written as a blocking step with named evidence sources precisely so the implementer cannot skip it.

**Routed to `developer` / `cross-service/`, not to the gate's own owner.** Every signal-queue write in the fleet passes through `scripts/orch-apply.sh`. Patching the two emitters leaves the append path open to the next producer; patching the choke point closes the class. Same reasoning the `FIX-EMITSIGNAL-...` row's `po_retarget` note already ratified for this project.

## STEP po-2 — [task_id: 7 × `ntg-*` signal rows] Retracted, with the citation on each row

**what-considered:** (a) leave them at `READ` and let cold-evict take them; (b) mark them `triaged` with a disposition; (c) `RETRACTED`.

**why-change:** Chose (c). `.claude/skills/signal-dashboard/SKILL.md` defines `RETRACTED` as exactly this — "verified false positive, no action taken" — and it is in `orch-cold-evict.sh`'s `TERMINAL_SIGNAL_STATUSES` default, so it does not strand. (a) is wrong on two counts: the rows are a standing **false accusation against chef** (their own payload confirms his absence claim while their summary says he contradicted it), and the `>24h` age gate cannot fire on a **future** `ts` until 2026-08-25T00:00Z, so "it will evict itself" is not true this cycle. (b) understates a provable falsehood.

Wrote `retraction_reason` on each of the 7 with the file:line citations, so the evidence survives the eventual prune — the row that owns the fix (STEP po-1) would otherwise lose its exhibit. Left the other 35 alone deliberately: their genuineness is **also** unverified (same provenance problem), and collapsing them is AC-8 of the minted row, not a PO action.

## STEP po-3 — [task_id: TASK_001-WATCHLIST-WRITE-THROUGH-INFRA] Lane-moved and retargeted, because a `status_note` would have changed nothing

**what-considered:** (a) add a `status_note` and leave it in `qa[]`; (b) mark it BLOCKED in place; (c) move `qa[] → ready[]` with `next_agent=ops`.

**why-change:** Chose (c). **Re-verified qa's refusal independently rather than trusting the handoff prose:** `docker image inspect vn-market-intelligence-mcp-mcp-server --format '{{.Created}}'` = `2026-08-23T13:52:50Z` (container created 2026-08-23 15:53:58 +0200 = 13:53:58Z); the dev commit `ce1f52127` landed 2026-08-23 17:11:08 +0200 = **15:11:08Z**. The running image **predates the code by 1h18m**. AC-7 (`docs/handoffs/TASK_001-WATCHLIST-WRITE-THROUGH-INFRA.md:65,126,148` — bind-mount round-trip via a real `add_to_watchlist` call from **outside** the container) is therefore not merely unverified, it is **structurally unrunnable**. The row is DEPLOY-blocked, not verify-blocked, and every qa dispatch reproduces the same correct refusal.

**The loop is measured, not predicted:** qa refused at commit `6163fecee`, 2026-08-23T22:08:36Z; the review-lane QA-drain re-claimed it **81 minutes later** at `claimed_at` 2026-08-23T23:29:49Z. One full cycle already observed.

(a) was the instruction I was given and I did not follow it as written, for a documented reason: the QA-drain picker selects on **lane + `next_agent`** and is blind to prose it cannot read (`feedback_pickers_blind_to_prose_status_note_autoclaim_contraindicated_row`). A note in `qa[]` with `next_agent=qa` would have re-picked on the next tick regardless. I wrote the note **and** moved the row, so the prose is the audit trail and the fields are the actuator. (b) BLOCKED would be false — nothing is blocking; a single-service rebuild unblocks it.

`next_agent=ops` is off the DRS-ratified auto-dispatch allowlist `[architect,ba,pm,po,agents-architect]`, so this row is reachable by manual/router dispatch only. That is deliberate and is stated on the row; it is the same class as the 10 other live `next_agent=ops` rows. The note carries both rebuild hazards explicitly — never `docker compose down` (`feedback_rebuild_recreate_destroys_peers`), and verify the image ID actually changed before claiming the deploy landed (`feedback_docker_rebuild_race_pgrep_deadlock`).

## STEP po-4 — [task_id: FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH] Folded, premise re-measured a third time

**what-considered:** only path — the routing table for `auto-push-abort` mandates re-measuring the premise before acting and the row already carries `dedup_key: "auto-push-abort:push-fail"`.

**why-change:** no change from plan. Re-measured rather than trusting the envelope, as that row's own routing rule warns: `git fetch origin main` then `git rev-list --count origin/main..HEAD` = **101 ahead**, `HEAD..origin/main` = **0 behind**. The envelope's own snapshot said `ahead=97` — stale on arrival, consistent with every prior occurrence. `wc -l apps/mcp-server/src/interface/mcp/routes/pushBctcLayoutHandler.ts` = **252**, unchanged (baseline 228, ceiling 250). Trajectory since mint: 46 → 95 → 97 → 101. Zero re-mints. Did **not** push (router constraint) and did **not** repoint the gateway URL.

## STEP po-5 — [task_id: FIX-AUDITOR-NOTEBOOK-SECOND-COMMIT-RETRO-REWRITES-PUBLISHED-OUTPUT-CONTRACT] Folded; the ≥2-cycle mint threshold is now met by this row itself

**what-considered:** (a) mint a fresh FIX for the `[notebook-immutability-guard]` fire; (b) log to volume-tracking only; (c) fold into the existing row and record that the repeat threshold is crossed.

**why-change:** Chose (c). `triage-signals.md`'s `bug-escalation` 4th-class rule says do **not** mint per occurrence (notebook-writing agents auto-prune mid-cycle, so the false-positive base rate is high) and mint **only** when the same agent fires on ≥2 separate cycles. That threshold is now met — but a matching row **already exists** and owns exactly this mechanism on exactly this agent: same agent (`system-auditor`), same mechanism (a second commit retroactively rewriting an already-published retained section), same guard still `mode=warn` so nothing blocked. Its title records cycle **c71**; this tick's fire is cycle **c114**. Bumped `occurrence_count` 1→2 and recorded on the row that it should now be read as confirmed-recurring rather than single-occurrence. (a) would have been the duplicate-mint failure I made last tick.

**Did not attempt to reproduce the diff.** The guard compares a retained section's bytes in the INDEX against HEAD; per the routing rule, a clean worktree read taken after the fact is **outcome** evidence and cannot refute the finding — absence of a reproducible diff is INCONCLUSIVE, never "benign".

## STEP po-6 — [task_id: CLEAN-DRAINSIGNALS-BUGESCALATION-DOCUMENTS-2-OF-4-LIVE-PAYLOAD-CLASSES] Minted small, and explicitly not as a routing bug

**what-considered:** (a) skip it as cosmetic; (b) mint a CLEAN doc-sync row.

**why-change:** Chose (b), P3. **It is not a routing failure and the row says so** — I verified the envelope arrived intact, because `drain-signals.md:224` routes every `bug-escalation` to "PO Step 0-SIG" unconditionally and the payload-shape list is descriptive, not a filter. It is a mis-disposition hazard for a dev-team agent reading the drain doc, and this exact gap has already produced one live incident: `triage-signals.md`'s 4th class was added 2026-08-05 *because* a `[notebook-immutability-guard]` fire on this same notebook "reached this row with no matching rule". Two docs describe one signal type; one was kept current, the other was not. AC-3 forbids touching `triage-signals.md` — it is already correct and is the SSOT; duplicating its disposition prose into the drain doc is how the two drifted apart in the first place.

## STEP po-7 — [task_id: dev_team_idle_chain.pending_triage_inbox] Cleared 8 of 12; held 4 against my own flow doc

**what-considered:** (a) unconditional CLEAR as `triage-signals.md` line 50 literally specifies; (b) clear 8, hold 4.

**why-change:** Chose (b), second consecutive tick. The held four — `recurring-bug` ×2 (`bd05e417`, `ba1ec99c`), `sprint_registry_dangling_ids` ×1 (`9fbfb401`), `system_issue` **underscore** ×1 (`31b7f837`) — have no Pipeline-A routing row. Clearing them would turn `signal-type-coverage-guard` green **by deleting the guard's own input** rather than by adding a routing row, and green-because-the-input-was-deleted is indistinguishable from green-because-fixed. Their fix rows are already open and were resolved live before this decision: `FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS` and `FIX-TRIAGESIGNALS-SYSTEMISSUE-UNDERSCORE-FORM-UNROUTED` (both `backlog[]`, `next_agent=agent-father`). The holds retire when those land.

**Never ran `scripts/audits/guard-signal-type-coverage.sh`** — it is not read-only (line ~258 writes live orch-state via `ORCH_APPLY_LIVE_FILE_OVERRIDE`) and its `--check` is a no-op alias, not a dry-run.

**Observed and recorded, not acted on:** two `envelope_id`s were delivered **twice** each (`f5a48a3a`, `9717a5dc`), so 8 entries carried only 6 distinct ids. `orch-apply.sh` accepted the duplicate-`envelope_id` appends without complaint. The subtractive CLEAR is duplicate-safe by construction (`map(select(...index...|not))` removes every matching entry), so this cost nothing today; it is noted on the durable-inbox note field as a latent gap rather than minted, because it is the same missing-content-dedup class the STEP po-1 row already owns at the choke point.
