# Decision Journal — triage-20260807T0605Z · po

**Trigger:** out-of-band router escalation — system-auditor Tier-1 c77 A-30 CRITICAL on mcp-server
(signal `sys-20260807T054825-771b`), plus a stale `status`/`status_note` contradiction on
`OPS-MCPSERVER-REBUILD-STALE-IMAGE-PREDATES-MEMLEAK-FIX`.
**Agent:** po
**Started:** 2026-08-07T05:52:00Z

---

### STEP po-S1 · po · 2026-08-07T06:05:54Z
**task-id:** FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER
**what-done:** Promoted BACKLOG→READY, priority high→critical, cleared `plan_only`, corrected a
non-existent `files[0]` path, attached the RAW evidence discharging the row's own gating first move.
**what-considered:**
- (a) "same fix, insufficient window" — 609f62800 only delays the leak past 12h.
- (b) "second, independent leak source" — a different mechanism the initDatabase guard cannot touch.
- (c) mint a fresh investigation row.
**why-decision:** (b), on a decisive discriminator rather than inference. The fix's own signature probe
(`backfillOCFForWatchlist` = 1 since boot vs 41689 pre-fix) proves 609f62800 is live AND working, so (a)
is falsified — the residual climb cannot be the initDatabase leak. (c) rejected on dedup: the architect
had already minted this exact row on 2026-08-05 as an explicit fast-follow. Its own gate ("correlate
`/health` sessionCount against MemPerc on the POST-deploy image") was precisely this tick's measurement,
so the correct move was to discharge the gate, not re-specify it.
**why-change:** Row was `plan_only` pending that correlation; the correlation is now done and ended in a
container death, so holding it plan_only would gate the only fix that addresses the live failure.

### STEP po-S2 · po · 2026-08-07T06:05:54Z
**task-id:** OPS-MCPSERVER-REBUILD-STALE-IMAGE-PREDATES-MEMLEAK-FIX
**what-done:** Closed backlog/BLOCKED → done_verified/DONE_VERIFIED, `next_agent: pm`, with a note
recording that its cited image `20f1e0a2c87` is stale (superseded by `8c06969b5a3`, then `115700a86e65`).
**what-considered:**
- Re-dispatch to ops (treat BLOCKED as authoritative).
- Mechanically flip `status` to match the note.
- Adjudicate the ACs and close.
**why-decision:** The ACs are the ground truth, not either field. All three are discharged and now
over-satisfied — every candidate image post-dates 609f62800, so the AC's premise holds regardless of
which is running. Re-dispatching would have rebuilt for nothing; a bare field rename would have left an
unadjudicated row on the board.
**why-change:** Router explicitly declined to resolve this mechanically because the disposition was a
judgment call. It was: the deploy happened, is evidenced, and the "Next: QA…" handoff is not lost —
`FIX-MCP-MEMORY-CODE-LEAK` is independently live at `next_agent=qa`.

### STEP po-S3 · po · 2026-08-07T06:07:12Z
**task-id:** FIX-AUDITOR-A30-DISCRIMINATOR-CRASH-CLIFF-SCORED-AS-RECLAMATION-DIP
**what-done:** Minted (backlog, `next_agent: developer`), then sharpened after reading the full verdict
chain; absorbed the phantom `FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE` and repointed the
dangling `depends[]` on `FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE`.
**what-considered:**
- Fold into an existing A-30 row.
- Treat the FOLD verdict as correct and stand down.
- Mint a new row against the script.
**why-decision:** The script returned FOLD/"benign GC sawtooth" for a window in which the container
actually died — verified against my own probes, not prose. Standing down would have accepted a verdict
that contradicts a `RestartCount` delta. No existing row covers it: the id that named this defect class
is referenced as a `depends[]` but exists in no lane and no archive, so folding was impossible and would
have left a P0 gated on a phantom forever.
**why-change:** First draft framed it as "crash cliff miscounted as a dip". Reading the whole `if`-chain
showed a stronger defect: both percentage branches are hard-gated on `DIPS -eq 0` (one 0.5pp wobble ⇒
FOLD), and the sole dip-immune branch keys on `OOMKilled=true`, which this fleet's memory deaths never
set. Every escalation path is closed by construction. Title and desc rewritten to the stronger claim.

### STEP po-S4 · po · 2026-08-07T06:05:54Z
**task-id:** FIX-MCP-MEMORY-CODE-LEAK
**what-done:** Left in `review[]`/`next_agent=qa`; attached a note separating the verified scoped fix
from the falsified memory-ceiling AC, with explicit QA guidance.
**what-considered:**
- Close it DONE_VERIFIED myself.
- Revert / reopen 609f62800 because memory still hit the cap.
- Annotate and leave the sign-off to QA.
**why-decision:** Third. Sign-off is QA's gate, not PO's — but QA was about to be handed a row whose AC
is unsatisfiable by that row alone, which would idle it indefinitely (its 12h window was never runnable:
an unrelated TASK_603 rebuild at 08-06T23:21Z reset the clock, and the container has not survived 7h
since). Reverting was the trap to avoid: the fix's own probe passes unambiguously.
**why-change:** No change to lane, owner or next_agent — annotation only.

### STEP po-S5 · po · 2026-08-07T06:10:00Z
**task-id:** (ambient — live infra decision)
**what-done:** Declined to rebuild or restart mcp-server despite standing full deploy autonomy.
**what-considered:**
- Targeted single-service rebuild.
- Controlled restart to clear the 873 leaked sessions and buy ~6h.
- No infra action.
**why-decision:** Nothing to deploy — the running image is already at HEAD and the reaper fix does not
exist yet, so a rebuild would be a no-op cache hit. A restart was defensible before 05:57:26Z, but the
container had already auto-restarted by the time the writes were staged; re-restarting a 10-minute-old
healthy container at 8.33% would buy nothing and cost an avoidable unclean-shutdown risk
(`OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN`: 49% unclean/7d).
**why-change:** The historical "never restart — it destroys trajectory evidence" constraint on this row
is now discharged (mechanism located in source, session count correlated, death captured), so the
decision turned purely on availability risk, and that argued for no action.
