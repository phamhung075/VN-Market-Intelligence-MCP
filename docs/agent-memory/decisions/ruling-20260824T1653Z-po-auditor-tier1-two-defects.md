### STEP po-R14 · po · 2026-08-24T16:53Z
**task-id:** FIX-AUDITOR-TIER1-PROBE-TEST-INVERTED-ASSERTION-L1422-FALSE-GREEN (minted)
**what-done:** MINTED into `ready[]` (P1, FIX, size S, zone `cross-service/`, next_agent=developer) after re-deriving the whole-board coverage scan myself and confirming zero open owners. Verified the defect at source rather than inheriting it: `check()` (L30-39) increments PASS only on the literal string `"true"`; 263 of 264 assertions build that argument as `$([ COND ] && echo true || echo false)`; line 1422 alone builds it as `$([ A != B ] || echo true)`, which yields `""` (FAIL) when the timestamps differ and `"true"` (PASS) when they are identical — the exact negation of its own label.
**what-considered:**
- Trusting the spawn prompt's coverage claim and minting on its authority — REJECTED per `feedback_bespoke_perTick_keys_in_shared_json_hide_real_coverage` and `feedback_auditor_dedup_citation_resolves_to_nonexistent_row_id`. Re-ran it wider: resolved `[paths(objects with id and (status or title))]` to enumerate every row-bearing container (12 of them, incl. `active_sprints[].tasks[]`, `closed_sprints[].tasks[]`, `signal_queue.rows`), then matched `id+title+description+status_note+acceptance_criteria+files+notes` — 4 fields more than the prompt used. Same verdict, now on my own evidence.
- Folding it into an existing "deflake" row rather than minting — REJECTED on inspection, not on name similarity. `FIX-RAG-TEMPORAL-DECAY-TEST-JITTER` is scoped to `apps/mcp-server/src/__tests__/135-rag-temporal-decay.test.ts` (zone `apps/mcp-server/`, priority low, files[] pins it) and `DEFLAKE-1187-POLLNEWS-DEAD-PATH` to `1187-pollnews-dead-path.test.ts`. Neither is a class-level owner of assertion-idiom defects; folding would have buried a P1 under a low.
- Filing to `backlog[]` — REJECTED. Diagnosis is complete to the line and the character, zone routes to generic developer, and no design question remains for architect/ba. Backlog is 536 deep; this would have been invisible.
- Shipping it as the one-character fix the diagnosis implies — REJECTED, and this is the part that needed judgement. Un-inverting alone re-arms a REAL flake: the two probe ticks can land inside the same wall-clock second, making `last_seen_at != first_seen_at` legitimately false. AC-2 mandates de-flaking, AC-3 mandates 3 consecutive identical-count runs (one green run proves nothing about a same-second race), AC-4 mandates an opt-IN idiom lint.
**why-decision:** The flakiness IS the bug, not noise around it. A 264/264 run is one where both ticks landed in the same second (the inverted assertion accidentally passing); the 263/1 run is the one where the ledger behaved CORRECTLY. That inversion is not cosmetic — `FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-1-PROBE-SCRIPT` was approved DONE_VERIFIED on a 264/264 run whose greenness depended on it, so an approval gate was decided on corrupted evidence.
**why-change:** Added AC-5, which the source material did not ask for: re-run the CORRECTED suite against the approval it corrupted, and if the ledger turns out NOT to advance `last_seen_at` on a DEBOUNCED tick, file a NEW row against the probe script — explicitly do NOT silently reopen the DONE_VERIFIED row. Also recorded exculpatory evidence in the row so the implementer does not re-derive it: live `auditor-tier1-spawn-debounce.json` entry[0] has `first_seen_at=12:42:19Z` vs `last_seen_at=12:45:14Z`, so production DOES advance it — this is a test-only defect and the label is the correct expectation.

### STEP po-R15 · po · 2026-08-24T16:53Z
**task-id:** FIX-AUDITOR-TIER1-PROBE-SCORES-DELIBERATELY-DISABLED-LAUNCHD-JOB-AS-DEGRADED
**what-done:** PROMOTED `backlog[]` → `ready[]` (held at P1, next_agent=architect), with `promoted_at/promoted_by/promotion_note` and a `status_note` carrying the never-re-arm constraint. Confirmed at source (`auditor-tier1-probe.sh` L719-731) that this row is the only path back to green.
**what-considered:**
- Leaving it in backlog on "T1 is bounded to ~1 respawn/hour, so it is tolerable" — REJECTED. The debounce bounds the cost but never reaches zero; ~24 wasted agent spawns/day forever, and a permanently-RED top-tier oracle trains the fleet to ignore T1. Reliability outranks everything else in my priority order.
- Clearing the verdict by re-adding or retargeting an ack — REJECTED, and forbidden by my own prior ledger ruling: an ack acknowledges DEGRADED things that have an open fix row, and a deliberately disarmed job is neither degraded nor has anything to fix. Retargeting to a convenient open row is the same error wearing a different hat.
- Re-arming `com.vn-market.fleet-push` — REJECTED as a standing user decision, re-verified live this tick with `plutil -p` (the plist is BINARY, so `grep -i disabled` reads as "key absent" — that false negative has already burned one pass): `"Disabled" => 1`, `StartInterval => 1800`, label ABSENT from `launchctl list`. Intended state, not a fault. Re-arming would push ~224 unpushed commits to origin/main unattended.
- Adding the label to the probe's existing `obsolete_labels` skip list (L696, currently the single entry `com.vn-market.socat-bridge`) — REJECTED as the ruling, recorded as a design hint only. "Obsolete" is the wrong semantics for a job that is deliberately disabled, and hardcoding one more label does not generalise. A distinct EXPECTED-DISABLED classification read from the plist's own `Disabled` key is the honest model and covers every future disarmed job. Left the choice to architect; did not pre-empt it.
**why-decision:** The prompt asserted the ack removal would not clear the verdict; I did not take that on trust and read the branch. Both the stale-ack branch (`elif`) and the no-ack branch (`else`) emit the byte-identical `printf 'launchd_agents\t%s\n' "$label"` — only the `ack_rc -eq 0` branch suppresses it. So removing the ack swaps a STALE-ACK annotation for a bare not-loaded annotation and changes neither signature nor verdict. Promotion is also cheap to act on: `ready[]` holds only 2 rows with `next_agent=architect`, so this lands in a shallow queue, not the 109-deep lane the raw count suggests.
**why-change:** Corrected the premise I was handed. The prompt framed the 16:40:57Z tick as evidence of post-removal behaviour; the timeline is the reverse. `auditor-tier1-last-trigger.json` `written_at=2026-08-24T16:40:57Z` still carries `STALE-ACK(tracked_by=FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH,status=DONE_VERIFIED)` — the `elif` branch — while commit `930297f37` landed at 16:44:23Z, 3m26s LATER. So that tick is a PRE-removal observation and the post-removal prediction, though sound by code reading, has not yet been observed live. Recorded so nobody later cites the 16:40Z artifact as proof the removal was ineffective.
