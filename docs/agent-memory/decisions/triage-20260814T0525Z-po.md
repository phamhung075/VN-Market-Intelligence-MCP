# PO Decision Journal — dev-team Step 1 Triage 2026-08-14T05:25Z

**task_id:** `FIX-AUDITOR-NOTEBOOK-COMPOSE-ACTUATOR-BUILT-TESTED-NEVER-WIRED` (primary), plus 15 folds + 2 mints
**Inputs:** 44 durable-inbox envelopes · 10 unclaimed Telegram bug reports (4846–4855) · `.task_board` (active 8 / backlog 398 / ready 97 / review 92 / qa 9) · `git log -30` @ `2d86b0242`
**Writes:** 2 `orch-apply.sh` pipes (task_total 733 → 735, both conservation-clean) · 1 `docs/improvement-proposals/` critique · 1 `docs/signals/po-*.json` · 10 `process_telegram_report`

---

## D-1 — Approved IMP-20260814 LANE-A, but ruled the three questions it deferred

**What was considered:** (a) approve as written, leaving AC-3, the section-cap and item 7 open for the implementer; (b) approve with the open questions closed by PO ruling; (c) reject to LANE-C on item 7.

**Why (b):** the proposal's own §Weakness is that this rewire has already been handed off twice and never landed. Handing it off a third time with three unresolved design questions attached is the same bet with worse odds — every open question is a place the implementer can stall or silently choose the cheap option. So: AC-3 closed as option (a) (bash-derived `c<NNN>`, model never chooses the number — option (b) puts policy inside a byte-mover that has other callers to protect); section-cap ruled as *raise the cap, accept reduced retention, no side-car* (co-locating evidence with verdict is the notebook's purpose; a side-car re-creates the two-plane split this repo has repeatedly lost data across); item 7 **severed** as a governance question rather than rejected — bundling a fleet-wide signal-lifecycle ruling into a wiring change is exactly the scope creep that produces a third stalled handoff.

**Not (c):** item 7 is genuinely LANE-C in character and the architect said so. But severing one item is a smaller, more honest instrument than reclassifying six good items to block on a governance debate.

**The one thing I changed in the proposal's own verification:** Success Signal 3 (`grep -c 'notebook-compose.sh' main.md > 0`) is **gameable and I replaced it**. It is satisfied by a commented-out line, by prose, and — the case that matters — by a correctly wired line whose Bash allowlist grant (proposal item 2) was missed, in which case the flow silently falls back to the freehand ladder while the signal reads green. That is this proposal's own failure mode reproduced one layer down. Replaced with a runtime-execution proof: the script's own stdout marker must be present in a real committed cycle.

## D-2 — Kept the fix scoped to a pilot, and the evidence for that arrived mid-tick

**What was considered:** widen the approved fix to all 37 APPEND-class agents, vs keep it on `system-auditor` alone.

**Why pilot-only:** `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist`. The proposal's Success Signals measure `system-auditor`'s file; they cannot certify 36 other agents, and a fleet rollout gated on a signal that does not measure it is exactly the pattern that memory records.

**Why this is not merely conservative:** five AC-2a `notebook-immutability-guard` fires landed in this tick's inbox across a three-hour window — `system-auditor` (c5, c6, c89), **`news-scout` (c266)** and **`alert-commander` (c179)**. The latter two do not share `system-auditor`'s local compose copy. Every prior occurrence on this defect was single-agent, which kept the "it's the local copy" reading alive; three agent types in one window **refutes it** and confirms the shared `.claude/skills/notebook-write/SKILL.md` path. So the pilot is correctly narrow *and* provably insufficient — the fleet fix is `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS`, raised P1 → P0, rollout opt-IN and gated on the pilot going green.

## D-3 — Folded the QA `nso_ts_key` bug rather than minting a fifth sibling

**What was considered:** mint a 5th notebook-autoprune row (dev-team's hand-off flagged it as distinct from the 4 existing ones, and mechanically it *is*), vs fold as AC-8 onto `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE`.

**Verified the mechanism myself before either.** Not on the relay: `nso_ts_key("## cycle-705 · 2026-08-14T01:33:11Z · … (commit \`761988143\`) …")` → `76198814000000000`; a plain sibling heading → `20260814010000000`. The optional `T…Z` group plus `tail -1` makes the parser key off the commit SHA's digit prefix, producing a value ~3.8× larger than any real key, so that section becomes permanently newest and undroppable. Live content in `docs/agent-memory/notebooks/qa.md`.

**Why fold:** same file, same function, same test suite, same `next_agent`, and the target row is already READY/P0 with `notebook-section-direction.sh` in `.files` and `nso_ts_key` named in AC-7. That row's own `lane_note` argues folding beats minting *at this exact site* because the script header already tracks six prior FIX-* tickets. Minting a fifth open notebook-autoprune row would fragment ownership further, which is the failure the row already documents. Distinctness of mechanism is a reason for a distinct **AC**, not a distinct row, when the fix site and owner are identical.

## D-4 — Corrected my own prior carry-over rather than acting on it

**What was considered:** trust the 04:15Z PO carry-over, which recorded A-33's `context-bloat-backstop.sh` finding as verified-true ("absent from all three settings files, 0 matches each, so the hook is dead") and instructed this tick to mint on recurrence.

**Why not:** the same inbox contained a live `context_bloat_breach` signal emitted **by that hook** at 04:31:10Z. A hook that emits cannot be missing. Re-verified at source: `.claude/settings.local.json` has 1 match and registers it as `bash "$(git rev-parse --show-toplevel …)/scripts/agents-flow/context-bloat-backstop.sh"`. The prior tick's grep was wrong.

**What was minted instead:** `FIX-AUDITOR-A33-HOOKLIVENESS-SETTINGS-PATH-MISRESOLVE-FALSE-DEAD-HOOK` — `tier1-probe.md:295-301` emits `settings-file-missing` only when `[ -f "$SETTINGS_LOCAL" ]` fails, and that file exists, so `$PROJECT_ROOT` is misresolving in the probe's runtime context (same class as `FIX-DEVTEAM-PREFLIGHT-ROOT-SYMLINK-CWD-FRAGILE`, cross-referenced, not folded). AC-2 is the load-bearing one: "could not resolve my own root" must emit a *distinct third token* and must never be reported as a hook-liveness failure, because liveness is simply **unknown** in that state. A false-dead verdict on a liveness probe is worse than no probe — and this one had already propagated a wrong conclusion into a PO carry-over.

## D-5 — Folded Telegram 4854 *with a correction that changes its row's acceptance*

**What was considered:** fold as the fifth instance of a known false positive (text is byte-identical to reports 4510/4511 already folded on `FIX-AUDIT-OUTPUT-CONTRACT-V3-…`).

**Why not, and why it matters:** report 4853 fired **39 seconds earlier** stating `emit-dashboard` lost the mutex and did *not* write the DASHBOARD row for `sys-20260814T041720-3a88`. So `dashboard_rows=0` was the **correct** reading of the artifact plane — this instance is a true positive. V3's defect is that it cannot *discriminate*, not that it is always wrong. Written onto the row as a hard constraint: any fix that suppresses or debounces V3 is a **false green** and would have silenced a correct detection of a genuinely lost CRITICAL dashboard row.

## D-6 — Minted the pre-gate/verdict divergence row; declined to mint on the `vn_index_cache` breach

**Minted** `FIX-AUDITOR-PREGATE-VERDICT-DIVERGENCE-MEMCREEP-NO-OWNING-CHECK`: three fires in 93 minutes (4846/4850/4851), no dedup hit — the three existing `audit-output-contract` rows all grade emission *counters*, none grades pre-gate-verdict vs cycle-verdict on a named dimension. Left **both** candidate root causes open on the row (stateful pre-gate re-probe, per the sibling `…DATATIER-REINVOKES-STATEFUL-PREGATE…` row's AC-3 fleet-grep ask; vs the detector comparing incomparable planes) because I have not distinguished them and guessing would send an implementer down one branch with false confidence.

**Declined** `db_integrity_breach` on `vn_index_cache` ("only 1 row, expected 2+ index types"): MED, single fire, addressed `to=dev-team`, and its `root_cause_hypothesis` is the auditor's own hypothesis, not a read contract. `feedback_contract_from_live_payload_not_schema_comment` — I have not verified that the cache is *supposed* to hold multiple index types. Re-verify next tick before minting.

---

**Routine folds, only path, no change from plan:** 3× `ci_red` → both existing file-scoped rows (failing-file read run at source on all 3 runs; identical 2-file set) · 4× sweep-guard `escalated=true` → `FIX-COMMITCONVENTION-…-SWEEPGUARD-HARDBLOCK` · 2× `system-issue` team-tool-recheck → decision already ruled REPLACE on 08-06 · `microservice_degraded` rag-service → row's own do-not-mint instruction · `sla-monitor` foreign_flow → third arithmetically-identical fire · 26/44 mis-routed envelopes → `FIX-DEVTEAM-DRAIN-ROUTES-NON-PO-ENVELOPES-TO-PO-STEP0SIG` (was 12/29 at mint, now 59%).

**Pre-checks:** supervised-hold = no-op (`.head.status=idle`, `active_task_id=null`, nothing held). Manual-dispatch sweep = `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` (REVIEW + `supervised` + `plan_only` + `next_agent=agent-father`, `qa_routed=false`, 15d, no draining lane per `bounded1-supervised-lane-report.sh`) folded into BATCH. TNB handoff read, fresh @02:37Z, findings already routed onto existing rows.

**Durable-inbox CLEAR not performed** — owned by dev-team Step 1 per `triage-signals.md` §7.
