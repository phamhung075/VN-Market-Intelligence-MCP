# Decision Journal — triage-20260801T0608Z · po

**Scope:** batched auditor findings (auditor-findings-20260801) — 3 findings, evidence-attach vs mint rulings
**Agent:** po
**Started:** 2026-08-01T06:08:34Z

---

### STEP po-S126 · po · 2026-08-01T06:19:30Z
**task-id:** FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM
**what-done:** Attached a dated evidence addendum + 3 new ACs and WITHDREW the row's own `scope_out` prohibition on the accumulation framing, after 40 read-only samples showed PID1 VmRSS byte-identical at 2521008 kB for 7min AFTER a 47-page job completed cleanly at 06:09:53Z.
**what-considered:**
- Mint a separate "idle plateau" row as the brief invited.
- Attach to the existing row but leave `root_cause`/`scope_out` intact.
- Attach AND amend the refuted clauses.
**why-decision:** The row already owns this exact mechanism (parent uvicorn, per-job burst, headroom) so a second row would be a duplicate; but leaving `scope_out` as written ("Do NOT carry forward an accumulation/leak framing — REFUTED") would have ordered the architect to discard the only framing today's data supports. An un-amended attach would have been worse than no attach.
**why-change:** Brief proposed "your call: widen or mint"; chose widen, plus an amendment it did not ask for because its own premise (idle accumulation) was falsified by the load control.

### STEP po-S127 · po · 2026-08-01T06:20:31Z
**task-id:** OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN
**what-done:** Attached the 2026-07-31 burst: 12 `mcpServerStartup` rows 11:36–12:40 with ZERO `mcpServerCleanShutdown` sentinels in 11:00–13:59 (only sentinel that day = 14:44:23), read-only from `cron_job_runs`.
**what-considered:**
- Mint an "A-21 blind to restart rate" row as a coverage gap.
- Attach to the existing unclean-shutdown churn row.
**why-decision:** The brief's mechanism ("clean ExitCode=0 restarts write a sentinel, so crashRestarts stays 0 and A-21 passes correctly") is falsified by the sentinel table — there were no sentinels, so A-21's own algorithm classifies all 11 gaps as crashes (~11 vs threshold 2) and should have WARNed. A-21's logic was not the cause, so a "missing check" row would encode a false premise. The verified fact is unclean-shutdown churn, which this row already owns.
**why-change:** Brief recommended attach-not-mint; agreed, but redirected the attach target and corrected the mechanism.

### STEP po-S128 · po · 2026-08-01T06:20:31Z
**task-id:** FIX-A21-PREDBOUND-2-TIER1-PROBE-DEDUPE-VIA-SHARED-ACTUATOR
**what-done:** Attached a design caveat: this row prescribes `docker exec` into mcp-server, which keeps A-21's transport coupled to the container it monitors while `tier1-probe.md:148-149` maps probe failure to "skip, NOT an infra finding". Recorded the verified host-side alternative (`file:...?immutable=1`, cross-checked identical to the in-container read).
**what-considered:**
- Mint a new row for the self-referential blind spot.
- Attach the caveat to the row that is about to re-implement the transport.
**why-decision:** The gap is real and uncovered by the three sibling A-21 rows, but PREDBOUND-2 is the row that will choose the transport — a caveat there changes the outcome; a separate row would race it. Also flagged explicitly that "a Tier-1 cycle ran and took the TOOL-UNAVAILABLE branch" is INFERRED, not verified (auditor notebook is overwrite-per-cycle), so it must be confirmed before being treated as established.
**why-change:** Declined the mint the finding implied; the fix belongs inside an existing in-flight design decision.

### STEP po-S129 · po · 2026-08-01T06:20:31Z
**task-id:** FIX-MARKET-MESSAGES-TIMESTAMP-FORMAT
**what-done:** Attached a falsification of the new "corroborating" evidence and left priority at P1. Measured live: rows matched by the space bound but not a T bound = 2; matched by a T bound but not the space bound = 0. Format-agnostic `julianday` ground truth = 27; the live space-separated 24h predicate also returns 27.
**what-considered:**
- Accept the finding and cite it as stronger corroboration than the 60-minute +61% figure.
- Test the claim's direction and denominator first.
**why-decision:** The claim was inverted (the defect is false INCLUSION, never exclusion — which the row's own title already states) and its "12 of 116 / 15 of 119 / 20 of 124" numbers are a denominator error: the denominator is the whole partially-pruned table back to 2026-05-18, not a 24h population. Recording the falsification prevents the same false corroboration being re-raised by a fourth tier.
**why-change:** Did NOT lower priority — the original 60-minute false-inclusion evidence is untouched and still justifies P1.

### STEP po-S130 · po · 2026-08-01T06:22:00Z
**what-done:** Ran both mandatory pre-checks; declined to re-stamp the manual-dispatch sweep's top candidate.
**what-considered:**
- Stamp `TE-T02` and fold it into this cycle's BATCH per the sub-flow.
- Surface it unstamped and leave sweep visibility intact.
**why-decision:** `TE-T02` already carries `po_manual_dispatch_flagged_at=2026-08-01T01:58:29Z` and was re-admitted only because that stamp aged past the 4h window without ever being dispatched. Re-stamping from an evidence-triage invocation that holds no dispatch authority would reset the clock and hide it for another 4h — the exact `FLAG-WITHOUT-DISPATCH-STRANDS-ROW` failure the staleness window exists to cure.
**why-change:** Sub-flow assumes the stamping tick can dispatch; this one cannot, so the stamp was withheld and the candidate reported upward instead.
