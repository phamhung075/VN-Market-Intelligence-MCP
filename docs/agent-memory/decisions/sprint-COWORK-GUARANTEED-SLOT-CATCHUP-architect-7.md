# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect (continuation 7)

**Sprint goal:** COWORK-GUARANTEED-SLOT-CATCHUP
**Agent:** architect
**Started:** 2026-08-26T01:00Z (rolled from -6.md, which sat at 154L/34919B against a 600L/36000B cap)

---

### STEP architect-S47 · architect · 2026-08-26T01:00Z
**task-id:** FIX-SQLITE-DOCKER-VIRT-CORRUPTION-ROOT-CAUSE-INVESTIGATION
**what-done:** Read-only forensics against the 4 preserved `.corrupt-*` snapshots (07-19, 07-30, 08-06, 08-26) plus live read-only `docker inspect`/`docker exec` probes. Wrote `docs/architecture-briefs/2026-08-26-sqlite-marketdb-corruption-6th-recurrence-crossboundary-engine-skew.md`. Lane-moved the row `backlog[]`→`ready[]`, `next_agent: developer`, `zone: multi`, `files` populated, via `orch-apply.sh`.
**what-considered:**
- Trusting ops' "Docker Desktop advisory-lock defect" as an established finding — REJECTED per dispatch instruction; treated as hypothesis, independently re-tested via header-byte forensics (file-format writer/reader version byte) rather than re-asserted.
- Treating tonight as "recurrence #6 of the same known issue" — REJECTED: header bytes show 07-19/07-30/08-06 are WAL-format (2/2) with a scattered multi-tree `btreeInitPage()` signature; 08-26 is rollback-journal format (1/1) — the first of the four to be so — with a narrow, structural signature instead (freelist off-by-1, one cross-tree stray pointer, 7788 rowid-disorder cells in one tree). Different failure class, not a continuation.
- Chasing the ~62.8M rowid magnitude as a corruption signal — REJECTED after reconciling against the live restored backup (`MAX(rowid)=61,330,052` at 04:30Z vs 62,841,915 at corruption, ~1.51M/20h): fully explained by ordinary `INSERT OR REPLACE` churn on `intraday_foreign_flow_5m` (non-AUTOINCREMENT rowid table, unconditional per-bucket REPLACE every 5 min per its own docstring). Flagged as a red herring so the implementer doesn't chase it.
- Recommending immediate named-volume migration (ops' option 1) — REJECTED, costed explicitly: already tried once (`ffa045e81` 04-25) and reverted after a VM-rebuild wiped it (`5ba622eca` 07-15); would ALSO require rewriting all 86 host-invoked script call-sites (52+34, grown from 25 four weeks ago) to `docker exec` — identical migration cost to the cheaper fix, for a strictly worse durability tradeoff while the backup hardening prerequisite (§5.4 of the 07-30 brief) is still unfixed.
**why-decision:** New, verified, previously-unrecorded finding tipped the recommendation: host bun (1.3.13 macOS) and container bun (1.3.13 oven/bun:1.3.13-debian) embed DIFFERENT SQLite engines (3.43.2 vs 3.51.2) against the identical bind-mounted file — confirmed live, twice, both directions. This and the refined advisory-lock/cache-coherency hypothesis (rollback-journal locking path, not the excluded WAL/SHM one) are both closed by one fix: route every host-invoked market.db script through a new `scripts/run-against-market-db.sh` `docker exec` wrapper (mirrors `orch-apply.sh`'s own SSOT-wrapper convention) instead of migrating the mount type.
**why-change:** Widened past the row's own framing (which asserted the lock-defect as fact) to independently re-derive the mechanism from evidence, per the dispatch's explicit instruction to match the originating signal's epistemics rather than the ops row's. Re-flagged (not re-fixed) the still-open 07-30 brief backup-integrity-gate gap as an unrelated but real, growing-risk finding.

### STEP architect-S48 · architect · 2026-08-26T01:17Z
**task-id:** FIX-FLEETPUSH-DISARM-EXISTS-ONLY-IN-UNTRACKED-PLIST-REPO-COPY-SILENTLY-REARMS
**what-done:** Re-verified the 1-line `Disabled=>1` diff live; census'd all 5 tracked `launchd/*.plist` install patterns (3 symlink-installed = drift-immune by construction, 2 copy-installed incl. fleet-push = the actual gap); wrote `docs/architecture-briefs/2026-08-26-fix-fleetpush-disarm-durable-install-guard.md`.
**what-considered:**
- (a) tracked `Disabled=1` + re-arm doc — REJECTED: inverts the tracked default to "disabled by design" for every future clone/host, forever, for a host-level override the row's own AC-5 frames as tied to the CURRENT CI-red/224-backlog situation, not a permanent design change (PUSH-AUTONOMY-1 itself is unmodified, still canonical). Also fixes only this one label's value, not a mechanism.
- (c) checked-in policy file the installer reads — REJECTED: needs the identical install-time enforcer as (b) to have any effect, so it pays (b)'s full cost plus a new schema/registry to maintain, for a benefit (an explicit reasoned record) this row already gets for free from this brief + the row's own po_note/evidence. Not worth a new parallel ledger for a problem confined to 1 label today.
**why-decision:** (b) install-time guard (`scripts/install-launchd-plist.sh`, refuses to overwrite an installed plist whose `Disabled` key reads true via `plutil -extract`, never grep) preserves the tracked plist's honest "armed by design" default (matches standing PUSH-AUTONOMY-1 intent) while making THIS host's current override durable against any future re-copy from source — without hardcoding a permanent global policy inversion.
**why-change:** No change from the row's own AC-3 menu; picked (b) over (a)/(c) with justification and cost, did not implement (design-only dispatch, no code shipped this cycle).

### STEP architect-S49 · architect · 2026-08-26T01:53Z
**task-id:** FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM
**what-done:** Verified root cause at source (`main.py:154` no `max_tasks_per_child`; `ocr_worker.py:107/209/222` module-global `PaddleOCR(vi)` cache; zero `malloc_trim` in the child). Read `application/extract_tables_usecase.py:531-567` and found a granularity mismatch the PO ruling did not name: recycling happens between tasks, but `ocr_pages_worker` loops over multiple rescue fires INSIDE one task, so recycling cannot bound within-one-document growth. Also traced the cited "6 fires pins the cap" evidence to source (`ocr_bench_inner.py`) and confirmed it measures `AutoFallbackOcrBackend`/PID1 (a sibling, now-retired mechanism), not this row's own `ocr_worker.py` child — flagged as an evidence-provenance gap, not silently inherited. Wrote `docs/architecture-briefs/2026-08-26-fix-pdfx-parent-process-memory-burst-headroom-worker-recycling.md` (amendment, per PO's explicit "do not author a second 29KB brief" instruction).
**what-considered:**
- (i) dedicated second `ProcessPoolExecutor` reserved for the Paddle rescue call only — REJECTED: `ProcessPoolExecutor`s aren't passable into an already-running worker, so finer-than-task granularity needs splitting `ocr_pages_worker`'s contract into a parent-orchestrated two-phase dispatch — touches `extract_tables_usecase.py`'s call contract, both existing test files, and doubles `lifespan.py`'s executor-shutdown bookkeeping. Real re-architecture, not the "small bounded design call" the ruling asked for.
- max_tasks_per_child tuned to some N>1 on the shared executor — REJECTED: reopens the exact defect being closed, just bounded to a smaller untested window; no measurement of `ocr_worker.py`'s own curve exists to justify any specific N>1 as safe.
**why-decision:** (ii) `max_tasks_per_child=1` on the EXISTING shared `ocr_executor` — one-line `main.py` change, zero change needed to `ocr_worker.py`'s already-correct "if None: load" cache pattern, and the only N that needs no curve-fitting to be provably correct. Paired with a mandatory safety belt (`BCTC_MAX_PADDLE_RESCUE_FIRES_PER_DOCUMENT` budget + `malloc_trim(0)` after each fire in the child) specifically because recycling structurally cannot reach the within-document compounding case §3 of the brief identifies.
**why-change:** No change from the PO's own ruling menu (i)/(ii); chose (ii) with the reasoning above. Added the granularity-mismatch finding and the evidence-provenance flag as new content this cycle, neither was in the PO's dispatch.

### STEP architect-S50 · architect · 2026-08-26T04:38:02Z
**task-id:** FIX-DEVTEAM-QADRAIN-SELECTION-BLIND-TO-QA-NOT-BEFORE-TIME-GATE / FIX-QADRAIN-DONE-TO-QA-SCORES-BACKWARD-CONSERVATION-ABORTS-WHOLE-DRAIN / FIX-DEVTEAM-QADRAIN-SKIP-BRANCH-STRANDS-ALREADY-LANEMOVED-ROW-IN-QA (shared hop, one script cluster, per po_scope_note precedent)
**what-done:** Read all 3 rows + 2 out-of-scope siblings (FIX-QADRAIN-NO-TASKID-LEVEL-CLAIM-DUPLICATE-QA-SPAWN, FIX-DEVTEAM-QADRAIN-PIPE-SWALLOWS-CAS-ABORT-NO-RETRY) at source, plus scripts/devteam-review-claim-qa-drain.jq, scripts/lib/devteam-eligibility.jq, scripts/orch-conservation-check.mjs, both qa-drain call sites in dev-team/flow/main.md, and qa/flow/main.md's Dispatch/Smart-Skip section. Wrote docs/architecture-briefs/2026-08-26-qadrain-shared-hop-timegate-conservation-skipstrand.md. Live-verified AC-4 (row 1) and the qa[] stranded-row sweep (row 3's AC-5) were ALREADY satisfied by today's earlier qa cycles/router notes -- did not redo either. Lane-moved all 3 rows in_progress[]->ready[], next_agent=developer, owner=developer, architect_review_note per row, via orch-apply.sh (ORCH_APPLY_DECLARED_BACKWARD_LANE_MOVES for the 3 ids -- a single dry-run first surfaced that moving all 3 in one write trips the >1 undeclared-backward-move tolerance; declared explicitly rather than 3 separate writes or raising tolerance). Self-verified at HEAD: a peer commit (99e8e9511, router T1 tick) swept my orch-apply write into its own message before I could commit separately -- content verified byte-correct at HEAD, no re-commit needed. Brief committed separately (97a45f124) under my own message.
**what-considered:**
- Row 1 fix confined to just the one predicate in the jq script (per po_scope_note) -- REJECTED as INCOMPLETE: router's own note + my re-verification show a second field name (qa_new_window_earliest_d1_close) and a second dispatch path (router hand-dispatch) both bypass that script entirely; a jq-only fix would not have prevented today's actual repeat.
- Row 2: (2) self-declaring script bypass, (3) full lane-vocabulary rename -- REJECTED per the row's own HARD FORBID reasoning (trust-the-writer defeats an independent guard; rename has zero-benefit-vs-cost since LANE_RANK is grep-confirmed used in exactly one place repo-wide).
- Row 3: (a) claim-before-write restructure -- REJECTED, reverses the 2026-08-06 throughput-cap design intentionally. (c) dedicated qa[] watchdog -- REJECTED, duplicates sibling row FIX-DEVTEAM-QA-LANE-STALE-AGE-WATCHDOG-BLIND's scope and only detects post-hoc, never prevents.
**why-decision:** Row 1 = two-layer fix (picker predicate + a NEW mandatory qa/flow Step 0d, the one true convergence point across every present and future dispatch path, scripted or hand). Row 2 = correct the LANE_RANK tier model (done shares review's pre-QA tier) rather than widen tolerance or add a bypass hatch -- restores the guard's own documented "batch claims always move forward" invariant to being true. Row 3 = SKIP branch self-reverts via the already-stamped drain_source_lane field -- zero window of strand, no new mechanism.
**why-change:** Widened row 1 past its own po_scope_note per the router's pre-cycle finding (confirmed, not taken on word). Rows 2/3 stayed within their own ranked-option menus; picked and justified, did not implement (developer's job).

### STEP architect-S51 · architect · 2026-08-26T08:30Z
**task-id:** UC-SDF-P2
**what-done:** Re-verified BA's confirmed root cause at source, then independently verified the row's own 4-unit sequencing guard: units 2 (cadence-policy alert-commander rows) and 3 (calendar_status wiring) are BOTH already shipped (commit 8c2acb44c + TASK_2008a) — found via `docs/architecture-briefs/2026-08-04-cadence-rationalization.md` item 1, which deliberately chose `_cron_fallback:true` for alert-commander (self-gating, safety/regulatory lane) rather than real adaptive intervals; the row's own "15min/240min" wording for unit 2 is wrong and would be a regression if implemented. Designed the two genuinely open units: (1) filename-contract fix, (4) regime/volatility writer. Wrote `docs/architecture-briefs/2026-08-26-uc-sdf-p2-filename-contract-fix-and-regime-volatility-writer.md`. Lane-moved review[]->ready[], next_agent=developer, via orch-apply.sh; read back post-commit.
**what-considered:**
- Unit 1: (a) rename Step 4.7's filename to the nominal tick + add a date component (the row's literal ask) — REJECTED: patches the same "two independent processes agree on a derived string" surface that already produced 2 confirmed defects + 1 near-miss (BA §3's seconds-dropping bug); (b) newest-mtime glob over `cycle-snapshot-*.json`, no filename key at all — CHOSEN: needs zero change to `tick-snapshot.md`, and structurally closes the "no date component" residue-collision concern as a side effect (selection is by real mtime, never by name).
- Unit 4 regime leg: (a) call `get_carry_trade_signal` fresh from inside the tool — REJECTED: duplicates the identical `/snapshot` HTTP call Step 4.7 already just made this tick; (b) read the now-fixed `cycle-snapshot-latest.json` in-process, lowercased — CHOSEN, no extra network cost, fresh once unit 1 lands.
- Unit 4 volatility leg: server-compute via the already-shipped-but-unwired `get_volatility_indicators` tool (completes the EMIT-DARK-v2 "move fields server-side, the dispatcher is narration-only" pattern — the same reasoning that already migrated calendar_status/signal_backlog/dev_queue_depth/container_vm_headroom_mb) — only viable option found; no existing producer anywhere else.
**why-decision:** Chose to keep the row's SEQUENCING GUARD but narrow it to units 1+4 only (unit 4's regime leg reads a file unit 1 fixes — landing 4 alone would swap "pinned unknown forever" for "pinned stale-but-plausible forever", worse not better); units 2/3 need no further action. Flagged two behavior-change risks explicitly rather than silently deciding them: `cowork-match-slots.js`'s freshness-downgrade will stop firing on most off-hours ticks once regime is real (intended, but a real behavior change); `cadence-policy.js`'s `computeTiers()` low-tier set (`unknown`/`low` only) would put `NORMAL` volatility into the high-cadence tier unless widened to include it — recommended widening, left as an explicit AC for the implementer rather than silently baked in.
**why-change:** Row asked for 4 units; delivered corrected scope (2 done, 2 designed) rather than re-implementing units already shipped. Design only, no code shipped this cycle (plan_only row).

### STEP architect-S52 · architect · 2026-08-26T09:51Z
**task-id:** FIX-JOURNALGUARD-ALERT-TRANSPORT-RUNS-ON-THE-PLANE-IT-MONITORS
**what-done:** Confirmed at source that both `call_tool` and `scripts/agents-flow/mcp-call.sh` reach the identical `coreSend()` inside `mcp-server` (`telegram.ts`) — no transport swap escapes the dependency. Found the row's own "obvious family" already live 2x (`cowork-guaranteed-slot-firer.sh`'s `_escalate_failure`, `maybe-deploy-vps.sh`) — direct curl to `api.telegram.org`, `.env`-sourced, zero MCP dependency. Wrote `docs/architecture-briefs/2026-08-26-journalguard-alert-transport-host-direct-telegram-spool.md`: new shared `scripts/lib/telegram-direct-send.sh` (extracted, not a 3rd bespoke copy) + new `scripts/audits/journal-guard-alert-notify.sh` (flush-then-send spool, NDJSON, 200-line cap) replacing the cron prompt's `call_tool` step in both `.claude/commands/crons/cron-market-db-journal-guard.md` and its verbatim-port registration doc.
**what-considered:**
- Refactor the firer's/maybe-deploy-vps.sh's existing inline curl copies onto the new shared lib now — REJECTED as in-scope: both are green, tested (firer: 53 assertions) P0 paths; DRY-only churn on working code is disproportionate risk. Flagged as an optional low-priority follow-up, not filed as a row (no live defect).
- AC-3 (cowork preflight-fallback): treat as same fix family and patch inline — REJECTED: that chain's dependency is on state-mutating coordination primitives (task_claim/heartbeat/election/slot-claim), not a one-way notification; no curl-equivalent substitutes a distributed lock. Sized at 5 flow docs, not 1 script.
**why-decision:** AC-3 disposition = explicitly OUT of scope for this row; minted `FIX-COWORK-PREFLIGHT-FALLBACK-NO-MCP-INDEPENDENT-PATH` (backlog, P1, next_agent=architect) so PO's own dedup finding ("no row found") doesn't leave it untracked — satisfies the row's "do not leave unaddressed by silence" instruction with an actuated row, not just prose.
**why-change:** none — followed the row's own prescribed evidence and AC set; shape of fix was deliberately left to me and I chose extend-existing-pattern per `always_extend_not_duplicate`.

### STEP architect-S53 · architect · 2026-08-26T10:20Z
**task-id:** FIX-DRS-CLAIM-HAS-NO-ALLOWLIST-GATE-OFF-ALLOWLIST-BLIND-DISPATCH
**what-done:** Verified the row's own status_note claims at source (grep-confirmed `is_design_router_allowed` never called in the claim script). Fixed `scripts/devteam-backlog-claim-design-router-sweep.jq`'s `$resolvable` filter to also gate on `is_design_router_allowed`. Wrote `design_router_default_allowlist` as a new SSOT def in `scripts/lib/devteam-eligibility.jq` (both promote's and claim's fallback now point at it) instead of threading a NEW `--argjson allowlist` through claim's own invocation. Added `AC-DRS-ALLOWLIST-GATE`/`AC-DRS-ALLOWLIST-SKIP-TO-NEXT` to `scripts/audits/devteam-dispatch-gate-satisfiability.sh`, verified both fail pre-fix / pass post-fix. Dry-run verified all 3 blast-radius rows unclaimable post-fix (scratch copy, never live). Checked BOUNDED-1/SLS/RLC for the same promote/claim gate asymmetry; wrote findings + minted a P2 follow-up row. Brief: `docs/architecture-briefs/2026-08-26-drs-claim-allowlist-gate-and-promote-claim-asymmetry.md`.
**what-considered:**
- Thread `--argjson allowlist '[...]'` into claim's own CLI invocation, mirroring promote exactly — REJECTED: empirically confirmed jq raises a compile error on any `$name` referenced but not CLI-bound, and claim is invoked with no such flag at 9 existing call sites (main.md + 8 audit-script fixtures) — adding the flag would require editing all 9 in lockstep, reintroducing "two call sites can drift" via the CLI plumbing instead of the value.
- Put the ratified default in the library as a def, called directly (no CLI arg needed) by claim, while promote keeps its existing `--argjson` call site (audit-trail visibility, zero call sites broken) — CHOSEN: exactly one literal system-wide, zero existing invocations touched.
**why-decision:** The row's own scope note asked me to decide, not defer; jq's compile semantics made "thread `--argjson allowlist` everywhere" a strictly worse option (blast radius on 9 working call sites) than "one library def, referenced directly."
**why-change:** none from the row's own FIX SHAPE paragraph — it named exactly this predicate and exactly this library; the CLI-plumbing question was the row's own explicit open decision, resolved above.

### STEP architect-S54 · architect · 2026-08-26T12:29:54Z
**task-id:** FIX-AUDITOR-NOTEBOOK-COMPOSE-ACTUATOR-BUILT-TESTED-NEVER-WIRED
**what-done:** Re-verified the row's title at source: FALSE (actuator wired+executing, main.md:1115, 4 marker commits pre-review). Corrected PO's own evidence: found 2 MORE Tier-1 marker commits on 2026-08-25 (post-review) — adoption is 2/20 not 0/20, still a forcing-function case. Corrected PO's named host: `pre-commit` cannot read the commit message (githooks(5): fires before git obtains it) — the marker check needs `commit-msg`, a hook this repo does not have yet. Root-caused Tier-1's structural gap: `tier1-probe.md` (lazy-loaded, ends after its own last section) has no "return to main.md Notebook Write" pointer, unlike Tier-2/3 which read `main.md` inline and flow straight into it. Wrote `docs/architecture-briefs/2026-08-26-fix-auditor-notebook-compose-tier1-adoption-gap-and-commitmsg-forcing-function.md`. Split into `FIX-AUDITOR-NOTEBOOK-COMPOSE-COMMITMSG-MARKER-GATE` (developer, `scripts/git-hooks/`, P0 — the real mechanical fix) and `FIX-AUDITOR-TIER1-PROBE-MISSING-RETURN-TO-NOTEBOOK-WRITE-POINTER` (agent-father, `docs/agents/system-auditor/flow/`, P2 — non-load-bearing complement). Parent row moved `review[]`→`archive[]`, status DONE, via `orch-apply.sh`; read back post-commit. Confirmed non-duplicate of `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES`/`FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` (both still open, different defect shapes).
**what-considered:**
- Add the marker check to existing `pre-commit` (PO's named host) — REJECTED after verifying `githooks(5)`: that hook runs before the commit message exists, so it structurally cannot see a message-embedded marker regardless of implementation effort.
- Hard-reject by default (like `_check_auditor_heartbeat_shapes`) vs warn-by-default+opt-in-reject (like `_check_notebook_immutability`/`_check_notebook_uuid_provenance`) — CHOSE warn-by-default: found one live legitimate no-marker class (AC-5 data-repair/renumber commits, precedent `35be008d0`) a hard reject would wrongly block, same fleet-blocking-regression shape `_check_notebook_immutability` itself hit and reverted same-day 2026-07-29.
**why-decision:** PO's own instruction explicitly allowed correcting the named host ("if you conclude pre-commit is wrong, name a different forcing function") — did so with a verifiable, tool-level citation rather than a judgment call.
**why-change:** Row asked for a straight split; delivered a split PLUS 2 evidence corrections (adoption is 2/20 not 0/20; host is wrong) that change the design but not the verdict (a mechanical, narration-proof gate is still required).

### STEP architect-S55 · architect · 2026-08-26T13:5xZ
**task-id:** FIX-MANUALDISPATCH-BLIND-TO-READY-ROWS-OFF-ALLOWLIST-WITHOUT-XOR-FLAG
**what-done:** Recomputed AC-1's number via the shared predicates (never PO's hand list): 15 live `ready[]` rows (not 24 — 10 more are `deps_satisfied`-blocked), 0 P0 (the 3 named P0s all left, router-confirmed). Then checked the claim against RLC, not just the 3 named gap-detector predicates: `scripts/devteam-backlog-claim-ready-lane-consumer.jq` gates ONLY on `sup!=true AND po!=true AND !wrapper AND deps_satisfied AND !detail_deferred AND a resolved next_agent/owner` — no allowlist, no dev-role check, by design (its own header names `agent-father`/`qa` as intended targets). All 15 rows already satisfy every RLC gate. Corroborated independently: PO's own `po_promote_note_20260806T2331` on one of the 3 named rows already re-verified RLC-eligibility on 2026-08-06, 20 days before the same row was cited as "zero pickers." Concluded the AC-2 fix (4th manual-dispatch predicate) would not close a gap — RLC already covers this class — and would introduce a NEW double-dispatch race (manual-dispatch-sweep's BATCH path never coordinates with RLC's claim/lane-move, unlike the 3 existing classes which are lane-disjoint or RLC-excluded by construction). Root-caused the REAL residual: RLC shares WIP<2 with 3 siblings, single-claim, no anti-starvation aging, against 113 live `ready[]` rows — several of the flagged rows are 20-43 days old despite RLC-eligibility the whole time. Brief: `docs/architecture-briefs/2026-08-26-fix-manualdispatch-ready-lane-off-allowlist-hole-already-rlc-covered.md`. No code touched (AC-2's own constraints honored by NOT widening `is_ready_xor_gap`/the DRS allowlist — the 4th predicate itself was the wrong shape, not merely unneeded). Minted `FIX-RLC-SHARED-WIP-BUDGET-SINGLE-CLAIM-STARVES-AGED-READY-ROWS` (backlog, P2, architect) for the real throughput problem. Parent row archived `DONE` via `orch-apply.sh`; `.head` idle-reset (still named this task).
**what-considered:**
- Build the prescribed 4th predicate anyway (AC-2's literal ask) — REJECTED: it would target a row set RLC already dispatches unconditionally, creating a double-dispatch race the sweep's own design note (never writes `.head`/moves lanes) has no way to prevent — the opposite of the sweep's purpose.
- Treat this as Outcome 1 with a narrower predicate scoped to exclude RLC-reachable rows — REJECTED: that predicate would be permanently empty by construction (RLC's target set IS this class), i.e. dead code, not a fix.
**why-decision:** Cross-checking against the actual claim script (not just the 3 named gap-detector predicates) is what the row's own AC-1 instruction demanded ("recompute through the shared predicates, never a second hand-maintained list") — applied one level further than the row anticipated, to the picker itself, not just the count.
**why-change:** Router flagged the count was stale; investigation found the underlying CLASSIFICATION was also wrong, not just the count — a materially different finding than "same hole, smaller."

### STEP architect-S56 · architect · 2026-08-26T19:35:00Z
**task-id:** FIX-PRECLAIM-INTENT-KEY-TTL-600-EXPIRES-UNDER-LONG-RUNNING-AGENT
**what-done:** Root-caused why this is NOT a copy-paste of the sibling fix: `task:<row-id>` is claimed by `dev-team`, which structurally cannot itself call the spawn tool a level deeper (nested-spawn limitation) and exits within ms — its own liveness proof comes from a LATER cron tick revisiting `.head`. `intent:<agent>:<key>` is claimed one layer further out, by the party that executes the real, blocking spawn — that context is inert (no interleaved tool call possible) for the agent's ENTIRE measured 10-25min runtime, so only the spawned agent itself is provably live throughout. Designed a single, bounded, opportunistic self-renewal: the claiming dispatcher embeds a `SELF_RENEW_BLOCK` text instruction into the spawn prompt it already builds; the spawned agent executes ONE `task_heartbeat(intent:<agent>:<key>, owner_client_session=<dispatcher's already-passed coordination_session>, ttl_seconds=3600)` as one of its first tool calls. Base claim ttl (600s) untouched. Wrote `docs/architecture-briefs/2026-08-26-fix-preclaim-intent-key-ttl600-spawned-agent-self-renewal.md` with the exact CARD.md/SKILL.md diffs for agent-father to apply verbatim.
**what-considered:**
- Raise the base claim ttl (600→3600) — REJECTED, row's own explicit constraint + sibling's AC-4 precedent ("moves the cliff, does not remove it"; a developer L-task or agent-father sweep can still exceed any static value).
- Cross-check the sibling's now-fixed `task:<row-id>` lock as a liveness backstop when `intent:` is found free — REJECTED, only covers sprint-task-linked dispatches, leaves pure ad-hoc router-user-intent dispatches (no `task:` counterpart) uncovered; Phase B must hold generically.
- Router-side concurrent heartbeat loop while the one blocking spawn call is in flight — REJECTED, structurally impossible: the router's own context does not regain control until the spawned agent's call returns, so there is no interleaved slot to run a heartbeat from.
- Per-agent-flow-file heartbeat step (~20 files) — would work but blows the row's own `.claude/skills/dispatch-claim/` file scope; flagged as a residual-risk fast-follow for agent classes with a plausible >60min tail, not built here.
**why-decision:** The chosen design is the only option that (a) stays inside CARD.md/SKILL.md, (b) never raises the base claim ttl, (c) keeps the bound (3600s, single-shot, never renewed again) strictly OUTSIDE the lock it extends, and (d) degrades byte-identically to today's pre-fix behavior if the spawned agent never reaches the instruction (no new stuck-lock class) — satisfying every constraint the row and its sibling's own AC-6 correction already established.
**why-change:** none from the row's own preferred direction ("heartbeat the intent key while the spawned agent is provably live, with an externally-held bound") — investigation confirmed it is not merely preferred but the only structurally viable mechanism, and supplied the concrete verbatim implementation.

### STEP architect-S57 · architect · 2026-08-28T23:40:00Z
**task-id:** FIX-BCTC-ANALYST-NOTEBOOK-COMPOSE-ACTUATOR
**what-done:** Designed the ACTUATOR: wire scripts/notebook-compose.sh into stage-log-notify.md §5a (c<NNN> derived in bash, model authors only the new section body, ONE actuator call `3 60`, branch on [notebook-compose] marker, marker embedded in commit msg) + scoped Bash grant (.claude/agents/bctc-analyst.md tools += Bash, description declares real write set, package Bash scope row forbidding docs/signals/ enumeration) + 5 verification gates (flow runtime / commit-time / CI bash-grant-coverage with baseline entry drop / smoke replay / live QA). Brief: docs/architecture-briefs/2026-08-28-fix-bctc-analyst-notebook-compose-actuator.md.
**what-considered:**
- section-cap 150 (system-auditor's PO ruling) vs 60 — 60: bctc-analyst template ≤10L, AC-2a cap, smoke-verified sections=3 at 60.
- grant shape: scoped Bash vs no grant (accept drift) — scoped grant: S51's doc-only remedy already falsified once (c187→2 sections); system-auditor pilot proves the actuator pattern works.
- sibling FIX-BCTC-ANALYST-STAGELOG-NOTIFY-NO-BASH (premise: remove Bash steps) — SUPERSEDED by this row's ruling (grant scoped Bash); flagged to PO, do not land both.
**why-decision:** Live-verified root cause (2 sections on disk) + smoke-tested the actuator against a copy of the live notebook ([notebook-compose] OK sections=3 direction=newest_first, zero script changes) — the fix is pure wiring + grant, no new primitives; the commit-message marker makes the verification gate mechanical and git-greppable, not doc-grep-dependent.
**why-change:** none from task's stated design track (po → architect → developer → qa); zone multi confirmed via files spanning docs/agents/ + .claude/agents/ + scripts/ + docs/data/ → next_agent=developer (Tier-2).

### STEP architect-S58 · architect · 2026-08-28T23:47:00Z
**task-id:** FIX-COWORK-LAYERC-NO-IDENTITY-PREAMBLE
**what-done:** Designed shared identity-preamble source + artifact-delta writeback gate for the Layer C firer (brief: docs/architecture-briefs/2026-08-28-fix-cowork-layerc-no-identity-preamble.md); lane-moved row backlog→review next_agent=developer.
**what-considered:**
- preamble home: inline spawn-fanout.md copy vs new scripts/agents-flow/cowork-identity-preamble.sh — script wins: one text + one substitution site, testable; inline copy = the documented 2nd-copy failure mode.
- writeback gate: exit-0 vs notebook-mtime-delta + peer-stamp discrimination — exit-0 alone is the measured null-fire defect; notebook path derivable from slot.agent (no per-agent table); schedule re-read discriminates dual-plane redundant fire from genuine null fire.
- session id: omit vs synthetic namespaced cowork-layerc:<slot>:<epoch> — leaf marker claims hard-require owner_client_session (chef/digest/fb/tnb verified at source); omit = fire fails one step later and the gate is never satisfiable.
**why-decision:** PO ruling (1) is prerequisite for (2); preamble must be ONE source both planes consume; gate must be filesystem-only (firer has no MCP) and fail-closed on null fire while staying silent on peer-stamped redundancy (cooldown-bounded escalation).
**why-change:** none from PO ruling; synthetic session-id interpretation flagged in brief §7 R1 for PO awareness (beyond literal wording, structurally required).
