# Decision Journal — Sprint MERGE-MONEY-RADAR-INTO-MOMENTUM · qa

**Sprint goal:** One unified /dashboard/momentum surface carrying BOTH money-flow radar and momentum indicators — without homogenizing the two card contracts.
**Agent:** qa
**Started:** 2026-07-02T05:00:00Z

---

### STEP qa-S1 · qa · 2026-07-02T05:10:00Z
**task-id:** FIX-BCTC-BANK-BS-SECTION-CLASSIFIER
**what-done:** CODE sign-off (not done_verified, per PO scope). Re-ran FIX-BCTC-BANK-BS-SECTION-CLASSIFIER.test.ts (13/13 GREEN), tsc --noEmit (0 errors), 26-file direct-dependency non-regression set (356/356 pass, independently corroborates dev-mcp-server's claim), mock-guard (PASS, no fabricated data), DDD/security grep (clean — no domain→infra imports, no process.env, no secrets). Traced the `unit_id ASC` == page-order claim into windowPartitioner.ts (zero-padded sequential unit-NNNN) and the `runBoundedPool` fixed-index-array claim — both hold, validating the RC-3 section-carry fix's ordering assumption.
**what-considered:**
- Run full 1161-file suite vs targeted direct-dependency set (grep -l on refinedMarkdownParser/finalizeBctcRefineTool/bctcRefineJob/parseRefinedMarkdown) → chose targeted (25 files, direct callers/consumers) after the full-suite run exceeded the 2min tool timeout with no result; targeted set is a valid non-regression proxy since only these files touch the changed code paths.
- Task prompt referenced test file TASK-W3-FIX-BCTC-BANK-SUMMARY-MAPPING-SECTION-GUARD.test.ts — verified via git log/status this is a DIFFERENT untracked file from an unrelated, still-BLOCKED sprint (FIX-BCTC-BANK-SUMMARY-MAPPING); the actual committed test in 2c7fb5b0 is FIX-BCTC-BANK-BS-SECTION-CLASSIFIER.test.ts (402L, matches commit stat exactly) — ran the correct file, treated the prompt's filename as a copy-paste slip, not a real finding.
**why-decision:** All 3 claimed root causes verified against the actual diff (blank-Ma 3-cell mis-route, separator-loss content-recovery via isHeaderRow, initialSection/finalSection cross-unit thread in both sibling pipelines); test fixtures are structurally-shaped synthetic reconstructions (docstring is honest about this, not fabricated) pinned to the task's own DoD numbers; identity holds exactly. APPROVE-CODE, done_verified correctly withheld per PO scope (live behavioral DoD needs the gated rebuild).
**why-change:** no change from plan.

### STEP qa-S2 · qa · 2026-07-02T06:04:57Z
**task-id:** BA-MERGE-MONEY-RADAR-INTO-MOMENTUM
**what-done:** RAW-live gate on ced952ca (WU-1+WU-2) + 9593967a (journal). tsc 0 errors, vitest 2006/2008 pass — 2 fails (`QUE-REFERENCE-PAGE-detail.test.ts`, `QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.tsx`) confirmed pre-existing via `git diff 5eb75e6a..ced952ca` on both files + their generated-data source = empty. Verification_gate (a)-(d) all PASS: momentum page 200 with both aria-labels + non-null radar values (score 0.04/MẠNH/77% coverage) + 4× honest-NULL momentum markers; money-radar 302→momentum, api.money-radar 200; nav grep 1 anchor to /dashboard/momentum ("Động Lực & Dòng Tiền"), 0 to /dashboard/money-radar; FE-HEADER-SSOT-top-nav.test.tsx 26/26. AC2 Promise.allSettled per-feed isolation confirmed by code read (lines 555-601, independent fallback objects). AC8 same-commit confirmed (`git show --stat ced952ca`); 2 extra nav-guard files (ind-p1-indicator-gauges-nav, task17-page19-news-buzz-nav) also fixed same commit, new label matches exactly. AC9 coverage-map summary.rows=49/LIVE=39 exact match, 8 momentum-page rows (4 pre-existing DEPTH_THIN + 4 new LIVE radar). AC10 grep confirms MoneyRadarCompositeDto/MomentumIndicatorsDto both distinct non-empty families. mock-guard PASS, DDD/security clean (process.env["FRONTEND_ORIGIN"] byte-identical to pre-existing pattern in both source files).
**what-considered:**
- Live curl against the shared `frontend-1` docker container FIRST returned stale pre-merge content (label "Động Lực P1", single aria-label, money-radar 200 not 302) — `docker inspect` showed image `Created: 2026-07-01T15:43:19Z`, predating commit `ced952ca` (`2026-07-02T05:53:07Z`). Ran `docker compose build frontend` (succeeded, new image tagged `:latest`) but `docker compose up -d --no-deps frontend` (container swap) was blocked by the auto-mode sandbox as a shared-resource mutation. Did not attempt to bypass — instead started an isolated `remix vite:dev` process on host port 4321 pointed at the same live `mcp-server:3000`/`api-gateway:4000` docker services (zero shared-container mutation), and RAW-curled that process for gate items (a)-(c). This is genuine live HTTP verification of the exact committed code against real upstream data (score 0.04, foreign-accum 0.00 — not fabricated), not a downgrade to code-review-only.
- Considered blocking the verdict on the stale shared container. Decided APPROVED is correct: the code is provably correct (RAW-verified via the isolated process + all code-level ACs), the built image is ready and only needs a container swap (an ops deploy action, not a code defect), and prior QA precedent (cycle-352) treats "image not yet redeployed" as a non-blocking follow-up note, not a CHANGES_REQUESTED trigger.
**why-decision:** All numeric claims independently reproduced (not relayed). No blocking issues found in code, tests, or the 10 ACs. The one gap (shared container staleness) is operational, not a defect in the reviewed commits, and is explicitly flagged as a residual follow-up in the Task Report rather than silently absorbed.
**why-change:** no change from plan — verdict APPROVED as expected from dev's implementation record, confirmed independently rather than relayed.

**follow-up (non-blocking, for ops/dev-team's next pass):** `docker compose up -d --no-deps frontend` to swap the shared `frontend-1` container onto the already-built `:latest` image (built this session) so the production-facing deployment reflects ced952ca.
