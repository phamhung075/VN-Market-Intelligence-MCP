# Decision Journal — po triage 2026-08-22T2016Z

task_id: intent:po:orchdash-fullsweep-triage-20260822 (router-direct dispatch, session 88555d2e)

Inputs: user-pasted `/dashboard/orchestration` snapshot, 6 candidate problem areas flagged by the
router with an explicit "may be stale, re-verify live" caveat. Output: **2 board mutations (1 mint,
1 widen), 0 duplicates, 5 already-closed items confirmed closed, 0 telegram.**

## Entry 1 — Header timestamp contradiction: real, already tracked, no mint

Top-level `.head` (canonical per `feedback_orchstate_dual_head_keys_toplevel_authoritative`) genuinely
stuck at `2026-08-15T09:25:10Z` / status=idle while `task_board._updated_at` shows live mutation as
recent as today. `.task_board.head` is the correctly-deprecated stub (not the bug). Root cause is
already carried by two live rows: `FIX-DEVFLOW-SELFCONTAINED-ZONE-FLOWS-SUCCESS-PATH-NO-HEAD-SYNC`
(READY, agent-father, P1) and `FIX-DEVTEAM-QA-LANE-STALE-AGE-WATCHDOG-BLIND` (BACKLOG, architect).
Corroborating evidence (not minted, just noted): 2 of 3 live `in_progress[]` rows (`UC-CCA-P3`,
`UC-CDC-P1`) are claimed_at 2026-08-14 with no `updated_at` since — exactly the stale-in-progress
shape those two rows already exist to fix. **No new mint — dedup held.**

## Entry 2 — Session-cron table "never fired": confirmed by design, not a bug

Read `layerBCronRegistry.ts` at source: `last_fire: null` / `status: SESSION_SCOPED` is a hardcoded,
architect-resolved (R2) row shape for ALL Layer-B rows — no durable ledger exists or was ever
scoped-in for CLI-session crons (`docs/architecture-briefs/2026-07-02-DASH-CRON-RECHECK-TABLE.md`
§5 explicitly excludes this). AC-20 in the frontend renders it as "Chưa từng chạy" on purpose
("never blank/fabricated"). **Not minted** — a deliberate, documented scope decision, not a defect.

## Entry 3 — Server-cron staleness cluster: three DISTINCT things tangled in one dashboard table

- **(a) Already-owned, correctly scoped:** `FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL`
  (P1, occurrence_count=3, extensively re-verified by prior PO ticks) explains WHY a job that fires
  exactly on its own `expected_last_fire` still renders STALE outside its market-hours window —
  `classifyCronLiveness()` ignores `expected_last_fire` and ladders on raw age. Confirmed still open,
  correctly diagnosed, **not touched.**
- **(b) NEW, minted — `FIX-CRONSTATUS-STATICMAP-MISSING-9-PAIRS`** (medium, dev-mcp-server): a
  DIFFERENT bug in the same file family. `resolveJobNameDb()`'s tier-1 `STATIC_JOB_NAME_MAP` only
  covers the 26 WATCHDOG_MANIFEST jobs (test-enforced: "exactly 26 entries 1:1"); tier-2 normalize
  fails on 9 CRONS keys whose DB `job_name` doesn't survive strip-trailing-"job" (same root-cause
  class the file's own header already documents for `summaryDaily`, just never widened to its
  siblings). Live-verified via `docker exec ... bun:sqlite` against `/app/data/market.db
  cron_job_runs`: summaryWeekly/Monthly/Quarterly/Yearly, foreignFlowFetch, marketOpen, marketClose,
  dataAuditDaily, publicContractsRefresh all have real DB history under a differently-spelled
  `job_name` and permanently render NEVER_FIRED. Highest-value strand: `dataAuditJob:daily` ran 4h
  ago (healthy) and is the one case where this bug hides a WORKING job, not a broken one.
- **(c) WIDENED, not duplicated — `FIX-CRON-NONRECOVERY-POST-HOST-SUSPENSION-TIER3-...`** (P1,
  minted 40min before my tick by a peer/earlier PO pass, scoped to only 3 writers). Live evidence
  this tick is much larger and mechanistically sharper: `schedulerWatchdogJob` — an UNCONDITIONAL
  24/7 every-10-min job, so classifier (a) cannot explain it — shows a genuine multi-day blackout
  with escalating gaps (6h50m → 33h → two isolated single ticks → 16h → a 5-run burst → then a full
  **99.7h/4d3.7h dead stop spanning three ordinary weekdays (8/19-21)**, self-recovering at
  `2026-08-22 12:40:04` with **no corresponding process/container restart** (mcpServerStartup's own
  last row is still 8/15). ~40 Layer-A jobs are non-ON_TIME right now; most stopped at the same 8/18
  boundary and have NOT yet resumed even though the watchdog itself came back 7.5h before my check —
  informative for the row's own AC-1 (per-job timer state, not one global flag). Appended as a new
  field (`po_blast_radius_widen_20260822T2020Z`) rather than editing the original note, preserving
  the peer's text verbatim — same append-only convention as everywhere else in this system.
- **why-change (b vs c):** kept as two separate rows on purpose. (b) is a display/resolution bug —
  fixing it doesn't restore a single stopped job. (c) is a real multi-day scheduler outage — fixing
  it doesn't correct a single mislabeled row. Folding them would let either fix appear to "close" the
  other's evidence.

## Entry 4 — Signal queue: all 15 rows already correctly routed, no dedup miss found

Read `.signal_queue.rows[]` in full: monthlySignalQualityAudit, brokerSanctionsSweep,
ragFtsRebuildCron, B-06 bctc-route disagreement (2 rows), 3 auditor_cycle_missing (tier1/2/3),
2 SHB narrative_contradiction (duplicate emission — same underlying CCATO finding, cosmetic dupe,
not re-mint-worthy on its own), morningBriefing stale, market_messages db_freshness, auditor
malformed-key orphan. All OPEN/READ = already dispositioned by the pipeline. **No new mint.**

## Entry 5 — Task board "39 total" mismatch: stale snapshot, not a missing lane

Router's pasted numbers (0/39/15/77) are far below live reality: backlog=444, ready=94, review=24,
qa=10, done=15, done_verified=29, in_progress=3 (=619, plus active_sprints/archive on top). All 7
flat lanes present and populated — confirmed this is snapshot staleness (the user's screenshot was
simply taken well before this tick), **not** a hidden/missing lane. `REFINE-CRON-ARM` and
`BCTC-PDF-PATH-BACKFILL` (the two 06-12 rows flagged as suspiciously old) are absent from the live
backlog entirely — already archived/resolved, not orphaned. No action.

## Entry 6 — Five named HIGH-priority items: all already closed, verified at source not by title match

`DRAIN-INJECTION-SAFE` → DONE (archive/2026-06.json + corroborated live in
`sprint-TOKEN-ECONOMY-TICK-PREFLIGHT-pm.md`: the injection-safe `mcp-call.sh` helper shipped).
`AUD-ND-1` → DONE (narrative confirms). `FLEET-HOST-SAFETY` sprint → absent from the current 15
active `sprint_goal.entries` (already closed/evicted). `FIX-DAILY-FF-VIEW-JOIN-ANCHOR` →
DONE_VERIFIED. BCTC `PUBLISH-INTEGRITY GAP` → closed when BAL-0 shipped DONE/QA-approved back in
June ("Publish-risk CLOSED" in the sprint's own row). `PREDICTION-CLAIMS-DAILY-CADENCE` → still
`OPEN` in `sprint_goal.entries` as expected, root cause note intact, correctly left untouched (an
unrelated architect amendment for a DIFFERENT sprint sits in `review[]` awaiting ratification — see
Entry 7). **Zero re-mints; router's "check status first" caution was warranted but all five check
out clean.**

## Entry 7 — Supervised-goahead pre-check: correct no-op, not skipped

Ran Step 1 of `po/supervised-goahead.md` live: `.head.status == "idle"` (Entry 1's own bug) so the
bash guard never even evaluates `should_hold` — nothing is currently held, matching the sub-flow's
own documented fast-path. Noted for completeness: `FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-
AND-JOURNAL-ARCHIVE` sits `review[]`/supervised with a same-day architect amendment awaiting PO
ratification — but it is not `.head.active_task_id` right now, so WF-2 isn't blocking it and this
gate correctly does not reach it. Not ratified this tick; unrelated to the dashboard sweep in scope.

## Entry 8 — Scope boundary: did not re-run channel-audit / manual-dispatch-sweep / TNB-audit

A peer PO tick completed `triage-signals.md`'s full chain ~50min before this session started
(`task_board._updated_at=2026-08-22T19:27:15Z` by `po`, durable inbox already drained 64→0 per
`triage-20260822T1927Z-po.md`). Re-running the full pre-flight chain this tick would race that
already-fresh state for zero new signal. Scoped this tick to the router's specific 6-item ask +
the two MANDATORY-but-cheap gates (signal-dashboard read, supervised-goahead) — both already
covered above. Flagged here rather than silently omitted.
