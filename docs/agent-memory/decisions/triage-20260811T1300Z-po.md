# PO decision — agent-father keep-cycle 2026-08-11T12:53Z findings (2026-08-11T13:00Z)

task_id: FIX-COWORK-AGENT-DESC-STALE-VS-DELIBERATE-BASH-GRANT (mint)
        + FIX-AGENTFATHER-KEEP-PRECHECK-GATE-BLIND-TO-3-OF-5-SCAN-SURFACES (mint)
        + CLEAN-ORPHAN-NOTEBOOKS-3-DEAD-20260811 (mint)

## Decision

**Finding 1 (3 CRITICAL tool-boundary mismatches) — GRANT ACCEPTED, DESCRIPTION IS THE DEFECT.**
Ruled the Bash grant on alert-commander/market-watcher/news-scout intentional and load-bearing.
Ordered the three *descriptions* corrected, the `tools:` lines left byte-identical.

**Finding 2 (46 notebooks vs 42 agents) — DECLINED the recommended one-off `scan-orphans.md` run.**
Ran the scan inline during triage instead (complete, results below), and minted the two structural
causes. A one-off run clears three dead files and changes nothing durable: the gate re-closes and
3 of the 6 findings are by-design and re-flag forever.

**Finding 3 (semble-search, LOW) — NO new row.** Folded as a 4th allowlist entry into finding 2's
row. Declined the proposed "Tool Agent template class" as its own task.

## What was considered

1. **Narrow the 3 Bash grants** (the report's other option). REJECTED, and explicitly forbidden in
   the row. Commit `610110e16` is titled `fix(claude/agents): grant Bash to ...` and its body names
   the two shipped S4 UNBLOCK rows the grant actuates. Narrowing re-breaks both:
   alert-commander loses commit-mutex (notebook was uncommitted 6 consecutive cycles pre-fix) *and*
   the session-id derivation for its published-marker `task_claim` — i.e. verified CRITICAL alerts
   fire with no duplicate-publish mutex, which is user-facing. news-scout/market-watcher lose
   `coverage-stamp.sh` (29/29 green, would go inert again).
2. **Correct the description text.** CHOSEN. `scripts/agents-flow/coverage-stamp.sh:95` writes
   `docs/data/coverage-state.json` today, by design, under its own mutex — so "No other filesystem
   writes permitted" is factually false as written, not merely imprecise. The text is the thing
   that is wrong.
3. **Build the missing `write_boundary` keys / `agent-write-boundary-guard` hook** the report notes
   are absent. REJECTED as scope for these rows and marked OUT OF SCOPE in row 1: the absence of
   mechanical enforcement is not what makes these three findings fire, and bundling it would stall
   a 3-line text fix behind a design question.
4. **Dispatch the explicit `scan-orphans.md` run** (agent-father's recommendation). REJECTED — the
   scan is what I already did; the *deliverable* was the finding list, and waiting on a dispatch to
   regenerate it would have cost a cycle for information now in hand.
5. **Chase 46 == 42.** REJECTED, and flagged as a trap in the row. The correct steady state after
   cleanup is 43 vs 42; the +1 is permanent.

## Why the change from plan

The "4-file gap" arithmetic is misleading and hides an inverse defect. The scan surfaces **five**
unmatched notebooks netted against **one** missing notebook, and only three of the six are real:

- DEAD (3, → CLEAN row): `dev-news-fetch` (+ its flow dir; the phantom is already adjudicated by
  `reference_news_fetch_zone_specialist_is_developer_not_phantom_dev_news_fetch`),
  `cowork-refactory-expert-2026-07-11-fr1-atomic`, `pm-alpha-s2-rag-fts-rebuild-cron` — stale 27-32d.
- BY DESIGN (3, → allowlist): `dev-team.md` (CLAUDE.md: "There is no dev-team agent type"; live
  08-09), `main.md` (router terminal, not a registered agent; live 08-07), and `refine_bctc_md`
  whose `init.md:110` declares `notebook: none` — so its MISSING_NOTEBOOK flag is a false positive.

And the reason it went unscanned for 3 cycles is structural, not incidental: `keep.md:33` scopes the
Pre-Check on `.claude/agents/*.md | docs/agents/*/flow/*.md`, but `scan-orphans.md` reads **five**
surfaces. Notebooks, tool packages and the roster are all outside the gate — so an ORPHAN_NOTEBOOK
finding can never open the gate that guards its own detection. Verified live at HEAD: last 3 commits
= 0 gate-scope hits, 2 notebook changes. That is the root cause; the skip streak was its symptom.

Allowlist mandated opt-IN per `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist`.
`AF-SEMBLE-INIT-DEF` already owns the semble-search *structural pattern* decision — row 2 only stops
the re-flag, deliberately not duplicating it.
