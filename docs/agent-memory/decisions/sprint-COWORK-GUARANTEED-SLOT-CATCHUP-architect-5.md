# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect

**Sprint goal:** Cowork guaranteed-slot catch-up (rolling sprint)
**Agent:** architect
**Started:** 2026-08-09T00:40:00Z (continuation of -4.md, which CAP-REACHED)

---

### STEP architect-S1 · architect · 2026-08-09T00:40:00Z
**task-id:** FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION
**what-done:** Read agent-father's full flow directory (main/keep/scan-orphans/sweep-fixes/review/create/edit*/team-tool-recheck) per PO's mandatory pre-design question — confirmed by exhaustive grep (zero `task_board`/`next_agent` hits outside 2 unrelated lines) that the cron never consumes its own board queue. Found a second, independent defect: `main.md`'s dispatch table needs structured `agent_name`+`change_description`, so even a generic "spawn with row context" dispatch (the DRS/PO-sweep pattern) would silently no-op into `keep.md`'s default branch. Designed two disjoint mechanisms: (A) new `agent-father/flow/board-drain.md` sub-flow with a SAFE_AUTO/NEEDS_RATIFY classifier (reuses `edit.md`'s existing prepare/apply split as the safety envelope + the already-shipped `improvement_approved_md` proposal lifecycle for the ratify path) for the 34-row/79% agent-father concentration; (B) a small widen (1→N/tick) of the already-shipped `manual-dispatch-sweep.md` for the 9-row ops-class remainder. Full brief: `docs/architecture-briefs/2026-08-09-agent-father-board-drain-and-ops-batch-widen.md`.
**what-considered:**
- Adding agent-father to the DRS allowlist (AC1's implicit "obvious" option) — rejected twice over: already ratified OUT for blast-radius reasons (`devteam-eligibility.jq:512-514`), AND mechanically broken regardless (§1.3 of the brief — DRS's generic spawn shape cannot satisfy agent-father's structured intent contract, would silently no-op).
- One unified mechanism (widen PO-sweep for both agent-father and ops/*) vs two separate ones — rejected unified: ops/* was excluded for a *safety* reason (generic input already works, just needs a human gate PO-sweep already provides); agent-father was excluded for a *translation-contract* reason (no generic flow to spawn at all) — same measured defect, two different root causes, conflating them would either under-gate ops or fail silently on agent-father.
- SAFE_AUTO classifier shape — mirrored `sweep-fixes.md`'s own pre-existing "auto-fix mechanical/cosmetic only, escalate anything requiring scope understanding" discipline rather than inventing new risk vocabulary; live-verified against 5 real stranded rows (multi-owner file spans, `supervised:true`, empty `files[]`) before finalizing, not asserted from the predicate design alone.
**why-decision:** Live re-measured (43 rows, not the mint's 44 — 34 agent-father/9 ops-class, 0 P0 today) via the row's own named instrument before designing. `commit_zone` in `init.md` already names a "signal-queue DONE-mark" carve-out never actually exercised by any flow file — reused/narrowly-widened that existing governance seam for board-drain's own lane-move write rather than inventing new write authority.
**why-change:** No change from AC1-4's framing — AC1's "new sweep lane" and "ratified PO BATCH" are not alternatives as originally read; they are the correct fix for the two genuinely different problem classes hiding inside one measured number.

### STEP architect-S2 · architect · 2026-08-11T13:37:00Z
**task-id:** UC-RDL-P4
**what-done:** Brownfield design for `dispatch_preflight` compositor (FR-1..FR-6/FR-8/FR-9 only, FR-7 forbidden per PO Q1 SPLIT). Layered on `getCycleBootstrap.ts`/`cycleBootstrapTool.ts` precedent. Full findings + file table + ratified response shape in `docs/handoffs/UC-RDL-P4-BA-spec.md` § [Architect] Brownfield Findings.
**what-considered:**
- Copying `getCycleBootstrap`'s `Promise.race` timeout wrapper verbatim vs. dropping it — dropped: all 4 sub-calls (`claimTask`/`heartbeatTask`/`listHeldTasks`) are synchronous bun:sqlite calls; a `setTimeout` race can never preempt a synchronous call (JS single-threaded), so the wrapper is decorative there too — SQLite's own `busy_timeout=5000` pragma is the real protection. Adds no value, only complexity.
- `orphans`/`roster` field shape: raw `LockRow[]` (BA's own literal type) vs. the aliased/normalized shape `taskListHeldTool.ts` produces (`owner`, `created_at`, ISO `expires_at`) — chose raw, matches FR-1's own signature, keeps this a pure pass-through. Flagged the divergence forward to `UC-RDL-P4B-DOC-CUTOVER` (FR-7) as a concrete risk note so that follow-up doesn't assume drop-in compatibility.
- `TaskKind` enum duplication (now 3 copies across `taskClaimTool.ts`/`taskListHeldTool.ts`/new `dispatchPreflightTool.ts`) — flagged (`feedback_preclaim_gate_taskkind_enum_drift` class), not fixed: consolidating touches 2 already-shipped files + the store's public type export, outside this L-task's PO-scoped bound.
- `SERVER_SESSION_ID`: duplicate the existing private IIFE into a 2nd file vs. extract — extracted to a new `serverSessionId.ts` (1 new file + 1-line import edit to `taskClaimTool.ts`) — this agent's own `always_extend_not_duplicate` constraint is a hard rule, and the extraction is zero-behavior-change, same class as the precedented size-lint splits.
**why-decision:** Every deferred/flagged item (D-6/D-7/D-9) was a genuine out-of-L-bound or out-of-Q1-scope call, not laziness — each has a named forward-pointer so it isn't silently lost. `docs/data/tool-registry.json` corrected from "hand-edit a JSON entry" (BA's literal §6 row 5) to "run `scripts/gen-tool-registry.ts`" after confirming the file's own `_maintained_by: "generator (do not hand-edit)"` header live — this is a real mechanism correction, not a stylistic preference.
**why-change:** Diverges from BA §6 row 5's literal instruction (hand-add a registry entry) — the live file header forbids exactly that; the generator produces the identical end-state (`totalCount` 183→184, new entry in `system` group) safely and atomically. Also adds 2 files (D-10's `serverSessionId.ts` extraction + its `taskClaimTool.ts` edit) not in BA's original fix-set — architect-decidable engineering scope per the BA spec's own closing line, kept minimal/mechanical.

### STEP architect-S3 · architect · 2026-08-11T16:42:00Z
**task-id:** UC-ASL-P5
**what-done:** Design for BA-rescoped FR-1 (tier1-probe.md `--category-type` per-check-class table) + FR-2 (triage-signals.md dedup-skip RESOLVED-closure). Closed all 3 open questions. Findings in `docs/handoffs/UC-ASL-P5-BA-spec.md` § [Architect].
**what-considered:**
- Q-status-guard: `READ`-only vs also accepting `NEW` — traced the ONLY resolvable-id path (dashboard-sourced, `drain-signals.md` §0a-D) and proved its batched write flips `NEW→READ` in the SAME atomic call that produces PO's `pendingSignals[]` entry, so `NEW` is unreachable at dedup-skip time today; chose `READ`-only (mirrors `task-archive.md`), flagged the durable-drain-stays-combined dependency forward.
- Q-A12-A19-boundary: ratified BA's `signal_feedback` default after confirming both candidate routing rules treat a single WARN identically for every non-mcp-server A-12-19 service — zero behavioral delta, not worth a 3rd branch.
**why-decision:** Re-verified every BA citation live (file:line) before designing per `never_design_without_reading`; the drain-signals.md trace was the deciding evidence for Q-status-guard, not an assumption.
**why-change:** No change from BA's FR-1/FR-2 scope; cleared `supervised`/`deploy_gate` on the board row per BA's flagged-not-actioned recommendation (sole justification, part-5 rebuild, now out of scope).

### STEP architect-S4 · architect · 2026-08-11T16:59:00Z
**task-id:** SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-WIRING
**what-done:** Read Task 1's findings doc + QA's DJ (qa-S46) before touching this row's original premise. Re-verified `cowork-guaranteed-slot-firer.sh:141-171` (`_fire_one_slot`) myself: straight passthrough matcher->claude, no eligibility/last_fired gate anywhere — confirms Task 1's "no wiring gate" conclusion at source, not from prose. Closed row findings-only (backlog->review, next_agent=qa), minted new `FIX-COWORK-GUARANTEED-SLOT-FIRER-NO-FAILURE-ESCALATION` (backlog, P1, next_agent=pm) for the real byproduct gap: firer has zero BUG-channel escalation on repeated CLI failure (no MCP access of its own, and the flow-level `send_telegram` never runs because `claude` dies pre-Step-0).
**what-considered:**
- Execute the original literal instruction (trace call chain for a self-latching gate) anyway — rejected: the gate does not exist per Task 1's live-verified evidence + my own re-read of the invocation loop; tracing further would be manufacturing analysis against a refuted premise.
- Close as pure findings-only with zero new task — rejected: would bury a real, cheap, well-precedented fix (67h zero-alert gap, direct cause of PO's own P0 escalation note) inside a closed row nobody re-opens.
- Reopen `FIX-GUARANTEED-SLOT-DUAL-PLANE-DOUBLE-FIRE`'s 2026-07-22 ratified "no new code" ruling given Task 1's Section 5 hint (dual-plane quota-burn amplifier) — rejected: PO's 2026-07-28 ruling explicitly forbids reopening any of the 6 consolidated rows before epic closeout; flagged for PO/ops awareness on the new FIX row's design note instead, not actioned.
**why-decision:** Design gap is real and traceable to source (script header explicitly states no gateway/MCP access; `run_firer()`'s non-zero return is never consumed by anything) — squarely a wiring/architecture finding, not infrastructure diagnosis. Reused the exact live pattern (`scripts/maybe-deploy-vps.sh:35-41`, direct curl to Telegram bypassing MCP) rather than inventing a new escalation mechanism — `always_extend_not_duplicate`.
**why-change:** Diverges from the row's own literal instruction (originally: trace + pinpoint an internal gate) because Task 1's findings refuted the premise the instruction was written against — task board conventions call for closing on the answer actually found, not the question originally asked. PRE-CLAIM attempted per dispatch-claim CARD.md (`task:SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-WIRING`); `mcp__gateway__call_tool` returned "No such tool available" — confirmed structural per WF-3 ruling (`docs/architecture-briefs/2026-06-07-wf3-dev-gateway-binding-ruling.md`, Option III): architect has no MCP gateway binding in its tool package (`docs/agents/tools/package/architect.md` lists only `mcp__semble__*`), by design — outer dispatcher holds the lock, specialists use file-based `.head`/board state only. Proceeded without it per that ratified invariant, not a workaround.

### STEP architect-S22 · architect · 2026-08-12T03:35:00Z
**task-id:** FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS
**what-done:** PO's premise-correction (P2→P0, allocator-retention refuted as sole cause) named 3 candidates to rule out. Ran 2 isolated `docker run --rm` repros (own cgroup, real image/model/corpus snapshot, new reusable scripts `scripts/audits/rag-{embedder,lancedb-search}-mem-arena-probe.py`) instead of reasoning from code alone. Candidate 1 (embedder per-call growth): +5.4 MiB/80 calls, asymptotes fast — ruled out as dominant. Candidate 2 (LanceDB reader/query growth): +340-444 MiB across ~20-60 calls then plateaus — confirmed dominant (~65-80x candidate 1), root cause named: `rag_entries` has zero vector index (only FTS on title/summary), so every `vector_search()` brute-force-scans the full vector column with no bound. Candidate 3 (FTS build): ruled out via code evidence (`CRON_RAG_FTS_REBUILD_ENABLED` defaults false, unset in this deployment — nightly rebuild never fires; lazy per-life build already memory-bounded by the shipped `RAG-FTS-BUILD-MEMORY-BOUND` fix). Wrote brief `docs/architecture-briefs/2026-08-12-fix-rag-embedder-idle-unload-second-growth-source.md`; updated Architecture SSOT (`docs/architecture/microservice/rag-service/infrastructure.md`) with the finding + the `768m`→`1g` cap reconciliation PO asked for (confirmed live: `MemLimit=1073741824`).
**what-considered:**
- Trust the PO's candidate ordering and design purely from code inspection (faster, matches an architect's normal design-only charter) vs actually running isolated repros against real code+data (chosen the latter — this row's own history is FULL of premises that looked right from prose/code alone and were later refuted by measurement; `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS`, a sibling row on THIS exact service, exists specifically because a prior agent trusted a plausible-looking read over a live measurement).
- Bind-mounting the LIVE `data/live/lancedb` directory read-only into the LanceDB probe (cheaper, no copy step) vs a snapshot copy (chosen copy — a live bind-mount would race the running container's concurrent writes/compaction, confounding the growth-curve measurement; documented the reasoning in the script's own docstring so a future rerun doesn't silently "optimize" this away).
- Recommending `malloc_trim(0)` as the primary fix (mirrors the FIX-PDFX precedent exactly, cheapest to justify) vs recommending a vector index (chosen the index — the SAME probe measured malloc_trim recovering only ~8-15% of the LanceDB-path growth here, vs. ~70-99% on the PDFX case; presenting the cheaper-but-wrong fix as primary because a precedent exists would be pattern-matching over evidence).
**why-decision:** Every candidate ruling is backed by a captured, reproducible numeric artifact from real production code + real (copied, not synthetic) data, not inference from reading `embedder.py`/`repositories.py` alone — matches `never_design_without_reading` and this row's own repeated history of prose-plausible-but-wrong premises.
**why-change:** PO's status_note already corrected the top-level premise (bounded→unbounded) before this cycle started; this cycle's own addition is discriminating WHICH of the 3 named candidates is dominant with hard numbers (PO left that open) and naming the concrete architectural root cause (absent vector index) + fix (IvfPq), rather than leaving all 3 candidates equally live for dev-rag-service to re-investigate from scratch.

### STEP architect-S23 · architect · 2026-08-12T10:23:00Z
**task-id:** FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION
**what-done:** Designed a two-arm Step 5.3 gate (Arm 1 cheap/advisory marker fast-path; Arm 2 load-bearing per-slot artifact-DELTA CONJUNCTION check, new `delivery_proof` schema in `cowork-schedule.json`, 2 new probe kinds `published_marker`/`db_probe` beyond the 5 the existing detector script covers). Wrote `docs/architecture-briefs/2026-08-12-fix-cowork-delivery-proof-gate-artifact-conjunction-design.md`.
**what-considered:**
- Extend `OFFFLOW_MARKERS` only (cheap) — REJECTED as sole fix: occurrence 3 (marker-less confabulated completion) proves marker-matching cannot be made durable; kept as non-load-bearing Arm 1 only.
- Wire Step 5.3 directly to today's `detect-analysis-only-exit.sh` — REJECTED as-is: PO's binding sequencing constraint (that script's OR-verdict PASSes the inverse-partial shape, its own sibling P0 row is still BACKLOG); would manufacture false-green gate authority AND spuriously block agents with no notebook (`refine_bctc_md`, confirmed no notebook file exists for it).
- Rewrite `last-fired.md` Step 5b's write mechanism directly (single-plane `published_marker` reconciliation) — REJECTED: that is `TASK-COWORK-CATCHUP-6`/FR-7's owned surface inside the still-open `BA-COWORK-GUARANTEED-SLOT-CATCHUP` epic; duplicating it would violate PO's 2026-07-28 "do not reopen the 6 consolidated rows" ruling.
- Gate one layer upstream instead (Step 5.3 filters `WON_SLOTS` before Step 5b ever runs, reusing the existing AC-P1-7-4 wiring pattern) — ADOPTED: zero conflict with FR-7's write-mechanism ownership, does not reopen any of the 4 confirmed PO-consolidated ids, and is broader (covers non-guaranteed + non-Telegram slots FR-7's marker-only check never will).
**why-decision:** Every occurrence's own evidence forced a specific constraint: occ 1 (no notebook) → need a non-file-based probe kind; occ 3 (no marker) → Arm 1 alone cannot be load-bearing; occ 7 (JSON landed, notebook did not) → conjunction, never disjunction, over ALL declared artifacts. Upstream WON_SLOTS-filter placement was the only option that satisfied the binding sequencing constraint without reopening epic-owned or PO-consolidated surface.
**why-change:** Row asked for a design covering item (A) marker extension + (B) schema + (C) epic sequencing + explicitly flagged (D) cron-spawn coverage as acceptable to punt. Delivered A+B+C in the brief; D relayed verbatim to `FIX-SYSTEM-AUDITOR-CYCLE-FINDINGS-NOT-SELF-PERSISTED` per the row's own instruction (spawn-fanout.md structurally cannot cover cron-spawned agents). Recommended SHADOW MODE first rollout (log-only, no `WON_SLOTS` exclusion) as an empirical backstop beyond what the row asked for, given the fleet-wide-gate-validated-on-one-file precedent.

### STEP architect-S24 · architect · 2026-08-12T10:55:00Z
**task-id:** OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE
**what-done:** dev-team review-lane secondary-drain dispatch (row misrouted here after PO repointed a self-target "dev-team"). RAW re-probed the 5 remaining UUID-keyed 0-row tickers (GVR/MBB/D2D/NKG/SSI) — unchanged since 08-05. Found the SECOND root cause the row was waiting on: their `bctc_vps_queue` rows (and, on a wider probe, ALL 12 of this row's cohort tickers' queue rows, 21 total DB-wide) are false-terminalized `url_not_found`/`last_attempt=NULL`/`attempts=6-7`, created 2026-04-28/30 — BEFORE the `last_attempt`-stamping fix (`FIX-BCTC-ENRICHER-STUCK-BACKLOG`, d92801332, 2026-07-02) shipped, permanently excluding them from every retry arm (`bctcQueueEnricherJob.ts` Arm1 needs `pending`, Arm2 needs `last_attempt IS NOT NULL AND attempts<6`, orphan-resync needs `pending`/`deferred_infra`). Ops's 07-10 one-off manual pdf_path-sync+PEK-trigger bypassed the queue entirely, which is why nothing progressed since. Found the fix already exists, committed same day as the last_attempt fix, never wired to auto-run and per live probe never invoked: `apps/mcp-server/src/migrations/reset-bctc-enricher-stuck-backlog-2026-04.ts` (dry-run-capable, idempotent) — its `STUCK_HOSE_TICKERS` allowlist contains all 12 of this row's cohort tickers verbatim. Wrote the full finding + a 3-step ops execution plan into the row's own `architect_review_note` (no handoff file — direct PO mint), moved `review[]`→`ready[]`, `next_agent`→`ops`.
**what-considered:**
- Treat this as infra-diagnosis out of architect's `not_my_job` boundary and bounce it back unactioned — rejected: the sibling `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` row (same cohort, same architect) already set live precedent for RAW docker-exec/log verification as legitimate brownfield evidence-gathering, not ops firefighting; the boundary excludes diagnosing live-infra *incidents*, not reading queue-lifecycle code + DB state to find a root cause for a design/routing decision.
- Design a new code fix (extend Arm2's predicate, or add a startup reset) — rejected as the PRIMARY action: a correctly-scoped, already-tested-pattern migration already exists and targets exactly this cohort; writing a second implementation of the same repair would violate `always_extend_not_duplicate`. Flagged the startup-wiring gap as an optional P2 follow-up only, per `enricher-liveness.md`'s own "recurring bug class" warning.
- Reassign next_agent to developer/pm for a SPIKE (matches the row's own earlier precedent, `dev-spike`) — rejected: nothing here is unknown or requires new code/tests; it is a bounded, already-written, dry-run-safe script invocation + RAW-verify, squarely ops execution (row's own `owner` field, matches its 07-10 TRACK1 execution precedent).
**why-decision:** Every claim is grounded in live evidence: `docker exec ... bun` readonly probes against the named-volume DB (queue-row status/attempts/last_attempt, financial_reports pdf_path/extraction_method), `git log`/file reads on the migration script and the fix commit it shipped alongside, and a DB-wide count (35 stuck rows, not just these 5) — never the row's own prose alone.
**why-change:** Row's dispatch note asked for "design work on the two root causes"; the actual second root cause turned out to be an execution gap (an existing fix never run), not a design gap — closed on the answer the evidence supports, consistent with this row's own long history of correcting its instructions against RAW probes (round5, 08-05 PO round).

### STEP architect-S25 · architect · 2026-08-13T23:33:45Z
**task-id:** SPIKE-BCTC-RECONCILE-EXHAUSTED-REAL-UUID-SUBSET
**what-done:** RAW-probed live DB (readonly, named-volume) for HUT 2025-Q3/BSR 2024-Q1/FRT 2024-Q1 — confirmed real UUIDs, pdf_path SET, 0 rows all 3 extraction tables. Read `pek_engine_adapter.py`/`pek_run_helper.py`/`routes_pek.py`/`bctcExtractReconcileJob.ts` at source. Full-history `docker logs vn-market-intelligence-mcp-pdf-extractor-1` grep (2026-08-08→present, 183 total FAILED traces) found root cause: non-blocking `threading.Semaphore(1)` in `extract_layout_and_tables()` raises `SemaphoreContendedError` instantly on contention instead of queueing; reconcile job's un-throttled batch re-fire (up to 20/tick) creates that contention every 30-min tick. FRT hit it on 8/8 of its passes (100%, fully reproduced); BSR/HUT show the same burst symptom one layer upstream (never reached pdf-extractor's log at all — mcp-server's own logs for that window are gone, container recycled 08-13). Wrote `docs/architecture-briefs/2026-08-13-fix-pek-extract-semaphore-contention-silent-drop.md` (fix: bounded blocking acquire, reusing `ocr_gateway.py`'s already-shipped `_OCR_SLOTS` pattern) + appended `[Architect] Brownfield Findings` to the SPIKE's handoff.
**what-considered:**
- A-30 memory-pressure hypothesis as the primary cause (row's own concurrent hypothesis) — downgraded to correlated-not-causal: the semaphore-contention burst (12:35:03-04Z) sits 1min before A-30's telegram (12:36:20Z), more parsimoniously read as A-30 being downstream of the same burst (thread-pool churn from N simultaneous to_thread calls), not an independent leak. Flagged for post-fix empirical re-check, not a separate mitigation.
- Genuinely-malformed-PDF hypothesis — ruled out: of 67 attempts that won the semaphore, only fallback-id write-gate 400s (53) + 1 timeout failed; the other 12 all succeeded (DONE). BSR/FRT's own 2025-Q1 filings (different PDF content) succeeded outright, proving the engine and these tickers' PDFs are healthy.
- Inventing a new async queue primitive vs reusing `ocr_gateway.py`'s existing bounded-acquire pattern for an analogous shared resource — chose reuse (`always_extend_not_duplicate`); same env-var convention, same optional-override-param shape as `slot(timeout_s=...)`.
**why-decision:** Every claim RAW-verified: live docker-exec DB queries (bctc_layout_units/table_rows/md_tables counts, financial_reports rows, bctc_vps_queue rows, pdf md5 checksums), full-history log greps with exact traceback text, and source reads of every file in the trigger→extract→write chain — not inferred from the row's own prose.
**why-change:** Row's dispatch asked to confirm/refute the A-30 hypothesis and find the real root cause if distinct. Found a 3rd, more specific mechanism (semaphore silent-drop) that the row's own concurrent hypothesis did not name outright, with A-30 folded in as a correlated symptom rather than an independent cause — resolves the "must not amnesty a 2nd defect behind fallback-shell's green AC" warning the sibling row's own PO fold carried.

### STEP architect-S26 · architect · 2026-08-14T02:29:12Z
**task-id:** FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING
**what-done:** Ratified BA's FR-0..FR-7 (7 exact before/after flow-doc edits) against the LIVE `chef.md`/`chef-dish.md` — re-read all 10 anchor quotes byte-for-byte rather than trusting the spec. Found chef-dish.md +44L/chef.md +5L drift since BA wrote the spec (intervening 2026-08-13 commit `c31ee006e`, unrelated Checkpoint-pointer insert), published a corrected live line map so agent-father does not patch the wrong location. Repo-wide grep confirmed zero non-flow-doc consumers of the `BIZ_CTX_*` tokens. Appended `[Architect] Brownfield Findings` to the BA-spec handoff; set `next_agent="agent-father"` + `architect_review_note` on the task_board row via `orch-apply.sh`.
**what-considered:**
- Route via pm for decomposition (architect flow's own RETURN template default) vs direct to agent-father — chose direct: zero application code, edits confined to 2 files agent-father already solely owns, precedent row `FIX-CHEF-QUALITY-VERDICT-FALSE-FULL-NO-LAYER-ASSERTION` (same zone) already carries `next_agent=agent-father` unshipped, and BA's own RETURN explicitly recommended this fan-out.
- Blocker Q1 (opportunistic-vs-force-widen ticker coverage) — ruled within remit rather than escalating: FR-3 as specified already bakes in the safe (opportunistic-only) default per NFR-3, so the open PO question does not block agent-father's execution; a later PO veto is a follow-up FR, not a rework.
**why-decision:** Line-number drift is real and would misdirect a literal patch if left uncorrected — cheap to verify (10 greps), high cost if silently trusted (agent-father edits the wrong line in a 775L file). Direct-to-agent-father routing matches both the dispatch table's exclusive agent-file-lifecycle ownership and the one live precedent for this exact file pair.
**why-change:** No change to BA's technical design (FR-0..FR-7 unmodified) — this cycle's only additions are the drift correction, the blast-radius confirmation, and the routing/blocker rulings BA's own RETURN asked architect to make.

### STEP architect-S27 · architect · 2026-08-14T11:05:00Z
**task-id:** FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58
**what-done:** Resolved BA's Blocker Q1 with an independent methodology (docker exec + bun:sqlite, readonly + busy_timeout — a bare readonly open hit SQLITE_BUSY against the live server, a reusable finding for the audit script's own DB read). CONFIRMS 34: DB, system-map.json, and frontend WATCHLIST_STOCKS are all three byte-identical (34/34/34, zero-diff every direction) as of this cycle — no live 41%-hole exists today. Designed candidate (i′) write-through (new systemMapWatchlistWriter.ts infra adapter reusing alertVerdictStore.ts's proven atomic tmp+rename pattern, 2 call sites in watchlist.ts, fail-open on file-write error) + candidate (ii) audit script (scripts/checks/watchlist-divergence-audit.ts, reuses computeWatchlistDiff/loadSsotWatchlist already exported from the 2026-07-11 migration script — considered extending db-integrity-probe.sh, rejected as a different concern, change-detection pre-gate vs correctness audit). Found domain-model.md's watchlist count doc is genuinely stale (33/30+3 vs live 34/33+1); technical-analysis's two docs are already accurate, no edit needed. Appended [Architect] Brownfield Findings to the BA handoff; next_agent -> pm for per-zone decomposition.
**what-considered:**
- Reconciling AC-2's named tickers (ACB/CTG/MBB/VPB) into the fix scope anyway vs flagging for PO re-scope — chose flag: those tickers exist in NONE of the three planes today (confirmed, not inferred), so satisfying AC-2 literally requires a content decision (calling add_to_watchlist), not an architecture fix; silently reinterpreting an AC is not architect's call.
- Building FR-6 frontend codegen now (BA flagged as "unconditionally required") vs deferring — chose defer: the live evidence this cycle shows zero frontend drift, building unused codegen machinery against a premise the same cycle's own probe refuted would be scope-creep, not the row's real deliverable.
**why-decision:** Every figure RAW-verified via a methodology independent of BA's own docker exec (different tool: bun:sqlite vs BA's unspecified query path), including a diff BA did not run (frontend vs system-map.json) — converging on the same zero-diff answer from two different angles is the strongest confirmation available without gateway access.
**why-change:** BA's spec assumed AC-2/FR-6 were live-blocking regardless of Blocker Q1's outcome; this cycle's evidence shows they are not, under EC-1 (BA's own edge case) which explicitly anticipated this exact outcome — no change to BA's FR-2/FR-3 mechanism design (write-through + audit), only to which FRs are actionable now vs deferred/re-scoped.

### STEP architect-S28 · architect · 2026-08-14T11:02:30Z
**task-id:** FIX-READYLANE-NO-SEVERITY-EXPEDITE-FIFO-BURIES-INCIDENT-P0
**what-done:** Adjudicated the row's 3 candidate shapes against PO's own live throughput-refutation (`po_measurement_20260814`); rejected (a)/(b) (comparator-only), adopted (c) as a dedicated "Incident-Lane Consumer" — independent `INCIDENT_CAP=2` budget outside shared `WIP≤2`, batch-claim, head-decoupled invocation (Session-Gate→Step-1 anchor, unconditional/every-tick) — mirroring QA-Drain's own proven Part1/2/3 shape. Reused the ALREADY-LIVE `po_expedited_at` field as the lane-selector (not a comparator key) instead of inventing `expedite_at`/`incident_ref`. Wrote `docs/architecture-briefs/2026-08-14-readylane-incident-lane-throughput.md`.
**what-considered:**
- Comparator-only fix (candidates a/b) — falsified LIVE this session: replayed RLC's own selection algorithm against the live board and found BOTH live `po_expedited_at` ready[] rows already rank #1/#2 of 68 eligible candidates, yet remain undispatched ~10h+ — perfect ordering provably cannot move a rank-1 row further forward, so a comparator change cannot close this gap.
- 7th rotation-candidate slot for the new consumer vs a head-decoupled unconditional site — chose head-decoupled: a 7th rotation member only shrinks the 1-in-6 throttle to 1-in-7, reintroducing the exact problem at smaller scale; head-decoupled dominates strictly (every tick, idle or busy).
- New `TaskBoardSchema` lane (mirrors QA-Drain's `qa[]`) vs reusing `in_progress[]` with a `claimed_by`-keyed WIP exclusion — chose reuse: `TaskBoardSchema` is `.strict()` on exactly 9 enumerated lanes (SECONDARY-Drain's own header already flags this as "a real schema change, out of scope"); a `claimed_by`-filtered `incident_wip_in_progress` def achieves the same budget-independence with zero schema change.
**why-decision:** QA-Drain's identical-shape fix has an in-repo MEASURED result (review[] PRIMARY 226→56 over 8 days, re-verified live this session, not trusted from the old brief) — the only mechanism in this repo with proof-of-drain on a structurally identical rotation-throttled single-claim starvation problem; reusing a proven mechanism over a novel one.
**why-change:** Row's own scope pre-listed only (a)/(b)/(c) with (c) least fleshed-out; selected + fully designed (c) per the row's own explicit invitation to propose a shape not listed if none of a/b/c pass PO's "would this have unblocked either measured case" test — (a)/(b) fail that test by live demonstration above, (c) (QA-Drain-mirrored) passes.

### STEP architect-S29 · architect · 2026-08-14T22:47:31Z
**task-id:** UC-CDC-P1
**what-done:** Ratified BA's WP-A (READY) with 3 design refinements + corrected the dispatch note's assumed 2-way zone split to a verified 3-way split; re-verified WP-B still BLOCKED live (UC-SDF-P2 unchanged); forwarded the CADRAT-1 co-ship stale-clause flag. Appended `[Architect] Brownfield Findings` to `docs/handoffs/UC-CDC-P1-BA-spec.md`; board row updated (`next_agent="pm"`, `architect_review_note`), stayed in `in_progress[]`, no lane move.
**what-considered:**
- FR-A1 inline `isVnTradingDay()` call (BA's literal text) vs routing through the file's existing `EmitPressureStateDeps` injectable seam — chose the seam: every other server-computed field in this exact file already uses it; inlining would be the one DDD-inconsistent exception, and the file's own test suite already assumes full-seam coverage.
- FR-A2 enforce via Zod-schema `z.enum()` hard-reject (BA's literal schema swap) vs validate inside `runEmitPressureState` — chose the latter: the tool's own docstring + `telemetry.md` Step 6.0 both document/depend on never-throws for this exact MANDATORY call site; a schema-boundary reject's actual surfaced shape to the calling flow-doc was not verified this cycle, so avoided depending on it.
- next_agent="developer" (dispatch note's suggestion) vs "pm" for 3-way decomposition — chose pm: independently confirmed `docs/agents/cowork-team/flow/*.md` is agent-instruction prose under the standing `po_routing_ruling_20260721` precedent (agent-father's zone, confirmed via its `commit_zone.allowed` including `docs/agents/`, developer's does not) — a single "developer" route would hand FR-A4/FR-A5 to a specialist with no commit-zone grant for those files.
**why-decision:** All 3 refinements are load-bearing brownfield facts re-verified live this cycle (deps-seam pattern, SessionStatus type-erasure, agent-father's actual commit_zone), not stylistic preference — each would have produced a broken or misrouted implementation if left as BA's/the dispatch note's literal text.
**why-change:** WP-A's FR text unchanged in substance (still FR-A1..FR-A5 as BA scoped); only the wiring mechanism (FR-A1), enforcement layer (FR-A2), and routing (zone split) are corrected. WP-B/co-ship flag: no change from BA's spec, re-verified only.
