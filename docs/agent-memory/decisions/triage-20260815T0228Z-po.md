# PO Decision Journal — review-lane sign-off 2026-08-15T02:28Z

task_id: OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN (DONE_VERIFIED, diagnosis phase) +
FIX-MCP-OOMKILL-DETECT-VIA-CGROUP-MEMORY-EVENTS (mint, P2) +
FIX-MCP-DUPLICATE-UNCOORDINATED-SHUTDOWN-HANDLERS (mint, P3)

## Input
dev-team review-lane secondary-drain, session 632721c2-41e4-4aff-8d06-a47cf80dc0d7.
Single stale `review[]` row: P1, `plan_only=true`, `supervised=true`, `branch:null`,
`next_agent=po`, `owner=dev-mcp-server`, `secondary_claimed_at=2026-08-15T02:23:56Z`.
Deliverable under review: `dev_diagnosis_20260808T1345` field +
`docs/architecture-briefs/2026-08-08-ops-mcp-restart-churn-cgroup-oom-diagnosis.md` (143L).
`head` was already `idle`/`active_task_id=null` — no head reset owed by this lane move.

### STEP po-S1 · po · 2026-08-15T02:28:17Z
**task-id:** OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN
**what-done:** DONE_VERIFIED sign-off on the diagnosis phase. Re-verified all five checkable
source claims independently before signing, then measured the current runtime outcome.
Moved `review[]`→`done_verified[]` with a `verification.raw_probe` carrying the live values.
**what-considered:**
- Sign off on the brief's own prose — REJECTED by construction. This row carried
  `supervised=true` for one reason: `FIX-MCP-RESTART-ALERT-DEPLOY-DISCRIMINATE` (2026-06-15)
  shipped the very sentinel this diagnosis says cannot fire, so the implementer must not
  self-certify. Accepting the narrative would reproduce the exact failure the flag guards.
- Hold the row open pending `FIX-MCP-MEMORY-CODE-LEAK` — REJECTED on evidence: that row is
  already `DONE_VERIFIED` (archive/2026-08.json, updated 2026-08-08T18:59:00Z) and the runtime
  confirms it worked. Holding open pending a landed, working fix is a no-op hold that would
  have parked a P1 in `review[]` indefinitely.
- Request rework — REJECTED. Nothing in the deliverable is unsound; the one thing it does not
  resolve (Docker's `OOMKilled` under-reporting inside containerd/runc) it explicitly declines
  to claim, with a stated reason, and that question is not what the row exists to answer.
- Reassign / re-route to ops — REJECTED. The row's own re-route condition (host OOM, Docker
  daemon fault, hypervisor) was checked and does not trigger: `CONSTRAINT_MEMCG` is the
  container-cgroup plane, i.e. the app's own growth against the app's own configured cap.
**why-decision:** Verified at source, not relayed. `HostConfig.Memory=3221225472` is byte-exact
against compose `limits.memory: 3g`; `news-fetch` is capped at `1g`, so the arithmetic excluding
it from the ~3GB `anon-rss` victims holds; `composition-root.ts:254` writes the sentinel;
`checkpoint.ts:335` `registerShutdownHook` exists with its opening log at `:337` and
`process.exit(0)` at `:344`; `startScheduler.ts:373` wires it. The mechanism itself
(SIGKILL is uncatchable) is a structural fact, not an inference, so the conclusion does not
depend on evidence that has since decayed. And the outcome is measurable now: container
`37e53e78` (a recreate — the diagnosed `758e5f87` is gone), `RestartCount=0`, 424.8MiB/3GiB
(13.83%) at ~31h uptime, against the diagnosed 2.312GiB/3GiB (77.05%) at ~12h16m. Same cap,
inverted curve.
**why-change:** no change from plan.

### STEP po-S2 · po · 2026-08-15T02:28:17Z
**task-id:** OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN (evidence limits, recorded not papered over)
**what-done:** Recorded two honest gaps on the row rather than signing around them.
**what-considered:**
- Assert the dmesg evidence was re-verified — REJECTED, it cannot be. The kernel ring buffer
  now starts at monotonic `2376744` (~2026-08-12T13:45Z), past the brief's `1916589`/`1951440`/
  `1986045` events. Those three raw lines are unrecoverable.
- Discount the diagnosis because of that — REJECTED. The brief recorded them with a byte-exact
  `task_memcg=/docker/758e5f87...` container ID plus cap arithmetic, which is the durable form.
  Expected retention decay is not a verification failure.
**why-decision:** One event genuinely remains unattributed: the 2026-08-11 14:20:07
`mcpServerStartup` has no preceding `mcpServerCleanShutdown` (prior sentinel 2026-08-08
19:06:00) and falls outside the dmesg window, so its cause is unknowable today. Single event,
no trend, flat memory curve since — not grounds to hold a P1 open, but it is exactly the case
the detection follow-up would have classified, so it is folded there rather than dropped.
**why-change:** no change from plan.

### STEP po-S3 · po · 2026-08-15T02:28:17Z
**task-id:** FIX-MCP-OOMKILL-DETECT-VIA-CGROUP-MEMORY-EVENTS
**what-done:** Minted P2 / `S` / `apps/mcp-server/` / `next_agent=dev-mcp-server`. Not
`plan_only`, not `supervised`.
**what-considered:**
- Close the family entirely now that the trigger is gone — REJECTED. This row's own charter
  says closing the leak removes the trigger but leaves the instrumentation gap, and that is
  still literally true: no userspace sentinel can EVER record a SIGKILL death, so A-21 can say
  a restart was unclean but never why, and the one API field that should answer it lies.
- Carry `supervised=true` forward into the implementation — REJECTED. The mechanism is now
  understood and the ACs are objectively measurable; a WF-2 supervised hold here would only
  reproduce the known `READY-XOR-SUP-OR-PLANONLY` stall class. The anti-self-certification
  requirement is carried instead by AC-4 (raw_probe must carry a real observed counter value)
  plus the RC-VERIF gate, and by routing to qa rather than self-closing.
- Scope it fleet-wide — REJECTED. Reading other containers' cgroups needs privilege this
  service must not take. Self-scoped `/sys/fs/cgroup/memory.events` `oom_kill` only; a fleet
  sweep is a separate ops-lane row if ever wanted.
**why-decision:** The class is demonstrably still live elsewhere: the retained ~2.5-day dmesg
window holds 4 memcg OOM kills, all `python3` — 3 at ~1.0GB against rag-service's `1g` cap, 1
at 2.35GB against pdf-extractor's `2.5g` cap. `docker inspect` will misreport those identically.
Closing this family without leaving a way to SEE the next one is how it went unnoticed for weeks.
**why-change:** no change from plan.

### STEP po-S4 · po · 2026-08-15T02:28:17Z
**task-id:** FIX-MCP-DUPLICATE-UNCOORDINATED-SHUTDOWN-HANDLERS
**what-done:** Minted P3 / `XS` / `apps/mcp-server/` / `next_agent=dev-mcp-server`, with the
brief's own severity claim CORRECTED on the row.
**what-considered:**
- Mint at the severity the brief implies — REJECTED on measurement. The brief reasoned that the
  shorter, import-free `checkpoint.ts` path "is very plausibly the one to win a real race", so
  even a graceful redeploy risks losing its sentinel. Checked against `cron_job_runs`: of the 5
  restarts since 2026-08-08, 4 wrote a clean-shutdown sentinel seconds before the next startup
  (08-08 16:59:46→16:59:54, 08-08 19:06:00→19:06:24, 08-11 18:25:17→18:25:32,
  08-13 19:18:30→19:18:42). Real latent defect, zero observed cost today.
- Fold it into po-S3's row — REJECTED, different fix, different file, different failure mode
  (graceful path vs crash path); folding would let the P3 hitch a ride on a P2 it does not share
  an acceptance test with.
**why-decision:** A plausible-mechanism argument from code reading is a hypothesis, and the
production data contradicts it. Minting at P3 with the contradicting measurement written onto
the row is what stops a future reader re-inflating it — and the row explicitly says do NOT
re-file it as a cause of the historical unclean-restart count, because at every one of those
boundaries BOTH handlers were absent, not racing.
**why-change:** Deviated from the brief's implied priority; deviation is recorded on the row
itself with the measurement that justifies it.
