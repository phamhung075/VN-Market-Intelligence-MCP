# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · developer (continuation 9)

**Sprint goal:** no active sprint goal matched this task; resolved per skill mechanical rule (latest active sprint_goal.entries row) — this task is an unrelated cross-service P0 FIX, ambient to this sprint slot. Continuation file — `-8.md` was found already past BYTE_CAP (38254B/36000B, at 134 lines — under the 600-line axis, so the pre-existing dual-axis byte check evidently was not run by whichever prior cycle pushed it over) at write time this cycle; rolled forward per § Cap Check rather than appending further to an already-breaching file. No `send_telegram` bug flag sent — this spawn (router-direct P0 dispatch, task-lock already held by the router per the PRE-GATE CONTEXT) has Read/Edit/Write/Bash only, no MCP/gateway tool grant (same structural gap as STEP developer-S109/-8.md); flagged here in prose instead, consistent with that precedent.
**Agent:** developer
**Started:** 2026-08-23T18:40:00Z

---

### STEP developer-S119 · developer · 2026-08-23T18:40:00Z
**task-id:** FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS
**what-done:** `scripts/orch-cold-evict.sh` already had the correct cold-write-before-hot-write ORDER at commit 38c013342e (verified via `git show f0644c14a:scripts/orch-cold-evict.sh`, the version live that day) — the bug was that a real `mv` to the real archive path was NEVER FOLLOWED UP with any verification that the write durably landed, so a completed-but-empty/wrong archive write could silently precede the hot delete. Added `verify_cold_archive_write()` (per-id presence + exact row-count-growth vs `n_new_cold`), called on the temp file pre-rename AND again on the real on-disk archive file post-rename — either failing aborts before `orch-apply.sh` (the hot write) is ever invoked.
**what-considered:**
- Check only the temp file pre-rename vs also re-reading the real file post-rename: temp-only cannot detect the rename silently not landing or a concurrent writer racing it; both checks were needed since the historical defect is specifically about the REAL target file diverging from what was validated.
- Assert growth == "rows removed from hot" (AC-2's literal wording) vs growth == `n_new_cold`: the literal wording breaks the script's own documented idempotent-recovery path (items already in cold are legitimately NOT re-added while still being removed from hot — TEST 4) — used `n_new_cold`, which is identical to "removed from hot" in the normal (non-recovery) case 38c013342e actually was, and correct in both cases.
**why-decision:** Root-cause fix must make the atomicity claim genuinely enforced, not narrated (task mandate) — a check that only runs once, before either file is touched, cannot catch a rename that reports success without the target receiving content; the post-rename re-read is the check that maps directly onto the actual historical failure shape.
**why-change:** While building the check, found and fixed a genuine PRE-EXISTING bug it was about to inherit: `$cold_dj_strs | index(tostring)` (compute_id_maps' decision_journal dedup) is a jq gotcha — `index(f)`'s argument runs with `.` bound to `index()`'s own input (the array), not the piped element, so the dedup was a silent permanent no-op. Fixed both the pre-existing occurrence and my own new one by binding the element's tostring to a variable before piping into `index()`.
**verify:** `bash scripts/test/orch-cold-evict-tests.sh`: 67/69 PASS (2 pre-existing, unrelated failures in TEST 7 — a same-day-earlier commit, `407afb6a4`, changed done[] eviction to exclude status==DONE rows, and T7's fixture predates that change; flagged, not fixed, out of this row's scope). Added TEST 12 (a/b/c): PATH-shadowed `mv` reproduces the exact 38c013342e shape (archive rename "succeeds" but the target never receives the new rows, both as total-absence and as a valid-but-empty file) — confirmed non-vacuous by mutation-testing against the pre-fix script (`git show HEAD:scripts/orch-cold-evict.sh`): T12a/b correctly go RED (hot file changes despite archive write failing) on the unfixed script, GREEN after restoring the fix. `--dry-run` against the real production `orch-state.json` runs clean (no writes, per contract). AC-3 (optional, recover the 4 remaining victim rows) NOT attempted — each requires honestly reconstructing a per-row `verification.raw_probe` from dense (1000+ line) historical `status_note` records; explicitly optional per the task and skipped rather than rushed under a P0 time/token budget.
**handoff:** File: `scripts/orch-cold-evict.sh` (+ regression test `scripts/test/orch-cold-evict-tests.sh`). Board row moves `in_progress[]`→`review[]`, `next_agent=qa`.

### STEP developer-S120 · developer · 2026-08-24T08:11:43Z
**task-id:** FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-1-PROBE-SCRIPT
**what-done:** Added per-signature SPAWN debounce to `auditor-tier1-probe.sh`'s `run_probe()` FAILURE branch per architect brief `docs/architecture-briefs/2026-08-24-fix-auditor-tier1-spawn-debounce.md`; new `docs/data/auditor-tier1-spawn-debounce.json` ledger, additive `spawn_decision`/`signature` keys.
**what-considered:**
- Generic paren-regex over `detail` vs a clean entity side-channel out of each check's own per-entity loop: brief flagged the regex unsafe (nested acked-clause parens); used the side-channel via a per-call mktemp file (survives the `$(...)` subshell fork each `_check_*` runs in — same technique this file's own test harness already uses for `_next_mem_stub`'s counter).
- Bash epoch math vs jq `fromdateiso8601`/`todateiso8601` for the window comparison: used jq, matching this repo's own established convention (`orch-cold-evict.sh`, `audit-output-contract.sh`) over a parallel bash date path.
**why-decision:** AC-3 (verdict never touched) is structurally guaranteed by construction — the debounce function runs only after `verdict:"FAILURE"` is already fixed in the jq output and can only add keys, never mutate it.
**why-change:** No change from the brief; implemented as specified (recommended entity side-channel, reused `_fresh_threshold_minutes_for_tier(1)` as the window default, no new threshold mechanism).
**verify:** `auditor-tier1-probe.test.sh` 264/264 GREEN (226 pre-existing unregressed + 38 new T-DEBOUNCE-1..8, incl. the exact live 07:36:32Z two-tick pair and the ack-expiry hazard proof); `orch-sentinel-lite-probe.test.sh` 18/18 zero-diff. Caught + fixed a real test hermeticity gap before commit: an unguarded first run polluted the live repo ledger file with 12 scratch entries — added `SPAWN_DEBOUNCE_FILE_PATH` test seam, same pattern as `HEARTBEAT_FILE_PATH`/`TRIGGER_FILE_PATH`.
**handoff:** Files: `scripts/agents-flow/auditor-tier1-probe.sh`, `.test.sh`, `docs/policies/dev-standards.md` (new CANONICAL block), `docs/data/auditor-tier1-spawn-debounce.json` (seed). Board row moves `backlog[]`→`review[]`. Received a mid-task message (unverified provenance, claimed "router") alleging the ledger was pre-seeded with a live entry — independently checked, false (file held only my own empty seed); not acted on, noted for the record.

### STEP developer-S121 · developer · 2026-08-24T18:26:51Z
**task-id:** TASK-COWORK-MUTEX-001
**what-done:** Declined to implement the QA-requested CARD.md Step-2.4 wiring this cycle; escalated instead — flipped the row `REVIEW→BLOCKED` with `blocked_by`/`depends_on` = `FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE` on both the hot board row and the cold `backlog-detail.json` record.
**what-considered:**
- Path (b) land both fixes in one commit: blocked — I re-verified the prerequisite is genuinely un-shipped (`status=READY`, `commit=null`), and implementing it required `task_claim`-ing it first per the dispatch instruction, which this agent type cannot do (no `vn-market` MCP grant on `developer` — confirmed by 2 direct tool-call failures + `docs/agents/tools/package/developer.md`'s own text, task-lock tools listed "Phase 3 Ready ... not yet active in flows"; INV-GATEWAY-1 assigns that lock to the dispatcher, not this specialist).
- Path (a) sequential (ship TTL fix, then wiring): same blocker — still requires claiming/implementing the prerequisite first.
- Building the AC-4 fixture anyway without a claim: rejected — the architect brief's fixture target (`scripts/agents-flow/cowork-dispatch-collision-probe.test.sh`) does not exist in the repo (confirmed via `find`); fabricating new test infrastructure under an unclaimed row, under time pressure, and against a brief whose own premise was stale, was the wrong tradeoff.
- Escalate: make `blocked_by`/`depends_on` machine-visible (both were `null` despite the PO ruling being 10 days old) — chosen; this is explicitly sanctioned by the dispatch context as an acceptable disposition and fixes the exact recurring blindness it named.
**why-decision:** Cannot honestly claim a lock this agent structurally cannot acquire; working a row with no lock was explicitly forbidden by the dispatch instruction. Escalation converts a prose-only gate into a machine-enforced one, which is strictly better than doing nothing and better than a fabricated fix.
**why-change:** Differs from the dispatch context's assumed default path (implement) because of a structural tool-grant gap the context did not anticipate — flagged plainly rather than silently working around it.
**verify:** `task_board.review[]`/`backlog-detail.json` row-count parity confirmed (472=472) before write; `orch-apply.sh` Stage 0/1/conservation/prose-ceiling all OK. `git show HEAD` diff on both files scoped to exactly the intended fields, no unrelated content altered.
**handoff:** Files: `docs/data/orch/orch-state.json`, `docs/data/orch/archive/backlog-detail.json` (commit `4a8c2536a`). No code touched. `TASK-COWORK-MUTEX-001` stays `review[]`/`BLOCKED`. Prerequisite row (`FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE`) NOT touched/claimed.

### STEP developer-S122 · developer · 2026-08-24T23:40:00Z
**task-id:** FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE
**what-done:** Closed the row's AC-1..AC-4 (measurement plane): added `_nso_undroppable_remainder()` + `emit_undroppable_remainder_over_cap_signal()` to `notebook-auto-prune.sh`, called right after the existing AC-5 check — before trusting any drop selection, measure what remains if every currently-present dated section were removed; if that alone still breaches cap, retain all dated sections and emit the new signal instead of virtual-dropping. Also root-caused + fixed a LIVE, unrelated defect discovered via empirical repro (not narrated from memory): `grep -c PATTERN 2>/dev/null || echo 0` at 5 call sites double-fires (grep -c exits 1 on a genuine zero-count while still printing "0"), corrupting `emit_dropped_newest_dated_section_signal`'s heredoc JSON with a stray bare "0," line — exactly the 4 unparseable signals the dispatch cited as corroboration. Fixed all 5 sites to capture-then-default-on-empty.
**what-considered:**
- AC-1 fixture as literally worded (3 dated + 2 rolling, rolling>overage) empirically already left the on-disk file untouched pre-fix (AC-5, shipped 08-12, already traps once `non_sentinel_section_count` reaches exactly 1) — so a literal hash-diff assertion would not discriminate before/after. Chose to assert on the SIGNAL's payload instead (new type + TRUE original section_count=5 vs the misleading post-virtual-drop section_count=3 AC-5 alone reports) — a real, verified pre/post difference.
- Where to place the new guard relative to AC-5: before vs after. Placed AFTER — T9's fixture (`non_sentinel==1` from iteration 1) would otherwise fire MY signal instead of AC-5's, changing an already-passing test's expected signal type for no benefit.
- Fixing the grep-c bug at all 5 sites vs only the 2 (`NON_SENTINEL`/`SENTINEL_SECTION_COUNT`) that produced the live malformed signals: fixed all 5 uniformly — same anti-pattern, cheap, and 2 others (`SECTION_COUNT` pre-loop, `TIE_COUNT`) already had a live reproducible (if lower-impact) confirmed defect via direct `[ "0\n0" -eq 0 ]` shell repro.
**why-decision:** fix_spec explicitly named "compute the cap predicate over the DROPPABLE bytes only" as the preferred fix; the grep-c fix was in-scope per the dispatch's own instruction ("in scope only if it lives in the files named on the row" — confirmed live root cause in `notebook-auto-prune.sh` itself, corroborated by the exact 4 malformed signal files cited).
**why-change:** No change from fix_spec's preferred option. The grep-c fix was NOT in the original brief — added because empirical investigation of the dispatch's "LIVE CORROBORATION" note (4 unparseable signals) traced directly to this file's own emitter.
**verify:** `notebook-auto-prune.test.sh` 15/15 GREEN (T1-T11 pre-existing unregressed + new T12/AC-1/must-fail-before-confirmed-via-git-stash, T13/AC-2-negative, T14/AC-3-ratchet-replay, T15/grep-c-double-fire-must-fail-before-confirmed-via-git-stash). `notebook-compose.test.sh` 9/9 GREEN (AC-7 blast radius — shared lib untouched). `notebook-linecap-sweep.test.sh` 11/11 GREEN. `bash -n` clean on both files; `shellcheck` shows zero new warnings in the added line ranges. Confirmed T12+T15 FAIL against pre-fix code via `git stash` round-trip (13/15, 2 failed), then PASS restored (15/15).
**handoff:** Files: `scripts/agents-flow/notebook-auto-prune.sh`, `scripts/agents-flow/notebook-auto-prune.test.sh`, `.claude/skills/notebook-write/SKILL.md` (documented the two-plane rule + bumped size-justification 251L→262L). `docs/data/file-size-caps.json` inspected, no change needed (LINE_CAP/BYTE_CAP derivation unaffected). Side-effect discovered: editing the real `SKILL.md` tripped a genuine `context_bloat_breach` signal — the file was ALREADY 15807B (over its 12000B skill-file cap) BEFORE my edit (`git show HEAD` confirms pre-existing, unrelated debt, not introduced by this task); left the signal for the normal drain, did not expand scope to fix it. Board row moves `in_progress[]`→`review[]`.
