# PO decision journal — triage tick 2026-08-24T00:37Z (run 01:04–01:12Z)

task_id: `FIX-AUDITOR-DCYCLE2-COMPLETION-EVIDENCE-PREDICATE-CANNOT-SEE-COMPLETED-CYCLES`

## Decision 1 — refuted both `auditor_cycle_missing` signals instead of treating them as a coverage hole

**what-considered:** (a) treat as a real coverage hole and escalate auditor availability, as the dispatching router instructed; (b) fold as noise; (c) verify the premise first.

**why-change:** Took (c), and it inverted the answer. The dispatch brief said "the tier-1 one is partly a consequence of the router declining a Tier-1 spawn on measured grounds, so treat it as a real coverage hole, not noise." Measured, the auditor ran anyway:

- Tier-1 c116 completed `2026-08-24T00:30Z` — `docs/agent-memory/notebooks/system-auditor.md` line 3 `## c116 · 2026-08-24T00:41Z`, line 5 `### Audit Run Tier-1 (2026-08-24 00:30Z — Runtime Ping)`, commit `3dc3ee791`. The signal fired at 00:41:14Z, 11 minutes later.
- Continuous tier-1 coverage across the alleged 3h gap: `7bb97380d` (21:30Z), `b491c4ec4` (22:30Z), `d612606ae` (23:16Z), `7da7c87a5` (23:30Z), `3dc3ee791` (00:30Z).
- Tier-2 completed 00:49Z (`4145766ac`) — 8 minutes *after* its own "missing cycle" signal.

So the gap is in the detector. Two distinct defects in `scripts/auditor-durability-sweep.sh` `_run_sweep_0b2()`:

1. **Tier-1, `_t1_latest_notebook_ts()` :217-238** — the loop keeps whichever tier-1 section came *last in file order*; it never takes a max. The compose actuator writes newest-first (commit `1e34634d9` stamps `direction=newest_first`), so "last in file order" is the **oldest** section. Replayed the function verbatim against the live notebook: returns `2026-08-23T21:03Z` (c114, line 242) while c116/00:41Z sits at line 3. Reported staleness *grows* as more history is retained.
2. **Tier-2/3, the `for n in 2 3` loop :304-325** — heartbeat-only, no notebook fallback at all. `_write_heartbeat` is gated on an empty failures list, so the heartbeat freezes by design for the whole duration of any genuine degradation.

Shared consequence, and the reason this is P1: the detector is **anti-correlated** with the condition it exists to catch — blind exactly during sustained degradation, noisy in the same window.

**Rejected remedies, stated on the row:** widening the bar, raising the cadence multiplier, or downgrading WARN severity. AC-3 is a negative control requiring the signal to still fire when notebook evidence is genuinely absent.

## Decision 2 — retargeted the fleet-push ack rather than removing it

The dispatch brief's premise ("dead `tracked_by` pointer") was wrong and I verified the correction at source before acting. `FIX-FLEET-PUSH-LAUNCHD-EXCONFIG-SILENT-DEAD` is `DONE_VERIFIED` in `docs/data/orch/archive/2026-08.json`, absent from `orch-state.json` (0 hits). The probe resolver (`auditor-tier1-probe.sh:259-266`) unions task_board lanes *inside* `orch-state.json` — including a lane named `archive`, which is **not** the monthly archive file — so it reports `ABSENT`. That label reads as a dangling id but means cold-evicted.

The ledger's staleness rule required removing that closed entry. Bare removal was wrong: fleet-push genuinely still exits 1, for a **different** cause. `fleet-push.log` shows the agent creating a worktree, symlinking node_modules, passing a bounded tsc check, reaching the push, then `[size-lint] FAIL … pushBctcLayoutHandler.ts baseline-tolerance-exceeded (228/252/250)` → `[pre-push] BLOCKED`. Repointed to `FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH` and verified the resolver now returns `BACKLOG`.

Also corrected `FIX-LAUNCHD-ACK-LEDGER-DEAD-TRACKEDBY-AND-LANE-RESOLUTION-FP` in place — its own note (a) called the pointer a true-positive dangling id and named `FIX-FLEET-PUSH-LOG-NO-ROTATION-UNPROVEN-ROOTCAUSE` as the repoint target. Both wrong; corrected rather than inherited.

**Not a threshold change.** Label set unchanged, staleness rule intact, entry expires when that row reaches DONE_VERIFIED. Recorded the honest caveat in the entry's `signature`: that row is deliberately held undispatched pending a user decision, so this ack will hold longer than a normal fix cycle.

**Masking risk, recorded on the new row:** `launchd_agents` was the last red dimension (mem_creep is back to PASS at 84.84% against an 85.00% gate — a 0.16pp margin, oscillating, not a resolution). Acknowledging it lets the probe return ALL_GREEN, which unfreezes the tier-1 heartbeat and will silence the tier-1 `auditor_cycle_missing` signal **without fixing either defect**. Stamped a `masking_warning` field so nobody reads the quiet as a fix.

## Decision 3 — signed off the 6-row `UC-CCA-P2` cohort

**what-considered:** (a) leave to the SECONDARY-drain at one per tick (6 more ticks); (b) batch off the cohort-level AC-4 result; (c) verify each at source under a per-row lock.

**why-change:** Took (c). (b) was explicitly warned against and would have been wrong for `UC-CCA-P2-MARKET-WATCHER`, which carries **no QA `status_note`** at all, unlike its 5 siblings — a cohort-level pass would have rubber-stamped the one row with no prior review. Verified it from scratch; it holds (`market-watcher/flow/main.md:61` is the real gate, "covers cycle.md AND eod.md").

Locked each row via `task_claim` before the write, released all 6 after. The schema refused `DONE_VERIFIED` without `verification.raw_probe{tool,args,live_value_observed,observed_at}` — correct discipline — so the per-file line numbers are on the rows, not in prose.

**Honest limit, recorded on every row:** all six have `acceptance: null`. There was no formal AC list to test. I verified the deliverable each row's title and QA note describe (Step 0-GW gate present; positioned before `task_claim` where the file makes one). Runtime behaviour of the gate was **not** exercised — this certifies the flow-doc insertion, not a live gateway-down drill.

One self-caught false positive worth recording: my first ordering check flagged `market-watcher/flow/cycle.md` as gate-after-claim. The `task_claim` at line 1 is prose inside the size-justification HTML comment, not a call site; real calls are at 251/288, both after the gate.

## Decision 4 — held 4 envelopes through the CLEAR, again

Re-grepped the Pipeline-A routing table before deciding, rather than repeating last run's conclusion: `sprint_registry_dangling_ids` 0 hits, `recurring-bug` 0 hits, `system_issue` (underscore) appears only in the count table at `:72` — the routing rows at `:39`/`:86` are the **hyphen** form. No row exists for any of the three, so clearing them would green the coverage guard by deleting its own input. Held. Verified by re-reading the file from disk after the write: inbox = 4, exactly those.

Did **not** run `scripts/audits/guard-signal-type-coverage.sh` — it writes live `orch-state.json` and its `--check` flag is a no-op alias, not a dry-run.
