# Decision Journal — PO triage tick 2026-08-25T15:18-15:30Z (router-direct) · po

**Scope:** Close the OCR-backend question with a board record, and unblock the row that actually carries the user's standing "improve BCTC extraction quality" goal.
**Agent:** po
**Supersedes:** `docs/agent-memory/decisions/triage-20260825T1345Z-po.md` on the PaddleOCR numbers ONLY. That tick's *verdict* (do not swap) stands; its *measurements* came from a benchmark that ran `lang="en"` and are retracted.
**Started:** 2026-08-25T15:18:49Z

---

### STEP po-S1 · po · 2026-08-25T15:20:00Z — dedup by SUBJECT before minting
**what-done:** Grepped for the OCR-backend subject across `task_board.archive[]`/`closed_sprints[]`/`done[]`/`done_verified[]` AND the cold archives `docs/data/orch/archive/{2026-06,2026-07,2026-08,backlog-detail}.json`. Zero rows cover the `lang=en->vi` fix or the backend-selection question.
**what-considered:** Four nearest neighbours surfaced on keyword and were read in full before being rejected as different subjects: `FIX-ERRAUDIT-W1-PEK-P0` (PaddleOCR *load failure*, a crash, not a lang config), `FACTORY-PDF-paddleocr-score-07-mask` (cell-score default of 0.7), `FIX-PDFX-TESSERACT-CONCURRENCY-VIOLATES-SINGLE-WORKER-INVARIANT` (worker concurrency), `FACTORY-PDF-extract-tesseract-config` (DPI/config constant extraction). Keyword overlap on `paddle`/`ocr`/`tesseract` is dense on this board — subject-level reading is what separated them.
**why-decision:** Row-dedup on this board is known blind to archived lanes and has re-shipped already-landed work. A keyword pass alone would have produced four false duplicates here and could have suppressed the mint entirely.
**why-change:** no change from plan.

### STEP po-S2 · po · 2026-08-25T15:22:00Z — the reported `task_zone=null` defect does not exist
**what-done:** Probed the field before fixing it. `task_zone` is carried by **0 of 837** board rows and the literal string appears **nowhere** under `scripts/` or `apps/`. The schema field is `zone`, and it has read `"apps/pdf-extractor/"` on this row since it was minted at 13:45Z.
**what-considered:** (a) Set `task_zone` as instructed — rejected: it would have added a field no consumer reads, and the passthrough schema would have accepted it silently, leaving a permanent decoy on the row. (b) Set `zone` — rejected as a no-op, it is already correct. (c) Report the probe artifact and change nothing — taken.
**why-decision:** "A null zone yields an unspawnable head" is a real failure mode, but it was not this row's state. Writing a fix for a defect that is not there manufactures the appearance of a repair and hides that the probe, not the row, is what needs correcting.
**why-change:** DEVIATION from the caller's brief, deliberate. Recorded on the row itself as `po_zone_probe_correction_20260825T1524Z` so the next reader does not re-derive it.

### STEP po-S3 · po · 2026-08-25T15:24:00Z — priority ruling: P1 + incident lane, NOT P0
**what-done:** Held the row at P1 and stamped `po_expedited_at`, which routes it through the Incident-Lane Consumer. Also placed it at `ready[0]` rather than appending.
**what-considered:**
- **Promote to P0** — rejected. There is no live outage, crash or data-loss here; it is a quality defect on a code path that has been silently degraded for as long as it has existed. Six genuine P0s already sit in `ready[]`. Inflating this one buys a single band position and devalues the band for everyone else.
- **Append with `+=` at P1** — rejected. RLC's tiebreak is *array index*, not age. Measured: our row would have ranked **60th of 60** eligible rank-1 rows, i.e. effectively never.
- **`po_expedited_at` + `ready[0]`** — taken. ILC is an UNCONDITIONAL every-tick site (no head gate, no rotation gate) with `INCIDENT_CAP=2` on a budget entirely separate from the shared `WIP<=2` slot. Measured live at mint time: zero other `ready[]` rows carry `po_expedited_at` and `incident_wip_in_progress = 0`.
- **Fold onto the sibling `FIX-PDFX-PEK-TABLE-REGION-COLLAPSED-TO-ONE-CELL...`** — rejected. That is a genuinely larger lever but it is an architect contract decision on a port signature; this row is a self-contained `S` that a specialist can land now. Kept as `related`.
**why-decision:** The expedite field is the mechanism built for exactly this case — "PO wants this dispatched now" without minting a fourth priority tier. Using the band instead would have been the lazier and less honest instrument.
**why-change:** Priority unchanged from the 13:45Z mint (P1). The *placement* changed.

### STEP po-S4 · po · 2026-08-25T15:24:03Z — verified the dispatch claim instead of asserting it
**what-done:** Ran `scripts/devteam-backlog-claim-incident-lane-consumer.jq` as a **dry run** (jq only, no `orch-apply.sh` pipe) against the post-write live board.
**what-considered:** Asserting "it will be picked up next tick" from reading the script — rejected. Prose reasoning about picker eligibility has been wrong on this board repeatedly; the script is cheap to actually run with the output thrown away.
**why-decision:** The dry run returns `ILC CLAIMS: FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS`, `dispatch_lane=dev-pdf-extractor`, and moves `.head` to it. Sole candidate. That is evidence, not a forecast.
**why-change:** no change from plan.

### STEP po-S5 · po · 2026-08-25T15:24:03Z — terminal lane choice: `done[]`/DONE, not `done_verified[]`
**what-done:** Minted `OCR-PADDLE-VI-LANG-FIX-AND-REBENCH` into `done[]` with `status: "DONE"`.
**what-considered:** `done_verified[]` was the first instinct — the work is complete and the commits are in HEAD. Rejected on two counts. (i) The schema gate (`checkVerificationGate`, `orchStateSchema.ts` §8A) requires `verification.raw_probe{tool,args,live_value_observed,observed_at}` on any `DONE_VERIFIED` row, and `RC_VERIF_GRANDFATHERED_IDS` is frozen and closed. Composing a `raw_probe` to satisfy a gate is precisely the fabrication that gate exists to stop. (ii) More importantly it would be *false*: the `lang="vi"` fix is committed but the live container was never rebuilt, so no independent production verification of this change exists. `DONE` is a member of `TERMINAL_SET`, so the row is still terminal and still cold-evicts on the normal `DONE_MAX_AGE_DAYS=7` path.
**why-decision:** The row's honesty about its own deployment state is load-bearing — it is what makes the separately-minted deploy-debt row legible.
**why-change:** no change from plan.

### STEP po-S6 · po · 2026-08-25T15:24:03Z — deploy debt: new row, because the obvious homes both reject it
**what-done:** Minted `OPS-PDFX-REDEPLOY-DEBT-LANG-VI-FIX-INERT-IN-PRODUCTION` (backlog, P3, `depends_on: ["UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT"]`).
**what-considered:**
- **Add an occurrence to `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`**, which is the systemic row for this exact class and already carries a `po_occurrences[]` array — this was the *preferred* option. Rejected on measurement: that row is **14,205 prose bytes**, already over the 12,000 ceiling. Growth-only Stage 2.5 hard-rejects any append. The prose ceiling is once again acting as a starvation mechanism on the rows that most need annotating.
- **Append to `UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT`** — rejected as an active contradiction. That row's own `non_goals` forbid rebuilding or restarting pdf-extractor, because AC-7's passive sampler needs >=12h of cgroup counters and every `memory.*` counter resets on container recreation. A "please rebuild" instruction on a row that says "do not rebuild" is how a dispatch gets burned.
- **A field on the terminal `done[]` row only** — rejected: terminal lanes are not swept, so it would be recorded and simultaneously lost.
**why-decision:** A separate row with a hard `depends_on` records the debt, keeps it out of the dispatch queue until the sampler window closes, and cross-links all three rows in both directions. `deps_satisfied` requires `DONE_VERIFIED`; the blocker is `READY`, so it cannot leak into a picker.
**why-change:** Escalated from "record it in prose" to "mint a dependency-gated row" once the two fold targets were measured and both failed.

### Not done, by scope
- Did not run the full PO pre-check chain (TNB audit, channel audit, signal-dashboard drain, supervised-goahead, manual-dispatch sweep). This was a router-direct, narrowly-scoped board tick, not a triage tick; four unrelated `docs/signals/bctc_signal_*` files are sitting untracked and undrained.
- Did not rebuild or redeploy pdf-extractor, and did not touch any container. Ops zone.
- Did not push. Standing disarm.
