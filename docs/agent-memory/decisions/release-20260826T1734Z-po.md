# Decision Journal — PO re-gate + release of the four PDFX rows frozen behind next_recheck_not_before=2026-08-26T17:11:00Z · po

**Scope:** router-dispatched ad-hoc ruling tick. Re-gate/release 4 `backlog[]` rows at status=BLOCKED, rule on the AC-7 sampler result, and leave no row gated by a mechanism no consumer honours.
**Agent:** po
**Started:** 2026-08-26T17:25:25Z
**task_id:** UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT (the row this tick actually advanced; the 4 frozen rows are its downstream)

---

### STEP po-S1 · po · 2026-08-26T17:26:00Z
**what-done:** Re-derived the row set and found the caller's dependency inventory materially incomplete on one row.
**what-considered:** The spawn prompt stated `FIX-PDFOCR-ORIENTATION-CORPUS-…` has `depends_on null`. True as written — but the row carries a **`.depends`** array (legacy field name) naming `FIX-MARKETDB-20260826-RESTORE-…`. `scripts/lib/devteam-eligibility.jq:232` `effective_depends_on()` unions `.depends_on` + `.depends` + `.blocked_by`, so that edge is fully live to all four pickers. Accepting "no dependency" would have released a row whose own `blocked_reason` declares "TWO HARD GATES, BOTH REAL", gate (1) being DB health.
**why-decision:** A caller-supplied dependency summary is a convenience copy. Resolved every id myself against the live board + all cold archives.
**why-change:** Ruling on that row changed from "release" to "release from the clock, keep a real DB-health dep".

### STEP po-S2 · po · 2026-08-26T17:29:00Z
**what-done:** Ruled on the AC-7 sampler result, and reached a materially different reading than the summary handed to me.
**what-considered:** The caller reported "peak 2.4975 GiB = 99.9% of cap; memory_events_max rose 1645→2539 (+894 allocation-throttle events during the window); oom=0, oom_kill=0" and explicitly declined to interpret it. Read at sample granularity the series is **not** a diffuse ramp: `memory_events_max` is flat at 1645 from 05:11:30Z to 13:59:14Z, steps 1645→2133→2348→2538 across 14:04:15Z–14:14:20Z, reaches 2539 at 14:49:30Z, and is flat for the remaining 2.6h. All +894 increments live in one ~45-minute episode. `anon` fell to 1.384 GiB at 14:54:33Z — **below** the 1.577 GiB pre-burst rest — so the pages were fully returned and there is no residue. I initially hypothesised an unreturned-allocator leak from the coarse endpoints; checking monotonicity (25 decreasing steps of 147) falsified it before it reached any row.
- Then correlated with `docker logs -t`: `POST /extract` burst 14:02:05Z–14:15:34Z including **8× `429 Too Many Requests`** (concurrency semaphore saturated) plus `POST /pek-extract` 14:46:18Z running 61-page `CTG_2025_Q4.pdf`.
**why-decision:** Two conclusions follow that the endpoint summary cannot support. (a) The window was **not passive** — it captured a real concurrent BCTC workload, which makes it more informative than specified, not contaminated-and-worthless. (b) 0 OOM kills is **not** evidence of headroom: an ordinary workload reached 99.90% of the 2,684,354,560 B cap, leaving 2,727,936 B (2.6 MiB) at peak. That is the number AC-5 of `DECIDE-PDFX-OCRWORKER-…` must sweep against.
**why-change:** Recorded the burst boundaries and the identified cause on `UNBLOCK-PDFX-OPS-…` (whose own `non_goals` says measurement results belong there) rather than restating the caller's endpoint framing.

### STEP po-S3 · po · 2026-08-26T17:31:00Z
**what-done:** Chose the replacement gating mechanism for all four rows: real `depends_on`/`depends` edges, never status=BLOCKED, never a task-lock.
**what-considered:**
- Re-freeze with a fresh `next_recheck_not_before` — rejected. Only `scripts/devteam-review-claim-qa-drain.jq` reads that key, over `review[] ∪ done[]`; it never sources `backlog[]`. The key is inert on these rows and was inert the whole time.
- A task-lock, as the caller offered — rejected. A lock is a *deferral* primitive with a TTL; three of these four rows are gated on a *completion event*, which is what a dependency edge encodes. A lock would also expire under a live holder (`feedback_resume_key_ttl_expires_under_live_agent_inverting_dedup`).
- Strip `FIX-BCTC-CTG-…`'s dependency on the archived `TASK-W5-…-CTG-CARRY-FORWARD` — rejected. Ran the real `devteam-eligibility.jq` with `--slurpfile archive`: it resolves **DONE_VERIFIED** from `archive/2026-07.json .done_tasks[181]` via `dep_status_map($archive)` (shipped 2026-07-28). All four pickers thread that slurpfile. Pointing at an archived id is correct; stripping it destroys provenance for zero mechanical gain.
**why-decision:** Every residual gate now has a live completion event and a live owner. Zero rows left in a state no consumer allowlist admits.
**why-change:** `MEASURE-PDFX-…-BASELINE` gained a NEW dep on `UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT` — see po-S4.

### STEP po-S4 · po · 2026-08-26T17:32:00Z
**what-done:** Sequenced the baseline BEHIND the rebuild, and re-routed the rebuild row from `po` to `ops`.
**what-considered:** Releasing `MEASURE-…` to run tonight on the current image was the caller's evident expectation. Rejected: its AC-3 must state the image id + git_sha (the running image's `vn.market.git_sha` label reads the literal string `unknown`), and my own binding `po_correction_20260826T0604Z` requires the baseline to name the `BCTC_MAX_PADDLE_RESCUE_FIRES_PER_DOCUMENT` in force. That constant ships in `1db5f9f81`, which is **not** in the running image. A baseline measured today would describe a configuration (unbounded rescue fires) that ceases to exist at the next rebuild, and would not be comparable with `DECIDE-…` AC-3/AC-4, which mandate a post-`1db5f9f81` image on the *same* document set.
- Leaving `UNBLOCK-PDFX-OPS-…` at `next_agent=po` — rejected. Its AC-1 is a container rebuild; its own title says "Docker/infra is out of dev-pdf-extractor charter"; `owner` was already `ops`; and PO's charter excludes infrastructure. A rebuild row pointed at PO is the stranding pattern. Set `next_agent=ops`, left AC-6 (ops hands numbers back, PO closes) untouched.
**why-decision:** The head of this chain was never one of the four frozen rows — it is `UNBLOCK-PDFX-OPS-…`, already `READY` in `ready[]` with no dependencies, gated only by prose that expired at 17:11Z. Releasing the four rows without naming that is hygiene without motion.
**why-change:** Also recorded that `scripts/pdf-extractor-cgroup-sampler.sh` (PID 86980) is `while true` with **no stop condition** and re-resolves the container id every loop — it will silently follow a rebuild into a new container and append to the same CSV. Freeze boundary recorded as `ts <= 2026-08-26T17:25:12Z`; kill before rebuild. Did **not** kill it myself: it is a live measurement owned by the ops row, and unilaterally destroying it is not PO's call.

### STEP po-S5 · po · 2026-08-26T17:34:37Z
**what-done:** Applied one transform via `scripts/po-release-20260826T1734Z-pdfx-ac7-gate.jq | bash scripts/orch-apply.sh`. Dry-ran the prose-ceiling checker and `orch-validate.mjs` against the candidate **before** piping.
**what-considered:** Writing five rows blind and letting `orch-apply.sh` arbitrate — rejected; two of the five (`MEASURE-…` at 10,223B, `UNBLOCK-PDFX-OPS-…` at 10,424B) sit close to the 12,000B ceiling and a hard-reject would have cost a full re-derivation.
**why-decision:** Pre-flighting the two gates that can abort the pipe is cheap and read-only.
**why-change:** no change from plan. Result: `stamped 5 row(s)`, conservation clean (`task_total 891→891`), ceiling `0 net-new-growth violations`, `Stage 0 + Stage 1 PASS`. No lane moves, no `.head` write (INV-GATEWAY-1).
