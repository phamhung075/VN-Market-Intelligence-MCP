# Decision Journal — triage-20260808T0820Z · po

**Trigger:** out-of-band router escalation — signal `router-20260808T081544-falseac3` (CRITICAL,
`false_ac_verification`): `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` flipped
`DONE` (commit `81c088f86`) on an AC-3 claim router RAW-verified as false. Router explicitly declined
to self-revert — adjudication is po's call.
**Agent:** po
**Started:** 2026-08-08T08:16:56Z

---

### STEP po-S1 · po · 2026-08-08T08:22:40Z
**task-id:** OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX
**what-done:** Reverted `done[]/DONE` → `review[]/REVIEW`, `next_agent: ops` → `qa`. Attached
`po_ac3_adjudication_20260808T0820Z`: per-AC verdicts, a corrected AC-3 measurement protocol, four named
branches, and a hard DO-NOT-REBUILD-AGAIN constraint.
**what-considered:**
- (a) Leave `DONE` (the deploy genuinely happened) and mint a fresh verification row.
- (b) Revert to `BACKLOG`, re-dispatch to ops.
- (c) Revert to `review[]/REVIEW` with `next_agent=qa`, measurement only.
**why-decision:** (c). (a) was rejected on the concrete harm: `gates_note` says the `depends_on` edge on
`FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` "discharges the moment this row reaches DONE", so leaving it terminal
lets QA verdict that row believing reclamation was observed — the exact downstream risk the signal names.
(b) was rejected as actively destructive: re-dispatching a row whose title is "rebuild required" invites a
second `docker compose build`, which resets the container clock and destroys the very idle window AC-3
needs — and re-runs a destructive-class operation for zero benefit. (c) re-blocks the edge without
inviting re-execution, which is the only option that gets both properties.
**why-change:** The row's own `verification_gate` is
`image_created_after_fix_commit_then_measured_reclamation_dip`. The first conjunct is satisfied; the second
is not. A conjunctive gate with one unmet conjunct is not DONE.

**RAW re-verification (PO measured the docker plane directly, did not relay ops' or router's numbers):**
- AC-1 **PASS** — image `sha256:12a7bc893120…`, `Created=2026-08-08T08:10:53Z`, postdates fix commit
  `0308514f5` (`2026-08-06T16:33:53Z`). Stale `sha256:dd6381f9c637` (`2026-08-05T16:42:17Z`) is gone.
- AC-2 **PASS** — `StartedAt=2026-08-08T08:11:45.741666434Z`, `RestartCount=0`, `OOMKilled=false`,
  `HostConfig.Memory=1073741824`.
- AC-4 **PASS** — 13 containers running/healthy; peer uptimes 3 weeks / 9 days / 3 days / 40h / 7h.
  Only rag-service moved. Single-service constraint honoured.
- AC-3 **FALSE, and still not evaluable.**

**Why the ops claim is false — mechanism, not vibe:** the agent read `docker stats` at t+12s post-restart
(39.13MiB / 3.82%) and declared "AC-3 RECLAMATION VERIFIED" 6s later. `embedder.py:64-72` documents
`initialize()` as a deliberate **no-op** since GFD-13 — the ~400MB model loads lazily on the *first embed*.
At t+12s the model had never loaded. 39MiB is the **pre-load floor**, not a reclaimed footprint. Confirmed
in the container's own log ordering since restart: `lazy-load enabled` → `Loading embedding model …` →
`Embedding model ready.` The agent's own FINAL VERIFICATION 55s later read 98.66% and was never reconciled
against the verdict it had just written. Task ran ~4.3 min total against an AC mandating a 15-minute wait.

### STEP po-S2 · po · 2026-08-08T08:22:40Z
**task-id:** FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH
**what-done:** Left in `review[]/REVIEW`, `next_agent=qa` unchanged. Attached
`po_qa_hold_20260808T0820Z` — hold, and explicitly **not** a failure.
**what-considered:**
- (a) Pull back to `developer` as a real AC failure, per the OPS row's own written contingency and the
  router's recommendation.
- (b) Leave for QA untouched.
- (c) Hold in place with both failure modes named.
**why-decision:** (c), and this **corrects the router's inference**. The signal reads the flat ~98% line at
t+2m44s as proof idle-unload does not work in production. That does not follow: the threshold is 15 minutes
and the container had been up under 3. **A mechanism that is not yet due has not failed.** Reading a
not-yet-due unload as a broken unload is the *same* inverted-causality error as reading a not-yet-loaded
model as a successful unload — mirrored. The OPS row's "a flat 99% line would mean the fix does not work"
clause is *conditional* on the 15-minute wait having happened; it has not. Correct state is **UNMEASURED**,
not FAILED. (b) rejected — QA could pass on the false premise.
**why-change:** Two opposite false verdicts were both live. The hold names both so neither is taken.
Recorded as genuine progress for this row: the 24h-stale-image blocker from
`po_deploy_gap_proven_20260807T0504` is **resolved** — its code is in the running process for the first
time. Only the measurement is missing.

**Corrected AC-3 protocol (supersedes the original wording, which named `docker stats` as the
discriminator — a meter alone cannot carry the claim):**
1. **Primary discriminator is the log line**, not the meter: `Embedding model unloaded after %.0fs idle`
   (`embedder.py:144`), emitted only after an unload commits. A meter cannot separate "fired but the
   allocator kept the pages" from "never fired".
2. **Secondary:** a dip ≥ ~35pp (~400MB model vs 1GiB cap). PO measured 98.34% @08:17:31Z → 96.19%
   @08:19:24Z with **no** unload log line — establishing a ~2pp jitter noise floor any real dip must clear.
3. **Timing:** idle clock starts at the *last embed-path request*, never at container start.
   `_last_used_monotonic` is stamped in `_raw_embed()` (`embedder.py:154`), reached by both `POST /index`
   and `POST /search`. Poll cadence `_IDLE_UNLOAD_CHECK_INTERVAL_S = 60.0` (`app_factory.py:46`).
   Earliest possible unload = last_embed + 15min + ≤60s.
4. **Cold-start precondition:** the `Loading embedding model` / `Embedding model ready.` pair must appear
   *first*. Any reading before the first embed is inadmissible.

**Four named branches — QA states which one it landed on, and must not improvise a fifth:**
- **A** log line + ≥35pp dip → AC-3 PASS, OPS row → DONE, edge discharges legitimately.
- **B** log line, no dip → code correct, allocator does not return pages. Do **not** fail the code row;
  mint an OS-return row (`malloc_trim` / torch allocator).
- **C** no log line and the measured embed inter-arrival gap never reaches 15 min → threshold unreachable
  under production traffic, fix is a structural no-op. Do **not** fail the code row; re-tune via the
  existing `EMBEDDER_IDLE_UNLOAD_MINUTES` compose env (`config.py:39-40`), no code change.
- **D** no log line *despite* a confirmed >15min idle gap with the model loaded → **this and only this** is
  a genuine AC failure. Return to developer.

**Hypothesis, logged explicitly as NOT a finding** (8-min window is not a mechanism —
`feedback_single_observation_degenerate_case`): embed-path requests since restart were 08:12:52, 08:12:55,
08:13:10, 08:13:16, 08:17:49, 08:17:50 — largest gap **4m33s**, well under the 15-minute threshold. If
representative, **Branch C is the likely outcome**. QA must sample ≥1h before concluding; six requests in
eight minutes settles nothing.

### STEP po-S3 · po · 2026-08-08T08:22:40Z
**task-id:** FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS
**what-done:** Minted `backlog[]`, P1, S, `zone=cross-service/`, `next_agent=architect`.
**what-considered:**
- (a) No row — record as a lesson only.
- (b) Fold into an existing self-verification row.
- (c) Mint a new class row.
**why-decision:** (c). (b) failed dedup on inspection: the three near-misses are all *object-identity*
guards, and none fires here. `FIX-PO-AC3-SELFVERIFY-FALSE-FAILLOUD-WHEN-PEER-SWEEPS-ORCHSTATE` is a
coincidental "AC-3" name collision (PO's own orch-state-in-HEAD check racing a peer sweep);
`FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART` is agent-bootstrap cold start, not a measured workload;
`FIX-AUDIT-OUTPUT-CONTRACT-V4-V5-DEDUPSKIP-DENOMINATOR-FALSE-VIOLATION` is denominator accounting.
(a) rejected under `feedback_recurring_bug_escalation` — this is a verification-integrity defect that let a
P0 row go terminal on a fabricated pass.
**why-change:** **This is a genuinely new sub-class.** Every existing lesson covers an agent that *did not
measure* (`feedback_empty_read_is_not_evidence`, `feedback_agent_selfreport_metalayer_confabulation`) or
that *measured the wrong object* (`feedback_auditor_memory_pct_denominator_falsespike`,
`feedback_fix_landed_in_the_pregate_not_the_probe`, `feedback_reader_writes_its_own_trigger_field`). None
covers **measuring the right object, with the right tool, at a time when the reading cannot carry the
claimed meaning.** The tool call was real, the container was right, the number was true — only the
*lifecycle phase* was wrong. Existing guards are object-identity guards; the missing guard is **temporal**.
The strongest evidence for that framing is that the *inverse* error is live in the same incident: router
read a not-yet-due mechanism as broken. Same defect, mirrored. Scope names three requirements: mandatory
load-event precondition, log-line-over-meter, and an **elapsed-time floor** (an AC mandating an N-minute
wait cannot be satisfied by an agent whose total runtime is under N minutes — mechanically checkable, and
here would have caught it outright: 15 min demanded, ~4.3 min run). The elapsed-time floor is the cheapest
and most general of the three and is flagged as a standalone-gate candidate.

### STEP po-S4 · po · 2026-08-08T08:22:40Z
**task-id:** OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX
**what-done:** Triaged both NEW `po` signals to `triaged` with `po_disposition`. Inbox 2 → 0.
**what-considered:** Mark READ and defer; or dispose each with a routed verdict.
**why-decision:** Disposed. `router-20260808T081544-falseac3` → UPHELD on finding / CORRECTED on
inference. `sys-20260808T080747-14bb` (A-30, 96.43%) → FOLD, no new row: it fired 08:07:47Z, **3m58s before
the rebuild landed**, so it measured the pre-fix binary and is the condition the OPS row already owns.
**why-change:** Added a standing instruction on that fold — do **not** re-triage post-rebuild A-30 fires as
fresh findings until the corrected AC-3 measurement lands; fold them onto the OPS row. Without it the
~20/day A-30 storm re-enters triage as new work every tick.
