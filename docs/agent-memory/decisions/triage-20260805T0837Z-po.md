# PO Triage — dev-team cron tick 2026-08-05T08:37Z

Ran at 2026-08-05T09:06-09:11Z. Inputs: 3 pendingSignals, 113 unresolved Telegram reports (dev-team's relay said 19 — see Correction 1), live `.task_board`, live named-volume DB.

## Correction 1 — the relay undercounted the inbox by 6x

dev-team's context said "19 new". `list_unresolved_reports()` returns **113 rows, all `status=new`**. Class breakdown: `[bctcExtractReconcile]` 45, `[BCTC]` 43, `[sla-monitor]` 6, `[BCTC-1345b]` 4, `[audit-output-contract]` 4, `[dev-team]` 2, `[system-auditor]` 2, `[router]` 2, and 5 singletons. Triaged the full 113, not the relayed 19. Two of the four mints below (`get_bctc_full` day-4 outage, audit-output-contract) came from reports that were not in the relay at all.

## Decision 1 — SUPERVISED-HOLD released (was blocking `.head`)

Not in the relay, found by the mandatory pre-check. `should_hold=true` on `UC-CRITIC-HOOKS-ENFORCEMENT` (`.head.active_task_id`, in_progress, promoted 06:45:27Z by the supervised-lane sweep, next_agent=ba). dev-team could not resume it.

**What-considered:** stamp / don't stamp / stamp-on-relay. Ratified independently per `supervised-goahead.md` §2 — never on a relayed verdict.
**Verification performed:** (a) deliverable exists — `docs/architecture-briefs/2026-07-12-ultracode-workflow-improvement-audit.md`, 382391 B, anchor `## Completeness critic — lanes not audited` at L1973. (b) Load-bearing claim re-derived **from live config, not brief prose** — `.claude/settings.local.json` has exactly 6 hook commands ending `2>/dev/null || true`, plus 1 more `|| true` in `.claude/settings.json` = 7 of 8 hook invocations swallow failure. Claim confirmed, count exact, still true 24 days on.
**Why-change from plan:** found one inaccuracy in the brief and carried the correction into the stamp rather than releasing silently. The brief names the *orch-state prewrite Zod gate* among the swallowing hooks; live config shows `orch-state-hook-prewrite.mjs` is the **one fail-closed hook** (no `|| true`). The swallowing orch-state hook is `orch-state-hook-bash-backstop.sh`. BA must scope to the bash-backstop and must not "fix" the already-correct prewrite gate.
**Outcome:** `po_goahead_20260805T090611` stamped on the row (not `.head` — row-level survives `.head` reuse). Re-ran dev-team's own predicate: `should_hold=false`.

## Decision 2 — the BCTC "extraction dormant" premise is REFUTED; real root cause found

**What-considered:** (a) fold the 45 reconcile reports into the existing P0 dormancy SPIKE, (b) re-open `FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION` as a failed fix, (c) probe first. Chose (c). (a) and (b) would both have been wrong.

**Decisive test** — readonly probe of the live named-volume DB (`docker exec vn-market-intelligence-mcp-mcp-server-1 bun`, `/app/data/market.db`):

| fact | value |
|---|---|
| `bctc_layout_units` | n=5134, MAX(extracted_at)=**2026-08-04 22:12:12** |
| per-day layout writes | 07-28:159, 08-01:1430, 08-02:430, **08-03:1865**, 08-04:463 |
| `bctc_vps_queue` status=done | **171**, spanning 2023-Q4 → 2026-Q2 |

The pipeline wrote 1865 layout units on 08-03 — the same day four reports claimed "0 rows". `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` (P0) asserts extraction is dormant since mid-June; that was true on 2026-07-16 and is **false now**. `FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION` (closed 07-10) shipped and works. This is a stale-premise P0, not a failed fix.

**Actual root cause, structural not statistical:** `financial_reports` holds 257 rows in two disjoint families with zero cross-over — 191 with a real UUID id and `data_env=NULL`, and **66 with id `LIKE 'fallback-%'` and `data_env='production'`** (crosstab 191/191 and 66/66, a perfect partition). Rows in `bctc_layout_units` with a `fallback-%` report_id: **0**. In `bctc_table_rows`: **0**. Meanwhile UUID-keyed reports hold all 5134 layout units. 63 of the 66 shells have `pdf_path` SET — real PDFs stranded on disk.

So for the fallback cohort, "0 rows after 8 reconciliation passes" is a **tautology, not a finding** — the extraction path keys on the UUID id and can never populate a `fallback-` id. The reconcile job then terminalises the row and fires a fail-loud Telegram BUG, forever. Minted `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` (P0).

**Second, separate live defect found and re-scoped onto the dormancy SPIKE:** `bctc_layout_units` last wrote 2026-08-04 22:12:12 but `bctc_table_rows` last wrote **2026-08-01 16:39:07**. Per-day table_rows: 07-29:451, 07-30:391, 07-31:109, 08-01:652, then nothing. Two consecutive stages of one pipeline, one alive and one silent for 4 days. Untracked by anything. The dormancy SPIKE was dropped P0→P1 and re-scoped to exactly this question.

## Decision 3 — answered the stranded review row's own standing question

`OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE` asked for a per-report_id re-probe of the 12-ticker cohort and whether PEK progressed since 2026-07-10T09:52Z. Ran it: **11/12 still exactly layout=0 AND table_rows=0; GAS still has no `financial_reports` row at all. No progress in 26 days.** 6 of the 11 (ACB/BID/EIB/HCM/HSG/POW) carry `fallback-` ids and are explained by Decision 2. The other 5 (GVR/MBB/D2D/NKG/SSI) carry real UUIDs and are still 0 — so the fallback mechanism is **necessary but not sufficient**; recorded on the row so nobody closes it on the fallback fix alone.

Also unstranded it: `next_agent` was literally `"dev-team"`, which dev-team must refuse (anti-recursion). Repointed to `architect` — the operational question is now answered, what remains is design.

## Decision 4 — period-mismatch batch folded, priority raised, and the row's own argument corrected

43 `[BCTC] Period mismatch` reports → folded onto `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`, occurrence 19→63, **P1→P0**. No new mint.

Two facts that were invisible from the 08-01 batch:
1. **Daily self-renewing loop.** The 08-03 and 08-04 batches are byte-identical 24h apart — same tickers, same supplied→detected pairs, and the same match/runner-up counts, which fingerprint the document text (4317≡4381 FRT 46vs10; 4318≡4382 KDC 51vs6; 4320≡4384 VND 115vs26; 4331≡4395 PDR 127vs41). ~21 BUG reports/day, permanently. 44 of the 113 unresolved reports are this one loop — it is now the largest generator of bug-channel noise and it drowns novel reports. That is the P0 justification.
2. **Counter-example to the row's own directional argument.** The row argues content-period is *always* strictly later than supplied (19/19, cited ~2^-19) and derives the always-latest/`docs[0]` model from it. Report 4407 (DPM, supplied 2025-Q4 → content 2025-Q1) is **earlier**. An always-resolves-to-latest actuator cannot produce that. The `listSscDocuments()`-has-no-quarter-parameter and `docs[0]` findings still stand as the fix target, but a fix designed to *reverse a directional bias* rather than *select explicitly by quarter* would leave DPM broken. Flagged for architect before design.

The detector (`FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT`) is correct and must not be weakened — this is the actuator behind it.

## Decision 5 — day-4 user-facing serving outage, reproduced live, previously untracked

Report 4408 (bctc-analyst self-escalation). Not taken on the agent's word — **reproduced through the gateway this tick**: `get_bctc_full(code="FPT")` → `"Chưa có dữ liệu BCTC"`. No board row existed for it.

Data is intact; this is purely a serving defect. FPT/HPG/VCB all have newest period 2026-Q2 with `total_assets=NULL`, `refine_status=PENDING`, `validation_status=pending_extraction`, while 2026-Q1 and 2025-Q4 are COMPLETE and validation-passed (e.g. VCB 2026-Q1 `total_assets=2550963342`, `passed`). The serving path selects the newest period and reports absence when it is empty, instead of falling back to the newest row with data. Blast radius: every ticker acquiring a new-quarter shell before extraction completes — i.e. the whole watchlist at the start of every quarter. Passive health cannot see it (`feedback_passive_health_masks_dead_data`).

Minted `FIX-BCTC-FULL-SERVING-EMPTY-NEWEST-PERIOD-HEAD-OF-LINE` (P0). Noted the sibling `FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW` (review/qa) — same bug class, different tool; architect should check for a shared row-selection helper and fix once. Also recorded the `code` vs `ticker` param trap so the next reproducer does not mistake a Zod error for the outage.

## Decision 6 — sla-monitor CRITICALs are cadence-mismatch FPs; folded, not minted

6 reports → folded onto `SPIKE-FRESHNESS-REMEDIATE-TRIAGE` (backlog/architect), which exists precisely to design this classification away.

- **foreign_flow** stale 3899/1020/1020 min vs a 10-min threshold. Verified against the documented harness, not the alert text: `bash scripts/check-foreign-flow-freshness.sh` → `verdict=PASS latest_date=2026-08-05 lcts=2026-08-04`, rc=0. Data is current. It is a once-per-session EOD dataset under a flat 10-min SLA — guaranteed to breach every night. The 1020min figure repeating exactly on two consecutive days is a publication-boundary signature, not drift.
- **price** stale 44/59/59 min — all three fired at exactly 02:00Z = 09:00 ICT, *before* the 09:15 ICT open. Last tick ~08:00-08:16 ICT is correct behaviour. Market-hours-blind (`feedback_auditor_freshness_threshold_market_hours_blind`).

**Why-change:** did not mint. This would be the 5th per-source patch in a class that already has `FIX-SLA-SBV-FX-BUSINESS-DAY-AWARE` (CANCELLED), `FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT`, `FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H`. Gave the SPIKE a concrete acceptance input instead.

## Decision 7 — resolver gap minted at the root, not patched per row

`FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE` (P1). Read the script rather than inferring: `scripts/devteam-review-claim-secondary-drain.jq:114` selects on `effective_next_agent(...) != "qa"`, and `effective_next_agent` has exactly one defaulting case (null→`"po"`); every non-null value passes through verbatim, including `"dev-team"`. **The script's own header (L20-21) already enumerates `dev-team=5`** — the author counted five such rows and shipped no case for them. So four more will strand identically. Repointing the one row by hand (Decision 3) is a symptom patch; the resolver is the fix.

Kept the anti-recursion refusal as correct behaviour — `feedback_devteam_flow_needs_nested_agent_spawn_subagent_cannot`, re-confirmed live by report 4411 this tick. Do not relax it as the fix.

## Decision 8 — three signals disposed inline, one routing gap closed permanently

- **`commit-sweep-guard` bug-escalation** — payload begins `[notebook-immutability-guard]`, which is a **4th payload class with no rule** in my own `triage-signals.md` (the table covers `[sweep-guard] BARE` / `INTERNAL:` / `SAME-FILE DIVERGENCE`, all of which parse `escalated=`; this class carries no such key). Checked what could still be checked: the named `## c18` section is byte-identical HEAD↔worktree, the file is now clean, and system-auditor has committed 4 times today (c20/c21/c22). Honest disposition: the index state at 07:42:36Z is unreconstructable post-commit, so this is **inconclusive, not "benign"** — and mode=warn never blocks, so risk is low either way. **Root-cause action:** added the missing routing row to `docs/agents/po/flow/triage-signals.md` so the next fire routes deterministically — including the rule that a clean `git status` read after the fact is outcome evidence and cannot refute an index-vs-HEAD finding, and a ≥2-cycle same-agent+section threshold before minting (auto-prune rewrites make interleaved-cycle FPs the high base rate).
- **`cowork-team` cowork-fire** — informational drain confirmation. Logged, no action.
- **`notebook-auto-prune-hook`** — addressed to claude-manager-helper, payload says `action_required: informational_no_action_needed`. Logged, no action.

## Disposed without mint (dedup / known)

`[BCTC-1345b]` ×4 + `[BCTC] write BLOCKED` ×3 → covered by `FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY-THAT-CANNOT-PRODUCE-ITS-OWN-VALUE` and `FIX-BCTC-VALIDATION-GATE-NONBANK-ZERO-SCALE`; the write-BLOCKED ones are the guard correctly preserving good data. `[BCTC-ZERO-URL-ALERT]` 4310 → self-resolved; `bctc_vps_queue.url_not_found` last_attempt is 2026-08-02T20:30:09 (the alert instant) and `done` rows continued through 08-04T22:35, so discovery recovered; standing case covered by `BCTC-ENRICHER-OLD-QUARTERS`. `[system-auditor]` OHLCV CRITICAL 4414 → already disposed as duplicate by report 4417 (PO's own correction, commit 55ddb1ef8). `[system-auditor]` rag-service A-30 memory 97.83% 4419 → known FP class, single fire, pendingObservations only. `[router]` 4416/4418 → superseded by 4417.

## Build Standard

Not applicable — no executable code. All `.task_board` writes via `jq | bash scripts/orch-apply.sh`; Stage 0 + Stage 1 PASS, conservation OK on every apply. All 4 mints and all 4 modifications RAW-verified present in the live file after write (`feedback_po_notebook_mint_never_reaches_orchstate_board`).
