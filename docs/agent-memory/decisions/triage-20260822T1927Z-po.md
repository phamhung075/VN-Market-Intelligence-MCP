# Decision Journal — po triage 2026-08-22T19:27Z

task_id: task:po-triage-20260822 (dev-team idle-tick rotation `step1_triage`, 2026-08-22T19:07Z)

Inputs: 64-envelope durable inbox (`.dev_team_idle_chain.pending_triage_inbox`, accumulating since
2026-08-15) + 10 hot `to=po` rows in `.signal_queue.rows[]` + 40 unresolved telegram reports +
575 open `task_board` rows. Output: **2 mints, 1 evidence fold, 64-envelope CLEAR, 0 re-dispatch.**

## Entry 1 — 112 of 114 inputs were already covered, and proving that was the work

- **what-considered:** (a) treat a 64-envelope backlog as a backlog *of work* and mint proportionally;
  (b) **CHOSEN** — dedup every envelope against the non-terminal lanes first and mint only what survives.
- **why-change:** the board already carries **575 open rows**. The failure mode here is not under-minting,
  it is minting a 576th row that duplicates one of the 575 and thereby buries the two that are real.
  Dedup scan used **lane membership** (`backlog[]+ready[]+in_progress[]+review[]+qa[]`) as the
  open/closed discriminant, never a status-token enum — per `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY`,
  which measured a status-enum scan missing 62.4% of open rows.
- **result:** 62/64 envelopes folded onto existing rows or closed outright. 2 survived.

## Entry 2 — The four `ci_red` envelopes: the dispatcher was right, and I checked anyway

- The dispatcher told me the size-lint blocker was fixed and to clear without re-investigating.
  **I ran the mandatory pre-dedup `gh run view --log-failed` on all four run_ids anyway**
  (31886091545 / 32585964001 / 32588886623 / 32591495299).
- **why-change:** `triage-signals.md`'s `ci_red` row makes that read unconditional — *"dedup MUST NOT be
  evaluated before this step completes"* — and its BACKSTOP forbids a "pre-existing" disposition that
  cannot name a matched row. A dispatcher assertion is not a matched row. Trusting it without the read
  would have produced a disposition indistinguishable from a fabricated one.
- **confirmed:** all four emit the identical single offending file
  `apps/mcp-server/src/interface/mcp/tools/financial-reports/getBctcPendingRefineTool.ts`, job `size-lint`
  → one `dedup_key`, one defect. Matched row `FIX-CI-SIZELINT-GETBCTCPENDINGREFINETOOL-BASELINE-TOLERANCE-EXCEEDED`
  is `DONE_VERIFIED`, and its `verification_gate: ci_green_on_subsequent_push` is **satisfied** — the run at
  `73e43bb3f` is `completed/success` and all four head_shas precede the fix commit `5944172e0`. Closed, no mint.

## Entry 3 — Why the inbox reached 64 in the first place

- **This is the finding the envelope count was actually reporting.** My own notebook from the 19:00Z cycle,
  90 minutes ago, records: *"did not clear `pending_triage_inbox[]` — that subtractive CLEAR belongs to
  dev-team Step 1, never PO (`triage-signals.md:7`)."* That reading is correct as written.
- `docs/agents/po/flow/triage-signals.md:7` assigns the CLEAR to dev-team's Step 1; **this tick's dispatcher
  assigned it to PO**. Both cannot be right, and while they disagree the envelopes are triaged repeatedly
  and cleared never — which is exactly the shape of a 64-entry inbox where the oldest `ci_red` (08-15) had
  *already* been correctly dispositioned by a prior PO cycle and came back anyway.
- **what-considered:** (a) follow the flow doc, decline the clear, report the conflict;
  (b) **CHOSEN** — perform the clear as instructed *and* record the doc conflict for repair.
- **why-change:** (a) is the option that has already been taken at least twice and is what produced the
  backlog. The clear is subtractive but fully guarded — `ORCH_APPLY_DECLARED_INBOX_TRIAGED` names all 64
  ids, `orch-conservation-check.mjs` verified `inbox_row_identity=clean`, an undeclared drop is hard-rejected.
  The write is safe to perform and the doc is not safe to leave contradictory. Landed 64→0 in ONE write.
- **not done:** did not edit `triage-signals.md` to match. PO does not own flow-doc repair, and picking a
  winner by fiat while the dispatcher and the doc disagree would just move the contradiction.

## Entry 4 — The two mints, and refusing to merge them

- **`FIX-CRON-NONRECOVERY-POST-HOST-SUSPENSION-TIER3-MORNINGBRIEFING-BACKTESTRUNS`** (P1, `cross-service/`,
  → dev-team). Three `auditor_cycle_missing` envelopes claimed tier-1/2/3 all missing. **Read the marker
  files rather than the signal text:** tier-1 `2026-08-22T19:07:09Z` FRESH, tier-2 `2026-08-22T18:32:21Z`
  FRESH (corroborated independently by commit `768ba0c7c`), tier-3 **`2026-08-14T02:35:24Z`, 8.6d stale
  against a 24h cadence**. Two of the three signals were self-resolving noise; the third was real.
  The tiers are three separate cron registrations (`cron-system-auditor.md`: `*/30`, `0 */4`, `0 4 * * *`),
  so *two recovered and one did not* is the whole finding — `feedback_cron_armed_but_wrong_prompt_variant`.
  Same 8-9d freeze on `morningBriefingJob` (209.5h) and the `backtest_runs` writer (6.9d).
- **★ anti-fold guard written into the row:** `ARCH-WATCHDOG-WEEKDAY-AWARE-THRESHOLD` (BACKLOG) proposes a
  weekend-aware threshold for exactly this job, and today is a Saturday — the fold is *tempting and wrong*.
  **209.5h spans six weekdays.** A weekend-aware threshold would suppress the alert without restoring the
  job. Row carries the refutation so a future triager cannot close it that way.
- **`FIX-SLA-BOND-MATURITY-STALE-13D-ZERO-BOARD-COVERAGE`** (P2, `apps/mcp-server/`, → ba). Kept **separate**
  from the row above despite both being staleness: 13.6d back-dates to ~2026-08-09, which **predates** the
  08-14 suspension boundary, so the suspension cannot be its cause. Merging would have let the suspension
  fix land and silently leave this one breaching. `backtest_runs` (6.9d, inside the window) went the other
  way and was folded *into* the suspension row rather than minted — same test, opposite answer.
- Both rows carry an AC of the form *"either the feed refreshes, or the SLA entry is removed with a written
  rationale"*. A permanently-breaching threshold is not monitoring; it trains the fleet to ignore the monitor.

## Entry 5 — The SAME-FILE DIVERGENCE fire: benign by evidence, not by assumption

- The `bug-escalation` triage row says: if the known-FP root cause `FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE`
  is still open → dedup-bump; if it has **shipped** → the fire is a *candidate genuine divergence* and must
  be verified. I found it `DONE_VERIFIED` in `archive/2026-08.json`, so the cheap disposition was unavailable.
- Ran the mandated two-blob form — `git diff 3e4a572b6 193c4c504`, **no pathspec** (appending one is a git
  usage error that returns 0 lines and exit 129, per `FIX-SWEEPGUARD-SAMEFILE-INSPECT-CMD-GIT-USAGE-ERROR-MANUFACTURES-FALSE-BENIGN`
  — the exact trap of manufacturing a false "benign" from a broken command).
- **Diff content:** 10 insertions / 4 deletions, entirely `ba_spec_complete` / `ba_handoff` /
  `ba_completed_at` stamps and `next_agent: ba → agents-architect` on the two ghost-zone P0 rows. BA's own
  work, corroborated by commits `cbb6a1987` + `947495fdf`. No unreviewed peer content. **Benign — and
  benign because the diff was read, not because the detector has a bad reputation.**

## Entry 6 — Not minting for the loudest signals

- **26 of 64 envelopes (41%) are notebook-prune breaches** on three files. All three already have owning
  rows (`CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` READY, `CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION`,
  `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE` READY). **Volume is not evidence of
  a missing row; here it is evidence of a row that exists and is not being dispatched.**
- **I tried to fold the growth measurement inline and `orch-row-prose-ceiling-check` blocked it** — that row
  is already at 12585B of inline prose. **The guard was right and I did not route around it.** The
  measurement is real and belongs in the handoff report instead: digest-predict.md was 38L/16279B when its
  row was written and is **45L/35214B today** — 2.16x, and 2.93x its 12000B cap. It is the loudest emitter
  in the inbox (10 envelopes + 1 `context_bloat_breach`) and the hook can *never* succeed on it
  (`section_count=1`, no drop candidate), so every future cycle adds another envelope. It needs dispatch.
- **`esc-deep-dive-request` FPT ESC-5**: not minted. `REFLOW-FPT-Q1-2026` (BACKLOG) carries the *same*
  `report_id` `e8ea3df5-3f32-413d-a3eb-c71634c0438d` and the signal's `unit-0008..unit-0014` is that row's
  "units 8-14" verbatim. Exact match.
- **`bctcExtractReconcile` circuit breaker** (telegram 4956, 6 rows → `enrich_failed` today): folded, not
  minted — `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` (REVIEW) owns the class. Noted for the
  owner that the vintages are **new** (2024-Q1/Q2, 2023-Q4; prior cohort was 2025-Q4/Q1-2026).
  `feedback_bctc_reconcile_exhausted_benign_during_market_hours_503_guard` does **not** apply — the
  cowork-fire payload records `calendar_status: weekend`, so there are no market hours to be inside of.

## Entry 7 — The routing gap, measured rather than asserted

- **42/64 (66%) of Pipeline-A envelopes hit the `any unknown type → log and skip` catch-all.** Independently,
  Pipeline B is worse: I evaluated `triage-signals.md`'s own `guard_signal_type_coverage` function against
  live state and it **FAILS** — the five live `to=po` types (`auditor_cycle_loss`, `auditor_cycle_missing`,
  `cron_fire_gap`, `db_freshness`, `narrative_contradiction`) appear in its `$routed` allowlist **zero**
  times. 10/10 = 100% unrouted. The guard was recorded PASS on 2026-08-01; the namespace fully rotated under it.
- **★ the guard is a shell function embedded in a flow doc, wired into no CI job and no cron — nothing has
  ever executed it.** A regression verifier that no scheduler invokes does not verify anything; it records an
  intention. That is why three weeks of drift went unobserved, and it is the highest-leverage sub-fix here.
- Folded onto `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE` (READY) as `po_evidence_20260822T192715`,
  using that row's existing dated-evidence-field convention rather than growing its prose blob.
  **Did not mint** — the row exists, is READY, and originally measured 64%; today's 66%/100% corroborates it.
