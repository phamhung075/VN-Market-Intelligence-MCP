# PO Decision Journal — triage 2026-08-23T18:10:02Z

**Tick:** dev-team 2026-08-23T17:37Z, rotation lane `step1_triage`. Inbox 44 envelopes / 13 types.

## D1 — task_id: FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE — split AC-8, do not certify durability

- **what-considered:** (a) accept AC-8 on QA's 1h51m evidence and close; (b) hold the row in review for a genuine ≥24h redispatch; (c) split AC-8 into the defect claim (AC-8a) and the durability/capacity claim (AC-8b) and move AC-8b to its own row with a stamped window.
- **why-change:** chose (c). (a) certifies a ≥24h durability claim on a window that has not closed — the exact shape of `feedback_ac3_durability_certified_on_window_that_ended_before_metric_settled`. (b) parks a P0 whose code is finished and non-regressed for 24h of wall clock, and invites a 3rd/4th QA redispatch that can only re-hold. (c) preserves the unproven claim intact rather than waiving it, and is consistent with this row's **own pre-existing PO-ratified `out_of_scope(a)`**, which predicted the pacing follow-up before any of it was measured — so it is a split, not a post-hoc goalpost move.
- **evidence relied on:** QA redispatch3 direct-commit verify (`3db7a8dc8` ancestor-OK, 1060 pass / 0 fail, image `sha256:5d7d36db65f4`, git_sha `0c9380859`); live DB deltas `bctc_layout_units` 5905→6142, `bctc_table_rows` 4940→5180.
- **explicitly NOT claimed:** that contention has fallen to ~zero. It has not — 15 raises / 34 triggered (44%) at measurement. That claim now lives, unproven, on `FOLLOWUP-PEK-EXTRACT-CONTENTION-PACING-CAPACITY-24H-WINDOW` with window start 2026-08-23T15:44Z and earliest honest re-measure 2026-08-24T15:44Z.
- **actuation:** ruling written to the row's `verify_note` (structural field — excluded from the prose ceiling, which the row is within 235B of); `next_agent=qa`; `redispatch_count` deliberately left at 2.

## D2 — task_id: FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS — partial inbox CLEAR

- **what-considered:** (a) clear all 44 envelopes as the flow doc's CLEAR block reads; (b) clear 41 and hold back the 3 whose types have no Pipeline-A routing row.
- **why-change:** chose (b). `guard-signal-type-coverage.sh` derives its live-type set from the same `pending_triage_inbox[]` array the CLEAR empties, so (a) would have turned the `signal-type-coverage-guard` CI job green **because its input was deleted**, with the routing table untouched — a green indistinguishable from a real fix. The flow doc reads as if everything should be cleared; it is wrong on this point and `FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE` already documents the same queue-shaped-measurement defect independently.
- **verification method:** hand-replayed the guard's two read-only extractors rather than executing it — the script writes the live board via `ORCH_APPLY_LIVE_FILE_OVERRIDE` and its `--check` flag is a no-op alias, not a dry-run. The failing CI log for run 32655888564 corroborates both the type set and the write path (two auto-mints attempted, aborted only on `bun: command not found`).
- **residual risk, disclosed:** 3 envelopes remain parked in the durable inbox indefinitely until agent-father lands the 2 table rows. Any peer that runs a blind `= []` CLEAR will silently manufacture the false green this decision exists to prevent.

## D3 — mint-vs-fold discipline

- **what-considered:** minting rows for the 20 notebook/ctxbloat envelopes, the DRS null-lane finding, the SLS stale-stamp claim, the pek `fallback-*` UUID rejection, and the 4 Telegram-report incidents.
- **why-change:** folded all of them onto existing open rows; zero duplicates minted. Two `recurring-bug` payloads carried their own `action_required`/`row_to_amend` and were honoured verbatim. The only three mints are subjects with **no** existing open row, each dedup-checked across all five non-terminal lanes and each recording which near-neighbours it deliberately did not absorb.
- **blocked once, honestly:** the intended fold target `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE` is grandfathered at 24298B; `orch-apply.sh` hard-rejected the growth. Redirected the fold to a smaller sibling in the same family rather than trimming historical prose or splitting the write to dodge the check.
