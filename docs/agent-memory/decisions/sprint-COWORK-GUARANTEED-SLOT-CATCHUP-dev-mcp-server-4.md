# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · dev-mcp-server (cont. 4)

**Sprint goal:** cowork guaranteed-slot catchup (ambient sprint label; this file's entries are per-task, not sprint-scoped work)
**Agent:** dev-mcp-server
**Started:** 2026-08-06T10:30:00Z (continuation of -3.md, CAP-REACHED 2026-08-06T10:05:29Z)

---

### STEP dev-mcp-server-S69 · dev-mcp-server · 2026-08-06T10:30:00Z
**task-id:** CI-PERFILE-STRUCTURAL-MITIGATION
**what-done:** Bound `scripts/ci-per-file-isolation.sh`'s parallelism default to auto-detected vCPU count (`nproc`/`sysctl` fallback) instead of hardcoded `P=16`; `.github/workflows/ci.yml` invocation dropped the literal `16` arg. Aggregation loop now checks the already-captured per-file `rc` (rc-fail-loud, AC-4) — a killed/crashed process with 0 parsed pass/skip/fail previously reported false-GREEN.
**what-considered:**
- raise the per-test timeout instead (AC-3's other named option) — rejected as primary fix: bunfig.toml already sets a generous 30000ms global default; the reproduced mechanism was scheduler-delay under oversubscription pushing real subprocess/network wall-clock budgets past their margin, not a too-tight timeout constant, so widening timeouts further would mask rather than remove the amplifier and slow the whole CI job.
- hardcode a smaller literal (e.g. 4) matching SPIKE's "2-4 vCPU" guess — rejected: brittle if GH changes runner specs again; `nproc` self-adjusts and is the "structural" (not one-off-tuned) fix the row's title asks for.
**why-decision:** Reproduced live on this dev machine: 2 full-suite runs on the SAME unchanged tree produced DIFFERENT failing-file sets (14 files @ P=16 vs 9 @ auto-P=12) — rotating identity is AC-1's own discriminator for runner/environment nondeterminism, not code-content causation. Captured a concrete mechanism instance (102-job-news-poll.test.ts: `rag_analyses` COUNT assertion expected 2 got 0, an unawaited-background-write race) matching AC-2's contention hypothesis exactly.
**why-change:** AC-5's literal "20 consecutive 0-fail local runs" not fully met — this dev host has other concurrent Claude Code agent sessions (detect-loop/standalone-team/cowork-team, confirmed via `ps`) consuming real CPU throughout measurement, so local convergence to 0 is not achievable regardless of P; deferred final AC-5 verification to `gh run rerun` on the real isolated GH-hosted runner post-push. AC-6 (re-evaluate 4 DEFLAKE-*/transient rows) deferred to whoever confirms AC-5 green.
**verification:** rc-fail-loud isolated-verified with a SIGKILL repro (self-killing scratch test file, not committed): old aggregation reported `TOTAL_FAIL=0`/exit 0 (false-GREEN) on a `rc=137` process; new aggregation reports `TOTAL_FAIL=1`/exit 1 (correct). `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: tools=183/cron=88 unchanged (no `src/` touched — code_under_test is repo-root `scripts/`+`.github/workflows/` only, matching the board row's own `code_under_test` field). Commit `edea85e66`.

### STEP dev-mcp-server-S70 · dev-mcp-server · 2026-08-06T10:53:00Z
**task-id:** CI-PERFILE-STRUCTURAL-MITIGATION
**what-done:** Pushed the fix (`edf05e701`), then used `gh run rerun <run-id> --job <databaseId>` to rerun the "bun test" job 5 CONSECUTIVE times on the real GH-hosted isolated runner (pinned SHA, run 31093800492).
**what-considered:** stop at 1 push-triggered run vs spend more CI minutes reproducing AC-5's "20 consecutive" bar on the authoritative infra — chose 5 as a CI-minutes-responsible sample large enough to be convergent evidence against an ~83% pre-fix red rate.
**why-decision:** the real isolated GH runner is the authoritative environment this row's AC targets, not my noisy shared local dev host (other Claude Code sessions confirmed consuming CPU during local measurement in S69) — real-infra data supersedes the local caveat.
**why-change:** none — this is the deferred AC-5 verification S69 explicitly promised.
**verification:** 5/5 runs: 15024 pass / 40 skip / 0 fail, zero variance (job ids 92590820753/92591696010/92592467574/92593218004/92593983661). The immediately-prior push (ba821dbdd) had failed on `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART.test.ts` — a different rotating file, corroborating not contradicting this row. Board row updated with `dev_followup_ci_verification` field, commit `4b15123b5`, pushed.

### STEP dev-mcp-server-S71 · dev-mcp-server · 2026-08-06T16:56:00Z
**task-id:** FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM
**what-done:** Added optional `fields:'ids'|'full'` (default `'full'`) to `get_bctc_refined`. `'ids'` SELECTs only `unit_id, window_status` (no `markdown` read from DB at all) and always returns `{report_id, total_units, units:[{unit_id, window_status}]}` — including empty `units:[]` on zero pushed units, never the `'full'` path's `{error}` shape. Updated `refine_bctc_md/flow/main.md` Phase 0 step 5 to call `fields:"ids"` and documented the shape it actually returns (was `{units:[{unit_id}]}`, never produced).
**what-considered:**
- change only the response-shaping layer, keep the SQL SELECT unbounded — rejected: task title explicitly names the SELECT itself ("SELECTs full markdown for every prior unit") as the defect; a response-shaping-only fix would still pay the DB read cost every resume fire.
- return `{error}` on zero rows for `fields:'ids'` (mirror `'full'`) — rejected: refine_bctc_md's very first resume fire on a fresh report legitimately has zero pushed units; that's a valid skip-set state, not an error, and forcing callers to special-case an error shape just to get an empty set defeats the purpose of a clean projection contract.
**why-decision:** AC-1 names the exact return shape; AC-2 requires the flow doc to match reality — the empty-set design closes a second, previously undocumented mismatch (the doc's `pushed_ids = Set(units.map(...))` line assumes `units` always exists, which the pre-fix `'full'`-only path could not honor on a fresh report).
**why-change:** none — matches PO's board deliverable verbatim; out-of-scope note (default shape frozen for bctc-analyst/ESC-5) respected — `'full'` path untouched byte-for-byte (AC-1b test asserts this).
**verification:** new `FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM.test.ts` 7/7 pass (served path via real `McpServer`+`_registeredTools`) — AC-1a ids shape/no-markdown, AC-1b full unchanged + zero-unit error preserved, AC-1c ids empty-set shape, AC-3 12-unit ids payload <4KB (measured, well under), 77-unit ids payload >10x smaller than full. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: tools=183/cron=88 unchanged (no tool added/removed). Fresh boot `PORT=3099`: `/health` toolCount=183, `/api/bctc-inspect` + `/dashboards/news-fetch/` both served (dashboard circular-dep check clean). Full local `bun test`: 15139 pass/40 skip/53 fail/1 error (470s) — within the documented 52-61 same-notebook flaky floor; targeted zone sweep (all `*bctc*`/`*BCTC*`-named test files + the 6 non-glob-matched files touching `bctc_refined_units`) 1380 pass/0 fail across 121 files confirms zero regression from this change; grep-confirmed none of the 52 full-suite failure names reference bctc/financial-reports/refine. Commit `ff36391dc`. Board `in_progress[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`; `.head` synced idle (branch:null direct-execute REVIEW flip). **Tool-grant gap flagged:** this agent's actual tool grant for this spawn is `Read/Edit/Write/Bash` only (no `mcp__gateway__call_tool`) — could not call `task_release(task:FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM)` myself as the router's spawn prompt requested; per this zone's OWN flow doc (INV-GATEWAY-1, `microservice-main.md` §6b) that release is the dispatcher session's responsibility anyway, so this is a documented note, not a skipped duty.

### STEP dev-mcp-server-S72 · dev-mcp-server · 2026-08-06T19:50:00Z
**task-id:** FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH
**what-done:** In `bctcFullTools.ts`'s `!latestRow` branch, gated the `vps_stale` refusal on `getDemandQueueDepth(db,"bctc")===0` (idle-no-work) — reused the demand-queue helper the sibling health-plane fix already shipped instead of re-deriving one.
**what-considered:**
- widen `BCTC_VPS_SLA_HOURS` — rejected per PO spec: trades a latched false-positive for a slower one, still fails on a legitimately drained queue.
- delete the gate outright — rejected: AC-2 negative control requires it to still fire on genuine staleness (actionable queue + stale push).
**why-decision:** mirrors the already-proven `formatHealth()` idle-no-work inference; depth===null (route absent from `DEMAND_QUEUE_SQL` or table missing) fails OPEN to pre-fix behavior — never fabricate a verdict from a missing table, matching the shipped sibling's own contract.
**why-change:** none — matches PO's fix spec verbatim.
**verification:** new `FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH.test.ts` 3/3 pass (positive/negative-control/fail-open). `bun tsc --noEmit` clean. Rebuilt `mcp-server` image (`824eeacf77bb…`, 2026-08-06T19:45:39Z) and recreated the container (`--no-deps`, single-service per house rule). RAW-verified against the rebuilt runtime 3 ways: (1) production DB in-container direct probe — `get_bctc_full(PLX,2026,Q1)` → honest-absence text, `get_vps_proxy_health(bctc)` → idle-no-work; (2) same call via the sanctioned Bash-bridge `mcp_call()` (gateway tools were absent from my own binding this session — degraded-mode workaround per gateway-call-contract.md §6b) — identical output, confirming the actual running MCP process agrees; (3) negative control on a scratch COPY of market.db (1 fabricated `pending` row, real 59h-stale push preserved) — `vps_stale` still fires AND `get_vps_proxy_health` still reports `Stale? YES`/`STALE:` — cross-plane invariant (AC-3) holds in both directions. Existing bctc-tools test files (15 files, incl. `1982-quality-burndown-CHIJ.test.ts`'s pre-existing vps_stale regression test) all still pass unmodified — that test's fixture has no `bctc_vps_queue` table, so depth resolves `null` and the gate fires exactly as before (fail-open, no behavior change for that fixture).
**Tool-grant gap flagged (recurring, see S71):** `mcp__gateway__call_tool` absent from this spawn's binding — used the sanctioned Bash-bridge (`scripts/agents-flow/mcp-call.sh`) for the mandated live probe instead; task_release for the sprint-task lock is the router's responsibility per spawn-prompt note, not mine this cycle.

### STEP dev-mcp-server-S73 · dev-mcp-server · 2026-08-07T00:00:00Z
**task-id:** TASK_601
**what-done:** `coordinationStore.ts` `gcExpiredLocks` Phase-1 SELECT WHERE clause gains `AND task_id NOT LIKE 'cron-registration:%'` — same exclusion class as the existing `published:*`/`cron:*`/`dev-team-cron-singleton` markers. Updated both doc-comment blocks (`ORPHAN_EMIT_ALLOW_LIST` header + inline Phase-1 comment) to document the new marker's rationale.
**what-considered:**
- only: handoff (TASK_601) prescribes the exact WHERE-clause line and comment content; no alternative implementation path — a plain SQL LIKE-exclusion is the only mechanism available given `task_kind=sprint-task` is intentionally reused (no new CHECK/enum per handoff's explicit "what not to do").
**why-decision:** matches the parent brief's diagnosis verbatim — `cron-registration:*` markers are designed to outlive their registering session (8-day backstop TTL vs <=30min session-presence expiry), so pre-fix they'd mint a bogus orphan-signal on every expiry.
**why-change:** none — scoped to exactly the 1 file / 2 doc-blocks / 1 WHERE-clause line the handoff specified; independently re-verified the Phase-2 DELETE (line ~587) has no task_id filter at all, so it was correctly left untouched (expired markers still GC).
**verification:** `bun tsc --noEmit` clean. Scoped `bun test src/__tests__/FU-LOCKSTORE-EXPIRED-GC.test.ts src/__tests__/task-lock-coordination-store.test.ts` — 54 pass/0 fail/163 expect() calls, 0 regressions. `gen-project-stats.ts --dry-run`: tools=183/cron=88 unchanged. Full suite intentionally not run — TASK_603 (depends on 601+602 both landing) owns that gate per dispatch instructions. Commit `951ddfdba`.

### STEP dev-mcp-server-S74 · dev-mcp-server · 2026-08-06T22:45:00Z
**task-id:** TASK_602
**what-done:** `tasksMdJanitorJob.ts` `KNOWN_LEGIT_PREFIXES` array gains `"cron-registration:"` (prefix branch, verified NOT the `-singleton` suffix branch). Extended `FIX-D4-HELD-LOCK-NO-BOARD-ROW-RECONCILE.test.ts` with a new `describe` block: 3 positive families (`cowork-team`/`detect-loop`/`standalone-team`) + 1 negative control (`some-random-id`).
**what-considered:**
- only: handoff (TASK_602) prescribed the exact array entry and test assertions; no alternative — item belongs in the prefix array by construction (`cron-registration:*` has no `-singleton` suffix).
**why-decision:** without this, D4's daily 03:00Z scan would flag every `cron-registration:*` held lock as an orphaned board-row-less lock the moment the sibling not-yet-shipped producer work starts minting them — a daily false-positive generator, same class as the already-whitelisted `esc-datacov:`/`intent:` markers.
**why-change:** none — scoped to exactly the 1 array entry + 4 test assertions the handoff specified. Did not touch `docs/agents/system-auditor/handlers.md`/`audit-dimensions.md` (explicitly agent-father's zone, separate task) or `coordinationStore.ts` (TASK_601, independent parallel task, landed separately as S73 above).
**verification:** Non-vacuousness per AC-3b mandate — `git stash push --keep-index -- tasksMdJanitorJob.ts` (source-only) → scoped test run = 27 pass/3 fail (RED) → `git stash pop` → re-run = 30 pass/0 fail (GREEN). `bun tsc --noEmit` clean. Full suite intentionally not run — TASK_603 (depends on 601+602 both landing) owns that gate. Commit `86b31eccd` (code+test), `a5fa7bf7c` (orch-state REVIEW + handoff record).

### STEP dev-mcp-server-S75 · dev-mcp-server · 2026-08-07T01:22:00Z
**task-id:** TASK_603
**what-done:** Added AC-3a negative/positive-control tests to `task-lock-coordination-store.test.ts` (TASK_601's `coordinationStore.ts` fix was already complete, no source change needed). Ran full `apps/mcp-server` suite (not a subset) twice. Deployed via single-service rebuild, RAW-verified image ID changed.
**what-considered:**
- handoff's pseudo-ORM test example (`db.insert('task_locks').values(...)`, `orphan_signals` table) vs the codebase's real convention (raw SQL `INSERT INTO task_locks`, signals ARE `task_locks` rows) — chose the real convention, matching the immediately-adjacent `cron:*`/`dev-team-cron-singleton` sibling tests byte-for-byte in structure.
- `pnpm test` (repo-wide) vs `pnpm --filter vn-market test` (scoped to the one workspace package) — root `pnpm test` already aliases to the filtered form; ran it directly, full apps/mcp-server suite, not a subset.
**why-decision:** the pseudo-ORM shape would not compile (no such API/table in this codebase) — vacuous test risk if blindly copied from the handoff's illustrative pseudocode. AC-4's "not a subset" mandate + this repo's documented order-dependent flaky floor (`FIX-MCP-SUITE-HEALTH-BASELINE`) means the correct evidence bar is net-new-failures=0 (name every pre-existing red), not literal 0-fail — ran twice to demonstrate the delta is flakiness, not regression.
**why-change:** none from the parent row's AC-3a/AC-4/AC-6/AC-5 spec — only the test-code idiom differs from the handoff's illustrative snippet (necessarily, since that snippet doesn't compile against this codebase).
**verification:** tsc clean. Scoped: 46 pass/0 fail (was 44); RED/GREEN non-vacuousness proof via manual WHERE-clause revert+restore (source byte-identical to HEAD after). Full suite ×2: 15157p/44f and 15156p/45f — all 45 Run-2 failures named by test suite, zero overlap with this diff's surface. AC-6: image `824eeacf77bb`→`115700a86e65` (differs, real rebuild), container healthy, `/health`+2 dashboard routes all verified live. AC-5: `git diff HEAD~1 HEAD` on both agent-father files empty. Commits `814182608`, `03af0f983`.

### STEP dev-mcp-server-S76 · dev-mcp-server · 2026-08-07T02:05:00Z
**task-id:** FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE
**what-done:** Verified pdf-extractor healthy throughout the 16:37Z batch (zero ERROR/WARN logs, /health 200 OK) via `docker logs`; mcp-server's own `data/logs/tool-get_bctc_page_image.log` shows unit-0014/page28's rasterize succeeded cleanly (no "page failed" WARN, PNG confirmed on the shared volume) yet the pushed unit still carries `image_unavailable`, and units-0015/0019 (pages 29/37) show ZERO tool-log entry at all — the loss happens after a successful server response, invisible server-side. Shipped AC2: `bctcImageFetchDegradedSignalWriter.ts` + wiring in `pushBctcRefinedUnitTool.ts` — fires one signal_queue row (severity HIGH) on the 2nd `image_unavailable`-flagged push for a report (rising-edge, never re-fires on the 3rd+).
**what-considered:**
- code fix in `getBctcPageImageTool.ts`/`pdfExtractorClient.ts` (task's suggested files) — rejected: every traced call that DID execute completed cleanly server-side; no code path in either file reproduces the observed symptom, so editing them would be unfalsifiable guessing, not a fix.
- fire on every `image_unavailable` push — rejected as noisy (1 flaky page ≠ batch degradation); threshold=2 matches this incident's real 3-in-12 shape without spamming on a single outlier.
**why-decision:** AC1 requires ruling code-vs-transient IN before touching code; live logs falsify BOTH the "pdf-extractor outage" and "code bug in the named files" hypotheses — root cause is the untraceable gap between a successful tool response and the calling Option-C subagent (shared-context base64 image payloads), agent-flow/architecture territory outside apps/mcp-server. AC2 is fully in-zone and directly actionable; did not touch the verified-correct 0.55/0.6 cap.
**why-change:** AC3 (re-run + confirm zero flags) explicitly NOT claimed — no fetch-plane code changed, so nothing to re-verify; reporting this honestly per the task's own instruction rather than forcing a false PASS. AC4 (page 30-41 ocr_corruption tail) intentionally left open for PO to split into its own row, as the task itself suggested.
**verification:** New `FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE.test.ts` 18/18 pass (flag matcher, rising-edge incl. 3rd-occurrence non-refire, SignalRowSchema shape, real-deps wiring). Existing bctc push/finalize/signal-writer suites unmodified, all pass. `bun tsc --noEmit` clean. Full suite: 15174 pass/45 fail/1 error — within this file's own documented flaky floor (S71:53, S75:44-45), zero bctc-named failures (grep-confirmed).

### STEP dev-mcp-server-S77 · dev-mcp-server · 2026-08-07T01:45:00Z
**task-id:** FIX-CI-SIZELINT-BCTCREFINED-PROJECTION-BASELINE
**what-done:** Added a `size-justification: 242L` header (first-10-lines window) to `getBctcRefinedTool.ts` explaining the `ff36391dc` projection-branch growth (162L→242L); `docs/data/size-lint-baseline.json` deliberately left untouched.
**what-considered:**
- extract the `fields:'ids'` branch into a sibling file (AC-1's other named option) — rejected: this dir's sibling single-tool files (getBctcOcfTool.ts 220L, pushBctcRefinedUnitTool.ts 227L, agmPlanTools.ts 234L) are the same size class; splitting would fragment one MCP tool + duplicate db/report_id wiring for no net LOC win.
- manually edit this file's stale baseline entry (162L) to match the new count — rejected: AC2 forbids re-grandfathering via the tool; a valid header alone bypasses the baseline lookup entirely (confirmed live precedent: composition-root.ts/orchStateSchema.ts both carry a header + a stale baseline entry and still pass `--check`).
**why-decision:** header route satisfies AC1 with a 1-file, comment-only diff and needs zero baseline.json edits — avoids the concurrent-edit conflict the router flagged against FIX-CI-SIZELINT-RAG-APP-FACTORY-BASELINE entirely, not just sequences around it.
**why-change:** none — AC1's "OR" already names this path.
**verification:** `--check` no longer lists this file; 3 pre-existing offenders remain, all owned elsewhere (rag-service ×2 in their own BACKLOG rows; `schema.ts` inside `FIX-MCP-MEMORY-CODE-LEAK`, review[]/qa). tsc clean. `FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM.test.ts` 7/7 pass. Commit `36fe87c31`.

**Merge Gate renumbering note:** authored in its own worktree as S76 (concurrent with agent a798e87183b29fadc's independent S76 on the same shared journal file, both branched before either landed); renumbered to S77 when reapplied to main since the sibling S76 landed first in Merge Gate order. Embedded timestamp above is the original authoring time, unchanged.

### STEP dev-mcp-server-S78 · dev-mcp-server · 2026-08-07T02:25:00Z
**task-id:** FIX-CI-SIZELINT-SCHEMA-TS-DEFLAKE-REGRESSION-372L
**what-done:** Extracted the always-run post-init migration block + the two foreign-flow migration helpers out of `schema.ts` into 2 new sibling files (`schema-post-init-migrations.ts`, `schema-foreign-flow-migrations.ts`); `schema.ts` 372L→261L (75L real headroom under the 336L baseline, not just under the 369L ceiling).
**what-considered:**
- Confirmed at source (not on trust) all 3 PO-suggested seams: the PRAGMA/connection-bootstrap block (`getDb`) and the WeakSet one-time-DDL guard are tightly coupled to the live busy_timeout-ordering + memory-leak deflake fixes — touching either raises AC4 risk for near-zero size gain. The always-run post-init migration block + the 2 standalone foreign-flow functions take only `db` as input and are already imported directly by 15+ tests — the clean, low-risk seam.
- 1 combined new file (~144L, over size-lint's own 120L unjustified threshold) vs 2 files split by responsibility — split chosen so BOTH land <=120L, avoiding a same-day justification-header need (mirrors FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L precedent).
**why-decision:** AC1 mandates extraction not another trim (2nd RED in 2 days off a comment-trim). `export {...} from "./schema-foreign-flow-migrations.js"` preserves the 15+ existing test import paths + the `1527-schema-slices.test.ts` backward-compat contract with zero test-file edits.
**why-change:** none from PO's AC1-5 — scope stayed exactly `schema.ts` + 2 new sibling files; `size-lint-baseline.json` untouched (AC2), no justification header anywhere.
**verification:** `size-lint --check`: schema.ts absent from offenders (3 unrelated pre-existing: getBctcRefinedTool.ts + 2 rag-service files). Scoped 91 pass/0 fail (1527-schema-slices + both foreign-flow test files). A/B full-suite (this worktree has no `node_modules` — missing `zod` inflates fail count env-wide, pre-existing/unrelated): WITHOUT fix 10476p/637f/350e; WITH fix 10477p/636f/349e — net -1 fail, matches documented flaky-floor delta, zero regression attributable to the extraction. `tsc`/`pnpm check` blocked in this worktree (no `node_modules`, lockfile drift) — pre-existing infra gap, out of this task's scope.

**Merge Gate renumbering note:** authored in its own worktree as S76 (third independent collision on this shared journal file — a798e87183b29fadc and a7cfe9046cd91202e both also claimed S76 in their own worktrees); renumbered to S78, landing last in this Merge Gate's sequential order.

### STEP dev-mcp-server-S79 · dev-mcp-server · 2026-08-07T03:35:00Z
**task-id:** FIX-CI-SIZELINT-PUSHBCTCREFINEDUNITTOOL-283L
**what-done:** Added `size-justification: 292L` header (mandatory first-10-lines window) to `pushBctcRefinedUnitTool.ts` explaining the 227L→283L growth is `73e65eafc`'s (this same session, FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE) AC2 signal-fire wiring; that commit never re-ran size-lint on the file it grew.
**what-considered:**
- Header (chosen) vs extracting the signal-fire branch into a helper — rejected extraction: the branch shares the same `db` handle + `report_id`/`effectiveFlags` already in the handler's closure, so a helper would just re-pass 3-4 params across a file boundary for no net LOC reduction, same reasoning already live on sibling `getBctcRefinedTool.ts` (242L header) and `composition-root.ts`/`orchStateSchema.ts`.
- Declared header count set to the file's EXACT post-edit line count (292L) rather than a round number, to leave zero tolerance-edge risk on `--check`.
**why-decision:** AC (from the fix task) names header-or-extraction as equally acceptable; header is the established one-file-per-MCP-tool precedent in this exact dir, comment-only, zero behavior risk on a P1 CI-red file.
**why-change:** none — task's own preference order matched.
**verification:** `size-lint --check` RC=0 (this file no longer listed). `tsc --noEmit` clean. Scoped: `TRUST-RED-sanity-gate`/`AR-refined-units-idempotency`/`FIX-REFINE-LOCK-TTL-RECLAIM`/`FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE` test files 46/46 pass. Full suite 15174 pass/45 fail/1 error (499s) — within documented flaky floor, zero bctc-named failures. Fixed this worktree's missing `node_modules` in apps/mcp-server via `bun install` run from INSIDE that dir (per prior notebook warning, never from repo root) — pre-existing infra gap, not this task's regression, now resolved for the rest of this session. Commit `bb1ac4cd1`.

**Merge Gate RAW-verified (router):** code-fix commit `bb1ac4cd1` cherry-picked clean onto main (no conflict, this journal's own S79 entry landed with it). Board-mint commit `b628dad5d` conflicted on `docs/data/orch/orch-state.json` (main had advanced) — reapplied via the standard abort/inspect-diff/jq-against-live-state/`orch-apply.sh` pattern: lane-moved the row `in_progress[]`→`review[]` (removing the router's own earlier self-mint stub, replacing with this row's full `dev_implementation_note`/`commit_sha`/`decision_journal_ref`), commit `de90b65ee`. `tree-verify.sh`/`c2-alert.sh` both clean on the resulting HEAD. Task lock `task:FIX-CI-SIZELINT-PUSHBCTCREFINEDUNITTOOL-283L` released. Worktree + branch removed.

**Push-backstop RAW-verify attestation (router, 2026-08-07T02:50Z):** the confidence-threshold lines this range's rebuild-raw-verify-check flagged (`schema-post-init-migrations.ts:39` `extraction_confidence < 0.1`; `bctcImageFetchDegradedSignalWriter.ts`/`pushBctcRefinedUnitTool.ts`'s `<=0.6` design-comment lines) are RAW-verified, not speculative: the `< 0.1` predicate is an unmodified relocation of a pre-existing, already-live SQL clause (moved during the schema.ts size-lint extraction, byte-identical logic, confirmed via `wc -l` 372L->261L RAW-verified this Merge Gate); the `<=0.6` cap is confirmed in `bctcSanityValidator.ts` source (can only pass confidence through or clamp to 0.4/0.1, never emit 0.55) per the original agent's own live investigation (pdf-extractor `docker logs` clean, per-tool `data/logs/tool-get_bctc_page_image.log` inspected directly, DB query against the live report) — reproduced verbatim above (S76) and RAW verified again during Merge Gate reapply.

### STEP dev-mcp-server-S80 · dev-mcp-server · 2026-08-07T03:27:32Z
**Merge Gate renumbering note (router):** authored in its own worktree as S79 (collided with the sizelint-fix worktree's independently-numbered S79, already landed on main at 03:35:00Z above); renumbered to S80, landing after it in this Merge Gate's sequential order. Original authoring timestamp preserved.

**task-id:** FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL
**what-done:** AC-1 RAW-verified live (`vps_push_log`, container logs) that `prices`+`foreign-flow` (2 separate VPS systemd services) went silent ~46h (08-05T04:29:40Z..08-07T02:00:11Z, ZERO push rows, not errors) while `news`/`sbv` on the SAME mcp-server receiving code path kept pushing — rules out an mcp-server defect/key-rotation/full-VM-outage, and DISPROVES the documented clock-drift precedent (would've silenced `sbv` too). `runForeignFlowFetcherJobCron` confirmed NOT the write path (dead-endpoint, `source:'none'` always, live-confirmed via logs). AC-2: new `scripts/migrations/backfill-foreign-flow-gap-2026-08-06.ts` live-probes the same upstream (`bgapidatafeed.../getliststockdata`) — confirmed UNRECOVERABLE (snapshot-only, no date param), matching the 07-22 precedent; zero rows ever written. AC-3: new `checkForeignFlowGap` (D-NEW4, `dataAuditJob.ts`) — holiday-aware calendar walk, escalates (flagged/critical) any zero-row trading day; no reconstruction path exists so `action:"none"` is never used (unlike conviction_history's sibling).
**what-considered:**
- Adopting the OPS-FFLOW-VPS-CLOCKDRIFT lead as-is — rejected, RAW-disproved (sbv/news unaffected, real VM-pause would freeze the whole clock).
- Backfilling via re-fetch — rejected, live-probed and confirmed the source is snapshot-only (no date param), same as the 07-22 precedent; fabricating values is forbidden.
**why-decision:** Root cause is VPS-side (ops zone, outside apps/mcp-server/), not fixable here; the load-bearing, in-zone fix is the completeness detector (AC-3) reusing checkConvictionHistoryGap's shape but always escalating (no backfill source exists for this table).
**why-change:** none from the task's own ACs.
**verification:** new test `FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` 9/9 pass + `scripts/migrations/__tests__/backfill-foreign-flow-gap-2026-08-06.test.ts` 8/8 pass; targeted suites (157-data-audit-job, FACTORY-SCHEDULER-split-dataAuditJob, FIX-CONVICTION-HISTORY-EOD-BACKFILL) 50/50 pass; `tsc --noEmit` clean. Live docker-exec dry-run of the new script against the named-volume DB confirms 08-06 rowCount=0, 08-05 truncatedTail=true, upstream verdict=UNRECOVERABLE.

### STEP dev-mcp-server-S81 · dev-mcp-server · 2026-08-08T16:55:14Z
**task-id:** FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER
**what-done:** Implemented architect's design brief (docs/architecture-briefs/2026-08-07-fix-mcp-sse-session-manager-reaper.md) verbatim: collapsed `SseSessionManager`'s 2 parallel Maps + 2 duplicated inline cleanup blocks into 1 `SessionRecord` map + 1 `evictSession()` called from every trigger (res.close/heartbeat-fail/idle-reap/max-age-reap/DELETE); idle(15min)/max-age(4h) reaper with `lastActivityAt` bumped in `handleMessage()`; new `DELETE /sse|/messages?sessionId=` route (server.ts). `stopReaper()` wired into `BunServerInstance.close()` alongside the pre-existing periodic-lock-reaper clear.
**what-considered:**
- §2.4's recommended (not required) `connect()` try/catch hardening — omitted: no test in the brief's 9-case plan covers it and it's explicitly flagged non-blocking; adding untested code would violate TDD-mandatory for this zone. Left as a documented pre-existing gap.
- 081 integration test's DELETE-round-trip: initial version raced a 5s internal AbortController against the first SSE read — flaky under full-1268-file-suite load (system-wide socket contention delays the first frame). Fixed by adopting the SAME graceful-degrade pattern the file's own pre-existing "GET /sse" test already uses (abort generously, catch AbortError, skip assertions) rather than inventing a new convention.
**why-decision:** Brief was already source-verified (SDK `shared/protocol.js:500-502` confirms `mcpServer.close()` cascades to `transport.close()`, one call suffices) and PO-corroborated live (container OOM-death 2026-08-07, 07-16h leak-refill cycle 2026-08-08) — zero design deviation.
**why-change:** none from the brief's own §2/§2.2/§2.3.
**verification:** 1862c extended 5→12 cases (T6-T12 assert every eviction trigger calls `mcpServer.close()`, T9 negative-control proves activity-bump resets idle clock) — 12/12 pass. 081 extended +2 (DELETE 200→404 round-trip, DELETE no-sessionId 400) — 10/10 pass, both files independently and together. `tsc --noEmit` clean. Full `bun test` (1268 files): rigorous control-vs-fix diff (git-stash isolate my 4 changed files, re-run full suite clean) — the SAME 44 pre-existing failures reproduce byte-identical in both runs (5000ms-class timeouts under full-suite CPU/socket contention, e.g. Task 1146/TSU-DEV-U5/FIX-B-2/Task 251, none touching this zone); the extra 0-2 failures beyond that baseline SHIFT IDENTITY on every rerun of either control or fix (Task 102→FIX-POLLNEWS-COUNTER-CONSERVATION→Task 1345a→FIX-1267, none in my zone) — decisive proof this is pre-existing environmental flakiness at full-suite scale, not a regression. Zero deterministic regressions attributable to this change. `PORT=3099` cold-start probe (3000 occupied by the live Docker container): clean boot, `/health` toolCount=183 sessions=0, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200 (dashboard circular-dep check), GET/DELETE `/sse` round-trip live-verified with a real sessionId. Live bonus finding: a `curl --max-time`-aborted connection never fired `res.on("close")` on this exact build — real-world confirmation of the root-cause leak shape, now bounded by the new idle reaper (unit-proven via T8/T9) instead of leaking forever as it did pre-fix. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88, both match pre-task baseline.

### STEP dev-mcp-server-S82 · dev-mcp-server · 2026-08-08T20:52:00Z
**task-id:** FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L
**what-done:** `coordinationStore.ts` was 1388L (baseline=1241L, upper=1365L, 23L overage), CI-RED on size-lint 7+ consecutive fires since ~2026-08-07T12:14Z. Read the file; its own ST-3 comment already flagged the `scheduled_tasks` query/mutation surface (10 functions, all taking an already-open `Database` param, never touching the private `task_locks` singleton) as a cohesive, structurally independent block. Extracted it verbatim into new `scheduledTaskStore.ts` (259L, own size-justification header); `coordinationStore.ts` re-exports via `export * from "./scheduledTaskStore.js"` so the 3 existing call sites needed zero edits. Table DDL stayed in `ensureCoordinationTable()`. Result: 1388L → 1173L, well under baseline — did NOT touch the `docs/data/size-lint-baseline.json:263` entry (1241, unchanged), per the row's own explicit anti-laundering mandate.
**what-considered:**
- Header on the untouched 1388L file — rejected: cleanly, mechanically splittable (10 self-contained CRUD functions, zero coupling to task_locks), a header would paper over a cheap structural fix, unlike checkForeignFlowGap.ts (S-earlier entry) where remaining logic was genuinely load-bearing prose.
- Raising the baseline number at size-lint-baseline.json:263 — forbidden by the row's own mint note (launders the regression); never attempted.
**why-decision:** A clean split along an already-documented seam ("ST-3: Internal helpers" comment) beats a header when free (zero behavior change, zero call-site edits via re-export) — same precedent as checkForeignFlowGap.ts.
**why-change:** none — split chosen on its merits from the row's own split-OR-header remedy.
**verification:** pushed `763ef6822`; live-verified on CI run `31277793877` — `size-lint` job now reports exactly 1 offender (`transport.ts`, sibling row FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L, not mine); my 2 files absent from the failing list. Same run's `bun test` job (CI's real per-file isolation runner) shows ONE unrelated `FAILEDFILE` (`167-prediction-market-job.test.ts`) — zero relation. `tsc --noEmit` clean. Focused 14-file suite 246/246 pass. `gen-project-stats.ts`: toolCount=183, cronJobCount=88, match baseline. Board → `review[]` via `orch-apply.sh` (`ebcdc1225`), `status:REVIEW`, `next_agent:qa`.

### CAP-REACHED · 2026-08-09T02:10:00Z
Byte cap breached pre-write (file at 35824/36000 bytes, 176B headroom, LINE_CAP=600/BYTE_CAP=36000 dual-axis check) discovered by dev-mcp-server-S83 pre-write cap check — a new STEP entry would not fit. Rolling further writes to `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md`.
