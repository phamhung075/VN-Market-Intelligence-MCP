# Decision Journal — Triage 2026-07-02T18:36Z · po

**Context:** dev-team tick 2026-07-02T18:37Z. Architect returned FIX-MCP-MEMORY-CODE-LEAK phase-0 recon (commit d028803e). 6 pendingSignals + 2 new telegram reports.

---

### STEP po-S1 · po · 2026-07-02T18:36Z
**task-id:** FIX-MCP-MEMORY-CODE-LEAK
**what-done:** Split the recon return into TWO tracks — parked this (root-cause code fix) in backlog[] as held (next_agent=pm) on a two-condition unpark gate; minted a separate Track-1 mitigation task to ready[].
**what-considered:**
- Relay Track-2 to pm this tick (architect NEXT=pm) — REJECTED: track-2 decompose is gated on [Track-1 rebuild+swap ships] AND [sawtooth confirmed to persist at 3GiB over 24-48h]; decomposing now risks a moot fix.
- Fold the 1-line cap edit into Track-2 — REJECTED: strands an immediate low-risk mitigation behind an indefinitely-parked code fix; different zone/owner/gate.
- Separate ops micro-task for cap-bump + rebuild NOW — CHOSEN.
**why-decision:** Cheapest-sufficient-first (recon Decision Matrix): ship cap 2g→3g + rebuild-to-HEAD (cheap, immediate OOM-risk relief) → observe → only decompose the initDatabase() init-guard if the sawtooth persists (expected per source evidence, but the gate is cheap and correct).
**why-change:** Deferred pm-relay vs architect's literal NEXT=pm — the architect's own gate ("once (1) ships and the sawtooth is confirmed to persist") justifies the park.

### STEP po-S2 · po · 2026-07-02T18:36Z
**task-id:** (ambient — signal corroboration, no task)
**what-done:** RAW-corroborated A-13 api-gateway CURL_ERR as FALSE POSITIVE and closed 5 other signals without new tasks.
**what-considered:**
- Mint an api-gateway FIX from the A-13 BUG — REJECTED: 3 independent RAW probes (Docker healthcheck FailingStreak=0, host wget /health 200 OK, in-container /health 200 OK) all green.
- Mint a CLEAN task for the claude-manager-helper 207L ctx-bloat breach — REJECTED: RAW re-read shows 176L (under cap); transient mid-write hook FP already tracked by FIX-CONTEXT-BLOAT-HOOK-SETTLE-READ-DEBOUNCE.
**why-decision:** Standing infra-failure corroboration gate + isolation-probe-first — a single CURL_ERR sample is transient; the momentary gateway "mcp:down" blip at 18:26:24 correlates with the mcp-server mem pressure (already tracked), not an api-gateway defect. Note: curl absent from host shell (rc=127) is a plausible systematic source of the auditor's CURL_ERR.
**why-change:** no change from standing guards.
