# PO decision journal — triage 2026-08-23T08:59–09:10Z · cron re-arm guard defect cluster

**task_id:** 5 rows minted (see D5). Router-direct triage, no dev-team tick.
**Spawned by:** router session `7be6b4cd`, intent `intent:po:cron-rearm-guard-defects`. User instruction verbatim: "send all problem to dev team for fix". Mid-task addendum from the same router after the 09:00Z cowork tick added 2 more findings.

## D1 — Verified every claim at source before minting; two came back CORRECTED

- **what-considered:** (a) mint straight off the router's brief (it was detailed and internally consistent); (b) re-read the three SKILL.md files, the schedule, and the scripts first.
- **why-decision:** (b). Memory `feedback_flow_doc_veto_manufactures_pass_read_spec_before_blaming_script` — a detailed brief is not evidence. It paid twice, see D2 and D4.
- **what-changed:** nothing in the brief's conclusions; two of its scoping premises.

## D2 — CORRECTION 1: the three cron families do NOT share one threshold, and they fail in OPPOSITE directions

- **measured:** `cron-cowork-team/SKILL.md:82-103` and `cron-detect-loop/SKILL.md:51-72` pass `orphan_threshold_seconds: 7200`; `cron-standalone-team/SKILL.md:68-91` passes `120` (tool minimum, deliberate per its own §1.4). All three carry byte-identical branch text and all three name session-presence as PRIMARY oracle.
- **why it matters:** the brief framed this as one blind window. It is one *shape* producing false-LIVE at T=7200 and false-DEAD (→ double-arm, memory `feedback_cron_standalone_step1b1_presence_absence_false_dead_double_arm`, confirmed 2x) at T=120. Two families failing in opposite directions from the same spec is the actual proof it is a spec bug, not operator error. Written into the row as the load-bearing argument.

## D3 — Defect 2 stays a SPIKE. I did not let it become a hypothesis-driven fix

- **what-considered:** (a) mint a FIX naming ttl-1800-with-no-renewal-hook as root cause — it is the most plausible of the three; (b) mint a SPIKE that forbids shipping on a hypothesis and names a *countable* test per candidate plane.
- **why-decision:** (b). The router explicitly wrote "Root cause UNKNOWN — do not guess." Deliberately NOT wired as a hard `depends_on` of the P0 so the architect brief is not stalled — the architect may legitimately conclude "remove presence from the cron decision path entirely", which needs no answer from the SPIKE.

## D4 — CORRECTION 2 (addendum): the already-specced catch-up fix cannot close the cited symptom

- **the addendum's read:** `catchup_raw` exists but Step 4.55 is unwired (`match-slots.md:40`, brief `2026-07-22-cowork-guaranteed-slot-catchup-design.md` §2.3/§2.1) — "plausibly the intended fix, check whether it just needs finishing."
- **measured, and it falsifies that:** `match-slots.md:40` defines `catchup_raw` verbatim as "one record per considered **`guaranteed:true`** slot". `refine-bctc-slot-1` — the very slot cited as evidence — is `guaranteed=false`. So are 6 of the 11 badly-stale slots. Finishing Step 4.55 as specced leaves the cited symptom untouched.
- **second falsification, worse:** the brief's premise is that `guaranteed:true` slots are covered by the 12 RemoteTriggers. Anchored at `2026-08-23T09:00Z`, **5 of 8** `guaranteed=true` slots are ≥9 days stale (`digest-sunday` 34.7d, `fb-weekend` 14.8d, `chef-eod` 10.0d, `fb-daily` 9.9d, `chef-morning` 9.1d). The guaranteed/non-guaranteed split may not be load-bearing at all. Both findings written into the row as mandatory architect input.
- **why-decision:** routed to architect, NOT developer. A "wiring-only" dispatch would have shipped a fix that provably misses the reported case.

## D5 — Routing: five rows, four entry points, deliberately not all through BA

- `FIX-CRON-REARM-STEP1B1-…-FALSE-LIVE` → **P0**, `ready[]`, architect. Raised P1→P0 on the addendum: modal path (every CLI restart), 8h10m confirmed, 8-day wedge potential (`ttl 691200`, no post-mortem renewal), and D6 means every dead minute is permanent loss.
- `FIX-COWORK-DAILY-SLOT-SILENT-SKIP-…-GUARANTEED-ONLY` → P1, `ready[]`, architect.
- `SPIKE-SESSION-PRESENCE-ROSTER-UNDERCOUNTS-LIVE-SESSIONS` → P2, `backlog[]`, dev-mcp-server (BOUNDED-1 lane).
- `FIX-CRONCREATE-CONTRACT-DIVERGENCE-…` → P3, `ready[]`, agent-father, `depends_on` the P0.
- `FIX-COWORK-TICKSNAPSHOT-STEP47-FALSE-PREMISE-…` → P3, `ready[]`, agent-father.
- **what-considered:** the router asked for the full BA → architect → PM → dev chain. Rejected for all five: these are FIX/SPIKE, not sprints. A BA spec for a 3-file doc-truth correction is process for its own sake, and BA authoring an AC for a plane it has not measured is the exact defect already tracked as `FIX-BA-AC-NAMES-PLANE-IMPLEMENTATION-NEVER-WRITES`. Entered the chain at the depth each row actually needs.
- **verified by execution, not by reading the row** (`zone-routing.md` § A2 mandate): all five predicates run live. Four `deps_ok=true` and picker-reachable; the fifth `deps_ok=false` **by design** — it edits the same three SKILL.md files as the P0, so it is dep-gated to serialise, not stranded.

## D6 — The two cowork rows must be read as a pair, and I said so on both

- The guard row is the *trigger*; the catch-up row is the *amplifier*. With no catch-up backstop, any dispatcher downtime — from this guard OR from host suspension (`project_host_suspension_causes_multiday_cron_silence_backlog_flush`, confirmed 2x) — converts to permanent silent data loss rather than a delayed fire. Neither row alone makes a restart survivable. Cross-reference written into both `status_note`s so a reader arriving at either one is told.

## D7 — Timezone trap promoted from an anecdote to a shipping requirement

- The router hit it mid-investigation; I hit it again independently (`cycle-snapshot-09:03.json` mtime prints "23 aoû 11:03" = 09:03Z). `stat -f '%Sm'` prints LOCAL; markers carry epoch/UTC; this host is CEST (UTC+2) with French locale, while memory `user_location` records GMT+7. Written into the P0's `status_note` as a MUST for the probe script — normalise to UTC explicitly, never parse locale month names. An anecdote in a handoff gets lost; a named requirement on the row does not.

## D8 — Mandatory pre-checks: one no-op, one BLOCKED by a defect already on the board

- **supervised-goahead:** ran dev-team's own `should_hold` predicate. `.head.status=idle`, `active_task_id=null` → nothing held. No-op.
- **manual-dispatch-sweep:** ran Step 1 live. Top candidate `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` (P1, READY-XOR). Step 2 stamp **ABORTED**: row is 26552B against a growth-only 12000B ceiling, candidate 26858B. Did **not** split the write (the check forbids exactly that) and did **not** bulk-run `orch-backlog-stub.sh` (outside a triage tick's blast radius). This is the **second consecutive PO tick** blocked by `FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS` — already `ready[]`/P1/architect. Recording the recurrence here rather than folding it onto that row, because folding it would grow that row and hit the same ceiling.
- **not run (deliberate):** channel-audit, TNB. Router-scoped single-intent dispatch with a live addendum arriving mid-task; widening scope would have delayed the P0.

## D9 — Addendum 2: minted a 6th row, and the verification made the finding STRONGER than reported

- **what-considered:** (a) fold into the Step 4.7 row — same owner, same directory, same P3, same "spec assumes a runtime surface that does not exist" class; (b) mint separately.
- **why-decision:** (b). The router said do not re-open minted rows, and a compound `verification_gate` would let one half ship green while the other rots. Both are agent-father edits under `docs/agents/cowork-team/flow/` and can be dispatched in one go anyway.
- **★ THE REPORTED MECHANISM WAS RIGHT BUT THE ATTRIBUTED CAUSE WAS INCOMPLETE, AND THE REAL ONE IS WORSE.** The addendum blamed the `.output` transcript symlink for carrying all six `OFFFLOW_MARKERS` via CLAUDE.md. Measured: project CLAUDE.md contains **3** of 6 (`PRE-CLAIM`, `session-presence`, `orphan-adoption`, one each); global CLAUDE.md contains **0**; `Coordination Results` / `Dispatch Routing` / `Expected Behavior` are **0 in both**. They come from `spawn-fanout.md` **itself** — lines 200-201, inside `IDENTITY_PREAMBLE`, which tells every spawn *"do NOT produce a 'Coordination Results / Dispatch Routing / Expected Behavior' summary"*. **The detector greps for a string list the dispatcher guarantees is in every spawn's own prompt.** The symlink is one surface that exposes it, not the cause. So fixing only the surface (the addendum's ask) leaves the detector self-defeating.
- **also measurably wrong:** `spawn-fanout.md:376` justifies the marker set as "Verbatim section headings / terms from the project-root CLAUDE.md router dispatch protocol." Half of them are not in that file at all. Flagged as its own fix item.
- **★ REFUSED TO ASSERT WHAT I COULD NOT REPRODUCE.** No `*.output` file exists anywhere under `~/.claude` at triage time, so the 187B-symlink→246939B-transcript shape is recorded on the row as **ROUTER-REPORTED, not PO-verified**, with an explicit instruction to re-confirm live before writing it into the doc as fact. Defect (b) is PO-verified and stands on its own without it — which is why the row is still worth minting even if (a) turns out to be a harness detail that changes.
- **not shipped, so P3 is right:** `grep -rlniE 'offflow|off-flow' scripts/` returns two unrelated po-lifecycle files; no script reads a `.output` path. Step 5.3 is still LLM-interpreted, so today it survives on dispatcher judgment — which is exactly what makes it a latent fleet-wide stamp-suppression outage the day someone scripts it.
</content>
</invoke>
