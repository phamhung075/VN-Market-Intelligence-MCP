# PO Decision Journal — triage 2026-08-14T19:57Z

task_id: FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE (escalation) + TASK-COWORK-MUTEX-001 (sequencing gate)

## Input
Router (session 9a78317d-60e4-495d-835e-f23805ea51b9) ran the dispatch-claim Step 2.4 cowork-slot
collision probe before spawning unified-agent for slot `chef-evening`, hit a collision on
`published:chef-evening:2026-08-13` (ttl 100800s, expires 2026-08-14T23:53:37Z), followed FR-4
literally — no spawn, WORK telegram — and routed two questions to PO: (1) dispatch chef-evening
anyway or skip, (2) signal agents-architect about the structural false-positive.

### STEP po-S1 · po · 2026-08-14T19:57:09Z
**task-id:** FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE
**what-done:** Ruled DISPATCH for tonight's chef-evening (router-side override, gate is a confirmed
false positive), and escalated the already-existing board row P1→P0 / developer→agents-architect
rather than minting a duplicate.
**what-considered:**
- Skip the slot and wait for the fix — rejected: costs a real guaranteed-slot dish for a lock held
  by a session that is provably dead, and the same block recurs identically tomorrow.
- Manually release/expire the stale 2026-08-13 marker — rejected: mutating a peer's anti-double-publish
  tombstone to work around a router-side read bug is the wrong layer and destroys forensic evidence.
- Dispatch and let chef.md's own gate arbitrate — CHOSEN.
- Mint a new row for the systemic defect — rejected: `dedup_key
  cowork-slot-collision-probe|fr3|defect:marker-ttl-exceeds-cadence-period` already covers it.
**why-decision:** The two gates are not redundant, they are differently-keyed, and only one of them
is correct. Step 2.4 prefix-matches `published:chef-evening:` and therefore cannot distinguish a
period from a period; chef.md Step 0.5 computes `MARKER_KEY = "published:" + SLOT_ID + ":" +
CYCLE_DATE_UTC` = `published:chef-evening:2026-08-14`, which is ABSENT from `task_list_held`, so the
authoritative publish-once mutex is open and will still block a genuine racer at its Phase-2 claim.
Double-publish risk of dispatching is therefore zero, not merely low. Three independent live
confirmations that this holder is not a peer mid-publish: today's key absent, no `cowork-slot:chef-evening`,
and no `session-presence` row for owner 0454e9d8 (dead session — it also still holds
`published:chef-morning:2026-08-14`, so one dead session is pinning two slots).
**why-change:** Escalation beyond the reported scope. The router reported one slot; the defect is
deterministic for all 5 daily slots carrying ttl 100800 (blocked window = 28h TTL − 24h cadence = 4h,
positioned to open exactly at fire time), so chef-evening is 100%-blocked every day, not intermittently.
Re-routed to agents-architect because the prefix match is a deliberate ruling in the 2026-07-29 design
brief (Candidate A, "zero date-basis duplication") — the brief's author must re-rule the axis. PO's
prior rejection of the naive exact-match fix stands and was NOT relaxed.

### STEP po-S2 · po · 2026-08-14T19:57:09Z
**task-id:** TASK-COWORK-MUTEX-001
**what-done:** Added a blocking sequencing gate: the QA-requested CARD.md Step-2.4 wiring must not
land before the TTL fix.
**what-considered:**
- Leave QA's CHANGES_REQUESTED to proceed normally — rejected, see below.
- Dispute the QA finding — rejected: it is correct, CARD.md still greps 0 hits for Step 2.4.
**why-decision:** Step 2.4 is currently only PARTIALLY reachable (CLAUDE.md step 2.5 names it, CARD.md
does not) — which is precisely why it has blocked 2 dispatches today instead of every dispatch.
Closing the reachability gap while the TTL defect is live would amplify a known-broken gate from
partial to fleet-wide, false-positive-blocking every router dispatch of every daily cowork slot.
**why-change:** Orders an existing correct finding; does not overturn it. Land both in one commit or
TTL-first.
