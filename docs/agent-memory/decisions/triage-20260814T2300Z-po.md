# PO Decision Journal — triage 2026-08-14T23:00Z

task_id: FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION (expedite) +
CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER (product decision) +
FIX-BCTC-REPARSE-STRANDED-LIST-GATE-STILL-QUERIES-STALE-FILENAME-PERIOD (mint)

## Input
dev-team Step 1 triage, session 632721c2-41e4-4aff-8d06-a47cf80dc0d7. 21 durable-inbox entries,
22 `status=new` telegram reports, 37 unresolved, `head=idle`, manual-dispatch sweep returned 88
live candidates (52 DRS-stranded, 36 backlog-XOR-gap). Caller pre-flagged 6 items and asserted two
premises: that a structural SPIKE for the auditor narrated-writes pattern was already minted, and
that the DXG chef gate-reversal is new evidence for a BA spec still in flight.

### STEP po-T1 · po · 2026-08-14T23:00:56Z
**task-id:** FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION
**what-done:** Verified the "structural SPIKE already minted" premise and found it FALSE — no such
row exists in any lane, nor anywhere in the nested document, nor among rows created after 18:40Z.
Declined to mint a replacement SPIKE. Instead expedited the pre-existing P0 owner row into BATCH as
an UNBLOCK and recorded the occurrence-10 evidence plus the measured stall cause onto that row.
**what-considered:**
- Mint the SPIKE the escalation claimed to have minted — rejected. `docs/protocols/fail-loud-protocol.md`
  names `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` **by id** as the owner of
  the exogenous-invocation design. A second row would be a duplicate of an architect-ratified P0.
- Re-mint under a different id to "unstick" it — rejected, and this is the load-bearing rejection.
  The row is not stuck for lack of a row. It is `review[]`/P0/`next_agent=pm`/`plan_only=true`,
  ratified 2026-08-14T00:33:23Z, 22.5h idle. `review[]` currently holds **19 P0 rows** against an RLC
  that dispatches ~1 row/tick, so P0 rank confers no expedite
  (`project_rlc_dispatch_starvation_p0_expedite_ineffective_20260814`). A new row enters the same
  queue at the same effective rank and starves identically — it would have produced the *appearance*
  of action and zero throughput.
- Escalate RLC starvation as its own new row — rejected this tick. It is already my carry-over item
  for the 6th consecutive tick; another row is exactly the failure mode above. It needs a ruling on
  dispatch policy, which is a different kind of artifact than a board row.
- Fold the existing row into BATCH as an UNBLOCK — CHOSEN.
**why-decision:** The escalation and the thing it escalated are the same defect. msg 4890 reported
that system-auditor Tier-2 c99 narrated 11 `signal_queue` writes that never landed, and published
that report with a board mint that also never landed. Adding an 11th row to a starved lane would
have repeated the pattern a third time in one day. The only action with non-zero expected throughput
is dispatching the row that already exists, out-of-band of the queue that is starving it.
**why-change:** no change from plan on the routing itself (`next_agent=pm` preserved, `plan_only`
preserved) — the change is dispatch path, not ownership.

### STEP po-T2 · po · 2026-08-14T23:00:56Z
**task-id:** CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER
**what-done:** Issued the product decision code-janitor has now requested twice
(cj-20260814T163219Z, cj-20260814T223258Z): RETIRE `team-tool-recheck` permanently. Minted the CLEAN
row whose AC is the exact idempotency marker the janitor's own payload names.
**what-considered:**
- Replace with a local cron per the no-RemoteTrigger migration rule — rejected. The migration rule
  says *how* to migrate a job that should survive, not *that* every killed job must. This one adds a
  5th standalone cron to a host with documented memory pressure, to restore a health report that has
  been absent 7+ weeks with no observed downstream consumer and no incident attributable to its
  absence; its coverage is already subsumed by system-auditor's tier sweeps.
- Leave the decision open and let the 30d prune drain the directory silently — rejected. That is the
  same end state but leaves the janitor re-emitting a decision-needed signal every session forever,
  because the suppression key is a file nobody was tasked to write.
- Retire, and make the marker file the AC — CHOSEN.
**why-decision:** The recurring signal is not noise to be suppressed; it is a correctly-working
idempotency guard whose key was never created. Writing the key is both the retirement record and the
suppression, so one small action closes the finding and the re-emission together.
**why-change:** no change from plan.

### STEP po-T3 · po · 2026-08-14T23:00:56Z
**task-id:** FIX-BCTC-REPARSE-STRANDED-LIST-GATE-STILL-QUERIES-STALE-FILENAME-PERIOD
**what-done:** Ran the requested batch-level sanity check on the 19-fire BCTC period-mismatch
quarantine batch. Concluded systemic-but-already-owned (no new row for the batch), and instead
minted the residual the shipped fix left behind.
**what-considered:**
- "16 independent bad source docs, working as designed" — refuted. In **19/19** fires the
  content-detected period is strictly LATER than the supplied one (Q1→Q4 same year ×10; Qn→Qn+k-years
  ×9). VND fired 5 times and preserved the *quarter* in all 5 while the year floated to
  newest-available. Independent per-document errors would distribute both directions; one-directional
  monotonicity across 19 samples is p ≈ 2e-6 under that null.
- Mint a new systemic-period-bug row — rejected on timeline. The fires ran 14:02–14:16Z;
  `FIX-BCTC-REPARSE-PERIOD-KEY-SYSTEMATICALLY-STALE-100PCT-QUARANTINE` shipped as `6cb8641d2` at
  15:36:25Z and is DONE_VERIFIED, and `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`
  (`03f7db28b`) covers the always-latest selection half. The batch is pre-fix evidence, not a
  regression, and the guard refusing to store is correct defensive behaviour.
- Read the shipped diff rather than the row status — CHOSEN, and it found the real gap.
**why-decision:** `bctcReparseJob.ts:477-487` now re-derives `effectiveYear/effectiveQuarter` from
document content and passes those to the pipeline, but `:616-627` still binds the already-filed
`SELECT COUNT(*) FROM financial_reports WHERE period_year=? AND period_type=?` to
`parseYearQuarterFromFilename()` — the same `yq` the fix exists to distrust. The gate therefore asks
whether the *wrong* period is stored, answers no, and re-strands the row on every job run. The fix's
own test only exercised the write path. Generalisable: when a fix establishes that an input is
systematically wrong, every other consumer of that input in the same file is in scope.
**why-change:** no change from plan.

## Premise corrections issued this tick
1. "Structural SPIKE already minted — just confirm it's on the board" → **not on the board**; declined
   to re-mint, expedited the existing owner instead (STEP po-T1).
2. "msg 4892 DXG gate reversal is new evidence for the in-flight BIZCTX BA spec's scope" → **already
   closed**. That report *is* the evidence PO converted to FR-8 at 20:36Z; agent-father shipped it at
   21:16Z. Row annotated `NOT VERIFIABLE YET` with the reason (no chef dish has fired post-FR-8).
3. "orch-cold-evict.sh failing repeatedly" → **not currently failing**. Hand-run 22:56Z: exit 0,
   Stage 0+1 PASS, conservation clean, 0 evictions. The 3 BUG reports are lost mtime-CAS races
   (`MTIME_CAS_RETRIES=3`, no backoff, `:1081`). Minted as P2 rather than treated as an outage.
4. msg 4858's "`OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` has ZERO rows in ANY lane" → **stale**;
   it is `review[]`/P0/`next_agent=po` as of 06:59:05Z.
