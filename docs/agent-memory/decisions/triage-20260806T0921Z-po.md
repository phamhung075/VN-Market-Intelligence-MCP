# PO Triage Decision Journal — 2026-08-06T09:21Z (dev-team Step 1, tick 2026-08-06T09:07Z)

Session: 24817246-8a3f-4511-95f7-1b4385797bee · 8 pendingSignals + 1 dashboard row + 1 mint + 4 row enrichments + 1 manual-dispatch stamp · 0 telegram reports (both planes empty)

### STEP po-S1 · po · 2026-08-06T09:21Z
**task-id:** FIX-CI-SIZELINT-SCHEMA-TS-BASELINE-TOLERANCE-377L
**what-done:** Ran the mandatory pre-dedup failing-file read on BOTH CI runs, resolved the `size-lint` fires to one file, deduped onto the existing row, and **backfilled the `dedup_key` field the row did not have**.
**what-considered:**
- Mint a new row for `CI-RED-f22225f2` (the templated disposition when PRIMARY dedup finds nothing)
- Dedup by eye onto the existing row and move on
- Dedup AND repair the field that made the automated dedup miss
**why-decision:** `gh run view 31085916894 --log-failed` and `... 31087473563 ...` both name the identical single offender: `apps/mcp-server/src/infrastructure/db/schema.ts — baseline-tolerance-exceeded (baseline=336L actual=377L upper=369L)`. That is `FIX-CI-SIZELINT-SCHEMA-TS-BASELINE-TOLERANCE-377L` (review[], P0). But the row carried **no `dedup_key` field at all**, so `triage-signals.md`'s PRIMARY (file-scoped) dedup — which matches on that field — could not have hit it, and SECONDARY (check_id/head_sha in title/status_note) also misses because both are SHA-derived and the SHA advances every tick. A correct-by-eye dedup that leaves the field absent guarantees the *next* drain re-mints the duplicate. Backfilling is the only disposition that survives the next tick.
**why-change:** The templated action for a PRIMARY miss is "mint". I overrode it because the miss was a **field-absence defect on the target row**, not evidence of an untracked failure — minting would have manufactured the duplicate the dedup exists to prevent.

### STEP po-S2 · po · 2026-08-06T09:21Z
**task-id:** FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART
**what-done:** Read the failing file for the **undrained** `ci-red-25111889` signal's `bun test` job, found the failure already predicted by an open backlog row, attached the live CI evidence and backfilled that row's `dedup_key` too. No mint.
**what-considered:**
- Skip entirely — the signal is not in this tick's `pendingSignals[]` and the router said the CI-red thread has an owner in flight
- Mint a new row now for the `bun test` failure
- Read the file, dedup onto whatever it hits, mint nothing
**why-decision:** The router's "already has an owner" applies to the **size-lint/schema.ts** thread only; `bun test` is a different job and `25111889`'s failure is a different file. Reading it cost one command and settled the question: `15007 pass / 40 skip / 1 fail`, `FAILEDFILE: src/__tests__/1299b-skill-gated-bootstrap.test.ts` — and `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART`'s own title already says *"cascading TDZ flake on 1299b"*. It is tracked, so no mint. This is exactly what the mandatory failing-file read is for: on job name alone (`bun test`) the signal carries zero discriminating information and I would have had to guess between "tracked" and "new".
**why-change:** Did not defer the read to next tick's drain. Deferring would have left next-tick-me with the same undone read and a row still missing its `dedup_key`, i.e. the duplicate-mint risk of po-S1 repeated.

### STEP po-S3 · po · 2026-08-06T09:21Z
**task-id:** FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-SCOPED-ACTOR
**what-done:** Dispositioned two `escalated=true` commit-sweep-guard signals as CONFIRMED TRUE POSITIVE, folded onto the existing family row, and recorded a *new* operational consequence on it.
**what-considered:**
- Open a new `bug-escalation` FIX row (the literal templated action for `escalated=true`)
- Fold onto the family row
- Read `git show --stat` on the landed commits and call them benign
**why-decision:** Dedup against the `FIX-COMMIT-SWEEP-GUARD-*` family hit four open rows, so fold, not mint. The disposition itself was verified **at source, not from `--stat`**: `scripts/git-hooks/pre-commit:833-834` sets `escalate_effective=reject` when `escalated=true`, and `:854-861` does `exit 1` — the actuator is genuinely wired and did block both attempts. The commits that then landed (`3bfd388ea`, `123903578`) are pathspec retries (`mode=SCOPED`, silent `exit 0` at `:453`), which is why they look identical to swept bare commits in a `--stat` diff. The third option is the explicitly INVALID disposition in my own triage table and I did not take it.
**why-change:** Added an observation the row did not previously carry: `actor` is the pooled coordination-session id, the counter never resets, so past threshold=3 *every* later bare commit by *any* agent in that session both blocks and emits a signal — 11→12 warns in 23 minutes here. That is a monotonic signal-noise amplifier distinct from the retroactive-counter defect the row already tracks.

### STEP po-S4 · po · 2026-08-06T09:21Z
**task-id:** CLEAN-SKILL-BLOAT-CRON-STANDALONE-DB-INTEGRITY-SPLIT-CHILDREN-BYTE-CAP
**what-done:** Minted P2/CLEAN for the two context-bloat breaches created by the skill split that shipped ~2h earlier.
**what-considered:**
- Treat as a false positive / intentional trade-off (the router's stated hypothesis: the children are large because they were extracted *from* an oversized parent)
- Fold onto `CLEAN-SKILL-BLOAT-TASK-LOCK` (same skill-file class)
- Mint a fresh row naming the split criterion as the root cause
**why-decision:** Measured rather than assumed. `register-job-db-integrity-weekday.md` = 198L / 16158B and `-offhours.md` = 196L / 15967B against caps of 200L / 12000B: both sit 1–4 lines **under** the line cap and 33–35% **over** the byte cap. So the router's hypothesis is refuted — the children were never under the byte cap at any point, and the parent *did* clear (`register.md` now 48L/1993B, other 3 children 11–15L). The split was right in direction and stopped at the wrong predicate. Not folded onto `CLEAN-SKILL-BLOAT-TASK-LOCK`: that is a 4-line line-cap trim on an unrelated skill, and collapsing a byte-cap-blind *split heuristic* into it would bury the actual root cause.
**why-change:** Also recorded a second, cheaper finding on the row: commit `3bfd388ea`'s own message asserts *"context-bloat-backstop.sh emits no breach for either line-count or byte-size predicates"* — the hook contradicted that within 2 minutes on both files. A false-green self-verification, same class as the narrate-vs-persist failures this board keeps hitting.

### STEP po-S5 · po · 2026-08-06T09:21Z
**task-id:** FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE
**what-done:** Attached the `UC-CRITIC-HOOKS-ENFORCEMENT` 24h stall to this row as its first live measurement. No new row for the "SLS ratification lands but never dispatches" gap the router flagged.
**what-considered:**
- Mint a new row for ratification-without-dispatch (router's suggestion)
- Fold onto `FIX-DEVTEAM-READY-REVIEW-LANE-SUPERVISED-PLANONLY-NO-PICKER`
- Fold onto the 24h head-pin row
**why-decision:** Read the row before deciding. `UC-CRITIC-HOOKS-ENFORCEMENT` **already carried `po_goahead_20260805T090611`**, so WF-2's `should_hold` was false from 2026-08-05T09:06:11Z onward — the hold was never the blocker, and a "ratification doesn't trigger spawn" row would name the wrong mechanism. What actually happened is that `.head` moved off the row and nothing brought it back for ~24.2h, which is precisely what the head-pin row's title predicts ("ONE silently-failed spawn takes all four dispatch lanes offline for up to 24h"). Ruled out `FIX-DEVTEAM-READY-REVIEW-LANE-SUPERVISED-PLANONLY-NO-PICKER` by reading its title: it scopes `ready[]`/`review[]` only and does not cover an `in_progress[]` row `.head` has abandoned.
**why-change:** Declined the router's suggested new row. The gap is real but already owned; what it lacked was a measurement, which this instance supplies.

### STEP po-S6 · po · 2026-08-06T09:21Z
**task-id:** TE-T16
**what-done:** Manual-dispatch sweep: 39 candidates, stamped and folded index-top `TE-T16` (P1, `next_agent=agent-father`).
**what-considered:**
- Skip the sweep this tick — the last three picks were all stamped and none dispatched
- Pick another non-index-top row to vary the target (last tick's tactic)
- Take index-top and let the stamp record the throughput failure
**why-decision:** Last tick I deliberately passed over `TE-T16` for `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` on the theory that the pick's *target* was the constraint. That row was stamped and also went undispatched, which falsifies the theory: varying the pick changes nothing because the bottleneck is router spawn capacity, not candidate selection. So I took index-top per the documented ordering and stopped optimising a selection that is not the binding constraint. Skipping was rejected because `flag_reentrant`'s 4h staleness window means an unstamped candidate is simply invisible for another cycle.
**why-change:** no change from plan — reverted to the documented index-top ordering after one tick of an experiment that produced no signal.
