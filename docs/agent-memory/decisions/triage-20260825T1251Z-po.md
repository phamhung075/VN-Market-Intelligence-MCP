# PO Triage — 2026-08-25T12:51–13:10Z

Targeted tick: one SECONDARY-Drain disposition + one INCIDENT_CAP ruling, plus a
coordinator correction on 8 inbox signals. Not a full triage sweep.

## D-1 — `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-SKILLS` → DONE_VERIFIED

**task_id:** FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-SKILLS

**What was considered:**
1. Sign off DONE_VERIFIED (chosen)
2. Move to `archive[]` keeping status DONE — the treatment I gave the 8 pm epic
   wrappers at 12:30Z
3. Leave in `done[]`, let the `updated_at` bump derank it
4. Rework / reassign / BLOCKED

**Why (1):** The row needed no decision at all — `po_signoff_verdict` = APPROVED was
already written 2026-07-21T19:29:49Z. Its only real defect was stopping at status DONE.
That is not cosmetic. `scripts/lib/devteam-eligibility.jq` `deps_satisfied` requires every
dep to resolve to `DONE_VERIFIED` — its own comment says "plain DONE is NOT sufficient".
`backlog[]` row `FIX-COMMIT-SWEEP-VICTIM-SELF-DETECT` depends on BOTH this row and its
sibling `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-HOOK` (already DONE_VERIFIED). Measured
live before the write: `deps_satisfied=false`, with this row as the sole unsatisfied dep —
undispatchable for 5 weeks for that one reason. Measured after: `true`.

**Why not (2):** archive[] was correct for the wrappers because *nothing shipped* there —
the only way to write DONE_VERIFIED would have been to fabricate a `verification.raw_probe`.
That does not apply here. This row shipped: commit `3e4f50d3a` exists, is an ancestor of
HEAD, and its `--stat` lists exactly the 3 in-scope files. I re-read all three at CURRENT
HEAD and checked all 6 acceptance criteria at source. So the raw probe is real, and
archive[] would have *hidden* a row whose true state is verified — strictly worse.

**Why not (3):** rotation is exactly what burned three SECONDARY picks in one day.

**Probe recorded on the row** (`verification.raw_probe`): commit-mutex:54-58 commit line ends
`)" -- <path1> <path2> ...`; commit-boundary carries a new `## RULE 2.5` between RULE 2 and
RULE 3 with a CORRECT/FORBIDDEN pair; commit:88 ends `-- <same explicit file paths staged in
Step 1>` (AC-1). RULE 3 backstop present and unmodified (AC-2). Collection logic untouched
(AC-3). Anti-pattern doc examples preserved (AC-4). The 4 Layer-2 sites untouched — the
commit `--stat` has only the 3 in-scope files (AC-5).

**Note on the spawn text:** the dispatch named `status_note`/`review_note` and a
"stale review[]-lane row" premise. All wrong for a `done[]`-origin pick. Already folded as
AC-7 on `FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY` at 12:30Z; nothing
further needed here. I read the row, not the premise.

## D-2 — INCIDENT_CAP is 0/2, and the producer/consumer field names never matched

**What was considered:** (a) trust the dispatch's numbers; (b) re-measure independently
(chosen); (c) leave the notebook claim standing.

**Independent measurement, live board:**
- `po_expedited_at` at ANY depth, ALL lanes incl. `active_sprints[].tasks[]`: **0**
- `.task_board.in_progress` length: **0**
- `incident_wip_in_progress` (run through the real `scripts/lib/devteam-eligibility.jq`,
  not a hand-written predicate): **0**
- → INCIDENT_CAP **0/2, both slots FREE**. The dispatch's number was right.

**But the dispatch's explanation was incomplete.** Its story was "the two stamped rows
completed and were cold-evicted, taking the field with them". Half true: git-traced
`po_expedited_at` across 120 orch-state commits — the last canonical holder was
`FIX-DEVTEAM-ELIGIBILITY-EFFECTIVE-NEXTAGENT-DETAIL-FIRST-NO-RECENCY-COMPARE`
(`po_expedited_at=2026-08-25T07:32:38Z`, DONE_VERIFIED), present at `687efc96` (12:18:50Z)
and gone by `5ac35302` (12:39:45Z). Confirmed.

**The larger finding it missed:** three rows carry PO escalations recorded ONLY as bespoke
per-tick prose keys — `po_expedite_20260824T1056Z` (CCATO, ready[]),
`po_expedite_20260824T0737Z` (MONTHLYSIGNALQUALITYAUDIT, backlog[]),
`po_expedite_20260824T0716Z` (CLEAN-SALVAGE-ORPHANED-WORKTREE, done_verified[]).
`is_po_expedited` reads ONLY `.po_expedited_at`. Two of those three are still on hot lanes —
never evicted at all. So the incident lane's input set was being zeroed by PO's own naming
inconsistency, not only by eviction. This is the `bespoke per-tick keys hide real coverage`
pattern again; the tell is that `grep expedit` finds them and the machine predicate does not.

**Verified the lane works before using it.** Dry-ran the real
`scripts/devteam-backlog-claim-incident-lane-consumer.jq` in scratch against the live board:
baseline picks **0** rows. With a stamp on the CCATO row it picks exactly that row,
`ready[] -> in_progress[]`, status IN_PROGRESS, `claimed_by="dev-team (incident-lane
consumer)"`, head -> developer. ILC is unconditional (no head gate, no rotation gate), so it
fires next dev-team tick.

**Decision: expedite ONE, not two.** Stamped canonical `po_expedited_at` on
`FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID` (P0, was
ready[] index 104 of 108 — LAST of 8 P0s, the array-index-tiebreak starvation).
Deliberately left the second slot free: `in_progress[]` has stranded rows four times, and the
explicit-terminal-shape fix has exactly ONE successful live test (08-23). Two stranded rows
would hold both incident slots indefinitely and kill the lane permanently; one costs half.
Stamp one, observe it drain, stamp more next tick.

**Second candidate, dry-run-proven, NOT stamped:** `TASK-BRANCHGUARD-POSTCHECKOUT-HOOK`
(ready[90]). Correcting my own carry-over: it is **P1**, status TODO — not P0. It is picked
second when both are stamped (P0 before P1, correct ordering). Premise still holds:
`scripts/git-hooks/post-checkout` and `.git/hooks/post-checkout` both absent.
`FIX-MONTHLYSIGNALQUALITYAUDITJOB-...` is NOT a candidate at all — it is in `backlog[]` and
ILC's scope is `ready[]` only, so stamping it there would buy nothing.

**Premise re-verified before expediting, not trusted.** The CCATO row's own prose claims
"108 rows, 27x amplification, EMITTER STILL LIVE". The first two are now STALE: the live
queue holds only **2** `ntg-*` rows and both carry real distinct clock reads (20:33:31Z /
20:36:14Z). The third is TRUE and I re-proved it: 6 brand-new `ntg-*` rows (suffixes
`3bcd96 6c2355 286c9c 328400 51973a 1d332d`) were FIRST introduced at commit `ddab2f4c`,
2026-08-25T07:07:04Z — `git log -S` on the suffixes shows first-introduction there, which
discriminates a live emission from a stale-full-doc restore. All 6 carried the frozen ts
`2026-08-24T00:00:00Z` (31h stale — that kills any "clock read once at midnight" theory),
all `technical_indicators-VNM` (one finding, 6x amplification), zero `dedup_key`. Removed
again 7.5 min later at `e3fb7e5f`. So the defect is the emission path, and it is live.

I replaced the two stale notes on the row with one corrected note. That was also forced:
the row was already 13010 B, over the 12000 B prose ceiling and grandfathered, and
`orch-row-prose-ceiling-check.mjs` hard-rejects any growth on an over-ceiling row
(`candidateBytes > liveBytes`). Correcting-and-shrinking satisfied both the guard and the
truth. Row now 12919 B.

## D-3 — Coordinator correction on the 8 inbox signals: minted exactly ONE

Instruction was to not mint on refuted premises and to re-verify at source. Complied.
- Confirmed the citation warning myself: the hangSeng signal resolves to
  `.signal_queue.rows[24]`, and `rows[25]` is the code-janitor row — exactly as warned.
  Resolved by id, never by index.
- **Minted:** `FIX-EVENING-GLOBALSNAPSHOT-ZERO-GUARD-DROPS-HANGSENG-ONLY-ROW` (P2, BACKLOG,
  `apps/mcp-server/`, dev-mcp-server). Verified at source: briefing
  `queryGlobalSnapshot.ts:32` includes `|| cpRow.hang_seng !== 0`; evening
  `queryGlobalSnapshotStep.ts:33` omits it, while both return `hangSeng` in the result.
  Dedup by SUBJECT across every lane (zero hits) + archive (one hit,
  `FACTORY-APP-split-assembleEveningSummary` DONE_VERIFIED — the refactor that carried the
  defect forward, not one that fixes it).
  I did NOT reduce it to the one-liner: the module header says the evening query is
  "deliberately NOT the same query" and lists two justified differences, saying nothing about
  the zero-guard. AC-1 therefore requires deciding from git history whether the omission was
  intentional BEFORE patching. AC-3 forces the frequency count to be read from the live
  volume, naming the `./data/market.db` decoy trap explicitly.
- **Not minted (refuted or already owned):** code-janitor dead-writer claim (writer alive,
  13 files); "513 stale pending" (real count, wrong DB — owned by
  `FIX-AUDITOR-C01-DAILY-OHLCV-MEASURES-STALE-HOST-DB-DECOY-FALSE-CRITICAL`, backlog[543],
  confirmed present); tier-2 oracle-soundness (premise false, 4 covering rows).
- **Left in the inbox, deliberately:** the 5 `auditor_cycle_*` rows. The correction states
  the 2 tier-1 ones are LATE TRUE POSITIVES of a real outage and that the circulating
  trigger-file-oracle remedy would SUPPRESS a correct alarm. I did not verify that outage
  myself this tick, and minting a covering row on someone else's unverified premise is the
  exact failure the correction warned about. Held for the next drain with the correction
  attached, rather than minted blind or silently dropped.
- **Rejected outright:** the spawn-debounce GC remedy that resets
  `spawn_count`/`first_seen_at` — it regresses shipped, QA-verified AC-4, test-locked at
  `auditor-tier1-probe.test.sh` T-DEBOUNCE-3.

## Incidental finding, not acted on
`FIX-BCTC-BANK-SUMMARY-MAPPING` appears **twice** across task_board lanes. Pre-existing —
present on the live board before my writes and unchanged by them. `orch-apply`'s dup-key
guard is a raw-text JSON key check, so a duplicate row *id* across two lanes passes every
stage. Not minted this tick (outside the assignment); recorded here so it is not lost.
