# Decision Journal — triage-20260812T0000Z · po

**Sprint goal:** ambient dev-team Step-1 triage tick (no active sprint task in scope)
**Agent:** po
**Started:** 2026-08-12T00:00:14Z

---

### STEP po-1 · po · 2026-08-12T00:00:14Z
**task-id:** FIX-CRON-ALERTSCAN-CONFIG-KEYS-ORPHANED-BY-PARALLEL-WRAPPER
**what-done:** Split taAlertScan/bbAlertScan out of the Layer-A false-positive fold and minted a dedicated FIX row.
**what-considered:**
- Fold all 9 cron_fire_gap signals into FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL, as the 21:00Z cycle did
- Mint one row per stale cron (9 rows)
- Discriminate on `last_fire` vs `expected_last_fire` and split only what the ladder bug cannot explain
**why-decision:** Live `/api/cron-status` separates the two classes cleanly: vpsProxyWatchdog's `last_fire` EQUALS its own `expected_last_fire` and is still STALE (ladder bug), while taAlertScan's `last_fire` is 109 days BEFORE its `expected_last_fire` — an age the ladder bug cannot manufacture. Source confirms why: `schedulerJobTable.ts:447-458` registers only `alertScanParallelJob`, and `CRONS.bbAlertScan` has no registration at all.
**why-change:** The 21:00Z cycle reached the same diagnosis but recorded it only as notebook prose plus a fold — which is exactly the defect that cycle itself minted `FIX-PO-REFUTED-DETECTOR-FINDING-HAS-NO-SUPPRESSION-FEEDBACK-CHANNEL` for. A refutation with no actuator re-fires forever; this row is the actuator.

### STEP po-2 · po · 2026-08-12T00:00:14Z
**task-id:** FIX-AUDITOR-A30-DEEPPROBE-TRUNCATES-ON-SECOND-CONTAINER-RAG-SERVICE
**what-done:** Minted a probe-tooling FIX from telegram report 4707 (3 consecutive Tier-1 DEFERs) with an AC that first tries to refute its own premise.
**what-considered:**
- Fold into FIX-AUDITOR-A30-PROBE-SH-MISSES-RAG-SERVICE-CONTAINER
- Fold into FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD
- Mint new, scoped to probe completion rather than probe scope or window length
**why-decision:** Neither candidate matches. The notebook shows rag-service IS now engaged (c41 L179, c42 L31), so the "never checks it" row is already superseded; and the window-length row wants the OPPOSITE change. `PROBES=12 x INTERVAL=25` = 300s per container, two engaged containers = 600s, and pdf-extractor — which always succeeds — is always probed FIRST. That predicts failure follows probe ORDER, not the container.
**why-change:** Report 4707 asserts a rag-service-specific cause. I did not accept it: AC-1 reverses the probe order specifically to refute the reporter's premise before any fix is written.

### STEP po-3 · po · 2026-08-12T00:00:14Z
**what-done:** Deduped all 3 CI-RED-fbbbaa31 failing files to existing rows; 0 new CI rows.
**what-considered:**
- only: run the mandatory `gh run view --log-failed` failing-file read BEFORE dedup, per triage-signals.md
**why-decision:** `=== FAILED FILES (2) ===` gave the bun-test set verbatim and the size-lint step named `apps/mcp-server/src/interface/mcp/transport.ts`; all 3 hit an existing file-scoped `dedup_key`. Job names alone would have been undiscriminating.
**why-change:** no change from plan — same disposition as the 21:00Z cycle. Third consecutive tick where CI triage mints nothing and the rows simply are not dispatched.

### STEP po-4 · po · 2026-08-12T00:00:14Z
**task-id:** TASK-COWORK-MUTEX-001
**what-done:** Re-admitted and stamped the top manual-dispatch-sweep candidate (P0, BACKLOG-XOR-GAP), folded into this tick's BATCH.
**what-considered:**
- only: it is rank-0 of 15+ candidates and its prior stamp (2026-08-11T13:30:22Z, ~10.5h) is past the 4h staleness window
**why-decision:** `flag_reentrant` re-admits it by design — a stamp without a dispatch is the exact strand `FIX-PO-MANUAL-DISPATCH-SWEEP-FLAG-WITHOUT-DISPATCH-STRANDS-ROW` cured. It was stamped 10.5h ago and is still BACKLOG, so it was stranded again.
**why-change:** no change from plan.
