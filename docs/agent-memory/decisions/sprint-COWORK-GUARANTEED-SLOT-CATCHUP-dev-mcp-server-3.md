# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · dev-mcp-server (continuation 3)

**Sprint goal:** Make cowork `guaranteed:true` an HONORED contract, not a false promise (see orch-state sprint_goal.entries[COWORK-GUARANTEED-SLOT-CATCHUP]).
**Agent:** dev-mcp-server
**Started:** 2026-08-01T00:54:13Z
**Continuation of:** `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md` (rolled — BYTE_CAP breached, see CAP-REACHED marker there)

---

### STEP dev-mcp-server-S56 · dev-mcp-server · 2026-08-01T00:54:13Z
**task-id:** FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT
**what-done:** In `macroTools.ts`, replaced `get_macro_snapshot`'s `text: JSON.stringify(data, null, 2)` (raw-JSON leak-trap) with `buildMacroSnapshotText()` — a generic recursive renderer (`humanizeKey`/`formatScalar`/`renderSection`) that walks the raw macro-indicators response and emits a bracketed `[Section]`/indented `Key: Value` prose block, same shape family as `get_market_snapshot`'s text. Added `data` (the raw upstream object, verbatim passthrough) as a new envelope field alongside `source_tier`/`text`/`fetchedAt` so synthesizing agents keep typed access without parsing prose. Added new `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT.test.ts` (16 tests: 8 pure-unit on `buildMacroSnapshotText`, 3 integration on the live tool). Rewired the `parseInner()` helper (duplicated across `089-tool-macro.test.ts`/`1423d-thien-thoi-snapshot.test.ts`/`1423f-deposit-rate-display.test.ts`/`1570c-dinh-gia-snapshot.test.ts`, plus one inline assertion in `1903a-dispatch-regression.test.ts`) from `JSON.parse(env.text)` to `env.data` — these 5 files' helper WAS the raw-JSON-leak-trap behavior itself, asserted as correct pre-fix. Updated `macro.md` (new invariant 1a) + `get_macro_snapshot.md` (signature/output-format refresh) — doc-review.
**what-considered:**
- Hardcoded per-field Vietnamese section labels (like `get_market_snapshot`'s bespoke `[HOSE]`/breadth lines) — rejected: directly violates the backlog's explicit `generic_mandate` ("Format the text field generically for ALL macro fields — no per-field/per-ticker hardcode"); a structural camelCase/kebab-case key-humanizer + recursive object walker satisfies both "human-readable" and "generic" simultaneously, and auto-covers any field the Go service adds tomorrow with zero code change here.
- How to keep typed values reachable once `text` becomes prose: (a) enumerate individual top-level struct fields like `vn_index`/`breadth` in `get_market_snapshot` — rejected, requires per-field enumeration (same generic_mandate violation); (b) a single generic `data` passthrough of the raw upstream object — CHOSEN, zero enumeration, matches the backlog's own "keep the structured fields available alongside (envelope unchanged)" wording literally, and mirrors the market-snapshot precedent of adding structured fields beside prose text without touching `source_tier`/`fetchedAt`.
- 6 test files (089/1423d/1423f/1570c/1903a — 1918a and 1881a were unaffected, confirmed by full read) that `JSON.parse` the inner `.text` were themselves asserting the exact behavior this task closes; fixed by rewiring their shared `parseInner`/one-off assertion to `env.data` rather than deleting coverage — every original assertion body (signals.carry.regime, oilUsd, etc.) still runs unmodified against the new field.
**why-decision:** the generic recursive renderer is the only design satisfying both the human-readable AC and the explicit generic_mandate at once; `data` passthrough is the minimal-diff way to keep "structured envelope fields still present" (verification_gate wording) without re-introducing per-field hardcode.
**why-change:** no material deviation from the backlog fix_spec — implements exactly "human-readable Vietnamese-friendly section block (same shape as get_market_snapshot), keep structured fields available alongside."
**verification:** New `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT.test.ts` 16/16 pass (RED-then-GREEN confirmed manually: old `JSON.stringify` behavior would fail MHT-01/MHT-02/etc.). 5 updated pre-existing files + 2 unaffected guard files: targeted 8-file macro suite 106/106 pass, 215 expect(). `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged. Fresh boot `PORT=3198 DB_PATH=:memory:`: `/health` toolCount=183. Full `bun test` x2 independent runs (captured to file, not tail-truncated): 15027 pass/40 skip/52 fail and 15026 pass/40 skip/53 fail — inside the documented FIX-MCP-SUITE-HEALTH-BASELINE band; grep-confirmed ZERO fail-list matches for `macro|089-tool-macro|1423d|1423f|1570c|1903a|1881a|1918a|FIX-MACRO-SNAPSHOT` in either full log. Docs updated: `docs/architecture/microservice/mcp-server/macro.md` (invariant 1a), `docs/agents/tools/list/get_macro_snapshot.md` (signature/output-format). Simplicity gate: PASS (Q1 no extra flags, Q2 all helpers multi-call-site/recursion-required, Q3 max 2 layers of indirection, Q4 all lines directly serve the AC).

### STEP dev-mcp-server-S57 · dev-mcp-server · 2026-08-01T01:39:00Z
**task-id:** FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L
**what-done:** Extracted `humanizeKey`/`formatScalar`/`renderSection`/`buildMacroSnapshotText` (the S56/e64ad8870 humanize-text block) out of `macroTools.ts` into new sibling `macroSnapshotText.ts` (109L). `macroTools.ts` re-imports + re-exports `buildMacroSnapshotText` for test-import backward-compat. macroTools.ts: 618L→530L (baseline 501L, upper tolerance 551L — now inside tolerance).
**what-considered:**
- Extraction into sibling module (CHOSEN) — mirrors FIX-CI-SIZELINT-TECHANALYSIS-ROUTER-NEW-OFFENDER-143L precedent (health_handler.go out of router.go): the renderer is self-contained, zero coupling to tool-registration code.
- `size-lint-justification.sh --update` (regenerate baseline) — explicitly forbidden by the task's own landmine warning: would grandfather the regression, disarming the guard for this file.
- size-justification header instead of splitting — rejected: no genuine structural reason extraction was impossible; would be a substitute for real debt reduction.
**why-decision:** extraction is the only option that both closes the size-lint gate AND doesn't disarm it for future regrowth; matches established repo convention.
**why-change:** none — implements PO's own preferred approach verbatim.
**verification:** `size-lint-justification.sh --check` exit 0 (0 offenders). `bun tsc --noEmit` clean. `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT.test.ts` + `089-tool-macro.test.ts` 34/34 pass, 72 expect(). Byte-identical function bodies (no logic edits, pure move). Committed `f28c6c239` (explicit-path stage, RUN-SOLO discipline — survived a mid-task shared-working-dir hijack by a concurrent peer session that transiently reverted the uncommitted edit; re-applied + committed immediately per the subagent-branch-hijack lesson). Pushed to `origin/main`; CI run `30678397978` headSha `f28c6c239ac8a4e8e8e97ad83dbf2d8b8e121113` `size-lint` job conclusion=`success` (verified via `gh run view --json jobs`).
