# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · developer

**Sprint goal:** no active sprint goal matched this task; resolved per skill mechanical rule (latest active sprint_goal.entries row) — this task is an unrelated cross-service P1 FIX, ambient to this sprint slot.
**Agent:** developer
**Started:** 2026-07-23T10:03:26Z

---

### STEP developer-S1 · developer · 2026-07-23T10:03:26Z
**task-id:** FIX-DRAIN-PERSIST-GUARD-COUNT-DRAINABLE-ONLY
**what-done:** Extracted drain-signals.js's inline "SKIP non-signal shape" check into a shared `isDrainableShape()` + read-only `--count-drainable` subcommand; both the drain-signals.md MANDATORY PERSIST GUARD and dev-team-tick-preflight.sh Step 5 idle-check now call it instead of a raw `ls docs/signals/*.json | wc -l`.
**what-considered:**
- Reimplement the from/type check in bash/jq inside the preflight script — rejected, task explicitly forbids forking a second predicate definition.
- Only fix drain-signals.md's own guard, leave preflight's idle-check raw-counting — rejected, preflight's own header comment says it mirrors the SAME guard fields, so leaving it raw would keep forcing Step 0a on litter-only ticks (the exact starvation symptom).
- Add `DRAIN_SIGNALS_DIR_OVERRIDE` env seam (mirrors existing `ORCH_APPLY_LIVE_FILE_OVERRIDE` convention) so the preflight's isolated test fixtures reach the shared script without touching live docs/signals/.
**why-decision:** Single source of truth for "is this a routable signal" — script is already the canonical drain implementation per drain-signals.md's own CANON-SCRIPT note; sharing it (not forking) is the only option consistent with that existing precedent and the task's explicit instruction.
**why-change:** Mid-implementation caught a real bug during verification: my first `sed -n 's/.../\+.../p'` extraction used GNU-only `\+` (BRE), which BSD/macOS `sed` silently fails to match — count came back empty on this host, masking the whole fix (idle-check always read 0, always "idle"). Replaced with portable bash parameter-expansion prefix-strip; caught only because I live-verified the guard decision on both fixtures per the task's hard gate, not by trusting green tests alone (the tests I'd hand-written matched the same buggy assumption until re-run against the real macOS sed).

### STEP developer-S2 · developer · 2026-07-23T10:22:58Z
**task-id:** FIX-LAUNCHD-PROBE-PRESENCE-ONLY-FALSE-GREEN
**what-done:** `_check_launchd_agents()` in `auditor-tier1-probe.sh` now matches the LABEL column exactly (field 3 of `launchctl list`'s tab-separated PID/Status/Label output) then asserts field 2 (Status) == "0"; a present-but-nonzero-status label fails with `<label>(exit-status:<code>)`, distinct from the pre-existing `<label>(not-loaded)` absent case. Obsolete-allow-list `case` skip left byte-identical (PO's explicit do-not-change).
**what-considered:**
- Substring-match the whole `lc_out` blob for the status too — rejected: no reliable way to isolate which line's status belongs to which label without field-splitting, risk of matching a different label's status on partial-string collision.
- awk exact-match on field 3 (label) then read field 2 (status) from the same matched line — chosen: real `launchctl list` (verified live on this host) is tab-delimited 3 columns, PID\tStatus\tLabel; exact-match avoids the old substring-match's false-positive class entirely as a side benefit.
**why-decision:** Only option that answers both "is it loaded" and "is it healthy" from one already-captured `lc_out` read, with zero new external calls, matching PO's generic "exists != works" framing.
**why-change:** No change from plan — PO's mechanism_verified_by_po pointed straight at the discarded status column; live-verified against real `launchctl list` output first (confirmed tab-delimited PID/Status/Label header) before writing the awk field-match, so the fix targets the real format not an assumed one.
**live-verify:** Post-fix live probe run flipped ALL_GREEN→FAILURE (exit 1): 3 of 4 repo-tracked non-obsolete LaunchAgents currently show nonzero last-exit status — `com.vn-market.fleet-push(exit-status:78)` (the PO-confirmed EX_CONFIG case), plus `com.vn-market.docker-events(exit-status:1)` and `com.vn-market.cowork-guaranteed-slot-firer(exit-status:143)`, newly surfaced by this same fix (not investigated/remediated — out of this task's scope, reported not papered over per instruction). `com.vn-market.socat-bridge` correctly absent from the failure list (obsolete allow-list).

### STEP developer-S3 · developer · 2026-07-23T12:30:53Z
**task-id:** FIX-DEVTEAM-BOUNDED1-PROSE-SEQUENCING-UNBACKED-GATE
**what-done:** Added `has_unbacked_sequencing_prose($detail_items)` to `scripts/lib/devteam-eligibility.jq` (board-OR-detail `po_sequencing_*` key present AND `effective_depends_on` empty) as a new conjunct in `is_bounded1_eligible` only; extended `bounded1-supervised-lane-report.sh` with a non-gating TERTIARY section listing every such row; added `scripts/audits/devteam-bounded1-prose-sequencing-gate-verify.sh`.
**what-considered:**
- Regex-mine the prose to extract a predecessor task-id and auto-write `depends_on` — explicitly forbidden by spec (brittle, defeats the shared-library's own fragility-avoidance principle); rejected.
- Wire the new conjunct into SLS/RLC too (they don't call `is_bounded1_eligible`, they compose their own subset) — rejected: root-cause incident was BOUNDED-1-specific (UC-CDC-P5 mis-promote); def lives in the shared file so SLS/RLC CAN adopt it later without recopying, but only BOUNDED-1 actually gates on it now (scope discipline).
**why-decision:** Conservative-skip (withhold, surface via report) is exactly what SPIKE-BOUNDED1-ELIGIBILITY-CONTRACT-REVIEW's design principle demands — one shared def, no forked/regex logic, gate lifts the instant PO encodes the dep.
**why-change:** No change from plan. Live-verified: UC-CDC-P5 (already hand-fixed with `depends_on` 2026-07-22) now evaluates `has_unbacked_sequencing_prose=false, is_bounded1_eligible=false` (still correctly held by pre-existing `deps_satisfied`, not double-gated). Pre-existing sibling verifier's CONTROL assertion fails identically with/without this change (confirmed via `git stash`) — harness bug in `make_isolated_fixture()` not clearing `.task_board.ready[]`, unrelated to this fix, reported not fixed (out of scope).

### STEP developer-S4 · developer · 2026-07-23T18:24:35Z
**task-id:** FIX-DRAINPRUNE-SKIP-LIVE-REFERENCED-PROCESSED-FILES
**what-done:** Confirmed the bug live at source (prune loop in scripts/agents-flow/drain-signals.js checked ONLY age, never references) — NOT already fixed. Added a referenced-file guard: gather every live task_board.*[].detail_ref (flat lanes + active_sprints/closed_sprints .tasks[]) + signal_queue.rows[].payload_ref from orch-state.json, basename-resolved; SKIP deletion (stderr skip note) if the >7d candidate's basename is in that set. Edited spec §0a-2 first (docs/agents/dev-team/flow/drain-signals.md), then the script, per its own "spec-first" note.
**what-considered:**
- Mirror Stage 1c's checkRefIntegrity() walk (orchStateSchema.ts) exactly vs a new ad-hoc lane list — chose mirroring so the guard's "referenced" definition can never drift from what actually blocks orch-apply.
- Full repo-relative path vs basename-only comparison — chose basename: robust to ROOT/SIG test overrides; live signal filenames are fingerprint/timestamp-unique so cross-directory basename collision is not a real risk here.
- Repoint the dangling ref at prune time (mirror FIX-DRAIN-PAYLOADREF-DANGLE-ON-MOVE) vs SKIP-only — task explicitly forbids restore-then-reprune; SKIP-only matches the stated fix shape, keeps the change additive.
**why-decision:** One fs.readFileSync + JSON.parse per run (no jq/execFileSync) is the smallest correct guard that closes the exact live incident (drain commit 395e224ad pruning a detail_ref'd file) without touching MOVE-time repoint or the existing age/legacy-mtime cutoff logic.
**why-change:** No change from plan — FIX SHAPE in the task matched implementation directly. Added a 4th test case beyond the 2 requested ACs (no orch-state.json present → guard degrades to age-only prune, fails open not closed) to prove the degradation path live, not just the happy path. 36/36 assertions pass (`node scripts/agents-flow/drain-signals.test.js`).

### STEP developer-S5 · developer · 2026-07-24T15:32:35Z
**task-id:** FACTORY-NEWS-dedup-handlers-maxitems
**what-done:** Added `DEFAULT_MAX_ITEMS`/`resolveMaxItems` to `domain/models.ts` (RED test first in `domain-models.test.ts`, 22/22 pass after); replaced the 4 near-identical POST/GET headline handlers in `interface/handlers.ts` with a `makeHeadlinesHandler(ingest, source)` factory returning `{post, get}`, registered on the same 4 routes. `handlers.ts` 171L→118L.
**what-considered:**
- Only path considered for `resolveMaxItems` signature: accept `number | string | undefined` so ONE function serves both the JSON-body channel (POST, no NaN guard — preserves `typeof x === 'number'` pass-through incl. NaN) and querystring channel (GET, `parseInt`+`isNaN` guard) without collapsing their genuinely different legacy semantics.
- Scope: task FILES list names only handlers.ts + domain/models.ts; left the separately-duplicated 15/10 defaults in `application/use-cases.ts` + 4 scraper files untouched — different call chain, not part of the 4-handler dedup, out of this task's explicit scope.
**why-decision:** Factory has 2 real call sites (Reuters/Bloomberg) so it clears simplicity-gate Q2; response envelope + error branch byte-diffed identical to all 4 originals (verified via `git diff`, not asserted).
**why-change:** No change from plan.
**verify:** `bun test` 241 pass/6 skip/0 fail (baseline 235/6/0, +6 net new = the added DEFAULT_MAX_ITEMS/resolveMaxItems tests). `bun tsc --noEmit` 0 errors. `eslint lint:ci --max-warnings 0` clean. `bun run sandbox --tier=all --module=news-fetch` 16/16 PASS. `grep -inE "DB_|API_KEY|SECRET|TOKEN|PASSWORD|NEWS_API_KEY"` on both changed source files → no matches.

### STEP developer-S6 · developer · 2026-07-24T15:56:54Z
**task-id:** FACTORY-NEWS-go-server-tier-split
**what-done:** Created `apps/news-fetch/internal/httpapi/{router.go,handlers.go}` (`Router(fetchers, s, logger, port)`, moved `handleHealth`/`handleRSSFetch`/`handleFetchAll`/`fetchResult`/`writeJSON` verbatim); `cmd/server/main.go` 254L→138L, now a thin composition root (env reads, `store.Open`, fetcher construction, `Router(...)`, graceful shutdown only).
**what-considered:**
- `Router` signature: added a `port string` 4th arg (task's shorthand `Router(fetchers, store, logger)` didn't account for it) since `/health` now derives its port from this, replacing the old hardcoded `5008` literal.
- Health `port` JSON field type: kept `int` (`strconv.Atoi`) not `string` — `apps/news-fetch/api/openapi.yaml` `/health` schema requires `port: integer`; a string type would silently break the documented contract.
**why-decision:** New `envInt("RSS_MAX_ITEMS", 20)` in main.go replaces both `NewVnEconomyFetcher(nil,20)`/`NewVnExpressFetcher(nil,20)` bare-literal call sites — smallest change removing both named hardcodes without touching `fetcher.go`'s own internal `<=0`→20 default guard.
**why-change:** No change from plan.
**verify:** `go build ./...` / `go vet ./...` clean, `gofmt -l` clean on all 3 touched files, `go test ./...` 2/2 packages pass (fetcher, store; httpapi has no dedicated test file — handlers are pure relocations). RAW-verify: ran the built binary locally (`PORT=15008 RSS_MAX_ITEMS=7`) — `/health` returned `{"port":15008}` (not 5008, proving the derivation), `/vneconomy/fetch`+`/fetch/all` hit live vneconomy.vn/vnexpress.net RSS and persisted 13/7 real rows into `rag_analyses` (confirmed via `sqlite3` query, `source_type='vneconomy'` row inspected), `/newsapi`+`/vps` returned deterministic zero-insert shape (no key/host) — response shape identical to pre-refactor. grep confirms 0 bare `20` (only `const defaultRSSMaxItems = 20`) and 0 literal `5008` in any JSON-building path (only remaining occurrence is the legitimate `envStr("PORT","5008")` fallback default).

### STEP developer-S7 · developer · 2026-07-24T23:09:42Z
**task-id:** FIX-DOWJONES-STALE-WRONG-VALUE
**what-done:** Verified root cause LIVE via `docker exec` into the running mcp-server container's named-volume DB (not host file — `feedback_live_db_is_named_volume_not_host_data`): dow_jones rows in `tracked_indicators` were news-mined garbage (10604/23750/23807/48221/76848, last write 2026-04-13, floor-only gate), and `get_system_status`'s "Auto-tracked Indicators" section ran its own unguarded query serving `dow_jones 23750` as current (report 3237) — a SEPARATE bug from the already-fixed DSI-MACRO-PHANTOM-STALE-GUARD (that guard only covers `buildMacroSection`). Fixed 3 layers: shared `indicatorPlausibility.ts` gate (fail-closed), retired dow_jones news-mining regex + added Yahoo `^DJI` live mirror (`yahooFinance.ts`, precedent = brent's backlog-921 removal), switched `get_system_status` to the already-proven `listTrackedIndicatorsFromDb()` staleness-aware read (generic — fixes every indicator, not just dow_jones).
**what-considered:**
- Full 12-symbol CommoditySnapshot pipeline (schema migration across ~8 test files) vs a lightweight standalone fetch+store pair writing only to tracked_indicators.
- One-off `if (indicator==='dow_jones')` band check vs a shared generic gate module reused by both writers (extraction regex + yahoo mirror) — mandate explicitly required "generic, not one-off".
**why-decision:** Lightweight pair chosen — minimal blast radius, matches computeMarketEarningYield.ts's existing "fetch + write straight to tracked_indicators" precedent; commodity_prices schema untouched. Shared gate module chosen for the generic mandate + reuse across both writers.
**why-change:** Simplicity-gate Q1 caught scope creep — trimmed 3 speculative ceiling additions (sp500/nasdaq/vnindex) not required by this AC; reverted to byte-identical prior bounds for those.
**verify:** New FIX-DOWJONES-STALE-WRONG-VALUE.test.ts 15/15 pass (band accept/reject incl. literal 23750/10604/76848, news-mining retirement, live ^DJI parse, fail-closed store + dedup, [STALE] tag presence/absence). Extended 1920c job test +3 (TC-8..10). Touched-file targeted suite 68/68 pass. `bun tsc --noEmit` clean. Full `bun test` suite kicked off as background verification (large repo, still running at journal time) — no regression signal from any touched-area subset run so far.

### STEP developer-S8 · developer · 2026-07-25T02:05:00Z
**task-id:** FIX-FB-GATE-POINT-PCT-MATH
**what-done:** Added Check-H to `scripts/fb-data-integrity-gate.sh` — blocks when |stated point delta − pct×prev_close| exceeds `POINT_PCT_MATH_TOLERANCE` (new header const, 1.0 pt); prev_close derived from live snapshot (`close/(1+changePct/100)`), never hardcoded. Generic via `INDEX_ALIASES` (VN-Index/VN30/HNX-Index/UPCOM). Also widened `FETCH_TICKERS` to always include the 4 standard index tickers.
**what-considered:**
- Letter for the new check: ticket title said "Check-F", but F (currency-unit) and G (structural) were already shipped after this ticket was minted 06-20 — used Check-H, the next unclaimed letter, documented the collision inline.
- prev_close source: batch API has no `prevClose` field; derived from already-fetched `close`+`changePct` rather than adding a new endpoint/field — matches task's "live snapshot" instruction with zero new surface area.
**why-decision:** Deriving prev_close from existing fetched fields keeps the check root-cause-fixed with no schema change; matching the existing Check-E/F/G python-heredoc idiom keeps it consistent, no new abstraction.
**why-change:** Letter F→H rename only; scope/logic unchanged from ticket.
**verify:** RAW fence — fixture `'−0,32 điểm (−0,32%)'` @ prev_close=1800 → `[BLOCK] Check-H ... delta=5.44 > 1.0pt tolerance`, exit 1. Fixture `'−5,9 điểm (−0,32%)'` same prev_close → `[PASS] fb-data-integrity-gate: 0 violations`, exit 0. Confirmed RED baseline: `git stash`-ed pre-fix script on the BLOCK fixture returned false PASS (proves the bug). Regression: real `docs/social/fb-post-2026-07-24.md` (VN-Index −13 điểm/−0,78%) with a matching live snapshot → 0 violations (no false positive); same file with live API unreachable → graceful skip, 0 violations. `bash -n` + `shellcheck` clean (1 pre-existing unrelated SC2034 on untouched `POST_DATE`). Simplicity gate: Q1–Q4 all NO.

### STEP developer-S9 · developer · 2026-07-25T02:05:56Z
**task-id:** FIX-FB-GATE-STALE-MACRO-GUARD
**what-done:** Added Check-I to `scripts/fb-data-integrity-gate.sh` (macro/FX stale-as-current + unverified-comparison guard, generic across usdVnd/gold/brent/fedRate/bondYield) + paired flow change in `docs/agents/fb-market-poster/flow/main.md` STEP 1b/4b: poster builds `$macro_provenance` (per-class `fetched_this_cycle` marker reusing the existing is_estimate discipline) after `get_macro_snapshot`, writes it to a temp file, gate reads it as new arg $4. Absent file = graceful skip (mirrors Check-B/D no-live-data posture); present file = any figure with no matching `fetched_this_cycle=true` entry (within class tolerance) blocks — I1 label if no entry at all, I2 label if a comparison phrase ("vẫn ở mức"/"từ mức"/"vượt ngưỡng") sits near a mismatched figure.
**what-considered:**
- Inline marker embedded in the public post text vs a JSON sidecar file (mirrors the existing `SNAPSHOT_FILE` $3 arg pattern). Sidecar chosen — an inline marker would either pollute the published FB post or need stripping before write, adding a new failure surface; the sidecar reuses proven architecture.
- Value-comparison via tolerance (delta vs live figure) vs a hard boolean "was this exact string type into the post via a live call" flag. Tolerance chosen — matches Check-B/D/H precedent and correctly distinguishes a live-fetched flat/unchanged rate ("đi ngang quanh X") from a stale carried number, without requiring exact-string round-trip tracking.
- Letter: ticket said "Check-G" but A-H already claimed (same pattern as prior Check-F→H mint) — used Check-I, documented inline per the ticket's collision-warning instruction.
**why-decision:** Provenance-gated design lets item-1 (stale-as-current) and item-2 (unverified comparison) share ONE mechanism — a comparison-phrase number that lacks live provenance is, by construction, always unprovenanced (no tool in this system fetches "yesterday's threshold"), so no separate detection path was needed; kept the changeset smaller.
**why-change:** Original single `.search()`-per-keyword-window design missed the classic multi-figure sentence ("tỷ giá ... 26.127, vượt ngưỡng 25.500" — both numbers in one sentence). Rewrote to `finditer` all figures per class with keyword-proximity gating instead of first-match-only, confirmed against the real corpus sentence.
**verify:** RAW fence — unprovenanced USD/VND `26.150` (prov file present, entry `fetched_this_cycle:false`) → `[BLOCK] Check-I1 stale-as-current ...`, exit 1. Honest-gap phrasing ("... công cụ chưa trả được") → `[PASS] fb-data-integrity-gate: 0 violations`, exit 0. `26.150` with prov entry `fetched_this_cycle:true, value:26150` → `[PASS] 0 violations`, exit 0. `"vẫn ở mức 25.500"` vs live prov `26150` → `[BLOCK] Check-I2 unverified-comparison-level ... delta=650.00 > 60.0 tolerance`, exit 1. RED baseline: `git show HEAD:scripts/fb-data-integrity-gate.sh` (pre-fix) run against both BLOCK fixtures → `[PASS] 0 violations` both times (proves the gap existed). Real-corpus multi-figure sentence (from 2026-06-08 post text, "26.127 ... vượt ngưỡng 25.500") with prov matching only 26.127 → correctly PASSes the live figure and BLOCKs only the 25.500 comparison. Regression: all 48 real `docs/social/fb-post-*.md`, no provenance file supplied → 0/48 Check-I false-fires (graceful skip); real `docs/social/fb-post-2026-06-03.md` with a MATCHING provenance file (usdVnd/gold/brent) → 0 violations end-to-end. `bash -n` clean. Simplicity gate: Q1–Q4 all NO (data-driven MACRO_CLASSES table mirrors existing Check-E/F/G/H idiom, no single-use abstractions).

### STEP developer-S10 · developer · 2026-07-25T02:26:06Z
**task-id:** FIX-FB-GATE-BREADTH-PCT-INTERNAL
**what-done:** Added Check-J to `scripts/fb-data-integrity-gate.sh` — a stated advancer/decliner breadth % must match counts cited elsewhere in the SAME post within `BREADTH_PCT_TOLERANCE` (new header const, 6.0pp). Stated-% extraction: (1) headline — first `N%` within a tight 30-char window AFTER the "độ rộng"/"breadth" keyword, same sentence only (prevents grabbing an unrelated later % — thanh khoản/ticker move — in the same sentence); (2) labelled — `N% mã tăng/giảm` either word order. Cited-counts extraction, label-agnostic (covers advancer/decliner/floor/ceiling generically without one regex per label): (a) explicit fraction `N (label)? của/trên/trong (tổng)? (khoảng|~)? M` (e.g. "81 của ~350"); (b) co-occurring `N mã tăng ... M mã giảm [... K mã đứng giá]` in one sentence, both advancer-view and decliner-view (and flat-view) are candidates. BLOCKs when the stated % is farther than tolerance from EVERY direction-matching candidate.
**what-considered:**
- Letter: ticket title said "Check-H" but that is STALE — A-I already claimed (Check-H = point-pct-math S8, Check-I = stale-macro S9, both this same catchup slot). Enumerated live via `grep -oE 'Check-[A-Z]' scripts/fb-data-integrity-gate.sh | sort -u` before writing (mandatory per task instruction) → used Check-J, the next unclaimed letter.
- Headline-% window size: first tried 80 chars (matching Check-I's macro proximity gate) — regression on the real corpus produced 4 false-fires: "độ rộng ... (128 tăng/180 giảm), thanh khoản giảm 22,4%" grabbed the unrelated thanh-khoản %; "độ rộng ... (VHM −1,14%..." grabbed a ticker's own move %; a reversed "mã tăng ... 63%" label-pattern grabbed a % that actually belonged to a later "mã giảm" clause. Fixed by (1) tightening the headline window to 30 chars — measured actual char-offsets of all 4 false triggers (42/55/47 chars) vs the synthetic true positive (13 chars), 30 cleanly separates them — and (2) tightening the reversed label-before pattern to require near-immediate adjacency (`mã tăng/giảm (:|là|ở mức)? N%`, no free-text gap) instead of a 15-char lookahead.
- Denominator for labelled counts without an explicit total: used tăng+giảm[+đứng giá] (whatever is present in the SAME co-occurring sentence) rather than requiring a fourth "tổng" citation — matches how real posts actually state breadth (no post in the 48-file corpus states an explicit grand total alongside tăng/giảm counts).
**why-decision:** Sentence-scoped + tight-window extraction was the only design that passed BOTH the literal ticket fixture (`độ rộng ~40%` + `81 của ~350` → BLOCK) and 0/48 real-corpus false-fires — a looser window (matching Check-I's 150-char proximity gate) is correct for macro figures (single-topic sentences) but wrong here because VN breadth sentences routinely chain 2-3 unrelated % figures (index move, breadth, thanh khoản) in one clause.
**why-change:** No change from plan; the window-size tightening above was iteration within the same implementation (caught during my own regression pass, before ever touching the real production file — prototyped standalone in scratchpad first).
**verify:** RAW fence — RED baseline: `git show HEAD:scripts/fb-data-integrity-gate.sh` (pre-fix, commit 41975d433) run against a full-structure fixture (`Độ rộng thị trường ~40%. ... 81 của ~350 mã ...`) with a snapshot file matching the post's own VN-Index level/pct (isolates the test from unrelated Check-D noise) → `[PASS] fb-data-integrity-gate: 0 violations`, exit 0 (proves the gap existed — pre-fix script had no mechanism to catch this). Post-fix same fixture → `[BLOCK] Check-J breadth-pct-internal: stated 'độ rộng...40%' = 40.0% does not match cited counts (81 của ~350 = 23.14%), delta=16.86pp > 6.0pp tolerance — internal inconsistency (headline breadth % vs cited counts)` / `[BLOCK] fb-data-integrity-gate: 1 violation(s) — fix ALL before STEP 5 write`, exit 1. Matching fixture (`độ rộng ~23%` + same `81 của ~350`) → `[PASS] fb-data-integrity-gate: 0 violations`, exit 0. Regression: all 48 real `docs/social/fb-post-*.md` (no snapshot arg, live-independent check) → 0/48 Check-J false-fires, including the two closest real near-misses — 2026-06-19 ("chỉ 25% mã tăng trong khi 63% mã giảm" vs cited 81/203/66=350 → implied 23.14%/58.0%, deltas 1.86pp/5.00pp, both ≤6.0pp tolerance) and three posts that previously false-fired during prototyping (2026-06-22-daily, 2026-06-27, 2026-07-11) before the window/adjacency fix, now all 0. `bash -n scripts/fb-data-integrity-gate.sh` clean. Simplicity gate: Q1–Q4 all NO (mirrors the existing Check-E/F/G/H/I python-heredoc idiom; no new abstraction, single new config const).

### STEP developer-S11 · developer · 2026-07-25T03:09:57Z
**task-id:** FIX-FB-GATE-WEEKLY-FRAME-MODE
**what-done:** Root cause (lesson L5, 2026-06-21): the gate had exactly ONE implicit frame — "latest daily snapshot" — so a WEEKLY post's own "+1,84% w/w" claim was compared against that DAY's daily changePct (−0,32%), a frame mismatch, not a fabrication; Check-C's selloff-language cross-check had the same defect (whole-post scan, no period awareness), false-flagging a correct sentence about a PRIOR week's real crash. Mechanism: (1) added an explicit `--frame=daily|weekly|monthly` flag (parsed out of argv before positional-arg assignment, default=daily=byte-identical prior behavior); (2) `build_period_snapshot()` derives a period-close series by curling the SAME REST surface the daily path already uses (`GET ${MCP_BASE}/mcp/api/prices/history` — the REST mirror of `get_price_history`, confirmed via `apps/mcp-server/src/interface/mcp/routes/priceHistoryHandler.ts`) for each INDEX_TICKERS entry, groups the returned daily OHLCV rows by ISO week / calendar month, and diffs the latest two distinct periods' closes — no new fetch path invented, no separate weekly table fabricated; (3) $3 (existing snapshot-file arg) is honored as-is in ANY frame — same shape, so a caller can supply a pre-computed weekly-close quotes JSON for deterministic replay/testing, exactly like today's daily testing already does; (4) Check-A (daily ±7% price-limit) is now gated to `FRAME=="daily"` only — a per-session exchange rule never applies to a week's cumulative move; (5) Check-C is period-scoped: SELLOFF_LINES now searches only the text BEFORE the mandated "Phân tích" section label (docs/agents/fb-market-poster/flow/main.md STEP 5 section order: Tóm tắt nhanh → Phân tích → Dự đoán/Tổng kết) — falls back to the whole file if no label found (never more restrictive than pre-fix) — plus a defense-in-depth relative-past-marker exclusion ("tuần trước"/"tháng trước"/"kỳ trước"/"phiên trước"/"trước đó"/"trước sốc", generic phrasing, no hardcoded dates). Flow docs updated: `main.md` documents the frame contract (its own STEP 4b call stays `--frame=daily` default); `weekly-recap.md` STEP 3b and `weekly-prediction.md` STEP 4b replaced the manual "WEEKLY MODE OVERRIDE" procedure with a concrete `--frame=weekly` invocation.
**what-considered:**
- Ticket's files[] named `docs/agents/digest-predict/flow/weekly.md` as "the weekly post caller". Investigated: that file has ZERO reference to `fb-data-integrity-gate.sh` anywhere (`grep -rln fb-data-integrity-gate docs/agents/` returns only `fb-market-poster/flow/{main,weekly-recap}.md`) — it is digest-predict's Sunday MARKET-channel Telegram digest (gated by the UNRELATED claim-truth-gate skill), not an FB post. The REAL weekly-post callers that already contain the exact "manual router override" interim language the ticket describes are `fb-market-poster/flow/weekly-recap.md` STEP 3b and `weekly-prediction.md` STEP 4b (both titled "WEEKLY MODE OVERRIDE", both instructing a manual re-run/rephrase workaround). Fixed the REAL callers instead of fabricating a no-op/incorrect gate reference into an unrelated agent's flow — left `digest-predict/flow/weekly.md` untouched, flagged the discrepancy in the RETURN to the dispatcher for QA/PO visibility.
- Section-scoping Check-C to "before Phân tích" vs. a token-level "past-tense marker only" approach: the concrete L5 fixture has TWO false-positive lines — one in Phân tích (prior-week crash, has a "tuần trước" marker) and one in Kịch bản/Dự đoán (a conditional "Nếu... thì rủi ro bán tháo" scenario, no past-tense marker at all — a hypothetical, not a historical reference). A marker-only approach would have left the second false positive uncaught (would still block, failing the exit-0 requirement). Section-scoping (backed by the SAME mandated 3-section template order already enforced elsewhere in this codebase) cleanly covers both with one generic, non-date-hardcoded mechanism; kept the marker exclusion too as cheap defense-in-depth.
- Weekly-close derivation: batch endpoint has no period-aggregate field, so derived it locally from `get_price_history`'s daily OHLCV rather than requesting a new server-side "weekly" endpoint/table — zero new surface area, matches the standing "reuse the existing data-access idiom" instruction.
**why-decision:** Frame-flag + period-derived-snapshot substitution required the LEAST code churn because Check-B/D/H already consume `$SNAPSHOT_JSON` generically (keyed by ticker, shape-agnostic to daily-vs-period) — swapping the SOURCE of that variable let all three checks work correctly in weekly/monthly mode with zero changes to their own logic. Check-A needed an explicit frame guard because its semantics (per-session price limit) do NOT generalize across periods the way B/D/H's tolerance-comparison logic does.
**why-change:** No plan changes — the digest-predict/weekly.md file-list correction above was surfaced during implementation (Step 0 investigation), not a mid-task pivot.
**verify:** RAW fence — RED baseline (pre-fix `scripts/fb-data-integrity-gate.sh` via `git show HEAD:...`, real `docs/social/fb-post-2026-06-21.md` + a daily-snapshot fixture reproducing that day's actual live daily quote VNINDEX close=1824.53/changePct=−0.32, both values taken verbatim from the post's own Friday-19/6 line) → `[BLOCK] Check-C breadth-narrative: ... live VN-Index=-0.32% (< ±2%)` + `[BLOCK] Check-D2 VN-Index-pct: post=1.84% live=-0.32% delta=2.16pp > 0.5pp tolerance` (+ 10 pre-existing unrelated Check-G4 markdown-bold hits from that older post's formatting drift), exit 1 — reproduces L5 exactly (weekly claim compared against daily snapshot). Post-fix, on a template-clean reproduction (same post, `**` markdown-bold markers stripped only — zero numeric/factual changes, isolates the frame-mode fix from that unrelated pre-existing G4 drift): (a) `--frame=weekly` + a weekly-close snapshot fixture (VNINDEX close=1824.53/changePct=1.8352 — independently verified via a live `GET /mcp/api/prices/history?code=VNINDEX&days=60` query against the running mcp-server confirming 12/6 close=1791.65 and 19/6 close=1824.53, i.e. the fixture is real historical data, not invented) → `[PASS] fb-data-integrity-gate: 0 violations`, exit 0; (b) SAME body, `--frame=daily` + the daily-snapshot fixture → `[BLOCK] Check-D2 ...`, exit 1 (daily semantics unchanged — Check-C no longer contributes but D2 alone still blocks); (c) isolated minimal fixture containing ONLY the historical-crash sentence in a Phân tích section → pre-fix `[BLOCK] Check-C breadth-narrative`/exit 1, post-fix `[PASS] 0 violations`/exit 0; POSITIVE CONTROL — same mechanism with the selloff sentence moved into Tóm tắt nhanh (genuinely current-period) → still `[BLOCK] Check-C breadth-narrative`/exit 1 post-fix (proves the fix is scoped, not a blanket disable). Live end-to-end (no snapshot file, forces the real `build_period_snapshot()` curl path against the running server, `--frame=weekly`): a synthetic post citing today's REAL weekly VN-Index figures (close=1686.11, −5,67% derived from real week-29/week-30 closes 1787.45→1686.11, cross-verified by an independent manual ISO-week grouping of the same `/mcp/api/prices/history` payload) → `[PASS] 0 violations`/exit 0; same post with a wrong figure (+1,20%) → `[BLOCK] Check-D2 ...` + `[BLOCK] Check-H point-pct-math ...`/exit 1. Monthly-frame smoke (`build_period_snapshot 70 month`, live) → correctly derives VNINDEX/VN30/HNXINDEX month-over-month %, gracefully skips UPCOMINDEX (insufficient distinct months). Regression: all 48 real `docs/social/fb-post-*.md`, default daily mode, SAME cached live snapshot reused for both pre-fix and post-fix runs (apples-to-apples) → 0 files with MORE violations post-fix; 7 files show exactly −1 violation each (269 vs 276 total), and a per-file diff of `[BLOCK]` lines confirms the ONLY line removed in all 7 is the Check-C breadth-narrative line — no other check's output changed in any of the 48 files. `bash -n scripts/fb-data-integrity-gate.sh` clean. `grep -oE 'Check-[A-Z]' scripts/fb-data-integrity-gate.sh | sort -u` → A..J only, no Check-K minted (extended A + C per the ticket's stated preference). Simplicity gate: Q1–Q4 all NO (frame flag reuses existing arg-parsing idiom; period derivation reuses the existing REST+curl+python3 idiom; no new abstraction layer).

### STEP developer-S12 · developer · 2026-07-25T15:24:33Z
**task-id:** FIX-COLDEVICT-DONE-LANE-TRIGGER-ACTION-AXIS-NOOP
**what-done:** Fixed all 3 coupled defects: (C) jq `[.arr // [] | .[].id // ""]` returned `[""]` on empty arrays (moved `[]` outside `// []`); (A) `done[]` STRING sort on `created_at` let `"unknown"` outrank ISO dates (switched to `sort_by(try fromdateiso8601 catch 0)`, epoch-0=oldest, matches the age-gate's own convention); (B) Step 5.5's count-only trigger was on a different axis than the script's count-AND-age action — replaced with `_step55_would_evict`, which asks `orch-cold-evict.sh --dry-run` itself and parses its `Byte reduction:` line, making trigger==action by construction and restoring the signal_queue disjunct the count proxy never had.
**what-considered:**
- Trigger fix: re-derive the exact rank+age predicate in bash (duplicate logic, drift risk) vs ask the real script via `--dry-run` (single source of truth) — chose the latter, cost is 1 extra local jq pass/tick vs the ~5x costlier full write pipeline it now gates.
- AC(2) malformed-row remediation: fabricate a plausible `created_at` (rejected — no-fake-data standing rule, no real creation-date source exists for these rows) vs evict via existing `KEEP_RECENT_DONE` tunable — chose eviction, no new code, and the 6 genuinely-recent rows serve as the AC(4) negative control for free.
**why-decision:** Same-computation trigger cannot re-drift from its action; eviction is the only remediation that doesn't fabricate data.
**why-change:** No change from plan.
**verify:** RAW fence — new `scripts/test/orch-cold-evict-tests.sh` TEST 7 RED (pre-fix: poison "unknown" NOT evicted, real DONE-RECENT row left alongside it; dry-run misreported 1/1 for genuinely-empty done_verified[]/archive[]) → GREEN post-fix (27/35, unrelated 8 fails are pre-existing SHG-5 lane-coherence gaps in the D4 fixture, unchanged by this diff, confirmed via `git log` predates this task). `dev-team-tick-preflight.test.sh` extended T21/T23/T29-31 (STUB_WOULD_EVICT) + new T33/T34 (stubbed axis-independence: high done[] count + stub=false → NOT invoked; done[] absent + stub=true → invoked) + T35 (REAL unstubbed `_step55_would_evict_REAL` against 2 pretty-printed fixtures, both directions: 0 evictable→false, 1 done_verified[] row→true) — 98/98 PASS. `orch-apply-wrapper-tests.sh` 42/42 unchanged. Live board: fixed sort auto-evicted 6/10 malformed rows via a concurrent dev-team tick reading the on-disk fix mid-session (commit 5100e9d63); remaining 4 cleared via one-time `KEEP_RECENT_DONE=6` live run (matches the 6 real-dated rows exactly) — `done[]` 10→6, 0 "unknown" remain, 6 negative-control rows byte-identical before/after. Live `--dry-run` steady-state now reports `Byte reduction: 0 bytes` (was misreporting phantom evictions pre-fix).

### STEP developer-S13 · developer · 2026-07-25T17:15:35Z
**task-id:** FIX-COVERAGE-SWEEP-BLANKET-STAMP-DEAD-TRIGGER
**what-done:** Built `scripts/agents-flow/coverage-stamp.sh` — a deterministic surgical jq patch (news-scout/market-watcher's LLM-prose "for each ticker analyzed, set X=now" was the root cause: re-executed against a 57-entry blob it non-deterministically blanket-stamped ALL tickers, permanently unsatisfying the 48h staleness trigger). Script touches ONLY named tickers' `last_covered_<agent>` field (merge, never full-doc regenerate), repairs the deleted top-level `sweep_config` key if absent, and wraps the read-modify-write in a `task_claim("coverage-state:main", kind=sprint-task, ttl=30s)` mutex (SF-1-style self-hold reentrant, fail-CLOSED on peer contention after 5 backoff retries). Also owns `--list-stale` (STALE_TICKERS filter+sort+slice using `sweep_config.{max_staleness_hours,sweep_batch_size}`, defaults 48/3) — the open question the row left me: script owns BOTH read+write, not write-only (see why-decision). Rewired all 4 read/write contract sites (news-scout stage-sentiment.md Step 0-sweep, stage-log-notify.md Step 7; market-watcher cycle.md Step 0-sweep + Step 5c) to call the script instead of hand-computed prose.
**what-considered:**
- BLOCKING DISCOVERY not in po's spec: news-scout/market-watcher's frontmatter (`.claude/agents/{news-scout,market-watcher}.md:5`) grants ONLY `Read, Write, Edit, mcp__gateway__call_tool` — no Bash. Neither agent can invoke a shell script directly; this is deliberate fleet-wide policy shared by every cowork gatherer/publisher agent (alert-commander, bctc-analyst, digest-predict, unified-agent, tran-ngoc-bau — same restricted set), not an oversight.
- 3 options: (a) grant Bash to these 2 agents (governance/blast-radius change, not mine to unilaterally decide), (b) wrap the script's logic in a new MCP tool (dev-mcp-server zone — must dispatch, not implement in this zone-clean session), (c) dispatcher-side wrapper that runs the script on the leaf agent's behalf pre/post-spawn (requires restructuring the bare `Launch subagent... execute flow/main.md` cron prompts to capture+relay subagent output — materially bigger than "1 script + 2 flow-doc edits"). Per the row's own instruction ("bounce with contradicting evidence rather than re-scope in place"), did NOT unilaterally pick (a)/(b)/(c) — shipped the script+tests (zone-correct, fully TDD'd, valid regardless of eventual transport) and flagged the gap loudly inline in all 4 edited flow-doc sites + board row + RETURN, rather than silently deciding or silently shipping doc prose the agent cannot execute.
- STALE_TICKERS ownership (the row's one genuinely open question): script owns it too, not just the write. Reason: AC 2 ("seed a stale ticker, run a cycle, assert force-included") has no automatable meaning if the filter stays LLM-prose-only — there is no code to invoke in a regression test. Making the script the read-side SSOT is what turns AC 2 into a real, repeatable test (T3/T4 in coverage-stamp.test.sh) instead of a spec re-verified by hand every time.
**why-decision:** Deterministic code (not LLM Edit-per-ticker) is what "surgical scripted patch" in the row means; Edit-tool-per-ticker was considered as a within-current-tool-grants fallback but rejected — it's still LLM-driven prose construction of old_string/new_string, doesn't remove the non-determinism class, just narrows blast radius; didn't want to ship something weaker than the specified direction and call it done.
**why-change:** No change to the WRITE-MECHANISM direction (read-modify-merge behind a mutex, exactly candidate (i) from the co-shipped lost-update row) — that part is fully built and tested. The transport (who invokes it) is a genuinely new open question po's RAW verification didn't check (it verified the DATA, not the CALLING AGENT's tool grant); flagged, not silently resolved.
**verify:** RED — `bash scripts/agents-flow/coverage-stamp.test.sh` before `coverage-stamp.sh` existed → hard fail (`ERROR: coverage-stamp.sh not found`), exit 1. GREEN — after implementation, 29/29 assertions pass in ~1.5s (T1/T2 positive: N<57 stamp produces >1 distinct-stamp group, untouched tickers keep OLD stamp byte-identical; T3/T4 negative/biconditional: >48h ticker + never-covered(null) ticker both surfaced by `--list-stale`, fresh ticker excluded, batch-size respected, null sorts oldest, missing-file fail-silent; T5/T6 preservation: sweep_config repaired from absent, existing sweep_config never clobbered, ALL pre-existing top-level keys survive, peer agent's `last_covered_*` field on a touched ticker is byte-identical after write — lost-update row's AC1/3; T7/T8 mutex: peer-held → fail-closed, file untouched, non-zero exit; self-held → heartbeat + proceeds; T9/T10: empty `--tickers` = config-repair-only (zero ticker mutation); unknown ticker created cleanly; T11/T12: invalid `--agent`/missing file rejected BEFORE any mutex call). Did NOT touch the LIVE `docs/data/coverage-state.json` — found it already carries a substantial uncommitted peer diff (58 ins/58 del, news-scout's live c200 cycle @16:14:39Z, the exact blanket-stamp artifact po measured); running the sweep_config repair against it now would conflate my structural change with that unrelated in-flight write in one commit, so deliberately deferred — sweep_config self-heals on the FIRST real invocation once transport is resolved. Ticker-count finding (flagged per row instruction, not resolved): coverage-state.json has 57 tickers; system-map.json's watchlist has 34 (33 active) — comm-checked and ALL 34 are a strict SUBSET of the 57 (0 in system-map missing from coverage-state); market-watcher's cycle.md Step 0-sweep referenced an undefined `WATCHLIST` (no `get_watchlist` call anywhere in that agent's flow docs except eod.md) — added the missing call as part of rewriting that exact block (minimal, in-scope, not the news-scout/58 vs 34 discrepancy po flagged, which remains unreconciled).

### STEP developer-S15 · developer · 2026-07-28T18:48:45Z
**task-id:** FIX-DEPSSATISFIED-COLD-ARCHIVED-DEP-RESOLVES-MISSING
**what-done:** `dep_status_map` in `scripts/lib/devteam-eligibility.jq` split into `dep_status_map($archive)` (archive-seeded map, hot lanes overwrite — hot wins for any id present both places) + a 0-arg `dep_status_map: dep_status_map([])` proxy kept byte-identical for every pre-existing caller. New `archive_status_map($archive)`/`normalize_archive_status($s)` (case/separator-insensitive `DONE_VERIFIED` match only). New `scripts/lib/archive-glob-cat.sh` (portable find|sort|for-loop concat of `docs/data/orch/archive/YYYY-MM.json`, avoids an xargs-empty-input / zsh-word-split trap I hit and fixed). Threaded `--slurpfile archive <(bash scripts/lib/archive-glob-cat.sh)` into the 3 REAL callers (BOUNDED-1 promote, SLS promote, RLC claim — verified by exhaustive `-f <file>.jq` grep, not guessed) — main.md's 3 bash blocks, dev-standards.md's canonical example, and devteam-dispatch-gate-satisfiability.sh's 4 invocations. Eviction guard: `scripts/orch-cold-evict.sh` `compute_id_maps()` now `include`s the shared lib (`-L "$REPO_ROOT"`), slurps `backlog-detail.json` (always the real repo path, never `ARCHIVE_DIR`-overridden — read-only reference, not a cold-write target), computes `$referenced_dep_ids` (union of `effective_depends_on` across TASK_LANES live rows) and filters it out of all 3 terminal-candidate sets (done[]/done_verified[]/flat-lane) via variable-shadow, `_raw` siblings kept only for an observability count (`n_guard_ref_held`/`guard_ref_held_ids`).
**what-considered:**
- Signature change risk: `dep_status_map` had exactly 4 real call sites repo-wide (grep-verified, not assumed) — considered a hard required-arg everywhere vs jq arity-overloading (0-arg proxies to 1-arg). Chose overloading: zero blast radius on any caller I might have missed, single merge implementation (0-arg is a literal `[]` call, can never semantically diverge), matches jq's own idiom for default params. Cost: def ORDER matters (1-arg must be defined before the 0-arg proxy references it) — hit + documented this compile-order gotcha inline.
- Multi-file `--slurpfile archive` glob: naive `<(cat docs/data/orch/archive/2026-*.json)` silently gives an EMPTY archive under this session's actual shell (zsh, no word-split on `$(ls ...)` multi-line capture) with zero error — would have shipped a fix that looks green but does nothing. Verified empirically, fixed with `find | sort | for-loop` (no xargs empty-input ambiguity either).
- Archive precedence: cold-archive SEEDS the map, hot lanes OVERWRITE — hot authoritative for a still-live/reopened id, archive pure fallback for ids evicted out of hot entirely.
- Eviction guard scope: TASK_LANES (backlog/review/qa/in_progress/ready) as the "live" referencing set — reused the script's own existing config var rather than inventing a new one; done[]/done_verified[] excluded from REFERENCING (terminal rows do not need active dispatch) but fully covered as GUARD TARGETS.
**why-decision:** PO's deliverable named "fold into dep_status_map... no forked logic" — arity-overloading IS one def family (same merge body), satisfies that literally without the all-call-sites-must-update risk of a hard signature break.
**why-change:** No change from plan; the referential guard was always the PLUS half, not optional.
**verify:** Live-data instrument (extended `scripts/audits/devteam-dispatch-gate-satisfiability.sh`, not a new one, per the row's own instruction): AC-DEP-1 (live, dynamic) 0 leaks post-fix, INFO line shows 34 pre-fix (hot-only) — matches PO's own measurement. AC-DEP-MECH: same real BOUNDED-1 promote script, only archive content varied (empty vs real) — before=NOT promoted, after=promoted, grounded in a real live cold-archived id (dynamic, no hardcoded id). AC-DEP-NEG-A (nowhere dep) / AC-DEP-NEG-B (cold-archived non-terminal status) both stay UNSATISFIED. AC-EVICT-1/2/3: real `orch-cold-evict.sh` run against a scratch `ORCH_STATE`+`ARCHIVE_DIR` fixture — referenced row HELD in hot, unreferenced sibling row evicted normally (guard proven both directions, live end-to-end, never touches the real live file). All pre-existing assertions in this instrument + `devteam-bounded1-prose-sequencing-gate-verify.sh` + `devteam-bounded1-detail-disposition-gate-verify.sh` + `bounded1-supervised-lane-report.sh` + `devteam-review-lane-drain-report.sh` re-run GREEN after the plumbing change (2 needed an added `--slurpfile archive` to their own `run_promote_picked_id` helpers, since bounded1 promote now requires it unconditionally). Incidental, verified-pre-existing-and-unrelated fix: `devteam-bounded1-detail-disposition-gate-verify.sh`'s `make_isolated_fixture()` never cleared `.task_board.ready`, so its equality-checking "control" assertion silently read the LIVE board's stale `ready[0]` — reproduced the identical failure against the git-HEAD (pre-my-fix) `promote-bounded1.jq`+`devteam-eligibility.jq` before touching it, confirming zero causal link to this task; one-line fix (`.task_board.ready = []` alongside the existing `.task_board.in_progress = []`) turned several previously-vacuous inequality assertions in that file into genuinely-meaningful ones too. Zero TypeScript touched (scripts/*.jq, scripts/*.sh, docs/*.md only) — `bun tsc --noEmit`/`bun test` not meaningfully applicable, not run. Graphify incremental update skipped: no Skill tool in this session's grant, and the doc edits are minor factual corrections to existing mechanism prose, not new conceptual surface area.

### STEP developer-S16 · developer · 2026-07-28T20:49:00Z
**task-id:** TASK-COWORK-CATCHUP-2
**what-done:** Wired CATCHUP-1's `cowork-catchup-predicate.js` into `cowork-match-slots.js`'s CLI entrypoint (`require.main===module` block only) — additive conditional `require`, computes `catchup_raw` via `computeCatchupCandidates(sched, nowUnix, {excludeSlotIds: hits.map(slot_id)}, {field, dowMatch})`, adds it as a new top-level JSON stdout key. `matchSlots()` itself untouched.
**what-considered:**
- excludeSlotIds source: `hits` (matchSlots()'s final post-dedup live-match set) vs raw cron-matched `candidates` — chose `hits`, matching the predicate module's own JSDoc ("slots already live-matched this tick") and the architect brief §2.1's "NOT already in the live cronMatches() result this tick" language most literally as "the tick's actual live match".
- Router's spawn-level standing invariant asked for commit-mutex; this agent's actual tool grant (frontmatter + the literal function list handed to this session) is `Read/Edit/Write/Bash` only — no MCP tool present, matching INV-GATEWAY-1's own stated rationale for specialist agents. Did not fabricate an MCP call I structurally cannot make; committed directly with strict pathspec-scoping (`git add <exact paths>` then `git commit ... -- <same exact paths>`, verified `git diff --cached` empty before AND after each commit) — the actual TOCTOU-immunity mechanism per the skill's own Step 3c rationale, achievable without the lock itself.
**why-decision:** matchSlots() signature-unchanged is an explicit NFR-2/AC in both the handoff and TLDR — safest reading is CLI-block-only edit, zero risk to the 26-scenario existing test file or any of the 3 downstream callers not yet wired (CATCHUP-3+).
**why-change:** No change from plan.
**verify:** RED confirmed live — stashed only the source-file diff (`git stash push -- cowork-match-slots.js`), re-ran the new CLI-contract test blocks → hard crash (`TypeError: Cannot read properties of undefined (reading 'find')`, catchup_raw absent) — then `git stash pop` restored the fix → GREEN, 43/43 assertions (26 pre-existing + 4 new CLI-level blocks: field-presence/type, a real eligible candidate surfacing end-to-end through an isolated mkdtemp CLI harness with a controlled fixture — deterministic regardless of wall-clock via a `catchup_max_lateness_minutes:999999`/`:-1` construction, not flaky time-window coincidence — a real ineligible `freshness_window_exceeded` candidate, and the missing-predicate-module graceful-fallback path (`catchup_raw=[]`, stderr WARN, exit 0, never a crash)). Sibling `cowork-catchup-predicate.test.js` 34/34 unchanged (file untouched). `bun tsc --noEmit` (apps/mcp-server) 0 errors — plain `.js` files outside `tsconfig.json` include, same AC-10 posture as CATCHUP-1. Doc updated: `docs/agents/cowork-team/flow/match-slots.md` Step 2+3 schema description now documents the new `catchup_raw` field shape (consumption wiring explicitly deferred to a later sprint task per architecture brief §2.1/§2.3, not this task's AC). Two pathspec-scoped commits pushed (`c5e7c6747` code+test, `fd5d4565e` docs), pre-push `tsc` hook green both times, `git diff --cached --name-only` empty before/after both.

### STEP developer-S14 · developer · 2026-07-28T13:40:00Z
**task-id:** TASK-COWORK-CATCHUP-1
**what-done:** Created `scripts/agents-flow/cowork-catchup-predicate.js` (pure domain module, 3 exports: `computeCatchupCandidates`, `mostRecentCronFireBefore`, `toVnDateString`) + `cowork-catchup-predicate.test.js` (34 assertions, AC-1/AC-2/AC-3 + basis/edge coverage). Migrated `docs/data/cowork-schedule.json` additively via a one-off script (`scripts/dev-task-cowork-catchup-1-migrate-schedule.js`, JSON.parse→mutate→JSON.stringify(null,2)+atomic rename, mirrors `cowork-write-last-fired.js`'s convention for this same hot file): added top-level `_dish_type_catchup_config` + `publish_date_basis` on the 8 guaranteed slots.
**what-considered:**
- `ctx` (3rd positional param, ambiguous in the handoff signature vs. router paraphrase): designed as `{excludeSlotIds}` — a caller-supplied set of slot_ids already live cron-matched this tick, so a slot firing on-time is never double-counted as its own catch-up candidate. Reserved hook for FR-3 wiring (CATCHUP-2/3), not required by AC-1/2/3 but kept the seam open rather than omitting a 3rd param the handoff explicitly named.
- Schema edit mechanism: hand-crafted `jq`/`Edit` surgical multi-site inserts vs. a one-off Node migrate script mirroring `cowork-write-last-fired.js` — chose the script: this file has 2 confirmed prior production corruptions from hand-rolled jq/shell edits (per that script's own header), and last_fired values across 8 target slots are not all unique enough for safe multi-line `Edit` anchors.
- `publish_date_basis` values: re-grepped live flow gate code at implementation time (not trusted from the 07-22 brief verbatim) — confirmed unchanged: chef.md:88-92 (VN_DATE), fb-market-poster/main.md:88-92 + weekly-recap.md:39-40 (VN_DATE / PERIOD_SAT), digest-predict/main.md:20-40,93-98 (UTC_DATE daily / periodKey Sunday), tran-ngoc-bau/main.md:34-39 (periodKey).
**why-decision:** Dependency-injected `{field, dowMatch}` (not `require`'d from `cowork-match-slots.js`) keeps the domain→domain dependency edge one-directional per the architect brief; zero I/O imports satisfies dev-standards.md's DDD golden rule directly (this is a `scripts/agents-flow/` cross-service module, not under `apps/mcp-server/src/domain/`, but the same golden rule was applied by convention, matching `cadence-policy.js`'s sibling precedent).
**why-change:** No change from plan.
**verify:** RED confirmed (module temporarily renamed away → `MODULE_NOT_FOUND`, test hard-fails) before GREEN. GREEN: `node scripts/agents-flow/cowork-catchup-predicate.test.js` 34/34 PASS; pre-existing `node scripts/agents-flow/cowork-match-slots.test.js` 26/26 PASS unchanged (untouched file). Schema migration verified: `jq -e .` parses clean; `[.slots[]|{slot_id,last_fired}]` diffed byte-identical across all 23 slots before/after; exactly the 8 named guaranteed slots carry the correct `publish_date_basis`; `git diff` on the schedule file shows only expected additive lines (new keys + the trailing commas their insertion required) plus one pre-existing unrelated uncommitted `digest-daily.last_fired` bump that predates this task. `bun tsc --noEmit` (apps/mcp-server) 0 errors — this task's files are plain `.js`/`.json`, outside `tsconfig.json`'s `include`, so AC-10 is satisfied as "no regression" rather than "newly typechecked". Full `bun test` baseline: kicked off fresh before/after run (large suite, background) — this task touches zero files under `apps/mcp-server/src/`, so no delta is structurally possible; comparing against the same-day (2026-07-28) established baseline of 53 pre-existing fails (S-prior session, unrelated to this change).

### STEP developer-S17 · developer · 2026-07-28T23:59:00Z
**task-id:** FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-HOOK
**what-done:** Shipped `scripts/git-hooks/pre-commit` (+`post-commit` companion) implementing brief §4.1's `$GIT_INDEX_FILE` basename discriminator; extended `install.sh`; added permanent regression suite `pre-commit.test.sh` (6/6 PASS).
**what-considered:**
- Ambient P0, explicit supervised dispatch per architect's parent-epic ruling (not zone-detect Tier-3) — worked directly on `main` (no branch) per project CLAUDE.md override of the generic developer flow's branch-setup step.
- `git diff --cached` failure inside the BARE path: initially swallowed via `2>/dev/null || true` (silently treats as "0 staged"), tightened to explicit exit-code capture routed to the same fail-open+loud UNKNOWN path — AC-3 is stricter than "don't swallow the discriminator's own error," it also covers this downstream call.
- `git log --oneline | grep -q` in the T4 rebase test flaked under `set -o pipefail` (SIGPIPE on early-exit grep read as pipeline failure) — replaced with variable-capture + glob match, not a live pipe.
**why-decision:** WARN-by-default (never REJECT fleet-wide) per architect's ratified disposition — hard-coded, not left as a judgment call at implementation time, since the brief documents the fleet-outage risk of defaulting to REJECT before Layer-1 migration completes.
**why-change:** No change from plan — brief + handoff were fully specified; only refinements were the two AC-3 hardening fixes above.
**verify:** `scripts/audits/verify-commit-sweep-discriminator.sh` PASS before+after (git 2.49.0). `shellcheck` clean on all 4 files. Live verification gate reproduced in a disposable scratch clone (not the live repo): actor A stages, actor B bare-commits — WARN mode loudly flags + commit lands (documented default); `GIT_SWEEP_GUARD_MODE=reject` mode rejects (exit 1), index untouched, A's content recovered, B's pathspec-scoped retry lands cleanly. AC-6 (binds INV-GATEWAY-1-exempt population) confirmed structurally — hook fires on every `git commit` regardless of caller/MCP-binding.

### STEP developer-S21 · developer · 2026-07-29T00:57:14Z
**task-id:** FIX-COWORK-SPAWNFANOUT-FLOWPATH-BYPASSES-DIGEST-DAILY-DEDUP-GATE
**what-done:** `spawn-fanout.md` Step 5.2 now dispatches `slot.trigger_prompt` (composed `flow_path` form is fallback-only) + a fail-loud pre-spawn consistency check refusing any slot whose `trigger_prompt`/`flow_path` name different files; fixed `digest-daily.flow_path` in `cowork-schedule.json` to `main.md` (agrees with `trigger_prompt` now); new shared predicate `extractPromptFlowPath()`/`slotEntryPathsAgree()` in `cowork-match-slots.js`; new `cowork-schedule-consistency.test.js` (9/9, incl. the live-schedule static assertion).
**what-considered:**
- PREFERRED general fix (dispatch trigger_prompt + assertion) vs ACUTE mitigation (flow_path-only edit) — row explicitly named general as preferred; did both, since the assertion's AC ("digest-daily passes after") requires the two fields to agree, and the general fix alone (without the schedule edit) would leave `flow_path` permanently lying about the real entry point even though it's no longer the dispatched value.
- Where the extraction predicate lives: one exported JS function in `cowork-match-slots.js` (already the schedule-parsing SSOT module) referenced by BOTH the static test and the spawn-fanout.md prose, vs. inlining the regex twice — one function, per the row's explicit anti-duplicate-predicate instruction (though that instruction was about the task_claim gate specifically, applied the same discipline here).
- Confirmed the launchd Layer-B backstop (`cowork-guaranteed-slot-firer.sh`) was NEVER affected — grepped it directly, already reads `trigger_prompt` verbatim; bug was isolated to Step 5.2's Layer-A live-session path only.
**why-decision:** flow_path stays in the schema (not removed) because Step 5.2's pre-spawn file-existence check still needs a plain path field to `test -f`; trigger_prompt's multi-line refine-bctc-slot-* shape isn't suitable for that check directly.
**why-change:** No change from plan. Task instructions named the MCP commit-mutex protocol explicitly, but `.claude/agents/developer.md` frontmatter tools grant is literally `Read, Edit, Write, Glob, Grep, Bash` — no MCP tool present — a real structural constraint (not an excuse), independently re-confirmed here, matching the same finding QA already validated once for this exact agent in `TASK-COWORK-CATCHUP-2`. Committed directly instead: explicit pathspec `git add`/`git commit`, `git diff --cached --name-only` emptiness-check before AND after each commit, no mutex call.
**verify:** RED reproduced directly against `git show HEAD:docs/data/cowork-schedule.json` (real pre-fix content, not a synthetic fixture) piped into the working tree — `cowork-schedule-consistency.test.js` TC-7 FAILs exactly on `digest-daily` (8/9); restored fixed content → 9/9 GREEN. Sibling `cowork-match-slots.test.js` 43/43 and `cowork-catchup-predicate.test.js` 34/34 unaffected (purely additive exports). `bun tsc --noEmit` (apps/mcp-server) 0 errors; zero files under `apps/mcp-server/src/` touched so no `bun test` delta is structurally possible. `jq -e .` on `cowork-schedule.json` parses clean. Graphify incremental update (doc-review Step 4): checked precondition before running — `env` confirms zero LLM API keys (GEMINI/ANTHROPIC/OPENAI) in this session, the exact root cause 3 prior sessions (FACTORY-ALERT, FACTORY-FRONTEND, ULTRACODE-AUDIT-FIXALL) hit as a hard structural failure; Part B semantic extraction also mandates the Agent tool, unavailable to a spawned specialist. Skipped rather than burn a guaranteed-fail cycle — flagged in WORK.md, not silently omitted.

### STEP developer-S22 · developer · 2026-07-29T04:46:04Z
**task-id:** FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE
**what-done:** Rewrote `docs/agents/alert-commander/flow/stage-dispatch-log.md` § 5 from the perpetual `## This session` + `### Alert Cycle` sub-block shape to APPEND-class one `## c<NNN> · <ISO-timestamp>` section per cycle, routed through `.claude/skills/notebook-write/SKILL.md` (AC-2/AC-3/AC-4). Archived the pre-fix 198L/124374B blob verbatim → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`; reset the live notebook to a 7L/1099B preamble, 0 `## ` sections (AC-4 blank-state). Updated `docs/evals/alert-commander.eval.json` AC-11 (was asserting the pre-fix `### Alert Cycle` heading as the required contract — would have certified a regression back to the broken shape).
**what-considered:**
- Section physical order: literal skill AC-3 Step 1d (append-to-end, matches qa.md) vs news-scout's prepend/newest-first — kept the shared skill's literal default; `notebook-auto-prune.sh` (post `b42f3fa3a`, code-verified) now locates the oldest section by heading-timestamp parsing, not physical position, so either order prunes correctly and this avoids a second undocumented per-agent deviation.
- Content budget: template capped ≤700B/≤8L, full narration redirected to `log_agent_work`'s context field — historical entries averaged ~4145B/632B-per-line; 3 retained sections at that density (AC-2) would still blow the 12000B cap even with correct section boundaries, so structure alone was insufficient.
**why-decision:** `depends: [FIX-NOTEBOOK-PRUNER-LINE-ONLY-SETPOINT-BYTE-CAP-NEVER-CONVERGES]` cleared per PO ruling relayed in dispatch, cross-checked against that row's own board `note` (source-verified, not taken on faith): it explicitly states this file "cannot help... under EITHER axis" — 190L sits under the line cap so the pruner hook never inspects it, and a single `## ` section has nothing to drop even once byte-eligible. Non-causal by the dependency's own text.
**why-change:** No change from plan — task's stated 2-part remedy (structural first, one-off split second) executed in that order, per its explicit instruction not to stop at the split alone.
**verify:** `grep -c "^## "` on live notebook = 0 (clean blank-state); `diff` against archive copy = byte-identical (verbatim preservation). Simulated 3-cycle budget: preamble 1099B + 3×700B template ≈ 3199B / ~22L, both caps clear with wide margin (contrast news-scout's own real 3-section total, 11801B, near the 12000B edge). No live MCP tools in this session's frontmatter grant (`Read, Edit, Write, Glob, Grep, Bash` only) — cannot invoke a real alert-commander cycle to observe a live write; structural correctness verified by design/simulation and by direct inspection of `notebook-auto-prune.sh`'s shipped timestamp-parsing logic, not a live transcript. `python3 -c json.load` on the edited eval file confirms valid JSON post-edit.

### STEP developer-S23 · developer · 2026-07-29T07:32:19Z
**task-id:** FIX-AUDITOR-HEARTBEAT-OUT-OF-CONTRACT-AGENT-WRITE-TIER1
**what-done:** New `_check_auditor_heartbeat_shapes` in `scripts/git-hooks/pre-commit` (always-reject, runs before the sweep-guard mode dispatch, never `GIT_SWEEP_GUARD_MODE`-gated): rejects any tier-1 commit not shaped `{last_healthy_at, checks:{6 keys, all PASS}}` and any tier-2/3 commit carrying a `checks` key. New `CANONICAL:SSOT-AUDITOR-HEARTBEAT-SOLE-WRITER` in `dev-standards.md`, cited from both writers. Live file repaired by running the real probe script (genuine ALL_GREEN), not hand-edited.
**what-considered:**
- AC4 semantic divergence (tier1="healthy" vs tier2/3="completed" under one "healthy" filename family): re-gate tier2/3 on green — REJECTED, starves heartbeat on a persistently-tracked DEGRADED cycle, recreates the exact spawn-storm auditor-signal-loop-P1 closed. Rename files/field — REJECTED, breaks FIX-AUDITOR-TIER1-FRESHNESS-CHECK-RELOCATE-TO-SENTINEL's already-planned OH-3.5 reads of these exact paths (sibling row, sequenced with this one). Chosen: keep both filenames/fields, make shape (bare vs `checks{}`) the structural, enforced semantic signal — smallest blast radius, no live consumer breaks.
- Guard location: git pre-commit hook (catches ANY writer, not just the documented main.md snippet — root cause was an out-of-contract write with NO authorizing step in the flow at all, so a script-only guard on the documented path would not have caught it) vs. extending the flow doc's own bash block. Hook wins — same precedent as the sibling sweep-guard already in this exact file.
**why-decision:** Root-cause is "no authorizing step produced the write" (confirmed: main.md:750's own `if` already correctly excludes tier-1) — the only enforcement point that binds regardless of HOW a bad write happens is commit-time, not flow-prose.
**why-change:** No change from plan — matches the task's 6-point acceptance criteria as dispatched.
**verify:** New `scripts/git-hooks/pre-commit-auditor-heartbeat.test.sh` RED (3/6 fail pre-guard) → GREEN (6/6 post-guard). No regressions: `pre-commit.test.sh` 6/6, `auditor-tier1-probe.test.sh` 141/141, both byte-identical scripts otherwise. `bash -n` syntax-clean on the hook. Graphify: no Skill-tool path available to this spawned agent (same structural constraint as S21/S22) — flagged in WORK.md, not silently skipped.

### STEP developer-S24 · developer · 2026-07-29T08:27:40Z
**task-id:** FIX-VPS-SSC-INSIDER-502
**what-done:** Live-diagnosed the `/proxy/ssc-insider` 502 WITHOUT VPS SSH (scope guardance permitted handoff to ops-vps-fetch only if SSH was required; it wasn't). Direct HTTP reproduction: VPS proxy 502→"Upstream HTTP 503 from congbothongtin.ssc.gov.vn/...jspx" (3x consistent); direct sandbox curl to the SAME upstream URL → 503 "No server is available to handle this request" (identical, non-VN egress); domain root also 503; parent `ssc.gov.vn` healthy (302 nginx) but its WebCenter app tier (`/webcenter/portal/ubck`) times out entirely. Confirmed genuine external SSC outage — no code defect, no geo-block, no VPS misconfig. Added an in-code comment at `vps-scripts/vps-proxy-server.js`'s `SSC_INSIDER_UPSTREAM` (root-cause + explicit anti-retry rationale) and updated `docs/architecture-briefs/2026-07-01-BA-PREDICTION-EVIDENCE-REVIVAL.md` + `docs/WORK.md` with the closure. CLOSED-NO-FIX (precedent: `9e69d12bb`).
**what-considered:**
- Add retry/backoff to `fetchUpstream()` for 503 — REJECTED: git-verified this exact fix was already tried for this exact SSC domain on the sibling BCTC path (`FIX-BCTC-SSC-503-RETRY` 06-16) and reverted (`B-05-FU-SSC-503-RETRY`, commit `a817b5139`) because the mcp-server caller's timeout budget (here: `sscInsider.ts` `withDeadline(30_000)`) is far tighter than any useful backoff, causing a 17-day queue freeze on the sibling path. Same mismatch would reproduce here.
- Search for an alternate/moved SSC URL — checked `ssc.gov.vn` root (healthy) and `/webcenter/portal/ubck` (times out, same app tier as congbothongtin) — no working alternate found, confirms broad app-tier degradation not a single dead route.
- Document-only comment vs. no code touch at all — chose comment: zero behaviour delta, prevents a future agent burning a cycle re-discovering and re-reverting the same retry (institutional-knowledge gap that literally already cost 17 days once on the sibling path).
**why-decision:** Root cause is external and non-code-fixable; decoupling_reason ("external portal may be down") is now a confirmed fact, not a hypothesis. FR-2.2 watchdog (already shipped, decoupled task) is the correct existing safety net for staleness.
**why-change:** No change from plan — task's own scope guardance anticipated exactly this outcome.
**verify:** `node --check vps-scripts/vps-proxy-server.js` clean (comment-only change, no test harness exists for this standalone Node script per repo convention — verified via `git log -- vps-scripts/vps-proxy-server.js` + sibling test files, none exercise its runtime logic directly). No `bun test`/`tsc` delta possible — zero files under `apps/mcp-server/src/` touched. Committed directly to `main` (no task branch — matches `branch:null` direct-execute precedent for CLOSED-NO-FIX / investigation-only rows, confirmed via `git branch --contains 9e69d12bb` = main only). Graphify: no Skill-tool path available to this spawned agent (same structural constraint as S21-S23) — flagged in WORK.md, not silently skipped.

### STEP developer-S25 · developer · 2026-07-29T13:05:00Z
**task-id:** FIX-DEVTEAM-EPIC-WRAPPER-AUTOCLOSE-SWEEP
**what-done:** New `scripts/devteam-wrapper-autoclose.jq` (single-script sweep, no promote/claim split) + 4 shared predicates appended to `scripts/lib/devteam-eligibility.jq` (`all_children_terminal`/`is_terminal_task_status`/`normalize_task_status`/`has_hold_reason`): sweeps `ready[]`∪`in_progress[]` for `is_epic_wrapper` rows whose `children[]` are ALL terminal (hot lane OR cold-archived, case/separator-normalized against orchStateSchema.ts TERMINAL_SET) and not `hold_reason`-held, moves them to `review[]` (status/next_agent/`.head`(b)-conditional sync per CANONICAL:SSOT-STATUSFLIP-LANEMOVE). New Step 4.4 in `post-cycle.md` invokes it then dispatcher-wraps a per-row `task:<id>` spawn of the resolved `next_agent` (usually pm) — genuine auto-dispatch, not just a lane move.
**what-considered:**
- Placement: main.md Step 0b head-idle fall-through (audit brief dev-team-loop-P3's own suggestion, BOUNDED-1-style) vs post-cycle.md Step 4.x — REJECTED main.md placement per the router's explicit dispatch instructions (post-cycle.md § Step 4, matching its own established Step 4.2/4.3 script-first economy); adopted BOUNDED-1/SLS/RLC/QA-Drain's *documentation* style (WIP-discipline note, jq|orch-apply pairing, Reusable Scripts pointer, verifier) without literally reusing their `.head`-gated fall-through mechanics, which don't apply outside that chain.
- Dispatch mechanism for "auto-dispatch pm": signal_queue write (post-cycle's own Step 4.0.5/4.3 idiom for "flag another agent") vs direct per-row `task:<id>` dispatcher-wrap + `Agent()` spawn — REJECTED signal_queue: its `to` receiver vocabulary is cross-team (po/tran-ngoc-bau/unified-agent/alert-commander), not an intra-dev-team task-dispatch channel, and a queued signal is still "wait for someone to read it," not literal auto-dispatch. Chosen: per-row lock (same `task:<id>` convention every other claim site uses), independent of `.head` (this dispatch isn't part of the head-tracked resume chain — mirrors the on-demand-spawn pattern already at the top of main.md).
**why-decision:** The router's task explicitly wants "auto-dispatches ... instead of relying on router luck" — only a real spawn satisfies that; a lane-move-only design (the audit brief's own honest caveat: "review[] has no automated drain yet ... so closure relies on PO/pm triage visibility") would just be automated-luck, not dispatch.
**why-change:** No change from plan — matches the task's own deliverable text verbatim (scan ready[]/in_progress[], all-terminal children incl. archived, auto-dispatch next_agent/owner).
**verify:** New `scripts/audits/devteam-wrapper-autoclose-verify.sh` — 10/10 synthetic ACs PASS (all-terminal hot+cold sweep, non-terminal-child block, case/separator-drift normalization, missing-child conservative-skip, hold_reason guard, non-wrapper passthrough no-op, `.head` sync positive+negative control, `in_progress[]` source lane, idempotency). Found+fixed a real jq `.`-rescoping bug (`$arr | index(.key)` evaluates `.key` against `$arr`, not the piped object — needed `.key as $k | $arr | index($k)`) via a minimal `jq -n` repro before it ever reached the fixture layer. Live scratch-copy end-to-end run against a copy of the real `docs/data/orch/orch-state.json` + `scripts/orch-state-validate.sh` (Zod schema) PASS, and an unmodified live-data dry run confirmed true no-op today (zero wrapper rows currently in ready[]/in_progress[] — matches `BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` already being closed by hand). Re-ran all 3 pre-existing `devteam-eligibility.jq`-dependent regression suites (`devteam-dispatch-gate-satisfiability.sh`, `devteam-bounded1-detail-disposition-gate-verify.sh`, `devteam-bounded1-prose-sequencing-gate-verify.sh`) — all rc=0, zero FAIL lines, confirming the append introduced no regression. No `bun test`/`tsc` — pure jq/bash/markdown, zero `apps/mcp-server/src/` files touched. Graphify: no Skill-tool path available to this spawned agent (same structural constraint as S21-S24) — flagged in WORK.md, not silently skipped.

### STEP developer-S26 · developer · 2026-07-29T16:22:34Z
**task-id:** FIX-ORCHSTATE-CONSERVATION-GUARD-QA-LANE-BLIND
**what-done:** Added `'qa'` to `FLAT_TASK_LANES` (`scripts/orch-conservation-check.mjs:70`) so `taskTotal()` sums `task_board.qa[]` into the whole-board magnitude the floor-ratio circuit-breaker compares; synced the script's own literal-formula header comment (the only place the formula is enumerated — `dev-standards.md` only references the metric name, never lists lanes, so it needed no edit). Left the historical, frozen `2026-07-10-auditor-orchstate-conservation-guard.md` design brief untouched (single-commit precedent confirmed via `git log --follow`, never edited post-implementation).
**what-considered:**
- Also editing the architecture brief's §4.1 formula for full sync — REJECTED: brief has exactly 1 commit in its history (created once, never touched after `orch-conservation-check.mjs` was later implemented against it) — dated design snapshots are historical record, not living docs; `dev-standards.md` is the living SSOT that cites it.
- only path for the code fix itself: one-token array literal addition, no alternative.
**why-decision:** Task's own deliverable text names the 2 sync targets explicitly ("dev-standards / script header") — grepped both, only the header enumerates lanes literally.
**why-change:** No change from plan.
**verify:** TDD RED→GREEN in `scripts/test/orch-apply-wrapper-tests.sh` (extended, not duplicated — this is the existing conservation-guard suite): wrote QA-COLLAPSE (qa[] wipe, 60→10 rows, must reject) + QA-APPEND-HAPPY (regression guard) FIRST — ran RED pre-fix: `QA-COLLAPSE — expected exit 1, got 0` (2 FAIL, exact class of the reported gap — a collapsed-both-sides live/candidate mismatch that never crossed the floor). Added `'qa'` to the array → re-ran: 48/48 PASS, including QA-COLLAPSE now exit 1 + fixture byte-unchanged, QA-APPEND-HAPPY still exit 0. `assert_real_live_unchanged` passed on every case — the real board file was never touched by the test run. No `apps/mcp-server/src/` files touched (pure `.mjs`/`.sh`), so no `bun test`/`tsc` delta applicable. Graphify: no Skill-tool path available to this spawned agent (same structural constraint as S21-S25) — flagged in WORK.md, not silently skipped.

### STEP developer-S27 · developer · 2026-07-29T17:05:00Z
**task-id:** FACTORY-GUARD-CI-SIZELINT-IMPL
**what-done:** New `scripts/audits/size-lint-justification.sh` (`--check`/`--update`), generated `docs/data/size-lint-baseline.json` (666 offenders), wired `size-lint` job into `ci.yml`, CANONICAL pointer in `dev-standards.md`, new `scripts/audits/size-lint-justification.test.sh` — per architect brief §2/§3 verbatim.
**what-considered:**
- File-list source: `find` over a configurable root vs `git ls-files --cached --others --exclude-standard` (mock-guard.sh's own live precedent) — chose the latter: matches brief's "tracked tree" framing, honors `.gitignore` by construction, and lets test fixtures be plain untracked files (no root-override plumbing needed).
- Baseline lookup: bash assoc-array vs per-file `awk` scan of a pre-dumped TSV — chose `awk` TSV scan (macOS default bash 3.2 has no `declare -A`, existing repo precedent already flags this portability constraint).
**why-decision:** Both choices directly reuse an already-live, already-verified idiom in this repo (mock-guard.sh's git-ls-files discovery; context-bloat-backstop.sh's ±10%/min-5L tolerance math) rather than inventing a new one — brief's own explicit instruction ("mirror the existing context-bloat-backstop.sh idiom").
**why-change:** One correction vs the brief's worked examples: live headers commonly declare an *approximate* count (`~180L`), not just a bare number — brief text never showed this, but a live `--check` dry-run against the real repo surfaced ~10 false-fails on legitimately-justified files until the regex was widened to accept an optional `~`. Also re-verified the offender count live (733→666, expected drift, board note pre-flagged this).
**verify:** `scripts/audits/size-lint-justification.test.sh` 6/6 PASS — all 4 DoD cases (live `--check` exit 0 post-baseline; synthetic new-offender fail; synthetic baseline-grown-past-tolerance fail; shrunk-file AND justified-file both dropped by `--update`) + 2 bonus controls (baseline-within-tolerance PASS; `--update` idempotency excl. timestamp, verified via `diff` on two consecutive runs). All fixtures in a disposable untracked dir, cleaned on exit — real repo/baseline never touched by the test. `.github/workflows/ci.yml` YAML validated via `python3 -m yaml.safe_load`. No `apps/mcp-server/src/` files touched — `bun test`/`tsc` structurally N/A. Graphify: no Skill-tool path available to this spawned agent (same structural constraint as S21-S26) — flagged in WORK.md, not silently skipped.

### STEP developer-S28 · developer · 2026-07-29T19:35:54Z
**task-id:** FACTORY-GUARD-CI-METRICMASK-IMPL
**what-done:** New `scripts/audits/metric-mask-lint.sh` (`--check`, zero-tolerance, detects `??`/`||`/python `or`/destructuring-default with a non-zero literal on `confidence|score|impact|magnitude|probability`-matching identifiers), fixed 3 `cascadeEngine.ts` `?? 0.6` masks + 1 `marketSentimentCalculator.ts` `?? 1.0` mask to honest-absence, annotated `watchlist.ts:198` + `brokerCredibilityTools.ts:51` with `metric-mask-allow:`, wired `metric-mask-lint` CI job, CANONICAL pointer in `dev-standards.md`, new `metric-mask-lint.test.sh` (10/10 PASS).
**what-considered:**
- cascadeEngine fix: `?? 0` (matches repo-wide `confidence ?? 0` idiom, 15+ existing sites) vs propagating `undefined` through a `detectMsciInclusion` signature change — REJECTED signature change: `?? 0` is behaviorally IDENTICAL to the old `?? 0.6` for every real input (both sit below the detector's own 0.7 credibility floor → reject), zero-risk, matches house style.
- marketSentimentCalculator fix: `?? 0` would fabricate a "zero impact" weight while still counting the row's confidence in the denominator (impact_score is genuinely nullable in the DB, unlike cascadeEngine's always-populated confidence) — chose EXCLUDE-the-row instead, mirroring the function's own existing confidence-null guard.
- Ran the built lint against the live repo BEFORE landing any fix: 6 raw hits — 5 were comment-prose false positives (fixed by skipping `//`/`#`/`*` lines) and 1 was a genuine 5th legit-config-default the brief's own manual sweep missed (`brokerCredibilityTools.ts:51` `baseConfidence = 1`, zod `.default(1)`-backed) — annotated it too rather than narrowing the detector's real-bug-catching power to dodge it.
**why-decision:** Both fixes preserve exact scoring behavior for the present-value case (proven by existing + new tests) while making the absent case honest, per the task's own instruction to read downstream consumer handling first.
**why-change:** Scope +1 file vs the brief's stated "4 real bugs + 1 legit default" — verify-live catch, not scope creep: same carve-out mechanism, zero behavior change, required for the zero-tolerance gate to open clean.
**verify:** `metric-mask-lint.sh --check` exits 0 post-fix (423 keyword-bearing files scanned); `metric-mask-lint.test.sh` 10/10 PASS; targeted suites 100% green (FIX-1279-msci-bullish 14/14, P0-4-market-sentiment-index 37/37, 1279a+247+FIX-1268+1224 cascade files 31/31, 915-broker-credibility 22/22, 082+220-watchlist 25/25); `tsc --noEmit` clean; full `bun test` 14904-14907 pass/40 skip/60-63 fail/15007 total, all fails pre-existing order-dependent pollution (tracked board row `FIX-MCP-SUITE-HEALTH-BASELINE`, still BACKLOG) confirmed by isolated re-run of 3 sampled failing files (32/32 pass standalone), none touching my changed files. shellcheck clean on both new scripts.

### STEP developer-S29 · developer · 2026-07-29T22:56:54Z
**task-id:** FACTORY-GUARD-CI-TSBOUNDARIES-IMPL
**what-done:** Wired `eslint-plugin-boundaries` fences into CI for mcp-server/news-fetch/frontend (3 new jobs) + `news-fetch-go-lint` (7th, was zero CI). Fixed 4 live/previously-invisible violations first (2x mcp-server Fence-B interface, 1x mcp-server Fence-B scheduler, 1x news-fetch Fence-C) + a 5th unrelated pre-existing frontend `lint:fence` breakage discovered live.
**what-considered:**
- mcp-server interface violations (`getMoneyRadarComposite.ts`): relocate the offending symbols to their correct DDD layer (infra for pure SQL query, new application usecase for credit-flow computation) vs leave in place + suppress — chose relocation: zero-tolerance gate must catch REAL future regressions, a suppress-comment would defeat the gate's whole purpose on day one.
- mcp-server scheduler violation (`recoverMissingOhlcvSession.ts`): move the FILE into scheduler/ (its sole caller + core dependency are both scheduler-owned; mcp-server's own eslint.config.mjs already calls scheduler "the application-layer orchestrator" for this brownfield service) vs extract the aggregator's tick-aggregation core into a new shared application usecase — REJECTED extraction: would touch the 250-line `ohlcvDailyAggregatorJob.ts` (hand-tuned circular-import dynamic-import design, outside this task's file list), high risk for a debt-fix; DoD's RAW-verify list explicitly names money-radar-composite + news-fetch article-fetch, NOT OHLCV recovery, supporting the lower-risk file-move read.
- news-fetch Fence-C (`fetchArticle.ts` → `PlaywrightBrowserFactory`): setter-injection DI (`setPlaywrightLauncher`, wired once at startup from `src/index.ts`) vs a factory-function signature change threaded through `createRouter`/`composition-root.ts` — chose setter injection: only touches the 2 files the brief/task actually list (`fetchArticle.ts` + `src/index.ts`), `src/index.ts` already runs synchronously before Bun accepts connections (verified: `bun --watch src/index.ts` is the real entrypoint), zero risk of the wiring firing too late.
- frontend's `lint:fence` failing on 5 `react-hooks/exhaustive-deps` "rule not found" errors, unrelated to boundaries and pre-existing (confirmed via git-stash baseline — 0 files of mine touched frontend before this): remove the 5 stale disable-comments (the referenced plugin isn't in package.json/node_modules at all — dead comments) vs add `eslint-plugin-react-hooks` as a new dependency — chose deletion: zero new dependency/config surface, pure dead-code removal, required for the new `frontend-eslint` CI job (this task's own deliverable) to open green.
**why-decision:** Every choice above minimizes touched-file blast radius while producing a REAL (not cosmetic) fix — each was RAW-verified against a `git stash`-reproduced pre-fix baseline to confirm the violation was genuine before fixing it, per house standard on verifying raw evidence over asserted claims.
**why-change:** `eslint .` (whole app-root glob, as brief's example command literally shows) does NOT work for mcp-server — it recurses into `node_modules` and crashes on a vendored package's own unrelated `eslint.config.js` (fast-uri → `neostandard`, uninstalled); scoped every CI job to `src/` (or the package's own existing lint script) instead, matching each service's own `eslint.config.mjs` `files:` scope.
**verify:** Baseline `eslint src/ --max-warnings=0` (mcp-server) reproduces exactly the 3 documented violations pre-fix, 0 post-fix. news-fetch: adding ONLY the elements-map fix (no code fix) surfaces the exact Fence-C hit predicted by the brief; full fix → 0. frontend `lint:fence`: 5 errors pre-fix (confirmed unrelated to any of my changes via stash), 0 post-fix. 2 standalone scratchpad RAW-verify scripts exercised the real post-relocation import graph end-to-end (not mocks) — money-radar composite (`foreign_net_direction=0.2` nonzero from seeded flow row, `credit_flow_direction=null` correctly HN-3-excluded) and news-fetch DI wiring (unconfigured→graceful error, configured→correct Contract B response, launcher invoked exactly once). Test regressions: 0 — mcp-server targeted 9-file suite 133/133, full suite 14797/3fail (all 3 pre-existing, stash-confirmed); news-fetch 241/241; frontend vitest 2183/2185 (2 pre-existing `QUE_DESCRIPTIONS` fails, stash-confirmed unrelated). `tsc --noEmit` clean on all 3 services. `size-lint-justification.sh --check` also clean after adding justification headers to 3 files that crossed 120L/tolerance as a direct consequence of this task's edits.

### STEP developer-S30 · developer · 2026-07-30T00:10:00Z
**task-id:** FACTORY-GUARD-CI-COMPROOT-LOGIC-IMPL
**what-done:** New `scripts/audits/composition-root-logic-gate.go` (go/ast+go/parser stdlib, `--check`, zero-tolerance: flags any `cmd/server/**/*.go` receiver method with if-count>=2 OR any for/range, escape hatch `composition-root-logic-allow:`). Moved macro-indicators' 2 real offenders' decision logic (`policyRatesAdapter.FetchPolicyRates` HTML/DB fallback+`IsEstimate`, `omoAdapter.FetchOMO` `ParseOK` fail-closed) into 2 new `pkg/application` resolvers (`PolicyRatesResolver`, `omoResolver`) that still implement the pre-existing `PolicyRatesProvider`/`OMOProvider` ports — `LiquidityStateUseCase` itself untouched. Adapters split into pure-delegation pairs; wired `composition-root-logic-gate` CI job (all 7 services, 1 job not 7 — tool is syntax-only) + CANONICAL pointer.
**what-considered:**
- OMO tenor-row struct-mapping loop (infra→app DTO, distinct named struct types, Go has no implicit slice conversion between them): keep as a free (non-receiver) helper `convertOMOTenorRows` in `cmd/server/adapters.go` (out of gate scope by construction, same free-helper carve-out as `envStr`/`splitCSV`) vs move the loop into `pkg/application` too — chose the free-function route: mechanical field-copy, not a business decision, avoids inventing a duplicate raw-tenor DTO type purely to dodge the gate.
- Resolvers implement the EXISTING `PolicyRatesProvider`/`OMOProvider` interfaces (zero change to `LiquidityStateUseCase`) vs threading new raw-provider types through the use case's own constructor — chose zero-touch: minimizes blast radius, DoD explicitly requires `/liquidity-state` output unchanged.
- Test filename: board-row note literally said `composition-root-logic-gate.test.go` — Go's toolchain only discovers `_test.go` (underscore); `.test.go` would silently never run under `go test`. Renamed to `composition-root-logic-gate_test.go` (root-cause fix, not a cosmetic rename) — same correction class as S29's `eslint .` brief-literal-vs-actually-working-command deviation.
- No Agent/Task-spawn or MCP tool available in this session (Read/Edit/Write/Bash only — "local subagents gateway blind" structural limitation, confirmed memory pattern) — could not literally dispatch `dev-macro-indicators`; did the `apps/macro-indicators/` zone work directly instead of narrating a dispatch I cannot execute. `/graphify docs --update` (Skill tool) is likewise unavailable — skipped, doc content itself is updated (dev-standards.md CANONICAL pointer).
**why-decision:** Every choice minimizes blast radius while making the gate's own tests actually executable and the fix behaviourally invisible to `/liquidity-state` callers.
**why-change:** Filename correction (`.test.go`→`_test.go`) and direct (non-dispatched) macro-indicators zone work are both structural corrections, not scope drift — flagged so a future agent doesn't "fix" the filename back to match the stale board-row text.
**verify:** Gate tool run live pre-fix on `apps/macro-indicators/cmd/server` reproduces exactly the 2 documented offenders (3 ifs / 4 ifs+1 for); 0 FP across the other 6 services (independently re-grepped: zero receiver methods exist outside macro-indicators' cmd/server at all — only `main()`+named free helpers). Post-fix: `go run … --check` across all 7 services exits 0; `go build`/`go vet`/`golangci-lint run` (Fence-A/B/C) clean; full `go test ./...` unchanged pass set. `composition-root-logic-gate_test.go` 7/7 PASS (2 offender shapes incl. for-only, allow-comment pass, allow-comment-with-blank-line-does-NOT-suppress, main/free-function never-flag, pure-delegation-shim never-flag, live all-7-services zero-violation regression). RAW-verify against the LIVE running container (not just unit tests): rebuilt+recreated `macro-indicators` (image id confirmed changed), `POST /liquidity-state` before/after — response body + `slog` warn lines byte-identical modulo wall-clock `fetched_at`; both hits landed on the exact 2 fallback/fail-closed branches the gate flagged (HTML parse failing live in prod right now → DB fallback; OMO `ParseOK=false`), so both moved decision paths were exercised for real, not just synthetically.

### STEP developer-S31 · developer · 2026-07-30T02:50:00Z
**task-id:** FACTORY-GUARD-CI-DEADCODE-IMPL
**what-done:** New `scripts/audits/dead-code-gate.sh` (`--check`, zero-tolerance, 4 checks on TRACKED files: `.bak`/`.backup`/`.patch`, `_deprecated/` segment, Go/TS twin scaffold, `//go:build ignore`). Fixed live debt first: deleted 2 stray `docker-compose.yml.backup`/`.patch` root files, mcp-server's 2 `_deprecated/` trees + the dedicated `1077-kinh-dich-wrapper.test.ts`, pdf-extractor's `_deprecated/mock_echo`, stock-price's `_deprecated/services_v1.go`+test (the only 2 `//go:build ignore` files repo-wide) — all zero-importer-verified independently via grep, not trusted from the board note. Surgical edit to `1081-sprint-054-smoke.test.ts` (removed `appendKinhDich` import + Scenario 5/5b/5c only, 17→14 tests, rest of 569L suite intact). `apps/technical-analysis/package.json` trimmed `bun-types`/`typescript` devDeps. Wired `dead-code-gate` CI job + CANONICAL pointer.
**what-considered:**
- Check-3 (twin scaffold) literal board-note rule ("any Go `cmd/server` + `package.json` + `src/` combo bans") vs a Dockerfile-content-aware refinement — literal rule immediately false-positives on the LIVE `apps/news-fetch` (WIP parallel Go port + its own go-lint CI job, TS side is the actually-deployed artifact per its Dockerfile). Chose the refinement: flag only when the service's Dockerfile has ZERO `src` reference (generalizes the confirmed technical-analysis dead-shape signal — its Dockerfile `COPY`d only `cmd/ pkg/ api/`) — RAW-verified `--check` reports 0 on news-fetch, 2 dedicated synthetic fixtures (blind/referencing pair) prove the exemption is deliberate not accidental.
- `apps/technical-analysis/src/` + its 3 `__tests__/*.test.ts`: board note said "delete" — live-verified ALREADY DELETED (commit `099afddd3`, 2026-07-28, an unrelated task, before this row was even dispatched); only the `package.json` devDep trim remained live-actionable. Board note's ~2,070 LOC estimate included this ~697L share, explaining the gap vs the ~1.4k LOC I actually found live.
- Full `bun test` "0 fail" DoD line vs the standing `FIX-MCP-SUITE-HEALTH-BASELINE` order-dependent full-suite red (dev-standards.md pinned reading: verify zero NET NEW failures via base-vs-head A/B, not literal 0-fail) — ran 3 full-suite passes (base worktree via disposable `git worktree add --detach HEAD`, HEAD twice): base 20fail/9files, head-run1 24fail/10files, head-run2 2fail/2files, every failing file present in base's set or a run-to-run flip on IDENTICAL code (1821a/1898b both pass 100% clean standalone) — zero deterministic net-new failures from this task.
**why-decision:** Every deviation is RAW-verified against live evidence (grep zero-importers, git log for the pre-existing deletion, 3-run A/B for the flaky suite), not asserted from the board note's design-time prose.
**why-change:** Check-3 refinement and the technical-analysis already-deleted finding are both root-cause corrections to the board note, not scope drift — flagging so a future agent doesn't "fix" check-3 back to the literal (broken) rule or re-attempt deleting an already-gone tree.
**structural-gap:** No MCP/gateway tool grant in this session (Read/Edit/Write/Bash only, confirmed at Step 0 — "local subagents gateway blind"). Could not release `task:FACTORY-GUARD-CI-DEADCODE-IMPL` (dev-team's coordination session holds it) or send Telegram; flipped the board row myself via `scripts/orch-apply.sh` (pure bash, no MCP needed) since that's explicitly permitted when `branch:null`. Coordinating dev-team session must pick up the MCP-only release step.
**git-hygiene:** Mid-task, ~12 of my staged `git rm` deletions were found unstaged (index reverted to match HEAD, files still gone from working tree) between a background full-suite run and the next check — cause not conclusively isolated (concurrent peer commits landed on `main` during the same window: `b7af12d1b`, `6074e7344`, both single-file pathspec-scoped system-auditor commits that should not have touched unrelated index entries under normal git semantics). Re-staged everything immediately before commit, verified `git status --porcelain -- <exact paths> | grep -v '^??' | wc -l == 0` both pre- and post-commit. No MCP access → could not claim `commit-mutex:main`; used plain pathspec-scoped `git add`/`git commit -- <exact paths>` only (never `-A`/`-am`).
**verify:** `dead-code-gate.sh --check` 0 on live repo; `dead-code-gate.test.sh` 8/8 PASS. mcp-server: `tsc --noEmit` clean, `eslint src/ --max-warnings=0` clean, `1081-*.test.ts` 14/14 pass. technical-analysis: `go build`/`go vet`/`go test ./...` (7 pkgs) clean, `golangci-lint run` 0 issues, `dashboard/build.sh` green (35/35 sandbox scenarios + headless render gate, esbuild/playwright-core confirmed load-bearing post-trim). pdf-extractor: `lint-imports` 3/3 contracts kept. stock-price: `golangci-lint run` 0 issues, `go test ./...` clean. Commit `448b41b0a`, 24 files, residual check 0 both times.

### STEP developer-S32 · developer · 2026-07-30T02:04:18Z
**task-id:** FACTORY-GUARD-CI-NOHARDCODE-IMPL
**what-done:** New `scripts/audits/no-hardcode-allowlist-scan.sh` (`--check`, zero-tolerance, 2 checks: temporal-combo `.includes('YYYY')`/Go `strings.Contains` near a literal-year `===`/`==`; ticker/code literal-branch `(ticker|symbol|code|action_code)` vs quoted 2-5-char ALL-CAPS with `HOSE|HNX|UPCOM|BLOOMBERG` denylist). Fixed 2 cosmetic reason-string branches (`backfillBctcScalarsTool.ts` CTG-only, `pharmaEventMapper.ts` IMP-only) to the generic message. Annotated `JANITOR-035` (`newsChainFallback.ts`) + `JANITOR-034` (`cascadeExecutor.ts` `LARGE_CAP_FALLBACK` + `priceSourceRouter.ts` `MAJOR_CAPS`) with `hardcode-scan-allow:`. Wired CI job + CANONICAL pointer. New `no-hardcode-allowlist-scan.test.sh` (9/9 PASS).
**what-considered:**
- Check-2 identifier-boundary regex: first draft excluded `.` from the valid-prefix-delimiter set (to block "somecode"-style substring false-matches) — this also silently blocked the REAL `report.action_code` property-access shape. Caught by running the built script against the live repo BEFORE trusting the design (it found 0 hits on the known CTG offender pre-fix, which should have been impossible) — fixed boundary to `[^A-Za-z0-9_]` (dot is now a valid delimiter).
- `priceBackfillService.ts:224` `ticker === "BAD"`: brief §2(c) explicitly excludes this as a documented test-fixture sentinel, out of this scan's offender count — but check-2's regex AS LITERALLY SPECIFIED still matches it (verify-live: live run found 3 non-test hits, not the expected 2). Not in the board's file list. Chose: annotate with `hardcode-scan-allow:` citing brief §2(c) verbatim (zero live-behavior change) over an ad-hoc "BAD" denylist entry (bad precedent, undocumented in brief §3) — mirrors `dead-code-gate`'s own check-3 board-note deviation.
- `JANITOR-034` sites (`LARGE_CAP_FALLBACK`/`MAJOR_CAPS` array declarations) are NOT actually caught by check-2 (array literals, not `===` comparisons) — annotated anyway per the board note's explicit instruction and brief §3's own text ("resolved via the escape hatch below, not automated detection") — a documentation/paper-trail marker, not an active suppression.
**why-decision:** Gate must open clean day one without forcing a premature semantics decision on 2 known-debt tickets already flagged as needing human design input; both deviations were discovered by actually running the tool against the live repo before trusting the brief's illustrative regex or the board note's literal file list.
**why-change:** +1 file vs board's 5-file list (`priceBackfillService.ts`) — verify-live catch, not scope creep, required for check-2 to open at zero.
**verify:** `no-hardcode-allowlist-scan.sh --check` exits 0 post-fix/post-annotation; `.test.sh` 9/9 PASS (TS+Go temporal-combo fail, ticker-branch + property-access shape fail, HOSE/HNX/UPCOM/BLOOMBERG denylist pass, annotated pass, unannotated-same-shape negative-control fail, comment-only pass). CTG/IMP fixes RAW-diffed: only reason/reasoning text changed — confirmed via `BEQ-2-backfill-eligibility`/`268-pharma-event` tests, which assert `status`/`table_row_count`/`code`/`eventType`/`direction` only, never reason text. `tsc --noEmit` clean. Targeted 17-file suite (backfill/pharma/cascade/price-router + callers) 191/191 pass. Full `bun test`: 14865-14869 pass/40 skip/52-56 fail across 2 runs (order-dependent baseline, matches `FIX-MCP-SUITE-HEALTH-BASELINE` pattern) — grep-confirmed zero fails reference any touched file/function in either run. shellcheck clean on both new scripts.
**structural-gap:** No MCP/gateway tool grant in this session (Read/Edit/Write/Bash only — "local subagents gateway blind"). Flipped the board row myself via `scripts/orch-apply.sh` (permitted, `branch:null`). Coordinating dev-team session must release `task:FACTORY-GUARD-CI-NOHARDCODE-IMPL`.
---
