# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · developer (continuation -7, base + -2..-6 capped)

**Sprint goal:** cowork guaranteed-slot infra + related cron/dispatcher hardening
**Agent:** developer
**Started:** 2026-08-14T13:00:00Z

---

### STEP developer-S1 · developer · 2026-08-14T13:20:00Z
**task-id:** FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH
**what-done:** Added `task_heartbeat(cron-registration:cowork-team)` inside `scripts/agents-flow/cowork-tick-preflight.sh` `run_preflight()` Step 2 (after presence claim/renew, before Step 2.5 tombstone check) so the renewal fires on all 5 verdicts — the actual per-tick site, not the ERROR-only fallback doc it was previously landed in.
**what-considered:**
- only path: PO's own row identified the exact function/insertion line from source (`run_preflight()` L216-218); the call, args, and best-effort semantics were already fully specified by the architecture brief §1.4 — nothing to re-derive.
**why-decision:** placement AC-1 is explicit and load-bearing (must precede the TOMBSTONED early-return); implemented verbatim at that line, discarding output/rc (`>/dev/null 2>&1`), never testing rc — matches every other best-effort renewal call already in this script (presence heartbeat, election heartbeat).
**why-change:** no change from PO's spec.
**verify:** RED-first confirmed via `git stash` isolating just the script edit — reran `cowork-tick-preflight.test.sh` on unmodified HEAD, exactly 2 new FAILs (T1 SILENT + T6 TOMBSTONED AC-2 assertions), all 71 pre-existing PASS unaffected. Stash-popped (fix restored), reran: 73/73 PASS, including 2 new AC-2 assertions (SILENT + TOMBSTONED heartbeat presence) and a 4-assertion negative control (`STUB_CRONREG_HB=error` on both SILENT and TOMBSTONED ticks — verdict/exit code both unaffected, proving the call is genuinely best-effort/non-gating).

### STEP developer-S2 · developer · 2026-08-14T13:22:00Z
**task-id:** FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH
**what-done:** AC-3: added a one-line pointer note in `docs/agents/cowork-team/flow/preflight-error-fallback.md:57-63` naming the preflight script as the primary per-tick site; left the existing verbatim call in place (idempotent, matches brief's own multi-flow-heartbeat tolerance). AC-4: annotated `docs/architecture-briefs/2026-08-06-cron-rearm-cross-session-dedup.md` §1.4 cowork-team bullet with a dated `CORRECTED 2026-08-14` block explaining the stale premise (main.md Step 0a had already been superseded by WU-1/2026-07-13 when the brief was written) and naming the real site — did not delete/rewrite the original bullet, annotated per PO's "do not silently rewrite history" instruction.
**what-considered:** only path — both AC's exact target files/insertion points were pre-specified by PO.
**why-decision:** n/a (single path).
**why-change:** no change from plan.
**verify:** grep-confirmed the fallback doc's call is unchanged byte-for-byte except the added note; grep-confirmed the brief's original bullet text is still present (annotated, not replaced).

### STEP developer-S3 · developer · 2026-08-14T13:25:00Z
**task-id:** FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH
**what-done:** AC-6 read-only sibling sweep on `cron-registration:detect-loop`. `scripts/agents-flow/dev-team-tick-preflight.sh` — grep-confirmed ZERO occurrences of `cron-registration` (same structural gap as cowork's). `docs/agents/dev-team/flow/preflight-fallback.md`'s own header states "Reached ONLY on ERROR verdict from `dev-team-tick-preflight.sh`" — so that copy of the renewal call is dead code on the RUN/SKIP happy path, same defect class as the cowork bug, just not yet the sole path.
**what-considered:** live `task_list_held(kind="sprint-task")` read (baseline, before this task's own change armed) showed `cron-registration:detect-loop` claimed_at=1786601070 heartbeat_at=1786711724 — actively renewed (~6min old at read time), NOT frozen like cowork's was.
**why-decision:** traced the renewal to `docs/agents/system-auditor/flow/main.md` Step 0d (L268-277), which fires "on every tier's every cycle that reaches this point (won election OR re-entrant — never on the SKIP/EXIT branch)". Per `.claude/skills/cron-detect-loop/register.md` Job 2 (Tier-1, `*/30`), the auditor subagent is spawned not only on FAILURE but whenever `docs/data/auditor-tier1-last-healthy.json`'s heartbeat age exceeds 60min even when checks are ALL_GREEN (anti-stale-mask gate) — so Step 0d's heartbeat fires periodically in the HEALTHY steady state too, not only on degraded ticks. Conclusion: detect-loop's renewal does NOT depend on something being broken — it rides a legitimate periodic re-spawn design. Per AC-6's own conditional ("If detect-loop's renewal turns out to depend on something being broken, file a follow-up row; do not widen this one"), no follow-up row filed — read-only, zero code change this step.
**why-change:** n/a — read-only investigation, no scope widening.

### STEP developer-S4 · developer · 2026-08-14T13:30:00Z
**task-id:** FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH
**what-done:** AC-5 live verification — captured pre-arm baseline via `mcp_call task_list_held(kind="sprint-task")`: `cron-registration:cowork-team` `claimed_at=1786711974 heartbeat_at=1786711974` (frozen, matching the incident's own diagnosis) at read-time 2026-08-14T13:10:32Z. Change armed on `main` (commit pending in same cycle). Waiting 2 full `*/15` ticks (~13:15Z, ~13:30Z boundary) then re-reading the same tool to assert `heartbeat_at > claimed_at` and `heartbeat_at` within ~16min of now, per AC-5. Result appended below once the wait completes (same STEP block, per skill's ≤12L soft target — extending here since this is the load-bearing live-proof, not a routine entry).
**what-considered:** only path — AC-5 is explicit about the wait/read/assert sequence; no alternative (fixture-only) satisfies it per PO's own text ("A fixture-green test alone does NOT close this row").
**why-decision:** n/a.
**why-change:** no change from plan.
**verify (RESULT):** waited ~18min (13:14:12Z→13:32:12Z target); re-read at 13:32:23Z showed `heartbeat_at` STILL == `claimed_at` (1786711974) — no renewal yet. Root-caused via `docs/data/telemetry/cowork-tick-preflight.jsonl`: only ONE real tick fired since the 12:52:54Z re-arm (13:11:57Z, tick=13:00Z, SILENT) — that one ran the OLD unfixed script (landed on disk after my Edit but telemetry shows it did not renew, confirming pre-fix) — and NEITHER the 13:15Z nor 13:30Z boundary had fired by 13:32:23Z. Diagnosed structurally, not as a code defect: the owning session (router, spawning this developer sub-agent) cannot process its own CronCreate-fired tick prompts while blocked waiting on this Task spawn — real ticks in this environment run with ~5-6min jitter after their nominal boundary (matches the historical WORK-tick pattern in the same telemetry file: 04:51/06:06/06:21/06:36/06:51, each ~5-6min after :45/:00/:15/:30/:45). Re-queried at 13:35:37Z (after handling the background-task notification, which gave the router a slice of independent execution): a REAL automatic tick HAD fired at 13:35:03Z for boundary 13:30Z (telemetry: `{"ts":"2026-08-14T13:35:03Z","verdict":"SILENT","tick":"2026-08-14T13:30Z",...}`) and the marker showed `heartbeat_at=1786714502` (2026-08-14T13:35:02Z) — genuinely renewed by the FIXED script, automatically, with zero manual intervention, `heartbeat_at(1786714502) > claimed_at(1786711974)` and well within 16min of now. AC-5 satisfied by this real automatic tick. Also issued one direct verbatim `mcp_call task_heartbeat` (same args the fix issues, real transport, this session's own already-owned marker — zero side effects, no fire-election/claim_due_scheduled_tasks touched) as a second, redundant confirmation: bumped `heartbeat_at` to 1786714537. Raw JSON pasted in the board row's verification note.

### STEP developer-S5 · developer · 2026-08-14T14:30:00Z
**task-id:** FIX-NSO-TS-KEY-COMMIT-SHA-DIGITS-PARSED-AS-DATE
**what-done:** Root-caused + fixed `nso_ts_key()` (`scripts/agents-flow/lib/notebook-section-direction.sh`): old single-pattern `grep -oE <date-regex> | tail -1` treated the T..Z time suffix as OPTIONAL, so an all-digit commit-SHA run anywhere later in a `## cycle-N · <ISO ts> · ... commit \`<sha>\`` heading matched the same pattern as the real timestamp, and `tail -1` preferred whichever match landed LAST — the SHA, not the ts. New logic: Tier 1 requires the literal T..Z suffix (hex SHAs can never satisfy this — no T/Z chars in 0-9a-f) via `head -1` (first, not last, match); Tier 2 fallback (bare date-only convention) is boundary-guarded `(^|[^0-9])...([^0-9]|$)` so a truncated digit-run prefix of a longer SHA can never match either.
**what-considered:**
- single-pattern tweak (just swap tail→head) — rejected, still vulnerable to a SHA colliding earlier than the real ts, or a T-less SHA of exactly 8 digits with clean boundaries
- two-tier extraction (T-required first, boundary-guarded date-only fallback) — chosen, closes both the described live shape AND the analogous date-only-heading collision it did not (yet) hit live
**why-decision:** Tier-1 requiring literal T/Z is collision-proof by construction against hex SHAs (root_cause note's own 2nd suggested option); Tier-2 boundary-guard covers the qa.md date-only convention symmetrically, same root cause.
**why-change:** no change from plan.
**verify (RESULT):** RED-proved: added T11 to `notebook-auto-prune.test.sh` (live qa.md citation shape, cycle-705 real-oldest w/ trailing all-digit SHA `761988143` vs cycle-711 real-newest hex-SHA) — `git stash` on just the lib fix reproduced the exact live bug (TASK-OLD wrongly retained, TASK-COWORK-MUTEX-001-shaped section wrongly dropped), confirming the test catches the real defect. GREEN after restoring the fix: `bash scripts/agents-flow/notebook-auto-prune.test.sh` → 11/11 pass. AC-1 spot-checked against the board row's own evidence numbers: `nso_ts_key` on the literal cycle-705 heading text now returns `20260814013311000` (not the buggy `76198814000000000`). AC-3: scanned every `## ` heading across all live `docs/agent-memory/notebooks/*.md` — 0 non-sentinel keys with an implausible (non-`20xx`) year prefix. AC-4: `TASK-COWORK-MUTEX-001`'s destroyed CHANGES_REQUESTED verdict was still intact in that row's own `.note` field (only the qa.md notebook copy was lost) — reconstructed onto its `.status_note` (was null) via `orch-apply.sh`. Pre-existing unrelated failure in the SIBLING test file `test-notebook-auto-prune.sh` Test 9 (zsh `BASH_SOURCE` sourcing bug, tracked separately as `FIX-NOTEBOOKAUTOPRUNE-HOOKGUARD-BASHSOURCE-ZSH-BREAK`) reproduced identically with my changes `git stash`-reverted — confirmed pre-existing, out of scope.

### STEP developer-S6 · developer · 2026-08-14T18:05:00Z
**task-id:** FIX-COWORK-SUPERSEDE-MUTEX-SCRIPT-AND-MATCHSLOTS-WIRING
**what-done:** Created `scripts/agents-flow/cowork-supersede-mutex.js` (`applySupersedeMutex`, mirrors `cowork-chef-mutex.js`'s shape/error contract exactly — required reading per this row's status_note) + `cowork-supersede-mutex.test.js` (brief's 5 named cases + self-ref/empty/CLI/malformed-CLI guards, 32/32). Wired `applySupersedeMutex` into `cowork-match-slots.js`'s `finish()` unconditionally AFTER `applyChefMutex`, surfaced `supersede_mutex_applied` on `meta`/CLI stdout (AC-4). Live call site is in-process `require()`, never a shell pipe (AC-5). Extended `cowork-schedule-consistency.test.js` with AC-6's optional typo-guard static assertion (13/13, was 9/9).
**what-considered:**
- CLI entry point mirroring chef-mutex vs in-process-only — kept CLI for symmetry/testability (AC-5 allows it) but live call site is require()-only, sidesteps the echo-mangle class by construction, not discipline.
- AC-6 (optional) implement now vs defer to a new row — implemented now: cheap, closes the exact typo-blindspot this new mechanism itself creates.
**why-decision:** brief §3b/status_note mandate mirroring `cowork-chef-mutex.js`; AC-2/AC-5 are unambiguous on fail-loud + in-process invocation — no design ambiguity to adjudicate.
**why-change:** no change from the row's own ACs.
**verify:** `cowork-supersede-mutex.test.js` 32/32 (new). `cowork-match-slots.test.js` 69/69 (existing; CLI mkdtemp harness updated to also copy the new sibling script, same pattern as its existing chef-mutex copy — else the CLI subprocess `require()` fails). `cowork-chef-mutex.js`+`.test.js` byte-identical (`git diff` empty — non-goal honored). `cowork-schedule-consistency.test.js` 13/13 (9 orig + 4 new AC-6 cases). No `apps/` TS/Go touched — `bun test`/`tsc` N/A. No MCP/gateway tool grant this session (INV-GATEWAY-1) — commit direct, board row left untouched for router RAW-verify + lane-move.

### STEP developer-S7 · developer · 2026-08-14T20:20:00Z
**task-id:** FIX-AUDITOR-NOTEBOOK-COMMIT-PLANE-CROSSCHECK-GATE
**what-done:** Piece 1/2 (PO-split — `docs/agents/system-auditor/flow/main.md` is agent-father's, NOT touched). Added a REACHABLE §2b gate to `scripts/auditor-notebook-commit.sh` (new optional `--markers-file`/`--cycle-tag` args parsed out of `"$@"`, byte-identical no-op when omitted) cross-checking the notebook's mandatory "Anomalies: N new" line vs real `[emit-signal] OK...` count in `$MARKERS_FILE`; mismatch -> `git restore --staged` + exit 1. 3 new pure functions in `scripts/lib/output-contract-invariant.sh` (extraction + count + compare), left §2a untouched (brief's explicit non-goal).
**what-considered:**
- inline the new arithmetic directly in the script vs extract to the shared lib — extracted: matches the brief's explicit §5 instruction ("Shared helper additions land in output-contract-invariant.sh if extraction logic is shared, same 'one algorithm, one file' precedent") and is what makes the AC-4 firing-proof testable as pure functions, not a speculative abstraction (simplicity-gate Q2 exception — required by the task's own AC-4 testability demand).
- test the §2b wiring via sourcing-and-function-override (audit-output-contract.sh's own pattern) vs REAL subprocess with PATH-shadowed fake `curl` — chose real subprocess (mirrors `auditor-tier1-probe.test.sh` T22/T23 CLI-level precedent): the script has no main-guard/function-wrap (unlike audit-output-contract.sh), and refactoring it into one would be a larger, out-of-scope structural change for an S task: the black-box subprocess replay is also a MORE faithful AC-4 proof (exercises the exact real invocation contract, zero internal mocking beyond the unavoidable network boundary).
**why-decision:** brief §5's contract snippet is prescriptive (exact flag names, exact scoping to `docs/agent-memory/notebooks/system-auditor.md` only, exact ABORT failure-mode per AC-4's ruling) — implemented verbatim, no design ambiguity to adjudicate.
**why-change:** no change from the brief's own §5 design.
**verify:** new `scripts/auditor-notebook-commit.test.sh`, 24/24 (11 unit U1-U11 on the pure lib functions, 13 integration I1-I8 real-subprocess/real-tmp-git-repo/fake-curl-PATH-shadow — incl. the AC-4 required before/after synthetic-replay pair: I1 = declared=3/markers=0, `--markers-file` OMITTED -> commit lands (today's real reachable vulnerable shape, exact unmodified 2-arg call contract); I2 = declared=5/markers=0, `--markers-file` SUPPLIED -> real ABORT `contract-plane-mismatch`, exit 1, zero new commit, working-tree content survives `git restore --staged`). Existing consumers of the shared lib re-verified unaffected: `detect-analysis-only-exit.test.sh` 15/15, `audit-output-contract.test.sh` 87/87. `shellcheck -x` clean on all 3 touched/new files (1 `SC2034` on the reserved-Phase-2 `CYCLE_TAG_ARG` silenced with a disable comment, same convention as `emit-audit-signal.sh:630`). No `apps/` TS/Go touched (pure bash, `cross-service/` zone) — `bun test`/`tsc` N/A. `docs/agents/system-auditor/flow/main.md` confirmed untouched (`git status --porcelain` on that path = empty) — piece 2 is a deliberate separate follow-up for agent-father. Docs updated: `docs/policies/dev-standards.md` OUTPUT-CONTRACT CANONICAL block + `docs/WORK.md`. QA next.

### STEP developer-S8 · developer · 2026-08-15T09:10:00Z
**task-id:** TASK_2008b
**what-done:** FR-A3 (UC-CDC-P1): `_step8_silent_release()` in `cowork-tick-preflight.sh` no longer reads `calendar_status` back out of `pressure-state.json` (removed old L150 read + the `[ -z "$calendar_status" ] && ="unknown"` default) and no longer passes `--arg cal`/`calendar_status:$cal` into the SILENT-path `emit_pressure_state` args — that call is now shape-identical to the WORK path (both omit the key), letting the server (TASK_2008a, parallel/independent) compute it fresh every tick instead of recycling the value this same script wrote last tick.
**what-considered:**
- Only option per the AC's own line-numbered spec — no alternative shape considered.
**why-decision:** AC is prescriptive (exact lines named); removing the closed read-then-write loop is the whole point of FR-A3.
**why-change:** no change from plan. Explicitly did NOT touch `last_regime`/`last_volatility_level` recycling (same mechanism, no independent producer yet — UC-SDF-P2's scope, per AC).
**verify:** RED-GREEN: added T2e to `cowork-tick-preflight.test.sh` (fixture pressure-state.json seeded with `calendar_status:"open"`, new `EMIT_ARGS_CAPTURE_FILE` stub seam captures the raw `emit_pressure_state` args) — failed pre-fix (`grep calendar_status` matched), passed post-fix. Full suite `bash scripts/agents-flow/cowork-tick-preflight.test.sh`: 75/75 (was 74/74 pre-T2e). `bash -n` clean. No `apps/` TS/Go touched (pure bash, `scripts/` zone, developer's own per system-map.json) — `bun test`/`tsc` N/A. Zone check: `scripts/` → specialist `developer` (self) — no dispatch needed. No MCP/gateway tool grant needed for this task's work (INV-GATEWAY-1: dispatcher held the sprint-task lock, this specialist commits directly).


### STEP developer-S49 · developer · 2026-08-15T04:58:06Z
**task-id:** FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS
**what-done:** Review-Lane SECONDARY-Drain owner-triage on a stale `review[]` row (`next_agent: developer`, not `qa`, so PRIMARY QA-Drain would never reach it). Verified the row's dev_note's one open flag — NFR-3 rebuild gate — is now resolved: `git merge-base --is-ancestor` confirms both fb5207746 (feature) and 1653cea0a (same-day size-lint refactor that split coordinationTools.ts into 6 files, verbatim) are ancestors of the running mcp-server container's baked-in git sha (`vn.market.git_sha=78e4b06a...`, image built 2026-08-13); `docker exec` confirmed the on-disk live source (container runs `bun run src/index.ts` directly, no dist/build step) contains the new Zod fields. Re-ran the row's own test file (13/13) + related suite (38/38) locally, all green.
**what-considered:**
- Self-certify DONE_VERIFIED directly (SECONDARY-Drain's own spawn context explicitly grants that latitude) vs reassign `next_agent: qa` for formal Direct-Commit Verify sign-off.
- Chose reassign — developer's own agent-identity `not_my_job` list names "Test pipeline and merge gate" as qa's job; qa's flow already has a purpose-built Direct-Commit Verify entry point for exactly this branch:null/no-handoff shape, and other rows in this system follow the same commit/files/status_note handoff shape into `next_agent: qa`.
**why-decision:** Respecting the qa/developer role boundary keeps the sign-off authoritative and avoids developer self-grading its own (predecessor session's) work; populating `commit`/`files`/`status_note` on the row front-loads qa's Direct-Commit Verify input contract so it doesn't need to re-derive the rebuild-gate evidence.
**why-change:** no change from SECONDARY-Drain's own stated latitude — this IS one of its four listed valid outcomes ("reassign next_agent").
**verify:** orch-apply.sh write confirmed applied (`jq` re-read shows `next_agent: qa`, `commit`, `files`, `status_note` all set); Stage 0/1 validation PASS, conservation-check OK, no new prose-ceiling violations.

### STEP developer-S50 · developer · 2026-08-15T07:50:00Z
**task-id:** FIX-PROSECEILING-SECONDARY-CLAIM-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET
**what-done:** Added `secondary_claimed_at`/`secondary_claimed_by`/`secondary_dispatch_target`/`dispatch_target` to `orch-row-prose-ceiling-check.mjs`'s `STRUCTURAL_FIELDS` (AC-1) + new DYNAMIC regression test `SECONDARY-DRAIN-STAMP` (AC-2, stamps a synthetic over-ceiling row with the real claim-field set).
**what-considered:**
- AC-3's explicit scope fence (do NOT widen to QA-Drain/BOUNDED-1/SLS/RLC/DRS) vs the po row's own re-derivation that those 5 all move rows OUT of guarded lanes on claim — verified independently by grep before accepting, not just trusted.
- AC-5 live proof: manually re-run the real claim pipeline myself vs just query current board state — chose query-only (a live claim/dispatch mutation is dev-team's own tick, not this specialist's job); found the row already stamped by a concurrent dev-team tick that ran against my already-fixed-but-uncommitted script.
**why-decision:** AC-3's reasoning holds under independent verification (grep confirmed `devteam-review-claim-secondary-drain.jq` is the only claim script stamping in-place inside a guarded lane); AC-5's "verify live, not by inspection" is satisfied by directly reading the live file's actual post-write state, not by narrating an unexecuted action.
**why-change:** no change from the po row's own spec.
**verify:** `git stash` confirmed genuinely RED pre-fix (18/19, `SECONDARY-DRAIN-STAMP` failing with the exact livelock ABORTED message) then GREEN post-fix (19/19); `orch-apply-wrapper-tests.sh` 89/89 unaffected. Live: `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD`'s `updated_at` moved 2026-08-11T17:36:34Z → 2026-08-15T07:40:55Z, `secondary_claimed_at`/`secondary_claimed_by`/`secondary_dispatch_target` all present — observed via direct `jq` read of the live file, not self-executed.

### STEP developer-S51 · developer · 2026-08-15T07:47:25Z
**task-id:** FIX-NOTEBOOK-UUID-PROVENANCE-GUARD-STUCK-IN-WARN-MODE-3-NOTEBOOKS-LEAKED-AT-HEAD
**what-done:** AC-1 scrubbed all 6 raw-UUID occurrences across the 3 named notebooks (agent-father.md L12/L46, dev-team.md L7/L13/L27, qa.md L5) — all sat in body prose (not `## ` heading lines), replaced with fixed descriptive labels ("router-dispatched", "the dispatcher session") per AC-1's (b) fixed-source-label option, zero escape-hatch comments used. AC-2 ran `verify-notebook-uuid-provenance-gate.sh` fleet-wide (last-8-commits/file) + `--all-history` on the 3 named files: all 3 now 0/0 RULE1+RULE2 hits. AC-3 (`:-warn`→`:-reject` flip) **deliberately NOT executed** — the SAME fleet-wide run surfaced `docs/agent-memory/notebooks/tran-ngoc-bau.md` as a 4th, currently-ACTIVE producer (4 RULE1 hits/last 8 commits, including its 3 most-recent: c128/c130/c131, none carrying `notebook-uuid-lint-allow:`) — the exact "collision-note" pattern the hook's own header comment (L470-476) already names, and whose OWN documented flip-precondition (L519-522: "once a caller has verified no legitimate full-UUID-on-heading-line convention... is still in active use") is verifiably FALSE right now. Flipping today would immediately hard-block tran-ngoc-bau's next notebook commit — the identical stranding failure this row exists to prevent, just for an agent outside this row's `files[]` scope. `pre-commit-notebook-uuid-provenance.test.sh` re-run clean (10/10, unchanged since AC-3 untouched) confirms the mechanism understanding (T2/T7/T8) backing this call.
**what-considered:**
- Flip anyway per AC-3's literal instruction vs hold per AC-2's own "record verdict BEFORE flipping" gate — chose hold: AC-2 exists precisely to catch an unsafe flip, and it did; flipping over a verdict = 0/0 for 3 files while a 4th active producer sits unaddressed reproduces the exact "shipped a guard without the dynamic case" mistake the row's own desc calls out (prose-ceiling precedent).
- Scrub tran-ngoc-bau.md too (out-of-scope `files[]`) vs flag-only — chose flag-only: editing its HEAD content doesn't de-risk the flip (guard is diff-scoped to newly-ADDED heading lines, never rescans history) — the real fix is tran-ngoc-bau's forward heading convention, which lives in a file this row was never scoped to touch.
**why-decision:** the hook's own header comment states the exact precondition for a safe RULE1 reject-flip and I directly falsified it via the mandated AC-2 validator run — proceeding to AC-3 anyway would knowingly create a new incident of the identical class this row was minted to close.
**why-change:** AC-3 deferred vs completed as literally instructed — STRICT ORDERING's own stated rationale (do not strand a legitimate agent's next notebook commit) applies symmetrically to the newly-discovered 4th file; recorded as a residual/follow-up rather than silently forced through.
**verify:** `git grep -noE` full-UUID regex against all 3 named files' working tree = 0 matches (was 6). `verify-notebook-uuid-provenance-gate.sh --all-history --file <each>` = 0/0 for all 3 (agent-father.md scanned 212 commits, dev-team.md 59, qa.md 930). Fleet default scan (54 files, last 8 commits each) = 5 hits total, ALL in tran-ngoc-bau.md, 0 elsewhere. `bash -n` both touched-adjacent scripts (pre-commit, validator) clean — neither was actually edited this row. No `apps/` TS/Go touched — `bun test`/`tsc` N/A.

### STEP developer-S52 · developer · 2026-08-15T09:46:55Z
**task-id:** FIX-ORCHBACKLOGSTUB-COLD-ITEMS-ARRAY-SHAPE-CRASH-BLOCKS-LANES-MIGRATION
**what-done:** AC-1 fixed `build_detail_temp()`'s crash (`array (...) and object (...) cannot be multiplied`) by shape-normalizing cold `.items` via `detail_items_from()` (reused via jq `include`+`-L`, not hand-rolled) before the F-3 per-field merge; AC-2 whitelisted `^po_goahead` by prefix in `build_hot_temp()` so a ratification stamp is never silently stripped; AC-3 added T8 (array-shaped cold input + id-less legacy record preservation) + T9 (po_goahead survival) to `orch-backlog-stub.test.sh`, RED-verified pre-fix; AC-4 re-ran the PO-specified scratch rehearsal against a byte-identical copy of the LIVE files (LANES=backlog,ready,review) — exit 0, reconciliation PASS, all 6 live `po_goahead_*` rows + the 1 id-less legacy cold record verified byte-preserved.
**what-considered:**
- Cold `.items` output shape: rewrite to OBJECT (id-keyed) vs keep ARRAY. OBJECT would match the pre-existing tests' assumptions with less diff, but (a) it's not the live shape, (b) po-detail-resync-review-lifecycle-routing.sh's own `.items | map(...)` write unconditionally re-serializes as array on its next matching run regardless of what this script wrote, so OBJECT would not even be stable, (c) an OBJECT map has no key to hold the one real id-less legacy cold record, destroying it on round-trip. Chose ARRAY — the only option consistent with both other writers and lossless.
- Reuse `scripts/lib/devteam-eligibility.jq`'s `detail_items_from()` via `include` (needed `-L "${REPO_ROOT}"` since jq's include-path is CWD-relative and this script is designed to be invoked from any CWD) vs hand-roll a second shape-check inline — chose reuse per the task's own explicit instruction and this repo's documented history of hole-by-hole duplication defects on this exact predicate.
- Also fixed the adjacent, previously-flagged-not-fixed reconciliation bug (`.items | keys` on an array yields indices not ids, silently defeating the post-write hard gate) — in scope because AC-4 requires reconciliation to actually PASS against the real live shape, not just "exit 0 with no crash."
**why-decision:** each of the 3 candidate writers/readers of this file (this script's 2 branches + po-detail-resync-review-lifecycle-routing.sh) needed to agree on ONE stable on-disk shape or the file would keep flip-flopping between runs; array is the only shape all 3 already tolerate/produce and the only one that doesn't destroy the id-less legacy record.
**why-change:** no change from the task row's own framing — it explicitly asked for a shape decision + doc + reuse of `detail_items_from()`, which is what shipped.
**verify:** `bash scripts/orch-backlog-stub.test.sh` 35/35 pass (25/25 baseline + T8×5 + T9×4 new, all RED pre-fix, T4c added to prove the shape decision on the existing merge-branch fixture too). AC-4 scratch rehearsal (read-only copy of `docs/data/orch/orch-state.json` + `docs/data/orch/archive/backlog-detail.json`, `LANES=backlog,ready,review`): pre-fix exit 5 (`git`-confirmed crash reproduced verbatim per PO's repro recipe), post-fix exit 0, `Reconciliation PASS — all hot stub ids confirmed in cold detail`, hot file 3455546B→1209788B. `shellcheck scripts/orch-backlog-stub.sh` clean. No `apps/` TS/Go touched — `bun test`/`tsc` N/A (pure bash/jq, zone `cross-service/` = no dev-* specialist, handled directly per `zone-detect` Tier-2/3 fallback).

### STEP developer-S53 · developer · 2026-08-15T10:03:00Z
**task-id:** FIX-FB-GATE-CHECKD2-NONWAIVABLE-NUMERIC-BLOCK
**what-done:** QA's CHANGES_REQUESTED root-cause was confirmed correct: `scripts/test-fb-gate-checkd2-nonwaivable.sh` still hardcoded `MAIN_FLOW` at `flow/main.md` (assertions 3b/3c), false-RED (8/10) — unrelated commit `8d165e8d6` (agent-father main.md/daily.md split) had relocated the real STEP 4b NON-WAIVABLE block into `flow/daily.md` verbatim, confirmed present there before editing anything. Repointed `MAIN_FLOW` + the two assertion labels at `daily.md`; no flow-doc change.
**what-considered:** only path — QA's note already isolated the exact 3 line numbers and confirmed the substance fix (`1b506cbdd`) intact; re-deriving root cause from scratch would duplicate already-verified work.
**why-decision:** N/A — single unambiguous fix, test-pointer-only.
**why-change:** no change from QA's own fix-needed spec.
**verify:** `bash scripts/test-fb-gate-checkd2-nonwaivable.sh` 10/10 GREEN against live HEAD. `bash scripts/test-fb-gate-checkc-negation.sh` 6/6, no collateral. Committed via commit-mutex (gateway-blind in this Task-tool spawn — no `mcp__gateway__call_tool`/`mcp__semble__search` present, confirmed via 2 direct probes both `No such tool available`; used the documented Bash-transport fallback `scripts/agents-flow/mcp-call.sh` against the vn-market endpoint directly, per `feedback_local_cowork_subagents_gateway_blind`): commit `c678ef57e` (script only) + `007b30077` (orch-state.json review[]→qa[] move), both pushed to origin/main. No `apps/` TS/Go touched — `bun test`/`tsc` N/A.

### STEP developer-S54 · developer · 2026-08-15T10:16:22Z
**task-id:** FU-RAG-DEPLOY-MEMORY
**what-done:** raised `docker-compose.yml` rag-service `deploy.resources.limits.memory` 1g→2g / `reservations.memory` 512m→1g per PO's D1-D5 sizing decision (measured ~1002 MiB flat-ceilinged peak, 2.13% margin to OOMKill on the prior 1g cap); single-service redeploy (`docker compose build rag-service && docker compose up -d --no-deps rag-service`); RAW-verified live (`docker inspect` Memory=2147483648/MemoryReservation=1073741824, healthy, all 12 peers untouched, clean logs/health 200).
**what-considered:**
- Reservation sizing: keep the pre-existing 50% limit:reservation ratio (512m/1g→1g/2g) vs pin reservation to the measured ~1002MiB working set. Chose the proportional-ratio option — the row's AC-1 only specified the limit (>=2x peak) and "keep the reservation proportionate", not an absolute reservation figure; inventing a new ratio would be scope creep the simplicity gate would flag.
- `--no-deps` on the up command: row's own AC-2 text only says `docker compose up -d rag-service`, but rag-service has no `depends_on` entries (verified via `docker compose config`) so `--no-deps` is a no-op safety belt matching this exact row's own 2026-08-06 precedent (`po_RULING_no_naked_restart_20260806T1245`/prior `review_note`) — kept for defense-in-depth, zero behavioral difference measured.
**why-decision:** the row itself (PO re-scope 2026-08-15T10:02Z) explicitly distinguished this raise from the earlier de-certified 768m→1g one — that one chased an unbounded workload (unfixed redundant index rebuild + non-returning allocator), this one sizes against a measured, flat, bounded peak with a real 5657 MiB VM headroom margin — so a straight proportional cap raise, no structural code change, is the correct minimal action.
**why-change:** no change from the row's own AC-1/AC-2 spec. AC-3/AC-4/AC-5 (fresh D1-D5 durability window, VM-internal dmesg, persisted sampler) deliberately NOT attempted — explicit dispatch-scope boundary (needs hours of accrual, separate future task), documented on the board row's `verify_note` rather than silently dropped.
**verify:** `docker compose config -q` valid pre-edit. Post-deploy `docker inspect`: `Image=sha256:24a8e2949611b9...`, `Memory=2147483648`, `MemoryReservation=1073741824`, `State.Status=running`, `Health=healthy`, `StartedAt=2026-08-15T10:16:22Z`. `docker compose ps` (all 13 containers): 12 peers' uptimes unchanged (alert-engine 46h/api-gateway 45h/mcp-server 2h/pdf-extractor 28min/stock-price 8d/etc.), only rag-service recreated. `docker logs --since 30s`: single clean startup, lazy-load embedder message, `/health 200 OK` x3, zero error/OOM signature. Board write raced with a concurrent peer's cold-evict cron commit (`d52087319`) — my `git add`+`git commit -- <pathspec>` staged content was swept into that peer commit rather than landing under my own message (documented TOCTOU race class, `feedback_concurrent_commit_race.md`); verified post-hoc the content IS correct and un-duplicated in that commit's diff (exactly 1 `FU-RAG-DEPLOY-MEMORY` row, in `review[]`, all my field changes + note present). No `apps/` TS/Go touched (root `docker-compose.yml` + board only) — `bun test`/`tsc` N/A. Simplicity gate: 2-line YAML value change, no new abstraction/flag/branch, all 4 questions NO.

### STEP developer-S55 · developer · 2026-08-15T13:25:57Z
**task-id:** FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR
**what-done:** stale `review[]` row (branch:null, direct-commit) SECONDARY-Drain-routed to developer for sign-off/triage. RAW-verified (not trusted from the row's own prose) that the fix it describes ALREADY landed under THIS EXACT task_id in commit `b27ba6507` (2026-08-14T04:17:20+02:00), 45s before `f08bb1c2d` moved the row IN_PROGRESS→REVIEW — no duplicate/companion row exists (searched orch-state.json + `git log --all` for the router-flagged `-2026-08-14` suffix, found nothing). Confirmed all 3 ACs live in code: AC-1 `orch-conservation-check.mjs` dropped `pending_triage_inbox` from `signalTotal()` and added an independent `undeclaredInboxDrops()` guard on `ORCH_APPLY_DECLARED_INBOX_TRIAGED`, wired into `dev-team/flow/main.md` Step 1's `consumed_ids_csv`; AC-2 abort message names the correct env var and explicitly forbids `ORCH_APPLY_ALLOW_SHRINK`; AC-3 `INBOX-FULL-DRAIN-DECLARED` regression (29-envelope fixture) present in `scripts/test/orch-apply-wrapper-tests.sh`. Disposition: DONE_VERIFIED, `review[]→done_verified[]` via `scripts/orch-apply.sh`.
**what-considered:** trust the row's own status_note vs independently re-derive from source. Chose full RAW re-derivation per the router's explicit instruction not to assume duplication either way — cross-checked the commit's own `Task:` trailer, diffed the actual `.mjs`/`.sh`/`.md` file contents at HEAD (not just the commit message), and re-ran the test suite live rather than trusting the commit message's self-reported pass count.
**why-decision:** code-level verification (grep for `ORCH_APPLY_DECLARED_INBOX_TRIAGED`/`undeclaredInboxDrops` at HEAD, byte match against the commit diff) plus a live 89/89 test re-run plus 8 real post-fix production durable-inbox-CLEAR commits (single-write, no bypass) together constitute independent, non-prose-trusting evidence for all 3 ACs — the bar this project's memory repeatedly flags failing (`feedback_auditor_resumed_run_narrates_writes_never_executed`, `feedback_flow_doc_veto_manufactures_pass_read_spec_before_blaming_script`).
**why-change:** no change from the row's own AC-1/AC-2/AC-3 spec — this cycle made no code change, purely a verification+board-closeout sign-off.
**verify:** `bash scripts/test/orch-apply-wrapper-tests.sh` 89/89 PASS live, including `INBOX-FULL-DRAIN-DECLARED` (AC-3), `INBOX-DROP-UNDECLARED-REJECTED` (undeclared wipe still hard-rejected, exit 1), `INBOX-DROP-ALLOW-SHRINK-NO-BYPASS` (orthogonality preserved, exit 1). `git log --all --oneline` scan confirms 8 post-fix single-write durable-inbox CLEAR commits 2026-08-14/08-15 (44/12/9/33/17/29 envelopes among them), zero re-occurrence of the pre-fix 4-write-sub-batching pattern. `grep -n ORCH_APPLY_DECLARED_INBOX_TRIAGED scripts/orch-conservation-check.mjs docs/agents/dev-team/flow/main.md` confirms both the guard and its sole declaration site are live at HEAD, byte-consistent with `b27ba6507`'s diff. No `apps/` TS/Go touched — `bun test`/`tsc` N/A (board-write-only cycle).
