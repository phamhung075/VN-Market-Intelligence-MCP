# PO decision journal — triage tick 2026-08-24T01:07Z (run 01:27–01:40Z, Review-Lane SECONDARY-Drain)

task_id: `UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW`

## Decision 1 — signed the row off to `done_verified[]` rather than re-opening it as actionable

**what-considered:** (a) accept the dispatching brief's framing that AC-1 is now satisfied by fresh measurement but D1-D5 remains an open question, and route the measurement; (b) re-run the D1-D5 window myself against the current container; (c) read the row's own terminal record first.

**why-decision:** Took (c). The row was already closed at `2026-08-15T10:02:53Z` by `po_RULING_20260815T1002Z`, which adjudicates all five ACs explicitly. The window it was said to be waiting for **had already run**: `2026-08-14T20:19:20Z → 2026-08-15T09:35:54Z`, 13.2h, 799 samples, series committed at `docs/incidents/data/rag-durability-2026-08-14T2015Z-66656926d503-samples.csv` (807 lines), with per-criterion verdicts attached to `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` in `durability_verdict` / `durability_samples` / `durability_growth_pp_per_min` / `durability_dmesg_query`. Option (b) would have re-measured something already measured and, worse, would have needed a restart that resets the second window currently in flight.

**why-change:** The brief was right that AC-1's satisfaction does not imply D1-D5 ran — that is a genuinely independent question and I checked it independently. It just resolves the other way.

**Confirmed at source, not relayed** (`verification.raw_probe` on the row): live image `sha256:24a8e2949611…` ≠ AC-1's pre-fix `sha256:4a955869f002`; `StartedAt=2026-08-15T10:16:22Z`; `RestartCount=0`; `HostConfig.Memory=2147483648`. Note this is a **third** image, not the ruling-era `sha256:26e681b0ca` — AC-1 only ever required inequality to the pre-fix hash, so the ruling stands.

## Decision 2 — deleted two fields from the row instead of adding a third correction above them

**what-considered:** (a) append the signoff and leave the history intact; (b) delete `status_note` and `po_triage_20260815T0040_reaffirm`.

**why-decision:** Took (b). Both are declared FALSE **verbatim** by the row's own `po_premise_correction_20260815T0915Z`, which has sat there since 2026-08-15 — and on 2026-08-24 a dispatcher still read the `status_note` as live and re-derived "the deploy has not happened" from it. A retraction stored *below* the claim it retracts does not stop the claim from being read. Their text survives quoted inside the correction field, so nothing is lost. This is the failure mode, not a cosmetic one: the row was never blocked by missing work, it was blocked by its own stale prose.

**why-change:** No change from plan; the deletion was not in the brief but follows directly from what the brief itself demonstrated.

## Decision 3 — self-corrected a false claim I had just written into that same row

**what-considered:** (a) leave it, it is a minor parenthetical; (b) retract it in place with a second `orch-apply` write.

**why-decision:** Took (b). My first signoff draft asserted the deletion was *forced* by `ORCH_ROW_PROSE_CEILING_BYTES=12000` because the row measured 13692B. That is false: `scripts/orch-row-prose-ceiling-check.mjs:105` sets `PROSE_CEILING_LANES = ['backlog','ready','review']`, so `done[]`/`done_verified[]` rows are never measured and no shrink was required. Writing a plausible-but-wrong technical premise into row prose is precisely what Decision 2 exists to clean up; leaving one behind in the same write would be self-refuting.

**Coverage observation, deliberately not minted:** the terminal lanes hold the board's longest rulings and are entirely outside that guard. That is a documented scoping decision (the brief measured only the three flat lanes), not a defect, so it is recorded here rather than filed.

## Decision 4 — cleared `supervised:true` on `FIX-OCRGATEWAY-INFLIGHT-BOOKKEEPING-DIVERGES-OS-TRUTH` but held it at P0

task_id: `FIX-OCRGATEWAY-INFLIGHT-BOOKKEEPING-DIVERGES-OS-TRUTH`

**what-considered:** (a) leave `supervised:true` as BOUNDED-1's revert note asked; (b) clear it and downgrade to P1 since the acute symptom subsided; (c) clear it and hold P0.

**why-decision:** Took (c). The revert note's premise is false in both halves. Its inertness claim — "supervised-without-plan_only has no sweep lane, which is the correct inert state" — is backwards: that is exactly `is_backlog_xor_gap` in `scripts/lib/po-manual-dispatch-eligibility.jq`, swept on **every** PO tick by `docs/agents/po/flow/manual-dispatch-sweep.md`. Setting the flag promoted the row to **#1 of 109** candidates ten minutes later. Its contraindication — "dev-pdf-extractor is already investigating this as part of its restart-loop dispatch" — has no referent: zero rows in any lane carry a pdf-extractor restart-loop investigation, and the container is stable (`StartedAt=2026-08-23T15:44:17Z`, `RestartCount=0`, `ExitCode=0`, ~9h45m). There is no second agent to avoid because there is no first agent.

**why-change:** Rejected (b) on principle. The symptom is milder — 20 `ocr_gateway.inflight` mismatch lines and 22 HTTP 429s against 418 `/extract` posts in the last 10h, versus 59 mismatches and 90/199 (45%) 429s in the window that minted it — but it still reproduces, and I have no reading on whether consumer impact cleared. Downgrading on that basis would be scoring on *absence of measurement* rather than on measurement. Left an explicit instruction on the row: re-measure the 429 rate first, re-rank on that number.

## Decision 5 — re-rated the TNB c133 CRITICAL instead of actioning the aggregate

**what-considered:** (a) accept CRITICAL and escalate all four items; (b) verify each of the four independently, then re-rate.

**why-decision:** Took (b), as instructed, and it split 3-real / 1-refuted. Confirmed: L5 false-gap-claim (`get_market_hexagram()` live at 01:30Z returns Hexagram 15 Khiêm / THUẬN LỢI / TIÊU CỰC / 64% while the 08-23 dish's `.known_gaps` carries `[gap:L5_kinhdich_unavailable]` and 08-22's does not); `causal_chains[]` regression (08-22 `chains=2 / conviction=0` vs 08-23 `chains=0 / conviction=0`, which kills the "zero clusters ⇒ empty chains" defence at source); fleet push blocked (252L vs 250L upper, ahead 114). Refuted: qa-responder orphaned — `cronConfig.ts:39` `*/12 * * * *`, registered at `schedulerJobTable.ts:1150`, `qaResponderSpawner.ts:62` returns `no_pending` on an empty queue, and `docs/architecture-briefs/2026-05-18-cowork-team-command.md:184` exempts it from cowork verbatim per AC-7.

**why-change:** Re-rated to **NEEDS_ATTENTION fleet-wide, CRITICAL upheld for the chef-dish methodology dimension only**. The 3/7 methodology score flipped on the C-step, which *is* the confirmed `causal_chains` item — so that rating is sound and should keep driving c134. But qa-responder was one of only two HIGHs carrying the fleet aggregate, and both surviving HIGHs already have owned board rows, so nothing is unowned. Verified all three c133 mints actually reached `backlog[]` rather than trusting the prior ACK's claim that they were created.
