---
session_id: pm-s317
sprint_id: ARCH-OHLCV-WRITER-SSOT-DURABLE
timestamp: 2026-06-17T05:30:00Z
agent: pm
---

# PM Decisions — 2026-06-17
# ARCH-OHLCV-WRITER-SSOT-DURABLE Atomization

---

## DJ-GATE-1: Architect design → 3 P0 subtasks atomization + parent normalization

**Parent task:** ARCH-OHLCV-WRITER-SSOT-DURABLE (recurring bug escalation, 4th recurrence)

**Parent design state:**
- Architect delivered: `docs/handoffs/ARCH-OHLCV-WRITER-SSOT-DURABLE-architect-design.md` (200L, final design + §6 SUBTASK-1..5 outline)
- Architecture brief: `docs/architecture-briefs/2026-06-17-ohlcv-writer-ssot-durable.md` (100L, writer-bypass class closure rationale)
- Parent task status: REVIEW → **DESIGN_COMPLETE** (normalized to done lane)

**Decision:** Parent task is a DESIGN deliverable, not an implementation task. The design is shipped (handoff + brief). Implementation now lives in 3 atomic child subtasks (P0) + 2 follow-ons (P1, backlog). Parent moved from ready/REVIEW → done/DESIGN_COMPLETE to signal: "design is done, do NOT re-design."

**Rationale:** Recurring-bug-escalation class requires architect closure first. Architect delivered. Implementation is PM's job (atomization → child tasks for dev). Parent row must not remain in ready[] as a re-dispatch hazard.

---

## DJ-GATE-2: 3 P0 subtasks created + ready to dispatch

**SUBTASK-1: Rewrite writeForeignFlowToOhlcv — merge-only UPDATE (no stub INSERT)**
- **Handoff:** `docs/handoffs/SUBTASK-OHLCV-WRITER-1-FOREIGN-FLOW-MERGE.md`
- **Scope:** Replace `INSERT … ON CONFLICT` stub with UPDATE-only in `ohlcvForeignFlowStore.ts` L57-69
- **Critical:** Verify BOTH callers (foreignFlowFetcher, pushForeignFlowHandler) treat `changes=0` as non-error
- **Size:** S (~2h)
- **Rebuil:** YES

**SUBTASK-2: Add SSOT writer-bypass annotation to ohlcvWriteService.ts**
- **Handoff:** `docs/handoffs/SUBTASK-OHLCV-WRITER-2-SSOT-ANNOTATION.md`
- **Scope:** JSDoc annotation documenting exhaustive writer inventory (8 writers A–H) + sentinel pattern
- **Note:** Documentation-only, no code logic changes; parallel with SUBTASK-1
- **Size:** XS (~30min)

**SUBTASK-3: Unit + integration tests T-1 through T-4 (regression + behavioral gate)**
- **Handoff:** `docs/handoffs/SUBTASK-OHLCV-WRITER-3-UNIT-TESTS.md`
- **Scope:** 4 unit tests (T-1..T-4, with T-4 as direct regression proof) + integration behavioral gate
- **T-4 REGRESSION PROOF:** After foreign-flow write on empty DB, `SELECT close FROM daily_ohlcv ... WHERE code=X AND date=D` returns ZERO rows (NOT a row with `close=0`)
- **Dependency:** Blocks on SUBTASK-1 merge (tests validate new SQL)
- **Size:** S (~2h)
- **Rebuild:** YES

**Decision:** All 3 READY, no cross-dependencies between SUBTASK-1 & SUBTASK-2. SUBTASK-3 depends on SUBTASK-1. Dispatch pattern: dev-mcp-server can start SUBTASK-1 + SUBTASK-2 in parallel (2 coding lanes). After SUBTASK-1 merges, SUBTASK-3 unblocks. Total estimated: 4–5 developer-hours.

---

## DJ-GATE-3: Router 60s re-fetch finding carried verbatim

**Finding:** CRONS.foreignFlowFetch fires **every 60 seconds** (confirmed startupHelpers.ts:219 + startScheduler.ts:840).

**Why this matters:** The architect's risk flag R-1 mentions "2–3h data-loss window," which correctly describes the WINDOW but overstates the PROBLEM. The fix is still correct because:
- Re-fetch is guaranteed within 60s
- The moment the real OHLCV bar lands (~03:00Z), the next fetch UPDATEs foreign-flow columns
- Same-day final-value loss ≤ ~60s
- Honest gap (no row) beats fake `close=0` stub

**Decision:** Carry this finding verbatim to dev in the workorder (WORKORDER-dev-mcp-server-OHLCV-WRITER-SSOT-DURABLE.md) so dev does NOT misinterpret R-1 and re-introduce compensating stub hacks. The producer fix (merge-only UPDATE) is definitive for this sprint.

---

## DJ-GATE-4: 2 follow-on tasks queued (backlog, P1, not P0)

**LINT-OHLCV-WRITE-BYPASS**
- Custom ESLint rule enforcing `/* OHLCV-WRITE-BYPASS-ALLOWED */` sentinel pattern
- Depends on SUBTASK-1
- Scope: P1, backlog (NOT in_progress)

**ARCH-DAILY-FOREIGN-FLOW-TABLE**
- Separate staging table to eliminate R-1 data-loss window entirely
- New ARCH task (requires architect design pass)
- Scope: P1, backlog (NOT in_progress)

**Decision:** These are deferred. SUBTASK-2 JSDoc + future ESLint rule (backlog item) will prevent a 5th writer from reintroducing the bug class. The separate table is the durable solution but too large for P0 scope.

---

## DJ-GATE-5: Board mutation (atomic, explicit path)

**Before:**
- ready: 3 (FIX-ALERT-SCAN-REJECT-STUB-BAR-P0, ARCH-OHLCV-WRITER-SSOT-DURABLE, DESIGN-GATHERER-DOUBLEFIRE-DEDUP-CLUSTER)
- in_progress: 1 (ARCH-CRON-SCHEDULER-RELIABILITY)
- backlog: 294
- done: 162

**After:**
- ready: 5 (3 old + 3 new SUBTASK-* removed parent + 1 added back = 5 - 1 + 3 = 5 ✓)
- in_progress: 1 (unchanged)
- backlog: 296 (294 + 2 follow-ons)
- done: 163 (162 + 1 parent)

**Parent task normalization:**
- ARCH-OHLCV-WRITER-SSOT-DURABLE: ready/REVIEW → done/DESIGN_COMPLETE
- Set: normalized_at, normalized_by, note="Design deliverable shipped; implementation in child subtasks"

**Method:** Atomic read → modify → write (no interactive edits). Staged only orch-state.json + this DJ entry + PM notebook update.

---

## DJ-GATE-6: Shared verification gate (both P0s together)

**When:** NEXT VN market open 2026-06-18, first TA scan 02:15Z (NOT 04:30Z, self-heal masks stubs)

**What:** Both P0s together (writeForeignFlowToOhlcv producer fix + FIX-ALERT-SCAN-REJECT-STUB-BAR-P0 consumer guard):
1. **RSI canonical match:** get_unreviewed_market_messages → no single-digit RSI, no 100.0, within 0.1pt
2. **No BB/gia-0 spam:** zero "giá 0 dưới BB" / scale-wrong messages, all 30 tickers
3. **Live DB probe:** daily_ohlcv via named-volume 02:00–03:30Z, ZERO `close=0` stubs on latest bar
4. **Generic:** all tickers, no per-ticker allowlist

**Decision:** Verification is a LIVE RAW probe (not a badge). Ops + QA gate it at market open. PM/router watches.

---

## PM Notebook Entry

Added to `docs/agent-memory/notebooks/pm.md`:

```
## c317 ARCH-OHLCV-WRITER-SSOT-DURABLE atomization · 2026-06-17T053000Z

**PARENT:** ARCH-OHLCV-WRITER-SSOT-DURABLE (recurring escalation, 4th recurrence, architect design FINAL)

**INPUT:** Architect handoff (`ARCH-OHLCV-WRITER-SSOT-DURABLE-architect-design.md`), brief (`2026-06-17-ohlcv-writer-ssot-durable.md`), router 60s re-fetch finding (do NOT justify stub hacks)

**OUTPUT:** 3 P0 subtasks (SUBTASK-OHLCV-WRITER-1/2/3) added to ready[]; 2 P1 follow-ons (LINT-OHLCV-WRITE-BYPASS, ARCH-DAILY-FOREIGN-FLOW-TABLE) added to backlog; parent task normalized ready/REVIEW → done/DESIGN_COMPLETE; 1 workorder doc (WORKORDER-dev-mcp-server-OHLCV-WRITER-SSOT-DURABLE.md); 3 handoff files (SUBTASK-*-*.md).

**Board mutation (atomic):**
- ready: 3 → 5 (removed parent, added 3 subtasks)
- backlog: 294 → 296 (added 2 follow-ons)
- done: 162 → 163 (parent moved)
- in_progress: 1 (unchanged, ARCH-CRON held)

**Sequencing:** SUBTASK-1 + SUBTASK-2 parallel (ready now); SUBTASK-3 after SUBTASK-1 merges. Dev-mcp-server WIP = 2 max (at limit with FIX-ALERT-SCAN-REJECT-STUB-BAR-P0). Both P0s gate on shared verification 2026-06-18 02:15Z market open (RSI canonical match, no BB spam, zero close=0 stubs on live DB).

**Decision journal:** DJ-GATE-1..6 documented above. Router 60s re-fetch finding carried verbatim to prevent dev stub-hack compensations.
```

---

## Summary

- Parent ARCH design delivered → normalized to done
- 3 P0 subtasks atomized + ready to dispatch
- 2 P1 follow-ons queued (backlog)
- Workorder + 3 handoff docs created
- Router 60s re-fetch finding documented (prevents misinterpretation of R-1)
- Shared verification gate (2026-06-18 02:15Z market open) gates both P0s + dispatch criteria clear

Next: dev-mcp-server accepts workorder, starts SUBTASK-1 + SUBTASK-2 in parallel.
