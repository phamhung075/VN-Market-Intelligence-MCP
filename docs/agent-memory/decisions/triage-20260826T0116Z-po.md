# PO Decision Journal — triage-20260826T0116Z

**Cycle:** 2026-08-26T01:24:01Z
**Agent:** po
**Scope:** adjudicate FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM + drain 16-envelope triage inbox

## D1 — task_id: FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM

**Decision:** UNBLOCK, sever the depends[] on the ops measurement row, re-route to architect with zone+files, prepend to ready[]. Primary remedy = ProcessPoolExecutor worker recycling (max_tasks_per_child). Cap raise REJECTED. Call budget DEMOTED to safety-belt.

**what-considered:** four candidate paths were on the table — (1) raise the 2.5 GiB cap, (2) explicit per-document/per-batch PaddleOCR call budget, (3) root-cause the retention, (4) something better. All four were evaluated against source read live this tick.

**why-change:** the row was blocked on the WRONG gate. Its `depends` pointed at UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT's B1-B4 measurement, but the fix never needed that measurement to be authored. Two independent questions had been serialized for 18 days. Root cause read at source, not inferred: `main.py:154` builds `ProcessPoolExecutor(max_workers=1)` with no `max_tasks_per_child`, so its single child is immortal for the container life; `ocr_worker.py:107/209/222` caches a module-global PaddleOCR(lang=vi) inside that immortal child; and BOTH shipped malloc_trim(0) mitigations are wired only into PID 1 (`pek_run_helper.py:133`, `routes_extract.py:112`), so the executor child has zero mitigation. That is why fire-count-proportional growth survived the malloc_trim fix.

**rejected — raise the cap:** per-container caps already sum ~13.5 GiB against 7.75 GiB host RAM (1.74x overcommit, documented host-memory-panic history). Converts a bounded container OOM into an unbounded host OOM.

**rejected as primary — call budget:** rations a leak rather than removing it, and silently degrades the exact capability the work exists to deliver (budget exhausted = the page tesseract blanked stays blank). Kept as a safety-belt shipped alongside the real fix.

**verified not assumed:** `python3 -V` in the live container returns 3.12.3, so `max_tasks_per_child` (3.11+) is available.

## D2 — task_id: FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS

**Decision:** CLOSE to done[]. Measurement complete, negative result valid and final. No further discriminator work authorised.

**why-change:** the architectural question this row routed to PO turns on a distinction the row did not draw — there are TWO fallback mechanisms, not one. The CONFIDENCE-gated `AutoFallbackOcrBackend` must CLASSIFY broken vs legitimate-but-sparse, and that is what failed (~0.4% base rate; line_ink_cov FPT p9 0.763 above DBC legit floor 0.688). The CHARACTER-COUNT rescue at `ocr_worker.py:455-495` only has to COMPARE two outputs and keep the longer, so a false fire costs latency and never costs correctness. (B) is already shipped and is the sanctioned bounded-rescue path; (A) is retired.

## D3 — task_id: FIX-MARKETDB-20260826-RESTORE-DROPPED-12205-FF5M-AND-54-EVIDENCE-ROWS-STILL-RECOVERABLE

**Decision:** mint P0, owner ops. ACK-WITH-CORRECTION on ops recovery commit ea8650291.

**why-change:** the recovery itself was correct, but its commit message asserts "no uncommitted data lost", which is false and will be trusted by future readers. Measured read-only, live db vs the preserved 00:31Z snapshot: intraday_foreign_flow_5m 137,890 vs 150,095 = 12,205 rows lost; evidence_fragments max id 1509 vs 1563 = 54 rows lost (exactly the writes the incident report recorded as having SUCCEEDED). The evidence_fragments b-tree in the snapshot is intact, so those 54 are cleanly recoverable today — but only until the snapshot is pruned, and 5+ older corrupt snapshots are already sitting there as prune candidates.

**what-considered:** whether the live `quick_check = ok` was restart-masked, per the cowork-team envelope's explicit warning that this is likely how 07-19/07-30/08-06 were each falsely closed. Ruled out by running the SAME check against the preserved snapshot, which DOES report the damage — the instrument is demonstrably non-blind.

## D4 — inbox drain

**Decision:** 16 envelopes triaged, then CLEAR. 4 envelopes (news-scout, bctc-analyst, dev-pdf-extractor, cowork-team) folded as ONE incident per the reporters' own dedup instruction. 3 envelopes (qa follow-up, router scope amendment, sweep outcome) merged into ONE corpus-sweep row at the router's 79-file/312-page scope, not the qa signal's understated 2-file scope. 2 context_bloat_breach on a live sprint journal → DEFER per standing policy, no mint. 4 bctc_signal routine + 1 clean cowork-fire → log only. 1 recurring-bug and 1 flow-defect → folded/minted.

**only path:** dedup ran via `scripts/po-board-dedup-search.sh` (jq-PATH-resolving, task_board-prefix-required) rather than grep, per the recorded id-collision defect.
