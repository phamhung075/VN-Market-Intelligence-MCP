# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · po

**Sprint goal:** continuation of `-po-2.md` (rolled at 34376B/36000B — pre-emptive, per that file's own carry-over instruction; the next entry would have breached and re-fired `context_bloat_breach`)
**Agent:** po
**Started:** 2026-07-31T02:28:00Z

---

### STEP po-S82 · po · 2026-07-31T02:28Z
**task-id:** po Step 0-SIG triage — single ci_red signal CI-RED-40dfb1f9 (signals.db id 3732)
**what-done:** DECLINED to mint `CI-RED-40dfb1f9-FIX`. Ran the by-hand failing-FILE read the prior tick's carry-over mandates (`gh run view 30598372356 --log-failed`) and mapped every failure 1:1 onto four already-open rows, then escalated five rows to P0 via `orch-apply.sh` (740→740 tasks, 131→131 signals, Stage 0+1 PASS, 5 rows stamped): `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` P1→P0, `FIX-CI-SIZELINT-MACRO-VMT-LIQUIDITY-RESOLVERS-NEW-OFFENDER` P1→P0, `FIX-CI-SIZELINT-MCPSERVER-SIX-UNCOVERED-OFFENDERS` P2→P0, `FIX-CI-SIZELINT-PDFX-EXTRACTION-ENGINE-TOLERANCE` P3→P0, `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` P1→P0. Each carries an evidentiary `status_note` naming the run, the offender numbers, and the sibling row ids.
**what-considered:**
- Apply `triage-signals.md §ci_red` dedup literally: layer 1 (title/id contains `CI-RED-40dfb1f9`) → 0 hits; layer 2 (status_note contains head_sha, status IN_PROGRESS/REVIEW) → 0 hits. The letter of the rule says MINT. Rejected: both layers are SHA-derived, and `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` already states in its own title that they "can never match across SHAs". Minting would have made this the 5th redundant row for one unfixed cause.
- Trust the spawn brief's board read ("NO open backlog/in_progress FIX task targeting frontend-eslint or size-lint"). Rejected — re-grepped every lane myself: the brief only swept `backlog[]`/`in_progress[]`, and the two most load-bearing owner rows live in `ready[]` (idx 49, 50). Same class as `feedback_po_notebook_mint_never_reaches_orchstate_board`.
- Escalate on a "red CI blocks the queue" argument. Rejected after checking: only 5 board rows carry a ci-green verification_gate and 4 of them ARE the CI rows, so that premise is not supported by the board.
- Widen `FIX-CI-SIZELINT-MACRO-VMT-...`'s `verification_gate` from `ci_green_on_subsequent_push` to its two siblings' `size_lint_file_level_then_ci_green`. Rejected for this tick — flagged in its status_note instead; gate widening needs an actuator dry-run first (`feedback_gate_widening_recommendation_requires_actuator_dry_run`).
**why-decision:** The failing-LOG read is decisive at the MECHANISM plane, not the job-name plane. frontend-eslint dies at `bun install --frozen-lockfile` with "lockfile had changes, but lockfile is frozen" — verbatim the dual-lockfile drift `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` owns. size-lint reports 8 offenders that partition exactly 1/6/1 across the three size-lint rows, including `extraction_engine.py baseline=208 actual=237 upper=228` byte-identical to the CI-RED-8e1e66e5 observation 54 min earlier. Zero new root causes ⇒ zero new rows. The real defect is non-convergence, not under-detection: the four owner rows sat at ready-idx 49/50 (P1, behind 19 P0s) and backlog-idx 371/372 of 374 (P2/P3, behind 20 P0s) — `priority_rank` then array index is the live sort key in `scripts/lib/devteam-eligibility.jq:121` and both promoters, so the size-lint conjunction had a structurally unreachable member and could never close. Priority is the in-mechanism lever; array reordering is not, so I moved rank only.
**why-change:** Escalated the meta row too, which the brief did not ask for. It is the only row that stops the loop-burn: 5 fires (07-30T17:45Z, 07-31T00:15Z/00:45Z/01:20Z/02:20Z) across 5 distinct head_shas with an identical failing-job set and identical 8-file offender set, each costing a full dev-team drain + PO triage tick and producing zero dedup hits. Fixing the red without fixing the dedup key just resets the same churn on the next red. Also worth recording against the prior tick's carry-over: `bun test` is now GREEN at this run, so `FIX-ORCHSTATE-HEAD-STAMP-DROPPED-CI-RED-1837A` is no longer part of the CI-green conjunction — it is down to frontend-eslint + the three size-lint rows.

### STEP po-S83 · po · 2026-07-31T04:59:55Z
**task-id:** FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY
**what-done:** Ratified the agents-architect brief at source (po_goahead_20260731T045857), cleared plan_only, routed next_agent po→agent-father; triaged 4 signals: 2 ci_red file-scoped dedup hits, 1 new mint (FIX-CI-SIZELINT-MCPSERVER-ENERGYTOOLS-NEW-OFFENDER), 2 context_bloat_breach deferred.
**what-considered:**
- Move the AMNESTY row review[]→ready[] so a lane sweep picks it up. Rejected: agent-father is OFF the DRS allowlist [architect,ba,pm,po,agents-architect], so ready[] + supervised=true would manufacture a READY-XOR/DRS-STRANDED row — the exact class manual-dispatch-sweep.md §Step 2 forbids creating ("additive stamp, never a lane move"). Dispatch goes through PO BATCH type=UNBLOCK instead.
- Stamp po_goahead on the architect's RAW-verify relay. Rejected per supervised-goahead.md §Step 2 — ratifying on a relayed verdict makes the gate vacuous. Re-derived the load-bearing claim myself by executing the brief's own AC-1 (gh run view RUNID --log-failed) against two live reds.
- Fold energyTools.ts into FDA-5 (the row whose commit af272fe1d caused it) instead of minting. Rejected: FDA-5's AC is about the estimate flag travelling in a structured field, not file size — folding silently widens a row already in review. Cross-referenced on FDA-5 for qa instead.
- Leave the sibling red (CI-RED-22bdf63b, undrained) for next tick as instructed. Rejected in part: I had already read its run log for the AC-1 evidence, so deferring the mint would have left main red an extra tick for a defect I could already name; the row's dedup_key will absorb that signal when it drains.
- Open a new row for the two context_bloat_breach signals. Rejected: FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE (ready, P1, agent-father) already owns "catch-all drops the signal's own `to` field"; these two are evidence for it, and both are live-sprint journals under the standing DEFER policy.
**why-decision:** Executing the unshipped brief BY HAND produced a materially different board than the shipped SHA-keyed rule would have: 2 correct dedup hits + 1 correct mint, versus 0 hits and either 2 duplicate rows or a blanket amnesty. It also surfaced that the "size-lint 3-way conjunction" note on the MACRO-VMT row went stale between 02:12Z and 04:10Z — run 30598372356 reported 8 offenders, run 30603458514 reported 1, and that 1 is MACRO-VMT's own file. That row could have turned size-lint green alone and its own status_note was telling readers it could not.
**why-change:** Also corrected the stale conjunction note and attached CI-plane per-file evidence to the two size-lint rows sitting in review[]→qa, which the dispatch did not ask for. Their files are provably out of the offender set (RAW offender-set diff across 3 runs + host wc -l), and orchStateSchema.ts clears via a size-justification header rather than a shrink — qa cannot derive either from a job conclusion, because size-lint as a JOB is still red on other files.

### STEP po-S84 · po · 2026-07-31T05:23:57Z
**task-id:** FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY (CHANGES_REQUESTED addendum) + po Step 0-SIG triage of 2 drained signals
**what-done:** Triaged both signals with 0 new mints. `ci-red-22bdf63b-20260731044716.json` → 3 FILE-scoped dedup HITS (bun.lock → FRONTEND-ESLINT row, usecases_vmt_liquidity_resolvers.go → MACRO-VMT row, energyTools.ts → ENERGYTOOLS row), each stamped with the verbatim offender/error line from a live `gh run view 30604839507 --log-failed`. `cowork-team-2026-07-31T05:06:38Z.json` (type=cowork-fire, to=dev-team, classification=FIRE, errors=[]) → SKIP, folded as third evidence instance into FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE. Then found and filed a CHANGES_REQUESTED-grade defect in the in-flight AMNESTY brief itself: stamped `po_changes_requested_20260731T0523` on the row and appended §8 ADDENDUM to `docs/architecture-briefs/2026-07-31-cired-triage-failedfile-dedup.md` (165→214L). orch-apply: Stage 0+1 PASS, 5 rows stamped, 741→741 tasks, 131→131 signals.
**what-considered:**
- Accept the dispatch's own framing that "this signal's dedup_key should now match those rows and dedup cleanly". Rejected — it cannot, by construction. The signal's `dedup_key` is `ci_red:<sha>:<jobs>` (probe-side, SHA-scoped); the rows' `dedup_key` is `ci_job:<job>|file:<path>` (PO-side, FILE-scoped). Different key spaces; the brief's own §4b says so explicitly. Matching on the signal's key would have produced 0 hits and 3 duplicate mints. Ran the FILE-scoped read instead.
- Trust last tick's notebook prose for the failing-file list rather than re-reading the run. Rejected — re-ran `gh run view 30604839507 --log-failed` live. Worth recording: the first invocation returned EMPTY and would have read as "no failing files" (`feedback_empty_read_is_not_evidence`); RC was 127 because macOS ships no `timeout(1)`, not because the log was clean. Re-ran without the wrapper: 2 verbatim size-lint offender lines + the frozen-lockfile error.
- Re-verify the FRONTEND-ESLINT row's file identity, which nothing asked for. Kept — the row has been READY and undispatched ~24.5h, so a wrong file identity would waste the dispatch. Confirmed at source: `.github/workflows/ci.yml:127-129` sets `working-directory: apps/frontend`, so the frozen lockfile is `apps/frontend/bun.lock` (234784B), and a competing `apps/frontend/package-lock.json` (487616B) is materially present — the "dual lockfile drift" is real, not inferred.
- Mint a new FIX row for the brief defect. Rejected — it targets the exact clause agent-father is editing right now; a second row would collide. Additive stamp + in-place brief addendum + a qa gate line instead. No lane move, no re-dispatch (dispatch explicitly warned against duplicating that dispatch).
- Silently fix the enum myself in `triage-signals.md`. Rejected — `docs/agents/*` is agent-father's zone and it holds the row; PO editing under a live implementer is the `subagent_branch_checkout_hijacks_shared_working_dir` class.
**why-decision:** The dedup_key mechanism works, but the brief hangs it off a predicate that is blind to most of the board. Measured on live orch-state at 05:23Z: 633 open rows across backlog/ready/in_progress/review/qa, of which only 238 carry a status in the brief's `TODO/IN_PROGRESS/REVIEW/BLOCKED` enum — 395 (62.4%) are invisible, because the dominant open tokens are BACKLOG (349) and READY (46) and neither is in the enum (`orchStateSchema.ts:420-428 LANE_ALLOWED_STATUSES`). All three of this tick's correct hits sit on READY/READY/BACKLOG rows, so an implementer following §3 literally matches zero and re-mints three duplicates — the rule would fail on exactly the case it exists to fix, and §2's headline positive control is only reproducible because I matched on dedup_key *without* the status filter. Same enum is in the `repair_task_request` row, so the blindness is pre-existing and the brief faithfully copies it forward. Fix: scan non-terminal LANES by name, drop the status-token enum. This is the `wrong_arg_type_silently_disables_a_verification_predicate` shape — a filter that never intersects its data disables the whole check while looking rigorous.
**why-change:** Also hand-dispatching all three CI rows in this tick's BATCH rather than only FRONTEND-ESLINT as last tick's carry-over planned. `in_progress[]` is empty (0 rows) and none of the three has ever been dispatched, so there is no double-spawn risk; main CI has been continuously RED and size-lint needs BOTH remaining rows to land — dispatching one of a two-member conjunction leaves the job red and burns another triage cycle on the same red.

### STEP po-S85 · po · 2026-07-31T06:57:25Z
**task-id:** FIX-NOTEBOOK-AUTOPRUNE-DIRECTION-UNRESOLVABLE-ZERO-TS-NOTEBOOKS (new) + po Step-1 dev-team triage of 4 drained signals
**what-done:** 4 signals triaged, 1 mint. `ci-red-5e97dbda` → 2 FILE-scoped dedup HITS, 0 mints (`apps/macro-indicators/pkg/application/usecases_vmt_liquidity_resolvers.go` → MACRO-VMT row; `apps/frontend/bun.lock` → FRONTEND-ESLINT row), each stamped with the verbatim offender/error line from a live `gh run view 30608987675 --log-failed`. `context_bloat_breach` on the dev-mcp-server sprint journal → DEFER + evidence onto FIX-DECISION-JOURNAL-BYTECAP-NO-ACTUATOR. `notebook_tiebreak_direction_unresolved_breach` + its 1-second-later `context_bloat_breach` twin on unified-agent.md → ONE new P1 row. Pre-checks: supervised-goahead no-op (head went idle 06:48:33Z mid-tick when FDA-7 returned to REVIEW); manual-dispatch-sweep 40 unflagged candidates (38 DRS-stranded + 2 ready-XOR), TE-T12 stamped and folded. orch-apply: Stage 0+1 PASS, 5 rows stamped, 741→742 tasks, 131→131 signals.
**what-considered:**
- Treat the tiebreak signal at face value and just add `unified-agent.md` to `docs/data/notebook-section-order.json`, which is literally what the signal's own `action_required` asks for. Rejected — it is the symptom fix. Replayed the derivation (`notebook-auto-prune.sh:396-453`, byte-identical logic) over all 46 live notebooks under bash: **11 files** sit permanently in the TIE_COUNT≥2 / dec=0 / inc=0 bucket because every one of their `## ` headings lacks a parseable timestamp and therefore collapses to the single sentinel key `99999999999999999` (:403-404). The override table holds 3 entries and has no producer. Adding a 12th entry by hand next tick, and a 13th after that, is the opt-OUT-allowlist shape (`feedback_fleetwide_gate_validated_on_one_file_optout_allowlist`).
- Blame the pruner for firing on unified-agent.md specifically. Rejected — under correct bash semantics that file resolves cleanly (dec=2 inc=1 ⇒ `newest_first`, which matches its true physical prepend order 07-31→07-30→07-29). So the 7 fires on that file are NOT reproducible against the current file state, and I did not assert a mechanism I could not replay. The reproducible, corpus-level defect is the 11-file zero-timestamp bucket — that is what the row claims.
- Assert the `\>` comparison bug at :438 as the live root cause. Rejected as a claim, kept as a hardening AC. Replaying the SAME file under zsh errors `condition expected: >` and silently books every differing pair as an `inc` vote — dec=0/inc=3 ⇒ `oldest_first`, the exact inverse of the truth, which would drop the newest tied section. But the shebang is `#!/usr/bin/env bash`, so it is only reachable via an explicit non-bash interpreter. Stated as SECONDARY on the row with both measurements, not as the diagnosis.
- Mint a row for the sprint-journal byte-cap breach. Rejected — the journal is sprint-SCOPED, so `decision-journal-archive.sh` does fire for it at sprint close, and dev-mcp-server was mid-flight writing that exact file when the breach fired. Pruning a live sprint journal races the writer (`feedback_ctxbloat_breach_on_live_sprint_file_defer`). Folded as evidence instead.
- Let the two CI rows ride in `ready[]` rather than re-folding them into BATCH. Rejected — see why-change.
**why-decision:** The tiebreak and the bloat signal are not two findings, they are one causal chain arriving 1 second apart from the same pruner run: derivation unresolvable ⇒ `exit 0` at :478-479 without pruning ⇒ the file grows past its byte cap ⇒ the bloat hook fires on the same file. That also means the two REVIEW rows about the byte-vs-line SETPOINT (`...LINE-ONLY-SETPOINT-BYTE-CAP-NEVER-CONVERGES`, `...LINECAP-SWEEP-BYTE-BLIND-BACKSTOP`) cannot converge on any of these 11 files even after they ship, because the pruner declines to run at all — so this row gates both of them and must not be merged into either. 11 fail-loud signals in ~35h clears the recurring-2+ bar.
**why-change:** Re-folded both CI rows into the BATCH although neither was asked for. `gh run view 30608987675 --log-failed` reports `[size-lint] FAIL — 1 offending file(s) (scanned 1353)` and that one file is MACRO-VMT's own — the conjunction that previously required 2-3 rows is now satisfiable by this single row, its two siblings having shipped (f4feb6551 + earlier, both REVIEW). Its own `status_note` still told readers otherwise, so it was corrected. FRONTEND-ESLINT is the only other row between main and full CI green, ~25h READY with zero dispatch attempts and never once observed green.

### STEP po-S86 · po · 2026-07-31T09:19:16Z
**task-id:** FIX-MARKET-MESSAGES-TIMESTAMP-FORMAT (backlog, P2→P1, attached — no mint)
**what-done:** Verified the reported `agent_signals.created_at` mixed-format split against live market.db (106 T-sep / 128 space-sep of 234; T-sep 100% `from_agent='alert-engine'` + `signal_type='verified_decision'`, first row 2026-07-30T14:46:43.612Z). Located the exact writer: `apps/mcp-server/src/infrastructure/db/alertStore.ts` `storeAlerts()` binds the SAME `alert.createdAt` value twice — raw into `created_at`, and through `datetime(?, '+2 hours')` into `expires_at` — so one row carries both formats (row 10065: created_at=`2026-07-30T14:46:43.612Z`, expires_at=`2026-07-30 16:46:43`, validated_at=`2026-07-30 14:46:43`). Every other producer normalises at `agentSignalStore.ts:544`. Attached to the existing row instead of minting; rescoped it from one table to the market.db canonical-format rule + both concrete sites; P2→P1.
**what-considered:**
- Mint a new `FIX-AGENTSIGNALS-CREATEDAT-ISO-FORMAT` row. Rejected — `FIX-MARKET-MESSAGES-TIMESTAMP-FORMAT` is the same defect family in the same zone (`apps/mcp-server/`), already in backlog (not the jammed review[] lane), and both sites want the SAME decision written down once. Board is 363 backlog / 219 review / 177 awaiting qa; a 4th whack-a-mole row for a 5×-recurring class is the wrong shape.
- Attach to `FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE` (same producer, same rows). Rejected — it sits in `review[]` with `next_agent=qa`; hanging a new unfixed symptom on a row awaiting sign-off strands it.
- Attach to `FIX-SIGNALQUEUE-DUP-ID-GUARD` ("normalize auditor ts to full ISO8601"). Rejected — different artefact (orch-apply.sh / signal_queue JSON), not market.db.
- Leave at P2 to avoid P1 inflation (82 P1s already in backlog). Rejected on the measurement, see why-decision — but flagged in the RETURN as a real concern.
**why-decision:** This is not latent. Measured live at 2026-07-31T09:16Z: `SELECT count(*) FROM agent_signals WHERE created_at >= datetime('now','-60 minutes')` returns **95**; the format-normalised answer is **59** — 36 rows falsely included, +61%. Cause: `'T'` (0x54) sorts above `' '` (0x20) at position 11, so within one calendar date every T row compares greater than every space row regardless of clock time (cross-day windows survive by accident because the date portion differs first). 180-min window: 98 vs 74. `agentSignalStore.ts:907` (`AND s.created_at >= datetime('now', ? || ' minutes')`) is a live consumer of exactly that predicate, so agents are being served up-to-7h-old alert-engine signals as minutes-fresh. Fix is one token — wrap the `created_at` placeholder in `datetime(?)` — and the adjacent `expires_at` column on the same row already proves SQLite parses the input correctly.
**why-change:** No change from plan; brief asked me to prefer attaching, and a genuine attachment point existed.

### STEP po-S87 · po · 2026-07-31T09:19:16Z
**task-id:** FIX-AUDITOR-C04-PARSEDAT-RECENCY-PREDICATE (backlog, P2 unchanged, rescoped — Finding 1 DECLINED as a false alarm)
**what-done:** Refuted the reported "today's session captured daily bars for 14% of tickers" incident, then folded its only real residue onto the existing auditor-predicate row (title extended to C-01/C-02; lane/priority/next_agent=architect/supervised/plan_only all left untouched). No mint, no sprint, no alert.
**what-considered:**
- Mint the coverage check the brief proposed (today's distinct-code count vs a trailing median of recent sessions, Tier-1/2/3). Rejected — it would fire EVERY day at 09:00Z. Same-day `daily_ohlcv` coverage is ~130 by design; a trailing-median comparator against a 940-row baseline built by an overnight job is a guaranteed daily false positive, the exact auditor-FP class the memory index is already full of.
- Mint a row for `recoverMissingOhlcvSession`'s branch-1 guard (`SELECT COUNT(*) ... WHERE date=?` `> 0` ⇒ `alreadyPresent`, so a 129/941 session is treated as complete and no recovery runs). Rejected as a real but low-severity latent defect: the nightly backfill covers the case unconditionally, and its only caller `ohlcvCandleGuard.ts` is watchlist-scoped anyway. Recorded in the notebook, not on the board.
- Edit `docs/agents/system-auditor/flow/main.md` directly to document the invariant. Rejected — that is an agent definition file; PO must not edit it. The C-04 row already carries `zone: docs/agents/system-auditor/flow/` and `next_agent: architect`, i.e. the correct owner and file.
**why-decision:** `intraday_ohlcv_5m` retains multiple days and is NOT rewritten by the backfill, so it is the clean discriminator `updated_at` cannot be: distinct codes per day = 118 (07-31), 119 (07-30), 118 (07-29), 118 (07-28), 117 (07-27), 118 (07-24), 118 (07-20). Today is identical to every prior session. `market_prices_history` holds a full healthy session (42,844 ticks, 118 codes, 02:00:00.098Z→09:00:13.033Z). Meanwhile EVERY historical date in `daily_ohlcv` shows 0–2 same-day writes against 900–1000 next-day writes (07-30: 2 vs 939; 07-24: 2 vs 984; 07-01: 0 vs 960) — the full universe has ALWAYS arrived from the nightly history backfill, which last night UPSERTed 750,317 rows (97% of the 774,221-row table, dates 2014-04-16→2026-07-30, 1,497 codes) and thereby clobbers `updated_at` on nearly every row. The 25 "frozen at 02:xx" tickers are alphabetically VFS…XPH, the tail of that walk — the only codes it reached after the 02:00Z open. So 129 is the designed steady state, and the router's premise "yesterday reached 900 rows by 08:36Z" is unsupported. The residue worth keeping is a mutation-timestamp defect identical in kind to C-04's `parsed_at`, plus the fact that C-01/C-02's window is `date >= date('now','-1 day')` = two calendar dates: run live at 09:12Z they return 947 and 1070, so they cannot measure same-day coverage at any threshold.
**why-change:** Diverged from the brief, which framed Finding 1 as the serious one. It is the false alarm; Finding 2 is the only live defect.

### STEP po-S88 · po · 2026-07-31T14:34:26Z
**task-id:** FIX-COWORK-SPAWNFANOUT-NO-SESSION-ID-IN-LEAF-ENTRY-PROMPT
**what-done:** Minted P1/S FIX (zone docs/agents/, next=agent-father) after reading spawn-fanout.md:288/292 + IDENTITY_PREAMBLE:240-253 myself — neither ENTRY_PROMPT branch injects a session id.
**what-considered:**
- Dedup into FIX-TASKCLAIM-OWNER-CLIENT-SESSION-MISSING-FLEET-FLOW-DOCS (REVIEW, next=po) — its own status_note explicitly deferred this as out-of-zone: "refine_bctc_md needs a spawn-prompt session-id channel too (cowork-team zone) — flagged not silently closed". Callee-side vs caller-side are different planes.
- SPIKE for the leaf-worker-class convention — rejected: CLAUDE.md step 3 already mandates the exact convention for router-dispatched agents and this very PO spawn carries coordination_session=. Precedent settles the design; it is one composition line.
**why-decision:** 2nd hit in 24h on two independent writer planes (signal_queue cow-20260731T140900 HIGH + telegram 4238, same minute), unrecoverable daily loss (last_fired is monotonic-forward-only), clears the recurring-bug bar.
**why-change:** no change from plan.

### STEP po-S89 · po · 2026-07-31T14:34:26Z
**task-id:** FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES
**what-done:** Minted P1/S FIX after measuring the live inbox: 128 hot signal_queue rows to=po span 16 types, and triage-signals.md's routing table intersects ZERO of them — 100% fall-through to "unknown type -> log and skip".
**what-considered:**
- Scope to system-issue only (the type that triggered it) — rejected: system-issue is 4 of 128; the miss is total, and a one-type patch would leave 124 rows still dark.
- No mint, log only (board is 361/224) — rejected: this is the mechanism that would have dropped today's HIGH escalation had the router not independently relayed telegram 4238.
**why-decision:** Measured 100% miss on the live plane, not a theoretical gap; corroborated by two secondary drifts (undeclared cowork-team/dev-team senders; live status "triaged" absent from the documented enum) proving PO ticks improvise past the table rather than execute it.
**why-change:** no change from plan.

### STEP po-S90 · po · 2026-07-31T14:34:26Z
**task-id:** FIX-MCP-SERVER-DEPLOY-LANE-STALL-REBUILD-REQUIRED-INERT
**what-done:** Folded the already-flagged (2026-07-31T00:29:27Z) but still-undispatched row into this tick's BATCH per manual-dispatch-sweep Step 3, with a 2nd independent runtime instance attached; rebuild_required count 9 -> 11.
**what-considered:**
- Mint a new row for telegram reports 4236/4237 (resolver stalled >72h) — rejected: FIX-SIGNAL-OUTCOMES-LIVENESS-GUARD-COUNTS-STRUCTURALLY-UNRESOLVABLE-ROWS already owns it in REVIEW, and the two firings 1h apart are the documented module-level _lastStalledAlertDate re-arm on container restart, not a new defect.
- Stamp a fresh top candidate (TE-T14) instead — rejected: 2 flagged rows are already stranded undispatched; flag rate is not the binding constraint, dispatch is.
**why-decision:** Image sha256:95ad7ae7ef39 built 2026-07-30T22:06:34Z vs fix commit 314e70718 authored 2026-07-31T00:34:15Z — 2h28m AFTER. The reports are pre-fix code running; disposition is deploy-lane, not resolver.
**why-change:** Deviated from manual-dispatch-sweep Step 2 (no new stamp this tick) — logged as a sub-flow defect: Step 1's idempotency guard excludes flagged rows, so a flagged row whose BATCH never dispatched can never be re-surfaced.
