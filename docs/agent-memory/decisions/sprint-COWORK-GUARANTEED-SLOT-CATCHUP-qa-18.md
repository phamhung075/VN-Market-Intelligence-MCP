# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa

**Sprint goal:** COWORK-GUARANTEED-SLOT-CATCHUP
**Agent:** qa
**Started:** 2026-08-12T09:37:23Z

---

### STEP qa-S78 · qa · 2026-08-12T09:37:23Z
**task-id:** FACTORY-APP-split-pollNews
**what-done:** Direct-Commit Verify — commit `0f23a703f` on main ancestry, touches all 8 files it claims (pollNews.ts + 5 new pollNews/ siblings + usecases.md + size-lint-baseline.json).
**what-considered:**
- Read diff/source myself: 5 helper clusters genuinely extracted, zero-external-call-site claim confirmed by grep (all consumer imports still resolve from pollNews.ts unchanged); dropped defaultTradingEconomicsFetcher confirmed zero remaining call sites. tsc clean, size-lint-justification.sh --check clean (only pre-existing untouched transport.ts fails), mock-guard PASS, DDD/security greps clean. Re-ran all 21 pollNews-touching test files myself in isolation: 109/109 pass (2 flake only under batch contention, isolated-pass confirms pre-existing, matches commit's own claim).
- BUT backlog DoD/approach (archive/backlog-detail.json) names 5 DIFFERENT target modules — insiderSignalDetector(→domain/), sourceFetchers, sourceHealth, ingestEntries, buildSignals — i.e. extraction of pollNews()'s own pipeline STAGES, target "~120L thin orchestrator", "each module <=120L". Delivered: pollNews.ts still 923L; the ~790L pipeline body itself untouched, only peripheral clusters (types/detectors/dedup/fetcher-impls/db-helpers) moved, 4/5 new files already exceed 120L (justified headers). Same-epic sibling FACTORY-APP-split-assembleBriefing hit its thin <=120L DoD fully in one commit (72L sequencer) — direct precedent this task falls far short of.
**why-decision:** vc-changes — execution is honest/regression-free (commit message itself discloses the gap) but DoD substantively unmet: the core goal never attempted, only a peripheral split landed. Routed qa[]->review[], redispatch_count=1, next_agent=owner dev-mcp-server (no task branch, per verify-committed convention) to continue the "one extraction per commit" ladder toward the actual pipeline-stage split.
**why-change:** none — I did not discover new facts beyond what the commit already discloses; declined to accept a self-labelled-partial commit as terminal DONE_VERIFIED against its own backlog DoD.

### STEP qa-S79 · qa · 2026-08-12T10:41:00Z
**task-id:** FIX-FB-GATE-CHECKC-NEGATION-LEXICON
**what-done:** Direct-Commit Verify — commits `31e39793a`(fix)+`ab38d4689`(journal) on main ancestry, both touch `scripts/fb-data-integrity-gate.sh`/`docs/agents/fb-market-poster/flow/main.md` as claimed.
**what-considered:**
- Read diff on disk: `chưa( phải| từng)?` alternation added to Check-C negation-strip (generic, not 2-phrase literal), `chỉ \d mã sàn`→`chỉ [0-9] mã sàn`. New `scripts/test-fb-gate-checkc-negation.sh` re-run by me: 6/6 pass on current main HEAD. Independently reproduced RED state myself (not trusted from prose): temp-overwrote the script with `31e39793a^`'s pre-fix version, re-ran harness → 4/6 pass, fixture-A false-BLOCKs exactly as claimed; restored fixed version, diff-confirmed byte-identical restore. shellcheck -S style before/after: identical single pre-existing SC2034 warning, 0 new. bash -n clean both files. mock-guard N/A (shell, not TS production source). process.env/secret greps clean.
- Cross-checked the "25 \d occurrences" commit-message claim programmatically (heredoc-boundary parse): actual pre-fix count is 24 (1 real bash grep context @L636 + 23 python3 heredoc `re` literals), not 25/24 as stated — off-by-one in the commit's own descriptive count. Immaterial: the substantive claim (exactly 1 real bash context existed, fixed; all others safely inside python heredocs, untouched) is 100% correct regardless.
**why-decision:** vc-approved — all 3 `verification_gate` criteria (backlog-detail.json) independently confirmed: mild-day negated fixture PASSes 0 violations, affirmative contrast fixture still BLOCKs, `[0-9]` floor-regex matches under this host's BSD-family grep -E. RED→GREEN proven by me, not merely re-trusting the row's own harness. Off-by-one prose miscount is cosmetic, not functional — does not block.
**why-change:** none — verified exactly the scope claimed; only new fact is the harmless off-by-one prose miscount, noted but non-blocking.

### STEP qa-S80 · qa · 2026-08-12T10:40:52Z
**task-id:** FIX-COLDEVICT-DONE-LANE-TRIGGER-ACTION-AXIS-NOOP
**what-done:** Direct-Commit Verify — commit `4bbfc065d` on main ancestry, touches exactly the 5 files.files_hint names (orch-cold-evict.sh/preflight.sh+.test.sh/orch-cold-evict-tests.sh/post-cycle.md).
**what-considered:**
- Read diff on disk for all 3 defects: (A) sort_by switched string->`try fromdateiso8601 catch 0` (epoch-0/oldest for poison values); (C) both `[.arr // [] | .[].id // ""]` phantom-1-on-empty sites fixed (`[]` moved outside `// []`) for done_verified[]/signal_queue.archive[], no other same-shape site remains (grepped); (B) trigger replaced with `_step55_would_evict` (real --dry-run "Byte reduction:" parse), post-cycle.md CANON spec updated in sync.
- Re-ran (not trusted from prose): orch-cold-evict-tests.sh 59/59, dev-team-tick-preflight.test.sh 124/124, orch-apply-wrapper-tests.sh 75/75 — all PASS (row's own "27/35, 8 pre-existing SHG-5 fails" was accurate for its own commit-time snapshot; later unrelated commits since fixed those, now 100%). Live `--dry-run` today: 0 bytes (AC-1 direction-i, steady state). AC-1 direction-ii + AC-2 verified via 2 REAL commits, not prose: `5100e9d63`(6 rows)+`1a208daa5`(4 rows)=exactly the 10 named "unknown" ids evicted; live done[] has 0 "unknown" left. AC-4 negative control confirmed both in fixture T7 (DONE-RECENT survives) and live (the 6 well-formed rows untouched by either cleanup commit). mock-guard N/A (shell, no production TS touched); process.env/secret greps clean.
**why-decision:** vc-approved — all 6 acceptance items independently reproduced against source+re-run, not re-trusted from review_note. No fabrication found; cost claim (0.587s) in the same order of magnitude as my own re-measure (~0.85-1.05s, larger hot-file today explains the delta).
**why-change:** none — verified exactly the scope claimed.

### STEP qa-S81 · qa · 2026-08-12T12:28:16Z
**task-id:** FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L
**what-done:** Direct-Commit Verify — commit `523ab712f` on main ancestry, a real descendant of the original failing SHA `c5d6ab5e` (2026-08-08→2026-08-12); `git show --stat` touches only transport.ts + journal/notebook.
**what-considered:**
- Re-measured line count myself: parent=265L, commit=241L, current main HEAD=241L (exact match, no drift). Read the full diff: change confined entirely to the top-of-file comment block, `import`-onward byte-identical — genuine comment-only edit. `size-lint-justification.sh --check`: PASS 0 offenders (reproduced, not trusted). `gh run view 31588469532`: headSha=523ab712f confirmed, size-lint job conclusion=success confirmed. Overall run failure independently confirmed unrelated: task-claim-owner-session-lint fails on 2 pre-existing grandfathered lines (qa/flow/main.md:22+215, log says "NOT a reintroduced regression"); bun test fails on 2 files that already carry their own open board rows, zero transport.ts touch.
- Re-ran targeted suites myself: 1862c-transport-session-eviction.test.ts 14/14, 251-mcp-tools.test.ts 16/16 (toolCount=183 live). tsc 0 errors. mock-guard PASS. DDD grep flagged 1 interface→infrastructure Logger import — confirmed pre-existing (present in parent commit too, unrelated to this comment-only diff, not a domain-layer violation).
**why-decision:** vc-approved — verification_gate=ci_green_on_subsequent_push satisfied on its own literal AC target (size-lint job specifically), commit is a genuine subsequent push not a relabel, all supporting claims (line count, diff scope, CI job split, targeted tests) independently reproduced.
**why-change:** none — verified exactly the scope claimed.

### STEP qa-S82 · qa · 2026-08-12T12:35:00Z
**task-id:** FIX-CONVICTION-HISTORY-EOD-BACKFILL
**what-done:** Direct-Commit Verify — commits `87150a8aa`(code+docs)+`7ff43dd57`(memory) on main ancestry, `git show --stat` matches all 9 claimed files exactly (scanMarket.ts/IMarketPriceRepository.ts/SqliteMarketPriceRepository.ts/new checkConvictionHistoryGap.ts/dataAuditJob.ts/3 arch docs/new test file).
**what-considered:**
- Read the diff on disk, not trusted from prose: Step 5c persistence loop genuinely moved above the `allSignals.length===0` early return in scanMarket.ts; `upsertConvictionHistory` void→boolean confirmed in both interface+impl, only 1 implementer+2 callers, both updated correctly. Full-read the new 349L checkConvictionHistoryGap.ts: self-deriving watchlist-count trading-day floor, MIN(conviction_history.date)-derived lookback, honest `action:"none"` unfixable-day path (never fabricates a changePct without a prior-day baseline) — substantive, not stub.
- Cron claim specifically verified: `checkConvictionHistoryGap` is called inside `runDailyChecks()` (plain fn) which `runDailyAudit()` wraps; the actual `scheduleCron(CRONS.dataAuditDaily, ...)` registration lives in `schedulerJobTable.ts` — a file NOT in this commit's diff. Structurally confirms no new cron.schedule call site; new check rides the existing `dataAuditJob:daily` job.
- Re-ran myself: new test file 10/10 pass; all 10 named targeted regression suites 165/165 pass (claimed 122/122 — codebase grew since 07-29, 0 fail either way, not a red flag). tsc 0 errors. mock-guard PASS on all 5 touched production files. Live boot (`DB_PATH=:memory:`): health 200, toolCount=183/89-cron-keys (claimed 184/88 — same 2-week drift, structurally unrelated since no tool/cron file touched here). Full `bun test`: 15250 pass/40 skip/56 fail/1271 files (claimed 14876/40/54/1237, consistent growth) — grepped all 56 fail names, zero reference conviction_history/scanMarket/dataAuditJob/checkConvictionHistoryGap/IMarketPriceRepository; VPS Proxy Health (1113-vps-proxy-health.test.ts) + insider-transactions (Task 1146) both reproduce failing in the full run yet pass 8/8 standalone — test-isolation/DB-state-leakage flakiness, file-disjoint from this task's diff, confirms pre-existing/unrelated without needing a risky `git stash` on this shared dirty repo.
**why-decision:** vc-approved — root-cause fix, EOD backfill, and cron-wiring claims all independently reproduced against source+live re-run; every numeric drift from the original 07-29 claim is explained by 2 weeks of unrelated repo growth, not by this task's own scope.
**why-change:** none — verified exactly the scope claimed.

### STEP qa-S83 · qa · 2026-08-12T12:46:39Z
**task-id:** OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX
**what-done:** Direct-Commit Verify (router-dispatched, mode=verify-committed) — commit `ca6d86869` on main ancestry, `git show --stat` matches exactly the 4 claimed files. Router flagged AC-3 durability as the crux (auditor c48 + router's own 11:44Z read found memory climbing past dev-rag-service's own 26min-window 78% claim, toward 90%) — extended my own fresh observation window.
**what-considered:**
- Read diff on disk: TOKIO_WORKER_THREADS/LANCE_CPU_THREADS env pins + `_vector_index_lock` double-checked-locking, both real, matches description. `docker exec cat` the live container's repositories.py byte-diffed clean against `git show` — image genuinely runs this commit, not stale. Logs since rebuild: exactly ONE "Vector index (IvfPq) built successfully" line despite 19 /search + 82 /index requests — lock proven effective in live traffic, not just unit test.
- AC-1/AC-2 independently confirmed (image ID+Created-ts changed; both peers Up/healthy, pdf-extractor's RestartCount=1 traced to an OLD Aug-11 01:19Z OOM unrelated to today's rebuild).
- AC-3 (crux): read `dmesg` INSIDE the Docker Desktop VM myself (`docker run --privileged --pid=host alpine nsenter -t1 dmesg -T`) — zero new OOM since the pre-fix 10:18:09Z/10:24:01Z pair through 12:42:40Z (>2h18m). Own 9-sample docker-stats series (90s cadence, 12:31:50Z-12:42:30Z) + auditor/router's earlier reads: trajectory climbed 78%(11:07Z)->90%(11:44Z) then bounded-oscillated 88-93% for 58+min with a genuine reclamation dip (954.4MiB->912.4MiB) and 3 flat reads after. RestartCount=0 throughout.
- Test claim raw-verify: ran pytest INSIDE the actual deployed image (non-bun-zone mandate) — 186/198 pass, NOT the claimed 198/198. Reproduced the same 12 failures against `ca6d86869^` in the same image (pre-existing, unrelated). 11/12 root-caused to missing `httpx2` (image has neither httpx nor httpx2, starlette==1.4.1 requires it for TestClient); 1 separate genuine pre-existing compaction-counter bug, reproduced 4x non-flaky. This commit's own 2 new test classes DO pass genuinely. mypy delta independently re-derived: +6, exact match. size-lint PASS 0 offenders.
**why-decision:** vc-approved, DONE_VERIFIED — AC-1/2/3 all independently confirmed (AC-3 durable but with a corrected ceiling figure, ~90% not ~78%, written into the row). The false "198/198 pytest-green" AC claim is a real, material finding (same host-vs-deployed-image false-green pattern this flow doc's own header already cites as a prior incident on this SAME zone) but judged non-blocking: 100% pre-existing, unrelated to this fix's scope, reproduced identically pre-fix, and this fix's own regression tests genuinely pass. Blocking a durable, independently-verified OOM fix over pre-existing unrelated test-infra debt would be disproportionate and hold back resolution of a multi-day-recurring P0. Flagged prominently in the row's own record instead so the false claim is never re-cited.
**why-change:** corrected AC-3's settled-ceiling figure (~90% not ~78%) and the pytest count (186/198 not 198/198) in the row's own status_note — both discovered via my own raw re-verification, not present in the original claim.

### STEP qa-S84 · qa · 2026-08-12T13:35:00Z
**task-id:** FU-ORCH-HOT-SUB150-SPRINT-LIFECYCLE
**what-done:** Direct-Commit Verify (router-dispatched, mode=verify-committed, branch:null) — row had no `commit`/`files[]` field, `detail_ref` dead (backlog-detail.json has no matching key). Fell back to `git log --oneline --all -- scripts/orch-cold-evict.sh`; found `b3d9a04eb`, whose own commit body carries `Task: FU-ORCH-HOT-SUB150-SPRINT-LIFECYCLE` trailer — self-identifying, confirmed on main ancestry.
**what-considered:**
- `git show --stat` touches exactly scripts/orch-cold-evict.sh + scripts/test/orch-cold-evict-tests.sh + its own journal + archive/2026-07.json — matches review_note's described scope, no scope creep.
- Read the diff on disk: decision_journal[] age-gated eviction genuinely added (rank by parsed .ts desc, keep 10 newest, evict rank>=10 if ts null/>14d old), INDEX-keyed hot removal (no stable id on these entries) + content-deduped cold append — matches description exactly, not a stub.
- sprint_goal.entries[]/active_sprints[] claim was verify-only (no code change) — confirmed pre-existing FIX-SPRINT-GOAL-STATUS-DRIFT-EVICT predicate already present at lines 10/118/509/875, genuinely covers both arrays.
- Re-ran `scripts/test/orch-cold-evict-tests.sh` myself: 59/59 PASS incl. new TEST 8 (6 assertions: rank-gate, age-gate-despite-rank, null-ts, non-contiguous INDEX removal correctness, idempotency, live-file-untouched) — matches claimed "6 assertions" exactly. Every "REAL live orch-state.json UNCHANGED" guard passed. `bun tsc --noEmit` (mcp-server zone) 0 errors — no TS touched by this commit, ran as baseline per never-skip-tsc constraint. mock-guard: no production TS files in scope, PASS (trivial).
- Live orch-state.json currently carries decision_journal=10 (cap holding), archive/2026-07.json carries 40 (39 from this commit + later drift) — consistent with the capped-retention behavior this commit introduced, not a red flag.
**why-decision:** vc-approved — commit self-identifies via its own Task: trailer, file scope matches review_note exactly, new eviction logic read on disk (not trusted from prose) is substantive and correct, and the new test (TEST 8) + full 59/59 suite genuinely reproduce green. Full <150KB target explicitly out of this task's narrow scope (task description says so) — not a blocker for this row.
**why-change:** none — verified exactly the scope claimed; no discrepancy found.

### STEP qa-S85 · qa · 2026-08-12T13:40:00Z
**task-id:** FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE
**what-done:** Direct-Commit Verify — commits `e0033de05`(flow+eval fix)+`37bc91500`(archive+reset)+`aeb72d959`(journal) on main ancestry, `git show --stat` matches claimed files exactly.
**what-considered:**
- Diff read on disk: stage-dispatch-log.md § 5 genuinely replaces the perpetual "## This session"/"### Alert Cycle" shape with one "## c<NNN>" section/cycle; eval AC-11 fixture updated to match (would else certify a regression).
- Live notebook today: 29L/5941B, 3 sections (c129-c131) — 131 real cycles since the 07-29 fix, retention (keep-last-3) actively operating, both caps clear with wide margin; archive file byte-identical to claimed 198L/124374B. Repo-wide grep: zero context_bloat_breach signals ever naming alert-commander post-fix (AC-2/3/4 all exceed the row's own bar).
- AC-5 (check digest-predict.md, report verdict either way): review_note never mentions it — the exact "silently skipping it" anti-pattern the row warned against. Live-checked myself: digest-predict.md is 41L/23477B, single "## " section, same structural defect, 1.96x over byte cap. Not lost at system level though — 2 separate active rows already track it independently (CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION, CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT, both routed claude-manager-helper/po).
**why-decision:** vc-approved — core fix strongly verified with 2 weeks of real production evidence, exceeding AC-1..4. AC-5's process-gap (dev never reported verdict) judged non-blocking: substance is independently covered by 2 pre-existing tracked rows; redispatch would be redundant churn, not a fix for anything actually uncovered. Flagged prominently in row's own status_note instead.
**why-change:** none — verified exactly the scope claimed; AC-5 gap noted, not treated as new work for this row.

### STEP qa-S86 · qa · 2026-08-12T13:29:25Z
**task-id:** FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE
**what-done:** Direct-Commit Verify — row self-contained via `review_note` prose (no `.commit`/`.files[]` fields); derived commits `0a7e91e5b`(code+tests+doc)+`c1e21aae4`(memory), both on main ancestry, `git show --stat` matches claimed 5-file set (alertStore.ts, schema-news.ts, new test, dup-emission sibling test, infrastructure.md doc).
**what-considered:**
- Read alertStore.ts on disk: `buildVerifiedDecisionPayload()` present, replaces old bare `'{}'` literal, output bound via `insertSignal.run(..., payloadJson, ...)` — parameterized, not concatenated. Emit-time guard (`payloadJson==="{}"` → fail-loud skip, base alerts row unaffected) confirmed live.
- Re-ran task test standalone: 6/6 pass (AC-1..AC-6 incl. MACRO stock_code-null preserved, dedup-collision-safe, emit-guard). Sibling `FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION.test.ts` (dedup index this fix interacts with): 10/10 pass, no regression.
- Full alert-name-filtered suite (`-t alert`, 814 tests/1265 files): 28 fail — every one of the 28 re-ran clean in isolation (086/002/1328e/SC-3/alertsHandler files all 0-fail standalone) → shared-DB/parallel-run test-isolation artifact, not caused by this diff; matches dev's own claim of pre-existing unrelated fail-set.
- `bun tsc --noEmit` (mcp-server): 0 errors. `mock-guard.sh` on both touched production files: PASS. Security: parameterized SQL only, no `process.env`, no secrets in diff.
**why-decision:** vc-approved — root cause (hardcoded `'{}'` literal) genuinely fixed with real bound content, new test coverage substantive (not a stub), no regression in the dedup sibling it was designed to stay collision-safe against, tsc/mock-guard/security clean. Live-effect (container restart) explicitly out of QA static-verify scope per dev's own note — ops follow-up, not a QA gate.
**why-change:** none — verified exactly the scope claimed.

### STEP qa-S87 · qa · 2026-08-12T14:04:19Z
**task-id:** FIX-VPS-SSC-INSIDER-502
**what-done:** Direct-Commit Verify — row had no `.commit`/`.files[]`; derived commit `8c72462d9` via `git log --oneline --all` on the 3 files named in status_note prose (vps-proxy-server.js, BA-PREDICTION-EVIDENCE-REVIVAL brief, WORK.md), confirmed on main ancestry, `git show --stat` matches exactly.
**what-considered:**
- Diff read on disk: vps-proxy-server.js change is a PURE 29-line comment block at SSC_INSIDER_UPSTREAM — zero functional/logic change confirmed (fetchUpstream()/route-handler untouched). `node --check` valid. `mock-guard.sh` PASS. No TS touched (JS VPS script outside tsconfig scope) — ran `apps/mcp-server bun tsc --noEmit` anyway as never-skip baseline: 0 errors.
- docs/WORK.md + architecture-brief diffs read verbatim against claimed closure text — landed exactly as described. Precedent commit `a817b5139` (B-05-FU-SSC-503-RETRY) independently confirmed real, on main ancestry, message matches the cited 17-day-queue-freeze rationale. Decision journal STEP developer-S24 confirmed present verbatim.
- LIVE RE-CHECK (2026-08-12, dispatcher explicitly asked "re-check upstream if you can"): congbothongtin.ssc.gov.vn RECOVERED from the uniform-503 seen at 07-29 diagnosis — root now 200, ssc.gov.vn/webcenter/portal/ubck now 302. BUT the exact scraped deep-link (SSC_INSIDER_UPSTREAM jspx) now returns genuine upstream 404 (verified -v, cold + session-cookie-warmed, same either way) — /proxy/ssc-insider still broken today, different failure mode than diagnosed. Does not contradict the commit's own (time-scoped) claim, but feature has not self-healed — flagged for router/PO as new backlog candidate, not a defect in this row.
**why-decision:** vc-approved — commit's own claims (comment-only change, docs updated, external-outage diagnosis accurate for 07-29) independently verified true; no fabrication. Live status has since evolved (404 now) but that is new information post-dating the commit, not evidence the commit lied.
**why-change:** none — verified exactly the scope claimed (diagnose 07-29 state + document + no unverified fix); live-status drift flagged separately for router awareness.

### STEP qa-S88 · qa · 2026-08-12T14:20:00Z
**task-id:** FIX-PDF-EXTRACTOR-TEST-SYS-MODULES-LEAK
**what-done:** Direct-Commit Verify — commits `1d8b1374f`(fix)+`8ac350161`(memory) on main ancestry, `git show --stat` matches claimed single file (test_low_text_density_ocr_rasterize.py); diff read on disk confirms all 4 raw `sys.modules[name]=stub` sites now wrapped `if name not in sys.modules:`, PIL/PIL.Image real-import-first `try:` blocks genuinely added.
**what-considered:**
- REBUILD_REQUIRED flag explicitly raised by dispatch — checked directly: image `Created=2026-08-08T11:34:24Z`/container started 08-08T13:39:55Z, both postdate the fix commit (07-29) and the zone's last code commit (07-31, `d808a6a11`). `docker exec grep` the LIVE container's file: all 4 guard sites present verbatim — fix is durably baked into the deployed image, not merely docker-cp hot-patched.
- Re-ran tests myself inside the (already-rebuilt) container, not trusted from prose: `test_ocr_backends.py`+rasterizer file both orderings = 47 passed each; +page_rasterizer = 32; +tesseract_retry = 26 — exact match to claimed evidence, order-independent. Full non-slow suite: 1047 passed/5 skipped/7 deselected/0 failed (claimed 1033 passed — 14-test delta explained by 2 weeks of unrelated suite growth since 07-29, skip/deselect/fail counts identical, not a red flag).
- mypy unrunnable inside the image (`no module named mypy` — dev tool, never shipped to runtime image) and blocked on host by a pre-existing env-only "pdf-extractor is not a valid Python package name" error (confirmed identical pre-existing on an unrelated 08-05 pdf-extractor task's own journal). Independently re-derived the A/B claim anyway: ran mypy on the isolated before/after test-file content — both produce the identical 23-error set (5 pre-existing `infrastructure/ocr_adapter.py` errors + 4 stub-attr errors in the test file itself, only line numbers shift) — 0 new errors confirmed.
**why-decision:** vc-approved, DONE_VERIFIED — root cause, guard-fix, and order-independence claims all independently reproduced from source+live re-run; REBUILD_REQUIRED gate satisfied (image genuinely postdates fix, live file byte-confirmed); mypy 0-new-errors claim independently re-derived via isolated A/B despite the image lacking the tool. DoD (backlog-detail.json: full non-slow suite green, order-independent) met.
**why-change:** none — verified exactly the scope claimed; only new fact is the pass-count growth (unrelated repo drift), noted non-blocking.
