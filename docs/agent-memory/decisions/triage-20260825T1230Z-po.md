# PO triage — 2026-08-25T12:26Z–12:40Z

**session:** 036ceaf1-bf34-46cd-92e4-8c6b213ff4bb
**trigger:** dev-team Review-Lane SECONDARY-Drain, tick 2026-08-25T12:07Z, row `FIX-CHEF-PUBLISHED-MARKER-RELEASE`
**task-id:** FIX-CHEF-PUBLISHED-MARKER-RELEASE · FIX-SUBAGENT-BRANCH-CHECKOUT-HIJACKS-SHARED-WORKING-DIR · FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY · FIX-PM-DECOMPOSE-CLOSEOUT-STEP-UNREACHABLE-PAST-RETURN-AND-MINT-OMITS-NEXTAGENT · FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK · FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE · FIX-DBINTEGRITY-HISTORY-FINDINGS-MEMBERSHIP-FREE-JUDGMENT-AND-MULTITABLE-TABLE-FIELD · FIX-DEVTEAM-GCCPREFLIGHT-WORKTREE-LOCK-GLOB-DEAD-CODE · FIX-NEWSSCOUT-PRODUCER-CADENCE-4H-EXCEEDS-AGENTSIGNALS-TTL-2H-CONSUMER-BLIND

---

## 1. The dispatched row was NOT contentless — the reader was looking at fields that do not exist

The dispatch brief stated `FIX-CHEF-PUBLISHED-MARKER-RELEASE` has empty `.description` and
`.status_note`, and asked for a separate row about "rows reaching the board with EMPTY descriptions."

**That class is a false positive and no row was minted for it.** Measured across the whole board:

| field | rows carrying it |
|---|---|
| `title` | 810 / 810 non-sprint rows |
| `description` | 5 / 568 backlog rows |
| `desc` | present (e.g. the second cited row, ~1.9 KB of it) |
| `status_note` / `note` | 255 / 266 rows |

`description` is not a schema field on this board. The dispatched row carries a full `title`, a
`detail_ref`, plus `blocked_reason`, `architect_review_note` and `pm_closeout_note` — ~5.9 KB of
content. The second cited row, `FIX-DISPATCH-GATE-SATISFIABILITY-FIXTURE-HEAD-TRUNCATION-FALSE-INVALID`
(`task_board.backlog[315]`), carries a 700-char `title`, a long `desc`, `root_cause_hypothesis`,
`scope` and `dedup_checked`. Neither is contentless.

**Real defect, and it is the SECOND HALF of an already-filed row.** The `Agent()` context string in
`docs/agents/dev-team/flow/main.md` § Review-Lane SECONDARY-Drain hardcodes *"is a stale review[]-lane
row (status=REVIEW, branch:null …)"* and instructs *"Read its status_note/review_note fields directly"*.
For a `done[]`-origin pick the lane is wrong, the status is wrong (`DONE`), and neither named field
exists. The receiver reads two empty fields and reports the row as contentless — which is what happened.
Folded onto `FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY` as new **AC-7**, priority
raised P1 → P0. Not minted separately: one row, one 4-line call site, one fix.

## 2. Root cause of the SECONDARY-drain lane burning every pick

`scripts/devteam-review-claim-secondary-drain.jq` candidate set = `review[]`(REVIEW) ∪ `done[]`(DONE),
filtered `effective_next_agent != "qa"`, with null/absent → `"po"`.

pm's 2026-08-23T13:38–13:41Z decomposition closeout wrote **8 epic wrappers into `done[]` with
`status=DONE` and `next_agent` unset**. Every one is therefore a permanent, non-actionable candidate.
All **three** of 2026-08-25's picks came from that cohort:

- 05:10:44Z `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR`
- 08:15:01Z `FIX-SUBAGENT-BRANCH-CHECKOUT-HIJACKS-SHARED-WORKING-DIR`
- 12:16:42Z `FIX-CHEF-PUBLISHED-MARKER-RELEASE`

Meanwhile `review[]` holds 25 rows and **not one has ever carried a `secondary_claimed_*` stamp**.

**what-considered:** (a) sign off to `done_verified[]` — REJECTED, `checkVerificationGate`
(`apps/mcp-server/src/infrastructure/orchStateSchema.ts:627`) hard-rejects `DONE_VERIFIED` without
`verification.raw_probe{tool,args,live_value_observed,observed_at}`, and these wrappers are not
delivery-verified (most children still TODO), so the only way to write that status is to fabricate a
probe; (b) leave in `done[]` and bump `updated_at` to derank in the age-sorted set (what the 08:24Z
session planned) — REJECTED, that is a rotation, and the rotation is what produced three wasted picks
in one day; (c) set `next_agent:"qa"` — REJECTED, hands non-actionable wrappers to the PRIMARY lane;
(d) **CHOSEN** — move to `.task_board.archive[]`, status `DONE` unchanged. `archive` is absent from
`LANE_ALLOWED_STATUSES` and from `checkVerificationGate`'s `flatLanes`, so `DONE` survives intact and
**nothing false is asserted**; the claim script never reads `archive[]`; ids stay resolvable because
`collectAllTaskIds` does include it.

**why-change:** fixes the class (8 rows), not the one dispatched row. 5 more wasted picks avoided.

**Safety checks run BEFORE the move:** zero rows anywhere carry any of the 8 ids in `depends_on`, so no
dependent could be falsely unblocked. Children resolution: 7 of 8 wrappers resolve all children on the
hot board; the 8th's child `TASK-DEV-MCP-SIGNAL-TYPE-REGISTRY` reads MISSING on the hot board only
because it was QA-approved 2026-08-25T05:30Z and cold-evicted to
`docs/data/orch/archive/2026-08.json .done_tasks[]` as `DONE_VERIFIED`. **A hot-board-only child probe
would have called that child lost.**

**Two dangling edges the move would have created, both caught by diffing `orch-validate` Stage 1g
live-vs-candidate rather than by assumption, both repaired in the same write:**

1. `task_board.review[9]` `FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE` carried a stale
   `blocked_by:["FIX-USDVND-THRESHOLD-SSOT"]` (pm had already re-pointed its `depends_on` to the two
   children in that same closeout and left `blocked_by` behind). Dropped. Row stays legitimately
   BLOCKED on `FIX-CHEF-L6-GOLD-FALSE-PREDICATE` (`task_board.backlog[182]`).
2. That drop then tripped **Stage 1e**: the wrapper carried the reverse edge
   `blocks:["FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE"]`, which Stage 1e requires to be two-sided.
   Both halves of the one edge retired together.

Final Stage 1g set is **byte-identical to pre-write** (10 rows, unchanged).

## 3. Adopting the dead 08:15Z session's work

`scripts/po-triage-20260825T0824Z-branchguard-signoff-and-inbox.jq` was sitting **untracked and
never applied** — the PO session dispatched at 08:15:01Z died on the account-level weekly API quota
(fleet outage 08:26Z → 12:00Z, reported in the 12:00Z `cowork-tick-telemetry` envelope) before piping
it through `orch-apply.sh`.

**Re-verified at source before adopting anything** (`feedback_auditor_resumed_run_narrates_unrecorded_escalation`):

- `scripts/git-hooks/post-checkout` **does not exist** — `ls` returns `fixtures/ install.sh post-commit
  pre-commit pre-push` + `.test.sh` siblings only. The branch-guard AC-1(a) deliverable is unshipped and
  the hazard is still live.
- `.claude/worktrees/agent-ae9ed2cd6f04b3686/.git` is a **regular file** (`file -b` → `ASCII text`), so
  `docs/agents/dev-team/flow/main.md:136`'s `.claude/worktrees/*/.git/*.lock` glob genuinely can never match.
- All fold targets resolved; both mint ids confirmed absent from the board.
- Prose-headroom numbers in its comments reproduced exactly (`ready[58]` = 64 B, `backlog[501]` = 1107 B,
  `ready[32]` = 2254 B).

Its block (1) was dropped (see §2(b)) but its **verified findings were preserved verbatim** onto the
archived row's `review_note`. Its block (10) was applied only together with the dependency repair it
lacked (§5). The file is kept, annotated with a `SUPERSEDED — NEVER APPLIED. DO NOT RUN` banner.

## 4. Cycle-snapshot chain — the diagnosis converged, and the fix shape changed with it

Four self-correcting cowork envelopes on one subject (07:29Z / 07:42Z / 07:58Z / 12:15Z). Only the last
holds. Folded onto `FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK`
(1107 B headroom, so the chain lives here and only the verdict is inlined on the row).

**WITHDRAWN by their own author:** "structurally unhittable"; "the reader holds `scheduled_utc` and ignores it".

**HOLDS — re-read verbatim at source by this session:** the reader's key derivation is **prose in two
disagreeing live copies**:

- `.claude/skills/cycle-bootstrap/SKILL.md:58` — `TICK = current UTC time formatted as HH:MM (round to nearest 5-min slot, e.g. 02:05 → "02:05")`
- `.claude/skills/step-0-cowork/SKILL.md:39` — `TICK = current UTC time as HH:MM (e.g. "02:05")` — **no rounding clause**

Copy B's own header defers to copy A as the full protocol and then restates it wrong. The reader is
therefore **non-deterministic per cycle**, not uniformly token-blind: c250 tick=07:05 probed 07:05
(rounded) HIT; c251 07:22 probed 07:22 (raw) MISS; c252 07:30 probed 07:35 (rounded) MISS; c253 07:45
probed 07:45 (nominal) MISS.

**Fix-shape consequence:** a writer-only move to the nominal key is **not** strictly hit-increasing —
it trades c250-style coincidental hits for deterministic ones. Writer **and both skill copies** must land together.

**Two inferences NOT verified — confirm at source before designing:** (a) `cycle-snapshot-latest.json`
is 4468 B while every Step 4.7 snapshot is ~16.6 KB with the same 4 top-level keys, so the promoter may
BUILD its own payload rather than copy — if so there is nothing to unify, only a third semantic to
delete; `emitPressureStateTool`'s source has still not been read. (b) `spawn-fanout.md`'s Consumer
Contract derives the window key from the token's leading 10 chars = the DATE only, so it may not cover
the snapshot case at all.

## 5. Dependency repair that block (10) needed

Retiring `TASK-DEVTEAM-IDLE-CHAIN-3-DRAIN-DURABILITY` (scope shipped 2026-08-08 under
`FIX-DEVTEAM-IDLE-CHAIN-P2A-DURABLE-DRAIN`, verified by reading the live flow doc) to `archive[]`
breaks `TASK-DEVTEAM-IDLE-CHAIN-4-TESTS-AC1-AC2-AC4`'s `depends:["TASK-DEVTEAM-IDLE-CHAIN-3-…"]`.

Re-pointed to `FIX-DEVTEAM-IDLE-CHAIN-P2A-DURABLE-DRAIN`, which is `DONE_VERIFIED` in
`docs/data/orch/archive/2026-08.json .done_tasks[]`. Strictly better than leaving it: the old edge
pointed at a BACKLOG row nobody would ever work, so that row was permanently unsatisfiable;
`deps_satisfied()` demands `DONE_VERIFIED` and the new target has it.

## 6. Inbox — 29 → 12

17 cleared (subtractive by `envelope_id`, `ORCH_APPLY_DECLARED_INBOX_TRIAGED` declared), 12 held.

**Dispositions.** FOLD (no re-mint): sweep-guard `escalated=false` → occurrence 32, byte-neutral bump
only (64 B headroom); `routed_to` label drift → occurrence 6 (widens that row by a spelling-determinism
axis: file plane deterministic via `computeRoutedTo`, dashboard plane unscripted, 19 rows spell
`PO Step 0-SIG` and 2 spell `po-step-0-sig`); load-average comma-decimal → occurrence 6; sprint
decision-journal context bloat → occurrence 5, DEFER (live tip of a rolling journal); 8 notebook
envelopes over 4 files → `CLEAN-NOTEBOOK-BYTECAP-…` occurrence 6 (125 B headroom → numeric only);
2 prune-dropped-newest → `FIX-NOTEBOOK-AUTOPRUNE-AC6-…` occurrence 8 (137 B headroom → numeric only);
news-scout uncommitted notebook → `TASK-OFFHOURS-SELFCOMMIT-FLOWDOC-REWIRE` (same-tick controlled
contrast: market-watcher committed `d41596771` pathspec-scoped, news-scout narrated `c282` and made no
commit — that row's own note already cites an uncommitted `c273`, so the recipe is still intermittent
two days on); code-janitor team-tool-recheck → `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE`
(the decision it asks for already exists on the board **three times over**; the sweep re-asks because
the escalation is a hardcoded string with no predicate over either writer liveness **or** an existing
ruling — **5 rows already crowd that subject; a 6th would be the defect, not the fix**).

MINTED (3): the two adopted from the 08:24Z session (`FIX-DBINTEGRITY-HISTORY-FINDINGS-…`,
`FIX-DEVTEAM-GCCPREFLIGHT-WORKTREE-LOCK-GLOB-DEAD-CODE`) plus
`FIX-NEWSSCOUT-PRODUCER-CADENCE-4H-EXCEEDS-AGENTSIGNALS-TTL-2H-CONSUMER-BLIND` — news-scout's producer
cadence (4 h) is double the `agent_signals` TTL (2 h), so `alert-commander-market` (28 weekday fires,
2 h query window) sees a structurally empty bus for ~2 h of every producer period and cannot tell it
from "no news". Nearest neighbour `FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE` is
**not** a duplicate: that is a mutex marker outliving its cadence and *blocking* a dispatch; this is a
data signal expiring before the next is produced and *starving* a consumer. Routed to architect because
its AC-3 makes it a class question over `docs/data/system-map.json`'s producer/consumer graph.

**A near-miss worth recording:** the 12 notebook envelopes arrived as 6 exact pairs and I was about to
file a duplicate-emission defect. Diffing pair members showed they differ **only** in `createdAt`
(e.g. 12:06:04Z vs 12:07:46Z) — the hook has no per-file debounce and fired twice on two writes inside
two minutes. `envelope_id` is a content hash that includes `createdAt`, which is why the ids differ.
Not a defect; no row minted.

## 7. Hold-back — 12 envelopes, deliberate

`scripts/audits/guard-signal-type-coverage.sh` derives its live-type set from
`.dev_team_idle_chain.pending_triage_inbox[]`, so clearing an envelope whose type has no routing row
turns the CI `signal-type-coverage-guard` **green with the routing table untouched**. Held (all
**fully triaged first** — folded or minted; held only to keep CI honest):

`root-cause-confirmed`, `root-cause-corrected`, `root-cause-mechanism-found`, `wiring-gap-measured`,
`flow-wiring-gap-controlled`, `cowork-tick-telemetry`, `notebook_undroppable_remainder_over_cap_breach`.

Routed set derived by hand-replaying the guard's read-only extractor over the Pipeline-A type column
(28 routed types) — the script itself was **not** run, per
`FIX-GUARD-SIGNAL-TYPE-COVERAGE-CHECK-FLAG-MISLEADING-NOT-DRYRUN` (`--check` is a no-op alias, not a
dry run, and it mutates the live board).

**The finding that makes this actionable, folded onto `FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE`:**
**6 of the 7 unrouted types come from ONE producer (cowork-team), and each is a fresh type minted for a
single report.** `root-cause-confirmed` / `root-cause-corrected` / `root-cause-mechanism-found` are three
type names for **three consecutive revisions of the same investigation** (07:42Z / 07:58Z / 12:15Z, all
on the cycle-snapshot key derivation) — not three kinds of signal. The producer is naming the CONTENT,
not the KIND, so a closed allowlist can never converge however many rows agent-father adds. The 7th
(`notebook_undroppable_remainder_over_cap_breach`) is the ordinary rename case and is already filed as
`FIX-SIGNAL-TYPE-ROUTING-GAP-notebook-undroppable-remainder-over-cap-breach` (`task_board.backlog[555]`).

## 8. Left undone

- `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-SKILLS` still sits in `done[]` carrying a 2026-08-23T13:39:19Z
  `secondary_claimed_at` with no dispatch (AC-5 of the readback row). Not touched: it is not
  `pm_decomposition_complete`, so it is outside the cohort this pass moved, and it may be genuinely
  actionable.
- `FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY` is P0 but `next_agent=agent-father`,
  which is **off** the DRS-ratified allowlist `[architect, ba, pm, po, agents-architect]` — promoting it
  to `ready[]` would buy nothing. Left in `backlog[]` and surfaced to the router as a manual-dispatch
  candidate instead.
- `TASK-BRANCHGUARD-POSTCHECKOUT-HOOK` is at `ready[]` index 90 of 108 with 8 P0s ahead; the Ready-Lane
  Consumer takes one row per turn and tiebreaks on **array index, not age**. It needs a hand-dispatch or
  a re-rank, not more triage. Deliberately not re-ranked here — that would displace 8 peer P0s.

---

## 9. ADDENDUM 12:46Z — I had to correct my own §4 fifteen minutes after writing it

Four envelopes landed at `drained_at=2026-08-25T12:42:01Z` (peer cowork drain) **after** the CLEAR in §6.
Two of them falsify notes this same session had just written. Correcting them rather than leaving a
superseded claim on the board — which is exactly what §4 criticised in someone else.

### 9a. The cycle-snapshot fix shape was wrong again — and the right one is smaller

§4 concluded "writer **and both skill copies** must land together". **That is now withdrawn too.**
Envelope `root-cause-mechanism-corrected` (the 5th revision of this one investigation):

- Writer (`tick-snapshot.md` Step 4.7, `FILE_TICK=$(date -u +%H:%M)`) **and** reader both sample **raw
  wall clock, at two different instants, at minute granularity**. Copy A hits only when the writer's raw
  minute lands in the reader's 5-minute bucket; copy B only on an exact minute match with no rollover.
  Both are structurally capped well under 100% *against this writer*. **Neither copy is "the correct one",
  so no rounding rule can bridge them.** The observed ~3-of-6 hit rate is what that geometry predicts.
- **The decisive observation:** at 12:08:48Z the reader **enumerated the directory, saw
  `cycle-snapshot-12:05.json`** (written 12:05:59Z, ~1–2 min old, comfortably inside the ≤7-minute
  freshness gate **both** copies already specify at `cycle-bootstrap:64` / `step-0-cowork:42-43`) **and
  discarded it** citing an "exact-tick match" rule. `grep` finds no exact/nearest/newest rule in either
  file. The agent invented it to fill a gap where the prose names a key but never says what to do with a
  near-miss.
- **Therefore the key does not need to agree at all.** Delete the key lookup; take the **newest**
  `cycle-snapshot-*.json` inside the 7-minute window the copies already agree on. The filename becomes an
  opaque uniquifier and the freshness gate becomes the whole contract. **No writer change**, no new field,
  no cross-file key agreement to maintain, and the near-miss gap that produced the invented rule closes by
  construction.

**Where it was written.** `FIX-CYCLE-SNAPSHOT-…-NOMINAL-TICK` had only **75 B** of prose headroom, so its
note was *shrunk* (951 → 979 chars, net +28 B) to carry the superseding verdict and a pointer. The full
design went onto `DESIGN-COWORK-FANOUT-T2-CYCLE-BOOTSTRAP-EXTRACTION` (`ready[]`, agent-father, ~10 KB
headroom) — **the row that already opens that exact Step -1 block in that exact file**. Cheapest possible
place to land it, and it cannot drift from the code it describes.

### 9b. A second, independent starvation path on the same producer/consumer pair

Envelope `tick-observations` is a hard, id-addressable instance: the 12:00Z tick spawned
news-scout-offhours and alert-commander-critical **in the same second** (12:06:55Z); news-scout wrote
agent_signals **11395-11398**; alert-commander's committed `c256` records its bus read as *"Only bus hit:
freshness-sla-monitor urgent_news id11394"*. **11394 < 11395** — the consumer read the bus strictly before
its same-second co-producer wrote to it, then correctly exited silent. The failure is **silent and
plausible** (a clean "no signal" exit), not loud.

**Not a duplicate of the row minted 15 minutes earlier, and the boundary is now written on both.**
`FIX-NEWSSCOUT-PRODUCER-CADENCE-4H-EXCEEDS-AGENTSIGNALS-TTL-2H-CONSUMER-BLIND` is **temporal** (signals
expire before the next producer run). `DESIGN-COWORK-FANOUT-PRODUCER-CONSUMER-ORDERING` is **ordering**
(consumer reads before producer writes, same tick). Fixing either alone leaves the other live.

The parent ordering row is **159 B OVER** the prose ceiling and can no longer accept any annotation, so the
evidence landed on `DESIGN-COWORK-FANOUT-T4-ALERT-COMMANDER-RECHECK-LOGIC` — the child that owns the
recheck guard — as its regression fixture.

### 9c. Two more brand-new types, two minutes after I filed the finding about them

The 12:42Z batch carries `root-cause-mechanism-corrected` and `tick-observations`. **Both are new type
names, neither existed when §7 was written.** That is live corroboration, inside two minutes, of the
finding folded onto `FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE`: this producer mints
a type per report. All four are unrouted, so all four are held under the same policy — inbox 12 → 16 by
peer append, not by anything left untriaged.

### 9d. The commit captured peer content, as designed-around but worth recording

`git show HEAD:docs/data/orch/orch-state.json` reported `inbox=16`, not the `12` read back from disk
moments before. Cause identified, not assumed: the peer drain appended 4 envelopes in the read→commit gap.
`git add -- <paths> && git commit -F <msg> -- <paths>` bounds the **file list**, never the **content**, on
a multi-writer file. Verified the peer append is purely additive and reverted nothing of mine: all 8
wrappers resolve at `task_board.archive[22..29]` in HEAD, all 3 mints at `backlog[567..569]`, and **0 of
17** cleared `envelope_id`s survive.

### 9e. Same hazard, opposite direction — my board write shipped under a peer's message

The mandatory AC-3 self-check fired on the self-correction commit: `docs/data/orch/orch-state.json` was
**not** in `7a0404657`. Cause located, not guessed — peer commit **`01d170930`**
(*"chore(signals): drain tick 12:37Z"*) landed in the gap between the `orch-apply.sh` write (12:46:06Z) and
the `git add`, and committed the hot file from the working tree, **sweeping this session's Write-D board
mutation in under its own message**. By the time `git add` ran there was nothing left to stage, so the path
was silently dropped from the commit.

Verified nothing was lost: `git show 01d170930:docs/data/orch/orch-state.json` carries
`po_scope_addendum_20260825T1245Z` on `DESIGN-COWORK-FANOUT-T2`, and all four Write-D mutations resolve in
HEAD's board.

§9d recorded this hazard in one direction (a peer's rows landing in *my* commit). This is the **reciprocal**
instance in the same session, ~10 minutes apart: `git add -- <paths> && git commit -F <msg> -- <paths>`
bounds the file list, never the content, and on a hot multi-writer file **either party can end up carrying
the other's bytes**. The practical consequence is attribution, not loss — but it means
"my commit contains my change" is not a safe inference on `orch-state.json`, and **AC-3 must check HEAD's
CONTENT, not just whether the path appears in one's own commit**. Both halves of this session's board work
are in HEAD; they are simply spread across three commits, two of which are not this session's.
