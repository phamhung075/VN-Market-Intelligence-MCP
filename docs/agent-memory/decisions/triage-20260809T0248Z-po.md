# PO decision — cron fire-gap escalation (2026-08-09T02:48Z)

task_id: FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL (mint) + FIX-MONTHLYSIGNALQUALITYAUDITJOB-MISSED-JULY-RECOVER-GUARD (reprice)

## Decision
Rejected the escalation's framing. Did NOT attach the "Aug-7 23-cron simultaneous stop" to the
monthly-audit row as corroborating evidence, and did NOT mint a container-crash-recovery row.
Minted a detector-defect row instead; repriced the monthly row P3→P2 on an independently
verified SECOND consecutive miss (2026-08-01), not on the escalated incident.

## What was considered
1. Attach as corroborating evidence + reprice on blast radius (the escalation's default).
   REJECTED — premise refuted at source.
2. Mint a separate "no cross-restart cron replay" row (the escalation's alternative).
   REJECTED — the mechanism is real in node-cron@3.0.3 but did not cause this incident;
   minting it would have shipped a fix for a failure that never happened.
3. Mint the detector defect + reprice the one genuine gap on its own merits. CHOSEN.

## Why the change from plan
The endpoint ships `expected_last_fire` in the same payload as `status`. Comparing the two
(the thing neither the auditor nor ops did) collapses 23 of 24 flagged rows to false positives:
15 fired within 0-2s of their own computed expected fire; 3 are a 7h timezone skew; 1 is a
cron dom/dow AND-vs-OR divergence; 2 are a merged job (alertScanParallelJob); 1 is a
deliberate env gate; 1 is benign jitter. Crash refuted by ops's own data — marketEarningYield
fired 09:30:01, eveningSummary 15:30:01 on 2026-08-07, hours after the alleged 08:50 death;
docker RestartCount=0, FinishedAt=0001-01-01.

## Near-miss verdict
eveningSummary / ohlcvDailyAggregator / ohlcvSanityCheck need NO urgent watch. All three fired
within 1s of their expected Friday fire (15:30:01 / 15:03:00 / 15:05:00 UTC). Their "~35h of 36h"
framing is the same broken age ladder. They will roll MISSED over Sunday and self-clear Monday.
The Monday 2026-08-10 check is unnecessary for them; it remains useful only as a cheap
confirmation that the detector-defect diagnosis is right.

## Verification performed before claiming any write
- Live `GET /api/cron-status` snapshot, per-row delta computed, not eyeballed.
- `cronLivenessClassifier.ts` read in full — classifier signature takes no expected-fire arg.
- `node-cron@3.0.3` `src/scheduler.js` + `src/time-matcher.js` read at source.
- `schedulerJobTable.ts:447-458` (merged alert scan) and `:132-140,673-681` (rag env gate) read.
- `docker inspect` for restart/exit state.
- Both board rows re-read on disk after `orch-apply.sh`.
