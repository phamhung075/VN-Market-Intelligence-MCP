# Decision Journal — Sprint SYSTEMIC-REMAKE-P1 · developer

**Sprint goal:** (ambient active sprint at time of write — these two tasks are their own PO brief-signoff, not part of this sprint's own goal)
**Agent:** developer
**Started:** 2026-07-07T21:20:00Z

---

### STEP developer-S1 · developer · 2026-07-07T21:20:00Z
**task-id:** F1-LAUNCHD-COWORK-BACKSTOP
**what-done:** Generalized scripts/cowork-fb-daily-firer.sh (fb-only, hardcoded UTC-window if-chain) into scripts/agents-flow/cowork-guaranteed-slot-firer.sh — calls cowork-match-slots.js (same matcher the live */15 dispatcher uses), filters to guaranteed===true, fires each match's trigger_prompt verbatim. Retired the fb-only script + plist into the generalized launchd/com.vn-market.cowork-guaranteed-slot-firer.plist.
**what-considered:**
- Copy the retired script's per-slot if-chain and add 4 more branches (chef/digest/tnb) — rejected per architecture brief §3: repeats the exact hardcode-accretion pattern that caused this outage class to recur, and every future guaranteed slot would need a script edit.
- Matcher-driven generalization (chosen) — one SSOT (cowork-schedule.json), zero drift between OS-level backstop and live dispatcher, zero script edits for future guaranteed slots.
**why-decision:** Matcher-driven design is the only option that actually closes the recurring-bug-escalation flag (brief §1) rather than deferring it to the next new slot.
**why-change:** No change from brief §3/§5.1-2. One addition beyond the brief text: discovered empirically (§3.7 told me to verify, not assume) that this macOS host has NEITHER `timeout` NOR `gtimeout` on PATH — implemented a pure-bash background-process + watchdog fallback in `_bounded_exec()`, proven by a regression test (T10) that a hung claude process is actually killed near the configured bound, not just documented as bounded.

### STEP developer-S2 · developer · 2026-07-07T21:20:30Z
**task-id:** FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED
**what-done:** Extended scripts/agents-flow/auditor-tier1-probe.sh with a 6th check (`_check_launchd_agents`) — every repo-tracked launchd/*.plist Label (read off the plist file itself, never hardcoded) must appear in `launchctl list` output. FAILURE verdict + detail naming the missing label(s) if not. Extended auditor-tier1-probe.test.sh with 19 new cases incl. the mandatory injected-fault pair (T25 FAILURE-on-missing, T26 ALL_GREEN-on-restore) per brief §6.7 / feedback_fence_false_green discipline.
**what-considered:**
- Hardcode the required label list (e.g. just "com.vn-market.cowork-guaranteed-slot-firer") — rejected: reintroduces the same hardcode-accretion problem Task 1 was built to avoid; a future new LaunchAgent would need a script edit to be covered.
- SSOT = this repo's own launchd/ directory (chosen) — every tracked *.plist's Label is required-loaded, read live each run. Matches Task 1's own "don't hardcode, read off the SSOT" design philosophy.
**why-decision:** Directly closes the gap the brief found: the old fb-daily-firer plist WAS loaded+firing 07-01→07-04, then silently unloaded with nothing detecting it — this check is the only mechanism in the pipeline that would have caught that unload (verdict=FAILURE → cron-detect-loop spawns system-auditor subagent → its flow alerts BUG channel; unchanged downstream path, no new alerting code needed inside this READ-ONLY pre-gate script per its own PURE SHELL/no-MCP-calls invariant).
**why-change:** No change from brief §3.8. Scoped the "+ bug alert" acceptance wording to what this specific script owns (verdict=FAILURE, detail names the label) — the actual Telegram alert is already inherited via the existing FAILURE→spawn-system-auditor pipeline (cron-detect-loop/SKILL.md Job 2), unchanged and out of this FIX task's file scope.

### STEP developer-S3 · developer · 2026-07-08T00:50:00Z
**task-id:** FIX-NEWS-CB-FALSE-CLOSED
**what-done:** Root-caused two live SOURCE HEALTH bugs in apps/mcp-server (verified apps/news-fetch/ is an unwired parallel pipeline via api-gateway NEWS_URL, not the owner). (A) intelligenceCycleJob.ts's defaultPollNews() re-injected no-op reuters/tradingeconomics stub fetchers every 15-min tick, which pollNews.ts's health loop treats as real failures (STUB_CAPABLE_KEYS doesn't cover them) — silently overwrote the Sprint-1833g recordDisabled() seed with an ever-climbing "down" count (79+, live-confirmed via get_system_status). Fixed by removing those 2 keys entirely, restoring pollNews.ts's own resolvedFetchers contract. (B) formatSourceHealthTable's 18-char name column truncates "Trading Economics" (17c) and "Trading Economics News" (22c) to the identical string — live-confirmed byte-identical duplicate row. Widened to 26c, header/separator now derived from the same constant.
**what-considered:**
- Re-widen the CB failure threshold (original 2026-06-13 filing's proposal) — rejected: the CB already correctly shows "down"; the real bug is a disabled source being re-touched at all, not a threshold miscalibration.
- Add teChromiumNews to STUB_CAPABLE_KEYS too (same class, also shows down/never) — deferred: that source has a real, non-deprecated fetcher (Task 1843 stub is CPU-protection only); its accuracy is a separate, unfiled concern outside this task's DoD (Reuters+TE-legacy only).
**why-decision:** Root-cause fix (stop the re-injection) matches DoD's explicit "fix at the actual call site, not by re-widening the CB threshold" instruction; matches pollNews.ts's own already-documented Sprint-1833g contract, so the fix is 2 deletions, not new logic.
**why-change:** No change from task brief. Original filing's "CB false-closed" framing was stale (dev-team's live pre-check already confirmed CB now correctly reports [OK]→"Ngưng"); root cause evolved into "disabled source shows as broken" — fixed that live bug instead per task's own note "symptom has evolved, build on this, don't re-derive."

### STEP developer-S4 · developer · 2026-07-08T06:06:37Z
**task-id:** FIX-NEWS-CB-FALSE-CLOSED
**what-done:** dev-team's post-rebuild live get_system_status re-check found the S3 fix incomplete: apps/mcp-server/src/interface/mcp/routes/pushNewsHandler.ts:84 (handlePushNews, fires on EVERY VPS POST /api/push-news event — far more frequent than the 15-min scheduled cycle) had its OWN independent `reuters: async()=>[]` / `tradingeconomics: async()=>[]` stub injection into pollNews() that commit aa87cfe05 never touched (aa87cfe05 only patched intelligenceCycleJob.ts's defaultPollNews, the scheduled-cron call site). Same root cause, same fix: omitted the two keys entirely from the fetchers object passed to pollNews() — pollNews.ts's resolvedFetchers contract (Sprint 1833g) only adds them when a caller explicitly injects a fetcher, so omitting them leaves the recordDisabled() seed untouched forever. Both call sites (intelligenceCycleJob.ts + pushNewsHandler.ts) are now fixed. Extended FIX-NEWS-CB-FALSE-CLOSED.test.ts with a source-scan check + a behavioral test invoking handlePushNews end-to-end confirming Reuters RSS / Trading Economics stay disabled after a push cycle (polls on CafeF RSS's health-record flip — early in pollNews — rather than the full pipeline-complete signal, to stay hermetic against real ragInsert network latency in this sandbox, RAG_SERVICE_URL unreachable). tsc clean. Targeted 41-file regression sweep: 353 pass / 11 unrelated fails, all in pre-existing pollNews tests (1332/1345a/102/1227/1288) hitting the same unreachable-RAG_SERVICE_URL 8s timeout — confirmed pre-existing/non-deterministic via git-stash isolation. Full `bun test` suite also run for baseline comparison: 14312 pass / 60 fail / 5 errors / 40 skip across 1177 files — failures span unrelated domains (insider transactions, market cap, MACD, SSE registration, vps_push_log across multiple files), the signature of full-suite-scale test-isolation breakdown (shared `:memory:` DB singleton across 1177 files in one process), not a regression from this 2-file diff. Commit 8810a34d5, pushed to main. Board flipped via orch-apply.sh: status stays REVIEW, next_agent→ops (needs live rebuild+swap+get_system_status re-verify, same as S3).
**what-considered:**
- Trust the S3 fix as complete and only re-verify live — rejected: dev-team's own live re-check already falsified that (Reuters RSS / Trading Economics still climbing "Suy giảm" 2 min post-restart), so re-verifying without a code change would just reproduce the same false-closed symptom.
- Widen STUB_CAPABLE_KEYS to cover reuters/tradingeconomics instead of removing the stub injection — rejected: same reasoning as S3, the actual bug is a disabled source being re-touched at all (via an unconditional stub), not a health-loop misclassification; matches the "fix at the actual call site" DoD instruction applied consistently across both call sites.
- Wait for the full pipeline-complete log signal in the new behavioral test (mirroring production fire-and-forget shape exactly) — rejected after it caused a real flaky timeout under concurrent test load (ragInsert's `AbortSignal.timeout(8_000)` against an unreachable RAG_SERVICE_URL, sequential per-item, up to ~24s worst case for 3 items): switched to polling CafeF RSS's `lastSuccessAt` flip, which fires immediately after the health-recording loop and well before the slow per-item embed loop — same assertion coverage, hermetic and fast (~2.6s).
**why-decision:** Identical fix pattern to S3 for consistency and lowest-risk (2 deletions, no new logic); the CafeF-RSS-health-flip polling signal isolates exactly the code path this task cares about (source-health recording) from an unrelated, pre-existing sandbox limitation (no reachable RAG service), avoiding a flaky regression test that would itself need future debugging.
**why-change:** No change from task brief — this is the second-pass continuation the router's revert commit (820d5c095) explicitly dispatched: "only the pushNewsHandler.ts:84 stub needs the same fix pattern applied... then a fresh rebuild+swap+live-verify."

### STEP developer-S5 · developer · 2026-07-08T08:20:00Z
**task-id:** FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE
**what-done:** Added a depends_on eligibility filter (inline OR backlog-detail.json detail_ref lookup, DONE_VERIFIED-only, conservative-skip-if-nowhere) to devteam-backlog-promote-bounded1.jq's candidate-selection stage; threaded --slurpfile detail through both call sites; new test-devteam-bounded1-depends-on.sh 17/17 pass.
**what-considered:**
- Filter only the already-picked top row post-hoc — rejected: task spec required filtering DURING selection so a blocked top row can't starve an eligible lower-ranked one; verified this with a real 2-candidate fixture (Case 2).
- Assume depends_on is always an array in backlog-detail.json — rejected after live grep found 7/321 rows store it as a bare string; added as_dep_array normalizer before shipping.
**why-decision:** Matches the router's own diagnosed root cause + precedent (revert_note on the live board row) exactly — DONE_VERIFIED-only mirrors existing repo convention, conservative-skip is strictly safer than blind-dispatch.
**why-change:** No change from task spec. Did not promote FACTORY-TECHANALYSIS-go-livepath-tests myself (PO declined manual promotion this cycle) — sanity-checked via scratch-copy dry-run only that the fixed script now picks it correctly.

### STEP developer-S6 · developer · 2026-07-08T18:15:00Z
**task-id:** FIX-DEVTEAM-PREFLIGHT-SF1-REENTRANT
**what-done:** Confirmed status_note's diagnosis exactly: `_step_sf1_claim()` in dev-team-tick-preflight.sh inspected only `.claimed` on a failed claim, unconditionally returning "peer holds it" -> SKIP even on self-hold. Mirrored `_step_fire_election()`'s existing self-hold branch: compare `current_holder.owner_client_session` vs self — self-hold now heartbeat-renews SF-1 and returns 0 (proceed) instead of false-SKIP; real peer (different session) still SKIPs, releases nothing (unchanged).
**what-considered:**
- Only path: mirror `_step_fire_election()`'s proven self-hold pattern already in the same file — no alternative design considered, the fix template was explicitly pre-diagnosed and verified correct by reading both functions side-by-side before editing.
**why-decision:** Exact symmetry with the sibling function eliminates the asymmetry that caused the bug; live-verified against the actual self-held dev-team-cron-singleton lock (owner_client_session == 5a45feda-431e-46c8-941d-a6539a0eca77): verdict flipped SKIP -> RUN, heartbeat renewed both locks, no lock corruption.
**why-change:** No change from status_note's diagnosis or the dispatch prompt's prescribed fix. Added T19 (self-hold -> RUN+heartbeat) + T20 (peer-hold regression guard) to the existing test harness — 55/55 pass.

### STEP developer-S7 · developer · 2026-07-08T19:10:00Z
**task-id:** FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE
**what-done:** Added `mcp_call_gateway_meta(tool_name, json_args)` to `scripts/agents-flow/mcp-call.sh` implementing the 3-step stateful handshake (`initialize` -> `notifications/initialized` -> `tools/call`, reusing the `mcp-session-id` header) against the live gateway; added `docs/standards/gateway-call-contract.md` §6 Degraded Mode (self-diagnosis, coverage matrix, discovery-first cross-ref, §6d de-escalation rule that stops CRITICAL re-raise churn — the task's core deliverable).
**what-considered:**
- Only path: SPIKE (docs/architecture-briefs/2026-07-08-gateway-blind-cli-handshake-spike.md §2/§5b/§6) already proved the exact 3-request sequence live via curl — implemented exactly that, reusing existing `_mcp_call_parse()` for the final `tools/call` response (same SSE-framed JSON-RPC shape, confirmed by my own live re-probe).
- Header capture: chose `curl -i` (headers+body on one stream, parsed via awk blank-line split) over a separate `-D tmpfile`, to avoid mktemp/cleanup churn and match the existing single-invocation style of `_mcp_call_curl()`.
**why-decision:** Live-verified against production (not just canned stubs) end-to-end for all 3 meta-tools before writing docs — caught a real gotcha this way: `search_tools`'s raw JSON-RPC schema requires `{"query":...}`, not `{"keyword":...}` as §2's native-tool-call pseudocode shows (different call surface); documented this explicitly in §6b so the bridge isn't shipped with a footgun.
**why-change:** No change from SPIKE §6 scope. Did NOT touch existing `mcp_call()` (kept endpoint/contract separate per explicit instruction) and did NOT add CLI-dispatch routing for the 3 meta-tool names (out of stated scope, would add an untested code path) — deliberately conservative diff. TDD: RED (`mcp-call-gateway-meta.test.sh`, function-not-found) -> GREEN (20/20, stubbed `_mcp_call_gateway_curl`) -> live smoke-test against production as an extra verification layer beyond the stub tests. shellcheck clean; existing `cowork-tick-preflight.test.sh` (20/20) + `dev-team-tick-preflight.test.sh` (55/55) re-run to confirm zero regression (both stub `mcp_call` after sourcing, unaffected by the addition).

### STEP developer-S8 · developer · 2026-07-09T10:05:00Z
**task-id:** FIX-CLOSEGATE-STEP4-ATOMIC-HANDOFF-SCRIPT
**what-done:** Minted `scripts/ops-closegate-handoff.jq` — one jq expr, `--arg task_id/from_lane/next_agent/now`, sets board row `.next_agent`, `error()`s if row absent, conditionally syncs `.head.next_agent/updated_at/updated_by` ONLY if `.head.active_task_id==$task_id`. Added runbook `docs/protocols/docker-deployment-runbook.md` § Close-gate table Step 4b row + updated Delegation-rule sentence.
**what-considered:**
- Only path: brief §2.1 fully specified inputs/behavior/precedent (`router-d1-claim.jq`/`devteam-backlog-claim-bounded1.jq`) — implemented exactly that contract, no design choice left open.
- Legacy `.task_id` field fallback (schema's own `collectAllTaskIds` checks both `id`+`task_id`) — rejected: brief literally says `select(.id==$task_id)`, adding a fallback would exceed the specified contract (simplicity gate).
**why-decision:** Matches brief §2.1 verbatim + grep-verified no hardcoded task-id/lane literal in the filter body (only literal is `"ops"` for `updated_by`, which the brief itself specifies).
**why-change:** No change from brief. No `.jq` unit-test harness convention exists in this repo (checked: `router-d1-claim.jq`/`devteam-backlog-claim-bounded1.jq` ship without one) — verified instead via 3 manual scratch-copy scenarios (row+head match / row-only, head untouched / absent-row error+empty-stdout) plus a real `bun scripts/orch-validate.mjs` PASS on the candidate output. Split scope per dispatch note: did NOT touch the commit-gate footnote (§2.2) or STEP ops-Sn enforcement (§2.3) or the commit-boundary SKILL.md zone table — those are the follow-on FIX-CLOSEGATE-STEP4-COMMIT-JOURNAL-DISCIPLINE task (agent-father, depends_on this task).

### STEP developer-S9 · developer · 2026-07-09T08:22:19Z
**task-id:** FIX-CLOSEGATE-STEP4-ATOMIC-HANDOFF-SCRIPT (qa CHANGES_REQUESTED bounce)
**what-done:** qa PASSED `scripts/ops-closegate-handoff.jq` outright (4/4 brief DoD checks + 3 scratch-test scenarios + `orch-validate` all clean) but CHANGES_REQUESTED on the doc side: `docs/protocols/docker-deployment-runbook.md:124` embedded the Step-4b invocation inside a GFM table cell with the shell pipe escaped as a backslash-pipe to keep the table syntax valid — as RAW TEXT (what `Read`/any text-consuming agent actually sees, the row's real audience) that backslash-pipe is not a valid shell pipe; copy-pasted literally, `jq` would receive the pipe character and `bash` as extra positional file args and exit 2, never reaching `orch-apply.sh`. Fix: moved the invocation out of the table cell entirely into a fenced ```bash code block placed directly below the table (real unescaped `|`); the cell prose now just points at the block below it.
**what-considered:**
- Option 1 (de-escape the pipe in place, keep it inline in the cell, rely on GFM code-span rendering to tolerate a raw pipe inside a table cell) — rejected after checking the GFM table spec: a pipe character inside a cell, even inside a code span/backticks, still terminates the column early unless backslash-escaped (column-split happens before code-span parsing); would have traded one broken-copy-paste bug for a broken-table-parsing bug.
- Checked for existing precedent of a copy-paste-critical command living in a fenced block directly below this table — none of the other 8 rows do this (all their commands stay inline in backticks, none contain a literal pipe), so no direct precedent to follow; set this as the pattern going forward for pipe-bearing table-row commands.
**why-decision:** Matches qa_note's own flagged "likely safer/more conventional" Option 2 exactly; verified end state satisfies all 3 dispatch requirements: (1) `grep -F` of the exact invocation string with a real unescaped `|` returns the line; (2) `grep -n '\\|'` across the whole file returns nothing (zero remaining escaped pipes anywhere); (3) all 9 raw lines of the table (header + separator + 7 rows) have exactly 4 pipe-delimited columns each — table still parses as valid GFM, contiguous, no line broke it early.

### STEP developer-S10 · developer · 2026-07-09T14:40:00Z
**task-id:** FIX-DEVTEAM-BOUNDED1-DETAIL-ITEMS-ARRAY-INDEX
**what-done:** `devteam-backlog-promote-bounded1.jq` L150 bound `$detail_items` as `($detail[0].items // {})` assuming an object shape, but live `backlog-detail.json` `.items` is a plain ARRAY of 437 id-bearing objects — `effective_depends_on`'s `$detail_items[.id]` object-index then crashed (`Cannot index array with string`) on the first `detail_ref`'d row scanned, aborting the whole transform via `orch-apply.sh`'s empty-candidate guard. Made the binding shape-defensive: id-key the array at ingest (`map(select(.id!=null)|{key:.id,value:.})|from_entries`); object input passes through unchanged.
**what-considered:**
- Only path: bug report's own suggested fix matched the precedent shape already used elsewhere in the file (`as_dep_array` normalizer) — implemented as specified, no design choice left open.
**why-decision:** Fixes the array-vs-object lookup shape only, zero depends_on semantic change, zero hardcoded task-id literals (grep-clean).
**why-change:** No change from dispatch. Added RED test Case 1c to existing `test-devteam-bounded1-depends-on.sh` (fixtures were all object-shaped before — that's WHY 17/17 passed while prod crashed); reproduced crash pre-fix, 18/18 GREEN post-fix. Verified live-data reproducer (`TASK17-FOREIGN-FLOW` against real `backlog-detail.json`, exit 0 post-fix) + end-to-end scratch-copy run through `orch-apply.sh` (`ORCH_APPLY_LIVE_FILE_OVERRIDE`, WIP forced to 0): exactly one row promoted, no empty-candidate abort. Inspected `devteam-backlog-claim-bounded1.jq` — never touches `backlog-detail.json`, no equivalent bug, left unchanged.
**why-change:** No change from qa's mandate. One self-correction caught mid-fix: my first draft referenced the pipe character in the cell's own prose as an inline code span (`` `|` ``), which itself reintroduced an unescaped pipe inside that same table cell (5 pipes instead of 4 on that one line) — caught via a raw pipe-count sanity check run across all 9 table lines before considering the fix done, reworded to avoid a literal pipe character in the prose entirely. Board (via `scripts/ops-closegate-handoff.jq`'s own atomic-write pattern, hand-applied here since this is a developer→qa bounce not an ops Close-Gate Step-4 handoff): status stays REVIEW (untouched per dispatch instruction), `next_agent` developer→qa, `qa_verdict` CHANGES_REQUESTED→null (matches this repo's existing CHANGES_REQUESTED→fix→re-review convention, precedent commit `975465911`), `.head.next_agent` synced to qa in the same atomic `orch-apply.sh` write (`.head.active_task_id` still pointed at this task). `scripts/ops-closegate-handoff.jq` itself untouched — qa already confirmed zero changes needed there.
