# Decision Journal — triage-20260815T0353Z · po

**Sprint goal:** ambient triage — dev-team Step 1, 17-entry durable inbox + 11 unresolved telegram reports
**Agent:** po
**Started:** 2026-08-15T03:53:02Z

---

### STEP po-S1 · po · 2026-08-15T04:00:00Z
**task-id:** FIX-AUDITOR-DCYCLE1-MALFORMED-KEY-SENTINEL-COLLAPSES-DISTINCT-LOSSES
**what-done:** Split an 11-signal / 11-telegram burst into 10 no-mints and exactly 1 new P2 FIX row, after establishing that the burst itself was a correct one-time drain and that only the `malformed-key` variant names a live, self-perpetuating defect.
**what-considered:**
- Mint one FIX per lost tick (10-11 rows) — REJECTED: near-duplicate reports for one underlying mechanism; the board is already at 415 backlog rows.
- Mint ONE row covering all 11 as "auditor cycles are being lost" — REJECTED: the losses are historical (08-11..08-14) and their root cause is already owned by `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES` (backlog, 11th+ occurrence). A second row would duplicate it.
- Mint NOTHING (treat the whole burst as expected drain noise) — REJECTED: that is right for 10 of 11 but would have discarded the malformed-key finding, which is the only part that is still live.
- Re-open / regress `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION` — REJECTED: that fix is correct and its live-fire is the proof it works. The new row explicitly fences this out as a non-goal.
**why-decision:** Four verified facts, none inherited from the signal text. (1) `docs/data/auditor-dedup-ledger.json` holds ONE key `auditor-cycle-loss:malformed-key` while >=4 distinct malformed marker filenames existed in that window — the sentinel is a constant, so N unrelated losses collapse to 1 undiagnosable signal. (2) Re-ran the sweep's own `find` predicate verbatim AFTER the sweep: 2 marker files it should have cleared are still on disk. (3) `.gitignore:45` was added by the same fix, so `git ls-files | grep -c auditor-cycle-markers` = 0 while those 2 files exist — QA's DONE_VERIFIED evidence line "zero tracked marker files" is structurally blind to exactly this residue. (4) Commit `9b7a82074`'s own message states the surplus markers are ABORT-preserved "for the next real Tier-1 cycle's retry" — a retry guaranteed to re-collide because the dedup key never varies.
**why-change:** No change from plan on the routing rule (dedup-first, one root cause per defect). The deviation is that the SHIPPED, QA-VERIFIED fix carries the new defect — so the dedup hit against its DONE_VERIFIED parent had to be argued as parent-not-duplicate rather than accepted as a close.

### STEP po-S2 · po · 2026-08-15T04:00:00Z
**task-id:** FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS
**what-done:** Folded 3 more `escalated=true` bug-escalation fires onto the existing P0 row (occurrence_count 5→8) and promoted it into this tick's BATCH instead of minting.
**what-considered:**
- Mint a new row per `escalated=true` fire — REJECTED by `triage-signals.md`'s own rule: the row already owns the REPEAT-OFFENDER-AFTER-BLOCK branch.
- Leave it in BACKLOG and only fold — REJECTED: the counter is the argument. 20 warns at mint (08-14T13:37Z) → 27 (22:55Z) → 30 (08-15T03:01Z), blast radius 5 files → 16 files, `outcome=blocked` throughout. Ten warns in 13.5h with zero remediation is not a backlog item.
**why-decision:** All 3 payloads parsed at source per the mandatory rule (never off `git show --stat`). `actor=632721c2-41e4-4aff-8d06-a47cf80dc0d7` is the same session running this triage, so the hook is now hard-blocking the fleet's own dispatcher. The row's own AC-4 predicted this: the payload names only the actor session, never the offending call-site, so nobody can act on it without a repo-wide hunt — and nobody has, for a day.
**why-change:** no change from plan.

### STEP po-S3 · po · 2026-08-15T04:00:00Z
**task-id:** FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED
**what-done:** Ran the row's own coverage guard live and folded the 3rd `auditor_cycle_loss` occurrence, separating the one genuine gap from the one known guard false positive.
**what-considered:**
- Add an `auditor_cycle_loss` row to `triage-signals.md` myself — REJECTED: PO editing its own routing table mid-triage is the kind of self-modification that should go through the row's assigned architect, and the row's scope already names 20+ types. A one-type patch would leave the guard red and train readers to ignore it (the row's own AC says so).
**why-decision:** `guard_signal_type_coverage` FAIL rc=1 at 03:57Z with `[auditor_cycle_loss, repair_task_request]`. `repair_task_request` IS routed (full Pipeline-A row) — it reads unrouted only because `$routed` mirrors Pipeline-B while the jq scans every `to==po` row, exactly the false positive this row's AC (b) documents. Recorded so the next triager does not re-investigate it. Also recorded that the FAIL set SHRANK from 6-11 types to 2, not from fixes but from rows ageing out of the hot queue — which is itself evidence for the AC that extends the guard to drained signal files.
**why-change:** no change from plan.

### STEP po-S4 · po · 2026-08-15T04:00:00Z
**task-id:** CLEAN-CTXBLOAT-DECISION-JOURNAL-COWORK-QA24-BYTECAP
**what-done:** Minted a first-fire CLEAN for the sprint-decision-journal bloat class, and folded the notebook-write SKILL.md breach onto its existing row with corrected live numbers.
**what-considered:**
- Fold the decisions/ breach onto one of the 5 existing bloat rows — REJECTED: all 5 target notebooks/, skill files, or orch-state.json. No row covers `decisions/`, and the longtail table's prescribed zone for this type (`docs/agent-memory/notebooks/`) does not even match the file.
- Prune the journal to fit — REJECTED and written into the AC as a prohibition: a sprint decision journal is an audit record; the correct remedy is a split.
- Mint a second row for notebook-write SKILL.md — REJECTED: `CLEAN-CTXBLOAT-NOTEBOOK-WRITE-SKILL-215L-OVER-200L-CAP` already owns it.
**why-decision:** Measured both files rather than trusting the payloads. The qa-24 journal is 175L/38222B — 29% of its line cap but 106% of its byte cap, i.e. pure long-prose overage. notebook-write SKILL.md is 251L/15807B live, vs the 215L/13016B in its own row's title and the 245L/15058B in the 00:28Z signal: it grew ~17% in under 4 days WHILE the CLEAN row sat in backlog. The cap is reported, never enforced.
**why-change:** no change from plan.

### STEP po-S5 · po · 2026-08-15T04:00:00Z
**task-id:** CLEAN-STALE-WORKTREE-AGENT-AE9ED2CD-FULLY-MERGED-3D-IDLE
**what-done:** Minted a CLEAN routed to qa for the stray branch surfaced in the spawn's `git branch` input.
**what-considered:**
- Ignore it (the spawn said "not necessarily yours to act on") — REJECTED: `po/flow/main.md` § Role in dev-team flow makes flagging 0-unmerged-commit branches and stale worktrees an explicit PO CLEAN duty.
- Delete it myself — REJECTED: PO does not perform repo surgery, and a dead worker's uncommitted files are real work (`feedback_dead_worker_uncommitted_live_file_revert`). The AC makes the rescue check a precondition.
**why-decision:** `git rev-list --count main..worktree-agent-ae9ed2cd6f04b3686` = 0, `git merge-base --is-ancestor 4a6d2174c main` = true, tip and directory both idle since 2026-08-12T17:12Z (~3 days). Still registered in `git worktree list`, so the branch cannot be deleted before `git worktree remove`.
**why-change:** no change from plan.
