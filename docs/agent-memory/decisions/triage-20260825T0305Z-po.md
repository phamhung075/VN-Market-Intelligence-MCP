# PO decision journal — triage tick 2026-08-25T02:52Z–03:06Z

task_id: TASK-PO-TRIAGE-SIGNALS-DOC-CORRECTION (primary) + Step 0-SIG inbox triage (15 envelopes → 0)
Dispatched by: dev-team tick 2026-08-25T02:37Z, Review-Lane SECONDARY-Drain (done[]-origin owner-triage class).

## Decision 1 — primary row verdict: REWORK, not DONE_VERIFIED

what-considered: DONE_VERIFIED / rework / reassign / BLOCKED.
why-change: 6 of 7 AC verified by execution; AC-4 falsified by measurement.

Verified PASS: the dated CORRECTION block exists (triage-signals.md:68-73) with the 112/109
month-by-month table, re-derived at the row's own commit 735dfa8 by the block's own stated method;
the falsified "≤1-2 fires each" text survives only as a quotation inside the correction, which is
correct authoring; the Pipeline-B `audit-handoff` row exists AND parses — proven by replaying the
guard's own `pipeline_b_section` + `extract_type_column` extractors, not by reading the table;
signal row `tra-20260822T203234` is still open at status=READ (now cold, in
`docs/data/orch/archive/2026-08.json` `.signal_rows[]`) and was not touched.

FAILED — AC-4. The block asserts system_issue/system-issue are "the two largest type classes in
the whole ~100+-type namespace". Measured by the block's own method:
signal_feedback 230, narrative_contradiction 144, microservice_degraded 121, system_issue 115,
system-issue 115 — ranks 4 and 5. Already false at commit time (signal_feedback 226 vs 112/108).
Inherited verbatim from architect brief `2026-08-23-signal-type-registry-...md` line 56, which
carries the same error. A correction block that deletes one falsified claim shipped another in the
same paragraph. Routed to review[] status=REVIEW next_agent=agent-father so the same SECONDARY
drain re-dispatches it; rework scope is that ONE clause.

The review_note's "guard PASS 8/8" is not reproducible today (exit 1), but NOT this row's fault:
Pipeline-A checking postdates it (commit 4e7aa7ea) and every current Pipeline-B unrouted type was
first tracked after this row completed.

## Decision 2 — running the guard mutated the board; removed my own 4 duplicates

`guard-signal-type-coverage.sh --check` is not read-only. Verifying AC-6 minted 4 backlog rows
(02:52:22-25Z), all subject-duplicates of the existing
`FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS` (which names
`recurring-bug` in its own title). Its dedup is dedup_key-exact, so it cannot see them.
Could not fold the evidence into that row — it is already 12156B, over the 12000B prose ceiling,
so any append would be rejected as net-new growth. Removed the 4 and filed the guard defect itself.
Validated the diagnosis empirically: post-CLEAR the Pipeline-A FAIL disappears entirely and the
guard mints nothing, because its Pipeline-A input is a drain-to-zero queue.

## Decision 3 — the CRITICAL was false, and that is worse than a true CRITICAL

`daily_ohlcv: 0 distinct codes (expected ≥25)` does not reproduce. Ran the C-01 query verbatim on
both planes: live container `/app/data/market.db` → 99 (PASS); host `./data/market.db` → 0, max
date 2026-08-12. Ruled out a timing race — the 99 rows for 2026-08-24 carry updated_at 09:00–15:03
on 08-24, 11+h before the 02:40:42Z probe. Used updated_at only as an upper bound on existence,
never as arrival time (it is mutation time; backfill rewrites most rows).
The sharp edge: `bctc_vps_queue: 1 stale pending >72h` from the SAME cycle reproduces EXACTLY on
the live plane (=1; host says 513). One cycle, two checks, two different databases. Mixed-plane
auditing yields false CRITICALs and, in the other direction, false GREENs.

## Decision 4 — mint the cause, fold the symptom (auditor tier-2)

The missed-cycle envelope folds into the existing 11h row. What I minted instead is the asymmetry:
tier-1's heartbeat is written by the probe's ALL_GREEN branch and carries a per-check evidence
object; tier-2/tier-3's is agent-written with no green gate and carries only a timestamp. Tier-2
stamped `last_healthy_at=02:40:07Z` on the cycle its own notebook §c1013 records as
"Anomalies: 1 new / Status: DEGRADED", having reported itself CLEAN. A health stamp that cannot go
stale while the agent keeps running is a liveness stamp wearing the wrong name — and the missed-
cycle detector reads it.

## Decision 5 — split the CCATO cohort, mint only the uncovered half

Evidence is in `.payload`, not `summary`. All 4 envelopes share (VNM, get_technical_indicators,
2026-08-24) and differ only in `returned_value`: 62.1 / 61 / 60 CONTRADICT the claim (genuine);
"not found in database" AGREES with it (false positive). Folded the 3 genuine into existing CCATO
coverage (SPRINT-CCATO-TRUTHGATE-MCP-NATIVE, FIX-CCATO-NTG-ROWS-..., + 3 more, checked across all
ten lanes). Minted only the FP: a second tool whose null return matches no `tool_null_marker`,
generalising `FIX-NTGPROBE-FOREIGNFLOW-...` from one probe to the marker set as a design.
Explicitly wrote "do not weaken the gate" into the row — 1-of-110 stands.

## Decision 6 — reproduced a reported remedy and found it wrong

Cowork reported the Step 5.1 load parse truncating 2.48→2 under a comma-decimal locale (correct)
and proposed an `LC_ALL=C` pin (wrong). Measured: as-written → `1`; with `LC_ALL=C` →
`1.652.893.70`, because C locale prints space-separated period decimals so `-F','` swallows all
three load averages. The cited precedent script has no `LC_ALL` and the cited path does not exist.
Filed with both measurements so nobody applies the pin alone. This is the item the 01:07Z tick
parked in Carry-over as "mint if it recurs" — it recurred twice.
