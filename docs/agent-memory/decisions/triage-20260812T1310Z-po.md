# Decision Journal — PO Router-Relay Triage 2026-08-12T13:10Z · po

**Sprint goal:** router-relay decision — route the agent-father registry-cron CRITICAL (notebook-auto-prune drop-oldest selects the NEWEST section on mixed-heading notebooks)
**Agent:** po
**Started:** 2026-08-12T13:07Z

---

### STEP po-S1 · po · 2026-08-12T13:10Z
**task-id:** FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE
**what-done:** Reproduced the reported selection from source, found the mechanism already owned by an open row, and folded occurrence 5 + three new ACs (AC-5 mixed-convention inversion, AC-6 no-silent-drop signal, AC-7 shared-lib blast radius) instead of minting a 7th sibling ticket.
**what-considered:**
- Mint a new FIX row (the relay framed it as a NEW finding)
- Fold onto FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE
- Fold onto FIX-NOTEBOOK-AUTOPRUNE-REGEX-HEADING-MISMATCH (review[], literally titled "drops NEWEST not oldest")
- Fold onto FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH (the `###`-vs-`##` trap, already cites agent-father.md)
**why-decision:** I sourced `lib/notebook-section-direction.sh` against the live committed file rather than trust the narrative: keys are `{3:SENTINEL, 58:SENTINEL, 120:20260812032352000}`, so `MIN_KEY` IS the EDIT section's real key and `TIE_COUNT=1` — the direction-aware tie-break at L463-509 is never reached. That rules OUT the regex row (headings parse fine) and the heading-level row (all `## `). It is exactly the `NSO_SENTINEL_KEY` mechanism the ROLLING-SECTIONS row's own `root_cause` already names, and that row already lists both script files in `.files`. Minting would fragment a defect whose script header already tracks 6 prior FIX-* tickets.
**why-change:** Relay asked for a new ticket; dedup says no. The genuinely new content is the failure SHAPE (selection inverts outright when exactly one section is dated), which is an AC on the existing row, not a new fix site.

### STEP po-S2 · po · 2026-08-12T13:10Z
**task-id:** FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE
**what-done:** Escalated P1→P0 and moved backlog[]→ready[] (status READY, supervised/plan_only=false), then put the row at the head of this run's BATCH.
**what-considered:**
- Leave it P1 in backlog[] (it is "only" a notebook hook)
- P0 + leave in backlog[]
- P0 + lane move only
- P0 + lane move + BATCH
**why-decision:** P0 because the loss is active, deterministic and SILENT: 5 confirmed drops across 3 notebooks in 6 days, and one file (agent-father.md, 172L/15376B vs a 12000B cap) is armed to lose content on its very next write. Every recovery so far ran through `git show HEAD:` on UNCOMMITTED work — that escape hatch fails closed the moment the losing write is a cycle's first write. Lane move because the row sat in backlog[] through all 5 occurrences with no sweep draining it. BATCH because of po-S2 @10:02Z: `ready[]` is append-only and the Ready-Lane Consumer tiebreaks by array index, so my row landed at index 83/85 behind 29 older P0s — measured, not assumed. The lane move alone is latency-negative; the 13:02Z entry confirms lane-move + BATCH cleared in ~3h.
**why-change:** No re-ordering of `ready[]` by hand — that is the full-doc mutation class the write contract forbids, and the mechanism is already owned by FIX-READYLANE-NO-SEVERITY-EXPEDITE-FIFO-BURIES-INCIDENT-P0.

### STEP po-S3 · po · 2026-08-12T13:10Z
**task-id:** CLEAN-NB-AGENT-FATHER-MIXED-HEADING-OVERCAP-DISARM
**what-done:** Minted ONE new row — a containment/disarm row for the still-armed file — into ready[] with next_agent=agent-father, P1, and an AC-2 that requires dating every `## ` heading, not just trimming bytes.
**what-considered:**
- No containment row; let the hook fix cover it
- Containment row to claude-manager-helper (owner of the sibling per-file CLEAN rows)
- Containment row to agent-father
- Ask the hook to be disabled fleet-wide until the fix lands
**why-decision:** The hook fix is a code change with a test suite; the file is armed NOW, and agent-father runs on a registry cron. Split of duty is clean: `scripts/agents-flow/` is outside agent-father's commit zone (which is why it correctly refused to patch), but `docs/agent-memory/` IS its zone, and AC-2 changes the heading convention agent-father itself emits — claude-manager-helper cannot own that. Disabling the hook fleet-wide was rejected: it is the only actuator for the 200L/12000B caps, and removing it trades silent over-drop for silent unbounded growth. AC-2 (date every heading) is load-bearing: PO's own 2026-08-07T05:13Z scope-correction proved a trim-only split regresses within a few cycles.
**why-change:** Trim-only was the obvious move and I rejected it on that prior scope-correction — same failure already observed on code-janitor/digest-predict.
