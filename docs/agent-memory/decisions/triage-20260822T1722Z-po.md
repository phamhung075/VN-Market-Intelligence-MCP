# Decision Journal — po triage 2026-08-22T17:22Z

task_id: FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS

## Entry 1 — Verify or refute agent-father's finding #3 (f25dc3d27 destroyed retained sections)

- **what-considered:**
  (a) Read the diff and judge by diffstat — rejected, that is the exact premise error this row's own
      `premise_correction_read_first` field already documents (an earlier report reasoned from `+43/-82`
      and reached a wrong conclusion).
  (b) Compare against the prose contract in `.claude/skills/notebook-write/SKILL.md` — insufficient alone;
      AC-3 Step 1g explicitly permits collapsing to "preamble + 1 section" as a backstop, so a 1-section
      result is not self-evidently a violation.
  (c) **CHOSEN** — counterfactual replay: reconstruct each commit's true parent blob and its new section,
      run the real `scripts/notebook-compose.sh` with the arguments `docs/agents/system-auditor/flow/main.md:1099`
      actually passes (`3 150`), compare the script's output to what was committed.
- **why-change:** (c) is the only option that distinguishes "the actuator ran and produced this" from
  "a model hand-wrote this". Result: `22039783e` → script 3 sections/73L vs committed 5 sections/212L;
  `f25dc3d27` → script 3 sections/172L retaining `## c103` and `## d4-auto` vs committed 1 section/179L.
  **Finding #3 CONFIRMED.** Independent corroboration needing no replay: main.md AC-3 derives
  `NEXT_N = max(c<NNN>)+1` in bash; parent max was c104, so the actuator emits c105 — the commit wrote
  `## c104`, duplicating the section it destroyed. A freehand heading, not a derived one.
- **precision that changed the outcome:** the mechanism was whole-section DROP, not the in-place body
  REWRITE this row's title names. Under AC-2a a whole-section drop is *authorized* mutation (1), so
  `_check_notebook_immutability` is correctly silent. This is why the fix cannot be another immutability
  guard — recorded on the row so AC(4) is not built wrong.

## Entry 2 — Root cause: which file, and is it in scope

- **what-considered:**
  (a) Accept agent-father's naming (`.claude/commands/crons/cron-db-data-integrity.md` is standalone and
      never reaches the dispatcher) and dispatch a cron-prompt-only fix.
  (b) **CHOSEN** — read the cron prompt directly before acting.
- **why-change:** (a) is refuted at L59 of that file: `Launch subagent (subagent_type=system-auditor).
  Read and execute docs/agents/system-auditor/flow/main.md` / `AUDIT_TIER=DATA`. The dispatcher is reached
  on every DATA fire. The real hole is `main.md` L138-143 (Tier Dispatch), which binds TIER=1/2/3/4/5 to
  §Notebook Append Gate → §Notebook write and has **no TIER=DATA row**; L130 parks DATA as "not yet a real
  branch". A cron-prompt-only fix would have left the primary defect in place. Written into the new row as
  `premise_correction_read_first` so the implementer cannot inherit the error.

## Entry 3 — Which agent gets the fix

- **what-considered:**
  (a) agent-father — `docs/agents/system-auditor/flow/main.md` is in its commit zone.
  (b) Split: agent-father for main.md, developer for the cron prompt.
  (c) **CHOSEN** — one `developer`, both files.
- **why-change:** agent-father is correct that `.claude/commands/crons/` is outside its zone
  (`commit-boundary/SKILL.md` L50). But (b) reproduces this task's own documented failure mode — the sibling
  row's `note_generalizable` records that **two** developer→agent-father handoffs for this same wiring both
  landed in `docs/signals/processed/` and neither shipped ("processed means read, not done"). Generic
  developer is not zone-restricted by that skill, so one agent lands AC-1+AC-2+AC-3 as one change with one
  verification. Row carries an explicit "UPDATE THIS ROW on completion, do not merely file a signal".
- **secondary:** rerouted the parent row `next_agent` agent-father→po. AC(4) is a mechanical gate in
  `scripts/git-hooks/pre-commit`; `scripts/` is explicitly **excluded** from agent-father's zone, so that
  routing could never land AC(4) and would have stranded the row a third time.

## Entry 4 — Prose-ceiling block

- **what-considered:** (a) retune `ORCH_ROW_PROSE_CEILING_BYTES`; (b) split the write into smaller pieces;
  (c) **CHOSEN** — route through the `detail_ref` cold store.
- **why-change:** (b) is explicitly named as a dodge by the checker's own fix text and just relocates the
  same bytes. (a) would disarm a guard that is working correctly — both rows were genuinely bloated
  (37975B / 15900B against a 12000B ceiling). (c) is the sanctioned escape hatch: 5 stale prose fields moved
  **verbatim** to `docs/data/orch/archive/backlog-detail.json`, each shasum-verified hot-vs-cold before the
  hot row was touched. Both rows net-shrank while gaining the new record.
