# PO Triage Decision Journal — 2026-08-26T02:28Z

**task_id:** triage-20260826T0228Z-po (dev-team tick 2026-08-26T02:07Z, Step 1, busy-tick Step-0b bypass)
**Board at entry:** ready=109 in_progress=1 qa=3 review=28 backlog=591. `.head` pinned to
FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM (LIVE peer). Push disarmed (CI RED origin/main a73f0f2c).

---

## Envelope dispositions (4/4 routed, inbox CLEARED by envelope_id)

| envelope | type | disposition |
|---|---|---|
| `d5db98630397` | router / flow-defect | **MINT** `FIX-ANALYSISBRIEFS-LEDGER-WRITE-WITH-NO-COMMITTER-PATHSPEC` |
| `543b9115ee07` | commit-sweep-guard / bug-escalation | **FOLD** → `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` (occ 6→7) + scope note on `CLEAN-DRAINSIGNALS-BUGESCALATION-DOCUMENTS-2-OF-4-LIVE-PAYLOAD-CLASSES` |
| `669588f1e864` | cowork-team / cowork-fire | **pendingObservations**, no board write — routine FIRE, `errors[]` empty, `silent:false`, 1 slot won |
| `6fd910360a17` | router / flow-defect | **FOLD** → `FIX-CYCLE-SNAPSHOT-...-NOMINAL-TICK` occ 3→4 (mint explicitly refused by the reporter) |

what-considered / why-change: on `543b9115ee07` the obvious target was the doc-drift row `CLEAN-DRAINSIGNALS-...`
(it names this exact payload class). Rejected as PRIMARY: that row is about *documentation* of the class,
not the *behaviour*. The behaviour has a tracked P0 root-cause row in `review[]`. Folding the occurrence onto
the doc row would have made the P0 look quieter than it is. Both got written, with the P0 as the occurrence
holder and the doc row getting only a scope note.

---

## Evidence that could NOT land inline (prose-ceiling block)

`scripts/orch-row-prose-ceiling-check.mjs` (ORCH_ROW_PROSE_CEILING_BYTES=12000, growth-only, **no bypass
env var by design**) rejected the first apply attempt naming 3 rows. Measured headroom:

| row | live prose | headroom |
|---|---|---|
| `FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK` | 11953B | **47B** |
| `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` | 11002B | 998B |
| `FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG` | 10736B | 1264B |

Telegram + HPG notes were rewritten to fit (940B / ~1250B). The cycle-snapshot row could not take prose at
all, so only its `occurrence_count` bump landed — that is free, because `occurrence_count` counts as prose but
`3` and `4` are both one byte. **Structural fields are exempt**, which is why the HPG re-disposition itself
(`status`, `blocked_by`, `next_agent`) cost zero bytes and landed unconditionally.

**This is a live reproduction of `FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS`
(ready[], P1, next_agent=developer), and that row is itself at 11810B/12000B — headroom 190B.** The row that
exists to fix "over-ceiling rows are unbumpable" cannot absorb the evidence of its own recurrence without
becoming an instance of it. The defect is self-sealing; it will keep recurring silently because the natural
place to record each recurrence is the one place that rejects the record. I did not attempt a 189-byte token
append — that would be dodging the check rather than reporting it.

### Full cycle-snapshot fold evidence (homeless — belongs on backlog row, rejected by ceiling)

Envelope `6fd910360a17`, router, 2026-08-26T02:15Z. **AC-4 stops being conditional.**

- AC-4 and `po_premise_amendment` item (4) both describe the prior-day same-name collision in the
  subjunctive ("a same-name collision *waiting for* today's tick", "the promoter *would* find"). It fired
  live at 02:05Z 2026-08-26 on the `alert-commander-market` slot, cycle c260.
- Producer wrote `docs/data/cycle-snapshot-02:04.json` (content `created_at` 2026-08-26T02:04:23Z).
  Consumer looked up `cycle-snapshot-02:05.json` — its own wall-clock minute, exact-match rule — and **HIT**
  a prior-day file, content `created_at` 2026-08-25T02:05:26Z, exactly 24h+0m old, still inside the 1440-min
  prune window. The valid ~1-minute-old 02:04 file was present in the same directory and ignored.
- Outcome: alert-commander checked the date *inside* the file, rejected it as wrong-day, and fell through to
  direct `get_cycle_bootstrap` + `get_macro_snapshot`. Correct data served. **The consumer's own date check
  was the only thing between this and 24h-old macro data being consumed as current.**

Two defects visible in one event:

1. **Stale HIT, not a MISS.** Every prior fold (occ 1-3) recorded a MISS — promoter finds nothing,
   `promoted:false`, falls through, costs one extra fetch. A miss degrades gracefully. A stale hit does not.
   Any AC-3 return-shape work must express *"found a file, it was from another day"* as a **third** state,
   distinct from both `{promoted:false,stale:false}` and `{promoted:false,stale:true}`.
2. **Prune window == collision period.** `scripts/tick-snapshot.md:58` prunes at `-mmin +1440`; the `HH:MM`
   filename namespace repeats every 1440 min. Those being **equal** guarantees every name has a same-named
   prior-day twin alive for the last minutes before prune. 37 snapshot files on disk, oldest
   2026-08-25T02:05Z — the prune works exactly as written, the residue is structural. Confirms AC-4's
   existing "prune BY DATE" wording is the right shape and that **widening the age threshold is not** — that
   would enlarge the twin window, not remove it.

Corroborates rather than contradicts `po_fold_20260825` ("the reader enumerated the dir, SAW a valid
~1-2min-old snapshot, and discarded it on a self-invented exact-match rule grep finds in NEITHER copy"):
same shape, 14h later, different agent, different slot — the self-invented exact-match rule is reproducible
across consumers, so it is not one agent's prose slip. Collateral: `cycle-snapshot-latest.json` still carries
tick `00:00` / `created_at` 2026-08-23T00:07:50Z, ~74h stale (was 44h at the 08-24T20:30Z amendment);
`cycle_snapshot_promoted=false` for the 35th consecutive tick by the dispatcher's own count.

---

## Carried-over PO ACs — discharged this tick

1. **Step 0-TNB item 7 — DONE.** Fresh `## PO ACK` block appended to `docs/handoffs/tnb-audit-latest.md`
   (2 ACK blocks now present). The two fleet-outage findings are **folded onto
   `BA-COWORK-GUARANTEED-SLOT-CATCHUP`, not deferred a 4th time**. Load-bearing datum recorded: morning_dish's
   own 180-min catch-up window had 103 *healthy* minutes (06:32-08:15Z) and nothing consumed them — the
   failure is not "no window", it is "window existed, healthy, no consumer fired".
2. **Telegram reports — the "20 new" figure was wrong, and the error is structural.** See below.
3. **`flow-defect` is a 5th live unrouted `to=po` type** — folded onto
   `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` and dispatched via this tick's BATCH.
4. **PDFX supervised-hold — confirmed moot by re-derivation, not by relay.** Ran the WF-2 `should_hold`
   probe against the live file: head row `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` has
   `supervised:null`, `plan_only:null`, zero `po_goahead*` keys, `.head` carries none either →
   `should_hold=false`. `supervised-goahead.md` is a genuine no-op this tick, not an unrun step.

---

## Correction to the caller's premise: telegram reports are 101, not 20

`read_telegram_reports(status="new")` → **20** rows, ids 5068-5087, all 2026-08-24.
`list_unresolved_reports()` → **101** rows, ids 5068-5168, spanning 2026-08-24T00:41Z → 2026-08-26T02:09Z.

Both start at the same id, so this is a **LIMIT, not a different filter**: `read_telegram_reports` defaults
to `limit=20` and its Zod schema hard-caps `limit` at 50 (verified — `limit:200` returns *"Number must be
less than or equal to 50"*). Combined with the never-acked defect
(`FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE`, P1, backlog, 58d), the queue head never advances, so the
window is **permanently pinned to the oldest 20**. 81/101 = **80% of the unresolved queue is structurally
unreachable by PO's own flow-documented call, forever**. Even at `limit=50` it is 50/101.

This reframes that row from a noise bug to a **starvation** bug, and it changes the fix shape: acking alone
is necessary but not sufficient — a consumer that can only ever see 20 of N re-starves the moment one
unackable row sits at the head. Needs offset/since-id paging, or repoint
`docs/agents/po/flow/telegram-reports.md:19` at the unbounded `list_unresolved_reports`.

Material in the invisible 81 that no PO tick has ever triaged: C-01 `daily_ohlcv: 0 distinct codes` CRITICAL
(5151), A-32 `mcp-server running 13h older code than latest verified fix` (5163), `orch-cold-evict.sh exit 1`
(5165), a 4-day BID 2025-Q4 period-mismatch repeat, and the whole `bctcScalarAggregator.ts 1206L>1205L`
fleet-push-block series. The 2 size-lint reports naming `tasksMdJanitorJob.ts` are stale — that offender was
already fixed (472L); the *live* blocker rotated to `bctcScalarAggregator.ts`.

---

## HPG re-disposition — the handed-down premise was FALSE

what-considered: (a) accept "impl landed, only qa remains" and route to qa; (b) close as already-fixed;
(c) verify first. Chose (c) because the row's own AC-1 says *verify through the RUNTIME tool via the gateway,
never off a host-side SQL read* — the AC is a live probe, and running it is cheaper than dispatching anyone.

why-change: the probe **falsifies** the premise. `get_bctc_full({code:"HPG"})` at 2026-08-26T02:2xZ, 33h after
impl commit `28f8509fc` (2026-08-24T17:40:46Z):

```
Operating Profit : 0 tỷ VND  (0.0%)        <- AC-1 RED
EBITDA           : 0 tỷ VND                <- AC-2 RED
structured_data.operating_cf : 0           <- AC-2 RED
profit_before_tax: 10.762.183,84   gross_profit: 8.365.068,61   net_profit: 9.055.918,20
Validation: passed_with_warnings — Net profit exceeds gross profit
fetchedAt: 2026-06-07T11:12:22.298Z   refine_status: PARTIAL
```

AC-4's guard condition (`net_profit > gross_profit AND operating_profit == 0`) is satisfied *exactly* and the
served output still only warns — it did not reject or flag the corrupt zero.

**The missing leg, which is why this is neither a BA spec nor qa verification:** `fetchedAt` is 2026-06-07 and
`refine_status` is PARTIAL. The served row is a **stored artifact extracted 11 weeks before the fix**.
`28f8509fc` changed the *extraction* path (`bctcScalarAggregator.ts` `findByCodeExcluding()` + general-bucket
fallback), which by construction only runs on a NEW extraction. No AC on the row asks for a re-extraction, so
**as written the ACs are unsatisfiable no matter how correct the code is.**

Discriminator handed to the next agent so it is not re-derived: (a) stored-artifact staleness — re-run
extraction/refine for HPG 2026-Q1 and re-probe; (b) stale container image — telegram 5163 (check A-32,
2026-08-25T03:56Z) independently reports *"mcp-server running 13h older code than latest verified fix"*, so
check the image/commit label first, it is cheap. Both can be true.

Row written: `status` BLOCKED→BACKLOG (it is actionable, not blocked), `blocked_by` cleared (the
`IMPL-ALREADY-LANDED-28f8509fc` token was false), `next_agent` ba→developer. **Left in `backlog[]`
deliberately** — the ready-lane consumer (`scripts/devteam-backlog-claim-ready-lane-consumer.jq:137-144`)
reads status + deps + next_agent and **no prose**, so a prose-only contraindication is unenforceable there.
This is the same lesson the prior tick learned the hard way; the lane placement is the enforcement.

---

## Also measured, not escalated

`CLEAN-NB-TRIM-BCTC-ANALYST` (backlog, 07-25) claims `docs/agent-memory/notebooks/bctc-analyst.md` is 14162B
vs a 12000B cap. Live: **8715B / 24L** — under both caps. Premise resolved, recommend CLOSE. Stamped with the
measurement; not lane-moved, so the close is a deliberate owner act rather than a triage side effect.

Diagnostic value of the same file for the notebook-immutability fold: the c184 rewrite happened on a file
**comfortably under cap**, and the delta is ~20 bytes of pure cosmetics (`**` bold markers stripped;
", not live market" added). An over-cap-pressure theory cannot explain that, which narrows
`FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` away from its own stated root cause: the compose step
appears to re-render retained sections rather than pass them through byte-identically, independent of cap
state. The whole-section drops in the same write (c183, c182) were authorized under AC-2a and are not part of
the finding.
