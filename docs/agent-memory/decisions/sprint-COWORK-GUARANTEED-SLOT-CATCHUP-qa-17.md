# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa

**Sprint goal:** Make cowork `guaranteed:true` an honored contract — bounded catch-up/miss-recording for elapsed guaranteed slots (see qa-16.md and earlier continuations for full vision text; this file is a byte-cap rollover, unrelated task below is an interleaved cowork-lane QA-drain row).
**Agent:** qa
**Started:** 2026-08-12T04:25:39Z

---

### STEP qa-S64 · qa · 2026-08-12T04:25:39Z
**task-id:** FACTORY-GUARD-CI-shared-package-import-check
**what-done:** Direct-Commit Verify — design-only architect brief task, no code to test (7th of 7 `ci-regression-prevention` guardrails, per row's own review_note). Commit `d220c8e1b` confirmed real: ancestor of `main` AND `origin/main..HEAD` empty (pushed); `git show --stat` matches claimed scope exactly (48L brief + orch-state.json +21L child-mint only + notebook/journal, zero production code).
**what-considered:**
- Did not trust review_note prose alone: independently re-ran the brief's core evidence claim myself — `wc -l` on all 3 `packages/shared-*/index.ts` = 76/73/37L (brief's exact numbers); repo-wide grep for `@vn-market/shared-types|shared-config|shared-db` import specifiers or `package.json` deps, excluding each package's own files, returns 0 hits — 3/3 packages genuinely 100% orphaned, not asserted.
- Verified both cited supporting commits exist on `main`: `ef62d2921` (shared-db stale-list already fixed) and `df6c1dc7d` (prior phantom-package prune precedent). Spot-checked the Alert/Signal name-collision claim directly in source (shared-types: 5-field `Alert{id,stock,severity,message,createdAt:Date}` vs live `alertGenerator.ts` 10-field with `actionCode` rename + `createdAt:string` — exact match); the cited `:41` line ref was accurate at the brief's own commit-time blob (`git show d220c8e1b:...`), current HEAD drift to `:42` is unrelated later churn, not a brief defect.
- Verified the owning-task ownership claim (design's central justification): `FACTORY-SHARED-wire-or-prune-shared-packages` exists in `backlog-detail.json` with `effort:L, risk:med, rebuild_required:true, status:BACKLOG` — matches the brief's cited properties exactly, and its own DoD text (reconcile-superset-first caution) independently corroborates why a CI-tooling child task should not preempt that domain decision.
- Zero-CI-coverage claim re-verified at commit-time blob equivalent (current `ci.yml` now DOES match the grep pattern, but only because the child `FACTORY-GUARD-CI-SHAREDPKG-IMPL` has since shipped its own job — read the diff, confirmed this is the expected/intended later addition, not a false "zero coverage" claim at design time).
- Child row `FACTORY-GUARD-CI-SHAREDPKG-IMPL` correctly minted (dev_agent/next_agent=developer, zone `cross-service/`, brief_ref wired) and has since progressed to `task_board.review` (own separate later QA gate, not re-verified exhaustively here per row's own scope instruction).
**why-decision:** APPROVED, DONE_VERIFIED. BASELINE/RATCHET (not zero-tolerance) design is well-justified on the ownership axis explicitly claimed: the 3 orphaned packages' keep-or-cut fix requires a domain-judgment field-reconciliation call already assigned to a separate larger task (verified real, correctly scoped, correctly sequenced) — forcing it here would duplicate/preempt that task. The guardrail still delivers real regression-prevention value (blocks any NEW zero-importer package immediately) while explicitly deferring the pre-existing-debt disposition, exactly mirroring the stated rationale — not a rationalization for skipping rigor.
**why-change:** none — no code exists in this task's own commit to fix; all checks structurally N/A (bun test/tsc/mock-guard) confirmed via diff-stat, not assumed. File rolled to `-17` this write (byte-axis cap breach on `-16.md`: 40950B > 36000B cap despite 201L < 600L — dual-axis rule, CAP-REACHED marker appended there).

### STEP qa-S65 · qa · 2026-08-12T04:26:43Z
**task-id:** FACTORY-GUARD-CI-rebuild-raw-verify-hook
**what-done:** Direct-Commit Verify — design-only architect brief task, no code to test (7th and LAST of 7 `ci-regression-prevention` guardrails). Commit `265451cc6` confirmed real: ancestor of `main` AND on `origin/main`, pushed clean; `git show --stat` matches claimed scope exactly (46L/10395B brief + orch-state.json +21L child-mint only + notebook/journal, zero production code).
**what-considered:**
- Independently re-verified BOTH design corrections AT SOURCE, not brief prose: `gh pr list --state all` → exactly 1 PR ever (#1, CLOSED, `mergedAt: null`); `gh api repos/:owner/:repo/branches/main/protection` → 404 Branch not protected. The "attached to the PR" category-error correction is sound — no PR/branch-protection gate exists, so wiring the check primarily into `scripts/git-hooks/pre-push` genuinely IS this repo's real merge gate, not a design shortcut.
- `dev-standards.md:1493` PUSH-AUTONOMY-1 §5 text matches the brief's quote word-for-word — the "DoD already exists as written policy" claim is accurate. Concrete compliance-gap commit `e3386bdfa` independently confirmed: real, self-flags `rebuild_required: true`, exact confidence-mask bug class named, zero RAW-verify/REALDATA attestation in its message. Sanity-checked the ratio 18 days later: 3 `VERIFY-*-REALDATA` rows exist repo-wide today vs 173 serving-code commits since the policy pin — gap direction holds, not overstated at design time.
- Verified the sibling-composition design claim at the code level too (child `FACTORY-GUARD-CI-RAWVERIFY-IMPL` already shipped independently, ahead of this row's own QA-drain queue — same out-of-order pattern as siblings dead-code-gate/no-hardcode, not a defect): `rebuild-raw-verify-check.sh`'s `FIELD_ERE`/`TRIGGER_PATH_ERE` literally reuse `metric-mask-lint.sh`'s field regex + the depguard-tier-boundaries layer paths verbatim; wired into `pre-push` (primary) + `ci.yml` `rebuild-raw-verify-hook` job (backstop) + `dev-standards.md` §5 cross-reference (incl. a 2026-08-07 post-ship regex fix) — all 5 files from the child's own note present and wired on disk.
**why-decision:** APPROVED, DONE_VERIFIED. Both flagged design corrections (PR-framing, pre-push-hook-as-primary wiring) are well-justified given this repo's actual autonomous direct-push workflow; brief adequate, child scoping correct.
**why-change:** none — no code exists in this task's own commit to fix; all checks structurally N/A (bun test/tsc/mock-guard) confirmed via diff-stat, not assumed.

### STEP qa-S66 · qa · 2026-08-12T04:45:12Z
**task-id:** FIX-OHLCV-CORP-ACTION-CONTINUITY
**what-done:** Direct-Commit Verify — real code change, `bun test`/`tsc`/mock-guard/DDD/security all independently re-run (not trusted from prose). Commits `90434daad`+`3f2acbdda` confirmed real (main+origin/main ancestry, `git show --stat` matches claimed file scope exactly).
**what-considered:**
- Read `ohlcvContinuityGuard.ts`+`getContinuityCheckedOhlcvSeries.ts` on disk directly: real per-exchange board limits (HOSE7/HNX10/UPCOM15, generic resolve fn, no hardcode), pure detector, reconcile-or-flag. Grepped `technicalIndicatorTools.ts` for `daily_ohlcv` post-fix — zero raw SQL reads remain, both logical sites (prefetch+fallback) route through the guard.
- Independently re-fetched live VJC 2026-06-08..19 via the SAME documented `docker exec ... bun -e` command — all 10 rows byte-identical to the test fixture (no fabrication). New suite 13/13 pass; tsc clean; mock-guard PASS; DDD grep clean (no domain→infra/app imports); no process.env/secrets in touched files.
- The "pre-existing flaky, unrelated" 2-failure claim: reconstructed the (since-deleted, real-at-commit-time per `git ls-tree 90434daad`) `_deprecated/1302-technical-indicators.test.ts` untracked in-place, ran it at HEAD (32p/2f, matches claim exactly) then git-swapped `technicalIndicatorTools.ts` to its pre-fix blob and re-ran — IDENTICAL 2 failures, byte-identical output, same test names — independently confirms unrelated (JSON-envelope format mismatch + a Date.now()-relative MACD-trend assertion in the legacy test, not caused by this fix). Restored file after, `git status` clean on `apps/mcp-server/`.
- Full suite (own run): 15229p/55f/40skip — 0 failures reference `ohlcvContinuityGuard`/`getContinuityCheckedOhlcvSeries`/`technicalIndicatorTools`/the new test file; all 55 match named pre-existing flaky classes (logVpsPush, insider_transactions, market_cap, MCP SSE, foreign_flow, telegram, etc.) — consistent with review_note's own 2-run claim (count drift vs prose is suite growth over 19 days, not regression). No new tool/cron registration confirmed via grep (184/88 claim structurally true).
**why-decision:** APPROVED, DONE_VERIFIED. Every load-bearing claim in review_note independently reproduced from source, not trusted as prose — real files, real wiring, real test data, real pre-existing-flake A/B.
**why-change:** none — verification matched the plan exactly.

### STEP qa-S67 · qa · 2026-08-12T04:56:00Z
**task-id:** FIX-DOWJONES-STALE-WRONG-VALUE
**what-done:** Direct-Commit Verify. Both commits (`57b916509` code, `734c7bd28` memory) confirmed real + main ancestry. `git show --stat` on `57b916509` matches all 8 claimed files (2 test, 6 production) — the 6-file `head -50` truncation on first pass was a paging artifact, re-ran untruncated and confirmed.
**what-considered:**
- Read each production diff on disk, not trusted from review_note prose: `indicatorPlausibility.ts` new file genuinely generic (14 indicators, dow_jones 25000-60000, `isPlausibleIndicatorValue()`); `commodityTracker.ts` regex for dow_jones removed + old inline MIN/MAX maps replaced by a call to the shared gate; `yahooFinance.ts` `fetchDowJonesIndex`/`storeDowJonesIndex` added (fail-closed, dedup-before-insert, reuses pre-existing `fetchSymbolPrice` helper); `commodityTrackerRefreshJob.ts` Block 3 genuinely wired (isolated try/catch, DI-injectable, `rowsWritten` sums 3 blocks); `systemTools.ts` swapped its own unguarded query for `listTrackedIndicatorsFromDb()` + `[STALE]` tag; `dataAuditShared.ts` INDICATOR_RANGES got a dow_jones critical band.
- Cross-checked the "separate bug from DSI-MACRO-PHANTOM-STALE-GUARD" claim against that commit (`3280d82ab`) directly: it touched only `marketContextBuilder.ts`/`commodityTracker.ts`, never `systemTools.ts` — confirms `get_system_status` was genuinely unguarded until this fix, not a re-claim of prior work. `listTrackedIndicatorsFromDb` itself pre-dates this task (same commit) — correctly reused, not fabricated.
- Ran tests myself: new `FIX-DOWJONES-STALE-WRONG-VALUE.test.ts` 15/15 pass (real fail-closed rejections logged for 23750/999999, real HTTP-timeout fallback). `1920c-...-refresh-job.test.ts` 10/10 (7 pre-existing + 3 new, matches "+3" claim). Reconstructed the claimed "7 files/68 tests" set by per-file counts (025-yahoo-finance 16, 1487-yahoo-finance-extended 7, 1920c 10, 234-system-status-merge 15, DPI-3-commodity-delta 4, DSI-MACRO-PHANTOM-STALE-GUARD 10, FIX-SYSTEM-STATUS-TE-TIMEOUT-GUARD 6 = 68) — exact match. Ran a wider 11-file superset (adds 1039/1489/1833l) = 95/95 pass, 0 fail — no regression beyond the claimed set either.
- `bun tsc --noEmit`: 0 errors (mcp-server). `mock-guard.sh --files` on all 6 touched production files: PASS. No `process.env`/secret/password hits in touched files (only a code comment mentioning "env"). None of the 6 touched files are domain-layer — DDD inbound-direction rule N/A, confirmed by path. Noted the plausibility band (25000-60000) would NOT catch a hypothetical future 48221-magnitude bad value — but the code's own comment is honest about this (doesn't claim to reject it), and the primary fix (regex retirement) removes that vector entirely regardless of band.
**why-decision:** APPROVED, DONE_VERIFIED. `rebuild_required:true`/live-cycle verification correctly deferred to ops per row's own note (container swap is user/ops-gated, out of QA's unit-level scope) — not a QA blocker.
**why-change:** none — verification matched the plan exactly.

### STEP qa-S68 · qa · 2026-08-12T05:05:00Z
**task-id:** FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED
**what-done:** Direct-Commit Verify. Both claimed commits (`1f4b491b1` code+test, `537911454` memory) confirmed real + main ancestry. `git show` on both bug fixes read on disk, not trusted from review_note prose.
**what-considered:**
- Bug 1 (envelope-parse): confirmed `signalOutcomeStore.ts` swapped hand-rolled flat-array fetch for canonical `getPriceHistory({code,days})` client, reads `envelope.history` (real shape), `days` now derived from `targetDate` gap (was hardcoded 3), and baseline `price_at_signal` gained the same Option-B fallback via new injectable `fetchHistoryFn` dep — matches claim exactly.
- Bug 2 (FK guard): confirmed `cleanExpired()` (agentSignalStore.ts) and `checkStaleAgentSignals()` both gained `AND id NOT IN (SELECT signal_id FROM signal_outcomes)`, with a `no such table` fallback for schemas without signal_outcomes; `getAccuracyStats` INNER→LEFT JOIN confirmed at the SQL line. `checkStalledResolutionLiveness()` self-alert guard confirmed wired into `runSignalOutcomeResolutionJob`.
- Live named-vol DB (`data/live/market.db`, bind-mounted `/app/data`, container image built 2026-08-11 — well after the 2026-07-25 fix commit, confirmed fix code present inside the running container via `docker exec grep`): queried myself, NOT trusted from claim. Gate (a): 105 total rows, 103 checked_at populated, only 2 pending — both `stock_code` MACRO/MULTI (non-ticker), exactly matching claim; live better than the deploy-time 98/105 snapshot (self-heal continuing 18d later). Gate (b): `calibration_snapshots` (separate subsystem, confirmed via grep: calibrationReportJob.ts/calibrationTools.ts, never touched by this diff) shows `computed_at` advancing weekly past 2026-06-14, latest 2026-08-09 resolved=17 (>4) — SCOPE NOTE in review_note verified correct, not deflection. Gate (c): no genuinely NEW signal_outcomes row exists post-fix (max id=105, created 2026-07-24, before the fix landed) to test literally-fresh end-to-end — traced this to a SEPARATE upstream recording-starvation issue already PO-ruled (`FIX-ALERT-CASCADE-OUTCOME-DEAD` CANCELLED 2026-07-31, "recorder is starved, not broken", 3 successor tasks minted) — not this task's scope; resolution-mechanism correctness for the full existing backlog + hourly `cron_job_runs` all-success + orphan id=66 resolving are treated as sufficient equivalent evidence.
- Ran tests myself: new `FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED.test.ts` 11/11 pass. 6 affected/precedent suites (signal-outcome-store, 244-signal-outcome, accuracy-context-tool, FACTORY-INFRA-agentSignal-sql-binding, 242-agent-signals, 1920g-prediction-claims) 76/76 pass. `bun tsc --noEmit` clean, `mock-guard.sh` PASS, DDD/process.env/secret greps clean (scheduler-layer infra imports are permitted, not domain→infra).
- Checked 4 subsequent commits touching these files since 2026-07-25 for regression/drift: `314e70718` (MACRO/MULTI liveness false-positive fix, confirms 103/105 live at that date too), `f5698cb76` (GC hardening, confirms 104 FK-blocked matches my own count), 2 unrelated — no regression to this fix's changes.
**why-decision:** APPROVED, DONE_VERIFIED. Both compounding bugs verified on disk + live DB independently; gate (a)/(b) directly confirmed live; gate (c) accepted via equivalent mechanism-correctness evidence given the literal precondition (a new post-fix row) can't occur due to an out-of-scope, already-tracked upstream issue.
**why-change:** none — verified exactly what the row scoped; upstream recording-starvation gap flagged as already-tracked, not folded into this verdict.

### STEP qa-S69 · qa · 2026-08-12T05:00:17Z
**task-id:** FIX-PENDING-REFINE-OUTPUT-235K-OVERFLOW
**what-done:** Direct-Commit Verify. Commits `51f162829`(code+test+doc)+`e0ed0369e`(journal) confirmed real+pushed. Code/test read on disk (not trusted from prose): both SQL branches (ticker+default) now do native `LIMIT ${limit} OFFSET ${offset}`, zod `.optional().default(20)` order fix genuine, served-path test's 45-row 3-page offset walk (20/20/5) exactly matches claim. New test 5/5, pre-existing 13/13, tsc clean, mock-guard PASS — all re-run myself.
**what-considered:**
- rebuild_required resolution: running `mcp-server` image built 2026-08-11 (after the 2026-07-25 commit); `docker exec grep` on the container's own `getBctcPendingRefineTool.ts` confirms the fixed source is live on disk inside the container — rebuild already landed.
- LIVE-verified the actual claim ("bounded inline payload, no file-path fallback") against REAL production data, not the test's synthetic pdf_path=NULL fixture (which short-circuits windows[] to near-zero weight). Called the real handler in-container: default (limit=20,offset=0) = 125,211 chars/bytes; limit=50 = 301,654; limit=100 (tool's own documented max) = 565,285 — MORE than 2x the originally-reported 235K overflow trigger. Deep-offset sample (offset=180) confirms ~5-6KB/row is a systemic average, not a front-of-queue outlier.
- This falsifies AC "bounded-inline-payload-no-file-path-fallback" across the tool's own advertised 1-100 range: only the DEFAULT (20) stays safely under the historic 235K trigger; any caller passing limit ≥~40 (well inside the schema's allowed/documented range) reproduces or worsens the exact bug this fix exists to close. MAX_LIMIT=100 was copied unchanged from other tools' convention without checking THIS tool's much heavier per-row (windows-partitioned PDF content) weight — root cause of the gap. No-data-loss (offset-walk) claim itself is NOT in question — only the size-bound claim.
**why-decision:** CHANGES_REQUESTED, not DONE_VERIFIED. Pagination/no-loss mechanics are solid and match the claim exactly, but the core defect (inline overflow) is only closed for the default page size — MAX_LIMIT needs recalibration (or a response-size guard) against real windows-heavy data, re-tested at limit=100 live, before this can close.
**why-change:** review_note claimed the fix was complete pending only a rebuild; live verification (which the row itself asked QA to perform) found the rebuild landed but surfaced a real unresolved gap the synthetic test fixture couldn't catch.

### STEP qa-S70 · qa · 2026-08-12T05:27:22Z
**task-id:** FIX-FB-GATE-POINT-PCT-MATH
**what-done:** Direct-Commit Verify — real code change to `fb-data-integrity-gate.sh`, Check-H independently re-run (not trusted from prose). Commits `4e1b07937`(code)+`d7a7c624c`(journal) confirmed real+main ancestry.
**what-considered:**
- Read diff on disk: Check-H heredoc computes `prev_close=close/(1+pct/100)` from live-only quotes, generic via `INDEX_ALIASES` (4 tickers), BLOCKs on `|stated-implied|>1.0pt`. SC2034 confirmed pre-existing on untouched `POST_DATE:195`, outside diff hunks.
- Re-ran both fixtures myself via `SNAPSHOT_FILE` injection (no network): inconsistent pair ("giảm 0,32 điểm (-0,32%)" @prev_close=1800) → Check-H BLOCK, `delta=5.44>1.0pt` — exact message match. Consistent pair ("giảm 5,90 điểm") → `[PASS] 0 violations`, exit 0 — exact match.
- RED-baseline reproduced directly (not trusted): checked out `4e1b07937~1` (parent), same inconsistent-pair fixture isolated from Check-D collision → `[PASS] 0 violations` exit 0 (false-pass confirmed); same fixture on post-fix HEAD → Check-H BLOCK exit 1.
- Regression: real `docs/social/fb-post-2026-07-24.md` w/ matching live snapshot → 0 violations; `MCP_BASE` pointed unreachable → graceful skip, 0 violations. `bash -n`+shellcheck re-run clean myself.
**why-decision:** APPROVED, DONE_VERIFIED. Every load-bearing claim (check exists+logic, BLOCK/PASS behavior, RED baseline, no-false-positive regression, graceful skip) independently reproduced from source/live-run.
**why-change:** none — verification matched the plan exactly; letter-collision (minted Check-F → shipped Check-H) documentation confirmed accurate and inline.

### STEP qa-S71 · qa · 2026-08-12T05:32:00Z
**task-id:** FIX-FB-GATE-STALE-MACRO-GUARD
**what-done:** Direct-Commit Verify — Check-I (macro/FX stale-as-current + unverified-comparison guard) + paired flow-doc STEP 1b/4b provenance-marker wiring. Commits `41975d433`(code+doc)+`c08a42f2d`(journal) confirmed real+main ancestry, diff read on disk not trusted from prose.
**what-considered:**
- Re-ran all 4 fixtures myself (own hand-built posts, own `$4` provenance JSON): unprovenanced figure (prov entry `fetched_this_cycle:false`) → I1 BLOCK exit 1; honest-gap phrasing → PASS exit 0; matching-provenance figure → PASS exit 0; unverified comparison level → I2 BLOCK exit 1 — all 4 exact match. Note: an ABSENT `$4` file (not a false-entry file) correctly graceful-skips per code intent — distinct from "unprovenanced," verified both paths.
- RED-baseline reproduced directly: `41975d433^` script has zero Check-I code; same BLOCK fixtures both false-PASS exit 0 on it — confirms gap existed.
- Regression re-run myself against ALL 57 CURRENT `docs/social/fb-post-*.md` (superset of claimed 48) with no prov file → 0 Check-I false-fires, graceful skip, matches claim substance. Flagged (non-blocking): git-ancestry+timestamp analysis shows only 41 such files existed at the actual commit timestamp (2026-07-25T04:05:33+02:00) — the other 7 landed in a later same-day batch-backfill commit (09:48, different session). "48" is not reproducible from commit-time repo state; likely reflects the dev's live working-tree at test-time in this shared-container repo, not fabrication — but prose≠commit-time-verifiable, logged for awareness.
- Real-corpus checks: fb-post-2026-06-08.md's exact "26.127 ... vượt ngưỡng 25.500" sentence isolated+re-tested → PASSes 26.127, BLOCKs only 25.500 (I2), exact match to commit msg. fb-post-2026-06-03.md w/ matching macro provenance AND matching price snapshot (needed to silence unrelated live Check-B/D noise from testing a June post against today's live prices) → 0 violations end-to-end, confirms claim. `bash -n` clean, mock-guard PASS (no prod TS touched, Smart-Skip bun test/tsc N/A).
**why-decision:** APPROVED, DONE_VERIFIED. Every functional claim (check logic, all 4 BLOCK/PASS fixtures, RED baseline, graceful-skip regression substance, real-corpus multi-figure + end-to-end examples) independently reproduced. The 48-vs-41 file-count discrepancy is a non-blocking prose-accuracy note, not a functional defect — re-validated with a stronger superset (57 files) live myself.
**why-change:** none — verification matched the plan; one minor prose-accuracy flag on regression file-count logged above, not gating.

### STEP qa-S72 · qa · 2026-08-12T05:40:00Z
**task-id:** FIX-FB-GATE-BREADTH-PCT-INTERNAL
**what-done:** Direct-Commit Verify — Check-J (breadth %-vs-cited-counts internal consistency). Commits `ce04d512a`(code)+`ce34c588f`(journal) confirmed real+main ancestry, diff read on disk not trusted from prose.
**what-considered:**
- Letter-collision claim verified live: `grep -oE 'Check-[A-Z]'` on both `41975d433`(parent) and `8e062bbd6`(claim-point) blobs → A-I already present, zero J — Check-J genuinely the next unclaimed letter, not a fabricated excuse.
- Built my own BLOCK/PASS fixtures (own text, not copy-pasted from prose) matching the described extraction logic (30-char headline window, label-agnostic fraction+co-occurring forms): BLOCK fixture ("Độ rộng thị trường ~40%." + "81 của ~350") → `[BLOCK] Check-J ... delta=16.86pp`, exit 1 — exact match incl. delta to 2dp. Matching fixture (~23%) → `[PASS] 0 violations`, exit 0.
- RED-baseline reproduced directly: `41975d433` blob has zero Check-J occurrences; same BLOCK fixture run against it → false `[PASS] 0 violations` exit 0 — gap confirmed real, not assumed.
- Regression re-run myself TWICE: (a) exact 41-file corpus as it existed at commit-time (`git ls-tree ce04d512a`, not the row's claimed "48") → 0/41 Check-J fires; (b) current 57-file corpus (16 files added since, none tested by dev) → 0/57 fires — stronger superset than claimed, confirms extraction isn't overfit to the originally-tested set. Near-miss 2026-06-19 file independently recomputed: 81/350=23.14% vs stated 25% (Δ1.86pp), 203/350=58.0% vs stated 63% (Δ5.00pp) — both ≤6.0pp, matches claim exactly.
- Same "48 vs actual-41-at-commit-time" prose discrepancy as sibling Check-I (qa-S71, same session) — copy-pasted boilerplate phrase, not re-counted; non-blocking per that established precedent, substance (0 false-fires) independently reconfirmed with a larger live sample. `bash -n` clean, mock-guard PASS (no prod TS touched, Smart-Skip bun test/tsc N/A — pure bash+python script).
**why-decision:** APPROVED, DONE_VERIFIED. Every functional claim (letter enumeration, extraction logic, BLOCK/PASS fixtures incl. exact delta math, RED baseline, regression substance beyond the claimed sample) independently reproduced from source/live-run, not trusted from prose.
**why-change:** none — verification matched the plan; one minor prose-accuracy flag on regression file-count logged above (same class as qa-S71), not gating.
