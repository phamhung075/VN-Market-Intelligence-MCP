# PO decision journal — triage 2026-08-23T08:59–09:15Z · durable-inbox drain (8 envelopes)

**task_id:** ambient triage tick — 1 net-new mint, 5 folds, 1 P0 prioritisation, 1 manual-dispatch stamp.
**Spawned by:** router session `007e33e4-b453-4bb3-8ab1-ef31495906a3`, intent `intent:po:step1-triage-inbox`. The 08:37Z dev-team tick selected `step1_triage`, found the lane genuinely due, but its terminal step is `Spawn po` and a Task-tool subagent cannot spawn — it correctly stopped before claiming `triage_key` and returned `NEXT: po`.
**Concurrency note:** a SECOND PO ran in parallel this window (router session `7be6b4cd`, journal `triage-20260823T0904Z-po.md`, intent `intent:po:cron-rearm-guard-defects`, 5 rows minted). Disjoint scope from this tick; no row touched by both. Flagged for the router because two PO sessions triaging simultaneously is not a state the claim gate is supposed to allow.

## D1 — Dedup-checked before every disposition; result was 1 mint from 9 inputs, not 9 mints

- **what-considered:** (a) treat each of the 8 inbox envelopes as its own finding, which is what their `priority: high` invites; (b) run each through its own routing rule's dedup gate first.
- **why-decision:** (b), per `triage-signals.md`'s `bug-escalation` row, which is explicit that `escalated=false` warns must NOT mint per occurrence. Outcome: 8 envelopes → 0 new rows, 2 folds. The one net-new row came from Pipeline B, not the inbox.
- **why-change:** no change from plan.

## D2 — The `[sweep-guard] SAME-FILE DIVERGENCE` class turned out to be dispositionable, contradicting the standing note

- **the standing rule:** its known-FP root cause `FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE` is `DONE_VERIFIED` and cold-archived (`archive/2026-08.json` `done_tasks[107]`), so branch 2 applies: a fire after the fix is a *candidate genuine* divergence and must be verified by the §2.7 two-blob diff. `FIX-COLDEVICT-WITHIN-FILE-PEER-CONTENT-CAPTURE`'s 2026-08-14 note declares that verification "undispositionable-by-construction on the PO tick" because the index state is unreconstructable.
- **what-considered:** (a) accept that note and log-and-skip as PO did on 08-14; (b) test the premise — the fire was 44 minutes old, so the blobs might still be in the object store.
- **why-decision:** (b). `git cat-file -t` confirmed BOTH blobs (`cf3023a5…`, `42771857…`) still resolve. `git diff cf3023a5… 42771857…` (two-blob form, NO pathspec) returned 64 insertions / 3 deletions, **all inside `.dev_team_idle_chain.pending_triage_inbox`** — dev-team draining 5 envelopes into the inbox between staging and commit. That is a legitimate concurrent writer of a different subtree, not unreviewed peer content. No escalation.
- **why-change:** the "undispositionable-by-construction" claim is wrong as a general statement — the class IS dispositionable whenever triage runs inside the gc window. That correction could not be written to its own row (12328B, 328B over the prose ceiling), so it lives here and in the notebook.

## D3 — `[heartbeat-guard]` is a FIFTH payload class with no routing rule at all

- **measured:** `grep -c 'heartbeat-guard'` returns **0** in both `docs/agents/po/flow/triage-signals.md` and `triage-signals-longtail.md`. The `bug-escalation` row enumerates exactly four payload classes; this one has been arriving since 2026-08-11 and has been dispositioned three times purely by PO improvisation.
- **why CI cannot see it:** `guard-signal-type-coverage.sh` keys on signal `type` (`bug-escalation`, which *is* routed), never on payload sub-class. The gap is structurally invisible to the guard that exists precisely to catch this shape.
- **what-considered:** (a) mint a dedicated routing-gap row; (b) fold onto `FIX-AUDITOR-TIER1-HEARTBEAT-HANDWRITE-RECURS-SAME-DAY-AS-ITS-OWN-FLOW-FIX`.
- **why-decision:** (b). That row already owns this payload class in practice (it is its own `origin`), its `next_agent` is already `architect` — exactly who would add the table row — and it had 8445B of prose headroom. Minting a 450th backlog row against an RLC that dispatches ~1/tick would have buried it.

## D4 — Two intended folds were physically blocked by the prose ceiling; both dedup to already-open rows

- **measured this tick:** 23 of 580 rows across the 3 guarded lanes (backlog/ready/review) are over the 12000B ceiling and can therefore receive no new field from any producer. `FIX-COLDEVICT-WITHIN-FILE-PEER-CONTENT-CAPTURE` is 328B over; `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` is 22589B over.
- **what-considered:** (a) mint a new row for the interaction; (b) dedup first.
- **why-decision:** (b), and it was right — BOTH halves are already tracked: `FIX-PROSECEILING-PO-MANUAL-DISPATCH-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET` (backlog) and `FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS` (ready). Zero net-new mints from this angle; evidence folded onto the first.

## D5 — manual-dispatch-sweep: I skipped its own rank-1 candidate, deliberately and on record

- **measured:** Step 1's exact jq returns 93 candidates. Ranked #1 is `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` at prose=26434B — 14434B over ceiling — so Step 2's 4-field stamp hard-rejects the whole write. #2 is also over (884B).
- **the real defect, now measured not asserted:** `sort_by([rank, idx])` is deterministic and Step 2 has no fallback branch, so every PO tick picks the SAME un-stampable row; the stamp fails, `po_manual_dispatch_flagged_at` is never set, `flag_reentrant` keeps it eligible, and it stays rank-1 forever. Head-of-line block is **permanent, not probabilistic**.
- **what-considered:** (a) attempt rank-1 and let the write abort (the flow's literal reading); (b) pre-check headroom and stamp the highest-ranked candidate that has any.
- **why-decision:** (b) — the router mandated checking headroom *before* writing, and an aborted `orch-apply.sh` would have discarded the four unrelated folds batched into the same pipe. Stamped `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` (rank 3, 1529B headroom) and recorded the skip reason on the row itself so the improvisation is not invisible.
- **why-change:** this improvisation is in no flow doc and will not survive a less careful tick. Said so verbatim on `FIX-PROSECEILING-PO-MANUAL-DISPATCH-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET`.

## D6 — CI: verified the old red is fixed, then verified a NEW red replaced it — did not report recovery

- **what the router first told me:** the red is the original `audit-handoff` failure, already fixed by `735dfa815`, CI simply has not run on the fix.
- **verified independently, all three legs:** (1) `git merge-base --is-ancestor 85384b32e 735dfa815` → CI's last run genuinely predates the fix; (2) run `32629387939` job list → `signal-type-coverage-guard` is the ONLY failure of 23 jobs, so nothing was hiding underneath; (3) extracted the **HEAD-committed** guard via `git show HEAD:…` and ran it against the docs tree from a symlinked root — PASS. I deliberately did not trust my first run, which had used the working-tree copy while the live peer had it dirty.
- **then the premise changed:** `dev-mcp-server` landed `4e7aa7eaf`/`cdcd6daba`/`1b2e94f40`, and its dual-pipeline guard immediately caught a genuinely new unrouted Pipeline-B type, `bctc_image_fetch_degraded`. Re-ran on the live tree: `FAIL — unrouted Pipeline-B to=po types: ["bctc_image_fetch_degraded"]`, exit 1.
- **why-decision:** CI is NOT recovered and I did not say it was. Old cause fixed, new cause real. No `ci_red` fingerprint recorded and no ci_red closed — no green has ever been observed at or after `735dfa815`, so there is nothing to close.

## D7 — Raised the guard's own auto-mint to P0 rather than let it park

- **what-considered:** (a) leave `FIX-SIGNAL-TYPE-ROUTING-GAP-bctc-image-fetch-degraded` in `backlog[]` as auto-filed; (b) prioritise and BATCH it.
- **why-decision:** (b). It is the sole remaining cause of CI red, the fix is one table row, and `backlog[]` is 449 deep against ~1 dispatch/tick — parking it means CI stays red indefinitely, the exact starvation that stranded `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` for 10 days. Set `priority=P0`, `size=S`, `next_agent=agent-father`.
- **why-change:** also corrected its `zone` from `docs/agents/po/flow/` to `cross-service/` — the auto-filer emits a path, but dev-team Step 3 accepts only `apps/<service>/` | `multi` | `cross-service/`, so the row was **not dispatchable as minted**. That is a defect in the auto-filer worth feeding back; recorded on the row.
- **routing:** `agent-father`, not `developer` — `triage-signals.md` is an agent flow doc, PO's own `boundary_rules` forbid PO editing it, and agent-father made its last edit (`735dfa815`). Did NOT silence `guard-signal-type-coverage.test.sh` TEST10; its 23/24 failure is the guard biting correctly.

## D8 — The new BCTC signal got a durable row, separate from the routing gap

- **what-considered:** (a) treat the routing-gap row as covering it; (b) route the signal's *content* on its own merits via the ROUTE-BY-`to` fallback.
- **why-decision:** (b). Different subjects: the gap is `signal-type-registry-gap:bctc_image_fetch_degraded`; the content is `bctc_image_fetch_degraded:69fcd047-a358-4067-a34c-af727617c473`. Fixing the table row would leave 2 units silently capped at ≤0.6 confidence. Severity HIGH, `to=po` resolves, summary names a concrete tool → the fallback's "clearly actionable" branch mandates a backlog row.
- **why-change:** left `root_cause` explicitly UNDIAGNOSED. The signal reports only the symptom (`image_unavailable`); whether `get_bctc_page_image`, the stored PDF, or pdf-extractor is at fault is what AC-1 must establish against the live runtime.
