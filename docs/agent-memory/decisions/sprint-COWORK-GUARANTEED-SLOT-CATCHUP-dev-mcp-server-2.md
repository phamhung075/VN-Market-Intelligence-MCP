# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · dev-mcp-server (continuation 2)

**Sprint goal:** Make cowork `guaranteed:true` an HONORED contract, not a false promise (see orch-state sprint_goal.entries[COWORK-GUARANTEED-SLOT-CATCHUP]).
**Agent:** dev-mcp-server
**Started:** 2026-07-31T10:06:45Z
**Continuation of:** sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md (rolled — byte_cap 36000 breached, bytes=94833/lines=386)

---

### STEP dev-mcp-server-S41 · dev-mcp-server · 2026-07-31T10:06:45Z
**task-id:** FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH
**what-done:** Fixed `pdfOcrWorker.ts`'s completeness gate to cap `expectedPages` at `OCR_MAX_PAGES` (80, same constant now shared with the extraction-loop cap) before computing `threshold`, and removed the upfront blanket `DELETE` on the "incomplete" branch, replacing it with a post-loop `DELETE ... AND page_number > maxPages` cleanup that only runs AFTER new pages are written (write-before-delete).
**what-considered:**
- threshold fix: cap `expectedPages` to `OCR_MAX_PAGES` vs. raise the 80-page cap itself — chose capping expectedPages (task's own suggested design); raising the cap changes OCR runtime/cost policy, out of this S-size task's scope and not what the defect is about.
- AC2 fix: keep the DELETE but move it after the loop vs. drop it entirely (rely on existing `INSERT OR REPLACE` per-page UPSERT, `UNIQUE(filename,page_number)`) — chose drop-entirely + defensive post-loop prune of pages beyond the new `maxPages` window (handles the one real edge case: a prior run's pdfinfo probe returning MORE pages than this run, e.g. a transient pdfinfo failure defaulting `totalPages=30`).
**why-decision:** Both ACs are satisfied by the SAME two edits with no new signature/param surface — the loop's existing `INSERT OR REPLACE` already gives per-page UPSERT semantics for free, so removing the redundant/harmful upfront DELETE was strictly additive safety, not a redesign.
**why-change:** none — matches the task's suggested design exactly (AC1: `Math.min(expectedPages, 80)`; AC2: write-before-delete / per-page UPSERT).

**RED→GREEN evidence** (`FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH.test.ts`, 2 tests):
RED (pre-fix, 183ms): AC1 test failed `alreadyExtracted` expected `true` got `false` (80-row capped extraction judged incomplete, threshold=102 vs uncapped 204); AC2 test failed `countRows` expected `10` got `0` (blanket DELETE wiped the 10 pre-existing rows before any re-extraction — the availability-gap defect, live-reproduced).
GREEN (post-fix, 278ms): both pass — `[pdfOcr] already extracted` fires for the 80-row/204-page fixture (cappedExpected=80, threshold=40), row count stays 80; the 10-row fixture is still correctly flagged `[pdfOcr] incomplete extraction detected` (genuine incompleteness under the NEW capped threshold too) but its 10 rows survive untouched (no DELETE fired).
Test isolation: `mock.module("node:child_process")` (restored in afterAll, same pattern as 047-bctc-orchestrator.test.ts) fakes the `pdfinfo` lookup (204 pages) and makes `execSync("which ...")` throw so `isOcrAvailable()` returns false — avoids a real 80-page/160s+ tesseract loop. `?isolate=` cache-busted import avoids `_ocrAvailableCache` poisoning from sibling test files that already probed the real dev-machine tools.

### STEP dev-mcp-server-S42 · dev-mcp-server · 2026-07-31T12:20:00Z
**task-id:** FU-BACKFILL-MULTIPLE-COVER-LETTERS
**what-done:** Replaced `backfillBctcPdfPaths`'s negative-match "consolidated" selection (`matches.filter(m => !isCoverLetterFilename)`, FIX-CTG-PDF-MISLINK 77092007) with a new positive-match gate `isConsolidatedReportFilename()` (Vietnamese "hop nhat" / English "consolidated" marker, mirrors hsxBctcFetcher.ts's rankItem() signal); applied to BOTH the NULL-pass and the heal-pass candidate filters + heal-pass re-check guard.
**what-considered:**
- root cause: backlog hypothesised "first-match-only" exclusion, but code already used `.filter()` (excludes ALL literal cover letters). Actual gap: negative-match bucket = "not a recognised cover letter" ≠ "is the real report" — a non-report file (explanatory note etc.) failing the cover-letter pattern could be wrongly selected or inflate ambiguity, live-confirmed via AC-3 fixture (old code: `updated=1`, wrongly linked the non-report file).
- sole-candidate override: generalised from `coverLetters.length===1` to `matches.length===1` (any lone file still links, unaffected by classification) — preserves AC-6/AC-7/AC-R6 regressions unchanged.
- heal-pass guard: broadened from `isCoverLetterFilename(current)` to `!isConsolidatedReportFilename(current)` — old guard only re-checked rows mislinked to a RECOGNISED cover letter; a row linked to an unclassified non-report file was never eligible for healing at all (AC-4).
**why-decision:** positive-match closes the actual defect class (any non-report file, not just pattern-recognised cover letters) while reusing the existing "hop nhat" signal already proven correct elsewhere in the codebase (hsxBctcFetcher.ts) — no new heuristic invented.
**why-change:** none — matches task's explicit fix requirement ("REQUIRES a positive match... else leave PENDING loudly").

**RED→GREEN evidence** (`FU-BACKFILL-MULTIPLE-COVER-LETTERS.test.ts`, 12 tests): granular RED confirmed in 2 layers — (1) `isConsolidatedReportFilename` added standalone first (no selection-logic change): AC-1/AC-3/AC-4/AC-2-GREEN genuinely FAILED against unmodified selection logic (AC-3 particularly: `updated=1`, old code silently linked the non-report file — the actual mislink reproduced live, not simulated); (2) `legacyNegativeMatchSelect()` — a frozen, non-imported snapshot of the exact pre-fix 77092007 decision branches — independently proves `{kind:"ambiguous"}` (fails to heal) on the identical AC-1 fixture. Post-fix: all 12 pass, plus full pre-existing suites `FIX-CTG-PDF-MISLINK.test.ts` (14) + `PI3-bctc-inspect-reopen2.test.ts` (regression) unaffected — 56/56 pass across the 3 files. One unrelated pre-existing gap found+sidestepped (not fixed, out of scope): `extractTicker()`'s `KEYWORD_SET` is missing `"CV"`, so a `CV_CBTT_TICKER_...`-ordered filename mis-parses ticker as `"CV"` — fixtures use the already-proven ticker-first `TICKER_CV_CBTT_...` ordering instead.

### STEP dev-mcp-server-S43 · dev-mcp-server · 2026-07-31T12:45:00Z
**task-id:** FIX-LANCEDB-INSERT-SEGFAULT
**what-done:** Re-verified backlog root cause first (mandatory per task): it does NOT hold — commit d29da3a8d (2026-05-24) rewired step-4 to HTTP `ragIndex`, 2wks BEFORE the note's own 2026-06-07T12:33:46Z repro; commit 456851797 (2026-07-16, CI-RED-da847805-FIX) deleted the last `@lancedb/lancedb` import repo-wide (confirmed: 0 import statements in src/ today). Found+fixed the REAL live analog: `insertBctcAnalysis.ts`'s `const inserter = insertAnalysisFn ?? (await getDefaultInsertAnalysis())` sat OUTSIDE its own try/catch — moved inside.
**what-considered:**
- `mock.module()` throwing-factory to simulate a module-load crash — worked in isolation but FLAKY under full suite: Bun hot-swaps (eagerly re-invokes factory at registration, not lazily at import) whenever the specifier is already cached — true almost always in full-suite order since ragHttpClient.js is transitively imported by dozens of files. Rejected after empirical diagnosis (3 throwaway diagnostic tests).
- deterministic `insertAnalysisResolverFn` injection seam (new optional param, both files already document "everything injectable") — chosen: order-independent, exercises the real call chain, matches existing file philosophy.
**why-decision:** RED confirmed genuinely (temp-reverted try/catch placement, same test file, full stack trace `insertBctcAnalysis:79→fetchParseAndStoreBctc:121`), then GREEN post-fix; both isolated AND full-suite (2/2 pass, 0/2 fail — no longer in full-suite fail list, unlike the mock.module version).
**why-change:** test-mechanism swap (mock.module→injectable resolver) after empirical full-suite flakiness; fix itself unchanged from first attempt (1-line move).
**boundary/disclosure:** dev-rag-service NOT touched (ragHttpClient.ts/rag-service unchanged) — fix is 100% mcp-server-side, no cross-zone patch. Full bun test: 56 pre-existing unrelated failures (VPS Proxy Health, get_market_cap, insider_transactions, balance/cashflow stores, MCP SSE — 5000ms-timeout/DB-contention class); verified NOT caused by this diff: my new test 0-fail in full run, sole `fetchParseAndStoreBctc`-touching failure (FIX-1267 AC-7) independently re-run standalone → 9/9 pass.

### STEP dev-mcp-server-S44 · dev-mcp-server · 2026-07-31T13:22:00Z
**task-id:** FIX-BCTC-1345B-REPORT-BATCH
**what-done:** Verified the dispatch brief's root-cause hypothesis first (mandatory): 1792's `bctc_signal_debounce` only blocks same-(ticker,period) re-fires within a 1h window; it does nothing for `bctcReparseJob` (scheduler/financial-reports/bctcReparseJob.ts), which runs DAILY and retries the same still-broken backlog 24h apart — the actual noise source. Added a persistent "already-open quality row" gate in `storeReport()` (parseBctcReport.ts): before the `[BCTC-1345b]` alert, query `financial_reports.validation_status` for the same (action_code, sort_key); skip the Telegram send when it's already `'low_confidence'` from a prior parse.
**what-considered:**
- batch-per-cycle digest (PO note's option a) — rejected: no existing "cycle" boundary primitive to hook into; XS/P3 size doesn't justify new batching infra.
- generic Task-1215 category dedup (`telegram.ts` `isDuplicateReport`, 4h window, requires a "📋 category" marker in the text) — considered tagging the message; rejected: 4h window still re-fires on the 24h reparse cadence, AND it would over-suppress genuinely-new-ticker findings sharing the category within 4h.
- reuse `financial_reports.validation_status` (chosen) — persistent (not window-bound), self-closing (row leaves `'low_confidence'` the moment a re-parse produces real confidence), zero new table/migration; matches the backlog note's own phrase "already covered by an open quality row" exactly.
**why-decision:** RAW-verified via `bctcReparseJob.ts`'s docstring+cadence ("Runs daily at 09:30 GMT+7") that daily-retry noise, not same-minute retry storms, is the live gap — 1792 already fully solved the storm case (its own 5/5 suite still green, untouched). Smallest fix that kills the recurring daily volume without new infra.
**why-change:** none from the brief's own instruction to verify then pick/propose — proposed a 3rd option since neither named approach (batch vs. naive suppress) cleanly matched the verified root cause.
**RED→GREEN evidence:** new `FIX-BCTC-1345B-REPORT-BATCH.test.ts` (3 tests). RED confirmed via `git stash` of the fix in isolation: the daily-retry regression test failed (2 sent, expected 1) while the other 2 tests already passed under old code; GREEN after restoring the fix (3/3). Targeted suites: `1792-conviction-debounce` (5/5), `1345e-integration-pipeline`+`1424a`+`1810c` (19/19) all green, unaffected. `bun tsc --noEmit` clean.
**boundary/disclosure:** the file's OTHER `isBctcSignalDebounced` call site (period-content-mismatch alert, same file) intentionally NOT touched — that branch REJECTS the parse (no financial_reports row is ever written), so there is no persistent row to gate against; out of the note's `[BCTC-1345b]` scope. Full `bun test`: same 56 pre-existing unrelated failures already documented in S43 above (VPS Proxy Health, get_market_cap, insider_transactions, balance/cashflow stores, MCP SSE — 5000ms-timeout/DB-contention class); re-ran the exact failing files standalone → 100% pass, confirming not caused by this diff.

### STEP dev-mcp-server-S45 · dev-mcp-server · 2026-07-31T14:20:00Z
**task-id:** FIX-CRON-JOB-RUNS-DOUBLE-LOG
**what-done:** RAW-verified live (docker exec, real market.db) which job(s) currently double-log: `predictionResolutionJob` produced exactly 2 `cron_job_runs` rows every single sampled day (07-21..07-30, 10 consecutive pairs) — a `rows_written=NULL` row immediately followed by a real-integer row, same started_at second. Root cause: `runPredictionResolutionWithDb`'s default fn called the self-recording `runPredictionResolutionJob()` wrapper, which ALSO calls `recordJobRun` internally — the SAME double-wrap class already fixed for evidenceAccumulator/baseRateComputation/bctcReparse (S43-adjacent sibling fixes), never applied to predictionResolutionJob.
**what-considered:**
- fix `predictionResolutionJob.ts` itself (remove its own recordJobRun) — rejected: would change the exported wrapper's own observable contract and diverge from the established sibling-fix footprint (base-rate/bctc-reparse both deliberately left the sibling module untouched, matching file-touch minimalism).
- default fn now calls the core `runPredictionResolution(db)` directly (chosen) — mirrors the exact fix shape already proven 3x in this same file; zero new abstraction, reuses the pre-existing exported core function.
- also checked live for OTHER double-logging jobs beyond predictionResolutionJob (14d dup-group scan): `pollNewsJob` (275 groups) is NOT the AC's blank+integer pattern — it's two independently-completed rows (own duration_ms/finished_at each), i.e. the separate FIX-SCHEDULER-DOUBLE-REGISTRATION class (still open, different board row) — correctly left untouched, out of this task's AC.
**why-decision:** predictionResolutionJob is the one live job matching the AC's exact described pattern (one blank + one integer row, same started_at) with a deterministic, code-level root cause — not a probabilistic multi-firing artifact.
**why-change:** none — matches the AC and the 3x-proven sibling-fix pattern.
**RED→GREEN evidence:** new `FIX-CRON-JOB-RUNS-DOUBLE-LOG.test.ts` (10 tests, mirrors FIX-BASE-RATE/FIX-BCTC-REPARSE structure). RED confirmed via `git stash` of the fix: T3/T4/T4b/T6/T6b genuinely failed against unmodified code (T4: expected 1 row, got 2). GREEN after `git stash pop` restore: 10/10 pass. Related-file regression (1420-cron-health-coverage, 1125/PRED-RESOLVER-GAP-FIX/1154-prediction-resolution-loop, FIX-BASE-RATE-COMPUTATION-CRON-DEAD, FIX-BCTC-REPARSE-DOUBLE-WRAP-DEDUP-GUARD, ARCH-CRON-recover-jitter, FACTORY-SCHEDULER-job-table-registry): 130/130 pass. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged. Fresh boot (DB_PATH=:memory:, port 3099): `/health` toolCount=183, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200.
**boundary/disclosure:** Full `bun test` (RAW, full-capture log not tail-truncated): 14987 pass/40 skip/56 fail, 565.38s — same pre-existing full-suite-only flake band already documented in S43/S44 above (VPS Proxy Health, get_market_cap, insider_transactions, balance/cashflow stores, MCP SSE, 5000ms-timeout/DB-contention class); grep confirms ZERO prediction/cron/scheduler/startupHelpers matches anywhere in the fail list. `predictionResolutionJob.ts`'s own `runPredictionResolutionJob()` export now unused-in-production (no test depends on it directly) — deliberately left in place, matching the base-rate fix's precedent.

### STEP dev-mcp-server-S46 · dev-mcp-server · 2026-07-31T15:20:00Z
**task-id:** FIX-CI-IMF-INTEGRATION-TEST-NONHERMETIC-LIVE-API
**what-done:** `1296b-imf-integration.test.ts` AC-7 (`runImfIndicatorPollerJob`) rewritten to inject `fetchFn`/`storeFn`/`classifyFn` via the poller's existing `ImfPollerOptions` DI seam instead of calling the default (live `fetch()` to `www.imf.org`); removed the now-unneeded 35s per-`it()` timeouts. AC-8 left untouched (already hermetic — DB-cache read + pure classify, confirmed by inspection, no `fetch()` in call chain) with an inline comment recording that confirmation.
**what-considered:**
- gate live-network describes behind an unset CI env flag (deliverable's 2nd stated option) — rejected: poller already exposes exactly the seam sibling `1353a-imf-indicator-poller-job-gaps.test.ts` uses for the identical purpose; mocking there is zero-new-pattern, whereas an env-flag gate would silently drop AC-7 coverage in CI forever (never set) instead of exercising it deterministically.
- root-cause (not just symptom): `fetchLatestImfIndicators()` does 3 SEQUENTIAL `fetch()` calls each with its own 15s `AbortSignal.timeout` — worst case ~45s, exceeding the test's own 35s timeout under IMF-side latency/WAF throttling (comment's "30s timeout" math never accounted for 3x sequential). Mocking removes this entirely rather than raising the timeout (which would just move the flake window).
**why-decision:** PO triage (2026-07-31T14:37Z) rejected flake-disposition; deliverable explicitly requires AC-7/AC-8 non-network. DI-mock is the existing sibling-proven pattern, smallest diff, zero behavior change to production code.
**why-change:** none from the deliverable — implemented exactly the "mock the poller/fetch boundary" option it names first.
**verification:** `bun test src/__tests__/1296b-imf-integration.test.ts` → 14/14 pass, 156-270ms (was previously subject to up to 3×35s timeouts). Hermeticity proof: re-ran with `HTTPS_PROXY=http://127.0.0.1:1` (closed port, any real fetch would fail-fast/hang) — still 14/14 pass in 269ms, confirming zero live network reached. `bun tsc --noEmit` clean; `bun run lint:ci` clean. Full-suite `ci-per-file-isolation.sh 16` (after `bun install --frozen-lockfile` — worktree `node_modules` was stale/missing `zod` et al., unrelated env gap, fixed): 14845 pass/40 skip/41 fail; target file NOT in the failed-files list; spot-re-ran 5 of the other 14 failed files standalone (083-tool-analysis, 251-mcp-tools, bctc-eval-integration) → 100% pass isolated, confirming P=16 local resource contention (same documented class as S43/S44/S45 above), not a regression from this diff.
**dev-team merge (S80):** independently re-ran `bun test src/__tests__/1296b-imf-integration.test.ts` and the closed-port-proxy hermeticity check on the main checkout — 14/14 pass both times, matching self-report exactly. Cherry-picked `63f90ed7a` onto main; `.head`/board lane-move redone fresh against live main state (not cherry-picked) per worktree-staleness discipline — `.head.active_task_id` still equaled this task's id live, so review-flip synced `.head` to idle/router.

### STEP dev-mcp-server-S47 · dev-mcp-server · 2026-07-31T16:10:00Z
**task-id:** INVESTIGATE-EMPTY-DATA-TABLES
**what-done:** SPIKE (investigation-only, no code): live-verified writer path for all 5 zero-row tables against the real bind-mounted `market.db` (not a stale mount) via `sqlite3 -readonly` row/`cron_job_runs` queries + live `curl` probes of the actual VPS proxy endpoints and origin sites. Findings doc: `docs/spikes/SPIKE-INVESTIGATE-EMPTY-DATA-TABLES.md`.
**what-considered:**
- trust the 2026-05-16 prior spike (`SPIKE_1922`) verdicts as still valid — rejected: 2.5 months elapsed, 4/5 verdicts had changed (jobs/schema landed since), task explicitly required fresh re-verification.
- static code-read only vs live-reproducing each writer's actual runtime response — chose live: static read alone would have missed that the "SSC outage" comment (07-29) is now stale (live curl returns 200, not 503) and that the real defect is JS-only ADF rendering, not the outage.
**why-decision:** live reproduction against the actual proxy/origin endpoints was the only way to distinguish "genuinely rare events" from "silently broken pipeline" — code alone (esp. the "never throws" fetchers) gives identical symptoms (0 rows, no error) for both classes.
**why-change:** none — matches SPIKE mode contract (investigation only, no fix landed inline).
**verdicts:** `credit_data`=empty-by-design/orphan (already guarded, no FIX needed); `insider_transactions`, `public_contracts`, `pharma_events`, `broker_sanctions`=dead-pipeline, each root-caused with a scoped follow-up FIX task proposed in the RETURN/spike doc (not fixed inline, per task's own "investigation-only" AC).

### STEP dev-mcp-server-S48 · dev-mcp-server · 2026-07-31T18:52:00Z
**task-id:** FACTORY-INFRA-delete-deprecated-rag-kinhdich
**what-done:** BOUNDED-1 auto-pickup of a 2026-06-15 audit-brief row; investigated before coding (mandatory) and found the entire DoD already satisfied by 2 unrelated prior tasks: `456851797` (2026-07-16, CI-RED-da847805-FIX) deleted `rag/_deprecated/{vectorstore,retriever,embeddings}.ts` + its 5 test anchors + the `rag/index.ts` barrel re-exports; `448b41b0a` (2026-07-30, FACTORY-GUARD-CI-DEADCODE-IMPL) deleted `infrastructure/_deprecated/kinhDichWrapper.ts` + `1077-kinh-dich-wrapper.test.ts` + surgically edited `1081-sprint-054-smoke.test.ts`.
**what-considered:** implement anyway (no-op diff) vs flip BLOCKED per the dispatch brief's own "files already gone" trigger — chose BLOCKED per explicit dispatcher instruction; an empty/no-op commit would misrepresent QA-reviewable work.
**why-decision:** grep-verified zero remaining `_deprecated`/`kinhDichWrapper` real imports repo-wide (only historical comments); all 5 target files already absent from disk.
**why-change:** none — dispatcher explicitly named this exact scenario as a BLOCKED trigger.

### STEP dev-mcp-server-S49 · dev-mcp-server · 2026-07-31T17:45:18Z
**task-id:** FACTORY-DOMAIN-extract-sla-config
**what-done:** Moved SignalType/SignalSlaConfig/DEFAULT_SLA_CONFIG out of freshnessSlaChecker.ts (966L) into new sibling domain/services/freshnessSlaConfig.ts (194L, size-justified); checker imports + re-exports all 3 unchanged, zero call-site changes.
**what-considered:**
- packages/shared-config (backlog approach's primary suggestion) — rejected: 0 real importers repo-wide, tracked separately (FACTORY-SHARED-wire-or-prune-shared-packages, shared-package-import-baseline.json), and outside apps/mcp-server/ zone boundary.
- same-zone domain sibling file (backlog approach's own "(or a freshness/config.ts)" alternative) — chosen: in-zone, matches alertThresholds.ts/newsNormalizerTables.ts split-file precedent, zero new package-linkage risk.
**why-decision:** lowest-risk in-zone option satisfying the DoD's actual intent (separate config DATA from breach-classification ALGORITHM) without preempting the separate wire-or-prune decision on the orphaned shared package.
**why-change:** fixed 1 brittle literal-substring assertion in 234-vps-health-sla.test.ts AC-9 broken by the legitimate re-export syntax — not scope creep, required for G12 green gate.
**verification:** tsc --noEmit clean; targeted SLA suite 212/212 pass (14 files); fresh PORT=3099 server boot clean (toolCount 183, 90 cron keys, no import errors); tool/scheduler counts match baseline (183/88); full mcp-server suite 2 runs (14988-14995 pass, 52-59 fail) — grep-confirmed 0 SLA/freshness/config-related failures in either run's fail list (pre-existing FIX-MCP-SUITE-HEALTH-BASELINE order-dependent red).

### STEP dev-mcp-server-S50 · dev-mcp-server · 2026-07-31T18:33:11Z
**task-id:** FIX-CI-SIZELINT-VPSPROXYSTALENESS-REGRESSION-123L
**what-done:** Extracted `EXPECTED_INTERVALS`/`MARKET_HOURS_ONLY_SERVICES`/`NEWS_QUIET_HOURS_SERVICES`/`SBV_BUSINESS_DAY_SERVICES` (pure data + their historical-rationale comments) out of `vpsProxyStaleness.ts` (123L, size-lint new-offender) into a new sibling `vpsProxyStalenessConfig.ts` (48L); `vpsProxyStaleness.ts` (now 107L) imports + re-exports the 4 names unchanged so `vpsProxyHealthFormat.ts` and the FIX-VPS-NEWS-STALE-FALSEPOS test's import path both keep working with zero call-site changes.
**what-considered:**
- `--update` the size-lint baseline to grandfather 123L (AC-6 landmine, explicitly forbidden) — rejected: would retroactively void the file's own prior split (FIX-CI-SIZELINT-MCPSERVER-SIX-UNCOVERED-OFFENDERS AC-4) and repeat the dishonest-header pattern.
- `size-justification` header on the 123L file (technically available) — rejected: same reasoning, this file's entire prior existence IS the justification-avoidance mechanism; declaring it "justified" contradicts its own docblock.
- extract the config table (chosen) — only real seam; identical shape to `freshnessSlaChecker.ts` -> `freshnessSlaConfig.ts` (commit `9930ee008`, same day, same agent) already proven in this zone.
**why-decision:** AC-1/AC-6 mandated extraction over justification-header; the SLA-config split earlier today is the exact precedent to copy, zero new pattern invented.
**why-change:** none — matches the dispatch brief's prescribed remedy exactly.
**verification:** `EXPECTED_INTERVALS.news` still `20`, `isStale()` body byte-identical (only moved, not edited) — AC-2 untouched. `size-lint-justification.sh --check`: PASS, 0 offenders (AC-4). `bun tsc --noEmit` in the LIVE container: clean w.r.t. both touched files (2 pre-existing unrelated errors, `CONTAM-7`/`FIX-OHLCV-SEED-CANDLE` test files referencing `scripts/migrations/*.ts` which the image doesn't bake — confirmed pre-existing, zero `vpsProxyStaleness` matches). Targeted AC-3 suites in the LIVE container (not worktree): `FIX-VPS-NEWS-STALE-FALSEPOS-news-threshold-calibration.test.ts` + `VPT-1-vps-proxy-health-endpoint.test.ts` + `1113-vps-proxy-health.test.ts` — 19/19 pass, 89 expect() calls, 0 fail. `gen-project-stats.ts --dry-run` (host): toolCount=183, cronJobCount=88 — unchanged. `/health`: toolCount 183, no import errors. Full `bun test` in container: 252 fail/14912 — grep-confirmed zero `vps`/`staleness`/`1113`/`VPT-1` matches in the fail list; every failing name is disk-path assertions (`run-*-debug.sh exists/executable`, `entrypoint.sh` AC-5b/6/7/8, `maybe-deploy-vps.sh exists`) for `scripts/`/`vps-scripts/` files the container image never bakes — a container-vs-host environment gap, not a code regression (higher count than the previously-documented 52-59 host-run band because this is a full-suite run INSIDE the container per AC-3's explicit instruction, not on host).

### STEP dev-mcp-server-S51 · dev-mcp-server · 2026-07-31T19:09:00Z
**task-id:** FACTORY-APP-console-to-logger
**what-done:** Replaced every `console.error/warn/log/info` call site (24 total) in 5 application-layer files with the existing `infrastructure/logger.ts` `ToolLogger` instance — `discoverBctcPdfUrlDirectApi.ts` (3x error), `parseBctcReport.ts` (1x error, file already imported logger), `backfillBctcPdfPaths.ts` (11x warn/info), `imfDataFetcher.ts` (1x warn), `bctcBatchTableBackfillJob.ts` (9x warn/log/error). Added the missing `logger` import to the 4 files that lacked it.
**what-considered:**
- literal `console.X(...)` -> `logger.X(...)` swap, preserving message content/level exactly (20 single-string-arg calls) — matches the brief's "mechanical, no restructure" instruction and the sibling `parseBctcReport.ts::storeReport()` precedent already in this file.
- `console.error("msg:", errorObj)` (2-arg, 3 sites in `discoverBctcPdfUrlDirectApi.ts`) — logger's signature is `(message, context?: Record<string,unknown>)`, not variadic; wrapped in `{ error: error instanceof Error ? error.message : String(error) }`, matching `getMoneyRadarComposite.ts`/`fetchParseAndStoreBctc.ts`'s own convention.
- `console.log` -> `logger.info` mapping (brief's own fallback rule) — confirmed by sibling usage (`checkSscReports.ts`, `assembleAlertDigest.ts` both use `logger.info` for status lines).
**why-decision:** every choice reuses an already-live convention in this same zone rather than inventing a new one.
**why-change:** none — matches the brief exactly; also fixed one stale doc line (`usecases.md:289` said "loud `console.warn`", now `logger.warn`) per mandatory doc-review.
**verification:** `bun tsc --noEmit` clean. Targeted suite (10 files covering all 5 changed files): 120/120 pass, 569 expect() calls. Fresh boot (`PORT=3099`, 3000 held by the live container): `/health` toolCount=183, zero import errors. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — baseline match. Full `bun test`, 2 independent runs: 14988/14995 pass, 59/52 fail, 2/0 errors — both land inside the already-documented `14988-14995 pass, 52-59 fail` FIX-MCP-SUITE-HEALTH-BASELINE band (S49/S50, same day). Grep-confirmed ZERO matches for any of the 5 filenames/their test names in either run's fail list; all 52-59 failures are the same pre-existing unrelated classes (vnstock store upserts, VPS proxy health, `send_telegram` routing, insider-transactions streaks, foreign-flow signal text). REBUILD_REQUIRED=true. Commit: `3a0619464`.
