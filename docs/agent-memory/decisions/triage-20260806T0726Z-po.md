# PO Decision — dev-team triage tick 2026-08-06T07:07Z (second po spawn, same tick)

Router dispatched this tick at 07:07Z with 21 drained signals. **A peer `po` was already executing the
same tick** (commits `60ef2ce99` 07:09Z, `ffc58145d` 07:21Z) — discovered only at notebook-write time,
after this session had already landed a board write. The overlap and its cleanup are STEP po-D1 below.

---

### STEP po-D1 · 2026-08-06T07:26Z — duplicate-mint detection and fold
**task-id:** FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND
**what-done:** Detected that I had minted a second copy of a row the peer `po` landed 2 minutes earlier; folded my content into the first-landed row and deleted my copy.
**what-considered:**
- Keep my copy (richer: 5 explicit `ac[]`, `evidence`, `files`) and delete the peer's — rejected: inverts normal dedup discipline and discards the peer's `origin_signal_id`/`related_rows` provenance.
- Leave both and let qa reconcile — rejected: two open rows for one defect is precisely the churn dedup exists to prevent.
- Fold into the FIRST-LANDED row, delete mine. **CHOSEN.**
**why-decision:** The dedup rule everywhere in `triage-signals.md` is *fold into the existing row, never keep the newer mint*. Applying it to my own mint is the only self-consistent reading. Folded across: `ac[]` (5), `evidence`, `files[]`, `size`, `supervised`/`plan_only`/`baseline_pass`.
**why-change:** One load-bearing correction to the peer's row while folding — its `zone` was `docs/agents/`, which is **not** one of the three tokens `dev-team` Step 3 accepts (`apps/<service>/`, `multi`, `cross-service/`). `main.md` § Role in dev-team flow: *"missing zone = batch rejected back to PO"*. An invalid zone would have bounced the row on first dispatch. Corrected to `cross-service/`.
**generalisable:** My dedup scan ran ~07:20Z and was clean; the peer's write landed ~07:21Z. **A dedup scan is only valid as of its read instant** — under concurrent peers, re-check immediately before the mint lands, not once at triage start. `orch-apply.sh`'s CAS guards the *file*, not the *semantics*: both writes were individually valid, so nothing failed loudly.

### STEP po-D2 · 2026-08-06T07:23Z — agent_flow_defect is an unrouted signal type
**task-id:** FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED
**what-done:** Ran the doc's own `guard_signal_type_coverage()` against the LIVE hot file. It FAILS: `["agent_flow_defect"]`. Minted P1 → agent-father.
**what-considered:**
- Add the missing table row by hand and move on — rejected, that is the symptom.
- Treat it as covered by `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` (review) — rejected: that row shipped *this guard*; it did not ship a caller for it.
- Mint, with the root cause framed as **the guard has no scheduled caller**. **CHOSEN.**
**why-decision:** 4 live rows carry `agent_flow_defect`, all 2026-08-06, and **the two most actionable signals of this tick are among them**. Under the documented path both would have hit the `any unknown type` catch-all → *log and skip*. They reached triage only because the router hand-narrated them in the spawn prompt. A routing table that works only when a human-equivalent relays around it is not a routing table.
**why-change:** Mid-tick, at 07:21:27Z, a **second** unrouted type arrived (`db_anomaly_illiquid_ohlcv`, `sys-20260806T072127-34b3`) — also absent from all three tables and from the guard's `$routed` array. Drift is ongoing at ~2 new types/hour during an active sweep, not a one-off omission. Recorded on the row as `po_corroboration_20260806T0721` + `AC-6`; it is the strongest argument for AC-4 (wire the guard to a caller that runs) over hand-patching rows.

### STEP po-D3 · 2026-08-06T07:23Z — agent-father partial-implementation handoff
**task-id:** FIX-NOTEBOOK-COMPOSE-SCRIPT-ACTUATOR (+ FIX-NOTEBOOK-IMMUTABILITY-ORPHANED-BODY-CHECK)
**what-done:** Split agent-father's 3 out-of-zone asks into 2 developer rows; satisfied its Fix D(b) ask by stamping `recurring_bug_escalation: true, prior_warns: 7` on the primary.
**what-considered:**
- One combined row — rejected: two different files (`scripts/notebook-compose.sh` vs `scripts/git-hooks/pre-commit`), independently shippable.
- Three rows (one per ask) — rejected: Fix C is *"do NOT flip reject-mode yet"*, a **non-action**. It has no deliverable of its own; it is a constraint on Fix B. Encoded as `AC-4`/`AC-5` on the Fix B row instead of a row that could only ever be closed by doing nothing.
- Two rows + Fix C as ACs. **CHOSEN.**
**why-decision:** `prior_warns=7` already crossed the fleet's 2+ recurring-bug threshold (`feedback_recurring_bug_escalation`) independently of the brief, and `orch-state.json` is in `commit_zone.excluded` for agent-father — so the mint was structurally impossible for the agent that asked for it.
**why-change:** Live corroboration arrived during this tick: `commit-sweep-guard-2026-08-06T065721Z-69006.json` fired `[notebook-immutability-guard]` on `system-auditor.md` §`## c45`. Per the guard's own triage rule the bar is *same agent+section on ≥2 cycles*; sections differ (08-05 vs `c45`), so this fire alone would **not** justify a mint — the mint stands on the brief + Fix D(b), and the fire is recorded as corroboration only. Not conflating the two is the point.

### STEP po-D4 · 2026-08-06T07:20Z — CI RED def6ac67, file-scoped dedup
**task-id:** CI-RED-def6ac67 (no row minted)
**what-done:** Ran the MANDATORY pre-dedup failing-file read before evaluating dedup, then folded onto the existing row.
**why-decision:** `gh run view 31077879870 --log-failed` → `FAILING_FILES=[apps/mcp-server/src/infrastructure/db/schema.ts]`, *baseline-tolerance-exceeded (baseline=336L actual=377L upper=369L)*. Derived `dedup_key = ci_job:size-lint|file:apps/mcp-server/src/infrastructure/db/schema.ts` — an **exact** match on `FIX-MCP-MEMORY-CODE-LEAK` (review, non-terminal). Appended the observed SHA + the +41L drift; no new row.
**why-change:** `failing_jobs[].name` was `size-lint` and **11 open rows share that job name**. Deduping on the job name would have hit the wrong row 10 times out of 11. The file path is the only discriminant, which is exactly why the file read is mandatory *before* dedup, not after.

### STEP po-D5 · 2026-08-06T07:19Z — three CRITICAL/WARN empty-table signals, premise re-verified
**task-id:** FIX-AUDITOR-EMPTYTABLE-CHECK-NO-WRITER-DISCRIMINATOR (fold, no mint)
**what-done:** Queried the live host bind-mount DB directly rather than accepting the auditor's counts.
**why-decision:** `price_alerts=0`, `alert_engine_records=0`, `deep_fetch_stats=0` all CONFIRMED real — **not** artefacts of the known stale-named-volume probe (`sau-20260806T065408`). Control in the same session: `daily_ohlcv=775599`, `alerts=144`, `financial_reports=257`, so the reader was not pointed at an empty volume.
**why-change:** Two findings the signals did not contain. (1) **`pdf_documents` does not exist as a table at all** (`no such table`) yet C-11 reported *"failed (actual=0)"* — the detector renders MISSING and EMPTY identically, a strictly stronger form of the missing writer-discriminator that row already tracks. (2) C-04 reported `financial_reports=30` against a live raw 257, C-08 reported `alerts=42` against 144 — both checks carry an undocumented window/predicate, so their numbers must stop being read as raw row counts. Attached as `po_live_evidence_20260806T0717`. (The peer po independently reached the complementary conclusion from the writer side — `price_alerts` is demand-driven, `alert_engine_records` has no production writer. Same row, two directions.)

### STEP po-D6 · 2026-08-06T07:24Z — db-mount signal was already tracked; illiquid-OHLCV declined
**task-id:** FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT (fold) / `sys-20260806T072127-34b3` (declined)
**why-decision:** The router's tick note asked whether `sau-20260806T065408-dbaccess-stale-mount` still needed a FIX. It does **not** — `FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT` (backlog, P1) was minted 2026-08-05T07:06Z and its `files[]` **already** names `scripts/agents-flow/db-integrity-probe.sh` and `.claude/commands/crons/cron-db-data-integrity.md`, with AC-1/AC-5 mandating exactly the host-bind-mount pattern the router recommends. This is dispatcher throughput, not a triage gap.
**why-change:** Declined `db_anomaly_illiquid_ohlcv` (336 rows, `high=0 AND low=0`) after re-running the count live: **336 — byte-identical to the count recorded as evidence on the db-mount row a day earlier.** An unchanged value 24h apart is stable residue, not a regression. Single WARN, no corroborating prior fire → observation, no mint. The number matching an existing row's evidence *exactly* is what made this decidable in one query.

### STEP po-D7 · 2026-08-06T07:23Z — manual-dispatch sweep
**task-id:** TE-T05
**what-done:** Ran the mandatory sweep; 41 eligible candidates; stamped and folded exactly ONE (TE-T05, P1, `DRS-STRANDED-OFF-ALLOWLIST`, `next_agent=agent-father`).
**why-decision:** TE-T05 sorts first by `[rank, idx]`. It was previously flagged `2026-08-01T03:55:50Z` and **never dispatched** — re-admitted by `flag_reentrant`'s staleness window, which is the exact `TE-T12` failure shape that window was built to cure.
**why-change:** Worth flagging to the router: **28 of the 41 candidates route to `agent-father`**, which is off the DRS allowlist by design, so none are reachable by unattended idle pickup. This sweep releases one per tick against a backlog that is not draining — a throughput problem no PO-side change can fix.

### STEP po-D8 · 2026-08-06T07:28Z — coordinator cross-check, escalation on evidence
**task-id:** FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED
**what-done:** Re-classified `sys-20260806T072127-34b3` from "stable residue, decline" to **live instance #3 of the auditor dedup-precheck defect**; escalated the tracked row `high → P0` and folded it into this tick's BATCH.
**what-considered:**
- Leave my D6 disposition as-is — rejected: D6 was right about the *residue* (336 unchanged in 24h) but wrong about the *signal*. The residue is not the finding; **the fact that it was re-signalled at all** is.
- Mint a 4th row for the re-fire — rejected explicitly, that is the exact churn this row exists to stop.
- Escalate the existing row + dispatch it. **CHOSEN.**
**why-decision:** `feedback_recurring_detection_vs_recurring_failed_fix`: 3 confirmed instances with **zero fix attempts** is a recurring *detection* of an un-shipped fix, not a recurring failed fix. The remedy is dispatch, not a blocker and not a new row. I did **not** take the relay on trust — I read `corroboration[]` live from the hot file and confirmed 3 entries before acting.
**why-change:** Two things sharpen the eventual fix, recorded as `po_ac_addendum_20260806T0728`. (1) The same 336 rows were keyed `db_integrity_breach:daily_ohlcv:OHLCV_H_L_ZERO` on instance #1 and `db_anomaly:daily_ohlcv:illiquid_ohlcv_incomplete` on instance #3 — **same rows, same defect, two different keys**, so a ledger key-match will still miss even after AC-1 lands; dedup must key on table + row-signature. (2) The auditor's OUTPUT-CONTRACT counter defect reproduced verbatim: its narrative claimed "suppressed by dedup" while its own contract line in the same report read `dedup_skipped=0 | signal_queue_rows_written=1`. **The fix's own verification therefore cannot use the auditor's self-report** — it must read `.signal_queue.rows[]` directly. A fix verified by the defective reporter would self-certify green.
**generalisable:** A priority bump on a row already ignored across 3 instances is inert — which is the failure mode this very row documents. Escalation without dispatch would have reproduced the defect at the triage layer. Hence bump **and** BATCH.

### STEP po-D9 · 2026-08-06T07:33Z — generalised a single typo into a board-wide hypothesis
**task-id:** FIX-BOARD-ZONE-TOKEN-OFF-CONTRACT-35PCT-DISPATCHABLE-ROWS
**what-done:** Measured `zone` token validity across both dispatchable lanes after correcting one row's zone during D1. **147 of 415 rows (35.4%)** carry a token that is not `apps/<service>/`, `multi`, or `cross-service/`. Minted P1 → architect.
**what-considered:**
- Report it in the RETURN only — rejected: `feedback_po_notebook_mint_never_reaches_orchstate_board`. A finding that exists only in a message to the router does not survive the tick.
- Mint a normalisation task that rewrites all 147 zones — **rejected**, and this is the important one.
- Mint a row that must **falsify itself at AC-2** before touching any data. **CHOSEN.**
**why-decision:** Two independent instances surfaced in one tick — TE-T05 (`zone: .claude/skills/`, flagged 08-01, still undispatched 4 days later) and the peer's `docs/agents/` row I corrected during the D1 fold. Two instances is the threshold at which a typo becomes a hypothesis worth measuring, and the measurement came back at a third of the dispatchable board.
**why-change:** I deliberately did **not** assert causation. The 35% is derived from `main.md`'s **documented contract**, not from the matcher dev-team Step 3 actually runs — those can differ, which is itself one of the two possible outcomes (AC-6: if off-contract zones dispatch fine, the *prose* is wrong and this is a much smaller docs-drift fix). AC-1 forces reading the matcher first; AC-2 forces demonstrating that off-contract rows dispatch at a materially lower rate **before** any row is edited. Also separated two mixed defect classes: genuinely off-contract namespaces (`docs/agents/`, `.claude/skills/`, `scripts/`) vs trailing-slash drift on otherwise-valid `apps/*` tokens.
**generalisable:** This is the same shape as the memory lesson `feedback_grepped_docstring_comment_not_actual_code_array` — I measured against a **doc**, so the number is a hypothesis, not a finding, until the implementation confirms it. Writing the falsification step into the AC is cheaper than discovering later that 147 rows were rewritten for nothing. Applied the D1 lesson too: re-scanned for a duplicate immediately before this mint rather than trusting the scan from the top of the tick.
