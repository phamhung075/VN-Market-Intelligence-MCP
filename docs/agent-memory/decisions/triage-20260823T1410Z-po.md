# Decision Journal — qa-drain queue disposition 2026-08-23T14:10Z · po

**Trigger:** declared NEXT from the qa review-lane drain (commit `8199e6023`) — two rows handed back with `next_agent=po`, plus 2 endorsed-but-unminted follow-ups.
**Agent:** po
**Started:** 2026-08-23T14:03:14Z

---

### STEP po-Q1 · po · 2026-08-23T14:10Z
**task-id:** RAG-FTS-BUILD-MEMORY-BOUND
**what-done:** Upheld qa's HOLD (`done_verified` still withheld, stays `review[]`, AC#1 time-gate unchanged at 2026-09-20); split AC#2 into its own row `RAG-FTS-AC2-PEAKMEM-WALLCLOCK-MEASURE`, minted BLOCKED not READY.
**what-considered:**
- Certify — rejected: the row's own text documents one prior false green certified at a 116-row corpus; corpus is 32184 vs a ~56k failure scale.
- Dispatch AC#2 READY, as the 08:20Z drain said — rejected: that drain never actuated ("DISPATCH IT" minted nothing, zero AC#2 rows on any lane after a full-board scan) AND it dropped the contamination constraint po itself recorded on 08-15. Two sibling rows (`FU-RAG-DEPLOY-MEMORY`, `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED`) hold uncertified durability windows on the same container; a rebuild is a deliberate memory stress.
- Restate the intent in prose again — rejected: that is the defect, not the fix.
**why-change:** AC#2 is answerable at any corpus size and its number gates ALPHA-S2's deadline retune, so it must not inherit AC#1's month-long time-gate; but it must not run before `FU-RAG-DEPLOY-MEMORY` certifies either. A BLOCKED row with a machine-readable `blocked_by` encodes both facts; prose encoded neither and evaporated.
**evidence:** RAW 2026-08-23T14:03Z — `docker inspect vn-market-intelligence-mcp-rag-service-1 --format '{{.HostConfig.Memory}}'` = 2147483648 (2 GiB); `docker stats` = 1.461GiB/2GiB = 73.03% idle, ~552MiB headroom (was ~165MiB on 08-15). qa's certification floor on the siblings (>=24h window opened after 2026-08-22T12:40Z) matured at ~2026-08-23T12:40Z — it was ~20h and "too early" when qa last looked at 08:4xZ.

### STEP po-Q2 · po · 2026-08-23T14:11Z
**task-id:** RAG-FTS-BUILD-MEMORY-BOUND
**what-done:** De-hardcoded AC#1 + test_plan (b) in `docs/data/orch/archive/backlog-detail.json` — they now instruct a live `docker inspect ... HostConfig.Memory` read at verification time instead of naming a cap figure.
**what-considered:**
- Re-pin the number to 2g — rejected: this AC said 768m at mint, was corrected to 1g on 2026-08-15 (commit `2f835ec63`), and was 2 GiB by 2026-08-23. Three staleness events in five weeks; a fourth pin guarantees a fourth.
**why-change:** Fix the recurring class, not the current instance. A qa run against a stale ceiling measures nothing. Fix requirement (corpus-size-INDEPENDENT memory) untouched — factual correction, not scope change.

### STEP po-Q3 · po · 2026-08-23T14:10Z
**task-id:** FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58
**what-done:** Back-filled `children[]` with the three TASK_00x ids; kept the row BLOCKED in `in_progress[]`, `next_agent=po`; recorded a field-by-field refutation of qa's two supporting claims.
**what-considered:**
- Close the parent — rejected: AC-1 (set equality) passes today but AC-3 (negative control: add a ticker, demonstrate the divergence is CAUGHT) has no mechanism at all. `add_to_watchlist` still writes SQLite with zero write-back. Today's zero-diff is a side effect of the 2026-07-31 bulk reseed, not a guard. Closing on AC-1 alone ratifies the drift that already cost one missed crisis detection (SPIKE_1946).
- Lane-move it out of `in_progress[]` — rejected on a measured guard interaction, not inertia: `scripts/orch-row-prose-ceiling-check.mjs:105` scopes `PROSE_CEILING_LANES` to `[backlog, ready, review]`, so an `in_progress[]` source is invisible to the live-side lookup (`liveBytes=0`) and this row's ~17.9kB lands as net-new growth past 12000B. Hard reject until D3 ships.
**why-change:** Both of qa's factual premises were false and would have propagated. (1) "Defect still live, 34 of 58, re-measured this cycle" — po measured BOTH planes: `mcp__gateway__call_tool(vn-market, get_watchlist)` = 34 tickers, `jq '[.project.watchlist[].ticker]' docs/data/system-map.json` = 34, sets IDENTICAL, zero diff either direction. There is no 58-ticker roster; qa re-measured only the file and took 58 from the row's TITLE, which architect (08-14, docker exec + bun:sqlite) and po (08-22, gateway) had each already refuted. (2) "Consuming a WIP slot" — `scripts/lib/devteam-eligibility.jq:115-118` excludes BLOCKED from `wip_in_progress` by construction; measured `wip_in_progress = 1`, and that one was a genuinely IN_PROGRESS peer row. The only actionable part of qa's return was its SECONDARY finding (absent `children[]`), which is now fixed. Partial mitigation qa missed: all 3 children already carry `parent_task_id`, so only the parent→child edge was lost.

### STEP po-Q4 · po · 2026-08-23T14:10Z
**task-id:** TASK_003-DOMAIN-MODEL-WATCHLIST-COUNT-FIX
**what-done:** `owner`/`next_agent` `generic-developer` → `developer`.
**what-considered:** Leave it — rejected: no `docs/agents/generic-developer/` exists, the token is absent from `.claude/skills/dispatch/SKILL.md`, and it occurred exactly once on the whole board. A READY row pointing at a non-existent agent is unspawnable.
**why-change:** Found while linking the parent's children; one field, zero risk, removes a silent strand.

### STEP po-Q5 · po · 2026-08-23T14:10Z
**task-id:** FIX-QA-VC-ACTUATOR-NO-REGRESSION-FIXTURES
**what-done:** Minted (backlog, BLOCKED on `FIX-QA-VC-LANEMOVE-PROSE-ONLY-NO-ORCHAPPLY-ACTUATOR`, `developer`, P1, `cross-service/`).
**what-considered:** Skip as "just test coverage" — rejected: qa found TWO blockers in the actuator patch pre-ship, and a fixture asserting the actuator's own output passes `scripts/orch-validate.mjs` would have caught both. Measured 2/2 on the one change it would have covered.
**why-change:** This is AC-4+AC-5 of a shipping row, carved out because `scripts/` is outside agent-father's `commit_zone`. Without an owning row it evaporates exactly as RAG-FTS's AC#2 did. Added a mandatory negative control (break the actuator, prove the verifier goes RED) per `feedback_janitor_false_green_verify`, and opt-IN scope only per `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist`.

### STEP po-Q6 · po · 2026-08-23T14:10Z
**task-id:** FEAT-COWORK-WRITE-LAST-FIRED-DESTAMP-MODE
**what-done:** Minted (backlog, BACKLOG, `developer`, P2, XS).
**what-considered:** Decline as convenience tooling — rejected after reading the script: its own header documents TWO production corruptions from hand-rolled inline implementations of this exact write, both silent. Rolling a stamp back has no tool today and is done by hand-editing `docs/data/cowork-schedule.json` — the same hazard on the same file, in the other direction.
**why-change:** The forward-only monotonic guard is correct for the Step 5b hot path and must stay; `--destamp` bypasses it for exactly one slot per invocation (a batched rollback is the shape of the jq needle-binding corruption the header warns about) and reuses the existing atomic write path.

### STEP po-Q7 · po · 2026-08-23T14:10Z
**task-id:** (ambient)
**what-done:** Did NOT touch `FIX-QA-VC-LANEMOVE-*` (→agent-father), `FIX-PM-DECOMPOSE-*` (→agents-architect), or `FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE` (→ops).
**why-change:** All three already routed by qa with those agents spawning in parallel; re-routing would race live peers. Verified their lane/next_agent are as the router described and left them untouched.
