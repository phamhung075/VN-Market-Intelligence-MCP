# PO decision journal — TASK-COWORK-CATCHUP-3 disposition + Step 0-SIG drain — 2026-08-23T10:33:27Z

**task-id:** TASK-COWORK-CATCHUP-3 · TASK-COWORK-CATCHUP-4 · TASK-COWORK-CATCHUP-5 · TASK-COWORK-CATCHUP-6 · TASK-COWORK-CATCHUP-9 · TASK-COWORK-CATCHUP-10 · FIX-SIGNAL-TYPE-ROUTING-GAP-cowork-fire · FIX-CI-BUNTEST-167-PREDICTION-MARKET-JOB
**Session:** `007e33e4-b453-4bb3-8ab1-ef31495906a3`, router-direct. Router holds `task:TASK-COWORK-CATCHUP-3` (not re-claimed).
**Board at start:** `.head` PINNED `in_progress`/`TASK-COWORK-CATCHUP-3`/`next_agent=developer` (claimed 10:24:37Z by the Ready-Lane Consumer); `in_progress[]=3`; `pending_triage_inbox=6`.
**Inputs:** `docs/architecture-briefs/2026-08-23-cowork-slot-durability-layer-inventory-and-miss-detection.md` (full) · `docs/architecture-briefs/2026-07-22-cowork-guaranteed-slot-catchup-design.md` §2.1/§2.3/§7 · live `orch-state.json` · `scripts/lib/devteam-eligibility.jq` · `scripts/orch-row-prose-ceiling-check.mjs` · `scripts/orch-cold-evict.sh` · `scripts/audits/guard-signal-type-coverage.sh` · `gh run view --log-failed` ×3 · repo-wide grep for `catchup_raw`/`catchup_eligible`/`catchup-check`
**Writes:** one `scripts/orch-apply.sh` pipe (transform in session scratchpad — one-shot, not reusable, deliberately not persisted to `scripts/`).

---

## S1 — CANCEL, not rescope. Four independent legs.

**what-considered:** (a) cancel; (b) rescope toward brief §6; (c) rescope to a narrowed Step 4.55 with a widened lateness bound; (d) leave BLOCKED pending more evidence.

**why-decision:** (a). Each leg below is independently sufficient; I did not need to lean on the architect's authority alone.

1. **Measured zero value.** Brief §4 ran the live matcher: `catchup_raw` = 8 records, `catchup_eligible=true` on **zero** (6 `rolled_past_vn_date`, 2 `freshness_window_exceeded`). `catchup_max_lateness_minutes` 60–1440 bounds recovery to one VN day; the outage was 96.5 h Standby. A fully-wired Step 4.55 recovers nothing.
2. **Explicit architect veto**, verbatim: *"Do not schedule 'wire Step 4.55' as the fix. It is not a fix for this failure at all."* The brief formally supersedes 2026-07-22 §2.1/§2.3 — exactly the sections this row implements (FR-9b + FR-3 + FR-5).
3. **Structural — mine, not the brief's, and it kills option (c).** The deliverable is a *dispatcher* sub-flow, i.e. **Layer B**, which brief §1 measures as session-scoped and evaporating on CLI exit. During every outage window this catch-up exists to cover, the dispatcher is itself down. It can only run once a session returns — by which time the predicate has already refused. **Absent exactly when needed, useless exactly when present.** That is independent of the lateness bound, so widening the bound would not rescue it either. This is why I refused (c) rather than treating it as a cheap compromise.
4. **Rescope was unavailable — the successor work already exists.** All six §6 items *plus* the §7 gate are already live rows under parent `FIX-COWORK-DAILY-SLOT-SILENT-SKIP-…`: `TASK-COWORK-{LAYERC-LASTFIRED-WRITEBACK, MISSED-FIRE-AUDIT, SCHEDULE-ONMISS-AND-SCOPE, CATCHUP-SCOPE-PREDICATE, DOC-TRUTH-LAYER-INVENTORY, PMSET-WAKE-ADJUNCT, STALE-SLOT-DISPOSITION-TABLE}`. Rescoping toward §6 would mint an eighth duplicate of work already owned by named agents.

**Nothing of value lost:** FR-5 (structured miss record), the one useful half, is superseded by item #2's miss detector — cron-derived expected-vs-actual, one row per `(slot_id, window)`, bounded look-back (R3), run from a script rather than a session-scoped step.

**why-change:** Router offered cancel-or-rescope. I took cancel on leg 4, which the router could not see because it requires cross-referencing the new rows against §6 item-by-item.

## S2 — The siblings share the premise. Two of them, and they were the live hazard.

2026-07-22 §2.3 specs **exactly three** `catchup_raw` consumers: dispatcher `catchup-check.md` (=CATCHUP-3), `cowork-tick-preflight.sh` Step 6.5 (=CATCHUP-4), `cowork-guaranteed-slot-firer.sh` (=CATCHUP-5). All three sit on the same zero-eligible predicate. **CATCHUP-4 and -5 were both `ready[]`/P0/deps-satisfied** — the next RLC tick claims them the same way, for the same reason. Cancelling only CATCHUP-3 would have fixed one instance of a three-instance defect.

**But blanket-cancelling all three would have been wrong** — see S3. Moved both `ready[]→backlog[]`, `status=BLOCKED`, `next_agent=architect`, `blocked_by` = the §6 chain + the new actuator row, with three named stale-spec points each.

## S3 — ★ The brief's own §6 is producer-only. Found by grep, not by reading.

Item #4 widens `catchup_scope` so `refine-bctc-*`/`bctc-analyst-*` reach `catchup_eligible:true`. Item #3 adds `on_miss: "catchup" | "skip_and_record"`. **Neither mints an actuator.** Repo-wide grep across `*.sh *.js *.md *.ts`: every hit on `catchup_raw`/`catchup_eligible` is a producer, a spec, or a doc. `catchup-check.md` does not exist; preflight and firer were never wired. **There is no consumer of `catchup_eligible` anywhere.** Brief AC-3 asserts only that the predicate's *output* is right, never that anything fires.

So on the day item #4 ships, the system correctly computes that `refine-bctc-slot-1` is catch-up-eligible, declares `on_miss:"catchup"` for it, and does nothing — **the exact producer-shipped/consumer-stranded shape `TASK-COWORK-CATCHUP-2` created on 2026-07-28**, which this row's own `po_note_20260806` already documented the cost of. Second occurrence of the same defect inside the same cluster. And `refine-bctc-slot-1` is precisely the slot whose 8-day staleness raised the parent row — so without an actuator, the widening does not fix its originating symptom.

Minted `FIX-COWORK-CATCHUP-ELIGIBLE-HAS-NO-ACTUATOR` (P1, architect), and it is **why CATCHUP-4/-5 are blocked rather than cancelled**: they are the only actuator candidates on the board. Architect decides (i) scope a minimal actuator, or (ii) rule `catchup_scope` diagnostic-only and **amend the brief in writing** — a legitimate answer, but `on_miss:"catchup"` that fires nothing is the same false promise brief §3 blames for `guaranteed` predicting nothing. Not to be settled by silence.

## S4 — The cancel created its own hazard. Caught before writing, not after.

`CANCELLED` ∈ `TERMINAL_SET`, and `scripts/lib/devteam-eligibility.jq:79-81` `is_terminal_task_status` counts a terminal dep as **satisfied**. `TASK-COWORK-CATCHUP-6` and `-9` both `depends_on: [… "TASK-COWORK-CATCHUP-3"]`. Cancelling CATCHUP-3 alone would have flipped both deps-satisfied and made them *newly dispatchable* — a strictly worse board than before I started.

Repointed in the same write: `-6 → TASK-COWORK-LAYERC-LASTFIRED-WRITEBACK`, `-9 → TASK-COWORK-DOC-TRUTH-LAYER-INVENTORY`, both `BLOCKED`. Both are also contradicted on the merits: `-6` is a **Layer-B** reconciler for `last_fired`, but brief §2 measured the actual mechanism as **Layer C** never calling postflight — a Layer-B reconciler cannot see those fires. `-9` would have documented the cancelled design *as shipped* (its handoff step 4 says so verbatim), which is brief §1's own doc-truth failure mode reproduced.

`-10` flagged, not cancelled: it targets `docs/protocols/cowork-master-cron-runbook.md`, the identical file and identical owner (`agent-father`) as `TASK-COWORK-DOC-TRUTH-LAYER-INVENTORY`, whose ACs are strictly stronger. Its dep chain already holds it, so flagging was enough today.

## S5 — Router item (2) is a false positive on both axes. I did not "fix" it.

Router reported `UC-CDC-P1` and `FIX-SYSTEM-MAP-WATCHLIST-…` as BLOCKED with **no `hold_reason`**. Measured:

- **Field-name error.** `hold_reason` is `null` fleet-wide (0 populated on any BLOCKED row). The canonical field is **`blocked_reason`** (15 populated). Both rows carry it, both populated, plus `blocked_by` arrays.
- **The lane move is defect-blocked, and that was already reasoned 7 min before the tick.** `scripts/orch-row-prose-ceiling-check.mjs:105` `PROSE_CEILING_LANES=['backlog','ready','review']` — `in_progress[]` is unguarded, so the id-keyed live lookup returns `liveBytes=0` for any row moving *out* of it, and `candidateBytes > 0` hard-rejects. Verified at source (lines 265-270), not taken on trust. The peer PO's `po_wip_ruling_20260823` records it, with the D3 row named as the blocker.

Both rows are already excluded from `wip_in_progress` (`devteam-eligibility.jq:117`), which is the outcome that matters. **Overriding a correct, evidence-backed prior ruling with a hand-correction would have been the defect, not the fix.** Left untouched.

## S6 — Step 0-SIG: 6 envelopes, not the 2 announced

Router named 2 fresh; my own fresh read (SSOT per `FIX-TRIAGE-INBOX-CLEAR-OWNERSHIP-PO-SELF-READ`) found **6** — 4 carry-over that never cleared.

| type | disposition |
|---|---|
| `ci_red` run 32630904807 | **File-scoped dedup hit.** Mandatory pre-dedup read run: `FAILEDFILE: src/__tests__/167-prediction-market-job.test.ts`, exact `dedup_key` match on `FIX-CI-BUNTEST-167-PREDICTION-MARKET-JOB`. SHA appended. Pass count moved 15119→15159 since the 2026-08-14 sighting while the same single file kept failing — **standing failure, not flaky-under-load**. |
| `ci_red` runs 32629387939 + 32631502135 | Both `signal-type-coverage-guard`. `--log-failed` names the cause verbatim: unrouted Pipeline-A type `["cowork-fire"]`, and the guard's own next line says a backlog row already tracks it. Dedup hit on `FIX-SIGNAL-TYPE-ROUTING-GAP-cowork-fire`. |
| `cowork-fire` | The unrouted type itself. Routed via the row above; no separate mint. |
| `bug-escalation` sweep-guard BARE | `escalated=false`, `prior_warns=0` → confirmed true positive, `pendingObservations` only. Spec forbids a per-occurrence mint. |
| `bug-escalation` notebook-immutability-guard (`pm.md`) | First fire for that agent+section pair; mint threshold is 2 cycles. Observation only. |

**★ The tracking row could never move: `next_agent` was unset.** Auto-filed by the guard at 09:25:41Z with no `next_agent`, which no dispatch lane resolves — so the row tracking a red CI job was itself undispatchable. Set `agent-father` (zone `docs/agents/po/flow/`; PO may not edit its own flow files per its own `boundary_rules`) + P1.

**★ Recorded a green I do not believe.** `guard-signal-type-coverage.sh:198` reads its input from the **live** `.dev_team_idle_chain.pending_triage_inbox[]`. My own CLEAR removes the `cowork-fire` envelope from that array, so the next CI run can pass **with the routing table still unfixed**. That green is an artifact of draining the guard's input, not evidence of a fix; the next cowork tick re-appends and it goes red again. Written onto the row so nobody claims it landed. Cf. `feedback_devteam_drain_step_can_itself_turn_signal_type_guard_red`.

## S7 — Two standing-defect mints, both verified at source first

- **`FIX-DISPATCH-GATES-BLIND-TO-PROSE-DISPOSITION`** (P1, architect). Full causal chain: 10:17Z peer PO *correctly* ratified CATCHUP-2 `DONE→DONE_VERIFIED` → `deps_satisfied` false→true for CATCHUP-3/4/5 → 10:24:37Z RLC claimed the P0 row. **Every field RLC read was accurate**; the contradiction lived only in prose. RLC did nothing wrong. Root cause: no machine-readable way to say "ruled out" — `BLOCKED` overloads *waiting on a dep* with *ruled out on the merits*, which is why pm correctly declined to use it. I stamped `disposition`/`disposition_reason_code`/`do_not_reopen`/`superseded_by_*` on the cancelled row **as a straw-man to critique, explicitly not as a standard** — nothing reads them yet, which is the same dead-field trap as `FIX-ARCHITECTHANDOFF-DEAD-FIELD-ZERO-READERS-…`. Gate must be proven by *executing* the predicates against a fixture.
- **`FIX-SIGNALQUEUE-OPEN-STATUS-DRAINED-BY-NOTHING-EVICTED-BY-NOTHING`** (P1, architect). Three probes. Live distribution `OPEN=6, READ=13, triaged=11` — **zero `NEW` rows**, so a NEW-only drain is vacuous here. `orch-cold-evict.sh:150` `TERMINAL_SIGNAL_STATUSES` omits `OPEN` → never evicted. Two distinct defects, flagged as not-to-be-conflated: a write-side producer emitting `OPEN`, and a lifecycle hole where `OPEN` can never leave the hot file. Not cosmetic: 3× `cron_fire_gap` + 1× `launchd_tracking_gap` are unread ops findings in the exact subject area of the 96.5 h Layer-C outage, one written the day the host woke.

## S8 — What I did not do

- Did **not** touch `UC-CDC-P1` / `FIX-SYSTEM-MAP-WATCHLIST-…` (S5).
- Did **not** cancel CATCHUP-4/-5 (S3 — they are the only actuator candidates).
- Did **not** mint a Pipeline-A `cowork-fire` table row myself: `docs/agents/po/flow/triage-signals.md` is PO's own flow doc, forbidden by `boundary_rules`. Routed to `agent-father`.
- Did **not** persist the transform to `scripts/` — one-shot, not reusable; persisting it would add dead code.
- Did **not** run `orch-cold-evict.sh` (bulk hot-file mutation, outside a decision-only tick), though the newly archived `CANCELLED` row is now eligible for it.
