# Sprint: DEPLOYMENT-INTENT-GATE-V2 (po agent)

## Context
Quality audit wrongly excused 6 services (stock-price, technical-analysis, kinh-dich-service, alert-engine, rag-service, news-fetch) with ONE tautological INFO check each ("is it correctly undeployed-by-design?"), running ZERO checks on actual live capability. architect redesigned the gate (brief `docs/architecture-briefs/2026-06-10-deployment-intent-gate-v2.md`, commit 803c4163). PO decision gate: confirm two-axis policy sound + external-auditor principle (capability delivered != container running), spot-probe ONE live capability, confirm NO container deploy proposed (host-panic guard), then APPROVE.

## Decision Journal

### STEP po-DIG-V2-S1: Two-axis deployment-intent gate policy — APPROVE (2026-06-10T19:22Z)
**task_id:** DEPLOYMENT-INTENT-GATE-V2
- **what-done:** Read architect brief (commit 803c4163). Sanity-probed live capability via gateway to confirm the premise that capability is served live from mcp-server's tool surface, decoupled from the standalone container. Confirmed standalone containers absent on host. Verdict: APPROVE.
- **live-spot-probe (premise proof):**
  - `get_market_snapshot` (stock-price capability) → `vn_index.price = 1803.71, change_pct +0.59, direction up, fetchedAt 2026-06-10T19:22:30Z, source_tier 2`. GENUINELY LIVE, not dark.
  - `get_alerts` (alert-engine capability) → 20 alerts last 7 days, most recent 2026-06-10T17:23 (news PLX), macro/volume/price-surge classes all flowing. GENUINELY LIVE.
  - Container guard: `docker ps --filter name=stock-price|wc -l` = 1 (header only), same for `kinh-dich`, `alert-engine` = all ABSENT. Running set = mcp-server, api-gateway, frontend, macro-indicators, pdf-extractor (+ headroom-proxy, mcp-gateway). The 6 capability-services have NO standalone container — capability is served from the mcp-server monolith. Premise CONFIRMED: capability delivered != container running.
- **what-considered:**
  - Path A — keep single tautological INFO per service: REJECTED. The check `"is <svc> correctly undeployed-by-design?"` reads its verdict from the same field it asserts (`host_runtime_set.not_deployed_by_design`) — zero capability signal. Masked 5 live-in-monolith capabilities + 1 dark one. This is the exact external-auditor failure the user flagged.
  - Path B — flip undeployed services to WARN/FAIL on container absence: REJECTED. Container absence is INTENTIONAL per HONOR-PANIC-GUARD (16GB host kernel-panics under full fleet, record project_host_memory_panic). Penalizing intentional absence is a false positive.
  - Path C (CHOSEN) — two orthogonal axes. Axis A (Container Reliability): 1 INFO/grey check per service, but the QUESTION rewritten to be HONEST/non-circular ("container intentionally absent — is its capability served by mcp-server WITHOUT a silent hard-dependency on the absent container?"). Axis B (Capability): N checks per service probed LIVE via `capability_manifest`, scored PASS/WARN/FAIL on real evidence across Functional/Freshness/Correctness/Degradation/Observability — NEVER auto-INFO. rag = only genuinely-dark capability (probe=null) → WARN with written rationale + backlog item to define a probe. Net 6 tautological INFO -> 30 real checks (+24, total 240 -> 264).
- **why-decision:** Aligns with the external-auditor principle: an auditor scores the CAPABILITY DELIVERED to the consumer, not the deployment topology. The live probes prove the premise empirically — the capability is delivered regardless of standalone-container state. Axis A keeps the panic guard honest (INFO, never escalated for intentional absence) while Axis B closes the audit blind spot. rag correctly lands WARN (capability may function but is unobservable — not INFO, not FAIL). Summary recompute-FROM-rows (never hand-edit) matches single-merge-writer discipline.
- **why-change:** Policy RATIFIED. Authorizes the system-auditor merge-writer to: (1) rewrite the 6 `*-AVAIL-01` checks in-place to the honest Axis-A template (keep check_id, keep INFO); (2) add the 24 Axis-B capability checks; (3) PROBE LIVE each Axis-B check via capability_manifest and set PASS/WARN/FAIL on real evidence (rag FUNC/OBS = WARN with rationale + backlog probe task); (4) recompute `overall` + per-cap roll-ups FROM rows; (5) apply the system-map.json `host_runtime_set._note` scope-fix so the INFO rule binds the Container-Reliability (Axis A) dimension ONLY. Sole writer of `docs/data/quality-checklist.json` + `docs/data/system-map.json` = system-auditor merge-writer; same commit window; router pushes.
- **guard-rails confirmed:** NO container deployment proposed — brief scope is checklist + one system-map `_note` field only; HONOR-PANIC-GUARD intact (16GB host, no fleet spin-up). PO did NOT write quality-checklist.json. Commit pathspec = this decision-journal file ONLY (+ orch-state if signal recorded), explicit from root, never -a/-A, mutex-guarded.
- **acceptance-criteria for merge-writer:** total checks = 264; each Axis-B check carries non-null `evidence` + `last_verified` from a live probe (not pre-scored); rag FUNC-01/OBS-01 = WARN with rationale; `overall` recomputed from rows; system-map `_note` scoped to Axis-A container-reliability; commit pathspec exactly `docs/data/quality-checklist.json docs/data/system-map.json`; no push.
