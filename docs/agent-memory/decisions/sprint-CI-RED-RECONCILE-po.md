# Sprint: CI-RED-RECONCILE (po agent)

## Context
ops landed PUSH-CI-FIX-TO-REMOTE (HEAD 9f063c9a → origin/main; dd79f811 FIX-CI-LINT-STACK verified in pushed set). Fresh run 27153704278 now validates fresh code: original lint fix proved out (5 Go lints + py-lint PASS, kinh-dich-ts-lint job deleted). TWO residuals visible on fresh code: GO-VERSION-class lint failures (2 jobs) + bun-test external-source failures. PO triages, resolves BUN-TEST-CI-GATE, opens GO-VERSION task(s), dedups, journals.

## Decision Journal

### STEP po-S1: Resolve BUN-TEST-CI-GATE (gate-clear → scope atomic FIX) (2026-06-08T17:17Z)
**task_id:** BUN-TEST-CI-GATE
- **what-done:** Gate dependency PUSH-CI-FIX-TO-REMOTE landed → gate unblocked. Pulled fresh-run failed log (run 27153704278) and classified the bun-test failures into two disjoint classes.
- **what-considered:**
  - Path A — close NOOP (stale-run artifact): REJECTED. bun test is RED on FRESH code, not stale.
  - Path B — one blanket "fix all bun failures" dev task: REJECTED (conflates two unrelated root causes; violates atomic-FIX).
  - Path C (CHOSEN) — split by root cause: (1) external-source network class → CI-environment flakiness (network-guard/skip-in-CI), (2) 123-integration-mcp count drift → genuine regression, singled out per router instruction.
- **why-decision:** Log evidence is decisive. The yahoo/sbv/insider/news failures all share the signature "live external source returns null/0 in CI sandbox" (network-blocked) — env flakiness, not logic. The 123-integration-mcp failure is at line 867 `expect(toolNames.length).toBeGreaterThanOrEqual(16)` → Received 15; ALL 14 named `toContain` core-tool assertions PASSED. So the count dropped because a NON-enumerated registered tool was removed — a real in-process `_registeredTools` registry drift, NOT network. Two root causes → two scopes.
- **why-change:** BUN-TEST-CI-GATE itself RESOLVED (its spike job — read fresh run, isolate real assertion, scope FIX — is complete). Spawned children: FIX-MCP-TOOL-COUNT-DRIFT (real) + folded network class into FIX-MCP-CI-NETWORK-GUARD. Dedup note: network-guard overlaps existing FIX-MCP-SUITE-HEALTH-BASELINE (TODO) — see po-S3.

### STEP po-S2: GO-VERSION residual — diagnose root cause BEFORE assigning (2026-06-08T17:17Z)
**task_id:** GO-VERSION-MISMATCH (diagnosis)
- **what-done:** Compared all 6 services' go.mod `go`/`toolchain` directives + each job's golangci-lint config + pulled the actual exit-3 error line for BOTH failing jobs.
- **what-considered:**
  - Hypothesis A — global ci.yml setup-go regression: REJECTED. ci.yml uses per-service `go-version-file: apps/<svc>/go.mod`; 4 of 6 services pass. Not a workflow-wide bug.
  - Hypothesis B — both failures = same "go1.24 < go1.25" cause (ops journal claim): REJECTED after reading raw logs. The two jobs fail for DIFFERENT reasons.
- **why-decision:** Raw log proves two distinct root causes:
  1. **macro-indicators** (`Macro Indicators Go Lint`): `Error: can't load config: the Go language version (go1.24) used to build golangci-lint is lower than the targeted Go version (1.25.0)`. macro-indicators/go.mod declares `go 1.25.0` — the ONLY service that over-declares (other 5 = `go 1.22`). golangci-lint v2.0.2 binary is built with go1.24 → exit 3. ROOT CAUSE = go.mod over-declaration vs repo standard. ZONE FIX (not ci.yml): align `go 1.25.0` → `go 1.22`. Owner: dev-macro-indicators.
  2. **technical-analysis** (`go-lint`): `Error: can't load config: unsupported version of the configuration: ""`. apps/technical-analysis/.golangci.yml is the ONLY one of 6 MISSING the `version: "2"` field (other 5 all have it); golangci-lint v2.0.2 requires it → exit 3. This is NOT a go-version issue at all — it is a v1→v2 config-migration miss left over from FIX-CI-LINT-STACK (which bumped the action to v7/golangci v2 but didn't migrate this one config). ZONE FIX: add `version: "2"`. Owner: dev-technical-analysis.
- **why-change:** Renamed the umbrella from one "GO-VERSION-MISMATCH" task into TWO atomic single-zone FIX tasks (different owners, different files, no shared commit) — FIX-MACRO-GO-DIRECTIVE + FIX-TA-GOLANGCI-CONFIG-V2. Neither is a ci.yml/workflow change; both are config alignment to the existing repo standard. NOT cross-service.

### STEP po-S3: Dedup + WIP + escalation check (2026-06-08T17:17Z)
**task_id:** CI-RED-RECONCILE (sprint-level)
- **what-done:** Cross-checked the 4 new tasks against board backlog; checked in_progress count vs WIP; ran recurring-bug rule.
- **what-considered / decisions:**
  - DEDUP: FIX-MCP-CI-NETWORK-GUARD overlaps FIX-MCP-SUITE-HEALTH-BASELINE (TODO, po, apps/mcp-server — "classify standing bun-test baseline 40 fail / 848 pass, define green baseline"). DECISION: keep them separate but LINK — NETWORK-GUARD is the CI-specific subset (guard live-source tests so ci.yml goes green); SUITE-HEALTH-BASELINE is the broader local-baseline definition. NETWORK-GUARD is the CI-blocking atomic slice; SUITE-HEALTH stays as the umbrella. Annotated cross-ref in both task notes.
  - WIP: in_progress = 0 → all 3 new dispatchable FIX tasks fit under cap. No throttle needed.
  - ESCALATION: recurring-bug rule (2+ commits same module → architect) does NOT fire. This is the FIRST fresh-code run. FIX-CI-LINT-STACK was the only prior CI commit (different defect: action version + ts-lint deletion). The TA golangci config miss is a LEFTOVER of that same fix, not a re-occurrence on a re-fixed module. No architect.
- **why-decision:** Atomic, single-zone, owner-clear fixes; no design needed; WIP-safe; no recurring-bug trigger.
- **why-change:** no change from plan.

### STEP po-S4: Triage tick 20260608T19xxZ — board hygiene merge + status reconcile + DEPGUARD activate (2026-06-08)
**task_id:** CI-RED-RECONCILE (sprint-level) + triage_key task:po-triage-20260608
- **what-done:**
  1. **SSOT dup-key merge.** `.task_board.active_sprints` had TWO entries logically keyed CI-RED-RECONCILE (index 24 `id=CI-RED-RECONCILE`, 6 tasks, rich PO history; index 25 `sprint_id=CI-RED-RECONCILE`, 2 tasks, dev REVIEW notes). Per feedback_ssot_duplicate_key (last-key-wins masks writes), the dev-team's REVIEW status + fix descriptions for FIX-MCP-TOOL-COUNT-DRIFT and FIX-MCP-CI-NETWORK-GUARD were hidden behind entry-24's stale TODO. Merged: absorbed entry-25's 2 task descriptions into entry-24's matching tasks, flipped both TODO→REVIEW, then deleted entry 25. Result: 26→25 sprints, 1 CI-RED-RECONCILE entry, all 6 tasks present.
  2. **CI status reconcile (verification gate).** RAW-VERIFIED: origin/main HEAD = 8ffb1985; local only 3 commits ahead, all chore(memory)/chore(cowork) — NO code pushed. Therefore the 4 CI fix tasks (FIX-MCP-TOOL-COUNT-DRIFT, FIX-MCP-CI-NETWORK-GUARD, FIX-MACRO-GO-DIRECTIVE, FIX-TA-GOLANGCI-CONFIG-V2) are fixed-LOCAL-but-UNPUSHED → stay REVIEW. NOT prematurely REVIEW: code is genuinely done locally per their decision journals; the gate that's unmet is "GREEN ci.yml on a sha != 8ffb1985", which needs an ops push. NO DONE flip.
  3. **FIX-TA-SANDBOX-DEPGUARD activated.** go-lint job RED on 8ffb1985 = cmd/sandbox/main.go:44 imports pkg/infrastructure (Fence-C depguard). Task ALREADY existed (backlog, created 2026-06-07T21:25:56Z) — NOT a new task (router context said "no task" but dedup found it). Surfaced now because FIX-TA-GOLANGCI-CONFIG-V2 migrated TA config to v2, removing the v1 parse-error mask. Real tracked debt; our config fix is correct (it revealed the debt). Activated TODO→IN_PROGRESS, priority→high. This is the net-new dev work needed for go-lint green.
- **what-considered:**
  - Flip FIX-MCP-*/FIX-MACRO/FIX-TA-GOLANGCI → DONE now: REJECTED (verification gate hard — no green run on a new sha; local green ≠ DONE).
  - Create a fresh FIX-TA-SANDBOX-DEPGUARD: REJECTED — dedup found existing backlog[49]; activated in place instead.
- **why-decision:** Dup-key merge recovers masked dev REVIEW status (data-integrity); REVIEW tasks held at the verification gate; the only dispatchable CI-green blocker (depguard) activated under WIP.
- **why-change:** sprint went 6 tasks (de-duplicated) + 1 backlog task activated; no DONE flips this tick.
- **recurring-bug check:** TA go-lint module — FIX-TA-GOLANGCI-CONFIG-V2 (config v2) and FIX-TA-SANDBOX-DEPGUARD (import boundary) are DISTINCT defects in the same zone, not a re-fix of one module. The depguard violation is debt the v1 config masked, not a regression introduced by the config fix. Architect escalation does NOT fire.

### STEP po-S5: ci-health-fix-bridge brief → UNBLOCK (agent-father + developer) (2026-06-08)
**task_id:** CI-HEALTH-FIX-BRIDGE (new)
- **what-done:** Read brief docs/architecture-briefs/2026-06-08-ci-health-fix-bridge.md (READY-FOR-IMPLEMENTATION). Per triage-signals.md brief_complete routing (targets docs/agents/* flow + a canonical script) → UNBLOCK, route agent-father (4 md/flow edits: ci-health-probe.md SSOT spec, main.md JUMP-TO+Step-0a.5, drain-signals.md ci_red row, triage-signals.md ci_red handler) + developer (scripts/agents-flow/ci-health-probe.js canonical script, gated on the flow SSOT).
- **why-decision:** This institutionalizes the user's standing Message-4 request (auto-detect CI failures in the dev-team flow). The brief is self-grounding (this very CI-RED-RECONCILE sprint is the motivating incident) and encodes the 4 hard constraints we relied on this session (STALE-RUN GATE proven by the 9f063c9a stale-run trap, 3-layer DEDUP, VERIFICATION GATE = green-on-subsequent-push, SAFE-JSON = no field interpolation). Agent-father md track does not consume dev WIP; developer script = the 2nd dispatch slot.
- **WIP:** post-tick WIP = FIX-TA-SANDBOX-DEPGUARD (dev) + ci-health-probe.js (dev via agent-father chain) = 2, at cap. bctc-analyst recurring escalation held as pendingObservation (below).

### STEP po-S6: other signals — judged, not actioned (2026-06-08)
- **bctc-analyst escalation + bctc_signal BATCH_RELEASE (cycle 25+, 6 release tickers CTG/VCB/REE/NVL/D2D/TCH get_bctc_full EMPTY despite stored PDFs; ACB/EIB PUB-5-blocked):** RECURRING (>2 fix cycles → recurring-bug rule). Largely a RE-ESCALATION of known work: get_bctc_full-empty root-caused in BEQ-1 (DONE); CTG fetch in FIX-CTG-1/2/3 (DONE); historical depth in BCTC-HIST-VPS-BACKFILL + BCTC-ENRICHER-OLD-QUARTERS (both DEFERRED-INFRA, VPS-upstream-blocked). NEW nuance worth an architect SPIKE: current-quarter PDFs are NOW physically stored (CTG 6MB/VCB 8.1MB dated 2026-06-05/08) yet serve EMPTY — i.e. the freshly-stored current-Q PDFs are not being picked up by refine/extraction (distinct from the historical-backfill defer). HELD this tick (WIP at cap); recorded as pendingObservation → next-tick architect SPIKE (recurring-bug-escalation, NOT point-patch) to pin why stored-current-Q PDFs don't reach get_bctc_full.
- **bctc_signal FPT routine:** informational analysis (esc_1..5 all false / DATA-COV-LIM; trick=medium seasonal OCF). Not dev work → skip.
- **context-bloat × 3 (dev-macro-indicators 172916Z, dev-mcp-server 173510Z+180055Z notebooks):** route per context-bloat-governance — notebook trim to ≤200L (janitor/agent-father op). CHORE/CLEAN, no dev WIP consumed.
- **cowork-team-20260608T180631Z:** cowork tick → informational, skip.
- **sau-c118-cron-crash (CRITICAL, vnstockFundamentalsRefresh 0%/1 run, zone dev-mcp-server):** signal_queue row → TRIAGED. JUDGMENT: TRANSIENT, no task. Single observation; FIX-MACRO-REFRESH-DEAD (b7ce338f) shipped this session may have perturbed the worker. WATCH: a 2nd crash next auditor tick → escalate FIX (recurring).

### STEP po-S7: Triage tick 20260608T19xxZ-b — JOB1 signal triage + JOB2 CI board reconcile (raw-over-badge correction) (2026-06-08)
**task_id:** CI-RED-RECONCILE (sprint-level) + signal_queue rows a33/b12/b13

**JOB2 — CI board reconcile. Router handed 4 status flips; I executed 2 verbatim and CORRECTED 2 against raw git (verify-raw-not-badges; no over-claim; never destroy verified work — feedback_auditor_false_positive_destructive).**

- **FIX-TA-SANDBOX-DEPGUARD → DONE-CODE-LOCAL-GREEN-PENDING-PUSH (router verbatim, raw-confirmed).** commit c2faac2d real + ancestor-of-HEAD; local sandboxCalculator adapter removes pkg/infrastructure import from cmd/sandbox; golangci-lint exit 0/"0 issues" per commit body. `git merge-base --is-ancestor c2faac2d origin/main` = NO → UNPUSHED. This is precisely why go-lint stays RED on origin/main 8ffb1985. Final DONE waits on push + green ci.yml (router owns push). Status matches router.
- **CI-HEALTH-FIX-BRIDGE → DONE (router verbatim, raw-confirmed).** Task did NOT exist on the board (created here as a `done` entry). Evidence both commits real: flow SSOT 973a52c0 (ci-health-probe.md + main.md Step-0a.5 + drain-signals ci_red row + triage-signals ci_red handler) + canonical script e7b53e20 (scripts/agents-flow/ci-health-probe.js, 251L). Wired into dev-team Step 0a.5. QA-verified by router. UNBLOCK chain from po-S5 shipped.
- **FIX-MCP-TOOL-COUNT-DRIFT → DONE (CORRECTED — router said reopen→TODO; raw git says DONE).** Router premise "NO mcp-server code change anywhere, fix does not exist" is FALSE. The router checked path `apps/mcp-server/test/123-integration-mcp.test.ts` (does not exist); the real file is `apps/mcp-server/src/__tests__/123-integration-mcp.test.ts`. RAW: commit 964f6e2e (ancestor-of-HEAD AND ancestor-of-origin/main 8ffb1985 → PUSHED) lowers `toBeGreaterThanOrEqual(16)→(15)` + adds `not.toContain("read_bctc_pdf")` (deregistered TSU-DEV-U3). On origin/main run 27157108271 (head 8ffb1985): `(pass) Cross-chain structural checks > all registered tools are present on the server`. The task's specific assertion PASSES. Reopening to TODO would destroy verified-correct, already-pushed work — REFUSED. Marked DONE.
- **FIX-MCP-CI-NETWORK-GUARD → DONE (CORRECTED — same false premise).** RAW: commits f18a9de9 + 0874a27f (both PUSHED, ancestors of 8ffb1985): 1146 hardcoded-date→relative daysAgo(); 1335 fixture schema sync + 15s timeout. On run 27157108271 all `Task 1146` assertions `(pass)`. Specific scope PASSES. Marked DONE.
- **NEW: CI-TEST-ISOLATION-SPIKE → TODO (architect SPIKE, the router's HONEST residual, correctly scoped).** Router was RIGHT that bun-test is still RED on origin/main — but NOT due to the 2 tasks above. Per-job raw of run 27157108271 (head 8ffb1985): `bun test` FAIL + `go-lint` FAIL (5 Go-lints + py-lint PASS). The bun-test residual is a SYSTEMIC class distinct from the 2 closed tasks: Task 278 peer-sync (timer 5000ms), Task 1487 Yahoo-Extended (external null), Task 1335 ENOENT mkdir __test-1335__ (fs/test-isolation), HOSE/HNX/UPCOM AbortError timeouts. ~systemic network/timer/DB-integration isolation failures, scope exceeds point-fixes → architect SPIKE on CI test-isolation strategy (not a dev point-patch). go-lint residual = FIX-TA-SANDBOX-DEPGUARD unpushed (above), NOT new.

**JOB1 — signal triage (3 NEW rows):**
- **sau-2026-06-08T18:36:27Z-a33 (A-33, vnstockFundamentalsRefresh CRASH, CRITICAL) → NEW TASK: FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE (architect, TODO, zone dev-mcp-server).** Recurring-bug rule FIRES: 3 crashes/24h (01:00, 18:07, 18:36 UTC) across c118/c119/c120. The prior tick (po-S6, sau-c118) explicitly set WATCH: "2nd crash next auditor tick → escalate FIX (recurring)" — that condition has now fired. Per recurring-bug-escalation: NOT a point-patch → architect root-cause SPIKE first (cron registry zone dev-mcp-server, fix path architect-first). Dedup: no existing open task matches vnstock/A-33/FundamentalsRefresh. Signal NEW→READ.
- **sau-c119-b12 (B-12, SBV_FX stale 65min, VPS vn-sbv-fetch unhealthy, CRITICAL) → NEW TASK: FIX-SBV-FX-VPS-FETCHER-UNHEALTHY (TODO, zone dev-macro-indicators).** Dedup: FIX-MACRO-REFRESH-DEAD / FIX-SBV-REFRESH-SILENT-SWALLOW / FIX-SBV-PUSH-TYPE-COERCE / FIX-SLA-EXEMPT-NEWS-SBVFX are all DONE — they fixed the MCP-side refresh code paths (silent-swallow, env-var, type-coerce, SLA exemption). The signal's NEW dimension is the VPS fetcher (`vn-sbv-fetch`) itself reported UNHEALTHY → upstream infra, not the already-fixed MCP code. Does NOT fold into a DONE task (different layer). Recurring (c112+c119). Held under WIP (see below). Signal NEW→READ.
- **sau-c119-b13 (B-13, 26 stale pending BCTC >72h, WARN) → FOLDED, no task. Signal NEW→RESOLVED.** Confirmed same known non-actionable rows: commit 157c0f40 (FIX-BCTC-VPS-QUEUE-STALE-TRIAGE, DONE) classified the 26 Q1-2026 rows as `blocked_pdf_extractor` (gated on A-20). These are the prior-sprint S3/S5 rows. No duplicate task. NOTE: A-20 architect fixes are now DONE (ARCH-A20-CPU-CGROUP-REVIEW, A20-EVENTLOOP-*) → the 26 rows may be re-queueable; that re-queue is the prior sprint's own follow-up ("re-queue after fix"), not a new triage task — left to the BCTC pull cron, recorded as pendingObservation.

**WIP cap ≤ 2 (actual dispatch):** Architect SPIKEs (A-33 + CI-TEST-ISOLATION) do not consume dev WIP. FIX-SBV-FX-VPS-FETCHER-UNHEALTHY = dev WIP. Current dev in_progress = FIX-TA-SANDBOX-DEPGUARD (1) + ci-health-probe chain landed. Net dispatchable this tick within cap. SBV-FX dev FIX queued TODO (depends on architect A-33/SBV diagnosis ordering); not over-dispatched.

**recurring-bug check:** A-33 vnstock crash FIRES (3×/24h) → architect SPIKE (correct). SBV B-12 recurring (c112+c119) but the prior fixes were MCP-side; the new VPS-fetcher-unhealthy is a fresh root-cause layer → dev FIX (architect not required yet; escalate if the VPS-fetcher fix also recurs).

## VERIFICATION GATE
Each fix proves out ONLY on a GREEN ci.yml run after a subsequent push. Local green ≠ done. After dev-* lands all 3 fixes locally → ops pushes → read next ci.yml run → only then mark DONE. Local-only fixes do not count.
FIX-MCP-TOOL-COUNT-DRIFT + FIX-MCP-CI-NETWORK-GUARD: marked DONE because their specific assertions ALREADY PASS on the pushed origin/main run 27157108271 (head 8ffb1985) — gate met for their scope. The overall ci.yml red is owned by the separate CI-TEST-ISOLATION-SPIKE (systemic) + FIX-TA-SANDBOX-DEPGUARD (unpushed go-lint), not these two.

## References
- Fresh run: https://github.com/phamhung075/VN-Market-Intelligence-MCP/actions/runs/27153704278
- ops journal: docs/agent-memory/decisions/sprint-CI-RED-RECONCILE-ops.md
- macro go.mod: apps/macro-indicators/go.mod (`go 1.25.0`, outlier)
- TA config: apps/technical-analysis/.golangci.yml (missing `version: "2"`)
- drift assertion: apps/mcp-server/src/__tests__/123-integration-mcp.test.ts:867

### STEP po-GATE-1: DJ-GATE-1 verification-gate reconcile — FIX-TA-SANDBOX-DEPGUARD DONE (2026-06-08)
**task_id:** FIX-TA-SANDBOX-DEPGUARD | **gate:** ci_green_on_subsequent_push **=> MET**

- **Status flip:** DONE-CODE-LOCAL-GREEN-PENDING-PUSH -> **DONE**.
- **Gate evidence (raw, router-confirmed):** router pushed origin/main `8ffb1985 -> f2986485`. `git merge-base --is-ancestor c2faac2d origin/main` = YES (fix commit c2faac2d now PUSHED, ancestor of origin/main). Fresh `ci.yml` **run_id 27159569677** on **sha f2986485** COMPLETED. Per-job raw: **go-lint = success** (the previously-RED go-lint job — the sole owner of the go-lint failure — is now GREEN). All 6 other Go lints + py-lint = success.
- **Why gate is satisfied:** verification_gate required "GREEN on a SUBSEQUENT push on a NEW sha." f2986485 != 8ffb1985 (new sha, subsequent push) and go-lint=success on that sha. Local green was insufficient (po-S7); this is the live origin proof. Evidence commit = **c2faac2d** (local sandboxCalculator adapter removes pkg/infrastructure import from cmd/sandbox; golangci-lint exit 0).
- **bun test residual — NO new task, NO change (anti-duplicate):** overall ci.yml run 27159569677 conclusion = failure SOLELY because `bun test` = failure. This is the DISJOINT systemic class already scoped as **CI-TEST-ISOLATION-SPIKE** (TODO, owner agents-architect, created in f2986485 / po-S7). Confirmed FULLY covered: that SPIKE owns the network/timer/fs/DB-integration isolation cohort (Task 278/1487/1335 + HOSE/HNX/UPCOM AbortError timeouts). Residual remains correctly owned by that SPIKE; the now-live **ci-health-probe** (CI-HEALTH-FIX-BRIDGE, DONE) will re-detect ci.yml RED on the next dev-team tick (Step 0a.5) and route it. Creating a 2nd bun-test task here would be a duplicate -> REFUSED.
- **Push:** NOT performed by po. Router owns the verification-gate push (already done: f2986485). This STEP is a local board-reconcile + commit only.

### STEP po-RECONCILE: two-brief empirical reconcile -> CI-BUN-TEST-MULTI-CLASS-FIX (2026-06-08T21xxZ)
**Trigger:** router double-spawned architect on the SAME problem (bun-test 639 CI failures); both completed + committed two CONFLICTING briefs. PO must reconcile into ONE plan empirically, not by authority.

- **Brief #1** `docs/architecture-briefs/2026-06-08-ci-bun-test-mass-failure.md` (a697392e): THREE independent classes, NO single cascade. Class A ~80-150 (macroTools HTTP-proxy seam removed), Class B ~300-400 1ms (TDD-RED living-spec), Class C ~100-150 5000ms (network isolation). KEY: ZERO passing CI runs in all 200 runs of history (703->639 fails) = ACCUMULATED DEBT, not a triggered regression. Recommends rename -> CI-BUN-TEST-MULTI-CLASS-FIX.
- **Brief #2** `docs/architecture-briefs/2026-06-08-ci-test-isolation-spike.md` (f13a97bf): TWO systemic roots dominate ~560/639. B1 ~269 = 1862c mock.module(@modelcontextprotocol/sdk/server/mcp.js) at MODULE scope, process-scoped + never restored -> poisons every test dispatched AFTER 1862c in same run (order-dependent cascade). B2 ~96 = 63 test files inline `CREATE TABLE rag_analyses` DDL missing guarded `ADD COLUMN data_env` -> prod inserts data_env unconditionally -> SQLiteError. Buckets B~560 / A 24 obsolete safe-remove / C ~39 real. CONSTRAINT: DWF-is-trading-day AC-P0-3-6 is an INTENTIONAL anti-stub canary, must stay RED, NEVER touch.

- **CONFLICT:** #2 = mock.module process-scoped poison cascade (~269, order-dependent); #1 = NO cascade, 3 independent classes. Both partly plausible (#1 may have missed bun mock.module process-scoping; #2 B2-DDL == #1 env-mutation note — they AGREE there).
- **RESOLUTION (empirical, NOT authority):** the mock.module-poison claim is the highest-leverage FALSIFIABLE hypothesis. FIRST dispatched task = `B1-MOCK-MODULE-EXPERIMENT`: dev-mcp-server applies ONLY the 1862c mock.restore() wrapper, re-runs `bun test`, measures FAIL-count delta vs baseline 639. delta ~= -269 -> brief #2 cascade wins (becomes batch-1). small delta -> brief #1 3-class framing holds. One experiment settles the direction before committing the team.

- **Board action:** superseded `CI-TEST-ISOLATION-SPIKE` (idx 6) -> DONE (architect SPIKE delivered 2 briefs; architect lane consumes NO dev WIP). Added umbrella `CI-BUN-TEST-MULTI-CLASS-FIX` (EPIC) + 5 ordered batches: B1-MOCK-MODULE-EXPERIMENT (slot1, DISPATCHED) -> B2-RAG-DDL-INITNEWSTABLES (DISPATCHED) -> CI-NETWORK-SKIP-GUARDS (TODO, dep B1) -> OBSOLETE-REMOVE-24 (TODO, dep B1, user directive) -> CLASS-B-PERTEST-TRIAGE (BLOCKED, needs PO prioritization after B1/B2). All zone apps/mcp-server (obsolete-removal has interface/infrastructure edit sites per brief #2).
- **User directive 'remove all obsolete failing tests':** scoped to OBSOLETE-REMOVE-24 = ONLY brief #2 Bucket A 24 evidence-backed tests (lines 152-249), each removal cites evidence commit hash. HARD: no blanket-delete; PROTECT DWF-is-trading-day AC-P0-3-6 canary.
- **REALISTIC GOAL surfaced to user (WORK + notebook):** CI has NEVER been green in 200 runs = accumulated debt; target is batch-wise bun-test FAIL-count REDUCTION (monotonic per-push drop), NOT instant full-green.
- **WIP<=2 dispatch (BGFAN-1, dispatcher mutex):** B1-MOCK-MODULE-EXPERIMENT + B2-RAG-DDL-INITNEWSTABLES (independent file sets, safe parallel). Both background dev-mcp-server.
- **Other drained signals triaged:** ci-test-isolation-spike (this brief, handled); context-bloat architect.md (215L > 200 soft cap — architect's OWN notebook, self-resolve via NB-PRUNE on next architect cycle, not PO-owned, no task); cowork-team tick (informational, no-op); sau-c121-a33 vnstock crash NEW -> FOLD into existing FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE (backlog, no dup).
- **Push:** NOT performed by PO. Router owns push + CI verification (gate = bun-test FAIL COUNT must DROP on subsequent push, full-green not yet reachable).

---

### STEP po-S8 (DJ-GATE-1): coverage-OOM root cause INVALIDATES the 639 phantom -> insert prereq FIX-CI-COVERAGE-OOM-CRASH (2026-06-08T22:10Z)
**task-id:** FIX-CI-COVERAGE-OOM-CRASH (new); B1-MOCK-MODULE-EXPERIMENT (re-scoped); CI-BUN-TEST-MULTI-CLASS-FIX (epic note)
**what-done:** Router raw-verified a corrected root cause that invalidates the shared premise of BOTH architect briefs. Authored the epic's slot-0 prerequisite + re-scoped B1 + annotated the epic.
**corrected-root-cause (raw, verify-raw-not-badges satisfied):** `apps/mcp-server/bunfig.toml` has `[test] coverage=true`; `.github/workflows/ci.yml` line 45 gating step runs bare `bun test` (inherits coverage=true). Raw CI run 27166197809 / job 80193895992 (HEAD 8fd9bde1): bun-test step's LAST log lines are the COVERAGE TABLE truncated mid-line at `taOhlcvBackfillJob.` then immediately `##[error]exit 1`. NO `Ran N tests / N pass / N fail` summary anywhere -> the process CRASHES (OOM / C++ panic) DURING coverage-table generation, BEFORE emitting any summary. Corroborated: B1 agent saw two coverage-ON runs crash (`panic: A C++ exception occurred` at OOM) before summary; only coverage-OFF produced a clean count = **443 fail**.
**what-considered:**
- Trust the 639 and scope downstream fixes against it -> REJECTED: 639 is a PHANTOM; the crashing run never emitted it cleanly. Every fix scoped to it is unmeasurable.
- Disable coverage globally in bunfig.toml -> REJECTED: would lose local-dev coverage + any separate non-gating coverage job. Over-broad.
- Surgical: change ci.yml line 45 ONLY to `bun test --coverage=false`, keep bunfig coverage=true -> CHOSEN.
**why-decision:** This is the PREREQUISITE that makes EVERY subsequent count trustworthy. Without a gating run that runs-to-completion and prints a clean `N fail` + a real exit code (test failures, not a crash), no failure count — and no per-batch delta — is measurable. It is the first measurable baseline in 200 RED runs.
**verification-gate:** after this lands (router owns push), the CI bun-test job MUST emit a clean `Ran N tests ... N fail` summary on the subsequent push and exit on real test failures, NOT a coverage-gen crash. Job MAY stay RED on genuine failures — EXPECTED, PASSES the gate. Gate = "clean summary emitted / no coverage-gen crash", NOT "0 fail".
**B1 re-scope:** DISPATCHED -> BLOCKED on FIX-CI-COVERAGE-OOM-CRASH. Its measured delta=0 is INCONCLUSIVE (suite never completed under coverage), NOT a refutation of the cascade hypothesis. mock.restore() wrapper already shipped in 8fd9bde1 (KEEP as hygiene). Re-run with `--coverage=false` AFTER prereq lands, delta measured vs REAL baseline 443, not phantom 639.
**downstream:** B2 / CI-NETWORK-SKIP-GUARDS / OBSOLETE-REMOVE-24 / CLASS-B-PERTEST-TRIAGE all re-baseline against the real CI count once visible — recorded in the epic note.
**why-change:** Differs from STEP po-RECONCILE which trusted 639 as the baseline. New raw evidence proves 639 was never cleanly produced; the empirical experiment B1 was measuring under a crashing toolchain.
**push:** NOT performed by PO. Router owns push + CI verification.
**dispatch:** PO did NOT spawn dev — router (dev-team dispatcher) owns the run_in_background spawn of dev-mcp-server for FIX-CI-COVERAGE-OOM-CRASH.

### STEP po-S9 (DJ-GATE-1): FIX-CI-COVERAGE-OOM-CRASH attempt #1 (c2ab2cea) FAILED — re-scope with VERIFIED bun-1.3.13 mechanism (2026-06-08T21:20Z)
**task-id:** FIX-CI-COVERAGE-OOM-CRASH (re-scope, 2nd fix attempt)
**what-done:** Router raw-verified the shipped fix is BROKEN; PO empirically verified the correct override mechanism against the EXACT CI bun version before re-scoping. NO unverified flag this time.
**raw-failure (CI run 27167284935, HEAD c2ab2cea, job "bun test"):** `bun test --coverage=false` -> `error: The argument '--coverage' does not take a value.` Job conclusion=failure at ARG-PARSE; suite NEVER ran. The prior step (po-S8) specified `--coverage=false` on an UNVERIFIED assumption that bun has a coverage value-flag — FALSE. bun `--coverage` is enable-only boolean.
**VERIFICATION DONE (the gate the last attempt skipped):** bun 1.3.13 is the pinned CI version (`.tool-versions`) AND is installed locally — exact match. Probed the real binary with a real source+test fixture. **Matrix (coverage-table emitted?):** bunfig `coverage=true`: bare `bun test`=ON, `bun test --coverage`=ON. bunfig `coverage=false`: bare=OFF, `bun test --coverage`=**OFF (flag ignored in 1.3.13!)**. bunfig absent: `--coverage`=ON. Also: `--coverage=false`=hard parse error; `-c ci-bunfig.toml` / `BUN_CONFIG_FILE` do NOT override `[test] coverage` when a local bunfig.toml exists (table still printed) -> Approach-2 separate-CI-bunfig is NOT viable. Failing-test exit code = non-zero under coverage=false (propagation OK).
**what-considered:**
- A1 set bunfig `coverage=false`, local devs opt-in via `bun test --coverage` -> CAVEAT FOUND: in 1.3.13 `--coverage` does NOT re-enable when bunfig says false; local opt-in story is partly broken. Still the only mechanism that reliably gives CI a no-coverage run.
- A2 separate CI bunfig via -c / env -> REJECTED: verified does NOT override the test-section coverage key; would be a 2nd unverified-flag mistake.
- A3 fix the underlying coverage-table OOM -> deferred unless disabling rejected; harder, off critical path.
**why-decision:** Route to architect SPIKE FIRST (recurring-bug rule: 2nd failed CI fix on the same ci.yml test step = 2+ commits same module). Architect picks the mechanism + handles the A1 local-coverage tradeoff (e.g. keep coverage local via a separate package.json script `test:cov` that calls `bun test --coverage` with bunfig coverage left absent/false, OR accept CI-bare = no coverage and document the local recipe). PO supplies the VERIFIED matrix so the spike is a 30-min confirmation, not open-ended. Hard gate carried into the spec: re-fix MUST be proven against bun 1.3.13 (`bun --version` + dry-run of exact CI command) BEFORE treated as fixed.
**verification-gate (unchanged):** CI bun-test job emits clean `Ran N tests ... N fail` summary on subsequent push (no coverage-gen crash, no arg-parse error). MAY stay RED on genuine failures.
**push:** NOT performed by PO. Router HOLDS the local drain commit until corrected ci.yml lands -> one push, one clean run.

### STEP po-S10: F-SUNDAY-SCHEDULER-FIRE (TNB c91 CRITICAL) REJECTED — false positive, calendar error (2026-06-08T21:20Z)
**task-id:** (ambient triage — TNB audit-handoff)
**what-done:** Raw-verified TNB's CRITICAL "weekday-only chef slots fired on Sunday" finding before any dispatch. Rejected as false positive; no dev task created.
**raw-evidence:** `2026-06-08` is a **MONDAY** (`date -u`: Lun; Sat=06-06 Sun=06-07 Mon=06-08). `new Date(Date.UTC(2026,5,8)).getUTCDay()===1`. Tested the cron-match SSOT `scripts/agents-flow/cowork-match-slots.js`: `cronMatches("13 2-8 * * 1-5", Sunday)===false` and `===false` for Monday-05:13 nominal — `dowMatch()` honors `1-5` correctly. Chef slots firing on a Monday is CORRECT (VN market open). Stale prices were Friday-close because audit ran pre-open Monday, not a scheduler defect.
**what-considered:**
- Create CRITICAL dev FIX for dispatcher day-of-week filter (TNB's ask) -> REJECTED: the code is correct, the audit premise (08=Sunday) is wrong. Dispatching here = auditor-false-positive destructive action (memory: auditor_false_positive_destructive).
- only: reject + route calendar correction to TNB c92.
**why-decision:** verify-raw-not-badges + no-FIX-on-non-bug. The dish-text "VN market OPEN (Sunday)" is a unified-agent day-LABEL error (LOW), folds into existing SPIKE-UNIFIED-NB-GAP, not a scheduler bug.
**why-change:** TNB findings normally become tasks (high severity); this one fails the raw-evidence gate, so the standard "high->task" rule is overridden by the false-positive guard.

### STEP po-S11 (DJ-GATE-1): CI-RED-RECONCILE milestone — coverage-OOM RESOLVED, sprint RE-BASELINED to REAL 702 (2026-06-08T21:52Z)
**task-id:** FIX-CI-COVERAGE-OOM-CRASH
**what-done:** Router-handoff said the CI gating job ran the FULL suite to completion for the first time in 200+ runs. PO verify-raw-not-badges before flipping anything: pulled CI run 27168638852 raw log + diffed shipped mechanism against HEAD f05795c3. CONFIRMED. Flipped FIX-CI-COVERAGE-OOM-CRASH BLOCKED->DONE. RE-BASELINED sprint to the REAL count. Dispatched next fix batches re-validated against the real failing-test signature breakdown.
**raw-evidence (CI run 27168638852, HEAD f05795c3, conclusion=failure):**
- Clean summary line emitted (FIRST in 200+ runs): `10742 pass / 29 skip / 656 fail / 46 errors` / `Ran 11427 tests across 1039 files. [343.72s]`. No coverage table, no C++ panic, no arg-parse error, no OOM-during-coverage-gen. Job RED on REAL test failures — EXPECTED, PASSES the gate ("clean summary emitted", NOT "0 fail").
- Mechanism shipped at HEAD: `git show HEAD:apps/mcp-server/bunfig.toml` -> `coverage = false` (line 26); `git show HEAD:.github/workflows/ci.yml` -> bare `run: bun test` (line 49) + summary grep (line 56); `scripts/test-coverage.sh` present at HEAD. Matches arch-S2 (DJ-GATE-1) dry-run-proof on bun 1.3.13.
- Architect DJ exists: `docs/agent-memory/decisions/sprint-CI-RED-RECONCILE-architect.md` STEP arch-S2 (DJ-GATE-1) with dry-run-proof. Brief `docs/architecture-briefs/2026-06-08-ci-coverage-off-mechanism.md` present.
**re-baseline (the actual point of this milestone):** REAL CI failure count = **656 fail + 46 errors = 702**. The **639 phantom** (cited in BOTH architect briefs a697392e + f13a97bf) is DISCARDED — those briefs sized against a crashing run that never emitted a clean count. The 443 coverage-OFF *local* number is also superseded by the authoritative CI number 702. Goal (driven by `/goal ci/cd pass`) = 0 fail.
**signature breakdown (raw `gh run view 27168638852 --log` grep — re-validated each bucket BEFORE sizing):**
- DDL-sync (B2, now BIGGER than scoped): `rag_analyses`/`data_env` = 93 hits; PLUS `daily_ohlcv has no column named data_env` (5), `no such column: statement_section` (3), `no such table: cron_job_runs` (2), `signal_quality_audit` (1), `notified_telegram` (1). B2 widened from rag-only -> SSOT-init-helper sweep across ALL inline DDLs missing the guarded ADD COLUMN.
- Network (dominant cohort): AbortError/fetch-failed/ETIMEDOUT/getaddrinfo/5000ms = 175 hits (HOSE/HNX/UPCOM/Yahoo/SBV/NewsAPI). Confirms CI-NETWORK-SKIP-GUARDS as the largest single fix lever.
- fs-isolation: ENOENT mkdir `data/__test-1335__` (test-sandbox dir not created in CI). Folds into network/CI-env guard batch.
- mock.module (B1 cascade hypothesis): only **17** hits for mock.module/mock.restore/@modelcontextprotocol — the ~269 cascade claim looks WEAK now the suite is measurable. mock.restore() wrapper already shipped in 8fd9bde1 (KEEP). B1 re-run de-prioritized: the data already refutes the large-cascade hypothesis; re-run is cheap confirmation, not a blocker.
- Top failing groups by count (E2E/integration, mostly network+DDL downstream): Task125 E2E-briefing 78, Task1159 morning-briefing 31, Task105 evening-summary 28, 1844a backtest-runs 28, 1423e macro-calendar 26, 1300b agent-memory 26.
**what-considered:**
- (a) Flip DONE off the green/conclusion badge — REJECTED (verify-raw-not-badges; conclusion is *failure*, and that is CORRECT here — the gate is the clean summary line, not a green job).
- (b) Re-baseline to 639 or 443 — REJECTED; both are pre-clean-run artifacts. Authoritative number is the CI clean summary = 702.
- (c) Re-run B1 mock experiment first — DEFERRED to low priority: 17 mock hits already refute the ~269 cascade; spend WIP on the validated high-leverage buckets (network 175, DDL 93+) instead.
- (d) CHOSEN: flip FIX-CI-COVERAGE-OOM-CRASH DONE; re-baseline EPIC to 702; dispatch B2 (DDL, widened) + CI-NETWORK-SKIP-GUARDS (unblocked — its B1 dep is moot) as the two highest-leverage batches under WIP<=2; keep OBSOLETE-REMOVE-24 + CLASS-B-PERTEST-TRIAGE sequenced after; PROTECT DWF-is-trading-day AC-P0-3-6 canary (intentional RED, NEVER touch).
**why-decision:** The coverage-OFF mechanism delivered the FIRST measurable baseline in 200 runs (arch-S2 hard-gate satisfied: real dry-run on pinned bun 1.3.13). With a real count, the two systemic buckets that the raw signatures actually support (network 175, DDL 93+) are the dispatch priority; the phantom-639-driven cascade framing is empirically dead. no-unverified-flags + recurring-bug rule both honored upstream by the architect spike.
**why-change:** B2 scope WIDENED beyond rag_analyses to a generic SSOT-init-helper sweep (daily_ohlcv + statement_section + missing tables share the same root: hand-rolled inline DDL drifting from schema-*.ts). CI-NETWORK-SKIP-GUARDS un-gated from B1 (its B1-experiment dependency is moot — B1 is no longer a blocking conflict-settler).
**verification-gate (carried to every dispatched batch):** bun-test FAIL+ERROR count must DROP on the subsequent push vs the 702 baseline (router owns push). Any CLI/config mechanism in a fix spec needs a real dry-run on pinned bun 1.3.13 before it enters the spec (no-unverified-flags, still in force).

### STEP po-S12 (DJ-GATE-1): 9454baad VERIFICATION GATE FAILED (+219) — REVERT-then-FIX-FORWARD ruling (2026-06-08T22:52Z)
**task-id:** B2-RAG-DDL-INITNEWSTABLES + CI-NETWORK-SKIP-GUARDS (verdicts); CI-TEST-SCHEMA-FIXTURE-SPIKE (new); CI-NETWORK-GUARDS-POLLNEWS-REFILE (new)
**trigger:** Out-of-band PRIORITY ci_red signal `docs/signals/ci-verification-gate-failed-9454baad-20260608T225200Z.json`. Router raw-verified both counts (baseline + after) from `gh run --log`; PO did NOT re-measure CI (router owns the numbers, PO owns the path decision per signal `decision_owner`).
**raw-evidence (router, accepted):** CI run 27171666087 / HEAD 9454baad = 10591 pass / 906 fail / 15 errors = **921 fail+error** vs baseline run 27168638852 / f05795c3 = **702**. NET **+219 WORSE** -> verification gate (fail+error must DROP vs 702) FAILED. Decomposition: BATCH-2 (pollNews.ts network guards) errors 46->15 (-31) = GENUINE WIN, isolated to pollNews.ts, independently verified 102+1288=14 pass/0 fail under CI=true. BATCH-1 (mechanized init*Tables injection into 464 test setup fns) = **+250 fails**: ~32 double-create "table X already exists", ~200 schema-divergence "no such column/table"/count-mismatch, ~32 NOT NULL (watchlist.exchange, agent_signals.expires_at). ROOT: one-canonical-schema injection collides with heterogeneous per-file inline DDLs.
**what-considered:**
- (a) Keep-and-fix-forward (leave 9454baad on main, patch the +250 in place): REJECTED. main on dev-mcp-server is a poisoned baseline right now (+219); every downstream batch would measure its delta against a regressed number, and the fix-forward IS the recurring mechanized-sweep that just failed. Leaving red-worse on main violates the monotonic-drop gate the whole sprint is built on.
- (b) Full revert of 9454baad (revert everything): REJECTED on the BATCH-2 dimension only. BATCH-2 is an isolated, independently-verified -31 errors win in pollNews.ts; a full revert throws away verified-correct work (never destroy verified work — feedback_auditor_false_positive_destructive).
- (c) CHOSEN — REVERT-then-FIX-FORWARD (partial): (1) REVERT 9454baad to restore known-good 702 NOW [router owns git revert+push]; (2) RE-FILE BATCH-2 pollNews.ts guards as a clean standalone FIX so the -31 win is preserved; (3) route the BATCH-1 schema-drift to ARCHITECT as a recurring-bug SPIKE for a shared canonical test-DB fixture / inline-DDL reconciliation — NOT another mechanized one-size injection.
**why-decision:** Restoring 702 is non-negotiable and time-critical — a red-worse main poisons the measurable baseline that took 200+ runs to obtain (po-S11). The -31 BATCH-2 win is real and isolated, so it must survive the revert via re-file. BATCH-1 is the **2nd mechanized churn of the CI-test-schema area** (1st = the init-helper sweep widened in po-S11/B2; ties to architect CI-TEST-ISOLATION-SPIKE) -> recurring-bug rule (2+ same module -> architect SPIKE BEFORE dev impl) FIRES HARD: no third mechanized sweep; the design must be a shared fixture / DDL reconciliation, decided by architect first.
**board action:** B2-RAG-DDL-INITNEWSTABLES DISPATCHED->REVERTED-RECURRING (its mechanized approach is the regression; superseded by the architect SPIKE, NOT re-dispatched as-is). CI-NETWORK-SKIP-GUARDS DISPATCHED->PARTIAL-WIN-REFILE (pollNews.ts part is the verified -31; re-filed clean as CI-NETWORK-GUARDS-POLLNEWS-REFILE). Added CI-TEST-SCHEMA-FIXTURE-SPIKE (agents-architect, TODO, recurring-bug, architect lane = NO dev WIP) + CI-NETWORK-GUARDS-POLLNEWS-REFILE (dev-mcp-server, TODO, depends revert). EPIC re-baseline note: target stays 702-then-monotonic-drop; the +219 excursion is reverted, not absorbed.
**WIP<=2:** architect SPIKE consumes no dev WIP. Dispatchable dev = CI-NETWORK-GUARDS-POLLNEWS-REFILE (1), gated on router's revert landing first (else it re-files onto a poisoned base). Within cap.
**DJ-GATE-1:** no DONE flip this STEP (both batch tasks move to non-terminal recurring/refile states; revert + green-on-subsequent-push owned by router).
**push/revert:** NOT performed by PO. Router owns the git revert of 9454baad + push + re-read of the subsequent ci.yml run. PO reports the ruling + dispatched tasks back to router for execution.
**recurring-bug check:** FIRES for the BATCH-1 CI-test-schema injection (2nd mechanized churn, same area as the init-helper sweep + CI-TEST-ISOLATION-SPIKE) -> architect SPIKE mandatory before any dev re-attempt. Does NOT fire for BATCH-2 pollNews.ts (isolated win, re-file is mechanical not a re-design).
**secondary signal (recurring-bug-auditor-weekday-mislabel-20260608T223700Z.json):** routed to agent-father, NOT actioned by PO (auditor flow .md edit is agent-father's zone; PO never edits agent .md). See po-S13.

### STEP po-S13: secondary signal — auditor weekday-mislabel -> agent-father (2026-06-08T22:52Z)
**task-id:** (ambient triage)
**what-done:** Triaged `docs/signals/recurring-bug-auditor-weekday-mislabel-20260608T223700Z.json` (to:agent-father, recurrence_count:3). Confirmed it is an agent-flow .md fix (compute weekday from `date +%A`, never model-named), NOT a PO/dev task. Routed to agent-father unchanged; PO does not edit agent .md (init.md forbidden_outputs + agent-md-factory rule). recurring-bug (3x) but the owner is agent-father (flow author), not architect-then-dev — it is a flow-authoring fix, not a code root-cause spike. Mirrors the tran-ngoc-bau F-SUNDAY false-CRITICAL class (kin po-S10). Impact cosmetic so far (date-agnostic checks), so MEDIUM not CRITICAL. No dev WIP. Verification gate (next 2 auditor runs print weekday matching `date +%A`) carried to agent-father.

### STEP po-S14 (DJ-GATE-1): out-of-band triage — 2 REVIEW->DONE flips + SPIKE decompose (2026-06-08T23:14Z)
**task-id:** CI-NETWORK-GUARDS-POLLNEWS-REFILE, CI-TEST-SCHEMA-FIXTURE-SPIKE
**what-done:** Flipped CI-NETWORK-GUARDS-POLLNEWS-REFILE REVIEW->DONE (router CI-measured run 27172540201 vs revert base a42d0835 = +259 passes, identical fail/error markers, ZERO regression; preserves -31 errors win; commit 64981565). Flipped CI-TEST-SCHEMA-FIXTURE-SPIKE REVIEW->DONE (brief docs/architecture-briefs/2026-06-09-ci-test-schema-fixture-spike.md, brief_complete signal raw-verified by router). Decomposed brief into FIX-SCHEMA-DRIFT-P1 (data_env, Phase 1, ~93) + FIX-SCHEMA-DRIFT-P2 (Phase 2+3 IF-NOT-EXISTS + missing cols, ~104, depends P1); parked Phase 4 as FU-SCHEMA-DRIFT-P4 backlog.
**what-considered:**
- Fold all of Phase 1-4 into one FIX (rejected: P4 needs per-file isolation verification not a sweep; one big task re-creates the 9454baad one-size failure mode)
- Open P1+P2 in parallel (rejected: both touch the 64 rag_analyses files -> P2 depends:P1 to serialize and keep WIP<=2, avoid two devs racing same files)
- Re-open POLLNEWS-REFILE / mark partial (rejected: router raw-verified a clean WIN, +259/0-regression)
**why-decision:** Brief's two-fixture-contract is the design SSOT and the CURE for the regressing class; per-class targeted P1/P2 is the path to /goal ci/cd pass without repeating the reverted mechanized injection. P4 deferred because P1/P2 may absorb much of its residual once re-measured.
**why-change:** no change — follows brief §9 scoping + recurring-bug rule (architect SPIKE already delivered, so PM may route P1/P2 straight to dev).
**DJ-GATE-1:** TWO DONE flips this STEP (POLLNEWS-REFILE + SCHEMA-FIXTURE-SPIKE), both committed in the SAME commit as this journal entry.
**dispatch order:** P1 first (dep = now-DONE spike only), P2 second (after P1 lands, frees the rag_analyses cohort). Both owner dev-mcp-server, zone apps/mcp-server/, WIP<=2 honored (0 IN_PROGRESS now). No push (router owns).
**still-open (parked observations, file as dev WIP frees, <=2 WIP):** A-33 vnstockFundamentalsRefresh cron CRASH (sau-c121-a33 CRITICAL); BCTC get_bctc_full empty 6 tickers #3106; pollNews 0-items #3102.

### STEP po-S15 (DJ-GATE-1): rule FIX-SCHEMA-DRIFT-P1 REVIEW->DONE + release P2 (2026-06-08T23:40Z)
**task_id:** FIX-SCHEMA-DRIFT-P1, FIX-SCHEMA-DRIFT-P2
- **what-done:** Adjudicated CI-P1-GATE-892f3bc2 (signal docs/signals/ci-p1-gate-result-892f3bc2-20260608T233759Z.json). Flipped FIX-SCHEMA-DRIFT-P1 REVIEW->DONE. Marked FIX-SCHEMA-DRIFT-P2 dispatch-ready (dep on P1 now satisfied; status stays TODO = dispatch trigger).
- **what-considered:**
  - Path A — keep REVIEW / rework P1 because literal gate FAILED (fail+error 1117->1156, +39): REJECTED. The marker method is provably BLIND to the data_env class ('no such column: data_env'=0 in BOTH baseline 54e20c8e AND after 892f3bc2 — a bun setup/file-load error that emits no (fail) marker). The metric cannot score what P1 fixed.
  - Path B (CHOSEN) — DONE on the unmasking diagnosis: pass +30 AND fail +39 (total markers +69) with ZERO existing-pass regressions => ~69 tests that died early on the data_env INSERT now run further (30 pass, 39 hit the NEXT schema layer = exactly P2 scope). Reverted-B2 (9454baad) classes did NOT return.
  - Path C — block P2 / wait for a clean P1 gate first: REJECTED. P2 scope IS the now-dominant visible fail class; sequencing P2 next is the only path that drops net fail+error.
- **why-decision:** PO RAW-VERIFIED every load-bearing claim (did not relay router badges): code diff zone-confined apps/mcp-server/ (non-code = docs/memory only); pollNews.ts application/usecases try/catch data_env fallback guard present (mirrors fredApi.ts); 74 data_env additions across 63 .test.ts files, NO destructive DROP/DELETE/removed-CREATE (old CREATE replaced by data_env-augmented IF-NOT-EXISTS); pre-push tsc OK. The literal gate is the wrong instrument here, not the wrong verdict — a blind metric cannot veto a raw-verified zero-regression win.
- **why-change:** no change from router recommendation, but adopted only AFTER independent raw re-verification (router-verify-raw-not-badges).
- **DJ-GATE-1:** ONE DONE flip this STEP (FIX-SCHEMA-DRIFT-P1), committed in the SAME commit as this journal entry.
- **next-gate note:** Future schema-drift gates must read bun GITHUB_STEP_SUMMARY (run web/step-summary) for a 702-comparable ABSOLUTE — the API-log tab-filtered marker method is structurally blind to setup-error classes (data_env, the 46-vs-7 bun-errors gap).
- **parked observations (file as dev WIP frees, WIP<=2):** A-33 vnstockFundamentalsRefresh cron CRASH (sau-c121-a33 CRITICAL); BCTC get_bctc_full empty 6 tickers #3106; pollNews 0-items #3102; carried from po-S14 still open.

### STEP po-S16 (DJ-GATE-1): rule FIX-SCHEMA-DRIFT-P2 REVIEW->DONE + promote FU-SCHEMA-DRIFT-P4 BACKLOG->TODO (2026-06-09T00:16Z)
**task_id:** FIX-SCHEMA-DRIFT-P2, FU-SCHEMA-DRIFT-P4
- **what-done:** Adjudicated CI-P2-GATE-6c3b8701 (signal docs/signals/ci-p2-gate-result-6c3b8701-20260609T001321Z.json). Flipped FIX-SCHEMA-DRIFT-P2 REVIEW->DONE. Promoted FU-SCHEMA-DRIFT-P4 BACKLOG->TODO (active FIX, dispatch trigger) scoped to the residual no-such-table singleton classes.
- **what-considered:**
  - Path A — keep P2 REVIEW / rework because CI workflow conclusion stayed `failure` (residual fails remain): REJECTED. CI red is expected — the VERIFICATION GATE is fail+error must DROP vs the 702 native baseline, and it dropped to 634 (-68). A red CI badge on a still-imperfect suite cannot veto a raw-verified net-improvement.
  - Path B (CHOSEN) — DONE on the native-to-native gate: pass +139, fail+error 702->634 (-68), errors 46->14 (-32), +110 tests now execute (P1 unmasking + P2 coverage), regression classes (double-create / NOT NULL / data_env) all 0. Genuine regression-free win.
  - P4 timing: promote now (CHOSEN) vs leave parked. P2 closing frees the schema-drift active-chain WIP slot (P1 DONE, P2 DONE => 0 active in the chain); the residual classes are now the dominant block toward /goal ci/cd pass and are RE-MEASURED (634), so the "wait until P1/P2 absorb it" condition in P4's park-note is satisfied — the residual persists and is squarely Phase-4 territory.
- **why-decision:** PO RAW-VERIFIED every load-bearing claim (did NOT relay router badges): read after-P2 native summary myself from `gh run view 27175100853 --log` = `10881 pass / 620 fail / 14 errors / Ran 11537 tests`, and baseline run 27168638852 = `10742 pass / 656 fail / 46 errors / Ran 11427 tests` => fail+error 702->634 confirmed independently. Verified e3501171 code = 117 files zone-confined apps/mcp-server/src/__tests__ + 1 orch-state board flip, single status key, DJ-GATE-1 step inline. The residual no-such-table classes (agent_signals/sbv_rates_history/positions/commodity_prices*/imf_indicators/daily_ohlcv) hit the SHARED SINGLETON initDatabase() path — NOT Contract-B inline-DDL files P2 could patch — = exactly brief Phase 4 (§3 + RISK-3 §5 pure-singleton isolation audit).
- **why-change:** no change from router recommendation, adopted only AFTER independent raw re-verification (router-verify-raw-not-badges).
- **DJ-GATE-1:** ONE DONE flip (FIX-SCHEMA-DRIFT-P2) + ONE backlog->active promote (FU-SCHEMA-DRIFT-P4), both committed in the SAME commit as this journal entry.
- **measurement note:** native-to-native ONLY for absolutes going forward. The tab-filtered MARKER method over-counts ~2x (reported 1164 fail vs native 620) — do NOT use it for the next gate. Next gate: bun native `NNN pass / NNN fail / NNN errors / Ran N tests` block vs the 634 absolute; fail+error must DROP.
- **P4 scope (dispatch-ready):** FU-SCHEMA-DRIFT-P4 = brief Phase 4 — audit the pure-singleton tests that hit initDatabase() and reference macro/portfolio tables not created in their path; fix per two-fixture-contract (Contract A canonical init creates these tables for the test, OR isolate to Contract B with minimal inline DDL). Residual classes: agent_signals, sbv_rates_history, positions, commodity_prices, commodity_prices_history, imf_indicators, daily_ohlcv. owner dev-mcp-server, zone apps/mcp-server/, size M. DoD: native fail+error DROP vs 634, tsc clean, per-file isolation pass; no regression (double-create/NOT NULL/data_env stay 0). WIP<=2 honored (P2 closed => P4 sole active schema-drift FIX). No push (router owns).
- **parked observations (file as dev WIP frees, WIP<=2):** A-33 vnstockFundamentalsRefresh cron CRASH (sau-c121-a33 CRITICAL); BCTC get_bctc_full empty 6 tickers #3106; pollNews 0-items #3102; carried from po-S14/S15 still open.

### STEP po-S17 (DJ-GATE-1): FU-SCHEMA-DRIFT-P4 REVIEW->DONE + open FU-SCHEMA-DRIFT-P5 (ARCHITECT SPIKE) + triage C-09 c283 (2026-06-09T01:18Z)
**task_id:** FU-SCHEMA-DRIFT-P4, FU-SCHEMA-DRIFT-P5
- **what-done:** Adjudicated CI-P4-GATE-e442cf11 (docs/signals/ci-p4-gate-result-e442cf11-20260609T011520Z.json). Flipped FU-SCHEMA-DRIFT-P4 REVIEW->DONE. Opened FU-SCHEMA-DRIFT-P5 as a recurring-bug ARCHITECT SPIKE (singleton-pollution canonical-fixture design) — backlog/TODO, dispatch trigger, WIP<=2 honored (P4 close frees the schema-drift active slot). Triaged signal_queue NEW CRITICAL sau-c283-c09 (macro_indicators country-coverage C-09) -> TRIAGED, route to dev-mcp-server.
- **what-considered:**
  - P4 DONE vs rework: DONE (CHOSEN). Native-to-native gate PASSED: pass 10881->10886 (+5), fail+error 634->629 (-5 BETTER), errors flat 14, tests flat 11537. The -5 is exactly the 5 tests in 1972-vndirect-ohlcv-null-coercion.test.ts (daily_ohlcv) flipped to pass by P4's Contract-B inline-DDL fix (data_env + foreign-flow columns). Regression-free. CI workflow stays red (residual remains) but red badge cannot veto a raw-verified net-improvement — same gate logic as P1/P2.
  - P5 as another per-file/mechanized sweep vs ARCHITECT SPIKE: SPIKE (CHOSEN). This is the 3rd touch of the CI-test-schema surface (P2 Contract-B, P4 per-file isolation, now residual) = recurring-bug-escalation rule -> ARCHITECT designs before any dev impl. A mechanized init-injection sweep was already tried (9454baad) and REVERTED for +219 regressions — explicitly do NOT repeat. The dominant residual no-such-table classes (agent_signals 37 / sbv_rates_history 19 / positions 19 / commodity_prices_history 19 / commodity_prices 16 / imf_indicators 3) fail ONLY under full-suite cross-file singleton pollution, unreachable by any per-file Contract-B fix; they need Contract-A canonical initDatabase() to create the tables OR run-order isolation — exactly a design question.
  - C-09 self-clear vs real concern: NOT self-clear (CHOSEN route to dev). The recent FIX-MACRO-REFRESH-DEAD (b7ce338f, DONE 2026-06-08T15:25Z) fixed the US/FRED refresh job (silent-swallow + env-var) — and I LIVE-VERIFIED the serve layer holds (get_macro_snapshot 2026-06-09T01:17Z: carry/yield computedAt fresh, fedFundsRate=3.62, is_estimate=false). BUT sau-c283-c09 is a DISTINCT dimension — db_integrity_breach on macro_indicators country COVERAGE (only 1 country in 26h window, expects >=8), measured 2026-06-09T00:30Z (~9h AFTER the fix). Different table/dimension; prior fix did not target multi-country population. Honest disposition = route to dev-mcp-server to verify coverage, not auto-close.
- **why-decision:** PO raw-verified the P4 gate claim against the signal's native summary (10886 pass / 615 fail / 14 errors / Ran 11537 tests = fail+error 629 vs after-P2 634) and live-verified macro serve-layer myself via get_macro_snapshot (not badge). P4 win is genuine but small (-5) exactly as predicted at dispatch — the residual is full-suite singleton pollution, a design problem, hence P5 SPIKE to ARCHITECT.
- **why-change:** no change from router recommendation; adopted after independent raw + live verification (router-verify-raw-not-badges).
- **DJ-GATE-1:** ONE DONE flip (FU-SCHEMA-DRIFT-P4) + ONE new backlog entry (FU-SCHEMA-DRIFT-P5) + ONE signal_queue status flip (sau-c283-c09 NEW->TRIAGED), all committed in the SAME commit as this journal entry. Single status key verified per board edit; fresh-read before write; single-row atomic edits; .signal_queue.rows preserved (no clobber).
- **dispatch ask (router owns):** route FU-SCHEMA-DRIFT-P5 to ARCHITECT for SPIKE (timebox 120m) BEFORE any dev impl; route sau-c283-c09 macro_indicators coverage to dev-mcp-server for verify. Do NOT push P4 close — router owns push + next CI gate (native fail+error must DROP vs 629).

### STEP po-S18 (DJ-GATE-1): CI-P5-GATE-541123b4 FAILED+reverted — P5-SELFHEAL REVIEW->REWORK + open FU-SCHEMA-DRIFT-P6 (ARCHITECT SPIKE) + close C-09 false-positive (2026-06-09T02:16Z)
**task_id:** FIX-SCHEMA-DRIFT-P5-SELFHEAL, FU-SCHEMA-DRIFT-P6, sau-c283-c09
- **what-done:** Adjudicated CI-P5-GATE-541123b4 (signal docs/signals/ci-p5-gate-result-541123b4-20260609T0210Z.json). Flipped FIX-SCHEMA-DRIFT-P5-SELFHEAL REVIEW->REWORK (gate failed + router already reverted). Opened FU-SCHEMA-DRIFT-P6 (architect SPIKE, recurring-bug, 4th touch). Reconciled sau-c283-c09 TRIAGED->RESOLVED (auditor false-positive, already fixed in 5d323430). Did NOT re-measure CI — router owns the gate and reverted in d1aa19c5.
- **what-considered:**
  - P5-SELFHEAL keep REVIEW vs REWORK: REWORK (CHOSEN). Verification gate FAILED — native fail+error 629->635 (+6), router already REVERTED schema.ts (d1aa19c5, byte-identical to pre-P5, tsc clean). REVIEW would falsely imply awaiting sign-off on a live fix; the fix is off main. REWORK = correct (re-design needed, hypothesis disproven empirically).
  - P6 as per-file patch / mechanized sweep vs ARCHITECT SPIKE: SPIKE (CHOSEN). This is the 4th touch of the CI-test-schema surface (P2 Contract-B, P4 per-file isolation, FU-P5 singleton-fixture, now P5-SELFHEAL slice-DDL drift) -> recurring-bug-escalation -> architect designs before dev impl. The self-heal DIRECTION is right (healed 4 no-such-table classes) but the 9 standalone init-slice DDLs must be RECONCILED with canonical first: at least one slice's CREATE TABLE IF NOT EXISTS is column-drifted (missing created_at, x3) so when it wins the fresh-:memory: race, IF NOT EXISTS blocks canonical repair. SPIKE scope = audit each of the 9 slices' columns vs canonical initDatabase(), decide self-heal-with-reconciled-DDL vs run-order test isolation. Explicitly NOT another per-file inline-DDL patch; NOT a mechanized one-size injection (9454baad reverted +219).
  - C-09 leave TRIAGED (route-to-dev per po-S17) vs RESOLVED: RESOLVED (CHOSEN). RAW-VERIFIED commit 5d323430 (FIX-C09-SCHEMA-MISMATCH) already root-caused + fixed this as an AUDITOR FALSE-POSITIVE: a95c514a rewrote the probe count(DISTINCT indicator_key)->count(DISTINCT country) without updating the >=8 threshold; macro_indicators is UNIQUE(country) (only 1 'vietnam' row ever exists) so >=8 became permanently impossible. Probe re-rewritten to non-null-indicator-count, threshold >=8->>=3; live DB has 3 non-null indicators. The po-S17 "route to dev verify" disposition is superseded — there is no real coverage regression, no multi-country writer exists, no dev task. Closing as the auditor-false-positive it is (verify-raw-not-badges: read the fix commit body + diff, not a badge).
- **why-decision:** PO accepted router's CI numbers (router owns the gate + revert per signal contract; did NOT re-measure) but independently RAW-VERIFIED the C-09 disposition against commit 5d323430 before flipping it to RESOLVED. P5-SELFHEAL REWORK + P6 SPIKE keep the recurring-bug rule honored (architect designs the slice-DDL reconciliation; no third churn).
- **why-change:** C-09 disposition CHANGED vs po-S17 (TRIAGED->RESOLVED) — new evidence (5d323430 landed after po-S17) proves it was a false-positive, not a real coverage regression; the prior route-to-dev is moot. P5-SELFHEAL: hypothesis disproven empirically (partial heal, net +6 worse), so REWORK not DONE.
- **DJ-GATE-1:** ONE status flip (FIX-SCHEMA-DRIFT-P5-SELFHEAL REVIEW->REWORK) + ONE new backlog entry (FU-SCHEMA-DRIFT-P6 architect SPIKE) + ONE signal_queue status flip (sau-c283-c09 TRIAGED->RESOLVED), all committed in the SAME commit as this journal entry. Single status key verified per board edit (P5-SELFHEAL=1, P6=1, c09=1); fresh-read before write; single-row atomic jq edits with [ -s ] empty-guard; .signal_queue.rows preserved EXACTLY 52 (verified post-write, all unique).
- **dispatch ask (router owns):** route FU-SCHEMA-DRIFT-P6 to ARCHITECT for SPIKE (timebox 120m) BEFORE any dev re-impl; WIP<=2 honored (architect lane = no dev WIP, 0 IN_PROGRESS). No push — router owns push + next CI gate (native fail+error must DROP vs 629). C-09 needs no further routing (resolved false-positive).

### STEP po-S19 (DJ-GATE-1): CI-P6-GATE-507e3cee FAILED+reverted — FU-P6 REVIEW->DONE + open FU-P7 (5th-touch architect SPIKE) + raw-verify 2 auditor CRITICALs (2026-06-09T02:55Z)
**task_id:** FU-SCHEMA-DRIFT-P6, FU-SCHEMA-DRIFT-P7, FIX-NEWS-VPS-CRASH-LOOP, sau-20260609T023050, sau-20260609T023051
- **what-done:** Adjudicated CI-P6-GATE-507e3cee (router pre-measured+reverted). Flipped FU-SCHEMA-DRIFT-P6 REVIEW->DONE (spike sound, direction (b) disproven). Opened FU-SCHEMA-DRIFT-P7 architect SPIKE (5th touch, add-missing-tables-to-canonical — only untried root-cause lever). RAW-VERIFIED both deferred auditor CRITICALs myself via get_sla_status + get_vps_service_health; both REAL; folded sau-...3051 (bctc-discover) into FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE; opened FIX-NEWS-VPS-CRASH-LOOP for sau-...3050 (news-vps, 2nd occ/48h recurring). Did NOT re-measure CI (router owns gate+revert).
- **what-considered:**
  - FU-P6 DONE vs REWORK: DONE (CHOSEN). The SPIKE diagnosis was correct (single-process closeDb() afterAll nullifies _db singleton; production getDb() fallback hits empty :memory:). Only the chosen direction (b) afterAll-reinit was disproven empirically (native 629->630 +1 WORSE, ZERO residual classes healed, buckets byte-identical to after-P4). Same pattern as P5 (spike sound, fix-hypothesis wrong) -> spike STAYS DONE, not REWORK. No separate FIX-SCHEMA-DRIFT-P6-IMPL board task existed (router did direction-(b) impl directly, 507e3cee->reverted b9e305ae); recorded that in P5-SELFHEAL note rather than inventing a phantom IMPL task to flip.
  - P7 lever choice: add-missing-CREATE-TABLE-to-canonical (CHOSEN). All 4 prior levers exhausted (P5 prod getDb self-heal +6/created_at-drift; P6 afterAll-reinit +1/zero-heal; P4 per-file inline-DDL reaches only per-file isolation; 9454baad mechanized injection +219). Residual buckets byte-identical across P4/P5/P6 = the canonical initDatabase()/slices NEVER CREATE the dominant tables (agent_signals/sbv_rates_history/positions/commodity_prices*/imf_indicators). Additive IF NOT EXISTS DDL into canonical is the only untried lever targeting root cause + exactly what the P4 gate signal flagged. HARD CONSTRAINT in spike: source each DDL from its OWNING production module, not invent (invented DDL = what sank P5).
  - sau-...3051 (bctc-discover) new task vs fold: FOLD (CHOSEN). RAW: get_sla_status bctc 598min/120min breached CRITICAL = matches auditor ~9.5h; vn-bctc-fetch healthy but data stale = fetch up, discovery/write dead = vnstockFundamentalsRefresh crash. Already scoped in FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE (TODO, critical). Dedup, no new task.
  - sau-...3050 (news-vps) dismiss-transient vs open task: OPEN (CHOSEN). RAW: get_sla_status news 84/30min breached CRITICAL + get_vps_service_health vn-news-fetch UNHEALTHY uptime 1h21m. RECURRING — 2nd occ in 48h (2026-06-07T06:33 same vn-news-fetch unhealthy ~1h44m; FIX-NEWS-VPS-PROBE DONE but did NOT stick). ~1h uptime each = crash-restart loop (mirror of fundamentals crash). Recurring-bug rule fired -> definitive root-cause FIX, not another dismiss.
- **why-decision:** PO accepted router CI numbers (router owns gate+revert per signal contract) but INDEPENDENTLY raw-verified both auditor CRITICALs against live get_sla_status + get_vps_service_health (verify-raw-not-badges) and detected the news-vps recurrence by scanning the signal_queue history before deciding to open a task.
- **why-change:** news-vps disposition ESCALATED vs prior cycle (probe -> definitive crash-loop FIX) because the prior probe did not make the service stick (recurrence). bctc-discover folded (no churn — root cause already boarded). No change vs router on P6/P7 routing.
- **DJ-GATE-1:** status flips this commit: FU-SCHEMA-DRIFT-P6 REVIEW->DONE; 2 new backlog entries (FU-SCHEMA-DRIFT-P7 architect SPIKE TODO + FIX-NEWS-VPS-CRASH-LOOP TODO); 2 signal_queue flips (sau-...3050 + sau-...3051 NEW->TRIAGED). Single status key verified per edit (all =1); signal rows preserved EXACTLY 54 (all unique, only statuses changed); fresh-read + [ -s ] empty-guard + jq-validate before atomic mv. P5-SELFHEAL stays REWORK (note appended re reverted P6 impl).
- **dispatch ask (router owns):** route FU-SCHEMA-DRIFT-P7 to ARCHITECT for SPIKE (timebox 120m, add-missing-canonical-tables) BEFORE any dev re-impl; route FIX-NEWS-VPS-CRASH-LOOP to ops-vps-fetch for journalctl recon then dev fix; FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE already TODO (covers bctc-discover). WIP<=2 honored (architect lane, 0 IN_PROGRESS). No push — router owns push + next CI gate (native fail+error must DROP vs 629).

### STEP po-S20 (DJ-GATE-1): CI-P7-GATE-3572444a FAILED+reverted — FU-P7 TODO->DONE + open FU-P8 (6th-touch architect SPIKE, 2 directions) + Bug A FIX-NEWS-VPS-HEALTH-SQL DONE (2026-06-09T03:34:12Z)
**task_id:** FU-SCHEMA-DRIFT-P7, FU-SCHEMA-DRIFT-P8, FIX-NEWS-VPS-HEALTH-SQL
- **what-done:** Adjudicated CI-P7-GATE-3572444a (docs/signals/ci-p7-gate-result-3572444a-20260609T0335Z.json; router pre-measured native 10886 pass / 616 fail / 14 err / Ran 11538 = fail+error 630 vs 629 after-P4 absolute = GATE FAILED +1 zero-heal; router already REVERTED P7 code 2a6044b1 + pushed). Flipped FU-SCHEMA-DRIFT-P7 TODO->DONE (spike sound, fix-hypothesis disproven). Opened FU-SCHEMA-DRIFT-P8 architect SPIKE (6th touch, 2 candidate directions a/b). Added Bug A FIX-NEWS-VPS-HEALTH-SQL as DONE (verified+pushed, +1 regression test, ops-rebuild note). Did NOT re-measure CI (router owns gate+revert). Did NOT push.
- **what-considered:**
  - FU-P7 DONE vs REWORK: DONE (CHOSEN). Spike diagnosis CORRECT — IDed the 7 close-no-init destroyers (103/1076/1291/182/1869b/231/283). Only the afterAll-reinit FIX-HYPOTHESIS was disproven empirically (native 629->630 +1 WORSE, ZERO residual classes healed, buckets BYTE-IDENTICAL to after-P4): per-file afterAll(closeDb; initDatabase) re-creates tables in destroyer teardown but the NEXT file's beforeEach/closeDb re-empties the :memory: singleton before the consuming file runs. Same pattern as P5/P6 (spike sound, fix wrong) -> spike STAYS DONE.
  - Signal ask #1 "flip FU-SCHEMA-DRIFT-P7-IMPL -> REWORK": NO such board task exists. Router did the P7 impl DIRECTLY (b9e305ae->3572444a gate run, reverted 2a6044b1) — identical to P6 where no IMPL task existed either (po-S19 precedent: do NOT invent a phantom IMPL task to flip). Recorded the reverted router-direct impl in the P7 spike row note instead. Honest disposition = spike DONE + note, no phantom row.
  - P8 as per-file patch / mechanized sweep vs ARCHITECT SPIKE: SPIKE (CHOSEN). 6th touch of CI-test-schema surface -> recurring-bug-escalation -> architect designs before dev impl. Per-file afterAll (P6+P7) AND production self-heal-on-drifted-slices (P5) BOTH exhausted. Framed 2 evidence-backed directions for architect to CHOOSE: (a) reconcile drifted created_at-omitting init slices THEN re-apply P5 self-heal in getDb() (P5 empirically healed 4 classes — proves self-heal works once slice-drift removed; the +6 was the created_at regression not the heal); OR (b) bun GLOBAL preload (bunfig.toml) re-init singleton on first getDb() after any destroyer empties it (GLOBAL not per-file — per-file proven dead). HARD CONSTRAINT carried into spike: source each DDL from its OWNING production module (invented DDL sank P5). Explicitly NOT another per-file inline-DDL patch; NOT mechanized one-size injection (9454baad +219 reverted).
  - Bug A board row vs no row: ADD as DONE (CHOSEN). FIX-NEWS-VPS-HEALTH-SQL (b3d3022c) is a LEGIT separate fix (vpsHealthPoller unixepoch normalise — healed its own lexicographic-MAX(timestamp) ordering false-UNHEALTHY bug + +1 passing regression test, test file 234 12->13, ZERO regression), RETAINED through the P7 revert (apps/ byte-identical to b3d3022c). Distinct from FIX-NEWS-VPS-CRASH-LOOP (the vn-news-fetch crash-restart loop). No board row existed -> created one as DONE to record it; note flags mcp-server CONTAINER REBUILD needed for the LIVE false-UNHEALTHY benefit (source in CI; live polling runs old image) -> dispatch ops (rebuild-after-dev-change).
- **why-decision:** PO accepted router CI numbers (router owns the gate+revert per signal contract; did NOT re-measure) but reconciled the board honestly against ACTUAL board state — refusing to flip a nonexistent FU-P7-IMPL phantom (P6 precedent) and recording the reverted router-direct impl in the spike note. P8 keeps the recurring-bug rule honored (architect chooses between 2 root-cause directions; no 6th dev churn before design).
- **why-change:** Diverged from signal ask #1 wording (no FU-P7-IMPL board row to flip) — equivalent honest outcome via P7 spike DONE + note (consistent with po-S19 P6 handling). No change vs router on P8 routing or Bug A disposition.
- **DJ-GATE-1:** status flips this commit: FU-SCHEMA-DRIFT-P7 TODO->DONE; 2 new board entries (FU-SCHEMA-DRIFT-P8 architect SPIKE backlog/TODO + FIX-NEWS-VPS-HEALTH-SQL done/DONE). Single status key verified per edit (all rows=1); signal_queue rows preserved EXACTLY 54 (byte-identical to pre-edit backup — NO whole-object rewrite, NO clobber); fresh-read + [ -s ] empty-guard + jq-validate before atomic mv on every edit; backlog 78->79, done 120->121 (exactly the 2 additions, no other rows mutated). Committed in the SAME commit as this journal entry.
- **dispatch ask (router owns):** route FU-SCHEMA-DRIFT-P8 to ARCHITECT for SPIKE (timebox 120m, choose direction a or b) BEFORE any dev re-impl; dispatch OPS to REBUILD mcp-server container for the live false-UNHEALTHY benefit of Bug A. WIP<=2 honored (architect lane, 0 IN_PROGRESS). No push — router owns push + next CI gate (native fail+error must DROP vs 629 AND residual buckets shrink toward zero).
- **RECOVERY (concurrent-writer SSOT clobber):** First po-S20 board write was committed (4657e981) but a concurrent writer truncated orch-state.json to 1 byte at my `git add` instant (commit captured the corrupt blob). system-auditor caught it Tier-1 CRITICAL; an infra agent restored the file to the po-S19 base (commit 3863ffb3), which discarded my 3 board edits while keeping this journal entry committed. RE-APPLIED all 3 edits (FU-P7 TODO->DONE, +FU-P8 SPIKE, +Bug A DONE) onto the restored base in ONE atomic jq pass, preserving the auditor's 2 new signal_queue rows (sigq 54->56). Lesson (feedback_concurrent_commit_race / feedback_jq_empty_guard_clobbers_ssot): serialize SSOT commits; single atomic transform over serial per-row mv; fresh-read base before re-apply.

### STEP po-S21 (DJ-GATE-1): CI-P8-GATE-6295cb32 FAILED+reverted — FU-P8-IMPL REVIEW->REWORK + PARK schema-drift (path B) + open FU-CI-PROFILE-629 (architect SPIKE, full-corpus 629 taxonomy) (2026-06-09T04:14:34Z)
**task_id:** FU-SCHEMA-DRIFT-P8-IMPL, FU-CI-PROFILE-629
- **DECISION: PATH B** — PARK the schema-drift cluster as best-effort-exhausted (629 = the schema-drift floor) and PIVOT CI-RED-RECONCILE to a full-corpus 629 failure taxonomy; attack the largest/cheapest cluster first.
- **what-done:** Adjudicated CI-P8-GATE-6295cb32 (signal docs/signals/ci-p8-gate-result-6295cb32-20260609T0410Z.json; router pre-measured native 10885 pass / 36 skip / 617 fail / 14 err / Ran 11538 = fail+error 631 vs 629 after-P4 absolute = GATE FAILED +2 WORSE, P5 pattern reproduced; router already REVERTED 4 P8 code files to 2a6044b1 in 880ffca6, apps/ byte-identical to 629 baseline, tsc clean, pushed). Flipped FU-SCHEMA-DRIFT-P8-IMPL REVIEW->REWORK (in_progress bucket; the board row is FU-SCHEMA-DRIFT-P8-IMPL — signal called it FIX-... but no such row exists; flipped the real one). Kept FU-SCHEMA-DRIFT-P8 architect spike DONE (direction sound, only created_at hypothesis disproven). Opened FU-CI-PROFILE-629 (architect SPIKE, TODO, timebox 120m, path-B deliverable). Did NOT open a 7th-touch schema-drift task. Did NOT re-measure CI (router owns gate+revert). Did NOT push.
- **what-considered:**
  - PATH A (7th touch, diagnostic-first reproduce created_at x3 + grep all CREATE TABLE + reconcile column-sets) vs PATH B (park schema-drift, profile full 629): PATH B (CHOSEN). Decisive ROI argument (router strategic input, independently weighed): the schema-drift cluster's BEST achievable outcome removes only ~tens of the 629 native fails (the 4-5 table-MISSING classes self-heal repeatably heals: sbv_rates_history/commodity_prices_history/imf_indicators/vps_service_health/daily_ohlcv), and even that nets ~breakeven because created_at x3 column-existence collision claws it back. 6 touches, 4 consecutive disproven fix-hypotheses (P5/P6/P7/P8) on ONE cluster that CANNOT reach the /goal 0-fail target by itself. Continuing path A = sunk-cost iteration on the lowest-ROI surface. Path B's full-corpus taxonomy is the actual unblock toward green: it tells us WHICH clusters dominate the 629 and which are cheapest, so the sprint attacks by ROI instead of re-litigating the schema surface a 7th time. Path A's created_at diagnosis (column-less competing CREATE TABLE winning the IF NOT EXISTS race) is RECORDED in the spike findings and can be revisited IF the taxonomy later shows schema-drift is the largest/cheapest remaining cluster — but it is demonstrably NOT, given it caps at ~tens.
  - FU-P8-IMPL REWORK vs DONE: REWORK (CHOSEN). Gate FAILED + router reverted; the IMPL is OFF main, hypothesis empirically disproven (same spike-sound/fix-wrong pattern as P5/P6/P7). REVIEW would falsely imply awaiting sign-off on a live fix. With schema-drift PARKED, the REWORK is effectively shelved (superseded by FU-CI-PROFILE-629), not re-queued for a 7th dev churn — recorded in the row note.
  - FU-SCHEMA-DRIFT-P8 spike DONE vs REWORK: DONE (kept). Spike direction (self-heal heals table-MISSING classes) is empirically SOUND and reproduced P5->P8; only the created_at=missing-DEFAULT hypothesis was disproven (it is a column-EXISTENCE drift, not a DEFAULT problem — a missing DEFAULT yields NOT NULL constraint error on INSERT, not 'no such column' on query). Spike-sound/fix-wrong => spike STAYS DONE (po-S18/S19/S20 precedent).
  - Owner of profiling task — architect vs analyst: ARCHITECT (CHOSEN). Dispatch table has no test-corpus-profiling row; a no-code root-cause taxonomy that drives subsequent attack tasks is design/investigation work = the SPIKE pattern used for every P-touch. market-analyst is investment Q&A, not CI. Deliverable is a findings/brief doc -> architect (docs/architecture-briefs/). zone apps/mcp-server/ (where bun-test corpus lives).
- **why-decision:** PO accepted router CI numbers + revert (router owns the gate per signal contract; did NOT re-measure) and made the autonomous strategic call to STOP iterating the schema-drift cluster (best-effort-exhausted, ROI-capped at ~tens, 4 disproven hypotheses) and redirect the sprint to a full-corpus failure taxonomy — the only path that can make real progress toward /goal ci/cd 0-fail.
- **why-change:** STRATEGIC PIVOT vs po-S18/S19/S20 (which each opened the next P-touch spike). The recurring-bug pattern is now conclusive: 4 consecutive disproven hypotheses on a ROI-capped cluster => PARK, don't escalate to a 7th touch. 629 documented as the schema-drift floor.
- **DJ-GATE-1:** status flips this commit: FU-SCHEMA-DRIFT-P8-IMPL REVIEW->REWORK (in_progress); 1 new backlog entry (FU-CI-PROFILE-629 architect SPIKE TODO). FU-SCHEMA-DRIFT-P8 spike untouched (DONE). Single status key verified on flipped row (=1); backlog 78->79 (exactly the 1 addition), in_progress count unchanged, done 122 unchanged, signal_queue.rows preserved EXACTLY 56 (byte-identical, NO whole-object rewrite); ONE atomic jq pass; temp validated [ -s ] && jq -e . && size>600000 (701293) BEFORE mv; commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write to serialize vs the cowork */15 dispatcher. Committed in the SAME commit as this journal entry.
- **dispatch ask (router owns):** route FU-CI-PROFILE-629 to ARCHITECT for SPIKE (timebox 120m) — deliver docs/architecture-briefs/2026-06-09-ci-629-failure-taxonomy.md (cluster -> root-cause -> native-count, ranked attack order; NO code change; gate = taxonomy delivered). Do NOT open the next attack dev task yet — gated on the taxonomy. WIP<=2 honored (architect lane, 0 IN_PROGRESS). No push — router owns push.

### STEP po-S22 (DJ-GATE-1): FU-CI-PROFILE-629 SPIKE delivered+router-verified -> TODO->DONE + OPEN first attack FIX (Cluster 1 SOLO) + QUEUE Cluster 2 & 5 follow-ons (2026-06-09T04:44:01Z)
**task_id:** FU-CI-PROFILE-629, FIX-CI-MCP-SDK-MOCK-CONTAM, FIX-CI-DATA-SYMLINK-ENOENT, FIX-CI-DEAD-REUTERS-TESTS
- **ATTACK-SCOPE DECISION: Cluster 1 SOLO-FIRST** (not the 1+2+5 bundle). Rationale: isolating the single test-file fix makes its expected ~355-drop cleanly attributable in CI BEFORE mixing in CI-workflow/symlink infra (Cluster 2) or dead-test deletion (Cluster 5) — preserving measurement integrity, zero prod-code risk, WIP<=2.
- **what-done:** Closed the FU-CI-PROFILE-629 architect SPIKE (deliverable docs/architecture-briefs/2026-06-09-ci-629-failure-taxonomy.md EXISTS, 7682B; gate = taxonomy delivered MET; router raw-verified the central Cluster-1 claim). Flipped backlog[78] FU-CI-PROFILE-629 TODO->DONE (+closed_at +closing note). Opened FIX-CI-MCP-SDK-MOCK-CONTAM (TODO, dispatch-ready, dev-mcp-server, apps/mcp-server/, high) as the FIRST isolated attack FIX, appended to CI-RED-RECONCILE.tasks. Queued FIX-CI-DATA-SYMLINK-ENOENT (Cluster 2, ~91) + FIX-CI-DEAD-REUTERS-TESTS (Cluster 5, 2 err) as status QUEUED (NOT active, depends on Cluster 1). Did NOT open Cluster 3/4 (gated on 1+2+5 clearing). Did NOT re-measure CI / re-verify the taxonomy (router owns + already verified). Did NOT push.
- **what-considered:**
  - Cluster 1 SOLO vs 1+2+5 BUNDLE: SOLO (CHOSEN). Router preferred solo so the 355-drop is cleanly attributable in CI before touching CI-workflow/symlink infra. Bundling would conflate a test-file fix, a CI-workflow/.github change, and dead-test deletion into one CI delta — losing per-cluster attribution and widening blast radius. Solo also keeps WIP minimal (one active FIX). Cluster 2+5 lose nothing by waiting — they are LOW-cost non-prod and queued ready.
  - FU-629 DONE vs leave-TODO: DONE (CHOSEN). Spike gate is taxonomy-delivered (no code), deliverable present + router raw-verified the central claim (file own comment L26-27 documents the cascade; L38-44 do the mock.module; mock.restore() L45 insufficient = Bun ESM cache leak). I independently raw-read 1862c L38-42 myself: MockMcpServer={connect} only — no .tool()/.registerTool() — confirming the root cause (verify-raw-not-badges, did NOT relay sub-agent verdict). Gate MET => DONE.
  - First-attack cluster = Cluster 1 vs Cluster 2/5: Cluster 1 (CHOSEN). Largest (~355/56%), single-file, ZERO prod-code risk, cleanly CI-measurable in isolation. Taxonomy ranked attack order #1. Maximum ROI for minimum risk.
  - Schema-drift (Cluster 6): NOT re-opened (PARKED, P4-P8 exhausted, ~4 fails) — explicit instruction + po-S21 PATH-B decision stands.
  - New-task placement = CI-RED-RECONCILE sprint .tasks[] vs backlog: sprint .tasks (CHOSEN) — all prior CI FIX tasks (FIX-SCHEMA-DRIFT-P1/P2, FIX-MCP-*, etc.) live there; keeps the sprint's attack lane cohesive. FU-629 stayed in backlog[78] (where po-S21 created it) — flipped in place, not relocated, to avoid churn.
- **why-decision:** PO accepted the router-verified taxonomy (router owns the verify per signal contract; did NOT re-verify) but INDEPENDENTLY raw-read the target test file (1862c L38-42) to confirm the root cause before opening the attack FIX, then made the autonomous solo-vs-bundle call on measurement-attribution grounds.
- **why-change:** PIVOT from diagnostic SPIKE phase to ATTACK FIX phase of CI-RED-RECONCILE (629 taxonomy delivered). Diverges from the P4-P8 schema-drift loop: the dominant cluster is mock-contamination (~355), not schema-drift (~4) — the 6 schema spikes were chasing the wrong cluster (now PARKED). First real ROI-ranked attack opened.
- **DJ-GATE-1:** status flips this commit: FU-CI-PROFILE-629 (backlog[78]) TODO->DONE (+closed_at +note); 3 new sprint tasks in active_sprints[24].tasks (FIX-CI-MCP-SDK-MOCK-CONTAM TODO + FIX-CI-DATA-SYMLINK-ENOENT QUEUED + FIX-CI-DEAD-REUTERS-TESTS QUEUED). Single status key verified on flipped 629 row (=1); backlog len 79 unchanged (in-place flip, no add/remove); sprint .tasks 19->22 (exactly the 3 additions); signal_queue.rows preserved EXACTLY 56 (byte-identical, NO whole-object rewrite, NO clobber); ONE atomic jq pass; temp validated [ -s ] && jq -e . && size>600000 (707093) BEFORE mv; commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write to serialize vs the cowork */15 dispatcher. Committed in the SAME commit as this journal entry.
- **dispatch ask (router owns):** route FIX-CI-MCP-SDK-MOCK-CONTAM to dev-mcp-server NOW (rewrite 1862c mock so MockMcpServer exposes .tool()/.registerTool() no-ops OR scope mock to SSE transport only; gate = native fail+error DROPS well below 629, target ~274). Hold FIX-CI-DATA-SYMLINK-ENOENT + FIX-CI-DEAD-REUTERS-TESTS QUEUED until Cluster 1 lands+measured, then flip to TODO. Do NOT open Cluster 3/4 yet. WIP<=2 honored (one active FIX). No push — router owns push + the next CI gate (native fail+error must DROP vs 629).

### STEP po-S23 (DJ-GATE-1): CI-CLUSTER1-GATE-2acb7192 PASSED -283 — FIX-CI-MCP-SDK-MOCK-CONTAM TODO->DONE + re-baseline 629->346 + un-queue Cluster 2 & 5 (2026-06-09T05:08:00Z)
**task_id:** FIX-CI-MCP-SDK-MOCK-CONTAM, FIX-CI-DATA-SYMLINK-ENOENT, FIX-CI-DEAD-REUTERS-TESTS
- **what-done:** Router ran the router-owned CI gate for Cluster 1 (FIX-CI-MCP-SDK-MOCK-CONTAM) and it PASSED — native fail+error DROPPED 629->346 (-283, -45%) at sha 2acb7192 (run 27184744909, bun job 80251172255: 11416 pass / 42 skip / 344 fail / 2 errors / Ran 11802 across 1039 files). Flipped active_sprints[24] (CI-RED-RECONCILE) task[19] FIX-CI-MCP-SDK-MOCK-CONTAM TODO->DONE (+closed_at +actual_result +note). RE-BASELINED the two remaining FIX-CI-* follow-on tasks (Cluster 2 task[20], Cluster 5 task[21]) baseline_pass + gate from the SUPERSEDED 629 absolute to the NEW 346 absolute (sha 2acb7192, run 27184744909, job 80251172255). Un-queued both QUEUED->TODO (dispatch trigger) baselined at 346. Kept Cluster-6 schema-drift (FU-SCHEMA-DRIFT-P5..P8 / P8-IMPL) PARKED — NOT re-opened.
- **what-considered:**
  - Re-measure vs accept router gate: ACCEPT (CHOSEN). Router owns the CI gate + push and raw-verified the native summary from gh run view --job=80251172255 --log; signal file ci-cluster1-gate-result-2acb7192-20260609T0505Z.json carries the full payload. Re-measuring would duplicate router-owned work and risk drift; PO board write is downstream of the verified gate.
  - Cluster-2 owner = dev-mcp-server vs CI-workflow owner: dev-mcp-server (CHOSEN, already set). The ENOENT swallow is in setup.ts under apps/mcp-server/src; the missing apps/mcp-server/data symlink is mcp-server's data zone. Single-zone apps/mcp-server/ -> dev-mcp-server per taxonomy. Cluster-5 owner=dev-mcp-server (delete 2 _deprecated reuters test files, zone apps/mcp-server/).
  - Un-queue both 2 & 5 vs one-at-a-time: BOTH->TODO (CHOSEN, router-instructed) — both LOW-cost non-prod, Cluster 5 is the cheapest possible next win (the ENTIRE remaining 2-error count). WIP<=2 honored: zero active dev FIX after Cluster 1 DONE, so opening 2 TODO follow-ons stays within bound (router/PM dispatches per WIP at run time).
- **why-decision:** Cluster-1 was the largest single net win of CI-RED-RECONCILE (-283) and EMPIRICALLY CONFIRMED the architect Option-A diagnosis: the 1862c mock.module() MockMcpServer lacked .tool()/.registerTool(), leaking a connect-only McpServer through Bun's ESM cache to 69+ downstream files; the full no-op stub set (tool/registerTool/_registeredTools/server/close/isConnected) cleared the cascade (fail -271, errors -12). Test COUNT rose +265 because ~265 previously-CRASHED tests now execute; ~72 of the ~355 estimate were real assertion failures hidden behind the crash and now VISIBLE (belong to other clusters). The 346 absolute SUPERSEDES 629 as the new gate floor for all downstream FIX-CI-* tasks.
- **why-change:** First real WIN of the attack phase (vs P5-P8 schema-drift which all netted breakeven-or-worse). No deviation from po-S22 plan — Cluster 1 SOLO-FIRST executed exactly as queued; 2 & 5 now unblocked per the queued dependency.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24].tasks[19] FIX-CI-MCP-SDK-MOCK-CONTAM TODO->DONE (+closed_at 2026-06-09T05:08Z +actual_result +note); tasks[20] FIX-CI-DATA-SYMLINK-ENOENT QUEUED->TODO + baseline_pass/gate re-pointed 629->346; tasks[21] FIX-CI-DEAD-REUTERS-TESTS QUEUED->TODO + baseline_pass/gate re-pointed 629->346. Single status key verified on each flipped row (=1). Sprint .tasks len 22 unchanged (in-place flips, no add/remove). signal_queue.rows preserved EXACTLY 57 (byte-identical, NO clobber). ONE atomic jq pass; temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write. Committed in the SAME commit as this journal entry. Cluster-6 schema-drift PARKED (NOT re-opened). NOT pushed (router owns push + next gate vs the 346 absolute).

### STEP po-S24 (DJ-GATE-1): CI-CLUSTER2-5-GATE-91afe344 PASSED -105 (errors->0) — Cluster-2 + Cluster-5 BOTH TODO->DONE + re-baseline 346->241 + open RE-PROFILE-CI-241-RESIDUAL (architect SPIKE) (2026-06-09T05:37:58Z)
**task_id:** FIX-CI-DATA-SYMLINK-ENOENT, FIX-CI-DEAD-REUTERS-TESTS, RE-PROFILE-CI-241-RESIDUAL
- **what-done:** Adjudicated CI-CLUSTER2-5-GATE-91afe344 (signal docs/signals/ci-cluster2-5-gate-result-91afe344-20260609T0530Z.json; router pre-measured + raw-verified the native summary, retained the code, bundled the push 4bbdce2e->91afe344). Both Cluster-2 (FIX-CI-DATA-SYMLINK-ENOENT) and Cluster-5 (FIX-CI-DEAD-REUTERS-TESTS) were cleared in ONE dev commit a5150685. Flipped BOTH active_sprints[24].tasks TODO->DONE (+closed_at +done_at +done_by +actual_result). RE-BASELINED the sprint absolute 346->241 onto the new RE-PROFILE task (the forward-looking floor). Opened RE-PROFILE-CI-241-RESIDUAL (architect SPIKE, TODO, high, zone apps/mcp-server/, timebox 120m) to re-profile the 241 residual into a FRESH cluster breakdown that supersedes the 629-era taxonomy. Kept Cluster-6 schema-drift PARKED (NOT re-opened). Did NOT re-measure CI (router owns gate). Did NOT push.
- **gate evidence (router-verified, accepted; native-to-native ONLY):** prior absolute run 27184744909 / sha 2acb7192 = 11416 pass / 42 skip / 344 fail / 2 errors / Ran 11802 = **346** fail+error. After Cluster-2+5 run 27185729719 / sha 91afe344 / bun job 80254121788 = 11534 pass / 42 skip / **241 fail** / **0 errors** / Ran 11817 = **241** fail+error. NET **-105 (-30%)**; ERRORS **2 -> 0** (first time ZERO this sprint). CI workflow conclusion stays `failure` (total fail != 0) — EXPECTED, CI RED until 0; the VERIFICATION GATE (native fail+error must DROP vs 346) is MET. The **346 absolute is SUPERSEDED** by **241** (sha 91afe344, run 27185729719, job 80254121788).
- **what-considered:**
  - Accept router gate vs re-measure: ACCEPT (CHOSEN). Router owns the CI gate + push and raw-verified the native summary from `gh run view --job=80254121788 --log`; signal carries the full payload. Re-measuring duplicates router-owned work and risks drift; PO board write is downstream of the verified gate (same disposition as po-S23).
  - Close BOTH in one DJ STEP vs split: BOTH (CHOSEN, router-instructed). The two fixes landed in ONE dev commit (a5150685) and were measured jointly by ONE gate run — they are not separately CI-attributable, so one DJ STEP stamping BOTH task ids is the honest record (no phantom per-cluster split of a joint measurement).
  - Re-profile NOW (architect SPIKE) vs swing at Cluster-3/4 directly: SPIKE FIRST (CHOSEN). The 629-era taxonomy estimates (Cluster-3 ASSERTION/LOGIC ~159, Cluster-4 UNDEFINED-FN getMarketMessageDigest ~21) were computed at the 629 absolute, PRE-UNMASK. Cluster-1 unmasked ~72 previously-crashed tests; Cluster-2 unmasked +15 more (ENOENT crash). The 241 residual composition has SHIFTED — swinging at a stale estimate risks mis-sizing the largest/cheapest bucket. A fresh group-by-error-signature breakdown of the actual 241 is the only honest input to the next ROI-ranked attack. owner = architect (no-code root-cause taxonomy = SPIKE pattern, same as FU-CI-PROFILE-629 in po-S21/S22); zone apps/mcp-server/ (bun-test corpus); deliverable = brief in docs/architecture-briefs/.
  - Cluster-6 schema-drift re-open: NO (PARKED per po-S21 PATH-B, P4-P8 exhausted, ~4 fails) — explicit instruction + standing decision.
- **why-decision:** PO accepted the router-verified native gate (router owns the gate per signal contract; did NOT re-measure) and made the autonomous call to re-profile before the next swing — the unmask effect from two consecutive cluster clears makes the 629-era taxonomy stale, so a fresh 241 breakdown is the prerequisite to attack by ROI rather than by an outdated estimate.
- **why-change:** no change from router recommendation; adopted the solo-measurement-then-re-profile sequencing consistent with the post-taxonomy attack phase opened in po-S22/S23. First time ERRORS hit ZERO this sprint.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24].tasks FIX-CI-DATA-SYMLINK-ENOENT TODO->DONE + FIX-CI-DEAD-REUTERS-TESTS TODO->DONE (both +closed_at/done_at/done_by/actual_result); +1 new sprint task RE-PROFILE-CI-241-RESIDUAL (TODO, architect SPIKE, carries the 241 absolute). Single status key verified on each flipped row (=1, no duplicate-key SSOT bug). Sprint .tasks 22->23 (exactly the 1 addition; 2 in-place flips). DONE count 12->14 (exactly the 2 flips). signal_queue.rows preserved EXACTLY 57 (byte-identical, NO whole-object rewrite, NO clobber). ONE atomic jq pass (scripts/po-s24-cluster2-5-gate.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write to serialize vs the cowork */15 dispatcher. _schema=v3 _ssot=true preserved. Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the new 241 absolute).
- **dispatch ask (router owns):** route RE-PROFILE-CI-241-RESIDUAL to ARCHITECT for SPIKE (timebox 120m) — deliver a fresh-cluster-breakdown brief in docs/architecture-briefs/ that groups all 241 native fails (from `gh run view --job=80254121788 --log`, sha 91afe344) by error signature, ranked by (size x low-prod-risk), superseding the 629-era taxonomy. Do NOT open the next attack dev task yet — gated on the re-profile. Cluster-6 schema-drift stays PARKED. WIP<=2 honored (architect lane, 0 IN_PROGRESS). No push — router owns push + next gate (native fail+error must DROP vs 241).

### STEP po-S25 (DJ-GATE-1): CI-C3-C1-GATE-1d83a5ff PASSED (partial) -48 (-20%) — C3 + C1 BOTH REVIEW->DONE (DONE-as-scoped) + re-baseline 241->193 + open C2 attack TODO + 2 residual + C4 spike + cowork-envelope (2026-06-09T06:56:35Z)
**task_id:** FIX-CI-C3-DB-SINGLETON-SIGNAL-OUTCOMES, FIX-CI-C1-MACRO-INJECT-SEAM-TESTS, FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE
- **what-done:** Adjudicated CI-C3-C1-GATE-1d83a5ff (signal docs/signals/ci-c3-c1-gate-result-1d83a5ff-20260609T0645Z.json; router pre-measured + raw-verified the native bun summary, retained both test-only fixes, bundled the push f95c79be->1d83a5ff = ONE CI run 27188621595, bun job 80263220946). Both C3 (active_sprints[24].tasks) and C1 (task_board.in_progress) flipped REVIEW->DONE as DONE-AS-SCOPED (+closed_at/done_at/done_by/actual_result). RE-BASELINED the sprint absolute 241->193 onto active_sprints[24].ci_absolute (193 fail + 0 err / sha 1d83a5ff / run 27188621595 / job 80263220946) and onto the new C2 task's baseline_pass/gate (the forward-looking floor). Opened FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE (TODO, dev-mcp-server, high, ~21, the cleanest next win — require()->ESM import, test-only). Opened TWO backlog stubs each marked needs-architect-filescope-before-dev: FIX-CI-C1-RESIDUAL-MACRO-FETCHER-TESTS (~37) + FIX-CI-C3-RESIDUAL-DB-DESTROYERS (~23). Opened SPIKE-CI-C4-KINH-DICH-DIACRITICS (architect, prod-vs-test decision, ~14 — NOT routed to dev blind). Opened a SEPARATE non-CI backlog task VERIFY-COWORK-MACRO-SNAPSHOT-ENVELOPE (cowork parse of the {source_tier,text:JSON,fetchedAt} envelope live since 98df0f43, independent of /goal ci/cd pass). Kept Cluster-6 schema-drift PARKED. Did NOT re-measure CI. Did NOT push.
- **gate evidence (router-verified, accepted; native-to-native ONLY):** prior absolute run 27185729719 / sha 91afe344 = 11534 pass / 42 skip / 241 fail / 0 errors = **241**. After C3+C1 run 27188621595 / sha 1d83a5ff / bun job 80263220946 = 11582 pass / 42 skip / **193 fail** / **0 errors** / Ran 11817 = **193** fail+error. NET **-48 (-20%)**; errors stay **0**. CI workflow conclusion stays `failure` (193 != 0) — EXPECTED, CI RED until 0; the VERIFICATION GATE (native fail+error must DROP vs 241) is MET. The **241 absolute is SUPERSEDED** by **193** (sha 1d83a5ff, run 27188621595, job 80263220946). Raw `(fail)` grep returns ~387 (~2x) — bun prints each fail twice; the native summary 193 is the ONLY authoritative absolute.
- **what-considered:**
  - DONE vs re-open C3/C1 given net -48 << projected -114: DONE-AS-SCOPED (CHOSEN, router-instructed). Empirical root cause = the architect's C1(71)/C3(43) cluster sizes were SUPERSET counts; the dev tasks were scoped to the NAMED files (C1=089/1423d/1423f/1570c/1903a; C3=1945b) and those are now CLEAN in CI. The residual belongs to MORE files sharing the same pattern but OUTSIDE the original scope -> NEW tasks, NOT a re-open. Re-opening would mis-attribute new files to closed task ids and break the DoD audit trail.
  - Next attack = C2 vs C1-residual/C3-residual: C2 (CHOSEN). C2 (~21, Task 1168 require()->ESM import) is architect-diagnosed, test-only, single mechanical interop fix, NO whack-a-mole. The residuals are LARGER but under-scoped (need a file enumeration) — stubbed behind architect file-scope confirm to avoid a blind seam sweep (the 9454baad mechanical-sweep anti-pattern cost +219 and was reverted).
  - C4 to dev vs SPIKE first: SPIKE (CHOSEN). C4 (~14) is the ONLY residual touching PROD strings (kinhDichTools.ts) — a wrong call ships ASCII to a Vietnamese-output product OR churns tests. Architect prod-vs-test ruling required first.
  - Cowork-envelope: SEPARATE non-CI backlog task (CHOSEN). It is a LIVE PRODUCTION risk (raw JSON to MARKET since 98df0f43), independent of /goal ci/cd pass — keeping it out of the CI sprint preserves honest CI accounting.
  - Cluster-6 schema-drift re-open: NO (PARKED per po-S21 PATH-B) — explicit instruction + standing decision.
- **why-decision:** PO accepted the router-verified native gate and closed C3+C1 as DONE-as-scoped (their named files are CI-clean), rebaselined to the new 193 floor, and queued exactly one dispatchable attack (C2, cleanest ROI) while gating the under-scoped residuals + the prod-string C4 behind architect confirmation — driving toward native fail+error=0 by ROI, not by blind sweeps.
- **why-change:** net -48 vs projected -114 is EXPECTED once the SUPERSET-cluster diagnosis is accepted (no regression: total tests 11817 unchanged, fail only decreased); the under-scope is tracked forward, not charged back to the closed tasks.
- **DJ-GATE-1:** status flips THIS commit: C3 (active_sprints[24].tasks) REVIEW->DONE + C1 (task_board.in_progress) REVIEW->DONE (both +closed_at/done_at/done_by/actual_result). +4 new CI tasks in active_sprints[24].tasks (C2 TODO; C1-residual + C3-residual + C4-spike all BACKLOG stubs) + sprint-level ci_absolute=193 stamped (+ ._updated_by=po-S25). +1 non-CI top-level backlog task (cowork-envelope). Single status key verified on EVERY flipped/created row (=1, paths(scalars) check, no duplicate-key SSOT bug). CI sprint .tasks 25->29; top-level backlog 80->81. No new in_progress (WIP<=2 honored: only FU-SCHEMA-DRIFT-P8-IMPL is live in_progress; C1 now DONE; all new tasks TODO/BACKLOG). ONE atomic jq pass (scripts/po-s25-c3-c1-gate.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, owner_agent:po) held across the SSOT write to serialize vs the cowork dispatcher. _schema=v3 _ssot=true preserved. Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the new 193 absolute).
- **dispatch ask (router owns):** route FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE to dev-mcp-server (require()->ESM import for getMarketMessageDigest + batchReviewMarketMessages in the Task 1168 test files; both fns implemented in marketMessageStore.ts:239/349; TEST-ONLY). BEFORE dispatching either residual, route to ARCHITECT for a short file-scope confirm (exact files + prod-regression ruling). Route SPIKE-CI-C4-KINH-DICH-DIACRITICS to ARCHITECT (prod-vs-test diacritics ruling, timebox 60m) — do NOT route C4 to dev blind. VERIFY-COWORK-MACRO-SNAPSHOT-ENVELOPE is independent of /goal ci/cd pass. Cluster-6 schema-drift stays PARKED. No push — router owns push + next gate (native fail+error must DROP vs 193).

### STEP po-S26 (DJ-GATE-1): CI-C2-GATE-7bea53d0 PASSED -21 (EXACTLY projected) — FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE REVIEW->DONE + re-baseline 193->172 (193 SUPERSEDED) + CONFIRM 3 po-S25 stubs dispatch-ready (NO new tasks) (2026-06-09T07:18:45Z)
**task_id:** FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE
- **what-done:** Adjudicated CI-C2-GATE-7bea53d0 (signal docs/signals/ci-c2-gate-result-7bea53d0-20260609T0715Z.json; router pre-measured + raw-verified the native bun summary, retained the test-only fix, pushed 39cf167d->7bea53d0 = ONE CI run 27189745293, bun job 80266839417). Flipped active_sprints[CI-RED-RECONCILE].tasks FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE REVIEW->DONE (+closed_at/done_at/done_by/actual_result). RE-BASELINED the sprint absolute 193->172 onto .ci_absolute (172 fail + 0 err / sha 7bea53d0 / run 27189745293 / job 80266839417; supersedes 193 explicitly marked SUPERSEDED) AND re-pointed the three forward-looking gate references (C1-residual, C3-residual, C4-spike) from the SUPERSEDED 193 absolute to the NEW 172 absolute. Opened NO new tasks. CONFIRMED the three po-S25 stubs are present + dispatch-ready. Kept Cluster-6 schema-drift PARKED (NOT re-opened). Did NOT re-measure CI. Did NOT push.
- **gate evidence (router-verified, accepted; native-to-native ONLY):** prior absolute run 27188621595 / sha 1d83a5ff = 11582 pass / 42 skip / 193 fail / 0 errors = **193**. After C2 run 27189745293 / sha 7bea53d0 / bun job 80266839417 = **11603 pass / 42 skip / 172 fail / 0 errors / Ran 11817 across 1037 files** = **172** fail+error. NET **-21 (EXACTLY the projected ~21)**; errors stay **0**. CI workflow conclusion stays `failure` (172 != 0) — EXPECTED, CI RED until 0; the VERIFICATION GATE (native fail+error must DROP vs 193) is MET. The **193 absolute is SUPERSEDED** by **172** (sha 7bea53d0, run 27189745293, job 80266839417).
- **empirical finding:** C2 was correctly scoped — the require()->ESM import fix in the SINGLE Task-1168 file (1168-market-message-digest.test.ts, 31 local pass) cleared EXACTLY its bucket with no spillover and no masked truth. Contrast C3+C1 (po-S25) which under-scoped because their clusters were category SUPERSETS; C2's ~21 was a single-file count so it landed precisely. NO regression: total tests 11817 unchanged.
- **what-considered:**
  - Accept router gate vs re-measure: ACCEPT (CHOSEN). Router owns the CI gate + push and raw-verified the native summary from `gh run view --job=80266839417 --log` + raw-verified commit 7bea53d0 TEST-ONLY (git show --name-only = ZERO non-test src paths). Re-measuring duplicates router-owned work and risks drift; PO board write is downstream of the verified gate (same disposition as po-S23/S24/S25).
  - DONE vs DONE-AS-SCOPED: plain DONE (CHOSEN). Unlike C3/C1 (po-S25, DONE-AS-SCOPED with residual under-scope), C2 landed EXACTLY its projection (-21) — the single Task-1168 file fully cleared its bucket, no residual carved off. Clean full close.
  - Open new residual tasks this round: NO (CHOSEN, router-instructed). The next two biggest buckets (C1-residual ~37, C3-residual ~23) are ALREADY open as po-S25 backlog stubs needing architect file-scope confirm; the router is dispatching that architect confirmation in parallel. Opening anything new would duplicate. PO's job this round = flip + rebaseline + confirm stubs.
  - Cluster-6 schema-drift re-open: NO (PARKED per po-S21 PATH-B, P4-P8 exhausted, ~4 fails) — explicit instruction + standing decision.
- **3 po-S25 stubs CONFIRMED present + dispatch-ready (status / owner):** FIX-CI-C1-RESIDUAL-MACRO-FETCHER-TESTS = BACKLOG / dev-mcp-server (needs: architect file-scope confirmation before dev); FIX-CI-C3-RESIDUAL-DB-DESTROYERS = BACKLOG / dev-mcp-server (needs: architect file-scope confirmation before dev); SPIKE-CI-C4-KINH-DICH-DIACRITICS = BACKLOG / architect (prod-vs-test diacritics ruling, timebox 60m). All three present, owners correct, gates re-pointed to the 172 absolute. Router dispatching the architect file-scope confirm in parallel.
- **why-decision:** PO accepted the router-verified native gate and closed C2 as a clean full DONE (-21 == projection), rebaselined to the new 172 floor everywhere 193 was referenced (marking 193 SUPERSEDED), confirmed the three queued stubs are dispatch-ready, and opened NO new tasks — driving toward native fail+error=0 by the architect-confirmed ROI order, not blind sweeps.
- **why-change:** no change from router recommendation; the -21 == projection result confirms the single-file C2 scoping was exact (contrast the SUPERSET-cluster under-scope of C3/C1 in po-S25). Honest accounting: total tests 11817 unchanged, fail only decreased.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[CI-RED-RECONCILE].tasks FIX-CI-C2-GETMARKETMESSAGEDIGEST-REQUIRE REVIEW->DONE (+closed_at/done_at 2026-06-09T07:18:45Z / done_by po-S26 / actual_result). NO new tasks (3 stubs already present from po-S25, confirmed not re-created). ci_absolute rebaselined 193->172 (+supersedes/delta/updated_by po-S26); ._updated_by=po-S26; three residual/spike gate references re-pointed 193->172 (193 SUPERSEDED). Single status key verified on the flipped C2 row (paths(scalars) last-key == "status", count == 1, no duplicate-key SSOT bug). Sprint .tasks len unchanged (1 in-place flip, NO add/remove). signal_queue.rows preserved byte-identical (NO whole-object rewrite, NO clobber). ONE atomic jq pass (scripts/po-s26-c2-gate.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write to serialize vs the parallel architect commit + the cowork dispatcher; released after. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 1 (FU-SCHEMA-DRIFT-P8-IMPL REWORK) <= 2. Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the new 172 absolute).
- **dispatch ask (router owns):** route the architect file-scope confirmation for FIX-CI-C1-RESIDUAL-MACRO-FETCHER-TESTS (~37) + FIX-CI-C3-RESIDUAL-DB-DESTROYERS (~23) (already in flight per router) + SPIKE-CI-C4-KINH-DICH-DIACRITICS (prod-vs-test ruling). After architect confirms file-scope, dispatch the largest confirmed bucket to dev-mcp-server; same native-summary method vs the NEW 172 absolute (fail must DROP). Continue until native fail+error = 0 (/goal ci/cd pass). Cluster-6 schema-drift stays PARKED. No push — router owns push + next gate.

### STEP po-S27 (DJ-GATE-1): CI-C1C3-RESIDUAL-GATE-a43dff49 PASSED -12 — C1-residual + C3-residual BOTH REVIEW->DONE (DONE-as-scoped) + re-baseline 172->160 (172 SUPERSEDED) + C4 BACKLOG->TODO (PROD-TOUCHING, ISOLATED push) + /goal REMOVE-if-obsolete clause recorded (2026-06-09T08:05:00Z)
**task_id:** FIX-CI-C1-RESIDUAL-MACRO-FETCHER-TESTS, FIX-CI-C3-RESIDUAL-DB-DESTROYERS, SPIKE-CI-C4-KINH-DICH-DIACRITICS
- **what-done:** Adjudicated CI-C1C3-RESIDUAL-GATE-a43dff49 (signal docs/signals/ci-c1c3-residual-gate-result-a43dff49-20260609T0755Z.json; architect file-scope brief docs/architecture-briefs/2026-06-09-ci-172-residual-filescope.md). Router pre-measured + raw-verified the native bun summary, raw-verified commit a43dff49 TEST/META-ONLY (8 .test.ts + 4 meta, ZERO production src via git show --name-only guard), bundled the push e920dc6d->a43dff49 = ONE CI run 27191715572 / bun job 80273289808. Flipped BOTH residuals (active_sprints[24].tasks) REVIEW->DONE as DONE-AS-SCOPED (+closed_at/done_at/done_by/resolution/actual_result). RE-BASELINED the sprint absolute 172->160 onto .ci_absolute (160 fail + 0 err / sha a43dff49 / run 27191715572 / job 80273289808; 172 marked SUPERSEDED). Flipped SPIKE-CI-C4-KINH-DICH-DIACRITICS BACKLOG->TODO (owner dev-mcp-server, type FIX, prod_touching=true, isolated_push=true, baseline 160) per architect ruling (a). Recorded the /goal REMOVE-if-obsolete clause into a new sprint-level long_tail_triage_policy object (REWRITE vs REMOVE classification) AND into the C4 gate/note. Opened NO new tasks. Kept Cluster-6 schema-drift PARKED. Did NOT re-measure CI. Did NOT push.
- **gate evidence (router-verified, accepted; native-to-native ONLY):** prior absolute run 27189745293 / sha 7bea53d0 = 11603 pass / 42 skip / 172 fail / 0 errors = **172**. After C1+C3 residuals run 27191715572 / sha a43dff49 / bun job 80273289808 = **11615 pass / 42 skip / 160 fail / 0 errors / Ran 11817 across 1037 files** = **160** fail+error. NET **-12**; errors stay **0**. CI workflow conclusion stays `failure` (160 != 0) — EXPECTED, CI RED until 0; the VERIFICATION GATE (native fail+error must DROP vs 172) is MET. The **172 absolute is SUPERSEDED** by **160** (sha a43dff49, run 27191715572, job 80273289808).
- **empirical finding (under-scope confirmed AGAIN):** architect projected 42 (C1) + 20 (C3) -> ~62 net; ACTUAL net -12. Of the 9 C1-scoped files, 5 (028/025/1423a/1487/1833l) were ALREADY GREEN — order-dependent/transient failures cleared by the upstream Cluster-1 mock-contam fix and no longer real by this round. The 4 genuinely-broken C1: country key VN-vs-vietnam in SLA service; getText() JSON-envelope .split w/ no newlines (98df0f43 macro HTTP-proxy seam); stale cron '0 6 * * *'; unmocked getMacroExternal causing real FRED HTTP 5s timeouts + A-3 .resolves vs re-throwing job. C3: 4 files fixed (FK signal_outcomes->agent_signals blocked DELETE -> FK-safe delete; InMemoryTransport never closed -> beforeEach connect() stall -> afterEach close). The -12 (not -62) reflects the C1 '42' tally being a category-wide SUPERSET mostly absorbed by the Cluster-1 upstream fix. SAME pattern as po-S25 (C3/C1 superset under-scope).
- **what-considered:**
  - Accept router gate vs re-measure: ACCEPT (CHOSEN). Router owns the CI gate + push and raw-verified the native summary from `gh run view --job=80273289808 --log` + raw-verified a43dff49 TEST/META-ONLY. Re-measuring duplicates router work + risks drift; PO board write is downstream of the verified gate (same disposition po-S23/S24/S25/S26).
  - DONE vs re-open the residuals given net -12 << projected -62: DONE-AS-SCOPED (CHOSEN, router-instructed). Both were category-wide SUPERSET scopes; all NAMED files are fixed or already-green. Residual same-pattern files OUT of named scope -> long-tail re-profile, NOT a charge-back to closed ids (re-opening mis-attributes new files + breaks DoD audit). Same call as po-S25.
  - C4 BACKLOG->TODO vs keep gated: TODO (CHOSEN). Architect micro-SPIKE delivered ruling (a) (prod-emits-diacritics) — the prod-vs-test decision the SPIKE existed to make is now made, so C4 is a dispatchable FIX. Owner moves architect->dev-mcp-server.
  - C4 isolated push vs bundle: ISOLATED (CHOSEN, architect ruling). C4 touches PRODUCTION code (kinhDichTools.ts/leadershipTools.ts/formatAccuracyReport) — unlike every prior test-only round it MUST get its own commit + CI run + careful verify (test-only-vs-prod separation; a prod regression must not hide behind a test-only delta). Path A (QUE_DATA local formatting) preferred — zero Go service / no new HTTP.
  - GOAL refinement REWRITE-vs-REMOVE: RECORD as policy (CHOSEN). User refined /goal 'ci/di all passe on GitHub, test update (remove if obsolete)' — obsolete tests asserting against deleted/superseded prod (removed seams, deleted reuters.js, pre-98df0f43 macro headers, stale cron) must be DELETED not patched. Folded into a sprint-level long_tail_triage_policy object + the C4 gate/note so every remaining round classifies each bucket REWRITE (prod exists, test stale) vs REMOVE (prod gone, test obsolete).
  - Cluster-6 schema-drift re-open: NO (PARKED per po-S21 PATH-B) — standing decision.
- **why-decision:** PO accepted the router-verified native gate, closed both residuals DONE-as-scoped (named files CI-clean; under-scope tracked forward to the long-tail, not charged back), rebaselined to the new 160 floor (172 SUPERSEDED), promoted C4 to a dispatchable PROD-TOUCHING FIX that MUST be an isolated push per architect ruling (a), and recorded the /goal REMOVE-if-obsolete lens into a durable triage policy — driving toward native fail+error=0 by ROI with the new prod-vs-test discipline.
- **why-change:** net -12 vs projected -62 is EXPECTED once the SUPERSET-cluster diagnosis is accepted (no regression: total tests 11817 unchanged, fail only decreased) — consistent with po-S25. NEW this round: the goal gained an explicit REMOVE-if-obsolete clause and the next round is the FIRST prod-touching one (isolated-push discipline introduced).
- **DJ-GATE-1:** status flips THIS commit: active_sprints[CI-RED-RECONCILE].tasks FIX-CI-C1-RESIDUAL-MACRO-FETCHER-TESTS REVIEW->DONE + FIX-CI-C3-RESIDUAL-DB-DESTROYERS REVIEW->DONE (both +closed_at/done_at 2026-06-09T08:05:00Z / done_by po-S27 / resolution DONE-as-scoped / actual_result) + SPIKE-CI-C4-KINH-DICH-DIACRITICS BACKLOG->TODO (+owner dev-mcp-server / type FIX / prod_touching / isolated_push / baseline_pass 160 / gate / note / updated_by po-S27). NO new tasks (in-place flips only; sprint .tasks len 29 unchanged). ci_absolute rebaselined 172->160 (+supersedes/delta/updated_by po-S27); +new sprint-level long_tail_triage_policy object (REWRITE-vs-REMOVE clause); ._updated_by=po-S27. Single status key verified on ALL THREE rows ([paths(scalars)|.[-1]]|map(select(.=="status"))|length==1 each, no duplicate-key SSOT bug). signal_queue.rows preserved EXACTLY 57 (NO whole-object rewrite, NO clobber). ONE atomic jq pass (scripts/po-s27-c1c3-residual-gate.jq); temp validated [ -s ] && jq -e . && size>600000 (739761) BEFORE mv. commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write to serialize vs the cowork dispatcher; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 1 (FU-SCHEMA-DRIFT-P8-IMPL) <= 2. Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the new 160 absolute).
- **dispatch ask (router owns):** route SPIKE-CI-C4-KINH-DICH-DIACRITICS (now TODO, FIX, dev-mcp-server, PROD-TOUCHING) to dev-mcp-server as an ISOLATED round — its OWN commit + OWN CI run + careful verify (do NOT bundle with any test-only fix). Path A (QUE_DATA local formatting per architect ruling (a)). Apply the REMOVE-if-obsolete lens: any test asserting against deleted/superseded prod -> DELETE, don't patch. Next gate: same native-summary method vs the NEW **160** absolute (sha a43dff49) — fail must DROP. After C4, re-profile the residual ~160 long tail against the taxonomy with the REWRITE-vs-REMOVE lens. Continue until native fail+error = 0 (/goal ci/di all passe). Cluster-6 schema-drift stays PARKED. No push — router owns push + next gate.

### STEP po-S28 (DJ-GATE-1): CI-C4-GATE-3663bd12 PASSED -25 (-16%, EXACTLY projected, clean no-under-scope) — SPIKE-CI-C4-KINH-DICH-DIACRITICS REVIEW->DONE + re-baseline 160->135 (160 SUPERSEDED) + re-profile residual 135 by ROOT-CAUSE into 4 next clusters (C5 UNMOCKED-HTTP TODO; C6 SSOT-drift / C7 assertion-logic / C8 REMOVE-triage BACKLOG) with REWRITE-vs-REMOVE lens (2026-06-09T08:33:20Z)
**task_id:** SPIKE-CI-C4-KINH-DICH-DIACRITICS
- **what-done:** Adjudicated CI-C4-GATE-3663bd12 (signal docs/signals/ci-c4-gate-result-3663bd12-20260609T0830Z.json; router pre-measured + raw-verified the native bun summary, raw-verified the FIRST prod-touching commit f7af6f08 scope CONTAINED to C4 file-scope — 2 prod src + 2 test + 4 meta, tsc clean, local 258/0 across 5 C4 files — and pushed the ISOLATED round d26b8afc->3663bd12 = its OWN CI run 27193376550 / bun job 80278819016; code RETAINED on origin, gate passed). Flipped SPIKE-CI-C4-KINH-DICH-DIACRITICS (active_sprints[24].tasks) REVIEW->DONE (+closed_at/done_at/done_by/resolution=DONE/actual_result). RE-BASELINED the sprint absolute 160->135 onto .ci_absolute (135 fail + 0 err / sha 3663bd12 / run 27193376550 / job 80278819016; 160 marked SUPERSEDED) and re-pointed long_tail_triage_policy.absolute to 135. RE-PROFILED the residual 135 long tail by ROOT-CAUSE pattern (NOT by file) with the REWRITE-vs-REMOVE lens into 4 next clusters; queued the cheapest high-confidence one (C5 UNMOCKED-HTTP) to TODO, the other three (C6 SSOT-drift, C7 assertion-logic, C8 REMOVE-triage) as BACKLOG stubs. Archived the gate signal -> docs/signals/processed/ (git mv). Kept Cluster-6 schema-drift PARKED. Did NOT re-measure CI. Did NOT push.
- **gate evidence (router-verified, accepted; native-to-native ONLY):** prior absolute run 27191715572 / sha a43dff49 = 11615 pass / 42 skip / 160 fail / 0 errors = **160**. After C4 run 27193376550 / sha 3663bd12 / bun job 80278819016 = **11639 pass / 42 skip / 135 fail / 0 errors / Ran 11816 across 1037 files** = **135** fail+error. NET **-25 (-16%, EXACTLY the architect-projected ~25)**; errors stay **0**. tests 11817->11816 (one obsolete test REMOVED: 1416 'Lỗi khi tính quẻ thị trường' — get_market_hexagram handler deleted TSH-1 2026-05-31). CI workflow conclusion stays `failure` (135 != 0) — EXPECTED, CI RED until 0; the VERIFICATION GATE (native fail+error must DROP vs 160) is MET. The **160 absolute is SUPERSEDED** by **135** (sha 3663bd12, run 27193376550, job 80278819016).
- **empirical finding (clean — NO under-scope, contrast C1/C3):** architect projected ~25 and it netted EXACTLY -25. Unlike C1/C3 (po-S25/S27) whose category-wide SUPERSET cluster counts mostly pre-absorbed upstream, the C4 1414/1416/285/1472/1410 spec family was a tight single-feature scope, so it landed precisely. Architect ruling (a) CONFIRMED: PROD was emitting ASCII fallback; the spec family was source-of-truth. FIX = changed PRODUCTION to UTF-8 Vietnamese diacritics + added explain_hexagram sections (Hào từ / Tượng truyện / 6 hào) via QUE_DATA local library (Path A — ZERO Go service change, no new HTTP; prod diff 56 ins / 30 del). REWRITE-vs-REMOVE lens applied per-file: 1416=REMOVE (prod gone), 1410=REWRITE (formatAccuracyReport returns object, test cast to .text), 1414/285/1472=REWRITE (prod stale, fixed prod not test). First prod-touching round of the sprint — isolated-push discipline held (own commit + own CI run, no test-only delta hiding a prod regression).
- **what-considered:**
  - Accept router gate vs re-measure: ACCEPT (CHOSEN). Router owns the CI gate + push and raw-verified the native summary from `gh run view --job=80278819016 --log` + raw-verified f7af6f08 prod scope CONTAINED to C4. Re-measuring duplicates router work + risks drift; PO board write is downstream of the verified gate (same disposition po-S23..S27).
  - DONE vs DONE-AS-SCOPED: plain DONE (CHOSEN). C4 landed EXACTLY its projection (-25) with no residual carved off — a tight single-feature scope, not a category superset. Clean full close (like C2 po-S26, unlike C1/C3).
  - Next cluster grouping by FILE vs ROOT-CAUSE: ROOT-CAUSE (CHOSEN). The under-scope lesson (architect cluster counts are category-wide supersets; dev fixes only NAMED files) means a file-by-file queue mis-sizes. Grouped the 135 by the three root-cause patterns the annotations reveal: unmocked-HTTP (028/1487 + likely siblings), SSOT-drift (1031 watchlist sector), assertion-logic (1792/1352a + siblings), plus a REMOVE-triage bucket for obsolete-prod tests.
  - Which cluster to queue dispatchable: C5 UNMOCKED-HTTP (CHOSEN as cheapest high-confidence). One mechanical pattern (mock/guard the fetch) repeated across N files, all REWRITE (prod fetchers exist), 6 confirmed seed fails (028=3 + 1487=3) plus likely backlog siblings — the largest single-pattern bucket with the lowest prod risk. C6 (SSOT single-fix) + C7 (needs architect prod-vs-test confirm) + C8 (needs architect REMOVE evidence) held BACKLOG to keep ONE dispatchable cluster at a time (WIP discipline + avoid the 9454baad mechanical-sweep anti-pattern that cost +219).
  - REMOVE-vs-REWRITE per bucket: C5/C6/C7 = REWRITE (prod still exists — mock the fetch / fix the SSOT value / rewrite the stale assertion); C8 = REMOVE (prod gone/superseded — DELETE with evidence-commit citation, mirror C4 1416 + OBSOLETE-REMOVE-24; protect intentional anti-stub canaries like DWF-is-trading-day AC-P0-3-6).
  - Cluster-6 schema-drift re-open: NO (PARKED per po-S21 PATH-B) — explicit instruction + standing decision.
- **why-decision:** PO accepted the router-verified native gate, closed C4 as a clean full DONE (-25 == projection, the first prod-touching isolated round), rebaselined to the new 135 floor (160 SUPERSEDED), and re-profiled the residual 135 by ROOT-CAUSE (not by file) with the REWRITE-vs-REMOVE lens — queuing exactly one cheapest high-confidence dispatchable cluster (C5 unmocked-HTTP, all REWRITE) and gating the SSOT/assertion/REMOVE buckets behind their respective single-fix / architect-confirm / evidence-triage requirements. Driving toward native fail+error=0 by root-cause ROI with the under-scope lesson baked into every new cluster's gate.
- **why-change:** no change from router recommendation; the -25 == projection confirms the tight single-feature C4 scoping (contrast the SUPERSET under-scope of C1/C3). NEW this round: residual re-profiled by ROOT-CAUSE pattern (under-scope lesson made structural — dev MUST enumerate the full job log; annotations gave first-10 only) and the REMOVE-if-obsolete bucket (C8) made an explicit standalone cluster.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[CI-RED-RECONCILE].tasks SPIKE-CI-C4-KINH-DICH-DIACRITICS REVIEW->DONE (+closed_at/done_at 2026-06-09T08:33:20Z / done_by po-S28 / resolution DONE / actual_result). +4 new CI tasks in active_sprints[24].tasks (C5 UNMOCKED-HTTP TODO; C6 SSOT-drift + C7 assertion-logic + C8 REMOVE-triage all BACKLOG stubs, each carrying root_cause + classification REWRITE-vs-REMOVE + baseline 135). ci_absolute rebaselined 160->135 (+supersedes/delta/updated_by po-S28); long_tail_triage_policy.absolute re-pointed to 135 + applies_to updated to ROOT-CAUSE grouping + under_scope_lesson recorded; sprint-level ._updated_by=po-S28. Single status key verified on the flipped C4 row ([paths(scalars)|.[-1]]|map(select(.=="status"))|length==1, no duplicate-key SSOT bug). Sprint .tasks 29->33 (exactly the 4 additions; 1 in-place flip). DONE count 18->19 (exactly the 1 flip). signal_queue.rows preserved (NO whole-object rewrite, NO clobber). ONE atomic jq pass (scripts/po-s28-c4-gate.jq); temp validated [ -s ] && jq -e . && size>600000 (752296) BEFORE mv. commit-mutex (task_kind:commit-mutex, owner_agent:po, ttl 120s) held across the SSOT write to serialize vs the cowork dispatcher; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2. Gate signal git mv'd -> docs/signals/processed/. Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the new 135 absolute).
- **dispatch ask (router owns):** route FIX-CI-C5-UNMOCKED-HTTP-FETCHES (now TODO, FIX, dev-mcp-server, all REWRITE) to dev-mcp-server — dev MUST first profile the FULL `gh run view --job=80278819016 --log` to enumerate EVERY unmocked-HTTP fail (the 028=3 + 1487=3 seeds are first-10 annotations only, partial), then mock the fetcher / add a network-skip guard per file. TEST-ONLY round (may bundle with other test-only fixes). For C7 (assertion-logic) + C8 (REMOVE-triage): route to ARCHITECT for a short prod-vs-test / REMOVE-evidence confirm BEFORE dev. C6 (SSOT watchlist sector) is a cheap VNH-misclassification single-fix (grep repo for stale hand-typed sector value, align to SSOT) dispatchable after C5. Next gate: same native-summary method vs the NEW **135** absolute (sha 3663bd12) — fail must DROP. Continue until native fail+error = 0 (/goal ci/di all passe, remove if obsolete). Cluster-6 schema-drift stays PARKED. No push — router owns push + next gate.

### STEP po-S29 (DJ-GATE-1): CI-C5-GATE-22470e44 FAILED — REGRESSION +10 (135->145), router REVERTED 275dd7c8 — FIX-CI-C5-UNMOCKED-HTTP-FETCHES REVIEW->REWORK + RE-DISPATCH to ARCHITECT (SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY) + C6 BACKLOG->TODO (dev) + KEEP ci_absolute=135 (NO rebaseline to 145) (2026-06-09T09:35:00Z)
**task_id:** FIX-CI-C5-UNMOCKED-HTTP-FETCHES
- **what-done:** Adjudicated CI-C5-GATE-22470e44 (signal docs/signals/ci-c5-gate-result-22470e44-20260609T0930Z.json; router pre-measured + raw-verified the native bun summary from `gh run view --job=80288899677 --log`, confirmed REGRESSION attributable to test-only commit 22470e44, and ALREADY REVERTED the 3 C5 test files to pre-C5 state — commit 275dd7c8, pushed 22470e44..275dd7c8, restores the verified-good 135/sha 3663bd12; watcher armed on revert run 27197018430 to confirm restoration to ~135). Flipped active_sprints[CI-RED-RECONCILE].tasks FIX-CI-C5-UNMOCKED-HTTP-FETCHES REVIEW->**REWORK** (NOT done; owner dev-mcp-server->architect; +reworked_at/reworked_by po-S29; blocked_by + depends = [SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY]; gate rewritten with the falsification evidence). Created new architect SPIKE **SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY** (TODO, owner architect, timebox 120, zone apps/mcp-server/src/__tests__/, HARD constraint = NO new file-top mock.module()). Flipped C6 **FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT** BACKLOG->**TODO** (owner dev-mcp-server, dispatched_by po-S29, baselined to 135). **KEPT ci_absolute at 135 (sha 3663bd12)** — did NOT rebaseline to the regressed 145. Did NOT re-measure CI. Did NOT push.
- **gate evidence (router-verified, accepted; native-to-native ONLY):** 135 absolute = sha 3663bd12 / run 27193376550 / job 80278819016 = 135 fail + 0 err / 11639 pass / Ran 11816. C5 under test = sha 22470e44 / run 27196358844 / job 80288899677 = **145 fail + 0 err / 11617 pass / Ran 11804** = **+10 WORSE** (pass -22, tests -12). The four targeted contamination victims got WORSE not fixed: Task1423=30, Task028=22, Task025=20, Task1487=8 (=80 fails in the exact files projected to drop to ~0), PLUS new collateral rag=24 + Task1124=24. Verdict GATE FAILED — REGRESSION; reverted by router (275dd7c8 = pre-C5 state of 083/123/ddd-1b = sha 3663bd12 content, pre-push tsc OK).
- **empirical finding (cure FALSIFIED):** the C5 fix BACKFIRED. Root cause of the backfire: the `ddd-1b-rag-http-client.test.ts` file-top `mock.module()` re-registration **RE-SPREAD the exact Bun ESM-cache (process-scoped) contamination it was meant to cure** — the C5 root-cause vector applied AS the cure reproduced the disease. The delegating-mock approach in 083/123 also failed to lift the sbv/yahoo null returns. The static-stub contamination theory's proposed FIX does NOT hold empirically. This mirrors the original Cluster-1 mock.module poison cascade (po-S23 FIX-CI-MCP-SDK-MOCK-CONTAM): module-scope mock.module() leaks across run order. CONCLUSION CARRIED INTO RE-SPEC: NO new file-top/module-scope mock.module() is an admissible cure.
- **what-considered:**
  - REWORK vs DONE vs leave REVIEW: **REWORK** (CHOSEN). Not DONE (regression, +10). Not left REVIEW (the review verdict is in — it FAILED). REWORK accurately marks "fix attempted, falsified, must be re-designed before re-attempt."
  - Re-baseline 135->145 (trust the regressed run as new floor): **REJECTED** — 145 is a REGRESSION the router already reverted; the verified-good 135 (sha 3663bd12) is restored and STANDS. Rebaselining to 145 would lock in the regression as the floor and make every future delta meaningless (would falsely credit the next fix for merely undoing 22470e44). ci_absolute KEPT at 135, untouched (still updated_by po-S28).
  - Re-dispatch C5 to dev (same vector, patch harder) vs ARCHITECT-first: **ARCHITECT-first** (CHOSEN, recurring-bug-escalation). This is the 2nd failed attempt on the C5 mock-contamination vector (the delegating-mock + ddd-1b mock.module() cure is empirically falsified). Per recurring-bug rule (2+ commits same module/vector -> architect), re-spec by architect BEFORE any new dev fix. Architect must re-examine WHY sbv/yahoo victims still null and propose a contamination-SAFE strategy (per-test beforeEach/afterEach mock+restore in describe scope, DI seam at the fetcher call-site, or skipIf-no-network guard) with the HARD constraint: NO new file-top mock.module() (that vector demonstrably leaks). Architect lane consumes NO dev WIP.
  - C6 dispatch now vs hold behind C5: **dispatch now** (CHOSEN). C6 (SSOT-watchlist-sector-drift, REWRITE) is INDEPENDENT of C5 (different root cause: hand-typed sector value drifted from docs/data/stock-classification.json SSOT; VNH-misclassification pattern) and dispatchable to dev against the standing 135 baseline. With C5 now in architect's lane (no dev WIP), dev is free for C6.
- **why-decision:** PO accepted the router-verified regression gate + the router's pre-emptive revert (135 restored), flipped C5 to REWORK (not a fake DONE), KEPT the verified-good 135 floor (refused to rebaseline to the reverted 145), routed C5 re-spec to ARCHITECT first under the recurring-bug rule with the falsified mock.module() vector banned, and dispatched the independent C6 SSOT-drift fix to dev against 135. Driving native fail+error toward 0 by root-cause ROI while protecting the measured baseline from a reverted regression.
- **why-change:** REVERSES the po-S28 dispatch ask (C5 -> dev-mcp-server as a mechanical mock/guard FIX). The dev attempt 22470e44 empirically FALSIFIED the static-stub/delegating-mock cure (it regressed +10 by re-spreading the contamination). New disposition: C5 owner dev->architect, status TODO->REVIEW->REWORK, a dedicated re-spec SPIKE inserted ahead of any dev re-fix, and the mock.module()-at-module-scope vector explicitly forbidden in the re-spec.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[CI-RED-RECONCILE].tasks FIX-CI-C5-UNMOCKED-HTTP-FETCHES REVIEW->REWORK (+reworked_at/reworked_by po-S29 2026-06-09T09:35:00Z / owner->architect / blocked_by+depends=[SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY] / gate+note rewritten). +1 new task SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY (TODO, architect, SPIKE, timebox 120, NO-file-top-mock.module() constraint). FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT BACKLOG->TODO (+dispatched_at/dispatched_by po-S29 / owner dev-mcp-server / baseline_pass=135). **ci_absolute UNCHANGED = 135 / sha 3663bd12 / updated_by po-S28 (NO rebaseline to 145 — verified by jq post-write).** Single status key verified on the C5 row (no status/state duplicate). Sprint .tasks 33->34 (exactly the 1 SPIKE addition; 2 in-place flips). signal_queue preserved (no whole-object rewrite). ONE atomic jq pass; temp validated [ -s ] && jq -e . && size>600000 (756780) BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) held across the SSOT write; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2 (TODO is dispatch-trigger, not in_progress). Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the standing 135 absolute).
- **dispatch ask (router owns):** (1) route **SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY** to ARCHITECT — re-examine WHY 028-sbv-rates / 1487-yahoo-finance-extended / ragHttpClient still return null in CI and output a brief with a contamination-SAFE offline pattern (NO new file-top mock.module(); keep prod fetcher = REWRITE not REMOVE; gate to drop native fail+error BELOW 135). (2) route **FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT** to dev-mcp-server NOW — grep repo for the stale hand-typed sector string (1031-expanded-watchlist-catalog asserts 'chemicals' gets 'other'), align to docs/data/stock-classification.json SSOT; native fail+error must DROP below 135. FIX-CI-C5-UNMOCKED-HTTP-FETCHES (REWORK) is BLOCKED on the architect SPIKE — do NOT re-dispatch to dev until the re-spec lands. Next gate: same native-summary method vs the standing **135** absolute (sha 3663bd12) — fail must DROP. No push — router owns push + next gate.

### STEP po-S30 (DJ-GATE-1): SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY REVIEW->DONE (architect brief landed) + FIX-CI-C5-UNMOCKED-HTTP-FETCHES REWORK->TODO (owner architect->dev-mcp-server, unblocked, DI-seam spec written into note) + C6 LEFT REVIEW (router gating) + KEEP ci_absolute=135 (2026-06-09T09:50:33Z)
**task_id:** SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY
- **what-done:** Read the architect brief docs/architecture-briefs/2026-06-09-spike-ci-c5-contam-safe-restrategy.md (committed+pushed; ruling = DI-seam pattern (b)). Flipped active_sprints[CI-RED-RECONCILE].tasks SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY REVIEW->**DONE** (+done_by po-S30 / resolution DONE; the architect already stamped result + done_at). Re-dispatched FIX-CI-C5-UNMOCKED-HTTP-FETCHES **REWORK->TODO**, owner architect->**dev-mcp-server**, cleared blocked_by+depends (the SPIKE it waited on is now DONE), and wrote the DI-seam fix spec + brief path + HARD no-new-mock.module() constraint + per-victim CI-log verification requirement into the task note. baseline_pass STAYS 135 (band ±3 per the brief addendum). C6 (FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT) LEFT at REVIEW — untouched (router is gating it against its own CI run; a separate PO action flips it DONE once that gate confirms). **KEPT ci_absolute at 135 (sha 3663bd12, updated_by po-S28)** — no rebaseline.
- **architect ruling (folded into the C5 task note for dev):** Root cause CONFIRMED — 083-tool-analysis.test.ts (lines 15-26) and 123-integration-mcp.test.ts (lines 35-40) install file-top mock.module() stubs for sbv.js/yahooFinance.js (+ a dead retriever.js mock in 123) that poison Bun's process-scoped ESM cache for ALL downstream files; the victims (028/025/1423a/1487) already use the CORRECT DI seam (httpClient param) and fail ONLY from upstream poisoning. FIX = REMOVE the file-top mock.module() calls from 083 and 123; pass _testCommodityFetcher/_testSbvFetcher/_testRagRetriever via callTool args for the run_impact_chain tests; ddd-1b + all 4 victim files get NO CHANGE. ONE conditional PROD change: add _testCommodityFetcher/_testSbvFetcher/_testRagRetriever optional Zod args to the run_impact_chain handler in apps/mcp-server/src/interface/mcp/tools/news-analysis/analysis.ts (same _test* pattern as _testHoseClient in get_market_snapshot; zero prod-path behavior change) IF not already present. HARD CONSTRAINT: NO new file-top/module-scope mock.module() anywhere. Projected post-fix ~113 (22 victims cleared) — 19 below the ±3 noise floor; per-victim CI-log verification must confirm each named test (028/025/1423a/1487) flips fail->pass individually.
- **what-considered:**
  - SPIKE DONE vs leave REVIEW: **DONE** (CHOSEN). The brief is committed+pushed and answers the spike question with a concrete chosen pattern (b) + per-victim plan + DI-seam audit + nondeterminism addendum. The spike deliverable (findings doc) exists; nothing left to review.
  - C5 owner architect->dev vs keep architect: **dev-mcp-server** (CHOSEN). The architect re-spec is complete (its lane is done); the fix is now mechanical test-file edits + one bounded prod seam addition — squarely dev-mcp-server zone (apps/mcp-server/). The recurring-bug-escalation requirement (architect-first after the 2nd failed vector) is SATISFIED by the landed brief, so dev re-dispatch is now correct.
  - clear blocked_by/depends vs keep: **clear** (CHOSEN). C5 was blocked_by + depends=[SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY]; that SPIKE is now DONE so the dependency is satisfied and must be cleared, else dev sees a phantom blocker.
  - baseline_pass 135 vs adjust to 132 noise floor: **135 kept** (CHOSEN). The instruction + the architect addendum both define the gate as the standing 135 (band ±3, real threshold clearly below ~132). baseline_pass stays the absolute 135 anchor; the ±3 band + per-victim flip requirement are captured in the note, not the baseline field.
  - touch C6 vs leave REVIEW: **leave REVIEW** (CHOSEN — explicit instruction). C6 is gating on its own CI run owned by the router; a separate PO action closes it. Touching it now would race the router's gate.
- **why-decision:** PO closed the architect re-spec SPIKE as DONE on the landed brief, re-dispatched the now-unblocked C5 fix to dev-mcp-server with the full DI-seam spec + brief pointer + the banned-vector constraint + the per-victim CI-log flip requirement baked into the task note, kept the verified-good 135 floor, and left C6 untouched at REVIEW per the router's separate gate. Driving native fail+error toward 0 by the architect-blessed contamination-safe vector.
- **why-change:** ADVANCES the po-S29 disposition (C5 parked in architect's lane behind the re-spec SPIKE). The SPIKE deliverable is in -> SPIKE DONE, C5 unblocked + re-routed to dev with the chosen DI-seam pattern. No change to the 135 floor or to C6 (still REVIEW).
- **DJ-GATE-1:** status flips THIS commit: active_sprints[CI-RED-RECONCILE].tasks SPIKE-CI-C5-CONTAM-SAFE-RESTRATEGY REVIEW->DONE (+done_by po-S30 / resolution DONE 2026-06-09T09:50:33Z); FIX-CI-C5-UNMOCKED-HTTP-FETCHES REWORK->TODO (+owner architect->dev-mcp-server / blocked_by=[] / depends=[] / dispatched_by po-S30 / note rewritten with DI-seam spec + brief path + no-mock.module() constraint + per-victim CI-log flip gate; baseline_pass UNCHANGED=135). FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT UNTOUCHED = REVIEW. **ci_absolute UNCHANGED = 135 / sha 3663bd12 / updated_by po-S28 (no rebaseline — verified by jq post-write).** Single status key verified on both flipped rows (no status/state duplicate). Sprint .tasks count UNCHANGED (2 in-place flips, 0 additions). signal_queue preserved (no whole-object rewrite). ONE atomic jq pass (scripts/po-s30-c5-redispatch.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) held across the SSOT write; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2 (TODO is dispatch-trigger, not in_progress). Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the standing 135 absolute).
- **dispatch ask (router owns):** route **FIX-CI-C5-UNMOCKED-HTTP-FETCHES** (now TODO, FIX, dev-mcp-server, zone apps/mcp-server/) to dev-mcp-server — apply the architect DI-seam ruling: (1) REMOVE file-top mock.module() for sbv.js/yahooFinance.js (+dead retriever.js) from 083 (lines 15-26) and 123 (lines 35-40); (2) pass _testCommodityFetcher/_testSbvFetcher/_testRagRetriever via callTool args for the run_impact_chain tests; (3) ddd-1b + 028/025/1423a/1487 get NO CHANGE; (4) ONE conditional prod seam in apps/mcp-server/src/interface/mcp/tools/news-analysis/analysis.ts run_impact_chain handler (add the 3 optional _test* Zod args, _testHoseClient pattern) IF not present. HARD CONSTRAINT: NO new file-top/module-scope mock.module() anywhere. Gate: native fail+error must DROP below 135 (band ±3; aim ~113) AND each named victim (028/025/1423a/1487) must flip fail->pass individually in the CI log. **C6 stays REVIEW — router owns its gate; do NOT re-dispatch C5 prematurely.** No push — router owns push + next gate.

### STEP po-S31 (DJ-GATE-1): CI-C5-GATE-fff91143 PASSED -33 (135->102) + CI-C6-GATE-37bb2e7d PASSED (named DGC assertion flip) — FIX-CI-C5-UNMOCKED-HTTP-FETCHES REVIEW->DONE + FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT REVIEW->DONE + REBASELINE ci_absolute 135->102 (135 SUPERSEDED) + OPEN FIX-CI-1423E-PREEXISTING-CLUSTER (Task 1423e, 22 pre-existing fails, architect-first triage) (2026-06-09T10:20:00Z)
**task_id:** FIX-CI-C5-UNMOCKED-HTTP-FETCHES, FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT, FIX-CI-1423E-PREEXISTING-CLUSTER
- **what-done:** Adjudicated TWO router CI gates in ONE batched atomic board write (no concurrent writer — both dev agents done). **(1) FIX-CI-C5-UNMOCKED-HTTP-FETCHES REVIEW->DONE** (+done_by po-S31 / resolution DONE / done_at 2026-06-09T10:20:00Z / dj_gate_1 sprint-CI-RED-RECONCILE-po.md#po-S31): gate signal docs/signals/ci-c5-gate-result-fff91143-20260609T1015Z.json — native fail+error 135->102 (-33, far beyond ±3 jitter band, UNAMBIGUOUS) AND all 4 named victims flipped fail->pass INDIVIDUALLY in the CI log (Task028 sbv-rates 9->0, Task025 yahoo-finance 7->0, Task1423a us10y ^TNX 6->0, Task1487 yahoo-extended 3->0); fix = DI-seam pattern (b) (removed file-top mock.module() from 083+123 incl dead retriever.js mock, added _testCommodityFetcher/_testSbvFetcher/_testRagRetriever injection seam to run_impact_chain in analysis.ts, zero prod hot-path change, NO new mock.module()), commit fff91143 pushed by router. **(2) FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT REVIEW->DONE** (+done_by po-S31 / resolution DONE / done_at 2026-06-09T10:20:00Z / dj_gate_1 #po-S31): gate signal docs/signals/ci-c6-gate-result-37bb2e7d-20260609T1010Z.json — named 1031 SECTOR_PEERS DGC assertion flipped fail->pass (domain "other"->"chemicals") across baseline job 80278819016 vs C6 job 80294081419; stale-test REWRITE to match prod SSOT (DGC=chemicals); commit 37bb2e7d already pushed. **(3) REBASELINE ci_absolute 135->102** (sha fff91143, run 27198910454, job 80297590558, native_fail 102, native_errors 0, fail_plus_error 102, tests 11816, pass 11672, skip 42, updated_by po-S31, updated_at 2026-06-09T10:20:00Z, supersedes "135 (sha 3663bd12, run 27193376550, job 80278819016)"); new band ~102 ±3 (floor ~99); also synced long_tail_triage_policy.absolute to 102. **(4) OPENED new cluster FIX-CI-1423E-PREEXISTING-CLUSTER** (Task 1423e — 22 pre-existing CI fails, TODO, owner architect, type FIX, size M, zone apps/mcp-server/src/__tests__/, root_cause UNKNOWN, baseline_pass=102): present in BOTH the 135 baseline AND the 102 C5 run byte-stable, NOT a C5 victim, surfaced during C5 gate disambiguation when naive grep -F 'Task 1423' over-matched the distinct 1423a victim; architect-first prod-vs-test triage REQUIRED (REWRITE if prod-correct+test-stale / FIX if prod-broken / REMOVE if test-obsolete) BEFORE any dev fix, per /goal REMOVE-obsolete clause + assertion-logic escalation rule; gate later vs the new 102 band. Did NOT guess its root cause.
- **what-considered:**
  - C5 DONE vs hold: **DONE** (CHOSEN). Gate is unambiguous on BOTH axes — the -33 absolute is far outside the ±3 band AND every named victim flipped individually; zero regression, tests unchanged.
  - C6 DONE despite +2 absolute (137 vs 135, inside ±3 band): **DONE** (CHOSEN). A single deterministic -1 data fix is unresolvable at the absolute level and EXPECTED to be masked by ESM-cache nondeterminism; the named DGC assertion fail->pass flip is the authoritative jitter-robust signal.
  - rebaseline 135->102 vs keep 135: **rebaseline** (CHOSEN). -33 is a clean verified improvement far beyond noise, victim-confirmed — a legitimate rebaseline event (unlike the prior po-S29 regressed 145 which was NOT baselined). 135 explicitly SUPERSEDED.
  - 1423e owner architect vs dev: **architect** (CHOSEN). Root cause UNKNOWN; the /goal REMOVE-obsolete clause + assertion-logic escalation rule require architect prod-vs-test confirmation BEFORE any dev fix to avoid patching a test that should be REMOVED (or masking a real prod break). Modeled on the existing FIX-CI-C8-REMOVE-OBSOLETE-TESTS-TRIAGE architect pattern.
  - 1423e baseline 102 vs 135: **102** (CHOSEN). New cluster must be gated against the live floor, not the retired 135.
- **why-decision:** PO closed both gated CI fixes as DONE on unambiguous router evidence (C5 -33 + 4 victim flips; C6 named assertion flip), rebaselined the absolute to the verified 102 floor, and opened the newly-disambiguated 22-fail Task-1423e cluster to architect-first triage rather than guessing — driving native fail+error toward 0 with the prod-vs-test discipline intact.
- **why-change:** ADVANCES po-S30 (C5 was re-dispatched TODO with the DI-seam spec; dev executed it and the router gate passed). C5 + C6 both close; the 135 floor that stood since po-S28 retires to 102. New residual ~102: next clusters Task 1423e (NEW, architect-first), C7 assertion-logic (1792/1352a/1328e/1407b), C8 REMOVE-triage; Cluster-6 schema-drift stays PARKED.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[CI-RED-RECONCILE].tasks **FIX-CI-C5-UNMOCKED-HTTP-FETCHES REVIEW->DONE** (+done_by po-S31 / resolution DONE / done_at 2026-06-09T10:20:00Z / gate_result CI-C5-GATE-fff91143); **FIX-CI-C6-SSOT-WATCHLIST-SECTOR-DRIFT REVIEW->DONE** (+done_by po-S31 / resolution DONE / done_at 2026-06-09T10:20:00Z / gate_result CI-C6-GATE-37bb2e7d); +1 new task **FIX-CI-1423E-PREEXISTING-CLUSTER** (TODO, architect, FIX, size M, root_cause UNKNOWN, baseline_pass=102). **ci_absolute REBASELINED 135->102 / sha 3663bd12->fff91143 / updated_by po-S28->po-S31 (135 SUPERSEDED — verified by jq post-write)**; long_tail_triage_policy.absolute synced to 102. Single status key verified on all 3 touched rows AND across all 223 tasks (paths(scalars) status: max=min=1). Sprint .tasks 34->35 (2 in-place flips + 1 addition). Unique task-id count 222->223 (+1, exactly the new 1423e task). Valid JSON; final size 762011 (>600000). signal_queue preserved (no whole-object rewrite). ONE atomic jq pass (scripts/po-s31-c5c6-done-rebaseline-1423e.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) held across the SSOT write; released after commit (ok:true). _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2 (TODO is dispatch-trigger, not in_progress). Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the new 102 absolute).
- **dispatch ask (router owns):** push the C5/C6 DONE + rebaseline board write (commit reported below). Then route **FIX-CI-1423E-PREEXISTING-CLUSTER** (TODO, FIX, architect, zone apps/mcp-server/src/__tests__/) to **architect** for prod-vs-test triage of the 22 byte-stable Task-1423e fails BEFORE any dev fix — confirm REWRITE (prod-correct, test-stale) vs FIX (prod-broken) vs REMOVE (test-obsolete); gate later vs the new 102 band (floor ~99). Continue the /goal loop on the remaining ~102 (C7 assertion-logic + C8 REMOVE-triage queued; Cluster-6 schema-drift PARKED).

### STEP po-S32 (DJ-GATE-1): CI-1423E-GATE-9ed78225 PASSED — FIX-CI-1423E-PREEXISTING-CLUSTER TODO->DONE (TEST-OBSOLETE REMOVE) + REBASELINE ci_absolute 102->91 (102 SUPERSEDED) (2026-06-09T10:45Z)
**task_id:** FIX-CI-1423E-PREEXISTING-CLUSTER
- **what-done:** Adjudicated the router CI gate signal docs/signals/ci-1423e-gate-result-9ed78225-20260609T1045Z.json (router raw-verified + already pushed, commit e5e65858) in ONE batched atomic board write (no concurrent writer — dev/architect idle). **(1) FIX-CI-1423E-PREEXISTING-CLUSTER TODO->DONE** (+done_by po-S32 / resolution DONE / done_at 2026-06-09T10:45:00Z / dj_gate_1 sprint-CI-RED-RECONCILE-po.md#po-S32 / gate_result CI-1423E-GATE-9ed78225): pattern = **TEST-OBSOLETE REMOVE** (per /goal clause: obsolete tests are REMOVED, not patched). The get_macro_calendar MCP tool was rewired domain->HTTP-proxy in commit 98df0f43, deleting the `_testReferenceDate` injection seam; apps/mcp-server/src/__tests__/1423e.test.ts (callMacroCalendar helper injected args["_testReferenceDate"]) depended ENTIRELY on that dead seam, so in CI (no port-5004 macro-indicators service) every call returned {error: macro-indicators service unavailable} cascading to ~22 structural fails. Architect verdict TEST-OBSOLETE (brief 2026-06-09-ci-1423e-preexisting-cluster.md, all 9 -> REMOVE); router raw-verified all 4 load-bearing claims; dev (dev-mcp-server) executed git rm 1423e.test.ts -> commit 9ed78225. **(2) REBASELINE ci_absolute 102->91** (sha 9ed78225, run 27200448050, job 80302822813, native_fail 91, native_errors 0, fail_plus_error 91, tests 11803, pass 11670, skip 42, updated_by po-S32, updated_at 2026-06-09T10:45:00Z, supersedes "102 (sha fff91143, run 27198910454, job 80297590558)"); new band ~91 ±3 (floor ~88); also synced long_tail_triage_policy.absolute mirror to 91.
- **gate evidence (router-verified, accepted; native-to-native, jitter-robust):** named **Task 1423 cluster 22 fail -> 0 fail** (per-victim exact-prefix tally across the REMOVE run vs the 102 baseline — UNAMBIGUOUS, zero residual); tests **-13** (11816->11803, exactly the removed file's 13 it() cases); **zero coverage lost** — protected sibling apps/mcp-server/src/__tests__/1423e-macro-calendar.test.ts (different filename, exercises macroCalendar.ts domain service directly) is UNTOUCHED and still **23/0**. The deleted file only tested the dead MCP `_testReferenceDate` seam, never the domain logic.
- **what-considered:**
  - DONE vs hold REVIEW: **DONE** (CHOSEN). The named per-victim flip (22->0) is the authoritative jitter-robust signal and is unambiguous; the architect TEST-OBSOLETE ruling + router raw-verification of all 4 claims + the protected-sibling 23/0 coverage check leave nothing to review.
  - rebaseline 102->91 vs keep 102: **rebaseline** (CHOSEN). The -11 absolute is direction-correct and the test SET changed (file removed), so the OLD 102 absolute no longer describes the suite; 102 explicitly SUPERSEDED. The +11 vs the ~80 projection is NOT a regression — removing a test FILE reorders the bun ESM-cache mock.module() contamination graph, shifting jitter on unrelated tests beyond the ±3 band (the absolute is only stable when the test SET is unchanged). The named per-victim 22->0 flip — not the absolute — is the gate of record.
  - REMOVE vs REWRITE the test: **REMOVE** (CHOSEN — architect ruling, /goal obsolete-test clause). Prod path is now an HTTP {days} proxy with no `_testReferenceDate` seam to inject; the test asserted against a seam that no longer exists. Rewriting would re-create a seam prod deleted; the domain logic is already covered by the protected sibling. Obsolete -> DELETE.
- **why-decision:** PO closed the architect-triaged Task-1423e cluster as DONE on the unambiguous router gate (22->0 named flip, zero coverage lost), rebaselined the absolute to the verified 91 floor, and recorded the TEST-OBSOLETE REMOVE pattern — driving native fail+error toward 0 with the prod-vs-test REMOVE-if-obsolete discipline intact.
- **why-change:** ADVANCES po-S31 (which OPENED 1423e to architect-first triage against the new 102 floor). The architect confirmed TEST-OBSOLETE, dev removed the file, the router gate passed -> 1423e closes; the 102 floor that stood since po-S31 retires to 91.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-1423E-PREEXISTING-CLUSTER TODO->DONE** (+done_by po-S32 / resolution DONE / done_at 2026-06-09T10:45:00Z / gate_result CI-1423E-GATE-9ed78225 / dj_gate_1 #po-S32 / root_cause rewritten UNKNOWN->TEST-OBSOLETE REMOVE). **ci_absolute REBASELINED 102->91 / sha fff91143->9ed78225 / updated_by po-S31->po-S32 (102 SUPERSEDED — verified by jq post-write)**; long_tail_triage_policy.absolute mirror synced to 91 (+ updated_by/updated_at po-S32). Single status key verified across all 223 tasks (paths(scalars) status length==1 for all -> OK). Sprint .tasks count UNCHANGED (1 in-place flip, 0 add/remove). Unique task-id count UNCHANGED = **223**. Valid JSON; final size 763279 (>600000). signal_queue preserved (no whole-object rewrite). ONE atomic jq pass (scripts/po-s32-1423e-done-rebaseline-91.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2 (DONE is terminal — this flip REDUCES open work). Committed in the SAME commit as this journal entry. NOT pushed (router owns push + CI verification).
- **dispatch ask (router owns):** none new — the gate already passed and was pushed (e5e65858). Continue the /goal loop toward native fail+error = 0 on the remaining ~91: next clusters C7 (assertion-logic: 1792-conviction-debounce, 1352a L248, 1328e/1407b) and C8 (REMOVE-obsolete-triage FIX-CI-C8-REMOVE-OBSOLETE-TESTS-TRIAGE — architect REMOVE-evidence first, protect DWF-is-trading-day canary); Cluster-6 schema-drift stays PARKED. Optional queue-to-PO: architect-recommended lint gate (carryTools.ts/macroTools.ts change without co-updated test file = CI warning — recurring 'MCP tool rewired to HTTP, test not co-updated' pattern). No push — router owns push + next gate.

### STEP po-S33 (DJ-GATE-1): SPIKE-CI-C1-MACRO-INJECT-SEAM verdict adjudicated -> SPIKE DONE + DISPATCH FIX-CI-C1-MACRO-INJECT-SEAM-TESTS (dev-mcp-server, ~71/91 dominant lever) + RE-PROFILE-CI-241-RESIDUAL REVIEW->DONE(SUPERSEDED) (2026-06-09T10:57:16Z)
**task_id:** SPIKE-CI-C1-MACRO-INJECT-SEAM, RE-PROFILE-CI-241-RESIDUAL, FIX-CI-C1-MACRO-INJECT-SEAM-TESTS
- **what-done:** Triaged the post-1423e-gate board (live floor 91, errors 0, sha 9ed78225). Picked the single highest-leverage actionable item: **C1 MACRO_INJECT_SEAM = ~71 of the 91 residual fails** (dominant lever). Read the architect spike deliverable brief (docs/architecture-briefs/2026-06-09-spike-ci-c1-macro-inject-seam.md) end-to-end. **(1) SPIKE-CI-C1-MACRO-INJECT-SEAM REVIEW->DONE** (+done_by po-S33 / resolution DONE / done_at 2026-06-09T10:57:16Z / dj_gate_1 #po-S33): the spike delivered an unambiguous verdict + ordered fix plan + C5-cure-compliant injection template. VERDICT = **(A) INTENTIONAL seam removal + partial (B) format change**: commit 98df0f43 (2026-05-23, P2-B1 HTTP rewire) intentionally removed the _testCommodityClient/_testSbvClient/_testDinhGiaInputs seams; get_macro_snapshot is now a thin HTTP proxy to the Go macro-indicators service (port 5004) emitting {source_tier,text:JSON,fetchedAt} with no human-readable section headers. Production signals are CORRECT and live — NO prod regression, NO prod change required for CI. **(2) DISPATCHED FIX-CI-C1-MACRO-INJECT-SEAM-TESTS** (NEW, TODO, dev-mcp-server, type FIX, size M, zone apps/mcp-server/src/__tests__/, baseline_pass=91) carrying the architect-confirmed approach verbatim: ~40 tests rewrite dead _test* params -> scoped globalThis.fetch mock in beforeAll/afterAll (EXACT template 1881a-source-tier.test.ts L92-135, already 20/0); ~31 tests rewrite section-header assertions -> {source_tier,text:JSON,fetchedAt} wrapper + inner JSON field checks; 5 named files with surgical scope limits (pure formatThienThoi/formatDinhGia units + 1881a reference DO-NOT-TOUCH); HARD C5-cure constraint NO new file-top/module-scope mock.module(); Sub-task C (Go formatter) DEFERRED. **(3) RE-PROFILE-CI-241-RESIDUAL REVIEW->DONE / resolution SUPERSEDED**: the stale 241/91afe344 absolute is retired — re-profiling already advanced through the C1 spike + the 1423e gate to the live 91 floor; the 241 taxonomy is no longer the attack-ranking basis. Closed as SUPERSEDED, not re-run.
- **what-considered:**
  - which lever this cycle — C1 (~71) vs C7/C8 (smaller) vs TA/MACRO go-lint: **C1** (CHOSEN). 71 of 91 = the dominant native-fail bucket; its spike already has a clean architect verdict + brief + template, so it is immediately dispatchable with zero further investigation. C7/C8 are smaller and architect-first-pending; the go-lint jobs are separate-job REVIEW items the router gates (see below) — not the biggest fail-count mover.
  - SPIKE DONE vs hold REVIEW: **DONE** (CHOSEN). The spike deliverable is complete — verdict, A/B partition (~40/~31), production-severity assessment, ordered 5-file fix plan, exact copy-paste injection template, recurring-bug guard. Nothing left to review; holding it blocks the dominant-lever dispatch.
  - dispatch as test-REWRITE vs REMOVE: **REWRITE** (CHOSEN — architect ruling). Unlike 1423e (whole file tested ONLY the dead seam -> REMOVE), the C1 tests still cover live macro signals served by the Go service; the seam moved (domain->fetch), so the injection point is rewritten, not the coverage deleted. Pure-helper units (formatThienThoi/formatDinhGia) already pass and stay untouched. Per /goal: stale-but-valid -> REWRITE.
  - RE-PROFILE close as SUPERSEDED vs re-run vs keep REVIEW: **SUPERSEDED** (CHOSEN). The 241 number is two rebaselines stale (241->102->91); the residual is now profiled into C1/C7/C8 against the live 91 floor. Re-running the 241 taxonomy would burn an architect timebox to re-derive a number already superseded. Closed.
  - new task id: **FIX-CI-C1-MACRO-INJECT-SEAM-TESTS** (CHOSEN — distinct from the prior DONE FIX-CI-C1-RESIDUAL-MACRO-FETCHER-TESTS; avoids reopening a closed id / under-scope charge-back).
- **why-decision:** PO adjudicated the architect C1 spike as a complete, unambiguous deliverable, closed it DONE, and routed the dominant 71/91 lever to dev-mcp-server with the exact fetch-mock DI-seam approach + surgical file scope + the absolute C5-cure constraint — the single highest-ROI next unit of work toward native fail+error = 0. Closed the stale 241 taxonomy as superseded to keep the board honest.
- **why-change:** ADVANCES po-S32 (which rebaselined to 91 after 1423e). The /goal loop now attacks the largest remaining cluster. After this fix gates, expect ~20-40 residual; next = C7 assertion-logic + C8 REMOVE-triage. Separately the two go-lint REVIEW items (FIX-TA-GOLANGCI-CONFIG-V2, FIX-MACRO-GO-DIRECTIVE) are router-gate items for "ci ALL passe" — left for router (NOT flipped here: TA config is migrated but go-lint still exits 1 on a pre-existing DIFFERENT violation FIX-TA-SANDBOX-DEPGUARD, so its job is not yet green).
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **SPIKE-CI-C1-MACRO-INJECT-SEAM REVIEW->DONE** (+done_by po-S33 / resolution DONE / done_at 2026-06-09T10:57:16Z / dj_gate_1 #po-S33); **RE-PROFILE-CI-241-RESIDUAL REVIEW->DONE** (+done_by po-S33 / resolution SUPERSEDED / done_at 2026-06-09T10:57:16Z / dj_gate_1 #po-S33 / note appended SUPERSEDED rationale); +1 new task **FIX-CI-C1-MACRO-INJECT-SEAM-TESTS** (TODO, dev-mcp-server, FIX, size M, zone apps/mcp-server/src/__tests__/, baseline_pass=91, dispatched_by po-S33). **ci_absolute UNCHANGED = 91 / sha 9ed78225 / updated_by po-S32 (no rebaseline — this cycle adjudicates a spike + dispatches a fix; the absolute moves only when the router gates the dev fix).** long_tail_triage_policy.absolute UNCHANGED = 91. Single status key to be verified across all tasks (paths(scalars) status length max=min=1). Sprint .tasks 35->36 (2 in-place flips + 1 addition). Unique task-id count 223->224 (+1, exactly FIX-CI-C1-MACRO-INJECT-SEAM-TESTS). Valid JSON; size to be >600000. signal_queue preserved (no whole-object rewrite — scoped jq map on .tasks only). ONE atomic jq pass (scripts/po-s33-c1-spike-done-dispatch.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2 (TODO is dispatch-trigger, not in_progress; 2 flips are REVIEW->DONE terminal which REDUCE open work). Committed in the SAME commit as this journal entry. NOT pushed (router owns push + the next CI gate vs the standing 91 absolute).
- **dispatch ask (router owns):** spawn **dev-mcp-server** run docs/agents/dev-mcp-server/flow/main.md on **FIX-CI-C1-MACRO-INJECT-SEAM-TESTS** (background) — apply the architect fetch-mock DI-seam rewrite across the 5 named files (scope limits + C5-cure NO-mock.module() constraint in the task note); the dev COMMITS with explicit pathspec, the ROUTER pushes + gates. Gate: native fail+error must DROP below 91 (band +/-3, aim ~20-40 residual) AND each of the 5 named macro test files flips its tests fail->pass individually in the CI log (use scripts/ci-native-gate-watch.sh against the new run). Separately, the router OWNS the two go-lint REVIEW items toward "ci ALL passe": (a) FIX-TA-GOLANGCI-CONFIG-V2 — config migrated (commit d73c7a40) but go-lint still exits 1 on pre-existing FIX-TA-SANDBOX-DEPGUARD violation; do NOT close until that job is green; (b) FIX-MACRO-GO-DIRECTIVE — go.mod go 1.25.0->1.22 align, verify on next push. PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S34 (DJ-GATE-1): CI-C1-ALREADY-SHIPPED-9ed78225 — FIX-CI-C1-MACRO-INJECT-SEAM-TESTS TODO->DONE (ALREADY-SHIPPED by 1d83a5ff, duplicate task) + NO REBASELINE (ci_absolute stays 91) + OPEN 3 NEW residual clusters 1124(24,DOMINANT)/1134(12)/1129(10) architect-first (2026-06-09T11:09:40Z)
**task_id:** FIX-CI-C1-MACRO-INJECT-SEAM-TESTS, FIX-CI-C1124-RESIDUAL-TRIAGE, FIX-CI-C1134-RESIDUAL-TRIAGE, FIX-CI-C1129-RESIDUAL-TRIAGE
- **what-done:** Adjudicated router gate signal docs/signals/ci-c1-already-shipped-9ed78225-20260609T1110Z.json (router raw-verified; evidence block trusted) in ONE batched atomic board write (no concurrent writer — dev idle, no mutex held). **(1) FIX-CI-C1-MACRO-INJECT-SEAM-TESTS TODO->DONE** (+done_by po-S34 / resolution "ALREADY-SHIPPED by 1d83a5ff; six victim prefixes 0-fail in 91-run; duplicate task object" / done_at 2026-06-09T11:09:40Z / gate_result CI-C1-ALREADY-SHIPPED-9ed78225 / dj_gate_1 sprint-CI-RED-RECONCILE-po.md#po-S34): the C1 macro-seam test rewrite ALREADY shipped as commit 1d83a5ff (Tue 2026-06-09 08:38), which `git merge-base --is-ancestor 1d83a5ff 9ed78225` confirms is an ancestor of the standing 91-run sha; po-S25 (commit 39cf167d) already rebaselined 241->193 citing CI-C3-C1-GATE-1d83a5ff. The re-dispatched dev-mcp-server agent correctly detected the fix was already committed and added ZERO new commits (idempotent NO-OP). All SIX C1 victim prefixes are 0 fails in the 91-run CI log (089=0, 1423d=0, 1423f=0, 1426c=0, 1570c=0, 1903a=0, raw-tallied vs bun_job 80302822813); dead-seam grep hits are JSDoc/`//` comments only; tsc clean; local bun 5 files 58/0. NO prod or test change required for C1 — this was a DUPLICATE task object. **(2) NO REBASELINE — ci_absolute stays native_fail 91 / native_errors 0 / sha 9ed78225 / run 27200448050 / job 80302822813 / updated_by po-S32 (UNCHANGED).** C1 was already counted INSIDE the standing 91; flipping the duplicate task to DONE adds ZERO improvement, so the absolute MUST NOT move. long_tail_triage_policy.absolute also UNCHANGED = 91. **(3) OPENED 3 NEW residual clusters** from the TRUE raw 91-run fail-prefix distribution (the po-S33 "~71/91 dominant lever" premise was STALE/WRONG): **FIX-CI-C1124-RESIDUAL-TRIAGE** (Task 1124 = 24 fails, NEW DOMINANT LEVER, TODO, owner **architect**, type FIX, size M, zone apps/mcp-server/src/__tests__/, root_cause UNKNOWN, baseline_pass=91) — architect-first prod-vs-test triage per /goal REMOVE-if-obsolete (confirm whether prod changed/was deleted BEFORE any fix); **FIX-CI-C1134-RESIDUAL-TRIAGE** (Task 1134 = 12, NEW, TODO, architect, size S, baseline_pass=91) stub; **FIX-CI-C1129-RESIDUAL-TRIAGE** (Task 1129 = 10, NEW, TODO, architect, size S, baseline_pass=91) stub. C7 (1407b 12 / 1328e 10 / 1792 / 1352a) and C8 already tracked (FIX-CI-C7-ASSERTION-LOGIC + FIX-CI-C8-REMOVE-OBSOLETE-TESTS-TRIAGE, both BACKLOG) — NOT duplicated.
- **what-considered:**
  - C1 DONE-as-ALREADY-SHIPPED vs reopen/redispatch: **DONE** (CHOSEN). Router raw-verified the fix is an ancestor of the live sha AND all six victim prefixes are 0-fail; the re-dispatch added zero commits (idempotent). Nothing to do — close the duplicate honestly.
  - rebaseline vs keep 91: **keep 91** (CHOSEN). The C1 fix landed BEFORE the 9ed78225 run, so its benefit is ALREADY inside the 91 absolute. Re-counting it would fabricate a phantom improvement. The absolute moves only when a fix lands that is NOT yet in the gated sha — not the case here.
  - 1124 owner architect vs dev: **architect** (CHOSEN). Root cause UNKNOWN; /goal REMOVE-if-obsolete clause requires architect prod-vs-test confirmation (changed/deleted prod?) BEFORE any dev fix — same discipline that closed 1423e as TEST-OBSOLETE and avoided patching a dead-seam test. Modeled on the existing C8 architect-triage pattern.
  - open 1134/1129 now vs defer: **open as stubs now** (CHOSEN). Captures the TRUE residual distribution while the raw tally is fresh; keeps the board the honest attack-ranking after the stale ~71/91 premise was corrected. Sequenced after the dominant 1124.
- **why-decision:** PO closed the duplicate C1 task as ALREADY-SHIPPED on unambiguous router evidence (ancestor-of-sha + six victim 0-fail + zero new commits), correctly DID NOT rebaseline (C1 already inside 91), and re-pointed the /goal loop at the TRUE dominant lever Task 1124 (24) via architect-first triage — correcting the stale po-S33 ~71/91 premise and keeping the absolute honest.
- **why-change:** SUPERSEDES the po-S33 premise (which dispatched FIX-CI-C1-MACRO-INJECT-SEAM-TESTS believing C1 was the ~71/91 dominant lever; raw tally proved C1 was already 0-fail and 1124 is the real dominant 24-fail bucket). The /goal loop now attacks 1124 (24) -> 1134 (12) -> 1129 (10), with C7/C8 already queued. After 1124 gates, expect ~67 residual.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1-MACRO-INJECT-SEAM-TESTS TODO->DONE** (+done_by po-S34 / resolution ALREADY-SHIPPED / done_at 2026-06-09T11:09:40Z / gate_result CI-C1-ALREADY-SHIPPED-9ed78225 / dj_gate_1 #po-S34); +3 NEW tasks **FIX-CI-C1124-RESIDUAL-TRIAGE** (TODO, architect, FIX, size M, root_cause UNKNOWN, baseline_pass=91), **FIX-CI-C1134-RESIDUAL-TRIAGE** (TODO, architect, FIX, size S, baseline_pass=91), **FIX-CI-C1129-RESIDUAL-TRIAGE** (TODO, architect, FIX, size S, baseline_pass=91). **ci_absolute UNCHANGED = 91 / sha 9ed78225 / updated_by po-S32 (NO rebaseline — C1 already counted inside 91; verified by jq post-write).** long_tail_triage_policy.absolute UNCHANGED = 91. Single status key verified across ALL tasks (paths(scalars) status length==1 for all -> []  = OK). Sprint .tasks 36->39 (1 in-place flip + 3 additions). Unique task-id count 224->227 (+3, exactly the three new residual clusters; no existing id dropped). Valid JSON; final size 770153 (>600000). signal_queue preserved (scoped jq map on active_sprints[24].tasks only — no whole-object rewrite). ONE atomic jq pass (scripts/po-s34-c1-already-shipped-open-residual-clusters.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) = 0 <= 2 (DONE is terminal — REDUCES open work; the 3 new TODOs are dispatch-triggers, not in_progress). Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + po notebook). NOT pushed (router owns push + the next CI gate).
### STEP architect-A1: FIX-CI-C1124-RESIDUAL-TRIAGE triage (2026-06-09T11:25Z)
**task_id:** FIX-CI-C1124-RESIDUAL-TRIAGE
- **what-done:** Full brownfield scan: test file (1124), prod handler (evidenceTools.ts), 3 infra stores, schema DDL (schema-system.ts), initDatabase, bunfig, CI signals (c1-already-shipped, c5-gate, 1423e-gate), git log 1124 file, sibling test 1117. Local run: 12/0 pass in isolation.
- **verdict:** REWRITE. Prod correct. Schema fixture complete (all 4 tables in initSystemTables lines 138/156/172/187). 24 CI fails = 12 it() × 2 (test timeout + afterEach stall) from InMemoryTransport+Client.callTool() hanging on CI Linux runner (Bun 1.3.13). Prior C3 afterEach fix (a43dff49) addressed inter-test stall; intra-test callTool() stall persists. Fix: replace InMemoryTransport+Client with _registeredTools direct handler invocation (1117 sibling pattern, CI-proven). NO mock.module().
- **what-considered:** REMOVE (no — prod seam not deleted), FIX-prod (no — pure SQLite handlers, no async defect), REWRITE (YES — transport layer stall).
- **why-change:** InMemoryTransport+Client is the only variable that differs between local (pass) and CI (fail). Direct _registeredTools invocation removes the transport layer entirely.
- **DJ-GATE-1:** FIX-CI-C1124-RESIDUAL-TRIAGE TODO → REVIEW. brief written: docs/architecture-briefs/2026-06-09-ci-c1124-evidence-tools-triage.md. orch-state task status updated (root_cause filled, reviewed_at stamped). Notebook updated. Committed with explicit pathspec.

- **dispatch ask (router owns):** spawn **architect** run docs/agents/architect/flow/main.md on **FIX-CI-C1124-RESIDUAL-TRIAGE** (background) — prod-vs-test triage of the 24-fail Task-1124 cluster BEFORE any dev fix: confirm whether prod changed/was deleted (REWRITE if prod-correct+test-stale / FIX if prod-broken / REMOVE if test-obsolete), output a brief with the per-file ruling + C5-cure-compliant DI template; then PO dispatches dev-mcp-server with the verdict; router pushes + gates vs the standing 91 band. 1134 (12) and 1129 (10) sequence after 1124. C7/C8 already queued; go-lint REVIEW items (FIX-TA-GOLANGCI-CONFIG-V2, FIX-MACRO-GO-DIRECTIVE) remain router-owned toward "ci ALL passe"; Cluster-6 schema-drift PARKED. PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S35 (DJ-GATE-1): C1124 architect REWRITE verdict accepted (TRIAGE REVIEW->DONE) + dispatch FIX-CI-C1124-EVIDENCE-TESTS-REWRITE (dev-mcp-server) + 2 Go-lint REVIEW->DONE (CI-VERIFIED-GREEN) + NO REBASELINE (ci_absolute stays 91) (2026-06-09T11:32:46Z)
**task_id:** FIX-CI-C1124-RESIDUAL-TRIAGE, FIX-CI-C1124-EVIDENCE-TESTS-REWRITE, FIX-MACRO-GO-DIRECTIVE, FIX-TA-GOLANGCI-CONFIG-V2
- **what-done:** Adjudicated TWO router-verified inputs in ONE batched atomic board write under commit-mutex. **(1) Accepted the architect REWRITE verdict** (brief docs/architecture-briefs/2026-06-09-ci-c1124-evidence-tools-triage.md, commit f0900f74) for **FIX-CI-C1124-RESIDUAL-TRIAGE REVIEW->DONE** (+done_by po-S35 / resolution REWRITE-accepted / done_at 2026-06-09T11:32:46Z / dj_gate_1 #po-S35): prod get_evidence_summary/create_prediction_claim is CORRECT — all 4 evidence tables (evidence_scores/evidence_fragments/evidence_likelihood_ratios/prediction_claims) are created by initSystemTables inside initDatabase (schema-system.ts lines 138/156/172/187), NO prod seam deleted; the 24 fails are test-infra-only (InMemoryTransport+Client.callTool() message loop stalls on Bun 1.3.13/Ubuntu CI -> 12 it() x 2 [test 5000ms timeout + afterEach stalled-client close] = 24 native fails). **(2) OPENED FIX-CI-C1124-EVIDENCE-TESTS-REWRITE** (TODO, owner **dev-mcp-server**, type FIX, size M, zone apps/mcp-server/src/__tests__/, baseline_pass=91, scope=ONLY 1124-evidence-tools-phase-bc.test.ts, brief referenced): replace transport+Client with server._registeredTools[name].handler() direct invocation (proven CI-green by sibling 1117-evidence-tools.test.ts on the SAME prod module; preferred 1881a pattern: beforeEach initDatabase + module-level _testDb/_testServer, afterEach closeDb). Acceptance: Task 1124 24->0 native fails in CI, NO new mock.module() (C5-cure ABSOLUTE — import chain pure SQLite), tsc clean. **(3) FIX-MACRO-GO-DIRECTIVE REVIEW->DONE** (+done_by po-S35 / resolution CI-VERIFIED-GREEN / gate_result CI-GO-PY-LINT-ALL-GREEN-39ee3464 / dj_gate_1 #po-S35): Macro Indicators Go Lint job = success across 3 CI runs (946d72a/e5e6585/7640616; runs 27201263379/27201056956/27200579594), go.mod go 1.25.0->1.22 landed. **(4) FIX-TA-GOLANGCI-CONFIG-V2 REVIEW->DONE** (+done_by po-S35 / resolution CI-VERIFIED-GREEN / gate_result CI-GO-PY-LINT-ALL-GREEN-39ee3464 / dj_gate_1 #po-S35): go-lint (TA) job = success across the same 3 runs; the prior FIX-TA-SANDBOX-DEPGUARD blocker is no longer on the board and the job no longer exits 1. Both Go closures per router signal docs/signals/ci-go-py-lint-all-green-39ee3464-20260609T1120Z.json (router raw-verified the entire Go+Python lint surface GREEN+STABLE x3 runs; SOLE remaining red = bun test 91).
- **what-considered:**
  - C1124-TRIAGE accept-REWRITE vs reject/re-triage: **accept** (CHOSEN). Architect brief is a complete brownfield scan with file:line evidence, schema-fixture confirmation (4 tables present), and a CI-proven sibling pattern (1117) on the identical prod module; router raw-confirmed 1117 uses _registeredTools direct callTool. Nothing to re-triage — verdict is sound and actionable. Test-only, regression surface ZERO.
  - C1124 fix owner dev-mcp-server vs architect: **dev-mcp-server** (CHOSEN). Triage is done; the rewrite is in-zone test mechanics (apps/mcp-server), exactly dev-mcp-server's lane.
  - Go-lint items REVIEW->DONE now vs leave for router: **close now** (CHOSEN, per router po_action_requested). po-S34 had deliberately LEFT these for the router because at that time TA go-lint still exited 1 on the pre-existing FIX-TA-SANDBOX-DEPGUARD violation. That blocker is now off the board AND the jobs are success x3 consecutive runs (3-run green stability defeats jitter) — the close condition is now met, and the router explicitly asked PO to batch them in on this board write.
  - rebaseline vs keep 91: **keep 91** (CHOSEN). This cycle adjudicates a triage + dispatches a test-fix + closes two Go-lint items that are ALREADY green inside the gated surface; ci_absolute tracks bun ONLY and no bun change has landed yet. The absolute moves only when the router gates the dev rewrite against a NEW run.
- **why-decision:** PO accepted a sound architect REWRITE verdict and routed the dominant 24-fail Task-1124 cluster to dev-mcp-server with surgical single-file scope + the C5-cure NO-mock.module() hard constraint + the CI-proven 1117/1881a _registeredTools template — the single highest-ROI unit toward bun-test 91->0. Simultaneously closed the two Go-lint REVIEW items on 3-run CI-green evidence (the SOLE remaining red is now unambiguously bun test = 91), keeping the board the honest attack-ranking.
- **why-change:** ADVANCES po-S34 (which opened C1124-TRIAGE architect-first and left the Go-lint items for the router pending the SANDBOX-DEPGUARD blocker clearing). The architect returned REWRITE; PO accepts and dispatches the dev fix. The Go-lint close-condition is now met (blocker gone + 3-run green), so PO batches both REVIEW->DONE per the router's explicit po_action_requested. The /goal "ci/di all passe" surface now reduces ENTIRELY to bun test 91->0; the next lever is FIX-CI-C1124-EVIDENCE-TESTS-REWRITE (24->0).
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1124-RESIDUAL-TRIAGE REVIEW->DONE** (+done_by po-S35 / resolution REWRITE-accepted / done_at 2026-06-09T11:32:46Z / dj_gate_1 #po-S35); **FIX-MACRO-GO-DIRECTIVE REVIEW->DONE** (+done_by po-S35 / gate_result CI-GO-PY-LINT-ALL-GREEN-39ee3464 / dj_gate_1 #po-S35); **FIX-TA-GOLANGCI-CONFIG-V2 REVIEW->DONE** (+done_by po-S35 / gate_result CI-GO-PY-LINT-ALL-GREEN-39ee3464 / dj_gate_1 #po-S35); +1 NEW task **FIX-CI-C1124-EVIDENCE-TESTS-REWRITE** (TODO, dev-mcp-server, FIX, size M, zone apps/mcp-server/src/__tests__/, baseline_pass=91, scope=ONLY 1124-evidence-tools-phase-bc.test.ts, brief docs/architecture-briefs/2026-06-09-ci-c1124-evidence-tools-triage.md, created_by po-S35). **ci_absolute UNCHANGED = native_fail 91 / native_errors 0 / sha 9ed78225 / run 27200448050 / job 80302822813 / updated_by po-S32 (NO rebaseline — bun-only metric, no bun change landed; verified by jq post-write).** long_tail_triage_policy.absolute UNCHANGED = 91. Single-status-key invariant verified across ALL tasks (each task has exactly 1 status key -> jq unique == [1] = OK). Unique task-id count 227->228 (+1, exactly FIX-CI-C1124-EVIDENCE-TESTS-REWRITE; comm diff confirms ZERO existing id dropped). Valid JSON; final size 774875 (>600000). signal_queue + all other objects preserved (scoped jq map on active_sprints[24].tasks only — no whole-object rewrite). ONE atomic jq pass (scripts/po-s35-c1124-triage-done-open-rewrite.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. _schema=v3 _ssot=true preserved. WIP in_progress (non-terminal) unchanged (3 REVIEW->DONE are terminal — REDUCE open work; the 1 new TODO is a dispatch-trigger, not in_progress). Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + po notebook + jq script). NOT pushed (router owns push + the next CI gate vs the standing 91 band).
- **dispatch ask (router owns):** spawn **dev-mcp-server** run docs/agents/dev-mcp-server/flow/main.md on **FIX-CI-C1124-EVIDENCE-TESTS-REWRITE** (background) — rewrite ONLY apps/mcp-server/src/__tests__/1124-evidence-tools-phase-bc.test.ts: remove Client + InMemoryTransport imports + _currentClient; replace makeTestSetup()+transport callTool with the preferred 1881a pattern (beforeEach initDatabase + module-level _testDb/_testServer; afterEach closeDb; callTool(toolName,args) via _testServer._registeredTools[toolName].handler). Seed helpers (seedEvidenceScore/seedEvidenceFragment/seedLikelihoodRatio) unchanged. HARD constraint: NO new mock.module() (C5-cure ABSOLUTE). The dev COMMITS with explicit pathspec; the ROUTER pushes + gates: Task 1124 native fail 24->0 in CI AND no new mock.module() AND tsc --noEmit clean, vs the standing 91 band. After this gates, the SOLE remaining red surface is the bun-test residual (expect ~67). PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S36 (DJ-GATE-1): CI-C1124-GATE-802a4d1b PASSED — FIX-CI-C1124-EVIDENCE-TESTS-REWRITE REVIEW->DONE (REWRITE-GATED) + REBASELINE ci_absolute 91->79 (91 SUPERSEDED) + sync long_tail.absolute->79 (2026-06-09T11:53:25Z)
**task_id:** FIX-CI-C1124-EVIDENCE-TESTS-REWRITE
- **what-done:** ONE batched atomic board write under commit-mutex on router gate-pass signal docs/signals/ci-c1124-gate-result-802a4d1b-20260609T1150Z.json (CI-C1124-GATE-802a4d1b). **(1) FIX-CI-C1124-EVIDENCE-TESTS-REWRITE REVIEW->DONE** (+done_by po-S36 / resolution REWRITE-GATED / done_at 2026-06-09T11:53:25Z / gate_result CI-C1124-GATE-802a4d1b / dj_gate_1 #po-S36): router raw-verified + gate-watched run 27203749620 to completion; named Task 1124 cluster (all 1124* prefixes) went **24 native fail -> 0** (jitter-robust per-victim exact-prefix tally, UNAMBIGUOUS); test-infra REWRITE confirmed (InMemoryTransport+Client.callTool() harness replaced with _registeredTools[name].handler() direct invocation, 1117/1881a pattern; zero new mock.module() [C5-cure honored]; tsc clean; 12/12 local pass; all 12 it() + 3 seed helpers unchanged = zero coverage lost). Dev landed it on sha 802a4d1b (job 80314056985). **(2) REBASELINE ci_absolute 91 -> 79** (native_fail 79 / native_errors 0 / fail_plus_error 79 / tests 11803 / pass 11682 / skip 42 / sha 802a4d1b / run 27203749620 / bun_job_id 80314056985 / supersedes "91 (sha 9ed78225, run 27200448050, job 80302822813)" / updated_by po-S36). **(3) long_tail_triage_policy.absolute synced -> "79 (sha 802a4d1b, run 27203749620, job 80314056985 — SUPERSEDES 91/9ed78225)"** (updated_by po-S36).
- **what-considered:**
  - Trust the named per-victim 24->0 flip vs distrust the +12-vs-projection native absolute: **trust the named flip** (CHOSEN). The authoritative jitter-robust gate is the per-victim exact-prefix tally (Task 1124 24->0, zero residual), NOT the native absolute. Projected ~67 (91-24) but landed 79; the +12 gap is NOT a regression — rewriting the harness (removing InMemoryTransport/Client imports) reorders the bun ESM-cache mock.module() contamination graph, shifting jitter on OTHER unrelated tests beyond the +/-3 band (the absolute is only stable when the import/test SET is unchanged). This caveat is the SAME mechanism po-S32 documented for the 1423e REMOVE (+11 vs projection). Carried verbatim in delta_vs_prior.
  - Rebaseline now vs keep 91: **rebaseline to 79** (CHOSEN, per router po_action_requested). Unlike po-S35 (a triage+dispatch with NO bun change landed), a bun test FILE was rewritten + pushed + gated on a NEW run (802a4d1b != 9ed78225). The absolute moves only when the router gates a dev change against a new run — that condition is now met. New band ~79 +/-3 (floor ~76).
- **why-decision:** PO closed the dominant 24-fail Task-1124 cluster as DONE on unambiguous router gate evidence (named per-victim 24->0, gate-watched to completion), correctly rebaselined the bun absolute to the gated new-run value 79, and preserved the ESM-jitter caveat so the +12-vs-projection is not mis-read as regression. The /goal "ci ALL passe" surface is now bun test 79 (sole remaining red); next lever FIX-CI-C1134-RESIDUAL-TRIAGE (12) then FIX-CI-C1129-RESIDUAL-TRIAGE (10).
- **why-change:** ADVANCES po-S35 (which dispatched the rewrite + kept 91 pending a gated run). The router gated run 27203749620 GREEN-for-scope (Task 1124 0-fail); PO accepts, flips DONE, and rebaselines 91->79. 91 is SUPERSEDED.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1124-EVIDENCE-TESTS-REWRITE REVIEW->DONE** (+done_by po-S36 / resolution REWRITE-GATED / done_at 2026-06-09T11:53:25Z / gate_result CI-C1124-GATE-802a4d1b / dj_gate_1 #po-S36). **ci_absolute REBASELINED 91->79** (native_fail 79 / native_errors 0 / fail_plus_error 79 / tests 11803 / pass 11682 / skip 42 / sha 802a4d1b / run 27203749620 / job 80314056985 / supersedes 91-9ed78225 / updated_by po-S36; delta_vs_prior preserves the +12-vs-projection ESM-jitter caveat — named per-victim flip authoritative; new band ~79 +/-3 floor ~76). **long_tail_triage_policy.absolute synced -> 79** (updated_by po-S36). Single-status-key invariant verified across ALL tasks (each task exactly 1 status key). Unique task-id count = 228 UNCHANGED (status flip only — NO new id, NO id dropped). Valid JSON; final size >600000. signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24] ONLY — no whole-object rewrite). ONE atomic jq pass (scripts/po-s36-c1124-done-rebaseline.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + po notebook + the jq script ONLY). NOT pushed (router owns push).

### STEP po-S37 (DJ-GATE-1): C1134 architect REWRITE verdict accepted (TRIAGE REVIEW->DONE) + OPEN FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE (dev-mcp-server) + NO REBASELINE (ci_absolute stays 79) (2026-06-09T12:07:53Z)
**task_id:** FIX-CI-C1134-RESIDUAL-TRIAGE, FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE
- **what-done:** ONE batched atomic board write under commit-mutex adjudicating the router-verified architect C1134 triage (brief docs/architecture-briefs/2026-06-09-ci-c1134-foreign-flow-triage.md, commit 16e11df8, router-verified + PUSHED, HEAD=origin=ee714e6c). **(1) FIX-CI-C1134-RESIDUAL-TRIAGE REVIEW->DONE** (+done_by po-S37 / resolution "REWRITE-GATED — dev to rewrite test harness, no prod change" / done_at 2026-06-09T12:07:53Z / dj_gate_1 #po-S37): architect returned **REWRITE (NOT remove, NOT fix-prod)** — prod get_foreign_flow (foreignFlowTools.ts) is CORRECT: pure SQLite, imports analyzeForeignFlow (domain) + getForeignFlowHistory (infra) with NO fetch/HTTP/baseUrl (no HTTP-proxy rewire); the db? injection arg in registerForeignFlowTools(server, db?) (lines 132-135) ALREADY exists and is used correctly by the test on every call; the _testFallback seam (line 165) is live, NOT deleted. All 12 fails = **1124-transport-hang signature** (IDENTICAL to the just-cured Task 1124): 1134-get-foreign-flow-tool.test.ts uses buildConnectedPair() (InMemoryTransport + Client.connect pair) + client.callTool() with NO afterEach cleanup; the InMemoryTransport message loop stalls in CI (Bun 1.3.13/Ubuntu single-process) -> 6 it() x 2 native fails = 12. No prod bug -> REWRITE only. **(2) OPENED FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE** (TODO, owner **dev-mcp-server**, type FIX, size S, priority high, zone apps/mcp-server/src/__tests__/, baseline_pass=91, brief referenced, scope=ONLY 1134-get-foreign-flow-tool.test.ts, created_by po-S37): DI template (architect-prescribed) = remove InMemoryTransport + Client imports + buildConnectedPair(); add module-level _testServer + _testDb set in beforeEach (registerForeignFlowTools(_testServer, _testDb)); replace client.callTool({name,arguments}) with await _testServer._registeredTools["get_foreign_flow"].handler(args). buildInMemoryDb() + seed helpers unchanged (only daily_ohlcv queried on injected-db path; no initDatabase needed). HARD constraint: NO new mock.module() (C5-cure ABSOLUTE — import chain is pure SQLite, zero mock needed). AC-4 caveat (days=35 Zod validation): with direct-handler invocation, dev to verify whether handler(args) returns {isError:true} OR throws on invalid input; if it throws, wrap in try/catch and assert the thrown error message instead of the isError field. Acceptance: Task 1134 12->0 native fails in CI, tsc clean, NO new mock.module(). Proven CI-green pattern: 1117, 1124 (24->0), 089, 1881a.
- **what-considered:**
  - C1134-TRIAGE accept-REWRITE vs reject/re-triage: **accept** (CHOSEN). Architect brief is a complete brownfield scan with file:line evidence (foreignFlowTools.ts:22-29/132-135/165; test:81-91/9/107..177), confirms zero prod change (git log -- foreignFlowTools.ts = 4 commits, no HTTP-proxy commit), and cites the SAME CI-proven 1124/1117 _registeredTools cure on the identical transport-hang mechanism. Verdict is sound + actionable; test-only, regression surface ZERO. Nothing to re-triage.
  - fix owner dev-mcp-server vs architect: **dev-mcp-server** (CHOSEN). Triage done; the rewrite is in-zone single-file test mechanics (apps/mcp-server) — exactly dev-mcp-server's lane.
  - REWRITE vs REMOVE: **REWRITE** (CHOSEN, per /goal). The 6 it() exercise genuine prod MCP-integration behavior (zero-detection guard, insufficient-data guard, direction/severity logic, Zod gate, JSON-envelope) NOT covered by any sibling test at the MCP-tool level (domain analyzeForeignFlow tested separately). Removing would lose coverage; prod is correct so there is nothing to fix. Test-stale harness -> rewrite.
  - rebaseline vs keep 79: **keep 79** (CHOSEN). This cycle adjudicates a triage + dispatches a test-fix; ci_absolute tracks bun ONLY and NO bun change has landed yet. The absolute moves only when the router gates the dev rewrite against a NEW run (mirrors po-S35).
- **why-decision:** PO accepted a sound architect REWRITE verdict and routed the dominant remaining 12-fail Task-1134 cluster to dev-mcp-server with surgical single-file scope + the C5-cure NO-mock.module() hard constraint + the CI-proven 1124/1117 _registeredTools["get_foreign_flow"].handler DI template + the AC-4 isError/throws caveat — the next-highest-ROI unit toward bun-test 79->0. The /goal red surface remains bun test 79 (sole red); next lever = this rewrite (12->0), then FIX-CI-C1129-RESIDUAL-TRIAGE (10).
- **why-change:** ADVANCES po-S36 (which closed C1124 + rebaselined 91->79 and named C1134 as the next lever). The architect returned REWRITE for C1134; PO accepts, flips TRIAGE DONE, and dispatches the dev fix. No bun change landed -> ci_absolute UNCHANGED.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1134-RESIDUAL-TRIAGE REVIEW->DONE** (+done_by po-S37 / resolution "REWRITE-GATED — dev to rewrite test harness, no prod change" / done_at 2026-06-09T12:07:53Z / dj_gate_1 #po-S37); +1 NEW task **FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE** (TODO, dev-mcp-server, FIX, size S, priority high, zone apps/mcp-server/src/__tests__/, baseline_pass=91, scope=ONLY 1134-get-foreign-flow-tool.test.ts, brief docs/architecture-briefs/2026-06-09-ci-c1134-foreign-flow-triage.md, created_by po-S37). **ci_absolute UNCHANGED = native_fail 79 / native_errors 0 / fail_plus_error 79 / tests 11803 / pass 11682 / skip 42 / sha 802a4d1b / run 27203749620 / bun_job 80314056985 / updated_by po-S36 (NO rebaseline — bun-only metric, no bun change landed; verified by jq post-write).** long_tail_triage_policy.absolute UNCHANGED = 79. Single-status-key invariant verified across ALL tasks (jq select length!=1 -> [] = OK). Unique task-id count 228->229 (+1, exactly FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE; total==unique==229, ZERO existing id dropped). WIP non-terminal in_progress = 0 (<=2; the 1 new TODO is a dispatch-trigger, not in_progress; the REVIEW->DONE flip is terminal — REDUCES open work). Valid JSON; final size 780612 (>600000). signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24].tasks ONLY — no whole-object rewrite). ONE atomic jq pass (scripts/po-s37-c1134-triage-done-open-rewrite.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + the jq script). NOT pushed (router owns push + the next CI gate vs the standing 79 band).
- **dispatch ask (router owns):** spawn **dev-mcp-server** run docs/agents/dev-mcp-server/flow/main.md on **FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE** (background) — rewrite ONLY apps/mcp-server/src/__tests__/1134-get-foreign-flow-tool.test.ts: remove InMemoryTransport + Client imports + buildConnectedPair(); add module-level _testServer + _testDb in beforeEach (registerForeignFlowTools(_testServer, _testDb)); replace client.callTool({name,arguments}) with await _testServer._registeredTools["get_foreign_flow"].handler(args); buildInMemoryDb() + seed helpers unchanged. AC-4: verify direct-handler returns {isError:true} OR throws for days=35 — adapt assertion (try/catch on thrown message if it throws). HARD: NO new mock.module() (C5-cure ABSOLUTE); tsc --noEmit clean. The dev COMMITS with explicit pathspec; the ROUTER pushes + gates: Task 1134 native fail 12->0 in CI vs the standing 79 band. After this gates, next lever is FIX-CI-C1129-RESIDUAL-TRIAGE (10). PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S38 (DJ-GATE-1): CI-C1134-GATE-8916675a PASSED — FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE REVIEW->DONE (REWRITE-GATED) + REBASELINE ci_absolute 79->73 (79 SUPERSEDED) + sync long_tail.absolute->73 (2026-06-09T12:28:34Z)
**task_id:** FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE
- **what-done:** ONE batched atomic board write under commit-mutex on router gate-pass signal docs/signals/ci-c1134-gate-result-8916675a-20260609T1235Z.json (CI-C1134-GATE-8916675a; CI run 27205559638, sha 8916675a, bun_job 80320331863). **(1) FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE REVIEW->DONE** (+done_by po-S38 / resolution "REWRITE-GATED — Task 1134 12->0 named flip confirmed CI run 27205559638" / done_at 2026-06-09T12:28:34Z / gate_result CI-C1134-GATE-8916675a / dj_gate_1 #po-S38): router raw-verified + gate-watched run 27205559638 to completion; named Task 1134 cluster (all 1134* prefixes) went **12 native fail -> 0** (jitter-robust per-victim exact-prefix tally, UNAMBIGUOUS — entire get_foreign_flow cluster cured, zero residual); test-infra REWRITE confirmed (InMemoryTransport+Client.callTool()/buildConnectedPair() harness replaced with _registeredTools["get_foreign_flow"].handler() direct invocation, 1124-transport-hang signature, 1117/1124 proven; zero new mock.module() [C5-cure honored]; tsc clean; 6/6 local pass; all 6 it() + buildInMemoryDb() + seed helpers unchanged except the two protocol-layer Zod assertions = AC-4/AC-6, see adaptation note). Prod get_foreign_flow (foreignFlowTools.ts) untouched — pure SQLite, NO HTTP rewire, db? injection arg already present. Dev dev-mcp-server-S15 landed it on commit 617bf6dd + mutex-release 8916675a (router raw-verified linear/owned-paths-only + PUSHED 412a9e77..8916675a). **(2) REBASELINE ci_absolute 79 -> 73** (native_fail 73 / native_errors 0 / fail_plus_error 73 / tests 11803 / pass 11688 / skip 42 / sha 8916675a / run 27205559638 / bun_job_id 80320331863 / supersedes "79 (sha 802a4d1b, run 27203749620, job 80314056985)" / updated_by po-S38). **(3) long_tail_triage_policy.absolute synced -> "73 (sha 8916675a, run 27205559638, job 80320331863 — SUPERSEDES 79/802a4d1b)"** (updated_by po-S38).
- **what-considered:**
  - Trust the named per-victim 12->0 flip vs distrust the +6-vs-projection native absolute: **trust the named flip** (CHOSEN). The authoritative jitter-robust gate is the per-victim exact-prefix tally (Task 1134 12->0, zero residual), NOT the native absolute. Projected ~67 (79-12) but landed 73; the +6 gap is NOT a regression — rewriting the harness (removing InMemoryTransport/Client imports) reorders the bun ESM-cache mock.module() contamination graph, shifting jitter on OTHER unrelated tests beyond the +/-3 band (the absolute is only stable when the import/test SET is unchanged). SAME mechanism po-S36 documented for the 1124 REWRITE (+12 vs projection) and po-S32 for the 1423e REMOVE (+11). Carried verbatim in delta_vs_prior, including the AC-4/AC-6 Zod-bypass adaptation note (direct-handler bypasses the SDK protocol-layer Zod wrapper — AC-4 asserts no-crash+valid envelope; AC-6 passes days:10 explicitly; only the protocol-layer Zod path is no longer exercised by this file, which is an inherent property of the C5-cure 1124-template and acceptable per the architect REWRITE verdict).
  - Rebaseline now vs keep 79: **rebaseline to 73** (CHOSEN, per router po_action_requested). A bun test FILE was rewritten + pushed + gated on a NEW run (8916675a != 802a4d1b). The absolute moves only when the router gates a dev change against a new run — that condition is now met. New band ~73 +/-3 (floor ~70).
- **why-decision:** PO closed the remaining 12-fail Task-1134 cluster as DONE on unambiguous router gate evidence (named per-victim 12->0, gate-watched to completion), correctly rebaselined the bun absolute to the gated new-run value 73, and preserved the ESM-jitter + AC-4/AC-6 Zod-bypass caveats so the +6-vs-projection is not mis-read as regression. The /goal "ci ALL passe" surface is now bun test 73 (sole remaining red); next lever FIX-CI-C1129-RESIDUAL-TRIAGE (10), then C7 (1407b/1328e/1792/1352a), C8. Cluster-6 schema-drift PARKED.
- **why-change:** ADVANCES po-S37 (which accepted the architect REWRITE verdict + dispatched the dev rewrite + kept 79 pending a gated run). The router gated run 27205559638 GREEN-for-scope (Task 1134 0-fail); PO accepts, flips DONE, and rebaselines 79->73. 79 is SUPERSEDED.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1134-FOREIGN-FLOW-TEST-REWRITE REVIEW->DONE** (+done_by po-S38 / resolution REWRITE-GATED / done_at 2026-06-09T12:28:34Z / gate_result CI-C1134-GATE-8916675a / dj_gate_1 #po-S38). **ci_absolute REBASELINED 79->73** (native_fail 73 / native_errors 0 / fail_plus_error 73 / tests 11803 / pass 11688 / skip 42 / sha 8916675a / run 27205559638 / job 80320331863 / supersedes 79-802a4d1b / updated_by po-S38; delta_vs_prior preserves the +6-vs-projection ESM-jitter caveat + AC-4/AC-6 Zod-bypass note — named per-victim flip authoritative; new band ~73 +/-3 floor ~70). **long_tail_triage_policy.absolute synced -> 73** (updated_by po-S38). Single-status-key invariant verified across ALL tasks (jq select length!=1 -> [] = OK, 0 violations). Unique task-id count = 229 UNCHANGED (status flip + ci_absolute/long_tail field mutations only — NO new id, NO id dropped; total==unique==229). Valid JSON; final size 781121 (>600000). signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24] ONLY — no whole-object rewrite). ONE atomic jq pass (scripts/po-s38-c1134-done-rebaseline.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po-S38, ttl 120s) claimed BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + commit-mutex.json + the jq script). NOT pushed (router owns push).
- **dispatch ask (router owns):** continue /goal loop toward native fail+error = 0. Remaining ~73 native fail. Next lever (architect-first prod-vs-test confirm before any fix, per /goal REMOVE-obsolete clause): FIX-CI-C1129-RESIDUAL-TRIAGE (Task 1129, 10), then C7 (1407b/1328e/1792/1352a), C8. Cluster-6 schema-drift stays PARKED. PO did NOT push and did NOT rebaseline beyond this gated value this cycle.

### STEP po-S39 (DJ-GATE-1): C1129 architect REWRITE verdict accepted (TRIAGE TODO->DONE) + OPEN FIX-CI-C1129-CALIBRATION-TEST-REWRITE (dev-mcp-server) + NO REBASELINE (ci_absolute stays 73) (2026-06-09T12:39:26Z)
**task_id:** FIX-CI-C1129-RESIDUAL-TRIAGE, FIX-CI-C1129-CALIBRATION-TEST-REWRITE
- **what-done:** ONE batched atomic board write under commit-mutex adjudicating the router-verified architect C1129 triage (brief docs/architecture-briefs/2026-06-09-ci-c1129-calibration-triage.md, commit bc5c560a, arch-S13, router-verified + PUSHED). **(1) FIX-CI-C1129-RESIDUAL-TRIAGE TODO->DONE** (+done_by po-S39 / resolution "REWRITE-GATED — dev to rewrite test harness, no prod change" / done_at 2026-06-09T12:39:26Z / dj_gate_1 #po-S39): architect returned **REWRITE (NOT remove, NOT fix-prod)** — prod registerCalibrationTools (apps/mcp-server/src/interface/mcp/tools/macro/calibrationTools.ts:276, registerCalibrationTools(server, db?)) is CORRECT: pure SQLite, db-injection arg live, NOT HTTP-rewired; signatures (b) deleted-seam and (c) prod-bug both RULED OUT. The 10 fails = **1124-transport-hang signature** (IDENTICAL to the just-cured Task 1124/1134): 1129-calibration-tools.test.ts = 5 it() x ~5003ms transport stall x 2 (test timeout + afterEach stalled client) = 10 native fails. No prod bug -> REWRITE only. **(2) OPENED FIX-CI-C1129-CALIBRATION-TEST-REWRITE** (TODO, owner **dev-mcp-server**, type FIX, size S, priority high, zone apps/mcp-server/src/__tests__/, baseline_pass=11688, baseline_absolute="73 (sha 8916675a, run 27205559638, job 80320331863)", reference_template=1134-get-foreign-flow-tool.test.ts, signature=1124-transport-hang, victims="Task 1129 (10 fails, 5 it())", brief referenced, scope=ONLY 1129-calibration-tools.test.ts, created_by po-S39): CURE = replace InMemoryTransport+Client.callTool() harness with direct (_testServer as unknown as RegisteredToolsServer)._registeredTools["get_calibration_report"].handler(args); module-level _testServer + _testDb; beforeEach builds in-memory DB + seeds + registerCalibrationTools(_testServer, _testDb); afterEach cleanup; read result.content[0]?.text. buildInMemoryDb() must inline the calibration_snapshots DDL (12 cols, IF NOT EXISTS) from schema-system.ts:213 — NO initDatabase() call (Contract-B pattern). ZOD-BYPASS: NONE required — all 5 ACs port verbatim (date is z.string().optional(), no .default/.coerce). HARD constraint: NO new mock.module() (C5-cure ABSOLUTE). Acceptance: all 5 it() pass via direct handler, zero new mock.module(), tsc clean, gate target Task 1129 (fail) 10->0. Proven CI-green sibling template = 1134-get-foreign-flow-tool.test.ts (commit 8916675a).
- **what-considered:**
  - C1129-TRIAGE accept-REWRITE vs reject/re-triage: **accept** (CHOSEN). Architect brief is a complete brownfield scan with file:line evidence (calibrationTools.ts:276; test 5 it() x ~5003ms x2=10), confirms zero prod change, and cites the SAME CI-proven 1124/1134 _registeredTools cure on the identical transport-hang mechanism. Sound + actionable; regression surface ZERO. Nothing to re-triage.
  - fix owner dev-mcp-server vs architect: **dev-mcp-server** (CHOSEN). Triage done; the rewrite is in-zone single-file test mechanics (apps/mcp-server) — dev-mcp-server's lane.
  - REWRITE vs REMOVE: **REWRITE** (CHOSEN, per /goal). The 5 it() exercise genuine prod get_calibration_report MCP-tool behavior; prod is correct so nothing to fix; removing loses coverage. Test-stale harness -> rewrite.
  - rebaseline vs keep 73: **keep 73** (CHOSEN). This cycle adjudicates a triage + dispatches a test-fix; ci_absolute tracks bun ONLY and NO bun change has landed yet. The absolute moves only when the router gates the dev rewrite against a NEW run (mirrors po-S37).
- **why-decision:** PO accepted a sound architect REWRITE verdict and routed the co-dominant remaining 10-fail Task-1129 cluster to dev-mcp-server with surgical single-file scope + the C5-cure NO-mock.module() hard constraint + the CI-proven 1124/1134 _registeredTools["get_calibration_report"].handler DI template + the inline-DDL (Contract-B, NO initDatabase) note — the next-highest-ROI unit toward bun-test 73->0. The /goal red surface remains bun test 73 (sole red); next lever = this rewrite (10->0), then C7 (1407b/1328e/1792/1352a), C8.
- **why-change:** ADVANCES po-S38 (which closed C1134 + rebaselined 79->73 and named C1129 as the next lever). The architect returned REWRITE for C1129; PO accepts, flips TRIAGE DONE, and dispatches the dev fix. No bun change landed -> ci_absolute UNCHANGED.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1129-RESIDUAL-TRIAGE TODO->DONE** (+done_by po-S39 / resolution "REWRITE-GATED — dev to rewrite test harness, no prod change" / done_at 2026-06-09T12:39:26Z / dj_gate_1 #po-S39); +1 NEW task **FIX-CI-C1129-CALIBRATION-TEST-REWRITE** (TODO, dev-mcp-server, FIX, size S, priority high, zone apps/mcp-server/src/__tests__/, baseline_pass=11688, baseline_absolute="73 (sha 8916675a, run 27205559638, job 80320331863)", scope=ONLY 1129-calibration-tools.test.ts, brief docs/architecture-briefs/2026-06-09-ci-c1129-calibration-triage.md, reference_template 1134-get-foreign-flow-tool.test.ts, created_by po-S39). **ci_absolute UNCHANGED = native_fail 73 / native_errors 0 / fail_plus_error 73 / tests 11803 / pass 11688 / skip 42 / sha 8916675a / run 27205559638 / bun_job 80320331863 / updated_by po-S38 (NO rebaseline — bun-only metric, no bun change landed; verified by jq post-write).** long_tail_triage_policy.absolute UNCHANGED = 73. Single-status-key invariant verified across ALL tasks (jq select length!=1 -> 0 violations). Unique task-id count (active_sprints[].tasks[] tracked set) 229->230 (+1, exactly FIX-CI-C1129-CALIBRATION-TEST-REWRITE; total==unique==230, ZERO existing id dropped). NOTE: a pre-existing cross-bucket dup (FIX-CI-C1-MACRO-INJECT-SEAM-TESTS appears top-level + nested) is present IDENTICALLY in original and temp — NOT introduced by this write, outside scoped edit. Valid JSON; final size 784583 (>600000). signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24].tasks ONLY — no whole-object rewrite). ONE atomic jq pass (scripts/po-s39-c1129-triage-done-open-rewrite.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po-S39, ttl 120s) claimed BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + commit-mutex.json + the jq script). NOT pushed (router owns push).
- **dispatch ask (router owns):** spawn **dev-mcp-server** run docs/agents/dev-mcp-server/flow/main.md on **FIX-CI-C1129-CALIBRATION-TEST-REWRITE** (background) — rewrite ONLY apps/mcp-server/src/__tests__/1129-calibration-tools.test.ts: remove InMemoryTransport + Client imports + buildConnectedPair()/callTool(); add module-level _testServer + _testDb in beforeEach (registerCalibrationTools(_testServer, _testDb)); replace client.callTool({name,arguments}) with await (_testServer as unknown as RegisteredToolsServer)._registeredTools["get_calibration_report"].handler(args); buildInMemoryDb() inlines calibration_snapshots DDL (12 cols, IF NOT EXISTS) from schema-system.ts:213 — NO initDatabase(); afterEach cleanup; read result.content[0]?.text. ZOD-BYPASS NONE (all 5 ACs port verbatim). HARD: NO new mock.module() (C5-cure ABSOLUTE); tsc --noEmit clean. The dev COMMITS with explicit pathspec; the ROUTER pushes + gates: Task 1129 native fail 10->0 in CI vs the standing 73 band. After this gates, next lever C7 (1407b/1328e/1792/1352a), then C8. PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S40 (DJ-GATE-1): CI-C1129-GATE-7040cfb6 PASSED — FIX-CI-C1129-CALIBRATION-TEST-REWRITE REVIEW->DONE (REWRITE-GATED) + REBASELINE ci_absolute 73->68 (73 SUPERSEDED) + sync long_tail.absolute->68 (2026-06-09T12:59:05Z)
**task_id:** FIX-CI-C1129-CALIBRATION-TEST-REWRITE
- **what-done:** ONE batched atomic board write under commit-mutex on router gate-pass signal docs/signals/ci-c1129-gate-result-7040cfb6-20260609T1255Z.json (CI-C1129-GATE-7040cfb6; CI run 27207225757, sha 7040cfb6, bun_job 80326149577; router-verified + pushed, commit 72415830). **(1) FIX-CI-C1129-CALIBRATION-TEST-REWRITE REVIEW->DONE** (+done_by po-S40 / resolution "REWRITE-GATED — Task 1129 cluster 10->0 named flip confirmed CI run 27207225757 (gate CI-C1129-GATE-7040cfb6); ZERO Zod-bypass adaptation required" / done_at 2026-06-09T12:59:05Z / gate_result CI-C1129-GATE-7040cfb6 / dj_gate_1 #po-S40): router raw-verified + gate-watched run 27207225757 to completion; named Task 1129 cluster (all 1129* prefixes) went **10 native fail -> 0** (jitter-robust per-victim exact-prefix tally, UNAMBIGUOUS — entire get_calibration_report cluster cured, zero residual); test-infra REWRITE confirmed (InMemoryTransport+Client.callTool() harness replaced with _registeredTools["get_calibration_report"].handler() direct invocation, 1124-transport-hang signature, green siblings 1117/1124/1134 proven; buildInMemoryDb() inlines calibration_snapshots DDL 12 cols schema-system.ts:213 NO initDatabase Contract-B; zero new mock.module() [C5-cure honored]; tsc clean; 5/5 local pass). **C1129 needed ZERO Zod-bypass adaptation** — date param is z.string().optional() with no .default/.coerce, so direct-handler behavior == protocol behavior; all 5 ACs ported VERBATIM (AC-5/AC-extra/AC-6 omit date undefined on both paths; AC-7/AC-7b pass explicit string values, no Zod transform). Unlike C1134 (AC-4/AC-6 adapted), C1129 has no Zod-validation-dependent assertion. Prod registerCalibrationTools (calibrationTools.ts:276) untouched — pure SQLite, db-injection arg live, NOT HTTP rewired. Dev dev-mcp-server-S16 landed commits 5c14e807 (test + journal + orch-state REVIEW flip) + 7040cfb6 (mutex release); router raw-verified both (linear, owned paths only — ONLY 1129-calibration-tools.test.ts touched, NO prod leak, NO new mock.module, transport imports removed, single-status-key OK, 230 unique, mutex released, DJ-GATE-1 dev-mcp-server-S16) + PUSHED 674b95d5..7040cfb6 (pre-push tsc OK). **(2) REBASELINE ci_absolute 73 -> 68** (native_fail 68 / native_errors 0 / fail_plus_error 68 / tests 11803 / pass 11693 / skip 42 / sha 7040cfb6 / run 27207225757 / bun_job_id 80326149577 / supersedes "73 (sha 8916675a, run 27205559638, job 80320331863)" / updated_by po-S40). **(3) long_tail_triage_policy.absolute synced -> "68 (sha 7040cfb6, run 27207225757, job 80326149577 — SUPERSEDES 73/8916675a)"** (updated_by po-S40).
- **what-considered:**
  - Trust the named per-victim 10->0 flip vs distrust the +5-vs-projection native absolute: **trust the named flip** (CHOSEN). The authoritative jitter-robust gate is the per-victim exact-prefix tally (Task 1129 10->0, zero residual), NOT the native absolute. Projected ~63 (73-10) but landed 68; the +5 gap is NOT a regression — rewriting the harness (removing InMemoryTransport/Client imports) reorders the bun ESM-cache mock.module() contamination graph, shifting jitter on OTHER unrelated tests beyond the +/-3 band (absolute only stable when the import/test SET is unchanged). SAME mechanism po-S38 documented for the 1134 REWRITE (+6) and po-S36 for 1124 (+12). Carried verbatim in delta_vs_prior, including the note that C1129 (unlike C1134) needed NO Zod-bypass adaptation.
  - Rebaseline now vs keep 73: **rebaseline to 68** (CHOSEN, per router po_action_requested). A bun test FILE was rewritten + pushed + gated on a NEW run (7040cfb6 != 8916675a). The absolute moves only when the router gates a dev change against a new run — that condition is now met. New band ~68 +/-3 (floor ~65).
- **why-decision:** PO closed the co-dominant 10-fail Task-1129 cluster as DONE on unambiguous router gate evidence (named per-victim 10->0, gate-watched to completion), correctly rebaselined the bun absolute to the gated new-run value 68, and preserved the ESM-jitter caveat + the ZERO-Zod-bypass note so the +5-vs-projection is not mis-read as regression. The /goal "ci ALL passe" surface is now bun test 68 (sole remaining red); next lever (architect-first prod-vs-test confirm) C7 (Task 1328e dominant, +Task 1792 +Task 1352a) plus co-dominant standalone Task 235 + Task 1173 surfaced in the 68-run distribution — re-tally the fresh 68-run per-prefix distribution before opening the next lever. Cluster-6 schema-drift PARKED.
- **why-change:** ADVANCES po-S39 (which accepted the architect REWRITE verdict + dispatched the dev rewrite + kept 73 pending a gated run). The router gated run 27207225757 GREEN-for-scope (Task 1129 0-fail); PO accepts, flips DONE, and rebaselines 73->68. 73 is SUPERSEDED.
- **DJ-GATE-1:** status flips THIS commit: active_sprints[24 / CI-RED-RECONCILE].tasks **FIX-CI-C1129-CALIBRATION-TEST-REWRITE REVIEW->DONE** (+done_by po-S40 / resolution REWRITE-GATED / done_at 2026-06-09T12:59:05Z / gate_result CI-C1129-GATE-7040cfb6 / dj_gate_1 #po-S40). **ci_absolute REBASELINED 73->68** (native_fail 68 / native_errors 0 / fail_plus_error 68 / tests 11803 / pass 11693 / skip 42 / sha 7040cfb6 / run 27207225757 / job 80326149577 / supersedes 73-8916675a / updated_by po-S40; delta_vs_prior preserves the +5-vs-projection ESM-jitter caveat + ZERO-Zod-bypass note — named per-victim flip authoritative; new band ~68 +/-3 floor ~65). **long_tail_triage_policy.absolute synced -> 68** (updated_by po-S40). Single-status-key invariant verified across ALL tasks (jq select length!=1 -> [] = OK, 0 violations). Tracked unique task-id count = 230 UNCHANGED (status flip + ci_absolute/long_tail field mutations only — NO new id, NO id dropped; total==unique==230). Valid JSON; final size >600000. signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24] ONLY — no whole-object rewrite). ONE atomic jq pass (scripts/po-s40-c1129-done-rebaseline.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po-S40, ttl 120s) claimed BEFORE the jq write; released after commit (separate mutex-release commit). Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + commit-mutex.json + the jq script). NOT pushed (router owns push).
- **dispatch ask (router owns):** continue /goal loop toward native fail+error = 0. Remaining ~68 native fail. Next lever (architect-first prod-vs-test confirm before any fix, per /goal REMOVE-obsolete clause): re-tally the fresh 68-run per-prefix distribution, then C7 cluster (Task 1328e dominant + Task 1792 + Task 1352a under FIX-CI-C7-ASSERTION-LOGIC) plus co-dominant standalone Task 235 + Task 1173. Cluster-6 schema-drift stays PARKED. Pending non-blocking cleanup: pre-existing cross-bucket duplicate id FIX-CI-C1-MACRO-INJECT-SEAM-TESTS (recursive 464 vs unique 463 at whole-board level; tracked active_sprints[].tasks[] set clean 230/230) — queue a cleanup task once critical path clears. PO did NOT push and did NOT rebaseline beyond this gated value this cycle.

### STEP po-S41 (DJ-GATE-1): Accept arch-S14 CONTAM verdict for Task 1328e — carve 1328e OUT of C7 (TRIAGE-DONE) + open FIX-CI-C1328E-047-CONTAM-STUB (dev-mcp-server, TODO). NO ci_absolute change. (2026-06-09T13:20:00Z)
**task_id:** FIX-CI-C1328E-047-CONTAM-STUB
- **what-done:** ONE batched atomic board write under commit-mutex on the router-pushed architect brief fa223773 (docs/architecture-briefs/2026-06-09-ci-c1328e-conviction-routing-triage.md; HEAD=origin=93397832, docs-only arch work — NO CI gate this step). **(1) NEW dev-fix task FIX-CI-C1328E-047-CONTAM-STUB** (TODO, dev-mcp-server, FIX, size S, priority high, zone apps/mcp-server/src/__tests__/, baseline_pass=11693, baseline_absolute="68 (sha 7040cfb6, run 27207225757, job 80326149577)", created_by po-S41). Embedded the EXACT cure scope: edit ONLY 047-bctc-orchestrator.test.ts (the CONTAMINATOR) per the 1352a teardown-describe precedent — (1) file-top frozen-real import captures for notifyTelegramAlert/notifyTelegramDocument/formatConvictionBlock/deleteTelegramBug BEFORE the existing mock.module(); (2) extend 047's EXISTING telegram stub with those 4 exports (Alert/Document->noop, formatConvictionBlock->real, deleteTelegramBug->noop); (3) mock.module("../infrastructure/notifiers/telegram.js", () => ({ ...frozenReals })) inside/after the existing afterAll to RESTORE on teardown. **C5-CURE ABSOLUTE: NO new file-top/module-scope mock.module(); ZERO change to 1328e-conviction-display.test.ts.** **(2) C7 holder FIX-CI-C7-ASSERTION-LOGIC** — recorded 1328e carve-out as `carve_outs[]` {seed 1328e-conviction-routing, status TRIAGE-DONE, reclassified "C7 assertion-logic -> C5 MOCK-CONTAM", routed_to FIX-CI-C1328E-047-CONTAM-STUB, carved_by po-S41} + appended title note. C7 STAYS BACKLOG for its remaining LIVE seeds (1792 independent, 1352a A-1 independent) — NOT closed wholesale.
- **what-considered:**
  - Close C7 entirely vs carve 1328e out and keep C7 BACKLOG: **carve + keep C7** (CHOSEN). The architect verdict reclassifies ONLY the 1328e seed out of C7's assertion-logic cluster into C5 MOCK-CONTAM. C7's other named seeds (1792 = independent bugMessages-empty/bctc_signal_debounce DB; 1352a A-1 = independent `Expected: 1, Received: 2`) are NOT cured by the 047 stub fix — closing C7 would orphan them. Per under_scope_lesson, do NOT charge category-wide closure to a single seed reclassification.
  - Scope 1352a A-1 + Task 1792 into this fix vs leave them out: **leave them OUT** (CHOSEN, per arch-S14 coverage projection). The 047 stub-extension cures 1328e 10->0 (primary) + 1352a's notifyTelegramDocument-SyntaxError 2->1 (partial). 1352a A-1 and Task 1792 are INDEPENDENT root causes needing separate later triage — scoping them here violates atomic-FIX.
  - Rebaseline ci_absolute now vs hold at 68: **hold at 68** (CHOSEN). Arch work was docs-only; NO test SET changed; no CI run gated. Per the established cadence, the bun absolute moves ONLY when the router gates a dev change against a NEW run — that condition is NOT met this step. ci_absolute + long_tail.absolute UNTOUCHED (still po-S40, native_fail 68).
- **why-decision:** PO accepted a sound architect CONTAM (test-harness/mock-contamination, classification c) verdict — prod telegram.ts is CORRECT (severity gate 553-555 MEDIUM/LOW return false validating AC6/AC7; conviction block 564-569; splitMessage 141 / TELEGRAM_MAX_LENGTH=4096 135 in coreSend 194; fetchFn injection 587-590 -> coreSend 186) — and routed the EXACT contaminator fix to the CONTAMINATOR (047), not the victim (1328e), per the 1352a teardown-restore precedent with the C5-cure NO-new-mock.module() hard constraint. The /goal "ci ALL passe" surface remains bun test 68 (sole red); this fix is the primary lever (1328e 10->0) + a partial bonus on 1352a (2->1).
- **why-change:** ADVANCES po-S40 (which closed C1129 + rebaselined 73->68 and named Task 1328e as the next dominant lever under C7). The architect returned CONTAM (not REWRITE) for 1328e; PO accepts, carves 1328e out of C7 as TRIAGE-DONE, and opens the dev contam-fix. No bun change landed -> ci_absolute UNCHANGED.
- **DJ-GATE-1:** active_sprints[24 / CI-RED-RECONCILE].tasks: **+1 NEW FIX-CI-C1328E-047-CONTAM-STUB (TODO, dev-mcp-server, S, high, zone apps/mcp-server/src/__tests__/, brief fa223773, projection 1328e 10->0 + 1352a 2->1 partial, C5-cure absolute, created_by po-S41)**; **FIX-CI-C7-ASSERTION-LOGIC** gains carve_outs[] (1328e TRIAGE-DONE, routed_to the new task) + title note, status UNCHANGED BACKLOG (remaining seeds 1792 + 1352a A-1 live). **ci_absolute UNCHANGED = native_fail 68 / native_errors 0 / fail_plus_error 68 / tests 11803 / pass 11693 / skip 42 / sha 7040cfb6 / run 27207225757 / job 80326149577 / updated_by po-S40** (NO rebaseline — docs-only arch step, no bun change, verified by jq post-write: native_fail==68, updated_by==po-S40). long_tail_triage_policy.absolute UNCHANGED = 68. Single-status-key invariant verified across ALL tasks (jq select bad-status -> 0 violations). Tracked unique task-id count (active_sprints[].tasks[]) 230->231 (+1, exactly FIX-CI-C1328E-047-CONTAM-STUB; total==unique==231, ZERO existing id dropped). Valid JSON; final size 790018 (>600000). signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24].tasks ONLY — no whole-object rewrite). ONE atomic jq pass (scripts/po-s41-c1328e-contam-triage-done-open-stub.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po-S41, ttl 120s) claimed BEFORE the jq write; released after commit (SEPARATE mutex-release commit). Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal). NOT pushed (router owns push).
- **dispatch ask (router owns):** spawn **dev-mcp-server** run docs/agents/dev-mcp-server/flow/main.md on **FIX-CI-C1328E-047-CONTAM-STUB** (background) — edit ONLY apps/mcp-server/src/__tests__/047-bctc-orchestrator.test.ts per the 3-step 047 stub extension above (frozen-real captures of notifyTelegramAlert/notifyTelegramDocument/formatConvictionBlock/deleteTelegramBug; extend the EXISTING telegram stub; mock.module RESTORE after afterAll). HARD: NO new module-scope mock.module() (C5-cure ABSOLUTE); ZERO change to 1328e-conviction-display.test.ts; tsc --noEmit clean. Dev COMMITS with explicit pathspec; ROUTER pushes + gates: Task 1328e native fail 10->0 (primary) + Task 1352a 2->1 (notifyTelegramDocument SyntaxError cured, partial). 1352a A-1 + Task 1792 = separate later triage. After this gates, re-tally the fresh per-prefix distribution; remaining C7 seeds (1792 + 1352a A-1) + co-dominant Task 235 + Task 1173 + C8. PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S42 (DJ-GATE-1): FIX carve_out status-key COLLISION in FIX-CI-C7-ASSERTION-LOGIC — rename carve_outs[0].status -> triage_state (value "TRIAGE-DONE" verbatim). Single-status-key invariant RESTORED. NO ci_absolute change. (2026-06-09)
**task_id:** FIX-CI-C7-ASSERTION-LOGIC
- **what-done:** ROUTER raw-verification of the po-S41 commit ec7aeac8 (NOT pushed; origin still 93397832) caught a **SINGLE-STATUS-KEY INVARIANT VIOLATION** that po-S41's own badge mis-reported as "0 violations". The carve_outs[] entry po-S41 added to FIX-CI-C7-ASSERTION-LOGIC used a nested key literally named `status` (value "TRIAGE-DONE"), so the C7 task object had TWO status-ending scalar paths — `.status` (lifecycle = BACKLOG) + `.carve_outs.0.status` (= "TRIAGE-DONE"). The invariant `[paths(scalars) as $p|select($p[-1]=="status")]|length!=1` flagged it; raw confirmed VIOLATION ["FIX-CI-C7-ASSERTION-LOGIC"]. This is the SSOT-duplicate-key defect class — a board-scanning/last-key-wins tool could misread C7's real lifecycle status. **FIX (this single follow-up commit on top of ec7aeac8, owned paths only): renamed the carve_out's `status` key -> `triage_state`** (value "TRIAGE-DONE" preserved verbatim; ALL other carve_out fields — seed, reclassified, verdict, routed_to, carved_by, carved_at — unchanged). Scoped jq map on active_sprints[24].tasks ONLY (scripts/po-s42-fix-carveout-statuskey.jq). NO new key ending in "status".
- **what-considered:**
  - Trust po-S41's "0 violations" badge vs re-run the raw invariant: **never trust own badge — verify raw** (CHOSEN, router-mandated). The badge was wrong; raw is authoritative. Router-verify-raw caught the regression before push, exactly as the SSOT-duplicate-key + router-verify-raw lessons prescribe.
  - Rename to `triage_state` vs any other name: **triage_state** (CHOSEN) — non-colliding, does NOT end in "status", keeps the semantic (triage lifecycle of the carved seed) distinct from the task's own lifecycle `status`.
  - Rebaseline ci_absolute vs hold at 68: **hold at 68** (CHOSEN). Pure structural key-rename in a docs/board carve_out — NO test SET changed, no CI run. Per cadence the bun absolute moves ONLY when the router gates a dev change against a NEW run — not met.
- **why-decision:** PO restored the single-status-key SSOT invariant by renaming the colliding nested key, eliminating the last-key-wins misread risk on C7's lifecycle status, while preserving the TRIAGE-DONE triage record verbatim. The router's raw-verification (not the agent's self-badge) is the gate of record.
- **why-change:** CORRECTS po-S41 (commit ec7aeac8), whose carve_out introduced the `status`-key collision. Same task_id; structural fix only — C7 stays BACKLOG, the 1328e carve-out + routed_to FIX-CI-C1328E-047-CONTAM-STUB remain intact.
- **DJ-GATE-1:** active_sprints[24 / CI-RED-RECONCILE].tasks: **FIX-CI-C7-ASSERTION-LOGIC** carve_outs[0] key `status` -> `triage_state` (value "TRIAGE-DONE" verbatim; all other carve_out fields unchanged); task lifecycle status UNCHANGED BACKLOG. **Single-status-key invariant RE-VERIFIED RAW = OK (0 violations)** across ALL tasks BEFORE commit. **ci_absolute UNCHANGED = native_fail 68 / native_errors 0 / fail_plus_error 68 / tests 11803 / pass 11693 / skip 42 / sha 7040cfb6 / run 27207225757 / job 80326149577 / updated_by po-S40** (NO rebaseline — structural key-rename, no bun change; verified jq post-write native_fail==68). long_tail_triage_policy.absolute UNCHANGED = 68. FIX-CI-C1328E-047-CONTAM-STUB still TODO owner dev-mcp-server (unchanged). Tracked unique task-id count UNCHANGED = 231 (total==unique==231; NO id added/dropped — key-rename only). Valid JSON; final size 790024 (>600000). signal_queue + _schema=v3 + _ssot=true preserved (scoped jq map on active_sprints[24].tasks ONLY). ONE atomic jq pass (scripts/po-s42-fix-carveout-statuskey.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + scripts/po-s42-fix-carveout-statuskey.jq). NOT pushed (router owns push).
- **dispatch ask (router owns):** PUSH the corrected stack (origin 93397832 -> the po-S42 fix sha) after raw-verifying single-status-key == OK. The FIX-CI-C1328E-047-CONTAM-STUB dispatch ask from po-S41 stands unchanged. PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S43 (DJ-GATE-1): ADJUDICATE arch-S15 c1328e re-triage — FIX-CI-C1328E-047-CONTAM-STUB REVIEW->DONE-SUPERSEDED (KEEP e57494d3 on main; 1352a side-win) + OPEN FIX-CI-C1485-TELEGRAM-MOCK-RESTORE (TODO, dev-mcp-server) owning the 1328e+235 cure. ci_absolute STAYS 68 (NO rebaseline). (2026-06-09 TUESDAY)
**task_id:** FIX-CI-C1485-TELEGRAM-MOCK-RESTORE
- **what-done:** Adjudicated the arch-S15 re-triage verdict (brief `docs/architecture-briefs/2026-06-09-ci-c1328e-retriage-arch-s15.md`, architect commit 501a936c; architect DJ STEP arch-S15) against the router gate-FAIL signal `ci-c1328e-gate-result-7f1f48b3-20260609T1338Z.json`. The arch-S14 cure (047 afterAll + extended stub, dev commit **e57494d3**, in REVIEW) FAILED its 1328e objective: Task 1328e stayed **10 fail -> 10 fail** on full-CI ordering (run 27209642428, job 80334814093). arch-S15 root cause = a **SECOND contaminator**: `apps/mcp-server/src/__tests__/1485-telegram-mock-isolation.test.ts` (CI pos 89) installs `notifyTelegramAlert: async () => ({ ok: true })` in TWO it() bodies with NO afterAll restore, leaking the stub into the Bun process-global ESM registry for the 800+ files after it. `{ ok: true }` is the EXCLUSIVE fingerprint in 1328e's failures (Expected:false Received:{ok:true}; fetchFn calls.length=0). 047's afterAll (pos 315) captured the already-contaminated module as `_real`, so its restore reinstalled the 1485 stub -> that is why arch-S14's cure failed. Prod `telegram.ts notifyTelegramAlert` is UNCHANGED and CORRECT (returns boolean `result.ok`, never `{ok:true}`); all 5 it() in 1328e assert correct current prod behavior => NOT REWRITE, NOT REMOVE. **(A)** FIX-CI-C1328E-047-CONTAM-STUB REVIEW -> **DONE-SUPERSEDED** (terminal): it did NOT cure 1328e but it DID cure the 1352a-document contam (1352a 4->2) and added legitimate 047 afterAll hygiene -> **KEEP on main, do NOT revert** (e57494d3 already pushed); 1328e objective reassigned (superseded_by FIX-CI-C1485-TELEGRAM-MOCK-RESTORE). **(B)** OPENED **FIX-CI-C1485-TELEGRAM-MOCK-RESTORE** (status TODO, owner dev-mcp-server) carrying the arch-S15 fix spec verbatim (add `afterAll` to bun:test import line 13 + file-bottom afterAll re-registering all 8 `_realMod1485` exports via mock.module on the telegram path; C5-CURE ABSOLUTE = zero new file-top mock.module, existing it()-body mock.module unchanged, NO change to 047/1328e). This task OWNS the 1328e + 235 cure. **(C)** ci_absolute + long_tail.absolute LEFT at 68 (gate did not improve the absolute). One atomic jq pass `scripts/po-s43-c1328e-adjudicate.jq` on active_sprints[24 / CI-RED-RECONCILE].tasks ONLY.
- **what-considered:**
  - 047-stub terminal state: leave dangling in REVIEW vs close-PARTIAL vs **DONE-SUPERSEDED** (CHOSEN). DONE-SUPERSEDED records the honest outcome — gate FAILED for 1328e but the 1352a-document side-win + afterAll hygiene are real and KEPT on main, and the 1328e objective is explicitly reassigned (superseded_by), so nothing dangles in REVIEW. Plain revert rejected: e57494d3 is a net-neutral-but-positive change already pushed; reverting would re-open the 1352a-document contam.
  - Rebaseline ci_absolute 68 -> lower vs **hold 68** (CHOSEN). The gate did NOT improve the absolute (68->68) and the named primary victim did not flip; per cadence the bun absolute moves ONLY when the router gates a dev change against a NEW run that improves it. Rebaseline deferred to AFTER the 1485 fix gates.
  - New-task WIP: TODO vs in_progress — **TODO** (CHOSEN) keeps non-terminal in_progress at 1 (<=2). Dev/router will flip it to in_progress on pickup.
- **why-decision:** PO accepted the arch-S15 SECOND-CONTAMINATOR verdict, gave the failed-but-useful prior cure an honest terminal state that preserves its real side-win on main, and opened a single correctly-scoped successor task that owns the 1328e+235 cure at the SOURCE (1485 teardown restore), under C5-cure ABSOLUTE. Absolute held at 68 because the gate did not improve it.
- **why-change:** SUPERSEDES the 1328e objective of FIX-CI-C1328E-047-CONTAM-STUB (arch-S14 hypothesis disproven on full-CI ordering). 047 change itself is KEPT on main (not reverted).
- **DJ-GATE-1:** active_sprints[24 / CI-RED-RECONCILE].tasks: **FIX-CI-C1328E-047-CONTAM-STUB** status REVIEW -> **DONE-SUPERSEDED** (+terminal_reason, superseded_by FIX-CI-C1485-TELEGRAM-MOCK-RESTORE, note); **FIX-CI-C1485-TELEGRAM-MOCK-RESTORE** OPENED status **TODO** owner dev-mcp-server (arch-S15 spec verbatim, brief 2026-06-09-ci-c1328e-retriage-arch-s15.md). **Single-status-key invariant VERIFIED RAW = OK** (max status-ending scalar paths per task object == 1 across ALL tasks). **Tracked unique task-id count 231 -> 232** (total==unique==232 over active_sprints[].tasks[].id; +1 = the one new task, no id dropped). **ci_absolute UNCHANGED = native_fail 68 / native_errors 0 / fail_plus_error 68 / tests 11803 / pass 11693 / skip 42 / sha 7040cfb6 / run 27207225757 / job 80326149577 / updated_by po-S40** (NO rebaseline; verified jq post-write native_fail==68). long_tail_triage_policy.absolute UNCHANGED = 68. WIP non-terminal in_progress == 1 (<=2). Valid JSON; final size 796106 (>600000). signal_queue + _schema + _ssot preserved (scoped jq map on active_sprints[24].tasks ONLY; no whole-object rewrite). ONE atomic jq pass (scripts/po-s43-c1328e-adjudicate.jq); temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po, ttl 120s) claimed BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal + scripts/po-s43-c1328e-adjudicate.jq). NOT pushed (router owns push). Cluster-6 schema-drift stays PARKED.
- **dispatch ask (router owns):** spawn **dev-mcp-server** run `docs/agents/dev-mcp-server/flow/main.md` on **FIX-CI-C1485-TELEGRAM-MOCK-RESTORE** (background) — edit ONLY `apps/mcp-server/src/__tests__/1485-telegram-mock-isolation.test.ts`: (1) add `afterAll` to the bun:test import (line 13); (2) at file bottom AFTER the describe block add an `afterAll` that re-registers all 8 `_realMod1485` exports (sendTelegramWork, sendTelegramMarket, sendTelegramBug, sendTelegram, notifyTelegramAlert, notifyTelegramDocument, formatConvictionBlock, deleteTelegramBug) via `mock.module("../infrastructure/notifiers/telegram.js", () => ({...}))`. C5-CURE ABSOLUTE: ZERO new file-top mock.module(); the two existing it()-body mock.module() stay UNCHANGED; NO change to 047, NO change to 1328e; tsc --noEmit clean. Dev COMMITS own paths explicit pathspec; ROUTER pushes + gates: Task 1328e 10->0 (primary) + Task 235 -3 (co-victim) + Task 1352a 2->1 (A-1 independent remains), net ~ -13/-14 from 68. PO closes REVIEW->DONE + rebaselines ci_absolute ONLY after the router gate confirms the per-victim flip. PO did NOT push and did NOT rebaseline this cycle.

### STEP po-S44 (DJ-GATE-1): CI-C1485-GATE-5e7c9b5c PASSED on PRIMARY (Task 1328e 10->0) — FIX-CI-C1485-TELEGRAM-MOCK-RESTORE REVIEW->DONE (scoped to 1328e cure ONLY) + REBASELINE ci_absolute 68->63 (68 SUPERSEDED) + Task 235 co-cure DID NOT MATERIALIZE -> carried as SEPARATE open lever + record fresh 63-run top-N (235 & 1173 co-dominant) (2026-06-09 TUESDAY)
**task_id:** FIX-CI-C1485-TELEGRAM-MOCK-RESTORE
- **what-done:** Adjudicated the router gate-PASS signal `docs/signals/ci-c1485-gate-result-5e7c9b5c-20260609T1415Z.json` (dev-mcp-server-S18, commits 922ea51f + 5e7c9b5c, pushed; gate run 27211911171 / bun_job 80342965730 / sha 5e7c9b5c). The arch-S15 cure (file-bottom afterAll registry-restore added to `1485-telegram-mock-isolation.test.ts`) is **CONFIRMED jitter-robust on the PRIMARY named target**: per-victim exact-prefix tally **Task 1328e 10 fail -> 0 fail** (entire 1328e conviction-routing cluster cured, zero residual). I independently re-tallied the 63-run from `gh run view --job=80342965730 --log`: grand-total native summary line reads **`63 fail`** / `63 tests`-grand / pass 11698 / skip 42 / errors 0 over 11803 tests; and `grep "Task 1328e"` on the `(fail)` lines = **0** (1328e absent => CURED). **(A)** FIX-CI-C1485-TELEGRAM-MOCK-RESTORE REVIEW -> **DONE** — scoped EXPLICITLY to the **1328e cure ONLY**. **(B)** REBASELINE ci_absolute **68 -> 63** (sha 5e7c9b5c, run 27211911171, job 80342965730, native_fail 63, native_errors 0, fail_plus_error 63, pass 11698, skip 42, tests 11803), supersedes "68 (sha 7040cfb6, run 27207225757, job 80326149577)"; long_tail_triage_policy.absolute synced to 63. **(C)** recorded the fresh 63-run per-prefix distribution to seed the next architect-first triage.
- **PREDICTED Task 235 CO-CURE DID NOT MATERIALIZE (explicit honest record):** arch-S15 predicted Task 235 (pos 775) as a co-victim of the same 1485 `{ ok: true }` stub leak, and the dev's 2-file local joint-pass (1485+235) appeared to confirm it. On **full CI**, Task 235 stays **6 fail -> 6 fail** — NO FLIP. The 2-file local joint-pass was a **FALSE POSITIVE**, the SAME failure mode that disproved arch-S14's 047-cure: a 2-file local repro cannot reproduce the full-CI 1036-file contamination ordering. 235's residual has a DIFFERENT or ADDITIONAL contaminator (or genuine assertion-logic) and is carried as its **OWN SEPARATE open lever**, requiring full-suite / N-file ORDERED evidence in its own architect-first triage. **This DONE does NOT claim 235 cured anywhere.** NOT a regression: 235 was 6 before and is 6 now. Tertiary Task 1352a unchanged 2->2 (out of scope, independent residual).
- **EVIDENCE-BAR reaffirmed:** Two consecutive cures (arch-S14 047, arch-S15-predicted 235 co-cure) have now shown a 2-file local joint-pass to be INSUFFICIENT proof of a full-CI contamination cure. Going forward, any co-victim / contamination-cure claim requires full-suite or N-file ORDERED repro (matching the real CI bun_job file order) before the gate — the authoritative gate signal is the named per-victim exact-prefix fail->0 flip on the real CI run, NEVER the local repro and NEVER the jitter-prone absolute.
- **FRESH 63-RUN TOP-N PER-PREFIX (fail) DISTRIBUTION (jitter-robust exact-prefix tally, job 80342965730; seeds next architect-first lever):**
  - **Task 235 = 6** (co-dominant; confirmed STANDALONE lever after the 1485 false-positive)
  - **Task 1173 = 6** (co-dominant)
  - Task 1839b = 4
  - Task 1792 = 4
  - Task 1282a = 4
  - Task 1879a = 2, Task 1793 = 2, Task 1503 = 2, Task 1416b = 2, Task 1352a = 2, Task 1336 = 2, Task 1331a = 2, Task 1309 = 2, Task 1100 = 2
  - (named-prefix tally sums to 42; remaining ~21 of the 63 are non-Task-prefixed fails — external-source/uncategorized long tail). **NEXT DOMINANT LEVER = co-dominant Task 235 (6) + Task 1173 (6).** Do NOT open the next fix task yet — router dispatches architect-first per the /goal prod-vs-test loop with full-CI-ordered-repro evidence bar.
- **what-considered:**
  - Scope of the DONE: claim BOTH 1328e+235 cured (matching original task title) vs **scope to 1328e ONLY** (CHOSEN). The task title carried "owns the 1328e+235 cure," but the gate proves ONLY 1328e flipped. Over-claiming 235 would repeat the arch-S14 false-positive pattern and corrupt the next triage. DONE is honestly scoped to the proven 1328e flip; 235 explicitly carried separate.
  - Rebaseline 68->63 vs hold 68: **rebaseline (CHOSEN)** — unlike po-S43 (gate did NOT improve the absolute), this gate improved it -5 (68->63, direction-correct, +5 pass) AND flipped the named primary victim. Per cadence the absolute moves when the router gates a dev change against a NEW run that improves it. New band ~63 +/-3 (floor ~60).
  - Open next-lever fix now vs record-only: **record-only (CHOSEN)** — router owns the architect-first dispatch on the /goal prod-vs-test loop; PO identifies + records the dominant residual (235+1173) but does NOT open the fix task.
- **why-decision:** PO accepted the router-verified PRIMARY 1328e 10->0 flip as the authoritative gate, gave FIX-CI-C1485 an honest DONE scoped to that proven cure, rebaselined the absolute to the improved 63 (gate improved it this time), explicitly refused to claim the unproven 235 co-cure (carried separate), and recorded the fresh top-N so the next architect-first triage opens the correct co-dominant lever (235+1173).
- **why-change:** sprint absolute 68 -> 63 (-5). FIX-CI-C1485 terminal (DONE, 1328e-scoped). Task 235 reclassified from "predicted co-victim of 1485" to "confirmed standalone open lever." No new fix task opened (router owns next dispatch).
- **DJ-GATE-1:** active_sprints[24 / CI-RED-RECONCILE]: **FIX-CI-C1485-TELEGRAM-MOCK-RESTORE** status REVIEW -> **DONE** (scoped to 1328e cure; +terminal_reason citing Task 1328e 10->0 named flip; +note recording Task 235 NO-FLIP carried as separate lever). **ci_absolute REBASELINED = native_fail 63 / native_errors 0 / fail_plus_error 63 / tests 11803 / pass 11698 / skip 42 / sha 5e7c9b5c / run 27211911171 / job 80342965730 / updated_by po-S44 / supersedes "68 (sha 7040cfb6, run 27207225757, job 80326149577)"**. **long_tail_triage_policy.absolute synced -> 63**. **Single-status-key invariant VERIFIED RAW = OK** (max status-like keys per task object == 1 across ALL tasks). **Unique task-id count UNCHANGED 232 (total==unique==232, 0 dups; no task added/removed, only a status flip).** Valid JSON; size > 600000. signal_queue + _schema + _ssot preserved (scoped jq on active_sprints[24] ONLY; no whole-object rewrite). temp validated [ -s ] && jq -e . && size>600000 BEFORE mv. commit-mutex (task_kind:commit-mutex, task_id:main, owner_agent:po-S44, ttl 120s) claimed via gateway task_claim BEFORE the jq write; released after commit. Committed in the SAME commit as this journal entry with EXPLICIT pathspec (orch-state.json + this journal). NOT pushed (router owns push + verification). Cluster-6 schema-drift stays PARKED. Did NOT touch FIX-CI-C1328E-047-CONTAM-STUB (DONE-SUPERSEDED), prod, or any test file.
- **dispatch ask (router owns):** continue /goal loop toward native fail+error = 0 (remaining 63). Next lever = architect-first prod-vs-test confirm (full-CI-ordered-repro evidence bar) on **co-dominant Task 235 (6) + Task 1173 (6)**, then the 4s (1839b/1792/1282a), then the 2s long tail, then C8 REMOVE-obsolete triage. Cluster-6 schema-drift PARKED. Pending non-blocking cleanup: pre-existing cross-bucket duplicate id FIX-CI-C1-MACRO-INJECT-SEAM-TESTS (queue once critical path clears — note: current unique==total==232, so no active dup right now).
