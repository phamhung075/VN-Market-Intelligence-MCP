# PO Decision Journal — triage 2026-08-26T01:37Z (dev-team tick, S3 dispatcher-wrap)

- task_id: FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED (+ 4 folded rows, see below)
- at: 2026-08-26T01:58:19Z
- session: 036ceaf1-bf34-46cd-92e4-8c6b213ff4bb

## STEP 1 — what-considered / why-change

**Consolidating a 34,450-byte prose row down under the 12,000-byte ceiling.** Only path: the row is
grandfathered over-ceiling, so `orch-row-prose-ceiling-check` rejects ANY net-new growth on it — the
manual-dispatch Step 2 stamp could not be applied without first shrinking it. Splitting to a detail_ref
was the alternative and was rejected: 10 of the 16 prose fields are superseded by this tick's own live
re-measurement, so archiving them to a second live file would preserve stale claims in a place future
pickers would still read. They are preserved verbatim below instead, which is durable (git) and inert.

**Why the row needed rewriting at all, not just stamping:** it names 3 signal types. One shipped, two are
dead, and 2 of its 6 ACs are already satisfied. Dispatching it as written would have sent agent-father to
add table rows for types that no longer fire and to sync a `$routed` array that no longer exists.

## Measured this tick (read-only, 2026-08-26T01:50-01:54Z)

| claim in the row | measured | verdict |
|---|---|---|
| `cowork-fire` unrouted (Pipeline A) | present in the Pipeline-A table, triage-signals.md | SHIPPED |
| `agent_flow_defect` unrouted | 0 doc hits, but also 0 live `to=po` rows | DEAD TYPE |
| `db_anomaly_illiquid_ohlcv` unrouted (AC-6) | 0 doc hits, 0 live rows | DEAD TYPE |
| AC-2: sync the guard's `$routed` array | no such array — the script parses the tables | OBSOLETE |
| AC-4: guard has no scheduled caller | scripts/audits/guard-signal-type-coverage.sh, wired at .github/workflows/ci.yml:556-602 job `signal-type-coverage-guard`, runs `--check` on every push/PR | SHIPPED |
| AC-5: sweep for further drift | ran it — 4 NEW unrouted types, 6 live rows | LIVE WORK |

Current unrouted live `to=po` types (discriminator: backticked type token absent from BOTH triage-signals.md
and triage-signals-longtail.md; positive controls `signal_feedback`=2 hits and `system-issue`=6 hits confirm
the discriminator is not vacuous): `audit_finding` (1 row), `detector_defect` (2), `preserved_bug_no_tracking_row` (1),
`tooling_defect` (2). All status=READ, i.e. already swallowed by the catch-all.

NOTE the catch-all is CIRCULAR for these: the ROUTE-BY-`to` fallback routes an unknown type to its own `to`
field, and `to` IS `po` on all 6 — so the fallback hands them back to the same step that could not route them.

## PRESERVED VERBATIM — the row as it stood before this tick's rewrite (34,450B prose, 16 fields)

**Not inlined — git already preserves it durably and inertly.** The row as it stood before this tick's
rewrite is the `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` entry of
`docs/data/orch/orch-state.json` at commit `2a0dbbc62e28fc8b707881f1f83717760c9427a7` (this journal's parent).
Retrieve with:

```bash
git show 2a0dbbc62e28fc8b707881f1f83717760c9427a7:docs/data/orch/orch-state.json \
  | jq '.task_board.backlog[] | select(.id=="FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED")'
```

The 11 fields dropped by the rewrite, with their byte sizes, so a future reader can tell at a glance
whether the one they want is in there: `status_note` 15056, `po_fold_20260811T1437Z` 2797,
`po_measurement_20260807T0444` 2266, `po_reroute_20260813T1629Z` 2255, `occurrence_2_20260811T1554` 1513,
`po_occurrence_20260815T0359` 1437, `po_measurement_20260812T1510Z` 1298, `po_corroboration_20260806T1821` 1118,
`po_corroboration_20260806` 1073, `po_fold_20260811T1237Z` 998, `po_corroboration_20260806T0721` 524,
`po_dispatch_blocker_note` 284.

Two claims inside the dropped prose are worth carrying forward because they are still-live scope and are
NOT captured by the 4-type measurement above:
- a `bug-escalation` payload class `[notebook-uuid-provenance-guard]` has no branch in the routing table,
  and the table has no DEFAULT branch for an unrecognised `bug-escalation` prefix;
- the `bug-escalation` row tells the reader to dedup against `FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE`,
  which has been cold-evicted to `docs/data/orch/archive/2026-08.json`, so every future SAME-FILE DIVERGENCE
  falls through to a branch that demands an investigation the brief's own section 2.7 says can never conclude.
  The table needs a rule for *the dedup target has been cold-evicted*.


## STEP 2 — other dispositions this tick (see notebook for the narrative)

- **Envelope 09baf67a (cowork-team CORRECTION, system-issue, MEDIUM)** — ACK-WITH-CORRECTION, 0 mints.
  Both withdrawals accepted and stamped onto FIX-SQLITE-DOCKER-VIRT-CORRUPTION-ROOT-CAUSE-INVESTIGATION.
  The envelope is itself stale on the -12205 figure (true distinct loss 3917; 8288 are PK-duplicate rows),
  and its one open test was already answered 42 minutes before it was filed by architect brief 8f7bc339e.
  Load-bearing action was NOT the correction: it was the third related_row, the evidence-destruction hazard.
- **Envelope 20f3c537 (cowork-fire, low)** — routine FIRE, errors=[], silent=false, 1 slot. No mint,
  volume-tracked only. `dev_head:"unknown"` present, the known placeholder tell for server-computed
  fields, so its `devq`/`signal_backlog` numbers were not used for any inference.
- **CLEAN-MARKETDB-FORENSIC-COPIES**: AC-3 said do not delete a copy a still-open row cites, but named no
  file and was written 08-24, before the snapshot existed. Stamped a named hard exclusion on
  data/live/market.db.corrupt-2026-08-26T0031Z with an explicit release condition.
- **The worktree pair**: RECOVER-ORPHANED-WORKTREE refuted by content measurement (all 6 work-product
  paths byte-identical to main, landed at 28f8509fc) and rerouted to qa; CLEAN-STALE-WORKTREE given the
  content-diff discriminator because its AC-1, measured via `git status`, is unsatisfiable by construction.
- **FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG**: implementation already in main; told ba
  not to write a spec. next_agent deliberately NOT flipped to qa (qa is off the DRS allowlist, the flip
  would strand the row with no picker).
- **Supervised-hold**: should_hold=true on FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM. NOT ratified.
  Reason recorded so the next tick does not re-derive it: next_agent=architect is currently PRODUCING the
  deliverable the checkpoint exists to gate. Ratifying a deliverable that does not exist yet is worse than
  a live hold. The hold is correctly timed and self-heals once the brief lands.

## STEP 3 — what did NOT get satisfied

- Step 0-TNB c136's two fleet-outage findings (05:15-06:32Z, 08:26-12:00Z on 08-25) were deferred by the
  23:41Z ACK and are STILL not re-triaged. Dedup says they are covered (BA-COWORK-GUARANTEED-SLOT-CATCHUP
  + TASK-COWORK-CATCHUP-6..9 + FIX-A29-CRON-GAP-NO-OUTAGE-WINDOW-DISCRIMINATOR), so no mint was correct,
  but no fresh ACK block was appended to the handoff this tick.
- The 4 newly-measured unrouted types were folded onto the existing P0 row rather than minted. If
  agent-father never picks that row up, they stay unrouted.
