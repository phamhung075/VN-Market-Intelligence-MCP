# Decision Journal — Sprint MERGE-MONEY-RADAR-INTO-MOMENTUM · architect

**Sprint goal:** (ambient — this entry belongs to router-prioritized task TOKEN-ECONOMY-TICK-PREFLIGHT, riding alongside the active sprint_goal entry)
**Agent:** architect
**Started:** 2026-07-02T00:00:00Z

---

### STEP architect-S1 · architect · 2026-07-02T12:10:00Z
**task-id:** TOKEN-ECONOMY-TICK-PREFLIGHT
**what-done:** Brownfield review of 3 WUs (cowork silent-path, dev-team SF-1-first, auditor Tier-1 shell healthcheck); live-verified the MCP-from-shell mechanism (stateless JSON-RPC-over-Streamable-HTTP, no initialize handshake, error via `.result.isError` not `.error`) against the running mcp-server container; wrote handoff with 11 risk notes + file-level change map.
**what-considered:**
- Node MCP client helper vs bash+curl+jq — chose bash for consistency with existing flow-doc idiom (leader-lock.md/blind-guard.md/match-slots.md all bash+jq) and zero new deps.
- Whether WU-3 needs the same MCP-call mechanism as WU-1/WU-2 — determined NO: probe.sh + system-map.json thresholds are pure-shell sufficient for the ALL-GREEN gate; MCP cross-ref stays subagent-only on failure, unchanged from today.
- Whether to silently drop the pre-existing unwired `cowork-tick-autosilent.sh` or flag it — flagged explicitly (R5) since it's incompatible with current telemetry.md Step 6.1 semantics and bypasses commit-mutex.
**why-decision:** Live-verifying the /mcp endpoint (rather than trusting the brief's one-line "JSON-RPC curl" claim) closed the single highest-risk unknown before pm decomposition — found the actual response is SSE-framed with a non-obvious `isError` shape that would have broken a naive implementation.
**why-change:** Brief's WU-2 section omits an explicit ERROR verdict (constraint #4 requires one) and the WU-1 step-list undercounts signal_queue draining vs its own flow-doc-update paragraph — both reconciled explicitly in the handoff (R4, R8) so pm doesn't silently drop scope.

### STEP architect-S2 · architect · 2026-07-02T18:15:00Z
**task-id:** FIX-MCP-MEMORY-CODE-LEAK
**what-done:** PHASE-0 recon only (no fix, no container action). RAW-verified (a) running image is 6 commits stale vs HEAD (built 2026-07-01T22:27:13Z; last apps/mcp-server commit a1689b0e @08:49:45Z) but none of the 6 pending commits touch schema.ts/server.ts/DB-init code — stale image explains nothing memory-relevant, rebuild is hygiene only. (b) host Docker budget 7.75GiB actual (not just the 8GiB nominal note), current live sum ~3.38GiB (43.6%) → 4.37GiB free; 2GB mcp-server cap has zero margin headroom while every other service float far under cap. (c) live `docker stats` caught mcp-server AT 99.98%/2GB cap mid-session (RAW, not from a stale reading) with CPU 108%; source read (not guessed) found the real allocation hotspot: `initDatabase()` (`schema.ts:148`) has NO already-initialized guard (unlike its own sibling `getDb()` singleton a few lines above) and is invoked via `await initDatabase()` at 68 call-sites across MCP tool handlers — confirmed literally the first line of `get_alerts` and others — so the full 3300-line, 10-domain-slice DDL+backfill sweep (incl. a JSON file read + full-watchlist bulk UPSERTs) re-executes on nearly every tool invocation, stacked with `createMcpServerInstance()` (`server.ts:335`, called fresh per `/mcp` POST as an intentional stateless-transport tradeoff for a Bun-JIT Symbol-corruption bug) rebuilding the full ~146-tool registry every request. Container logs during the session showed both firing every few seconds under concurrent agent load — consistent with the sawtooth (not monotonic-leak) shape PO already diagnosed.
**what-considered:**
- Trusting the "many recent commits ⇒ stale image" prior as sufficient explanation vs verifying which commits actually changed memory-relevant code — chose to diff commit paths against schema.ts/server.ts explicitly; found zero overlap, so ruled OUT stale-image-alone as root cause rather than asserting it by pattern-match.
- Full heap-snapshot profiling now vs a cheaper log-correlation check first — deferred both (out of scope this pass per HARD CONSTRAINT), but designed the profiling approach in the brief so dev-mcp-server doesn't have to invent the method: correlate existing `docker logs` request-rate against `docker stats` mem-trace first (zero code change), then JSC heap-snapshot diff (`bun --inspect` + `bun:jsc generateHeapSnapshot()`) under synthetic load only if correlation is inconclusive.
- Recommending a cap bump alone vs pairing it with the code finding — cap bump is real headroom (safe given 4.37GiB free) but flagged explicitly as a band-aid: raising the ceiling on an unbounded per-request re-init pattern under rising concurrent-agent load will eventually refill any cap given.
**why-decision:** The 68-call-site / no-guard finding is code-verified (not inferred from symptoms) and predates all 6 pending commits — it exists in HEAD too, so a fresh rebuild will very likely still slam whatever cap it's given. This makes the (c) gate ("only if fresh-HEAD-at-adequate-cap still slams cap") effectively already tripped by static analysis, without needing to wait for the user-gated rebuild to find out empirically.
**why-change:** PO's brief scoped (c) as conditional on doing (a)+(b) first; delivering the concrete hotspot now (rather than a generic "profile if needed" placeholder) saves a full round-trip once the rebuild/cap-bump ships and the sawtooth persists, per user directive "fix root cause definitif not recurrent symtom."
