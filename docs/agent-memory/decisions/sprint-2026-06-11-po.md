# Decision Journal — Sprint 2026-06-11 · po

**Sprint goal:** no goal set — F1 triage (empty canonical alerts table)
**Agent:** po
**Started:** 2026-06-11T08:34:59Z

---

### STEP po-S1 · po · 2026-06-11T08:34:59Z
**task-id:** ALERT-WRITER-RECONCILE
**what-done:** Triaged F1 empty `alerts` table; refuted router hypothesis; created tracked SPIKE owned by dev-mcp-server.
**what-considered:**
- Cron the Go alert-engine `/evaluate` (router hypothesis)
- Persist digest-path alerts into `alerts`
- Verify the already-scheduled mcp-server scan crons on the LIVE container
**why-decision:** Evidence settles SSOT. alert-engine `cmd/server/main.go:40` opens its OWN `alert_engine.db` (writes `alert_engine_records`/`alert_mutes`), and `/evaluate` (router.go) is request-validation only — cronning it CANNOT populate `market.db.alerts`. The canonical writers are 3 mcp-server crons already scheduled in `cronConfig.ts` (taAlertScan/bbAlertScan `*/15 2-8 * * 1-5`, foreignFlowAlert) inserting `INSERT INTO alerts ... sent_by='server'`. NO split-brain: digest "Cảnh báo gần nhất" reads `FROM alerts` (assembleBriefing/assembleEveningSummary/assembleAlertDigest) — the SAME table the future frontend reads. Dev snapshot `market.db` cron_job_runs holds only `pollNews` (newest 2026-06-09) so it CANNOT prove writer-dead → must verify on live volume, not snapshot. Hence SPIKE (verify-live), not a writer rewrite.
**why-change:** Differs from router plan: router proposed cron-the-Go-engine / persist-digest-path; both rejected because they target the wrong DB / a non-existent split-brain. Empty table is most likely "no extreme RSI/BB fired" + snapshot≠live, not a dead writer.

### STEP po-S2 · po · 2026-06-11T11:15:08Z
**task-id:** PRED-RESOLVER-GAP-FIX
**what-done:** Filed ONE deduped BACKLOG FIX (dev-mcp-server) for prediction resolver stamping resolved_at but leaving resolution_outcome NULL on claims 1/6/7 (stuck pending forever); no dispatch.
**what-considered:**
- Skip — but no existing PRED-RESOLVER backlog entry (dedup scan clean), so a tracked item is needed
- Split into two tasks (actual_price-fetch vs neutral-rule) — rejected: same resolver cron, one coherent FIX, size M
**why-decision:** Both sub-bugs share one root surface (the resolver's terminal-state logic) so a single FIX keeps the fix atomic; AC pins the invariant (no resolved_at with NULL outcome) which both branches must satisfy. BACKLOG-only per task scope — normal dev cycle picks it up.
**why-change:** no change from plan — backlog-record-only, no fix started, no dev dispatched.

### STEP po-S3 · po · 2026-06-11T17:53:21Z
**task-id:** FIX-EVIDENCE-PIPELINE-STARVED
**what-done:** Triaged router DATA-FRESHNESS AUDIT (5 silent-ingestion findings, live-DB ground truth) into deduped zone-routed backlog tasks: MERGED P1 vnstock silent-write-path into FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE; filed 4 NEW (FIX-EVIDENCE-PIPELINE-STARVED, FIX-PREDICTION-SIGNALS-EMPTY, FIX-CRON-JOB-RUNS-DOUBLE-LOG, INVESTIGATE-EMPTY-DATA-TABLES).
**what-considered:**
- P1 vnstock: new task vs merge — MERGED (same pipeline root; crash-spike already architect-owned, recurring-bug rule)
- P3 empty tables: 5 tasks vs 1 — ONE SPIKE per router directive (avoid backlog bloat)
- prediction_signals: dedup vs CO-OBS-01/PRED-RESOLVER — kept DISTINCT (write/threshold gap, not observability/resolver)
**why-decision:** Each finding is a genuine green-but-does-nothing integrity bug, not cosmetic staleness; merge where same root pipeline, single-task where router asked, cross-ref related-but-distinct items so dedup is explicit not silent. All zones = apps/mcp-server/ (write-path/producer/accumulator/logger all in mcp-server); vnstock keeps architect-SPIKE owner.
**why-change:** no change from plan — router gave verified evidence + ACs; I transcribed them into tracked tasks, BACKLOG-only, no dispatch, no fix.
