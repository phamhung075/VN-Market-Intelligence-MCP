# Decision Journal — Sprint SYSTEMIC-REMAKE-P1 · dev-pdf-extractor

**Sprint goal:** Phase-1 containment-now: idle-loop gates, detector fix drain + READ->RESOLVED closure, narrative-drift quarantine. Bounded, no redesign, ships on main today.
**Agent:** dev-pdf-extractor
**Started:** 2026-07-08T00:00:00Z

---

### STEP dev-pdf-extractor-S1 · dev-pdf-extractor · 2026-07-08T00:00:00Z
**task-id:** FACTORY-PDF-paddleocr-score-07-mask
**what-done:** Replaced `float(cell.get("score", 0.7))` in `_cells_to_row_bands` (pek_engine_adapter.py:569) with a direct read of the raw score: present score (incl. real 0.0) passes through unchanged; absent score emits named `_MISSING_CELL_SCORE_SENTINEL` (None -> JSON null) instead of a fabricated 0.7.
**what-considered:**
- (a) compute a real ink-density proxy mirroring `_detect_row_bands` in generic_md_table_extractor.py — rejected: that sibling reads a `row_dark` per-row pixel-darkness array from the rasterized page image; `_cells_to_row_bands` only receives clamped `y0..y1` ints + PaddleOCR cell dicts, no image data plumbed in at this call depth. Threading the image through `_run_table_extraction` -> `_map_bboxes_to_zones` -> `_cells_to_row_bands` would be a much larger refactor than Effort:S/Risk:low warrants, for a value (row_density) confirmed unconsumed downstream beyond visual overlay + tests.
- (b) named sentinel (None) — chosen: cheap, greppable, JSON-safe (null), zero behavior change on the present-score path, matches audit's explicit fallback option.
**why-decision:** Both PaddleOCR cell-construction call sites in `_run_table_extraction` already always set `score` explicitly (incl. `0.0` when OCR returns no text_conf) — the missing-key branch only guards a generic/defensive `cells: List[Dict]` contract for callers this helper doesn't control (tests, future cell sources). Sentinel is the correct fix at that boundary; ink-density proxy would be solving a problem (no image data) that doesn't exist at this call site today.
**why-change:** no change from plan — task brief explicitly offered (a)/(b) as either/or; picked (b) per scope/effort match.

### STEP dev-pdf-extractor-S2 · dev-pdf-extractor · 2026-07-09T00:00:00Z
**task-id:** FACTORY-PDF-split-extractLayoutFirst-execute
**what-done:** Extracted `ExtractLayoutFirstUseCase.execute()` (~480L, Tier0-3 inline) into `_tier0_document_map`, `_tier1_zone_pages`, `_tier2_ocr_and_stitch`, `_tier3_invariant_gate` (46/45/52/68L); execute() is now a 147L linear pipeline threading results, eval-push calls kept at same logical points. Signature/quarantine semantics unchanged.
**what-considered:**
- Tier1's per-unit page-zoning loop (~115L) and Tier3's per-unit gate loop (~150L) each alone exceeded the DoD's 120L/method cap even after extraction — added `_zone_unit_pages` (110L) and `_gate_check_unit` (117L) as per-unit sub-helpers, called once each from a `for unit in ...` loop.
- Dropped 2 pre-existing dead locals while relocating code (unused `import os` in old Step-2 scope; unused `unit_page_type` local in old Step-3 scope) — zero behavior change, confirmed via RAW-verify.
**why-decision:** Sub-helpers were the only way to satisfy the explicit <=120L-per-method DoD without changing gate/zoning logic; simplicity-gate Q2 exception applies (required by task DoD, not speculative).
**why-change:** no change from plan — approach matches backlog-detail exactly; sub-helpers are an implementation detail within the 4 named Tier methods.
**verify:** RAW-verify harness (3 injected-fake scenarios: Tier0-abort, Tier1-abort, happy-path w/ pass+quarantine+ocr_error+vision_verify) — execute() return dict + push_layout call args + all 3 eval_push_stage calls byte-identical before/after (JSON diff empty). `pytest -q`: 1088 passed/12 failed/1 skipped both before and after (12 pre-existing failures confirmed unrelated to this file — no import of it).

### STEP dev-pdf-extractor-S3 · dev-pdf-extractor · 2026-07-09T00:00:00Z
**task-id:** FACTORY-PDF-delete-deprecated-inspect
**what-done:** Deleted the deprecated `/inspect` viewer surface: removed `InspectionStore` import+construction+param from `main.py`/`register_routes` (handlers.py), deleted the 4 route closures (`GET /inspect`, `/inspect/pdfs`, `/inspect/pdf/{doc_id}`, `/inspect/extraction/{doc_id}`, ~135L incl. the now-dead `HTMLResponse`/`Response`/`Path`/viewer-path imports), deleted `infrastructure/inspection_store.py` + `interface/viewer.html`, deleted the 3 tests-only-for-this-surface files (`test_inspection_store.py`, `test_inspection_routes.py`, `test_pi3_served_url_acceptance.py`, 72 tests total), and dropped the `inspection_store=` kwarg from 3 unrelated call sites (`test_local_pdf_input.py` x3, `test_fu1_fail_loud.py` x1, `scenarios/pek_single_doc_extraction.py` x3) that only needed it as a required-positional stub.
**what-considered:**
- Delete outright vs env-gate off — checked `2026-05-24-pdf-inspect-reground.md` §9 QA mandate + git log for PI-3-redo/REOPEN-2 sign-off: found `ca955baf1 chore(po/pdf-inspect): REOPEN-2 final re-sign — DONE on real data + meta-lesson` (PO closed DONE on real `market.db` data), plus the mcp-server `/api/bctc-inspect` surface has 15+ commits of continued feature work since (LF-OVERLAY, HC-DEV-6/7, BEQ fixes, FIX-INSPECT-NAV-RELOAD) — confirms it is the live, QA-approved, actively-maintained SSOT replacement, not pending sign-off. Chose outright delete per the backlog-detail default.
**why-decision:** QA-sign-off-pending gate (the only condition for env-flag-instead-of-delete) does not apply — evidence is the opposite (long-shipped, actively-developed replacement).
**why-change:** no change from plan.
**verify:** `grep -rn "InspectionStore\|inspection_store\|viewer.html"` across `apps/pdf-extractor` → zero hits. `create_app()` real composition root imported directly (scratch env vars, no Docker) → route table has zero `/inspect*` paths, all real routes intact (`/extract`, `/extract-layout-first`, `/extract-tables`, `/extract-md-tables`, `/pek-extract`, `/rasterize`, `/page-text`, `/health`). Live `TestClient` on the real `app` object: `GET /health` → 200; `GET /inspect` → 404; `GET /inspect/pdfs` → 404 (both were previously live routes). `pytest -q`: 12 failed/1016 passed/1 skipped — identical 12 pre-existing env-only failure set to baseline (byte-for-byte same test IDs, confirmed via `git stash`/`git stash pop` A/B); the 72-test drop (1088→1016) is exactly the 3 deleted test files (29+18+25=72 via `git show HEAD:<path> | grep -c def test_`), zero other regressions. mypy: same pre-existing `pdf-extractor is not a valid Python package name` env error as baseline. `handlers.py` 808→673L (-135L), `main.py` 280→271L (-9L).

### STEP dev-pdf-extractor-S4 · dev-pdf-extractor · 2026-07-09T02:58:11Z
**task-id:** FACTORY-PDF-fix-application-infra-leak
**what-done:** Added `DocLangSerializerPort` Protocol (`domain/modules/financial_reports/ports.py`, sibling to `DocLangWritePort`) typing the `.serialize(tables, report_id) -> str` surface; retyped `DocLangSerializeUseCase.__init__`'s `serializer` param to the Port and dropped `from infrastructure.doclang_serializer import DocLangSerializer` (was the only `from infrastructure` import in `application/`); added `Fence-C: source=application forbidden=infrastructure,interface` to `pyproject.toml [[tool.importlinter.contracts]]`. `main.py` composition root already wired the concrete `DocLangSerializer` instance positionally — no change needed there (protocols are structural, no `implements` needed).
**what-considered:**
- Port's `tables` param type: `List[application.dtos.ExtractedTableDTO]` (precise but domain→application import) vs `List[Any]` (matches sibling ports' Dict/str generic-passthrough convention, keeps domain decoupled).
- Chose `List[Any]` — every other Protocol in this file (`TableAssemblerPort`, `GenericMdTableExtractorPort`, etc.) types payloads as `Dict`/`List[str]`, never imports a concrete application DTO; importing `ExtractedTableDTO` would itself be a domain→application leak (file's own header states "Zero imports from infrastructure/, application/, interface/").
**why-decision:** `List[Any]` is the only option consistent with the file's own documented domain-purity rule and existing sibling-port precedent; mypy structurally accepts `List[ExtractedTableDTO]` args against a `List[Any]`-typed Protocol param (Any is bidirectionally compatible), so no caller-side type friction.
**why-change:** no change from plan — task brief's 4 steps executed as specified.
**verify:** `lint-imports` (venv, pyproject config): 3 contracts kept / 0 broken (was 2/0 before — Fence-C is new and passes). `grep -rn "^from infrastructure\|^import infrastructure"` under `application/` → zero real import hits (3 comment-only mentions of `infrastructure.ocr_worker` in an unrelated file, untouched). `pytest __tests__/unit/test_doclang_serializer.py -q`: 13/13 passed (unchanged assertions — `NullDocLangWriteAdapter` still returns `""`, XML output untouched, no serializer/usecase logic edited, only the type/import). Full suite `pytest -q -m "not slow"`: 1013 passed/9 failed — A/B via `git stash`: identical 8 pre-existing env-only failures (PIL.Image.new AttributeError/poppler-missing rasterizer/pandas) both with and without this change; the 9th (`test_extract_layout_and_tables_raises_on_timeout`) reproduced GREEN in isolation both times — timing-flake, unrelated file. mypy: baseline env-blocking `pdf-extractor is not a valid Python package name` error identical before/after (confirmed via stash A/B); scoped `--explicit-package-bases` mypy run on the 2 changed files: 18 pre-existing `[type-arg]`/`[arg-type]` errors, byte-identical (only line-number shift) before/after — zero new errors from the added Protocol. rebuild_required=true (application-layer code) — task board moved to REVIEW, next_agent=ops for Docker Microservice Code-Change Close Gate; not self-closed.

### STEP dev-pdf-extractor-S5 · dev-pdf-extractor · 2026-07-09T00:00:00Z
**task-id:** FACTORY-PDF-split-generic-md-table
**what-done:** Split the 4111L `generic_md_table_extractor.py` god-file into `infrastructure/generic_md_table/` (constants, markdown_emit, grid_cleanup, ordinal_grid, document_map, page_zoning, unit_ocr, extractor) behind a 164L thin re-export shim, 8 sequential commits (one sub-module/PR), `_process_page` split into 3 named stage helpers (Step A / Step A2 / Steps C5-G per-region).
**what-considered:**
- Live/dead symbol grouping for ordinal_grid.py: split further along MD-EXTRACT-N generation boundaries vs one file — chose one file (deep intra-family call chains, extractor.py chains all 12 live functions in strict sequence; matches task's approved module list).
- constants.py scope: centralize ALL top-level constants (incl. 2 verbatim-duplicated ones and scattered LF-EXTRACT/title-band blocks) vs leave some local to consuming submodule — chose full centralization (task explicitly names "a shared constants.py for the regex/constant block"; simpler single SSOT; de-dupes the 2 same-value re-declarations for free).
**why-decision:** Extraction correctness verified mechanically at every stage (ast span extraction + byte-diff of every moved function body against the pre-move source) rather than manual retype, eliminating transcription risk on a ~4000L move.
**why-change:** no change from plan — approach matched the backlog-detail brief's named module list exactly.
**verify:** Baseline established first (`python -m pytest -q`: 11 failed/1017 passed/1 skipped — 11 pre-existing env-only failures, no PDF fixtures/Tesseract binary/poppler in sandbox). Ran full suite after every one of the 8 stages — identical 11-failure set + 1017 passed every time (one run showed a 12th failure, `test_extract_layout_and_tables_raises_on_timeout`, reproduced as a known timing-flake per S4's verify note — passes standalone, confirmed via re-run). Symbol-parity: AST-diffed the original module's 112 top-level names (git `c2069debd`, pre-split) against the final shim's resolved `dir()` namespace — 0 missing (except 5 stdlib typing re-exports with zero callers, confirmed via repo-wide grep), 0 unexpected extras. `test_generic_md_table_extractor.py` (the module's own primary unit suite, incl. AC-1 Fence-A AST import-boundary check): 149/149 passed post-split. `main.py` composition-root import smoke-tested (`build_document_map`/`zone_page`/`ocr_unit`/`GenericMdTableExtractor` all resolve). Every new file <=120L or carries an honest `# size-justification:` header. mypy: pre-existing env-blocking `pdf-extractor is not a valid Python package name` error confirmed identical on the ORIGINAL unmodified file via `git stash`/`git stash pop` A/B — not caused by this change, skipped per S4 precedent. G12 sandbox-runner gate: N/A — `pilot-status-pdf-extractor.json` status=DONE (pilot closed 2026-05-24) and the literal `sandbox_runner.py --tier=primitive/module` script referenced by the flow doc no longer exists in this repo state (only `sandbox/` dashboard-trace tooling remains) — pytest is the live DoD gate for FACTORY-type maintenance tasks. rebuild_required=true (infra code moved/restructured) — task board moved to REVIEW, next_agent=qa; not self-closed.

### STEP dev-pdf-extractor-S6 · dev-pdf-extractor · 2026-07-10T00:32:00Z
**task-id:** FIX-BCTC-BT3D-ROWS-THRESHOLD-STALE
**what-done:** Updated `test_extract_tables_bt3d_real_ocr.py` line 152 assertion `rows_stored >= 80` → `>= 79`, with an inline comment tracing to FR-5 dedup (commit `0ae36a0eb`) and the stale-threshold root cause (set `d1be505048`, before FR-5 existed).
**what-considered:**
- only: PO's FIX directive was explicit (update threshold to 79, do not touch dedup logic or re-inflate raw count) — root cause already fully traced in the linked FIX-BCTC-FPT-BT5-BALANCE-GATE decision doc (spurious "868" code from an income-statement EPS 4-value-column bleed, correctly dropped by FR-5 dedup).
**why-decision:** 79 is the verified correct post-dedup row count; no alternative considered.
**why-change:** no change from plan.
**verify:** `-m slow` real-OCR test (Tesseract, ~15s, after installing missing `pdf2image`/`pytesseract` into the project's own `.venv` per checked-in `requirements.txt`): 1 passed (was FAIL on `rows_stored=79 < 80`). Full non-slow suite: 1018 passed/8 failed/7 deselected — `git stash`/`git stash pop` A/B confirms all 8 failures are byte-identical pre-existing env-only failures (missing `pandas` module + rasterizer page-count test-isolation issue), unrelated to this 1-line test-threshold change. Board row self-minted (PO batch bypassed PM decomposition, no board row existed) into `task_board.review[]`, next_agent=qa — unblocks FIX-BCTC-FPT-BT5-BALANCE-GATE's review-lane verification (same named test, first assertion no longer short-circuits before the balance_pass check).
