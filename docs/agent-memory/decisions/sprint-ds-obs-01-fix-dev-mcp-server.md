# Decision Journal — Sprint ds-obs-01-fix · dev-mcp-server

**Sprint goal:** DS-OBS-01-FIX — determine at source whether `freshnessSlaMonitor` sends a WORK/BUG telegram alert when any source exceeds its `stale_threshold`, fix root cause if not. Structurally identical audit-then-fix to the just-shipped sibling BCT-OBS-02-FIX.
**Agent:** dev-mcp-server
**Started:** 2026-07-23

---

### STEP dev-mcp-server-S1 · dev-mcp-server · 2026-07-23T17:20:00Z
**task-id:** DS-OBS-01-FIX
**what-done:** RAW-verified at source. Located `apps/mcp-server/src/scheduler/system/freshnessSlaMonitorJob.ts` (registered in `schedulerJobTable.ts` as `freshnessSlaMonitorJob`, live cron). Traced the FULL breach path: `querySignalAges` → `checkDataFreshnessSla` (domain, classifies HIGH if `age > threshold`, CRITICAL if `age > threshold × 1.5`) → for each breach, `recordSlaBreach` (DB audit row only) → market-hours gate (price/foreign_flow off-hours suppressed) → 60-min cooldown gate → `escalateToCommander(signalType, ageMinutes, thresholdMinutes, severity)`. The **default** `escalateToCommander` implementation does exactly one thing: `postSignal(db, {fromAgent:"freshness-sla-monitor", toAgent:"alert-commander", signalType:"urgent_news", ...})` — a write into the `agent_signals` SQLite table. It never calls `sendTelegramWork`/`sendTelegramBug`. `sendTelegramWork` is imported and called exactly once in the whole file, unconditionally, for the once-per-UTC-day `buildDailySummary()` coverage snapshot (FR-5) — not gated on breach, not labelled as an alert, fires at most once/day regardless of how many/how severe the breaches are that day. `sendTelegramBug` was not imported or called anywhere in the file (confirmed via grep — 0 hits) prior to this fix.
**what-considered:**
- Trust `docs/data/quality-checklist.json` DS-OBS-01="PASS" (evidence: "observability demonstrably working — WORK-channel msg IDs 2743–2766; original FAIL was wrong recheck_how") — rejected outright: that exact evidence string is byte-identical across 4 different, unrelated checklist rows (grep confirmed lines 380, 3398, 3440, 4301 of quality-checklist.json all carry the identical "2743–2766" sentence) — a copy-pasted self-report confab, the same false-positive class the BCT-OBS-02-FIX sibling already found and flagged in this session (`feedback_agent_selfreport_metalayer_confabulation.md`). Not corroborating evidence for THIS specific job.
- Treat the `postSignal(...toAgent:"alert-commander")` escalation as "good enough" (agent-to-agent handoff is a legitimate architecture pattern elsewhere in this codebase) — rejected after checking the actual downstream consumer: `docs/agents/alert-commander/flow/stage-signals.md` line 24 (documented finding, 2026-07-12, still live/current per `git status`) states verbatim: *"Recurring `freshness-sla-monitor` synthetic `urgent_news` signals (infra SLA-breach noise, not real market news) are suppressed under this rule every cycle, correctly."* Alert-commander's own MARKET firing gate (position-danger 3-cond / watchlist-opportunity 4-cond / CRITICAL-override verified_chain|legal_risk|crisis_velocity per `docs/policies/alert-policy.md`) has no branch that would ever convert an infra-noise urgent_news signal into a WORK or BUG telegram send — alert-commander only ever posts to MARKET (event-gated), WORK (its own cycle-status ping), or BUG (its own error path). So the signal-bus escalation is a genuine, confirmed dead end for observability purposes — not a stylistic difference from a direct send, an actual missing alert.
- Consider the daily `buildDailySummary()` WORK message sufficient (it does list every signal's current age) — rejected: it is emitted unconditionally once/day regardless of breach state, is not labelled as an alert, does not scale with severity or breach count, and per `1313-channel-routing-enforcement.test.ts` this file is explicitly classified a "watchdog" file — the checklist's own metric text ("BUG channel has freshness alert when threshold exceeded") demands a breach-triggered send, not a passive daily digest a human would have to read and manually cross-reference against 12 threshold numbers.
**why-decision:** Root cause is real: `freshnessSlaMonitor` breach escalation never reaches a human-visible WORK/BUG telegram channel. `escalateToCommander`'s signal-bus write is consumed and correctly discarded by alert-commander every cycle. Fix mirrors the exact precedent shipped for the sibling issues in this same session: BCT-OBS-02-FIX (`sscCheckerJob.ts`, injectable `sendWorkAlertFn`) and the earlier FR-OBS-01-FIX (`bctcOverdueCheckJob.ts`) — an injectable sender fn, non-fatal on send failure, wired at the exact site the missing behavior belongs.
**why-change:** none — first pass.

### STEP dev-mcp-server-S2 · dev-mcp-server · 2026-07-23T17:30:00Z
**task-id:** DS-OBS-01-FIX
**what-done:** Implemented in `apps/mcp-server/src/scheduler/system/freshnessSlaMonitorJob.ts`: at the same cooldown-gated escalation site as `escalateToCommander` (inside `if (!isEscalationCooldownActive(...))`, after the existing market-hours-off gate), added a direct channel send: CRITICAL severity → `sendBugFn` (new injectable param, defaults to `sendTelegramBug`), HIGH severity → `sendWorkFn` (existing injectable param, already used for the daily summary, reused here). Message format: `[sla-monitor] {severity} breach: {signalType} stale {ageMinutes}min (threshold {thresholdMinutes}min)`. Wrapped in its own try/catch, independent of the `escalateToCommander` try/catch, so a send failure cannot affect `escalations`/`markEscalationSent` bookkeeping (mirrors the non-fatal pattern from both precedents). Gated by the SAME cooldown as the signal-bus escalation (60 min per signal type) to avoid channel spam on a persisting breach across 30-min poll cycles — this is a deliberate reuse of existing infrastructure, not new spam-control logic. New test file `DS-OBS-01-FIX-sla-breach-work-bug-alert.test.ts`, 6 cases: TC-1 HIGH→WORK (not BUG), TC-2 CRITICAL→BUG (not a breach-scoped WORK message), TC-3 off-hours market-hours-only signal skips both, TC-4 escalation-cooldown-active skips both, TC-5 no-breach sends neither, TC-6 `sendBugFn` throwing is non-fatal (escalation bookkeeping still completes) — all GREEN (26 expect() calls).
**what-considered:**
- Route ALL breaches (HIGH and CRITICAL alike) to BUG, matching the checklist's literal metric text "BUG channel has freshness alert when threshold exceeded" verbatim — rejected: HIGH is the "exceeded" tier and CRITICAL is "severely exceeded" (`age > threshold × 1.5`) per the domain service's own `classifySeverity()` doc comment; routing every HIGH breach (including borderline near-threshold noise) straight to BUG would misuse the channel semantics already established elsewhere in this codebase (BUG = dev-actionable failure, WORK = status/visibility) and contradicts the channel-routing discipline `1313-channel-routing-enforcement.test.ts` exists to enforce. CRITICAL→BUG / HIGH→WORK keeps the alert genuinely actionable-graded while still satisfying the checklist's underlying intent (a real breach IS now observable in a human channel).
- Fire the direct alert unconditionally on every breach regardless of cooldown, to guarantee maximum visibility — rejected: would spam WORK/BUG every 30-min poll cycle for a signal stuck in breach; the existing 60-min cooldown gate is exactly the spam-control mechanism this job already trusts for the signal-bus path, reusing it keeps both escalation channels consistent and avoids introducing a second, divergent rate-limit policy.
- Add the send as a new top-level pass, decoupled from the escalateToCommander cooldown/market-hours gates — rejected: would duplicate the gate logic (market-hours skip + cooldown check) that already exists at the exact right site; reusing the site keeps the fix minimal and avoids two independently-drifting gate implementations.
**why-decision:** Matches the checklist's intent (a genuine SLA breach is now observable via WORK/BUG) while preserving the codebase's existing severity→channel semantics and the job's own spam-control cooldown, and stays fully consistent with the two prior sibling fixes' injectable-sender/non-fatal pattern in this same session.
**why-change:** none from S1's diagnosis.

### STEP dev-mcp-server-S3 · dev-mcp-server · 2026-07-23T17:55:00Z
**task-id:** DS-OBS-01-FIX
**what-done:** Verification. `bun tsc --noEmit` clean (0 errors). New test file: 6/6 pass. Full sibling freshness-SLA test suite (`234-vps-health-sla`, `1354b-freshness-sla-monitor-helpers`, `1407b-sla-market-hours-gate`, `1920i-freshness-sla-extension`, `1352c-freshness-sla-monitor-e2e-sscchecker-guard`, `freshness-coverage-map-checker`, `1313-channel-routing-enforcement`): 95/95 pass, no regressions. Full mcp-server suite A/B via `git stash` on the one modified source file (new test file moved out during baseline run to isolate the comparison): AFTER (fix applied) = 14690 pass / 40 skip / 42 fail / 46678 expect(); BEFORE (baseline, fix stashed out) = 14684 pass / 40 skip / 42 fail / 46652 expect(). Diffed the two `(fail)` line sets directly: the only deltas are one flaky news-polling timing pair swapping (`Task 102 pollNews aggregates counts` vs `Task 1227 pollNews recordSuccess Reuters`, both 5000ms-class timeout tests, both fully unrelated files) — zero deltas touch `sla`/`freshness`/`DS-OBS`/telegram files. In particular `235-telegram-send-merge.test.ts`'s `channel='bug' sends to BUG channel` failure (received `{ok:true}` instead of expected `999` — looks like cross-test-file `mock.module` pollution under full-suite parallel load) is present **identically in both** BEFORE and AFTER logs — confirmed pre-existing, not caused by this fix's new `sendTelegramBug` import.
**what-considered:** n/a — verification step, no decision branch.
**why-decision:** RAW-verified at source per the task's constraint (never trust a badge/self-report); A/B git-stash proves the 42 pre-existing full-suite failures are unrelated to this change, isolating the one file genuinely touched (`sendTelegramBug`/`sendTelegramWork` are shared infra used by dozens of files — the A/B stash on just my file, not the shared infra, is the correct isolation boundary).
**why-change:** none — closes out verification.
