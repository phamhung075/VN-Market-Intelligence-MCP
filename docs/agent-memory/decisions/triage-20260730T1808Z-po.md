# Decision Journal — Sprint triage-20260730T1808Z · po

**Sprint goal:** ambient governance tick (router-dispatched: sprint-goal-entries-overage — `.sprint_goal.entries[]`=18 vs cap 15, flagged by claude-manager-helper context-janitor-cycle Pass 5; no active sprint_goal entry owns this work)
**Agent:** po
**Started:** 2026-07-30T18:08:19Z

---

### STEP po-S1 · po · 2026-07-30T18:09Z
**task-id:** (ambient — sprint_goal cap remediation)
**what-done:** Built the archive candidate set by joining each of the 18 visions against every `task_board` lane; 5 had zero referencing rows (DOCLANG-SERIALIZE, KINHDICH-HOVER-DETAIL, PREDICTION-CLAIMS-DAILY-CADENCE, MONEY-RADAR-P0, MERGE-MONEY-RADAR-INTO-MOMENTUM).
**what-considered:**
- Close the 3 oldest by `created_at` (cheapest path to exactly 15)
- Close only visions with zero board rows AND independent shipped-evidence
**why-decision:** age is not completion — `VN-MACRO-TOOLING` (06-14) is still ACTIVE in `task_board.active_sprints` with a backlog row. Zero-row is a necessary orphan-safety gate, not sufficient on its own; empty is not evidence of shipped.
**why-change:** no change from plan.

### STEP po-S2 · po · 2026-07-30T18:09Z
**task-id:** (ambient — sprint_goal cap remediation)
**what-done:** Held PREDICTION-CLAIMS-DAILY-CADENCE OPEN despite it passing the zero-row gate, and archived the other 4.
**what-considered:**
- Archive all 5 zero-row visions (18→13, most headroom)
- Archive 4, hold the prediction one
**why-decision:** its success_metric is LIVE — `prediction_claims` gaining rows on consecutive days. `daily-predict.md` exists and `main.md:127` routes it at the `digest-daily` `30 17 * * *` slot, i.e. the mechanism shipped, but mechanism-exists is not outcome-verified and passive health masks dead data. Closing it would retire a goal whose product outcome nobody has measured.
**why-change:** no change from plan.

### STEP po-S3 · po · 2026-07-30T18:09Z
**task-id:** (ambient — sprint_goal cap remediation)
**what-done:** Re-verified all 4 archive candidates on a plane independent of the board before closing: QueName.tsx:79-95 field-by-field vs the vision's product_decision; live HTTP `/dashboard/money-radar`→302→`/dashboard/momentum` (200); `get_money_radar_composite` in tool-registry + 4 source sites; `doclang_serializer.py` + usecase + unit test wired through `main.py`/`handlers.py`.
**what-considered:**
- Trust the `done_verified` / qa-APPROVED commits on record
- Re-verify raw this triage
**why-decision:** a sign-off commit proves a gate ran on some past tree, not that the artifact survives at HEAD; internal consistency is not corroboration. Cost was one curl plus four greps.
**why-change:** no change from plan.

### STEP po-S4 · po · 2026-07-30T18:09Z
**task-id:** (ambient — sprint_goal cap remediation)
**what-done:** Closed the 4 by writing canonical `status:"DONE_VERIFIED"` + `closure_evidence` via `orch-apply.sh`, then evicted them with `scripts/orch-cold-evict.sh` into `docs/data/orch/archive/2026-07.json .closed_sprint_goals[]`.
**what-considered:**
- Delete the entries in the jq transform (the 2026-07-11 and 07-20 precedent, evidence surviving only in `_note` prose)
- Canonical-terminal-status + cold-evict
**why-decision:** `orchStateSchema.ts` §11 says the drift class that grew this array past its cap is precisely non-canonical terminal tokens the cold-evict predicate can never match. Deleting would have papered over the cap while leaving that machinery unexercised; this path leaves each vision's full evidence in cold storage.
**why-change:** deviates from the two prior PO precedents, deliberately.

### STEP po-S5 · po · 2026-07-30T18:09Z
**task-id:** (ambient — sprint_goal cap remediation)
**what-done:** Ran `orch-cold-evict.sh --dry-run` BEFORE the status flip as a control, then again after.
**what-considered:**
- Run the eviction directly (dry-run output is predictable from the predicate)
- Take a baseline first
**why-decision:** cold-evict has no age gate and sweeps done/done_verified/signals/decision_journal in the same pass — a known class of orphaning un-triaged rows. Baseline was 0 in every category, so the after-run delta of exactly 4 sprint_goal entries proves the blast radius was mine alone.
**why-change:** no change from plan. Landed 18→14, one slot of headroom under cap 15.
