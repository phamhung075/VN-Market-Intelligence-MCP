# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · developer

**Sprint goal:** no goal set (rollover continuation of sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md — base file hit CAP-REACHED 2026-07-30T23:23:53Z)
**Agent:** developer
**Started:** 2026-07-31T01:53:00Z

---

### STEP developer-S49 · developer · 2026-07-31T01:53:00Z
**task-id:** FIX-ORCHSTATE-HEAD-STAMP-DROPPED-CI-RED-1837A
**what-done:** Extended `scripts/orch-stamp-updated-at.mjs` Stage 1.5 diff-based stamper to also cover `.head` (previously rows-only); repaired live `.head` via `orch-apply.sh`; added AC-6 positive-control tests to `scripts/test/orch-apply-wrapper-tests.sh`.
**what-considered:**
- Where `.head.updated_by`'s value comes from on a detected change: (a) invent a 4th CLI arg threaded through orch-apply.sh (touches the reference-only file, wider blast radius, no existing "who is calling" concept in a generic bash wrapper); (b) never backfill it, leave caller-supplied-only (matches "updated_by comes from the caller" literally but leaves the exact recurrence bug AC-2 targets open — a future 3-field literal write would still drop it forever); (c) self-contained backfill inside the .mjs only, preserving caller value when present, honest non-agent placeholder only when absent — chosen.
- Whether to exclude `updated_by` from the head-changed diff predicate (mirroring only `updated_at` exclusion, as rows do) — chose to exclude ONLY `updated_at`, same as rows, so a legitimate updated_by-only change (rare) still counts as "changed" and isn't invisible to the stamper.
**why-decision:** (c) satisfies AC-2's literal text ("also stamps .head.updated_at/.head.updated_by") AND its intent (recurrence prevention) without touching orch-apply.sh's call signature (file list restricts scope to the 2 named files) and without fabricating a false agent identity (git-log precedent: every `updated_by`/`_updated_by` field in this schema — _meta, signal_queue, task_board, and now head — is caller-supplied when present; the fallback only fires when the caller structurally cannot supply one, e.g. the bare 3-field literal antipattern from root_cause).
**why-change:** No change from the row's own AC — AC-4's landmine (don't tighten HeadSchema as primary fix) and AC-3's landmine (don't touch the test) both honored; `apps/mcp-server/src/infrastructure/orchStateSchema.ts` and `apps/mcp-server/src/__tests__/1837a-pipeline-state.test.ts` untouched, read-only per the row's reference-only file list.
**verify:** RED-before-GREEN confirmed both planes: (1) `bun test src/__tests__/1837a-pipeline-state.test.ts` — 3 pass/2 fail before live-file repair, 5 pass/0 fail after; (2) `scripts/test/orch-apply-wrapper-tests.sh` — 3 new HEAD-STAMP-* assertions FAIL on the pre-fix `.mjs` (stashed control run), all 60/60 PASS post-fix, including the 3 that were RED. Idempotency positive-control run directly against the REAL live file (not just fixtures): re-running `orch-apply.sh` with an unchanged `.` candidate produced `head=unchanged` in Stage 1.5 stdout and byte-identical `.head` before/after. `bun tsc --noEmit` clean (no TS touched). Full suite `bash scripts/ci-per-file-isolation.sh 16`: 14830 pass / 40 skip / 36 fail across 12 files — all 12 pre-existing failures are unrelated news-poll/source-health/circuit-breaker tests, confirmed pre-existing via baseline re-run on stashed-out changes (34/34 pass on that subset regardless of this fix being present or absent). `1837a-pipeline-state.test.ts` not in the failed-files list. AC-5 (CI-plane verify) requires a subsequent push+CI run — not yet observed this cycle, flagged for QA/next verify pass.
**operational note:** live `docs/data/orch/orch-state.json` `.head` repair (AC-1) landed correctly but was swept into an adjacent peer commit (`b5f2e9c8b`, `chore(tasks): FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE IN_PROGRESS → REVIEW`) touching the same hot file via explicit-pathspec commit — content verified byte-identical to the intended repair (`updated_at`/`updated_by` present, real timestamp), no separate re-commit needed. Concurrent peer session also force-switched the shared working tree to `task/FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE` mid-session (git branch changed under me); recovered via `git checkout main`, confirmed both owned files intact via content grep before proceeding — no data loss, but a live multi-agent shared-working-directory hazard worth flagging to ops/architect (task-branch checkout by a peer agent inside a shared cwd is not isolated, per `parallel_dispatch.conflict_check` in developer/init.md which assumes `isolation: "worktree"` for parallel work — this peer did not have that isolation).

### STEP developer-S50 · developer · 2026-07-31T07:55:00Z
**task-id:** FIX-NOTEBOOK-AUTOPRUNE-DIRECTION-UNRESOLVABLE-ZERO-TS-NOTEBOOKS
**what-done:** `scripts/agents-flow/notebook-auto-prune.sh`'s same-day/tied-heading direction vote structurally deadlocked (dec=0,inc=0,forever) whenever EVERY retained `## ` section in a file lacks a parseable timestamp — read the real 11 named files first (code-janitor, cowork-refactory-expert x2, digest-predict, idea-forge, market-analyst, ops-mainserver-fetch, pm-alpha-s2-rag-fts-rebuild-cron, po, semble-search, dev-technical-analysis) and found they all share generic ROLLING headings (`## Known patterns / preferences`, `## Identity`, `## Current state`...) matching `.claude/skills/notebook-write/SKILL.md`'s own pre-existing "stable ROLLING heading" concept — not per-cycle dated journal entries at all, so no timestamp signal (real OR git-blame-derived — verified empirically: for template-generated files every heading's blame commit is the SAME bulk-creation commit, so blame degenerates too) can ever exist for them. Shipped 3 changes: (TERTIARY) exclude sentinel-vs-real pairs from the vote (was injecting a phantom vote from trailing rolling headings, e.g. unified-agent.md's `## Prior cycles`); (PRIMARY/AC-1) replace the terminal "refuse to guess, exit 0" branch with a documented default (`newest_first`, drop physically-last) plus a new non-blocking informational signal (`notebook_tiebreak_direction_defaulted`) — the file's own votes and the override table are both still consulted FIRST, unchanged, this is purely the final fallback; (AC-3) `[ "$prev_key" \> "$cur_key" ]` (bash-`[`-only) → `[ "$prev_key" -gt "$cur_key" ]` (POSIX, identical ordering for the fixed-width all-digit ts_key strings, portable). `docs/data/notebook-section-order.json` kept but `_note`/`_maintained_by` corrected to state the true contract (AC-2: manual-only, as-needed, no automated producer by design — the derivation never NEEDS it now, satisfying "make it genuinely obsolete" without inventing a risky auto-writer for a table whose own contract requires human `git log -1 -p` verification to populate safely).
**what-considered:**
- Zero-signal default direction: `newest_first` (drop physically-last) vs `oldest_first` (drop physically-first) vs a size-based ("drop biggest section") heuristic — traced AC-4's own digest-predict.md concretely by hand first (2 sections: `## Known patterns / preferences` 8.6KB, `## Cycle — 13:52 UTC` ~43KB): `oldest_first` drops Known-patterns, leaves the 43KB Cycle section alone, hits the pre-existing single-section safe-fail immediately, NO byte-cap convergence (reproduces the exact prior WORK.md finding for this file); `newest_first` drops Cycle, leaves Known-patterns, converges under cap. Chose `newest_first` — it is the ONLY one of the two directional choices that satisfies AC-4's explicit live-convergence requirement for this named file; a size-based heuristic was rejected as UNSAFE in general (could drop a large-but-just-written newest entry in a genuine journal, the exact anti-pattern the sibling fix already closed) even though it also happens to converge digest-predict.md.
- Git-blame-per-heading as the zero-signal fallback (PO's own suggested example, "physical-order-vs-file-mtime comparison") vs a documented static default — tested empirically FIRST on 3 real files (semble-search.md, idea-forge.md, digest-predict.md's own 2 headings): blame committer-time is IDENTICAL for every heading in template-bulk-created files (same commit authored them all at once) and, for digest-predict.md specifically, blame correctly identifies `Known patterns` as chronologically OLDER (true) but dropping it is exactly the choice that FAILS AC-4 convergence (see above) — rejected, not because it's wrong, but because "objectively true chronological order" and "which section must go for the byte cap to converge" are provably different questions for a non-chronological/rolling-heading file, and AC-4 requires the latter.
- Whether to also fix `_dup_clear_markers_for_file()`'s unrelated zsh `NOMATCH` glob-error noise (surfaced only by my own new Test 9, which invokes the script under zsh directly) — left untouched: different subsystem (duplicate-heading detector, a different task's code), not reachable in production (shebang forces bash), and "never touch files outside assigned task scope" — flagged for a follow-up row instead of scope-creeping the fix.
- Renamed the old `notebook_tiebreak_direction_unresolved_breach`/`notebook-tiebreak-unresolved-*.json` signal type+glob to a new `notebook_tiebreak_direction_defaulted`/`notebook-direction-defaulted-*.json` (informational, priority normal) rather than reusing the old type with new semantics — chose rename: the old type's own historical `docs/signals/processed/*` files (cited as this task's evidence) mean "unresolved, nothing pruned"; silently repurposing that same type string to now mean "resolved, pruned via default" would corrupt any downstream consumer's interpretation of historical vs future signals of that type.
**why-decision:** every design choice was validated against the row's own concrete numbers (the 11 named files' real heading text, the real byte counts for unified-agent.md/digest-predict.md) before committing to it, not derived from the row's prose alone — per this session's standing verify-raw-not-badges discipline.
**why-change:** no change from PO's stated AC-1/AC-2/AC-3/AC-4; the specific default DIRECTION value (`newest_first`) was PO's own worked "e.g." suggestion space ("physical-order-vs-file-mtime... or make the sentinel direction-aware") not a literal mandate — picked via direct AC-4 convergence testing as documented above, not guessed.
**verify:** `bash scripts/agents-flow/notebook-auto-prune.test.sh` 7/7 PASS (T1-T6 unaffected, T7 updated — its old assertion tested the exact "refuse forever" anti-pattern this task closes, now asserts the safe default + informational signal). `bash scripts/agents-flow/test-notebook-auto-prune.sh` 9/9 PASS (T1-T8 unaffected, new T9: bash vs zsh invocation of the SAME fixture produces an IDENTICAL outcome — proved RED pre-fix by hand-tracing the vote under the old `\>` semantics before writing the fix, GREEN post-fix, both invocation paths verified in the actual test run). New `scripts/agents-flow/notebook-direction-corpus-replay.sh` (AC-1, reusable, isolated sandbox, forces a full drain of all 46 real notebooks to exercise every tie depth reachable, not just each file's current state): pre-fix (against `git show HEAD:...` snapshot) 21/46 deadlocked — a SUPERSET of the 11 named live files, since the forced full-drain exposes further sub-ties inside files that resolve fine at their CURRENT content shape; post-fix 0/46 deadlocked. Separately isolated the vote-logic-only diff (no cap/loop confounders) across all 46 files' CURRENT real headings: exactly 2 files show a vote-TALLY difference (qa-responder.md, unified-agent.md — fewer counted votes since sentinel-pairs are now excluded) but the FINAL resolved DIRECTION is IDENTICAL pre/post for both — confirmed 0 files regress. AC-4 live convergence (RAW, not simulated): ran the fixed hook directly against the actual live `docs/agent-memory/notebooks/unified-agent.md` (197L/25703B→90L/11562B) and `digest-predict.md` (192L/51298B→33L/9508B) — both now under the live `docs/data/file-size-caps.json` cap (200L/12000B, read at runtime, not hardcoded); verified every retained section byte-identical to its pre-prune slice via targeted `diff` (only whole sections dropped, consistent with the pre-existing immutability contract) — `docs/agent-memory/notebooks/digest-predict.md`'s single new signal `docs/signals/notebook-direction-defaulted-...json` left in place, unconsumed (mailbox pattern owned by claude-manager-helper, not this agent, matching many other pre-existing untracked signal files already sitting in `docs/signals/`). `shellcheck -x` on `notebook-auto-prune.sh`: same single pre-existing SC2295 info-only finding (unrelated line), 0 new findings. No `apps/` TS/Go touched (zone `cross-service/`, pure bash+JSON+md) — `bun test`/`tsc` structurally N/A. No MCP/gateway tool grant this session (Read/Edit/Write/Bash only) — board-flip via `scripts/orch-apply.sh` directly, `task_release`/`send_telegram` not callable; flagged for the coordinating dev-team session (`owner_client_session=64c7c677-0f0f-4cee-a3ce-dba79d70b7ae`) to perform on my behalf.

### STEP developer-S51 · developer · 2026-08-01T02:30:00Z
**task-id:** FIX-DEVTEAM-IDLE-CHAIN-DANGLING-DEPS-STRAND-5-P0-ROWS
**what-done:** AC-3 (remaining scope; AC-1/AC-2 already applied by dev-team at mint time). Added `orchStateSchema.ts` §13 `checkDependsDivergence()` — HARD FAIL (Stage 1f, `orch-validate.mjs`) when a row carries BOTH `.depends` and `.depends_on` and `.depends` names an id absent from `.depends_on` — the exact shape of the incident (commit 2833b71bf wrote the corrected refs to `.depends_on` only, leaving deleted `S1-SCHEMA-SELECTION`/`P1B-STAMP` resident in `.depends`, silently resurrected forever by `effective_depends_on()`'s union). Plus §14 `checkMissingDependencyReport()`/`collectHotDepStatusLaneIds()` — NON-FATAL report (Stage 1g) of rows whose effective dep set resolves MISSING in both hot (7 flat lanes, mirrors `dep_status_map()`'s own scan) and cold archive (`docs/data/orch/archive/YYYY-MM.json .done_tasks[]`, read best-effort/fail-soft in `orch-validate.mjs`).
**what-considered:**
- Hard-fail vs warn for the divergence guard (AC-3 left this open): chose HARD FAIL, matching Stage 1c/1e sibling precedent in the same file, justified by a live measurement FIRST (0 violations across all 9 lanes as of 2026-08-01, post AC-1 cleanup) proving it cannot break any row today.
- Report-check lane scope: measured live via a scratch jq harness reusing `scripts/lib/devteam-eligibility.jq`'s real `dep_status_map($archive)`/`effective_depends_on` before choosing scope — flat 7 lanes alone gave 4, +active_sprints gave 55, +closed_sprints-only (excl. active_sprints) gave exactly 8, matching the task's own stated "8 other rows" — confirms `dep_status_map()` never scans active_sprints/closed_sprints (so active-sprint intra-sprint deps are permanent, benign, WIP-normal MISSING noise; closed_sprints is settled/frozen, a genuine cleanup signal like the flat lanes).
- Did NOT touch `scripts/lib/devteam-eligibility.jq` (listed in the row's `files` but not required by AC-3's actual text) or change `deps_satisfied()` MISSING semantics (AC-4 constraint) — report-only, no runtime dispatch behavior change.
**why-decision:** hard-fail-with-live-proof mirrors this file's own established Stage 1b/1c/1e justification pattern; the report's lane scope was derived from the runtime mechanism's ACTUAL behavior (not guessed), and the resulting count matched the task's own independently-stated figure — cross-verification, not coincidence.
**why-change:** no change from AC-3/AC-4's text; scope decisions (hard-fail choice, report lane set) were explicitly left to developer judgment by the task and are recorded above with their justification.
**verify:** `bun scripts/orch-validate.mjs` (real live `docs/data/orch/orch-state.json`) → exit 0, Stage 1g prints exactly 8 rows (4 backlog: FIX-BCTC-CTG-BALANCE-SHEET-REFINE/FIX-VPS-SYSTEMD-STARTLIMIT-HARDENING/VERIFY-FIX-VPS-SSH-TRIGGER-FAIL-LOUD-REALDATA/FIX-RAG-COMPACTION-DISK-AMPLIFICATION; 4 closed_sprints: TASK-FFT-L3A/L3B/L4, TASK-502-MOMENTUM-FRONTEND) — matches the row's stated count exactly, confirms Stage 1f/1g do not break them. New `apps/mcp-server/src/__tests__/FIX-DEVTEAM-IDLE-CHAIN-DANGLING-DEPS-STRAND-5-P0-ROWS.test.ts`: 14/14 PASS (incident-shape positive control reproducing the real deleted-id names, subset/no-op/sprint-nested/bare-string negative+positive controls for §13; MISSING negative control + resolved-clean + active-vs-closed-sprint scope proof for §14; 7-lane vs sprint-exclusion proof for the new helper). `bun test src/infrastructure/__tests__/orchStateSchema.test.ts` 104/104 PASS (no regression on the 5 pre-existing stages). `bun scripts/test-orch-validate-ac.mjs` 29/29 PASS (pre-existing CLI-level AC harness, unaffected — its fixture never sets `.depends`/`.depends_on`). `bun tsc --noEmit` clean. `bash scripts/audits/size-lint-justification.sh --check` clean for `orchStateSchema.ts` (header bumped 839L→1074L with a dated delta note, same convention as every prior Stage addition in this file). Full-suite `bun test` not re-run beyond the targeted surface — `FIX-MCP-SUITE-HEALTH-BASELINE` standing red is out of scope per the pinned CANONICAL reading (targeted/merge-gate suite, not repo-wide). No MCP/gateway tool grant needed for the code work; `task_release`/notebook write handled per this cycle's closeout.

### STEP developer-S52 · developer · 2026-08-01T04:50:00Z
**task-id:** FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR
**what-done:** AC-1 documented the same-file differently-staged-hunk case as an explicit NON-GOAL of the pathspec-commit mandate (brief new §2.7 + Risk flag 5 + Test-strategy/Files addenda; `dev-standards.md`'s CANONICAL sweep-guard block, the doc the row's own text pointed at). AC-2 added T11 (pins git's `-o`/`--only` semantics — pathspec commit takes WORKING TREE content of the named path, not the staged subset — replaying the row's exact `git apply --cached`-then-pathspec-commit reproduction) + T12 (negative control) to `pre-commit.test.sh`. AC-3 shipped `_detect_samefile_pathspec_only_divergence()` in `scripts/git-hooks/pre-commit` — compares each SCOPED-commit path's about-to-land blob (scratch index) against its blob in the REAL shared index; mismatch → stderr WARN + `write_signal`, never gates (ignores `GIT_SWEEP_GUARD_MODE` entirely, always returns 0). AC-4 migrated `cowork-refactory-expert.md:57`'s bare-commit-with-directory-add example to the pathspec form.
**what-considered:**
- Detector placement: BEFORE vs AFTER the existing `[ "$mode" = "SCOPED" ] && exit 0` gate — before, so it can actually run for SCOPED commits (after would be dead code); kept AC-4's original "SCOPED never blocked" guarantee by having the exit remain unconditional regardless of what the detector found.
- Divergence signal: compared `git rev-parse ":$f"` under the scratch `$GIT_INDEX_FILE` (already set by git for the commit about to land) vs the same call with `GIT_INDEX_FILE` overridden to the real `.git/index` — chosen over `ps`-argv parsing (brief §2.3 already flags that as fragile/best-effort-only) or a working-copy diff (would need a temp checkout); blob comparison is O(1) git-plumbing, no new dependency, mirrors the existing discriminator's own git-internals-based design.
- Whether to also touch the 3 `.claude/skills/commit-mutex|commit-boundary|commit/SKILL.md` files that carry the actual pathspec-commit mandate TEXT: did NOT — architecture brief §7's own zone table assigns `.claude/skills/` to agent-father, not developer; the row's explicit `Files:` list names only `dev-standards.md` among knowledge docs, and the row's own AC-1 parenthetical points there too — honored the zone boundary over the more ambiguous prose reading.
**why-decision:** empirically verified the exact bug first (scratch-repo repro matching the row's own 2/2 claim byte-for-byte before writing any doc or test), then built the detector against that SAME verified mechanism rather than the general "-o" manpage description alone — avoids shipping a detector tuned to a misunderstanding of the semantics.
**why-change:** no change from AC-1..AC-4's text; AC-4 was explicitly marked lower-priority/do-if-room — landed since AC-1..AC-3 completed cleanly with room left, per the row's own instruction.
**verify:** `bash scripts/git-hooks/pre-commit.test.sh` 12/12 PASS (T1-T10 unchanged/unaffected — re-ran before AND after the detector addition, byte-identical pass set; new T11/T12 both PASS). `shellcheck` on both `scripts/git-hooks/pre-commit` and `pre-commit.test.sh` — 0 new findings (pre-existing SC2030/SC2031 info-only notices on T7-T10 actor-subshell exports, untouched by this change). `bash -n` syntax-clean. Manually verified false-positive-clean against T4's rebase-replay scenario (instrumented trace: real-index blob == scratch-index blob for both replayed commits, 0 divergence flagged) and against the ordinary `git add <path> && git commit -- <path>` idiom (T2/T12) before committing the detector logic. This is a pure bash/markdown zone (`cross-service/`) — no `apps/` TS/Go touched, `bun test`/`tsc` structurally N/A. Live-fired: this task's OWN commit ran through the newly-edited hook (repo's `.git/hooks/pre-commit` is a symlink to the file just edited) — confirmed clean (exact 5-file pathspec commit, no divergence warning, no sweep).

### STEP developer-S53 · developer · 2026-08-01T03:35:06Z
**task-id:** FIX-DEVTEAM-REVIEW-LANE-SECONDARY-DRAIN
**what-done:** Implemented brief §2 (`docs/architecture-briefs/2026-08-01-review-lane-drain-throughput-and-secondary-sweep.md`) verbatim: new `scripts/devteam-review-claim-secondary-drain.jq` (age-ordered pick of the oldest `review[]` row where `status=="REVIEW" && effective_next_agent!="qa"`, stamps `secondary_claimed_at`/`secondary_claimed_by`/`secondary_dispatch_target` IN PLACE — no lane move, never writes `.head`); new shared predicate `resolved_secondary_dispatch_target` (`scripts/lib/devteam-eligibility.jq`, next_agent-or-"po"); new `### Review-Lane SECONDARY-Drain` section in `docs/agents/dev-team/flow/main.md`, inserted between Session Gate and Step 1 PO Triage (the head-decoupled anchor the qadrain-head-slot-decouple SPIKE brief already named) — unconditional (no `head.status` gate), single-row cap, ALWAYS falls through to Step 1 (never JUMP TO end, per this task's own AC). Re-measured live at implementation time (not the stale 41 in the row's own desc): review[]=249 (246 REVIEW/3 BLOCKED), PRIMARY(qa)=202, SECONDARY=44.
**what-considered:**
- Compose a new `is_secondary_drain_eligible($detail_items)` predicate in the shared lib (status+next_agent bundled) vs inline `select(.value.status=="REVIEW") | select(...!="qa")` in the claim script — chose inline, mirroring `devteam-review-claim-qa-drain.jq`'s and `devteam-backlog-promote-bounded1.jq`'s own established convention of keeping lane/status membership checks in the caller, only flag-resolution logic in the shared lib.
- `age_epoch`: copy into the new script (mirrors QA-Drain's own local copy) vs extract to the shared lib — brief §2b explicitly recommends NOT forcing extraction at 2 callers; followed verbatim.
**why-decision:** brief §2 is the ratified, architect-authored design (PO-triage-sourced mint); implementing faithfully avoids re-litigating a settled shape and keeps this lane's file family/pattern consistent with SLS/RLC/DRS/QA-Drain for the next reader.
**why-change:** no change from the brief's own §2b text.
**verify:** scratch-copy dry-run against a live-board copy (never the real file): claims the single oldest SECONDARY row, `.head` byte-identical before/after, `.task_board.review` array length unchanged (in-place stamp confirmed, no lane move). `jq -n 'include "scripts/lib/devteam-eligibility"; ...'` load-check passes; empty-review[] fixture confirmed no-op/idempotent. `bun tsc --noEmit` 0 errors (no TS touched — pure jq/bash/md zone `cross-service/`). `bun test`: 15044 pass / 40 skip / 57 fail / 1 error (baseline re-measured live, well above the row's stale 9408 reference — suite grows over ticks per the task's own instruction); the 57 fail/1 error are structurally unrelated to this change — grep-confirmed zero test file executes/imports either touched jq file (2 hits are comment-only references), and no TypeScript file was touched at all, so this task cannot have caused or fixed any of them. Graphify: no Skill-tool path available to this spawned agent (same structural constraint as prior S21-S27/S31 entries) — flagged in WORK.md, not silently skipped.

### STEP developer-S54 · developer · 2026-08-01T04:05:00Z
**task-id:** FIX-COLDEVICT-SIGNALQUEUE-NO-AGE-GATE-ORPHANS-READ-ROWS
**what-done:** AC-1 added `SIGNAL_MAX_AGE_HOURS` (default 24) ANDed with the existing status check in `orch-cold-evict.sh`'s `signal_queue.rows[]` selection (null/unparseable `.ts` = oldest, matching done[]/decision_journal[] convention). AC-2 TEST 9 in canonical `scripts/test/orch-cold-evict-tests.sh` (fresh/73h/100h/NEW/null-ts fixtures, live run + idempotent re-run, 47/47 suite pass). AC-3 live-verified `docs/data/orch/orch-state.json`: 0 signal evictions before AND after (no live row is currently `READ`; all are `triaged`/`RETRACTED` — separate, unfixed status-vocab drift, flagged not fixed here). AC-4: SKILL.md needed NO edit.
**what-considered:** Task text asserted SKILL.md's "documented contract" is 48h. Verified live: SKILL.md + dashboard-protocol.md BOTH currently say 24h — `git log -p` on SKILL.md shows this was a DELIBERATE HSC-7 change (commit `bff0362c2`, 2026-06-26, msg: "criteria changed to 24h threshold"), a month before this task was minted. The memory citation of "48h" (CONFIRMATION 3, incident memory) is itself stale — it matches `docs/agents/dev-team/flow/drain-signals.md` §0a-D-PRUNE, a DIFFERENT flow doc never updated at HSC-7 (still says 48h + references the removed `signal_queue.archive[]` lane).
**why-decision:** Implemented 24h (live doc), not 48h (task text) — AC-4's own stated intent is "script matches doc SSOT", and the doc unambiguously says 24h, consistently, deliberately, for 5 weeks. Blindly hardcoding 48h would have CREATED the exact doc/script drift AC-4 exists to prevent. Did not edit SKILL.md (already correct) or drain-signals.md (out of this task's file scope, dev-team-owned flow doc) — flagged the drain-signals.md drift via a LOW `system-issue` signal to po (`dev-20260801T035943`, direct bash+jq+orch-apply.sh write, no MCP grant this session) instead of silently absorbing or unilaterally hot-patching it.
**why-change:** Numeric deviation from the task's literal "use 48h" instruction — justified above with git-history evidence, not a guess; AC-2's own 73h fixture value is cutoff-window-agnostic (passes under both 24h and 48h) so no fixture redesign was needed. SECONDARY grep (`TERMINAL_SIGNAL_STATUSES` call sites): only `orch-cold-evict.sh` itself; `dev-team-tick-preflight.sh` Step 5.5 fully delegates to the script (no own copy of the predicate) — no code change needed there.
**verify:** `bash -n` syntax-clean. `scripts/test/orch-cold-evict-tests.sh` 47/47 PASS (T1-T8 regression-clean, new T9 6/6). Non-vacuousness of T9's fixture hand-verified via `git stash`: pre-fix script wrongly evicted 2/2 fixture rows (both fresh+stale), post-fix evicts exactly 1 (stale only) — stash popped, fix confirmed restored before committing. Live dry-run before/after fix: `signal_queue.rows[] would evict: 0` both times, byte-identical hot file. This is a pure bash/jq zone (`cross-service/`) — no `apps/` TS/Go touched, `bun test`/`tsc` N/A. Commit `d963b487b` (pathspec: `scripts/orch-cold-evict.sh scripts/test/orch-cold-evict-tests.sh`).

### STEP developer-S55 · developer · 2026-08-01T04:35:06Z
**task-id:** FIX-DEVTEAM-PREFLIGHT-STEP55-COLDEVICT-STDOUT-LEAK-CORRUPTS-VERDICT
**what-done:** AC-1: `_step55_run_cold_evict()` in `dev-team-tick-preflight.sh` now captures `bash orch-cold-evict.sh 2>&1` into a var (same pattern the `--dry-run` seam already used) and unconditionally re-emits to stderr — never lets the child's stdout stream reach this script's own stdout, which every caller (main.md's `VERDICT_JSON=$(...)`) parses as one JSON document. AC-2: new T36 in `dev-team-tick-preflight.test.sh` — captures the REAL (post-fix) function under `_step55_run_cold_evict_REAL` (T35-style), adds a `$USE_REAL_COLD_EVICT` stub toggle, and points `$ROOT` at a hermetic fixture dir whose fake `orch-cold-evict.sh` reproduces the exact live-incident leak shape; asserts `run_preflight`'s stdout parses with `jq -e .` and the leaked lines land on stderr instead. 103/103 suite PASS. AC-3: added a 5th JUMP-TO table row in `docs/agents/dev-team/flow/main.md` for `$VERDICT` empty/unparseable (routes to `preflight-fallback`, same as ERROR). AC-4: live re-verified via a scoped scratch script calling the REAL `_step55_run_cold_evict` directly (never `run_preflight` — HARD CONSTRAINT forbids live-claiming SF-1/fire-election outside a real tick) against a COPY of the actual live `orch-state.json`: stdout=0 bytes, all 5593 bytes of progress+report text correctly on stderr.
**what-considered:**
- Fix only the wrapper call site (`_step55_run_cold_evict`) vs also patching `orch-cold-evict.sh`'s own internal SHG-3 gate call (line ~930, `bash orch-state-validate.sh` unredirected) which I found is the ACTUAL leak source (its delegate `orch-validate.mjs` writes a non-fatal Stage-1g REPORT straight to stdout, confirmed by direct repro: `bash orch-cold-evict.sh 1>out 2>err` puts the REPORT text in `out`). Chose wrapper-only: matches the row's explicit `files` scope (3 files, not `orch-cold-evict.sh`), and a boundary capture at the CALLER is architecturally correct regardless of where inside the child process something forgets to redirect — more robust than chasing one call site.
- T36 fixture-based vs driving the REAL orch-cold-evict.sh end-to-end inside the stubbed test harness — chose fixture: the real script's non-dry-run path performs actual file writes (cold archive + orch-apply.sh gated hot write) even against an isolated copy, adding flakiness/slowness risk to a suite whose own header mandates "zero real side-effecting calls"; the fixture reproduces the exact reported byte shape deterministically.
**why-decision:** the row's own root-cause pin + AC-1 wording named this exact capture pattern; verifying it against BOTH a controlled fixture (T36, deterministic) AND real live-shaped data (AC-4 scratch script, empirical) closes the gap between "the stub proves the wiring" and "the real leak source is actually swallowed."
**why-change:** none from the row's AC text — flagged (not fixed) the deeper `orch-cold-evict.sh:930-932` leak-through as an out-of-scope finding for po/architect follow-up, since fixing it there too is not in this row's file list.
**verify:** `bash scripts/agents-flow/dev-team-tick-preflight.test.sh` 103/103 PASS; re-ran against pre-fix code via `git stash` — T36 fails 4/5 checks as expected (non-vacuous regression guard), restored post-fix all green. `bash -n` clean both files; `shellcheck -x` 0 new findings. AC-4 scratch verification: exit=0, stdout=0 bytes, stderr=5593 bytes (incl. the real `[orch-validate] Stage 0 + Stage 1 PASS` line that pre-fix would have leaked). No `apps/` TS/Go touched (zone `cross-service/`) — `bun test`/`tsc` structurally N/A.
