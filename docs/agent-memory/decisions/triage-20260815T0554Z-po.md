# Decision Journal — triage-20260815T0554Z · po

**Sprint goal:** targeted owner-triage — dev-team Review-Lane SECONDARY-Drain sweep, single `review[]` row held `plan_only:true`/`supervised:true`; a PLAN review, not a code sign-off
**Agent:** po
**Started:** 2026-08-15T05:54:35Z

---

### STEP po-S1 · po · 2026-08-15T05:54:35Z
**task-id:** FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX
**what-done:** Ratified dev-mcp-server's PLAN-ONLY design (system-map.json SSOT for the D4-R1b lock-prefix whitelist), authorized implementation (`plan_only` true→false, `po_goahead_20260815T055435` stamped, `next_agent`/`owner` → dev-mcp-server, row stays in `review[]`), and attached three binding acceptance clauses AC-5/6/7 that the plan as written does not satisfy. Appended the full verification trail as § 8 of `docs/handoffs/FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX-spec.md`. Withdrew two of PO's own prior claims on this row as factually wrong. Split one out-of-scope root cause to a new backlog row.
**what-considered:**
- Ratify as-is and route straight to implementation — REJECTED. Two defects let the fix ship with the SSOT silently inert while 100% of the stated acceptance reads GREEN: (i) § 5b's verbatim "After" JSON block nests the list at `.known_legit_lock_prefixes.prefixes` while § 5a's reader and § 5b's own closing prose both specify a bare `string[]`; an implementer copy-pasting the verbatim block — the entire point of a "verbatim diffs" section — trips `!Array.isArray` and silently returns `FALLBACK_KNOWN_LEGIT_PREFIXES` behind a `console.warn`. Because the fallback already carries both new prefixes, the false positive is still suppressed, the `D4 SKIP` lines still print, and every § 5e test still passes. (ii) § 5e cannot detect that at all: its only loader test deliberately targets the fallback path via `/nonexistent/path`, and every `isKnownLegitPattern` assertion passes identically under SSOT or fallback. Net effect without correction: "three hardcoded copies" becomes "one hardcoded copy plus a decorative JSON key", undetectably.
- Bounce the row for a spec-revision round trip, then implement in a later cycle — REJECTED. The row was created 2026-07-25 and the corrections are precisely scoped; two round trips for edits the implementer performs anyway is the churn-without-convergence pattern. Instead the corrections were made *binding* by appending them to `acceptance` (what QA checks) rather than left as prose in a note, which cannot be silently skipped.
- Accept dev-mcp-server's refutation of PO's own earlier `pipeline_mismatch` category-error claim on its say-so — REJECTED per `supervised-goahead.md` § Step 2 (never ratify on a relayed verdict). Re-derived it from source instead.
- Move the row to `ready[]` — REJECTED: `supervised:true` + `plan_only:false` is exactly one of the two flags, the DRS READY-XOR stranding shape. Kept in `review[]`, whose SECONDARY-drain picker is proven live (it dispatched this very review).
- Move the review prose to the row's `detail_ref` cold store as the ceiling guard directs — REJECTED as unavailable, not declined: this row has no `detail_ref`, and the sanctioned hatch `scripts/orch-backlog-stub.sh` is itself blocked by P1 `FIX-ORCHBACKLOGSTUB-COLD-ITEMS-ARRAY-SHAPE-CRASH-BLOCKS-LANES-MIGRATION` (minted 2026-08-15T04:19Z). Put the detail in the spec doc — the artifact the implementer actually reads — and kept the row lean. No ceiling retune, no split write.
- Authorize with AC-5/6/7 binding + full trail in the spec doc — CHOSEN.
**why-decision:** The core design decision is genuinely sound and I ratified it on re-checked evidence, not deference: option 2b (system-map.json SSOT + non-empty fail-open fallback) is right, and its rejection of a structural `<kind>:<TICKER>:<PERIOD>` regex holds because the live lock `bctc-dataquality:vnindex-crosstool-mismatch` is not ticker/period-shaped, so a shape regex would have missed the very false positive that was forming. PLAN-ONLY was genuinely honored: zero commits on `tasksMdJanitorJob.ts` since `claimed_at`, worktree clean on all four files; the two commits that did touch the listed paths (`15492ff61` cowork SSOT, `d89392752` VPS route set) are unrelated peer work. Prefix coverage passes the test my own 08-11 fold set — the spec covers both `bctc-dataquality:` and `data-quality-anomaly:`, not just the row title's known-wrong prefix. AC-7 is my own design ruling and the one an implementer would not have caught: `tasksMdJanitorJob` is a daily cron (`cronConfig.ts:198`) inside the long-lived scheduler process (`schedulerJobTable.ts:953-957`), so § 5a's module-scope `const KNOWN_LEGIT_PREFIXES = load...()` is evaluated once at container start and every later `system-map.json` edit is inert until restart — silently re-creating the exact staleness class this row exists to kill, and contradicting § 5a's own comment "add new prefixes to system-map.json, not here". The `seedWatchlist` precedent the spec leans on does not transfer: `WATCHLIST_SEED` is module-scope because seeding is startup-scoped by design, per its own comment.
**why-change:** Two corrections to PO's own record, both from reading source I had asserted about twice without opening. (1) `po_second_facet_20260728`'s "category error — a whitelist entry alone will NOT silence it, the FP count only halves", restated in `po_fold_20260811T1237Z` as "compares against EVERY held lock, structurally a false positive by construction": WRONG both times. `tasksMdJanitorJob.ts:632` reads `for (const lock of survivingLocks)` — the post-R-1b set — and `git blame` puts that line in `e109f49f81`, the same commit that ported R-1b, so one whitelist entry silences both the R-3 `not_found` and R-2 `pipeline_mismatch` emissions. dev-mcp-server was right and I was wrong; both claims withdrawn on the row. Generalizable: I characterized a loop twice, 14 days apart, without reading it, and it survived into a P2 scope statement a downstream implementer would have widened the fix on — read the loop before characterizing the loop. (2) The same fold's "THIRD" defect calling bctc-analyst's `task_kind='sprint-task'` an enum misuse: WRONG. `taskClaimTool.ts:60` defines the enum as exactly `["cowork-slot","sprint-task","dashboard-row","commit-mutex","intent","orphan-signal","session-presence"]` — no guard/marker member exists, so `sprint-task` is the only available value, and four other agents (`cron-registration:*`, `esc-datacov:*`, `esc-deepdive:*`, `po-triage-*`) do the same. That missing enum category is the real root cause one level below this row: it is why D4's `kind:"sprint-task"` scan set is polluted with non-tasks, which is the only reason a name-prefix whitelist has to exist at all. Tellingly, `session-presence`/`commit-mutex`/`intent:*` do have their own enum members and are already excluded structurally. Split to `FIX-TASKKIND-ENUM-NO-GUARD-MARKER-CATEGORY` (P3, zone multi, next_agent architect, depends_on this row), with the spec's own § 7 flag about the undocumented `bctc-dataquality:` lock-kind naming drift folded in as its AC-4 rather than minted as a third row. This row is explicitly NOT blocked on it.
**note:** Live-state correction recorded on the row for whoever implements: all three `bctc-dataquality:*` locks cited in the 08-11 fold have TTL-expired, and the hot `signal_queue` holds zero `sau-d4-*` rows. The false positive is currently DORMANT — the "2-per-day into PO's inbox" framing that drove P3→P2 is not true today. It recurs at the next mint (these ran ~8 days, 08-06 → 08-14), so P2 stands, but the instruction is implement properly rather than fast.
