# PO Triage Decision — 2026-07-25T14:07–14:31Z

**task_id:** FIX-SIGNALQUEUE-TRIAGED-NO-EXIT-TRANSITION
**Context:** dev-team Step 1 triage. Two items: (1) signal_queue 124-row strand, (2) unacked launchd label burning ~3M tokens/day.

## Item 1 — signal_queue TRIAGED strand

**what-considered:**
- (a) Add `triaged`+`RETRACTED` to `TERMINAL_SIGNAL_STATUSES` + skill PRUNE criteria.
- (b) Make `SignalRowSchema.status` a strict `z.enum` so improvised values fail loud.
- (c) Fix the missing exit transition + the false-green echo, leave the vocabulary alone. **CHOSEN.**

**why-change (both offered options rejected):**
The ask's premise — "`triaged` is improvised, exists in no contract" — is FALSE. The supporting grep was
`grep -rn '"triaged"'`: lowercase and double-quoted. The contract writes it UPPERCASE and unquoted in prose:
- `docs/agents/system-auditor/handlers.md:240` — hot file holds "ONLY active rows (status `NEW` or `TRIAGED`...)"
- `docs/agents/po/flow/scripts-registry.md:44` — po-s137 status-guard "NEW/READ->RESOLVED — the cold-evictable terminal, NOT TRIAGED"
- `.claude/commands/crons/cron-db-data-integrity.md:130` — dedup treats TRIAGED as already-open; `:138` forbids detection-only crons from flipping to it

So TRIAGED is canonical and deliberately NON-TERMINAL, and its absence from both prune lists is the DESIGN, not the gap.
(a) would therefore evict rows whose tracked work is still open.
(b) was already formally refuted on 2026-07-12 — `docs/architecture-briefs/2026-07-12-ultracode-workflow-improvement-audit.md:1155`
item (5): the enum omits TRIAGED and ACUTE-RESOLVED-ROOT-TRACKED, both live vocabulary; since `orch-apply.sh`
Zod-validates the WHOLE document on every write, one legitimate TRIAGED flip after tightening would hard-reject
ALL subsequent orch-state writes fleet-wide (`project_mcp_server_write_wedge` class).

**Actual root cause (verified):** TRIAGED has no exit transition. The only automated one,
`docs/agents/pm/flow/task-archive.md:118`, is
`.signal_queue.rows |= map(if .id == $sid and .status == "READ" then .status = "RESOLVED" else . end)` —
a TRIAGED row hits the `else .` no-op. Line 122 then echoes `Signal closure: ${SID} READ→RESOLVED`
UNCONDITIONALLY, so every skipped closure has been logging success.

**Evidence:** 125 rows = 124 triaged + 1 RETRACTED, zero NEW/READ/RESOLVED (the hot queue holds ONLY stranded
rows). All 124 older than the 24h gate (oldest 2026-07-20T05:26:06Z) — status is the sole blocker, not freshness.
Cold archive `.signal_rows` = 304 rows, 95 READ + 209 RESOLVED, **zero triaged** — none has ever been evicted.
(The "337x triaged in archive" figure counts `last_triaged_at`/`last_triaged_by` KEY names: 249 key-path matches,
0 scalar values.) Not accelerating: newest triaged row is 2026-07-23T18:33Z, ~44h stale → high, not P0.
Backfill tractability: 122/124 have `triaged_by`, 49 have `disposition` (45 naming a board id), **0** have `tracked_by`.

**Deferred to architect:** which of the two documented semantics governs, and the disposal rule for the ~64% of
rows with no machine-readable back-reference. Not PO's call to pre-decide.

**Dedup:** read and cleared 5 adjacent rows absent from the supplied prior-art list —
`FIX-BACKLOG-TERMINAL-ROW-DRIFT-EVICT-BLIND` (isomorphic, `task_board.backlog[]` plane),
`P1-DETECTOR-CLOSURE-TRIAGE-SIGNALS` (forward half of the same mechanism — sequence first),
`FU-ORCH-HOT-SUB150-SPRINT-LIFECYCLE`, `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT`,
`FIX-COLD-EVICT-EXCLUDE-IDS-VS-HARD-COHERENCE`. Plus the 5 supplied. No duplicate.

## Item 2 — launchd ACK

**what-considered:** (a) ack `com.vn-market.cowork-guaranteed-slot-firer` against
`FIX-GUARANTEED-SLOT-FIRER-FANOUT-TRUNCATION`; (b) judge the signature out of scope and mint a new row.

**why-change:** chose (a). Verified from raw sources, not the relayed summary: `launchctl print` reports
`last exit code = 143`; `cowork-guaranteed-slot-firer.log:1313-1316` shows `13:22:25Z invoking (bounded 1800s)
slot=fb-weekend` → `13:52:25Z flow exited exit_code=143` — exactly 1800s. That is bound **(b) OUTER** as
documented verbatim in the row's own `two_distinct_bounds_do_not_conflate` field, and matches its own reproducer
(chef-morning 1800s SIGTERM, 2026-07-22). Row confirmed `BACKLOG`, so the ack is not stale-on-arrival.

**Correction to the relayed framing:** the "Background tasks still running after 600s" line is bound (a) INNER and
is **not** today's evidence — `cowork-guaranteed-slot-firer-error.log` mtime is 2026-07-18T22:27, unchanged, exactly
as the row already recorded. Today's run produced its deliverable (main body done 13:25:29Z) and was then SIGTERMed
27 min later at the outer bound. So today's instance is the bound firing on an already-complete run, not work
truncation. Same row, correct bound.

**Accepted tradeoff:** the ledger is label-granular, so a not-loaded/crash-at-startup failure of this label is now
also suppressed. Recorded in an additive `signature` field on the entry (the probe reads only `.acked[]?.label`,
`auditor-tier1-probe.sh:274`, so extra fields are inert). Mitigation is the ledger's STALENESS RULE — remove on
FANOUT-TRUNCATION DONE_VERIFIED.

**Verification:** probe re-run with `HEARTBEAT_FILE_PATH` redirected to scratch → `verdict: ALL_GREEN`, acknowledged
labels named in `detail`. Live heartbeat untouched (still 13:44:06Z). Executable verified, not prose.
