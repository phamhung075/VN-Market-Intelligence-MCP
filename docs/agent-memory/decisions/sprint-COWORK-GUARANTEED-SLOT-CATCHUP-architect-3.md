# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · architect

**Sprint goal:** cowork guaranteed-slot catch-up + related supervised-lane FIX rows
**Agent:** architect
**Started:** 2026-08-06T07:01:55Z

---

### STEP architect-S1 · architect · 2026-08-06T07:01:55Z
**task-id:** GUARD-COWORK-NOTEBOOK-AGENTS-SELF-EDIT-FLOW-DOC
**what-done:** Wrote plan-only architecture brief (docs/architecture-briefs/2026-08-06-guard-cowork-notebook-agent-write-boundary.md) for the recurring cowork-agent self-edit-flow-doc class.
**what-considered:**
- Strip Edit/Write from the 7 agents — rejected, already false-premised by PO's own row note (breaks docs/signals/*.json routing, ~207 committed files by design).
- Patch each of the 15 flow-file call sites individually — rejected, violates always_extend_not_duplicate; traced root cause to ONE shared skill (cowork-end-cycle → doc-self-heal) with zero boundary awareness.
- New bespoke boundary-config file vs extending docs/data/system-map.json .project.agents[] — chose extend (CLAUDE.md names it the structural-data SSOT; agents[] entries already exist).
- New heavy improvement_proposal-style PO-critique lane for doc-fix-proposal vs a light single-verify lane — chose light (factual/schema-typo class doesn't need 5-field critique + LANE-A/B/C; reuses edit.md apply engine as-is).
**why-decision:** Root-caused via grep (cowork-end-cycle Step 3 → doc-self-heal, loaded by all 7 at 15 sites incl. the exact incident's own "Doc self-heal" self-report string) rather than treating 4 instances as 4 separate agent bugs — fixes the single control point instead of the symptom sites. Mechanism (PreToolUse hook keyed on the verified Claude Code agent_type hook field, confirmed by reading the installed CLI's own Zod schema, not assumed) makes the boundary real per the row's explicit ask ("mechanism, not prose"); doc-self-heal's new Step 0 gives the redirected agent something to DO instead of a dead end, closing item 3 in the same design.
**why-change:** No change from plan; PLAN-ONLY scope honored throughout — no code/config shipped, brief + board update + signal only.

### STEP architect-S2 · architect · 2026-08-06T07:36:18Z
**task-id:** FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND
**what-done:** Designed 2 in-place edits to main.md Step 0b (WF-1 task_status source-array widen + new WF-1b terminal-lane self-heal branch; WF-2 row-lookup widen as defense-in-depth) + a new synthetic-fixture regression verifier spec; wrote full design into the row's `architect_review_note`, set `next_agent=developer`, moved `backlog[]→ready[]` for RLC pickup.
**what-considered:**
- Widen WF-1's task_status source lanes only (no new branch) — rejected: "DONE" still != "BLOCKED" so the existing `if` never fires; the S2 spawn still happens. Only an explicit new branch that JUMPs away satisfies AC-2.
- Patch WF-2's should_hold alone to add done[]/done_verified[] — rejected as the SOLE fix: a non-supervised terminal row already yields should_hold=false today and would STILL fall through to S2; kept only as literal-AC-1 defense-in-depth, documented as provably unreachable once WF-1b lands.
- Reuse `is_terminal_task_status`/`normalize_task_status` (scripts/lib/devteam-eligibility.jq, already backing wip_in_progress + wrapper-autoclose) vs. a fresh literal DONE/DONE_VERIFIED check — chose reuse (always_extend_not_duplicate).
**why-decision:** Traced the exact control-flow reason should_hold-only or lane-widen-only fixes silently fail to prevent the S2 spawn (neither JUMPs away); WF-1b mirrors the BLOCKED carve-out's proven shape (idle-reset + JUMP TO drain-signals) minus its lane-move, since a terminal row is already correctly lane-placed by the closing specialist (INV-GATEWAY-1 — it just can't write `.head`).
**why-change:** Found the row's stated duplicate-mint already self-resolved by po (`po_dedup_fold_20260806T0726`) before this pass started — no dedup work needed, verified live via jq/python before writing (file had been concurrently edited since first read).

### STEP architect-S3 · architect · 2026-08-06T08:20:00Z
**task-id:** FIX-VNINDEX-CROSS-PLANE-PLAUSIBILITY-GATE
**what-done:** Ratified BA's FR-1 target (vn_index_cache/market_prices.VNINDEX) but with a load-bearing correction: read the actual Go adapter (`repositories_market_index.go`, BA never opened it) — macro-indicators' own tier-1 PRIMARY query reads `market_prices.VNINDEX` directly off the SAME physical SQLite file, so on the tier-1 path this is not a second plane, it's the same row twice. Designed `evaluateVnIndexPlausibility()` (new domain guard) + exact `macroTools.ts` wiring (single mutation point before both `text` render and raw `data` passthrough) + EC-1's fail-open/fail-closed branches. Appended full design to `docs/handoffs/FIX-VNINDEX-CROSS-PLANE-PLAUSIBILITY-GATE-BA-spec.md`; moved board row `backlog[]→ready[]`, `next_agent=dev-mcp-server`; annotated sibling `FIX-VNINDEX-ESTIMATE-IMPLAUSIBLE-DELTA-GATE` with a fold-forward pointer (not closed — fold happens on completion per BA §7).
**what-considered:**
- market_prices raw query (BA's literal wording) vs the existing typed `getVnIndexCache()` reader — chose the reader (already typed, already carries `fetched_at`, avoids a 2nd ad-hoc query; same underlying write event either way, verified via `vnIndexRefreshJob.ts`).
- New "≤10 min" staleness constant (BA's EC-1 wording) vs reusing `freshnessSlaChecker.ts`'s existing `signalType:"price"` SLA — chose reuse: it's already market-hours-aware (EC-2's requirement satisfied for free, no weekend/holiday false-staleness) and avoids a duplicate hardcoded threshold.
- Whether to overclaim FR-2's "tier-1 misreport" defense — traced it precisely: catches the fixture-fallback incident class + tier-mislabeling, does NOT catch a corrupted-but-honestly-tier1-reported value (same physical row both sides) — documented the limit explicitly rather than let a stronger-than-true guarantee ship in a code comment.
- Route `next_agent` to `pm` vs directly to `dev-mcp-server` via `ready[]` — chose direct-to-dev-mcp-server (design is fully atomic: 1 zone, 2 new files + 1 wired file, exact insertion point + field-level gap-token spec already given), matching the S2 precedent (RLC picks up an architect-resolved ready[] row, no PM decomposition needed).
**why-decision:** Brownfield-first: read `repositories_market_index.go` before ratifying BA's "genuinely independent write path" claim — it wasn't, on the tier-1 path — and that correction changes what the shipped code-comment is allowed to claim. Self-caught a live jq comma-in-`|=`-RHS bug on the first orch-apply attempt (multi-field update silently dropped all but the first clause) — verified via read-back before treating the write as done, then re-applied correctly with `+` merge.
**why-change:** No change to the durable AC (Part 2 / FR-1 cross-plane gate) — refinement is entirely in the documented scope-of-defense and the reuse choices, not the requirement itself.

### STEP architect-S4 · architect · 2026-08-06T09:03:00Z
**task-id:** FU-RAG-DEPLOY-MEMORY
**what-done:** First confirmed OOMKill on rag-service (router RAW-verified docker events). Designed the sizing remediation: raise docker-compose.yml memory limit 768m→1g / reservation 256m→512m, plus a structural idle-unload timer for the embedder singleton (no unload path existed).
**what-considered:**
- Verify actual host/VM headroom before recommending a number, rather than trusting the "16GB host" framing on the row — live `docker info`/`free -h` inside the VM shows Docker Desktop is capped at 8192MiB (7.75GiB), not 16GB; sum of all container limits is already ~12.25GiB (1.58x over-commit) against ~2.74GiB actual usage.
- Cap-raise only vs cap-raise + idle-unload — chose both: the raise closes today's crash, but the corpus grows unboundedly and the embedder has no release path, so a bigger cap just re-fails later at a higher number.
- New idle-unload machinery vs some other footprint reduction (smaller model, quantization) — idle-unload is symmetric to the EXISTING lazy-load pattern (GFD-13) already proven safe in this exact file, lowest-risk structural option.
**why-decision:** Host macOS swap is separately at 86% (sysctl vm.swapusage) — flagged as an independent, pre-existing risk, but a container memory LIMIT is a ceiling not a reservation, so raising it does not itself add host pressure unless usage grows into it; verified this distinction live before recommending the number.
**why-change:** no change from plan — PO explicitly framed the choice as "raise cap, reduce footprint, or both"; this delivers both rather than picking one.

### STEP architect-S5 · architect · 2026-08-06T09:03:00Z
**task-id:** FIX-SYSTEM-AUDITOR-CYCLE-FINDINGS-NOT-SELF-PERSISTED
**what-done:** Tested PO's background-vs-foreground discriminator (consistent with, not provable from repo evidence alone) and found independent, non-self-report proof of the failure class: 6 orphaned empty `.auditor-cycle-markers-<TICK>.tmp` scratch files (3 known Tier-2 ticks + 3 NEW Tier-1 ticks from 2026-08-05, previously unflagged) — each one mechanical evidence a cycle won its fire-election and died before its own final cleanup line, with zero matching notebook sections. Designed a plan_only fix: Step 0b stale-marker/stale-draft sweep (external detectability within one tick) + reorder notebook-write earlier + durable scratch-draft-then-compose (self-heals via deterministic script/skill logic, not model reconstruction).
**what-considered:**
- Trust the discriminator as given vs go find independent evidence — went looking, found the 6 orphaned markers were an EVEN STRONGER, mechanically-provable signal than the discriminator itself.
- Fix only the notebook-write step vs treat it as a whole-cycle durability gap — the empty (zero-marker) content of all 6 orphans shows death happens very early, before any anomaly emission, so scoping the fix to "the write step specifically" would under-diagnose it.
- Add more instructions/emphasis to the flow doc vs convert to a checked, self-healing mechanism — chose the latter, matching this codebase's own established precedent (`auditor-notebook-commit.sh`'s header: narrated sequences for hard invariants cannot be trusted).
**why-decision:** The false-peer-citation symptom PO flagged (citing an unrelated session's c44 as its own) is the signature of a model reconstructing lost content from memory near the end of a long turn — the fix must make reconstruction unnecessary (durable draft persisted at composition time), not just detect the loss after the fact.
**why-change:** no change from plan — plan_only preserved per router's instruction; moved BACKLOG→READY (design complete) with next_agent=pm since the fix spans a flow doc + a shared skill, not a single atomic file.

### STEP architect-S6 · architect · 2026-08-06T09:03:00Z
**task-id:** FIX-CHEF-PUBLISHED-MARKER-RELEASE
**what-done:** Corrected the row's own premise (grep found zero live `task_release` call sites on any `published:*` key — the 08-06 release was a manual PO action on a content judgment, not an automated "chef.md cleanup"). Designed unified with sibling FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR per PO's "one lifecycle, one design" ruling: Component A propagates `scheduled_utc_time` (already computed by `cowork-catchup-predicate.js`, unconsumed) into `spawn-fanout.md`'s trigger_prompt for BOTH live and catch-up fires so retries can never re-derive a different key; Component B adds a mechanical Published Marker Release Gate (delivery-evidence check) before any future release, human or automated.
**what-considered:**
- Redesign TASK-COWORK-CATCHUP-3's dispatcher wiring from scratch vs reuse it — reused; it's already READY/developer-owned with a full handoff, my gap-find was that it only covers the catch-up path, not live matches, which I closed by extending `cowork-match-slots.js` symmetrically instead of duplicating logic.
- Silently correct the row's premise vs state it plainly on the board — stated it plainly; the row's title otherwise misdirects an implementer toward deleting code that doesn't exist.
- Flip FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR's own status/next_agent since I designed its content too vs leave it untouched — left it untouched (out of this cycle's assigned scope), added an additive cross-reference field only so PM/developer don't re-derive the same mechanism twice.
**why-decision:** The 08-06 incident showed a release performed on a content judgment ("wrong dish on this slot") where a delivery check would have refused it — Component B targets exactly that gap, not a hypothetical automated-release code path that no longer exists.
**why-change:** no change from plan — BACKLOG→READY, next_agent=pm (multi-file, multi-zone fix needs decomposition), blocked_by cleared (PO's own note already ruled it unblocked this cycle).

### STEP architect-S7 · architect · 2026-08-06T09:23:00Z
**task-id:** FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN
**what-done:** 3rd PO manual-dispatch pick, 1st real spawn. Found the row's own 2026-07-22 remedy shipped but cannot keep pace (226 PRIMARY review rows vs qa[]<1, drain sits LAST in a 5-lane head-idle fall-through). Found un-cited-by-this-row prior art (SPIKE-DEVTEAM-QADRAIN-HEAD-SLOT-DECOUPLE Part1/2/3 + this row's own 08-01 brief) already designed most of the fix but never dispatched. Added 4 new findings: Part2's depends_on was circularly deadlocked on a row itself stuck in review[] 8d; Part2's illustrative code had drifted from Part3's batch shape; Part3 was mixed-zone (main.md wrongly assigned to developer, not agent-father); pure age-order starves 7 same-day P0 rows behind a 13-14d P2/P3 wall. Wrote `docs/architecture-briefs/2026-08-06-review-lane-qadrain-throughput-unblock.md`, promoted+refined both child rows to READY, moved this row ready[]->backlog[]/BLOCKED with machine-readable depends_on both children.
**what-considered:**
- Re-derive a fresh Part2/Part3 design vs finish the existing ones — finished existing: both were already implementation-ready, the gap was dispatch + 4 integration defects, not a missing design.
- Fold Part2+Part3 into one row vs keep the 2-row split — kept split: zones differ (agent-father vs developer), only moved the misrouted main.md half.
- Leave this parent row status READY (next_agent=pm) vs BLOCKED — chose BLOCKED with explicit depends_on: it is genuinely non-actionable until children ship, and this row's own history proved "eligible-but-unpicked" is a worse state than honest BLOCKED (orch-validate confirmed BLOCKED is invalid in ready[] lane — required the ready->backlog lane move, learned live).
**why-decision:** Every claim grounded in live jq reads (226/40/361/60/1 lane counts, 8d-stale transitive dep, 27 P0/7 same-day rows, commit_zone.allowed grep) or direct grep of main.md/the two prior briefs — never trusted the priors' narration alone (both were 5+ days old and one dependency they cited as live had since become obsolete).
**why-change:** Router asked for a fresh design; found 90% already designed and mis-filed as undispatched rather than unplanned — this cycle's real work was unblocking + integration-correcting, not re-designing from zero.

### STEP architect-S8 · architect · 2026-08-06T11:05:00Z
**task-id:** GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS
**what-done:** Adjudicated PO's 3 candidate directions (task_claim mutex / append-only / fan-out cap) using raw tied_sections evidence (11 signal files) — found non-monotone retained-set AND 3 simultaneous duplicate `## cycle-504` headings, proving concurrent lost-update + stale-counter allocation, not a pruning-order bug. Ruled out the "3rd writer formatter hook" hypothesis by reading context-bloat-backstop.sh in full (signal-only, never mutates notebook content). Wrote `docs/architecture-briefs/2026-08-06-guard-notebook-concurrent-write-mutex.md`: (a) is correct direction but `task_claim`'s `task_kind` is a closed 7-value Zod enum with no fitting kind — needs an additive `"notebook-write"` extension (companion dev-mcp-server row); (b) can't work standalone (retention/prune needs full-rewrite, no true append primitive exists); (c) confirmed insufficient alone by an independent fact (the original tran-ngoc-bau.md incident was N=2, not N=9). Recommended task_claim mutex bracketing only the mechanical read-merge-write span (never the LLM's authoring time, to avoid this repo's own corroborated TTL-hazard lesson) + a stat-based staleness guard in notebook-auto-prune.sh as bash-only defense-in-depth (deliberately NOT a new mkdir/flock lock file, since docs/agent-memory is a named, still-unexcluded VirtioFS-race bind-mount per head-lock-self-cure.md — reusing task_claim's server-side lock state sidesteps that hazard class entirely).
**what-considered:**
- New mkdir/flock-style lock file under docs/agent-memory/notebooks/ vs task_claim — rejected the filesystem lock: this exact directory is named as a live, confirmed (not hypothesized) VirtioFS bind-mount race hazard (H4, git HEAD.lock incident history), so a new atomic-create primitive there inherits an already-burning bug class instead of avoiding it.
- Having notebook-auto-prune.sh call task_claim directly vs having the calling AGENT hold the claim across its own Write (whose cascading PostToolUse hook then runs inside that held window) — chose the latter: verified no MCP-from-bash bridge exists anywhere in this repo, so the hook literally cannot call task_claim; piggybacking on the agent's own held window is the only feasible route and costs zero extra primitives.
- Recommending scripts/notebook-compose.sh as a NEW script vs cross-referencing the sibling brief's already-designed one — cross-referenced only (always_extend_not_duplicate): that script is a different row's (FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS) deliverable, not yet implemented; my AC-7 bracket is written to wrap whichever compose mechanism exists, current or future.
**why-decision:** Read the actual TaskKind Zod enum + coordinationStore.ts source (not assumed reachable) before recommending (a) — found the gap that would have made the recommendation silently unimplementable. Read docs/protocols/head-lock-self-cure.md before proposing any filesystem primitive, per this project's own corroborated-incident memory on that exact class of bug.
**why-change:** No change from PO's ask (adjudicate, don't just report) — added the task_kind-enum gap and the VirtioFS risk flag as decisive new findings neither PO's triage nor the router signal had surfaced.

### STEP architect-S9 · architect · 2026-08-06T20:10:00Z
**task-id:** FIX-EXECUTETIER-PHASE35-RESUME-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION
**what-done:** Adjudicated the mint's own open question (blind-copy vs design call) against 3 already-shipped precedents in this SAME codebase (post-cycle.md Step 4.4 Wrapper-Autoclose per-row loop; main.md QA-Drain idle+head-decoupled batches; SECONDARY-Drain) — all 3 already claim+spawn MULTIPLE independent `task:<id>` rows per tick with NO `.head` relationship, using the exact "no release on success, per-item except-only release" shape. Ruling: the fix applies to EVERY claim in `spawned_batch` unconditionally — `task:<id>` gates task liveness, not `.head`; there is no different-correctness case for multi-task batches. Rewrote execute-tier.md Phase-3.5 (per-task try/except replacing the batch-wide `finally`; added missing `owner_client_session` to both task_claim/task_release, matching P1-FINAL/TASK_1980). Also adjudicated `po_scope_widen_20260806` candidate (b) (Step-0b short-circuit on `head.updated_by` prefix) — rejected: provenance can't distinguish "already spawned, hold" from "collided, retry legitimately," and duplicates a liveness check `task_claim` already answers correctly once (a) lands. Wrote+live-ran `scripts/audits/execute-tier-phase35-locklifetime-verify.sh` against the REAL task_claim/task_release primitives (mirrors adb426877's own live-proof methodology) — self-caught a jq `//`-on-`false` footgun in my first draft (silently discarded a genuine `claimed:false`), fixed to explicit null-checks, re-ran: 4/4 PASS.
**what-considered:**
- Blind-copy S2's single-task pattern only for the BOUNDED-1-JUMP-TO-execute case, leave genuine multi-task PM-tier batches on the old finally-release shape — rejected: the 3 existing multi-item precedents above prove the pattern generalizes with zero modification; carving out a "different" case would itself be the unreviewed design deviation.
- Adopt candidate (b) (Step-0b provenance short-circuit) instead of / alongside (a) — rejected alone and rejected as a supplement: fixing the lock's own hold-duration is necessary and sufficient; the provenance check is strictly weaker and risks permanently stalling legitimate S2 retries on a collided-but-never-spawned BOUNDED-1 head.
- Live network round-trip vs synthetic-fixture harness (this file's own execute-tier-branchnull-review-headidle-verify.sh precedent) for the regression script — chose live: the defect lives in task_claim/task_release's OWN mutex semantics, not a local jq transform: no synthetic replay proves anything the real primitive doesn't already assert.
**why-decision:** Grounded every claim in a live grep/read of the 3 precedent call sites (not the mint script's own unverified "different correctness" hypothesis) plus a live MCP round-trip, not flow-doc prose alone — same evidentiary bar the original S2 fix (adb426877) set.
**why-change:** PO's `po_occurrence_3_20260806` also flagged a COMPOUNDING defect (`head.next_agent` left as literal `"dev-team"`, not spawnable) — left explicitly out of scope in the flow-doc comment (distinct bug in BOUNDED-1's own claim script, not yet a tracked row) rather than silently folding a fix for it into this row's deliverable.

### STEP architect-S10 · architect · 2026-08-07T13:23:49Z
**task-id:** FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER
**what-done:** Source-verified the fast-follow I flagged on the 2026-08-05 `FIX-MCP-MEMORY-CODE-LEAK` brief (Finding 2), now promoted to critical after PO's own root-cause chase ended in an observed container death. Read `transport.ts` line-by-line: `mcpServer` (line 95) is a bare local, never `.close()`d by either existing eviction path (`res.on("close")`, heartbeat-write-fail) — confirmed via SDK source (`shared/protocol.js:500-502`, `server/sse.js`) that `McpServer.close()` alone cascades to `transport.close()`, so one call suffices. Cross-corroborated the "gateway dials per-call, drops it" traffic shape independently via `perCallCounterStore.ts`'s own header comment (unrelated feature, same fact). Design: collapse the 2 parallel `Map`s + 2 duplicated inline cleanup blocks into 1 `SessionRecord` map + 1 `evictSession()` every trigger (close/heartbeat-fail/idle-reap/max-age-reap/new `DELETE` route) calls — the duplication itself is why `mcpServer.close()` went missing from BOTH existing paths, not just the absent-reaper half. `lastActivityAt` bumped in `handleMessage()` drives idle timeout; `createdAt` drives a hard max-age backstop. Wrote `docs/architecture-briefs/2026-08-07-fix-mcp-sse-session-manager-reaper.md` (full test plan: 7 new unit cases extending `1862c-transport-session-eviction.test.ts` incl. a negative control proving the activity-bump actually resets the idle clock, not just exists; 2 new integration cases extending `081-bun-mcp-server.test.ts` for the new `DELETE` route).
**what-considered:**
- Wiring `transport.onclose` as a 6th eviction trigger vs relying only on the 5 proactive call sites — rejected onclose: the SDK already fires it from inside `transport.close()`, which `evictSession()`'s own `mcpServer.close()` call already reaches: wiring it back would be a self-re-entrant no-op, not a new mechanism.
- Having the `DELETE` route be the primary fix vs defense-in-depth only — defense-in-depth: the `gateway` MCP server that originates the dominant per-call-dial traffic is out-of-repo (confirmed no `/sse` client code exists under the unrelated Go `apps/api-gateway/` zone) — cannot be made to send clean DELETEs, so only the reaper actually bounds the leak regardless of caller behavior.
- Idle/max-age threshold values — flagged explicitly as informed-not-derived defaults (constructor params, same override pattern the existing `_heartbeatIntervalMs` already uses), with the exact `/health sessionCount` vs `MemPerc` correlation PO already ran once recommended as the empirical tuning loop post-deploy, not asserted as final.
**why-decision:** Read the installed SDK source directly (not the type declarations alone) to confirm the `.close()` cascade before designing around it — a wrong assumption there would have produced a design calling both `transport.close()` and `mcpServer.close()` redundantly or, worse, only the wrong one.
**why-change:** No change from the row's own `po_rawverify_20260807T0600Z` design constraint (retain McpServer alongside transport, add idle/max-age reaper + DELETE handler) — this brief operationalizes exactly that into file-level changes + tests; added the unify-into-one-eviction-path structural point (not explicit in PO's note) as the reason both PRE-EXISTING paths, not just the missing reaper, needed the same fix.

### STEP architect-S11 · architect · 2026-08-07T19:15:00Z
**task-id:** FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT
**what-done:** Verified premise (grep confirmed zero signal_queue/signal-dashboard references in either unified-agent/ or alert-commander/ flow trees; neither init.md declares the mechanism). Corrected the repro fixture: po-20260720T052606 is now status=triaged (PO folded it 2026-07-22 into FIX-CHEF-L6-GOLD-FALSE-PREDICATE, since the task-board row's own po_repro_case/po_status_decision fields were stale). Decided hybrid: SSOT-flagged push at cowork-team/spawn-fanout.md (new signal_queue_push flag on 6 cowork-schedule.json slots) + minimal no-op-default consumption step added to chef.md/cycle.md, reusing dev-team drain-signals.md's existing dash:signal_queue: lock namespace. Wrote docs/architecture-briefs/2026-08-07-fix-signalqueue-receiver-delivery-contract.md.
**what-considered:**
- Pure-(a) full pull/READ step in each leaf flow — rejected: task_claim/task_release marked "Phase 2 Ready, not active" in both tool packages; duplicates the CAS-guard dance already centralized in cowork-team (5th recurrence risk of the documented-consumer-no-producer class); works against both flows' explicit narrow-scope design.
- Pure-(b) push-only, no consumption contract — rejected alone: fixes the mechanical NEW->READ transition but not the behavioral cost this row itself names (ongoing wrong output); chef.md's fixed recipe has no step defining what to do with unsolicited prompt content.
- (c) remove both from Receivers table — rejected: both are live multi-sender targets; removal reproduces the same undeliverable-class gap one level up with no replacement route.
- Generic push for every cowork-fired slot.agent — rejected: tran-ngoc-bau is also cowork-fired AND already implements a correct pull (bootstrap.md Step 0b-DASH) — generic push would race/duplicate a working path. Scoped via an explicit SSOT flag instead of a hardcoded 2-agent allowlist.
**why-decision:** Hybrid is the only option that closes both the mechanical gap (row leaves NEW) and the behavioral gap (signal actually consumed) while reusing already-active tooling (cowork-team's live task_claim/task_release) instead of activating dormant wiring in two more flows, and without disturbing tran-ngoc-bau's already-correct independent path.
**why-change:** No prior design existed for this row (fresh architecture decision, PO ordering ruling only fixed sequencing vs FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT, not the mechanism). Lock-plane statement (§6 of the brief) added per PO's explicit fileplane-split ruling requirement so FIX-DRAIN-FILEPLANE-PEER-COLLISION-GUARD's closure is not blocked by an apparent contradiction.

### STEP architect-S12 · architect · 2026-08-07T21:40:00Z
**task-id:** SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS
**what-done:** Probed BOTH planes for 34 tickers (33 watchlist + HCM per PO fold): SERVING via `get_bctc_full` (bash `mcp_call` — gateway meta-tools absent this session), DB via direct `data/live/market.db` read (bind-mount confirmed, `financial_reports` count matches last QA note). Read `checkPublishability` (PUB-1) source and traced the exact mechanism blocking every mode-3 ticker: `refine_status='PENDING'`, not `validation_status`. 27/33 unservable, decomposed 11/4/11 across modes 1/2/3. No new row minted.
**what-considered:**
- Trust the SPIKE's own pre-assigned mode-3 home (`FIX-BCTC-VALIDATION-GATE-NONBANK-ZERO-SCALE`) — rejected: live code read proves `validation_status` plays no role in the PUB-1 block; every mode-3 ticker has real `extraction_method=pdf-parse` non-zero scalars and 0 `bctc_refined_units` rows, exact match to `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP`'s own stated scope. Re-routed mode-3 there instead of blind-copying the SPIKE's assumption.
- Filter DB by `period_quarter=1` (int) vs `sort_key='2026-Q1'` (text) — caught mid-probe that 2/257 rows store `period_quarter` as literal `"Q1"` (HUT's own row); switched to `sort_key` for every subsequent query to avoid silently mis-classifying HUT as DB-absent.
- Mint a 4th mode for group {NVL,SSI,VCI,HCM} — rejected: all 4 are DB-present/non-zero/refine-PENDING, identical mechanism to 7 other watchlist mode-3 tickers; PO's fold decision (a) confirmed correct.
**why-decision:** Every classification is grounded in a live tool call + a live DB row this tick, not a prior signal/notebook claim — disproved (not just left "unproven") the SSI/NVL 07-15→07-21 regression via `parsed_at` timestamps predating the reparse window by 6 weeks with zero writes since.
**why-change:** Corrected the SPIKE's own mode-3 routing assumption (§6a of the brief) after source-level evidence contradicted it; flagged (not fixed, plan_only) a stale dormancy premise on `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP` (producer active today, backlog growing 151→181→230) and a scope-narrowing finding on `FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T` (11/15 of its named cohort moved on to later stages; 2 new true members BID/DAG surfaced).
