# PO decision journal — triage of code-janitor "optimize workflow tools actual agent use" audit, 2026-08-22T16:41Z

**task_id:** FIX-JANITOR-DEADCODE-AUDIT-CALLER-SURFACE-TOO-NARROW-MASS-FALSE-POSITIVES (mint) + DEBT-SCRIPTS-ONESHOT-JQ-TRANSFORM-ACCUMULATION-NO-RETIREMENT-POLICY (mint) + CLEAN-SCRIPTS-54-UNREFERENCED-QUARANTINE-FIRST (mint) + CWO-T4-P0-TUSTATS-PERAGENT (fold) + CHECK-TOOL-INTERFACE-SCHEMA-DRIFT-DOC-VS-LIVE (fold)
**Inputs:** code-janitor audit findings relayed via router · `docs/agent-memory/modules/tool-usage-stats.json` + its git history · `docs/data/tool-registry.json` · live gateway probe · full-repo reference scan (11938 git-tracked regular files) · `.task_board` (429 backlog rows)

## S1 — The MCP "13 dead tools" finding: 13/13 false positives, zero deletions warranted

**what-considered:** (a) accept the list and open a tool-consolidation sprint per janitor's recommendation (c); (b) spot-check two or three tools then decide; (c) test the oracle itself before testing any individual tool.

**why-decision:** Took (c). The router's spawn brief explicitly flagged the stats-file-undercounting risk, and this project's memory carries the matching precedent (`reference_agent_signals_is_a_rolling_2h_ttl_window_not_an_accumulating_log`, `feedback_pressure_state_caller_supplied_fields_dead_server_computed_live`). Testing individual tools first would have inherited the oracle's defect.

First finding is a path error: janitor cited `docs/data/tool-usage-stats.json`, which **does not exist**. The real file is `docs/agent-memory/modules/tool-usage-stats.json` — 975 bytes, three keys (`generatedAt`, `toolCounts`, `uniqueTools`), no window metadata of any kind.

The oracle is a **rolling window that resets**, proven from its own git history, not argued:

| revision | uniqueTools | get_market_snapshot |
|---|---|---|
| 2026-07-10 | 17 | 700 |
| 2026-07-10 | 19 | 789 |
| 2026-07-16 → 08-07 | 0 (×5 revisions) | absent |
| 2026-08-07 | 15 | 48 |
| 2026-08-12 | 68 | 1994 |
| 2026-08-22 (janitor's read) | 30 | 951 |

Counts are **non-monotonic** and `uniqueTools` repeatedly collapses to 0. A tool's absence means "not called in the current window", never "dead".

Direct refutation: **8 of the 13** flagged tools carry real call counts in the 2026-08-12 snapshot, 10 days before the audit — `get_foreign_room`=39, `get_bctc_full`=12, `get_bctc_report_id`=9, `get_insider_sentiment`=10, `get_market_sentiment_index`=10, `get_breadth_thrust`=3, `get_rate_limit_status`=3, `get_relative_strength`=3. Acting on recommendation (c) would have deleted eight demonstrably-working tools.

The other 5 decompose without any deletion either. `get_agent`, `get_deficit`, `get_files`: **zero exact-word matches** anywhere in `docs/agents/**` — janitor's grep was unanchored and matched `get_agent` inside `get_agent_signals`. They were never documented, so "documented but unused" is false on both halves. `get_financial_summary`: registered in `tool-registry.json` *and* referenced at `docs/agents/market-analyst/flow/main.md:106` — simply idle this window. `get_current_date`: the one real drift, inverted from janitor's framing — it is referenced at `docs/agents/digest-predict/flow/main.md:50` but does **not** exist on the live server (probed: `MCP error -32602: Tool get_current_date not found`); benign today because the call site reads "call get_current_date **if that tool is available**".

**why-change:** Recommendation (c) REJECTED in full — no MCP tool is a deletion candidate. The finding is redirected at the oracle: folded a new prerequisite AC onto the existing `CWO-T4-P0-TUSTATS-PERAGENT` row (make the file self-describing with window metadata) rather than minting a duplicate, and folded the `get_current_date` case onto `CHECK-TOOL-INTERFACE-SCHEMA-DRIFT-DOC-VS-LIVE` as a ready-made fixture, since that row already exists precisely to catch documented-vs-live drift.

## S2 — The "619 orphan scripts / 84% dead" finding: magnitude wrong by ~11x, same root cause

**what-considered:** (a) accept and plan a staged sweep; (b) defer entirely on the in-flight-files concern janitor raised; (c) re-run the orphan detection with a corrected corpus and re-derive the number.

**why-decision:** Took (c). Both of janitor's findings came from one grep methodology, so once the tool finding proved to be a grep artifact the script count needed independent re-derivation before any planning.

Janitor resolved callers from a single surface: `docs/agents/*/flow/main.md`. That structurally excludes sub-flow docs (this fleet deliberately splits flows — `po/sprint-kickoff.md`, `cowork-team/flow/pressure-cadence.md`), `.claude/skills/**`, `.claude/commands/crons/*.md`, `apps/*/src/__tests__/**`, `docs/policies/dev-standards.md` CANONICAL pointers, and script-to-script calls.

Measured on the 268 non-jq scripts — janitor surface: **61** referenced; flows+policies+skills+CI+hooks: **168** (2.75x). Against the full corpus of 11938 git-tracked regular files, only **54 of 908** script basenames are referenced nowhere at all (5.9%), and 51 of those are one-shot dated `.jq`. For `.sh` specifically, **174 of 177** are referenced. So "619 with no confirmed caller (84%)" overstates the real orphan set by roughly 11x.

One methodological trap worth recording: an early pass of mine reported only 10 non-jq orphans and 545 "referenced" `.jq`, because `apps/mcp-server/scripts` is a **symlink to `../../scripts`**, so `grep -r` followed it and every file matched itself. Re-ran restricted to git-tracked regular files with symlinks excluded. Self-match contamination is the mirror image of janitor's too-narrow corpus and would have produced an equally wrong answer in the opposite direction.

**why-change:** Minted `FIX-JANITOR-DEADCODE-AUDIT-CALLER-SURFACE-TOO-NARROW-MASS-FALSE-POSITIVES` at P1 — the audit *method* is the defect, and it will keep manufacturing false positives on every future janitor cycle until fixed. AC-3 requires a negative control replayed against real files, not inspection.

## S3 — Recommendation (b) ("archive dead .jq dispatchers, delete experimental agents-flow/*.js"): REJECTED, would break production

**what-considered:** (a) accept as the "medium-risk" tier janitor labelled it; (b) accept the `.js` deletions but hold the `.jq` archiving; (c) verify each named file against the wider surface first.

**why-decision:** Took (c). Every named file is load-bearing:

- `scripts/agents-flow/cowork-match-slots.js` — invoked from `.claude/skills/cron-cowork-team/SKILL.md` **and** `.claude/commands/crons/cron-fb-market-poster.md`. Deleting it breaks the cowork master dispatcher fleet-wide.
- `scripts/agents-flow/cowork-tick-preflight.sh` — invoked from `.claude/commands/crons/cron-code-janitor.md`. **code-janitor proposed deleting a script its own cron runs.**
- `scripts/agents-flow/cowork-supersede-mutex.js` — wired in `docs/agents/cowork-team/flow/pressure-cadence.md`.
- `scripts/agents-flow/cowork-tick-postflight.sh` — a CANONICAL pointer in `docs/policies/dev-standards.md`.
- `scripts/agents-flow/cadence-policy.js` — under live test by `apps/mcp-server/src/__tests__/DWF-phase1-cadence.test.ts`; deleting it turns CI red.

**why-change:** REJECTED in full and recorded as such on the P1 row, so no future agent re-attempts it from the same stale finding. This is the concrete cost of the narrow-corpus defect and is the strongest justification for the P1 priority.

## S4 — What is genuinely real: the one-shot .jq accumulation, and the safe residue

**what-considered:** (a) close the whole audit as false-positive and take no action; (b) mint one broad cleanup row; (c) separate the real policy gap from the small safe cleanup and size them differently.

**why-decision:** Took (c). Janitor's *framing* was wrong but the mass it noticed is real, with a clean root cause. `scripts/` is 908 files: 626 `.jq`, 177 `.sh`, 50 `.ts`, 17 `.js`, 13 `.py`, 9 `.mjs`. The `.jq` class is 69% of the directory and single-use by construction — names are per-tick and per-decision (`po-s108-idle-wip-promote-groom-terminal-backlog.jq`, `router-d41-fbop-liveverify-partial-queryrecon-20260615.jq`, `po-triage-20260808T1248Z-auditor-verdict-transcription.jq`). By prefix: 265 `po-`, 46 `qa-`, 16 `devteam-`, 15 `dev-fix-`, plus a large `router-` family. **516 of 626 are older than 30 days; zero were modified in the last 7 days.**

They exist because CLAUDE.md's orch-state write contract requires every board mutation to be a jq transform piped through `orch-apply.sh`, and Script Persistence says anything "useful for the work or reusable later" must be saved to `scripts/` and never `/tmp`. The policy has a SAVE rule but **no retirement rule and no reusable-vs-one-shot distinction**, so one-shots accumulate monotonically. That is the actual defect, and it is spec work — it needs an explicit ruling on where one-shot transforms belong, which puts two standing directives in tension. Routed `next_agent: ba`, not a direct FIX.

Janitor's recommendation (a) — canonicalize "the 41 active scripts" into `dev-standards.md` as SSOT — REJECTED as specified: 41 is derived from the broken surface, and a hand-maintained list of live scripts would drift within days. Folded into the DEBT row as "generate it or don't build it".

**why-change:** Split into two rows of honest size: `DEBT-SCRIPTS-ONESHOT-JQ-TRANSFORM-ACCUMULATION-NO-RETIREMENT-POLICY` (P2, SPRINT-S, `next_agent: ba`) for the policy gap, and `CLEAN-SCRIPTS-54-UNREFERENCED-QUARANTINE-FIRST` (P3) for the 54 genuinely-unreferenced files — 51 one-shot `.jq`, 2 one-shot payload `.json`, and `probe-nso-sheet-names.cpython-313.pyc`, a compiled artifact that should never have been committed. The CLEAN row mandates re-deriving the set at execution time (≈74 files were in flight from concurrent agents) and reuses the existing quarantine-first `scripts/audits/clean-obsolete-files.sh` rather than any bare deletion, which also honours janitor's own correct instinct to defer the sweep.

## S5 — Meta: why this triage wrote no code and deleted nothing

**what-considered:** (a) act on the "low-risk" tier immediately since janitor labelled it safe; (b) act on nothing until the method is fixed.

**why-decision:** Took (b) for all deletion/consolidation actions. Every one of janitor's three tiers — including the "low-risk" (a) — was derived from the same defective caller resolution, so risk labels assigned by the defective method carry no information. The only safe immediate action is the one whose predicate is strictly stronger than the corrected method ("referenced nowhere in the entire repo"), which is what the CLEAN row uses.

**why-change:** No change from plan. Consistent with the finding itself, this tick's own one-shot jq transform was written to the session scratchpad rather than persisted into `scripts/` — adding a 627th dated one-shot while minting a row about their accumulation would have been self-refuting.

## S6 — Mid-cycle addition: orch-sentinel OH-4 crons never armed (agents-architect brief F3-2)

**task-id:** FIX-ORCH-SENTINEL-OH4-CRONS-NEVER-ARMED-DEAD-OBSERVABILITY-MECHANISM (mint) + CWO-T4-P0-TUSTATS-PERAGENT (second fold)

**what-considered:** (a) accept the relay and arm the crons as asked; (b) reject as out of scope since arming crons is a router action; (c) re-verify every claim, mint the row with corrected premises, and hand the un-doable part back explicitly.

**why-decision:** Took (c). Carry-over rule (1) from 2026-08-15 applies directly — a dispatch that quotes a document has made a claim, not supplied evidence. Re-verified all five load-bearing facts and every one held: `orch-sentinel-scorecard.md` reads `Last run: 2026-07-22T00:17:57Z | Mode: LITE` with OH-2.1/2.2/2.3/3.1/3.2 all literally `(pending first FULL run)` and an mtime of 22 Jul (31 days frozen); `orch-sentinel/flow/main.md:87` runs OH-4 only under `MODE=FULL`; `.claude/commands/crons/cron-orch-sentinel.md` states in its own text "Neither cron is armed yet"; zero mentions of orch-sentinel across all three re-arm `SKILL.md` files; and no existing row covers arming — `CLEAN-REGISTER-ORCH-SENTINEL-TREEMAP-SYSMAP` is tree-map/system-map registration, correctly distinguished by architect. The finding is real and it is the "built, tested, PO-approved, never wired" pattern.

**why-change:** Minted at P2 with `depends_on: CWO-T4-P0-TUSTATS-PERAGENT`, and applied three corrections rather than accepting the framing as given:

**(1) Arming first would have made things worse, not better.** `dim-oh4-capability-utilization.md:33` reads the same `tool-usage-stats.json` and treats "absent from `.toolCounts`" as "call count 0", then increments a persistent `oh4_3_zero_streak` counter per tool across runs. That is code-janitor's exact misreading, encoded in a weekly cron with memory. Arming OH-4 against the un-fixed rolling-window file would have **industrialised the false positive** and accumulated multi-week "dead tool" streaks against tools that are merely idle in the sampled window. The sequencing gate is the most valuable part of this row; arming was the ask, and arming alone was the trap.

**(2) The proposed actuator does not exist.** The dispatch said to arm "via crons:cron-orch-sentinel skill". There is no `.claude/skills/cron-orch-sentinel/` — only three cron skills exist. What exists is a *command* file that explicitly disclaims arming ("agent-father does NOT arm/register any CronCreate itself"). Recorded the two real paths instead: a router `CronCreate`, or adding orch-sentinel to `cron-standalone-team` (its natural home — same standalone-periodic-auditor class as that skill's existing four). AC-6 hands the CronCreate back to the router explicitly, because a subagent cannot perform it and narrating it as done would be the fabrication class this fleet already tracks.

**(3) The causal claim was wrong and I did not let it stand.** The relay framed my 13-tool finding as "partly a monitoring gap rather than genuinely dead". It is not partly anything — 13/13 false positives, zero deletion candidates. An unarmed OH-4 cannot explain janitor's error, because OH-4 consumes the *same* defective file under the *same* "absent means zero" assumption. These are two independent gaps sharing one bad input. Stated plainly on the row so the softer framing is not re-inherited: fixing the input helps both; arming OH-4 alone helps neither.

Additionally flagged the brief's own F3-1 trend for correction. "43→30 of 183 tools ever called, concentration 86.5%→89.8%, trend moved the wrong direction" compares two rolling-window snapshots as if they were cumulative census counts. The 2026-08-12 revision of that same file recorded **68** unique tools — higher than both endpoints — so the "decay" is window-width variance, not decay. Noted on the row so OH-4's trend logic does not inherit the framing when it is finally armed.

**Genuinely additive from the brief, folded onto CWO-T4:** F3-4 supplies a **second, independent undercount mechanism** I had not found — tools populated server-side in-process never traverse the MCP transport and are invisible to this telemetry by construction (`post_agent_signal` 6 writes vs `get_agent_signals` 684 reads, because `freshnessSlaMonitorJob.ts` writes that table in-process). So a zero count now has three possible meanings — idle this window, server-internal writer, or genuinely unused — and the file lets a reader distinguish none of them. That strengthens the same conclusion from a different direction and materially raises the value of the CWO-T4 fix.
