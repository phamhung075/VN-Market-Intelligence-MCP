# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · agent-father (continuation 4)

**Sprint goal:** COWORK-GUARANTEED-SLOT-CATCHUP
**Agent:** agent-father
**Started:** 2026-08-23T16:10:00Z
**Rolled from:** `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-agent-father-3.md` (byte cap 36000B breached at 37804B; see its CAP-REACHED marker)
**STEP numbering continues across the chain** — next id is `agent-father-S64`, not S1.

---

### STEP agent-father-S64 · agent-father · 2026-08-25T03:05:00Z
**task-id:** FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-MAINFLOW
**what-done:** Wired § Incident-Lane Consumer (ILC) — Head-Decoupled Invocation into dev-team's main.md at the Session-Gate→Step-1 anchor (FIRST of ILC→SECONDARY-Drain→QA-Drain→Step1), calling the already-shipped `.jq` claim script verbatim per architect brief §4d; committed 94716bfe6, flipped REVIEW/next_agent=qa, `.head` idle-reset (commit c677e3ac9).
**what-considered:**
- Copy SECONDARY-Drain's readback (`.task_board.review[]`-only) verbatim — REJECTED: dispatcher flagged it live-caught this same tick missing a `done[]`-origin claim (its own script's candidate set is `review[] UNION done[]`).
- Copy SECONDARY-Drain's hardcoded spawn-text premise ("status=REVIEW, branch:null") — REJECTED: false for a non-review-origin pick.
- Generic all-lane readback + row-derived spawn text (own row's status/lane/claimed_by, never hardcoded) — CHOSEN.
**why-decision:** Both neighbour-section defects were reproduced live by the dispatching session minutes before this task started; copying them into a brand-new section would ship the identical silent-lane-miss/false-premise class a 3rd time. Brief's own bash sample (`.task_board.in_progress[]`-only) is not actually in conflict — the shipped script's SOLE destination lane is `in_progress[]` (verified by reading it), so the generic scan and the brief's narrower query return an identical result set.
**why-change:** No change from brief's design — SECONDARY-Drain's own two defects were NOT retrofitted (out of this row's scope), flagged in RETURN for a follow-up row instead.

### STEP agent-father-S65 · agent-father · 2026-08-25T18:55:00Z
**task-id:** FIX-DEVTEAM-RESUME-KEY-TTL-3600-LAPSES-UNDER-LIVE-AGENT-REOPENING-DOUBLE-SPAWN-WINDOW
**what-done:** Router hand-dispatch (off DRS-allowlist). Added a keepalive renewal to S2 dispatcher-wrap's AND ILC's own `claimed:false` (peer-held) branch — `task_heartbeat(resume_key, owner_client_session=current_holder.owner_client_session, ttl_seconds=3600 unchanged)` — committed `faf84a6f6`. Did NOT flip the board row (orch-state.json is outside agent-father's commit_zone.allowed per FU-AGENT-FATHER-ORCH-SCOPE); reported BOARD recommendation via RETURN for router/PO to land.
**what-considered:**
- Raise `ttl_seconds` — REJECTED outright, AC-4's own explicit trap (moves the cliff, doesn't remove it).
- Have S2 heartbeat using `$CLAUDE_CODE_SESSION_ID` of the renewing tick — REJECTED: each cron fire is a brand-new session (`task-lock-protocol.md` § Session-Singleton Subclass confirms cross-tick session-id churn); Rung A match would fail, `hb.ok=false`.
- Sweep ALL of ILC's own previously-claimed batch rows (INCIDENT_CAP=2, only the "top" is `.head`-narrated) — CONSIDERED then REJECTED: traced that a non-`.head`-narrated row is never re-evaluated for dispatch by ANY code path (not S2, not any claim script — those only select from `ready[]`), so its lock's eventual expiry is inert, not a duplicate-spawn vector; would have been unjustified scope growth.
- Mirror the SAME renewal at SLS/RLC/DRS/QA-Drain (identical code shape) — REJECTED: out of this row's own named `files`/AC-3 scope (S2 + ILC only); flagged as an identical, unfixed residual in-file, same "cited not fixed" convention as prior entries.
- Renew using `current_holder.owner_client_session` read verbatim off the SAME failed-claim response, at BOTH S2's and ILC's own peer-held branch — CHOSEN: only tool-contract-compliant cross-tick renewal path (Rung A requires exact `owner_client_session` match; this supplies the row's own recorded value, never forged), satisfies AC-3's "both call sites, independently."
**why-decision:** Once the peer-held branch renews on every tick that observes it, `resume_key` cannot organically expire while `.head`/task_status still shows the row genuinely in flight and dev-team keeps ticking (AC-1) — WF-3/WF-4 (byte-unchanged) remain the sole authority for a genuinely-abandoned pin, so a cron that truly stops ticking still recovers exactly as before (AC-2). AC-2's "proven not asserted" bar is met by control-flow inspection (this file's own established verification standard for the Idle-Tick Rotation Selection's single-writer guarantee), NOT by an empirical 60+min live run — infeasible to execute as agent-father with no gateway-bound live specialist to observe for an hour.
**why-change:** No architect brief exists for this row (PO ruling ac-authored directly, `triage-20260825T1815Z`) — design derived directly from reading `coordinationStore.ts`/`taskHeartbeatTool.ts`/`taskClaimTool.ts` source (Rung A match semantics, `current_holder` shape) rather than following a pre-set spec, flagged in RETURN as UNVERIFIED-empirically/VERIFIED-by-construction.

### STEP agent-father-S66 · agent-father · 2026-08-25T19:55:00Z
**task-id:** FIX-SIGNAL-TYPE-ROUTING-GAP-notebook-undroppable-remainder-over-cap-breach
**what-done:** Added ONE Pipeline-A routing row for `notebook_undroppable_remainder_over_cap_breach` to `docs/agents/po/flow/triage-signals.md` (28 -> 29 parsed types), carrying PO's already-adjudicated disposition (dedup on `payload.file` -> FOLD, else CHORE owner=claude-manager-helper zone=docs/agent-memory/notebooks/) plus the semantic clause that separates it from its three siblings.
**what-considered:**
- Fix the EMIT side (constrain the producer / closed `z.enum()` on `SignalRowSchema.type`) — REJECTED, and this is the trap the row's own class has re-tripped 4x: the 2026-08-23 architect brief measured direction (a) and rejected it because `orch-apply.sh` Stage 1 is whole-document all-or-nothing, so an enum converts one misrouted signal into a fleet-wide rejected write. Emit side stays an OPEN namespace BY RATIFIED DESIGN; only the CONSUME-side routing table is in scope here.
- Also fix the 5 sibling unrouted Pipeline-A types measured live this run (`audit_finding`, `data_stale`, `detector_defect`, `signal_feedback`, `tooling_defect`) — REJECTED: each has its own auto-filed row, and PO's promotion rationale explicitly scopes this dispatch to ONE table row; the structural answer is owned by FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE (architect).
- Hardcode today's fold target `CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` into the table cell — REJECTED: a task id in a durable flow doc goes stale the day that row closes; the `payload.file` dedup rule already resolves it and is self-maintaining.
- Run `guard-signal-type-coverage.sh --check` against LIVE `orch-state.json` to satisfy the AC verbatim — REJECTED after reading the script: `--check` is NOT a dry-run, it mints backlog rows via `orch-apply.sh`. Proven, not assumed: the full-live-state run against a COPY minted 4 NEW rows (audit_finding, data_stale, detector_defect, tooling_defect). Ran fixture-only instead; live SHA `d603921f...` verified byte-identical before and after every invocation.
**why-decision:** The guard derives its allowed set by PARSING this exact table, so one row is both necessary and sufficient — verified by a paired control on the REAL script with only the doc differing: pre-fix doc (HEAD) -> `FAIL ... ["notebook_undroppable_remainder_over_cap_breach"]` exit 1; post-fix working-tree doc -> `PASS — Pipeline A: 1 live type(s) routed (29 known)` exit 0, same fixture both times, neither run mutated the fixture (dedup hit) or the live file.
**why-change:** The AC as written is currently VACUOUS through no fault of this row — PO's 18:15Z tick folded and CLEARED every `notebook_undroppable_remainder_over_cap_breach` envelope, so the type is no longer live in `pending_triage_inbox[]` and the guard cannot name it either way. The AC's parenthetical ("a matching table row exists in the Pipeline-A section") is therefore the substantive test, and the fixture pair is what makes it a proof rather than an inspection.

### STEP agent-father-S67 · agent-father · 2026-08-25T22:19:17Z
**task-id:** FIX-DEVTEAM-RESUME-KEY-TTL-3600-LAPSES-UNDER-LIVE-AGENT-REOPENING-DOUBLE-SPAWN-WINDOW
**what-done:** PO ruled AC-2 FALSIFIED on faf84a6f6 (renewal froze `.head.resume_attempts`, disabling WF-3, stranding a dead-but-once-committed specialist forever). Added the increment to S2's renew branch only, sourced outside the renewed lock; corrected WF-4's now-false "safety net" comment (AC-8); left ILC's own peer-held branch unbounded with inline reasoning. Committed `ecb825731`.
**what-considered:**
- Revert faf84a6f6 — REJECTED, PO's ruling forbids it explicitly (the duplicate-spawn window it closed was real and confirmed live).
- Mirror the same increment inside ILC's own inner loop too — REJECTED after tracing: that branch can never re-fire for the same row (its claim script only ever selects from `ready[]`, and a claimed row leaves it permanently), so there is no repeating loop there to bound, and bumping `.head.resume_attempts` there would be WRONG for a non-head-narrated batch row (tracks a different task).
- Source liveness from something outside the lock entirely (option c) — REJECTED as unneeded scope growth; option (a), PO's own "cheapest, most surgical" pick, already satisfies AC-6.
**why-decision:** `bare_task_id == head.active_task_id` by construction, so bounding at S2 alone transparently also covers any ILC-originated head-pin from its 2nd tick onward — no ILC-side change needed.
**why-change:** Executed proof this pass, not control-flow-only: the exact new jq transform run 3x against a scratch fixture advances `resume_attempts` 0->3 with WF-3's own verbatim threshold check firing exactly at tick 3; `git log --grep` run against this repo's REAL history confirms WF-4's grep genuinely finds a `Task:`-trailer commit (faf84a6f6 itself) and takes the conservative branch — both AC-2/AC-7 halves demonstrated, not asserted.

### STEP agent-father-S68 · agent-father · 2026-08-26T00:54:41Z
**task-id:** FIX-PO-TRIAGE-INBOX-CLEAR-ECHO-PIPE-MANGLES-JSON-UNDER-ZSH-SILENT-NOOP
**what-done:** Replaced both `echo "$var" | jq` calls (lines 53-54) in docs/agents/po/flow/triage-signals.md's durable-inbox CLEAR block with `printf '%s' "$var" | jq` — zsh's builtin echo interprets `\n` escapes by default (bash's does not), corrupting any envelope payload containing a literal `\n` before jq ever parses it. Swept the whole file for the same idiom — no other `echo "$var" |` occurrences found.
**what-considered:**
- Wrap with `set +o BSD_ECHO` / force POSIX-mode echo — REJECTED: fragile, invocation-shell-dependent, and the doc's own repro already proves `printf` is portable across bash+zsh with zero flag dependence.
- Switch the whole block to `<<<"$var"` here-string — REJECTED: functionally equivalent to printf here but a bigger diff for no added safety; printf is the minimal, already-idiomatic fix and matches PO's own root-cause spec verbatim.
**why-decision:** `printf '%s' "$var"` never interprets backslash escapes in ANY POSIX shell (bash or zsh), by construction — it treats `$var` as a literal data argument, not a format string with escape expansion. Confirmed live: pre-fix `zsh -c 'x={"a":"line1\nline2"}; echo "$x" | jq -c .'` reproduces the exact production failure (`jq: parse error: Invalid string: control characters...`); post-fix `printf` variant returns `{"a":"line1\nline2"}` exit 0. Also dry-ran the full two-line block against a realistic 2-envelope payload (one with an embedded `\n`) end-to-end — both `consumed_ids` and `consumed_ids_csv` came out correct.
**why-change:** Row title correction carried forward per PO's own status_note (not re-derived): this is a FAIL-LOUD ABORT, not a SILENT-NOOP — orch-apply refuses the empty candidate on control-character parse failure and leaves the live file untouched, so no partial drain occurs. Did NOT rename the row id (head-pinned, `orchStateSchema.ts` superRefine enforces head->row referential integrity) — correction recorded in the row's own status_note instead. Did NOT attempt the co-cited FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED in this same hop despite it being sanctioned: that row's status_note spans 10+ historical triage ticks naming a dozen+ distinct unrouted-type gaps accumulated since 2026-08-06, most already superseded by unrelated commits since — fully verifying and terminal-shaping it here would risk a false-DONE on a row whose true remaining scope I have not independently re-measured; left untouched in backlog[] for a dedicated pass.

### STEP agent-father-S69 · agent-father · 2026-08-26T03:10:00Z
**task-id:** FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED
**what-done:** Re-measured the row's own live AC scope (not just the flow-defect status_note) — 5 unrouted types across both pipelines (audit_finding, detector_defect, preserved_bug_no_tracking_row, tooling_defect on Pipeline B; flow-defect/flow_defect on Pipeline A). Added table rows to triage-signals.md for all 5, plus a Pipeline-A bridge row for the 4 Pipeline-B types after a router-supplied live detector_defect(to=architect) envelope proved the bridge gap real, plus an AC-3 fix for the any-unknown-type fallback's circular to==po self-loop.
**what-considered:**
- Fix only flow-defect (the router's narrower "PROBLEM" framing) — REJECTED after reading the row's own AC-1 which explicitly names all 4 original types as unfixed; a flow-defect-only patch would leave the row's real scope open under a false REVIEW.
- Leave the 2 stale self-filed `signal-type-registry-gap:*` rows (flow_defect, preserved_bug_no_tracking_row) untouched — REJECTED, they are now directly duplicative of this fix and would confuse the next reader; closed both DONE in the same write.
**why-decision:** Verified BEFORE/AFTER against a scratchpad copy of orch-state.json (guard mints on invocation, never run against live) — BEFORE reproduces both the live detector_defect Pipeline-A miss and a synthetic flow-defect/flow_defect pair; AFTER is a clean PASS on the real edited doc. This also falsifies PO's "guard may scan only one plane" hypothesis directly — the guard's own historical self-filed gap rows (flow_defect, auditor_cycle_missing, signal_feedback) prove Pipeline-A scanning already works; the real gap is the disjoint per-pipeline rule design plus CI only ever seeing a post-drain committed snapshot.
**why-change:** drain-signals.md §0a-3 (dev-team's own routing-annotation table, flagged mid-task by the router) is explicitly OUT of this row's scope — that doc's own text calls its routing column "informational only," so a missing row there does not drop or misroute a signal, unlike triage-signals.md's tables. Filed the CI-blind/broad-bridge finding as a separate row (FIX-GUARD-SIGNAL-TYPE-COVERAGE-PIPELINEA-CI-BLIND-AND-BROAD-BRIDGE-GAP, owner=developer) rather than expanding this row further, since scripts/.github are outside agent-father's commit_zone.

### STEP agent-father-S70 · agent-father · 2026-08-26T20:04:00Z
**task-id:** FIX-BEHAVIORAL-VERIFICATION-GATE-DEPLOY-AWARE-ORDERING
**what-done:** Built 6 of §9's 7 files in-zone (po/flow/main.md, ba/flow/main.md, qa/flow/main.md, ops/flow/docker.md, orch-sentinel dim-oh2/main/emit-scorecard.md, 3 commits: ee158a9ea mint-side, 900d640ad enforcement-side, 0bf6a073c handoff signal) — the qa CHANGES_REQUESTED conditional and ops's Behavioral-Predicate Probe loop are now live prose+jq, not narration. Corrected §9's file target for `oh2_4_owner_fail_streak`: `docs/agent-memory/modules/tool-usage-stats.json` has no OH-STATE key (holds unrelated per-tool call counts) — the real writer is `emit-scorecard.md` Step 1.4, wrote the counter there instead.
**what-considered:**
- Editing `orchStateSchema.ts` (§5c) myself since it's a 1-condition addition — REJECTED, router's zone boundary is explicit and apps/ is outside `commit_zone.allowed`; filed a precise patch-spec signal instead (docs/signals/2026-08-26-fix-behavioral-verification-gate-schema-handoff-dev-mcp-server.json), including a discovered ambiguity the brief never resolved (live `priority` field is a mixed P0-P3/high-low convention, 82+1 'high'/'HIGH' rows would silently fall outside a bare `=== 'P0'` check).
- A single combined commit for all 7 files — REJECTED, the router's ordering hazard requires mint-side to land strictly before/with enforcement, never after; needed the mint-side commit's own landing timestamp as the literal `BEHAVIOR_PREDICATE_CUTOFF`, which only exists after that commit lands. Split into 2 ordered commits + 1 handoff commit.
**why-decision:** Captured `date -u` (19:57:54Z) immediately before staging the mint-side commit, then read back its actual landing time (19:58:30Z via `git show -s --format=%cI HEAD`) — cutoff is 36s conservative, confirming no P0/P1 apps/ row could have been minted citing the field before the field's own authoring capability existed. `git show --name-only HEAD` on all 3 commits confirms exactly the intended files landed, nothing dragged in from the live peer (dev-team, preflight) writing the same tree.
**why-change:** Did NOT touch `docs/standards/task-schema.md` despite it being the nominal "canonical shape" reference every po sub-flow cites — it's outside `commit_zone.allowed` (docs/standards/ not listed) and §9 never named it either; noted as a pre-existing gap (doesn't document `verification` at all) but not fixed here, not this row's scope.

### STEP agent-father-S71 · agent-father · 2026-08-28T23:30:00Z
**task-id:** FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS
**what-done:** Added 21 missing routing rows across triage-signals.md + longtail (Pipeline-A: `recurring-bug`, `sprint_registry_dangling_ids`, `system_issue` alias, 5 audit/detector bridge rows, `auditor_probe_anomaly`, `agent-output-contract-violation`, cowork_defect_*/tick_*/telemetry; Pipeline-B: `system_issue` alias + 5 low-traffic rows in longtail). Flipped the P0 row to review[].next_agent=qa and CANCELLED+archived 14 member routing-gap rows in one orch-apply.sh write.
**what-considered:**
- Change dev-team's main.md:162 recurring-bug template instead of adding the row — REJECTED: the row's own scope is emitter-literal table rows; the template is valid-by-routing once the row exists (b4e5ec27's "or" branch).
- Fix the system_issue emitter (tasksMdJanitorSignalWriter.ts) to the hyphen form — REJECTED: apps/ is outside agent-father's commit_zone and the underscore is a live literal (112 fires); alias row matches the flow-defect/flow_defect precedent (AC-1 option a).
- Verify by running the guard — REJECTED: it mints live; verified by hand-replaying its two read-only extractors + one guarded fixture run (ORCH_APPLY_LIVE_FILE_OVERRIDE→/tmp).
**why-decision:** BEFORE (HEAD docs + synthetic fixture) reproduces UNROUTED_A=15/UNROUTED_B=6; AFTER is clean on both, and the real guard script exits 0 PASS against the disposable fixture, exit 1 on HEAD docs — honest green, not input-deletion green.
**why-change:** Envelope 31b7f837's hold-back (SYSTEMISSUE member AC-3) is stale — PO's 2026-08-26 secondary-drain cleared the inbox to zero before this hop; the fix stands on the table rows alone, and the empty live inbox (vacuous-green trap) is covered by the synthetic-fixture verification.
