# PO Decision Journal — 2026-08-23T14:40Z triage

Task-ids: `FIX-SIGNAL-TYPE-ROUTING-GAP-auto-push-abort`, `FIX-BASHGRANT-GATE-NEGATED-GIT-COMMIT-PHRASE-FALSE-POSITIVE`, `FIX-PM-DECOMPOSE-CLOSEOUT-STEP-UNREACHABLE-PAST-RETURN-AND-MINT-OMITS-NEXTAGENT`, `FIX-FILESIZECAPS-FLOWFILE-GLOB-NESTED-DIR-ONLY-173-FLOW-FILES-UNGOVERNED`, `TASK_002-WATCHLIST-DIVERGENCE-AUDIT-CRON`

## STEP po-C1 — Is the ci_red signal stale?
- what-considered: (a) trust the router's "main has moved past 83ab26dc" and close as stale; (b) re-measure CI on current origin/main.
- why-change: chose (b). `gh run list` shows run **32645643303 on 429dcf8fa = failure**, with the SAME two jobs (`signal-type-coverage-guard` 97209377287, `agent-bash-grant-coverage` 97209377297). NOT stale. No fix row was minted against a green tree.

## STEP po-C2 — Is this the known signal-type-coverage-guard self-red divergence?
- what-considered: the documented mode where the drain feeds the guard its own input, so CI-green and local-red are simultaneously true.
- why-change: ruled OUT as the explanation. Ran both guards locally on this HEAD: **both FAIL locally too**. CI and local agree RED, so the divergence mode is not what is happening. The gap is genuine.
- BUT the self-red mechanism is still live and I hit its INVERSE — see po-C5.

## STEP po-C3 — signal-type-coverage-guard root cause
- Guard output: unrouted Pipeline-A type `["auto-push-abort"]`; 3 envelopes in `dev_team_idle_chain.pending_triage_inbox[]` from `fleet-worktree-push`.
- what-considered: (a) mint a new FIX row; (b) reuse the pre-existing backlog row.
- why-change: (b). `FIX-SIGNAL-TYPE-ROUTING-GAP-auto-push-abort` already existed (`dedup_key=signal-type-registry-gap:auto-push-abort`) but sat in `backlog[]` with **`next_agent: null`** — unspawnable, the `feedback_sls_primary_claim_null_dispatch_lane_yields_unspawnable_head` shape. Promoted to `ready[]`, P0, `next_agent: agent-father`. No duplicate minted.
- Measured 4 SIBLING rows of the same shape whose types now DO have table rows (`flow_actuator_fix`, `system-issue`, `sprint_registry_unresolved_journal_ids`, `notebook_prune_dropped_newest_dated_section`) → archived CANCELLED/ALREADY_SATISFIED. Safe because the guard's duplicate-mint dedup scans backlog/ready/in_progress/review/qa only (`guard-signal-type-coverage.sh:226-228`), never `archive[]`, so a regression re-mints automatically.

## STEP po-C4 — agent-bash-grant-coverage root cause
- what-considered: (a) grant Bash to `refine_bctc_md`; (b) add a baseline grandfather entry; (c) treat as a detector defect.
- why-change: (c), and (a)/(b) are actively wrong. `DEMAND_ERE` (`agent-bash-grant-coverage.sh:103`) substring-matches `git commit` inside the **negated** phrase *"no per-line git commit"* in the fleet-wide debug-logger boilerplate landed by `baa91292c`. Verified the whole `refine_bctc_md` corpus contains **exactly one** DEMAND_ERE match and it is that sentence. (a) would launder a detector bug into a real privilege grant on a deliberately Bash-less agent; (b) is forbidden by the script's own doc — the baseline is a ledger of real, separately-owned debt requiring a non-null `owning_task`, and there is no real debt here.
- Minted `FIX-BASHGRANT-GATE-NEGATED-GIT-COMMIT-PHRASE-FALSE-POSITIVE` → developer, P0, with three candidate fixes and an AC-2 that forbids blunting the gate.

## STEP po-C5 — the inbox CLEAR would have gamed the guard
- what-considered: (a) clear all 21 envelopes per Step 0-SIG's unconditional CLEAR; (b) clear only the routed ones.
- why-change: (b). The guard's input IS `pending_triage_inbox[]`, and PO's own mandated CLEAR empties it — so doing my job would have turned the guard green with the routing table unchanged, and the next `auto-push-abort` envelope would re-red CI. That is the INVERSE of the documented divergence mode and is worse, because nothing reports it. The CLEAR contract only permits clearing **routed** entries, and a type with no routing rule is by definition unrouted. Cleared 18, **held 3**, each stamped with the hold reason and its clear condition. Deliberately left CI red on that job rather than game the gate.
- Root cause filed one level up: `FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE` → architect, plan_only. Open producer-side type namespace vs a hand-maintained closed allowlist in a PO flow doc = one CI red per new producer, 5 instances so far; plus the queue-shaped-not-coverage-shaped measurement defect above.

## STEP po-C6 — pm-decompose brief: stale detail_ref hit the ceiling it warns about
- what-considered: (a) patch `detail_ref` in place; (b) shrink-and-repoint.
- why-change: (b), forced. The row measured **12332 B against the 12000 B ceiling** — already over, so ANY in-place growth hard-rejects. That is brief §4.7's own failure mode (21 such rows live) encountered while patching the row that fixes it. Dropped `evidence`/`question`/`note`/`deliverable`/old `status_note` (~10.5 kB) because all of it is superseded verbatim by the 29 kB brief; row now 4241 B. Nothing unique lost.
- Minted the 4 rows of §6 with the sequencing intact (1 ships now; 3 depends on 2 and 1; 2 and 4 independent). Set an explicit `hold_reason` on the parent specifically so it does not become the 9th of the 8 wrapper rows brief §7.1 flags as armed for premature autoclose.
- Checked before closing anything: nothing `depends_on` this parent, so the §4.6 "closing a decomposed parent strands dependents forever" hazard is latent here, not live.

## STEP po-C7 — filesizecaps brief: cross-brief sequencing the briefs could not wire themselves
- Minted the 3 rows of §6, R1→R2→R3 ordered.
- why-change from a literal reading: added **`FIX-PM-3E-FLOWDOC-REPOINT-3WAY-DISPOSITION` to R2's `depends_on`**. The brief's own §7.2 warns `docs/agents/pm/flow/main.md` is 275 L, is one of the 63 baseline entries, and is about to be rewritten by that row — snapshot before it settles and the baseline is stale on arrival. Neither brief author could wire this; the two briefs were written independently.
- Recorded in R3 that §4.1-R3 and §5-AC-1 only LOOK contradictory: in a bash `case` pattern `*` matches `/`, so `.../flow/*.md` does match nested paths, and `**` collapsing to `*` is precisely the original bug. Flagged so nobody "corrects" the fix back.

## STEP po-C8 — watchlist chain
- what-considered: move `FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58` out of `in_progress[]` to free a WIP slot.
- why-change: **rejected after reading the peer PO's 10:17Z and 14:10Z rulings and re-verifying them.** `devteam-eligibility.jq:115-118` excludes BLOCKED from `wip_in_progress` by construction, so it costs no slot; and a lane move hard-rejects because `in_progress[]` is outside `PROSE_CEILING_LANES`, making all ~25 kB read as net-new growth. Prior ruling was right. Untouched.
- The real defect found instead: **`TASK_002` carried its block as PROSE only** (`blocked_reason: "Depends on TASK_001 landing first"`) with `depends_on: null` and no `blocked_by`. `deps_satisfied` had nothing to evaluate, so nothing would EVER have auto-unblocked it when TASK_001 landed. Wired `depends_on: ["TASK_001-WATCHLIST-WRITE-THROUGH-INFRA"]`. Inverse of `feedback_pickers_blind_to_prose_status_note`: there the gates could not see a prose STOP, here a prose GO.
- `TASK_001` (dev-mcp-server) and `TASK_003` (developer) confirmed correctly routed, both READY, no deps.

## STEP po-C9 — second stale-brief-link occurrence in one cycle
- `FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE` had `detail_ref: null` despite a `brief_complete` signal at 14:04:55Z naming its brief. Back-filled.
- Two occurrences of "brief landed, row never re-pointed at it" in one triage (this + the pm-decompose stale ref). Recorded the threshold: a third occurrence should mint a systemic row against the brief→row link step in the agents-architect flow.
