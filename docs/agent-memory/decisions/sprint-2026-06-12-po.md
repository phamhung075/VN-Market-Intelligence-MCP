# Decision Journal — Sprint 2026-06-12 · po

**Sprint goal:** dev-team triage (cron tick 192519Z) — classify drained signals + new ci_red into BATCH
**Agent:** po
**Started:** 2026-06-12T19:29:28Z

---

## STEP po-S1 [task_id: CI-RED-8081e584-FIX] — open CI-RED FIX (reconciled new-regression)
- what-considered: (a) skip as known CLUSTER-A 1338 debt; (b) skip as ~170 vitest debt; (c) open new FIX.
- why-change: RAW-pulled run 27369577099 log — 1338 NOT in failed list (CLUSTER-A resolved, CI-RED-RECONCILE DONE); failed files = 1293a-signal-type-safety / 1295a-signal-builders / VPT-1-vps-proxy-health (mcp-server bun-test, NOT frontend vitest). Genuine regression adjacent to post_agent_signal validator + EVIDENCE-ACCUM signal-builder work. Dedup: no open task on check_id or head_sha. → path (c). zone=apps/mcp-server/. WIP-preempt note for router (preempts FU-SCHEMA-DRIFT-P8 REWORK; CI RED blocks merges).

## STEP po-S2 [task_id: CI-RED-8081e584-FIX] — TNB c93 ACK + orphan-lock ruling
- what-considered: only path — ACK c93, decide CLEAN on orphan lock.
- why-change: no change from plan. TNB c93 HIGH findings all covered by active sprints / addressed / out-of-dev-zone → no new tasks. Orphan lock esc-datacov:FPT:Q1-2026:ESC-3: `.locks` now EMPTY (self-expired since 06-11) → LET-EXPIRE, no CLEAN task (signal already READ).

## STEP po-S3 [task_id: BCTC-CTG-FLEET-SERVE-SPIKE] — open SPIKE→architect for recurring CTG/fleet BCTC serve gap
- what-considered: (a) reopen FIX-CTG-3 (point re-fetch); (b) un-supersede FU-CTG-REFINE-PICKUP as a FIX; (c) skip — board says DONE+SUPERSEDED; (d) SPIKE to architect.
- why-decision: RAW-verified `get_bctc_full(CTG)` STILL "Chưa có dữ liệu BCTC" + `get_recent_fixes(15)` has NO bug#2776 — cycle-14 escalation (signal 2787) is accurate, board over-claims. 4 point-fixes shipped (FIX-CTG-1/2/3 + REFINE-PICKUP) across multiple commits, board=DONE, live=empty 7+ days → `feedback_recurring_bug_escalation` (2+ commits same module → block + architect) + `feedback_remediation_overclaims_derived_layer`. FU-CTG-REFINE-PICKUP SUPERSEDED rationale ("serve honest corrupt-skip; re-extract=fleet cron scope") disproven by live 14-cycle blindness on a top-5 bank. Another point-fix would be closed DONE while live stays broken. → path (d): architect SPIKE on why refine→serve never materializes CTG (row 69fa303f extracted but PUB-1/PUB-3 withholds), scoped to fleet class (CTG/VCB/D2D + 28 tickers signal 2776), output findings doc not a fix.
- why-change: dispatcher said "all covered" for CTG (BCTC-FETCH-CORRECTNESS active) — but that sprint's CTG subtasks are all DONE/SUPERSEDED with no open work; live proof shows the closure was premature. Reopening as SPIKE, not duplicating a closed FIX.

## STEP po-S4 [task_id: FU-SCHEMA-DRIFT-P8-IMPL] — do NOT dispatch REWORK (PARK upheld)
- what-considered: (a) dispatch P8-IMPL [REWORK] now that CI-RED preempt lifted (per dispatcher framing); (b) leave PARKED.
- why-decision: task's own note = DDL-DEFAULT hypothesis empirically DISPROVEN (4th consecutive P5/P6/P7/P8 fail, +2 WORSE vs 629 baseline); cluster PARKED best-effort-exhausted per prior PO path-B; 629 = documented schema-drift FLOOR; "No 7th touch opened"; superseded by FU-CI-PROFILE-629. Dispatching = re-opening a disproven path, violates recurring-bug-escalation. → path (b) PARK. Dispatcher's "dispatchable" framing is mechanically true (REWORK state) but contradicts the task content's own no-7th-touch ruling.
- why-change: corrected my own carry-over which only said "CI-RED preempts P8" — the deeper truth is P8 is PARKED regardless of CI-RED.

## STEP po-S5 [task_id: RLI-FORENSICS-CLEANUP] — DEFER (date-gated 2026-06-14)
- what-considered: only path — defer.
- why-decision: task explicitly scheduled for 2026-06-14 (7-day retention) "ONLY after confirming live DB integrity_check=ok at that date." Today=06-12. Not dispatchable for 2 days. → no batch; carry-over.

## STEP po-S6 [task_id: FIX-FINALIZE-STATUS-STUCK-PARTIAL] — open 3 FIX (BCTC refine-queue deadlock) → ARCHITECT
- what-considered: (a) reopen UNBLOCK-CTG-REFINE-DRAIN (DONE); (b) fold into REFINE-CRON-ARM; (c) route straight to dev-mcp-server as one-shot patch; (d) open deduped FIX batch → architect ruling.
- why-decision: dev-team dispatcher raw-verified vs live DB + gateway probes 3 distinct root causes NOT covered by any open task. BUG1 (P0): finalize_bctc_refine(DONE) returns ok+rows_parsed:106 but refine_status stays PARTIAL → get_bctc_pending_refine permanently re-serves ACB fea19bae (27/27 windows DONE) → 34 PENDING never drain → deadlocks active P0 REFINE-CRON-ARM (tasks[24]). UNBLOCK-CTG-REFINE-DRAIN was a SQL-predicate + FAILED-retry fix (different root); reopening would re-close-while-live-broken. BUG2 (P1): extraction_confidence never recomputed at finalize → PUB-5 (<50%) permanent block; BAL-1b/1d DONE-MOOT fixed bank-path/report_scope NOT confidence-recompute → root still open. BUG3 (P2): get_bctc_pending_refine ignores ticker arg + no by-report_id fetch tool. RECURRING-ESCALATION: 18 prior touches of finalize/refine path → per feedback_recurring_bug_escalation route ARCHITECT for state-machine ruling (recompute-at-finalize vs PUB-5 coverage-aware; status-override path) before a 19th patch lands. → path (d). All 3 zone=apps/mcp-server/src/, owner=dev-mcp-server, sprint=BCTC-ANALYTICS-LAYER. Sequencing: BUG2+BUG3 depend on BUG1 (finalize must run clean first). DIG 173038f2 REJECTED_SANITY = human /api/bctc-inspect, NOT a dev task (informational).
- why-change: dispatcher proposed "create deduped FIX task(s)"; I added the architect gate on top because finalize/refine is at 18 touches — recurring-bug-escalation makes architect the chain head, not dev directly.
