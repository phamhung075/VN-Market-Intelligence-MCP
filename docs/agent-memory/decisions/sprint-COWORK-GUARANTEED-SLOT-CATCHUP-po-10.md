# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · po (continuation 10)

**Sprint goal:** no goal set
**Agent:** po
**Started:** 2026-08-15T10:09:20Z
**Rolled from:** sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-9.md (220L/37605B > 600L/36000B byte cap)

---

### STEP po-S67 · po · 2026-08-15T10:07:22Z
**task-id:** UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW
**what-done:** Adjudicated ops's D1-D5 FAIL: upheld AC-3 on D3's cap clause, REFUTED its "leak persists" root cause from ops's own series, answered AC-4 myself via in-VM dmesg, attached the six D5 fields to the LANCECORE row, closed the embedder row as premise-refuted and re-scoped FU-RAG-DEPLOY-MEMORY to capacity sizing. One `orch-apply.sh` pipe, 4 rows stamped, task_total 694→694.
**what-considered:**
- Accept ops's FAIL + its stated cause and dispatch dev-rag-service to FIX-RAG-LANCECORE as AC-5 directs, vs re-derive the numbers first
- Attach D5 evidence myself vs re-dispatch ops for a corrected measurement pass vs both
- Treat AC-4 "INCONCLUSIVE — not accessible from this host" as a real environment limit vs test it directly
- Mint a new capacity row vs re-scope FU-RAG-DEPLOY-MEMORY vs attach the cap work to the LANCECORE row
- Raise the cap (option a, already tried once and de-certified) vs bound the peak working set in code vs hold pending more data
- Let the current 13.6h window run to its 24h D1 floor before acting, vs act now
**why-decision:** Re-derived every number before ruling, and the inference did not survive it. Ops's extraction is sound — I reproduced 799 well-formed samples/mean 63.52/max 97.87 against its 781/63.02/97.87 — but "memory leak persists" is refuted by that same data: peak-ceiling regression across 14 sawtooth cycles is +0.048 pp/cycle (first peak 97.87% at 23:07Z, last 97.29% at 09:17Z, 10h apart), the trough floor is flat at 32-48% for 10.5h (+0.266 pp/cycle), and each cycle RELEASES 47-55pp (~480-565 MiB) versus the ~35pp a full model release predicts. A leak does not return pages, and a rising trough floor is the discriminator it would have to trip. The mechanism is not inferred from the curve — the container's own logs name it: "Embedding model unloaded after 9xxs idle (threshold=900s)" / "Loading embedding model ... (~400MB)". So dispatching FIX-RAG-LANCECORE, whose entire scope is "re-diagnose at the lance-core layer", would have burned a sixth cycle on a dead hypothesis — the exact five-consecutive-no-op-folds pattern this saga is documented for. Ops's volatility figure is also the wrong statistic: D3 says "fit the growth rate", ops reported adjacent-sample absolute deltas (2.368 pp/min mean, 93.120 max), which on a sawtooth measures tooth amplitude; the real fits are +0.0642/+0.0228/+0.0503 pp/min over nested sub-windows — non-monotonic, i.e. tracking sawtooth phase, not trend, so that clause is UNDECIDABLE here and is not the basis of the fail. D3's cap clause is, and it is unambiguous (338/799 samples >85%, peak ~1002 MiB of a 1073 MiB limit). AC-4 was not an environment limit: the nsenter1 query ran first try and returned ZERO oom_memcg for container 66656926d503 across the whole window — that is a runbook-reachability gap, since the method is documented only in row prose. Chose re-scope over mint because this row's own standing instruction forbids a third tracking row and FU-RAG-DEPLOY-MEMORY already owns docker-compose.yml and the original cap decision. Chose the cap raise over bounding the peak because the workload is now MEASURED bounded — sizing a ceiling against a flat-ceilinged peak is ordinary capacity engineering, unlike the de-certified 768m→1g raise which was sized against genuinely unbounded growth (redundant per-restart index rebuild + non-returning allocator, both since fixed). Did not wait for the 24h D1 floor: the cap failure is already established and elapsed time cannot cure it.
**why-change:** Departed from AC-5's own written fail-path, which names FIX-RAG-LANCECORE as the destination — I routed to FU-RAG-DEPLOY-MEMORY instead, because AC-5 was authored when a leak was still the working hypothesis. Also departed from "PO does not measure": I re-ran the statistics and read dmesg myself rather than re-dispatching ops, because the question was whether the evidence supports the certification decision I own, and a second ops pass would have re-measured the same container for another 13h before anyone noticed the inference error. Two defects recorded against myself rather than ops: (1) RAG-MEM-DURABILITY-BAR v2's D3 says "fit" but never forbids adjacent-sample deltas and never says what a bounded-oscillating workload looks like — the peak-ceiling and trough-floor drift tests belong in v3 as the actual leak discriminators; I did not amend the bar mid-incident. (2) The in-VM dmesg method exists only in row prose, which is why ops could not find it. Separately, ops's raw series lived only in /tmp and three concurrent samplers with two schemas had corrupted 796 of 2391 lines while it was being cited as the evidence chain — persisted the cleaned 799-sample series into the repo, and deliberately did NOT kill the samplers, since ending a live window is an ops call and D1 is still accruing.
