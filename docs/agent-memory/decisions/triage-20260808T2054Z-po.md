# PO Triage Decision Journal — 2026-08-08T20:54Z tick

**task_id:** FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L (primary), FIX-CI-BUNTEST-1862C-TRANSPORT-SESSION-EVICTION, FIX-PO-MANUAL-DISPATCH-SKIP-STAMP-FIELD-MISMATCH-STARVES-SWEEP, FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED
**Session:** 165f4245-6173-4054-87fd-c55bb626265f (dev-team Step 1 dispatch)

---

## D-1 — Rejected the relayed premise that 763ef6822 resolved the CI red

**What was considered:** dev-team's context said `763ef6822` "is very likely what resolves the ci_red/size-lint signal". Two paths: (a) accept, mark the signal stale/resolved, return NOTHING for CI; (b) check live.

**Why the change from plan:** checked live. `gh run list --branch main` shows the CI workflow RED on **both commits after** the fix — `763ef6822` (run 31277793877, failure) and `ebcdc1225` (run 31277932133, failure). The triage-signals BACKSTOP is explicit that the CI plane's green baseline is 0 fail and "pre-existing / already remediated" is never a valid disposition without a matched open FILE-scoped row. Accepting the relay would have closed the loudest true-positive on the board.

**Mandatory pre-dedup failing-file read (performed, not skipped):** `gh run view 31277932133 --log-failed` →
- `bun test`: `15080 pass / 40 skip / 1 fail`, `=== FAILED FILES (1) ===`, `FAILEDFILE: src/__tests__/1862c-transport-session-eviction.test.ts`
- `size-lint`: `[size-lint] FAIL — 1 offending file(s) (scanned 1370): apps/mcp-server/src/interface/mcp/transport.ts — baseline-tolerance-exceeded (baseline=126L actual=237L upper=138L)`

`coordinationStore.ts` is genuinely gone from the offender set — `763ef6822` did fix its own file. The relay was half-right and that is the dangerous shape: one of two offenders fixed reads exactly like "CI is recovering" if you never open the log.

## D-2 — Both CI failures deduped to open rows; the finding is mint-without-dispatch

**What was considered:** mint new rows per the ci_red template vs fold.

**Only path:** fold. File-scoped dedup hit on both:
- `ci_job:size-lint|file:apps/mcp-server/src/interface/mcp/transport.ts` → `FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L` (BACKLOG, minted 18:05Z)
- `ci_job:bun test|file:src/__tests__/1862c-transport-session-eviction.test.ts` → `FIX-CI-BUNTEST-1862C-TRANSPORT-SESSION-EVICTION` (BACKLOG, minted 18:51Z)

**The actual finding is not a detection gap — it is a dispatch gap.** Ten `ci_red` signals fired between 13:23Z and 20:23Z. Every one was correctly detected, correctly deduped, correctly folded. The main-branch CI plane has been RED continuously for **7h30m** because both rows were minted into `backlog[]` and never dispatched. Detection worked perfectly and bought nothing. Re-minting an 11th row would have been the churn pattern; the correct action is to **promote the existing rows into this tick's BATCH**, which is what was done.

## D-3 — Three rag-service A-30 entries are ONE chronic condition, not three incidents

**Only path:** FOLD. Cycles c380/c381/c382 report the same sustained-memory finding, and the auditor's own 7-day dedup ledger already suppressed c382. Non-terminal-lane scan found the condition owned across `FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS`, `FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD`, `RAG-SERVICE-AVAIL-01-FIX`, `OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN`. Per `feedback_auditor_mcpserver_a21_a30_memory_fp_reemit_churn`, no mint. Same for the 4 `repair_task_request` entries (all four matched an existing row by check_id) and the sweep-guard `escalated=true` (matched `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES`).

## D-4 — The manual-dispatch sweep has been dead since 16:06Z, and I could see it because the same row ranked #1 twice

**What was considered:** (a) fold the top-ranked candidate `TASK-COWORK-MUTEX-001` into BATCH as its own row type says (TASK/M/dev); (b) re-skip it; (c) ask why a row skipped 5 hours ago is still ranked #1.

**Why the change from plan:** took (c). `manual-dispatch-sweep.md` Step 1 suppresses a candidate through exactly one field — `flag_reentrant` reads `po_manual_dispatch_flagged_at`. The documented *skip* path writes `po_manual_dispatch_skipped_at` + `po_manual_dispatch_skipped_reason`. **Nothing reads those two fields.** So a correctly-reasoned skip is invisible to the predicate and the row re-ranks #1 forever, monopolising a Step 2 that selects exactly ONE row per invocation.

**Measured, not inferred:** re-ran Step 1 verbatim at 20:56Z. `TASK-COWORK-MUTEX-001` returns as `candidate[0]`, `reflag:false`, 4h50m after its 16:06:57Z skip — past the 14400s staleness window, which is the very mechanism meant to re-admit stale rows. Live field census: 1 row carries `skipped_at`, 5 carry `flagged_at`, and the 1 skipped row is the P0 that outranks all 5. **The sweep has produced zero new selections since 16:06Z.** Minted `FIX-PO-MANUAL-DISPATCH-SKIP-STAMP-FIELD-MISMATCH-STARVES-SWEEP`. Same "documented consumer, no documented producer" family as this sub-flow's own origin row, one layer down: here the producer writes a field no consumer reads.

**Did not close TASK-COWORK-MUTEX-001 and did not dispatch it as a dev task.** The 16:06Z PO was right that its deliverable looks already-shipped, and right not to close a P0 on a partial match. Folded as a **SPIKE** — the AC-level diff its own `REQUIRED NEXT ACTION` asks for — and stamped `flagged_at` so the sweep unblocks this tick rather than waiting for the fix.

## D-5 — Coverage guard is RED and the gap tripled in 48h; still refused to mint

**Only path:** fold onto `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED`, whose scope is already exactly "add the missing types to both tables AND to the guard's own `$routed` array". Live re-run returns **10** unrouted `to=po` types, up from the 2 recorded on 08-06: `auditor_convergence_dedup_gap`, `auditor_fabricated_signal_id`, `bug-escalation`, `cron_fire_gap`, `db_freshness_breach`, `false_ac_verification`, `microservice_memory_degraded`, `process_gap`, `recurring-bug-corroboration`, `vps_proxy_stale`. Raised P1→high. Noted that `microservice_memory_degraded` is an underscore/hyphen alias of the routed `microservice_degraded` — that one wants alias-normalisation, not a 10th table row, and saying so on the row saves the implementer a wrong turn.

## D-6 — Honoured the SSE recheck hold rather than acting on the process_gap

**Only path:** ACK, no action. `next_recheck_not_before=2026-08-08T23:06:00Z` on `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` (qa[]). Did not resume, did not sign off, did not re-stamp. Note the coupling worth carrying: the two CI rows dispatched this tick are **both** debt from that same SSE reaper commit (`b746c112b`) — the size regression and the eviction test. Their outcome is evidence for the 23:06Z sign-off.

## D-7 — Notebook written by section-prepend + prune, never full-overwrite

`feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries_20260806` records this defect landing on `po.md` itself at 18:08Z when two PO sub-sessions ran concurrently. The 19:26Z section's bytes were preserved verbatim; only the oldest section was pruned.
