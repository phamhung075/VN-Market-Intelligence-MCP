# Decision Journal — PO triage 2026-08-11T16:56Z

**task_id:** `po-triage-20260811T1656Z` (dev-team Step 1, `step1_triage` idle rotation)
**Session:** 165f4245-6173-4054-87fd-c55bb626265f

---

## STEP po-1 — Disposition the 93-report telegram backlog

- **what-considered:** (a) mint one row per report; (b) mint one row per *cluster*; (c) dedup-first, mint only what no open row covers.
- **why-change:** chose (c). Full-text scan of the five non-terminal lanes matched an open row for 91/93. (a) would have added ~90 rows of pure duplication to an already 774-row board; (b) would still have re-minted defects that are already tracked at P0. Report id 4674 (`RECONCILE CIRCUIT BREAKER TRIPPED`) is the pipeline's own escalation and states verbatim *"this is a systemic incident, do NOT triage these rows one-by-one"* — obeying it is the documented behaviour, not a shortcut.
- **outcome:** 87 `duplicate`, 5 `monitoring`, 1 `fixed`. `list_unresolved_reports()` re-called after the run → `[]`. Dedup ratio 91/93 = 97.8%.

## STEP po-2 — sbv-omo "unreachable": verify the premise before dispositioning

- **what-considered:** (a) trust the alert and route to ops as a service outage; (b) dedup as noise (2 fires only); (c) probe the service before deciding.
- **why-change:** chose (c) per the `system-issue` row's "verify the premise before acting" rule. All three of the alert's own suggested causes were falsified live: container Up 12 days healthy; `/health` 200 in 2.6ms; `GET /liquidity-state` → 405 (route registered, wrong method). The real mechanism only appeared on `POST` with a JSON body: **HTTP 000 after t=18.42s** against `deadlineMs = 15_000` (`sbvOmoLiquidityCronJob.ts:63`). Had I taken (a), ops would have been dispatched to restart a healthy container.
- **outcome:** minted `FIX-MACRO-LIQUIDITY-STATE-HANDLER-EXCEEDS-CRON-15S-DEADLINE` (`apps/macro-indicators/`, `dev-macro-indicators`, high). Recorded a second, independent defect on the same row: the error string conflates deadline-exceeded with unreachable and actively misdirects triage. Explicitly forbade "raise deadlineMs" as the fix.

## STEP po-3 — news-scout ENOENT: FIX vs SPIKE

- **what-considered:** (a) FIX — re-create/repair the knowledge path; (b) SPIKE — mechanism unknown; (c) skip as transient.
- **why-change:** rejected (a) outright: the file is present (6364B), `git ls-files` tracked, `git check-ignore` clean, last touched by `caba878b7`. "Repairing" a present file would manufacture a false green. Rejected (c) because the cost is a whole lost production cycle — news-scout's newest notebook entry is `c249 · 12:07:40Z`, there is no `c250`. Chose (b): I can prove the report is false but not *why*. Decisive evidence that it is agent-specific rather than an environment outage: the same 16:09:37Z batch dispatched market-watcher, which loads the identical path and completed (notebook written 16:11 UTC).
- **outcome:** minted `SPIKE-NEWSSCOUT-KLFL-FALSE-ENOENT-ON-PRESENT-TRACKED-SKILL-FILE`, timebox 120, `docs/agents/` → `agent-father` per the artifact-class routing ruling. Three hypotheses written on the row; required that "haiku agent mis-narrated a different failure" be ruled in/out first, since news-scout is the only haiku agent in that batch. Recorded that `Read` **is** granted, so this is not the `limitation=structural / check the tool grant` class.

## STEP po-4 — ci_red ×2: file-scoped dedup

- **what-considered:** (a) dedup on `check_id`/`head_sha`; (b) mint per signal; (c) mandatory failing-file read then file-scoped dedup.
- **why-change:** chose (c) as the flow requires. `failing_jobs[]` carried only `bun test`/`size-lint`, which is zero-information. `gh run view --log-failed` on both runs returned byte-identical `=== FAILED FILES (2) ===`; all three resulting files already carry exact `dedup_key` rows. (a) would have failed by construction (SHA-derived keys never match across advancing SHAs); (b) would have duplicated three open rows.
- **outcome:** 0 mints. Observed-SHA ledgers appended to all three rows. Surfaced a drift the dedup would otherwise have buried: `transport.ts` is 265L, not the 237L in the row's own title, against unchanged baseline=126L/upper=138L.

## STEP po-5 — A-30 memory: refuse the relayed CRITICAL

- **what-considered:** (a) accept the reported rag-service CRITICAL (99.53%, 5MiB free) and escalate; (b) re-verify at source.
- **why-change:** chose (b). `docker stats` shows rag-service at **76.91%** — recovered unaided — and `State.StartedAt=2026-08-11T01:00:43Z` proves no restart in ~16h, so the "RestartCount=9 over 15h" framing reads a cumulative counter as a rate. pdf-extractor is the genuine sustained case: 94.90% → 95.31% within this tick. Getting the direction right matters here specifically because the A-30 discriminator has previously misscored a crash-cliff as a reclamation dip.
- **outcome:** no new mint. Folded onto the three open A-30 rows, recording rag-service as the **positive** reclamation control and pdf-extractor as the **negative** — usable as fixtures when the row is implemented.

## STEP po-6 — routine folds

- **what-considered:** only path: dedup-check, fold, no mint.
- **why-change:** no change from plan. 4 `bug-escalation` (all `escalated=true`) → `FIX-SWEEPGUARD-…` occurrence 28→32, recording that the worst counter has gone 13→31 against an unchanged threshold of 3 with two concurrent offenders. `context_bloat_breach` → new `CLEAN-CTXBLOAT-NOTEBOOK-WRITE-SKILL-215L-OVER-200L-CAP` (breach re-measured at source: exactly 215L/13016B), routed to `claude-manager-helper` per the long-tail table. 4 `bctc_signal` + 3 `cowork-fire` + `notebook_tiebreak_direction_defaulted` (self-declared `informational_no_action_needed`) + `esc-deep-dive-request` (already ESC-DISPATCHed at drain) → logged, no mint. Guard drift (unrouted 3→4) folded onto `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED`.

## STEP po-7 — manual-dispatch sweep

- **what-considered:** only path: take the top candidate by `[priority_rank, idx]`.
- **why-change:** no change from plan. 82 eligible; top is `TE-T03` (DRS-STRANDED-OFF-ALLOWLIST, `agent-father`, stale-flagged 2026-08-07T05:04:44Z ≈ 4d, well past the 4h re-admission window). Verified its dependency `TE-T01` is `DONE_VERIFIED` in `docs/data/orch/archive/2026-07.json` before selecting — the predicate asserts this, but a stale-flagged row re-entering after 4 days deserves the check rather than the assertion.
- **outcome:** stamped and folded into BATCH.
