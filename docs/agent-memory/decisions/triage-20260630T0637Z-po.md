# PO Triage Decision — dev-team tick 2026-06-30T06:37Z

**Spawn:** dev-team Step 1 PO triage. Head IDLE (MARKET-INDICATOR-DEPTH-P0 closed; TASK-501/502 done_verified).
**Inputs:** read_telegram_reports(new)=none · list_unresolved_reports()=[] · 4 drained pendingSignals + 1 NEW signal_queue row.

## Decisions

### 1. CHEF double-publish (signal_queue cow-20260630T0515-chef-doublepublish, type=system_issue, MEDIUM) → FIX
- **RAW-verified** root via payload `docs/signals/cowork-chef-doublepublish-2026-06-30.md` + its 06:21Z update.
- **Two-gap root:** (a) `pressure-state.stale_warning=true` (DETERMINISTIC — cycle-snapshot age >20min) forces PRESSURE_MODE=legacy at Step 4.2; legacy mode sets `CADENCE_MATCHES = raw MATCHES` with NO filtering (`pressure-cadence.md` Step 4.5b) → cadence gate that would suppress chef-intraday (240min off-market vs ~62min elapsed) is bypassed. (b) No same-tick CHEF mutex; published-markers key per-slot so chef-morning vs chef-intraday never collide.
- **Why FIX A (not C):** payload 06:21Z update proves `stale_warning` is correct (reports real stale data) → suppressing it (fix C) is the WRONG lever. Robust fix = unconditional same-tick CHEF mutex in BOTH modes (legacy is entered legitimately when data is stale).
- **RECURRING class:** chef-evening double-post (po-s90 f8c12ca9 → folded ARCH-HEADLESS-GATEWAY-COWORK-NOPOST) + chef-intraday dedup-granularity (c53a7df5 → folded FU-CHEF-MARKER-INFLOW, DEFERRED, never shipped). No durable fix ever shipped → this is the consolidating root fix, NOT another per-slot patch.
- **Action:** minted `FIX-COWORK-CHEF-SAMETICK-MUTEX` → backlog[] (size S, zone cross-service/, owner+next_agent cowork-refactory-expert, dispatcher=main). Files: `pressure-cadence.md` Step 4.5b (primary) / `match-slots.md` Step 4b / `spawn-fanout.md` Step 5. Consolidates DEFERRED `FU-CHEF-MARKER-INFLOW`. Signal NEW→TRIAGED.
- **Routing (po-s109 dead-route guard):** dispatch table = "update cowork agents → cowork-refactory-expert → **main**". cowork-refactory-expert is maintenance-lane (mutex-wrapped, on-demand) — NOT dev-team-cron auto-adopt. So minted to backlog[] (durable, dispatcher=main) NOT ready[]; BATCH carries route_to=cowork-refactory-expert for the router to dispatch.

### 2. signal #4 drift note (emit_pressure_state stale_warning) — SAME root as #1 → covered by the FIX. No separate task.

### 3. context-bloat ba.md (210/200, +10) & pm.md — NOTE only, no task.
- `HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING` is DONE_VERIFIED (AC-5 BLOCKING + headless hook backstop) → both files self-cap on next notebook write. That task EXPLICITLY warns against per-agent treadmill mints. Maintenance-lane (claude-manager-helper) cron handles residue. Act only if fence red.

### 4. cowork-fire heartbeats (05:09:28Z, 05:24:39Z, errors=[]) — informational, skip.

### 5. Stale local branch `ci-red-fix-buntest` (git log main..branch EMPTY = 0 unmerged commits) → CLEAN → qa (violates "NO branches"; flow CLEAN rule). Low priority; returned in BATCH (not minted — trivial in-tick delete).

## BATCH returned to router
- FIX `FIX-COWORK-CHEF-SAMETICK-MUTEX` (size S, zone cross-service/, route_to cowork-refactory-expert, baseline_pass=true) — already on backlog[].
- CLEAN `ci-red-fix-buntest` stale-branch delete → qa (low).
