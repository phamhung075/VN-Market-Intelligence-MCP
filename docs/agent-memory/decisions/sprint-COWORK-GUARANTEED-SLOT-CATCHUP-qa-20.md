# Decision Journal — Sprint COWORK-GUARANTEED-SLOT-CATCHUP · qa

**Sprint goal:** Make cowork `guaranteed:true` an honored contract — bounded catch-up firing for elapsed guaranteed slots, or a structured (non-silent) miss.
**Agent:** qa
**Started:** 2026-08-13T20:29:36Z
**Continuation of:** sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md (CAP-REACHED, byte cap 36000 breached at 43649B/200L — dual-axis cap, line count was under 600 but byte density over)

---

### STEP qa-S113 · qa · 2026-08-13T20:27:00Z
**task-id:** FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT
**what-done:** Direct-Commit Verify (dev-team Review-Lane QA-Drain, `branch:null`). Both commits `78f945fb2`/`abd890ef1` confirmed ancestors of `main`; `git show --stat` matches every claimed file. Read `getSlaThreshold("bctc")` at source, not trusted from review_note prose — confirmed it branches ONLY on `isBctcEarningsWindowActive(now)` (boolean) → `cfg.earningsWindowThresholdMinutes`(1440)/`cfg.defaultThresholdMinutes`(10080), zero `minutesSinceX(now)` age term remains.
**what-considered:**
- AC-5: pulled `docs/data/system-map.json` `.project.data_sources["bctc-discover"].sla` myself, diffed against `DEFAULT_SLA_CONFIG`'s bctc entry — byte-match (168h/10080min default, 24h/1440min earnings-window), independent of the row's own citation.
- AC-4: grep-confirmed `marketHoursThresholdMinutes`/`offHoursThresholdMinutes` gone from `SignalSlaConfig`/`DEFAULT_SLA_CONFIG`; only surviving hit is the new test's own `toBeUndefined()` guard.
- Widened test scope beyond the claimed 8-file/107 count in 3 rings: ring1 (7 named files) 110/0; ring2 (+9 more bctc/SLA files found via grep) 140/0; ring3 (full 18-file set importing `freshnessSlaChecker`/`freshnessSlaConfig`) 271/0 — zero fixture-ripple missed. `tsc --noEmit` 0 errors, `mock-guard.sh` PASS, DDD/security greps clean.
- AC-2 (fixed-across-3-emissions, both regimes) and AC-3 (bidirectional clear+fire+recovery) read line-by-line in the new dedicated test file, not summary-trusted — both genuinely exercised.
- Did NOT re-run the full 14870-test suite (609s cost judged out of proportion — the 18-file direct-importer ring is exhaustive for this change's actual blast radius).
**why-decision:** vc-approved, DONE_VERIFIED. Zero ISSUE — commits real, root-cause fix matches claim exactly at source (not paraphrase), config values SSOT-verified independently, bidirectional proof genuinely present and passing, no fixture-ripple missed.
**why-change:** none — verdict matches dev agent's own claim. Appended `[QA] Review Record` to the row's `review_note` field (row had no `status_note`); attached `verification.raw_probe` on the retry after validator flagged its absence on the first write attempt.

### STEP qa-S114 · qa · 2026-08-13T22:35:00Z
**task-id:** FIX-COVERAGE-STAMP-TTL-30-BELOW-SCHEMA-MIN-60-FAILS-CLOSED
**what-done:** Direct-Commit Verify (dev-team Review-Lane QA-Drain, `qa[]` row, `branch:null`). Did NOT trust the row's own note (router "RAW-verified" claim) at face value — independently re-derived all 3 ACs from source.
**what-considered:**
- AC1: `git show a63ab8b0d~1:scripts/agents-flow/coverage-stamp.sh | sed -n 150,165p` — confirmed pre-fix line 158 literal was exactly `ttl_seconds:30`. Post-fix on `main`, ancestor-confirmed, same literal now `900`. `origin/main` HEAD == local `main` (524e1239d) — commit genuinely pushed, not just local.
- AC2: grepped ALL of `scripts/` (`.sh`/`.js`/`.mjs`/`.ts`/`.py`/`.jq`) for `task_claim`+`ttl_seconds` literals: every other call site is either a fixed literal ≥60 (600/900/1800/5400/120/60/180/1000/3600) or an env-overridable var with a safe ≥60 default (`auditor-notebook-commit.sh` `$MUTEX_TTL` default 90; `emit-dashboard-row.sh` literal 90). No second offender found — matches commit message's own grep claim.
- AC3 (the hard gate — refused lint/grep-only close): live `docs/data/coverage-state.json` (uncommitted working-tree write, `git status -M`, pre-existing committed baseline still 2026-07-25) shows 34-watchlist-ticker `last_covered_news_scout`=`last_covered_market_watcher`≈2026-08-13T20:12:5x/06Z — genuinely after fix commit's UTC ts (21:28:58+02:00 = 19:28:58Z). Cross-checked against BOTH agents' own notebooks, not the row's prose: news-scout c262 (12:11Z, pre-fix) explicitly logged `Coverage-state update SKIPPED — ttl_seconds=30 < schema min=60` (reproduces the bug); c264 (20:10:26Z start, post-fix) shows no skip, ends "Coverage-state update via coverage-stamp.sh (tickers in impact set: FPT,VIC,HPG,+34-stock chain)" — matches the exact tickers/timestamps now in the live file. market-watcher's 20:11Z cycle carries a structured (non-prose) metrics-table field `coverage_state_updated | yes`, `Stocks: 34` matching the 34 fresh-stamped tickers. Ran `coverage-stamp.test.sh` myself (stubbed-mcp regression, unaffected by TTL value either way) — 29/29 pass. `mock-guard.sh --files coverage-stamp.sh` PASS (bash, no prod-source scan scope).
- Noted but non-blocking: c263 (16:10Z, still pre-fix) narrates "Coverage-state update completed" with no SKIPPED caveat, yet the live file has zero trace of an Aug-13-16:xxZ write — that specific cycle's claim looks unverified/inaccurate (known `feedback_auditor_resumed_run_narrates_writes_never_executed` class), but does not affect this row's own AC3 evidence chain since c264+market-watcher (post-fix, structurally corroborated by file content) independently satisfy it.
**why-decision:** vc-approved, DONE_VERIFIED. All 3 ACs hold under my own independent re-derivation (commit diff, exhaustive grep, live file + dual-agent notebook cross-check) — not the row's own note. Genuine post-fix fleet cycle write observed, matches `verification_gate=coverage_state_write_observed_after_fix` literally.
**why-change:** none — followed verify-committed sub-flow exactly, including its explicit "do not close on lint/grep alone" instruction for AC3.

### STEP qa-S115 · qa · 2026-08-13T22:30:03Z
**task-id:** FIX-AUDITOR-VERDICT-TRANSCRIPTION-PROSE-OVERRIDES-MACHINE-VERDICT
**what-done:** Direct-Commit Verify (dev-team Review-Lane QA-Drain, `qa[]` row, `branch:null`). Refused to trust the dev's own review_note or the spawn brief's claimed test counts — re-ran both suites myself and manually replayed every acceptance fixture directly via `scripts/audit-output-contract.sh`'s standalone CLI mode, bypassing the dev's own test harness.
**what-considered:**
- Commit ancestry: `git merge-base --is-ancestor` confirmed `6f10770cb`/`a8cae55e3`/`5f1c89f3f` all on `main`; `git show --stat 6f10770cb` matches all 4 row-claimed files plus `auditor-tier1-probe.test.sh` and new `docs/data/auditor-tier1-last-trigger.json` (sole-writer, distinct from `auditor-tier1-last-healthy.json` — confirmed via `git show` diff, only a timestamp field changed on the healthy file, nothing structural).
- Independent full test re-run: `audit-output-contract.test.sh` 87/0 (exact grep -c PASS/FAIL); `auditor-tier1-probe.test.sh` 226/0. Cross-checked against parent commit via `check()`-call count diff: 56→87 (+31) and 214→226 (+12) — both match the claimed deltas exactly, not just the claimed totals.
- Manual CLI replay (own fixtures, not the dev's): OCC-1 (raw=ESCALATE/declared=FOLD) → exit 1, verdict=ESCALATE, names check=A-30. OCC-2 (trigger=FAILURE/mem_creep, declared=ALL_GREEN) → exit 1, verdict=DIVERGENCE naming mem_creep/A-30; same trigger + declared=WARN:dedup-SKIP → exit 0, verdict=CLEAN. Machine-verdict-no-declared → VIOLATION/ESCALATE (silent-omission wording present). Declared-no-machine → INFO log only, exit 0. All-flags-absent → byte-identical pre-fix STDOUT, no `verdict=` suffix, exit 0 (V1-V5 unchanged).
- `shellcheck -x -e SC1091` on both changed `.sh`: 2 warnings, diffed byte-for-byte against the parent-commit blob — confirmed pre-existing (only line-shifted), zero new warnings. `mock-guard.sh` correctly N/A (no TS/JS production files touched).
- AC-5: grep-confirmed the pre-gate script makes zero `call_tool`/MCP calls (1 hit, a comment stating this fact) — a live tick is structurally incapable of posting a signal. Ran the real live pre-gate myself against the actual fleet (rag-service not breaching today) → ALL_GREEN, trigger file correctly persisted. This mutated 3 tracked files (`auditor-tier1-last-healthy.json`/`auditor-tier1-last-trigger.json`/`auditor-output-contract-violations.json` — the last one via my own OCC-1/OCC-2/AC-4a manual replays exercising the durable-violation-record append path, additional corroboration it works) — reverted all 3 via `git checkout --` before sign-off to avoid leaving QA-manual-test trash data in the repo.
**why-decision:** vc-approved, DONE_VERIFIED. Every one of the row's 6 acceptance criteria independently reproduced from source, not from the dev's or router's self-report — this is exactly the failure class this task exists to prevent, so held to a higher skepticism bar than a normal verify-committed pass.
**why-change:** none — verdict matches dev's own claim. Lane-moved `qa[]→done_verified[]` in the same `orch-apply.sh` write as the status flip + synced `.head` idle (rule (b), branch:null flip) per CANONICAL:SSOT-STATUSFLIP-LANEMOVE; validator rejected the first write attempt (missing `verification.raw_probe`), retried with `{tool,args,live_value_observed,observed_at}` attached — same gap qa-S113 hit on a prior row, same fix.

### STEP qa-S116 · qa · 2026-08-13T23:57:00Z
**task-id:** FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE
**what-done:** Direct-Commit Verify (QA-Drain, `qa[]` row, `branch:null`, commit `5ee697852`). RAW-ran all 4 claimed suites myself + tsc + mock-guard; read `checkDecorativeSequencingFields()` at source and independently reproduced AC-3's negative control on a scratch copy (not the test file's own fixtures).
**what-considered:**
- Suites: `FIX-*.test.ts` 11/11, `orchStateSchema.test.ts` 120/120 (claim said 104 — +16 from unrelated later commit `ad6e422e9`, confirmed via git log on the test file, not a regression), `orch-backlog-stub.test.sh` 7/7, `test-orch-validate-ac.mjs` 29/29, tsc clean, mock-guard PASS.
- AC-1/AC-3: injected a fresh reverse-only `blocks` edge + a `co_edit` value on a scratch `orch-state.json`, ran `orch-validate.mjs` directly — both hard-rejected, real exit 2; confirmed `orch-apply.sh:138` aborts the write on any non-zero validator exit ("live file untouched").
- AC-2: live's 2 remaining non-empty `blocks` rows confirmed properly backed at their targets; `orch-state-validate.sh` exits 0 clean; malformed prose preserved under `migrated_blocks_prose`. AC-5: `git show --stat` on `backlog-detail.json` empty — untouched.
**why-decision:** vc-approved, DONE_VERIFIED — every AC independently reproduced from source/live data, not the `dev_close_note`'s own prose.
**why-change:** none — verdict matches dev's own claim. No gateway/MCP tool grant this session (Read/Edit/Write/Bash only) — could not `task_release`; router must release `task:FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE` on my behalf.

### STEP qa-S117 · qa · 2026-08-14T01:33:11Z
**task-id:** FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES
**what-done:** Direct-Commit Verify (QA-Drain, `qa[]` row, `branch:null`, commit `761988143`). Independently re-ran the detector's own test suite + both historical repros named in the row's `verification_gate`, did not trust the developer's pasted 15/15 count or the router's independent-run note.
**what-considered:**
- Ancestry/scope: `git merge-base --is-ancestor 761988143 main` and `origin/main` both true; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook.
- Own re-run: `detect-analysis-only-exit.test.sh` 15/15 PASS. Read the core diff at source (not narration): MANDATORY-PLANE CHECK reads `docs/agents/<id>/init.md`'s `memory.notebook` field, never the artifact under test (AC-2). Grep-swept `docs/agents/*/init.md` myself — only `refine_bctc_md` declares `notebook: none`, matching T14's GREEN control and confirming the conservative mandatory-by-default doesn't mass-false-positive the fleet.
- Re-ran both `verification_gate` historical repros live: system-auditor 2026-08-12T05:35-05:55Z → DETECTED (that signal_queue row has since drained, honestly no longer isolates the mechanism alone, but the fix still fires); news-scout 2026-08-07T03:55-04:30Z → DETECTED `mandatory_status=violation` (ledger=1/notebook=0) — flipped from the pre-fix live PASS, the exact defect this row exists to close.
- AC-3 regression check: diffed T8-T12 — zero lines changed, confirmed non-regression of the c80 partial-write contract path.
- `shellcheck -x -e SC1091` 0 warnings both changed `.sh` files; `mock-guard.sh` correctly N/A (zone is pure bash+md, no TS/JS production files touched, `bun test`/`tsc` N/A).
**why-decision:** vc-approved, DONE_VERIFIED — all 5 ACs independently reproduced from source/live repo, not from the developer's or router's own prose.
**why-change:** none — verdict matches developer's own claim. Lane-moved `qa[]→done_verified[]` in the same `orch-apply.sh` write as the status flip + synced `.head` idle (rule (b)) per CANONICAL:SSOT-STATUSFLIP-LANEMOVE; validator rejected the first write attempt (missing `verification.raw_probe`), retried with `{tool,args,live_value_observed,observed_at}` attached — same recurring gap as qa-S113/S115/S116. No gateway/MCP tool grant this session (Read/Edit/Write/Bash only) — could not `task_release`; the coordinating dev-team/router session (`632721c2-...`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.

### STEP qa-S118 · qa · 2026-08-14T03:01:57Z
**task-id:** FIX-PDFX-EXTRACTION-ENGINE-EMPTY-STRING-SWALLOW
**what-done:** Direct-Commit Verify (QA-Drain, `qa[]` row, `branch:null`, commit `200eabcf3`). Ran ALL tests inside the actual deployed container (`vn-market-intelligence-mcp-pdf-extractor-1`, `/app` md5-byte-matched the repo working tree) per the non-bun-zone rule, not host Python; did not trust the dev's pasted 1056/1044 count.
**what-considered:**
- Ancestry/scope: commit real on `main` + `origin/main`; `git show --stat` matches all 4 claimed files. Traced full AC1 propagation chain at source: `_ocr_page` raises `OcrPageFailedError` → `_extract_text_ocr_sync`'s except tuple re-propagates (not re-swallowed) → `services.py:109 except PDFProcessingError` marks `doc.status="failed"` and re-raises BEFORE `store_extraction()` (never reached) → `usecases.py:89-91` does NOT re-raise it (unlike Capacity/Deadline) so it falls to a clean `status="failed"` DTO — confirmed zero diff lines outside the 4 files, matching the "zero services/usecases/HTTP changes" claim exactly.
- AC2: independently re-ran the 4 negative-control tests (OCR-succeeds-empty/whitespace, blank-page-degrades-not-raises) — all PASS, confirmed distinct code path from the new raise branch.
- AC3: targeted 35/35 PASS (12 new + 2 new + concurrency-invariant + nonblocking suites unmodified). Full suite: 1047 passed/1 failed/11 skipped (1059 collected, matches claimed 1056+3=1059 total; disposition drift explained by container-env difference, immaterial). The 1 failure (`test_bt3_fix3_real_ocr_fidelity`, AC-12 row-count off-by-one) independently confirmed UNRELATED: zero import overlap with the 2 touched files (different pipeline — `ocr_adapter.py`/`text_table_extractor.py`), predates this commit (2026-05-26), `@pytest.mark.slow`+real-Tesseract by its own docstring, and reproduced FAILING on the pre-fix source too via a disposable `docker run --rm` with the old files bind-mounted over the image (isolated from the live container) — non-regression proven, not assumed.
- Falsification independently reproduced on the same disposable pre-fix container run: new test module fails COLLECTION (`ImportError: OcrPageFailedError`). `lint-imports` re-run: 3 kept/0 broken. `mock-guard.sh` PASS. `scope_out` respected — 0 diff lines in `ocr_gateway.py`.
**why-decision:** vc-approved, DONE_VERIFIED — all 3 ACs independently reproduced from source/live container, not from the dev's own status_note prose; the 1 orthogonal full-suite failure isolated to a different pipeline and proven pre-existing via bisect, not swept under "unrelated" on trust.
**why-change:** none — verdict matches dev's own claim. Lane-moved `qa[]→done_verified[]` in the same `orch-apply.sh` write as the status flip; `.head.active_task_id` was NOT this task (a different row) so left untouched per rule (b) — no idle-sync needed. No gateway/MCP tool grant this session (Read/Edit/Write/Bash only) — could not `task_release`; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-PDFX-EXTRACTION-ENGINE-EMPTY-STRING-SWALLOW` and is the authoritative release path.

### STEP qa-S119 · qa · 2026-08-14T03:33:17Z
**task-id:** FIX-STRANDED-SWEEP-CLASSIFY-AGENT-MODEL-SWITCH
**what-done:** Direct-Commit Verify (QA-Drain, `qa[]` row, `branch:null`, commits `5737ac189`+`2a8530c72`). Did not trust the dev's claimed 25/25 (19+6) count — re-ran the suite myself.
**what-considered:**
- Ancestry/scope: both commits confirmed on `main`+`origin/main`; `git show --stat` matches exactly the 4 claimed files, nothing else.
- Read `_is_model_switch_only()`/`_is_owned_elsewhere()`/UNKNOWN-bucket age-gate at source, not narration — AC1 fail-safe (unmatched/empty diff → UNKNOWN), AC3 (shared `SSS_AGE_HOURS` gate, deletions exempt), AC4 (4 new glob entries) all confirmed matching the diff exactly.
- Own re-run `stranded-state-sweep.test.sh`: 25/25 PASS. Independently diffed distinct `ok "<name>"` identifiers HEAD~1 vs HEAD (not trusted from prose): 19 unchanged + 6 new = 25, exact match. `shellcheck`/`mock-guard.sh` clean (bash-only zone, no TS touched). scope_out: `--stat` on both commits lists neither `.claude/agent-models.json` nor `.claude/agents/*.md`.
**why-decision:** vc-approved, DONE_VERIFIED — all 5 ACs independently reproduced from source/live repo, not from the dev's own review_note prose.
**why-change:** none — verdict matches dev's own claim. `[QA] Review Record` appended to the row's own `status_note` (had none); lane-moved `qa[]→done_verified[]` in the same `orch-apply.sh` write; validator rejected the first attempt (missing `verification.raw_probe`), retried with `{tool,args,live_value_observed,observed_at}` attached — same recurring gap as qa-S113/S115/S116/S117. `.head` was a different task, left untouched (rule b). No gateway/MCP tool grant this session — could not `task_release`; coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) is the authoritative release path.

### STEP qa-S120 · qa · 2026-08-14T04:12:49Z
**task-id:** FIX-AUDITOR-T1-PREGATE-MEMCREEP-SINGLE-POINT-SAMPLE
**what-done:** Direct-Commit Verify (QA-Drain, `qa[]` row, `branch:null`, no `commit`/`files[]` on the row — predates the drain). Derived commit `2e2446853` via `git log --all -- scripts/agents-flow/auditor-tier1-probe.sh`, cross-checked date/message against the row's title, acceptance text and `promoted_at`.
**what-considered:**
- Ancestry: `2e2446853` confirmed ancestor of both `main` and `origin/main`. `git show --stat` matches `scripts/agents-flow/auditor-tier1-probe.sh`+`.test.sh` (+docs/WORK.md+own journal), nothing else out of scope.
- Read `_check_mem_creep()` at source, not narration: loops `MEM_CREEP_SAMPLES` (≥2, clamped) `docker stats` samples per container, gates off the WORST (max) pct — a breach in ANY sample forces FAILURE. Own re-run `auditor-tier1-probe.test.sh`: 226/226 PASS incl T54 (reproduces the cited incident verbatim: sample1=70.00% GREEN then sample2=99.91% FAIL → FAILURE, worst named not first) and T55-T58 (worst-of-window not last-sample, N-call-count proof, clamp floor).
- `shellcheck`: 2 warnings in the `.sh` file both git-blame-confirmed to predate this commit (not in its diff hunks); 1 new cosmetic SC2034 in `.test.sh`'s cleanup block (same idiom as the pre-existing `LAUNCHD_DIR`/`LAUNCHD_ACK` resets), non-blocking. `mock-guard.sh` N/A (pure bash zone). Live sanity: ran the actual script against real docker — mechanism live, real FAILURE returned on an unrelated pre-existing rag-service condition (out of scope, not re-signalled).
**why-decision:** vc-approved, DONE_VERIFIED — both ACs (N≥2 samples over a short window; regression test reproduces the GREEN→FAIL flip) independently reproduced from source/live re-run, not trusted from commit-message prose.
**why-change:** none — verdict matches the commit's own claim. `[QA] Review Record` + derived `commit`/`files[]` appended to the row (had none); lane-moved `qa[]→done_verified[]` in the same `orch-apply.sh` write; validator rejected the first attempt (missing `verification.raw_probe`), retried with `{tool,args,live_value_observed,observed_at}` attached — same recurring gap as qa-S113/S115/S116/S117/S119. `.head.active_task_id` was a different task (`FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING`), left untouched (rule b). No gateway/MCP tool grant this session — could not `task_release`; coordinating router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) is the authoritative release path.
