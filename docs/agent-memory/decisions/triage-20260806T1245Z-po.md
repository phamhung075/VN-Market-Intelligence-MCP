# PO Decision Journal — triage 2026-08-06T12:45Z (rag-service near-OOM: is the REVIEW fix live?)

### STEP po-S146 · po · 2026-08-06T12:45:00Z
**task-id:** FU-RAG-DEPLOY-MEMORY
**what-done:** Answered the router's deploy question with raw evidence and authorised the cap raise (768m→1g, reservations 256m→512m) for immediate manual dispatch to developer. The fix in REVIEW **is** live — image created 2026-08-05T16:49:10Z, 5h33m after commit `22232ad2b`, and `docker exec` shows the landed code in the running container (`_compact_lock` L138, `finally:` L293, `_insert_count = 0` L297).
**what-considered:**
- (a) undeployed-fix-still-pending → verify then expedite deploy — REJECTED, falsified by both proofs
- (b) deployed-fix-doesn't-work → recurring-bug-escalation on the code — REJECTED
- (c) deployed fix was never scoped to close this symptom; residual is a separate already-designed row — ADOPTED
**why-decision:** The router offered (a) and (b) as the two branches, and the answer is neither. FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP's own AC6 says verbatim "Does NOT claim to close the restart loop... the residual belongs to FU-RAG-DEPLOY-MEMORY" — the disclaimer predates the symptom and was written by the developer who landed the fix. Treating this as a failed fix would have re-opened a correct, working change (60min of logs: one clean start 12:17:21Z, one model load, no optimize storm, no error signature) and left the actual remedy — decided by architect at 09:04Z — untouched for another cycle.
**why-change:** Deviated from the router's framing that REVIEW might mean "code-landed-awaiting-QA-merge, not deployed". That is a reasonable prior and it was wrong here; the only way to know was to read the running container, not the board. Generalisable: **a lane name is a claim about process state, never about deployed state — resolve deploy questions against the container, never against `status`.**

### STEP po-S147 · po · 2026-08-06T12:45:00Z
**task-id:** FU-RAG-DEPLOY-MEMORY
**what-done:** Refused an interim container restart as a stopgap, and recorded it as a standing disposition on the row rather than a one-off answer.
**what-considered:**
- (a) restart now to buy headroom against the 0.7MiB-free state — REJECTED
- (b) no restart at all until the cap change lands — ADOPTED
**why-decision:** Three independent reasons, all measured not argued. `restart: unless-stopped` is already set, so the OOM path self-recovers — observed at 08:17:30.93Z, a start 1.6s after the die; a manual restart duplicates an armed mechanism. It buys ~25 min: today's 12:17:21Z restart had refilled to 99.91% by 12:42Z, and the 08:17Z one read 95.09% within minutes. And it costs a ~400MB cold model reload (confirmed in logs on every start) with degraded service throughout. The cap-raise recreate **is** a restart — it just returns with ~270-300MiB of headroom instead of 0.7MiB.
**why-change:** no change from plan.

### STEP po-S148 · po · 2026-08-06T12:45:00Z
**task-id:** FU-RAG-DEPLOY-MEMORY
**what-done:** Invoked recurring-bug-escalation against the **dispatch path**, pointed it at the existing owner row, and explicitly did not mint a new one.
**what-considered:**
- (a) mint a row for "P1 ready[] rows with finished designs never get dispatched" — REJECTED as duplicate
- (b) re-mint the A-30 memory finding (3rd signal today) — REJECTED as FP churn
- (c) attach the evidence to FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE's QA and demand manual dispatch this tick — ADOPTED
**why-decision:** Detection fired 8+ times (c47–c55, three signals today), triage worked, architect design completed 09:04Z — and 3h40m later `docker-compose.yml` was byte-identical to its 2026-07-28 commit with nothing in `in_progress[]`. The failure is not detection and not design; it is that `ready[]` is not a dispatch source. FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE already owns exactly that, in REVIEW since 10:16Z. Minting (a) would be the second row against one defect; (b) is the churn pattern already refused on this row's history.
**why-change:** Escalation aimed at a *process* path rather than code — a use the rule had not been put to before. The operative consequence is that the fix for the dispatch gap is itself stuck in the lane it fixes, so it cannot dispatch this row: **manual dispatch this tick is mandatory, waiting for the lane to heal is what produced the 3h40m.**

### STEP po-S149 · po · 2026-08-06T12:45:00Z
**task-id:** FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH
**what-done:** Minted P2/S, zone `apps/rag-service/`, `next_agent=developer`, `depends_on` FU-RAG-DEPLOY-MEMORY — actuating §3 of the architect brief, which was designed at 09:03Z and given no row.
**what-considered:**
- (a) leave it inside `architect_review_note` on FU-RAG-DEPLOY-MEMORY — REJECTED
- (b) fold it into the cap-raise row as an extra AC — REJECTED
- (c) separate row, sequenced after the cap raise — ADOPTED
**why-decision:** (a) is the defect I had just escalated in po-S148, one level down — a decision living in a note field is not dispatchable, which is how §3 was about to be lost the same way the cap raise nearly was. (b) would bloat a compose-only two-line change into a Python + lifespan + test change and delay the thing that stops the OOM. Sequencing is for measurement, not politeness: against a cap the service already saturates, an idle-unload's effect is indistinguishable from the noise of a container at 99.9%.
**why-change:** PO minted execution work off an architect brief without a fresh design pass — justified because §3 is complete (env var, asyncio task in `build_lifespan`, `gc.collect()`, reload via the existing double-check lock). The cap raise **buys time, not closure** — the brief says so itself, and the corpus grows ~103 rows/h against a fixed unreleasable baseline, so a cap sized for today loses the same argument in weeks. That prediction is already on this row from 2026-07-29 and was proven right once by the 08:17:28Z OOM kill.
