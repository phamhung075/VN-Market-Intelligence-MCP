# PO Decision Journal — review-lane time-gate re-check 2026-08-15T03:27Z

task_id: RAG-FTS-BUILD-MEMORY-BOUND (HOLD, done_verified still WITHHELD; AC corrected in place)
secondary: FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED (factual heads-up only, no re-triage)

## Input
dev-team review-lane secondary-drain, session 632721c2-41e4-4aff-8d06-a47cf80dc0d7.
Single stale `review[]` row: P1/M, `branch:null` (direct-commit, no task branch/handoff),
`next_agent=po`, `owner=developer`, in `review[]` since 2026-07-15, last touched by my own
`po_ack_scope_correction_20260729T1126`. `po_timegate_next_recheck_after=2026-08-12` had matured.
Prior disposition: qa PASS-WITH-CAVEAT 2026-07-15, `done_verified` WITHHELD pending corpus
repopulation to the ~56254-row scale AC#1 names.

### STEP po-S1 · po · 2026-08-15T03:27:01Z
**task-id:** RAG-FTS-BUILD-MEMORY-BOUND
**what-done:** Re-measured the corpus live (`GET :5002/embed/health` index_size=30618→30619),
recomputed the realised growth rate, RAW-verified the live cgroup limit and idle memory baseline,
then HELD the row (no lane move, no qa dispatch) while correcting AC#1 + test_plan in place
(768m→1g) and re-classifying `blocked_on` from a pure wall-clock gate to a compound gate.
**what-considered:**
- DONE_VERIFIED sign-off — rejected: AC#1 has still never been run at scale; nothing certifies it.
- Dispatch qa to re-run AC#1 now at 54% corpus — rejected on two independent grounds (below).
- Pure time-gated hold with a refreshed date, changing nothing else — rejected: it would have
  left a factually stale AC (a ceiling removed 9 days earlier) in the path of the next qa run.
- Hold + AC correction + blocker re-classification — chosen.
**why-decision:** The 2026-08-12 gate fired correctly but the answer is still "not yet", for a
reason the old note did not contain. Three new facts: (1) realised growth is 47.7 rows/h over
406.0h, not the 103 rows/h my 07-29 note projected off a 10.3h window — testable slides
~2026-08-16 → ~2026-09-06; (2) AC#1/test_plan hardcode a "768m cgroup ceiling" that commit
`2f835ec63` (2026-08-06, sibling row FU-RAG-DEPLOY-MEMORY) replaced with 1g — live compose
`memory: 1g`, `docker inspect MemLimit=1073741824`; (3) live idle baseline is 858.6MiB/1GiB
(83.85%, flat over 3 samples, warm, zero FTS activity) vs this row's own 07-15 flag of ~92% of
768m ≈ 707MiB — absolute idle RSS grew ~707→~859MiB while the cap rose 256MiB, so the raise
bought no durable headroom, only ~165MiB. Hence the binding constraint is headroom, not corpus
scale, and waiting for 56k makes the test harder rather than easier (demand and baseline both
scale with the corpus). qa was not dispatched because at ~165MiB headroom the test is ONE-SIDED:
a GREEN would be informative even at 54% corpus, but a RED is unattributable between this fix
failing and the unresolved leak having consumed the headroom first — the exact mirror of the
false-GREEN at 116 rows that caused the original WITHHOLD. Second, independent ground: sibling
P0 FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED is live in `qa[]` on this same
container needing a ≥24h dmesg-clean window; a deliberate memory-stress rebuild would contaminate
it. Cost of waiting is low (ALPHA-S2-RAG-FTS-REBUILD-CRON is BLOCKED but deploy-pending, no
active incident); cost of a mid-window OOM is high.
**why-change:** Differs from the 07-29 plan, which anticipated only a corpus-scale re-check and
instructed future-me not to re-derive. Not a re-derivation: the projection was falsified, the AC
was found stale, and the blocker changed category. Deliberately did NOT build on a 03:22Z sample
reading 864MiB at `model_loaded:false` — RSS is not returned to the OS on model unload, so it
cannot support the tempting "container exceeds its cap at idle by 56k rows" extrapolation; the
caveat is recorded on the row instead.

### STEP po-S2 · po · 2026-08-15T03:28:40Z
**task-id:** FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED
**what-done:** Added `po_durability_window_void_flag_20260815` — a factual heads-up that the
≥24h durability window qa opened 2026-08-14T08:41:48Z on container `632080976c9b` is void: the
live container is a different id `66656926d503`, recreated 2026-08-14T20:15:22Z (~11.5h in).
In-VM dmesg (nsenter1) shows ZERO `oom_memcg` events for both `632080976c9b` and `66656926d503`;
the last rag-service kernel kills remain on the older `92e6017318e4`. Also flagged that the
2026-08-13T14:33:14Z kill in the same dmesg output belongs to `9d56864e3d85`/`tesseract`, not
rag-service. `status`/`next_agent`/`owner` deliberately UNCHANGED.
**what-considered:**
- Record it only on my own row — rejected: the P0's actor would never read it, orphaning the
  finding (the analysis-only-exit failure class).
- Re-triage the P0 row (status/next_agent change) — rejected: out of this dispatch's scope and
  it is actively owned; a factual flag is sufficient and non-destructive.
**why-decision:** Reading the recorded 2026-08-15T08:41:48Z floor at face value today would
certify a window that only ran 11.5h on the observed container — the 6th false certification in
this saga, and precisely the failure mode that row's own history warns about. Cheap to prevent,
expensive to miss. I did not investigate why the recreate happened; that is the owner's call.
**why-change:** Not in the original dispatch scope; raised incidentally because the two rows
share a container and my headroom measurement is a direct input to the P0's amplifier hypothesis.
