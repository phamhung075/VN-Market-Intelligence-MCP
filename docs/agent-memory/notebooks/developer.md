# Developer — Notebook

**Last updated:** 2026-08-26T13:10:00Z | **Cycle:** FIX-ORCHAPPLY-CAS-BASELINE-CAPTURED-AFTER-CALLER-JQ-READ (session 036ceaf1, review-lane SECONDARY-drain re-dispatch, redispatch_count=1)

## Session 2026-08-26T12:20:00Z — TASK-COWORK-CATCHUP-9 (docs/, developer, P0 S, session 036ceaf1, router hand-dispatch, BOUNDED-1 idle-capacity pickup)

**Re-scoped instead of executing the handoff verbatim — the row was frozen 2026-08-23 on a content objection (not just a dependency), and the objection was still correct on re-check.** Per-touch-point verdicts: (1) `cowork-schedule.json` `guaranteed` semantics comment — STILL-VALID, unowned by either `FIX-COWORK-DELPROOF-2-SCHEDULE-SCHEMA` (`delivery_proof` field only) or `TASK-COWORK-SCHEDULE-ONMISS-AND-SCOPE` (`on_miss`/`catchup_scope` fields only), but the handoff's proposed text overclaimed live catch-up delivery — wrote a corrected, disk-verified version instead. (2) `match-slots.md` `catchup_raw` contract — ALREADY DONE by `TASK-COWORK-DOC-TRUTH-LAYER-INVENTORY` (DONE_VERIFIED, commit `f4e944454`, 2026-08-23) — wrote nothing. (3) `spawn-fanout.md` `is_catchup:true` note — NO-LONGER-TRUE (the tag is emitted nowhere in the codebase; its producer row `TASK-COWORK-CATCHUP-3` is CANCELLED) and a materially-equivalent, accurate note already shipped there (agent-father, commit `df08ec793`) — wrote nothing. (4) durability-brief addendum — wrote the ACCURATE version (FR-1/FR-2/FR-8/`publish_date_basis` shipped; FR-3/FR-5/FR-7/FR-9 verified NOT shipped, with the exact grep/read evidence per item) instead of the false "per-caller wiring shipped" narrative the frozen handoff specified. (5) cron-runbook — this row's own handoff never assigned it (only the TLDR mentions it, deferring to `TASK-COWORK-CATCHUP-10`/agent-father) — wrote nothing.

**LESSON — "correct semantics" (AC-8's actual wording) is not the same obligation as "the handoff's verbatim text."** A frozen row's own content objection can still leave real, unowned, honest residue — re-scoping is not the same operation as either "execute as specced" or "close empty."

**Multi-writer race, no data lost:** `docs/data/cowork-schedule.json`'s edit landed inside a peer cowork-team tick's own commit (`d6144f64c`, "chore(cowork): 12:00Z tick") — its Step 5b `last_fired` writer re-serializes the whole document and read my uncommitted Edit-tool change before I could commit it myself. Content verified byte-identical and correct at HEAD via `git show d6144f64c -- docs/data/cowork-schedule.json`; no separate commit was needed or possible for that file.

**Verification:** `jq empty docs/data/cowork-schedule.json` valid; `node scripts/agents-flow/cowork-schedule-consistency.test.js` 13/13 pass; `node scripts/agents-flow/cowork-match-slots.js` exit 0, `catchup_raw` still 8 candidates (no regression). Commit `cdd159a49` (`docs/architecture-briefs/2026-07-07-cowork-guaranteed-slot-durability.md` addendum). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-9.md` STEP developer-S130 (§ STEP dev-T9). Row moves `in_progress[] -> review[]`, `next_agent=qa`, `.head` reset idle, same orch-apply write.

## Session 2026-08-26T13:10:00Z — FIX-ORCHAPPLY-CAS-BASELINE-CAPTURED-AFTER-CALLER-JQ-READ (scripts/, cross-service/, developer, P0 M, session 036ceaf1, review-lane SECONDARY-drain re-dispatch, redispatch_count=1)

**QA CHANGES_REQUESTED (qa-S202) on 2 findings against the already-landed AC-1/AC-2/AC-3/AC-5 (commits 894a6617e+4adbf5b1b, 109/109 green): AC-4 unmet on both disjuncts (no caller migrated, no discoverable follow-up row), and AC-3's threshold=1 still lets the ORIGINAL bug's single-row-revert shape through.** Addressed both directly, did not do a third thing.

**AC-4 — migrated a real caller (disjunct (a), the dispatch's stated preference over minting a follow-up row):** `scripts/orch-cold-evict.sh` — the sole live SSOT eviction script (always executes, not a flow-doc), chosen because it already runs its own internal mtime-CAS retry loop, i.e. it already had the exact residual-gap shape AC-1 exists to close. Added `get_hash()` (mirrors `orch-apply.sh`'s own), captured `HASH_BEFORE` at the very top of the retry loop — before `compute_id_maps()`'s own read — and threaded it through as `ORCH_APPLY_CALLER_BASELINE_HASH` on the `orch-apply.sh` invocation. This closes a window the script's own pre-existing mid-loop mtime check does NOT cover: a peer write landing after that mid-loop check but before `orch-apply.sh`'s own (previously self-captured, i.e. late) CAS baseline.

**AC-3 threshold=1 — kept, defended in-file rather than silently left or blindly closed.** Grep-verified (`.task_board.<lane> = [...]` across every flow doc) exactly 3 live call sites depend on today's undeclared single-row-backward tolerance: `docs/agents/qa/flow/main.md` (CHANGES_REQUESTED, qa->review) and TWO independent triggers in `docs/agents/dev-team/flow/main.md` (WF-1a BLOCKED self-heal + resume-attempt-bound-exceeded self-heal, both in_progress->backlog). Lowering `CONSERVATION_MAX_UNDECLARED_BACKWARD_MOVES` to 0 without migrating all 3 to `ORCH_APPLY_DECLARED_BACKWARD_LANE_MOVES` (multi-file, cross-agent-type, out of this row's bounded scope) would fail-close 3 live, load-bearing self-heal/QA paths — strictly worse than the residual gap. Documented this trade-off, with the exact 3 call sites named, directly in `orch-conservation-check.mjs`'s own header comment (not just the row) so a future migration effort doesn't have to re-derive the caller list. AC-1 is the architecturally correct fix for the single-row case specifically: once a caller supplies a hash baseline, ANY staleness (1 row or 100) is caught directly, independent of the lane-rank heuristic.

**LESSON — a design constant's "why" belongs next to the constant, not only in a row's prose.** `CONSERVATION_MAX_UNDECLARED_BACKWARD_MOVES=1`'s original comment only argued the SUFFICIENT side (1 covers every known workflow); it never named which live callers actually depend on it, so a later reader (QA, correctly) couldn't tell "considered and defensible" from "never examined." Naming the call sites in-file closes that gap for the next reader without requiring a repo-wide re-grep.

**AC-5 proof (fires-then-passes, not just green):** new `scripts/test/orch-cold-evict-tests.sh` TEST 13 — a PATH-shadowed `mv` performs the real cold-archive rename then injects a peer mutation to the hot fixture in the exact residual window. (a) FIRES: run aborts non-zero after exhausting `MTIME_CAS_RETRIES`, hot file retains the peer's injected row and this run's own stale candidate is correctly rejected (`BL-DONE-1` still present, never evicted). (b) PASSES: identical fixture, no injection — normal eviction still succeeds exit 0. Confirmed the guard was genuinely a no-op pre-fix, not just asserting it: `git stash`-reverted only `scripts/orch-cold-evict.sh`, re-ran the same new test — all 4 of (a)'s assertions FAIL (peer row silently clobbered, stale candidate applied), (b) still passes; restored the fix, re-ran, all green.

**Verification:** `bash scripts/test/orch-cold-evict-tests.sh` → 75/77 (baseline 67/69 + 8 new TEST-13 assertions all green; the 2 remaining FAILs are pre-existing T7 poison-eviction failures, unrelated, unchanged before/after). `bash scripts/test/orch-apply-wrapper-tests.sh` → 109/109 unchanged (`orch-apply.sh` untouched this pass — only `orch-cold-evict.sh`, its test file, and a comment-only `orch-conservation-check.mjs` header update). `bash -n` clean both shell files. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-9.md` capped at byte-axis (42295B/36000B) and rolled to `-10.md` STEP developer-S131 — best-effort bug-channel cap notification skipped (no MCP gateway tool bound in this specialist's tool grant; same structural class as this flow's documented `/graphify` gap). Row stays `review[]`, `status=REVIEW`, `next_agent=qa` (QA-drain terminal shape per dispatch instruction).
