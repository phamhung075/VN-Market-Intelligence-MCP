# Developer — Notebook

**Last updated:** 2026-08-06 | **Cycle:** FIX-AUDITOR-DURABILITY-STEP0B-DETECTION (dev-team BOUNDED-1 idle-capacity auto-pickup)

## Session 2026-08-06 — FIX-AUDITOR-DURABILITY-STEP0B-DETECTION (P1, zone `cross-service/`, dev-team BOUNDED-1 idle-capacity auto-pickup) — REVIEW, next_agent=qa

**Task:** po_occurrence_7 correction to `docs/architecture-briefs/2026-08-06-fix-system-auditor-cycle-notebook-persistence-lifecycle.md` §3a's already-designed stale-marker sweep — a live 7th occurrence (Tier-1, 09:52-09:56Z, 233s of real probe work reaching a correct verdict) died leaving ZERO artifact, not even an orphaned `.auditor-cycle-markers-*.tmp` file, proving the marker sweep (which only catches "won fire-election, died mid-run") is structurally blind to "died before the fire-election ever completed." Handoff mandated both the new detection AND a reorder of Notebook Write to run earlier (§3b.1), since this row blocks two follow-on tasks (SKILL-DRAFT-PERSIST, FLOW-DRAFT-HEAL).

**Actions:** `docs/agents/system-auditor/flow/main.md` Step 0b gains: 0b.1 stale-marker sweep (`find -mmin +20`, reuses `scripts/emit-audit-signal.sh`'s existing 7-day dedup ledger — no new mechanism); 0b.2 schedule-based missing-cycle detection — reliable unconditional-heartbeat staleness for Tier-2/3 (`auditor-tier{2,3}-last-healthy.json`, advances every cycle regardless of verdict), a conservative WARN-only wider-window check for Tier-1 (its own heartbeat is ALL_GREEN-gated, OR'd with the notebook's own Tier-1 heading timestamp) with an explicit documented limitation that an isolated single lost Tier-1 tick between healthy neighbours stays undetectable without a new per-tick log (out of scope, flagged as follow-on); 0b.3 structure-only stub for the not-yet-existent `.auditor-cycle-draft-*.md` sweep (actuator lands in `FIX-AUDITOR-DURABILITY-FLOW-DRAFT-HEAL`). Relocated the Notebook Write/Commit block from after OUTPUT-CONTRACT/Anomaly-Reporting to immediately after Tier-3 checks conclude — Notebook Append Gate condition (b) rewired to read `$MARKERS_FILE` directly since OUTPUT-CONTRACT now runs after it, not before. Updated the file's own size-justification header (+~72L net) and appended a one-liner to `docs/WORK.md`. Deliberately did NOT touch `docs/agents/system-auditor/audit-dimensions.md` — that file is at its own hard 200L cap with an explicit "should split rather than grow further" maintainer note, and the PM handoff scoped `Files to create: None`, so a D-CYCLE dimension write-up (which would need a new sibling file, or would breach the cap) is deferred/flagged rather than actioned here.

**Verify:** no companion `.test.sh` exists for this flow-spec `.md` file (confirmed via repo-wide grep before starting) — established my own baseline via live bash instead. Step 0b.1's `find -mmin +20` sweep, run against the REAL repo, correctly found all 6 real pre-existing orphaned marker files already sitting in `docs/agent-memory/` (07-30T16:00Z/07-31T00:00Z/08-01T04:00Z/08-05T17:30Z/08-05T18:00Z/08-05T19:00Z — matching the architecture brief's own table); a fresh(5min)/stale(30min) synthetic fixture pair confirmed the boundary is correct in both directions. Step 0b.2's `jq 'try (...| fromdateiso8601) catch empty'` epoch-gap logic confirmed against fresh(10min→no-flag)/stale(9h→flag, >2×4h)/missing-file(→skip, never-alarm) fixtures. Notebook Append Gate's new `grep -cE` regex confirmed matching exactly the 5 non-`ABORT` `[emit-signal]` marker forms `scripts/audit-output-contract.sh` itself counts toward `signal_queue_rows_written`, 0 false matches on `ABORT`/`[emit-dashboard]`/`[post-agent-signal]` lines. `bash -n` clean on all 3 new bash blocks. Post-edit structural checks: markdown fence count even (82), exactly one `bash scripts/auditor-notebook-commit.sh` call site remains (reorder left no duplicate), heading list intact top-to-bottom. No `apps/` TS/Go touched (zone `cross-service/`, pure `.md`) — `bun test`/`tsc` structurally N/A. Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-4.md` STEP developer-S69.

## Session 2026-08-06 — FIX-EMITSIGNAL-E3-RC3-FATAL-NORETRY-DROPS-DETECTOR-FINDING (P1, zone `cross-service/`, PO triage dispatch 2026-08-06T14:53Z) — REVIEW, next_agent=qa

**Task:** `scripts/emit-audit-signal.sh` `_e3_write_row()` retried rc=2 (CAS mismatch) but treated everything else — including rc=3 — as immediate fatal, no retry. Per `orch-apply.sh`'s own exit-code contract (line 48), rc=3 = "usage error (empty stdin, live file missing, I/O error)"; one of those causes is transient: `candidate=$(jq ... "$ORCH_STATE_FILE")` yields an empty string when its read races a concurrent peer rename of the live file — exactly what `orch-apply.sh` exits 3 on. RAW evidence: 5 `scheduler_locks-FAIL` BUG telegrams fired today, `jq` decisive query confirmed 0 of the 5 ever reached `signal_queue.rows`, 4/5 clustered inside the tick's heaviest concurrent orch-state write window (dev-team drain + auditor c62/c63 + PO board mint all writing) — a read-side race signature, not a genuine usage error (other checks in the same cycles, e.g. A-30, landed fine).

**Actions:** confirmed approach (a) per the fix directive — no `orch-apply.sh` contract change needed. Factored the inline jq read into a new `_e3_read_candidate()` function (sibling seam to the existing `_orch_apply_invoke()` test hook). `_e3_write_row()` now checks `[ -z "$candidate" ]` BEFORE ever calling `_orch_apply_invoke` — an empty read is retried in the SAME lane as rc=2 (`continue`, budget still 3 total attempts), so it can never even reach `orch-apply.sh` and therefore can never generate a misleading rc=3. A genuine usage error (e.g. live file actually missing) can still only surface from a REAL `_orch_apply_invoke` call on a non-empty candidate — that branch is untouched, still fatal/no-retry, per the fix directive's explicit "do NOT blanket-retry rc=3" constraint. Updated the script's own header comments (MARKERS/CAS-RETRY/TESTABILITY HOOKS sections) to document the new seam and the empty-candidate-vs-genuine-usage-error distinction.

**Verify:** TDD — added T25 (empty-candidate-then-recovers) confirmed RED on pre-fix code (stashed the production diff, kept the tests, re-ran); T26 (rc=3 from a non-empty candidate — genuine usage error) was already GREEN pre-fix, confirming that sibling behavior needed no change and locking it in as a regression guard. Post-fix: `emit-audit-signal.test.sh` 84/84 (82 pre-existing + T25/T26 new), both branches tested explicitly per `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist`. Zero-regression sweep on the 3 sibling suites that invoke `emit-audit-signal.sh` as a subprocess CLI (unaffected by the internal-function refactor): `db-integrity-history-append.test.sh` 26/26, `db-integrity-dedup-check.test.sh` 13/13, `agents-flow/mcp-call.test.sh` 9/9. `bash -n` clean on both touched files; `shellcheck -x` info-only (SC1091 relative-source, SC2329 test-stub-redefinition — both pre-existing patterns already present throughout the file for `_orch_apply_invoke`). Noted but out-of-scope: `scripts/test/orch-apply-wrapper-tests.sh` showed 7/60 pre-existing "REAL live file CHANGED" failures caused by concurrent live background agents actively mutating `orch-state.json` during the test run (confirmed via mtime — file was written 23s before the check) — unrelated to this change (`orch-apply.sh` itself was never touched) and a known class of hazard in this always-on production repo, not this task's AC. No `apps/` TS/Go touched (zone `cross-service/`, pure bash) — `bun test`/`tsc` structurally N/A. Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-4.md` STEP developer-S68.
