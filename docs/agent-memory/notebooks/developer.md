# Developer — Notebook

**Last updated:** 2026-08-06 | **Cycle:** FIX-MARKETDB-JOURNALMODE-GUARD-SHIPPED-BUT-NEVER-ARMED

> **NOTE (concurrency, standing limitation, not part of any task AC):** `docs/agent-memory/notebooks/*.md` has no CAS/mtime guard analogous to `orch-apply.sh`'s for `orch-state.json` — a same-window concurrent-agent notebook write can silently orphan a just-landed section even after `git log` shows the commit landed first. Flagged for po/architect as a candidate follow-up; not fixed in this task.

## Session 2026-08-06 — FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT (P1, zone `cross-service/`, dev-team BOUNDED-1 auto-pickup) — REVIEW, next_agent=qa

**Task:** 2026-07-15 commit 5ba622eca moved the live DB off the docker named volume onto a host bind mount (`./data/live:/app/data`); `db-integrity-counts.sh`/`db-integrity-probe.sh` were never migrated — every invocation made docker silently auto-create a fresh empty volume, so the counts helper returned null (fail-open) and the probe voted SPAWN unconditionally for 21 days, unnoticed.

**Actions:** AC-1: chose direct host-bind sqlite3 read (`MARKET_DB_HOST_PATH`, no docker at all) — already the shipped pattern in `db-empty-table-classify.sh`. Migrated `db-integrity-counts.sh`, `db-integrity-probe.sh`, and `ops-bctc-enrich-reverify-pulljob.sh`'s two read-only display queries. AC-4: both scripts fail loud on a genuinely-unreachable DB now; probe.sh keeps its documented FAIL-OPEN/SPAWN verdict (correct, deliberate design) but gained a loud stderr diagnostic instead of silence. AC-5: rewrote the DB ACCESS block in `cron-db-data-integrity.md` + both `register-job-db-integrity-*.md` mirrors (kept byte-identical) plus `bctc-extraction-runbook.md`'s 2 verification snippets (found via full-repo grep, not in the task's file list). AC-6: corrected the same now-inverted claim in `system-auditor/flow/main.md`'s C-01..C-16 section (prose only — its actual `docker exec` query mechanism was never broken); the equivalent private memory note lives outside this repo and cannot be edited by this agent — flagged for the router. AC-7: new `scripts/db-integrity-mount-drift-check.sh` (+ test) resolves the mcp-server container's live `/app/data` mount and fails loud if it diverges from the observer's configured path; not a CI job (no docker stack in GitHub Actions), pointed to from the cron doc for local/ops use.

**Verify:** AC-2 two-plane diff (host sqlite3 vs mcp-server's own bun:sqlite reader) — 6/6 columns byte-identical, live. AC-3: probe run twice, no writes between — 2nd call SKIP-SPAWN, live. New tests `db-integrity-counts.test.sh` 18/18 (hand-counted fixture + LIVE replay), `db-integrity-mount-drift-check.test.sh` 12/12 (incl. LIVE replay — PASSES against the real running stack today), `db-integrity-probe.test.sh` updated (docker→sqlite3 stub) 27/27. Sibling regression sweep zero-regression: `db-integrity-history-append.test.sh` 26/26, `db-integrity-dedup-check.test.sh` 13/13, `db-empty-table-classify.test.sh` 36/36, `emit-audit-signal.test.sh` 75/75. Post-fix repo grep confirms only comments + historical `.jq`/spike artifacts remain. No `apps/` TS/Go touched — `bun test`/`tsc` N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S61.

## Session 2026-08-06 — FU-RAG-DEPLOY-MEMORY (P1, zone `ops/infra`, PO manual-dispatch triage-20260806T1245Z-po) — REVIEW, next_agent=qa

**Task:** rag-service container OOM-killed (exit137, `docker events` `container oom`/`die` 08:17:28-30Z) then re-crested to 99.9% within ~25min of every restart — architect brief (2026-08-06T09:03Z) had already fully decided the fix (`docker-compose.yml` rag-service `deploy.resources.limits.memory` 768m→1g, `reservations.memory` 256m→512m) but it sat unexecuted 3h40m because the automated ready-lane dispatch path is broken (`FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE`, separate row) — PO manually dispatched this row to `developer` with no gate remaining.

**Actions:** edited `docker-compose.yml` (lines 165-172, rag-service `deploy:` block only) — `memory: 768m→1g`, `memory: 256m→512m`. Recreated single-service: `docker compose up -d --no-deps rag-service` (no `build` needed for a compose-only limits change). No other service touched. Out-of-scope items explicitly left alone per the brief's own sequencing: `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` (P2, structural follow-up, sequenced after this cap raise) and `FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` (separate row, already in `review[]`, disclaims closing this memory symptom).

**Verify:** `docker inspect` — `Created`=2026-08-06T12:57:38Z (new, was 2026-08-05T16:49:10Z), `RestartCount`=0, `Status`=running, `OOMKilled`=false, `Health`=healthy within seconds of start; `MemoryLimit`=1073741824B(1g)/`MemoryReservation`=536870912B(512m), confirmed changed from 805306368/268435456. `docker stats` immediately post-start: 322.3MiB/1GiB=31.47% (cold — lazy-load embedder not yet warmed; logs confirm "lazy-load enabled... will load on first embed call"). `docker compose ps`: all 12 sibling containers untouched, original uptimes intact (3wk/12d/7d/6d unchanged), all healthy — `--no-deps` confirmed peer-safe, no down/stop/kill/rm/--force-recreate used (`feedback_preserve_multi_container_microservice_architecture` honored). Container logs clean: startup + `/health` 200 OK, zero error/OOM signature. No `apps/` TS/Go touched (root `docker-compose.yml` config only) — `bun test`/`tsc` structurally N/A. Commit `2f835ec63`. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S63.

## Session 2026-08-06 — FIX-MARKETDB-JOURNALMODE-GUARD-SHIPPED-BUT-NEVER-ARMED (P0, zone `cross-service/`, router dispatch) — REVIEW, next_agent=qa

**Task:** `scripts/audits/verify-market-db-journal-mode.sh` (shipped 2026-07-30, self-tests green) had zero call sites — no cron, no CI, no flow doc, no auditor probe — while the exact condition it detects went live and produced the 5th market.db corruption on 2026-08-06 (`e370f5f51`, 2026-07-31, dropped `mode=ro` from a stock-price DSN, silently re-arming WAL 14h after the DELETE-mode fix). Detection-plane structural fix; the code-plane fix is a separate, parallel `FIX-STOCKPRICE-...` row.

**Actions:** AC-1: new `*/15 * * * *` standalone cron `market-db-journal-guard` via the existing `cron-standalone-team` lane (Job 6) — `.claude/commands/crons/cron-market-db-journal-guard.md` → `register-job-market-db-journal-guard.md` → `SKILL.md` idempotency-guard entry 6/6, byte-identical prompt (python-diff verified). No subagent spawn — runs the probe and branches on its own exit code; FAIL(2)/ERROR(3) both alert `send_telegram(channel="bug", ...)` verbatim verdict line (ERROR-too is a deliberate extension beyond AC-1's literal FAIL-only wording). AC-2: new sibling `scripts/audits/verify-market-db-journal-source-guard.sh` — opt-IN allowlist (sole owner `schema.ts`, never opt-OUT), TS/JS `PRAGMA journal_mode=` proximity-window (WINDOW=20, calibrated 0-FP against `coordinationStore.ts`'s real 27-line token/SET gap) + Go `_journal_mode=` DSN token (the exact gap a TS-only sweep would miss — `e370f5f51`'s shape). Wired into CI (`verify-market-db-journal-source-guard` job). AC-4: new `docs/policies/market-db-journal-mode-policy.md` (DELETE + synchronous=FULL, sole owner, WHY, durability trade-off, `e370f5f51` cautionary example), referenced from `docs/ARCHITECTURE.md`. Did NOT rewrite the existing runtime script.

**Verify:** source-guard `--check` PASS on live repo, 0 offenders; `.test.sh` 6/6 (rearmed TS/Go fixtures FAIL with correct tag, coordinationStore-shape negative control + Go-test-file + bare-PRAGMA-read all PASS). Runtime guard run live at authoring time: genuine FAIL (journal_mode=wal, wal_present=true, shm_present=true — not synthetic) — `send_telegram(channel="bug", ...)` proven delivered via `scripts/agents-flow/mcp-call.sh`'s `mcp_call` bridge, `message_id: 4809` (AC-1's verification-gate requirement: alert path proven live, not just self-tested). CI YAML validated (`python3 -c yaml.safe_load`). AC-3 both-guards-required proven distinctly (source fixture pair + runtime `--self-test`, neither substituting the other). No `apps/` TS/Go touched (bash+markdown+CI-yaml only) — `bun test`/`tsc` N/A. Graphify incremental update SKIPPED — full doc semantic-extraction pipeline requires subagent calls outside this run's tool grant (Bash/Read/Write/Edit only); flagged for a follow-up `/graphify docs --update --no-viz` by a session with that capability. Commits `590dd4124` (implementation), `49e0b318b` (board lane-move). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S64.
