# Developer — Notebook

**Last updated:** 2026-08-06 | **Cycle:** FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT

> **NOTE (concurrency, standing limitation, not part of any task AC):** `docs/agent-memory/notebooks/*.md` has no CAS/mtime guard analogous to `orch-apply.sh`'s for `orch-state.json` — a same-window concurrent-agent notebook write can silently orphan a just-landed section even after `git log` shows the commit landed first. Flagged for po/architect as a candidate follow-up; not fixed in this task.

## Session 2026-08-06 — FIX-CI-TASKCLAIM-CHEF-MD-OWNER-SESSION-PAYDOWN (P1, zone `cross-service/`, PO triage dispatch) — REVIEW, next_agent=qa

**Task:** CI RED (`CI-RED-246eb859`, task-claim-owner-session-lint) — `chef.md:108` `PUBLISH_CLAIM` `task_claim` call omits `owner_client_session`, a non-optional `z.string()` on `coordinationTools.ts:104-110`. Lint's own LINE-MOVED label confirmed root cause: `ff1745e5a` (TE-T16 chef.md split) shifted the grandfathered call site 102→108; NOT a new/reintroduced defect. 2nd instance of this structural class (1st: PO-flow row, already closed).

**Actions:** added `owner_client_session` to the `task_claim` call, substitution-instruction pattern mirrors `pm/flow/main.md:127` + `commit-mutex/SKILL.md` (never the literal `$CLAUDE_CODE_SESSION_ID` text — known hazard). Dropped the `docs/agents/unified-agent/flow/chef.md` entry from `task-claim-owner-session-baseline.json` (20→19) per PO instruction — did NOT re-run `--update` (would just re-grandfather the new line and mask the debt again). AC5 (re-key baseline file+snippet vs file+line, or document line-keying-is-deliberate) intentionally NOT actioned: row's `files` scope is `[chef.md, baseline.json]` only, excludes the lint script; sibling row already ruled that widening out of scope (gate-widening needs an actuator dry-run first, standing rule) and the script's header already documents the ratchet rationale.

**Verify:** `bash scripts/audits/task-claim-owner-session-lint.sh --check` → PASS RC=0, 0 new offenders (274 files scanned, 19 grandfathered sites). `task-claim-owner-session-lint.test.sh` 9/9 PASS. No `apps/` TS/Go touched (docs-only) — `bun test`/`tsc` N/A. Commits `be3545412` (chef.md + baseline), `256aea687` (board lane-move backlog[]→review[]). AC3 (CI green on subsequent push) deferred to qa/post-push — outside this agent's push scope.

## Session 2026-08-06 — FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED (P0, zone `cross-service/`, dev-team BOUNDED-1 idle-capacity auto-pickup) — REVIEW, next_agent=qa

**Task:** system-auditor's DEDUP-ENFORCEMENT clause (`.claude/commands/crons/cron-db-data-integrity.md` + both `register-job-db-integrity-*.md` mirrors) was pure prose — nothing ever read `.task_board`/`.signal_queue.rows[]` before a signal write — so the SAME 336-row daily_ohlcv residue was re-signalled 3x (2026-08-05T07:06Z, 08:18:46Z, 2026-08-06T07:21:27Z), twice under a DIFFERENT dedup_key/type string for the identical rows. PO raised to P0 after 3 confirmed live instances sat BACKLOG ~24h with zero fix attempts (recurring DETECTION of an un-shipped fix, not a recurring failed fix — dispatch was the correct action).

**Actions:** AC-1: new `scripts/db-integrity-dedup-check.sh` — deterministic classifier, table-name + curated-synonym match (NOT the naive per-token match tried and rejected mid-implementation — see decision journal for the live over-match evidence) against ALL open `.task_board` lanes (backlog/ready/in_progress/review/qa) + `.signal_queue.rows[]` (case-insensitive status). Folded into `scripts/db-integrity-history-append.sh` — the ONE call the cron already makes unconditionally every cycle — so every `verdict==REAL` finding is now dedup-gated by construction, not by an agent's judgment. AC-2: already-open findings get `signal_id:"already-open:<matched id>"` + the raw `dedup_check` JSON embedded (no signal_queue write); fresh findings get written via `emit-audit-signal.sh --e3-only`. AC-4: `db-integrity-counts.sh` gained `context.ohlc_violation_distinct_dates` so the exact "concentrated on one date" confabulation (real: 20 distinct dates) is mechanically falsifiable. AC-5: new `--payload-ref` flag on `emit-audit-signal.sh`; the new actuator hardcodes `to=dev-team`/`payload_ref=docs/data/db-integrity-history.json` — closes the 3x to="po"+payload_ref=null routing defect structurally. AC-6: explicit GROWTH-DELTA EXCEPTION policy text added to all 3 cron prompt copies — the table-linkage gate is BINARY, no count-delta reopen path offered at all (the C-08 1→121→22→42 non-monotonic history is the documented cautionary example for why not).

**Verify:** `db-integrity-dedup-check.test.sh` 13/13, `db-integrity-history-append.test.sh` (new) 26/26, `emit-audit-signal.test.sh` 75/75 (73 pre-existing + 2 new) — 114/114 total, zero regressions. AC-3 replay against all 17 live `db_integrity_breach`/`db_anomaly*` signal_queue rows: 17/17 already_open=true against today's board, matching PO's own historical FOLD/FALSE-POSITIVE/DUPLICATE dispositions on every one (no true positive lost in the available sample); a deliberately-unrelated fresh table correctly scores already_open=false. Known residual FP disclosed: table "macro_indicators" can false-match a task title naming the agent "dev-macro-indicators" — flagged for a future evidence-based synonym tightening pass, not fixed here. `db-integrity-counts.sh`'s new SQL column could not be execution-tested (no docker/live DB in this sandbox — same constraint as every prior edit to that file). No `apps/` TS/Go touched (bash+markdown only) — `bun test`/`tsc` N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S59.

## Session 2026-08-06 — FIX-CI-PARITY-CLAUDEMD-CRON-LITERAL-EXEMPTION-SHAPE (P0, zone `cross-service/`, dev-team BOUNDED-1 auto-pickup) — REVIEW, next_agent=qa

**Task:** CI RED (T-U3-4 + T-U3-7) — CLAUDE.md:48's `/cron-standalone-team` bullet wrote its per-skill cron count as bare prose ("4 standalone crons") instead of the `(N crons)` paren-wrap convention line 47 already used, so `findCronCountLiterals`'s whole-match paren-wrap exemption read it as a divergent GLOBAL literal vs `project-stats.json` cronJobCount=88. PO ruled (read both sides): CLAUDE.md side is the defect; test-side change limited to the narrow AC2 correction, no regex widening.

**Actions:** NONE — RAW-verified this exact fix already shipped by commit `c3c5d63d2` (2026-08-05T19:45:25+02:00, same author, prior developer cycle): CLAUDE.md:48 wrapped as `(4 standalone crons)` (punctuation-only, zero word/order change) + T-U3-7 tightened from `hits.every(h=>h.exempted)` to `exemptedHits.length>0` (AC2), `findCronCountLiterals` untouched (AC3, confirmed no diff to that function in the commit). Board row was left `in_progress` (stale flip — [ship completion] pattern) despite the fix landing; this cycle's only work was the verification + board close.

**Verify:** `bun test src/__tests__/tool-registry-parity.test.ts` 17/17 pass, 39 expect() calls, 0 fail (live re-run, not trusted from commit message). CI corroboration: origin RED run `31028825500` (head `f080fbc08`, `ci-red-f080fbc0` signal) failure confirmed; `git merge-base --is-ancestor f080fbc08 c3c5d63d2` confirms the fix postdates the RED head; `gh run view 31098224312 --json jobs` (HEAD `4408c9283`, ancestor of/including the fix) shows job "bun test" conclusion=success. Board `in_progress[]`→`review[]`, status REVIEW, next_agent=qa, `.head` synced idle same write (`orch-apply.sh`, commit `133fa6a8a`). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S60. No code touched this cycle — `bun test`/`tsc` re-verified clean, no new diff.

## Session 2026-08-06 — FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT (P1, zone `cross-service/`, dev-team BOUNDED-1 auto-pickup) — REVIEW, next_agent=qa

**Task:** 2026-07-15 commit 5ba622eca moved the live DB off the docker named volume onto a host bind mount (`./data/live:/app/data`); `db-integrity-counts.sh`/`db-integrity-probe.sh` were never migrated — every invocation made docker silently auto-create a fresh empty volume, so the counts helper returned null (fail-open) and the probe voted SPAWN unconditionally for 21 days, unnoticed.

**Actions:** AC-1: chose direct host-bind sqlite3 read (`MARKET_DB_HOST_PATH`, no docker at all) — already the shipped pattern in `db-empty-table-classify.sh`. Migrated `db-integrity-counts.sh`, `db-integrity-probe.sh`, and `ops-bctc-enrich-reverify-pulljob.sh`'s two read-only display queries. AC-4: both scripts fail loud on a genuinely-unreachable DB now; probe.sh keeps its documented FAIL-OPEN/SPAWN verdict (correct, deliberate design) but gained a loud stderr diagnostic instead of silence. AC-5: rewrote the DB ACCESS block in `cron-db-data-integrity.md` + both `register-job-db-integrity-*.md` mirrors (kept byte-identical) plus `bctc-extraction-runbook.md`'s 2 verification snippets (found via full-repo grep, not in the task's file list). AC-6: corrected the same now-inverted claim in `system-auditor/flow/main.md`'s C-01..C-16 section (prose only — its actual `docker exec` query mechanism was never broken); the equivalent private memory note lives outside this repo and cannot be edited by this agent — flagged for the router. AC-7: new `scripts/db-integrity-mount-drift-check.sh` (+ test) resolves the mcp-server container's live `/app/data` mount and fails loud if it diverges from the observer's configured path; not a CI job (no docker stack in GitHub Actions), pointed to from the cron doc for local/ops use.

**Verify:** AC-2 two-plane diff (host sqlite3 vs mcp-server's own bun:sqlite reader) — 6/6 columns byte-identical, live. AC-3: probe run twice, no writes between — 2nd call SKIP-SPAWN, live. New tests `db-integrity-counts.test.sh` 18/18 (hand-counted fixture + LIVE replay), `db-integrity-mount-drift-check.test.sh` 12/12 (incl. LIVE replay — PASSES against the real running stack today), `db-integrity-probe.test.sh` updated (docker→sqlite3 stub) 27/27. Sibling regression sweep zero-regression: `db-integrity-history-append.test.sh` 26/26, `db-integrity-dedup-check.test.sh` 13/13, `db-empty-table-classify.test.sh` 36/36, `emit-audit-signal.test.sh` 75/75. Post-fix repo grep confirms only comments + historical `.jq`/spike artifacts remain. No `apps/` TS/Go touched — `bun test`/`tsc` N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S61.
