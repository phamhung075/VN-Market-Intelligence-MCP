# Developer — Notebook

**Last updated:** 2026-08-23T18:40:00Z | **Cycle:** FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS (session 007e33e4, router-dispatched)

## Session 2026-08-23T18:40:00Z — FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS (scripts/, developer, P0 M, session 007e33e4, router-dispatched)

**Root cause was NOT ordering.** `scripts/orch-cold-evict.sh` already wrote cold-archive-before-hot-delete at commit 38c013342e (verified against the live script version that day via `git show f0644c14a:scripts/orch-cold-evict.sh`). The actual defect: a real `mv` to the real archive path was never followed by any check that the write durably landed — a completed-but-empty/wrong archive write could silently precede the hot delete and nothing detected it. 10 terminal rows were destroyed 2026-08-06 with zero corresponding archive write, later misdiagnosed as "phantom prerequisites" for 5 downstream P0 rows.

**Fix:** added `verify_cold_archive_write()` — per-id presence across all 6 cold-target arrays + exact row-count-growth assertion against `n_new_cold` — called TWICE: on the temp file pre-rename (catches a build_cold_temp selection bug) AND again on the real on-disk archive file post-rename (catches the rename silently not landing or a concurrent writer racing it — the actual historical shape). Either failing aborts the WHOLE run before `orch-apply.sh` (the hot write) is ever invoked. A hot-delete-without-archive-write outcome is now structurally unreachable through this script's own control flow, not just narrated-safe.

**Found and fixed a genuine pre-existing bug while building the check:** `$cold_dj_strs | index(tostring)` (decision_journal dedup in `compute_id_maps`) is a jq gotcha — `index(f)`'s argument runs with `.` bound to `index()`'s own input (the array being searched), not the piped element, so the dedup was a silent permanent no-op (every retry would have duplicated decision_journal entries in cold). Fixed both that occurrence and the identical pattern in my own new check.

**Regression test (TEST 12 a/b/c, `scripts/test/orch-cold-evict-tests.sh`):** PATH-shadowed `mv` reproduces 38c013342e exactly — rename call reports success but the target either never appears (a) or appears valid-but-empty (b, the closer real-incident analog). **Mutation-tested, not assumed:** reverted to the pre-fix script and re-ran — T12a/b correctly go RED (hot file changes despite archive write failing), confirming the test is non-vacuous. Full suite: 67/69 PASS — the 2 remaining failures are pre-existing and unrelated (TEST 7's fixture predates a same-day-earlier commit, `407afb6a4`, that changed done[] eviction semantics; flagged, not fixed, out of this row's scope).

**AC-3 (optional — recover the 4 remaining victim rows) explicitly NOT attempted.** Each requires honestly reconstructing a per-row `verification.raw_probe` from dense (1000+ line) historical `status_note` records — the schema hard-rejects a `DONE_VERIFIED` write without one (`orchStateSchema.ts checkVerificationGate`), and the task itself says to restore without it (with an honest note) rather than fabricate. Skipped under P0 time/token budget rather than rushed; said so explicitly instead of narrating completion.

**Journal note:** `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-8.md` was already past its byte cap (38254B/36000B) before this cycle touched it — rolled to `-9.md` per the decision-journal skill's cap-check rather than appending further; no bug telegram sent (no MCP/gateway tool grant on this router-direct spawn, same structural gap as a prior same-file cycle).

---

## Session 2026-08-23T15:30:00Z — FIX-BASHGRANT-GATE-NEGATED-GIT-COMMIT-PHRASE-FALSE-POSITIVE + TASK_003-DOMAIN-MODEL-WATCHLIST-COUNT-FIX (scripts/, docs/, developer, P0 S + P1 XS, session 7be6b4cd, router-dispatched)

**Priority 1 (CI-red, P0):** `scripts/audits/agent-bash-grant-coverage.sh` DEMAND_ERE substring-matched `git commit` inside the NEGATED phrase "no per-line git commit" (fleet-wide debug-logger-protocol boilerplate, commit `baa91292c`) — refine_bctc_md's only DEMAND_ERE hit in its whole corpus, a pure false positive (the agent genuinely demands no Bash). PO's diagnosis verified exactly as stated before touching code. Fix: `demand_is_negated()` (perl, per-occurrence) strips a DEMAND_ERE hit only when a negation trigger (no/not/never/without/none) sits within 0-2 short filler words directly before it — general guard for the class, not a one-off string exclusion. **DID NOT** grant Bash to refine_bctc_md and **DID NOT** baseline the finding, both explicitly forbidden by the row. Verified: `--check` exit 0 (was exit 1), refine_bctc_md `bash_demand=no-demand check1=OK`; `bctc-analyst`'s real (un-negated) demand evidence unaffected, stays `GRANDFATHERED`. Added 2 regression fixtures (Case 10: negated-only phrase → no mismatch; Case 11: negated line + real demand elsewhere → still detected) to the pre-existing `agent-bash-grant-coverage.test.sh`, now 12/12 GREEN.

**RE-SCORED CLASS EVIDENCE (asked for, not optional):** diffed the full 42-agent pass/fail table before/after. 9 agents' `bash_demand` column changed (demand→no-demand): `dev-alert-engine, dev-api-gateway, dev-kinh-dich, dev-macro-indicators, dev-pdf-extractor, dev-rag-service, dev-stock-price, dev-technical-analysis` (8 — all already Bash-granted for real, separate reasons per the script's own header "Scope note", so their CHECK-1/CHECK-2 status columns did NOT change) + `refine_bctc_md` (the 1 whose status genuinely flips FAIL→OK). Confirmed by hand-grepping each of the 8's corpus: the fleet-wide debug-logger phrase was their ONLY hit too — previously mis-scored "demand" when their corpus text has no real git-commit step.

**Priority 2 (P1, XS):** `docs/architecture/microservice/frontend/domain-model.md:72` said "33 entries (30 active + 3 inactive)" — stale since sprint-054 (only VEA went inactive). Verified raw (not from the handoff's stated numbers) against `system-map.json` (34/33/1=VEA) and `apps/frontend/app/domain/market.ts` (34 `{ ticker: ... }` entries, VEA the sole `active:false`) before editing. Exactly 1 line changed in 1 file per AC-4. **Found but NOT fixed (scope fence, flagging for po/architect):** the very next line (74) — "Active tickers (30): ..." — is ALSO stale (lists only 32 tickers, PLX missing entirely, mislabeled count 30 vs true 33). Same parent task (`FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58`) territory, different row; the handoff's own scope-out explicitly barred touching anything beyond line 72.

**CONCURRENT-PEER HAZARD OBSERVED, NOT ACTED ON:** `git status` showed `apps/mcp-server/src/interface/mcp/tools/system/watchlist.ts` as an UNCOMMITTED working-tree modification (551L vs 475L baseline, over the size-lint tolerance cap of 522L) plus 2 new untracked files, consistent with the live peer session (`007e33e4...`, `TASK_001-WATCHLIST-WRITE-THROUGH-INFRA`) mid-edit in this SAME shared tree. `size-lint-justification.sh --check` fails on that file right now — not my file, not my task, did not touch it. If it's still uncommitted/over-cap at push time it may block the shared pre-push gate for ANYONE pushing, independent of their own change; flagging for po/architect, not something I should fix.

---

## Session 2026-08-23T14:35:00Z — ready-queue sweep, 7 rows (session 7be6b4cd, router-dispatched)

**Shipped, all `ready[]`→`review[]` `next_agent=qa`, direct-commit (branch:null → QA verify-committed):** FIX-DONELANE-… Components 1/2/6 (`407afb6a4`, joint with the peer's 4/5); FIX-COWORK-GUARANTEED-SLOT-FIRER-NO-FAILURE-ESCALATION (`e806bcda1`, 53/53); FIX-CHEF-MARKER-KEY-ANCHOR-1 (`777ec912f`, 90/90); TASK-CRON-LIVENESS-PROBE-SCRIPT+TESTS (`2c9998018`, 87/87); FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-SCRIPTS (`cd0039432`, 116/0); FIX-ORCHSTATE-MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER (`1d6cad4f5`, 41/41).

**AN AC CAN BE WRONG ABOUT THE ENVIRONMENT — CHECK GROUND TRUTH BEFORE IMPLEMENTING IT.** The firer row's architect note, its handoff AC-1 and `docs/data/system-map.json` all named `TELEGRAM_BUG_CHAT_ID`. No such key exists in `.env` (real: `TELEGRAM_REPORT_BUG_CHANNEL_ID`); system-map has the wrong name for all three channels. Implementing AC-1 literally would have shipped an escalation that silently never fires — the exact defect class the row exists to close. Accepted both names, real first; reported the system-map drift rather than patching it (not my zone).

**A CROSS-CHECK MANDATED BY A BRIEF IS NOT CEREMONY.** Stage 1i's brief required re-running the jq SSOT (`bounded1-supervised-lane-report.sh`) on the same board. My JS port reported a violator the jq counted as 0 — an epic wrapper, handler-less BY DESIGN, which is why the report scopes its gating class `non-wrapper`. Caught pre-ship only because the brief said to run it.

**MUTATION-TEST A RANKING YOU CLAIM IS LOAD-BEARING.** The cron probe's whole thesis is O1 (process-absence) above O2 (transcript-mtime). Hoisting O2 above O1 on a scratch copy made the mutant answer LIVE for the 2026-08-23 corpse — reproducing the 8h10m outage — and F1 went red. That is the difference between a test that asserts the ordering and one that merely runs under it.

**`scripts/audits/` IS NOT UNIFORMLY READ-ONLY.** `guard-signal-type-coverage.sh` mints a backlog row through `orch-apply.sh` (registry self-filing fallback). Running it to *read* a convention mutated the live board. Committed the mint (legitimate detector output) and flagged it — an audits/ script with a write side effect and no `--dry-run` is a trap.

**DIAGNOSE A RED GATE BEFORE ADDING TO IT.** `devteam-dispatch-gate-satisfiability.sh` was exit-1 at baseline, 4 failures, none mine, each root cause verified not assumed: fixture rot vs a validator gate added AFTER the fixture (proved by replaying against the pre-change script); a branch testing RAW lane length while the gate uses the BLOCKED-excluding `wip_in_progress`; live-board contamination of a fixture that should have been isolated. Now 116/0.

**Zone discipline, 4×:** ANCHOR-2/3/4 and the ILC/probe call sites are under `docs/agents/`|`.claude/skills/` = agent-father's `commit_zone`. Re-routed ANCHOR-2/3/4's `next_agent` rather than breach; named the rest in each `review_note`. Net: ILC and the cron probe are shipped+tested but invoked on NO tick until agent-father lands their call sites — said explicitly so nobody reads REVIEW as "live".

---
