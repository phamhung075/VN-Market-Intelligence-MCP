# Developer — Notebook

**Last updated:** 2026-08-08 | **Cycle:** FIX-AUDITOR-B12-DOUBLE-INVOKE-EMIT-MARKER-LOSS (cross-service/, SLS dispatch, session 165f4245) — REVIEW, next_agent=qa

## Session 2026-08-08 — FIX-AUDITOR-B12-DOUBLE-INVOKE-EMIT-MARKER-LOSS (cross-service/, SLS dispatch, session 165f4245) — REVIEW, next_agent=qa

**Task:** PO-investigated row: a single B-12 (`data_stale: sbv_fx`) finding produced TWO `emit-audit-signal.sh` invocations 2s apart 2026-07-29T10:33:36Z/38Z (agent_signals #9885/#9886, signal_queue `sys-...13e5`/`sys-...7f95`, distinct ids ruling out a CAS retry). Only invocation 2's marker reached `$MARKERS_FILE`, so `audit-output-contract.sh` under-reported `telegram_sent=0` for a Telegram that DID fire, and its own `V1 VIOLATION` self-correction line was never pasted into the notebook. Constraint: do NOT touch `_e3_write_row`'s CAS-retry loop (evidence excludes it).

**Actions:** (1) **Check-id collision** — `docs/agents/system-auditor/flow/main.md:388` lists `B-12` in the per-source freshness pool while `:408` separately fixed `B-12` to "Rate limits: no source at 100%" — reassigned the rate-limit check to `B-14` with a distinct `rate_limit_saturated:` dedup_key prefix (never `data_stale:`), so the two conceptually different checks can never again collide on one id/dedup_key. (2) **Same-cycle idempotency guard, script-level** — new `_e3_already_emitted_this_cycle()` in `emit-audit-signal.sh`, called in `run_emit_signal()` BEFORE `_run_e1` (not inside `_e3_write_row` — that function is byte-identical, confirmed via `awk` extraction diff against HEAD). Given `--cycle-tag` (all main.md call sites already pass it) + a non-empty `dedup_key`, re-reads `.signal_queue.rows[]` for an existing row sharing both; if found, short-circuits the ENTIRE run (zero E-1, zero E-3) and prints `[emit-signal] SKIP-duplicate-invocation ...`. `_build_row_json` now also threads `dedup_key` onto every row (additive, `.passthrough()`, mirrors `audit_cycle_tag`/`payload_ref` precedent) so the guard has something to match against. (3) **Marker capture, made non-optional** — all 6 real `emit-audit-signal.sh`/`emit-dashboard-row.sh` call sites in main.md now pipe through `| tee -a "$MARKERS_FILE"` — capture is part of the bash command itself, not a separate "please also paste this" step. (4) **V1 VIOLATION, durable not prose** — found the 2026-08-06 Notebook-Write reorder (`FIX-AUDITOR-DURABILITY-STEP0B-DETECTION` §3b.1) now runs notebook compose+commit BEFORE `### OUTPUT-CONTRACT` even executes, so the old "paste the VIOLATION line into the notebook" instruction now describes a step that already ran — structurally stale, not just ignorable prose. Rather than re-order the whole section (disproportionate blast radius), `audit-output-contract.sh` now durably records every VIOLATION itself (`_record_violation_durable`, tmp+mv, `docs/data/auditor-output-contract-violations.json`, same idiom as the dedup-ledger) synchronously before returning — independent of any downstream narration. Added a MANDATORY `[OUTPUT-CONTRACT-VIOLATIONS]: NONE|<lines>` RETURN-template field (same "print NONE, never omit" discipline as `CONTRACT-CONTRADICTION`).

**Verify:** `emit-audit-signal.test.sh` 123/123 (108 baseline + T34-T37: T34 reproduces the EXACT B-12/sbv_fx incident shape — 2 invocations, same dedup_key+cycle_tag, 2s-apart timestamps — and proves `post_agent_signal` called exactly once + exactly one signal_queue row; T35 proves cross-cycle re-fire is unaffected; T36 proves the guard only engages with `--cycle-tag`; T37 proves the new `dedup_key` field). `audit-output-contract.test.sh` 56/56 (42 baseline + T5b + T14-T17: durable violations file created/accumulates/matches printed VIOLATION text side-by-side). Live sandboxed end-to-end demo (scratch dir, stubbed `mcp_call`, real script code) reproduced the exact incident and showed the fix: invocation 1 `OK`, invocation 2 `SKIP-duplicate-invocation`, `post_agent_signal` called ONCE, ONE signal_queue row, `[OUTPUT-CONTRACT] signals_posted=1 | telegram_sent=1 | signal_queue_rows_written=1`. `_e3_write_row` confirmed byte-identical (`awk` range-extract diff, empty). `bash -n` clean on all 4 changed scripts. No `apps/` TS touched — zone is scripts/flow-docs, `bun test`/`tsc` structurally N/A. Commits: `fix(cross-service): emit-audit-signal.sh + audit-output-contract.sh` (scripts+tests), `docs(system-auditor): B-12 collision + marker-capture + V1-VIOLATION enforcement` (main.md).

## Session 2026-08-07 — FIX-EMITSIGNAL-E3-RC1-OPAQUE-FATAL-DROPS-DETECTOR-FINDING (P1, zone `cross-service/`, dev-team dispatch, session f298ccf7) — REVIEW, next_agent=qa

**Task:** PO RAW-verified telegram BUG 4477/4478 (`[emit-signal] BUG: E-3 write/read-back failure rc=1`, check_id=A-21) as a TRANSIENT peer-write race, not universal breakage — but the transience itself dropped 2 detector findings off the board plane silently (`sys-20260807T014959-6afe`/`sys-20260807T015106-7023` never landed in `.signal_queue.rows[]`; the SAME actuator succeeded 72s later with zero code change). AC-1 diagnosability: `_orch_apply_invoke()` discarded orch-apply.sh's stderr, so the BUG telegram carried only `rc=1`. AC-2 correctness: `scripts/orch-apply.sh` exits 1 from THREE unrelated branches (validator/updated_at-stamp/conservation aborts) collapsed into one fatal-no-retry lane. AC-3: extend `emit-audit-signal.test.sh` with a transient-validator-failure-then-recovers case, RED at HEAD.

**Actions:** `_orch_apply_invoke()` now captures orch-apply.sh's stderr into global `ORCH_APPLY_STDERR` (previously left to fall through uncaptured to the parent shell's inherited stderr). `_e3_write_row()` greps it for the `"[orch-apply] ABORTED:"`/`"ERROR:"` reason line and threads it into both the `e3-write-failed`/`e3-cas-exhausted` marker and the anti-false-green BUG telegram (AC-1); routes a VALIDATOR-abort rc=1 into the SAME CAS-retry lane as rc=2 (its candidate is a live-file read that can race a peer mid-write) while a CONSERVATION/updated_at-stamp-abort rc=1 stays fatal, zero retry (its candidate is already live+exactly-one-appended-row — never a race, AC-2). Added T32 (validator-abort retries+recovers, no ABORT marker, row lands) and T33 (conservation-abort stays fatal, reason propagates into marker+telegram) to `scripts/emit-audit-signal.test.sh`, direct siblings of the existing T25/T26 rc=3 fixtures.

**Verify:** `bash scripts/emit-audit-signal.test.sh` → 108 passed/0 failed (baseline confirmed 98/0 GREEN at HEAD before any change; T32/T33 confirmed RED at HEAD via `git stash` on the production script alone, GREEN after). `bash scripts/db-integrity-history-append.test.sh` (sibling subprocess caller of this script) → 28/0, unaffected. `bash -n` + `shellcheck` clean on both changed files. No `apps/` TS touched (zone `cross-service/`, pure bash) — `bun test`/`tsc` structurally N/A. Environment note: this fresh worktree had zero `node_modules` and no local `docs/data/db-integrity-history.json` (both pre-existing, gitignored, worktree-isolation gaps, not code) — symlinked `node_modules`/`apps/mcp-server/node_modules` from the main checkout and wrote a local `[]` placeholder for the missing history file (same class of gap the prior cycle's Verify note above already documents). Commit `48925bb34` (fix: scripts/emit-audit-signal.sh + .test.sh). Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-4.md` STEP developer-S76.

## Session 2026-08-07 — FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER (apps/mcp-server/, dev-team RLC dispatch, session 165f4245) — REVIEW, next_agent=qa

**Task:** PM decomposition Task 1 of `FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD` (fix_spec(a)+(c)/AC1+AC3). PO RAW-verified 2026-08-06 that `task_release`/`task_heartbeat` on `orphan-signal:*` rows are unreachable no-ops (row's `owner_client_session` is always NULL — server-reaper owned — so no client session param can ever match the P1-FINAL sole-key rung). 3 subtasks: (1) `heartbeatTask()` optional `ttl_seconds`(60-691200)/`payload_patch`, (2) null-session ladder on `heartbeatTask`/`releaseTask` for orphan-signal rows (owner_agent + original_owner_client_session echo, live-session rows never match), (3) two-statement UPDATE pattern (not CTE).

**Actions:** Extended `heartbeatTask()`/`releaseTask()` (`coordinationStore.ts`) with an additive optional 3rd `options` param — byte-identical behavior when omitted (NFR-2; confirmed via the 2 pre-existing call sites, both still call with exactly 2 positional args). FR-1: ttl clamp mirrors `task_claim`'s bounds; `payload_patch` shallow-merges via a second UPDATE statement, reusing whichever rung (live-session vs ladder) matched statement 1 — malformed/absent existing payload handled non-fatally (EC-6). FR-2: ladder gated STRICTLY on the row's own `owner_client_session IS NULL` + `task_kind='orphan-signal'` columns (never a caller flag) so NFR-1's anti-theft invariant can't be bypassed by a caller supplying a coincidentally-matching `owner_agent`/echo against a live-session row — verified with a dedicated regression test on both functions. New `coordinationStore.test.ts` (first test file this module has ever had): 13 tests, confirmed RED (7/13) before implementation. Scope discipline: did NOT touch `coordinationTools.ts` (Zod schema exposure) — that's the separate, dependent Task 2, still READY/unclaimed; the new capability is implemented but not yet reachable via any MCP call until Task 2 ships + container rebuild (NFR-3). Zone note: `apps/mcp-server/` maps to `dev-mcp-server` in `system-map.json`'s coarse zone table, but this specific atomic task was dispatched with `next_agent=developer`/`dispatch_lane=developer` directly by dev-team's RLC — treated the RLC's explicit per-row dispatch_lane as authoritative over the coarse zone table for this coordination-primitives (cross-cutting, not one microservice's domain) task, consistent with developer's own flow scope statement ("apps/mcp-server/ root only... Implement MCP tools... that span multiple zones"). This session's granted tool set is Read/Edit/Write/Bash only (no MCP/Skill-invocation tool) — graphify step structurally skipped, documented in the handoff rather than silently omitted.

**Verify:** `coordinationStore.test.ts` 13/13 GREEN (confirmed RED 7/13 before implementation). `bun tsc --noEmit` 0 errors. `eslint` clean. Targeted merge-gate suite (18 files touching coordinationStore/lock mechanics) 307/307 pass, 0 regressions. Full `bun test` 45 fail/1 error of 15281 — matches documented standing baseline `FIX-MCP-SUITE-HEALTH-BASELINE` (`docs/policies/dev-standards.md:1364`), not caused by this change. Commits `d6c4e6006` (code+test), `c4b7b2060` (docs). Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-4.md` STEP developer-S78.
