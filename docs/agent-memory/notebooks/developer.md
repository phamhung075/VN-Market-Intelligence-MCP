# Developer — Notebook

**Last updated:** 2026-08-28T23:55:00Z | **Cycle:** FIX-BCTC-ANALYST-NOTEBOOK-COMPOSE-ACTUATOR (zone multi, dev-team Step 3 direct-execute)

## Session 2026-08-28T23:55:00Z — FIX-BCTC-ANALYST-NOTEBOOK-COMPOSE-ACTUATOR (zone multi, developer, P1 M, dev-team Step 3 direct-execute, session-123eed97)

**Implemented the architect brief (38b2c87cc) verbatim — wired the `scripts/notebook-compose.sh` actuator into `bctc-analyst` + granted scoped Bash.** §5a rewire (stage-log-notify.md): c<NNN> derived in bash from the file's own headings (never LLM-chosen, FIX-AGENT-NOTEBOOK-UUID-PROVENANCE), `date -u` timestamp (removed the No-Bash `fetchedAt`/`computedAt` fallback), model authors ONLY the ≤10L new-section body into a scratch file with a machine-built heading, ONE actuator call `bash scripts/notebook-compose.sh docs/agent-memory/notebooks/bctc-analyst.md <new-section-file> 3 60` (max-sections=3 per AC-2, section-cap=60 per AC-2a — NOT system-auditor's 150), branch on the `[notebook-compose]` marker (OK/WARN → §5d commit with the marker embedded in the message; ABORT → BUG telegram + skip commit + `[NOTEBOOK-GATE-ABORT]`, NEVER narrated-Write fallback; ERROR → BUG telegram + continue). Grant (`.claude/agents/bctc-analyst.md`): `tools:` += Bash + description declares the real write set, SAME commit (CI CHECK 2). Package Bash scope row added (`docs/agents/tools/package/bctc-analyst.md`): PERMITTED = date -u / grep·sort·tail·wc on the notebook path / the ONE compose actuator / notebook-path git; FORBIDDEN = docker, network, arbitrary writes, rm -rf, AND any docs/signals/ enumeration (drain-misread class must not reopen). main.md SIGNAL-FILE WRITE VERIFICATION block reworded (grant is scoped; verification-premise rule UNCHANGED).

**LESSON — a grant's own docs must not contradict the grant.** While rewiring, the same-file ESC-DISPATCH block still claimed "bctc-analyst has NO Bash (cannot run orch-apply.sh, cannot write orch-state.json)" — the conclusion survives (the scoped grant excludes orch-apply.sh/orch-state.json) but the premise was now false; reworded both it and the shared `debug-logger-protocol.md` line 23 ("Bash-less" → "scoped-Bash") in the same commit so the stale-claim class this task exists to close doesn't linger one file over.

**Verification:** VG-4 smoke/replay on a COPY of the live 2-section notebook + new section → `[notebook-compose] OK sections=3 dropped=0 lines=30 bytes=8470 direction=newest_first`, post `grep -c '^## c'` = 3 (script itself ZERO code change — header comment only). VG-3 `scripts/audits/agent-bash-grant-coverage.sh --check` PASS (bctc-analyst grant=demand, check1+check2 OK) + `--update` → baseline count 0 (grandfathered entry dropped per its own `_note` contract) + re-check PASS. `scripts/notebook-compose.test.sh` → 9/9 (comment-only script edit). `size-lint-justification.sh --check` PASS (0 offenders). Commit + board flip next. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S135.

## Session 2026-08-26T15:23:00Z — FIX-AUDITOR-NOTEBOOK-COMPOSE-COMMITMSG-MARKER-GATE (scripts/git-hooks/, developer, P0 S, session 036ceaf1, BOUNDED-1 idle-capacity pickup)

**Built this repo's first `commit-msg` git hook — `scripts/git-hooks/commit-msg`, PILOT-SCOPED to `docs/agent-memory/notebooks/system-auditor.md`** — the mechanical forcing function for the notebook-compose actuator's `[notebook-compose OK|WARN ...]` marker convention, per `docs/architecture-briefs/2026-08-26-fix-auditor-notebook-compose-tier1-adoption-gap-and-commitmsg-forcing-function.md` §Child A. Rejected the row's own named trap (a `pre-commit` guard reading `.git/COMMIT_EDITMSG`) without re-litigating it — corroborated independently first (`grep -c COMMIT_EDITMSG scripts/git-hooks/pre-commit` = 0).

**LESSON — smoke-test a spec's own regex against REAL corpus data before trusting it, even when the spec is otherwise fully worked-out.** The brief's literal marker regex (`\[[^]]*(OK|WARN)[^]]*\]`) does NOT match the real nested-bracket marker shape it itself lists as observed (`[[notebook-compose] OK ...]`, live commit `efe62d83d`) — a negated-bracket-class `[^]]*` cannot cross the inner `]`, so it silently stops short and the alternation never reaches "OK". Caught this by writing the T1 test case for that exact shape FIRST and watching it fail against the brief's own bash, before wiring `install.sh`. Fixed with a widened `\[([^][]|\[[^]]*\])*(OK|WARN)([^][]|\[[^]]*\])*\]` (tolerates one level of bracket nesting either side of the token), re-verified against all 4 real observed shapes plus negative controls (bare `[skip ci]`, OK-substring words TOKEN/BROKEN with no brackets at all — neither false-matches). Documented as a DEVIATION FROM BRIEF in both the row's `status_note` and the `dev-standards.md` CANONICAL pointer, not silently absorbed.

**Safety discipline (per the row's explicit gate — this hook runs on EVERY commit fleet-wide):** syntax-checked (`bash -n`) + smoke-tested in a disposable `mktemp -d` scratch repo BEFORE ever running `install.sh`, confirming exit 0 on every non-reject path first. Only then wired `install.sh` (`pre-push pre-commit post-commit commit-msg` loop) and re-ran it — confirmed via `ls .git/hooks/` that all 4 symlinks now exist.

**Verification:** `bash scripts/git-hooks/commit-msg-notebook-compose-marker.test.sh` → 5/5 PASS (T1 marker-present-all-4-shapes / T2 no-marker-warn / T3 no-marker-reject / T4 escape-hatch-under-reject / T5 non-notebook-touching-noop). `bash scripts/git-hooks/pre-commit.test.sh` → 15/15 unchanged, no regression from the `install.sh` edit. Commits: `16e2096bb` (hook + test + install.sh + dev-standards.md), `62da86b34` (board lane-move `in_progress[]`→`review[]`, `.head` reset idle, same orch-apply write). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S132.

**OPEN — AC-2 (live WARN observed on stderr for the next narrated notebook commit) is NOT yet satisfied.** No commit has touched `docs/agent-memory/notebooks/system-auditor.md` since the hook was installed (~2026-08-26T15:16Z) — the most recent one (`56f41ab1c`) predates install by roughly 40 minutes. Flagged explicitly in the row's `status_note` rather than claimed done; the next real system-auditor notebook commit lacking a marker (very likely within the next Tier-1/Tier-2/Tier-DATA cycle, given this file's own ~14/60 marker-adoption ratio in recent history) is the actual observation QA/next-tick should confirm.

## Session 2026-08-28T23:34:28Z — FIX-PREPUSH-SIZELINT-6-OFFENDERS (multi-zone apps/mcp-server + apps/pdf-extractor, developer, P1 M, dispatcher-spawned dev-team Step 3)

**Unblocked the fleet push (blocked since >= 2026-08-26, local main ~1046 ahead of origin): remediated all 6 pre-push size-lint offenders per the gate's own rules** — added a CURRENT size-justification header to each (the gate's sanctioned "carries a valid header → PASS" documented-debt rule), after per-file commit-history verification that the growth is legitimate reviewed feature work, not bloat. Rejected re-baselining all 6 via `--update` (ratchets the baseline + silently grandfathers the NEW offender) and rejected splitting production OCR/aggregation paths (cohesion loss, no lint benefit). Both zones already use this convention (financialFiguresRules.ts same dir; ordinal_grid.py 1210L same service).

**Per offender (actual L / declared L):** bctcScalarAggregator.ts 1207/1200 — cohesive DDD scalar aggregation, HPG fallback 28f8509fc; ocr_backends.py 690/690 — 3 backends + factory + recall-aware confidence helpers; ocr_orientation.py 235/230 — NEW file, 3 pure rotation fns + measured-evidence docstring (the core-regression-gate case — justified, NOT grandfathered); ocr_worker.py 579/580 — 4 ProcessPoolExecutor workers (picklable-module contract); page_rasterizer.py 251/250 — rasterizer + orientation-aware save helper; pek_engine_adapter.py 1299/1300 — PEK engine wrapper (never-import pdf_extract_kit.tasks contract).

**Verification:** size-lint --check PASS (0 offenders, 1457 scanned); size-lint-justification.test.sh 6/6 PASS; py_compile clean ×5; pnpm --filter vn-market check (tsc) clean; task-claim-owner-session-lint + tool-registry-parity PASS. Commit `b4e885a64` (6 files, +6L net, explicit pathspec). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S133. Row moved `backlog[]`→`review[]`, `status=REVIEW`, `next_agent=qa`, same orch-apply write (CANONICAL:SSOT-STATUSFLIP-LANEMOVE).

## Session 2026-08-29T00:05:00Z — HOOK-ENFORCEMENT-BASH-HEURISTIC-GUARD (scripts/agents-flow/, developer, P1 M, dev-team Step 3 direct-execute, session-123eed97)

**Built `scripts/agents-flow/orch-bash-direct-write-guard.mjs` (+ `.test.mjs`)** — the hook-enforcement-plane brief §6.2 rule-class-2 PreToolUse Bash heuristic guard for direct-bypass writes to `docs/data/orch/orch-state.json` that skip the mandated `orch-apply.sh` wrapper. Shipped Stage-1 OBSERVE-ONLY (default, per §8 staged rollout): would-flag commands append one JSONL line to the new git-tracked `docs/data/orch-bash-guard-would-block.log`, exit 0, never block. Stage-2 BLOCK (`ORCH_BASH_GUARD_MODE=block`, exit 2) is present but PO-gated — both Stage-1 criteria (deliberate violation fires the log; zero FP over >=20 real events) must be met first. Kill switch `ORCH_BASH_GUARD_DISABLE=1` per §8. Settings registration left to the user (config-admin boundary — `config_admin_flag` NOT flipped); CANONICAL pointer added in dev-standards.md.

**LESSON — "write-verb targeting the path" is an ASSOCIATION question, not a substring question.** The brief's literal regex reading (`>|>>|tee|mv|cp|sed -i`) false-positives on `cp orch-state.json /tmp/backup.json` (read-source copy) and `jq '.a | select(.x > 1)' orch-state.json` (quoted `>` in a jq filter). A tokenizer that binds each verb to its operand (redirect-op immediately before/glued; tee operand; mv/cp destination = last operand; sed -i file operand) is what makes the zero-FP criterion (Stage-1 promotion blocker) actually reachable — while still honestly documenting the obfuscation limit (`X="..."; > "$X"`) as a tested pass-through.

**Verification:** `bun test scripts/agents-flow/orch-bash-direct-write-guard.test.mjs` → 32/32 PASS (all write-verbs, allow-through incl. obfuscation-limit/read-source/quoted-`>`, Stage-2 gating, kill switch, fail-open error paths). Live smoke: deliberate `jq ... > orch-state.json` one-liner → 1 would-block log line; legit `orch-apply.sh` pipe / `git checkout` / `orch-validate.mjs` read → 0 lines (zero FP). Sibling `orch-state-hook.test.mjs` 21/21 unchanged. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S134. Row moves `backlog[]`→`review[]`, `status=REVIEW`, `next_agent=qa`, same orch-apply write. FLAGGED for PO: 4 origin-null sibling backlog rows (STAGE0-SMOKE-TEST, BASH-DIRECT-WRITE-GUARD-IMPL, DEGRADATION-LOG-INSTRUMENTATION, MCP-COORDINATION-ROUTE) overlap this batch's decomposition.
