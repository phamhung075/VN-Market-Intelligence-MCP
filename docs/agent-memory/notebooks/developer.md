# Developer — Notebook

**Last updated:** 2026-08-26T03:15:00Z | **Cycle:** FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS (session 036ceaf1, router hand-dispatch)

## Session 2026-08-26T03:13:48Z — FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE (cross-service/, developer, P0 S, session 036ceaf1, router hand-dispatch)

**Implemented the architect's ruled Axis D+C together, exactly as ruled — did not re-litigate the rejected exact-match axis.** Step 2.4's FR-3 block (`.claude/skills/dispatch-claim/SKILL.md`) was a bare `published:<slot_id>:*` prefix match with no notion of "which period" — 28h `MARKER_TTL` outliving a 24h/604800s cadence guaranteed a live prior-period marker at the exact moment the next fire dispatched (chef-evening = 100% of every fire). Added `CADENCE_SEC_BY_BASIS` (86400 daily / 604800 weekly, keyed on the live `docs/data/cowork-schedule.json` `.slots[].publish_date_basis`) as Axis D (age-bound: `AGE_SEC = now - claimed_at >= CADENCE_SEC` -> not a collision), landed together with Axis C (stale-owner: reuses the Phase A.5 `roster` already read, zero extra round trip) in the same FR-3 revision. `MARKER_TTL` constants and the prefix-match SHAPE are untouched — PO's rejected exact-match recommendation (telegram report 4861) stays rejected.

**The brief's fixture-design premise was wrong — the "existing 3-case harness" it says to extend never existed.** `scripts/agents-flow/cowork-dispatch-collision-probe.test.sh` was absent from the repo; confirmed via `git log --diff-filter=A` (no commit ever created it) and the live board (`TASK-COWORK-MUTEX-002`, the test-harness task, sits untouched in `backlog[]`). Created the file fresh instead of extending — same 4 cases the brief's §4 specifies (daily 4h-overlap no-block, daily within-cadence still-blocks, weekly 24h-overlap no-block, stale-owner no-block), plus 2 regression guards (unmapped-slot byte-identical pre-fix behavior, untouched exact `cowork-slot:` match) directly validating the two "unchanged" claims the SKILL.md comments make.

**LESSON — an architect brief's fixture-design section can assume prior art that doesn't exist; verify before extending.** `git log --diff-filter=A -- <path>` is the one-command check; would have wasted a cycle trying to `Read` a nonexistent file otherwise.

**No standalone `.sh` implementation exists for Step 2.4 to source into the test** — it is router-interpreted SKILL.md prose. Wrote a byte-faithful bash+jq mirror of the FR-3 pseudocode directly in the test file (documented as a manual-lockstep risk in its header) — same no-live-MCP-calls convention `cowork-tick-preflight.test.sh` already uses. Ran a negative control (pre-fix bare-prefix reimplementation) against the T1 fixture data to prove the test genuinely discriminates the defect, not just passes vacuously: pre-fix logic returns a collision on that exact data; post-fix logic (both SKILL.md and the test mirror) does not.

**Verification:** `bash scripts/agents-flow/cowork-dispatch-collision-probe.test.sh` -> 6 passed, 0 failed. `shellcheck` clean. Commit `4f72c694c` (`.claude/skills/dispatch-claim/SKILL.md`, new `scripts/agents-flow/cowork-dispatch-collision-probe.test.sh`, `docs/WORK.md`, decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-9.md` STEP developer-S129). Zero `apps/mcp-server` change (none needed — `claimed_at`/`publish_date_basis` both already live per the brief's own confirmed reads). Row moved `in_progress[] -> review[]`, `next_agent=qa`, `.head` reset idle, same orch-apply write — unblocks `TASK-COWORK-MUTEX-001` (review[], BLOCKED on this row) once QA confirms.

## Session  — FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS AC-3 (cross-service/, developer, P0 M, session 036ceaf1, review-lane SECONDARY-drain dispatch)

**QA's "permanently unrecovered" verdict was about live+archive absence, not blob absence — the parent blob (`git show 38c013342e^`) still held all 4 remaining victims.** Decided per row on evidence, no shortcuts: 3 restored, 1 retired.

**Restored to done_verified[]:** FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE (commits f51ed9ede2/e000e91f1 re-confirmed ancestor-OK + still live at HEAD; dropped a dangling `blocks` ref whose target archived via the normal non-defective evict path) and FIX-DEVTEAM-QADRAIN-HEAD-WRITE-CONDITIONAL (commit 9fe706fa2 re-confirmed, conditional `.head` guard still live). Both carry `verification.raw_probe` honestly reconstructed from their own historical QA status_note, never fabricated.

**Restored to done[]:** FIX-FB-WEEKEND-DEDUP-GATE (status DONE, no RC-VERIF gate; its own secondary_triage_result is its raw evidence; dedup gate spot-re-checked still present at HEAD).

**Retired (not restored) to archive[] as CANCELLED, superseded_by=CONTAM-10-WRITER-H:** FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0. Its own verify_note (written ~13h BEFORE the destructive evict) already said SUPERSEDED — a verbatim DONE_VERIFIED restore would need a raw_probe this row's history never captured; fabricating one is forbidden, so CANCELLED is the honest shape. CONTAM-10-WRITER-H confirmed live+DONE_VERIFIED with its own raw-probe record.

**LESSON — a supersession note is not a probe.** Restoring a DONE_VERIFIED row verbatim is only safe when the row's own history contains something reconstructible as raw_probe; when it only contains a "this was superseded" note, the honest move is CANCELLED + superseded_by, not a manufactured probe.

**Verification:** single orch-apply.sh write (all 4 rows + FIX-ORCHCOLDEVICT next_agent flip in one transform), commit 7320028e1. FIX-ORCHCOLDEVICT stays in review[], next_agent=qa for a final AC-3-focused re-verify (AC-1/AC-2/AC-4 untouched, already QA-confirmed). Lock task:FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS left held for the dispatcher.

(header timestamp correction: the empty "## Session  —" heading above is session start 2026-08-26T00:27:27Z — printf %s arg was dropped when authoring it, appending here since notebooks are append-only)

---

---

## Session 2026-08-26T05:01:53Z — FIX-QADRAIN-DONE-TO-QA-SCORES-BACKWARD-CONSERVATION-ABORTS-WHOLE-DRAIN + FIX-DEVTEAM-QADRAIN-SELECTION-BLIND-TO-QA-NOT-BEFORE-TIME-GATE + FIX-DEVTEAM-QADRAIN-SKIP-BRANCH-STRANDS-ALREADY-LANEMOVED-ROW-IN-QA (scripts/, cross-service/, developer, 3x P0, session 036ceaf1, router hand-dispatch per architect brief)

**Implemented all three exactly as designed by the architect brief (`docs/architecture-briefs/2026-08-26-qadrain-shared-hop-timegate-conservation-skipstrand.md`) — did not redesign, in the brief's own sequencing order.** (1) `scripts/orch-conservation-check.mjs` `LANE_RANK` decoupled from `FLAT_TASK_LANES` array position into an explicit tier map — `review`/`done` share tier 3 (both pre-QA staging feeding `qa[]`=tier 4), only `done_verified`=tier 5 stays terminal. This removed the live time-bomb: at >=2 done-origin `next_agent=="qa"` rows, every `orch-apply` write previously aborted, dragging down forward-moving `review[]` rows in the same batch. Verified `LANE_RANK` has exactly one call site repo-wide before touching it (grep). (2) New shared `gate_not_before_keys`/`is_gated_not_before($now)` predicate in `scripts/lib/devteam-eligibility.jq` (known-list: `qa_not_before`, `next_recheck_not_before`, `qa_new_window_earliest_d1_close`), wired into `scripts/devteam-review-claim-qa-drain.jq`'s candidate filter. (3) New mandatory Step 0d in `docs/agents/qa/flow/main.md`, before ANY jump target — the qa-flow convergent backstop that also covers router hand-dispatch, which the picker-side predicate structurally cannot reach. (4) New `scripts/devteam-qadrain-skip-revert.jq`, wired into both QA-Drain SKIP branches (idle-tick + head-decoupled) in one pass per call site (same call sites Sec1a's AC-7 touches) — reverses a stranded row via its `drain_source_lane` stamp, no `redispatch_count` charge, never piped through `|| true` (would reproduce `FIX-DEVTEAM-QADRAIN-PIPE-SWALLOWS-CAS-ABORT-NO-RETRY` on a new call site).

**Tests — a green suite alone would not have caught a silent regression here, so I built discriminating fixtures for each change.** New `scripts/test-orch-conservation-lane-rank.sh` (12/12): AC1 positive (2 done-origin rows land clean), AC2 NEGATIVE CONTROL (a genuine 2-row `qa[]->done[]` stale revert still aborts — proves the guard stays armed, not merely disarmed), AC3 (exact 03:22Z 2-review+2-done replay lands all 4), plus 2 non-goal controls (same-tier `review<->done` lateral never flagged even 2-at-once; the pre-existing single-row `qa[]->review[]` CHANGES_REQUESTED revert unaffected). Ran the AC1 fixture against the pre-fix HEAD copy of the `.mjs` file to confirm the harness genuinely reproduces the abort, not a vacuous pass (it does: exit 1 pre-fix, exit 0 post-fix, byte-identical fixture). `scripts/test-devteam-donelane-drain.sh` extended with I9-I13 (not-before gate: future/past-excludes-includes, `next_recheck_not_before` symmetric proof, absent-gate negative control, month+year-boundary pairs) and a new AC-3 fixture set for the skip-revert script (revert + re-pick-up-on-a-subsequent-claim-script-invocation, proving "picked up on a subsequent tick" without needing a live tick to elapse + 2 defensive no-ops). 60/60 + 12/12, all green.

**Known-list kept in sync in both places, verified by direct grep diff — this is the brief's own flagged drift risk.** `gate_not_before_keys` in `scripts/lib/devteam-eligibility.jq` and the mirrored copy in `docs/agents/qa/flow/main.md` Step 0d carry the identical 3-field ordered list.

**Doc-sync (not a new AC, matches this repo's self-consistency convention):** `docs/policies/dev-standards.md`'s CANONICAL "Lane-placement dimension" entry was asserting an unqualified "batch claims always move forward" — corrected to name the `review`/`done` tier-equivalence explicitly, with a dedicated CORRECTED-2026-08-26 paragraph, so it does not silently go stale again exactly as the `.mjs` file's own header comment did pre-fix.

**Verification:** commit `fe013f22f` (all 8 code/doc/test files, single commit per the brief's own "same-file siblings ... can share one hop" precedent) + board-state commit `732748dd1` (1 `orch-apply.sh` write: all 3 rows `IN_PROGRESS -> REVIEW`, `next_agent=qa`, `branch:null`, `commit:fe013f22f`, per-row `review_note`, `claimed_by`/`claimed_at`/`owner_client_session` cleared; conservation guard reported `lane_placement=clean`; `.head` already idle, untouched). Self-verified both commits at HEAD. `in_progress[]` holds zero of my rows (1 unrelated peer row remains). No `apps/mcp-server` TypeScript change — confirmed `LANE_RANK` has zero consumers outside its own file before editing.
