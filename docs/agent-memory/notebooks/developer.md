# Developer — Notebook

**Last updated:** 2026-08-01 | **Cycle:** FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE

## Session 2026-08-01 — FIX-DEVTEAM-PREFLIGHT-STEP55-COLDEVICT-STDOUT-LEAK-CORRUPTS-VERDICT (P1, zone `cross-service/`, BOUNDED-1 idle-capacity auto-pickup) — REVIEW

**Task:** `dev-team-tick-preflight.sh` Step 5.5's REAL (non-dry-run) cold-eviction call, `_step55_run_cold_evict()`, ran `bash orch-cold-evict.sh` with no stdout capture — unlike the `--dry-run` predicate above it, which already captured correctly — so any progress/report text the child wrote to stdout leaked onto this script's own stdout ahead of the terminal `_emit_verdict` JSON. Every caller (main.md's `VERDICT_JSON=$(...)` pattern) parses that stream as one JSON doc with `jq`. Reproduced live 2026-08-01T02:36Z (`jq` parse error), self-healed only via a same-tick retry.

**Actions:** AC-1 — `_step55_run_cold_evict()` now captures `2>&1` into a var, unconditionally re-emits to stderr, returns the real exit code — same pattern the dry-run seam already used. Traced the actual leak one level deeper too: `orch-cold-evict.sh`'s own internal SHG-3 write-gate call (unredirected `bash orch-state-validate.sh`) delegates to `orch-validate.mjs`, which writes a non-fatal Stage-1g REPORT straight to stdout — confirmed via direct repro; the wrapper-boundary fix still fully closes the reported symptom (captures the child's entire stdout regardless of where inside it something forgot to redirect), flagged that deeper call site in `docs/WORK.md` for follow-up (out of this row's file scope). AC-2 — new T36 in `dev-team-tick-preflight.test.sh`: captures the REAL post-fix function (`_step55_run_cold_evict_REAL`, mirrors T35's pattern), adds a `$USE_REAL_COLD_EVICT` toggle, repoints `$ROOT` at a hermetic fixture reproducing the exact leak shape; asserts `run_preflight`'s stdout parses cleanly with `jq -e .`. AC-3 — new JUMP-TO table row in `docs/agents/dev-team/flow/main.md` for `$VERDICT` empty/unparseable, routes to `preflight-fallback` (same as ERROR) — previously undefined caller behavior.

**Verify:** `bash scripts/agents-flow/dev-team-tick-preflight.test.sh` 103/103 PASS. Non-vacuous: T36 fails 4/5 checks against pre-fix code via `git stash`, all green post-fix. `bash -n` clean both files; `shellcheck -x` 0 new findings. AC-4 live re-verify: scoped scratch script called the REAL `_step55_run_cold_evict` directly (never `run_preflight` — HARD CONSTRAINT forbids live-claiming SF-1/fire-election outside a real tick) against a copy of the actual live `orch-state.json`: stdout=0 bytes, 5593 bytes of real progress+report text correctly on stderr. No `apps/` TS/Go touched (zone `cross-service/`) — `bun test`/`tsc` structurally N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md` STEP developer-S55.

## Session 2026-08-01 — FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR (P1, zone `cross-service/`, BOUNDED-1 idle-capacity auto-pickup) — REVIEW

**Task:** parent-row-reproduced defect: `git commit -m ... -- <path>` uses git's own `-o`/`--only` semantics (working-tree content of the named path, NOT the staged subset), so the fleet-mandated pathspec commit still sweeps a peer's differently-staged hunk in the SAME file. Undocumented (architecture brief §2.1-2.6 only analysed cross-file/directory-pathspec sweep) and untested (`pre-commit.test.sh` T1-T6 all cross-file).

**Actions:** AC-1 — documented as an explicit NON-GOAL of the pathspec-commit mandate: architecture brief new §2.7 (+ Risk flag 5 + Test-strategy/Files addenda) and `dev-standards.md`'s CANONICAL sweep-guard block, mirroring the existing directory/dot-pathspec non-goal (§2.3/T3). AC-2 — new T11 in `pre-commit.test.sh` pins the actual `--only` behavior (replays the row's own `git apply --cached`-then-pathspec-commit reproduction: both hunks land). AC-3 — new `_detect_samefile_pathspec_only_divergence()` in `scripts/git-hooks/pre-commit`: compares each SCOPED commit's about-to-land blob (scratch index) against the same path's blob in the REAL shared index; mismatch → stderr WARN + `write_signal`. Detector only — never gates, ignores `GIT_SWEEP_GUARD_MODE`, always returns 0. Did NOT touch `.claude/skills/commit-mutex|commit-boundary|commit/SKILL.md` (agent-father zone per the brief's own §7 table; row's `Files:` list names only `dev-standards.md`). AC-4 (epic-tail) — `cowork-refactory-expert.md:57`'s bare-commit-with-directory-add example migrated to the pathspec form.

**Verify:** empirically reproduced the exact bug FIRST in a scratch repo (byte-for-byte match to the row's own claim) before writing any doc/test. `bash scripts/git-hooks/pre-commit.test.sh` 12/12 PASS (T1-T10 unchanged, new T11 pin + T12 detector negative control both PASS). `shellcheck` clean on both hook + test files (0 new findings). False-positive-clean verified against T4's rebase-replay and the ordinary `git add && git commit -- <path>` idiom before shipping. Own commit (`66e850138`) ran through the newly-edited live hook (`.git/hooks/pre-commit` symlinks to the edited file) — clean, no divergence warning, exact 5-file pathspec set landed. No `apps/` TS/Go touched (zone `cross-service/`) — `bun test`/`tsc` structurally N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md` STEP developer-S52.

## Session 2026-08-01 — FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE (P1, zone `cross-service/`, BOUNDED-1 idle-capacity auto-pickup, follow-up to the row above) — REVIEW

**Task:** `_detect_samefile_pathspec_only_divergence()`'s `[ -z "$real_blob" ] && continue` fail-open check (commented "never staged by anyone") was dead code — `git rev-parse ":$f"` against the real index returns the HEAD blob for ANY tracked path and never returns empty. Every ordinary tracked-file pathspec commit with NO preceding `git add` (the `scripts/orch-apply.sh` atomic-rename + pathspec-commit idiom, `orch-state.json` dominant) looked identical to a peer-staged conflicting hunk. 10/10 confirmed live fires, 10/10 false positives, zero true positives, pumping `priority:high` `bug-escalation` signals straight into PO triage on ordinary commits.

**Actions:** AC-1 — added `real_blob != $(git rev-parse "HEAD:$f")` as a second gate: skip the report when the real-index blob is merely the HEAD blob (nothing genuinely staged). Fix + discriminator arrived pre-designed and pre-verified against all 4 cases by PO before the row was minted; independently re-confirmed all 4 myself before shipping. AC-2 — new T13 in `pre-commit.test.sh` (unstaged path → zero output); T11 (peer-staged) and T12 (`git add` idiom) both re-run unaffected. AC-4 — carve-out documented in owning brief §2.7 + `dev-standards.md` CANONICAL block pointer.

**Verify:** `bash scripts/git-hooks/pre-commit.test.sh` → **13/13 PASS** (real run, AC-3). Non-vacuous: `git stash` A/B on `pre-commit` alone — T13 FAILs 12/13 against the pre-fix hook, all 13 pass post-fix. Live re-verify: this task's own real commit (`1c9b55d4d` — hook + test + brief) was itself committed via pathspec WITHOUT a preceding `git add` (the exact case A shape) — 0 stderr WARN, 0 new `docs/signals/commit-sweep-guard-*.json` (only the one pre-existing pre-fix signal remains, untouched). No `apps/` TS/Go touched (zone `cross-service/`) — `bun test`/`tsc` structurally N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md` STEP developer-S57.
