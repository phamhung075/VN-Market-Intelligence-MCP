# Developer — Notebook

**Last updated:** 2026-08-29T02:15:00Z | **Cycle:** FIX-BCTC-DATA-GAP-FAMILY fix_round 1 (qa vc-changes redispatch)

## Session 2026-08-29T02:10:00Z — FIX-BCTC-DATA-GAP-FAMILY fix_round 1 (apps/mcp-server enricher/queue tests, developer, P1 L, qa vc-changes redispatch)

**QA returned vc-changes on 2 stale Arm-2 assertions (commit-introduced RED on main).** Both asserted the OLD `attempts < 6` bound that U1 deliberately reversed to `attempts <= MAX_ENRICH_ATTEMPTS+1` (attempts=6 re-eligible past grace; cap ≥7 excluded). Per QA verdict the CODE is correct (architect brief) — NO source change. Fix: moved the negative-cap fixture to attempts=7 in `FIX-BCTC-ENRICHER-STUCK-BACKLOG.test.ts` ("attempts >= 7 NOT selected") and `BCTC-1943-queue-reset-and-retry.test.ts` ("attempts < 7"), preserving each test's original churn-cap intent at the new boundary. The positive attempts=6 case was already covered by `FIX-BCTC-DATA-GAP-FAMILY-queue-liveness.test.ts` U1.1 — no duplicate added (mirrors the 240-bctc-full same-class update in 34c3a66ac).

**LESSON — full-battery bctc REDs can be STICKY test-pollution, not code regressions.** While verifying I hit 5 additional REDs (reparse AC-1/AC-2 + zeroextract AC-3 replay) that pass in isolation. Root-caused: `1975-bootstrap-enum-bctc-analyst-guard.test.ts` calls getDb() WITHOUT `DB_PATH=":memory:"`, caching the REAL `data/market.db` as the module-level `_db` singleton; every later file in the same bun process then gets the REAL DB despite its own `DB_PATH=":memory:"`, and its fixture writes PERSIST across runs → next run sees the leftovers (e.g. reparse's `seed-good-NGAYNOPNC` UNIQUE violation). Proven pre-existing: parent fbad14ced fails identically on its 2nd full-battery run. Cleaned the polluted rows from `data/market.db` (real dev rows + `data/live/market.db` untouched); battery green (1376/0). Out of this narrow fix's scope — flagged for ops/QA follow-up.

**Verification:** enricher + queue + 240-bctc-full batteries 50 pass / 0 fail; full bctc battery 1376 pass / 0 fail (clean dev DB); `bun tsc --noEmit` 0 errors; size-lint PASS. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S141. Row flips review[]→review[] (same lane), `next_agent=qa`, same orch-apply write (CANONICAL:SSOT-STATUSFLIP-LANEMOVE). Heartbeat: no MCP tool surface in this subagent session — dispatcher-held lock, release delegated (INV-GATEWAY-1). Graphify skipped (no Skill-tool binding — structural gap).

## Session 2026-08-29T00:45:00Z — FIX-COWORK-DISPATCH-AXISD-AGEBOUND-PERIODKEY (cross-service .claude/skills/dispatch-claim/, developer, P2 S, dev-team Step 3 direct-execute)

**Fixed Step 2.4 FR-3 Axis D in `.claude/skills/dispatch-claim/SKILL.md` per PO ruling (detector_defect 5d1ca94e).** Defect: the 08-26 Axis D staleness test (`AGE_SEC = now - claimed_at >= CADENCE_SEC`) is pure claimed_at arithmetic — it only tracks the period boundary if every fire claims at a non-decreasing time-of-day. Live case 2026-08-26T19:47:35Z: held `published:chef-evening:2026-08-25` (period key = YESTERDAY) had claimed_at 2026-08-25T19:53:25Z → AGE_SEC=86050 < 86400 → Axis D took the CURRENT-PERIOD branch and did NOT `continue`, blocking a structurally-prior-period marker (5m50s band; recurs weekly-scale, and inverts to a false-clear when the current fire runs later). Fix: PERIOD_KEY suffix extracted from the task_id (e.g. "2026-08-25") compared against CURRENT_PERIOD_KEY derived from the slot's own publish_date_basis (utc_date/vn_date → today's date; iso_week_period → current Mon–Sun range matching digest-predict's periodKey; vn_date_saturday_anchor → PERIOD_SAT per weekly-recap.md:47). Mismatch → prior/future-period → `continue`; AGE arithmetic kept ONLY as indeterminate-case fallback (never weaker than pre-fix). MATCH shape (prefix, not exact key) unchanged — PO's exact-match rejection stands.

**LESSON — "files[]" on a minted board row can point at the wrong file; trust the design brief + the live code, not the row's file list.** The row listed `docs/agents/cowork-team/flow/spawn-fanout.md` (inherited zone default), but grep proved spawn-fanout.md has no Axis D — the probe lives in `.claude/skills/dispatch-claim/SKILL.md` (2026-08-23 brief §5 table + 2026-08-28 LAYERC brief "touches Step 2.4 Axis D" + WORK.md all agree). Fixed the real file; corrected `files[]` on the lane-move write so QA reviews the right artifact.

**LESSON — when a probe's predicate becomes string-compared, fixtures must use date-relative keys, not hardcoded dates.** The 08-26 fixture hardcoded `2026-08-25`/`2026-08-26`/`2026-W34` marker keys — fine while only claimed_at was tested, but the period-key guard compares the suffix against TODAY's derived key, so fixed dates would silently change meaning as days pass. Rewrote the fixture to compute keys from NOW (YESTERDAY/TODAY/LAST-WEEK/PREV-SAT) + added the exact live band cases (T1b: YESTERDAY key claimed NOW-86050s inside the 5m50s band; T2b: TODAY key claimed NOW-86401s — period-key beats age, no false-clear; T3b weekly band; T7/T8 saturday-anchor basis) + 3 negative controls proving the pre-fix age logic BLOCKS/CLEARS the new fixtures (non-vacuous).

**Verification:** `bash scripts/agents-flow/cowork-dispatch-collision-probe.test.sh` → 14/14 PASS (T1..T8 + NEG-1..3). jq strftime period-key derivations cross-checked against bash `date` ground truth (utc/vn/iso/sat all match). No apps/mcp-server change. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S139. Board flip to review[] next_agent=qa next. Graphify skipped (no Skill-tool binding — structural gap).

## Session 2026-08-29T01:30:00Z — FIX-COWORK-FANOUT-LOAD1MIN-COMMA-LOCALE-PARSE (scripts/agents-flow + docs/agents/cowork-team, developer, P2 S, dev-team Step 3 direct-execute)

**Fixed the cowork fan-out gate's comma-locale LOAD_1MIN parse (spawn-fanout.md Step 5.1, cowork-fire 30d983ac).** Defect: the inline `awk -F','` split truncated comma-locale uptime to its integer part ("2,19" → "2"; live on this host: "4,22" → "4"), and per-tick LLM improvisations with `tr -d ','` produced "219" > 2*CORES → silent NORMAL→DEGRADED inversion (max_parallel 4→2). Fix per row status_note ("scripted gate (scripts/agents-flow/)"): NEW `scripts/agents-flow/cowork-load-probe.sh` — extracts the first token after "load average(s):", normalizes ONLY the decimal comma → dot; emits "4.22"/"2.19", fail-loud exit 2/3 with empty stdout on unparseable (caller treats load as unknown — NFR-P1-3 fail-safe). Deliberately NO LC_ALL=C pinning (po-triage occurrence-6 remedy note: pinning + comma-splitting = concatenation garbage). spawn-fanout.md L199 points at the script ("Edit the script, never a second copy of the parse"); Step 5.2's re-probe line left untouched per critical-region boundary — it inherits via the "same as Step 5.1" pointer.

**LESSON — one comma-locale value is TWO failure classes: truncation (4,22→4 via -F',') AND concatenation (2,19→219 via tr -d ','); the only parse that survives both is token-extract-then-normalize-decimal.** Also: validate locale fixes against the LIVE host shape first — this machine prints macOS "load averages:" space-separated, not Linux "load average:" comma-separated; fixtures must cover all four shapes (macOS/Linux × dot/comma) or the "2,19" case passes while the host's own shape still truncates.

**Verification:** `bash scripts/agents-flow/cowork-load-probe.test.sh` → 20/20 PASS (exact incident "2,19"→"2.19" never 219; all 4 locale/platform shapes; integer/zero/multi-digit; fail-loud; real-host smoke); live cross-check probe == LC_ALL=C ground truth (5.10 == 5.10). Heartbeat via mcp-call.sh → ok:false/expires_at:0 — expected: lock held by the dispatcher session (INV-GATEWAY-1); release delegated to dispatcher. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-10.md` STEP developer-S140. Row flips backlog[]→review[] next_agent=qa, same orch-apply write (CANONICAL:SSOT-STATUSFLIP-LANEMOVE); files[] extended with the 2 new scripts (AXISD precedent). Graphify skipped (no Skill-tool binding — structural gap).
