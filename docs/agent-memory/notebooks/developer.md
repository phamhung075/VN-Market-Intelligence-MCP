# Developer — Notebook

**Last updated:** 2026-08-08T10:20:00Z | **Cycle:** FIX-AUDITOR-A30-DISCRIMINATOR-CRASH-CLIFF-SCORED-AS-RECLAMATION-DIP (cross-service/, router SLS-dispatch, session 165f4245) — implementation landed, REVIEW, next_agent=qa.

## Session 2026-08-08T10:20:00Z — FIX-AUDITOR-A30-DISCRIMINATOR-CRASH-CLIFF-SCORED-AS-RECLAMATION-DIP (cross-service/, router SLS-dispatch, session 165f4245)

**Task:** recurring-failed-fix (first recorded 2026-08-05 in memory `feedback_a30_discriminator_crash_cliff_misscored_as_reclamation_dip`, not fixed then; re-fired for real 2026-08-07T05:56:47Z when the mcp-server container actually died mid-window — RestartCount 0->1, OOMKilled=false, ExitCode=0 — and `scripts/audits/verify-a30-mcp-memory-reclamation.sh` still returned FOLD). Landed all 5 fixes from `po_amendment_20260807T0610Z`, single file in scope.

**Design:** (a) RestartCount/StartedAt/OOMKilled/ExitCode/FinishedAt now re-read AFTER the sample loop, not just before; any restart/started delta forces `ESCALATE` naming both before/after values. (b) removed the `dips==0` hard-gate from both percentage escalation branches — `min_pct>93` is now ungated (this alone reproduces the exact bug shape: "sustained 99% with one 0.6pp jitter" now escalates); the OLD `max_pct>97 && dips==0` branch would have become a NEW single-spike false-positive generator once simply ungated (a lone benign ~97.8% GC peak would trip it), so replaced it entirely with a jitter-tolerant `median_pct>97` check instead — caught this via a dedicated negative-control test (T3, single-spike-then-smooth-reclaim) before shipping, not by inspection alone. (c) any single-step drop >40pp is now a `discontinuity` (own counter), structurally excluded from `reclamation_dips`. (d) `ExitCode==0` + a `FinishedAt` delta between before/after reads is accepted as a death signature alongside `OOMKilled==true` (this fleet's documented OOMKilled-less memory-death signature). (e) the VmHWM-vs-VmRSS tautology narrative ("HWM >> RSS proves reclamation") is gone — VmHWM is now compared against the cgroup memory limit (`HostConfig.Memory`) and its own prior value across the window; advancing + pinned at >=90% of cap is a new, genuine `ESCALATE` signal (previously VmHWM was purely narrative, never wired into the verdict at all).

**Test-infra bug caught mid-authoring (own notebook-worthy lesson):** refactored the script into a sourceable `run_a30_reclamation_check()` function (BASH_SOURCE guard, same idiom as `db-integrity-mount-drift-check.sh`/`auditor-tier1-probe.sh`) so it could be tested via a stubbed `docker()` shell function. First test-harness draft reused the SAME variable names as the script's own internal `local` vars for the stub config (e.g. a test var named `OOM_BEFORE`) — bash `local` is dynamically scoped, so once `run_a30_reclamation_check` declared its own `local OOM_BEFORE`, every nested call (including the `docker()` stub) silently saw the (empty) function-local instead of the test's intended value. 2 of 13 test cases silently no-op'd (always read the `:-default` fallback) until every stub var was renamed with a `STUB_` prefix that collides with nothing the script declares — caught by manually inspecting raw JSON output, not by the test's own pass/fail signal (both cases still "passed" against the wrong assertion until I actually looked at the printed state block).

**Verify:** `scripts/audits/verify-a30-mcp-memory-reclamation.test.sh` 13/13 PASS, both PATH bash and macOS system `/bin/bash` 3.2.57. T1 = the board's own mandatory `verification_gate`: exact 2026-08-07T05:56Z 12-sample series (99.01,98.93,3.05,...) + RestartCount 0->1 -> `verdict==ESCALATE` (hand-confirmed `FOLD` against the pre-fix verdict-chain logic first, matching the live incident, before writing any of the 5 fixes). Live-verified against the real running `vn-market-intelligence-mcp-mcp-server-1` container (3 probes/1s) — valid JSON, verdict=FOLD, container genuinely healthy (restart_count stable at 3, VmHWM well under the 3GiB cap). No `apps/` TS/Go touched — `bun test`/`tsc` structurally N/A.

**Closeout:** Doc-consumer sync required (JSON field names + 2 of 3 reason substrings changed) — `docs/agents/system-auditor/flow/tier1-probe.md` § A-30 override (parsed-field list + reason->severity mapping widened 3->6 CRITICAL substrings, dropped the stale dangling-phantom-row citation), `docs/agents/cowork-team/flow/pressure-read.md`, `docs/agents/system-auditor/probe.sh` comment — all same commit (`a69a79b3d`). Board row `backlog[]` -> `review[]`, `status=REVIEW`, `next_agent=qa`, `commit_sha` + `review_note` + `absorbs_confirmed` added, via `orch-apply.sh` (commit `ad719252a`) — `.head` deliberately left untouched (pinned to an unrelated concurrent peer task, not this row's `active_task_id`); post-write id-set + full-content diff (excluding the one moved row) both empty, confirming the write was surgical despite git's large line-diff artifact from the array shift. Also absorbs the never-minted `FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE` phantom dependency — fix (e) covers its scope, unblocking `FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE`'s `depends_on` once this row reaches DONE_VERIFIED.

---

## Session 2026-08-08T08:10:00Z — FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE (cross-service/, session 165f4245)

**Task:** implement the already-ratified plan-only spec (`docs/handoffs/FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE-spec.md`, commit `220c5158a`), landing live edits with PO's `po_goahead_20260808T075740` bundle correction (all 4 sites, not the spec's original 2-3).

**Design:** Re-verified every load-bearing number live before editing rather than trusting citations: `jq '.project.infrastructure.vps.routes | length'` = 8, source_id list byte-identical to spec's; PO's fleet regex `'all [0-9]+ routes|([0-9]+ geo-blocked routes)'` over `docs/agents/` returned exactly the 3 hits PO's ratification named (main.md:407, audit-dimensions.md:26, init.md:17) before editing. Applied main.md:407's route-set/coverage-table/verdict-logic/PASS-gate replacement verbatim from spec §2; audit-dimensions.md:26 and init.md:17 trivial 1-line swaps per spec §7.1 + PO's correction; OH-3.2 (`dim-oh3-auditor-blindspot.md:24-31`) redesigned to the 2-leg SSOT-vs-doc-hardcode sweep per spec §5, WIDENED the Leg-2 grep target list to all 3 doc files (spec's own diff only named main.md + audit-dimensions.md) — otherwise the new regression guard would never catch a future drift on init.md, the exact site PO's correction exists to cover.

**Self-inflicted-bug caught and fixed:** my own size-justification header delta notes (added per this repo's convention, describing the retired literal) quoted the exact retired phrases ("all 7 routes", "7 geo-blocked routes") verbatim — live-running the new OH-3.2 Leg-2 regex against my own edited files caught 3 self-matches before commit (the regex doesn't distinguish live-checked prose from changelog prose describing history). Re-worded to `all [7] routes` / `([7] geo-blocked routes)` (digit still readable, regex-inert) — re-ran both the new Leg-2 regex and PO's fleet regex, 0 hits, confirmed clean.

**Verify:** `grep -rnoE 'all [0-9]+ routes|([0-9]+ geo-blocked routes)' docs/agents/` → 0 hits post-landing (was 3 pre-landing, matching PO's own count). Fence-balance and line-count checks on all 4 files pass (main.md 1299L unchanged net after header-note-only-growth math, audit-dimensions.md 200L unchanged, init.md 169L unchanged, dim-oh3 71L). Acceptance (5) cross-reference note (`sbv_fx_b12_rootcause_crossref_20260808`) already present on `FIX-AUDITOR-B12-DOUBLE-INVOKE-EMIT-MARKER-LOSS` from the plan-only pass — confirmed live, no further action needed.

---

## Session 2026-08-08T04:56:44Z — TASK-COWORK-SIGNAL-NAMING-CONTRACT (cross-service/standards, SLS direct dispatch, session 165f4245) — REVIEW, next_agent=po

**Task:** plan_only + supervised (inherited from parent FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING — "no code ships without parent re-adjudication by PO"). Task 4/4 of the parent's Phase-1 decomposition — LAST child: specify FR-5 (`docs/standards/mcp-tools.md` Naming Contract subsection, the two ticker-keyed/dish-keyed file families + the WINDOW_KEY invariant) + FR-6 (`docs/agents/dev-team/flow/drain-signals.md` one-line fingerprint-neutrality clarification). Deliverable is a spec document, not a doc edit — neither target file touched.

**Design:** Read the row + parent row's full decision history (BA spec, architect brief, both PO sign-offs, all 3 sibling task specs already in `review[]` for pattern consistency) plus architecture brief §6 (FR-5), §7 (FR-6), §8/§9/§10. Live-re-verified both target files' exact current state before writing anything, rather than trusting the brief's cited line numbers: `mcp-tools.md`'s `## Signal Bus — Naming Contract` section confirmed spanning lines 148-166 exactly as the brief cited (148-164 for the existing block) — zero drift found, brief's proposed subsection text reproduced verbatim. `drain-signals.md`'s §0a-1 fingerprint description pinned to the actual live line (93, not stated by the brief) as the insertion point for FR-6's one-liner, ahead of the existing match/no-match sub-bullets (94-97). Independently re-derived (not re-cited) `scripts/agents-flow/drain-signals.js`'s fingerprint formula at the actual `crypto.createHash` call site (lines 173-174) — traced `from`/`type`/`payload`/`createdAt` to their JSON-field origins and confirmed no filename-bearing variable (`base`/`dest`/`fp_path`) ever reaches the hash — genuinely zero code change required, AC-3 closes as a confirmed NO-OP.

**Closeout:** Spec written to `docs/handoffs/TASK-COWORK-SIGNAL-NAMING-CONTRACT-spec.md` (commit `d24138296`). Board row moved `in_progress[]` → `review[]`, `status=REVIEW`, `next_agent=po`, `owner=po`, `supervised`/`plan_only` preserved unchanged, `spec_doc` pointer + `developer_review_note` added, via `orch-apply.sh` (commit `4afd19837`) — `.head` reset to idle in the same write, validator/conservation-check both passed clean (task_total 778→778). Post-write duplicate-scan (`paths(scalars)` search for `id=="TASK-COWORK-SIGNAL-NAMING-CONTRACT"` across the whole board) confirmed exactly ONE copy of the row exists, correctly placed in `review[]` — ran this check because the raw commit diff's line-based text matching made the change look larger/stranger than the semantic edit actually was (66/56 line diff for what is structurally one array-element move plus one unrelated concurrent peer-sweep promotion that landed on the live file between read and apply, same pattern as prior sibling tasks in this decomposition). `mcp__gateway__call_tool` NOT present in this session's own tool bindings (checked directly, not from memory) — used the Bash-bridge fallback (`scripts/agents-flow/mcp-call.sh`) per `docs/standards/gateway-call-contract.md` §6b for `task_release`; see decision journal / closing summary for the exact returned result.

---
