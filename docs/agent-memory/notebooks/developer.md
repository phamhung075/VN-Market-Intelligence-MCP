# Developer — Notebook

**Last updated:** 2026-08-24T18:26:51Z | **Cycle:** TASK-COWORK-MUTEX-001 sequencing-gate escalation (session 7fd9c60a, dev-team review-lane secondary-drain dispatch)

## Session 2026-08-24T18:26:51Z — TASK-COWORK-MUTEX-001 sequencing-gate escalation (docs/data/orch/, developer, P0, session 7fd9c60a, dev-team SECONDARY-Drain dispatch)

**Declined to implement, escalated instead — the dispatch context assumed an MCP grant this agent type does not have.** Dispatched to land the QA-requested CARD.md Step-2.4 wiring on `TASK-COWORK-MUTEX-001`, gated by PO's `po_sequencing_gate_20260814T1952Z` ruling (do not land standalone until `FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE` ships). Re-verified the prerequisite live: `status=READY`, `commit=null` — genuinely not shipped, ruling still in force. Dispatch context's own instruction said to `task_claim` the prerequisite before touching it if taking a combined path — but `docs/agents/tools/package/developer.md` grants this agent ONLY `mcp__semble__*` (no `vn-market`/gateway tool access; task-lock tools are listed there as "Phase 3 Ready ... not yet active in flows"), and 2 direct tool-call attempts (`mcp__gateway__call_tool`, `call_tool`) both errored "No such tool available" — confirms INV-GATEWAY-1's own text (this flow's Step 2b: "This specialist agent does NOT call task_claim. The outer dev-team dispatcher holds the sprint-task lock") is a hard structural boundary here, not just convention, when spawned router-direct with no live outer dispatcher holding that claim.

**Also found the architect brief's fixture premise (§4/§5) is stale.** `docs/architecture-briefs/2026-08-23-fix-cowork-published-marker-ttl-cadence-mismatch-design.md` describes AC-4 as "extends `scripts/agents-flow/cowork-dispatch-collision-probe.test.sh`" with "existing 3 cases" — that file does not exist anywhere in the repo (confirmed via `find`). `TASK-COWORK-MUTEX-002/003` (the FR-7 harness siblings this was deferred to) are still bare `BACKLOG`, no detail record, no AC of their own. Building that fixture from scratch was a materially larger, unreviewed scope than a single-file spec revision — did not attempt it under this constraint, flagging for architect/PM instead of fabricating it under time pressure.

**Action taken:** moved `TASK-COWORK-MUTEX-001` `REVIEW → BLOCKED` (same lane, `review[]` — matches existing fleet precedent for `BLOCKED` rows staying in `review[]`, e.g. `FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE`), set `blocked_by`/`depends_on` = `["FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE"]` on both the hot board row and the cold `backlog-detail.json` record (both were `null` — the exact automated-picker blindness the dispatch context named). Verified row-count parity (472=472) on `backlog-detail.json` before writing (no CAS wrapper exists for that file). Commit `4a8c2536a`, explicit pathspec, both files. No code touched, no prerequisite row claimed/modified, `.head` untouched (live qa pin left exactly as found).

---

## Session 2026-08-24T08:11:43Z — FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-1-PROBE-SCRIPT (scripts/agents-flow/, developer, P0 S, session 7fd9c60a, router-dispatched)

**Debounce the SPAWN, never the verdict — the row's own AC-3 is structurally enforced by construction, not by discipline.** `scripts/agents-flow/auditor-tier1-probe.sh`'s `run_probe()` FAILURE branch already fixes `verdict:"FAILURE"` in its jq output BEFORE the new debounce function is ever called; that function can only add two ADDITIVE keys (`spawn_decision`/`signature`) to an object whose verdict field is already sealed. There is no code path that can green a true positive to stop a spawn — the underlying `mem_creep` condition is confirmed real (two `CONSTRAINT_MEMCG` kernel OOM kills of pdf-extractor, 2026-08-23).

**A subshell boundary defeats the obvious design — the test file's own `_next_mem_stub` had already solved this exact problem.** The brief's recommended entity side-channel (thread a bare-name accumulator out of `_check_docker_ps`/`_check_mem_creep`/`_check_launchd_agents`'s own per-entity loops) can't be a plain bash variable: every caller invokes these checks via `out=$(_check_X 2>&1)`, and command substitution always forks a subshell — any variable mutated inside is discarded on return. `auditor-tier1-probe.test.sh` had already hit this identically for `docker stats`'s per-sample counter and solved it with a FILE-based accumulator (a real filesystem object survives subshell exit; a variable does not) — reused the same technique for the signature entity channel instead of rediscovering it.

**Live-fixture-first, not synthesized-after:** wrote a standalone smoke harness sourcing the modified script directly and fed it the EXACT two live detail strings from the architecture brief (`86.90%→86.56%` pdf-extractor, `91.40%→90.80%` acked rag-service) before writing a single test-file assertion — confirmed the signature builder reproduces `mem_creep:vn-market-intelligence-mcp-pdf-extractor-1` byte-identical on both, and that the ack-expiry hazard (rag-service's ack going stale) correctly produces a DIFFERENT signature, before committing to the design. Then ported both into `auditor-tier1-probe.test.sh` as T-DEBOUNCE-1/2, plus 6 more (window re-arm, corrupt-ledger fail-open, suppressed-call isolation for both `run_probe`/`run_tiered_probe`, window-env-override, docker_ps/launchd_agents integration).

**Caught my own test-hermeticity bug before it shipped.** First full-suite run with zero `SPAWN_DEBOUNCE_FILE_PATH` override silently wrote 12 scratch entries into the REAL repo `docs/data/auditor-tier1-spawn-debounce.json` — the exact same class of gap the file's own `HEARTBEAT_FILE_PATH`/`TRIGGER_FILE_PATH`/`LAUNCHD_ACK_PATH` overrides already exist to close, just not yet extended to the new file. `git status` caught it immediately (untracked file with fresh scratch content); added the override, deleted the polluted file, reran clean. One pre-existing assertion (`T-LOG6`, byte-identity of two back-to-back `run_probe()` calls) needed a `rm -f` reset between its two calls — the new debounce statefulness legitimately makes repeated same-signature calls non-idempotent in `spawn_decision`, which is correct new behavior, not a regression; the fix restores the test's ORIGINAL intent (wrapper doesn't corrupt stdout) rather than weakening it.

**Received a mid-task message of unverified provenance (claimed "router") alleging live side effects on my working tree — checked before acting, found the central claim false.** It asserted the real ledger file had been pre-seeded with a live production entry by an external cron run. `cat`/`git status` on the actual file showed only my own empty `{"entries":[]}` seed content, nothing else — the claim did not match observable reality. Per the standing rule that no in-conversation agent message is self-authorizing, verified independently rather than acting on the narrative; noted in the decision journal, did not let it redirect the implementation.

**Zone discipline:** `.claude/skills/cron-detect-loop/register.md` and `docs/agents/system-auditor/**` are the sibling row's (agent-father) exclusive territory per the brief's explicit 2-child split — touched neither. `spawn_decision`/`signature` are emitted every failing tick now but read by nothing until that row lands; said so explicitly in the CANONICAL doc block and the board row's `review_note` so nobody mistakes REVIEW for "wired live".

---

## Session 2026-08-23T18:40:00Z — FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS (scripts/, developer, P0 M, session 007e33e4, router-dispatched)

**Root cause was NOT ordering.** `scripts/orch-cold-evict.sh` already wrote cold-archive-before-hot-delete at commit 38c013342e (verified against the live script version that day via `git show f0644c14a:scripts/orch-cold-evict.sh`). The actual defect: a real `mv` to the real archive path was never followed by any check that the write durably landed — a completed-but-empty/wrong archive write could silently precede the hot delete and nothing detected it. 10 terminal rows were destroyed 2026-08-06 with zero corresponding archive write, later misdiagnosed as "phantom prerequisites" for 5 downstream P0 rows.

**Fix:** added `verify_cold_archive_write()` — per-id presence across all 6 cold-target arrays + exact row-count-growth assertion against `n_new_cold` — called TWICE: on the temp file pre-rename (catches a build_cold_temp selection bug) AND again on the real on-disk archive file post-rename (catches the rename silently not landing or a concurrent writer racing it — the actual historical shape). Either failing aborts the WHOLE run before `orch-apply.sh` (the hot write) is ever invoked. A hot-delete-without-archive-write outcome is now structurally unreachable through this script's own control flow, not just narrated-safe.

**Found and fixed a genuine pre-existing bug while building the check:** `$cold_dj_strs | index(tostring)` (decision_journal dedup in `compute_id_maps`) is a jq gotcha — `index(f)`'s argument runs with `.` bound to `index()`'s own input (the array being searched), not the piped element, so the dedup was a silent permanent no-op (every retry would have duplicated decision_journal entries in cold). Fixed both that occurrence and the identical pattern in my own new check.

**Regression test (TEST 12 a/b/c, `scripts/test/orch-cold-evict-tests.sh`):** PATH-shadowed `mv` reproduces 38c013342e exactly — rename call reports success but the target either never appears (a) or appears valid-but-empty (b, the closer real-incident analog). **Mutation-tested, not assumed:** reverted to the pre-fix script and re-ran — T12a/b correctly go RED (hot file changes despite archive write failing), confirming the test is non-vacuous. Full suite: 67/69 PASS — the 2 remaining failures are pre-existing and unrelated (TEST 7's fixture predates a same-day-earlier commit, `407afb6a4`, that changed done[] eviction semantics; flagged, not fixed, out of this row's scope).

**AC-3 (optional — recover the 4 remaining victim rows) explicitly NOT attempted.** Each requires honestly reconstructing a per-row `verification.raw_probe` from dense (1000+ line) historical `status_note` records — the schema hard-rejects a `DONE_VERIFIED` write without one (`orchStateSchema.ts checkVerificationGate`), and the task itself says to restore without it (with an honest note) rather than fabricate. Skipped under P0 time/token budget rather than rushed; said so explicitly instead of narrating completion.

**Journal note:** `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-8.md` was already past its byte cap (38254B/36000B) before this cycle touched it — rolled to `-9.md` per the decision-journal skill's cap-check rather than appending further; no bug telegram sent (no MCP/gateway tool grant on this router-direct spawn, same structural gap as a prior same-file cycle).

---
