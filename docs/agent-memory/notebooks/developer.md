# Developer — Notebook

**Last updated:** 2026-08-25T13:55:00Z | **Cycle:** FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID (session 036ceaf1, dev-team incident-lane consumer dispatch)

## Session 2026-08-25T13:55:00Z — FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID (cross-service/, developer, P0 M, session 036ceaf1, dev-team incident-lane consumer dispatch, po_expedited_at)

**AC-1 first, and it inverted the fix: the "forged writer id" premise is REFUTED.** The ntg-* rows carrying the frozen ts `2026-08-24T00:00:00Z` came from the REAL production writer, driven by `apps/mcp-server/src/__tests__/CCATO-MCP-T5-USECASE.test.ts` — 5 of its cases reach a FAIL verdict overriding NEITHER `writeSignalsFn` NOR `orchStatePath`, so `runNarrativeTruthGate` falls through to `DEFAULT_ORCH_STATE_PATH` (the LIVE orch-state.json) with the suite's injected clock. Every `bun test` run appended exactly 6 rows to production. Arithmetic is decisive: those 5 cases yield `returned_value` {60, 61, 61, 62.1, 62.1, "not found in database"} = 1/2/2/1; PO measured 12/24/24/12 over 12 batches. Claim text, `agent_id:"chef"` and the ts are verbatim T5 constants. So "NEITHER sanctioned engine can have produced them" is FALSE, and the "7 inverted verdicts" are T5's deliberate missing-`tool_null_markers` case.

**LESSON — a frozen/impossible timestamp in production data points at a TEST RUNNER before it points at a forger.** Injected clocks are the only thing here that legitimately emit a second-identical ts across hours of wall clock. Grep the test tree for the literal timestamp FIRST: `grep -rn "<frozen ts>" apps scripts` found it in seconds, in the same file that held the claim text and the agent id.

**LESSON — replaying a clock-skew guard needs a PER-ROW emission clock.** My first replay applied one wall clock to rows emitted 10h apart and produced 2 false quarantines against genuine live rows — it read exactly like an over-broad gate. Reconstruct each candidate's own emission time from git before scoring it.

**Did NOT ship AC-2 (producer auth in `orch-apply.sh`) or AC-8 (collapse live rows), both premise-refuted; reported as not-done, not narrated as done.** `_updated_by:"narrative-truth-gate"` was never forged — the TS writer is a second sanctioned producer that legitimately stamps it, so that check would reject genuine rows and still miss the leak (which never touches `orch-apply.sh`). Only 2 ntg-* rows remain, both real distinct clock reads.

**Shipped:** T5 `baseDeps()` stubs `writeSignalsFn`; new `narrativeContradictionGuards.ts` screens at the write boundary with a distinct greppable marker per class, quarantine never silent drop; `dedup_key` on BOTH engines, honoured on append so repeats bump `.occurrences`; summary quotes the probe evidence (python/TS byte-identical). CAUSAL REPRO: removing only the stub re-fires exactly 6 `NTG-GUARD-REFUSED-TEST-HARNESS-LIVE-WRITE` lines, live sha byte-identical. Replay of all 8 real rows at per-row clocks: 6 BLOCKED, 2 genuine ACCEPTED, negative controls 2 accepted + 1 collapsed to `occurrences=2`. 13/13 new, `tsc` clean, 307/307 across all 19 adjacent test files. `in_progress[]` -> `review[]`, next_agent=qa.

## Session 2026-08-25T07:55:00Z — FIX-DEVTEAM-ELIGIBILITY-EFFECTIVE-NEXTAGENT-DETAIL-FIRST-NO-RECENCY-COMPARE (scripts/lib/, developer, P0 S, session 036ceaf1, dev-team incident-lane consumer dispatch, po_expedited_at)

**Fixed the confirmed-in-code defect: `effective_owner()`/`effective_next_agent()` (`scripts/lib/devteam-eligibility.jq`) resolved detail-FIRST unconditionally, never comparing the two `updated_at` stamps both planes carry.** A hot-row reroute (board `next_agent` bumped) was silently reverted by the stale `docs/data/orch/archive/backlog-detail.json` copy on every subsequent read — live instance in the row's own note: `FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE` rerouted qa->ops on the hot board at 13:50Z while the detail copy still read qa stamped ~1h older, so QA-Drain re-claimed and burned a third redundant qa pass. Added `parse_ts()` (ISO8601->epoch, `null` on absent/unparseable — never a bogus epoch-0 that would silently invert the fallback) and rewrote both defs: when BOTH sides carry a non-empty value AND both carry a parseable `updated_at`, the strictly-more-recent side wins; detail-first is retained UNCHANGED as the tie-break/one-side-absent fallback, preserving the 2026-07-16 `FIX-DEVTEAM-BOUNDED1-EFFECTIVE-DISPOSITION-BOARD-FALLBACK-GATE` intent the row's own status_note warned not to regress.

**Verified with ad-hoc jq fixtures (scratchpad, not committed) covering both the live incident shape and its symmetric reverse, plus every pre-existing fallback shape.** PEK-shaped divergence (board `ops`@13:50Z newer than detail `qa`@12:51:27Z) resolves `ops` post-fix (was `qa` pre-fix — the bug); the reversed fixture resolves the newer side symmetrically; board-only, detail-only, no-`updated_at` legacy rows, and exact-tied timestamps all reproduce byte-identical pre-fix output — so no live row lacking a timestamp changes behavior. `effective_owner()` got the identical treatment in the same commit (AC-3).

**Ran the mandated satisfiability audit as an A/B, not a single post-hoc pass.** `bash scripts/audits/devteam-dispatch-gate-satisfiability.sh`: 114/116 PASS both BEFORE (git-stash A) and AFTER (B) the change, byte-identical failing assertion (`SLS gate (in_progress<2) is SATISFIABLE at in_progress=1` — its `[0:1]` fixture slice currently lands on this very row's own live in-flight `in_progress[0]` entry; a pre-existing live-board-shape condition, unrelated to the resolver logic, confirmed unchanged by the resolver fix) — zero regression against the row's hard-stop constraint. `bash scripts/audits/bounded1-supervised-lane-report.sh`: identical output before/after, exit 0 both.

**A `git stash`/`stash pop` A/B round-trip collided with a peer's live concurrent notebook write — recovered without touching it.** Stashing to get a clean "before" comparison tree captured `docs/agent-memory/notebooks/alert-commander.md` (a peer agent's own in-flight APPEND-class cycle) alongside my change; between stash and pop the peer wrote to that same file AGAIN, and `git stash pop` correctly refused rather than silently overwriting the newer content. Recovered surgically via `git checkout stash@{0} -- <path>` per-file (my own edit + the other genuinely-pre-existing dirty files), never touching `alert-commander.md` — confirmed intact and separately committed by that agent (`740d1a8a9`) moments later.

**Verification:** commit `6b94d0832` — `scripts/lib/devteam-eligibility.jq` only, explicit pathspec on both `add` and `commit`. Board row `IN_PROGRESS` → `REVIEW`, `next_agent`=qa (commit `4e81cf3d9`). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-9.md` STEP developer-S125 (commit `5abfa4405`).

---

## Session 2026-08-25T04:15:00Z — FIX-AUDITOR-TIER1-PROBE-SCORES-DELIBERATELY-DISABLED-LAUNCHD-JOB-AS-DEGRADED (scripts/agents-flow/, developer, P1 S, session 036ceaf1, dev-team incident-lane consumer dispatch)

**Implemented the architect's brief verbatim — design was already done, this cycle was execution + live verification, not redesign.** `_check_launchd_agents()`'s not-loaded branch only ever had two dispositions ("bad" or ack-ledger "acked"), so `com.vn-market.fleet-push`'s deliberate user-chosen `Disabled=>1` disarm (to stop an unattended push of ~224 unpushed commits) was laundered through the ack ledger, which cannot self-expire the way a real "degraded + open fix row" ack can. Added a third disposition, `_launchd_label_expected_disabled()`, checked BEFORE the ack lookup: reads `$LAUNCHD_INSTALLED_DIR/<label>.plist` via `plutil -p` (never `grep -i disabled` — that already produced one false negative on this exact binary plist), OR `launchctl print-disabled` membership. Either mechanism short-circuits the label out of `bad`/`acked`/the spawn-debounce entity side-channel entirely, into a new `expected_disabled` bucket named in `detail` with wording textually distinct from `acknowledged-degraded` (AC-4). Loaded-but-bad-exit-status branch deliberately untouched per the brief — `Disabled=>1` can never be loaded, so that branch is always a live, possibly-crashing process.

**One design gap the brief itself flagged, resolved by matching an existing file idiom rather than guessing.** The brief's pseudocode literally wrote `"label(disabled-by-plist)" # or "(disabled-by-launchctl)"` — ambiguous about which annotation text to hardcode. Instead of hardcoding either, had the new function itself PRINT which mechanism matched (same reason/rc convention `_launchd_label_acked()` already uses two functions above it) — more honest detail text for zero extra surface, and it let me independently verify AC-3 (the print-disabled-only path) in isolation: forced the plist-file check to miss via `LAUNCHD_INSTALLED_DIR_PATH=/nonexistent-...` in a scratch smoke script and confirmed the print-disabled membership check alone still correctly returns EXPECTED-DISABLED for fleet-push (both mechanisms are independently true for this label in this environment, so this was the only way to prove mechanism 2 isn't just dead code shadowed by mechanism 1).

**Verified every execution-verifiable AC live, ran the mutating probe exactly once.** `bash scripts/agents-flow/auditor-tier1-probe.sh` (single run) returned `verdict:"ALL_GREEN"`, `checks.launchd_agents:"PASS"`, detail naming `com.vn-market.fleet-push(disabled-by-plist)` under `expected-disabled` and `com.vn-market.docker-events(exit-status:143)` still under the unchanged `acknowledged-degraded` clause (AC-6/AC-7, regression control confirmed — docker-events' installed plist genuinely has no `Disabled` key). At the moment of that run, live-reconfirmed `plutil -p` still showed fleet-push `Disabled=>1` and `launchctl list | grep -c com.vn-market.fleet-push` = 0 (the AC-6 constraint the GREEN must hold under). `docs/data/auditor-tier1-spawn-debounce.json`'s `launchd_agents:com.vn-market.fleet-push` entry's `last_seen_at`/`spawn_count` were byte-identical before and after the run (AC-8 — label structurally never re-enters the failing-entity side-channel). `jq '.acked[]|select(.label=="com.vn-market.fleet-push")'` returned empty both before and after (AC-5). AC-9 self-check: reviewed every Bash command run this cycle — only `plutil -p`/`launchctl list`/`launchctl print-disabled`/`grep` (all read-only); no `launchctl enable/load/bootstrap`, no plist edit, anywhere.

**Peer-file coordination held cleanly.** `scripts/agents-flow/auditor-tier1-probe.test.sh` was flagged out-of-scope (sibling row `FIX-AUDITOR-TIER1-PROBE-TEST-INVERTED-ASSERTION-L1422-FALSE-GREEN`, same coordination_session, INCIDENT_CAP=2 pair) — did not edit it. It had already been committed by the sibling (`3fa945522`) by the time I ran it read-only for a regression check: full suite 265/265 GREEN against my probe.sh change, zero new failures, zero new shellcheck warnings, `bash -n` clean.

**Verification:** commit `0e3128e1f` — `scripts/agents-flow/auditor-tier1-probe.sh` + `docs/data/auditor-launchd-ack.json` (comment-only addition closing the loop so nobody re-adds a fleet-push ack entry). `git show --stat` confirmed exactly those 2 files, nothing swept in from the shared working tree (orch-state.json, tool-usage-stats.json, the sibling's test file — all correctly excluded). Runtime state mutated by the one live probe run (`auditor-tier1-last-trigger.json`/`-last-healthy.json`/`-spawn-debounce.json`) deliberately left uncommitted — that's the separate `chore(auditor): persist tier-N probe state` commit lane, not this task's file scope. Board row `IN_PROGRESS` → `REVIEW`, `next_agent`=qa.

---

