# Developer — Notebook

**Last updated:** 2026-08-08T03:56:10Z | **Cycle:** TASK-COWORK-SIGNAL-BCTC-REKEY (cross-service/bctc-analyst, SLS direct dispatch, session 165f4245) — REVIEW, next_agent=po

## Session 2026-08-08T03:56:10Z — TASK-COWORK-SIGNAL-BCTC-REKEY (cross-service/bctc-analyst, SLS direct dispatch, session 165f4245) — REVIEW, next_agent=po

**Task:** plan_only + supervised (inherited from parent FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING — "no code ships without parent re-adjudication by PO"). Task 2/4 of the parent's Phase-1 decomposition: specify bctc-analyst's FR-2 (filename rekey) + FR-7 (routine-mode explicit emit line) + the WINDOW_KEY sequencing pin. Deliverable is a spec document, not application code — none of the 4 named flow files touched.

**Design:** Read the row + parent row's full decision history (identical to task 1's context — same 3-instance-same-day incident, same PO amendments; Amendment 3 affects `derive_window_key()` internals not this task's scope, Amendment 1 descopes FR-4/tnb entirely, neither touched here) plus architecture brief §3 (FR-2+FR-7, all 4 subsections) and §8/§9 (test strategy, AC mapping). Live-re-verified all 4 target files before writing anything (not from the brief's paraphrase): `cycle.md` Step 0c (lines 52-89) already runs after stage-bootstrap/before stage-analyze — confirmed the right pin point, purely additive one-line addition after the existing `CYCLE_MODE` block. `stage-analyze.md` line 114 confirmed as the sole release-mode emit line (`{YYYYMMDD}`→`{WINDOW_KEY}` substitution only); confirmed the Routine Mode section (lines 6-82, Steps 1-4c) has genuinely NO emit-line instruction anywhere — FR-7's new line goes at the end of Step 4c (after line 80), immediately before the `---`/`## Release Mode` boundary, exactly where the brief specifies. `stage-consolidate.md`'s `## Output` section (lines 61-66) re-confirmed stale — re-read `stage-log-notify.md` in full and confirmed Stage 5 (5a-5e, Notebook+Notify+Deadline) contains zero `bctc_signal` write instructions, so the cross-reference correction points to the real location (stage-analyze.md R4/Step-4c) instead. `stage-log-notify.md` §5d-1 (lines 38-48): renamed `<cycle_tick_ISO>` to `<WINDOW_KEY>` in the `task_claim` mutex key, replaced the "how it's computed" prose with a pointer to the Step-0c pin (no change to claim/skip logic) — this is the NFR-3 single-source-of-truth fix the brief's §3.1 describes ("same value, same format, just sourced once instead of twice"). Cited `derive_window_key()` from task 1's already-delivered spec (signature, `YYYYMMDDTHHMMZ` return format, single-derivation-per-cycle contract) rather than redefining it — the worked filename example `bctc_signal_HPG_20260807T2100Z_routine.json` matches task 1's own Test Case 2 result independently, cross-confirming the two specs agree on the same concrete instance.

**Closeout:** Spec written to `docs/handoffs/TASK-COWORK-SIGNAL-BCTC-REKEY-spec.md` (commit `710dd6ac8`). Board row moved `in_progress[]` → `review[]`, `status=REVIEW`, `next_agent=po`, `owner=po`, `supervised`/`plan_only` preserved unchanged, `spec_doc` pointer + `developer_review_note` added, via `orch-apply.sh` (commit `2e7352478`) — `.head` reset to idle in the same write, validator/conservation-check both passed clean (task_total 778→778, a pure lane move). `mcp__gateway__call_tool` availability re-tested this session (not assumed from memory) — see task_release step below.

## Session 2026-08-08T03:28:25Z — TASK-COWORK-SIGNAL-DERIVE-WINDOWKEY (cross-service/shared, SLS direct dispatch, session 165f4245) — REVIEW, next_agent=po

**Task:** plan_only + supervised (inherited from parent FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING — "no code ships without parent re-adjudication by PO"). Task 1/4 of the parent's Phase-1 decomposition: specify `derive_window_key()`, the shared pure function bctc-analyst and chef-intraday writers will both call for a collision-safe WINDOW_KEY. Deliverable is a spec document, not application code — no writer files touched, no shared-function file created.

**Design:** Read the parent row's full decision history (3-instance-same-day incident, 2026-07-22 cycle_id-vs-window-anchor CAUTION, BA spec, architect brief, PO's `po_architect_signoff_20260807T0545` with 3 binding amendments) and the architecture brief §2 (FR-1 3-branch pseudocode). Branches 1 (scheduled_utc= token, preferred, not yet live) and 3 (EC-1 ad-hoc) carried forward **unchanged**. Branch 2 (the fallback — ACTIVE path day one, since no live trigger_prompt carries scheduled_utc= yet) replaced per PO's binding Amendment 3: the brief's original `TODAY_UTC + CRON_HOUR:00Z` unconditional date-concat re-introduces the exact UTC-day-boundary collision class this row exists to close. New algorithm: build the 3-candidate set `{yesterday, today, tomorrow}` at `CRON_HOUR:00Z` (re-verified live crons: bctc slots 1-4 = 15/18/21/0 UTC, confirming the midnight-boundary case is real), select the one with minimum `|delta|` from `live_mcp_fetched_at`; proved the 3-candidate window is always sufficient (consecutive occurrences are 24h apart, `FETCHED_TS` always lies in one calendar day, so the nearest is always yesterday/today/tomorrow's occurrence — no wider scan ever needed). Tie-break (defensive, unreachable under the live cron table today per PO's own instruction to state one anyway): scan chronologically, strict `<` comparison, so a tie is resolved toward the earlier candidate by construction — no separate branch needed. Worked both PO-mandated test cases: slot-4 `live_mcp_fetched_at=D 23:57Z` → deltas 172620s/86220s/180s → resolves `(D+1)T0000Z` (matches); slot-3 `live_mcp_fetched_at=(D+1) 08:00Z` → deltas 39600s/46800s/133200s → resolves `(D)T2100Z` (matches, and with `D=2026-08-07` independently reaches the brief's own worked example `20260807T2100Z`). Return format fixed as `YYYYMMDDTHHMMZ` (no seconds) — matched to the brief's two concrete worked examples, which both omit `SS`, rather than its looser `YYYYMMDDTHHMMSSZ-style` prose gloss; documented this discrepancy explicitly so a future implementer doesn't add a seconds field. Purity confirmed against NFR-2/EC-3: no Bash `date` calls anywhere, only a `jq` lookup on the already-loaded schedule JSON + arithmetic on the passed-in `live_mcp_fetched_at`. Documented the shared-dependency contract (signature, format, single-derivation-per-cycle NFR-3) for tasks 2/3 (`TASK-COWORK-SIGNAL-BCTC-REKEY`, `TASK-COWORK-SIGNAL-CHEF-INTRADAY`) to code against without modification.

**Closeout:** Spec written to `docs/handoffs/TASK-COWORK-SIGNAL-DERIVE-WINDOWKEY-spec.md` (commit `df0a91c60`). Board row moved `in_progress[]` → `review[]`, `status=REVIEW`, `next_agent=po`, `owner=po`, `supervised`/`plan_only` preserved unchanged, `spec_doc` pointer + `developer_review_note` (both test-case results inline) added, via `orch-apply.sh` (commit `62ca96465`) — `.head` reset to idle in the same write. `mcp__gateway__call_tool` was absent from this session's tool binding (confirmed by an actual failed call, not from memory) — used the degraded-mode Bash-bridge fallback per `docs/standards/gateway-call-contract.md` §6b (Bash-equipped caller profile): `source scripts/agents-flow/mcp-call.sh; mcp_call "task_release" '{"task_id":"task:TASK-COWORK-SIGNAL-DERIVE-WINDOWKEY","owner_client_session":"165f4245-6173-4054-87fd-c55bb626265f"}'` → `{"ok":true,"released":1}`.

## Session 2026-08-08T02:15:00Z — FIX-ORCHSTATE-SIGNALQUEUE-UNCOMMITTED-ROWS-LOST-TO-PEER-FULLDOC-WRITE (cross-service/, SLS dispatch, session 165f4245) — REVIEW, next_agent=qa

**Task:** PO's row claimed 2 live untriaged WARN `signal_queue` rows (`sys-20260729T103337-13e5`/`sys-20260729T103338-7f95`, sbv_fx staleness, `to=po`) were DESTROYED by a peer full-doc write between 10:41-10:47Z on 2026-07-29 — `orch-conservation-check.mjs`'s 0.5 magnitude-ratio floor (131/133=98.5%) cannot see a 2-row loss. Scope: name the write path, add a row-identity conservation dimension distinct from the magnitude guard, verify legit drain/cold-evict still passes + the magnitude guard is unchanged.

**Investigation FIRST, before any code (AC-1):** Traced commit `3e257beba` ("drain 2026-07-29T10:37Z tick") — its `docs/data/orch/orch-state.json` diff shows ZERO signal_queue.rows[] id changes vs parent (confirmed via `git show <sha>:path | jq`, not trusted from the row's prose), matching the row's own claim of a silent drop. BUT: the SAME commit's `docs/data/orch/archive/2026-07.json` diff shows BOTH sbv_fx rows APPENDED there, status READ. Checked the LIVE repo right now: both rows are still present in `docs/data/orch/archive/2026-07.json` `.signal_rows[]` today — **not destroyed**. Write path = `scripts/orch-cold-evict.sh` (invoked from `docs/agents/dev-team/flow/drain-signals.md` §0a-D-PRUNE), correctly routed through `orch-apply.sh`. Mechanism = the (at-the-time, pre-2026-08-01) no-age-gate immediate-eviction of just-flipped-READ signal rows — the EXACT class already diagnosed and closed by `SIGNAL_MAX_AGE_HOURS` (`FIX-COLDEVICT-SIGNALQUEUE-NO-AGE-GATE-ORPHANS-READ-ROWS`, shipped 2026-08-01, one week before this task). Also found a DIFFERENT, later, already-manually-fixed instance of the same underlying no-age-gate defect (commit `3d171d196`, different row ids, ~30min after 3e257beba) — confirms the class, not this specific pair. PO's "absent from archive[]" evidence checked `.signal_queue.archive[]`, the deprecated inline lane cold-evict.sh unconditionally empties to `[]` every run (RC-1/HSC-7) — that field is ALWAYS empty by design, so checking it can never distinguish "destroyed" from "correctly cold-evicted." This is a "check the other plane" miss, not a real loss.

**Actions (still shipped the row's own AC-2 scope as legitimate defense-in-depth, independent of the misdiagnosis):** `scripts/orch-conservation-check.mjs` gains a row-identity dimension: any live `.signal_queue.rows[]` id absent from candidate must be present in candidate `.signal_queue.archive[]` OR named in new env `ORCH_APPLY_DECLARED_SIGNAL_EVICTIONS` (comma-separated ids) — else HARD REJECT, exit 1, live untouched. Deliberately **never bypassable by `ORCH_APPLY_ALLOW_SHRINK`** (orthogonal claim: "total may shrink a lot" ≠ "any row may vanish unaccounted for"). Wired the declaration into `scripts/orch-cold-evict.sh`'s single `orch-apply.sh` invocation, sourced directly from its own already-computed `rm_sig_rows` map (mechanically in lockstep with the actual removal, never hand-maintained) — this is the sole legitimate remover of `signal_queue.rows[]` entries; `pm/task-archive.md` delegates its own signal-row eviction to this same script, so no second wiring site needed.

**Verify:** `bash scripts/test/orch-apply-wrapper-tests.sh` 75/75 (60 pre-existing unaffected + 15 new: `ROW-DROP-REJECTED` undeclared drop → exit 1 live-unchanged; `ROW-DROP-ALLOW-SHRINK-NO-BYPASS` proves the bypass does NOT cover row-identity; `ROW-DROP-DECLARED-ALLOWED` + `ROW-DROP-ARCHIVE-ACCOUNTED` both → exit 0; `ROW-APPEND-HAPPY` regression guard). `bash scripts/test/orch-cold-evict-tests.sh` 53/53 unaffected (T9/T10 exercise real signal_queue eviction end-to-end through the newly-wired declaration — proves AC-3 via the actual production script, not just a synthetic fixture). AC-4 (magnitude guard unchanged): existing COLLAPSE test still fires; re-demonstrated standalone (20 backlog → 0, no bypass) → exit 1. All 4 ACs also re-run standalone against scratch fixtures outside the test harness (commands in the task's final report) to confirm independent reproducibility. No `apps/` TS/Go touched (zone scripts/ + docs/policies/) — `bun test`/`tsc` N/A.
