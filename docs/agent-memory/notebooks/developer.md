# Developer — Notebook

**Last updated:** 2026-07-10 | **Cycle:** D1-BACKLOG-HYGIENE-SWEEP-EXECUTE (residual b+c+d)

## Session 2026-07-10 — D1-BACKLOG-HYGIENE-SWEEP-EXECUTE (RESIDUAL, sprint BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP, router-dispatched, zone `scripts/`) — sub-scopes (b)/(c)/(d) EXECUTED, row left READY for router flip

**Task:** D1's own row was left `ready[]/READY` after D2.5 (`b7d669061`) landed and unblocked it (board-flip `9339f0840`). Note field re-scoped D1 to 3 remaining residual items (sub-scope (a) cold-evict already shipped `42e565c7b`, prior cycle): (b) close CONFIRM-TERMINAL exception `FACTORY-INTERFACE-split-server-ts` via `devteam-close-task-done-verified.jq`; (c) 8 lane-moves from D0B's `triage_result.exceptions[]` RELABEL entries; (d) relabel `FIX-BCTC-BANK-SUMMARY-MAPPING` status DONE→BLOCKED.

**Independent verification before acting:** (1) read D1's live `note` field directly — matched the dispatch prompt's restated scope exactly (9 target rows: 8 lane-moves + 1 relabel, plus the separate close-exception). (2) Read D0B's archived `done_verified` row (`docs/data/orch/archive/2026-07.json`, deep-search by id — NOT under `.backlog_detail[]` as the prompt guessed, it's a top-level `done_tasks[]`-style entry) — `triage_result.exceptions[]` carries exactly 15 items (1 CONFIRM-TERMINAL, 4 EXCLUDE, 10 RELABEL). Of the 10 RELABEL, 2 were already resolved by prior cycles (D1's own row, and `FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID` — confirmed absent from the live board entirely, already evicted by the sub-scope-(a) cold-evict sweep) — the remaining 8 match the note's list exactly. (3) `git log -1` on all 4 stage commits (`bce8be44b`, `821bbbeea`, `56f922c93`, `8c228ffa6`) — all 4 exist and match their expected subjects. (4) `bun scripts/orch-validate.mjs` baseline — confirmed exactly 9 coherence warnings, and the 9 flagged IDs are exactly the 8 lane-move ids + `FIX-BCTC-BANK-SUMMARY-MAPPING` (matches dispatch prompt's claim; the row's own `note` field said "16→0" which referred to a stale earlier count — actual live baseline was 9, corroborated by a router `router_note` on a neighboring row: "9 warnings, same 9 IDs as D1-residual note (16-7=9, matches po arithmetic)").

**Execution:** Read `apps/mcp-server/src/infrastructure/orchStateSchema.ts` `LANE_ALLOWED_STATUSES` (D2.5-added) to confirm target-lane admissibility: `review→{REVIEW,BLOCKED}`, `in_progress→{IN_PROGRESS,BLOCKED}`, `backlog→{BACKLOG,BLOCKED}` — all 9 moves/relabels land coherent. Wrote one combined jq filter (`scripts/` write-gate pattern, `jq -f <script> --arg now --arg by | scripts/orch-apply.sh`) doing all 3 sub-scopes in a SINGLE atomic write (avoids 3 separate CAS races): captured the 8 move-out rows + the FACTORY row from `backlog[]` first, sanity-asserted (`error()`) all 9 were found before mutating, then removed the 9 from `backlog[]`, appended the 5 REVIEW rows to `review[]` (status unchanged), the 3 IN_PROGRESS rows to `in_progress[]` (status unchanged), relabeled `FIX-BCTC-BANK-SUMMARY-MAPPING` status DONE→BLOCKED in place (stays in `backlog[]`, stamped `relabeled_at/by/reason`), and closed `FACTORY-INTERFACE-split-server-ts` to `done_verified[]` (mirrors `devteam-close-task-done-verified.jq`'s shape: `status/done_verified/verified_by/verified_at/signoff_note`, since it needed to land in the same atomic write as the lane-moves). Dry-ran the filter to a scratchpad candidate first, pre-validated with `orch-validate.mjs` (0 warnings) + `orch-conservation-check.mjs` (task_total 473→473, signal_total 0→0 exact) before piping through the real `orch-apply.sh` (exit 0, CAS+validate+conservation all green inside the wrapper too).

**Post-write verification:** `orch-validate.mjs` on live file → 0 coherence warnings (9→0 confirmed). `orchStateSchema.test.ts` full suite → 103 pass / 0 fail / 622 expect() calls, including the live-data lane-coherence assertion. `orch-conservation-check.mjs` re-run standalone against last-committed HEAD vs live-now → exact conservation (473/473, 0/0). D1's own row confirmed untouched — still `ready[]`/`READY` (left for router to RAW-verify + flip, per instruction). `git diff` inspected in full — clean 9-row relocation/relabel, no collateral mutation to any other row.

**Result:** Committed `<pending — see next commit>` (orch-state.json only, explicit path). D1-residual (b)+(c)+(d) complete. D5-BACKLOG-HYGIENE-VALIDATOR-HARDENING's depends (`D3, D2.5, D1`) now all satisfied once router flips D1 — router's next step, not mine.

**Scope discipline:** did not touch D5 or any other sprint task. Did not flip D1's own board row (router's job, RAW-verify-then-flip pattern). Left the 2 already-resolved RELABEL ids (D1 itself, `FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID`) untouched — no rework needed, already correctly landed by prior cycles. DJ: `sprint-BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP-developer.md`.

## Session 2026-07-10 — D1-BACKLOG-HYGIENE-SWEEP-EXECUTE (RETRY, sprint BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP, router-dispatched, zone `scripts/`) — EXECUTED LIVE, unblocked

**Task:** Retry of the BLOCKED cycle below. Router reported D0B (`98d26dc01`) re-derived + persisted the full 15-item `triage_result.exceptions[]` to both D0 and D0B `done_verified` rows, closing the gap that self-blocked the prior attempt. Router pre-verified the persistence; this cycle re-verified independently before trusting it (same discipline as the block).

**Independent re-verification:** read both `.task_board.done_verified[id=D0-...]` and `[id=D0B-...]` `triage_result.exceptions[]` directly — both carry exactly 15 entries (1 CONFIRM-TERMINAL `FACTORY-INTERFACE-split-server-ts`, 4 EXCLUDE, 10 RELABEL). The 4 EXCLUDE ids (`FIX-BCTC-BANK-SUMMARY-MAPPING`, `FIX-ALERT-OPEN-ZERO-PRICE-RACE`, `FU-PROFILE-DATA-VERIFY`, `REFLOW-MBB-Q1-2026`) match the router-restated list exactly — used only after this direct confirmation, not the restated list on trust. Noted (non-blocking, not my scope): D0's own `exclude_count`/`relabel_count` summary fields say 4/11 but the array itself enumerates 4/10 — a 1-off metadata drift inside D0's own row, doesn't affect the EXCLUDE set I needed.

**Execution:** (1) flipped D1's own row `BLOCKED→BACKLOG` (itself one of D0/D0B's own RELABEL findings, left for D1 to execute) via `orch-apply.sh`. (2) `orch-cold-evict.sh --dry-run` with the 4 `--exclude-ids` confirmed 55 flat-lane rows would evict with the excludes skipped; cross-checked `FIX-BCTC-BANK-SUMMARY-MAPPING` (status=DONE) would have been swept WITHOUT the flag, proving the exclusion genuinely active. (3) live run — evicted 2 `done[]` + 15 `done_verified[]` + 55 flat-lane rows (54 backlog + 1 review) to `archive/2026-07.json` `.backlog_detail[]`, plus 2 signal rows + 1 signal-archive row (existing categories). Conservation-guard (`orch-conservation-check.mjs`, both the internal `orch-apply.sh` check and a standalone re-run against pre-change HEAD) OK: `task_total 545→473` (delta -72, exact match to evicted count), `signal_total 2→0`. Confirmed post-write the 4 excludes are still hot, absent from cold; the 55 evicted IDs enumerated and spot-checked against the exclude set (no overlap).

**Result:** coherence warnings 72→16 (`orch-validate.mjs`) — 1 from the D1 flip itself, 55 from the eviction. Committed `42e565c7b`.

**Scope discipline:** close-exception (`FACTORY-INTERFACE-split-server-ts` via `devteam-close-task-done-verified.jq`) and the 10 RELABEL lane-relocations remain OUT OF SCOPE this cycle — router explicitly narrowed D1 to eviction-only, unchanged from the prior attempt. DJ: `sprint-BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP-developer.md` STEP developer-S5.

## Session 2026-07-10 — D1-BACKLOG-HYGIENE-SWEEP-EXECUTE (sprint BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP, router-dispatched, zone `scripts/`) — BLOCKED, not fake-green

**Task:** Router asked to run D4's newly-extended `orch-cold-evict.sh` live against `orch-state.json`, using D0's `triage_result` to build `--exclude-ids` (the 4 D0-flagged false-positives). Router explicitly required verifying exact IDs from that field, no guessing.

**Finding (blocking):** D0 (`DONE_VERIFIED`, commit `26ffe7567`) claims "73 confirm-terminal, 4 exclude, 11 relabel" but its `triage_result` field only persists aggregate counts + 2 named exceptions (1 confirm-terminal, 1 exclude — `FIX-BCTC-BANK-SUMMARY-MAPPING`). Verified via `git show 26ffe7567` (diff touches only D0's own board row + pm.md, no per-row annotation) and a repo-wide grep for `exclude_count`/`relabel_count`/the task ID — no other artifact exists anywhere. The other 3 exclude IDs and all 11 relabel IDs are unrecoverable. This is the same class of bug PM's own notebook flagged as "3RD confirmed data-loss instance this session" (agent claims a deliverable that isn't actually persisted) — a 4th instance, this time in a board-row field rather than a notebook.

**Evidence gathered (read-only):** `orch-cold-evict.sh --dry-run --exclude-ids FIX-BCTC-BANK-SUMMARY-MAPPING` → 55 rows (54 backlog + 1 review) currently match `TERMINAL_TASK_STATUSES` and would auto-evict with only 1 of 15 needed protections applied. Confirmed the pool already contains at least 1 of D0's 15 flagged rows (the known exclude, status=DONE) — the script has zero awareness of D0's categorization beyond `--exclude-ids`, so the other 14 are plausibly in-pool too, unprotected. D4's own tooling behaved correctly (dry-run byte-identical, no mutation) — the blocker is upstream data, not D4's code. Also confirmed (git log) D3 already landed (`e7fc1d44a`, 62 rows normalized) — live coherence warnings now 72 (down from architect's 129 baseline), matching D3's expected drop.

**Action taken:** Did NOT execute live eviction (R-CRIT-1 risk — silently archiving genuinely-open work, exactly what architect's Exception 1 spot-check already proved happens). Flipped D1's own board row `BACKLOG→BLOCKED` with a status_note documenting the gap + remedy (D0 needs to append the full 4+11 ID lists before D1 can re-run), committed `d45c03f1a`. No task rows evicted, no cold archive file touched, no other row mutated.

**Scope discipline:** Brief §8 D1 also covers closing Exception 2 (`devteam-close-task-done-verified.jq`) and relocating D2 candidates — router explicitly narrowed this cycle to eviction-only, so neither was attempted. `send_telegram` unavailable in this sub-agent's toolset (Read/Edit/Write/Bash only, same limitation noted in the prior D4 cycle) — escalation relayed via RETURN block to router instead. DJ: `sprint-BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP-developer.md` STEP developer-S4.

## Session 2026-07-10 — D4-BACKLOG-HYGIENE-ORCH-COLD-EVICT-EXTEND (sprint BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP, router-dispatched, zone `scripts/`)

**Task:** `scripts/orch-cold-evict.sh` had zero code path reading `task_board.backlog[]` (or sibling flat lanes review/qa/in_progress/ready) — root cause of a 55+-row backlog-hygiene sweep: rows whose status flips to a terminal value in-place without a lane move permanently strand there. Architect brief `2026-07-10-backlog-hygiene-verify-prune-sweep.md` §8 D4 row = spec; corrected PO's mis-named cold-sink target (`archive/backlog-detail.json` is the LIVE hot-stub/cold-detail split, NOT a terminal-eviction sink).

**Fix:** new Pass-1 category in `orch-cold-evict.sh` scanning `{backlog,review,qa,in_progress,ready}` (done/done_verified excluded — already handled) for `TERMINAL_TASK_STATUSES` (new env var, default byte-identical to existing `TERMINAL_SPRINT_STATUSES` = TERMINAL_SET SSOT, not a new definition). Cold sink = the dormant `.backlog_detail[]` field already in the monthly-archive schema (line ~323, always `[]` since inception, now wired both branches of `build_cold_temp`). Added `COLD_BACKLOG_DETAIL_IDS` idempotency read mirroring the existing pattern. Added `--exclude-ids` (repeatable + comma-separated forms) / `EXCLUDE_TASK_IDS` env safety valve for D0's one-time migration exclude-list.

**Test:** new `scripts/test/orch-cold-evict-tests.sh` (none existed before — R-HIGH-1, sole SSOT eviction script) — 27/27 GREEN: evict-correctness, non-terminal-skip (BLOCKED/REVIEW/IN_PROGRESS untouched), `--exclude-ids` both forms, idempotent re-run (byte-identical hot+cold), conservation-guard-still-fires (paired control: direct `orch-apply.sh` no-bypass call rejected exit 1 + the real script succeeds via its existing bypass), `--dry-run` zero-mutation + cold-file-not-created. Mirrors `orch-apply-wrapper-tests.sh`'s fixture/real-live-hash-unchanged pattern — never ran against live `docs/data/orch/orch-state.json`.

**Regression check:** manual fixture with done[]/done_verified[]/active_sprints[]/sprint_goal.entries[]/signal_queue rows populated alongside new terminal backlog/review rows — confirmed all 5 pre-existing eviction categories (done age/keep_n, done_verified all-terminal, active_sprints terminal+BCTC-*, sprint_goal.entries terminal, signal rows+archive) still function correctly alongside the new 6th category in the same run.

**Scope discipline:** did NOT execute the new pass against live `orch-state.json` — that's D1's job (depends on D0's parallel triage output). Updated `docs/policies/dev-standards.md` CANONICAL block for orch-cold-evict.sh (D4 extension + new test pointer) + `docs/WORK.md` one-liner. graphify skipped — no Skill-tool invocation available in this sub-agent's toolset (Read/Edit/Write/Bash only); `graphify-out/graph.json` already ~7 weeks stale independent of this task. DJ: `sprint-BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP-developer.md`. Board: own row `D4-BACKLOG-HYGIENE-ORCH-COLD-EVICT-EXTEND` backlog→done_verified via `orch-apply.sh`.
