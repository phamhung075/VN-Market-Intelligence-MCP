# Developer — Notebook

**Last updated:** 2026-08-06 | **Cycle:** FIX-DEPTHTHIN-B-GATEWAY-TA-PATH-REWRITE

> **NOTE (concurrency, standing limitation, not part of any task AC):** `docs/agent-memory/notebooks/*.md` has no CAS/mtime guard analogous to `orch-apply.sh`'s for `orch-state.json` — a same-window concurrent-agent notebook write can silently orphan a just-landed section even after `git log` shows the commit landed first. Flagged for po/architect as a candidate follow-up; not fixed in this task.

## Session 2026-08-06 — FIX-DEVTEAM-QADRAIN-THROUGHPUT-CAP (P1, zone `scripts/`, architect direct dispatch, `.jq`-only scope) — REVIEW, next_agent=qa

**Task:** `scripts/devteam-review-claim-qa-drain.jq` was the one lane in the dev-team idle-fallthrough chain still `sort_by(.age)` with no priority term (BOUNDED-1/SLS/RLC/DRS already use `priority_rank`) — 27 live P0 review rows, 7 same-day, would queue behind a 13-14d P2/P3 wall. Architect's brief also folded in the one-row-per-tick throughput cap. Sibling `main.md` wiring half correctly moved to agent-father (zone: developer has no `commit_zone` on `docs/agents/**`).

**Actions:** sort `sort_by(.age)`→`sort_by([priority_rank, .age])` (reused the existing shared def, no new file). Batch-claim: `$take=min($take_budget, candidates length)`, `$take_budget` read via `$ARGS.named.take_budget // 1` (NOT a directly-referenced `--argjson` var — compiles/runs even when the caller omits it, backward-safe for the pre-existing single-claim `main.md` site). Whole batch shares one `claimed_at`/`claimed_by`; `.head` narrates only the batch's top-ranked row. Extended `devteam-dispatch-gate-satisfiability.sh`'s QA-Drain fixture with the priority-ordering positive control.

**Verify:** RED (git-stash-isolated pre-fix fixture: age-only sort picked an older P2 over a same-day P0) → GREEN (post-fix: P0 picked at default `take_budget=1`; both batched, P0-first, at `take_budget=2`; over-budget request caps cleanly). Full satisfiability harness re-run clean on every QA-Drain/head-guard/new assertion; one unrelated pre-existing BOUNDED-1 live-board-state failure confirmed identical on unmodified HEAD (out of scope). No `apps/` TS/Go touched (zone `scripts/`, pure `.jq`+`.sh`) — `bun test`/`tsc` N/A. Commits `ce053fdc8` (scripts+tests), `191349afb` (docs/WORK.md), `71309d1b7` (board). **Incident, self-corrected:** a bare `git stash` mid-verify briefly scooped up a live peer's (agent-father) uncommitted `main.md` edit; `stash pop` correctly refused rather than overwrite; recovered via targeted per-file `git checkout stash@{0} -- <path>`, confirmed zero data loss before dropping the stash. Lesson: never bare `git stash` in this shared working tree.

## Session 2026-08-06 — UC-CRITIC-HOOKS-ENFORCEMENT (P1 SPIKE→FIX, zone `cross-service/`, ba→architect→developer relay) — REVIEW, next_agent=qa

**Task:** 4 CRITICAL/HIGH hook invocations (`orch-state-hook-bash-backstop.sh`, `context-bloat-backstop.sh`, `notebook-auto-prune.sh`, `branch-hygiene-stop.sh`) all ended in `2>/dev/null || true` — a crashed prerequisite (missing `git`/`jq`, unreadable stdin, corrupted governance SSOT) was indistinguishable from a legitimate pass.

**Actions:** FR-1 dropped `2>/dev/null || true` from exactly these 4 invocation strings in `.claude/settings.local.json` (LIVE-FILE-ONLY — untracked/gitignored, no commit surface; 3 LOW-tier hooks + `orch-state-hook-prewrite.mjs` untouched). FR-2: new `scripts/agents-flow/lib/hook-guard.sh` (`hg_run` — merged-stream capture, zero new subprocess forks; `hg_resolve_project_root` — `command -v git` builtin discriminates "git missing" from "not a repo"), sourced by all 4 scripts, applied ONLY at named input-boundary guards (stdin/jq extraction, PROJECT_ROOT, validator-missing check, `file-size-caps.json` SSOT-corruption guard — architect's "highest-value target"); `branch-hygiene-stop.sh` got opposite polarity (crash flips a `problems[]` entry + `exit 1`, vs. the old `""`/`true` fallthrough reading as clean). New convention: 0=pass/no-op (unchanged), 1=prerequisite crash. Deliberately did NOT wrap `wc -l/-c` calls (not a BA-named dependency-removal risk, regression risk > coverage gain) or `notebook-auto-prune.sh`'s separate `LINE_CAP` SSOT-read fallback (already-safe, architect-flagged non-gating). Found+fixed one incidental bug: `orch-state-hook-bash-backstop.sh`'s `mktemp` template had a suffix after the `X`s — BSD/macOS `mktemp` doesn't randomize that, so every run after the first collided with "File exists" (previously invisible, swallowed by the old wrapper). FR-3: new A-33 "Hook Enforcement Liveness" check in `tier1-probe.md`, reuses `emit-audit-signal.sh` — zero new plumbing.

**Verify:** `orch-state-hook.test.mjs` AC-3 retitled + 2 crash cases → 21/21 PASS. `context-bloat-backstop.test.sh` +T5 (corrupted caps SSOT → exit 1) → 5/5 PASS. `notebook-auto-prune.test.sh` +T8 (jq missing → exit 1) → 8/8 PASS. NEW `branch-hygiene-stop.test.sh` (first-ever coverage, 4 cases incl. git-missing crash) → 4/4 PASS. `bash -n`/`jq empty` clean on every touched file. No `apps/` TS/Go touched — `bun test`/`tsc` N/A. Full Implementation Record: `docs/handoffs/UC-CRITIC-HOOKS-ENFORCEMENT-BA-spec.md`. **Flag for QA:** the `.claude/settings.local.json` FR-1 edit will NOT show in `git diff` — verify on disk directly.

## Session 2026-08-06 — FIX-DEPTHTHIN-B-GATEWAY-TA-PATH-REWRITE (P3, zone `multi`, dev-team BOUNDED-1 idle-capacity auto-pickup) — CANCELLED, no next_agent (closed)

**Task:** row claimed gateway `/ta` path-rewrite was an unimplemented TODO blocking dashboard MA/RSI/MACD/BB, plus `/api/price-history` allegedly missing `generatedAt`. Router pre-flagged: gateway fix landed 2026-06-11 (`854fe7ba0`), predating this row's 2026-06-25 mint — RAW-verify before touching code, per generic_mandate distinguish routing-gap vs sibling `FIX-TA-INDICATORS-TIER3-ROUTING` computation-gap.

**Actions:** none — RAW-verified live and found ALL THREE sub-asks already satisfied, none by this task. (1) `apps/api-gateway/pkg/infrastructure/registry.go:42` confirms `PreservePath:true` for `ta` is live today, unchanged since `854fe7ba0`. (2) Dashboard TA indicators were never sourced via the gateway at all: `apps/frontend/app/components/charts/StockChart.tsx` client-computes MA20/MA50/BB/RSI14/MACD from `/api/price-history` candles (shipped 2026-05-17); `/dashboard/technical` route was deleted 2026-06-28 (`87871e06e`, content now in `/dashboard/analysis` via `TechnicalZone.tsx`, which explicitly documents "do NOT add server-side TA value display here" as the intended boundary). (3) `/api/price-history` has served `generated_at` since creation (`b1b7394ce`, 2026-06-11) — live `docker exec` curl confirmed; frontend already consumes it; rejected a literal camelCase rename as inconsistent with the endpoint's snake_case envelope. Sibling `FIX-TA-INDICATORS-TIER3-ROUTING` confirmed still genuinely open/unpromoted — not duplicated.

**Verify:** `git show 854fe7ba0`; `grep PreservePath registry.go`; `git log --diff-filter=D -- dashboard.technical.tsx`; live curl `localhost:3000/api/price-history/FPT` → real candles + `generated_at`. Board row `in_progress[]`→`archive[]`, status `CANCELLED` (precedent `FIX-ALERT-CASCADE-OUTCOME-DEAD`), `.head` reset idle/router same write. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` STEP developer-S4. No `apps/` code touched — `bun test`/`tsc` N/A.
