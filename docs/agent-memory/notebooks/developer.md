# Developer — Notebook

**Last updated:** 2026-08-01 | **Cycle:** FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR

## Session 2026-08-01 — FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR (P1, zone `cross-service/`, BOUNDED-1 idle-capacity auto-pickup) — REVIEW

**Task:** parent-row-reproduced defect: `git commit -m ... -- <path>` uses git's own `-o`/`--only` semantics (working-tree content of the named path, NOT the staged subset), so the fleet-mandated pathspec commit still sweeps a peer's differently-staged hunk in the SAME file. Undocumented (architecture brief §2.1-2.6 only analysed cross-file/directory-pathspec sweep) and untested (`pre-commit.test.sh` T1-T6 all cross-file).

**Actions:** AC-1 — documented as an explicit NON-GOAL of the pathspec-commit mandate: architecture brief new §2.7 (+ Risk flag 5 + Test-strategy/Files addenda) and `dev-standards.md`'s CANONICAL sweep-guard block, mirroring the existing directory/dot-pathspec non-goal (§2.3/T3). AC-2 — new T11 in `pre-commit.test.sh` pins the actual `--only` behavior (replays the row's own `git apply --cached`-then-pathspec-commit reproduction: both hunks land). AC-3 — new `_detect_samefile_pathspec_only_divergence()` in `scripts/git-hooks/pre-commit`: compares each SCOPED commit's about-to-land blob (scratch index) against the same path's blob in the REAL shared index; mismatch → stderr WARN + `write_signal`. Detector only — never gates, ignores `GIT_SWEEP_GUARD_MODE`, always returns 0. Did NOT touch `.claude/skills/commit-mutex|commit-boundary|commit/SKILL.md` (agent-father zone per the brief's own §7 table; row's `Files:` list names only `dev-standards.md`). AC-4 (epic-tail) — `cowork-refactory-expert.md:57`'s bare-commit-with-directory-add example migrated to the pathspec form.

**Verify:** empirically reproduced the exact bug FIRST in a scratch repo (byte-for-byte match to the row's own claim) before writing any doc/test. `bash scripts/git-hooks/pre-commit.test.sh` 12/12 PASS (T1-T10 unchanged, new T11 pin + T12 detector negative control both PASS). `shellcheck` clean on both hook + test files (0 new findings). False-positive-clean verified against T4's rebase-replay and the ordinary `git add && git commit -- <path>` idiom before shipping. Own commit (`66e850138`) ran through the newly-edited live hook (`.git/hooks/pre-commit` symlinks to the edited file) — clean, no divergence warning, exact 5-file pathspec set landed. No `apps/` TS/Go touched (zone `cross-service/`) — `bun test`/`tsc` structurally N/A. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md` STEP developer-S52.

## Session 2026-08-01 — FIX-DEVTEAM-IDLE-CHAIN-DANGLING-DEPS-STRAND-5-P0-ROWS AC-3 (P0, zone `cross-service/`, BOUNDED-1 idle-capacity auto-pickup) — REVIEW

**Task:** AC-1 (clear stale `.depends` on 3 rows)/AC-2 (dedupe, moot under the fix applied) already landed by dev-team at mint time. Remaining scope: `scripts/lib/devteam-eligibility.jq`'s `effective_depends_on()` UNIONS `.depends_on`+`.depends`+`.blocked_by` — editing only one field can never shrink the effective set. Commit `2833b71bf` corrected `.depends_on` on 3 rows after deleting `S1-SCHEMA-SELECTION`/`P1B-STAMP` but left them resident in the legacy `.depends` field, silently resurrected forever, permanently fail-closing `deps_satisfied()` — starved 5 P0 rows for 3 days.

**Actions:** `orchStateSchema.ts` §13 `checkDependsDivergence()` — HARD FAIL (`orch-validate.mjs` Stage 1f) when a row carries BOTH `.depends`/`.depends_on` and `.depends` names an id absent from `.depends_on`. §14 `checkMissingDependencyReport()`/`collectHotDepStatusLaneIds()` — NON-FATAL report (Stage 1g) of rows whose effective dep set resolves MISSING in both hot 7 flat lanes (mirrors `dep_status_map()`'s own scan) + cold archive `.done_tasks[]`. Scope excludes `active_sprints[].tasks[]` (in-flight intra-sprint deps — normal WIP noise, `dep_status_map()` never scans sprint-nested tasks at all, ~47 rows of pure noise) but includes `closed_sprints[].tasks[]` (settled/frozen cleanup signal) — derived from the runtime mechanism's real lane coverage via a scratch jq harness, not guessed. AC-4 honored: did not touch `deps_satisfied()` MISSING semantics.

**Verify:** live run `bun scripts/orch-validate.mjs` on the real board — exit 0, Stage 1g prints exactly 8 rows (4 backlog + 4 closed_sprints), matching the row's own stated count exactly. New `apps/mcp-server/src/__tests__/FIX-DEVTEAM-IDLE-CHAIN-DANGLING-DEPS-STRAND-5-P0-ROWS.test.ts` 14/14 PASS (incident-shape positive control reproducing the real deleted ids). `orchStateSchema.test.ts` 104/104 PASS, `test-orch-validate-ac.mjs` 29/29 PASS, `bun tsc --noEmit` clean. `size-lint-justification.sh --check` clean (header bumped 839L→1074L with dated delta note). `docs/policies/dev-standards.md` CANONICAL block + `docs/WORK.md` updated. graphify attempted (`graphify docs --update --no-viz`) — structurally unavailable this session (no LLM API key configured), not fabricated. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md` STEP developer-S51.

## Session 2026-07-31 — FIX-NOTEBOOK-AUTOPRUNE-DIRECTION-UNRESOLVABLE-ZERO-TS-NOTEBOOKS (P1, zone `cross-service/`, BOUNDED-1 idle-capacity auto-pickup) — REVIEW

**Task:** `notebook-auto-prune.sh`'s same-day/tied-heading direction vote deadlocked forever (dec=0,inc=0, TIE_COUNT>=2) for any file where EVERY retained `## ` section lacks a parseable timestamp — 11 confirmed live files (code-janitor, cowork-refactory-expert x2, digest-predict, idea-forge, market-analyst, ops-mainserver-fetch, pm-alpha-s2-rag-fts-rebuild-cron, po, semble-search, dev-technical-analysis), all sharing generic ROLLING headings (`## Known patterns / preferences`, `## Identity`) not per-cycle dated entries. The override table (`docs/data/notebook-section-order.json`) covered only 3 files with no producer, so these 11 fail-louded and exited 0 without pruning — 11 signals/35h, 2 files (unified-agent.md, digest-predict.md) actually breached byte cap.

**Actions:** AC-3 hardening: `[ "$prev_key" \> "$cur_key" ]` (bash-`[`-only, silently inverts under zsh) → `[ "$prev_key" -gt "$cur_key" ]` (POSIX-portable, identical ordering for the fixed-width all-digit ts_keys). Tertiary: excluded sentinel-vs-real pairs from the vote (was injecting a phantom vote from trailing rolling headings). AC-1 primary: terminal "refuse forever" branch replaced with a documented default (`newest_first`, drop physically-last) + new non-blocking `notebook_tiebreak_direction_defaulted` informational signal — file's own votes/override still consulted first, unchanged. AC-2: override table kept (still useful for the 2 known append-style files it correctly overrides) but `_note`/`_maintained_by` corrected — derivation never NEEDS it now, no automated producer by design (a wrong auto-entry would silently invert the drop direction).

**Verify:** new `scripts/agents-flow/notebook-direction-corpus-replay.sh` (reusable, isolated sandbox, forces full drain of all 46 real notebooks): pre-fix 21/46 deadlocked (superset of the 11 named — forced drain reaches sub-ties current content doesn't expose), post-fix 0/46. Isolated vote-logic diff across all 46 files' current real headings: 2 files show a vote-tally change (fewer phantom votes) but IDENTICAL final direction both ways — 0 regressions. AC-4 live convergence (RAW, real files): `unified-agent.md` 197L/25703B→90L/11562B, `digest-predict.md` 192L/51298B→33L/9508B, both under the live 12000B cap; retained sections verified byte-identical to pre-prune slices. `notebook-auto-prune.test.sh` 7/7 (T7 updated — old assertion tested the exact anti-pattern this closes), `test-notebook-auto-prune.sh` 9/9 (new T9: bash-vs-zsh identical outcome). No `apps/` TS/Go touched — `bun test`/`tsc` N/A. No MCP/gateway tool grant this session (Read/Edit/Write/Bash only) — board-flip via `scripts/orch-apply.sh` directly; `task_release`/`send_telegram` flagged for the coordinating `64c7c677-0f0f-4cee-a3ce-dba79d70b7ae` session. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md` STEP developer-S50.
