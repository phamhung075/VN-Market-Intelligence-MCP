# Developer — Notebook

**Last updated:** 2026-08-14T06:21:26Z | **Cycle:** UC-CDC-P7 Phase 2 partial (cross-service/, sprint ULTRACODE-AUDIT-FIXALL) — moved cowork-team Step 4.5/4.5c into cowork-match-slots.js, new cowork-tick-postflight.sh (last_fired+snapshot+retention sweep), I16 SSOT recipient field; Phase 1 file-collapse parked.

## Session 2026-08-14T02:15:00Z — FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR (cross-service/, developer, dev-team Ready-Lane Consumer dispatch, session 632721c2)

**Task:** P0 (severity-expedited twice, sat at ready[] position 47-48 for ~7h despite expedite) — `orch-conservation-check.mjs`'s `signal_total` magnitude-ratio floor folded `dev_team_idle_chain.pending_triage_inbox[]` alongside `signal_queue.rows[]`, but the inbox is a QUEUE meant to shrink to zero on every legitimate dev-team Step-1 triage pass, not an accumulating log — a single large legitimate clear tripped the same 0.5-ratio floor built to catch accidental mass-deletion, with the sole bypass (`ORCH_APPLY_ALLOW_SHRINK`) explicitly forbidden to this caller. Reproduced live TWICE: 29 envelopes/4 artificial sub-batch writes (2026-08-11), 248 envelopes/4 writes (2026-08-14) — the second occurrence also confirmed the row's own predicted self-reinforcing harm: a 2026-08-13 PO cycle triaged 147 envelopes but the blocked clear meant they were never removed, re-presented to every subsequent tick while 101 more piled on top.

**Fix (AC-1):** removed `pending_triage_inbox` from `signal_total` entirely (`signalTotal()` now sums only `signal_queue.rows.length` — that array's own floor is deliberately UNCHANGED, per the row's explicit instruction not to weaken it) and gave the inbox its own independent, never-bypassable per-envelope row-identity guard instead (`undeclaredInboxDrops()`), mirroring the already-shipped `signal_queue.rows[]` row-identity dimension one level down. New `ORCH_APPLY_DECLARED_INBOX_TRIAGED` env var (comma-separated `envelope_id`s) is the caller's "this id was legitimately triaged" marker — no archive-lane equivalent exists for this array, so the declared set is the only accounting mechanism. An undeclared drop (full wipe OR a single entry) is still hard-rejected, exactly matching AC-1's literal "every envelope_id ... with no corresponding marker" wording, not a weaker full-wipe-only special case.

**AC-2 (abort message):** the new inbox row-identity abort message explicitly tells the caller NOT to set `ORCH_APPLY_ALLOW_SHRINK` (forbidden + doesn't apply to row-identity anyway) and instead names `ORCH_APPLY_DECLARED_INBOX_TRIAGED` as the compliant path.

**Wiring:** the ONE sanctioned removal site — `docs/agents/dev-team/flow/main.md` § Step 1 "Durable-inbox CLEAR" — already computed `consumed_ids` before this task; it now also exports `consumed_ids_csv` and passes it through as the declaration, so a full clear-to-zero lands in ONE write with zero new computation, closing the artificial-sub-batching workaround entirely.

**Deviation from row's Files list (documented, not silent):** the row named `drain-signals.md`/`triage-signals.md` as touch points, but neither file owns the CLEAR write — both only APPEND to the inbox (drain-signals.md §0a-D/§0a-1) or route `pendingSignals[]` in-memory (triage-signals.md) — `dev-team/flow/main.md` § Step 1 is the sole real call site. Added cross-reference notes to both listed files pointing at the real mechanism instead of forcing an unused hook into them.

**Verify (AC-3):** `bash scripts/test/orch-apply-wrapper-tests.sh` → 89/89 PASS (75 pre-existing unaffected + 14 new — `INBOX-FULL-DRAIN-DECLARED` is AC-3's literal fixture: a 29-envelope inbox drained to 0 in ONE write through the full `orch-apply.sh` chain with `ORCH_APPLY_DECLARED_INBOX_TRIAGED` set and NO `ORCH_APPLY_ALLOW_SHRINK`, exit 0; plus `INBOX-DROP-UNDECLARED-REJECTED`/`INBOX-DROP-ALLOW-SHRINK-NO-BYPASS`/`INBOX-APPEND-HAPPY` negative/orthogonality/regression controls). `node scripts/agents-flow/drain-signals-durable.test.js` → 48/48 PASS (updated the 2026-08-09 "Conservation Guard Extension" cases in place — undeclared full/single-entry drops now REJECTED via row-identity, not the removed magnitude dimension; identical drops ACCEPTED once declared; 2 new cases). `bash scripts/test/orch-cold-evict-tests.sh` → 59/59 unaffected (`signal_queue.rows[]` dimension untouched). `bun test scripts/agents-flow/orch-state-hook.test.mjs` → 21/21 unaffected (hook-parity caller of `signalTotal()`). `bun tsc --noEmit` (apps/mcp-server) clean — no `apps/` files touched, ran as a sanity check per flow. `bash -n` clean on `orch-apply.sh` (comment-only changes).

**Docs:** `docs/policies/dev-standards.md` — marked the 2026-08-09 "durable pending-triage-inbox dimension" paragraph SUPERSEDED (kept, not deleted, per this repo's convention) + new CANONICAL "Inbox row-identity dimension" entry + corrected exit-code doc lines + corrected the test-harness CANONICAL block's stale "46/46" count to 48/48. `docs/agents/dev-team/flow/main.md` § Step 1 CLEAR block gains the `consumed_ids_csv`/env-var wiring + a size-justification header entry (1137→1152, +15L). `docs/agents/dev-team/flow/drain-signals.md` gains a cross-reference note (216→228, +12L). `docs/agents/po/flow/triage-signals.md` gains a cross-reference note (no header, file <120L). `docs/WORK.md` one-liner appended.

**Graphify:** SKIPPED, documented not silent — `/graphify docs --update --no-viz` requires Part B semantic-extraction parallel subagents (the skill's own spec), which needs the `Agent`/`Task` tool; this spawned session carries `Read/Edit/Write/Bash` only (the same structural no-Agent-tool gap this flow file's own header already names 4x). Corroborating evidence this is a standing gap, not new: `graphify-out/graph.json` mtime is 2026-05-23 — no developer session has actually run this step in ~3 months despite the mandate.

**Closeout:** 1 commit pending, pathspec-scoped (`scripts/orch-conservation-check.mjs` + `scripts/orch-apply.sh` + `docs/agents/dev-team/flow/main.md` + `docs/agents/dev-team/flow/drain-signals.md` + `docs/agents/po/flow/triage-signals.md` + `docs/policies/dev-standards.md` + `scripts/test/orch-apply-wrapper-tests.sh` + `scripts/agents-flow/drain-signals-durable.test.js` + `docs/WORK.md` + this notebook + decision journal). Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP developer-S105. Board flip → REVIEW + `.head` idle-reset in the same `orch-apply.sh` write per execute-tier.md's Status-Flip = Lane-Move rule (branch:null direct-execute).

---

## Session 2026-08-14T06:21:26Z — UC-CDC-P7 Phase 2 partial (cross-service/, developer, BOUNDED-1 auto-pickup, session 632721c2)

**Task:** SPRINT-L, RESCOPE — collapse the 12-file cowork-team flow to ~7 + push per-tick logic into scripts. Rescope note mandates Phase 2 (script moves) BEFORE the pressure-file merge (Phase 1) + I16 SSOT fix + TE-T03/TE-T13 coordination check. **Landed Phase 2 + I16 only; Phase 1 file-collapse explicitly parked, not silently dropped.**

**TE-T03/TE-T13 check:** both `DONE_VERIFIED` (TE-T03 2026-08-11, TE-T13 2026-07-13) before this session started — zero collision, `main.md` untouched.

**Phase 2a:** `cowork-match-slots.js` now applies Step 4.5 freshness-downgrade (`applyFreshnessDowngrade`, gatherer set derived from `parallel_group=="gatherers"`, no literal) + Step 4.5c CHEF mutex (in-process, both modes) at the tail of every call. `matchSlots()`'s array-return contract UNCHANGED (new `options.meta` out-param, additive-only — NFR-2-style). CLI stdout + `options.meta` gained `pressure_mode/downgraded/suppressed_cadence/chef_mutex_applied/due_reasons/cadence_minutes`. Threaded into `cowork-tick-preflight.sh`'s WORK verdict (`_emit_verdict` new optional 8th arg) + `match-slots.md`'s ERROR path. `pressure-cadence.md` Steps 4.5/4.5c marked SUPERSEDED (script does it now), historical incident record kept not deleted.

**Self-caught bug:** `${8:-{}}` in `_emit_verdict` — a literal `{}` inside a `${VAR:-default}` clause defeats bash brace-matching, silently appending a stray `}` after every non-empty 8th arg (corrupted the JSON envelope on every WORK verdict once I wired the passthrough). Found via the new tests, not narration. Fixed to `${8:-}` + `[ -z ] && extra="{}"`.

**Phase 2b:** new `cowork-tick-postflight.sh` — (a) `last_fired` batch write, verbatim delegation to existing `cowork-write-last-fired.js`, zero reimplementation; (b) cycle-snapshot assembly, same jq contract as `tick-snapshot.md`; (c) NEW `docs/signals/processed/cowork-team-*.json` >14d retention sweep (I13), scoped to already-`processedAt`-stamped files ONLY — deliberately narrower than a blind sweep to avoid the exact "unconditional processed/ prune" bug `drain-signals.js` was hardened against; `git rm` stages, does not commit (mirrors `purge-legacy-processed-signals.sh`).

**I16:** added `cowork_signal_recipient:true` to po/tran-ngoc-bau/unified-agent/alert-commander in `system-map.json` (never derivable from `type=="cowork"` — drops po+tran-ngoc-bau). Wired `cowork-tick-preflight.sh` Step 7 to read it (fail-safe fallback to the old literal on missing file — every pre-existing fixture still passes unmodified). Updated `work-tick.md` + `dispatch/SKILL.md` prose pointers + `system-map-query/SKILL.md`.

**Verify:** `cowork-match-slots.test.js` 43→69/69. `cowork-tick-preflight.test.sh` 58→67/67 (new T2c/T2d/T-SSOT). New `cowork-tick-postflight.test.sh` 28/28 — real isolated git-repo fixture (not mocked), incl. a `git rm` empty-leading-dir-prune gotcha caught mid-write (fixed via a permanent anchor file in the fixture). `cowork-chef-mutex.test.js`/`cowork-schedule-consistency.test.js`/`cowork-catchup-predicate.test.js`/`cowork-guaranteed-slot-firer.test.sh` all unaffected. `bun test`/`tsc` N/A — zero `apps/` TS/Go touched.

**Parked (explicit, not dropped):** Phase 1 file-merge (pressure-read.md+pressure-cadence.md→pressure.md, fold pressure-emit.md into telemetry.md, delete slot-claim.md's retired 4.6b block, merge last-fired.md into spawn-fanout.md, main.md JUMP-TO update) — NOT started. Wiring `last-fired.md`/`tick-snapshot.md` to actually CALL `cowork-tick-postflight.sh` — NOT done: Step 4.7 (snapshot) must run BEFORE spawn-fanout (Step 5), Step 5b (last_fired) AFTER — a single combined call site is not a drop-in; needs 2 separate invocations (SKIP_SNAPSHOT/SKIP_RETENTION flags already support the split) verified against a live dry-run tick, out of scope for this pass.

**Closeout:** commit pending, pathspec-scoped. Decision journal `sprint-ULTRACODE-AUDIT-FIXALL-developer.md` STEP developer-S24. Board flip → REVIEW + `.head` idle-reset in the same `orch-apply.sh` write per execute-tier.md's Status-Flip = Lane-Move rule (branch:null direct-execute), `status_note` documents the park items for the next pickup.

---
