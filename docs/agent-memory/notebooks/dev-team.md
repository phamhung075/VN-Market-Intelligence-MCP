# dev-team notebook

**Last updated:** 2026-08-07 | **Sprint:** current

> Archive: `docs/archive/notebooks/dev-team-2026-05-21.md` (full session history prior to 2026-05-21 trim)

## Current state (tick 2026-08-07T18:07Z) — run directly in router/main session (full tool binding incl. Agent/Task/mcp__gateway__*), NOT a spawned subagent — closes the nested-spawn gap tracked by FIX-DEVTEAM-CRON-TICK-NO-SPAWN-CAPABILITY-NO-ROSTER-AGENT-CARRIES-TASK-TOOL for this tick
- Preflight script (`dev-team-tick-preflight.sh`) verdict=RUN, SF-1+fire-election already held; also ran a cold-evict pass (4 items hot→cold, commit `f6de5523f`) as part of its own Step 5.5. Entered at gcc-preflight: no HEAD.lock, worktree prune clean, no expired worktree locks.
- Step 0a drain: `drain-signals.js` ran twice (2nd pass to pick up the CI probe's own emission, same-tick per flow intent) — 40 signals routed-to-po first pass (bctc routine ×4, bctc esc-deep-dive-request ×1, commit-sweep-guard ×5, context-bloat ×2, cowork-team telemetry ×18, notebook-direction-defaulted ×2, notebook-single-section-breach ×7, janitor-health-recheck ×1), 1 `ci_red` (HEAD `8b71ca51`, job=size-lint) on the 2nd pass. 50 aged processed/ files pruned, 6 referenced files correctly skipped. Committed `b0966b730` + `6296d67bb`, post-commit residual check = 0 both times.
- ESC-DISPATCH (bctc-analyst FPT/Q1-2026/ESC-5, severity=HIGH): GATE-A pass (HIGH floor met); GATE-B Tier-1 board-dedup hit (REFLOW-FPT- prefix / guard_key match already open on board) → TERMINAL-EXIT, no Opus spawn — recurrence already tracked.
- Step 0b: `.head` was idle (prior session's WF-1 reset at 06:26Z survived untouched all day — no dev-team tick ran between 06:26Z and this one, matching the earlier-diagnosed session-death gap). WIP=1 → BOUNDED-1 skipped (WIP not <1).
- **SLS claimed `FIX-INFRA-CRITICAL-VOLUME-PRESENCE-HEALTHCHECK` (supervised+plan_only, promoted at 04:14Z by an earlier tick, sat in ready[] ~14h) — did NOT dispatch.** Row's own `note`/`supervised_note` explicitly say "PLAN-ONLY: do NOT dispatch — schedule after ops confirms /pek-extract resumes"; found no evidence (notebooks/git-log) that precondition has been met. SLS's promote/claim scripts have no prose/precondition gate analogous to BOUNDED-1's dedicated PROSE-SEQUENCING GATE — a real structural gap, not a one-off. Reverted the claim by hand (row moved in_progress→ready[] with status=READY, claimed_at/claimed_by stripped, `.head` reset idle) rather than spawn ops against an explicit block. Filed `gate-design-gap` signal to po (`docs/signals/dev-team-sls-no-prose-donotdispatch-gate-20260807T182501Z.json`) recommending an architect-designed fix (structured `blocked_until` field, or SLS's own prose gate mirroring BOUNDED-1's).
- Continued to RLC (since the SLS revert meant nothing actually dispatched this tick — literal "JUMP TO end" only applies on a real SLS dispatch). RLC claimed + dispatched `FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER` (P0, pm-decomposed 2026-07-22, zone=apps/mcp-server/, PO 10x-corroborated 2026-08-06: `task_release` on orphan-signal rows returns `{ok:true,released:0}` — null-owner_client_session rows are structurally unreachable by any client session, both call sites in orphan-adoption.md are dead no-ops) → spawned `developer` in background (agent `adfd042ed7610db5d`), outer lock `task:FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER` held (no release on dispatch success, per lock-lifetime rule — TTL=3600s or head-leaves-in_progress reopens it).
- Committed board writes pathspec-scoped (`a2e95eb85`: orch-state.json + the new signal only) — left ~15 other agents' concurrently-dirty files (bctc-analyst/digest-predict/news-scout/unified-agent/system-auditor notebooks, analysis-briefs, auditor state files, fb-post, etc.) untouched, not my lane, avoids the sweep-guard class of bug. Sweep-guard fired a WARN (bare `git commit -m` after `git add`, no pathspec on the commit itself, even though only 2 files were staged) — landed correctly this time but the correct pattern going forward is `git commit -m "..." -- <files>` directly, no separate bare-commit step.
- Released SF-1 + fire-election cleanly at jump:end.
- Also ran the live-fired system-auditor Tier-1 (ALL_GREEN, fresh, no spawn) and Tier-2 (stale-green heartbeat 725min>480min → spawned system-auditor, returned 0 anomalies) crons this session, both session-scoped cron jobs I re-armed earlier after diagnosing the prior session's silent death (~06:26–06:28Z, ~11h gap, root cause unknown — see prior cycle).

## Lessons / patterns

- **When :3000 (mcp-server backend) is down, the MUTEX SYSTEM is down too — task_claim/task_release are vn-market tools served by that same backend.** So you cannot mutex-wrap an emergency recovery spawn (chicken-and-egg: the mutex can't return until the backend does, and the backend needs ops to return). Resolution: spawn a SINGLE ops agent WITHOUT mutex for backend recovery (no fleet-concurrency risk from one agent); the mutex requirement is a fleet-panic guard, not applicable to the one spawn that restores the mutex. A failed call_tool returning `connection refused :3000` (vs a structured `{"ok":...}`) is the tell that the backend — not just the tool — is down. Recovery = single-service `docker compose up -d mcp-server` (verify host healthy first: SIGTERM signal=15 + mem-free ≠ OOM = false-positive destructive stop, AUD-ND-1 class), NEVER `up -d` the whole project (host-panic).
- **Verify the DEPLOY TARGET, not just that the code is correct — a guard on the wrong deployer protects nothing.** GUARD-1/2/3 passed every local check (bash -n, deliberate-violation exit-1, clean-render, QA 6/6) but were added to `scripts/deploy-vps-proxy.sh` (`${VULTR_IP}`) while the LIVE host is Vinahost via `scripts/deploy-vinahost.sh` (memory: Vultr decommissioned 2026-04-13). PO's design-phase "canonical=deploy-vps-proxy.sh" + QA's "TE handled by vinahost, non-blocking" were both plausible-but-unchecked; relaying either would have shipped a guard that leaves the live deploy path unguarded + the TE script's `__TE_API_KEY__` un-converted. When a repo has >1 deployer/host, the FIRST raw check is "which one targets the live host" (grep both for target IP/host + which scripts each ships) before trusting any "canonical deployer" claim. Cousin of [[feedback_router_verify_raw_not_badges]].
- **A "compress-to-pointer" move is only safe if the pointer TARGET exists — verify the destination, not just the line count.** pm compressed a sprint block in TASKS.md (90→80L, cap met = LOOKED green) but the "full items in SPRINT_GOAL.md §X" pointer it added resolved to NOTHING (section never written) → 5 backlog item bodies (ACs/triggers/routing) survived only in git history. `wc -l ≤cap` is NOT proof of a lossless compress; `grep` the pointer target for the moved IDs before trusting it. Same false-green family as [[feedback_fence_false_green]] (config reports 0 errors while checking nothing).
- **A recon's INFERRED root cause is a hypothesis, not fact — make the fixer verify-raw before acting on it.** ops-vps-fetch recon read "boot-1/boot-2 08:57Z" log lines and I inferred VPS-reboot-killed-socat; passed that to ops as the working hypothesis. ops verify-raw'd (uptime 48d, socat alive via curl 200) and found the REAL cause (unrendered `__MCP_BASE__`/`__API_KEY__` placeholders). The wrong hypothesis cost nothing because ops re-verified instead of blindly restarting socat. Never relay a recon's suggested root-cause bucket as settled.
- **A deploy that copies a repo template can silently clobber a rendered live config — green code tests don't cover it.** cafef sprint (814088b0) fixed is_blocked() (verified cafef 0→20), but the SAME deploy overwrote /root/fetch-vn-news.sh's rendered API_URL/API_KEY lines with raw `__PLACEHOLDER__` template → ~1h push outage. The code fix was correct; the DEPLOY-RENDER step was skipped. Lesson: any VPS-script deploy needs a pre-deploy placeholder-leak guard (reject `__[A-Z_]+__` in deployed artifacts) + render step; prefer `${VAR:-default}` env-fallback form (foreign-flow has it, fetch-vn-news.sh didn't). Filed VPS-DEPLOY-PLACEHOLDER-GUARD.
- **brief_complete can arrive AFTER its own impl already shipped same-session** — the context-resume-economy brief was fully implemented (b38ac812) before its signal was even drained; PO raw-verified current code state and caught it instead of re-dispatching done work. Always verify the brief's claimed pre-state against live code before scoping.
- **A SKILL/flow file can breach its cap *because* a fresh feature added load-bearing content** — the fix is lazy-load extraction (pointer the bodies to a sibling .md), NEVER a naive prune that would delete a live contract. Route .md cap fixes through architect→agent-father (agent-md-factory), not code CLEAN→qa.
- **VERIFY A SUB-AGENT'S COMMIT BY READING THE RAW DIFF — a "1-line" task can produce a multi-line secret leak (2026-06-01, VPS-DEPLOY T6).** fixer was told "edit ONE line of .env, no `-A`/`-f`"; it `git add -f`'d the gitignored .env and committed the WHOLE file (21 lines, all secrets incl. Vinahost root pw) into the tip, then self-reported "DONE, 1 line, minimal." `git show --stat <commit>` showed `new file mode 100644`/`--- /dev/null` → proof it was a fresh add of a gitignored secrets file, NOT a 1-line edit. Containment clean ONLY because unpushed (139-ahead): `reset --soft HEAD~1` + `git rm --cached .env` + confirm `git log --all -- .env` empty → purged, no rotation. LESSONS: (1) a sub-agent that rationalizes around an explicit constraint ("given the requirement to commit, I used -f") is a red flag — that constraint existed to stop exactly what it did; (2) ALWAYS `git show --stat`+diff a sub-agent commit before trusting its summary, especially post gate-reject; (3) `new file mode` on a path you expected to already exist = force-added-past-gitignore tell; (4) durable fix lives in the agent flow not the incident → FU-FIXER-NO-FORCE (HIGH, agent-father). Kin of [[feedback_router_verify_raw_not_badges]] + the no-`--force` binding.

- Worktree harness auto-merge works when tracks are disjoint (no conflicts).
- PO file/line refs are advisory — source-of-truth is grep. Developer must verify before blind-edit.
- BA spec discoveries can downgrade priorities.
- MCP gateway outage pattern: Docker Desktop virtiofs socket deadlock. Only USER action (Docker Desktop restart + extra_hosts) can break the cycle.
- Zero-MCP parallel tracks: When gateway down, frontend zone is only productive work.
- HEAD.lock cure pattern: ~21min average stale lock cleared cleanly. age>0, size=0B, no live pid → safe rm.
- **Auditor staleness alerts get lazily bucketed** — #3020 "VPS proxy stale" was auto-attributed to the known VPS-SOCAT-PERSIST, but raw diagnosis (socat alive + /health 200) found 3 unrelated VPS-side root causes. Always root-cause a stale-data alert from the upstream end, don't trust the auditor's suggested bucket.
- **Spawned-agent gateway wedge is lane-specific:** ops-vps-fetch (Bash/SSH only, no MCP gateway tool) executes fine when spawned, even in sessions where gateway-dependent code agents (po/dev-*) are wedged. Route gateway-independent infra work to the SSH lanes when the gateway-wedge is suspected.

## Carry-over

- **1928a-mcp-gateway-dns-extra-hosts** (URGENT-F1): Docker Desktop restart required. Unblocks 1929a/1930a/1930b/1930c/1922i.
- **1862c-E-dashboard** (HIGH): Cloudflare dashboard SSE ingress — user action needed.
- **1897b** (HIGH): Docker .git/ exclusion — user action needed.
