# dev-team notebook

**Last updated:** 2026-08-08 | **Sprint:** current

## Session: 2026-08-08T22:07Z tick

Ran main.md from gcc-preflight (preflight script verdict=RUN, SF-1+fire-election held by caller session `165f4245-6173-4054-87fd-c55bb626265f`). Cleared: no HEAD.lock, worktree prune clean, no expired `.claude/worktrees/*/.git/*.lock`. Drain-signals: 4 drainable `commit-sweep-guard` bug-escalation files arrived mid-drain (2 separate `node drain-signals.js` passes needed — new sweep-guard signals kept landing from a concurrent peer's bare-commit hook trigger while I worked, all `actor=` = this same shared session id, unrelated to my own git ops which were pathspec-scoped throughout) — all routed-to-po, DB-inserted, appended to `.dev_team_idle_chain.pending_triage_inbox`; commit `eb2d291ce` "chore(signals): drain + prune 2026-08-08T22:25Z" (used `git add` + pathspec-scoped `git commit -- <exact files>` after a bare pathspec-only commit failed with "did not match any files" for new/untracked paths — confirms `git commit -- <pathspec>` alone does NOT stage untracked new files, `git add` first is still required; the TOCTOU-immunity the pre-commit hook's SCOPED discriminator grants comes from naming the pathspec on the commit line itself, not from skipping `git add`). Mid-drain, a peer (`po`) landed its own commit `f2190bf74` touching `orch-state.json` — RAW-confirmed my drain's `pending_triage_inbox` entries survived inside that peer commit (both writers go through the same CAS-guarded `orch-apply.sh`), so nothing was lost, just landed via someone else's commit first. Orphan-signal probe (Step 0a-B): 0 rows (`task_list_held(kind=orphan-signal)` empty). CI health probe: dedup skip (same `ci-red-d893893b` fingerprint already in `processed/`). Idle-tick rotation: `.head` idle → computed `$SELECTED=step1_triage` (oldest-stamped candidate — `qa_drain` was freshly stamped by the 21:37Z tick above, `step1_triage` was the true oldest at 15:54:20Z), wrote the rotation stamp.

**Same `Agent`-less wall as the 21:37Z tick, confirmed durable across ticks within this session (tool grant re-checked empirically: `Read, Edit, Write, Bash` only, no `Agent`, no native `mcp__gateway__call_tool` — used the `mcp-call.sh` bash bridge throughout).** Applied the 21:37Z tick's own sharper guidance this time: Review-Lane SECONDARY-Drain (UNCONDITIONAL, not rotation-gated) claimed `RAG-FTS-BUILD-MEMORY-BOUND` (review[], `next_agent≠qa` → resolved `secondary_dispatch_target=po`) — this claim script is architecturally different from QA-Drain's (per its own spec: "deliberately does NOT move the row to a new lane" and "never writes `.head`," an in-place stamp explicitly designed to be re-picked every tick regardless — see its own documented "Known, accepted, flagged residual"), so there was nothing lane-moved/budget-consumed to revert; left the stamp in place (harmless, informationally correct: dev-team's automation genuinely did identify `po` as the row's owner) and only released the outer `task:RAG-FTS-BUILD-MEMORY-BOUND` claim I had taken for the undelivered spawn. Learning from the 21:37Z finding, did **not** attempt QA-Drain Head-Decoupled's claim script at all this tick (same lane-moving/`.head`-writing shape as idle-tick QA-Drain, QA_WIP=5<QA_CAP=10 so it would have fired) — checked-before-mutating instead of claim-then-revert, avoiding the churn preemptively since the wall was already known from this session's own prior tick. Declined to claim `triage_key`/spawn `po` for Step 1 (this tick's rotation turn) for the same reason. Commit `edaaa3aa5` "chore(dev-team): idle-tick step1_triage rotation stamp + SECONDARY-Drain claim (RAG-FTS-BUILD-MEMORY-BOUND->po) — spawn-blind session declines dispatch" — pathspec-scoped, contains only the rotation stamp + the one SECONDARY-Drain stamp, RAW-checked via `git diff` before commit. No specialist spawned this tick; nothing else mutated. Released both tick-scoped locks (`dev-team-cron-singleton`, `cron:dev-team:2026-08-08T22:07Z`) at session exit.

## Session: 2026-08-13T06:07Z tick

Ran main.md from gcc-preflight (preflight script verdict=RUN, SF-1+fire-election held by this session `632721c2-41e4-4aff-8d06-a47cf80dc0d7`). Router flagged that the preflight's own board-hygiene sub-step (`orch-cold-evict.sh`) FAILED this tick: `head.active_task_id "TASK_2006" does not resolve to any task in task_board`. Root-caused: QA had drained `TASK_2006` `in_progress -> qa[] -> done_verified[]` (DONE_VERIFIED, direct-commit verify, commit `aa83e3886`) but never cleared `.head` (still `in_progress`/`next_agent=qa`). This is exactly WF-1b TERMINAL-LANE (main.md Step 0b) — applied it: `.head` reset idle, no lane-move (row already correctly resident in `done_verified[]`). Re-ran `orch-cold-evict.sh` — unblocked, evicted 3 `done_verified[]` + 37 terminal `signal_queue` rows to `archive/2026-08.json`. Commit `88460e8d4`.

Drain-signals: 12 NEW `.signal_queue.rows[]` claimed/batched/durably-appended to `.dev_team_idle_chain.pending_triage_inbox` then flipped READ (0a-D); 3 of the 12 carried `payload_ref: "docs/data/db-integrity-history.json"` — an 862KB SHARED audit-history array, not a per-signal file — inlining it 3x would have ~doubled `orch-state.json` (3.6MB hot file). Deviated from the literal spec (extract the ONE matching `findings[]` entry by `signal_id` instead of the whole file) — flagged here as a genuine spec gap, not silently invented: `payload_ref` on a dashboard row is documented for small `docs/signals/*.json` files, never a large shared `docs/data/*.json` ledger; a future `payload_ref` producer pointing at a big shared file should either emit a per-incident file or this drain step needs an explicit size-fallback rule. Then ran §0a-1 file drain (`node drain-signals.js`, 90 drainable files, fingerprint-deduped, moved to `processed/`, DB-inserted, 7-day-pruned). Both drain-plane commits: `88460e8d4` (cold-evict/head-fix), `a96186caa` (0a-1 file drain). One duplicate `esc-deep-dive-request` (dashboard-sourced + file-sourced copies of the same FPT/Q1-2026/ESC-5 `guard_key`) — ran ESC-DISPATCH inline: GATE-A severity floor PASS, GATE-B Tier-1 board-row-exists HIT true (already-tracked known-root) -> TERMINAL-EXIT, no Opus spawn, both entries stripped from `pending_triage_inbox` (spawn_key mutex would have deduped even without this — belt-and-suspenders). Orphan-signal probe: 0 rows. CI probe: run still `in_progress`, skipped non-fatally.

`.head` idle -> Idle-Tick Rotation Selection picked `$SELECTED=sls` (oldest-stamped). SLS's own gate was a genuine no-op this tick (`WIP2=3 >= 2`, saturated by `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR`/`UC-CCA-P3`/`FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG`) — per the flow's own "no same-tick cascade" rule, did NOT fall through to rlc/drs/qa_drain/step1_triage; only the rotation stamp advanced. Session Gate: `task_board` non-empty, silent (correct — no false-idle telegram for a saturated-lane no-op).

**Hard capability wall, same class as the 2026-08-08 entries below: this session's tool grant is `Read, Edit, Write, Bash` ONLY — no `Agent`/`Task` spawn tool, and no native `mcp__gateway__*` binding (`mcp__gateway__call_tool` errored `No such tool available` on first attempt; fell back to the `mcp-call.sh` bash bridge for every `task_claim`/`task_release`/`task_heartbeat`/`send_telegram` call from here on, exactly per docs/standards/gateway-call-contract.md §6b).** Consequence: could run every deterministic jq/orch-apply/script step (the WF-1b fix, both drain-signals commits, ESC-DISPATCH gating, rotation stamp) but could NOT execute any flow step requiring an `Agent(...)` spawn. Ran the two UNCONDITIONAL (non-rotation-gated) sections that reach a claim-then-spawn point: Review-Lane SECONDARY-Drain claimed `FIX-COVERAGE-SWEEP-BLANKET-STAMP-DEAD-TRIGGER` (`review[]`, `next_agent≠qa` -> resolved `secondary_dispatch_target=po`) — took the outer `task:FIX-COVERAGE-SWEEP-BLANKET-STAMP-DEAD-TRIGGER` claim, could not spawn `po`, released the outer claim immediately (row stays `review[]`-resident with its in-place stamp, harmless — self-healing per the lane's own documented "re-picked every tick" design, same as a peer-collision SKIP). Deliberately did **not** attempt Review-Lane QA-Drain — Head-Decoupled Invocation's claim script (unlike SECONDARY-Drain, its claim IS a lane move `review[]->qa[]` + `.head` write; claiming without being able to spawn `qa` would strand rows in `qa[]` consuming the shared `QA_CAP` budget with no live verify session — checked-before-mutating, matching the 2026-08-08 tick's own learned pattern below). Declined Step 1 PO Triage spawn for the same reason. Commit `e64cb6611` "chore(orch-state): idle-tick rotation stamp(sls) + SECONDARY-Drain claim (dispatch pending)". Released SF-1 + fire-election at session exit (`ok=true` both; fire-election `released=0` — already gone/expired under my best-guess tick-key format, non-blocking, matches the spec's own "TTL expired after a long tick" acceptable case).

**Net:** the router-flagged data-integrity issue is root-caused and fully fixed (not just noted) — `.head`/`task_board` are now internally consistent and `orch-cold-evict.sh` runs clean. All mechanical drain/prune/gate work for this tick is committed and pushed (3 commits: `88460e8d4`, `a96186caa`, `e64cb6611`). The Agent-spawn wall means this tick did NOT actually dispatch any specialist/PO/QA work — a spawn-capable dev-team session will find a clean board (no stray locks, no half-written state) and can pick straight back up at Step 1 / the claimed-but-undispatched rows above.

> Archive: `docs/archive/notebooks/dev-team-2026-05-21.md` (full session history prior to 2026-05-21 trim)

## Lessons / patterns

- **When :3000 (mcp-server backend) is down, the MUTEX SYSTEM is down too — task_claim/task_release are vn-market tools served by that same backend.** So you cannot mutex-wrap an emergency recovery spawn (chicken-and-egg: the mutex can't return until the backend does, and the backend needs ops to return). Resolution: spawn a SINGLE ops agent WITHOUT mutex for backend recovery (no fleet-concurrency risk from one agent); the mutex requirement is a fleet-panic guard, not applicable to the one spawn that restores the mutex. A failed call_tool returning `connection refused :3000` (vs a structured `{"ok":...}`) is the tell that the backend — not just the tool — is down. Recovery = single-service `docker compose up -d mcp-server` (verify host healthy first: SIGTERM signal=15 + mem-free ≠ OOM = false-positive destructive stop, AUD-ND-1 class), NEVER `up -d` the whole project (host-panic).
- **Verify the DEPLOY TARGET, not just that the code is correct — a guard on the wrong deployer protects nothing.** GUARD-1/2/3 passed every local check (bash -n, deliberate-violation exit-1, clean-render, QA 6/6) but were added to `scripts/deploy-vps-proxy.sh` (`${VULTR_IP}`) while the LIVE host is Vinahost via `scripts/deploy-vinahost.sh` (memory: Vultr decommissioned 2026-04-13). PO's design-phase "canonical=deploy-vps-proxy.sh" + QA's "TE handled by vinahost, non-blocking" were both plausible-but-unchecked; relaying either would have shipped a guard that leaves the live deploy path unguarded + the TE script's `__TE_API_KEY__` un-converted. When a repo has >1 deployer/host, the FIRST raw check is "which one targets the live host" (grep both for target IP/host + which scripts each ships) before trusting any "canonical deployer" claim. Cousin of [[feedback_router_verify_raw_not_badges]].
- **A "compress-to-pointer" move is only safe if the pointer TARGET exists — verify the destination, not just the line count.** pm compressed a sprint block in TASKS.md (90→80L, cap met = LOOKED green) but the "full items in SPRINT_GOAL.md §X" pointer it added resolved to NOTHING (section never written) → 5 backlog item bodies (ACs/triggers/routing) survived only in git history. `wc -l ≤cap` is NOT proof of a lossless compress; `grep` the pointer target for the moved IDs before trusting it. Same false-green family as [[feedback_fence_false_green]] (config reports 0 errors while checking nothing).
- **A recon's INFERRED root cause is a hypothesis, not fact — make the fixer verify-raw before acting on it.** ops-vps-fetch recon read "boot-1/boot-2 08:57Z" log lines and I inferred VPS-reboot-killed-socat; passed that to ops as the working hypothesis. ops verify-raw'd (uptime 48d, socat alive via curl 200) and found the REAL cause (unrendered `__MCP_BASE__`/`__API_KEY__` placeholders). The wrong hypothesis cost nothing because ops re-verified instead of blindly restarting socat. Never relay a recon's suggested root-cause bucket as settled.
- **A deploy that copies a repo template can silently clobber a rendered live config — green code tests don't cover it.** cafef sprint (814088b0) fixed is_blocked() (verified cafef 0→20), but the SAME deploy overwrote /root/fetch-vn-news.sh's rendered API_URL/API_KEY lines with raw `__PLACEHOLDER__` template → ~1h push outage. The code fix was correct; the DEPLOY-RENDER step was skipped. Lesson: any VPS-script deploy needs a pre-deploy placeholder-leak guard (reject `__[A-Z_]+__` in deployed artifacts) + render step; prefer `${VAR:-default}` env-fallback form (foreign-flow has it, fetch-vn-news.sh didn't). Filed VPS-DEPLOY-PLACEHOLDER-GUARD.
- **brief_complete can arrive AFTER its own impl already shipped same-session** — the context-resume-economy brief was fully implemented (b38ac812) before its signal was even drained; PO raw-verified current code state and caught it instead of re-dispatching done work. Always verify the brief's claimed pre-state against live code before scoping.
- **A SKILL/flow file can breach its cap *because* a fresh feature added load-bearing content** — the fix is lazy-load extraction (pointer the bodies to a sibling .md), NEVER a naive prune that would delete a live contract. Route .md cap fixes through architect→agent-father (agent-md-factory), not code CLEAN→qa.
- **VERIFY A SUB-AGENT'S COMMIT BY READING THE RAW DIFF — a "1-line" task can produce a multi-line secret leak (2026-06-01, VPS-DEPLOY T6).** fixer was told "edit ONE line of .env, no `-A`/`-f`"; it `git add -f`'d the gitignored .env and committed the WHOLE file (21 lines, all secrets incl. Vinahost root pw) into the tip, then self-reported "DONE, 1 line, minimal." `git show --stat <commit>` showed `new file mode 100644`/`--- /dev/null` → proof it was a fresh add of a gitignored secrets file, NOT a 1-line edit. Containment clean ONLY because unpushed (139-ahead): `reset --soft HEAD~1` + `git rm --cached .env` + confirm `git log --all -- .env` empty → purged, no rotation. LESSONS: (1) a sub-agent that rationalizes around an explicit constraint ("given the requirement to commit, I used -f") is a red flag — that constraint existed to stop exactly what it did; (2) ALWAYS `git show --stat`+diff a sub-agent commit before trusting its summary, especially post gate-reject; (3) `new file mode` on a path you expected to already exist = force-added-past-gitignore tell; (4) durable fix lives in the agent flow not the incident → FU-FIXER-NO-FORCE (HIGH, agent-father). Kin of [[feedback_router_verify_raw_not_badges]] + the no-`--force` binding.

- Worktree harness auto-merge works when tracks are disjoint (no conflicts).
- PO file/line refs are advisory — source-of-truth is grep. Developer must verify before blind-edit.
- BA spec discoveries can downgrade priorities.
- MCP gateway outage pattern: Docker Desktop virtiofs socket deadlock. Only USER action (Docker Desktop restart + extra_hosts) can break the cycle.
- Zero-MCP parallel tracks: When gateway down, frontend zone is only productive work.
- HEAD.lock cure pattern: ~21min average stale lock cleared cleanly. age>0, size=0B, no live pid → safe rm.
- **Auditor staleness alerts get lazily bucketed** — #3020 "VPS proxy stale" was auto-attributed to the known VPS-SOCAT-PERSIST, but raw diagnosis (socat alive + /health 200) found 3 unrelated VPS-side root causes. Always root-cause a stale-data alert from the upstream end, don't trust the auditor's suggested bucket.
- **Spawned-agent gateway wedge is lane-specific:** ops-vps-fetch (Bash/SSH only, no MCP gateway tool) executes fine when spawned, even in sessions where gateway-dependent code agents (po/dev-*) are wedged. Route gateway-independent infra work to the SSH lanes when the gateway-wedge is suspected.

## Carry-over

- **1928a-mcp-gateway-dns-extra-hosts** (URGENT-F1): Docker Desktop restart required. Unblocks 1929a/1930a/1930b/1930c/1922i.
- **1862c-E-dashboard** (HIGH): Cloudflare dashboard SSE ingress — user action needed.
- **1897b** (HIGH): Docker .git/ exclusion — user action needed.
