# Architect — Notebook

**Last updated:** 2026-08-14 11:07 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-14T11:07Z — FIX-READYLANE-NO-SEVERITY-EXPEDITE-FIFO-BURIES-INCIDENT-P0 (P0, `cross-service/`, dev-team Ready-Lane Consumer dispatch)

**Task:** Design a severity/incident expedite path for `ready[]` dispatch. Row's own `po_measurement_20260814` field PARTIALLY REFUTED the row's own title before design started: PO re-measured live and found a row already severity-expedited to the FRONT of its priority class still was not dispatched ~7h later, and a separately-promoted row sat 24 days unactioned at position 51/88 — concluding the binding constraint is THROUGHPUT (RLC claims one row per invocation, runs only on the ~1-in-6 idle ticks the 6-way rotation selects `"rlc"` for), not comparator/ordering. Mandated: adjudicate the row's 3 candidate shapes (a/b/c) against "would this have unblocked either measured case," propose a shape outside the list if none pass.
**Findings:** Independently reproduced PO's refutation live, not trusted from the row's own text: replayed RLC's own exact selection algorithm (`sort_by([priority_rank, idx])` over the full `devteam-eligibility.jq` gate chain) against the live board and found BOTH live `po_expedited_at`-carrying `ready[]` rows already rank #1/#2 of RLC's own 68-row eligible candidate set, yet remain undispatched 10h+ stale — a third, independently-measured live data point proving perfect ordering cannot unblock a row that is already unbeatable on ordering grounds. Confirmed root cause at the mechanism level: dev-team's own cron is hourly (`7 * * * *`, singleton-guarded), Idle-Tick Rotation Selection (main.md §552) picks exactly ONE of 6 candidates per tick with an explicit "no same-tick cascade" rule, so RLC gets a turn roughly once per 6 hourly ticks, additionally gated by the shared `WIP≤2` budget it shares with SLS/BOUNDED-1/DRS. Candidates (a) expedite-field-as-comparator and (b) age-weighted tiebreak both FAIL the "would this have unblocked either case" test by this live demonstration (both are pure ordering changes, and ordering was never the actual constraint). Candidate (c) PASSES: reused the in-repo QA-Drain precedent (`docs/architecture-briefs/2026-07-29-qadrain-head-slot-decouple.md` + `2026-08-06-review-lane-qadrain-throughput-unblock.md`), which fixed a structurally identical rotation-throttled single-claim starvation via (independent budget outside shared WIP≤2) + (batch-claim N-per-invocation) + (a second, head-decoupled, unconditional-every-tick invocation site) — re-measured its live result this cycle too: `review[]` PRIMARY count is 56 today, down from 226 at the fix's own 2026-08-06 baseline (269 peak), a real ~4x drain over 8 days of continued inflow. Designed the mirrored fix, "Incident-Lane Consumer" (ILC): reuses the ALREADY-LIVE `po_expedited_at`/`po_expedited_by` PO convention (5 live rows today, previously read by zero code) as a LANE-SELECTOR gate (not a new `expedite_at`/`incident_ref` comparator field, and not a sort-key ahead of `priority_rank` — severity changes throughput priority, never bypasses the supervised/plan_only safety gate); new independent `INCIDENT_CAP=2` budget (`incident_wip_in_progress`, a `claimed_by`-keyed exclusion mirroring `wip_in_progress`'s own BLOCKED/TERMINAL exclusion — deliberately reuses the existing `in_progress[]` lane rather than adding a 10th `TaskBoardSchema` lane, since that schema is `.strict()` on exactly 9 enumerated lanes); single head-decoupled invocation site (Session-Gate→Step-1 anchor, placed FIRST among the 3 unconditional blocks, ahead of SECONDARY-Drain/QA-Drain-Head-Decoupled) — no 7th rotation-candidate slot (would only shrink the throttle to 1-in-7, not remove it). Reuses the exact proven `$head_free` conditional-`.head`-write idiom verbatim (RLC/SLS/DRS/QA-Drain) — zero new write pattern, keeping the single-linear head-writer collision-freedom proof intact per the task's hard constraint. Flagged the benign RLC/ILC overlap explicitly (not silently absorbed): safe by the same "already removed from `ready[]` by a predecessor in the same sequential `orch-apply.sh` write" argument every sibling consumer's own header already relies on.
**Output:** `docs/architecture-briefs/2026-08-14-readylane-incident-lane-throughput.md`. Board row (`task_board.in_progress[]`) updated via `orch-apply.sh`: `next_agent="pm"`, `architect_completed_at`, `architect_review_note` — stayed in `in_progress[]`, no lane move; `.head.next_agent` resynced to `"pm"` in the SAME write (head-nextagent-write-coherence convention, `docs/architecture-briefs/2026-08-14-devteam-head-nextagent-write-coherence.md`, applied proactively even though that row's own WF-2b runtime check has not shipped yet).
**Next:** pm mints 2 implementation rows per brief §5 — `FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-SCRIPTS` (developer, no `depends_on`: new `is_po_expedited`/`incident_wip_in_progress` defs in `scripts/lib/devteam-eligibility.jq`, new `scripts/devteam-backlog-claim-incident-lane-consumer.jq`, satisfiability-instrument extension) then `FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-MAINFLOW` (agent-father, `depends_on:["FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-SCRIPTS"]` — a HARD dependency here, unlike QA-Drain's own optional one, since the main.md call site references a script file that does not exist until SCRIPTS ships: new ILC section + SECONDARY-Drain intro cross-ref + Reusable Scripts bullet + Invariants clause). Post-deploy: re-check whether the 2 live `po_expedited_at` `ready[]` rows finally dispatch within the next couple of hourly ticks — the row's own empirical DoD.

## 2026-08-14T02:29Z — FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING (P0, `cross-service/`, BA-spec handoff — router corrected stale `dispatch_lane="ba"` to architect, BA already complete)

**Task:** Ratify BA's TRACE (drop = Step 4 thesis-build, primary, + 3 compounding gaps) and its 7 exact before/after flow-doc edits (FR-0..FR-7, `chef.md`+`chef-dish.md`, zero new code) that wire Step-0-gathered `bctc_signal_*` business-context into per-ticker `conviction_calls[]` rationale. Resolve or route BA's one open blocker (opportunistic-vs-force-widen ticker coverage).
**Findings:** Re-read all 10 anchor quotes in BA's §2 against the LIVE files byte-for-byte (not trusted from the spec) — all 7 FRs sound, content unchanged. Found real line-number drift: `chef-dish.md` +44L / `chef.md` +5L since BA wrote the spec, from an intervening unrelated commit (`c31ee006e`, 2026-08-13 Checkpoint-pointer insert) — published a corrected live line map so agent-father doesn't patch the wrong spot (e.g. FR-6's `BIZ_CTX_OK` is now at `chef-dish.md:535-537`, not `:490-492`). Repo-wide grep for `BIZ_CTX` confirms zero non-flow-doc consumers — genuinely zero application-code blast radius. Blocker Q1 ruled within remit: FR-3 already bakes in BA's safe opportunistic-only default (NFR-3), so it doesn't block execution — PO veto stays available as a follow-up, not a rework. Precedent check: `FIX-CHEF-QUALITY-VERDICT-FALSE-FULL-NO-LAYER-ASSERTION` (`ready[]`, same zone) already carries `next_agent=agent-father` unshipped, confirming direct architect→agent-father routing (no PM) is an established pattern for this file family, not novel.
**Output:** `[Architect] Brownfield Findings` appended to `docs/handoffs/FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING-BA-spec.md` (corrected line map, blast-radius check, DDD/BUILD-STANDARD classification, blocker ruling, fan-out rationale). Board row updated via `orch-apply.sh`: `next_agent="agent-father"`, `architect_completed_at`, `architect_review_note` — stayed in `in_progress[]`, no lane move.
**Next:** agent-father executes FR-0..FR-7 directly (single wave, 2 files, 10 edit sites, no `depends_on` graph — no PM decomposition step). Verification gate (per row): next chef dish must persist `known_gaps[]` without the biz-ctx gap token AND ≥1 `conviction_calls[]` rationale must carry a `business_context_cited` field traceable to a `bctc_signal_*` read that cycle — RAW-verify the synthesis JSON, not the notebook self-report.

## 2026-08-14T11:05Z — FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58 (P1, `multi`, dev-team Review-Lane SECONDARY-Drain — BA spec complete, Blocker Q1 open)

**Task:** BA's spec (FR-1..FR-8, remedy trade-off table reframing PO's 3 candidates through a §0 durability finding: watchlist table proven non-durable, 5 SQLite corruption incidents) flagged Blocker Q1 (BA lacks gateway access, could not re-verify live `get_watchlist` — RPC-verified only via `docker exec` at the DB layer) for architect to re-check before finalizing design.
**Findings:** Confirmed architect also lacks `mcp__gateway__call_tool` (own tools_package has no gateway grant — live call errored `No such tool available`, matching BA's exact gap). Re-verified Blocker Q1 via an independent methodology anyway: `docker exec` + `bun:sqlite` readonly query direct against the live container's `market.db` (bare readonly open hit `SQLITE_BUSY` — needed `PRAGMA busy_timeout`, a reusable finding for whatever audit script ships). Result: **CONFIRMS 34** — DB, `system-map.json`, and (went one step further than BA) the frontend `WATCHLIST_STOCKS` constant are all three byte-identical, zero-diff every direction, right now. No live 41%-hole exists today (EC-1's outcome). Root cause confirmed at source (`watchlist.ts` add/remove = plain DB writes, zero file write-back; `seedWatchlist()` = INSERT/UPDATE-only, never DELETEs) — BA's one-way-sync diagram is exactly right. Designed candidate (i′) write-through (new `systemMapWatchlistWriter.ts`, reuses `alertVerdictStore.ts`'s existing atomic tmp+rename pattern; flagged a schema gap BA's plan missed — `add_to_watchlist` has no free-text `sector` param, so a domain→sector reverse-derivation would be lossy, recommend adding one) + candidate (ii) audit script (reuses `computeWatchlistDiff`/`loadSsotWatchlist`, already exported from the 2026-07-11 migration script; considered extending `db-integrity-probe.sh`, rejected — different concern, change-detection pre-gate vs correctness audit). Found `domain-model.md`'s watchlist count doc genuinely stale (33/30+3 vs live 34/33+1); technical-analysis's two docs already accurate.
**Output:** `[Architect] Brownfield Findings` appended to `docs/handoffs/FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58-BA-spec.md`. Board row (`task_board.review[]`) updated via `orch-apply.sh`: `next_agent="pm"`, `architect_completed_at`, `architect_review_note` flagging AC-2 for PO re-scope (ACB/CTG/MBB/VPB exist in none of the 3 planes today — restoring them is a content decision via `add_to_watchlist`, not a code fix).
**Next:** pm decomposes into 3 subtasks: (1) `apps/mcp-server/` write-through hook + optional `sector` schema field → dev-mcp-server; (2) `scripts/` audit script + cron wiring → developer/dev-mcp-server, `depends_on` (1); (3) `domain-model.md` 1-line count fix → generic developer, no dependency, can land first. Route AC-2 rescope question to PO before QA sign-off.
