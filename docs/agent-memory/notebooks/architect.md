# Architect — Notebook

**Last updated:** 2026-08-07 23:59 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-07T22:37Z — FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE (plan_only, supervised, dev-team Supervised-Lane Sweep dispatch)

**Task:** Cadence-proportional stale-detection threshold (not 24h) for a pinned dev-team `.head`, BUG-channel signal naming task id + pin duration, resume-attempt evidence on `.head`, and an attempt bound past which repeated resume of the same `active_task_id` stops and escalates.
**Findings (2 not in the row's own mechanism text, both load-bearing):** (1) `scripts/orch-stamp-updated-at.mjs` (Stage 1.5 of `orch-apply.sh`) auto-refreshes `.head.updated_at` on ANY write touching ANY other `.head` field — the new resume-attempt counter this row itself requires (AC-3) would silently defeat an age check keyed off `.head.updated_at` the moment it starts writing; the fix keys the age check off the `in_progress[]` row's own `claimed_at` (set by all 5 claim scripts, untouched by the head-only auto-stamp) instead. (2) the CURRENT 24h check has NO supervised-hold exemption (entry gate at `main.md:253` gates the whole WF-1..S2 chain, WF-2 included, on `updated_at<24h`) — `po_live_evidence_20260806T0921` on this same row already measured a 24.2h supervised-hold span, so a naive in-place `24h→2h` edit would turn a rare edge case into a routine false-positive against every legitimate PO-ratification wait. Design decision: threshold **2h** (basis: 30min measured cron cadence x4, 2x the existing 1h `task:<id>` lock TTL backstop, ~12x tighter than the 24h it replaces), evaluated in a NEW WF-4 check placed strictly AFTER WF-2 (never before/parallel), corroborated against `git log` since `claimed_at` (no matching commit required to actually reset — AC-1's own "no `.head` movement AND no commit" wording, lesson `feedback_internal_consistency_is_not_corroboration_check_the_other_plane` applies directly). New WF-3 check (3-attempt bound, ~90min worst case on the exact 2026-07-25 observed failure shape) escalates to `status:BLOCKED` + `hold_reason` (reused existing field) + `resume_attempt_bound_exceeded_at/_by` (new pair — deliberately NOT `blocked_by`, which is already schema-meaningful as a reverse dependency edge, `orchStateSchema.ts:707` — would have corrupted dep resolution). Both `HeadSchema`/`TaskSchema` confirmed live `.passthrough()` — zero schema change required for correctness; typed fields spec'd as an explicitly optional companion. PO's 2026-08-05 adjacent write-coherence note (`.head.next_agent` not re-synced on same-tick row reassignment) scoped OUT with reasoning — different trigger class (instantaneous write-ordering vs this row's temporal-staleness ACs), matches lesson `feedback_selfreport_conflates_two_dbadjacent_defects_by_topic`; recommended standalone follow-up `FIX-DEVTEAM-HEAD-NEXTAGENT-RESYNC-ON-REASSIGN` for PO to mint.
**Output:** `docs/architecture-briefs/2026-08-07-devteam-head-pin-stale-threshold-resume-bound.md` — full ready-to-apply diff spec for `docs/agents/dev-team/flow/main.md` (entry-gate edit, WF-3/WF-4 new blocks, S2 one-line insertion, deleted 24h sibling branch), 2 BUG-channel signal shapes, optional companion schema/regression-verifier spec. Board: row moved `in_progress[]`→`review[]`, `next_agent=agent-father` — corrected from the dispatch prompt's own "developer" guess using the standing `po_routing_ruling_20260721` artifact-class ruling (agent-instruction prose under `docs/agents/**` routes to agent-father; `developer` carries no commit-zone grant there and its own init.md forbids `.ts`/`.py`), already applied twice to this exact file (TE-T02, 2026-08-06 qadrain brief §1c).
**Next:** agent-father implements per the brief §5 (ready-to-apply patch text); developer companion (§7/§8, schema typing + regression verifier) is optional/non-blocking, flagged for PO to mint separately if wanted, not spawned this cycle. Outer sprint-task lock `task:FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE` left untouched (dev-team holds it, BGFAN-1 discipline).

## 2026-08-07T23:57Z — FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM (plan_only, supervised, dev-team Supervised-Lane Sweep dispatch)

**Task:** Diagnose which parent-uvicorn-process code path drives pdf-extractor's PID1 RSS by 100s of MiB-to-~1GiB per OCR job (AC1), measure a 15-wide burst peak honestly (AC2), either land a fix keeping peak ≤80% of the 2.5GiB cap or write an evidence-backed capacity recommendation (AC3), discriminate unreleased-Python-heap vs malloc-arena-fragmentation vs deliberate-cache with an artifact (AC5), and re-measure the idle floor (AC6) — without OOMKilled as a success signal (AC7) or restarting/stopping/killing the live container.
**Findings:** Code-read (`interface/pek_run_helper.py`, `infrastructure/pek_engine_adapter.py`, `main.py:154-230`) named the mechanism: `POST /pek-extract` runs doclayout_yolo/YOLOv10 (torch) + PaddleOCR PP-StructureV2 (PaddlePaddle) CPU inference DIRECTLY INSIDE PID1 via `asyncio.to_thread` + an internal `ThreadPoolExecutor` — thread-isolated only, unlike the already-fixed tesseract path (`POST /extract`, real `ProcessPoolExecutor` OS process, `main.py:154`). Built an isolated reproduction (`docker run --rm` from the SAME image, own cgroup — deliberately NOT the live PID1, given the live container already touched 98.5% of its cap in this very life per a fresh `docker inspect`/`/proc/1/status` read; firing load at that shared cgroup risks the exact silent-death failure this row investigates and is an infra-load-test action outside this role's charter). Repro script `scripts/audits/pdfx-pek-mem-arena-probe.py`, real 59-page BCTC PDF (`data/pdfs/NVL.pdf`): model load alone = +845.4 MiB RSS (main.py's own "~80MB" comment is stale by >10x); one layout-detection pass = +485.2 MiB cold / +236.4 MiB steady-state (2nd pass); `gc.collect()` recovers 0 bytes both times; `malloc_trim(0)` recovers 165.9 MiB / 235.6 MiB (~99.7% of steady-state growth) — decisively discriminates AC5's three candidates toward **glibc malloc-arena-fragmentation** as the dominant per-job mechanism (rules out Python-heap retention; rules out deliberate-cache as the *dominant* driver, since a real native pool would be immune to `malloc_trim`). Live idle-floor re-sample on the production container (read-only, tesseract child count 0, idle ≥30min confirmed via `docker logs`): 1,847,472 kB (73.9% of cap) → 1,559,376 kB (62.4%) over 28 minutes, DECLINING not flat — second independent data point for the row's own "late/deferred partial release" finding. New finding not in the row's own text: swap is active on this host (~776 MiB of PID1 swapped out at the first sample) — any future capacity-cap conversation needs RSS+Swap, not cgroup `memory.current` alone. Also found the row's own AC2 literal instruction ("15-wide `POST /extract` burst") targets the WRONG endpoint — `/extract` only exercises the already-fixed tesseract path, never `PekEngineAdapter`; corrected spec (hit `/pek-extract`, sequential not concurrent given its non-blocking `Semaphore(1)`) written into the brief for dev-pdf-extractor to run ops-supervised, not executed here.
**Output:** `docs/architecture-briefs/2026-08-07-fix-pdfx-parent-process-memory-burst-headroom.md` — full mechanism artifact, corrected AC2 burst spec, AC3 remedy design (`malloc_trim(0)` in `_run_pek_extract`'s `finally`, both success/exception paths — an 11-failure PEK retry-storm found this cycle, 2026-08-04T14:35-17:40Z on two fixed report_ids, still pays the full allocation cost before failing), AC4 regression-floor plan, AC5 discrimination table, AC6 idle evidence, AC7 gate design (VmRSS/MemPerc/RestartCount, never OOMKilled), files-to-modify list, DDD layer note, test strategy, risk flags. Reusable probe script persisted to `scripts/audits/pdfx-pek-mem-arena-probe.py` per Script Persistence convention. Board: row moved `in_progress[]`→`review[]`, `next_agent=dev-pdf-extractor` (system-map.json Tier-1 zone match for `apps/pdf-extractor/`), `supervised`/`plan_only` preserved, `.head` idle-reset.
**Next:** dev-pdf-extractor implements the `malloc_trim` fix + `main.py` comment correction + unit test (brief §6/§10), then executes the corrected §5 live burst under ops supervision + §8's DB-count-verified regression floor before DONE. If the honest post-fix peak-under-burst still exceeds 80% of cap, escalate the capacity-cap question to po per AC3 — not decided in this brief. Outer sprint-task lock `task:FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` left untouched (dev-team holds it, BGFAN-1 discipline).
