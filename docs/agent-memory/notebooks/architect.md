# Architect — Notebook

**Last updated:** 2026-07-29 07:35 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-07-29T07:35Z — FIX-A21-CRASH-WINDOW-PREDECESSOR-BOUND-FALSE-NEGATIVE (zone=multi, P1 supervised plan_only, PO board row)

**Task:** A-21 crash-restart counter drops any crash whose predecessor startup lies outside a fixed 8h fetch window — the fixed 2x lookback that doubles as the predecessor lookup cannot bound an unbounded inter-startup gap. Same defect ported 1:1 across `restartCadenceAlertJob.ts` (production Telegram path), `tier1-probe.md`, and the 2026-07-23 design brief that originated the query. 3rd recurrence in this discriminator's git history.
**Finding:** Live-verified read-only (`docker exec … bun -e`, `readonly:true`) rather than trusted on the row's own text: HEAD's hand-ported query reproduces `crashRestarts:0`; importing and invoking the REAL exported `runRestartCadenceAlertJob` with an injected readonly db + inert `sendFn` reproduces the same `0` (confirms the bug is in the shared logic, and confirms the injection point makes docker-exec-import a safe, live-viable reuse path — proves AC(6) is achievable, not just theoretical). Proposed fix (single `LAG(started_at) OVER (ORDER BY started_at)` unbounded-predecessor query, 4h window kept ONLY as the outer candidate filter) returns `crashRestarts:1`, predecessor `2026-07-28 22:58:42`, matching the row's stated ground truth exactly. `LAG`/`ROW_NUMBER` window functions are already idiomatic in this codebase (foreignRoomStore.ts, moneyRadarStore.ts) — not a new pattern.
**Output:** `docs/architecture-briefs/2026-07-29-fix-a21-crash-window-predecessor-bound.md`. Ordering ruling vs `OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN` (mandatory per row's `cross_row_interaction`): SHIP TOGETHER, not sequenced — traced concretely that the counting fix alone does not introduce a NEW false-positive class (that exposure already exists today for any two startups <8h apart, driven by the frozen clean-shutdown sentinel, not the window bound) but does make the counter correctly reachable while the sentinel stays dark; recommended a same-PR staleness caveat on the alert message (reusing `ALERT_THRESHOLD`, no new constant) rather than blocking on, or ignoring, the sibling row. Recommended split: dev-mcp-server (apps/mcp-server/ code+test), agent-father (tier1-probe.md mirror — matches the 2026-07-23 brief's own precedent for this exact file).
**Next:** Did not self-mint the two implementation rows or move status/lane (`supervised:true`, matching the FIX-POLYMARKET-FETCH-DEAD-GEOBLOCK-ACTUATOR precedent below) — wrote the split recommendation to `architect_review_note` via `orch-apply.sh`; row stays `BACKLOG`, router/PO to dispatch the split.

## 2026-07-28T23:51Z — FIX-POLYMARKET-FETCH-DEAD-GEOBLOCK-ACTUATOR (zone=apps/mcp-server/, P1 supervised, PO board row)

**Task:** Rule RESTORE-via-VPS vs RETIRE for a prediction_markets plane dead 28d (detector-right/actuator-blind split — a sibling row already fixed the staleness-alert path; this row is the underlying fetch). `fetchPolymarkets()` never throws on transport failure, so the poll job's `cron_job_runs` reads status=success the whole time.
**Finding:** WebSearch on the frozen gamma-api TLS artifact (`*.anj.fr` cert) surfaced a live fact PO's triage note didn't have: France's ANJ (national gambling regulator) ordered French ISPs to block Polymarket outright on 2026-07-16, after finding rigged markets (hacked weather-sensor bets) and zero KYC. mcp-server runs on France-hosted infra (`franceSummaryJob`). This converts the (a)/(b) choice from a pure cost/effort tradeoff into a should-not: a Vinahost VN-egress VPS hop would likely technically defeat the block, but that means engineering evasion of a sovereign regulator's order against a source it found fraudulent — not the same problem class as the VN-source geoblocks (SSC/HNX/SBV/CafeF) the VPS pattern exists for.
**Output:** RULED RETIRE. Wrote the full design to the board row's `architect_review_note` (this row is a direct PO board mint, not a BA/sprint pipeline task, so it follows the field convention 2 other in-progress rows already use rather than a `docs/handoffs/` file): kill acquisition (`config.ts` enabled-default flip); a REQUIRED-either-way `fetchPolymarkets()` throw-on-transport-failure fix (traced the actual mechanism — `recordJobRun`/`wrapRun` marks `cron_job_runs` by promise-reject, but `runPredictionMarketPoll`'s own outer catch + Step 3's inner catch both swallow unconditionally, which is the literal reason 28d of dead fetches read success); drop the 3 named consumer refs; retire the now-purposeless `prediction.md` flow + its `main.md` dispatch row + `init.md` pointer entirely (partial-editing it leaves a flow with nothing left to do); deregister the MCP tool; flagged `quality-checklist.json`'s 2 SLA rows for qa coordination (verified no "retired" status token exists in that file's enum — did not invent one).
**Next:** dev-mcp-server — board row `next_agent` set, `decision:"RETIRE"` stamped, stayed in `ready[]` (no lane-move — status unchanged). Router/PO must dispatch next; this was an explicit supervised dispatch to architect, not auto-picked.

## 2026-07-28T15:48Z — FIX-PDFX-TESSERACT-CONCURRENCY-VIOLATES-SINGLE-WORKER-INVARIANT (zone=apps/pdf-extractor/, P0 supervised plan_only, router out-of-band spawn)

**Task:** Blueprint only — root-cause 10 live tesseract processes against a code-declared, code-commented, unit-tested single-worker OCR invariant; no code, no container action, read-only whitelist (`docker stats/inspect/ps/logs/exec ps`, GET+health), ~27 MiB headroom so even a write probe was forbidden.
**Finding:** **Read the process table before trusting a PPID.** The row's load-bearing premise — "every PPID is 1 ⇒ re-parented ⇒ the spawner is gone" — is false here: `Dockerfile:117` uses exec-form `CMD python3 -m uvicorn`, so **PID 1 IS the app**, and `PPID=1` is what a perfectly normal in-process child looks like. One atomic `ps -Lo tid,stat,wchan -p 1` settled it: exactly **10 python threads in `do_sys_poll`** (subprocess pipe-wait) paired 1:1 with 10 tesseract children, event loop idle in `do_epoll_wait`. Zero orphans; the guards were never on the hot path. The real bound was **CPython's asyncio DEFAULT executor** — `min(32, os.cpu_count()+4)`, and `docker info` NCPU=6 ⇒ **exactly 10**, which *is* the "pinned at 10" shape. `wchan` is the cheap discriminator between "waiting on a child" and "idle on a queue" — `ps` alone can't tell them apart. Second lesson: **an uncancellable offload turns a client timeout into a ratchet.** `pdfExtractorClient.ts:69 AbortSignal.timeout(120_000)` aborts, but `asyncio.to_thread` cannot be cancelled and nothing kills the subprocess, so every abandoned request permanently consumes a slot — the 122s reparse drip fills the pool and Tier 1a now fails 100%, logged benignly as `serviceStatus:null` "null/short". Third: **five prose declarations of an invariant across four files enforced nothing** (`main.py:149-152`, `handlers.py:451`, `extractor.py:66-68`, `:131`) — the two real guards were process-global and correct, just wired to `/extract-tables` (0 calls in 6h) and `/pek-extract` (14) while `/extract` carried 182.
**Output:** `docs/architecture-briefs/2026-07-28-pdfx-tesseract-concurrency-invariant.md` — single `infrastructure/ocr_gateway.py` funnel (process-global `BoundedSemaphore`, gateway-OWNED thread pool so OCR can't head-of-line-block the 6 legit `to_thread` users, bounded 5s wait→429, per-page deadline via pytesseract `timeout=` which terminates the child, cancellation binding, `/proc`-derived `os_children` published ALONGSIDE the counter because the whole defect was bookkeeping diverging from OS truth), all 6 pytesseract sites rewired behind a static fence test. Kept and SUBORDINATED `main.py:154 ProcessPoolExecutor` rather than deleting it (it is the ARCH-A20/PDF-AVAIL-02 fix) by acquiring in the parent — two bounds in different processes provably cannot compose. Regression test names why `TestSemaphoreGuard` passed: it hand-acquires the semaphore and calls the adapter directly, proving "rejects while held" (a tautology about the guard) and never "is on the path"; replacement drives 15 concurrent `POST /extract` through the real `create_app()` and asserts a concurrency **watermark ≤ 1**, and must be run RED first.
**Next:** dev-pdf-extractor — row BACKLOG→READY, `plan_only:false`, `supervised:true`, `detail_ref` set. SEQUENCING CLAIM WITHDRAWN — I asserted "land FACTORY-PDF-extract-tesseract-config first" without checking, and it is FALSE: that refactor is already on main (`cfe0a78d7`), all 6 importers already import `tesseract_config` at HEAD; REVIEW here means landed-awaiting-QA, not pending-work. Lesson: **REVIEW is not a pending edit — `git log -- <file>` before asserting a collision.** Declined to encode it as `depends` (the field IS enforced via devteam-eligibility.jq, and `deps_satisfied` needs DONE_VERIFIED — it would have pinned a P0 behind a P2's QA queue: a false blocker). Also verified by EXECUTION, not flow-doc prose: `is_bounded1_eligible`=false and RLC's select excludes supervised, so nothing auto-picks this row — router/PO must dispatch it. Runtime reap stays USER-GATED and unexecuted; brief corrects its benefit to ~0.79-0.93 GiB (not ~890 MiB — PID 1 alone holds 1.68 GiB) and flags its real cost (`extraction_engine.py:177-178` swallows OCR failure to `""`). Companion mcp-server 429/backoff row recommended to PO, NOT minted, NOT a blocker.
