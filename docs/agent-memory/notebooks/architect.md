# Architect — Notebook

**Last updated:** 2026-08-26 01:17 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-26T01:17Z — FIX-FLEETPUSH-DISARM-EXISTS-ONLY-IN-UNTRACKED-PLIST-REPO-COPY-SILENTLY-REARMS (P0, po mint, DRS dispatch, zone=cross-service/)

**Task:** Fleet-push's user disarm (`Disabled=>1`) lives only in the installed plist; the tracked repo copy is unchanged (`RunAtLoad=1`, no `Disabled` key). Design-only: choose durability approach among (a) tracked `Disabled=1`+re-arm doc, (b) install-time guard refusing silent overwrite, (c) checked-in policy file — do NOT just commit `Disabled=1` unruled. HARD: never re-arm fleet-push, never push, never grep the binary plist.
**Findings:** Re-verified the 1-line diff live (only `Disabled=>1` differs). Census'd all 5 tracked `launchd/*.plist`: 3 (cowork-guaranteed-slot-firer, socat-bridge, socat-tls-bridge) are installed as **symlinks** to the repo file — drift-immune by construction; fleet-push (and docker-events) are installed as independent **copies** — the actual gap. No install script exists anywhere in the repo today; the only documented step (`docs/standards/cron-jobs.md:359`) assumes the installed copy already exists. `PUSH-AUTONOMY-1` (dev-standards.md, still canonical, unmodified) frames autonomous push as the standing DESIGN intent — the current disarm is a live host-level override tied to the present CI-red/224-backlog situation, not a stated permanent design change, so the tracked plist's "armed by design" default is still correct.
**Output:** `docs/architecture-briefs/2026-08-26-fix-fleetpush-disarm-durable-install-guard.md` — chose (b): new `scripts/install-launchd-plist.sh` guard, refuses to overwrite an installed plist whose `Disabled` key reads true (`plutil -extract Disabled raw`, never grep — live-tested against both fleet-push=present/rc0 and docker-events=absent/rc1 as a working/non-working pair), becomes the sole documented install/re-arm path in cron-jobs.md. Justified against (a) (would invert the tracked default to "disabled by design" forever, for every future clone, for a temporary override) and (c) (needs the identical install-time enforcer as (b) plus a new policy-file schema, for a reasoned-record benefit this brief + the row's own po_note already provide). Costed plainly: new script is S-size and unbuilt yet — the gap stays open until it lands; it is a discipline control (bypassable by manual `cp`), same class as `orch-apply.sh`. Journal STEP architect-S48 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md`). Row lane-moved `in_progress[]`→`ready[]`, `next_agent: developer`, `.head` reset to idle, via `orch-apply.sh`; read back post-commit.
**Next:** developer implements `scripts/install-launchd-plist.sh` + its fixture-only test (never targets the real installed plist) + the `docs/standards/cron-jobs.md` re-arm-line repoint, per the brief §6. Does NOT touch `launchd/com.vn-market.fleet-push.plist` (tracked copy stays armed-by-design) nor the installed copy at all.

## 2026-08-26T01:00Z — FIX-SQLITE-DOCKER-VIRT-CORRUPTION-ROOT-CAUSE-INVESTIGATION (P1, ops mint, zone=multi)

**Task:** 6th market.db corruption recurrence. Ops' row asserted "Docker Desktop advisory-lock defect" as established fact; dispatch instructed treating it as hypothesis and matching the originating signal's own careful epistemics. Hard constraints: no write to market.db, `date -u` for all timestamps, forensics against the preserved read-only `.corrupt-*` snapshots only.
**Findings:** Header-byte forensics (file-format writer/reader version byte) across all 4 preserved snapshots is the decisive move: 07-19/07-30/08-06 are ALL WAL-format (2/2) sharing ONE scattered multi-tree `btreeInitPage()`-error-11 signature (the known SHM-torn-write mechanism, recurring because the DELETE-mode mitigation kept getting silently defeated — 08-06 specifically corrupted while the Go DSN WAL-re-armer, fixed same-day 12:41 UTC, was still live). Tonight (08-26) is rollback-journal format (1/1) — the FIRST of the four to be so — with a genuinely different, narrower signature: 7788 rowid-disorder cells concentrated in Tree 180 (`intraday_foreign_flow_5m`), exactly 1 stray cross-tree pointer in Tree 96 (`pdf_extracted_text`), `Freelist: size is 0 but should be 1`, 37 orphaned pages — the textbook non-atomic multi-page-commit fingerprint, not disk-level scatter. New finding absent from every prior record: host bun (1.3.13 macOS) and container bun (1.3.13 oven/bun:1.3.13-debian) embed DIFFERENT SQLite engines (3.43.2 vs 3.51.2) against the identical bind-mounted file — verified live, twice, both directions. Re-verified mount topology live (`docker inspect`): still host bind-mount, unchanged since 2026-07-30, resolving the standing memory note's own already-self-superseded ambiguity. Reconciled the ~62.8M rowid magnitude as a red herring (INSERT OR REPLACE churn on a non-AUTOINCREMENT table, ~1.51M rowids/20h verified against the live restored backup), not evidence of tampering.
**Output:** `docs/architecture-briefs/2026-08-26-sqlite-marketdb-corruption-6th-recurrence-crossboundary-engine-skew.md`. Recommended (costed, not waved at): do NOT re-migrate to a named volume now (tried once, reverted after a VM-rebuild wipe; would cost the same 86-script docker-exec retrofit as the cheaper fix anyway) — instead a new `scripts/run-against-market-db.sh` wrapper (mirrors `orch-apply.sh`'s SSOT convention) routing every host-invoked market.db script through the container's own engine, eliminating both surviving candidates (engine-version skew; refined advisory-lock/cache-coherency gap on the rollback-journal locking path) at once. Re-flagged the still-open 07-30 brief backup-integrity-gate gap (unfixed, unrelated to root cause but determines next-incident blast radius). Row lane-moved `backlog[]`→`ready[]`, `next_agent: developer`, `zone: multi`, `files` populated, via `orch-apply.sh`; read back post-commit. Journal STEP architect-S47 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md` — rolled continuation, -6.md was at 154L/34919B against the 600L/36000B cap).
**Next:** developer implements `scripts/run-against-market-db.sh` + the `docs/policies/dev-standards.md` CANONICAL wrapper rule + `checkpoint.ts` backup integrity-gate/multi-generation fix per the brief §6. First step for the implementer: audit which of the 86 host-script call sites' current invocation convention already assumes container context vs. genuinely needs the retrofit (not enumerated this session).

## 2026-08-25T03:24Z — FIX-AUDITOR-TIER1-PROBE-SCORES-DELIBERATELY-DISABLED-LAUNCHD-JOB-AS-DEGRADED (P1, po_expedited, dev-team incident-lane consumer dispatch, zone=cross-service/)

**Task:** `_check_launchd_agents()` (scripts/agents-flow/auditor-tier1-probe.sh) has only two dispositions for a not-loaded label — bad, or ack-ledger-suppressed. Fleet-push's ack entry was removed 2026-08-24 (commit `930297f37`, staleness rule, pending this row as the durable fix) so Tier-1 has scored `verdict=FAILURE` every 30min tick since. HARD CONSTRAINT (status_note): never re-arm fleet-push by any mechanism (plist Disabled key, launchctl enable/load/bootstrap); remedy is a scoring change only.
**Findings:** Live-verified this cycle: `plutil -p ~/Library/LaunchAgents/com.vn-market.fleet-push.plist` → `Disabled=>1`; `launchctl print-disabled gui/$(id -u)` independently confirms membership; repo-copy plist has NO Disabled key (confirms AC-2's "read the installed plist, never the repo copy" is load-bearing, not defensive). Regression control: docker-events' installed plist has no Disabled key — stays on the existing ack-ledger path, untouched. `_check_launchd_agents()` already has an rc=0-with-note pattern (reused for the acked case) that `run_probe()` treats as ALL_GREEN-with-transparency — reused verbatim for the new disposition rather than inventing a second channel. `docs/data/auditor-tier1-last-trigger.json`/`spawn-debounce.json` confirmed the live FAILURE + spawn_count=9 this cycle, proving the debounce (sibling row) bounds churn but can never converge the verdict — only this design does.
**Output:** `docs/architecture-briefs/2026-08-25-fix-auditor-tier1-launchd-expected-disabled.md` — new `_launchd_label_expected_disabled()` (installed-plist Disabled key OR `launchctl print-disabled` membership), new `LAUNCHD_INSTALLED_DIR_PATH` env var, EXPECTED-DISABLED labels excluded from the spawn-debounce entity side-channel by construction. Scope decision: loaded-but-nonzero-exit branch deliberately untouched (a Disabled=1 job can never be loaded, so widening there risks hiding a real crash). Authored AC-6..AC-9 (execution-verifiable: live probe run must return ALL_GREEN while fleet-push stays disabled/absent from launchctl list) on top of PO's mint-time AC-1..AC-5. Journal STEP architect-S46. Row lane-moved `in_progress[]`→`ready[]`, `next_agent: developer`, `.head` reset to idle — both via `orch-apply.sh`. Had to shrink the `architect_review_note` twice to clear `orch-row-prose-ceiling-check.mjs` — the row's pre-existing prose (status_note/po_ruling/po_expedite_note/evidence/acceptance) already sat at ~9085B against the 12000B ceiling with a 0B live baseline (in_progress[] is outside the 3 tracked lanes), leaving only ~2900B of headroom for any new field.
**Next:** developer implements per the brief (`auditor-tier1-probe.sh` + comment-only `auditor-launchd-ack.json` edit); do NOT touch `auditor-tier1-probe.test.sh` — owned by sibling `FIX-AUDITOR-TIER1-PROBE-TEST-INVERTED-ASSERTION-L1422-FALSE-GREEN`, flagged in the brief that its `launchctl()` stub will need a `print-disabled` branch.
