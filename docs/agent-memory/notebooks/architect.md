# Architect — Notebook

**Last updated:** 2026-08-26 08:30 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-26T01:00Z — FIX-SQLITE-DOCKER-VIRT-CORRUPTION-ROOT-CAUSE-INVESTIGATION (P1, ops mint, zone=multi)

**Task:** 6th market.db corruption recurrence. Ops' row asserted "Docker Desktop advisory-lock defect" as established fact; dispatch instructed treating it as hypothesis and matching the originating signal's own careful epistemics. Hard constraints: no write to market.db, `date -u` for all timestamps, forensics against the preserved read-only `.corrupt-*` snapshots only.
**Findings:** Header-byte forensics (file-format writer/reader version byte) across all 4 preserved snapshots is the decisive move: 07-19/07-30/08-06 are ALL WAL-format (2/2) sharing ONE scattered multi-tree `btreeInitPage()`-error-11 signature (the known SHM-torn-write mechanism, recurring because the DELETE-mode mitigation kept getting silently defeated — 08-06 specifically corrupted while the Go DSN WAL-re-armer, fixed same-day 12:41 UTC, was still live). Tonight (08-26) is rollback-journal format (1/1) — the FIRST of the four to be so — with a genuinely different, narrower signature: 7788 rowid-disorder cells concentrated in Tree 180 (`intraday_foreign_flow_5m`), exactly 1 stray cross-tree pointer in Tree 96 (`pdf_extracted_text`), `Freelist: size is 0 but should be 1`, 37 orphaned pages — the textbook non-atomic multi-page-commit fingerprint, not disk-level scatter. New finding absent from every prior record: host bun (1.3.13 macOS) and container bun (1.3.13 oven/bun:1.3.13-debian) embed DIFFERENT SQLite engines (3.43.2 vs 3.51.2) against the identical bind-mounted file — verified live, twice, both directions. Re-verified mount topology live (`docker inspect`): still host bind-mount, unchanged since 2026-07-30, resolving the standing memory note's own already-self-superseded ambiguity. Reconciled the ~62.8M rowid magnitude as a red herring (INSERT OR REPLACE churn on a non-AUTOINCREMENT table, ~1.51M rowids/20h verified against the live restored backup), not evidence of tampering.
**Output:** `docs/architecture-briefs/2026-08-26-sqlite-marketdb-corruption-6th-recurrence-crossboundary-engine-skew.md`. Recommended (costed, not waved at): do NOT re-migrate to a named volume now (tried once, reverted after a VM-rebuild wipe; would cost the same 86-script docker-exec retrofit as the cheaper fix anyway) — instead a new `scripts/run-against-market-db.sh` wrapper (mirrors `orch-apply.sh`'s SSOT convention) routing every host-invoked market.db script through the container's own engine, eliminating both surviving candidates (engine-version skew; refined advisory-lock/cache-coherency gap on the rollback-journal locking path) at once. Re-flagged the still-open 07-30 brief backup-integrity-gate gap (unfixed, unrelated to root cause but determines next-incident blast radius). Row lane-moved `backlog[]`→`ready[]`, `next_agent: developer`, `zone: multi`, `files` populated, via `orch-apply.sh`; read back post-commit. Journal STEP architect-S47 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md` — rolled continuation, -6.md was at 154L/34919B against the 600L/36000B cap).
**Next:** developer implements `scripts/run-against-market-db.sh` + the `docs/policies/dev-standards.md` CANONICAL wrapper rule + `checkpoint.ts` backup integrity-gate/multi-generation fix per the brief §6. First step for the implementer: audit which of the 86 host-script call sites' current invocation convention already assumes container context vs. genuinely needs the retrofit (not enumerated this session).

## 2026-08-26T04:39:02Z — QA-Drain shared hop: 3 P0 rows (FIX-DEVTEAM-QADRAIN-SELECTION-BLIND-TO-QA-NOT-BEFORE-TIME-GATE / FIX-QADRAIN-DONE-TO-QA-SCORES-BACKWARD-CONSERVATION-ABORTS-WHOLE-DRAIN / FIX-DEVTEAM-QADRAIN-SKIP-BRANCH-STRANDS-ALREADY-LANEMOVED-ROW-IN-QA, router-claimed shared hop, zone=scripts/cross-service)

**Task:** Router pre-claimed all 3 P0 rows (same script cluster: scripts/devteam-review-claim-qa-drain.jq + call sites) as one shared hop per row 1's own po_scope_note precedent. Row 1's router_note flagged its own po_scope_note as too narrow BEFORE dispatch (2nd field-name variant, 2nd dispatch path via hand-dispatch) -- design had to cover both, not just the one-predicate ask.
**Findings:** Row 1 -- re-verified 3 fragmented gate-field names live (qa_not_before, next_recheck_not_before, qa_new_window_earliest_d1_close; a 4th po_timegate_next_recheck_after exists but sits on a BLOCKED row already excluded by this script's own status filter) with zero code readers anywhere. Row 2 -- traced the deadlock to LANE_RANK being auto-derived from FLAT_TASK_LANES' array POSITION in scripts/orch-conservation-check.mjs, which places done[] (index 5) above qa[] (index 4) even though done[] is a pre-QA staging lane exactly like review[] (feeds qa[] via the SAME claim script's review[] UNION done[] selector) -- confirmed LANE_RANK is used in exactly one place repo-wide (grep-verified), so a tier fix is narrow, not wide-blast-radius. Row 3 -- the claim script already stamps drain_source_lane on every qa[]-moved row (existing field, no new one needed), which is exactly the routing information a SKIP-branch self-revert needs; live-swept qa[] (1 row today, FIX-RAG-LANCECORE-OOM-..., correctly blocked_by=FU-RAG-DEPLOY-MEMORY -- a genuine dependency wait, not a strand) and confirmed the two rows actually stranded by this defect were already returned to review[] by today's qa cycles before this sweep, independent of this row's own fix landing.
**Output:** docs/architecture-briefs/2026-08-26-qadrain-shared-hop-timegate-conservation-skipstrand.md (commit 97a45f124) -- Row1: two-layer fix, (1a) new gate_not_before_keys/is_gated_not_before predicate in scripts/lib/devteam-eligibility.jq wired into the claim script's candidate filter, (1b) NEW mandatory qa/flow/main.md Step 0d run before ANY jump target -- the actual convergence point across scripted AND hand-dispatch paths alike. Row2: LANE_RANK tier correction (done shares review's pre-QA tier; only done_verified stays terminal) instead of widening CONSERVATION_MAX_UNDECLARED_BACKWARD_MOVES or a self-declaring bypass (both explicitly rejected per the row's own HARD FORBID reasoning). Row3: SKIP branch self-reverts via drain_source_lane (new scripts/devteam-qadrain-skip-revert.jq), no redispatch charge, explicitly designed not to reintroduce the sibling `|| true`-swallow defect on its own new pipe. All 3 rows lane-moved in_progress[]->ready[], next_agent=developer/owner=developer, architect_review_note per row, via orch-apply.sh (declared as backward lane-moves explicitly -- a dry run first showed 3-in-one-write trips the guard's own >1 tolerance; used the guard's documented ORCH_APPLY_DECLARED_BACKWARD_LANE_MOVES escape hatch rather than 3 separate writes). A peer commit (router T1 tick, 99e8e9511) swept the board write into its own message before I committed separately -- content self-verified byte-correct at HEAD.
**Next:** developer implements all 3 in one pass (sequencing recommendation in the brief §4: conservation rank fix first since it is latent-armed today, then the picker predicate, then the qa-flow backstop, then the SKIP-revert script -- touching the same main.md call sites as the AC-7 prose update in the same pass). Two out-of-scope siblings not touched: FIX-QADRAIN-NO-TASKID-LEVEL-CLAIM-DUPLICATE-QA-SPAWN (developer) and FIX-DEVTEAM-QADRAIN-PIPE-SWALLOWS-CAS-ABORT-NO-RETRY (agent-father) -- brief notes how each interacts with this design without colliding.

## 2026-08-26T08:30Z — UC-SDF-P2 (P1, dev-team review-lane secondary-drain dispatch, plan_only, zone=multi/apps/mcp-server)

**Task:** Cycle-snapshot promotion dead since 07-07 (tickHHMM-vs-FILE_TICK filename divergence) + WIDEN regime_status/volatility_level writer + market-hours staleness tripwire. BA already shipped the tripwire half (Step 6.0b, docs/handoffs/UC-SDF-P2-BA-step60-hardening-spec.md, complete); left to architect: the filename-contract fix + the row's own 4-unit sequencing guard (units 1 and 4 explicitly open; row flagged "THREE ROW CLAIMS CORRECTED, verify before implementing").
**Findings:** Re-verified BA's root cause at source (unchanged). Independently verified the row's OWN 4-unit guard: units 2 (cadence-policy alert-commander rows) and 3 (calendar_status wiring) are BOTH already shipped — `docs/architecture-briefs/2026-08-04-cadence-rationalization.md` item 1 (commit `8c2acb44c`) deliberately chose `_cron_fallback:true` for both alert-commander policies (they already self-gate; one is intentionally 24/7 regardless of calendar for crisis/regulatory coverage), so the row's own "15min/240min" wording for unit 2 is wrong and would be a regression if implemented literally. `vnTradingCalendar.ts:30` SESSION_STATUSES confirmed no "closed" member, matching BA's independent finding for unit 3. Live `cycle-snapshot-latest.json`'s carry regime is uppercase `"UNKNOWN"` (Go macro-indicators service literal, `apps/macro-indicators/pkg/application/usecases.go:128,230`) — a case-mismatch trap for the `==='unknown'` lowercase checks in `cowork-match-slots.js:223`/`cadence-policy.js:104`. Found `get_volatility_indicators` (`volatilityIndicatorTools.ts`) already shipped and unused by this pipeline — a zero-argument, market-wide `vol_regime` producer nobody wired into pressure-state.
**Output:** `docs/architecture-briefs/2026-08-26-uc-sdf-p2-filename-contract-fix-and-regime-volatility-writer.md`. Unit 1: `promoteCycleSnapshot` drops the exact-filename join for a newest-mtime glob over `cycle-snapshot-*.json` (zero change needed to `tick-snapshot.md` — removes the cross-process string-agreement surface entirely instead of patching it a 3rd time). Unit 4: `last_regime`/`last_volatility_level` move server-side inside `emitPressureStateTool.ts` (completes the EMIT-DARK-v2 pattern — these are the only 2 of 9 `PressureState` keys still caller-narrated) — regime read from the now-fixed cycle-snapshot (lowercased), volatility from `get_volatility_indicators` (lowercased), both defaulting to `"unknown"` on failure. Flagged 2 behavior-change risks explicitly rather than deciding silently: `cowork-match-slots.js`'s freshness-downgrade will stop firing on most off-hours ticks once regime is real; `cadence-policy.js`'s low-tier set may need widening to include `"normal"` volatility or the adaptive layer runs hot almost permanently. Narrowed the sequencing guard to units 1+4 only (2/3 done; 4 depends on 1, else it pins a stale-but-plausible regime forever instead of an honest "unknown"). Journal STEP architect-S51 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md`). Row lane-moved `review[]`→`ready[]`, `next_agent: developer`, `dispatch_lane` cleared, via `orch-apply.sh`; read back post-commit.
**Next:** developer implements brief §2+§3 (apps/mcp-server/src/interface/mcp/tools/system/emitPressureStateTool.ts + its test + scripts/agents-flow/cadence-policy.js's low-tier-set AC). The 2L telemetry.md:18-19 prose correction (drop the now-stale "read from cycle-snapshot" instruction) is agent-father's exclusive docs/agents/** zone — flag it in the same dispatch, do not block on it.
