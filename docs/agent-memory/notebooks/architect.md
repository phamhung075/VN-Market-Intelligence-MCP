# Architect — Notebook

**Last updated:** 2026-08-07 20:10 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-07T20:10Z — SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS (plan_only, supervised, dev-team Supervised-Lane Sweep dispatch)

**Task:** True Q1-2026 BCTC servability census across the watchlist SERVING path (`get_bctc_full`) vs ops's self-contradictory 29-servable/~40-unservable-of-58 classification. Fold-vs-mint verdict deferred to this SPIKE's output per PO's own `scope_decision`.
**Method:** probed BOTH planes for 34 tickers (33 `system-map.json` watchlist active + HCM, folded per `po_decision_a_20260721T16`) — SERVING via live `get_bctc_full` (gateway meta-tools absent this session's tool binding; used `scripts/agents-flow/mcp-call.sh` degraded-mode Bash path per `gateway-call-contract.md` §6, corroborated once, not re-raised) and DB via direct read of the live bind-mounted `data/live/market.db` (`docker inspect` confirmed bind + 0-byte -wal; `financial_reports` row count 257 matched the REPARSE row's own 2026-08-06 QA figure, confirming live not decoy).
**Findings:** 27/33 watchlist tickers (81.8%) unservable, decomposed mode-1=11 (10 zero-signature + BSR scale-variant) / mode-2=4 (BID, DAG, HUT, PLX) / mode-3=11 (DBC DIG KDC MSN NVL SAB SHB SSI VCI VIX VND) + HCM. **Corrected the SPIKE's own routing assumption**: read `checkPublishability` (`bctcFullTools.ts` PUB-1) source and found mode-3 is gated by `refine_status='PENDING'`, not `validation_status` as the SPIKE's deliverable text assumed — every mode-3 ticker has real non-zero `pdf-parse`-extracted scalars and 0 `bctc_refined_units` rows, an exact match to the ALREADY-EXISTING `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP` row, not `FIX-BCTC-VALIDATION-GATE-NONBANK-ZERO-SCALE`. DB-plane timestamps (`parsed_at`) **disprove** (not merely leave unproven) the SSI/NVL 07-15→07-21 "regression" — both extracted in early June, refine-pending ever since, never touched by the July reparse batch. Confirmed VNM/VIC (+ VHM, previously unnamed) carry the mode-1 signature but are pre-existing June OCR failures, not reparse-batch spread (`parsed_at` predates the 07-19/07-20 window by 5-6 weeks, unchanged since). Found only 2 of `FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T`'s original 15-ticker cohort (HUT, PLX) are still true ingest-stall members — 11 moved on to mode-1/mode-3; surfaced BID and DAG as 2 new true ingest-stall members never previously named on that row (DAG has zero stored PDFs for ANY period — deepest gap in the census). Flagged `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP`'s "dormant since 07-04" premise as stale — producer fired today (`bctc_refined_units` MAX=2026-08-07T16:41Z) but backlog is still growing (151→181→230), a throughput not dormancy problem. Caught a `period_quarter` INTEGER-vs-string(`"Q1"`) type-drift mid-probe (2/257 rows, one is HUT's own row) — switched every DB query to `sort_key` text match to avoid silently mis-classifying HUT as DB-absent.
**Verdict:** FOLD, no epic, no new row minted — all 27 unservable tickers resolve into the 3 pre-existing modes once mode-3 is re-routed; one real gap noted (bulk remediation of the 11 mode-1 watchlist tickers among the 55 already-corrupt DB rows has no dedicated owning row today) but flagged for PO to adjudicate rather than minted unilaterally (outside plan_only SPIKE authority).
**Output:** `docs/architecture-briefs/2026-08-07-bctc-q1-2026-servability-census.md`. Board: row moved `in_progress[]`→`review[]`, `next_agent=po` (scope decision explicitly reserved for PO by the task's own `scope_decision` field).
**Lock/`.head` disposition (dispatch-note ambiguity, followed the explicit safe default):** `task_list_held` confirmed `task:SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS` (`sprint-task`) is owned by `dev-team` (not architect), `owner_client_session` = the SLS coordination session id, TTL expiring ~20:52Z. My own flow (`architect/flow/main.md`) says nothing about lock/`.head` release — genuinely ambiguous per the dispatch note's own test — so per its explicit fallback I left BOTH the sprint-task lock and the toplevel `.head` (`active_task_id=SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS`, `next_agent=architect`, `status=in_progress`) **untouched**, did not guess past my flow doc. dev-team: RAW-verify this row now sits `review[]`/`next_agent=po` and release/refresh `.head` accordingly on your next tick (lock will also self-expire ~20:52Z if not renewed).
**Next:** PO adjudicates §9 epic-vs-fold + §6a mode-3 re-routing correction + §9a unowned mode-1 batch-remediation gap; decide whether to narrow `FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T` per §7 and refresh `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP`'s stale premise per §8.

## 2026-08-07T19:15Z — FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT (P1, plan_only, supervised, dev-team Supervised-Lane Sweep dispatch)

**Task:** Decide receiver-delivery contract for `.signal_queue.rows[]` — Receivers table lists `unified-agent`/`alert-commander` as valid `to` targets but neither flow has any inbox/READ step (verified: zero `signal_queue`/`signal-dashboard` hits in either flow tree, and neither `init.md`'s `inter_agent.receives_from` declares the mechanism at all). Options given: (a) add pull/READ step to both flows, (b) push via `spawn-fanout.md`, (c) remove from Receivers table.
**Repro correction:** `po-20260720T052606` (cited as the live NEW fixture) is actually `status=triaged` — PO folded it 2026-07-22 into `FIX-CHEF-L6-GOLD-FALSE-PREDICATE` (BACKLOG) + `FIX-CHEF-L6-TOKEN-PERSISTENCE-RECURRING` (BLOCKED) once it became clear the directive would never reliably be delivered/applied. Task-board row's own `po_repro_case`/`po_status_decision` fields were stale (2026-07-21, never updated after PO's 2026-07-22 action) — this strengthens rather than weakens the finding: PO abandoned the channel and worked around it.
**Decision — hybrid:** SSOT-flagged push (new `signal_queue_push: true` on 6 `cowork-schedule.json` slots; `spawn-fanout.md` new Step 5.2b reuses dev-team's existing `dash:signal_queue:<id>` lock namespace, marks NEW→READ, injects a bounded `## CROSS-TEAM SIGNAL` block into `ENTRY_PROMPT`) + a minimal no-op-default consumption step added to `chef.md`/`cycle.md` (parses the block if present, applies `methodology-flag` rows as an authoritative override, logs receipt; absent → zero cost). Rejected pure-(a): `task_claim`/`task_release` marked "Phase 2 Ready, not active" in both tool packages, and a generic per-agent pull would duplicate the CAS-guard dance cowork-team already centralizes (5th recurrence of the documented-consumer-no-producer class). Rejected (c): both `to` values are live multi-sender targets, removal reproduces the same gap one level up. Rejected pure-(b) alone: fixes the mechanical NEW→READ flip but not the behavioral cost (ongoing wrong output) the row itself names. Scoped the push via an explicit flag (not generic-for-every-slot.agent) because `tran-ngoc-bau` is also cowork-fired and already implements a correct pull (`bootstrap.md` Step 0b-DASH) — generic push would race a working path.
**Lock-plane statement (mandatory per PO's fileplane-split ruling):** DB plane (`.signal_queue.rows[]`) — reuses the existing `dash:signal_queue:` lock, cowork-team becomes a 2nd writer on disjoint `to` sets. File plane (`docs/signals/*.json` mv) — untouched, out of scope; does not contradict `FIX-DRAIN-FILEPLANE-PEER-COLLISION-GUARD`.
**Output:** `docs/architecture-briefs/2026-08-07-fix-signalqueue-receiver-delivery-contract.md`. Board: row moved `in_progress[]`→`review[]`, `next_agent=pm` (design spans 3 flow files + 1 schedule flag + 2 init.md entries — small but multi-file; PM decomposes into developer subtasks, no BA spec needed, `BUILD-STANDARD: not-applicable`). `.head` reset idle (architect has Bash/gateway write access — did the full closeout itself, not leaving it for router pickup).
**Next:** PM decomposes into developer tasks per §8 of the brief; recommend landing sibling `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT` only after Component A's lock-reuse pattern here settles (same lock family).

## 2026-08-07T05:12Z — FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING (P1, plan_only, supervised, sprint=COWORK-RELIABILITY)

**Task:** BA spec complete + PO go-ahead ratified; design a WINDOW_KEY/cycle-id keying scheme for `docs/signals/*.json` writers (bctc-analyst, chef/unified-agent) so intra-day cycles stop colliding on one filename, without re-keying on raw `cycle_id` (2026-07-22 CAUTION — run-start-keyed, not a safe dedup key) and without letting filename-key and mutex-key disagree (NFR-3).
**Design:** One shared `derive_window_key()` pure function (§2 of the brief) — prompt-parsed `scheduled_utc=` once `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR` Component A ships (live-verified NOT yet on any `trigger_prompt` today), else slot→cron-lookup fallback (the ACTIVE path day one). bctc-analyst: pin at `cycle.md` Step 0c (before Stages 1-4 write), reuse verbatim at Stage 5's existing published-marker guard; filename → `bctc_signal_{TICKER}_{WINDOW_KEY}_{mode}.json`; bundled FR-7 (explicit routine-mode emit line, previously undocumented) + a stale cross-reference fix in `stage-consolidate.md` (pointed at the wrong file for the actual signal-file write). chef-intraday only: filename gains `-{VN_HOUR}`, reusing the EXACT value the live multi-fire `MARKER_KEY` already uses (NFR-3) — deliberately NOT switched to a UTC hour yet, since that would require migrating the mutex key's own basis too (out of scope for the sibling ANCHOR row); flagged as a named Phase-2 follow-on for PO, not silently bundled. FR-6 (drain reader): confirmed NO-OP at source (`drain-signals.js:173` fingerprint never includes filename) — doc-only line added, zero code change.
**FR-4 (tnb) RAW-verify performed, not deferred:** git log since the 2026-07-29 marker-cadence fix shows 3 tnb-audit cycles (c121/c122/c123), zero repeat `c<NNN>` collision. Did NOT design a new write-serialization mechanism against unreproduced evidence — flagged disposition (monitor-only vs. cheap `task_claim`-based defense-in-depth; confirmed `task_claim` IS available to tnb, unlike dev-*/qa specialists under INV-GATEWAY-1/WF-3) to PO as a risk-tolerance call.
**Output:** `docs/architecture-briefs/2026-08-07-cowork-signal-filename-cycleid-keying.md`. Board: `next_agent` set to `po` (plan_only+supervised preserved, no code shipped, no PM/dev handoff this cycle per dispatch instruction).
**Next:** PO reviews brief, adjudicates 2 open items (FR-4 disposition; EC-2 Phase-2 UTC-hour migration follow-on task), decides developer dispatch.
