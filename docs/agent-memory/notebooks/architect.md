# Architect — Notebook

**Last updated:** 2026-08-22 21:11 UTC | **Sprint:** PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-22T21:11Z — SPRINT-PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP (P-high SPRINT-L, ba→architect, zone apps/mcp-server/, router-direct dispatch with PRE-CLAIM)

**Task:** BA spec-complete on FR-1..FR-5 (prediction engine anti-calibration: LR table computed but never applied to scores; weekly Brier measurement feeds back into nothing; flat `*0.90` haircut arithmetically incapable of fixing 85%/95%-bucket 0%-hit-rate anti-calibration; a redundant prompt-layer `score*top_likelihood_ratio` multiplier). n=17 resolved predictions, n=3 per extreme bucket — po's Entry 7 ruling (no per-ticker/per-evidence-type refit off this sample) binding throughout; design verified structurally, not via backtest.
**Findings:** Designed one ordered pipeline converging at `get_evidence_summary`'s new `published_probability_{direction}` fields: FR-1 LR-weighted score (write time, nightly accumulator) → FR-3 evidence-based shrinkage (fragment-count × LR-sample-size, multiplicative — both dimensions must be strong to escape shrinkage; VPB's 2-fragment/untrusted case collapses to exactly 0.5) → FR-2 calibration-curve-derived correction factor (new `calibration_correction_factors` table, confidence-bucket-keyed only — NOT cross-tabbed by direction, to avoid slicing n=17 thinner than po's Entry 7 already ruled out) → final `[0.05,0.95]` clamp. FR-3's "must retire daily-predict.md:25 TIGHTENING haircut" clause is NOT superseded by evidence-based shrinkage by construction (different signal, macro not evidence) — folded `regime` in as an optional param on the shrinkage function (relocates the existing constant into the one pipeline stage) rather than dropping it or leaving a second stacked multiplier; flagged explicitly as a small MCP-schema scope-expansion for PM/QA visibility. FR-4: resolved the linked SPIKE's own root-class question — cache never recomputed once a stock's fragments all expire (accumulator's stock-enumeration simply stops visiting it), not a distinct prune-timing bug; fixed via a `MAX_SCORE_AGE_DAYS=30`-bound honest-degrade check (reuses the existing 30-day fragment-window constant, not a new number). Found 2 regression risks BA did not flag, both from reading actual test fixtures at source: (1) `getLikelihoodRatios` (plural, no try/catch, unlike its singular sibling) will THROW against `1118-evidence-accumulator-job.test.ts`'s fixture (confirmed: creates no `evidence_likelihood_ratios` table at all) — fixed by hardening the store function with the same defensive catch its singular sibling already has; (2) `1128-calibration-report-job.test.ts`'s own local schema (does not import schema-system.ts) will throw on FR-2's new Step 6.5 write — flagged as a mandatory fixture DDL addition (can't be defensively swallowed on the write side without hiding a real regression).
**Output:** `[Architect] Brownfield Findings` + `## RETURN (architect)` appended to `docs/handoffs/SPRINT-PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP-BA-spec.md` (full pipeline ordering, new store schema, DDD table, test strategy, risk flags). Decision journal `docs/agent-memory/decisions/sprint-PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP-architect.md` (6 STEP entries). Board row updated via `orch-apply.sh`: `next_agent="pm"`, `architect_design_complete=true`, `architect_completed_at`, `architect_handoff` — 0 net-new-growth ceiling violations post-apply.
**Next:** pm breaks design into atomic dev-mcp-server tasks, dependency order FR-4 (independent) → FR-1 (independent) → FR-3+FR-5 together (FR-5 depends on FR-1's `published_probability`) → FR-2 (independent, but Step 6.5 lands alongside the shared bucket-formula extraction).

## 2026-08-22T20:02Z — FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE (P1 supervised+plan_only, review[]-resident, router-dispatched — dev-team review-lane secondary-drain flagged architect but has no spawn capability)

**Task:** Row carried `po_verdict: AMEND-THEN-RESUBMIT` (2026-08-14) on my own 08-08 brief, with 3 blocking amendments (A1/A2/A3), 1 non-blocking (AC-0), and rulings on 3 open questions (Q1/Q2/Q3) — the `po_goahead_20260814T143024` stamp explicitly authorized resuming architect for this bounded amendment pass, NOT implementation. Router relay (dev-team's structural-wall pattern) surfaced it today after sitting untouched 8 days.
**Findings:** Re-measured the resolver live against today's file rather than trusting the brief's 08-08/PO's 08-14 numbers (both stale by design — PO's own instruction). Cross-checked my methodology against the real `scripts/agents-flow/decision-journal-archive.sh --all --dry-run` (`known_ids=100` matched my broad-union count exactly). Confirmed A1 is still empirically necessary today: broad-union dangling=11 (same headline PO saw 08-14, but the underlying set churned — `COWORK-GUARANTEED-SLOT-DURABILITY` genuinely resolved, `CHORE-COMMIT-OVERHEAD`/`UC-RDL-P4` are new); strict-union (drops the weak `.done_tasks[].sprint` signal) dangling=22 — the +11 delta the broad union still hides, incl. `COWORK-GUARANTEED-SLOT-CATCHUP`/`TOKEN-ECONOMY-AUDIT`/`ULTRACODE-AUDIT-FIXALL`/`FLOW-PRICE-ALPHA-LOOP`, all still `sprint_goal`-active or task-lane-live. Today's real (not simulated) `--dry-run` still lists `WOULD-ARCHIVE` for `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-25.md`; `archived(would)` has risen 299→346→355 across the 3 measurement dates — the anti-correlation PO flagged is continuing. Designed A2's new step-0 task-id-collision check (2 confirmed live instances: `UC-RDL-P4` — literally a task id written into `RDL-P4-DISPATCH-TOOL-DEV.sprint`; `BA-IND-P1-MOMENTUM-FRONTEND` — 2 rows nested inside a different, already-closed sprint, per ratified Q1). Resolved A3's branch-table self-contradiction per PO's explicit ruling (exact-match `sprint_goal.status=="active"` only; `PLANNING` alone is never a liveness signal) — verified live that `sprint_goal.entries[].status` actually carries a 3rd value, `OPEN` (`PREDICTION-CLAIMS-DAILY-CADENCE`, zero task refs), outside the brief's assumed 2-word vocabulary — flagged as new Q4 rather than silently guessed. Designed (not shipped, plan_only) the AC-0 safety-valve for `decision-journal-archive.sh --all` plus the `dev-standards.md:60`/`WORK.md` doc-fix shape. Row was already 17715B, well over the 12000B row-prose-ceiling (`orch-row-prose-ceiling-check.mjs`) — any net-new growth hard-rejects (confirmed from the script's own WARN semantics: over-ceiling-and-not-growing is grandfathered, over-ceiling-and-growing is not). Compressed `architect_review_note` from 2330B to 1987B (net -343B) to fit the amendment summary at zero net growth, and used the (structural, zero-cost) `detail_ref` field to point at the full amendment instead of inlining it.
**Output:** Appended `## 11. AMENDMENT (architect, 2026-08-22)` (8 subsections, §11.1–§11.8) to `docs/architecture-briefs/2026-08-08-fix-sprint-registry-dangling-ids-reconciliation-design.md` — full re-baseline, A1/A2/A3/AC-0 corrected designs, amended per-id classification table (22 ids), amended sequencing. Board row updated via `orch-apply.sh`: `architect_review_note` (compact, replaced), `detail_ref` (new, points at §11 anchor), `next_agent="po"`, `updated_at`/`updated_by` — confirmed 0 net-new-growth ceiling violations post-apply. Status/lane untouched (`REVIEW`/`review[]` — same stamp-in-place convention as the prior cycle's row).
**Next:** po re-ratifies the amendment (§11.1–§11.8) and rules on new Q4 (`OPEN` status vocabulary). Still `plan_only` — no implementation authorized yet.

## 2026-08-22T19:03Z — FIX-GHOSTZONE-CONVICTION-ASC-LIMIT-TRUNCATES-NEWEST + FIX-GHOSTZONE-FOREIGN-FLOW-MAXDATE-MISSING-NONNULL-GUARD (P0 sibling pair, BA→architect, zone apps/mcp-server/)

**Task:** BA spec-complete on 2 P0 GHOST-ZONE rows (both `router → ba → architect`, router flagged BA's `next_agent` field mis-set to a non-existent "agents-architect" agent — corrected to "pm" on both rows this cycle, per the real BA→Architect→PM chain). Conviction-history: `ORDER BY date ASC LIMIT 2000` on a table grown 766→3942 rows keeps the OLDEST 2000, freezing every symbol's score 64 days stale. Foreign-flow: `MAX(date)` subquery omits the `foreign_volume IS NOT NULL` guard its own docstring mandates, so a NULL-batch day gets picked as "latest" and the outer filter empties the whole response.
**Findings:** Traced both root causes to source. Conviction: `convictionHistoryHandler.ts` buildSnapshot (Map last-write-wins) + buildSeries (append-in-order) BOTH depend 100% on the store's ASC return order with zero internal sort — confirmed BA's warning that a naive `ORDER BY date DESC` flip would fix staleness while silently corrupting every symbol to its OLDEST score. Correct shape: inner `ORDER BY date DESC LIMIT ?` (newest-N) wrapped in an outer `ORDER BY date ASC` (restores contract) — zero caller-side change. NFR-2 (calendar-day vs row-LIMIT, architect-owned per BA): chose keep-row-LIMIT — BA's own Scope Out locks the client-facing `?limit=[1,2000]` clamp, and that SAME value passes straight through as the store's SQL bound, so a calendar-day window would silently break that locked contract, not just be lower-priority. Flagged a PM follow-up (not this task): wire a coverage-floor check into the EXISTING `checkConvictionHistoryGap.ts` audit-check plumbing so the ~59-day-out recurrence self-detects. Foreign-flow: one-line subquery fix, verified via SQLite three-valued-logic that the all-NULL-table-wide edge case degrades safely to the existing empty-response path with zero new code.
**Output:** `[Architect] Brownfield Findings` + `## RETURN (architect)` appended to both BA-spec handoff files (`docs/handoffs/FIX-GHOSTZONE-CONVICTION-ASC-LIMIT-TRUNCATES-NEWEST-BA-spec.md`, `docs/handoffs/FIX-GHOSTZONE-FOREIGN-FLOW-MAXDATE-MISSING-NONNULL-GUARD-BA-spec.md`) — exact SQL for both fixes, regression-test designs (conviction AC-16 with an explicit anti-DESC-flip assertion; foreign-flow AC-15 + 2 edge cases), risk flags. Board rows updated via `orch-apply.sh`: `next_agent="pm"` (corrected from BA's erroneous "agents-architect") + `architect_design_complete=true`/`architect_completed_at`/`architect_handoff` on both. `.head` untouched (was idle, rows not yet claimed).
**Next:** pm creates developer task(s) — zero file overlap between the two fixes, safe to dispatch together or sequentially to the same dev-mcp-server session.
