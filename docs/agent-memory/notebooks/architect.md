# Architect — Notebook

**Last updated:** 2026-08-12 10:46 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-12T03:40Z — FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS (P0, `apps/rag-service/`, direct PO-mint FIX row, premise-correction cycle)

**Task:** PO triage 2026-08-12T03:03:00Z escalated P2→P0 and PARTIALLY REFUTED the row's own premise: the original bounded-allocator-retention mechanism (one-time step to a ~86-90% baseline after idle-unload) cannot produce the measured signature (94.25%→98.73% over 50 min post-restart, zero reclamation dips, restart-resistant). PO named 3 candidates to rule out (per-request embedder accumulation, LanceDB reader/mmap accumulation, FTS build path overlap) and asked for a >=2h cold-start growth-rate AC design + a container-memory-limit reconciliation (1GiB, not the sibling row's assumed 768m).
**Findings:** Did not reason from code alone — ran 2 new isolated `docker run --rm` repros (own cgroup, real image/model/corpus snapshot, mirrors the `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` precedent's methodology exactly). Candidate 1 (embedder): 80 real `_raw_embed()` calls grew RSS only +5.4 MiB, asymptoting flat within ~20 calls — ruled out as dominant. Candidate 2 (LanceDB `search()`): 200-600 real calls against a 26,730-row corpus snapshot grew RSS +340-444 MiB before plateauing — confirmed dominant, ~65-80x candidate 1. Root cause named with an artifact: `rag_entries` has **zero vector index** (only FTS on title/summary), so every `vector_search()` brute-force-scans the full vector column with no bound; `malloc_trim(0)` recovers only ~8-15% of this growth (vs ~70-99% on the PDFX case), so allocator hygiene alone will not fix it — the real lever is a `lancedb.index.IvfPq` vector index (confirmed importable at the pinned lancedb 0.36.0). Candidate 3 (FTS build): ruled out as a continuous contributor via code evidence — `CRON_RAG_FTS_REBUILD_ENABLED` defaults false and is unset in this deployment, so the nightly rebuild has never fired; the lazy per-life build is a bounded one-time step already fixed by `RAG-FTS-BUILD-MEMORY-BOUND`. Memory-limit reconciliation confirmed live + in compose: 1 GiB (`MemLimit=1073741824`), not 768m — flagged (not fixed) the 2 remaining stale-`768m` text locations (`RAG-FTS-BUILD-MEMORY-BOUND`'s own row text, `repositories.py:67`'s comment) for whoever next touches either.
**Output:** `docs/architecture-briefs/2026-08-12-fix-rag-embedder-idle-unload-second-growth-source.md` (full investigation, live evidence, corrected >=2h AC methodology, fix design). 2 new reusable probe scripts: `scripts/audits/rag-embedder-mem-arena-probe.py`, `scripts/audits/rag-lancedb-search-mem-arena-probe.py`. Architecture SSOT updated (`docs/architecture/microservice/rag-service/infrastructure.md` — no-vector-index finding + `768m`→`1g` cap correction). Row's `architect_review_note` written via `orch-apply.sh`; moved `backlog[]`→`ready[]`/`READY` (was `BACKLOG`), `next_agent`→`dev-rag-service` (owner already `dev-rag-service`), priority stays P0. `BUILD-STANDARD: not-applicable` (bug-fix/diagnosis, in-zone, no new primitives).
**Next:** dev-rag-service implements the vector-index fix + optional periodic-`malloc_trim` hygiene, executes the brief's §5 >=2h live cold-start growth-rate/plateau measurement (ops-supervised — this is the row's actual AC now), and extends `test_dfr_p3_hybrid_search.py`/`test_search_usecase.py` with a recall regression check before DONE. PRE-CLAIM: architect has no MCP gateway binding (WF-3 Option III, ratified) — proceeded on file-based `.head`/board writes only.

## 2026-08-12T10:23Z — FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION (P0, `cross-service/`, PO-mint FIX row, plan_only, re-admission after 5.5d-stale mint)

**Task:** PO re-admitted a P0 plan_only row (6-day-stale, never dispatched): cowork Step 5.3's exogenous delivery-proof gate only positive-matches the router-latch narration shape (6 verbatim markers); 2+ RAW-verified misses this week (refine_bctc_md PARTIAL_EXIT, system-auditor "next actions for the LLM") passed clean. Binding sequencing constraint: must not wire the gate to the sibling detector (`FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-...`) while it still PASSes the inverse-partial shape.
**Findings:** No prior architect draft existed (the referenced stale stamp was PO's own mint entry, not a partial design). Occurrence evidence forces 3 hard constraints markers alone cannot satisfy: occ 1 (`refine_bctc_md` has no notebook file at all) needs a non-file probe kind; occ 3 (marker-less confabulated completion) proves marker-matching cannot be the load-bearing mechanism; occ 7 (chef-morning: synthesis JSON landed, notebook did not) requires CONJUNCTION over every declared artifact, never disjunction. `TASK-COWORK-CATCHUP-6`/FR-7 (still BACKLOG, epic open) already designs a single-plane `published_marker` reconciler for `last-fired.md` Step 5b — reused its exact read rather than duplicating it, and gated one layer upstream (`WON_SLOTS` filter before Step 5b runs) to stay clear of PO's 2026-07-28 "do not reopen the 6 consolidated rows" ruling (confirmed by id — none of the 4 protected ids is this row).
**Output:** `docs/architecture-briefs/2026-08-12-fix-cowork-delivery-proof-gate-artifact-conjunction-design.md` — two-arm Step 5.3 gate (Arm 1 cheap/advisory marker fast-path; Arm 2 load-bearing per-slot `delivery_proof` conjunction, new `cowork-schedule.json` schema, 2 new probe kinds beyond the detector script's 5 native planes). Hard `depends_on` on the sibling detector-fix row for the script-reuse sub-piece; recommended SHADOW MODE for the first rollout. Board row moved `ready[]`→`in_progress[]`→`review[]`, `architect_review_note` written, `next_agent`→`pm` (schema sub-task flagged for agent-father per `cowork-schedule.json`'s own `_maintained_by` stamp).
**Next:** pm decomposes into (a) developer task — Step 5.3 + `last-fired.md` comment, `depends_on` the sibling detector-fix row; (b) agent-father task — `delivery_proof` schema across 23 slots. Cron-spawn coverage (item D) relayed verbatim to `FIX-SYSTEM-AUDITOR-CYCLE-FINDINGS-NOT-SELF-PERSISTED` (already READY/pm), not actioned here.

## 2026-08-12T10:46Z — OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE (high, `apps/mcp-server/`, dev-team review-lane secondary-drain dispatch — row's own next_agent was a self-target "dev-team" PO had repointed to architect)

**Task:** Row had 5 remaining UUID-keyed 0-row 2025-Q4 tickers (GVR/MBB/D2D/NKG/SSI) still unexplained after a sibling row (`FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE`) explained the other 6 as a tautological fallback-id write-gate gap. Dispatch note asked for "design work on the two root causes."
**Findings:** RAW re-probed (docker exec, readonly, named-volume DB) — unchanged since 08-05, still exactly 0 rows/layout-units for all 5. Found the actual second root cause is a QUEUE-LIFECYCLE orphaning, not an extraction defect: all 12 of this row's cohort tickers' `bctc_vps_queue` rows (21 DB-wide) are false-terminalized `url_not_found`/`last_attempt=NULL`/`attempts=6-7`, created 2026-04-28/30 — BEFORE `FIX-BCTC-ENRICHER-STUCK-BACKLOG` (d92801332, 2026-07-02) shipped the `last_attempt`-stamping fix. `last_attempt=NULL` + `attempts>=6` permanently excludes them from every retry arm in `bctcQueueEnricherJob.ts` (Arm1 needs `pending`; Arm2 needs `last_attempt IS NOT NULL AND attempts<6`; orphan-resync needs `pending`/`deferred_infra`) — no automatic path will ever re-fire them. Ops's 07-10 one-off manual pdf_path-sync + direct `/api/trigger-pek-extract` bypassed the queue entirely, which is why nothing progressed since. Best finding: the fix already exists — committed in the SAME commit as the `last_attempt` fix, `apps/mcp-server/src/migrations/reset-bctc-enricher-stuck-backlog-2026-04.ts` (dry-run-capable, idempotent), allowlist verified to cover all 12 of this row's cohort tickers verbatim — but it was never wired to auto-run and, per live probe, never invoked. Zero new code needed; this is a pure ops-execution gap.
**Output:** Full evidence trail in the row's `.detail` (2 appends: root-cause + zone correction apps/pdf-extractor/→apps/mcp-server/); 3-step ops action plan + expected-outcome split in `architect_review_note` (row has no handoff file — direct PO mint). Board moved `review[]`→`ready[]`/READY, `next_agent`→`ops` (matches row's own `owner`, matches the 07-10 TRACK1 execution precedent). `BUILD-STANDARD: not-applicable`.
**Next:** ops runs the existing migration (`--dry-run` first) + RAW-verifies over 1-2 reconcile cycles. Flagged (not minted) a P2 follow-up: wire the migration into a startup-safe generic reset so a 3rd occurrence of this bug class doesn't need a 3rd hand-authored script — PO/pm call. Did not touch the sibling `FIX-BCTC-FALLBACK-...` row (BLOCKED, owned by architect, work already delegated to its `-write`/`-READ-SIDE-FAST-FOLLOW` children) — out of this row's scope, cross-referenced only. task_claim held by dev-team's outer PRE-CLAIM lock for this dispatch — released in closeout per normal task-lock discipline (architect has no MCP gateway binding, WF-3 Option III; file-based board write only).
