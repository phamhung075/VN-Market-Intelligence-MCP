# Architect — Notebook

**Last updated:** 2026-08-24 08:32 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-24T08:32Z — FIX-SIGNALQUEUE-OPEN-STATUS-DRAINED-BY-NOTHING-EVICTED-BY-NOTHING (P1 router hand-dispatch, zone=multi)

**Task:** Router hand-dispatch (next_agent=architect is non-dev, held out of BOUNDED-1; not plan_only, so SLS can't see it either — genuine dispatch-layer gap, see Next). Sibling developer row `FIX-ORCHAPPLY-SIGNALROW-STATUS-UNVALIDATED-ADMITS-UNPICKABLE-UNEVICTABLE-VALUES`'s own AC-2 explicitly deferred "what is the ratified status set, and what happens to OPEN" to this row's ruling; AC-1 deferred the to:ops delivery-hole decision the same way.
**Findings:** Live file is already 100% clean against the vocabulary I ratified (READ=86/RETRACTED=12/triaged=8/NEW=1, zero OPEN, zero lowercase new — PO's own dedup this morning already cleared the population this row's title names). Traced the write topology: `orch-validate.mjs` (shell/jq path) and `writeOrchStateAtomic` inside `appendSignalQueueRow` (TS in-process path, used by walEscalation.ts/tasksMdJanitorJob.ts/3 signal writers) BOTH converge on the exact same `OrchStateSchema.safeParse` — one Zod enum closes both paths, no dual mechanism needed. Biggest finding: `apps/mcp-server/src/scheduler/walEscalation.ts:30` is a SANCTIONED, correctly-cased, already-shipped `to:'ops'` producer (WAL-checkpoint escalation) — proves the delivery hole empirically (ops has zero cowork-schedule.json entries, no polling loop, no push-spawn target), not hypothetically. Ruled to:ops INVALID, repointed to to:po (PO's triage-signals.md already bridges auditor/scheduler findings to owner:ops task-board mints from there — no new consumer needed). Ratified 8-member SignalStatusEnum (NEW/READ/RESOLVED/SUPERSEDED/ACUTE-RESOLVED-ROOT-TRACKED/triaged/TRIAGED/RETRACTED); OPEN and PARTIAL both removed from the type (zero sanctioned producer for either) and hard-rejected at write, not quarantined — quarantine is the wrong tool for a provable producer mistake. Declined a dual-file live-vs-candidate grandfather script (prose-ceiling-check.mjs's own pattern, cited by the sibling row's AC-3) since the live file is provably clean today; chose hard Zod enum + a pre-deploy live-conformance re-check instead, backed by a new standing drift-check script (§6 of the brief) so the status/orch-cold-evict.sh mirror and the to/system-map.json mirror can never silently diverge the way the 2026-08-08 fix's allowlist did. Also found (not fixed, flagged for the developer child) that `appendSignalQueueRow`'s retry loop has no try/catch around the write — a Zod throw propagates uncaught, contradicting the function's own "do NOT throw" docstring; this ruling's enum is what will first make that reachable.
**Output:** `docs/architecture-briefs/2026-08-24-fix-signalqueue-status-and-recipient-vocabulary.md`. Enriched the existing developer row in place (files+=[walEscalation.ts, new drift-check script], size S→M, 4 new AC items carrying the concrete ruling) rather than minting a duplicate mechanism row. Minted one new docs-only child, `FIX-SIGNALQUEUE-STATUS-TO-VOCABULARY-DOCS` (agent-father, depends=[], parallel-safe — the vocabulary is fixed by this brief, not pending developer discretion). This row converted to the umbrella (children/depends=[both], next_agent=qa), mirroring the auditor-tier1-spawn-debounce precedent's exact shape. Journal STEP architect-S45.
**Next:** developer picks up the enriched mechanism row; agent-father picks up the docs child in parallel; qa does the joint cross-check (pre-deploy live-check ran clean, drift-check script passes, AC-6 regression on a 3rd unseen value rejects, walEscalation.ts repoint tested, docs cite the shipped enum byte-for-byte) once both reach review. Declared the BOUNDED-1/SLS dispatch-layer gap that stranded this row out of scope (general defect, not signal-queue-specific) — named, not minted: `FIX-BOUNDED1-NONPLANONLY-NONDEV-NEXTAGENT-NO-SWEEP-LANE`, for PO/PM if they want it actively tracked rather than passively surfaced by `bounded1-supervised-lane-report.sh`'s SECONDARY section.

## 2026-08-24T07:41Z — FIX-AUDITOR-TIER1-FOLD-VERDICT-NOT-DURABLE-RESPAWNS-AUDITOR-EVERY-TICK (P0 router hand-dispatch, PO ruling-20260824T0716Z, zone=multi)

**Task:** Tier-1 mem_creep FAILURE (pdf-extractor, TRUE POSITIVE — 2 real CONSTRAINT_MEMCG OOM kills of python3 child workers, dmesg-confirmed, invisible to every docker-plane probe) has no spawn debounce, so the identical unchanged breach respawns a full system-auditor subagent every 30min (~80k tokens/cycle, ~48x/day). AC-1..AC-7 mint-time + `po_ruling_20260824T0716Z` + `po_addendum_20260824T0722Z`: debounce the SPAWN, never the VERDICT — AC-3 is load-bearing, not defensive.
**Findings:** A live tick fired mid-design (coordinator, 07:36:32Z) and proved a naive full-detail-string hash debounces nothing — percentages drift every tick (86.90%→86.56%), never repeat. Designed a normalized signature (`checkname:unacked-entity-list`) that excludes percentages, status codes, AND the `(also acknowledged-degraded, tracked: ...)` transparency clause — the last exclusion is what makes the design hold for the rag-service ack-expiry hazard (`expiry_hazard_20260824T0716Z`, `FU-RAG-DEPLOY-MEMORY` one sign-off from DONE_VERIFIED) with zero new code: an ack going stale moves that container from the clause into the primary breach list, changing the signature by construction, forcing an immediate spawn (AC-4). Generic `\([^)]*\)` paren-stripping was traced against the live nested-parens shape and found to mistokenize prose as entity names — recommended a clean entity-name side-channel out of the 3 check functions instead. Window = 60min, reusing (not reinventing) the script's own existing `_fresh_threshold_minutes_for_tier` 2x-cadence convention; rejected 2h/4h as less-precedented and less conservative than AC-3's paramountcy warrants. Cron-prompt fix REPLACES the old unconditional `verdict=FAILURE→spawn` OR-arm (not an appended exception) so the stale-heartbeat arm can never re-fire behind a debounced decision — both arms are one signal per the dispatch note, confirmed live (heartbeat 379.3min stale precisely because FAILURE withholds the write).
**Output:** `docs/architecture-briefs/2026-08-24-fix-auditor-tier1-spawn-debounce.md`. Zone=multi split into 2 dependent rows via `orch-apply.sh`: `FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-1-PROBE-SCRIPT` (developer, scripts/agents-flow/, depends=[]) and `FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-2-FLOWDOC-CRON-PROMPT` (agent-father, docs/agents/system-auditor/ + cron-detect-loop skill, depends on half 1 — diverges from the FIX-A21-PREDBOUND precedent's fully-independent children since these two halves share one integrated runtime field contract). Parent row converted to umbrella (owner=po, next_agent=qa, depends=[both children]) — self-minted the split directly per AC-6's imperative, no new `supervised:true` gate added. Journal STEP architect-S44.
**Next:** developer picks up half 1 (script+test+CANONICAL doc block), agent-father picks up half 2 once half 1 lands, qa does the joint field-contract cross-check on the umbrella once both children reach review.

## 2026-08-23T13:15Z — Router manual-dispatch batch: 4 ready[] rows (P1/P2/high, non-dev off-DRS-allowlist, router-direct dispatch)

**Task:** Router hand-dispatched 4 `ready[]` rows stuck behind ~470-row RLC backlog (non-dev `next_agent`, off the DRS allowlist, no picker will ever reach them): FIX-NEWSSCOUT-OFFHOURS-SELFCOMMIT-PROSE-RECIPE-INTERMITTENT, UC-ASL-P3, FIX-ORCHSTATE-MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER, FIX-USDVND-THRESHOLD-SSOT. Worked sequentially, one WIP at a time.
**Findings (one per row, full detail in each brief):**
- **news-scout/market-watcher off-hours self-commit:** 26-line prose recipe (`task_claim` mutex + git commit + BUG escalation) is not deterministically LLM-executable — same class `coverage-stamp.sh` already solved via `mcp-call.sh`. AC3/AC4 (stale header / uncommitted c273) deliberately NOT touched — live evidence, must not hand-commit.
- **UC-ASL-P3:** LIVE COLLISION — `FIX-AUDITOR-C04-PARSEDAT-RECENCY-PREDICATE` claimed by a peer dev-team session at 12:56:49Z TODAY, actively building the SAME target script with only `checks.c04` — added the missing `depends_on` edge. Resolved all 7 BA ARCH-RATIFY questions; overruled BA's SLA-Resolver suggestion for C-06 (wrong domain — reuse `vnTradingCalendar.ts`/`marketHours.ts` via `check-foreign-flow-freshness.sh`'s own `bun -e` idiom instead). Repointed 3 sibling predicate rows (C-06/C-11/C-12) so they don't independently edit `main.md` anymore.
- **FIX-ORCHSTATE-MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER:** decided PHASED enforcement (non-fatal REPORT stage first, mirrors `orchStateSchema.ts:658-661`'s own precedent; fatal promotion gated on 14 clean days) over jumping straight to a fatal `superRefine`. Predicate reused (not reinvented) from `bounded1-supervised-lane-report.sh`. Rejected auto-deriving `next_agent` from zone (would silently defeat the deliberate-dispatch signal the flags exist to create).
- **FIX-USDVND-THRESHOLD-SSOT:** the row's own "three live planes" framing was stale — full-repo grep found `currencySignal()` (plane 2, 25500) is DEAD CODE, zero callers (+3 sibling dead fns in the same file). Found an unreported double-counting bug: `cascadeEngine.ts` runs the old fixed `MACRO_ADJUSTMENTS` AND the newer σ-based `DYNAMIC_MACRO_MAP` additively on identical domains. Option (a) relative/σ-threshold is the SSOT decision — already shipped (`macroThresholds.ts`), not new design; fix = port the Go classifier onto the same formula + retire the TS static rule + delete dead code.
**Output:** 4 briefs in `docs/architecture-briefs/` (2026-08-23-newsscout-marketwatcher-offhours-selfcommit-mechanize.md, -uc-asl-p3-auditor-db-checks-freeze.md, -fix-orchstate-mint-flagged-row-no-handler.md, -fix-usdvnd-threshold-ssot.md). All 4 board rows updated via `orch-apply.sh` (`architect_design_complete`, `architect_handoff`, `next_agent` set: 3× pm, 1× developer), 0 net-new-growth prose-ceiling violations. Journal STEPs architect-S40..S43.
**Next:** pm picks up 3 of the 4 (news-scout mechanize, UC-ASL-P3, USDVND SSOT — each needs a multi-owner split); developer picks up the orch-validate.mjs REPORT stage directly. Flagged, not fixed: fleet-wide static/dynamic macro double-counting beyond USD/VND (oil/gold), and 3 dead `macroTools.ts` functions beyond `currencySignal` — candidate follow-ups for PO/PM.
