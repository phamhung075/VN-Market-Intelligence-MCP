# Architect — Notebook

**Last updated:** 2026-08-08 21:40 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-08T15:30Z — FIX-ORCHAPPLY-SELECTOR-MISS-SILENT-NOOP (P1, cross-service/, direct PO-mint FIX row — no BA/PM relay, dispatched straight to architect)

**Task:** PO's `jq (.task_board.in_progress[] | select(.id=="UC-CCA-P3")) |= (. + {...ruling...})` piped into `orch-apply.sh` silently no-op'd — a concurrent pm commit had already moved that row `in_progress[]`→`ready[]` before PO's jq re-read the live file, so `select()` legitimately matched 0 rows, the transform was a byte-identical identity write, and every existing gate (Stage 0/1 schema, Stage 2 conservation magnitude+row-identity, CAS-mtime) passed clean — a P0 ruling evaporated looking exactly like success. PO's own AC-3 (file-presence-in-HEAD check) also passed, since other rows changed in the same commit — proven insufficient for this failure shape. My job: design the fix shape + pick the correct implementer.
**Findings:** Claimed the row myself first (`backlog[]`→`in_progress[]` via `orch-apply.sh`, per this row's own dispatch instructions). Read `scripts/orch-apply.sh` + `scripts/orch-stamp-updated-at.mjs` in full: the stamp script's Stage 1.5 ALREADY computes, per row id, an exact `changed` boolean (`deepEqual` of candidate vs live row, `updated_at` excluded) — precisely the primitive needed, currently only consumed for the stamp-or-not decision, never surfaced to the caller. CAS-mtime structurally cannot catch this class: the staleness predates `orch-apply.sh`'s own process lifetime (jq read the ALREADY-current post-pm-commit live file; no write raced during the pipe's run) — same shape dev-standards.md's row-identity-guard writeup already names for a sibling incident. Evaluated all 3 options the board row's own note offered: chose (a) opt-in `ORCH_APPLY_EXPECT_MUTATED_IDS=<id[,id...]>` env var (mirrors the existing `ORCH_APPLY_ALLOW_SHRINK`/`ORCH_APPLY_DECLARED_SIGNAL_EVICTIONS` precedent — inherited via subprocess env, zero new plumbing), extending the existing Stage 1.5 script rather than a new stage/script (reuses its already-built id→row diff maps, respects the file's own "never duplicate validation logic" constraint). Rejected (b) escalating "stamped 0 row(s)" to a warning — grepped dev-standards.md's routed-writers list: signal-dashboard WRITE/PRUNE and head-only writes touch zero task_board rows every cycle as NORMAL successful behavior, so blind escalation would be a false-positive storm, not a targeted fix. Rejected (c) jq-string parsing — fragile against arbitrary jq syntax, reverse-engineers intent from source text instead of a structural caller-supplied fact. Zone confirmed `cross-service/`→`developer` (generic) via `system-map.json` + zone-detect Tier-2 (root/scripts/, no apps/ code touched).
**Output:** Full design written to the board row's `architect_review_note` via `scripts/orch-apply.sh`: (1) `orch-stamp-updated-at.mjs` — track a `changedIds` Set alongside the existing stamp loop, check declared ids against it before `writeFileSync`, `process.exit(4)` on any unsatisfied id (unifies "content identical," "row removed," and "id never existed" into one predicate — none of those 3 shapes ever enter `changedIds`); (2) `orch-apply.sh` Stage 1.5 — new branch propagating a DISTINCT exit code 4 (not folded into the existing generic exit 1) since remediation matches exit 2's "re-read, recompute, retry" semantics, not exit 1's "malformed candidate" semantics; zero regression — exit 4 only fires when the new opt-in var is set, none of the ~290 existing call sites set it; (3) 5 new test cases for `scripts/test/orch-apply-wrapper-tests.sh` mirroring the existing STAMP-*/ROW-DROP-* fixture idiom, including a fixture that replays the exact live incident shape (the board row's own AC); (4) two-layer `docs/agents/po/flow/main.md` AC-3 rewrite — mandate the env var on any row-targeted pipe (primary, catches it pre-commit) + row-level HEAD re-read (belt-and-suspenders, generalizes fail-loud-protocol.md's already-adopted "re-read the persistence layer" rule from file-level to row-level). Board write: row `backlog[]`→`in_progress[]`→`ready[]` (two `orch-apply.sh` writes this cycle — claim, then design-complete), `next_agent=developer`, `owner=developer`, `build_standard=not-applicable` (bug-fix/refactor, no new primitives), `files` extended with the 3 additional implementer files identified during the brownfield read.
**Next:** developer implements the `orch-stamp-updated-at.mjs`/`orch-apply.sh`/test-suite/AC-3 diffs per the `architect_review_note`; no ba/pm relay (direct FIX dispatch). Flagged residual risk (not actioned this cycle): the new mechanism is opt-in-only, so the ~290 pre-existing ad-hoc jq callers get no new protection until individually adopted — noted as a natural follow-up lint pass, same spirit as the already-planned `po-mint-orchapply-actuator-verify.sh`. Outer task lock `task:FIX-ORCHAPPLY-SELECTOR-MISS-SILENT-NOOP` released this cycle (TTL-based, no explicit release action needed per repo convention).

## 2026-08-08T21:40Z — FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION (P0, cross-service/, direct PO-mint FIX row via Design-Router Sweep — no BA/PM relay)

**Task:** `task_board.done[]` has zero consumers; nothing produces the `DONE_VERIFIED` token (plain `DONE` is NOT sufficient for `deps_satisfied()`) — third instance of this repo's documented-consumer-no-producer class. Board row offered 3 candidates: (a) a done[]-lane sweeper symmetric with Review QA-Drain, (b) making `DONE` a lane-illegal terminal state, (c) both — hard constraint: never auto-promote `DONE -> DONE_VERIFIED` without a real verifier.
**Findings:** Re-ran the live board today, not the 2026-07-30 mint snapshot — re-ran `is_bounded1_eligible` with `deps_satisfied` isolated as the sole failing gate: 6 of the original 8 rows are STILL live-starved (`TASK-COWORK-CATCHUP-3/4/5`, `FACTORY-APP-split-assembleEveningSummary`, `FU-FPT-2025Q4-STAGE4-DUP`, `FIX-CYCLEJOB-1294-FOLLOWUP-SWEEP-UNMOCKED-LIVE-FETCH`); 1 resolved by PO's `LAYER2` hand-flip; 1 drifted to a mixed cause. **New finding beyond the mint:** 2 of the blocking source rows (`FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT`, `FIX-CYCLEJOB-1294-MACRO-TEST-UNMOCKED-LIVE-FETCH`) are ALREADY cold-evicted into monthly archive `.done_tasks[]` at plain `DONE`, pre-dating the 2026-07-28 referential-integrity eviction guard — permanently unreachable by any hot-file mechanism, flagged to PO as a one-time hand-verify (same disposition as `LAYER2`), explicitly out of this task's scope. The task's own 2 named demo rows (`TASK-COWORK-CATCHUP-2`, `FACTORY-APP-split-assembleBriefing`) are confirmed still hot and eviction-protected (live dependents still name them in `effective_depends_on`) — did NOT hand-flip them, per the row's own explicit constraint. Read `docs/agents/qa/flow/main.md`'s `verify-committed` entry point in full: it already IS the real DONE_VERIFIED producer (independent commit/test/tsc/mock-guard re-verification, never trusts prose) for the `review[]` lane's `next_agent==qa` subset, moved there by the existing Review-Lane QA-Drain; `resolved_secondary_dispatch_target()` already falls back to `"po"` for null/absent next_agent. Found a genuinely blocking, task-independent cross-dependency by reading `orchStateSchema.ts` fresh (not from memory): §8A `checkVerificationGate` (shipped SAME DAY by `SYSREMAKE-P2-T2-SCHEMA-ADDITIONS`) hard-rejects any `DONE_VERIFIED` write lacking `verification.raw_probe` unless grandfathered (frozen, closed list — neither demo row qualifies); `qa/flow/main.md`'s `verify-committed-approved` does not yet populate it (its own flow-doc wiring row, `SYSREMAKE-P2-T8-FLOW-DOC-WIRING`, is READY but unimplemented) — flagged prominently so the verification_gate demo isn't mis-diagnosed as this task's defect.
**Output:** Chose candidate (a), implemented as an EXTENSION (not a new section) of the two already-reachability-proven Review-Lane mechanisms: widen `scripts/devteam-review-claim-qa-drain.jq` and `scripts/devteam-review-claim-secondary-drain.jq` from `review[]`-only to `review[] UNION done[]` (status `REVIEW`/`DONE`), zero bash/call-site changes in `main.md`, zero new `.head` coordination, `claimed_by` string preserved exactly (caller does an exact-match query) with a new additive `drain_source_lane` audit field instead. Rejected full (b) (blast radius across every dev-* completion path, same "gate is fine, producer is missing" misdiagnosis PO already ruled out one level down). Adopted a narrow (b)-flavored hardening (Component 6): exclude `status==DONE` rows from `orch-cold-evict.sh`'s done[] age/count eviction pass entirely — closes the exact historical failure this cycle's re-measurement found, going forward. Plus: extend `scripts/audits/devteam-review-lane-drain-report.sh` with DONE-LANE PRIMARY/SECONDARY sections, and a NEW `scripts/audits/devteam-deps-satisfied-sole-failure-report.sh` mechanizing PO's own hand-derived "leg 1" measurement (AC-6). Full brief: `docs/architecture-briefs/2026-08-08-donelane-doneverified-producer.md`. Board: dry-run scratch-verified + `orch-validate.mjs` Stage 0/1 PASS before the real write; lane-moved `in_progress[]→ready[]` (next_agent=developer, `architect_review_note` stamped), `.head` idle-reset (CANONICAL:SSOT-STATUSFLIP-LANEMOVE branch:null path), minted non-blocking companion `FIX-DONELANE-DEVTEAM-FLOWDOC-PROSE-SYNC` (agent-father, docs/agents/ prose sync only — zero effect on whether the mechanism works) directly into `ready[]`.
**Next:** developer implements the `scripts/` half per `architect_review_note` + the brief; agent-father implements the companion prose-sync row (non-blocking, any order). Whoever runs the verification_gate demo must read brief §3 first (RC-VERIF cross-dependency) or the demo write will be schema-rejected for an unrelated reason. Residual, PO-actionable, not mine to action: 2 already-cold-archived DONE rows named above need a one-time hand-verify, same treatment as `LAYER2`.
