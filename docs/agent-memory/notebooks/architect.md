# Architect — Notebook

**Last updated:** 2026-08-05 16:35 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-05T16:35Z — SPIKE-BOUNDED1-ELIGIBILITY-CONTRACT-REVIEW (zone=cross-service/, high, dev-team Review-Lane SECONDARY-Drain dispatch, stale review[]-lane sign-off)

**Task:** Independently re-verify (not the 07-21 self-report) whether the "refactor eligibility gating into one shared detail-resolution contract" recommendation is still ADOPTED-AND-HOLDING 15 days later, then dispose the stale row.
**Design/Finding:** `scripts/lib/devteam-eligibility.jq` confirmed live (31 defs, last commit TODAY), all 5 originally-cited call sites still `include` it (grep-verified), adoption expanded to 30+ callers — every picker built since 07-21 extends this ONE library, never forks. 2 isolated regression suites pass live (5/5, 7/7; one dated today closed a real self-target bug through the shared fn). Ran the 2 broader dispatch-chain audit instruments for corroboration — both exit non-zero, but neither FAIL traces to this library's predicates: (1) a stale `ready[]` `promoted_by` stamp survived a same-day PO WIP-eviction because `devteam-backlog-claim-bounded1.jq` has no self-contained WIP recheck (relies on `main.md:549`'s outer bash gate — harmless today, WIP=1 live, but a latent gap); (2) 5 backlog rows resolve `dispatch-lane=none` because owner+next_agent are both null at mint time — the resolver correctly reports unresolvable, an ongoing instance of the already-PO-tracked mint-hygiene residual, not a predicate defect.
**Output:** `review[]→done_verified[]` via `orch-apply.sh`; full evidence in `completion_note`. Both new findings flagged out-of-scope (telegram + status_note pointer), not folded into this row.
**Next:** PO/dev-team triage: (a) decide whether `devteam-backlog-claim-bounded1.jq` needs its own WIP recheck or the eviction path should clear stale stamps; (b) data-fix the 5 null-owner/null-next_agent backlog rows.

## 2026-08-05T09:40Z — FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE (zone=apps/mcp-server/, P0/M, direct PO board-mint, dev-team triage tick)

**Task:** 66 `fallback-<TICKER>-<YEAR>-<Q>` financial_reports rows (0/66 ever produced a bctc_layout_units/bctc_table_rows row) root-caused as "why are fallback-% ids excluded from extraction" — 45 fail-loud `[bctcExtractReconcile]` BUGs in 36h.
**Design:** Live-confirmed (docker logs, pdf-extractor container, 96h) that `pushBctcLayoutHandler.ts`/`pushBctcTableHandler.ts`/`pushBctcMdTablesHandler.ts` gate `report_id` on `isValidUuid()` FORMAT — pre-dating SPRINT-1330's fallback-id minting by weeks. PEK genuinely extracts (67/67 pages etc.) but the callback push is HTTP-400-rejected every time (50x/96h, 20 distinct ids) — the reconcile job's 3-table success check is a guaranteed-0 tautology, not a diagnosis. Fix: swap the FORMAT gate for an EXISTENCE gate (`SELECT 1 FROM financial_reports WHERE id=?`) in all 3 handlers — `db` already DI'd, zero new plumbing; strictly TIGHTER than today (closes an orphan-write gap on syntactically-valid-but-unknown UUIDs), zero injection-risk regression (writes already fully parameterized).
**Finding:** all 3 existing push-handler test suites' happy-path fixtures never seed a `financial_reports` row for their own golden UUID before pushing — a pre-existing gap that must be fixed alongside the code change, not just a nice-to-have (flagged exact fixture + 2 new required test cases in the brief). Grepped `isValidUuid` fleet-wide: ~14 MORE call sites (bctcInspectHandler.ts x6 = the actual `get_bctc_page_text`/`get_bctc_page_image` MCP tool surface, +correction/eval handlers, +2 local copies) gate the identical assumption on the READ side — out of scope for this row's AC but flagged as a required fast-follow or the newly-written data stays functionally unreadable by every consumption tool.
**Output:** `docs/architecture-briefs/2026-08-05-fix-bctc-fallback-shell-reports-structurally-unextractable.md`. `architect_review_note` written via `orch-apply.sh`, `backlog[]→ready[]`, `next_agent:pm`.
**Next:** pm decomposes into dev-mcp-server task (3 handler files + 3 test files); PO to mint the read-side fast-follow FIX row (§5 of brief) sequenced AFTER this row verifies live.

## 2026-08-05T09:32Z — FIX-AUDIT-OUTPUT-CONTRACT-SIGNALQUEUE-ROWS-WRITTEN-SELFREPORT-MISMATCH (zone=cross-service/, P2/S, direct PO board-mint, dev-team triage tick)

**Task:** `scripts/audit-output-contract.sh`'s V1 independent `.signal_queue` cross-check fired 4x/50min, BOTH directions (marker>independent AND independent>marker); PO asked to rule out ordinary concurrent-write timing skew before concluding "trust the higher value" is wrong.
**Design:** Ruled out concurrency by reproducing BOTH directions against the real script in a single-process, zero-peer scratch harness — deterministic, not a race. Bug A (marker>independent, 4412/4415): `--cycle-start-ts`=`$FIRE_TICK` is minute-precision, row `.ts` is second-precision, raw jq string `>=` drops any row in the tick's own clock-minute. Bug B (independent>marker, 4420 — the dangerous inflate-self-report direction): no call site passes `--from-agent`, so every tier/session shares the default `"system-auditor"` identity and V1 picks up ANY peer tier's concurrent row.
**Finding:** The reconciliation rule itself was not the defect — it was being fed corrupted operands. Once Bug A (port the `to_epoch` helper already live in `emit-audit-signal.sh::_ledger_prune_and_lookup()`) and Bug B (thread the already-unique `FIRE_TASK_ID` through as a new `--cycle-tag`/`audit_cycle_tag` passthrough field, zero schema migration — `SignalRowSchema.passthrough()` confirmed live) are fixed, take-the-max is provably sound: the only remaining divergence sources (post-write drain, or a dropped marker line) both genuinely favor the higher operand.
**Output:** `docs/architecture-briefs/2026-08-05-fix-audit-output-contract-signalqueue-mismatch.md` (diffs, test plan T12/T13). `architect_review_note` written via `orch-apply.sh`, `backlog[]→ready[]`, `next_agent:developer` (matches `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` precedent, same S/P2 shape).
**Next:** developer implements brief §4 (2 scripts + 7 call-site flag threads) + §5 tests; verification gate = replay §0's two repros, both must show no VIOLATION.
