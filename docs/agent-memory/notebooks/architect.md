# Architect — Notebook

**Last updated:** 2026-08-25 03:31 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-25T03:24Z — FIX-AUDITOR-TIER1-PROBE-SCORES-DELIBERATELY-DISABLED-LAUNCHD-JOB-AS-DEGRADED (P1, po_expedited, dev-team incident-lane consumer dispatch, zone=cross-service/)

**Task:** `_check_launchd_agents()` (scripts/agents-flow/auditor-tier1-probe.sh) has only two dispositions for a not-loaded label — bad, or ack-ledger-suppressed. Fleet-push's ack entry was removed 2026-08-24 (commit `930297f37`, staleness rule, pending this row as the durable fix) so Tier-1 has scored `verdict=FAILURE` every 30min tick since. HARD CONSTRAINT (status_note): never re-arm fleet-push by any mechanism (plist Disabled key, launchctl enable/load/bootstrap); remedy is a scoring change only.
**Findings:** Live-verified this cycle: `plutil -p ~/Library/LaunchAgents/com.vn-market.fleet-push.plist` → `Disabled=>1`; `launchctl print-disabled gui/$(id -u)` independently confirms membership; repo-copy plist has NO Disabled key (confirms AC-2's "read the installed plist, never the repo copy" is load-bearing, not defensive). Regression control: docker-events' installed plist has no Disabled key — stays on the existing ack-ledger path, untouched. `_check_launchd_agents()` already has an rc=0-with-note pattern (reused for the acked case) that `run_probe()` treats as ALL_GREEN-with-transparency — reused verbatim for the new disposition rather than inventing a second channel. `docs/data/auditor-tier1-last-trigger.json`/`spawn-debounce.json` confirmed the live FAILURE + spawn_count=9 this cycle, proving the debounce (sibling row) bounds churn but can never converge the verdict — only this design does.
**Output:** `docs/architecture-briefs/2026-08-25-fix-auditor-tier1-launchd-expected-disabled.md` — new `_launchd_label_expected_disabled()` (installed-plist Disabled key OR `launchctl print-disabled` membership), new `LAUNCHD_INSTALLED_DIR_PATH` env var, EXPECTED-DISABLED labels excluded from the spawn-debounce entity side-channel by construction. Scope decision: loaded-but-nonzero-exit branch deliberately untouched (a Disabled=1 job can never be loaded, so widening there risks hiding a real crash). Authored AC-6..AC-9 (execution-verifiable: live probe run must return ALL_GREEN while fleet-push stays disabled/absent from launchctl list) on top of PO's mint-time AC-1..AC-5. Journal STEP architect-S46. Row lane-moved `in_progress[]`→`ready[]`, `next_agent: developer`, `.head` reset to idle — both via `orch-apply.sh`. Had to shrink the `architect_review_note` twice to clear `orch-row-prose-ceiling-check.mjs` — the row's pre-existing prose (status_note/po_ruling/po_expedite_note/evidence/acceptance) already sat at ~9085B against the 12000B ceiling with a 0B live baseline (in_progress[] is outside the 3 tracked lanes), leaving only ~2900B of headroom for any new field.
**Next:** developer implements per the brief (`auditor-tier1-probe.sh` + comment-only `auditor-launchd-ack.json` edit); do NOT touch `auditor-tier1-probe.test.sh` — owned by sibling `FIX-AUDITOR-TIER1-PROBE-TEST-INVERTED-ASSERTION-L1422-FALSE-GREEN`, flagged in the brief that its `launchctl()` stub will need a `print-disabled` branch.

## 2026-08-24T15:57Z — UC-MDH-P2 (P1 SPRINT-S, DRS-dispatched via ba, zone=multi)

**Task:** Design FR-3/FR-4/FR-6's atomic deploy-gated bundle (remove dead `append_session_record` MCP tool) + adjudicate B2 file-ownership split, per BA's spec (`docs/handoffs/UC-MDH-P2-BA-spec.md`, commit `5e1015444`). Design-only dispatch — deploy_gate=user-approved-off-market, no build/rebuild this cycle.
**Findings:** Confirmed `scripts/gen-tool-registry.ts`/`gen-project-stats.ts`/`gen-tools-index.sh` are live working generators chained source→registry.json→project-stats.json+tools/list/INDEX.md — FR-4 needs almost zero manual JSON surgery (one hand-maintained line in `system-map.json`), not the fresh 3-way sync BA implied. Caught 2 BA scope corrections via direct file reads: (1) BA's 13-file FR-5 consumer list conflates *instructional* docs (safe-now) with *structural-inventory* docs (must land atomically w/ FR-3 or create a false "the tool doesn't exist" claim the running container contradicts) — reclassified `docs/agents/tools/list/append_session_record.md` (orphaned per-tool stub, no generator deletes it) and `docs/agents/tools/list/INDEX.md` (GENERATED, hand-editing would violate its own header) from FR-5→FR-4-extended, real FR-5 safe-now scope = 10 files not 13; (2) BA's FR-6 said "delete the 2 append_session_record test cases" but live read of the 17-`it()` test file found 6 to delete + 2 to surgically edit (both also exercise `update_memory_file`, outright deletion would drop real coverage) — exact line ranges documented. Ruled B2 YES: `digest-predict/init.md`, `market-analyst/init.md`, `tools/package/digest-predict.md` are agent-father's exclusive lane (evidence: agent-father's own `commit_zone.allowed` + its `create.md`/`edit-prepare.md`/`scaffold-files.md` explicitly author `tools/package/<agent>.md` + live fleet precedent `dev-standards.md:1987` already ruled the identical file class exclusive on a different row) — FR-5 splits 3 (agent-father) / 7 (developer).
**Output:** Appended `[Architect] Brownfield Findings` to `docs/handoffs/UC-MDH-P2-BA-spec.md` (exact line-range deletes for `agentMemoryUpdateTools.ts`/`registry.ts`/the test file, the 5-step FR-4 regen sequence, B2 ruling + FR-5's corrected 3/7 split, FR-1/2/7 confirmatory verification, risk flags). Journal STEP architect-S9. Row lane-moved `in_progress[]`→`ready[]`, `next_agent: pm` per its own NEXT.
**Next:** pm decomposes into per-owner tasks: developer (FR-1/2/3/4/6/7 + 7 of FR-5's files, size M — the deploy-gated half stages but does not deploy/rebuild), agent-father (FR-5's 3 agent-identity files, safe-now). B1 (does `deploy_gate` cover the whole row or only FR-3/4/6?) is PO's, running in parallel — did not answer it.

## 2026-08-24T08:32Z — FIX-SIGNALQUEUE-OPEN-STATUS-DRAINED-BY-NOTHING-EVICTED-BY-NOTHING (P1 router hand-dispatch, zone=multi)

**Task:** Router hand-dispatch (next_agent=architect is non-dev, held out of BOUNDED-1; not plan_only, so SLS can't see it either — genuine dispatch-layer gap, see Next). Sibling developer row `FIX-ORCHAPPLY-SIGNALROW-STATUS-UNVALIDATED-ADMITS-UNPICKABLE-UNEVICTABLE-VALUES`'s own AC-2 explicitly deferred "what is the ratified status set, and what happens to OPEN" to this row's ruling; AC-1 deferred the to:ops delivery-hole decision the same way.
**Findings:** Live file is already 100% clean against the vocabulary I ratified (READ=86/RETRACTED=12/triaged=8/NEW=1, zero OPEN, zero lowercase new — PO's own dedup this morning already cleared the population this row's title names). Traced the write topology: `orch-validate.mjs` (shell/jq path) and `writeOrchStateAtomic` inside `appendSignalQueueRow` (TS in-process path, used by walEscalation.ts/tasksMdJanitorJob.ts/3 signal writers) BOTH converge on the exact same `OrchStateSchema.safeParse` — one Zod enum closes both paths, no dual mechanism needed. Biggest finding: `apps/mcp-server/src/scheduler/walEscalation.ts:30` is a SANCTIONED, correctly-cased, already-shipped `to:'ops'` producer (WAL-checkpoint escalation) — proves the delivery hole empirically (ops has zero cowork-schedule.json entries, no polling loop, no push-spawn target), not hypothetically. Ruled to:ops INVALID, repointed to to:po (PO's triage-signals.md already bridges auditor/scheduler findings to owner:ops task-board mints from there — no new consumer needed). Ratified 8-member SignalStatusEnum (NEW/READ/RESOLVED/SUPERSEDED/ACUTE-RESOLVED-ROOT-TRACKED/triaged/TRIAGED/RETRACTED); OPEN and PARTIAL both removed from the type (zero sanctioned producer for either) and hard-rejected at write, not quarantined — quarantine is the wrong tool for a provable producer mistake. Declined a dual-file live-vs-candidate grandfather script (prose-ceiling-check.mjs's own pattern, cited by the sibling row's AC-3) since the live file is provably clean today; chose hard Zod enum + a pre-deploy live-conformance re-check instead, backed by a new standing drift-check script (§6 of the brief) so the status/orch-cold-evict.sh mirror and the to/system-map.json mirror can never silently diverge the way the 2026-08-08 fix's allowlist did. Also found (not fixed, flagged for the developer child) that `appendSignalQueueRow`'s retry loop has no try/catch around the write — a Zod throw propagates uncaught, contradicting the function's own "do NOT throw" docstring; this ruling's enum is what will first make that reachable.
**Output:** `docs/architecture-briefs/2026-08-24-fix-signalqueue-status-and-recipient-vocabulary.md`. Enriched the existing developer row in place (files+=[walEscalation.ts, new drift-check script], size S→M, 4 new AC items carrying the concrete ruling) rather than minting a duplicate mechanism row. Minted one new docs-only child, `FIX-SIGNALQUEUE-STATUS-TO-VOCABULARY-DOCS` (agent-father, depends=[], parallel-safe — the vocabulary is fixed by this brief, not pending developer discretion). This row converted to the umbrella (children/depends=[both], next_agent=qa), mirroring the auditor-tier1-spawn-debounce precedent's exact shape. Journal STEP architect-S45.
**Next:** developer picks up the enriched mechanism row; agent-father picks up the docs child in parallel; qa does the joint cross-check (pre-deploy live-check ran clean, drift-check script passes, AC-6 regression on a 3rd unseen value rejects, walEscalation.ts repoint tested, docs cite the shipped enum byte-for-byte) once both reach review. Declared the BOUNDED-1/SLS dispatch-layer gap that stranded this row out of scope (general defect, not signal-queue-specific) — named, not minted: `FIX-BOUNDED1-NONPLANONLY-NONDEV-NEXTAGENT-NO-SWEEP-LANE`, for PO/PM if they want it actively tracked rather than passively surfaced by `bounded1-supervised-lane-report.sh`'s SECONDARY section.
