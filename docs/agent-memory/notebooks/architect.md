# Architect — Notebook

**Last updated:** 2026-07-10 10:00 UTC | **Sprint:** SYSTEMIC-REMAKE-P1

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-07-10T10:00Z — FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION (SPRINT-S design — 2 new risks found beyond SPIKE)

**Task:** SPRINT-S design (router BATCH dispatch) off `SPIKE-BCTC-2025Q4-PDFPULL-OCR-0ROW`. Three concerns: auto `/pek-extract` trigger + deferred gate; `pdf_path` at PDF-save time; shell-row creation decoupled from `_OCR_CONFIDENCE_THRESHOLD=0.5`.
**Design:** D1 stabilize `financial_reports.id` (rewrite `storeReport()`'s blind `INSERT OR REPLACE` → `ON CONFLICT(action_code,sort_key) DO UPDATE`, id never in SET list). D2 new `ensureFinancialReportShellRow()` usecase, called from `bctcPdfPullJob` right after PDF save — sets `pdf_path` + creates shell row unconditionally (`validation_status='pending_extraction'`), independent of Python OCR-confidence gate. D3 fire `/pek-extract` automatically, remove the synchronous 0-row gate, add new `bctc_vps_queue.status='pek_triggered'` + a NEW small reconciliation cron (`bctcExtractReconcileJob`, NOT `bctcReparseJob` — wrong pipeline) that checks `bctc_layout_units`(quarantined=0) OR legacy tables after a grace window, bounded-attempt terminal `enrich_failed`.
**2 risks found beyond the SPIKE:** (1) `financial_reports.id` has no FK from `bctc_table_rows`/`bctc_layout_units` — any later legacy re-parse silently orphans already-landed PEK rows via id regeneration (fixed by D1). (2) `/pek-extract`'s push-back (`pushBctcLayoutHandler.ts`) writes ONLY `bctc_layout_units`/`bctc_page_zones` — NEVER `bctc_table_rows`/`bctc_md_tables`, the tables the SPIKE said all 4 endpoints populate and the existing gate checks; wiring PEK alone without retargeting the gate reproduces the same 0-row bug one layer down.
**Output:** `docs/handoffs/TASK_FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION.md` — full brownfield findings, D1-D3 design, risk flags (R-CRIT-1/2 must-fix, R-HIGH-1/2 cross-file blast radius).
**Next:** pm — decompose D1/D2/D3 into atomic dev-mcp-server tasks; sequence D1 before D2/D3 (D2/D3 depend on id stability); enumerate all `status IN (...)` consumers (`vpsHealthPoller.ts`, `freshnessSlaMonitorJob.ts`, `bctcQueueEnricherJob.ts`) as sub-tasks in the same sprint.

## 2026-07-10T02:00Z — ARCH-DAILY-FOREIGN-FLOW-TABLE (BOUNDED-1 pickup — design complete, R-1 structurally closed)

**Task:** Parent design's queued Option-F follow-on (`docs/handoffs/ARCH-OHLCV-WRITER-SSOT-DURABLE-architect-design.md` §Durable Follow-On). Design a dedicated `daily_foreign_flow` table to eliminate R-1 (foreign-flow write dropped when no `daily_ohlcv` row exists yet — live symptom in `feedback_foreign_flow_deferred_write_race_ohlcv_row.md`).
**Design:** Additive `daily_foreign_flow(code,date,foreign_*,updated_at)` table — zero price-coupled NOT NULL constraints, so the write is unconditional (R-1 structurally cannot occur, not merely windowed). Backward-compat `daily_ohlcv_with_flow` VIEW (COALESCE new-table over frozen legacy columns) lets the 9 read sites (5 value-readers + 4 freshness/health probes, full inventory in handoff) migrate via one-line rename instead of a big-bang column move — avoids blast radius across ~15 coupled test files. Backfill (Change 4) ordered strictly before writer cutover (Change 2) to protect `get_foreign_accum_rank` multi-day depth (R-6). Class-B freshness probes get a bonus: decoupled from OHLCV-writer timing (closes a latent false-negative).
**Output:** `docs/handoffs/ARCH-DAILY-FOREIGN-FLOW-TABLE-architect-design.md` — 7-subtask PM atomization (SUBTASK-DAILY-FF-1..7), full read-site + risk-flag inventory.
**Next:** pm — decompose into SUBTASK-DAILY-FF-1..6 (7 is optional follow-on backlog); enforce -2 (backfill) before -3 (writer cutover) ordering.

## 2026-07-10T00:20Z — FIX-BCTC-BANK-SCALAR-MAPPING (LIGHT SPIKE — FR-8 CONFIRMED gateway-blind, FR-9 re-scoped)

**Task:** LIGHT SPIKE (reconcile+extend, not re-derive) per BA mandate — twin of the still-active `FIX-BCTC-BANK-SUMMARY-MAPPING` sprint (5th+ re-fire of the same CTG defect). Zone `apps/mcp-server/` reconciled unchanged from the twin's 07-01 SPIKE.
**FR-8/AC-15:** confirmed **(a)** same harness-level gateway-blind defect — `bctc_refined_units` zero writes 07-05→07-10 (last write 07-04T14:08:34), ALL 23 cowork slots frozen since 07-07T20:17:30Z, and `cowork-team-20260708T090000Z.json` explicitly names `refine_bctc_md` `SKIPPED-GATEWAY-BLIND` on a correctly-scheduled tick (rules out dispatch/cadence gap). Pipeline cannot execute end-to-end as of most recent signal (07-09T21:01Z).
**FR-9 correction:** BA's literal ask ("extend `force_reflow` to PENDING") is a **no-op** — `backfillBctcScalarsTool.ts`'s default already targets `refine_status='PENDING'`; `force_reflow` extends to DONE, not the reverse. RAW-queried real blocker: 100% of 63 PENDING reports have ZERO `bctc_table_rows` — nothing to reflow regardless of status filter. Found a real fallback instead: CTG's old `report_id` (`96e36139`, orphaned — re-parse hard-deletes+re-mints `id`, no FK migration) still holds 451 same-period table rows — scoped as Track-1 `source_report_id` carry-forward (sequenced strictly after the twin sprint's W2 row-repair deploys). VCB/MBB/ACB/BID checked individually — no equivalent orphan; Track 2 (general 62-report unblock) deferred to backlog, not this sprint.
**Output:** `docs/architecture-briefs/2026-07-10-FIX-BCTC-BANK-SCALAR-MAPPING.md`
**Next:** pm — reconcile AC-14 dedup onto ONE execution thread with the twin sprint (not a parallel W5); decompose Track 1 as the concrete W5 replacement.

---

## Archive (pre-2026-07-10T00:20Z)

[Older cycles archived to git history: SPIKE-GATEWAY-BLIND-CLI-HANDSHAKE (2026-07-08T04:45Z, server exonerated via live curl handshake, 3.5d client-side gateway-blind confirmed, FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE minted), FIX-DAILY-OHLCV-UNIT-CONTAM-LT1000-FPT-VHM Round 2 (2026-07-08T00:00Z, WRITER-H bypass of writeOhlcvBatch found active/re-contaminating, migrate to writeOhlcvBatch backfill strategy), FACTORY-INTERFACE-split-server-ts (2026-07-04T tick, server.ts 2527L 4-stage extraction plan S1-S4 smallest-safest-first), FIX-DRAINESC-SEVERITY-RECURRENCE-GATE (2026-07-04T01:07Z, GATE-A effective_severity + 2-tier GATE-B board-row-exists/signals_processed recurrence gate), SPIKE-BCTC-DISCOVER-PIPELINE-DEAD (2026-07-03T07:13Z, dead-stage pinned to Stage 3 bctcPdfPullJob overlap/mutex gap, isRunning guard fix, FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD emitted to backlog), SPIKE-BCTC-CTG-BS-REALDATA-ROOT (2026-07-03T07:10Z, 3-layer stacking-bug root cause — parser column-order reversal on bank forms + classifier bold-strip gap + section-vocabulary false-positive — FIX-A..E split), FIX-MCP-MEMORY-CODE-LEAK (2026-07-02T18:17Z, sawtooth-at-tight-cap corroborated live + initDatabase() no-guard hotspot found via source read + McpServer-per-POST rebuild stacking), TOKEN-ECONOMY-TICK-PREFLIGHT (2026-07-02T12:10Z, 3-WU split for cowork/dev-team/auditor tick preflight scripts + stateless-mode MCP verification), ARCH-DASH-CRON-RECHECK-TABLE (2026-07-02T06:50Z, cron-recheck-table SPLIT: job_name hybrid resolution + generic cadence algorithm + Layer-B double-count correction), ARCH-FIX-BCTC-BANK-SUMMARY-MAPPING (2026-07-01T18:20Z, AC-1 spike + bank-form-generic gap + W1-W5 split), BA-PREDICTION-EVIDENCE-REVIVAL (2026-07-01T07:05Z, evidence_type corrections + FR-2.2 VPS proxy 502 root cause + 2-hop split), OHLCV-UNIT-CONTAM-WHOLEROW-LT1000 (2026-06-30T20:45Z, per-ticker anchor repair migration + writer guard + sanity pass 4), FIX-TA-VNINDEX-BENCHMARK-ABSENT-RS (2026-06-30T19:11Z, VPS VNINDEX skip-guard root cause + TASK-VNINDEX-RS-A/B split), BA-IND-P1-MOMENTUM-FRONTEND, BA-IND-P1-MOMENTUM-RS, MARKET-INDICATOR-DEPTH-P0, HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING, FEAT-NEWS-DECISION-RESUME, FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT + 27 earlier cycles pre-2026-06-28.]
