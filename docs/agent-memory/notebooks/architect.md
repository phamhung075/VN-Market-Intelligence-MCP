# Architect — Notebook

**Last updated:** 2026-08-06 07:36 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-06T07:36Z — FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND (zone=cross-service/, P1, PO-direct-mint FIX, design-only)

**Task:** dev-team Step 0b pipeline-resume (main.md WF-1 task_status L263-266, WF-2 should_hold L289-298) never scans done[]/done_verified[] — a gateway-less specialist closing its own head task (INV-GATEWAY-1, normal) leaves `.head` pinned in_progress at a finished task, and the next tick re-spawns it. Live-hit 2026-08-06 (router hand-patched `.head` before the duplicate spawn fired).
**Design:** 2 in-place edits, no new sub-flow: (1) widen WF-1's task_status source array to include done[]/done_verified[] (in_progress/active_sprints kept first — conservative-safe if a row is ever transiently in both during a lane-move race); (2) new WF-1b branch reusing `is_terminal_task_status` (scripts/lib/devteam-eligibility.jq, already backing wip_in_progress/wrapper-autoclose) — on a terminal hit, idle-reset `.head` exactly like the BLOCKED carve-out's jq write but WITHOUT its lane-move (row already correctly resident), JUMP TO drain-signals. This is the only branch that can satisfy "never fall through to S2" — should_hold-only widening cannot (a non-supervised terminal row already yields should_hold=false today and still falls through). WF-2's own row-lookup also widened per AC-1's literal text, flagged as provably-unreachable-post-fix defense-in-depth (do not "clean up" without re-verifying).
**Test strategy specified:** new `scripts/audits/devteam-pipeline-resume-terminal-lane-verify.sh`, mirrors `execute-tier-branchnull-review-headidle-verify.sh`'s synthetic-fixture-only pattern — positive (done[]/done_verified[] head-pin → idle-reset), negative (genuine in_progress unaffected; BLOCKED carve-out unaffected).
**Note:** row's own stated duplicate-mint (two backlog[] copies, same id) was already self-resolved by po (`po_dedup_fold_20260806T0726`) via a concurrent write between my first read and this write — verified live (jq+python recount) before writing, no dedup action needed here.
**Output:** `architect_review_note` written on the row (full design), `backlog[]→ready[]`, `next_agent=developer`, via `orch-apply.sh`. Flagged co-edit adjacency: FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE (BACKLOG, architect, same Step 0b block, different mechanism) — re-diff before either lands.
**Next:** developer implements; RLC (Ready-Lane Consumer) is the designed picker for an architect-resolved `ready[]` row — no PM decomposition needed (already atomic, single file, ACs fully specified by PO).

## 2026-08-06T07:03Z — GUARD-COWORK-NOTEBOOK-AGENTS-SELF-EDIT-FLOW-DOC (zone=multi, P1, supervised+plan_only, retry of a session that died pre-output)

**Task:** 4 confirmed instances (2 reverted/kept precedents 07-15 + a 3rd stranded-uncommitted-but-good instance found by PO 07-28) of 7 "notebook-only" cowork agents self-editing their own/sibling flow docs. PO's own row note already corrected the original strip-Edit premise (breaks ~207 committed docs/signals/*.json files by design).
**Root cause found (not assumed):** grep traced ALL 4 instances to ONE shared skill — `cowork-end-cycle/SKILL.md` Step 3 unconditionally invokes `doc-self-heal/SKILL.md` for every cowork agent with zero boundary check (15 call sites across the 7 agents; instance 1's own self-report string "Doc self-heal" is a verbatim match). Not 4 separate agent bugs — one control point.
**Design:** 4-part fix, one commit (brief §8 — atomicity matters, mechanism-before-channel leaves a dead end): (1) `docs/data/system-map.json .project.agents[]` gains a `write_boundary` allow-list per agent (7 named, 2 explicitly excluded with reasons); (2) new PreToolUse `Write|Edit` hook keyed on the Claude Code hook payload's `agent_type` field — verified against the installed CLI's own Zod schema (not assumed) that this field exists and equals the Task-tool spawn name; registered in `.claude/settings.json` (NOT `.settings.local.json` — confirmed gitignored, a separate pre-existing gap for the sibling `orch-state-hook-prewrite.mjs`, flagged not fixed); (3) `doc-self-heal/SKILL.md` new Step 0 redirects a blocked agent to a `docs/signals/doc-fix-proposals/` artifact + companion signal (reuses existing Pipeline A drain — no new drain code) instead of a dead end; PO gets one new triage row (independent re-verify — PO holds `mcp__gateway__call_tool`, agent-father does not) and agent-father gets one new dispatch row reusing `edit.md`'s existing apply engine; (4) 7 frontmatter + `cowork-boundary/SKILL.md` corrected to state the real write surface. Item 2 (alert-commander's original skip-`get_market_context` idea) re-specced correctly, explicitly NOT implemented — follow-up only, sequenced after this fix.
**Output:** `docs/architecture-briefs/2026-08-06-guard-cowork-notebook-agent-write-boundary.md`. Board: `in_progress[]→done[]`, `status:DONE`, `next_agent`/`routed_to`=agent-father, via `orch-apply.sh`. Routing signal emitted (`docs/signals/architect-2026-08-06T070333Z-brief-complete.json`, `to:po`, reuses the existing `brief_complete` triage row → `route_to: agent-father`).
**Next:** agent-father implements per brief §4.1-4.4. PO's `mcp__gateway__call_tool` grant is load-bearing for the new doc-fix-proposal re-verify step — do not delegate that specific check to agent-father.
**Note:** decision journal (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-2.md`) hit its byte cap (37871/36000) on arrival — rolled to `-3.md` per the skill's own rule; this spawn holds no `mcp__gateway__call_tool` (tools: Read/Edit/Write/Glob/Grep/Bash only) so the mandatory CAP-REACHED Telegram was structurally unavailable — filed as a `bug-escalation` signal instead (`docs/signals/architect-2026-08-06T070333Z-bug-escalation.json`).

## 2026-08-05T19:13Z — FIX-AGENT-NOTEBOOK-UUID-PROVENANCE part-b (zone=multi, P1, supervised+plan_only, SLS dispatch)

**Task:** Part (a) [agent-father, 07-29] fixed the AC-1 prose rule; this row's remaining half is a mechanical commit-time guard against a session-UUID-shaped heading, shaped to avoid the git-short-SHA false-positive the row's own notes flagged.
**Design/Verify:** Corpus replay (`scripts/audits/verify-notebook-uuid-provenance-gate.sh`, new) found 79 historical bare-hex-token hits, ALL in `system-auditor.md` pre-07-29 (holding clean since) — the cited `ad265f86` incident was the tail of a standing daily failure, not a one-off. Second, distinct leak shape found live in `tran-ngoc-bau.md` (full UUID as heading-line prose, not the AC-1 token) — a currently-uncommitted recurrence of the same pattern sits in that agent's working tree now; flagged, not fixed (different agent's own notebook, out of scope).
**Output:** Shipped directly (small enough; used judgment per spawn latitude). `scripts/git-hooks/pre-commit` `_check_notebook_uuid_provenance` (+pure `_notebook_uuid_line_verdict`), diff-scoped, 2 rules: full-UUID-on-heading-line (unambiguous, opt-in reject) / bare-6-8-hex-first-token (ambiguous, WARN-forever, `c<NNN>` counter excluded FIRST since `c` is itself hex). 10/10 new tests (`pre-commit-notebook-uuid-provenance.test.sh`), sibling suites still 13/13+6/6 green. Brief: `docs/architecture-briefs/2026-08-05-fix-agent-notebook-uuid-provenance-guard.md`. `notebook-write/SKILL.md` AC-1 cross-referenced. Board: `in_progress[]→review[]`, `next_agent=qa`, `.head`→idle, via `orch-apply.sh`.
**Next:** qa to verify guard behavior + brief; CI companion deliberately deferred (brief §5) — PO to schedule only if hook-only coverage proves insufficient.
