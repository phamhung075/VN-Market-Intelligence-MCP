# Architect — Notebook

**Last updated:** 2026-08-07 22:07 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-07T22:00Z — FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER (plan_only, supervised, dev-team Supervised-Lane Sweep dispatch)

**Task:** Answer po_residual_measurement_20260728's two sub-questions on this row's own 07-21 fix (Supervised-Lane Sweep, SLS): (1) should SLS's `AND` gate become an `OR`, or should the two flags collapse? (2) should Ready-Lane Consumer also read `backlog[]`, or should mint-time discipline be enforced?
**Sub-Q2 finding:** already substantially resolved by two things PO's 07-28 snapshot predates — DRS (`FIX-BOUNDED1-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-LANE`, shipped 07-30) and the PO manual-dispatch-sweep escape hatch (`FIX-PO-NO-PRODUCER-FOR-MANUAL-DISPATCH-ESCAPE-HATCH`, shipped 07-31). Off-allowlist residual has its own actively-owned tracking row (`FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION`, occurrence_count:2). Nothing shipped for this sub-question — re-solving it would have duplicated live, already-owned work.
**Sub-Q1 finding + fix:** verified PO's premise true by reading the actual gate code (`is_bounded1_eligible`) — BOUNDED-1 already excludes a row on EITHER flag alone, so SLS's `AND` buys no extra safety BOUNDED-1 didn't already provide. Rejected both proposed fixes: AND→OR would silently reopen the already-ratified DRS decision that a `supervised` dev-role row needs a human gate, not unattended auto-dispatch (SLS's spawn is the same zero-human-gate mechanism DRS's ratification explicitly excluded that class from); flag-collapse would lose real, independently-consumed signal (e.g. `FIX-SPRINT-TASK-HEARTBEAT-LOCK`: `supervised:true`+`plan_only:false`+`next_agent:developer` — a real distinction, not incidental). Shipped a 3rd option instead: new predicate `is_backlog_xor_gap` (`scripts/lib/po-manual-dispatch-eligibility.jq`), folded as a 3rd candidate class (`BACKLOG-XOR-GAP`) into the EXISTING `docs/agents/po/flow/manual-dispatch-sweep.md` mechanism — same 1-per-tick + 4h-bounded-re-admission safety envelope, zero widening of BOUNDED-1/SLS/DRS's own predicates. Live count 39 rows (`backlog[]` XOR-flag rows with dev-role-or-absent `next_agent`, disjoint by construction from DRS/DRS-STRANDED-OFF-ALLOWLIST territory). Corrected 4 stale Lane × Gate Coverage Matrix rows in `docs/agents/dev-team/flow/main.md` (2 falsely said "out of scope"; 2 `ready[]` rows never updated after `READY-XOR-SUP-OR-PLANONLY` shipped 07-31 — caught in the same pass, unrelated staleness). `docs/policies/dev-standards.md` CANONICAL mirror kept in lockstep. 1-line cross-ref added to `docs/agents/po/flow/zone-routing.md` Step A2.
**Verification:** `scripts/audits/po-manual-dispatch-sweep-verify.sh` extended with 9 new fixture positive/negative controls (dev-role hit, absent-`next_agent` hit, non-dev-allowlisted miss, non-dev-off-allowlist miss, epic-wrapper miss, deps-unsatisfied miss, both-flags-true miss, no-flags miss) + 1 end-to-end Step-1-selection replay — all green. `scripts/audits/bounded1-supervised-lane-report.sh` extended with a new BACKLOG-XOR-GAP section (informational, non-gating), live count matches manual census exactly (39), PRIMARY/SECONDARY/DRS/READY-XOR sections byte-unaffected. Full end-to-end dry run against a scratch copy of the live board (`cp` + `ORCH_APPLY_LIVE_FILE_OVERRIDE`, never the live file): replayed Step 1 selection for a real live candidate (`TASK-COWORK-MUTEX-001`), applied Step 2's additive stamp write through `orch-apply.sh` unmodified — Zod+conservation PASS, lane row-counts identical before/after (additive-only, no lane-move confirmed), live file hash unchanged before+after. The board-row closeout write (this row's own status→REVIEW + lane move + `.head` idle-reset) was ALSO dry-run verified against a separate scratch copy first (caught + fixed a schema violation — `head.next_action` must be string not null — and switched `.head` from a wholesale replace to a merge, preserving any future extra keys) before applying to the live file.
**New finding, flagged not fixed:** `bounded1-supervised-lane-report.sh` PRIMARY gate (this row's own original AC — every `supervised && plan_only` row must resolve a dispatch lane) currently FAILS live: 4 backlog rows minted 2026-07-29 with BOTH `owner` and `next_agent` null (`FIX-ORCHSTATE-SIGNALQUEUE-UNCOMMITTED-ROWS-LOST-TO-PEER-FULLDOC-WRITE`, `FIX-PUSH-DELIVERY-ERROR-RATE-ALERT`, `FIX-RAG-COMPACTION-DISK-AMPLIFICATION`, `FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE`). Orthogonal to both sub-questions (mint-time hygiene, not gate design) — not fixed here since guessing an owner for 4 unowned rows is outside plan-only authority; recommended to PO in the closeout note.
**Output:** `docs/architecture-briefs/2026-08-07-fix-bounded1-supervised-lane-no-sweeper-mechanism-and-answer.md`. Board: row moved `in_progress[]`→`review[]`, `next_agent=po`, `architect_review_note_20260807` field added (full findings). `.head` idle-reset (architect has Bash write access — did the full closeout itself). Did NOT call `task_release` on the outer `task:FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` sprint-task lock (dev-team holds it, BGFAN-1 discipline — this session also has no MCP tool grant to call it either way).
**Next:** PO reviews the fold-into-existing-mechanism decision + the 4-row mint-hygiene finding; census (BACKLOG-XOR-GAP=39) expected to trend toward 0 via `manual-dispatch-sweep.md`'s existing bounded per-tick drain, not hand-triage.

## 2026-08-07T22:07Z — FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET (plan_only, supervised, dev-team Supervised-Lane Sweep dispatch)

**Task:** Technical blueprint for BA's already-made product decision (news-chain fallback hints move OUT of `financial_reports` into a new non-authoritative `bctc_news_fallback_hints` table) — DDL, `newsChainFallback.ts` write-target rewrite, 7-test rewrite plan (6 named + Finding F-1's 7th), 2 new serving-plane regression tests, EC-5 live-DB-count probe.
**Findings:** Brownfield-reverified every BA-named file byte-exact this cycle — zero material line-number drift on any of the 7 cited tests or the 9 cited source files. EC-5 live probe (mandatory, via read-write `bun:sqlite` handle inside the running container — readonly handles risk WAL blind-spots per `feedback_integrity_helper_readonly_wal_blinded`): `SELECT COUNT(*) FROM financial_reports WHERE extraction_method='news_inference'` = **0** in production `market.db` today, reconfirming BA's historical-note claim fresh at design time — no data-migration branch needed. Design decision: new table DDL goes into `schema-financial-reports.ts`'s `initFinancialReportsTables()` (10+ live precedent tables added there since Sprint 209), NOT `bctc-schema.ts`'s frozen `SQLITE_DDL` string (zero new tables landed there in the same window, and it's covered by an equivalence test — higher risk to touch). Kept `tryNewsChainFallback()`'s in-memory return contract (the zero-valued `FinancialReport`-shaped object) byte-identical — BA's own FR-3 says no functional change needed on the `resolvePdfText`/`fetchParseAndStoreBctc` caller side, and changing the shape would ripple into 3+ caller files for zero benefit since nothing downstream persists/joins against it once the DB write moves off `financial_reports`. Removed the existing-row id-reuse dance as dead code (FR-1 makes the original PEK-FK/ID-ORPHAN motivation structurally moot for the new write path — nothing else FKs to the new table's id). Found one genuine new implementation gotcha (not in BA's spec): the AC-2 test's extension to a serving-plane assertion (§6 task 7) will silently pass for the WRONG reason (PUB-1 refusal, not a real arm-b1 proof) unless the seed row also sets `refine_status='PARTIAL'` and inserts a `bctc_table_rows` row — flagged explicitly in the blueprint with the exact fix.
**Output:** `docs/architecture-briefs/2026-08-07-fix-bctc-newschain-fallback-zeros-write-target-blueprint.md` — full DDL, rewrite code sketches, 7-test rewrite table, 2 new test sketches (reusing the existing `callTool`/`_registeredTools` harness from `fix-bctc-identity-serve-guard.test.ts`), grep-verify/RED-before/full-suite process steps, files-to-modify list, risk flags, BUILD-STANDARD=not-applicable. FR-7 (admin-inspector companion) flagged non-blocking for PO, explicitly not folded in. Board: row moved `in_progress[]`→`review[]`, `next_agent=dev-mcp-server` (sized for a single implementation pass — comparable to the sibling arm-(b2) fix `dev-mcp-server` already shipped in one pass; BA's own §6 header addresses "architect → developer" directly, not via PM decomposition).
**Next:** dev-mcp-server implements per the blueprint §3-§10; RED-before mandatory before GREEN; full suite before DONE. Outer sprint-task lock `task:FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET` left untouched (dev-team holds it, BGFAN-1 discipline).
