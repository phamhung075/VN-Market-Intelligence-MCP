# Architect — Notebook

**Last updated:** 2026-08-09 00:45 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-08T23:37Z — FIX-ORCHSTATE-HOTFILE-BLOAT-INLINE-PROSE-NOT-TERMINAL-DRIFT (P1, cross-service/, `supervised:true plan_only:true` direct PO-mint FIX row via Supervised-Lane Sweep — design only, no code)

**Task:** orch-state.json is 3.73MB (measured today: 3.59MB); PO's mint confirmed the driver is multi-KB inline prose on live `backlog[]`/`ready[]`/`review[]` rows, NOT terminal-status drift (zero terminal rows in those 3 lanes — the existing `TE-T15`/`FIX-BACKLOG-TERMINAL-ROW-DRIFT` rows would evict nothing). Row asked me to pick: (a) per-row write-gate byte ceiling, (b) one-time prose→`detail_ref` migration, or (c) both.
**Findings:** Re-measured live (623 rows across the 3 lanes, not PO's 517 — normal churn). 3 brownfield facts beyond the mint note: (F-1) the "intended remedy," `scripts/orch-backlog-stub.sh` (HSC-4), only ever fires once, on NEW `backlog[]` item creation — it has no `ready[]`/`review[]` equivalent and never re-fires on lane promotion or review-lifecycle edits, so 95% of `ready[]` rows and 44% of `review[]` rows never got a `detail_ref` at all (minted directly past the one place the tool is called). (F-2) Even where `detail_ref` IS present (105/186 `review[]` rows), fresh prose keeps re-accreting under NEWLY-INVENTED field names (`review_note`, `architect_review_note_<date>`, timestamped `po_*` fields...) because `TaskSchema` is still `.passthrough()` — confirmed this is a known, already-approved-but-blocked future promotion (`SSOT-W1-SERVER-ENFORCE`'s `.strict()` flip, gated on zero live unknown-key warnings, not yet true). (F-3, the one that changes the fix shape) `orch-backlog-stub.sh`'s cold-merge is DATA-DESTRUCTIVE on re-run: reproduced in an isolated scratch fixture (not the live file) — a row's hot-side prose added since its last stub is silently discarded on the next stub run (shallow `$new_items + .items` merge, existing-cold-wins-wholesale). This makes the "just re-run the existing tool on review[] too" reading of (b) actively dangerous as-is.
**Output:** Chose (c), both, sequenced behind F-3's fix as a hard prerequisite: (1) fix the cold-merge to a per-field deep merge (`.items * $new_items`, jq recursive merge — verified in isolation to preserve cold-only prose AND pick up hot-side new fields); (2) generalize `orch-backlog-stub.sh` additively (`LANES` config var, default unchanged=`backlog`, zero call-site churn for the ~10 existing callers) and run it once live under commit-mutex across `backlog[]+ready[]+review[]` — executes (b); (3) new sibling script `scripts/orch-row-prose-ceiling-check.mjs` (mirrors `orch-conservation-check.mjs`'s live-vs-candidate CLI contract, deliberately NOT extending `orch-stamp-updated-at.mjs` which is mid-extension for the unrelated in-flight `FIX-ORCHAPPLY-SELECTOR-MISS-SILENT-NOOP` fix), wired as `orch-apply.sh` Stage 2.5 — growth-only: a row already over the data-driven 12KB default ceiling (≈p90 across all 3 lanes measured today) that isn't growing is a non-blocking WARN, only NET NEW inline growth past ceiling hard-rejects, pointing the caller at `detail_ref` — executes (a) without the whole-file brick-risk class PO's own D-4 ruling already flagged for the sibling `FIX-ORCHAPPLY-NO-HOTFILE-SIZE-TELEMETRY-WARN` task. Full brief: `docs/architecture-briefs/2026-08-09-fix-orchstate-hotfile-inline-prose-ceiling.md`. Board: `next_agent=developer`, direct dispatch (no BA/PM relay), matches this session's own precedent (`FIX-ORCHAPPLY-SELECTOR-MISS-SILENT-NOOP`, `FIX-DONELANE-...`) for this exact row class.
**Next:** developer implements steps 1-3 above per the brief's §5 task table (step 3, the live migration run, inherits `supervised:true` — hot-file surgery, not autonomous-pickup). This row explicitly unblocks `FIX-ORCHAPPLY-NO-HOTFILE-SIZE-TELEMETRY-WARN`'s deferred hard-ceiling half (step 2 above gets the file under any sane whole-file ceiling; step 3 stops it re-inflating past it). **Two INV-GATEWAY-1-shaped tool-grant gaps hit this cycle, both flagged to router, not actioned by me:** (1) cannot call `task_release`/`task_claim` MCP tools myself (no gateway grant — this agent-type's standing grant is Read/Edit/Write/Glob/Grep/Bash only; per INV-GATEWAY-1 these are dispatcher-exclusive by design anyway, so this is not a deviation, just a reporting requirement); (2) decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-4.md` hit CAP-REACHED this cycle (150L/37334B > 36000B byte cap) — `CAP-REACHED` marker appended, but the skill's mandated `send_telegram(channel="bug", ...)` alert could not be sent (no MCP tool grant); router should either send it or note the journal needs archiving.

## 2026-08-09T00:45Z — FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION (P1, cross-service/, direct PO-mint FIX row via Design-Router Sweep — plan_only, no BA/PM relay)

**Task:** 43 live backlog rows (re-measured this cycle via the row's own named instrument, `bounded1-supervised-lane-report.sh` → `is_drs_stranded_off_allowlist`; mint note's 44/48 counts are normal churn, not a discrepancy) resolve a `next_agent` no automated lane picks up: 34 (79%) → `agent-father`, 9 → `ops`-class (`ops`/`ops-mainserver-fetch`/`ops-vps-fetch`)/`code-janitor`. PO's mint mandated answering, from agent-father's OWN flow docs, whether its standalone cron already drains its own board queue before designing anything.
**Findings:** Exhaustive grep across the entire `docs/agents/agent-father/` directory: zero `task_board`/`next_agent` references outside 2 unrelated lines — the daily `keep.md` cron is a closed loop over agent-definition-compliance data (orphan/roster/Top-5-checks), structurally blind to `.task_board`. Deeper finding beyond PO's own question: `main.md`'s dispatch table needs STRUCTURED `agent_name`+`change_description` inputs (`edit.md`/`edit-prepare.md` Step 1 does `Glob: .claude/agents/<agent_name>.md`) — unlike every OTHER DRS-allowlisted agent (architect/ba/pm/po/agents-architect), which all accept a generic "read this task, do the work" input. This means the DRS/PO-sweep's existing generic spawn pattern (`Agent(agent, task_id)` + "run main.md, board row is X") would silently no-op into `keep.md`'s own "Default when unclear" branch for agent-father specifically — a SECOND, independent reason (beyond the already-ratified blast-radius exclusion, `devteam-eligibility.jq:512-514`) that simply widening the DRS allowlist is wrong, not just riskier. `ops`/`ops-mainserver-fetch`/`ops-vps-fetch` do NOT share this defect — their own `main.md` flows already accept generic task-shaped input; their DRS exclusion was purely a live-infra-mutation safety concern, already satisfied by the existing (but 1-row/tick-capped) `po/flow/manual-dispatch-sweep.md`. `init.md`'s `commit_zone` already names a "signal-queue DONE-mark" carve-out into `orch-state.json` that no flow file currently exercises (git-log-verified: past "(agent-father)"-attributed board moves were done by the dispatching router post-RETURN, not by agent-father itself) — a reusable, narrow governance seam.
**Output:** Two disjoint mechanisms (AC1's "new sweep lane" and "ratified PO BATCH" read as the correct fix for two genuinely different problem classes, not alternatives): (A) new `docs/agents/agent-father/flow/board-drain.md`, wired as an unconditional new step in `keep.md`'s existing daily cron, with a `classify_board_drain_row` SAFE_AUTO/NEEDS_RATIFY split (mirrors `sweep-fixes.md`'s own pre-existing "auto-fix mechanical only, escalate anything requiring scope understanding" posture — live-verified against 5 real stranded rows, not asserted from the predicate alone) — SAFE_AUTO runs `edit.md`'s existing prepare+apply then lane-moves to `review[]` for human sign-off (never auto-closes DONE); NEEDS_RATIFY runs `edit-prepare.md` ONLY (plan-only/spec-first, AC1's named envelope) and mints a `docs/improvement-proposals/` DRAFT reusing the ALREADY-SHIPPED `improvement_approved_md` consume/status-lifecycle verbatim — zero new approval machinery. New `effective_files` predicate added to the shared `devteam-eligibility.jq` (missing from the library, confirmed by grep — 2/5 spot-checked rows carry empty board-level `files[]`). Narrow `commit_zone` carve-out (auditable, restricted to rows board-drain itself processed that cycle). (B) small widen of `manual-dispatch-sweep.md` (1→4 rows/tick: 1 generic unchanged + up to 3 for the ops-class + a new 4th `AGENT-FATHER-PROPOSAL-PENDING` candidate class feeding PO's decision back into (A)'s own proposal lifecycle) for the 9-row remainder — reuses the already-proven-live mechanism, no new PO infrastructure. Regression: extended the ALREADY-EXISTING `bounded1-supervised-lane-report.sh` DRS-STRANDED-OFF-ALLOWLIST section with a WARN>15/FAIL>25 ratified ceiling (today's baseline 43) rather than a new duplicate-counting script; plus a new SYNTHETIC-only `agent-father-board-drain-classify-verify.sh` for the classifier itself. Full brief: `docs/architecture-briefs/2026-08-09-agent-father-board-drain-and-ops-batch-widen.md`.
**Next:** pm decomposes into tiered developer tasks per the brief's §7 table (shared-lib predicate → sub-flow+keep.md wiring → init.md governance diff → PO-sweep widen + audit-script ceiling, several disjoint-file tiers can run parallel). AC3 is explicitly a POST-SHIP live-count assertion (re-run the audit script ~4-5 days after ship, count must land ≤15 or carry an explicit PO-ratified higher residual) — flagged in the brief's task table so PM/QA don't close this row on code-complete alone. Decision journal: base file + `-2`/`-3`/`-4` all CAP-REACHED (last at 2026-08-08T23:37Z) — this cycle's entry opened a new `-5.md` continuation (header + entry written), same roll convention the skill itself specifies.
