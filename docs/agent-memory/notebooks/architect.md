# Architect — Notebook

**Last updated:** 2026-08-05 19:13 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-05T19:13Z — FIX-AGENT-NOTEBOOK-UUID-PROVENANCE part-b (zone=multi, P1, supervised+plan_only, SLS dispatch)

**Task:** Part (a) [agent-father, 07-29] fixed the AC-1 prose rule; this row's remaining half is a mechanical commit-time guard against a session-UUID-shaped heading, shaped to avoid the git-short-SHA false-positive the row's own notes flagged.
**Design/Verify:** Corpus replay (`scripts/audits/verify-notebook-uuid-provenance-gate.sh`, new) found 79 historical bare-hex-token hits, ALL in `system-auditor.md` pre-07-29 (holding clean since) — the cited `ad265f86` incident was the tail of a standing daily failure, not a one-off. Second, distinct leak shape found live in `tran-ngoc-bau.md` (full UUID as heading-line prose, not the AC-1 token) — a currently-uncommitted recurrence of the same pattern sits in that agent's working tree now; flagged, not fixed (different agent's own notebook, out of scope).
**Output:** Shipped directly (small enough; used judgment per spawn latitude). `scripts/git-hooks/pre-commit` `_check_notebook_uuid_provenance` (+pure `_notebook_uuid_line_verdict`), diff-scoped, 2 rules: full-UUID-on-heading-line (unambiguous, opt-in reject) / bare-6-8-hex-first-token (ambiguous, WARN-forever, `c<NNN>` counter excluded FIRST since `c` is itself hex). 10/10 new tests (`pre-commit-notebook-uuid-provenance.test.sh`), sibling suites still 13/13+6/6 green. Brief: `docs/architecture-briefs/2026-08-05-fix-agent-notebook-uuid-provenance-guard.md`. `notebook-write/SKILL.md` AC-1 cross-referenced. Board: `in_progress[]→review[]`, `next_agent=qa`, `.head`→idle, via `orch-apply.sh`.
**Next:** qa to verify guard behavior + brief; CI companion deliberately deferred (brief §5) — PO to schedule only if hook-only coverage proves insufficient.

## 2026-08-05T17:58Z — FACTORY-STOCK-split-sandbox (zone=stock-price, P1/M, dev-team Review-Lane SECONDARY-Drain sign-off)

**Task:** 27-day-stale `review[]` row (dev-stock-price reviewed 2026-07-09T20:55:11Z, no automated sweep existed for non-qa `next_agent` rows until today's SECONDARY-Drain went live) awaiting architect sign-off on the 743L `apps/stock-price/cmd/sandbox/main.go` split by executor seam.
**Design/Verify:** Read commit `0d93b3c95`'s own message as the review-of-record: 8 files (main/discover/helpers/dispatch/exec_primitive_normalizer/exec_primitive_selector/exec_primitive_staleness/exec_module_resolution), pure file-move, 2 files >120L (123L/150L) explicitly justified as single cohesive executors. `git log 0d93b3c95..HEAD -- apps/stock-price/cmd/sandbox/` = EMPTY — zero drift since review, so the month of staleness is a throughput gap, not an accuracy gap. Independently re-ran the AC live rather than chain-trusting the note: `go build`/`go vet ./cmd/sandbox/...` clean, `golangci-lint run` 0 issues, `CGO_ENABLED=0 go build` clean, built binary against all 11 real `docs/scenarios/stock-price/{primitives,module}/*.json` fixtures -> 11/11 PASS — matches the original claim exactly.
**Finding:** `apps/stock-price/Dockerfile` builds ONLY `./cmd/server/` — `cmd/sandbox/` is never baked into the deployed image, so `backlog-detail.json`'s `rebuild_required:true` (a generic 2026-06-15 audit-batch default) does not actually apply to this row; no Docker Microservice Code-Change Close Gate needed.
**Output:** Closed `review[]->done_verified[]`, `status:DONE_VERIFIED`, via `scripts/devteam-close-task-done-verified.jq` -> `orch-apply.sh`. `.head` untouched (active_task_id was a different row). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-2.md` STEP architect-S34.
**Next:** None — row is terminal. Gateway MCP tool access absent this session (Read/Edit/Write/Bash only); `task_release("task:FACTORY-STOCK-split-sandbox")` deferred to the spawning dev-team session per its own documented fallback.

## 2026-08-05T17:52Z — FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER (zone=cross-service/, P1/M, direct PO board-mint, dev-team Execution spawn)

**Task:** PO triage found main CI-red for 30 consecutive runs since 08-01 — `.git/hooks/pre-push` runs only tsc + rebuild-raw-verify, both gated behind `CODE_TOUCHING_REGEX` which excludes `docs/`, so `size-lint`/`task-claim-owner-session-lint`/`tool-registry-parity` (all CI-only) never run locally on a doc-only push and 2 of the 3 real breakages landed on exactly those excluded paths. AC1 (the gate on the whole row): measure before widening — don't assume the 3 candidate checks are cheap.
**Design:** Measured (not assumed) `size-lint-justification.sh --check` ~13.3-14.0s + `task-claim-owner-session-lint.sh --check` ~5.5-6.1s + `bun test tool-registry-parity` ~0.1-0.3s, 3 runs each = combined ~19.5-20s local wall-clock — ~4.5x under the 90s `commit-mutex:main` TTL (the mutex's critical section wraps `git push`, so pre-push wall-clock is inside that clock). Confirms the row's AC2 premise rather than assuming it. Live bonus: running the 3 checks against current HEAD reproduced the exact 3 CI-red conditions already on GitHub Actions — proof the mechanism would have caught all 3 breakages locally. Design: add the 3 checks UNCONDITIONALLY to `scripts/git-hooks/pre-push` (not path-gated), placed BEFORE the `PRE_PUSH_SKIP_TSC` early-exit (today mislabeled — it bypasses the whole hook, not just tsc; reordering restores that var to its documented scope without expanding the escape hatch). tsc + `rebuild-raw-verify-check.sh` stay on the existing `CODE_TOUCHING_REGEX` gate, untouched — that 94s-vs-90s constraint is orthogonal to this fix.
**Finding:** `.github/workflows/ci.yml` needs NO change — the row's own initial files-guess was wrong; size-lint/task-claim-lint are already separate CI jobs and the parity test already runs inside the existing full-suite `test` job. The pre-existing tsc-alone-94s-over-90s-TTL condition on CODE-touching pushes is unchanged/out of scope — flagged for PO, not fixed here (adding ~20s more to an already-over-budget path is a difference of degree on a separately-tracked pre-existing condition, not a new-in-kind problem this row creates).
**Output:** `docs/architecture-briefs/2026-08-05-fix-ci-gates-invisible-to-prepush-docs-path-filter.md`. `architect_review_note`/`architect_brief` written via `orch-apply.sh`, `backlog[]→ready[]`, `next_agent: developer` (PM decomposition skipped — single file + 1 new test file + 1 dev-standards.md CANONICAL entry). Fence explicitly restated in both the brief and the note: NOT closeable by the 3 symptom rows (size-lint/parity/task-claim) going green.
**Next:** developer implements per the brief's §2 spec + adds `scripts/git-hooks/pre-push.test.sh` (no existing test file for the hook itself) + the CANONICAL dev-standards.md entry. Blast-radius risk flagged: this hook gates every push fleet-wide — must be test-green + shellcheck-clean + live-HEAD-dry-run-reproduces-the-3-known-failures before QA calls it DONE, given the historical TS2367 sole-tsc-red precedent of a broken blocking hook stranding the whole fleet.
