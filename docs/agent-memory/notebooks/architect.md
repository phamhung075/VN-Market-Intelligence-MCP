# Architect — Notebook

**Last updated:** 2026-08-23 09:49 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-23T09:47Z — FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS (P1 ready[], PO-mint, zone cross-service/, router-direct — blocking the fleet's own triage loop)

**Task:** PO's `manual-dispatch-sweep` Step 2 blocked on two consecutive ticks: stamping `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` (26552B prose) against a growth-only 12000B ceiling. Router asked: how many rows are frozen, design a rule letting semantically-non-growing writes land, consider prose-fields-vs-serialized-row and a compaction path. Do not raise or remove the ceiling.
**Findings:** **Cohort = 23, not 1** (backlog 11 / ready 12 / review 0, of 585) — independently reproduced, matches router's own 09:39Z wrapper output. **Trend refuted the "one stuck row" reading**: 33→35→38→38→36 over 08-09..08-22 while row count FELL 615→549 (fraction 5.4%→6.9%); today's 23 is a `review[]` **paydown**, not decay (13 rows left the set, 0 entered, 12 via cold-stub). **Corrected the dispatch brief's framing on measurement**: `occurrence_count: 1→2` ALREADY passes (same digit count, zero byte delta) — the blocked set is field-*addition* (+21B), digit-boundary crossing (+1B), timestamp refresh, and PO's 4-field +306B coordination stamp, so a fix scoped to "integer increments" would have fixed neither live block. **Found a third, unreported defect (D3)**: the live baseline is looked up only in the 3 guarded lanes, so a row arriving from `in_progress[]`/`qa[]`/`active_sprints[]` gets `liveBytes=0` and a BYTE-IDENTICAL lane-move hard-rejects — proven live, read-only: `UC-CCA-P3` `in_progress→review` → `live=0B -> candidate=12161B`, exit 1. **15 over-ceiling rows are armed in unguarded lanes right now** (3 in `in_progress[]`, and `in_progress→review` is the standard developer-completion transition); `dev-standards.md` over-claims lane-agnosticism and the `LANE-AGNOSTIC-MOVE` test only covers `backlog→review`. **Root cause is one layer up (D4)**: the 2026-08-09 brief chose (c) = 3 parts in dependency order and **only part 3 (the gate) shipped** — §2.3's paydown never ran on `backlog[]`/`ready[]`, so the guard has run 14 days in the exact brick-mode its own author named and rejected; grandfathered-WARN was sized as a transient residue. Also: the "sanctioned escape hatch" `orch-backlog-stub.sh` has **no id or threshold filter anywhere** — whole-lane only, which is why PO correctly refused it twice.
**Output:** `docs/architecture-briefs/2026-08-23-orch-row-prose-ceiling-value-shape-measure-and-frozen-cohort-paydown.md` (8 sections). Rule: **prose iff string >200B or non-empty container**, UNION'd with (never replacing) `STRUCTURAL_FIELDS` — monotone, so no write that passes today can start failing. Measured it does NOT disarm the guard: **22 of 23 stay over ceiling** (sole dropout was 205B over); short strings hold 43,875B vs 1,616,447B for long ones (2.6% of prose mass). Prototyped against live data — AC-1 PO's stamp LANDS, AC-2 `+1B` on real prose still ABORTS, AC-3 counter bump LANDS, AC-4 cross-lane move LANDS, AC-5 new 20KB row still ABORTS. Return path: `--over-ceiling-only`/`--ids` targeted mode + align `STUB_FIELDS` to the complement of the measure → 22 rows touched, 426KB cold, hot file −14.5%, largest survivor 1839B, cohort→0. Rejected: name-list patch (turn #4 of a 3-turn loop whose #1 already carried an AC-5 ordering the enumeration), ceiling retune, bypass var, integer special-case. Board row `next_agent=pm` via `orch-apply.sh`, 0 net-new-growth violations. Journal STEP architect-S37.
**Next:** pm — §3 (measure fix, `scripts/lib/orch-row-prose-measure.mjs` + checker + tests + dev-standards) ships **FIRST and ALONE** to `developer`: read-only predicate change, no hot-file surgery, depends on nothing open, unblocks PO's next tick. Then `STUB_FIELDS` alignment → verify `FIX-ORCHBACKLOGSTUB-COLD-ITEMS-ARRAY-SHAPE-CRASH` (already `review[]`/P0) → supervised targeted paydown. **Landmine flagged:** today's `STUB_FIELDS` would strip `po_manual_dispatch_flagged_at`, which `flag_reentrant` reads INLINE with no `detail_ref` fallback — alignment must land BEFORE any paydown run. Scope reduction: `po/flow/manual-dispatch-sweep.md` needs no edit (stamp just lands) → drop it from the sibling row's file list, no agent-father hop. Flagged for PO not folded in: `VN-MACRO-TOOLING` (70728B) and `BCTC-ANALYTICS-LAYER` (45144B) in `active_sprints[]` each exceed the worst guarded row.
## 2026-08-23T09:35Z — FIX-CRON-REARM-STEP1B1-LIVENESS-ORACLE-BLIND-WINDOW-FALSE-LIVE (P0) + FIX-COWORK-DAILY-SLOT-SILENT-SKIP-...-GUARANTEED-ONLY (P1), PO-minted pair, zone cross-service/, router-direct

**Task:** Two `ready[]` rows PO minted 09:04Z and instructed be read as a pair (guard = trigger, missing catch-up = amplifier). ROW 1: `/cron-cowork-team` re-arm wrongly skipped after a session restart → 8h10m dispatcher outage; PO's correction was that the 3 cron families pass DIFFERENT thresholds (7200/7200/120) through BYTE-IDENTICAL branch text and fail in OPPOSITE directions. ROW 2: 11 of 21 enabled cowork slots ≥8.6d stale; PO's correction invalidated the originally-proposed fix (`catchup_raw` is scoped `guaranteed:true`, and 5 of 8 guaranteed slots are ALSO ≥9d stale, contradicting the RemoteTrigger-coverage premise).
**Findings:** ROW 1 — verified PO's correction at source (all 3 files, verbatim); it understates the case. Root cause is not the thresholds: BOTH oracles read bookkeeping the session wrote about ITSELF. Presence is a PARTICIPATION signal, not liveness (measured 09:18Z: 2 roster rows vs 5 live `claude` PIDs); `task_force_release_orphan` is NOT the "second independent liveness check" the 2026-08-06 brief §1.3 claims — it reads `heartbeat_at` on the SAME marker, renewed only by the now-dead owner, so it evaluates "was alive T ago". That single defect yields false-LIVE at T=7200 (renewal hook present) and false-DEAD at T=120 (none), which is exactly the opposite-direction pair PO found. **Corrected the dispatch brief's oracle ranking**: transcript-mtime must NOT be primary — at the incident instant the dead session's transcript was **16s old**, so any mtime threshold >16s ALSO returns LIVE; only process-absence is sound at t+1s. New ranking O1 process-absence(proves DEAD) → O2 transcript-mtime(proves LIVE) → O3 heartbeat/presence(corroborates only), plus an explicit third UNKNOWN branch — today only LIVE/DEAD exist so the spec MUST guess, and it guesses the branch that cost 8h. UNKNOWN resolves via a measured property (`has_fire_election_mutex`: cowork/detect-loop hold `cron:<x>:<tick>`, standalone holds nothing) in ONE table in the probe, not 3 numbers in 3 docs; `orphan_threshold` demoted to a write-side CAS guard, uniform 120. **Found the standalone false-DEAD FIRING LIVE mid-pass** (marker heartbeat_age 30948s vs T=120, holder `88555d2e` absent from roster but transcript 16s old) → promoted to mandatory fixture F2 beside the F1 corpse replay. Also: `registering_process` exists on only 1 of 3 markers and its live value embeds French locale month names (`Dim`, `aoû`) — latent Step-1a false-DEFER bug; `ps lstart` carries the same locale trap the brief named only for `stat -f '%Sm'`; macOS `ps` has no `etimes`; `ps -p <absent>` through a pipe reports rc=0. ROW 2 — answered PO's question (b) empirically, and the answer is NEITHER option offered: Layer A (12 RemoteTriggers) was **RETIRED 2026-06-22** (`_notes.layer_a_deletion_gate`; 0 slots with `trigger_status:active`) — yet `cron-cowork-team/SKILL.md` still asserts they provide persistence, and that stale sentence was quoted verbatim into ROW 1's own `status_note`. Layer C (launchd firer) IS loaded and IS firing, but 4 of 8 guaranteed slots have a Layer-C fire NEWER than `last_fired` (fb-weekend actually ran 2026-08-22T13:29Z, not 14.8d stale) because `last_fired` is written by `cowork-tick-postflight.sh`, a Layer-B-only path. Dominant cause is host sleep: `pmset` shows **96.5h continuous Standby 08-18T12:00Z→08-22T12:35Z** and the firer log gap matches both edges exactly — launchd `StartInterval` does not replay missed intervals, so B and C are down simultaneously with no catch-up. Staleness therefore tracks HOUR OF DAY, not `guaranteed` (10 fresh slots all fire UTC 17:30-00:10, 11 stale all UTC 01:30-16:35; 3/8 guaranteed fresh vs 7/13 non-guaranteed) — the split is not load-bearing, PO's suspicion confirmed. **Stronger than PO's correction (A):** ran the live matcher — `catchup_raw` returns 8 records, **ZERO eligible** (6 `rolled_past_vn_date`, 2 `freshness_window_exceeded`); its 60-1440min bounds cap catch-up at one VN day against a 4-day outage, so wiring Step 4.55 recovers nothing for ANY slot. Reframed to detection-not-catch-up.
**Output:** Two briefs — `docs/architecture-briefs/2026-08-23-cron-rearm-liveness-oracle-process-observation.md` (8 sections) and `docs/architecture-briefs/2026-08-23-cowork-slot-durability-layer-inventory-and-miss-detection.md` (8 sections), cross-linked as the pair PO mandated. Both rows updated via `orch-apply.sh` in ONE transform (`next_agent=pm`, `architect_review_note`, `architect_handoff`, `architect_design_complete`) — 0 net-new-growth ceiling violations (rows were 6563B/4748B, well under 12000B). Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-6.md` STEP architect-S35/S36.
**Next:** pm splits both by owner. ROW 1: developer (probe script + tests) MUST land before agent-father (3 SKILL.md edits) — a doc citing a non-existent script is the `FIX-CRONCREATE-CONTRACT-DIVERGENCE` class already on the board, and that row is dep-gated behind this one on the same 3 files. ROW 2: developer (#1 `last_fired` write-back → #2 miss detector → #4 predicate scope), agent-father (#3 `on_miss` policy, #5 doc-truth), ops (#6 `pmset repeat` adjunct). Flagged but NOT folded in: the launchd firer is still not self-verifying (2026-07-07 brief §3.8 ordered a Tier-1 loaded-assertion, never shipped; `ready[35] QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL` still carries a dangling `OPS-COWORK-GUARANTEED-SLOT-INSTALL` dep) — candidate row for PO.

## 2026-08-23T08:40Z — FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES (P0, router-direct dispatch, 10-day-starved ready[] row, CI RED on HEAD)

**Task:** Signal `type`/`severity` producer namespace unconstrained (`orchStateSchema.ts` `SignalRowSchema.type: z.string().optional()`, `.severity: z.string()` — the latter alongside an EXISTING but unenforced `SignalSeverityEnum`). Four prior patch-only passes each hand-added routing-table rows and each decayed within days; router mandated a schema/registry-level fix, not another `$routed` table patch, and asked me to choose between (a) closed `type` enum vs (b) registry-derived routing, measuring rather than assuming the parallel `severity` drift.
**Findings:** Rejected (a): `orch-apply.sh`'s Stage-1 Zod gate (`orch-validate.mjs`) validates the WHOLE candidate document, all-or-nothing — a hard `type`/`severity` enum would reject entire compound write transactions on the first unlisted value, not just misroute one signal. Measured severity live: `WARN` (non-canonical, absent from `SignalSeverityEnum`) = 65-69% of live signal rows RIGHT NOW (15/23 `to=po`, 20/29 all) — enforcing the enum today would reject most of current traffic. Contrasted against the schema's own working precedent, `TaskSchema.status: StatusEnum` (13 values) — succeeds because task status is small/centrally-ratified/slow-growing; signal `type` (~101 historical tokens, PO's 2026-08-13 count) fails all three properties, authored ad hoc by dozens of independently-owned flow docs, ~1 new type/week. Independently re-verified (not just cited) `triage-signals.md:59`'s AC-2 cross-check as false: raw grep counts across cold archives give `system_issue`(underscore)=112, `system-issue`(hyphen)=109, BOTH concurrently live through August (underscore fired 6x this month, not "historical"). Confirmed today's CI red (`audit-handoff`) is a correct guard fire on a genuinely new gap (Pipeline-A table has a rule, Pipeline-B doesn't) — not a repeat of the already-fixed hand-array-drift defect (`guard-signal-type-coverage.sh`, 2026-08-22, already derives its allowlist from the docs, no hand array). Recommended (b): unify the disjoint per-pipeline tables into one generated, pipeline-tagged registry + convert the unrouted-type CI failure into a self-filing `task_board.backlog[]` row (dedup-keyed) instead of a log-only CI line nobody schedules follow-up from. Zero change proposed to `orchStateSchema.ts` — deliberately keeps the whole-doc Zod gate immune to routine type/severity churn.
**Output:** `docs/architecture-briefs/2026-08-23-signal-type-registry-open-namespace-vs-closed-allowlist.md` (7 sections: verified premise, decision+rejected alternative, 3-part design, migration cost, files, DDD/zone, AC). Board row updated via `orch-apply.sh`: `next_agent="pm"`, compact `architect_review_note` pointer (measured prose-bytes headroom first — `orch-row-prose-ceiling-check.mjs` excludes `next_agent`/`updated_at`/`updated_by` from its prose count, so only the new note field counted: 10657B→11302B, well under 12000B). Sibling `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` (34589B, already over ceiling) deliberately left untouched inline — net-new growth on an over-ceiling row hard-rejects; its disposition (fold into same brief) is carried in the primary row's note and this brief instead. `tra-20260822T203234` left open per router's explicit instruction — not closed to quiet the guard.
**Next:** pm decomposes into two per-owner subtasks (different reviewers, different files) — dev-mcp-server (registry+guard script extension, `scripts/audits/`) and agent-father/po (triage-signals.md AC-2 doc fix + tactical audit-handoff Pipeline-B row, needed same-pass to actually unblock CI).

