# Architect — Notebook

**Last updated:** 2026-08-26 10:20 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-26T08:30Z — UC-SDF-P2 (P1, dev-team review-lane secondary-drain dispatch, plan_only, zone=multi/apps/mcp-server)

**Task:** Cycle-snapshot promotion dead since 07-07 (tickHHMM-vs-FILE_TICK filename divergence) + WIDEN regime_status/volatility_level writer + market-hours staleness tripwire. BA already shipped the tripwire half (Step 6.0b, docs/handoffs/UC-SDF-P2-BA-step60-hardening-spec.md, complete); left to architect: the filename-contract fix + the row's own 4-unit sequencing guard (units 1 and 4 explicitly open; row flagged "THREE ROW CLAIMS CORRECTED, verify before implementing").
**Findings:** Re-verified BA's root cause at source (unchanged). Independently verified the row's OWN 4-unit guard: units 2 (cadence-policy alert-commander rows) and 3 (calendar_status wiring) are BOTH already shipped — `docs/architecture-briefs/2026-08-04-cadence-rationalization.md` item 1 (commit `8c2acb44c`) deliberately chose `_cron_fallback:true` for both alert-commander policies (they already self-gate; one is intentionally 24/7 regardless of calendar for crisis/regulatory coverage), so the row's own "15min/240min" wording for unit 2 is wrong and would be a regression if implemented literally. `vnTradingCalendar.ts:30` SESSION_STATUSES confirmed no "closed" member, matching BA's independent finding for unit 3. Live `cycle-snapshot-latest.json`'s carry regime is uppercase `"UNKNOWN"` (Go macro-indicators service literal, `apps/macro-indicators/pkg/application/usecases.go:128,230`) — a case-mismatch trap for the `==='unknown'` lowercase checks in `cowork-match-slots.js:223`/`cadence-policy.js:104`. Found `get_volatility_indicators` (`volatilityIndicatorTools.ts`) already shipped and unused by this pipeline — a zero-argument, market-wide `vol_regime` producer nobody wired into pressure-state.
**Output:** `docs/architecture-briefs/2026-08-26-uc-sdf-p2-filename-contract-fix-and-regime-volatility-writer.md`. Unit 1: `promoteCycleSnapshot` drops the exact-filename join for a newest-mtime glob over `cycle-snapshot-*.json` (zero change needed to `tick-snapshot.md` — removes the cross-process string-agreement surface entirely instead of patching it a 3rd time). Unit 4: `last_regime`/`last_volatility_level` move server-side inside `emitPressureStateTool.ts` (completes the EMIT-DARK-v2 pattern — these are the only 2 of 9 `PressureState` keys still caller-narrated) — regime read from the now-fixed cycle-snapshot (lowercased), volatility from `get_volatility_indicators` (lowercased), both defaulting to `"unknown"` on failure. Flagged 2 behavior-change risks explicitly rather than deciding silently: `cowork-match-slots.js`'s freshness-downgrade will stop firing on most off-hours ticks once regime is real; `cadence-policy.js`'s low-tier set may need widening to include `"normal"` volatility or the adaptive layer runs hot almost permanently. Narrowed the sequencing guard to units 1+4 only (2/3 done; 4 depends on 1, else it pins a stale-but-plausible regime forever instead of an honest "unknown"). Journal STEP architect-S51 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md`). Row lane-moved `review[]`→`ready[]`, `next_agent: developer`, `dispatch_lane` cleared, via `orch-apply.sh`; read back post-commit.
**Next:** developer implements brief §2+§3 (apps/mcp-server/src/interface/mcp/tools/system/emitPressureStateTool.ts + its test + scripts/agents-flow/cadence-policy.js's low-tier-set AC). The 2L telemetry.md:18-19 prose correction (drop the now-stale "read from cycle-snapshot" instruction) is agent-father's exclusive docs/agents/** zone — flag it in the same dispatch, do not block on it.

## 2026-08-26T09:51Z — FIX-JOURNALGUARD-ALERT-TRANSPORT-RUNS-ON-THE-PLANE-IT-MONITORS (P0, dev-team ready-lane consumer claim, zone=scripts/)

**Task:** market-db-journal-guard's BUG alert fires via `send_telegram`, which executes inside `mcp-server` — the exact container being down is what makes the probe fire, so the alert transport is dead at the precise moment every alert fires (7/7 lost across a 74-min outage 2026-08-25). Shape of fix deliberately not prescribed. AC-3 required an explicit in/out-of-scope call on a named second instance (cowork dispatcher's `preflight-error-fallback.md`, 4+ aborted ticks same morning).
**Findings:** Confirmed both `call_tool` and `scripts/agents-flow/mcp-call.sh` reach the identical in-container `coreSend()` (`telegram.ts`) — no transport swap alone escapes the dependency. The "obvious family" the row named (direct-to-Telegram HTTP from the host) is already live twice in this repo, uncombined: `cowork-guaranteed-slot-firer.sh`'s `_escalate_failure()` and `maybe-deploy-vps.sh`, both direct-curl to `api.telegram.org`, `.env`-sourced, zero MCP dependency — confirmed `.env` (host-readable, gitignored) actually carries `TELEGRAM_BOT_TOKEN` + the 3 channel ids. AC-3: the cowork-fallback chain's dependency is on state-mutating coordination primitives (task_claim/heartbeat/election/slot-claim across 5 flow docs), not a one-way notification — no curl-equivalent substitutes a distributed lock; not the same fix family, sized far past this row.
**Output:** `docs/architecture-briefs/2026-08-26-journalguard-alert-transport-host-direct-telegram-spool.md`. New shared `scripts/lib/telegram-direct-send.sh` (extends, does not triplicate, the existing pattern) + new `scripts/audits/journal-guard-alert-notify.sh` (flush-then-send NDJSON spool, 200-line cap, atomic rewrite) replacing the cron prompt's `call_tool` step in both `.claude/commands/crons/cron-market-db-journal-guard.md` and its verbatim-port registration doc. AC-3 dispositioned explicitly OUT of scope in the brief; minted `FIX-COWORK-PREFLIGHT-FALLBACK-NO-MCP-INDEPENDENT-PATH` into `task_board.backlog[]` (P1, next_agent=architect) so the finding is tracked, not left in prose only. Journal STEP architect-S52 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md`). Row lane-moved `in_progress[]`→`review[]`, `next_agent: qa`, via `orch-apply.sh`.
**Next:** developer implements the brief (shared lib + notify script + test + both cron-prompt call sites + the `dev-standards.md` CANONICAL pointer extension). Separate architect (any) picks up the newly minted cowork-preflight-fallback row when scheduled — needs its own design pass across `work-tick.md`/`leader-lock.md`/`slot-claim.md`/`tick-snapshot.md`/`telemetry.md`, not a quick follow-on to this one.

## 2026-08-26T10:20Z — FIX-DRS-CLAIM-HAS-NO-ALLOWLIST-GATE-OFF-ALLOWLIST-BLIND-DISPATCH (P0, router direct-execute dispatch — DRS's own claim gate is the defect, cannot self-route; zone=scripts/)

**Task:** `scripts/devteam-backlog-claim-design-router-sweep.jq` never applied the ratified DRS agent-identity allowlist (promote always has) — claim's only filter was `promoted_by` + a non-empty claim-time-resolved lane. Live-reproduced at the 10:07Z tick: promote correctly moved an on-allowlist P0, claim ignored it and took a DIFFERENT DRS-stamped P0 (`FIX-FLEETPUSH-DISARM-...`) resolving to `developer` — off-allowlist. Router caught it pre-spawn, rolled back, dispatched straight to architect (implement, not just design).
**Findings:** Grep-confirmed the claim script's candidate filter never calls `is_design_router_allowed`. Row's own scope note asked me to decide: thread `--argjson allowlist` into claim's CLI invocation, or centralize the default in the library. Empirically confirmed jq raises a compile error on any `$name` referenced without a matching CLI-bound flag — claim is invoked with NO `--argjson allowlist` at 9 existing call sites (main.md + 8 audit fixtures); adding the flag everywhere would relocate the "two call sites can drift" risk from the value to the CLI plumbing. Checked BOUNDED-1/SLS/RLC for the same promote/claim asymmetry: RLC is structurally immune (no promote/claim split — all gates evaluated live at claim). BOUNDED-1's claim never re-verifies promote's substantive eligibility gates (lower severity — no identity gate to bypass, still funnels through zone-detect). SLS's PRIMARY claim path never re-verifies `effective_supervised && effective_plan_only` — closer analogue to this bug (SLS's whole no-allowlist justification rests on that pair staying true).
**Output:** `docs/architecture-briefs/2026-08-26-drs-claim-allowlist-gate-and-promote-claim-asymmetry.md`. New `design_router_default_allowlist` def in `scripts/lib/devteam-eligibility.jq` (single SSOT — both promote's and claim's fallback point at it; claim calls it directly, zero new CLI flags, zero of the 9 existing call sites touched). Claim script's `$resolvable` now also filters through `is_design_router_allowed`. 2 new regression cases (`AC-DRS-ALLOWLIST-GATE`, `AC-DRS-ALLOWLIST-SKIP-TO-NEXT`) in `scripts/audits/devteam-dispatch-gate-satisfiability.sh`, verified fail-pre-fix/pass-post-fix against an isolated pre-fix copy of the claim script. Dry-run (scratch copy, never live) confirmed all 3 blast-radius rows still unclaimable and the on-allowlist P0 still correctly claimed. Minted `FIX-BOUNDED1-SLS-CLAIM-NO-PROMOTE-TIME-GATE-RECHECK` (P2, next_agent=architect) for the sibling-lane finding. Journal STEP architect-S53. Row lane-moved `in_progress[]`→`review[]`, `next_agent: qa`, via `orch-apply.sh`.
**Next:** qa verifies the fix (regression suite green, blast-radius rows confirmed unclaimable). Separate architect picks up `FIX-BOUNDED1-SLS-CLAIM-NO-PROMOTE-TIME-GATE-RECHECK` when scheduled.
