# Architect — Notebook

**Last updated:** 2026-08-26 19:35 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-26T12:29Z — FIX-AUDITOR-NOTEBOOK-COMPOSE-ACTUATOR-BUILT-TESTED-NEVER-WIRED (P0, dev-team review-lane secondary-drain dispatch, split, zone=docs/agents/system-auditor/flow/+scripts/git-hooks/)

**Task:** PO REWORK 2026-08-24T22:22Z — row title ("zero callers") is false, actuator wired+executing since 2026-08-14; real defect is Tier-1 adoption (never invoked per PO). Split cross-zone, brief in shape of the 2026-08-14 pilot brief, name a forcing function (PO suggested pre-commit).
**Findings:** Corrected PO evidence: found 2 MORE Tier-1 marker commits on 2026-08-25 (d1ceb8cd9, 0a7d11978), both post-review — adoption is 2/20 not 0/20, verdict unchanged (still needs a mechanical gate). Corrected PO named host: `scripts/git-hooks/pre-commit` fires BEFORE git obtains the proposed commit message (githooks(5)) — structurally cannot read a message-embedded `[notebook-compose OK|WARN ...]` marker. Repo has no `commit-msg` hook today. Root-caused the Tier-1-specific adoption gap: `tier1-probe.md` (lazy-loaded, separate ~800L file) ends with no pointer back to `main.md`'s Notebook Write section, unlike Tier-2/3 which read `main.md` inline and flow straight into it.
**Output:** `docs/architecture-briefs/2026-08-26-fix-auditor-notebook-compose-tier1-adoption-gap-and-commitmsg-forcing-function.md`. Split into `FIX-AUDITOR-NOTEBOOK-COMPOSE-COMMITMSG-MARKER-GATE` (developer, `scripts/git-hooks/`, P0 — new `commit-msg` hook, warn-by-default with a `notebook-compose-marker-allow:` escape hatch for the legitimate data-repair-commit class) and `FIX-AUDITOR-TIER1-PROBE-MISSING-RETURN-TO-NOTEBOOK-WRITE-POINTER` (agent-father, `docs/agents/system-auditor/flow/`, P2, non-load-bearing complement — 3-line pointer, not a restatement). Parent row archived (status DONE) via `orch-apply.sh`, `.head` untouched. Confirmed non-duplicate of `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES`/`FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE`.
**Next:** developer implements Child A (commit-msg hook + test + install.sh + dev-standards.md CANONICAL pointer). agent-father implements Child B (tier1-probe.md return pointer) independently, no ordering dependency.

## 2026-08-26T13:57Z — FIX-MANUALDISPATCH-BLIND-TO-READY-ROWS-OFF-ALLOWLIST-WITHOUT-XOR-FLAG (P0, dev-team design-router sweep dispatch, zone=cross-service/)

**Task:** PO claimed a ready[] row carrying an off-DRS-allowlist non-dev `next_agent` and NEITHER `supervised` nor `plan_only` is reachable by ZERO pickers (24 rows, 3 P0) — asked for a 4th manual-dispatch predicate in `scripts/lib/po-manual-dispatch-eligibility.jq`.
**Findings:** AC-1 recompute via the real shared predicates gave 15 rows (not 24 — 10 more are `deps_satisfied`-blocked), 0 P0 (confirmed the router's pre-dispatch correction). Went one step further than the recompute asked: checked the claim against `scripts/devteam-backlog-claim-ready-lane-consumer.jq` (RLC), not just the 3 named gap-detector predicates — RLC gates ONLY on `sup!=true AND po!=true AND !wrapper AND deps_satisfied AND !detail_deferred AND a resolved next_agent/owner`, with NO allowlist/dev-role check by design (its own header names `agent-father`/`qa` as intended targets, bypassing zone-detect on purpose). All 15 rows already satisfy every RLC gate — this "hole" is RLC's designed target class, not a gap. Independently corroborated: PO's own `po_promote_note_20260806T2331` on one of the 3 named example rows already re-verified RLC-eligibility 20 days before the same row was cited as zero-pickers evidence here. Root-caused why `is_ready_xor_gap` genuinely needs the manual-dispatch mechanism but this "neither flag" class does not: RLC's gate is an AND (both false required) — XOR rows fail it and are truly excluded; neither-flag rows trivially pass it. Building the prescribed predicate would not close a gap and would open a NEW double-dispatch race (manual-dispatch-sweep's BATCH path never coordinates with RLC's claim/lane-move — unlike the 3 existing classes, which are lane-disjoint from or excluded by RLC by construction).
**Output:** `docs/architecture-briefs/2026-08-26-fix-manualdispatch-ready-lane-off-allowlist-hole-already-rlc-covered.md`. No code touched in any of the 4 listed files — AC-2's own constraints (do not widen `is_ready_xor_gap`/the DRS allowlist) honored by not building the 4th predicate at all, since it was the wrong shape, not merely unneeded. Real residual found instead: RLC shares `wip_in_progress<2` with 3 siblings, single-claim/turn, no anti-starvation aging, vs 113 live `ready[]` rows — several flagged rows are 20-43 days old despite RLC-eligibility the whole time. Minted `FIX-RLC-SHARED-WIP-BUDGET-SINGLE-CLAIM-STARVES-AGED-READY-ROWS` (backlog, P2, next_agent=architect) for that separately-scoped throughput problem. Journal STEP architect-S55 (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-7.md`). Parent row archived `DONE` via `orch-apply.sh`; `.head` idle-reset (still named this task); diff-checked before commit — clean, no peer writes in the read→commit gap.
**Next:** PO/router to note the ready[]-lane portion of this defect class does not exist (RLC already covers it) when triaging future "manual-dispatch escape hatch" claims — check the actual claim script's own gate, not only the 3 named gap-detector predicates, before minting a 4th. Separate architect picks up `FIX-RLC-SHARED-WIP-BUDGET-SINGLE-CLAIM-STARVES-AGED-READY-ROWS` when scheduled.

## 2026-08-26T19:35Z — FIX-PRECLAIM-INTENT-KEY-TTL-600-EXPIRES-UNDER-LONG-RUNNING-AGENT (P0, dev-team Ready-Lane Consumer dispatch, sibling of a shipped fix not a duplicate, zone=.claude/skills/dispatch-claim/)

**Task:** Router `intent:<agent>:<key>` PRE-CLAIM (ttl=600, CARD.md's own "hard gate") has no heartbeat renewal — measured 4x same-tick-family (architect 22m50s, agent-father 11m10s + 1082s, po 16m49s), all past ttl at return, 3 of 4 already `released:0`. Sibling row `FIX-DEVTEAM-RESUME-KEY-TTL-3600-...` already fixed the analogous `task:<row-id>` lock — this row is that fix's untouched twin in a different namespace, not the same bug.
**Findings:** Root-caused why the sibling's mechanism does NOT transfer: `task:<row-id>` is claimed by `dev-team`, which cannot itself call the spawn tool a level deeper (nested-spawn limitation) — it hands off and exits within ms, so its liveness proof comes from a LATER cron tick revisiting `.head` (30min cadence, 3600s ttl, 2x headroom). `intent:<agent>:<key>` is claimed one layer further out, by whoever executes the REAL blocking spawn — that context is inert (no interleaved tool call slot exists) for the agent's entire measured runtime, so ONLY the spawned agent itself is provably live throughout. Raising the base ttl was explicitly out (row's own constraint + sibling's AC-4: "moves the cliff"); cross-checking the sibling's now-fixed `task:` lock as a backstop only covers sprint-task-linked dispatches, not pure ad-hoc router-user-intent ones; a router-side concurrent heartbeat loop is structurally impossible given the blocking-call constraint.
**Output:** `docs/architecture-briefs/2026-08-26-fix-preclaim-intent-key-ttl600-spawned-agent-self-renewal.md`. Design: the claiming dispatcher embeds a `SELF_RENEW_BLOCK` text instruction into the spawn prompt it already builds (reusing the ALREADY-passed `coordination_session` value, no new plumbing); the spawned agent executes ONE `task_heartbeat(intent:<agent>:<key>, owner_client_session=<dispatcher's session>, ttl_seconds=3600)` as one of its first tool calls — single-shot, bounded, never a recurring loop; base claim ttl (600s) untouched; an agent that never reaches the instruction degrades byte-identically to today. Exact CARD.md/SKILL.md diffs (+1L CARD.md, new § Step 2.5b in SKILL.md) supplied for agent-father to apply verbatim, plus a verification plan (scratch-`coordination.db` replay, never live). Journal STEP architect-S56. Row lane-moved `in_progress[]`→`ready[]`, `next_agent: agent-father`, status READY, via `orch-apply.sh`; `.head` idle-reset in the same write.
**Next:** agent-father applies the CARD.md/SKILL.md diffs verbatim and runs the brief's §4 verification plan against a scratch coordination.db copy before marking done.
