# Architect — Notebook

**Last updated:** 2026-08-06 09:23 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-06T09:23Z — FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN (P1, supervised, review-lane throughput — router dispatch, decision in `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-3.md` §S7)

**Task:** 3rd PO manual-dispatch pick on this row (07-28 promote, 08-01 sweep, 08-06 sweep), 1st actual dispatch — router-flagged that `po_manual_dispatch_flagged_at` was narrated-not-written in a prior cycle. PO's framing: review[]≈270 rows against qa[]=1, is the `qa[]<1` cap still right, does QA sign-off need batching, does age-ordering need priority-awareness.
**Found the remedy already 90% designed, not undispatched-because-unplanned:** this row's own 2026-07-22 fix (Review-Lane QA-Drain block + `verify-committed` qa mode) shipped and works, but sits LAST in a 5-lane head-idle-only fall-through (BOUNDED-1→SLS→RLC→DRS→QA-Drain) with a `qa[]<1` single-slot cap — un-cited-by-this-row prior art (`SPIKE-DEVTEAM-QADRAIN-HEAD-SLOT-DECOUPLE`, Part 1 DONE_VERIFIED; this row's own 08-01 brief, Part 3's `QA_CAP=10` batch design) already covered most of remedy (a)/(b)/(c), just never dispatched.
**4 new findings that unblock it (full detail: `docs/architecture-briefs/2026-08-06-review-lane-qadrain-throughput-unblock.md`):** (1) Part 2's `depends` on `FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` was circularly deadlocked — that row's own dependency (`TASK-DEVTEAM-IDLE-CHAIN-1-SCHEMA-UTILITIES`) sits in `review[]` itself, 8 days stale, one of the 226 rows this fix would drain; dropped as obsolete (SECONDARY-Drain already proved the same insertion anchor safe without it). (2) Part 2's 07-29 illustrative code was still the pre-batch single-claim shape — unified with Part 3's `QA_CAP`/`TAKE_BUDGET` design so idle-tick and busy-tick invocation sites behave identically. (3) Part 3 was mixed-zone (touched `docs/agents/dev-team/flow/main.md`, agent-father's commit zone per `po_routing_ruling_20260721`, but assigned solely to developer) — split: `.jq`-only stays with developer, `main.md` half folded into Part 2 (agent-father). (4) Pure age-ordering starves urgency: 27 live P0 review rows, 7 arrived TODAY (age=0d) and would queue behind a 13-14d wall of P2/P3 `FACTORY-*` cleanup rows — added `sort_by([priority_rank, .age])` to Part 3's AC, reusing the existing shared def (`scripts/lib/devteam-eligibility.jq`) BOUNDED-1/RLC/DRS already use.
**Output:** promoted+refined `FIX-DEVTEAM-QADRAIN-INVOCATION-HEAD-DECOUPLED` (backlog→ready, agent-father, unified Part2+3-main.md scope, dropped deadlocked dep) and `FIX-DEVTEAM-QADRAIN-THROUGHPUT-CAP` (backlog→ready, developer, `.jq`-only scope + priority-ordering + backward-compat-default ACs) via `orch-apply.sh`. This parent row moved `ready[]→backlog[]`/`BLOCKED` (orch-validate rejected `BLOCKED` inside `ready[]` — lane-coherence Stage 1b, learned live) with machine-readable `depends_on` both children — genuinely non-actionable until they ship, not merely unpicked.
**Next:** agent-father + developer implement the 2 children (no PM decomposition needed — both already atomic, single-owner, fully speced); DoD unchanged (`review[]` PRIMARY count trending DOWN across 5 consecutive dev-team ticks, ids named) — re-triage this parent row once both are `DONE_VERIFIED`.

## 2026-08-06T09:03Z — 3-task PO triage batch: FU-RAG-DEPLOY-MEMORY / FIX-SYSTEM-AUDITOR-CYCLE-FINDINGS-NOT-SELF-PERSISTED / FIX-CHEF-PUBLISHED-MARKER-RELEASE (router dispatch, decisions in `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-3.md` §S4-S6)

**FU-RAG-DEPLOY-MEMORY (P1, INFRA):** first confirmed OOMKill (docker events: oom 08:17:28Z→die exitCode=137→auto-restart). Verified LIVE headroom before sizing: Docker Desktop VM is capped at 8192MiB (7.75GiB), NOT the full 16GB host — `docker stats` sums ~2.74GiB actual usage vs 4.1GiB available inside the VM; host macOS swap separately at 86% (independent, pre-existing risk, not worsened by a container LIMIT raise). Decision: `docker-compose.yml` rag-service memory 768m→1g, reservation 256m→512m (measured need ~726-750MiB anon-resident + burst/slack — 768m proved insufficient twice). Structural follow-up designed: embedder.py idle-unload timer (no unload path existed — flagged 3x on this row's history, never actioned) so the container stops being pinned near-cap permanently after the first request. Brief: `docs/architecture-briefs/2026-08-06-rag-service-memory-sizing-remediation.md`. Board: `ready[]` stays, `next_agent=developer`.
**FIX-SYSTEM-AUDITOR-CYCLE-FINDINGS-NOT-SELF-PERSISTED (P1, plan_only, root-cause ask):** tested PO's background-vs-foreground discriminator (consistent, not provable from repo alone) and found stronger independent evidence: 6 orphaned EMPTY `.auditor-cycle-markers-<TICK>.tmp` scratch files (3 known + 3 NEW from 2026-08-05, both Tier-1 and Tier-2 cadences) — each is mechanical proof a cycle won its fire-election and died before its own final cleanup line, zero matching notebook sections. Root cause: no durability checkpoint between fire-election-won and full cycle closeout across a 965L narrated flow; the already-hardened commit script only protects the LAST step, not reaching it. Designed (plan_only, no code here): Step 0b stale-marker sweep (externally detectable within one tick, any tier) + reorder notebook-write earlier + durable scratch-draft-then-compose so a dropped cycle self-heals via deterministic script/skill logic, not model reconstruction (the false-peer-citation symptom's actual mechanism). Brief: `docs/architecture-briefs/2026-08-06-fix-system-auditor-cycle-notebook-persistence-lifecycle.md`. Board: `backlog[]→ready[]`, `next_agent=pm`.
**FIX-CHEF-PUBLISHED-MARKER-RELEASE (P1, sibling of FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR):** corrected the row's own premise — grep found ZERO live `task_release` call sites on any `published:*` key; the 08-06 release was a manual PO action on a content judgment, not automated "chef.md cleanup" (that shape predates the 07-03 fix). Designed unified with the sibling ANCHOR row per PO's "one lifecycle, one design" ruling: Component A propagates `scheduled_utc_time` (already computed by `cowork-catchup-predicate.js`, unconsumed) into `spawn-fanout.md`'s trigger_prompt for BOTH live and catch-up fires (found live matches don't expose the field either — extended `cowork-match-slots.js` symmetrically) so a retry can never re-derive a different key than the run it's retrying; Component B adds a mechanical Published Marker Release Gate (delivery-evidence check) before any future release. Brief: `docs/architecture-briefs/2026-08-06-cowork-marker-lifecycle-anchor-and-release.md`. Board: `backlog[]→ready[]`, `next_agent=pm`, `blocked_by` cleared. Left FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR's own status untouched (out of this cycle's scope) — added an additive `architect_crossref_20260806` field only, pointing PM/developer at the same brief.
**Next:** pm decomposes the 2 plan_only/multi-file rows into dev-team tasks; developer picks up FU-RAG-DEPLOY-MEMORY directly (single-zone, atomic).

## 2026-08-06T08:20Z — FIX-VNINDEX-CROSS-PLANE-PLAUSIBILITY-GATE (zone=apps/mcp-server/, P1, supervised+plan_only, ba spec → architect)

**Task:** ba's spec ratified the durable AC (Part 2 cross-plane gate; Part 1 correctly disproven/closed) and recommended `market_prices.VNINDEX` as FR-1's comparison target. My job: confirm target + design EC-1's fail-open/fail-closed rule precisely.
**Correction found (brownfield-first, BA didn't reach this file):** `apps/macro-indicators/pkg/infrastructure/repositories_market_index.go` — macro-indicators' own tier-1 PRIMARY query is `SELECT price FROM market_prices WHERE code='VNINDEX'...` against the SAME physical SQLite file mcp-server writes (readonly mount). On the tier-1 happy path this is the identical row read twice, not a second plane — `vn_index_cache` is no more independent (same write event, `vnIndexRefreshJob.ts` writes both tables from one `fetchVnIndex()` call). Ratified the target anyway (only cheap local read available, conclusively catches the ACTUAL incident class — fixture-fallback tier-4) but documented precisely what FR-2's "tier-1 misreport" defense does/doesn't cover so the shipped code-comment doesn't overclaim.
**Design:** New `domain/services/vnIndexPlausibilityGuard.ts` (`evaluateVnIndexPlausibility`, sibling of `macroOutlierGuard.ts`) — fail-closed only when local ref untrustworthy AND macro is_estimate/tier>=3; fail-open when local ref untrustworthy but macro tier-1; unconditional >=5% divergence check (local-denominator, matches `macroOutlierGuard.ts`'s own `>=` convention) whenever local ref is fresh. Reused `freshnessSlaChecker.ts`'s existing `signalType:"price"` SLA for EC-1's freshness gate instead of a new hardcoded threshold — already market-hours-aware, satisfies EC-2 for free. Gap-token = `vnIndex`/`vnIndexDelta`→null, `vnIndexDirection`→"unknown" (matches Go's own `computeDelta` no-baseline convention); `is_estimate`/`source_tier` stay visible (diagnostic). Single mutation point in `macroTools.ts` before BOTH `buildMacroSnapshotText(data,...)` and the raw `data` passthrough — flagged as a 2-channel-leak risk if split.
**Output:** Appended `[Architect] Brownfield Findings` to `docs/handoffs/FIX-VNINDEX-CROSS-PLANE-PLAUSIBILITY-GATE-BA-spec.md`. Board: moved `backlog[]→ready[]`, `next_agent=dev-mcp-server` (design fully atomic, no PM decomposition needed — same pattern as S2 below). Annotated sibling `FIX-VNINDEX-ESTIMATE-IMPLAUSIBLE-DELTA-GATE` (row 207, P2) with a fold-forward pointer, not closed (fold happens on this row's completion per BA §7).
**Self-caught bug:** first orch-apply attempt used a jq `,`-separated multi-field object inside `|=` RHS — jq's `|=` silently took only the FIRST comma-clause (architect_review_note applied, status/next_agent/updated_by all silently dropped). Caught by read-back verification before treating the write as done; re-applied with `+`-merge object literal, verified again. Note for future writes: NEVER comma-chain multiple field assignments inside a `|=` RHS — use `. + {a:.., b:..}` or top-level `|` pipe stages instead.
**Next:** dev-mcp-server implements per the handoff's §B/§C (exact file list, insertion point, gap-token field spec, test strategy already given — atomic single-zone). FR-3 (macro-indicators DTO baseline-provenance field) flagged fast-follow, not gating.
