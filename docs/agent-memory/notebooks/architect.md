# Architect — Notebook

**Last updated:** 2026-08-07 13:23 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-07T13:23Z — FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER (P0-critical, supervised, router manual-dispatch — sprint=BACKLOG/ambient, fast-follow of my own 2026-08-05 finding)

**Task:** PO's manual-dispatch-sweep top-ranked candidate; own fast-follow from `FIX-MCP-MEMORY-CODE-LEAK`'s Finding 2 (2026-08-05), promoted to critical after PO's post-deploy `/health sessionCount` vs `MemPerc` correlation ended in an observed container death (6h36m uptime, 873 un-reaped sessions, ~86/h net growth, VmHWM still climbing at the cap). Confirm mechanism + design the fix.
**Verified mechanism (source-read, not re-derived from PO's note):** `apps/mcp-server/src/interface/mcp/transport.ts` — `mcpServer` (line 95) is a bare local, never `.close()`d by EITHER existing eviction path (`res.on("close")` line 84-92, heartbeat-write-fail line 65-79). Confirmed via installed SDK source (`shared/protocol.js:500-502`, `server/sse.js`) that `McpServer.close()` alone cascades to `transport.close()` — one call suffices, no need to close both separately. Independently cross-corroborated the "gateway dials a fresh SSE connection per tool-call and drops it" traffic shape via `perCallCounterStore.ts`'s own header comment (unrelated feature, same underlying fact, 3rd independent source after the architecture doc and PO's own measurement).
**Design:** Collapse the 2 parallel `Map`s (`sessions`, `heartbeatIntervals`) + 2 duplicated inline cleanup blocks into 1 `SessionRecord` map (`transport`+`mcpServer`+`heartbeatInterval`+`createdAt`+`lastActivityAt`) + 1 `evictSession()` method every trigger calls — the duplication is itself why `mcpServer.close()` went missing from BOTH pre-existing paths, not just the absent-reaper half. `handleMessage()` bumps `lastActivityAt` (idle-timeout basis); `createdAt` drives an independent hard max-age backstop. New `DELETE /sse|/messages?sessionId=` route — explicit defense-in-depth, NOT the primary fix: the `gateway` MCP server originating the dominant traffic is out-of-repo (confirmed no `/sse` client exists under the unrelated Go `apps/api-gateway/` zone), so it can't be made to send clean DELETEs; the reaper is what actually bounds the leak. Idle/max-age thresholds (15min/4h) flagged explicitly as informed-not-derived defaults (constructor params, same override idiom the existing `_heartbeatIntervalMs` already uses) — recommended the same post-deploy correlation PO already ran once as the empirical tuning loop, not a priori.
**Output:** `docs/architecture-briefs/2026-08-07-fix-mcp-sse-session-manager-reaper.md` (full design, DDD note — entirely interface-layer, scan clean — + test plan: 7 new unit cases extending `1862c-transport-session-eviction.test.ts` incl. a negative control proving the activity-bump actually resets the idle clock, 2 new integration cases extending `081-bun-mcp-server.test.ts` for the `DELETE` route). Board: `architect_review_note` stamped, `next_agent=dev-mcp-server` (single-zone `apps/mcp-server`, no BA spec, no new primitives — PM decomposition skipped, matches the sibling fix's own precedent).
**Next:** dev-mcp-server implements §2 of the brief, extends the 2 named test files, full suite green, then re-runs the `/health sessionCount` vs `MemPerc` correlation post-deploy before closing the row.

## 2026-08-07T05:12Z — FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING (P1, plan_only, supervised, sprint=COWORK-RELIABILITY)

**Task:** BA spec complete + PO go-ahead ratified; design a WINDOW_KEY/cycle-id keying scheme for `docs/signals/*.json` writers (bctc-analyst, chef/unified-agent) so intra-day cycles stop colliding on one filename, without re-keying on raw `cycle_id` (2026-07-22 CAUTION — run-start-keyed, not a safe dedup key) and without letting filename-key and mutex-key disagree (NFR-3).
**Design:** One shared `derive_window_key()` pure function (§2 of the brief) — prompt-parsed `scheduled_utc=` once `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR` Component A ships (live-verified NOT yet on any `trigger_prompt` today), else slot→cron-lookup fallback (the ACTIVE path day one). bctc-analyst: pin at `cycle.md` Step 0c (before Stages 1-4 write), reuse verbatim at Stage 5's existing published-marker guard; filename → `bctc_signal_{TICKER}_{WINDOW_KEY}_{mode}.json`; bundled FR-7 (explicit routine-mode emit line, previously undocumented) + a stale cross-reference fix in `stage-consolidate.md` (pointed at the wrong file for the actual signal-file write). chef-intraday only: filename gains `-{VN_HOUR}`, reusing the EXACT value the live multi-fire `MARKER_KEY` already uses (NFR-3) — deliberately NOT switched to a UTC hour yet, since that would require migrating the mutex key's own basis too (out of scope for the sibling ANCHOR row); flagged as a named Phase-2 follow-on for PO, not silently bundled. FR-6 (drain reader): confirmed NO-OP at source (`drain-signals.js:173` fingerprint never includes filename) — doc-only line added, zero code change.
**FR-4 (tnb) RAW-verify performed, not deferred:** git log since the 2026-07-29 marker-cadence fix shows 3 tnb-audit cycles (c121/c122/c123), zero repeat `c<NNN>` collision. Did NOT design a new write-serialization mechanism against unreproduced evidence — flagged disposition (monitor-only vs. cheap `task_claim`-based defense-in-depth; confirmed `task_claim` IS available to tnb, unlike dev-*/qa specialists under INV-GATEWAY-1/WF-3) to PO as a risk-tolerance call.
**Output:** `docs/architecture-briefs/2026-08-07-cowork-signal-filename-cycleid-keying.md`. Board: `next_agent` set to `po` (plan_only+supervised preserved, no code shipped, no PM/dev handoff this cycle per dispatch instruction).
**Next:** PO reviews brief, adjudicates 2 open items (FR-4 disposition; EC-2 Phase-2 UTC-hour migration follow-on task), decides developer dispatch.

## 2026-08-06T20:11Z — FIX-EXECUTETIER-PHASE35-RESUME-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION (P0, 3rd-occurrence recurring, router direct-dispatch)

**Task:** Adjudicate whether main.md's already-shipped LOCK-LIFETIME fix (S2/SLS/RLC/DRS/QA-Drain-resume: no release on claim-success, `ttl_seconds:3600` is the lifetime bound, release only on the spawn's own exception path) applies to EVERY claim in execute-tier.md Phase-3.5's `spawned_batch`, or only the single-task BOUNDED-1-JUMP-TO-execute case — plus a 2nd candidate design (`po_scope_widen_20260806`): a Step-0b short-circuit on `head.updated_by` prefix.
**Ruling (a):** applies to EVERY claim, unconditionally. `task:<id>` is a per-task_id mutex ("does this task already have a live spawn") with NO relationship to `.head` — found 3 already-shipped precedents proving the pattern already generalizes to multi-item, `.head`-decoupled batches with zero modification: `post-cycle.md` § Step 4.4 Wrapper-Autoclose (explicit: "independent of the head-idle fall-through's own single resume-pointer... NOT `.head`"), main.md's QA-Drain (idle-tick + head-decoupled sites, `picked_batch` loop), SECONDARY-Drain. The mint's own "different correctness for multi-task batches" hypothesis doesn't survive once the lock's scope (per-task liveness) is untangled from `.head`'s scope (single resume-pointer).
**Ruling (b):** rejected. A provenance check on `head.updated_by` can't distinguish "already spawned, hold" from "outer_claim collided, never spawned, should legitimately retry" — both leave the identical prefix. Adopting it would either permanently strand a collided BOUNDED-1-origin head or offer no real protection past the lock's own TTL — a second, weaker mechanism duplicating what fixing (a) already answers correctly. Also does not address the same signal's COMPOUNDING finding (`head.next_agent` left as literal non-spawnable `"dev-team"`) — flagged as a distinct, not-yet-tracked defect, left out of scope.
**Shipped:** rewrote execute-tier.md Phase-3.5 (per-task try/except, batch-wide `finally` removed, added missing `owner_client_session` to task_claim/task_release per P1-FINAL/TASK_1980). Wrote + live-ran `scripts/audits/execute-tier-phase35-locklifetime-verify.sh` against the REAL task_claim/task_release primitives (mirrors commit `adb426877`'s own live-proof methodology, not a synthetic fixture — the defect lives in the primitive's own mutex semantics): 4/4 PASS, including the negative-control counterfactual (2nd claim only succeeds after an explicit release, proving the pre-fix duplicate-spawn window). Full rationale: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-3.md` §S9.
**Found mid-verification:** first script draft had a jq `//`-on-`false` bug (`.claimed // false` silently discards a genuine `claimed:false`, since jq's `//` falls through on `false` not just `null`) — self-caught via the FAIL output, fixed to explicit `if .claimed == null then ... end`, re-ran clean.
**Next:** none — row is self-contained (flow-doc edit + regression script), no PM decomposition needed. Board flip to REVIEW/DONE with journal entry per DJ-GATE-1.
