# Architect — Notebook

**Last updated:** 2026-08-11 16:59 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-11T13:26Z — UC-RDL-P4 (P1, `apps/mcp-server/`, SPRINT-M/L, `supervised:true`, BA→architect pipeline — PO ratified Q1 SPLIT + Q2 build-now-rebuild-later)

**Task:** Brownfield design for a composite `dispatch_preflight` MCP tool collapsing the router's 4-call Phase 0a/A/A.5/B preflight into 1. PO scope-gated: FR-7 (CLAUDE.md/dispatch-claim doc cutover) forbidden this wave — tool + registry sync + unit tests only.
**Findings:** Layered cleanly on `getCycleBootstrap.ts`/`cycleBootstrapTool.ts` precedent, but found one structural reason NOT to copy it verbatim: its `Promise.race` timeout wrapper is decorative for synchronous work (all 3 of ITS OWN sub-calls are plain sync functions, same as this tool's `claimTask`/`heartbeatTask`/`listHeldTasks` — a `setTimeout` race can never preempt a synchronous call in a single-threaded runtime; `coordination.db`'s real protection is its own `busy_timeout=5000` pragma). Bigger find: `docs/data/tool-registry.json` carries `"_maintained_by": "generator (do not hand-edit)"` — BA's §6 fix-row 5 ("hand-add an entry") would have violated the file's own contract; the correct mechanism is `bun scripts/gen-tool-registry.ts` (scans `server.tool()` call sites under `apps/mcp-server/src/interface/mcp/tools/`, auto-groups the new file by first-level subfolder — lands in `system` group with zero generator change). Also confirmed `orphans`/`roster` must stay raw `LockRow[]` (BA's own literal type) NOT the aliased `owner`/`created_at`/ISO-`expires_at` shape `taskListHeldTool.ts` produces for its own callers — a real divergence the FR-7 follow-up must not assume away, flagged forward explicitly. Found and flagged (not fixed, out of L-bound) a pre-existing `TaskKind` 7-value Zod enum duplicated in 2 files already (`feedback_preclaim_gate_taskkind_enum_drift` class) — this task adds a 3rd, unavoidable copy.
**Output:** New `dispatchPreflight.ts` (application) + `dispatchPreflightTool.ts` (interface) + a small mandatory extraction (`serverSessionId.ts`, dedupes the `owner_session` diagnostic IIFE that would otherwise be copy-pasted into a 2nd file — `always_extend_not_duplicate` is a hard constraint, not a suggestion) + `dispatchPreflight.test.ts` (6 FR-9 groups, reusing the existing `_injectCoordinationDb`/`COORDINATION_DB_PATH`-bad-path test harnesses verbatim, zero new test infra). `coordinationTools.ts` gets a 7th registration. `tool-registry.json` regenerated (183→184), never hand-edited. Zero edits to `coordinationStore.ts` or `coordination/index.ts` (PO's own correction on the latter verified live). Minted linked follow-up `UC-RDL-P4B-DOC-CUTOVER` (backlog, `depends_on:["UC-RDL-P4"]`, `supervised:true`, carries FR-7's already-complete spec forward + the field-shape warning above) since PO's ruling required it and it did not already exist. Full findings: `docs/handoffs/UC-RDL-P4-BA-spec.md` § [Architect] Brownfield Findings (D-1..D-10).
**Next:** pm decomposes FR-1..FR-6/FR-8/FR-9 into developer task(s) — single zone, tightly-coupled usecase+tool pair, likely one atomic task. `.head.next_agent` flipped to `pm`; `UC-RDL-P4` row stamped `architect_design_complete`/`architect_build_standard:"lean"`/`architect_linked_followup`. Both orch-state writes applied via `orch-apply.sh` (conservation check OK, task_total 769→770 — the 1 new backlog row).

## 2026-08-11T16:42Z — UC-ASL-P5 (P1, `cross-service/`, BA→architect pipeline, BA-rescoped — 2 of 4 original brief parts declined by BA before this task started)

**Task:** BA independently re-verified the row's 4-part brief and declined 2 parts outright (signal-dashboard SKILL types table, already consciously non-exhaustive; enum tighten, would wedge 18/22 live `signal_queue` rows carrying `status="triaged"`). Surviving scope: FR-1 (`tier1-probe.md` Tier-1 emit `--category-type` mistyping) + FR-2 (`triage-signals.md` dedup-skip branches never close their originating `signal_queue` row). BA left 3 HOW-level open questions + flagged (not actioned) that the row's `supervised:true`/`deploy_gate` fields may now be stale since the only part requiring an mcp-server rebuild was declined.
**Findings:** Re-verified every BA file:line citation live before designing. FR-1's shared "Emit per failure" template (`tier1-probe.md:328-343`) is the ONE call site for A-01–A-11/A-12–A-19/A-21/A-32 simultaneously (no dedicated blocks); A-20/A-33 have their own. Q-status-guard (accept `NEW` alongside `READ` on the dedup-skip RESOLVED-flip?) resolved by tracing `drain-signals.md` §0a-D's batched write (`:78-83`): it flips a claimed row's `NEW→READ` in the SAME atomic call that produces the `pendingSignals[]` entry PO later triages, and confirmed (via `anomaly-task-bridge/SKILL.md` + `ci-health-probe.md` payload shapes) that this dashboard path is the ONLY way FR-2's target row-id is ever resolvable at all — so `NEW` is provably unreachable at dedup-skip time today; chose `READ`-only, mirroring `task-archive.md`'s existing guard exactly, and flagged the durable-drain-combined-write dependency this reasoning rests on for a future regression fixture. Q-A12-A19-boundary ratified BA's `signal_feedback` default (transport-probe fact ≠ confirmed degradation, matches the file's own debounce design intent; verified zero routing-behavior delta against `system-map.json` severity + both candidate PO routing rules). Q-emit-script-signature confirmed zero script change (`emit-audit-signal.sh`'s `--category-type` passthrough already open/verbatim).
**Output:** Full findings + 4 design decisions + risk flags appended to `docs/handoffs/UC-ASL-P5-BA-spec.md` § [Architect] Brownfield Findings. Zero files created; 2 flow docs to modify (both flow-doc/prose edits — FR-1 infrastructure layer, FR-2 interface/application layer; zero server code, zero schema, zero rebuild). Cleared `supervised`/`deploy_gate` on the board row (its sole justification, the declined part-5 rebuild, no longer applies) — added `architect_gate_clear_note`, left prior gate-provenance fields intact as audit trail. `BUILD-STANDARD: not-applicable` (bug-fix/refactor class, no `apps/<svc>` involved).
**Next:** pm decomposes FR-1+FR-2 into one developer task (tight S-size scope, no zone split warranted — `cross-service/` → `developer`). `.head` and board row `next_agent` flipped to `pm` via `orch-apply.sh`.

## 2026-08-11T16:59Z — SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-WIRING (P0, `cross-service/`, PM-direct-mint diagnostic spike, Task 2/2 of the SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING decomposition)

**Task:** Row's own `note` still carried PM's pre-Task-1 framing — trace the call chain for a self-latching `last_fired`-recency predicate hypothesized to block all 8 guaranteed:true slots simultaneously. Explicitly instructed to verify that premise against sibling Task 1's actual findings before executing it blindly, not assume it still held.
**Findings:** Task 1 (DONE_VERIFIED, QA direct-commit verify APPROVED, DJ qa-S46) already conclusively answered the underlying question with live evidence: firer invoked correctly every 900s tick, 14 distinct fires across all 8 slots since 2026-08-08T20:31:19Z, 0 successes, 100% `exit_code=1` bearing the claude CLI's own "You've hit your weekly limit" string — external Anthropic account quota exhaustion, not a wiring defect. Re-verified myself at source (`cowork-guaranteed-slot-firer.sh:141-171` `_fire_one_slot`): straight matcher→claude passthrough, no eligibility/`last_fired` gate exists anywhere in the path — the self-latching-predicate hypothesis is REFUTED, not just unconfirmed. Traced one further real thing while in the file: the script explicitly declares (own header, line 75) it has no gateway/MCP access of its own, and `run_firer()` correctly returns non-zero on any slot failure (lines 218-222) — but nothing ever consumes that exit code. That is the direct mechanical explanation for PO's own P0 escalation note on the parent row ("Nothing escalated this on any channel ... guaranteed:true is currently a silent false promise") — not a gate bug, an absent escalation path.
**Output:** Closed this row findings-only (backlog→review, next_agent=qa, `architect_review_note` carries full reasoning) — no code change here, original premise refuted. Minted new `FIX-COWORK-GUARANTEED-SLOT-FIRER-NO-FAILURE-ESCALATION` (backlog, P1, next_agent=pm, `BUILD-STANDARD: not-applicable`) with a concrete design: reuse the exact live curl-direct-to-Telegram pattern already at `scripts/maybe-deploy-vps.sh:35-41` (bypasses MCP entirely — the only viable path, since the firer has none) to alert BUG on repeated `exit_code!=0`, cooldown-gated. Cross-referenced `FIX-GUARANTEED-SLOT-DUAL-PLANE-DOUBLE-FIRE`'s already-ratified 2026-07-22 "no new code" `architect_ruling` (both firing planes fire independently, dedup is publish-marker-only) as a quota-burn amplifier worth PO/ops awareness — did NOT reopen it (PO's 2026-07-28 ruling forbids reopening any of the 6 consolidated rows before the BA-COWORK-GUARANTEED-SLOT-CATCHUP epic closeout). Added a short closeout-recommendation note on the parent `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` row for pm.
**Next:** qa direct-commit verifies this row's findings (same pattern as sibling FIRER task). pm separately decomposes the new FIX row into a developer handoff, and should consider closing the parent SUPERSEDE-WIRING row citing both diagnostic children's conclusive answer. PRE-CLAIM attempt confirmed architect has no MCP gateway binding (WF-3 Option III, ratified) — proceeded on file-based `.head`/board writes only, consistent with that invariant.
