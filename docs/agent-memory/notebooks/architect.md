# Architect — Notebook

**Last updated:** 2026-07-08 04:50 UTC | **Sprint:** SYSTEMIC-REMAKE-P1

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-07-08T04:45Z — SPIKE-GATEWAY-BLIND-CLI-HANDSHAKE (CONCLUSIVE — server exonerated, client-side confirmed)

**Task:** SPIKE, cross-service/, timebox 120min. Own-session confirmed gateway-blind on spawn (tool schema = {Read,Edit,Write,Bash} only, zero mcp__* — matches the pattern under investigation).
**Root cause:** NOT server/config. Ran full curl MCP handshake (initialize→notifications/initialized→tools/call) live against `https://zenmidi.com/gateway/mcp` — 100% clean success end-to-end, going further than the prior dev-team probe's incomplete bare-`tools/list` test. Cross-checked `docker logs mcp-gateway`: zero successful `call_tool invoked` since 2026-07-04T19:10:48Z (~3.5d silence), unaffected by a 2026-07-07T16:35Z manual container restart (RestartCount=0/ExitCode=0) — server-side restart did not fix client-side blindness, independently ruling out the server. Hypothesis (out-of-repo, flagged for user/Anthropic): CLI's MCP client negotiates its connection once per top-level process and does not self-heal on staleness/idle-timeout/downstream-restart; Task-tool/background subagents inherit the parent's (possibly dead) connection rather than negotiating fresh.
**Repo-actionable:** existing `FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP` (caba878b7+83bca6c04, shipped 07-07) already covers the 8 no-Bash cowork cycle agents' Step-0 bootstrap call site — correctly scoped, not duplicated. Gap found: Bash-equipped dispatcher agents (dev-team/PO/architect) have a proven-working but undocumented `mcp-call.sh` workaround, and the 3 gateway meta-tools (list_servers/list_server_tools/search_tools) have zero bash-callable equivalent. Proved the exact 3-request stateful sequence needed to close that gap (§2 of brief) — handed off as a scoped FIX rather than implemented here.
**Output:** `docs/architecture-briefs/2026-07-08-gateway-blind-cli-handshake-spike.md`
**Board:** SPIKE → DONE (conclusive). Minted `FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE` (backlog, S, next:developer) — mcp-call.sh gateway-meta extension + gateway-call-contract.md §6 degraded-mode doc (self-diagnosis, workaround coverage matrix, de-escalation/dedup rule to stop per-tick CRITICAL re-raises).
**Next:** developer (follow-up FIX, not urgent — workaround already in daily use).

## 2026-07-08T00:00Z — FIX-DAILY-OHLCV-UNIT-CONTAM-LT1000-FPT-VHM Round 2 (WRITER-H GAP FOUND — active leak)

**Task:** PO re-triage of duplicate telegram 3518/3519 (ohlcv-sanity whole-row contamination alert, VHM/VIC). Round-1 design (2026-06-30, CONTAM-10-MIGRATION/WRITER/SANITY) already shipped+deployed, but the live repair (CONTAM-10-EXEC) was never run — RAW dry-run today shows contamination GREW to 6,533 rows/27 tickers (from 10 rows/7 days).
**Root cause (live RAW-verified, not local decoy DB):** `handlePushOhlcvHistory` (`apps/mcp-server/src/interface/mcp/routes/ohlcvBackfillHandler.ts`, `/api/push-ohlcv-history`, fired every ~15-30min by `vps-scripts/fetch-ohlcv-backfill.sh` via `ohlcv_backfill_queue` poll — confirmed live/active) was never migrated to `writeOhlcvBatch` — raw INSERT guarded by `validateOhlcvUnit` only (intra-row), structurally blind to whole-row-uniform [100,999] contamination, same class Round-1 already fixed inside `ohlcvWriteService.ts` but this writer bypasses that chokepoint entirely. Confirmed active: VHM/VIC + 22 other tickers share one `updated_at=2026-07-07T19:10:48Z` batch (~750 rows/ticker, full 3yr history), contaminated range extends to 2026-07-07 (yesterday).
**PO hazard investigated (flat cold-start seed bars as anchors):** confirmed live — VHM/VIC 2026-04-30/05-01 flat `O=H=L=C` seed rows have `volume>0` (not the FR-G3 `volume=0` signature), so they DO get picked as anchor (`anchor_close=146000`/`214000`). Value is numerically correct (PO-confirmed) so ×1000 math stays safe — no predicate change. Explicit design decision: do NOT redesign toward a boundary/discontinuity-date-scan (that's what would be corrupted by treating a 2-day flat seed as "where clean data starts"); the existing per-row anchor-ratio predicate never needs to locate a boundary, so it's already immune. A plain `O=H=L=C` exclusion filter would be UNSAFE too — 148,803 live rows are legitimately-flat (thin-liquidity) bars, verified via direct query.
**Fix:** migrate Writer H to `writeOhlcvBatch(rows, db, {conflictStrategy:"backfill"})` — drop-in, same conflict semantics already used by Writer D. Confirmed non-overlapping with stalled `FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0` (Decision D-3 touches the same function but only for intra-row high/low type-coercion, already landed/GREEN per `TASK-OHLCV-WIC-2-writer-h-coerce.test.ts`) — this task's migration preserves that pre-pass and adds cross-day scale detection on top.
**Output:** `docs/handoffs/FIX-DAILY-OHLCV-UNIT-CONTAM-LT1000-FPT-VHM.md` § Round 2 + `docs/architecture-briefs/2026-06-30-OHLCV-UNIT-CONTAM-WHOLEROW-LT1000.md` § 10 addendum.
**Next:** pm — decompose CONTAM-10-WRITER-H (writer fix, apps/mcp-server/src/interface/mcp/routes/) + optional CONTAM-10-MIGRATION-ANCHOR-DIAG (cosmetic report annotation, scripts/migrations/) + CONTAM-10-EXEC-2 (live repair re-run, SEQUENTIAL — new hard gate on WRITER-H landing first, else immediate re-contamination on next VPS poll). Backlog-only: Writer E hardening, brand-new-ticker cold-start blind spot, VPS-script threshold defense-in-depth.

## 2026-07-04T (tick) — FACTORY-INTERFACE-split-server-ts (PLANNING BRIEF ONLY, backlog-detail was NOT_FOUND)

**Task:** P0, zone apps/mcp-server/. server.ts (2527L) is the live MCP tool-serving entry (/mcp,/sse,/messages → toolRegistry) — max blast radius, zero-behavior-change staged extraction required, user-gated.
**Inventory:** ~55 routes already follow the `routes/<name>Handler.ts` one-liner-delegation convention (Pattern A: `handle<Name>(req,res,db)`; Pattern B: pure `build<Name>Response()→{status,body}`, e.g. `foreignFlowStatusHandler.ts`). 18 routes still inline (~1300L), none on the `/mcp` tool-dispatch hot path — they're VPS-push/pull + ops-debug REST endpoints sharing the same `handleRequest` if-chain (hence still whole-surface blast radius on compile/order/return bugs).
**Plan:** 4 staged PRs smallest-safest-first (supersedes the June-15 audit's "largest-first" note): S1 5 debug-trigger routes + extract `requireVpsApiKey` guard (satisfies separately-tracked `FACTORY-INTERFACE-vps-auth-guard-dedup`, BACKLOG) — RECOMMENDED FIRST, core logic already isolated+tested, wiring-only move. S2 macro-push (TE/GSO/Reuters). S3 OHLCV backfill lifecycle (unit-guard + retry-storm Telegram — real logic, more risk). S4 BCTC VPS proxy ingestion (largest, multipart+fire-and-forget+cross-service fetch) — do last. S5 optional/deferred (watchlist, accuracy/digest, thin wiring, low value).
**server.ts.bak:** confirmed stale (pre-dates 2026-05-20 /mcp rewrite, not compiled/imported), already has its own ticket `FACTORY-INTERFACE-delete-bak-files` (BACKLOG) — not bundled here.
**Output:** `docs/architecture-briefs/2026-07-04-server-ts-staged-extraction.md`
**Next:** pm/po — schedule Stage 1 as its own dev-mcp-server task once user approves; each stage independently `pnpm check`-gated + smoke-tested + revertible.

---

## Archive (pre-2026-07-04T (tick))

[Older cycles archived to git history: FIX-DRAINESC-SEVERITY-RECURRENCE-GATE (2026-07-04T01:07Z, GATE-A effective_severity + 2-tier GATE-B board-row-exists/signals_processed recurrence gate), SPIKE-BCTC-DISCOVER-PIPELINE-DEAD (2026-07-03T07:13Z, dead-stage pinned to Stage 3 bctcPdfPullJob overlap/mutex gap, isRunning guard fix, FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD emitted to backlog), SPIKE-BCTC-CTG-BS-REALDATA-ROOT (2026-07-03T07:10Z, 3-layer stacking-bug root cause — parser column-order reversal on bank forms + classifier bold-strip gap + section-vocabulary false-positive — FIX-A..E split), FIX-MCP-MEMORY-CODE-LEAK (2026-07-02T18:17Z, sawtooth-at-tight-cap corroborated live + initDatabase() no-guard hotspot found via source read + McpServer-per-POST rebuild stacking), TOKEN-ECONOMY-TICK-PREFLIGHT (2026-07-02T12:10Z, 3-WU split for cowork/dev-team/auditor tick preflight scripts + stateless-mode MCP verification), ARCH-DASH-CRON-RECHECK-TABLE (2026-07-02T06:50Z, cron-recheck-table SPLIT: job_name hybrid resolution + generic cadence algorithm + Layer-B double-count correction), ARCH-FIX-BCTC-BANK-SUMMARY-MAPPING (2026-07-01T18:20Z, AC-1 spike + bank-form-generic gap + W1-W5 split), BA-PREDICTION-EVIDENCE-REVIVAL (2026-07-01T07:05Z, evidence_type corrections + FR-2.2 VPS proxy 502 root cause + 2-hop split), OHLCV-UNIT-CONTAM-WHOLEROW-LT1000 (2026-06-30T20:45Z, per-ticker anchor repair migration + writer guard + sanity pass 4), FIX-TA-VNINDEX-BENCHMARK-ABSENT-RS (2026-06-30T19:11Z, VPS VNINDEX skip-guard root cause + TASK-VNINDEX-RS-A/B split), BA-IND-P1-MOMENTUM-FRONTEND, BA-IND-P1-MOMENTUM-RS, MARKET-INDICATOR-DEPTH-P0, HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING, FEAT-NEWS-DECISION-RESUME, FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT + 27 earlier cycles pre-2026-06-28.]
