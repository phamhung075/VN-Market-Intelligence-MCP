# Architect — Notebook

**Last updated:** 2026-07-29 21:29 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-07-29T21:29Z — FIX-BOUNDED1-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-LANE (zone=cross-service/, P1 supervised plan_only, dispatched by dev-team Supervised-Lane Sweep)

**Task:** Design a sweep lane for the residual set of backlog rows whose `next_agent` is non-dev-role AND not also caught by SLS (not both `supervised`+`plan_only`) — row's own text cited 61 rows, "no dedicated sweep lane yet."
**Finding:** Live re-verify (never trust the row's own stale count): 61 → **122** (BACKLOG/TODO-scoped, same status filter BOUNDED-1/SLS use), 9h later same day. The existing `bounded1-supervised-lane-report.sh` SECONDARY section (83 rows) is a *different, non-equivalent* set (supervised-XOR-plan_only, a flag condition) from this task's target (a routing condition minus the SLS flag-pair) — 86/122 live rows carry *neither* flag at all, invisible to SECONDARY entirely. Read today's sibling brief (`2026-07-29-qadrain-head-slot-decouple.md`) end-to-end before designing: PO's live dry-run proved `devteam-review-claim-qa-drain.jq`'s unconditional `.head` overwrite — safe only by a "control-flow proves head is idle" argument — breaks the instant reachability is widened. Checked whether that pattern is unique to QA-Drain: it is not — `devteam-backlog-claim-bounded1.jq`/`-supervised-lane-sweep.jq`/`-ready-lane-consumer.jq` all share the identical unconditional-overwrite shape, flagged as a separate out-of-scope follow-up rather than silently left. Agent-risk breakdown of the 122 (by resolved `next_agent`, not the roster's flat `dev-core` type bucket which doesn't distinguish them): `agent-father` carries a broad `Read/Edit/Write/Glob/Grep/Bash` tool grant despite a design-only charter (`not_my_job: writing production code`) — a real risk asymmetry vs. `architect`/`ba`/`pm`/`po`/`agents-architect`, which have no such grant.
**Output:** `docs/architecture-briefs/2026-07-29-fix-bounded1-nondev-nextagent-residual-no-dispatch-lane.md` — new "Design-Router Sweep (DRS)" idle-fallthrough consumer: shares the existing WIP<2 `in_progress` budget (4th writer, not a new budget — DRS's target lane IS the same concurrency resource BOUNDED-1/SLS/RLC already meter, unlike QA-Drain's genuinely orthogonal `qa[]` budget); placed 4th in the fixed chain (after RLC, before QA-Drain, reasoning from relative trust-ordering not just "somewhere in the chain"); mandated the wrapper-autoclose-style conditional-guard `.head` write from day 1 (never unconditional, even though current placement makes that theoretically safe — today's incident proved "safe by placement" isn't durable); scoped a narrower allowlist than SLS's (`{architect,ba,pm,po,agents-architect}`, 76/122 rows) since SLS's real safety net is the supervised+plan_only double-flag itself, which 86/122 residual rows don't carry — `agent-father`/`ops*`/`qa` explicitly flagged for a separate PO ruling, not silently included or excluded.
**Next:** Did not implement (plan_only); did not touch `.head`/task_board (dev-team owns the RAW-verify+flip). `NEXT: pm` once PO ratifies the allowlist question (§6 of the brief).

## 2026-07-29T12:54Z — FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE (zone=cross-service/ per row, actual subject docs/agents/system-auditor/, P0 supervised plan_only, PO board row, recurring_bug_count=6)

**Task:** tier1-probe.md clause-4 `vmhwm_kb>vmrss_kb` veto is a tautology (VmHWM is VmRSS's monotone high-water mark, true almost always) sitting ABOVE the severity map, pre-empting even the OOMKilled/peak>97% CRITICAL branches. Escalated P1->P0 after a 2026-07-29T05:41:23Z ESCALATE(peak>97%) was downgraded to PASS and the container OOM-restarted 113s later.
**Finding:** Router relayed 2 live pieces of evidence mid-session, both engaged directly rather than deferred. (1) A real reclaim: mcp-server 99.37%->94.96% (~132MiB) in 50s — proves the FP class clause 4 targets is real. (2) A live occurrence-#4: the 12:39Z tick logged clause 4 firing verbatim ("VmHWM>VmRSS -> peak-before-window reclamation proven -> VETO downgrade to PASS") while rag-service sat at 99.12%/~6.7MiB free, invisible — confirms VmHWM carries no timestamp and cannot prove an in-window event. Proved `verdict=="ESCALATE"` already requires the script's own `DIPS==0` conjunct for both non-OOM branches (script:94,96) — any genuine in-window reclaim ALREADY resolves FOLD without doc-layer help, so clause 4 can only ever suppress genuine escalations (evidenced 4x, 1 fatal). Not "reverting the override wholesale" (a named dead end): the entire multi-probe script (window/OOM/DIPS logic) stays untouched; only the LATER doc-layer add-on is removed.
**Output:** 2-commit design (stopgap reorder ships alone first per AC8; WARN-veto removal ships second) written to the row's `architect_review_note` via `orch-apply.sh`. Kept "no reclamation dip"->WARN (not promoted to CRITICAL — the one fatal event fired via the separate peak>97% branch). No script-layer DIPS-conjunct change (05:00Z window folds on percentage grounds alone, independent of DIPS). Flagged (not fixed, out of scope): production actually runs verify-a30 as `6 13` (65s span, not the unused `12 25` default) — shorter than today's observed ~90s reclaim arc, a real residual for a future cadence row. Flagged (not fixed): rag-service would false-positive under the redesign once PLANE B's per-container scope widens — the future widening row must port the same ack-ledger PLANE A already ships, not a threshold.
**Next:** Did not implement (plan_only). Set `next_agent=agent-father` (docs/agents/*+scripts/audits/* zone, matches sibling row's qa-established precedent); left status=BACKLOG, owner=architect, zone field uncorrected (po already flagged it stale on this exact row; no qa finding here justifies rewriting it, so I left it too, flagged not silently rewritten). Did not touch `blocked_by`/`blocks` linkage to the sibling P0 scope row (PO's call).

## 2026-07-29T09:20Z — SPIKE-DASHBOARD-TIER-HEALTH-CURL-ERR-FLAP (zone=cross-service/, plan_only supervised, router-dispatched)

**Task:** Isolate real-outage vs probe-FP for the api-gateway/frontend health-check CURL_ERR flap (13 occurrences/9d) and decide the remediation — po's `po_root_cause_20260729T0848` had already pinned the mechanism (two independent 3000ms constants colliding: probe.sh's client budget vs `capability_prober.go`'s per-probe timeout).
**Finding:** Confirmed po's diagnosis, refined the mechanism from source. The 9-downstream deployed-service health fan-out (`services.go` Aggregate) is already parallel via goroutines — NOT the bottleneck po's prose implied. The real sequential cost is `CapabilityProber.ProbeAll` (≤8 manifest entries), and in the LIVE deployment it is 100% wasted work: `NOT_DEPLOYED_SERVICES=` is empty (`docker-compose.yml`) yet `CapabilityProber` is never given the not-deployed set (`main.go:54`) — the port's own documented contract (`domain/ports.go:22-24`, "probes...not-deployed services") is simply unimplemented by the adapter, and several of the wasted probes are live `POST /mcp tools/call` requests to mcp-server. Also confirmed structurally-guaranteed-cold: Tier-1 auditor cadence `*/30min` (1800s) vs 60s probeTTL = 30x, so every audit tick is cold by construction; docker's own 30s HEALTHCHECK interval stays mostly warm, explaining why only the auditor (never docker) flapped.
**Output:** `docs/architecture-briefs/2026-07-29-apigw-health-capability-probe-latency.md`. Split into 2 children (router explicitly asked for the split this cycle, unlike the FIX-A21/POLYMARKET precedent below where I deferred to router/PO): `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` (developer, cross-service/ — raise/repoint timeout, capture curl exit code instead of collapsing to CURL_ERR, wire the deferred debounce, fix the "unreachable" mislabel) and `FIX-APIGW-HEALTH-CAPABILITY-PROBE-GATE-PARALLEL-SINGLEFLIGHT` (dev-api-gateway, apps/api-gateway/ — gate `ProbeAll` on the actual not-deployed set first (near-zero-cost, no observable-output change), then parallelize+single-flight the remainder, then decouple `/healthz` into a true liveness route — `router.go` already calls it a "k8s liveness alias" but wires it identically to `/health`).
**Next:** Closed the SPIKE `DONE` (deliverable = findings + remediation split, not code); minted both children to `backlog` with `next_agent` set; Half B is `supervised:true` (touches the code path docker's own container-health gate depends on); the `docker-compose.yml`/probe-target repoint is explicitly sequenced AFTER Half B ships, not bundled into it.
