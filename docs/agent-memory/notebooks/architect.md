# Architect — Notebook

**Last updated:** 2026-07-12 19:28 UTC | **Sprint:** FLOW-PRICE-ALPHA-LOOP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-07-12T19:28Z — ALPHA-S1-CANDLE-RECOVER + STARTUP-CANDLE-GUARD + OHLCV-BACKFILL-DONE-BUG (wave-1 architect split, READY_FOR_PM)

**Task:** Router dispatch — 3 P0/P1 supervised `zone:multi` backlog rows blocking BOUNDED-1 pickup (~11h stalled). Split into zone-scoped dev tasks per FLOW-PRICE-ALPHA-LOOP sprint goal.
**Finding:** All 3 verified single-zone (`apps/mcp-server/`, not multi). Live RAW probe (docker exec bun:sqlite, read-only) FALSIFIED the dispatch's "ticks purged ~11h ago" framing: `market_prices_history` holds a complete 07-10 session and ZERO 07-11 rows — ticks were never captured that day, not purged. Confirmed `taOhlcvBackfillJob`/`ohlcvBackfill.ts`'s direct-fetch coverage gates check total-row-count only (no recency) AND call a VNDirect endpoint 3 independent code comments confirm geo-blocked from France — neither would recover this gap even patched for recency. Only the VPS-relay pipeline (`ohlcv_backfill_queue` trigger, systemd 30-min oneshot timer, confirmed deployed) is viable. `handleOhlcvBackfillDone` confirmed line-level: `done=1` flips unconditionally, `barsPushedTotal` parsed but never used to gate the outcome.
**Output:** `docs/handoffs/ALPHA-S1-architect-design.md` — new shared `recoverMissingOhlcvSession()` (application layer, reused by both S1-CANDLE-RECOVER's one-off recovery script and S1-STARTUP-CANDLE-GUARD's new scheduler guard); guard reuses existing-but-orphaned `vnTradingCalendar.ts` (`isVnTradingDay`, previously wired only to a deregistered MCP tool); backfill-done-bug reuses the existing R-5 retry/escalate ladder + traces all 5 existing BT-1..BT-5 tests for regression safety, adds a mutual-exclusion guard vs the existing depth-probe re-queue to prevent a double-fire.
**Next:** pm — correct `zone:multi`→`apps/mcp-server/` + `supervised:true`→`false` on all 3 rows (no further TASK_2xxx fan-out needed, each already atomic/S-size); add `depends:["ALPHA-S1-CANDLE-RECOVER"]` to STARTUP-CANDLE-GUARD (shared-function reuse). Reconciliation note recorded: ALPHA-S2-FOREIGN-FLOW-WRITE-RACE already resolved by PO (live `.head`, commit `af465eb93`) before this cycle started — no action needed, wave-2 scope untouched.

## 2026-07-10T19:20Z — BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP (PLAN-ONLY — root cause re-confirmed, 2 concrete mislabels found, PM multi-task decomposition)

**Task:** PO-adjudicated dispatch (SHARPENED+PROMOTED 17:03Z) — `task_board.backlog[]` (385 rows) has 129 live lane-coherence warnings because `scripts/orch-cold-evict.sh` never scans `backlog[]` (only done/done_verified/active_sprints/signal_queue). PO scoped D0-D5; architect asked to verify/refine + decompose.
**Finding:** Root cause 100% confirmed by direct 553L read of `orch-cold-evict.sh`. PO's counts exactly match live recompute (55 terminal-in-backlog / 12 mislaned-active / 1 review DONE_VERIFIED / 58 TODO+DEFERRED = 126, +3 review-BLOCKED PO didn't enumerate = 129). **2 corrections + 1 new gap:** (1) PO's D1 archive target (`archive/backlog-detail.json`) is wrong — that's the live-task detail-prose split (361/384 backlog rows point there for OPEN work); correct target is the dormant `.backlog_detail[]` field already present-but-empty in every monthly cold archive since inception. (2) NEW: `LANE_ALLOWED_STATUSES` has no lane that accepts `BLOCKED` anywhere (7 live rows structurally homeless) — blocks D5's hard-fail flip, needs new D2.5 (PO-ratified schema extension, mirrors ADD-1 `READY` precedent). (3) Spot-checked 6 of the 55+12 rows: found 2 REAL mislabels in opposite directions — `FIX-BCTC-BANK-SUMMARY-MAPPING` falsely `DONE` (live-DB-probed the exact defect via `docker exec ... bun:sqlite`, still reproducing today, corroborated by its own BLOCKED review-lane sub-task with a same-day independent qa finding); `FACTORY-INTERFACE-split-server-ts` falsely `BLOCKED` (all 4 shipped commits present in git log, matches this notebook's own archived 07-04 entry below). Proves D0 cannot be a blind bulk operation.
**Output:** `docs/architecture-briefs/2026-07-10-backlog-hygiene-verify-prune-sweep.md` (full evidence, D2.5 design + rejected alternative, D0-D5 sequencing table with dependencies/risk tiers) + `docs/handoffs/TASK_BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP.md`.
**Next:** pm — decompose D3/D2.5/D4/D0 (parallel-safe) → D1 (after D0+D4 land) → D5 (after D3+D2.5+D1, 0 warnings confirmed live). No board writes by architect beyond the next_agent handoff (execution explicitly out of architect's own-execution mandate given confirmed size + non-trivial error rate).

## 2026-07-10T15:56Z — FIX-AUDITOR-ORCHSTATE-FULLDOC-OVERWRITE-CLOBBERS-SSOT (PLAN-ONLY — LIVE gap found, design complete, code change required)

**Task:** BOUNDED-1 auto-pickup of a 2026-06-26 `recurring_bug_escalation:true` P1 ticket (system-auditor signal-write allegedly emits a fresh minimal-scaffold orch-state doc instead of read-modify-append; source incident commit `de595a44`, backlog 312→0, signal_queue.rows 97→1, `.head` nulled). Router pre-corrected zone-detect default (developer→architect) per `plan_only:true`.
**Finding — two-part, NOT stale (unlike the sibling task-lock ticket):** (1) ROUTING half already shipped 2026-06-27/06-30 — `SSOT-W1-ORCH-APPLY-WRAPPER` (`86286d265`) made `scripts/orch-apply.sh` the single gated write path; writer-audit (`docs/signals/processed/orch-state-writer-audit.json`) confirms all 13 live sites incl. signal-dashboard (system-auditor's own path) route through it; `.head` now Zod-required. (2) CONTENT-SAFETY half (fix_spec's CONSERVATION check) was NEVER implemented anywhere. **EMPIRICALLY REPRODUCED LIVE** this session (zero risk — used the project's own `ORCH_APPLY_LIVE_FILE_OVERRIDE` test-safety idiom): built a disposable 320-backlog/100-signal-row fixture, fed a scaffold candidate mirroring `de595a44`'s exact shape through the REAL current `scripts/orch-apply.sh` → `[orch-apply] OK`, exit 0, backlog 320→0, signal_rows 100→1. Root cause: `Lane = z.array(TaskSchema)` has no `.min()`; 6 of 7 flat lanes are `.optional().default([])`; a document that omits them entirely still validates. **Interim "Telegram-only" mitigation LAPSED**: `docs/agents/system-auditor/flow/main.md` still mandates signal_queue writes unconditionally (4 sites, all "no skip path"); `docs/data/orch/archive/2026-07.json` shows 138 continuous auditor signal rows through 2026-07-08 — the mitigation was never durably wired into the flow doc and could not survive cron's unattended cadence. Ticket has been live-exploitable, not mitigated, for the full 2 weeks since filing.
**Design:** magnitude-bounded conservation circuit-breaker as new Stage-2 gate in `scripts/orch-apply.sh` (whole-board `task_total`/`signal_total` must retain ≥50% of live value — NOT per-lane never-decrease, which I proved would false-positive on normal backlog→ready→in_progress single-task moves AND break the 2 already-shipped legitimate bulk-eviction writers `scripts/orch-cold-evict.sh` + `pm/flow/task-archive.md`, both already routed through orch-apply.sh). Narrow named bypass (`ORCH_APPLY_ALLOW_SHRINK=<reason>`) for those 2 blessed callers only, mirroring the existing `ORCH_APPLY_LIVE_FILE_OVERRIDE` precedent. New shared `scripts/orch-conservation-check.mjs` (bun) invoked by both `orch-apply.sh` and the PreToolUse hook — single SSOT, no per-caller duplication, covers all ~290/tick writers not just the auditor. Verification gate extends the EXISTING `scripts/test/orch-apply-wrapper-tests.sh` harness (COLLAPSE/APPEND-HAPPY/SHRINK-ALLOWED cases), not a new file.
**Output:** `docs/architecture-briefs/2026-07-10-auditor-orchstate-conservation-guard.md` — full root-cause re-verification, empirical repro commands, rejected-design proof, verification-gate spec.
**Next:** po — recommend routing implementation to dev-mcp-server (same zone as SSOT-W1-ORCH-APPLY-WRAPPER's author); zero apps/mcp-server/src changes, zero container rebuild (host-side scripts only). No commit made by architect (PLAN-ONLY; router independently re-verifies + commits).

---

## Archive (pre-2026-07-10T15:56Z)

[Older cycles archived to git history: FIX-TASKLOCK-OWNER-SESSION-SERVER-SCOPED-DEFEATS-MUTEX (2026-07-10T15:45Z, PLAN-ONLY — stale, already fixed 2026-06-28 via CROSS-SESSION-MULTI-TEAM-ORCH TASK_1980, owner_client_session now sole ownership key), FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION (2026-07-10T10:00Z, SPRINT-S design — D1 financial_reports.id stability fix unblocks D2/D3, R-CRIT-1/2 must-fix risk flags), ARCH-DAILY-FOREIGN-FLOW-TABLE (2026-07-10T02:00Z, BOUNDED-1 pickup — additive `daily_foreign_flow` table + `daily_ohlcv_with_flow` VIEW to structurally close R-1 foreign-flow write-drop, 7-subtask PM atomization), FIX-BCTC-BANK-SCALAR-MAPPING (2026-07-10T00:20Z, LIGHT SPIKE — FR-8 confirmed gateway-blind, FR-9 re-scoped to Track-1 source_report_id carry-forward), SPIKE-GATEWAY-BLIND-CLI-HANDSHAKE (2026-07-08T04:45Z, server exonerated via live curl handshake, 3.5d client-side gateway-blind confirmed, FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE minted), FIX-DAILY-OHLCV-UNIT-CONTAM-LT1000-FPT-VHM Round 2 (2026-07-08T00:00Z, WRITER-H bypass of writeOhlcvBatch found active/re-contaminating, migrate to writeOhlcvBatch backfill strategy), FACTORY-INTERFACE-split-server-ts (2026-07-04T tick, server.ts 2527L 4-stage extraction plan S1-S4 smallest-safest-first), FIX-DRAINESC-SEVERITY-RECURRENCE-GATE (2026-07-04T01:07Z, GATE-A effective_severity + 2-tier GATE-B board-row-exists/signals_processed recurrence gate), SPIKE-BCTC-DISCOVER-PIPELINE-DEAD (2026-07-03T07:13Z, dead-stage pinned to Stage 3 bctcPdfPullJob overlap/mutex gap, isRunning guard fix, FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD emitted to backlog), SPIKE-BCTC-CTG-BS-REALDATA-ROOT (2026-07-03T07:10Z, 3-layer stacking-bug root cause — parser column-order reversal on bank forms + classifier bold-strip gap + section-vocabulary false-positive — FIX-A..E split), FIX-MCP-MEMORY-CODE-LEAK (2026-07-02T18:17Z, sawtooth-at-tight-cap corroborated live + initDatabase() no-guard hotspot found via source read + McpServer-per-POST rebuild stacking), TOKEN-ECONOMY-TICK-PREFLIGHT (2026-07-02T12:10Z, 3-WU split for cowork/dev-team/auditor tick preflight scripts + stateless-mode MCP verification), ARCH-DASH-CRON-RECHECK-TABLE (2026-07-02T06:50Z, cron-recheck-table SPLIT: job_name hybrid resolution + generic cadence algorithm + Layer-B double-count correction), ARCH-FIX-BCTC-BANK-SUMMARY-MAPPING (2026-07-01T18:20Z, AC-1 spike + bank-form-generic gap + W1-W5 split), BA-PREDICTION-EVIDENCE-REVIVAL (2026-07-01T07:05Z, evidence_type corrections + FR-2.2 VPS proxy 502 root cause + 2-hop split), OHLCV-UNIT-CONTAM-WHOLEROW-LT1000 (2026-06-30T20:45Z, per-ticker anchor repair migration + writer guard + sanity pass 4), FIX-TA-VNINDEX-BENCHMARK-ABSENT-RS (2026-06-30T19:11Z, VPS VNINDEX skip-guard root cause + TASK-VNINDEX-RS-A/B split), BA-IND-P1-MOMENTUM-FRONTEND, BA-IND-P1-MOMENTUM-RS, MARKET-INDICATOR-DEPTH-P0, HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING, FEAT-NEWS-DECISION-RESUME, FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT + 27 earlier cycles pre-2026-06-28.]
