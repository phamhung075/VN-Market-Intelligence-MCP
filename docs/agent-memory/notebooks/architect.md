# Architect — Notebook

**Last updated:** 2026-08-23 13:15 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-23T13:15Z — Router manual-dispatch batch: 4 ready[] rows (P1/P2/high, non-dev off-DRS-allowlist, router-direct dispatch)

**Task:** Router hand-dispatched 4 `ready[]` rows stuck behind ~470-row RLC backlog (non-dev `next_agent`, off the DRS allowlist, no picker will ever reach them): FIX-NEWSSCOUT-OFFHOURS-SELFCOMMIT-PROSE-RECIPE-INTERMITTENT, UC-ASL-P3, FIX-ORCHSTATE-MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER, FIX-USDVND-THRESHOLD-SSOT. Worked sequentially, one WIP at a time.
**Findings (one per row, full detail in each brief):**
- **news-scout/market-watcher off-hours self-commit:** 26-line prose recipe (`task_claim` mutex + git commit + BUG escalation) is not deterministically LLM-executable — same class `coverage-stamp.sh` already solved via `mcp-call.sh`. AC3/AC4 (stale header / uncommitted c273) deliberately NOT touched — live evidence, must not hand-commit.
- **UC-ASL-P3:** LIVE COLLISION — `FIX-AUDITOR-C04-PARSEDAT-RECENCY-PREDICATE` claimed by a peer dev-team session at 12:56:49Z TODAY, actively building the SAME target script with only `checks.c04` — added the missing `depends_on` edge. Resolved all 7 BA ARCH-RATIFY questions; overruled BA's SLA-Resolver suggestion for C-06 (wrong domain — reuse `vnTradingCalendar.ts`/`marketHours.ts` via `check-foreign-flow-freshness.sh`'s own `bun -e` idiom instead). Repointed 3 sibling predicate rows (C-06/C-11/C-12) so they don't independently edit `main.md` anymore.
- **FIX-ORCHSTATE-MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER:** decided PHASED enforcement (non-fatal REPORT stage first, mirrors `orchStateSchema.ts:658-661`'s own precedent; fatal promotion gated on 14 clean days) over jumping straight to a fatal `superRefine`. Predicate reused (not reinvented) from `bounded1-supervised-lane-report.sh`. Rejected auto-deriving `next_agent` from zone (would silently defeat the deliberate-dispatch signal the flags exist to create).
- **FIX-USDVND-THRESHOLD-SSOT:** the row's own "three live planes" framing was stale — full-repo grep found `currencySignal()` (plane 2, 25500) is DEAD CODE, zero callers (+3 sibling dead fns in the same file). Found an unreported double-counting bug: `cascadeEngine.ts` runs the old fixed `MACRO_ADJUSTMENTS` AND the newer σ-based `DYNAMIC_MACRO_MAP` additively on identical domains. Option (a) relative/σ-threshold is the SSOT decision — already shipped (`macroThresholds.ts`), not new design; fix = port the Go classifier onto the same formula + retire the TS static rule + delete dead code.
**Output:** 4 briefs in `docs/architecture-briefs/` (2026-08-23-newsscout-marketwatcher-offhours-selfcommit-mechanize.md, -uc-asl-p3-auditor-db-checks-freeze.md, -fix-orchstate-mint-flagged-row-no-handler.md, -fix-usdvnd-threshold-ssot.md). All 4 board rows updated via `orch-apply.sh` (`architect_design_complete`, `architect_handoff`, `next_agent` set: 3× pm, 1× developer), 0 net-new-growth prose-ceiling violations. Journal STEPs architect-S40..S43.
**Next:** pm picks up 3 of the 4 (news-scout mechanize, UC-ASL-P3, USDVND SSOT — each needs a multi-owner split); developer picks up the orch-validate.mjs REPORT stage directly. Flagged, not fixed: fleet-wide static/dynamic macro double-counting beyond USD/VND (oil/gold), and 3 dead `macroTools.ts` functions beyond `currencySignal` — candidate follow-ups for PO/PM.

## 2026-08-23T13:00Z — FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER (SPRINT-S ready[]→pm, ba-pipeline, zone apps/mcp-server/)

**Task:** BA spec (docs/handoffs/FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER-BA-spec.md) — 2 facet `<select>` (quarter, ticker) over the existing flat 257-doc `#doc-select` in bctc-inspector.html, pure client-side filter (no backend change), plus buildLabel()'s AC9 display fix in bctcInspectHandler.ts. BA characterized AC9 as a 2/257-row (HUT) type-coercion fix only.
**Findings:** Ran `buildLabel()` live (`node -e`) before designing — the "2/257" framing understates the bug. `period_type` already holds `'Q1'..'Q4'` on every row (BA's own FR-4 finding); `buildLabel()`'s stale comment assumed `'QUARTERLY'`/`'ANNUAL'` and unconditionally appends a 2nd `` Q${period_quarter}` `` token, so ALL 255 normal numeric rows already render `"VCB Q1 Q1 2025"` (duplicate), not just the 2 HUT string rows (`"HUT Q1 QQ1 2024"`). BA's literal FR-3b prescription (parseInt-only coercion) would NOT reach AC9's own stated target — it only turns `"QQ1"` into a 2nd `"Q1"`, still duplicated. Real fix: skip the appended quarter token whenever `period_type` is already `/^Q[1-4]$/`-shaped (new exported `normalizeQuarter()`). Also found `apps/mcp-server/src/__tests__/PI3-bctc-inspect.test.ts:361` (AC-14) hardcodes the CURRENT BUGGY `"VCB Q1 Q1 2025"` as expected — must be corrected to `"VCB Q1 2025"` in the same commit (in-scope test fix, not a regression to preserve). Confirmed zero domain/application/infra touch (BA's DDD map upheld). FR-7 selection-preservation designed via direct `select.value =` mutation (no synthetic `change` dispatch) — avoids the existing change-handler's unconditional PDF/OCR/table/MD refetch (lines 1146-1170). FR-8's `<option disabled>` needs `selected` too, or the closed `<select>` renders blank (HTML default-selection algorithm skips disabled-only option lists) — flagged for developer.
**Output:** `[Architect] Brownfield Findings` appended to docs/handoffs/FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER-BA-spec.md. Test strategy: extend PI3-bctc-inspect.test.ts (server-side `normalizeQuarter()` + corrected AC-14) + new FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER.test.ts (client-side mirrored pure functions, same convention as 1976-bctc-inspector-page-nav.test.ts). BUILD-STANDARD: lean. Board row moved backlog→ready via orch-apply.sh, owner=architect, next_agent=pm. Journal STEP architect-S39.
**Next:** pm — break into dev-mcp-server task(s): (1) bctcInspectHandler.ts buildLabel/normalizeQuarter fix + PI3-bctc-inspect.test.ts update, (2) bctc-inspector.html FR-1/2/4-8, (3) new client-side pure-function test file. Single zone, BA's own S-size framing still holds (same 2 files, no new-file scope growth from the AC9 correction) — PM's call on task count.

## 2026-08-23T10:05Z — FIX-SIGNAL-INBOX-NON-DRAINABLE-ENVELOPE-50-OF-51-FILES-SILENTLY-CLASSED-LITTER (P1 ready[]→developer, PO-mint, zone cross-service/, router-direct)

**Task:** PO measured `drainable_count=1` of 51 `docs/signals/*.json` files (`isDrainableShape()` skips any file with `from`/`source`/`type`/`signal_type` all null — never enveloped, never routed, never pruned, oldest 25 days). Hard constraint: do NOT blanket-widen the reader — `BY_PATH_CONSUMER_FAMILIES` proves some non-envelope files are legitimate by-path inputs, misdiagnosed as litter 4 times already. Router asked for a discriminator, measured against the live inbox.
**Findings:** Re-ran live: `drainable_count` moved 1→2 (inbox is live, 2 fresh enveloped files landed mid-triage). The 49 non-drainable are NOT one population: **21 are `price_anomaly_*` by-path (correctly exempt — do not touch, would be the 5th misdiagnosis)**, **26 are well-formed-but-unenveloped litter** (mostly historical `cowork-team-*` telemetry pre-dating a since-closed writer defect), and **2 are a THIRD failure mode PO's row didn't name — malformed JSON that never reaches `isDrainableShape()` at all**, from a live, currently-undetected bug (`notebook-auto-prune.sh:574-575`, `grep -c ... || echo 0` double-emits `"0\n0,"` whenever a count is zero, zero test coverage of the emitted JSON's validity). Also found: `price_anomaly_*` has no age ceiling or prune anywhere in the codebase — unbounded growth of a correctly-working population (oldest 52d), a different, lower-urgency gap than PO's ask.
**Output:** `docs/architecture-briefs/2026-08-23-signal-inbox-orphan-escalation-discriminator.md`. Design: age-bounded, one-shot, dedup'd escalation (new `signal_inbox_orphans` ledger table in the existing `signals.db` + a normal enveloped signal `drain-signals.js` emits and routes to PO itself) for the 26+2 only — `isDrainableShape()`/`isByPathConsumerFamilies()` left byte-for-byte unchanged, rejecting reader-widening. Two floors, both derived from existing fleet constants not new hardcodes: 30min (2x fastest cowork cron cadence) for malformed JSON, 7d (the drain's own existing DB/processed-prune cutoff) for orphan-shape. Board row `next_agent=developer` via `orch-apply.sh`, 0 net-new-growth violations. Journal STEP architect-S38.
**Next:** developer implements §3 (ledger table + escalation branches in `drain-signals.js`, spec-first in `drain-signals.md`, `mcp-tools.md` extension, `spawn-fanout.md` stale-citation fix). Flagged, not fixed here: `price_anomaly_*` unbounded growth (no prune anywhere) and the live `notebook-auto-prune.sh` grep bug (not in this row's `files[]` scope; related `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE` already tracks the drop-logic side of that file, not this JSON-corruption symptom) — both candidate follow-up rows for PO/PM.
