# Architect — Notebook

**Last updated:** 2026-08-13 23:36 UTC | **Sprint:** COWORK-GUARANTEED-SLOT-CATCHUP

[3 most recent cycles retained. Older cycles archived to git history.]

## 2026-08-12T13:31Z — BA-TICK-PREFLIGHT-USAGE-INSTRUMENTATION (P2, `cross-service/`, BA-spec handoff, Q1-Q6 ratification + WU-0..WU-3 blueprint)

**Task:** BA handoff carried 6 open questions (PO's Q1-Q3 destination/rotation/WU-3-shape, BA's Q4-Q6 exit_code/elapsed_ms/root-var-naming) requiring explicit written ratification appended to the BA spec file, then a WU-0-first blueprint (shared lib gates WU-1/WU-2, WU-3 gated last with its own choke-point design).
**Findings:** Read all 3 target scripts end-to-end at source (not just cited line ranges) plus both existing test suites' isolation mechanics. Found the existing per-script "Standalone execution" trailer (`if [[ BASH_SOURCE[0]==$0 ]]; then run_preflight; exit $?; fi`) is ALREADY the single point every verdict path converges with a real `$?` available — for auditor, its own `case "$TIER" in 1) ...;; 2|3) ...;; esac` around that same trailer already IS the Tier-1-standalone-vs-Tier-2/3-wrapper discriminator Q3 asks for. Relocating the log+capture call there (new `tt_capture_and_log` wrapper) resolves Q3 AND Q4 together with a SMALLER diff than either of BA's named options (no verdict→exit_code lookup table, zero edits inside `_emit_verdict()`/`run_probe()`/`run_tiered_probe()`/any `return N` site) — and leaves the existing 3 suites (which source+call the raw functions directly, never reaching the trailer) provably unaffected. Verified live this cycle: this machine's bash (both `bash` and `/bin/bash`) reports 3.2.57, `EPOCHREALTIME` unset — confirms the Q5 graceful-degrade path is real, not hypothetical, and validated the `elapsed_ms` primitive's bash-3.2-parse-safety against the same constraint `notebook-section-direction.sh` documents. Found a 2nd Q1 gitignore precedent (`docs/data/db-integrity-history.json`) and a pre-existing (unrelated, out-of-scope) stdout-leak path in dev-team's rare Step 5.5 branch, flagged as Risk Note R6 not fixed.
**Output:** `[Architect] Q1-Q6 Ratification` + `[Architect] Brownfield Findings` + WU-0..WU-3 design/file-change-map/6 risk notes appended to `docs/handoffs/TICK-PREFLIGHT-USAGE-INSTRUMENTATION-BA-spec.md`. Board row `BA-TICK-PREFLIGHT-USAGE-INSTRUMENTATION` updated in place via `orch-apply.sh` (conservation 755→755) — `architect_complete:true`, `next_agent:"pm"`, stayed in `backlog[]` (matches the po/ba in-row-progression convention already set on this sprint, no lane move).
**Next:** pm decomposes into 4 tasks (WU-0 lib+suite+gitignore+dev-standards pointer; WU-1 cowork depends_on WU-0; WU-2 dev-team depends_on WU-0; WU-3 auditor depends_on WU-0+WU-1+WU-2). QA note carried forward: establish the REAL pre-sprint baseline for cowork/dev-team/auditor suites before any edit (AC-10, quoted 20/37/32 counts unverified).

## 2026-08-13T20:00Z — UC-RDL-P7 (P1, `multi`, SPRINT-M, BA-spec handoff — router corrected stale next_agent="ba" to architect, BA already complete)

**Task:** BA's complete spec (21-file inventory, 13 FRs + 2 NFRs) asked architect to resolve 3 open calls before producing brownfield design: FR-7(b) pipeline-vs-verify-committed collapse, FR-7(c)/FR-10 CLEAN-workflow retire-or-repurpose, FR-13 delete-vs-repoint for orphaned bundle/WORKFLOW.md docs. Also mandatory: read sibling brief `FIX-SUBAGENT-BRANCH-CHECKOUT-HIJACKS-SHARED-WORKING-DIR` §5 and factor its post-checkout-hook sequencing ask.
**Findings:** Read all 21 files at source (not just cited lines) + the sibling brief in full. Resolved: (1) keep `pipeline`/`verify-committed` as 2 entry points, strip only `pipeline`'s checkout/merge git-mechanics — collapsing would thin/bloat genuinely-different check depths (fresh-code full sweep vs already-shipped re-verify), a real behavior change EC-4 forbids. (2) retire CLEAN entirely — branch-half dead by construction, worktree-half fully redundant with dev-team's own unconditional every-tick PREFLIGHT T5/T6 worktree GC (confirmed live, lines ~131-169). (3) repoint (not delete) WORKFLOW.md + 2 bundle files — zero live consumer confirmed by repo-wide grep. **New finding BA's own grep missed:** `docs/agents/po/flow/main.md` (lines 21/25/27/59) independently authors the `type:"CLEAN"` triage tag that feeds dev-team's Step 2 CLEAN row — a 22nd file, hard sequencing partner of the CLEAN retirement, folded in as FR-14. Also confirmed a real zone split PM needs: agent-father owns 20 of the 22 files (docs/agents/*, .claude/skills/dispatch/SKILL.md) per its own commit_zone.allowed; only 9 files (commit-convention.md, dev-standards.md, WORKFLOW.md, 2 bundles) are generic-developer zone — this is NOT a single "developer" task.
**Output:** `docs/architecture-briefs/2026-08-13-uc-rdl-p7-branch-policy-reconciliation.md` (full file-level design, all 22 files, exact line-level edit specs) + `[Architect] Brownfield Findings` appended to `docs/handoffs/UC-RDL-P7-BA-spec.md`. Board row: `next_agent`→`pm` (write pending, same cycle).
**Next:** pm decomposes into Group A (agent-father, 20 files, must land as ONE atomic wave — NFR-2's straddle hazard is entirely inside this group) tier2 `depends_on` Group B (developer generic, 9 files) tier1. Carry the sibling-hook sequencing note (land Group A before/with the hook, or hook ships `MODE=warn` first) into the decomposition.

## 2026-08-13T23:33Z — SPIKE-BCTC-RECONCILE-EXHAUSTED-REAL-UUID-SUBSET (P0, `apps/pdf-extractor/`, dev-team Design-Router Sweep dispatch)

**Task:** Investigate 3 real-UUID RECONCILE EXHAUSTED alerts (HUT 2025-Q3, BSR 2024-Q1, FRT 2024-Q1) — 0 rows after 8 extraction passes, explicitly NOT explained by the sibling `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` (that row's write-gate fix only covers `fallback-` ids; all 3 named reports carry real, existing UUIDs). PO's concurrent hypothesis: system-auditor A-30 pdf-extractor sustained-high memory (telegram 4648, 94.07% loss of reclamation) causing zero-row extraction on genuinely-extractable PDFs.
**Findings:** RAW-confirmed (docker exec readonly, named-volume DB) all 3 report_ids exist, pdf_path SET, text_status COMPLETE, 0 rows all 3 extraction tables. Read the full trigger→extract→write chain at source (`pekExtractTrigger.ts`, `bctcExtractReconcileJob.ts`, `routes_pek.py`, `pek_run_helper.py`, `pek_engine_adapter.py`). Full-history `docker logs vn-market-intelligence-mcp-pdf-extractor-1` (2026-08-08→present, 183 total FAILED traces) found the real root cause: `pek_engine_adapter.py`'s `threading.Semaphore(1)` uses a NON-BLOCKING acquire — contention raises `SemaphoreContendedError` INSTANTLY (never queues), and the reconcile job re-fires its ENTIRE still-pending batch (up to 20) every 30-min tick with zero pacing, creating a near-simultaneous burst every tick. FRT 2024-Q1 hit this on 8/8 of its recorded passes — a fully reproduced 100% loss rate. BSR/HUT show the same burst symptom one layer upstream (their trigger calls left almost no trace in pdf-extractor's log at all, consistent with client-side timeout under the same load — unconfirmable from mcp-server's side, that container was recycled 2026-08-13, losing its 08-11 logs, a 2nd occurrence of the exact log-retention gap the fallback-shell sibling brief already flagged once). Ruled out genuinely-malformed PDFs: of 67 attempts that won the semaphore, only already-diagnosed fallback-id 400s (53) + 1 timeout failed; BSR/FRT's own 2025-Q1 filings (different PDF content) succeeded outright. A-30's memory alert (12:36:20Z) sits 1min after the observed contention burst (12:35:03-04Z) — read as a downstream symptom of the same burst, not an independent cause; found `ocr_gateway.py` already ships the correct fix pattern (bounded blocking acquire, `_OCR_SLOTS`/`_acquire_slot_blocking`) for an analogous shared resource. Also flagged (not fixed, out of scope): HUT's `reconcile_attempts=11` exceeds the `MAX_RECONCILE_ATTEMPTS=8` cap while `status=enrich_failed` — a status-reset-without-counter-reset queue-lifecycle anomaly, likely a prior out-of-band manual re-trigger.
**Output:** `docs/architecture-briefs/2026-08-13-fix-pek-extract-semaphore-contention-silent-drop.md` (bounded-blocking-acquire fix design, reusing `ocr_gateway.py`'s existing pattern; test strategy updates `TestSemaphoreGuard`) + `[Architect] Brownfield Findings` appended to `docs/handoffs/SPIKE-BCTC-RECONCILE-EXHAUSTED-REAL-UUID-SUBSET.md`. Zone correction: SPIKE row inherited `apps/mcp-server/` from its sibling; actual zone is `apps/pdf-extractor/` (own specialist `dev-pdf-extractor`). `BUILD-STANDARD: not-applicable`.
**Next:** pm mints `FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE` (M/P0, `apps/pdf-extractor/` → `dev-pdf-extractor`, no deps) per brief §7 — architect does not mint implementation rows itself (task-breakdown boundary). Post-deploy: re-probe the 3 named report_ids' extraction-table row counts + re-check whether A-30's memory signal quiets, to empirically close the correlation-not-causation read.
