# QA — Notebook

## cycle-393 · 2026-07-08 · FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE — DONE_VERIFIED (independent re-verify of a jq eligibility-gate fix, no code/tsc/bun-test surface)

Dev-team dispatched this task_board.review row directly (no TASK_NNN handoff branch — a script+doc fix flowing through orch-state.json like the recent FACTORY-* board-driven tasks). Did NOT rubber-stamp the developer's stated "17/17" + "manually sanity-checked" dry-run claim. Read the full diff of `scripts/devteam-backlog-promote-bounded1.jq` at commit `70bb222f6` myself: confirmed the new `select(.value | deps_satisfied($detail_items; $status_map))` runs INSIDE the candidate list-comprehension, BEFORE `sort_by([.rank, .idx])` — genuinely filters during candidate selection, not a post-hoc check on the already-chosen top pick (this matters: a post-hoc-only filter would starve eligible lower-ranked rows behind a blocked top row). Confirmed `effective_depends_on` resolves BOTH production shapes: inline `.depends_on` on the board row, and — for `detail_ref`'d rows — the lookup in `docs/data/orch/archive/backlog-detail.json` `.items[<id>].depends_on`, normalized via `as_dep_array` (handles the 7/321 bare-string real-data-drift rows, verified by reading the test's Bonus case). Confirmed `deps_satisfied` requires `status == "DONE_VERIFIED"` in `dep_status_map` (built by scanning ALL 7 task_board lanes) — plain `DONE` is explicitly proven insufficient by a dedicated test case — and a dep id resolving in NO lane maps to sentinel `"MISSING"` (never equals `DONE_VERIFIED`) → conservative-skip, not silently ignored. Ran `bash scripts/test-devteam-bounded1-depends-on.sh` myself directly (not trusted from the developer's stated count): 17/17 PASS, exact match. Went further: ran my OWN independent read-only dry-run against LIVE `docs/data/orch/orch-state.json` + LIVE `docs/data/orch/archive/backlog-detail.json` (`jq --slurpfile detail ... -f scripts/devteam-backlog-promote-bounded1.jq` piped to a scratch file, NEVER through `orch-apply.sh`) and confirmed the fixed script now SKIPS `FACTORY-TECHANALYSIS-delete-orphaned-ts-service` (both declared prerequisites still plain `BACKLOG`) and promotes `FACTORY-TECHANALYSIS-go-livepath-tests` instead (`depends_on: []`) — reproduces the fix's stated claim exactly on real live data, not just the hermetic fixtures. Grep-confirmed `--slurpfile detail docs/data/orch/archive/backlog-detail.json` genuinely threaded through BOTH invocation call sites (`docs/agents/dev-team/flow/main.md` BOUNDED-1 block + tools-list pointer, and `docs/policies/dev-standards.md` canonical pointer). `git diff 1fe8e6a40..3a006db69 -- scripts/devteam-backlog-claim-bounded1.jq` = empty — genuinely unchanged; re-read the claim script directly and confirmed it structurally needs no depends_on logic of its own (only claims whichever `ready[]` row carries `promoted_by == "dev-team (bounded-1 auto-pickup)"`, i.e. whatever promote already gated). Confirmed DJ-GATE-1: `sprint-SYSTEMIC-REMAKE-P1-developer.md` STEP developer-S5 present and substantive (not boilerplate). Scope check: `git diff --stat 1fe8e6a40..3a006db69` touches only the expected files; the `FACTORY-TECHANALYSIS-delete-orphaned-ts-service` `revert_note` diff visible bundled inside commit `3a006db69` was traced by its own timestamp (`07:55:12Z`) to PREDATE this task's `created_at` (`08:06:09Z`) — a pre-existing manual revert (dirty-tree state) swept into the same later commit as the routine board-flip write, not new scope creep introduced by this task. Confirmed `FACTORY-TECHANALYSIS-go-livepath-tests` is STILL plain `BACKLOG` in the live board right now — nothing was actually auto-promoted; the fix only corrects future BOUNDED-1 auto-pickup ticks, per PO's explicit decline of manual promotion this cycle. This task touches only `.jq`/`.md`/memory files — no TS production source, so `bun test`/`bun tsc --noEmit`/DDD/mock-guard are N/A (Smart-Skip); `git push origin main`'s pre-push `pnpm --filter vn-market check` (tsc) still ran clean as a sanity byproduct. VERDICT: all 9 verification points independently PASS. Flipped `FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE` `REVIEW` → `DONE_VERIFIED` via new `scripts/qa-fix-devteam-bounded1-depends-on-gate-done-verified.jq` + `orch-apply.sh` (array-move `review[]`→`done_verified[]`, guarded on `.head.active_task_id`). `.head` idle-reset (`status:"done"`, `next_agent:"router"`, `active_task_id:null`). Lock release: no gateway MCP tool access in this specialist sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — the dispatching dev-team router session (`5a45feda-431e-46c8-941d-a6539a0eca77`) releases `intent:dev-team:*`/any outer lock on my behalf per its own stated fallback; the board-flip write itself (plain bash/jq through `orch-apply.sh`) was performed directly since it needs no MCP binding. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S14.

## cycle-392 · 2026-07-08 · FACTORY-MACRO-delete-dead-ts-tree — DONE_VERIFIED (independent RAW-verify of a pure-deletion FACTORY task, round-2/po-fold-in)

Router had already independently re-verified round 1 (BLOCK, 9 live `__tests__` importers found) and round 2 (deletion executed after po's FOLD-IN scope expansion) — this is the final qa gate before DONE_VERIFIED. Did NOT trust either report: re-confirmed all 7 DoD paths gone from disk; git-blamed the pre-delete tag `macro-pre-delete` to prove `src/`'s collapse to fully-empty was structurally inevitable (it held ONLY `_deprecated/` + `infrastructure/scrapers/` at the round-2 parent commit — `application/`/`domain/`/`interface/`/`index.ts` were already gone from an earlier, unrelated TS→Go migration, not over-deleted by this task). Ran uncached `go build/vet/test -count=1 ./...` myself: 33 `_test.go` files, **11** packages (6 primitive + domain + application + infrastructure + interface/http + module), 288 top-level tests, 543 incl. subtests, 0 fail. `golangci-lint run --config .golangci.yml` = 0 issues. G12 sandbox: primitive 18/18 PASS, module 2/2 PASS. Fence-A/B/C re-verified via real `go list -f '{{.Imports}}'` import graph (not the grep heuristic) = CLEAN on all 3. Env-credential audit (`DB_`/`API_KEY`/`SECRET`/`TOKEN`/`PASSWORD`) = clean, all legit config reads. Whole-repo grep for live importers of any deleted path outside the deletion set = 0 (2 hits excluded as pre-existing stale doc-comments — `apps/mcp-server/.../clients.ts:571` git-blamed 2026-05-16, `macro_investment_clock.go:5` git-blamed 2026-05-23, both pre-dating this task by ~7 weeks; `infrastructure/scrapers` hits elsewhere all belong to the unrelated `apps/news-fetch` service). Confirmed the 3 `pkg/primitive/*.go` doc-comment diffs are comment-only (zero behavior change) by reading the actual diff. `pnpm -r list` confirms macro-indicators silently drops out of the workspace (`apps/*` is a directory glob, not an enumerated list); root `pnpm-lock.yaml` never had a macro-indicators entry (it used its own now-deleted `bun.lock`) — no other file needed changing. CI bun-test job is scoped to `apps/mcp-server` only (`ci.yml:19`); macro-indicators CI (`ci.yml:85-94`) is golangci-lint-only — unaffected by `package.json` removal. **testing.md spot-check found one real inaccuracy**: summary line claimed "8 packages" but the true count (verified via `go list` enumeration of dirs containing `*_test.go`) is 11 — all other numbers (33/288/543/0-fail) reproduced exactly via a fresh `go test -v -count=1`. Fixed this directly (doc-only, non-production, non-fabricated-data correction — the underlying test evidence was 100% real, just one aggregate mistallied) rather than bouncing back to dev for a 1-line miscount, consistent with the systemic-review directive to reduce churn without convergence. No-cache `docker build` of the Dockerfile succeeded clean (temp images removed after); confirmed the live prod container `vn-market-intelligence-mcp-macro-indicators-1` was untouched throughout (still `Up 15h (healthy)`, unrebuilt) — `rebuild_required=false` honored, no deploy/swap performed. All 5 task commits (`daaa1774f`, `33c42ecb8`, `39be5019a`, `02114f582`, `fb62e138e`) confirmed present with matching content. VERDICT: all DoD clauses independently RAW-confirmed. Flipped `FACTORY-MACRO-delete-dead-ts-tree` `REVIEW` → `DONE_VERIFIED` via `scripts/qa-factory-macro-delete-dead-ts-tree-done-verified.jq` + `orch-apply.sh` (array-move `review[]`→`done_verified[]`, guarded on `.head.active_task_id`). `.head` idle-reset (`status:"done"`, `next_agent:"router"`, `active_task_id:null`) — kept in sync with the board row in the SAME transform. Follow-up correctly left out of scope (flagged by dev, not touched): `infrastructure.md`/`domain-model.md`/`usecases.md`/`api-reference.md` still describe the deleted TS layer and need a Go-real rewrite — recommend a follow-up backlog item for po/architect. Lock release: no gateway MCP tool access in this specialist sub-session (INV-GATEWAY-1) — router releases `task:FACTORY-MACRO-delete-dead-ts-tree` on my behalf. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S13.

## cycle-391 · 2026-07-08 · FACTORY-PDF-paddleocr-score-07-mask — DONE_VERIFIED (live-endpoint RAW-verify, docker exec into deployed image)

Router had already independently re-verified the diff + re-ran tests, and ops had already rebuilt+swapped the pdf-extractor container (image `0c83b2c5f8ad`) — but neither is a substitute for the standing gate: actually exercising the DEPLOYED code. Confirmed image identity myself first: `docker inspect vn-market-intelligence-mcp-pdf-extractor-1` → `sha256:0c83b2c5f8ad...`, exact match, container `Up (healthy)`. Confirmed the deployed file is genuinely the fix, not a stale/bind-mounted worktree: `sha256sum /app/infrastructure/pek_engine_adapter.py` inside the container == local fixed source, byte-identical. A real end-to-end PDF-triggered call would never actually exercise the missing-score branch (PaddleOCR's normal PP-StructureV2 output always carries `score`; the null-guard is defensive against malformed/partial cells) — used the task's explicit fallback: `docker exec -i` into the container's own live `python3` process, imported the REAL deployed module (`mod.__file__` confirmed = `/app/infrastructure/...`, not local tree), and directly invoked both `_cells_to_row_bands()` and the full `_map_bboxes_to_zones()` wiring path with a 3-cell fixture (present score 0.88, score key ABSENT, explicit real 0.0). Both call sites returned `row_density` = `[0.88, null, 0.0]` exactly — missing-score cell emits JSON `null` (never the old fabricated `0.7`), explicit `0.0` correctly preserved as a real measurement (not coerced to missing), present score unchanged. Bonus: `curl localhost:5001/health` confirmed genuine live HTTP serving (`{"status":"ok","service":"pdf-extractor","ocr_source_ok":true}`), not just a running process. VERDICT: fix genuinely deployed and working, no regressions. Flipped `FACTORY-PDF-paddleocr-score-07-mask` `REVIEW` → `DONE_VERIFIED` via `scripts/qa-factory-pdf-paddleocr-score-07-mask-done-verified.jq` + `orch-apply.sh` (array-move `review[]`→`done_verified[]`, guarded on `.head.active_task_id`). `.head` idle-reset (`status:"done"`, `next_agent:"router"`, `active_task_id:null`). Flagged separately: ops's own decision-journal entry for this identical task_id was mis-filed under `sprint-DOCLANG-SERIALIZE-ops.md` ("no goal set" header) instead of the correctly-resolved `SYSTEMIC-REMAKE-P1` (matching dev-pdf-extractor's own entry and the documented `tail -1`-of-active-entries mechanism) — a sprint-resolution bug on ops's side, not a scope dispute. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S12.

**Retention note:** cycle-390 (FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED — DONE_VERIFIED, independent re-verification + mutation test, 79/79) and older pruned this write per AC-2 (keep last 3). Full detail preserved in `reports/TASK_REPORT_FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED.md` and prior git history of this file (which also carries cycle-389: F1-LAUNCHD-COWORK-BACKSTOP DONE_VERIFIED / cycle-388: FACTORY-INTERFACE-source-confidence-10-mask DONE_VERIFIED / cycle-387: FACTORY-INTERFACE-source-confidence-10-mask QA-APPROVED HELD REVIEW / cycle-386: FACTORY-INTERFACE-sequential-confidence-05-mask DONE_VERIFIED / cycle-385: FACTORY-INTERFACE-sequential-confidence-05-mask QA-APPROVED HELD REVIEW / cycle-384: CONTAM-10-WRITER-H DONE_VERIFIED / cycle-383: CONTAM-10-WRITER-H HELD REVIEW / cycle-382: PDF-TEST-01-FIX HELD/BLOCKED / cycle-381: FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP APPROVED / cycle-380: KD-OBS-01-FIX APPROVED / cycle-379: CI-RED-c5b5f885-FIX APPROVED / cycle-378: FACTORY-ALERT-consolidate-dual-engines APPROVED / cycle-377: B-05-FU-SSC-503-RETRY PASS / cycle-376: FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK APPROVED / cycle-375: FIX-ALERT-COMMANDER-DEAD-NO-SLOT PASS / cycle-374: FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD PASS / cycle-373: FIX-FB-DAILY-FIRER-CLI-FLAG APPROVED / cycle-372: FIX-BCTC-BANK-BS-COLUMN-ORDER PASS / cycle-371: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
