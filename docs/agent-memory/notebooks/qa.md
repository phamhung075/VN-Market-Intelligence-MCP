# QA — Notebook

## cycle-407 · 2026-07-09 · FACTORY-SCHEDULER-job-table-registry — Step 5 FUNCTIONAL PASS, but ops Step 4 SHA-gate genuinely FAILED — bounced to ops (not benign)

Formal Step 5 after dev-mcp-server split 79 `scheduleCron` blocks into `schedulerJobTable.ts` (57 generic-loop + 22 bespoke via `registerBespokeJobs`) + `walEscalation.ts` extraction (commit a25bdc617), ops claimed Steps 1-4 done. Gateway-blind this session (no `mcp__gateway__*` bound) — all verification below is direct docker/git/sqlite/curl, no MCP mediation needed.

Independent live proof of execution (not just registration), via `cron_job_runs` table: `askQueueCheckJob` (bespoke, non-generic-loop) fired+succeeded 2x post-restart (00:12:00/01); generic-loop jobs (vpsServiceHealthJob "polled=5 stored=5", intelligenceCycleJob, restartCadenceAlertJob, bctcQueueEnricherJob, deepFetchVps/MainJob) fired+succeeded at 00:15:00-01. 18/18 runs since restart = success, 0 errors. Re-ran (not trusted) on current HEAD: tsc 0 errors, eslint clean (3 files), targeted test 15/15, all 6 touched test files 53/53, and re-ran dev's own equivalence script myself — reproduced the identical 1 flagged mismatch (`JOB_NAME_BREADTH_PERSISTER`, a non-literal identifier the parser can't resolve) and independently traced it to source: same imported const used old+new → 79/79 confirmed. No regression: `/health` toolCount=183, `/api/cron-status` 200 (85 keys), dashboards 200, docker-exec-cat vs `git show a25bdc617` byte-identical for all 3 touched scheduler files, 11/11 peer services healthy (only mcp-server restarted). WAL now 4.0MB (grew naturally since ops's 1.2MB reading, normal sawtooth pre-next-checkpoint), well under 10MB — `wal-escalation-1783551601546` (29.8MB origin) confirmed resolved.

BLOCKING (routed to ops, not developer — zero code defect): `bash scripts/verify-deploy-sha.sh mcp-server` exits 1 live — deployed `vn.market.git_sha` label = literal `"unknown"`. Unlike cycle-406/404's benign HEAD-drift false-positives, re-confirmed this against the explicit code-commit SHA too (same fail either way) — the label was genuinely never set. Root cause: ops's own notebook entry for this task ran `docker compose build mcp-server` with no `--build-arg GIT_SHA=...`, unlike every other entry in the same file. Runbook: non-zero SHA-gate = deploy BLOCKED, do not declare complete. Remediation is mechanical (code already proven byte-identical) — ops rebuild with the flag + re-run the gate, then straight to po (no re-qa needed). Board: `task_board.review[]` row `status:"REVIEW"`, `next_agent:"ops"`, `rebuild_required:true`, `qa_verified_at/by` set via jq+`orch-apply.sh`; `.head` synced. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S28.

## cycle-406 · 2026-07-08 · FACTORY-INFRA-split-telegramCommands — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (Steps 1-4, deployed SHA e92b08310, commit 93c063c70). dev-mcp-server split `telegramCommands.ts` (1071L→267L) into `telegram/{format,commandHandlers,newsHandler,recapRenderer}.ts` + `infrastructure/db/{watchlistReadStore,systemHealthStore,agentFeedbackStore}.ts`, and fixed a real infra→application layering violation: `/recap*`'s fetch step (was `telegramCommands.ts` importing `assembleEveningSummary`/`generatePeriodicSummary` directly) moved to new `application/usecases/orchestrateRecapCommand.ts`, invoked from `interface/mcp/routes/webhookHandler.ts` via a new `RecapResolvers` DI param.

Deploy-integrity spot-check (own, not blind trust): docker-exec+shasum on all 10 touched/created files — byte-identical to git HEAD in the live container. `scripts/verify-deploy-sha.sh` showed drift (deployed=e92b08310 vs HEAD=93c063c70) — re-derived as BENIGN: `git diff --stat e92b08310..93c063c70` touches only `docs/agent-memory/notebooks/ops.md` + `docs/data/orch/orch-state.json` (ops's own Step 1-4 board-write commit, 0 app files).

RAW-verify `/recap`/`/recapw`/`/recapm`: reproduced the exact production wiring in-process inside the live container (`handleTelegramCommand` → `RecapResolvers` identical to `webhookHandler.ts`'s `RECAP_RESOLVERS` → `orchestrateEveningRecap`/`Weekly`/`Monthly` → `assembleEveningSummary`/`generatePeriodicSummary` → `recapRenderer`) against the live named-volume `market.db` (readonly handle — deliberate precaution against mutating production state from a synthetic chat id), with call-trace flags proving all 3 resolvers actually fired (not the degraded error-text fallback). Cross-checked rendered numbers against independent direct DB queries — all exact matches: VN-Index close 1853.7/+0.31% → "1.854 điểm (tăng 6 điểm, tăng 0,31%)"; VCB 61.600 VND/+0.49%/vol 205.100; watchlist count 41; FPT open position (5000 CP @ 80.300, now 72.100) → rendered loss 41.000.000 đ/10,21% matches `(72100-80300)*5000` exactly. Top recap headline independently found in `rag_analyses` (published same-day, sentiment=bullish) — confirms real live data, not fabricated. Two non-blocking artifacts of my own readonly test handle (not code defects): `generatePeriodicSummary`'s pre-existing `market_summaries` upsert failed "readonly database" for week/month (production's r/w handle would succeed; rendered output unaffected); `assembleEveningSummary`'s real `reports/<date>-evening.json` write fired for real (gitignored path, matches actual production behavior, no repo pollution).

Non-recap spot-check (6, exceeding the 2-3 ask): `/watchlist`/`/health`/`/price VCB`/`/news`/`/check_position`/`/help` — all correct; router-dispatch output byte-identical to calling the underlying handler functions directly; numeric ones match live DB exactly (same figures as the recap cross-check).

Layering-fix runtime trace (stronger than dev-mcp-server's pre-merge static grep): `bun build --target=bun` — Bun's real module resolver — on `telegramCommands.ts` inside the container: 31-module transitive bundle, **0** occurrences of `assembleEveningSummary`/`generatePeriodicSummary`/`application/usecases` anywhere. Same for `webhookHandler.ts` (interface layer, full app graph, 1217 modules): 25 occurrences — confirms the usecase IS correctly wired there. Confirmed `handleTelegramCommand` has exactly ONE caller codebase-wide. One PRE-EXISTING unrelated infra→application import found elsewhere (`telegram.ts`→`getPatternSummary.js`, the alerts notifier, untouched by this task) — out of scope, not a regression. Also confirmed (git-diff against pre-split `e9b16e850^`) a cosmetic `+`-prefix quirk on the portfolio-P/L line ("lỗ +10,21%") is pre-existing, not introduced by this refactor — flagged, not filed.

VERDICT: PASS. Per runbook delegation rule (ops Steps 1-4 / qa Step 5 / po Step 6) and standing local precedent (qa-S24/S26) — did NOT flip to `DONE_VERIFIED`. Added `qa_verified_at`/`qa_verified_by`/`qa_verify_note` fields to the existing `task_board.review[]` row (did not overwrite dev-mcp-server's `review_note` or ops's `ops_close_gate_note`), moved `next_agent` qa→po via `jq`+`scripts/orch-apply.sh`; `.head.next_agent`/`.next_action` synced to po in the same write (confirmed `.head.active_task_id` was still this task before writing). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S28 (pending link).

## cycle-405 · 2026-07-08 · FIX-COWORK-STEP5-BACKSTOP-TRUSTS-STALE-TRIGGER-STATUS — standard review PASS, DONE_VERIFIED

Standard QA review (NOT Docker Close Gate — `rebuild_required:false`, doc+JSON only) of agents-architect's brief (`docs/architecture-briefs/2026-07-08-cowork-step5-stale-trigger-status.md`, commit `8a627771f`) + agent-father's implementation (commit `287b181ee`) re-keying `spawn-fanout.md` Step 5.0's `BACKSTOP_SLOTS`/`NO_BACKSTOP_SLOTS` discriminator from stale `trigger_status=="active"` to live-maintained `_superseded_by==null`, plus splitting `cowork-schedule.json`'s 9 stale `trigger_status:"active"` rows into 2 classes (5 real-trigger slots → `"superseded"`; 4 never-had-one slots → field removed). Both commits already RAW-verified by the dispatching router; ran an independent review rather than rubber-stamping.

Proved the partition claim logically rather than just spot-checking: `NO_BACKSTOP_SLOTS`'s condition (`trigger_id==null OR _superseded_by!=null`) is the exact De Morgan negation of `BACKSTOP_SLOTS`'s (`trigger_id!=null AND _superseded_by==null`) — exhaustive/mutually-exclusive by construction, including slots that never carry a `_superseded_by` key (jq treats a missing key as `null`, but those slots also carry `trigger_id:null`, so they land in `NO_BACKSTOP_SLOTS` via that arm regardless — checked concretely against `digest-daily`/`bctc-analyst-slot-*`/`refine-bctc-slot-*`). `git grep -n trigger_status` swept the ENTIRE tracked tree, not just `docs/agents/cowork-team/` (per the task's own instruction to check any other consumer of `cowork-schedule.json`) — ~90 hits individually triaged: only `spawn-fanout.md`'s own new deprecation comment lives inside cowork-team/; `cowork-master-cron-runbook.md`'s jq display examples already carried pre-existing prose stating the field "no longer drives any live behavior"; `scripts/router-cowork-backstop-trigger-writeback.jq` WRITES `trigger_status="active"` but is an unreferenced historical one-off tool (not wired into any cron/flow file) — a writer not a reader, doesn't contradict the "only live consumer" claim; `docs/handoffs/TASK_1951d.md`'s `cowork_schedule` SQL-table reference was verified (not assumed) to be dead planning-doc text — that table was never actually built, the JSON file has always been the sole SSOT. All remaining hits are historical briefs/journals/notebooks/signals/archives — prose, not executable.

Reconciliation checks done via direct commit diffs, not narrative: `git show 05a8bffa6 -- cowork-schedule.json` confirmed that commit touched only `._notes.layer_a_deletion_locked` + gate text + unrelated `last_fired` bumps; `git show 287b181ee -- cowork-schedule.json` confirmed this fix's diff touches ONLY the 9 `.slots[].trigger_status` fields, zero `._notes.*` — provably disjoint. `F1-CLOUD-TRIGGER-DECOMMISSION` board row confirmed still plain `BACKLOG` on its own untouched 2-launchd-fires-per-slot gate. `jq empty cowork-schedule.json` clean; re-ran all 4 brief §5 DoD jq checks raw myself, matching results. Both commits confirmed ancestors of `origin/main`.

VERDICT: PASS, no blocking issues. Closed `task_board.review[]`→`task_board.done_verified[]` directly (status `DONE_VERIFIED`, `next_agent:"router"`) via new `scripts/qa-fix-cowork-step5-backstop-stale-trigger-status-done-verified.jq` + `scripts/orch-apply.sh` — standard doc+JSON FIX close per dispatch instruction, no Docker Close Gate/Step-6 po handoff applicable. Board-only move, deliberately did NOT touch top-level `.head` (correctly tracks the unrelated in-progress `FACTORY-INFRA-split-telegramCommands`, confirmed live before and after this write). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S27.

**Retention note:** cycle-404 (FACTORY-INFRA-split-vnstockBridge) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file.
