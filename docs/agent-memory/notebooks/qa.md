# QA — Notebook

## cycle-498 · 2026-07-29 · UC-GCP-P7 — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row stranded 6 days, /commit skill rescope)

Row `qa[]`, `branch:null`. Commits `a5202512c` (fix) + `2cd532595` (memory) both `git merge-base --is-ancestor main` confirmed, dated 2026-07-23 matching `completed_at`; `git show --stat a5202512c` touches exactly `.claude/skills/commit/SKILL.md` + `.claude/commands/commit.md` as claimed. Read CURRENT content of both files (not the diff alone) and checked all 5 RESCOPE acceptance items against it: Step 4 merge-and-clean-branch deleted (zero residual grep hits for `Step 4`/`git branch -d`/`git checkout main`); Step 2 rewritten to one `commit-mutex:main` acquire/critical-section/release per category, pointing at `commit-mutex/SKILL.md` Step 3d-PUSH (grep-confirmed live, TTL=90s); Step 1 stranded-peer-file mtime<2h age guard against `commit-boundary/SKILL.md` § RULE 2 (grep-confirmed the cited zone table exists); hardcoded `Co-Authored-By: Claude Sonnet 4.6` line confirmed deleted, replaced with a pointer to `commit-convention.md` as trailer SSOT (that doc's own `## Trailers` section independently confirmed to carry no hardcoded model-name trailer, consistent with the pointer); `.claude/commands/commit.md` confirmed exactly 1 line, and grep-confirmed it is the ONLY file repo-wide carrying `trigger: /commit`. Developer's own DJ entry (`sprint-ULTRACODE-AUDIT-FIXALL-developer.md` STEP developer-S22, `task-id: UC-GCP-P7`) cross-checked present. Docs/skill-only change — `bun test`/`tsc`/mock-guard structurally N/A (Smart-Skip).

Non-blocking out-of-scope finding: `docs/references/bundles/bundle-developer.md` § Commit Format still carries its own separate hardcoded `Co-Authored-By: Claude Opus 4.6` line + numeric `task(NNN)` format + branch-merge steps — a pre-existing, unrelated developer-agent direct-commit lazy-load bundle (not the `/commit` skill, not named in this row's scope note per INV-GATEWAY-1: specialist sub-agents don't invoke `/commit`). Flagged for PO triage, not folded into this row's verdict.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (task_total 670→671, qa-lane move nets +1 by design). `.head.active_task_id` was `UC-GCP-P7` — synced to idle in the same write. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S20.

## cycle-499 · 2026-07-31 · FIX-MCP-SERVER-DEPLOY-LANE-STALL-REBUILD-REQUIRED-INERT — DONE_VERIFIED (verify-then-close, `in_progress[]` row, no branch/handoff; genuinely-stale claim confirmed and fixed via rebuild, not another false-negative)

Row `in_progress[]`, no branch. Third round on this row: 1st cited proof was falsified (wrong container path, PO-corrected), 2nd instance (commit `314e70718`, non-ticker liveness-guard exclusion) needed independent re-check. Found the correct in-container path myself first (`/app/src/scheduler/alerts/signalOutcomeResolutionJob.ts`, not `apps/mcp-server/src/...`) — process is `bun run src/index.ts` running straight from `/app/src`, no build/dist step, so grepping that file IS runtime truth, not readiness. Pre-rebuild it lacked the `AND stock_code GLOB '[A-Z][A-Z][A-Z]'` clause; corroborated live: `signal_outcomes` had exactly 2 unresolved >72h rows, both `MACRO`/`MULTI` — the exact false-positive shape the fix targets, matching unresolved telegram 4236/4237. Genuinely stale, not a repeat false alarm.

Rebuilt mcp-server ONLY: `docker compose build mcp-server && docker compose up -d --no-deps mcp-server`, run from the MAIN CHECKOUT (not this qa worktree — the worktree resolves a different compose project name and would bind-mount its own `docs/data`/`data/live` over production's; confirmed via `docker compose ps --format json` that only the main checkout resolves `.Project=vn-market-intelligence-mcp`). All 12 peer containers' `CreatedAt` unchanged before/after — `--no-deps` held. New container healthy in 8s; GLOB clause confirmed present post-rebuild at the runtime plane. Re-derived the `rebuild_required` count at source (jq over live board, not inherited): 11 rows, 9 `apps/mcp-server/` + 2 other services. Spot-checked 3 of the 9 mcp-server rows' claimed markers directly in the fresh container (target fix + `FIX-VPSHEALTH-DEMANDROUTE-EMPTYQUEUE`'s `getDemandQueueDepth`/idle-no-work branch + `FIX-FOREIGN-FLOW-COVERAGE`'s `handleOhlcvCodes`) — all 3 present, strongly suggesting the rebuild also resolved the other 6 mcp-server rows (not individually closed here — flagged fast-follow, each still needs its own qa runtime verify).

`pressure-state.json` baseline_pass clause 1 (`container_vm_headroom_mb` numeric) reconfirmed live: 4407. Clause 2 (`degraded:false`): that field lives in cowork-team's own tick telemetry payload, not in pressure-state.json itself (9-key schema, no `degraded` key) — last emission (2026-07-30T17:37:20Z) read `false`, headroom 2053 then, now 4407 (well above the 1500 floor), so the mechanism is healthy — but every sampled 2026-07-31 tick (17 files, incl. a genuine 4-slot fan-out) omits the field entirely, a separate cowork-team narration-fidelity gap, not this row's defect. Recommended PO mint that as its own row rather than folding it in here.

VERDICT: DONE_VERIFIED. Board row moved `task_board.in_progress[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (task_total 744→744, pure lane-move). `.head.active_task_id` was the sibling `FIX-COWORK-SPAWNFANOUT-...` task, not this one — left `.head` untouched per instruction. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa.md` §qa-S10.

## cycle-500 · 2026-08-01 · FIX-DEVTEAM-QADRAIN-HEAD-WRITE-CONDITIONAL — APPROVED, DONE_VERIFIED (direct-commit verify, `review[]` row, PO Step-2 S4 UNBLOCK direct-dispatch — this row IS the QA-Drain picker's own fix, run outside the picker to avoid circularity)

Row `review[]`, no `branch` field (direct-execute path). Commit `9fe706fa2` confirmed on main ancestry (`git merge-base --is-ancestor`). `git show --stat` matches `.files[]` exactly (`scripts/devteam-review-claim-qa-drain.jq`, `scripts/audits/devteam-dispatch-gate-satisfiability.sh`), zero foreign files. Read the full diff directly rather than trusting the dev's review_note prose: the `.head` write in the `review[]->qa[]` claim branch is now wrapped in `if $head_free then {...} else .head end`, `$head_free = ($hs=="idle" or $hs=="done" or $ha==null)` — mirrors `devteam-wrapper-autoclose.jq:122-128`'s proven guard shape; `review[]->qa[]` lane-move logic itself byte-unchanged (only the trailing `.head` assignment block differs from parent).

Re-ran `scripts/audits/devteam-dispatch-gate-satisfiability.sh` myself — the self-report's `baseline_pass:true` was PO-attested from the row's review_note, NOT independently confirmed pre-QA; my own run: exit 0, 57 `[PASS]` / 0 `[FAIL]`, including the new `AC-QADRAIN-HEAD-GUARD` negative (`.head` byte-identical when a different task is genuinely busy, lane-move still fires) + positive (`.head` IS written when idle, regression-guards the existing call site) controls. `mock-guard.sh --files` on both touched files — PASS ("No production source files to scan", `.jq`/`.sh` fall outside its `.ts/.py/.go` scope by design). No TS files touched — `bun test`/`tsc` structurally N/A (Smart-Skip); the audit script itself IS this row's targeted regression suite and is fully green.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (review 245→244, done_verified 1→2, conservation check task_total 756→756, pure lane-move). `.head.active_task_id` was `null` (idle) — this task was never `.head.active_task_id`, so rule (b) head-sync does not apply; left `.head` untouched. Noted in the row's `status_note`: `scripts/devteam-review-claim-qa-drain.jq` is now safe to run normally on subsequent ticks — the defect it exists to fix (unconditional `.head` clobber) is closed and independently re-confirmed, not just self-reported. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa.md` §qa-S11.

## cycle-501 · 2026-08-04 · sprint CADENCE-RATIONALIZATION-20260804 — 5/5 dispatched rows APPROVED, DONE_VERIFIED (direct-commit verify); 2 undispatched rows flagged, not reviewed

Dispatch named 7 rows in `review[]`; only 5 were actually there (`CADRAT-1/2/4/5/6`) — `CADRAT-3` and `CADRAT-7` were still `ready[]`/`READY`/`next_agent:agent-father`, no commit, no dev close-out. Confirmed via lane-scan across all `task_board` keys before assuming the dispatch premise, per `feedback_empty_read_is_not_evidence`. Held both out of scope rather than reviewing nothing; reported the discrepancy back instead of silently absorbing it.

For the 5 real rows: all commits (`8c2acb44c`, `ae6f30c01`, `9af50bb26`, `d4371fdcf`, `eadf69998`) confirmed on main ancestry (`git merge-base --is-ancestor`), each `git show --stat` matched its row's claimed file set. Re-ran every test suite myself rather than trusting close-out prose (feedback_router_verify_raw_not_badges): `bun test DWF-phase1-cadence.test.ts` 61/61, `db-integrity-probe.test.sh` 27/27, `dev-team-tick-preflight.test.sh` 124/124 (+ confirmed zero hermeticity leak — widen-state file absent from real repo post-run), `orch-sentinel-lite-probe.test.sh` 18/18. `bun tsc --noEmit` clean (only 1 row touched TS). `shellcheck` on all new/modified `.sh` files — only the same pre-existing SC1091 info-notice class already accepted elsewhere in `scripts/agents-flow/` (diffed one against its own parent commit to confirm pre-existing, not newly introduced). `mock-guard.sh` PASS (no production `.ts/.py/.go` touched outside the 1 test file). Security grep clean across all new files.

One non-blocking finding on CADRAT-1: its AC-2 literal `jq` check (`== 0` zero dup pairs) actually returns 2 — traced to 2 pre-existing `(policy_id, calendar_status)` dup groups (`chef-intraday|open`, `gatherer-standard|open`) unrelated to alert-commander, confirmed byte-unchanged before/after this commit via `git show ~1`. The row's own 10 new rows introduce zero new dups; approved non-blocking rather than literal-AC-block a scope-fenced pre-existing condition the row was never asked to fix.

CADRAT-4's hardest AC (zero CronCreate invocation) independently re-verified by grepping every `CronCreate(` occurrence across the full commit diff — all live inside `register.md` markdown/heredoc content, never an actual tool call. I did not invoke `/cron-standalone-team` or any other fleet-rearm skill myself during this QA pass (grep-confirmed at close-out — session tool log carries zero `CronCreate` calls, per the hard constraint on this dispatch).

VERDICT: 5× APPROVED, DONE_VERIFIED. Moved `task_board.review[]`→`task_board.done_verified[]` for all 5 in one `jq`+`scripts/orch-apply.sh` write (review 264→259, done_verified 2→7, conservation check task_total 777→777, pure lane-move). DJ: `sprint-CADENCE-RATIONALIZATION-20260804-qa.md` §qa-S1..S6.
