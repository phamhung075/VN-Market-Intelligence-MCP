# QA — Notebook
## cycle-765 · 2026-08-14T18:01:35Z · FIX-CRON-JOB-RUNS-DOUBLE-LOG (apps/mcp-server/, commit `0a73c5b0490d370cc01d28a9040b56f4920c1aac`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]` drain field — derived via `git show --stat`, matched exactly against the commit-message file list. Commit confirmed real, on `main` ancestry. Read the diff directly, not `return_summary` prose: `startupHelpers.ts`'s `runPredictionResolutionWithDb` default fn swapped the self-recording `runPredictionResolutionJob()` wrapper call (double `recordJobRun`) for a direct `runPredictionResolution(db)` core call (single `recordJobRun`), and replicated the T4 `shouldSkipRecoveryReplay` dedup guard — already defined in-file, used identically by the sibling `runBctcReparseWithDb`/`runBaseRateComputationWithDb` wrappers — into this wrapper; `predictionResolutionJob.ts` itself untouched, matching the stated sibling-fix file-touch footprint exactly. Re-ran independently: new `FIX-CRON-JOB-RUNS-DOUBLE-LOG.test.ts` 10/10 pass (19 expect() calls); same 9-file related-file regression batch cited in the commit message (1420-cron-health-coverage, 1125/PRED-RESOLVER-GAP-FIX/1154 prediction-resolution, FIX-BASE-RATE-COMPUTATION-CRON-DEAD, FIX-BCTC-REPARSE-DOUBLE-WRAP-DEDUP-GUARD, ARCH-CRON-recover-jitter, FACTORY-SCHEDULER-job-table-registry, plus new file) = 140/140 pass, matches claimed 130+10 exactly. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files startupHelpers.ts` exit 0 PASS. DDD: `startupHelpers.ts` is `scheduler/`, not `domain/` — pre-existing `infrastructure/` imports (recordJobRun, breakers) out of scope for the domain-import ban, zero new import added. `process.env`/secrets grep clean on both touched files. Full 14987-test suite reclaim not repeated — targeted+related-regression scope satisfies the flow's own substitution instruction; claimed 56-fail count sits in the documented pre-existing 52-59 flake band already cross-referenced across 6+ sprint journals. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S4.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. `.head.active_task_id` was this exact task-id → reset to idle in the SAME write. Dispatcher-wrap lock `task:FIX-CRON-JOB-RUNS-DOUBLE-LOG` (held by dev-team) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool grant this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED again this cycle: file now carries 5 sections under active concurrent-peer write contention (`cycle-764` landed moments before this write) — appended additively only, no existing section touched, matching the same defer-prune precedent `cycle-762`/`cycle-763` already established above; next single-writer cycle should reconcile back to the 3-section cap.

## cycle-766 · 2026-08-14T18:02:14Z · INVESTIGATE-EMPTY-DATA-TABLES (apps/mcp-server/, commit `d4569c5b8`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row for a SPIKE (investigation-only, not a code FIX) with `.commit_sha` but no `.files[]` drain field — derived via `git show --stat`. Commit `d4569c5b8c81e04ef7fce6ff8d81aa7b2030269b` confirmed real, on `main` ancestry; touches exactly 3 files, all docs (`docs/spikes/SPIKE-INVESTIGATE-EMPTY-DATA-TABLES.md`, `docs/agent-memory/notebooks/dev-mcp-server.md`, `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md`) — zero production code, matching the SPIKE's own "investigation-only, zero code changes" AC. Did not trust dev-team's own RAW-verify prose in `review_note` — independently re-derived every substantive claim: (1) live `sqlite3 -readonly` on `data/live/market.db` (the actual bind-mounted prod DB per `docker-compose.yml`, not a stale named volume) confirms all 5 tables (`credit_data`/`insider_transactions`/`public_contracts`/`pharma_events`/`broker_sanctions`) = 0 rows today. (2) `brokerSanctionsJob.ts:99-105` `defaultFetchSanctions()` confirmed a verbatim `TODO` stub returning `[]`. (3) `brokerSanctionsJob.ts:45` `QUARTER_MONTHS=[3,6,9,12]` confirmed exact. (4) `muasamcong.ts:35-40` `getMuasamcongUrl()` confirmed to return the proxy env var (or `MUASAMCONG_ORIGIN` fallback) verbatim, no `?path=` composition. (5) `davPharmacyJob.ts:18,35` confirmed to import/call only `shouldSkipRecoveryReplay`, never `shouldRunCatchup` — cross-checked `startupHelpers.ts:102` exports a distinct `shouldRunCatchup` consumed by 3 other scheduler files, corroborating the no-catchup-guard claim. (6) `sscInsider.ts` `parseInsiderHtml()` confirmed a plain `<tr>`/`<td>` regex extractor, structurally incapable of parsing the JS-only Oracle ADF portal response. `bun tsc --noEmit` (apps/mcp-server) 0 errors (sanity check — no production file in this commit); `mock-guard.sh` N/A (zero non-doc files); `process.env`/secrets grep clean on all 3 changed files; DDD scan N/A (no `src/` files touched). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S7.

ONE FRESHNESS CAVEAT surfaced during re-verification, non-blocking: `review_note`'s phrase "`davPharmacyCheckJob` `cron_job_runs` = exactly 1 success ever (2026-04-30)" is now stale vs a live re-query — `cron_job_runs` shows a 2nd success at `2026-07-31T23:00:01Z` (= Aug-1 ICT monthly fire), landing after the SPIKE commit's own timestamp (`2026-07-31T18:05:56+02:00`) but before this QA pass. This does NOT undermine the diagnosis: June-1 and July-1 were STILL both missed despite the intervening April-30 and this later July-31 run, which corroborates rather than contradicts the unswept startup-catchup-guard-gap root cause (same class as 6 already-fixed sibling jobs this sprint). The committed SPIKE doc text itself is accurate as written at its own point-in-time (before that day's 23:00 UTC run) — nothing false was committed; staleness is confined to the `qa[]` row's later `review_note` restatement, not the artifact. VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:INVESTIGATE-EMPTY-DATA-TABLES` (held by router) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool grant this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) still DEFERRED — file now carries 6 sections; none touched, appended additively; a future single-writer cycle should reconcile back to the 3-section cap, well within the 200L hard cap regardless (file is 21L total post-append).

## cycle-766 · 2026-08-14T18:02:16Z · FIX-VPS-NEWS-STALE-FALSEPOS (apps/mcp-server/, commit `b08045ef0`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from `git show --stat`, matched exactly against the row's own `review_note` file list (dev-team's prior RAW-verify). Commit `b08045ef0be9a143f1eb4cd8766671870695d2b9` confirmed real, on `main` ancestry. Read diff directly, not trusted from prose: `EXPECTED_INTERVALS.news` (`vpsProxyStaleness.ts`) 10→20 + stale "every 5min" comment replaced with the live-cadence rationale; `VPT-1-vps-proxy-health-endpoint.test.ts` two threshold-comment updates (10min→20min); new `FIX-VPS-NEWS-STALE-FALSEPOS-news-threshold-calibration.test.ts` (58L, 4 cases: 15min-not-stale repro, p99-not-stale, genuine-60min-stale, threshold-bound assertion) — exactly the 3 files claimed, no scope drift. Re-ran independently: new calibration test 4/4 pass, VPT-1 endpoint test 7/7 pass, wider regression (VPT-1 + 1113-vps-proxy-health + 1920i-freshness-sla-extension + freshness-coverage-map-checker + new test, 5 files) 67/67 pass, `bun tsc --noEmit` 0 errors, `mock-guard.sh --files vpsProxyStaleness.ts` exit 0 PASS, `process.env`/secrets grep clean. Confirmed no reversion from a later unrelated config-split refactor (`vpsProxyStalenessConfig.ts`, commit `9930ee008`, post-dates this fix) — live `EXPECTED_INTERVALS.news` still `=20`. DDD: touched prod file is `interface/` layer (not `domain/`), imports from `domain/`+`infrastructure/` permitted per golden rule (dev-standards.md:1507 bans only `domain/`→`infrastructure/`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S5.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note` (row already carried dev-team's own RAW-verify prose there); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total=703 unchanged, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:FIX-VPS-NEWS-STALE-FALSEPOS` (held by dev-team, ttl_seconds=3600) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED again this cycle, same live concurrent-peer write contention already documented by every section above (file now 4 sections) — appended additively only, no existing section touched; next single-writer cycle should reconcile back to the 3-section cap.
