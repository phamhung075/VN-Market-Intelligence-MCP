## cycle-815 · 2026-08-23T14:52Z · RAG durability re-assessment at the matured floor + review/ready drain — 4 DONE_VERIFIED, 2 HELD with root cause fixed, 5 ready[] rows found non-executable, 1 self-retraction

**Both RAG rows HELD, but the reason changed class.** `FU-RAG-DEPLOY-MEMORY`'s ≥24h floor genuinely matured (window 2026-08-22T12:40:04Z→14:21Z = 25h41m). D1 PASS — zero kernel OOM in a dmesg buffer spanning 2026-08-20T11:53:13Z→2026-08-23T14:20:33Z, and I proved the negative non-vacuous (901 `docker0` hits in the same buffer) and the VM clock UTC before trusting it. D2 PASS (`16c59b5e929f`, StartedAt unmoved, RestartCount 0). D4 PASS. Cap read LIVE = `2147483648`, never from the thrice-stale AC prose. **D3/D5 are not merely unmet, they are unreconstructible: nobody was sampling.** Verified there was no alternative source — 0 memory lines in 10630 rag-service log lines since window open, no metrics endpoint, no cron/launchd sampler, `docker stats` has no history. Three consecutive cycles had each reached a mature clock and then discovered the same void; the prior cycle's instruction to "collect ≥6 samples SPREAD across the window" was never satisfiable retroactively. **Root cause was missing instrumentation, not missing patience** — so instead of deferring a fourth time I shipped `scripts/durability-mem-sample.sh` (locked against the concurrent-sampler corruption that destroyed the 2026-08-14 series, refuses an uncapped container, stamps D2 identity + live cap into the CSV header at open and re-reads identity at close) and opened a real window at 14:36:47Z, earliest legit close 2026-08-24T14:37Z.

**Refused to fit D3 on what existed, after computing it.** The six ad-hoc point reads are sawtooth phase samples (73.03% → 54.10% in 17 min, embedder unload), and a controlled 26-sample/13.1-min series fits to **+0.129 pp/min** — which would "FAIL" D3 on pure short-window noise. Certifying *and* rejecting on that number would both be wrong; `po_RULING_20260815T1002Z` had already established this for this exact service. Parked the measured values under `qa_durability_probe_…` and **deliberately did NOT use the six canonical D5 field names**, so no later reader mistakes a partial D1/D2/D4 for a satisfied D5. Also carried forward, quantified: observed peak 73.03% of 2GiB ≈ 1496MiB vs the ~1002MiB the cap was sized against — real headroom ~1.37x, not the 2.04x the deploy-half certification claims. And the recorded D3 FAIL on the LANCECORE row (338/799 samples >85%) was measured on the retired 1GiB container (its own CSV header says `mem_limit_bytes=1073741824`) — that FAIL must not be carried forward as standing, nor read as a pass; it simply does not apply.

**Escalation, proved by execution with a control:** `FU-RAG-DEPLOY-MEMORY` is in `RC_VERIF_GRANDFATHERED_IDS` (orchStateSchema.ts:569), and `checkVerificationGate` guards on bare membership with zero retraction/void awareness. Identical no-`raw_probe` DONE_VERIFIED transform on a fixture: grandfathered row → Stage 0+1 PASS, **write applied**; non-grandfathered control → `VALIDATION FAILED … without verification.raw_probe`, ABORTED. So the exemption, not the evidence, decides — on an OOM-class P0 with three recorded false greens in-family and a downstream row (`RAG-FTS-AC2-PEAKMEM-WALLCLOCK-MEASURE`) pressuring it toward DONE_VERIFIED. Bar §4 documents this as a known open gap; this is the live proof it is exploitable on the worst possible member. Did not patch (apps/mcp-server, out of zone) — recorded an interim binding process control on the row.

**I RETRACTED A GOVERNANCE CLAIM ~10 MINUTES AFTER MAKING IT.** Two rows were flipped to DONE_VERIFIED carrying `updated_by=developer` with `qa_verified_by` absent; I called that self-certification that "never passed the qa gate". **Wrong.** A concurrent peer qa session had fully verified and journaled both (STEP qa-S162, qa-S167). I inferred a governance failure from two row fields without grepping `docs/agent-memory/decisions/` for the task-id. Corrected on both rows to what the evidence supports — a *provenance-stamping* gap (the board alone cannot distinguish a qa verdict from an owner self-flip), which is real but far smaller. **Absence of a field is not evidence of absence of the work.** This is the second consecutive cycle I have had to walk back a conclusion reached by reasoning from a signal instead of executing a check against it; last cycle it was a failure mode I never asked the frequency of, this cycle a field I never cross-referenced.

**The peer flips were substantively correct, and I added evidence they lacked.** For the cowork firer I ran a real-environment no-network reachability smoke test the peer's suite structurally could not (it stubs `CURL_BIN`): real `.env`, no chat-id override, only `SLOT_MATCHER_CMD` forced to exit 7 → zero `ESCALATION-BLOCKED`, `_bug_chat_id()` resolved non-empty, well-formed sendMessage POST built. Scope check by grep not assumption: **the script reads `last_fired` zero times**, so it cannot false-escalate against the 9 stale slots. Measured the fleet myself: 21 enabled slots, exactly 9 at ≥8d (fb-daily 10d, chef-eod 10d, chef-morning 9d — the 3 guaranteed — plus 6 non-guaranteed). **Dispatch brief correction: `digest-sunday` is NOT dark**, live `last_fired` is 2026-08-23T13:47:27Z. `fb-daily` is the one genuinely suspect guaranteed slot — 08-14 and 08-17 are missed weekday fires the 08-18→08-22 outage does not explain.

**`FIX-DONELANE` DONE_VERIFIED, and I completed its own gate rather than waiving it.** Its verification_gate demands "a done[] row transitioning to DONE_VERIFIED through the new producer with real command output", so I verified the child first: `FACTORY-APP-split-assembleBriefing` (drained `done[]`→`qa[]` with `drain_source_lane=done`) re-ran clean — 366 pass/0 fail across 38 files, exactly matching its 2026-07-28 figure, tsc 0, mock-guard PASS — and flipping it moved the sole-`deps_satisfied` starved count 59→58 with `FACTORY-APP-split-assembleEveningSummary` leaving the starved set. That row had been APPROVED on 2026-07-28 and then stranded at status DONE for **26 days** because nothing produced the token. AC-5 was simply omitted from the `review_note`, so I measured it: every remaining member of the pure-DONE-token class has a named reason (2 depend on cold-evicted rows, out of any hot-file drain's reach per brief §0/§5; 2 depend on `TASK_RUNIDLE-1-AUDIT`, hot in `done[]` with `next_agent=pm`, inside SECONDARY-drain's eligible set awaiting 1-row/tick throughput). AC-3 checked line-by-line, not by count: all 5 `DONE_VERIFIED` occurrences across both `.jq` files are comments, zero writes.

**`FIX-MARKETWATCHER-EODMD` released from a 8-day hold by evidence, not by clock.** The 08-15 hold wanted ≥1 clean news-scout-offhours self-commit post-fix; three exist (`c61f190e4`, `541146148`, `cea834da2`), all post-dating both the wiring fix and the false-positive guard. Best co-fire evidence: at the 2026-08-22T20:09Z tick both notebook paths committed 37s apart, both clean. Verified no content loss per-commit via numstat rather than asserting it — every commit is an add-newest/drop-oldest autoprune shape, none is the large-deletion/small-insertion signature a clobber would leave. Stated the residual instead of glossing it: the specific eod.md 16:00Z weekday co-fire the guard targets has not occurred (market-watcher-eod is itself 8d stale), so that guard is unit-correct but not live-exercised; its failure direction is a spurious BUG alert, never data loss. Also declared what I could NOT check — no MCP binding this session, so the BUG channel was never read.

**All five `ready[]` rows with `next_agent=qa` are non-executable, and 4 of 5 hide it.** T9 needs T3/T5/T6 (all READY, `files=[]`); FANOUT-T8 needs T1/T2/T4 (all TODO); SESSION-DOWN names two gates absent from every hot lane *and* its assertion 2 requires killing the cowork master while five peer agents were live (refused on safety; recorded assertion 1 PASS so it is not redone); STALE-SLOT-DISPOSITION-TABLE's 4 declared deps are all TODO and its named "frozen 09:00Z snapshot" does not exist on disk (its title also says 11 stale slots; live measurement says 9 — remeasure, carry neither integer). **Systemic: four of the five carry `depends_on: []` while naming prerequisites in prose only**, so the gating is invisible to `deps_satisfied()` and they present as dispatchable. Recorded the measured blocker on each and routed to `po` — declined to re-lane to BLOCKED myself, since re-declaring dependencies is po/pm's call. Refused to run partial gates: a partial gate reported as a gate is the exact false-green these rows exist to prevent.

Peer churn was heavy — `task_total` moved 782→778→787 mid-cycle from concurrent eviction/mint; re-checked after each write and all 4 of my verified rows remain in hot `done_verified[]`, none evicted. `FIX-CHEF-MARKER-KEY-ANCHOR-1` carried commit `777ec912f`, which is NOT on main ancestry (amended away by my own prior pass into `e315472b7`, absorbing a peer's staged files); a peer had already corrected the field, and I re-derived the correction independently by shasum-comparing all 3 claimed files across both commits (identical). Note `git cat-file -t` reports that dangling commit as existing — only the ancestry check catches it. DJ `sprint-…-qa-26.md` STEP qa-S168..S172; that file is now 34094B against a 36000B cap, so the next writer should roll to `-27`. No push (pre-push `task-claim-lint` deterministically RED). No `task_release` — INV-GATEWAY-1, no MCP grant this sub-session.
