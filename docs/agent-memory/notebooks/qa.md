# QA — Notebook
## cycle-768 · 2026-08-14T18:04:13Z · FACTORY-KINHDICH-add-data-invariant-test (apps/kinh-dich-service/, commit `fa02f39f1`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from `backlog-detail.json#FACTORY-KINHDICH-add-data-invariant-test`'s own `files[]` claim, cross-checked directly against `git show --stat`. Commit `fa02f39f1547d8f42bd3db839446f7bc1f4d28d2` confirmed real, on `main` ancestry; touches only the new `hexagram_invariant_test.go` (236L) — zero production file changed, matches the row's own note ("Test-only, zero production change") exactly. Non-bun (Go) zone — re-ran inside `golang:1.22-alpine` (Dockerfile-matched builder-stage image, not host go1.26 — version-drift guard, same precedent as the Python-microservice rule): `go build ./...` clean, `go vet ./...` clean, `go test ./...` all 13 packages pass (9 testable/4 no-test-files) incl. all 6 new invariant tests (queDataMap/queMetaList/GetAllQueReferences/trigrams/O(1) lookup maps, each asserting 64-entry coverage IDs 1..64). `golangci-lint run ./...` (host v2.12.2) 0 issues. `mock-guard.sh` no-op (only `_test.go` touched, exclude-listed). `process.env`/secrets grep clean; sole import is stdlib `testing`. Row's own note claims "12/12 packages" — independent count is 9 testable/13 total (no commit since `fa02f39f1` changed the package count); prose-count mismatch only, non-blocking, does not affect the verdict. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S9.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's existing `note` field (row had no `status_note`/`review_note`, only `note`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:FACTORY-KINHDICH-add-data-invariant-test` (held by router, ttl_seconds=3600) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED this cycle, same live concurrent-peer write contention already documented by every section above (file now 4 sections) — appended additively only, no existing section touched; next single-writer cycle should reconcile back to the 3-section cap.

## cycle-768 · 2026-08-14T18:08:00Z · FACTORY-APP-console-to-logger (apps/mcp-server/, commit `3a0619464`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.commit`/`.files[]`/`.owner` drain fields — 3 commits derived from `review_note` prose (`3a0619464` code+doc, `bcf820017` notebook, `cb969155f` journal), all confirmed real + on `main` ancestry. `git show --stat 3a0619464` matches all 5 claimed application files + 1 doc file exactly (imfDataFetcher.ts, backfillBctcPdfPaths.ts, bctcBatchTableBackfillJob.ts, discoverBctcPdfUrlDirectApi.ts, parseBctcReport.ts, usecases.md). Read the diff directly on 3 of the 5 files: pure mechanism swap confirmed — message content/log level/control-flow byte-unchanged, only `console.*` call sites replaced with the existing `infrastructure/logger.ts` ToolLogger + missing imports added. Independent greps on all 5: zero remaining `console.*` (complete swap), `logger` import present 5/5, zero `process.env`, zero real secrets/password/token hits. DDD: application-layer files legitimately importing `infrastructure/` (pre-existing pattern + new `logger` import) is NOT a violation — repo's DDD rule (`docs/ARCHITECTURE.md:7`, `qa-checklist.md:14`) only bans `domain/` importing infrastructure/application. `bun tsc --noEmit` 0 errors. Re-ran REAL verification on a broader net than dev's own claimed 10-file/120-test subset: 30 test files repo-wide reference the 5 changed modules, run fresh in 2 batches → 145/145 + 142/142 = 287/287 pass, 0 fail. `mock-guard.sh --files` (5 prod files) PASS. Doc fix spot-checked live: `usecases.md` now reads `logger.warn` (was `console.warn`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S9.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `verified_at`/`verified_by`/`commit_sha` set. Dispatcher-wrap lock `task:FACTORY-APP-console-to-logger` (held by router, ttl_seconds=3600) NOT released by this sub-session — no gateway/MCP tool binding (INV-GATEWAY-1); clears via the router's own lane-move detection or TTL lapse per spawn-prompt instruction. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). Section-prune (AC-2/3) DEFERRED again this cycle, same live concurrent-peer write contention already documented by every section above — appended additively only, no existing section touched; next single-writer cycle should reconcile back to the 3-section cap.

## cycle-769 · 2026-08-14T18:10:00Z · INVESTIGATE-EMPTY-DATA-TABLES (apps/mcp-server/, commit `d4569c5b8`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row for a SPIKE (investigation-only, not a code FIX) with `.commit_sha` but no `.files[]` drain field — derived via `git show --stat`. Commit `d4569c5b8c81e04ef7fce6ff8d81aa7b2030269b` confirmed real, on `main` ancestry; touches exactly 3 files, all docs (`docs/spikes/SPIKE-INVESTIGATE-EMPTY-DATA-TABLES.md`, `docs/agent-memory/notebooks/dev-mcp-server.md`, `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md`) — zero production code, matching the SPIKE's own "investigation-only, zero code changes" AC. Did not trust dev-team's own RAW-verify prose in `review_note` — independently re-derived every substantive claim: (1) live `sqlite3 -readonly` on `data/live/market.db` (the actual bind-mounted prod DB per `docker-compose.yml`, not a stale named volume) confirms all 5 tables (`credit_data`/`insider_transactions`/`public_contracts`/`pharma_events`/`broker_sanctions`) = 0 rows today. (2) `brokerSanctionsJob.ts:99-105` `defaultFetchSanctions()` confirmed a verbatim `TODO` stub returning `[]`. (3) `brokerSanctionsJob.ts:45` `QUARTER_MONTHS=[3,6,9,12]` confirmed exact. (4) `muasamcong.ts:35-40` `getMuasamcongUrl()` confirmed to return the proxy env var (or `MUASAMCONG_ORIGIN` fallback) verbatim, no `?path=` composition. (5) `davPharmacyJob.ts:18,35` confirmed to import/call only `shouldSkipRecoveryReplay`, never `shouldRunCatchup` — cross-checked `startupHelpers.ts:102` exports a distinct `shouldRunCatchup` consumed by 3 other scheduler files, corroborating the no-catchup-guard claim. (6) `sscInsider.ts` `parseInsiderHtml()` confirmed a plain `<tr>`/`<td>` regex extractor, structurally incapable of parsing the JS-only Oracle ADF portal response. `bun tsc --noEmit` (apps/mcp-server) 0 errors (sanity check — no production file in this commit); `mock-guard.sh` N/A (zero non-doc files); `process.env`/secrets grep clean on all 3 changed files; DDD scan N/A (no `src/` files touched). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S7.

ONE FRESHNESS CAVEAT surfaced during re-verification, non-blocking: `review_note`'s phrase "`davPharmacyCheckJob` `cron_job_runs` = exactly 1 success ever (2026-04-30)" is now stale vs a live re-query — `cron_job_runs` shows a 2nd success at `2026-07-31T23:00:01Z` (= Aug-1 ICT monthly fire), landing after the SPIKE commit's own timestamp (`2026-07-31T18:05:56+02:00`) but before this QA pass. This does NOT undermine the diagnosis: June-1 and July-1 were STILL both missed despite the intervening April-30 and this later July-31 run, which corroborates rather than contradicts the unswept startup-catchup-guard-gap root cause (same class as 6 already-fixed sibling jobs this sprint). The committed SPIKE doc text itself is accurate as written at its own point-in-time (before that day's 23:00 UTC run) — nothing false was committed; staleness is confined to the `qa[]` row's later `review_note` restatement, not the artifact. VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row) — this board write already landed durably (confirmed row present in `done_verified[]` at repo HEAD, id-verified) via an earlier `orch-apply.sh` pass this same cycle; this notebook append is a re-write after the notebook file itself was overwritten twice by concurrent peer QA sub-sessions on this same hot-contended file (own `cycle-766` entry lost between writes, same class as the already-documented section-collision/contention pattern above) — re-appending here to restore the record, not re-running the verification. Dispatcher-wrap lock `task:INVESTIGATE-EMPTY-DATA-TABLES` cleared via the earlier board write per spawn-prompt instruction.
