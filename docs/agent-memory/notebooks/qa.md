# QA — Notebook

## cycle-676 · 2026-08-12T14:30:50Z · TICK-WU-1-COWORK-WIRING + TICK-WU-2-DEVTEAM-WIRING (developer, real code, sprint TICK-PREFLIGHT-USAGE-INSTRUMENTATION) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `976e7c5b7`+`ac53ec856`)

Router-dispatched, RAW-verify both (not trusted from developer's self-report). Both landed same cycle, no task branch (frontmatter `branch:` unused, direct commits to `main`).

`git show --stat`/`git show` on both production commits: each is EXACTLY the claimed 1-line `source lib/tick-telemetry.sh` + trailer swap (`run_preflight`→`tt_capture_and_log "<script>" run_preflight`) + comment block — zero touches inside `run_preflight()`/`_emit_verdict()`/Step 5.5/any `return` site. Test-file diffs are pure appends (0 deletions each). Neither commit touches `orch-state.json`.

Re-ran both suites myself: cowork 58/58, dev-team 146/146 — exact match to claim, not copy-pasted. Independently re-verified the AC-10 PRE-EDIT baseline (not trusted from the journal chain) by checking out the immediate parent commit into a disposable `git worktree`: 40/40 and 124/124 — exact match, both baselines real.

`shellcheck -S warning` on all 4 touched files myself: clean, exit 0. Live standalone smoke-run of BOTH wired trailers beyond the test suite's stubs (`bash <script>.sh` directly, real `CLAUDE_CODE_SESSION_ID` exported, scratch `PREFLIGHT_ROOT`): both produced exactly one valid JSON stdout doc, exit codes matched real verdicts (cowork ERROR→1, dev-team SKIP→0), telemetry lines written with correct 7-key shape, zero `CLAUDE_CODE_SESSION_ID` value found in either log file via grep. Grep-confirmed the only `CLAUDE_CODE_SESSION_ID` references in either `.sh` are each script's pre-existing presence/error-message logic, untouched by this diff. Zero `python3` hits (jq-only). `docs/agents/cowork-team/flow/main.md`/`docs/agents/dev-team/flow/main.md` still show the identical single-bash-call invocation line — no new tool call on the silent/skip path.

Read WU-2's T-LOG6 R6 test source directly: genuinely overrides `_step55_run_validate` (the correct still-unfixed R6 site, distinct from the already-fixed `_step55_run_cold_evict`), asserts the leak DOES reach caller stdout (negative control) before asserting graceful `verdict:"UNKNOWN"` degrade — not a fabricated test.

VERDICT: DONE_VERIFIED both. `[QA] Review Record` appended to both task handoff files. Flipped both `REVIEW→DONE_VERIFIED` via one `jq | orch-apply.sh` transform on `task_board.active_sprints[](id=TICK-PREFLIGHT-USAGE-INSTRUMENTATION).tasks[]` (`verification.raw_probe` attached per schema requirement, `commit_sha`/`qa_verified_by`/`qa_verified_at`/`next_agent:pm` set, mirrors WU-0's own QA-cycle row shape). Deliberately did NOT commit `docs/data/orch/orch-state.json` — carries unrelated uncommitted peer-agent dirt; same discipline as WU-0's QA cycle. Own QA DJ `sprint-TICK-PREFLIGHT-USAGE-INSTRUMENTATION-qa.md` §qa-S3/S4. Unblocks WU-3-AUDITOR-WIRING (pm's to decompose/dispatch). No `task_release` called — dispatcher's own responsibility per INV-GATEWAY-1.

## cycle-675 · 2026-08-12T14:20:00Z · FIX-PDF-EXTRACTOR-TEST-SYS-MODULES-LEAK (dev-pdf-extractor/dev-team-drain, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `1d8b1374f`+`8ac350161`)

Router-dispatched (`mode=verify-committed`, Review-Lane QA-drain, `branch:null` row, explicit REBUILD_REQUIRED flag to double-check — dispatcher noted the docker-cp verification "does not persist past a container restart/rebuild"). Commits confirmed on main ancestry; `git show --stat` matches the single claimed file (`test_low_text_density_ocr_rasterize.py`) exactly. Diff read on disk: all 4 raw `sys.modules[name]=stub` sites (pdfplumber/fitz/paddleocr/PIL+PIL.Image) now genuinely wrapped in `if name not in sys.modules:` guards, PIL/PIL.Image real-import-first `try:` blocks added — matches the described root-cause fix, not a stub.

REBUILD_REQUIRED gate checked directly, not assumed: pdf-extractor image `Created=2026-08-08T11:34:24Z` (container started 08-08T13:39:55Z), both postdate the fix commit (07-29) and the zone's last code commit (07-31, `d808a6a11`) — `docker exec grep` the LIVE container's file confirms all 4 guard sites present verbatim, the fix is durably baked into the deployed image, not merely the docker-cp hot-patch the developer used for their own verification.

Re-ran tests myself inside the (already-rebuilt) container, not trusted from prose: `test_ocr_backends.py`+rasterizer file both orderings = 47 passed each; +`test_page_rasterizer.py` = 32 passed; +`test_ocr_unit_tesseract_retry.py` = 26 passed — exact match to the claimed evidence, order-independent both ways. Full non-slow suite: 1047 passed/5 skipped/7 deselected/0 failed (claimed 1033 passed — the 14-test delta is explained by 2 weeks of unrelated suite growth since 07-29; skip/deselect/fail counts identical, not a red flag).

mypy unrunnable inside the runtime image (`no module named mypy` — dev-only tool, never shipped) and blocked on host by a pre-existing env-only "pdf-extractor is not a valid Python package name" error (confirmed pre-existing on an unrelated prior pdf-extractor task's own notebook entry). Independently re-derived the 0-new-errors claim anyway: ran mypy on the isolated before/after test-file content out-of-tree — both produce the identical 23-error set (5 pre-existing `infrastructure/ocr_adapter.py` errors + 4 stub-attr errors in the test file itself, only line numbers shift).

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`verification.raw_probe` attached with the full container-image/test/mypy evidence, conservation-check clean, task_total 755 preserved, `.head` untouched — was tracking a peer in-flight row). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no handoff file — direct-commit row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S88. Router dispatch held `task:FIX-PDF-EXTRACTOR-TEST-SYS-MODULES-LEAK` (TTL 3600s) — no gateway tool binding in this specialist sub-session (INV-GATEWAY-1), release left to the dispatcher/router per its own instruction.

## cycle-674 · 2026-08-12T14:10:00Z · FIX-GET-FOREIGN-ROOM-TOOL-RESULT-TOKEN-BUDGET (dev-mcp-server/dev-team-drain, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `3e8c5db91`)

Router-dispatched (`mode=verify-committed`, Review-Lane QA-drain, `branch:null` row). Row had no `.commit`/`.files[]`; found `3e8c5db91` via `git log --grep`, confirmed on main ancestry, `git show --stat` matches all 3 source files (new `P0-2-foreign-room-suite.test.ts`, new `foreignRoomAnalyzer.ts`, modified `foreignRoomTools.ts`) + 3 docs.

Re-ran independently, not trusted from prose: `P0-2-foreign-room-suite.test.ts` 43/43 pass; `bun tsc --noEmit` 0 errors; `mock-guard.sh` PASS both prod files; DDD clean (`foreignRoomAnalyzer.ts` — the new domain module — carries ZERO import statements, fully pure; the interface file's infra/application imports are expected wiring, not a violation). Neighbor-consumer suite reproduced exactly: `MONEY-RADAR-P0-T2-COMPOSITE.test.ts`+`money-radar-api.test.ts`+`IND-P1-MCP-REST-GAUGES-ENDPOINT.test.ts` (65) + `P0-2-foreign-room-suite` (43) = 108/108.

LIVE-verified against the actually-deployed container (image built 2026-08-11T18:25Z) via `scripts/agents-flow/mcp-call.sh`: `get_foreign_room()` with no `code` → 9,909 chars / 10 tickers, `tickers_rollup.total_tickers`=106 (full universe), `more_available`=true, `fetch_more.omitted_codes` length=96 (106-10 checks out) — down from the claimed pre-fix 74,245 chars/106 tickers, reproduced live not just asserted. `code=VCB` returns the untrimmed single-ticker path as designed. `/api/indicator-gauges` `.foreign_room` block confirmed to carry NO `tickers[]` — 3rd consumer genuinely unaffected. `toolCount` discrepancy noted and resolved: dev's 07-29 ephemeral-boot claim was 184, live production now reads 183 — this commit's own diff adds/removes zero tool registrations (read in full), so the drift is unrelated later-commit churn between 07-29 and the 08-11 image build, not a regression from this fix.

Full-suite baseline claim (14903 pass/55 fail on 07-29) not re-run wholesale — standing `FIX-MCP-SUITE-HEALTH-BASELINE` red, dev-standards.md pinned CANONICAL reading says targeted+neighbor suite is the authoritative DoD, not the repo-wide run. Instead independently checked TODAY's live CI post-fix (run `31602576573`, 2026-08-12): 15111 pass/40 skip/7 fail, only 4 failing files, none foreign-room-named; spot-checked the one FOREIGN-*-named failure (`FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts`) directly — confirmed unrelated (`checkForeignFlowGap` trading-day-gap logic tripping on a stale hardcoded 2026-08-06 date, not foreign-room/token-budget code) — zero overlap corroborated independently, not just keyword-scanned.

CAVEAT (dispatcher-flagged, verified correct): the backlog row's literal "3 consecutive live cycles recurrence-free" AC cannot be satisfied inside a single review pass — it requires observing multiple real alert-commander cron cycles post-deploy in production traffic, which has not happened yet. The mechanical/behavioral gate that AC exists to enforce (bounded tool-result size, flagged-first-then-|net| ranking, full-universe rollup, fetch_more re-fetch handle) IS closed and live-verified working. Did NOT hold the row in REVIEW for something no single review pass can satisfy — flipped DONE_VERIFIED for the fix itself, with the open recurrence clause called out explicitly in the record and a recommendation for PM/PO to spawn a lightweight follow-up live-observation monitoring task (not a code-fix task) to formally close it.

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`verification.raw_probe` attached incl. the live mcp-call.sh probe values, conservation-check clean, task_total 755 preserved). `[QA] Review Record (direct-commit verify)` appended to the row's own pre-existing `review_note` field (no `status_note` on this row) including the open-AC caveat + follow-up-task recommendation. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S89. Router dispatch held `task:FIX-GET-FOREIGN-ROOM-TOOL-RESULT-TOKEN-BUDGET` (TTL 3600s) — no gateway tool binding in this specialist sub-session (INV-GATEWAY-1), release left to the dispatcher/router per its own instruction.
