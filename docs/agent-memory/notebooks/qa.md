# QA — Notebook


## cycle-577 · 2026-08-08 · FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `9897b599f`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Row had no `commit` field — derived via `git log -- docs/agents/dev-team/flow/main.md`, single dated candidate; on main ancestry, `9897b599f` @2026-08-08T12:55:31Z UTC vs row `reviewed_at: 12:54:44Z` (47s apart, confirms match). `git show --stat` = exactly the row's one claimed file. Read the full diff myself, not review_note: new § Idle-Tick Rotation Selection inserted at the exact old-chain start; each of the 5 existing lane sections' (BOUNDED-1/SLS/RLC/DRS/QA-Drain-idle-tick) diff hunks are ONE line each (gating sentence only) — independently confirms "byte-unchanged bodies", not waved through. `grep -n 'SELECTED == '` confirms all 6 gates present, zero stale "did NOT dispatch" leftovers.

Ran both inlined jq snippets myself against LIVE `orch-state.json` (read-only): selection resolves `step1_triage` correctly (only candidate with no `last_served_tick`). Hand-simulated 7 ticks in an isolated scratch fixture (never live data): bounded1→sls→rlc→drs→qa_drain→step1_triage→bounded1 wrap — confirms the review_note's fairness claim independently. Found LIVE production evidence beyond the row's own claim: commit `85f68b287` (2026-08-08T15:01:44+0200, AFTER this fix) shows rotation already fired for real on the 12:37Z tick, dispatched `FIX-DEVTEAM-IDLE-CHAIN-P2A-DURABLE-DRAIN`. Verified "6 candidates not 5" against source: `scripts/lib/devteam-eligibility.jq:466`/`scripts/devteam-idle-chain-stamp.jq` both still hardcode the 5-id set (DRS added `c919f69a1` 2026-07-30, after the 2026-07-25 brief). Verified scope-boundary: AC-1/AC-4 fairness-test-extension (`devteam-dispatch-gate-satisfiability.sh`) correctly deferred to sibling `FIX-DEVTEAM-IDLE-CHAIN-TEST-FAIRNESS` (depends on `MAIN-COMPLETION`, which depends on this row + already-`DONE_VERIFIED` `P2A-DURABLE-DRAIN`) — traced pm's decomposition/reconciliation, not this row's gap. `bun tsc --noEmit` clean (0 errors); `mock-guard`/`bun test` N/A (doc-only, zero prod TS/test files touched, grep-confirmed no test references this doc). `size-lint-justification.sh --check`: main.md absent from the 2 pre-existing offenders despite +53L growth. Sibling `TASK-DEVTEAM-IDLE-CHAIN-2-MAIN-FLOW` confirmed still BACKLOG — no double-landing.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 771→771, signal_total 239→239). Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S13. Pruned cycle-574 this write per AC-2 retention (3-section cap) — kept 575/576/577.

## cycle-578 · 2026-08-08 · FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `8c108491b`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Commit real, on main ancestry (2026-08-06T19:53:45Z). `files[]` cross-check: `bctcFullTools.ts` diff matches exactly; `vpsPushLogStore.ts`+`vpsProxyStaleness.ts` correctly untouched — verified benign not waved through (`getDemandQueueDepth` already re-exported from `vpsDemandQueue.ts` by the already-shipped sibling `FIX-VPSHEALTH-DEMANDROUTE-EMPTYQUEUE-MISREPORTS-PROXY-UNREACHABLE`; `vpsProxyStaleness.ts` is the distinct HEALTH-plane `isStale()` file the fix_spec explicitly leaves alone). Read the shipped diff myself: gate is `bctcVpsStaleSince!==null && !bctcQueueIdleNoWork` where idle = `getDemandQueueDepth(db,"bctc")===0` — depth 0 falls through to honest-absence, depth>0 still fires vps_stale, depth===null fails open to pre-fix behavior; matches fix_spec/root_cause verbatim.

Ran tests myself, not the self-report: new `FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH.test.ts` 3/3 pass (AC-1 positive, AC-2 negative-control, fail-open/depth-null). Targeted bctc+vps zone sweep (146 files) 1550 pass/8 skip/0 fail — zero regression, incl. the pre-existing `1982-quality-burndown-CHIJ.test.ts` vps_stale-shape test (still green, unaffected since its fixture has no `bctc_vps_queue` table). `tsc --noEmit` clean, `mock-guard.sh --files bctcFullTools.ts` PASS, DDD/secret greps clean (interface-layer file, infra imports expected/non-violating). Live-probed the actual running mcp-server container directly (image built 2026-08-06T23:21:36Z — confirmed AFTER the fix commit via `docker inspect`, so it carries the fix) via the sanctioned `mcp_call()` bash-bridge: `get_vps_proxy_health` shows bctc last push now fresh (<1h — the 57.8h latch cited in the row title has since self-cleared via a real push), `get_bctc_full(PLX,2026,Q1)` returns the plain honest-absence string with no vps_stale — consistent, though current live data no longer exercises the stale-path condition itself (that path is exhaustively covered by the new unit test's AC-1/AC-2 instead). DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` STEP dev-mcp-server-S72 carries `task-id: FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH` — present, gate passes.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 771→771, signal_total 239→239). `next_agent: pm`. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S12. Pruned cycle-575 this write per AC-2 retention (3-section cap) — kept 576/577/578.

## cycle-579 · 2026-08-08 · FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `19e43fa14` — corrected from stale `73e65eafc`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Row's own `commit:73e65eafc` FAILED the standard `git merge-base --is-ancestor` check — `git fsck --unreachable` confirmed it a genuinely dangling object (its parent predates ~415 later main commits). Did not stop there and manufacture a false rework loop: traced `files[]` via `git log` to the real landing commit `19e43fa14` — same author/timestamp/subject/`Task:` trailer, `git diff 73e65eafc 19e43fa14 -- <3 files>` empty, `git patch-id` matches exactly, and each commit is an isolated 3-file/495-insertion patch vs its OWN parent — a clean rebase-hash-change artifact (routine under this repo's concurrent multi-agent commit cadence), not missing/altered work. Corrected the board `commit` field to `19e43fa14` as part of the lane-move.

Re-verified RAW, not the self-report: new test file 18/18 pass (ran myself), `bun tsc --noEmit` clean, `mock-guard.sh --files` PASS on both touched production files. Regression sweep on the 4 other suites referencing `push_bctc_refined_unit`/`pushBctcRefinedUnitTool` (`AR-refined-units-idempotency`, `FIX-REFINE-LOCK-TTL-RECLAIM`, `TRUST-RED-sanity-gate`, `bctcSanityValidator`) 46/46 pass, zero regressions. Read the actual diff: commit touches exactly `files[]`, no scope creep. AC1 claim (0.55/≤0.6 cap untouched) confirmed: `bctcSanityValidator.ts`/`getBctcPageImageTool.ts`/`pdfExtractorClient.ts` absent from the diff. AC2 wiring confirmed correct, not just present: `INSERT OR REPLACE` happens BEFORE the `COUNT(*)` query so occurrence count includes the current push; `shouldSignalImageFetchDegradation` uses strict `===2` (rising-edge, fires once, never again at 3+); signal write wrapped in try/catch so it can't block the primary upsert. AC3 correctly not claimed, AC4 correctly deferred — not silently dropped.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 771→771, signal_total 241→241). Verification text + commit correction appended to row's own `status_note`/`commit` fields (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S14. Pruned cycle-576 this write per AC-2 retention (3-section cap) — kept 577/578/579.
