# QA — Notebook

## cycle-526 · 2026-08-06 · FACTORY-ALERT-dedup-window-config — CHANGES_REQUESTED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `43f4e3add` confirmed on main ancestry, `git show --stat` matches exactly (`domain/models.go`+`models_test.go`, `module/alert_pipeline/pipeline.go`+`pipeline_test.go`, 4 docs). Read the diff, not review_note prose: `CooldownConfig.DedupWindowMinutes` (new named field, default 60) now feeds `HasDuplicateFingerprint(fingerprint, p.cfg.DedupWindowMinutes)` (was `p.cfg.CooldownMinutes`=30), reason string via `fmt.Sprintf`. Default 60 cross-checked against `mcp.config.json:215 alertQuality.dedupWindowMinutes` + `config.ts:663 numVal(...,60)` — real, not invented. Re-ran independently: `go build`/`go vet` clean, `go test ./...` 8/8 packages, 60 top-level/70 incl. subtests PASS (matches claim exactly), `golangci-lint` 0 issues, `mock-guard.sh` PASS, DDD/security clean, DJ-GATE-1 satisfied. **Code verdict: correct.**

CHANGES_REQUESTED anyway — not a code defect, a RUNTIME AC gap this row's own DoD names verbatim ("RAW-verify the served suppression decision after rebuild"). RAW-verified the LIVE container, not source: `docker inspect vn-market-intelligence-mcp-alert-engine-1` → image created 2026-07-15T15:13:50Z, StartedAt 2026-07-15T15:16:48Z — 13 days BEFORE commit `43f4e3add` (2026-07-28T21:25:42Z). Binary grep on `/app/server`: 0 hits for the new `"seen within"` reason string, 1 hit for the OLD `"seen recently"` — definitive proof the deployed binary predates this fix (and 4 sibling alert-engine FACTORY commits since 07-24). `/health` confirms the container is up, just running stale code — the served suppression decision today still reuses `CooldownMinutes=30`, exactly the pre-fix defect. Same class PO escalated TODAY P2→P1 as `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` (3 prior live occurrences); that row's own AC-2 routes ops-first-then-qa when `rebuild_required` is unsatisfied — applied here rather than routing to developer (no commit is owed).

VERDICT: CHANGES_REQUESTED. Moved `task_board.qa[]`→`task_board.review[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 791→791, qa 4→3, review 242→243). `next_agent`/`owner: ops` (not developer — defect is undeployed, not unwritten), `rebuild_required` added (missing→true), `redispatch_count: 1`. DJ: `sprint-FACTORY-ALERT-dedup-window-config-dev-alert-engine.md` §qa-S1.

## cycle-527 · 2026-08-06 · FIX-CRON-DST-LOCAL-EVAL-MOMENT-ANCHORED-EXPRESSIONS — APPROVED, DONE_VERIFIED (direct-commit verify, `review[]` row, router-scoped single-row spawn, `branch:null`)

Commits `36e109170` + `dd7a036b6` both confirmed on main ancestry. Read all 5 corrected files live via `git show HEAD:<path>`, not from the row's own prose. AC-1 db-data-integrity Job A CEST `15,45 4-11 * * 1-5`/CET `15,45 3-10 * * 1-5` exact match. AC-2 Job B CEST `0 0 * * *`/CET `0 23 * * *` exact match. AC-3 system-auditor Tier-3 CEST `0 4 * * *`/CET `0 3 * * *` exact match; diffed `36e109170` to confirm the `FIRE_TICK` value itself correctly stayed `02:00Z` (that was always the true UTC target, never wrong) — only the comment changed, to explain cron and FIRE_TICK are now in sync. AC-4 orch-sentinel FULL CEST `18 5 * * 0`/CET `18 4 * * 0`, LITE CEST `48 3 * * *`/CET `48 2 * * *` exact match, both still correctly unarmed. AC-5 structurally matches both precedent files (`cron-claude-manager-helper.md`, `cron-auditor-page-reverify.md`) — same ⚠️ blockquote + inline CEST/CET-in-cron-line shape, not just present.

AC-6 (flagged most-likely-half-done, checked hardest): CONFIRMED both layers actually updated, not just one. Idempotency-guard literals in `cron-standalone-team/SKILL.md` (items 1-2) + `cron-detect-loop/SKILL.md` (item 4) carry the new CEST values. The real `CronCreate` actuator prompts — `register-job-db-integrity-{weekday,offhours}.md` + `cron-detect-loop/register.md` Job 4 — independently also carry the new values with CET-changeover notes (a guard-only fix would have passed this same grep while still re-arming stale crons). Grepped all 5 old literals (`15,45 2-9 * * 1-5`, `15 22 * * *`, `0 2 * * *`, `18 3 * * 0`, `48 1 * * *`) across the full `.claude/skills/` + `.claude/commands/crons/` tree: zero live-actuator hits remain — only historical "before this fix" prose in `cron-system-auditor.md`/`cron-auditor-page-reverify.md`, plus one unrelated illustrative TICK-computation example in `dispatch-claim/SKILL.md` (fire-election reference doc, not a CronCreate call site). AC-7 confirmed correctly NOT attempted — no JS-matcher-migration file in either commit's diff, explicitly deferred in the commit message.

Cross-checked the row's own `note` self-report against everything above — no mismatch found; journal commits `419cdb804`/`674a7a961` confirmed real, DJ-GATE-1 satisfied (`task-id:` trailer present twice in `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-agent-father.md`); `.head` confirmed genuinely untouched (still pointed at unrelated `TE-T05`). One non-blocking observation logged, not held against this row: `docs/agents/system-auditor/flow/data-writer-provenance.md:18` still captions the pre-fix Job A/B literals, but explicitly defers to `cron-db-data-integrity.md` as the live spec host — cosmetic staleness, outside this row's AC-6 scope (not a re-arm skill/actuator).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.review[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 784→784, review 248→247). `.head` left untouched (was not pointing at this task). No `bun test`/`tsc`/DDD/mock-guard run — commit touches only `.md`/`.json` cron-authoring docs, zero `.ts` production source.

## cycle-528 · 2026-08-06 · FIX-COWORK-SPAWNFANOUT-FLOWPATH-BYPASSES-DIGEST-DAILY-DEDUP-GATE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commits `3caf5f0c1` (fix+test+flow-docs) + `8452cd5c8` (memory) confirmed on main ancestry, `git show --stat` matches all claimed files (`spawn-fanout.md`, `cowork-team/flow/main.md`, `cowork-schedule.json`, `cowork-match-slots.js`, new `cowork-schedule-consistency.test.js`). Live re-run, not trusted from row prose: `cowork-schedule-consistency.test.js` 9/9 GREEN. RED-reproduced by swapping in `git show`'d pre-fix `cowork-schedule.json` content into the working tree — TC-7 FAILs exactly on `digest-daily` (8/9), proving the test discriminates the real defect, not just today's clean state; restored byte-identical after (diffed against a pre-experiment copy — confirmed no residue). Sibling `cowork-match-slots.test.js` 43/43 and `cowork-catchup-predicate.test.js` 34/34 unaffected. `bun tsc --noEmit` clean; grepped commit stat directly — zero `apps/mcp-server/` files touched, so the row's "no `bun test` delta structurally possible" claim holds. `mock-guard.sh --files` on the 4 touched non-memory files: PASS.

Traced the fix mechanism end-to-end rather than trusting the self-report: `spawn-fanout.md` Step 5.2's `ENTRY_PROMPT` now resolves to `slot.trigger_prompt` — digest-daily's names `main.md`, confirmed `main.md`'s Step pre-D `DAILY-PREDICT DEDUP GATE` (`published:digest-daily:<UTC_DATE>`, still live) genuinely gates entry into `daily-predict.md`. Later same-day/07-31 commits (`IDENTITY_PREAMBLE`, `SESSION_ID_LINE`) layer cleanly on top of `ENTRY_PROMPT` without reverting the fix — read the current file, not just this commit's diff, to confirm durability. Launchd Layer-B backstop (`cowork-guaranteed-slot-firer.sh`) independently re-confirmed (grep, not cited) already reading `trigger_prompt` verbatim — was never affected. Confirmed the row's explicit prohibition held: did NOT find a copied dedup gate in `daily-predict.md` (still exactly 1 `task_claim`, the commit-mutex). All 4 AC clauses hold (general trigger_prompt-dispatch fix, not just the acute schedule-field mitigation; negative-control first-fire-still-runs preserved since gate logic itself is untouched; static assertion fails-before/passes-after specifically on `digest-daily`; shared predicate `extractPromptFlowPath()`/`slotEntryPathsAgree()` used by both runtime prose and test, no duplicate copy).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 786→786). `next_agent: pm`. `.head` untouched (was pointing at an unrelated task). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S53. Structural note: no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — could not directly call `task_release`; flagged to the calling dispatcher to release `task:FIX-COWORK-SPAWNFANOUT-FLOWPATH-BYPASSES-DIGEST-DAILY-DEDUP-GATE` itself.

## cycle-528 · 2026-08-06 · FIX-AUDITOR-MEMACK-HEADROOM-FLOOR-AND-DEAD-TRACKEDBY — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `3b096d4b2` confirmed on main ancestry, touches all 3 `files_hint` files + `docs/WORK.md`. Did NOT trust `developer_review_note`'s own 167/167 claim — re-ran `auditor-tier1-probe.test.sh` independently on current HEAD: 181/181 pass (this row's own T44-T53 = 26 assertions all green — floor boundary T45/T46, below-floor T44 using PO's exact live number 97.11%/22.2MiB, staleness T47/T48 mem-arm + T51/T52 launchd-arm, fail-loud T49/T50/T53; pre-existing regression guards T35-T43 mixed-case/uncapped-skip still green; T54-T58 belong to sibling `FIX-AUDITOR-T1-PREGATE-MEMCREEP-SINGLE-POINT-SAMPLE` landed after per this row's own `sequencing_note`, non-conflicting). shellcheck re-run against the EXACT commit content (checked out via `git show <sha>:path`, not current HEAD, to isolate this diff's own delta) and diffed vs the parent commit: identical warning set both before/after (2 in probe.sh: SC2254:556, SC2034:820; pre-existing info/warn set in probe.test.sh unchanged) — zero new warnings introduced, confirms the commit's own "shellcheck clean" claim. `mock-guard.sh` PASS (pure shell zone, no `apps/` TS/JS touched, `bun test`/`tsc` structurally N/A per developer's own correct claim).

Went beyond the developer's own 07-29 AC6 capture: ran the live probe against TODAY's real fleet (2026-08-06T20:14:46Z) — rag-service's cap has since moved to 1GiB (an unrelated `FU-RAG-DEPLOY-MEMORY` infra change, out of this row's scope), at 95.92% = 41.8MiB headroom computed correctly off the LIVE `docker inspect` cap (confirms `_mem_headroom_mib` is not hardcoded to the 768MiB figure baked into the commit message), correctly just above `MEM_FLOOR_MIB=40` → ALL_GREEN/suppressed as designed; `tracked_by=FU-RAG-DEPLOY-MEMORY` resolved to `REVIEW` (live, non-terminal) against the real `orch-state.json`, proving `_task_status_in_orch_state` reads the actual board, not a fixture. AC1-AC5 all independently reproduced (T40/T46=AC1, T44/T45=AC2, T47/T48/T51/T52=AC3, T37/T41=AC4-regression, T42=AC5-regression); AC6 raw evidence present in `developer_review_note`. No blocking issues.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 786→786). `next_agent: pm`. `.head` untouched. DJ: `sprint-INFRA-AUDIT-qa.md` §qa-S1.
