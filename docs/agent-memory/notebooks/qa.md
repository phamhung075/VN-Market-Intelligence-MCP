# QA — Notebook


## cycle-557 · 2026-08-07 · TASK_603 (final gate) + PARENT FIX-CRON-REGISTRATION-PREFIX-NOT-EXCLUDED-ORPHANEMIT-AND-D4-R1B — APPROVED, both DONE_VERIFIED (direct-commit verify, commit `814182608` already on main)

Router explicitly instructed independent reproduction, not trust of self-report. Did all 4 independently:

AC-3a: reverted `AND task_id NOT LIKE 'cron-registration:%'` myself from `coordinationStore.ts` → scoped run **45 pass/1 fail RED**, exact negative-control test failing → restored → `git diff --quiet` confirmed byte-identical to HEAD → re-ran → **46 pass/0 fail GREEN**. Both halves of the negative control (no signal + row still deleted) asserted in one test at source (lines 1141-1165); positive control at 1167-1191.

AC-4: ran the FULL suite myself (`bun test`, 473.80s, not trusting either of the developer's 2 runs) → **15157 pass/40 skip/44 fail/48043 expect()**, 15241 tests/1265 files. Pass/skip/fail counts exact match to developer's own Run 1; expect()-count off by 2 (48043 vs claimed 48041) — same order-dependent flaky-floor class the developer used to explain their own Run1↔Run2 delta, not a regression (flagged in report/handoff as minor discrepancy, non-blocking). Grepped all 44 failing test names + `awk`-isolated the `task-lock-coordination-store.test.ts` block: zero overlap with coordinationStore/task_locks/cron-registration/tasksMdJanitorJob/isKnownLegitPattern/gcExpiredLocks. Net-new-failures = 0.

AC-6: `docker inspect vn-market-intelligence-mcp-mcp-server-1 --format '{{.Image}}'` → `sha256:115700a86e65...` — matches claim + router's own prior independent check. `Created` 2026-08-06T23:21:39Z, `healthy`. All 11 peer containers in `docker compose ps` show unchanged multi-day/week `Created` timestamps — single-service rebuild confirmed, no fleet `down`/`up`. `docker-compose.yml` git log shows no touch by this task or neighbors.

AC-5: grep/diff-checked both agent-father files (`handlers.md`, `audit-dimensions.md`) across all 4 named commits (`814182608`/`03af0f983`/`8e756c36d`/`7aa8247b4`) — zero touches in any. `git log -1` confirms last actual edit predates this task by weeks (2026-07-18/2026-07-25).

tsc 0 errors, mock-guard PASS (test-only diff, Smart-Skip DDD/security).

VERDICT: TASK_603 APPROVED, DONE_VERIFIED. Parent row also flipped `READY`→`DONE_VERIFIED` in the SAME `orch-apply.sh` write per handoff's explicit "Handoff to QA" instruction (this is QA's responsibility, not developer's) — conservation OK (task_total 757→757, signal_total 204→204), 2 rows stamped. `next_agent: pm` both. Sequencing constraint (brief §4.4) now satisfied — router's held agent-father Lane-1 dispatch unblocked. QA Review Record appended to `docs/agents/dev-team/handoff/TASK_603.md`; report `reports/TASK_REPORT_603.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-8.md` §qa-S4.

## cycle-559 · 2026-08-08 · FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commits `d6c4e6006`+`c4b7b2060`)

dev-team Review-Lane QA-Drain batch dispatch (7-row batch, this row highest-ranked, `.head.active_task_id` pointed here at claim time). Verified both commits on main ancestry; `git show --stat` matches `dev_note`'s file claims (`coordinationStore.ts`+`coordinationStore.test.ts`). Read the full diff myself, not the dev_note prose: `heartbeatTask`/`releaseTask` gain an additive 3rd `options` param — FR-1 two-statement UPDATE (ttl_seconds clamp 60-691200, then payload_patch shallow-merge via `parseJsonObject` EC-6-safe helper), FR-2 Rung-B null-session ladder gated strictly on the ROW's own `owner_client_session IS NULL AND task_kind='orphan-signal'` columns (never a caller-supplied flag) — NFR-1 anti-theft structurally intact (a live-session row can never match Rung B).

Re-ran `bun test src/infrastructure/__tests__/coordinationStore.test.ts` myself: 13/13 pass/27 expect — exact match to dev_note claim. `bun tsc --noEmit`: 0 errors. Ran a broader targeted regression than claimed: 20 lock/coordination test files (grep `coordinationStore|task_heartbeat|heartbeatTask|releaseTask|task_release|task_claim`, superset of dev's claimed 18) → 332 pass/0 fail/991 expect, zero regressions. `mock-guard.sh --files coordinationStore.ts` PASS. DDD (infra/application import grep) and security (`process.env`/secrets grep) both clean. NFR-2 backward-compat independently confirmed at the call-site level (not just the diff comment): grepped all live callers of `heartbeatTask(`/`releaseTask(` outside store/test files — only `coordinationTools.ts:174/208` + `bctcRefineJob.ts:532`, all still 2-arg calls, untouched by this commit. Scope boundary (Zod exposure = separate Task 2 `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS`, still READY) confirmed genuine against the PM decomposition handoff, not a gap. Repo-wide `bun test` not re-run in full — pinned CANONICAL reading (`dev-standards.md:1435`, targeted/merge-gate suite governs over the standing tracked `FIX-MCP-SUITE-HEALTH-BASELINE` full-suite red).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 778→778). `next_agent: pm`. `.head.active_task_id` was this row (branch:null) — synced `.head` to idle (`status:idle, active_task_id:null, next_agent:router`) in the same write per `CANONICAL:SSOT-STATUSFLIP-LANEMOVE` rule (b). Verification text appended to row's own new `status_note` field (no handoff file, per `verify-committed` sub-flow). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S2 (concurrent sibling QA sub-agent also writing to the same file this window — appended after their entry, no overwrite).

## cycle-559 · 2026-08-08 · FIX-BCTC-FPT-BT5-BALANCE-GATE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, no `commit`/`files[]` stamped — derived `87ccf249a` via `git log -- vn_number_normalize/primitive.py`)

dev-team Review-Lane QA-Drain batch-claimed row (`FIX`, P1, `apps/pdf-extractor/`), routed by prior PO ruling (2 dispositions on the row: PO 07-22 owner-assign, PO 08-05 SECONDARY-Drain next_agent→qa). No `commit`/`files[]` on the row itself — derived via `git log --oneline --all -- domain/primitives/vn_number_normalize/primitive.py`: 1 candidate (`87ccf249a`, 2026-07-10T01:01:51+02:00), confirmed on main ancestry, `git show --stat` matches review_note's exact file claim (`primitive.py` + new `test_vn_number_normalize.py`).

Reproduced the fix directly rather than trusting prose: `vn_number_normalize("43.751.466.292,590")` → `"43751466292590"` — the OCR-misread-last-separator case now normalizes correctly (was `"43751466292.59"`, the 1000x-shrink root cause of the 43T VND imbalance); genuine 1-2-digit VN decimals (`"1.234,56"`→`1234.56`, `"123,45"`→`123.45`) unaffected — confirms the new `_VN_OCR_MISREAD_LAST_SEP_RE` gate (requires ≥1 preceding thousands-dot group + exactly 3 digits after comma) doesn't over-generalize.

Re-ran targeted suite myself (11 files, `-m "not slow"`): 213 passed/0 fail/1 deselected — exact match to review_note. Independently ran the named real-OCR integration test the review_note flagged as still-red (`test_extract_tables_bt3d_real_ocr.py::test_extract_tables_usecase_real_ocr_path`, `-m slow`, actual Tesseract, ~32s): it now PASSES (1/1) — better than dev's own report. Traced why: sibling commit `42cf264f4` (`FIX-BCTC-BT3D-ROWS-THRESHOLD-STALE`, already QA-verified DONE at `806ea73d3`) landed same-day, updating the stale `rows_stored>=80` assertion to `>=79`; ancestor-order confirmed `87ccf249a`→`42cf264f4`→main. Both fixes together close the row's own flagged remaining gap.

Full non-slow suite: random test order → 1051 pass/1 fail (`test_pek_engine_adapter.py` forced-timeout test, unrelated); re-ran deterministic (`-p no:randomly`) → 1052 passed/7 deselected/0 fail — confirms the random-order failure is an unrelated pytest-randomly ordering flake (this diff touches only a pure, stateless domain function with no shared state), not a regression. DDD: `primitive.py` imports only `re`+`__future__`, zero infra/application imports, matches its own documented domain-layer contract. `mock-guard.sh --files primitive.py` PASS. Python zone — no bun/tsc applicable, no secrets/env touched. New unit tests (4) non-vacuous: exact-value assertions + a negative boundary case (`test_ocr_misread_last_separator_requires_preceding_dot_group`) + a non-regression genuine-decimal case.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 778→778, signal_total 229→229). Stamped `next_agent: pm` (was stale `qa` on the row). Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S3.

## cycle-560 · 2026-08-08 · FIX-ORPHAN-FR1-FR3-FR6-SKILL-DISPATCH-CLAIM — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commits `234902038`+`5ac21c593`+`749d0e307`)

dev-team Review-Lane QA-Drain batch dispatch. All 3 commits real, on main ancestry, `git show --stat` matches claimed files exactly (SKILL.md only for code commit — zero `apps/` TS/Go touched). Did not trust `review_note` prose — read the live diff + current file myself. FR-1: cross-checked the central factual claim against live source — `coordinationTools.ts` `task_heartbeat` Zod schema still only `task_id`/`owner_client_session` (confirmed by reading the tool registration directly), so the rewritten "NOT YET reachable via the live tool schema" prose is accurate; `coordinationStore.ts` `HeartbeatOptions`/`heartbeatTask()` confirms the backend `payload_patch`/`ttl_seconds`/`owner_agent` support is real (sibling `FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER`, commit `d6c4e6006`, on main). FR-3: read the full guard (SKILL.md :359-440) — batch `board_snapshot` hoisted outside the per-signal loop (EC-8), one jq pass covers flat lanes + `active_sprints[].tasks[]` (EC-2), `task:` prefix stripped (EC-1), lane classification matches the acceptance line (ready/in_progress=active; review/qa/done/done_verified/backlog=terminal, no BLOCKED carve-out; not-found=terminal, never defaults active), runs before the `redispatch_count>=N_MAX` branch. FR-6: `owner_agent` present at the escalation `task_heartbeat` call (:454), additive, correctly annotated NOT-YET-LIVE. Header size-justification (593L declared) matches actual 593L; fence-balance even (26). `bun test`/`tsc`/`mock-guard` correctly N/A (zero production source in scope). DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-4.md` STEP developer-S79 present, task-id stamped.

NON-BLOCKING OBSERVATION (flagged in `status_note`, not actioned): file's byte-cap predicate (TE-T24, cap×60=12000B) breaches at 31235B — this predicate is never honored by a size-justification header (byte-cap independent of line-cap, per `context-bloat-backstop.sh`'s own design). Pre-existing (file already 23933B, 2x over, before this task) and worsened +7302B, but all added content is load-bearing FR-1/FR-3/FR-6 text. Per standing precedent (`feedback_ctxbloat_breach_on_live_sprint_file_defer`) this class routes to po/claude-manager-helper's governance sweep, not a QA merge gate — not blocking.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 778→778). `next_agent: pm`. `.head` already idle/unrelated to this row (head-decoupled dispatch per spawn prompt) — untouched. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S4 (concurrent sibling QA sub-agent also wrote §qa-S3 to the same file this window — appended after, no overwrite).
