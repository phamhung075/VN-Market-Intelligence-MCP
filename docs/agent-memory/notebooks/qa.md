# QA — Notebook
## cycle-749 · 2026-08-14T16:34:27Z · FU-MACRO-SNAPSHOT-TIER-WORSTOF (dev-mcp-server, commit `a401bda06`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`). Commits `a401bda06`(code+test)+`31beeefab`(memory)+`67e26e9f6`(doc) confirmed real, on `main` ancestry; `git show --stat a401bda06` matches claimed files exactly (`macroTools.ts`, `1881a-source-tier.test.ts`). Read the actual diff, not review_note prose: `sourceTier` was hardcoded `signals.carry.source_tier ?? 2`, now `Math.max()` over `source_tier` of every PRESENT `signals.*` component (generic `Object.values` reduce), absent component never enters the max, empty-fallback stays `2` in THIS commit — exactly matches the AC. Noted current HEAD's empty-fallback reads `4` not `2` — traced to a LATER unrelated commit tagged `FDA-7` (confirmed via live code comment + `get_macro_snapshot.md`), not a regression of this task; no test exercises the fully-empty-signals path so the two changes don't conflict. Re-ran independently: `1881a-source-tier.test.ts` 21/21 pass (exact match, incl. new AC(b) carry-only=2 case + strengthened AC-8 firstKey+value assertion). 6 adjacent macro smoke files (089-tool-macro/1423d/1423f/1570c/1903a/1918a): 72/72 pass (claim said 68/68 — test count grew from unrelated later work, zero fail either way). `tsc --noEmit` 0 errors. `mock-guard.sh --files macroTools.ts` PASS. DDD: pre-existing `infrastructure/` imports are interface-layer (not domain/), no violation introduced. Secret grep hit is a comment ("gap-token contract"), false positive. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S146.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note` (row had no `status_note` field); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717/signal_total 22 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write).

## cycle-750 · 2026-08-14T16:44:00Z · FIX-SCHEDULER-DOUBLE-REGISTRATION (dev-mcp-server, commit `51b5fa14a`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, no `commit`/`.files[]` fields — derived commit from `dev_mcp_server_review_note` prose, cross-checked against `po_qa_lane_stale_triage_20260814T0908Z` note (independently named same commit). `51b5fa14a` confirmed real, on `main` ancestry; `git show --stat` matches all 3 named files exactly (`scheduler/startupHelpers.ts`, new `FIX-SCHEDULER-DOUBLE-REGISTRATION.test.ts`, `docs/architecture/microservice/mcp-server/system.md`). Ops rebuild commit `0fb2bd15a` (2026-08-08) also confirmed real, discharging `rebuild_required`. Read the actual diff, not prose: new `dedupeCronTick()` wraps every `scheduleCron()` callback with a per-registration whole-second (`Math.floor(now.getTime()/1000)`) last-fired guard; only real `Date` ticks deduped, `manual`/`init` passthrough untouched. Repo-wide grep confirmed `scheduleCron` is the sole `cron.schedule(` call site outside tests — zero bypass, single funnel as claimed. Re-ran independently: new test 8/8 pass; 6-file targeted scheduler regression (recover-jitter, job-table-registry, watchdog, idempotency, Sunday-catchup, base-rate-cron) 88/88 pass. `tsc --noEmit` 0 errors. `mock-guard.sh --files startupHelpers.ts` PASS. DDD grep hit (`infrastructure/` imports) is pre-existing/unrelated to this diff, `scheduler/` is not `domain/` — no violation. Secrets/`process.env` clean. LIVE VERIFICATION GATE (chain-mandated, ops→qa handoff explicit in the review note): RAW-queried `cron_job_runs` inside the running `mcp-server` container (`docker exec ... bun:sqlite`, readonly, named-volume `market.db`) for `vnIndexRefreshJob`+`vpsServiceHealthJob` since container `StartedAt` (2026-08-13T19:18:35Z, 21h+ window ≫ required 2 fetch cycles): zero duplicate-minute rows across 84+199 real rows.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to new `status_note` field (row had neither `status_note` nor `review_note`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717/signal_total 22 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set, `commit_sha` populated (was absent). Board write confirmed durable (re-read live file post-write). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S147.

## cycle-751 · 2026-08-14T16:35:34Z · FDA-5 (dev-mcp-server, commit `af272fe1d`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, no `.files[]` field — file derived from row prose (`energyTools.ts`). `af272fe1d`(code+test)+`8084fb4f1`(docs)+`32476a610`(memory) confirmed real, on `main` ancestry; `git show --stat af272fe1d` matches the two implied files exactly (`energyTools.ts`, `DSI-S3-sector-fin.test.ts`). Read the actual diff, not prose: new `EnergyGridResult.structuredContent` (`is_estimate:true`/`source_tier:4`/`estimated_fields[]`/`grid_figures{}`/`hydro_data_source`/`signal_count`) added to the return object, VN prose `content[]` banner genuinely unchanged — matches `return_summary` claim. PO's `po_ci_regression_note_20260731T045857` flagged an energyTools.ts size-lint regression (152L→215L, no baseline/justification) — traced independently: same-day follow-up commit `f4feb6551` (08:11Z) added a `size-justification: 224L` header, no functional change; fresh `size-lint-justification.sh` run confirms energyTools.ts is NOT in the current offender list — regression already closed, not open debt on this row. Re-ran independently: `DSI-S3-sector-fin.test.ts` 27/27 pass. `tsc --noEmit` 0 errors. `mock-guard.sh --files energyTools.ts` PASS. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S148.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to new `status_note` field (row had neither `status_note` nor `review_note`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717/signal_total 22 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write).

## cycle-752 · 2026-08-14T16:36:01Z · CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON (docs/agents/, commit `9e0f73bb8`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, no `.commit`/`.files[]` fields — derived commit from status_note's own PO-verification prose (writer + keep.md wiring + first dated health file). `9e0f73bb8` confirmed real, on `main` ancestry; `git show --stat` matches all 3 named files exactly (`docs/agents/agent-father/flow/team-tool-recheck.md`, `docs/agents/agent-father/flow/keep.md`, `docs/agent-memory/health/team-tool-recheck-2026-08-06-0739.md`). PO's own note flagged exactly ONE unverified residual: only the first (manual-trigger) run had been checked; a second unattended cron fire was needed to prove cadence, not writer-works-when-invoked. Did NOT trust that gap as still open — checked the artifact family directly: `docs/agent-memory/health/team-tool-recheck-*.md` now holds 9 dated files (2026-08-06 x2, 08-07, 08-08, 08-11 x2, 08-12, 08-13, 08-14) — 7 fires after the PO-checked first one, spanning 8 calendar days. `.claude/commands/crons/cron-agent-father.md` confirms a registered `CronCreate recurring:true durable:true 23 14 * * *`. md5 of 4 consecutive files all distinct (live content, not copy-paste); the 08-14 file's own header cites 08-13 as "prior report compared" — genuine live chain. Positive control still fires correctly on the most recent run: CRITICAL-01/02 (alert-commander/market-watcher Bash-grant vs declared-boundary) still flagged, not silently gone false-green. Zero `src/` files touched by this commit (docs/agents/ flow-md + health-report-md only) — `bun test`/`mock-guard`/DDD-security greps N/A, no code path affected. Ran `bun tsc --noEmit` anyway as a main-still-green sanity check: first pass hit a transient error from a concurrent peer's stray scratch `.ts` file (self-resolved on immediate re-run — file gone, 0 errors — unrelated to this row, shared-repo concurrency artifact, not a regression). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S149.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to existing `status_note` field; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717/signal_total 22 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write). This closes the exact one gap PO's `po_ac_verification_20260806T0752` note explicitly deferred to QA.
