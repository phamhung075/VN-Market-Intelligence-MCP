# QA — Notebook

## cycle-384 · 2026-07-08 · CONTAM-10-WRITER-H — DONE_VERIFIED (post-swap live-gateway RAW-probe PASS)

Final gate after ops's gated swap (`e2bac5b0a`, image `4c8ea4cfd41f`, container healthy, peers unaffected — independently re-confirmed via my own `docker inspect`, not trusting the badge). Ran the TRUE live-gateway RAW-probe deferred last cycle: real POST to `http://localhost:3000/api/push-ohlcv-history` on the actual running `vn-market-intelligence-mcp-mcp-server-1` container. Used synthetic ticker `QAPROBE1` (pre-checked absent from `daily_ohlcv`/`watchlist`; no FK on `code` column, so zero collision risk regardless of date) — pushed a clean full-VND anchor row (2020-01-02, 131000/133000/129000/130000) then a contaminated batch shaped like the real leak evidence (2020-01-03, open=131/high=133/low=129/close=130, 100-999 VND range). Both `{ok:true,inserted:1,skipped:0}`. Verified the PERSISTED row (not just the response) via an in-container `bun:sqlite` read (same-uid as the writer → correctly sees committed WAL pages) — landed at 131000/133000/129000/130000, whole-row ×1000 corrected, volume left unscaled. Container log confirmed: `scale_correction QAPROBE1 2020-01-03: prevClose=130000 current=130 ratio=1000.0 → ×1000`. Caught myself nearly mis-reading a false negative: the external read-only sidecar (`keinos/sqlite3 file:...?immutable=1`, same pattern as `db-integrity-counts.sh`) returned 0 rows for the same query right after the write — WAL-blind by design (documented gotcha), not a probe failure; switched to the in-container read for ground truth. CLEANUP: deleted both synthetic rows immediately post-verify (in-container `DELETE`, 2 rows removed, confirmed 0 remaining via in-container read + `GET /api/prices/history?code=QAPROBE1` → 404); `daily_ohlcv` total row count (737441) unaffected net of the transient rows; no production ticker/date touched at any point. Flipped `CONTAM-10-WRITER-H` → `DONE_VERIFIED` via `scripts/orch-apply.sh` (validated, exit 0). `.head` idle-reset (`next_agent: "router"`) — `CONTAM-10-EXEC-2` (depends satisfied) is now unblocked for a future dev-team dispatch; did not dispatch it myself, that's the router's job. DJ: sprint-OHLCV-UNIT-CONTAM-WHOLEROW-LT1000-qa.md §qa-S4 (DJ-GATE-1).

## cycle-383 · 2026-07-08 · CONTAM-10-WRITER-H — QA-APPROVED, HELD REVIEW (ops swap pending, dev-mcp-server 7fa78ac42)

Gated dev-mcp-server's fix migrating `handlePushOhlcvHistory` (VPS backfill-queue poller route, ~15-30min cadence, actively re-contaminating daily_ohlcv: 6,533 rows/27 tickers) from raw INSERT+naive validateOhlcvUnit to `writeOhlcvBatch(rows, db, {conflictStrategy:"backfill"})` | Verdict: **code+tests APPROVED, held REVIEW** (not done_verified — live-container RAW-probe is ops-gated). Diff review: `UPSERT_BACKFILL_SQL` confirmed byte-identical semantics to prior raw INSERT (foreign-flow cols untouched); WIC-2 parse-and-reject pre-pass confirmed preserved verbatim (only de-indented out of `db.transaction`); response shape correctly derives from `writeResult`; Writer-H inventory row correctly flipped to "Migrated". Re-ran targeted suite myself (not trusting self-report): 5 files/38 tests/167 expect — exact match to dev's claim; also grepped for other live-route callers dev didn't list and found+ran one more (`CONTAM-7-ohlcv-unit-contam-integration.test.ts`, 45/45 green). `tsc --noEmit` 0 errors, DDD clean (interface→application allowed direction), security clean (process.env confined to test files, matches a pre-existing 14-file repo convention — not new), `mock-guard.sh` PASS exit 0 on both modified prod files. Independently verified the 1 "genuine regression" dev claims to have found+fixed (`TASK-VNINDEX-RS-B-durability.test.ts` FR-B1-TC2): confirmed the bar's FR-S1 seed-bar exemption legitimately does not apply (O!=H!=L!=C), so it correctly flows through `normalizeOhlcvToVnd` x1000 — matches Writers A/C/D, not a masked regression. RAW-probe: `docker inspect` confirmed the live container is still on the pre-fix image (not the rebuilt `4c8ea4cfd41f`) — corroborates dev's "not yet swapped" claim; did NOT probe the shared container (would exercise old code AND pollute the live prod DB with test rows) and did NOT self-authorize `docker compose up -d` (ops-gated, standing policy) — instead wrote+ran my OWN RAW-probe test (not reusing dev's file) against an isolated local `createBunServer` instance: real HTTP POST to the real route with a contaminated batch (open=131/close=130) + clean ~130,000-VND history → PASS, row corrected to x1000 scale. Full bun suite independently re-run in background under heavy host contention (2 concurrent `bun test` procs observed) — did not reach 100% within this cycle (~300/1171 files covered), zero new OHLCV-related failures seen in the covered portion; targeted+adjacent-caller suites (the files that actually exercise this diff) are complete and green — the load-bearing evidence. Held at REVIEW per task's own gate (true live-gateway probe needs the ops swap); `status_note: "code/tests QA-approved, pending ops swap + post-swap RAW-probe"`; `.head` updated `next_agent: "ops"` to request the swap; CONTAM-10-EXEC-2 stays BLOCKED (do not start early — active leak until swap deploys). DJ: sprint-OHLCV-UNIT-CONTAM-WHOLEROW-LT1000-qa.md §qa-S3.

## cycle-382 · 2026-07-07 · PDF-TEST-01-FIX — HELD/BLOCKED (real gap found, not closed DONE_VERIFIED)

Audit task: are pdf-extractor's PEK-INTEGRATE/layout-first/OCR extraction paths covered by tests, and do those tests currently pass? | Verdict: **coverage adequate, suite NOT reliably green — held**. Dispatcher's pre-verify peek claimed 349 test files; real count is 60 `.py` files (349 likely counted `__pycache__` bytecode) — did not trust either number, ran pytest myself. `.venv/bin/python` at repo root is a stale dev venv (missing PIL/pandas/pytesseract); used the live `vn-market-intelligence-mcp-pdf-extractor-1` container (real deps) as authoritative runtime. 5 named target files in isolation: `138 passed, 0 failed` — real production code exercised, not mocks-only (`test_pek_engine_adapter.py` 39 tests incl. semaphore contention/release, lazy-load singleton, fail-loud+quarantine on layout-crash/paddle-load-failure/runtime-table-extraction-failure; `test_layout_invariants.py` 33 tests on real `domain/primitives/layout_invariants/primitive.py` balance-identity/monotonic-codes/orphan-ratio checkers, pure functions, zero mocking; OCR cluster 66 tests on real `infrastructure/ocr_backends.py` backend-selection+auto-fallback policy + real `_to_pil()` ndarray/PIL-passthrough conversion + real retry loop in `generic_md_table_extractor.ocr_unit`). Coverage verdict: **adequate** for all 3 named areas. BUT full non-slow suite (1086 collected, 7 deselected slow) = `1079 passed, 7 failed` — did not accept isolated-pass as sufficient (task explicitly warned against file-count theater); root-caused + reproduced all 7 failures to ONE test-hygiene bug: `__tests__/unit/test_low_text_density_ocr_rasterize.py:91,102,110,123-124` unconditionally stomps `sys.modules["PIL"|"PIL.Image"|"fitz"|"pdfplumber"|"paddleocr"]` at import time with no restore (no `conftest.py`, single pytest process → poisons every file collected afterward). Reproduced twice: `[this file + test_ocr_backends.py]` → 1 fail (`PIL.Image.new` AttributeError); `[this file + test_page_rasterizer.py]` → 4 fail (fake fitz stub's hardcoded `page_count=4` leaks into a real 2-page-PDF assert, `4 != 2`). Same root cause explains the other 2 full-run failures (`test_ocr_unit_tesseract_retry.py` AC1/AC2, identical error). This sits squarely inside the audited OCR-fallback scope (polluting file's own docstring: "ocr_pages rasterize fallback") — not waved off as unrelated noise. Did NOT close DONE_VERIFIED: filed follow-up gap `FIX-PDF-EXTRACTOR-TEST-SYS-MODULES-LEAK` (owner dev-pdf-extractor, zone pdf-extractor, priority medium, size S — fix: `monkeypatch.setattr`/explicit-teardown fixture/extend the file's own already-correct `_ensure_stub()` conditional pattern to all 5 modules). `PDF-TEST-01-FIX` board-moved `in_progress`→`review`, `status: BLOCKED`, `status_note` cites full evidence, `blocked_on` points to the new task id — via `orch-apply.sh` myself. `.head` idle-reset myself (no downstream agent to forget it). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S5. Report: reports/TASK_REPORT_PDF-TEST-01-FIX.md.

**Retention note:** cycle-381 (FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP — APPROVED) and older pruned this write per AC-2 (keep last 3). Full detail preserved in `reports/TASK_REPORT_FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP.md` and prior git history of this file (which also carries cycle-380's collapsed pointer: KD-OBS-01-FIX APPROVED / cycle-379: CI-RED-c5b5f885-FIX APPROVED / cycle-378: FACTORY-ALERT-consolidate-dual-engines APPROVED / cycle-377: B-05-FU-SSC-503-RETRY PASS / cycle-376: FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK APPROVED / cycle-375: FIX-ALERT-COMMANDER-DEAD-NO-SLOT PASS / cycle-374: FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD PASS / cycle-373: FIX-FB-DAILY-FIRER-CLI-FLAG APPROVED / cycle-372: FIX-BCTC-BANK-BS-COLUMN-ORDER PASS / cycle-371: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
