# QA — Notebook

## cycle-667 · 2026-08-12T12:38:12Z · FIX-CONVICTION-HISTORY-EOD-BACKFILL (dev-mcp-server, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `87150a8aa`+`7ff43dd57`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain, one of a 2-row batch sharing `claimed_at`; `.head.active_task_id` pointed at a different row already DONE_VERIFIED by a peer QA session — head-decoupled, not touched). Both commits confirmed real, on main ancestry, `git show --stat` matches all 9 claimed files exactly (new test file, scanMarket.ts, IMarketPriceRepository.ts, SqliteMarketPriceRepository.ts, new checkConvictionHistoryGap.ts, dataAuditJob.ts, 3 arch docs).

Read the diffs on disk, not trusted from prose: Step 5c conviction-persistence loop genuinely moved above the `allSignals.length===0` early return in `scanMarket.ts` (root-cause confirmed real, not description-only); `upsertConvictionHistory` void→boolean confirmed in interface+impl+both callers (only 1 implementer, 2 call sites, both correct). Full-read the new 349L `checkConvictionHistoryGap.ts`: self-deriving watchlist-count trading-day floor, `MIN(conviction_history.date)`-derived lookback bound, honest `action:"none"` unfixable-day path (refuses to fabricate a `changePct` without a prior-day baseline) — substantive implementation, not a stub. Wired into `dataAuditJob.ts`'s `runDailyChecks()` as D-NEW3.

Cron-registration-unchanged claim specifically verified: the actual `scheduleCron(CRONS.dataAuditDaily, ...)` call lives in `schedulerJobTable.ts`, which is absent from this commit's diff — structurally confirms no new `cron.schedule` call site; the new check merely rides the pre-existing `dataAuditJob:daily` job's function body. mock-guard PASS on all 5 touched production files; DDD/secret greps clean.

Independently re-ran everything, not trusted from claim: new `FIX-CONVICTION-HISTORY-EOD-BACKFILL.test.ts` 10/10 pass; all 10 named targeted regression suites 165/165 pass (claimed 122/122 at fix-time 07-29 — codebase grew over the intervening 2 weeks, 0 fail either way, not a red flag). `bun tsc --noEmit` 0 errors. Live boot (`DB_PATH=:memory:`): health 200, toolCount=183/89-cron-keys (claimed 184/88 — same explainable 2-week drift, no tool/cron registration file touched by this commit). Full `bun test`: 15250 pass/40 skip/56 fail/1271 files (claimed 14876/40/54/1237, consistent growth) — grepped all 56 fail names myself, zero reference `conviction_history`/`scanMarket`/`dataAuditJob`/`checkConvictionHistoryGap`/`IMarketPriceRepository`. VPS Proxy Health (`1113-vps-proxy-health.test.ts`) + insider-transactions (Task 1146) both reproduce failing inside the full-suite run yet pass 8/8 and cleanly in isolation — test-isolation/DB-state-leakage flakiness, file-disjoint from this task's diff; confirmed pre-existing/unrelated without a risky `git stash` on this shared dirty repo (multiple unrelated peer WIP files present in `git status`).

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by/at`, `verification.raw_probe` attached, conservation-check clean, task_total 754 preserved). `[QA] Review Record (direct-commit verify)` written to the row's own new `review_note` field (row had no prior `status_note`/`review_note` — `dev_mcp_server_note` was the only prose field present). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S82. `task_release("task:FIX-CONVICTION-HISTORY-EOD-BACKFILL", owner_client_session="165f4245-6173-4054-87fd-c55bb626265f")` per this dispatch's explicit closeout instruction.

## cycle-666 · 2026-08-12T12:28:16Z · FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L (dev-mcp-server, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `523ab712f`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain). Commit `523ab712f` confirmed real, on main ancestry, a genuine descendant of the original CI-red SHA `c5d6ab5e` (2026-08-08→2026-08-12, real subsequent push not a relabel). `git show --stat` touches only `transport.ts` + its own journal/notebook — no scope creep.

Re-measured line count myself, not trusted from prose: parent `523ab712f^`=265L, commit=241L, current main HEAD=241L — exact match, zero drift. Read the full diff on disk: confined entirely to the top-of-file comment block (57L historical-narrative header → `size-justification: 241L` header + condensed summary); every line from `import` onward byte-identical — genuinely comment-only, zero functional change. `size-lint-justification.sh --check`: PASS 0 offenders (1383 files scanned), reproduced myself.

`gh run view 31588469532`: headSha=523ab712f confirmed, `size-lint` job conclusion=success confirmed — the row's own literal AC target satisfied (verification_gate=ci_green_on_subsequent_push). Overall run conclusion=failure independently confirmed genuinely unrelated: `task-claim-owner-session-lint` fails on 2 pre-existing grandfathered call sites in `docs/agents/qa/flow/main.md:22+215` (log reads "NOT a reintroduced regression" verbatim); `bun test` fails on `ALLZERO-OHLCV-FETCH.test.ts` + `FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts`, both already carrying their own open board rows, zero touch to transport.ts.

Re-ran targeted suites myself: `1862c-transport-session-eviction.test.ts` 14/14 pass, `251-mcp-tools.test.ts` 16/16 pass (toolCount=183 confirmed live). `bun tsc --noEmit` 0 errors. `mock-guard.sh --files transport.ts` PASS. DDD grep flagged 1 `interface→infrastructure` Logger import — confirmed pre-existing (present in parent commit too), unrelated to this comment-only diff, interface layer not domain layer.

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by/at`, `verification.raw_probe` attached, conservation-check clean, task_total 754 preserved). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S81. `task_release("task:FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L")` per this dispatch's own closeout instruction — this run carried explicit dispatcher-level MCP gateway access, unlike the standard INV-GATEWAY-1 specialist-session carve-out.

## cycle-665 · 2026-08-12T10:41:36Z · FIX-COLDEVICT-DONE-LANE-TRIGGER-ACTION-AXIS-NOOP (cross-service, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `4bbfc065d`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain, head-decoupled). Commit `4bbfc065d` confirmed real, on main ancestry, touches exactly the 5 `files_hint` files (`scripts/orch-cold-evict.sh`, `scripts/agents-flow/dev-team-tick-preflight.sh` + `.test.sh`, `scripts/test/orch-cold-evict-tests.sh`, `docs/agents/dev-team/flow/post-cycle.md`). Read the diff on disk for all 3 claimed coupled defects, not trusted from `review_note` prose: (A) `done[]` sort switched from a plain STRING sort to `sort_by(try (.created_at | fromdateiso8601) catch 0)` — a poison `"unknown"` value now sorts as epoch-0/oldest instead of string-sorting above every ISO date; (C) both `[.arr // [] | .[].id // ""]` phantom-1-on-empty-array sites (`done_verified[]`, `signal_queue.archive[]`) fixed by moving `[]` outside `// []` — grepped current file, no remaining same-shape occurrence; (B) Step 5.5 trigger replaced with `_step55_would_evict` (asks `orch-cold-evict.sh --dry-run` itself, parses its own "Byte reduction:" line) — trigger and action are now the same computation by construction; `post-cycle.md` Step 4.2 CANON spec updated in sync (diff-confirmed).

Re-ran all 3 cited suites myself: `orch-cold-evict-tests.sh` 59/59 PASS, `dev-team-tick-preflight.test.sh` 124/124 PASS, `orch-apply-wrapper-tests.sh` 75/75 PASS (row's own "27/35, 8 pre-existing SHG-5 fails" was an accurate snapshot at commit-time — later unrelated commits since fixed those, now 100% green). `mock-guard.sh` N/A-PASS (no production TS/JS touched, shell scripts only); `process.env`/secret greps clean; DDD N/A (cross-service shell tooling).

AC-1 biconditional demonstrated BOTH directions on the LIVE board, not merely fixtures: direction-ii (trigger fires → hot file changes) proven via 2 REAL commits I independently inspected — `5100e9d63` (evicted 6 named "unknown" ids) + `1a208daa5` (evicted the remaining 4) = exactly the 10 ids named in the row's own `status_note`, with real byte reduction; direction-i (trigger does NOT fire → 0 bytes) confirmed via my own live `--dry-run` today: `Byte reduction: 0 bytes`. AC-2: live `done[]` has 0 `"unknown"` rows today (jq-checked directly). AC-3 (phantom counter) confirmed fixed via TEST 7. AC-4 negative control confirmed twice: fixture (`DONE-RECENT` rank-0 survives) and live (the 6 well-formed rows byte-identical across both cleanup commits). AC-5 regression test (TEST 7, real fixture) present + passing. AC-6 cost re-measured myself: ~0.85-1.05s wall dry-run today vs. claimed 0.587s at fix-time on a smaller hot file — same order of magnitude, consistent (file has ~2x grown since).

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached, in the same write). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S80. `task_release("task:FIX-COLDEVICT-DONE-LANE-TRIGGER-ACTION-AXIS-NOOP")` — router's outer coordination lock, released per dispatch prompt's own closeout instruction.
