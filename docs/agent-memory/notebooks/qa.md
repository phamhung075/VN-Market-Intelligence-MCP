# QA — Notebook

## cycle-704 · 2026-08-13T23:59:22Z · FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE (report-analyzer, commit `5ee697852`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — orch-state `blocks`/`co_edit` sequencing fields were write-only-decorative, PO's own reverse-only `blocks` guard on `FU-BACKFILL-REAL-FILENAMES` (07-25) silently didn't bind. Did not trust `dev_close_note` prose at face value; RAW-verified all 5 ACs from `note`/`po_stubfields_scoping_20260725T1715`/`po_coedit_foldin_20260725T1737` against live code.

Commit `5ee697852` confirmed on `main` ancestry; `git show --stat` matches all 7 claimed files. RAW-ran every claimed suite myself, not trusted from prose: `FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE.test.ts` 11/11 PASS; `orchStateSchema.test.ts` 120/120 PASS (claim said 104/104 — traced the +16 delta to unrelated later commit `ad6e422e9` "RC-VERIF completion gate" via `git log` on the test file, not a regression); `orch-backlog-stub.test.sh` 7/7 PASS; `test-orch-validate-ac.mjs` 29/29 PASS; `bun tsc --noEmit` clean; `mock-guard.sh` PASS on all touched production files.

AC-1/AC-3 (the negative control — the criterion distinguishing a real fix from cleanup): read `checkDecorativeSequencingFields()` at source (`orchStateSchema.ts:978-1073`) AND independently reproduced the negative control myself, distinct from the shipped test file's own fixtures — injected a fresh reverse-only `blocks` edge and a non-empty `co_edit` value onto a live row in a scratch copy of `orch-state.json`, ran `orch-validate.mjs` directly: both hard-rejected with a real exit code 2 and a specific fix hint. Confirmed `orch-apply.sh:138` aborts the write on ANY non-zero validator exit ("live file untouched") — the guard binds at the real write path, not only in isolation.

AC-2 (7-row disposition): live's 2 remaining non-empty `blocks` rows (`VMT-7a..e→VMT-7-REGISTER`, `CONTAM-10-WRITER-H→CONTAM-10-EXEC-2`) independently confirmed properly backed via their targets' own `depends` arrays; `FIX-MCP-SUITE-HEALTH-BASELINE`'s malformed prose confirmed preserved verbatim under `migrated_blocks_prose` (`blocks` itself now `null`, correctly skipped by the guard); `bash scripts/orch-state-validate.sh` exits 0 clean on the live file today (0 Stage-1e issues). The 2 `migrated_co_edit_partner` rows no longer appear on the live board at all — traced via `git log`: both independently shipped in `adb426877`, went `DONE_VERIFIED`, later pruned by normal done-lane archival lifecycle — unrelated drift, not a defect of this fix.

AC-4: `scripts/orch-backlog-stub.sh:64` `STUB_FIELDS` confirmed carrying `depends_on,depends,blocked_by` (`dev_close_note`'s `:57` line citation is stale by ~7 lines from an added comment block — non-blocking doc-accuracy nit only). AC-5: `git show --stat 5ee697852 -- docs/data/orch/archive/backlog-detail.json` empty — confirmed untouched, no hand-edit.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` STEP `developer-S35`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live data. `[QA] Review Record (direct-commit verify)` appended to the row's `dev_close_note` (no `status_note` field present on this row). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S114/S115). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 729=729). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool`/`mcp__semble__search` binding present — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE` and is the authoritative release path.

## cycle-703 · 2026-08-13T22:30:03Z · FIX-AUDITOR-VERDICT-TRANSCRIPTION-PROSE-OVERRIDES-MACHINE-VERDICT (report-analyzer, commit `6f10770cb`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row. This row's own subject IS agent self-report vs machine-truth divergence — held it to a higher skepticism bar than a normal verify-committed pass, refused to trust the dev's review_note, the developer's own decision-journal claim, or the spawn brief's stated test counts; re-derived everything from source.

Commit ancestry: `git merge-base --is-ancestor` true for `6f10770cb`(impl)/`a8cae55e3`(journal+notebook)/`5f1c89f3f`(board flip, out of scope per spawn brief — router-verified separately). `git show --stat 6f10770cb` matches all 4 row-claimed files (`audit-output-contract.sh`, `audit-output-contract.test.sh`, `auditor-tier1-probe.sh`, `system-auditor/flow/main.md`) plus `auditor-tier1-probe.test.sh` and new `docs/data/auditor-tier1-last-trigger.json`; confirmed sole-writer discipline held — the diff to `auditor-tier1-last-healthy.json` is a timestamp-only line, no shape/semantic change.

Independent full test re-run (own invocation, not the dev's pasted numbers): `bash scripts/audit-output-contract.test.sh` → 87 passed/0 failed; `bash scripts/agents-flow/auditor-tier1-probe.test.sh` → 226 passed/0 failed. Cross-checked against the parent commit via `check()`-call count diff, not just totals: 56→87 (+31) and 214→226 (+12) — both exact.

Manually replayed every acceptance fixture directly via `scripts/audit-output-contract.sh`'s own standalone CLI mode (`[[ "${BASH_SOURCE[0]}" == "${0}" ]]` entry point), entirely bypassing the dev's own `.test.sh` harness, using my own fixture files: OCC-1 (raw=ESCALATE/declared=FOLD) → exit 1, `verdict=ESCALATE`, `check=A-30 raw=ESCALATE declared=FOLD`. OCC-2 (trigger=FAILURE/mem_creep, declared=ALL_GREEN) → exit 1, `verdict=DIVERGENCE`, `trigger_check=mem_creep dimension=A-30`; same trigger + `declared=WARN:dedup-SKIP vs c48` → exit 0, `verdict=CLEAN`, no VIOLATION line. Machine-verdict-with-no-declared-counterpart → VIOLATION (`silent omission` wording present), forces `verdict=ESCALATE`. Declared-with-no-machine-counterpart → `INFO declared-no-machine-counterpart`, exit 0, NOT a violation. All three new flags absent → byte-identical pre-fix STDOUT (no `verdict=` suffix at all), exit 0 — V1-V5 genuinely unchanged.

`shellcheck -x -e SC1091` on both changed `.sh` files: 2 warnings (SC2254, SC2034) — diffed byte-for-byte against the parent-commit blob to confirm pre-existing (only line-shifted 613→652 / 877→961), zero new warnings introduced by this diff. `mock-guard.sh` correctly reports no production source files in scope (shell + markdown only).

AC-5 (live Tier-1 tick, zero new po-addressed signals): grep-confirmed the pre-gate script (`auditor-tier1-probe.sh`) makes zero `call_tool`/MCP calls anywhere (1 grep hit total, a comment stating exactly this) — structurally incapable of posting a signal regardless of verdict. Ran the real live pre-gate myself against the actual fleet (rag-service currently not breaching) → `verdict=ALL_GREEN`, trigger file correctly persisted. This mutated 3 tracked files as a side effect of my own manual replays (`auditor-tier1-last-healthy.json`/`auditor-tier1-last-trigger.json` from the live run, `auditor-output-contract-violations.json` from my OCC-1/OCC-2/AC-4a manual replays exercising the durable-violation-record append path — useful corroboration it works, but not this task's own output) — reverted all 3 via `git checkout --` before sign-off.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP `developer-S103`, task-id trailer matches.

VERDICT: `DONE_VERIFIED`, zero ISSUE — every one of the row's 6 acceptance criteria independently reproduced from source. `[QA] Review Record (direct-commit verify)` appended to the row's `review_note`. `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/qa-S114). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 730=730). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). Structural note, consistent with every prior cycle this sprint: this sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team session (`632721c2-...`) holds `task:FIX-AUDITOR-VERDICT-TRANSCRIPTION-PROSE-OVERRIDES-MACHINE-VERDICT` and is the authoritative release path via its own finally-block/TTL.
