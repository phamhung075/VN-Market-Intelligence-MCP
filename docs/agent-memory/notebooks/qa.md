# QA — Notebook

## cycle-521 · 2026-08-06 · FIX-DEVTEAM-BOUNDED1-PROSE-SEQUENCING-UNBACKED-GATE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row's own `commit: ec8f17ab6` confirmed on main ancestry; `git show --stat` matches all 4 claimed files exactly (`scripts/lib/devteam-eligibility.jq`, `bounded1-supervised-lane-report.sh`, new `devteam-bounded1-prose-sequencing-gate-verify.sh`, `docs/agents/dev-team/flow/main.md`). Read the `has_unbacked_sequencing_prose` def + its wiring into `is_bounded1_eligible` directly — board-OR-detail OR-precedence matches every sibling `effective_*` def's convention; deliberately does not regex-parse `po_sequencing_*` prose (per spec).

Re-ran the task's own regression verifier myself, not trusted from review_note prose: `devteam-bounded1-prose-sequencing-gate-verify.sh` → 5/5 PASS (AC-1 unbacked-not-eligible, AC-1b backed-eligible once `depends_on` populated, AC-1c detail-side symmetry, AC-2 live UC-CDC-P5 shape stays held by `deps_satisfied` not double-gated, control unaffected). Also ran the 2 broader system-wide audits for due diligence: `devteam-dispatch-gate-satisfiability.sh` has 1 pre-existing FAIL ("BOUNDED-1 no-ops at in_progress>=1") and `bounded1-supervised-lane-report.sh` FAILs on 4 unrelated backlog rows with no dispatch lane — both structurally unrelated to this diff (different gate entirely: WIP<1 cap lives in the CLAIM script, not `is_bounded1_eligible`; this task's conjunct is ADD-only, it can only ever narrow eligibility, never cause a spurious promote — cannot be the cause of a "should-no-op-but-fired" defect). Sibling `devteam-bounded1-detail-disposition-gate-verify.sh`, flagged broken in the review_note as a pre-existing harness bug, now passes 10/10+control clean — fixed since by an unrelated later commit, no action needed either way.

No `.ts`/apps/ touched (pure jq+bash+md) — `bun test`/`tsc --noEmit` structurally N/A (Smart-Skip); `mock-guard.sh --files` → PASS "no production source files to scan"; DDD/security greps clean. DJ-GATE-1 confirmed: developer's own journal entry present (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` line 30).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 800→800). `.head` untouched (unrelated active_task_id: `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). Decision journal rolled qa-2→qa-3 this cycle (byte-cap breach, 42481B>36000B — see qa-2's own CAP-REACHED sentinel; `send_telegram(bug)` skipped, no gateway/MCP tool grant this sub-session). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S38.

## cycle-522 · 2026-08-06 · BCTC-REPORT-ID-LOOKUP-TOOL — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row's own `commit: 7a9eea6bd` confirmed on main ancestry (author-date 2026-07-23); `git show --stat` matches all 3 code files exactly (new `FIX-BCTC-REPORT-ID-LOOKUP-TOOL.test.ts`, new `getBctcReportIdTool.ts`, `registry.ts` +2L purely additive) plus 7 doc/data files. Re-ran the row's own test myself, not trusted from status_note prose: 5/5 PASS, 22 expect() calls. `bun tsc --noEmit` clean (0 errors, whole-repo). `registry.ts` diff is 1 import + 1 array entry — zero modification to any existing registration, no plausible regression surface. DDD clean: new file lives in `interface/mcp/tools/` (interface→infrastructure import expected, not a domain violation; `src/domain/` untouched). Security clean: no `process.env`/secrets, parameterized SQL (`$code`/`$year`/`$quarter`). `mock-guard.sh --files` PASS. `tool-registry.json`/`project-stats.json` internally consistent.

AC "verify ESC-5 fires on a known-escalation case" independently corroborated LIVE in production, not just unit-tested: `docs/agent-memory/notebooks/bctc-analyst.md` 2026-08-06 entries show ESC-5 actually firing TRUE for FPT via this tool (report_id `e8ea3df5` resolved, `get_bctc_refined` confirms 7/15 units confidence 0.45<0.50) — fired c141, re-fired 2026-08-06 post-TTL-expiry — and correctly resolving `report_id:null`+`existing_refine_status` diagnostic for HPG/VCB/DXG. The row's own "not yet deployed" rebuild note (2026-07-23) is stale, superseded by this live 2026-08-06 evidence and `bctc-analyst/flow/main.md`'s own live-verified 2026-07-24 (c119) annotation.

Full `bun test` run twice as supplementary due-diligence beyond this entry point's documented scope (touched-test/targeted-zone, not full suite — verified per that scope already above): run1 15114 pass/40 skip/71 fail/1 error/15225 total (711s, contended); run2 (partial, up to 4 concurrent `bun test` processes observed on host — a peer session's own full-suite run) showed only timeout-pattern failures on unrelated tools (`get_insider_transactions` Task 1146, `logVpsPush` 1858c). `grep -i getBctcReportId\|get_bctc_report_id\|registry.ts` across both run logs = zero matches in any failure/output line.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 800→800). `.head` untouched (unrelated active_task_id: `FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S39.

## cycle-523 · 2026-08-06 · FIX-MCP-DOCKERFILE-ENTRYPOINT-KNOWNHOSTS-REGRESSION — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `8884569a0` confirmed on main ancestry; `git show --stat` matches all 4 claimed files (Dockerfile +12L COPY/chmod/ENTRYPOINT trio, entrypoint.sh rewritten non-fatal degrade, new 210L `FIX-MCP-DOCKERFILE-ENTRYPOINT-KNOWNHOSTS-REGRESSION.test.ts`, `sshExec.ts` +9L comment-only). Re-ran the row's own test myself: 11/11 PASS (static Dockerfile wiring, source-guard for no `set -e`/no `exit 1`, live `sh`-subprocess behavioral checks against a fake `ssh-keyscan`). `bun tsc --noEmit` clean. `mock-guard.sh --files` on all 3 touched production files → PASS. DDD/security greps clean.

Full `bun test` run TWICE: 66 fail then 52 fail on an unchanged tree (dev's own stated baseline was 41) — the count itself moving between runs is the flake signature (many concurrent cowork agents committing live), not a regression; `grep -i "entrypoint\|sshExec\|KNOWNHOSTS\|Dockerfile"` across both full logs = zero matches anywhere.

Went beyond dev's own verification ceiling: dev's sandbox could not reach the VPS; THIS environment does (`nc -zv 125.212.251.27 22` succeeds). Read `docker logs` of the actually-running mcp-server container (image built today, post-fix) — known_hosts seeded cleanly across several real restarts today, zero WARN lines. Then `docker exec`'d in and ran `ssh -o StrictHostKeyChecking=yes -i /run/secrets/vps_ssh_key root@VPS "echo ..."` directly against that seeded known_hosts — SUCCEEDED. This live-proves the row's own acceptance clause "StrictHostKeyChecking=yes SSH succeeds" as a mechanism, clearing the PO ruling's gating condition for the still-deferred `accept-new`→`yes` code revert (scope item 2, deliberately not done in this commit). Did NOT flip `sshExec.ts` myself (`not_my_job`: production code is dev's) — flagged the now-unblocked follow-up in `status_note` for developer/PO to action; `send_telegram(work)` attempted but unavailable — no gateway/MCP tool grant this qa sub-session (Read/Edit/Write/Bash only, same known limitation as qa-2's own S38 CAP-REACHED note).

VERDICT: APPROVED, DONE_VERIFIED. Scope items 1+3 fully verified against committed code, tests, and live production behavior; item 2 is a real but separate out-of-scope-for-this-commit follow-up, not held against this row. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 800→800). `.head` untouched (unrelated active_task_id: `FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S40.

## cycle-524 · 2026-08-06 · FE-PG-BCTC-EVAL-_INDEX-FUNC-FIX — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had no top-level `commit`/`files[]` — derived `aaf487834` from review_note prose ("Committed aaf487834 (8 files, apps/frontend/)"), confirmed on main ancestry, author-date 2026-07-24 (row only just claimed by the drain today, 08-06 — pre-existing committed work awaiting QA). `git show --stat` matches all 8 claimed files exactly (domain/bctc-eval.ts, StatusBadge.tsx, EvalTable.tsx, dashboard.bctc-eval._index.tsx, api.bctc-eval.$.tsx, 2 test files, domain-model.md). Commit carries correct `Task:`/`AC:` trailers.

review_note flagged its own root-cause divergence from the PO narrative (loader never threw; real crash was a render-time TypeError in StatusBadge destructuring an undefined STATUS_CONFIG entry, because live mcp-server payloads only carry `stage_statuses` keys for stages actually computed while the domain type declared all 6 required) — treated as a reason for MORE scrutiny, not less, per house convention on self-reported divergence.

Re-ran everything myself, not trusted from prose: targeted tests (`bctc-eval-list.test.tsx`, `bctc-eval-detail.test.ts`, `bctc-eval-proxy-trailing-slash.test.ts`) 36/36 PASS. Full frontend suite: 2183/2185 PASS — the 2 failures (`QUE-REFERENCE-PAGE-detail.test.ts`, `QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.tsx`, both "exactly 2 own keys" on `QUE_DESCRIPTIONS`) confirmed pre-existing + out-of-zone: `git log` on `que-descriptions.generated.ts` shows last touch was `067e484d8` (unrelated hexagram-tooltip codegen rewrite that added a 3rd `hoverSummary` key), zero overlap with this commit's 8 files; reproduces identically with unrelated peer dirty-tree changes stashed out. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` PASS on all 5 touched production files.

Went beyond static checks: LIVE-verified against the actually-running frontend container (`vn-market-intelligence-mcp-frontend-1`, up ~12d, consistent with the commit's own "rebuilt + recreated, image ID verified" note). `curl` direct: `GET /dashboard/bctc-eval` → 200, no "Something went wrong" boundary text, real ticker rows render (MBB/HVN/HPG confirmed in response body); `GET /api/bctc-eval` (no trailing slash) → 200 (was 404 pre-fix). This corroborates the commit's specific mechanism claim (trailing-slash proxy bug + StatusBadge undefined-status crash), not just a generic "page loads."

DJ-GATE-1: dev-frontend's own journal entry present (`sprint-QUALITY-AUDIT-FRONTEND-COVERAGE-dev-frontend.md` line 10, `task-id: FE-PG-BCTC-EVAL-_INDEX-FUNC-FIX`) — gate satisfied independent of my own entry.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 795→795). `next_agent: pm` (review_note claims this closes 5 sibling NEEDS_REVIEW rows — pm/po to re-run the quality-audit checklist and flip those, out of this row's own scope). `task_release` for `task:FE-PG-BCTC-EVAL-_INDEX-FUNC-FIX` attempted but unavailable — no gateway/MCP tool grant this qa sub-session (Read/Edit/Write/Bash only, same known limitation as qa-2's S38/qa-3's S40 notes); dispatcher TTL (3600s) is the authoritative fallback release path per WF-1. New decision journal file created this cycle: `sprint-QUALITY-AUDIT-FRONTEND-COVERAGE-qa.md` §qa-S1 (task's own sprint id, matching the existing po/dev-frontend journal convention for this sprint — not the currently-active ambient sprint).
