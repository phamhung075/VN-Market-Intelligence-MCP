# QA — Notebook

## cycle-525 · 2026-08-06 · FIX-FB-GATE-CHECKD2-NONWAIVABLE-NUMERIC-BLOCK — CHANGES_REQUESTED, sent back (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commits `1b506cbdd`+`7b87ac372` confirmed on main ancestry; `git show --stat` matches all claimed files (3 flow docs + new `scripts/test-fb-gate-checkd2-nonwaivable.sh`). Diffs re-read directly (not trusted from review_note): main.md STEP 4b, weekly-recap.md STEP 3b, weekly-prediction.md STEP 4b all correctly rewrite waivability to per-check (Check-C prose only; Check-A/B/D/D1/D2/E/F/H/J non-waivable) — exactly as claimed.

Re-ran the developer's own regression harness live instead of trusting "GREEN 10/10" prose: `scripts/test-fb-gate-checkd2-nonwaivable.sh` → 8/10 pass, 2 FAIL, both in section (3) doc-prose assertions (checks against `docs/agents/fb-market-poster/flow/main.md` for `NON-WAIVABLE`/`Check-D2 fix protocol`/Check-C waive sentence). Root-caused: a LATER unrelated commit `8d165e8d6` (chore(agent-father/TE-T26): split main.md at MODE ROUTER, 2026-08-06, same day) shrank main.md 994L→88L, relocating this task's entire STEP 4b block into new `docs/agents/fb-market-poster/flow/daily.md:640-641,677-678` — grep-confirmed content present there verbatim, and weekly-recap.md/weekly-prediction.md cross-refs were already correctly repointed to `daily.md` by that split. NOT a substance regression of this fix — a test-script path drift (harness hardcodes the pre-split `main.md` location, scripts/test-fb-gate-checkd2-nonwaivable.sh:57/168/174). Assertions (1)/(2) (live-fixture Check-D2 BLOCK proof + no-regression PASS contrast) and (3a)/(3d) still hold; sibling `test-fb-gate-checkc-negation.sh` unaffected (6/6). `docs/social/fb-post-2026-07-25.md` confirmed untouched (0 diff), as claimed.

VERDICT: CHANGES_REQUESTED. Per verify-committed's own mechanical rule (any failing check → vc-changes), a currently-RED task-owned regression proof blocks done_verified even though root cause is a foreign later commit. Moved `task_board.qa[]`→`task_board.review[]` (status QA→REVIEW), `owner`/`next_agent`=developer (row had no `.owner`; used `updated_by` fallback), `redispatch_count` 0→1, `status_note` carries the exact 2-line fix needed (repoint MAIN_FLOW/grep targets from main.md to daily.md) via `jq`+`scripts/orch-apply.sh`. `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S43.

## cycle-525 · 2026-08-06 · FIX-PREDCLAIM-CREATIONPRICE-UNGATE-ZOD-CONTRACT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `6feec3ab1` confirmed on main ancestry; `git show --stat` matches all 4 named production files + 4 docs exactly. Read the full diff, not review_note prose: price lookup unconditional; `insertPredictionClaim` gained a `.strict()` Zod door requiring finite `creation_price`; sibling `intelligenceCycleJob.ts` fixed same way; stale docstring corrected. Deliverable (c) grep-verified: `insertPredictionClaim` is the ONLY INSERT into `prediction_claims`; UPDATE paths never touch those columns.

Re-ran independently: tsc 0 errors, targeted 6-file suite 96/96, full `bun test` 15139 pass/53 fail (clears row's own floor). Row's own AC-1 (live-endpoint proof) flagged "PENDING-REBUILD" — did not trust at face value: container rebuilt 12 days AFTER this commit (unrelated reasons), live code byte-identical to HEAD, `curl :3000/api/prediction-claims` shows ids 18-30 all non-null creationPrice (full reversal of pre-fix run), pre-fix NULL rows still correctly "excluded". AC-1 SATISFIED, not deferred.

VERDICT: APPROVED, DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via jq+orch-apply.sh (conservation OK). `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S44.

## cycle-525 · 2026-08-06 · FACTORY-TECHANALYSIS-delete-orphaned-ts-service — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `099afddd3` confirmed on main ancestry; `git show --stat` matches all 5 claimed `files[]` (index.ts, domain/services.ts, application/usecases.ts, infrastructure/calculator.ts, interface/handlers.ts) + siblings (domain/models.ts, domain/repositories.ts, infrastructure/repositories.ts) + 4 docs + 2 Go comment updates. Did not trust review_note prose — independently re-ran the row's own `dod`: repo-wide `grep "technical-analysis/src"` → zero live import hits (only historical archive-JSON prose); `src/`+`tsconfig.json` confirmed gone; `package.json` now carries only `build:dashboard`, zero `hono` dep.

`go build ./cmd/server/...` + `go vet ./...` clean; `go test ./...` 12/12 packages ok (exact match to claim). `mock-guard.sh --files` on all 5 claimed (deleted) paths → PASS ("no production source to scan", correct for a deletion). Dockerfile confirmed untouched (still `COPY cmd/ pkg/ api/` only); docker-compose `technical-analysis:` block references Dockerfile context only. Sibling same-day commit `448b41b0a` (unrelated CI dead-code-gate task) independently corroborates clean post-deletion state. No orphaned `__tests__`/`.test.ts` remain anywhere under `apps/technical-analysis`.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 10→9, done_verified 9→10, conservation OK task_total 790→790). `next_agent: pm`. `.head` untouched (active_task_id pointed at unrelated `DEFLAKE-VNSTOCK-3STATEMENT`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S45.
