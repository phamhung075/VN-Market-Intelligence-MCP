# QA — Notebook

## cycle-373 · 2026-07-03 · FIX-FB-DAILY-FIRER-CLI-FLAG — PASS (bash-script scope, no test suite)

Verified developer's 1-line fix to the launchd fb-daily-firer backstop (commit b19ac43c5, deletes `--no-update-notification` at scripts/cowork-fb-daily-firer.sh:138; 66ed3df09 journal-only) | Verdict: APPROVED. Per task scope, did NOT run the full TS suite (irrelevant to a bash script, zero test coverage). `git show b19ac43c5 --stat` = exactly 1 file, 1 deletion, the deleted line is `--no-update-notification`. `bash -n` clean (0 syntax errors). Installed CLI (`claude --version` 2.1.198): `--help` shows `--dangerously-skip-permissions` (grep -ic 2) and zero hits for `--no-update-notification` — confirms genuinely unsupported, removal correct not a version-skew guess. Live-reproduced the incident verbatim by re-adding the flag: `error: unknown option '--no-update-notification'` matches `fb-daily-firer-error.log` exactly. `grep -rn "no-update-notification" scripts/ launchd/` → 0 hits, no other broken caller. Root-cause doc `docs/signals/router-fbfirer-dead-cli-flag-20260703.md` matches the diff 1:1; secondary items (double-fire cadence, log tee-noise, untracked-log gitignore) correctly left out of scope. Deliberately did NOT invoke the corrected flag set live against the real flow path (would trigger a genuine fb-market-poster publish side-effect, out of scope for a flag-syntax check). DJ: sprint-MERGE-MONEY-RADAR-INTO-MOMENTUM-qa.md §qa-S3 (ad-hoc task, resolved to the active sprint's journal per convention). Report: reports/TASK_REPORT_FIX-FB-DAILY-FIRER-CLI-FLAG.md. Board flip left to router — did not touch orch-state.json myself.

## cycle-372 · 2026-07-03 · FIX-BCTC-BANK-BS-COLUMN-ORDER — PASS (deep-verify)

Deep-verify of dev-mcp-server's composite bank-form fix (commits d69b13f41 code + e73a53688 docs, already on main, superseding the failed FIX-BCTC-BANK-BS-SECTION-CLASSIFIER round from cycle-370) | Verdict: PASS. Commit-scope re-check: exactly the 7 claimed files. tsc --noEmit clean. Targeted: built own 23-file superset (grep on every changed symbol: refinedMarkdownParser/bctcFormType/bctcRowRepair/isBankFormFromRows/parseVnNumber/resolveColumnLayout/detectSection/SECTION_HEADERS/FOLDED_SECTION_KEYWORDS/TCTDHN/B02a/bankForm) → 389 pass/0 fail/1172 expect; FIX-BCTC-BANK-BS-COLUMN-ORDER.test.ts standalone 16/0/54 expect (report_id ctg-96e36139-real-e2e) — matches router's re-run exactly. Full suite: 14230 pass/42 skip/65 fail/6 errors/44638 expect across 1168 files (620.76s), then the known Bun-1.3.13 C++ teardown panic — used the documented ceiling testBaselineFail=348 (65 << 348), not the stale baseline_pass=9408 the router flagged. Regression-domain check done two independent ways: (1) grepped all 65 `(fail)` lines for bctc/bank/refinedMarkdown/parseVnNumber/column-order/detectSection keywords → 0 hits; (2) awk-mapped every fail line to its nearest preceding test-file header → all 65 land in pre-existing network/timing-flaky suites (pollNews ×~15, VPS-proxy/logVpsPush ×13, insider-transactions ×9, foreign-flow ×6, market-cap ×3, climate-signal ×4, telegram, MCP-SSE registration). One filename false-positive caught and cleared: `1405b-bctc-vps-fixes.test.ts` (bctc in name) — inspected directly, its 3 fails are in the "FIX 2 — logVpsPush" block (vps_push_log DB race), file imports only vpsPushLogStore/vpsHealthPoller, zero import of any changed file. DDD/security/mock-guard all clean (mock-guard exit 0). DJ-GATE-1: dev-mcp-server's sprint-FIX-BCTC-BANK-BS-COLUMN-ORDER-dev-mcp-server.md carries task-id — gate satisfied. Appended [QA] Review Record to the handoff. Deploy-gated follow-up noted (post-deploy finalize_bctc_refine on live CTG report still required to unfreeze total_assets) — out of this gate's scope. DJ: sprint-FIX-BCTC-BANK-BS-COLUMN-ORDER-qa.md §qa-S1. Report: reports/TASK_REPORT_FIX-BCTC-BANK-BS-COLUMN-ORDER.md. Board flip left to router per explicit dispatch boundary — did not touch orch-state.json myself.

## cycle-371 · 2026-07-03 · FIX-BCTC-ENRICHER-STUCK-BACKLOG — PASS (final DoD gate)

FIX-BCTC-ENRICHER-STUCK-BACKLOG behavioral DoD sign-off (review→done_verified gate, not a code round, consumed no coding WIP slot) | Verdict: PASS. RAW re-verified every dev-mcp-server review_note claim independently, did not take it on faith: (1) deploy — `git merge-base --is-ancestor d92801332 HEAD` = true (commit dated 2026-07-02T00:11Z, well before build); `docker inspect` image sha256:a169f5e2b7e2 healthy/RestartCount=0/StartedAt=2026-07-03T04:38:34Z matches board exactly; mem 698.6MiB/3GiB=22.7% (up from the reported 15.55% ~1h prior — natural growth, still far below the pre-fix 96.81%/1.936GiB@2GiB scar; A-30 mem-leak relief holds). (2) live DB (bun:sqlite inside the running container, no sqlite3 CLI present, table bctc_vps_queue in /app/data/market.db): row 255868 (ACV) attempts 0→5 (exceeds the claimed →2), last_attempt NULL→'2026-07-03 05:45:10', continuously advancing across 3+ `*/15` cron_job_runs cycles (05:15×2/05:30/05:45 all success) — 2nd independent proof row HVN (255882) same pattern (attempts→5, last_attempt updating). Idempotency: re-ran the migration's exact 21-ticker SELECT predicate (status='url_not_found' AND last_attempt IS NULL AND period_year=2025/Q4 AND created_at window AND ticker IN 21) → 0 rows matched, confirmed idempotent. 18/21 rows now carry a real HSX source_url (exceeds review_note's claimed 4/21 — more cycles elapsed since); ACB/BID progressed further to enrich_failed (URL found, downstream PDF parse failed — a different code path, correctly covered by this fix's newly-extended Arm-2). Non-blocking anomaly logged: CTG (255871) still attempts=0/last_attempt=NULL after 3 observed cycles while its 20 siblings all advanced — flagged as a follow-up, not a DoD blocker (out of scope: this fix is about last_attempt stamping on terminalizing statements, not per-ticker discovery success). (3) `bun test FIX-BCTC-ENRICHER-STUCK-BACKLOG.test.ts` 8/0 fresh (not relayed) + `bun tsc --noEmit` 0 errors. DJ: sprint-FIX-BCTC-ENRICHER-STUCK-BACKLOG-qa.md §qa-S1. Report: reports/TASK_REPORT_FIX-BCTC-ENRICHER-STUCK-BACKLOG.md. Board flip left to router (dispatch contract) — did not touch orch-state.json myself.

**Retention note:** cycle-370 (3-item review-lane gate: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED) and older pruned this write per AC-2 (keep last 3). Full detail preserved in `docs/agent-memory/decisions/sprint-FIX-BCTC-BANK-BS-SECTION-CLASSIFIER-qa.md`, `sprint-FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION-qa.md`, `sprint-FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT-qa.md`, `reports/TASK_REPORT_FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION.md`, `reports/TASK_REPORT_FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT.md`, and prior git history of this file.
