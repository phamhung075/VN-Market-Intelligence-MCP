# QA — Notebook

## cycle-727 · 2026-08-14T13:04:00Z · FIX-PO-TRIAGE-SIGNALS-CIRED-TEMPLATE-STATUS-TODO-REJECTED-BY-VALIDATOR (agent-father, commit `c1c97e340`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit_sha CORRECTED)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Row's stored `.commit_sha` (`b164e37781f0...`, 40-char, real+on-main) FAILED check-3: `git show --stat` on it is an unrelated agent-father notebook-merge commit (`STEP-COWORK-*-agent-father.md`/`notebooks/agent-father.md`/a context-bloat signal json) — touches ZERO of the row's claimed po/flow files. The prior dev-team review_note's own cross-check ("commit `cb6ba9567` cherry-picked, sha above post-merge, confirmed, diff matches exactly") is therefore unverifiable as stated against that hash — flagged for fleet visibility, not re-litigated (underlying work IS real, just mis-pointed).

Derived the real commit via `git log --all --grep=<task-id>` (exactly 1 hit, zero ambiguity): `c1c97e3402671d5d95cae16fef36ffe835279d69`, confirmed `git merge-base --is-ancestor` main, `git show --stat` touches exactly `docs/agents/po/flow/{triage-signals,channel-audit,sprint-kickoff}.md` + new `scripts/audits/po-triage-mint-backlog-status-lane-coherence-verify.sh` — matches the row's AC exactly (5x `status:"TODO"`→`"BACKLOG"` across the 3 files). `cb6ba9567` (byte-identical author/date/message, different tree) is a parallel-worktree duplicate NOT on main ancestry — dead-end, correctly superseded by `c1c97e340`'s landing.

Live-verified on main HEAD directly, not trusting any single commit's diff: zero `status: "TODO"` tokens remain across all 15 `docs/agents/po/flow/*.md`; every `.task_board.backlog[]` mint append in that directory now writes `status: "BACKLOG"`; `orchStateSchema.ts` `LANE_ALLOWED_STATUSES.backlog` independently re-read = `{BACKLOG, BLOCKED}` unchanged (out-of-scope guard honored). Re-ran the fix's own regression harness fresh (not trusted from prose): `scripts/audits/po-triage-mint-backlog-status-lane-coherence-verify.sh` → 42/42 PASS, real live `orch-state.json` confirmed untouched by that fixture-harness run (git status clean on that path both before and after).

No production TS/JS source touched (pure prose templates + 1 new bash audit script) — `bun test`/`tsc --noEmit`/`mock-guard.sh`/DDD/security scans correctly N/A, not skipped without basis; the purpose-built regression script IS the targeted verification for this template-vs-schema-validator artifact class (no existing bun test suite covers markdown mint-template prose).

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S130.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE, but with a data-integrity CORRECTION persisted: `.commit_sha` rewritten from the wrong `b164e377...` to the real `c1c97e340...` in the same write. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`, `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 27=27). `.head.active_task_id` is a DIFFERENT row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, same QA-Drain batch) — left untouched. `next_agent` omitted entirely on the DONE_VERIFIED row (terminal-row convention). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-PO-TRIAGE-SIGNALS-CIRED-TEMPLATE-STATUS-TODO-REJECTED-BY-VALIDATOR` and is the authoritative release path (per spawn prompt, explicitly not this session's responsibility).

## cycle-726 · 2026-08-14T13:02:15Z · TE-T17 notebook prune-bypass (developer, commits `32529e57f`+`29e17fca8`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, PO stale-triage re-queue (2026-08-14T09:08Z note: prior claim 2026-08-05 produced zero durable QA output over 9 days, TTL-expired, re-queued). Row carried no `.commit`/`.files[]`; used PO's cited commits `32529e57f` (fix) + `29e17fca8` (memory), both confirmed real + on `main` ancestry, `git show --stat` matches every path the developer's own status_note names.

RAW-re-verified all 5 AC on LIVE main HEAD, not trusted from prose: (1) ops.md 1197L→29L at landing, currently 194L — durably under the 200L cap 3 weeks later, mechanism holding not just point-in-time-true. (2) 23 incident bodies present under `docs/incidents/`, live one-line pointer in ops.md. (3) `notebook-linecap-sweep.sh` wired into `code-janitor/flow/main.md`'s "every scan" section (:123-145) — own test run 11/11 PASS. (4) `ops/flow/main.md`'s BLOCKING `wc -l` pre-commit gate present (:91-98). (5) `.test-notebook-prune-debug/` confirmed absent.

Also ran the paired regression suites myself: `notebook-auto-prune.test.sh` 10/10 PASS; `test-notebook-auto-prune.sh` 8/9 PASS — 1 FAIL (Test 9, bash-vs-zsh vote-inversion). Traced root cause instead of either absorbing or dropping it: `git show 32529e57f:...notebook-auto-prune.sh` has zero `BASH_SOURCE`/`hook-guard` refs — the breaking `source lib/hook-guard.sh` line was added by a LATER unrelated commit `f4d35b5df` ("fail-loud crash discriminator for load-bearing hooks"), postdating TE-T17's own commit. Not a TE-T17 regression — this sub-session has no `mcp__gateway__call_tool` binding (INV-GATEWAY-1, no `send_telegram` reach), so minted a new `task_board.backlog[]` row `FIX-NOTEBOOKAUTOPRUNE-HOOKGUARD-BASHSOURCE-ZSH-BREAK` myself instead (P2, owner/next_agent developer, zone `scripts/agents-flow/`) — durable tracking, did not block this row on it. `mock-guard.sh` PASS (no production TS/Go source, all `.sh`/`.md`), security greps clean.

DJ: `sprint-TOKEN-ECONOMY-AUDIT-qa.md` STEP qa-S14.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field, attached `verification.raw_probe` on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 740=740, signal_total 27=27). `.head.active_task_id` is a DIFFERENT row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, same QA-Drain batch) — left untouched. `next_agent: pm` set on the DONE_VERIFIED row. This sub-session's tool grant has no `mcp__gateway__call_tool` binding (INV-GATEWAY-1) — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:TE-T17` and is the authoritative release path (per spawn prompt, explicitly not this session's responsibility).

## cycle-725 · 2026-08-14T13:01:20Z · FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H (dev-mcp-server, commit `88863a82d`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`, no `.commit`/`.files[]` field on the row (predates the drain) — derived commit via `git log --all -- <new test file named in the row's own review_note>`: `88863a82d`, confirmed on `main` ancestry. `git show --stat` matches all 4 files the review_note claims: `freshnessSlaChecker.ts`, `freshnessSlaMonitorJob.ts`, new `FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H.test.ts`, `1920i-freshness-sla-extension.test.ts`.

Did not trust dev-mcp-server's own review_note claims — re-ran everything. New test file: own `bun test` → 8/8 pass (AC-1 bidirectional 20d-ok/40d-breach both via `checkSignalSla` and the full `checkDataFreshnessSla` pipeline; config pin at 43200min/30d; fixed-not-calendar-derived threshold check; exact-boundary 43200/43201; anti-false-green test pinning the real production 76067min figure still breaches CRITICAL post-fix). Sibling `1920i-freshness-sla-extension.test.ts` → 23/23 pass, confirming the row's `TC-6f` edit did not regress anything else in that file. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` on both touched production files → PASS, no fabricated-data patterns.

Read the diff at source, not the commit prose: `DEFAULT_SLA_CONFIG` `signal_quality_audit.defaultThresholdMinutes` 2880→43200 confirmed exactly as claimed; both misleading "tracks the monthly cron" comments corrected in-place in `freshnessSlaChecker.ts` (x2) and `freshnessSlaMonitorJob.ts` (x1) to state the real writer mechanism (`post_agent_signal`'s `price_confirmation`/`urgent_news`+`confidence` path via `insertSignalQualityAudit()`, single call site) — the row's original title/hypothesis (monthly-cron misclassification) was itself wrong, and the review_note's own root-cause correction was independently re-verified true at source, not rubber-stamped.

AC-3 (row explicitly required a live probe/follow-up, not a silent close): independently confirmed both resulting rows are live on the current board, not narration-only — `FIX-MONTHLYSIGNALQUALITYAUDITJOB-MISSED-JULY-RECOVER-GUARD` (`backlog[]`, P2, dev-mcp-server, documents the confirmed-missed July cron fire) and a PO-filed residual `FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT` (currently itself in `qa[]`, P2, dev-mcp-server, separate QA cycle — flags that the 30d threshold was sized off 6 lifetime rows that may be noise if the write path is unreachable).

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S129.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field, `verification.raw_probe` attached on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 740=740, signal_total 27=27). `.head.active_task_id` is a DIFFERENT row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, same QA-Drain batch) — left untouched. This sub-session's tool grant has no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H` and is the authoritative release path (per spawn prompt, explicitly not this session's responsibility).
