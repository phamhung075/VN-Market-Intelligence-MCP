# QA — Notebook

## cycle-740 · 2026-08-14T15:02:41Z · FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE (architect, commit `7298e1ffb`) — Direct-Commit Verify, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row predating the drain (no `.commit`/`.files[]` wiring). Derived commit via `git log` on both claimed `files[]` entries: `7298e1ffb20ee2e8242c58cae3a8d9431d06177f` (2026-07-30T10:35:02+02:00, matches review_note's own claimed "2026-07-30T08:40Z"), confirmed real + on `main` ancestry.

`git show --stat` matches `probe.sh` but NOT `emit-audit-signal.sh` — zero diff to that file in this commit despite it being in the row's own `files[]`. Did NOT trust the review_note's own explanation blindly: read `emit-audit-signal.sh` in full myself — confirmed it is a pure named-arg passthrough, zero hardcoded "unreachable"/A-12-specific wording anywhere; the debounce fire/skip decision correctly lives in the consumer (`tier1-overrides.md`), architecturally this script never needed to change. Same "flagged deviation, independently re-confirmed accurate, not a silent scope-drop" shape as cycle-738's `assembleEveningSummary.ts` zero-diff.

A1 confirmed live: `probe.sh:343` `--max-time 5` (was 3). A2 confirmed: `_classify_curl_exit()` (`probe.sh:34-42`, CLIENT_TIMEOUT/CONN_REFUSED/DNS_FAIL/EMPTY_REPLY/CURL_ERR_n) applied only on the transport-failure branch, real non-200 HTTP unchanged. Ran `probe.test.sh`: today's file is 24/24 PASS (extended by 2 later, unrelated A-30 commits `6ff38d27e`/`a9e03849a`) — re-diffed the commit's OWN additions and confirmed exactly T1-T7 landed here, matching the claimed "7/7 PASS" at commit time. A3 confirmed: N=3-consecutive transport-error debounce wired into new `tier1-overrides.md`, applies only to transport-classified FAILs, landed in the SAME commit as A1/A2 (never alone, per its own scope carve-out). A4 confirmed: the "unreachable" gloss fixed at its real template source (`tier1-probe.md`'s Health-Endpoints + Emit-per-failure sections) — repo-wide grep of remaining "unreachable" hits (`main.md`/`handlers.md`) confirmed unrelated checks (docker-daemon/cron-status/DB), correctly untouched. A5 spot-checked: zero live A-04/A-13 hits repo-wide outside the cited archived brief — holds.

`bash -n` clean both `.sh` files. `mock-guard.sh --files probe.sh`: PASS (bash not scanned as prod source, same precedent as cycle-736). DDD/secret/process.env greps on both files: zero matches.

DJ: rolled `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` — already over its 36000-byte cap (37618B/169L, well under the 600L line-axis but over the byte-axis) BEFORE this cycle's own entry; appended `CAP-REACHED` sentinel, rolled to new `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md`, wrote STEP qa-S141 there (a peer session concurrently used the same file for its own STEP qa-S142 — both entries landed cleanly, no collision). Could not `send_telegram` for the CAP-REACHED event — no MCP/gateway tool grant this session (Read/Edit/Write/Bash only), same structural gap the row's own developer flagged for itself.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field (left the unrelated PO-triage `status_note` field untouched); `commit_sha` + `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write. Dry-ran the jq transform to a scratch file first — caught the live board had moved between reads (concurrent peer QA-drain sessions, `done_verified[]` count drifted 23→24→26 across my own read/dry-run/write) and re-read fresh immediately before the real write to avoid a CAS-stale candidate. `orch-apply.sh`: Stage0+1 PASS (pre-existing Stage 1g dependency-report noise only, unrelated), conservation OK (task_total 728=728, signal_total 24=24). Re-read the live file after write: row physically absent from `.task_board.qa[]`, present in `.task_board.done_verified[]` with `status:"DONE_VERIFIED"`. Non-blocking note flagged in the review record: the row's `.files[]` field is stale vs the actual commit diff. Task-lock held by the dev-team dispatcher per spawn instruction — no `task_release` call made here (also no MCP/gateway grant to make one).

## cycle-739 · 2026-08-14T15:02:40Z · TE-T33 (developer, commit `82a69523807289a00fc6c8edae6769201a914b78`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row fully self-contained (`commit`, `files[]`, `owner`). Commit `82a695238...` confirmed real, on `main` ancestry (`git merge-base --is-ancestor` PASS); `git show --stat` matched all 7 claimed `files[]` exactly, zero extra/missing (2 new scripts, 4 doc edits, 1 shared-hook edit).

Re-ran everything live, not trusted from commit prose: `cold-archive-sweep.test.sh` 14/14 PASS (exact match). Re-ran the 3 sibling suites the commit claims exercised by its shared-hook edit on `notebook-auto-prune.sh` (new opt-in `NOTEBOOK_PRUNE_EXTRA_GOVERNED_PATH` guard): `memory-prune-sweep.test.sh` 12/12 and `decision-journal-archive.test.sh` 26/26 both exact matches; `notebook-linecap-sweep.test.sh` now 11/11 (claim said 7/7 at commit time) — diffed and traced to a LATER unrelated commit `7fd919c15` adding 4 test cases to that file after TE-T33 shipped, not a regression of this row. Also ran `notebook-auto-prune.test.sh` itself (the file TE-T33 actually modified): 11/11 PASS — new guard is default-empty/no-op confirmed in the diff (belt-and-suspenders empty-pattern check). No `.ts` touched → DDD/tsc N/A; security greps (`process.env`/secrets) clean on all 3 touched `.sh` files; `mock-guard.sh` PASS (no production TS/Go source to scan). Doc wiring cross-checked live: code-janitor Cold Archive Sweep section correctly slotted, dev-standards.md CANONICAL pointer present, dev-team's 2 DJ-GATE-1 grep-pattern fixes (`sprint-*-*.md`→`sprint-${SPRINT_ID}-*.md`) now match qa/pm's own already-correctly-scoped pattern (grep-confirmed both files).

DJ: `sprint-TOKEN-ECONOMY-AUDIT-qa.md` STEP qa-S15 (file hit byte-cap 36398/36000 on this write — lines still 500/600 — `CAP-REACHED` marker appended; next entry for this sprint/agent rolls to `-2.md`; bug-channel alert skipped, no MCP gateway binding in this sub-session per INV-GATEWAY-1, noted here instead of silently dropped).

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write (row not RC-VERIF-grandfathered — new entrant to `DONE_VERIFIED`, not one of the 33 frozen legacy ids). Dry-ran the jq transform to a scratch file first and inspected it (lane lengths qa 10→9, done_verified 24→25; row absent from `qa[]`, present in `done_verified[]` with correct fields) before applying. `orch-apply.sh`: Stage0+1 PASS (pre-existing Stage 1g dependency-report noise only, unrelated), conservation OK (task_total 728=728, signal_total 24=24). Re-read the live file after write to confirm the physical lane-move, not trusted from exit code alone. Task-lock held by the dev-team dispatcher per spawn instruction — no `task_release` call made here (INV-GATEWAY-1).

## cycle-738 · 2026-08-14T14:50:00Z · FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION (dev-mcp-server, commit `8521c4a233`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`, no pre-existing `.commit`/`.files[]` drain fields but a fully self-contained `review_note` naming both commits. Confirmed `8521c4a233b2f02821bbc6e11ef7b0377b7c3840` (code+tests) + `ba4005738` (docs) real, on `main` ancestry, `git show --stat` matches every `files[]` entry plus 2 the row didn't list (`eveningSummaryJob.ts` also wired to the new wrapper; `1516-france-summary-foreign-flow.test.ts` AC-4 updated) — `assembleEveningSummary.ts` genuinely has ZERO diff, confirming the row's own flagged deviation (touching the frozen `foreign_net_vol <> 0` SQL filter would break `ZERO-PAD-TOPN` T-1/T-2) is accurate and correctly reasoned, not a silent scope-drop.

New `formatForeignFlowSectionOrUnavailable()` wraps the raw formatter, renders the canonical "Khối ngoại: Dữ liệu không khả dụng (pipeline tạm dừng)" line when `movers.length===0`, wired into both bulletin call sites; `franceSummaryJob`'s outer guard narrowed `length>0` → `!==undefined`. New test file: 11/11 pass, 18 expect — exact claim match. Row's own targeted 5-file sweep (1783+ZERO-PAD-TOPN+1516+1503+new): 47/47 pass — exact claim match. Went WIDER than the row's own claim: grepped every OTHER test file in the repo calling `formatFranceSummaryVI`/`formatEveningSummaryLines` (10 more files: 1519/1785/1512/1513/1383/1364/1552/1553/1450/1444) — 62/62 pass, zero regression across literally every production caller of the touched code.

`bun tsc --noEmit` 0 errors. `mock-guard.sh` on all 3 touched prod files PASS. DDD scan clean — both touched job files carry documented "Layer: interface/scheduler" headers, `infrastructure`/`application` grep hits are legitimate type/logger imports at that layer, zero domain-file violation; `foreignFlowSection.ts` itself has no imports at all. Security greps clean. `gen-project-stats.ts --dry-run`: toolCount=183/cronJobCount=88, exact claim match. Independently grepped every production (non-test) call site of the row's own load-bearing claim "`getForeignFlowMoversFn` confirmed test-only today" — `schedulerJobTable.ts`/`startScheduler.ts`/`orchestrateRecapCommand.ts` all call `runFranceSummary()`/`assembleEveningSummary({db})` with zero wiring of that param — claim holds. Full local `bun test` (own run, not the row's cited 15123/52 count): 15288 pass/40 skip/57 fail/48311 expect (678.68s) — within the documented 52-60 pre-existing flaky band cross-referenced against 15+ prior decision-journal precedents this repo; per standing `FIX-MCP-SUITE-HEALTH-BASELINE` literal 0-fail on the unscoped full suite is not the actionable gate here — the 15-file targeted+adjacent sweep is the valid net-new-regression check and it is fully clean.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S140.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field; `commit_sha` + `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write (row not RC-VERIF-grandfathered). Dry-ran the jq transform to a scratch file first and inspected it before applying — caught nothing wrong (an earlier same-session mixed-query probe threw a transient "Cannot index array with string" on the scratch file that did not reproduce on immediate re-query; treated as a read race, not a real structural fault — confirmed `type == object` and correct qa/done_verified lengths on clean re-reads before committing to the real write). `orch-apply.sh`: Stage0+1 PASS (unrelated pre-existing Stage 1g dependency-report noise only), conservation OK (task_total 736=736, signal_total 25=25). Re-read the live file after write: row physically absent from `.task_board.qa[]`, present in `.task_board.done_verified[]` with `status:"DONE_VERIFIED"`. Task-lock held by the dev-team dispatcher per spawn instruction — no `task_release` call made here.
