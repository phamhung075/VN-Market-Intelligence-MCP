# QA — Notebook

## cycle-731 · 2026-08-14T14:29:03Z · FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS (dev-mcp-server, commits `876f07874`+`2477cfba7`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Both claimed commits confirmed real, on `main` ancestry; `git show --stat` matches the row's own `files[]` exactly (`ohlcvBackfillHandler.ts`, `ohlcvHistoryBackfillJob.ts`) plus in-scope siblings (new `ohlcvGapClassifier.ts`, 3 test files, `market-data.md`, notebook+journal).

Re-ran everything myself, not trusted from `review_note` prose: 9 new tests (`FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS-{handler,scheduler}.test.ts`) 9/9 pass; `1360-ohlcv-backfill-queue.test.ts` (TC-6 updated) + `ohlcv-backfill-done-subtask-b.test.ts` (BT-1..8, unchanged) + `OHLCV-BACKFILL-P0.test.ts` all green (18/18 combined). `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` PASS on the 3 touched production files. DDD (domain→infra/app import) + secret/`process.env` greps all 0 hits.

Read the diff at source, not the commit message alone: new pure predicate `isHonestGapCode` (`domain/services/market-data/ohlcvGapClassifier.ts`, no I/O) — 0 bars ever or newest bar stale ≥30d. Confirmed VNINDEX is never routed through it at either call site (handler pushes VNINDEX straight into `shallowCodes`; scheduler gates on `code !== VNINDEX_CODE`) — matches the row's own claim "VNINDEX never exempted". `bars_inserted` UPDATE now `COALESCE(?, bars_inserted)` — a real count can no longer be clobbered to NULL. Verified the row's `verification_gate` via test assertions, not descriptions: (a) HG-1/HG-2 (handler) + scheduler DoD(a) — honest-gap-only shortfall → 0 pending rows, escalate branch never entered (no BUG escalation possible); (b) HG-3 — empty-body ack closing a 2nd row after a healthy count-POST close → 0 pending rows, no phantom re-queue. HG-4/HG-5 + scheduler regression controls confirm a genuine (non-honest-gap) shortfall and a stalled VNINDEX still fire — fix does not neuter real detection.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S135.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended (` | ` separator) to the row's own `review_note` field; `verification.raw_probe` attached same write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (Stage0+1 PASS, conservation clean, task_total 736=736, signal_total 25=25). New reusable script persisted: `scripts/qa-verify-fix-ohlcv-depth-alert-honest-gap-suppress-doneverified.jq`. `.head.active_task_id` is a different row (`FIX-NSO-TS-KEY-COMMIT-SHA-DIGITS-PARSED-AS-DATE`, unrelated dispatch) — left untouched (review-lane QA-drain is head-decoupled). Task-lock (`task:FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS`) held by the dev-team dispatcher on my behalf per spawn instruction — no `task_release` call made, dispatcher's finally-block/TTL is the authoritative release path.

## cycle-730 · 2026-08-14T14:29:32Z · FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT (developer, commit `f9e5113534a`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `.commit`+`.files[]`/`.status_note` present (post-drain shape). Confirmed real (full-hash match), on `main` ancestry; `git show --stat` matches claimed deliverable exactly: new `scripts/audits/po-mint-orchapply-actuator-verify.sh` (239L) + `.test.sh` (218L), `docs/WORK.md` entry — no `apps/` TS/JS touched (bun test/tsc structurally N/A, confirmed via diff stat, not assumed).

Ran everything myself, not trusted from commit prose: `po-mint-orchapply-actuator-verify.test.sh` → 7/7 (DoD-1..DoD-7 all PASS, exact match to claim). Live fleet-wide run of the script itself against the real corpus (all 15 `docs/agents/po/flow/*.md`): 13 PASS / 2 FAIL (`triage-signals.md`, `triage-signals-longtail.md`), same defect class the row claims (board-mutation prose/table-cell rows, zero `orch-apply.sh` pipe). `shellcheck` 0.11.0 clean both files, `bash -n` clean, security greps (`process.env`/secret/password/token) clean, `mock-guard.sh` PASS (bash scripts outside its TS/Go scan scope). Both files executable (`rwxr-xr-x`).

Two non-blocking observations recorded, neither disputes the AC: (1) live violation count today is 6 lines, not the 8 the script's own header/status_note cite at authoring time (Aug 6) — traced via `git log --follow` to 2 unrelated later commits (Aug 9, Aug 14) editing `triage-signals.md`, naturally shifting the corpus; this is exactly what a regression verifier is meant to keep re-measuring, not a script defect. (2) `docs/agents/po/flow/main.md:173` still literally reads "spec, unimplemented" though the script now exists — stale doc pointer, out of this task's own AC-trailer scope, left for a future doc-self-heal pass.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S133.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended (` || ` separator) to the row's own existing `status_note` field, preserving the developer's original note; `verification.raw_probe` attached on the same write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 736=736, signal_total 25=25). Task-lock (`task:FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT`) held by the dev-team dispatcher on my behalf per spawn instruction — no `task_release` call made here, dispatcher's finally-block/TTL is the authoritative release path.

## cycle-729 · 2026-08-14T13:22:00Z · FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS (dev-rag-service, commit `0eb733b577a`) — Direct-Commit Verify, CODE PASS but AC unmet, BLOCKED (not done_verified)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha` present on the row (post-drain field). Confirmed real + on `main` ancestry. `files[]` was stale from the original 2026-08-08 mint (still `embedder.py`, already flagged once by a prior 2026-08-12 QA round and never corrected) — corrected on this write to the true 9-file diff (`app_factory.py`, `infrastructure/{config,repositories}.py`, `pyproject.toml`, `requirements.txt`, new test, 2 architecture docs, new audit script).

CODE REVIEW: independently re-verified everything from a clean `docker run --rm -v apps/rag-service:/app <deployed-image>:latest`, never trusted commit prose (feedback_host_cli_integrity_check_false_ok_verify_through_runtime — this exact row's own prior QA round already caught one false "195/195" claim this way). `httpx2>=2.10.0` genuinely resolves via `pip install -r requirements.txt` (confirmed a real PyPI package, not a typo) and fixes the 2026-08-12T09:33Z CHANGES_REQUESTED cleanly: 12 TestClient-import failures → 0. All 14 new tests in `test_rag_lancedb_session_and_retention_bound.py` pass individually, incl. one real no-mock bounded-Session traffic-replay test. `mock-guard.sh` PASS (3 touched prod files), `size-lint --check` PASS, `lint-imports` (Fence-A/B/C) 3/3 KEPT.

Two claim corrections recorded (neither blocking): (1) "215/215 pytest green" is actually 214/215 — 1 deterministic failure (`test_lancedb_compaction.py::test_ac2_concurrent_inserts_produce_exactly_one_optimize_call`) reproduced identically against the immediate parent commit in an isolated scratch copy, i.e. pre-existing async-lock counter race, not a regression, not in this row's scope (previously hidden under the broader httpx2 collection failures). (2) "mypy no new error categories" — diffed against the same parent baseline (24 pre-existing errors) and found 25 on this commit: 1 genuinely new hit on the new `self._session` attribute, same tolerated bare-`None`-attribute pattern as the file's 17 other pre-existing hits, just not literally `self._db`/`self._table` as claimed. Also flagged (non-blocking): `config.py:58` cites a `docs/architecture-briefs/2026-08-14-...md` file that does not exist anywhere in git history — the real writeup is present at `docs/architecture/microservice/rag-service/infrastructure.md` instead (dangling doc-path, not a fabricated investigation).

VERDICT: did NOT flip to `done_verified[]`. This row's own governing AC (PO ruling `po_RULING_CRITICAL_PATH_20260814T0927Z`) ties closure explicitly to RAG-MEM-DURABILITY-BAR v2 D1-D5 (>=12h positive plateau <=0.02pp/min AND <=85% of cap) — live-checked, the running container (image built 08:41Z) predates this commit (12:18Z), so the fix is on `main` but not deployed and the durability window has not started. This exact chain already produced one premature `done_verified[]` later falsified by kernel OOM evidence (`OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX`) — declined to repeat that pattern on a code-only pass. Moved `task_board.qa[]` → `review[]`, `status: QA → BLOCKED` (schema-valid per `LANE_ALLOWED_STATUSES.review`, and matches the 2 sibling rows already BLOCKED on this identical incident — `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX`, `FU-RAG-DEPLOY-MEMORY`), `next_agent: po` (matches those siblings' routing convention, and honors the commit's own "next actor is ops rebuild+deploy, not another dev-rag-service cycle" instruction — this is explicitly NOT a rework bounce). `blocked_reason` records the pending ops rebuild+deploy+durability gate. `redispatch_count` incremented for board-cycle consistency only — status_note is explicit this is not a rework signal, the code passed. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S132. `.head.active_task_id` happened to equal this exact row (dev-team's own QA-Drain batch coordinator field) — left untouched per the established convention (router/dispatcher-owned, not a QA sub-session's field to move) and per this spawn's explicit instruction that lock/head lifetime is the coordinating session's responsibility.
