# QA — Notebook

## cycle-662 · 2026-08-12T09:39:00Z · FACTORY-APP-split-pollNews (dev-mcp-server, real code) — Direct-Commit Verify, CHANGES_REQUESTED (commit `0f23a703f`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain). Commit `0f23a703f` confirmed real, on main ancestry, touches all 8 files it claims. Read the diff on disk: 5 helper clusters (`types.ts`/`insiderDetectors.ts`/`signalDedup.ts`/`defaultFetchers.ts`/`dbHelpers.ts`) genuinely extracted from `pollNews.ts`, zero-external-call-site-change claim confirmed by grep (every consumer import — `pollNews`/`PollNewsResult`/`SourceFetchers`/`detectInsiderFamilyBuying`/`detectInsiderSelling`/`_resetAllDarkAlert`/`PollNewsOptions` — still resolves unchanged from `pollNews.ts`); dropped `defaultTradingEconomicsFetcher` confirmed zero remaining call sites repo-wide. `bun tsc --noEmit` clean; `size-lint-justification.sh --check` clean (only pre-existing untouched `transport.ts` fails, matching the commit's own disclosure); `mock-guard.sh` PASS; DDD (application may import domain+infra, no forbidden `interface/` import) + `process.env`/secret greps clean. Re-ran all 21 pollNews-touching test files myself in isolation (not trusted from the commit's "13 files" claim, which undercounts): 109/109 pass — the 2 files (`1324-push-news-all-sources`, `FIX-POLLNEWS-COUNTER-CONSERVATION`) that flaked when I ran the batch under 2-min contention both passed clean standalone, matching the commit's own pre-split-HEAD reproduction claim.

ISSUE (why CHANGES_REQUESTED despite all mechanical checks passing): the backlog DoD/approach (`archive/backlog-detail.json`) names 5 SPECIFIC target modules — `insiderSignalDetector`(→`domain/`)/`sourceFetchers`/`sourceHealth`/`ingestEntries`/`buildSignals` — i.e. extraction of `pollNews()`'s own internal pipeline STAGES, with the explicit target "leave `pollNews.ts` a ~120L orchestrator" and "each module <=120L". What actually shipped: `pollNews.ts` is still 923L; the ~790L pipeline body (fetch/health-tracking → dedup/insert → cascade/alert-generation) is completely untouched — only peripheral clusters were moved, none of which correspond to the approach's named stage targets (`insiderSignalDetector` also landed under `application/usecases/pollNews/`, not `domain/` as specified). 4/5 new files already exceed the 120L cap (size-justification headers present, so the codebase-wide size-lint gate passes, but the task's own literal DoD text is unmet). The commit message itself is honest about this ("further staged decomposition... not done this pass") but the row was routed into `qa[]` as if terminal/complete. Direct same-epic precedent: `FACTORY-APP-split-assembleBriefing` (commit `4744b0792`) hit the identical "thin <=120L" DoD FULLY in one commit (1327L→72L sequencer, all 19 steps extracted) — proof this shape is achievable and this delivery falls far short of it.

VERDICT: CHANGES_REQUESTED (vc-changes). Not a regression, nothing broken — genuinely incomplete against its own Definition of Done. Moved `qa[]`→`review[]` via `orch-apply.sh` (`status:REVIEW`, `redispatch_count:1`, `next_agent`→`dev-mcp-server` per verify-committed convention — no task branch exists for `fixer`). Issue detail appended to the row's own (previously absent) `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S78 (prior `-17.md` crossed its byte-cap last cycle). `task_release` is the router's job — not attempted here (no MCP gateway tool binding in this session).

## cycle-661 · 2026-08-12T09:35:53Z · FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS (dev-rag-service, real code) — Direct-Commit Verify, CHANGES_REQUESTED (commit `4c8c601e6`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain). Commit `4c8c601e6` confirmed on main ancestry. `files[]` mismatch: row's claimed `apps/rag-service/infrastructure/embedder.py` does not appear anywhere in this commit's diff — stale mint-time metadata; actual scope (architect-superseded per the row's own `note`) is `app_factory.py` + `infrastructure/repositories.py` + `interface/handlers.py` (lazy LanceDB IvfPq vector index + `malloc_trim(0)` sweep) + 2 test files + 3 docs.

Re-ran pytest myself against the ACTUAL deployed image (`vn-market-intelligence-mcp-rag-service:latest`, same deps as production `requirements.txt` resolution — not host/sandbox python, which lacks matching `lancedb`/`fastapi`/`starlette` versions), via an ephemeral `docker run` with the current repo mounted over `/app` (never touched the live serving container). Result: 183 passed / 12 failed of 195 — NOT the "195/195 pytest green" the commit trailer + row `note` claim. Cross-checked against a `git worktree` build of the parent commit (`99ed7c8b0`, same image): baseline = 166/175, 9 pre-existing failures (`test_dfr_p3_hybrid_search.py`, `test_embedder_idle_unload.py`, `test_gfd13_lazy_load.py`, `test_lancedb_compaction.py`) all sharing one root cause — `fastapi.testclient.TestClient` raises `RuntimeError: ... requires the httpx2 package` because neither `httpx` nor `httpx2` is declared anywhere in `requirements.txt`/`pyproject.toml`, and the deployed image resolved `fastapi==0.141.1`/`starlette==1.4.1` which needs it. Pre-existing, not this row's fault. BUT this commit's own 3 new tests (`TestRebuildVectorIndexAdminEndpoint::test_returns_ok`/`test_calls_build_vector_index`/`test_503_when_vector_store_not_wired`) reproduce the identical failure — a genuine regression this commit's own new admin-endpoint tests introduce, not merely inherited baseline noise.

`mock-guard.sh --files` on the 3 touched production files: PASS. `size-lint-justification.sh --check`: rag-service files clean (only the pre-existing, disclosed, unrelated `apps/mcp-server/.../transport.ts` offender fails, as the commit message itself already discloses). `mypy` re-check skipped — not installed in the production runtime image (dev-only extra, not in `requirements.txt`); non-blocking secondary claim only.

VERDICT: CHANGES_REQUESTED. Moved `qa[]`→`review[]` via `orch-apply.sh` (`status:REVIEW`, `redispatch_count:1`, `next_agent`/`owner`→`dev-rag-service` per verify-committed convention — no task branch exists for `fixer`). ISSUE detail appended to the row's own `status_note`. Row's own text ALSO already concedes the PO-ratified live AC (po-S157, ≥2h ops-supervised cold-start growth-rate+plateau) has not been executed — independent second reason DONE_VERIFIED would have been premature. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S77 (file crossed its 36000B byte-cap on this write — `CAP-REACHED` appended, future entries roll to `-18.md`). `task_release` is the router's job — not attempted here (no MCP gateway tool binding in this session).

## cycle-660 · 2026-08-12T09:31:00Z · FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST (dev-mcp-server, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `03f7db28b`)

Router-dispatched (`mode=verify-committed`). Row carried NO `commit`/`files[]` (predates the drain's field convention, minted 2026-08-01) — derived the commit via `git log` on the architect-brief-named files, cross-checked its trailer (`Task: FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`) matches exactly, confirmed main ancestry + no drift since (`git log -1` on all 3 touched production files resolves to the same commit). The actuator fix behind the already-shipped period-mismatch detector: 100+/day live BUG-channel refusals traced to `fetchParseAndStoreBctc.ts` taking `docs[0]` unconditionally because `listSscDocuments()` has no quarter parameter.

Read the diff on disk, not trusted from handoff prose: new `documentTitlePeriodExtractor.ts` (pure Vietnamese title regex, both year+quarter or null, never guesses one from the other) + new `selectSscDocument.ts` (`selectSscDocumentForPeriod`, title-first with `deriveQuarterFromPublishedAt` fallback, throws named `SscDocumentPeriodNotFoundError` — never falls back to `docs[0]`) genuinely wired into `fetchParseAndStoreBctc.ts:92` replacing the old `docs[0]!` line; no-match converts to a debounce-gated Telegram bug + the function's pre-existing `return null` contract. `listSscDocuments()` itself untouched, matching the architect's ratified reuse constraint (`checkSscReports.ts` still needs all quarters back).

Independently re-ran, not trusted from prose: new test file 15/15 pass (35 expect, incl. AC-3 family-A Q4-first-listing regression — Q4 doc confirmed NOT selected on a Q1 request); `293-ocr-fallback-pipeline.test.ts` (touched fixture-only) 6/6; own grep found a WIDER 42-file bctc/ssc/checkSscReports/newsChainFallback regression superset (handoff claimed 39) — 399/404 pass, 5 skip, 0 fail, no regressions beyond the claimed set. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` on all 3 touched production files: PASS. DDD domain→infra/app import grep clean on the new domain file; process.env/secret greps clean (one "token" grep hit is a `parseQuarterToken` var name, not a secret).

AC-1..AC-4 verified DONE from source + re-run. AC-5 (historical backfill slice) confirmed still explicitly deferred, not silently dropped: `BCTC-ENRICHER-OLD-QUARTERS` is `DEFERRED-SEQUENCED` (archive/2026-07.json), not landed — matches the architect brief's own sequencing ruling and the handoff's own note that AC-5 is a QA-deferred step, not a dev-mcp-server deliverable this cycle. Not treated as blocking.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S76. `task_release` is the router's job — not attempted here.
