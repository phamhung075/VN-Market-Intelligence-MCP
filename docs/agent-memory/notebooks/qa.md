# QA — Notebook

## cycle-659 · 2026-08-12T06:16:29Z · FIX-FB-GATE-WEEKLY-FRAME-MODE (developer, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `f384267fd`+`00d971e67`)

Router-dispatched (`mode=verify-committed`). Row had NO `review_note`/`status_note` at all (predates the drain's field convention, minted 2026-07-25). Reconstructed context from dev's own decision journal (`00d971e67`, STEP developer-S11) instead of a board note, then verified independently — did not trust the prose. Adds `--frame=daily|weekly|monthly` to `fb-data-integrity-gate.sh` (closes lesson L5: a WEEKLY post's real w/w change was false-blocked against the latest DAILY snapshot).

Read the diff on disk: `--frame` parsed out of argv pre-positional (default=daily, byte-identical prior behavior — confirmed identical usage-error exit 2 with/without the flag); `build_period_snapshot()` genuinely derives a period-close series via `GET .../prices/history` grouped by ISO week/calendar month (same REST surface daily path already uses); Check-A (±7% HOSE limit) genuinely gated to `FRAME==daily`; Check-C selloff cross-check genuinely period-scoped to text before the "Phân tích" label.

Built OWN fixtures (not copy-pasted from the journal): weekly post + weekly-close snapshot → 0 Check-D1/D2 violations; SAME post w/ daily-frame+daily-snapshot → Check-D1+D2 BLOCK (delta=36.2pts/2.16pp) — exact L5 mismatch reproduction, proves a real period-aware comparison, not a blanket disable. Check-A: own +9.5% weekly-cumulative fixture → 0 violations under `--frame=weekly`, `[BLOCK]` under `--frame=daily` — frame-gating confirmed both directions. Check-C: selloff phrase before "Phân tích" → BLOCK; same phrase moved after the label (historical ref) → PASS — period-scope boundary confirmed both directions.

RED baseline reproduced myself via `git show f384267fd~1` (pre-fix parent, not stash): the script has literally zero `--frame` concept — `--frame=weekly` gets swallowed as `$1` (post-file arg), usage-error exit 2 — confirms the L5 gap was real. Live end-to-end smoke against the running mcp-server (no snapshot arg, forces the real curl+grouping path): `--frame=monthly` correctly derived a real ~+3.0% MoM VNINDEX move (BLOCK on a wrong figure, PASS on the correct one); `--frame=weekly` independently derived a distinct ~+1.1% WoW figure — both genuinely live. Full 57-file real `docs/social/fb-post-*.md` corpus, apples-to-apples (same cached live snapshot fed to pre-fix-parent and post-fix-at-commit script copies): 0 files with a NEW block, 11 Check-C false-positive removals — 0 regressions, exceeds the journal's own claimed 0/48. Doc changes (`main.md`/`weekly-recap.md`/`weekly-prediction.md` STEP notes) confirmed present; `main.md`'s content later moved into `daily.md` via an unrelated split (`8d165e8d6`) but the FRAME MODE section survived intact and is still extended by a later commit — corroborates durability.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached). `[QA] Review Record` written to the row's own (previously absent) `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S75. `task_release` is the router's job — not attempted here.

## cycle-658 · 2026-08-12T06:00:20Z · FIX-HNX-OFFHOURS-FORCE-FETCH-ERROR-DOWNGRADE (dev-mcp-server, stale-duplicate claim, no code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (journal `c2c413def`, no code commit — duplicate of `93e9dbeb8`)

Router-dispatched (`mode=verify-committed`, dev-team review-lane QA-drain). Unusual row: dev's own `review_note` claims this is a STALE-BACKLOG-ROW DUPLICATE — the identical off-hours ERROR-log-severity fix already shipped under sibling ticket `FIX-HNX-OFFHOURS-ERROR-DOWNGRADE` (commit `93e9dbeb8`), QA-approved (`a73bf035d`), rebuild-confirmed live (`f0ac29998`) — all 78min BEFORE this row was even minted. Did not rubber-stamp the claim; independently re-derived every leg.

Verified the sibling chain is real, on main ancestry, and correctly ordered: `93e9dbeb8`(fix, `git show --stat` confirms `hnx.ts`+test+journal+handoff) → `a73bf035d`(QA APPROVED) → `f0ac29998`(rebuild CONFIRMED live) → `423d77bd1`(board finalize DONE) — all via `git merge-base --is-ancestor` + `git log <range>`. minted_at `03:14:47Z` minus fix-commit `01:56:45Z`(UTC) = 78min — arithmetic checks.

Read HEAD source myself, not trusted from prose: `hnx.ts:354` (`fetchHnxPrices`) and `:436` (`fetchUpcomPrices`) both branch on `isVnTradingWindow(options?.now)` exactly as claimed — in-window `logger.error`, off-hours `logger.debug`. `git log --oneline -- hnx.ts` confirms `93e9dbeb8` is the last commit to touch this file — nothing has drifted since QA-approval. Spot-checked all 3 named call sites: `sectorRotationTools.ts:181/188`, `portfolioTools.ts:160/163`, `marketTools.ts` (`forceOpts={force:true}` var, lines 244/253) — each wraps the fetcher call in `.catch()` logging `logger.warn`, not `error`, and only fires on thrown rejections; these fetchers never throw (all error paths caught internally and resolve `[]`) — no independent error-log bypass exists. Repo-wide grep for `force:\s*true` + hnx/upcom confirmed no missed literal site; `marketTools.ts`'s variable-wired `forceOpts` site independently read and confirmed same behavior.

Re-ran verification myself: `bun test src/__tests__/FIX-HNX-OFFHOURS-ERROR-DOWNGRADE.test.ts` 6/6 pass; `186/1059/283/084/TASK17-PAGE9/182` sector+portfolio+market suites 91/91 pass, 0 fail; `bun tsc --noEmit` 0 errors; `mock-guard.sh --files hnx.ts sectorRotationTools.ts portfolioTools.ts marketTools.ts` PASS. Decision journal `c2c413def` (the only commit this row itself produced) confirmed real + main-ancestry.

VERDICT: APPROVED — duplicate-closure claim upheld, nothing to ship. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file, per verify-committed convention). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S74. `task_release` is the router's job — not attempted here.

## cycle-657 · 2026-08-12T05:58:59Z · FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE (dev-mcp-server, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `6cca18746`+`64c9475db`) [restored — dropped from HEAD by a concurrent peer-session notebook-commit race, known bug class `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries`; recovered verbatim from this session's own prior Read of the pre-race working tree]

Router-dispatched (`mode=verify-committed`, dev-team review-lane QA-drain). Row carried NO `review_note`/`status_note` at all — the drain picked up a REVIEW row minted 2026-07-25, before the drain's own status_note convention existed. Reconstructed context myself from `git log --oneline --all -i --grep`: code commit `6cca18746` (fix+test), journal `64c9475db`, board closeout `ddb8080bc` — all confirmed on main ancestry via `git merge-base --is-ancestor`.

Read the diff on disk, not trusted from prose. Confirmed `get_macro_calendar` (`carryTools.ts`) genuinely drops the HTTP GET to the macro-indicators FDA-4 honest-unavailable stub (`withDeadline`/fetch/try-catch fully removed) and repoints at the already-existing, previously-unwired `domain/services/macro/macroCalendar.ts#getMacroCalendar()` pure function — new payload `{source_tier:3, is_estimate:true, status:"ok", events, currentMonthIsPivotWindow, nextPivotWindow, pivotWindowWarning}`.

Ran tests myself: new `FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE.test.ts` 7/7 pass; precedent+affected suites (1423c, 1423c-carry-signal, 1423e-macro-calendar, 1881a-source-tier) + new: 88/88 pass, 0 fail. `bun tsc --noEmit` clean. DDD grep flagged an interface-layer import of `domain/services/` in `carryTools.ts` — checked: 53 other files under `src/interface/mcp/tools/**` already import `domain/services/` directly, established codebase-wide precedent, not a new violation. `process.env`/secret greps clean. `mock-guard.sh --files` PASS.

The row's own `rebuild_required:true` flag was the real open question, not just a code-review pass. Container `vn-market-intelligence-mcp-mcp-server-1` image created 2026-08-11T18:25Z, 18 days after the fix commit — timing alone insufficient, so verified CONTENT: `docker exec grep` on the live container's `carryTools.ts` confirms the post-fix source (`getMacroCalendar` import present, no `withDeadline`/fetch). Went further than a code-presence check: executed the ACTUAL registered `get_macro_calendar` MCP tool handler live inside the running container (`docker exec bun -e`, same `McpServer`+`registerCarryTools()`+`_registeredTools[name].handler()` harness as the test file) — real live output `{status:"ok", source_tier:3, is_estimate:true, eventsLen:6}` with real 2026-09 FOMC/CPI/PMI events. Directly confirms `get_macro_calendar` genuinely returns non-empty `events[]` in the live running service today, not merely in unit tests.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached). `[QA] Review Record (direct-commit verify)` written to the row's own (previously absent) `status_note` field — no handoff file, per verify-committed convention. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S73. `task_release` is the router's job — not attempted here.
