# QA — Notebook
## cycle-769 · 2026-08-14T18:10:00Z · INVESTIGATE-EMPTY-DATA-TABLES (apps/mcp-server/, commit `d4569c5b8`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row for a SPIKE (investigation-only, not a code FIX) with `.commit_sha` but no `.files[]` drain field — derived via `git show --stat`. Commit `d4569c5b8c81e04ef7fce6ff8d81aa7b2030269b` confirmed real, on `main` ancestry; touches exactly 3 files, all docs (`docs/spikes/SPIKE-INVESTIGATE-EMPTY-DATA-TABLES.md`, `docs/agent-memory/notebooks/dev-mcp-server.md`, `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md`) — zero production code, matching the SPIKE's own "investigation-only, zero code changes" AC. Did not trust dev-team's own RAW-verify prose in `review_note` — independently re-derived every substantive claim: (1) live `sqlite3 -readonly` on `data/live/market.db` (the actual bind-mounted prod DB per `docker-compose.yml`, not a stale named volume) confirms all 5 tables (`credit_data`/`insider_transactions`/`public_contracts`/`pharma_events`/`broker_sanctions`) = 0 rows today. (2) `brokerSanctionsJob.ts:99-105` `defaultFetchSanctions()` confirmed a verbatim `TODO` stub returning `[]`. (3) `brokerSanctionsJob.ts:45` `QUARTER_MONTHS=[3,6,9,12]` confirmed exact. (4) `muasamcong.ts:35-40` `getMuasamcongUrl()` confirmed to return the proxy env var (or `MUASAMCONG_ORIGIN` fallback) verbatim, no `?path=` composition. (5) `davPharmacyJob.ts:18,35` confirmed to import/call only `shouldSkipRecoveryReplay`, never `shouldRunCatchup` — cross-checked `startupHelpers.ts:102` exports a distinct `shouldRunCatchup` consumed by 3 other scheduler files, corroborating the no-catchup-guard claim. (6) `sscInsider.ts` `parseInsiderHtml()` confirmed a plain `<tr>`/`<td>` regex extractor, structurally incapable of parsing the JS-only Oracle ADF portal response. `bun tsc --noEmit` (apps/mcp-server) 0 errors (sanity check — no production file in this commit); `mock-guard.sh` N/A (zero non-doc files); `process.env`/secrets grep clean on all 3 changed files; DDD scan N/A (no `src/` files touched). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S7.

ONE FRESHNESS CAVEAT surfaced during re-verification, non-blocking: `review_note`'s phrase "`davPharmacyCheckJob` `cron_job_runs` = exactly 1 success ever (2026-04-30)" is now stale vs a live re-query — `cron_job_runs` shows a 2nd success at `2026-07-31T23:00:01Z` (= Aug-1 ICT monthly fire), landing after the SPIKE commit's own timestamp (`2026-07-31T18:05:56+02:00`) but before this QA pass. This does NOT undermine the diagnosis: June-1 and July-1 were STILL both missed despite the intervening April-30 and this later July-31 run, which corroborates rather than contradicts the unswept startup-catchup-guard-gap root cause (same class as 6 already-fixed sibling jobs this sprint). The committed SPIKE doc text itself is accurate as written at its own point-in-time (before that day's 23:00 UTC run) — nothing false was committed; staleness is confined to the `qa[]` row's later `review_note` restatement, not the artifact. VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row) — this board write already landed durably (confirmed row present in `done_verified[]` at repo HEAD, id-verified) via an earlier `orch-apply.sh` pass this same cycle; this notebook append is a re-write after the notebook file itself was overwritten twice by concurrent peer QA sub-sessions on this same hot-contended file (own `cycle-766` entry lost between writes, same class as the already-documented section-collision/contention pattern above) — re-appending here to restore the record, not re-running the verification. Dispatcher-wrap lock `task:INVESTIGATE-EMPTY-DATA-TABLES` cleared via the earlier board write per spawn-prompt instruction.

## cycle-770 · 2026-08-14T19:36:03Z · FIX-BCTC-1345B-REPORT-BATCH (apps/mcp-server/, commit `893aca43b`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from `review_note` prose, cross-checked directly against `git show --stat`. Commits `893aca43b` (fix+test) and `8a08bb510` (notebook+journal) both confirmed real, on `main` ancestry; the fix commit touches exactly the 2 claimed files (`parseBctcReport.ts`, new `FIX-BCTC-1345B-REPORT-BATCH.test.ts`) — no more, no less. Read the diff directly rather than trusting prose: the change gates the existing `[BCTC-1345b]` Telegram alert on a new `financial_reports.validation_status = 'low_confidence'` lookup for the same `(action_code, sort_key)`, ANDed with the pre-existing Task-1792 1h retry-storm debounce (left fully intact underneath) — an additive persistent-open-quality-row guard, reusing the existing column, zero new table/migration. Re-ran REAL verification independently, not trusted from the row's own claim: new test file `FIX-BCTC-1345B-REPORT-BATCH.test.ts` 3/3 pass (RED/GREEN daily-reparse-retry regression after aging the 1792 debounce row past its 1h window; no-over-suppression across 3 distinct new tickers; self-closing once `validation_status` flips off `low_confidence`, later regression re-alerts). 4 named sibling suites re-run fresh (`1792-conviction-debounce`, `1345e-integration-pipeline`, `1424a-bctc-unit-scale-mismatch`, `1810c-vnm-unit-mismatch`): 24/24 pass, no regression. `bun tsc --noEmit`: 0 errors (empty output). `mock-guard.sh --files parseBctcReport.ts`: PASS (no fabricated-data patterns). DDD: the diff adds zero new imports (reuses the file's existing `getDb` import); application-layer files importing `infrastructure/db`/`infrastructure/logger` is the documented, already-established pattern — `docs/policies/qa-checklist.md:14` only forbids `domain/` importing `infrastructure/`/`application/`. `process.env`/secrets grep on the changed production file: clean. Noted (not re-litigated) the row's own PO SCOPE-HOLD (2026-08-01): this verdict signs off dedup/VOLUME only — the `[BCTC-1345b]` alert TEXT correctness (naming a rule family that cannot produce its own 0.1 value) is tracked separately by `FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY-THAT-CANNOT-PRODUCE-ITS-OWN-VALUE` (backlog, P2, dev-mcp-server). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S10.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note` (row had no separate `status_note` field); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=696/candidate=696, signal_total live=37/candidate=37; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). Dispatcher-wrap lock `task:FIX-BCTC-1345B-REPORT-BATCH` cleared via this board write per spawn-prompt instruction — no direct `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Section count now 5 in this file (>3-cap) — same live concurrent-peer write contention already documented by every section above; appended additively only, no existing section touched; next single-writer cycle should reconcile back to the cap.

## cycle-771 · 2026-08-14T19:37:53Z · FIX-BCTC-SLA-FRESHNESS-EXCLUDE-TERMINAL (apps/mcp-server/, commit `f0831e6be`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from the row's own `note` prose, cross-checked directly against `git show --stat`. Commit `f0831e6be` confirmed real, on `main` ancestry; touches exactly the 3 claimed files (`freshnessSlaChecker.ts`, `freshnessSlaMonitorJob.ts`, `FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts`). Read the diff directly rather than trusting prose: `queryBctcPipelineRuntimeState()` narrowed the bctc `queueDepth` SQL from `status IN ('pending','url_not_found','enrich_failed','pek_triggered')` to `status IN ('pending','pek_triggered')` — a generic status-list exclusion (no ticker/date literal), satisfying the backlog's `generic_mandate` ("Exclude ALL terminal states generically — no per-ticker carve-out"). A reader contract was also documented on `PipelineRuntimeState` in the domain file (`freshnessSlaChecker.ts`) for future queue-backed readers. Re-ran REAL verification independently, not trusted from the row's own claim: touched test file 23/23 pass (new/updated cases C-2, C-2c, C-2d, C-10 directly assert queueDepth excludes url_not_found/enrich_failed/done/deferred_infra generically, and a queue holding ONLY terminal rows produces `queueDepth=0` and zero CRASH escalation — satisfies the backlog's `verification_gate`). Independently derived the same "25-file" targeted freshness/SLA suite the commit's own note claims (grep for `freshnessSlaChecker|freshnessSlaMonitorJob|queryBctcPipelineRuntimeState` across `src/__tests__`) and re-ran it fresh: 338/338 pass, 862 expect() calls — matches the note's claim exactly. `bun tsc --noEmit`: 0 errors (exit 0). `mock-guard.sh --files` (2 prod files): PASS. DDD: `freshnessSlaChecker.ts` is a `domain/` file with zero `infrastructure`/`application` imports — clean. `process.env`/secrets grep clean on both touched prod files. Independently confirmed the note's claim that a sibling duplicate bug in `vpsHealthPoller.ts` was "deliberately NOT touched" — genuinely absent from the commit's diffstat. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S12.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (row had no `status_note`/`review_note`, only `note`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=696/candidate=696, signal_total live=37/candidate=37; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). `.head.active_task_id` was a different row in the same 8-row QA-Drain batch (`FIX-DEVTEAM-COLDEVICT-FAILURE-REPORT-SWALLOWS-STDERR`) — left untouched, not this task's responsibility to reset. Dispatcher-wrap lock `task:FIX-BCTC-SLA-FRESHNESS-EXCLUDE-TERMINAL` cleared via this board write per spawn-prompt instruction — no direct `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Section count now 6 in this file (>3-cap) — same live concurrent-peer write contention already documented by every section above; appended additively only, no existing section touched; next single-writer cycle should reconcile back to the cap.
