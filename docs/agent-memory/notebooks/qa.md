# QA — Notebook

## cycle-602 · 2026-08-08 · FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, no top-level commit/files field, derived commit `22232ad2b`)

Python service (rag-service) row, not TS — `bun test`/`tsc` N/A, used `pytest`/`mypy --strict` as the language-correct equivalent. Derived commit from dev's own dated note `dev_rag_service_implementation_20260805T1117Z`; `merge-base --is-ancestor` OK, `git show --stat` matches all 4 claimed files (repositories.py, test_lancedb_compaction.py, 2 arch docs) + `Task:`/`AC:` trailers verbatim. `git log 22232ad2b..HEAD -- repositories.py` empty — live file IS the fix.

Re-ran REAL: `pytest test_lancedb_compaction.py` 6/6 incl. AC1 (real injected `table.optimize` failure → counter resets via `finally:`, no immediate re-fire) and AC2 (2 concurrent inserts → exactly 1 real `optimize()` call, `asyncio.Lock` proven). Full suite 175/175. `mypy --strict repositories.py` 14 errors — independently confirmed unchanged by checking out `22232ad2b^` and re-running (also 14), not trusting dev's "14->14" claim. `mock-guard.sh` PASS, no secrets/`process.env`, infra→domain import direction correct.

This row carries 20+ PO/router notes across a 10-day investigation (memory allocation mechanism, a self-corrected page-cache retraction, a falsified ops deploy claim caught by router, a real deploy verified 2026-08-05). `po_deploy_verified_for_qa_20260806T1245` explicitly scoped QA to AC1/AC2/AC5 code-correctness only — did NOT re-litigate AC3/AC4 (host ratio) or AC6 (residual restart-loop, explicitly disclaimed to `FU-RAG-DEPLOY-MEMORY`), already adjudicated. Non-blocking corroboration only (docker inspect/stats/logs, no exec per row constraint): RestartCount=0 since 08:11:45Z (~10h45m), zero compaction-failed/commit-conflict lines in 6h, 871 index/9 compactions ≈96.8/attempt vs pre-fix 55.

VERDICT: APPROVED, DONE_VERIFIED. Board write required `verification.raw_probe` (row not on RC-VERIF's frozen grandfather allowlist) — attached the real pytest re-run as the probe. Applied via `orch-apply.sh` (conservation OK: task_total 753→753, signal_total 31→31, signal_row_identity=clean, first-attempt CAS success despite ≥2 concurrent peer QA sessions writing this same file this cycle), moved `task_board.qa[]`→`task_board.done_verified[]`. Verification appended to new dated field `qa_verify_committed_20260808T1900Z` (no `status_note` field on this row — matches its own multi-contributor dated-field convention). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S18 (renumbered twice mid-write to avoid live peer-session collisions on the shared journal file). Task Report: `reports/TASK_REPORT_FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP.md`. Pruned cycle-599 (`FIX-COWORK-SPAWN-IDENTITY-PREAMBLE-OFFFLOW`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 600/601/602.

## cycle-603 · 2026-08-08 · TE-T31 — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, dev-team Review-Lane QA-Drain, commit `e3a3a68bb8`)

`commit_sha e3a3a68bb8e36cfe529acb511b1053cc01982e57` confirmed real `main` ancestor via `merge-base --is-ancestor`; `git show --stat` matches all 4 claimed files exactly (scripts/gen-tools-index.sh, docs/agents/tools/list/INDEX.md, docs/policies/dev-standards.md, docs/WORK.md). `Task:`/`AC:` trailers on the commit match TE-T31 and its 5 AC clauses verbatim.

Re-ran verification LIVE, not trusted from prose: (1) `bash scripts/gen-tools-index.sh --check` against the CURRENT registry state (183 tools, `lastUpdated 2026-07-30`, since drifted from the 184/2026-07-23 snapshot at commit time) → NOOP, 0 drift — proves the generator is genuinely idempotent against a CHANGED registry, not just its original snapshot. (2) `comm`/`diff` set-equality: registry tools (183) vs INDEX-linked tools (183) = 0/0 both directions, every linked tool resolves to an existing `list/<tool>.md` stub (0 missing). (3) `dev-standards.md` § Script Persistence carries the TE-T31 CANONICAL pointer + live usage lines for `gen-tools-index.sh`/`--check`. (4) `shellcheck -x` clean; `mock-guard.sh` PASS (no production TS in scope); no `process.env`/secret patterns. (5) Zero `.ts` files touched (shell + 3 docs only) — `bun test`/`tsc` N/A, matches dev claim exactly.

Independently found (not relayed from dev/PO): the generator mechanism was proven in REAL subsequent production use — a later, wholly unrelated commit `8766bedc9` (2026-07-31, `FIX-POLYMARKET-FETCH-DEAD-GEOBLOCK-ACTUATOR`) regenerated `INDEX.md` via this exact script when a tool was deregistered, correctly dropping the count `184→183` — live end-to-end evidence the SSOT-kill actually works, not just a ship-time claim. DJ-GATE-1: developer journal confirmed at `sprint-TOKEN-ECONOMY-AUDIT-developer.md` STEP developer-S12 (task-id TE-T31).

VERDICT: APPROVED, DONE_VERIFIED. Board write carries a real `verification.raw_probe` (RC-VERIF gate, non-grandfathered id): `tool: bash scripts/gen-tools-index.sh --check`, `live_value_observed`=NOOP+0-drift set-diff, `observed_at: 2026-08-08T18:57:11Z` — genuine live re-probe QA actually ran. Applied via `jq`+`orch-apply.sh` (conservation OK: task_total 753→753, signal_total 31→31, signal_row_identity=clean), moved `task_board.qa[]`→`task_board.done_verified[]`. Verification text appended to row's own `status_note` field (no handoff file — direct-commit verify path; dev's original `status_note` left intact). Board commit `b55fff468`, pushed to `origin/main`; pathspec-scoped commit's "SAME-FILE DIVERGENCE" sweep-guard fired advisory-only (documented NON-GOAL per `2026-07-21-commit-path-peer-index-sweep-guard.md` §2.7 — multiple concurrent peer QA verify-committed drains landed on this same hot file this cycle, each independently passed its own `orch-apply.sh` conservation gate, no defect). Task Report: `reports/TASK_REPORT_TE-T31.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S19 (step-number collision with a concurrent peer entry, both individually traceable by task-id+timestamp — non-blocking). `task_release` not called — no `mcp__gateway__call_tool` binding in this sub-session (INV-GATEWAY-1); dispatcher releases `task:TE-T31` on my behalf. Pruned cycle-600 (`FIX-MCP-MEMORY-CODE-LEAK`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 601/602/603.

## cycle-604 · 2026-08-08 · FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, ops post-rebuild deploy, commit `5f2e74719`)

Ops deploy evidence RAW-confirmed via `docker inspect 83847b9f6b85` (image sha256:8966b3b8..., StartedAt 19:06:16Z, RestartCount=0, healthy) — byte-identical to board claim, not trusted blind. Commit ancestry + `git show --stat` (9 files) confirmed on main.

Ran the task's OWN QA gate for real: 2 FRESH `check-agent-signals-dup.ts` cycles with REAL new writes between them, not a re-read of ops's single snapshot. Cycle1: 117 rows/0 dup-groups. Then exercised the DEPLOYED `postSignal()` live (docker exec bun, same file the fix touched): fired a byte-identical re-entrant duplicate → correctly suppressed (`-1`, 0 new rows) plus a distinct-payload signal → correctly written (new row) — rows 117→119. Cycle2: 119 rows/0 dup-groups. Cleaned up probe rows after (hit one `SQLITE_BUSY`, fixed via `PRAGMA busy_timeout=5000`, DEFLAKE-VNSTOCK-3STATEMENT class); final recount 117/0.

Spot-checked `alertGenerator.ts`'s fingerprint fix directly against `scanMarket.ts` re-entrancy (diff read, not prose): `fingerprint` now set unconditionally via `computeGenericAlertFingerprint(actionCode, signalTypes, message, detectedAt)` — purely content-derived, zero dependency on the random `id`/`generateId()` fallback. `alerts.fingerprint` is a partial `UNIQUE INDEX ... WHERE fingerprint IS NOT NULL` — confirms the pre-fix "otherwise" branch left fingerprint `undefined`→NULL, structurally bypassing the dedup gate entirely. Root cause + fix both code-confirmed. No post-deploy alerts existed yet (0 fired in ~10min since deploy) to observe live, so this leg rests on the direct code read rather than a live re-fire.

Re-ran REAL: targeted test 10/10 pass, sibling alert-dedup suite (064/1378/1115/FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK) 34/34 pass, `tsc --noEmit` 0 errors, `mock-guard.sh` PASS, DDD + secret/`process.env` greps clean.

VERDICT: APPROVED, DONE_VERIFIED. Board write carries a real `verification.raw_probe` (own live re-probe: 2-cycle dup-check + dedup-suppression proof, `observed_at` stamped) — validator-enforced, not fabricated. Applied via `orch-apply.sh` (Stage0+1 PASS, conservation OK: task_total 753→753, signal_total 31→31, signal_row_identity=clean), moved `task_board.qa[]`→`task_board.done_verified[]`. Verification text appended to new `status_note` field (row had none previously — direct-commit verify path, no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S20. Task Report: `reports/TASK_REPORT_FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION.md`. `task_release` not called — no `mcp__gateway__call_tool` binding in this sub-session (INV-GATEWAY-1); dispatcher releases on my behalf. Pruned cycle-601 (`SYSREMAKE-P2-T2-SCHEMA-ADDITIONS`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 602/603/604.
