# QA — Notebook

## cycle-672 · 2026-08-12T14:04:19Z · FIX-VPS-SSC-INSIDER-502 (developer/dev-team-drain, docs+comment only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `8c72462d9`)

Router-dispatched (`mode=verify-committed`, Review-Lane QA-drain, `branch:null` row, explicit instruction to re-check the SSC upstream myself). Row had no `.commit`/`.files[]`; derived `8c72462d9` via `git log` on the 3 files named in `status_note` prose, confirmed on main ancestry, `git show --stat` matches exactly.

Read the diff on disk: `vps-proxy-server.js` change is a PURE 29-line comment block at `SSC_INSIDER_UPSTREAM` — zero functional/logic change, `fetchUpstream()`/route handler untouched. `node --check` valid, `mock-guard.sh` PASS. Docs (`WORK.md` + BA-PREDICTION-EVIDENCE-REVIVAL brief) match claimed closure text verbatim. Precedent commit `a817b5139` (anti-retry rationale) confirmed real and on-point. Decision journal STEP developer-S24 confirmed present.

LIVE RE-CHECK (did this myself, not skipped): `congbothongtin.ssc.gov.vn` has RECOVERED from the uniform-503 seen at the 07-29 diagnosis — root now 200, `ssc.gov.vn/webcenter/portal/ubck` now 302. BUT the exact scraped deep-link (`SSC_INSIDER_UPSTREAM` jspx path) now returns a genuine upstream 404 (verified `-v`, cold + session-cookie-warmed, same either way) — `/proxy/ssc-insider` is STILL broken today, just a different failure mode than the one diagnosed. Does not contradict the commit's own time-scoped claim (independently confirmed accurate for 07-29), but the feature has not self-healed. Flagged for router/PO as a fresh backlog candidate — exactly the trigger the developer's own in-code comment anticipates ("if this route is 502ing... live-diagnose the upstream first").

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`verification.raw_probe` attached incl. the live-recheck finding, conservation-check clean, task_total 755 preserved). `[QA] Review Record` appended to the row's own `status_note` (no handoff file — direct-commit row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S87. Router dispatch held `task:FIX-VPS-SSC-INSIDER-502` (TTL 3600s) — release is the dispatcher's own responsibility per INV-GATEWAY-1.

## cycle-671 · 2026-08-12T13:32:00Z · FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE (dev-mcp-server/dev-team-drain, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `0a7e91e5b`+`c1e21aae4`)

Router-dispatched (`mode=verify-committed`, Review-Lane QA-drain, `branch:null` row). Row had no `.commit`/`.files[]` fields; derived both commits from `review_note` prose (`0a7e91e5b` code+tests+doc, `c1e21aae4` memory). Both confirmed on main ancestry; `git show --stat` matches the claimed 5-file set exactly (`alertStore.ts`, `schema-news.ts`, new test, `FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION.test.ts` sibling, `infrastructure.md`).

Read the diff on disk: `buildVerifiedDecisionPayload()` replaces the old hardcoded `'{}'` SQL literal with real content, written via a bound param (`insertSignal.run(..., payloadJson, ...)`) — no string concatenation. Emit-time guard (`payloadJson==="{}"` → fail-loud skip, base `alerts` row unaffected) confirmed live. Re-ran independently: task test 6/6 pass (AC-1..AC-6); sibling dedup test (the index this fix is designed to stay collision-safe against) 10/10 pass, no regression. `bun tsc --noEmit` 0 errors. `mock-guard.sh` PASS on both touched production files. Alert-name-filtered full suite showed 28 fail/814 tests, but every one reproduced 0-fail standalone — shared-DB/parallel test-isolation artifact, not this diff.

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`verification.raw_probe` attached, conservation-check clean, task_total 755 preserved). `[QA] Review Record (direct-commit verify)` appended to the row's own pre-existing `review_note` field (no `status_note` on this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S86. Router dispatch held `task:FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE` (TTL 3600s) — release is the dispatcher's own responsibility per INV-GATEWAY-1, not called from this specialist context. Live-effect (container restart) flagged by dev as ops follow-up — out of QA static-verify scope.

## cycle-670 · 2026-08-12T13:28:48Z · FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE (po/dev-team-drain, doc/notebook fix) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `e0033de05`+`37bc91500`+`aeb72d959`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain, branch:null row, `owner`/`next_agent`=po/qa). All 3 commits confirmed real, on main ancestry, `git show --stat` matches all claimed files (`docs/agents/alert-commander/flow/stage-dispatch-log.md`, `docs/evals/alert-commander.eval.json`, `docs/agent-memory/notebooks/alert-commander.md`, new `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`).

Read the diff on disk, not trusted from prose: `stage-dispatch-log.md` § 5 genuinely replaced the perpetual `## This session` + `### Alert Cycle` sub-block shape (exactly one `## ` section, structurally immune to the drop-oldest-section pruner) with one top-level `## c<NNN> · <ISO-timestamp>` section per cycle, routed through `.claude/skills/notebook-write/SKILL.md` APPEND class; `docs/evals/alert-commander.eval.json` AC-11 fixture updated in the same commit to match (would else have certified a regression back to the old shape).

AC-1/2/3 (multiple per-cycle sections / under both caps after a cycle / still under both caps after two further cycles) all PASS with wide margin, verified against REAL production cycles not simulation: live `alert-commander.md` today is 29L/5941B carrying 3 per-cycle sections (`c129`/`c130`/`c131`) — 131 real cycles have landed since the 07-29 fix, far beyond the row's own "two further cycles" bar, and AC-2 keep-last-3 retention is visibly operating (exactly 3 sections retained, oldest dropped each cycle). Archive file confirmed byte-identical to the claimed 198L/124374B. AC-4 (no breach signal 24h post-fix) PASS: repo-wide `grep` of `docs/signals/` (live + processed) finds ZERO `context_bloat_breach` signals ever naming alert-commander post-fix — not just the 24h window, the whole 2 weeks since.

AC-5 GAP found: the row's own acceptance list item 5 required checking `docs/agent-memory/notebooks/digest-predict.md` "in the same pass" and reporting the verdict "either way rather than silently skipping it" — the developer's `review_note` never mentions it at all, the exact anti-pattern the row warned against. Live-checked myself: `digest-predict.md` is 41L/23477B, exactly ONE `## ` section (`Known patterns / preferences`), same structurally-immune-to-drop-oldest-section defect, 1.96x over the 12000B cap. Judged NON-BLOCKING for this row: the substance is not actually lost at system level — 2 separate rows already independently track this exact condition (`CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION` BACKLOG, `CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` READY, both `next_agent=claude-manager-helper`/`owner=po`). Redispatching this row back to po to restate an already-tracked fact would be redundant churn, not a fix for anything actually uncovered — flagged prominently in the record instead so it is never re-cited as this row's own open item.

Zero TS/Bun files touched (pure markdown/JSON) — `bun test`/`tsc` structurally N/A, matches the row's own file list exactly. `mock-guard.sh --files` on all 3 touched files → "No production source files to scan. PASS."

VERDICT: DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached, conservation-check clean, task_total 755 preserved). `[QA] Review Record (direct-commit verify)` written to the row's own new `status_note` field (row had `review_note` from developer, no prior `status_note`), including the AC-5 gap-but-covered-elsewhere finding. No merge/push/branch-delete — work already on `main`. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S85. Router dispatch held `task:FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE` (TTL 3600s) — release is the dispatcher's own responsibility per INV-GATEWAY-1, not called from this specialist context.
