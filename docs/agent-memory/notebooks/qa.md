# QA — Notebook

## cycle-408 · 2026-07-09 · FACTORY-PDF-split-extractLayoutFirst-execute — Step 5 live E2E RAW-verify PASS, routed to po

Step 5 (docker-deployment-runbook.md § Microservice Code-Change Close Gate) after dev-pdf-extractor split `ExtractLayoutFirstUseCase.execute()` (~480L Tier0-3 inline) into 4 private Tier methods + 2 sub-helpers (all ≤120L), commit c3f30df24 — unit-level equivalence already RAW-verified byte-identical (injected-fake harness) + full pytest 1088/1088-non-preexisting-fail, both by dev and independently re-run by router. ops's Steps 1-4: image d3abfe8bb831, container healthy, `git_sha` label deferral (plain `git_sha` vs script's `vn.market.git_sha`) pre-documented/settled per runbook Phase B — not re-litigated per explicit dispatch instruction; independently confirmed via `git merge-base --is-ancestor c3f30df24 <label-sha>`=true that the deployed image genuinely contains this commit.

Fired 2 REAL `POST /extract-layout-first` requests (not health-check-only) exercising the full Tier0→3 pipeline on real BCTC PDFs with existing `financial_reports` rows: (1) REE_2025_Q4.pdf (24pg) → 202 → DONE units_total=12 passing=0 quarantined=12(orphan_rows) pushed=True; (2) ACB Q1-2026 (33pg, had a stale 2026-05-30 pre-refactor-era baseline of 22 units/0 quarantined in `bctc_layout_units`) → 202 → DONE units_total=22 (exact Tier0 match to the 6-week-old baseline) passing=10 quarantined=12 pushed=True. Independently confirmed both pushes persisted (not just log-echoed) via direct `bctc_layout_units` query — fresh `extracted_at` timestamps, correct row counts. Zero exceptions/tracebacks/warnings across both full log windows; zero 5xx in entire container log history; container stayed `healthy` throughout both runs.

ACB's quarantine delta vs its stale baseline (0→12) is not a regression signal: `git log` confirms ≥2 intervening commits directly touching `layout_invariants`/`bctc_code_whitelist` gate logic (GATE-VISION 1588a5910, eval-detector-hook 5ce052f3a) landed between the 2026-05-30 baseline and this refactor commit — fully explains the delta independent of c3f30df24; the Tier0 unit-count match (22, exact) across that 6-week gap is the relevant equivalence signal and it held. REE's 100% quarantine (12/12, all `orphan_rows` ratio=1.000) is a single-document OCR/orphan-row characteristic, not a bug: no exception, correct response shape, matches the algorithm's documented store-not-drop behavior for failed units — extraction-quality tuning is outside Step 5 code-change-verification scope.

VERDICT: PASS. Added `qa_verify_at`/`qa_verify_by`/`qa_note` to the existing `task_board.review[]` row, `next_agent` qa→po (did not overwrite dev-pdf-extractor's `review_note` or ops's `ops_note`) via `jq`+`scripts/orch-apply.sh`; `.head` synced same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S29.

## cycle-407 · 2026-07-09 · FACTORY-SCHEDULER-job-table-registry — Step 5 FUNCTIONAL PASS, but ops Step 4 SHA-gate genuinely FAILED — bounced to ops (not benign)

Formal Step 5 after dev-mcp-server split 79 `scheduleCron` blocks into `schedulerJobTable.ts` (57 generic-loop + 22 bespoke via `registerBespokeJobs`) + `walEscalation.ts` extraction (commit a25bdc617), ops claimed Steps 1-4 done. Gateway-blind this session (no `mcp__gateway__*` bound) — all verification below is direct docker/git/sqlite/curl, no MCP mediation needed.

Independent live proof of execution (not just registration), via `cron_job_runs` table: `askQueueCheckJob` (bespoke, non-generic-loop) fired+succeeded 2x post-restart (00:12:00/01); generic-loop jobs (vpsServiceHealthJob "polled=5 stored=5", intelligenceCycleJob, restartCadenceAlertJob, bctcQueueEnricherJob, deepFetchVps/MainJob) fired+succeeded at 00:15:00-01. 18/18 runs since restart = success, 0 errors. Re-ran (not trusted) on current HEAD: tsc 0 errors, eslint clean (3 files), targeted test 15/15, all 6 touched test files 53/53, and re-ran dev's own equivalence script myself — reproduced the identical 1 flagged mismatch (`JOB_NAME_BREADTH_PERSISTER`, a non-literal identifier the parser can't resolve) and independently traced it to source: same imported const used old+new → 79/79 confirmed. No regression: `/health` toolCount=183, `/api/cron-status` 200 (85 keys), dashboards 200, docker-exec-cat vs `git show a25bdc617` byte-identical for all 3 touched scheduler files, 11/11 peer services healthy (only mcp-server restarted). WAL now 4.0MB (grew naturally since ops's 1.2MB reading, normal sawtooth pre-next-checkpoint), well under 10MB — `wal-escalation-1783551601546` (29.8MB origin) confirmed resolved.

BLOCKING (routed to ops, not developer — zero code defect): `bash scripts/verify-deploy-sha.sh mcp-server` exits 1 live — deployed `vn.market.git_sha` label = literal `"unknown"`. Unlike cycle-406/404's benign HEAD-drift false-positives, re-confirmed this against the explicit code-commit SHA too (same fail either way) — the label was genuinely never set. Root cause: ops's own notebook entry for this task ran `docker compose build mcp-server` with no `--build-arg GIT_SHA=...`, unlike every other entry in the same file. Runbook: non-zero SHA-gate = deploy BLOCKED, do not declare complete. Remediation is mechanical (code already proven byte-identical) — ops rebuild with the flag + re-run the gate, then straight to po (no re-qa needed). Board: `task_board.review[]` row `status:"REVIEW"`, `next_agent:"ops"`, `rebuild_required:true`, `qa_verified_at/by` set via jq+`orch-apply.sh`; `.head` synced. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S28.

## cycle-409 · 2026-07-09 · CI-RED-06043b3c-FIX — GH Actions confirmed green, routed to po

verification_gate=ci_green_on_subsequent_push check for the fix commit (68c2de81c6, "add \"done\" to validStatuses" in `1837a-pipeline-state.test.ts` L96 — dev-mcp-server, one-line test-enum addition, test-file-only, rebuild_required=false; router had already independently confirmed commit content/diff/board fields, not re-litigated here).

`gh run view 28990352248 --json name,status,conclusion,jobs`: run already `status=completed` by the time I checked (no polling needed) — `headSha` 68c2de81c6bdc99be848fcbccf7a017ac19e6844 (exact match to the fix commit) and `displayTitle` matches the commit subject verbatim, so no stale/superseded-run risk. Overall `conclusion=success`, all 8 jobs green including the previously-failing one: **"bun test"** (databaseId 86028554823) `conclusion=success`, step "Run tests (per-file isolation)" completed 02:44:32Z→02:46:48Z with no failure. Other 7 jobs (py-lint, Kinh Dich/Macro/API-Gateway/Alert-Engine/Stock-Price go-lint, go-lint) also all `conclusion=success`.

VERDICT: CI green confirmed end-to-end for this fix. DJ written first (DJ-GATE-1): sprint-CI-RED-06043b3c-FIX-qa.md §qa-S1. Added `qa_verify_at`/`qa_verify_by`/`qa_note` (cites run 28990352248 + bun-test job 86028554823, both conclusion=success) to the existing `task_board.review[]` row, moved `status` REVIEW→READY, `next_agent` qa→po, via `jq`+`scripts/orch-apply.sh` (never raw overwrite); did not touch dev-mcp-server's `review_note`. Committed board+journal+notebook with explicit paths only (no `git add -A`) — working tree has heavy unrelated concurrent churn from other agents.

**Retention note:** cycle-406 (FACTORY-INFRA-split-telegramCommands) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file.
