# QA — Notebook

## cycle-702 · 2026-08-13T21:29:00Z · FIX-COVERAGE-STAMP-TTL-30-BELOW-SCHEMA-MIN-60-FAILS-CLOSED (dev-mcp-server, commit `a63ab8b0d`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row whose own `note` already claimed all 3 ACs RAW-verified by the router. Did NOT trust that claim at face value — independently re-derived all 3 from source per the dispatch's explicit instruction.

AC1: `git show a63ab8b0d~1:scripts/agents-flow/coverage-stamp.sh` confirmed pre-fix line 158 was literally `ttl_seconds:30`; post-fix commit bumps it to `900`. `git merge-base --is-ancestor a63ab8b0d main` true, and `origin/main` HEAD (`524e1239d` at read-time) matched local `main` — genuinely pushed, not local-only.

AC2: grepped ALL of `scripts/` (every extension — `.sh`/`.ts`/`.js`/`.mjs`/`.py`/`.jq`) for `task_claim`+`ttl_seconds` literals. Every other call site is either a fixed literal ≥60 (600/900/1800/5400/120/60/180/1000/3600) or an env-overridable var with a safe ≥60 default (`auditor-notebook-commit.sh` `$MUTEX_TTL` default 90; `emit-dashboard-row.sh` literal 90) — coverage-stamp.sh was genuinely the sole <60 offender, matching the fix commit's own grep claim.

AC3 (the hard gate, refused lint/grep-only close per the row's own spec): live `docs/data/coverage-state.json` shows 34 watchlist tickers stamped `2026-08-13T20:12:55Z`(news_scout)/`20:12:06Z`(market_watcher) — genuinely after the fix commit's UTC ts (21:28:58+02:00 = 19:28:58Z). Cross-checked against BOTH agents' own notebooks, not the row's prose: news-scout c262 (12:11Z, pre-fix) explicitly logged `Coverage-state update SKIPPED — ttl_seconds=30 < schema min=60` (reproduces the bug pre-fix); c264 (cycle_start 20:10:26Z, post-fix) carries no skip note and names the exact tickers now stamped fresh. market-watcher's 20:11Z cycle carries a structured (non-prose) metrics-table field `coverage_state_updated | yes`, `Stocks: 34` matching the 34 fresh-stamped tickers. Noted but non-blocking: a THIRD cycle (news-scout c263, 16:10Z, still pre-fix) narrates "Coverage-state update completed" with no SKIPPED caveat, yet the live file has zero trace of an Aug-13 16:xxZ write — that cycle's own claim looks unverified/inaccurate (known narrates-writes-never-executed class), but does not affect this row's AC3 evidence chain since c264+market-watcher independently satisfy it.

Ran `coverage-stamp.test.sh` myself (stubbed-mcp regression, unaffected by the TTL literal's value either way) — 29/29 pass. `mock-guard.sh --files coverage-stamp.sh` PASS (bash script, outside its prod-source scan scope).

VERDICT: `DONE_VERIFIED`, zero code ISSUE. Board flip `task_board.qa[]`→`done_verified[]` via `orch-apply.sh`, `verification.raw_probe` attached on first write attempt (conservation clean, task_total 731=731; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in a follow-up `orch-apply.sh` write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b), since the first write left it stale (missed on the first pass, corrected before committing). Committed + pushed both files myself (`ef717f224`, `main` → `origin/main`) — this sub-session DOES carry Bash/git but attempted `mcp__gateway__call_tool(task_release)` per the dispatch's explicit ask and it errored `No such tool available` (no gateway binding this sub-session, consistent with INV-GATEWAY-1's documented specialist-session gap); the coordinating dev-team session (`632721c2-...`) holds `task:FIX-COVERAGE-STAMP-TTL-30-BELOW-SCHEMA-MIN-60-FAILS-CLOSED` and is the authoritative release path via its own finally-block/TTL.

## cycle-701 · 2026-08-13T20:27:00Z · FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT (dev-mcp-server, commits `78f945fb2`+`abd890ef1`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row. Both commits confirmed ancestors of `main`; `git show --stat` matches every claimed file (`freshnessSlaChecker.ts`, `freshnessSlaConfig.ts` untouched-file-cited-in-comments-only, 6 fixture-ripple test files, new `FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT.test.ts`).

Read the review_note's AC-1 claim at source, not trusted: `getSlaThreshold("bctc")` (freshnessSlaChecker.ts:636-640) branches ONLY on `isBctcEarningsWindowActive(now)` (a boolean) → `cfg.earningsWindowThresholdMinutes` (1440) or `cfg.defaultThresholdMinutes` (10080) — zero `minutesSinceX(now)` age term left in the bctc branch, confirming the "second age clock 5439min behind" root cause was correctly fixed, not just described. AC-5: pulled `docs/data/system-map.json` `.project.data_sources["bctc-discover"].sla` myself and diffed against `DEFAULT_SLA_CONFIG`'s bctc entry — `default_stale_threshold_hours:168`(=10080min)/`earnings_window.stale_threshold_hours:24`(=1440min) byte-match, independent of the row's own account. AC-4: `marketHoursThresholdMinutes`/`offHoursThresholdMinutes` grep-confirmed gone from `SignalSlaConfig`/`DEFAULT_SLA_CONFIG`, only surviving reference is the new test's own `toBeUndefined()` regression guard.

Did not stop at the claimed 8-file/107-test scope — widened in 3 rings: ring1 (the 7 files review_note names) 110 pass/0 fail; ring2 (+9 more bctc/SLA-threshold-touching files found via grep, e.g. `bctc-eval-routes`, `FIX-SLA-EXEMPT-NEWS-SBVFX`) 140 pass/0 fail; ring3 (full 18-file set of every test importing `freshnessSlaChecker`/`freshnessSlaConfig`) 271 pass/0 fail — zero fixture-ripple missed anywhere in the blast radius. `bun tsc --noEmit` 0 errors. `mock-guard.sh` PASS on the touched domain file. DDD grep clean (no infra/application imports — the one "infrastructure" hit is a doc-comment). No `process.env`/secrets. AC-2 (fixed-across-emissions, 3 samples >1h apart both regimes) and AC-3 (bidirectional clear+fire+recovery) read line-by-line in the new test, not summary-trusted — both genuinely exercised and passing. Decision journal confirmed: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md` STEP `dev-mcp-server-S26`, task-id line present. Notebook commit (`abd890ef1`) content matches code and closing report.

Did not independently re-run the full 14870-test `bun test` (609s cost, out of proportion to a change whose entire blast radius — all 18 direct importers — was already exhaustively re-run green); scope judged sufficient per verify-committed protocol.

VERDICT: `DONE_VERIFIED`, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` (row had `review_note`, not `status_note` — used the field that actually existed). `verification.raw_probe` attached on first write attempt after validator flagged its absence (RC-VERIF schema — tool/args/live_value_observed/observed_at). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 732=732; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Structural note: this sub-session's tool grant is Read/Edit/Write/Bash only — no `mcp__gateway__call_tool` binding present, same INV-GATEWAY-1 gap the dev agent hit — could NOT call `task_release`/`send_telegram` myself; per the dispatch prompt's own framing, the coordinating dev-team session holds `task:FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT` and must release it after RAW-verifying this write.

## cycle-700 · 2026-08-13T19:30:07Z · FACTORY-INFRA-split-agentSignalStore (ops rebuild follow-up, commit `9ad31c026` deployed at `78e4b06af`) — post-rebuild live-verify, DONE_VERIFIED

Follow-up to my own cycle-699 (held `DONE`, withheld `DONE_VERIFIED` — container was ~2 days stale). `ops` reported (commit `37a03065d`) having rebuilt mcp-server, SHA-verified, health-checked, and DB-checked (30 cols/115 rows/id=10884, `is_correlation_stub` claimed pre-existing). Did NOT trust that prose — independently re-derived all 4 asks.

**SHA/deploy check (mine, not ops's):** `docker inspect` label `vn.market.git_sha=78e4b06afcfc1a9282966f8b258205fdd4cb4983`, container created `2026-08-13T19:18:30Z`. `git merge-base --is-ancestor 78e4b06af HEAD` true (current main HEAD `2deae06b0`, 2 commits ahead — both doc/orch-state-only, confirmed via `git show --stat`, zero `apps/` touch). `git diff --stat 78e4b06af HEAD -- apps/` empty — zero app-code drift since deploy, so the running image genuinely reflects current main HEAD's code, not just the label's literal SHA. Ran `scripts/verify-deploy-sha.sh mcp-server 78e4b06af...` myself → `OK`.

**`is_correlation_stub` provenance (mine, not ops's citation):** ops cited commit `94de167ea`/2026-06-10 as the origin — checked it directly, WRONG: that commit is `FIX-ALERT-ORPHAN-CORRELATION` (adds `alert_id`, dated 2026-06-13), no `is_correlation_stub` anywhere in its diff. Found the real origin via `git log -S"is_correlation_stub" -- apps/`: commit `c06030015` (2026-06-19, `alertStore.ts`'s `ensureCorrelationStubColumn`, a plain idempotent `ALTER TABLE ADD COLUMN`) — genuinely pre-existing, ~2 months before this refactor. Confirmed zero references to `is_correlation_stub` anywhere across all 20 new `agentSignals/*.ts` modules (`grep -rl`, no match) — the split never touched this column's owner file. Substance of ops's claim (pre-existing, not introduced by this refactor) held; the specific commit/date citation was wrong.

**Live health + round-trip write/read (mine, not ops's numbers):** curled `/health` myself → `status=ok, toolCount=183`. Called `post_agent_signal` directly against the running container's `/mcp` HTTP endpoint (`from_agent=to_agent=qa-live-verify`, `signal_type=signal_feedback`, `stock_code=QATEST`) → wrote row `id=10885`; `get_agent_signals(agent=qa-live-verify,status=all)` read it back with matching title/detail marker. Independently queried the live DB myself (`docker exec ... bun -e`, `bun:sqlite` readonly, inside the actual running container, not a host/sandbox copy): 30 columns, 116 rows, `MAX(id)=10885` — exactly baseline 114 + ops's own +1 arrival (115/id=10884, captured at rebuild time) + my own probe write (116/id=10885, captured now) — a fully consistent monotonic chain, no gaps, no resets.

VERDICT: `DONE_VERIFIED`. All 4 asks (SHA-genuine, `is_correlation_stub` pre-existing, live health, live write path intact) independently re-derived and held. Moved `task_board.qa[]` → `task_board.done_verified[]` via `orch-apply.sh` (conservation clean, task_total 732=732; Stage 1g's 16 pre-existing warnings unrelated, none touching this row), `next_agent: pm`, `verification.raw_probe` attached with the concrete tool/args/live_value_observed/observed_at evidence above. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S112. `task_heartbeat` called on `task:FACTORY-INFRA-split-agentSignalStore` at cycle start per router instruction; `post_agent_signal`/`get_agent_signals` called directly against the container's `/mcp` HTTP endpoint via `curl` (not the gateway MCP binding — not available in this sub-session, consistent with INV-GATEWAY-1's documented specialist-session gap). No `task_release`/`send_telegram` called — dispatcher's finally-block + TTL expiry is the authoritative release path.
