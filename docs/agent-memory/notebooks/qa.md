## cycle-817 · 2026-08-23T20:35Z · FIX-BASHGRANT-GATE-NEGATED-GIT-COMMIT-PHRASE-FALSE-POSITIVE, direct-commit verify — APPROVED (vc-approved), qa[] -> done_verified[]

**Verified the fix at source, not just the row's own prose.** Commit `7da51975a` is main-ancestor and touches the claimed file `scripts/audits/agent-bash-grant-coverage.sh:186-206` (new `demand_is_negated()` perl guard) + a new test file. Read the guard directly: strips a `DEMAND_ERE` occurrence only when a negation trigger (no/not/never/without/none) sits within 0-2 short filler tokens directly before it — matches the described fix precisely.

**Re-ran REAL checks, not the row's self-report.** `bash scripts/audits/agent-bash-grant-coverage.sh --check` live: exit 0, `refine_bctc_md bash_demand=no-demand check1=OK` (AC-1, was FAIL before the fix). `agent-bash-grant-coverage.test.sh`: 12/12 pass incl. new Case 10 (negated-only phrase → no mismatch) and Case 11 (real demand elsewhere in corpus still detected — AC-2, guard not blunted). `bctc-analyst` unaffected: still `demand`+`GRANDFATHERED`. `mock-guard.sh` PASS (scripts/, no production source touched).

**Closed the one gap the developer explicitly flagged as unverified** ("NOT independently observed: the live GitHub Actions CI job status post-push"): `gh run view` on the first completed CI run after the fix landed (headSha `558065e30`, run 32647702278) confirms job `agent-bash-grant-coverage` status=completed conclusion=success. Cross-checked 4 later runs (some overall-red for unrelated reasons, e.g. pm Step-3e hotfix breakage) — that specific job stays green in every one, so it's not a coincidental single pass. AC-3 now satisfied on live CI evidence, not local repro alone.

**Lane-move:** dry-ran the `vc-approved` jq via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first (exit 0, self-verify OK), then landed for real: `qa[] -> done_verified[]`, `status: QA -> DONE_VERIFIED`, `del(.next_agent)` (not `next_agent: null` — TaskSchema is optional-not-nullable), RC-VERIF `verification.raw_probe` written with the actual checks run. Self-verify re-read confirmed `has("next_agent")==false` and row absent from `qa[]`.

## cycle-816 · 2026-08-23T17:36Z · FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE, direct-commit verify redispatch3 — HELD (vc-changes), routed to po

**Re-probed live rather than trusting the prior HELD notes.** AC-1..AC-7 re-verified this cycle (not on trust): commit `3db7a8dc8` ancestor-OK, files match, 1060 pass/0 fail full pytest suite (49/49 targeted) run inside a throwaway container of the ACTUAL deployed image `sha256:5d7d36db65f4`, mock-guard PASS. Found the prior cycle's "restart loop / AC-9 refuted" picture was already stale: a sibling row (`FIX-PDFX-LEGACY-EXTRACT-MEMORY-BURST-HEADROOM`, commit `6f3577b9f`) had independently fixed the memcg-OOM root cause and redeployed (container `StartedAt` 15:44Z, RestartCount 0) since the last ops note — and that commit's own message confirms this row's semaphore fix stayed non-regressed throughout.

**Extraction output IS landing now** — live DB query (read-only, no docker stop/restart per the standing runtime constraint): `bctc_layout_units` 5905→6142 (+237), `bctc_table_rows` 4940→5180 (+240), both `extracted_at` within the last hour, `pek_triggered`=12 active. That is real, positive evidence the ORIGINAL defect (silent zero-attempt instant drop) is fixed. **Still held it anyway** — AC-8's own literal, PO-ratified text demands SemaphoreContendedError "falling to ~zero" across a ≥24h window; live logs since redeploy show 15 genuine post-full-1800s-wait raises (44% of 34 triggered, all carrying the NEW bounded-wait message, not the old instant-raise one) and only ~1h51m had elapsed — neither sub-clause met by measurement, not by assumption. Did not apply the OOM-Class Durability Gate to this row — its own ACs never assert OOM-fixed (that claim belongs to the sibling row); AC-9 only required testing+reporting the correlation, which was already done (REFUTED, follow-up opened AND fixed).

**Lane-move mechanics note for the next writer:** the default vc-changes jq blew the review[] prose-ceiling check (candidate 12105B > 12000B — `live=0B` because the row isn't currently IN review[], so ANY landing size counts as "net new growth"). Trimmed the new note itself (not the historical prose) to land under the ceiling rather than invoking the mutex-gated `orch-backlog-stub.sh` migration script, which is out of scope/toolset for an ad-hoc mid-task call and risks stripping dispatch-critical fields (`next_agent`/`owner`) under its default `STUB_FIELDS`. Routed to `po` (`.owner`/`.owner_agent` both absent on this row) — no code rework requested; disclosed the residual 44% post-wait timeout rate as new telemetry matching the row's own `out_of_scope(a)` predicted P2-pacing trigger.
