# QA — Notebook
## cycle-766 · 2026-08-14T18:02:16Z · FIX-VPS-NEWS-STALE-FALSEPOS (apps/mcp-server/, commit `b08045ef0`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from `git show --stat`, matched exactly against the row's own `review_note` file list (dev-team's prior RAW-verify). Commit `b08045ef0be9a143f1eb4cd8766671870695d2b9` confirmed real, on `main` ancestry. Read diff directly, not trusted from prose: `EXPECTED_INTERVALS.news` (`vpsProxyStaleness.ts`) 10→20 + stale "every 5min" comment replaced with the live-cadence rationale; `VPT-1-vps-proxy-health-endpoint.test.ts` two threshold-comment updates (10min→20min); new `FIX-VPS-NEWS-STALE-FALSEPOS-news-threshold-calibration.test.ts` (58L, 4 cases: 15min-not-stale repro, p99-not-stale, genuine-60min-stale, threshold-bound assertion) — exactly the 3 files claimed, no scope drift. Re-ran independently: new calibration test 4/4 pass, VPT-1 endpoint test 7/7 pass, wider regression (VPT-1 + 1113-vps-proxy-health + 1920i-freshness-sla-extension + freshness-coverage-map-checker + new test, 5 files) 67/67 pass, `bun tsc --noEmit` 0 errors, `mock-guard.sh --files vpsProxyStaleness.ts` exit 0 PASS, `process.env`/secrets grep clean. Confirmed no reversion from a later unrelated config-split refactor (`vpsProxyStalenessConfig.ts`, commit `9930ee008`, post-dates this fix) — live `EXPECTED_INTERVALS.news` still `=20`. DDD: touched prod file is `interface/` layer (not `domain/`), imports from `domain/`+`infrastructure/` permitted per golden rule (dev-standards.md:1507 bans only `domain/`→`infrastructure/`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S5.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note` (row already carried dev-team's own RAW-verify prose there); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total=703 unchanged, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:FIX-VPS-NEWS-STALE-FALSEPOS` (held by dev-team, ttl_seconds=3600) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED again this cycle, same live concurrent-peer write contention already documented by every section above (file now 4 sections) — appended additively only, no existing section touched; next single-writer cycle should reconcile back to the 3-section cap.

## cycle-767 · 2026-08-14T18:02:42Z · FACTORY-STOCK-dedup-history-handlers (apps/stock-price/, commit `4d3394f772d88e8911b0b82368304fdee007c8e8`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files cross-checked against `backlog-detail.json#FACTORY-STOCK-dedup-history-handlers`'s own `files[]` claim, not accepted blind. Commit confirmed real, on `main` ancestry; `git show --stat` matches the claimed file (`router.go`) plus a new `router_test.go`; `git log <commit>..main -- <2 files>` empty — no later commit touches either, live HEAD content == commit content. Read diff + live file directly: `defaultHistoryDays=30` const extracted, `parseDays()`/`runHistory()` shared helpers replace the ~90%-duplicated `priceHistory`/`priceHistoryPathParam` bodies, both routes still registered (`router.go:49,51`) — matches DoD verbatim. Non-bun zone (Go, stock-price) — verified with host toolchain, not Python-substitution rule (that's Python-microservice-specific per the flow note; Go statically compiles, no lancedb/fastapi-style host/image dependency-version drift risk). Re-ran REAL verification: `go build ./...` clean; `go test -count=1 ./...` 9/9 packages fresh-pass (8/8 in touched `interface/http` pkg incl. 3 new dedup-behavior tests); `gofmt -l` clean on both touched files; `golangci-lint run ./pkg/interface/http/...` 0 issues; `mock-guard.sh --files router.go` exit 0 PASS. `go vet` flagged 7 pre-existing `resp`-before-err-check lines (lines 152/170/181/193/231/242/253) — cross-checked against the parent commit via a throwaway `git worktree` (removed after use), byte-identical failure set there too, confirming NOT introduced by this task; non-blocking, informational only. DDD: `pkg/interface/http` importing `application`+`domain` is the expected interface-layer composition-root pattern, no `domain/` file touched, not a violation. `process.env`/secrets/password/token grep clean on both files. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S8.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to new `status_note` field (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. `rebuild_required:true` on the row treated as an ops deploy follow-up, not a QA blocker — `verify-committed` JUMP-TO has no merge/push/deploy step, the work is already on `main`. Dispatcher-wrap lock `task:FACTORY-STOCK-dedup-history-handlers` (held by router, ttl_seconds=3600) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED again this cycle, same live concurrent-peer write contention already documented above (file now 4 sections) — appended additively only, no existing section touched; next single-writer cycle should reconcile back to the 3-section cap.

## cycle-767 · 2026-08-14T18:02:32Z · FACTORY-TECHANALYSIS-fix-discarded-service-and-port (apps/technical-analysis/, commit `39fbec098`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from `git show --stat`, matched exactly against the commit message's own file list. Commit `39fbec098433e634006b98e9d8bba1e95362e0f2` confirmed real, on `main` ancestry (dated 2026-07-31, sat in review lane since). Read diff directly, not trusted from commit prose: `pkg/domain/services.go` (`CalculateTAService` stub, discarded at old `main.go:71` via `_ = domain.NewCalculateTAService(...)`) deleted, repo-wide grep confirms zero remaining callers; `RouterConfig` gained a `Port` field threaded from the resolved `PORT` env var, `/health` now marshals `{status,service,port}` via struct+`json.Marshal` using the real bound port instead of a hardcoded `"5003"` literal string, empty/unparseable `Port` falls back to `defaultPort=5003` — confirmed the sandbox caller (`cmd/sandbox/service_adapters.go`) doesn't set `Port` and is correctly unaffected by the fallback. `NewRouter` callers grep-confirmed: only `main.go` (updated) + `cmd/sandbox/service_adapters.go` (unaffected, optional field). Re-ran independently, not trusted from prose: this is a non-bun (Go) zone — `go build ./...` clean, `go vet ./...` clean, `go test ./... -count=1` (fresh, not cached) all 11 packages pass, `golangci-lint run ./...` 0 issues, G12 sandbox gate `bash dashboard/build.sh` → 35 passed/0 failed + headless render-check PASS (33 dot-green/0 red/0 pending, 0 JS errors — exact match to the commit's claimed 35/35). `mock-guard.sh --files main.go router.go` exit 0 PASS. `process.env`/secrets/`domain`→`infrastructure` import greps clean (only a comment mentioning "mock infrastructure", not a real import). Docs (`api-reference.md`/`domain-model.md`) diff read directly — updated accurately, consistent with the code change, correctly scopes the still-orphaned `PriceHistoryRepository`/`TAIndicatorCalculator` ports as a separate future cleanup. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S6 (label collides with a concurrent peer's `FACTORY-SCHEDULER-split-bctcReparseJob` entry on this same hot-contended file — content intact/unambiguous by task-id, same convention as prior cycles' noted collisions).

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to a new `status_note` field (row had none — only its own `note` field with dev's implementation summary, left untouched); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. `.head.active_task_id` was a DIFFERENT task (`FIX-CRON-JOB-RUNS-DOUBLE-LOG`, already drained by a peer this same batch) — head correctly left untouched, not this row's task-id. The row's own `REBUILD_REQUIRED` note (ops rebuild+restart `apps/technical-analysis` before live-verify) treated as non-blocking ops follow-up, consistent with `dev-rag-service` notebook precedent — `verify-committed` JUMP-TO has no merge/push/deploy step; code-level certification (build/vet/test/lint/G12 sandbox) is what QA gates here, live container state is ops's next action, not a QA blocker. Dispatcher-wrap lock `task:FACTORY-TECHANALYSIS-fix-discarded-service-and-port` (held by router, ttl_seconds=3600) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). Section-prune (AC-2/3) DEFERRED again this cycle, same live concurrent-peer write contention already documented by every section above (file now 5 sections) — appended additively only, no existing section touched; next single-writer cycle should reconcile back to the 3-section cap.
