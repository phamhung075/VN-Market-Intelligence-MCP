# QA — Notebook

## cycle-698 · 2026-08-13T16:55:12Z · FIX-VNINDEX-CACHE-STARTUP-PURGE (dev-mcp-server, real code, commit `998ad9b44`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), explicit dispatcher instruction to RAW re-verify the developer's own pivot away from the board's original hypothesis. `git merge-base --is-ancestor` confirmed `998ad9b44` on `main`; `git show --stat` matches both claimed files exactly (new `FIX-VNINDEX-CACHE-STARTUP-PURGE.test.ts`, `schema-market-data.ts`). Read the diff directly, not the commit message summary: `initMarketDataTables()` runs `PRAGMA table_info(vn_index_cache)`, DROPs the table only when it exists AND lacks the `code` column — an exact match to the claimed "DROP legacy/orphan shape before CREATE" mechanism, no paraphrase drift.

Re-ran `FIX-VNINDEX-CACHE-STARTUP-PURGE.test.ts` fresh, not trusted from prose: 4/4 pass (VSP-1..4). VSP-4 is the load-bearing case — proves re-running init against an already-migrated table with a live row never re-drops it, i.e. code-level proof the DROP path is permanently closed post-migration, directly relevant to the developer's deliberately-skipped 2nd live restart.

Sanity-checked the "orphan table shadowing `CREATE TABLE IF NOT EXISTS`" root-cause story against real git history, not the commit's own framing: `b50ef1777` (2026-05-16, Sprint-1922b) retired the orphan with a comment only ("DO NOT add new writers/query this table") — confirmed NO DROP statement in that diff, table left physically live. `ac8ad6c78` (2026-06-20, FIX-VNINDEX-CACHE-EMPTY-REFRESH-PATH) then added a fresh `CREATE TABLE IF NOT EXISTS` for a code-keyed schema — explains exactly why that fix's own `:memory:` test suite always passed (no orphan in a fresh DB) while every live named-volume write failed since inception. Plausible AND correctly diagnosed, not just a good story.

`tsc --noEmit` 0 errors, `mock-guard.sh` PASS, DDD/security greps clean. Neighbor suites independently re-run, matched claimed counts exactly: `1527-schema-slices.test.ts` 78/78, `FIX-VNINDEX-CACHE-EMPTY-REFRESH-PATH.test.ts` 5/5, a 5-file freshness-SLA set 64/64. Separately-filed `FIX-SQLITE-DOCKER-VIRT-CORRUPTION-RECUR-20260730` (cited as the reason the 2nd live restart was skipped) independently confirmed genuine, not fabricated: real row in `docs/data/orch/archive/2026-07.json`, `created_by=dev-mcp-server`, `created_at=2026-07-30T04:10:09Z`, source cites this exact task's own restart window (03:58-04:05Z), `status=DONE_VERIFIED`, resolution narrative matches `docs/incidents/2026-07-30-sqlite-docker-virt-corruption-fix-permanent.md` exactly.

VERDICT: DONE_VERIFIED. DoD judgment call: `verification_gate` in backlog-detail.json demands a 2nd off-hours restart-survival leg but was written under the now-refuted "purge on startup" hypothesis — accepted VSP-4's code-level equivalent + the one live restart already performed (confirmed correct pre-cron-tick migration) in place of it, since the 2nd restart was skipped for a genuine, corroborated, concurrently-active P0 corruption incident (not a hand-waved excuse). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached per RC-VERIF schema. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 732=732; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S110. No `task_release`/`send_telegram` called — no MCP gateway tool bound this session; dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1. Orch-state write left UNCOMMITTED per convention — dispatcher will push.

## cycle-697 · 2026-08-13T16:27:28Z · FACTORY-GUARD-CI-RAWVERIFY-IMPL (developer, real code, commits `127c4a597`+`654181692`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), 3rd such pass this session after SHAREDPKG/NOHARDCODE, explicitly asked to RAW re-verify not trust prose. Row carried no `.commit`/`.files[]` — derived via `git log --all -- scripts/audits/rebuild-raw-verify-check.sh`, 2 hits: primary `127c4a597` (2026-07-30, this task's own scope) + follow-up `654181692` (2026-08-07, separate ATTEST_ERE fix task, same files). Both confirmed ancestors of `main` HEAD; `git show --stat` matches all 5 claimed files exactly (new script + test.sh, `pre-push`, `ci.yml`, `dev-standards.md`).

RAW re-ran fresh: `rebuild-raw-verify-check.test.sh` → 12/12 PASS live, not stale claimed 9/9 — traced the 3-test delta to the separate later fix commit (regression + AC2 corpus tests); all 9 originally-named cases (trigger-fail, commit-msg/inline/non-trigger PASS, decisions-journal, no-field-match, colocated-test, usage-error, zero-sha) individually confirmed present+passing among the 12, so the developer's own claim still holds as a true subset. shellcheck 0 findings on all 3 touched shell files (`rebuild-raw-verify-check.sh`, `.test.sh`, `pre-push`); `ci.yml` YAML-parse-validated.

Did NOT stop at the bundled test file — built an INDEPENDENT scratch git repo and exercised `rebuild-raw-verify-check.sh` directly with hand-crafted commits: unattested trigger (added `confidence` field under `apps/*/src/infrastructure/`) → exit 1 BLOCK; same commit amended with a `RAW-verify:` message token → exit 0 PASS; doc-only diff (non-trigger path) → exit 0 PASS; missing args → exit 2; zero-SHA range → exit 0 fail-open. All 5 matched the claimed behavior exactly — highest-confidence verify this session since it's a security/CI gate script.

Confirmed pre-push wiring is nested INSIDE the existing `CODE_TOUCHING_REGEX`-gated block (line 86-97) — doc-only-push fast-skip provably unaffected. CI job `rebuild-raw-verify-hook` confirmed with `fetch-depth: 0` + correct `github.event.before`/`github.sha` args. `git show --name-only` on both commits: zero `apps/` files touched — `bun test`/`tsc` correctly N/A; `mock-guard.sh` correctly reports no production TS/JS source to scan (PASS). Decision-journal entry `developer-S34` confirmed present at the cited path with matching task-id.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe` attached per RC-VERIF schema on the first write attempt (built into the jq transform from the start this time, not a retry). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S109. No `task_release`/`send_telegram` called — no MCP gateway tool bound this session (per dispatcher's own framing, INV-GATEWAY-1); dispatcher's finally-block + TTL expiry is the authoritative release path. Orch-state write left UNCOMMITTED per convention — dispatcher will push.

## cycle-696 · 2026-08-13T15:57:41Z · FACTORY-GUARD-CI-SHAREDPKG-IMPL (developer, real code, commit `9e9389089`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), same batch as the DEADCODE/NOHARDCODE/COMPROOT-IMPL cycles this tick, explicitly asked to RAW re-verify every claim from live repo state not prose. Row carried no `.commit`/`.files[]` — derived `9e9389089` via `git log --all -- scripts/audits/shared-package-import-check.sh`, sole hit, confirmed ancestor of `main`. `git show --stat` matches all 6 claimed files exactly (new script + test.sh, new `docs/data/shared-package-import-baseline.json`, `ci.yml`, `dev-standards.md`, `WORK.md`).

RAW re-ran fresh, not trusted from prose: `--check` on live repo HEAD → exit 0 (~8s); baseline covers exactly 3 packages — `git ls-files packages/*/package.json` confirms `shared-config`/`shared-db`/`shared-types` are the ONLY `@vn-market/`-scoped packages in the repo, all 3 printed BASELINE. Independently re-grepped all 3 myself OUTSIDE the script (manual `grep -rlE`, own-dir excluded) → zero real importers for all 3, matches the script's own verdict — did not trust the script's self-report alone. Check-2 advisory lines (McpConfig/loadMcpConfig/Alert/Signal/+6 more) emit correctly, non-blocking, exit stays 0.

`shared-package-import-check.test.sh` 4/4 PASS (rc=0/1/0/0) — read all 4 case bodies, not just the summary line: case1 baseline-listed zero-importer fixture passes (BASELINE line); case2 synthetic new zero-importer fixture NOT in baseline fails (FAIL line); case3 synthetic fixture WITH a real importer passes despite ALSO being baseline-listed (real-importer wins); case4 live-repo run (no overrides) emits the Alert/Signal/McpConfig advisory lines without affecting the green exit. Fixture dir trap-cleaned, `git status --porcelain` confirms zero residue post-run.

Read the script's own source (not the claim) to confirm the docs/** exclusion is structurally real: `list_search_files()` globs only `apps/**/*.ts`+`package.json` and `packages/**/*.ts`+`package.json`, both in the default path and the test-override env var path — zero `docs/**` anywhere in either search corpus. CI job confirmed wired live at `.github/workflows/ci.yml:481-504` (YAML parse-validated via `python3 yaml.safe_load`); `dev-standards.md:1280-1321` CANONICAL pointer content matches actual script behavior, not just present. shellcheck: `.sh` 0 findings; `.test.sh` 1 SC2329 info (`cleanup()` trap-invoked false positive) — confirmed byte-for-byte identical finding already present on sibling `size-lint-justification.test.sh` (same trap-cleanup idiom, pre-existing accepted pattern in this repo), non-blocking. `git show --stat` confirms zero `apps/` source touched (zone=cross-service/, pure bash+yaml+md+json) — `bun test`/`tsc` correctly N/A, not run (would be false-negative noise on a zone with zero TS/JS change).

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no handoff file — direct-commit path); `verification.raw_probe` attached per RC-VERIF schema (first attempt without it hit a validator hard-fail, corrected same write). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 735=735; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S108. No `task_release` called and no `send_telegram` sent — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only, confirmed by tool-list absence not assumption); the developer's own review_note flagged this same lineage gap. Orch-state write left UNCOMMITTED per this session's established convention — dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1, and the dispatcher (which holds `task:FACTORY-GUARD-CI-SHAREDPKG-IMPL`) will RAW-verify this report and commit.
