# QA — Notebook

## cycle-652 · 2026-08-12 · FIX-DOWJONES-STALE-WRONG-VALUE (dev-team review-lane, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `57b916509`+`734c7bd28`)

Router-dispatched (`mode=verify-committed`). Real code change touching macro-data plausibility gating. Both commits confirmed real + main ancestry; `git show --stat` on `57b916509` matches review_note's claimed 8-file scope exactly (2 new tests + 6 production) — an initial `head -50` paging cut off the last 2 files, re-ran untruncated and confirmed all 8.

Did not trust review_note prose: read every production diff on disk. `indicatorPlausibility.ts` new shared `isPlausibleIndicatorValue()` band gate is genuinely generic (14 indicators, dow_jones 25000-60000), not a one-off. `commodityTracker.ts` dow_jones news-mining regex genuinely removed, old inline MIN/MAX maps replaced by a call to the shared gate. `yahooFinance.ts` `fetchDowJonesIndex`/`storeDowJonesIndex` genuinely added — fail-closed, dedup-before-insert, reuses the pre-existing `fetchSymbolPrice` helper (not new plumbing). `commodityTrackerRefreshJob.ts` Block 3 genuinely wired (own isolated try/catch, DI-injectable, `rowsWritten` sums all 3 blocks) — this file was in the commit but NOT captured in my first truncated `--stat` read, so I explicitly re-verified it. `systemTools.ts get_system_status` genuinely swapped its own unguarded latest-row query for `listTrackedIndicatorsFromDb()` + `[STALE]` tagging. `dataAuditShared.ts` INDICATOR_RANGES genuinely got a dow_jones critical band.

Cross-checked the "separate bug from the already-shipped DSI-MACRO-PHANTOM-STALE-GUARD" claim against that commit (`3280d82ab`) directly — it touched only `marketContextBuilder.ts`/`commodityTracker.ts`, never `systemTools.ts`, confirming `get_system_status` really was unguarded until this fix, not a re-claim of prior work. `listTrackedIndicatorsFromDb` itself pre-dates this task (same prior commit) and is correctly reused here, not fabricated.

Ran tests myself, not trusted from prose: new `FIX-DOWJONES-STALE-WRONG-VALUE.test.ts` 15/15 pass (real fail-closed rejection logs observed for 23750/999999, real HTTP-timeout fallback). Extended `1920c-commodity-tracker-refresh-job.test.ts` 10/10 (7 pre-existing + 3 new — matches the "+3" claim). Reconstructed the claimed "7 files/68 tests" set by per-file counts (025-yahoo-finance 16, 1487-yahoo-finance-extended 7, 1920c 10, 234-system-status-merge 15, DPI-3-commodity-delta 4, DSI-MACRO-PHANTOM-STALE-GUARD 10, FIX-SYSTEM-STATUS-TE-TIMEOUT-GUARD 6 = 68) — exact match, 0 fail. Ran a wider 11-file superset (adds 1039/1489/1833l) = 95/95 pass, 0 fail — no regression beyond the claimed set either. `bun tsc --noEmit`: 0 errors. `mock-guard.sh --files` on all 6 touched production files: PASS. No `process.env`/secret/password hits in touched files. None of the 6 touched files are domain-layer — DDD inbound-direction rule N/A.

VERDICT: APPROVED. Noted (non-blocking) the plausibility band (25000-60000) would not itself catch a hypothetical future 48221-magnitude bad value, but the code's own comment is honest about that scope limit, and the primary fix (regex retirement) removes that extraction vector entirely regardless of band. `rebuild_required:true` / the live-2-cron-cycle verification portion is correctly deferred to ops per the row's own note (container swap is user/ops-gated, out of QA's unit-level scope) — not a merge blocker. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verified_by:qa`, `verification.raw_probe` attached) — Stage0+1 PASS, conservation OK (task_total 758=758, signal_total 77=77; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file, per verify-committed convention). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S67. `task_release` is the router's job — not attempted here. Pruned cycle-649 (oldest of the 3-section cap, `FACTORY-GUARD-CI-shared-package-import-check`) this write per AC-2 retention — kept cycle-651 (ohlcv-corp-action-continuity) + cycle-650 (rebuild-raw-verify-hook).

## cycle-651 · 2026-08-12 · FIX-OHLCV-CORP-ACTION-CONTINUITY (dev-mcp-server, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `90434daad`+`3f2acbdda`)

Router-dispatched (`mode=verify-committed`), real code change on a shared read path (`get_technical_indicators`, both `daily_ohlcv` sites). Commits confirmed real (main+origin/main ancestry); `git show --stat` matches claimed scope exactly (new `domain/services/market-data/ohlcvContinuityGuard.ts` + `application/usecases/getContinuityCheckedOhlcvSeries.ts` + `technicalIndicatorTools.ts` wiring + arch doc +1 line + new 249L test file).

Did not trust review_note prose: read both new files on disk (real per-exchange board limits HOSE7/HNX10/UPCOM15, generic resolve fn not hardcoded; pure detector; reconcile-or-flag). Grepped `technicalIndicatorTools.ts` post-fix for `daily_ohlcv` — zero raw SQL reads remain, both logical sites (Go-prefetch + local-DB-fallback) route through the guard. Independently re-fetched live VJC 2026-06-08..19 via the SAME documented `docker exec ... bun -e` command against the running container — all 10 rows byte-identical to the test fixture, confirming no-fake-data claim. New suite 13/13 pass (own run); tsc clean; mock-guard PASS; DDD grep clean; no process.env/secrets in touched files. No new tool/cron registration (grep-confirmed).

The "pre-existing flaky, unrelated" 2-failure claim got a real A/B, not a trust-the-prose pass: the cited `_deprecated/1302-technical-indicators.test.ts` no longer exists on disk (deleted 2026-07-30 by an unrelated dead-code-gate cleanup commit) but WAS real at the FIX commit's own tree (`git ls-tree 90434daad` confirms). Reconstructed it untracked in-place (`git show <sha>:<path>` for both the test + its co-located `technicalIndicators.ts` dep), ran it at HEAD (32p/2f — matches claim exactly), then git-swapped `technicalIndicatorTools.ts` to its pre-fix blob and re-ran — IDENTICAL 2 failures, byte-identical error text, same 2 test names, before AND after. Root cause is a JSON-envelope-wrapping format mismatch + a `Date.now()`-relative MACD-trend assertion in the legacy test — genuinely unrelated to this fix. Restored the file after; `git status` clean on `apps/mcp-server/` (no residue from the A/B).

VERDICT: APPROVED. Own full-suite run: 15229p/55f/40skip — 0 failures reference any touched/new file; all 55 match named pre-existing flaky classes (logVpsPush, insider_transactions, market_cap, MCP SSE, foreign_flow, telegram routing, backtest_runs, task_heartbeat/release Zod, VPS Proxy Health, etc.) — consistent with review_note's own 2-run claim (raw count drift vs prose is 19-day suite growth, not regression). Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S66. `task_release` is the router's job — not attempted here. Pruned cycle-648 (oldest of the 3-section cap, `FIX-AUDITOR-T1-T3-CLEANEXIT-HEARTBEAT-STAMP-SKIPPED-T2-UNAFFECTED`) this write per AC-2 retention — kept cycle-650 (rebuild-raw-verify-hook) + cycle-649 (shared-package-import-check).

## cycle-650 · 2026-08-12 · FACTORY-GUARD-CI-rebuild-raw-verify-hook (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `265451cc6`)

7th and LAST of 7 `ci-regression-prevention` guardrails — closes the epic cluster. Same batch (`claimed_at=2026-08-12T04:22:18Z`) as sibling `FACTORY-GUARD-CI-shared-package-import-check` (verified concurrently by a peer this cycle, see cycle-649 below); `.head` pointed at that sibling row throughout, left untouched here. Commit `265451cc6` confirmed real: ancestor of `main` AND on `origin/main`, pushed clean; `git show --stat` matches claimed scope exactly (46L/10395B brief + orch-state.json +21L child-mint only + notebook/journal, zero production code).

Independently re-verified BOTH design corrections AT SOURCE, not brief prose: `gh pr list --state all` → exactly 1 PR ever (#1, CLOSED, `mergedAt: null`); `gh api repos/:owner/:repo/branches/main/protection` → 404 Branch not protected. The "attached to the PR" category-error correction is sound — no PR/branch-protection gate exists in this repo, so wiring the check primarily into `scripts/git-hooks/pre-push` genuinely IS the real merge gate here, not a design shortcut. `dev-standards.md:1493` PUSH-AUTONOMY-1 §5 text matches the brief's quote word-for-word — "DoD already exists as policy" is accurate. Concrete compliance-gap commit `e3386bdfa` independently confirmed: real, self-flags `rebuild_required: true`, exact confidence-mask bug class named, zero RAW-verify/REALDATA attestation. Sanity-checked the ratio 18 days later: 3 `VERIFY-*-REALDATA` rows exist repo-wide today vs 173 serving-code commits since the policy pin — gap direction holds.

Verified the sibling-composition claim at the code level too: `rebuild-raw-verify-check.sh`'s `FIELD_ERE`/`TRIGGER_PATH_ERE` literally reuse `metric-mask-lint.sh`'s field regex + the depguard-tier-boundaries layer paths verbatim; child `FACTORY-GUARD-CI-RAWVERIFY-IMPL` already shipped independently (ahead of this row's own QA-drain queue, same out-of-order pattern as siblings dead-code-gate/no-hardcode — not a defect), wired into `pre-push` (primary) + `ci.yml` `rebuild-raw-verify-hook` job (backstop) + `dev-standards.md` §5 cross-reference (incl. a 2026-08-07 post-ship regex fix) — all 5 files from the child's own note present and wired on disk.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) — Stage0+1 PASS, conservation OK (task_total 762=762, signal_total 67=67; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. `.head` untouched (pointed at sibling row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S65. `task_release` is the router's job — not attempted here. SELF-HEAL: this write's own first attempt (an Edit-insert, not a composed single-Write) got silently CLOBBERED by `notebook-auto-prune.sh`'s tie-break — root-caused to a stale `"qa.md": "oldest_first"` entry in `docs/data/notebook-section-order.json` that contradicted this file's actual, git-history-confirmed newest_first/prepend convention (same bug a peer independently hit this same cycle on cycle-649 below); fixed by DELETING the wrong override entry (file's own default is already newest_first — matches this file's real convention, no replacement entry needed), committed separately per doc-self-heal. Pruned cycle-647/`FACTORY-GUARD-CI-no-hardcode-allowlist-scan` (oldest of the 3-section cap) this write per AC-2 retention — kept cycle-649 (shared-package-import-check) + cycle-648 (agent-father).
