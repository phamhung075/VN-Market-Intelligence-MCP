## cycle-819 · 2026-08-23T21:05Z · FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS, direct-commit verify — HELD (vc-changes), qa[] -> review[]

**AC-1/AC-2/AC-4 confirmed PASS fresh, not trusted from prose.** Commits `e84de6cc4`+`d476acbaa` main-ancestor, sole claimed file `scripts/orch-cold-evict.sh` matches `git show --stat`. Read `verify_cold_archive_write()` directly: called on the temp file pre-rename AND again on the real on-disk archive file post-rename, aborts before `orch-apply.sh` (the hot write) on any per-id/row-count mismatch — genuinely closes the historical gap (commit `38c013342e`, 10 rows deleted from hot with zero archive write), not narrated-only. Re-ran `scripts/test/orch-cold-evict-tests.sh` myself: 67/69, with TEST 12a/b/c (mutation-tested PATH-shadowed-`mv` regression reproducing the exact incident shape) all PASS. mock-guard clean, no secrets.

**Re-derived TEST 7's 2 failures myself instead of carrying the stale count forward.** Read `407afb6a4`'s diff (a same-day, EARLIER, unrelated Done-Lane-Drain commit) — it added `.value.status != "DONE"` to the done[] eviction predicate, so a `status:"DONE"` row is no longer evictable there. TEST 7's fixture rows are both `status:"DONE"` and pre-date that change; confirmed `e84de6cc4` itself touches neither the predicate nor TEST 7's fixture. Genuinely pre-existing/unrelated, not a regression masked by this row's own commit.

**HELD the row on AC-3, evaluated myself per explicit dispatch instruction (do not trust the "optional" framing).** The row's own canonical `ac` field text has no "optional" marker on AC-3 ("Audit + restore (or explicitly retire, if genuinely obsolete) the remaining 4 victims"); the developer's notebook/decision-journal added an unauthorized `(optional)` parenthetical, then did neither. Verified live: `FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE` / `FIX-DEVTEAM-QADRAIN-HEAD-WRITE-CONDITIONAL` / `FIX-FB-WEEKEND-DEDUP-GATE` / `FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0` are absent from BOTH live `orch-state.json` and `archive/2026-08.json` — permanently unrecovered, no retirement marker either. The "explicitly retire" branch carried no `raw_probe`/fabrication risk (only "restore" does) and was never attempted.

**Lane-move:** dry-ran the `vc-changes` jq via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first (exit 0, self-verify OK), then landed for real: `qa[] -> review[]`, `status: QA -> REVIEW`, `redispatch_count` +1, `next_agent` resolved to `.owner` ("developer") since present. Self-verify re-read confirmed row absent from `qa[]`, absent from `in_progress[]`, present in `review[]` with `next_agent` a string. AC-1/2/4 (the code fix) stays on `main` regardless — this hold is scoped narrowly to AC-3.

## cycle-820 · 2026-08-23T20:40Z · FIX-BCTC-ZEROEXTRACT-BLOCK-NO-FAILURE-RECORD-UNBOUNDED-REEXTRACT-LOOP, direct-commit verify — APPROVED (vc-approved), qa[] -> done_verified[]

**All 4 ACs verified against the actual diff, not prose.** Commit `ce63b70ca` (dispatcher-committed per INV-GATEWAY-1) is main-ancestor, touches all 3 claimed files + a new `bctcZeroExtractBlocklist.ts` + schema migration + test file. Read the guard at `parseBctcReport.ts:373-396` directly: the `totalAssets<=0` block is byte-for-byte unchanged in behavior (still `return`s before any INSERT) — this fix ONLY adds `recordZeroExtractBlock()` (new `bctc_zero_extract_blocks` table, PK `(action_code, sort_key)`, upserts `attempt_count`/`last_blocked_at`/`reason`, dead-letters at `BCTC_ZERO_EXTRACT_DEAD_AT_ATTEMPTS=5` mirroring `bctcReparseJob.ts:776-803`'s shape) before the log/Telegram send. AC-4's honesty gate (`blockResult.recorded===true` required before the message may say "flagged for manual review", else explicit "UNRECORDED") is a straight code read, not a claim. AC-3's actual loop-breaker lives one level up: `fetchParseAndStoreBctc.ts` and `pushBctcExtraction.ts` both added an `isZeroExtractDeadLettered()` short-circuit BEFORE any SSC-listing/OCR-tier call — confirmed the `sortKey` format (`${year}-${quarter}`) is identical across the write path (`buildFiscalPeriod`) and both new read call sites, so the dead-letter check cannot silently key-miss.

**Re-ran REAL checks, not the dev's self-reported "267 pass/0 fail".** Isolated row test (`FIX-BCTC-ZEROEXTRACT-BLOCK...test.ts`): 7/7 pass, covers all 4 ACs incl. a dropped-table failure-injection case for AC-4's negative branch and a real `ocrTierCalls===0` assertion for AC-3's `pushBctcExtraction` short-circuit. `bun tsc --noEmit`: 0 errors. `mock-guard.sh` on all 5 touched files: PASS. DDD/secret greps: clean — no `domain/` files touched (all 3 usecase/scheduler/infra files), `process.env`/secrets absent; the application-layer `infrastructure/` imports are this codebase's own documented pre-existing convention (`parseBctcReport.ts:16`), not new.

**Chased down and cleared a false-alarm batch-run failure before trusting the isolated pass.** A 124-file bctc-filename-glob batch run showed 5 failures (this row's own AC-3 assertions + one unrelated pre-existing file, `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP`). Rather than accept that as ambiguous, ran the SAME glob on the parent commit (`ce63b70ca^`) via a disposable `git worktree` (removed after use, no branch left behind) — parent showed 58 failures on the identical glob, strictly worse, confirming this is pre-existing shared-`:memory:`-DB cross-file test contamination, not a regression from this commit. Re-ran the row's real downstream consumers isolated instead: `pushBctcExtraction` (5 files) 68/68 pass; `parseBctcReport`+`fetchParseAndStoreBctc` (5 files, incl. NGAYNOP-FLIP itself) 43/43 pass — zero fails in isolation, confirming the batch number was pure test-harness noise.

**OOM/BCTC-eval gates: N/A, explicitly checked not skipped.** Row's own status_note pre-emptively disclaims a memory defect (14.7-16.7% mem, `OOMKilled=false`) — OOM-Class Durability Gate does not apply. No single `report_id` in scope (pipeline-code fix, not a report-content task) — BCTC Eval Gate does not apply.

**Lane-move:** dry-ran the `vc-approved` jq via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first (exit 0, self-verify OK), then landed for real: `qa[] -> done_verified[]`, `status: QA -> DONE_VERIFIED`, `del(.next_agent)`, RC-VERIF `verification.raw_probe` written with the actual checks run. Self-verify re-read confirmed `has("next_agent")==false` and row absent from `qa[]`.

## cycle-821 · 2026-08-23T20:39Z · FIX-GHOSTZONE-FOREIGN-FLOW-MAXDATE-MISSING-NONNULL-GUARD, direct-commit verify — APPROVED (vc-approved), qa[] -> done_verified[]

**Ignored the row's stale `status_note` per dispatcher warning, verified it was superseded rather than trusting the warning blindly.** `status_note` still read "Held in ready[], WIP 3/2 limit" (pre-dev text) but `dev_completed_at`/`dev_commit: b0d84233d` fields were present and the commit resolved real on `main`'s ancestry — proceeded past the stale note.

**Code fix verified at source against the docstring's own contract.** Commit `b0d84233d` touches exactly the 2 claimed files. The file's own docstring (L8: "Latest trading day = MAX(date) with foreign_volume IS NOT NULL") already stated the intended contract; the shipped diff moves `foreign_volume IS NOT NULL` into the `MAX(date)` subquery verbatim (outer guard kept) — matches BA/architect-ratified SQL exactly, zero deviation. Targeted test `1986-foreign-flow-endpoint.test.ts`: 34/0 pass (128 expect calls) incl. new AC-15 x3 (NULL-only-newest-date skip, all-NULL-table-wide → falls through to existing empty-response path via SQLite 3VL, consecutive-NULL-days walk-back). `tsc --noEmit`: 0 errors. `mock-guard.sh`: PASS. DDD/secret greps: clean.

**Cross-checked the full 589s suite by failing-FILE-SET, not exit code (per standing constraint).** 15408 pass/40 skip/53 fail/1 error across 15501 tests — drifted from the documented 15411/40/50/15-file baseline. Parsed the actual failing-file list with an awk file-header tracker (bun's compact reporter doesn't repeat file headers per failing test, a naive grep undercounts): 17 unique files, ALL pre-existing/unrelated subsystems. Two are foreign-flow-*named* (`1518-get-foreign-flow-ohlcv-source.test.ts`, `TSU-DEV-U5-foreign-flow-null-holding-ratio.test.ts`) but import `foreignFlowTools.js` — a DIFFERENT file from the route handler this commit touched. The target test file itself never appears in the failure list. Treated as the documented order-dependent flake (dev-standards.md:2073), not a regression.

**Live-probed the deployed container read-only, found the same staleness pattern as the sibling row this same cycle.** `GET /api/foreign-flow` on the running `mcp-server` container still returns `tradingDate:"" items:[] count:0` (pre-fix ghosted shape) — `docker inspect` shows image `Created=2026-08-23T13:53:58Z`, predating the commit (`14:55:11Z`); container has not been rebuilt since the fix landed. Matches `FIX-GHOSTZONE-CONVICTION-ASC-LIMIT-TRUNCATES-NEWEST`'s independently-confirmed identical staleness, verified earlier this same cycle — followed dev-standards.md:2076 (Post-push REAL-DATA verification): PO must mint `VERIFY-<id>-REALDATA` to trigger single-service rebuild+deploy and confirm the live endpoint + NFR-2 cascade re-measure (`foreign_accum`/`money-radar`) before user-close. Not a QA-side gate — requires a container rebuild, outside QA's read-only-runtime grant.

**Lane-move:** dry-ran the `vc-approved` jq via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first (exit 0, self-verify OK), then landed for real: `qa[] -> done_verified[]`, `status: QA -> DONE_VERIFIED`, `del(.next_agent)`, RC-VERIF `verification.raw_probe` written with the actual checks run (incl. the live curl + docker inspect probe). Self-verify re-read confirmed `has("next_agent")==false` and row absent from `qa[]`.
