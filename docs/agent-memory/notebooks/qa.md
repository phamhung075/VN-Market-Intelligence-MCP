# QA — Notebook

## cycle-613 · 2026-08-09 · FIX-SWEEPGUARD-SAMEFILE-INSPECT-CMD-GIT-USAGE-ERROR-MANUFACTURES-FALSE-BENIGN — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `2cb279b5d`)

dev-team Review-Lane QA-Drain spawn — this row's `branch` field was NON-null (a raw commit SHA, matching `commit_sha`, not a `task/NNN-*` branch name) — verified against the live row + `git log` first, then matched `verify-committed` (not `pipeline`), confirmed the SHA is a real ancestor of `main`. `git show --stat` matches every file the review_note describes: `scripts/git-hooks/pre-commit` (`_detect_samefile_pathspec_only_divergence`), `docs/agents/po/flow/triage-signals.md` §2.7, new T14 in `pre-commit.test.sh`, plus decision-journal + developer-notebook entries. Read the live diffs directly (not the commit message): both AC-1/AC-2 call sites dropped the `-- <path>` limiter on the two-blob `git diff <A> <B>` form (which has no pathspec support — was a hard usage error, rc=129/0 lines). AC-3: in-code comment + developer's own journal entry flag the prior FP row's "10/10 confirmed false positives" verdict UNSUPPORTED (correctly left out-of-scope, size:S).

Re-ran `bash scripts/git-hooks/pre-commit.test.sh` fresh, not trusted from prose: 14/14 PASS incl. new T14, which extracts the detector's actual printed command and independently proves fixed-form rc=0/non-empty stdout AND that re-appending `-- <path>` reproduces rc=129/empty stdout — pins the exact regression this row closes. `bun tsc --noEmit`: N/A, zero apps/ TypeScript touched (zone `cross-service/`, pure bash+md). `mock-guard.sh --files scripts/git-hooks/pre-commit`: PASS ("no production source files to scan" — bash outside scanned extensions).

VERDICT: APPROVED. Moved `FIX-SWEEPGUARD-SAMEFILE-INSPECT-CMD-GIT-USAGE-ERROR-MANUFACTURES-FALSE-BENIGN` `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached — not grandfathered, hand-built from evidence above) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 765→765). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no handoff file for this drain class). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S28. No merge/push/branch-delete needed — work already on `main`. Released outer lock `task:FIX-SWEEPGUARD-SAMEFILE-INSPECT-CMD-GIT-USAGE-ERROR-MANUFACTURES-FALSE-BENIGN` per this QA-Drain spawn's own contract (held by dev-team, session 165f4245). Pruned cycle-610 (`DS-OBS-01-FIX`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 611/612/613.

## cycle-614 · 2026-08-09 · FIX-ORPHAN-FR8-TEST-COORDINATION-STORE — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `8af71e8c2`+`75ac06a90`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`). Both commits confirmed real ancestors of `main` via `git merge-base --is-ancestor`; `git show --stat` matches exactly the claimed scope — `8af71e8c2` (test-only, +52L `coordinationStore.test.ts`) and `75ac06a90` (docs: `docs/WORK.md` + PM-decomposition handoff Implementation Record). No production source touched by either commit.

Re-ran everything myself, not trusted from `dev_note`/`status_note` prose: targeted `coordinationStore.test.ts` alone = 16/16 pass (33 expect()), matches claim exactly. Re-ran the developer's own cited 7-file coordination/lock merge-gate suite verbatim (`task-lock-coordination-store.test.ts` + `task-lock-reaper-timer.test.ts` + `task-lock-coordination-tools.test.ts` + `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts` + `commit-mutex-coordination.test.ts` + `DWF-coordination-phase2.test.ts` + `coordinationStore.test.ts`) = 143/143 pass, 0 fail, 404 expect() — matches dev's claimed 143/143 exactly (first self-picked file substitute mismatched at 145/145; corrected once I read the handoff's exact file list, not my own guess). `bun tsc --noEmit`: 0 errors. Test-only change — DDD/security greps + `mock-guard.sh` correctly Smart-Skipped per flow doc's own carve-out (zero production source in either commit). Subtask 5 (live integration round-trip) correctly NOT claimed done — dev's own note honestly discloses it blocked on Task 2 (`FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS`, still REVIEW not DONE_VERIFIED); not fabricated, not re-litigated here.

VERDICT: APPROVED. Moved `FIX-ORPHAN-FR8-TEST-COORDINATION-STORE` `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `next_agent:pm`, `verification.raw_probe` attached — not grandfathered, hand-built from evidence above) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 765→765, signal_total 47→47). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no handoff file for this drain class). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S28 (step-number collision with a concurrent peer QA-Drain session's own S28 on a different task-id — both entries coexist intact, non-destructive, known artifact of parallel drain spawns). `task_release("task:FIX-ORPHAN-FR8-TEST-COORDINATION-STORE")` attempted but this specialist session carries no `mcp__gateway__call_tool` binding (structural — session tool grant is Read/Edit/Write/Bash only, matching INV-GATEWAY-1's documented sub-session limitation); dispatcher (dev-team, session 165f4245) is the authoritative release path per its own finally-block/TTL. Pruned cycle-611 (`FIX-DEVTEAM-IDLE-CHAIN-MAIN-COMPLETION`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 612/613/614.

## cycle-615 · 2026-08-09 · FIX-SSE-SOAK-VERIFY-DEPENDS-ON-SHARED-CONTAINER-UPTIME-3RD-RESET — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `a3d7ff35f`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`). This row's own history: soak-verify reset 3x because the reaper's >=4h max-age branch was evidenced solely via the shared `dev-mcp-server` container's `StartedAt`, which any peer's unrelated rebuild resets (8+ peers legitimately rebuild it). Dispatch context flagged this explicitly — check for a structural fix, not a 4th plain retry. One was already landed: commit real, on `main` ancestry, `git show --stat` matches all 3 claimed `files[]` exactly (`transport.ts`, new T13/T14 in `1862c-transport-session-eviction.test.ts`, `infrastructure.md`). Read the live diff directly, not trusted from prose: injectable `_now: () => number = Date.now` 8th ctor param (same override idiom as the 4 existing timing params) threaded through all 3 `Date.now()` call sites. T13/T14 construct the manager with heartbeat/idle/max-age left `undefined` (the REAL shipped 4h default, not a shrunk override) + inject a fake clock, proving the actual shipped branch fires with zero real elapsed time and zero container-uptime dependency — closes the exact defect class that reset this row 3x.

Re-ran myself: `1862c-transport-session-eviction.test.ts` 14/14 pass (45 expect), 3 consecutive clean reruns. `bun tsc --noEmit` (mcp-server) 0 errors. `mock-guard.sh --files transport.ts` PASS. DDD grep flagged one `infrastructure/` import — pre-existing `Logger` type, interface-layer file (not domain), unchanged by this commit, not a violation. Only non-test call site (`server.ts:401`) uses 3 positional args — new `_now` default is safe, no breakage.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` — id not RC-VERIF-grandfathered) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 765→765, signal_total 47→47). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S29. Sibling row `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` deliberately left untouched — this write replaces the verification METHOD, not that row's own live QA sign-off. `task_release("task:FIX-SSE-SOAK-VERIFY-DEPENDS-ON-SHARED-CONTAINER-UPTIME-3RD-RESET")` attempted but this specialist session carries no `mcp__gateway__call_tool` binding (INV-GATEWAY-1); dispatcher (dev-team, session 165f4245) is the authoritative release path. Pruned cycle-612 (`FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 613/614/615.
