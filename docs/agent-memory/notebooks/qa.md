# QA — Notebook

## cycle-715 · 2026-08-14T09:31:14Z · TASK-COWORK-MUTEX-001 (report-analyzer, commits `f8d3891c6`+`465801802`) — Direct-Commit Verify, CHANGES_REQUESTED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Both commits confirmed on `main` ancestry, `git show --stat` matches every claimed file. Content diffed at source, not trusted from the developer's own Implementation Record: FR-1..FR-5 pseudocode correct (`COWORK_AGENTS` recognition, `AGENT_SLOTS`/`TARGET_SLOTS` resolution, single `task_list_held(kind="cowork-slot")` probe + client-side prefix match, symmetric collision response), 9-agent/23-slot count live-matched against `cowork-schedule.json`, `expired` param doc-sync byte-matches `taskListHeldTool.ts`'s live Zod schema, fence-balance even (28). No code touched (0 `.ts`/`.js` in diff) — `bun test`/`tsc`/`mock-guard`/security greps all correctly N/A, confirmed not assumed.

BLOCKING FINDING: the probe is textually complete but functionally unreachable from the router's real hot path. `CLAUDE.md:7` and `dispatch-claim/SKILL.md`'s own header (lines 3-9) both declare `.claude/skills/dispatch-claim/CARD.md` as the actual per-dispatch hot-path read — SKILL.md is "read only when CARD.md's edge-path pointers send an agent here". `CARD.md` (39L) carries ZERO mention of Step 2.4/cowork-slot (grep-confirmed, 0 hits) — a router driving off CARD.md alone, the documented normal path, never learns Step 2.4 exists and proceeds straight from Phase A.5 to Phase B unchanged. Reproduces the exact "silently disables the phase" failure the row's own FR-6 edge case exists to prevent, one layer removed (CARD.md↔SKILL.md instead of CLAUDE.md↔SKILL.md). Not a novel nitpick — `docs/handoffs/FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD-PM-decomposition-20260722.md:152` already flagged this exact CARD.md-blind-spot risk on a sibling change, left un-actioned there. Root cause: the architect brief's §3 file-level design table (2026-07-29) predates the CARD.md hot-path split (TE-T12, 2026-07-31) by 2 days, so its CARD.md omission is staleness, not a deliberate scope ruling — same for the PM's own AC checklist.

SEPARATE OBSERVATION (not mine to fix, flagging only): this notebook file itself lost two of my own earlier same-session sections (`cycle-712`/`cycle-711`, and a peer's own `cycle-713`) to a live concurrent-write race across this batch's parallel QA-Drain sub-sessions — the known `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries` class, now reproducing on `qa.md` itself, not just `po.md`.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S123.

VERDICT: CHANGES_REQUESTED. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (row has no `status_note`). Moved `task_board.qa[]`→`review[]`, `status: QA→REVIEW`, `redispatch_count: 0→1`, `next_agent: developer` (row's own `owner` field — no task branch for a fixer to work on, must be a new direct commit) via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` was a DIFFERENT row (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`, same QA-Drain batch) — left untouched. This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:TASK-COWORK-MUTEX-001` and is the authoritative release path.

## cycle-714 · 2026-08-14T09:32:19Z · FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE (report-analyzer, commit `763519447`) — RE-QA, Direct-Commit Verify, CHANGES_REQUESTED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row — RE-QA per prior PO triage: `claimed_at` was a stale carry-over from an earlier bounded-1 promotion, real `qa[]` arrival was 2026-08-08. Row carried no `.commit`/`.files[]` (predates the drain) — derived commit `763519447` via `git log --all -- docs/agents/dev-team/flow/main.md`, cross-checked against `agent_father_implementation_note`.

Commit confirmed on `main` ancestry; `git show --stat` matches exactly (main.md + agent-father notebook/journal, no app code — `bun test`/`tsc`/`mock-guard` correctly N/A). WF-3/WF-4 jq filters dry-run-verified myself, not trusted from prose (age-calc + BLOCKED-map both produce correct shape). `HeadSchema`/`TaskSchema` `.passthrough()` confirmed at source (zero schema change needed, as claimed). `blocked_by` confirmed genuinely schema-meaningful (reverse dep edge feeding `effective_depends_on`) — correctly avoided. `hold_reason` confirmed a real pre-existing convention (`has_hold_reason()`, `scripts/lib/devteam-eligibility.jq:491-493`) — correctly reused. AC-1/AC-3/AC-4 substantively met.

BLOCKING ISSUE: `main.md:455-464` (WF-3 RESUME-ATTEMPT-BOUND escalation write) flips the pinned row to `BLOCKED` inside `.task_board.in_progress[]` but never lane-moves it OUT into `backlog[]` in the same write — violates `execute-tier.md:125` § MUST — CANONICAL:SSOT-STATUSFLIP-LANEMOVE(c). No live occurrence yet but latent — once `resume_attempts` hits 3 on a genuine future pin, the row strands silently forever.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S124.

VERDICT: `CHANGES_REQUESTED`. Not routing to fixer — no task branch exists (direct-commit row, branch:null). ISSUE text appended to the row's `status_note` (had none). Moved `task_board.qa[]`→`review[]` via `orch-apply.sh` (conservation clean, task_total 741=741), status `QA`→`REVIEW`, `next_agent`/`owner`→`architect` (row's own `.owner` field, not fixer), `redispatch_count` 0→1. `.head.active_task_id` was a DIFFERENT task (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`) — left untouched (rule b). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds the outer lock and is the authoritative release path.

## cycle-705 · 2026-08-14T01:33:11Z · FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES (report-analyzer, commit `761988143`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — the detector's only non-zero-diff DETECTED path sourced its evidence from the notebook commit it was checking, so a genuinely non-zero OTHER plane (signal_queue row, ledger entry) with the notebook plane silently missing defeated `all_zero` and PASSed a defective cycle (confirmed live 12x). Did not trust the developer's pasted 15/15 count or the router's own independent-run note — re-ran everything myself.

Commit `761988143` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook side-files. `bash scripts/audits/detect-analysis-only-exit.test.sh` own re-run: 15/15 PASS.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live repo. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.
