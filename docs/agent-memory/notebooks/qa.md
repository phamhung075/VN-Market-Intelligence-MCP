# QA — Notebook

## cycle-525 · 2026-08-06 · FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Executed PO's 2026-07-22T15:55:33Z disposition (this row's OWN two-part AC, decoupled from the separately-tracked `PERF-PEK-PER-PAGE-LATENCY` HPG reflow gap — not re-litigated here). `dev_commit ad26f2f95` + `qa_commit 568e3b404` confirmed real and on main ancestry; `git show --stat` matches all 5 named files exactly. Read the full diffs, not trusted from the prior qa_note prose: AC1 (fail-loud) — `triggerPushBctcExtraction()` now returns a discriminated `PushBctcExtractionOutcome` (`done|async_routed|failed`); all-tiers-exhausted / `runPipeline()`-null / `runPipeline()`-throw all now return `{outcome:"failed"}` instead of silently falling through to implicit success; `applyPushBctcExtractionOutcome()` marks `bctc_vps_queue` status='failed' (retryable, NEVER 'done') + fires `sendTelegramBug`. AC2 (large-PDF path) — confidence pre-gate (reuses EXISTING `extractPdfText()`/`PDF_CONFIDENCE_HIGH_THRESHOLD` classifier, no new constant) routes `confidence<1.0` PDFs to `ensureShellRow → overwritePdfPathUnconditional` (MUST-FIX Risk1, verified genuinely no `IS NULL` guard) `→ triggerPekExtraction`; `confidence===1.0` keeps the unchanged sync gauntlet.

Re-ran independently: `bun test` on the 3 touched test files = 29/29 pass (matches dev's own count); `bun tsc --noEmit` 0 errors; DDD scan clean (both touched files are interface/scheduler layer, not `domain/`); security scan clean; `mock-guard.sh --files` PASS on both production files. Test suite is substantive (13 dedicated tests, real in-memory DB, not shallow assertions). Confirmed current `main` HEAD still carries this logic unreverted; `mcp-server` container currently Up healthy (rebuilt many times since 2026-07-13, none reverted this fix).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 791→791). `next_agent: pm`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S46.

## cycle-525 · 2026-08-06 · FIX-CADENCE-TNB-AUDIT-WEEKLY-MARKER-BLOCKS-DAILY-CRON — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `d0fe0f4fb` confirmed on main ancestry; `git show --stat` matches all touched files (tran-ngoc-bau/flow/main.md, cowork-team/flow/spawn-fanout.md, cowork-schedule.json, tools/package/tran-ngoc-bau.md, dwf-ops-runbook.md). AC(1)-(3) re-read live in-file: Step G derives `WORK_DATE` from `TZ="Asia/Ho_Chi_Minh" date +%Y-%m-%d`, keys `published:tnb-audit:<date>` ttl 100800; spawn-fanout.md "Weekly slots" now lists digest-sunday only; the key-period==cron-period invariant is written down in both files' comments + the runbook. AC(5): diff contains no release/immunity-semantics changes — UC-CCA-P3 territory untouched.

AC(4) — the row's own review_note said "NOT verifiable this session (requires the 2026-07-29T20:13Z fire, future)". Did not stop at a single-day check: grepped for concrete post-fix evidence across FOUR separate cycles instead of trusting self-report. `docs/signals/processed/cowork-team-2026-07-30T20:28:58Z.json` shows `published:tnb-audit:2026-07-31` live with a routine same-day mutex loss (peer claimed ~6min prior — normal daily-key behavior, NOT the multi-day blackout). Committed `docs/handoffs/tnb-audit-latest.md` c121 (2026-07-31T20:23Z, committed at `ebbf62699`) states `published:tnb-audit:2026-08-01 -> claimed:true ... confirmed still holding correctly, no collision`. `tran-ngoc-bau` notebook c122 (~2026-08-04T20:29Z) shows `claimed:true` on `published:tnb-audit:2026-08-05` with a full audit executed 6 days post-fix. Zero GATE-BLOCKED/`claimed:false`/"already published" hits for tnb-audit found anywhere post-fix, versus 3 confirmed GATE-BLOCKED cycles (c116/c117/c118, 07-22..07-24) pre-fix under the old weekly key, matching the row's own predicted blackout exactly — the predicted recurrence is gone after the fix, corroborated across 07-30/07-31/08-01/08-04/08-05.

Zero `.ts`/production source touched (docs/config only) — `bun test`/`tsc --noEmit`/DDD/mock-guard structurally N/A per verify-committed's own "targeted zone suite" clause. Ran the actually-relevant targeted suites instead: `node scripts/agents-flow/cowork-schedule-consistency.test.js` 9/9 PASS; `node scripts/agents-flow/cowork-catchup-predicate.test.js` 34/34 PASS (confirms `publish_date_basis:"vn_date"` is a pre-existing recognized basis in the live catchup-predicate logic, not a new unsupported value). `jq empty` on both touched JSON files: valid. DJ-GATE-1: confirmed `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-agent-father.md` STEP agent-father-S2 already carries the task-id.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 791→791). `next_agent: pm`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S46.

## cycle-525 · 2026-08-06 · FIX-NOTEBOOK-PRUNER-LINE-ONLY-SETPOINT-BYTE-CAP-NEVER-CONVERGES — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

No `.commit` field on the row — derived via `git log` on `files[]`, cross-checked date against `reviewed_at`: commit `b42f3fa3a` confirmed on main ancestry. Diff matches the row's primary claimed file `scripts/agents-flow/notebook-auto-prune.sh` (early-exit :135 + loop-break :174 both now require `lines<=LINE_CAP AND bytes<=BYTE_CAP`, `BYTE_CAP=LINE_CAP*60` read from the same SSOT `context-bloat-backstop.sh` reads, never a second hardcoded literal) and ships the new `notebook-auto-prune.test.sh` AC-5 requires. The other 3 `files[]` entries (`context-bloat-backstop.sh`/`.test.sh`, `notebook-write/SKILL.md`) are NOT in this commit's diff — consistent with the row's own note text, which explicitly scopes those as adjacent-verify-only, not required to touch; re-ran both anyway.

Re-ran on CURRENT HEAD, not just the original commit — 6 later commits touched this same file for other tasks since. `notebook-auto-prune.test.sh` 8/8 PASS: T1 byte-axis 142L/30403B→49L/10166B, T2 line-axis 250L/3920B→167L, T3 within-both-caps untouched, T4 single-section byte-only safe-fail no truncation with `byte_cap` in payload — exactly this row's AC1-4. `context-bloat-backstop.test.sh` 5/5 PASS, unaffected. Production corroboration: of the 18 notebooks the row measured over-cap 2026-07-25, 17 converged under both caps unattended by 2026-08-06 (re-measured live); the 1 holdout, `digest-predict.md` (37L/13918B), is hitting the documented single-section safe-fail path exactly as spec'd (option [a]) — a signal fired, no truncation, the accepted structural edge case the row's own note flagged as marginal — not a defect. AC6 held: `SPIKE-SATURATED-COUNT-THRESHOLD-GATES-SWEEP` still `READY`, not closed, as instructed. `bun test`/`tsc` structurally N/A (0 files under `apps/` touched); `mock-guard` N/A (`.sh`, not `.ts` production source); DDD/security scan clean.

Two adjacent issues surfaced during verification, NEITHER held against this row: (1) `po_regression_flag_20260728` (wrong-section-dropped same-day tie) is real but a SEPARATE defect, already fixed by later commit `c280e00cd` (own row `FIX-NOTEBOOK-AUTOPRUNE-SAMEDAY-TIE-DROPS-NEWEST`, still `REVIEW`, out of scope here). (2) Discovered incidental regression: legacy `test-notebook-auto-prune.sh` Test 9 now crashes (exit 1) under zsh invocation, caused by a LATER unrelated commit `7552421bc` (lib extraction via bash-only `${BASH_SOURCE[0]}`) — confirmed production-safe (`.claude/settings.local.json:62` always invokes this hook via explicit `bash`, never zsh); reported to bug channel for that commit's owning row.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 791→791). `next_agent: pm`. `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S50.

## cycle-525 · 2026-08-06 · FIX-CHEF-EVENING-DUP-DATE-MISLABEL-INVESTIGATE — CHANGES_REQUESTED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Derived commit `8cb767471` via git log on chef.md/cowork-schedule.json — confirmed on main ancestry, matches review_note: chef.md Step 0.5 pins ONE canonical `CYCLE_DATE_UTC`, reused verbatim at Step 7.6 filepath+date_vn, Step 8b header, marker key; `cowork-schedule.json` publish_date_basis vn_date→utc_date. AC(1)/(2) CONFIRMED across all 9 post-fix evening synthesis JSONs sampled (07-29→08-06): zero VN-local leaks, all 3 surfaces always mutually consistent = UTC date of execution.

AC(3) ("exactly one `published:chef-evening:<date>` marker per real calendar day") NOT held — two live pieces of evidence found, neither the original mechanism: (a) `unified-agent-synthesis-2026-07-30-evening.json` + `-2026-07-30-chef-evening.json`, two full independently-computed dishes 6m29s apart, same day, both correctly UTC-dated but divergent filename/dish_type — chef.md gates all synthesis behind the Step 0.5 marker claim, so both reaching Step 7.6 persist implies both claims succeeded (SLOT_ID not canonicalized at Step 0.5, only the date was); (b) fresh signal_queue entry filed TODAY (`cowork-20260806T064440Z-chef-evening-retry-date-drift`, HIGH, RAW-verified, routed to po) — this row's own wall-clock-pinned `CYCLE_DATE_UTC` has no backfill-target-date concept, so a 10h43m-delayed retry orphaned 08-05 evening + prematurely tombstoned 08-06's marker 13h early.

`bun test`/`tsc`/`mock-guard` structurally N/A — commit touches only `chef.md` (flow doc) + `cowork-schedule.json` (config), zero `.ts` production source.

VERDICT: CHANGES_REQUESTED. Component 1 (VN-local leak) durably fixed, credited. Held at REVIEW pending po adjudication of AC(3) (SLOT_ID canonicalization scope-fold vs `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS`; backfill-target-date param routed to architect). Moved `task_board.qa[]`→`task_board.review[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 791→791, qa 5→4, review 241→242). `next_agent: po` (row's own `.owner`), `redispatch_count: 1`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S51.
