# QA — Notebook


## cycle-572 · 2026-08-08 · FIX-AUDITOR-DURABILITY-STEP0B-DETECTION — CHANGES_REQUESTED (direct-commit verify, `qa[]` row, `branch:null`, commit `92ef5b3a2`+`af9ad098c`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Commit real, on main ancestry; `git show --stat` matches the claimed file (`docs/agents/system-auditor/flow/main.md`) exactly. Read the full diff myself, not the review_note prose: Step 0b.1 (stale-marker sweep, mtime>20min, reuses `emit-audit-signal.sh`'s 7-day dedup ledger), Step 0b.2 (schedule-based tier2/3 heartbeat staleness + WARN-only tier-1 OR-check), Step 0b.3 (stub) + the Notebook Write/Commit block reorder + Notebook Append Gate condition-(b) rewire all correctly match the claims — grep regex is an exact match to `audit-output-contract.sh`'s 5 non-ABORT marker forms; `jq fromdateiso8601` threshold logic correct. Dependents `FIX-AUDITOR-DURABILITY-SKILL-DRAFT-PERSIST`/`FLOW-DRAFT-HEAL` correctly `depends_on` this row.

Went past text-correctness into LIVE production evidence (~44h since deploy) — this is what flipped the verdict. Two genuinely FRESH orphaned markers exist, created by real post-deploy Tier-1 cycles: `.auditor-cycle-markers-2026-08-06T21:09:00Z.tmp` (a real `[emit-signal] SKIP-dedup` line — won its fire-election, died mid-run, exactly the shape Step 0b.1 targets) and `.auditor-cycle-markers-2026-08-07T00:00Z.tmp` (`[cycle-init]` only). Both are 30h+ past the 20min staleness bar; dozens of subsequent Tier-1 cycles ran since (live notebook entries through today, `c86` @10:33:53Z) yet neither got swept — and zero `D-CYCLE-1` rows exist anywhere in `.signal_queue` (live+archive) or `auditor-dedup-ledger.json`. Independently computed Step 0b.2's own tier-3 predicate right now: `auditor-tier3-last-healthy.json` is 52h35m stale, past the step's own 48h WARN bar by ~4.5h — unfired, zero `D-CYCLE-2` ever emitted (tier-2 is within its own bar, so this is tier-3-specific). Also found 6/8 marker files got accidentally git-tracked by an unrelated commit (`2ddbe5321`, pm) — breaks the "`rm -f` is safe reclaim" premise the architecture-brief explicitly assumed ("currently sit untracked"); `.gitignore` has no entry for this pattern unlike the sibling `docs/data/cycle-snapshot-*.json.tmp`.

VERDICT: CHANGES_REQUESTED. The prose/logic itself is sound (verified independently), but there is zero confirmed live execution despite continuously-satisfied trigger conditions — an unproven "durability" mechanism should not yet unblock 2 dependent tasks that build on it. Moved `task_board.qa[]`→`task_board.review[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 767→767), `redispatch_count: 1`, routed `next_agent: developer` (row's own `owner`, not fixer — no task branch exists, direct-commit path only). Full evidence + fix-ask in row's own `status_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S7. Bug-channel telegram flagging the live tier-3 52h+ gap ATTEMPTED, not sent — `mcp__gateway__call_tool` unavailable in this sub-session (no gateway tool grant, same known local-cowork-subagent gateway-blind limitation as prior cycles) — flagged for router to relay; finding is durably captured in the row's own `status_note` regardless. Pruned cycle-569 this write per AC-2 retention (3-section cap) — kept 570/571/572.

## cycle-573 · 2026-08-08 · FIX-DEVTEAM-IDLE-CHAIN-P2A-DURABLE-DRAIN — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `5ad4a3f92`)

Head-decoupled QA-Drain dispatch (mode=verify-committed), one of a batch of 7 rows sharing `claimed_at`. Commit real, on main ancestry; `git show --stat` matches both claimed `files[]` (`drain-signals.md`, `drain-signals.js`) exactly (plus `drain-signals.test.js`, expected but not separately claimed). Read the full diff, not the review_note prose: both §0a-1 (file channel) and §0a-D (dashboard channel) correctly reordered to non-destructive-classify → ONE `orch-apply.sh`-gated batch append to `.dev_team_idle_chain.pending_triage_inbox` → gated destructive mv/fingerprint/DB-INSERT (§0a-1) or NEW→READ flip (§0a-D, same write) only on append success; failure path retains everything untouched, logs WARN, releases claims for retry — matches architect brief `docs/architecture-briefs/2026-07-25-devteam-idle-chain-rotation-durable-inbox.md` §3.1. `dev_team_idle_chain` already Zod-schema-supported (prior dependency task `TASK-DEVTEAM-IDLE-CHAIN-1-SCHEMA-UTILITIES`) — no schema gap.

Ran `drain-signals.test.js` myself, not the self-report: 46/46 pass incl. new durable-append-FAILURE case (no orch-state.json → file RETAINED, not moved, stdout/stderr assertions) and durable-append-SUCCESS case (golden stdout unchanged + `pending_triage_inbox` gets exactly 1 real envelope with `routed_to` resolved via the mirrored §0a-3 table, payload deep-equal not a pointer) — assertions are concrete, not trivial. `bun tsc --noEmit` structurally N/A at repo root for this change (plain `.js`/`.md`, not TS-app-scoped; root `tsconfig.json` lacks a `bun-types` devDep — confirmed pre-existing, unchanged by this commit, not a regression it caused). `mock-guard.sh --files` on `drain-signals.js` → PASS. Went past unit tests to LIVE production evidence: live `orch-state.json`'s `.dev_team_idle_chain.pending_triage_inbox` holds 21 real envelopes with `drained_at` timestamps through 15:26:06Z, and `.dev_team_idle_chain._updated_at`/`rotation.qa_drain.last_served_tick` both read 15:28:29Z — the mechanism was observably firing this exact tick, not just green in isolation.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 771→771, signal_total 239→239). `next_agent: pm`. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S9. Pruned cycle-570 this write per AC-2 retention (3-section cap) — kept 571/572/573.

## cycle-574 · 2026-08-08 · FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `6ff38d27e`, final confirming pass)

This router session (165f4245) already RAW-verified `6ff38d27e` earlier the same day at elevated rigor (safety-critical A-30 mem-reclamation gate: recurring-bug occurrence #4, P0, agent-father supervised router-direct dispatch per `po_redispatch_ruling_20260808T1445Z`). This pass re-confirms the same facts still hold at HEAD rather than re-investigating from scratch. Re-ran ancestry (`git merge-base --is-ancestor 6ff38d27e main` OK) and `git log 6ff38d27e..main` over all 7 PLANE A/B files — empty, zero drift, no peer commit landed on any of them since the earlier pass. Row's own `files[]` (stale, predates the closeout) names PLANE A's `auditor-tier1-probe.sh`/`auditor-launchd-ack.json` — neither touched by this commit, checked not waved through: both last touched by earlier already-CLOSED PLANE A commits (`2e2446853` 07-30, `3b096d4b2` 07-29), correctly untouched per qa's own 2026-07-28 "do NOT re-open" ruling. Commit's actual diff (`tier1-probe.md`, `probe.sh`+test, `verify-a30-mcp-memory-reclamation.sh`+test) matches the closeout narrative exactly.

Root fix confirmed present via grep: `_a30_run_investigate_gate()`/`_a30_resolve_capped_containers()`/`_a30_gate_decision()`/`_a30_invoke_deep_probe()` all live in `probe.sh`, evaluating the ≥85% deep-probe gate PER CONTAINER (was: once, against mcp-server only — the c51/c53 same-day matched-pair false-negative the ruling documented). Amendment A confirmed: `vmrss_kb`/`VMRSS_KB` fully absent from `verify-a30-mcp-memory-reclamation.sh` outside comments. Amendment B confirmed: both surviving VmHWM `docker exec` call sites gated behind `_a30_headroom_ok()` — host-side `docker stats`/`docker inspect` arithmetic only, zero exec to decide, sourcing PLANE A's already-shipped `_mem_headroom_mib()`/`MEM_FLOOR_MIB=40` read-only (`scripts/agents-flow/auditor-tier1-probe.sh` has zero diff at this commit, its own suite re-confirmed clean). Re-ran all 3 claimed suites myself, not the self-report: `probe.test.sh` 16/16 (incl. T8-T13 gate-decision + c51/c53 replay), `verify-a30-mcp-memory-reclamation.test.sh` 15/15 (incl. T13 below-floor-skips-exec, T14 escalate-via-minpct-fallback), `auditor-tier1-probe.test.sh` (PLANE A, untouched) 181/181 — zero regression, identical counts to the closeout claim. `mock-guard.sh` correctly N/A (bash-only, tool scope `.ts/.tsx/.js/.py/.go`); `bun tsc` N/A (zero apps/ TS touched).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 771→771, signal_total 239→239). `next_agent: pm`. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S8. Pruned cycle-571 this write per AC-2 retention (3-section cap) — kept 572/573/574.
