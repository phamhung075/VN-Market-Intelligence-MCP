# QA — Notebook

## cycle-723 · 2026-08-14T13:00:58Z · UC-CCA-P1-GWBLIND-DEDUP (report-analyzer, commit `a27d7cd21`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Commit `a27d7cd21` confirmed on `main` ancestry; `git show --stat` = exactly 1 file (`.claude/skills/step-0-cowork/SKILL.md`, 40 lines: 4 ins/36 del), confirming `no-flow-rewiring-attempted` (part (b) correctly left for TE-T11, already `DONE_VERIFIED`).

Re-verified all 4 AC-trailer items at source, not from commit prose: (1) `dup-block-deleted` — old lines 53-88 (GATEWAY-BLIND classification table + fallback Write() schema + DEFER protocol) confirmed gone, fleet-wide grep found 0 residual copies; (2) `stub-le-4-lines` — counted exactly 4 content lines in the replacement; (3) `points-at-cycle-bootstrap-error-handling-ssot` — target `cycle-bootstrap/SKILL.md:93 § Error handling (fail-loud)` confirmed real, carrying the full CONFIRMED-BLIND/TRANSIENT table + retry logic + Write-fallback protocol verbatim, not a phantom pointer; (4) confirmed by the diff scope itself (1 file only).

Cross-checked audit brief `2026-07-12-ultracode-workflow-improvement-audit.md#cowork-cycle-agents-P1` Rescope item (2) "amend TE-T11 DoD text" — already satisfied pre-existing (TE-T11's `note` field already reads "composite POINTS to ...SSOT (not full embeds)", `DONE_VERIFIED` 2026-08-06, predates this commit — a PO RESCOPE-time joint edit, not a gap left by this row).

Pure `.claude/skills/*.md` docs change, zero TS/Go production source touched — `bun test`/`tsc --noEmit`/DDD-import-grep/`mock-guard.sh` structurally N/A (same precedent as TE-T11's own verify, its sibling row).

DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa-2.md` STEP qa-S30.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. Added a `status_note` field (row had none) carrying `[QA] Review Record (direct-commit verify)`, attached `verification.raw_probe` on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 740=740, signal_total 27=27; retried once after a CAS mtime concurrent-write reject, second apply succeeded). `.head.active_task_id` is a DIFFERENT row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, same QA-Drain batch) — left untouched. This sub-session's tool grant has no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:UC-CCA-P1-GWBLIND-DEDUP` and is the authoritative release path (per spawn prompt, explicitly not this session's responsibility).

## cycle-722 · 2026-08-14T13:00:27Z · FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH (dev-mcp-server, commit `1b9425d6f`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Commits `1b9425d6f` (fix+test) and `b1a05ab56` (notebook/DJ) confirmed on `main` ancestry; `git show --stat` matches the row's sole `files[]` entry (`pdfOcrWorker.ts`) plus the new regression test. Did not trust dev-mcp-server's own claims — re-verified everything at source.

Diff read at source: AC1 — threshold now `Math.max(Math.min(expectedPages, OCR_MAX_PAGES=80)*0.5, 3)` instead of raw `expectedPages`, so a full capped-80 extraction reaches a terminal "already extracted" state — confirmed correct. AC2 — the upfront blanket `DELETE FROM pdf_extracted_text WHERE filename = ?` is gone; the pre-existing per-page `INSERT OR REPLACE` UPSERT keeps stale rows visible in place, and a new defensive `DELETE ... AND page_number > ?` prune runs ONLY after the write loop (write-before-delete, confirmed by line order). AC3: own `bun test` on the new file → 2/2 pass; own targeted 8-file OCR suite run (292-ocr-audit, BPE-DEV-3-ocr-coverage-fixes, 1068-reparse-ocr-fallback, 1352c-ocr-health-logging, 1945d-reparse-pipeline-gap, FIX-CTG-3-STEP-D, 296-ocr-pipeline-e2e + new file) → 89 pass/1 skip/0 fail, 90 tests — exact match to dev's 89/89 claim. `tsc --noEmit` clean, `mock-guard.sh` PASS, DDD/secret greps clean.

AC4 (live 3-file boot verify): dev-mcp-server honestly reported "NOT ATTEMPTED" (no restart in their session, correctly deferred to ops). QA closed the gap instead of accepting it as-is: `vn-market-intelligence-mcp-mcp-server-1` started 2026-08-13T19:18:35Z (after the fix commit), confirmed running fixed source in-container. Queried live `market.db` via `docker exec ... bun -e` (bun:sqlite readonly): all 3 named files (GVR_2026_Q1.pdf, GVR_2025_Q4.pdf, MSN annual-report PDF) stable at c=80/maxp=80. `docker logs --since` boot window grepped for all 3 filenames: only `"[pdfOcr] already extracted"` with `cappedExpected:80` fired, zero `"starting"`/`"incomplete extraction detected"`/DELETE — AC4 confirmed PASS on live evidence QA obtained directly, beyond the dev's own non-claim.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S127.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to a new `status_note` field (row had none), attached `verification.raw_probe` on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 740=740, signal_total 27=27). `.head.active_task_id` is a DIFFERENT row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, same QA-Drain batch) — left untouched. `next_agent` deleted on the DONE_VERIFIED row (terminal-row convention). This sub-session's tool grant has no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH` and is the authoritative release path (per spawn prompt, explicitly not this session's responsibility).

## cycle-716 · 2026-08-14T09:33:46Z · FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET (dev-mcp-server, commit `215010308`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Did not trust dev-mcp-server's own 19/19 + 15289/56-full-suite claim — re-verified independently. Commit `215010308` confirmed on `main` ancestry; `git show --stat` matches all 6 claimed files (3 test files + `newsChainFallback.ts` + `schema-financial-reports.ts` + `usecases.md`). Own `bun test` on all 3 touched test files → 19/19 pass exact match. `tsc --noEmit` clean, `mock-guard.sh` PASS on both prod files, security greps clean. DDD scan flagged pre-existing app→infra imports in `newsChainFallback.ts` — confirmed byte-identical on the parent commit, not introduced by this diff.

Read every diff hunk at source: grep-confirmed zero remaining `INSERT`/`UPSERT` into `financial_reports` (only a read-only arm-b1 SELECT remains); new `bctc_news_fallback_hints` table has 7 hint-scalar columns only, zero balance-sheet columns. All 6 named tests + Finding F-1's 7th genuinely rewritten (RED-1/6/7/8-first-call + both ID-ORPHAN Q1/Q2 cases) to assert writes land in the new table + zero `financial_reports` rows created — not deleted, not skipped. AC-4 honest-absence and arm-(b1) non-regression both gained new SERVING-plane (`get_bctc_full`) assertions, confirmed passing live ("Chua co du lieu BCTC", never "[CORRUPT DATA"). AC-6 RAG-non-leak locked by new `FR-5` test (`insertAnalysisFn` never invoked on the fallback branch), confirmed passing.

Did NOT independently reproduce the full 15289/56 suite claim — own full-suite re-run was still in-flight (I/O-bound) past a reasonable wait window; not blocking, the flow's Direct-Commit Verify spec requires only the touched-file targeted suite (satisfied, exact match).

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S123.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. Added a `status_note` field (row had none) carrying `[QA] Review Record (direct-commit verify)`, attached `verification.raw_probe` (own `bun test` run) on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` was a DIFFERENT task (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`, same QA-Drain batch) — left untouched. This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET` and is the authoritative release path.

## cycle-715 · 2026-08-14T09:31:14Z · TASK-COWORK-MUTEX-001 (report-analyzer, commits `f8d3891c6`+`465801802`) — Direct-Commit Verify, CHANGES_REQUESTED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Both commits confirmed on `main` ancestry, `git show --stat` matches every claimed file. BLOCKING FINDING: the probe is textually complete but functionally unreachable from the router's real hot path — `CLAUDE.md:7`/`dispatch-claim/SKILL.md` header both declare `CARD.md` the actual per-dispatch hot-path read, and `CARD.md` (39L) carries ZERO mention of Step 2.4/cowork-slot (grep-confirmed). Reproduces the exact "silently disables the phase" failure the row's own FR-6 edge case exists to prevent, one layer removed.

SEPARATE OBSERVATION (not mine to fix, flagging only): this notebook file itself lost two of my own earlier same-session sections (`cycle-712`/`cycle-711`, and a peer's own `cycle-713`) to a live concurrent-write race across this batch's parallel QA-Drain sub-sessions — the known `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries` class, now reproducing on `qa.md` itself, not just `po.md`.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S123.

VERDICT: CHANGES_REQUESTED. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (row has no `status_note`). Moved `task_board.qa[]`→`review[]`, `status: QA→REVIEW`, `redispatch_count: 0→1`, `next_agent: developer` (row's own `owner` field) via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` was a DIFFERENT row — left untouched. No `mcp__gateway__call_tool` binding — no `task_release` call made; coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) is the authoritative release path.
