# QA — Notebook


## cycle-560 · 2026-08-08 · FIX-ORPHAN-FR1-FR3-FR6-SKILL-DISPATCH-CLAIM — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commits `234902038`+`5ac21c593`+`749d0e307`)

dev-team Review-Lane QA-Drain batch dispatch. All 3 commits real, on main ancestry, `git show --stat` matches claimed files exactly (SKILL.md only for code commit — zero `apps/` TS/Go touched). Did not trust `review_note` prose — read the live diff + current file myself. FR-1: cross-checked the central factual claim against live source — `coordinationTools.ts` `task_heartbeat` Zod schema still only `task_id`/`owner_client_session` (confirmed by reading the tool registration directly), so the rewritten "NOT YET reachable via the live tool schema" prose is accurate; `coordinationStore.ts` `HeartbeatOptions`/`heartbeatTask()` confirms the backend `payload_patch`/`ttl_seconds`/`owner_agent` support is real (sibling `FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER`, commit `d6c4e6006`, on main). FR-3: read the full guard (SKILL.md :359-440) — batch `board_snapshot` hoisted outside the per-signal loop (EC-8), one jq pass covers flat lanes + `active_sprints[].tasks[]` (EC-2), `task:` prefix stripped (EC-1), lane classification matches the acceptance line (ready/in_progress=active; review/qa/done/done_verified/backlog=terminal, no BLOCKED carve-out; not-found=terminal, never defaults active), runs before the `redispatch_count>=N_MAX` branch. FR-6: `owner_agent` present at the escalation `task_heartbeat` call (:454), additive, correctly annotated NOT-YET-LIVE. Header size-justification (593L declared) matches actual 593L; fence-balance even (26). `bun test`/`tsc`/`mock-guard` correctly N/A (zero production source in scope). DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-4.md` STEP developer-S79 present, task-id stamped.

NON-BLOCKING OBSERVATION (flagged in `status_note`, not actioned): file's byte-cap predicate (TE-T24, cap×60=12000B) breaches at 31235B — this predicate is never honored by a size-justification header (byte-cap independent of line-cap, per `context-bloat-backstop.sh`'s own design). Pre-existing (file already 23933B, 2x over, before this task) and worsened +7302B, but all added content is load-bearing FR-1/FR-3/FR-6 text. Per standing precedent (`feedback_ctxbloat_breach_on_live_sprint_file_defer`) this class routes to po/claude-manager-helper's governance sweep, not a QA merge gate — not blocking.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 778→778). `next_agent: pm`. `.head` already idle/unrelated to this row (head-decoupled dispatch per spawn prompt) — untouched. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S4 (concurrent sibling QA sub-agent also wrote §qa-S3 to the same file this window — appended after their entry, no overwrite).

## cycle-561 · 2026-08-08 · OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX — DONE_VERIFIED, BRANCH B (unusual dispatch: PO-adjudicated AC-3 measurement, not a standard code verify)

Router required stating which of PO's 4 named AC-3 branches (`po_ac3_adjudication_20260808T0820Z`) this landed on, no 5th allowed. Confirmed the unload log line myself (`docker logs --timestamps`): `"Embedding model unloaded after 910s idle (threshold=900s)"` @08:32:59Z — rules out Branch C/D. Cross-checked dip magnitude against 2 independent sources rather than trusting the router's relayed figure: PO's own pre-unload baseline (96.19-98.34%) vs system-auditor's committed cron reading (77.98%/798.5MiB, `sys-20260808T083446-4705`, DASHBOARD.md:890-897) = ~18-20pp, well short of the ~35pp bar → BRANCH B (code correct, OS allocator not returning pages — same class already ratified for pdf-extractor via `malloc_trim(0)`). Reproduced on a 2nd load/unload cycle myself (reload 08:49:50Z→97.67%/1000MiB @08:56:57Z).

VERDICT: this row DONE_VERIFIED (AC-1/2/4 PO-verified+re-confirmed live by me; AC-3 satisfied on Branch B's code-correctness bar). Minted `FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS` (backlog, P2) in the same write (conservation OK, task_total 778→779). Did NOT touch `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH`'s status — out of this dispatch's claimed lock scope; its `depends_on` edge on this row now discharges. `next_agent: pm`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S5.

## cycle-562 · 2026-08-08 · UC-CRITIC-HOOKS-ENFORCEMENT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, commit `f4d35b5df`)

Head-decoupled Review-Lane QA-Drain dispatch. Commit real, `git merge-base --is-ancestor f4d35b5df HEAD` = true. Did not trust the implementer's own self-report numbers — reproduced everything myself. Read all 4 named hook scripts (`orch-state-hook-bash-backstop.sh`, `context-bloat-backstop.sh`, `notebook-auto-prune.sh`, `branch-hygiene-stop.sh`) on disk: each now `source`s new `scripts/agents-flow/lib/hook-guard.sh` (`hg_run` merged-stream discriminator + `hg_resolve_project_root`) and exits 1 (not swallowed) at every named input-boundary guard when a prerequisite (`git`/`jq` binary, stdin capture, SSOT parse) itself crashes — distinct from the pre-existing "legitimately nothing to do" exit-0 no-op paths, which are unchanged. `branch-hygiene-stop.sh` correctly uses the OPPOSITE polarity (its `git` calls feed a report, not an early exit) — confirmed by reading it, not just trusting the commit message's claim of this.

Cross-checked the live-file claim directly (not from prose): `.claude/settings.local.json` is untracked/gitignored on this machine (`git check-ignore` confirmed) — `jq`-parsed its `hooks` block myself and confirmed the outer `2>/dev/null || true` is DROPPED on exactly these 4 invocation strings; `tmux-agent.sh status` + the `graphify` PreToolUse hook still correctly retain `|| true` (out of scope per architect's FR-6 exemption, not silently missed). FR-5 confirmed via `git log`: zero commits touching `orch-state-hook-prewrite.mjs` since its last unrelated commit (`5dc629d3c`) — the one fail-closed hook stays untouched as required.

Ran all 4 regression suites myself rather than trusting the claimed pass counts: `branch-hygiene-stop.test.sh` 4/4 (T4 = deliberate crash injection, PATH override strips `git`, asserts exit 1 + `PREREQUISITE-FAILURE`, not read as clean); `context-bloat-backstop.test.sh` 5/5 (T5 = corrupted `file-size-caps.json`, asserts exit 1, not the pre-fix silent "non-governed path" exit 0); `notebook-auto-prune.test.sh` 8/8 (T8 = `jq` missing via PATH override, exit 1); `bun test orch-state-hook.test.mjs` 21/21 incl. AC-3's 2 new crash-injection cases (jq-unavailable PATH override + read-only `TMPDIR`, both assert exit 1 + `PREREQUISITE-FAILURE` on stderr). This is the exact regression-test gap the row's title named ("a crashed validator is indistinguishable from a pass") — now closed and test-locked on all 4 scripts, not just claimed. New A-33 "Hook Enforcement Liveness" check confirmed present in `system-auditor/flow/tier1-probe.md` (FR-3). `dev-standards.md` CANONICAL blocks updated to match live behavior. `shellcheck` on all 5 touched scripts: 2 pre-existing warnings only (SC2254/SC2034 in `context-bloat-backstop.sh`), confirmed via `git show` diff to be on lines NOT touched by this commit — no new lint regressions introduced. `git status` clean on every file this task touched.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 775→775, signal_total 230→230). Verification text appended to row's own new `status_note` field (no handoff file). Committed pathspec-scoped (`f77daa61e`).

## cycle-563 · 2026-08-08 · FIX-CI-TASKCLAIM-CHEF-MD-OWNER-SESSION-PAYDOWN — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `be3545412`)

Head-decoupled Review-Lane QA-Drain dispatch. Commit real, on main ancestry, `git show --stat` matches both claimed files exactly (`chef.md` + `task-claim-owner-session-baseline.json`). Re-ran, did not trust dev's self-report: `task-claim-owner-session-lint.sh --check` RC=0/0 offenders/19 grandfathered (matches claimed 20→19 drop); `task-claim-owner-session-lint.test.sh` 9/9 PASS. `chef.md:108-113` `owner_client_session` uses the substitution-instruction pattern, byte-diffed against `pm/flow/main.md:127` — matches (AC4 held, no literal `$CLAUDE_CODE_SESSION_ID`). `baseline.json`: jq-confirmed zero entries remain for `chef.md`.

AC3 CI-plane, verified live not assumed: `gh run view 31252190892` — headSha `72814d824` is a confirmed descendant of `be3545412` (itself after `246eb859`); job `task-claim-owner-session-lint` conclusion=`success`. Overall workflow conclusion=`failure`, but the sole failing job is `size-lint` — checked `.github/workflows/ci.yml`, confirmed these are two genuinely distinct named jobs (`size-lint-justification.sh` file-size ratchet vs `task-claim-owner-session-lint.sh` owner-session ratchet), not the same CI job under two names. Dup-check vs sibling ask: searched for any live `FIX-CI-SIZELINT-*` row — all 8 found are archived `DONE_VERIFIED` in `docs/data/orch/archive/2026-08.json`, none live on board — confirmed not a duplicate of this row. AC5 (re-key baseline file+snippet vs line) explicitly deferred by dev citing the sibling first-instance row's standing ruling (gate-widening needs an actuator dry-run, out of scope for a same-shape fix) — accepted, matches precedent.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh`; write landed in a concurrent peer commit (`f77daa61e`, same-second sweep) — working tree verified clean/matching HEAD post-write, no re-commit needed. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S18.

## cycle-566 · 2026-08-08 · CI-PERFILE-STRUCTURAL-MITIGATION — APPROVED, DONE_VERIFIED, commit `edea85e66` — full record in DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S24. Independently verified 27/29 real post-fix CI runs green (93%, vs pre-fix ~17%) via `gh run list`/`gh api`; fix is generic runner infra (parallelism auto-detect + rc-fail-loud), not a single-file patch — confirms general nondeterminism fix, not papering over one rotation. 2/29 residual on `1281-cooldown-config.test.ts` flagged non-blocking (own Date.now()-boundary timing issue, distinct mechanism). AC-5 literal bar (20-consec) unmet but strongly corroborated; AC-6 left open per row's own note. Board write (`.task_board.qa[]`→`.task_board.done_verified[]`) + DJ entry independently durable regardless of this notebook line's own repeated-clobber race (known concurrent-write issue on this file).

## cycle-565 · 2026-08-08 · FIX-DEVTEAM-QADRAIN-INVOCATION-HEAD-DECOUPLED — APPROVED, DONE_VERIFIED, commit `92ff5fb43`+`f1fddde0d` — full record in DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S19. Board write (`.task_board.qa[]`→`.task_board.done_verified[]`, conservation OK 776→776) + DJ entry independently confirmed durable regardless of this notebook line's own repeated-clobber race.
