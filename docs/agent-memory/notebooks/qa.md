# QA — Notebook

## cycle-524 · 2026-08-06 · FE-PG-BCTC-EVAL-_INDEX-FUNC-FIX — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had no top-level `commit`/`files[]` — derived `aaf487834` from review_note prose ("Committed aaf487834 (8 files, apps/frontend/)"), confirmed on main ancestry, author-date 2026-07-24 (row only just claimed by the drain today, 08-06 — pre-existing committed work awaiting QA). `git show --stat` matches all 8 claimed files exactly (domain/bctc-eval.ts, StatusBadge.tsx, EvalTable.tsx, dashboard.bctc-eval._index.tsx, api.bctc-eval.$.tsx, 2 test files, domain-model.md). Commit carries correct `Task:`/`AC:` trailers.

review_note flagged its own root-cause divergence from the PO narrative (loader never threw; real crash was a render-time TypeError in StatusBadge destructuring an undefined STATUS_CONFIG entry, because live mcp-server payloads only carry `stage_statuses` keys for stages actually computed while the domain type declared all 6 required) — treated as a reason for MORE scrutiny, not less, per house convention on self-reported divergence.

Re-ran everything myself, not trusted from prose: targeted tests (`bctc-eval-list.test.tsx`, `bctc-eval-detail.test.ts`, `bctc-eval-proxy-trailing-slash.test.ts`) 36/36 PASS. Full frontend suite: 2183/2185 PASS — the 2 failures (`QUE-REFERENCE-PAGE-detail.test.ts`, `QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.tsx`, both "exactly 2 own keys" on `QUE_DESCRIPTIONS`) confirmed pre-existing + out-of-zone: `git log` on `que-descriptions.generated.ts` shows last touch was `067e484d8` (unrelated hexagram-tooltip codegen rewrite that added a 3rd `hoverSummary` key), zero overlap with this commit's 8 files; reproduces identically with unrelated peer dirty-tree changes stashed out. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` PASS on all 5 touched production files.

Went beyond static checks: LIVE-verified against the actually-running frontend container (`vn-market-intelligence-mcp-frontend-1`, up ~12d, consistent with the commit's own "rebuilt + recreated, image ID verified" note). `curl` direct: `GET /dashboard/bctc-eval` → 200, no "Something went wrong" boundary text, real ticker rows render (MBB/HVN/HPG confirmed in response body); `GET /api/bctc-eval` (no trailing slash) → 200 (was 404 pre-fix). This corroborates the commit's specific mechanism claim (trailing-slash proxy bug + StatusBadge undefined-status crash), not just a generic "page loads."

DJ-GATE-1: dev-frontend's own journal entry present (`sprint-QUALITY-AUDIT-FRONTEND-COVERAGE-dev-frontend.md` line 10, `task-id: FE-PG-BCTC-EVAL-_INDEX-FUNC-FIX`) — gate satisfied independent of my own entry.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 795→795). `next_agent: pm` (review_note claims this closes 5 sibling NEEDS_REVIEW rows — pm/po to re-run the quality-audit checklist and flip those, out of this row's own scope). `task_release` for `task:FE-PG-BCTC-EVAL-_INDEX-FUNC-FIX` attempted but unavailable — no gateway/MCP tool grant this qa sub-session (Read/Edit/Write/Bash only, same known limitation as qa-2's S38/qa-3's S40 notes); dispatcher TTL (3600s) is the authoritative fallback release path per WF-1. New decision journal file created this cycle: `sprint-QUALITY-AUDIT-FRONTEND-COVERAGE-qa.md` §qa-S1 (task's own sprint id, matching the existing po/dev-frontend journal convention for this sprint — not the currently-active ambient sprint).

## cycle-525 · 2026-08-06 · FE-PG-QUALITY-AUDIT-LASTVERIFIED-RENDER-FIX — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `3a9803677` confirmed on main ancestry (author-date 2026-07-25T14:05:48Z, matches row's `reviewed_at` 14:08:07Z within 2min); `git show --stat` matches the row's 1 declared file (`dashboard.quality-audit.tsx`) + 3 undeclared sibling files (new `check-verification.ts`+test, new e2e spec) — read all 4, code matches review_note claim-for-claim (LastVerifiedBadge fresh/stale/unknown 3-way, 7d D-PAGE window, typeof-guard wrapping the pre-existing unchanged `classifyStaleBadge`).

Re-ran independently: `tsc --noEmit` 0 errors. Targeted `check-verification.test.ts` 16/16 PASS (all 4 live timestamp shapes bare-date/second/ms/microsecond + 7d boundary + 4 non-string-guard cases, matches claim exactly). Full vitest 2183 pass/2 fail/2185 — the 2 fails (`QUE_DESCRIPTIONS` "exactly 2 own keys", same pair as cycle-524) reconfirmed pre-existing/out-of-zone (zero grep overlap, file untouched by this commit). `mock-guard.sh --files` PASS on both touched production files. DDD clean (`check-verification.ts` imports only `./stale-badge`, zero infra/route/component imports). Security clean; the one `process.env` line in the route file is pre-existing outside this diff (confirmed via `git show`), not new debt.

PENDING-REBUILD corroborated live, not trusted from prose: `docker inspect vn-market-intelligence-mcp-frontend-1` created 2026-07-24T18:02:36Z, ~20h BEFORE this commit. `curl :3001/dashboard/quality-audit` (200) grepped for the literal new column header `Last Verified` → zero matches; the `stale`/`UNKNOWN` hits present in the raw HTML are pre-existing check `evidence`/`question` prose (topically about staleness elsewhere, e.g. `vps_stale`), not the new badge component — live container genuinely does not yet serve this fix. Per `PUSH-AUTONOMY-1` §5 + established precedent (`FIX-PREDCLAIM-DASHBOARD-HITRATE-HONESTY` qa review, same shape) this is a separate ops/PO-owned single-service-rebuild gate, not a QA merge-gate blocker — held APPROVED at code level, all 4 ACs proven via unit tests + the dev's own throwaway-dev-server e2e run against real mcp-server :3000 data. Corrected the row's stale "Rebuild is USER-GATED" framing in status_note — that phrase is retired policy (`feedback_po_deploy_rebuild_full_autonomy_no_user_gate`, 2026-08-01: no user gate, PO/ops dispatch directly); no `VERIFY-<id>-REALDATA` task exists yet for this row (board-checked) — flagged for PO to mint + dispatch ops rebuild, not held against this row.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh`. DJ: `sprint-QUALITY-AUDIT-FRONTEND-COVERAGE-qa.md` §qa-S2.

## cycle-525 · 2026-08-06 · FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `120e5d42b` confirmed on main ancestry; `git show --stat` matches all 3 claimed files exactly (`pressure-cadence.md` Step 4.5c rewrite, new `scripts/agents-flow/cowork-chef-mutex.js`, new companion `.test.js`). Re-ran the row's own test myself, not trusted from review_note prose: `node scripts/agents-flow/cowork-chef-mutex.test.js` → 25/25 PASS (TC-1..8, incl. TC-7 CLI-stdin integration + TC-8 regression guard). `mock-guard.sh --files` PASS on the new production script. `bun tsc --noEmit` (apps/mcp-server) 0 errors — commit touches zero TS/apps files, so this is a no-regression baseline, not a targeted check (Smart-Skip applies: pure docs/ + plain-Node scripts, outside the bun/TS surface).

Went beyond the row's own claims: independently re-reproduced BOTH failure modes live under zsh against the REAL unmodified `docs/data/cowork-schedule.json` (not the test's synthetic fixture) — (a) the OLD `SCHEDULE=$(cat ...); echo "$SCHEDULE" | jq` pattern still throws the exact historical `jq: parse error: Invalid string: control characters ... must be escaped` on today's live chef-intraday `trigger_prompt`, proving the defect is real on live data, not theoretical; (b) the documented NEW Step 4.5c invocation (`printf '%s' "$MATCHES" | node cowork-chef-mutex.js` → `jq -n --argjson`) run against `MATCHES=[chef-morning, chef-intraday]` on that same real file yields `chef_mutex_applied:true`, `matches=[chef-morning]` only, `dropped=[chef-intraday]` — the row's own AC ("coincidence tick fires exactly 1 CHEF dish") satisfied end-to-end against live data, not just unit-asserted. DJ-gate: developer's own journal entry confirmed present (`sprint-FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT-developer.md`).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 794→794). `.head` untouched. `task_release` for `task:FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT` attempted via gateway (best-effort per INV-GATEWAY-1). New decision journal file created this cycle: `sprint-FIX-COWORK-CHEF-MUTEX-ECHO-JQ-DEFEAT-qa.md` §qa-S1 (task's own sprint id, matching developer's convention for this task).

## cycle-525 · 2026-08-06 · FIX-FB-GATE-CHECKD2-NONWAIVABLE-NUMERIC-BLOCK — CHANGES_REQUESTED, sent back (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commits `1b506cbdd`+`7b87ac372` confirmed on main ancestry; `git show --stat` matches all claimed files (3 flow docs + new `scripts/test-fb-gate-checkd2-nonwaivable.sh`). Diffs re-read directly (not trusted from review_note): main.md STEP 4b, weekly-recap.md STEP 3b, weekly-prediction.md STEP 4b all correctly rewrite waivability to per-check (Check-C prose only; Check-A/B/D/D1/D2/E/F/H/J non-waivable) — exactly as claimed.

Re-ran the developer's own regression harness live instead of trusting "GREEN 10/10" prose: `scripts/test-fb-gate-checkd2-nonwaivable.sh` → 8/10 pass, 2 FAIL, both in section (3) doc-prose assertions (checks against `docs/agents/fb-market-poster/flow/main.md` for `NON-WAIVABLE`/`Check-D2 fix protocol`/Check-C waive sentence). Root-caused: a LATER unrelated commit `8d165e8d6` (chore(agent-father/TE-T26): split main.md at MODE ROUTER, 2026-08-06, same day) shrank main.md 994L→88L, relocating this task's entire STEP 4b block into new `docs/agents/fb-market-poster/flow/daily.md:640-641,677-678` — grep-confirmed content present there verbatim, and weekly-recap.md/weekly-prediction.md cross-refs were already correctly repointed to `daily.md` by that split. NOT a substance regression of this fix — a test-script path drift (harness hardcodes the pre-split `main.md` location, scripts/test-fb-gate-checkd2-nonwaivable.sh:57/168/174). Assertions (1)/(2) (live-fixture Check-D2 BLOCK proof + no-regression PASS contrast) and (3a)/(3d) still hold; sibling `test-fb-gate-checkc-negation.sh` unaffected (6/6). `docs/social/fb-post-2026-07-25.md` confirmed untouched (0 diff), as claimed.

VERDICT: CHANGES_REQUESTED. Per verify-committed's own mechanical rule (any failing check → vc-changes), a currently-RED task-owned regression proof blocks done_verified even though root cause is a foreign later commit. Moved `task_board.qa[]`→`task_board.review[]` (status QA→REVIEW), `owner`/`next_agent`=developer (row had no `.owner`; used `updated_by` fallback), `redispatch_count` 0→1, `status_note` carries the exact 2-line fix needed (repoint MAIN_FLOW/grep targets from main.md to daily.md) via `jq`+`scripts/orch-apply.sh`. `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S43.
