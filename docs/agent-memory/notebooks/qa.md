# QA — Notebook

## cycle-621 · 2026-08-11 · FIX-ORPHAN-FR7-VERIFY-TOOL-REGISTRY — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `618654d96`+`557653c59`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, row carried no `.commit`/`.files[]` fields — took commits `618654d960` (handoff+WORK.md) and `557653c59f` (notebook+journal) from the row's own `dev_note` prose). Both confirmed `main` ancestors via `git merge-base --is-ancestor`. Claim is "no-op, verification-only" for FR-7: `docs/data/tool-registry.json`'s `system` group `task_heartbeat`/`task_release` entries are bare name strings inside `tools[]`, no per-tool object anywhere in the file — independently confirmed via `jq`, not trusted from prose. Read `scripts/gen-tool-registry.ts` myself: its regex captures only the string-literal tool name, never the trailing Zod schema object — structurally incapable of drifting on a param-schema change.

Re-ran everything myself, not trusted from prose: `bun scripts/gen-tool-registry.ts --dry-run` → `totalCount=183`, `system: 41 tools`, byte-identical to the committed file. `bun test src/__tests__/tool-registry-parity.test.ts` (apps/mcp-server) 17/17 pass, 39 expect() — matches claim exactly. `bun tsc --noEmit` (apps/mcp-server) 0 errors. `git show --stat` on both commits confirms no production source touched (handoff.md + WORK.md + notebook + journal only) — mock-guard N/A, consistent with the row's own no-edit claim. Confirmed the cited `taskHeartbeatTool.ts`/`taskReleaseTool.ts` split (cited as a freshness cross-check) actually exists on disk.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 13→13). `[QA] Review Record (direct-commit verify)` appended to the row as new `status_note` field (no prior status_note/review_note, no handoff-file requirement for this drain class — dev_note already carried the finding). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S37. `task_release` for outer `task:FIX-ORPHAN-FR7-VERIFY-TOOL-REGISTRY` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (same gateway-blind pattern as cycle-617/619/620); dispatcher (dev-team, session 165f4245) is the authoritative release path per its own finally-block/TTL, matching INV-GATEWAY-1. Pruned cycle-618 (`FIX-PREDICTION-SIGNALS-EMPTY`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 619/620/621.

## cycle-622 · 2026-08-11 · FACTORY-INTERFACE-vps-auth-guard-dedup — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `cf28a32e8`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, `review_note` cited commit `cf28a32e8` directly). Confirmed `main` ancestor via `git merge-base --is-ancestor`; `git show --stat` matches all 5 `detail_ref.files[]` exactly (`server.ts`, `pushPricesHandler.ts`, `pushSbvRatesHandler.ts`, `pushForeignFlowHandler.ts`, `pushNewsHandler.ts`). Read every hunk line-by-line, not trusted from prose: each site swaps its inline 401 block for `if (!requireVpsApiKey(req,res)) return;`; the shared guard body (`routes/_shared/requireVpsApiKey.ts`) is byte-identical to the removed inline blocks — same x-api-key-then-Bearer precedence, same `!==` compare, same 401 `{error:"Unauthorized"}` body, zero behavior change. `foreignFlowStatusHandler.ts`'s `buildForeignFlowStatusResponse` (deliberately left unmigrated per claim) confirmed genuinely different contract — returns `{status,body}`, takes caller-supplied `requestApiKey` — legitimate exception, not scope-dodge.

Repo-wide grep for the old inline pattern (`authHeader...replace("Bearer`) returns zero hits outside the guard's own definition — dedup exhaustive, no orphaned copies remain. Client-side `VPS_PUSH_API_KEY` usages in fetchers/scheduler (agmPlanFetcher, bctcHttpFetcher, boardDetailsFetcher, sscInsider, bctcPdfPullJob, newsHeadlinesRefreshJob) are outbound header-senders — a different pattern, correctly out of scope. Re-ran fresh, not trusted from prose: new `FACTORY-INTERFACE-vps-auth-guard-dedup.test.ts` 11/11 pass; broader targeted batch (1406a/1406b/1892a/FIX-SBV-PUSH-TYPE-COERCE/debug-trigger-smoke+new file) 56/56 pass, 0 fail. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files <6 touched>` PASS. DDD grep: interface→infrastructure/application imports on touched files pre-existing (interface layer, architecturally permitted); only new import is same-layer `_shared/requireVpsApiKey.js`. `process.env` hit at server.ts:400 confirmed untouched by this diff's hunks. No secret/password/token literals introduced. Full-suite 42-fail baseline claim cross-corroborated against 3 sibling journals dated 2026-07-23 independently recording the same count same day, pre-dating this commit.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 13→13). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S39. `task_release` for outer `task:FACTORY-INTERFACE-vps-auth-guard-dedup` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (same gateway-blind pattern as cycle-617/619/620/621); dispatcher (dev-team, session 165f4245) is the authoritative release path per its own finally-block/TTL, matching INV-GATEWAY-1. Pruned cycle-619 (`FACTORY-STOCK-resolve-dead-writeback`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 620/621/622.

## cycle-623 · 2026-08-11 · FACTORY-INTERFACE-move-kinhdich-ta-scoring-down — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `b6b659ea0`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, `review_note` cited commit `b6b659ea0` directly). Confirmed `main` ancestor via `git merge-base --is-ancestor`; `git show --stat` matches: new 415L `application/services/kinhDich/kinhDichScoring.ts` (9 functions), `interface/mcp/tools/kinhdich/kinhDichTools.ts` (-414L, now imports+calls), `scheduler/.../defaultComputeHexagrams.ts` dynamic-import repointed, `docs/microservice/mcp-server/kinhdich.md` updated, 7 test files' import paths (review_note said "6 test files" — actual diff shows 7, harmless prose undercount, non-blocking, no functional gap). Independently byte-diffed the full 9-function block (computeSentimentScore/computeFundamentalsScore/computePriceScore/computeForeignFlowScore/computeSectorScore/computeMacroScore/tickerJitter/computeHaoScores/computeMacroIndicatorScore) old-file-parent vs new-file: identical except one comment-line edit (no logic/formula change) — confirms the byte-diff claim myself, not trusted from prose.

Layer-placement decision (application, not domain, per dev's own DJ S1): cross-checked against this codebase's existing convention `application/usecases/getForeignRoom.ts` (own header text: "orchestrates domain + infrastructure") — matches, not a DDD layering violation; `domain/` files untouched by this commit. TA-scoring-math non-move (DJ S2, cites G5/P2-B2 architecture — TA domain service quarantined to `_deprecated/`, permanent home Go microservice port 5003) cross-checked against `docs/architecture-briefs/2026-07-24-factory-guard-ci-dead-code-gate.md`'s independent G5/`_deprecated/` corroboration — legitimate documented exception, not scope-cut; commit diff confirms zero touch to `technicalIndicatorTools.ts`. Re-ran fresh, not trusted from prose: `bun tsc --noEmit` (mcp-server) 0 errors; targeted kinhdich/hexagram/hao/macro-score/sector-score/foreign-flow/diacritics regression (21 files, superset of the claimed 19) 480 pass/4 skip/0 fail across 484 tests. `mock-guard.sh --files (kinhDichScoring.ts, kinhDichTools.ts, defaultComputeHexagrams.ts)` PASS. process.env/secret greps clean on touched files. DJ-GATE-1: `sprint-FACTORY-INTERFACE-move-kinhdich-ta-scoring-down-dev-mcp-server.md` carries `task-id:` trailer ×2 (S1 + S2) — gate satisfied.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 13→13). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S40. `task_release` for outer `task:FACTORY-INTERFACE-move-kinhdich-ta-scoring-down` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (same gateway-blind pattern as cycle-617/619/620/621/622); dispatcher (dev-team, session 165f4245) is the authoritative release path per its own finally-block/TTL, matching INV-GATEWAY-1. Pruned cycle-620 (`FACTORY-ALERT-delete-deprecated-domain`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 621/622/623.
