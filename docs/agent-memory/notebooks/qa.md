# QA — Notebook

## cycle-598 · 2026-08-08 · FIX-COLDEVICT-TERMINAL-SIGNAL-STATUSES-OMITS-TRIAGED-RETRACTED — APPROVED, DONE_VERIFIED (direct-commit verify, `review[]` row, `branch:null`, commit `42e7c6048`, router-dispatched not dev-team-cron)

Router-dispatched (not dev-team-cron) direct-commit engagement, router held the outer sprint-task lock for the whole developer→QA chain (INV-GATEWAY-1) — no `task_claim`/`task_heartbeat`/`task_release` called on my side. Row lived in `review[]` (status REVIEW), not `qa[]` — `pipeline`'s `git checkout task/NNN` precondition inapplicable (no branch, straight to `main`), so used `verify-committed` regardless of the doc's literal `qa[]` wording since the underlying shape (self-contained row, `branch:null`, `commit_sha` present) matches exactly. `42e7c6048` confirmed real `main` ancestor via `merge-base --is-ancestor`; `git show --stat` matches all 4 claimed files (skill doc, WORK.md, script, test file) exactly.

Independently re-verified all 3 things the dispatcher explicitly flagged, not trusted from prose: (1) AC-1 age gate — read `compute_id_maps`'s actual predicate, `SIGNAL_MAX_AGE_HOURS`/`$sig_cutoff` still ANDed into the signal-row selector, byte-identical logic to pre-fix; live-confirmed all 10 remaining `triaged` rows <24h old (oldest ts 2026-08-07T22:34:08Z vs now 18:29Z), correctly held back — age gate genuinely untouched. (2) exact-string match — grepped for `ascii_downcase`/`ascii_upcase`/case-fold near the predicate, none found; `.status | IN($tsig_arr[])` is literal jq equality, both `triaged` and `TRIAGED` needed their own entry exactly as claimed. (3) skill-doc sync — `.claude/skills/signal-dashboard/SKILL.md` § PRUNE criteria line now byte-identical to the script's real `TERMINAL_SIGNAL_STATUSES` default (7 entries incl. the pre-existing `ACUTE-RESOLVED-ROOT-TRACKED` the dev's own note said was already missing before this fix) — no new drift introduced.

Tests: `orch-cold-evict-tests.sh` own re-run 59/59 (incl. new T11), matches claim exactly. `orch-apply-wrapper-tests.sh` own re-run: 73/76 (3 fail), NOT the claimed 75/75 — investigated rather than accepted or silently flagged: traced to an UNCOMMITTED, in-flight peer WIP edit sitting dirty in the shared working tree on `apps/mcp-server/src/infrastructure/orchStateSchema.ts` (a new RC-VERIF `raw_probe` completion-gate, not yet on `main`). Proved unrelated with `git show HEAD:orchStateSchema.ts | grep -c raw_probe` = 0 (committed schema has zero references; this task's own 3 commits never touch that file) vs 10 in the dirty working copy — confirms the failure is a transient cross-session artifact of the shared working directory, not a regression from this diff. Accidentally `git stash -u`'d mid-investigation while checking parent-commit state — caught immediately, `git stash pop`'d back before any other command ran, working tree confirmed restored to its pre-stash dirty state (same file list). `shellcheck -x` clean on both touched scripts. Live evidence: independently confirmed the peer/scheduled cold-evict commit `d1ba52d18` (no `Task:` trailer — genuinely separate from this task's own chain) landed sequenced right after `42e7c6048`, dropped `signal_queue.rows` 248→31 (21 READ + 10 triaged) — jq-verified exact match to the dispatcher's claimed count.

VERDICT: APPROVED, DONE_VERIFIED. Board write hit the SAME uncommitted peer `raw_probe` gate (rejected the new `DONE_VERIFIED` row without one) — rather than downgrade to plain `DONE` (schema-safe but understates the actual rigor performed) or block on a peer's unrelated local WIP timeline, attached a genuinely-performed `verification.raw_probe` (tool=jq, live `signal_queue` count/histogram + age-gate spot-check, `observed_at` stamped) — a real independent live re-probe I actually ran, not fabricated. Re-validated clean, applied via `orch-apply.sh` (conservation OK: task_total 754→754, signal_total 31→31, signal_row_identity=clean), moved `task_board.review[]`→`task_board.done_verified[]`, committed (`424880ccd`) + pushed to `origin/main`. Verification text appended to row's own new `status_note` field (no handoff file — direct-commit verify path; `review_note` from dev left intact). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S14. No CronCreate/CronDelete/CronList or `/cron-*` skill invoked this cycle (standing constraint honored). Pruned cycle-595 (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 596/597/598.

## cycle-599 · 2026-08-08 · FIX-COWORK-SPAWN-IDENTITY-PREAMBLE-OFFFLOW — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, no commit/files[] field at all, derived commit `add3f13a1`)

Row carried neither `commit` nor `files[]` (detail_ref checked too, also absent) — used the flow's own review_note-prose fallback: `git log --all` against the files named in the row's own `review_note` (spawn-fanout.md, last-fired.md, main.md, cowork-schedule-consistency.test.js) → `add3f13a1`, whose own message carries `Task: FIX-COWORK-SPAWN-IDENTITY-PREAMBLE-OFFFLOW` verbatim. Confirmed `main` ancestor; `git show --stat` matches all 4 claimed docs exactly.

3 later commits (`0d16f28ce` session-id inject, `6452935ab` tombstone, `9c509d295` cron-rearm) also touch the same files — read CURRENT live source, not the diff alone: `spawn-fanout.md` Step 5.2 `IDENTITY_PREAMBLE` and the exogenous Step 5.3 off-flow-router-latch detector remain fully intact, layered cleanly under the later `SESSION_ID_LINE` append (no clobber). `last-fired.md` AC-P1-7-4 and `main.md`'s JUMP-TO row both present, byte-consistent with the claim. Re-ran (not trusted) the sibling regression suite `cowork-schedule-consistency.test.js` live: 9/9 PASS, matches claim exactly.

VERDICT: APPROVED, DONE_VERIFIED. Zero `.ts`/production source touched (5 docs/memory files) — `bun test`/`tsc --noEmit`/`mock-guard.sh` structurally N/A. Row's own `verification_gate` is explicitly BEHAVIORAL (next live off-flow incident, or a synthetic Step 5.3 injection dry-run) — dev's own review_note flags it unconfirmable in-dispatch; APPROVED on the structural/static claims only, consistent with prior docs-only verify-committed precedent (cycle-598). Board write hit RC-VERIF's `verification.raw_probe` gate — attached a real re-probe (tool=`node cowork-schedule-consistency.test.js` + live source grep, `observed_at` stamped), applied via `orch-apply.sh` (conservation OK at apply time: task_total 753→753, signal_total 31→31), moved `task_board.qa[]`→`task_board.done_verified[]`. Write landed on `main` bundled into a peer's concurrent hot-file sweep commit `b55fff468` (shared working tree — confirmed my row's content present on `HEAD`, not lost). Decision journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` hit its byte cap (37691B > 36000B cap at 191L) mid-cycle — appended `CAP-REACHED` marker, rolled to new `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S15. Pruned cycle-596 (`FIX-SWEEPGUARD-ESCALATION-...`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 597/598/599.

## cycle-600 · 2026-08-08 · FIX-MCP-MEMORY-CODE-LEAK — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `609f62800`, derived via `dev_implementation_note` prose fallback — no top-level `commit`/`files[]` field)

7-week-old P1 memory-leak row with a long PO history (sawtooth false-positives, a real OOM-adjacent wedge 07-22, an emergency pre-QA deploy authorization 08-05, a falsified-then-rescoped AC 08-07/08-08). Found `609f62800` via `dev_implementation_note` prose; `merge-base --is-ancestor` confirmed `main` ancestor; `git show --stat` matches all 4 claimed files exactly (schema.ts, 002-db-schema.test.ts, 2 architecture docs). Re-ran REAL, not trusted from prose: `bun test 002-db-schema.test.ts` 26/26 pass; `bun tsc --noEmit` 0 errors; `mock-guard.sh` PASS on schema.ts; `process.env`/secret greps clean.

`po_ac_rescope_20260808T1759Z` formally narrowed this row's AC to the `initDatabase()` bootstrap-sweep signature probe only (the 12h memory-ceiling AC + RestartCount≥2h gate were re-homed to `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER`, structurally unsatisfiable here — a different leak source). Independently re-measured the rescoped AC live, not relayed from PO's own re-verify: `docker logs vn-market-intelligence-mcp-mcp-server-1 | grep -c backfillOCFForWatchlist` = 1 occurrence since boot (container RestartCount=0, StartedAt 16:59:50Z, ~2h uptime) vs this row's own documented pre-fix baseline ~52/10min / 41689 since the 07-31 image — AC PASSES. `WeakSet<Database>` guard confirmed present both in git HEAD (schema.ts:76,177-190) AND via `docker exec grep` inside the actual running container, not just source. toolCount=183 re-confirmed via `/health`. schema.ts now 261L; live `size-lint --check` run confirms zero mention of schema.ts (the size-lint debt this row's own commit caused was separately discharged by `FIX-CI-SIZELINT-SCHEMA-TS-BASELINE-TOLERANCE-377L`, per the row's own `dedup_key_retire_note` — not this row's obligation per rescope).

VERDICT: APPROVED, DONE_VERIFIED. Board write required attaching `verification.raw_probe{tool,args,live_value_observed,observed_at}` (validator-enforced on DONE_VERIFIED) — attached the genuine docker-logs probe just run. Applied via `orch-apply.sh` (conservation OK: task_total 753→753, signal_total 31→31, signal_row_identity=clean), moved `task_board.qa[]`→`task_board.done_verified[]`. Verification text appended to row's own `status_note` field (no handoff file — direct-commit verify path; all prior PO/dev/ops history left intact). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S16. `task_release` not called — no `mcp__gateway__call_tool` binding in this sub-session (INV-GATEWAY-1); dispatcher releases `task:FIX-MCP-MEMORY-CODE-LEAK` on my behalf. Pruned cycle-597 (`FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` Step-5 checkpoint, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 598/599/600.
