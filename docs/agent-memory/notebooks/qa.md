# QA — Notebook

## cycle-509 · 2026-08-06 · CCATO-MCP-T2-CLAIM-MAP-LOADER — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

T2 of 8 (SPRINT-CCATO-TRUTHGATE-MCP-NATIVE). Row had no top-level `commit`/`files[]` — derived both from review_note prose (commits `f79019f9b` impl + `533a745d3` memory), both confirmed real and on main ancestry. `git show --stat` matches every claimed file exactly: new `claimToolMapLoader.ts`(104L)/`claimToolMapValidator.ts`(90L, both <=120L split for size-lint) + new test file + 3 doc files, zero existing production files modified — a pure addition.

Re-ran everything myself rather than trusting the review_note prose (`feedback_router_verify_raw_not_badges`): `bun test CCATO-MCP-T2-CLAIM-MAP-LOADER.test.ts` → 14 pass/0 fail/25 expect(), exact match. `bun tsc --noEmit` (whole `apps/mcp-server`) → 0 errors. DDD grep clean (only a type-only `ClaimToolMap` import from domain — correct direction). Security grep clean (`non_ticker_tokens` field-name substring hits only, no real secrets/`process.env`). `mock-guard.sh --files` PASS. `size-lint-justification.sh --check` PASS 0 offenders (row claimed 2 pre-existing at commit time — since resolved by later unrelated size-lint fixes on main, not a regression). Read both new files' full diff directly: fail-loud `ClaimToolMapLoadError` on all 4 malformed-shape paths (missing/unreadable file, unparsable JSON, non-object body, missing/mistyped fields), additive top-level SSOT fields tolerated not rejected, zero coercion. `getProjectRoot()` used for the new path constant instead of the brief's literal unsafe `__dirname` hop-count pattern (flagged deviation) — independently verified correct: that fixed hop-count resolves above the real root both in local dev and the deployed container.

Grep-confirmed zero production consumers of the 2 new modules repo-wide (only the new test file imports them; one unrelated comment-only string match) — full-suite regression structurally impossible for this pure-addition commit, same class as sibling `CCATO-MCP-T1-DOMAIN-ENGINE` verify; skipped the full 15085-test run, targeted file + whole-project tsc sufficient. Doc updates (`domain-model.md`/`infrastructure.md`/`testing.md`) read directly — accurate, describe the landed loader/validator faithfully.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation check task_total 814→814, pure lane-move). `.head` was already idle/`next_agent:pm` (unrelated peer state) — left untouched. DJ: `sprint-SPRINT-CCATO-TRUTHGATE-MCP-NATIVE-qa.md` §qa-S2.

## cycle-510 · 2026-08-06 · FIX-DEVTEAM-IDLE-CHAIN-DANGLING-DEPS-STRAND-5-P0-ROWS — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain, `branch:null`)

Row's provenance splits across 2 commits (not the 1 `commit_sha` field alone): AC-1 (clear stale `.depends` on 3 rows, `orch-state.json`) landed earlier via `5cba4c9fa`; AC-3 (durable write-time guard) via the row's own `111e1a688`. Both confirmed on main ancestry. `git show --stat 111e1a688` matches: new `orchStateSchema.ts` §13 `checkDependsDivergence()` (hard-fail) + §14 `checkMissingDependencyReport()` (non-fatal), `orch-validate.mjs` Stage 1f/1g wiring, `dev-standards.md`/`WORK.md` doc updates, new test file. `scripts/lib/devteam-eligibility.jq` deliberately untouched (AC-4 forbids altering the union/`deps_satisfied` semantics) — live-grepped line 188, confirmed unchanged, matching AC-4 by design not omission.

Re-ran everything myself, not trusted from review_note prose (`feedback_router_verify_raw_not_badges`): new test 14/14 pass, `orchStateSchema.test.ts` 104/104 pass (no regression on 5 pre-existing stages), `scripts/test-orch-validate-ac.mjs` 29/29 pass — exact match to claims. `bun tsc --noEmit` clean, `mock-guard.sh` PASS, `size-lint-justification.sh --check` PASS, DDD/secrets greps clean. Ran live `node scripts/orch-validate.mjs` on the current board myself: exit 0, Stage 1f = 0 divergence issues (no regression 5 days post-land), Stage 1g reports 10 rows now (was 8 at land time — expected drift from new backlog rows since, mechanism intact, still non-fatal).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 4→2, done_verified 11→13, conservation check task_total 814→814, pure lane-move). `.head` untouched (deprecated/re-collapsed key, canonical `.head` unaffected). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S22.

## cycle-511 · 2026-08-06 · FIX-CI-SIZELINT-BCTC-1345B-PARSE-VALIDATOR-PAIR — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commit `b56dc6cc2` confirmed on main ancestry, `git show --stat` matches both row `files[]` entries plus 2 new domain modules it extracted (`confidenceFinancialReasonBuilder.ts` 79L, `financialFiguresRules.ts` 300L carrying its own size-justification header, not a baseline bump). Re-measured line counts myself rather than trusting the status_note prose (`feedback_router_verify_raw_not_badges`): `parseBctcReport.ts`=916L (≤944L upper), `financialFiguresValidator.ts`=162L (≤337L upper) — exact match. `size-lint-justification.sh --check` PASS, 0 offenders (1364 files scanned). `docs/data/size-lint-baseline.json` confirmed untouched both by `git status` and `git diff 7ac55adc8..b56dc6cc2` on that path — no silent baseline bump, matching the row's own explicit prohibition.

Re-ran everything myself: `bun tsc --noEmit` clean. DDD grep: the 3 domain files carry zero infrastructure imports; `parseBctcReport.ts`'s infra imports are application-layer (`src/application/usecases/`) and permitted under the golden rule (`domain/` has ZERO imports from `infrastructure/` — application importing infra is standard orchestration, not a violation). Secrets grep clean. `mock-guard.sh --files` PASS. Grepped all 21 test files referencing the changed/extracted symbols and ran them: 160 pass/0 fail/399 expect() — exact match to the status_note's own claimed count. Row's `verification_gate: ci_green_on_subsequent_push` independently re-verified RAW rather than trusted from prose: confirmed CI run headSha `1ff241d2e` (2026-08-06T13:31Z, freshest main run at verify time) is a genuine descendant of `b56dc6cc2` via `git merge-base --is-ancestor`, `size-lint` job conclusion=`success`.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 9→8, done_verified 9→10, conservation check task_total 813→813, pure lane-move). `next_agent` flipped `null`→`"pm"` (validator rejects null there; matches the 2 other `pm`-routed `done_verified` rows' convention). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S26.

## cycle-511 · 2026-08-06 · FIX-OPS-DEPLOY-SELFREPORT-FABRICATED-FUTURE-TIMESTAMP-NONEXISTENT-IMAGE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row's `commit_sha` `1f15f47b2` confirmed on main ancestry. No `files[]` field — derived the touched file from the row's own `note` prose ("fixed docs/agents/ops/flow/docker.md"); `git show --stat` confirms exactly one file touched (+13/-4), matching. Read the full diff myself: new "Deploy-Evidence Capture" block requires literal `date -u` + `docker inspect StartedAt/RestartCount` + `docker image inspect` output pasted before any Pass claim, tightens Pass/Fail to require StartedAt-postdates-dispatch + confirmed-existing image hash, and forbids claiming an un-executed task_board write on Fail. Cross-checked the note's 2 factual claims live rather than trusting prose (`feedback_router_verify_raw_not_badges`): `scripts/verify-deploy-sha.sh` genuinely exists and `docker-deployment-runbook.md:123` already documents it as the SHA gate — pointer accurate, not fabricated; `docs/agents/ops/flow/main.md:74` genuinely delegates "any restart/rebuild, even single-service" to `docker.md § Post-Rebuild Health Verification" — confirms the fix is general (fires on every future trigger via the one shared delegation point) as AC-4's trailer claims, not scoped-only.

No production/test code in the diff (pure flow-doc fix) — `bun tsc --noEmit` (mcp-server, whole project) 0 errors as baseline sanity; `mock-guard.sh --files` → "No production source files to scan. PASS"; `size-lint-justification.sh --check` PASS 0 offenders. Full `bun test` not applicable — zero `.ts`/test files touched. AC-3 (actually complete the rag-service redeploy) confirmed correctly deferred per the note to the separate `UNBLOCK-DEPLOY-RAG-SERVICE`/`FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` rows, not silently dropped.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 9→8, done_verified 9→10, conservation check task_total 813→813, pure lane-move). `.head` untouched (unrelated active_task_id). Pre-existing Stage 1g report (9 rows, dangling `.depends`) unrelated to this task, not caused by this write. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S27.

## cycle-511 · 2026-08-06 · FIX-CI-PARITY-CLAUDEMD-CRON-LITERAL-EXEMPTION-SHAPE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Verification-only close row (fix already shipped by a prior developer cycle, board flip never followed). Commit `c3c5d63d2` confirmed on main ancestry, `git show --stat` matches both `files[]` entries exactly (CLAUDE.md + tool-registry-parity.test.ts, 8 ins/3 del). Read the full diff myself rather than trusting status_note prose (`feedback_router_verify_raw_not_badges`).

AC4 (punctuation-only, zero semantic change): confirmed — CLAUDE.md:48 `"the 4 standalone crons (db-...)"` → `"the (4 standalone crons) (db-...)"`, only 2 parens added, zero word add/remove/reorder; current on-disk content matches the commit exactly. AC3 (no exemption-predicate widening): confirmed — `findCronCountLiterals`'s regex/wrap-check body is byte-unchanged in the diff; only the T-U3-7 assertion changed from `hits.every(h=>h.exempted)` to `exemptedHits.length>0`, matching the PO ruling's exact instruction (AC2).

Re-ran independently: `bun test tool-registry-parity.test.ts` → 17/17 pass (exact match to commit msg's own claim). `bun tsc --noEmit` (mcp-server) 0 errors. `mock-guard.sh --files` → PASS/N-A (CLAUDE.md + test file, no production source in scope). CI-plane RAW-verified beyond the row's self-report: `gh run view 31098224312` (headSha `4408c9283`, the run immediately after the fix landed) — job "bun test" conclusion=success. Went further and pulled the freshest main CI run (`31106283894`, headSha `1ff241d2e`, ~6h later) — "bun test" job still success, gate durable, no regression since.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 10→9, done_verified 8→9, conservation check task_total 813→813, pure lane-move). `.head` untouched (unrelated active_task_id `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S25.
