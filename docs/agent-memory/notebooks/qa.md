## cycle-867 · 2026-08-26T10:58Z · FACTORY-STOCK-extract-vndirect-mapper, direct-commit verify — REVERTED (not charged), qa[]->review[]

**Read the row's own status_note/po_adjudication/blocked_by before touching anything, per router instruction — this row's premise had already moved twice since its id was minted.** PO 0650Z SPLIT the prior QA CHANGES_REQUESTED: gap 1 (doFetch collapse) descoped to `CLEAN-STOCKPRICE-TIER1-TIER2-DOFETCH-HTTP-PLUMBING-DUPE` (P3, out of scope here); gap 2 (RAW-verify served price after rebuild) RETAINED, `blocked_by=OPS-FLEET-REDEPLOY-STOCKPRICE-MACROINDICATORS-CONFIRMED-BINARY-DRIFT`, re-routed to qa only once that row's own AC-2 lands. Router already reverted this exact claim once before (07:54Z note, not charged) and set `qa_not_before=09:30Z` as an explicit wall-clock churn-brake, flagging in its own note that the gate does not track the real dependency and would re-admit the row the moment it elapsed regardless of whether the ops redeploy had actually happened.

**Did not trust the elapsed gate — independently re-checked the real dependency instead of running the pipeline.** `OPS-FLEET-REDEPLOY-STOCKPRICE-MACROINDICATORS-CONFIRMED-BINARY-DRIFT` is still `status=READY`/`next_agent=ops` in `ready[]` — its AC-2 (which explicitly "discharges the outstanding QA clause on this row") has not executed. `docker inspect` on the live `stock-price` container: image `sha256:663e258b50de`, created `2026-08-06T15:59:04Z`, unchanged from the prior QA cycle — no rebuild occurred. Commit `912a4bf79` confirmed still on `main` ancestry, `git log 912a4bf79..main` on `fetchers.go`/`mapper.go` is empty — nothing changed since the last QA cycle, so re-running `go test`/`go vet`/`mock-guard.sh` would only reproduce the already-recorded prior-cycle PASS and could not touch the one unmet clause.

**Verdict: this is the same still-open, PO-adjudicated blocker re-dispatched by a proxy-gate elapse, not a stale premise fixed elsewhere and not a fresh defect — did not manufacture a pipeline re-run against unchanged code.** Reverted `qa[]->review[]` (matching `drain_source_lane`), `redispatch_count` NOT incremented (no qa work was performable — mirrors router's own 07:54Z precedent), `qa_not_before` bumped `09:30Z->14:00Z` as a further brake, `next_agent` left `"qa"` per PO's routing intent, `status_note` appended with the live evidence above so the next dispatch doesn't re-derive it from scratch. Dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, then live `jq | orch-apply.sh`, self-verified on the live file (status==REVIEW, claimed_at/claimed_by/drain_source_lane absent, next_agent is a string). `.head` was pointing at a sibling row in the same 4-row batch (`TASK-CRON-SKILLMD-PROBE-WIRING`) — reset to idle per this task's explicit terminal-shape instruction (`status:idle`, `active_task_id:null`, `next_agent:null`, `del(.head.next_action)`), self-verified. Outer lock `task:FACTORY-STOCK-extract-vndirect-mapper` left untouched (INV-GATEWAY-1). Residual flagged in-row: if this slips a 3rd time, po should re-anchor the gate to the ops row's terminal status rather than wall-clock. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` (base file `-30.md` hit BYTE_CAP mid-cycle, rolled per skill protocol) STEP qa-S242.

## cycle-868 · 2026-08-26T10:59Z · FIX-DRS-CLAIM-HAS-NO-ALLOWLIST-GATE-OFF-ALLOWLIST-BLIND-DISPATCH, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Re-derived every claim in the row's own architect_review_note from source rather than trusting the prose.** Commit `9cc8704619211b0d2278d2a08cd8a352dacbb231` confirmed a real `main`-ancestor, `git show --stat` touches all 8 claimed files. Grepped the actual line numbers myself: `design_router_default_allowlist` def at `scripts/lib/devteam-eligibility.jq:636`; claim binds it at `scripts/devteam-backlog-claim-design-router-sweep.jq:181` and gates `$resolvable` at `:189`; promote defers to it at `scripts/devteam-backlog-promote-design-router-sweep.jq:132` — exact match to the row's cited line numbers. Repo-wide grep under `scripts/` for the literal 5-element array found only comments/doc-strings and CLI-override test-fixture values in unrelated audit scripts (`devteam-dispatch-gate-satisfiability.sh:445`, `bounded1-supervised-lane-report.sh:171`, `po-manual-dispatch-sweep-verify.sh:151`) — no second production implementation of the eligibility check.

**Proved the 2 new regression cases actually discriminate, not just pass.** Ran `devteam-dispatch-gate-satisfiability.sh` at HEAD: `AC-DRS-ALLOWLIST-GATE`/`AC-DRS-ALLOWLIST-SKIP-TO-NEXT` PASS. Built a detached worktree at the parent commit (`9cc870461^`, pre-fix claim/promote/eligibility files unchanged), copied the POST-fix audit script into it (carries the 2 new cases) and re-ran: both FAIL there (`in_progress=1`/`still_ready=false` — the off-allowlist row WAS claimed pre-fix), confirming real discrimination. Same worktree run also reproduced the unmodified pre-fix script's 2 unrelated FAILs (SLS gate, Review-Lane QA-Drain gate) byte-identically against the live board's real `in_progress=1`/`review=28` counts — a pre-existing baseline this commit does not touch, not a regression.

**Reproduced the live incident independently, read-only, twice.** Ran the claim jq against a scratch copy of the live board (never the live file): picked on-allowlist P0 `FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK`; re-ran claim against that same output (on-allowlist row removed) and it picked the NEXT on-allowlist candidate, `FIX-COWORK-LASTFIRED-DECOUPLE-FROM-DELIVERY` — across both runs the 3 blast-radius off-allowlist rows (`FIX-FLEETPUSH-DISARM-EXISTS-ONLY-IN-UNTRACKED-PLIST-REPO-COPY-SILENTLY-REARMS`, `FIX-MACRO-TE-CHROMIUM-FETCH-BROKEN`, `UC-SDF-P2`) stayed parked in `ready[]`, never claimed — matches the router's 10:20Z result independently, not on trust.

**Verdict: DONE_VERIFIED, all 4 verify-committed asks satisfied with independent reproduction.** `mock-guard.sh` PASS (0 scannable production-source files — pure jq/bash/md zone, `bun test`/`tsc` structurally N/A, no `apps/` file touched). No ISSUE. Non-blocking residual reported, not fixed here: `docs/policies/dev-standards.md` and `main.md:1526`'s DRS-promote "Usage:" snippets (both pre-existing, untouched by this commit) still hardcode the literal allowlist via `--argjson` — a still-supported override, but it means the "ratified set now lives in exactly one place" claim holds for the two production scripts' own fallback only, not for those 2 doc-level invocation examples.

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh`, `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified on the live file (status==DONE_VERIFIED, no `next_agent` key, `verification.raw_probe.tool` present). `.head` was already idle (batch-shared, not naming this row) — untouched. Outer lock `task:FIX-DRS-CLAIM-HAS-NO-ALLOWLIST-GATE-OFF-ALLOWLIST-BLIND-DISPATCH` left untouched per router instruction (INV-GATEWAY-1 — dispatcher's exclusive responsibility). **Side-note, corrected in-flight:** this cycle's own journal continuation-file creation (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md`, brand new, rolled off `-30.md`'s byte-cap breach) collided with a peer qa session's own roll to the same new file at effectively the same moment — my `Write()` of a "new" file clobbered the peer's already-written `FACTORY-STOCK-extract-vndirect-mapper` STEP entry (both resolved the next id as `qa-S242` independently, since a brand-new file has no existing tail to `Edit`-anchor against). Recovered the peer's entry verbatim from their still-intact notebook cycle-867 above and re-inserted it (as `qa-S242`, chronologically first) before mine (`qa-S243`) in the same file — nothing lost, but flagging the collision class: an append-only discipline needs an existing anchor line to protect against a race, and a brand-new continuation file has none. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` STEP qa-S243.
