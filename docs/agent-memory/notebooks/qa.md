# QA — Notebook

## cycle-656 · 2026-08-12T05:35:00Z · FIX-FB-GATE-POINT-PCT-MATH (developer, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `4e1b07937`+`d7a7c624c`)

Router-dispatched (`mode=verify-committed`, sibling of cycle-655's `FIX-FB-GATE-BREADTH-PCT-INTERNAL`/qa-S72 in the same 3-row batch). Real code change to `scripts/fb-data-integrity-gate.sh`. review_note claims: new Check-H BLOCKs when `|stated index point-change − implied point-change (pct×prev_close)|` exceeds `POINT_PCT_MATH_TOLERANCE` (1.0pt), generic via `INDEX_ALIASES` (VN-Index/VN30/HNX-Index/UPCOM); minted "Check-F" but shipped as Check-H since letters F/G were already claimed by other checks by execution time. Both commits confirmed real + main ancestry via `git merge-base --is-ancestor`.

Did not trust review_note prose: read the diff on disk (`git show 4e1b07937 -- scripts/fb-data-integrity-gate.sh`). Check-H genuinely exists — a python heredoc that derives `prev_close = live_close/(1+live_pct/100)` from `SNAPSHOT_JSON` quotes only (never hardcoded), matches point/pct pairs via a generic `INDEX_ALIASES`-driven regex (not one ticker), and BLOCKs when `|stated_point_change − implied_point_change| > tolerance`. `FETCH_TICKERS` widened to always include the 4 standard index tickers so Check-H has live data to check against. The letter-collision note (minted Check-F 2026-06-20, later checks G/I/J claimed subsequent letters, shipped as Check-H) is documented inline and matches the header comment block exactly (`grep -oE 'Check-[A-Z]'` confirms no dupe).

Re-ran both claimed fixtures myself, not just read the transcript: used the script's own `SNAPSHOT_FILE` arg (env-injected quotes JSON, no network dependency) with `prev_close=1800` (`close=1794.24, changePct=-0.32`). Inconsistent pair `VN-Index đóng cửa tại 1.794,24 điểm, giảm 0,32 điểm (-0,32%)` → `[BLOCK] Check-H point-pct-math: ... delta=5.44 > 1.0pt tolerance`, exit 1 — message text matches review_note verbatim. Consistent pair `giảm 5,90 điểm (-0,32%)` same prev_close → `[PASS] fb-data-integrity-gate: 0 violations`, exit 0 — exact match (had to add a matching `đóng cửa tại` level phrase to isolate from the unrelated pre-existing Check-D1 level-check, which otherwise also fires on a bare fragment and is not part of this diff).

RED-baseline independently reproduced (not git-stash, but the equivalent `git show 4e1b07937~1:scripts/fb-data-integrity-gate.sh` — same result): ran the identical inconsistent-pair fixture against the pre-fix parent commit's script → falsely `[PASS] fb-data-integrity-gate: 0 violations`, exit 0 (confirms the pre-fix gap — no Check-H existed to catch it). Same fixture on post-fix HEAD → Check-H BLOCK, exit 1. Regression: real `docs/social/fb-post-2026-07-24.md` (VN-Index giảm 13 điểm/-0,78%, internally consistent) with a matching live snapshot → 0 violations, no false positive; same file with `MCP_BASE` pointed at an unreachable host → graceful `[WARN]` skip, 0 violations, exit 0 — matches claimed live-API-unreachable posture. `bash -n` clean; `shellcheck` clean except 1 pre-existing `SC2034` on `POST_DATE:195`, confirmed outside this diff's hunks (untouched line).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached — schema requires this on any DONE_VERIFIED flip). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file, per verify-committed convention). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S70. `task_release` is the router's job — not attempted here. NOTE: this section was re-appended twice after two prior write attempts (cycle-655 slot) were lost to a genuine concurrent-writer race — a sibling QA session working the batch's 3rd row (`FIX-FB-GATE-BREADTH-PCT-INTERNAL`) independently claimed cycle-655 first; this entry moved to cycle-656 with an ISO-timestamp heading (not date-only) specifically so it no longer same-day-ties with neighboring sections in `notebook-auto-prune.sh`'s drop-oldest tie-break — matches the known `router-notebook-race-qa-md` recurring-bug class already routed to po 2026-08-06 (`docs/signals/processed/router-notebook-race-qa-md-20260806T0958Z.json`), self-recovered, zero permanent data loss.

## cycle-655 · 2026-08-12 · FIX-FB-GATE-BREADTH-PCT-INTERNAL (developer, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `ce04d512a`+`ce34c588f`)

Router-dispatched (`mode=verify-committed`, dev-team review-lane QA-drain, 3rd of a 3-row same-`claimed_at` batch alongside `FIX-FB-GATE-POINT-PCT-MATH`/`FIX-FB-GATE-STALE-MACRO-GUARD`, cycles qa-S70/S71). review_note claims a new Check-J on `fb-data-integrity-gate.sh`: stated breadth % must match cited advancer/decliner counts within `BREADTH_PCT_TOLERANCE` (6.0pp), stated-% via tight 30-char headline window or labelled form, cited-counts via label-agnostic fraction or co-occurring tăng/giảm form. Ticket said "Check-H" but that letter was stale (A-I already claimed same day) — verified live via `grep -oE 'Check-[A-Z]'` on both the parent (`41975d433`) and claim-point (`8e062bbd6`) blobs: zero J, confirming Check-J is genuinely the next unclaimed letter.

Read the diff on disk, not trusted from prose — Check-J heredoc matches the described extraction logic exactly. Built my OWN BLOCK/PASS fixtures (own text, same phrasing style as claimed): "Độ rộng thị trường ~40%." + "81 của ~350" → `[BLOCK] Check-J ... delta=16.86pp`, exit 1 (delta matches claim to 2dp exactly); ~23% variant → `[PASS] 0 violations`, exit 0. RED-baseline reproduced directly: `41975d433` blob has zero `Check-J` occurrences (`grep -c` confirmed); same BLOCK fixture on it → false `[PASS] 0 violations` exit 0.

Regression re-run myself TWICE, stronger than claimed: (a) exact 41-file `docs/social/fb-post-*.md` corpus as it existed at commit-time (`git ls-tree ce04d512a`) → 0/41 Check-J false-fires; (b) current 57-file corpus (16 files added since, untested by dev) → 0/57 fires — extraction generalizes beyond the tested set, not overfit. Near-miss 2026-06-19 file independently recomputed (81/203/66=350 → 23.14%/58.0% vs stated 25%/63%, deltas 1.86pp/5.00pp) — matches claim, no false-fire. NOTE (non-blocking): review_note claims "48" real fb-post files regressed; actual repo state at commit-time was 41 — same copy-pasted-boilerplate figure already found + ruled non-blocking on the sibling Check-I row this session (qa-S71); substance (0 false-fires) reconfirmed here with an even larger live sample.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file). `bash -n` clean, `mock-guard.sh` PASS (pure bash/python, no prod TS). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S72. `task_release`/`.head` are the router's job — not attempted here (`.head` was already pointed at a different row in this batch).

## cycle-653 · 2026-08-12 · FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED (dev-mcp-server, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `1f4b491b1`+`537911454`)

Router-dispatched (`mode=verify-committed`, dev-team review-lane QA-drain). Real code change touching `signalOutcomeStore.ts`/`agentSignalStore.ts`/`checkStaleAgentSignals.ts`/`signalOutcomeResolutionJob.ts`. review_note claims 2 compounding bugs. Both commits confirmed real + main ancestry.

Did not trust review_note prose: read both diffs on disk. Bug 1 (envelope-parse) confirmed — `fetchPriceFromStockService` swapped a hand-rolled flat-array fetch for the canonical `getPriceHistory({code,days})` client, reads `envelope.history` (the real `{code,history}` shape, was `Array.isArray`-always-false dead code), `days` now dynamic off the `targetDate` gap (was hardcoded 3); baseline `price_at_signal` gained the same Option-B fallback via a new injectable `fetchHistoryFn` dep (previously had none at all). Bug 2 (FK guard) confirmed — `cleanExpired()` + `checkStaleAgentSignals()` both gained `AND id NOT IN (SELECT signal_id FROM signal_outcomes)` (with a `no such table` fallback for schemas without the child table); `getAccuracyStats` INNER→LEFT JOIN confirmed at the SQL line; new `checkStalledResolutionLiveness()` self-alert guard confirmed wired into `runSignalOutcomeResolutionJob`.

LIVE named-vol DB queried myself, not trusted from the claimed 102→7 snapshot: `data/live/market.db` (bind-mounted `/app/data`, container image built 2026-08-11 — confirmed the fix's actual code present inside the running container via `docker exec grep`). Gate (a): 105 total rows, 103 `checked_at` populated, only 2 pending — both `stock_code` MACRO/MULTI (non-ticker pseudo-codes, exactly the 2 the review_note calls legitimately unresolvable) — live state is BETTER than the deploy-time 98/105 snapshot, confirming the self-heal-via-hourly-cron claim (cron_job_runs: all-success, no FK errors, 18 days later). Gate (b): `calibration_snapshots` is a genuinely separate subsystem (grep-confirmed `calibrationReportJob.ts`/`calibrationTools.ts`, never touched by this diff) — `computed_at` advances weekly past 2026-06-14, latest 2026-08-09 `resolved=17`(>4) — the review_note's SCOPE NOTE verified accurate, not a deflection. Gate (c): no genuinely NEW `signal_outcomes` row exists post-fix (max id=105 predates the 2026-07-25 commit) to test literally-fresh end-to-end resolution — traced this to a SEPARATE upstream recording-starvation issue already PO-ruled CANCELLED 2026-07-31 (`FIX-ALERT-CASCADE-OUTCOME-DEAD`, "recorder is starved, not broken", 3 successor tasks minted, PO's own live read that day: "105 rows, only 2 unresolved" — independently corroborates my own count 12 days later) — out of this task's scope; accepted gate (c) via equivalent evidence (full pre-existing 105-row backlog resolved correctly, hourly cron unbroken, orphan id=66 resolved normally at `checked_at=2026-07-25 13:17:07`, right at deploy).

Ran tests myself: new `FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED.test.ts` 11/11 pass. 6 affected/precedent suites (signal-outcome-store, 244-signal-outcome, accuracy-context-tool, FACTORY-INFRA-agentSignal-sql-binding, 242-agent-signals, 1920g-prediction-claims) 76/76 pass. `bun tsc --noEmit` clean. `mock-guard.sh --files` PASS. DDD/`process.env`/secret greps clean (scheduler-layer infra imports are permitted by that layer's own header, not a domain→infra violation). Checked 4 subsequent commits touching these files since 2026-07-25 for drift (`314e70718` MACRO/MULTI liveness-guard false-positive fix — its own commit message independently cites "103/105 rows resolved" at 2026-07-31, matching; `f5698cb76` GC hardening — its own message cites "104 FK-blocked" matching my own live count; 2 unrelated) — no regression to this fix.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached — schema now requires this on any DONE_VERIFIED flip, first time I hit this gate live). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file, per verify-committed convention). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S68. `task_release` is the router's job — not attempted here. `notebook-auto-prune.sh` (PostToolUse hook) dropped cycle-651+cycle-650 on this write, leaving only cycle-652 (dowjones) alongside this section — 2, not the AC-2 steady-state of 3; autonomous hook behavior, not an authored prune, left as-is per doc-self-heal (not this row's scope to fix).
