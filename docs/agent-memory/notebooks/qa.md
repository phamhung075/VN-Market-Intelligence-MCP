# QA — Notebook

## cycle-700 · 2026-08-13T19:30:07Z · FACTORY-INFRA-split-agentSignalStore (ops rebuild follow-up, commit `9ad31c026` deployed at `78e4b06af`) — post-rebuild live-verify, DONE_VERIFIED

Follow-up to my own cycle-699 (held `DONE`, withheld `DONE_VERIFIED` — container was ~2 days stale). `ops` reported (commit `37a03065d`) having rebuilt mcp-server, SHA-verified, health-checked, and DB-checked (30 cols/115 rows/id=10884, `is_correlation_stub` claimed pre-existing). Did NOT trust that prose — independently re-derived all 4 asks.

**SHA/deploy check (mine, not ops's):** `docker inspect` label `vn.market.git_sha=78e4b06afcfc1a9282966f8b258205fdd4cb4983`, container created `2026-08-13T19:18:30Z`. `git merge-base --is-ancestor 78e4b06af HEAD` true (current main HEAD `2deae06b0`, 2 commits ahead — both doc/orch-state-only, confirmed via `git show --stat`, zero `apps/` touch). `git diff --stat 78e4b06af HEAD -- apps/` empty — zero app-code drift since deploy, so the running image genuinely reflects current main HEAD's code, not just the label's literal SHA. Ran `scripts/verify-deploy-sha.sh mcp-server 78e4b06af...` myself → `OK`.

**`is_correlation_stub` provenance (mine, not ops's citation):** ops cited commit `94de167ea`/2026-06-10 as the origin — checked it directly, WRONG: that commit is `FIX-ALERT-ORPHAN-CORRELATION` (adds `alert_id`, dated 2026-06-13), no `is_correlation_stub` anywhere in its diff. Found the real origin via `git log -S"is_correlation_stub" -- apps/`: commit `c06030015` (2026-06-19, `alertStore.ts`'s `ensureCorrelationStubColumn`, a plain idempotent `ALTER TABLE ADD COLUMN`) — genuinely pre-existing, ~2 months before this refactor. Confirmed zero references to `is_correlation_stub` anywhere across all 20 new `agentSignals/*.ts` modules (`grep -rl`, no match) — the split never touched this column's owner file. Substance of ops's claim (pre-existing, not introduced by this refactor) held; the specific commit/date citation was wrong.

**Live health + round-trip write/read (mine, not ops's numbers):** curled `/health` myself → `status=ok, toolCount=183`. Called `post_agent_signal` directly against the running container's `/mcp` HTTP endpoint (`from_agent=to_agent=qa-live-verify`, `signal_type=signal_feedback`, `stock_code=QATEST`) → wrote row `id=10885`; `get_agent_signals(agent=qa-live-verify,status=all)` read it back with matching title/detail marker. Independently queried the live DB myself (`docker exec ... bun -e`, `bun:sqlite` readonly, inside the actual running container, not a host/sandbox copy): 30 columns, 116 rows, `MAX(id)=10885` — exactly baseline 114 + ops's own +1 arrival (115/id=10884, captured at rebuild time) + my own probe write (116/id=10885, captured now) — a fully consistent monotonic chain, no gaps, no resets.

VERDICT: `DONE_VERIFIED`. All 4 asks (SHA-genuine, `is_correlation_stub` pre-existing, live health, live write path intact) independently re-derived and held. Moved `task_board.qa[]` → `task_board.done_verified[]` via `orch-apply.sh` (conservation clean, task_total 732=732; Stage 1g's 16 pre-existing warnings unrelated, none touching this row), `next_agent: pm`, `verification.raw_probe` attached with the concrete tool/args/live_value_observed/observed_at evidence above. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S112. `task_heartbeat` called on `task:FACTORY-INFRA-split-agentSignalStore` at cycle start per router instruction; `post_agent_signal`/`get_agent_signals` called directly against the container's `/mcp` HTTP endpoint via `curl` (not the gateway MCP binding — not available in this sub-session, consistent with INV-GATEWAY-1's documented specialist-session gap). No `task_release`/`send_telegram` called — dispatcher's finally-block + TTL expiry is the authoritative release path.

## cycle-697 · 2026-08-13T16:27:28Z · FACTORY-GUARD-CI-RAWVERIFY-IMPL (developer, real code, commits `127c4a597`+`654181692`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), 3rd such pass this session after SHAREDPKG/NOHARDCODE, explicitly asked to RAW re-verify not trust prose. Row carried no `.commit`/`.files[]` — derived via `git log --all -- scripts/audits/rebuild-raw-verify-check.sh`, 2 hits: primary `127c4a597` (2026-07-30, this task's own scope) + follow-up `654181692` (2026-08-07, separate ATTEST_ERE fix task, same files). Both confirmed ancestors of `main` HEAD; `git show --stat` matches all 5 claimed files exactly (new script + test.sh, `pre-push`, `ci.yml`, `dev-standards.md`).

RAW re-ran fresh: `rebuild-raw-verify-check.test.sh` → 12/12 PASS live, not stale claimed 9/9 — traced the 3-test delta to the separate later fix commit (regression + AC2 corpus tests); all 9 originally-named cases (trigger-fail, commit-msg/inline/non-trigger PASS, decisions-journal, no-field-match, colocated-test, usage-error, zero-sha) individually confirmed present+passing among the 12, so the developer's own claim still holds as a true subset. shellcheck 0 findings on all 3 touched shell files (`rebuild-raw-verify-check.sh`, `.test.sh`, `pre-push`); `ci.yml` YAML-parse-validated.

Did NOT stop at the bundled test file — built an INDEPENDENT scratch git repo and exercised `rebuild-raw-verify-check.sh` directly with hand-crafted commits: unattested trigger (added `confidence` field under `apps/*/src/infrastructure/`) → exit 1 BLOCK; same commit amended with a `RAW-verify:` message token → exit 0 PASS; doc-only diff (non-trigger path) → exit 0 PASS; missing args → exit 2; zero-SHA range → exit 0 fail-open. All 5 matched the claimed behavior exactly — highest-confidence verify this session since it's a security/CI gate script.

Confirmed pre-push wiring is nested INSIDE the existing `CODE_TOUCHING_REGEX`-gated block (line 86-97) — doc-only-push fast-skip provably unaffected. CI job `rebuild-raw-verify-hook` confirmed with `fetch-depth: 0` + correct `github.event.before`/`github.sha` args. `git show --name-only` on both commits: zero `apps/` files touched — `bun test`/`tsc` correctly N/A; `mock-guard.sh` correctly reports no production TS/JS source to scan (PASS). Decision-journal entry `developer-S34` confirmed present at the cited path with matching task-id.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe` attached per RC-VERIF schema on the first write attempt (built into the jq transform from the start this time, not a retry). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S109. No `task_release`/`send_telegram` called — no MCP gateway tool bound this session (per dispatcher's own framing, INV-GATEWAY-1); dispatcher's finally-block + TTL expiry is the authoritative release path. Orch-state write left UNCOMMITTED per convention — dispatcher will push.
