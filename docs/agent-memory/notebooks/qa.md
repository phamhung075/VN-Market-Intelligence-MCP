# QA — Notebook

## cycle-554 · 2026-08-06 · FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, `commit_sha` present on the row itself: `f28c6c239`)

Commit `f28c6c239` confirmed real + on main ancestry; `git show --stat` matches the row's claimed offending file (`macroTools.ts`) plus the new extraction sibling `macroSnapshotText.ts` (109L) the commit message describes. `caused_by_commit e64ad8870` also confirmed on main ancestry (the commit whose inline `buildMacroSnapshotText` addition originally pushed `macroTools.ts` from 510L to 618L, crossing baseline=501L/upper=551L).

Did not trust the row's "530L"/"0 offenders" claims at face value — re-derived live: `wc -l macroTools.ts` today shows **540L**, not 530L. Investigated rather than flagging as a discrepancy: `git show f28c6c239:macroTools.ts | wc -l` = exactly 530, confirming the row's own claim was accurate AT commit time; `git log f28c6c239..main -- macroTools.ts` shows one later, unrelated commit (`123903578`, FIX-VNINDEX-CROSS-PLANE-PLAUSIBILITY-GATE) added the extra 10L — still well inside the 551L upper tolerance, not a regression of this row. Ran `size-lint-justification.sh --check` live: it FAILs today with 4 offenders, but grep-confirmed neither `macroTools.ts` nor `macroSnapshotText.ts` is among them — the 4 (`schema.ts`, `getBctcRefinedTool.ts`, `app_factory.py`, `embedder.py`) are the SAME unrelated pre-existing offenders already flagged independently by sibling qa-drain verifies this cycle (cycle-552/553 journal, qa-S1/S2 of this sprint journal) — corroborating cross-check that this is real independent drift, not this row's regression.

Checked the DDD grep hit myself rather than waving it through: `macroTools.ts` imports `infrastructure/logger` + `infrastructure/fetchers/fetchDeadline` — confirmed via `git show e64ad8870^:macroTools.ts` both imports pre-date this fix (present before the offending commit too, untouched by this extraction) and the file lives in `interface/mcp/tools/` (62 other sibling files import `infrastructure/logger` the same way) — established layer convention, not a domain→infrastructure violation.

Re-ran REAL checks, not the row's own prose: `bun test FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT.test.ts 089-tool-macro.test.ts` → 34/34 pass, 72 `expect()` calls, exact match to the row's claim. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` on both touched production files → PASS. CI plane independently re-verified, not trusted from status_note: `gh run view 30678397978 --json jobs` → `size-lint` job `conclusion=success`, confirming the claimed post-push green the row's `verification_gate` (`ci_green_on_subsequent_push`) required.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation check OK at write time). `next_agent: pm`. Verification text appended to the row's own `status_note` field. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S9. Task lock held by dispatcher coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); flagging to the calling dispatcher to release `task:FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L` itself. NOTEBOOK RETENTION: appended via targeted Edit anchored on the exact prior last line to avoid a lost-update race against concurrent sibling QA-drain sub-agents; noted this file already lost cycle-550/551 content to a concurrent overwrite before this write (not caused by this edit) — flagging for a later single-writer pruning/recovery pass, not attempting recovery myself mid-drain.

## cycle-554 · 2026-08-06 · FIX-DEVTEAM-REVIEW-LANE-SECONDARY-DRAIN — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, `commit` present on row: `87bd59032`)

Commit `87bd59032` real + on main ancestry. `git show --stat`: 2 of the row's own 3 `files[]` present (`dev-team/flow/main.md`, `scripts/lib/devteam-eligibility.jq`); 3rd (`docs/data/orch/orch-state.json`) absent from the diff. Did not wave this through on prose — it is the mechanism's runtime read/write target (hot file, `orch-apply.sh`-gated), never a static source file a code commit would touch, and confirmed benign two ways rather than assumed: (1) own scratch-copy dry-run (never the live file) of `scripts/devteam-review-claim-secondary-drain.jq` reproduced the documented behavior exactly — `review[]` length unchanged 203→203, `.head` byte-identical, correctly claimed the single oldest `REVIEW`+`next_agent!=qa` row; (2) live production evidence: 9 rows already carry `secondary_claimed_at` (2026-08-05→2026-08-06), including this SAME dispatch tick's own live exercise (`FACTORY-NEWS-fix-source-logging` → `po`, `secondary_claimed_at 21:43:25Z`) — mechanism demonstrably firing correctly end-to-end, independently reconfirmed rather than taken on the dispatcher's or the row's own word.

0 TS files touched (jq + md only) — ran the dedicated regression `scripts/devteam-eligibility-resolved-secondary-dispatch-target.test.sh` (5/5 pass: null/absent/`dev-team`-self-target/pass-through cases) plus `bun tsc --noEmit` (0 errors) and `mock-guard.sh --files` on both touched files → "No production source files to scan. PASS." Did not re-run the full monorepo `bun test` suite (no TS surface in this diff; targeted checks above cover the actual change).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation check: task_total 755→755, signal_total 203→203 — OK). `next_agent: pm`. Verification text appended to row's own `status_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S8 (2026-08-06T21:49:48Z). Task lock held by dispatcher coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); flagging to the calling dispatcher to release `task:FIX-DEVTEAM-REVIEW-LANE-SECONDARY-DRAIN` itself.

## cycle-555 · 2026-08-06 · FIX-DEVTEAM-PREFLIGHT-STEP55-COLDEVICT-STDOUT-LEAK-CORRUPTS-VERDICT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit_sha on row: `f2b41e6df`)

Dispatch note flagged that the dispatcher's own live tick this same run reproduced the exact live corroborating shape (multi-line cold-evict progress block ahead of the JSON verdict, jq parse succeeded regardless) — treated as useful context only, RAW-verified the committed fix myself regardless. `f2b41e6df` real + on main ancestry, but `git show --stat` only lists 2 of the row's 3 `files[]` (the `.sh` + `.test.sh`, truncated-path display); switched to `git show --name-only` and found the 3rd claimed file, `docs/agents/dev-team/flow/main.md`, lands in a companion same-session commit `ffb39318f` (also main-ancestor-confirmed, `Task:` trailer names this exact task id, carries AC-3/AC-4 — the JUMP-TO table's new empty/unparseable-VERDICT row). Together the 2 commits cover all 3 claimed files; flagged the row's own `commit_sha` field as incomplete metadata (names only 1 of 2) in the board's `[QA] Review Record`, non-blocking.

Re-ran REAL verification, not trusted from prose: `bash scripts/agents-flow/dev-team-tick-preflight.test.sh` live → 124/124 PASS (was 103/103 at commit time — 21 unrelated tests landed since, no regression). Went further than trusting the commit message's own stash-test claim: independently reverse-applied ONLY the `f2b41e6df` `.sh` hunk against current HEAD in an isolated scratch copy (never the live repo) — T36's 4 targeted checks (single-JSON-stdout via `jq -e .`, RUN verdict, both leak lines routed to stderr not stdout) FAIL pre-fix / PASS post-fix, matching the commit's own "fails 4/5 checks... all green post-fix" claim exactly (5th check, bare exit=1, trivially passes either way). Ran a same-environment control (unmodified post-fix script, identical minimal scratch dir) and got 2 extra unrelated failures (T14, T35(ii)) there too — confirmed these are scratch-environment artifacts (missing `$ROOT` repo structure for non-hermetic "REAL" checks), not attributable to this fix; the true isolated signal is exactly T36's 4 checks.

No TS/production source touched (pure bash+md, zone `cross-service/`) — `bun tsc --noEmit` structurally N/A; `mock-guard.sh --files "scripts/agents-flow/dev-team-tick-preflight.sh"` → "No production source files to scan. PASS."; DDD/security grep N/A (no infrastructure/application imports, no `process.env`/secrets in either commit's diff). `bash -n` clean both scripts; `shellcheck -x` 0 new findings (1 pre-existing informational SC1091, unrelated — sourced file not found by shellcheck's static follower, not a real defect). DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md:73` task-id-stamped, predates this verify — all 4 ACs justified there including the deeper out-of-scope `orch-cold-evict.sh:930-932` leak-source finding (correctly flagged, not fixed, since that file isn't in this row's `files[]`).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (fresh re-read immediately before write — conservation OK task_total 755→755, signal_total 203→203). `next_agent: pm`. Verification text appended to the row's own new field `qa_direct_commit_verify_20260806` (row has no `status_note`/handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S10 (2026-08-06T21:52:30Z). Task lock held by dispatcher coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); dispatcher will release `task:FIX-DEVTEAM-PREFLIGHT-STEP55-COLDEVICT-STDOUT-LEAK-CORRUPTS-VERDICT` itself per its own instruction, not attempted here.
