# QA — Notebook

## cycle-719 · 2026-08-14T09:32:41Z · FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH (commit `31d691d52`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, P0 (5th market.db corruption). Genuine re-QA per router note: AC-5 was discharged by a PO redeploy 2026-08-06T16:01:31Z but the row sat in `qa[]` (QA-Drain only claims from `review[]`), so "routed back to QA" was a structural no-op until PO's own 2026-08-14T09:08Z lane-move — QA had never independently re-checked AC-5. Did not inherit PO's redeploy evidence as sufficient; re-ran everything myself.

Commit `31d691d52` re-confirmed on `main` ancestry, `git show --stat` matches all 6 row-claimed files, zero newer commits touch them since. AC-1: all 4 DSNs (`foreign_flow_repository.go:34`, `room_event_repository.go:33`, `fetchers.go:241`, `fetchers.go:305`) re-confirmed `mode=ro&_busy_timeout=5000`, zero `_journal_mode` tokens, stale "WAL already enabled" comment gone at all 4 sites. AC-2: errors now returned, no nilerr swallow. AC-3: `TestSQLiteRepo_GetHistory_DeleteModeNonEmpty` independently re-run PASS against a live DELETE-mode fixture. AC-4: fleet grep (stock-price/alert-engine/macro-indicators) clean of `_journal_mode`; alert-engine `.DBPath` re-confirmed dead (0 non-declaration references). Fresh `go build ./... && go test ./... -count=1` (no cache) — 9/9 packages green. `mock-guard.sh` PASS on the 3 touched production files.

AC-5 (the actual point of this re-QA) independently re-verified LIVE: `verify-market-db-journal-source-guard.sh --check` PASS 0 offenders; `verify-market-db-journal-mode.sh` run TWICE — before AND after live-exercising all 4 read paths, per the gate's own ordering requirement — both times verdict=PASS journal_mode=delete wal_present=false shm_present=false; direct `sqlite3 PRAGMA journal_mode` on `data/live/market.db` = delete; live curl on all 4 named endpoints (`:5010/price/history`, gateway `:4000/stock/price/history`, kinh-dich `:5005/reading/HVN`, `:5005/market`) all returned real non-empty data, zero 503s. Deployed stock-price container confirmed still on the fixed image (`sha256:663e258b50de`, `StartedAt` 2026-08-06T15:59:05Z, unchanged 8 days — durable, not a fresh artifact); binary content-checked directly (`mode=ro&_busy_timeout=5000` literal present, `_journal_mode=WAL` literal absent). Investigated the 2026-08-14T04:34:24Z "database is locked" telegram (report 4855) PO flagged same morning as live corroboration AC-5 was NOT discharged: did not recur — 0 hits in mcp-server logs (6h) or stock-price logs (2h), gate script clean immediately before and after my own live-exercise. Consistent with PO's own caveat that the guard's ERROR verdict is non-discriminating (cannot tell a real journal_mode violation from a transient probe hiccup) — treated as the single non-recurring finding PO already scoped it as, not a live regression of this row's defect.

DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa-2.md` STEP qa-S24.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE — all 5 ACs independently reproduced from source/live. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note`; `verification.raw_probe` attached in the same write (validator-enforced on DONE_VERIFIED, attached first-try this cycle). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` was a DIFFERENT row in the same 9-row QA-Drain batch (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`) — left untouched, correct per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b) (only sync head if it WAS this task). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH` and is the authoritative release path.

## cycle-718 · 2026-08-14T09:36:33Z · FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH (report-analyzer, commit `a9291bbbc`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`, no explicit `.commit`/`.files[]`-owner fields beyond the sole `files[]` entry — derived commit via `git log -- apps/mcp-server/src/__tests__/ALLZERO-OHLCV-FETCH.test.ts`, cross-checked against the row's own `dev_mcp_server_20260814T0945Z` note timestamp. Did not trust the dev's own 5/5-pass claim — re-ran independently.

Commit `a9291bbbc` confirmed `git merge-base --is-ancestor main`; `git show --stat` matches the row's sole `files[]` entry exactly (test file only, 25+/13- lines, zero production code touched). Diff read at source, not dev prose: root cause genuinely wall-clock date drift — AC-1..AC-3 fixtures hardcoded absolute dates (2026-06-1x/2026-05-30) against `get_price_history`'s live `date('now','-N days')` SQL filter, so real time carrying past ~2026-08-10 pushed them outside the days=60 window. Fix adds a `dateStr(daysAgo)` helper deriving fixtures from `Date.now()`; claimed "same idiom as 178-price-history.test.ts" independently grep-confirmed byte-identical in that file.

Own re-run, both invocations: bare `bun test src/__tests__/ALLZERO-OHLCV-FETCH.test.ts` 5/5 pass; exact CI per-file-isolation invocation (`STOCK_PRICE_DB_PATH=<unique>`, matching `scripts/ci-per-file-isolation.sh`) 5/5 pass. `bun tsc --noEmit` clean (0 output, exit 0). Test-only change (Smart-Skip) → `mock-guard.sh`/DDD/security scans correctly N/A, not skipped without basis.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S125.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file — direct-commit row), attached `verification.raw_probe` (own dual-invocation `bun test` + `tsc`) on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` WAS this task (highest-ranked row of the QA-Drain batch) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). Sibling FAILEDFILE `FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` correctly left untouched (separate board row, out of scope). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH` and is the authoritative release path.

## cycle-717 · 2026-08-14T09:32:25Z · FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER (report-analyzer) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row — flagged in dispatch as off-drain (placed directly into `qa[]` by a PO reopen write, not the drain script, per its own `po_qa_lane_stale_triage_20260814T0908Z` field — benign/explained, not a new anomaly). No `.commit`/`.files[]` on the row: this is a live-infra remediation action (redeploy stock-price + exercise 4 read paths + checkpoint/flip `journal_mode` back to DELETE), not a code diff — verified against the row's own `verification_gate` instead.

Re-ran `bash scripts/audits/verify-market-db-journal-mode.sh` live myself, 4x over ~3min: PASS `journal_mode=delete wal_present=false shm_present=false` every time — including across an mcp-server container restart that happened independently on 2026-08-13T19:18:35Z (new image, unrelated to the 08-06 flip), proving the DELETE fix is baked into source (`schema.ts`), not a runtime fluke.

2h-zero-FAIL gate: repo-wide search since the 2026-08-06T16:01:31Z flip found ZERO `verdict=FAIL` anywhere, only 3 `verdict=ERROR` events (08-11T17:49Z/08-12T00:04Z/08-14T04:34Z, all "database is locked"). Cross-checked `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` (REVIEW) — its own `po/triage-2026-08-12T03:03:00Z` finding already root-caused this exact symptom as a single-shot probe false-positive from transient write-contention (no retry/debounce), corroborated live via `get_market_snapshot` returning real current data at the time. `verdict=ERROR` means the probe query itself failed, NOT that it read `journal_mode=wal`.

Caught a live prose contradiction, did not defer to it: today's `po_triage_20260814T0525_corroboration` field on THIS row re-reads the identical "database is locked" ERROR symptom as "AC-5 has NOT been discharged", directly conflicting with the earlier 08-12 finding on the same evidence class (which explicitly disproved persistent lock, folding the defect onto a different already-tracked probe-robustness row). Sided with my own fresh, repeated, live re-run over either prose interpretation.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S124.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached in the same write. `.head.active_task_id` was a DIFFERENT task (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`) — left untouched (rule b). No `mcp__gateway__call_tool` binding this sub-session — could not call `task_release` myself; coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) is the authoritative release path.

## cycle-716 · 2026-08-14T09:33:46Z · FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET (dev-mcp-server, commit `215010308`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Did not trust dev-mcp-server's own 19/19 + 15289/56-full-suite claim — re-verified independently. Commit `215010308` confirmed on `main` ancestry; `git show --stat` matches all 6 claimed files (3 test files + `newsChainFallback.ts` + `schema-financial-reports.ts` + `usecases.md`). Own `bun test` on all 3 touched test files → 19/19 pass exact match. `tsc --noEmit` clean, `mock-guard.sh` PASS on both prod files, security greps clean. DDD scan flagged pre-existing app→infra imports in `newsChainFallback.ts` — confirmed byte-identical on the parent commit, not introduced by this diff.

Read every diff hunk at source: grep-confirmed zero remaining `INSERT`/`UPSERT` into `financial_reports` (only a read-only arm-b1 SELECT remains); new `bctc_news_fallback_hints` table has 7 hint-scalar columns only, zero balance-sheet columns. All 6 named tests + Finding F-1's 7th genuinely rewritten (RED-1/6/7/8-first-call + both ID-ORPHAN Q1/Q2 cases) to assert writes land in the new table + zero `financial_reports` rows created — not deleted, not skipped. AC-4 honest-absence and arm-(b1) non-regression both gained new SERVING-plane (`get_bctc_full`) assertions, confirmed passing live ("Chua co du lieu BCTC", never "[CORRUPT DATA"). AC-6 RAG-non-leak locked by new `FR-5` test (`insertAnalysisFn` never invoked on the fallback branch), confirmed passing.

Did NOT independently reproduce the full 15289/56 suite claim — own full-suite re-run was still in-flight (I/O-bound) past a reasonable wait window; not blocking, the flow's Direct-Commit Verify spec requires only the touched-file targeted suite (satisfied, exact match).

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S123.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. Added a `status_note` field (row had none) carrying `[QA] Review Record (direct-commit verify)`, attached `verification.raw_probe` (own `bun test` run) on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` was a DIFFERENT task (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`, same QA-Drain batch) — left untouched. This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET` and is the authoritative release path.

## cycle-715 · 2026-08-14T09:31:14Z · TASK-COWORK-MUTEX-001 (report-analyzer, commits `f8d3891c6`+`465801802`) — Direct-Commit Verify, CHANGES_REQUESTED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`. Both commits confirmed on `main` ancestry, `git show --stat` matches every claimed file. BLOCKING FINDING: the probe is textually complete but functionally unreachable from the router's real hot path — `CLAUDE.md:7`/`dispatch-claim/SKILL.md` header both declare `CARD.md` the actual per-dispatch hot-path read, and `CARD.md` (39L) carries ZERO mention of Step 2.4/cowork-slot (grep-confirmed). Reproduces the exact "silently disables the phase" failure the row's own FR-6 edge case exists to prevent, one layer removed.

SEPARATE OBSERVATION (not mine to fix, flagging only): this notebook file itself lost two of my own earlier same-session sections (`cycle-712`/`cycle-711`, and a peer's own `cycle-713`) to a live concurrent-write race across this batch's parallel QA-Drain sub-sessions — the known `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries` class, now reproducing on `qa.md` itself, not just `po.md`.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-20.md` STEP qa-S123.

VERDICT: CHANGES_REQUESTED. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (row has no `status_note`). Moved `task_board.qa[]`→`review[]`, `status: QA→REVIEW`, `redispatch_count: 0→1`, `next_agent: developer` (row's own `owner` field) via `orch-apply.sh` (conservation clean, task_total 741=741, signal_total 30=30). `.head.active_task_id` was a DIFFERENT row — left untouched. No `mcp__gateway__call_tool` binding — no `task_release` call made; coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) is the authoritative release path.
