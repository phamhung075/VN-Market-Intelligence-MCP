# QA — Notebook

## cycle-631 · 2026-08-11 · FACTORY-INFRA-agentSignal-sql-binding — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `a881b2489`)

Dev-team Review-Lane QA-Drain spawn (`qa[]` row, `branch:null`, batch of 3 sharing `claimed_at`), dispatcher explicitly flagged the `review_note`'s A/B-probe claim on `getSignalEffectiveness`'s `days` field for spot-check. Confirmed `main` ancestry (`git merge-base --is-ancestor`); `git show --stat a881b2489` matches all 4 claimed files exactly (new test, `agentSignalStore.ts`, dev's own journal, dev-mcp-server notebook).

Read the actual diff, not trusted from review_note prose: all 5 caller-supplied-value sites genuinely converted to bound `?` placeholders — `getSignals` `hoursBack` (was raw template-literal arithmetic) + `signalType` (was manual `.replace(/'/g,"''")` escape), mark-as-read `UPDATE...IN(ids)` (was raw-joined ids in `db.exec`, now generated placeholder-count + `.run(...ids)`, matching `coordinationStore.ts`/`seedWatchlist.ts` precedent), `getSignalEffectiveness` `days` (was raw template literal) + `fromAgent`/`signalType` (was manual escape). No structural table/column interpolation existed either way (both pre/post).

Spot-checked the flagged `days`-field claim specifically: new test's malicious payload uses `"...DROP TABLE...;--" as unknown as number` — a real TS-bypass cast, since `GetEffectivenessOptions.days` has zero runtime validation inside the store fn itself. BUT the one live MCP tool boundary that calls it (`agentSignalTools.ts` `get_signal_effectiveness`) already does `z.coerce.number().int().positive()` on `days` — so the pre-fix exploit path required bypassing that layer (direct fn call / a future unvalidated caller), not a live MCP-reachable prod CVE today. Review's own wording ("genuinely exploitable pre-fix site" via A/B probe) does NOT overclaim MCP-reachability — accurate as stated, not inflated.

Re-ran REAL verification myself: new `FACTORY-INFRA-agentSignal-sql-binding.test.ts` 9/9 pass live; `bun tsc --noEmit` 0 errors; `mock-guard.sh --files agentSignalStore.ts` PASS. Ran the FULL agentSignalStore-dependent regression net (32 files by grep, wider than review's claimed 26): 335/335 pass, 0 fail — same clean result on the broader set, no regressions.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 30→30). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. `.head.active_task_id` pointed at a sibling batch row (`FACTORY-APP-pollNews-layering-fix`, not this one) — left untouched, no reset needed. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S48 (rolled from `-15.md`, which hit BYTE_CAP 37067>36000 — CAP-REACHED appended, new continuation file opened). `send_telegram` for the CAP-REACHED notice not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list, matching prior cycles' `task_release` gap. Pruned cycle-628 (`FIX-CRON-CODEJANITOR-NO-PRESPAWN-GATE-BOOTS-FULL-SESSION-4X-DAILY`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 629/630/631.

## cycle-632 · 2026-08-11 · FACTORY-APP-pollNews-layering-fix — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `a2a386d24`)

dev-team Review-Lane QA-Drain spawn (`qa[]` row, `branch:null`, `mode=verify-committed`, same 3-row batch as cycle-631's sibling, this the highest-ranked). dev-mcp-server self-closeout: moved `globalSourceTracker`(+`_resetGlobalSourceTracker`) singleton from `interface/mcp/tools/news-analysis/sourceHealthTools.ts` to new `infrastructure/observability/sourceHealthRegistry.ts`, resolving the Fence-B violation `eslint.config.mjs` already documented by name (pollNews.ts importing interface-layer state, `FENCE-LEGACY`). Confirmed `a2a386d24` on `main` ancestry (`git merge-base --is-ancestor`); `git show --stat` matches all 12 claimed files exactly. Byte-compared the singleton block old-file vs new-file myself (seed list, `recordDisabled` calls, `globalThis` stash key, reset logic) — genuinely identical, pure relocation. Confirmed the `eslint-disable-next-line ... FENCE-LEGACY` comment is actually gone from pollNews.ts and its import repointed; `eslint.config.mjs` comment updated; `systemTools.ts` + `tradingEconomicsChromium.ts` (2 dynamic imports) both repointed. Repo-wide grep confirms zero dangling imports to the old interface-layer path.

Independently re-ran, not trusted from prose: `bun tsc --noEmit` 0 errors; `eslint` on all 6 touched production files 0 errors/warnings; targeted 7-file pollNews/source-health suite 42/42 pass, 0 fail; `mock-guard.sh --files` PASS on all 6 files; security grep (process.env/password/secret/token) on every added diff line clean.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) AND reset `.head`→idle in the SAME `orch-apply.sh` write — `.head.active_task_id` pointed at this exact row, CANONICAL:SSOT-STATUSFLIP-LANEMOVE (`dev-team/flow/main.md:1071`) applies to any terminal-status flip, not only CHANGES_REQUESTED (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 30→30). `[QA] Review Record (direct-commit verify)` appended to `review_note`. DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S49 (verified present before flip). `task_release` not attempted — no gateway binding surfaced this sub-session.

OPERATIONAL FINDING: two prior append attempts to this notebook were silently dropped mid-session by a concurrent QA sibling session's own notebook write (`feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries` class, previously seen on `po.md`, now confirmed on `qa.md` too) — this write uses the correct AC-3 compose-in-memory-then-single-Write protocol (was mistakenly using Edit/append on the first two attempts) to land cleanly. Pruned cycle-629 (oldest, `SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-FIRER`) this write per AC-2 retention (3-section cap) — kept 630/631/632.

## cycle-633 · 2026-08-11 · FACTORY-INFRA-split-ssc-fetchers — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `315bc0728`)

dev-team Review-Lane QA-Drain spawn (`qa[]` row). dev-mcp-server self-closeout split `ssc.ts` (1029L→186L) into `sscCommon.ts`/`sscPortal.ts`/`hoseDisclosure.ts`/`hnxDisclosure.ts`/`upcomDisclosure.ts`, self-flagging a scope deviation: canonical ticket wants a `disclosures/` subfolder + <=120L cap, actual is flat sibling files, citing this session's `newsNormalizer` split as precedent. Confirmed `315bc0728`+`64d014937` on `main` ancestry; `git show --stat` matches all 6 files exactly.

Independently re-proved zero-drift (not trusted from prose): comment/blank-stripped, import/export-normalized, multiset-sorted diff of old `ssc.ts` vs. concatenated new files — ONLY delta is 5 new cross-module import lines, every statement byte-identical; confirmed the 3 claimed export-only flips at source. Re-ran a BROADER regression than the review's cited 9-file/85-test claim (repo-wide grep for real imports of the 6 modules — 17 files): 201 pass/3 skip/0 fail/204 tests. `tsc --noEmit` clean, `mock-guard.sh` PASS, DDD/env/secret greps clean. `hose.ts`/`hnx.ts` (unrelated fetchers) confirmed untouched, no collision.

Independently judged the flagged deviation, not rubber-stamped: ALL 6 files exceed 120L (worst `sscPortal.ts` 388L, >3x cap) — worse than the review_note's own framing (only cited ssc.ts's 186L). `size-lint-justification.sh --check` passes today only because `size-lint-baseline.json`'s last regen (08-09) postdates this split (07-24) and swept it in as grandfathered — a baseline-timing artifact, not genuine justification (no file carries an in-file `size-justification:` header). Weighed against the identically-shaped `FACTORY-DOMAIN-split-newsNormalizer` precedent (also unsupervised BOUNDED-1 auto-pickup, same no-subfolder/oversized-file pattern up to 503L) already QA-APPROVED/DONE_VERIFIED — rejecting this row for the same deviation shape would be an inconsistent gate given proven-nil functional risk.

VERDICT: APPROVED, ACCEPTED-AS-IS on the deviation. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 30→30). `[QA] Review Record` appended to `review_note`. `.head` already idle, no reset needed. DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S48 (this file independently hit BYTE_CAP 39898>36000 on my own write too — CAP-REACHED appended; a peer session's own identical check already landed a CAP-REACHED marker there moments earlier at a different timestamp, benign duplicate, non-corrupting). 2 non-blocking findings for PO/architect: (1) add size-justification headers to the 6 ssc files; (2) size-lint baseline-grandfather silently absorbs post-gate-creation files as pre-existing debt when a regen lands after their creation — worth its own FIX row. `task_release`/`send_telegram` not attempted — no gateway binding surfaced this sub-session. Pruned cycle-630 (oldest, `SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-WIRING`) this write per AC-2 retention (3-section cap) — kept 631/632/633.
