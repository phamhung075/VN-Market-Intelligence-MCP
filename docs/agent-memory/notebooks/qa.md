# QA — Notebook

## cycle-402 · 2026-07-08 · FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE — independent re-verify PASS, DONE_VERIFIED

Follow-up from SPIKE-GATEWAY-BLIND-CLI-HANDSHAKE (architect). Developer added `mcp_call_gateway_meta()` + 3 private helpers to `scripts/agents-flow/mcp-call.sh` (3-step stateful handshake: `initialize`→`notifications/initialized`→`tools/call`, reusing the minted `mcp-session-id` header) bridging the 3 gateway meta-tools (`list_servers`/`list_server_tools`/`search_tools`), plus §6 Degraded Mode in `docs/standards/gateway-call-contract.md` (self-diagnosis, coverage matrix, §6d de-escalation rule that stops CRITICAL re-raise churn). Router had already RAW-verified (20/20 stub tests, shellcheck clean, regression suites 20/20+55/55, grep confirms `mcp_call()` untouched at line 93) before dispatching — did not rubber-stamp that: read the actual diff (a6031047e) myself, confirmed `mcp_call()` is byte-unchanged (purely additive insertion after its closing brace). Re-ran everything myself: 20/20 new tests, shellcheck+bash -n clean, both regression suites unchanged. Went beyond stub re-runs — sourced the REAL (non-stubbed) function and live-called all 3 meta-tools against the actual production gateway (`https://zenmidi.com/gateway/mcp`): `list_servers`/`list_server_tools` succeeded; `search_tools` with the doc's claimed-correct `{"query":...}` succeeded; `search_tools` with the doc's claimed-wrong `{"keyword":...}` failed with the EXACT error text the new §6b footgun note quotes (`unexpected additional properties ["keyword"]`) — corroborated the doc's factual accuracy live, not by re-reading prose.

Security: no `process.env`, no hardcoded secrets/tokens; `mcp-session-id` header value sanitized (`tr -d` whitespace/CR/LF) before reuse; endpoint genuinely unauthenticated, matches the cited SPIKE. Scope check: `git diff --stat a6031047e^..c80473c93` touches exactly the 6 claimed files (script, test, doc, journal, notebook, orch-state) — zero `apps/*`, no scope creep. Task-board hygiene: row confirmed present in `review[]` only (not duplicated in `in_progress[]`); `.head` confirmed untouched (`active_task_id` still `FACTORY-APP-split-fetchParseAndStoreBctc`, owned by a concurrent parallel FACTORY dispatch) per explicit instruction — only this task's own row moved. DJ-GATE-1 confirmed: developer journal STEP developer-S7 substantive.

VERDICT: APPROVED, no blocking issues. Flipped `task_board.review[]`→`task_board.done_verified[]` (status DONE_VERIFIED, next_agent=router) via `jq`+`scripts/orch-apply.sh` — script+doc-only fix, no Docker Close Gate applicable. `.head` deliberately left untouched (owned by the concurrent FACTORY dispatch) — only this row moved, per explicit dispatch instruction. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S23. Report: reports/TASK_REPORT_FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE.md.

## cycle-401 · 2026-07-08 · FIX-DEVTEAM-PREFLIGHT-SF1-REENTRANT — independent re-verify PASS, DONE_VERIFIED

Standard Dev Team Handoff Chain review (not a Docker Close Gate — bash script + test file only, no rebuild/deploy). Developer fixed `_step_sf1_claim()` in `scripts/agents-flow/dev-team-tick-preflight.sh` (commit 216205e0c): the function read only `.claimed` on a failed SF-1 claim and unconditionally returned "peer holds it" → SKIP, even when `current_holder.owner_client_session` equaled the calling session's own ID — 4 real consecutive false-peer SKIPs stalled the dev-team cron loop for over an hour. Did not trust the dispatcher's own pre-verify at face value: read the actual diff myself and confirmed `_step_sf1_claim()` now mirrors the sibling `_step_fire_election()`'s existing self-hold branch exactly — on `claimed:false`, compares `current_holder.owner_client_session` to self; self-hold heartbeat-renews SF-1 and returns 0 (proceed); genuine peer (different session) falls through unchanged to the original SKIP path (a), releases nothing, still returns 1 — the safety-critical peer-collision distinction is NOT weakened. Re-ran `dev-team-tick-preflight.test.sh` myself directly: 55/55 PASS (53 pre-existing unchanged + T19 self-hold→RUN+heartbeat/no-release/no-telegram + T20 regression guard: real peer-hold still SKIPs). Checked for other callers: `_step_sf1_claim()` has exactly one call site (`run_preflight()`); grepped the whole repo for anything else assuming the old broken behavior — only decision-journal/notebook/architecture-brief prose references it, nothing else silently depended on it.

Found one pre-existing residual of the SAME bug class, correctly out of this task's scope: `docs/agents/dev-team/flow/main.md` Step 0-PREFLIGHT-FALLBACK's inline SF-1 claim pseudocode (~L158-161, ERROR-fallback/manual-run path only) still has no self-hold branch, unlike its own sibling fire-election block immediately below it (~L194-203) which does — pre-existing, not introduced/worsened by this commit, already flagged in the script's own header comment (L29-33), reached only on transport/malformed-JSON error or a fully-manual run. Flagged non-blocking as a follow-up candidate (task-breakdown is not QA's job, did not mint a new backlog row). DDD scan not applicable (bash, no domain/infra layering); security scan clean (no `process.env`, no hardcoded secrets in the diff); shellcheck flags only 2 pre-existing info-level nits on lines untouched by this diff. DJ-GATE-1 confirmed: developer journal STEP developer-S6 substantive.

VERDICT: PASS. Flipped `task_board.review[]`→`task_board.done_verified[]` (status DONE_VERIFIED, next_agent=router) via new `scripts/qa-fix-devteam-preflight-sf1-reentrant-done-verified.jq` + `scripts/orch-apply.sh`, matching the sibling non-Docker dev-team script-fix precedent (FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE, commit 12c7725fd) rather than the Docker Close Gate `done`-lane pattern. `.head` synced to status=done/active_task_id=null/next_agent=router in the SAME write, closing the head-sync gap named in the dispatch prompt. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S22.

## cycle-400 · 2026-07-08 · FACTORY-DOMAIN-relocate-stock-catalog — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (commit 64a1a135b board-flip, docs-only outcome commit 3489d4e64). dev-mcp-server relocated the 66-ticker STOCK_CATALOG reference table out of `stockAliases.ts` (828L→264L) into a new pure-data `stockAliases/catalog.ts` (618L, 0 imports). Dispatching router had already independently RAW-verified both prior stages before dispatching (commits/board/wiring/tsc for dev; SHA-gate/health/peer-containers for ops) — this session's scope was Step 5 only, and every check below was re-derived from artifacts, not accepted from the review_note or dispatch prompt. Re-ran `verify-deploy-sha.sh` myself: drift (deployed=64a1a135b vs HEAD=3489d4e64) confirmed BENIGN — `git diff --stat` since the deployed commit touches only ops's own docs-only outcome journal + orch-state.json, zero apps/mcp-server changes, ancestry holds. `/health` toolCount=183 matches `docs/data/project-stats.json` SSOT exactly (183, and infrastructureStatus.toolCount=183); live `cron.schedule` grep=2 matches SSOT cronJobCount=2 (task untouched scheduler, no collateral regression). Did NOT trust dev-mcp-server's own "scratch equivalence script" claim: wrote an independent A/B harness, `git worktree add --detach` at `cec87c726^` (genuine pre-move parent), imported OLD single-file `stockAliases.ts` vs NEW split `stockAliases.ts`+`catalog.ts` at HEAD side-by-side — deep-equality diffed the full STOCK_CATALOG (66/66 tickers) plus all 5 exported functions: `getAliasesForCode`/`getCompanyName` for all 66 tickers + 1 unknown code, `detectStocksInText` across 15 realistic Vietnamese multi-ticker/context-guard/no-match headlines (caught and corrected my own wrong assumption about its signature via a first-run TypeError — real signature is `(text, watchlistCodes)`, 2-arg), `tickerWholeWordMatch`, `stripSourceAttributionSuffix` — 0 mismatches. `bun tsc --noEmit` clean (mine); `npx eslint` on both touched files clean (mine); independently confirmed `catalog.ts` literally has 0 import statements. Re-ran the claimed targeted 211/211 myself — exact match. Went beyond the claimed "consumer 195/195": built a broader 51-file consumer set (every test file referencing direct consumers `stockSearch.ts` or `newsNormalizer.ts`) — 748 pass/0 fail (752 total incl. skips), a superset with zero failures.

Live-container artifact check (explicitly required by dispatch, not just `/health`): `docker cp`'d both live files out of the running container, byte-identical to host HEAD via `diff`. This QA session has no `mcp__gateway__call_tool` binding (subagents are gateway-blind), so instead of accepting that as a ceiling: (1) `docker cp`'d a script INTO the container and ran it via the container's own bun runtime importing the actual on-disk deployed modules — correctly resolved `getCompanyName`/`getAliasesForCode`/STOCK_CATALOG membership for VCB/FPT/HPG/VNM/HVN (GVR probed and found absent from STOCK_CATALOG — confirmed pre-existing in both pre-move and post-move files via grep, not a relocation regression, since STOCK_CATALOG's 66 tickers aren't a 1:1 mirror of the 41-stock watchlist); (2) independently constructed a raw MCP JSON-RPC handshake (`POST /mcp`, stateless per-request transport, no session-id required) and called the live `get_watchlist` tool end-to-end — confirmed the deployed tool-dispatch path executes correctly post-relocation. Cleaned up both container scratch scripts (confirmed removed), removed the git worktree, `git status --short apps/mcp-server/` clean.

VERDICT: pure-relocation claim holds on every axis (SHA-gate ancestry, live byte-identical deployed source×2 files, an independently-authored A/B harness — not the dev's own script — showing 0 mismatches across the full catalog and all 5 exported fns, independently-reproduced targeted 211/211 plus a broader 748/0 consumer superset, clean tsc+eslint, a genuinely 0-import pure-data catalog.ts, live in-container execution resolving real tickers correctly, and a live raw MCP tool-call proving the deployed tool-dispatch path works). Did NOT flip to `DONE_VERIFIED` — appended findings to the existing `review_note`, moved `next_agent` to `po` via new `scripts/qa-factory-domain-relocate-stock-catalog-hold-review.jq` + `scripts/orch-apply.sh`. Guard-script note: the board-row's own `next_agent` copy had lagged at `ops` (dev-mcp-server's original handoff target; ops advanced `.head.next_agent` to `qa` after Steps 1-4 but never synced the board-row's copy of that field) — the guard was written to check `.head` (the documented authoritative pointer) instead of the stale row copy, per `feedback_orchstate_dual_head_keys_toplevel_authoritative`; first write attempt correctly refused on the stale-copy guard, re-derived and re-ran successfully. `.head.next_agent` synced to `po` in the same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S21 (also backfilled §qa-S20 for cycle-399, which the prior session's notebook entry referenced but had not yet appended to the DJ file).

## cycle-399 · 2026-07-08 · FACTORY-DOMAIN-extract-bctc-parsing-lib — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (Steps 1-4 commits e3e0dfeb9/26f4f310f). dev-mcp-server extracted shared BCTC parsing primitives (`lib/lineScan.ts` canonical `findValue`/`findValueByCode`/`scaleNumericFields`/`detectDivisor`, `lib/vasPatterns.ts` shared VAS regex) and migrated `balanceSheetExtractor`/`cashFlowExtractor`/`incomeStatementExtractor`/`bctcScalarAggregator` one at a time; `bctcMagnitudeValidator` deliberately left untouched. Named risk: `findValue` unifies a 2-arg row-code-guard variant (balanceSheetExtractor) and a 4-arg ASCII-fallback variant (income/cashFlow) into one opt-in `{rowCodeGuard, fallback}` options object. SHA-gate: `scripts/verify-deploy-sha.sh mcp-server` reported drift (deployed=dd37ec6c3 vs HEAD=26f4f310f) — confirmed BENIGN not a Step 4 failure: `git diff --stat dd37ec6c3..26f4f310f` is docs-only (1 memory doc + orch-state.json), and `git merge-base --is-ancestor 4f15363da(last code commit) dd37ec6c3(deployed)` holds — all 5 code commits are ancestors of the deployed image. Did NOT trust the SHA label or review_note alone: `docker cp`'d all 7 touched/created files out of the live container and diffed byte-for-byte against host HEAD — ALL byte-identical (Bun runs src/*.ts directly, no dist build). `/health` toolCount=183 matches baseline, container Up (healthy). Independently re-ran the BCTC-domain suite myself (grep -rl over the 7 module names across `src/__tests__`, 40 files found — close to review_note's claimed 42): 414 pass/4 skip/0 fail; `bun tsc --noEmit` clean.

Went beyond the task's own suggested checklist (real-data A/B, not just re-running the existing test suite): `git worktree add --detach` at `cb3c1ebe0` (immediate parent of first migration commit `7f65dfccd` — genuine pre-migration baseline) vs current HEAD. Pulled REAL inputs off a `docker cp` copy of the live named-volume `market.db`: (1) FPT 2025-Q4 (report_id e71f845d, non-bank) — concatenated 27 non-quarantined `bctc_layout_units.stitched_markdown` pages as rawText, ran `extractBalanceSheet`+`extractIncomeStatement`+`extractCashFlow` in both worktrees — stdout JSON AND stderr guard/warning logs byte-identical (md5 match), including the FIX-BCTC-MAGNITUDE-NORMALIZE override and extractorGuards rejections firing identically; (2) VCB 2025-Q4 (report_id bdcfa5e0, bank-form) via `bctc_refined_units.markdown` — byte-identical output, confirms the `rowCodeGuard:true` path on real bank data too; (3) `aggregateScalars` (detectDivisor relocation) against real `bctc_table_rows` for FPT 2026-Q1 (145 rows) and CTG 2026-Q1 (96e36139, bank, 451 rows) — both byte-identical `ScalarAggregateResult` JSON. `bctcMagnitudeValidator`: `git diff cb3c1ebe0..HEAD` on this file is a pure 13-line comment insertion (0 lines changed/deleted, 0 new imports) — zero logic/import-path change confirmed at the diff level, not just by test pass; independently re-ran `bctcMagnitudeValidator.test.ts` + `FIX-BCTC-MAGNITUDE-NORMALIZE.test.ts` standalone: 32/32 pass. Cleaned up both harness scripts from the working tree and worktree, removed the git worktree (`git worktree remove --force`) — confirmed `git status --short` clean under `apps/mcp-server/`.

VERDICT: pure-relocation claim holds on every axis (SHA-gate ancestry, live byte-identical deployed source×7 files, independent test re-run, tsc, and real-data A/B on 2 non-bank + 2 bank reports covering all 3 statement extractors + the aggregator, plus diff-level confirmation the untouched validator has zero logic change). Did NOT flip to `DONE_VERIFIED` — appended findings to the existing `review_note`, moved `next_agent` qa→po (status held REVIEW) via new `scripts/qa-factory-domain-extract-bctc-parsing-lib-hold-review.jq` + `scripts/orch-apply.sh` (guarded on review[]+status+next_agent+head.active_task_id), `.head.next_agent` synced to `po` in the same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S20.

**Retention note:** cycle-398 (FACTORY-INTERFACE-extract-finalizeBctc-usecase — HELD REVIEW, live A/B DB proof the 127→128 row delta pre-dated this task's relocation) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file.
