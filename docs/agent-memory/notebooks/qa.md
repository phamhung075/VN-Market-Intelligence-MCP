# QA — Notebook

## cycle-502 · 2026-08-04 · CADRAT-3 + CADRAT-7 — 2/2 APPROVED, DONE_VERIFIED (direct-commit verify, po lane-move existence-only, ACs still qa's job)

Narrow continuation of cycle-501's held-back 2 rows. Po independently confirmed `5ee84f0f5`/`d916aa40b` exist on `main` and lane-moved `ready[]`→`review[]` but explicitly did NOT adjudicate ACs. Read both diffs directly rather than trusting po's/dev's prose (`feedback_router_verify_raw_not_badges`).

CADRAT-3: read the full `code-janitor/flow/main.md` + `agent-father/flow/keep.md` post-edit — Pre-Check gate sits correctly between Step 0b and Decision Tree / Steps 1-2 (AC-1/AC-3); the 3 every-scan sweeps (Memory Prune/Notebook Line-Cap/Cold Archive) sit in doc order AFTER Decision Tree with no conditional wrapper — confirmed unconditional+reachable, not assumed (AC-2). AC-4 dry-run proof re-run myself against 4 REAL historical windows (not the row's claimed outputs): src/ scope fall-through (`8c2acb44c~3..8c2acb44c` → hits `apps/mcp-server/src/__tests__/...`) + skip (`5ee84f0f5~3..5ee84f0f5` → zero src hits); `.claude/agents`+flow scope fall-through (`d4371fdcf~3..d4371fdcf` → hits `docs/agents/dev-team/flow/main.md`) + skip (`c5250771c~3..c5250771c` → zero hits) — all 4 correct. `claude-manager-helper/flow/main.md` confirmed absent from diff (AC-5). `scripts/audits/size-lint-justification.sh --check` run myself — neither file in the 2-file FAIL list (both pre-existing, unrelated) (AC-6). `grep -i CronCreate` on the full diff: zero matches (AC-7). Doc-only — bun test/tsc/mock-guard structurally N/A.

CADRAT-7: jq per-slot diff (`sort_by(.slot_id)`, before/after the commit, 23 slots both sides) shows ONLY `news-scout-sentiment`'s cron/utc_description/vn_description changed — no other slot touched, verified via jq not eyeball (AC-4). Full before/after slot object read directly: `last_fired`/`trigger_id`/`policy_id`/`guaranteed`/`enabled` byte-identical (AC-5). `init.md:94` mirrors the new value; repo-wide grep for the stale cron string returns only historical/archival hits, zero live config (AC-3). Slot count 23→23, valid JSON (AC-6). Frequency unchanged 5/wk, zero CronCreate (AC-7). Re-ran `cowork-schedule-consistency.test.js` myself — 9/9 pass (incl. TC-7, all 23 slots agree). Confirmed the dirty working-tree `cowork-schedule.json` is unrelated to this commit: `git diff -U0 ... | grep -v last_fired` leaves only bare `@@ ... @@` headers, zero content — 100% last_fired runtime churn from the live dispatcher, exactly as po flagged, independently reproduced not just trusted.

Zero `CronCreate` calls issued by this QA pass itself (grep-confirmed at close-out — no `send_telegram`/MCP tool calls made this cycle at all beyond the board write); did not invoke `/cron-cowork-team`, `/cron-detect-loop`, or `/cron-standalone-team`.

VERDICT: 2× APPROVED, DONE_VERIFIED. Moved `task_board.review[]`→`task_board.done_verified[]` for both in one `jq`+`scripts/orch-apply.sh` write (review 261→259, done_verified 7→9, conservation check task_total 777→777, pure lane-move). `.head.active_task_id` was `FIX-DEPTHTHIN-B-GATEWAY-TA-PATH-REWRITE` (unrelated) — left untouched. DJ: `sprint-CADENCE-RATIONALIZATION-20260804-qa.md` §qa-S7..S8.

## cycle-503 · 2026-08-06 · FIX-CI-SIZELINT-SCHEMA-TS-BASELINE-TOLERANCE-377L — APPROVED, DONE_VERIFIED (direct-commit verify, P0 router direct-dispatch bypassing QA-Drain, `review[]` row, `branch:null`)

CI red ~14h/~20 runs, size-lint job, sole offender `schema.ts` (336L baseline, 369L upper tolerance, 377L actual — grown by `609f62800` FIX-MCP-MEMORY-CODE-LEAK init-guard comments). Verified `daaef1d21` (+`9e5220f31` doc line-ref, +`569755a05` memory) on main ancestry. Read the full diff myself, not the commit message: every hunk is comment-only, `_initializedDbs`/`WeakSet` guard logic byte-identical pre/post. `schema.ts` = 361L, `size-lint-justification.sh --check` exit 0. `size-lint-baseline.json` genuinely untouched (3 independent signals: count/entries still 666, `generated_at` pre-dates the task, last-touch commit `22cd084d4` is an ancestor of `daaef1d21~1`) — not just trusting the commit message's "did not run --update" claim. `bun tsc --noEmit` clean, `mock-guard.sh` PASS. Re-ran targeted regression myself: `002-db-schema.test.ts`+`1489-tracked-indicators-dedup.test.ts`+4×`daily-foreign-flow-*.test.ts` = 64 pass/0 fail/217 expect() — exact match to dev's claimed count.

`verification_gate:ci_green_on_subsequent_push` stays OPEN: fix is real but not yet on origin (`gh run list` confirms origin/main HEAD `25111889a` still `failure`) since it hasn't been pushed. Per PUSH-AUTONOMY-1 §3 the push executor is the dev-team chain-mutex session or router-on-instruction — qa does not hold that authority in a direct verify-committed dispatch outside the normal chain. Flagged to PO/router to execute the push and RAW-verify `gh run` green on the new SHA to close the gate and record the ci_red_fingerprint close-out.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.review[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (review 269→268, done_verified 9→10, conservation check task_total 805→805, pure lane-move). `.head.active_task_id` was `UC-CRITIC-HOOKS-ENFORCEMENT` (unrelated) — left `.head` untouched. Also observed: `TE-T17` sits in `qa[]` claimed 2026-08-05T07:48:15Z (~26h) with no live lock (router's own `task_list_held` probe) — 3rd-4th "ratified/claimed but never dispatched" instance today per router; flagging for PO, not actioned here (out of this task's scope). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa.md` §qa-S12.

## cycle-504 · 2026-08-06 · CCATO-MCP-T1-DOMAIN-ENGINE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row claimed by dev-team review-lane QA-Drain, `branch:null`)

T1 of 8 (SPRINT-CCATO-TRUTHGATE-MCP-NATIVE). Verified commit `4d20a76a2` on main ancestry (`git merge-base --is-ancestor`). `git show --stat` matches the row's claimed files exactly: `apps/mcp-server/src/domain/services/narrativeTruthGate/{claimCandidateScanner,verdictClassifier,quarterResolver}.ts` + `apps/mcp-server/src/__tests__/CCATO-MCP-T1-DOMAIN-ENGINE.test.ts` — a pure addition, zero existing files modified, making full-suite regression structurally impossible (confirmed via the diff itself, not the row's prose claim).

Independently re-ran everything rather than trusting the review_note's self-reported "28/28 pass" (`feedback_router_verify_raw_not_badges`): `bun test src/__tests__/CCATO-MCP-T1-DOMAIN-ENGINE.test.ts` → 28 pass/0 fail, exact match. `bun tsc --noEmit` (whole `apps/mcp-server` zone) → 0 errors. DDD grep on the 3 ported domain files → zero `infrastructure`/`application` imports. Security grep → zero `process.env`, zero real secrets (the only "token" hits are `nonTickerTokens` variable-name substrings, not literal tokens). `mock-guard.sh --files` on the 3 production files → PASS exit 0. Read `quarterResolver.ts` + `verdictClassifier.ts` directly (not just the scanner already excerpted in the review_note) — confirmed pure functions, zero I/O, injectable clock (no direct `Date.now()`), no hardcoded literals, matches the brief's byte-faithful-port + DDD-purity claims.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 10→9, done_verified 10→11, conservation check task_total 804→804, pure lane-move). DJ: `sprint-SPRINT-CCATO-TRUTHGATE-MCP-NATIVE-qa.md` §qa-S1 (new sprint journal file — first qa entry against this sprint id).

## cycle-504 · 2026-08-06 · FFLOW-STALE-0723-B-RECHECK-HARNESS — APPROVED, DONE_VERIFIED (direct-commit verify, dev-team Review-Lane QA-Drain spawn, `qa[]` row, `branch:null`)

Commits `a7b3709db`+`e5440901` confirmed on main ancestry. `git show --stat` matched claimed deliverable exactly: `scripts/check-foreign-flow-freshness.sh` (325L NEW) + pointers in `ops/flow/vps.md`, `system-auditor/flow/main.md`, `dev-standards.md` § Script Persistence, `get_market_foreign_flow.md`. Did not trust the row's own `review_note` badge (`feedback_router_verify_raw_not_badges`) — re-ran the script myself: `--self-test` 3/3 (STALE fixture 2026-07-21→exit 2, FRESH fixture→exit 0, Sat weekend-nuance resolves LCTS to preceding Friday→exit 0); live run (no fixture) `verdict=PASS latest_date=2026-08-06 lcts=2026-08-06`→exit 0 — both branches of `verification_gate` demonstrated (non-zero on the historical stale value, zero on today's real fresh data). Read `vnTradingCalendar.ts` directly — `isVnTradingDay`/`shiftDateDays`/`mostRecentTradingDayOnOrBefore` all exist and match the script's `bun -e` call (AC-B6 confirmed at code level, zero hardcoded holidays). `mock-guard.sh` PASS ("No production source files to scan" — `.sh` outside `.ts/.py/.go` scope). No TS touched — `bun test`/`tsc` N/A (Smart-Skip).

AC-B8 wiring specifically checked for real-vs-aspirational: grepped notebook git history and found commit `a0d7b1aad` (2026-07-28 system-auditor tier-2 cycle) actually invoking the harness live inside a real Tier-2 Freshness Sweep, citing genuine stdout (`verdict=PASS latest_date=2026-07-28`) to cross-check an unrelated `sbv_fx` SLA finding — confirms this is an executing cron-cycle gate, not just a written doc pointer. All 8 ACs (B1–B8) verified against live code/output.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 10→9, done_verified 10→11, conservation check task_total 804→804, pure lane-move). `.head.active_task_id` was `UC-CRITIC-HOOKS-ENFORCEMENT` (unrelated) — left `.head` untouched. DJ: `sprint-FFLOW-STALE-0723-qa.md` §qa-S1.

## cycle-505 · 2026-08-06 · FIX-PREDCLAIM-BACKFILL-NULL-CREATIONPRICE — APPROVED, DONE_VERIFIED (direct-commit verify, dev-team Review-Lane QA-Drain, `qa[]` row, `branch:null`; router flagged deadline-timing check)

Router's spawn brief flagged that today (2026-08-06) is past the row title's 2026-07-28 auto-exclusion deadline and asked to check whether the fix actually landed in time. Checked commit dates FIRST, not review_note prose: `964355605`+`499c28f25` both on main ancestry, dated 2026-07-25 14:5x CEST — 3 days BEFORE the 07-28T16:35Z tick. This is a late QA-drain *claim* of an already-timely fix, not a late fix.

Re-ran independently: `bun test scripts/migrations/__tests__/backfill-predclaim-creation-price.test.ts` → 14 pass/0 fail. `mock-guard.sh` PASS. No process.env/secrets/infra-application imports. `tsc` structurally N/A — `scripts/` sits outside every tsconfig `include[]` (root + apps/mcp-server), same as 5+ pre-existing `scripts/migrations` test pairs using the identical `.ts`-suffixed import style. LIVE RAW-verify (not self-report, `feedback_router_verify_raw_not_badges`): `GET /api/prediction-claims` (localhost:3000) — ids 13/14/15/16/17 all non-null `creationPrice` exactly matching claimed values (217300/202100/214000/213100/58900), AND all 5 have since resolved with real outcomes (correct/wrong/wrong/correct/wrong) — none converted to excluded. Cross-checked `daily_ohlcv` directly via `docker exec bun` query on VIC/VNM for those exact dates — bar closes match exactly, no fabrication. The 6 already-excluded rows (1,8,9,10,11,12) correctly remain untouched per PO's 2026-07-25 scope-narrowing ruling (deliverable (a) only). `calibration.total` now 33 vs review_note's "held at 17" — organic growth (16 new claims minted since 07-25), `resolved`/`excluded` both unchanged — not a dropped row.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 8→7, done_verified 5→6, conservation check task_total 805→805, pure lane-move). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa.md` §qa-S13.
