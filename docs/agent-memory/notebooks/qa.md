# QA — Notebook

## cycle-399 · 2026-07-08 · FACTORY-DOMAIN-extract-bctc-parsing-lib — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (Steps 1-4 commits e3e0dfeb9/26f4f310f). dev-mcp-server extracted shared BCTC parsing primitives (`lib/lineScan.ts` canonical `findValue`/`findValueByCode`/`scaleNumericFields`/`detectDivisor`, `lib/vasPatterns.ts` shared VAS regex) and migrated `balanceSheetExtractor`/`cashFlowExtractor`/`incomeStatementExtractor`/`bctcScalarAggregator` one at a time; `bctcMagnitudeValidator` deliberately left untouched. Named risk: `findValue` unifies a 2-arg row-code-guard variant (balanceSheetExtractor) and a 4-arg ASCII-fallback variant (income/cashFlow) into one opt-in `{rowCodeGuard, fallback}` options object. SHA-gate: `scripts/verify-deploy-sha.sh mcp-server` reported drift (deployed=dd37ec6c3 vs HEAD=26f4f310f) — confirmed BENIGN not a Step 4 failure: `git diff --stat dd37ec6c3..26f4f310f` is docs-only (1 memory doc + orch-state.json), and `git merge-base --is-ancestor 4f15363da(last code commit) dd37ec6c3(deployed)` holds — all 5 code commits are ancestors of the deployed image. Did NOT trust the SHA label or review_note alone: `docker cp`'d all 7 touched/created files out of the live container and diffed byte-for-byte against host HEAD — ALL byte-identical (Bun runs src/*.ts directly, no dist build). `/health` toolCount=183 matches baseline, container Up (healthy). Independently re-ran the BCTC-domain suite myself (grep -rl over the 7 module names across `src/__tests__`, 40 files found — close to review_note's claimed 42): 414 pass/4 skip/0 fail; `bun tsc --noEmit` clean.

Went beyond the task's own suggested checklist (real-data A/B, not just re-running the existing test suite): `git worktree add --detach` at `cb3c1ebe0` (immediate parent of first migration commit `7f65dfccd` — genuine pre-migration baseline) vs current HEAD. Pulled REAL inputs off a `docker cp` copy of the live named-volume `market.db`: (1) FPT 2025-Q4 (report_id e71f845d, non-bank) — concatenated 27 non-quarantined `bctc_layout_units.stitched_markdown` pages as rawText, ran `extractBalanceSheet`+`extractIncomeStatement`+`extractCashFlow` in both worktrees — stdout JSON AND stderr guard/warning logs byte-identical (md5 match), including the FIX-BCTC-MAGNITUDE-NORMALIZE override and extractorGuards rejections firing identically; (2) VCB 2025-Q4 (report_id bdcfa5e0, bank-form) via `bctc_refined_units.markdown` — byte-identical output, confirms the `rowCodeGuard:true` path on real bank data too; (3) `aggregateScalars` (detectDivisor relocation) against real `bctc_table_rows` for FPT 2026-Q1 (145 rows) and CTG 2026-Q1 (96e36139, bank, 451 rows) — both byte-identical `ScalarAggregateResult` JSON. `bctcMagnitudeValidator`: `git diff cb3c1ebe0..HEAD` on this file is a pure 13-line comment insertion (0 lines changed/deleted, 0 new imports) — zero logic/import-path change confirmed at the diff level, not just by test pass; independently re-ran `bctcMagnitudeValidator.test.ts` + `FIX-BCTC-MAGNITUDE-NORMALIZE.test.ts` standalone: 32/32 pass. Cleaned up both harness scripts from the working tree and worktree, removed the git worktree (`git worktree remove --force`) — confirmed `git status --short` clean under `apps/mcp-server/`.

VERDICT: pure-relocation claim holds on every axis (SHA-gate ancestry, live byte-identical deployed source×7 files, independent test re-run, tsc, and real-data A/B on 2 non-bank + 2 bank reports covering all 3 statement extractors + the aggregator, plus diff-level confirmation the untouched validator has zero logic change). Did NOT flip to `DONE_VERIFIED` — appended findings to the existing `review_note`, moved `next_agent` qa→po (status held REVIEW) via new `scripts/qa-factory-domain-extract-bctc-parsing-lib-hold-review.jq` + `scripts/orch-apply.sh` (guarded on review[]+status+next_agent+head.active_task_id), `.head.next_agent` synced to `po` in the same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S20.

## cycle-398 · 2026-07-08 · FACTORY-INTERFACE-extract-finalizeBctc-usecase — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (commit db2eddb75, deployed SHA f9ed7f850). Task's own DoD explicitly required MORE than liveness (confirm finalize_bctc_refine's actual output unchanged, not just HTTP 200). Health: `/health` toolCount=183 matches baseline; container Up (healthy). `verify-deploy-sha.sh` reported drift (deployed=f9ed7f850 vs current HEAD=c2ed1b2e8) — confirmed BENIGN: `git diff --stat f9ed7f850..c2ed1b2e8 -- apps/mcp-server/` empty (2 intervening commits are ops's own docs-only board-flip + DJ-journal). `docker cp`'d all 8 touched files (interface tool + 7 new `application/usecases/finalizeBctcRefine/*.ts`) out of the live container and diffed byte-for-byte against host HEAD — ALL byte-identical. Independently re-ran the claimed 22-file/293-test targeted BCTC suite myself: 293 pass/0 fail, exact match; `bun tsc --noEmit` clean. Invoked `finalize_bctc_refine` live inside the container against a real report_id (FPT 2025-Q4, refine_status=DONE, no human corrections) — completed end-to-end with no crash, `{ok:true, rows_parsed:128, effective_status:"DONE"}`; extraction_confidence bumped 0.8125→1.0, bctc_table_rows 127→128. Rather than accept that delta as ambiguous, ran a rigorous isolated A/B: `git worktree add --detach` at `56ee74725^` (immediate pre-extraction commit), checkpointed a clean scratch copy of the pre-invocation live DB into two identical files, ran the OLD monolithic handler against one and the NEW `runFinalizeBctcRefine` (current HEAD) against the other — both produced byte-identical results; diffed the two resulting DBs' `bctc_table_rows`/`financial_reports`/`bctc_eval_results` — IDENTICAL in every field except `computed_at`. **This proves the 127→128 row delta and confidence bump are NOT caused by this task's relocation** — both pre- and post-extraction code reproduce them identically; root-caused to `refinedMarkdownParser.ts` (untouched, not in the 8-file scope) emitting one extra markdown-table-header artifact row, and BLOCK-5's pre-existing no-downgrade override (shipped before this task) simply firing for the first time on this report. `bctc_eval_results` stage 5 (MARKDOWN_RENDER) stayed red before/after (pre-existing `bctc_md_tables` data-quality gap, unrelated). Cleaned up: removed `/app/qa-raw-verify-finalize.ts` from container, removed scratch `apps/mcp-server/ab-new-run.ts` from working tree, removed the git worktree — confirmed clean. Per runbook delegation rule and explicit dispatch instruction not to repeat the FACTORY-SCHEDULER-alert-confidence-literals self-close deviation, did NOT flip to `DONE_VERIFIED` — moved the row `task_board.qa[]`→`task_board.review[]` (status held REVIEW), `next_agent` qa→po, via new `scripts/qa-factory-interface-extract-finalizebctc-usecase-hold-review.jq` + `scripts/orch-apply.sh`, `.head` synced to `status:"review"`/`next_agent:"po"` in the same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S19.

## cycle-397 · 2026-07-08 · FACTORY-SCHEDULER-prediction-default-dedup — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (commit bf35b2999 board-flip; deployed SHA 9dd0260...). Health: GET /health toolCount=183, matches docs/data/project-stats.json baseline; container "Up (healthy)". Re-ran `scripts/verify-deploy-sha.sh mcp-server` myself: reports drift (deployed=9dd0260 vs HEAD=bf35b2999) — confirmed BENIGN, not a Step 4 failure: `git show bf35b2999 --stat` touches ONLY orch-state.json (ops's own docs-only board-flip advancing HEAD after the gate check), and `git merge-base --is-ancestor 5310ce0fb 9dd0260` confirms the actual code commit IS an ancestor of the deployed SHA with zero intervening app-code commits. Did NOT trust the SHA label alone (or dev/ops self-reports): `docker cp`'d the ACTUAL executing source files out of the live container (Bun runs src/*.ts directly, no dist build) and diffed byte-for-byte against host HEAD — `predictionMarketJob.ts`, `infrastructure/config.ts`, and `mcp.config.json` all BYTE-IDENTICAL (config confirms predictionMarkets.{volumeSpikeThresholdUsd:50000, probabilityShiftPct:5, minUniqueWallets:10}, no override, matching DEFAULT_PREDICTION_SIGNAL_CONFIG field-for-field). Read the full call chain myself: `startScheduler.ts:323` calls `runPredictionMarketPoll()` with ZERO args → `opts.signalConfig` undefined → try branch → `resolvePredictionSignalConfig(pm)` — confirms the exact deduped path is what actually executes on every real cron fire, not a hypothetical. Independently re-ran the new 7-test file myself (not trusted from review_note): 7 pass/0 fail, reproduced dev-mcp-server's claimed count exactly; `bun tsc --noEmit` clean, also reproduced myself. **Caught a genuine live natural cron fire** (no manual trigger needed, stronger evidence than a forced invocation): `cronConfig.ts` predictionMarketPoll=`*/30 * * * *`, deploy landed 11:14:23Z — ran a background poll (not a guessed sleep) until the next `:30` boundary, then re-pulled `docker logs` + re-`docker cp`'d the live `market.db`+`-wal`+`-shm` and queried with local sqlite3 (container has no sqlite3 binary). Job fired naturally at 2026-07-08T11:30:01.811Z: `fetched 6 markets` / `detected 0 signals` / `cycle complete — markets: 6, signals: 0, high: 0, critical: 0, notified: 0`. RAW DB query: 0 new `prediction_signals` rows since deploy (COUNT(*) still 7, most recent detected_at unchanged) — a legitimate quiet-market data fact (this table has produced only 7 rows total across ~3 months, 0-signal cycles are the historical norm), not a defect: the "detected 0 signals" log line only fires AFTER `detectPredictionSignals(...)` returns using the confirmed-identical `signalConfig`, proving the deduped try-branch resolution executed successfully end-to-end with no crash against real, quiet Polymarket data. VERDICT: pure-refactor claim holds at every layer (unit test, byte-identical deployed source×3 files, live call-chain trace, live natural cron fire, zero DB drift). Per runbook delegation rule and explicit dispatch instruction (do NOT repeat the FACTORY-SCHEDULER-alert-confidence-literals self-close deviation — 2nd occurrence = recurring bug per po's own escalation trigger), did NOT flip to `DONE_VERIFIED` — appended findings to the existing `review_note`, moved `next_agent` qa→po (status held REVIEW) via new `scripts/qa-factory-scheduler-prediction-default-dedup-hold-review.jq` + `scripts/orch-apply.sh`, `.head.next_agent` synced to `po` in the same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S18.

**Retention note:** cycle-396 (FACTORY-SCHEDULER-alert-confidence-literals — DONE_VERIFIED, manual live-trigger of 3 real alert jobs confirmed 3 distinct varying confidence values replacing the old flat literal, dead-code concern self-raised+resolved via alertScanParallelJob trace) and cycle-395 (FACTORY-TECHANALYSIS-reconcile-ta-contract — HELD REVIEW, TA Go-service contract RAW-verify: openapi.yaml field-by-field cross-check, 4 live probes + /health, non-blocking doc-trail gap flagged) and cycle-394 (FACTORY-TECHANALYSIS-go-livepath-tests — DONE_VERIFIED, independent re-verify of Go test-coverage backfill: go test/vet/lint clean, both new test files read against production usecases.go/router.go confirming genuine non-tautological assertions, sandbox 35/35 + headless render PASS, doc self-heal reproduced firsthand) and cycle-393 (FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE — DONE_VERIFIED, independent re-verify of a jq BOUNDED-1 depends_on eligibility gate: candidate-list filter placement, effective_depends_on dual-shape resolution, 17/17 own test run, own live read-only dry-run reproduction against real orch-state+backlog-detail) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file (which also carries cycle-392: FACTORY-MACRO-delete-dead-ts-tree DONE_VERIFIED / cycle-391: FACTORY-PDF-paddleocr-score-07-mask DONE_VERIFIED / cycle-390: FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED DONE_VERIFIED / cycle-389: F1-LAUNCHD-COWORK-BACKSTOP DONE_VERIFIED / cycle-388: FACTORY-INTERFACE-source-confidence-10-mask DONE_VERIFIED / cycle-387: FACTORY-INTERFACE-source-confidence-10-mask QA-APPROVED HELD REVIEW / cycle-386: FACTORY-INTERFACE-sequential-confidence-05-mask DONE_VERIFIED / cycle-385: FACTORY-INTERFACE-sequential-confidence-05-mask QA-APPROVED HELD REVIEW / cycle-384: CONTAM-10-WRITER-H DONE_VERIFIED / cycle-383: CONTAM-10-WRITER-H HELD REVIEW / cycle-382: PDF-TEST-01-FIX HELD/BLOCKED / cycle-381: FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP APPROVED / cycle-380: KD-OBS-01-FIX APPROVED / cycle-379: CI-RED-c5b5f885-FIX APPROVED / cycle-378: FACTORY-ALERT-consolidate-dual-engines APPROVED / cycle-377: B-05-FU-SSC-503-RETRY PASS / cycle-376: FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK APPROVED / cycle-375: FIX-ALERT-COMMANDER-DEAD-NO-SLOT PASS / cycle-374: FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD PASS / cycle-373: FIX-FB-DAILY-FIRER-CLI-FLAG APPROVED / cycle-372: FIX-BCTC-BANK-BS-COLUMN-ORDER PASS / cycle-371: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
