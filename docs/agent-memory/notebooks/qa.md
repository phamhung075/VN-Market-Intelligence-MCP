# QA — Notebook

## cycle-706 · 2026-08-14T03:01:57Z · FIX-PDFX-EXTRACTION-ENGINE-EMPTY-STRING-SWALLOW (report-analyzer, commit `200eabcf3`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — `_ocr_page()`'s generic `except Exception: return ""` was swallowing genuine OCR failures (tesseract crash, corrupt raster) into a hollow low-quality-success shape, byte-indistinguishable from a real sparse/blank page. Ran everything myself inside the ACTUAL deployed container (`vn-market-intelligence-mcp-pdf-extractor-1`), not host Python — `/app` md5-byte-matched the repo working tree, confirming the running service already serves this commit.

Commit `200eabcf3` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 4 claimed files (`domain/errors.py`, `infrastructure/extraction_engine.py`, + 2 test files). AC1: traced the full propagation chain at source, not narration — new `OcrPageFailedError(PDFProcessingError)` raised by `_ocr_page()`, re-propagated (not re-swallowed) through `_extract_text_ocr_sync`'s own except tuple, caught by `services.py:109`'s `except PDFProcessingError` which sets `doc.status="failed"` and re-raises BEFORE `store_extraction()` at line 100 (never reached) — then `usecases.py:89-91` does NOT re-raise it (unlike Capacity/Deadline, which do for 429/503), so it falls into a clean `status="failed"` response DTO. Confirmed zero diff lines outside the 4 files — the "zero services/usecases/HTTP-layer changes" claim holds exactly, not by trust.

AC2: independently re-ran the 4 negative-control tests (OCR-succeeds-empty, OCR-succeeds-whitespace, blank-page-degrades-confidence-not-raise, blank-page-among-others-does-not-abort) — all PASS, confirmed a genuinely blank page still returns `""` with no raise, a distinct code path from the new raise branch.

AC3: targeted re-run (12 new failure-swallow tests + 2 new extract_pdf_service tests + `test_ocr_concurrency_invariant.py` + `test_extraction_engine_nonblocking.py`) = 35/35 PASS. Full suite: 1047 passed / 1 failed / 11 skipped (1059 collected — matches the claimed 1056+3=1059 total; disposition of the 3 "pre-existing env-only" failures shifted to 1 failure + skips because the long-running container's environment (has pandas, has the PDF fixture) differs from whatever the developer ran at commit-time — immaterial, total collected count is stable). The 1 failure (`test_bt3_fix3_real_ocr_fidelity`, AC-12 row-count off-by-one, 79 vs required [80,110]) independently confirmed UNRELATED to this diff: zero import/code overlap (uses `ocr_adapter.py`/`text_table_extractor.py`, a completely different extraction pipeline than `PdfplumberExtractionEngine`), predates this commit by 2 months (introduced 2026-05-26, commit `8dbb19e3d`), marked `@pytest.mark.slow` with its own docstring warning of real-Tesseract non-determinism — and reproduced FAILING on the PRE-FIX source too via a disposable `docker run --rm` with the old `errors.py`/`extraction_engine.py` bind-mounted over the image (isolated from the live container, no risk to the running service). Non-regression proven by bisect, not assumed by "different file" hand-waving.

Falsification independently reproduced (same disposable pre-fix container): the new test module fails COLLECTION with `ImportError: cannot import name 'OcrPageFailedError' from 'domain.errors'` — confirms the fix is load-bearing, matching the dev's claim exactly. `lint-imports --config pyproject.toml` re-run myself: Fence-A/B/C all KEPT, 3/0. `mock-guard.sh --files domain/errors.py infrastructure/extraction_engine.py` → PASS. `scope_out` respected: `ocr_gateway.py`/semaphore/deadline mechanism has 0 diff lines.

DJ-GATE-1: developer's own journal confirmed present, `sprint-FIX-PDFX-EXTRACTION-ENGINE-EMPTY-STRING-SWALLOW-dev-pdf-extractor.md`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE (1 orthogonal pre-existing full-suite failure flagged non-blocking, isolated to an unrelated pipeline and proven pre-existing via bisect — worth its own fleet housekeeping row but does not gate this task). `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 735=735). `.head.active_task_id` was NOT this task (a different row, `FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING`) — left untouched per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b), no idle-sync applicable. This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-PDFX-EXTRACTION-ENGINE-EMPTY-STRING-SWALLOW` and is the authoritative release path.

## cycle-705 · 2026-08-14T01:33:11Z · FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES (report-analyzer, commit `761988143`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — the detector's only non-zero-diff DETECTED path sourced its evidence from the notebook commit it was checking, so a genuinely non-zero OTHER plane (signal_queue row, ledger entry) with the notebook plane silently missing defeated `all_zero` and PASSed a defective cycle (confirmed live 12x). Did not trust the developer's pasted 15/15 count or the router's own independent-run note — re-ran everything myself.

Commit `761988143` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook side-files. `bash scripts/audits/detect-analysis-only-exit.test.sh` own re-run: 15/15 PASS.

Read the core diff at source, not narration: the new MANDATORY-PLANE CHECK reads `docs/agents/<id>/init.md`'s `memory.notebook` field (`none` → not mandatory, else → mandatory), never the artifact under test — satisfies AC-2's "never key on the agent's own claim" carry-forward from the parent row. AC-1's naive `all_zero`→`any_zero` widening is explicitly rejected in the diff in favor of this per-plane subset. Grep-swept `docs/agents/*/init.md` myself (not trusting the dev's "only refine_bctc_md" claim) — confirmed only `refine_bctc_md` declares `notebook: none`; every other fleet agent defaults to mandatory=1, so the conservative default does not mass-false-positive the fleet.

Re-ran BOTH historical repros named in `verification_gate` live, not from prose: system-auditor `--since-ts 2026-08-12T05:35:00Z --until-ts 2026-08-12T05:55:00Z` → DETECTED (`all_zero`+`mandatory_status=violation`; that signal_queue row has since drained per the developer's own honest note, so it no longer isolates the mechanism alone, but the detector still correctly fires). news-scout `--since-ts 2026-08-07T03:55:00Z --until-ts 2026-08-07T04:30:00Z` → DETECTED `mandatory_status=violation` (`ledger=1`/`notebook=0`) — flipped from the pre-fix live PASS documented in the row's own evidence, the exact inverse-partial shape this row exists to close.

AC-3 regression check (own diff, not trusted): T8-T12 (the c80 partial-write contract fixtures) show zero lines changed in the commit diff — confirmed non-regression. AC-4: RED-1 (T2, signal_queue>0/notebook=0), RED-2 (T4, ledger>0/notebook=0), GREEN (T14, no-notebook-mandate agent) all present and green; T6 doubles as AC-5's `--cycle-tag` investigation, empirically proving the identical blind spot closed by the same fix. `shellcheck -x -e SC1091` 0 warnings both changed `.sh` files. `mock-guard.sh` correctly N/A — zone is pure bash+md, no TS/JS production source touched, `bun test`/`tsc --noEmit` N/A.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP `developer-S104`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live repo. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.
