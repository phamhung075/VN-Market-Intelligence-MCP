# QA — Notebook

## cycle-549 · 2026-08-06 · TE-T14 — CHANGES_REQUESTED (verify-committed, `qa[]` row, `branch:null`, `.commit_sha: 013c90710`)

Commit `013c90710` confirmed real + on main ancestry, `git show --stat` matches — touches only `docs/agents/system-auditor/flow/main.md` Step 0c (+ own agent-father decision journal/notebook, no board files). Live-ran the row's own new jq projection against `docs/data/system-map.json`: exit 0, counts 11/12/0/28/7/12 match `review_note` exactly — the projection is internally correct for the 6 paths it names.

BUT `review_note`'s central claim ("no semantic change to what system-auditor consumes") does not hold: its own audit method only grepped OTHER files for dependence on the old prose shape, never checked THIS file's own downstream consumption beyond the 6 bulleted key-paths. Grepping `main.md` itself for every `system-map.json` reference surfaced 2 real in-file consumers the new projection silently drops: (1) `main.md:297` Cron Fire Check (A-29) iterates `microservices[0].crons` — a live ~70-entry array, entirely absent from the new `{id, external_port, zone}` selector; (2) `main.md:325-339` SLA Resolver (SSOT: `system-map.json .project.data_sources[].sla`, FIX-BCTC-SLA-THRESHOLD-360) needs `.sla.mode`/`.sla.earnings_window.*` per source, dropped from the new `{id, expected_cadence_hours, stale_threshold_hours, geo_blocked}` selector — live-verified 2/28 sources (`bctc-discover`, `bctc-push`) carry an `sla` block, flat `stale_threshold_hours=168h` vs the sla-resolved out-of-window effective threshold up to ~1714h (per the file's own worked examples, lines 344-349). If Step 0c's jq output becomes the agent's sole in-cycle view of `system-map.json` (exactly its stated purpose), losing `sla` reintroduces the exact false-CRITICAL B-05 bug FIX-BCTC-SLA-THRESHOLD-360 already fixed once, and A-29 loses its entire cron list. Root cause: the OLD step did an imperative full-Read, so these fields stayed implicitly present even though never bulleted; the NEW jq projection actively strips anything unselected — a real behavior change, not a doc nit.

No application code touched (docs/agents/ + docs/agent-memory/ only) — `bun test`/`tsc`/`mock-guard` structurally N/A (Smart-Skip, doc-only diff).

VERDICT: CHANGES_REQUESTED. Moved `task_board.qa[]`→`task_board.review[]` via `jq`+`scripts/orch-apply.sh`, `redispatch_count: 0→1`, `status_note` appended with the 2 file:line gaps + fix options (add `crons`/`sla` to the projection, or explicitly scope down the "6 key-paths"/"no semantic change" claim). `next_agent: agent-father` (row's own `.owner` — no task branch exists for a fixer; owner must apply a new direct commit). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S3. No gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — flagging to the calling dispatcher (coordination session f298ccf7-8cf4-452d-9a5a-57dcb47e65ac) to release `task:TE-T14` itself. NOTEBOOK RETENTION: file over the 3-section AC-2 cap under confirmed heavy concurrent sibling QA-drain write pressure (same disposition as the 3 immediately-preceding entries) — appended via targeted Edit anchored on the exact prior last line; pruning deferred to a later single-writer cycle.

## cycle-549 · 2026-08-06 · FIX-CI-SIZELINT-TECHANALYSIS-ROUTER-NEW-OFFENDER-143L — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, `.commit_sha` on row: `969b5d96b`)

Commit `969b5d96b` confirmed real + on main ancestry (`git merge-base --is-ancestor`). `git show --stat` matches `files[]` exactly: `router.go` -29L, new `health_handler.go` +41L, no other files touched. Read both files live, not trusted from commit prose: `health_handler.go` holds `defaultPort`/`healthResponse`/`handleHealth` exactly as claimed, mirrors the sibling `*_handler.go` file-per-handler convention (AC-1); `router.go` still calls `handleHealth(port)` at the same call site with the `defaultPort` fallback via `strconv.Atoi(cfg.Port)` unchanged (AC-2 confirmed by source read, not prose). `go build`/`go vet` clean; `go test ./...` all 12 packages pass; `TestHealth_Returns200`+`TestHealth_ReflectsConfiguredPort` re-run fresh (`-count=1`, non-cached) PASS; `golangci-lint run ./...` 0 issues (AC-3).

`router.go` absent from `size-lint-justification.sh --check`'s offender list, confirmed twice (AC-4 for this row's own file scope) — repo-wide `--check` still fails on 4 UNRELATED offenders (`schema.ts`/`getBctcRefinedTool.ts`/`app_factory.py`/`embedder.py`, mcp-server+rag-service) that this SAME cycle's sibling sub-agent (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S1) independently flagged on a different row as pre-existing drift from other work landed after 2026-07-31 — corroborated, not this fix's regression, out of this row's `files[]` scope. No `size-lint-baseline.json` grandfather entry added for `router.go`, no `--update` run, no justification header (AC-6 landmine avoided). AC-5 independently re-verified on the CI plane myself: `gh run view 30672400942` → headSha `d93e4fbc17862f9d65720fe1bc6424b5913c8369` (exact match to the row's own `status_note` claim), `size-lint` job `conclusion=success`; confirmed `969b5d96b` is an ancestor of that headSha and that headSha is an ancestor of `main`. `mock-guard.sh --files` (both touched files) → PASS. DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-technical-analysis.md` STEP dev-technical-analysis-S3, task-id stamped (2026-08-01T01:15:00Z), predates this verify.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (fresh re-read immediately before write — conservation OK task_total 761→761, signal_total 203→203). `next_agent: pm`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S2. Task lock claimed under this dispatcher's coordination session f298ccf7-8cf4-452d-9a5a-57dcb47e65ac — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); flagging to the calling dispatcher to release `task:FIX-CI-SIZELINT-TECHANALYSIS-ROUTER-NEW-OFFENDER-143L` itself. NOTEBOOK RETENTION: same disposition as the immediately-preceding entries — file now 6 sections under confirmed heavy concurrent-write pressure from sibling QA-drain sub-agents on the same dispatcher session; appended via targeted Edit anchored on the exact prior last line to avoid a lost-update race; pruning deferred to a later single-writer cycle.

## cycle-550 · 2026-08-06 · FIX-COWORK-FLOWDOC-STALE-TRANSPORT-GAP-CAVEAT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain spawn)

Commit `438e66958` confirmed real + on main ancestry, `git show --stat` matches exactly the 4 claimed files (3 flow docs + agent-father journal). Re-derived AC-1/AC-4 myself, not trusted from dev-team's review_note: repo-wide grep for TRANSPORT GAP/no-transport/holds-no-Bash across `market-watcher/flow/cycle.md` + both `news-scout/flow/stage-*.md` → zero matches; `git show --stat 438e66958 -- '.claude/agents/*.md'` → empty (AC-4 negative control). Read the full diff: both Step-0 list-stale caveats and both Step-5c/Step-7 write caveats now correctly gate the hand-filter/skip fallback on a genuine Bash-invocation error, not the stale tool-grant premise; live-checked the fix's own cited lines (`market-watcher.md:5`, `news-scout.md:5`) — both confirm `Bash` present, citations accurate.

Independent finding, not this row's scope but flagged not buried: AC-2/AC-3 (coverage-state.json un-freezing) still false today, 5 days post-commit. Live-reproduced why myself — ran `coverage-stamp.sh` directly, it fails deterministically (`task_claim ttl_seconds=30` violates the MCP tool's own `minimum:60` schema, "fail-CLOSED, no write performed" every attempt), matching market-watcher's own TODAY 20:06Z cycle log `coverage_state_updated: no [script-transport-error]`. Cross-checked project memory (`project_stale_transport_gap_note_blocks_coverage_state_writes.md`) — this exact TTL-validation failure mode is the ALREADY-TRACKED sibling `FIX-COVERAGE-STATE-CROSS-AGENT-LOST-UPDATE` (P1, BACKLOG, `next_agent:ba` since 2026-07-16, scope explicitly widened 2026-08-01 for precisely this mutual-failure/TTL mode) — confirmed via this commit's own diff that it never touches the mutex/ttl description text, so not a regression introduced here. This row correctly removed the false premise so the real, already-owned bug could surface instead of staying masked (topic≠mechanism discipline — did not fold a different defect into this verdict).

No production TS/Go touched (docs + journal only) — `bun test`/`tsc`/DDD-grep/`mock-guard.sh --files` structurally N/A (`mock-guard` confirmed "No production source files to scan. PASS."). DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-agent-father.md:374` task-id-stamped, predates this verify.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (fresh re-read immediately before write — conservation OK task_total 763→763, signal_total 203→203). `next_agent: pm`. Appended the AC-2/AC-3 finding to the row's own `status_note` (audit trail, not blocking) rather than routing back to `owner`/CHANGES_REQUESTED — outcome verified true for this row's actual scope, only the downstream un-freeze depends on a different, already-tracked row. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S4. Task lock claimed under this dispatcher's coordination session f298ccf7-8cf4-452d-9a5a-57dcb47e65ac — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); flagging to the calling dispatcher to release `task:FIX-COWORK-FLOWDOC-STALE-TRANSPORT-GAP-CAVEAT` itself. NOTEBOOK RETENTION: same disposition as the immediately-preceding entries — file now 7 sections under confirmed heavy concurrent-write pressure from sibling QA-drain sub-agents on the same dispatcher session; appended via targeted Edit anchored on the exact prior last line to avoid a lost-update race; pruning deferred to a later single-writer cycle.
