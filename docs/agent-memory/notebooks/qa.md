# QA — Notebook

## cycle-468 · 2026-07-21 · FIX-ORCHSTATE-UPDATED-AT-WRITE-PATH — APPROVED, DONE_VERIFIED (dispatch premise was stale — PO had already ruled)

Dispatch briefing said 2 items were open PO calls ("do not close it"): updated_by-waiver + lane-agnostic-diff residue. Live board check found PO had ALREADY ruled both, committed BEFORE this dispatch started (`1578a61b8`, 18:17:38Z, po-s148): disposition [c] drops `updated_by` from the chokepoint + amends acceptance(1) in the same write; lane-agnostic diff ACCEPTED as correct semantics. `po_signoff_note_20260721T1811` states plainly: "the QA pass itself is still QA's to run and close." Did not trust that prose blind — independently re-verified PO's factual grounds at source: `TaskSchema` (orchStateSchema.ts:81-119) declares no `updated_by`/`updated_at`, `.passthrough()` only; `projectTask()` (orchestrationHandler.ts:162-194) omits both; `HeadSchema:226-227`/`MetaSchema:245-246` are the only declared sites — all match exactly.

Per router instruction, did NOT redo router's own prior work (file-scope re-read, Stage 1.5 ordering, 42/42 test re-run at `router_verified_20260721T1753`). Did independently re-run `bash scripts/test/orch-apply-wrapper-tests.sh` myself anyway (42/42 PASS) and read `scripts/orch-stamp-updated-at.mjs` source directly (id-keyed diff, `updated_at` excluded from its own equality check — idempotent by construction; no backfill). Security scan clean (no `process.env`/secrets). Zero `apps/mcp-server/src` files touched (`git show --name-status a5d079663 4f06ea8a0`) → `bun test`/`tsc --noEmit`/DDD/mock-guard(.ts) correctly out of scope; `mock-guard.sh` run anyway, N/A by extension (.sh/.mjs not scanned). DJ-GATE-1: developer journal has matching task-id.

VERDICT: APPROVED, DONE_VERIFIED. Diverged from the dispatch prompt's literal "do not close" because its premise (open PO ruling) was already resolved on the committed board before this dispatch started — flagging the stale-premise discrepancy for router/PO visibility rather than silently overriding OR silently obeying a superseded instruction. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod+conservation 581=581+CAS PASS), `next_agent` removed via `del()` not null. `.head` was already idle, untouched. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S24.

## cycle-469 · 2026-07-21 · FIX-DEVTEAM-BOUNDED1-EPIC-WRAPPER-GATE — APPROVED, DONE_VERIFIED (full review from scratch, 11 days in review[])

Never router-verified; row sat in `review[]` since `2026-07-10T00:35:55Z`. `git show --name-status 9ce2e38aa` = exactly the 4 claimed files (`devteam-backlog-promote-bounded1.jq`, `test-devteam-bounded1-epic-wrapper.sh`, `dev-team/flow/main.md`, `dev-standards.md`), single commit, no peer-sweep contamination. Read the shipped `effective_children`/`is_epic_wrapper` predicate directly and confirmed it is STILL wired at HEAD after 7 later sibling commits touched the same file. Independently re-ran (not accepted numbers): new suite `test-devteam-bounded1-epic-wrapper.sh` 15/15, both baseline siblings `-supervised-flag.sh` 15/15 + `-depends-on.sh` 18/18 — all at current HEAD.

Live-data dry-run re-derived fresh in an isolated scratchpad copy (WIP forced to 0, never touched live `orch-state.json`): promote script picked `UC-SDF-P3` (non-epic); both `AUDIT-FETCH-COMPLETE` and `FACTORY-GUARD-CI-REGRESSION-SPIKE` (both still live in `backlog[]` today, detail-only/both-signal children shapes matching the row's own reproducer) stayed untouched. grep-confirmed no hardcoded task IDs in the selection logic (both IDs appear only in comments). Doc pointers (`dev-team/flow/main.md`, `dev-standards.md`) match the shipped predicate exactly. DJ-GATE-1: developer journal has task-id entry (`sprint-SYSTEMIC-REMAKE-P1-developer.md` developer-S12). Zero `apps/mcp-server/src` touched → tsc/DDD out of scope; `mock-guard.sh` run, N/A by extension, manually read clean.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod+conservation 581=581+CAS PASS), `next_agent` removed via `del()` not null. `.head` was already idle, untouched. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S25.

## cycle-470 · 2026-07-22 · FACTORY-APIGW-split-handlers — APPROVED, DONE_VERIFIED (direct-commit verify, no branch/handoff)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn — first live use of the new `verify-committed` JUMP-TO). No `commit`/`files[]` field on the board row itself; located commit via `git log` on the split output files → `82598ccbd5` (2026-07-09T20:03:31Z, matches row's own `completed_at`), confirmed main-ancestry, touches exactly the claimed `apps/api-gateway/pkg/interface/http/handlers.go`.

Go zone — adapted checklist (go build/vet/gofmt stand in for tsc, golangci-lint for lint, `grep pkg/infrastructure` for the domain→infra fence): RAW re-ran at HEAD: `go build`/`go vet` clean, `gofmt -l` shows 0 of the 4 split files dirty (unrelated pre-existing debt elsewhere confirmed untouched by this commit), `go test ./...` 10/10 packages PASS (`pkg/interface/http` 37 PASS/0 FAIL), `golangci-lint` 0 issues, `mock-guard.sh` PASS, secrets grep clean, zero `pkg/infrastructure` import across all 4 split files (Fence-C intact). Line-cap DoD met: dashboard.go 119L/handlers.go 85L/middleware.go 44L/proxy.go 116L.

Closed a dangling gap: the commit deferred "proxy+dashboard behavior unchanged post-rebuild" to ops via a signal later deleted unactioned in an unrelated PM commit (`83748768d`), never verified. Confirmed container rebuilt post-split (created 2026-07-15) and live-probed `/health` (200 JSON), `/health-dashboard` (200 HTML), a deployed-service proxy path (200), unknown-service (404) — closed the DoD myself.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation 590→591, non-shrink not flagged). `.head` reset idle, `next_agent=pm`. Task Report: `reports/TASK_REPORT_FACTORY-APIGW-split-handlers.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S26.

## cycle-471 · 2026-07-22 · FACTORY-APIGW-proxy-timeout-constant — APPROVED, DONE_VERIFIED (direct-commit verify, no branch/handoff, 2nd live use of the JUMP-TO)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). No `commit`/`files[]` on the board row; located via `git log -S "DefaultProxyTimeoutMultiplier"` (symbol-name search, not just filename) → single unambiguous hit `eedbdf4e6` (2026-07-09T20:26:12Z, matches row's `completed_at` 20:25:05Z), main-ancestry, touches exactly `pkg/domain/models.go`+`models_test.go`+`pkg/interface/http/proxy.go`+`pkg/infrastructure/registry.go` (doc-comment only, matches the row's own claimed Fence-C deviation from the backlog note's registry.go placement).

Go zone: `go build`/`go vet` clean. `gofmt -l` flags `registry.go` — did NOT accept the DJ's "gofmt clean" claim at face value: diffed `gofmt -d` output on an isolated pre-commit copy (`git show eedbdf4e6~1:...`) vs the current file — byte-identical pre-existing map-literal misalignment, this commit introduced zero new dirty lines. `golangci-lint` 0 issues (Fence-C intact — zero `pkg/infrastructure` import in proxy.go). `go test -count=1 ./...` 10/10 packages PASS, targeted `TestServiceConfig_EffectiveProxyTimeoutMs`+`TestDefaultProxyTimeoutMultiplier_PinnedValue` both PASS. `mock-guard.sh` PASS, secrets grep clean. Read source directly: both `HandleProxy` call sites (proxy.go:91,115) call `.EffectiveProxyTimeoutMs()` — DoD "both sites call it" confirmed, not trusted from prose.

Same dangling-gap pattern as the immediately-prior sibling (FACTORY-APIGW-split-handlers, cycle-470): the row's own `rebuild_required:true` deferred-verification signal (`ops-rebuild-verify-api-gateway-20260709T2022Z.json`) was deleted unactioned in an unrelated PM commit (`83748768d`), then pruned in a drain tick (`1f9fbf52b`) — never verified. Closed it myself: container confirmed created 2026-07-15 (post-commit), live-probed `/health`(200), `/macro/health`(200 — exercises the explicit `ProxyTimeoutMs:120000` override branch), `/stock/health`(200 — exercises the default-multiplier fallback branch), unknown-service(404).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 589→590, non-shrink not flagged — same benign growth pattern as qa-S26). `.head` reset idle (`active_task_id:null`, `next_agent:null`). DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S27.

## cycle-472 · 2026-07-22 · FACTORY-NEWS-split-sandbox — APPROVED, DONE_VERIFIED (direct-commit verify, 4th live use of the JUMP-TO)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). No `commit`/`files[]` on the board row; derived via `git log --all` on the 7 named split files → single unambiguous hit `39e3791c0` (2026-07-10T00:29:18+02:00, matches row's `completed_at` 22:27:00Z within clock precision), on main ancestry, `git show --stat` touches exactly the 7 claimed `src/sandbox/{cli,registry,discover,equal,execute,report,runner}.ts` (+ DJ/notebook/board docs), no later commit re-touched them. Line caps re-measured at HEAD: 48/57/21/120/56/82/75L, all ≤120L (execute.ts lands exactly at the cap, matches DJ).

Host `bun test`: 202 pass/6 skip/3 fail/3 errors — same 3 route-test files (`1899a-core-smoke`/`routes-health-reuters`/`routes-bloomberg`) failing on `Cannot find package 'hono'`, identical known host-only-service dep gap as the immediately-preceding sibling FACTORY-NEWS-dedup-normalizeDate (cycle-471's qa-S28 counterpart), unrelated to `sandbox/`. Host `bun tsc`/`bun run check` unrunnable (`node_modules` absent). `mock-guard.sh --files <7 sandbox files>` → "No production source files to scan. PASS." — read the script's own `EXCLUDE_PATHS` regex to confirm `/sandbox/` is deliberately excluded as dev-tooling, not a false-negative. DDD (`infra`/`application` import) grep clean, secrets grep clean (the one `SECRET|TOKEN|PASSWORD` hit is the env-audit gate's own detection regex literal in `cli.ts`, not a leaked value — gate reads `Bun.env`, not `process.env`, pre-existing).

Same dangling-gap pattern as the 3 prior siblings (FACTORY-APIGW-split-handlers/proxy-timeout-constant, FACTORY-NEWS-dedup-normalizeDate): closed the row's own `rebuild_required` gap myself. Container confirmed created 2026-07-15 (post-commit); `docker exec` byte-diffed the live `/app/src/sandbox/execute.ts` against the committed version — `diff` clean, IDENTICAL. Full suite INSIDE container: 74 pass/0 fail/117 expect(), `tsc --noEmit` exit 0. Extra check beyond DJ scope: ran the sandbox CLI itself live inside the container — env-audit gate correctly fired `SECURITY VIOLATION` against the container's real `DB_PATH`/`DB_READONLY` env (proves the gate, relocated into `cli.ts` unchanged, still guards real-creds execution); re-ran with scrubbed env → gate passed but 0 scenarios found, root-caused to `PROJECT_ROOT` (`resolve(import.meta.dir, '../../../..')`) landing outside `/app` at the container's shallower path depth — a pre-existing structural mismatch (dev tool assumes host repo layout), not a regression from the split (`import.meta.dir` resolution unchanged, confirmed byte-identical).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 597→598, non-shrink not flagged — same benign growth pattern as qa-S26/27/28). `.head` reset idle, `next_agent=pm`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S29.

## cycle-473 · 2026-07-22 · FIX-BCTC-BT3D-ROWS-THRESHOLD-STALE — APPROVED, DONE_VERIFIED (direct-commit verify, Python zone, 5th live use of the JUMP-TO)

Row was `branch:null`, thin (commit/files/completed_at/completed_by all null). Located single unambiguous commit `42cf264f4` via `git log --all -- apps/pdf-extractor/` (subject matches task-id verbatim); main ancestry confirmed; `git show --stat` scope tight (only `test_extract_tables_bt3d_real_ocr.py` 8-line hunk + own decision journal + own board-row insert, zero production files); `git log --follow` confirms no later commit re-touched the test file. Live working-tree file byte-identical to commit (`git diff` empty); live container (built 2026-07-21, post-commit) file also byte-identical (`docker exec cat` + diff, empty).

Host `.venv` has full real-OCR deps (pytest/pdf2image/pytesseract, tesseract 5.5.2) — targeted `pytest -m slow` (the test's own docstring: "one-shot real OCR — do not run in CI loops") PASSED 3x consecutively (12-13s each) against the real FPT PDF, confirms `>=79` matches live post-FR-5-dedup behavior. Unfiltered full suite showed this SAME test FAILING (`rows_stored=0`, OCR image-decode error) plus 9 other failures — did not accept as regression: git-stash A/B (reverted the assertion in-place to `>=80`, re-ran the identical full suite) reproduced the IDENTICAL 10-failure signature, proving pre-existing test-isolation/cache pollution unrelated to this edit; root-caused to sibling `test_bt3_fix3_row_fidelity.py` hardcoding a container-only `/app/data/pdfs/...` path that fails to open on host and pollutes the shared rasterizer cache. `-m "not slow"` regression run: 8 failed/1022 passed/7 deselected, matching the dev's own claimed baseline (1018/8/7 at commit time; drift = later unrelated commits). Container run of the same test SKIPPED not FAIL: `_FPT_PDF_PATH` (line 42-46, pre-existing, unchanged by this commit) hardcodes this admin's host absolute path and doesn't resolve inside the container — a pre-existing structural gap outside this 1-line threshold fix's scope, not a new regression. `mock-guard.sh --files` PASS (no production files, test-only per Smart-Skip); DDD/secrets grep clean (N/A, test-only).

VERDICT: APPROVED, DONE_VERIFIED. Row was thin — backfilled `commit`/`files`/`completed_at`(from commit author date)/`completed_by`(`dev-pdf-extractor`, matches `owner`) on the write. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 596→597, non-shrink not flagged — same benign growth pattern as qa-S26/27/28/29). `.head` reset idle, `next_agent=pm`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S30.
