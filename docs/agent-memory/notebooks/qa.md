# QA — Notebook

## cycle-530 · 2026-08-06 · FIX-EXECUTETIER-PHASE35-RESUME-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Did NOT trust `architect_review_note`'s own prose. Commits `702afaca0` (fix) + `385ffbd33` (memory) both confirmed on main ancestry (`git merge-base --is-ancestor`); `git show --stat` matches claimed scope exactly — `702afaca0` touches only `execute-tier.md` + new `scripts/audits/execute-tier-phase35-locklifetime-verify.sh`, `385ffbd33` touches only the decision-journal + architect notebook. Read the shipped diff directly: the batch-wide `finally` is fully gone — Step 2+3 is now a per-task try/except, releasing ONLY the specific task_id whose own spawn call threw, never on the success path; grepped the whole file, the sole surviving `finally` token is prose describing the OLD pre-fix shape for context, not live code. `owner_client_session` confirmed present on BOTH `task_claim` (Step 1) and `task_release` (except block). Cross-checked the shape against `main.md`'s 7 existing `LOCK-LIFETIME` sites and `post-cycle.md`'s Wrapper-Autoclose — identical no-release-on-success/except-only-release convention; this is a straight port, not a novel pattern.

Live-ran `scripts/audits/execute-tier-phase35-locklifetime-verify.sh` myself, TWICE, against the REAL `task_claim`/`task_release` MCP primitives (`mcp-call.sh` targets `localhost:3000/mcp` w/ `zenmidi.com` fallback — not a mock): 4/4 PASS both runs (AC-1 positive: 2nd claim from a different session fails while the 1st is unreleased, `current_holder` correctly reports session A; AC-2 negative control: retry only succeeds once the 1st is explicitly released). `mock-guard.sh --files` on both touched files: PASS ("no production source to scan" — correctly N/A, pure docs/flow+shell zone, zero `apps/` TS/JS touched, so `bun test`/`tsc` are structurally N/A too, consistent with this codebase's established precedent for flow-doc+shell fixes). `shellcheck` on the new script: 1 new warning-level `SC2164` (`cd` without `|| exit`) not present in the sibling `execute-tier-branchnull-review-headidle-verify.sh` (0 warnings) — flagged as a non-blocking style nit only (not security/correctness-relevant; the script already checks every `mcp_call` exit code explicitly). JSON built exclusively via `jq -n --arg` (SAFE-JSON honored, zero shell-injection surface). No blocking issues.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 781→781). `next_agent: pm`. `.head` untouched (was pointing at an unrelated task, `FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT`). Structural note: no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — could not directly call `task_release`; flagged to the calling dispatcher to release `task:FIX-EXECUTETIER-PHASE35-RESUME-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` (owner_client_session=f298ccf7-8cf4-452d-9a5a-57dcb47e65ac) itself.

## cycle-531 · 2026-08-06 · FIX-DEVTEAM-STEP0B-RESUME-SUPERVISED-HOLD-GATE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `plan_only`/`supervised` retained per row convention, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Did NOT trust `architect_review_note`'s own prose. Commit `adb426877` confirmed on main ancestry, `git show --stat` matches claimed scope exactly — 1 file, `docs/agents/dev-team/flow/main.md`, +44/-6. Read the shipped diff directly: WF-2 SUPERVISED-HOLD check inserted between the pre-existing WF-1 BLOCKED carve-out and the S2 dispatcher-wrap, exactly as the deliverable specified; on hold, `head` is left UNCHANGED (unlike BLOCKED's idle-reset) so a later `po_goahead_*` stamp resumes on the very next tick — matches the claimed design intent, not just prose.

Live-drove the ACTUAL shipped jq filter (not a paraphrase), `include "scripts/lib/devteam-eligibility"`, against 7 fresh synthetic fixtures in scratch, never against the live file: (1) `supervised:true`, no `po_goahead_*` anywhere → `should_hold=true`; (2) same row + `po_goahead_20260806` stamp ON THE ROW → `false`; (3) same row + stamp ON `.head` instead → `false` (both stamp locations honored — the filter ORs row-keys with head-keys); (4) non-supervised baseline row → `false` (no false-positive on the common case, negative control). Confirmed `effective_supervised()`/`detail_items_from()` are the real shared defs in `scripts/lib/devteam-eligibility.jq` — no forked copy. Also confirmed the CURRENT file's WF-1/WF-1b/WF-1c/WF-2 ordering is intact — WF-1b (TERMINAL-LANE) and WF-1c (READY-LANE) were added later, same day, by two unrelated rows, and correctly retitle this gate's cross-ref to "run FOURTH": re-drove `task_status` resolution on 3 more fixtures — BLOCKED row (`supervised:true`) → WF-1 fires, never reaches WF-2; `done[]`-resident row → WF-1b fires (`is_terminal_task_status=true`); `ready[]`-resident row (`supervised:true`) → WF-1c fires. All 3 pre-empt WF-2 correctly, so the later insertions did not silently regress this gate's reachability or ordering.

Co-edit sibling `FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION`'s lock-lifetime edit (landed in the SAME commit, per the PO co-edit binding) spot-checked present at all 4 named sites (S2/SLS/RLC/QA-Drain) — no `finally` on the success path, `except`-only release with `raise`; that row's own gate is a separate id and was not independently re-graded here, only checked for non-regression (file now carries 7 `except:` blocks total — growth from later unrelated sections, not a revert of this fix). No committed regression script exists for THIS row specifically — the note says it was proven live against fixtures + the real `task_claim`/`task_release` MCP primitives, not a checked-in test file; re-derived the equivalent live myself rather than trusting the prose alone. Zone is `docs/agents/dev-team/flow/` (orchestration flow doc, no `apps/<service>/` DDD layer touched) — `bun test`/`tsc`/DDD/security N/A (Smart-Skip), consistent with QA precedent on this same file's sibling rows (`FIX-EXECTIER-HEADSYNC-BRANCHNULL-REVIEW-IDLE`, `FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND`). No blocking issues.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 781→781, commit `c34880f8b`). Caught + corrected my own stale `next_agent:"qa"` carried into `done_verified[]` in a follow-up write (commit `dd28bcc10`) → `next_agent:"pm"` per `qa/flow/main.md`'s verify-committed-approved RETURN contract. `.head` untouched (was pointing at an unrelated task, `FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT`). Structural note: no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — could not directly call `task_release`; flagged to the calling dispatcher to release `task:FIX-DEVTEAM-STEP0B-RESUME-SUPERVISED-HOLD-GATE` (owner_client_session=f298ccf7-8cf4-452d-9a5a-57dcb47e65ac) itself.

## cycle-531 · 2026-08-06 · FIX-NOTEBOOK-AUTOPRUNE-SAMEDAY-TIE-DROPS-NEWEST — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row predated the drain (no `.commit`/`.files[]`) — fallback path: derived commit via `git log` on the 3 files named in `review_note` = `c280e00cd` (2026-07-30T11:11:35+02:00). Confirmed on main ancestry; `git show --stat` matches the claimed 3-file scope exactly (`notebook-auto-prune.sh`, `notebook-auto-prune.test.sh`, `notebook-section-order.json`); commit's own `Task:` trailer = row id verbatim. Read the LIVE tie-break code (survived 2 later refactor commits, `9b0764631`/`7552421bc`, which extracted the algorithm into a shared `lib/notebook-section-direction.sh` without changing semantics) — case-statement still correct: `newest_first` drops the physically-LAST tied section (keeps the newest/physically-first, matches prepend convention), `oldest_first` drops physically-FIRST. Re-ran `notebook-auto-prune.test.sh` myself: 8/8 PASS (T5/T6/T7 = the 3 tie-break scenarios claimed; T8 is an unrelated jq-missing case added by a later hook-crash-discriminator commit on the same file — explains the row's own "7/7" vs live "8/8", not a regression).

Existing coverage looked thin on multi-iteration ties, so built my OWN additional fixture independent of the shipped suite: a prepend-convention notebook with a genuine 3-way same-day tie as the file's actual global minimum plus 1 distinguishing newer anchor (first attempt had a fixture bug — an "older" anchor I added ended up BELOW the tie group's key, becoming the sole global min and defeating the test; caught it, rebuilt without that anchor). Ran the live hook against the corrected fixture: correctly consumed the tied group oldest-to-newest across 3 shrinking-tie-group loop iterations (3-way→2-way→unique), never touching the physically-first/newest tied member until it was the sole section left — confirms the mechanism generalizes beyond the shipped single-iteration fixture. `mock-guard.sh` PASS/N-A (bash+JSON only, no `apps/` source — consistent with the row's own `bun test`/`tsc` N/A claim). No blocking issues.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 781→781). `next_agent: pm`. `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-4.md` §qa-S56. Structural note: no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — could not directly call `task_release`; flagged to the calling dispatcher to release `task:FIX-NOTEBOOK-AUTOPRUNE-SAMEDAY-TIE-DROPS-NEWEST` (owner_client_session=f298ccf7-8cf4-452d-9a5a-57dcb47e65ac) itself.
