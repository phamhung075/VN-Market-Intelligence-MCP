# QA — Notebook

## cycle-388 · 2026-07-08 · FACTORY-INTERFACE-source-confidence-10-mask — DONE_VERIFIED (post-swap sanity check PASS)

Final gate after ops's gated container swap (`f617d0c67`, image `180382145ee7` → `35c8117c1f85`). Independently re-confirmed, not trusted: `docker inspect c0dc4c1c7168 --format '{{.Image}}'` → `sha256:35c8117c1f85...` (exact match), container `Up (healthy)`, `StartedAt=2026-07-08T04:44:33Z`. RAW-queried the LIVE post-swap container's DB myself (in-container `bun -e` on `bun:sqlite` against `/app/data/market.db`, not a host copy): `GROUP BY source_confidence` → `{0.1:380, 0.4:2, 1.0:3257}`, NULL count → 0, total 3639 — **exact match** to the pre-swap distribution independently captured at §qa-S8 — confirms the swap did not regress the INSERT write path, exactly as expected for this behavior-preserving structural-hardening fix (no distribution delta was ever expected). `curl http://localhost:3000/health` → 200, `toolCount:183` unchanged, independently curled myself. Scanned the full post-swap log window (`docker logs c0dc4c1c7168 --since 2026-07-08T04:44:33Z`, 80k+ lines) for `finalizeBctcRefine`/`SqliteError`/`constraint failed`: zero matches — no live BCTC finalize run occurred in the post-swap window (only routine startup housekeeping: bctc-poison-cleanup, backfillBctcPdfPaths, bctcReparseJob cadence-skip), so nothing new to regress-check on that point, and no error signal either. VERDICT: POST-SWAP SANITY CHECK PASS. Flipped `FACTORY-INTERFACE-source-confidence-10-mask` `REVIEW` → `DONE_VERIFIED` via `scripts/qa-factory-interface-source-confidence-10-mask-done-verified.jq` + `orch-apply.sh` (array-move `review[]`→`done_verified[]`, guarded on `.head.active_task_id`, mirrors the `FACTORY-INTERFACE-sequential-confidence-05-mask` §qa-S7/cycle-386 precedent). No new follow-up bug discovered this cycle (unlike the sibling task). `.head` idle-reset (`status:"done"`, `next_agent:"router"`, `active_task_id:null`). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S9. Report: reports/TASK_REPORT_FACTORY-INTERFACE-source-confidence-10-mask.md.

## cycle-387 · 2026-07-08 · FACTORY-INTERFACE-source-confidence-10-mask — QA-APPROVED, HELD REVIEW (ops swap pending, dev-mcp-server 0f76b3872)

Gated dev-mcp-server's `resolveSourceConfidence()` extraction in `finalizeBctcRefineTool.ts` (was `row.source_confidence ?? 1.0` at the INSERT site) | Verdict: **code+tests APPROVED, held REVIEW** (rebuilt image `35c8117c1f85` not yet swapped — ops-gated, container still `180382145ee7`). Independently re-verified, not trusted: read `refinedMarkdownParser.ts` in full — `source_confidence` is `Math.min()` across `parseTrustFlag()` results (0.2 red / 0.4 yellow / 1.0 no-flag, floored to 0.1 on unparseable numeric cell) computed on every row unconditionally, no `undefined`-returning path exists — confirmed the resolver's `undefined` branch is genuinely unreachable in the current pipeline (provably dead code, not an active mask). RAW-verified the live named-volume DB myself (in-container `bun -e` on `bun:sqlite` against `/app/data/market.db` inside the still-running PRE-fix container, `vn-market-intelligence-mcp_market_data` named volume): `GROUP BY source_confidence` → `{0.1:380, 0.4:2, 1.0:3257}`, NULL count → 0, total 3639 — **exact match** to the claimed figures, obtained via my own independent query. Code review: `resolveSourceConfidence()` uses strict `!== undefined` (preserves real `0`/`1.0`, NOT NULL column never violated). New `FACTORY-INTERFACE-source-confidence-10-mask.test.ts` (6/6) correctly covers both branches incl. explicit `0`/`1.0` edge cases and the strict-undefined fallback. Targeted+adjacent re-run myself (7 files: new test + HC-human-confirm + AR-parser-dv + TASK-W2-FIX-BCTC-BANK-SUMMARY-MAPPING-ROW-REPAIR + FU-5b-parens-negative-parser + BANK-AWARE-1-consumer-audit + FU-6f-eval-blob-blockers): **167/167 pass, 511 expect** — exact match to dev claim. `tsc --noEmit` 0 errors. DDD clean (0 import-line diff in either modified file — grep-confirmed). Security clean (no `process.env`/secrets). `mock-guard.sh` PASS exit 0. Zero new MCP tool registration (0-diff `server.tool()` call) — no architect-review trigger. Full `bun test` kicked off in background per standard gate; per established precedent (S2/S4, CONTAM-10-WRITER-H, cycle-385) treated as corroborating only. Held at REVIEW: `docker compose up -d` swap is ops-gated; `.head.next_agent` → `"ops"`. Because this fix is confirmed behavior-preserving (no observable delta expected), downgraded the post-swap QA hop from a before/after delta hunt to a RAW smoke-test re-query of the same distribution + NULL-count query (verify write path still works under the new image) + server health + tool count — reflected in `.head.next_action` via `scripts/qa-factory-interface-source-confidence-10-mask-hold-review.jq` + `orch-apply.sh`. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S8. Report: reports/TASK_REPORT_FACTORY-INTERFACE-source-confidence-10-mask.md.

## cycle-386 · 2026-07-08 · FACTORY-INTERFACE-sequential-confidence-05-mask — DONE_VERIFIED (post-swap sanity check PASS + new bug filed)

Final gate after ops's gated container swap (`675451c2c`, image `180382145ee7`). Independently re-confirmed, not trusted: `docker inspect vn-market-intelligence-mcp-mcp-server-1 --format '{{.Image}}'` → `sha256:180382145ee7...` (exact match), container `Up (healthy)`; `curl http://localhost:3000/health` → 200, `toolCount:183` unchanged. Went beyond the "lighter sanity check" the S6 REVIEW note called for and exercised the optional live-wire probe of `sequential_market_analysis` (task left this to my judgment) — a real `tools/call` POST to `/mcp` with a stated hypothesis and confidence deliberately omitted. Result: the call never reached `handle()` — `originalHandler is not a function`. Root-caused: `registerSequentialMarketAnalysisTools` calls `server.registerTool(name, {...,handler:tool.handle})`, but the real SDK signature is `registerTool(name, config, cb)` (`node_modules/.pnpm/@modelcontextprotocol+sdk@1.29.0.../dist/esm/server/mcp.js:699` + `.d.ts:150-157`) — `config.handler` isn't a recognized field, no 3rd `cb` arg is ever passed, so `registeredTool.handler` is permanently `undefined`; `server.ts`'s per-call telemetry wrapper throws on every real invocation, on every transport. Confirmed PRE-EXISTING via `git show 1b1397025^:...sequential-market-analysis.ts` — byte-identical broken call before this fix landed, `1b1397025` never touches `registerSequentialMarketAnalysisTools`. Sanity-checked it's not a systemic `/mcp`-route bug: `get_market_snapshot` (correct `server.tool(...)` 4-arg API) over the same route/container returned real live data. This *reinforces* the S6 REVIEW verdict rather than contradicting it — this tool never had ANY live wire surface, not just no surface for `confidence` specifically — so a full RAW HTTP probe genuinely could not have added signal here. Confidence-fix itself remains correctly verified at the unit level (`tool.handle()` called directly — the real code path once the unrelated wiring bug is fixed) — zero regression from `1b1397025`. Flipped `FACTORY-INTERFACE-sequential-confidence-05-mask` `REVIEW` → `DONE_VERIFIED` via `scripts/orch-apply.sh` (array-move `review[]`→`done_verified[]`, mirrors `FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP` precedent). Filed the newly-discovered defect as `FIX-SEQUENTIAL-ANALYSIS-TOOL-DEAD-HANDLER` (new `task_board.backlog[]` row + `backlog-detail.json` entry, owner dev-mcp-server, priority high, exact repro+root-cause+fix included) rather than blocking or silently dropping it — out of scope, unrelated root cause, not my job to fix. `.head` idle-reset (`status:"done"`, `next_agent:"router"`) pointing at the new follow-up, mirroring `CONTAM-10-WRITER-H`. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S7 (DJ-GATE-1). Report: reports/TASK_REPORT_FACTORY-INTERFACE-sequential-confidence-05-mask.md.

**Retention note:** cycle-385 (FACTORY-INTERFACE-sequential-confidence-05-mask — QA-APPROVED HELD REVIEW) and older pruned this write per AC-2 (keep last 3). Full detail preserved in `reports/TASK_REPORT_FACTORY-INTERFACE-sequential-confidence-05-mask.md` and prior git history of this file (which also carries cycle-384: CONTAM-10-WRITER-H DONE_VERIFIED / cycle-383: CONTAM-10-WRITER-H HELD REVIEW / cycle-382: PDF-TEST-01-FIX HELD/BLOCKED / cycle-381: FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP APPROVED / cycle-380: KD-OBS-01-FIX APPROVED / cycle-379: CI-RED-c5b5f885-FIX APPROVED / cycle-378: FACTORY-ALERT-consolidate-dual-engines APPROVED / cycle-377: B-05-FU-SSC-503-RETRY PASS / cycle-376: FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK APPROVED / cycle-375: FIX-ALERT-COMMANDER-DEAD-NO-SLOT PASS / cycle-374: FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD PASS / cycle-373: FIX-FB-DAILY-FIRER-CLI-FLAG APPROVED / cycle-372: FIX-BCTC-BANK-BS-COLUMN-ORDER PASS / cycle-371: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
