# QA — Notebook

## cycle-638 · 2026-08-11 · FACTORY-PDF-extract-tesseract-config — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `cfe0a78d7`, first Python pilot)

dev-team Review-Lane QA-Drain spawn (`qa[]` row, `branch:null`, `mode=verify-committed`). First `apps/pdf-extractor` (Python/pytest) row through my Direct-Commit Verify path — flagged in the spawn prompt as a known PSM-6-drift risk class (`project_bctc_ocr_psm_drift`), so verified the `--psm 6` warning was preserved exactly, not just relocated. Row's own `review_note` claimed: extracted the copy-pasted Tesseract lang/psm/DPI config (5 named call sites, each with its own DO-NOT-REMOVE `--psm 6` warning) into new `infrastructure/tesseract_config.py`; `generic_md_table_extractor.py` resolved to 2 real files behind an existing FACTORY shim (`generic_md_table/extractor.py` + `unit_ocr.py`) — 6 real call sites total. Confirmed `cfe0a78d7` (+`43d0c92ad` memory) on `main` ancestry; `git show --stat` matches all 6 real call sites + the docs file exactly.

RAW-verified, not trusted from prose: new module's boxed docstring warning ("DO NOT remove or change TESSERACT_PSM6_CONFIG") is now the sole authoritative copy, citing BT3-FIX3-PSM/drift #4 by name; every one of the 6 call-site diffs shows the literal replaced 1:1 (`lang="vie+eng"`→`TESSERACT_LANG`, `config="--psm 6"`→`TESSERACT_PSM6_CONFIG`, `dpi=200`/`resolution=200`→`OCR_RASTER_DPI`) with the per-site inline warning comment collapsed to a one-line pointer. Post-refactor repo grep: 0 remaining hardcoded `"--psm 6"`/`"vie+eng"` literals outside `tesseract_config.py` (2 hits are docstring parameter-examples in `unit_ocr.py`, not live calls); the only remaining `dpi=200` is in `pek_engine_adapter.py` (PaddleOCR engine, correctly out of scope per the backlog spec).

Ran `test_ocr_adapter_psm6_guard.py` live: 3/3 PASS. Ran full suite `-m "not slow"` on current `main` HEAD first: 2 fail/1050 pass — diverges sharply from the row's claimed 1022 pass/8 fail baseline. Rather than accept either number blind, isolated the diff with a clean `git worktree` A/B pinned exactly at `cfe0a78d7` vs `cfe0a78d7^` (not current HEAD, which carries ~2.5wk of unrelated intervening commits): BOTH give byte-identical `8 failed, 1022 passed, 7 deselected`, same 8 test names (2 pre-existing `pandas`-missing-dependency failures in untouched `test_ocr_backends.py`, 2 `test_ocr_unit_tesseract_retry.py`, 4 `test_page_rasterizer.py` — all env/order-dependent, none in the 6 touched files) — independently reproduces the row's own equivalence claim exactly; the current-HEAD delta is unrelated drift from later commits, not this change. `lint-imports` (DDD Fence-A/B/C): 3 kept, 0 broken. `mypy --explicit-package-bases infrastructure/tesseract_config.py`: 0 errors (matches claim). Security grep (password/secret/api-key/token/os.environ) on all 7 changed files: clean. Docs diff (`infrastructure.md`) accurately reflects the new module and preserves the "load-bearing, do not change" language.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 766=766, signal_total 57=57, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. DJ-GATE-1: dev-pdf-extractor's own `sprint-FACTORY-PDF-extract-tesseract-config-dev-pdf-extractor.md` (task-id stamped ×4, pre-satisfied); own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S51. `rebuild_required:true` DEFERRED by dev (code-only, no container touched) — correctly out of QA scope, ops decision. `task_release` is the router's job per this task's own dispatch note (dispatcher-wrap lock held by router) — not attempted here. Pruned cycle-635 (oldest, `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES`) this write per AC-2 retention (3-section cap) — kept 636/637/638.

## cycle-639 · 2026-08-11 · FACTORY-INFRA-split-stores-and-migrations (dev-mcp-server) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `76c73b4c1`+`abaf44fd1`+`97af4860e`)

3rd row of the same batch (`.head.active_task_id` pointed at this exact row, `claimed_at` shared with the 2 sibling rows already drained by a peer QA session — file was mid-write by that peer between my initial read and my own write, rebuilt the jq transform against the live file immediately before applying to avoid clobbering it). Row's own `status_note` claimed a 2026-07-24 rebuild+RAW-DB-verify pass (`ops single-svc rebuild a3efeeddd`, `financial_reports` 226 rows/60 cols intact, `vnstock_financials` 170 rows persist) — task explicitly asked to re-confirm via git log/show, not trust prose. `a3efeeddd` is NOT a git object (`git merge-base --is-ancestor` fails to resolve it) — it's an ops-tool rebuild-action id, unverifiable via git.

RAW-verified the code claim first: `76c73b4c1`/`abaf44fd1`/`97af4860e` all confirmed on `main` ancestry (dated 2026-07-24, matches review_note); `git show --stat 76c73b4c1` matches the claimed 13-file split exactly. Read the code directly, not trusted from prose: `vnstockStore.ts` is a 37L pure re-export barrel with all 19 claimed exports; the 8 split store files' line counts match exactly (73/118/47/47/68/67/66/240L); `schema-financial-reports.ts`'s `FINANCIAL_REPORTS_COLUMN_MIGRATIONS` array has exactly 22 entries iterated by one `runColumnMigrations()` loop. Re-ran the equivalence test live: 23/23 pass. Ran a BROADER targeted suite than the row's own claim (72 files — every test importing the touched modules, vs the claimed 19+12=31) — 886/886 pass, 0 fail. `bun tsc --noEmit` clean. DDD/security greps on all 12 production files clean; `mock-guard.sh` PASS.

For the unverifiable-via-git rebuild claim: did not stop at "cannot confirm" — independently re-probed the LIVE claim instead. Exec'd into the running `mcp-server` container (`vn-market-intelligence-mcp-mcp-server-1`, healthy) and diffed its on-disk `vnstockStore.ts` against repo HEAD — byte-identical, proving the split code is actually deployed to production, not just committed. Queried the live production `market.db` directly via `bun:sqlite` (readonly): `financial_reports` now 257 rows/86 cols (up from claimed 226/60 — 18 days of continued production writes + additional migrations landed since), `vnstock_financials` 174 rows (up from 170) — both healthy and growing, not frozen/corrupted. All 22 golden migration columns + `idx_fr_extraction_method` index confirmed present in the live DB, proving the declarative migration loop genuinely executes correctly in production, not just in the in-memory test DB.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached — the container-diff + live-DB probe, since the row's own cited action-id was unverifiable) AND reset `.head`→idle in the SAME `orch-apply.sh` write (`.head.active_task_id` pointed exactly at this row) — Stage0+1 PASS, conservation OK (task_total 766=766, signal_total 57=57, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (row uses `status_note` throughout, not `review_note`). DJ-GATE-1: `sprint-FACTORY-INFRA-split-stores-and-migrations-dev-mcp-server.md` task-id stamped ×5, pre-satisfied; own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S52. `task_release`/`send_telegram` not attempted — router (dispatcher) holds the outer lock and owns release per this task's own dispatch note, and no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (matching the recurring gap noted cycles 634-638). Pruned cycle-636 (oldest, `FIX-MACRO-LIQUIDITY-STATE-HANDLER-EXCEEDS-CRON-15S-DEADLINE`) this write per AC-2 retention (3-section cap) — kept 637/638/639.

## cycle-640 · 2026-08-11 · FACTORY-ALERT-shared-vocab-package (dev-alert-engine, Go) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `9d91af5bb`)

Router-dispatched (`task_id=FACTORY-ALERT-shared-vocab-package mode=verify-committed`). Row carried NO `commit`/`files[]`/`owner`/`status_note`/`review_note` at all — only `reviewed_at`/`reviewed_by:dev-alert-engine` (predates the drain, first row this thin I've seen). Derived commit via `git log --all -- <detail_ref files[]>` (3 candidates listed: `domain/models.go`, `signal-classifier/classifier.go`, `cooldown-gate/gate.go`): `9d91af5bb` — dated 13:20:08Z, 1m52s before `reviewed_at` (13:22:00Z), commit message itself names the task-id verbatim — high-confidence match, confirmed `git merge-base --is-ancestor` on `main`.

DoD is disjunctive: "either a Fence-A-approved shared vocab package consumed by all three, or the cooldown-gate literal collapsed". `git show --stat` shows the commit touches only `cooldown-gate/gate.go` (1 of 3 files) — the low-risk fallback, so `models.go`/`classifier.go` untouched is by design, not a gap (commit message explicitly reasons through why: no established Fence-A precedent for sibling-primitive import, kept classifier self-contained). Verified `.golangci.yml` fence-a deny-list live, not trusted from commit prose: `pkg/domain` import is genuinely NOT on the primitive deny-list (only application/infrastructure/interface/mattn-sqlite3 denied), corroborating the claim. Read `gate.go` directly: L92 now reads `domain.SeverityCritical` (was a hand-copied `"critical"` literal); string value byte-identical across all 3 files. `git diff 9d91af5bb..HEAD -- gate.go` empty — no later commit reverted it.

Ran REAL verification live, not trusted from prose (first Go-primitive row through this path since `FACTORY-STOCK-resolve-dead-writeback`): `go build ./...`/`go vet ./...` clean; `go test -count=1 ./...` 8/8 packages pass fresh, incl. targeted `cooldown-gate` suite 10/10 (specifically `TestCheck_CriticalBypass`/`TestCheck_CriticalMacroNoBypass`, the two paths exercising the collapsed literal); `golangci-lint run` (Fence-A/B/C) 0 issues; `mock-guard.sh --files gate.go` PASS; security greps (process.env/password/secret/token) clean.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 765=765, signal_total 69=69, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` written to a newly-added `status_note` field (row had none). DJ-GATE-1: `sprint-FACTORY-ALERT-shared-vocab-package-dev-alert-engine.md` (task-id stamped) pre-satisfied; own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S53. `.head.active_task_id` pointed at a SIBLING batch row (`FACTORY-FRONTEND-split-market-summaries`, same `claimed_at` — batch of 3), not this one — left `.head` untouched, matching the sibling-row convention from cycles 637/638. `task_release` is the router's job per this task's own dispatch note — not attempted here. Pruned cycle-637 (oldest, `FACTORY-SCHEDULER-dedup-briefing-formatters`) this write per AC-2 retention (3-section cap) — kept 638/639/640.
