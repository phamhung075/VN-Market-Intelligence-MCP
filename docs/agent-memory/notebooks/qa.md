# QA — Notebook

## cycle-555 · 2026-08-06 · FIX-DEVTEAM-PREFLIGHT-STEP55-COLDEVICT-STDOUT-LEAK-CORRUPTS-VERDICT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit_sha on row: `f2b41e6df`)

Dispatch note flagged that the dispatcher's own live tick this same run reproduced the exact live corroborating shape (multi-line cold-evict progress block ahead of the JSON verdict, jq parse succeeded regardless) — treated as useful context only, RAW-verified the committed fix myself regardless. `f2b41e6df` real + on main ancestry, but `git show --stat` only lists 2 of the row's 3 `files[]` (the `.sh` + `.test.sh`, truncated-path display); switched to `git show --name-only` and found the 3rd claimed file, `docs/agents/dev-team/flow/main.md`, lands in a companion same-session commit `ffb39318f` (also main-ancestor-confirmed, `Task:` trailer names this exact task id, carries AC-3/AC-4 — the JUMP-TO table's new empty/unparseable-VERDICT row). Together the 2 commits cover all 3 claimed files; flagged the row's own `commit_sha` field as incomplete metadata (names only 1 of 2) in the board's `[QA] Review Record`, non-blocking.

Re-ran REAL verification, not trusted from prose: `bash scripts/agents-flow/dev-team-tick-preflight.test.sh` live → 124/124 PASS (was 103/103 at commit time — 21 unrelated tests landed since, no regression). Went further than trusting the commit message's own stash-test claim: independently reverse-applied ONLY the `f2b41e6df` `.sh` hunk against current HEAD in an isolated scratch copy (never the live repo) — T36's 4 targeted checks (single-JSON-stdout via `jq -e .`, RUN verdict, both leak lines routed to stderr not stdout) FAIL pre-fix / PASS post-fix, matching the commit's own "fails 4/5 checks... all green post-fix" claim exactly (5th check, bare exit=1, trivially passes either way). Ran a same-environment control (unmodified post-fix script, identical minimal scratch dir) and got 2 extra unrelated failures (T14, T35(ii)) there too — confirmed these are scratch-environment artifacts (missing `$ROOT` repo structure for non-hermetic "REAL" checks), not attributable to this fix; the true isolated signal is exactly T36's 4 checks.

No TS/production source touched (pure bash+md, zone `cross-service/`) — `bun tsc --noEmit` structurally N/A; `mock-guard.sh --files "scripts/agents-flow/dev-team-tick-preflight.sh"` → "No production source files to scan. PASS."; DDD/security grep N/A (no infrastructure/application imports, no `process.env`/secrets in either commit's diff). `bash -n` clean both scripts; `shellcheck -x` 0 new findings (1 pre-existing informational SC1091, unrelated — sourced file not found by shellcheck's static follower, not a real defect). DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md:73` task-id-stamped, predates this verify — all 4 ACs justified there including the deeper out-of-scope `orch-cold-evict.sh:930-932` leak-source finding (correctly flagged, not fixed, since that file isn't in this row's `files[]`).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (fresh re-read immediately before write — conservation OK task_total 755→755, signal_total 203→203). `next_agent: pm`. Verification text appended to the row's own new field `qa_direct_commit_verify_20260806` (row has no `status_note`/handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S10 (2026-08-06T21:52:30Z). Task lock held by dispatcher coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); dispatcher will release `task:FIX-DEVTEAM-PREFLIGHT-STEP55-COLDEVICT-STDOUT-LEAK-CORRUPTS-VERDICT` itself per its own instruction, not attempted here.

## cycle-556 · 2026-08-06 · FIX-COLDEVICT-SIGNALQUEUE-NO-AGE-GATE-ORPHANS-READ-ROWS — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, `commit_sha` on row: `d963b487b`)

Row carried a `po_qa_hold_20260801T0432Z` sequencing hold: sign-off blocked unless discriminator-gap child `FIX-COLDEVICT-MALFORMED-TS-CATCH0-EVICTS-FRESH-SIGNAL-ROWS` lands first, else `catch 0` silently defeats this row's own new age gate for any non-second-precision `.ts`. Did not wave the hold through — checked ancestry directly: child commit `f0644c14a` IS on main AND is a direct descendant of this row's own `d963b487b` (55min later, same day, on top of it). Read the live combined code myself (`orch-cold-evict.sh:353-373` normalizer `coldevict_ts_epoch_or_oldest` + `:515-519` eviction predicate) — confirmed the predicate calls the normalizing helper, not a bare `catch 0`, so both fixes compose correctly, not merely coexist. Hold verified SATISFIED against the exact main HEAD this review runs against.

Commit `d963b487b` real + on main ancestry; `git show --stat` matches 2 of the row's 4 `files[]` (`scripts/orch-cold-evict.sh`, `scripts/test/orch-cold-evict-tests.sh`). The other 2 claimed files (`dev-team-tick-preflight.sh`, `signal-dashboard/SKILL.md`) correctly untouched, not silently dropped — grep-confirmed repo-wide zero OTHER `TERMINAL_SIGNAL_STATUSES` call site with an independent no-age-gate defect (preflight.sh fully delegates to the script, no own copy), and `SKILL.md`'s 24h contract was already correct pre-fix (`git blame`: HSC-7 commit `bff0362c2`, 2026-06-26).

Re-ran REAL verification, not trusted from prose: `scripts/test/orch-cold-evict-tests.sh` on current main → 53/53 PASS (TEST 9 this row's own fresh/73h fixtures, TEST 10 child's malformed-ts fixtures, T1-T8 regression-clean). Live `--dry-run` against the REAL `orch-state.json`: `signal_queue.rows[] would evict: 0 items` — matches AC-3's before/after-0 claim. `mock-guard.sh --files` on the touched production file → "No production source files to scan. PASS." (bash/jq zone, correct N/A not a skipped check). `bun tsc --noEmit` N/A — `git show --stat` confirms zero `.ts`/`.go` files touched. DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md:65` task-id-stamped (STEP developer-S54), predates this verify.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 755→755, signal_total 203→203). `next_agent: pm`. Verification text appended to the row's own new field `qa_direct_commit_verify_20260806` (row has no `status_note`/handoff file). DJ: own new continuation file `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-8.md` §qa-S1 (created to avoid a concurrent-write collision with sibling QA-drain sub-agents writing qa-7.md in the same window). NOTE for next reviewer: sibling child row `FIX-COLDEVICT-MALFORMED-TS-CATCH0-EVICTS-FRESH-SIGNAL-ROWS` itself still sits `status=REVIEW`/`next_agent=qa` on the board — separate task-id, out of this dispatch's scope, board bookkeeping only (its code is already live and re-verified working here). Task lock held by dispatcher coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); dispatcher will release `task:FIX-COLDEVICT-SIGNALQUEUE-NO-AGE-GATE-ORPHANS-READ-ROWS` itself per its own instruction, not attempted here.

## cycle-556 · 2026-08-06 · FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, review_note cites commits `526181a37`/`da207aa82`)

Cited commits are BOTH dangling (`git fsck --unreachable`), not on main ancestry — a git-contention replay artifact (sibling commit `30f97f7f2` on main literally says "(replay of e8945d047)"). Did not hard-fail per the verify-committed script's literal step-2 — used `git patch-id` to find the content-identical landed commits by same-day subject+author-date match: `c6730ddc7` (code, patch-id `10123ac0...` == `526181a37`) and `2bb3d6665` (memory, patch-id `53d14d48...` == `da207aa82`), both confirmed main ancestors. Same resolution shape as a sibling agent-father row this same drain cycle (`fd000eca4`/`92ba46360` cherry-pick) — flagged as a correction in the board write, not silently accepted or blocked.

Verified `c6730ddc7` directly: new domain module `periodContentExtractor.ts` (zero infra/application imports) + `parseBctcReport.ts` Step-0 gate — read the code myself, throw precedes Step-1 extraction/storeReport entirely (AC-2/AC-5 "written under neither key" holds by construction, not by claim). Re-ran both new test files on current main: 16/16 pass (43 expect) + 14/14 pass (32 expect), exact match to claim; every AC has a dedicated test by name. `bun tsc --noEmit` 0 errors, `mock-guard.sh` PASS, SQL fully parameterized, zero secrets/env.

AC-1 (data correction) independently corroborated live by PO twice same-day plus 4 more live-production evidence rounds through 2026-08-01 — the Step-0 guard fired correctly on real traffic (19-24 real quarantines observed, zero false negatives); acquisition-side skew root-caused and split into its own row (`FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`) per PO's own note explicitly granting sign-off on this row's ACs alone.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 755→755, signal_total 203→203). `next_agent: pm`. Verification text appended to row's own `review_note` field (no `status_note`/handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-7.md` §qa-S1 (own file, avoids concurrent-write collision with sibling QA-drain sub-agents writing qa-6.md). Task lock held by dispatcher coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); dispatcher will release `task:FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT` itself per its own instruction.
