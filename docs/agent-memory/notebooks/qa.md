## cycle-839 · 2026-08-25T15:50Z · FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS, direct-commit verify — PARTIAL PASS (3/4 AC), qa[] -> review[]

**A time-gated AC is only as honest as its arithmetic — so I computed the base rate instead of eyeballing the silence.** `e6adc6dfe` landed 2026-08-25T14:59:30Z; AC-3 wants zero new BARE fires over a *subsequent* 24h, so earliest close is 2026-08-26T14:59:30Z and only 0h47m (3.3%) had elapsed at verify time. Zero fires so far looks like evidence and is not: across the last 40 entries in `.git/sweep-guard.log` the MEDIAN inter-fire gap is 0.68h and **48% of historical gaps already exceed 47 minutes**, against a base rate of 14-22 fires/day. The observed silence is the modal outcome under the *unfixed* regime. Refused AC-3, did not "provisionally" pass it.

**Classifying the fire population turned a vague "partial fix" into a quantified forward risk.** Bucketed all 59 BARE fires since 2026-08-22 by staged-file signature: cowork-tick (the class this commit fixes) = 4/59 (6.8%); system-auditor notebook = 16/59 (27.1%, 6 actor sessions); ad-hoc mega-sweep n>=20 = 1/59; misc agent commits = 38/59 (64.4%). So **93.2% of the live fire population is untouched** and AC-3 as literally worded ("zero new BARE fires") is unlikely to pass on this fix alone. That belongs on the row *before* someone re-verifies tomorrow and reports a bare FAIL — recorded as an explicit PO scope question.

**AC-4's real failure mode was never "is the code right", it was "does the LIVE hook have it".** Checked `.git/hooks/pre-commit` is a symlink to `scripts/git-hooks/pre-commit` (byte-identical, `caller_chain` present 6x), then rebuilt the developer's sandbox probe myself rather than trusting "verified live in a sandboxed repo": a real BARE commit emitted `caller_chain=` on **all three** surfaces (stderr banner, log line, `docs/signals/` payload), naming both the invoking command line and the top-level `claude --dangerously-skip-permissions -n detect-loop` family. Re-ran `pre-commit.test.sh`: 15/15. Also checked the AC-1/AC-2 remedy is not inert — telemetry.md Step 6.2 delegates to `commit-mutex/SKILL.md`, whose Step 2c is itself pathspec-scoped — and corroborated the diagnosis against the log (fires 08-24T21:08:29Z / 08-25T00:08:22Z, actor 7a47f7c6, staged exactly the two cowork tick artifacts).

**`scripts/verify-fleet-commit-pathspec.sh` is RED but all 6 FAILs are prose false positives — inspected line-by-line, none is a live instruction.** Two causes: (a) allowlist LINE-NUMBER DRIFT (3 sites — developer/flow/main.md allowlisted `:104` now `:105`; dev-standards.md `:805`/`:1465` now `:1428`/`:2204`), and (b) never-allowlisted RULE-2.5 prose notes (market-watcher cycle.md:306, eod.md:106, news-scout stage-log-notify.md:45) that each sit *below* their own correctly-scoped fence and say so verbatim ("the trailing `-- <path>` **above**"), while the script's `LOOKAHEAD=10` only scans FORWARD. Line-anchored allowlist keys are inherently drift-prone; a backward lookbehind kills class (b). That script is a *different* row's AC-3 — flagged, not absorbed, and not treated as a blocker here since this row's AC-1/AC-2 were verified by direct call-site inspection.

**Lane-move had to route around a 3-byte prose-ceiling margin.** The row's prose measured 11997B against `ORCH_ROW_PROSE_CEILING_BYTES=12000`, and its live baseline is 0 (qa[] is NOT in `PROSE_CEILING_LANES`, so a qa[]->review[] move gets no grandfather) — the canonical `vc-changes` actuator's `status_note` append + `qa_changes_requested_at` would have hard-aborted at orch-apply Stage 2.5. Put the entire verdict in `verify_note` instead (it IS in `STRUCTURAL_FIELDS`, so it costs zero prose bytes), dropped `qa_changes_requested_at`, and deleted the now-stale `drain_source_lane` — prose SHRANK to 11989B. Ceiling-checked the candidate file before applying. Landed `qa[]->review[]`, `status=REVIEW`, `next_agent=po`, `redispatch_count=1`; self-verified by re-read. Full analysis: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S213.

## cycle-840 · 2026-08-25T15:54Z · FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY, direct-commit verify — CHANGES_REQUESTED (5/7 AC), qa[] -> review[]

**The fix itself is correct and I proved it discriminates, rather than reading the diff.** `183e1ad8f` is main-ancestor, touches the single claimed file, tsc 0 errors. Ran BOTH controls end-to-end on scratch fixtures via `ORCH_APPLY_LIVE_FILE_OVERRIDE` (live file confirmed untouched after each): positive — the real claim script stamped done[]-origin `FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER`, the NEW all-lane readback returned it non-empty with `_lane=done`, `status=DONE`, `secondary_dispatch_target=pm` honoured verbatim, while the OLD `review[]`-only readback returned EMPTY on that same fixture; negative — with done[] aged out, `RAG-FTS-BUILD-MEMORY-BOUND` reads back byte-identical old-vs-new, and exactly 1 row matches across ALL lanes (the all-lane widening introduces no false positive). AC-1's in-file justification is truthful: the claim script's own header does say `candidate lanes: .task_board.review[] UNION .task_board.done[]`.

**Two ACs refused, and the second one is a live negative, not an unelapsed window.** AC-6 ("a claim that actuates but reads back empty must log a distinguishable line") has ZERO implementation — the whole block still holds exactly one `log` statement, the peer-collision `SECONDARY-DRAIN SKIP`; there is no empty-readback branch at all, so an `orch-apply` ABORT swallowed by the trailing `|| true` is still byte-identical to "nothing eligible". The commit message never claims AC-6 either. AC-4 (live drain evidence): ZERO `secondary_claimed_*` stamps of ANY origin since the fix landed 12:54:37Z — newest on the board is 12:48:40Z — yet dev-team tick 15:37Z (`094c201a7`) demonstrably reached the head-decoupled QA-Drain, which sits strictly AFTER SECONDARY-Drain in the documented ILC->SECONDARY->QA-Drain->Step-1 chain, with 20 eligible union candidates. Traversed, produced nothing, left no trace — which is exactly the AC-6 symptom, now in "block never actuated" form rather than "readback lane-blind" form.

**Do not launder the 4 pre-existing review[]-lane stamps into an AC-4 pass.** They are real (`08-24T01:58:13Z`, `18:18:12Z`, `22:16:13Z`, `08-25T04:16:23Z`) and one has a confirmed matching dispatch (`SPIKE-BCTC-EXTRACTION-DORMANT` stamped 04:16:23Z target `dev-mcp-server` -> row `updated_by=dev-mcp-server` 04:23:32Z), which also falsifies the row's own premise that review[] rows had NEVER been stamped — but every one predates the fix. They prove the review[] half was never the broken half; they say nothing about this commit. Flagged to PO: AC-4's literal review[]-row bar is unreachable by construction while the oldest union candidate is done[]-origin (`FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER`, 2026-08-23T13:41:02Z) and the script re-picks strictly oldest every tick until that row moves — re-scope to "any-origin post-fix stamp WITH matching dispatch".

**Lane-move + routing deviation.** Dry-ran the vc-changes block on a fixture first (validator/conservation/prose-ceiling green, self-verify OK), then landed `qa[]->review[]` for real, `redispatch_count:1`, `qa_changes_requested_at`. Deviated from the flow's canonical `($t.owner // $t.owner_agent // "po")`: `.owner` is `qa` here, so the shipped chain emits a SELF-LOOP to an agent constitutionally forbidden from editing flow docs — used `agent-father` (implementer + `docs/agents/*/flow` owner) and recorded the deviation on the row, plus the warning that agent-father is OFF the DRS allowlist so it needs a PO/router hand-hop (or this lane's own pick). Prose headroom measured before writing (9685B of 12000B) using the checker's own `STRUCTURAL_FIELDS` exclusion, not a `tostring|length` proxy. `devteam-dispatch-gate-satisfiability.sh` at HEAD: 113 PASS / 2 FAIL, both provably unrelated — that script never reads `main.md`, and its zero SECONDARY assertions are this call site's real, still-open regression gap (agent-father flagged it; `scripts/` is outside its commit_zone). Full analysis: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md`, STEP qa-S214.

## cycle-841 · 2026-08-25T16:12Z · FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID — verify-committed: DONE_VERIFIED 6/8 AC (1 refuted, 1 met-by-state), qa[]->done_verified[]

**Score the row's PREMISE before its checklist — this title is a false accusation.** "NEITHER sanctioned engine can have produced them" + forged `_updated_by` are both FALSE, re-proved not inherited from `verify_note`: deleting ONLY the new `writeSignalsFn: () => {}` stub from `CCATO-MCP-T5-USECASE.test.ts` and re-running that one file fires **exactly 6** `NTG-GUARD-REFUSED-TEST-HARNESS-LIVE-WRITE` lines, live `orch-state.json` sha byte-identical either side. `runNarrativeTruthGate.ts:87/110` defaults `writeSignalsFn` to the REAL writer and forwards an undefined `orchStatePath` (-> `DEFAULT_ORCH_STATE_PATH`) with the frozen clock: a unit test wrote production state on every `bun test`.

**"NOT DONE" and "REFUTED AS WRITTEN" are different verdicts; only the second may pass.** AC-2 wanted producer auth in `orch-apply.sh` because "every signal_queue write passes through it". Checked rather than accepting either side: `appendSignalQueueRow` writes `writeFileSync`+`renameSync` DIRECTLY (`orchStateStore.ts:217-218`), so the leak never touched `orch-apply.sh`; and `SIGNAL_WRITER_ID="narrative-truth-gate"` (`narrativeContradictionSignalWriter.ts:56`) is a SECOND SANCTIONED producer — the check misses this leak AND rejects genuine rows. Passed the row with the refutation recorded file:line, and flagged the residual (orch-apply validates shape, never provenance) as needing a NEW row — never re-hang a real gap on refuted evidence.

**An AC can be harmful to implement, and saying so is part of the verdict.** AC-7's second clause rejects a `ts` "second-identical to an existing row", but `runNarrativeTruthGate.ts:110` passes ONE `now` into the whole finding loop — every row of one legitimate run is second-identical BY CONSTRUCTION. Marked CONTRAINDICATED so no redispatch re-demands it.

**Replay the SURVIVING population, at PER-ROW clocks.** The gate says "reconstruct all 42 rows"; 42 (later 110) are gone — the real set is 8 (6 leaked + 2 genuine). All 8 through the SHIPPED guards at each row's own wall clock (`git log -S` on its introducing commit): 6 quarantined, 2 ACCEPTED, **0 false rejects**. Per-row matters: genuine `82a3ee` sits 11.8min from its commit stamp vs a 15min tolerance, so one batch clock manufactures a false quarantine.

**EXECUTE "byte-faithful twin" claims; RUN the dedup jq.** python `build_summary` vs TS `buildContradictionSummary` on 6 inputs incl. 2 overflow edges: ALL BYTE-IDENTICAL. Shipped bash `jq` reduce run verbatim: 2 repeats -> `occurrences=3`, same-key/different-`type` separate, unkeyed rows untouched. `loadNullMarkers()` read 10 markers from `claim-tool-map.json` at runtime; its substring/case-insensitive semantics MATCH the bash classifier at `narrative-truth-gate.sh:262-271`, so apparent over-breadth is SSOT parity.

**Full-suite disjointness is structural — grep it, do not read it.** 15479 pass / 52 fail over 1286 files (dev figure reproduced exactly on a 2nd run; 1st gave 15480/51, so +/-1 is flake). Extracted the failing FILE set (17): every one holds ZERO refs to `orchStateStore|appendSignalQueueRow|narrativeContradiction|signal_queue`. 2nd forward flag on the row: `collapseOrPrepend` bumps `.occurrences` in place WITHOUT resetting `.status`, so a same-day re-emission of an already-READ finding never resurfaces as NEW — bounded to one day by `dedup_key`'s `{cycle}`, and exactly what AC-6 mandates. Detail: qa-29.md STEP qa-S215.
