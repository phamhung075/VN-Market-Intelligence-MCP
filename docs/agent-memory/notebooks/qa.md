# QA — Notebook

## cycle-378 · 2026-07-04 · FACTORY-ALERT-consolidate-dual-engines — APPROVED (Go alert-engine dual-engine consolidation, dev-team a7609fe456674beb9)

Gated dev-team's UNCOMMITTED consolidation of two duplicate alert-evaluation engines (12 files, apps/alert-engine/) onto tested `alert_pipeline.Pipeline`; `application.EvaluateAlertUseCase` now a thin DTO adapter, `cmd/server/main.go`'s `_ = alertpipeline.New(...)` discard removed (Pipeline actually wired in) | Verdict: **APPROVED**. Did not trust dispatcher's raw-verify badges — re-ran independently: `go build ./...` exit 0, `go vet ./...` exit 0, `go test ./... -count=1` 7 pkg ok/0 fail (matches exactly), `CGO_ENABLED=0 go run ./cmd/sandbox -tier=all -module=alert-engine -scenario=all` 11/11 pass, `golangci-lint run ./...` (v2.12.2) 0 issues. Confirmed the 4 claimed whitespace-only files (dtos.go/sqlite.go/telegram.go/router.go) via `git diff` are pure gofmt realignment, 0 behavior change. Scrutinized all 5 flagged reconciliation decisions against ground truth, not worker's claims: (1) store-timing (Fired decoupled from Telegram delivery) — `api/openapi.yaml:117-142` literally defines `fired`/`telegram_sent` as two independent booleans, confirming the contract text; traced that the OLD LIVE engine (`application.EvaluateAlertUseCase`, pre-diff `evaluate.go`) already stored before Telegram with `SentToTelegram` defaulting to 0 (Go zero value, field never set in the struct literal) — the engine requiring Telegram-success-before-store (old tested `alertpipeline.Pipeline`) was NEVER wired into `main.go` (`_ = alertpipeline.New(...)` discard), so no regression vs actual serving reality; repo-wide grep for `SentToTelegram`/`sent_to_telegram` outside alert-engine → 0 external readers, non-blocking. Confirmed no double-store on retry: `HasDuplicateFingerprint` dedup check runs BEFORE `StoreAlert` in the same gate sequence, so a retried identical request short-circuits at the dedup step. (2) `cooldown_sec` standardized to a constant on every branch — confirmed old live engine's `evaluate.go` explicitly hardcoded `CooldownSec: 0` only on the muted branch (inconsistency, not an invariant); `openapi.yaml:120-123` describes it as a static per-stock window value, consistent with constant; `git diff` on `evaluate_test.go` confirms no test ever asserted the old 0. (3) Ordering kept from tested engine (classify→dedup→cooldown→mute→store), not the old live engine's mute-first — confirmed every suppression branch returns `Fired:false` regardless of gate order, so a simultaneous-suppression edge only changes which `Reason` string wins, never the `Fired` outcome. (4) Channel routing via `signal-classifier.Classify` (`classifier.go:48-59`) confirmed byte-identical to the old live inline switch for all 4 valid severities (critical/high→market, medium/low→work) — plus a genuine improvement: old switch had no invalid-severity rejection (silently defaulted to work channel and fired), new classifier rejects invalid severities before evaluation, consistent with `openapi.yaml:77-84`'s strict enum. (5) `EvaluateAlertResponse` retains all 7 documented fields; adapter (`evaluate.go`) is pure DTO mapping + one `time.Now()` injection, zero orchestration logic remains in `pkg/application/`; `pkg/domain/_deprecated/services_v1.go` confirmed zero non-test imports anywhere in `pkg/`/`cmd/` (repo-wide grep) — not resurrected; dead `domain.EvaluateAlertResult` type removed alongside, 0 references confirmed. Cross-checked the 4 updated architecture docs (`docs/architecture/microservice/alert-engine/{usecases,api-reference,domain-model,testing}.md`, not flagged by dispatcher but present and accurate) against every finding above, including reading `infrastructure/telegram.go`'s `TelegramClient.Send` in full to independently verify its own claim that Send never returns a non-nil error in any branch (grounds the swallowed-Telegram-error divergence as zero live risk). DDD PASS (0 domain→infra/app imports), Security PASS (0 hardcoded secrets, 0 process.env pattern). Non-blocking notes only: `SentToTelegram` column permanently 0 now (was already the serving reality, no downstream reader depends on it); 2 unrelated untracked files in working tree not part of this diff (auditor-tier2-last-healthy.json, systemic-review incident doc) — flagged so dispatcher's commit scope stays limited to the 12 code + 4 doc files reviewed. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S1. Report: reports/TASK_REPORT_FACTORY-ALERT-consolidate-dual-engines.md. Working tree left UNCOMMITTED per dispatch instructions — dispatcher commits + flips orch-state, did not touch it myself.

## cycle-377 · 2026-07-03 · B-05-FU-SSC-503-RETRY — PASS (SSC step1 honest fast-fail, dev-vps-crawls)

Gated dev-vps-crawls's revert of FIX-BCTC-SSC-503-RETRY's 60s retry (commits `a817b5139` code+test, `33353a814` notebook/journal/report — both index-only on main, NOT pushed, matches dispatch exactly, 0 UUID leak) | Verdict: **PASS**. Confirmed PO scope-correction matched, not the original inverted spec (original backlog asked to ADD a 60s retry — the exact cause of the 17-day freeze; PO re-specced to REMOVE it and fast-fail instead). Re-ran everything myself, did not trust dev's reported numbers: `python3 vps-scripts/test_discover_bctc_ssc_fastfail.py` 7/7 PASS, `python3 vps-scripts/test_discover_bctc_title_classifier.py` 35/35 PASS (0 regression), `python3 -m py_compile vps-scripts/discover-bctc-urls-browser.py` clean, `bash scripts/audits/mock-guard.sh --files "vps-scripts/discover-bctc-urls-browser.py"` PASS exit 0. Read the full diff line-by-line, not stat: confirmed `_SSC_STEP1_TIMEOUT_SECONDS=4` is the sole timeout passed to `_ssc_get()` in step1, `import time` fully removed (`grep -n "time\."` whole-file → 0 hits, not just the diffed hunk), 2-attempt retry loop + `_SSC_STEP1_RETRY_WAIT` constant both fully gone (`grep` whole-file → 0 hits). Cross-checked the claimed 5s caller budget against ground truth rather than trusting the fix's own comment: `bctcQueueEnricherJob.ts:57 DISCOVERY_TIMEOUT_MS=5_000` + `bctcDiscovery.ts:351 timeout ?? 5_000` — both confirmed 5000ms, so the 4s cap genuinely has a 1s margin, not merely asserted. Security: fetch uses `urllib.request` against a fixed constant URL, no external value shelled out anywhere (`grep subprocess\|os.system\|shell=True` whole-file → 0 hits in code) — 0 shell-injection surface; on error returns `None` fast via a labelled stderr print (honest fail-fast, not a silent swallow). Read the new 7-test file in full: tests are meaningful (monkeypatch `_ssc_get` to raise simulated 503/timeout/404 with no real network I/O, assert `elapsed<1.0s` — tighter than the 5s AC — assert single-call count, assert the passed timeout kwarg itself `<5.0`, assert structural absence of the removed retry constant/`time` import as a named regression guard). 0 UUID/secret/process.env leak on both gated commits. DDD N/A (vps-scripts/ is outside `apps/mcp-server/src/` layer boundaries — pure Python subprocess script, no domain/infrastructure import graph applies). Flagged one non-blocking observation (not a gate blocker): `discover-bctc-urls-browser.py:1068` step2/3b download POST still uses `timeout=60`, above the 5s caller budget — confirmed a genuinely different code path (only reached after step1 already succeeded within budget; caller still hard-aborts the whole call at 5s regardless, so this cannot itself cause a NEW freeze the way the step1 retry did) — recommended follow-up BACKLOG item for hygiene/consistency, does not block. Deploy-to-VPS correctly NOT performed (code-only DoD, ops follow-up, user/auto-mode-gated live-host write) — does not restore SSC/HOSE discovery success, correctly scoped as separate external outage + separate HSX Strategy-0 root cause, both honestly reported by dev. DJ: sprint-B-05-FU-SSC-503-RETRY-qa.md §qa-S1/S2/S3. Report: reports/TASK_REPORT_B-05-FU-SSC-503-RETRY.md (dev's original + QA Gate section appended, dev's report kept intact not overwritten). Board flip left to router — did not touch orch-state.json myself.

## cycle-376 · 2026-07-03 · FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK — APPROVED (two-tier fix, ROBUST deploy-gated)

Gated dev-mcp-server's two-tier fix (commits 3badf5fe5 9 files + ce4051a7b notebook) for the legal_risk re-fire gap this same notebook flagged in cycle-375 (qa a522cb30f) | Verdict: APPROVED. Did NOT trust dev's reported numbers — re-ran everything myself: isolated new test 7/7 pass/15 expect; broadened the adjacent-suite net beyond dev's claimed file list (case-insensitive grep across `src/__tests__/` for legalrisk/alertverdict/get_legal_risk_signals/write_alert_verdict → 14 files, wider than dev's 11) → 124/124 pass/402 expect, 0 regressions. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run` confirms toolCount=183 unchanged (additive param, no new tool). Ran the FULL suite TWICE independently (not once): run1 14236 pass/42 skip/69 fail/5 errors, run2 14239 pass/42 skip/66 fail/11 errors, both 1170 files, both well under ceiling=348 (project-stats.json) — cross-run variance itself proves pre-existing flakiness (identical code, different fail/error counts both runs). Mapped every one of the 66 `(fail)` lines (run2) to 20 unique source test files, grep-confirmed NONE import legalRiskTools/alertVerdictTools/alertVerdictStore; 0 keyword hits for legal_risk/alertVerdict anywhere across either ~22K-line full log — all failures are the known pre-existing pollNews/VPS-push-log/network-timeout flake cluster. Gate item 2 (shared days=30 NOT lowered): confirmed via grep — 7 other call sites across 6 agent flows (bctc-analyst, digest-predict, unified-agent x2, fb-market-poster x2) remain bare, unaffected. Gate item 3 (TC4 repeat-fire-suppression genuinely scoped + no-suppression intent preserved): TC4/TC5/TC6 independently re-run green, AND traced `write_alert_verdict`'s call site (`stage-dispatch-log.md:24`: fires AFTER `send_telegram`) — the dedup guard sits strictly downstream of the actual CRITICAL alert dispatch, cannot suppress the alert itself; `stage-signals.md`'s unconditional CRITICAL rule is untouched, satisfying alert-policy.md's "never suppress legal_risk" clause. Gate item 4: QUICK tier LIVE (stage-bootstrap.md flow doc, no deploy needed); ROBUST tier (legalRiskTools.ts hours_back + alertVerdictTools.ts dedup) is DEPLOY-GATED — merged to main, no effect until next mcp-server rebuild. DDD PASS: neither file lives under domain/, interface→infrastructure imports are the established permitted pattern (ARCHITECTURE.md L7: only domain/ is barred from infrastructure/). Security PASS: 0 process.env/secret, mock-guard exit 0, 0 UUID leak in the 2 gated commits (notebook entry correctly uses "Provenance: router-dispatched", no raw UUID). Flagged an OUT-OF-SCOPE recurring-pattern finding (not a blocker here): 3 OTHER already-merged dev-mcp-server notebook entries (85267b624/1a9cda30b/e73a53688) still carry the raw session UUID unscrubbed, predating this task — recommend follow-up redaction commit. DJ: sprint-FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK-qa.md §qa-S1. Report: reports/TASK_REPORT_FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK.md. Board flip left to router — did not touch orch-state.json myself.

**Retention note:** cycle-375 (FIX-ALERT-COMMANDER-DEAD-NO-SLOT — PASS) and older pruned this write per AC-2 (keep last 3). Full detail preserved in `reports/TASK_REPORT_FIX-ALERT-COMMANDER-DEAD-NO-SLOT.md` and prior git history of this file (which also carries cycle-374's collapsed pointer: FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD PASS / cycle-373: FIX-FB-DAILY-FIRER-CLI-FLAG APPROVED / cycle-372: FIX-BCTC-BANK-BS-COLUMN-ORDER PASS / cycle-371: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
