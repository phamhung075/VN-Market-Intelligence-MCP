# QA — Notebook

## cycle-619 · 2026-08-09 · FACTORY-STOCK-resolve-dead-writeback — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `c0f6bae58`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, row carried no `.commit`/`.files[]`/`.owner` — derived commit `c0f6bae5842fa99ba36384b77299d081aad432d9` via `git log --all -- <detail_ref files[]>`; commit date 2026-07-23T23:51:19Z UTC is 43s before `reviewed_at` (2026-07-23T23:52:02Z) — high-confidence match. Confirmed `main` ancestor via `git merge-base --is-ancestor`. `git show --stat` matches 2/3 detail_ref files exactly (`fetchers.go`, `ports.go`); the 3rd (`_deprecated/services_v1.go`) was only the dead caller cited as deadness evidence in the approach note, not itself edited by this task — it was later deleted entirely by unrelated commit `448b41b0a` (2026-07-30, cross-service dead-code-CI-gate sweep), independently corroborating zero live importers ever existed.

Diff read line-by-line, not trusted from prose: `SaveQuote` removed from `PriceHistoryPort` interface (`ports.go`) and `SQLitePriceHistoryRepository` (`fetchers.go`); `ownDBPath` field + `CREATE TABLE market_prices_cache` + INSERT DDL all dropped; `main.go`'s `STOCK_PRICE_DB_PATH` env wiring removed — matches detail_ref's mandated DELETE-path approach exactly (no wire-it, no new hot-path write introduced). Re-verified against live `main` HEAD myself, not the commit's own claims: `go build ./...` clean; `go test -count=1 ./...` 9/9 packages pass fresh (uncached); `grep -rn "SaveQuote\|market_prices_cache" apps/stock-price/` = 0 matches repo-wide, confirming complete removal persists at HEAD, not just at the historical commit; `mock-guard.sh --files fetchers.go,ports.go,main.go` PASS. DoD fully met: SaveQuote removed from repo+port, cache DDL/INSERT dropped, no live caller broken, build+tests green, live price-serving path unchanged.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached — id not RC-VERIF-grandfathered, confirmed against live allowlist) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 755→755, signal_total 68→68). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no prior status_note/review_note field, no handoff file for this drain class). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S34. `task_release` attempted for outer `task:FACTORY-STOCK-resolve-dead-writeback` — errored `No such tool available` (no `mcp__gateway__call_tool` binding in this sub-session, same gateway-blind pattern as cycle-617); dispatcher (dev-team, session 165f4245) is the authoritative release path per its own finally-block/TTL, matching this dispatch's own INV-GATEWAY-1 framing. Pruned cycle-616 (`FIX-DEVTEAM-IDLE-CHAIN-TEST-FAIRNESS`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 617/618/619.

## cycle-620 · 2026-08-11 · FACTORY-ALERT-delete-deprecated-domain — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `314461cbd`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, row carried none of `.commit`/`.files[]`/`.owner` — no drain fields at all, predates the field-population convention). Derived commit `314461cbdd43580d38b0be30951ab2ea1ab364fa` via `git log --all -- <detail_ref files[]>` (`apps/alert-engine/pkg/domain/_deprecated/services_v1.go` + `services_v1_test.go`, per `backlog-detail.json`, since `.task_board.qa[]` row itself carried no `review_note`/`status_note`). Commit date 2026-07-24T01:21:22Z is 57s before row's own `reviewed_at` (2026-07-24T01:22:19Z) — high-confidence match. Confirmed `main` ancestor via `git merge-base --is-ancestor`; `git show --stat` matches both claimed `files[]` exactly (298L: 150+148, plus a `domain-model.md` doc update and its own dev-side decision-journal file).

Re-verified against live `main` HEAD myself, not commit prose: `apps/alert-engine/pkg/domain/_deprecated/` confirmed absent from working tree; `grep -rn "_deprecated" --include="*.go" apps/alert-engine` = 0 matches repo-wide; `go build ./...` clean; `go vet ./...` clean; `go test ./... -count=1` fresh (uncached), all 8 testable packages pass (application, domain, infrastructure, interface/http, module/alert_pipeline, primitive/cooldown-gate, primitive/dedup-key-builder, primitive/signal-classifier). `mock-guard.sh` not applicable — sole production files touched were pure deletions, nothing left to scan. DoD (`backlog-detail.json` `.dod` field) fully met: directory deleted, grep confirms no live importer, go build+tests green, generic/no runtime change (main.go was already rewired off any `domain.*` path by a prior task, unaffected by this delete). Dev's own decision journal (`sprint-FACTORY-ALERT-delete-deprecated-domain-dev-alert-engine.md`) carries a matching `task-id:` trailer.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 13→13). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no prior field, no handoff file for this drain class). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S38. `task_release`/heartbeat for outer lock is the dispatcher's (dev-team, session 165f4245) responsibility per INV-GATEWAY-1 — not called from this sub-session (no gateway binding). Pruned cycle-617 (`FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 618/619/620.

## cycle-621 · 2026-08-11 · FIX-ORPHAN-FR7-VERIFY-TOOL-REGISTRY — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `618654d96`+`557653c59`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, row carried no `.commit`/`.files[]` fields — took commits `618654d960` (handoff+WORK.md) and `557653c59f` (notebook+journal) from the row's own `dev_note` prose). Both confirmed `main` ancestors via `git merge-base --is-ancestor`. Claim is "no-op, verification-only" for FR-7: `docs/data/tool-registry.json`'s `system` group `task_heartbeat`/`task_release` entries are bare name strings inside `tools[]`, no per-tool object anywhere in the file — independently confirmed via `jq`, not trusted from prose. Read `scripts/gen-tool-registry.ts` myself: its regex captures only the string-literal tool name, never the trailing Zod schema object — structurally incapable of drifting on a param-schema change.

Re-ran everything myself, not trusted from prose: `bun scripts/gen-tool-registry.ts --dry-run` → `totalCount=183`, `system: 41 tools`, byte-identical to the committed file. `bun test src/__tests__/tool-registry-parity.test.ts` (apps/mcp-server) 17/17 pass, 39 expect() — matches claim exactly. `bun tsc --noEmit` (apps/mcp-server) 0 errors. `git show --stat` on both commits confirms no production source touched (handoff.md + WORK.md + notebook + journal only) — mock-guard N/A, consistent with the row's own no-edit claim. Confirmed the cited `taskHeartbeatTool.ts`/`taskReleaseTool.ts` split (cited as a freshness cross-check) actually exists on disk.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 13→13). `[QA] Review Record (direct-commit verify)` appended to the row as new `status_note` field (no prior status_note/review_note, no handoff-file requirement for this drain class — dev_note already carried the finding). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S37. `task_release` for outer `task:FIX-ORPHAN-FR7-VERIFY-TOOL-REGISTRY` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (same gateway-blind pattern as cycle-617/619/620); dispatcher (dev-team, session 165f4245) is the authoritative release path per its own finally-block/TTL, matching INV-GATEWAY-1. Pruned cycle-618 (`FIX-PREDICTION-SIGNALS-EMPTY`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 619/620/621.
