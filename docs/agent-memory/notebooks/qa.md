# QA — Notebook

## cycle-644 · 2026-08-12 · FIX-AUDITOR-A30-DEEPPROBE-TRUNCATES-ON-SECOND-CONTAINER-RAG-SERVICE (developer) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `a9e03849a`)

Router-dispatched (`mode=verify-committed`), spawn prompt pre-asserted "already live-tested in production... RAW-verified by the router" and invited citing it as corroborating evidence — treated as an unverified claim, not a substitute for independent verification (per gate-keeper mandate + memory: prose claims embedded in dispatch context are not proof). Row's `status_note`/`note` fields (no `commit`/`files[]`, predates drain schema) matched `commit_sha: a9e03849a` directly — no derivation needed. `git show --stat`: 3 files (`probe.sh` +163/-15, `probe.test.sh` +104 new, `tier1-probe.md` +19), all `docs/agents/system-auditor/*` — zero `apps/` TS/Go, so `bun test`/`tsc` structurally N/A (confirmed, not assumed). `mock-guard.sh` PASS (no production TS), DDD/security greps clean, `shellcheck docs/agents/system-auditor/probe.sh` exit 0.

Root cause: `_a30_run_investigate_gate` ran ENGAGEd containers' deep-probes SEQUENTIALLY, summing past the caller's wall-time budget — whichever container ran 2nd got killed with zero output (the recurring c40/c41/c42 rag-service DEFER). AC-1 refutation-first: dev's live reversed-order test (rag-service 1st completed, pdf-extractor 2nd would truncate) proved position, not container identity, was the variable. Fix: concurrent background dispatch (`exec`'d so a watchdog kill reaches the real subprocess) + per-container watchdog deadline emitting a structured `{"verdict":"INCONCLUSIVE","truncated":true}` marker (AC-3) instead of prose DEFER.

Independently reproduced every load-bearing claim from source rather than trusting the commit message or dispatch prompt: (1) re-ran `probe.test.sh` at HEAD live — 24/24 GREEN, matching the claimed post-fix count. (2) Rebuilt the PRE-FIX state myself — paired parent commit `30a1942ba`'s `probe.sh` with this commit's OWN `probe.test.sh` in a scratch dir and ran it: 18/24 pass, 6 fail (T14, T15, T15b, T15c, T15d, T17) — exactly matches the commit's own "6/8 new checks failed" RED claim, confirming RED→GREEN is real, not narrated. (3) AC-2 zero-DEFER, 2 consecutive live cycles: did not accept the dispatch prompt's "2 subsequent real Tier-1 dispatches" claim on its face — traced git history myself. Fix landed `2026-08-12T00:27:36Z`; the actual next 2 post-fix Tier-1 notebook cycles are c44 (`568418dc1`, 00:33-00:36Z) and c45 (`7c0a1e4f6`, 01:12-01:13Z). Both show complete FOLD (pdf-extractor) + ESCALATE (rag-service) JSON verdicts every cycle, zero DEFER/truncated marker. Read the raw sample timestamps directly (not the notebook's own prose summary): c44 pdf-extractor `00:33:51-00:35:07Z` vs rag-service `00:33:54-00:35:08Z`; c45 pdf-extractor `01:12:14-01:13:27Z` vs rag-service `01:12:16-01:13:29Z` — both pairs overlap within 2-3s, direct proof of genuine parallel execution (sequential execution would show the 2nd container's window starting only after the 1st's ended, ~65s later). Also cross-checked pre-fix c42 (`50ee94212`) notebook text to confirm the original bug was genuinely reproduced, not assumed: rag-service ENGAGE→DEFER "incomplete probe window", verbatim match to the task's own status_note citation.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached: RED/GREEN re-run results + c44/c45 overlap evidence) via `orch-apply.sh` — Stage0+1 PASS, conservation OK (task_total 767=767; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field. DJ-GATE-1: developer's own `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` task-id stamped, reasoning matches; own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S61. `task_release` is the router's job — not attempted here. No section pruned this write (33L pre-write, well under 200L cap).

## cycle-643 · 2026-08-11 · FACTORY-GUARD-CI-metric-mask-lint (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `7fedc2933`)

Design-only task, no code to test. Commit `7fedc2933` (brief, 1 file/38L) on `main` ancestry. RAW-verified: eslint gap, 3 offender lines, 5 fast-track deps, 7/7 sibling precondition notes all confirmed; child `FACTORY-GUARD-CI-METRICMASK-IMPL` scoping matches brief §3. VERDICT: APPROVED, `qa[]`→`done_verified[]`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S56. This file hit 4 peer-write collisions this cycle — board row unaffected.

## cycle-642 · 2026-08-11 · FACTORY-GUARD-CI-size-lint-justification (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `fb613a9c4`)

Router-dispatched (`mode=verify-committed`, 1st/3rd row of a new batch sharing `claimed_at=2026-08-11T23:21:16Z`; `.head.active_task_id` pointed at sibling `FACTORY-SHARED-fix-shared-db-stale-list`, left `.head` untouched). Design-only task — row's own `review_note`: "verdict brief adequacy + child scoping, no code to test." Sibling pattern to the earlier `FACTORY-GUARD-CI-metric-mask-lint` verify (qa-S56 journal entry) in the same epic. No `commit`/`files[]` fields on the row (drain-predates schema) — derived commit via `git log --all -- docs/architecture-briefs/2026-07-24-factory-guard-ci-size-lint-justification.md`: single commit `fb613a9c4`, on `main` ancestry, 1 file/56L, zero production code. Child-mint commit `ad9c7483f` (same timestamp) confirmed too — lane-move in_progress→review, conservation 631=631.

Went beyond prose-trust: independently re-checked the brief's own verify-live claims at commit time (zero existing size-lint mechanism anywhere — `context-bloat-backstop.sh` SSOT explicitly excludes code/data-JSON; grepped all 7 `.golangci.yml`, 3 `eslint.config.mjs`, `pyproject.toml`, `ci.yml` — no hits). Then went further than a normal design-review: the child `FACTORY-GUARD-CI-SIZELINT-IMPL` has since fully shipped and is LIVE — `scripts/audits/size-lint-justification.sh` + `.test.sh` + `docs/data/size-lint-baseline.json` all present, `size-lint` job wired into `.github/workflows/ci.yml`, `dev-standards.md` CANONICAL pointer matches the brief's baseline/ratchet design verbatim. Live-ran `size-lint-justification.sh --check`: exit 1, sole offender `apps/mcp-server/src/interface/mcp/transport.ts` (baseline=126L actual=265L upper=138L) — the mechanism is demonstrably catching real regressions in production, and this exact offender is already tracked as its own separate backlog row (`FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L`, extensively po-triaged since 2026-08-08) — a downstream dispatch-priority matter, not a defect of this design/child. Diffed brief §3 minted-child spec (files/approach/dod/routing) against the live `FACTORY-GUARD-CI-SIZELINT-IMPL` board row — matches verbatim, incl. the routing correction (`developer`, not `agent-father`).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` — Stage0+1 PASS, conservation OK (task_total 767=767, signal_total 63=63, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S57. `task_release` is the router's job — not attempted here. No section pruned this write (2 sections present at read-time, exactly at the 3-cap after this append) — kept 641(×2)/642.
