# QA — Notebook

## cycle-734 · 2026-08-14T14:33:24Z · FIX-AUDITOR-EMPTYTABLE-CHECK-NO-WRITER-DISCRIMINATOR (developer, commit `d16b7c212`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row predates the drain (no `.commit`/`.files[]` shape). Derived commit via `git log --all -- <files[] from the row>` cross-checked against `completed_at` 2026-08-06T10:40:00Z: `d16b7c212209ff83c108ee65e96d5b97230c4bbf` (committer-date 08:30:10Z, ~10min prior), commit trailer itself carries `Task:`/`AC:` matching this id. Confirmed on `main` ancestry, `git show --stat` matches all 5 `files[]` exactly.

Row carried a `po_QA_GATE_WARNING_20260806T0833` hard gate-ordering instruction: run the POSITIVE control (a genuine class-a table must still fire CRITICAL) BEFORE trusting any "FPs went quiet" result, because a sibling row (`FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT`) documented a fail-open probe class that could make everything go silent for the wrong reason. Honored literally: ran `scripts/db-empty-table-classify.sh` live against `data/live/market.db` for `daily_ohlcv`/`market_prices` FIRST — both `class=a`/`severity_ceiling=may_stay_critical` (detector genuinely reads the live DB, can still fire CRITICAL). THEN evaluated the 3 originally-firing tables: `price_alerts`→`class=c` (sole writer `priceAlertTools.ts:148`, on-demand-tool), `alert_engine_records`→`class=b` (only writer site is `apps/alert-engine/pkg/infrastructure/sqlite.go`, bucketed `excluded-other-db` — a separate physical db file, empirically correct), `pdf_documents`→`exists=false`/`class=SCHEMA-MISSING`/`always_report` (missing-vs-empty conflation closed). All 5 match the row's own AC exactly.

Ran `scripts/db-empty-table-classify.test.sh` live (not trusted from commit prose): 40/40 pass, 0 fail, 0 skip — includes T7 mandatory negative control (synthetic class-a fixture forced to 0 rows still resolves `may_stay_critical`, proving no blanket suppression) and T11 live replay against the real repo+DB (live DB present on this host, so T11 ran rather than SKIPped) reproducing the same 5-table result above. T12 WAL-conditional-safety sub-suite (a later same-file sibling fix) also passed 4/4.

Verified item (3) (label rename): `scripts/db-integrity-counts.sh` emits `ohlc_violations_count`/`scale_gt100x_count`/`vnindex_cache_rows_count`/`low_confidence_reports_count` live; repo-wide grep for the old `db1_`/`db2_`/`db3_`/`c04_`-prefixed keys returns zero live consumers, only historical "formerly"/"renamed FROM" comment text. Verified the actuator wiring is real, not prose-only: `.claude/commands/crons/cron-db-data-integrity.md` ANOMALY CLASSES point 1 calls `bash scripts/db-empty-table-classify.sh <table>` and maps class/severity_ceiling to verdict mechanically, carrying the same PO-mandated "verify positive control fires first" instruction inline. The developer's `register.md` re-sync claim was true at commit time; a later same-day PROMPT-DEDUP commit replaced the byte-identical copy with a single SSOT pointer (`register-job-db-integrity-{weekday,offhours}.md` now reference the authoring doc verbatim) — supersedes, does not contradict.

`bash -n` clean on all 3 touched shell files. `mock-guard.sh --files` on the 2 touched production shell files → "No production source files to scan. PASS." (scans `.ts`/`.go` only; zone is bash+markdown, matches developer's own note that `bun test`/`tsc`/DDD/security-grep are structurally N/A — confirmed via `git show --stat`, no `apps/**` touched). Security greps clean.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S135.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own (pre-existing, PO-authored) `status_note` field, `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write (first attempt without it hard-failed `orch-validate.mjs` — corrected). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (Stage0+1 PASS, conservation clean, task_total 736=736, signal_total 25=25) — verified post-write by re-reading the live file directly (row confirmed absent from `qa[]`, present in `done_verified[]` with `status: DONE_VERIFIED`), not trusted from the write command's own exit code alone.

Transient near-miss during this cycle, self-resolved, flagged for the record: an initial read found two OTHER same-batch sibling rows (`FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT`, `FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE`) sitting in `qa[]` with `status: QA` despite a decision-journal entry narrating `vc-approved, DONE_VERIFIED` for each — matching this exact journal's own recorded qa-S126 "2nd pass" failure mode (review-record prose appended, lane-move never executed). Re-checked minutes later: a concurrent peer QA sub-session working the same batch had already re-verified and correctly landed both (now `done_verified[]`, confirmed live) — no action needed from this session, noted only because it is the 3rd live instance of that failure shape.

## cycle-733 · 2026-08-14T14:32:56Z · FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH (developer, commits `28197b3d5`+`787703374`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha` present but WRONG — points to `3a9118e24`, which touches only `docs/WORK.md` (a log-entry commit), not the actual fix. Grep/git-log traced the real implementation: `28197b3d5` (fix(cross-service/fix-cowork-cronreg-marker-renewal): ...) + follow-up doc-fix `787703374`. Both confirmed real, on `main` ancestry; `git show --stat` matches every file the `verification_note` claims (`scripts/agents-flow/cowork-tick-preflight.sh`, `.test.sh`, `docs/agents/cowork-team/flow/preflight-error-fallback.md`, `docs/architecture-briefs/2026-08-06-cron-rearm-cross-session-dedup.md`, `docs/policies/dev-standards.md`). Corrected `commit_sha` to `28197b3d5` on this write — treated as a bookkeeping-only field error (precedent: cycle-729's `files[]` self-heal), not a substantive AC failure, since every AC independently reproduced true.

Re-ran everything myself, not trusted from prose: `cowork-tick-preflight.test.sh` → 73/73 GREEN (incl. T1 SILENT + T6 TOMBSTONED AC-2 checks, and the T-CRONREG-NEG negative control proving a transport failure on the new heartbeat call never gates/alters verdict/exit code). RED-first reproduced independently, not assumed: reverted the script to its parent commit (`28197b3d5^`) with the already-updated test file in place, reran → 71/73 (2 new FAILs: T1 AC-2, T6 AC-2), exact match to the claimed RED-first, then restored the file clean (`git status` zero diff). `shellcheck -S warning` 0 findings. AC-1 placement read at source: heartbeat sits after Step 2 presence claim/renew, before Step 2.5's tombstone early-return — fires on all 5 verdicts, `>/dev/null 2>&1`, rc never tested. AC-3: original fallback-doc call kept verbatim + pointer note added. AC-4: architecture brief §1.4 carries a dated `CORRECTED 2026-08-14` annotation under the original (stale-when-written) bullet, not a rewrite. AC-6 sibling sweep independently reproduced at source (not trusted): `system-auditor/flow/main.md` Step 0d fires `cron-registration:detect-loop`'s heartbeat unconditionally after winning fire-election (never the SKIP/EXIT branch), and `.claude/skills/cron-detect-loop/register.md` Job 2/3's anti-stale-mask gate (heartbeat age>60min forces spawn even on ALL_GREEN) is real — a healthy-steady-state renewal path, correctly not filed as a follow-up. `non_goals` honored: zero diff to any `cron-*/SKILL.md` TTL/`orphan_threshold_seconds` value. No `apps/` TS/Go touched across all 3 commits — `bun test`/`tsc` N/A, confirmed via diff stat. `mock-guard.sh` PASS (no production TS/JS matched pattern). Security greps clean.

AC-5 (LIVE VERIFICATION) note: this sub-session holds no `mcp__gateway__call_tool` binding (INV-GATEWAY-1) to independently re-call `task_list_held` live. Corroborated instead via the live gitignored telemetry file `docs/data/telemetry/cowork-tick-preflight.jsonl`, which shows real automatic SILENT/TOMBSTONED ticks continuing every ~15min after the fix commit landed (13:13:51Z): 13:35:03Z/13:59:22Z/13:59:27Z/14:07:31Z/14:25:44Z, most recent ~6min before this check — and by independently recomputing the epoch timestamps `verification_note` quotes (`date -u -r`): `1786711974`=`2026-08-14T12:52:54Z` (frozen incident value, matches), `1786714502`=`2026-08-14T13:35:02Z`, `1786714537`=`2026-08-14T13:35:37Z` — all exact matches to the claimed prose.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S137.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE (one field correction, non-blocking). `[QA] Review Record (direct-commit verify)` appended (` || ` separator) to the row's own `verification_note` field; `commit_sha` corrected; `verification.raw_probe` attached same write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (Stage0+1 PASS, conservation clean, task_total 736=736, signal_total 25=25). `.head` untouched — review-lane QA-drain is head-decoupled and this row was never `.head.active_task_id`. Task-lock (`task:FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH`) held by the dev-team dispatcher on my behalf per spawn instruction — no `task_release` call made here, dispatcher's finally-block/TTL(3600s) is the authoritative release path.
