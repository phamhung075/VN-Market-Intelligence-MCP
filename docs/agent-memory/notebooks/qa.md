## cycle-807 · 2026-08-23T13:56:00Z · FIX-SIGNAL-TYPE-ROUTING-GAP-bctc-image-fetch-degraded (docs/agents/po/flow/triage-signals-longtail.md; commit `a309c9334`) — Direct-Commit Verify, DONE_VERIFIED

Dispatcher's scoping note verified, not taken on trust: guard is red RIGHT NOW but on Pipeline-A types `flow_actuator_fix`/`system-issue`, not this row's Pipeline-B type. Reproduced live: `guard-signal-type-coverage.sh --check` prints only a Pipeline-A `FAIL` line (both types already separately backlog-tracked, own `dedup_key` rows confirmed live); no `unrouted Pipeline-B` line — per the script's own control flow (prints both FAIL blocks if both fail), Pipeline-B genuinely PASSES. `.test.sh` 23/24, TEST10 fails for that identical unrelated reason (its own comment: "the guard doing its job, do not silence"). Cross-checked commit boundary: current HEAD is many commits ahead of `a309c9334`; the 2 Pipeline-A gaps trace to a later, unrelated commit (`4e7aa7eaf`, already separately reviewed this session as `vc-changes`, qa-S47). Commit `a309c9334` ancestor-confirmed on `main`, touches exactly the 1 claimed file. Independently re-verified CI (not taken on dispatch-note claim): `gh run view 32630904807 --json headSha,jobs` → headSha=this exact commit, job `signal-type-coverage-guard` conclusion=success. `mock-guard.sh` PASS (doc-only). `bun tsc --noEmit` clean. Not OOM-class — Durability Gate N/A.

VERDICT: `DONE_VERIFIED`, zero blocking issue. Appended `[QA] Review Record (direct-commit verify)` to the row's `status_note`; `verification.raw_probe` attached same write (CI run probe). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, in one `orch-apply.sh` write. Measured own snapshot before/after: `qa[]` 7→4, `done_verified[]` 32→34 — wider than ±1 because my `jq | orch-apply.sh` pipe read the live file fresh and correctly picked up 2 already-applied concurrent peer moves (`TE-T23`, `UNBLOCK-FLEETPUSH-SIZELINT-ORCHSTATESCHEMA-NEW-OFFENDER-BLOCKS-ALL-PUSHES`, both legitimate, independently reported elsewhere — see cycle-806 and their own Task Report). Row confirmed absent from `qa[]`, present in `done_verified[]` with `status_note`/`verification` intact. `orch-apply.sh` Stage 0/1 PASS, conservation OK (task_total live=761/candidate=761). SHARED-WORKDIR NOTE: commit `55f100b2b` (mine) ended up bundling all 3 concurrent moves (mine + TE-T23 + FIX-BCTC-1345B, see cycle-805/806) since all 3 sub-sessions write the same live file — confirmed durable, pushed to `origin/main`, content independently correct per-row regardless of commit-message attribution; no redo needed. Task Report `reports/TASK_REPORT_FIX-SIGNAL-TYPE-ROUTING-GAP-bctc-image-fetch-degraded.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-25.md` STEP qa-S48. No MCP gateway binding this sub-session (INV-GATEWAY-1) — did not attempt `task_release`.

## cycle-808 · 2026-08-23T11:36:00Z · FU-RAG-DEPLOY-MEMORY (docker-compose.yml rag-service memory cap; commit `4df192e05`) — Direct-Commit Verify, OOM-class, HELD (neither `vc-approved` nor `vc-changes`)

AC-1/AC-2 (cap raise) confirmed real: `4df192e05` main-ancestor, docker-compose.yml rag-service 1g→2g/512m→1g diff matches; live container `StartedAt=2026-08-15T10:16:22Z` unbroken, `RestartCount=0`, `MemLimit=2147483648`. AC-3 (RAG-MEM-DURABILITY-BAR v2 D1-D5): refused to trust PO's outage-window prose — independently re-derived it from `cron_job_runs` in the mcp-server's `market.db` (copied out via `docker cp`, queried with `sqlite3`/`python3` since mixed ISO-vs-space timestamp formats in that table make naive string-range `WHERE` filters silently wrong — grouped by `substr(started_at,1,10)` instead): zero rows 2026-08-19/20/21, last pre-outage row 08-18 11:55:19, first real-cadence resume 08-22 12:40:04 — confirms PO's ~99.7h-silent-scheduler claim.

Target window OPEN=2026-08-22T12:40:04Z → close ~2026-08-23T11:33Z = ~22h53m, short of the D1 ≥24h floor by ~67min. In-VM kernel dmesg (`docker run --rm --privileged --pid=host justincormack/nsenter1 /bin/sh -c "dmesg -T | grep -iE 'oom|killed process'"`) buffer spans 08-20T08:29Z→now, fully covering the target window: zero rag-service `oom_memcg` hits (only unrelated hit is the tesseract/pdf-extractor container). D2/D4 PASS (identity unbroken, no mitigation in-window). D3/D5 NOT satisfied: only 3 tail-clustered point-samples this cycle (po 08:20Z=38.05%, mine 11:30Z=37.99% cold, 11:33Z=70.66% immediately post a live-traffic-triggered embed-model reload, flat over next 65s, still <85% cap) — not the ≥6-sample spread series D5 requires. Flagged for the next actor: the 1447MiB post-reload plateau is materially above the "~1002MiB measured peak" the cap-raise commit sized against — not a fail (still under cap) but worth characterising with a real series, not dismissed as noise.

DECISION: neither `vc-approved` (D1 duration + D5 sample-density unmet) nor `vc-changes` (zero evidence of a code/config defect — forcing rework onto a non-existent defect would violate CLAUDE.md's fix-root-cause rule) fits the evidence. Followed the fleet's own precedent for the identical gap (`FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` 2026-08-14T08:52Z QA cycle: "Status intentionally LEFT AT qa... only elapsed time + monitoring is missing"): held the row at `qa[]`, status/lane UNCHANGED, wrote the full D1-D4 findings + evidence + an explicit `2026-08-23T12:40:04Z` recheck floor onto `status_note` via a real `orch-apply.sh` write (not narrated only) — `qa[]` depth unchanged at 3 before/after (own read), row confirmed still present in `qa[]` with the new `status_note`/`updated_at`/`updated_by` fields set. `orch-apply.sh` Stage 0/1 PASS (16 pre-existing Stage-1g ref WARNs, 23 pre-existing prose-ceiling WARNs, 0 net-new growth), conservation OK. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-25.md` STEP qa-S157. Own commit `a41c4f945` (orch-state.json + journal, explicit pathspec). No Task Report written (no verdict reached — nothing to report against; row stays `qa[]` for a follow-up cycle). No MCP gateway binding this sub-session (INV-GATEWAY-1) — did not attempt `task_release`.

## cycle-809 · 2026-08-23T13:22:13Z · TASK-BCTC-INSPECT-LABEL-FIX (bctcInspectHandler.ts buildLabel() quarter-dup fix; commit `237fa6e26`) — Direct-Commit Verify (adapted source lane), DONE_VERIFIED

Row arrived via `review[]` (status `REVIEW`, `next_agent: qa`) not `qa[]` — no task branch (`branch: none`, matches project-wide CLAUDE.md "NO branches" policy), so the flow's literal `pipeline` checkout step doesn't apply either. Applied Direct-Commit Verify mechanics with source/target lanes adapted to the row's actual position (`review[]`→`done_verified[]`, guard on `status=="REVIEW"` not `"QA"`) since the technical precondition (branch:null, direct main commit) is identical; documented the deviation in handoff/report/journal for traceability.

Independently re-ran every check rather than trusting the developer's prose: `bun test PI3-bctc-inspect.test.ts` 49/49 (103 expect, exact match); 4 sibling regression files 60/60 (154 expect, exact match, untouched per `git show --stat`); `bun tsc --noEmit` clean; `mock-guard.sh` PASS; DDD scan clean (sole flagged import is a pre-existing untouched `interface→application` line, not a domain→infrastructure golden-rule violation per dev-standards.md:1796); security scan clean. Read the diff directly — `buildLabel()`/`normalizeQuarter()` match architect D-1 spec verbatim, AC-14 assertion + 6-case unit-test block both correct including the honestly-documented `"Q0"`→`0` edge case. BCTC Eval Gate + OOM Durability Gate both N/A.

VERDICT: `DONE_VERIFIED`. Appended `[QA] Review Record` to both the handoff file and the row's `status_note`; `verification.raw_probe` attached same write. Self-verified persisted (`done_verified[]` contains id with `status==DONE_VERIFIED`; absent from `review[]`). `orch-apply.sh` Stage 0/1 PASS, conservation OK (task_total 768→768). Task Report `reports/TASK_REPORT_TASK-BCTC-INSPECT-LABEL-FIX.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-26.md` STEP qa-S158 (NEW continuation file — `-25.md` was already over its 36000-byte cap at 44047B despite being under the 600-line cap, the FIX-DECISION-JOURNAL-SKILL-CAPCHECK-LINE-ONLY-NO-BYTE-ROLLOVER gap; rolled + logged `CAP-REACHED` marker on `-25.md`). No MCP gateway binding this sub-session (INV-GATEWAY-1) — did not attempt `task_release`.

## cycle-810 · 2026-08-23T13:56:00Z · Review-Lane QA drain, 13 rows (12 review[] next_agent=qa + 1 stranded in_progress[]) — 8 DONE_VERIFIED, 2 CHANGES_REQUESTED (one a RETRACTION of my own verdict), 2 HELD, 1 not-verifiable

Rows arrived in `review[]`/`REVIEW`, not `qa[]`/`QA`, so the freshly-shipped vc-* actuator refuses them by construction — used the cycle-809 adapted form again and filed that gap as a finding rather than a workaround.

DISPATCH FRAMING WAS WRONG IN BOTH DIRECTIONS, and reading only `status_note` would have produced 3 false rejections: FIX-COWORK-TICKSNAPSHOT-STEP47, FIX-COWORK-SPAWNFANOUT-STEP53 and FIX-AUDITOR-C04 all carry their implementation record in `verify_note`/`updated_by`, not `status_note`, and all three ARE shipped (`b545a69cc`, `d05864c40`). SPAWNFANOUT's `status_note` still literally reads "LATENT, NOT SHIPPED" while the fix is on main. Stale prose over-claims AND under-claims — check every field, and check git, before believing either.

TWO DEFECTS FOUND ONLY BY EXECUTING, NOT READING. (1) FIX-QA-VC-LANEMOVE (my own flow file) shipped an actuator that CANNOT LAND A WRITE: `next_agent: null` at qa/flow/main.md:218 violates orchStateSchema.ts:208 `z.string().optional()` (optional, NOT nullable) — live repro `VALIDATION FAILED (10 issues) ... expected string, received null` → `ABORTED: validator exit 2`; of 30 live done_verified rows 27 string / 3 key-absent / 0 null. Root cause: the `.head` idiom copied in, where null IS legal (HeadSchema:324). Second blocker in the same block: it never writes `verification.raw_probe`, which RC-VERIF requires for any DONE_VERIFIED — surfaced the instant blocker 1 was fixed. So the fix for "prose-only lane move" was an actuator that hard-rejects twice over. Its own predicted D3 prose-ceiling caveat then fired on its own rejection (9986B→13293B), forcing the detail_ref route. (2) A concurrent pm agent reported FIX-PM-DECOMPOSE unusable; I re-tested instead of accepting — claim 1 HELD (both Step-3e branches resolve `$row` from in_progress[]+active_sprints[] only; a `ready[]` parent yields an ID-LESS GHOST ROW in done[] plus the parent stranded in ready[], and the partial branch SILENTLY no-ops at exit 0), claim 2 did NOT hold (its `next_agent: null` is inside `.head`, which is legally nullable — that defect belongs to the qa row and had been carried across). Measured the real distribution rather than trusting "8 of 9": children[]-bearing rows sit backlog 10 / done 9 / ready 6 / in_progress 1.

RETRACTED MY OWN DONE_VERIFIED on that pm row ~25min after issuing it. I had found the identical not-found hazard on first pass and sized it "non-blocking fail-safe" because Zod catches it — true but irrelevant: I reasoned from the failure mode and never asked HOW OFTEN the miss fires. **A fail-safe rejection is still total loss of function when it fires on the common path; measure lane distribution before calling a not-found branch an edge case.**

CLEAN-NB-AGENT-FATHER (flagged as possible false-shipped) re-verified TRUE: 2c46efece real/on-main, archive file live, notebook 99L/9179B, 3/3 dated headings — and the "0 immutability WARNs" negative is non-vacuous, the guard fired 09:52Z today on pm.md/architect.md. HELD 2 with no defect evidence: PEK (REBUILD_REQUIRED verified true — container up 8 days, predates the 14:42 fix, so AC-8/AC-9 structurally uncertifiable) → ops; RAG-FTS (its own field says "done_verified STILL WITHHELD", time-gated 2026-09-20, prior false green on record) → po. FIX-SYSTEM-MAP-WATCHLIST not QA work at all (`blocked_reason`: "Not dispatchable work", BLOCKED/plan_only/supervised, no commit, defect still live at 34/58, work lives in 3 children) → po; it also carries NO `children[]`, a live instance of the very drift the pm row's Step 3e exists to stop.

Suite: tsc exit 0; mock-guard PASS; full `bun test` 15358 pass / 51 fail / 40 skip, exit 1 — pre-existing and unrelated (all 10 verified commits touch only .md + orch-state.json, zero .ts/.py), partly tracked as DEFLAKE-1187-POLLNEWS-DEAD-PATH. Checked for a false-green and there is none: pnpm propagates `ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL ... Exit status 1`. Peer commit 58bd68df6 cold-evicted 8 of my verified rows to archive/2026-08.json mid-cycle — confirmed normal eviction, not loss (Review Record + raw_probe intact); the retracted row was pulled back to hot review[] and its archive copy deleted (hot=1/archive=0). Endorsed 2 out-of-zone follow-ups for the router to mint (scripts/ verifier+fixtures for the qa actuators; `--destamp` mode for cowork-write-last-fired.js) — not minted here. Report `reports/TASK_REPORT_REVIEW-LANE-QA-DRAIN-20260823T1330Z.md`. DJ qa-S159/qa-S160. No MCP grant this sub-session (INV-GATEWAY-1) — no task_release attempted.
