## cycle-846 · 2026-08-25T20:41Z · FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE, direct-commit verify — DONE_VERIFIED (all AC PASS), qa[]->done_verified[]

**Verified on COPIES of real notebooks, never the hook's own self-report.** Sandboxed the actual committed `notebook-auto-prune.sh` (own git repo, own `file-size-caps.json` fixture) and ran it against copies of 4 real live notebooks — `ba.md`, `digest-predict.md` (both cited in the row's own status_note as live victims), `tran-ngoc-bau.md`, and `dev-pdf-extractor.md` (the exact file this row's dispatch cites as previously damaged by this hook). Live files sha256-verified byte-unchanged before/after every run.

**AC-1 confirmed live, not just on the synthetic fixture:** on `ba.md`/`digest-predict.md`, post-fix fires `notebook_undroppable_remainder_over_cap_breach` on iteration 1 with TRUE original section counts (e.g. ba.md: section_count=5, non_sentinel=3, sentinel=2, undroppable_byte_count=13916 > 12000 cap alone); all 3 dated sections retained byte-identical. Built a stronger real-content fixture (ba.md's own rolling section duplicated 6x, keeping 3 real dated sections, to avoid the pre-existing AC-5 cardinality==1 shortcut) — pre-fix script (checked out from parent commit) vs post-fix script produce IDENTICAL on-disk output here too: both retain all dated content because the PRIOR ticket's AC-5 already gates full-cardinality drain; the real, provable pre-fix defect is the SIGNAL being misleading (reports post-virtual-drop counts, e.g. section_count=13/non_sentinel=1 instead of true 15/3) and wasteful (N virtual-drop iterations before the safe-fail engages) — exactly matches the developer's own disclosure, confirmed by re-execution rather than accepted from prose.

**AC-2 confirmed live on the actual previously-damaged file.** `dev-pdf-extractor.md` (5 dated sections, 0 sentinel, 15935B over 12000B cap): pre-fix and post-fix scripts produced byte-for-byte IDENTICAL results — dropped the 2 true-oldest sections, retained the 3 newest, and the retained sections diffed byte-identical against the original (no in-place content mutation). Same parity confirmed on `tran-ngoc-bau.md`.

**Full suites re-executed myself:** `notebook-auto-prune.test.sh` 15/15, `notebook-compose.test.sh` 9/9 (AC-7 blast radius), `notebook-linecap-sweep.test.sh` 11/11 — all green, matching the commit's claim. `bash -n` clean; `shellcheck` shows the identical 3 pre-existing info-level warnings pre- and post-commit (0 new). All 5 `grep -c` double-fire sites (587,630,699,701,751) confirmed fixed at source — zero executable `... || echo 0` idioms remain.

**Lane-moved via the mandated `jq | orch-apply.sh` actuator** (dry-run via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first, then live), `qa[]->done_verified[]`, self-verified by re-read; RC-VERIF raw_probe fields filled from checks actually executed above. Decision journal collision (2 concurrent sessions both wrote `qa-S219`) — renumbered mine to `qa-S220`. Full data: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S220.

## cycle-848 · 2026-08-25T21:14Z · FIX-PDFX-OCR-ORIENTATION-UNDETECTED-ROTATED-BCTC-PAGES-READ-UPSIDE-DOWN, direct-commit verify — DONE_VERIFIED (all 6 AC PASS), qa[]->done_verified[]

**Neither of the two flagged risk conditions changed the method or the outcome.** Stale-container condition: never touched the long-running `-1` service container at all — every check (new unit tests, full-zone regression, the developer's own persisted probe harness) ran in a fresh `docker run --rm -v <repo>/apps/pdf-extractor:/app <image>`, so a rebuild gap is irrelevant to this verdict. Peer-collision condition (dev-pdf-extractor live on the adjacent Tesseract-confidence row, same 2 files `ocr_adapter.py`/`pek_engine_adapter.py`): `git status` stayed clean for `apps/pdf-extractor/` for the full verify window and zero commits landed in-zone between `905e32be1` and the lane-move — no torn read ever materialized; the peer's own activity was confined to its own ad hoc `ocr-conf-dbc-*` probe containers, observed via `docker ps`, never touched.

**Mechanical + regression: 34/34 new + full-suite delta exactly matches the new file, 0 new failures, checked against an isolated baseline, not a stale precedent-belief.** `test_ocr_orientation.py` 34/34 pass. Full suite post-fix: 1109 passed/1 failed/11 skipped. Built a throwaway `git worktree` at the parent commit `e9144ea75` under scratchpad (never touched the shared main tree, removed after) and ran the identical suite there: 1075 passed/1 failed/11 skipped — same single failure both sides (`test_bt3_fix3_real_ocr_fidelity`, a missing-PDF-fixture artifact of this bind-mount technique — the file exists on host at `data/pdfs/...FPT...pdf` but isn't under the code-only mount), delta is exactly +34, 0 new failures either side.

**AC-3/AC-4 independently re-measured against the real PDF, not accepted from the commit's own numbers.** Re-ran the developer's own persisted `scripts/audits/ocr-orientation-probe.py` against `VIC_2026_Q1.pdf` for all 9 cited pages: 11/34/60/67 → rotate=90, vi-score 0->17/0->15/1->77/1->42; 13/14/15/16/41 → rotate=0, `identical=True`. Every one of the 9 pages matched the commit prose to the exact integer.

**AC-1/AC-2/AC-6 confirmed by reading the diff, not the commit message's own summary.** AC-1: the falsified "BCTC tables are not rotated" premise is genuinely replaced (not just reworded) at all 3 named sites, each with an accurate corrected explanation. AC-2: the new OSD probe is routed through the shared `ocr_gateway._exec_tesseract` (new `mode="osd"` branch) — same concurrency bound/deadline/orphan-reaper as every other tesseract call, at all 3 named sites plus `page_rasterizer.py`. AC-6: page-image leg is genuinely wired end-to-end (`force` threaded `schemas.py` → `routes_rasterizer.py` → `page_rasterizer.py`, sane `False` default); the text leg is confirmed out-of-zone (a 4th OCR construction site in `apps/mcp-server`) — the escalation signal (`docs/signals/processed/pdfx-orientation-fourth-ocr-site-mcpserver-2026-08-25T203747Z.json`) and the runbook GAP note (`bctc-extraction-runbook.md:197`) both exist and match the claim verbatim, satisfying the AC's own "name it, wire only if in-zone" wording.

**Supplementary gates:** `mock-guard.sh --files` PASS on all 8 changed production files; DDD-equivalent (domain importing infrastructure) and secret/`os.environ` greps both clean. BCTC Eval Gate: curled `/api/bctc-eval/1f53ef33-...` directly on mcp-server's exposed port 3000 (qa has no MCP grant, per INV-GATEWAY-1) — 409 EVAL_NOT_COMPUTED, non-blocking per gate spec. OOM-Class Durability Gate ruled not-applicable: AC-5 here is a one-time cost disclosure for a new code path (cgroup memory.peak delta 0.0, confirmed methodology-correct in the cost-probe script), not a claim that a prior crash/OOM is now fixed-and-durable.

Lane-moved via the mandated `jq | orch-apply.sh` actuator (dry-run via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first, then live), `qa[]->done_verified[]`, self-verified by re-read; RC-VERIF raw_probe fields filled from checks actually executed above. Full record: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S222.

## cycle-849 · 2026-08-25T21:20Z · FIX-PM-3E-FAILLOUD-HOTFIX, direct-commit re-verify — DONE_VERIFIED (APPROVED), qa[]->done_verified[]

**Re-verified the corrected commit, not the one the row's status_note leads with.** The row still carries an earlier CHANGES_REQUESTED narration against `04ee05faa`; the actual fix to re-verify is `2b653dbde` (row's own `.commit` field), a one-line escape of the stray apostrophe QA's prior round pinpointed at `docs/agents/pm/flow/main.md:209` (`no-op'd` → `no-op'\''d`). Confirmed on `git show --stat`/diff: exactly 1 file, 1 line changed.

**Independently reproduced both the break and the fix, not trusted from the router's data point.** Extracted the identical fenced block (lines 169-223, fence markers at 168/224 unchanged across both commits) from both git blobs and ran `bash -n` on each: pre-fix (`04ee05faa`) — `syntax error near unexpected token ')'` at extracted-block line 43, matching QA's own prior finding and the router's independent probe; post-fix (`2b653dbde`) — clean parse, exit 0.

**Went beyond a parse check — replayed the actual jq guard logic on both branches against a full live-shaped fixture.** Built a scratch fixture (real board copy + 1 synthetic `in_progress[]` row) and ran the exact shipped jq verbatim through the real `orch-apply.sh` via `ORCH_APPLY_LIVE_FILE_OVERRIDE` (Zod/conservation/prose-ceiling gates all fired for real, not stubbed). 4/4 scenarios: non-closeout NOT-FOUND refusal (correct fixed error message rendered, jq exit non-zero, `orch-apply.sh` received empty stdin and aborted, fixture byte-identical), non-closeout TERMINAL-lane refusal (same), non-closeout happy path (`next_agent` applied correctly, exit 0, full validator PASS against the entire real board content), closeout happy path (row moved `in_progress[]`→`done[]` with `status`/`closed_at`/`children[]`, exit 0). Live `orch-state.json` confirmed untouched throughout (0 grep hits for the synthetic id, `git diff` clean on that file before this task's own lane-move write).

**Scope respected — did not fault the row for row 3's job.** The brief's own sequencing note says row 3 supersedes this block later and this hotfix does not make Step 3e work on `ready[]`/`backlog[]` parents; verdict is scoped to AC-4 (distinct refusal per class, live file byte-identical) and AC-5 (shell wrapper propagates orch-apply failure) only, both confirmed met.

Lane-moved via the mandated `jq | orch-apply.sh` actuator, `qa[]->done_verified[]`, self-verified by re-read; RC-VERIF `raw_probe` fields filled from checks actually executed above. Full record: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S223.

## cycle-850 · 2026-08-25T23:46Z · FIX-MCPSERVER-PDFOCRWORKER-OCRONEPAGE-NO-ORIENTATION-4TH-OCR-SITE, router-dispatched AC-2/AC-3 close — DONE_VERIFIED, review[]->done_verified[]

**Never trusted the confidence field, exactly as instructed — the row exists because it self-reports high confidence on garbage.** Read raw extracted TEXT before/after on every named exemplar page instead. Resolved the real filenames myself rather than trusting the cited names: DBC pages 12/15 garbled only in `DBC_2024_Q4.pdf` + `DBC_2025_Q4.pdf` (byte-identical duplicate PDFs under distinct filename keys) — every other DBC quarter file already held clean text at those pages, so a blind "run it on DBC" would have been wrong.

**AC-2 (live DB invalidation):** dry-run then `--apply` via `scripts/migrations/reextract-pdf-ocr-orientation.ts` against the real bind-mounted live DB (`DB_PATH=data/live/market.db`, not the stale host `data/market.db`) for all 3 resolved filenames. VIC (71pp) ran ~7min in background; both DBC files ~1min each. Row counts preserved post-migration (71/18/18).

**AC-3 (after-measurement):** BEFORE = reversed/mirrored mojibake on every sampled page (DBC 12/15 both files; 13 of VIC's 19 cited rotated pages: 10,11,31,34,35,48,57,60,61,65,66,70,71). AFTER = correct upright Vietnamese with proper diacritics on every one (e.g. DBC p12 "Tăng, giảm TSCĐ hữu hình", VIC p60 "PHỤ LỤC 1 — CƠ CẤU TỔ CHỨC"). Non-rotated control pages (VIC p1/p3/p5) confirmed unaffected. Does not touch `bctc_refined_units`/`window_status` — out of this row's scope by design.

**Found + disclosed, did not silently absorb or drop:** `VIC_2025_Q4.pdf` + `VIC_2026_Q2.pdf` carry the same pre-fix garble, unremediated — outside this row's named scope, filed as a follow-up signal (`docs/signals/pdfocr-orientation-corpus-additional-affected-files-20260825T234250Z.json`) to `po` rather than expanding scope unbounded.

Lane-moved via the mandated `jq | orch-apply.sh` actuator (dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first), `review[]->done_verified[]`, self-verified by re-read on the live file. RC-VERIF `raw_probe` filled from checks actually executed above. Full record: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S224.

## cycle-851 · 2026-08-26T00:35Z · FIX-DRS-CLAIM-TRUSTS-CACHED-DISPATCH-LANE-NOT-EFFECTIVE-NEXT-AGENT, direct-commit verify — CHANGES_REQUESTED (AC-4 not met), qa[]->review[]

**AC-1/AC-2/AC-3/priority-ordering independently re-verified PASS — kept.** Built my OWN adversarial jq fixtures (not the shipped ones) against the real committed scripts/devteam-backlog-claim-design-router-sweep.jq: stale cached dispatch_lane='ba' vs fresh board next_agent='architect' -> claims 'architect' correctly; null dispatch_lane+resolvable -> claims correctly, never null; null dispatch_lane+unresolvable -> REFUSES, head stays idle; stale P1 stamp at lower array index vs fresh P0 at higher index -> P0 wins. All 8 of the commit's own new isolated regression fixtures (AC-DRS-*/AC-SLS-PRIMARY-*) PASS. A/B via a throwaway git worktree at the parent commit (removed after) with IDENTICAL current live board data: same 3 pre-existing failures (SLS gate, AC-EVICT-1/3) reproduce byte-identically pre-fix and post-fix — 0 regressions from this commit.

**AC-4 (this row's own addendum) NOT satisfied — verified at the PREDICATE level per dispatch instruction, not the diff.** The row's verify_note claims 0/8 pre-existing stamps needed a change and blames the 3 residual mismatches on "board edit didn't bump updated_at." Independently re-derived scripts/lib/devteam-eligibility.jq's effective_next_agent(): when both sides are non-empty but detail.updated_at is ABSENT, the recency conjunct is unconditionally false and detail always wins, no matter how fresh board is. Live-tested via the real included predicate against live orch-state.json on 2 of the row's own named 3 rows: FIX-COWORK-LASTFIRED-DECOUPLE-FROM-DELIVERY (board=pm @ 22:30:08Z fresh, detail=architect no-ts) resolves 'architect' not 'pm'; FIX-BCTC-BANK-SUMMARY-MAPPING (board=architect @ 22:30:08Z fresh, detail=ba no-ts) resolves 'ba' not 'architect'. Board IS bumped in both cases — the causal note is FALSIFIED, no board bump can ever fix this. Also reproduced the scope independently: 72/104 (~69%) of all next_agent-bearing backlog-detail.json items lack updated_at, closely matching (within 1) an existing signal (rtr-20260825T2258-effnextagentnullts, filed 2026-08-25T22:58Z, ~79min before this row was even claimed) that already escalates this exact defect to architect as a routing-semantics (a)/(b)/(c) call — not duplicated, cross-referenced instead.

Lane-moved qa[]->review[] via the mandated `jq | orch-apply.sh` actuator (dry-run via ORCH_APPLY_LIVE_FILE_OVERRIDE first), next_agent=po (row's own .owner), redispatch_count 0->1, self-verified by re-read on the live file. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S226 (cap-breached mid-cycle at 59172/36000B, rolled to -30.md for future entries).

## cycle-852 · 2026-08-26T03:04Z · FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS, direct-commit verify (AC-3 redispatch) — DONE_VERIFIED, qa[]->done_verified[]

**Diffed the actual blobs, not the commit message.** `git show 38c013342e^..38c013342e` confirms exactly 10 rows destroyed (9 done_verified + 1 done), zero write to `archive/2026-08.json` despite the commit message narrating one — root claim confirmed real, not assumed. AC-1/AC-2/AC-4 re-confirmed fresh (not re-trusted from the 08-23 QA pass): `verify_cold_archive_write()` present in `scripts/orch-cold-evict.sh`, wired both pre-rename (temp) and post-rename (real archive path), aborts before hot delete on either mismatch. Re-ran `scripts/test/orch-cold-evict-tests.sh` myself: 67/69, TEST 12a/b/c (this row's own PATH-shadowed-`mv` regression, reproducing 38c013342e's exact shape) all PASS; the 2 TEST 7 fails pre-date this fix (commit 407afb6a4, same-day-earlier, unrelated `done[]` predicate change) — live orch-state.json checksum unchanged before/after. Exercised the live code path against a full scratch COPY of the real board+archive dir (never live): `--dry-run` and a real write both exit 0, `SYMMETRY-CHECK OK` both call sites.

**AC-3 (this redispatch's focus) verified complete, all 10 victims accounted for on both sides of the diff.** 6 restored 08-23 (3 still live in `done_verified[]`: FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW / FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-HOOK / OPS-COWORK-GUARANTEED-SLOT-INSTALL; 3 — CCATO-MCP-T1/T2/T4 — since legitimately re-cold-evicted post-fix, symmetric commit `0a3a7f5b7` 125/125) + 4 restored/retired 08-26T00:21:44Z via commit `7320028e1`: FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE + FIX-DEVTEAM-QADRAIN-HEAD-WRITE-CONDITIONAL restored then legitimately re-cold-evicted (symmetric commit `9e656568f` 230/230) into `archive/2026-08.json` carrying honestly-reconstructed (never fabricated-fresh) `raw_probe` fields, cited commits (`f51ed9ede2`/`e000e91f1`/`9fe706fa2`) all confirmed main-ancestor, both fixes independently re-confirmed still live in source; FIX-FB-WEEKEND-DEDUP-GATE restored to `done[]` (status DONE, no raw_probe gate); FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0 retired to `task_board.archive[]` as CANCELLED, superseded_by CONTAM-10-WRITER-H (confirmed live DONE_VERIFIED with its own raw-probe). Zero fabrication anywhere in the trail.

**Related-signal check (telegram 5165, "orch-cold-evict.sh exit 1"):** traces to an already-tracked, separate, self-healing defect — `FIX-COLDEVICT-CAS-RETRY-BUDGET-3-EXHAUSTS-UNDER-NORMAL-PEER-WRITE-PRESSURE` (backlog[]) — the CAS retry-loop code (`MTIME_CAS_RETRIES=3`) is untouched by this row's commits and has multiple prior occurrences that self-heal next tick; not a resurfacing of this row's fix. Could not read report 5165's literal body (qa has no MCP/gateway grant, INV-GATEWAY-1 by design), but every post-fix cold-evict commit visible in git log through 08-26T03:11Z is symmetric and clean.

Lane-moved `qa[]->done_verified[]` via the mandated `jq | orch-apply.sh` actuator (dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first), claim identity (`owner_client_session`/`claimed_at`/`claimed_by`) cleared same write, `.head` untouched (pinned to a different live peer row throughout), self-verified by re-read on the live file.
