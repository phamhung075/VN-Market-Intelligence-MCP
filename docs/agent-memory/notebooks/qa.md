# QA — Notebook

## cycle-455 · 2026-07-15 · ALPHA-S2-FOREIGN-FLOW-WRITE-RACE — APPROVE (whole-epic merge-gate, router relay)

Feature already on trunk + CI-green (HEAD 4491a1f2e) — router-orchestrated relay, sibling of cycle-454, router owns epic close/mutex. RAW-ran the new `ALPHA-S2-FF-intraday-5m-compactor.test.ts` myself: 6 pass/0 fail/51 expect() — exact match to dev's claim. Own 10-file additive-regression sweep on pre-existing foreign-flow write-path tests (1132/1406b/1503/1275/FIX-1275/1392/1407/1517/1986/1491): 97 pass/0 fail/272 expect() — zero regressions to `upsertForeignFlow`/`writeForeignFlowToOhlcv`. `1190-pipeline-watchdog.test.ts` 16/16 (confirms `schedulerFileCount===67`) + `FACTORY-SCHEDULER-job-table-registry.test.ts` 15/15. `bun tsc --noEmit` standalone exit 0. `mock-guard.sh` PASS.

All 8 testable AC (#1-#8, brief §8) mapped to covering test/code-read, all PASS: DDL byte-match; Step 6b additive `INSERT OR IGNORE` + rolling-24h purge in its own inner try/catch (zero change to the two pre-existing upserts); `lastNonNull` COALESCE-style fold confirms LAST-value-in-bucket (grepped for min/max/OHLC logic — zero matches, not copied from the sibling's OHLC shape); idempotency/gap-tolerance/no-market-hours/forward-preservation all covered by the new test's own AC-tagged titles; startup one-shot wired non-fatal; cron+docs registered in all 3 places. AC#9 (board `zone` fix) correctly out of QA scope, router-owned.

Docs-sync arithmetic checked past the surface diff, not just eyeballed: `cron-registry.json` `.jobs[]`=68 vs `schedulerFileCount`=67 looked like a fresh 1-off drift — traced the full commit history (`43f4c8a22`→`b4224e278`→`c60774ef8`→`a1265227e`) and confirmed `jobs_len − schedulerFileCount = 1` is a STABLE pre-existing baseline invariant predating both ALPHA-S2 epics; SUB4's +1 bump (66→67) correctly preserves it (unlike the sibling's own since-corrected +2 scope-creep). `system-map.json` `.crons` length=68 matches `jobs_len` exactly.

Out-of-scope integrity: `git show be8f8b032 d0da2b89d 8c67947de a1265227e 4491a1f2e --name-only` = exactly 9 files, all `apps/mcp-server/src/{infrastructure/db,interface/mcp/routes,scheduler,__tests__}/`+docs; zero `domain/`/`application/` files touched; `upsertForeignFlow`/`writeForeignFlowToOhlcv` absent from all 5 commits — FIX-half (`3201c86cc`) confirmed untouched, not re-opened. Security: 100% parameterized SQL, zero `process.env`, zero `any`, zero new secrets, DI seam (`db?`/`nowMsFn?`) clean.

VERDICT: APPROVE. No blocking issues. Non-blocking: full `bun test` suite hangs on a pre-existing sla-monitor/reaper interval-leak (test-hygiene backlog item for po, unrelated to this epic — not run per dispatch instruction). Did not push production code (already CI-green), did not touch `orch-state.json`/`task_board`, did not modify SUT. Task Report: `reports/TASK_REPORT_ALPHA-S2-FOREIGN-FLOW-WRITE-RACE.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S12.

## cycle-456 · 2026-07-15 · ALPHA-S2-OMO-LIQUIDITY-CRON — APPROVED, DONE_VERIFIED (final relay hop 4/4)

RAW-verified dev-mcp-server commit ae45fd0e7 (9 files), final hop after router's own RAW-verify (36/36+tsc0 already confirmed) — did not redo that smoke, went deeper into branch behavior instead. Read `sbvOmoLiquidityCronJob.ts` in full: all 3 branches match brief §3/§4 exactly — HARD fail (`macroFetch` `ok:false`) → `sendTelegramBug()`+`logger.error` every time (test: `notifyCallCount===1`); SOFT fail (`omo.is_estimate===true`) → `logger.warn` ONLY, zero Telegram call (test: `notifyCallCount===0`); success (`is_estimate===false`) → `logger.info`, zero DB write — proven by a structural source-inspection assertion (greps compiled file for `getDb`/`.prepare(`/`bun:sqlite`, not a mocked-DB trap). 15s bounded deadline confirmed under the 60s NFR-2 cap. Confirmed genuine reuse via `git log`: `macroFetch<T>()` predates this commit (`c76a97764`), `LiquidityStateResponseSchema` predates it too (`5c2f4f631`) — neither reimplemented.

Ran all 3 test surfaces myself: new `ALPHA-S2-OMO-LIQUIDITY-CRON.test.ts` 5/5 (18 expect, incl. a 4th schema-mismatch branch beyond brief's 3); `1190-pipeline-watchdog.test.ts` 16/16 (`schedulerFileCount===68`); `FACTORY-SCHEDULER-job-table-registry.test.ts` 15/15. `bun tsc --noEmit` exit 0. `mock-guard.sh` PASS. DDD: file imports `infrastructure/` directly — confirmed established scheduler-layer composition-root convention (byte-matched sibling `macroIndicatorRefreshJob.ts`), not a domain violation. Zero `process.env`/secrets. Cron registered in all 3 doc places, `schedulerFileCount` 67→68 consistent. Two non-blocking pre-existing observations (not introduced by this task, traced via `git log`/`git show <commit>~1`): (a) the 1190 test's own `it()` title string never tracked the asserted value across 6 prior bumps (stale since inception at 43); (b) `system-map.json`'s `.crons[]` length carries a stable +1 offset vs `cron-registry.json`'s `schedulerFileCount` that pre-dates this commit. `project-stats.json` confirmed generator-untouched (`--dry-run`: toolCount/cronJobCount unchanged; its own `schedulerFileCount` field is a dead/uncomputed carry-over, distinct from cron-registry.json's field of the same name).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.in_progress[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation-check PASS, task_total=577 both sides, in_progress 1→0, done_verified 42→43). `.head`/`.task_board.head` set idle — was the active head task. Did NOT touch/claim/release the umbrella chain-mutex (router-owned). Committed+pushed `71f1422ec` (origin==local AHEAD=0, pre-push tsc green). Unblocks `ALPHA-S4-REGIME-GATE-V1`. Task Report: `reports/TASK_REPORT_ALPHA-S2-OMO-LIQUIDITY-CRON.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S13.

## cycle-457 · 2026-07-15 · ALPHA-S2-RAG-FTS-REBUILD-CRON — BLOCKED (rag-service OOM, not a code defect)

RAW-verified commit `35cc8cd56` (11 files) against `2026-07-15-alpha-s2-rag-fts-rebuild-cron.md` — code clean: `ragRebuildFts()`/`runRagFtsRebuildCron()` single-branch HARD-fail matches brief exactly, registered in cronConfig.ts (`15 20 * * *` UTC)/schedulerJobTable.ts/3 docs, `schedulerFileCount` 68→69 consistent. Own RAW test run: new test 4/4 (18 expect), `1190-pipeline-watchdog` 16/16, `FACTORY-SCHEDULER-job-table-registry` 15/15 = 35/0. `tsc --noEmit` exit 0, `mock-guard.sh` PASS, DDD clean (scheduler→infrastructure, established convention, matches OMO sibling).

Confirmed live mcp-server container (Up since 2026-07-14T20:14:37Z) predates this commit — `docker exec` grep of the running container's `schedulerJobTable.ts` = 0 matches for `ragFtsRebuildCronJob` (also 0 for sibling `sbvOmoLiquidityCronJob`); direct `cron_job_runs` table query (91 distinct job_names) confirms neither new cron has ever fired — deploy-pending, mcp-server rebuild is ops-gated, not attempted.

Per dispatch instruction, attempted the LIVE BM25 round-trip directly against rag-service (bypasses the mcp-server deploy-pending gap — same call `ragRebuildFts()` makes). Found a REAL, REPRODUCED (2/2) blocker: `POST /admin/rebuild-fts` OOM-crashes the rag-service container at its current ~56,254-row corpus (4x the design brief's 14k-row assumption). `docker stats` showed memory climb to 97.7%/99.9% of the 768MiB `deploy.resources.limits.memory` cgroup ceiling (docker-compose.yml) on both a lazy hybrid-search trigger (RestartCount 258→259) and a direct, isolated `/admin/rebuild-fts` call (259→260, ~250s elapsed, well past the 90s cron deadline and the brief's 30-60s estimate) — neither attempt produced a completed rebuild, so the nonce-index→MISS→rebuild→HIT sequence could not be executed (the rebuild step itself never finishes). Host has ample headroom (16GB, other containers well under their own limits) — this is rag-service's own cgroup ceiling, not host-wide pressure. Stopped at 2/2 (further attempts = more live-service disruption, zero new evidence).

VERDICT: BLOCKED. Not a defect in `35cc8cd56` — mcp-server code/tests/registration are clean and mergeable as-is. Blocker is rag-service capacity: `_build_fts_index()` (unmodified, already-shipped DFR-P3) cannot complete within the 768MiB limit at current corpus scale, and even where memory hasn't yet crashed it, elapsed time already exceeds the 90s deadline 2.7x. Deploying this cron as-is risks a nightly rag-service OOM-restart (service-wide RAG outage) — worse than the silent gap it fixes. Left board row `in_progress[]` untouched, no DONE flip, no lock touched. Recommend architect/ops: raise rag-service memory limit and/or stream `_build_fts_index()` and/or re-tune the 90s deadline, then re-run this round-trip. Task Report: `reports/TASK_REPORT_ALPHA-S2-RAG-FTS-REBUILD-CRON.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S14.

## cycle-458 · 2026-07-16 · UC-ASL-P2 — CHANGES_REQUESTED (dev→qa relay, sprint ULTRACODE-AUDIT-FIXALL)

RAW-ran `scripts/emit-audit-signal.test.sh` 6x back-to-back — 48/48 every time, 0 flake (guards the `e35634fd7` row-id race; own stress test: 500 tight-loop `_gen_row_id()` calls same-second, 0 collisions). `context-bloat-backstop.test.sh` 2/2. AC-1/AC-2/AC-4/AC-5/AC-6 all confirmed via diff-review (`git show 35d408423`/`319e4f40c`) + live grep (`docs/references/`+`scripts/` clean of the deleted-file reference; historical-doc hits exempted per AC-5 wording).

**AC-3 BLOCKING**: `scripts/emit-audit-signal.sh:232` feeds `--category-type` straight into `post_agent_signal`'s `signal_type` arg — a closed Zod enum (`agentSignalStore.ts:39-50`, 11 members, `signal_feedback` among them but NOT `data_stale`/`db_integrity_breach`). Live-probed via real gateway call: `MCP error -32602 invalid_enum_value` for `data_stale`. Then ran the real (unmocked) script with `main.md` site-1's exact args against a scratch orch-state copy: `[emit-signal] ABORT e1-failed ...`, `signal_queue.rows` count unchanged (2→2) — E-3 never reached. This hits main.md sites 1 (Tier-2 `data_stale`) and 2 (Tier-3 `db_integrity_breach`) — the two highest-frequency audit categories — every real invocation loses the signal-queue row AND the Telegram alert. Sites 3/4 (`--e3-only`) and tier1-probe.md sites 5/6 (`signal_feedback`, valid enum member, unchanged from legacy) are unaffected. Root cause: FR-2/architect-design-1 unified the E-3 row's free-form `type` field with E-1's enum-constrained `signal_type` arg; legacy code deliberately kept them separate (hardcoded `signal_feedback` for E-1 at every site). Fix is small/bounded (decouple the two fields in `_run_e1()`), not a redesign — recommend fixer round 1, with a parallel note to architect to correct FR-2's wording so it isn't re-taught.

VERDICT: CHANGES_REQUESTED. Did not promote board/children to `done_verified`, did not touch `orch-state.json`/`.head`, did not commit/push production code, did not merge. Task Report: `reports/TASK_REPORT_UC-ASL-P2.md`. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S4.

## cycle-459 · 2026-07-16 · CI-RED-da847805-FIX — APPROVED (RAW-verified, router promotes)

Gate: `gh run view 29486509712 --json conclusion,headSha,jobs` → `conclusion:"success"`, `headSha` byte-matches fix commit `456851797`, `bun test` job individually `success` (completed 09:19:02Z). `git merge-base --is-ancestor da847805231d0252ed6e092a4f5b4f7d678fed67 456851797...` confirms NEW sha downstream of red, not a same-sha re-run.

Dead-code claim RAW-checked (not badge-trusted): repo-wide grep for real `import`/`require` statements (not comments) referencing `_deprecated`/`@lancedb/lancedb` in `apps/mcp-server/src/**` = zero. Two textual false-leads ruled out: `checkLancedbDrift.ts` (unrelated audit-check, DI'd count fn, doesn't import the deleted module) and `_deprecated/fetchers/reuters.ts` (different unrelated subsystem). `rag/_deprecated/` dir confirmed deleted (`ls` → no such file). `apps/rag-service/` (port 5002, Python) confirmed sole live LanceDB path via `ragHttpClient.ts` base URL + `lancedb` imports in `repositories.py`/`config.py`/tests.

Highest-risk check — security coverage for the deleted `security-sql-injection.test.ts`: read `apps/rag-service/infrastructure/repositories.py:105-134` directly, confirmed `_validate_level`/`_validate_action_code`/`_sanitize` are real live functions applied inside `LanceDBVectorStore`'s query-building path (the active LanceDB boundary) — same guard class as the deleted TS functions, coverage not lost, just relocated to the live boundary.

Own corroboration (non-blocking, cheap): `bun test` on 6 direct rag-consumer sibling files = 40 pass/0 fail/86 expect, zero segfault; own `bun tsc --noEmit` exit 0; `git show --stat 456851797` = exactly 11 files matching dev's described scope 1:1.

VERDICT: APPROVED. Did NOT self-promote review→done_verified (router's job per gate instructions) — did not touch `orch-state.json`/`.task_board`. Task Report: `reports/TASK_REPORT_CI-RED-da847805-FIX.md`. DJ: `sprint-CI-RED-da847805-FIX-qa.md` §qa-S1.
