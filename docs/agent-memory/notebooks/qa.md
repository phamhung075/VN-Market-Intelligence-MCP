# QA — Notebook

## cycle-636 · 2026-08-11 · FIX-MACRO-LIQUIDITY-STATE-HANDLER-EXCEEDS-CRON-15S-DEADLINE — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `e10e2a500`, Go pilot)

dev-team Review-Lane QA-Drain spawn (`qa[]` row, `branch:null`). First Go-pilot (apps/macro-indicators) row through my Direct-Commit Verify path — TS/Bun conventions swapped for `go test`/`go vet`/`go build`/`golangci-lint`. Row's own `commit_sha` (`eb6524da9`) was STALE — `git merge-base --is-ancestor` FAILS (orphaned/rewritten hash, byte-identical commit message to `e10e2a500` which IS on `main`); used the dispatcher-supplied `e10e2a500` instead, `git show --stat` matches all 8 claimed files (2 application, 3 infrastructure, 3 arch-docs). Corrected `commit_sha` on the row in the same write rather than leaving the stale value.

dev-macro-indicators wrapped both `LiquidityStateUseCase.Execute`'s upstream SBV HTML fetches (`policyRatesProvider.FetchPolicyRates` + `omoProvider.FetchOMO`, previously raw-ctx/30s/45s independently) in ONE shared `context.WithTimeout(ctx, domain.FetchBudgetSec*time.Second)` (8s), mirroring the pre-existing BOP/NSO-chain "whole chain, one budget" idiom. Re-ran REAL verification, not trusted from prose: `go test ./pkg/application/...` + `./pkg/infrastructure/...` targeted, then full `go test ./...` across all 12 packages — 0 fail, incl. `TestFetchDeadline_LiquidityState_BothHanging_SharedBudget` at exactly 8.00s (proves ONE shared budget, not stacked 8s+8s=16s which would still miss the 15s cron deadline). `go build ./...` clean, `go vet ./...` clean, `golangci-lint run` (Fence-A/B/C depguard) 0 issues. `mock-guard.sh --files` PASS on the 3 touched production `.go` files (script is language-agnostic via `_test.go` filename exclusion, not TS-only). DDD direction verified: `pkg/domain` imports zero `pkg/infrastructure` (clean); infra importing domain for the shared const is the correct direction. Security greps clean. BCTC eval gate N/A (unrelated data domain).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` + corrected stale `commit_sha` in the SAME `orch-apply.sh` write (Stage0+1 PASS, conservation OK — task_total 769→769, signal_total 41→41). `.head` already idle/null, not pointing at this row — left untouched. DJ-GATE-1 pre-satisfied by dev-macro-indicators (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-macro-indicators.md` §S4) before dispatch; own QA entry written to `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S50 (2nd entry of that label this file — benign concurrent-session numbering collision with a peer's cycle-635 entry, both entries land intact, non-corrupting). Residual out-of-zone defect (mcp-server error-string conflation) correctly left untouched by dev, flagged for a follow-up FIX row — not this row's concern. New reusable script: `scripts/qa-verify-fix-macro-liquidity-state-cron-deadline-doneverified.jq`. Pruned cycle-633 (oldest, `FACTORY-INFRA-split-ssc-fetchers`) this write per AC-2 retention (3-section cap) — kept 634/635/636.

## cycle-637 · 2026-08-11 · FACTORY-SCHEDULER-dedup-briefing-formatters (dev-mcp-server) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `eb9f87f38` + `2f5232b0d`)

Router-dispatched (`mode=verify-committed`), row's own `review_note` claimed: 5 shared briefing formatters (globalSnapshotSection/foreignFlowSection/moversSection/alertLines/vnIndexFreshness, all ≤120L) extracted into new `scheduler/briefings/format/` subfolder, breaking the franceSummaryJob↔morningBriefingJob↔eveningSummaryJob job→job cross-import cycle (3 edges); thin re-export lines kept on all 3 job files so 9 existing test files importing formatters directly from job files resolve unchanged. Confirmed both `eb9f87f38` (code, 8 files) and `2f5232b0d` (journal+notebook) on `main` ancestry; `git show --stat eb9f87f38` matches exactly the claimed 8 files.

RAW-verified, not trusted from prose: independently re-derived the "9 byte-diffed function bodies" claim — extracted pre-refactor bodies from `eb9f87f38^` (job files) and post-refactor bodies from `eb9f87f38` itself (not current HEAD — `foreignFlowSection.ts` was touched again by an unrelated later commit `8521c4a23`, now 77L not 45L, still ≤120L so the claim's line-count bound holds). All 9 units (`deltaArrow`, `formatGlobalSnapshotSection`, `formatForeignFlowSection`, `fmtVolume`, `fmtMoverLine`, `formatMoversSection`, `severityLabel`, `formatAlertLines`, `isVnIndexFresh`) diffed byte-empty. Grep-confirmed zero job→job cross-imports remain fleet-wide in `scheduler/briefings/` — all 3 jobs import only from `./format/*.js`; confirmed the dead imports the journal claims were dropped (`sectorPeers` out of eveningSummaryJob.ts, `TelegramMessageFactory` out of franceSummaryJob.ts) moved cleanly into the format/ files with no orphan left. DDD: infrastructure/application import greps on the 8 touched files DO hit — correctly not a violation, interface/scheduler layer legitimately depends on both (pre-existing self-documented pattern in each file's own DDD-layer docblock), no domain-layer file touched, no reversed dependency introduced.

Independently re-ran, not trusted from prose: targeted 9-file suite (`1783-foreign-flow-bulletin`/`1503-ohlcv-foreign-flow`/`1784-sector-alert-format`/`1552-evening-vnindex-freshness`/`1511-morning-briefing-global-snapshot`/`1512-evening-global-snapshot`/`1424-evening-sector-aggregation`/`1794-eod-vol-rsi`/`FIX-DIGEST-FOREIGN-FLOW-ZERO-PAD-TOPN`) → 89 pass/0 fail/188 expect() calls (matches claim exactly). `bun tsc --noEmit` clean. `mock-guard.sh --files` PASS on all 8 touched production files. Security greps (process.env/password/secret/token) clean. Did not re-run the claimed full-suite 2x (14748/52-fail flaky-class) — targeted suite + tsc already satisfies the flow's re-verification bar; that class is independently documented elsewhere.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 766=766, signal_total 57=57; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise, not caused by this write). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. DJ-GATE-1: `sprint-FACTORY-SCHEDULER-dedup-briefing-formatters-dev-mcp-server.md` (task-id stamped, S1-S3) present before flip; own QA entry `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S51. `task_release` is the router's job per this task's own dispatch note (dispatcher-wrap lock held by router) — not attempted here. Pruned cycle-634 (oldest, `TE-T03`) this write per AC-2 retention (3-section cap) — kept 635/636/637.

## cycle-638 · 2026-08-11 · FACTORY-PDF-extract-tesseract-config — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `cfe0a78d7`, first Python pilot)

dev-team Review-Lane QA-Drain spawn (`qa[]` row, `branch:null`, `mode=verify-committed`). First `apps/pdf-extractor` (Python/pytest) row through my Direct-Commit Verify path — flagged in the spawn prompt as a known PSM-6-drift risk class (`project_bctc_ocr_psm_drift`), so verified the `--psm 6` warning was preserved exactly, not just relocated. Row's own `review_note` claimed: extracted the copy-pasted Tesseract lang/psm/DPI config (5 named call sites, each with its own DO-NOT-REMOVE `--psm 6` warning) into new `infrastructure/tesseract_config.py`; `generic_md_table_extractor.py` resolved to 2 real files behind an existing FACTORY shim (`generic_md_table/extractor.py` + `unit_ocr.py`) — 6 real call sites total. Confirmed `cfe0a78d7` (+`43d0c92ad` memory) on `main` ancestry; `git show --stat` matches all 6 real call sites + the docs file exactly.

RAW-verified, not trusted from prose: new module's boxed docstring warning ("DO NOT remove or change TESSERACT_PSM6_CONFIG") is now the sole authoritative copy, citing BT3-FIX3-PSM/drift #4 by name; every one of the 6 call-site diffs shows the literal replaced 1:1 (`lang="vie+eng"`→`TESSERACT_LANG`, `config="--psm 6"`→`TESSERACT_PSM6_CONFIG`, `dpi=200`/`resolution=200`→`OCR_RASTER_DPI`) with the per-site inline warning comment collapsed to a one-line pointer. Post-refactor repo grep: 0 remaining hardcoded `"--psm 6"`/`"vie+eng"` literals outside `tesseract_config.py` (2 hits are docstring parameter-examples in `unit_ocr.py`, not live calls); the only remaining `dpi=200` is in `pek_engine_adapter.py` (PaddleOCR engine, correctly out of scope per the backlog spec).

Ran `test_ocr_adapter_psm6_guard.py` live: 3/3 PASS. Ran full suite `-m "not slow"` on current `main` HEAD first: 2 fail/1050 pass — diverges sharply from the row's claimed 1022 pass/8 fail baseline. Rather than accept either number blind, isolated the diff with a clean `git worktree` A/B pinned exactly at `cfe0a78d7` vs `cfe0a78d7^` (not current HEAD, which carries ~2.5wk of unrelated intervening commits): BOTH give byte-identical `8 failed, 1022 passed, 7 deselected`, same 8 test names (2 pre-existing `pandas`-missing-dependency failures in untouched `test_ocr_backends.py`, 2 `test_ocr_unit_tesseract_retry.py`, 4 `test_page_rasterizer.py` — all env/order-dependent, none in the 6 touched files) — independently reproduces the row's own equivalence claim exactly; the current-HEAD delta is unrelated drift from later commits, not this change. `lint-imports` (DDD Fence-A/B/C): 3 kept, 0 broken. `mypy --explicit-package-bases infrastructure/tesseract_config.py`: 0 errors (matches claim). Security grep (password/secret/api-key/token/os.environ) on all 7 changed files: clean. Docs diff (`infrastructure.md`) accurately reflects the new module and preserves the "load-bearing, do not change" language.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 766=766, signal_total 57=57, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. DJ-GATE-1: dev-pdf-extractor's own `sprint-FACTORY-PDF-extract-tesseract-config-dev-pdf-extractor.md` (task-id stamped ×4, pre-satisfied); own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S51. `rebuild_required:true` DEFERRED by dev (code-only, no container touched) — correctly out of QA scope, ops decision. `task_release` is the router's job per this task's own dispatch note (dispatcher-wrap lock held by router) — not attempted here. Pruned cycle-635 (oldest, `FIX-SWEEPGUARD-ESCALATION-HAS-NO-ACTUATOR-REPEAT-OFFENDER-13-STRIKES`) this write per AC-2 retention (3-section cap) — kept 636/637/638.
