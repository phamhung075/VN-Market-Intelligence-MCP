# QA — Notebook

## cycle-699 · 2026-08-13T19:10:00Z · FACTORY-INFRA-split-agentSignalStore (dev-mcp-server, real code, commit `9ad31c026`) — Direct-Commit Verify, APPROVED-CODE-LEVEL, held DONE (not DONE_VERIFIED)

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), verifying the 10-branch INSERT cascade collapse (`detectSignalColumns()`+`buildSignalInsert()`) + 1774L→20-module split of `agentSignalStore.ts`. `git merge-base --is-ancestor` confirmed `9ad31c026` on `main`; `git show --stat` matched all 26 claimed files, barrel 60L, all 20 `agentSignals/*.ts` modules ≤120L (max 114L, `postSignal.ts`).

Re-ran the 25 round-trip tests fresh (not trusted from prose): 25/25 pass, 222 `expect()` calls across `insertBuilder.test.ts`/`columnDetect.test.ts`/`FACTORY-INFRA-split-agentSignalStore-roundtrip.test.ts`. Went further than trusting the test file's own framing — read the PRE-REFACTOR parent commit's `_postSignalInner` 7-deep nested cascade line-by-line (`git show 9ad31c026~1:...agentSignalStore.ts`) and confirmed `buildSignalInsert()`'s column/bind order is a byte-identical mechanical reproduction for all 10 reachable flag branches (A through J, including the two least-obvious ones: I = validation-only-no-chain legacy shape, J = base-schema-only original Task 242 shape). Widened the "agentSignal-adjacent" net beyond the claimed 38 files to all 57 grep-matched test files: 641/641 pass, 0 fail — broader net, still clean.

tsc clean. DDD/security greps clean on all 26 touched files (infra-internal only; `earningsConflictGuard.ts` imports domain from infra — correct direction). `mock-guard.sh` PASS. Full suite: 15288 pass/55 fail/40 skip — EXACT match to the specialist's claimed count. Did NOT take "pre-existing, grep-confirmed unrelated" on faith (memory: `feedback_router_verify_raw_not_badges`) — built a detached git worktree at the parent commit, symlinked `node_modules`, ran the isolated 17-file/158-test subset containing every one of the 55 failing test names on BOTH commits: byte-identical 153 pass/5 fail with the identical 5 test names (`FIX-FOREIGN-FLOW-MISSING-TRADING-DAY` AC-5/AC-5c, `ALLZERO-OHLCV-FETCH` AC-1/AC-2/AC-3) on both pre- and post-refactor code. Direct A/B proof the other 50 full-suite-only failures are ordering/isolation artifacts of running all 15383 tests together, not caused by this commit. `toolCount=183` confirmed live against `tool-registry.json`; `cronJobCount=88` not independently reconfirmed to the exact number but zero scheduler/cron/tool-registration files were touched by this commit (`git show --stat` — only `db/agentSignals/*` + docs + `size-lint-baseline.json`), so it structurally could not have moved.

`REBUILD_REQUIRED=true` (flagged on the row) confirmed REAL, not stale: `docker inspect` shows the live `mcp-server` container was created 2026-08-11T18:25:16Z — nearly 2 days BEFORE this commit's timestamp (2026-08-13T18:38:18+02:00). The running container does not carry this code. Per `docs/protocols/docker-deployment-runbook.md` § Microservice Code-Change Close Gate (CANONICAL): a microservice change is not "shipped" until rebuilt + SHA-gated + live-verified (steps 1-5); PO only marks DONE after all 5 pass. Separately, `apps/mcp-server/src/infrastructure/orchStateSchema.ts`'s `checkVerificationGate` hard-rejects any `DONE_VERIFIED` write lacking `verification.raw_probe` unless the id is in the frozen `RC_VERIF_GRANDFATHERED_IDS` allowlist — confirmed this id is NOT on that list (read the array directly).

VERDICT: APPROVED-CODE-LEVEL, explicitly NOT `DONE_VERIFIED` — code is genuinely correct and independently re-verified at every layer available without a live container, but shipping `DONE_VERIFIED` here would both violate this repo's own canonical Close-Gate rule and fail its own Zod schema gate (no raw-probe evidence exists to attach, honestly, since no rebuild has happened). Moved `task_board.qa[]` → `task_board.done[]` (status `DONE` — schema-valid without a raw_probe per `LANE_ALLOWED_STATUSES`, unlike `done_verified[]` which requires `DONE_VERIFIED` + raw_probe), `next_agent: ops`, so the close-gate rebuild sequence (`docker compose build/up mcp-server` + `scripts/verify-deploy-sha.sh` + live `/health` + diff against the specialist's already-captured PRE-REBUILD baseline: 29-col branch-A schema, 114 rows, latest id=10883) gets dispatched — a follow-up QA live-verify pass is still required post-rebuild before this can flip to `done_verified[]`. This mirrors well-established precedent already live in `docs/data/orch/archive/2026-06.json` (multiple rows use the identical "APPROVED-CODE-LEVEL / done_verified WITHHELD / REBUILD_REQUIRED" pattern), not an invented shortcut. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (conservation clean, task_total 734=734; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S111. No `task_release`/`send_telegram` called — no MCP gateway tool bound this session; dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1. Orch-state write left UNCOMMITTED per convention — dispatcher will commit/push.

## cycle-697 · 2026-08-13T16:27:28Z · FACTORY-GUARD-CI-RAWVERIFY-IMPL (developer, real code, commits `127c4a597`+`654181692`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), 3rd such pass this session after SHAREDPKG/NOHARDCODE, explicitly asked to RAW re-verify not trust prose. Row carried no `.commit`/`.files[]` — derived via `git log --all -- scripts/audits/rebuild-raw-verify-check.sh`, 2 hits: primary `127c4a597` (2026-07-30, this task's own scope) + follow-up `654181692` (2026-08-07, separate ATTEST_ERE fix task, same files). Both confirmed ancestors of `main` HEAD; `git show --stat` matches all 5 claimed files exactly (new script + test.sh, `pre-push`, `ci.yml`, `dev-standards.md`).

RAW re-ran fresh: `rebuild-raw-verify-check.test.sh` → 12/12 PASS live, not stale claimed 9/9 — traced the 3-test delta to the separate later fix commit (regression + AC2 corpus tests); all 9 originally-named cases (trigger-fail, commit-msg/inline/non-trigger PASS, decisions-journal, no-field-match, colocated-test, usage-error, zero-sha) individually confirmed present+passing among the 12, so the developer's own claim still holds as a true subset. shellcheck 0 findings on all 3 touched shell files (`rebuild-raw-verify-check.sh`, `.test.sh`, `pre-push`); `ci.yml` YAML-parse-validated.

Did NOT stop at the bundled test file — built an INDEPENDENT scratch git repo and exercised `rebuild-raw-verify-check.sh` directly with hand-crafted commits: unattested trigger (added `confidence` field under `apps/*/src/infrastructure/`) → exit 1 BLOCK; same commit amended with a `RAW-verify:` message token → exit 0 PASS; doc-only diff (non-trigger path) → exit 0 PASS; missing args → exit 2; zero-SHA range → exit 0 fail-open. All 5 matched the claimed behavior exactly — highest-confidence verify this session since it's a security/CI gate script.

Confirmed pre-push wiring is nested INSIDE the existing `CODE_TOUCHING_REGEX`-gated block (line 86-97) — doc-only-push fast-skip provably unaffected. CI job `rebuild-raw-verify-hook` confirmed with `fetch-depth: 0` + correct `github.event.before`/`github.sha` args. `git show --name-only` on both commits: zero `apps/` files touched — `bun test`/`tsc` correctly N/A; `mock-guard.sh` correctly reports no production TS/JS source to scan (PASS). Decision-journal entry `developer-S34` confirmed present at the cited path with matching task-id.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe` attached per RC-VERIF schema on the first write attempt (built into the jq transform from the start this time, not a retry). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S109. No `task_release`/`send_telegram` called — no MCP gateway tool bound this session (per dispatcher's own framing, INV-GATEWAY-1); dispatcher's finally-block + TTL expiry is the authoritative release path. Orch-state write left UNCOMMITTED per convention — dispatcher will push.
