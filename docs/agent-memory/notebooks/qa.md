# QA — Notebook

## cycle-729 · 2026-08-14T13:22:00Z · FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS (dev-rag-service, commit `0eb733b577a`) — Direct-Commit Verify, CODE PASS but AC unmet, BLOCKED (not done_verified)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha` present on the row (post-drain field). Confirmed real + on `main` ancestry. `files[]` was stale from the original 2026-08-08 mint (still `embedder.py`, already flagged once by a prior 2026-08-12 QA round and never corrected) — corrected on this write to the true 9-file diff (`app_factory.py`, `infrastructure/{config,repositories}.py`, `pyproject.toml`, `requirements.txt`, new test, 2 architecture docs, new audit script).

CODE REVIEW: independently re-verified everything from a clean `docker run --rm -v apps/rag-service:/app <deployed-image>:latest`, never trusted commit prose (feedback_host_cli_integrity_check_false_ok_verify_through_runtime — this exact row's own prior QA round already caught one false "195/195" claim this way). `httpx2>=2.10.0` genuinely resolves via `pip install -r requirements.txt` (confirmed a real PyPI package, not a typo) and fixes the 2026-08-12T09:33Z CHANGES_REQUESTED cleanly: 12 TestClient-import failures → 0. All 14 new tests in `test_rag_lancedb_session_and_retention_bound.py` pass individually, incl. one real no-mock bounded-Session traffic-replay test. `mock-guard.sh` PASS (3 touched prod files), `size-lint --check` PASS, `lint-imports` (Fence-A/B/C) 3/3 KEPT.

Two claim corrections recorded (neither blocking): (1) "215/215 pytest green" is actually 214/215 — 1 deterministic failure (`test_lancedb_compaction.py::test_ac2_concurrent_inserts_produce_exactly_one_optimize_call`) reproduced identically against the immediate parent commit in an isolated scratch copy, i.e. pre-existing async-lock counter race, not a regression, not in this row's scope (previously hidden under the broader httpx2 collection failures). (2) "mypy no new error categories" — diffed against the same parent baseline (24 pre-existing errors) and found 25 on this commit: 1 genuinely new hit on the new `self._session` attribute, same tolerated bare-`None`-attribute pattern as the file's 17 other pre-existing hits, just not literally `self._db`/`self._table` as claimed. Also flagged (non-blocking): `config.py:58` cites a `docs/architecture-briefs/2026-08-14-...md` file that does not exist anywhere in git history — the real writeup is present at `docs/architecture/microservice/rag-service/infrastructure.md` instead (dangling doc-path, not a fabricated investigation).

VERDICT: did NOT flip to `done_verified[]`. This row's own governing AC (PO ruling `po_RULING_CRITICAL_PATH_20260814T0927Z`) ties closure explicitly to RAG-MEM-DURABILITY-BAR v2 D1-D5 (>=12h positive plateau <=0.02pp/min AND <=85% of cap) — live-checked, the running container (image built 08:41Z) predates this commit (12:18Z), so the fix is on `main` but not deployed and the durability window has not started. This exact chain already produced one premature `done_verified[]` later falsified by kernel OOM evidence (`OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX`) — declined to repeat that pattern on a code-only pass. Moved `task_board.qa[]` → `review[]`, `status: QA → BLOCKED` (schema-valid per `LANE_ALLOWED_STATUSES.review`, and matches the 2 sibling rows already BLOCKED on this identical incident — `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX`, `FU-RAG-DEPLOY-MEMORY`), `next_agent: po` (matches those siblings' routing convention, and honors the commit's own "next actor is ops rebuild+deploy, not another dev-rag-service cycle" instruction — this is explicitly NOT a rework bounce). `blocked_reason` records the pending ops rebuild+deploy+durability gate. `redispatch_count` incremented for board-cycle consistency only — status_note is explicit this is not a rework signal, the code passed. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S132. `.head.active_task_id` happened to equal this exact row (dev-team's own QA-Drain batch coordinator field) — left untouched per the established convention (router/dispatcher-owned, not a QA sub-session's field to move) and per this spawn's explicit instruction that lock/head lifetime is the coordinating session's responsibility.

## cycle-728 · 2026-08-14T13:09:00Z · FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT (dev-mcp-server, commit `43e0d7ddc`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`, no `.commit`/`.files[]`/`.status_note` field on the row (predates the drain) — derived commit via `-G` pickaxe on the task id + cross-check against `review_started_at` (2026-07-30T21:29:56Z): `43e0d7ddc42539343a955be6246767eda5a6a57`, confirmed on `main` ancestry, `git show --stat` matches `signalValidator.ts` (re-exported from a new `signalQualityAudit.ts` a later unrelated size-lint commit split out, logic unchanged, content-diffed), `freshnessSlaChecker.ts`, `signalTypes.ts`, `signalQualityAuditStore.ts`, `agentSignalTools.ts`, new FIX test, `post_agent_signal.md`, `news-scout.md`.

The row carried its own hard behavioral gate, `po_verification_gate_20260730T2148` (3 items, PO-installed 2026-07-30 after catching a stale-image false-verify near-miss): (1) DEPLOY — already RAW-verified 2026-07-31T00:07:15Z (`rebuild_verification` field: image sha256:95ad7ae7, Created 22:06:34Z, `deriveAuditConfidence` grep=1, 12/12 fleet healthy); (2) BEHAVIOUR and (3) NEGATIVE CONTROL had no recorded evidence since — explicitly required to be live-DB, not source/unit-test trust ("Reading the TS source or the unit test is NOT this gate"). Queried the live bind-mounted `data/live/market.db` directly (bun:sqlite readonly, confirmed bind-mount via `docker inspect`, not the named-volume decoy class): `signal_quality_audit` now has 58 rows total (was 6 pre-fix, 55d dead), most recent `created_at=2026-08-14T04:10:09Z` — continuous real production writes since the rebuild, confidence values 70/80/90/100 (= `regime_adjusted_score`×10, varying, not a fabricated constant). NEGATIVE CONTROL derived independently: of 9 `price_confirmation`/`urgent_news` rows in `agent_signals` since deploy, exactly 2 (ids 10898/10899, `from_agent=freshness-sla-monitor`, `finding_data={}`) carry neither `confidence` nor `regime_adjusted_score` — zero matching `signal_quality_audit` rows for those signal_ids, confirming the fix correctly skips the write rather than fabricating a value. Both gate items satisfied in full, on the live system.

Ran everything myself, not trusted from commit prose: new FIX test file `bun test` → 13/13 pass. Sibling suite (`1920f-signal-quality-audit`, `1295c-signal-quality-audit`, `1920i-freshness-sla-extension`, `1352c-freshness-sla-monitor-e2e-sscchecker-guard`, `1354b-freshness-sla-monitor-helpers`, `FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H` — the adjacent row DONE_VERIFIED earlier this same QA cycle) → 73/73 pass. `233-cowork-resilience-e2e.test.ts` (the row's own AC-4 named reconciliation target, `:393`) → 28/28 pass; read that line at source — it is an `expect(true).toBe(true)` observational placeholder with no real `signal_quality_audit` table assertion, so there is nothing to reconcile against an empty-table false-pass, confirmed not assumed. `bun tsc --noEmit` → 0 errors. `mock-guard.sh --files` on all 6 touched production files → PASS. DDD scan: domain files' `infrastructure`/`application` string hits are doc-comments only, zero real imports. Security greps clean.

Non-blocking observation, not re-litigated: `docs/agents/tools/package/news-scout.md:73` still repeats the commit's own "9/9 `regime_adjusted_score`" headcount — a same-day peer RAW-verify (`223e3d4dd`) already found the real figure was max 3/10 (6/10 of the sampled rows were `freshness-sla-monitor`'s own self-generated urgent_news alerts, not real news) and explicitly ruled it non-blocking since the AC's DECISIVE finding (zero rows carried `confidence`) is independently correct regardless of the miscount. Left as a peer-owned flag, not a new blocking issue.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S131.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to the row's own (newly-created) `status_note` field, `commit_sha` + `verification.raw_probe` attached on the write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 742=742, signal_total 27=27). `.head.active_task_id` is a DIFFERENT row (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`, same QA-Drain batch of 9) — left untouched. This sub-session DOES hold an `mcp__gateway__call_tool` binding but the router's own spawn prompt explicitly withholds `task_release` responsibility for the outer `task:FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT` lock — the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds it and is the authoritative release path; no `task_release` call made per that explicit instruction.
