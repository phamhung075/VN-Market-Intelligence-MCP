# QA — Notebook
## cycle-754 · 2026-08-14T16:35:27Z · FACTORY-RAG-delete-dead-sqlite-repo (rag-service, commit `768bee954`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.commit`/`.files[]` fields — PO's own `po_qa_lane_stale_triage_20260814T0908Z` note supplied the fallback commit `768bee954`. Confirmed real, on `main` ancestry; `git show --stat` matches all 4 files named in dev-rag-service's `review_note` exactly (`application/usecases.py`, `domain/repositories.py`, `infrastructure/repositories.py`, `__tests__/integration/test_rag_integration.py`) plus journal/notebook/arch-doc trims. Live source re-read, not trusted from prose: `domain/repositories.py` now holds ONLY `VectorStorePort`/`EmbedderPort` (`AnalysisRepositoryPort` gone); `IndexUseCase` docstring no longer references the phantom `analysis_repo` param; repo-wide + arch-doc grep for `SQLiteAnalysisRepository|AnalysisRepositoryPort` = zero hits. Python zone — ran `pytest` INSIDE the actual deployed `rag-service` image (never host, per non-bun-zone rule): full suite 203 pass/12 fail. Traced the 12 fail, not assumed pre-existing: caused by a DIFFERENT, already-documented, unrelated stale-image gap — `requirements.txt`'s `httpx2` pin landed 2026-08-14T10:18Z UTC (`git log -S httpx2`), this image was built 2026-08-14T08:39Z UTC (`docker image inspect .Created`), i.e. BEFORE the pin; none of the 12 failing test files reference the deleted symbols (grep-confirmed). The actually-touched file, `test_rag_integration.py`, run in isolation: 6/6 pass clean. `mypy` (installed transiently inside the image via `pip3 install mypy`, scoped to the 3 touched production files, `--ignore-missing-imports`): 21 pre-existing errors, zero referencing `SQLiteAnalysisRepository`/`AnalysisRepositoryPort`/`sqlite3`/`analysis_repo` (grep-confirmed) — deletion introduced no new errors. `mock-guard.sh --files "usecases.py domain/repositories.py infrastructure/repositories.py"` PASS. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S151.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717 unchanged, signal_total 22 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`).

## cycle-755 · 2026-08-14T16:40:00Z · FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY-THAT-CANNOT-PRODUCE-ITS-OWN-VALUE (apps/mcp-server/, commit `7ac55adc8`) — Direct-Commit Verify, CHANGES_REQUESTED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit`/`.files[]` present. `7ac55adc8` confirmed real, on `main` ancestry; `git show --stat` matches both claimed files exactly. AC-1 genuinely met (alert now names the actual detector w/ figures — cross-stmt/intra-BS unit mismatch or the specific BCTC-VAL-NN rule — never an unconditional VNM/VEA ticker hint). Re-ran independently: `FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY.test.ts` 9/9 pass, `bun tsc --noEmit` clean, `mock-guard.sh` PASS on all 4 touched files (2 claimed + 2 later-split siblings `financialFiguresRules.ts`/`confidenceFinancialReasonBuilder.ts` per the 2026-08-05 size-lint split). Found AC-2 broken on independent re-derivation, not trusted from `note` prose: the live gate at `confidenceFinancialReasonBuilder.ts:73-77` (`matchesVnmVeaSignature`) flags the VNM/VEA hint on rule-MEMBERSHIP alone (any violation is VAL-01/03/10), never checking whether that rule fired ALONE or whether confidence actually landed on 0.0/0.8 as AC-2 requires. Reproduced live: figures `{totalAssets:1000, totalEquity:500, totalLiabilities:400, operatingMargin:2.0, netRevenue:-1}` → `validateFinancialFiguresDetailed` returns confidence=0.6 (VAL-03+VAL-05 stacked), yet the built message still appends the VNM/VEA signature claim — outside the AC-2-mandated {0.0,0.8} set. Reachable in production: any low-extraction-confidence report whose figures trip VAL-03 alongside VAL-05/VAL-06/VAL-01-SCALE ships the false VNM/VEA claim at financial=0.4 or 0.6 — the exact misleading-hint defect class this task exists to close, relocated rather than removed. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S152.

VERDICT: `CHANGES_REQUESTED`. Issue + fix suggestion (gate on `violations.length===1`, equivalently `confidence===0.0||confidence===0.8`, plus a regression test for the VAL-03-co-firing case) written to the row's `status_note` field (row had none). Not routed to fixer — no task branch exists (direct-commit row, `branch:null`); routed to the row's own `owner`. Board write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717 unchanged, signal_total 22 unchanged). Row moved `qa[]`→`review[]`, `status: QA→REVIEW`, `next_agent`/`owner`=`dev-mcp-server`, `redispatch_count: 0→1`. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `review[]` with `status_note` set).

## cycle-756 · 2026-08-14T16:36:20Z · FIX-CI-BUNTEST-1108-AGENT-WORK-LOG-STORE (po/dev-mcp-server, commit `319ef3cac`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`). `qa[]` row, `.commit`/`.files[]` present. Commits `319ef3cac`(test fix)+`119bc03c2`(notebook+journal) confirmed real, on `main` ancestry; `git show --stat 319ef3cac` matches the row's sole `files[]` entry exactly (`1108-agent-work-log-store.test.ts`), 19 ins/6 del, no other file touched. Diff read directly, not trusted from prose: fixture INSERT widened from exact `datetime('now','-30 days')` to `datetime('now','-30 days','+5 seconds')` to close a real independent-clock race between the fixture's own `datetime('now')` and `purgeOldAgentWorkLogs`'s separate `datetime('now')` call. Cross-checked `purgeOldAgentWorkLogs` (`agentWorkLogStore.ts:180-192`) byte-unchanged — strict `<` comparison, confirming "no production code changed" claim exactly. Re-ran independently: isolated file 17/17 pass (exact match). `bun tsc --noEmit` 0 errors — first pass transiently hit a concurrent peer's `scratch-check-ac2.ts` (file absent from filesystem/git the moment checked, matches several sibling entries' independent same-cycle observation of the same shared-repo concurrency artifact), 2nd run clean. `mock-guard.sh` PASS (no production source touched). Secrets/`process.env` clean. Smart-Skip applies (test-only diff) — DDD/security full scans correctly skipped, not a gap. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S150.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 717/signal_total 22 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write). `.head.active_task_id` still pointed at this row (dispatcher batch-tracking 9 rows by shared `claimed_at`) — left untouched per INV-GATEWAY-1 / CANONICAL:SSOT-STATUSFLIP-LANEMOVE(b) (dispatcher's own responsibility, not per-row QA's); no `task_release` call made (dispatcher holds the lock for the whole batch).
