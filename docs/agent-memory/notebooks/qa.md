# QA — Notebook

## cycle-712 · 2026-08-14T08:53:00Z · FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED (report-analyzer, commit `82216e291`) — Direct-Commit Verify, durability window OPEN, NOT closed

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, P0 crash-loop. Code review sound: `_maybe_build_vector_index()` now checks `table.list_indices()` before rebuilding, correctly targets the discriminated root cause (per-process `_vector_index_built` flag vs on-disk-persisted LanceDB index across restarts), graceful `list_indices()`-raises fallback preserved. Re-verified independently, not trusted from prose: new `TestVectorIndexPersistsAcrossRestart` (3/3) genuinely PASS inside a freshly-built deployed image; full suite 189/12 (the 12 are the SAME pre-existing httpx2-dependency + compaction-counter failures a prior QA record on this zone already root-caused, not a regression); mypy error-for-error diffed against the parent commit — 16=16, identical classes, only line-shift, zero new; mock-guard PASS; security greps clean.

CRITICAL FINDING: `REBUILD_REQUIRED: true` had NOT been actioned by anyone — the live container was still on the OLD pre-fix image (content-hash-confirmed: `docker exec grep list_indices` zero hits, md5 matched the ca6d86869-only hash). Performed the rebuild+redeploy myself (single-service `docker compose build rag-service` + `up -d rag-service`, never `down`) — new image built+deployed, content-verified live, all 12 peer services unaffected throughout. This starts the durability clock, it does not close it: PO's ruling (`po_crossref_20260814T0700Z`) requires >=24h wall-clock zero-new-`oom_memcg` dmesg evidence on the deployed image before any DONE/DONE_VERIFIED flip — a single QA pass structurally cannot produce that. Left status at `qa` (not flipped DONE_VERIFIED, not routed to changes-requested — the code itself has no defect). Wrote exact next-check timestamp (>=2026-08-15T08:41:48Z) + new container id into the row's `status_note` so the next actor doesn't have to re-derive it. Also corrected the PO-flagged misleading `infrastructure.md` § OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX caveat (independent action, done regardless of the durability outcome) — added an explicit forward-pointer so the section can no longer be misread as a complete fix.

DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa-2.md` STEP qa-S23.

VERDICT: row left at `qa` (no lane move) — durability bar genuinely cannot be certified in one pass; fabricating a green here would repeat the exact AC-3-premature-certification defect this row exists to fix. `status_note` updated with full rebuild evidence + explicit next-check instructions (dmesg filtered to the NEW container id, not the retired one). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — no `task_release` call made; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` and is the authoritative release path.

## cycle-711 · 2026-08-14T06:33:27Z · UC-CDC-P7 (report-analyzer, commits `15492ff61`+`2e82756ac`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null` — RESCOPE Phase 2 only (script-layer moves + I16 SSOT recipient field), Phase 1 (12→7 file collapse) deliberately parked per the row's own "Phase2 BEFORE merge" sequencing. Did not trust the dev's self-reported 260/260-pass claim, TE-coordination claim, or the bug-fix/park-honesty narrative — re-verified all independently against live artifacts.

Both commits `git merge-base --is-ancestor`-confirmed on `main`. `git show --stat 15492ff61` matches all 16 claimed files exactly; `docs/agents/cowork-team/flow/main.md` absent from the diff, confirmed untouched. TE-T03/TE-T13 re-checked at source (not prose): TE-T03 `DONE_VERIFIED` `qa_verified_at=2026-08-11T18:22:00Z`, TE-T13 `DONE_VERIFIED` `verified_at=2026-07-13T15:10:00Z` — zero collision. Own re-run of all 7 test suites: cowork-match-slots.test.js 69/69, cowork-tick-preflight.test.sh 67/67, cowork-tick-postflight.test.sh(new) 28/28, cowork-chef-mutex 25, cowork-schedule-consistency 9/9, cowork-catchup-predicate 34/34, cowork-guaranteed-slot-firer 28/28 — sum 260/260, 0 fail, exact match. `mock-guard.sh` PASS on touched production files, `shellcheck -S warning` clean on both touched `.sh` files. `bun tsc --noEmit` N/A confirmed structurally — zero `.ts` files touched, `scripts/agents-flow/` outside every `tsconfig` include array. Self-caught bug fix read at source: `cowork-tick-preflight.sh:107-112` — `${8:-{}}` brace-matching defeat replaced with `${8:-}` + explicit `[ -z "$extra" ] && extra="{}"`, matches claim verbatim. I16 SSOT field verified end-to-end: `cowork_signal_recipient:true` on exactly po/tran-ngoc-bau/unified-agent/alert-commander in `system-map.json`; `cowork-tick-preflight.sh:90-95` reads it with a fail-safe fallback to the same literal set. Phase-1-parked claim independently NEGATIVE-verified: `docs/agents/cowork-team/flow/` still 14 files (pressure-read.md/pressure-cadence.md still separate, slot-claim.md 4.6b block still present); grep for `cowork-tick-postflight` in last-fired.md/tick-snapshot.md/main.md/work-tick.md = 0 hits — the wiring gap is genuinely unwired, nothing silently dropped.

DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa-2.md` STEP qa-S22.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 734=734, signal_total 28=28). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:UC-CDC-P7` and is the authoritative release path.

## cycle-705 · 2026-08-14T01:33:11Z · FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES (report-analyzer, commit `761988143`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — the detector's only non-zero-diff DETECTED path sourced its evidence from the notebook commit it was checking, so a genuinely non-zero OTHER plane (signal_queue row, ledger entry) with the notebook plane silently missing defeated `all_zero` and PASSed a defective cycle (confirmed live 12x). Did not trust the developer's pasted 15/15 count or the router's own independent-run note — re-ran everything myself.

Commit `761988143` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook side-files. `bash scripts/audits/detect-analysis-only-exit.test.sh` own re-run: 15/15 PASS.

Read the core diff at source, not narration: the new MANDATORY-PLANE CHECK reads `docs/agents/<id>/init.md`'s `memory.notebook` field (`none` → not mandatory, else → mandatory), never the artifact under test — satisfies AC-2's "never key on the agent's own claim" carry-forward from the parent row. AC-1's naive `all_zero`→`any_zero` widening is explicitly rejected in the diff in favor of this per-plane subset. Grep-swept `docs/agents/*/init.md` myself (not trusting the dev's "only refine_bctc_md" claim) — confirmed only `refine_bctc_md` declares `notebook: none`; every other fleet agent defaults to mandatory=1, so the conservative default does not mass-false-positive the fleet.

Re-ran BOTH historical repros named in `verification_gate` live, not from prose: system-auditor `--since-ts 2026-08-12T05:35:00Z --until-ts 2026-08-12T05:55:00Z` → DETECTED (`all_zero`+`mandatory_status=violation`; that signal_queue row has since drained per the developer's own honest note, so it no longer isolates the mechanism alone, but the detector still correctly fires). news-scout `--since-ts 2026-08-07T03:55:00Z --until-ts 2026-08-07T04:30:00Z` → DETECTED `mandatory_status=violation` (`ledger=1`/`notebook=0`) — flipped from the pre-fix live PASS documented in the row's own evidence, the exact inverse-partial shape this row exists to close.

AC-3 regression check (own diff, not trusted): T8-T12 (the c80 partial-write contract fixtures) show zero lines changed in the commit diff — confirmed non-regression. AC-4: RED-1 (T2, signal_queue>0/notebook=0), RED-2 (T4, ledger>0/notebook=0), GREEN (T14, no-notebook-mandate agent) all present and green; T6 doubles as AC-5's `--cycle-tag` investigation, empirically proving the identical blind spot closed by the same fix. `shellcheck -x -e SC1091` 0 warnings both changed `.sh` files. `mock-guard.sh` correctly N/A — zone is pure bash+md, no TS/JS production source touched, `bun test`/`tsc --noEmit` N/A.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP `developer-S104`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live repo. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.
