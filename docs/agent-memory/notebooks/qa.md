# QA — Notebook

## cycle-733 · 2026-08-14T14:32:56Z · FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH (developer, commits `28197b3d5`+`787703374`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha` present but WRONG — points to `3a9118e24`, which touches only `docs/WORK.md` (a log-entry commit), not the actual fix. Grep/git-log traced the real implementation: `28197b3d5` (fix(cross-service/fix-cowork-cronreg-marker-renewal): ...) + follow-up doc-fix `787703374`. Both confirmed real, on `main` ancestry; `git show --stat` matches every file the `verification_note` claims (`scripts/agents-flow/cowork-tick-preflight.sh`, `.test.sh`, `docs/agents/cowork-team/flow/preflight-error-fallback.md`, `docs/architecture-briefs/2026-08-06-cron-rearm-cross-session-dedup.md`, `docs/policies/dev-standards.md`). Corrected `commit_sha` to `28197b3d5` on this write — treated as a bookkeeping-only field error (precedent: cycle-729's `files[]` self-heal), not a substantive AC failure, since every AC independently reproduced true.

Re-ran everything myself, not trusted from prose: `cowork-tick-preflight.test.sh` → 73/73 GREEN (incl. T1 SILENT + T6 TOMBSTONED AC-2 checks, and the T-CRONREG-NEG negative control proving a transport failure on the new heartbeat call never gates/alters verdict/exit code). RED-first reproduced independently, not assumed: reverted the script to its parent commit (`28197b3d5^`) with the already-updated test file in place, reran → 71/73 (2 new FAILs: T1 AC-2, T6 AC-2), exact match to the claimed RED-first, then restored the file clean (`git status` zero diff). `shellcheck -S warning` 0 findings. AC-1 placement read at source: heartbeat sits after Step 2 presence claim/renew, before Step 2.5's tombstone early-return — fires on all 5 verdicts, `>/dev/null 2>&1`, rc never tested. AC-3: original fallback-doc call kept verbatim + pointer note added. AC-4: architecture brief §1.4 carries a dated `CORRECTED 2026-08-14` annotation under the original (stale-when-written) bullet, not a rewrite. AC-6 sibling sweep independently reproduced at source (not trusted): `system-auditor/flow/main.md` Step 0d fires `cron-registration:detect-loop`'s heartbeat unconditionally after winning fire-election (never the SKIP/EXIT branch), and `.claude/skills/cron-detect-loop/register.md` Job 2/3's anti-stale-mask gate (heartbeat age>60min forces spawn even on ALL_GREEN) is real — a healthy-steady-state renewal path, correctly not filed as a follow-up. `non_goals` honored: zero diff to any `cron-*/SKILL.md` TTL/`orphan_threshold_seconds` value. No `apps/` TS/Go touched across all 3 commits — `bun test`/`tsc` N/A, confirmed via diff stat. `mock-guard.sh` PASS (no production TS/JS matched pattern). Security greps clean.

AC-5 (LIVE VERIFICATION) note: this sub-session holds no `mcp__gateway__call_tool` binding (INV-GATEWAY-1) to independently re-call `task_list_held` live. Corroborated instead via the live gitignored telemetry file `docs/data/telemetry/cowork-tick-preflight.jsonl`, which shows real automatic SILENT/TOMBSTONED ticks continuing every ~15min after the fix commit landed (13:13:51Z): 13:35:03Z/13:59:22Z/13:59:27Z/14:07:31Z/14:25:44Z, most recent ~6min before this check — and by independently recomputing the epoch timestamps `verification_note` quotes (`date -u -r`): `1786711974`=`2026-08-14T12:52:54Z` (frozen incident value, matches), `1786714502`=`2026-08-14T13:35:02Z`, `1786714537`=`2026-08-14T13:35:37Z` — all exact matches to the claimed prose.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S137.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE (one field correction, non-blocking). `[QA] Review Record (direct-commit verify)` appended (` || ` separator) to the row's own `verification_note` field; `commit_sha` corrected; `verification.raw_probe` attached same write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (Stage0+1 PASS, conservation clean, task_total 736=736, signal_total 25=25). `.head` untouched — review-lane QA-drain is head-decoupled and this row was never `.head.active_task_id`. Task-lock (`task:FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH`) held by the dev-team dispatcher on my behalf per spawn instruction — no `task_release` call made here, dispatcher's finally-block/TTL(3600s) is the authoritative release path.

## cycle-732 · 2026-08-14T14:30:00Z · FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE (developer, commit `d89392752`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha: "d89392752"` present on the row. Confirmed real, on `main` ancestry, `git show --stat` matches exactly the 4 claimed doc files (`system-auditor/flow/main.md`, `audit-dimensions.md`, `init.md`, `orch-sentinel/flow/dim-oh3-auditor-blindspot.md`) — zero code touched, `bun test`/`tsc`/`mock-guard` correctly N/A (confirmed via diff stat, not assumed). Read the full diff of all 4 files directly, not trusted from commit prose or the row's own `review_note`.

Live-ran the landed jq route-derivation (`.project.infrastructure.vps.routes[].source_id` over `system-map.json`) — output matches the doc's cited 8-entry list verbatim (`ssc-iboard`/`bctc-discover`/`muasamcong`/`bctc-push`/`foreign-flow`/`sbv-vps`/`news-vps`/`vietstock-agm-plan`). Live-ran the landed OH-3.2 fleetwide regex sweep over the 3 named doc files (`grep -noE '[Aa]ll [0-9]+ routes|[0-9]+ (geo-blocked )?routes'`) — 0 hits, matches commit's own claim (was 3 pre-landing).

Independently confirmed the doc's embedded code claims at source (not trusted from prose): `vpsPushLogStore.ts:13` `VpsService` union includes `foreign-flow`, `:97` hardcoded services array `[prices,news,sbv,bctc]` excludes it — dead branch, exactly as documented. `vpsHealthPoller.ts:27-31` 5-service list matches the doc's service-plane column exactly; `:124` confirms `vn-sbv-fetch`'s 35-min threshold, corroborating the AC(5) crossref's 30<35 argument.

No gateway binding in this sub-session (`mcp__gateway__call_tool` unavailable) — could not directly replay `get_vps_proxy_health`/`get_vps_service_health` live to walk AC(2)/(3) end-to-end tool-call-by-tool-call. Compensated with full source-level verification of every factual premise the verdict logic depends on (service lists, dead branch, thresholds — all confirmed accurate) plus a manual trace of the verdict table: no-coverage routes (`muasamcong`/`vietstock-agm-plan`) FLAG WARN by construction, never silent PASS; any `get_vps_service_health` unhealthy entry blocks a bare cycle PASS even when the proxy plane reads all-`ok`. Internally consistent, correctly closes the cited 2026-07-29T10:44Z cross-plane PASS-masking gap.

AC(5): confirmed field `sbv_fx_b12_rootcause_crossref_20260808` present on the archived `FIX-AUDITOR-B12-DOUBLE-INVOKE-EMIT-MARKER-LOSS` row (`docs/data/orch/archive/2026-08.json:5761`), additive only, does not re-litigate that row's already-approved closeout.

VERDICT: all 5 acceptance criteria met. `[QA] Review Record (direct-commit verify)` written to the row's own (newly-created) `status_note` field, `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached (required by `orch-validate.mjs` for any `DONE_VERIFIED` flip — first attempt without it hard-failed validation, corrected same write). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 736=736, signal_total 25=25). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S134. No `task_release` call made — this sub-session has no gateway binding; the coordinating dev-team/router session is the authoritative release path per INV-GATEWAY-1 and this spawn's explicit instruction.

## cycle-730 · 2026-08-14T14:29:32Z · FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT (developer, commit `f9e5113534a`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `.commit`+`.files[]`/`.status_note` present (post-drain shape). Confirmed real (full-hash match), on `main` ancestry; `git show --stat` matches claimed deliverable exactly: new `scripts/audits/po-mint-orchapply-actuator-verify.sh` (239L) + `.test.sh` (218L), `docs/WORK.md` entry — no `apps/` TS/JS touched (bun test/tsc structurally N/A, confirmed via diff stat, not assumed).

Ran everything myself, not trusted from commit prose: `po-mint-orchapply-actuator-verify.test.sh` → 7/7 (DoD-1..DoD-7 all PASS, exact match to claim). Live fleet-wide run of the script itself against the real corpus (all 15 `docs/agents/po/flow/*.md`): 13 PASS / 2 FAIL (`triage-signals.md`, `triage-signals-longtail.md`), same defect class the row claims (board-mutation prose/table-cell rows, zero `orch-apply.sh` pipe). `shellcheck` 0.11.0 clean both files, `bash -n` clean, security greps (`process.env`/secret/password/token) clean, `mock-guard.sh` PASS (bash scripts outside its TS/Go scan scope). Both files executable (`rwxr-xr-x`).

Two non-blocking observations recorded, neither disputes the AC: (1) live violation count today is 6 lines, not the 8 the script's own header/status_note cite at authoring time (Aug 6) — traced via `git log --follow` to 2 unrelated later commits (Aug 9, Aug 14) editing `triage-signals.md`, naturally shifting the corpus; this is exactly what a regression verifier is meant to keep re-measuring, not a script defect. (2) `docs/agents/po/flow/main.md:173` still literally reads "spec, unimplemented" though the script now exists — stale doc pointer, out of this task's own AC-trailer scope, left for a future doc-self-heal pass.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S133.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended (` || ` separator) to the row's own existing `status_note` field, preserving the developer's original note; `verification.raw_probe` attached on the same write. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 736=736, signal_total 25=25). Task-lock (`task:FIX-PO-MINT-ACTUATOR-REGRESSION-VERIFIER-SCRIPT`) held by the dev-team dispatcher on my behalf per spawn instruction — no `task_release` call made here, dispatcher's finally-block/TTL is the authoritative release path.
