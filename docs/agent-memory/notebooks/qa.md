# QA — Notebook

## cycle-551 · 2026-08-06 · FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit derived from `review_note` prose: `70202e9da`)

Dispatch explicitly flagged NOT to verify in isolation (cross-linked to `FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT`, same 07-19..07-20 incident window) and to weigh a residual concern from `po_live_evidence_20260801T0047`: "the guard is containment not cure, the source is still firing." Weighed both before signing off, did not skip either.

CODE: `70202e9da` confirmed real + on main ancestry; `git show --stat` matches `review_note`'s claimed files exactly (`parseBctcReport.ts`, `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP.test.ts`, `usecases.md`, dev-mcp-server notebook). Read the live guard myself at `parseBctcReport.ts:373-395`: `report.balanceSheet.totalAssets <= 0` is evaluated UNCONDITIONALLY and returns before any INSERT/UPDATE reaches the DB on either arm (b1: `existingGoodRow.total_assets>0`, would-overwrite-good; b2: no prior row, would-manufacture-corrupt) — `published_at`/ngay_nop is structurally unreachable from a corrupt-extraction write, by construction not just by test. Re-ran REAL verification, not trusted from prose: `bun test src/__tests__/FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP.test.ts` 7/7 pass (25 expect calls, covers both b1+b2); `bun tsc --noEmit` 0 errors; `mock-guard.sh --files apps/mcp-server/src/application/usecases/parseBctcReport.ts` → PASS.

LIVE PRODUCTION DB, not just prose or a single-night spot-check — this resolves the two live `verification_gate` clauses `qa_approval_note` explicitly held open 16 days ago ("post-deploy operational verification, not satisfiable in this review pass"). Queried the bind-mounted live `market.db` directly (`data/live/market.db`, confirmed via `docker inspect` as the exact volume `mcp-server` writes, not a stale host copy). `cron_job_runs`: `bctcReparseJob` ran 49 times since fix-deploy (2026-07-21T15:34:14Z) — 07-21/28/29/30/31, 08-02/03/04/06 — 38 success/11 crashed over 30d (crashes are job-reliability, orthogonal to data corruption). Across ALL 257 `financial_reports` rows: `MAX(published_at) = 2026-07-20T13:49:56Z` — strictly BEFORE fix-deploy; zero rows carry a `published_at` after the fix shipped. Zero rows have `total_assets<=0 AND parsed_at` after fix-deploy (checked explicitly) — not one corrupt write has landed since the guard shipped; the 55 currently-corrupt rows already in the DB all predate the fix and are untouched remediation debt, explicitly out of THIS row's scope (unchanged since `po_acceptance_reconciliation_20260721T1649` — tracked on `OPS-BCTC-REFINE-REPASS-NONBANK-5T`/`W5-FU-CTG-REFINE-96e36139`/`SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS`). 3 tickers got a genuine non-corrupt reparse since the fix (D2D 07-31, VCB+VND both 08-06 today): `parsed_at` updated, `published_at` unchanged at its original 07-19/07-20 value — immutability holds on the successful/non-blocked rewrite path too, not just the blocked one.

`verification_gate`: (a) "no ticker acquires NGAY NOP=run-date over 3 consecutive cycles" — CONFIRMED non-vacuously over 16 days/~10 write-producing cycles (59-70 rows/cycle per `rows_written`; `po_false_pass_warning_20260730`'s dormancy concern does not apply now, producer is active). (b) write-back suppression on both arms — CONFIRMED via own 7/7 re-run AND the zero-post-deploy-corrupt-write DB finding, stronger evidence than the isolated Telegram-log spot-checks the board already carried. (c) ngay_nop immutable across reparse — CONFIRMED by the same `MAX(published_at)` finding plus the 3 live writethrough spot-checks.

CROSS-LINK, read in full not just the first note: the sibling row's own evidence trail has three same-night entries (00:47Z, 01:58Z, 02:58:55Z) — the LATER two (which this row's own dispatch prompt did not quote) reattribute the "source still firing" residual to a genuinely separate, already-spun-out acquisition-layer defect (SSC doc-selection always returns the latest document regardless of requested quarter), now its own tracked row `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (live-checked: `task_board.ready[]`, not yet worked). The sibling row's own final note declares itself "qa-signoff-ready on its own detector ACs" and routes acquisition evidence to the new row, not either of these two. So the residual IS real — the generator still produces zero-extraction attempts — but it was never this row's acceptance scope: this row owns Part 2 (durable write-back) only; Part 1 (stop-the-bleed) was explicitly ops-routed at mint time, and the generator's root cause is now a 3rd, separately-owned row. The guard's only job was to make those attempts harmless, and 16 days of production evidence confirms it has.

VERDICT: APPROVED, DONE_VERIFIED. This row's own numbered acceptance (1)(2)(3) and `verification_gate` (a)(b)(c) are ALL met by live evidence, independent of the still-open sibling row and the still-unworked acquisition row. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (fresh re-read immediately before write — conservation OK task_total 763→763, signal_total 203→203). `next_agent: pm`. Full verification text appended to the row's own new field `qa_direct_commit_verify_20260806` (row has no `status_note`/handoff file to append to). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S4 (2026-08-06T21:34:53Z entry). Task lock claimed under this dispatcher's coordination session f298ccf7-8cf4-452d-9a5a-57dcb47e65ac, ttl_seconds=3600 — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); flagging to the calling dispatcher to release `task:FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP` itself. NOTEBOOK RETENTION: same disposition as the immediately-preceding entries — appended via targeted Edit anchored on the exact prior last line to avoid a lost-update race against concurrent sibling QA-drain sub-agents; pruning deferred to a later single-writer cycle.

## cycle-552 · 2026-08-06 · FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, `commit_sha` present on the row itself: `66e850138`)

Commit `66e850138` confirmed real + on main ancestry. `git show --stat` matches the row's own claimed `files[]` (pre-commit.test.sh, brief `2026-07-21-commit-path-peer-index-sweep-guard.md`, `cowork-refactory-expert.md`, `dev-standards.md`) plus `scripts/git-hooks/pre-commit` itself — the actual hook the fix modifies, correctly not double-listed since `.test.sh` was already named. Read the shipped detector code directly (`pre-commit:650-704`), did not trust the commit-message's own "detector only, never gates" claim: `_detect_samefile_pathspec_only_divergence()` unconditionally `return 0`s, and the caller does `[ "$mode" = "SCOPED" ] && exit 0` regardless of what the detector wrote to stderr — AC-3 holds by construction. AC-1 (brief §2.7 + `dev-standards.md` CANONICAL block both documenting the same-file `--only` case as an explicit NON-GOAL, mirroring the existing §2.3 directory-pathspec precedent, correct `git-commit(1)` citation) and AC-4 (`cowork-refactory-expert.md:57`'s bare `git add .claude/cowork/ && commit` example migrated to the explicit pathspec form) both read and confirmed accurate against the live file content, not the diff summary alone.

Did NOT stop at the commit's own "12/12 PASS" claim — re-ran `scripts/git-hooks/pre-commit.test.sh` live on current `main` myself: got **13/13**, one more than the row claims. Investigated the discrepancy rather than treating it as a red flag or ignoring it: `git log 66e850138..main -- scripts/git-hooks/pre-commit scripts/git-hooks/pre-commit.test.sh` shows a later, separate commit `1c9b55d4d` ("same-file sweep-guard detector false-positives on unstaged pathspec commits") that added T13 and hardened the same detector this row shipped (gates the divergence check on `real_blob != HEAD_blob`, not just `real_blob` non-empty, to stop flagging tracked-but-never-`git add`-ed pathspec commits). This row's own T11/T12 both still pass unchanged on current `main` — the extra test is a genuine downstream improvement to this row's own detector, not a regression or a sign this row's tests were miscounted. Recording this because a shallow "test count doesn't match" read could have wrongly produced a CHANGES_REQUESTED here.

No production TS/Go touched (2 docs + 1 architecture brief + 2 shell scripts) — `git show --stat` confirms zero `.ts`/`.go` files in the diff, so `bun test`/`tsc --noEmit`/DDD-grep are structurally N/A, not skipped. Ran `mock-guard.sh --files` directly against all 5 touched paths anyway (never trust N/A from prose alone) → `"No production source files to scan. PASS."`. DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-2.md:44` task-id-stamped, predates this verify.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (orch-apply's own conservation check: task_total 754→754, signal_total 203→203 — OK). `next_agent: pm`. Full verification text appended to the row's own new field `qa_direct_commit_verify_20260806` (row carries no `status_note`/handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S6 (2026-08-06T21:47:24Z entry). Task lock held by this dispatcher's coordination session `24817246-8a3f-4511-95f7-1b4385797bee` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); flagging to the calling dispatcher to release `task:FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR` itself. NOTEBOOK RETENTION: same disposition as the immediately-preceding entries — appended via targeted Edit anchored on the exact prior last line to avoid a lost-update race against concurrent sibling QA-drain sub-agents; pruning deferred to a later single-writer cycle.
