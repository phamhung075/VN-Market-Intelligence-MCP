# QA — Notebook

## cycle-738 · 2026-08-14T14:50:00Z · FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION (dev-mcp-server, commit `8521c4a233`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`, no pre-existing `.commit`/`.files[]` drain fields but a fully self-contained `review_note` naming both commits. Confirmed `8521c4a233b2f02821bbc6e11ef7b0377b7c3840` (code+tests) + `ba4005738` (docs) real, on `main` ancestry, `git show --stat` matches every `files[]` entry plus 2 the row didn't list (`eveningSummaryJob.ts` also wired to the new wrapper; `1516-france-summary-foreign-flow.test.ts` AC-4 updated) — `assembleEveningSummary.ts` genuinely has ZERO diff, confirming the row's own flagged deviation (touching the frozen `foreign_net_vol <> 0` SQL filter would break `ZERO-PAD-TOPN` T-1/T-2) is accurate and correctly reasoned, not a silent scope-drop.

New `formatForeignFlowSectionOrUnavailable()` wraps the raw formatter, renders the canonical "Khối ngoại: Dữ liệu không khả dụng (pipeline tạm dừng)" line when `movers.length===0`, wired into both bulletin call sites; `franceSummaryJob`'s outer guard narrowed `length>0` → `!==undefined`. New test file: 11/11 pass, 18 expect — exact claim match. Row's own targeted 5-file sweep (1783+ZERO-PAD-TOPN+1516+1503+new): 47/47 pass — exact claim match. Went WIDER than the row's own claim: grepped every OTHER test file in the repo calling `formatFranceSummaryVI`/`formatEveningSummaryLines` (10 more files: 1519/1785/1512/1513/1383/1364/1552/1553/1450/1444) — 62/62 pass, zero regression across literally every production caller of the touched code.

`bun tsc --noEmit` 0 errors. `mock-guard.sh` on all 3 touched prod files PASS. DDD scan clean — both touched job files carry documented "Layer: interface/scheduler" headers, `infrastructure`/`application` grep hits are legitimate type/logger imports at that layer, zero domain-file violation; `foreignFlowSection.ts` itself has no imports at all. Security greps clean. `gen-project-stats.ts --dry-run`: toolCount=183/cronJobCount=88, exact claim match. Independently grepped every production (non-test) call site of the row's own load-bearing claim "`getForeignFlowMoversFn` confirmed test-only today" — `schedulerJobTable.ts`/`startScheduler.ts`/`orchestrateRecapCommand.ts` all call `runFranceSummary()`/`assembleEveningSummary({db})` with zero wiring of that param — claim holds. Full local `bun test` (own run, not the row's cited 15123/52 count): 15288 pass/40 skip/57 fail/48311 expect (678.68s) — within the documented 52-60 pre-existing flaky band cross-referenced against 15+ prior decision-journal precedents this repo; per standing `FIX-MCP-SUITE-HEALTH-BASELINE` literal 0-fail on the unscoped full suite is not the actionable gate here — the 15-file targeted+adjacent sweep is the valid net-new-regression check and it is fully clean.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S140.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field; `commit_sha` + `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write (row not RC-VERIF-grandfathered). Dry-ran the jq transform to a scratch file first and inspected it before applying — caught nothing wrong (an earlier same-session mixed-query probe threw a transient "Cannot index array with string" on the scratch file that did not reproduce on immediate re-query; treated as a read race, not a real structural fault — confirmed `type == object` and correct qa/done_verified lengths on clean re-reads before committing to the real write). `orch-apply.sh`: Stage0+1 PASS (unrelated pre-existing Stage 1g dependency-report noise only), conservation OK (task_total 736=736, signal_total 25=25). Re-read the live file after write: row physically absent from `.task_board.qa[]`, present in `.task_board.done_verified[]` with `status:"DONE_VERIFIED"`. Task-lock held by the dev-team dispatcher per spawn instruction — no `task_release` call made here.

## cycle-737 · 2026-08-14T14:39:45Z · DEFLAKE-VNSTOCK-3STATEMENT (dev-mcp-server, commit `8931e47c7`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha: "8931e47c7"` present. Confirmed real, on `main` ancestry; `git show --stat` matches all 3 claimed files (`schema.ts`, `vnstock-3statement.test.ts`, `1466-sync-db-corruption-bail.test.ts`), diff content matches every prose-claimed defect exactly (busy_timeout PRAGMA reordered to first on a fresh connection; on-disk `Date.now()`-keyed DB path replaced with true `:memory:`; the 1466 test's mock.module "restore" fixed to spread the captured module into a plain object instead of re-registering its own stub).

Independently re-reproduced every AC-4 claim, never trusted commit prose: 20x solo per-file-isolation `bun test vnstock-3statement.test.ts` = 0/20 fail; 27x interleaved (both orderings) with obs-3's co-failing sibling `167-prediction-market-job.test.ts` = 0/27 fail; 18x the 3-file mock.module repro = 0/18 fail — exact match to claimed "0/18 (was 8/18)". Broader-than-claimed 84-file vnstock/foreign-flow/schema zone sweep (claim was 25 files) = 892 pass/3 skip/0 fail — zero regression from the `schema.ts` connection-bootstrap change. `bun tsc --noEmit` 0 errors; `mock-guard.sh --files schema.ts` PASS; DDD/secret greps clean. AC-2's "no retries added" held — no `test.retry`/rerun logic found. Confirmed the one later commit touching `schema.ts` (`6ca2a0c65`) explicitly left the busy_timeout ordering untouched per its own message, re-grep-confirmed live.

Attempted an unscoped full `bun test` for AC-4's literal "full suite stays green" line but killed it: a concurrent peer session's own full-suite run drove both processes into CPU oversubscription, producing "timed out after 5000ms" false fails (environmental, not a regression — ironic given this is the exact flake class this row fixes). Per CANONICAL `docs/policies/dev-standards.md:1611`, full-suite literal 0-fail isn't the actionable gate anyway (standing `FIX-MCP-SUITE-HEALTH-BASELINE` red) — substituted the 84-file zone sweep as the net-new-failure check instead.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S139.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record` appended to `status_note`; `verification.raw_probe` attached same write (row not grandfathered). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (Stage0+1 PASS, conservation clean). Re-read the live file fresh right before write (qa lane had shrunk 7→4 from concurrent peer QA-drain sub-sessions since first read) to avoid a CAS-stale candidate; re-read after write to confirm the lane-move landed. Task-lock held by dispatcher — no `task_release` call made here.

## cycle-736 · 2026-08-14T14:35:00Z · FIX-NOTEBOOK-AUTOPRUNE-REGEX-HEADING-MISMATCH (developer, commit `3a2b9228d`) — Direct-Commit Verify, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row predating the drain (no `.commit`/`.files[]`/`.owner`-field wiring, `entered_review_at: 2026-07-12T07:36:27Z`, over a month old). Derived commit via `git log` on `scripts/agents-flow/notebook-auto-prune.sh` + date cross-check: `3a2b9228d25d2f95ba8c9a63fc1f45273f1b4ba5` (2026-07-12T09:37:51+02:00), confirmed real + on `main` ancestry, `git show --stat` matches every file the review_note claims (notebook-auto-prune.sh, its test file, main.md dedup, orch-state.json, decision journal, follow-up signal).

Did NOT trust review_note prose. Independently re-derived the widened regex logic (originally inline in this commit, extracted 2026-08-06 by `7552421bc` into `scripts/agents-flow/lib/notebook-section-direction.sh`'s `nso_ts_key()` — the extraction's own header claims "zero behavior change", re-verified true here) against all 4 real heading shapes the row claims: compact `cycle-YYYYMMDDTHHMMZ`, dashed-no-seconds `Tick YYYY-MM-DDTHH:MMZ`, dashed-with-seconds/ms, date-only — all resolve to the correct 17-char sort key; undated headings correctly sentinel. Ran BOTH regression suites inside an isolated `git worktree --detach HEAD` (kept the shared dirty tree, and a DIFFERENT concurrent developer's live uncommitted WIP, completely untouched): canonical `notebook-auto-prune.test.sh` 10/10 PASS; legacy `test-notebook-auto-prune.sh` tests 1-4 (this row's own 2 new real-format regression cases, exactly as claimed) + 5-8 (later dup-heading task) all PASS. Test 9 (a LATER, unrelated task's bash-vs-zsh AC-3 check, added 2026-07-31) crashes on `${BASH_SOURCE[0]}` under zsh — chronology proves this is caused by even-later, unrelated commits (UC-CRITIC-HOOKS-ENFORCEMENT's hook-guard.sh sourcing + the 2026-08-06 lib extraction, both landing after 3a2b9228d) and is already a separately-tracked BACKLOG row (`FIX-NOTEBOOKAUTOPRUNE-HOOKGUARD-BASHSOURCE-ZSH-BREAK`) — not this row's regression, not blocking. `mock-guard.sh`/DDD-import-greps/secret-greps on the touched production file: clean (mock-guard does not scan `.sh` as production source; zero real hits).

FLAGGED per dispatcher instruction, NOT resolved: this row's own regex-widening fix and the concurrent task `FIX-NSO-TS-KEY-COMMIT-SHA-DIGITS-PARSED-AS-DATE` touch the EXACT SAME file+function (`lib/notebook-section-direction.sh`'s `nso_ts_key()`) — confirmed via `git blame`/`git diff HEAD` showing an in-flight uncommitted rewrite dated 2026-08-14T16:26:03+02:00 (today, live at verification time). An adversarial probe (heading with a trailing numeric commit-SHA) confirms this row's own "optional time-suffix + `tail -1`" widening is precisely what created the SHA-collision surface the concurrent fix is now closing — a real, adjacent defect, but out of THIS row's AC scope (which required matching real heading FORMATS only, not defending against adversarial digit collisions in heading text). No conflict resolved here, just flagged.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached (RC-VERIF schema gate). `orch-apply.sh` accepted on first attempt with the probe attached. Re-read the board fresh after the write to CONFIRM the row physically left `.task_board.qa[]` and landed in `.task_board.done_verified[]` — not trusted from the write command's exit code alone. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S138.

Non-blocking, flagged not fixed: this rigor was prompted by finding a 4th live instance of this exact batch's own recurring failure shape (already self-documented at cycle-734/qa-S135 as its 3rd instance: "review-record prose appended, lane-move never executed"). `FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH` (a peer cycle's own qa-S137, claimed DONE_VERIFIED 14:31:12Z): `.status` reads `DONE_VERIFIED` on two reads minutes apart, but the array element is STILL physically inside `.task_board.qa[]`, never relocated to `.task_board.done_verified[]` — violates this flow's own CANONICAL:SSOT-STATUSFLIP-LANEMOVE ("status-flip = lane-move MUST, same write"). Unlike the prior 2 instances (self-resolved by a peer re-pass before this cycle read them), this one was still unresolved as of this write. Flagged to router/dispatcher, not corrected (different row, out of this task's scope).
