# QA — Notebook
## cycle-747 · 2026-08-14T15:02:15Z · MD-FUNC-01-FIX (dev-mcp-server, commit `2fb65b5bc1`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`). Commit `2fb65b5bc1c75a2ac12bd2f11e4382799e021786` (memory/journal-only, zero prod-code diff) confirmed real, on `main` ancestry; `git show --stat` matched both `files[]` entries exactly (dev-mcp-server notebook + decision journal). Did NOT trust status_note prose alone — read `marketTools.ts:334-347` live: `vn_index{price,change_pct,direction}` struct genuinely present, `direction` dynamically derived from `vnIndex.changePct` sign (not hardcoded), `price`/`change_pct` sourced from `fetchVnIndex()`'s real VNDirect fetch. Re-ran independently: `1982-quality-burndown-CHIJ.test.ts` MD-FUNC-01 section 18/18 pass (exact claim match); 3 adjacent market-tool test files alongside = 82 pass/0 fail total. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files marketTools.ts` PASS. `process.env`/secret greps clean. Cross-checked referenced fix commits `815ccaedd`/`ddc36452e`: both real, on `main` ancestry, predate this verify commit. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S142.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record` appended to `status_note`; `verification.raw_probe` attached same write (not RC-VERIF-grandfathered). Board write confirmed durable (row absent from `qa[]`, present in `done_verified[]`, `status:"DONE_VERIFIED"`, `qa_verified_at`/`qa_verified_by` set) — re-read live file post-write to confirm. This notebook entry itself required 7 attempts to land under extremely heavy concurrent peer QA-drain full-overwrite contention on this shared file (same condition a peer's own cycle-745 entry independently reported). Task-lock held by the dev-team dispatcher per spawn instruction — no `task_release` call made here (INV-GATEWAY-1).

## cycle-745 · 2026-08-14T15:03:19Z · FIX-BCTC-REPARSE-DOUBLE-WRAP-DEDUP-GUARD (dev-mcp-server, commit `cf862f920`) — Direct-Commit Verify, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`). Commits `cf862f920`(code+test)+`815752129`(journal+notebook) confirmed real, on `main` ancestry; `git show --stat` matches all 3 claimed files exactly. AC-1 `shouldSkipRecoveryReplay` guard added before `recordJobRun`. AC-2 default fn maps `resolved+failed`→`rowsWritten`, neutralizes `bctcReparseJob.ts:941`'s self-record block (confirmed no bypass callers). AC-3 startup catch-up gated by `shouldRunCatchup`. New test 13/13 pass incl. AC-4 case. 53-file regression 503/1skip/0fail. tsc 0 errors. mock-guard PASS. DDD/secrets clean. AC-5 correctly deferred. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S145. Board write confirmed durable (commit `a25a790f2` lineage) — this notebook entry itself was lost repeatedly to concurrent peer full-overwrites before landing.

## cycle-745 · 2026-08-14T15:06:00Z · BCT-OBS-01-FIX (dev-mcp-server, commit `ccddb9ae6a2`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `commit_sha` present, `.files[]` = notebook + decision-journal only (verification-only task, no prod code touched). Confirmed real, on `main` ancestry, `git show --stat` matches both `.files[]` entries exactly. Re-read `dataAuditJob.ts`/`telegram.ts` source directly, both AC confirmed exactly as claimed. Re-ran `157-data-audit-job.test.ts`: 22/22 pass. `bun tsc --noEmit` 0 errors. Went WIDER: live `cron_job_runs`/`system_logs` probe confirmed the gate still fires TODAY, not just historically; `docker exec` confirmed Telegram env vars still set live. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S143.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record` appended to `status_note`; `verification.raw_probe` attached same write. Board write confirmed durable. Very heavy concurrent peer QA-drain contention on both decision journal and notebook this cycle.

## cycle-748 · 2026-08-14T16:05:24Z · FIX-BCTC-REPARSE-PERIOD-KEY-SYSTEMATICALLY-STALE-100PCT-QUARANTINE (dev-mcp-server, commit `6cb8641d2`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

`review[]` row (already committed to `main`, no branch). 4 commits confirmed real main-ancestry: `6cb8641d2`(code+test) `a1eb8f164`(docs) `2884d8614`(board lane-move) `04cb7d39c`(notebook). `git show --stat 6cb8641d2` touches ONLY `bctcReparseJob.ts`+new test file — `periodContentExtractor.ts` (the guard) byte-for-byte untouched, satisfying the row's own "must not weaken the guard" constraint. Root cause: `reparseSingleWithOcrFallback` derived its supplied period ENTIRELY from `parseYearQuarterFromFilename(payload.filename)` (stale queue-row filename), never consulting extracted text, so the write-time `checkPeriodContentConsistency` guard correctly refused every reparse (100% quarantine, 19/19 real incidents). Fix calls the same pure `extractPeriodFromContent(rawText)` the guard itself uses and prefers its signal when confident, falling back to filename-derived period only when content is inconclusive. Traced by code-read (not trusting AC prose): the identical `rawText` string flows verbatim through `pdfTextOverride` → `resolvePdfText` → `checkPeriodContentConsistency`, and since `extractPeriodFromContent` is pure+deterministic, the guard's recomputation always matches the upstream signal — `consistent:true` is now guaranteed whenever content is confident; inconclusive-content path provably unchanged (regression-proof by construction, not just by test sample). Ran independently: new test file 2/2 pass; targeted 8-file bctc-reparse/guard regression 91/0 fail/217 expect() (superset of claimed 73); full suite 15290 pass/40 skip/57 fail — EXACT match to implementer's claim; grepped all 57 fail lines, zero reference reparse/period-key/bctcReparseJob; spot-verified 2 BCTC-adjacent fail files (`293-ocr-fallback-pipeline.test.ts` isolated-run reproduces identical 4/6 timeout fail, `FIX-1267`) neither imports `bctcReparseJob.ts` — pre-existing/unrelated confirmed independently, not just via git-stash claim. `tsc --noEmit` 0 errors. DDD clean (domain fn zero imports, correct scheduler→domain direction). Security/secrets clean. `mock-guard.sh` PASS. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` STEP dev-mcp-server-S4, `task-id:` present — DJ-GATE-1 satisfied. Verification gate `reparse_run_stores_at_least_one_row_no_period_mismatch` directly demonstrated by AC-2 (pipeline receives content-consistent period, `result:true`, no mismatch-error path taken). Minor non-blocking note: board row's own `.files[]` (missing `financial-reports/` subdir) predates this implementer (po/triage mint-time metadata), not an implementation defect.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record` + `verification.raw_probe` appended same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 718/signal_total 22 unchanged). Row moved `review[]`→`done_verified[]`. `.head` was already pointing elsewhere (`UC-ASL-P3`) before this write — confirmed unaffected, no head-stale correction needed (known bug class checked and ruled out, not applicable here).
