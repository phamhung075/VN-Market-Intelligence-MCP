# QA — Notebook

## cycle-375 · 2026-07-03 · FIX-ALERT-COMMANDER-DEAD-NO-SLOT — PASS (config-restoration + risk assessment)

Gated cowork-refactory-expert's config-only fix (commit 6b86a47aa: cowork-schedule.json +2 alert-commander slots, system-map.json:1312 text, dev's own journal — exactly 3 files, 0 board/orch-state/code touch) | Verdict: PASS. `jq -e .` on both JSON files → exit 0/0. Slot field-shape diffed (`keys|sort`) against `news-scout-offhours` + `bctc-analyst-slot-1`: full parity except `_note`, an established optional field used by 9 other slots. Cron correctness verified functionally not just by eye: `node scripts/agents-flow/cowork-match-slots.test.js` 16/16 pass, TC-4/5 exercise `*/15 2-8 * * 1-5` verbatim; `0 */4 * * *` byte-identical to the proven `news-scout-offhours` cron. `flow_path` (`docs/agents/alert-commander/flow/main.md`) exists. system-map:1312 text cross-checked against alert-policy.md:46 + cycle.md:17 + stage-signals.md:21 — accurate. Reactivation-flood risk (router DoD item 4) traced into actual tool source, not just flow docs: `verified_chain`/`urgent_news` (via `get_cycle_bootstrap`→`get_agent_signals`) are self-limiting (TTL expiry + read-marking, agentSignalStore.ts:896-913); `crisis_velocity` (`get_crisis_early_warning`) is a live current-hour computation, not a historical replay — neither floods. `legal_risk` is a CONFIRMED, NOT-fully-guarded gap: `stage-bootstrap.md:42` calls `get_legal_risk_signals()` bare (no days/hours_back anywhere in flow docs), tool defaults `days=30` (legalRiskTools.ts:236) with no expiry/read-state/already-alerted tracking, feeding Stage 3's unconditional `legal_risk | any | CRITICAL now` — plus alert-policy.md explicitly bars suppression for legal_risk. Any legal_risk hit inside the rolling 30-day window will re-fire as "new" CRITICAL every subsequent cycle (15min market-hours / 4h off-hours) until it ages out — `write_alert_verdict` is a blind append, no pre-check. Pre-existing in unmodified alert-commander flow docs + legalRiskTools.ts (this commit touches neither) — routed as a mandatory follow-up (new BACKLOG task recommended: bound `get_legal_risk_signals()` to a short recency window or add already-alerted dedup) rather than a block, since blocking would leave alert-commander dead longer (worse than a real-but-repeated accurate alert). No `.ts` files touched by this commit → full bun test/tsc suite out of scope per Smart-Skip spirit; ran the two existing tests that directly exercise the touched surface instead (DWF-phase1-cadence.test.ts 51/0, cowork-match-slots.test.js 16/16). 0 UUID/secret/process.env leak. DJ: sprint-FIX-ALERT-COMMANDER-DEAD-NO-SLOT-qa.md §qa-S1/S2. Report: reports/TASK_REPORT_FIX-ALERT-COMMANDER-DEAD-NO-SLOT.md. Board flip left to router — did not touch orch-state.json myself.

## cycle-374 · 2026-07-03 · FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD — PASS (deep-verify)

Independent DoD gate on dev-mcp-server's module-level `_isRunning` overlap guard for `bctcPdfPullJob.ts` (commit 8bc0b5b5, 3-file scope: prod file + new test + dev's decision journal — matches dispatch exactly, 0 UUID leak) | Verdict: APPROVED. Read the full 619-line diff line-by-line (not stat alone): it is a mechanical try/finally re-indent of the pre-existing body (zero logic altered), cross-checked byte-identical in shape against `breadthHistoryPersisterJob.ts` (module `let _isRunning`, early-return zero-counts before any DB query, `finally{_isRunning=false}`) — the "mirrors verbatim" claim holds. `bun tsc --noEmit` 0 errors. New test alone: 3 pass/0 fail/25 expect (test (c)'s throw-path genuinely executed, not assumed). 6 suites that actually import `bctcPdfPullJob.js` (1352a-async-extraction-race, B3-space-urls-fix, FIX-BCTC-ENRICH-SILENT-0ROWS, FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD, FIX-BCTC-VPS-QUEUE-SYNC, bctc-pdf-pull-job) together: 66 pass/0 fail/239 expect. Full suite: 14236 pass/42 skip/62 fail/5 errors/44663 expect across 1169 files (627.33s), then the known Bun-1.3.13 C++ teardown panic — ceiling=348 (docs/data/project-stats.json), 62<<348. Every `(fail)` line and every "Unhandled error between tests" block (5, all pollNews 5s timeouts) mapped to source file and grepped for bctc/pdfpull/financial-report keywords → 0 hits; one filename false-positive (`1405b-bctc-vps-fixes.test.ts`) inspected directly — pre-existing `vps_push_log` DB race, does not import the changed file. Zero changed-domain regressions. DDD: file lives in `scheduler/` (outermost layer per `domain←application←interface←scheduler`, ARCHITECTURE.md L7) — its infra imports are architecturally permitted, not a violation. Security: 0 process.env, 0 hardcoded secrets, mock-guard exit 0. Sole caller `startScheduler.ts:361` reads only `.downloaded` (grep-confirmed only call site) — unaffected by the additive `skippedReason` field. DJ-GATE-1: dev-mcp-server's `sprint-FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD.md` carries task-id — gate satisfied. No handoff `.md` created — ad-hoc FIX/S ticket off a SPIKE finding, no PM/dev handoff-doc precedent for this task class (matches 3 preceding ad-hoc gates). DJ: sprint-FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD-qa.md §qa-S1. Report: reports/TASK_REPORT_FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD.md. Board flip left to router — did not touch orch-state.json myself.

## cycle-373 · 2026-07-03 · FIX-FB-DAILY-FIRER-CLI-FLAG — PASS (bash-script scope, no test suite)

Verified developer's 1-line fix to the launchd fb-daily-firer backstop (commit b19ac43c5, deletes `--no-update-notification` at scripts/cowork-fb-daily-firer.sh:138; 66ed3df09 journal-only) | Verdict: APPROVED. Per task scope, did NOT run the full TS suite (irrelevant to a bash script, zero test coverage). `git show b19ac43c5 --stat` = exactly 1 file, 1 deletion, the deleted line is `--no-update-notification`. `bash -n` clean (0 syntax errors). Installed CLI (`claude --version` 2.1.198): `--help` shows `--dangerously-skip-permissions` (grep -ic 2) and zero hits for `--no-update-notification` — confirms genuinely unsupported, removal correct not a version-skew guess. Live-reproduced the incident verbatim by re-adding the flag: `error: unknown option '--no-update-notification'` matches `fb-daily-firer-error.log` exactly. `grep -rn "no-update-notification" scripts/ launchd/` → 0 hits, no other broken caller. Root-cause doc `docs/signals/router-fbfirer-dead-cli-flag-20260703.md` matches the diff 1:1; secondary items (double-fire cadence, log tee-noise, untracked-log gitignore) correctly left out of scope. Deliberately did NOT invoke the corrected flag set live against the real flow path (would trigger a genuine fb-market-poster publish side-effect, out of scope for a flag-syntax check). DJ: sprint-MERGE-MONEY-RADAR-INTO-MOMENTUM-qa.md §qa-S3 (ad-hoc task, resolved to the active sprint's journal per convention). Report: reports/TASK_REPORT_FIX-FB-DAILY-FIRER-CLI-FLAG.md. Board flip left to router — did not touch orch-state.json myself.

**Retention note:** cycle-372 (FIX-BCTC-BANK-BS-COLUMN-ORDER — PASS) and older pruned this write per AC-2 (keep last 3). Full detail preserved in `docs/agent-memory/decisions/sprint-FIX-BCTC-BANK-BS-COLUMN-ORDER-qa.md`, `reports/TASK_REPORT_FIX-BCTC-BANK-BS-COLUMN-ORDER.md`, and prior git history of this file (which also carries cycle-371's collapsed pointer: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
