# QA — Notebook
## cycle-771 · 2026-08-14T19:37:53Z · FIX-BCTC-SLA-FRESHNESS-EXCLUDE-TERMINAL (apps/mcp-server/, commit `f0831e6be`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — files derived from the row's own `note` prose, cross-checked directly against `git show --stat`. Commit `f0831e6be` confirmed real, on `main` ancestry; touches exactly the 3 claimed files (`freshnessSlaChecker.ts`, `freshnessSlaMonitorJob.ts`, `FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts`). Read the diff directly rather than trusting prose: `queryBctcPipelineRuntimeState()` narrowed the bctc `queueDepth` SQL from `status IN ('pending','url_not_found','enrich_failed','pek_triggered')` to `status IN ('pending','pek_triggered')` — a generic status-list exclusion (no ticker/date literal), satisfying the backlog's `generic_mandate` ("Exclude ALL terminal states generically — no per-ticker carve-out"). A reader contract was also documented on `PipelineRuntimeState` in the domain file (`freshnessSlaChecker.ts`) for future queue-backed readers. Re-ran REAL verification independently, not trusted from the row's own claim: touched test file 23/23 pass (new/updated cases C-2, C-2c, C-2d, C-10 directly assert queueDepth excludes url_not_found/enrich_failed/done/deferred_infra generically, and a queue holding ONLY terminal rows produces `queueDepth=0` and zero CRASH escalation — satisfies the backlog's `verification_gate`). Independently derived the same "25-file" targeted freshness/SLA suite the commit's own note claims (grep for `freshnessSlaChecker|freshnessSlaMonitorJob|queryBctcPipelineRuntimeState` across `src/__tests__`) and re-ran it fresh: 338/338 pass, 862 expect() calls — matches the note's claim exactly. `bun tsc --noEmit`: 0 errors (exit 0). `mock-guard.sh --files` (2 prod files): PASS. DDD: `freshnessSlaChecker.ts` is a `domain/` file with zero `infrastructure`/`application` imports — clean. `process.env`/secrets grep clean on both touched prod files. Independently confirmed the note's claim that a sibling duplicate bug in `vpsHealthPoller.ts` was "deliberately NOT touched" — genuinely absent from the commit's diffstat. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S12.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (row had no `status_note`/`review_note`, only `note`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=696/candidate=696, signal_total live=37/candidate=37; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). `.head.active_task_id` was a different row in the same 8-row QA-Drain batch (`FIX-DEVTEAM-COLDEVICT-FAILURE-REPORT-SWALLOWS-STDERR`) — left untouched, not this task's responsibility to reset. Dispatcher-wrap lock `task:FIX-BCTC-SLA-FRESHNESS-EXCLUDE-TERMINAL` cleared via this board write per spawn-prompt instruction — no direct `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Section count now 6 in this file (>3-cap) — same live concurrent-peer write contention already documented by every section above; appended additively only, no existing section touched; next single-writer cycle should reconcile back to the cap.

## cycle-772 · 2026-08-14T19:38:29Z · FIX-DEVTEAM-COLDEVICT-FAILURE-REPORT-SWALLOWS-STDERR (cross-service/, commits `e817334d1`+`4658cf1b0`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `.head.active_task_id` this row per spawn prompt). Row's own `.files[]` (`docs/agents/dev-team/flow/main.md`, `scripts/orch-cold-evict.sh`) matched NEITHER commit's actual diffstat — developer's `status_note` already explains why: the real swallow point is `scripts/agents-flow/dev-team-tick-preflight.sh` Step 5.5 (`_step55_run_cold_evict`), not `main.md` (no inline copy exists there). Used the flow's Fallback path instead of hard-failing on the stale files[] claim: re-derived the real 4-file set via `git show --stat` (`docs/WORK.md`, `docs/agents/dev-team/flow/post-cycle.md`, `scripts/agents-flow/dev-team-tick-preflight.sh`, `scripts/agents-flow/dev-team-tick-preflight.test.sh`). Both commits confirmed real, on `main` ancestry. Read the diff directly, not trusted from prose: `_step55_run_cold_evict()` already captured combined stdout+stderr into a LOCAL var (printed only to this script's own stderr) but never exposed it past return — confirmed the caller previously branched on exit code alone. Fix stashes output+rc into module globals; new `_step55_is_benign_cas_loss()` regex checked byte-for-byte against `orch-cold-evict.sh:1081`'s live `ABORT: CAS retry limit (${MTIME_CAS_RETRIES}) exceeded — concurrent writer; hot file unchanged` line — exact match, not a guess. Benign-CAS loss now logs only (zero telegram); genuine failure now reports real exit code + truncated verbatim stderr tail via the file's pre-existing `_trunc()` helper. `post-cycle.md` Step 4.2 doc-parity update read and matches the runtime spec. Re-ran REAL verification independently: full `dev-team-tick-preflight.test.sh` = 154/154 — independently confirmed the exact 146-pre-existing baseline by checking out parent commit `5ad6a51a8` into a scratch git worktree and re-running BEFORE reading the fix's own diff (not accepted from commit-message prose); 8 new checks = T30b (benign CAS-loss → zero telegram) + T30c (genuine failure → telegram carries exit code + verbatim stderr), matches claim exactly. `shellcheck -S warning` clean on both changed files. `mock-guard.sh --files` on the touched prod script: PASS (pure bash, no TS/prod-domain source — N/A class). `process.env`/secrets grep clean. `verification_gate` (`next_coldevict_failure_report_names_the_actual_error`) satisfied structurally, not just narratively: genuine-failure telegram literally embeds `exit <rc>` + `stderr: <tail>`; benign CAS-contention loss (the exact transient class PO's manual re-runs proved across 23 prior occurrences) now emits zero telegram. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S11.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=696/candidate=696, signal_total live=37/candidate=37; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. `.head.active_task_id` WAS this exact task-id (confirmed live before write) → reset to idle (`status:idle, active_task_id:null, next_agent:router`) in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b) — no new dispatch issued in this write, matches the qa-S3 precedent this same sprint. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`, `.head` idle). Dispatcher-wrap lock `task:FIX-DEVTEAM-COLDEVICT-FAILURE-REPORT-SWALLOWS-STDERR` cleared via this board write per spawn-prompt instruction — no direct `task_release` call made (no gateway/MCP tool binding this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Dropped oldest section (`cycle-769`) this write to reconcile the file back to the 3-section cap (was 6 sections from concurrent-peer accumulation, now 3) — no other retained section touched (AC-2a).

## cycle-773 · 2026-08-14T19:37:50Z · FIX-AGENTSIGNALS-EXPIRED-GC-CRON (apps/mcp-server/, commit `f5698cb76`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]`/`.owner` drain fields — 5 files derived via `git show --stat`. Commit `f5698cb76dce925af4cb2c4ac5530fccd268158f` confirmed real, on `main` ancestry, still unreverted at current HEAD (`051299dbd`). Row carried a prior `po_ruling_20260801T0222` (next_agent qa→ops for a mcp-server rebuild before live-verify, to avoid verifying against a stale pre-fix image) — confirmed the row's own `[ops 2026-08-11T18:30Z]` note is genuine and already satisfied (single-service rebuild completed, image `62dbe9da404d`) before proceeding. Read the diff directly rather than trusting prose: `agentSignalStore.cleanExpired()` and `checkStaleAlerts()` D-3/D-4 both wrap the predicate column in `datetime()` (not just the RHS threshold) per `feedback_sqlite_iso8601_datetime_strcompare_bypass`; D-4's threshold tightened `60d`→`30d`, `resolution_notes` marker updated to `auto-resolved by retention GC`, UPDATE-only (never hard-deletes) — deliberately did NOT narrow `cleanExpired()` to a literal `status IN ('read','expired')` since no `'expired'` status is ever written — matches the row's own root-cause note exactly (existing `dataAuditJob:daily` cron already covers both tables). Re-ran REAL verification independently: 3 targeted test files (`157-data-audit-job.test.ts`, `242-agent-signals.test.ts`, `FACTORY-SCHEDULER-split-dataAuditJob.test.ts`) 47/47 pass fresh, 171 expect() calls. `bun tsc --noEmit` (apps/mcp-server): 0 errors (empty output). `mock-guard.sh --files` (2 prod files): PASS. `process.env`/secrets grep clean on both touched prod files; DDD clean (both files are infra/scheduler layer, no `domain/`→infra/application violation). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S12.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (row had no `status_note`/`review_note`, only `note`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=696/candidate=696, signal_total live=37/candidate=37; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row) — this board write already landed durably before this notebook append (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). This is a re-append: an earlier append of this same entry (as `cycle-772`) was lost when a concurrent peer QA sub-session overwrote this file mid-cycle with its own drop-oldest-section prune (same class as the already-documented `cycle-769` re-append above, and every section-collision note in this file's recent history) — re-appending to restore the record, not re-running the verification. Dispatcher-wrap lock `task:FIX-AGENTSIGNALS-EXPIRED-GC-CRON` cleared via the earlier board write per spawn-prompt instruction.
