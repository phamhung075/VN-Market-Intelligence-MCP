# QA — Notebook

## cycle-409 · 2026-07-09 · CI-RED-06043b3c-FIX — GH Actions confirmed green, routed to po

verification_gate=ci_green_on_subsequent_push check for the fix commit (68c2de81c6, "add \"done\" to validStatuses" in `1837a-pipeline-state.test.ts` L96 — dev-mcp-server, one-line test-enum addition, test-file-only, rebuild_required=false; router had already independently confirmed commit content/diff/board fields, not re-litigated here).

`gh run view 28990352248 --json name,status,conclusion,jobs`: run already `status=completed` by the time I checked (no polling needed) — `headSha` 68c2de81c6bdc99be848fcbccf7a017ac19e6844 (exact match to the fix commit) and `displayTitle` matches the commit subject verbatim, so no stale/superseded-run risk. Overall `conclusion=success`, all 8 jobs green including the previously-failing one: **"bun test"** (databaseId 86028554823) `conclusion=success`, step "Run tests (per-file isolation)" completed 02:44:32Z→02:46:48Z with no failure. Other 7 jobs (py-lint, Kinh Dich/Macro/API-Gateway/Alert-Engine/Stock-Price go-lint, go-lint) also all `conclusion=success`.

VERDICT: CI green confirmed end-to-end for this fix. DJ written first (DJ-GATE-1): sprint-CI-RED-06043b3c-FIX-qa.md §qa-S1. Added `qa_verify_at`/`qa_verify_by`/`qa_note` (cites run 28990352248 + bun-test job 86028554823, both conclusion=success) to the existing `task_board.review[]` row, moved `status` REVIEW→READY, `next_agent` qa→po, via `jq`+`scripts/orch-apply.sh` (never raw overwrite); did not touch dev-mcp-server's `review_note`. Committed board+journal+notebook with explicit paths only (no `git add -A`) — working tree has heavy unrelated concurrent churn from other agents.

## cycle-410 · 2026-07-09 · FACTORY-PDF-fix-application-infra-leak — Step 5 live-verify PASS, routed to po

Step 5 after dev-pdf-extractor swapped `application/doclang_serialize_usecase.py`'s concrete `infrastructure.DocLangSerializer` import for a `DocLangSerializerPort` Protocol (commit bfe92c225, main.py wiring untouched, pure type/import — zero intended behavior change) and ops's Steps 1-4 (image b9f9c26cb82d).

No HTTP route triggers doclang-serialize yet (handlers.py: "wired, not yet called") — so no direct live-endpoint exercise was possible; used the (b) fallback. Re-confirmed independently: live image=b9f9c26cb82d exact match to ops's report; `GET /health`=200; zero error/exception/traceback lines in container logs since the swap; `lint-imports` re-run live in-container = 3 contracts kept/0 broken including the new Fence-C (application→infrastructure,interface forbidden); `grep ^from infrastructure application/` on the container = zero hits. Re-ran `test_doclang_serializer.py` live in-container myself: 8 passed/5 skipped — not 13/13 as dev reported from their sandbox venv; traced the 5-skip to a pre-existing `except ImportError: pytest.skip("doclang not available (expected in container)")` guard (the optional `doclang` XSD-validator package is genuinely absent from the production image by design) — confirmed via `git show bfe92c225 --stat` that this test file was NOT touched by the diff, so the discrepancy is an environment difference, not a regression.

Went further than the unit tests: exercised the EXACT composition-root wiring from main.py (`DocLangSerializer`+`FilesystemDocLangWriteAdapter` → `DocLangSerializeUseCase`, same construction args) live in the container with synthetic sample-fixture data (scratch `/tmp` write dir, cleaned up after, zero production data touched) — produced correct well-formed `.dclg.xml`, sample header/row content faithfully round-tripped through the serializer, confirming the Protocol-typed usecase param still accepts the concrete infrastructure class correctly at runtime (structural typing verified live, not just via test double). 13/13 docker services healthy, zero peer regression.

VERDICT: PASS. Added `qa_verify_at`/`qa_verify_by`/`qa_note` to the existing `task_board.review[]` row, `next_agent` qa→po, status stays `REVIEW` (this task's convention, matching cycle-408/qa-S29/qa-S30 precedent — not the READY convention used for non-docker CI-only fixes) — did not overwrite dev-pdf-extractor's `review_note` or ops's `ops_note`/`ops_verified_at/by` — via `jq`+`scripts/orch-apply.sh`; `.head` synced same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S31.

**Retention note:** cycle-408 (FACTORY-PDF-split-extractLayoutFirst-execute) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file.

## cycle-411 · 2026-07-09 · FACTORY-FRONTEND-extract-computeDecision — Step 5 PASS, routed to po

Step 5 after dev-frontend moved `computeDecision`/`DecisionResult` out of `dashboard.analysis.tsx` into `app/domain/analysis/decision.ts` (commit 2819d710c, 13 magic numbers hoisted to named consts) and ops's Steps 1-4 (image 871d76885836). Router explicitly flagged ops's own report/journal as unreliable this cycle — treated nothing as pre-verified, redid the container/route checks myself too, not just the diff/test asks.

Independently confirmed: (1) container `docker ps`/`docker inspect` — healthy, `Image`=sha256:871d76885836, `git_sha` label=5d9ec1859, 5 consecutive healthy checks. (2) `verify-deploy-sha.sh frontend` now reads drift (deployed=5d9ec1859 vs HEAD=6a0f93948) — investigated, not accepted at face value: `git diff --stat` between the two shows only `orch-state.json`+notebook/journal, zero `apps/frontend/` — benign doc-only post-build drift (known class, memory `project_verify_deploy_sha_benign_doc_drift`), and `2819d710c` confirmed ancestor of the deployed SHA with zero further frontend diff — deployed image genuinely carries the reviewed code. (3) Line-by-line diff of old-inline vs new `decision.ts`: all 13 named consts map 1:1 to the original literals, if/else structure and label/color mapping verbatim — no sign-flip/off-by-one found. (4) Live route `/dashboard/analysis?stock=VCB`=200, decision panel rendered real "GIỮ" output with reasons "KD: GIU (tich cuc)" + "Giá -1.1% (5 phiên)" — hand-recomputed the score (0 + -1 = -1 → HOLD_SCORE band) and it matched exactly, i.e. genuine behavioral proof not a bare 200. (5) Retargeted test `1937-decision-logic.test.ts` re-run myself: 10/10 pass. (6) Full suite: 2047 pass/2 fail (QUE-TOOLTIP/QUE-REFERENCE, confirmed pre-existing/unrelated — file not touched by this diff); `FE-HEADER-SSOT-top-nav.test.tsx` 26/26; `tsc --noEmit` 0 errors.

VERDICT: PASS. Board `task_board.review[]` row stays `status:"REVIEW"`, `next_agent` qa→po, `qa_verify_at`/`qa_verify_by`/`qa_note` added (dev-frontend's `review_note`/ops's `ops_note` untouched) via `jq`+`scripts/orch-apply.sh`; `.head` synced same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S32.

## cycle-412 · 2026-07-09 · FACTORY-FRONTEND-split-dashboard-analysis — Step 5 PASS, routed to po

Step 5 after dev-frontend split 1836L `dashboard.analysis.tsx` into 5 pure formatters (`app/domain/formatters/*`) + 22 presentational components (`app/components/analysis/*`, all ≤120L) — route down to 476L (19 commits a5e629411..41279090e) — direct sequel to FACTORY-FRONTEND-extract-computeDecision (cycle-411/qa-S32). ops's Steps 1-4 (image sha256:2135c729b9a9, container healthy, `git_sha` label=4c4c59f3f).

Independently confirmed, not trusted from ops's/dev-frontend's prose: (1) `docker inspect` — live image=sha256:2135c729b9a9 exact match, container healthy. (2) `verify-deploy-sha.sh frontend` reads drift (deployed=4c4c59f3f vs current HEAD=b907a8ea6) — investigated: `git diff --stat` between the two touches only orch-state.json/notebook/journal, zero `apps/frontend/` — benign doc-only post-build drift (same class as cycle-411/qa-S32, memory `project_verify_deploy_sha_benign_doc_drift`); deployed image genuinely carries the reviewed code. (3) Live curl content checks on both routes (not bare 200s): base `/dashboard/analysis` — Watchlist grid real counts, KinhDichMarketPanel+MacroSignalPanel both render real data (zero "Không có dữ liệu" fallback), StockDetailPanel/AiDeepDivePanel correctly absent (no stock selected). `?stock=VNM` — decision panel renders "GIỮ" reasons "KD: GIU (tich cuc)"+"Giá +1.3% (5 phiên)" — hand-recomputed (KD no-match=0, price+1, score=1 in [HOLD_SCORE=-1,BUY_SCORE=2) band) matched exactly; InfoSourcePanel/MacroImpactPanel/StockDetailBottomGrid(Kinh Dịch+MiniPriceTable)/AiDeepDivePanel (real brief sections, not 404 fallback) all present with real content, zero error-boundary/crash markers. (4) Full vitest re-run myself: 2047 pass/2 fail (QUE-TOOLTIP/QUE-REFERENCE, confirmed pre-existing+unrelated — zero commits in the extraction range touch either file). tsc 0 errors. eslint: same 5 pre-existing `react-hooks/exhaustive-deps` config errors in CorporateEventsZone/FinancialsZone/TechnicalZone (confirmed zero diff on those files in range). (5) Playwright `render-check.spec.ts` (G12) run directly against the LIVE deployed container at :3001 (not an isolated dev-server port) — 3/3 pass, stronger signal than an isolated-port run since it exercises the actual production artifact.

Cosmetic (non-blocking, carried to po): route header + review_note + ops's own note all say "457L"; actual `wc -l` = 476L — stale count predating the header comment itself (ops already flagged this too).

VERDICT: PASS. Board `task_board.review[]` row stays `status:"REVIEW"`, `next_agent` qa→po, `qa_verify_at`/`qa_verify_by`/`qa_note` added (dev-frontend's `review_note`/ops's `ops_note` untouched) via `jq`+`scripts/orch-apply.sh`; `.head` synced same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S33.
