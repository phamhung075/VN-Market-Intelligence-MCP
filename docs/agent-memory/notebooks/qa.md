# QA — Notebook


## cycle-588 · 2026-08-08 · FIX-CI-SIZELINT-PUSHBCTCREFINEDUNITTOOL-283L — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `61acf40aa` — corrected from stale/dangling `bb1ac4cd1`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Dispatcher pre-flagged the same dangling-commit pattern as cycle-582/584/586: row's own `commit_sha` (`bb1ac4cd1`) FAILED `git merge-base --is-ancestor`, `git fsck --unreachable` confirmed a genuine dangling object. Real landing commit `61acf40aa` passed ancestor check on both local main and origin/main, identical `git patch-id` to the dangling object (Merge Gate reapply artifact). Corrected `commit_sha` as part of the lane-move.

Diff at `61acf40aa` is comment-only: +9L size-justification header, zero deletions/logic touched (verified line-by-line). AC1: header `size-justification: 292L` present in first-10-line window, `wc -l`=292 exact match. AC2: `size-lint-baseline.json` still carries a stale 227L entry for this file, but the script's `is_justified_now()` short-circuits on the current header BEFORE consulting baseline — the stale entry is inert, no `--update` laundering occurred; scoped check (`SIZE_LINT_INCLUDE_OVERRIDE`) → PASS 0 offenders.

AC3/AC4: live CI ground truth (`gh run view 31267224323`, headSha=384ce899, confirmed ancestor of current HEAD) size-lint job log shows exactly 1 offender, `coordinationStore.ts` (separate READY row `FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L`) — this file absent from the failing-file list. Local full-tree scan surfaces a 2nd apparent offender, `transport.ts`, but that traces to a DIFFERENT in-flight row (`FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER`, QA lane) whose change landed on local HEAD 16 commits AFTER the CI run above completed — out of this row's scope, not a regression here.

Re-ran RAW, not the dev's self-report: `bun tsc --noEmit` 0 errors; `mock-guard.sh --files pushBctcRefinedUnitTool.ts` PASS; targeted suite (TRUST-RED-sanity-gate + AR-refined-units-idempotency + FIX-REFINE-LOCK-TTL-RECLAIM + FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE) 46/46 pass, 159 expect() — exact match to dev's claim. DDD/security greps clean (infra imports pre-existing, unchanged by this diff).

VERDICT: APPROVED, DONE_VERIFIED — file genuinely under threshold, live CI ground truth confirms this file is not the size-lint offender, diff is a pure header addition with zero functional regression. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 759→759, signal_total 245→245). Stamped `commit_sha:61acf40aa` onto the row (corrected from dangling). Verification text appended to row's own `status_note` field. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S7 (step-number collision with 2 concurrent peer entries this cycle — content intact, not lost). No pruning needed this write (2 retained sections + new = 3, at cap).

## cycle-589 · 2026-08-08 · FIX-DEVTEAM-QADRAIN-THROUGHPUT-CAP — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `ce053fdc8`, commit_sha backfilled from null) [re-appended — a peer's concurrent full-`Write` clobbered my first attempt at this same cycle slot minutes ago, same recurring race as cycle-584's own note this sprint]

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Row's own `review_note` named 2 commits explicitly (`ce053fdc8` scripts+harness, `191349afb` WORK.md) — both re-confirmed main ancestors myself; `git show --stat ce053fdc8` matches this row's own `files[]` exactly (`scripts/devteam-review-claim-qa-drain.jq`, `scripts/audits/devteam-dispatch-gate-satisfiability.sh`), zero `apps/` touched (bun test/tsc N/A claim genuinely true, not asserted on trust).

Read the full diff, not the message: candidate sort `sort_by([.rank,.age])` with `rank: priority_rank` — confirmed the PRE-EXISTING shared def at `scripts/lib/devteam-eligibility.jq:121-128`, reused not reinvented (that file genuinely untouched in the diff). `take_budget` read via `$ARGS.named.take_budget // 1`, never a bound `--argjson` — confirmed backward-safe by compiling/running the script live with the flag omitted against the current board. Batch-claim + shared `claimed_at`/`claimed_by` stamp + the self-reported review[]-removal bugfix (`.key as $k | $batch_idx | index($k)`) all present exactly as claimed.

Ran `devteam-dispatch-gate-satisfiability.sh` LIVE myself: all 7 new AC-QADRAIN-PRIORITY-ORDER/AC-QADRAIN-TAKE-BUDGET assertions PASS. Independently reproduced the claimed TDD RED→GREEN outside the harness too, with a fresh fixture: checked out `ce053fdc8^`'s (parent, pre-fix) script — picks the WRONG older P2 (RED); HEAD's script on the same fixture — picks the P0 (GREEN).

2 pre-existing unrelated FAILs remain in the full harness run (BOUNDED-1 "correctly no-ops when in_progress>=1" + SLS gate satisfiability) — the row's own `review_note`/WORK.md entry undercounts this as "one." Checked out `ce053fdc8^`'s unmodified versions of both changed files and re-ran: byte-identical FAIL set pre- and post-fix, confirming both are genuinely pre-existing/live-board-state-dependent and out of this row's file scope — cosmetic self-report gap, non-blocking.

VERDICT: APPROVED, DONE_VERIFIED — AC(a) take_budget-default-1-backward-safe, AC(b) sort_by-priority_rank-age, AC(c) satisfiability-fixture-priority-positive-control all independently re-verified against live code + live harness re-run, not the dev's self-report. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 759→759, signal_total 245→245, signal_row_identity=clean). Backfilled `commit_sha` (was null) → `ce053fdc8` per dispatcher's explicit request. Verification text appended to row's own `status_note` field (none pre-existing — no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S8. No pruning needed this write (587/588 already 2 retained + this new one = 3, at cap).

## cycle-590 · 2026-08-08 · FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER — code-correctness APPROVED, held REVIEW/next_agent=ops (Docker Close Gate — NOT flipped DONE_VERIFIED)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed), PRIORITY/CRITICAL — primary root cause of the A-30 mcp-server memory chain. RAW-read all 4 claimed commits' diffs (not the message): `b746c112b` transport.ts collapses 2 parallel Maps + 2 duplicated cleanup blocks into 1 `SessionRecord{transport,mcpServer,heartbeatInterval,createdAt,lastActivityAt}` map + 1 `evictSession()` — confirmed called from all 5 claimed triggers (`res.on("close")`→connection_closed, heartbeat-write-fail catch→heartbeat_write_failure, `reapStaleSessions()` idle branch→idle_timeout, its max-age branch→max_age, new `closeSession()` DELETE path→client_delete); server.ts gained the DELETE `/sse|/messages?sessionId=` route (400/200/404) + `sessions.stopReaper()` wired into `BunServerInstance.close()`; `docs/architecture/microservice/mcp-server/infrastructure.md` doc section matches the code. Re-ran tests myself: `1862c-transport-session-eviction.test.ts` 12/12 pass (36 expect — T9 is a genuine negative control, T10 proves max-age independent of a live idle clock, T12 proves idempotent double-eviction, none tautological); `081-bun-mcp-server.test.ts` 10/10 pass (26 expect, live `client_delete` log observed). `tsc --noEmit` 0 errors, `mock-guard.sh` PASS, DDD/security greps all pre-existing (confirmed via parent-commit diff, zero new violations).

ROUTING GAP CAUGHT AND CORRECTED: `apps/mcp-server/src/` is baked at Docker build time (`COPY apps/mcp-server/src/ ./src/`, Dockerfile:62 — not volume-mounted, confirmed via docker-compose.yml). `docs/protocols/docker-deployment-runbook.md` § Microservice Code-Change Close Gate applies — a code change isn't shipped until the container is rebuilt + SHA-verified. This row's own `claimed_by` routed it straight past that gate into `qa[]`, but the SAME board commit (`d24ddf6b6`) correctly applied it to sibling `FIX-SCHEDULER-DOUBLE-REGISTRATION` (held REVIEW/next_agent=ops/rebuild_required=true) in the identical push — an inconsistency, not two legitimate different rulings. Corrected: moved `task_board.qa[]`→`task_board.review[]`, `status: QA→REVIEW`, `next_agent→ops`, `rebuild_required:true` added, `.head` synced (was pointing at this `active_task_id`) — via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 759→759, signal_total 245→245). Did NOT flip to `done_verified` — code is correct but not yet deployed; PO's standing no-restart-while-in-flight ruling is now discharged, ops may proceed. `qa_review_note`/`qa_reviewed_at`/`qa_reviewed_by` added to the row (no `status_note` field existed). Task Report: `reports/TASK_REPORT_FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S7 (duplicate step-number collision with 2 concurrent peer writes to the same file — content intact, not lost). `task_release` NOT called (INV-GATEWAY-1, no gateway MCP binding in this sub-session) — dispatcher must release `task:FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` on my behalf. Pruned cycle-587 this write per AC-2 retention (3-section cap) — kept 588/589/590.
