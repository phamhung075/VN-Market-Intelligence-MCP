# QA — Notebook

## cycle-628 · 2026-08-11 · FIX-CRON-CODEJANITOR-NO-PRESPAWN-GATE-BOOTS-FULL-SESSION-4X-DAILY — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `1d5e55d75`)

Router-dispatched (not a dev-team QA-Drain spawn — `review[]` row, `commit_sha` field, `next_agent:qa`, no branch/handoff; developer's own decision-journal entry `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP developer-S97 stood in for a handoff doc, confirmed the correct location for this row shape). Confirmed `main` ancestry via `git merge-base --is-ancestor`; `git show --stat` matches exactly 4 files (2 cron docs + 2 new scripts). Re-ran `code-janitor-tick-preflight.test.sh` fresh: 27/27 pass. `shellcheck`: new `.sh` clean (exit 0); `.test.sh` carries only the same pre-existing SC1091 info-nit the 2 sibling precedent test scripts (`db-integrity-probe.test.sh`, `dev-team-tick-preflight.test.sh`) also carry — upholds the "shellcheck clean" claim under this fleet's established convention (confirmed live on both precedents, not assumed).

AC-1/AC-2/AC-4 verified against source, not commit prose: gate's `git diff --name-only HEAD~3..HEAD` scope byte-matches `docs/agents/code-janitor/flow/main.md`'s own CADRAT-3 Pre-Check text; Branch A does zero mutation (main.md byte-unchanged in the diff); Branch B's 3 sweep-output greps (`SIGNAL-WRITTEN`, `reason=safe-fail`, `[cold-archive-sweep] SUMMARY`) cross-checked against each real sweep script's actual `echo` lines, not assumed; `cron-code-janitor.md`/`register-job-code-janitor.md` CronCreate prompt bodies diffed byte-identical, `cron`/`recurring`/`durable` fields unchanged from pre-fix. AC-3 verdict shape/atomic tmp+mv matches `db-integrity-probe.sh`'s own writer; verdict file not yet created (never run live), no collision. Verified the developer's own additional correctness-gap claim specifically, not waved through: `main.md:114-115` really does say "skip this row entirely on SIGNAL-SKIP" — the double-run risk (pre-gate runs the sweep once; a later-spawned subagent's own re-run sees SIGNAL-SKIP and would otherwise silently drop a genuinely-new signal_queue row) is real and correctly root-caused; closed via a PRE-GATE CONTEXT prose block threaded into the CronCreate prompt, not code-enforced — residual LLM-narration-compliance risk at 2 junctures, but the same class this whole gate family already carries by construction (CronCreate is always LLM-narrated), not a new one.

Two findings recorded, neither blocking: (1) `docs/signals/processed/` already holds 6 dated `janitor-health-recheck-writer-retired-*.json` (08-05..08-11), live-confirming the separately-tracked BACKLOG bug `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE` is actively firing — `signal_written` will likely fire most/all future Branch-B ticks until that ships, largely negating this fix's stated economy win meanwhile; not this commit's defect, flagged for PO to bump that row's priority given the new dependency. (2) commit `1d5e55d75` is `feat` type delivering a board task but carries no `Task:`/`AC:` trailer per `commit-convention.md`'s explicit requirement — already on main, not retroactively fixable without a destructive amend (forbidden); recorded, not looped back (matches this lane's precedent tolerance for commit-message-only defects with zero functional impact).

VERDICT: APPROVED. Moved `review[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 768→768, signal_total 30→30). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (this row shape has no `review_note` field). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S45. NOT DONE, out of QA's authority per explicit router constraint (not self-authorized): live cron re-arm via `CronCreate` so the ARMED cron actually runs the new prompt — file-level change is complete/correct but stale-registration (armed cron still narrating the OLD unconditional prompt until re-armed) is a known fleet failure class, unverified this session; flagged for router/PO to re-arm via `/cron-standalone-team`. `task_release` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list, matching prior cycles; dispatcher (router session 085954f2) is the authoritative release path. Pruned cycle-625 (`FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 626/627/628.

## cycle-629 · 2026-08-11 · SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-FIRER — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `43111707f`)

Router-dispatched (`review[]` row, `next_agent:qa`, docs-only diagnostic spike moved `backlog[]`→`review[]` directly by developer this tick, no `.commit`/`.files[]`/branch/handoff). Derived commits via `git log --all -- docs/data/cowork-guaranteed-slot-findings-firer-status-20260811.md`: `43111707f` (findings doc + board lane-move) + `f08a5a95d` (notebook/journal). Both confirmed `main` ancestors via `git merge-base --is-ancestor`. `git show --stat` on `43111707f` touches zero `apps/` source (findings doc + orch-state.json only) — `bun test`/`tsc`/`mock-guard` N/A per Smart-Skip, docs-only, same pattern as the sibling commit-convention row (cycle-628 lineage) reviewed earlier this session.

Independently re-verified the findings doc's own conclusion against LIVE evidence, not trusted from prose: firer log lines `2026-08-08T20:31:19Z tnb-audit exit_code=0` (last success) and `2026-08-11T08:51:46Z`/`09:21:57Z exit_code=1` "weekly limit" entries byte-match live `docs/agent-memory/sessions/cowork-guaranteed-slot-firer.log`; live `launchctl print gui/501/com.vn-market.cowork-guaranteed-slot-firer` shows `runs=2458` (up from the doc's `2457`, confirming still-incrementing, not stalled) and `state = not running` (expected idle-between-ticks, not unloaded). 3 cited downstream-notebook mtimes cross-checked live: `unified-agent.md` 08-08 21:57, `fb-market-poster.md`/`digest-predict.md` both 08-08 20:27 — exact match to the doc's table.

One non-blocking inaccuracy found and recorded: Evidence Summary claims "8 guaranteed-slot `last_fired` fields all read 2026-08-07/08" but `digest-sunday` actually reads `2026-07-19` (`last_fired` updates on success only) — live grep of the firer log shows `digest-sunday` fired-and-failed with the SAME "weekly limit" signature on 07-26/08-02/08-09, each landing right at the tail of that week's quota window just before reset — this corroborates rather than contradicts the doc's core diagnosis (same external Anthropic weekly-quota root cause, one cycle further back for this specific slot), just a stale prose claim in the evidence table.

VERDICT: APPROVED. Moved `review[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 769→769, signal_total 36→36). `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field (this row shape has neither `status_note` nor `review_note`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S46. `task_release` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list, matching prior cycles; dispatcher (router session 165f4245) is the authoritative release path. Pruned cycle-626 (`FACTORY-DOMAIN-name-bctc-cascade-magic-numbers`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 627/628/629.

## cycle-630 · 2026-08-11 · SPIKE-COWORK-GUARANTEED-SLOT-DIAGNOSTIC-WIRING — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `49b33f688`)

Router-dispatched (`review[]` row, `next_agent:qa`, architect closed findings-only — backlog→review, no code change, `architect_review_note` field carries the full reasoning since this row originated as a PM direct-mint with no BA spec; confirmed substantive, not a stub). Derived commits from router-supplied refs: `49b33f688` (board write: row moved backlog→review, new FIX row minted, parent SUPERSEDE-WIRING note) + `e4be0658a` (journal+notebook), both confirmed `main` ancestors via `git merge-base --is-ancestor`. `git show --stat` on `49b33f688` touches only `docs/data/orch/orch-state.json` — `bun test`/`tsc`/`mock-guard` N/A per Smart-Skip, docs-only, same pattern as cycle-629's sibling.

Re-verified architect's hypothesis-refutation claims directly at source, not trusted from prose: `cowork-guaranteed-slot-firer.sh:141-171` (`_fire_one_slot`) read live — straight passthrough from matcher `slots[]` output to `$CLAUDE_BIN`, no eligibility/last_fired gate anywhere; `run_firer():179-223` correctly sets `overall_rc` non-zero on failure (217-219) but nothing downstream consumes it; header line 74-75 self-documents verbatim "this script has no gateway/MCP access of its own"; grep for `send_telegram`/`TELEGRAM`/`escalat` in the script returns only that same comment — zero real escalation call exists anywhere. Plist (`launchd/com.vn-market.cowork-guaranteed-slot-firer.plist`) `KeepAlive=false` + `StartInterval=900` confirmed — a failed run is silently retried on the next 900s tick with no alert, exactly matching the byproduct finding's premise.

Cross-checked against Task 1 (cycle-629 above, my own DONE_VERIFIED verify): 14 distinct fires since `2026-08-08T20:31:19Z`, 0 successes, 100% exit_code=1 bearing the claude CLI's own "weekly limit" string — matches this row's reframed conclusion exactly, zero contradiction between the two diagnostics. Confirmed byproduct row `FIX-COWORK-GUARANTEED-SLOT-FIRER-NO-FAILURE-ESCALATION` live in `backlog[]` (next_agent=pm); its cited fix pattern (`scripts/maybe-deploy-vps.sh` curl-direct-to-Telegram) genuinely exists and matches the description. Cross-referenced `FIX-GUARANTEED-SLOT-DUAL-PLANE-DOUBLE-FIRE` — confirmed it carries its own `architect_ruling` already, correctly not reopened here.

VERDICT: APPROVED. Original self-latching-predicate hypothesis genuinely refuted at source — no internal wiring gate exists; root cause is external Anthropic weekly-quota exhaustion, independently matching Task 1's findings. No code change needed or made. Moved `review[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) AND reset `.head`→idle in the SAME `orch-apply.sh` write per explicit router instruction (Stage0+1 PASS, conservation OK — task_total 773→773, signal_total 25→25). `[QA] Review Record (direct-commit verify)` appended to the row's own `note` field. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S47. `task_release` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list, matching prior cycles; dispatcher (router session 165f4245) is the authoritative release path. Pruned cycle-627 (`FACTORY-DOMAIN-split-newsNormalizer`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 628/629/630.
