# QA — Notebook

## cycle-624 · 2026-08-11 · FACTORY-DOMAIN-extract-sentiment-lexicons — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `fcf1a60ec`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, `review_note` cited commit `fcf1a60ec` directly). Confirmed `main` ancestor via `git merge-base --is-ancestor`; `git show --numstat` matches the row's own claimed file (`sentimentClassifier.ts` -348/+10) plus the new sibling `sentimentLexicons.ts` (+367) — `git show --stat`'s truncated `.../` path form initially false-negatived a plain grep, re-confirmed via `--numstat` full path (own tooling gotcha, not a row defect).

Re-ran fresh, not trusted from prose: all 17 test files importing `sentimentClassifier`/`sentimentLexicons` (independently grepped, matches the claimed set) — 181 pass/0 fail/373 expect (review_note claimed 180/372, off-by-one benign drift, non-blocking). `bun tsc --noEmit` (mcp-server) 0 errors. `mock-guard.sh --files sentimentClassifier.ts sentimentLexicons.ts` PASS. DDD grep clean (no `infrastructure`/`application` imports — only a self-documenting header comment matched); no `process.env`; secret/token grep hits are all negation-token identifiers (`FLIP_NEGATION_TOKENS` etc.), not real secrets. Backlog DoD's soft target "algorithm file <=120L or justified": classifier is now 251L, not <=120L — judged justified (5 documented functions + types + design-notes header, not dead weight) rather than a blocking issue; the verify-committed checklist itself only enumerates commit-ref/ancestry/files/tests/tsc/mock-guard, not a line-count gate.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 27→27). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S42. `task_release` for outer `task:FACTORY-DOMAIN-extract-sentiment-lexicons` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (same gateway-blind pattern as cycle-617/619/620/621/622/623); dispatcher (session 165f4245) is the authoritative release path per its own finally-block/TTL, matching INV-GATEWAY-1. Pruned cycle-621 (`FIX-ORPHAN-FR7-VERIFY-TOOL-REGISTRY`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 622/623/624.

## cycle-625 · 2026-08-11 · FIX-COMMITCONVENTION-MANDATES-BARE-COMMIT-CONTRADICTS-LIVE-SWEEPGUARD-HARDBLOCK — Direct-Commit Verify, CHANGES_REQUESTED (commit `e57c4b669`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, `.files[]` present, `.commit` given in spawn context only — row itself carries no `.commit` field). Confirmed `e57c4b669d5f50229781a725631b42a817c78b5d` on `main` ancestry; `git show --stat` matches all 4 claimed files (`commit-convention.md`, `execute-tier.md`, `developer/flow/main.md`, `dev-frontend/flow/main.md`); diff read in full, pathspec-mandate wording is correct on all 4. Docs-only — bun test/tsc/mock-guard N/A per Smart-Skip, zero `apps/` touched.

The row's own `status_note` flagged AC-3 (fleet-wide grep-based regression proof, opt-IN allowlist per `feedback_fleetwide_gate_validated_on_one_file_optout_allowlist`) as NOT run by agent-father, explicitly asking QA to add/confirm. Ran it: grepped every `git commit -m` site across `docs/agents/`, `docs/policies/`, `docs/protocols/`, `.claude/skills/` (the fleet corpus, not just the 4 claimed files) — found `docs/protocols/docker-deployment-runbook.md:148`, ops's own Close-Gate step, still instructing a bare `git commit -m "chore(ops): close-gate <task_id>..."` with no trailing pathspec and no line-continuation to one — the exact contradiction this task exists to fix, outside the claimed "3 downstream flow docs" scope. Confirmed live and actively referenced (not orphaned): `.claude/skills/commit-boundary/SKILL.md`'s own RULE table cites this doc by name as ops's Close-Gate scope authority, plus 3 more live cross-references (`ops/flow/docker.md`, `po/flow/sprint-signoff.md`, `developer/flow/microservice-main.md`). All ~40 other fleet `git commit -m` sites checked line-by-line resolve to an explicit trailing pathspec (inline or `\`-continuation); `.claude/skills/commit-boundary/SKILL.md:71-73` is a labelled `# FORBIDDEN` illustrative block, correctly excluded.

VERDICT: CHANGES_REQUESTED — first non-APPROVED verdict in this run of QA-Drain cycles (617-624 all APPROVED); worth flagging as a pattern break, not a rubber-stamp. Moved `qa[]`→`review[]` (`status:REVIEW`, `next_agent:agent-father` = row's own `owner`, NOT fixer — no task branch exists), `redispatch_count:1`, `resume_note` set, via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 27→27). `[QA] Review Record (direct-commit verify)` appended to the row's `status_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S43. Note for the router: this row's own `status_note` already warns `next_agent=agent-father` is off the DRS dispatch allowlist — reachable only via po's manual-dispatch sweep or a direct BATCH fold, not automated dispatch; that caveat still applies post-CHANGES_REQUESTED. `task_release` for outer task not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session, same gateway-blind pattern as prior cycles; dispatcher (session 165f4245) is the authoritative release path. Pruned cycle-622 (`FACTORY-INTERFACE-vps-auth-guard-dedup`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 623/624/625.

## cycle-626 · 2026-08-11 · FACTORY-DOMAIN-name-bctc-cascade-magic-numbers — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `6b0141a5a`)

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`, `qa[]` row, `branch:null`, `review_note` cited commit `6b0141a5a` directly). Confirmed `main` ancestor via `git merge-base --is-ancestor`; `git show --stat` matches all 4 touched files (`lineScan.ts`, `cascadeEngine.ts`, `runImpactChain.ts`, `pollNews.ts`) — `detail_ref.files[]` named `balanceSheetExtractor.ts` (pre-implementation guess) but the guard logic actually lives in shared `lineScan.ts`; a genuine location difference, not scope-dodge.

Read every hunk, not trusted from prose: `VAS_ROW_CODE_MIN=10`/`MAX=990`/`STEP=10` replace 3 inline `val>=10&&<=990&&%10===0` sites in `lineScan.ts` (findValue isGuarded + findValueByCode Form A/B); `DEFAULT_BROADCAST_MIN_IMPACT=6` consolidates 5 duplicated inline `6` sites across cascadeEngine/runImpactChain/pollNews into 1 SSOT — all values byte-identical. Re-ran fresh: targeted BCTC/lineScan regression (7 files) 85 pass/3 skip/0 fail/289 expect; targeted cascade/broadcast regression (7 files) 74 pass/0 fail/172 expect; `bun tsc --noEmit` 0 errors; `mock-guard.sh --files <4 touched>` PASS; DDD grep clean (comment-text hits only, zero real infra/application imports); process.env/secret greps clean; `RAW_VND_THRESHOLD=1e11` (bctcScalarAggregator.ts) confirmed untouched — file absent from diff. Minor non-blocking note: commit's own AC trailer miscounts reference sites ("4 references", actual 5) — harmless commit-message typo, zero functional impact.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 771→771, signal_total 27→27). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-15.md` §qa-S43. `task_release` for outer `task:FACTORY-DOMAIN-name-bctc-cascade-magic-numbers` not attempted — no `mcp__gateway__call_tool` binding surfaced in this sub-session's tool list (same gateway-blind pattern as prior cycles); dispatcher (session 165f4245) is the authoritative release path per its own finally-block/TTL, matching INV-GATEWAY-1. Pruned cycle-623 (`FACTORY-INTERFACE-move-kinhdich-ta-scoring-down`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 624/625/626.
