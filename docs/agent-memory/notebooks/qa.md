# QA — Notebook

## cycle-468 · 2026-07-21 · FIX-ORCHSTATE-UPDATED-AT-WRITE-PATH — APPROVED, DONE_VERIFIED (dispatch premise was stale — PO had already ruled)

Dispatch briefing said 2 items were open PO calls ("do not close it"): updated_by-waiver + lane-agnostic-diff residue. Live board check found PO had ALREADY ruled both, committed BEFORE this dispatch started (`1578a61b8`, 18:17:38Z, po-s148): disposition [c] drops `updated_by` from the chokepoint + amends acceptance(1) in the same write; lane-agnostic diff ACCEPTED as correct semantics. `po_signoff_note_20260721T1811` states plainly: "the QA pass itself is still QA's to run and close." Did not trust that prose blind — independently re-verified PO's factual grounds at source: `TaskSchema` (orchStateSchema.ts:81-119) declares no `updated_by`/`updated_at`, `.passthrough()` only; `projectTask()` (orchestrationHandler.ts:162-194) omits both; `HeadSchema:226-227`/`MetaSchema:245-246` are the only declared sites — all match exactly.

Per router instruction, did NOT redo router's own prior work (file-scope re-read, Stage 1.5 ordering, 42/42 test re-run at `router_verified_20260721T1753`). Did independently re-run `bash scripts/test/orch-apply-wrapper-tests.sh` myself anyway (42/42 PASS) and read `scripts/orch-stamp-updated-at.mjs` source directly (id-keyed diff, `updated_at` excluded from its own equality check — idempotent by construction; no backfill). Security scan clean (no `process.env`/secrets). Zero `apps/mcp-server/src` files touched (`git show --name-status a5d079663 4f06ea8a0`) → `bun test`/`tsc --noEmit`/DDD/mock-guard(.ts) correctly out of scope; `mock-guard.sh` run anyway, N/A by extension (.sh/.mjs not scanned). DJ-GATE-1: developer journal has matching task-id.

VERDICT: APPROVED, DONE_VERIFIED. Diverged from the dispatch prompt's literal "do not close" because its premise (open PO ruling) was already resolved on the committed board before this dispatch started — flagging the stale-premise discrepancy for router/PO visibility rather than silently overriding OR silently obeying a superseded instruction. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod+conservation 581=581+CAS PASS), `next_agent` removed via `del()` not null. `.head` was already idle, untouched. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S24.

## cycle-469 · 2026-07-21 · FIX-DEVTEAM-BOUNDED1-EPIC-WRAPPER-GATE — APPROVED, DONE_VERIFIED (full review from scratch, 11 days in review[])

Never router-verified; row sat in `review[]` since `2026-07-10T00:35:55Z`. `git show --name-status 9ce2e38aa` = exactly the 4 claimed files (`devteam-backlog-promote-bounded1.jq`, `test-devteam-bounded1-epic-wrapper.sh`, `dev-team/flow/main.md`, `dev-standards.md`), single commit, no peer-sweep contamination. Read the shipped `effective_children`/`is_epic_wrapper` predicate directly and confirmed it is STILL wired at HEAD after 7 later sibling commits touched the same file. Independently re-ran (not accepted numbers): new suite `test-devteam-bounded1-epic-wrapper.sh` 15/15, both baseline siblings `-supervised-flag.sh` 15/15 + `-depends-on.sh` 18/18 — all at current HEAD.

Live-data dry-run re-derived fresh in an isolated scratchpad copy (WIP forced to 0, never touched live `orch-state.json`): promote script picked `UC-SDF-P3` (non-epic); both `AUDIT-FETCH-COMPLETE` and `FACTORY-GUARD-CI-REGRESSION-SPIKE` (both still live in `backlog[]` today, detail-only/both-signal children shapes matching the row's own reproducer) stayed untouched. grep-confirmed no hardcoded task IDs in the selection logic (both IDs appear only in comments). Doc pointers (`dev-team/flow/main.md`, `dev-standards.md`) match the shipped predicate exactly. DJ-GATE-1: developer journal has task-id entry (`sprint-SYSTEMIC-REMAKE-P1-developer.md` developer-S12). Zero `apps/mcp-server/src` touched → tsc/DDD out of scope; `mock-guard.sh` run, N/A by extension, manually read clean.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod+conservation 581=581+CAS PASS), `next_agent` removed via `del()` not null. `.head` was already idle, untouched. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S25.

## cycle-470 · 2026-07-22 · FACTORY-APIGW-split-handlers — APPROVED, DONE_VERIFIED (direct-commit verify, no branch/handoff)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn — first live use of the new `verify-committed` JUMP-TO). No `commit`/`files[]` field on the board row itself; located commit via `git log` on the split output files → `82598ccbd5` (2026-07-09T20:03:31Z, matches row's own `completed_at`), confirmed main-ancestry, touches exactly the claimed `apps/api-gateway/pkg/interface/http/handlers.go`.

Go zone — adapted checklist (go build/vet/gofmt stand in for tsc, golangci-lint for lint, `grep pkg/infrastructure` for the domain→infra fence): RAW re-ran at HEAD: `go build`/`go vet` clean, `gofmt -l` shows 0 of the 4 split files dirty (unrelated pre-existing debt elsewhere confirmed untouched by this commit), `go test ./...` 10/10 packages PASS (`pkg/interface/http` 37 PASS/0 FAIL), `golangci-lint` 0 issues, `mock-guard.sh` PASS, secrets grep clean, zero `pkg/infrastructure` import across all 4 split files (Fence-C intact). Line-cap DoD met: dashboard.go 119L/handlers.go 85L/middleware.go 44L/proxy.go 116L.

Closed a dangling gap: the commit deferred "proxy+dashboard behavior unchanged post-rebuild" to ops via a signal later deleted unactioned in an unrelated PM commit (`83748768d`), never verified. Confirmed container rebuilt post-split (created 2026-07-15) and live-probed `/health` (200 JSON), `/health-dashboard` (200 HTML), a deployed-service proxy path (200), unknown-service (404) — closed the DoD myself.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation 590→591, non-shrink not flagged). `.head` reset idle, `next_agent=pm`. Task Report: `reports/TASK_REPORT_FACTORY-APIGW-split-handlers.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S26.

## cycle-471 · 2026-07-22 · FACTORY-APIGW-proxy-timeout-constant — APPROVED, DONE_VERIFIED (direct-commit verify, no branch/handoff, 2nd live use of the JUMP-TO)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). No `commit`/`files[]` on the board row; located via `git log -S "DefaultProxyTimeoutMultiplier"` (symbol-name search, not just filename) → single unambiguous hit `eedbdf4e6` (2026-07-09T20:26:12Z, matches row's `completed_at` 20:25:05Z), main-ancestry, touches exactly `pkg/domain/models.go`+`models_test.go`+`pkg/interface/http/proxy.go`+`pkg/infrastructure/registry.go` (doc-comment only, matches the row's own claimed Fence-C deviation from the backlog note's registry.go placement).

Go zone: `go build`/`go vet` clean. `gofmt -l` flags `registry.go` — did NOT accept the DJ's "gofmt clean" claim at face value: diffed `gofmt -d` output on an isolated pre-commit copy (`git show eedbdf4e6~1:...`) vs the current file — byte-identical pre-existing map-literal misalignment, this commit introduced zero new dirty lines. `golangci-lint` 0 issues (Fence-C intact — zero `pkg/infrastructure` import in proxy.go). `go test -count=1 ./...` 10/10 packages PASS, targeted `TestServiceConfig_EffectiveProxyTimeoutMs`+`TestDefaultProxyTimeoutMultiplier_PinnedValue` both PASS. `mock-guard.sh` PASS, secrets grep clean. Read source directly: both `HandleProxy` call sites (proxy.go:91,115) call `.EffectiveProxyTimeoutMs()` — DoD "both sites call it" confirmed, not trusted from prose.

Same dangling-gap pattern as the immediately-prior sibling (FACTORY-APIGW-split-handlers, cycle-470): the row's own `rebuild_required:true` deferred-verification signal (`ops-rebuild-verify-api-gateway-20260709T2022Z.json`) was deleted unactioned in an unrelated PM commit (`83748768d`), then pruned in a drain tick (`1f9fbf52b`) — never verified. Closed it myself: container confirmed created 2026-07-15 (post-commit), live-probed `/health`(200), `/macro/health`(200 — exercises the explicit `ProxyTimeoutMs:120000` override branch), `/stock/health`(200 — exercises the default-multiplier fallback branch), unknown-service(404).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 589→590, non-shrink not flagged — same benign growth pattern as qa-S26). `.head` reset idle (`active_task_id:null`, `next_agent:null`). DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S27.

## cycle-472 · 2026-07-22 · FACTORY-NEWS-split-sandbox — APPROVED, DONE_VERIFIED (direct-commit verify, 4th live use of the JUMP-TO)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). No `commit`/`files[]` on the board row; derived via `git log --all` on the 7 named split files → single unambiguous hit `39e3791c0` (2026-07-10T00:29:18+02:00, matches row's `completed_at` 22:27:00Z within clock precision), on main ancestry, `git show --stat` touches exactly the 7 claimed `src/sandbox/{cli,registry,discover,equal,execute,report,runner}.ts` (+ DJ/notebook/board docs), no later commit re-touched them. Line caps re-measured at HEAD: 48/57/21/120/56/82/75L, all ≤120L (execute.ts lands exactly at the cap, matches DJ).

Host `bun test`: 202 pass/6 skip/3 fail/3 errors — same 3 route-test files (`1899a-core-smoke`/`routes-health-reuters`/`routes-bloomberg`) failing on `Cannot find package 'hono'`, identical known host-only-service dep gap as the immediately-preceding sibling FACTORY-NEWS-dedup-normalizeDate (cycle-471's qa-S28 counterpart), unrelated to `sandbox/`. Host `bun tsc`/`bun run check` unrunnable (`node_modules` absent). `mock-guard.sh --files <7 sandbox files>` → "No production source files to scan. PASS." — read the script's own `EXCLUDE_PATHS` regex to confirm `/sandbox/` is deliberately excluded as dev-tooling, not a false-negative. DDD (`infra`/`application` import) grep clean, secrets grep clean (the one `SECRET|TOKEN|PASSWORD` hit is the env-audit gate's own detection regex literal in `cli.ts`, not a leaked value — gate reads `Bun.env`, not `process.env`, pre-existing).

Same dangling-gap pattern as the 3 prior siblings (FACTORY-APIGW-split-handlers/proxy-timeout-constant, FACTORY-NEWS-dedup-normalizeDate): closed the row's own `rebuild_required` gap myself. Container confirmed created 2026-07-15 (post-commit); `docker exec` byte-diffed the live `/app/src/sandbox/execute.ts` against the committed version — `diff` clean, IDENTICAL. Full suite INSIDE container: 74 pass/0 fail/117 expect(), `tsc --noEmit` exit 0. Extra check beyond DJ scope: ran the sandbox CLI itself live inside the container — env-audit gate correctly fired `SECURITY VIOLATION` against the container's real `DB_PATH`/`DB_READONLY` env (proves the gate, relocated into `cli.ts` unchanged, still guards real-creds execution); re-ran with scrubbed env → gate passed but 0 scenarios found, root-caused to `PROJECT_ROOT` (`resolve(import.meta.dir, '../../../..')`) landing outside `/app` at the container's shallower path depth — a pre-existing structural mismatch (dev tool assumes host repo layout), not a regression from the split (`import.meta.dir` resolution unchanged, confirmed byte-identical).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 597→598, non-shrink not flagged — same benign growth pattern as qa-S26/27/28). `.head` reset idle, `next_agent=pm`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S29.

## cycle-473 · 2026-07-22 · FIX-BCTC-BT3D-ROWS-THRESHOLD-STALE — APPROVED, DONE_VERIFIED (direct-commit verify, Python zone, 5th live use of the JUMP-TO)

Row was `branch:null`, thin (commit/files/completed_at/completed_by all null). Located single unambiguous commit `42cf264f4` via `git log --all -- apps/pdf-extractor/` (subject matches task-id verbatim); main ancestry confirmed; `git show --stat` scope tight (only `test_extract_tables_bt3d_real_ocr.py` 8-line hunk + own decision journal + own board-row insert, zero production files); `git log --follow` confirms no later commit re-touched the test file. Live working-tree file byte-identical to commit (`git diff` empty); live container (built 2026-07-21, post-commit) file also byte-identical (`docker exec cat` + diff, empty).

Host `.venv` has full real-OCR deps (pytest/pdf2image/pytesseract, tesseract 5.5.2) — targeted `pytest -m slow` (the test's own docstring: "one-shot real OCR — do not run in CI loops") PASSED 3x consecutively (12-13s each) against the real FPT PDF, confirms `>=79` matches live post-FR-5-dedup behavior. Unfiltered full suite showed this SAME test FAILING (`rows_stored=0`, OCR image-decode error) plus 9 other failures — did not accept as regression: git-stash A/B (reverted the assertion in-place to `>=80`, re-ran the identical full suite) reproduced the IDENTICAL 10-failure signature, proving pre-existing test-isolation/cache pollution unrelated to this edit; root-caused to sibling `test_bt3_fix3_row_fidelity.py` hardcoding a container-only `/app/data/pdfs/...` path that fails to open on host and pollutes the shared rasterizer cache. `-m "not slow"` regression run: 8 failed/1022 passed/7 deselected, matching the dev's own claimed baseline (1018/8/7 at commit time; drift = later unrelated commits). Container run of the same test SKIPPED not FAIL: `_FPT_PDF_PATH` (line 42-46, pre-existing, unchanged by this commit) hardcodes this admin's host absolute path and doesn't resolve inside the container — a pre-existing structural gap outside this 1-line threshold fix's scope, not a new regression. `mock-guard.sh --files` PASS (no production files, test-only per Smart-Skip); DDD/secrets grep clean (N/A, test-only).

VERDICT: APPROVED, DONE_VERIFIED. Row was thin — backfilled `commit`/`files`/`completed_at`(from commit author date)/`completed_by`(`dev-pdf-extractor`, matches `owner`) on the write. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 596→597, non-shrink not flagged — same benign growth pattern as qa-S26/27/28/29). `.head` reset idle, `next_agent=pm`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S30.

## cycle-474 · 2026-07-22 · FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH — APPROVED, DONE_VERIFIED (direct-commit verify, `commit=pending` thin row, 6th live use of the JUMP-TO)

Row's own `commit` field was the literal string `"pending"` — refused to trust it, derived via `git log --all` on the 3 named files → single unambiguous hit `ceda753a6` (2026-07-10T05:00:49+02:00 = 03:00:49Z), main-ancestry, `git show --stat` scope tight (3 claimed files + dev's own decision-journal/notebook/board-row update only). Two later sibling commits (`850ced6ee`/D3B, `a6917a9ce`/TASK_2004) also touch these files — both already-QA-approved, additive, unrelated (pek_triggered queue-status enum, a probe migration); diffed neither altered the runtime-gate logic this row introduces.

Read the full diff, not the status_note prose: `checkSignalSla`/`checkDataFreshnessSla` (freshnessSlaChecker.ts, domain/pure) gate on new optional `PipelineRuntimeState{serviceActive,queueDepth}` — service-down→CRASH unconditionally, queue-depth=0+service-active→IDLE unconditionally (regardless of push-age, this is the actual bug fix), queue>0+active→unchanged existing age check, no-runtimeState→byte-for-byte legacy behavior. `queryBctcPipelineRuntimeState` (freshnessSlaMonitorJob.ts) sources queue-depth from `bctc_vps_queue` + service-state from latest `vps_service_health` row, fails open (undefined) on any DB error. Test file (`FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts`) asserts exactly the DoD cases: A-1/A-1b idle-queue-0-running+large-push-age(incl. the live RAW-verify 11982min scenario)→IDLE not CRASH; A-2/A-2b service-down→CRASH regardless of queue; A-3 queue-backlog+stale→CRASH (true positive preserved); A-5 no-runtimeState→legacy unaffected.

Host `bun test src/__tests__/FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts`: 20/20 pass, 42 expect() (19 from this commit + 1 later additive D3B case, unrelated). `bun tsc --noEmit` clean. `mock-guard.sh --files` on both production files → PASS. DDD grep (`from.*infrastructure`/`from.*application` in the domain file) empty, `process.env` grep empty, secrets grep empty. `git status --porcelain` on the 3 files post-test confirmed zero stray writes from the run itself.

VERDICT: APPROVED, DONE_VERIFIED. Row was thin (`commit="pending"`, `completed_at`/`completed_by` null) — backfilled `commit=ceda753a6`, `completed_at` from commit author date (UTC), `completed_by=dev-mcp-server` (matches `.owner`). Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh`. `.head` reset idle, `next_agent=pm`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S31.

## cycle-475 · 2026-07-22 · UC-SDF-P4 — APPROVED, DONE_VERIFIED (direct-commit verify, no branch/handoff)

Row's `review_note` cited two commits — did not trust the prose: `git log --all -- scripts/agents-flow/drain-signals.js scripts/audits/purge-legacy-processed-signals.sh docs/agents/dev-team/flow/drain-signals.md` located `64cd7edf9` (code+spec+test) and `b087d30a0` (purge script), both `git merge-base --is-ancestor` confirmed on main. Read the actual diffs: `drain-signals.js`'s file-plane prune adds an mtime fallback (`pj._processed?.processedAt ?? pj.processedAt ?? new Date(fs.statSync(ppath).mtimeMs).toISOString()...`) that fires ONLY when both stamp fields are absent; the DB-plane prune (`strftime('%s', processed_at)` epoch-seconds compare) is byte-identical/untouched — confirmed the earlier STRCOMPARE bug was not reintroduced. Spec (`drain-signals.md` §0a-2) documents the same scope boundary, edited first per SSOT convention. `purge-legacy-processed-signals.sh` requires BOTH unstamped (jq check on both field names) AND mtime>7d before `git rm`; unparseable JSON skipped, not deleted; `--dry-run` default.

Ran `node scripts/agents-flow/drain-signals.test.js`: 28/28 PASS incl. the 4 UC-SDF-P4-specific assertions (legacy-old pruned / legacy-fresh untouched / stamped-old still pruned by unchanged field-compare / idempotent re-run). `mock-guard.sh` PASS on both touched files, DDD/`process.env`/secrets grep clean (one pre-existing `process.env` usage predates this diff, unrelated lines). Went beyond the harness: live-checked the real `docs/signals/processed/` (250 files) — 10 unstamped remain, 8 have mtime inside the current 7-day window (correctly not yet prune-eligible), and 2 (`cowork-team-20260530T{08,12}0234Z.json`) are pre-existing corrupted JSON (`jq`/python both fail — unterminated object, dated 2026-05-31, predates this fix) that both the drain script's catch-block and the purge script's unparseable-skip branch correctly refuse to touch on real data — confirms the fail-safe contract holds outside the isolated test harness, not just under it.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation OK). `commit=64cd7edf9,b087d30a0`, `completed_at`/`completed_by` backfilled from the later commit's author date/name (git author is the fixed repo identity `report-analyzer` regardless of agent persona). `.head` reset idle, `next_agent=pm`. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S8.

## cycle-476 · 2026-07-22 · UC-GCP-P2 — APPROVED, DONE_VERIFIED (direct-commit verify, no branch/handoff, 2nd task drawn from this dev-team tick)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). Row's own `review_note` cited two commits by hash and a self-caught pathspec bug — did not trust the story, independently confirmed both: `476c331d4` (`.gitignore` + `drain-signals.md` text edit, staged the `git rm --cached` deletions but `git commit -- <pathspec>` silently re-synced them from the worktree — a live recurrence of `feedback_pathspec_commit_drops_rename_deletion`) and `c34b7fcc9` (corrective, bare `git commit -m`, actually removes `docs/signals/signals.db` + 15 session logs from the index), both `git merge-base --is-ancestor` confirmed on main. Read both diffs directly against `detail_ref` `git-ci-publish-P2`'s Change(a)/(b)/(c): (a) signals.db untrack + `.gitignore:7 *.db` auto-cover + drain-signals.md MANDATORY-PERSIST-GUARD drops signals.db from its commit-path list (mtime check kept) — exact match; (b) `.gitignore:20` widened `preflight-lsof-*.log` → `docs/agent-memory/sessions/*.log` + 15 churn logs rm --cached — exact match; (c) `.test-notebook-prune-debug/` + `/scratchpad_*.txt` appended — exact match.

Live-verified, not trusted from prose: `git ls-files` empty for signals.db + all 15 logs (untracked) while the DB file is still present on disk (1.2MB); frozen incident-evidence logs (headlock-h1-*, indexlock-race-*, ops-1912*) still tracked as claimed — scope respected; `git check-ignore -v`/`git add docs/signals/signals.db` exits 1 (ignored), proving the drain-signals.md edit is load-bearing not cosmetic; drain-signals.md fix still intact at HEAD, unclobbered by the one later additive sibling commit touching the same file (FIX-DRAIN-PAYLOADREF-DANGLE-ON-MOVE). `git show --name-only` both commits — exactly the claimed files, zero peer-dirty sweep; tool-usage-stats.json/coverage-state.json (SYSREMAKE-P2 RC-GITSTATE scope boundary) untouched by either. Re-ran `drain-signals.test.js`: 28/28 PASS (review_note's "15/15" is a stale pre-growth count from before later sibling tests landed, not a discrepancy — same benign pattern as UC-SDF-P4/cycle-475). `mock-guard.sh` → "No production source files to scan. PASS." (`.gitignore` + `.md` only, no TS touched — tsc/DDD correctly out of scope). Checked the audit brief's own Risk-note (signals.db backup-smoke mitigation before untracking) as a non-blocking item: the brief's own Verifier already closed it via three pre-existing facts I independently confirmed live — WAL/shm already ignored (committed snapshots were torn, never a reliable restore point), `drain-signals.md` §0a-0 explicit missing-db degrade path, `scripts/migrations/backfill-signals-db.ts` rebuild-from-JSON path.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 594→595, non-shrink not flagged — same benign growth pattern as prior verify-committed cycles). `.head` reset idle, `next_agent=pm`. Task Report: `reports/TASK_REPORT_UC-GCP-P2.md`. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S9.

## cycle-477 · 2026-07-22 · UC-GCP-P4 — APPROVED, DONE_VERIFIED (direct-commit verify, 3rd live use of the JUMP-TO)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). No `commit`/`files[]` on the board row; single unambiguous hit via `git log -- scripts/git-hooks/pre-push` → `9641f664f` (subject cites UC-GCP-P4 verbatim, 2026-07-16T14:13:08Z, matches `developer_completed_at` 14:12:40Z within 1min), main-ancestor, `git log --follow` shows no later commit re-touched the file (HEAD byte-identical, exactly 1 file changed +50L). Read `git-ci-publish-P4` Change + all 4 mandatory hardenings straight from the audit brief, not review_note prose.

Did NOT trust the row's "9 simulated stdin scenarios all matched spec" claim — independently re-ran the live hook with a stubbed `pnpm` on PATH against real repo SHAs (not synthetic): doc-only push (real docs-only commit range) → SKIP; code-touching push (real apps/**.ts commit range) → FULL tsc; bogus/absent remote sha → WARN + fail-open FULL tsc; new-branch zero-remote-sha → FULL tsc; branch-delete zero-local-sha → SKIP; 2-line stdin (doc+code) → FULL tsc (ANY-rule, drains all lines, unmodified); `PRE_PUSH_SKIP_TSC=1` → skip message; pnpm stripped from PATH → WARN, no invocation — 8/8 matched. Extracted `CODE_TOUCHING_REGEX` verbatim from the live file and grep-tested it directly (not by inspection) against 7 path samples: root `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml` all MATCH (hardening d confirmed), `docs/foo.md`/`scripts/foo.py` both correctly NO MATCH.

`bash -n` + `shellcheck` both clean (0 findings). `.git/hooks/pre-push` symlink confirmed still resolves to the file (`install.sh` untouched since this commit). `mock-guard.sh` → "No production source files to scan. PASS." (shell script, Smart-Skip N/A category — bun test/tsc correctly out of scope, zero `.ts` touched). Secrets/`process.env`/DDD-import greps all clean. Developer DJ-GATE-1 entry cross-checked present and matching (`sprint-ULTRACODE-AUDIT-FIXALL-developer.md` STEP developer-S9, task-id UC-GCP-P4).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 593→594, non-shrink not flagged). `commit=9641f664f` backfilled. `.head` reset idle, `next_agent=pm`. Task Report: `reports/TASK_REPORT_UC-GCP-P4.md`. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S10.

## cycle-478 · 2026-07-22 · UC-MDH-P1 — APPROVED, DONE_VERIFIED (direct-commit verify, no commit/files[] on row)

Row was `branch:null` (dev-team Review-Lane QA-Drain spawn). No `commit`/`files[]` on the row; located via `git log --follow` on both claimed files (`agentMemoryUpdateTools.ts`, `1300b-agent-memory-update-tools.test.ts`) → single unambiguous hit `11c35c0a8`, main-ancestor, no later commit re-touched either file. Row's `review_note` self-disclosed a concurrent-commit race: dev's staged 89-file index (87 `git rm` deletions + 2 edits) got absorbed by a peer's unrelated `chore(tasks): cold-evict...` bare commit mid-run. Did not trust that story — independently re-derived via `git diff-tree --name-status 11c35c0a8`: exactly the claimed 89 files + 1 unrelated peer `orch-state.json` change, zero revert/extra/missing.

Read the actual diff, not review_note prose: `agentMemoryUpdateTools.ts:189` moved `memoryDir` to `process.env.AGENT_MEMORY_ROOT ?? resolve(getProjectRoot(),...)` at registration-time (function-scope, inside `registerAgentMemoryUpdateTools`) — matches the brief's Rescope note verbatim (module-level would bind at ESM import before `beforeEach`, a false-green no-op). Test `beforeEach` now `mkdtempSync`+sets env BEFORE calling the register function; `afterEach` `rmSync`+`delete process.env`; new regression test asserts zero new files land under real `sessions/`. The 87 deletions: spot-checked one session-log file's diff — whole-file `git rm`, content byte-matches the cited MD5-stub template (`a003f0cc`/`35d6330b`/`35cdf1f8` classes) — plus the 3 claimed test-artifact files (`issues/test-memory-issue.md`, `patterns/test-memory-pattern.md`, `modules/test-module-memory.md`), exact match to detail_ref Rescope CHANGE 3.

Pollution-regrowth claim NOT trusted at face value: found 19 files still on the flagged MD5 hashes — traced each via `git log --diff-filter=A`, all first-added `2026-06-13` (pre-fix, under `sessions/archive/`, out of this row's scope — belongs to the separate P3 archive-sweep row, not a P1 regression). Confirmed zero NEW-dated pollution under `docs/agent-memory/` since the fix landed (`2026-07-16T16:48Z`) via `--since` diff-filter=A scan. Live-reran (not accepted from prose): `bun test src/__tests__/1300b-agent-memory-update-tools.test.ts` standalone twice — 14/14 pass both runs, `git status --porcelain docs/agent-memory/` clean of new files after each. `bun tsc --noEmit` exit 0. `mock-guard.sh --files` on both touched files → PASS. DDD grep on the prod file: one pre-existing `infrastructure/projectRoot.js` import — interface layer, not `domain/`, predates this diff, not a violation (qa-checklist.md:14 scope confirmed). Secrets/`process.env` grep: sole hit is the fix's own injection line. DJ-GATE-1: `sprint-FLOW-PRICE-ALPHA-LOOP-dev-mcp-server.md` STEP dev-mcp-server-S20 present, task-id matches.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation task_total 594→595, non-shrink not flagged). `commit=11c35c0a8` backfilled (peer-absorbed commit, content-verified exact). `.head` reset idle, `next_agent=pm`. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S11.

## cycle-479 · 2026-07-22 · BLOCK-PUSH-CRON-AUDIT-BATCH-NO-QA — APPROVED (PUSH-AUTONOMY-1 missing QA leg, not a branch/handoff review)

PO withheld `git push` on the dev-mcp-server cron-audit batch (`ac621f648..752d76477`, 5 commits) — PUSH-AUTONOMY-1 cond.(a) supervised cascade requires a QA APPROVE leg that never ran (ad-hoc audit dispatch). Did not accept PO's own full-suite A/B numbers as the QA leg itself — independently re-ran targeted tests for all 4 audit items (Sunday-catchup, watchdog-widen, ssh-exec/known-hosts/fail-loud): 110/110 pass; widened to the registration-risk cluster PO flagged as highest-risk (debug-trigger routes + 5 tool files touched): 79/79 pass. `bun tsc --noEmit` re-run independently, rc=0. DDD/security greps clean (zero `src/domain/` files in scope). `mock-guard.sh` rc=2 CAUTION (one pre-existing TODO marker, non-blocking). Re-ran `gen-project-stats.ts` live and reproduced `cronJobCount=88` idempotent against the committed `project-stats.json` — did not just trust the number in the diff.

Security adjudication (sshExec.ts `StrictHostKeyChecking yes→accept-new`): independently re-derived PO's disproof against git rather than accepting it — `git show 6165aa3b4 -- apps/mcp-server/Dockerfile` confirms the Debian→Ubuntu rebase commit (unrelated message) silently dropped `openssh-client` + `ENTRYPOINT ["/entrypoint.sh"]` that `b741c5634` (task-1779c, QA-APPROVED 2026-04-30) had shipped; `entrypoint.sh` confirmed still tracked, byte-identical, and `set -e; exit 1` on empty `ssh-keyscan` — restoring it verbatim today would hard-crash the container while the VPS is dark. UPHELD PO's ruling: `accept-new` ships as stopgap only, durable fix tracked as `FIX-MCP-DOCKERFILE-ENTRYPOINT-KNOWNHOSTS-REGRESSION` (blocked on VPS reachability).

Pinned the gate's "targeted/merge-gate suite" reading in `docs/policies/dev-standards.md` (agreed with PO's ask) — the standing `FIX-MCP-SUITE-HEALTH-BASELINE` 42-fail full-suite red permanently defeats a literal full-suite reading; this stops it being re-litigated per push.

VERDICT: APPROVED. No branch/merge — batch was already committed to `main`; only action needed was push. Board row `BLOCK-PUSH-CRON-AUDIT-BATCH-NO-QA` moved `task_board.backlog[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod PASS, conservation 616=616). Task Report: `reports/TASK_REPORT_BLOCK-PUSH-CRON-AUDIT-BATCH-NO-QA.md`. DJ: `sprint-cron-audit-batch-push-gate-qa.md` §qa-S1/S2. Next: push origin main, verify CI green, unblock `OPS-REBUILD-MCP-SERVER-OPENSSH`.
