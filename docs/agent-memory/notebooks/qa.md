## cycle-843 · 2026-08-25T20:30Z · FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS, direct-commit verify — HOLD, no lane-move (qa_not_before not elapsed)

**A row can name its own re-dispatch gate, and that gate outranks "re-verify now."** The row carries `qa_not_before: 2026-08-26T14:59:30Z` and an explicit PO-authored routing note: any Review-Lane QA-Drain pick before that timestamp "MUST read qa_not_before first and return HOLD without consuming a redispatch." This drain claimed the row at `2026-08-25T20:21:26Z` — only 5.4h/24h (22.6%) into AC-3's window, 18.6h early. Re-ran what's checkable anyway rather than stopping cold: AC-1/AC-2 confirmed present at source (`docs/agents/cowork-team/flow/telemetry.md` Step 6.2, `main.md` dispatch table, both in commit `e6adc6dfe`, main-ancestor confirmed). AC-4 independently re-executed live (not trusted from prose) in a fresh sandbox repo with the real hook symlinked in: `caller_chain` appeared on all 3 claimed surfaces (stderr banner, `.git/sweep-guard.log`, `docs/signals/` bug-escalation payload), naming both the invoking command line and the top-level `claude -n <family>` identity; re-ran `scripts/git-hooks/pre-commit.test.sh` myself, 15/15 PASS, matching the commit's claim exactly.

**Did not certify AC-3 on favorable-looking early data — that is the exact anti-pattern this row's own PO ruling was written to prevent.** Checked anyway, for completeness, not for a verdict: since the anchor only 1 BARE fire logged (system-auditor class, not cowork-tick), and >=3 tick commits (`4def20db9`, `74c01149f`, `8fda9e649`) each touch ONLY `docs/data/cowork-schedule.json` + their own `docs/signals/cowork-team-*.json` — i.e. already pathspec-scoped, satisfying AC-3's clause-(ii) positive-exercise requirement if the window closes clean. But `feedback_ac3_durability_certified_on_window_that_ended_before_metric_settled` and this row's own ruling text ("clause (ii) is MANDATORY and NON-WAIVABLE... an absence-based AC needs a liveness companion or it certifies blindness") both bar treating 22.6%-elapsed silence as a pass. Per instruction, did not write `orch-state.json` (dispatcher's job) — reported HOLD for the router to action (no lane-move, no `redispatch_count` bump). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S217.

## cycle-844 · 2026-08-25T20:35Z · FIX-NOTEBOOKAUTOPRUNE-GREPC-DOUBLE-EMIT-WRITES-MALFORMED-SIGNAL-JSON, direct-commit verify — DONE_VERIFIED (AC-2 PARTIAL), qa[]->done_verified[]

**The fix landed under a different row's commit, incidentally — verify the ARTIFACT, not the row's own commit field (this row never had one).** `73d4ce109` (task FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE) was PO-cited in `status_note` as also closing this row; re-derived independently rather than trusting the citation: `git log --oneline --all -- scripts/agents-flow/notebook-auto-prune.sh` shows it's the only/most-recent commit touching the file since the fold, `git show --stat` confirms all 5 named sites (587/588, 630/631, 699/700, 701/702, 751/752) now read `VAR=$(...grep -c... 2>/dev/null)` then `[ -z "$VAR" ] && VAR=0` — zero executable `grep -c ... || echo` idioms remain (3 comment-only hits).

**Reproduced must-fail-before/pass-after myself, not trusted from commit prose.** Built an isolated fixture from the PARENT commit (`091c1242318`, script+libs copied to `/private/tmp`, live tree sha256-verified untouched throughout) and ran the shipped `notebook-auto-prune.test.sh` (post-fix version, containing the new T15 regression) against it: T15 FAILS pre-fix (real malformed-JSON signal emitted, byte-for-byte the same defect shape as the live victims — bare `0,` line after `"sentinel_section_count": 0`), then re-ran on the live post-fix tree: T15 PASSES, 15/15 suite green. Micro-repro of the root idiom independently: `grep -c NOMATCH 2>/dev/null || echo 0` really does capture the two-line string `"0\n0"` on this shell/grep; the new capture-then-`[ -z ]`-default idiom captures a clean `"0"`; a real match still captures the correct non-zero count (AC-4 negative control).

**AC-3's blast-radius claim re-verified against the LIVE inbox, not the row's own snapshot.** Full `docs/signals/*.json` rescan: 51/51 parse under `jq .` (0 invalid) — down from the 4 PO had named+repaired the previous tick. Confirmed those exact 4 named victim files (`dev-mcp-server.md`, `tran-ngoc-bau.md` x3, all 2026-08-24T20:41-22:32Z) are no longer sitting in the live inbox at all — they're in `docs/signals/processed/` with valid `sentinel_section_count:0`, i.e. they actually cleared `isDrainableShape()` and drained, not merely repaired-but-stuck.

**AC-2 scored PARTIAL, not FAIL — the shipped T15 meets the AC's actual bar (JSON-parseability assertion) but only for 1 of 5 call sites.** T15 exercises the NON_SENTINEL/SENTINEL_SECTION_COUNT site end-to-end and asserts exactly what AC-2 demands (jq-parseable signal, not just "var==0"). The other 4 sites got the identical textual fix (verified structurally) but have no dedicated JSON-emission harness — noted on the row rather than silently accepted or used to fail an otherwise-correct fix.

**Journal collision, self-corrected:** a concurrent QA session's `STEP qa-S217` (FIX-SWEEPGUARD row, committed `07074c440`) landed in the shared decision-journal file between my read and my append; renumbered mine to `qa-S218` rather than leave a duplicate step-id. Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S218.

## cycle-845 · 2026-08-25T20:40Z · FIX-ORCHAPPLY-MKTEMP-SUFFIX-DEFEATS-RANDOMIZATION-BSD-CONCURRENT-WRITER-COLLISION, direct-commit verify — CHANGES_REQUESTED (3/5 AC), qa[]->review[]

**Core mechanism (AC-1/2/3) independently reproduced correct, not trusted from the router's own note.** Real Darwin/BSD box (`uname -a` confirmed): pre-fix template `mktemp ./.orch-apply-XXXXXXXX.json` collides on the 2nd call (`mkstemp failed: File exists`, literal unexpanded name written to disk); post-fix template (`X`-run trailing, no suffix, `.json` appended via a separate `mv`) produced 10/10 distinct names sequentially and, run through the FULL `orch-apply.sh` (not just bare `mktemp`) 6-way truly concurrently against an `ORCH_APPLY_LIVE_FILE_OVERRIDE` fixture, zero `mkstemp`/`File exists` — only the expected CAS-mismatch aborts on stale baselines, zero leftover temp files. CAS guard / atomic same-dir `mv` / cleanup trap all confirmed byte-identical pre/post diff except the TMP-creation line.

**Regression suite: ran it 3x, first 2 runs "failed" for a reason that has nothing to do with this fix.** `orch-apply-wrapper-tests.sh` runs 1-2: 28 then 13 FAIL, but EVERY FAIL across both runs is exclusively the suite's single-snapshot "REAL live file CHANGED" meta-check (grep-confirmed: zero non-meta logic failures either run) — root-caused via `git log`/`git diff` to a live PEER session's real `done_verified` lane-move landing mid-run (commit `d8530380b` region, working-tree write ~20:26:5xZ); structurally impossible for this suite to cause it since every invocation targets a throwaway fixture path, never `REAL_LIVE`. Run 3 landed in a quiet window: clean 109/109, live sha256 unchanged start-to-finish — matches the agent's own reported figure exactly.

**But 2 of the row's own 5 explicit ACs are unmet, held to the checklist rather than the prose.** AC-4 demands a persisted regression test proving BEFORE-fails/AFTER-passes, stated platform-honestly — the commit diff touches ONLY `scripts/orch-apply.sh` (1 file), `orch-apply-wrapper-tests.sh` has zero mktemp/BSD/collision assertions, and the developer's own notebook confirms the BEFORE/AFTER proof was ad hoc session-only execution, never committed as code — nothing in the repo would catch a regression of this exact defect. AC-5 demands the `cleanup()` trap gain a log line naming the temp path before it's silently `rm -f`'d on a failure branch — `cleanup()` is byte-identical to pre-fix, zero `printf`/log statement anywhere in the diff.

**Routed to `developer`, not `.owner` verbatim.** The row's `.owner` field reads `"qa"` (current handler, not implementer) — same self-loop hazard already caught live on `FIX-DEVTEAM-SECONDARY-DRAIN-CALLER-READBACK-REVIEW-LANE-ONLY` (cycle-840, this notebook). Used `developer` (matches the commit's own author identity per `b91a9aadc`) and named the hazard in `status_note`. Dry-ran the `vc-changes` block via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first (validator/conservation/prose-ceiling green, self-verify OK), landed for real with a caller-supplied CAS baseline hash, self-verified by re-read. Full data: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S219.
