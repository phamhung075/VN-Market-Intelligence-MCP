# QA — Notebook

## cycle-515 · 2026-08-06 · FIX-MARKETDB-JOURNALMODE-GUARD-SHIPPED-BUT-NEVER-ARMED — resumed dispatch, duplicate-spawn discovered mid-run, no 2nd board write

Separately dispatched (mode=verify-committed) for this exact task-id. Independently re-derived cycle-514's verdict from scratch (own commands, not reading the notebook first) before discovering mid-run that a CONCURRENT peer qa sub-session (same coordination_session) had already written cycle-514 + the board move moments earlier — board re-checked 3x live, row visibly transitioned QA→done_verified[] during my own investigation. Genuine duplicate-dispatch (dev-team Review-Lane QA-Drain spawned 2 qa instances on 1 task-id), not a corruption risk (`orch-apply.sh` CAS-mtime guard held; both peers reached identical verdict/commit `590dd4124`) but wasted redundant tokens — flagging for router/PO QA-DRAIN-HEADDECOUPLED dispatch-hygiene, not re-litigating the verdict.

Re-confirmed cycle-514's sibling finding is STILL live and STILL unactioned: `FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER` remains `status:DONE` in the `done` lane (never reopened despite cycle-514 flagging its Step-2-redeploy claim as evidenced-false) — re-verified myself: stock-price container still `RestartCount=0`/image build `2026-07-31T00:41Z`, `curl :5010/price/history?code=HVN` still empty, `curl :5005/reading/HVN` still 503 "insufficient price data". kinh-dich has been fully down since 07-31, still down now, and the "for visibility only" flag in cycle-514 had not converted into an actual reopen/mint. Escalated directly: `send_telegram(channel="bug")` → message_id 4854 (reopen ask + the `foreign-accum-rank` "database is locked" symptom cycle-514 also found). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S33.

## cycle-516 · 2026-08-06 · FIX-LAUNCHD-PROBE-PRESENCE-ONLY-FALSE-GREEN — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had no `commit`/`files[]` fields — derived `e0d9187cd` via `git log` grep on the task-id in commit bodies for `scripts/agents-flow/auditor-tier1-probe.{sh,test.sh}`. Confirmed on main ancestry; commit author-date (`2026-07-23T10:24:35Z`) matches the row's own `reviewed_transition_at` (`10:24:47Z`, 12s later) and `owner:developer` matches the commit author role.

Read `_check_launchd_agents()` on CURRENT HEAD directly, not just the original diff (a later unrelated commit, `b9484fa7a`, wraps the same core with acked-ledger suppression) — the exact-match-then-status-check mechanism is unweakened: awk field-3 exact LABEL match against `launchctl list`, then field-2 Status must `== "0"`, failure names `<label>(exit-status:<code>)` distinct from the pre-existing `<label>(not-loaded)`. `not_a_bug_do_not_change` clause confirmed intact: `obsolete_labels="com.vn-market.socat-bridge"` still byte-identical, still `continue`s before either presence or status is evaluated.

Re-ran `auditor-tier1-probe.test.sh` myself (`feedback_router_verify_raw_not_badges`): 181/181 PASS (suite grew past the commit's claimed 102/102 via later unrelated fixes) — specifically confirmed T33 (loaded+status78→FAIL naming label+code)/T34 (loaded+status0→PASS)/T35 (obsolete-allow-listed+absent→PASS) all green. `mock-guard.sh --files auditor-tier1-probe.sh` → PASS. No `.ts`/apps/ touched (zone `cross-service/`, pure bash) — `bun tsc --noEmit` structurally N/A.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S36.

## cycle-516 · 2026-08-06 · FIX-EXECTIER-HEADSYNC-BRANCHNULL-REVIEW-IDLE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Commits `020b9eeee`+`7b6d878ab` (row's own field) confirmed on main ancestry. `git show --stat` matches: `execute-tier.md` § MUST(b) gained explicit branch:null sub-rule (REVIEW flip of a branch:null task MUST set `head={status:idle, active_task_id:null, next_agent:router}`, never mirror the task's own status) + new regression script `scripts/audits/execute-tier-branchnull-review-headidle-verify.sh` + developer's decision journal/notebook. Doc/shell-only change, zero `src/` TS touched → `bun test`/`tsc`/`mock-guard` correctly N/A (Smart-Skip). Re-ran the regression script myself (not trusted from prose) — PASS, matches header claim. Confirmed a later unrelated commit (`6e1125251`, BLOCKED-disambiguation clause) is additive only — the branch:null idle clause is byte-intact in the live file.

Live dogfood corroboration (not just self-report): `review[]`=254/`qa[]`=10, QA-Drain actively batch-claiming rows (commit `252fea4f3`, 9 rows incl. this one, `.head` untouched/unrelated) — the row's own AC (branch:null REVIEW flip leaves head idle; subsequent dispatcher tick reaches QA-Drain without a hand-reset) is exercised live by this exact dispatch, matching the review_note's dogfood claim.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 805→805). `.head` untouched (unrelated active_task_id: `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S34.

## cycle-517 · 2026-08-06 · FIX-DRAINPRUNE-SKIP-LIVE-REFERENCED-PROCESSED-FILES — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had no top-level `commit`/`files[]` — derived `56be7b77e` from review_note prose, confirmed on main ancestry. `git show --stat` matches: `drain-signals.js` gains `gatherLiveReferencedBasenames()` (same lane walk as Stage 1c `checkRefIntegrity()`) guarding the 7d prune loop — SKIP-only, never deletes a referenced file — plus `drain-signals.md` §0a-2 spec update (edited first, per convention) and `drain-signals.test.js`.

Re-ran `node scripts/agents-flow/drain-signals.test.js` myself, not trusted from prose → 36/36 pass incl. the 5 new assertions. Read case-1 seed data directly: a `backlog[]` row's `detail_ref` pointing at a >7d-old processed file, exact AC-2 scenario, not paraphrased. "No Stage1c dangle" holds by construction (SKIP-only guard precedes every `unlinkSync`, no bypass). `mock-guard.sh` PASS; no `.ts` touched (plain Node, outside apps/mcp-server) → `bun tsc` N/A (Smart-Skip); `git log` confirms this is the latest, unreverted touch to the file.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 806→806). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S34.

## cycle-518 · 2026-08-06 · FIX-DRAIN-PERSIST-GUARD-COUNT-DRAINABLE-ONLY — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had NO `commit`/`files[]`/`status_note` fields at all — derived `980937380` via `git log -- scripts/agents-flow/drain-signals.js` cross-checked against developer's own decision-journal entry + `docs/WORK.md`'s matching dated entry; confirmed on main ancestry. Read the full diff myself: `isDrainableShape()` added once, shared (not forked) by the new `--count-drainable` subcommand AND the pre-existing drain-loop "SKIP non-signal shape" check — matches the row's own root_cause/deliverable verbatim.

Re-ran both real test suites myself, not trusted from commit prose: `node drain-signals.test.js` → 36/36 PASS incl. 3 new Fixtures A/B/C (residue-only inbox→0, genuine+litter mixed→1, missing dir→0); `bash dev-team-tick-preflight.test.sh` → 124/124 PASS incl. T14 (genuine drainable signal still blocks RUN-IDLE, negative control) and T32 (litter-only `SIGNALS_DIR` resolves RUN-IDLE, the row's positive AC). `mock-guard.sh --files "drain-signals.js dev-team-tick-preflight.sh"` → PASS. No `.ts`/apps/ touched (zone `cross-service/`, plain Node+bash+md) — `bun tsc --noEmit` structurally N/A (Smart-Skip).

VERDICT: APPROVED, DONE_VERIFIED. Both stated ACs (residue-only-does-not-trip / genuine-still-trips) directly reproduced via independent re-run of the real test suites. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 806→806). `.head` untouched (unrelated active_task_id: `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S35.

## cycle-519 · 2026-08-06 · FIX-CRON-SSCCHECKERJOB-DEAD-87D — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had no top-level `commit`/`files[]` — derived `791b0e4dc` from status_note prose, confirmed on main ancestry. `git show --stat` matches all 10 claimed files (3 new test files, `cronStatusCompute.ts`, `sscCheckerJob.ts`, `startScheduler.ts`, dev journal/notebook, `cron-jobs.md`, `news-analysis.md`). Guard moved from top-of-`runSscCheck()` to inside the `recordJobRun()` callback so the daily cron always writes honest `success`/`rows_written:0` telemetry when it correctly no-ops; `dataAuditJob:weekly` (a SECOND, independently-discovered dead job, same restart-timing mechanism, different job_name/window) got its own `startScheduler.ts` startup-catchup probe; class fix adds a registry-invariant test reading `STATIC_JOB_NAME_MAP`'s real keys via `_staticJobNameMapKeysForTests()` instead of a 4th hand-copied fixture.

Re-ran all 3 test files myself: 12 pass/0 fail/21 expect() — AC-1/AC-2/AC-3 directly assert the honest-telemetry fix AND the network-safety non-regression (checkSscReports() never invoked when guard fires). `bun tsc --noEmit` clean, `mock-guard.sh` PASS, DDD/security greps clean (pre-existing infra imports unchanged, not new). Read current-HEAD `sscCheckerJob.ts` (a later unrelated commit layered a WORK-telegram summary on top) — class-fix mechanism unweakened.

`do_not_absorb` requires an ACTUAL successful run in `cron_job_runs`, not just watchdog-manifest visibility — did not trust status_note prose (`feedback_router_verify_raw_not_badges`). RAW live query, production mcp-server container (recreated 2026-08-06, post-fix): `sscCheckerJob` fires daily 13:00 UTC success/rows_written=0 continuously since 2026-07-24; `dataAuditJob:weekly` fires Saturday 18:00 UTC success since 2026-07-25 and 2026-08-01 — both previously-dead jobs demonstrably alive in production. `known_unknown` (downstream BCTC dependents possibly stale ~3mo) also independently corroborated live: `bctcQueueEnricherJob`/`bctcExtractReconcileJob`/`bctcPdfPullJob` (the cited superseding pipeline) all firing every 10-20min right now — no stale-data crisis.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 806→806). `.head` untouched (unrelated active_task_id: `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S38.
