## cycle-833 · 2026-08-25T00:24Z · FIX-ORCHAPPLY-CAS-BASELINE-CAPTURED-AFTER-CALLER-JQ-READ, direct-commit verify — CHANGES_REQUESTED, qa[] -> review[]; `.head` reset (named this row)

**Router's 3-part reading (i)-(iii) confirmed ALL TRUE at source, not narrated.** (i) `orch-apply.sh:259`'s `bun scripts/orch-conservation-check.mjs` call is a bare top-level statement, no `if`-gate, no env-var toggle — runs unconditionally on every write, and `undeclaredBackwardLaneMoves()` is confirmed independent of `ORCH_APPLY_ALLOW_SHRINK` at source (own comment block: "never bypassable"). (ii) Grepped the WHOLE repo (`.claude/skills`, `docs/agents`, `docs/policies`, `scripts`) for `ORCH_APPLY_CALLER_BASELINE_HASH`/`_MTIME` — the ONLY hits outside `orch-apply.sh` itself are `docs/policies/dev-standards.md` (documentation) and `scripts/test/orch-apply-wrapper-tests.sh` (the test harness); zero real call sites set either var, so all ~290 writes/tick still hit the self-captured-mtime fallback and the original read→transform staleness gap is fully open for every one of them. (iii) Read the `LANE-BACKWARD-SINGLE-TOLERATED` test body directly: it builds a candidate moving exactly ONE id `qa[]->review[]` with nothing else touched and asserts exit 0 — structurally identical to what a stale-full-doc candidate reverting exactly one row would look like; the guard's threshold (`CONSERVATION_MAX_UNDECLARED_BACKWARD_MOVES` default 1) cannot distinguish "sanctioned single-row op" from "1-row stale revert" by construction. My OWN verdict-write on this very row printed `lane_placement=clean (1 tolerated backward move(s): FIX-ORCHAPPLY-CAS-BASELINE-CAPTURED-AFTER-CALLER-JQ-READ)` — live production proof of the exact same shape.

**Re-ran the real test suite myself rather than trusting the router's re-run claim.** `bash scripts/test/orch-apply-wrapper-tests.sh`: 109/109 PASS, independently reproduced (not merely re-cited). Read both diffs (`orch-apply.sh`, `orch-conservation-check.mjs`) line-by-line at source — AC-1 caller-baseline mechanism + byte-identical fallback confirmed correct; AC-2 `CALLER-BASELINE-STALE-REJECTED`/`-SEQUENTIAL-HAPPY` confirmed exit-2/exit-0 respectively; AC-5's FIRES-not-just-green requirement satisfied (`LANE-BACKWARD-MULTI-REJECTED`, `CALLER-BASELINE-STALE-REJECTED` both prove real rejection, not narrated).

**AC-4 finding beyond the dispatch's own framing — this is why the verdict is CHANGES_REQUESTED, not a caveated PASS.** The row's own AC-4 text is a disjunction: "callers migrated in the same change, OR a tracked follow-up per caller." Neither holds. Grepped the live board (every lane), `docs/data/orch/archive/2026-07.json`, `archive/2026-08.json`, `archive/backlog-detail.json`, `docs/agent-memory/decisions/`, `docs/agent-memory/notebooks/` for any caller-migration tracking artifact tied to this mechanism: zero hits anywhere. "Migration tracked as a follow-up" exists only as prose — in the commit message, the row's own `implementation_note`, and a `docs/WORK.md` done-log entry (itself just a record of what happened, not a forward-tracking mechanism) — none of which a future dispatcher can discover and pick up. This is the fleet's known narrated-not-actuated pattern applied to an acceptance criterion, not a closed disjunct.

**Verdict:** CHANGES_REQUESTED, routed to the row's own `owner` field (`developer`) per Direct-Commit Verify convention (no branch to hand a fixer). Required: (a) migrate ≥1 real orch-apply caller to `ORCH_APPLY_CALLER_BASELINE_HASH` end-to-end, or (b) mint an actual findable follow-up board row naming every caller needing migration + correct the over-claiming `implementation_note`. AC-1/2/3/5 are genuinely solid and left untouched — this is a narrowing (the reproduced 5-row incident shape is now caught) not a full closure (the original bug's 1-row shape is still fully reachable in production).

**Lane-move:** own jq guarding `status=="QA"`, dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` fixture first (validator/conservation/prose-ceiling all green), then landed `qa[]->review[]` for real with `redispatch_count:1`, `next_agent:"developer"`. `.head` DID name this row (active_task_id match) — reset to idle in the SAME write. Self-verify re-read: `status==REVIEW`, `next_agent` type==string, `.head.status==idle`. Full analysis: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-28.md` STEP qa-S202. Commit pending this cycle's close (orch-state.json + journal, explicit pathspec).

## cycle-834 · 2026-08-25T02:43Z · FIX-REAPER-ORPHAN-MINT-KEYS-ON-TTL-ONLY-NO-SESSION-LIVENESS-CHECK, direct-commit verify — APPROVED, qa[] -> done_verified[]; `.head` reset (corrected mid-task, named this row)

**Re-derived every claim the dispatcher had already made, none accepted on say-so.** All 3 commits (`0f6891872` fix+tests, `a2e52c745` notebook, `ffbd8cca7` decision journal) confirmed main-ancestors; `git show --stat` confirms both claimed files touched. Read the diff at source: one `NOT EXISTS` correlated subquery added to Phase-1's SELECT with a `t.` table alias applied consistently (incl. `excludeClause` -> `t.task_id != ?`, its sole use site, no other caller affected — grepped both `gcExpiredLocks(` call sites). Phase-2 DELETE confirmed a wholly separate, unconditional, unaliased query — structurally untouched, an expired lock GCs either way. Targeted 4-file suite: 89 pass/0 fail/250 expect, exact match to `implementation_note`'s 89/89 claim. `tsc --noEmit` 0 errors; DDD grep N/A-clean (file IS infrastructure, no domain violation possible); security grep clean; `mock-guard.sh` PASS.

**Independently reproduced the load-bearing polarity proof rather than trusting the dispatcher's own mutation-test result.** Patched `presence.expires_at > unixepoch('now')` to `> 99999999999` (never-true), re-ran the 4-file suite: 87 pass/2 fail, and the 2 failures were exactly `AC-1` (suppress-on-live) + `AC-4` (live-incident regression) — the two suppress-cases; `AC-2`/`AC-2b`/`AC-3` (never-suppress cases) stayed green. Confirms an absent/undercounting presence roster can only fail toward today's behaviour, never invert into false-suppression. Reverted the mutation, `git diff` on the file empty after — confirmed clean before proceeding.

**Full-suite baseline compared by failing FILE SET, not exit code, per house SOP.** Ran full `apps/mcp-server` suite myself instead of citing the dispatch's 50/15 figure: 15467 pass/40 skip/51 fail/1285 files [520s]. Extracted the fail set via an awk state-machine on literal `(fail)` markers (51 markers, sums matched exactly across 16 files): 15/16 are an exact name match to the documented baseline list; the 16th (`102-job-news-poll.test.ts`, 1 fail) is new but zero grep-hits for `coordinationStore`/`task_locks`/`session-presence`, and reproduces 10/10 pass in isolation (20.69s) — a shared-tree-load timeout flake, not attributable to this diff.

**Mid-task correction, verified before acting.** Dispatching session flagged `.head` was still pinned to this row (`active_task_id` match) contrary to the original spawn prompt's "`.head` is idle" claim — re-read `.head` myself first (confirmed true, not accepted on say-so), then landed the lane-move AND the `.head` reset to `{idle, null, router}` in the SAME `orch-apply.sh` write, dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`. Self-verified both halves against the live file post-write: `status==DONE_VERIFIED`, `has("next_agent")==false`, `raw_probe.tool != null`, `.head.status==idle && .head.active_task_id==null && .head.next_agent=="router"`. Full analysis: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-28.md` STEP qa-S203. Commit pending this cycle's close (orch-state.json + journal, explicit pathspec).
