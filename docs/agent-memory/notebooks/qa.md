# QA — Notebook

## cycle-696 · 2026-08-13T15:57:41Z · FACTORY-GUARD-CI-SHAREDPKG-IMPL (developer, real code, commit `9e9389089`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), same batch as the DEADCODE/NOHARDCODE/COMPROOT-IMPL cycles this tick, explicitly asked to RAW re-verify every claim from live repo state not prose. Row carried no `.commit`/`.files[]` — derived `9e9389089` via `git log --all -- scripts/audits/shared-package-import-check.sh`, sole hit, confirmed ancestor of `main`. `git show --stat` matches all 6 claimed files exactly (new script + test.sh, new `docs/data/shared-package-import-baseline.json`, `ci.yml`, `dev-standards.md`, `WORK.md`).

RAW re-ran fresh, not trusted from prose: `--check` on live repo HEAD → exit 0 (~8s); baseline covers exactly 3 packages — `git ls-files packages/*/package.json` confirms `shared-config`/`shared-db`/`shared-types` are the ONLY `@vn-market/`-scoped packages in the repo, all 3 printed BASELINE. Independently re-grepped all 3 myself OUTSIDE the script (manual `grep -rlE`, own-dir excluded) → zero real importers for all 3, matches the script's own verdict — did not trust the script's self-report alone. Check-2 advisory lines (McpConfig/loadMcpConfig/Alert/Signal/+6 more) emit correctly, non-blocking, exit stays 0.

`shared-package-import-check.test.sh` 4/4 PASS (rc=0/1/0/0) — read all 4 case bodies, not just the summary line: case1 baseline-listed zero-importer fixture passes (BASELINE line); case2 synthetic new zero-importer fixture NOT in baseline fails (FAIL line); case3 synthetic fixture WITH a real importer passes despite ALSO being baseline-listed (real-importer wins); case4 live-repo run (no overrides) emits the Alert/Signal/McpConfig advisory lines without affecting the green exit. Fixture dir trap-cleaned, `git status --porcelain` confirms zero residue post-run.

Read the script's own source (not the claim) to confirm the docs/** exclusion is structurally real: `list_search_files()` globs only `apps/**/*.ts`+`package.json` and `packages/**/*.ts`+`package.json`, both in the default path and the test-override env var path — zero `docs/**` anywhere in either search corpus. CI job confirmed wired live at `.github/workflows/ci.yml:481-504` (YAML parse-validated via `python3 yaml.safe_load`); `dev-standards.md:1280-1321` CANONICAL pointer content matches actual script behavior, not just present. shellcheck: `.sh` 0 findings; `.test.sh` 1 SC2329 info (`cleanup()` trap-invoked false positive) — confirmed byte-for-byte identical finding already present on sibling `size-lint-justification.test.sh` (same trap-cleanup idiom, pre-existing accepted pattern in this repo), non-blocking. `git show --stat` confirms zero `apps/` source touched (zone=cross-service/, pure bash+yaml+md+json) — `bun test`/`tsc` correctly N/A, not run (would be false-negative noise on a zone with zero TS/JS change).

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` (no handoff file — direct-commit path); `verification.raw_probe` attached per RC-VERIF schema (first attempt without it hit a validator hard-fail, corrected same write). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 735=735; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S108. No `task_release` called and no `send_telegram` sent — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only, confirmed by tool-list absence not assumption); the developer's own review_note flagged this same lineage gap. Orch-state write left UNCOMMITTED per this session's established convention — dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1, and the dispatcher (which holds `task:FACTORY-GUARD-CI-SHAREDPKG-IMPL`) will RAW-verify this report and commit.

## cycle-695 · 2026-08-13T15:30:06Z · FACTORY-GUARD-CI-NOHARDCODE-IMPL (developer, real code, commit `5b4a13949`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), explicitly asked to RAW-verify every claim, same as the DEADCODE-IMPL cycle moments earlier this same tick. Row carried no `.commit`/`.files[]` — derived `5b4a13949` via `git log --all -- scripts/audits/no-hardcode-allowlist-scan.sh`, confirmed ancestor of `main`. `git show --stat` matches all 11 claimed files exactly (new script + test.sh, `ci.yml`, `dev-standards.md`, 6 touched TS files, `WORK.md`).

RAW re-ran fresh, not trusted from prose: `no-hardcode-allowlist-scan.sh --check` on live repo → PASS 0 offenders; `no-hardcode-allowlist-scan.test.sh` 9/9 PASS; shellcheck clean on both new scripts. CI job confirmed wired `.github/workflows/ci.yml:439`, `dev-standards.md` CANONICAL pointer present via direct grep.

Verified the 2 cosmetic fixes are genuinely behavior-preserving via line-level diff (not trusted from message): `backfillBctcScalarsTool.ts`'s CTG-only reason branch and `pharmaEventMapper.ts`'s IMP-only reasoning branch both collapse to the generic string only — `status`/`table_row_count`/`code`/`direction`/`confidence` fields untouched in both diffs.

Verified the 2 known-debt annotations are real, not fabricated: `JANITOR-034`/`JANITOR-035` both genuinely pre-exist in `docs/data/code-janitor-known-findings.json` with detail matching the commit message verbatim; confirmed `hardcode-scan-allow:` comments landed at `cascadeExecutor.ts`/`priceSourceRouter.ts`/`newsChainFallback.ts`. Regex-replayed the live `TICKER_BRANCH_ERE` by hand against the `LARGE_CAP_FALLBACK`/`MAJOR_CAPS` array-declaration lines — confirmed it genuinely does NOT match (no `===` on those lines), matching the claimed "paper-trail marker, not active suppression" framing exactly; separately confirmed it DOES match `priceBackfillService.ts:224`'s `ticker === "BAD"`, and that annotation's citation of brief §2(c) is real — `ohlcvWriteService.ts:49` verbatim labels Writer F "Historical seed/mock only... sentinel present", exactly as claimed.

`tsc --noEmit` 0 errors. Targeted suite: my first grep pass on literal filenames found only 16 files (missed `1279a-msci-inclusion-cascade-red.test.ts`, caught on a 2nd pass grepping exported symbol names) → 17 files, 182 pass/0 fail (commit's own claim was 191/191 — 9-test delta over 2 weeks of intervening commits to shared test files since the 07-30 commit; zero fail either way, treated as non-blocking drift not a discrepancy in the underlying claim). `mock-guard.sh` PASS on all 7 touched production files. DDD/security scan on touched files: clean (no infra imports, no `process.env`/secrets).

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 739=739; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S107. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.

## cycle-694 · 2026-08-13T15:17:05Z · FACTORY-GUARD-CI-DEADCODE-IMPL (developer, real code, commit `448b41b0a`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`). Row carried no `.commit`/`.files[]` — derived `448b41b0a` via `git log --all -- scripts/audits/dead-code-gate.sh`, confirmed sole ancestor on `main` AND `origin/main`. This is the DEADCODE-IMPL child (real code, 24 files); the sibling design-only brief row (`FACTORY-GUARD-CI-dead-code-gate`, commit `3b4d5a0d3`) was a separate already-verified QA cycle on 2026-08-12 (cycle-647 in this file's prior history), not re-litigated here. `git show --stat` matches all 24 claimed files exactly.

RAW re-ran everything fresh, not trusted from prose: `dead-code-gate.sh --check` on live repo → PASS 0 offenders; `dead-code-gate.test.sh` 8/8 PASS (all 4 DoD synthetic-offender cases + tracked-only control + twin-scaffold Dockerfile pair proving the news-fetch exemption is deliberate). CI job wired at `.github/workflows/ci.yml:420`, `dev-standards.md` CANONICAL pointer present via direct grep. All 7 named deletions confirmed gone from the tracked tree (`git ls-files --error-unmatch` per path); zero dangling imports remain — only harmless historical comments reference the deleted `kinhDichWrapper.ts`/`reuters.ts`, no live `import` statements.

Downstream zone claims reproduced fresh across all 4 zones: mcp-server `tsc --noEmit` 0 errors, `eslint src/` 0 issues, `1081-sprint-054-smoke.test.ts` 14/14 pass; technical-analysis `go build`/`go vet`/`go test ./...` all clean, `golangci-lint run` 0 issues, `dashboard/build.sh` 35/35 sandbox scenarios + headless render gate PASS (esbuild/playwright-core still load-bearing post-devDep-trim); pdf-extractor `lint-imports` 3 contracts kept/0 broken; stock-price `golangci-lint run` 0 issues. DDD scan on the 2 touched TS files: `analysis.ts`'s infrastructure/application imports are the expected interface-layer composition-root pattern, not a violation. Security scan clean. `mock-guard.sh` PASS.

Ran a FRESH full `bun test` (not reused from claim): 15262 pass/40 skip/56 fail/15358 total (492.5s) — cross-checked every one of the 56 fail names against every file this task touched (dead-code-gate scripts, ci.yml, .gitignore, fetchers/index.ts, news-analysis/analysis.ts, technical-analysis package.json/bun.lock): zero overlap, all 56 sit in the documented pre-existing order-dependent flake band (backtest_runs/logVpsPush/VPS-Proxy-Health/task_heartbeat-Zod/insider-transactions/etc.), tracked by still-BACKLOG board row `FIX-MCP-SUITE-HEALTH-BASELINE` — matches developer's own 3-pass base-vs-head A/B claim (base 20fail/9files, head-run1 24fail/10files, head-run2 2fail/2files).

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 739=739; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S106. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.
