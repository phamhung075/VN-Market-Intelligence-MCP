## cycle-855 · 2026-08-26T03:45Z · FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Multi-cycle measurement row PO already closed (po_closure_20260826T0116Z) — verified by artifact, not narration.** Confirmed the ORIGINAL production fix `e9144ea75` (ancestor of main) genuinely replaces `mean(conf)`-over-recognised-only with `min(precision, _ink_coverage-recall)` — read the actual diff, not the commit prose — so a total-page-miss now collapses confidence via the recall term (answers the dispatch's core "does it change the denominator" question). Confirmed the row's CLOSING commit `efc4d6d79` (status_note-named; row carries no `.commit` field) is docs+scripts-only, `ocr_backends.py` + `PDF-Extract-Kit` untouched — the AC-mandated "do not modify" constraint honored.

**Row `.files[]` is STALE** — lists cycle-1's 3 audit scripts (`ocr_confidence_probe_inner.py`/`ocr-confidence-probe.sh`/`ocr_bench_inner.py`), not `efc4d6d79`'s actual 5 touched paths. Bookkeeping drift across 2 redispatch cycles, not fabrication — status_note unambiguously names the real closing commit and it independently checks out.

**Externally cross-checked, not merely internally self-consistent.** n=6 sample manifest (`scripts/audits/ocr-widen-sample-manifest.json`) md5-verified byte-for-byte against live `data/pdfs/` — 6/6 match exactly. `DECISION-PDFX-OCR-TEXT-BACKEND-DEFAULT-FLIP-TO-AUTO` confirmed CANCELLED in `archive[]` with matching rollback-trigger numbers (46% fire rate, 494 hard-limit events). `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` confirmed live-tracked (`backlog[]`, BLOCKED on the same AC8 market-hours gate, code already landing at `1db5f9f81`) — po_closure's "ready[]/architect" cite was accurate as of 01:16Z, simply progressed since. `docker-compose.yml` confirms `OCR_TEXT_BACKEND` unset for pdf-extractor (defaults `tesseract-vie`), matching the "never enabled in production" claim.

**Live OCR-probe re-run explicitly withheld, not silently skipped.** Current time fell inside the 02:00-08:59Z weekday market-hours hard gate and the live `pdf-extractor` container was confirmed Up/healthy (`docker ps`, left untouched) — re-running the probes to reproduce the raw ink_cov/line_ink_cov numbers myself would have meant invoking live OCR inference, explicitly forbidden this cycle. Treated as non-fatal: `efc4d6d79` changed zero production code, and the underlying `e9144ea75` fix was already independently pytest/docker-verified by a prior qa cycle at 17:32Z (outside market hours). The DBC p12/p15 confidence values (0.535/0.586) cited in this cycle's decision doc exactly match my own separate cycle-848 finding on the sibling orientation-fix row — strong non-fabrication corroboration.

Lane-moved `qa[]->done_verified[]` via the mandated `jq | orch-apply.sh` actuator (dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first, then live), claim identity (`owner_client_session`/`claimed_at`/`claimed_by`) nulled same write, `.head` untouched (already idle, did not name this row), self-verified by re-read on the live file. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S230.

## cycle-856 · 2026-08-26T04:10Z · FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS, direct-commit verify — HOLD (not a verdict), qa[]->review[]

**A row's own `qa_not_before` gate is a hard precondition, not a suggestion — checked it BEFORE running any check.** Hand-dispatched into `qa[]` at 04:07Z, but the row's own `po_ruling_ac3_scope_20260825` sets `qa_not_before=2026-08-26T14:59:30Z` (earliest closable, AC-3's 24h window anchored at commit `e6adc6dfe`, 2026-08-25T14:59:30Z) and explicitly states "a pick before that timestamp MUST read qa_not_before first and return HOLD without consuming a redispatch." Only ~13h/24h (54%) had elapsed at dispatch time — did not attempt to certify AC-3.

**Re-confirmed the already-PASSED ACs fresh rather than trusting the prior QA cycle's note verbatim** (row sat 8h stranded, per spawn-context caution). AC-1/AC-2/AC-4 all still hold: `e6adc6dfe` still an ancestor of `main`; `telemetry.md` Step 6.2 + `main.md` dispatch-table edit intact; `.git/hooks/pre-commit` still the symlinked fixed hook; `scripts/git-hooks/pre-commit.test.sh` re-run 15/15 PASS. AC-3 spot-checked as informational-only (explicitly NOT a verdict): 0 cowork-tick-signature BARE fires in `.git/sweep-guard.log` since anchor (3 fires total since anchor, all system-auditor/unified-agent class — none matched `cowork-schedule.json`/`cowork-team-*` signature); clause(ii) positive-exercise-proof strongly satisfied (32 commits touched `docs/data/cowork-schedule.json` since anchor, 3 spot-checked all clean 2-file pathspec-scoped diffs, not bare sweeps).

**Never self-looped `next_agent`.** Spawn instructions forbid routing UNVERIFIED-BLOCKED back to `qa` (re-dispatches straight to me) — routed to `po` instead, matching the row's own established "next_agent po -> qa" convention so PO (or a future timed re-check) hands it back to qa only after the window closes.

Lane-moved `qa[]->review[]` via the mandated `jq | orch-apply.sh` actuator (dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first — also confirmed the write would NOT trip the row-prose-ceiling guard despite the live-side id lookup only scanning `backlog/ready/review`, since prose bytes measure 5.7KB, well under the 12KB ceiling — then live), claim identity (`claimed_at`/`claimed_by`/`drain_source_lane`) deleted same write, `redispatch_count` deliberately left UNCHANGED at 1 (HOLD does not consume a redispatch), `.head` untouched (already idle, did not name this row), self-verified by re-read on the live file AND at git HEAD (commit `a4317a831`) to confirm no peer sweep absorbed the write. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S231.

## cycle-857 · 2026-08-26T04:14Z · FU-RAG-DEPLOY-MEMORY, direct-commit verify — UNVERIFIED-BLOCKED (not a code defect), qa[]->review[]

**3-day-stranded qa[] row, hand-dispatched (router-flagged FIX-DEVTEAM-QADRAIN-SKIP-BRANCH-STRANDS-ALREADY-LANEMOVED-ROW-IN-QA).** Live-checked, not narrated: D1 mid-window dmesg query (per `po_ops_followup_20260825T1624`'s explicit ask for a mid-window capture, not close-only) = 0 OOM hits, buffer covers the full elapsed window with margin. D2 container identity unchanged (`16c59b5e929f`, `StartedAt` matches window-open exactly, `RestartCount=0`). D4 no mitigation — `git log --since=<window-open>` on `docker-compose.yml`+`apps/rag-service/` = 0 commits. D3 partial (871 samples so far, sawtooth 29.39-84.80%, 0 over the 85% cap line) — informational only, window incomplete. AC-1/AC-2 code/config re-confirmed: `4df192e05` still on `main` ancestry, live 2GiB cap matches docker-compose + `docker inspect`.

**Decisive fact: the D1 >=24h wall-clock floor simply had not elapsed.** Window opened `2026-08-25T13:18:55Z`, earliest legitimate close `2026-08-26T13:18:55Z`; live check ran `2026-08-26T04:07:57Z`, ~9h11m short. Certifying now would repeat the exact "unsettled window" defect `RAG-MEM-DURABILITY-BAR v2` exists to prevent, and this row's own history already has 3 recorded false certifications from exactly that mistake — held UNVERIFIED-BLOCKED rather than inferring a pass from clean-so-far readings.

**Departed from this row's own 4-cycle historical convention ("stays qa[], no lane move") — deliberately, per this session's explicit no-strand mandate.** Moved `qa[]->review[]`, `next_agent=po` (row carries no `.owner`/`.owner_agent` field; po is the actor who has tracked this row's cadence across cycles). Did NOT touch `blocked_by` on the gated sibling `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` — that clear is a PASS-only action and this cycle is not a PASS.

**Ceiling-safe relocation, not deletion.** Dry-run (`ORCH_APPLY_LIVE_FILE_OVERRIDE`) confirmed the plain qa[]->review[] move would hard-reject at `orch-row-prose-ceiling-check.mjs` (live baseline reads 0 since `qa[]` sits outside its 3 checked lanes; candidate prose was 18.7KB against a 12KB ceiling). Merged the row's full current prose into its existing `detail_ref` cold record (`docs/data/orch/archive/backlog-detail.json`, id-matched, round-trip-validated before write) as a new timestamped field alongside the untouched prior history, then wrote a short (~1.7KB) pointer `status_note` inline. Re-ran the dry-run clean (0 net-new-growth violations), then applied live and self-verified both the hot row (`status==REVIEW`, string `next_agent`) and the cold merge (`qa_verify_20260826T0408Z` present) before committing. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S232.

## cycle-858 · 2026-08-26T05:49Z · FIX-DEVTEAM-QADRAIN-SELECTION-BLIND-TO-QA-NOT-BEFORE-TIME-GATE, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Verified the developer's own fix for the exact defect class that produced cycle-856/857's two HOLDs above.** `fe013f22f` (arch brief `2026-08-26-qadrain-shared-hop-timegate-conservation-skipstrand.md` sec1a+1b): new `gate_not_before_keys`/`is_gated_not_before` predicate in `scripts/lib/devteam-eligibility.jq` (known-list: `qa_not_before`, `next_recheck_not_before`, `qa_new_window_earliest_d1_close`), wired into `scripts/devteam-review-claim-qa-drain.jq`'s candidate filter; mirrored list added as mandatory Step 0d in `docs/agents/qa/flow/main.md`, before ANY jump target — the convergent backstop covering router hand-dispatch, which the picker-side predicate structurally cannot reach.

**Did not accept the reported 60/60 at face value — re-ran fresh, then went past the shipped suite.** `scripts/test-devteam-donelane-drain.sh` 60/60 confirmed live, incl. I9-I13 (future/past/symmetric-field/absent-gate/month+year-boundary). Three checks beyond the suite: (1) mutation test — stripped the one gate-wiring line from a scratch copy, re-ran the suite, exactly the 13 gate-related cases (I9-I13) failed and nothing else — proves the fixtures actually discriminate a quietly-inert gate rather than passing vacuously; (2) live-isolate fixture built from the two real currently-gated rows (`FIX-SWEEPGUARD-...`/`FU-RAG-DEPLOY-MEMORY`, real field names+values) with `next_agent` flipped to `qa` — both excluded at the real current `$(date -u)`, a no-gate control row included; same fixture with the gate value moved to the past included both — isolates the gate's own causal effect rather than resting on the coincidental `next_agent=po` already sitting on those two live rows; (3) malformed-value probe (malformed string / explicit null / empty string / wrong JSON type on `qa_not_before`, none of which the shipped suite covers) — all four read as non-gating (row included), never as gate-forever nor always-gated, confirming `parse_ts`'s null-safety holds through `is_gated_not_before`. Confirmed epoch comparison (`fromdateiso8601`) in the code, not lexicographic string compare.

**No TS/production source in this commit — bun test/tsc/mock-guard/DDD/security scans are N/A, confirmed rather than skipped.** `git show --stat` on `fe013f22f` touches only `.jq`/`.md` files; `mock-guard.sh` run against the two claimed files returned "no production source files to scan" PASS. One incidental grep hit outside this row's scope (a PO ad-hoc script using `next_recheck_not_before` on backlog/ready rows) is a separately-filed follow-up (`FIX-DEVTEAM-BLOCKED-STATUS-FREEZES-ROWS-NO-CONSUMER-ALLOWLIST-ADMITS-IT`) for a different lane, not a gap here.

Lane-moved `qa[]->done_verified[]` via the mandated `jq | orch-apply.sh` actuator (dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first, then live), `next_agent` deleted same write (RC-VERIF `verification.raw_probe` populated with the real tools/args/observations above), self-verified on the live file. `.head` was pinned to this row (`active_task_id`/`next_agent=qa`, stale from the claiming drain) — reset to idle in a same-cycle follow-up `orch-apply.sh` write since the vc-approved template itself carries no `.head` clause. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S233.

## cycle-859 · 2026-08-26T06:18Z · FIX-DEVTEAM-QADRAIN-SKIP-BRANCH-STRANDS-ALREADY-LANEMOVED-ROW-IN-QA, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Verified BOTH SKIP call sites, not just one — the exact half-patch risk the spawn brief flagged.** `docs/agents/dev-team/flow/main.md`: idle-tick site L986-1001 (revert invocation L995-997), head-decoupled site L1312-1322 (revert invocation L1318-1320). Both explicitly documented and confirmed in-file as never piped through a discard operator — a bare pipe to `orch-apply.sh`, log-loudly-on-failure by construction (grepped for every invocation of the revert script first, then read each call site full-width, not just the grep hit line).

**Faithfulness check on `scripts/devteam-qadrain-skip-revert.jq`: restores, never drops.** Read the jq — moves the row OUT of `qa[]` and back INTO `.task_board[drain_source_lane]` with `status` flipped to match (`REVIEW`/`DONE`), clears exactly the 3 fields the claim script itself stamps (`claimed_at`, `claimed_by`, `drain_source_lane`) — symmetric with `devteam-review-claim-qa-drain.jq` own stamp set, confirmed by reading that script too. 2 defensive no-ops (id absent from `qa[]`; row already progressed past `status==QA`) both present and asserted.

**Mutation check discriminated — did not take the 60/60 claim on trust.** `bash scripts/test-devteam-donelane-drain.sh` live: 60/60 pass. Then substituted a row-dropping variant of the revert script in place (git tracked, clean working tree confirmed first; restored via `git checkout` after) — reran the same suite: 56/60, exactly the 4 AC-3 revert-shape assertions failed (leaves-qa / lands-in-review / status-flip / status_note), the other 56 (incl. the 2 defensive no-ops) stayed green as expected. Coverage discriminates the real defect, not vacuous.

**Conservation verified two ways, not asserted from the commit message.** (1) Read `scripts/orch-conservation-check.mjs`: a single undeclared backward-lane move (qa[]->review[]/done[]) is explicitly the sanctioned shape (default max 1 tolerated), matching this revert exactly. (2) Built a scratch orch-state.json fixture (live board + 1 synthetic stranded qa[] row) and ran the REAL `jq -f skip-revert.jq | orch-apply.sh` (via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, never touching the live file) — landed in `review[]`, status=REVIEW, claim fields cleared, conservation check reported OK with task_total unchanged and the 1 tolerated backward move named.

**`scripts/` pointer registered, not just claimed** — `docs/agents/dev-team/flow/main.md:1528`, matches the file per Script Persistence convention.

Row carries no `.files[]` (predates per-file stamping on this drain path) — the `.files[]`-touch check is conditionally N/A per the flow own wording since commit and owner are both present (full missing-commit fallback did not apply). Confirmed scope instead via `git show --stat fe013f22f` against the row own zone (`docs/agents/dev-team/flow/`) plus the new script. No apps/mcp-server change in this commit (bun test/tsc/DDD/security N/A, confirmed via `git show --stat`, not skipped).

Lane-moved `qa[]->done_verified[]` via the mandated `jq | orch-apply.sh` actuator, `next_agent` deleted same write (RC-VERIF `verification.raw_probe` populated with the real tools/args/observations above), self-verified by re-read on the live file. `.head` left untouched — still pinned to a DIFFERENT row from the same 4-row batch (`FIX-QADRAIN-DONE-TO-QA-SCORES-BACKWARD-CONSERVATION-ABORTS-WHOLE-DRAIN`), not mine to reset.

## cycle-859 ·  · FIX-QADRAIN-DONE-TO-QA-SCORES-BACKWARD-CONSERVATION-ABORTS-WHOLE-DRAIN, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Verified developer's fix for the sibling defect (LANE_RANK array-position vs tier) that co-produced cycle-858's not-before gate work.** `fe013f22f` decoupled `LANE_RANK` from `FLAT_TASK_LANES` array position into an explicit tier map (`backlog:0,ready:1,in_progress:2,review:3,done:3,qa:4,done_verified:5`) so a legit `done[]->qa[]` drain no longer scores BACKWARD; `done_verified` stays sole terminal. `grep -rn LANE_RANK` = exactly 1 code call-site (line 471, the comparison itself) — matches the review_note's claim.

**Did not trust the shipped 12/12 or the blast-radius narrative — re-derived both independently.** Ran `test-orch-conservation-lane-rank.sh` (12/12), `test-devteam-donelane-drain.sh` (60/60), `test/orch-apply-wrapper-tests.sh` (109/109) live — all matched claimed counts. Built 3 fixtures NOT copied from the diff: (A) 3 done + 1 review batch -> qa in one write, exit 0 clean (independently proves the blast-radius restoration — a legit forward batch no longer aborts); (B) `done_verified[]->qa[]` 2-row stale revert, exit 1 rejected; (C) `review[]->backlog[]` 2-row stale revert, exit 1 rejected (two negative controls on boundaries the shipped suite did not cover, confirming the guard stays armed, not merely disarmed).

**Mutation check: reverted the fix in scratch (parent commit copy), re-ran the shipped suite against it.** 8/12 checks flip red, scoped exactly to AC1/AC2/AC3 (the rows targeting this defect) — NG1/NG2 and the AC3 content-check stay green, since those exercise dimensions the fix does not touch. Confirms the suite discriminates the real defect rather than passing vacuously. Bonus finding: under the PRE-FIX code, my own negative-control fixture B would NOT have been rejected either (old array-position rank scored `done`(5) as "ahead of" `qa`(4), so a `qa->done` revert read as forward, not backward) — the old bug was bidirectional-wrong at this boundary, not just false-positive on the forward direction; the fix corrects both.

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh`, `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified on the live file. `.head` was pinned to this row — reset to idle in a same-cycle follow-up write (`next_action` required `del()`, not `null` — same optional-not-nullable contract as `next_agent`). Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S234.
