# QA — Notebook

## cycle-605 · 2026-08-08 · FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER — Step 5 checkpoint #2, still NOT sign-off-able (soak-clock reset by unrelated rebuild + CI regression triage)

Dispatched to continue PO's Step 5 (`>=4h` `sessionCount`-vs-`MemPerc` soak correlation) plus investigate a CI-red on this fix's own test file. `docker inspect` found the container had been RE-CREATED since checkpoint #1: StartedAt=2026-08-08T19:06:16Z, Image sha256:8966b3b8 (was 16:59:50Z / sha256:630fa5d2). Traced to commit `550fda673` (19:08:23Z) — an unrelated ops rebuild for `FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION` (cycle-604 above) that happens to share this container, which reset this row's soak clock a 3rd time on its own history. Content-verified (docker exec grep, not assumed) the new image still carries `SessionRecord`/`reapStaleSessions`/`stopReaper`/`evictSession`/DELETE route — no functional regression, only elapsed-time loss. Real elapsed uptime at checkpoint: ~59min of the required >=4h.

CI regression triage (router flagged `FIX-CI-BUNTEST-1862C-TRANSPORT-SESSION-EVICTION`, owner dev-mcp-server, not duplicated): ran `1862c-transport-session-eviction.test.ts` myself rather than trust the CI summary. 0/10 fail on a quiet local machine; reproduced 16/20 fail under manufactured CPU contention (48× `yes` on a 12-core box, mirroring `SPIKE_CI-PERFILE-ISOLATION-FLAKE.md`'s method) — always the same assertion, T9 (negative control) `expect(manager.sessionCount).toBe(1)` receives `0`. Root cause: T9's own `setInterval(fn, 5)` activity-bump helper races the test-compressed 15ms idle-timeout/5ms reaper-sweep; under contention the bump callback itself can be delayed past 15ms, so the idle reaper fires first. `git log b746c112b..HEAD -- transport.ts server.ts 1862c*.test.ts` is empty — present since the fix's own origin commit, not a later regression. Production idle-timeout is 15 MINUTES not 15ms (~180000× more jitter margin) — this failure mode cannot occur in the running service; it is a test-harness ms-scale-timer margin artifact, not a product defect in `evictSession`/`reapStaleSessions`.

Live gate checks on the current (post-reset) container: RestartCount=0, OOMKilled=false, `toolCount=183`/`cronJobCount=88` both match baseline (live `/health` + `bun scripts/gen-project-stats.ts` re-run). 3 samples 20s apart show sessionCount/MemPerc moving together (44/12.67% → 44/12.67% → 42/12.65%), `/proc/1/status` VmHWM(507404kB) > VmRSS(440864kB) — reclamation active, not pinned. 43 `idle_timeout` evictions confirmed firing in container logs since 19:06Z, 0 `max_age` (expected — container age ~1h ≪ 4h threshold).

VERDICT: Step 5 still INCOMPLETE — not fabricating a >=4h reading from a ~1h sample. Wrote `qa_step5_checkpoint_2_20260808T2005Z` + corrected `next_recheck_not_before` (21:00Z was stale, computed off the superseded container) to `2026-08-08T23:06:00Z` (4h off the ACTUAL current container start) via `jq`+`orch-apply.sh` — no status/lane/next_agent change, row stays `qa`/next_agent=qa. Recommended PO not sign off Step 6 until that mark; CI regression triaged as environment/test-margin (not code-correctness blocking) but left open on its own tracked row per PO's explicit framing. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S21. `task_release` best-effort per INV-GATEWAY-1 (no gateway binding in this sub-session; dispatcher releases `task:FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` on my behalf). Pruned cycle-602 (`FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 603/604/605.

## cycle-606 · 2026-08-09 · TASK_RUNIDLE-1-AUDIT — CHANGES_REQUESTED (docs-only audit review, `review[]` row, `next_agent:qa`, direct-commit to main, no branch, commits `7e253e0f0`/`81ec46953`/`5aa72cfaf`)

Verified `docs/architecture-briefs/2026-08-09-active-sprints-accumulator-gap.md` (Tier 1 of the FIX-RUNIDLE-PREDICATE-D-ACTIVE-SPRINTS-PERMANENT-FLOOR decomposition) against LIVE `orch-state.json`, not trusted from prose. Most of it holds byte-exact: `closed_sprints[]`=20 (all 4 cited evidence commits real, subjects match); all 8 `active_sprints[]` rows' status/task-status-breakdown/dispatchable-counts/key-counts(7-17)/malformed `...14ZZ` timestamp independently reproduced exactly; GAP-1 steady-state framing, the latent predicate-drift bug (§3.3), and the SPIKE-SATURATED-COUNT-THRESHOLD-GATES-SWEEP / FIX-SPRINT-REGISTRY-DANGLING-IDS dedup reasoning all check out.

Found a load-bearing factual error in the GAP-2 diagnosis. Brief's §4 table marks `SYSREMAKE-P2-STRUCTURAL-REMAKE-ROUTE` as having `tasks[]` "absent (no `subtasks` either — bare pointer row)", `dispatchable:0`, `childless:yes`. Live: the sprint DOES carry a 9-entry `subtasks[]` (T1-T9), and all 9 exist as real board rows — T1/T2 already `done_verified[]` (T1 closed 2026-08-08T18:43:08Z, commit `ad6e422e9`, one day before this brief's own claimed audit date), T3-T9 (7 rows) sit `ready[]` status READY, dispatchable right now. Brief's §4 Notes also claims CCATO-TRUTHGATE-MCP-NATIVE's 8 named `subtasks[]` ids were "searched across every flat lane... zero matches anywhere on the board" — false: 5 of 8 (T3/T5/T6/T7/T8) exist as real `ready[]` READY rows (only T1/T2/T4 are actually dangling), a fact already documented live 3 days earlier at `orch-state.json:9178` (po's 2026-08-06T11:29Z malformed-timestamp finding names all 5 by id). §5.2's causal narrative ("no board-visible existence at all") and §7/§8's recommendations ("GAP-2's two childless sprints have zero dispatchable nested tasks", "both currently-stale sprints qualify today") both inherit this overstatement — SYSREMAKE-P2 is not childless and has active recent work; the real gap is narrower (PM's reactive scan doesn't resolve `subtasks[]` pointers into the flat lanes it doesn't nest).

VERDICT: CHANGES_REQUESTED (round 1) — file:line issues + fix-requested appended to `docs/handoffs/TASK_RUNIDLE-1-AUDIT.md` `[QA] Review Record`. Everything else in the brief verified accurate; scope of requested fix is narrow (re-measure §4's GAP-2 columns + correct §5.2/§7/§8 narrative against live `ready[]`/`done_verified[]`, resolving `subtasks[]` ids instead of asserting non-existence). Blocks TASK_RUNIDLE-2-REDESIGN/3-STALENESS dispatch (both `depends_on: [TASK_RUNIDLE-1-AUDIT]` at DONE) until corrected. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S22. No board-lane/status mutation made by QA — `changes-requested` JUMP-TO does not move lanes itself; router/NEXT:developer handles redispatch. `task_release` best-effort per INV-GATEWAY-1 (no gateway binding in this sub-session). Pruned cycle-603 (`TE-T31`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 604/605/606.

## cycle-607 · 2026-08-09 · TASK_RUNIDLE-1-AUDIT — round-1 re-review, APPROVED (commit `541282b0f`)

Did NOT trust developer's round-1 self-report. Re-resolved all 17 GAP-2 `subtasks[]` ids independently via fresh `jq` against LIVE `orch-state.json` this cycle: CCATO T1/T2/T4 = not found on any flat lane or `docs/data/orch/archive/*.json` (genuinely dangling — the archive only contains unrelated prose mentions of the bare id string, no actual board row), T3/T5/T6/T7/T8 = `ready[]`/READY/`next_agent=dev-mcp-server` (5 dispatchable). SYSREMAKE-P2 T1/T2 = `done_verified[]` with `updated_at` 2026-08-08T18:43:08Z/19:01:23Z and `commit_sha: ad6e422e9` on both (byte-exact to the brief's claim), T3-T9 = `ready[]`/READY (7 dispatchable). Exact match to both round-1's self-report and my own round-0 findings — no residual error.

Byte-diffed §2/§3/§5.1/§6 between commit `7e253e0f0` (round-0) and `541282b0f` (round-1) via anchor-scoped `awk`+`diff` (not trusting the "untouched" claim on prose alone) — all four sections confirmed identical, zero drift. Only §1's exec-summary GAP-2 bullet, §4's two GAP-2 table rows + Notes, §5.2, §7, §8 were edited; §4's 6 non-GAP-2 rows unchanged in diff context. `git diff` on `orch-state.json` confirms a clean `backlog[]`→`review[]` board-row move with no unrelated mutation and no new `active_sprints[]`/`sprint_goal.entries[]` entry minted.

VERDICT: APPROVED. Moved `TASK_RUNIDLE-1-AUDIT` `review[]`→`done[]` (`status:DONE`, `qa_verified_by:qa`) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 758→758, signal_total 41→41). `[QA] Review Record` round-1 appended to `docs/handoffs/TASK_RUNIDLE-1-AUDIT.md`. Task Report: `reports/TASK_REPORT_RUNIDLE-1-AUDIT.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-14.md` §qa-S23. **Flagged caveat (not a blocker):** `scripts/lib/devteam-eligibility.jq` `deps_satisfied()` requires the dep to resolve to `DONE_VERIFIED`, not plain `DONE` — TASK_RUNIDLE-2-REDESIGN/3-STALENESS are logically unblocked (sole dep now terminal) but won't auto-pick-up via BOUNDED-1/design-router until this row is also `DONE_VERIFIED` or PM/PO manually dispatches; out of this task's own scope (instruction was `move to done[]`). `task_release` best-effort per INV-GATEWAY-1 (no gateway binding in this sub-session). Pruned cycle-604 (`FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 605/606/607.
