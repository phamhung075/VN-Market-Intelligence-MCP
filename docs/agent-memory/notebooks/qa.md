# QA — Notebook


## cycle-583 · 2026-08-08 · FIX-EMITSIGNAL-E3-RC1-OPAQUE-FATAL-DROPS-DETECTOR-FINDING — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `bbf3d907f`, 3-way abort-branch classification fix)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Dispatcher flagged this as a subtle 3-way branch-classification fix (validator-abort vs stamping-abort vs conservation-abort), easy to get partially right — verified all 3 ACs against live code, not the commit message. AC-1: `_orch_apply_invoke()` now captures orch-apply.sh stderr (`2>&1`, was previously discarded) into `ORCH_APPLY_STDERR`; `_e3_write_row()` greps `^[orch-apply] (ABORTED|ERROR):` and threads the reason into both the ABORT marker and `_send_orphan_bug_telegram` — confirmed present in the diff. AC-2: `orch-apply.sh:141`'s validator-abort text exact-matches the grep pattern `^[orch-apply] ABORTED: validator exit` → routes to the CAS-retry `continue` lane (rc left at true 1, not overwritten to 2); the stamping-abort (:164) and conservation-abort (:184) branches both lack that string so both correctly fall through to the fatal no-retry `else` — a genuine 3-way split, not a 2-way approximation mislabeled as 3.

AC-3: T32 (validator-abort-then-recovers) + T33 (conservation-abort stays fatal, reason propagates) added as direct siblings of T25/T26. Did not trust the commit message's RED/GREEN claim — checked out `bbf3d907f^`'s `emit-audit-signal.sh` into the live tree myself and reran the suite: 12/123 fail (T32 4/4 fail; T33 2/6 fail, exactly the 2 AC-1 reason-propagation checks) confirming genuine RED at parent; restored the HEAD file (`git diff` clean after) and reran: 123/123 pass, confirming GREEN at HEAD. `bash -n` clean, `shellcheck` only pre-existing unrelated SC1091 info. `mock-guard.sh` PASS (no scannable production TS — bash zone `cross-service/`), `bun test`/`tsc` structurally N/A.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 767→767, signal_total 244→244). Stamped `commit:bbf3d907f` onto the row. Verification text appended to row's own `note` field (row had no `status_note`, unlike siblings this sprint — no handoff file either). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S2 — note: this journal file was hit by a peer full-`Write` mid-cycle (overwrote qa-S1/qa-S17 back to a fresh `qa-S1` header) between my read and append; recovered by re-appending after the peer's write landed, no data of mine lost, but flagging the pattern (matches `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries_20260806` — decision-journal continuation files are as exposed to this race as the notebook was). Pruned cycle-580 this write per AC-2 retention (3-section cap) — kept 581/582/583.

## cycle-584 · 2026-08-08 · FIX-CI-SIZELINT-BCTCREFINED-PROJECTION-BASELINE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `ed0df780d` — corrected from stale/dangling `36fe87c31`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Router-supplied brief cited the dev's own claimed commit `36fe87c31` — did not trust it: `git merge-base --is-ancestor` FAILED, `git fsck --unreachable` confirmed a genuinely dangling object. Traced the real landing commit via `git log --all --grep` on the `Task:` trailer to `ed0df780d` — identical `git patch-id` on both, same author/timestamp/subject, diff byte-identical modulo the commit-hash header (routine rebase-hash-change artifact, same class already seen this sprint at cycle-582 embedder.py). Corrected board `commit` field as part of the lane-move.

AC1: `size-justification: 242L` header present in the mandatory first-10-line window; `wc -l` on the file = 242, exact match. AC2: `git show --stat ed0df780d` = exactly 1 file changed (+8/-0, comment-only) — `size-lint-baseline.json` (the row's 2nd `files[]` entry) genuinely untouched, matching AC2's explicit "do NOT `--update`" instruction — the `files[]` listing is the in-scope-if-needed convention, not a required-touch. AC3 (literal "RC=0 locally") and AC4 (`gh run ... == success`) not met at full-repo/CI-job scope, but this row's OWN file confirmed clear two independent ways: (a) `git worktree add --detach HEAD` clean-tree scan — 1 offender only, `coordinationStore.ts`; (b) live CI `gh run view 31266835487` (headSha=HEAD `b744d509e`) size-lint job log — same single offender, `getBctcRefinedTool.ts` absent from both. A local dirty (uncommitted) peer `transport.ts` also surfaced as a false 2nd offender on the raw working tree — excluded, confirmed peer-WIP pollution via clean-worktree + live-CI cross-check (same pollution class as cycle-581/582). The genuine remaining offender, `coordinationStore.ts`, is already tracked by its own separate row (`FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L`, status `READY`, created 2026-08-08T00:01Z — AFTER this fix commit landed 2026-08-07T01:36Z), so the CI job's continued redness is a rotating-offender artifact of the shared full-repo gate, not a regression this fix caused.

Re-ran RAW, not the dev's self-report: `bun test FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM.test.ts` 7/7 pass, 50 expect() (exact match to claim); `bun tsc --noEmit` 0 errors; `mock-guard.sh --files getBctcRefinedTool.ts` PASS; diff read directly is comment-only (8 insertions, 0 deletions) — DDD/security greps trivially clean, zero behavior change confirmed.

VERDICT: APPROVED, DONE_VERIFIED — this row's own AC1-AC2 fully satisfied; AC3/AC4 satisfied at the row's correct per-file scope (same precedent as cycle-581/582/583 `FIX-CI-SIZELINT-*` verifies this same sprint — a shared full-repo lint job resolved by several independently-tracked per-file rows, not by any single one alone). Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 767→767, signal_total 244→244). Stamped `commit:ed0df780d` onto the row (corrected from dangling). Verification text appended to row's own `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S4 (re-appended after a peer full-`Write` clobbered my first attempt at §qa-S17 — see that file's own note). Pruned cycle-581 this write per AC-2 retention (3-section cap) — kept 582/583/584.

## cycle-585 · 2026-08-08 · FIX-ORCHSTATE-SIGNALQUEUE-UNCOMMITTED-ROWS-LOST-TO-PEER-FULLDOC-WRITE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `941883d76`, commit_sha backfilled from null)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Row carried a dev-team-flagged twist: the developer's own re-investigation reversed the row's original premise (2 signal rows "destroyed") to "genuinely present, never destroyed" — did not trust the note's prose, independently `jq`-queried `docs/data/orch/archive/2026-07.json .signal_rows[]` myself and confirmed both `sys-20260729T103337-13e5`/`sys-20260729T103338-7f95` present today, status READ, all fields byte-matching the row's own original evidence text.

Independently confirmed the new fix is real, not just claimed: `undeclaredSignalRowDrops()` in `scripts/orch-conservation-check.mjs` is a genuine row-identity dimension, unconditionally checked BEFORE the `ORCH_APPLY_ALLOW_SHRINK` bypass path (exit 1, never bypassable) — and genuinely wired into `scripts/orch-cold-evict.sh:1033` via `ORCH_APPLY_DECLARED_SIGNAL_EVICTIONS`, sourced from the SAME already-computed `rm_sig_rows` map used for the actual removal (lockstep, not hand-maintained). Ran both suites live myself (not trusted from claim): `orch-apply-wrapper-tests.sh` 75/75 (new ROW-DROP-REJECTED + ROW-DROP-ALLOW-SHRINK-NO-BYPASS present, PASS), `orch-cold-evict-tests.sh` 53/53 (T9/T10 unaffected). All 3 cited commits (`941883d76`, `441e9ee0e`, `2834187a7`) confirmed ancestors of HEAD via `git merge-base --is-ancestor`.

VERDICT: APPROVED, DONE_VERIFIED — AC-5's "moot" closure is legitimate (not a dodge): the original AC-5 presupposed destruction that the corrected re-investigation disproved, independently corroborated. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 759→759, signal_total 245→245, signal_row_identity=clean). Backfilled `commit_sha` (was null) → `941883d76`. Verification text appended to row's own `status_note` field (had none — no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S6. Pruned cycle-582 this write per AC-2 retention (3-section cap) — kept 583/584/585.
