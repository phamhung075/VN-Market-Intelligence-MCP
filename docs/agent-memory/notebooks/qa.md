# QA — Notebook
## cycle-764 · 2026-08-14T18:00:06Z · FACTORY-DOMAIN-extract-sla-config (apps/mcp-server/, commits `9930ee008`+`4097ffaea`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.commit`/`.files[]` drain fields — both commits derived from the row's own `review_note` prose ("Commits: 9930ee008 (code), 4097ffaea (memory)"), files cross-checked against `git show --stat` directly, not accepted blind. Both commits confirmed real, on `main` ancestry (`git merge-base --is-ancestor`); `git show --stat 9930ee008` matches all 3 claimed files exactly: `apps/mcp-server/src/domain/services/freshnessSlaChecker.ts`, new `apps/mcp-server/src/domain/services/freshnessSlaConfig.ts`, `apps/mcp-server/src/__tests__/234-vps-health-sla.test.ts`. Read the diff directly, not trusted from prose: `SignalType`/`SignalSlaConfig`/`DEFAULT_SLA_CONFIG` moved verbatim (byte-identical, rationale doc-comments intact) into the new sibling `freshnessSlaConfig.ts` (194L, own size-justification header); `freshnessSlaChecker.ts` now imports the 3 names and re-exports them unchanged so every existing import path (barrel, SLA MCP tools, scheduler job, all SLA test files) keeps working with zero call-site changes. Test hunk confirmed: the one brittle literal-substring `234-vps-health-sla.test.ts` AC-9 assertion updated to check the new re-export line, matches claim exactly. `git log --follow` shows one later unrelated commit (`f0831e6be`, bctc_vps_queue terminal-state exclusion) touched `freshnessSlaChecker.ts` after this one — explains current 829L vs the commit's stated 966L→814L, legitimate subsequent drift outside this task's scope, not scope creep in the verified commit itself. Re-ran REAL verification, not trusted from prose: targeted SLA/freshness zone suite (18 files referencing `freshnessSlaChecker`/`freshnessSlaConfig`/`SignalSlaConfig`/`DEFAULT_SLA_CONFIG`, broader net than the row's own claimed 14) 271 pass / 0 fail / 730 expect() calls, including the exact AC-9 assertion. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files` (both touched prod files) exit 0 PASS. `scripts/audits/size-lint-justification.sh --check` (both files) PASS — 0 unjustified offenders. DDD grep hits on both files are docstring text only ("Layer: domain/services — must not import from application/ or infrastructure/"), not actual import statements — zero real domain→app/infra imports confirmed by reading. `process.env`/secrets greps clean. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S7.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:FACTORY-DOMAIN-extract-sla-config` (held by router) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool grant this qa sub-session, best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`).

## cycle-765 · 2026-08-14T18:01:29Z · FACTORY-SCHEDULER-split-bctcReparseJob (apps/mcp-server/, commit `f0a622472`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.commit`/`.files[]` drain fields — commit derived from `review_note` prose (`f0a622472`), single file scope from `backlog-detail.json#FACTORY-SCHEDULER-split-bctcReparseJob`'s own `files[]` claim. Commit `f0a622472eccd0bccbcc1f15e7dd977b7e0db74c` confirmed real, on `main` ancestry; `git show --stat` shows exactly 1 file changed, matching the claim (`bctcReparseJob.ts`, 116 ins/67 del). Read diff directly, not trusted from prose: `MIN_PDF_TEXT_CHARS=100`/`MIN_PDF_CONFIDENCE=0.3` named constants substituted into exactly 4/2 raw-literal occurrences across the Tier 1a/1b/2/3 extraction gates (resolved values byte-unchanged); `processStrandedFeedback(db, rows, reparse, notify, fileExistsFn)` extracted verbatim from `runBctcReparseJob`'s for-loop body, called once, partial counters merged unchanged into `result` — pure mechanical extraction, kept module-private (not exported, no test imports it directly — dev's own scoping claim confirmed true). Re-ran REAL verification on a broader net than dev's own claimed 21 files: all 24 test files referencing `bctcReparseJob` (13 direct import + 4 `mock.module` + 7 string-ref only) → 220 pass / 1 skip / 0 fail / 747 expect() calls. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files bctcReparseJob.ts` exit 0 PASS. DDD: file is `scheduler/financial-reports/`, not `domain/` — pre-existing `infrastructure/` imports (logger, db/schema, fetchers/pdf, notifiers/telegram) out of scope for the domain-import ban, not a violation. `process.env`/secrets grep clean (only false-hit "tokens" inside a comment). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S6 (label collides with a concurrent peer's `FACTORY-TECHANALYSIS-fix-discarded-service-and-port` entry on this same hot-contended file — content intact/unambiguous by task-id, same convention as prior cycles' noted collisions).

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field (row already carried dev's own prose there, no `status_note` field present — same precedent as `cycle-763`/`cycle-764` above); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:FACTORY-SCHEDULER-split-bctcReparseJob` (held by router) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool grant this qa sub-session, best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`).

## cycle-765 · 2026-08-14T18:01:35Z · FIX-CRON-JOB-RUNS-DOUBLE-LOG (apps/mcp-server/, commit `0a73c5b0490d370cc01d28a9040b56f4920c1aac`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `.commit_sha` but no `.files[]` drain field — derived via `git show --stat`, matched exactly against the commit-message file list. Commit confirmed real, on `main` ancestry. Read the diff directly, not `return_summary` prose: `startupHelpers.ts`'s `runPredictionResolutionWithDb` default fn swapped the self-recording `runPredictionResolutionJob()` wrapper call (double `recordJobRun`) for a direct `runPredictionResolution(db)` core call (single `recordJobRun`), and replicated the T4 `shouldSkipRecoveryReplay` dedup guard — already defined in-file, used identically by the sibling `runBctcReparseWithDb`/`runBaseRateComputationWithDb` wrappers — into this wrapper; `predictionResolutionJob.ts` itself untouched, matching the stated sibling-fix file-touch footprint exactly. Re-ran independently: new `FIX-CRON-JOB-RUNS-DOUBLE-LOG.test.ts` 10/10 pass (19 expect() calls); same 9-file related-file regression batch cited in the commit message (1420-cron-health-coverage, 1125/PRED-RESOLVER-GAP-FIX/1154 prediction-resolution, FIX-BASE-RATE-COMPUTATION-CRON-DEAD, FIX-BCTC-REPARSE-DOUBLE-WRAP-DEDUP-GUARD, ARCH-CRON-recover-jitter, FACTORY-SCHEDULER-job-table-registry, plus new file) = 140/140 pass, matches claimed 130+10 exactly. `bun tsc --noEmit` 0 errors. `mock-guard.sh --files startupHelpers.ts` exit 0 PASS. DDD: `startupHelpers.ts` is `scheduler/`, not `domain/` — pre-existing `infrastructure/` imports (recordJobRun, breakers) out of scope for the domain-import ban, zero new import added. `process.env`/secrets grep clean on both touched files. Full 14987-test suite reclaim not repeated — targeted+related-regression scope satisfies the flow's own substitution instruction; claimed 56-fail count sits in the documented pre-existing 52-59 flake band already cross-referenced across 6+ sprint journals. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S4.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=703/candidate=703, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. `.head.active_task_id` was this exact task-id → reset to idle in the SAME write. Dispatcher-wrap lock `task:FIX-CRON-JOB-RUNS-DOUBLE-LOG` (held by dev-team) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool grant this qa sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED again this cycle: file now carries 5 sections under active concurrent-peer write contention (`cycle-764` landed moments before this write) — appended additively only, no existing section touched, matching the same defer-prune precedent `cycle-762`/`cycle-763` already established above; next single-writer cycle should reconcile back to the 3-section cap.
