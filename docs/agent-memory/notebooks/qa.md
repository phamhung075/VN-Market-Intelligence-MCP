# QA — Notebook
## cycle-758 · 2026-08-14T17:37:00Z · FIX-LANCEDB-INSERT-SEGFAULT (apps/mcp-server/, commit `41f522117`+`32279f9a0`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.files[]`/`.owner` drain fields — derived from `review_note` prose (fallback path). Both commits confirmed real, on `main` ancestry; `git show --stat` matches all 4 review_note-claimed files exactly (`insertBctcAnalysis.ts`, `types.ts`, `fetchParseAndStoreBctc.ts`, new `FIX-LANCEDB-INSERT-SEGFAULT-step4-guard.test.ts`). Root-cause reversal claim independently re-derived, not trusted from prose: live `grep -rn "@lancedb/lancedb" apps/mcp-server/src` returns only 2 comment-only hits, zero production import statements — the claimed native-segfault path (superseded by G5b HTTP rewire `d29da3a8d` + `CI-RED-da847805-FIX` `456851797`) is categorically dead today. Diff read directly: the default-inserter resolution line genuinely moved from outside to inside the try/catch in `insertBctcAnalysis.ts`, new `insertAnalysisResolverFn` injection seam threaded through `types.ts`/`fetchParseAndStoreBctc.ts` exactly as described — no scope creep. Re-ran REAL verification: new test file 2/2 pass (matches AC1/AC2 RED→GREEN claim); AC3 regression subset (1181-financial-reports-persist/1352a-async-extraction-race/1352b-pdf-extractor-wiring/p2-f-rag-http-rewire + new file) 33/33 pass, byte-identical to claim; `bun tsc --noEmit` 0 errors; `mock-guard.sh` PASS on all 3 touched production files; secrets/`process.env` grep clean; DDD scan N/A — all touched files are `application/usecases/` (file's own header documents application may import both domain/ and infrastructure/), zero `domain/` layer files in scope. Did not re-run the full 14974-test suite (575s, pre-existing flake band already documented) — targeted regression matches the JUMP-TO's own instruction. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S154.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `review_note` (row had no `status_note` field — appended to the row's existing narrative field instead, same precedent as `TASK-DEVTEAM-IDLE-CHAIN-1-SCHEMA-UTILITIES`); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 712/signal_total 21 unchanged; Stage 1g reported 16 pre-existing unrelated dependency-resolution warnings, non-fatal, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`).

## cycle-759 · 2026-08-14T17:39:00Z · FU-CTG-DISCOVERY-FILENAME-FILTER (vps-scripts, commit `1c9959cdd1`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.files[]` drain field — derived via `git show --stat` (fallback path, matches row's own `return_summary` exactly). Commit `1c9959cdd16e80831c1ae90e6d62e192972e7e60` confirmed real, on `main` ancestry; touches exactly `vps-scripts/discover-bctc-urls-browser.py` + new `test_discover_bctc_filename_classifier.py`. Non-bun zone (vps-scripts, stdlib-only imports — no docker image, host `python3` == VPS deployed runtime, substitution rule N/A). Re-ran independently, not trusted from prose: new suite 15/15 pass, full `vps-scripts/` suite 57/57 pass (matches claim exactly), `py_compile` clean, `mock-guard.sh` PASS, secrets/`process.env` clean. Read diff directly: `is_cover_letter_filename()` checks the resolved PDF's final path segment (post query-strip+URL-decode, case-insensitive) for `cv_cbtt`/`cong_van_cbtt`; wired into `_fetch_pdf_url()`'s href loop so a rejected cover-letter attachment does not short-circuit the scan — later legit hrefs in the same ArticlesFileAttach response still return. Matches AC and the article-613699 repro in the commit message; scope confirmed contained to `vps-scripts/`, no `apps/mcp-server` change needed. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S153.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 712/signal_total 21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. VPS deploy correctly withheld per standing user-gate policy — not a QA blocker (verify-committed JUMP-TO has no merge/push/deploy step, work already on `main`). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`).

## cycle-761 · 2026-08-14T17:38:47Z · FDA-6 (apps/mcp-server, commit `6a6d47e3a`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with `commit_sha` but no `.files[]`/`.owner` drain fields — `files[]` derived from `backlog-detail.json#FDA-6` fallback (matches the row's own `return_summary` claim). Commit `6a6d47e3abb92e519afcb93ed9be868b79f1f47c` confirmed real, on `main` ancestry; `git diff-tree --no-commit-id --name-only -r` matches all 4 non-doc claimed files exactly (`creditFlowTools.ts`, `computeCreditFlowSignal.ts`, `creditFlowAnalyzer.ts`, `DSI-S3-sector-fin.test.ts`) — note: `git show --stat`'s own path-truncation (`...`) gave a false grep-negative on first check, resolved via `git diff-tree --name-only` instead. Re-ran REAL verification, not trusted from prose: `bun test src/__tests__/DSI-S3-sector-fin.test.ts` 27/27 pass; `bun tsc --noEmit` 0 errors; `mock-guard.sh --files` (3 touched prod files) exit 2 CAUTION — pre-existing unrelated commented-out TODO at `computeCreditFlowSignal.ts:230`, not a fabricated-data marker, non-blocking per spec. Read commit body directly: `CreditData.date` now `null` when `mortgageIsEstimate||yoyIsEstimate` (fixes the row's date=now fabrication defect), new `structuredContent` field (`is_estimate`/`source_tier`/`estimated_fields`/`mortgage_is_estimate`/`yoy_is_estimate`/`fully_estimated`/`current_date`/`previous_date`) added — matches `return_summary` claim exactly; VN prose claimed byte-identical (not independently diffed, additive-key-only change, low risk). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S2.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note` (row had none — had `return_summary` instead, left untouched); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 712/signal_total 21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Dispatcher-wrap lock `task:FDA-6` (held by dev-team) cleared via this board write per spawn-prompt instruction — no explicit `task_release` call made (best-effort/dispatcher responsibility, INV-GATEWAY-1). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`).

## cycle-760 · 2026-08-14T17:38:03Z · FIX-COWORK-SUPERSEDE-MUTEX-SCRIPT-AND-MATCHSLOTS-WIRING (scripts/agents-flow/, commit `662d1fcc3`+`87e0554db`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Router-dispatched (`mode=verify-committed`), row already RAW-verified by router (commits exist/pushed, file list matches, 4 suites re-run clean) — independently re-derived from scratch per this flow's own standard, not trusted from the router's note. Both commits confirmed real, on `main` ancestry; `git show --stat` matches all 5 claimed files exactly (new `cowork-supersede-mutex.js`+`.test.js`, modified `cowork-match-slots.js`+`.test.js`+`cowork-schedule-consistency.test.js`). AC-1 through AC-6 checked against the actual diff/source, not prose: `applySupersedeMutex` reads only `.supersedes` off scheduleSlots, drops named victims present in `matches`, self-ref guarded, order-preserving (AC-1); `fail()` envelope byte-diffed identical in shape to `cowork-chef-mutex.js`'s own error contract (AC-2); 5 brief-named cases present as TC-1..TC-5, all pass (AC-3); `grep -n "finish("` confirmed all 3 `matchSlots()` return points (legacy/legacy-fallback/adaptive) route through the shared `finish()` tail which chains `applySupersedeMutex` after `applyChefMutex` unconditionally (AC-4); wiring diff shows in-process `require()`, never a shell pipe (AC-5); AC-6 static assertion (TC-8..11) passes against the LIVE `cowork-schedule.json`, which the already-merged sibling row (commit `5918c55fe`) wired `market-watcher-eod.supersedes=["market-watcher-offhours"]` into — confirmed via `jq` this row's actuator is genuinely no longer inert. Re-ran all 4 test suites independently: `cowork-supersede-mutex.test.js` 32/32, `cowork-match-slots.test.js` 69/69, `cowork-schedule-consistency.test.js` 13/13, `cowork-chef-mutex.test.js` 25/25 confirmed byte-identical/untouched (`git log -1` predates this commit). `mock-guard.sh` PASS on both touched production files. `tsc --noEmit` N/A — `scripts/agents-flow/` sits outside `tsconfig.json`'s `include` (plain Node zone, confirmed by reading tsconfig directly, not assumed). Scope check: `git show --name-only` on both commits confirms zero touch to `cowork-chef-mutex.js`/`.test.js`, `docs/data/cowork-schedule.json`, `docs/agents/**`, `orch-state.json` — all non_goals honored, out-of-scope work correctly lives in the sibling commit. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S154.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 712/signal_total 21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`). Notebook journal shared file `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` showed a live concurrent-peer step-number collision at `qa-S154` (a different peer QA session's `FIX-LANCEDB-INSERT-SEGFAULT` entry landed at the same step label mid-write) — fixed the local interleaving artifact (one stray duplicated line) without touching the peer's actual content, renumbered the peer's header to `qa-S155` to de-collide.
