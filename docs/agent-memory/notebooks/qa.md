# QA — Notebook

## cycle-412 · 2026-07-09 · FACTORY-FRONTEND-split-dashboard-analysis — Step 5 PASS, routed to po

Step 5 after dev-frontend split 1836L `dashboard.analysis.tsx` into 5 pure formatters (`app/domain/formatters/*`) + 22 presentational components (`app/components/analysis/*`, all ≤120L) — route down to 476L (19 commits a5e629411..41279090e) — direct sequel to FACTORY-FRONTEND-extract-computeDecision (cycle-411/qa-S32). ops's Steps 1-4 (image sha256:2135c729b9a9, container healthy, `git_sha` label=4c4c59f3f).

Independently confirmed, not trusted from ops's/dev-frontend's prose: (1) `docker inspect` — live image=sha256:2135c729b9a9 exact match, container healthy. (2) `verify-deploy-sha.sh frontend` reads drift (deployed=4c4c59f3f vs current HEAD=b907a8ea6) — investigated: `git diff --stat` between the two touches only orch-state.json/notebook/journal, zero `apps/frontend/` — benign doc-only post-build drift (same class as cycle-411/qa-S32, memory `project_verify_deploy_sha_benign_doc_drift`); deployed image genuinely carries the reviewed code. (3) Live curl content checks on both routes (not bare 200s): base `/dashboard/analysis` — Watchlist grid real counts, KinhDichMarketPanel+MacroSignalPanel both render real data (zero "Không có dữ liệu" fallback), StockDetailPanel/AiDeepDivePanel correctly absent (no stock selected). `?stock=VNM` — decision panel renders "GIỮ" reasons "KD: GIU (tich cuc)"+"Giá +1.3% (5 phiên)" — hand-recomputed (KD no-match=0, price+1, score=1 in [HOLD_SCORE=-1,BUY_SCORE=2) band) matched exactly; InfoSourcePanel/MacroImpactPanel/StockDetailBottomGrid(Kinh Dịch+MiniPriceTable)/AiDeepDivePanel (real brief sections, not 404 fallback) all present with real content, zero error-boundary/crash markers. (4) Full vitest re-run myself: 2047 pass/2 fail (QUE-TOOLTIP/QUE-REFERENCE, confirmed pre-existing+unrelated — zero commits in the extraction range touch either file). tsc 0 errors. eslint: same 5 pre-existing `react-hooks/exhaustive-deps` config errors in CorporateEventsZone/FinancialsZone/TechnicalZone (confirmed zero diff on those files in range). (5) Playwright `render-check.spec.ts` (G12) run directly against the LIVE deployed container at :3001 (not an isolated dev-server port) — 3/3 pass, stronger signal than an isolated-port run since it exercises the actual production artifact.

Cosmetic (non-blocking, carried to po): route header + review_note + ops's own note all say "457L"; actual `wc -l` = 476L — stale count predating the header comment itself (ops already flagged this too).

VERDICT: PASS. Board `task_board.review[]` row stays `status:"REVIEW"`, `next_agent` qa→po, `qa_verify_at`/`qa_verify_by`/`qa_note` added (dev-frontend's `review_note`/ops's `ops_note` untouched) via `jq`+`scripts/orch-apply.sh`; `.head` synced same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S33.

## cycle-413 · 2026-07-09 · FACTORY-MACRO-split-sandbox — Step 5 PASS, DONE_VERIFIED (no ops step)

Step 5 after dev-macro-indicators split `cmd/sandbox/main.go` (831L, no header) into main.go(103L)/discovery.go(86L)/primitives.go(366L)/module.go(250L) + collapsed 6 `executeMacroXxx` comparators into one `compareFields` helper (60c9a880a) + board/journal/notebook writes (c4194ebdc). Router had already RAW-verified commits/build/vet/test/lint/line-counts/`.gitignore` root-cause/Dockerfile-scope/atomic head-sync — my job was the parts a router-level check can't cover.

Read the full diff against the reconstructed pre-split `main.go` (`git show 60c9a880a^:...main.go`) line-by-line: `compareFields`/`formatVal` genuinely preserve the original per-field `%q`(string)/`%v`(number) formatting + exact error text for all 6 primitive comparators AND both module-tier diff sites (`executeMacroSignalsBatch` loop, `executeMacroSignalsBuild` 19-field compare) — no silent logic change. Did not trust the dev's git-stash before/after report — reproduced independently from scratch: `git worktree add` at the parent commit, built both old and new sandbox binaries, ran both against the identical live 20-scenario fixture suite (`docs/scenarios/macro-indicators/**`) — byte-identical stdout (timestamps stripped) + exit 0 both. Constructed my own 2 forced-mismatch probes (primitive: score 8→999; module: yieldSpread.label CHEAP→__FORCED_WRONG__) and ran both binaries — byte-identical FAIL text + exit 1, both before and after. Read `dispatch_test.go` in full (8 tests, real dispatch-routing assertions, not tautological) — found one real but non-blocking limitation: the 6-way primitive subtest uses `shouldPass=false` (always true/nil) for all 6 cases, so it can't catch a within-group case-label swap via `go test` alone (backlog-detail.json DoD explicitly asked for "light coverage" only, not exhaustive — real fixture suite would catch it). Repo-wide grep for every moved identifier confirmed zero cross-package references (`package main`). Full zone suite (`go build/vet/test -count=1` all 11 packages + `golangci-lint run ./...`) green beyond cmd/sandbox-only.

VERDICT: PASS, no genuine defect. Lane decision: task instructions suggested `review[]`→`done[]`+`next_agent:po` citing `a6c10eab5` (frontend, which needed an ops Docker Close Gate + separate po Step-6) — but this task has NO ops step (cmd/sandbox not deployed). Found the closer same-zone precedent `1938f5c6d` (`FACTORY-MACRO-delete-dead-ts-tree`): qa flips `review[]`→`done_verified[]` directly + `.head` reset (`status:"done"`, `next_agent:"router"`) when no ops step is needed. Followed that precedent via `scripts/qa-factory-macro-split-sandbox-done-verified.jq`+`scripts/orch-apply.sh` (single transform, no head/board desync). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S34.

## cycle-414 · 2026-07-09 · FACTORY-MACRO-split-repositories — Step 5 PASS, routed to po

Step 5 after dev-macro-indicators split `pkg/infrastructure/repositories.go` (905L, 6 adapters) into 5 per-adapter files sharing a new package-level `openReadOnly(dbPath)` helper (c3962350d) and ops's Steps 1-4 (rebuild GIT_SHA=9d712bdad..., container healthy, SHA-gate PASS at build, 3f8515d46). Unlike sibling `FACTORY-MACRO-split-sandbox` (cycle-413), this IS the live-deployed `cmd/server` service — full 6-step Close Gate applies. Ops's own journal already cites a `/snapshot` probe under a "Step 5" label; treated those numbers as a claim to independently re-derive, not relay.

Independently confirmed, not trusted from ops's report: (1) `docker inspect` — container healthy since 07:21:18Z, `git_sha` label exact match. `GET /health`=200. (2) `verify-deploy-sha.sh macro-indicators` now reads drift (deployed=9d712bdad vs current HEAD=5b326844c) — investigated: `git diff --stat` touches zero `apps/macro-indicators/`, and `git merge-base --is-ancestor c3962350d 9d712bdad`=true (refactor commit genuinely baked into the deployed build) — benign doc-only drift, same class as qa-S32/S33 (memory `project_verify_deploy_sha_benign_doc_drift`). (3) Re-ran `POST /snapshot` myself TWICE 3s apart (not copying ops's numbers): vnIndex ticked 1841.29(ops)→1842.46→1843.08, `dataSource:"live"`; oilUsd/goldUsd also moved slightly between my 2 calls — proves genuinely live-computed, not frozen/cached. usdVnd/vndDepositRate/fedFundsRate/earningYield/carrySpread byte-identical to ops's report on both my calls (expected: daily-fixing fields, sticky vs intraday VN-Index) and internally consistent with the response's own embedded reasoning strings. All 8 fields non-null/non-zero/plausible. (4) Tool/route surface unaffected: `git diff c3962350d^..c3962350d -- pkg/interface/http/router.go` = EMPTY — route-registration file untouched by the refactor; live router still exposes exactly `/health`+`/snapshot`+6 optional VMT routes. (5) Source-level: `repositories.go` gone, 5 new files at exact claimed sizes (110/159/283/141/335L), `openReadOnly` is a single package-level func called from all 5 adapter files (grep-confirmed); `cmd/server/main.go` composition root wired to all 6 constructors from the split files. (6) Fresh `go build/vet/test ./... -count=1` myself: all 11 packages green. (7) `docker logs` since start: zero error/panic lines.

VERDICT: PASS. Board-status precedent check: row's `status` was `"QA"` (canonical enum, set by ops). Checked `git show a6c10eab5` (qa-S33's own commit, sibling Close Gate) — qa did NOT touch `status` there either (stayed `"REVIEW"`); cross-checked 10 po-S4..S15 journal entries all describing "Step 6 (po-only DONE_VERIFIED)" as po's exclusive terminal-flip authority (po-S3 adjudicated a prior qa-self-close as a genuine, forgiven deviation). Established convention = never touch `status` myself, only flip `next_agent`. Left `status:"QA"` untouched; added `qa_verify_at`/`qa_verify_by`/`qa_note`, flipped `next_agent` qa→po (board row AND `.head.next_agent`, same atomic write) via `jq`+`scripts/orch-apply.sh`. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S35.

**Retention note:** cycle-411 (FACTORY-FRONTEND-extract-computeDecision) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file.

## cycle-415 · 2026-07-09 · FIX-CLOSEGATE-STEP4-ATOMIC-HANDOFF-SCRIPT — code/design review, CHANGES_REQUESTED

Non-Docker code/design review (doc/script-only, ops Close Gate deliberately skipped per `b80d14d39`). `scripts/ops-closegate-handoff.jq` itself PASSES all brief §5 DoD checks — verified myself via 3 fresh scratch-copy scenarios (row+head match/head-elsewhere-untouched/row-absent→`error()`, plus edge-case variants) against a copy of the live `orch-state.json`, zero hardcoded task-id/lane literals (grep-confirmed), zero new `orch-validate.mjs` schema violations on the candidate output (123 pre-existing warnings unchanged, exit 0).

Found a real, reproduced blocking defect the brief's own DoD didn't cover but this review's dispatch explicitly asked to check: `docs/protocols/docker-deployment-runbook.md:124`'s invocation example is embedded in a markdown table cell with the required shell pipe escaped as `\|` (necessary GFM table syntax) — as RAW TEXT (i.e. exactly what the `Read` tool / any text-consuming agent sees, ops's actual audience), the literal `\|` survives, breaking the copy-paste: bash treats it as an escaped literal character, jq receives `|`/`bash`/`scripts/orch-apply.sh` as extra positional input-file args, errors (`Could not open file |`), and dumps the full JSON to stdout instead of piping to `orch-apply.sh` — the atomic write never applies (though loud/non-zero-exit, not silent; live file confirmed untouched in my repro). The `.jq` script's own header comment and the architecture brief §2.1 both give the SAME invocation unescaped and are correctly copy-paste-safe — only the runbook's table-embedded copy is broken; no other table cell in `docs/protocols/*.md` embeds a raw shell pipe, so no in-repo precedent for how to handle it correctly.

VERDICT: CHANGES_REQUESTED. Did NOT flip the board row — left `FIX-CLOSEGATE-STEP4-ATOMIC-HANDOFF-SCRIPT` exactly as found (`status:REVIEW`, `.head` untouched, still pointing at this task/qa) per the FAIL-path instruction; no `orch-state.json` write this cycle. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S36.

## cycle-416 · 2026-07-09 · FIX-CLOSEGATE-STEP4-ATOMIC-HANDOFF-SCRIPT — RE-REVIEW, PASS, DONE

Re-review of my own cycle-415 CHANGES_REQUESTED. Developer moved the Step-4b invocation out of the GFM table cell into a fenced ```bash``` block below the table (commit `690cb304e`, +7/-1L doc-only); cell prose now just points at the block. Re-verified by ACTUALLY TESTING, not visual inspection: extracted the invocation raw via `sed -n '131p'` (not retyped) — confirmed a real unescaped `|`. `grep -n '\|'` whole-file: zero matches. Table (9 raw lines, code-span-aware parse): 4 pipes/3 cols consistently every row — parses as valid GFM, unchanged shape from pre-fix. Ran the extracted invocation `bash -x` against a scratch `orch-state.json` copy (`ORCH_APPLY_LIVE_FILE_OVERRIDE`, live file md5 unchanged throughout) with a fake task_id: genuine 2-stage shell pipe confirmed in the trace (jq and bash as separate commands), jq's own `error()` fired (task_id not found — expected/fine for a fake id), orch-apply.sh received empty stdin and cleanly aborted exit 3 — NOT the old exit-2 pipe-parsing failure. For contrast, reproduced the pre-fix `\|` shape on the same scratch file: exact old bug reproduced verbatim (`Could not open file |`/`bash`, exit 2) — confirms the fix genuinely resolves what was caught. Bonus: full positive-path end-to-end (synthetic matching row+head) completed successfully, both writes landed, `orch-validate.mjs` clean. `scripts/ops-closegate-handoff.jq` confirmed untouched since `b80d14d39` (already PASSED cycle-415).

VERDICT: PASS. Terminal-lane call: `type:FIX`, genuinely no ops step applicable (doc-only, zero `apps/*` touched) — closer precedent is `FIX-SPRINT-GOAL-STATUS-DRIFT-EVICT` (qa closes `review[]`→`done[]` directly) over the FACTORY `done_verified[]`+router pattern (cycle-413). Dry-ran the transform on a scratch copy first (0 unrelated rows/fields touched, coherence-warnings unchanged 123→123) before applying live: `task_board.review[]`→`done[]` (status DONE, qa_verdict PASS, completed_by qa) + `.head` idle-reset (`active_task_id:null`, `next_agent:"router"`) in one `jq`+`scripts/orch-apply.sh` write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S37.

## cycle-417 · 2026-07-09 · FIX-CLOSEGATE-STEP4-COMMIT-JOURNAL-DISCIPLINE — PASS, DONE

Reviewed agent-father's doc/skill/memory landing (`6f054ea3a`) + router's board handoff (`f511c1d39`) against brief §2.2+§2.3 and §5 DoD items 3-4. `git show --stat` confirmed exact file counts on both commits (8 and 1 respectively); `git branch -r --contains`/`git log origin/main -1` confirmed both on origin (`f511c1d39` IS origin/main HEAD). `grep "^## STEP ops-S"` on `sprint-SYSTEMIC-REMAKE-P1-ops.md` confirmed S1-S6 fully sequential; diffed the commit's parent to independently reproduce the claimed duplicate-S2 renumber fix. All 3 offender files confirmed genuinely deleted (`test -e`/`git ls-files`/repo-wide `find`, all empty). Read the runbook's new § Step 4/4b Commit-Gate Invariant + `.claude/skills/commit-boundary/SKILL.md`'s new `ops` zone row directly against §5 DoD items 3-4 text — both literally satisfied.

Real but non-blocking gap found (dispatch explicitly asked me to check this): grepped `docs/agents/ops/{init.md,handlers.md,flow/docker.md}` for any reference to the runbook/Close-Gate/Commit-Gate — zero hits. The new invariant has NO direct hook into ops's own bootstrapped knowledge or flow catalog; enforcement depends entirely on the pre-existing dispatch-prompt citation chain (`developer/flow/microservice-main.md:162`, `po/flow/sprint-signoff.md:13` both already cite the same runbook § heading the new subsection nests under). Judged acceptable for THIS task's literal DoD scope (§5 items 3-4 only require the doc text) and consistent with the cited precedent (agents-architect's Brief-Commit Invariant is also doc-only/self-attested, not code-gated) — flagged in the board `qa_note` for a possible future ops-flow-wiring hardening task, not treated as blocking.

VERDICT: PASS. Flipped `task_board.review[]`→`done[]` (status DONE, qa_verdict PASS, completed_by qa, qa_reviewed_at, qa_note with full findings) + `.head` idle-reset (`active_task_id:null`, `next_agent:"router"`) in one `jq`+`scripts/orch-apply.sh` write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S38.

## cycle-418 · 2026-07-09 · FACTORY-DOMAIN-split-cascade-engine — Step 5 PASS, routed to po

Step 5 after dev-mcp-server split `cascadeEngine.ts` (3739L→779L) into `cascade/{rules/*.ts (10 files), macroAdjustments.ts, comboDetectors.ts}` barreled via `cascade/rules/index.ts`, and ops's Steps 1-4 (container `vn-market-intelligence-mcp-mcp-server-1` rebuilt, commit `acc249eda5`, `git_sha` router-verified `f5b9b1f9f9bc88a7ddfbd43071a99a6949733f09`). MCP gateway interactive connector pending-approval this session (dispatch note) — used the `scripts/agents-flow/mcp-call.sh` direct-HTTP bridge for every tool call; confirmed working on the first call.

Independently confirmed: (1) `docker inspect` — `git_sha` label exact match, healthy; composite `/health`=200 all 9 services ok. (2) `run_impact_chain` called live via the bridge with a sector-rule-triggering headline ("gói hỗ trợ", "quỹ bình ổn") — real 24-entry causal chain incl. 4 domain hits with the EXACT unique title strings from the split `cascade/rules/sectorRules.ts` ("Chính phủ can thiệp hỗ trợ thị trường — bullish reversal catalyst..." etc. across securities/banking/real_estate/retail) + 19 correctly-attributed watchlist actions + Kinh Dịch enrichment; each domain entry also carried a live `[Macro: goldUSDPerOz>4000=4116.7 → -0.06 securities]` line proving the split `cascade/macroAdjustments.ts` is wired against a real live gold-price fetch. (3) Second probe (war+stimulus combo text) reproduced sectorRules.ts's documented policy-intervention-override (bullish response wins over co-occurring "chiến tranh") — exercises `findKeyword`/`detectPolicyInterventionCombo` in the split `cascade/comboDetectors.ts`. Traced `cascadeEngine.ts`'s own import sites (line 213 `SECTOR_RULES` loop, line 327 `detectPolicyInterventionCombo`) beforehand to confirm both genuinely resolve to the new split files, not a stale inline copy. (4) 2 peer tools spot-checked for collateral rebuild regression: `get_market_snapshot` (real live VN-Index 1840.70/-0.70%, breadth 91/203/66) and `get_alerts` (20 real live alerts, incl. an unrelated steel-sector-rule firing on HPG) — both real/non-error.

Non-blocking observation: `MSCI_INCLUSION_RULES`/`MSCI_WATCHLIST_RULES`/`MSCI_EXCLUSION_RULES` (also part of the split rule-file set) are currently NOT consumed by the live MSCI-detection path (`cascadeEngine.ts` calls `detectMsciInclusion`/etc. from a separate, non-split `msciDetector.ts` with its own inline keywords) — a pre-existing wiring gap outside this split's Step 1-3 scope (dev's own review_note already scoped this), flagged for po/backlog visibility, not a Step-5 blocker; chose the (genuinely live) sector-rule hit instead per the dispatch's own "e.g." wording.

VERDICT: PASS. Board `task_board.review[]` row stays `status:"REVIEW"`, `next_agent` qa→po, `qa_verify_at`/`qa_verify_by`/`qa_note` added; `.head` synced same atomic `jq`+`scripts/orch-apply.sh` write (guarded on `.head.active_task_id` match, dry-run rehearsed on scratch first, 123→123 coherence warnings unchanged). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S39.

## cycle-419 · 2026-07-09 · FACTORY-SCHEDULER-split-dataAuditJob — Step 5 PASS, routed to po

Step 5 after dev-mcp-server split `dataAuditJob.ts` (1300L→353L) into `audit-checks/*.ts` (19 files) + `dataAuditShared.ts` (`7b62f73e7`), ops's Steps 1-4 self-reported (`STEP ops-S8`, container recreated healthy 9s). Did not trust the self-report — independently re-derived everything against the live container.

Actually invoked the REAL `runDailyAudit`/`runWeeklyAuditWithDb` entry points inside the running container against the live production DB (not a scratch copy), injecting a no-op `telegramFn` (avoid a manual-trigger Telegram alert masquerading as a real scheduled one) + a future-clock `nowMsFn` (bypass the T4 recency dedup guard only — DB timestamps stay real, all SQL `datetime('now')`). Daily: 40 findings, zero throw. Weekly: 46 findings, all 7 W-checks fired incl. `checkDuplicatePriceHistory`'s >50%-delete safeguard correctly aborting a 41566/41684-row dedup (pre-existing safety behavior, verbatim-preserved). `cron_job_runs` shows fresh `success` rows for both `dataAuditJob:daily` and `dataAuditJob:weekly` under the new module layout. Re-derived toolCount=183 myself. MD5-diffed all 19 `audit-checks/*.ts` + `dataAuditJob.ts` + all 3 scheduler-wiring files (container vs git HEAD) — 100% byte-identical (stronger proof than a grep-count proxy, whose exact original command isn't documented/reproducible — my `cron.schedule` greps gave 2/5/8 depending on scope, none matching the historical "=3"). Targeted 12-file suite: 106 pass/0 fail. tsc/DDD/security/mock-guard: all clean.

Found a real, non-blocking process gap: `scripts/verify-deploy-sha.sh mcp-server` (ran myself) → EXIT 1, deployed label="unknown" (Dockerfile ARG default) — ops's own `STEP ops-S8` journal has NO SHA-gate row at all (every prior S1-S7 entry does), meaning ops rebuilt without `--build-arg GIT_SHA=...` this occurrence. Different from the known benign-doc-drift FP class (label is literally "unknown", not a stale-but-real SHA). Chose NOT to block: the MD5 content-hash evidence is direct, stronger proof the served code is correct — none of the dispatch's 3 block conditions (red tests/live-invocation errors/wrong findings) apply. Flagged to BUG channel + this entry as an ops Step-4 process finding, not re-patched by qa.

VERDICT: PASS. Board `task_board.review[]` row stays `status:"REVIEW"`, `next_agent` qa→po via NEW `scripts/qa-closegate-handoff.jq` (generalized, parameterized task_id/from_lane/next_agent/now, zero hardcoded literals — qa-side counterpart of `scripts/ops-closegate-handoff.jq`, replacing the one-off `qa-factory-*-step5.jq` per-task files); `.head` synced same write (guarded on `.head.active_task_id` match). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S40.

## cycle-420 · 2026-07-09 · FACTORY-SCHEDULER-split-intelligenceCycleJob — Step 5 PASS, routed to po

Step 5 (15-min cron hot path) after dev-mcp-server split `intelligenceCycleJob.ts` (1381L→975L) into `intelligenceCycle/{types.ts,marketHours.ts,defaults/*.ts×9}` (`0e1e48dad`), ops Steps 1-4 (`c7d88ac60`). `docker inspect` label (`0b6d97bf3`) mismatched current HEAD at first look — investigated instead of assuming drift: `git diff --stat 0e1e48dad..c7d88ac60 -- apps/` empty (all intervening commits doc/orch-state-only), `0e1e48dad` confirmed ancestor of the deployed label → benign doc-drift (`project_verify_deploy_sha_benign_doc_drift`, 4th occurrence), re-gated pinned to the real build SHA → EXIT 0. `:3000/health` hit myself: 200, toolCount=183; composite `:4000/health`: 200/9-ok.

`docker logs` since restart: zero hits for intelligenceCycle/hexagram/import-error grep. No manual-trigger tool exists for this job (checked all `*DebugTrigger*` files — news/price/sbv/foreignFlow/bctc only) — waited for the natural 15-min tick instead of skipping the check: polled `cron_job_runs`, tick fired 13:00:01→13:02:05 post-rebuild, `status:"success"`, `error_msg:null`, duration 124917ms matching the pre-rebuild historical ~123s profile exactly; log line `[intelligence-cycle] cycle complete ... errors:0 mode:"reduced"` clean. Read `defaultComputeHexagrams.ts` (146L) directly: `_lastHexagramComputedAt`+`resetHexagramCooldown`+`defaultComputeHexagrams` co-located, cooldown-gate logic intact, repo-wide grep confirms no scattered duplicate state — invariant NOT broken. Live `import()` of the running container's module: `isMarketHours`/`resetHexagramCooldown`/`runIntelligenceCycle` all resolve as real callables (not just present in source), `isMarketHours` correct across 5 timestamps (open/close boundary/before-open/after-close/weekend). Host-side: targeted test file 25/25 pass, `tsc --noEmit` clean.

Non-blocking: a 3rd writer-guard OHLC rejection (GEX 2025-05-13) observed in my own live-tick log window, same unrelated `ohlcvWriteService` class ops already flagged (CTD/ELC) — not caused by this split. Flagged (not fixed) ops's ops.md notebook-commit gap this cycle per dispatch, on record.

VERDICT: PASS. Board `task_board.review[]` row stays `status:"REVIEW"`, `next_agent` qa→po via the EXISTING `scripts/qa-closegate-handoff.jq` (reused, not re-authored); `.head` synced same write (guarded on `.head.active_task_id` match). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S41.

## cycle-421 · 2026-07-09 · FACTORY-PDF-split-generic-md-table — Close Gate PASS, DONE_VERIFIED (no ops-adjacent step)

Close Gate after dev-pdf-extractor split the 4111L `generic_md_table_extractor.py` god-file into `infrastructure/generic_md_table/*.py` (8 files) behind a 164L thin re-export shim (8 sequential commits `21686062b..f261bd4b6`), ops's Steps 1-4 (`8162ce433`/`530ab8fa0`, image sha256:131d16bdfba5, container healthy, `/health` 200 `ocr_source_ok:true`).

Independently confirmed, not trusted from ops's/dev's prose: (1) `docker inspect` — running image sha256:131d16bdfba5 exact match. `docker exec` into the container: all 8 split submodules present + compiled (`.pyc` for both cpython-312 AND cpython-313 — proves genuine runtime import, not dead files), shim confirmed 164L; `python3 -c "from infrastructure.generic_md_table_extractor import GenericMdTableExtractor, build_document_map"` resolves live, `GenericMdTableExtractor` genuinely IS `infrastructure.generic_md_table.extractor.GenericMdTableExtractor` — zero import errors. Full container log scan since boot: zero error/traceback/exception lines. (2) Behavioral parity — pulled 2 real BCTC PDFs out of the running container (FPT Q1-2026, HPG Q4-2025), rasterized real pages at 200 DPI (PyMuPDF, matching prod), ran `GenericMdTableExtractor.extract_md_tables()` from the current repo (identical to deployed image, `wc -l` byte-count-matched) vs a `git worktree` of the pre-split 4111L god-file — output JSON byte-for-byte IDENTICAL both PDFs (FPT 12 tables, HPG 7 tables, real VND balance-sheet/income-statement/cash-flow figures, md5-matched). Also ran the extractor live INSIDE the running container itself via `docker exec` (real production `rasterize_report()` on a container-resident PDF, not a host mirror) — same 12-table structure, zero exceptions; cleaned up the resulting `/data/bctc-page-images` test artifact afterward (chose in-process/docker-exec invocation over the live `/extract-md-tables` HTTP route specifically to avoid its fire-and-forget MCP push writing synthetic data into the production DB under a fake report_id). (3) pytest — found `c2069debd` (used for the dev's AST symbol-parity claim) is NOT the right pre-split baseline for a pytest A/B: 20 unrelated pdf-extractor commits sit between it and Stage-1. Isolated the TRUE immediate pre-split parent (`ca5a8a545`, `21686062b^`) instead: pre-split = 11 failed/1017 passed/1 skipped; post-split HEAD re-run = 11 failed/1017 passed/1 skipped, IDENTICAL failing test-ID set both times (all pre-existing env-only: page_rasterizer×4, ocr_backends PIL-passthrough, ocr_unit_tesseract_retry×2, extract_md_tables_fpt×2, extract_tables_bt3d_real_ocr, bt3_fix3_row_fidelity). One earlier post-split run showed a 12th failure (`test_extract_layout_and_tables_raises_on_timeout`) — reproduced GREEN on re-run, confirming the dev's own documented timing-flake, not a regression.

VERDICT: PASS, zero regressions found. Lane decision: no ops-adjacent po review step required for this pure infra-split (task instructions explicitly directed review→done_verified) — followed the established same-service precedent (`qa-factory-pdf-paddleocr-score-07-mask-done-verified.jq`): new checked-in `scripts/qa-factory-pdf-split-generic-md-table-done-verified.jq` flips `task_board.review[]`→`done_verified[]` + `.head` idle/router reset in one `jq`+`scripts/orch-apply.sh` write (dry-run rehearsed on scratch first, 124→124 coherence warnings unchanged, guarded on `.head.active_task_id` match). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S42.

## cycle-422 · 2026-07-10 · TASK-W5-FIX-BCTC-BANK-SUMMARY-MAPPING-CTG-CARRY-FORWARD — APPROVE substance, HELD REVIEW (DJ-GATE-1)

dev-mcp-server landed this in review (not self-closed) reporting 5/6 AC PASS + 1 escalated (AC-TRACK1-3: CTG's carried-forward 451 orphaned rows have zero balance_sheet section, scalars stay garbage). Did not trust the self-report — RAW-verified live via `docker exec` (readonly SQLite handle, no writes) against the named-volume DB: target/source both 451 rows, identical 208 income_statement+173 cash_flow+70 notes breakdown, zero balance_sheet — exact match to claim. CTG scalars confirmed unchanged garbage (`total_assets=0`, `net_revenue=3910`, `net_margin_pct≈229157%`, `refine_status=PARTIAL`). VCB/FPT/VNM confirmed untouched (values match claim exactly). report_id freshness re-confirmed current. `bun test` on the new migration script: 8/8 pass (matches claim) — the in-memory integration test proves the reflow codepath itself is correct when balance-sheet data exists, corroborating the live PARTIAL result is a genuine data gap not a script bug. `tsc` clean, `mock-guard.sh` PASS.

Went further on the escalation itself (not just trusting "root defect deeper than scope"): queried every orphaned `bctc_table_rows.report_id` with no `financial_reports` match (5 total, incl. architect brief's RISK-3 "4 unidentified" orphans) to check whether a cheap in-repo balance-sheet rescue for CTG was sitting unclaimed. Two are VCB (confirmed via `bctc_refined_units.markdown` header text). The other two (`4316f6d1`/`d6f1885f`) DO have `balance_sheet` rows, but use a "Tài sản ngắn hạn" current/non-current split — the standard NON-BANK layout, structurally incompatible with CTG's bank-format BS — different, unidentified ticker, not CTG. No hidden fix available; the escalation is real and correctly left unfabricated (no-fake-data policy).

Found an independent, blocking-but-non-code gap the dev's own report didn't surface: DJ-GATE-1 requires a decision-journal entry stamped `task-id:** <this task>` before any REVIEW/DONE flip (dev-mcp-server's own flow line 164 states this explicitly) — grepped every `sprint-FIX-BCTC-BANK-SUMMARY-MAPPING-*.md`/`sprint-FIX-BCTC-BANK-SCALAR-MAPPING-*.md` decision file, zero matches for this task-id (the stale dev-mcp-server sprint journal's latest entry is stamped with an earlier, different task-id).

VERDICT: APPROVE (technical substance — all PASS claims RAW-verified, escalation legitimate) but HELD at `status:"REVIEW"` (not flipped done_verified) per QA's own DJ-GATE-1 clause — `status_note:"journal-missing"` + `next_agent:"dev-mcp-server"` written to the board row + `.head`, single atomic `jq`+`scripts/orch-apply.sh` write (dry-run rehearsed on scratch first, 123→123 coherence warnings unchanged). Recommended (not built — pm's job) a follow-on backlog task for CTG balance-sheet agentic-refine, blocked_on gateway-blind resolution. Could not call `send_telegram` myself (no MCP gateway binding in this QA sub-session, Bash/Read/Edit/Write only) — flagged to dispatcher to notify work channel. DJ: sprint-FIX-BCTC-BANK-SUMMARY-MAPPING-qa.md §qa-S2.

## cycle-423 · 2026-07-10 · review-drain sweep (router-dispatched, no board row) — 1 closed, 2 left BLOCKED

Router-initiated targeted sign-off sweep on 3 stale `task_board.review[]` rows PO flagged as far outside the healthy REVIEW-age cluster and missing `entered_review_at`. Not a normal Step-5/Close-Gate pipeline item.

**ARCH-SHIP-WAVE-REAUDIT** (sitting DEFERRED since 2026-06-11, re-parked 3×): traced the full downstream chain instead of trusting the stale `park_reason` prose — pm already decomposed the architect brief into 8 tasks same day (`7cf1d9bf`), ALL 8 implemented + QA-APPROVED by 2026-06-12T12:22Z (`487ca5091`, journal `sprint-SHIP-WAVE-REAUDIT-qa.md` S1-S10), all 9 rows archived `DONE` in `archive/2026-06.json`. PO's park (`b1de4ff92`, 06-13T08:48Z) postdated that completion by >20h and was never re-checked across 2 subsequent relabels (06-15, 06-27). Both cited UNPARK CONDITIONs independently also already satisfied (ARCH-CRON-SCHEDULER-RELIABILITY done_verified; VN-MACRO-TOOLING WAVE-1 mcp-server VMT-6 DONE). Pre-existing DJ-GATE-1 entry confirmed (architect-S1/S2). Closed `review[]`→`done_verified[]`, `status:DONE_VERIFIED`, `qa_verdict:APPROVED`, backfilled `entered_review_at` from the architect-brief commit timestamp. DJ: sprint-SHIP-WAVE-REAUDIT-qa.md §qa-S11.

**TASK-W5-FIX-BCTC-BANK-SUMMARY-MAPPING-VALIDATION-REINGEST** + **W5-FU-CTG-REFINE-96e36139** (sibling rows, same CTG report_id 96e36139): RAW-probed live named-volume `market.db` via `docker exec` sidecar — genuinely still blocked, NOT safe to sign off. `financial_reports` total_assets=0.0, validation_status=`low_confidence` (soft, not hard-block). `bctc_table_rows` for this report_id: 451 rows, ZERO tagged `balance_sheet` — same classifier defect. Decisive: those rows' `extracted_at`=2026-07-03T05:16:10Z predates the `FIX-BCTC-BANK-BS-COLUMN-ORDER` fix commit `d69b13f41` (2026-07-03T08:03:47Z) by ~3h — the currently-served data was extracted BEFORE the fix; the operational reingest has never re-run against the now-live fixed code (container rebuilt 2026-07-09T12:51Z, postdates the fix). Cross-corroborated with the independent, more-recent sibling finding (cycle-422 above, same report_id) that balance_sheet may be genuinely absent at the source, not just a stale reingest — flagged as an escalation path if a fresh reingest still yields 0 rows. Left both BLOCKED, `next_agent` unchanged (ops), backfilled `entered_review_at` on both from their own dispatcher/adoption timestamps, added `status_note` with full evidence. DJ: sprint-FIX-BCTC-BANK-SUMMARY-MAPPING-qa.md §qa-S3/S4.

Single `jq`+`scripts/orch-apply.sh` write for all 3 rows (dry-run diffed against scratch candidate first — confirmed zero other review-lane rows touched, rest-of-document byte-identical; 123→123 coherence warnings unchanged, non-blocking). `.head` untouched (was already idle, this sweep sits outside the dev-team pipeline chain).

## cycle-424 · 2026-07-10 · FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID — code+tests APPROVED, HELD REVIEW (rebuild pending)

dev-mcp-server landed a writer-level existence guard (`SELECT 1 FROM alerts WHERE id=?` before the `agent_signals` co-write, both `storeAlerts`/`storeAlertsFromCommander`) + D-NEW2 audit tripwire, claiming 0 live orphans and "63 fail/9 err full-suite, zero overlap, pre-existing flake." Did not trust either claim. Diff-read confirmed the guard is generic/parameterized, not hardcoded. Re-ran fresh: new test 8/8, targeted set (10 files) 129/129, tsc clean, DDD/security/mock-guard clean. Independently re-ran the FULL suite myself (not relying on the report): 14456 pass/40 skip/56 fail/5 errors — grepped all 56 failing names for alert/signal/orphan/audit overlap (zero), isolation-reran 3 samples (one reproduces a real local `chromium` binary missing + 5s timeout — env flake; other 2 pass clean in isolation) — confirms pre-existing/unrelated, not just relayed.

Live-DB RAW-probe done myself via `docker exec bun` against the real serving DB (found via `DB_PATH` env + volume-mount inspection — `/app/data/market.db`, NOT one of 6 other same-container `.db` files lacking `agent_signals`): 0 orphans, 10/10 resolve — reproduces the claim. Went further: `docker inspect` the running image — built 2026-07-09T12:49:50Z, BEFORE the fix commit (2026-07-10T04:30:25Z) — proves the container is still running pre-fix code; the 0-orphan snapshot only means no fingerprint collision has recurred yet under the OLD code, not that the fix is live.

VERDICT: code+tests APPROVED. Held `task_board.review[]` at `status:REVIEW` (not DONE_VERIFIED) per standing qa-S7/S9 precedent — production behavior claim can't be signed off before the code is actually deployed. `next_agent` qa→ops, `qa_verified_at`/`qa_verified_by`/`qa_note` added (dev's `status_note` untouched) via new `scripts/qa-fix-agent-signals-orphan-alertid-hold-review-ops.jq`+`scripts/orch-apply.sh` (dry-run diffed first, only this row+`.head` changed, 123→123 coherence warnings unchanged). `.head` synced same write. Filed `docs/signals/ops-rebuild-verify-mcp-server-20260710T0449Z.json` (deferred rebuild+swap+post-deploy 0-orphan re-check across >=1 alert-scan cycle). DJ-GATE-1 confirmed satisfied (dev-mcp-server-S28). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S43.

## cycle-425 · 2026-07-10 · FIX-BCTC-D1-STABILIZE-REPORT-ID — APPROVED, DONE_VERIFIED

dev-mcp-server rewrote `storeReport()`'s `INSERT OR REPLACE INTO financial_reports` (mints a fresh `randomUUID()` id on every re-parse of the same `(action_code, sort_key)`, orphaning `bctc_layout_units`/`bctc_page_zones` plain-TEXT report_id refs — no real FK) as `INSERT ... ON CONFLICT(action_code, sort_key) DO UPDATE SET <all cols except id>` + a pre-check SELECT so the in-memory `report.id` stays stable too. Router already RAW-verified diff+new-test+tsc+board-lane; did the full gate beyond that. Re-ran new test fresh (3/3, 17 assertions), tsc clean, DDD/security/mock-guard all PASS. Ran all 13 files (not just the 1 new one) that call `parseBctcReport`/`storeReport`: 95/95 pass — zero regression.

Grepped repo-wide for other `INSERT OR REPLACE INTO financial_reports` sites (router's explicit ask): found a SECOND live one in `newsChainFallback.ts:348` (`tryNewsChainFallback`, reachable via `resolvePdfText.ts:232` when PDF/OCR fully fails) — same DELETE-then-INSERT/fresh-id pattern D1 fixes, untouched by this diff, never in the architect's brownfield scan. Verified this is genuinely pre-existing (not a regression from this diff) via `git stash` A/B: `1294b-bctc-fallback.test.ts` (exercises this 2nd site) hangs identically with the fix in place and with it stashed out — pre-existing sandbox/env hang, unrelated to storeReport's SQL change.

VERDICT: APPROVED, `DONE_VERIFIED` — D1 does exactly what it was designed+scoped to do (design doc + board `files:` both scope only `parseBctcReport.ts`), zero regression, no live-deploy claim to gate on (pure code/SQL correctness). Did NOT block on the 2nd INSERT-OR-REPLACE site — out of D1's explicit scope, pre-existing, blocking would stall the D2-D7 chain without the proper channel — flagged as a new non-blocking backlog finding for pm/architect instead (matches standing flag-not-absorb pattern). Board flip via `jq`+`scripts/orch-apply.sh`, `next_agent` qa→pm (dispatch D2 `FIX-BCTC-D2-ENSURE-SHELL-ROW` next). Could not `send_telegram` myself (no MCP gateway binding in this QA sub-session) — flagged to dispatcher. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S44.

## cycle-426 · 2026-07-10 · FIX-BCTC-D2-ENSURE-SHELL-ROW — CHANGES_REQUESTED

dev-mcp-server landed `ensureFinancialReportShellRow()` (idempotent `INSERT...ON CONFLICT(action_code,sort_key) DO UPDATE SET pdf_path=excluded.pdf_path WHERE pdf_path IS NULL`, first-insert `validation_status='pending_extraction'`), wired into `bctcPdfPullJob.ts` Step 4b after `savePdf()`. Router already RAW-verified files/tests/tsc/mock-guard. Re-ran the full gate independently: 28/28 targeted tests fresh (6 `it()` in the new file, not 7 as dev self-reported — confirmed off-by-one, not a red flag), tsc clean, DDD/security/mock-guard all PASS, diff matches the design doc's D2 SQL fragment verbatim, no D1 regression, R-LOW-1 (validation_status enum blast-radius) re-checked clear across all 7 reader files.

Went further than the diff itself — traced the shell row's downstream read path (the design doc's own Test Strategy explicitly delegates "integration/E2E" to QA). Wrote a scratch script inserting a shell row and reading it back live: `extraction_confidence` reads back as `1` (100%), because the INSERT's column list omits it, so SQLite silently applies the schema's `DEFAULT 1.0` (`bctc-schema.ts:750`) to a row with literally zero extracted data (`total_assets`/`net_revenue` both NULL). Confirmed via grep this is the ONLY `financial_reports` INSERT site in the whole codebase that omits `extraction_confidence` — a genuinely new landmine, not a pre-existing dormant one. Traced who reads it: `get_bctc_full` is safe (PUB-1 blocks `refine_status='PENDING'` first), but `get_financial_summary`/`compare_financials` (`reports.ts:277-356`,`:392-457`) have no such gate — only `checkBctcIdentityGuard`, which by design fails OPEN on `total_assets==null`. Result: before D2, these two tools correctly returned "No financial data found" for an unextracted ticker+period; after D2 as shipped, the same call returns "Confidence: 100%" + "Net Revenue: 0.0 tỷ VND" — a confidently-fabricated-looking reading, and durably so (not transient) for tickers like GAS whose scalar tiers never cross the 0.5 threshold (the design doc's own concern-3 example) — `parseBctcReport.ts`'s own confidence-0 skip-insert guard means the legacy pipeline will never overwrite this shell row for such tickers. No existing test covers this combination.

VERDICT: CHANGES_REQUESTED — a real, diff-caused (not pre-existing) regression, distinct from D1's out-of-scope 2nd-call-site finding. Blocking issue (file:line): `ensureFinancialReportShellRow.ts:110-138` — add `extraction_confidence` to the INSERT column list, bind `0`, not the silent `DEFAULT 1.0`. Non-blocking backlog flag: `reports.ts`'s two tools still need a `validation_status`/`refine_status` gate (mirrors the existing `bctcIdentityGuard.ts` VNM→VEA→CTG serve-layer-guard precedent) to fully close the misleading-render gap — out of D2's own file scope, filed as a separate pm/architect backlog item, not required to land D2. Board `task_board.review[FIX-BCTC-D2-ENSURE-SHELL-ROW]`: `next_agent` qa→dev-mcp-server, `qa_verdict`/`qa_note`/`qa_reviewed_at` added, `status` stays `REVIEW` (no `CHANGES_REQUESTED` enum value — matches the `069d23e38` field-convention precedent), `.head` untouched (points at the parent umbrella task, not this sub-task). Round 1 — routes to dev-mcp-server (fixer), not architect. Could not `send_telegram` (no MCP gateway binding this session) — flagged to dispatcher. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S45.

## cycle-427 · 2026-07-10 · FIX-BCTC-D2-ENSURE-SHELL-ROW round-2 — APPROVED, DONE_VERIFIED

dev-mcp-server applied the exact minimum fix my round-1 blocking finding specified: added `extraction_confidence` to the INSERT column list bound to `0`, strengthened the new test's first case with `expect(row!.extraction_confidence).toBe(0)`. Router already RAW-verified independently; did not skip my own gate. Re-read `ensureFinancialReportShellRow.ts:110-142` — confirmed literal `0` in the VALUES clause. Ran my own fresh scratch insert+read-back (same live-probe method that caught the round-1 regression, not just a static read): row now reads back `extraction_confidence:0, total_assets:null, net_revenue:null, validation_status:'pending_extraction'` — the exact fix confirmed, regression closed. Re-ran the 3-file targeted suite fresh: 28/28 pass, 109 expect() calls. `tsc --noEmit` clean. DDD scan clean. Security scan clean (no `process.env`, no secrets). `mock-guard --files` PASS on all 4 touched/new files. Confirmed `reports.ts` genuinely untouched (`git status` — no entry) and round-2 diff scope is exactly the 2 files claimed (both still untracked/new, no scope creep).

VERDICT: APPROVED, DONE_VERIFIED — fix closes the exact regression, no new regression introduced, all gates green. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (dry-run diffed first: only this row moved, review 22→21, done_verified 1→2, `.head` byte-identical, valid JSON, no dup keys). `qa_verdict`→`APPROVED`, `qa_note` appended with round-2 summary (round-1 text preserved as historical record), `qa_verified_at`/`qa_verified_by` added (mirrors D1's own field-pattern precedent), `next_agent` qa→pm to dispatch D3 next in the 7-task chain. `.head` left untouched — points at the parent umbrella task `FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION`, not this sub-task. Could not `send_telegram` (no MCP gateway binding this sandboxed sub-session, same known gap as round 1) — flagged to dispatcher/router. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S46.

## cycle-428 · 2026-07-10 · FIX-BCTC-D3A-PEK-TRIGGER-HELPER — APPROVED, DONE_VERIFIED

Router already independently re-ran the new test (6/6), an 8-file regression (82/82), `tsc`, and `mock-guard.sh` — did not re-run those, went for a fresh-eyes read instead. Read `pekExtractTrigger.ts` (new, infra/fetchers) in full + `bctcVpsIngestHandler.ts`'s diff line-by-line: the returned discriminated union (`queued|market_hours|pdf_extractor_error|unreachable`) preserves the exact original branch order (503-check first, then `!ok`, then success, all inside one outer try/catch covering fetch AND `.text()` errors) — no branch dropped or added. `log.error`/`log.info` message text + meta keys byte-identical to the pre-refactor inline code; the `res.end(result.body || JSON.stringify(...))` empty-body fallback for market_hours preserved verbatim. Re-ran the new test file myself (not trusting the number): 6/6 pass, 25 expect() — exact match, and confirmed the assertions are substantive not tautological (spy-logger captures real call args per branch; the "defaults to shared logger" test genuinely exercises the real singleton — its info line printed to console during my run, proving the default-param path runs real code). Grepped the whole repo: the new helper is called from exactly 1 site (the handler) — confirms the stated D3B auto-trigger wiring genuinely is NOT present yet (`bctcPdfPullJob.ts` has zero pek-extract references), matching the dispatch's explicit out-of-scope instruction. Confirmed zero `process.env`/secrets and zero DB writes in the new file — the `no fake data` lens is genuinely N/A here (pure network passthrough, no persistence to fabricate).

VERDICT: APPROVED, DONE_VERIFIED — confirmed pure mechanical extraction (zero new logic/branches beyond the original inline try/catch), response shapes byte-identical by my own direct read, not delegated trust. DJ-GATE-1 satisfied (`sprint-SYSTEMIC-REMAKE-P1-dev-mcp-server.md` has task-id-stamped entry present). Board row moved `task_board.review[]`→`done_verified[]`, `done_verified:true`, `qa_verdict`→`APPROVED`, `next_agent` qa→pm, `commit:"pending"` (left for router to RAW-verify+commit, per explicit dispatch instruction — did not commit myself). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S47.

## cycle-429 · 2026-07-10 · FIX-BCTC-SERVE-GATE-FINANCIAL-REPORTS — APPROVED, DONE_VERIFIED

Closes the gap D2 left open (my own cycle-426 non-blocking flag): `get_financial_summary`/`compare_financials` now gate on `validation_status==='pending_extraction'`, mirroring `get_bctc_full`'s PUB-1 but on `validation_status` not `refine_status` (architect rationale: legacy scalar-pipeline rows never touch `refine_status`, sit at DB default forever with real data). Router already re-ran tests(6/6,20 expect())/tsc/mock-guard/diff-read — dispatch scoped my review to 6 fresh-eyes substance points instead of re-verifying those.

(1) Regression-proof test (legacy `validation_status='pending'`, DB DEFAULT — `bctc-schema.ts:818` confirmed) asserts real report header AND a real fixture value ("39" from netRevenue~39.5B) — non-trivial. (2) compare_financials one-pending case: message contains the pending period, explicitly asserts the OTHER (real) period absent — genuinely distinguishes which period, not generic. (3) Read `reports.ts` directly: gate sits strictly before `checkBctcIdentityGuard` in both tools. Went further — traced `ensureFinancialReportShellRow.ts`'s INSERT: `total_assets` is genuinely NULL (column omitted, no schema DEFAULT), and `bctcIdentityGuard.ts` fails OPEN on null by its own docstring — so absent this gate, a shell row would have silently rendered a fabricated-looking "Confidence:100%,NetRevenue:0.0 tỷ VND" reading (the exact pre-fix regression), never even reaching a guard-caught message. Placement is correct AND load-bearing. (4) Both new messages read in full: pure English, matches `reports.ts`'s own convention, no cross-import from `bctcFullTools.ts` Vietnamese. (5) `git status --porcelain` confirms exactly 2 files touched. (6) `[[feedback_no_fake_data_real_fetch]]`: all 3 pending-extraction fixtures call the REAL `ensureFinancialReportShellRow` usecase (import+call sites read directly); 5 legacy-row fixtures use raw-SQL by necessity (simulating pre-existing DB state), plausible values. Independently re-ran the new test file myself: 6 pass/0 fail/20 expect() — exact match. Informational item confirmed accurate: `docs/agents/tools/list/compare_financials.md` genuinely documents a different tool (`compareTools.ts` actually registers `compare_stocks`, different params/output) — stale doc, left untouched, out of scope.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via new `scripts/qa-fix-bctc-serve-gate-financial-reports-done-verified.jq`+`scripts/orch-apply.sh` (dry-run diffed first: review 22→21, done_verified 3→4, rest-of-doc byte-identical, coherence warnings 130→130 unchanged). `qa_verdict`→`APPROVED`, `next_agent` qa→pm, `commit:"pending"` (router RAW-verifies+commits). `.head` deliberately untouched — points at the parent umbrella task (`FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION`), same guard convention as D1/D2/D3a. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S48.

## cycle-430 · 2026-07-10 · FIX-BCTC-D3B-GATE-PEK-TRIGGERED-STATUS — APPROVED, DONE_VERIFIED

Router already independently RAW-verified diff genuineness (0-row gate removed, `triggerPekExtractionForReport` called with shell-row id, all 4 outcomes fold into one unconditional `pek_triggered` write, shellRow-undefined edge case advances non-fatally, dead `enrichFailed`/`sendBugFn` removed), the 3 status-enum consumer diffs, repo-wide grep confirming exactly 3 consumer files, 76/76 tests (231 expect()), tsc, mock-guard on 10 files. Did not re-derive any of that — dispatch scoped 5 fresh-eyes points.

(1) Outcome-folding: traced whether D3C (not yet built, design doc §D3) can correctly re-derive a terminal verdict from `bctc_layout_units` row-counts alone regardless of which of the 4 `PekTriggerOutcome` variants fired. Correctness IS preserved (row-count is the sole ground-truth signal for all 4). Flagged non-blocking: market_hours/502-class rows can never land real rows without an ACTIVE re-fire, while queued/202 rows are already in-flight — folding makes these indistinguishable at the DB-column level, so D3C's reconciliation loop must adopt a uniform always-re-fire-on-still-zero policy, not passive re-check, to guarantee eventual convergence. Already an explicitly open PM decision in the design doc itself (§D3 line 73) — recommended this note travel into D3C's dispatch.

(2) Read the full `bctc-pdf-pull-job.test.ts` diff: TC-14 does a real `SELECT` read-back for the shell-row id; TC-15/16 correctly test the job's own unconditional-write logic via injected outcome stubs + real DB reads; TC-17 forces a REAL sqlite failure (`DROP TABLE financial_reports`, not a mock) and proves the catch/non-fatal/skip-but-advance edge case end-to-end — the strongest of the 4.

(3) Read `FIX-BCTC-ENRICH-SILENT-0ROWS.test.ts`'s full rewrite: SUPERSEDED header explicitly maps all 8 historic ACs to a successor test or a stated merge-rationale (AC-1/2/3/5/6→1 test, AC-4/4b/8→1 test, AC-7→its own test) — zero ACs vanish silently.

(4) D3C-dependency risk (status_note's own flag): confirmed accurately review-window-acceptable — project standing policy gates all deploys behind a separate user-gated ops step, and D3C already sits in `backlog[]` set to auto-promote once this lands.

(5) No-fake-data: diff touches only status enum + control-flow + synthetic test fixtures.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via new `scripts/qa-fix-bctc-d3b-gate-pek-triggered-status-done-verified.jq`+`scripts/orch-apply.sh` (dry-run diffed first: review 22→21, done_verified 4→5, `.head` byte-identical, coherence warnings 130→130 unchanged). `qa_verdict`→`APPROVED`, `next_agent` qa→pm (dispatch D3C next), `commit:"pending"` (router RAW-verifies+commits). `.head` deliberately untouched — same guard convention as D1/D2/D3a/SERVE-GATE. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S49.

## cycle-431 · 2026-07-10 · FIX-BCTC-D3C-RECONCILE-JOB — APPROVED, DONE_VERIFIED

New `bctcExtractReconcileJob.ts` cron (`5,35 * * * *`) resolves `pek_triggered` rows to `done`/`enrich_failed` via OR-across-3-tables (`bctc_layout_units` non-quarantined / `bctc_table_rows` / `bctc_md_tables`), bounded `MAX_RECONCILE_ATTEMPTS=8` (own `reconcile_attempts` column, separate from pre-existing `attempts`), uniform re-fire on every still-zero pass. Router already independently verified board row/diff/tsc/mock-guard/own-test-run/2-sibling-regression/counters/notebook/DJ-GATE-1 — dispatch scoped review to 5 fresh-eyes points.

(1) Re-fire-vs-passive: read `pushBctcLayoutHandler.ts` directly — DELETE-before-INSERT genuinely runs inside the SAME `db.transaction()` as the re-INSERT, confirming idempotency is real; `bctc_vps_queue` structurally cannot distinguish 202-in-flight from 503/502-never-started, so uniform re-fire is the only policy guaranteeing eventual convergence for the 503/502 class. SOUND, endorsed.

(2) Cross-file ownership grep: `bctcExtractReconcileJob.ts` confirmed sole writer moving rows OUT of `pek_triggered`. Found NON-BLOCKING: `bctcQueueEnricherJob.ts`'s pre-existing (2026-07-02) Arm-2 grace-period retry recycles `enrich_failed`→`pending` after 7d without ever zeroing `reconcile_attempts` (or the identically-affected pre-existing `attempts`) — a recycled row can re-exhaust almost immediately on its 2nd `pek_triggered` episode instead of a fresh ~4h budget. Mirrors an already-accepted pre-existing gap (same issue on `attempts`), not a regression, not silent (BUG still fires) — recommended backlog follow-up; fix belongs in 2 out-of-scope files from already-approved prior tasks.

(3) `financial_reports` UNIQUE(action_code, sort_key) CONFIRMED present (`bctc-schema.ts`, executed via `db.exec(SQLITE_DDL)`) — same pair the job's lookup + D1's `ON CONFLICT` both target. No duplicate-row risk.

(4) Independently ran the FULL 105-file `*bctc*`/`*pek*` per-file-isolation sweep myself (new `scripts/qa-bctc-pek-test-sweep.sh`, modeled on `ci-per-file-isolation.sh`): 1153 pass/4 skip/8 fail, all 8 isolated to the pre-existing unrelated `1294b-bctc-fallback.test.ts` standalone timeout (grep-confirmed zero symbol overlap) — matches claim exactly on pass/fail/attribution.

(5) `financial-reports.md` invariant #8 read against actual code — accurate on every clause.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via new `scripts/qa-fix-bctc-d3c-reconcile-job-done-verified.jq`+`scripts/orch-apply.sh` (dry-run diffed first: review 22→21, done_verified 6→7, `.head` byte-identical, coherence warnings 130→130 unchanged). `qa_verdict`→`APPROVED`, `next_agent` qa→pm, `commit:"pending"` (router RAW-verifies+commits). `.head` deliberately untouched (idle/null) — same guard convention as D1/D2/D3a/D3b/SERVE-GATE. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S50.
