# QA — Notebook
## cycle-759 · 2026-08-14T17:39:00Z · FU-CTG-DISCOVERY-FILENAME-FILTER (vps-scripts, commit `1c9959cdd1`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.files[]` drain field — derived via `git show --stat` (fallback path, matches row's own `return_summary` exactly). Commit `1c9959cdd16e80831c1ae90e6d62e192972e7e60` confirmed real, on `main` ancestry; touches exactly `vps-scripts/discover-bctc-urls-browser.py` + new `test_discover_bctc_filename_classifier.py`. Non-bun zone (vps-scripts, stdlib-only imports — no docker image, host `python3` == VPS deployed runtime, substitution rule N/A). Re-ran independently, not trusted from prose: new suite 15/15 pass, full `vps-scripts/` suite 57/57 pass (matches claim exactly), `py_compile` clean, `mock-guard.sh` PASS, secrets/`process.env` clean. Read diff directly: `is_cover_letter_filename()` checks the resolved PDF's final path segment (post query-strip+URL-decode, case-insensitive) for `cv_cbtt`/`cong_van_cbtt`; wired into `_fetch_pdf_url()`'s href loop so a rejected cover-letter attachment does not short-circuit the scan — later legit hrefs in the same ArticlesFileAttach response still return. Matches AC and the article-613699 repro in the commit message; scope confirmed contained to `vps-scripts/`, no `apps/mcp-server` change needed. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S153.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 712/signal_total 21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. VPS deploy correctly withheld per standing user-gate policy — not a QA blocker (verify-committed JUMP-TO has no merge/push/deploy step, work already on `main`). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`).

## cycle-762 · 2026-08-14T17:41:00Z · FDA-10 (apps/mcp-server, commit `ec27c69d3108`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Router-dispatched (`mode=verify-committed`), `qa[]` row with `commit_sha` but no `.files[]`/`.owner` — `files[]` derived from `backlog-detail.json#FDA-10` fallback (single file, matches status_note claim). Commit `ec27c69d3108032244de7c18c9b9641d1c81cacb` confirmed real, on `main` ancestry; `git show --stat` touches exactly `shippingIndex.ts`, 2 ins/3 del. Diff read directly, not trusted from prose: deletes stale "SCFI proxied via TE or BDI-related symbol" comment (L7) + corrects `SHIPPING_SYMBOLS` JSDoc (L73-77) to state SCFI has no free Yahoo ticker and is NOT fetched/proxied — matches status_note verbatim; live `grep -i scfi` on current file shows only the corrected line, no residual misleading claim. Re-ran REAL verification: `bun test src/__tests__/252-shipping-index.test.ts` 8/8 pass (exact claim match), `bun tsc --noEmit` 0 errors, `mock-guard.sh --files shippingIndex.ts` exit 0 PASS. Comment-only — `SHIPPING_SYMBOLS`/fetch logic byte-unchanged, confirmed by diff (no non-comment line touched). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` (over byte-cap mid-write, CAP-REACHED marker present — entry landed pre-marker) STEP qa-S154 (collides with a concurrent peer's step label on this same hot-contended file; content itself intact/unambiguous by task-id).

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note`; `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total=712 live=candidate unchanged, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`. Dispatcher-wrap lock `task:FDA-10` (held by dev-team) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway tool binding available in this sub-session; best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED this cycle: file under live concurrent-peer write contention (≥3 sibling sessions racing the same file within the same minute, confirmed by interleaved out-of-order `cycle-760`/`cycle-761` headers already present above) — appended additively only, no existing section touched, to avoid clobbering a peer's in-flight write; next single-writer cycle should reconcile back to the 3-section cap.

## cycle-763 · 2026-08-14T17:40:44Z · FDA-7 (apps/mcp-server/, commit `24022a53a`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with no `.commit`/`.files[]` drain fields — commit derived from `.commit_sha`, file scope from `backlog-detail.json#FDA-7`'s own `files[]` claim. Commit `24022a53a` confirmed real, on `main` ancestry; `git show --stat` matches the claimed file (`macroTools.ts`) plus `macroSnapshotGuard.ts` (doc-comment only), new `089-tool-macro.test.ts` assertions, and `get_macro_snapshot.md` doc — all in-scope for the FIX. Read the diff directly, not `return_summary` prose: `source_tier` fallback flipped `?? 2` → `?? 4` (conservative/unknown) when no present `signals.*` component carries a tier; `fetchedAt` fallback flipped from fresh-stamping `new Date().toISOString()` to explicit `?? null` (fail-loud absence) when Go upstream omits provenance — exact match to the backlog note's stated FIX, no scope drift. Re-ran independently: `089-tool-macro.test.ts` 21/21 pass (incl. 4 new FDA-7 RED→GREEN tests: null-fetchedAt-on-omission, tier=4-signals-present-but-tierless, tier=4-signals-absent, no-regression real-fetchedAt passthrough). `bun tsc --noEmit` clean (0 output). `mock-guard.sh --files macroTools.ts macroSnapshotGuard.ts` exit 0 PASS. DDD: `interface/mcp/tools/macro/` importing `infrastructure/logger.js`+`fetchDeadline.js` pre-existing/untouched by this diff's hunks, not a `domain/` file — no violation. `process.env`/secrets grep clean on both files. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S155.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to `status_note` (row had none); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total 712/signal_total 21 unchanged). Row moved `qa[]`→`done_verified[]`, `qa_verified_at`/`qa_verified_by` set. Section-prune DEFERRED this cycle, same rationale as `cycle-762` immediately above (live multi-session write contention on this file) — appended additively only. Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]` with `status:DONE_VERIFIED`).

## cycle-763 · 2026-08-14T17:39:00Z · FU-BACKFILL-MULTIPLE-COVER-LETTERS (apps/mcp-server/, commit `c515ec142`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Router-dispatched (`mode=verify-committed`), `qa[]` row with no `.files[]`/`.owner` drain fields — files derived from `review_note` prose and cross-checked against `git show --stat` directly, not accepted blind (`detail_ref#FU-BACKFILL-MULTIPLE-COVER-LETTERS` in `backlog-detail.json` predates implementation, no `files[]` there either). Commit `c515ec142fe0e2094b25dbd45b7818255aeb2a40` confirmed real, on `main` ancestry; `git show --stat` matches all 3 claimed files exactly (new `FU-BACKFILL-MULTIPLE-COVER-LETTERS.test.ts`, `backfillBctcPdfPaths.ts`, `docs/architecture/microservice/mcp-server/usecases.md`). Re-ran REAL verification, not trusted from prose: new test file 12/12 pass; wider regression batch (new file + `FIX-CTG-PDF-MISLINK.test.ts` + `PI3-bctc-inspect-reopen2.test.ts`) 56/56 pass, byte-identical to claim; `bun tsc --noEmit` 0 errors; `mock-guard.sh --files backfillBctcPdfPaths.ts` exit 0 PASS; `process.env`/secrets grep clean. DDD: file greps a `from.*infrastructure` hit (`logger.js`) but is `application/usecases/`, not `domain/` — dev-standards.md golden rule scopes the ban to `domain/` only, application importing infrastructure is the documented established pattern, not a violation; zero `domain/` files in scope. Read the fix directly: new `isConsolidatedReportFilename()` positive-match gate (Vietnamese "hop nhat"/English "consolidated") applied to both the NULL-pass and heal-pass, replacing the old negative-match `!isCoverLetterFilename` — matches the described mechanism exactly. Doc section confirmed present in the commit itself. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-23.md` STEP qa-S1.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's existing `review_note` field (row already carried dev's own prose there); `verification.raw_probe` attached same write via `scripts/orch-apply.sh` (Stage 0/1 PASS, conservation OK task_total live=712/candidate=712, signal_total=21 unchanged; 16 pre-existing non-fatal Stage 1g dependency-resolution warnings, none touching this row). Row moved `qa[]`→`done_verified[]`, status `QA`→`DONE_VERIFIED`, `qa_verified_at`/`qa_verified_by`/`next_agent:pm` set. Dispatcher-wrap lock `task:FU-BACKFILL-MULTIPLE-COVER-LETTERS` (held by dev-team) cleared via this board write per spawn-prompt instruction — no `task_release` call made (no gateway/MCP tool grant this qa sub-session, best-effort/dispatcher responsibility per INV-GATEWAY-1, TTL is the authoritative backstop). Board write confirmed durable (re-read live file post-write, row absent from `qa[]`, present in `done_verified[]`). Section-prune (AC-2/3) DEFERRED again this cycle, same live concurrent-peer contention as `cycle-762`'s own note (file already ≥4 sections at write time) — appended additively only; next single-writer cycle should reconcile to the 3-section cap.
