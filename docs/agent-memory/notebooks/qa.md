## cycle-864 · 2026-08-26T08:29Z · FIX-RCVERIF-GRANDFATHER-EXEMPTION-IGNORES-RETRACTION-VOID-MARKERS, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**dev-mcp-server implemented+self-reviewed but correctly refused to self-certify (same agent type authored the fix); I trusted none of its claims on read alone.** Confirmed independently: `d569db23a`/`f0ee4481f` both ancestors of `main`; parsed `RC_VERIF_GRANDFATHERED_IDS` myself with comments stripped — count=50, `FU-RAG-DEPLOY-MEMORY` present only inside 2 cross-ref comments, absent as an actual array member; the cross-ref comment (removal site + §8A AMENDMENT block) is real. Built my OWN detached worktree at `d569db23a~1` (did not reuse dev's) and ran the exact QA repro fixture through that pre-fix code's real `orch-validate.mjs` — exit 0 (bug reproduced live, independently); identical fixture at current HEAD — exit 2. Worktree removed after. `orchStateSchema.test.ts`: 122/122 pass, RCV-9-a/RCV-9-b isolated-filtered to confirm 2/2 specifically. `tsc --noEmit` clean, `mock-guard.sh` PASS, DDD clean (file imports only `zod`), security clean. Repo-wide grep: zero references to `RC_VERIF_GRANDFATHERED_IDS`/`checkVerificationGate` outside `orchStateSchema.ts` + its own test.

**Ruled on the open question by reading the row's own AC text verbatim, not the quoted excerpt.** `generic_mandate`/AC-1 explicitly pre-authorizes "(b) alone" contingent only on a cross-ref comment (confirmed present) — AC-1(a) is NOT load-bearing for this row. `checkVerificationGate()` genuinely still has zero marker-awareness (bare `!RC_VERIF_GRANDFATHERED_IDS.has(id)`, read directly at source) — acceptable per this row's own AC framing, not a gap I invented tolerance for. Independently re-checked the 2 flagged substring hits: `CONTAM-10-WRITER-H` (all hits are unrelated OHLCV-data-contamination prose, zero void/retract) and `SSOT-W1-HOOK-ENFORCE` (sole "void" hit is inside "avoids") — both confirmed false positives, concur with dev-mcp-server.

**Baseline-trap: file-set differed from BOTH recorded baselines, investigated rather than waved through.** dev-mcp-server's reported 17-file failing set swaps `1288-poll-news-shape`/`1839b-notebook-protocol` in for the 08-24-drifted set's `1324-push-news-all-sources`/`FIX-1267-ssc-circuit-breaker`. Ran both in isolation: `1288-poll-news-shape` passes 4/4 standalone (full-suite-only flake, matches the baseline's own documented timeout/environmental character); `1839b-notebook-protocol` genuinely fails standalone, but root cause is a stray non-`.md` file (`unified-agent-synthesis-2026-08-25-chef-intraday.json`) dropped into `docs/agent-memory/notebooks/` by an unrelated commit `455e3299c` (chef/unified-agent lane, dated 2026-08-25 — 2 days after this fix), zero content relation to orchStateSchema/RC-VERIF. Neither attributable to this change.

**Non-blocking residual, concur with dev-mcp-server's own recommendation:** the general defect the task title names (gate ignoring retraction/void markers) is closed only for `FU-RAG-DEPLOY-MEMORY` specifically; the same exploit would silently reproduce if a retraction/void marker later appears on any of the remaining 50 grandfathered ids. Not blocking this row per its own AC-1 text — flagged for a future lightweight follow-up (marker-aware check or periodic audit).

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh` (dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, then live), `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified on the live file. `.head` was not touched (row was head-decoupled, dispatched via Review-Lane QA-Drain). Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S239.

## cycle-865 · 2026-08-26T10:55Z · TASK-CRON-SKILLMD-PROBE-WIRING, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Standing finding was "probe exists, zero readers" — checked the CALLER, not the callee.** `ec8cc7340`+`98f20610b` both confirmed ancestors of `main`, together touch all 3 claimed `files[]`. Grepped repo-wide for `cron-marker-liveness-probe.sh` excluding the script's own file: confirmed genuine Step 1b.1 invocations in all 3 SKILL.md (cowork-team L92, detect-loop L55, standalone-team L64) — distinct from the many incidental brief/handoff/report mentions of the same path. Live-ran the probe for all 3 families myself: exit 0, well-formed JSON each time (`LIVE`/`LIVE`/`SELF`). Old byte-identical `released:false -> treat as LIVE -> STOP no-op` branch: 0 grep hits across all 3.

**Measured every cap claim myself via `wc -l`/`wc -c`, never trusted status_note's own figures.** F5 gate re-run: `grep -c "orphan_threshold_seconds: 7200"` across `cron-*/SKILL.md` = 0/0/0 (uniform 120 confirmed). v2 `registering_process` fingerprint present in Step 1c of all 3. Sizes: cowork `SKILL.md` 199L/11432B + `register.md` 167L/11555B; standalone `SKILL.md` 200L/11728B (AT the 200L line-cap, not over, matches router's own note) + `register.md` 108L/5332B; detect-loop `SKILL.md` untouched 175L/10064B. All 5 touched/created files clear both 200L/12000B caps.

**Confirmed the named residual stayed exactly where it should.** `CLEAN-CTXBLOAT-CRON-DETECT-LOOP-REGISTER-12349B-OVER-12000B-BYTECAP` (`backlog[]`, status `BACKLOG`, owner `claude-manager-helper`) is untouched by either commit in this row and remains open — not closed, not re-filed. `mock-guard.sh` PASS (0 scannable-extension files — `.md` outside its scanned set, documented gap for doc-only rows). All 3 `depends_on` rows independently confirmed `DONE_VERIFIED` — this row was not dispatched prematurely.

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh` (dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, then live), `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified on the live file. `.head` named this exact row (`active_task_id`, `next_agent:"qa"`) — reset it to idle in a separate write (`status:idle`, `active_task_id:null`, `next_agent:null`, `del(.head.next_action)`), self-verified. Outer lock `task:TASK-CRON-SKILLMD-PROBE-WIRING` left untouched per router instruction (INV-GATEWAY-1 — dispatcher's exclusive responsibility). Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S240.

## cycle-866 · 2026-08-26T10:57Z · FIX-JOURNALGUARD-ALERT-TRANSPORT-RUNS-ON-THE-PLANE-IT-MONITORS, direct-commit verify — CHANGES_REQUESTED, qa[]->review[]

**Re-derived the router's premise from source instead of accepting it.** Confirmed `coreSend` (`apps/mcp-server/src/infrastructure/notifiers/telegram.ts:167`) builds the `api.telegram.org` URL at `:198` and executes inside the `mcp-server` container regardless of transport — `call_tool` (gateway) and `scripts/agents-flow/mcp-call.sh` (targets `http://localhost:3000/mcp` then `https://zenmidi.com/vn-market/mcp`, both resolving to the same `mcp-server` process per `docker-compose.yml`'s `3000:3000` mapping) both hit the same `coreSend`. Premise TRUE — `send_telegram` cannot alert while `mcp-server` is the thing that's down.

**What actually shipped is a design-only architecture brief, not a fix, and the row's own `files[]` doesn't match what landed.** Commit `671496470` touches only the new brief + `architect.md` notebook + one signal file — neither of the row's claimed `files[]` (`scripts/market-db-journal-guard.sh`, `launchd/`) appears in it, and `scripts/market-db-journal-guard.sh` never existed on disk at any point (empty `git log`), so that field was already stale at mint time. Zero implementation exists: `scripts/lib/telegram-direct-send.sh`, `scripts/audits/journal-guard-alert-notify.sh`, and its test are all absent; `.claude/commands/crons/cron-market-db-journal-guard.md` still calls `call_tool(server="vn-market", tool="send_telegram", ...)` verbatim, unedited. The brief's OWN header/RETURN says "design only... `next_agent: developer`" — directly contradicting the terminal-shape commit (`8e71cf972`) that routed the row to `qa[]`/`next_agent=qa` as if implementation were complete.

**Verified the brief's factual claims anyway, since the design itself may still be sound even though the routing is wrong.** Both cited precedents check out live: `cowork-guaranteed-slot-firer.sh`'s `_escalate_failure`/`_load_env`/`_bug_chat_id`/`_alert_cooldown_ok` and `maybe-deploy-vps.sh` both genuinely do a host-side `curl -X POST https://api.telegram.org/bot<token>/sendMessage`, `.env`-sourced; `.env` has exactly 1 `TELEGRAM_BOT_TOKEN` + 3 real channel-id lines; `docs/data/system-map.json`'s drifted `TELEGRAM_BUG_CHAT_ID` alias is confirmed still present. AC-3 disposition (cowork preflight-error-fallback out of scope) is coherent, not a silent AC loss — successor row `FIX-COWORK-PREFLIGHT-FALLBACK-NO-MCP-INDEPENDENT-PATH` genuinely exists in `task_board.backlog[]`, P1/architect, with a specific coordination-primitive-vs-one-way-notification distinction and a concrete evidence window.

**Verdict: CHANGES_REQUESTED, not DONE_VERIFIED — the underlying P0 remains 100% unfixed on `main` today.** AC-1 ("with mcp-server stopped, a forced exit-3 tick delivers a user-visible alert") is provably false right now: no code exists to make it true, and the brief itself says the developer must live-verify it before calling this done. Certifying DONE_VERIFIED here would falsely close a live P0 alert-transport outage. Lane-moved `qa[]->review[]` via `jq | orch-apply.sh` (dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, then live), `status: QA->REVIEW`, `next_agent` set to the row's own `.owner` (`"po"`) since there is no branch for a fixer to work — `redispatch_count` incremented, `status_note` appended with the full finding and an explicit instruction to re-route to `developer` per the brief's own RETURN block, self-verified on the live file. `.head` was already idle (owned this batch cycle by a peer row, not touched by me). Outer lock `task:FIX-JOURNALGUARD-ALERT-TRANSPORT-RUNS-ON-THE-PLANE-IT-MONITORS` left untouched per router instruction (INV-GATEWAY-1). Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S241.
