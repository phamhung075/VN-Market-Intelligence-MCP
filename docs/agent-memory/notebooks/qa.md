# QA — Notebook

## cycle-691 · 2026-08-13T13:38:44Z · FACTORY-GUARD-CI-METRICMASK-IMPL (developer, real code, commits `7a758d90f`+`33aee81e2`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), router explicitly asked for RAW re-verify of every review_note claim, not trust. Row carried no `.commit`/`.files[]` — cited commits taken from review_note prose, then independently confirmed: `7a758d90f` (feat) + `33aee81e2` (memory) both on main ancestry (`git merge-base --is-ancestor` OK), `git show --stat` matches all claimed files exactly (`scripts/audits/metric-mask-lint.sh` new, `cascadeEngine.ts`, `marketSentimentCalculator.ts`, `watchlist.ts`, `brokerCredibilityTools.ts`, `.github/workflows/ci.yml`, `dev-standards.md`, `metric-mask-lint.test.sh`).

Diffs verified line-by-line against claim: `cascadeEngine.ts:356,375,394` `seedEntry.confidence ?? 0.6` → `?? 0` (×3, feeding `detectMsciInclusion`/`Watchlist`/`Exclusion`); `marketSentimentCalculator.ts:174` `row.impact_score ?? 1.0` → row excluded entirely via new `if (row.impact_score === null) continue` guard; `watchlist.ts:198` + `brokerCredibilityTools.ts:51` both carry `metric-mask-allow:` annotations exactly as claimed (the 5th catch beyond brief scope). Ran `metric-mask-lint.sh --check` live: PASS, 0 offenses (434 keyword-bearing files — vs dev's 423 at 07-29, expected 2-week codebase-growth drift, not a discrepancy). Proved zero-tolerance is REAL not asserted: injected a synthetic untracked file with `?? 50` — lint correctly FAILed (exit 1, exact file:line), cleaned up after. `metric-mask-lint.test.sh` reproduced 10/10 PASS. CI wiring confirmed: `metric-mask-lint` job present in `.github/workflows/ci.yml`, `ubuntu-latest`/checkout-only. `shellcheck` clean both new scripts. `tsc --noEmit` 0 errors. `mock-guard.sh --files <4 touched prod files>` PASS. DDD scan: domain-layer files (`cascadeEngine.ts`, `marketSentimentCalculator.ts`) carry zero real infrastructure/application imports (grep hits are comments only); interface-layer files' infra imports (`watchlist.ts`, `brokerCredibilityTools.ts`) are the expected composition-root pattern, not a violation. Security scan clean.

Targeted suites reproduced EXACTLY matching dev's claimed counts: FIX-1279-msci-bullish 14/14, P0-4-market-sentiment-index 37/37, cascade files (1279a+247+FIX-1268+1224) 31/31, 915-broker-credibility 22/22, watchlist (082+220) 25/25 — 129/129. Ran a FRESH independent full `bun test` (not reused from dev's claim): 15262 pass/40 skip/56 fail/1 error/15358 total (480.28s) — total grew vs dev's 2-week-old 15007 (matches this sprint's own documented weekly test-count growth trend seen across sibling QA cycles); fail count 56 sits inside the same contemporaneous pre-existing flake band (44-63) independently confirmed by multiple sibling QA verify cycles this same week, tracked by still-BACKLOG board row `FIX-MCP-SUITE-HEALTH-BASELINE`; grep-confirmed ZERO of the 56 fails + 1 unhandled-error (DB-closed race in an unrelated `pollNews` 5000ms-timeout test) reference cascade/sentiment/watchlist/broker/credibility/msci/metric-mask by name or file.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 744 preserved; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S103. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.

## cycle-690 · 2026-08-13T13:03:51Z · FE-PG-INTEL-FRESH-FIX (dev-frontend, real code, commit `eddeb7cbd`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), third sibling in the FE-PG-*-FRESH-FIX family (after FE-PG-_INDEX-FRESH-FIX, FE-PG-BCTC-FRESH-FIX). Router explicitly asked for the same rigor as the immediately-prior sibling cycle, not the same conclusion pre-assumed. Row carried no `.commit`/`.files[]` — derived via `git log -- dashboard.intel.tsx`: `eddeb7cbd` (2026-07-29), cross-checked against dev-frontend's own decision journal STEP dev-frontend-S4 (same date). Re-verified fresh, not from the row's own `review_note` prose: confirmed on main ancestry; `git show --stat`+diff matches claimed files exactly (`dashboard.intel.tsx` FreshnessBadge import + render + `useFreshnessRevalidator("daily")` call wiring, `fetchIntelData` now threads `data_asof` through instead of discarding it, + new `FE-PG-INTEL-FRESH-FIX-dashboard-intel-loader.test.ts`, 6 cases). Backend `data_asof` field confirmed live in source (`marketDigestHandler.ts:71,169`) — same endpoint/field as sibling `FE-PG-_INDEX-FRESH-FIX`, matches the row's no-forked-logic claim.

Targeted 6/6 new unit tests GREEN. Full vitest suite: 2183 pass/2 fail/2185 total — the 2 fails (`QUE-REFERENCE-PAGE-detail.test.ts`, `QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.tsx`, "exactly 2 own keys" on `QUE_DESCRIPTIONS`) confirmed pre-existing + unrelated: neither file was touched by commit `eddeb7cbd` (only `dashboard.intel.tsx` + the new test file changed), and this exact failure pair was already confirmed unrelated on both prior sibling cycles. `tsc --noEmit` clean. `mock-guard.sh --files dashboard.intel.tsx` PASS.

Per task instructions, independently re-checked the row's own PENDING-REBUILD deploy claim rather than trusting it: `docker inspect vn-market-intelligence-mcp-frontend-1` Created=2026-07-24T18:02:36Z (predates this 07-29 fix commit); `curl localhost:3001/dashboard/intel` → 200 OK, zero occurrences of "Cập nhật lúc"/FreshnessBadge/`bg-green-100`/`bg-green-500` markup in the served HTML — confirms live :3001 still does not serve this fix, same class as both sibling rows, ops-owned gap not a code defect. Did not re-run the row's cited Playwright suite — matches the depth both immediately-prior sibling verify cycles (cycle-688/689) settled on, neither of which re-ran Playwright either.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 745 preserved; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-QUALITY-AUDIT-FRONTEND-COVERAGE-qa.md` STEP qa-S3. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.

## cycle-688 · 2026-08-13T12:12:26Z · FE-PG-_INDEX-FRESH-FIX (dev-frontend, real code, commit `3184247ab`+`969360919`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`). Row carried `.commit`/`.files[]`/`.owner` — self-contained. Re-verified fresh, not from the row's own `review_note` prose: commit `3184247ab` (+docs `969360919`) confirmed real, on main ancestry; `git show --stat` matches claimed files exactly (dashboard._index.tsx FreshnessBadge/useFreshnessRevalidator wiring + new 6-case unit-test file). Backend `data_asof` field confirmed live in source (`marketDigestHandler.ts:71,169`).

MAJOR FINDING: local `node_modules` (pnpm-resolved) had drifted off the committed `bun.lock` — produced a false esbuild "Host version mismatch" service crash AND a false-positive 30-test TopNav Router-context cascade across 6 unrelated test files (TopNav.tsx last touched 2026-06-30/07-02, weeks before this commit — could not be caused by this diff). `bun install --frozen-lockfile` (CI-parity, matches `.github/workflows/ci.yml`) made both artifacts vanish, confirming environment drift not a code regression — flagged to bug channel separately, not folded into this row's verdict.

Post-reinstall: targeted 6/6 new unit tests GREEN; full suite 2183 pass/2 fail (exact same pre-existing QUE-TOOLTIP failures already confirmed unrelated by the 2026-07-29 dev-team RAW-verify — total grew 2172→2185 naturally from later unrelated commits, failure set unchanged); `tsc --noEmit` clean; `mock-guard.sh` PASS. Re-checked the PENDING-REBUILD deploy-gap per task instructions: `docker inspect vn-market-intelligence-mcp-frontend-1` Created=2026-07-24T18:02:36Z (predates this 07-29 fix commit); `curl localhost:3001/dashboard` shows no freshness/stale markup in served HTML — still stale, unchanged since the 07-29 note.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 748 preserved; Stage 1g's pre-existing unrelated warnings, none touching this row). Live `:3001` deploy-gap left as an ops-owned finding (same PENDING-REBUILD class as sibling rows), not blocking. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S101. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.
