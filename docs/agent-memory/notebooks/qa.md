# QA — Notebook

## cycle-492 · 2026-07-28 · FIX-BDI-SHIPPING-STALE-404-GUARD — APPROVED, DONE_VERIFIED (direct-commit verify, 3-commit stack repairing the 3-day-red stale `"ổn định"` assertion PO root-caused; source-verified before trusting the swap)

Row `qa[]`, `branch:null`. Verified 9374e65e0 (test-only)/967e3578d (memory)/15da0ab80 (lane-move) all on main ancestry via `git merge-base --is-ancestor`; diff-stat confirms 9374e65e0 touches exactly `1408-tool-diacritics.test.ts` (5 insertions/4 deletions, assertion-only, zero production code). Did NOT blind-trust the dev/PO's prescribed replacement string — read `supplyChainTools.ts:114-125` myself first: the `indices.length===0` branch genuinely emits `"TỔNG KẾT: Không đủ dữ liệu vận tải biển hiện thời để kết luận..."`, exact match to the new `toContain()`. Re-ran (not trusted on report alone): `1408-tool-diacritics.test.ts` 8/8 pass; the 12-file targeted merge-gate suite (1408 + 10 precedent-listed supply-chain/commodity/BDI suites + FIX-BDI-SHIPPING-STALE-404-GUARD.test.ts itself) 115/115 pass — matches dev's claimed count exactly; `bun tsc --noEmit` exit 0.

Independently corroborated the repo-wide `bun test` attribution (dev claimed 56 fail/1 error → standing `FIX-MCP-SUITE-HEALTH-BASELINE` + 1 unrelated pre-existing `_deprecated/1302-technical-indicators.test.ts` failure) rather than accepting at face value: confirmed `FIX-MCP-SUITE-HEALTH-BASELINE` is a real tracked `BACKLOG` row (not invented), and ran `1302-technical-indicators.test.ts` standalone myself — 2 fail (MA/RSI/MACD text assertions), both untouched by and unrelated to this diff's supply-chain/diacritics scope. Full-suite run itself attempted in-session but did not complete under heavy concurrent host load (~15k tests, multiple sibling `bun test` processes observed live via `ps`) — not blocking since `dev-standards.md:590` CANONICAL pins targeted-suite-governs, and this diff's blast radius (1 assertion-only file) was independently confirmed regardless of the exact repo-wide count.

DJ-GATE-1 confirmed: dev-mcp-server's journal entry present (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md` STEP dev-mcp-server-S12, task-id stamped) before this verify ran.

VERDICT: APPROVED (mechanism-level — source-verified fix genuinely matches live behaviour, zero regression, journal present). Board row moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation task_total 665→666, qa-lane move nets +1 by design, same pattern as cycle-490/489). Underlying `ci_red` signals (CI-RED-6ba39d3c/bda56d1c/1a4cbfb0) explicitly left OPEN in the review note — this commit is NOT YET PUSHED to `origin/main`; PUSH-AUTONOMY-1 push + a fresh CI run returning `conclusion=success` on a new SHA are still required before those signals close (out of this row's own direct-commit-verify scope). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa.md` §qa-S3.

## cycle-491 · 2026-07-28 · FIX-PDFX-TESSERACT-CONCURRENCY-VIOLATES-SINGLE-WORKER-INVARIANT — APPROVED, DONE_VERIFIED (direct-commit verify, P0 supervised OCR concurrency invariant; mechanism proven, memory-headroom claim corrected not accepted)

Row `review[]`, `branch:null`, commit `4bac2b85d` main-ancestor confirmed. Did NOT trust the falsification claim on the dev's word (router's central ask: "a test that cannot fail proves nothing") — built a throwaway `git worktree` at `4bac2b85d^` (unmodified pre-fix main; `ocr_gateway.py` doesn't exist there, so the literal committed test can't even import — reconstructed the equivalent by patching `pytesseract.image_to_string` directly, same 15-wide real-ASGI-route harness) and independently reproduced RED, peak=15, matching the dev's claimed number exactly. Ran the actual committed `test_ocr_concurrency_invariant.py` on HEAD myself: 8/8 GREEN. Fixed-seed (12345) full-suite run on both trees: identical 7-failure set byte-for-byte (dev claimed "11" — pytest-randomly order-dependent count variance, not a discrepancy in the zero-regression claim, which held under same-seed-both-trees). `lint-imports` live: 3/3 contracts KEPT. `mypy` env-bug ("pdf-extractor is not a valid Python package name") reproduced verbatim. Read installed `pytesseract` 0.3.13 source directly (matches dev's cited version) — confirmed `timeout_manager` really does `kill()`+`RuntimeError` on `TimeoutExpired`, so the deadline mechanism genuinely terminates the OS subprocess, not just the asyncio wrapper — the guaranteed ratchet-break is real. Traced `extract_tables_usecase.py:563` — `slot_async()` genuinely wraps the ProcessPoolExecutor dispatch, composing the cross-process bound correctly (Gap-1 holds). Grep-verified (not doc-trusted): zero live mcp-server callers of `/extract-md-tables`/`/extract-layout-first` exist in production code — the unrewired `generic_md_table/extractor.py` seam is reachable-but-unexercised, not live-unguarded (Gap-2 correctly scoped).

Live re-check (read-only ps/health/logs only, zero writes to any endpoint): image `sha256:a75ddd7336` confirmed, RestartCount=0, healthy; ~15 independent samples over several minutes of real production traffic — tesseract count never exceeded 1; `docker logs --since 30m` tally of real `/extract` calls: 57×200/29×429/0×500 — the 429 backpressure contract is live-firing in production right now, unprompted corroboration beyond what was asked. Router flagged a live memory finding mid-task (parent-process RSS dominance) — independently reproduced before accepting it, not taken on trust: MemPerc back to 78-83% (VmHWM 97% of the 2.5GiB cap) ~23min post-restart under ordinary (non-burst) traffic, PID1 (uvicorn) alone RSS ~2.0-2.1GiB dominates while tesseract stays bounded at 0-1 (~90-95MiB). This falsifies the dev's close-out/notebook claim "MemPerc ~95%→~9-11%" as durable — it is a restart artifact. AC-4 (headroom ≤80%) and the live-burst clause of AC-2 (real burst against the running container) were never actually satisfied — only a synthetic unit-test stood in. Recorded as an explicit, non-blocking correction in `status_note` per verify-raw-not-badges, NOT silently accepted and NOT folded into this row (root driver is a separate, undiagnosed mechanism out of this row's scope/title) — recommended PO mint a new row for parent-process RSS accumulation.

VERDICT: APPROVED (mechanism-level — concurrency invariant real/on-path, falsification independently RED→GREEN reproduced, ratchet broken, backpressure live-firing, observability live-confirmed, zero regressions, 3/3 import-linter). AC-4/live-AC-2 explicitly flagged UNMET, not silently waved through. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation task_total 663=663/signal_total 130=130 — same write as status flip, CANONICAL:SSOT-STATUSFLIP-LANEMOVE). `qa_commit=4bac2b85d`. DJ: `sprint-FIX-PDFX-TESSERACT-CONCURRENCY-qa.md` §qa-S1..S3.

## cycle-490 · 2026-07-28 · UC-GCP-P8 — APPROVED, DONE_VERIFIED (direct-commit verify, oldest row in an 83-deep review-lane class, stranded-state-sweep.sh classifier)

Row `qa[]`, `branch:null` (dev-team Review-Lane QA-Drain, 5-day-old stranded row). `dev_commit=231860c6a`, main-ancestor confirmed, `git show --stat` confirms all 7 claimed files touched. Re-ran `stranded-state-sweep.test.sh` myself — 19/19 PASS, exit 0 (matches dev claim). Per dispatcher's explicit ask, also ran a LIVE 5-day-drift sanity check (`--plan` against the CURRENT 44-entry dirty tree, not the dev's 2026-07-23 57-entry snapshot): exit 0, valid JSON, 7 owned-elsewhere/5 young-skipped/20 unknown(cap-enforced)/0 auto-commit — classifier still healthy, no regression.

Grep-confirmed the "zero git/orch-state writes itself" claim: no `git add`/`commit` in the script, `ORCH_STATE` touched only via one read-only `jq` dedup probe. The commented `# task_claim(...)`/`# task_release(...)` lines in `post-cycle.md` Step 4.3 are NOT a defect — cross-checked Step 4.2 immediately above, identical established commented-pseudocode mutex convention already live in that file. `shellcheck -S warning` (project bar, cycle-486 precedent): both script+test clean, exit 0 (default-severity run flagged only 2 info-level SC2015/SC2329 on the test file). mock-guard/secrets/process.env clean; Smart-Skip applies (shell+test+docs only, zero `.ts`).

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` in the same write as the status flip (CANONICAL:SSOT-STATUSFLIP-LANEMOVE); conservation `task_total` 662→663 expected (`qa[]` lane sits outside the conservation formula's `FLAT_TASK_LANES`, so this lane-move nets +1 by design, not data loss). `.head` synced idle (`next_agent:"router"`) per dispatcher instruction — terminal, no further routing. Task Report: `reports/TASK_REPORT_UC-GCP-P8.md`. DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S19.

## cycle-489 · 2026-07-28 · UC-GCP-P1 — APPROVED, DONE_VERIFIED (direct-commit verify, oldest row in a 117-deep review lane, docs/policy consolidation)

Row `qa[]`, `branch:null` (dev-team Review-Lane QA-Drain, first live fire in days). No `commit`/`files[]` on the row — derived from `review_note`'s own cited hashes, both confirmed `git merge-base --is-ancestor main`: `7dcf90919` (impl) + `8c6d71683` (memory/journal). Did not trust the review_note prose; independently re-verified all 6 acceptance points against the live tree: (1) `docs/policies/commit-convention.md` exists, 140L single file, format matches real recent `git log` (slug task IDs, `Task:`/`AC:` trailers, feat/fix/chore/docs types all observed live). (2) All 3 sibling docs (`-format.md`/`-exemptions.md`/`-examples.md`) confirmed deleted, zero remaining references to their filenames outside `docs/WORK.md`/decision-journal/audit-brief prose (descriptive, not live callers). (3) Grepped BOTH `commit-convention.md §` refs and bare path refs repo-wide: the 2 dangling `.claude/knowledge/commit-convention.md` callers the row named (`commit-convention-audit.sh:33`, `2026-05-17-commit-convention-audit.md:51`) are confirmed fixed; remaining `.claude/knowledge/commit-convention.md` hits are all pre-existing archival docs (old `TASK_18xx`/`reports/`/`docs/archive/`), zero hits in any live `docs/agents/*/flow/*.md` or `.claude/skills/*`. (4) `-a`/`-am` rule: diffed the new SSOT lines 38-39 against the pre-consolidation `commit-convention-format.md:36` (via `git show 7dcf90919^:...`) — carried forward verbatim, same wording/incident citation. (5) `commit-convention-audit.sh`: diff is exactly an 11-line header-banner + comment fix (loop logic untouched) — deprecated in place, not silently stale; independently re-grepped zero live invocation paths outside itself. Live full run of the script itself is a 10,746-commit pure-bash loop over the full `--since 2026-05-10` window — pre-existing O(n) design, unrelated to this task's diff; killed after >40s CPU with no output rather than burn host resources already near memory ceiling — script content review substitutes for a full live run here since behavior is fully explained by the diff. (6) `tree-map.md:74` — 4-file subtree collapsed to 1 consolidated node, no orphans. Zero `src/`/`apps/` production files touched by either commit → tsc/DDD/bun-test out of scope by Smart-Skip; quick secrets/`process.env` grep on the touched shell script clean.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` in the same write as the status flip (CANONICAL:SSOT-STATUSFLIP-LANEMOVE). `.head` left idle, untouched (was already idle at dispatch). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa.md` §qa-S1.
