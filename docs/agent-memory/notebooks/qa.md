# QA — Notebook

## cycle-460 · 2026-07-16 · UC-CRITIC-GATEWAY-CONTRACT-DRIFT — PASS, DONE_VERIFIED (doc-only mechanical rename)

RAW-verified fixer commit `2ec39e96c` (mechanical doc-only rename `mcp__claude_ai_gateway__`→`mcp__gateway__`, 8 target files + 1 fixer decision doc, 9 total). `grep -rn claude_ai_gateway` over the 8 target files → exactly 1 hit, `gateway-call-contract.md:16` — the bad-server-**VALUE** exclusion list for the `server=` argument (paired with `vnmarket`/`vn_market`), read in full context, NOT a tool-prefix and correct to keep verbatim. `jq empty quality-checklist.json` exit 0. `git show --name-only 2ec39e96c | grep -E '^(apps|packages|scripts)/'` → zero matches, confirmed zero code paths touched. Read `gateway-call-contract.md` L10-20 directly: L13 now shows the canonical `mcp__gateway__call_tool(server="vn-market", tool="<bare_name>", arguments={...})` block. All 4 gate validations PASS.

VERDICT: PASS. Promoted `UC-CRITIC-GATEWAY-CONTRACT-DRIFT` `review[]`→`done_verified[]` (status=DONE_VERIFIED + qa_note/qa_verified_at/qa_verified_by) via `jq | scripts/orch-apply.sh` — validator PASS, conservation PASS (task_total 542→542, signal_total 0→0). Both `.head`/`.task_board.head` set to matching idle shape (next_agent=router). POST-SHIP PM-ACTION (pre-approved architect+pm): struck P9 bullet from `UC-RDL-UNVERIFIED-BATCH` note (9→8 remaining proposals) and P12 bullet from `UC-CCA-UNVERIFIED-BATCH` note (8→7 remaining proposals) — both rows unchanged `type:SPIKE` in `backlog[]`, zero task rows removed, each note annotated with a struck-trail pointer. Did not push (router's job). DJ: `sprint-ULTRACODE-AUDIT-FIXALL-qa.md` §qa-S7.

## cycle-461 · 2026-07-16 · FR-DEGRADE-01-FIX — APPROVED, DONE_VERIFIED (root-cause gap closed)

RAW re-ran dev-mcp-server commit `00dca96fe` myself (not trusting the 38/0 claim): `bun test 240-bctc-full.test.ts 1982-quality-burndown-CHIJ.test.ts` = 38 pass/0 fail, 107 expect(). Read the 3 new tests directly: stale-flagged case (`insertStaleBctcPush(3)`, 72h>48h SLA) asserts `content[0]` still contains `=== BCTC SUMMARY: VCB ===` (never withheld) AND `FR-DEGRADE-01` note, `content[1]` parses to `stale===true`+`stale_since` string+`stale_age_hours>48`, underlying `structured_data.net_profit` still served; fresh-push and no-vps_push_log-rows cases both assert fail-open `stale===false`/`stale_since===null`. `bun tsc --noEmit` exit 0. `mock-guard.sh --files bctcFullTools.ts` PASS (no fabricated-data pattern).

Code-reviewed the diff directly (not just the tests): the pre-existing `bctcVpsStaleSince`/new `bctcVpsStaleAgeHours` computation (empty-catch fail-open around the `vps_push_log` query — missing table/no rows → stays `null` → `stale=false`) is now threaded into the ORDINARY success path (data-present, `latestRow` truthy) — `textOutput` gains a `staleNote` array-join when non-null, `structured_data` gains `stale`/`stale_since`/`stale_age_hours` fields — not only the `!latestRow` branch (which already had it from the 2026-06-10 fix). Convention matches `DS-DEGRADE-01`'s `{stale, stale_since, source}` shape in `get_public_contracts`. DJ-GATE-1 pre-check: `sprint-FLOW-PRICE-ALPHA-LOOP-dev-mcp-server.md` carries a matching task-id entry.

Full-suite background reds (1518-foreign-flow timeouts, 1407b-coverage-map `market_messages`) confirmed zero file overlap with the 2 changed files (`bctcFullTools.ts`, `240-bctc-full.test.ts`) — known pre-existing flaky class, not blocking.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation preserved). `.head`/`.task_board.head` kept idle (was already idle). Committed+pushed (carries the 3 unpushed dev commits `00dca96fe`/`22050845b`/`ef60d2964` FF). Task Report: `reports/TASK_REPORT_FR-DEGRADE-01-FIX.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S17.

## cycle-462 · 2026-07-16 · FR-OBS-01-FIX — APPROVED, DONE_VERIFIED (mis-channeled alert now hits WORK)

RAW re-ran dev-mcp-server commit `7ce61568e` myself (not trusting 11/0): `bun test 316-bctc-overdue-check.test.ts` = 11 pass/0 fail/39 expect(); also re-ran the 3 cited siblings (1358a/1303i/1050) together = 37 pass/0 fail/116 expect() across 4 files, corroborating the "26/26 across siblings" claim. `bun tsc --noEmit` exit 0. `mock-guard.sh --files bctcOverdueCheckJob.ts schedulerJobTable.ts` PASS.

Read the diff directly: `alerts` insert (`insertAlert.run`) byte-identical pre/post-fix — still feeds `get_alerts` + cascade chain. New WORK-channel send sits INSIDE the same `info.changes>0` guard that gated the alert insert itself (per-week dedup id — 3rd new test proves no double-fire on same-week re-run). `telegram.ts` (home of the shared BUG-only `notifyTelegramAlert`, telegram.ts:592) is NOT in the changed-file list — confirmed `git show --stat` — so the shared HIGH/CRITICAL BUG dispatch for every other alert type is provably untouched, zero regression. `schedulerJobTable.ts` diff confirmed comment-only (cron registration object untouched).

Full-suite: did not re-run 14575 tests (Smart-Skip — scheduler bugfix, no new domain/tool/cross-service); `grep -rl "bctcOverdueCheckJob\|schedulerJobTable" src/__tests__/` against the flaky-class name patterns (vps_push_log/insider-tx/OCR-cache/foreign-flow) → zero matches, structurally corroborating dev's zero-overlap claim.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation preserved). `.head`/`.task_board.head` kept idle. Task Report: `reports/TASK_REPORT_FR-OBS-01-FIX.md`. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S18.

## cycle-463 · 2026-07-21 · FIX-DRAIN-PAYLOADREF-DANGLE-ON-MOVE — CHANGES_REQUESTED (ENOBUFS silent-swallow reproduced against reviewed commit at live file size)

Commit `84096f617` message is a peer's (shared-main sweep); verified FILE CONTENT via `git show 84096f617 -- scripts/agents-flow/drain-signals.js` — matches dev's review_note claim (movedRefs map + repointPayloadRefs() jq filter, orch-apply.sh-gated, bound `--argjson`, no string interpolation). Independently re-ran (isolated mkdtemp harness, exact commit content, never touched live orch-state.json) → 18/18 pass, matching dev's self-report. All 3 verification_gate assertions independently confirmed: (1) file→`processed/`, (2) `payload_ref` repointed fragment-preserving, (3) `bun scripts/orch-validate.mjs` exits 0. Non-vacuity: row confirmed referenced pre-drain + repointed post-drain; negative control (pre-fix `84096f617^`=`64cd7edf9`, zero signal_queue/payload_ref refs confirmed by grep) reproduces the dangle live, `orch-validate` exit 2.

Extended stress test beyond the row's literal verification_gate text: live `docs/data/orch/orch-state.json` is 1,114,264 bytes TODAY — built an isolated >1MB fixture at that real size and ran it against the REVIEWED commit's `repointPayloadRefs()`. Its `jq` `execFileSync` call has no `maxBuffer` (Node default 1,048,576B) — throws `ENOBUFS`, caught by a NON-fail-loud branch (`console.error WARN...non-fatal...; return`, drain exits 0) — payload_ref stays dangling, `orch-validate` Stage 1c then exit 2 on the next fleet write. Reproduces the row's own target defect class via a size-triggered path, live and current (not hypothetical), and contradicts the dev's own review_note design rationale ("silently swallowing that failure would just reproduce the exact bug being fixed"). Found: a candidate fix already sits UNCOMMITTED in the working tree (maxBuffer:64MB + converts this catch to fail-loud `process.exit(1)` + new ENOBUFS regression scenario) — ran it → 22/22 pass. Also empirically confirmed (separate isolated harnesses) the skip-when-no-match no-op path (byte-identical file when no row matches moved files) and the CAS-retry contract (exit-2 semantics match `orch-apply.sh`'s documented contract, fresh-candidate-per-attempt; single-writer CAS-gated write exercised successfully end-to-end in the gate scenario — multi-writer race not stress-tested, bounded effort).

VERDICT: CHANGES_REQUESTED (round 1). Row held in `task_board.review[]` (status stays REVIEW, in-place annotate — precedent po-s140 "stays in review[], no promotion"), `next_agent`→developer, `qa_verdict`/`qa_blocking_issue`/`qa_test_suite_result` fields added. `.head` updated to point next_agent=developer with the fix pointer. `orch-state.json` NOT committed (per router directive — dirty, riding a separate fleet-push; left for owning fleet-push/PO). DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S19.

## cycle-464 · 2026-07-21 · FIX-DRAIN-PAYLOADREF-DANGLE-ON-MOVE — APPROVED, DONE_VERIFIED (qa-S19 blocking issue closed)

Follow-up pass on the qa-S19 CHANGES_REQUESTED (ENOBUFS silent-swallow at live file size). Fix landed split across 2 unrelated bare `git commit -m` (84096f617 router "fix(auditor-tier1)...", 0e28eed23 cowork-tick "chore(cowork): tick 16:15Z...") — verified CONTENT in HEAD via `git show --stat`+`git log -p`, not tree cleanliness (tree-clean is meaningless evidence here). Own suite `node scripts/agents-flow/drain-signals.test.js` 22/22 PASS. Read the fixed branches directly: line ~263 `!result.changed` still `return`s quietly (no exit); line ~243 `execFileSync(..., {maxBuffer: JQ_MAX_BUFFER})` with `JQ_MAX_BUFFER=64*1024*1024`; line ~261/285/289 catch blocks now `process.exit(1)` (was WARN+return).

3 independent re-derivations (never touched live orch-state.json): (a) benign-branch — seeded orch-state with a signal_queue row pointing at an unrelated (non-moved) file, ran drain → exit 0, stderr "no signal_queue rows referenced moved files", file byte-identical before/after. (b) RED-before — stripped `maxBuffer` from a temp copy of the script (simulating pre-fix), built the SAME >1MB (1,300,576B, confirmed >1,048,576B) fixture, ran it: exit 1/`spawnSync jq ENOBUFS`/payload_ref stuck at pre-move path; re-ran identical fixture against live HEAD script: exit 0/payload_ref correctly repointed — confirms the precondition assertion is real, not tautological. (c) caller-reachability — `drain-signals.md`:86 shows the specialist runs this via Bash; `main.md`:56 declares `cowork-error-boundary` SKILL ("ANY tool call fails after 1 retry" → BUG telegram + EXIT) as the flow-wide error boundary, so a non-zero Bash exit is not silently swallowed by the dev-team caller.

VERDICT: APPROVED, DONE_VERIFIED. Board row moved `task_board.review[]`→`done_verified[]` via `jq`+`scripts/orch-apply.sh` (Zod+conservation 563=563+CAS all PASS), `next_agent` removed via `del()` not null (promotion lesson). `.head` reset to idle. DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S20.

## cycle-465 · 2026-07-21 · FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP — CHANGES_REQUESTED (serving path still corrupt/absent for 15/16 flagged tickers)

Per router mandate, verified through `get_bctc_full` (serving path), NOT the DB plane, now that pdf-extractor is healthy (docker: "Up 33 minutes (healthy)"; `bctcExtractReconcileJob`/`bctcPdfPullJob` both `last_status=success` within 5min of the probe — reconcile ran essentially concurrent with this check). Live-probed all 16 tickers named in the row's PLANE-4 evidence:

- 8/16 STILL `[CORRUPT DATA — SKIP] total_assets=0`: CTG, DGC, FRT, GEX, VJC, DXG, KDH, PDR.
- 7/16 now "Chưa có dữ liệu BCTC" — no row at all (regressed further, worse than corrupt): D2D, KDC, SAB, VND, DBC, MSN, SHB.
- 1/16 (VCB) serves plausible non-zero data (total_assets=2,550,963.3 tỷ VND, confidence 75%, validation=passed).

Controls outside the 16-list (ACB, FPT) both serve correctly with plausible non-zero data — confirms `get_bctc_full` itself works; the failure is data-plane, not tool-plane. `get_earnings_calendar` deliberately NOT scored per router's explicit 13/15 false-green warning (corroborating only — still shows all 16 as ĐÃ NỘP with unchanged 07-19/07-20 NGAY NOP). The developer's write-back guard (skip write when existing total_assets>0 AND new write would set total_assets<=0) is structurally correct but cannot repair rows that are ALREADY at total_assets=0 or ALREADY absent — remediation of the 16 was explicitly scoped OUT to separate rows (OPS-BCTC-REFINE-REPASS-NONBANK-5T, W5-FU-CTG-REFINE-96e36139) by the developer's own journal, but this row's own `verification_gate` text requires "get_bctc_full for 3 previously-corrupt tickers returns servable data with non-zero total_assets" — tested 9, only 1 passes.

VERDICT: CHANGES_REQUESTED (round 1). Board row moved `task_board.review[]`→`task_board.in_progress[]` (status IN_PROGRESS, `next_agent` stays dev-mcp-server, `qa_verdict`/`qa_blocking_issue`/`qa_reviewed_at` fields added) via `jq`+`scripts/orch-apply.sh` (Zod+conservation 563=563+CAS PASS, same write as cycle-464). `.head` reset to idle. Next round: do not re-verify via calendar status alone; require `get_bctc_full` non-zero `total_assets` across the 16 (or an explicit router/PO-signed scope split naming which subset this row actually owns vs the separately-tracked remediation rows). DJ: `sprint-FLOW-PRICE-ALPHA-LOOP-qa.md` §qa-S21.
