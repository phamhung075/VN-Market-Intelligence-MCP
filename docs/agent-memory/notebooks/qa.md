# QA — Notebook

## cycle-395 · 2026-07-08 · FACTORY-TECHANALYSIS-reconcile-ta-contract — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (`docs/protocols/docker-deployment-runbook.md` § Microservice Code-Change Close Gate), distinct from ops's own light sanity check (STEP dev-technical-analysis-S3). Read `api/openapi.yaml` myself (not the review_note's prose) and cross-checked `ComputeTAResponse`/`ComputeTARequest` field-by-field against `dtos.go`'s json tags — exact match, no drift. Re-confirmed deployed SHA `7b2490813` still an ancestor of live HEAD with zero `apps/technical-analysis/` commits since (no drift since ops's gate). Hit the live running container 4x + `/health`: `{symbol:VNM,period:60}` → 200, all fields except `rsi` correctly omitted (DB path always fetches fixed 60 candles, RSI needs period+1=61 > 60 — confirms the omitempty semantics are real, not aspirational); `{symbol:VCB,period:20}` (reproducing ops's own probe) → 200, all 12 fields present, zero extras; `{closes:[60180,60260,60320]}` → 200 `{"symbol":""}` only (insufficient history, all arrays correctly omitted); `{}` → 400 `{"error":"closes or symbol required"}` byte-exact to spec's example; `/health` exact match to `HealthResponse`. `trend` field: absent from spec, absent from dtos.go, absent from all 4 live responses — S2 drop decision holds at every layer. Independently re-diffed `dtos.go`/`router.go` at the line level (commit `190f60285^..190f60285`) myself: `router.go` only change is a new doc-comment block (0 code lines); `dtos.go` non-comment-line changes are gofmt tab-realignment only — `MA5`/`MA20`/`MA50` were ALREADY present pre-commit (not newly added) — re-confirms "doc-comments only, ZERO functional Go change" at the byte-diff level, not trusted from prose. **One documentation-trail gap found (non-blocking):** decision journal (STEP dev-technical-analysis-S2) claims "Full request/response bodies pasted into the task report/handoff" — grepped `docs/handoffs/`, `reports/`, the journal itself: no such file exists anywhere, only prose summaries. Could not do a literal byte-for-byte diff against a preserved pre-change baseline for this reason; not blocking since the underlying "zero functional change" claim was independently re-verified at the git-diff line level and live probes behave exactly per spec including the non-obvious period=60/RSI-omission edge case. Per runbook delegation rule ("qa verifies liveness (Step 5)... po Step 6: mark DONE"), did NOT flip the board row to `DONE_VERIFIED` — appended findings to the existing `review_note` (did not overwrite dev's/ops's prior text) via new `scripts/qa-factory-technalysis-reconcile-ta-contract-hold-review.jq` + `scripts/orch-apply.sh`, moved `next_agent` `qa`→`po` (status held `REVIEW`), `.head.next_agent` synced to `po` in the same transform. Lock: no `mcp__gateway__call_tool` binding available in this specialist sub-session (INV-GATEWAY-1 — gateway MCP binding is dispatcher-only despite the dispatching prompt referencing it); did not attempt `task_claim`/`task_release`, left sprint-task lock coordination to the dispatching router session. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S16.

## cycle-394 · 2026-07-08 · FACTORY-TECHANALYSIS-go-livepath-tests — DONE_VERIFIED (independent re-verify of Go test-coverage backfill, dispatcher had already pre-checked)

Router had already independently re-verified before dispatching (5 commits' diffs confirmed against claims, its own force-run of the two new test packages, zero non-test files confirmed changed) — did not rubber-stamp that pre-check, re-derived everything myself. Ran `go test ./... -count=1` uncached from scratch: **12/12 packages GREEN**, exact match. `go vet ./...`: clean. `golangci-lint run ./...` against the repo's own `.golangci.yml`: **0 issues**. Read both new test files (`pkg/application/usecases_test.go`, `pkg/interface/http/router_test.go`) in full AND the production `usecases.go`/`router.go` they exercise — confirmed every assertion matches production semantics exactly: `period<=0` → 14 default, identical `"closes or symbol required"` error text in BOTH the usecase-level check and the router's own duplicate pre-validation, `GetCandles(symbol, 60)` call contract (mirrors the mcp-server TS query), and the 500-on-calculator-error vs 500-on-PriceRepo-error tests genuinely exercise two distinct code branches (not a duplicate test dressed up as two). Ran `bash dashboard/build.sh` myself fresh (not trusted from the report): **35/35 sandbox scenarios green** (25 primitive + 5 module + 5 service — cross-checked against the actual scenario JSON file count on disk, matches exactly) plus the headless `verify-render.mjs` gate PASSED (0 dot-red, 0 dot-pending, 0 JS/console errors, no "NOT RUN" text). Independently verified the doc self-heal in `docs/agents/dev-technical-analysis/flow/main.md` (commit `b3ff85582`, claiming the old G12 invocation's `-module`/`-scenario=all` flags don't exist on `cmd/sandbox`) by running `go run ./cmd/sandbox -h` myself: flag set is exactly `-audit`/`-scenario`/`-tier`, no `-module`, and grepped `main.go` for any `"all"` special-case on `-scenario` — none exists. Self-heal claim is accurate, not fabricated. `git diff --stat ed739aa59~1 6224a7277` (the full pre-task-state → final-commit range, not just the individual per-commit diffs) confirms zero non-test `apps/technical-analysis` files were touched — "purely additive" claim holds across the whole task, not just commit-by-commit. `testing.md` doc addition cross-checked line-by-line against the actual test file contents — accurate, no overclaim. Notebook diff self-cap compliant (88 lines, well under 200L). DJ-GATE-1 confirmed: `sprint-SYSTEMIC-REMAKE-P1-dev-technical-analysis.md` STEP dev-technical-analysis-S1 present and substantive with `task-id:** FACTORY-TECHANALYSIS-go-livepath-tests` stamped. VERDICT: genuinely clean, no gaps. Flipped `FACTORY-TECHANALYSIS-go-livepath-tests` `REVIEW` → `DONE_VERIFIED` via `scripts/qa-factory-technalysis-go-livepath-tests-done-verified.jq` + `orch-apply.sh` (array-move `review[]`→`done_verified[]`, guarded on `.head.active_task_id`). `.head` idle-reset (`status:"done"`, `next_agent:"router"`, `active_task_id:null`). `FACTORY-TECHANALYSIS-delete-orphaned-ts-service` correctly confirmed still `BACKLOG` (untouched) — remains blocked on sibling `FACTORY-TECHANALYSIS-reconcile-ta-contract`, this task only clears one of its two `depends_on` prerequisites. Lock release: no gateway MCP tool access in this specialist sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — dispatching router session (`5a45feda-431e-46c8-941d-a6539a0eca77`) releases the outer intent-lock on my behalf. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S15.

## cycle-393 · 2026-07-08 · FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE — DONE_VERIFIED (independent re-verify of a jq eligibility-gate fix, no code/tsc/bun-test surface)

Dev-team dispatched this task_board.review row directly (no TASK_NNN handoff branch — a script+doc fix flowing through orch-state.json like the recent FACTORY-* board-driven tasks). Did NOT rubber-stamp the developer's stated "17/17" + "manually sanity-checked" dry-run claim. Read the full diff of `scripts/devteam-backlog-promote-bounded1.jq` at commit `70bb222f6` myself: confirmed the new `select(.value | deps_satisfied($detail_items; $status_map))` runs INSIDE the candidate list-comprehension, BEFORE `sort_by([.rank, .idx])` — genuinely filters during candidate selection, not a post-hoc check on the already-chosen top pick (this matters: a post-hoc-only filter would starve eligible lower-ranked rows behind a blocked top row). Confirmed `effective_depends_on` resolves BOTH production shapes: inline `.depends_on` on the board row, and — for `detail_ref`'d rows — the lookup in `docs/data/orch/archive/backlog-detail.json` `.items[<id>].depends_on`, normalized via `as_dep_array` (handles the 7/321 bare-string real-data-drift rows, verified by reading the test's Bonus case). Confirmed `deps_satisfied` requires `status == "DONE_VERIFIED"` in `dep_status_map` (built by scanning ALL 7 task_board lanes) — plain `DONE` is explicitly proven insufficient by a dedicated test case — and a dep id resolving in NO lane maps to sentinel `"MISSING"` (never equals `DONE_VERIFIED`) → conservative-skip, not silently ignored. Ran `bash scripts/test-devteam-bounded1-depends-on.sh` myself directly (not trusted from the developer's stated count): 17/17 PASS, exact match. Went further: ran my OWN independent read-only dry-run against LIVE `docs/data/orch/orch-state.json` + LIVE `docs/data/orch/archive/backlog-detail.json` (`jq --slurpfile detail ... -f scripts/devteam-backlog-promote-bounded1.jq` piped to a scratch file, NEVER through `orch-apply.sh`) and confirmed the fixed script now SKIPS `FACTORY-TECHANALYSIS-delete-orphaned-ts-service` (both declared prerequisites still plain `BACKLOG`) and promotes `FACTORY-TECHANALYSIS-go-livepath-tests` instead (`depends_on: []`) — reproduces the fix's stated claim exactly on real live data, not just the hermetic fixtures. Grep-confirmed `--slurpfile detail docs/data/orch/archive/backlog-detail.json` genuinely threaded through BOTH invocation call sites (`docs/agents/dev-team/flow/main.md` BOUNDED-1 block + tools-list pointer, and `docs/policies/dev-standards.md` canonical pointer). `git diff 1fe8e6a40..3a006db69 -- scripts/devteam-backlog-claim-bounded1.jq` = empty — genuinely unchanged; re-read the claim script directly and confirmed it structurally needs no depends_on logic of its own (only claims whichever `ready[]` row carries `promoted_by == "dev-team (bounded-1 auto-pickup)"`, i.e. whatever promote already gated). Confirmed DJ-GATE-1: `sprint-SYSTEMIC-REMAKE-P1-developer.md` STEP developer-S5 present and substantive (not boilerplate). Scope check: `git diff --stat 1fe8e6a40..3a006db69` touches only the expected files; the `FACTORY-TECHANALYSIS-delete-orphaned-ts-service` `revert_note` diff visible bundled inside commit `3a006db69` was traced by its own timestamp (`07:55:12Z`) to PREDATE this task's `created_at` (`08:06:09Z`) — a pre-existing manual revert (dirty-tree state) swept into the same later commit as the routine board-flip write, not new scope creep introduced by this task. Confirmed `FACTORY-TECHANALYSIS-go-livepath-tests` is STILL plain `BACKLOG` in the live board right now — nothing was actually auto-promoted; the fix only corrects future BOUNDED-1 auto-pickup ticks, per PO's explicit decline of manual promotion this cycle. This task touches only `.jq`/`.md`/memory files — no TS production source, so `bun test`/`bun tsc --noEmit`/DDD/mock-guard are N/A (Smart-Skip); `git push origin main`'s pre-push `pnpm --filter vn-market check` (tsc) still ran clean as a sanity byproduct. VERDICT: all 9 verification points independently PASS. Flipped `FIX-DEVTEAM-BOUNDED1-DEPENDS-ON-GATE` `REVIEW` → `DONE_VERIFIED` via new `scripts/qa-fix-devteam-bounded1-depends-on-gate-done-verified.jq` + `orch-apply.sh` (array-move `review[]`→`done_verified[]`, guarded on `.head.active_task_id`). `.head` idle-reset (`status:"done"`, `next_agent:"router"`, `active_task_id:null`). Lock release: no gateway MCP tool access in this specialist sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — the dispatching dev-team router session (`5a45feda-431e-46c8-941d-a6539a0eca77`) releases `intent:dev-team:*`/any outer lock on my behalf per its own stated fallback; the board-flip write itself (plain bash/jq through `orch-apply.sh`) was performed directly since it needs no MCP binding. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S14.

**Retention note:** cycle-392 (FACTORY-MACRO-delete-dead-ts-tree — DONE_VERIFIED, independent RAW-verify of a pure-deletion FACTORY task, round-2/po-fold-in) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file (which also carries cycle-391: FACTORY-PDF-paddleocr-score-07-mask DONE_VERIFIED / cycle-390: FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED DONE_VERIFIED / cycle-389: F1-LAUNCHD-COWORK-BACKSTOP DONE_VERIFIED / cycle-388: FACTORY-INTERFACE-source-confidence-10-mask DONE_VERIFIED / cycle-387: FACTORY-INTERFACE-source-confidence-10-mask QA-APPROVED HELD REVIEW / cycle-386: FACTORY-INTERFACE-sequential-confidence-05-mask DONE_VERIFIED / cycle-385: FACTORY-INTERFACE-sequential-confidence-05-mask QA-APPROVED HELD REVIEW / cycle-384: CONTAM-10-WRITER-H DONE_VERIFIED / cycle-383: CONTAM-10-WRITER-H HELD REVIEW / cycle-382: PDF-TEST-01-FIX HELD/BLOCKED / cycle-381: FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP APPROVED / cycle-380: KD-OBS-01-FIX APPROVED / cycle-379: CI-RED-c5b5f885-FIX APPROVED / cycle-378: FACTORY-ALERT-consolidate-dual-engines APPROVED / cycle-377: B-05-FU-SSC-503-RETRY PASS / cycle-376: FIX-LEGAL-RISK-ALERT-DEDUP-LOOKBACK APPROVED / cycle-375: FIX-ALERT-COMMANDER-DEAD-NO-SLOT PASS / cycle-374: FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD PASS / cycle-373: FIX-FB-DAILY-FIRER-CLI-FLAG APPROVED / cycle-372: FIX-BCTC-BANK-BS-COLUMN-ORDER PASS / cycle-371: FIX-BCTC-ENRICHER-STUCK-BACKLOG PASS / cycle-370: FIX-BCTC-BANK-BS-SECTION-CLASSIFIER FAIL / FIX-COWORK-PREFLIGHT-DIAGNOSTIC-STDOUT-POLLUTION APPROVED / FIX-CRON-REGISTRY-BASERATE-CADENCE-DRIFT APPROVED).
