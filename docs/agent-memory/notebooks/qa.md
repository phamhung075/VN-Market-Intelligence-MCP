# QA — Notebook

## cycle-708 · 2026-08-14T04:12:49Z · FIX-AUDITOR-T1-PREGATE-MEMCREEP-SINGLE-POINT-SAMPLE (report-analyzer, commit `2e2446853`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with NO `commit`/`files[]` — predated the drain entirely (`claimed_at` was this same tick, but the underlying fix landed 2026-07-30). Derived the commit myself via `git log --all -- scripts/agents-flow/auditor-tier1-probe.sh`, cross-checked message/date against the row's own title, `acceptance`, and `promoted_at`: commit `2e2446853` (2026-07-30T10:04:02+02:00, developer, BOUNDED-1 auto-pickup). Confirmed ancestor of both `main` and `origin/main`; `git show --stat` matches exactly `scripts/agents-flow/auditor-tier1-probe.sh` + `.test.sh` (plus `docs/WORK.md` + the commit's own decision-journal doc) — nothing else.

Read `_check_mem_creep()` at source, not the commit-message narration: `MEM_CREEP_SAMPLES` (>=2, default 2, clamped up if invalid) / `MEM_CREEP_SAMPLE_INTERVAL_SEC` (default 2s) top-of-script knobs; the per-container check now loops N `docker stats` samples and gates ALL_GREEN/FAILURE off the WORST (max) parsed pct observed in the window — a breach in any single sample forces FAILURE for that container even if another sample in the same invocation reads comfortably GREEN. Any sample's stats-unavailable/unparseable still breaches (unchanged fail-loud, now applied per-sample).

Independently re-ran `bash scripts/agents-flow/auditor-tier1-probe.test.sh` myself (did not trust the commit's own pasted 181/181 figure — the file has grown since): 226/226 PASS on current HEAD, including T54-T58. T54 reproduces the cited live incident verbatim: sample1=70.00% (would read GREEN under the old single-point gate) then sample2=99.91% (the exact pdf-extractor MemPerc figure from the incident) → verdict FAILURE, detail names the WORST sample not the first. T55 proves worst-of-window not last-sample via reversed sample order. T56-T58 prove the loop actually calls `docker stats` N times (file-based call-count assertion, guards against a fix that reads the knob but never loops) and that `MEM_CREEP_SAMPLES<2` is defensively clamped back to the AC's floor.

`shellcheck -x -e SC1091` on both changed files: the 2 warnings in `auditor-tier1-probe.sh` both git-blame-confirmed to predate this commit (2026-07-21/2026-07-16, outside its diff hunks). `auditor-tier1-probe.test.sh` carries 8 more warnings — 7 pre-existing (blame-confirmed), 1 new cosmetic `SC2034` (`MEM_CREEP_SAMPLES=2` reset at the end of the T57/T58 block) — same defensive-reset idiom already used a few lines above for `LAUNCHD_DIR`/`LAUNCHD_ACK`, not new debt, non-blocking. `mock-guard.sh` N/A (pure bash zone, no TS/JS production source touched). Live sanity: ran the actual script against real docker (`MEM_CREEP_SAMPLE_INTERVAL_SEC=1`) — multi-sample loop demonstrably live and operating, returned a real FAILURE on an unrelated, pre-existing rag-service memory condition (out of this task's scope, not re-signalled here).

DJ-GATE-1: developer's own journal confirmed present, `sprint-FIX-AUDITOR-T1-PREGATE-MEMCREEP-SINGLE-POINT-SAMPLE-developer.md` (task-family precedent: TASK_ID used as SPRINT_ID, not the mechanically-resolved but unrelated active sprint), 3 STEP entries, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. Both ACs (N>=2 samples over a short window; regression test reproduces the observed GREEN->FAIL flip) independently reproduced from source/live re-run, not trusted from commit-message prose. `[QA] Review Record (direct-commit verify)` + derived `commit`/`files[]` written to the row (had none — direct-commit row predating the drain, no handoff file). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 732=732). `.head.active_task_id` was NOT this task (`FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING`) — left untouched per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116/S117/S119). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-AUDITOR-T1-PREGATE-MEMCREEP-SINGLE-POINT-SAMPLE` and is the authoritative release path.

## cycle-707 · 2026-08-14T03:33:17Z · FIX-STRANDED-SWEEP-CLASSIFY-AGENT-MODEL-SWITCH (report-analyzer, commits `5737ac189`+`2a8530c72`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — `stranded-state-sweep.sh`'s classifier mis-fired ~40% of the time: (M1/AC3) the UNKNOWN bucket had no mtime age gate unlike AUTO-COMMIT, so a file an agent was actively editing was reported stranded every tick; (M2/AC1/AC4) `_is_owned_elsewhere()` had no pattern for agent-model-switch churn or several routine agent-output artifacts. Did not trust the developer's claimed 25/25 (19 pre-existing + 6 new) count or AC-satisfaction claims — re-verified everything independently.

Both commits confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches exactly the row's claimed scope: `scripts/agents-flow/stranded-state-sweep.sh` + `.test.sh` (code), `docs/policies/dev-standards.md` + `docs/WORK.md` (docs) — nothing else, and critically neither `.claude/agent-models.json` nor any `.claude/agents/*.md` appears in either commit's diffstat (scope_out honored).

Read the diff at source, not narration: new `_is_model_switch_only()` runs `git diff HEAD --unified=0` on the target path, strips to +/- content lines, and requires EVERY line to match a narrow `current_mode`/`model` value-line ERE — an empty diff, unparseable diff, or any unmatched content line returns 1 (fail-safe default: falls through to UNKNOWN, never silently owned). AC3: the UNKNOWN bucket (script L172-179) now runs the identical `_age_hours()`/`SSS_AGE_HOURS` young-skip block AUTO-COMMIT already used, deletions still exempt. AC4: `_is_owned_elsewhere()`'s case-glob extended with `docs/data/auditor-dedup-ledger.json`, `docs/data/DASHBOARD.md`, `docs/data/unified-agent-synthesis-*.json`, `docs/social/fb-post-*.md` — confirmed verbatim in the case statement.

Independently re-ran `bash scripts/agents-flow/stranded-state-sweep.test.sh`: 25/25 PASS, matching the claim exactly. Cross-checked the 19-pre-existing/6-new split myself (not trusted from prose) by diffing distinct `ok "<name>"` test-case identifiers between `HEAD~1` and `HEAD` of the test file: 19 unchanged names + 6 new names (AC1 positive×2/negative×1, AC4 routine-class, AC3 young-excluded + aged-past-gate) = 25, exact match. `shellcheck -x -e SC1091` on both changed `.sh` files: 0 warnings (only pre-existing info-level `SC2015`/`SC2329`, same idiom already used throughout the file — not new debt). `mock-guard.sh` PASS (pure bash zone, no production TS/JS touched, `bun test`/`tsc --noEmit` N/A). Live sanity re-run of the sweep on the current repo HEAD: mechanism runs clean (41 total_dirty, 15 owned_elsewhere, 25 young_skipped, 1 unknown — a stray `.tmp` marker file unrelated to this fix); no `.claude/agent-models.json`/`.claude/agents/*.md` dirty files exist right now to exercise AC1 live, so that AC's live-repo proof rests on the unit tests (already independently re-run and confirmed).

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` STEP `developer-S36`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. All 5 ACs independently reproduced from source/live repo, not trusted from the developer's `review_note` prose. Row's own AC5 scratchpad evidence files (`sss-ac5-plan-20260730T0731Z.json`/`sss-ac5-stderr-20260730T0731Z.log`) no longer present on disk — expected (scratchpad files are ephemeral by convention), not a blocking gap; the underlying mechanism is independently proven by the unit-test suite + this session's own live re-run. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (row had none — direct-commit row, no handoff file). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` was NOT this task (a different row) — left untouched per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116/S117). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-STRANDED-SWEEP-CLASSIFY-AGENT-MODEL-SWITCH` and is the authoritative release path.

## cycle-705 · 2026-08-14T01:33:11Z · FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES (report-analyzer, commit `761988143`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — the detector's only non-zero-diff DETECTED path sourced its evidence from the notebook commit it was checking, so a genuinely non-zero OTHER plane (signal_queue row, ledger entry) with the notebook plane silently missing defeated `all_zero` and PASSed a defective cycle (confirmed live 12x). Did not trust the developer's pasted 15/15 count or the router's own independent-run note — re-ran everything myself.

Commit `761988143` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook side-files. `bash scripts/audits/detect-analysis-only-exit.test.sh` own re-run: 15/15 PASS.

Read the core diff at source, not narration: the new MANDATORY-PLANE CHECK reads `docs/agents/<id>/init.md`'s `memory.notebook` field (`none` → not mandatory, else → mandatory), never the artifact under test — satisfies AC-2's "never key on the agent's own claim" carry-forward from the parent row. AC-1's naive `all_zero`→`any_zero` widening is explicitly rejected in the diff in favor of this per-plane subset. Grep-swept `docs/agents/*/init.md` myself (not trusting the dev's "only refine_bctc_md" claim) — confirmed only `refine_bctc_md` declares `notebook: none`; every other fleet agent defaults to mandatory=1, so the conservative default does not mass-false-positive the fleet.

Re-ran BOTH historical repros named in `verification_gate` live, not from prose: system-auditor `--since-ts 2026-08-12T05:35:00Z --until-ts 2026-08-12T05:55:00Z` → DETECTED (`all_zero`+`mandatory_status=violation`; that signal_queue row has since drained per the developer's own honest note, so it no longer isolates the mechanism alone, but the detector still correctly fires). news-scout `--since-ts 2026-08-07T03:55:00Z --until-ts 2026-08-07T04:30:00Z` → DETECTED `mandatory_status=violation` (`ledger=1`/`notebook=0`) — flipped from the pre-fix live PASS documented in the row's own evidence, the exact inverse-partial shape this row exists to close.

AC-3 regression check (own diff, not trusted): T8-T12 (the c80 partial-write contract fixtures) show zero lines changed in the commit diff — confirmed non-regression. AC-4: RED-1 (T2, signal_queue>0/notebook=0), RED-2 (T4, ledger>0/notebook=0), GREEN (T14, no-notebook-mandate agent) all present and green; T6 doubles as AC-5's `--cycle-tag` investigation, empirically proving the identical blind spot closed by the same fix. `shellcheck -x -e SC1091` 0 warnings both changed `.sh` files. `mock-guard.sh` correctly N/A — zone is pure bash+md, no TS/JS production source touched, `bun test`/`tsc --noEmit` N/A.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP `developer-S104`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live repo. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.
