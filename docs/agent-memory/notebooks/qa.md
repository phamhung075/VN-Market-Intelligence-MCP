# QA — Notebook

## cycle-643 · 2026-08-11 · FACTORY-GUARD-CI-metric-mask-lint (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `7fedc2933`)

Design-only task, no code to test. Commit `7fedc2933` (brief, 1 file/38L) on `main` ancestry. RAW-verified: eslint gap, 3 offender lines, 5 fast-track deps, 7/7 sibling precondition notes all confirmed; child `FACTORY-GUARD-CI-METRICMASK-IMPL` scoping matches brief §3. VERDICT: APPROVED, `qa[]`→`done_verified[]`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S56. This file hit 4 peer-write collisions this cycle — board row unaffected.

## cycle-642 · 2026-08-11 · FACTORY-GUARD-CI-size-lint-justification (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `fb613a9c4`)

Router-dispatched (`mode=verify-committed`, 1st/3rd row of a new batch sharing `claimed_at=2026-08-11T23:21:16Z`; `.head.active_task_id` pointed at sibling `FACTORY-SHARED-fix-shared-db-stale-list`, left `.head` untouched). Design-only task — row's own `review_note`: "verdict brief adequacy + child scoping, no code to test." Sibling pattern to the earlier `FACTORY-GUARD-CI-metric-mask-lint` verify (qa-S56 journal entry) in the same epic. No `commit`/`files[]` fields on the row (drain-predates schema) — derived commit via `git log --all -- docs/architecture-briefs/2026-07-24-factory-guard-ci-size-lint-justification.md`: single commit `fb613a9c4`, on `main` ancestry, 1 file/56L, zero production code. Child-mint commit `ad9c7483f` (same timestamp) confirmed too — lane-move in_progress→review, conservation 631=631.

Went beyond prose-trust: independently re-checked the brief's own verify-live claims at commit time (zero existing size-lint mechanism anywhere — `context-bloat-backstop.sh` SSOT explicitly excludes code/data-JSON; grepped all 7 `.golangci.yml`, 3 `eslint.config.mjs`, `pyproject.toml`, `ci.yml` — no hits). Then went further than a normal design-review: the child `FACTORY-GUARD-CI-SIZELINT-IMPL` has since fully shipped and is LIVE — `scripts/audits/size-lint-justification.sh` + `.test.sh` + `docs/data/size-lint-baseline.json` all present, `size-lint` job wired into `.github/workflows/ci.yml`, `dev-standards.md` CANONICAL pointer matches the brief's baseline/ratchet design verbatim. Live-ran `size-lint-justification.sh --check`: exit 1, sole offender `apps/mcp-server/src/interface/mcp/transport.ts` (baseline=126L actual=265L upper=138L) — the mechanism is demonstrably catching real regressions in production, and this exact offender is already tracked as its own separate backlog row (`FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L`, extensively po-triaged since 2026-08-08) — a downstream dispatch-priority matter, not a defect of this design/child. Diffed brief §3 minted-child spec (files/approach/dod/routing) against the live `FACTORY-GUARD-CI-SIZELINT-IMPL` board row — matches verbatim, incl. the routing correction (`developer`, not `agent-father`).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` — Stage0+1 PASS, conservation OK (task_total 767=767, signal_total 63=63, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S57. `task_release` is the router's job — not attempted here. No section pruned this write (2 sections present at read-time, exactly at the 3-cap after this append) — kept 641(×2)/642.

## cycle-641 · 2026-08-11 · FACTORY-FRONTEND-split-orchestration (dev-frontend) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `c73a494ee`)

2nd row of the same 3-row batch (`claimed_at` shared with `FACTORY-FRONTEND-split-market-summaries`, `.head` pointed at the sibling not this one — left `.head` untouched). Row carried no `commit`/`files[]`/`owner` (predates drain fields) — derived via `git log --all -- app/domain/orchestration/* app/components/orchestration/*`: single commit `c73a494ee`, dated 2026-07-24 (matches `entered_review_at`), on `main` ancestry; `git show --stat` matches all 29 claimed files exactly (DTOs→`types.ts`, staleness→`staleness.ts`, `taskStatusClasses`→`task-status-classes.ts`, 11 render components + `doneTaskGrid.ts`, 10 new test files, `vite.config.ts`, `domain-model.md`).

RAW-verified, not trusted from prose: byte-diffed the DTO block (`StepDto`...`OrchState`), the `isStale` predicate, and `taskStatusClasses` against the pre-split route (`c73a494ee^`) — verbatim moves, zero shape/logic drift, only new file-header docstrings added. All 11 component files land at the exact claimed line counts (`HeadPanel`52/`TaskGroup`57/`StepCard`61/`DecisionAccordion`66/`DoneTaskRow`88/`DoneTaskGroup`104/`TaskBoard`74/`SignalQueue`88/`SprintGoal`49/`Narrative`69/`doneTaskGrid.ts`16), all ≤120L. `POLL_MS=5000` + the polling/visibility-pause loader effect byte-identical. `TASK-DASH-CRON-2` types/components correctly left untouched in the route (out of scope, confirmed via grep — `CronStatus`/`CronStatusRow*` never migrated). No circular imports (components never import back from the route). `tsc --noEmit` clean; `eslint` `lint:fence` (DDD Fence-A/B/C boundaries, a real tool run, not just manual greps) 0 violations on all touched files. `mock-guard.sh --files` PASS (16 production files). Security greps clean — the 2 `process.env` hits (route SSR origin, `vite.config.ts` PORT) pre-date this commit (grep-confirmed present verbatim on `c73a494ee^`), not introduced by it.

Full frontend `vitest` was blocked repo-wide at first attempt by a pre-existing environment defect: `esbuild@0.21.5`'s own JS harness resolving to a mismatched `@esbuild/darwin-x64@0.28.1` binary hoisted into pnpm's `.pnpm/node_modules` virtual-store fallback (traced with `require.resolve` + `paths`) — reproduced identically on a totally unrelated pre-existing test file, confirming pre-existing/repo-wide, not caused by this commit. Root-caused, then worked around via `ESBUILD_BINARY_PATH` env override pointed at the correctly-versioned binary already present in the pnpm store (`@esbuild+darwin-x64@0.21.5`) — zero file writes. Targeted 13-file/104-test orchestration suite (incl. the claimed 9 new staleness + 8 new `taskStatusClasses` unit tests): 100% pass. The commit's own full-suite equivalence claim ("95 files/2112 tests, 2110 pass, 2 pre-existing `QUE_DESCRIPTIONS` fails") has drifted 18 days later to 100 files/2185 tests, 32 fail/11 files under today's `main` HEAD — did not accept either number blind: built 2 detached `git worktree`s pinned exactly at `c73a494ee` and its parent `c73a494ee^`, both symlinked to the SAME today's `node_modules` (no `package.json` diff in this commit, safe to share) — both produced byte-identical 95/83 files at their respective commit, and BOTH produced the exact same 32-failure/11-file set (`FE-HEADER-SSOT-top-nav`, `QUE-REFERENCE-PAGE-detail`, `QUE-TOOLTIP-DRY-1a-codegen-pipeline`, `ind-p1-indicator-gauges-nav`, `ind-p1-momentum-nav`, 6× `task17-page1{4..9}-*-nav`), confirming zero regressions from this refactor — today's drift is unrelated later-commit fallout, matching the exact test count the commit itself claimed as its own baseline (95/2112 at `c73a494ee`, 83/2049 at parent — both match the commit message precisely).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, hand-built `verification.raw_probe` attached) via `orch-apply.sh` (Stage0+1 PASS, conservation OK — task_total 765=765, signal_total 71=71, signal_row_identity=clean; Stage 1g's 16-row report is pre-existing unrelated dependency-resolution noise). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. DJ-GATE-1: `sprint-FACTORY-FRONTEND-split-orchestration-dev-frontend.md` (task-id stamped ×7) pre-satisfied; own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S54. `rebuild_required:true` in backlog-detail correctly DEFERRED by dev (code-only, no container touched, task scope explicitly bound CODE-ONLY/NO-REBUILD, user-gated) — out of QA scope. `task_release` is the router's job per this task's own dispatch note — not attempted here. Pruned cycle-638 (oldest, `FACTORY-PDF-extract-tesseract-config`) this write per AC-2 retention (3-section cap) — kept 639/640/641.

## cycle-642 · 2026-08-11 · FACTORY-SHARED-fix-shared-db-stale-list (dev-mcp-server) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `ef62d2921`)

Router-dispatched, `.head.active_task_id` pointed exactly at this row. Row's own `review_note` (no `commit`/`files[]`/`owner`, predates drain schema) claimed: added `schema-backtesting` to `DB_SCHEMA_MODULES` (`packages/shared-db/index.ts`), array now matches all 9 on-disk `schema-*.ts` files, zero importers, no test harness so sync-comment alone satisfies DoD. Confirmed `ef62d2921` (+journal `5c20d7b88`+notebook `160c0b410`) on `main` ancestry; `git show --stat` matches the sole real diff exactly (7 ins, 1 file).

RAW-verified: `bun -e` import-eval of the file confirms the 9-entry array evaluates correctly; cross-checked against `schema.ts`'s OWN categorization (its 9 `init*Tables` imports, kept structurally separate from its 2 migration-helper imports) rather than trusting a naive on-disk glob — the array matches every real schema-slice module exactly. Zero-importer claim re-verified 2 independent ways: repo-wide grep (no hits outside the package) + `scripts/audits/shared-package-import-check.sh --check` PASS (shared-db still baseline-listed orphan). DDD/security greps clean, `mock-guard.sh` PASS, `bun tsc --noEmit` (apps/mcp-server) 0 errors.

FLAGGED, not actioned (out of scope): a LATER unrelated commit `6ca2a0c65` (2026-08-07, `FIX-CI-SIZELINT-SCHEMA-TS-DEFLAKE-REGRESSION-372L`) added 2 new `schema-*.ts` files (migration helpers, no `init*Tables` export) — a literal re-run of this commit's own added `ls | grep '^schema-'` instruction now surfaces 11, not 9, making the manual sync-check textually stale again. The array is still semantically correct (matches every real schema slice); the drift is in the comment's literal glob instruction, caused entirely by later work, not a defect in this commit — accurate at landing time (2026-07-24, 9/9). Recommend PM/PO follow-up: scope the sync-comment glob to `init*Tables`-exporting files, or rename the 2 migration files off the `schema-` prefix.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) AND reset `.head`→idle in the SAME `orch-apply.sh` write (`.head.active_task_id` pointed exactly at this row) — Stage0+1 PASS, conservation OK (task_total 767=767, signal_total 63=63, signal_row_identity=clean; Stage 1g's 16-row report pre-existing unrelated). DJ-GATE-1: `sprint-FACTORY-SHARED-fix-shared-db-stale-list-dev-mcp-server.md` task-id stamped, pre-satisfied; own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S58. `task_release` is the router's job — not attempted here. No section pruned this write (3 sections present post-write incl. this one — 639/640 already absent, dropped a prior cycle — kept 641(×2)/642).
