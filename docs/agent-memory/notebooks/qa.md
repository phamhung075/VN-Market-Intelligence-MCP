# QA — Notebook

## cycle-650 · 2026-08-12 · FACTORY-GUARD-CI-rebuild-raw-verify-hook (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `265451cc6`)

7th and LAST of 7 `ci-regression-prevention` guardrails — closes the epic cluster. Same batch (`claimed_at=2026-08-12T04:22:18Z`) as sibling `FACTORY-GUARD-CI-shared-package-import-check` (verified concurrently by a peer this cycle, see cycle-649 below); `.head` pointed at that sibling row throughout, left untouched here. Commit `265451cc6` confirmed real: ancestor of `main` AND on `origin/main`, pushed clean; `git show --stat` matches claimed scope exactly (46L/10395B brief + orch-state.json +21L child-mint only + notebook/journal, zero production code).

Independently re-verified BOTH design corrections AT SOURCE, not brief prose: `gh pr list --state all` → exactly 1 PR ever (#1, CLOSED, `mergedAt: null`); `gh api repos/:owner/:repo/branches/main/protection` → 404 Branch not protected. The "attached to the PR" category-error correction is sound — no PR/branch-protection gate exists in this repo, so wiring the check primarily into `scripts/git-hooks/pre-push` genuinely IS the real merge gate here, not a design shortcut. `dev-standards.md:1493` PUSH-AUTONOMY-1 §5 text matches the brief's quote word-for-word — "DoD already exists as policy" is accurate. Concrete compliance-gap commit `e3386bdfa` independently confirmed: real, self-flags `rebuild_required: true`, exact confidence-mask bug class named, zero RAW-verify/REALDATA attestation. Sanity-checked the ratio 18 days later: 3 `VERIFY-*-REALDATA` rows exist repo-wide today vs 173 serving-code commits since the policy pin — gap direction holds.

Verified the sibling-composition claim at the code level too: `rebuild-raw-verify-check.sh`'s `FIELD_ERE`/`TRIGGER_PATH_ERE` literally reuse `metric-mask-lint.sh`'s field regex + the depguard-tier-boundaries layer paths verbatim; child `FACTORY-GUARD-CI-RAWVERIFY-IMPL` already shipped independently (ahead of this row's own QA-drain queue, same out-of-order pattern as siblings dead-code-gate/no-hardcode — not a defect), wired into `pre-push` (primary) + `ci.yml` `rebuild-raw-verify-hook` job (backstop) + `dev-standards.md` §5 cross-reference (incl. a 2026-08-07 post-ship regex fix) — all 5 files from the child's own note present and wired on disk.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) — Stage0+1 PASS, conservation OK (task_total 762=762, signal_total 67=67; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. `.head` untouched (pointed at sibling row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S65. `task_release` is the router's job — not attempted here. SELF-HEAL: this write's own first attempt (an Edit-insert, not a composed single-Write) got silently CLOBBERED by `notebook-auto-prune.sh`'s tie-break — root-caused to a stale `"qa.md": "oldest_first"` entry in `docs/data/notebook-section-order.json` that contradicted this file's actual, git-history-confirmed newest_first/prepend convention (same bug a peer independently hit this same cycle on cycle-649 below); fixed by DELETING the wrong override entry (file's own default is already newest_first — matches this file's real convention, no replacement entry needed), committed separately per doc-self-heal. Pruned cycle-647/`FACTORY-GUARD-CI-no-hardcode-allowlist-scan` (oldest of the 3-section cap) this write per AC-2 retention — kept cycle-649 (shared-package-import-check) + cycle-648 (agent-father).

## cycle-649 · 2026-08-12 · FACTORY-GUARD-CI-shared-package-import-check (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `d220c8e1b`)

Router-dispatched (`mode=verify-committed`), 6th of 7 `ci-regression-prevention` guardrails per the row's own review_note. Design-only task, no code to test. Commit `d220c8e1b` confirmed real: ancestor of `main` AND `origin/main..HEAD` empty (pushed clean); `git show --stat` matches claimed scope exactly (48L brief + `orch-state.json` +21L child-mint only + notebook/journal, zero production code).

Did not trust the row's DISPATCHER CONFABULATION-GUARD prose alone: independently re-derived the core evidence. `wc -l` on all 3 `packages/shared-{types,config,db}/index.ts` = 76/73/37L (brief's exact numbers). Repo-wide grep for `@vn-market/shared-{types,config,db}` import specifiers / `package.json` deps, excluding each package's own files, returned 0 hits — 3/3 packages genuinely 100% orphaned, matching the brief's core evidence claim exactly. Verified both cited supporting commits real (`ef62d2921` shared-db stale-list fix, `df6c1dc7d` prior phantom-package prune precedent). Spot-checked the Alert name-collision claim directly in source: shared-types 5-field `Alert{id,stock,severity,message,createdAt:Date}` vs live `alertGenerator.ts` 10-field with `actionCode` rename + `createdAt:string` — exact match; the cited `:41` line ref was accurate at the brief's own commit-time blob, current-HEAD `:42` drift is unrelated later churn, not a brief defect.

Design-decision focus per the task's own instruction (verdict brief adequacy + child scoping, BASELINE/RATCHET vs zero-tolerance justification): verified the central ownership claim — `FACTORY-SHARED-wire-or-prune-shared-packages` exists in `backlog-detail.json` with `effort:L/risk:med/rebuild_required:true/status:BACKLOG`, matching the brief's cited properties exactly, and its own DoD text (reconcile-to-superset-first caution, "Alert/Signal/ServiceHealth shapes are structurally different across apps... reconcile FIRST or you erase served fields") independently corroborates why the keep-or-cut fix is a domain-judgment call not safe to fold into a CI-tooling child task. BASELINE/RATCHET is well-justified on that claimed axis — distinct from the zero-tolerance dead-code/no-hardcode siblings (whose debt was mechanically/safely fixable in the same child task) — and the design still delivers real regression-prevention value (blocks any NEW zero-importer package immediately) while correctly deferring pre-existing debt to its already-assigned owner. Child row `FACTORY-GUARD-CI-SHAREDPKG-IMPL` correctly minted (`dev_agent`/`next_agent`=developer, zone `cross-service/`, `brief_ref` wired) and has since progressed independently to `task_board.review` (own separate QA gate, not re-verified exhaustively here).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) AND reset `.head`→idle in the SAME `orch-apply.sh` write (`.head.active_task_id` pointed exactly at this row) — Stage0+1 PASS, conservation OK (task_total 762=762, signal_total 67=67, signal_row_identity=clean; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field (no `status_note` field present on this row's schema). Own QA DJ rolled to a new continuation file this write: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-17.md` §qa-S64 (byte-axis cap breach on `-16.md`: 40950B > 36000B despite 201L < 600L — dual-axis rule, `CAP-REACHED` marker appended there). `task_release` is the router's job — not attempted here. NOTE: this notebook file was clobbered mid-write twice by concurrent peer full-overwrites (known recurring class, `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries`) before this entry landed — re-applied via a single atomic python rewrite + immediate scoped commit to close the race window. Pruned cycle-647/`FACTORY-GUARD-CI-dead-code-gate` (oldest of the 3-section cap, verified earliest per its own journal stamp qa-S63 03:57:03Z vs no-hardcode's qa-S62 03:56:02Z) this write per AC-2 retention — kept cycle-648 (agent-father) and cycle-647/no-hardcode.

## cycle-648 · 2026-08-12 · FIX-AUDITOR-T1-T3-CLEANEXIT-HEARTBEAT-STAMP-SKIPPED-T2-UNAFFECTED (agent-father) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `92af91ef4`)

Router-dispatched (`mode=verify-committed`), part of the SAME 3-row batch as the sibling cycles below (`claimed_at=2026-08-12T03:52:58Z`; `.head.active_task_id` pointed exactly at this row — reset to idle in the same write). Commit `92af91ef4` on `main` ancestry, touches ONLY `docs/agents/system-auditor/flow/main.md` (+74/-3) + `docs/agent-memory/notebooks/agent-father.md`.

Row's own `files[]` (a nonexistent `docs/agents/system-auditor/main.md` + both `auditor-tier{1,3}-last-healthy.json` data files) never appear in the commit diff — cross-checked against the commit's own `task:` trailer + agent-father's notebook entry ("did NOT hand-stamp either data file — prior PO ruling") to confirm stale board metadata, not a wrong-commit citation; corrected `files[]` on the row in the same write. Read the flow-doc diff directly (not `review_note` prose): confirmed present verbatim — post-write read-back assert (`HB_READBACK` vs captured `HB_TS`), a 2nd `scripts/auditor-notebook-commit.sh` call scoped to the heartbeat path only (gated behind read-back match), new mandatory `[HEARTBEAT]` RETURN-block line (NOT-APPLICABLE/tier-1, OK|ABORT/tier-2-3), and the RAW-CITE GATE + Tier-1 Runtime Ping rule making a T1 "heartbeat written" claim categorically false on every verdict.

Independently dry-ran the write→read-back→commit-gate bash logic in a scratch fixture (not trusting agent-father's own claimed scratch-test): success case correctly writes+reads-back+would-commit; forced-failure case (read-only dir) correctly detects the mismatch, emits ABORT, leaves the file unchanged, skips the commit gate — matches the exact bug class (T3 byte-identical stale write silently narrated as fresh) this fix targets.

FLAGGED, not actioned: no live Tier-2/Tier-3 cycle has fired since the fix landed (03:25:02Z, ~30min elapsed at verify time) — recommend PO/router confirm `[HEARTBEAT] OK ts=...committed=<sha>` + a fresh git-committed data file after the next real T2/T3 cycles. The 1 live post-fix cycle so far (T1 c47, 03:37Z) avoided a false heartbeat claim but used prose `HEARTBEAT=unchanged` instead of the new literal `[HEARTBEAT]:` bracket format — format/compliance note only, not a defect in this commit. T1's actual missing-write-path gap remains correctly out of scope, owned by sibling row `FIX-AUDITOR-TIER1-HEARTBEAT-HANDWRITE-RECURS-SAME-DAY-AS-ITS-OWN-FLOW-FIX`.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) AND reset `.head`→idle in the SAME `orch-apply.sh` write (`.head.active_task_id` pointed exactly at this row) — Stage0+1 PASS, conservation OK (task_total 765=765, signal_total 59=59, signal_row_identity=clean; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field, `files[]` corrected. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S63 (note: a peer concurrently wrote its own unrelated §qa-S63 to the same journal file — label collision, not data loss, both entries intact). `task_release` is the router's job — not attempted here. Pruned cycle-646/cycle-645 (both from the OLDER 02:22:14Z batch, already fully superseded) this write per AC-2 retention (3-section cap, applied by actual batch-recency since peer concurrent writes already scrambled file position-ordering) — kept both 647-batch siblings (03:52:58Z, same batch as this row).
