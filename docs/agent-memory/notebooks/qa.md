# QA — Notebook

## cycle-709 · 2026-08-14T04:58:03Z · FIX-VPSHEALTH-DEMANDROUTE-EMPTYQUEUE-MISREPORTS-PROXY-UNREACHABLE (dev-mcp-server, commits `6113890bb`+`0f8e8ee07`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row already RAW-verified by dev-team (`raw_verified_by=dev-team`, 2026-07-30). Task instruction explicit: do not trust that note at face value, run own checks — did so. Both commits real, confirmed ancestors of `main` AND `origin/main`. `git show --stat` matches diff exactly (new test file 245L, `vpsPushLogStore.ts` +60, `vpsProxyTools.ts` +46/-7, tool doc + architecture doc). Root-cause: `get_vps_proxy_health` derived STALE from raw last-push age alone and hardcoded "VPS may be down or unreachable" even for bctc, a DEMAND-DRIVEN route with 0 pending queue rows while sibling routes (prices/news/sbv) pushed fine on the same proxy. Fix separates OBSERVATION (`isStale()`, unchanged) from INFERENCE: new `getDemandQueueDepth()`/`DEMAND_QUEUE_SQL` registry (pending-only, mirrors the C-16 auditor convention) downgrades a demand-driven route with a confirmed-empty queue to `IDLE-NO-WORK`, excluded from the STALE/proxy-down line. Fail-open: route absent from registry or query failure → null, pre-fix behavior unaffected.

Read source directly, not commit-message prose: `getDemandQueueDepth`/`DEMAND_QUEUE_SQL` and `formatHealth()` were later split out (`vpsDemandQueue.ts`, `vpsProxyHealthFormat.ts`) by an unrelated size-lint refactor (`FIX-CI-SIZELINT-MCPSERVER-SIX-UNCOVERED-OFFENDERS`) — confirmed the idle-no-work logic and OBSERVATION/INFERENCE split survived intact, re-exported from `vpsPushLogStore.ts` for back-compat. Own re-run: targeted test 7/7 PASS (AC scenario + control where a real pending row still triggers STALE + fail-open when table absent + 4 `getDemandQueueDepth` unit tests) — matches claim. Sibling suite (`1113-vps-proxy-health.test.ts`+`VPT-1-vps-proxy-health-endpoint.test.ts`): 22/22 PASS — matches claim. Went further than the prior raw-verify: ran the broader downstream suite of tasks built ON TOP of this fix (`1406b-vps-health-idle-constraint`, `FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH`, `FIX-BCTC-VPS-QUEUE-STALE-TRIAGE`, `FIX-BCTC-VPS-QUEUE-SYNC`, `FIX-VPS-HEALTH-FRESHN`) — 47/47 PASS, no regression. `bun tsc --noEmit` clean. DDD scan (domain→infrastructure/application imports) and security scan (process.env, password/secret/token) on the 4 touched production files: 0 matches. `mock-guard.sh --files` on the same 4 files: PASS.

`rebuild_required=true` left untouched on the row — genuine, outstanding ops/deploy concern (live undeployed tool still reproduces the pre-fix bug per the prior raw_verify_note), orthogonal to this source-level QA gate; not something this verify step resolves.

DJ: `sprint-FIX-VPSHEALTH-DEMANDROUTE-EMPTYQUEUE-MISREPORTS-PROXY-UNREACHABLE-qa.md` STEP qa-S1 (new journal file — first QA cycle against this task-id).

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` written to the row's `status_note` field (had none — direct-commit row, no handoff file). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 734=734, signal_total 31=31). `.head.active_task_id` WAS this task — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the first write attempt this cycle (tool/args/live_value_observed/observed_at all populated up front — no retry needed, unlike the qa-S113/S115/S116/S117/S119/cycle-705/cycle-708 recurring gap). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-VPSHEALTH-DEMANDROUTE-EMPTYQUEUE-MISREPORTS-PROXY-UNREACHABLE` and is the authoritative release path (per this task's own dispatch note: released implicitly by moving the row off `qa[]`, not by a direct `task_release` call).

## cycle-705 · 2026-08-14T01:33:11Z · FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES (report-analyzer, commit `761988143`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — the detector's only non-zero-diff DETECTED path sourced its evidence from the notebook commit it was checking, so a genuinely non-zero OTHER plane (signal_queue row, ledger entry) with the notebook plane silently missing defeated `all_zero` and PASSed a defective cycle (confirmed live 12x). Did not trust the developer's pasted 15/15 count or the router's own independent-run note — re-ran everything myself.

Commit `761988143` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook side-files. `bash scripts/audits/detect-analysis-only-exit.test.sh` own re-run: 15/15 PASS.

Read the core diff at source, not narration: the new MANDATORY-PLANE CHECK reads `docs/agents/<id>/init.md`'s `memory.notebook` field (`none` → not mandatory, else → mandatory), never the artifact under test — satisfies AC-2's "never key on the agent's own claim" carry-forward from the parent row. AC-1's naive `all_zero`→`any_zero` widening is explicitly rejected in the diff in favor of this per-plane subset. Grep-swept `docs/agents/*/init.md` myself (not trusting the dev's "only refine_bctc_md" claim) — confirmed only `refine_bctc_md` declares `notebook: none`; every other fleet agent defaults to mandatory=1, so the conservative default does not mass-false-positive the fleet.

Re-ran BOTH historical repros named in `verification_gate` live, not from prose: system-auditor `--since-ts 2026-08-12T05:35:00Z --until-ts 2026-08-12T05:55:00Z` → DETECTED (`all_zero`+`mandatory_status=violation`; that signal_queue row has since drained per the developer's own honest note, so it no longer isolates the mechanism alone, but the detector still correctly fires). news-scout `--since-ts 2026-08-07T03:55:00Z --until-ts 2026-08-07T04:30:00Z` → DETECTED `mandatory_status=violation` (`ledger=1`/`notebook=0`) — flipped from the pre-fix live PASS documented in the row's own evidence, the exact inverse-partial shape this row exists to close.

AC-3 regression check (own diff, not trusted): T8-T12 (the c80 partial-write contract fixtures) show zero lines changed in the commit diff — confirmed non-regression. AC-4: RED-1 (T2, signal_queue>0/notebook=0), RED-2 (T4, ledger>0/notebook=0), GREEN (T14, no-notebook-mandate agent) all present and green; T6 doubles as AC-5's `--cycle-tag` investigation, empirically proving the identical blind spot closed by the same fix. `shellcheck -x -e SC1091` 0 warnings both changed `.sh` files. `mock-guard.sh` correctly N/A — zone is pure bash+md, no TS/JS production source touched, `bun test`/`tsc --noEmit` N/A.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP `developer-S104`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live repo. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.
