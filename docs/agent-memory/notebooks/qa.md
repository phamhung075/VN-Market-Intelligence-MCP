# QA — Notebook

## cycle-664 · 2026-08-12T10:42:16Z · FIX-FB-GATE-CHECKC-NEGATION-LEXICON (developer, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commits `31e39793a`+`ab38d4689`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain, one of a 3-row batch sharing `claimed_at`; `.head.active_task_id` matched this row, idle-reset in the same write). Both commits confirmed real, on main ancestry, touch `scripts/fb-data-integrity-gate.sh` + `docs/agents/fb-market-poster/flow/main.md` exactly as claimed. Read the diff on disk: `chưa( phải| từng)?` negation alternation added to Check-C's `SELLOFF_AFFIRM_LINES` strip (generic, not a 2-phrase literal — matches the backlog row's own `generic_mandate`); `chỉ \d mã sàn` → `chỉ [0-9] mã sàn` (BSD-grep portability).

Independently re-ran the new `scripts/test-fb-gate-checkc-negation.sh` on current main HEAD: 6/6 pass. Not satisfied trusting the row's own RED→GREEN prose — independently reproduced RED myself: temp-overwrote the gate script with `31e39793a^`'s pre-fix version (`git show`), reran the same harness → 4/6 pass, fixture-A (mild-day negated panic statement) false-BLOCKs exactly as claimed; restored the fixed version, diff-confirmed byte-identical to the committed file before continuing. All 3 `verification_gate` acceptance criteria (`archive/backlog-detail.json`) independently confirmed: mild-day `chưa`-negated fixture PASSes 0 violations, affirmative-panic contrast fixture still BLOCKs, `chỉ [0-9] mã sàn` matches under this host's BSD-family `grep -E`. `shellcheck -S style` before/after: identical single pre-existing `SC2034` warning on `POST_DATE`, 0 new. `bash -n` clean both files. `mock-guard.sh --files` N/A (shell scripts, no production TS/Go source touched). `process.env`/secret greps clean.

Minor non-blocking finding: cross-checked the review_note's own "audited all 25 `\d` occurrences" tally via a programmatic heredoc-boundary parse — actual pre-fix count is 24 (1 real bash `grep -ciE` context @L636 + 23 python3 heredoc `re` raw-string literals, not 24 as the "the other 24" phrasing implies). Off-by-one in descriptive prose only — the substantive audit conclusion (exactly one real bash context existed and was the only one needing the fix; every other occurrence sits safely inside a python heredoc, correctly left untouched) is fully correct and independently confirmed. Did not block on this.

VERDICT: APPROVED (vc-approved). Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `verification.raw_probe` attached documenting the independent 6/6-post-fix + 4/6-pre-fix-RED re-run, `next_agent:pm`). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field (no handoff file exists for this branch:null row). `.head` reset to idle in the same write — this row uniquely matched `.head.active_task_id` at dispatch time (CANONICAL:SSOT-STATUSFLIP-LANEMOVE(b); the batch's other 2 sibling rows are sibling-QA-session concerns, head-decoupled from this write). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S79. `task_release("task:FIX-FB-GATE-CHECKC-NEGATION-LEXICON")` is the router/dev-team dispatcher's job per INV-GATEWAY-1 — not attempted here (no MCP gateway tool binding in this specialist session).

## cycle-663 · 2026-08-12T10:41:50Z · FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO (cross-service/, real code) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `1fcfa72da`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain). Commit confirmed on main ancestry (`git merge-base --is-ancestor`); `git show --stat` matches both claimed files (`scripts/audits/mock-guard.sh`, `scripts/audits/mock-guard.test.sh`) plus a `docs/WORK.md` log entry. Read the diff on disk: `EXCLUDE_PATHS` gained exactly one `_test\.go` alternative (AC1/AC4 — mirrors the existing `.test.`/`.spec.` filename convention already on that line, zero new per-directory literal); `--full` mode's file discovery switched from `grep -r apps/` to `git ls-files --cached --others --exclude-standard` (AC2) — independently confirmed the pathspec globs DO recurse subdirectories (3105 files returned, spot-checked deeply nested paths) and return ZERO `apps/mcp-server/~/.bun/**` hits (vendored cache genuinely excluded by construction, not merely by claim).

Independently re-ran, not trusted from prose: `bash scripts/audits/mock-guard.test.sh` 7/7 PASS (AC1 Go-test-file no-hard-fail; 2 AC3 negative controls — same stub URL in a non-test .go file and an explicit mockData identifier in a non-test .ts file both still HARD-FAIL, detector not blinded; AC3 clean-fixture exit-0 baseline; AC2 real-gitignored-`~/.bun`-cache sentinel invisible to `--full`, cleanup verified with zero leftover; AC4 structural single-convention grep check). Live `bash scripts/audits/mock-guard.sh --full` re-run: exit 2 (CAUTION-only, ~11 pre-existing unrelated TODO markers, non-blocking per post-cycle.md L32), zero HARD-FAIL lines — confirms the row's own "exit 1→2" claim; the row's actual stated symptom (recurring exit=1 HARD-FAIL every dev-team post-cycle tick) is resolved. shellcheck: 1 info-level SC2329 on the test file's `cleanup()` (invoked via `trap cleanup EXIT`, not a direct call — false positive, consistent with the row's "shellcheck clean" claim). process.env/secret greps clean.

AC3's self-disclosed honest caveat ("exits 0 on a clean tree" doesn't hold against the live `apps/` tree's pre-existing, unrelated CAUTION markers) judged reasonable, not a defect — the test suite's own controlled clean-fixture DOES exit 0 (proves the mechanism), and the row's stated symptom/impact text is scoped entirely to the exit=1 HARD-FAIL, never claims the live CAUTION backlog must also vanish.

CAUTION for future verifiers: attempted an independent RED (pre-fix) reproduction via `git stash push -- scripts/audits/mock-guard.sh` — no-op (file already matched committed HEAD, nothing to stash). Do NOT use bare `git stash` against this shared live repo — the subsequent `git stash pop` hit an unrelated PRE-EXISTING `stash@{0}` (a peer agent's WIP) and safely aborted with zero data touched, stash preserved untouched. Relied instead on direct diff read + current-state re-run (sufficient — diff is small and legible).

VERDICT: APPROVED, DONE_VERIFIED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by/at`, `verification.raw_probe` attached, conservation-check clean, task_total 753 preserved). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field. Own QA DJ `sprint-INFRA-AUDIT-qa.md` §qa-S2. `task_release` is the router's job — not attempted here (no MCP gateway tool binding in this session).

## cycle-662 · 2026-08-12T09:39:00Z · FACTORY-APP-split-pollNews (dev-mcp-server, real code) — Direct-Commit Verify, CHANGES_REQUESTED (commit `0f23a703f`)

Router-dispatched (`mode=verify-committed`, review-lane QA-drain). Commit `0f23a703f` confirmed real, on main ancestry, touches all 8 files it claims. Read the diff on disk: 5 helper clusters (`types.ts`/`insiderDetectors.ts`/`signalDedup.ts`/`defaultFetchers.ts`/`dbHelpers.ts`) genuinely extracted from `pollNews.ts`, zero-external-call-site-change claim confirmed by grep (every consumer import — `pollNews`/`PollNewsResult`/`SourceFetchers`/`detectInsiderFamilyBuying`/`detectInsiderSelling`/`_resetAllDarkAlert`/`PollNewsOptions` — still resolves unchanged from `pollNews.ts`); dropped `defaultTradingEconomicsFetcher` confirmed zero remaining call sites repo-wide. `bun tsc --noEmit` clean; `size-lint-justification.sh --check` clean (only pre-existing untouched `transport.ts` fails, matching the commit's own disclosure); `mock-guard.sh` PASS; DDD (application may import domain+infra, no forbidden `interface/` import) + `process.env`/secret greps clean. Re-ran all 21 pollNews-touching test files myself in isolation (not trusted from the commit's "13 files" claim, which undercounts): 109/109 pass — the 2 files (`1324-push-news-all-sources`, `FIX-POLLNEWS-COUNTER-CONSERVATION`) that flaked when I ran the batch under 2-min contention both passed clean standalone, matching the commit's own pre-split-HEAD reproduction claim.

ISSUE (why CHANGES_REQUESTED despite all mechanical checks passing): the backlog DoD/approach (`archive/backlog-detail.json`) names 5 SPECIFIC target modules — `insiderSignalDetector`(→`domain/`)/`sourceFetchers`/`sourceHealth`/`ingestEntries`/`buildSignals` — i.e. extraction of `pollNews()`'s own internal pipeline STAGES, with the explicit target "leave `pollNews.ts` a ~120L orchestrator" and "each module <=120L". What actually shipped: `pollNews.ts` is still 923L; the ~790L pipeline body (fetch/health-tracking → dedup/insert → cascade/alert-generation) is completely untouched — only peripheral clusters were moved, none of which correspond to the approach's named stage targets (`insiderSignalDetector` also landed under `application/usecases/pollNews/`, not `domain/` as specified). 4/5 new files already exceed the 120L cap (size-justification headers present, so the codebase-wide size-lint gate passes, but the task's own literal DoD text is unmet). The commit message itself is honest about this ("further staged decomposition... not done this pass") but the row was routed into `qa[]` as if terminal/complete. Direct same-epic precedent: `FACTORY-APP-split-assembleBriefing` (commit `4744b0792`) hit the identical "thin <=120L" DoD FULLY in one commit (1327L→72L sequencer, all 19 steps extracted) — proof this shape is achievable and this delivery falls far short of it.

VERDICT: CHANGES_REQUESTED (vc-changes). Not a regression, nothing broken — genuinely incomplete against its own Definition of Done. Moved `qa[]`→`review[]` via `orch-apply.sh` (`status:REVIEW`, `redispatch_count:1`, `next_agent`→`dev-mcp-server` per verify-committed convention — no task branch exists for `fixer`). Issue detail appended to the row's own (previously absent) `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-18.md` §qa-S78 (prior `-17.md` crossed its byte-cap last cycle). `task_release` is the router's job — not attempted here (no MCP gateway tool binding in this session).
