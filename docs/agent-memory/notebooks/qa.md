# QA — Notebook

## cycle-685 · 2026-08-13T10:55:15Z · FIX-ORCHSTATE-CONSERVATION-GUARD-QA-LANE-BLIND (developer, real code, commit `9b39ed5cc`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`claimed_at`=10:55:15Z), routed here via router PRE-CLAIM. Row has NO `.commit`/`.files[]`/`.review_note`/`.status_note`/`.detail_ref` at all — the leanest row shape seen in this drain so far, pure `root_cause`/`deliverable`/`acceptance` prose only. Derived commit via `git log --all -- scripts/orch-conservation-check.mjs` (the file the `root_cause` names by line-anchor): found `9b39ed5cc` carrying literal `Task:`/`AC:` trailers matching this id exactly — highest-confidence match seen in the drain (no cross-checking needed beyond the trailer itself).

Commit `9b39ed5cc` confirmed on main ancestry (`git merge-base --is-ancestor` exit 0). Read the live file directly, not trusted from commit prose: `FLAT_TASK_LANES` (scripts/orch-conservation-check.mjs:139) = `['backlog','ready','in_progress','review','qa','done','done_verified']` — `'qa'` present; the header-comment formula above it lists `+ length(qa)` in the identical position — code==doc parity holds. Checked `dev-standards.md`'s CANONICAL conservation-guard block directly: it never enumerates the lane list literally (only names the metric + points to the script/brief), so the commit's own claim ("no dev-standards edit needed, confirmed by grep") checks out — not just trusted prose. A later, unrelated commit (`cc7e86829`, FIX-DEVTEAM-IDLE-CHAIN-TEST-DURABLE) extended the same header comment further (signal_total dimension) — confirmed `'qa'` survived that extension untouched in both the array and the prose.

Re-ran the actual acceptance instrument myself, not trusted from commit prose or test-file diff alone: `bash scripts/test/orch-apply-wrapper-tests.sh` → **75/75 PASS**, including the two cases this task's own AC names verbatim — `QA-COLLAPSE` (dedicated fixture, qa[] wiped 50→0 against a 10-row backlog baseline, task_total 60→10 crosses the 0.5 floor, correctly rejected exit 1, fixture byte-unchanged — the negative control) and `QA-APPEND-HAPPY` (a normal single-row qa[] append, correctly accepted exit 0, length+1 — proves normal additive writes are unaffected). No TS files touched (only `.mjs`/`.sh`/`.md`) — `mock-guard.sh --files scripts/orch-conservation-check.mjs` PASS (no production source in its scan scope); ran `apps/mcp-server`'s `bun tsc --noEmit` anyway as a broader regression backstop — clean, 0 errors.

VERDICT: DONE_VERIFIED. Since the row carried no prose field to append to, wrote `[QA] Review Record (direct-commit verify)` into a NEW `review_note` field on the row (row's pre-existing fields — `root_cause`/`deliverable`/`acceptance`/`note`/`related` — all left untouched). Moved `qa[]`→`done_verified[]` via `orch-apply.sh` in the same write (`verification.raw_probe` attached — validator initially hard-rejected the first attempt for missing `verification.raw_probe{tool,args,live_value_observed,observed_at}`, corrected and re-applied; conservation-check clean, task_total 746 preserved; Stage 1g surfaced 16 PRE-EXISTING unrelated missing-dependency warnings, none touching this row — non-fatal, not from this write). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S98. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.

## cycle-684 · 2026-08-13T10:31:06Z · FIX-DEVTEAM-EPIC-WRAPPER-AUTOCLOSE-SWEEP (developer, real code, commit `29bdc58cf`) + SPIKE-BOOTSTRAP-BROADCAST-CATALYST-CONSUME (dev-mcp-server, real code, commit `2ae2c19d1`) — Direct-Commit Verify batch, both APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain batch, `claimed_at`=2026-08-13T07:24:57Z, spawned by router as an outer-lock resume (dev-team's own attempted spawn this tick logged "spawn blocked, no nested-Agent tool this session" — this session is the actual execution). Both rows predate the drain's `.commit`/`.files[]` field convention — no such fields on either row. Row 1 has neither `review_note` nor `status_note`, only a prose `.note` field (per router's dispatch note) plus `reviewed_at`/`reviewed_by` from a prior developer self-review pass; row 2 has a detailed `review_note`.

**Row 1** — derived commit via `git log --all --grep` on the task id (not file-path grep, since `.note` names no specific file): found `29bdc58cf` (2026-07-29T13:09:14+02:00, 27s before the row's own `reviewed_at`). Confirmed on main ancestry; `git show --stat` matches its own commit-message-claimed 5 files exactly (`post-cycle.md`, `main.md`, new `scripts/devteam-wrapper-autoclose.jq`, new `scripts/audits/devteam-wrapper-autoclose-verify.sh`, `scripts/lib/devteam-eligibility.jq` +53L append-only). Cross-corroborated against dev-team's own contemporaneous RAW-verify commit (`bd6f3f013`, same day, independent second source before I ever looked). Content matches the row's `.note` field verbatim (Step 4.4 sweeps `ready[]`/`in_progress[]` for epic-wrapper rows with `all_children_terminal`, auto-dispatches). Independently re-ran the verify script myself, not trusted from prose: `devteam-wrapper-autoclose-verify.sh` **10/10 PASS**, exact match. Confirmed the 4 new predicates still genuinely present in current `devteam-eligibility.jq` (file gained 4 later unrelated feature-appends since, none touching these 4 defs — checked by name+line). `tsc --noEmit` clean, zero secret/`process.env` hits on the 3 scripts, `mock-guard.sh` N/A (jq/sh, not TS/JS it scans). One later commit (`dc2152a10`) touched `post-cycle.md` again but only a single unrelated notebook-commit-pathspec line, confirmed via diff — not the Step 4.4 section.

**Row 2** — derived commit via `git log --all --grep` on the task id + file-path grep on the new test file's own name (both converged on the same commit): `2ae2c19d1`. Confirmed on main ancestry; `git show --stat` matches every file `review_note` claims (`agentSignalStore.ts` +93L, `getCycleBootstrap.ts` +25L, new `SPIKE-BOOTSTRAP-BROADCAST-CATALYST-CONSUME.test.ts` 173L, `get_cycle_bootstrap.md` +7L). Diff read directly, not trusted from prose: `getBroadcastSignals(db)` genuinely non-consuming (`WHERE to_agent='all' AND expires_at>now`, zero status filter, zero UPDATE); `getInboxSignals()` unions it with `getSignals()`'s consuming result, dedup by id — exact match. Cross-corroborated against dev-team's own contemporaneous RAW-verify (`19424a2c2`, same day). Re-ran tests myself: new test file 5/5 pass (exact match to claim). Re-derived the "targeted 12-file suite" via grep on the 4 touched symbols across `__tests__/` → 12 pre-existing files, ran together: **103/103 pass** — claim said 87/87 (count mismatch, 0 fail either way; same non-blocking prose-miscount class as cycle-682's TASK_2006 suite-glob mismatch below, not fabrication — plausibly file composition drifted over 2 intervening weeks of unrelated commits touching the shared store). Full 13-file run (incl. new test): 108/108. `tsc --noEmit` clean, `mock-guard.sh` PASS on both production files. DDD: `getCycleBootstrap.ts`'s infra import is pre-existing (same import line present before this diff, only the import LIST widened) — not a new violation. Live health probe (port 3000, running container): `toolCount=183` vs claimed 184 — deployed image is stale/drifted (2+ weeks of intervening unrelated deploys since this commit), non-blocking since the diff touches zero tool-registration files.

VERDICT (both): DONE_VERIFIED. `[QA] Review Record (direct-commit verify)` written to each row's own `status_note` field (new field on row 1, since none existed — `.note`/`reviewed_at`/`reviewed_by` left untouched). Moved `qa[]`→`done_verified[]` for both, one combined `orch-apply.sh` write (`verification.raw_probe` attached to both, `orch-validate` Stage 1g surfaced 16 PRE-EXISTING unrelated missing-dependency warnings — non-fatal, not from this write; conservation-check clean, task_total 748 preserved; `.head` left untouched — still tracking the outer dispatcher-held task-lock wrapper, not row-specific, per established precedent below). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEPs qa-S96/qa-S97. No `task_release` called — router holds the outer sprint-task lock this cycle (dispatcher's own responsibility per INV-GATEWAY-1); confirmed router's noted CI-red on current HEAD is unrelated (this row's commit predates it by 2 weeks, own targeted suite is clean).

## cycle-686 · 2026-08-13T11:02:38Z · FIX-DRAIN-PAYLOADREF-DANGLE-ON-MOVE — Direct-Commit Verify, APPROVED, DONE_VERIFIED (DUPLICATE-MINT FINDING)

Dev-team Review-Lane QA-Drain spawn (router PRE-CLAIM, `task_id=FIX-DRAIN-PAYLOADREF-DANGLE-ON-MOVE mode=verify-committed`). Row has no `.commit`/`.files[]`/`.owner`; `review_note` (reviewed_by=developer) reads as fresh work. Derived commit via `git blame` on `repointPayloadRefs()`/`movedRefs` in `scripts/agents-flow/drain-signals.js` → `84096f6170`, bundled under an unrelated peer subject (`fix(auditor-tier1): skip obsolete socat-bridge plist`, sibling commit `0e28eed23` same tick) — content verified via `git show --stat`/`git log -p`, on main ancestry.

**Duplicate-mint finding:** `git log --all --grep="FIX-DRAIN-PAYLOADREF"` + `docs/data/orch/archive/2026-07.json` prove this exact task-id was already implemented, QA-APPROVED, DONE_VERIFIED, and round-2 re-gated on **2026-07-21** (double QA pass, `qa_regate_round2_by=qa`), then cold-evicted to archive. The live row's `review_note` is a **verbatim copy** of the archived row's `review_note`. Traced provenance: re-entered `review[]` via a 2026-07-29 "un-strand stale row" board commit (`21d4c1809`), sat ~2 weeks, then today's QA-Drain picked it up as new. No functional defect — the code fix is real and still correctly live at HEAD (independently re-verified below), so approved rather than blocked; flagged the task-id-collision class in status_note for PO/board-hygiene follow-up (not re-mint prevention — outside QA's remit).

Independently re-ran `node scripts/agents-flow/drain-signals.test.js` → **51/51 PASS**, including the exact 3-assertion `FIX-DRAIN-PAYLOADREF gate` (file→processed/, payload_ref repointed, `orch-validate.mjs` exits 0) plus the ENOBUFS `maxBuffer` regression scenario from the original round-1 CHANGES_REQUESTED fix. `mock-guard.sh --files scripts/agents-flow/drain-signals.js` → PASS. Zero DDD import violations (plain script, not domain/src); `process.env` hits are pre-existing override conventions, zero secrets. `tsc` N/A — file is outside any tsconfig include path.

VERDICT: APPROVED, DONE_VERIFIED (genuinely re-verified live, not rubber-stamped from the duplicate prose). `[QA] Review Record` appended to the row's own `status_note`; `verification.raw_probe` attached (RC-VERIF gate — id not grandfathered); `next_agent` removed via `del()`. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 746 preserved; Stage 1g's 16 warnings pre-existing/unrelated). `.head` left untouched — batch dispatch, peer row (`FIX-ORCHSTATE-CONSERVATION-GUARD-QA-LANE-BLIND`) already closed by a separate pass. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S99. No `task_release` called — dispatcher's own responsibility per INV-GATEWAY-1.
