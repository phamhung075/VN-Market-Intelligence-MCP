## cycle-870 · 2026-08-26T14:22Z · FIX-ORCHAPPLY-CAS-BASELINE-CAPTURED-AFTER-CALLER-JQ-READ, direct-commit re-verify (redispatch_count=1) — DONE_VERIFIED, qa[]->done_verified[]

**My own prior CHANGES_REQUESTED (qa-S202, 2026-08-24) blocked solely on AC-4 (zero real callers migrated + no findable follow-up row). Re-derived the developer's disjunct-(a) response from source instead of crediting the commit message.** Grep-confirmed `scripts/orch-cold-evict.sh`'s `get_hash()`/`HASH_BEFORE` (captured at retry-loop top, before `compute_id_maps()` reads the hot file) threads `ORCH_APPLY_CALLER_BASELINE_HASH` into `scripts/orch-apply.sh:158-166`, which prefers hash > mtime > self-capture (mechanism itself unchanged since round 1, `orch-apply.sh` untouched this pass). Confirmed this is a REAL production caller, not just a test: `git merge-base --is-ancestor 0f1bf668e <commit>` true for BOTH of today's live cold-evict commits (`49b04fbaf` 15:39Z, `b1d3b977e` 16:10Z — the very tick I was dispatched from), and `git log 0f1bf668e..b1d3b977e -- orch-cold-evict.sh orch-apply.sh` empty (no revert landed in between).

**AC-5 trap: did not credit the developer's stash-repro narration or the green run alone.** Built my own isolated reproduction via `git worktree add --detach <scratch> 0f1bf668e^` (genuinely pre-migration `orch-cold-evict.sh`, confirmed no `ORCH_APPLY_CALLER_BASELINE_HASH`/`get_hash` present), overlaid the CURRENT test file (carries TEST 13) and ran it there: all 4 T13a fires-assertions FAIL (`"guard is a no-op"`, `"PEER-INJECTED row missing"`, stale candidate applied) while the T13b no-injection control still passes — 71/77 total in that worktree vs 75/77 at HEAD, delta of exactly 4 matching the 4 fires-assertions. Removed the worktree after. Ran both suites live at HEAD: `orch-cold-evict-tests.sh` 75/77 (2 FAILs = pre-existing unrelated T7 poison-eviction, confirmed by name/line, matches claimed 67/69 baseline + 8 new); `orch-apply-wrapper-tests.sh` 109/109 (untouched this pass, confirmed by diff).

**AC-3 ruling (the genuinely open question): threshold=1 kept — ruled SATISFIED, not silently left.** Independently grepped (not trusted) all 3 named call sites: `docs/agents/qa/flow/main.md` `vc-changes` (`qa[]->review[]`, the exact path I am running right now), `docs/agents/dev-team/flow/main.md:327-340` (WF-1a BLOCKED self-heal) and `:471-487` (resume-attempt-bound-exceeded self-heal), both `in_progress[]->backlog[]` — all 3 real, and none sets `ORCH_APPLY_DECLARED_BACKWARD_LANE_MOVES` (0 hits outside the checker's own file), so all 3 genuinely depend on the undeclared tolerance, confirming lowering to 0 unmigrated would fail-close them. Also found, independent of the developer's own framing: `orch-conservation-check.mjs:595` appends `(N tolerated backward move(s): <id>)` to the OK line whenever a within-tolerance backward move occurs, surfaced to every caller's stderr via `orch-apply.sh` — the AC's own "reject OR loudly flagged" disjunct is met even for the tolerated case, not silent as I'd implied in my own prior pass's phrasing.

**Verdict: DONE_VERIFIED — all 5 ACs met, 4 with fresh independent evidence, not prose.** `mock-guard.sh --files "scripts/orch-cold-evict.sh scripts/orch-conservation-check.mjs"` → "No production source files to scan. PASS." (bash/mjs zone, `bun test`/`tsc` structurally N/A — no `apps/` file touched). Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh`, `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified (status==DONE_VERIFIED, no `next_agent` key, `verification.raw_probe.tool` present, 0 rows left in `qa[]`). `.head` was pinned to this row (in_progress, next_agent=qa) — reset to idle in a follow-up guarded write (`if .head.active_task_id == $tid then ... else . end`), self-verified. Outer lock left untouched per router instruction (INV-GATEWAY-1, dispatcher's exclusive responsibility). Noted but explicitly NOT scored: `orch-cold-evict.sh`'s separate BSD-`mktemp` non-randomization defect at `:1234`/`:1250` (literal `.hot-evict-XXXXXXXX.tmp` in this tick's own log) — belongs to `FIX-ORCHAPPLY-MKTEMP-SUFFIX-DEFEATS-RANDOMIZATION-BSD-CONCURRENT-WRITER-COLLISION`, a separate row, per the dispatch prompt's explicit instruction not to conflate. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` STEP qa-S245.

## cycle-871 · 2026-08-26T15:55Z · FIX-AUDITOR-NOTEBOOK-COMPOSE-COMMITMSG-MARKER-GATE, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

Re-ran both regression suites myself (`commit-msg-notebook-compose-marker.test.sh` 5/5, `pre-commit.test.sh` 15/15, no regression from the `install.sh` edit), `bash -n` clean on all 3 shell files, `.git/hooks/commit-msg` symlink confirmed live and pointing at the tracked source, ancestry + all 4 claimed files confirmed touched by `16e2096bb`.

**Corrected the dispatcher's own framing on the regex-scope question.** `git show --stat efe62d83d` proves the nested-bracket DATA-tier commit touches the SAME pilot notebook (`docs/agent-memory/notebooks/system-auditor.md`), not "a different notebook outside this pilot's path gate" as the spawn prompt claimed — DATA tier was wired into the identical shared notebook-write terminal binding (`FIX-AUDITOR-DATA-TIER-NOTEBOOK-WRITE-PATH-UNWIRED`). Live-tested both regexes against that real message: the brief's narrow regex fails to match it, the shipped widened regex matches. So the regex widening is load-bearing TODAY for this exact pilot scope, not merely a hedge against a not-yet-merged fleet-wide rollout.

Reproduced both dispatcher-flagged residual gaps live in a scratch repo: (a) a `git mv` onto the pilot notebook path produces `R100`, confirmed to bypass the `^[AM]` path-gate entirely — but this is a pre-existing blind spot already shared by `_check_auditor_heartbeat_shapes`/`_check_notebook_immutability` in `pre-commit` (same `^[AM]` pattern, grep-confirmed), not unique to this new hook, so ruled fleet-wide follow-up, non-blocking. (b) empty-`$COMPOSE_MARKER` degrade on the ALL_GREEN SKIP path confirmed unreachable today (SKIP stages nothing for the notebook, no commit is ever created, per `main.md:1057`) — non-blocking.

**AC-2 (live WARN observed on a real notebook commit) ruled non-blocking-pending, not held in review.** Zero qualifying commits landed since install (7 real commits post-`16e2096bb`, none touch the notebook) — developer disclosed this honestly rather than claiming it. Signed off anyway: the mechanism was independently re-exercised via a realistic git-init/add/commit harness (not just direct script invocation) plus a confirmed-live symlink, and this mirrors the standing precedent set by `FIX-AUDITOR-DATA-TIER-NOTEBOOK-WRITE-PATH-UNWIRED`'s own AC-5 (a live/trend criterion correctly left open post-DONE_VERIFIED rather than blocking sign-off).

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh` (dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`), `next_agent` deleted same write, `verification.raw_probe` populated with the real commands above, self-verified on the live file. `.head` left untouched — outer lock/row lock is the dispatching dev-team session's, not re-claimed or released here. Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` STEP qa-S246.

## cycle-872 · 2026-08-26T17:19Z · FIX-FLEETPUSH-DISARM-EXISTS-ONLY-IN-UNTRACKED-PLIST-REPO-COPY-SILENTLY-REARMS, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Not-Before Gate Check (Step 0d) ran first: row carried none of qa_not_before/next_recheck_not_before/qa_new_window_earliest_d1_close — proceeded unchanged to Direct-Commit Verify, no reversal.** Ruled this row NOT OOM/crash-durability-class myself against the Pipeline's own detection rule (title/AC/status_note assert nothing about crash/OOM/memory bound — it is a launchd install-time guard) rather than taking the dispatch prompt's word for it.

Verified commit `ced606a22` on main ancestry, touching all 3 claimed files (`scripts/install-launchd-plist.sh`, `scripts/install-launchd-plist.test.sh`, `docs/standards/cron-jobs.md`). Ran `bash scripts/install-launchd-plist.test.sh` live myself: 19/19 pass (fixture-only, `/private/tmp` tree via `INSTALL_LAUNCHD_PLIST_DST_DIR`, confirmed never touches the real installed plist). `bash -n` clean on both scripts. `mock-guard.sh --files scripts/install-launchd-plist.sh` -> \"No production source files to scan. PASS.\" (bash zone, no `apps/` file touched, `bun test`/`tsc` correctly N/A).

**AC-3 (option (b), not tracked Disabled=1) independently confirmed, not credited from the commit message:** `git show --stat ced606a22 -- launchd/com.vn-market.fleet-push.plist` and `git diff HEAD` on that same file both empty; `plutil -p` on the tracked repo copy shows no `Disabled` key, `RunAtLoad=1`/`StartInterval=1800` unchanged — the entire fix lives in the new install-time guard script, the tracked plist is untouched. **AC-4 (plutil never grep) independently reproduced, not trusted from prose:** read the shipped script — line 110 uses `plutil -extract Disabled raw`, never grep; then independently reproduced the exact grep-false-negative live on the REAL installed fleet-push plist (`grep -c Disabled` = 0 matches rc=1, `file` confirms Apple binary plist) — the precise failure mode this AC exists to close. **AC-5 (no re-arm, no push) independently confirmed against the live host, not the developer's claim:** `plutil -extract Disabled raw` on the real installed plist = `true` rc=0 (still disarmed); `launchctl list | grep vn-market` shows fleet-push entirely absent (not loaded — docker-events/docker-cleanup/socat-tls-bridge/cowork-guaranteed-slot-firer present, fleet-push is not); grepped the shipped script for every `launchctl` token — all 3 are comment/echo text, none executed; `git rev-list --count origin/main..HEAD` = 996 (origin/main HEAD `a73f0f2c7`, matching the live `ci-red-a73f0f2c` signal) — no push happened. Captured the installed plist's mtime before and after my own fixture-only test run: `Aug 24 14:33:42 2026` both times, unchanged — my own verification did not mutate the thing it verified (the trap the dispatch prompt named explicitly).

**Verdict: DONE_VERIFIED — no ISSUE found, all 5 ACs independently reproduced against live evidence (script source, real installed plist, launchctl list, git ancestry/diff), not developer prose.** Dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, then live `jq | orch-apply.sh`, self-verified on the live file (status==DONE_VERIFIED, no `next_agent` key, `verification.raw_probe.tool` present, 0 rows left in `qa[]` for this id). `.head` left untouched (still pinned to this row per dispatch instruction — dispatching dev-team session's exclusive responsibility to reset, not re-claimed here). Outer lock `task:FIX-FLEETPUSH-DISARM-EXISTS-ONLY-IN-UNTRACKED-PLIST-REPO-COPY-SILENTLY-REARMS` untouched per INV-GATEWAY-1 (dispatcher's responsibility). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` STEP qa-S247. Did not verify: the `docker-events` label's own missing dedicated doc block in `cron-jobs.md` — honestly disclosed by the developer and in the doc diff as a named, non-blocking gap, out of this P0's bounded scope.

## cycle-873 · 2026-08-26T21:10Z · FIX-DEVTEAM-INCIDENT-LANE-CONSUMER-MAINFLOW, direct-commit re-verify (redispatch_count=1->2) — CHANGES_REQUESTED, qa[]->review[]

**Rework corrected 3 false-claim instances (not the 2 I originally cited by stale line number) — all 3 verified true against `scripts/lib/devteam-eligibility.jq` ground truth: `wip_in_progress` (L115-118) has NO `claimed_by` filter and counts incident rows; `incident_wip_in_progress` (L158-162) filters on `claimed_by=="dev-team (incident-lane consumer)"`; the comment block (L153-157) documents the asymmetry as deliberate/conservative ("shared budget sees MORE load, never less"). Corrected sites: ILC UNCONDITIONAL paragraph (L1037), ILC spawn-context string (L1132), Invariants bullet (L1544) — read each by content past the L1 size-justification comment, none by a stale line number. Confirmed the 3rd (Invariants) is genuinely in-scope, not the decoy `devteam-wrapper-autoclose` bullet (L1534, untouched, correctly TRUE about a different direction: rows leaving ready/in_progress can only shrink the count).**

**Did not stop at "3 instances corrected" — grepped the whole file for the same false-claim shape myself and found a 4th, uncorrected site: `docs/agents/dev-team/flow/main.md:1526` (Reusable Scripts bullet for `devteam-backlog-claim-incident-lane-consumer.jq`) still reads "...the DISTINCT marker `incident_wip_in_progress` counts, excluding this lane from the shared `WIP<=2` slot" — same false claim, untouched by this commits 3-hunk diff (confirmed via `git show f44e4bc04`).** CHANGES_REQUESTED, routed to `.owner` (agent-father), `redispatch_count` 1->2. Runtime-behavior concern (2 in-flight incident rows can saturate WIP<=2 and starve BOUNDED-1/SLS/RLC/DRS) is real but separate per the router's binding scope ruling — minted as a new `signal_queue` row (`qa-20260826T2110-incidentcapwipsaturation`, type=system-issue, to=po) rather than reopening this row for it.

Lane-moved `qa[]->review[]` via `jq | orch-apply.sh` (dry-run rehearsed first via `ORCH_APPLY_LIVE_FILE_OVERRIDE`), self-verified on the live file (status==REVIEW, next_agent is a string=agent-father, 0 rows left in `qa[]` for this id). `.head` confirmed byte-identical before/after (this lane never writes `.head`). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` STEP qa-S248.

## cycle-874 · 2026-08-28T23:43Z · FIX-PREPUSH-SIZELINT-6-OFFENDERS, direct-commit verify — DONE_VERIFIED, review[]->done_verified[]

**Not-Before Gate (Step 0d) ran first: row carries none of qa_not_before/next_recheck_not_before/qa_new_window_earliest_d1_close — proceeded unchanged.** Hand-dispatched row sits in `review[]` (status=REVIEW, next_agent=qa) — the QA-Drain claim script (review[]→qa[]) never ran, so I adapted the flow's vc-approved jq to read from `review[]`/status REVIEW per the chain contract's explicit "move review[] → done_verified[] in the SAME write" (kept all RC-VERIF raw_probe requirements).

**Verified commit b4e885a64 on main ancestry; scope check clean — `git show --stat` = EXACTLY the 6 claimed files, +1 line each, all comment-only size-justification headers, zero behavior change.** Row residency: 1 row in review[], 0 dup in qa[]/done_verified[], next_agent=qa.

**Re-ran every claimed check myself, none trusted from prose:** size-lint `--check` PASS 0 offenders (1457 scanned); per-file header tolerance re-derived from live wc -l (1200/1207, 690/690, 230/235, 580/579, 250/251, 1300/1299 — all within +10%/min-5L); `size-lint-justification.test.sh` 6/6; py_compile clean x5; `tsc --noEmit` exit 0; `tool-registry-parity` 17/17; `task-claim-owner-session-lint` PASS; `mock-guard.sh` PASS on the 6 prod files; `pre-push.test.sh` 4/4. Confirmed the pre-push hook's `run_doc_shaped_checks()` wires size-lint + task-claim-lint + tool-registry-parity on EVERY push — the exact gate that was blocking fleet pushes since >= 2026-08-26.

**Ruled not OOM-class myself** (title/AC/status_note assert nothing about crash/OOM/memory bound). **Behavioral-predicate AC out of scope** (zone="multi" does not start with `apps/`). **Python zone:** change is 6 comment lines, zero code path altered — py_compile (syntax) is the proportionate check; full pytest in the deployed image would exercise nothing new (flow's host-drift rule targets code changes).

**Verdict: vc-approved.** Dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE`, then live `jq | orch-apply.sh` (review[]→done_verified[], status REVIEW→DONE_VERIFIED, `del(.next_agent)`, raw_probe populated from the real commands above), self-verified on the live file (status==DONE_VERIFIED, no next_agent key, verification.raw_probe.tool present, 0 rows left in review[] for this id). `.head` untouched (idle, not pinned to this row). Decision journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-31.md` STEP qa-S249.
