## cycle-841 · 2026-08-25T16:12Z · FIX-CCATO-NTG-ROWS-NOT-PRODUCED-BY-EITHER-SANCTIONED-ENGINE-FORGED-WRITER-ID — verify-committed: DONE_VERIFIED 6/8 AC (1 refuted, 1 met-by-state), qa[]->done_verified[]

**Score the row's PREMISE before its checklist — this title is a false accusation.** "NEITHER sanctioned engine can have produced them" + forged `_updated_by` are both FALSE, re-proved not inherited from `verify_note`: deleting ONLY the new `writeSignalsFn: () => {}` stub from `CCATO-MCP-T5-USECASE.test.ts` and re-running that one file fires **exactly 6** `NTG-GUARD-REFUSED-TEST-HARNESS-LIVE-WRITE` lines, live `orch-state.json` sha byte-identical either side. `runNarrativeTruthGate.ts:87/110` defaults `writeSignalsFn` to the REAL writer and forwards an undefined `orchStatePath` (-> `DEFAULT_ORCH_STATE_PATH`) with the frozen clock: a unit test wrote production state on every `bun test`.

**"NOT DONE" and "REFUTED AS WRITTEN" are different verdicts; only the second may pass.** AC-2 wanted producer auth in `orch-apply.sh` because "every signal_queue write passes through it". Checked rather than accepting either side: `appendSignalQueueRow` writes `writeFileSync`+`renameSync` DIRECTLY (`orchStateStore.ts:217-218`), so the leak never touched `orch-apply.sh`; and `SIGNAL_WRITER_ID="narrative-truth-gate"` (`narrativeContradictionSignalWriter.ts:56`) is a SECOND SANCTIONED producer — the check misses this leak AND rejects genuine rows. Passed the row with the refutation recorded file:line, and flagged the residual (orch-apply validates shape, never provenance) as needing a NEW row — never re-hang a real gap on refuted evidence.

**An AC can be harmful to implement, and saying so is part of the verdict.** AC-7's second clause rejects a `ts` "second-identical to an existing row", but `runNarrativeTruthGate.ts:110` passes ONE `now` into the whole finding loop — every row of one legitimate run is second-identical BY CONSTRUCTION. Marked CONTRAINDICATED so no redispatch re-demands it.

**Replay the SURVIVING population, at PER-ROW clocks.** The gate says "reconstruct all 42 rows"; 42 (later 110) are gone — the real set is 8 (6 leaked + 2 genuine). All 8 through the SHIPPED guards at each row's own wall clock (`git log -S` on its introducing commit): 6 quarantined, 2 ACCEPTED, **0 false rejects**. Per-row matters: genuine `82a3ee` sits 11.8min from its commit stamp vs a 15min tolerance, so one batch clock manufactures a false quarantine.

**EXECUTE "byte-faithful twin" claims; RUN the dedup jq.** python `build_summary` vs TS `buildContradictionSummary` on 6 inputs incl. 2 overflow edges: ALL BYTE-IDENTICAL. Shipped bash `jq` reduce run verbatim: 2 repeats -> `occurrences=3`, same-key/different-`type` separate, unkeyed rows untouched. `loadNullMarkers()` read 10 markers from `claim-tool-map.json` at runtime; its substring/case-insensitive semantics MATCH the bash classifier at `narrative-truth-gate.sh:262-271`, so apparent over-breadth is SSOT parity.

**Full-suite disjointness is structural — grep it, do not read it.** 15479 pass / 52 fail over 1286 files (dev figure reproduced exactly on a 2nd run; 1st gave 15480/51, so +/-1 is flake). Extracted the failing FILE set (17): every one holds ZERO refs to `orchStateStore|appendSignalQueueRow|narrativeContradiction|signal_queue`. 2nd forward flag on the row: `collapseOrPrepend` bumps `.occurrences` in place WITHOUT resetting `.status`, so a same-day re-emission of an already-READ finding never resurfaces as NEW — bounded to one day by `dedup_key`'s `{cycle}`, and exactly what AC-6 mandates. Detail: qa-29.md STEP qa-S215.

## cycle-842 · 2026-08-25T17:35Z · FIX-PDFX-TESSERACT-CONFIDENCE-MEAN-OVER-NONEMPTY-MASKS-TOTAL-PAGE-MISS, direct-commit verify — CHANGES_REQUESTED, review[] -> in_progress[]

**Grepped the binary before trusting it — the dev's own notebook flagged a prior stale-image miss on this exact fix.** Rebuilt `pdf-extractor:latest` fresh from clean HEAD (`git status --short -- apps/pdf-extractor/` confirmed 0 diff), then `docker run --rm --entrypoint sh ... grep -n 'RESCUE FIRED' /app/infrastructure/ocr_backends.py` before running anything, so every check below ran against a binary provably containing e9144ea75's instrumentation. Live container left untouched throughout (still on the old image sha).

**AC-2/AC-3/AC-4 as scoped to FPT Q4 2025 all independently reproduced.** Page 9: read live `market.db` readonly (bun:sqlite) — md_len 452, all 9 figures verbatim; also reproduced from my own from-scratch `ocr-backend-bench.sh tesseract-vie auto` run: 29/30 units byte-identical sha256, rescue fired exactly 1x on page 9. cgroup memory.peak 50.2%/46.0% of the 2560 MiB cap, 0 hard-limit hits either backend — matches the commit's claim in magnitude. One number did NOT reproduce: the claimed +3.8% table-phase overhead measured +54.9% on my run (same script) — flagged, not blocking (AC-4's literal text only needs "under cap with headroom," which holds).

**The router's "highest-value check" was the deciding one, and it failed.** Ran the same probe + bench pair against a SECOND real document (DBC_2025_Q4.pdf, unrelated issuer, same production corpus — not cherry-picked). Rescue fired on 6/13 (46%) table regions vs 1/30 (3.3%) on FPT. Read every flagged region's raw tesseract text myself (never inferred from the score, which would be circular) — all 6 are coherent, correctly-parsed VND accounting lines, not misses. Ink-coverage floor for a confirmed-legitimate region measured 0.329 — under the 0.5 gate AND under the FPT-derived "0.674 lowest-legit" floor the whole design argument rests on.

**A second, undisclosed risk showed up that the brief didn't even ask about: memory, not just diacritics.** Auto-mode on the second document pinned the container at EXACTLY the 2.5 GiB cgroup cap (memory.peak == memory.max, 494 `memory.max` events, 0 on FPT) — did not OOM-kill, but repeatedly hit the hard ceiling under ordinary input. This is a materially different risk profile than the single-document "49.8%, 0 hard-limits" the row certified against.

**Unprompted corroboration, not coincidence.** `.head.next_action` (read before I wrote anything) already named `PROBE-PDFX-OCR-CONFIDENCE-SECOND-DOCUMENT-MARGIN` as an open gate (G2) on this exact question, with its own AC-2 bar: "lowest-legit ink coverage >= 0.50, no legitimate region at/below a genuinely-broken one." My data fails that bar outright (0.329 legitimate). Per that row's own AC-5, retuning the 0.5 constant to compensate is a move PO already rejected once — flagged so nobody quietly does it here instead.

**Lane-move mirrored this board's real dispatcher path, not the flow-doc's `qa[]`-sourced template verbatim.** This row was never routed through a distinct `qa[]` lane — the developer's own commit moved it `in_progress[]->review[]` directly. Reversed the same way: `review[]->in_progress[]`, `status: REVIEW->IN_PROGRESS`, `next_agent=dev-pdf-extractor`, `redispatch_count 0->1`. Dry-ran via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first (validator/conservation/prose-ceiling green — target lane isn't a `PROSE_CEILING_LANES` member so the row's 11.9KB prose was never a constraint here), landed for real, self-verified by re-read. Full data + reproduction commands: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S216; row's own `verify_note`.

## cycle-843 · 2026-08-25T20:30Z · FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-ROUTER-SESSION-20-WARNS, direct-commit verify — HOLD, no lane-move (qa_not_before not elapsed)

**A row can name its own re-dispatch gate, and that gate outranks "re-verify now."** The row carries `qa_not_before: 2026-08-26T14:59:30Z` and an explicit PO-authored routing note: any Review-Lane QA-Drain pick before that timestamp "MUST read qa_not_before first and return HOLD without consuming a redispatch." This drain claimed the row at `2026-08-25T20:21:26Z` — only 5.4h/24h (22.6%) into AC-3's window, 18.6h early. Re-ran what's checkable anyway rather than stopping cold: AC-1/AC-2 confirmed present at source (`docs/agents/cowork-team/flow/telemetry.md` Step 6.2, `main.md` dispatch table, both in commit `e6adc6dfe`, main-ancestor confirmed). AC-4 independently re-executed live (not trusted from prose) in a fresh sandbox repo with the real hook symlinked in: `caller_chain` appeared on all 3 claimed surfaces (stderr banner, `.git/sweep-guard.log`, `docs/signals/` bug-escalation payload), naming both the invoking command line and the top-level `claude -n <family>` identity; re-ran `scripts/git-hooks/pre-commit.test.sh` myself, 15/15 PASS, matching the commit's claim exactly.

**Did not certify AC-3 on favorable-looking early data — that is the exact anti-pattern this row's own PO ruling was written to prevent.** Checked anyway, for completeness, not for a verdict: since the anchor only 1 BARE fire logged (system-auditor class, not cowork-tick), and >=3 tick commits (`4def20db9`, `74c01149f`, `8fda9e649`) each touch ONLY `docs/data/cowork-schedule.json` + their own `docs/signals/cowork-team-*.json` — i.e. already pathspec-scoped, satisfying AC-3's clause-(ii) positive-exercise requirement if the window closes clean. But `feedback_ac3_durability_certified_on_window_that_ended_before_metric_settled` and this row's own ruling text ("clause (ii) is MANDATORY and NON-WAIVABLE... an absence-based AC needs a liveness companion or it certifies blindness") both bar treating 22.6%-elapsed silence as a pass. Per instruction, did not write `orch-state.json` (dispatcher's job) — reported HOLD for the router to action (no lane-move, no `redispatch_count` bump). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-29.md` STEP qa-S217.
