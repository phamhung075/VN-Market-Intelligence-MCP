# QA — Notebook

## cycle-693 · 2026-08-13T14:29:04Z · FACTORY-GUARD-CI-COMPROOT-LOGIC-IMPL (developer, real code, commit `9431a7cdd`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), router explicitly asked for RAW re-verify, not trust — row also carried a prior dev-team self-verify from 2026-07-30 (own notebook), re-done independently anyway, not relied on. Row carried no `.commit`/`.files[]`/`.owner` — derived `9431a7cdd` via `git log --all -- <claimed files>`, confirmed on main ancestry, `git show --stat` matches all 7 claimed files exactly (composition-root-logic-gate.go, composition-root-logic-gate_test.go, adapters.go, main.go, usecases_vmt_liquidity_resolvers.go, ci.yml, dev-standards.md).

Re-ran fresh: gate `--check` across all 7 real services → 0 violations (matches claim); `composition-root-logic-gate_test.go` 7/7 PASS; `go build`/`go vet`/`golangci-lint run` clean on macro-indicators; `go test ./... -count=1` 12/12 packages pass. Confirmed `policyRatesAdapter.FetchPolicyRates`/`omoAdapter.FetchOMO` structurally gone from `adapters.go` — replaced by 0-branching delegation shims (`policyRatesHTMLAdapter`/`policyRatesDBAdapter`/`omoRawAdapter`); decision logic now lives in the new `usecases_vmt_liquidity_resolvers.go`; `LiquidityStateUseCase` itself 0-diff at this commit (a later unrelated commit `e10e2a500` touched it, out of scope). CI job wiring (7-service positional-arg list) + `dev-standards.md` CANONICAL pointer both confirmed via direct read, not cited from prose.

Live-container claim independently reproduced right now (not just trusting the 07-30 verify): the container is still running the same pre-commit image (2-week uptime, `Created` predates the fix commit), hit `POST /liquidity-state` on port 5004 — both claimed fallback branches genuinely live (`policy_rates.source: "sbv_rates DB fallback (HTML parse failed)"` is_estimate:true, `omo.blocked_reason: "OMO HTML parse: no add/absorb rows found"`). `mock-guard.sh` PASS on all 4 touched production Go files.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 741 preserved; Stage 1g's pre-existing unrelated warnings, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S105. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.

## cycle-692 · 2026-08-13T14:05:47Z · FACTORY-GUARD-CI-TSBOUNDARIES-IMPL (developer, real code, commit `66ab9deb3`+companion `8c45fc1a0`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), router explicitly asked for RAW re-verify of every review_note claim, not trust. Row carried no `.commit`/`.files[]`/`.owner` — derived via `git log --grep=TSBOUNDARIES -i`: `66ab9deb3` (feat, wires 3 ESLint fence jobs + news-fetch-go-lint + fixes 3 live DDD violations + news-fetch elements-map gap + 5 dead frontend lint-disable comments), companion `8c45fc1a0` (frontend bun.lock regen, unblocks frontend-eslint's install step — already separately verified by a prior QA cycle 2026-08-06 `f38f02223` under its own task id, out of this row's scope). Both confirmed real, on main ancestry.

`git show --stat` matches all 17 claimed files exactly. Line-by-line diff-read (not trusted from commit-message prose) confirmed every DDD fence fix genuinely landed: `getMoneyRadarComposite.ts` (application/usecases) import lines now point to `infrastructure/db/foreignFlowQueries.js` + sibling `./computeCreditFlowSignal.js` instead of reaching up into `interface/mcp/tools/**` (Fence-B fixed); `marketWideForeignFlowTool.ts` shrunk to a re-export shim, query SQL moved byte-identical into the new infra file; news-fetch `eslint.config.mjs` confirmed newly maps `src/routes/**` to `"interface"` type (previously unclassified/invisible to the plugin); `fetchArticle.ts` no longer imports `infrastructure/` directly — new `setPlaywrightLauncher` DI seam wired from `src/index.ts` (composition-root) at module load; `recoverMissingOhlcvSession.ts` confirmed physically relocated to `scheduler/market-data/`, both downstream import paths repointed. Frontend: grep-confirmed zero `exhaustive-deps` disable-comments remain in the 3 named zone files.

LIVE re-run (not reused from claim): eslint fences exit 0 on all 3 services (mcp-server/news-fetch/frontend, frontend only carries pre-existing plugin-deprecation warnings, zero violations — same noise class prior sibling cycle logged); `tsc --noEmit` clean x3; `golangci-lint run --config .golangci.yml` on news-fetch Go module → 0 issues. mcp-server 9-file targeted suite: 132 pass/0 fail (claim was 133/133 — 1-test count delta, non-blocking, 0 fail either way, no related test file found via grep to explain it, treated as prose-counting variance not a regression). news-fetch full suite: 241 pass/0 fail/6 skip — exact match. frontend full vitest: 2183 pass/2 fail/2185 — exact match; both fails (QUE-TOOLTIP-DRY-1a-codegen-pipeline / QUE-REFERENCE-PAGE-detail, QUE_DESCRIPTIONS key-count) confirmed pre-existing+unrelated, files untouched by this commit, already confirmed unrelated by 2+ sibling QA cycles this same week.

`mock-guard.sh` CAUTION (exit 2, non-blocking) on 1 marker in `computeCreditFlowSignal.ts:230` — `git log -p` confirmed this exact commented-out `fetchViraSurvey` TODO line pre-existed verbatim in `creditFlowTools.ts` before relocation (moved, not newly fabricated). `size-lint-justification.sh --check` PASS (0/1384 files). `metric-mask-lint.sh --check` PASS (0/434 files). DDD scan (grep forbidden imports on all 9 touched files): clean. Security scan (process.env/password/secret/apikey): zero hits.

STRONGEST evidence: pulled live production CI via `gh run view` on main's most recent push (run `31707531319`) — all 4 jobs this task added show green (`mcp-server-eslint`/`news-fetch-eslint`/`frontend-eslint`/`News Fetch Go Lint`). Overall workflow shows red, but inspected both failing jobs' own logs directly and confirmed zero relation: `bun test` fails at the pre-existing full-suite per-file-isolation step (tracked flake class, board row `FIX-MCP-SUITE-HEALTH-BASELINE`, none of this task's 9 files involved); `task-claim-owner-session-lint` is an unrelated lint job with zero connection to eslint-plugin-boundaries. AC "ci-green" proven with real GH Actions evidence for this task's own scope, not just local re-run.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 742 preserved; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). No merge/push/branch-delete needed — work already on main. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S104. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.

## cycle-691 · 2026-08-13T13:38:44Z · FACTORY-GUARD-CI-METRICMASK-IMPL (developer, real code, commits `7a758d90f`+`33aee81e2`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

Dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), router explicitly asked for RAW re-verify of every review_note claim, not trust. Row carried no `.commit`/`.files[]` — cited commits taken from review_note prose, then independently confirmed: `7a758d90f` (feat) + `33aee81e2` (memory) both on main ancestry (`git merge-base --is-ancestor` OK), `git show --stat` matches all claimed files exactly (`scripts/audits/metric-mask-lint.sh` new, `cascadeEngine.ts`, `marketSentimentCalculator.ts`, `watchlist.ts`, `brokerCredibilityTools.ts`, `.github/workflows/ci.yml`, `dev-standards.md`, `metric-mask-lint.test.sh`).

Diffs verified line-by-line against claim: `cascadeEngine.ts:356,375,394` `seedEntry.confidence ?? 0.6` → `?? 0` (×3, feeding `detectMsciInclusion`/`Watchlist`/`Exclusion`); `marketSentimentCalculator.ts:174` `row.impact_score ?? 1.0` → row excluded entirely via new `if (row.impact_score === null) continue` guard; `watchlist.ts:198` + `brokerCredibilityTools.ts:51` both carry `metric-mask-allow:` annotations exactly as claimed (the 5th catch beyond brief scope). Ran `metric-mask-lint.sh --check` live: PASS, 0 offenses (434 keyword-bearing files — vs dev's 423 at 07-29, expected 2-week codebase-growth drift, not a discrepancy). Proved zero-tolerance is REAL not asserted: injected a synthetic untracked file with `?? 50` — lint correctly FAILed (exit 1, exact file:line), cleaned up after. `metric-mask-lint.test.sh` reproduced 10/10 PASS. CI wiring confirmed: `metric-mask-lint` job present in `.github/workflows/ci.yml`, `ubuntu-latest`/checkout-only. `shellcheck` clean both new scripts. `tsc --noEmit` 0 errors. `mock-guard.sh --files <4 touched prod files>` PASS. DDD scan: domain-layer files (`cascadeEngine.ts`, `marketSentimentCalculator.ts`) carry zero real infrastructure/application imports (grep hits are comments only); interface-layer files' infra imports (`watchlist.ts`, `brokerCredibilityTools.ts`) are the expected composition-root pattern, not a violation. Security scan clean.

Targeted suites reproduced EXACTLY matching dev's claimed counts: FIX-1279-msci-bullish 14/14, P0-4-market-sentiment-index 37/37, cascade files (1279a+247+FIX-1268+1224) 31/31, 915-broker-credibility 22/22, watchlist (082+220) 25/25 — 129/129. Ran a FRESH independent full `bun test` (not reused from dev's claim): 15262 pass/40 skip/56 fail/1 error/15358 total (480.28s) — total grew vs dev's 2-week-old 15007 (matches this sprint's own documented weekly test-count growth trend seen across sibling QA cycles); fail count 56 sits inside the same contemporaneous pre-existing flake band (44-63) independently confirmed by multiple sibling QA verify cycles this same week, tracked by still-BACKLOG board row `FIX-MCP-SUITE-HEALTH-BASELINE`; grep-confirmed ZERO of the 56 fails + 1 unhandled-error (DB-closed race in an unrelated `pollNews` 5000ms-timeout test) reference cascade/sentiment/watchlist/broker/credibility/msci/metric-mask by name or file.

VERDICT: DONE_VERIFIED, zero code ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note`; `verification.raw_probe` attached. Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 744 preserved; Stage 1g's 16 pre-existing warnings unrelated, none touching this row). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-19.md` STEP qa-S103. No `task_release` called — no MCP gateway tool bound in this specialist invocation (Read/Edit/Write/Bash only); dispatcher's finally-block + TTL(3600s) expiry is the authoritative release path per INV-GATEWAY-1.
