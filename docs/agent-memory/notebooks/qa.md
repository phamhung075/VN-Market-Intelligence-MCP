# QA — Notebook

## cycle-647 · 2026-08-12 · FACTORY-GUARD-CI-no-hardcode-allowlist-scan (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `9581fa432`)

Design-only task, no code to test — row's own `review_note`: "verdict brief adequacy + child scoping — NO code to test." Same epic/pattern as cycle-646's sibling (`FACTORY-GUARD-CI-depguard-tier-boundaries`), 5th of 7 `ci-regression-prevention` guardrails. Commit `9581fa432` confirmed real: ancestor of `main` (`git merge-base --is-ancestor`) AND present on `origin/main` (`git branch -r --contains`) — not prose-only. `git show --stat` matches claimed scope exactly: brief file (50L new) + `orch-state.json` (+21L, child-row mint only) + architect notebook/journal, zero production code touched.

Did not trust the row's `review_note` prose alone: spot-checked 3/5 claimed offender sites at commit-time blob — `newsChainFallback.ts:224`, `backfillBctcScalarsTool.ts:199`, `pharmaEventMapper.ts:288` — all match verbatim. Confirmed the "zero existing CI/lint hardcode coverage" claim via grep on commit-time `ci.yml` (1 hit only, the unrelated Fence-A import-allowlist comment, exactly as the brief describes). Confirmed `JANITOR-034`/`JANITOR-035` both exist in `docs/data/code-janitor-known-findings.json` (`status: proposed`), backing the brief's "requires human design decision, not mechanical generalization" framing for the annotate-not-generalize design call (vs a generic cross-file ticker-overlap detector, explicitly deferred to avoid FPs against dozens of legit overlapping domain rule tables). Went further: child row `FACTORY-GUARD-CI-NOHARDCODE-IMPL` (minted `dev_agent`/`next_agent`=developer, zone `cross-service/`, `brief_ref` wired) has since shipped independently and sits in `task_board.review` (own separate later QA gate, not re-verified exhaustively here — 5-site scope + fixes + annotations + CI wiring landed per the brief's §4 file list).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) — Stage0+1 PASS, conservation OK (task_total 765=765, signal_total 59=59, signal_row_identity=clean; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. `.head` untouched (pointed at unrelated row `FIX-AUDITOR-T1-T3-CLEANEXIT-HEARTBEAT-STAMP-SKIPPED-T2-UNAFFECTED`, not this one). Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S62. `task_release` is the router's job — not attempted here. Pruned cycle-644 (oldest by position, `FIX-AUDITOR-A30-DEEPPROBE-TRUNCATES-ON-SECOND-CONTAINER-RAG-SERVICE`) this write per AC-2 retention (3-section cap) — kept 646/645.

## cycle-646 · 2026-08-12 · FACTORY-GUARD-CI-depguard-tier-boundaries (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `d44818f1e`)

Design-only task, no code to test — row's own `review_note`: "verdict brief adequacy + child scoping; do NOT re-verify child-task offender lists exhaustively." Sibling of `FACTORY-GUARD-CI-metric-mask-lint`/`size-lint-justification` in the same epic (own prior cycles already pruned by peer concurrent writes to this file — 3-row batch sharing `claimed_at=2026-08-12T02:22:14Z`, this is 1 of 3, `.head` pointed at a different sibling row so left untouched). Commit `d44818f1e` on `main` ancestry; diff matches claimed scope exactly (46L brief + 2 backlog-row mints, notebook/journal only, zero production code).

Spot-checked (not exhaustive) the dispatcher's 3 anchors: Python Fence-C confirmed live verbatim at `apps/pdf-extractor/pyproject.toml:69-76`; TS fence content pre-existing in mcp-server/news-fetch `eslint.config.mjs` (real gap was zero eslint CI wiring); Go composition-root offenders (macro-indicators `adapters.go` FetchPolicyRates/FetchOMO) as claimed. Went further: both minted children (`FACTORY-GUARD-CI-TSBOUNDARIES-IMPL`, `FACTORY-GUARD-CI-COMPROOT-LOGIC-IMPL`) have since shipped and sit in `task_board.review` (own later QA gate, untouched here) — landed diffs match brief §4 scope verbatim (5 new CI jobs incl. `composition-root-logic-gate`, `scripts/audits/composition-root-logic-gate.go`+`_test.go` present, macro-indicators `pkg/application/` liquidity resolvers present, `adapters.go` no longer defines FetchPolicyRates/FetchOMO).

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` via `orch-apply.sh` — Stage0+1 PASS, conservation OK (task_total 767=767, signal_total 70=70, signal_row_identity=clean). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S59. `.head` untouched (correctly left as-is — pointed at sibling row `FIX-CRON-ALERTSCAN-CONFIG-KEYS-ORPHANED-BY-PARALLEL-WRAPPER`, already resolved below by a peer). `task_release` is the router's job — not attempted here. Pruned cycle-643 (oldest by position, `FACTORY-GUARD-CI-metric-mask-lint`) this write per AC-2 retention (3-section cap) — kept 645/644.

## cycle-645 · 2026-08-12 · FIX-CRON-ALERTSCAN-CONFIG-KEYS-ORPHANED-BY-PARALLEL-WRAPPER (dev-mcp-server) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `93c920f63`)

Router-dispatched (`mode=verify-committed`, 1 of a 3-row batch sharing `claimed_at=2026-08-12T02:22:14Z`; `.head.active_task_id` pointed exactly at this row — reset to idle in the same write). Row uses `commit_sha`/`status_note`/`dev_note` fields (post-drain schema, no derivation needed). Collapsed `taAlertScan`/`bbAlertScan` CRONS keys into one `alertScanParallel` key (`cronConfig.ts`), repointed `schedulerJobTable.ts`'s `alertScanParallelJob` entry's `cron:` field to it (2 files/18 ins/5 del).

RAW-verified, not trusted from `dev_note` prose: commit on `main` ancestry, `git show --stat` matches claimed scope exactly. Targeted `bun test` (10 cron/scheduler files — cronStatusCompute, cronStatusHandler, 1309c-alert-scan-parallel-job, FACTORY-SCHEDULER-job-table-registry, FIX-SCHEDULER-DOUBLE-REGISTRATION, 1307/1309-*-alert-scan-job, FIX-CRON-WATCHDOG-COVERAGE-2026-07-22, layerBCronRegistry, FIX-CRON-SSCCHECKERJOB-DEAD-87D-classfix-registry-invariant): 140 pass/0 fail. `tsc --noEmit` 0 errors. `mock-guard.sh` PASS. DDD/security greps clean (schedulerJobTable.ts's pre-existing `../infrastructure/db/checkpoint.js` import predates this commit, not domain code, not introduced here). Repo-wide grep: ZERO residual `CRONS.taAlertScan`/`CRONS.bbAlertScan`/`CRON_TA_ALERT_SCAN`/`CRON_BB_ALERT_SCAN` anywhere (src, tests, docker-compose, .env). Independently re-read `cronStatusCompute.ts:151-177` directly: `alertScanParallel` correctly excluded from `STATIC_JOB_NAME_MAP` (deliberate, per dev's design rationale); `normalizeJobToken('alertScanParallel')===normalizeJobToken('alertScanParallelJob')==='alertscanparallel'` — tier-2 self-resolution confirmed exactly as claimed (AC-2).

AC-3 (2 consecutive clean auditor Tier-1 cycles for taAlertScan/bbAlertScan) is a post-deploy live-observation check — needs a container rebuild to deploy this code, then 2 live auditor cycles after. Dev's own `dev_note` + dispatcher coordination context both pre-flagged this as a known, non-blocking gap; this commit's own AC trailer scopes itself to AC-1/AC-2 only, both fully satisfied and independently code-verified. FLAGGED, not actioned: recommend PO/ops track a rebuild+2-cycle-observe follow-up to formally close AC-3 — do not let this row closing to DONE_VERIFIED silently drop that observability step.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) AND reset `.head`→idle in the SAME `orch-apply.sh` write (`.head.active_task_id` pointed exactly at this row) — Stage0+1 PASS, conservation OK (task_total 767=767, signal_total 70=70, signal_row_identity=clean; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S60. `task_release` is the router's job — not attempted here. Pruned cycle-642 (oldest, `FACTORY-GUARD-CI-size-lint-justification`) this write per AC-2 retention (3-section cap) — kept 644/643.

## cycle-647 · 2026-08-12 · FACTORY-GUARD-CI-dead-code-gate (architect, design-only) — Direct-Commit Verify, APPROVED, DONE_VERIFIED (commit `3b4d5a0d3`)

4th of 7 ci-regression-prevention siblings (peer already cleared `no-hardcode-allowlist-scan` this same batch, `claimed_at=2026-08-12T03:52:58Z`, 3-row batch — `.head` pointed at a different row, `FIX-AUDITOR-T1-T3-CLEANEXIT-HEARTBEAT-STAMP-SKIPPED-T2-UNAFFECTED`, left untouched). Row's own `review_note`: "verdict brief adequacy + child scoping — NO code to test." Commit `3b4d5a0d3` on `main` AND `origin/main` ancestry; `git show --stat` matches claimed scope exactly (46L brief + orch-state.json +20L child-mint only + notebook/journal, zero production code). Independently traced the separate closeout commit `b418c9ad6` too (not just the design commit) — confirmed in_progress→review flip, head→idle, child `brief_ref` backfill all happened as claimed.

Read the full 46L brief directly: ticket-premise corrections (`.bak` count stale, real debt is 4 `_deprecated/` dirs + an unlabeled `apps/technical-analysis/src/` orphan from the PO-decided Go rewrite), zero-existing-CI-coverage audit, sound zero-tolerance sizing call (100% pure-subtraction/zero-import-verified vs size-lint's refactor-risk baseline case), 4-check `dead-code-gate.sh` design, explicit knip/ts-prune/vulture defer rationale — all load-bearing, no unsupported claims. Child `FACTORY-GUARD-CI-DEADCODE-IMPL` correctly minted at commit time (dev_agent/next_agent=developer, zone `cross-service/`, into `task_board.backlog`) and has since shipped independently (own `review`-lane row, next_agent=qa, separate QA gate) — spot-checked only that its scope narrative matches brief §3 verbatim (incl. a transparently-logged sound deviation: Check-3 twin-scaffold rule generalized to avoid a live news-fetch false-positive), not re-verified exhaustively per this row's own instruction.

VERDICT: APPROVED. Moved `qa[]`→`done_verified[]` (`status:DONE_VERIFIED`, `qa_verified_by:qa`, `verification.raw_probe` attached) via `orch-apply.sh` — Stage0+1 PASS, conservation OK (task_total 765=765, signal_total 59=59, signal_row_identity=clean; Stage 1g's 16-row report pre-existing unrelated). `[QA] Review Record (direct-commit verify)` appended to the row's own `review_note` field. Own QA DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-16.md` §qa-S63. `.head` untouched (pointed at a different row). `task_release` is the router's job — not attempted here. Pruned cycle-644 (oldest, `FIX-AUDITOR-A30-DEEPPROBE-TRUNCATES-ON-SECOND-CONTAINER-RAG-SERVICE`) this write per AC-2 retention (3-section cap) — kept 646/645.
