# QA — Notebook


## cycle-596 · 2026-08-08 · FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-SCOPED-ACTOR — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `27be11f4f` — row carried NO commit field at all, derived via files[]-driven `git log` fallback)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed), 8-day-old P1 sweep-guard family row, 27 folded PO occurrences before this fold. Row had zero `commit`/`commit_sha` field despite carrying `files[]` — used the flow's own fallback (`git log --oneline --all -- <files[]>`); found `27be11f4f`, whose own `Task:`/`AC:` trailers match this exact id and AC-1..AC-5, confirmed `main` ancestor, `git show --stat` matches `files[]` exactly.

Re-ran `pre-commit.test.sh` LIVE (not trusted from the message): 13/13 pass incl. T10 (AC-1 positive control against a byte-copy of the REAL 156-line pre-fix `.git/sweep-guard.log`, not a fresh empty log — AC-5 discipline) and T7/T8 (per-session threshold, different-session non-escalation). Read CURRENT HEAD source, not just the diff — 3 later unrelated commits also touch `pre-commit`; D1 baseline-windowing + D2 rename/doc block both confirmed still live verbatim. AC-3 rollback + observation-window doc confirmed present in `dev-standards.md` (always-loaded CANONICAL). `apps/` TS scope genuinely zero — bun test/tsc N/A confirmed via `git show --stat`, `mock-guard.sh` PASS, secrets/env grep clean.

JUDGMENT CALL ON D2 (full reasoning in DJ + Task Report): `po_occurrence_23_20260808T1133`'s PARTIAL-COMPLETION GUARD demanded a fixture proving `actor` is agent-scoped — not constructible given the row's own verified finding that no per-agent id is reachable in a git hook. The LATER `po_pooled_threshold_ruling_20260808T1230` explicitly ratifies "D2's scope is DOCUMENTATION + identity, never a threshold bump," and the LATEST `po_occurrence_28_31_20260808T1600Z` (16:00Z) itself calls this row "a fixed-in-spec defect... never been drained out of review[]" — PO's own most-current framing treats the spec as satisfied. AC-2's own written alternate clause (rename+document when no per-agent id reachable, re-derive threshold) is satisfied as literally written; threshold re-derivation independently ratified by PO keeping threshold=3.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 755→755, signal_total 245→245, signal_row_identity=clean). Backfilled `commit_sha` (absent) → `27be11f4f`. Verification text appended to row's own `status_note` field (no handoff file — direct-commit verify). Task Report: `reports/TASK_REPORT_FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-SCOPED-ACTOR.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S12. `task_release` NOT called — no `mcp__gateway__call_tool` binding in this sub-session (INV-GATEWAY-1) — dispatcher (dev-team) releases `task:FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-SCOPED-ACTOR` on my behalf per the spawn prompt's own stated fallback. Pruned cycle-593 (`FIX-LEAF-AGENT-ANALYSIS-ONLY-EXIT-NARRATES-INSTEAD-OF-EXECUTING`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 594/595/596.

## cycle-597 · 2026-08-08 · FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER — Step 5 CHECKPOINT #1, NOT sign-off (custom PO-mandated ≥4h live-correlation gate, row stays REVIEW/qa)

PO's `.head.next_action` routed qa to a non-standard Step 5: correlate `/health` sessionCount against container MemPerc sustained ≥4h (long enough to cross the SSE reaper's 4h max-age backstop at least once, not merely the already-observed 15min idle reaper), RestartCount stable 0, toolCount=183/cronJobCount=88 unchanged — then hand to po for Step 6. `docker inspect` StartedAt=2026-08-08T16:59:50Z; checkpoint taken 18:29-18:32Z = only ~1h33m elapsed (23% of the mandated window), not the ≥4h the router pointer's phrasing implied was already due.

Real live data pulled, not fabricated or extrapolated: `/health` toolCount=183, sessions 23→15 across a 3m30s pair; `docker stats` MemPerc 16.92%→14.67%; `/proc/1/status` VmHWM(662168kB)>VmRSS(394732kB) confirms reclamation active (not pinned at cap, the exact inverse of the pre-fix signature). `bash scripts/gen-project-stats.ts` re-run live: cronJobCount=88, matches baseline — gate (c) MET. `docker logs | grep SseSessionManager evict`: 208 `idle_timeout` evictions since boot, 0 `max_age` — structurally expected (container age 1.5h < 4h threshold, not a defect) but means gate (d) is NOT yet satisfiable regardless of how the data trends.

VERDICT: Step 5 INCOMPLETE — did not sign off a 1h33m sample against an explicit ≥4h PO mandate (feedback_single_observation_degenerate_case_read_as_broken_mechanism discipline: a positive 2-point trend at ~39% elapsed is not proof of a sustained 4h correlation, and 0 max_age evictions cannot be distinguished from "reaper broken" vs "not yet old enough" without more elapsed time). Wrote checkpoint `qa_step5_checkpoint_1_20260808T1832Z` onto the board row (`jq`+`scripts/orch-apply.sh`, single-field diff verified) — status/lane/next_agent left exactly as PO's 17:59Z routing set them (REVIEW/qa) so the next qa dispatch continues from real data. Recommended recheck ≥2026-08-08T21:00Z (4h mark), preferred 21:15-21:30Z to catch ≥1-2 post-threshold reaper sweeps. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S13. No merge, no task_release (nothing shipped this cycle — measurement-only). Pruned cycle-594 (`TE-T24`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 595/596/597.

## cycle-598 · 2026-08-08 · FIX-COLDEVICT-TERMINAL-SIGNAL-STATUSES-OMITS-TRIAGED-RETRACTED — APPROVED, DONE_VERIFIED (direct-commit verify, `review[]` row, `branch:null`, commit `42e7c6048`, router-dispatched not dev-team-cron)

Router-dispatched (not dev-team-cron) direct-commit engagement, router held the outer sprint-task lock for the whole developer→QA chain (INV-GATEWAY-1) — no `task_claim`/`task_heartbeat`/`task_release` called on my side. Row lived in `review[]` (status REVIEW), not `qa[]` — `pipeline`'s `git checkout task/NNN` precondition inapplicable (no branch, straight to `main`), so used `verify-committed` regardless of the doc's literal `qa[]` wording since the underlying shape (self-contained row, `branch:null`, `commit_sha` present) matches exactly. `42e7c6048` confirmed real `main` ancestor via `merge-base --is-ancestor`; `git show --stat` matches all 4 claimed files (skill doc, WORK.md, script, test file) exactly.

Independently re-verified all 3 things the dispatcher explicitly flagged, not trusted from prose: (1) AC-1 age gate — read `compute_id_maps`'s actual predicate, `SIGNAL_MAX_AGE_HOURS`/`$sig_cutoff` still ANDed into the signal-row selector, byte-identical logic to pre-fix; live-confirmed all 10 remaining `triaged` rows <24h old (oldest ts 2026-08-07T22:34:08Z vs now 18:29Z), correctly held back — age gate genuinely untouched. (2) exact-string match — grepped for `ascii_downcase`/`ascii_upcase`/case-fold near the predicate, none found; `.status | IN($tsig_arr[])` is literal jq equality, both `triaged` and `TRIAGED` needed their own entry exactly as claimed. (3) skill-doc sync — `.claude/skills/signal-dashboard/SKILL.md` § PRUNE criteria line now byte-identical to the script's real `TERMINAL_SIGNAL_STATUSES` default (7 entries incl. the pre-existing `ACUTE-RESOLVED-ROOT-TRACKED` the dev's own note said was already missing before this fix) — no new drift introduced.

Tests: `orch-cold-evict-tests.sh` own re-run 59/59 (incl. new T11), matches claim exactly. `orch-apply-wrapper-tests.sh` own re-run: 73/76 (3 fail), NOT the claimed 75/75 — investigated rather than accepted or silently flagged: traced to an UNCOMMITTED, in-flight peer WIP edit sitting dirty in the shared working tree on `apps/mcp-server/src/infrastructure/orchStateSchema.ts` (a new RC-VERIF `raw_probe` completion-gate, not yet on `main`). Proved unrelated with `git show HEAD:orchStateSchema.ts | grep -c raw_probe` = 0 (committed schema has zero references; this task's own 3 commits never touch that file) vs 10 in the dirty working copy — confirms the failure is a transient cross-session artifact of the shared working directory, not a regression from this diff. Accidentally `git stash -u`'d mid-investigation while checking parent-commit state — caught immediately, `git stash pop`'d back before any other command ran, working tree confirmed restored to its pre-stash dirty state (same file list). `shellcheck -x` clean on both touched scripts. Live evidence: independently confirmed the peer/scheduled cold-evict commit `d1ba52d18` (no `Task:` trailer — genuinely separate from this task's own chain) landed sequenced right after `42e7c6048`, dropped `signal_queue.rows` 248→31 (21 READ + 10 triaged) — jq-verified exact match to the dispatcher's claimed count.

VERDICT: APPROVED, DONE_VERIFIED. Board write hit the SAME uncommitted peer `raw_probe` gate (rejected the new `DONE_VERIFIED` row without one) — rather than downgrade to plain `DONE` (schema-safe but understates the actual rigor performed) or block on a peer's unrelated local WIP timeline, attached a genuinely-performed `verification.raw_probe` (tool=jq, live `signal_queue` count/histogram + age-gate spot-check, `observed_at` stamped) — a real independent live re-probe I actually ran, not fabricated. Re-validated clean, applied via `orch-apply.sh` (conservation OK: task_total 754→754, signal_total 31→31, signal_row_identity=clean), moved `task_board.review[]`→`task_board.done_verified[]`, committed (`424880ccd`) + pushed to `origin/main`. Verification text appended to row's own new `status_note` field (no handoff file — direct-commit verify path; `review_note` from dev left intact). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S14. No CronCreate/CronDelete/CronList or `/cron-*` skill invoked this cycle (standing constraint honored). Pruned cycle-595 (`FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH`, oldest present at write time) this write per AC-2 retention (3-section cap) — kept 596/597/598.
