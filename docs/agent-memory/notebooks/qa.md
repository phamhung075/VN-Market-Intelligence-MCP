# QA — Notebook

## cycle-744 · 2026-08-14T15:04:29Z · FIX-AUDITOR-HEARTBEAT-OUT-OF-CONTRACT-AGENT-WRITE-TIER1 (cross-service/system-auditor, commit `612ee0954`+`06f15af0d`) — Direct-Commit Verify, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row with self-declared 2-commit `.commit` field (guard+docs+test / live-file repair) — both confirmed real, on `main` ancestry, `git show --stat` matches every claimed file for both exactly.

Independently re-verified all 6 AC, not trusted from `status_note` prose alone: ran `pre-commit-auditor-heartbeat.test.sh` live (6/6, matches claimed RED 3/6→GREEN 6/6); regression `pre-commit.test.sh` (15/15, grown from claimed 6/6 via unrelated later commits) + `auditor-tier1-probe.test.sh` (226/226, grown from claimed 141/141) both 0-fail; `bash -n` clean; `mock-guard.sh` PASS (shell not TS-scanned); DDD N/A (no `apps/mcp-server/` touched); security greps clean. Read the live INSTALLED `.git/hooks/pre-commit` symlink (byte-identical to tracked source, not stale) and confirmed `_check_auditor_heartbeat_shapes || exit 1` runs unconditionally before the sweep-guard mode dispatch (AC2/AC3). Read live `main.md` §Tier-2/3 Heartbeat Write — `AUDIT_TIER` 2/3 gate intact, writer still bare `{last_healthy_at}`, no `suppress_heartbeat` port (AC6). `CANONICAL:SSOT-AUDITOR-HEARTBEAT-SOLE-WRITER` cited from both writers plus `tier1-probe.md` and the guard's own REJECT strings (AC1, wider than claimed).

Went wider than the row's own claim: found 3 real `docs/signals/processed/commit-sweep-guard-*.json` REJECT signals (2026-08-08, 2026-08-11×2) proving this guard has already blocked 3 further real out-of-contract hand-write attempts live in production since landing — none reached `main`. Cross-checked the row's own `po_live_recurrence_20260808T1248Z` note (a 4th incident, `34521315f`, that DID reach git by changing only the `last_healthy_at` value while keeping `checks{}` shape intact — outside this SHAPE-only guard's scope by design, not a guard defect) — confirmed via `git log` this was already reverted same-day by `e9a8c6015`, restoring the exact prior-genuine value the PO note prescribed; remediation already complete. Live tier1 file today (`2026-08-14T14:43:21Z`) passes the shape guard; tier2/tier3 carry no `checks` key.

DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` STEP developer-S23 carries `task-id: FIX-AUDITOR-HEARTBEAT-OUT-OF-CONTRACT-AGENT-WRITE-TIER1` verbatim.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-21.md` STEP qa-S141.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write. Dry-ran the jq transform to a scratch file first, inspected before applying. `orch-apply.sh`: Stage0+1 PASS (pre-existing Stage 1g dependency-report noise only), conservation OK (task_total 728=728, signal_total 24=24). Re-read the live file after write: row physically absent from `.task_board.qa[]`, present in `.task_board.done_verified[]` with `status:"DONE_VERIFIED"`. Pathspec-scoped commit `cc3a4502f` (orch-state.json only). Task-lock held by the dev-team dispatcher per spawn instruction — no `task_release` call made here.

## cycle-743 · 2026-08-14T15:05:37Z · FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW (dev-mcp-server, commit `8b4f977fd`) — Direct-Commit Verify, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row, `branch:null`, PO-ratified architect spec (`po_goahead_20260812T035700`) implemented verbatim per its own `reviewed_note`. Commit `8b4f977fdf4d0784d4190a3fd244ce3f8e704be4` confirmed real, on `main` ancestry, `git show --stat` matches all 5 `files[]` entries exactly (`windowPartitioner.ts`, `refinedMarkdownParser.ts`, `finalizeBctcRefine.ts`, `continuation-stitch.md`, `main.md`) plus 2 flagged extras (`getBctcPendingRefineTool.ts` wire pass-through + the new regression test file) — consistent with the row's own disclosure. No later commit touched any of the 5 files since.

Read the actual diffs line-by-line, not the reviewed_note prose: `resolveColumnLayout(headerCells, inheritedLayout)` now falls back to `inheritedLayout ?? "code-first"` (was hardcoded `"code-first"`) — the exact defect this task exists to fix (a maxWindowPages-truncated tail window's headless markdown was silently swapping code/label on label-first bank continuation tables). `parseRefinedMarkdown`'s new `initialColumnLayout` param / `ParseResult.finalColumnLayout` and `finalizeBctcRefine.ts`'s `carryColumnLayout` are a genuine, verified structural mirror of the pre-existing `carrySection`/`finalSection` thread — same call-site shape, same ordering guarantee. `windowPartitioner.ts`'s new `pendingTruncationTail` local correctly delays the cap-hit flag by exactly one loop iteration onto the NEXT window (the true tail), matching the `Window.truncated_continuation` docstring.

New fixture `FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW.test.ts`: 7/7 pass, 37 expect — exact match to the row's own claim (`rows_parsed=5`, tail-window code/label order correct, BEQ-7 override to PARTIAL). Broader targeted sweep across 31 BCTC/refine-pipeline test files (grep-derived superset covering every touched function/module): 404/404 pass, 0 fail — zero regression (row claimed 412/32 via a differently-scoped grep; zero-fail is the decisive signal either way). `bun tsc --noEmit`: 0 errors. `mock-guard.sh` on all 4 touched production files: PASS. DDD scan: all 3 core touched files live in `application/` (not `domain/`) — `logger`/`db` infra imports are pre-existing, legitimate application-layer deps; `refinedMarkdownParser.ts` (module header claims "pure function, no I/O") genuinely has ZERO infrastructure imports, confirming its own claim. Secret/`process.env` greps clean.

Cross-checked 2 supplementary claims live, not trusted from prose: `gen-project-stats.ts --dry-run` → `toolCount=183/cronJobCount=88`, exact match. `size-lint-justification.sh --check` → PASS, 0 unjustified offenders (`windowPartitioner.ts` actual 154L vs its own header's declared 149L — 5L drift, within the script's own +/-10%/min-5L tolerance, non-blocking). Doc diffs (`continuation-stitch.md`/`main.md`) reviewed — informational-only, no new leaf-worker step introduced, matches claim.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S144.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `reviewed_note` field; `commit_sha`+`qa_verified_at`/`qa_verified_by`+`verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write (row not RC-VERIF-grandfathered — created 2026-08-01, after the 2026-07-17 cutoff). Dry-ran the jq transform to a scratch file first — found it already stale (qa lane had shrunk from an earlier-observed 8 to 3 under concurrent peer QA-drain sub-sessions) — re-read the live board fresh immediately before the real write and rebuilt the transform against it via the canonical `jq | orch-apply.sh` pipe (its own CAS-mtime guard covers any remaining race). `orch-apply.sh`: Stage0+1 PASS (pre-existing Stage 1g dependency-report noise only, unrelated), conservation OK (task_total 728=728, signal_total 24=24). Re-read the live file after write: row physically absent from `.task_board.qa[]`, present in `.task_board.done_verified[]` with `status:"DONE_VERIFIED"`. Task-lock held by the dev-team dispatcher per spawn instruction (INV-GATEWAY-1) — no `task_release` call made here.

## cycle-742 · 2026-08-14T15:04:36Z · ALT-FUNC-02-FIX (dev-mcp-server, commit `6fed9dc8b`) — Direct-Commit Verify, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `qa[]` row self-declared "verification-only, no code change needed". Row's own commit `6fed9dc8bcd21752729363d0fbcd97060592535a` touches ONLY notebook + decision-journal — confirmed real, on `main` ancestry, `git show --stat` matches both `files[]` entries exactly. Did not stop there: independently confirmed the row's cited underlying fix commit `815ccaedd` (2026-06-10T17:24:08Z) is itself real, on `main` ancestry, and its `alertAccuracy.ts` diff genuinely adds the top-level `accuracy_rate:number|null` scalar (`hits/scoreable`, `null` iff `hits+misses===0`) — the actual AC this backlog row exists to verify.

Grep-confirmed `accuracy_rate` is still live in the current source, un-clobbered by the one later commit that also touched the file (`e170624d3`, unrelated ISO-8601 datetime-strcompare fix, verified out-of-scope by reading its diff). Re-ran the row's cited tests myself, not trusted from prose: `1982-quality-burndown-CHIJ.test.ts` 18/18 + `183-alert-accuracy.test.ts`/`1847d-D-alert-tools.test.ts` 28/28 = 46/46 pass, exact match to the row's own claim. `bun tsc --noEmit` 0 errors. DDD scan: `alertAccuracy.ts`'s `infrastructure/db/schema`+`alertStore` imports are interface→infrastructure (allowed DDD layering, zero domain-file violation). Secret/`process.env` greps clean. No production source in THIS commit → mock-guard N/A (Smart-Skip, docs-only diff).

Independent LIVE probe (`mcp-call.sh get_alert_accuracy {}`), deliberately not the row's own cited numbers (which were captured ~10min earlier and had already rolled — 30-day alert window is not static): `accuracy_rate:1` (in `[0,1]`), `insufficientSample:false` — contract genuinely satisfied on live `main` right now, not just historically. Raw counts differ (my probe: total 425/hits 30 vs row's total 628/hits 110) but that's expected window drift, not a discrepancy — the shape/contract is identical. Cross-checked `docs/data/quality-checklist.json` independently: `ALT-FUNC-02.status="PASS"` since `2026-06-10T18:00:00Z` citing the same commit `815ccaed`.

DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-22.md` STEP qa-S142.

VERDICT: `DONE_VERIFIED`, zero blocking ISSUE. `[QA] Review Record (direct-commit verify)` appended to the row's own `status_note`; `commit_sha` + `verification.raw_probe{tool,args,live_value_observed,observed_at}` attached same write. Dry-ran the jq transform to a scratch file first (lane lengths at build-time qa 8→7, done_verified 26→27) — re-read the live board fresh immediately before the real write and found it had moved further under concurrent peer QA-drain sub-sessions (qa 8→3, done_verified 26→31) — rebuilt the transform against the fresh read to avoid a CAS-stale candidate. `orch-apply.sh`: Stage0+1 PASS (pre-existing Stage 1g dependency-report noise only, unrelated), conservation OK (task_total 728=728, signal_total 24=24). Re-read the live file after write to confirm the physical lane-move: row absent from `.task_board.qa[]`, present in `.task_board.done_verified[]` with `status:"DONE_VERIFIED"`, `next_agent:"pm"`. Task-lock held by the dev-team dispatcher per spawn instruction (INV-GATEWAY-1) — no `task_release` call made here.
