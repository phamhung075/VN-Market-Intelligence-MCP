## cycle-859 ·  · FIX-QADRAIN-DONE-TO-QA-SCORES-BACKWARD-CONSERVATION-ABORTS-WHOLE-DRAIN, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Verified developer's fix for the sibling defect (LANE_RANK array-position vs tier) that co-produced cycle-858's not-before gate work.** `fe013f22f` decoupled `LANE_RANK` from `FLAT_TASK_LANES` array position into an explicit tier map (`backlog:0,ready:1,in_progress:2,review:3,done:3,qa:4,done_verified:5`) so a legit `done[]->qa[]` drain no longer scores BACKWARD; `done_verified` stays sole terminal. `grep -rn LANE_RANK` = exactly 1 code call-site (line 471, the comparison itself) — matches the review_note's claim.

**Did not trust the shipped 12/12 or the blast-radius narrative — re-derived both independently.** Ran `test-orch-conservation-lane-rank.sh` (12/12), `test-devteam-donelane-drain.sh` (60/60), `test/orch-apply-wrapper-tests.sh` (109/109) live — all matched claimed counts. Built 3 fixtures NOT copied from the diff: (A) 3 done + 1 review batch -> qa in one write, exit 0 clean (independently proves the blast-radius restoration — a legit forward batch no longer aborts); (B) `done_verified[]->qa[]` 2-row stale revert, exit 1 rejected; (C) `review[]->backlog[]` 2-row stale revert, exit 1 rejected (two negative controls on boundaries the shipped suite did not cover, confirming the guard stays armed, not merely disarmed).

**Mutation check: reverted the fix in scratch (parent commit copy), re-ran the shipped suite against it.** 8/12 checks flip red, scoped exactly to AC1/AC2/AC3 (the rows targeting this defect) — NG1/NG2 and the AC3 content-check stay green, since those exercise dimensions the fix does not touch. Confirms the suite discriminates the real defect rather than passing vacuously. Bonus finding: under the PRE-FIX code, my own negative-control fixture B would NOT have been rejected either (old array-position rank scored `done`(5) as "ahead of" `qa`(4), so a `qa->done` revert read as forward, not backward) — the old bug was bidirectional-wrong at this boundary, not just false-positive on the forward direction; the fix corrects both.

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh`, `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified on the live file. `.head` was pinned to this row — reset to idle in a same-cycle follow-up write (`next_action` required `del()`, not `null` — same optional-not-nullable contract as `next_agent`). Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S234.

## cycle-860 · 2026-08-26T06:23Z · FACTORY-KINHDICH-extract-hexagram-data, direct-commit verify — DONE_VERIFIED, qa[]->done_verified[]

**Behavior preservation DEMONSTRATED, not assumed** — the whole point of a Factory extraction. Wrote a temp in-package Go dump-test, ran it in a worktree at the pre-refactor parent commit and against current HEAD: `GetAllQueReferences()` JSON output is byte-identical across all 64 hexagram entries (5761L dump, 0 diff lines). Also dumped the intermediate refactor-only commit (928557a66, before the diacritics fix 88e93338f): reproduced a real 3-value diacritics regression (THUAN LOI/BAT LOI/TRUNG TINH lost their Vietnamese diacritics) across all 64 `MarketTrendLabel` entries — and confirmed both shipped tests (`TestQueReferenceProseInvariant`, `TestQueReferenceHexagram1Snapshot`) PASSED anyway on that broken commit. Non-discriminating for label-content corruption — flagging as a real, narrow test-suite gap (1/64 ids, 2/9 fields covered by the snapshot test), not blocking since the fix already landed and my own re-derivation confirms HEAD is correct.

**Mutation-check confirms the snapshot test IS discriminating within its narrow scope.** Corrupted ID=1 `coreMeaning.en` in a scratch worktree copy of `hexagram_prose.json` — `TestQueReferenceHexagram1Snapshot` failed exactly as expected. Non-vacuous on what it covers; just very thin breadth.

**Re-ran every gate fresh, did not trust the reported pass.** `go build ./...` + `go vet ./...` clean; `go test ./...` all zone packages green; `golangci-lint run ./...` → "0 issues." (depguard fences hold); G12 sandbox both tiers fresh: primitive 15/15 GREEN, module 2/2 GREEN — matches the commit-message claim exactly. `mock-guard.sh` PASS on both touched production files. Live container cross-check: `curl :5005/hexagram/{1,2,30,64}/explain` matches the source-derived dump exactly, diacritics correct — RAW-verify claim independently reproduced, not just trusted.

**No orphaned dead code** — grep confirms `buildAllQueReferences` fully removed, no leftover copy. `depends_on` prerequisite (`FACTORY-KINHDICH-add-data-invariant-test`) confirmed `DONE_VERIFIED` on 2026-08-14, before this row — sequencing respected. `size-lint --check`: file not among the 6 fleet offenders (header says 163L, actual 167L — 4L drift, non-blocking).

Lane-moved `qa[]->done_verified[]` via `jq | orch-apply.sh`, `next_agent` deleted same write, RC-VERIF `verification.raw_probe` populated with the real commands/observations above, self-verified on the live file. `.head` was already idle and not pinned to this row (a peer qa session's, stale from an earlier cycle) — left untouched, not mine to reset. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S235. Cleaned up: temp in-package dump-test file + 3 scratch worktrees used for the comparison — none committed, none left in the tree (router flagged one untracked leftover mid-cycle; removed immediately).

## cycle-861 · 2026-08-26T06:24Z · FACTORY-STOCK-extract-vndirect-mapper, direct-commit verify — CHANGES_REQUESTED, qa[]->review[]

**nil-vs-0 preserved end-to-end, independently mutation-tested, not just read.** `912a4bf79` rewires Tier1/Tier2 to call the pre-existing `vndirect-quote-mapper` primitive; the sole raw dereference (`mapper.go:118`) is guarded by `if item.Change != nil` at `:117` (and `:121` for `PctChange`). Collapsed the guard in a scratch edit (nil->0, reproducing the exact bug this row exists to fix) and re-ran the package suite: exactly the 3 nil/0-discriminating subtests (`null_change_and_pctChange...`, `partial_null...`, `omitted_change_fields...`) failed, the other 10 stayed green — proves the coverage discriminates, not vacuous. Reverted via `git checkout`, diff-confirmed byte-identical to pre-mutation.

**Held CHANGES_REQUESTED on 2 gaps against the row's OWN DoD text (not my summary), even though the router's specific nil/0 ask checks out clean.** (1) DoD says "Tier1/Tier2 collapsed to doFetch" — no `doFetch()` exists anywhere in `fetchers.go`; `diff` of the two `FetchPrice` bodies shows they are now byte-identical except the URL string (`:40` vs `:108`) — only the JSON-mapping (~50L) was deduped, ~44L of HTTP-fetch plumbing (client call, headers, status/error handling, body read) is still duplicated verbatim, unmet by construction. (2) DoD's closing clause "RAW-verify the served Price for sample tickers is unchanged after rebuild" — the running `stock-price` container (`docker images`/`inspect`: built 2026-08-06T17:58:56+02:00) predates this commit (2026-08-26T07:21:22+02:00) by ~20 days; live `POST /price/fetch` on the deployed container is still served by the pre-refactor code, so the DSI-INV-1 null-to-0 bug is still live in production today despite the fix being on `main`. `rebuild_required:true` per `backlog-detail.json`'s `dispatch_precondition`, not honored.

**Did not rebuild the container myself** — infra/ops action, outside qa's `not_my_job` boundary, and this is a known, already-tracked systemic gap (`FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`, READY, agent-father) with 2 prior confirmed occurrences already on file (mcp-server memleak, stock-price's OWN prior `FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER` 6-day outage) — flagged as a 3rd/4th live data point for that row rather than treated as this dev's one-off mistake.

**Everything else re-run fresh and green.** `go build`/`go test ./...` (9/9 pkgs), `go vet` clean on touched files (pre-existing `router_test.go` findings confirmed unrelated + pre-dating this commit), `golangci-lint run ./...` (depguard fences) 0 issues, G12 sandbox independently re-run — primitive 9/9, module 2/2, matches the commit message exactly — `mock-guard.sh` PASS, DDD import scan + secrets scan clean, no orphaned dead copy of the old inline mapper anywhere in the tree. Non-blocking observation: the approach text's named `ThousandsVNDScale`/`vnFloors` map were never introduced — inherited from the already-`DONE_VERIFIED` `49bc309a3` primitive, not this commit; substance of dedup (single source of truth) achieved, naming spec not literally matched.

Lane-moved `qa[]->review[]` via `jq | orch-apply.sh` (dry-run rehearsed via `ORCH_APPLY_LIVE_FILE_OVERRIDE` first, then live), `redispatch_count` incremented, `next_agent` falls through to `po` (row carries no `.owner`/`.owner_agent`) — said so in the status_note per the flow's own instruction. Self-verified by re-read on the live file, then committed by explicit path. Full trace: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-30.md` STEP qa-S236.
