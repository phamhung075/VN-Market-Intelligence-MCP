# QA — Notebook


## cycle-591 · 2026-08-08 · FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `147ce3a68` — corrected from stale/dangling `926d6e779`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed), P1 data-integrity. Dispatcher pre-flagged the same dangling-commit pattern as cycle-582/584/586/588: row's own `commit_sha` (`926d6e779`) FAILED `merge-base --is-ancestor`, `fsck --unreachable` confirmed genuinely dangling (Merge Gate reapply artifact — same author/subject/timestamp). Real landing `147ce3a68` confirmed ancestor of both local main and origin/main; diffed the actual production/test code files between both commits directly — byte-identical, only journal/board bookkeeping differs.

AC-1 (root cause): row's own live VPS-push-log evidence read directly — zero push attempts for prices+foreign-flow across the incident window while news/sbv (same receiving code path) kept pushing normally — genuinely disproves the OPS-FFLOW-VPS-CLOCKDRIFT precedent (would've silenced sbv too) and correctly scopes root cause to VPS ops zone, outside apps/mcp-server/. AC-2 (backfill): did NOT trust "UNRECOVERABLE" from prose — RAW re-ran the actual `backfill-foreign-flow-gap-2026-08-06.ts` script LIVE inside the production mcp-server container (docker exec, real named-volume DB): confirms 08-06 rowCount=0, 08-05 truncatedTail=true TODAY, and live re-probed the real bgapidatafeed.vps.com.vn upstream — sample keys are tick-level fields only (fBVol/fSVolume/fRoom/etc.), zero date/range param, VERDICT=UNRECOVERABLE, zero rows fabricated. Matches the row title's own "permanent data loss, no backfill path" premise — AC-2 never claimed restoration, only honest confirmation, which is what shipped.

AC-3 (completeness detector, load-bearing): wiring confirmed in diff (`dataAuditJob.ts` runDailyChecks calls `checkForeignFlowGap`). Went beyond the test suite — directly imported+ran `findForeignFlowGapDays` LIVE against the real production DB inside the container: correctly flags 2026-08-06 among real zero-row trading days (each independently confirmed `COUNT(*)=0`). This live run ALSO surfaced 14 additional pre-existing July 2026 zero-row trading days (capped at `MAX_GAP_DAYS_PER_RUN=15`, independently confirmed genuinely zero-row) not in this row's scope — the detector doing exactly what AC-3 specified (escalate ANY zero-row day, not just 08-06), surfacing a larger latent gap for future PO triage once the nightly `dataAuditJob:daily` cron's own escalation fires (last ran 2026-08-08T16:00Z, before the container's 16:59Z restart — confirmed the new code IS in the running image, `bun run src/index.ts` direct-from-source, container started AFTER the fix commit).

Re-ran RAW, not the dev's self-report: new tests 9/9 + 8/8 pass (exact match); targeted regression (157-data-audit-job 22 + FACTORY-SCHEDULER-split-dataAuditJob 9 + FIX-CONVICTION-HISTORY-EOD-BACKFILL 10) = 41/41 pass — dev's own review_note claims this trio "50/50 pass," a self-report arithmetic error (every individual per-file count I re-ran is exactly correct; the aggregate figure appears to double-count the new detector test's own 9 passes). Zero actual failures anywhere — flagged for calibration, non-blocking. `tsc --noEmit` 0 errors, `mock-guard.sh` PASS, DDD/secret greps clean.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 759→759, signal_total 245→245). Stamped `commit_sha:147ce3a68` onto the row (corrected from dangling). Verification text appended to row's own `status_note` field (no handoff file). Task Report: `reports/TASK_REPORT_FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S9. Both the board write and journal entry were swept into a concurrent peer QA session's own commit (`c5d6ab5e5`) before I reached my own commit step — confirmed byte-identical content, nothing lost. `task_release` NOT called (INV-GATEWAY-1, no gateway MCP binding this sub-session) — dispatcher must release `task:FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL` on my behalf. Pruned cycle-588 this write per AC-2 retention (3-section cap) — kept 589/590/591.

## cycle-590 · 2026-08-08 · FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER — code-correctness APPROVED, held REVIEW/next_agent=ops (Docker Close Gate — NOT flipped DONE_VERIFIED)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed), PRIORITY/CRITICAL — primary root cause of the A-30 mcp-server memory chain. RAW-read all 4 claimed commits' diffs (not the message): `b746c112b` transport.ts collapses 2 parallel Maps + 2 duplicated cleanup blocks into 1 `SessionRecord{transport,mcpServer,heartbeatInterval,createdAt,lastActivityAt}` map + 1 `evictSession()` — confirmed called from all 5 claimed triggers (`res.on("close")`→connection_closed, heartbeat-write-fail catch→heartbeat_write_failure, `reapStaleSessions()` idle branch→idle_timeout, its max-age branch→max_age, new `closeSession()` DELETE path→client_delete); server.ts gained the DELETE `/sse|/messages?sessionId=` route (400/200/404) + `sessions.stopReaper()` wired into `BunServerInstance.close()`; `docs/architecture/microservice/mcp-server/infrastructure.md` doc section matches the code. Re-ran tests myself: `1862c-transport-session-eviction.test.ts` 12/12 pass (36 expect — T9 is a genuine negative control, T10 proves max-age independent of a live idle clock, T12 proves idempotent double-eviction, none tautological); `081-bun-mcp-server.test.ts` 10/10 pass (26 expect, live `client_delete` log observed). `tsc --noEmit` 0 errors, `mock-guard.sh` PASS, DDD/security greps all pre-existing (confirmed via parent-commit diff, zero new violations).

ROUTING GAP CAUGHT AND CORRECTED: `apps/mcp-server/src/` is baked at Docker build time (`COPY apps/mcp-server/src/ ./src/`, Dockerfile:62 — not volume-mounted, confirmed via docker-compose.yml). `docs/protocols/docker-deployment-runbook.md` § Microservice Code-Change Close Gate applies — a code change isn't shipped until the container is rebuilt + SHA-verified. This row's own `claimed_by` routed it straight past that gate into `qa[]`, but the SAME board commit (`d24ddf6b6`) correctly applied it to sibling `FIX-SCHEDULER-DOUBLE-REGISTRATION` (held REVIEW/next_agent=ops/rebuild_required=true) in the identical push — an inconsistency, not two legitimate different rulings. Corrected: moved `task_board.qa[]`→`task_board.review[]`, `status: QA→REVIEW`, `next_agent→ops`, `rebuild_required:true` added, `.head` synced (was pointing at this `active_task_id`) — via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 759→759, signal_total 245→245). Did NOT flip to `done_verified` — code is correct but not yet deployed; PO's standing no-restart-while-in-flight ruling is now discharged, ops may proceed. `qa_review_note`/`qa_reviewed_at`/`qa_reviewed_by` added to the row (no `status_note` field existed). Task Report: `reports/TASK_REPORT_FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-13.md` §qa-S7 (duplicate step-number collision with 2 concurrent peer writes to the same file — content intact, not lost). `task_release` NOT called (INV-GATEWAY-1, no gateway MCP binding in this sub-session) — dispatcher must release `task:FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` on my behalf. Pruned cycle-587 this write per AC-2 retention (3-section cap) — kept 588/589/590.

## cycle-592 · 2026-08-08 · TE-T28 — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `3e4fd2747`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Row carried `commit`/`files[]` already (post-drain shape). Verified `3e4fd2747660a170152c2876d838b47d97eed199` is a real commit (`git cat-file -t`) and on main ancestry (`git merge-base --is-ancestor` true) before reading anything else. `git show --stat` matches all 29 claimed `files[]` exactly (26 new `docs/agents/tools/list/*.md` stubs + `anti-hallucination/SKILL.md` + `dev-standards.md` + new `scripts/gen-tool-list-stubs.py`).

Did not trust "0 flagged registry-only / 0 missing / 0 extra" from the row's own `status_note` — independently recomputed the diff myself (`docs/data/tool-registry.json` 183 tool names vs live `docs/agents/tools/list/` basenames): 0 missing, 1 non-tool "extra" (`INDEX.md`, a directory index, not fabricated litter) — claim confirmed. Did not trust "idempotency re-proven" from prose — ran `python3 scripts/gen-tool-list-stubs.py --dry-run` myself LIVE: `missing=0`, "nothing to do," genuine no-op. Spot-checked 2 generated stubs (`get_market_hexagram.md`, `schedule_task.md`) against the reference `get_price_history.md` shape — `schedule_task.md`'s 10 params correspond to a real live schema, no invented fields; no-param tools correctly show "No parameters" rather than a guessed row.

`bun tsc --noEmit` (apps/mcp-server): 0 errors. `bun test`: N/A for a targeted suite — `git show --name-only` confirms zero `.ts`/`.go` production files touched by this commit (26 `.md` stubs + 1 SKILL.md + 1 policy doc + 1 new `.py` script); full-suite reading correctly pinned to the `BLOCK-PUSH-CRON-AUDIT-BATCH-NO-QA` CANONICAL (dev-standards.md — standing `FIX-MCP-SUITE-HEALTH-BASELINE` red, scope to touched surface). `mock-guard.sh --files scripts/gen-tool-list-stubs.py` → PASS. Fleet-wide grep for dependents on the OLD anti-hallucination SSOT wording (`docs/agents/tools/list/` as sole SSOT): 0 hits — the SKILL.md L55 rewrite orphans nothing live.

VERDICT: APPROVED, DONE_VERIFIED — all checks RAW-true, no `ISSUE` at any verify step. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 755→755, signal_total unchanged). Verification text appended to row's own `status_note` field (no handoff file — direct-commit verify). DJ: `sprint-TOKEN-ECONOMY-AUDIT-qa.md` §qa-S12. `task_release` NOT called for the outer sprint-task lock (INV-GATEWAY-1, no gateway MCP binding this sub-session) — dispatcher (dev-team) releases `task:TE-T28` on my behalf per the spawn prompt's own stated fallback. Pruned cycle-589 this write per AC-2 retention (3-section cap) — kept 590/591/592.
