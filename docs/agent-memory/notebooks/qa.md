# QA — Notebook

## cycle-705 · 2026-08-14T01:33:11Z · FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES (report-analyzer, commit `761988143`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — the detector's only non-zero-diff DETECTED path sourced its evidence from the notebook commit it was checking, so a genuinely non-zero OTHER plane (signal_queue row, ledger entry) with the notebook plane silently missing defeated `all_zero` and PASSed a defective cycle (confirmed live 12x). Did not trust the developer's pasted 15/15 count or the router's own independent-run note — re-ran everything myself.

Commit `761988143` confirmed on `main` AND `origin/main` ancestry; `git show --stat` matches all 3 row-claimed files (`detect-analysis-only-exit.sh`/`.test.sh`, `fail-loud-protocol.md`) plus journal/notebook side-files. `bash scripts/audits/detect-analysis-only-exit.test.sh` own re-run: 15/15 PASS.

Read the core diff at source, not narration: the new MANDATORY-PLANE CHECK reads `docs/agents/<id>/init.md`'s `memory.notebook` field (`none` → not mandatory, else → mandatory), never the artifact under test — satisfies AC-2's "never key on the agent's own claim" carry-forward from the parent row. AC-1's naive `all_zero`→`any_zero` widening is explicitly rejected in the diff in favor of this per-plane subset. Grep-swept `docs/agents/*/init.md` myself (not trusting the dev's "only refine_bctc_md" claim) — confirmed only `refine_bctc_md` declares `notebook: none`; every other fleet agent defaults to mandatory=1, so the conservative default does not mass-false-positive the fleet.

Re-ran BOTH historical repros named in `verification_gate` live, not from prose: system-auditor `--since-ts 2026-08-12T05:35:00Z --until-ts 2026-08-12T05:55:00Z` → DETECTED (`all_zero`+`mandatory_status=violation`; that signal_queue row has since drained per the developer's own honest note, so it no longer isolates the mechanism alone, but the detector still correctly fires). news-scout `--since-ts 2026-08-07T03:55:00Z --until-ts 2026-08-07T04:30:00Z` → DETECTED `mandatory_status=violation` (`ledger=1`/`notebook=0`) — flipped from the pre-fix live PASS documented in the row's own evidence, the exact inverse-partial shape this row exists to close.

AC-3 regression check (own diff, not trusted): T8-T12 (the c80 partial-write contract fixtures) show zero lines changed in the commit diff — confirmed non-regression. AC-4: RED-1 (T2, signal_queue>0/notebook=0), RED-2 (T4, ledger>0/notebook=0), GREEN (T14, no-notebook-mandate agent) all present and green; T6 doubles as AC-5's `--cycle-tag` investigation, empirically proving the identical blind spot closed by the same fix. `shellcheck -x -e SC1091` 0 warnings both changed `.sh` files. `mock-guard.sh` correctly N/A — zone is pure bash+md, no TS/JS production source touched, `bun test`/`tsc --noEmit` N/A.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-6.md` STEP `developer-S104`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live repo. `[QA] Review Record (direct-commit verify)` appended to the row's `status_note` field (no handoff file — direct-commit row). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 733=733). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S115/S116). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool` binding — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` and is the authoritative release path.

## cycle-704 · 2026-08-13T23:59:22Z · FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE (report-analyzer, commit `5ee697852`) — Direct-Commit Verify, APPROVED, DONE_VERIFIED

dev-team Review-Lane QA-Drain spawn (`mode=verify-committed`), `branch:null` row — orch-state `blocks`/`co_edit` sequencing fields were write-only-decorative, PO's own reverse-only `blocks` guard on `FU-BACKFILL-REAL-FILENAMES` (07-25) silently didn't bind. Did not trust `dev_close_note` prose at face value; RAW-verified all 5 ACs from `note`/`po_stubfields_scoping_20260725T1715`/`po_coedit_foldin_20260725T1737` against live code.

Commit `5ee697852` confirmed on `main` ancestry; `git show --stat` matches all 7 claimed files. RAW-ran every claimed suite myself, not trusted from prose: `FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE.test.ts` 11/11 PASS; `orchStateSchema.test.ts` 120/120 PASS (claim said 104/104 — traced the +16 delta to unrelated later commit `ad6e422e9` "RC-VERIF completion gate" via `git log` on the test file, not a regression); `orch-backlog-stub.test.sh` 7/7 PASS; `test-orch-validate-ac.mjs` 29/29 PASS; `bun tsc --noEmit` clean; `mock-guard.sh` PASS on all touched production files.

AC-1/AC-3 (the negative control — the criterion distinguishing a real fix from cleanup): read `checkDecorativeSequencingFields()` at source (`orchStateSchema.ts:978-1073`) AND independently reproduced the negative control myself, distinct from the shipped test file's own fixtures — injected a fresh reverse-only `blocks` edge and a non-empty `co_edit` value onto a live row in a scratch copy of `orch-state.json`, ran `orch-validate.mjs` directly: both hard-rejected with a real exit code 2 and a specific fix hint. Confirmed `orch-apply.sh:138` aborts the write on ANY non-zero validator exit ("live file untouched") — the guard binds at the real write path, not only in isolation.

AC-2 (7-row disposition): live's 2 remaining non-empty `blocks` rows (`VMT-7a..e→VMT-7-REGISTER`, `CONTAM-10-WRITER-H→CONTAM-10-EXEC-2`) independently confirmed properly backed via their targets' own `depends` arrays; `FIX-MCP-SUITE-HEALTH-BASELINE`'s malformed prose confirmed preserved verbatim under `migrated_blocks_prose` (`blocks` itself now `null`, correctly skipped by the guard); `bash scripts/orch-state-validate.sh` exits 0 clean on the live file today (0 Stage-1e issues). The 2 `migrated_co_edit_partner` rows no longer appear on the live board at all — traced via `git log`: both independently shipped in `adb426877`, went `DONE_VERIFIED`, later pruned by normal done-lane archival lifecycle — unrelated drift, not a defect of this fix.

AC-4: `scripts/orch-backlog-stub.sh:64` `STUB_FIELDS` confirmed carrying `depends_on,depends,blocked_by` (`dev_close_note`'s `:57` line citation is stale by ~7 lines from an added comment block — non-blocking doc-accuracy nit only). AC-5: `git show --stat 5ee697852 -- docs/data/orch/archive/backlog-detail.json` empty — confirmed untouched, no hand-edit.

DJ-GATE-1: developer's own journal confirmed present, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` STEP `developer-S35`, `task-id:` line matches verbatim.

VERDICT: `DONE_VERIFIED`, zero ISSUE — all 5 ACs independently reproduced from source/live data. `[QA] Review Record (direct-commit verify)` appended to the row's `dev_close_note` (no `status_note` field present on this row). `verification.raw_probe` attached on the retry after the validator flagged its absence on the first write attempt (same recurring gap as qa-S113/S114/S115). Moved `task_board.qa[]`→`done_verified[]` via `orch-apply.sh` (conservation clean, task_total 729=729). `.head.active_task_id` WAS this task (`in_progress`/`next_agent:qa`) — synced to idle in the SAME write per CANONICAL:SSOT-STATUSFLIP-LANEMOVE rule (b). This sub-session's tool grant is Read/Edit/Write/Bash only, no `mcp__gateway__call_tool`/`mcp__semble__search` binding present — could not call `task_release` myself; the coordinating dev-team/router session (`632721c2-41e4-4aff-8d06-a47cf80dc0d7`) holds `task:FIX-ORCHSTATE-BLOCKS-FIELD-WRITE-ONLY-DECORATIVE` and is the authoritative release path.
