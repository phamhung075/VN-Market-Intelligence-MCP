# QA — Notebook

## cycle-508 · 2026-08-06 · FIX-BCTC-FULL-SERVING-EMPTY-NEWEST-PERIOD-HEAD-OF-LINE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain, `branch:null`, P0 user-facing outage day 4)

Commit `70257dfc0` confirmed on main ancestry, `git show --stat` matches dev_note's claimed 3 files exactly (`bctcFullTools.ts`, `240-bctc-full.test.ts`, `financial-reports.md`), zero later commits re-touch either. Read the full diff myself, not the commit message: no-filter path now runs a `CANDIDATE_WINDOW=6` sort_key-DESC scan reusing the EXISTING exported `checkPublishability` gate (no duplicated PUB-1..8 logic) to pick the newest servable candidate, falls through to the unchanged rejection when none qualify; explicit `{year,quarter}` branch is a separate untouched else-arm (byte-identical query); honest `fallbackNote` + machine-readable `fallback_from_newest_sort_key` added — matches architect brief §4 verbatim.

Re-ran everything myself rather than trusting the row's own dev_note (`feedback_router_verify_raw_not_badges`): `bun test 240-bctc-full.test.ts` → 25 pass/0 fail/81 expect(), exact match to dev's claim. `bun tsc --noEmit` (apps/mcp-server) → 0 errors. `mock-guard.sh --files` PASS. DDD grep flagged pre-existing `infrastructure` imports (`getDb`/`logger`) — legitimate, this file is `interface/mcp/tools/` layer per dev-standards.md's DDD table, not `domain/` — no violation. Security grep clean.

This row's OWN `verification_gate` is a LIVE tool call, not a test suite — did not stop at green tests. Dev's own `dev_note` flagged `REBUILD_REQUIRED` and explicitly asked not to rebuild autonomously; verified rather than trusted this blocker: `docker inspect` shows the `mcp-server` image was already rebuilt 2026-08-06T08:41Z (after the fix's 2026-08-05T09:50Z commit) and is the currently-running container — `docker exec` grep on the live `/app` source confirms `CANDIDATE_WINDOW`/`fallback_from_newest_sort_key` are present, i.e. the REBUILD_REQUIRED precondition was already silently satisfied by a prior rebuild, no fresh ops dispatch needed. Gateway-blind qa sub-session (no `mcp__gateway__call_tool` binding) — worked around via direct JSON-RPC POST to the live production MCP endpoint (`https://zenmidi.com/vn-market/mcp`). Called `get_bctc_full(FPT|HPG|VCB)` live: all 3 flagship tickers now return real structured 2026-Q1 data (balance sheet/income statement/ratios/prose) plus the honest fallback line "Kỳ mới nhất (2026-Q2) chưa được trích xuất — hiển thị kỳ gần nhất có dữ liệu (2026-Q1)" and `fallback_from_newest_sort_key="2026-Q2"` in `structured_data` — zero "Chưa có dữ liệu BCTC" hits (AC-1/AC-2 both pass, live, not just unit-tested). AC-3 (explicit-period path untouched) also live-verified as a negative control: `get_bctc_full(FPT, year=2026, quarter=Q2)` still correctly returns the bare rejection string for that exact unfilled period — zero silent substitution.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (qa 4→3, done_verified 11→12, conservation check task_total 814→814, pure lane-move). `.head.active_task_id` was `null`/idle (unrelated) — left `.head` untouched, `next_agent: pm` unblocks downstream. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S24.

## cycle-508 · 2026-08-06 · FIX-PO-BATCH-MINT-NO-WRITE-ACTUATOR — CHANGES_REQUESTED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain, `branch:null`)

Commit `3ce726a6e` confirmed on main ancestry, `git show --stat` matches status_note's claim (7 files: 4 po sub-flows + main.md + agent-father notebook + sprint journal). Read the full diff myself, not the row's status_note prose. AC-1 CONFIRMED: `sprint-kickoff.md`/`channel-audit.md`/`market-group.md`/`telegram-reports.md` genuinely gained inline `jq ... | bash scripts/orch-apply.sh` pipes at every `.task_board`/`.sprint_goal` mutation point, dangling "§2.3" pointer removed from 2 of them. AC-2 CONFIRMED: `main.md` commit-mutex `own_paths` widened to include `docs/data/orch/orch-state.json`. AC-3's RETURN self-verification (`git show --stat` before claiming "committed") CONFIRMED added.

BUT AC-3's other half — "wire a regression verifier under `scripts/audits/` that greps `po/flow/*.md` for un-piped mutations and fails on any hit" — is NOT delivered: `scripts/audits/po-mint-orchapply-actuator-verify.sh` does not exist on disk (confirmed via `find`). `main.md:173` states it plainly: "spec, unimplemented ... Filed as follow-up via this task's RETURN handoff, not authored here." The row's own author explicitly labels this "the actual closure test" against false-green on an 8-occurrence recurring class (`feedback_po_notebook_mint_never_reaches_orchstate_board`) — not optional polish. Checked the disclosed limitation is genuinely structural, not an excuse (`feedback_agent_reported_limitation_may_be_structural`): `docs/agents/agent-father/init.md:62-65` confirms `commit_zone.allowed=[docs/agents/, docs/agent-memory/, .claude/skills/, .claude/agents/]` — `scripts/` is in neither `allowed` nor `excluded`, i.e. structurally unreachable from that zone. Doc-only diff (zero `.ts/.js/.py` touched) — bun test/tsc/mock-guard structurally N/A (Smart-Skip).

VERDICT: CHANGES_REQUESTED. Moved `task_board.qa[]`→`task_board.review[]` via `jq`+`scripts/orch-apply.sh` (review 263→264, conservation task_total 814→814, pure lane-move), `redispatch_count` 0→1, appended findings to `status_note`. Reassigned `owner`+`next_agent` `agent-father`→`developer` (zone-correct for `scripts/` authorship) instead of literal owner — redispatching to agent-father would only reproduce a 9th unproductive "structural" occurrence (`feedback_recurring_detection_vs_recurring_failed_fix`); agent-father's own notebook exit note already recommended NEXT: developer/architect. `.head.active_task_id` was `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` (unrelated) — left `.head` untouched. Doc-side actuator fix (defects A+B, the row's own title) is genuinely landed — flagged not to be redone.

SIDE-FINDING (fixed inline, not this row's scope): this notebook's own writes were being silently reverted — `notebook-auto-prune.sh`'s same-day tie-break defaults to `newest_first`(prepend) when a file has zero distinguishing signal, but `qa.md`'s "cycle-N · YYYY-MM-DD" headings are date-only (no time) AND this file is genuinely `oldest_first`(append) — confirmed via `git log -1 -p` per `docs/data/notebook-section-order.json`'s own prescribed method. Every retained section ties on the same day-key, so the buggy default was dropping the physically-LAST (newest, just-written) section instead of the true oldest — 2 prior QA cycles this session (cycle-506/507 entries above) already hit this and worked around it via raw-bash-append+immediate-commit without fixing the root cause. Added `"qa.md": "oldest_first"` to `docs/data/notebook-section-order.json.overrides` (the table's own documented self-service repair path, no code change) so future Edit-tool writes to this file resolve correctly instead of requiring a manual bypass every cycle. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S21.

## cycle-509 · 2026-08-06 · CCATO-MCP-T2-CLAIM-MAP-LOADER — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

T2 of 8 (SPRINT-CCATO-TRUTHGATE-MCP-NATIVE). Row had no top-level `commit`/`files[]` — derived both from review_note prose (commits `f79019f9b` impl + `533a745d3` memory), both confirmed real and on main ancestry. `git show --stat` matches every claimed file exactly: new `claimToolMapLoader.ts`(104L)/`claimToolMapValidator.ts`(90L, both <=120L split for size-lint) + new test file + 3 doc files, zero existing production files modified — a pure addition.

Re-ran everything myself rather than trusting the review_note prose (`feedback_router_verify_raw_not_badges`): `bun test CCATO-MCP-T2-CLAIM-MAP-LOADER.test.ts` → 14 pass/0 fail/25 expect(), exact match. `bun tsc --noEmit` (whole `apps/mcp-server`) → 0 errors. DDD grep clean (only a type-only `ClaimToolMap` import from domain — correct direction). Security grep clean (`non_ticker_tokens` field-name substring hits only, no real secrets/`process.env`). `mock-guard.sh --files` PASS. `size-lint-justification.sh --check` PASS 0 offenders (row claimed 2 pre-existing at commit time — since resolved by later unrelated size-lint fixes on main, not a regression). Read both new files' full diff directly: fail-loud `ClaimToolMapLoadError` on all 4 malformed-shape paths (missing/unreadable file, unparsable JSON, non-object body, missing/mistyped fields), additive top-level SSOT fields tolerated not rejected, zero coercion. `getProjectRoot()` used for the new path constant instead of the brief's literal unsafe `__dirname` hop-count pattern (flagged deviation) — independently verified correct: that fixed hop-count resolves above the real root both in local dev and the deployed container.

Grep-confirmed zero production consumers of the 2 new modules repo-wide (only the new test file imports them; one unrelated comment-only string match) — full-suite regression structurally impossible for this pure-addition commit, same class as sibling `CCATO-MCP-T1-DOMAIN-ENGINE` verify; skipped the full 15085-test run, targeted file + whole-project tsc sufficient. Doc updates (`domain-model.md`/`infrastructure.md`/`testing.md`) read directly — accurate, describe the landed loader/validator faithfully.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation check task_total 814→814, pure lane-move). `.head` was already idle/`next_agent:pm` (unrelated peer state) — left untouched. DJ: `sprint-SPRINT-CCATO-TRUTHGATE-MCP-NATIVE-qa.md` §qa-S2.
