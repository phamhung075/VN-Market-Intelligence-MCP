# QA — Notebook

## cycle-519 · 2026-08-06 · FIX-CRON-SSCCHECKERJOB-DEAD-87D — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row had no top-level `commit`/`files[]` — derived `791b0e4dc` from status_note prose, confirmed on main ancestry. `git show --stat` matches all 10 claimed files (3 new test files, `cronStatusCompute.ts`, `sscCheckerJob.ts`, `startScheduler.ts`, dev journal/notebook, `cron-jobs.md`, `news-analysis.md`). Guard moved from top-of-`runSscCheck()` to inside the `recordJobRun()` callback so the daily cron always writes honest `success`/`rows_written:0` telemetry when it correctly no-ops; `dataAuditJob:weekly` (a SECOND, independently-discovered dead job, same restart-timing mechanism, different job_name/window) got its own `startScheduler.ts` startup-catchup probe; class fix adds a registry-invariant test reading `STATIC_JOB_NAME_MAP`'s real keys via `_staticJobNameMapKeysForTests()` instead of a 4th hand-copied fixture.

Re-ran all 3 test files myself: 12 pass/0 fail/21 expect() — AC-1/AC-2/AC-3 directly assert the honest-telemetry fix AND the network-safety non-regression (checkSscReports() never invoked when guard fires). `bun tsc --noEmit` clean, `mock-guard.sh` PASS, DDD/security greps clean (pre-existing infra imports unchanged, not new). Read current-HEAD `sscCheckerJob.ts` (a later unrelated commit layered a WORK-telegram summary on top) — class-fix mechanism unweakened.

`do_not_absorb` requires an ACTUAL successful run in `cron_job_runs`, not just watchdog-manifest visibility — did not trust status_note prose (`feedback_router_verify_raw_not_badges`). RAW live query, production mcp-server container (recreated 2026-08-06, post-fix): `sscCheckerJob` fires daily 13:00 UTC success/rows_written=0 continuously since 2026-07-24; `dataAuditJob:weekly` fires Saturday 18:00 UTC success since 2026-07-25 and 2026-08-01 — both previously-dead jobs demonstrably alive in production. `known_unknown` (downstream BCTC dependents possibly stale ~3mo) also independently corroborated live: `bctcQueueEnricherJob`/`bctcExtractReconcileJob`/`bctcPdfPullJob` (the cited superseding pipeline) all firing every 10-20min right now — no stale-data crisis.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 806→806). `.head` untouched (unrelated active_task_id: `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S38.

## cycle-520 · 2026-08-06 · FIX-AUDITOR-TIER1-PROBE-ACKED-LAUNCHD-DEATH-SUPPRESSION — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Successor row to cycle-516's `FIX-LAUNCHD-PROBE-PRESENCE-ONLY-FALSE-GREEN` (same file family). Row had no top-level `commit`/`files[]` — derived `b9484fa7a` from review_note prose, confirmed on main ancestry. `git show --stat` matches all 4 claimed files: `auditor-tier1-probe.sh`, `.test.sh`, new `docs/data/auditor-launchd-ack.json`, `docs/WORK.md`. New ACK LEDGER lets `_check_launchd_agents` report a launchd death as "acknowledged-degraded" instead of a fresh FAILURE when its label is listed with an open `tracked_by` backlog row — mixed unacked+acked still fails, naming only the unacked one.

Read current-HEAD `_check_launchd_agents`/`_launchd_label_acked` directly, not just the original diff — the mechanism is intact underneath a LATER independent fix (`FIX-AUDITOR-MEMACK-HEADROOM-FLOOR-AND-DEAD-TRACKEDBY`, 2026-07-29, already shipped) that adds live `tracked_by` staleness resolution against orch-state.json — a superset of this row's own AC ("remove the fix-row → verdict flips back to FAILURE"), not a weakening. Re-ran `auditor-tier1-probe.test.sh` myself: 181/181 PASS incl. this row's own T36-T39 (acked-only→ALL_GREEN / mixed→FAILURE naming only the new one / uncovered-ledger→FAILURE / all-healthy-with-ledger→ALL_GREEN, no schema drift).

Live-exercised `_check_launchd_agents` against the REAL running system today (sourced script, called the function directly): rc=0, `com.vn-market.docker-events(exit-status:1)` reported acknowledged-degraded — its `tracked_by` (`FIX-LAUNCHD-DOCKER-EVENTS-EXIT1-CRASHLOOP`) independently confirmed `BACKLOG` (live, non-terminal) via `jq` on orch-state.json — the row's own AC reproduced live, not only in fixtures. `mock-guard.sh --files` PASS ("no production source files to scan" — scoped to `apps/*.ts/tsx/py/go`, correctly excludes `scripts/`); DDD N/A (bash); process.env/secret greps clean. No `.ts`/apps/ touched (zone `cross-service/`, pure bash+JSON) → `bun tsc --noEmit` structurally N/A (Smart-Skip), same precedent as sibling rows this batch.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 800→800). `.head` untouched (unrelated active_task_id). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-2.md` §qa-S37.

## cycle-521 · 2026-08-06 · FIX-DEVTEAM-BOUNDED1-PROSE-SEQUENCING-UNBACKED-GATE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row's own `commit: ec8f17ab6` confirmed on main ancestry; `git show --stat` matches all 4 claimed files exactly (`scripts/lib/devteam-eligibility.jq`, `bounded1-supervised-lane-report.sh`, new `devteam-bounded1-prose-sequencing-gate-verify.sh`, `docs/agents/dev-team/flow/main.md`). Read the `has_unbacked_sequencing_prose` def + its wiring into `is_bounded1_eligible` directly — board-OR-detail OR-precedence matches every sibling `effective_*` def's convention; deliberately does not regex-parse `po_sequencing_*` prose (per spec).

Re-ran the task's own regression verifier myself, not trusted from review_note prose: `devteam-bounded1-prose-sequencing-gate-verify.sh` → 5/5 PASS (AC-1 unbacked-not-eligible, AC-1b backed-eligible once `depends_on` populated, AC-1c detail-side symmetry, AC-2 live UC-CDC-P5 shape stays held by `deps_satisfied` not double-gated, control unaffected). Also ran the 2 broader system-wide audits for due diligence: `devteam-dispatch-gate-satisfiability.sh` has 1 pre-existing FAIL ("BOUNDED-1 no-ops at in_progress>=1") and `bounded1-supervised-lane-report.sh` FAILs on 4 unrelated backlog rows with no dispatch lane — both structurally unrelated to this diff (different gate entirely: WIP<1 cap lives in the CLAIM script, not `is_bounded1_eligible`; this task's conjunct is ADD-only, it can only ever narrow eligibility, never cause a spurious promote — cannot be the cause of a "should-no-op-but-fired" defect). Sibling `devteam-bounded1-detail-disposition-gate-verify.sh`, flagged broken in the review_note as a pre-existing harness bug, now passes 10/10+control clean — fixed since by an unrelated later commit, no action needed either way.

No `.ts`/apps/ touched (pure jq+bash+md) — `bun test`/`tsc --noEmit` structurally N/A (Smart-Skip); `mock-guard.sh --files` → PASS "no production source files to scan"; DDD/security greps clean. DJ-GATE-1 confirmed: developer's own journal entry present (`sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` line 30).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 800→800). `.head` untouched (unrelated active_task_id: `FIX-AUDITOR-DURABILITY-STEP0B-DETECTION`). Decision journal rolled qa-2→qa-3 this cycle (byte-cap breach, 42481B>36000B — see qa-2's own CAP-REACHED sentinel; `send_telegram(bug)` skipped, no gateway/MCP tool grant this sub-session). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S38.

## cycle-522 · 2026-08-06 · BCTC-REPORT-ID-LOOKUP-TOOL — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`)

Row's own `commit: 7a9eea6bd` confirmed on main ancestry (author-date 2026-07-23); `git show --stat` matches all 3 code files exactly (new `FIX-BCTC-REPORT-ID-LOOKUP-TOOL.test.ts`, new `getBctcReportIdTool.ts`, `registry.ts` +2L purely additive) plus 7 doc/data files. Re-ran the row's own test myself, not trusted from status_note prose: 5/5 PASS, 22 expect() calls. `bun tsc --noEmit` clean (0 errors, whole-repo). `registry.ts` diff is 1 import + 1 array entry — zero modification to any existing registration, no plausible regression surface. DDD clean: new file lives in `interface/mcp/tools/` (interface→infrastructure import expected, not a domain violation; `src/domain/` untouched). Security clean: no `process.env`/secrets, parameterized SQL (`$code`/`$year`/`$quarter`). `mock-guard.sh --files` PASS. `tool-registry.json`/`project-stats.json` internally consistent.

AC "verify ESC-5 fires on a known-escalation case" independently corroborated LIVE in production, not just unit-tested: `docs/agent-memory/notebooks/bctc-analyst.md` 2026-08-06 entries show ESC-5 actually firing TRUE for FPT via this tool (report_id `e8ea3df5` resolved, `get_bctc_refined` confirms 7/15 units confidence 0.45<0.50) — fired c141, re-fired 2026-08-06 post-TTL-expiry — and correctly resolving `report_id:null`+`existing_refine_status` diagnostic for HPG/VCB/DXG. The row's own "not yet deployed" rebuild note (2026-07-23) is stale, superseded by this live 2026-08-06 evidence and `bctc-analyst/flow/main.md`'s own live-verified 2026-07-24 (c119) annotation.

Full `bun test` run twice as supplementary due-diligence beyond this entry point's documented scope (touched-test/targeted-zone, not full suite — verified per that scope already above): run1 15114 pass/40 skip/71 fail/1 error/15225 total (711s, contended); run2 (partial, up to 4 concurrent `bun test` processes observed on host — a peer session's own full-suite run) showed only timeout-pattern failures on unrelated tools (`get_insider_transactions` Task 1146, `logVpsPush` 1858c). `grep -i getBctcReportId\|get_bctc_report_id\|registry.ts` across both run logs = zero matches in any failure/output line.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (pure lane-move, conservation OK task_total 800→800). `.head` untouched (unrelated active_task_id: `FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-3.md` §qa-S39.
