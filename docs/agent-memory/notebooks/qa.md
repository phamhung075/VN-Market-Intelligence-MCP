# QA — Notebook


## cycle-557 · 2026-08-07 · TASK_603 (final gate) + PARENT FIX-CRON-REGISTRATION-PREFIX-NOT-EXCLUDED-ORPHANEMIT-AND-D4-R1B — APPROVED, both DONE_VERIFIED (direct-commit verify, commit `814182608` already on main)

Router explicitly instructed independent reproduction, not trust of self-report. Did all 4 independently:

AC-3a: reverted `AND task_id NOT LIKE 'cron-registration:%'` myself from `coordinationStore.ts` → scoped run **45 pass/1 fail RED**, exact negative-control test failing → restored → `git diff --quiet` confirmed byte-identical to HEAD → re-ran → **46 pass/0 fail GREEN**. Both halves of the negative control (no signal + row still deleted) asserted in one test at source (lines 1141-1165); positive control at 1167-1191.

AC-4: ran the FULL suite myself (`bun test`, 473.80s, not trusting either of the developer's 2 runs) → **15157 pass/40 skip/44 fail/48043 expect()**, 15241 tests/1265 files. Pass/skip/fail counts exact match to developer's own Run 1; expect()-count off by 2 (48043 vs claimed 48041) — same order-dependent flaky-floor class the developer used to explain their own Run1↔Run2 delta, not a regression (flagged in report/handoff as minor discrepancy, non-blocking). Grepped all 44 failing test names + `awk`-isolated the `task-lock-coordination-store.test.ts` block: zero overlap with coordinationStore/task_locks/cron-registration/tasksMdJanitorJob/isKnownLegitPattern/gcExpiredLocks. Net-new-failures = 0.

AC-6: `docker inspect vn-market-intelligence-mcp-mcp-server-1 --format '{{.Image}}'` → `sha256:115700a86e65...` — matches claim + router's own prior independent check. `Created` 2026-08-06T23:21:39Z, `healthy`. All 11 peer containers in `docker compose ps` show unchanged multi-day/week `Created` timestamps — single-service rebuild confirmed, no fleet `down`/`up`. `docker-compose.yml` git log shows no touch by this task or neighbors.

AC-5: grep/diff-checked both agent-father files (`handlers.md`, `audit-dimensions.md`) across all 4 named commits (`814182608`/`03af0f983`/`8e756c36d`/`7aa8247b4`) — zero touches in any. `git log -1` confirms last actual edit predates this task by weeks (2026-07-18/2026-07-25).

tsc 0 errors, mock-guard PASS (test-only diff, Smart-Skip DDD/security).

VERDICT: TASK_603 APPROVED, DONE_VERIFIED. Parent row also flipped `READY`→`DONE_VERIFIED` in the SAME `orch-apply.sh` write per handoff's explicit "Handoff to QA" instruction (this is QA's responsibility, not developer's) — conservation OK (task_total 757→757, signal_total 204→204), 2 rows stamped. `next_agent: pm` both. Sequencing constraint (brief §4.4) now satisfied — router's held agent-father Lane-1 dispatch unblocked. QA Review Record appended to `docs/agents/dev-team/handoff/TASK_603.md`; report `reports/TASK_REPORT_603.md`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-8.md` §qa-S4.

## cycle-558 · 2026-08-07 · FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `4a6d53151`)

Row's own `status_note` flagged that architect self-verified first (unit test 5/5 + live re-query) then self-corrected to route through independent QA before DONE_VERIFIED — router dispatch explicitly asked me to re-derive AC-2/AC-3 myself, not trust the architect's own re-query. Did that: commit `4a6d531518b74182eb8b2f3217abe66977bc083f` real, on main ancestry, touches exactly the 4 files named in `architect_review_note` (no `files[]` on the row itself). Read the diff directly — `resolved_secondary_dispatch_target()` one-line change: `if ($na|length)>0 then $na else "po" end` -> `if ($na|length)>0 and ($na != "dev-team") then $na else "po" end`, correct and minimal. AC-1 (exactly one caller repo-wide) grep-confirmed myself: only `scripts/devteam-review-claim-secondary-drain.jq:123` invokes it in production code.

Re-ran regression myself rather than trust "5/5"/"7/7" from prose: `scripts/devteam-eligibility-resolved-secondary-dispatch-target.test.sh` 5/5 PASS, `scripts/orch-backlog-stub.test.sh` 7/7 PASS (sibling unaffected). AC-2/AC-3: the architect's 4 named rows are 2 days stale — 3 of 4 have churned off `review[]` entirely (normal), the 4th now shows `next_agent=="qa"` with a leftover `secondary_dispatch_target="po"` stamp (consistent, not contradictory). Rather than chase vanished rows, re-derived the general invariant live via a scratch jq script `include`-ing the real `devteam-eligibility.jq`: 0/193 current `review[]` REVIEW-status rows resolve `effective_next_agent`/`resolved_secondary_dispatch_target` to `"dev-team"`. Cross-checked with a full dry run of the real production script against a scratch copy of `orch-state.json` (never wrote live) — zero of 11 stamped rows resolve to `"dev-team"`. `bun tsc --noEmit` 0 errors (zone cross-service/, zero apps/ TS touched); `mock-guard.sh` N/A. DJ-GATE-1: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-architect-2.md` STEP architect-S25 present.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 753→753, signal_total 204→204). `next_agent: pm`. Verification text appended to row's own `status_note` (no handoff/`review_note` field). DJ: own new file `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-10.md` §qa-S1 (qa-8.md was mid-write/clobbered by a concurrent peer QA sub-agent per qa-9.md's own note — started fresh to avoid re-collision rather than appending to any in-flight sibling file). Task lock `task:FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE` held by dispatcher coordination session `f298ccf7-8cf4-452d-9a5a-57dcb47e65ac` — no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1); dispatcher releases it per its own instruction, not attempted here.

## cycle-558 · 2026-08-07 · FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, `commit_sha`: `1c9b55d4d`)

Dispatch prompt + the row's own `desc` both claimed sibling `FIX-SWEEPGUARD-SAMEFILE-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR` (`66e850138`) was still live in `review[]/next_agent=qa`, gating AC-3. Live-queried instead of trusting either — sibling is absent from every board lane, found only in `docs/data/orch/archive/2026-08.json` with `status: DONE_VERIFIED`; cross-confirmed via `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-6.md` §qa-S6 (2026-08-06T21:47:24Z) — QA had already independently verified it, live-reran 13/13, and reasoned about this row's own T13 as a genuine follow-up, not a regression. Sequencing gate satisfied by prior work, not a live blocker (stale fence-prose, resolved not waved through). Also: dispatch said the row lives in `review[]`; it's actually in `qa[]` (matches this flow's own input spec) — no duplicate found, metadata-only inaccuracy.

Did not trust "13/13 PASS" from the commit message — re-ran `bash scripts/git-hooks/pre-commit.test.sh` live myself: 13/13 PASS. Read T11/T12/T13 source (non-vacuous: T11 asserts stderr non-empty + exact `SAME-FILE DIVERGENCE`/`f11.txt` content confirming the peer-staged case still fires; T12/T13 assert stderr strictly empty). Read `pre-commit:650-700` directly: new gate `[ "$real_blob" = "$head_blob" ] && continue` live at line 673 (AC-1). Diffed the architecture-brief commit to confirm the real §2.7 addendum text (AC-4). Commit `1c9b55d4d` real, on main ancestry, touches all 3 claimed `files[]` exactly. No production TS/Go touched — `bun test`/`tsc` N/A, `mock-guard.sh` PASS.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 753→753). `next_agent`: none set on row (P1 FIX, no explicit pm handoff field) — treating as `pm` per default lane-move convention. Verification text appended to row's own new `status_note` field (no handoff file). DJ: own new file `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-9.md` §qa-S1 — created after a concurrent peer QA sub-agent's full-overwrite write to `qa-8.md` clobbered my first append there mid-cycle (same collision class as `feedback_qa_notebook_fulloverwrite_drops_concurrent_peer_entries_20260806.md`, now confirmed live on the decision-journal file too, not only notebook.md). Lock `task:FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE` held by dispatcher coordination session `f298ccf7-8cf4-452d-9a5a-57dcb47e65ac` — dispatcher releases it per its own instruction, not attempted here (no gateway/MCP tool grant this sub-session).

## cycle-559 · 2026-08-08 · FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commits `d6c4e6006`+`c4b7b2060`)

dev-team Review-Lane QA-Drain batch dispatch (7-row batch, this row highest-ranked, `.head.active_task_id` pointed here at claim time). Verified both commits on main ancestry; `git show --stat` matches `dev_note`'s file claims (`coordinationStore.ts`+`coordinationStore.test.ts`). Read the full diff myself, not the dev_note prose: `heartbeatTask`/`releaseTask` gain an additive 3rd `options` param — FR-1 two-statement UPDATE (ttl_seconds clamp 60-691200, then payload_patch shallow-merge via `parseJsonObject` EC-6-safe helper), FR-2 Rung-B null-session ladder gated strictly on the ROW's own `owner_client_session IS NULL AND task_kind='orphan-signal'` columns (never a caller-supplied flag) — NFR-1 anti-theft structurally intact (a live-session row can never match Rung B).

Re-ran `bun test src/infrastructure/__tests__/coordinationStore.test.ts` myself: 13/13 pass/27 expect — exact match to dev_note claim. `bun tsc --noEmit`: 0 errors. Ran a broader targeted regression than claimed: 20 lock/coordination test files (grep `coordinationStore|task_heartbeat|heartbeatTask|releaseTask|task_release|task_claim`, superset of dev's claimed 18) → 332 pass/0 fail/991 expect, zero regressions. `mock-guard.sh --files coordinationStore.ts` PASS. DDD (infra/application import grep) and security (`process.env`/secrets grep) both clean. NFR-2 backward-compat independently confirmed at the call-site level (not just the diff comment): grepped all live callers of `heartbeatTask(`/`releaseTask(` outside store/test files — only `coordinationTools.ts:174/208` + `bctcRefineJob.ts:532`, all still 2-arg calls, untouched by this commit. Scope boundary (Zod exposure = separate Task 2 `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS`, still READY) confirmed genuine against the PM decomposition handoff, not a gap. Repo-wide `bun test` not re-run in full — pinned CANONICAL reading (`dev-standards.md:1435`, targeted/merge-gate suite governs over the standing tracked `FIX-MCP-SUITE-HEALTH-BASELINE` full-suite red).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 778→778). `next_agent: pm`. `.head.active_task_id` was this row (branch:null) — synced `.head` to idle (`status:idle, active_task_id:null, next_agent:router`) in the same write per `CANONICAL:SSOT-STATUSFLIP-LANEMOVE` rule (b). Verification text appended to row's own new `status_note` field (no handoff file, per `verify-committed` sub-flow). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-11.md` §qa-S2 (concurrent sibling QA sub-agent also writing to the same file this window — appended after their entry, no overwrite).
