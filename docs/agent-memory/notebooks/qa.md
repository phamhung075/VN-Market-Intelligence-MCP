# QA — Notebook

## cycle-533 · 2026-08-06 · FIX-AUDITOR-CALLER-PROSE-OVERRIDES-DOCUMENTED-DETECTOR-THRESHOLD — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain head-decoupled spawn, `branch:null`, matches `feedback_a30_prose_overrides_embedded_escalate_verdict`/`feedback_auditor_prose_guard_noncompliance_trend_narration_script_mislabel` lesson class)

Did NOT trust `review_note` prose alone. Commits `1565b0d1d` (code+docs) + `0cea9ccdf` (notebook+journal) confirmed on main ancestry; `git show --stat` on `1565b0d1d` matches claimed 4-file scope exactly (`system-auditor/flow/main.md`, `tier1-probe.md`, `emit-audit-signal.sh`, `emit-audit-signal.test.sh`). Peer commit `c919f69a1` (claimed to carry the `CANONICAL:AUD-CP-1` `dev-standards.md` entry, not re-committed by `1565b0d1d`) verified: entry present verbatim at HEAD `dev-standards.md`:174+, and `1565b0d1d --stat` confirmed does NOT touch `dev-standards.md` — no double-commit.

Two-gap fix, graded separately since they carry different verification weight. Gap 1 (flow refusal) is necessarily flow-doc prose — system-auditor is LLM-driven off its own spec, and AC(1) itself specifies "the system-auditor flow states an explicit PRECEDENCE rule". `main.md` §CALLER-INSTRUCTION PRECEDENCE (AUD-CP-1) block confirmed landed before Tier Dispatch, binding by position not per-check duplication; mandatory `CONTRACT-CONTRADICTION` RETURN line added. `tier1-probe.md`'s 4-line breadcrumb verified strictly OUTSIDE the protected verdict-mapping span — diffed the `**Verdict:**` block text alone (pre-fix commit vs post-fix commit): byte-identical, empty diff, confirms `po_scope_amend_20260729T0721`/AC(5) respected.

Gap 2 (artifact provenance) is the code-enforced half this row's memory-lesson class actually demands, so re-derived RED-then-GREEN myself rather than trusting the claim: swapped the pre-fix `emit-audit-signal.sh` into the live tree (restored after — `git diff` empty confirms exact restore), ran the CURRENT test file against it — `T13`/`T14`/`T15` provenance assertions genuinely FAIL (row emits fine, `exit=0`, but carries no/wrong `provenance`); re-ran against HEAD's script — all 3 PASS. Confirmed `provenance:"detector"` is a hardcoded literal inside `_build_row_json()`, the sole row-construction function (`grep`: defined once, called once) — no `--provenance` CLI flag exists anywhere in the file, so no caller-controllable path to any other value exists. That is what makes the documented threshold's artifact win over caller prose as code, not narration (matches AC(4) preferred-shape: provenance value set is `{detector}` only).

Full `emit-audit-signal.test.sh` re-run live at HEAD: 98/0 fail (grown from the commit-time claimed 53/53 via unrelated later test additions — not a regression, `T13`-`T15` confirmed present+passing). `pre-commit-auditor-heartbeat.test.sh` re-run: 6/6 PASS, unaffected. `shellcheck --severity=warning`: clean. Zone `cross-service/`, pure bash+md, no `apps/` TS/Go touched — `bun test`/`tsc`/DDD/security N/A (Smart-Skip). No blocking issues.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 775→775). Corrected stale `next_agent:"qa"` carried from pre-review dispatch → `next_agent:"pm"` in a same-cycle follow-up write, per the `qa/flow/main.md` verify-committed-approved RETURN contract (mirrors the fix already applied to the `FIX-DEVTEAM-STEP0B-RESUME-SUPERVISED-HOLD-GATE` sibling this same cycle). `.head` untouched (was pointing at unrelated `FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT`). Structural note: no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — could not directly call `task_release`; flagged to the calling dispatcher to release `task:FIX-AUDITOR-CALLER-PROSE-OVERRIDES-DOCUMENTED-DETECTOR-THRESHOLD` (owner_client_session=f298ccf7-8cf4-452d-9a5a-57dcb47e65ac) itself.

## cycle-534 · 2026-08-06 · FIX-SQLITE-JOURNALMODE-WAL-REARM-DEFEATS-DELETE-MITIGATION — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, no `.commit`/`.files[]` — fallback derivation)

Derived commits via `git log -- <file>` on the files named in `review_note`: `a7e437e16` (AC-1, `bctcEvalBackfillRunner.ts` swaps its own `new Database(dbPath)`+`PRAGMA journal_mode=WAL` for `schema.ts`'s shared `getDb()/closeDb()` singleton — sole journal_mode owner) and `a365b6455` (AC-3/AC-4, `scripts/audits/verify-market-db-journal-mode.sh` regression guard). Both on main ancestry, commit dates (07-30 13:58 CEST) match the row's `reviewed_at`. `schema.ts:133 PRAGMA journal_mode=DELETE` confirmed live as the sole owner; runner no longer opens its own connection.

AC-2 gap (review_note self-disclosed 4 `scripts/migrations/*.ts` WAL-setters out of dev-mcp-server's `apps/mcp-server/` zone) checked for abandonment, found actioned: those 4 files now carry an explanatory `NOTE: journal_mode is NOT set here (FIX-SCRIPTS-MIGRATIONS-MARKETDB-WAL-REARM-SAME-DEFECT...)`, and that follow-up task was minted + already implemented (`3bc8c9d55`) — it is currently its own separate `qa[]` row, not folded into this verdict.

Re-ran the guard live rather than trusting prose: `verify-market-db-journal-mode.sh` against the running `mcp-server` container → `PASS journal_mode=delete wal_present=false shm_present=false`; `--self-test` proved both branches live (WAL fixture exit=2 FAIL, DELETE fixture exit=0 PASS). `bun tsc --noEmit` 0 errors. Targeted zone suite (`bctc-eval-routes`/`-detectors`/`-integration` + `002-db-schema`, 4 files) 68 pass / 0 fail. `mock-guard.sh --files bctcEvalBackfillRunner.ts` → PASS. No DDD violation — the file's `interface/mcp/routes/` layer legitimately imports `application`+`infrastructure` (pre-existing wiring pattern for a route file, unrelated to this fix, not a regression).

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 771→771), corrected `next_agent:"qa"`→`"pm"` same-cycle. `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-4.md` §qa-S59. Lock `task:FIX-SQLITE-JOURNALMODE-WAL-REARM-DEFEATS-DELETE-MITIGATION` released via `task_release` (dispatcher-side call, this sub-session had gateway access).

## cycle-534 · 2026-08-06 · FIX-SCRIPTS-MIGRATIONS-MARKETDB-WAL-REARM-SAME-DEFECT — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, dev-team Review-Lane QA-Drain spawn, `branch:null`, `mode=verify-committed`)

Did NOT trust `reviewed_note` prose alone (dev-team's own RAW-verify note claimed this fully verified). Commit `3bc8c9d55` confirmed real and on main ancestry (`git merge-base --is-ancestor`). `git show --stat`/full diff matches the row's claimed 4-file scope exactly: `run-finalize-bctc-refine.ts:36`, `dedupe-mislabeled-bctc-period.ts:368`, `resync-watchlist-sysmap-2026-07-11.ts:266`, `carry-forward-bctc-orphaned-rows.ts:361` — each site's `db.exec("PRAGMA journal_mode = WAL")` line is replaced by a documentary comment only, `PRAGMA foreign_keys = ON` preserved untouched, no other PRAGMA site touched (AC-1 satisfied literally).

AC-2 (coordination.db/`:memory:` fixtures out of scope) verified independently rather than trusted: repo-wide `grep "journal_mode.*WAL"` shows the only other live sites are `apps/mcp-server/src/infrastructure/db/coordinationStore.ts` (coordination.db, explicitly out of scope) and the 4 `scripts/smoke-task-lock*.ts` scripts (all `new Database(":memory:")`) — confirms these 4 migration files really were the only market.db WAL-rearm sites left after the sibling fix, scope-completeness holds.

AC-3: re-ran `bash scripts/audits/verify-market-db-journal-mode.sh` myself against the live `vn-market-intelligence-mcp-mcp-server-1` container — `verdict=PASS journal_mode=delete wal_present=false shm_present=false`, exit 0. Re-ran the 3 existing migration test suites with dedicated coverage (`dedupe-mislabeled-bctc-period.test.ts`, `carry-forward-bctc-orphaned-rows.test.ts`, `resync-watchlist-sysmap-2026-07-11.test.ts`): 32 pass / 0 fail. `run-finalize-bctc-refine.ts` has no dedicated test file (pre-existing gap, unrelated to this fix) — its change is the identical mechanical line-removal+comment pattern, zero logic delta. `mock-guard.sh --files` PASS on all 4 touched files. `apps/mcp-server` `bun tsc --noEmit` 0 errors (root `tsconfig.json` `include` is `src/**/*`+`*.ts` only, does not cover `scripts/` — N/A for that zone, but zero type-risk from a pure line-deletion+comment edit). No blocking issues.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 775→775, qa 9→8, done_verified 4→5). `next_agent: pm`. `.head` untouched. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-4.md` §qa-S59. Structural note: no gateway/MCP tool grant this sub-session (Read/Edit/Write/Bash only, INV-GATEWAY-1) — could not directly call `task_release`; flagged to the calling dispatcher (owner: dev-team, session 24817246-8a3f-4511-95f7-1b4385797bee) to release `task:FIX-SCRIPTS-MIGRATIONS-MARKETDB-WAL-REARM-SAME-DEFECT` itself.

## cycle-534 · 2026-08-06 · FIX-DEVTEAM-CLAIM-SCRIPTS-UNCONDITIONAL-HEAD-OVERWRITE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, no `.commit`/`.files[]` — fallback path)

Derived commit `3519a09e4` from `review_note` prose (filenames + short-SHA), confirmed on main ancestry. `git show --stat`/diff matches claimed scope exactly on all 4 write sites across 3 files: `devteam-backlog-claim-bounded1.jq`, `devteam-backlog-claim-supervised-lane-sweep.jq` (PRIMARY+FALLBACK), `devteam-backlog-claim-ready-lane-consumer.jq` — each now computes `$head_free` (idle/done/active_task_id-null) before writing `.head`, else preserves it untouched; guard shape byte-identical to the cited `devteam-backlog-claim-design-router-sweep.jq` (DRS) precedent.

Re-ran `bash scripts/audits/devteam-dispatch-gate-satisfiability.sh` myself, not trusted from the commit's own "48/48" claim: 71/71 PASS, 0 FAIL, exit 0 (grew from 48 via unrelated later harness additions; all 8 AC-BOUNDED1/SLS/RLC-HEAD-GUARD assertions present+green, negative+positive halves both pass). Confirmed script never mutated the live `orch-state.json` (pre-existing dirty diff was unrelated ambient board activity). `mock-guard.sh --files` on the 3 jq files → PASS (no production TS/Go source, N/A). No `apps/` source touched (pure jq+bash, zone `cross-service/`) — `bun test`/`tsc` structurally N/A. Journal cross-check: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer.md` §developer-S41 present, task-id matches.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK task_total 775→775). `next_agent: pm`. `.head` untouched (was already idle/unrelated, guard fix itself never touched by this write). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-4.md` §qa-S59. Lock `task:FIX-DEVTEAM-CLAIM-SCRIPTS-UNCONDITIONAL-HEAD-OVERWRITE` pre-claimed on my behalf by the calling dispatcher (owner_agent=dev-team, session 24817246-8a3f-4511-95f7-1b4385797bee) — released via `task_release` at end of cycle.
