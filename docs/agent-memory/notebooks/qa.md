# QA — Notebook

## cycle-404 · 2026-07-08 · FACTORY-INFRA-split-vnstockBridge — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (Steps 1-4, deployed SHA 91c9d19b0, commit 0a44fe472). dev-mcp-server split `vnstockBridge.ts` (1206L) into `vnstockBridge.ts` (330L, keeps the 11 public `fetchVnstock*` wrappers) + `fetchers/vnstock/{runtime.ts,index.ts,scripts/*.ts}` (11 Python template builders + shared `wrapVnstockScript()` preamble). Used a different ticker than ops's own spot-check (FPT, not VNM) and covered more domains (7 of 11: prices/tradingStats/shareholders/officers/financials/balanceSheet/cashFlow vs ops's 2). Re-confirmed container identity myself (`docker inspect` git_sha label = deployed SHA, fresh StartedAt; both docker-port-discovered ports 3000/4004 → `/health` ok/toolCount=183; `docker exec find` confirms all 11 split files present).

Live-invoked the split functions directly via `docker exec`+`bun run` for FPT: `fetchVnstockPrices`/`fetchVnstockShareholders` byte-identical to the live named-volume `market.db` (`daily_ohlcv`/`vnstock_shareholders`); `fetchVnstockTradingStats` matches the persisted DB row on every field; `fetchVnstockOfficers` real+plausible. `fetchVnstockFinancials`/`BalanceSheet`/`CashFlow` returned a uniform zero-value pattern (yearReport=0/revenue=0/pe-pb-roe-roa=null) — investigated rather than waved off: git-diffed `financials.ts`'s generated Python body against the pre-split `FINANCE_SCRIPT` (`git show 9f4a8eef6`) — byte-identical logic/column-keys, split changed nothing; queried `vnstock_balance_sheet` DB history for FPT — a 2026-04-15 row (months pre-split) has real non-zero data while the most recent row (2026-07-08T19:08:36Z, BEFORE this container even started) already shows the same zero pattern — confirms a pre-existing upstream vnstock data-availability characteristic, not a split regression.

`tsc --noEmit` (host, fresh): 0 errors. Targeted vnstock suite (18 files) re-run fresh: 210/210. Full `bun test` re-run FRESH (3rd independent run of this exact suite for this task): 14383 pass/40 skip/60 fail/4 errors/45067 expect(), 1181 files, 653.38s, then the same documented Bun 1.3.13 crash-at-teardown (non-authoritative, strictly post-summary) already seen twice in this task's own `review_note`; grepped all 60 fail-lines + 4 error traces — zero vnstock mentions (all map to known-unrelated classes: 1898b RSS-push flake, `src/_deprecated/1302-technical-indicators.test.ts`, plus a cluster of 5000ms-timeout tests consistent with host contention from this verification's own concurrent docker execs). SHA-gate re-run showed drift vs current HEAD — re-derived as benign: `git diff --stat 91c9d19b0..HEAD -- apps/` empty (2 intervening commits are docs-only orch-state writes).

VERDICT: PASS, no regression in any of 7 sampled domains + tsc + targeted + full suite; the one genuine anomaly (zero-value financials) traced to ground truth as pre-existing. Per runbook delegation rule (ops Steps 1-4 / qa Step 5 / po Step 6) and this file's own repeated precedent (qa-S24/S25) — did NOT flip to `DONE_VERIFIED`. Appended findings to the existing `review_note`, moved `next_agent` qa→po via `jq`+`scripts/orch-apply.sh`, `.head.next_agent` synced to `po` in the same write. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S26. Report: reports/TASK_REPORT_FACTORY-INFRA-split-vnstockBridge.md.

## cycle-403 · 2026-07-08 · FACTORY-APP-split-fetchParseAndStoreBctc — Step 5 RAW-verify PASS, HELD REVIEW, routed to po for Step 6

Formal Step 5 (docs/protocols/docker-deployment-runbook.md § Microservice Code-Change Close Gate) after ops rebuilt+SHA-gated mcp-server (Steps 1-4, commit b549e88d0). dev-mcp-server split `fetchParseAndStoreBctc.ts` (895L→117L orchestrator) into `bctc/resolvePdfText.ts`, `bctc/newsChainFallback.ts`, `bctc/insertBctcAnalysis.ts`, `bctc/types.ts`, hoisting 4 news-fallback confidence literals to named constants (`NEWS_FALLBACK_BASELINE=0.55`/`TEMPORAL_DISCOUNT=0.8`/`FALLBACK_CONF_MIN=0.45`/`FALLBACK_CONF_MAX=0.65`). Task's own DoD explicitly required confirming no resolved-value drift, not just green tsc/tests. Did not trust either self-report: diffed the pre-split file (`git show 5027fda09^`) against the new `newsChainFallback.ts` line-for-line — the `finalConfidence = baseline*discount*avgConfidence`, clamped to `[min,max]` only when `discount===1.0` else `TEMPORAL_DISCOUNT` applied uncapped, is structurally and numerically IDENTICAL; `resolvePdfText.ts`'s OCR-cache confidence ladder (0.2/0.3/0.5 thresholds) also byte-identical. Live named-volume `market.db` currently holds 0 `extraction_method='news_inference'` rows (rare-trigger fallback hasn't fired since rebuild — not a regression signal), so could not A/B this specific path against fresh output; used the strongest available substitute: `docker cp`'d the running container's actual `bctc/newsChainFallback.ts` + `bctc/resolvePdfText.ts` and diffed byte-for-byte against host HEAD — both IDENTICAL (diff exit 0), i.e. the code that will execute on the next real fallback fire is proven unchanged.

Independently confirmed the container is genuinely running the new code (did not trust ops's self-report): `docker exec` into the live container shows all 4 new files present under `/app/src/application/usecases/bctc/`, orchestrator=117L. `scripts/verify-deploy-sha.sh mcp-server` (default expected=current HEAD `fb246ae51`) reports drift — confirmed BENIGN by re-deriving myself: `git diff --stat c1d6c2c7f..HEAD -- apps/mcp-server/` is EMPTY (the 2 intervening commits are ops's own docs-only close-gate commit + an unrelated po telegram-triage commit), and `verify-deploy-sha.sh mcp-server c1d6c2c7f45e15ce57ab033e0d5df03415f2c43c` (the actual commit ops built+deployed, containing this task's code) exits 0/PASS. `GET /health` confirms `toolCount=183` matching `docs/data/project-stats.json` baseline, container Up(healthy). Queried live `market.db` via `bun:sqlite` inside the container: `financial_reports` total=80, matching ops's claim exactly (16 ocr_pdf/64 pdf-parse, most recent rows from 2026-07-07, pre-dating this rebuild — DB genuinely live and intact, no post-refactor breakage). DDD/security grep on all 5 touched/created files: clean (application-layer infra imports match this file's own documented layering comment — not a domain→infrastructure violation; zero `process.env`/secret/token literals).

VERDICT: PASS, no confidence-value drift, container genuinely runs the reviewed code. Per runbook delegation rule (ops Steps 1-4 / qa Step 5 / po Step 6 — the DONE flip is po's job, not qa's) and standing local precedent (qa-S18/S19/S20/S21 in `sprint-SYSTEMIC-REMAKE-P1-qa.md`, all holding REVIEW + routing po rather than repeat the `FACTORY-SCHEDULER-alert-confidence-literals` qa self-close deviation) — did NOT flip to `DONE_VERIFIED`. Appended findings to the existing `review_note` (did not overwrite dev-mcp-server's or ops's prior text), moved `next_agent` from `qa` to `po` via new `scripts/qa-factory-app-split-fetchparseandstorebctc-hold-review.jq` + `scripts/orch-apply.sh` (guarded on review[]+status+head.active_task_id), `.head.next_agent` synced to `po` in the same write; `.head.updated_at`/`updated_by` confirmed to reflect this write (not a stale copy of dev-mcp-server's or ops's prior timestamp). DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S24.

## cycle-402 · 2026-07-08 · FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE — independent re-verify PASS, DONE_VERIFIED

Follow-up from SPIKE-GATEWAY-BLIND-CLI-HANDSHAKE (architect). Developer added `mcp_call_gateway_meta()` + 3 private helpers to `scripts/agents-flow/mcp-call.sh` (3-step stateful handshake: `initialize`→`notifications/initialized`→`tools/call`, reusing the minted `mcp-session-id` header) bridging the 3 gateway meta-tools (`list_servers`/`list_server_tools`/`search_tools`), plus §6 Degraded Mode in `docs/standards/gateway-call-contract.md` (self-diagnosis, coverage matrix, §6d de-escalation rule that stops CRITICAL re-raise churn). Router had already RAW-verified (20/20 stub tests, shellcheck clean, regression suites 20/20+55/55, grep confirms `mcp_call()` untouched at line 93) before dispatching — did not rubber-stamp that: read the actual diff (a6031047e) myself, confirmed `mcp_call()` is byte-unchanged (purely additive insertion after its closing brace). Re-ran everything myself: 20/20 new tests, shellcheck+bash -n clean, both regression suites unchanged. Went beyond stub re-runs — sourced the REAL (non-stubbed) function and live-called all 3 meta-tools against the actual production gateway (`https://zenmidi.com/gateway/mcp`): `list_servers`/`list_server_tools` succeeded; `search_tools` with the doc's claimed-correct `{"query":...}` succeeded; `search_tools` with the doc's claimed-wrong `{"keyword":...}` failed with the EXACT error text the new §6b footgun note quotes (`unexpected additional properties ["keyword"]`) — corroborated the doc's factual accuracy live, not by re-reading prose.

Security: no `process.env`, no hardcoded secrets/tokens; `mcp-session-id` header value sanitized (`tr -d` whitespace/CR/LF) before reuse; endpoint genuinely unauthenticated, matches the cited SPIKE. Scope check: `git diff --stat a6031047e^..c80473c93` touches exactly the 6 claimed files (script, test, doc, journal, notebook, orch-state) — zero `apps/*`, no scope creep. Task-board hygiene: row confirmed present in `review[]` only (not duplicated in `in_progress[]`); `.head` confirmed untouched (`active_task_id` still `FACTORY-APP-split-fetchParseAndStoreBctc`, owned by a concurrent parallel FACTORY dispatch) per explicit instruction — only this task's own row moved. DJ-GATE-1 confirmed: developer journal STEP developer-S7 substantive.

VERDICT: APPROVED, no blocking issues. Flipped `task_board.review[]`→`task_board.done_verified[]` (status DONE_VERIFIED, next_agent=router) via `jq`+`scripts/orch-apply.sh` — script+doc-only fix, no Docker Close Gate applicable. `.head` deliberately left untouched (owned by the concurrent FACTORY dispatch) — only this row moved, per explicit dispatch instruction. DJ: sprint-SYSTEMIC-REMAKE-P1-qa.md §qa-S23. Report: reports/TASK_REPORT_FIX-GATEWAY-BLIND-DEGRADED-MODE-PROCEDURE.md.

**Retention note:** cycle-401 (FIX-DEVTEAM-PREFLIGHT-SF1-REENTRANT) and older pruned this write per AC-2 (keep last 3). Full detail preserved in prior git history of this file.
