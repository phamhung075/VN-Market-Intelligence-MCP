# QA — Notebook


## cycle-570 · 2026-08-08 · FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `f055226cf`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Commit real, on main ancestry; `git show --stat` matches 2/3 claimed `files[]` exactly — `db-integrity-history-append.sh` correctly untouched (verified benign: `EMIT_SIGNAL_TELEGRAM_SINK` is exported at test top-level and inherited by the subprocess `bash "$EMIT_SH"` call, no code change needed there). Ran both suites myself rather than trusting the 98/28 self-report: `emit-audit-signal.test.sh` 123/123 (incl. new T27-T31 sink assertions), `db-integrity-history-append.test.sh` 28/28 (incl. new T4 sink-content + exactly-1-line assertions). Read `_telegram_send()` directly: single `mcp_call "send_telegram"` call site left, gated behind `[ -n "$TELEGRAM_SINK" ]` early-return before mcp_call, both `_send_bug_telegram`/`_send_orphan_bug_telegram` route through it — no unconditional bypass remains (AC-1/2/3 held). AC-4 re-verified not trusted: `db-integrity-dedup-check.test.sh` has zero real emit-audit-signal/mcp_call/send_telegram hits (1 comment only) — N/A closure correct. AC-5 correctly deferred — `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` confirmed live BACKLOG. shellcheck: 3 new SC2329/SC2034 notes in the test file, diffed vs pre-commit — same false-positive class as 8 pre-existing (sourced-script fn-redefinition pattern), no new class; production file + `db-integrity-history-append.test.sh` gained zero new warnings. `bun tsc`/`mock-guard` N/A — zero apps/ TS touched, bash scripts only.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 767→767, signal_total 233→233). `.head` unrelated to this row (head-decoupled, points at `FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE`) — untouched. Verification text appended to row's own `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S5. Pruned cycle-567 (CLEAN-SKILL) this write per AC-2 retention (3-section cap) — kept 568/569/570.

## cycle-571 · 2026-08-08 · FIX-EMITSIGNAL-E3-RC3-FATAL-NORETRY-DROPS-DETECTOR-FINDING — APPROVED, DONE_VERIFIED (direct-commit verify, `qa[]` row, `branch:null`, commit `7cc052083`)

Head-decoupled QA-Drain dispatch (mode=verify-committed). Commit real, on main ancestry. `files[]` names `scripts/orch-apply.sh` but the commit doesn't touch it — checked rather than waved through: row's own `note` documents this as approach-a ("no orch-apply.sh contract change"), `git log -- scripts/orch-apply.sh` confirms no commit near this date, genuinely untouched not a missed file. Read the diff myself: new `_e3_read_candidate()` seam is called BEFORE `_orch_apply_invoke`; an empty read sets rc=2+continue (same retry lane as CAS mismatch, up to 3 attempts); a genuine rc=3 can still only surface from a real invoke on a non-empty candidate — that branch unchanged, still fatal/zero-retry. Ran tests myself, not the self-report: `emit-audit-signal.test.sh` 123/123 incl. T25 (empty-candidate-then-recovers: retries, succeeds attempt 2, row lands, no ABORT) and T26 (genuine rc=3 on non-empty candidate: no retry, ABORT e3-write-failed rc=3, row never appended, BUG telegram fires) — both green, both branches covered per feedback_fleetwide_gate_validated_on_one_file_optout_allowlist. Sibling zero-regression sweep reproduced: db-integrity-history-append.test.sh 28/28, db-integrity-dedup-check.test.sh 13/13, mcp-call.test.sh 9/9. `tsc --noEmit` clean (0 errors); `mock-guard.sh --files` correctly N/A for a `.sh` file (tool scope is `.ts/.tsx/.js/.py/.go` only, not a masked gap). shellcheck: 1 pre-existing SC1091 info note, diffed vs parent commit — unchanged, zero new warnings on touched lines.

VERDICT: APPROVED, DONE_VERIFIED. Moved `task_board.qa[]`→`task_board.done_verified[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 767→767, signal_total 233→233). `next_agent: pm`. Verification text appended to row's own new `status_note` field (no handoff file). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S6. Pruned cycle-568 this write per AC-2 retention (3-section cap) — kept 569/570/571.

## cycle-572 · 2026-08-08 · FIX-AUDITOR-DURABILITY-STEP0B-DETECTION — CHANGES_REQUESTED (direct-commit verify, `qa[]` row, `branch:null`, commit `92ef5b3a2`+`af9ad098c`)

Head-decoupled Review-Lane QA-Drain dispatch (mode=verify-committed). Commit real, on main ancestry; `git show --stat` matches the claimed file (`docs/agents/system-auditor/flow/main.md`) exactly. Read the full diff myself, not the review_note prose: Step 0b.1 (stale-marker sweep, mtime>20min, reuses `emit-audit-signal.sh`'s 7-day dedup ledger), Step 0b.2 (schedule-based tier2/3 heartbeat staleness + WARN-only tier-1 OR-check), Step 0b.3 (stub) + the Notebook Write/Commit block reorder + Notebook Append Gate condition-(b) rewire all correctly match the claims — grep regex is an exact match to `audit-output-contract.sh`'s 5 non-ABORT marker forms; `jq fromdateiso8601` threshold logic correct. Dependents `FIX-AUDITOR-DURABILITY-SKILL-DRAFT-PERSIST`/`FLOW-DRAFT-HEAL` correctly `depends_on` this row.

Went past text-correctness into LIVE production evidence (~44h since deploy) — this is what flipped the verdict. Two genuinely FRESH orphaned markers exist, created by real post-deploy Tier-1 cycles: `.auditor-cycle-markers-2026-08-06T21:09:00Z.tmp` (a real `[emit-signal] SKIP-dedup` line — won its fire-election, died mid-run, exactly the shape Step 0b.1 targets) and `.auditor-cycle-markers-2026-08-07T00:00Z.tmp` (`[cycle-init]` only). Both are 30h+ past the 20min staleness bar; dozens of subsequent Tier-1 cycles ran since (live notebook entries through today, `c86` @10:33:53Z) yet neither got swept — and zero `D-CYCLE-1` rows exist anywhere in `.signal_queue` (live+archive) or `auditor-dedup-ledger.json`. Independently computed Step 0b.2's own tier-3 predicate right now: `auditor-tier3-last-healthy.json` is 52h35m stale, past the step's own 48h WARN bar by ~4.5h — unfired, zero `D-CYCLE-2` ever emitted (tier-2 is within its own bar, so this is tier-3-specific). Also found 6/8 marker files got accidentally git-tracked by an unrelated commit (`2ddbe5321`, pm) — breaks the "`rm -f` is safe reclaim" premise the architecture-brief explicitly assumed ("currently sit untracked"); `.gitignore` has no entry for this pattern unlike the sibling `docs/data/cycle-snapshot-*.json.tmp`.

VERDICT: CHANGES_REQUESTED. The prose/logic itself is sound (verified independently), but there is zero confirmed live execution despite continuously-satisfied trigger conditions — an unproven "durability" mechanism should not yet unblock 2 dependent tasks that build on it. Moved `task_board.qa[]`→`task_board.review[]` via `jq`+`scripts/orch-apply.sh` (conservation OK, task_total 767→767), `redispatch_count: 1`, routed `next_agent: developer` (row's own `owner`, not fixer — no task branch exists, direct-commit path only). Full evidence + fix-ask in row's own `status_note`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-qa-12.md` §qa-S7. Bug-channel telegram flagging the live tier-3 52h+ gap ATTEMPTED, not sent — `mcp__gateway__call_tool` unavailable in this sub-session (no gateway tool grant, same known local-cowork-subagent gateway-blind limitation as prior cycles) — flagged for router to relay; finding is durably captured in the row's own `status_note` regardless. Pruned cycle-569 this write per AC-2 retention (3-section cap) — kept 570/571/572.
