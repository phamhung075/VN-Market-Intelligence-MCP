# PO Notebook

_Last: 2026-08-05T16:30Z (dev-team Step 1 triage, 16:07Z drain). Prior 2026-08-05T11:00Z (cron tick 10:37Z — live rag-service incident)._

## This cycle

- **The rag-service A-30 signal was a true positive about LIVE STATE and a non-finding about the CODE — so the missing action was the deploy, not another diagnosis.** Verified at source rather than inheriting dev-team's characterisation: `22232ad2b` landed 11:16Z, and re-reading `apps/rag-service/infrastructure/repositories.py` shows the reset really is in a `finally:` (:293-297) with a real `asyncio.Lock` (:138). Fix committed, container still pre-fix. Dispatched ops. **This is the PDF-AVAIL-02 shape caught at hour 5 instead of day 7.**
- **Five observations, three observers, one unshipped fix.** 07:41Z 97.83% · 10:06Z 96.91% / 23.7MiB free (below the 40MiB floor) · 12:07Z 97.71% (contradicting a Tier-1 ALL_GREEN emitted the same minute) · 16:18Z 95.77% no-reclamation. Recurrence here measures **deploy latency**, not detector noise — `feedback_recurring_detection_vs_recurring_failed_fix`.
- **`FU-RAG-DEPLOY-MEMORY` still carried "user-gated infra" on three separate `po_*` fields.** Last cycle retired that language on the CLEAN-EXIT row and missed its sibling. Retired here too. The blocker that remains is a genuine architect sizing call; the measurement gate it was waiting on was discharged 2026-07-29 (anon ~95%, ~726MiB resident on a 768MiB cap).
- **BCTC period-mismatch: the detector shipped, the actuator did not — do not read one as coverage for the other.** `FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT` (review[]) is what emits the 39 quarantine messages, and it is working correctly by refusing to store. `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0, BACKLOG since 08-01) is the only row that stops the fires.
- **Re-measured that P0 instead of re-asserting it: 19 fires at mint → 39 now, 13 tickers, all inside 24h,** 20 of them in an 11-minute burst. Every mismatch skews LATER, and **VND alone appears under five different supplied periods** (2023-Q4/2024-Q1/2024-Q2/2024-Q4/2025-Q1) each resolving to a different detected period — that is the `docs[0]`-unconditional signature read off the data, not off the row's own prose.
- **`list_unresolved_reports()` overflows any relayer's context (71KB), so I read all 124 myself.** The RECONCILE-EXHAUSTED flood (37) is already owned by `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` (ready[]) and the CIRCUIT BREAKER TRIPPED (3) is the *shipped* emission breaker doing its job — both FOLD. The 2 SLA CRITICALs fold into the FFLOW-STALE-0723 pair. **The 43 `[BCTC]` reports were the only untracked-at-the-root signal in the pile, and they were invisible in the relayed summary.**
- **8/8 sweep-guard `escalated=true` fires today self-corrected.** Checked mechanism, not outcome: the peer session's `8c116aef6` landed its exact two files **4 seconds** after the 16:19:58Z warn. No new row, threshold untouched per standing ruling. But the **rate is accelerating** — 1/hr → 2-in-a-minute → 3-in-12-min across 2 sessions. Rate is a proxy for how much of the fleet the row's script-actuator half has not reached; it is evidence about that row's coverage, never about the guard being noisy.
- **`TE-T02` has been re-flagged and never dispatched since 2026-08-01T01:58Z (~4.6d).** The bounded re-admission window is working as designed and still converging on nothing — the staleness fix cured invisibility, not throughput.

Pre-checks: signal_queue 0 NEW `to=po` · supervised-hold `should_hold=false` (`.head` idle; CLEAN-EXIT row already carries `po_goahead_20260805T105500`) · manual-dispatch sweep 18 candidates, `TE-T02` selected · TNB handoff still c121 (07-31), nothing new.

### Carry-over
- **Post-deploy, do not accept a memory-percent drop as the success predicate.** AC6 stands: this fix removes the compaction burst multiplier only. The ~700MiB embedder singleton keeps the container killable. Watch for the disappearance of repeated-optimize bursts and ExitCode=0 restarts instead.
- **Never `docker exec` into rag-service** — the exec shares the cgroup and SIGKILLed it on 07-29. Probe via host port `/embed/health`.
- If period-mismatch fires keep climbing after the SSC row is dispatched, quarter-blindness is not the whole story — re-check whether backfill re-submits the same PDF under many quarters.
- `RAG-FTS-BUILD-MEMORY-BOUND` stays time-gated to **~2026-08-12**; four PO ticks have now re-derived it for zero decision change. Stop re-deriving.
- `OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN` still in `ready[]` awaiting a slot. `FIX-RAG-COMPACTION-DISK-AMPLIFICATION` is sequenced *after* the counter fix by its own dep note — re-size once the deploy lands.
