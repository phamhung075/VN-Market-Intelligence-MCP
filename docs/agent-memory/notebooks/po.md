# PO — Notebook

## 2026-08-12T03:03Z · the row I was asked to mint had existed for four days at P2 — the gap was never detection, it was pricing

- Triage of 17 `pendingSignals[]` + 14 reports (4708-4721) + 3 undispositioned hot rows + manual-dispatch sweep. 1 `orch-apply.sh` pipe, **1 mint, 14 amendments, 3 signal rows closed**, `task_total` 764→765, `.head` untouched (idle, `updated_by=qa`). Conservation check clean.
- **★ THE DISPATCHER ASKED ME TO MINT `FIX-RAG-SERVICE-MEMORY-LEAK-INVESTIGATION` AND WAS RIGHT ABOUT THE URGENCY AND WRONG ABOUT THE GAP.** `FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS` has been in `backlog[]` since 08-08 naming this exact service and symptom — at **P2**, which is why three escalation cycles rolled past it. Minting a sibling would have split the evidence across two rows and left both under-priced. Folded + P2→**P0**. **When a peer reports "no row exists for X", that is a claim about visibility, not about the board — dedup first, and if a row exists, the finding is that its priority is wrong.**
- **★ I REFUTED THE ROW'S OWN STATED MECHANISM WHILE ESCALATING IT.** Its premise is a *bounded* allocator retention (~180-210MiB never returned after idle-unload → flat ~86-90% baseline). Measured: fresh restart 00:15:13Z → 94.25% at +15min → 98.73% at +50min, zero dips, `RestartCount` 11→12. **A bounded retention is flat after the first unload; this is monotonic growth from a cold process.** So there is a second, unbounded source and the AC must be a heap-growth-rate over ≥2h from cold, not a before/after unload dip. Live at triage: 965MiB/1GiB = 94.24%.
- **★ THE REPORTER'S RECOMMENDED FIX WOULD NOT HAVE FIXED IT, AND CHECKING COST ONE `grep`.** Report 4709 asked for "a severity tier in the A-30 dedup key" so the below-floor CRITICAL stops being swallowed. But `emit-audit-signal.sh:489-516` **already** implements the escalation bypass (`new_rank > stored_rank`), and the ledger already persists `sev`. It skipped because the stored entry is *already* `sev:3` and the new fire is *also* CRITICAL — `3 > 3` is false. **The rank enum saturates at 3, so once any key reaches CRITICAL every later CRITICAL for it is silenced for the rest of the 7-day window regardless of magnitude (86.6% → 99.33% below-floor was invisible).** That also widens `FIX-AUDITOR-EMIT-SEVERITY-LABEL-FLAT-ESCALATION-BYPASS-NEVER-FIRES`, whose stated fix — bind `--severity` to the measured value — is necessary but **not sufficient**: both fires here were correctly labelled.
- **★ THE ONE CONTROL THAT WAS HEALTHY REDIRECTED THE WHOLE INVESTIGATION.** Report 4720 saw T1 + T3 heartbeats both stale-while-claimed-fresh and proposed a shared broken helper, "audit all 3 tiers together". Read all three files: T1 `2026-08-11T19:32:31Z` (7.4h), T3 `2026-08-11T12:19:11Z` (14.7h) — but **T2 `2026-08-12T02:24:19Z`, fresh, 28min.** Same day, same helper surface. A shared helper cannot be broken for two callers and fine for the third. Minted scoped to T1+T3's clean-exit branches with **T2 named as the working reference to diff against**. **Two corroborating failures look systemic until you check whether anything in the same class is passing — the passing case is the discriminator, not noise.**
- **★ I DISPROVED TWO CRITICALs BY USING THE PRODUCT.** 4712/4713 (`price stale 74min`, `foreign_flow stale 1020min`, both stamped 02:00:0xZ = 09:00 GMT+7 = market open). Arithmetic put both last-goods exactly where a market-hours-gated feed and an EOD-daily feed *should* be; `get_market_snapshot` at 02:58Z then returned live VN-Index 1787.78 with `sessionTime 02:58:29`. Folded to `FIX-SLA-MONITOR-MARKET-OPEN-BOUNDARY-DETERMINISTIC-DAILY-BREACH` with the scope note that it must also cover **cadence** (daily/monthly feeds vs a flat 10min threshold) or it closes one row of three.
- SAME-FILE DIVERGENCE fired for the first time since its FP root cause went `DONE_VERIFIED` — so per the flow it is now a *candidate genuine* divergence, not an auto-fold. Ran the corrected two-blob form: **14 pure insertions, one `signal_queue` row appended by system-auditor.** Benign, and the fixed inspect command worked as specified. 4 × `escalated=true` sweep-guard blocks all carry **`actor` = the dispatcher session itself**, always staging a *peer's* notebook — the hook's per-session ladder can never converge an actor that re-enters every tick (occ 8→12).
- 3 CI failing files read from `--log-failed` before dedup (`ALLZERO-OHLCV-FETCH`, `FOREIGN-FLOW-…-NO-BACKFILL`, `transport.ts` now **265L** vs upper 138L), all 3 hit existing file-scoped `dedup_key` rows. **0 new CI rows, third consecutive cycle.** Identical 15091/40/5 tally across both HEADs — deterministic, not flaky; the dispatcher's "flaky class" framing is the one thing I'd correct.

### Carry-over (03:03Z)
- **Fourth consecutive cycle: ~1 mint, 0 dispatches.** `backlog[]` 390, `ready[]` 76, `in_progress` **4**. The rag row sat 4 days at P2; `FIX-AUDITOR-EMIT-SEVERITY-…-NEVER-FIRES` is 15 days old and is *also* a `BACKLOG-XOR-GAP` with no eligible picker. Detection has not been the constraint for a week.
- Manual-dispatch sweep re-admitted `TE-T06` (stale-flagged 08-06T22:05Z, never dispatched). **It splits `system-auditor/main.md` — the same file my new heartbeat FIX must edit.** Stamped with an explicit co-edit warning; land the FIX first or rebase.
- `bctcReparseJob` gap is now **monotonic across 3 probes** (28.4h→32.3h→36.4h) with no fire between, so the 08-11 "schedule-blind false CRITICAL" fold no longer covers it. Recorded a discriminator on `FIX-BCTC-REPARSE-DOUBLE-WRAP-DEDUP-GUARD`; `sqlite3` is **absent from the mcp-server image** (rc=127) so the host-side probe is not a valid instrument — do not repeat it.
- `market.db` "database is locked" (4708) was **transient**, disproved by reading through the same DB 3h later. The transferable defect is the probe's: a single shot with no debounce on the most-expected WAL outcome.
