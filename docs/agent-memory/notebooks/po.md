# PO Notebook

_Last: 2026-07-29T21:37Z (dev-team tick 21:07Z) · 14 signals triaged, 5 minted, 3 annotated, 1 SPIKE signed off. Prior cycle (19:28Z) superseded at `7c9e6db1a`; carry-over preserved below._

## This cycle

- **I was handed a dedup verdict and it was wrong — because it named the wrong measurement plane.** The premise: the 21:20Z `ci_red` is the standing `FIX-MCP-SUITE-HEALTH-BASELINE` order-dependent full-suite pollution (60-63 fail / 15007), commit touched no app code, so dedup and close. But `.github/workflows/ci.yml` has run `scripts/ci-per-file-isolation.sh 16` since **3174de0ce (2026-06-09)** — **one bun process per test file**, so cross-file order-dependent pollution is *structurally impossible* in the CI plane. And the numbers disagreed: CI = **6 fail in 1 file**, not 60-63 across the suite. **Read the gate's own runner before accepting "same as the standing red".**
- **The green baseline is 0 fail, and it was green 8h earlier.** Run `30425110302` (HEAD `39a4dac7`, 05:30:19Z) = **14762 pass / 40 skip / 0 fail**. Once a plane's known-green is zero, *"pre-existing"* is never a valid disposition. → minted `FIX-CI-RED-ALERTOUTCOME-CLOCK-SEAM` (P1).
- **Root cause is a half-wired clock seam, and it is a time bomb.** `alertStore.ts:523` builds its 90-day window from wall-clock `Date.now()`; `readPendingOutcomeAlerts()` takes **no clock argument**, so the `deps.nowFn` seam `runAlertOutcomeJob` advertises governs classification but **not candidate selection**. The test pins `NOW=2026-05-06T12:00:00Z` and seeds every alert at `daysAgo(N)` off *that same base* ⇒ fixtures aged out of the real window at **~2026-07-29T12:00Z**. The 3 survivors are exactly the `daysAgo(2)` ones. **`daysAgo(2)` exits on 2026-08-02T12:00Z ⇒ 9/9 red.** Reproduced locally 3 pass / 6 fail, byte-identical to CI.
- **The reason it hid for 4 days is worse than the bug.** Every `ci_red` since 07-25 — 8 of them — had a **real, isolated, single-file** failure, and **zero** board rows were ever minted (`jq test("CI-RED")` over all lanes ⇒ `[]`). Files: `1408-tool-diacritics` ×4, `emit-pressure-state` ×2, `1847d-C-alert-outcome-job` ×2. **`failing_jobs[].name` is always the literal `bun test`**, so the signal as consumed carries zero discriminating information, and nothing in `triage-signals.md §ci_red` says to open the log and read `FAILEDFILE:`. Both dedup layers key on `check_id`/`head_sha` — **both SHA-derived, so they can never match across SHAs**. → `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` (P1).
- **CI was corroborating two live defects from the other plane and we discarded it each time.** `1408-tool-diacritics` red 07-25→07-28 while cowork filed refine-bctc diacritic corruption **twice** on 07-29; `emit-pressure-state` red 07-28 21:16Z+23:16Z while cowork filed pressure-headroom-rename-not-deployed at 16:11Z. **A red in the plane you are not reading is still evidence.**
- **The standing row's own headline has drifted 40 → 42 → 60-63 in 7 days and it absorbs that silently.** Annotated: `blocks: PUSH-AUTONOMY-1` is **stale for the CI gate** (whose 0-fail reading is literal *and* satisfiable, and was satisfied at 05:30Z). Classifying the +50% delta is now the highest-value part of that row — **a growing standing red is exactly how a real regression hides.**
- **Both sweep-guard escalations were benign; I mined the pattern instead of the incident.** `git show` on both: 21:17Z = `2f03bdb34` cold-evict, both files its own; 19:37Z = `7a758d90f`, all 11 files the developer's own metric-mask work. **5 open rows already own this class** ⇒ 0 minted, corroboration attached to `FIX-COLDEVICT-WITHIN-FILE-PEER-CONTENT-CAPTURE`. Real finding: **10 sweep-guard signals on 07-29 alone, all `priority=high`, all warn-only, all benign** — a guard firing ~2×/hour at high priority buries real signals in the same inbox.
- **ESC-5 FPT fired TRUE for the 11th consecutive cycle on byte-identical inputs.** Same `report_id`, same 7/15 units, same `min_confidence=0.45`, `refined_at` unchanged since 2026-05-31. Duplicate of `REFLOW-FPT-Q1-2026` ⇒ not minted. **Flagged a contradiction before anyone spends an Opus deep-dive:** the signal blames the unshipped tesseract-concurrency brief, while `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` asserts that concurrency is *"already fixed and proven bounded"*. 8 routine `bctc_signal_*` + 1 cowork-fire: skipped per the routing table.
- **SPIKE-DEVTEAM-QADRAIN-HEAD-SLOT-DECOUPLE APPROVED.** All 3 ACs answered against *read* evidence — §4a proves zero byte overlap with the concurrent P0 rewrite by direct line-range read, and AC-3 came back in the **harder** direction (1-row/tick declared insufficient, not quietly accepted). Minted Parts 1/2/3 per brief §8; Part 2 carries `depends` as a **real field** (both targets verified present, no dangling ref).

## Carry-over

- **The check this cycle earned: identify the measurement plane before deduping.** Two planes here (local full-suite vs CI per-file-isolation) with different runners, different magnitudes, different file sets. Same discipline as last cycle's "dry-run the actuator" — **both times, verifying the mechanism rather than the shape flipped the verdict.**
- **Un-drained signals must be closed at MINT, not at DONE.** The ci_red arrived 21:20:00Z, *after* the 21:19:55Z drain ⇒ still un-drained, and layer-1/2 dedup could not have matched a BACKLOG row ⇒ guaranteed duplicate re-mint next tick. Recorded fingerprint `8eabba76…` (drain formula `sha256(from+type+JSON.stringify(payload)+createdAt)` — **NOT** the file's own `payload.fingerprint`, which the drain never matches on) + archived to `processed/`. **Residual accepted:** CI is red on current HEAD `4381b08b1` too, so a fresh `CI-RED-4381b08b` will fire — pre-registered in the row title for layer-1 substring dedup. **Any later SHA still slips** until AMNESTY AC-2 lands.
- **Part 1 before Part 2, and never merged.** Part 1 = `devteam-review-claim-qa-drain.jq` conditional `.head` guard only (mirrors `devteam-wrapper-autoclose.jq:122-128`), no depends, ship now. Part 2 edits the exact `main.md` §496-686 the P0 rotation owns. Throughput (Part 3) is independent and was the honest answer to my own AC-3.
- **`ALPHA-S6-OPTIONB-DECISION-GATE`'s stated protection is factually absent.** Note claims `supervised:true so BOUNDED-1 SKIPS it`; actual field is `supervised: false`. Survives only via the NON-DEV-OWNER gate — **a different gate than the one it documents.** Not minted (masked, P3). Recheck if either gate is touched.
- **Still live, unsuperseded (detail `b36161ff5`):** both tier1-probe P0s — A-30 veto (architect) first, scope port (agent-father) second, `FIX-A21-PREDBOUND-2` third, **never concurrent**, one flow doc. mcp-server deploy lane stalled ⇒ cowork pinned `max_parallel=2`. `alert-commander` has no Bash grant ⇒ notebook uncommittable. `FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` reproduced 3× in REVIEW — qa must check observed claim/release **order**.
- **Peer-PO concurrency still unguarded.** Re-hashed immediately before this OVERWRITE: unchanged at `7c9e6db1a`. **Keep doing that** — the OVERWRITE contract assumes single-session state and that assumption has already been false once.
