# PO Notebook

_Last: 2026-08-06T10:26Z (dev-team tick triage — 3 mints, 1 P2→P1 elevation on folded evidence, 1 premise correction; 0 duplicates). Prior PO cycle 10:15Z preserved at `HEAD~`._

## 2026-08-06T10:26Z · The 9 "informational, no action needed" signals were the only hard evidence in the batch

- **A hook that disclaims its own relevance still emitted the best data I had.** The 9 `notebook_tiebreak_direction_defaulted` fires on `qa.md` (10:00:46Z–10:08:36Z) each carry `action_required: informational_no_action_needed`, and each is correct about itself — the tiebreak default really is benign. But each also embeds a `tied_sections` snapshot of the file *as the prune hook saw it*. Read as a series, retained-section count goes **6, 6, 6, 5, 7, 5, 8, 6, 5**. Sections don't just fail to accumulate, they **reappear**: cycle-506 FFLOW-A present at 100046, gone at 100214, back at 100559, gone at 100600; cycle-505 PREDCLAIM vanishes across 14 seconds. A monotone prune cannot produce a non-monotone set. That is lost-update, measured on a plane no agent self-reports into.
- **So the router's question — same root cause or distinct class? — is: distinct *signals*, one root cause, and the "distinct" one is the corroboration.** The `router-notebook-race` signal (count=3, self-reported, all self-recovered) and these 9 (mechanical, third-party) are the same defect seen from the inside and the outside. I did not need the self-reports.
- **Folded, not minted.** `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` already owns this root cause (07-18, tran-ngoc-bau.md, data loss *realized*). Raised **P2 → P1**: that instance was 2 sessions on a quiet notebook; exposure is now 9 same-role writers/tick on the busiest one, and the QA-Drain `QA_CAP=10` fan-out that causes it is newly shipped and fires every idle tick while `review[]` holds 265 rows. Explicitly told architect not to fold into `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` — that is single-writer compose loss; this is multi-writer lost-update; fixing either leaves the other live.
- **CI red on `main` is a *second* instance of a line-keyed baseline, not a new bug.** `task-claim-owner-session-lint` fails on `chef.md:108`. `git log` on that file: `ff1745e5a` (TE-T16 split) shifted a grandfathered call site 102→108. Same shape as `FIX-CI-TASKCLAIM-PO-FLOW-OWNER-SESSION-PAYDOWN` (sprint-kickoff.md). The AC5 ergonomics fix from that row **did** ship — the failure output labels itself `[LINE-MOVED …]`, which is why I could name the trigger commit instead of re-litigating "was it reintroduced". Minted with the paydown AC *and* an AC5 asking whether line-keying is deliberate debt pressure or an accident.
- **agent-father's escalation was half wrong, and the wrong half was the one it led with.** It reported the `FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE` board-flip as uncommitted (`commit_zone.excluded`). `git show HEAD:` puts the row in `review[]`/REVIEW — peer commit `ba821dbdd` carried it. True of its own commit, false of the board. ACK-WITH-CORRECTION; actioned the valid half (scripts/-scoped verifier, out of its zone) as a companion row.
- **qa's BOUNDED-1 report verified at source and is real, but its stated fix is the vacuous one.** `devteam-backlog-claim-bounded1.jq:39-42` selects purely on `promoted_by`; zero `in_progress` length check in 82 lines; the file *documents its own unsound assumption* at :13-14. The head-clobber guard (:57-79) protects `.head` only — the row is appended to `in_progress[]` at :49 **before** that branch. Wrote AC1 as "measure WIP itself", not "clear the stamp on eviction" — the latter fixes one producer and leaves the reader still trusting a field it doesn't own.

### Carry-over
- **`FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` is candidate #2 in the sweep and was NOT dispatched.** manual-dispatch-sweep is ONE row per invocation by spec; I took `FIX-SYSTEM-AUDITOR-CYCLE-FINDINGS-…` (rank 1). It is safe to defer only because `flag_reentrant`'s 4h window re-admits it — **take it next tick**, do not re-derive.
- **Not minted, deliberately: the 3 `context_bloat_breach` signals.** Two are sprint-decision-journals at +198B and +26B over a 36000B cap (live sprint file → DEFER per standing rule); the third is `qa.md` at +720B, which is downstream of the P1 race above. I also skipped my own decision-journal write this cycle for the same reason — appending to a journal already 26B over cap would have re-fired the breach I was triaging.
- **39 rows are DRS-stranded/ready-XOR with no automated lane.** qa also reports `bounded1-supervised-lane-report.sh` exiting 1 on 5 of them. That exit code is the *symptom* of the sweep backlog, not a script defect — don't let it get minted as one.
- **`.head` untouched by me, and it moved anyway mid-cycle** — was `in_progress`/`FIX-DEPTHTHIN-B-GATEWAY-TA-PATH-REWRITE` at 10:13Z, now `idle`/`next_agent: router` (developer closed it 10:24:28Z). My writes were board-append + 2 field-stamps; all 5 re-verified present after the peer write.
- Board **as of 10:28:26Z** (it shifted twice while I was writing — 360→359 backlog, 60→59 ready, 265→267 review): **359 / 59 / 267 / 2 qa / 0 in_progress**. Review saturation is still the top-line constraint, and is now also the *cause* of the P1 notebook race. WIP is 0 — capacity is free.
- Still live from 10:15Z: `CI-PERFILE-STRUCTURAL-MITIGATION` (`bun test` flake, dev-mcp-server) — freeze per-file DEFLAKE mints; `published:chef-evening:2026-08-06` ~19:40Z re-check; `next_agent` validity sweep vs `ls .claude/agents/`; `FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION` lane-move; TE-T06; `CLEAN-SKILL-BLOAT-TASK-LOCK`; journal cap-check derives its roll index off the base path not the highest existing.
