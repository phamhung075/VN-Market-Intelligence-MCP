# PO Notebook

_Last: 2026-07-29T05:55Z (router-dispatched triage: a30-veto-tautology-suppressed-critical) · 0 rows minted, 3 rows updated, 2 previously-undispatchable rows given owners._

## This cycle

- **A live CRITICAL was suppressed by a rule we wrote, and the service died 113 seconds later during trading hours.** Tier-1 05:41:23Z: MemPerc 99.15%, `verdict=ESCALATE` reason `peak >97%` → clause 4 of `tier1-probe.md:167-171` downgraded it to PASS, `signals_posted=0`. Container down 05:43:16.520Z, RestartCount 3→4, VN market open. The detector was right; the *spec* told the agent to throw the finding away.
- **Zero mints. `task_total` 676→676.** Prior art existed (`FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE`, minted 07-25) and `git log --since=<minted_at>` over both suppressor files returned **zero commits** — so the defect is live at HEAD and this is a second miss of the same open row, not new work. Attached, did not re-mint.
- **I falsified that row's own root_cause.** It asserts "Upstream script is NOT at fault." Wrong: `verify-a30-...sh:94,96` carry `[ "$DIPS" -eq 0 ]` as a **conjunct** on both memory-level branches, so one 0.51pp dip makes the level irrelevant (05:00Z: min 85.73, max 93.72, dips=2 → FOLD). Two independent suppressors, two layers. **Folded into the same row anyway** — that script is already in its `files[]`, and two owners editing one script is the churn `EPIC-AUDITOR-DETECTOR-CORRECTNESS-DRAIN` exists to stop. AC(7) now rejects the row if only one layer is fixed.
- **Split the stopgap out and ordered it first.** The 07-25 row honestly warned "(a) would still have vetoed it" — but that was scoped to the WARN branch. A `peak >97%` CRITICAL is precisely what (a) unblocks, and (a) needs no threshold decision. AC(8) rejects a close-out that bundles (a) with the (b) redesign. **A ship-able fix blocked behind a redesign is why this survived four days and two misses.**
- **Two rows were on the board and structurally unpickable — I fixed that from a file read, not a guess.** `OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN` and `FIX-MCP-MEMORY-CODE-LEAK` both had `next_agent` unset. Grep put the clean-shutdown sentinel at `apps/mcp-server/src/composition-root.ts:254` → dev-mcp-server, and moved the churn row's zone `cross-service/`→`apps/mcp-server/` (the old zone would have routed to the wrong specialist). Neither is among the 12 rows the EPIC enumerates, so I did not pre-empt it.
- **`ExitCode=0` + `OOMKilled=false` reads clean; our own predicate says it is a crash.** 5 `mcpServerStartup` rows since the last `mcpServerCleanShutdown` (2026-07-22). Exit 0 is not an uncaught SIGTERM (that is 143) — so either the process exits via a path that never reaches :254, or the sentinel write races the DB close. I wrote that as a **lead, not a conclusion**, plus a re-route-to-ops condition if the death turns out to be external.
- **Found a future-dated all-clear inside the leak row.** `po_corroboration_20260729` reads "OPS verification 2026-07-29T22:06Z … no restart warranted" and cites `RestartCount=2` while live is 4; row `updated_at` is 07-**28**T22:06Z. It is a 07-28 observation mis-stamped a day forward. **Left it in place and prepended the correction** — rewriting it would erase the hazard's own evidence. Unread, it would have been quoted as a same-day all-clear against today's outage.
- **Re-verified rather than inheriting, for two read-only calls.** `docker inspect` (RestartCount=4, Exit=0, cap 3221225472) *and* `get_system_status` uptime 8m 7s @05:51:26Z — the second back-solves to the 05:43:1xZ start from a source that is not the Docker API. Internal consistency in the input was not corroboration. Zero infra actions; PLAN-ONLY held.

## Carry-over

- **The A-30 row is now 4 days old, P1, `recurring_bug_count=6`, and has missed twice.** If the next tick finds it still in BACKLOG with no architect activity, stop re-attaching evidence to it — that is the 3rd variant of the same ask, and it escalates to the user instead.
- **Test next tick:** did the pm spawn for `FIX-DEVTEAM-IDLE-CHAIN-STEP1-TRIAGE-STARVATION` happen? Still zero decomposition children ⇒ the out-of-band-spawn route is as dead as the SLS route; escalate, do not re-file.
- `RAG-FTS-BUILD-MEMORY-BOUND` — **do not re-measure before 2026-08-12** (`po_timegate_next_recheck_after` stamped). Early look justified only if growth collapses to ~0.
- `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` (P0, `owner=po`, BLOCKED) — mine. Confirm the BLOCKED disposition is still legitimate, not inherited.
- **New standing check:** when a row's own note asserts "X is not at fault", treat that as a *claim to re-verify*, not a settled constraint. Today it was wrong and had been steering the fix for four days.
- Standing, 8th carry: verify my own commits carry my authorship (`git log --author`).
