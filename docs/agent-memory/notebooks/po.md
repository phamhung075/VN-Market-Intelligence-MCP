# PO — Notebook

## 2026-08-15T12:24Z · The gate named a job; the workflow was red for someone else's reason.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain — `FIX-CI-TASKCLAIM-QA-FLOW-OWNER-SESSION-PAYDOWN`, agent-father reporting AC1 done and AC2 explicitly *not* committed. **Output: 1 `orch-apply.sh` pipe (review[]→done_verified[], 31→30 / 26→27, task_total 698→698 conservation-clean, `.head` untouched) + the AC2 baseline trim landed as a real file edit.**
- **★ THE VERIFICATION GATE NAMED A JOB, AND I ALMOST GRADED IT ON THE WORKFLOW.** `gh run list` shows `CI … completed/failure` on every recent SHA — read at that granularity the gate `ci_green_on_subsequent_push` is unmet and the row holds. But `gh run view <id> --json jobs` shows `task-claim-owner-session-lint` = **success on all four** post-fix runs I probed; the umbrella RED is **`size-lint`**, offender `getBctcPendingRefineTool.ts` (baseline=464L actual=605L upper=510L), already owned by its own backlog row. **A whole-workflow conclusion is the MAX over unrelated jobs** — grading a per-job gate on it means a finished row waits on debt it does not own, forever, and the wait looks principled.
- **★ THE HANDOFF *WAS* THE DEFECT, AND IT HAD ALREADY RUN ONCE.** agent-father could not land AC2: `docs/data/` is outside its `commit_zone.allowed`. Its note cites the sibling row doing exactly the same handoff. So the pattern is a **relay loop** — each hop is individually correct and the 11-line deletion never lands. I checked whether the sibling's trim ever arrived: the baseline holds **zero** `po/flow/main.md` entries, so it did — but only because someone eventually **stopped relaying and executed**. I had the reach; relaying a third time would have left the lint grandfathering two lines that no longer violate, **re-armed for the exact line-shift that minted this row**.
- **★ THE REGENERATOR WOULD HAVE COMMITTED THE SIN THE AC FORBIDS IN WRITING.** The file's own `_note` says entries "drop automatically on the next `--update`" — the tempting one-liner. But `--update` rewrites the **whole** baseline and would silently grandfather any NEW offender introduced since, turning a debt-paydown into precisely the re-grandfathering AC2 bans. Surgical `jq` delete of the 2 keyed entries, count 19→17, diff verified at **1 file / 1 insertion / 11 deletions**. **A generated file's refresh command is not a substitute for a scoped edit when the file is also a ratchet.**
- **★ I RE-RAN THE LINT *AFTER* THE TRIM, NOT BEFORE.** Pre-trim PASS proves the doc fix; it says nothing about the baseline edit, which is the only change that could break the gate. Post-trim: RC=0, *"0 new offenders … baseline carries 17 grandfathered site(s)"*. **Verify the artifact you just changed, not the one you changed first.**
- **★ LINE NUMBERS WENT STALE AGAIN, ONE DAY AFTER I WROTE THAT LESSON DOWN.** Dispatch said the sites were at 22/**221**; live they are 22/**235** — agent-father's own fix grew the block. It located them by **content**, correctly. Checking by the quoted number would have manufactured a false REWORK on a correct fix — the identical trap as yesterday's `:137`→`:159`. **Twice in two cycles is not coincidence: a stored line number is a stale cache by construction.**
- **The schema rejected me twice, and both rejections were right.** `next_agent: null` (Zod `.optional()` ≠ nullable — drop the key), then missing `verification.raw_probe`. Same two as yesterday, so the lesson had **not** actually stuck. It forced my four `gh` run-ids and both lint outputs onto the row as a **replayable** probe instead of leaving them in this transcript.
- **Carry-over:** (1) **Resolve a per-job gate at job granularity** — a red umbrella is the max over unrelated jobs, never evidence about your job. (2) When the assigned agent *structurally cannot* land an AC, **the next agent with the reach executes** — do not relay a scoped edit a third time. (3) Never close a ratchet-file debt with its own `--update` regenerator. (4) Re-run the gate **after** the last edit. (5) Locate code by content; treat every stored line number as stale.

## 2026-08-15T11:59Z · Reproducing the old failure is what made the new pass believable.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain — `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY`, my own 08-13 AC-2 rework returning for close-out. **Output: 3 `orch-apply.sh` pipes — review[]→done_verified[] (32→31 / 26→27), 1 row minted (`FIX-DEVFLOW-COWORK-SSOT-RESIDUAL-3-AGENTS`, task_total 698→699), 1 self-correction. `.head` untouched (already idle, never named this row).**
- **★ I RAN THE OLD, BROKEN RULE ON PURPOSE — THAT IS THE STEP THAT CARRIED THE PROOF.** This same AC had already been narrated-but-not-landed once: `6ddb1a812`'s message asserted *"matches all 3 live phrasings"* while its diff added **2**. So a second "14/14 PASS" narrative was worth nothing. I wrote my **own** implementation of the Check #2 rule text and ran it **twice** — once against the **pre-fix** 2-phrasing/ASCII-only trigger, once against the live one. The old trigger reproduced **PASS=5 / FAIL=9**, and the 9 names came back **byte-identical to my own 08-13 `ac2_gap` list**. That negative control is the whole verification: it proves the probe *discriminates*, so 14/14 means the fix landed rather than that I wrote a probe which passes everything. **A green run alone cannot distinguish "fixed" from "probe broken open."**
- **★ VERIFYING BY LINE NUMBER WOULD HAVE FAILED A FIX THAT IS FINE.** My 08-13 verdict pinned the AC-1 pointer target at `fail-loud-protocol.md:137` (STOP-RELEASE `:141`). Today that section is at **:159 / :163** — the file grew underneath it. Re-resolving **by section name** confirmed AC-1 intact; a line-anchored re-check would have produced a false REWORK on the one AC that was never in question. **Line numbers are a cache of a file's shape, and nobody invalidates that cache.**
- **★ MY OWN CLOSE-OUT PROSE WAS WRONG AND I CAUGHT IT ONLY BY VERIFYING THE PART I HAD ALREADY DISMISSED.** I wrote that the 3 residual agents "still resolve `cowork-error-boundary`". Checking before minting: **only `dev-team` does.** `dev-mainserver-crawls:6` and `dev-vps-crawls:6` point at a **different** skill, `cowork-boundary/SKILL.md`, which contains **zero** occurrences of "error boundary" and zero STOP-RELEASE. They pass Check #2 **solely because the phrase sits in their own pointer line** — the target carries no contract at all. That is **worse** than the false-green AC-3 fixed, where the target at least had real content. Wrote the correction onto the row rather than leaving a disproved sentence in a DONE_VERIFIED record.
- **★ THE SCHEMA MADE ME PRODUCE EVIDENCE I ALREADY HAD BUT HAD NOT SERIALIZED.** `DONE_VERIFIED` was rejected twice before landing: once for `next_agent: null` (`z.string().optional()` — null is not absent; I removed the key), once for a missing `verification.raw_probe{tool,args,live_value_observed,observed_at}`. The second rejection was **correct and useful** — it forced my re-run out of the transcript and into a re-executable command on the row. **A sign-off gate that demands a replayable probe is the structural answer to narrated-not-landed**, which is exactly the defect this row needed two rework cycles to clear.
- **Signed rather than held.** The 3 residual agents were fenced out of `files[]` by **my own** 08-13 scope ruling — holding the row would have punished agent-father for obeying it. Minted the follow-up with a **content-based** AC (resolved target must itself contain STOP-RELEASE, 11/14→14/14) instead of a phrase-presence one, so it cannot be closed by the same false-green shape.
- **Carry-over:** (1) **Always run the pre-fix condition too** — a passing probe with no negative control is indistinguishable from a broken probe. (2) Re-resolve doc pointers **by section name**; line numbers in a stored verdict are stale by construction. (3) When writing a residual into a close-out, **verify it before minting** — I was wrong about 2 of 3. (4) A repeat narrated-not-landed AC should be closed by **executing** the check, never by reading its description. (5) `null` ≠ absent to Zod `.optional()`.
