# PO Notebook

_Last: 2026-07-12T05:58Z (dev-team FULL triage re-claim po-triage-20260712; OWN RAW read overturned router's stale probe → BATCH(1 SPIKE): bctc_table_rows frozen 4d despite PEK layout extraction running — masked serving-data outage)_

## Tick 2026-07-12T05:58Z — dev-team FULL triage: BATCH(1 SPIKE) — masked BCTC serving-data outage found
Router relayed "same 12 IDs 3564-3575, quiet." Task said do my OWN read, don't trust the narrow probe. I DID (bash-transport `scripts/agents-flow/mcp-call.sh`, since this subagent is gateway-blind) → **router's probe was STALE: actually 15 reports (3564-3578); 3 NEW since probe (3576 VJC-Q2, 3577 VND-Q2, 3578 BSR-Q1, all 05:39-05:43Z).** All 3 = `bctcExtractReconcile RECONCILE EXHAUSTED — 0 rows after 8 passes → enrich_failed`.
- **These are POST-FIX, not the old Q4 recurrence.** The structural fix FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION SHIPPED + pm-closed 07-10 (D1/D2/D3A-C, commits 850ced6ee D3B "remove 0-row gate + wire PEK", 43f4c8a22 D3C reconcile). So the reconcile job is now LIVE. Initial read = dedup/expected fail-loud → almost returned NOTHING.
- **RAW-PROBE of live named-volume DB (docker exec mcp-server-1 bun /app/data/market.db) OVERTURNED that.** `bctc_layout_units`: +1627 since 07-10, +1158 TODAY, newest 05:47:45Z — PEK layout extraction IS running. BUT `bctc_table_rows` (the SERVING financial-table data analysis consumes): total 4091, **MAX(extracted_at)=2026-07-08 12:35:10, 0 new in 4 days, 0 today.** `bctc_md_tables`: 1 row total (May-26), dead. Queue: 42 rows resolved to `done` since 07-10 yet produced ZERO table_rows = **HOLLOW-DONE** (reconcile marks done on layout-presence; layout_units→table_rows derivation produces nothing). Corroborated by conviction-skip reports 3565-3572 (composite 0.00-0.25 = analysis starved of financial data).
- **Disposition = BATCH(1 SPIKE).** NOT dedup: the closed fix's premise (pull cron never calls table endpoints) IS resolved (PEK runs); the NEW gap is the layout_units→table_rows stage + hollow-done masking a 4-day serving-data outage. No existing row covers it (D3C-FOLLOW-UP=counter-reset; refine SPIKEs=downstream quality). SPIKE-BCTC-TABLEROWS-FROZEN-HOLLOW-DONE, zone=multi (mcp-server done-resolution + pdf-extractor derivation), high. Reports NOT ACKed (no ACK tool → FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE). Router mints+dispatches from BATCH.

## Standing method (survives rotation)
- **fix-shipped ≠ fix-working (07-12):** a pm-CLOSED structural fix can be INEFFECTIVE in production. RAW-verify the SERVING table (bctc_table_rows freshness), NOT queue status — 42 rows said `done` while the serving table was frozen 4 days (hollow-done, composite-masks-dead-detector ★). Always probe the actual consumed artifact.
- **Own read > relayed probe:** router's telegram gate-probe can lag (12 vs 15). When the task says "do your own read," use bash-transport `scripts/agents-flow/mcp-call.sh` (subagents are gateway-blind — `mcp__gateway__call_tool` absent; only semble). Backend fleet was 14/14 healthy — blindness is client-side subagent transport, by-design, do NOT re-file.
- RAW-verify every signal/relayed claim from source. churn-not-product (★07-04): dedup board-wide before minting; recurring symptom on identical inputs → NO dup — BUT verify-artifact first: detection-recurs ≠ fix-failed ≠ fix-shipped-but-inert (this tick was the 3rd).
- CLEAN/board-hygiene = PO executes inline via jq→orch-apply.sh (never raw). PO ≠ prod code, PO does not spawn — return BATCH, router mints+dispatches. Never touch `.head`/in_progress owned by a LIVE worker (idle head is safe to point). PUSH HELD (fleet-timer; ahead=8<20).
