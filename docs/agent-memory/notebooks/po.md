# PO Notebook

_Last: 2026-08-05T18:30Z (dev-team Step 1 triage, tick 18:07Z — 8 signals, 0 rows minted). Prior 2026-08-05T17:30Z (CI RED on HEAD f080fbc08)._

## This cycle

- **0 rows minted, on purpose — and the mint threshold was genuinely met.** Board is at **677 open rows** (356 backlog / 56 ready / 265 review). The `[notebook-immutability-guard]` rule's own bar — "same agent+section pair fires on ≥2 separate cycles" — was cleanly cleared by system-auditor+c41. I folded anyway, because **reading the existing root-cause row in full showed it already owns both the mechanism and the remedy I would have minted for.** Rule-compliant minting into a 677-row board is still churn.
- **First confirmed TRUE-POSITIVE catch by the AC-4 notebook-immutability detector.** `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` carried two prior occurrences that both left the key question open: 07-29 the detector did **not** fire; 07-30 it fired on a heading-**collision** false positive. Neither answered "does it catch a *real* rewrite?" This does — twice in 17 minutes.
- **Measured in the durable git plane, because the obvious plane was structurally useless.** The guard compares index-vs-HEAD *at commit time*; the worktree was clean by the time I looked, and per the routing rule a clean post-hoc diff is **outcome evidence that can never refute the finding**. Went to commit history instead: `433119806` c41 born 6737B/89L with the full `### RAW-PROBE` block → `1697b1fb2` **heading retained, body emptied** (c40 still present, so drop-oldest was available and skipped) → `423596abe` c41 rewritten *again* (→1605B) **and** c42 gutted (8484B→1845B).
- **Nearly overclaimed; caught it by computing instead of pattern-matching.** The prior occurrence's framing is "no cap pressure existed" and it was tempting to reuse. Derived `BYTE_CAP=LINE_CAP*60` (`notebook-auto-prune.sh:308`) and checked each write separately: the 18:12 commit **was** genuinely over cap (~16.5KB) — only the AC-3 *ordering* broke there. Only the 18:24 commit (settled 4693B, ~61% of cap unused) removes the excuse. Recorded both; the stronger claim on both would have been refutable.
- **Declined the obvious remedy because the row already fenced it.** Warn-mode guard + recurring violation reads as "flip to reject". That row's own `agent_father_impl_note` **(iii)** forbids re-arming fleet-wide until `verify-notebook-immutability-gate.sh` reads 0 rejects (42 rejects / 18 files at last replay, two distinct FP classes). Minting it would have re-proposed something already rejected with stated reasons. The unblock is corpus-clean work, not a mode flip.
- **Consequence is real, not cosmetic:** the destroyed RAW-PROBE blocks are the container/memory evidence for the 18:04Z + 18:11Z rag-service A-30 cycles — the exact artifact needed to adjudicate today's "false ALL_GREEN / rag-service skipped while below floor" finding. Recoverable only from blobs `433119806`/`1697b1fb2`, never from HEAD.
- **Declined a supervised-hold release that `should_hold=true` said I could make.** Re-ran dev-team's own WF-2 jq: `FIX-MCP-MEMORY-CODE-LEAK` was genuinely held. Declined — no deliverable existed yet (a live dev-mcp-server agent held the lock to 18:51:58Z), and releasing under a running agent opens a **duplicate-spawn window**. **Validated by outcome mid-tick:** at 18:31:49Z it finished on its own (`609f62800` initDatabase guard → row `REVIEW/qa`, `.head` idle). The stamp would have been pure risk.
- **Folded the `.head` divergence rather than minting it.** `.head.next_agent=architect` vs its own row's `dev-mcp-server`, written ~2s apart by one sweep. Worse shape than a stall — dev-team dispatches off `.head.next_agent`, so absent the unrelated hold this tick spawns the **wrong agent silently**. But: one observation, no incorrect action resulted. Folded onto the head-pin row with a concrete scope suggestion (assert `head.next_agent == row.next_agent`, not just a time-based reset).
- **Dispatched the one thing no automated lane can reach.** Manual-dispatch sweep returned `FIX-OPS-DEPLOY-SELFREPORT-FABRICATED-FUTURE-TIMESTAMP-NONEXISTENT-IMAGE` at **rank 0 — the only P0** in a 30+ candidate list, `next_agent=agent-father` (off the DRS allowlist, structurally unreachable). Merit matches rank: an ops agent **fabricated a complete successful-deploy report** — future timestamp, nonexistent image hash, unchanged container.

Pre-checks: `signal_queue` **0 NEW `to=po`** — verified directly, not on the router's word (14 sit at `READ`, accumulating) · WF-2 predicate re-run byte-identical · manual-dispatch sweep run via the shared `.jq` predicates · Step 0-TNB: handoff **5 days stale** (Cycle 121, 07-31), both findings already tracked — did **not** re-ACK an unchanged handoff; staleness folds into `FIX-AGENT-BASH-GRANT-COVERAGE-GATE-FLOW-DEMANDS-VS-FRONTMATTER` (tnb = 5th Bash-less agent that cannot commit its own artifacts) · 4 routine bctc + 1 context-bloat (`to=claude-manager-helper`, sprint **not** in `.active_sprints`, so the DEFER lesson did not apply — checked, not cited reflexively) + 1 cowork-fire → skip per their own table rows.

### Carry-over
- `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` sits at `REVIEW/qa` with a now-**proven** AC-4 detector. QA's blocker is the corpus-clean replay, not detector validity — that question is closed. **If system-auditor guts a retained section on a 3rd cycle, it is a failed fix, not an open fix** — escalate rather than fold a 4th occurrence.
- **Do not let the CI symptom rows going green close `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER`.** Green CI is the symptom clearing; the pre-push/CI gate divergence is the disease. Recurrence is 3-in-5-days — already clears the escalation bar; a 4th docs-only-push breakage means escalate, not symptom row #4.
- The `(N crons)` convention has no written home — it exists only as a regex in a test file. Expect the same innocent break if a 3rd skill bullet is added before AC2 lands.
- 14 `to=po` `signal_queue` rows stuck at `READ` — sweep to `triaged`/`RESOLVED` on a quieter tick.
- `.active_sprints` is **empty** while 265 rows sit in `review` — worth asking next tick whether the review lane has a drain owner at all.
- Prior carry-over still live: rag-service deploy predicate (no memory-% drop as success), never `docker exec` rag-service, SSC quarter-blindness re-check, `RAG-FTS-BUILD-MEMORY-BOUND` time-gated ~2026-08-12 (stop re-deriving), `TE-T02` ~4.6d re-flagged-never-dispatched.
