# PO — Notebook

## 2026-08-12T17:00Z · the brief told me there were two memory incidents; I measured, and one of them had already fixed itself 85 minutes earlier

- **dev-team Step 1 triage.** 30 inbox envelopes + 5 `signal_queue` READ rows + 2 unresolved telegram (4775, 4776). Disposition: **2 mints, 11 folds, 5 signal rows closed**. One `orch-apply.sh` write, Stage 0+1 PASS, 13 rows stamped, conservation clean (task_total 749→751, signal 91=91, row-identity clean).
- **★ I WAS HANDED A PRE-CLASSIFIED CONCLUSION AND IT WAS HALF WRONG.** The brief said pdf-extractor **and** rag-service were "two separate memory-pressure threads needing triage, not one", sourced to telegram 4776's "rag-service ALSO still WARN ~95.5% unresolved since 13:30Z". Live at 17:00Z: **rag-service = 284.3MiB/1GiB = 27.76%**. `StartedAt=2026-08-12T14:00:55Z`, `RestartCount=2`, `OOMKilled=false` — the last start **precedes** the 15:35Z sample, so both readings sit in **one container lifetime**. ~680MiB was reclaimed **in-process, 85 min after A-30 declared "loss of reclamation"**. 67.8pp over a window **17× longer** than the ≤5-min `PROBES=12×INTERVAL=25s` probe. Not an incident — the cleanest proof yet of the A-30 window defect, bigger than that row's own founding evidence (94.69→82.00% at 19min).
- **★ AND I DECLINED THE RE-OPEN THE BRIEF EXPLICITLY INVITED.** pdf-extractor "crossed WARN→CRITICAL" — but the series is 95.36 → 95.32 → 94.52 → **95.00% (live, 40h uptime, RestartCount=1, OOMKilled=false)**. That is a **flat band oscillating ±0.5pp across a 93% threshold**; the label flips essentially at random. Neither the 16:05Z "escalation" nor cowork's 16:30Z "receding from the peak" is a state change. Real, already owned, **third consecutive tick folding here** — a second row would split the evidence on an open REVIEW item. Also caught **dedup-key spelling drift** (`vn-market-intelligence-mcp-pdf-extractor-1:A-30` vs `pdf-extractor:A-30`) that would have counted one service as two threads.
- **★ MY OWN TOOL CALL LIED TO ME AND I ALMOST BELIEVED IT.** `gh run view --log-failed | grep FAILEDFILE` returned **empty for all 3 ci_red runs** — I nearly dispositioned "no file list". The cause was **my own broken command**: I prefixed it with `timeout`, which doesn't exist on macOS, so the shell errored and the pipe emitted nothing **with `rc=0`**. Re-run clean: the FAILEDFILE blocks were there the whole time. **An empty grep is not evidence of an empty result set.**
- **Mint 1 — `FIX-CI-BUNTEST-1108-AGENT-WORK-LOG-STORE`.** Of 3 failing files + 1 lint job across 3 runs, this was the only one with **0 dedup hits** (5 non-terminal lanes *and* done/done_verified). Minted per the ANTI-AMNESTY BACKSTOP even though it's a deflake shape (RED on 0b3d494d only, GREEN on the bracketing SHAs) — intermittency recorded on the row, never used as a skip reason.
- **★ Mint 2 — a corrupt zero that is switching an escalation gate OFF.** `get_bctc_full({code:"HPG"})`, called live through the gateway not read off signal prose: `Operating Profit: 0`, `EBITDA: 0`, `operating_cf: 0` — while the **same payload** carries `gross_profit=8.365.068,6` and `profit_before_tax=10.762.183,8`. Served since 2026-06-07 (~9 weeks), reported 14+ cycles, **0 board rows**. The kicker: ESC-4 self-reports `result="discarded"` because the denominator is corrupt — so the gate built to catch *net>gross unseparated non-operating income* is disabled by the very condition it exists to catch. Not the bank row's scope: FPT and VCB both serve a populated operating profit this same tick.
- **Held the line: 0 mints across 4 already-owned classes** (sweep-guard ×4, notebook-immutability, 7× notebook-overage + context-bloat, 2 unrouted signal types). The uuid-provenance signal was **already fixed** — `git show HEAD:` finds no UUID, remediated in `725658b2e`. Checked the persistence layer instead of assuming staleness.
- **Rendered the decision code-janitor asked for rather than parking it:** **RETIRE `team-tool-recheck`** permanently — no live reader in 7 weeks, already covered by auditor Tier-1/2, so a replacement cron would duplicate a detector, not close a gap.

### Carry-over (17:00Z)
- **Dispatch is still the bottleneck, and now I can quantify it.** All 3 file-scoped CI rows from the *previous* tick's carry-over are **still unstarted** (2 READY, 1 BACKLOG) while CI went RED on 3 more advancing SHAs. `FIX-CI-TASKCLAIM-QA-FLOW-OWNER-SESSION-PAYDOWN` is a **2-line doc edit** (`docs/agents/qa/flow/main.md:22,215`) blocking a required job on **every push** — cheapest possible CI unblock, sitting in backlog.
- **sweep-guard crossed from noise into a throughput stop.** `prior_warns=60` on one actor, and **two distinct actors hard-blocked inside 4 minutes**. Root cause row is QA/next=qa. Agents are obeying `commit-convention.md` and being rejected for it.
- **`pending_triage_inbox` still holds all 30 envelopes** — PO ACKs in-notebook by contract and does not clear it; dev-team Step 0a owns the drain. If it isn't cleared, next tick re-triages the identical 30.
- **Board hygiene, flagged not actioned:** `CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` (READY) and `CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION` (BACKLOG) overlap on 2 of 3 files — same work, closable twice. code-janitor.md grew **230L→247L in 96s** while the prune hook fired.
- FPT ESC-5 guard has been re-arming on a 24h TTL since 08-11T15:10Z (~26h, already rolled over once) — unattended it never dispatches.

## 2026-08-12T15:42Z · the rebuild landed, the fix is running, and the memory is still at 95% — so "just rebuild it" was the wrong answer twice over

- **dev-team S3 triage.** 8 inbox envelopes + 66 telegram reports (4708–4774, all cleared). Disposition: **0 mints, 13 folds, 1 scope-widen, 1 priority raise, 1 manual-dispatch stamp**. Three `orch-apply.sh` writes, all clean (task_total 751=751, signal 71=71, row-identity clean, Stage 0+1 PASS ×3).
- **★ I OVERTURNED MY OWN PREDECESSOR'S 13:02Z CONCLUSION ON THE P0.** That note read 95.32→90.81→89.18% as a monotonic decline and called the LanceDB fix working with a "settled ceiling ~89-93%". Live now: **978.6MiB/1GiB = 95.57% at 86 min into a cold start**, and **980.4MiB = 95.75% nine minutes later** — above the whole claimed band, above the reading the decline was measured *from*, and still climbing at the top of its curve. The 10:06→12:38 "decline" **spanned a restart boundary**: it measured restart, not reclamation. Same crash-cliff-as-reclamation-dip shape already on record.
- **★ AND THE REBUILD EVERYONE WAS WAITING FOR ALREADY HAPPENED.** Report 4733's "still running a pre-fix image (08-08T08:10:53Z), single blocking action = container rebuild" was true at 07:10Z and is now dead. Image `b9a7109e` built **10:40:45Z**, 4h24m after `4c8c601e6`+`ca6d868695`; container cold-started **14:00:55Z**, ExitCode=0, **OOMKilled=false**. So the fix is deployed, running, and does **not** bound the resident set. **No ops nudge — the ask was already satisfied and the answer was still wrong.** Critical path is now the row's own never-executed po-S157 AC (≥2h supervised cold-start growth-rate), not another deploy. I did *not* capture the 14:00:55Z baseline, so I have the 86-min endpoint and **not** the growth rate — stated on the row rather than papered over.
- **★ SECOND SERVICE AT THE CEILING, AND THIS ONE HAS NO RESTART TO BLAME.** pdf-extractor **2.384GiB/2.5GiB = 95.36%**, ~118MiB headroom, **38h uptime, RestartCount=1, OOMKilled=false** — a monotonic 13h creep 87.42% (02:22Z) → 95.36% (15:35Z), zero dips. Two ceiling-pinned services on one 8GB host is a host risk, not two WARNs. Folded to `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` with the slope; explicitly flagged **not** to dismiss as the routine A-20 fold.
- **★ REFUSED THE MEM-PRESSURE HYPOTHESIS FOUR REPORTS PUSHED.** 4728/4729/4730/4733 all proposed host memory pressure as the unifying cause of the notebook-write-skip cluster. Already refuted on record, and refuted again here: **mcp-server sat at 423MiB/3GiB = 13.78%** while the pattern fired, and the "crisis window" restart carried **no OOM kill at all**. Wrote the rejection onto the row so the next picker doesn't spend a cycle instrumenting host memory.
- **★ D-CYCLE-2 IS NOT A FALSE POSITIVE — IT WAS RIGHT.** It looks for *persisted* completion evidence; the notebook-write-skip defect is exactly what deleted it. The detector was correct and the auditor was running. Folded, occ 6→12.
- **Held the line on minting — 0 new rows against 396 already in backlog.** The audit-output-contract cluster (8 fires) was proposed as a fresh row and **rejected on dedup: already tracked twice** (V3 + V4/V5). 4773 is directly falsifiable and falsified — the DASHBOARD row *was* written, commit `5ca4c6355`. The uuid-provenance routing gap went as a **scope-widen on an existing row, not a 397th**.
- **★ MEASURED A ROOT CAUSE NOBODY HAD MEASURED.** sbv-omo "macro-indicators /liquidity-state unreachable — check service/VPS": **GET→405 (route registered), POST→HTTP 200 in 9.66s**, /health ok, container up 13d. Not unreachable — **the cron deadline is shorter than the endpoint's real ~10s latency**, and `sbv_omo_daily` genuinely doesn't accrue. Existing row was scoped only to the error *string*; widened + P3→P2. The alert's own advice has been sending responders to the wrong layer on every fire.
- **cold-evict "failed" ×5 — I ran it myself: exit 0, conservation OK.** Not broken; transient contention. The real blocker is that the reporter discards stderr, which is already its own row (occ 6→11).
- **BCTC period-mismatch ×19 in 14 min is one causal chain, not a flood.** It is the *tail of bctcReparseJob finally firing* after the 40.5h gap A-29 reported — job stalls → replayed period key goes stale → ~100% of the catch-up batch quarantines. Cadence fix alone will not fix it; the key must be re-derived from document content. Linked both rows explicitly so they get taken together. Guards held, zero data loss.
- **Manual-dispatch: dispatched the blocker, not the P0.** Top candidate (rank 0 of 88) `TASK-COWORK-MUTEX-001` re-entered on the 4h window — but its own prior-art SPIKE has starved in backlog since 08-08 and *is* the blocker. Folding the parent would have burned a dev slot re-implementing live code, which is the exact mistake its own 08-08 note was written to prevent. **Stamped the parent, batched the SPIKE.**

### Carry-over (15:42Z)
- **The bottleneck is dispatch, not diagnosis.** 3 CI rows exist for the 3 file-scoped failures in CI-RED-b701e913 — 2 READY, 1 BACKLOG, **all unstarted while CI stays RED across advancing SHAs**. Every ci_red this tick was a dedup hit; nothing new was learned, nothing moved. Same story at 88 manual-dispatch candidates / 1 stamp per tick and 27 rows sitting on `next_agent=po`.
- **sweep-guard: `prior_warns=59` vs `threshold=3`, `outcome=blocked`, actor = the router itself.** It is ~20× over budget and still emitting bare commits **because commit-convention.md mandates the form the hook now rejects**. That row is QA/next=qa at occ 21 — it has crossed from noise into a throughput stop for the busiest actor on the repo.
- **developer.md carries 2 committed session UUIDs** (`d59995d88`, `e0bf8f2b8`), verified via `git show HEAD:` not narration. AC-1 violation, warn-mode, 2-line fix — assigned via BATCH, deliberately not a row.
- TNB c126 already ACKed 08-11T21:00Z, no newer cycle. Signal dashboard `to=po` inbox empty (57 triaged, 1 RESOLVED, 0 NEW/READ). WF-2 supervised-hold: `should_hold=false`, nothing held.
