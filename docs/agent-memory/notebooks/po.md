# PO — Notebook

## 2026-08-08T00:08Z · dev-team Step 1 triage — 1 ci_red + 22 reports + TNB c124; 3 mints, 5 folds, 1 ratification

### What actually happened
- 5 `orch-apply.sh` pipes, all PASS, conservation OK (774→777), `.head` untouched. Minted `FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L` (high, `apps/mcp-server/`), `FIX-SLS-CLAIM-TIME-REVALIDATION-GATE` (P1, architect), `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE` (P1, developer). Folded onto 5 existing owners. Telegram queue drained 22→0. TNB c124 ACK appended, 0 new rows from it (as c124 itself asked).

### Decisions worth keeping
- **Both premises handed to me with the ci_red were false, and both were cheap to falsify.** The dispatching tick named HEAD `a20cbf56c`'s own diff as the likely cause — it is a docs/signals-only drain+prune commit. Real cause is `d6c4e6006` (18:43Z), proven on **two** CI runs rather than inferred: run `31196442451` (16:12Z, before) lists 1 offending file, `31214251207`→`31214251115` (20:04Z, after) lists 2. It also said the failure "may already be moot" — `git rev-parse origin/main` is **still** `a20cbf56c`; `7f244cca3`/`7c8846e8f`/`68a53f926` are unpushed locals touching neither file, and live `wc -l` reads 1388/181.
- **One ci_red, two files, two dispositions.** The mandatory FAILEDFILE read returned `coordinationStore.ts` (baseline-exceeded) *and* `checkForeignFlowGap.ts` (new-offender). Only the second matched a `dedup_key` → folded (occurrence 2). The first matched nothing on any non-terminal lane, and the ANTI-AMNESTY backstop forbids a "pre-existing" disposition with no matched row → minted. size-lint only goes green when **both** close; each row says so.
- **Verified the janitor's premise instead of answering its question — and the question was already dead.** It asked replace-vs-retire for the `team-tool-recheck` writer, claiming silence since 06-23. The local-cron replacement shipped 08-06 (`9e0f73bb8`) and 3 reports exist since, the last (`df641de4c`) ~10h *before* the signal fired. Answering "retire" would have killed a job restored 36h earlier. **Two real defects found by reading the script:** no liveness predicate at all (line 122 is a string literal), and the line-109 idempotency guard searches `docs/signals` at `-maxdepth 1` while the drain moves emissions to `processed/` — 4 such files exist, and this journal shows I already retracted the same signal at S65 *and* S70. Failed-fix recurrence → P1.
- **Collapsed two SLS symptoms into one root-cause row.** Prose do-NOT-dispatch ignored (3 fires on `FIX-INFRA-CRITICAL-VOLUME-PRESENCE-HEALTHCHECK`) and stale `dispatch_lane` on a multi-hop row (`FIX-BCTC-NEWSCHAIN-...`, would have re-spawned a finished ba) are **one** mechanism: the claim path re-reads nothing at claim time. Split rows would let a fix land on one branch while the other re-fires.
- **The `po_goahead` ratification target changed under me mid-tick.** The row moved `in_progress`→`review` at 23:58:16Z (architect closeout `a107d9135`), so `next_agent` became dev-pdf-extractor and the thing to ratify became the blueprint, not my memory reading. Confirmed its load-bearing claim **from source**: `main.py:154` process-isolates tesseract, `pek_run_helper.py:57` only thread-isolates PEK inside PID1 — exactly why the earlier concurrency fix bounded the child and not the parent. Cleared `plan_only` (design phase done + ratified), kept `supervised`.
- **My first stamp silently no-op'd and `orch-apply` still said OK.** It targeted `in_progress[]` after the peer move. Only the post-write re-read caught it. Second catch: zsh command-substitution ate three backtick-quoted code spans out of the ratification text — repaired via `--rawfile` and re-grepped for the landed strings. **`orch-apply` exit 0 proves the file was written, never that your intended row was.**

### Carry-over
- **BATCH has 5 entries, all needing router dispatch.** Top two are already-open rows that outrank the new mints: `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` (escalated **P0** — A-30 CRITICAL is firing ~20×/day against a fix that is already written and merely undeployed) and `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0, occurrence 63→**82** after a *second* 19-report period-mismatch batch, ready[] and never dispatched).
- **Manual-dispatch sweep: 84 eligible candidates** (43 DRS-stranded, 39 backlog-XOR, 2 ready-XOR, 4 stale re-admissions) — up from 46 last tick. Sweep dispatches exactly ONE per invocation by design, so this queue is growing ~2× faster than it drains. Needs a dedicated drain, not one row per PO tick.
- *(inherited, still owed by me — 6th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
- *(inherited)* `FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` remains the notebook-concurrency chain's critical path; `AC7-SKILL` stays BLOCKED until it lands.
- *(inherited)* Journal base file still breaching (660L/85047B vs 600/36000); hand-picked `-7.md` again. `CHORE-PRUNE-SPRINT-COWORK-GUARANTEED-SLOT-CATCHUP-DECISION-JOURNAL` owns it; still unlanded.

## 2026-08-07T13:11Z · agent-father sweep triage — both findings already minted; 0 new rows

### What actually happened
- Router relayed 2 findings from the agent-father maintenance sweep (`ac58cf667`/`df641de4c`/`e261e01d9`) as if they were new decisions. **Prior-art probe across all board lanes disproved that:** both were already minted by my own 2026-08-06T13:45Z tick. Minted **nothing**. One `orch-apply.sh` pipe, PASS attempt 1, `task_total` 774→774 (conservation confirmed).
- 5 mutations in that one write: occurrence-2 folds onto `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` + `AF-SEMBLE-INIT-DEF`; notebook-gap fold onto `CLEAN-STRANDED-REPO-STATE-20260806`; manual-dispatch stamp on the SSE row; signal `cja-20260807T103120` → RETRACTED. `to=po` NEW inbox drained 1→0.

### Decisions worth keeping
- **The re-fire is stronger evidence than the original, because the probe changed under it.** `ac58cf667` re-targeted sweep Checks 1/3/5 off the thin `.claude/agents/<id>.md` stub onto `docs/agents/<id>/init.md` — the stub had been producing **42/42 false-FAIL** (a 100% fail rate is the tell). The Error-Boundary finding *survived* that correction. I re-ran Check 2 myself over all 14 `docs/agents/dev-*/` dirs: 0 hits for exactly the 8 named agents, 1 each for dev-frontend/dev-mcp-server/dev-mainserver-crawls/dev-team/dev-vps-crawls. True positive, verified at source, not relayed.
- **Two claims in the relayed brief were wrong and I did not inherit them.** (1) "not agent-father's zone" is FALSE — `agent-father/init.md` `commit_zone.allowed` includes `docs/agents/`, and all 4 files in `files[]` sit under it. (2) "direction needs picking" on semble is STALE — `po_ruling_20260806T1345Z` already picked it (formalize the tool-style exception, minimal floor = `version:` + 1 boundary line; no delete, no full compliance).
- **Held P1 against the sweep's MEDIUM label.** MEDIUM is the check's generic severity, not a board priority. The occurrence-1 rationale stands: a dev-* agent dying with no on-failure contract strands `.head` non-idle → the documented recurring duplicate-spawn family.
- **Recorded AC-2 as PARTIALLY landed so the implementer does not redo it.** `ac58cf667` already made Check 2 case-insensitive, globbed `flow/*.md`, and added prose to follow thin-pointer microservice agents to the shared flow file. Remaining: generalise to a real one-hop `-> Run shared flow:` resolution. AC-1/AC-3/AC-4 verified NOT landed (`grep -ci 'error boundary' microservice-main.md` == 0; dev-frontend:17 + dev-mcp-server:18 still resolve to the cowork SSOT that omits the `.head` idle-reset).
- **Semble's premise changed without its direction changing.** The "re-firing daily" driver was the fleet-wide wrong-target bug, not the missing exception. Post-fix it is a genuine 1-of-42 true positive. ACs stay; only the AC-1 rationale sentence needs restating. Kept P3.
- **Resolved the 46-vs-42 notebook gap instead of deferring it** (agent-father punted it to "next cycle"). The 4 is a NET figure hiding **5 orphans minus 1**. Set-difference: 3 orphans are legitimate by design (`main` = router, `dev-team` = *not an agent type* per CLAUDE.md, `dev-news-fetch` = developer's zone), `refine_bctc_md` declares `notebook: none` in its own init.md, and only **2** are genuinely stale task-suffixed one-offs (`cowork-refactory-expert-2026-07-11-fr1-atomic`, `pm-alpha-s2-rag-fts-rebuild-cron`, ~27d/~23d). A 2-file code-janitor delete, not an orphan-scan sprint. Folded rather than minted a 377th row.
- **Retracted the code-janitor signal on a disproven premise.** It claimed the `team-tool-recheck-*.md` writer was silent since 2026-06-23 and asked me to decide replace-vs-retire. Three files exist from the last 48h, newest today (`df641de4c`). The recheck was already migrated off dead RemoteTrigger `trig_019Q8D5xtt...` to agent-father's local Step 5b — the no-RemoteTrigger directive is satisfied, the decision was moot. Its detector should key on newest-file mtime, not a hardcoded last-known date.

### Carry-over
- **BATCH returned with 2 entries, both needing router dispatch — neither self-promotes.** (1) `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` (P1/S, agent-father, `docs/agents/`) — its own `dispatch_note` marks it DRS-STRANDED-OFF-ALLOWLIST, so PO/router manual dispatch is its **only** path; it has now sat ~23h and re-fired once. (2) `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` (critical, supervised, architect) — the sweep's own top candidate, stamped `po_manual_dispatch_flagged_at` this tick; **4h staleness window**, so if the router does not dispatch it, it re-enters a later sweep rather than stranding.
- **The manual-dispatch sweep surfaced 46 stranded candidates, ~30 of them `next_agent=agent-father`.** That is a systemic queue, not a backlog of one-offs — worth a dedicated agent-father drain rather than one row per PO tick (sweep dispatches exactly ONE per invocation by design).
- **Journal base file still breaching** (660L/85047B vs 600/36000); `§ Cap Check`'s roll lives in a shell variable only, so I hand-picked `-7.md` again. `CHORE-PRUNE-SPRINT-COWORK-GUARANTEED-SLOT-CATCHUP-DECISION-JOURNAL` owns it; still unlanded.
- *(inherited, unresolved)* `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` still makes A-30 measure a pre-fix rag-service binary. Escalate the row; do not mint per occurrence.
- *(inherited, still owed by me — 5th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
- *(inherited)* `FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` remains the notebook-concurrency chain's critical path; `AC7-SKILL` stays BLOCKED until it lands.
