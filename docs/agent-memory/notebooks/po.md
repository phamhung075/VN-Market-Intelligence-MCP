# PO Notebook

_Last: 2026-07-15T22:00Z (dev-team :07 triage — 3 cowork signals: 1 dedup-enrich, 2 mint, all cross-checked vs router dedup-targets)_

## Tick 2026-07-15T22:00Z — triage 3 cowork-team to:po signals (all MED)
Board RAW pre: backlog 400, in_prog 0, review 25, ready 0, signal_queue 3 NEW. Apply: `po-s145-*.jq | orch-apply.sh` (Zod Stage0+1 PASS, conservation task_total 538→540 +2, CAS clean). Idempotent dry-run verified (re-run delta 0, 402 unique ids, no double-enrich) BEFORE apply.

**S1 cow-20260715T212837 (self-edit 2nd GUARD instance, content CORRECT) → DEDUP-ENRICH, RESOLVED.** Router asked: is `GUARD-COWORK-NOTEBOOK-AGENTS-SELF-EDIT-FLOW-DOC` now moot? NO. Not moot, not dismissed — the signal SHARPENS it. Folded into the row (P2→P1, recurring-confirmed, +enrichment field): (a) 2nd instance (bctc-analyst c089, one tick after alert-commander, an agent the row NAMES) ⇒ class qualifies alone; (b) INVERTS the revert precedent — 3/3 schema claims RAW-probed vs live tools before keep (asset_class/code required), committed ac1b13268 ⇒ review the CONTENT not the author; (c) FALSE-PREMISE fix: option-1 "sole legit write is a notebook" is FACTUALLY WRONG — ~207 committed docs/signals/*.json across 7 agents by design; frontmatter must describe reality FIRST or enforcement breaks routing fleet-wide; (d) 3rd scope item = a doc-fix-proposal channel via agent-father. Self-report-FP guard applied: "content correct" is RAW-probe evidence in ac1b13268, not agent say-so (feedback_agent_selfreport_metalayer_confabulation).

**S2 cow-20260715T212838 (date-keyed filenames clobber, 3rd instance) → MINT `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING` (P1/M/cross-service/ba), READ.** Router dedup-targets FU-CTG-DISCOVERY-FILENAME-FILTER + FU-BACKFILL-REAL-FILENAMES do NOT cover it (handoff checked; both are fetch/backfill filename heuristics, not signal-write cycle_id keying). DISTINCT. CONFIRMED clobber: bctc_signal_FPT_20260715_routine.json signal 9240 overwritten by 8120. Harm = drain reads files to route → N clobbers N−1's UNROUTED signal silently (dropped escalation). 3 instances same class same day (chef synth, tnb notebook, bctc). Fix = key filename by cycle_id. Folded §5 signal_id-reuse observation UNPROVEN (live DB = named volume, do NOT diagnose from host file).

**S3 cow-20260715T214005 (fire-election lock released Step 6 → self-refire) → MINT `FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE` (P1/S/cross-service/ba), READ.** Router flagged likely-distinct from P1 `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` — CONFIRMED distinct: P1 = peer/two-path/two-session; this = single-session self-refire (released:1 TWICE on cron:cowork:2026-07-15T21:30Z), namespace merge changes nothing. 3rd member of "guard released when work ends" class (#1 UC-CCA-P3 P0, #2 the P1 mutex row). Fix = tombstone-not-lock: preflight compares pressure_state.tick_id==nominal_tick ⇒ SUPPRESS before election (tick dying pre-Step-6 must still re-run). Coupling hazard: long tick → re-fire AND legacy-mode-no-dedup simultaneously. Added sibling_refire_row marker on the P1 row.

## Carry-over
- **RETURN to router: BATCH of 2 NEW PLAN-ONLY mints** (FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING, FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE) — both plan_only, next_agent=ba, zone cross-service/, sprint COWORK-RELIABILITY. Do NOT idle-auto-pickup as coding lanes (PLAN-ONLY, design-first). S1 needs no batch entry (folded into GUARD row, now P1).
- **Recurring class watch:** the "guard released when work ends" pendulum now has 3 tracked members (UC-CCA-P3 P0 peer-owned + 2 P1). A single shared invariant (`published/tick ⇒ TOMBSTONE never release`) would close all three — candidate for one architect umbrella, not 3 point fixes. Flagged to router, not minted (avoid over-fragmenting a peer-owned P0's scope).
- **Untriaged (out of scope):** BCTC pipeline telegram reports 3358–3370 (reconcile-exhausted DBC/DXG/FRT/GEX/KDC/KDH/MSN Q4, low-confidence, zero-URL) — known recurring class, covered by 30+ BCTC backlog rows + FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE (explains their perpetual status=new). Not user requests, no mint.
- **Parked (peer-owned, not re-triaged):** UC-CCA-P3 P0; FIX-VNINDEX-CROSS-PLANE-PLAUSIBILITY-GATE; MARKET #933 correction (user go/no-go). BOUNDED-1 idle-pickup router-owned.
