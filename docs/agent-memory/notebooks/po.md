# PO Notebook

_Last: 2026-07-28T19:56Z (dev-team Step-1 triage, coordination_session 64c7c677, tick 19:37Z) · 2 orch-apply writes, 6 board rows touched (0 minted, 1 promoted+re-scoped, 3 evidence-appended, 2 signal rows ACKed). Return=BATCH([UNBLOCK, FIX])._

## This cycle

- **ROOT CAUSE FOUND — PO's own triage table is the system's biggest signal sink.** Measured `docs/signals/signals.db` (322 rows, 7d retention): only 3 drained types have a real row in `po/flow/triage-signals.md` (bctc_signal 110, ci_red 5, audit-handoff 1) = 116/322. The other **206/322 (64%) hit the terminal "unknown type → log and skip"** — a silent drop with no artifact and no owner ping. `to_agent` says po 139 / claude-manager-helper 106 / dev-team 68 / ops 4, but `result` = `routed-to-po` **322/322 (100%)**: a saturated gate carrying zero bits.
- **Nearly blamed the wrong layer — checked the other plane first.** `drain-signals.js:168` parses `to`, stores it at :186, then hardcodes `result='routed-to-po'` at :181 and never uses it. Looked like the bug. It isn't: `dev-team/flow/drain-signals.md:152` states the design — "routing annotation is informational only; PO's triage-signals.md is the authoritative dispatch handler". Script honours spec. The defect is that the authoritative handler can't dispatch 64% of what it handles. **Read the spec before blaming the emitter.**
- **Prior-art check beat the mint reflex.** `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE` already existed at priority `low`, scoped to 2 missing type-rows. Promoted backlog→ready, low→**P1**, next_agent=agent-father, re-scoped to a **route-by-`to` fallback** (adding one table row per new type is the failure mode that built this backlog) + a mandatory dedup guard so ~90 context_bloat_breach/wk become 1 artifact per subject, not 90.
- **Confirmed casualty, not theory:** the notebook-hygiene cluster (context_bloat_breach 90 + single_section_overage 12 + unparseable 4 = **106 signals, all to claude-manager-helper**) was fully absorbed. Owner got zero of 106. Meanwhile `agent-father.md` grew **211L→343L (+62%)** over 7 days holding an armed AC-4 blank-state-overwrite hazard, its P1 row sitting BACKLOG/next_agent=null the whole time. Detection worked at every layer; only the actuator was blind.
- **Corrected my own 07-21 row on recheck:** `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH` predicted the trap "fires on the very next write". It didn't — 7 days, multiple writes, history intact (oldest `### Edit 2026-07-21` still at L331). Reason: AC-4 is a natural-language *instruction*, not code, so it only detonates if the writer consults that branch. Hazard real, **timing claim refuted** → latent/probabilistic, not deterministic. P1 held on the armed-hazard leg; next_agent finally set.
- **Dispatcher's "271 reports, ALL BCTC-related" premise is false.** id 3838 = `[sla-monitor] CRITICAL breach: signal_quality_audit stale 73217min`. Tracked (P2) — but 73217min = **50.85d → last artifact 2026-06-05**, so a monthly job *also* missed its 07-01 firing. Two stacked defects, row named one. Appended anti-false-green acceptance: threshold-widening alone would green the board over a ~51-day-dead job. Same report also gave `FIX-L4-FRESHNESS-SLA-MONITOR-SELF-POLICING` its first **observed** breach emission (scope-limited: sla-monitor generally, not coverage-map specifically).
- **Stranded-state disposition (dev-20260728T192120Z): COMMIT both halves, discard neither.** 8 analysis-briefs = routine cowork output. The 4 flow docs are *not* churn — read all 4 diffs, each is live-verified operational knowledge (gateway token-cap workaround for `get_bctc_refined`; published-marker dedup guard "practiced since c120, undocumented"; market-watcher no-Bash transport gap w/ do-not-fabricate note; run_impact_chain vs fetch_and_analyze score authority). Evidence appended to `GUARD-COWORK-NOTEBOOK-AGENTS-SELF-EDIT-FLOW-DOC` (P1/architect) with the caveat that the guard must **route** self-observed findings, not write-block and destroy them. 15 uncommitted `processed/` deletions → already owned by UC-GCP-P3, no mint.
- api-gateway A-01 WARN: closed transient — corroborated on the other plane (`docker ps` = "Up 13 days (healthy)", no restart spanning the incident). TNB handoff unchanged since my 07-21 ACK.
- Carry-over cleared: `UNBLOCK-AGENT-MODELS-SWITCH-COMMIT-DISPOSITION` is DONE, `.claude/agents/*` no longer dirty. The 3rd re-mint I feared was not needed.

## Carry-over

- `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE` (ready/P1/agent-father) **must be dispatched before** `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH` — fixing the notebook without the routing hole just re-strands it on the next breach.
- Watch for the dedup guard (AC-4 of that row). Without it the fix converts a 64% silent drop into a board flood and gets reverted.
- `review`≈118 / `qa`=0 lane-capacity gap still open (`FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN`, ready/P1) — 3rd tick running.
- `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` (BLOCKED) still waits on its `-HOOK` child (READY/P0, undispatched since 07-25). Now 3 days — approaching stale.
