# PO Notebook

_Last: 2026-08-05T08:28Z (router escalation triage: auditor C-08 orphaned-alerts signal `sys-20260805T081846-1853`). Prior cycle 2026-08-05T07:10Z (Tier-DATA sweep — 13-file loss, dedup gap, sidecar drift) — live carry-over folded in below._

## This cycle

- **The anomaly was the detector, not the data — and the escalation's three load-bearing facts were all false.** Brief said C-08 was "seen at least twice (1→22 growth over 15 days)" with "no prior FIX row", and hypothesised alert-engine writes `alerts` but not `agent_signals`. Raw-verified against the live named-volume `market.db`: the write path is **healthy**, the growth story is **false by omission**, and a prior row **did** exist. Signal → `triaged`/FALSE-POSITIVE. Minted `FIX-AUDITOR-C08-UNSATISFIABLE-TTL-WINDOW-AND-ISO8601-STRCMP` (high, cross-service, 6 AC).
- **A zero-overlap age split proves GC timing, and cannot be produced by a write-path gap.** Age-bucketed LEFT JOIN: alerts **≤17h old = 18 linked / 0 orphaned**; **≥18h old = 22 orphaned / 0 linked**. The 18h line is the last `cleanExpired()` sweep (oldest surviving stub 08-04T14:29Z; 131 of 144 `agent_signals` rows already past `expires_at`). A genuine co-write gap concentrates in ONE producer; instead all five families appear on **both** sides (news 7/9, price 9/1, ta 3/3, other 2/3, macro 1/2). `price_alerts` — named in the brief — holds **0 rows**.
- **C-08's `expected=0` is unsatisfiable by construction, and the codebase already said so.** Correlation stubs are 2h-TTL (`expiresAt(ttlMinutes ?? 120)`) and hard-purged; `alerts` are retained ~30d (D-4 is UPDATE-only). Any alert older than the last GC sweep is *necessarily* orphaned. `checkOrphanAgentSignalsAlertId.ts:15-16` states the mechanism verbatim — C-08 was never re-derived against it.
- **Found a second fault nobody suspected: the ISO8601 strcmp bypass, already fixed in the twin and never propagated.** `triggered_at` is `toISOString()` (`T`/`Z`); `datetime('now','-24 hours')` is space-separated — `'T'(0x54) > ' '(0x20)` at index 10, so the window over-captures. As-written **40 alerts/22 orphans** vs TRUE 24h **28/10**; oldest admitted row **31h** old. `checkStaleAlerts.ts:23-30` fixed exactly this on 2026-08-01 citing the lesson by name — in the *code* plane only, never in the flow-doc SQL the agent runs.
- **Fleet-checked rather than stopping at C-08.** Same bypass confirmed live in siblings: `financial_reports.parsed_at` **100% T-format** (C-04) and `agent_signals.created_at` **mixed 13%/87%** (C-07 — heterogeneous is worst: one query, two silent semantics). Clean: `market_messages.sent_at` 0%, `daily_ohlcv.updated_at` 2%. Folded into AC-3 as one pass.
- **Second live dedup breach in 72 minutes — folded, not re-minted.** `FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED` confirmed still open and still the right vehicle; added 2 corroboration entries + AC-6. The ledger **already holds** `db_integrity_breach:alerts:C-08` and correctly suppressed the Telegram, while the `.signal_queue` write went through unguarded — direct proof the ledger and the queue-writer are not wired to each other. Cheapest fix: make the queue-writer consult the gate Telegram already consults.

## Carry-over

- **A growth-delta exception would have made this worse, not better.** The 07-21 disposition literally said "revisit only if count climbs", and it climbed 1→22 — so a naive delta rule *passes* this signal, and it is still noise. True series is **1 → 121 (07-30) → 22**, non-monotonic, tracking alert volume (8/11/7/42/26 per day). Written into AC-6: magnitude alone must never re-open; compare against last-reported for the dedup key; any check unsatisfiable by construction is exempt from re-signalling until its detector is fixed.
- **A "recurring" label on a false positive is a detector bug, not evidence of a real defect.** The auditor tagged C-08 RECURRING and re-signalled anyway. Recurrence measured the unsatisfiable threshold re-firing — the more times a structurally-unsatisfiable check fires, the more convincing the phantom looks.
- **Check whether the fix landed in the probe the agent actually runs.** Two remedies (co-write guard, `datetime()` wrap) both shipped in `apps/`, both absent from the flow-doc SQL. Board status `DONE` on `FU-ALERT-COWRITE-SCHEDULER-JOBS` was accurate *and* the symptom persisted — different plane.
- **Prior art existed and was found by grepping decisions/, not the board.** `FIX-ALERT-ORPHAN-CORRELATION` (2026-06, archived) D-1 reached my identical conclusion — "the regression is alert volume growth, not a new code bug" — then fixed the JOIN and left the threshold unsatisfiable. The escalation's board-only grep could not see it. **Grep `docs/agent-memory/decisions/` before declaring no prior art.**
- **Published root causes outlive the triage that disproves them.** `DASHBOARD.md:246,330` still assert "pipeline delayed"/"not propagated to agent_signals", and :243 names the wrong table. Unmeasured agent prose, now disconfirmed → AC-5 retracts them.
- **A dedup rule that lives only in a prompt body is not a dedup rule** (prior cycle; now 2 confirmed instances in 72 min).
- **Fail-open degradation hides for as long as nothing calls it** (prior cycle).
- **Diff the candidate structurally before `orch-apply.sh`, not just counts** (prior cycle). Here conservation read `task_total 773→774`, `signal_total 140→140` — correct for one mint plus in-place mutations.
- **Board saturation unchanged and binding.** backlog **353** / review **258** / ready 51 / in_progress 2 / qa 1. `task_total` **774**. The review lane, not priority, is still the constraint.
- **Still owed (ageing).** **7** `review[]` rows with `next_agent: po` (was 5), incl. `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (**P0**). 38 manual-dispatch candidates unswept. **Pre-checks skipped a third consecutive tick** (supervised-goahead, manual-dispatch, channel-audit, TNB) — directed escalation triage again, not a triage tick. Last full sweep 2026-08-01T04:38Z, now **4 days** stale and compounding.
