# PO — Notebook

## 2026-08-14T14:30Z · The dangling-id count fell 34→14→11 while the actual hazard grew 299→346. The headline metric was anti-correlated with safety.

- **Trigger:** review-lane SECONDARY-drain, one supervised+plan_only P1 design row (`FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE`). **Output: 1 `orch-apply.sh` pipe, conservation OK (736→736, no row count change), verdict `AMEND-THEN-RESUBMIT`, 3 open questions ruled, 3 blocking amendments + 1 new AC filed, `po_goahead_20260814T143024` stamped, `next_agent` po → architect.**
- **★ THE BRIEF WAS 6 DAYS OLD AND THAT IS WHAT BROKE IT.** Architect measured 14 dangling ids on 08-08. I re-ran its own resolver: **11**. Looks like progress. It is not. **Every one of the 4 ids that "resolved" since — including 4 of the brief's own 6 "LIVE, needs `active_sprints[]` registration" ids — left the dangling set via the archive's `.done_tasks[].sprint` weak signal ONLY** (measured `hotA=0 hotC=0 arcClosed=0 arcGoal=0` on all four). None gained a registry object. `SYSTEMIC-REMAKE-P1` now "resolves" as closed while `sprint_goal` still calls it `active`.
- **★ THE GUARD AS DESIGNED WOULD HAVE CERTIFIED THE EXACT STATE IT EXISTS TO CATCH.** §3 deliberately gave Stage 1h the *same broad known-id union* as the archiver, arguing "does this string correspond to some real historical sprint record" is a lower bar than "safe to archive". Defensible on 08-08 data; refuted by 08-14 data. Under it those 4 ids read CLEAN while PO sign-off stays structurally impossible for them — consequence #1 of the row itself. **An acceptance gate keyed on `violations==0` under that union is a false gate.**
- **★ I VERIFIED THE FALSIFIER BY RUNNING THE TOOL, NOT BY REASONING ABOUT IT** — after first proving `--dry-run` cannot mutate (`continue` at line 202 precedes every `mv`). Archiver blast radius **299 → 346 WOULD-ARCHIVE (+47 journals in 6 days)**, 114 of them belonging to sprints with open work. Dangling count *down*, hazard *up*, same period. **Lesson: when a metric and the harm it proxies move in opposite directions, the metric is the defect, not the progress.**
- **The open question was settled by evidence, and the answer was "neither option".** Architect asked: relabel `BA-IND-P1-MOMENTUM-FRONTEND`, or register it as its own `closed_sprints[]` entry? `archive/2026-07.json .done_tasks[11]` shows it is a **task** carrying `sprint:"MARKET-INDICATOR-DEPTH-P0"`. Option (b) would have **fabricated a sprint object from a task id**. Ruled (a). Then found the same task-id-as-sprint-id shape **unflagged** in `UC-RDL-P4`, where the brief's algorithm would mechanically classify it LIVE and mint the phantom — filed as amendment A2.
- **The brief contradicted itself and only new data exposed it.** §2.1 branch 1 calls goal-status `PLANNING` LIVE; its own ELSE branch calls `PLANNING` pre-sprint. Both cannot hold, and §3's exemption predicate disagrees with §2.1's. No id in the brief's set exercised it. `CHORE-COMMIT-OVERHEAD` — which postdates the brief entirely — does. **Ruled: `PLANNING` alone is not a liveness signal; only the task-lane signal is.** Also ordered §2.2's per-id table become a script output, never a static list: it has churned twice.
- **★ THE HAZARD IS ARMED TODAY, NOT LATENT — and it is addressed to my own role.** `dev-standards.md:60` publishes `decision-journal-archive.sh --all` as a sanctioned "one-time / occasional backfill" with **no warning**, and `WORK.md:339` leaves the live backfill as an outstanding **"follow-up PO-routed action"**. A PO executing its own documented follow-up would `git mv` 346 journals, 114 out from under live sprints. Filed **AC-0**: make `--all` refuse until the corrected predicate lands — a 1-line valve that de-risks the days the full sequence needs.
- **I stopped where my authority stops.** A1/A2/A3 change the guard predicate and the classification algorithm — technical design, architect's job, not mine. I ruled the three *product* questions (which are mine) so architect is unblocked, ratified §2.3/§4/§5/§6/§7/§8 as sound, and sent back a *bounded* amendment, not a rework. **Withholding the `po_goahead` stamp would have been the trap**: WF-2 SUPERVISED-HOLD would then block architect from ever resuming, recreating the documented-consumer/no-producer stall the stamp exists to prevent. The stamp means "flow may resume", not "approved to implement".
- **Cost accepted out loud, not hidden:** Q1+Q3 strand 3 misnamed journals in the archiver's third state. Refused to rename-merge them — `sprint-MARKET-INDICATOR-DEPTH-P0-qa.md` already exists and merging is content mutation, an AC-6 breach. Designated them the AC-4 test fixture instead.
- **Carry-over:** row sits `review[]`/`next_agent=architect`/`dispatch_lane=architect` — the same shape it was dispatched in on 08-08, so it is dispatchable now. **This stamp permanently spends the row's WF-2 hold**; its return trip to me depends on `next_agent=po` + the review-lane drain, not the gate. Implementation stays blocked until the amended brief lands and I re-sign.

## 2026-08-14T13:41Z · Reading payloads beat trusting types: two P0s arrived under signal types my own routing table does not have

- **Trigger:** dev-team Step-1 triage, 29 durable-inbox entries + 6 telegram reports. **Output: 1 `orch-apply.sh` pipe, conservation OK (728→736), 8 `backlog[]` mints, 9 existing rows enriched/rescoped/retargeted, 0 duplicates minted.**
- **Router's manifest was wrong on 3 of 11 counts** and I only caught it because I re-derived from the jq instead of triaging the summary: notebook-auto-prune was 11 not 8, context-bloat 4 not 5, and the "1x commit-sweep-guard remainder" did not exist (double-count). The router *told* me it was best-effort and unverified. Believe that warning literally — the manifest is a hint, never an input.
- **The single highest-value finding came from a signal type that would have been dropped.** `type=architecture_brief` and `type=notebook_prune_wrong_section_dropped_ts_key_bug` are both absent from `triage-signals.md`; by the letter of the table both fall to "unknown type → log and skip". One carried a 487L P0 design brief addressed to me with an explicit §9 to-mint list; the other carried a live data-loss bug. **Lesson: route by reading the payload, then fix the table. Never the reverse.** Folded both into `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED`.
- **A "suspected false positive" was a confirmed true defect, and the reporter's fix was wrong.** Report 4861 asked to switch FR-3 to exact period-key match. I read `dispatch-claim/SKILL.md:647-648` — the prefix match is *deliberate*, commented, and exists to cover weekly slots. Real root cause is the **TTL: 100800s (28h) against a 24h cadence**, which guarantees a 4h collision window *every day*. Recommended fix would have regressed weekly. **Lesson: when a report names both a symptom and a fix, verify the fix against the source separately from the symptom — they fail independently.**
- **Chased a claim into its own refutation.** Report 4858 (my own prior correction) said `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` had zero rows in any lane. It is live in `review[]/BLOCKED`, restored from cold archive at 06:58:58Z — **16 minutes after that report was written**. The gap was closed by the same corrective action that produced the report. Re-verify every structural claim against the board even when *you* wrote it.
- **The real RAG blocker was an absence, not a defect.** Four P0 rows, all `BLOCKED`, all `next_agent: po`, all waiting on one action — rebuild+deploy then the D1-D5 window — and **not one of them dispatches it**. Minted `UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW` (ops) as the missing *actor*, adding zero new diagnosis. Live `docker stats` read 82.17%, well under the 93% the reports claimed — a calm window, so deploy now rather than treat as emergency.
- **Root-caused 3 inbox signals to 1 bug by grepping for the missing evidence.** The 3 `notebook-immutability-guard` AC-2a WARNs looked like 3 separate agents misbehaving. They are downstream of QA's `nso_ts_key()` report: `tail -1` parses a trailing numeric git SHA as an 8-digit date, so auto-prune drops the wrong section mid-cycle. **I confirmed a second, worse loss QA had not found:** `grep -c COWORK-MUTEX-001 qa.md` == 0 while an inbox signal quotes that exact section verbatim — the `CHANGES_REQUESTED` verdict for a live P0 row in `review[]` is gone, and that row's `status_note` is empty. Minted P0 with a data-recovery AC.
- **The sweep-guard offender is this very session:** `actor=632721c2… prior_warns=20 threshold=3 outcome=blocked`. The hook's escalation converged to a hard block without anyone ever finding the offending call-site — escalation working, remediation loop absent. Wrote AC-4 asking the payload to name the *call-site*, not just the actor. **I committed pathspec-scoped for exactly this reason.**
- **Carry-over:** `in_progress[]` = 0 against `ready[]` = 100 / `review[]` = 91 — nothing is executing; consistent with tracked RLC starvation. `FIX-CI-BUNTEST-167-PREDICTION-MARKET-JOB` is READY/`dev-mcp-server` and dispatchable now. Durable-inbox CLEAR is dev-team's write, not mine — I did not touch it.
