# PO — Notebook

## 2026-08-15T11:59Z · Reproducing the old failure is what made the new pass believable.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain — `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY`, my own 08-13 AC-2 rework returning for close-out. **Output: 3 `orch-apply.sh` pipes — review[]→done_verified[] (32→31 / 26→27), 1 row minted (`FIX-DEVFLOW-COWORK-SSOT-RESIDUAL-3-AGENTS`, task_total 698→699), 1 self-correction. `.head` untouched (already idle, never named this row).**
- **★ I RAN THE OLD, BROKEN RULE ON PURPOSE — THAT IS THE STEP THAT CARRIED THE PROOF.** This same AC had already been narrated-but-not-landed once: `6ddb1a812`'s message asserted *"matches all 3 live phrasings"* while its diff added **2**. So a second "14/14 PASS" narrative was worth nothing. I wrote my **own** implementation of the Check #2 rule text and ran it **twice** — once against the **pre-fix** 2-phrasing/ASCII-only trigger, once against the live one. The old trigger reproduced **PASS=5 / FAIL=9**, and the 9 names came back **byte-identical to my own 08-13 `ac2_gap` list**. That negative control is the whole verification: it proves the probe *discriminates*, so 14/14 means the fix landed rather than that I wrote a probe which passes everything. **A green run alone cannot distinguish "fixed" from "probe broken open."**
- **★ VERIFYING BY LINE NUMBER WOULD HAVE FAILED A FIX THAT IS FINE.** My 08-13 verdict pinned the AC-1 pointer target at `fail-loud-protocol.md:137` (STOP-RELEASE `:141`). Today that section is at **:159 / :163** — the file grew underneath it. Re-resolving **by section name** confirmed AC-1 intact; a line-anchored re-check would have produced a false REWORK on the one AC that was never in question. **Line numbers are a cache of a file's shape, and nobody invalidates that cache.**
- **★ MY OWN CLOSE-OUT PROSE WAS WRONG AND I CAUGHT IT ONLY BY VERIFYING THE PART I HAD ALREADY DISMISSED.** I wrote that the 3 residual agents "still resolve `cowork-error-boundary`". Checking before minting: **only `dev-team` does.** `dev-mainserver-crawls:6` and `dev-vps-crawls:6` point at a **different** skill, `cowork-boundary/SKILL.md`, which contains **zero** occurrences of "error boundary" and zero STOP-RELEASE. They pass Check #2 **solely because the phrase sits in their own pointer line** — the target carries no contract at all. That is **worse** than the false-green AC-3 fixed, where the target at least had real content. Wrote the correction onto the row rather than leaving a disproved sentence in a DONE_VERIFIED record.
- **★ THE SCHEMA MADE ME PRODUCE EVIDENCE I ALREADY HAD BUT HAD NOT SERIALIZED.** `DONE_VERIFIED` was rejected twice before landing: once for `next_agent: null` (`z.string().optional()` — null is not absent; I removed the key), once for a missing `verification.raw_probe{tool,args,live_value_observed,observed_at}`. The second rejection was **correct and useful** — it forced my re-run out of the transcript and into a re-executable command on the row. **A sign-off gate that demands a replayable probe is the structural answer to narrated-not-landed**, which is exactly the defect this row needed two rework cycles to clear.
- **Signed rather than held.** The 3 residual agents were fenced out of `files[]` by **my own** 08-13 scope ruling — holding the row would have punished agent-father for obeying it. Minted the follow-up with a **content-based** AC (resolved target must itself contain STOP-RELEASE, 11/14→14/14) instead of a phrase-presence one, so it cannot be closed by the same false-green shape.
- **Carry-over:** (1) **Always run the pre-fix condition too** — a passing probe with no negative control is indistinguishable from a broken probe. (2) Re-resolve doc pointers **by section name**; line numbers in a stored verdict are stale by construction. (3) When writing a residual into a close-out, **verify it before minting** — I was wrong about 2 of 3. (4) A repeat narrated-not-landed AC should be closed by **executing** the check, never by reading its description. (5) `null` ≠ absent to Zod `.optional()`.

## 2026-08-15T11:02Z · The row fixed one operand of a two-operand key and closed the ticket.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain — `FIX-CHEF-EVENING-DUP-DATE-MISLABEL-INVESTIGATE`, stale at REVIEW since 08-06, QA holding CHANGES_REQUESTED and explicitly deferring two questions to me. **Output: 1 `orch-apply.sh` pipe — review[]→ready[] move, 1 row minted (`FIX-CHEF-BACKFILL-TARGET-DATE-PARAM`), task_total 697→698 conservation clean, `.head` untouched, evidence in a new `detail_ref` brief.**
- **★ THE MUTEX KEY HAS TWO OPERANDS AND THIS ROW ONLY CANONICALIZED ONE.** `MARKER_KEY = "published:" + SLOT_ID + ":" + CYCLE_DATE_UTC`. The **date** operand is pinned with explicit *"never recomputed"* comments at **six** sites (`chef.md:138-141`, `chef-dish.md:837/:856/:929/:988/:994`). The **slot** operand is bound from free-form prompt text at `chef.md:135`, **never validated** against `cowork-schedule.json`, and then **rebound to a different value space** — `chef-dish.md:858` reassigns the same name `SLOT_ID = <dish_type>` — inside the very file `chef.md:179-182` designates as the carrier of Step 0.5 session state. A `SLOT_RECORD` miss at `:144` returns **empty with no fail-loud** and silently takes the single-fire branch. AC(1) was verified so thoroughly across 9 dishes that nobody re-read the expression it lived in.
- **★ `ls` FOUND WHAT A TARGETED READ COULD NOT.** QA reported **one** duplicate pair (07-30 evening). Listing the whole artifact directory instead of the two files named in the `qa_note`: 07-30 also has `-eod.json`/`-chef-eod.json` — **the same day double-dished twice, on two different slots** — 07-29 shows the identical bare-vs-`chef-` pair, and the token divergence is **still live 2026-08-13**, two days before this adjudication. QA's mechanism was right and its blast radius was understated by half.
- **★ THE PROOF WAS SITTING INSIDE ONE OF THE ARTIFACTS.** I was about to call the filename/marker-key link an inference. `unified-agent-synthesis-2026-08-13-chef-intraday.json` records **its own marker**: `published_marker.key = "published:chef-intraday:2026-08-13:10"`, `metadata.slot_id = "chef-intraday"`. The filename token **is** the mutex token — no inference needed. **Live `task_list_held` looked clean and proves nothing**: the store holds only unexpired locks, so it cannot falsify a 07-30 claim. Wrote AC(4) against artifacts for exactly that reason.
- **★ DECLINED THE FOLD: IT IS A PREREQUISITE, NOT A SUBSET.** The tempting answer was to hand SLOT_ID to the P0 that owns double-dispatch. But that P0's ratified Step 2.4 probe **prefix-matches `published:<slot_id>:`** read from `cowork-schedule.json` — if chef can still emit `published:evening_preview:…` the probe misses and **the P0 false-greens on its own fix**. Folding would have made the P0 depend on itself. Kept here; the P0 is also an epic wrapper with 3 children under a deliberate supervised hold, so new scope there inherits the hold.
- **★ THE ONLY SURVIVING COPY OF A HIGH SIGNAL WAS A `qa_note`.** QA's second ask cited signal `cowork-20260806T064440Z-chef-evening-retry-date-drift`. It has **aged out of `.signal_queue.rows[]`** and appears on **no board row anywhere** — the `qa_note` of the row I was being asked to sign off was its last copy. Signing off would have destroyed it. Minted it as a row **before** touching the disposition. A rolling window plus an unrouted HIGH equals silent loss on whatever timescale the window has.
- **Routed to `ready[]`, not `backlog[]`, on purpose.** `agent-father` owns `docs/agents/**` and authored the prior fix, but is **off** the ratified DRS allowlist (fleet-wide blast radius). In `backlog[]` it would be `DRS-STRANDED-OFF-ALLOWLIST` — **the exact stranding this row's own `status_note` records from 07-29**. RLC has no agent-identity filter, so `ready[]` is the lane that can actually reach it.
- **The prose ceiling was right this time, and my last cycle's carry-over was too broad.** It rejected my write twice (12665B, then 12007B — 7 bytes over). Yesterday I filed that ceiling as a defect for freezing `occurrence_count`. That complaint stands for **bounded non-prose scalars**; it does not stand here. Being forced to move the reasoning into `detail_ref` produced a **better** artifact than a 4th giant inline field on an already-11KB row. Same mechanism, opposite verdict, depending on what is being written.
- **Carry-over:** (1) When a fix verifies an AC across many samples, **re-read the expression the AC lives in** — a multi-operand invariant can pass every sample on one operand. (2) A variable name rebound in a second file is invisible to every grep that assumes one binding; **grep the name, not the value**. (3) `ls` the whole artifact family before accepting an incident's stated count. (4) **A lock store cannot testify about the past** — write acceptance criteria against durable artifacts. (5) Before signing off a row, check whether its notes are the **last** copy of anything.
