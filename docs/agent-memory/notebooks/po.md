# PO Notebook

## 2026-08-24T08:5xZ · The OOM kills ops called proof of failure are the motivation for a fix that already shipped

- **Trigger:** router hand-dispatch, one scoped ruling on `UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT` + 2 routing/priority calls. **Output: 2 orch-apply writes, 4 rows mutated, 0 minted.** head idle throughout, never written. `task_total` 807→807.
- **★ RULING: ops' AC-4 verdict CORRECT (FAILED), ops' AC-5 conclusion WITHDRAWN.** The two kernel memcg OOM kills are real and correctly timestamped (`dmesg -T` gives absolute UTC directly — no uptime arithmetic needed; VM clock confirmed UTC; buffer spans 08-20T15:43:31Z→now, 961 `docker0` lines = non-vacuous). But `oom_memcg=/docker/6ec27ac85c8e` is **not among the 13 containers in `docker ps -a --no-trunc`**. It is the retired pdf-extractor created **08-15T09:47:36Z** (QA read it "Up 8 days" at 08-23T13:50Z). Live container `2edf0c9c9905` started **08-23T15:44:17Z** — 77 min and 45 min *after* both kills.
- **★ The router's correction was right in verdict but imprecise where it matters: the kills are MID-fix, not PRE-fix.** The retired container **did** carry AC-3's `malloc_trim` (`c3fd44766`, 08-15T09:07Z) — wired **only** into `/pek-extract`'s `pek_run_helper`. A **second** trim landed 08-23T15:25:15Z as **`6f3577b9f`** *"trim native alloc after every /extract call (memcg-OOM root cause)"*, and **its own commit body cites these exact two kills** ("~31 min apart"; 14:27:11Z→14:58:52Z = 31m41s) as its motivation. So they prove **AC-3 was insufficient ALONE** — a real finding — and they are evidence **FOR** a shipped fix, not against the running one. Live image `5d7d36db` (git_sha `0c9380859`) carries **both**; `merge-base --is-ancestor` TRUE for each.
- **★ Efficacy is NOT "undetermined" — the natural sample is large and clean.** 563 `POST /extract` + 58 `/pek-extract` in 17.5h, **all HTTP 200**, `memory.events oom_kill=0`/`oom=0`, zero dmesg OOM lines across a buffer covering the whole container life, zero `MemoryError`. The production workload **is** the burst test and it already ran **621 times**. A synthetic burst adds power, not first-order evidence.
- **★ Arithmetic: 88.37%, not 86.3%.** `kB` in `/proc/PID/status` is **KiB**. 2,316,488 KiB ×1024 = 2,372,083,712 B / 2,684,354,560 = **88.37%**; over the 80% bar by **214.2 MiB**. Ops used correct KiB math for the OOM `anon-rss` figures and decimal in the same block. **Verdict direction unchanged — a correction, not a reversal.**
- **★ Rescinded my OWN 07:22Z expedite: P0 → P1.** It rested on "a LIVE crash source". There is none. Held at P1 (not lower) on `memory.peak == memory.max` + the drift below. Declined to hold P0 on `peak==max` + 88.4% alone: `pgsteal/pgscan = 4,964,445/5,033,338 = **98.6%**`, `workingset_refault_anon = 0`, `memory.events.max` **frozen** across 8 samples over 2m21s. That is reclaim succeeding, not a death spiral.
- **★ AC-4's bar REPLACED on the sibling — the old one was unfalsifiable in the pass direction.** VmHWM is a **monotone lifetime** high-water mark of **PID 1 only**: once touched it never falls, so AC-4-via-VmHWM is permanently FAILED for the container's whole life however well the fix works — and **both kernel victims were forked children** whose RSS is not in PID 1's VmHWM at all. New bar **B1** peak `anon` ≤80% of cap (what the OOM killer charges; kills were at 94.8%/94.2%) · **B2** `oom_kill==0 && oom==0` · **B3** `pgsteal/pgscan ≥0.90 && workingset_refault_anon==0` (this is what makes `peak==max` non-damning) · **B4** idle anon drift ≤32 MiB.
- **★ NEW FINDING the router did not ask about — idle anon drift with zero traffic.** `anon` 1,447,403,520 B @03:56Z → **1,743,306,752 B @08:33Z = +282 MiB in 4.6h**, while `docker logs -t --since 04:00Z` shows **zero non-`/health` requests**. Both trims are wired to **request finally-blocks** — with no requests, **neither can have run**. If real this is a third mechanism neither fix addresses. Honest caveat: two samples from two sessions, not one series. Became **B4** and the priority question of the new AC-7/AC-8.
- **★ Replaced the aborted burst test with a passive sampler, not another burst.** AC-7: ≥12h, 5-min cadence, CSV under `docs/incidents/data/`, script persisted under `scripts/`; allocates nothing in the target cgroup so the 500 MB safety gate is irrelevant to it. AC-8: report B1–B4. **Explicitly: clean B1–B4 closes the sibling — no synthetic burst required.** Did **not** mandate a restart to re-arm the peak instruments (destroys 17.5h of counters); noted `memory.peak` is **write-resettable on kernel ≥6.12** (host `6.12.76-linuxkit`, verified) *after* pre-reset values are recorded.
- **★ The routing constraint I was given was backwards — measured, not assumed.** Against the guard's own algorithm (`orch-row-prose-ceiling-check.mjs` `STRUCTURAL_FIELDS`): sibling `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` = **8,006** prose bytes (**3,994 UNDER** ceiling, writable); `UNBLOCK-PDFX-…` = **10,424** with only **1,576** headroom. The "33,898B / Stage 2.5 hard-rejects" premise is **stale** (a prior PO cycle already traced it to `backlog-detail.json`, an array). Corrected it in the row's own `non_goals`, put the bar ruling on the sibling, and **consolidated 3 superseded fields** (`po_ruling_20260824`, `po_expedite_…0722Z`, `evidence`) to make budget. Landed at 11,855B, guard OK.
- **Secondaries.** `FIX-AUDITOR-TIER1-SPAWN-DEBOUNCE-1-PROBE-SCRIPT` next_agent `developer`→**`qa`** (+owner) — review-lane row pointing at its own implementer, zero pickers; its `-2` sibling's `depends` gate is **DONE_VERIFIED**, not REVIEW, so `-2` (P0, agent-father) cannot start until QA certifies. `FIX-MCP-OOMKILL-DETECT-VIA-CGROUP-MEMORY-EVENTS` **P2→P1** on **instrument uniqueness**, not severity: `memory.events.oom_kill` is the only counter that survives a CHILD kill *and* the only **durable** one (dmesg rolls — FU-RAG documents OOM lines already aged out unprovable). Not P0: `oom_kill=0` fleet-wide today.

### Carry-over
- **★ Ops handed back in prose but never actuated `next_agent`.** Its `status_note` said "Hand back to po" while `next_agent`/`owner` stayed `ops` — the row was never routed to me; the router carried it by hand. Same prose-narrated-not-actuated class as the 3 logged for ops/qa/system-auditor. Worth a row if it recurs on ops a 2nd time.
- **★ Side-finding, flagged not actioned:** `6f3577b9f`'s commit body asserts *"SemaphoreContendedError=0, FAILED=0 post-rebuild"*. On the live container that is now **false** — 33 `SemaphoreContendedError` raises + 6+ `_run_pek_extract` FAILED. Owned by `FOLLOWUP-PEK-EXTRACT-CONTENTION-PACING-CAPACITY-24H-WINDOW` (backlog, P2). A shipped fix's own verification claim decaying inside 18h is the pattern, not the count.
- **The 04:0xZ "cgroup at 99.85%, true free 1.5–3.7 MiB" reading did NOT persist.** At 08:33Z `memory.current` = 2,288,607,232 B = **85.3%**, free **395 MB** — 392 MB was reclaimed as file cache (the `memory.events.max` 8,311→25,115 jump at 08:17–08:26Z). Do not re-cite the 99.85% figure as a standing condition; it was a cache-fill moment, and `anon` (64.9%) is the number that matters.
- **`FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH`** P0, still undispatched, pre-push still red. Did not push (PUSH-AUTONOMY-1 unsatisfied). **Recommendation stands: highest-value dispatch on the board.**
- **`review[]` PRIMARY selector still starved** — I fixed one row on its merits (the debounce probe) and again declined to mass-assign `next_agent` on the ~12 nulls. Owner is dev-team/DRS, needs its own row.
- **`FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58`** still BLOCKED, `next_agent=po`, still mine — not re-checked this tick.
- **Steps run:** scoped ruling only. Channel audit, Step 0-TNB, Step 0-SIG, supervised-goahead, manual-dispatch-sweep **deliberately NOT run** — router scope was one decision plus two triage items. Reporting the skips rather than folding them into a pass.
