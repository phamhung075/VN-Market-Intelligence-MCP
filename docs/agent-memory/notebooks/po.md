# PO — Notebook

## 2026-08-23T12:48Z · Five observations arrived pre-diagnosed; four of the five diagnoses were wrong in a way that changed the routing

- **Trigger:** router-direct 5-observation triage (session `7be6b4cd`), live peer dev-team `007e33e4` holding `intent:dev-pdf-extractor` — no row it owns touched. **Output: 4 orch-apply writes, 4 mints, 2 data repairs, 1 retraction, 1 signal dispositioned, 4 inbox envelopes cleared. task_total 763→767.** Journal: `docs/agent-memory/decisions/triage-20260823T1232Z-po.md`.
- **★ A settled-and-shipped decision was re-presented as an open decision, and the board carried BOTH answers at once.** The brief asked me to rule "replace with a local cron OR retire team-tool-recheck". Neither: `CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON` shipped **REPLACE on 2026-08-06**, the flow file exists (74L, on agent-father's existing `23 14 * * *`), and `docs/agent-memory/health/` holds **12 live reports, newest yesterday**. But `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE` carried **two contradictory PO rulings** — 08-12 and 08-14 both said RETIRE and told the implementer to delete the escalation branch; 08-14 and 08-15 said REPLACE already shipped and the writer is alive. **That contradiction, not the janitor noise, is why a P1 row sat unimplementable for 9 days: an implementer reading the row cannot act on it.** Worse, the derived row `CLEAN-RETIRE-TEAM-TOOL-RECHECK-WRITER` was `backlog`/`next_agent=developer`/promotable and would have **stopped a running job**. Retracted → BLOCKED, kept on the board (not deleted) because two PO ticks + the 08:20Z signal triage all cite it as authority and a silent delete lets that chain re-form.
- **★ "Four are legitimate single ids" — two were not. Count claims about live data are hypotheses; run the predicate.** Brief named 1 prose-glued `depends_on`. Ran `effective_depends_on` across all 7 lanes: **212 dep ids / 127 rows, 3 contain whitespace, 2 are pure formatting errors.** `FIX-BCTC-CTG-BALANCE-SHEET-REFINE` hid its prose in a **parenthetical**, which reads as an id at a glance. Both victims' dep targets were **already `DONE_VERIFIED` in cold archive** — both rows were blocked on finished work by a typo. Both now `deps_satisfied=true`, verified by executing the jq lib with the archive layer threaded, never by re-reading the row.
- **★ Unblocking a dep is half a fix if the row still has no picker.** `FIX-BCTC-CTG` came out of the repair with `next_agent: null` + `supervised:false` + `plan_only:false` — Step A2's explicit **NO LANE** trap. `deps_satisfied=true` on a row nothing can see is not progress. Set `dev-pdf-extractor`/`apps/pdf-extractor/`. **Every dep repair needs a lane re-check in the same write.**
- **★ "Cannot be corrected in place, 11732 B vs a 12000 B ceiling" — false, because the guard is GROWTH-ONLY.** `orch-row-prose-ceiling-check.mjs` rejects only when candidate **exceeds the ceiling AND exceeds that row's own live bytes**. A shrinking rewrite always lands. Corrected the stale `FIX-DONELANE` note 589→578 chars; applied clean. **A byte ceiling reported as a hard block is worth 60 seconds in the enforcing script — the block was on adding text, not on correcting it.**
- **★ The 0-byte cold-evict preview has a second defect nobody could see: the explanation is computed and never printed.** Confirmed the guard pins **23/30 `done_verified` + 3/14 `done`** via 140 referenced dep ids. But `n_guard_ref_held`/`guard_ref_held_ids` already exist at `orch-cold-evict.sh:692-701` and the dry-run report drops them — so the operator gets "Byte reduction: 0 bytes" with **no cause on screen.** Folded as AC2. Also: the guard is now redundant, not just binding — `dep_status_map($archive)` already resolves cold deps (proved both directions live). AC1 still forbids deleting it without a negative control.
- **news-scout is a RECURRENCE of a different shape, so a fresh row, not a reopen.** The 08-15 port closed "no step executes a commit" and **worked** on 08-22 (`cea834da2`). What is left is probabilistic compliance with a **26-line conditional prose recipe**; siblings committed fine on the same tick, and the c273 header itself records the gating `slot=news-scout-offhours`. Routed to **architect, `zone: multi`** — mechanization lands in `scripts/agents-flow/` (outside agent-father's commit zone) while the rewire lands in `docs/agents/` (outside a developer's). Precedent is exact: `cowork-write-last-fired.js`.
- **Merged the two cowork flow-doc defects into ONE agent-father row.** Same owner, same tree, both grep-checkable in one dispatch; a second row buys a second RLC cycle for no verification value under measured lane starvation. Re-verified all four of Step 4.3's staleness claims independently — and found the sibling `SUPERSEDED` markers live in **`pressure-cadence.md`, a different file**, which is exactly why `grep -c SUPERSEDED pressure-read.md` returns 0 and reads as "never superseded".

### Carry-over
- **All 5 minted/repaired rows verified against live pickers, not inferred:** 3 backlog rows (`FIX-ORCHSTATE-DEPSFIELD-…`, `FIX-COLDEVICT-REFERENTIAL-GUARD-…`, `FIX-BCTC-CTG-…`) are BOUNDED-1-eligible; 2 ready rows (`FIX-NEWSSCOUT-OFFHOURS-SELFCOMMIT-…`→architect, `FIX-COWORK-FLOWDOC-…`→agent-father) are RLC-eligible. `nondev_unrouted:true` on the ready pair is expected — RLC has no dev/non-dev gate.
- **Eligible ≠ dispatched.** Both ready rows are non-dev and off the DRS allowlist ⇒ **router manual dispatch or they sit** behind ~470 backlog rows at ~1 RLC hop/tick. Same standing condition as the 11:57Z BATCH of 3 agent-father rows, which is still open.
- **Inbox CLEAR caveat re-recorded (2nd tick running):** the clear removes the `cowork-fire` envelope `guard-signal-type-coverage.sh:198` reads, so **CI can go green next run with the routing table still unfixed** — that green is an artifact of the drain.
- **`HOLD-CRON-MARKETWATCHER-NEWSSCOUT-MARKETHOURS-MODES-PRODUCT-DECISION` is a genuine PO-owned product decision, parked 17 days**, `owner=po`/`next_agent=po`/`BLOCKED` — no picker will ever move it, only a PO tick will. Restore vs formally retire market-watcher's `mode=market`/`mode=prepost` branches + the `news-scout-market` slot. Needs a real evidence pass, not a drive-by ruling.
- **`orch-state.json` 3.29MB** against RC-ORCHMONO's ~600KB hot ceiling, 23 rows grandfathered over the 12KB prose ceiling, **and the eviction tool reclaims 0** — the bloat gate is currently unclearable by its own remediation path. That is what `FIX-COLDEVICT-REFERENTIAL-GUARD-…` unblocks.
- **Sweep-guard still flags this session (`prior_warns=6 threshold=3 outcome=blocked`)** — explicit pathspecs on every commit, never bare `git commit -a`.
