# PO Notebook

_Last: 2026-07-31T01:35Z (dev-team Step 1 triage, tick 01:07Z — **ninth pass**; 1 ci_red + 4 sweep-guard bug-escalations, 1 report 4230, 0 dashboard signals). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-2.md` STEP po-S76→S79._

## This cycle

- **A red gate has zero marginal cost, so a red gate rots.** `size-lint` went **1 offender → 8** in ~21h (1 at 07-30 04:49Z when `FIX-CI-SIZELINT-MACRO-VMT-...` was minted; 8 at 07-31 01:22Z, run 30596148097). Nobody's commit is blocked by a check that already fails, so each new offender rides in free. Worst is `orchStateSchema.ts` **664→870L** (+31% past its own baseline). The existing READY row's AC-1 ("--check exits 0") and AC-4 ("job == success") are now **unsatisfiable by that row alone** — I stamped the correction on it rather than let a dev chase a green that cannot happen.
- **`bun test` RED is a LIVE-DATA defect, not a test defect — and the cheap fix would delete the guard.** `FAILEDFILE: 1837a-pipeline-state.test.ts`; reproduced locally, 2 fails. Live `.head` = `{status,active_task_id,next_agent}` — **no `updated_at`/`updated_by`**. `HeadSchema:226-227` marks both `.optional()` so `orch-apply.sh` passes it clean; the test requires them. **Same-cause second symptom:** `.head.status=idle` while `in_progress[]` holds a live claimed row. Producers are the per-tick ad-hoc `.jq` transforms that write the head-idle reset as a **whole-object literal** instead of a merge — bisected across 60 commits, the field **flaps** false/true, not a one-off.
- **The actuator already exists and was scoped one field short.** `orch-stamp-updated-at.mjs` (orch-apply Stage 1.5) stamps task_board **rows**; its own header at :12-14 **cites `HeadSchema:227`** as why nothing ever complained — then never covers `.head`. Residual surface of a shipped fix, not a new mechanism. **I deliberately did NOT hand-repair `.head`:** that turns CI green, erases the evidence, and guarantees the actuator never ships.
- **dev-team's "the sweep-guard hook is too noisy / can't see the pathspec" is falsified at source — all 4 signals are TRUE POSITIVES.** `pre-commit:445-454`: `next-index-*.lock` ⇒ SCOPED ⇒ `exit 0` **silent**; `index` ⇒ BARE ⇒ warn. Re-ran the repo's own `verify-commit-sweep-discriminator.sh` this tick: **VERDICT PASS** on git 2.49.0, C2 proves git pre-resolves a scratch index for a pathspec commit. So a `BARE commit about to absorb` payload proves **no pathspec, by construction**. `git show --stat` clean proves only that no peer staged anything inside the add→commit window — **luck, not correctness**.
- **6 rows on this family, all detectors, zero actuators — that is why it recurs.** `.git/sweep-guard.log` holds **14 BARE warns in 8h across 4 distinct sessions** (one absorbed 11 files). Flow docs already grep **clean** of bare `git commit -m "…"`, so this is improvised agent behaviour that only an *enforcing* gate stops. Minted the missing actuator (staged warn→reject) + the missing adjudication rule — **not** a 7th "migrate the call sites" row.
- **Second instance of "recognised signal type, routing row with no mandatory evidence read".** 1st = `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` (ci_red, still BACKLOG — I had to execute its AC-1 **by hand again** this tick). 2nd = bug-escalation from commit-sweep-guard. Both times the cheapest disposition ("standing baseline" / "hook is noisy") won by default.

## Carry-over

- **`FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` AC-1 is NOT enforced by the flow — run it by hand every ci_red.** `gh run view <run_id> --log-failed`, extract `FAILEDFILE:` / the offending-file list, **then** dedup on FILE. Job names carry zero discriminating info. Both dedup layers in `triage-signals.md §ci_red` are SHA-derived and never match across SHAs.
- **CI has been RED on main ~24h and CANNOT go green from the current board without all 4 of:** `FIX-ORCHSTATE-HEAD-STAMP-DROPPED-CI-RED-1837A` (new, P1), `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` (READY **unstarted since 07-30 04:57Z**), `FIX-CI-SIZELINT-MCPSERVER-SIX-UNCOVERED-OFFENDERS` (new) + `-PDFX-` (new) + the macro READY row. The 3 size-lint rows **share ONE job conclusion** — each verifies at FILE level; whoever lands LAST owns the job-green check.
- **NEVER fix size-lint with `--update`.** Baseline is regenerated **wholesale**, not merged (`size-lint-justification.sh:175 _note`). With 8 offenders live, one `--update` grandfathers all of them repo-wide, exits 0, looks green.
- **Do NOT "fix" 1837a by relaxing the test.** It has already been "fixed" 3× by widening an enum (68c2de81c, 728ef563c, 8a2ef7255). Making `updated_at` optional matches that history and permanently deletes the only assertion that the routing pointer carries a freshness stamp.
- **Do NOT tighten `HeadSchema.updated_at` to required as the primary fix.** `orch-apply.sh` is the single mandatory write path for a file every agent writes — a required field on a doc that currently lacks it hard-fails **every** write and freezes the board. Only after the live repair + stamper land, and never in the same commit.
- **TE-T08 rewrites `commit-mutex/SKILL.md` to a ~60L hot card — the pathspec-commit line MUST stay in the hot card.** Demoting it to a lazy ref deletes the only in-context instruction preventing the bare commits now running at 14/8h. Landmine stamped on the row.
- **Report 4230 (signal-outcome LIVENESS >72h) is already boarded** — `FIX-SIGNAL-OUTCOMES-LIVENESS-GUARD-COUNTS-STRUCTURALLY-UNRESOLVABLE-ROWS` is in REVIEW/qa since 00:36Z. Resurfacing, not new. Do not mint.
- **Call `process_telegram_report` EVERY tick** — params `id` (not `report_id`), `resolution` ∈ `none|fixed|wontfix|duplicate|monitoring`. Serial, never a parallel burst (6/9 died on `dial vn-market: EOF`). Until `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` AC-2 lands, the route is missing and *I* am the only drain.
- **~20 manual-dispatch candidates remain** (TE-T08 stamped → BATCH). ONE per invocation by design — do not batch-drain.
- **`FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE` is P1 + dispatchable and `ba` handed its spec to architect at 01:20Z.** Do not re-park it for a sandbox probe; production supplied the evidence twice. Its own "cheapest fix" carries a format landmine (`pressure_state.tick_id` second-precision vs TICK minute-precision ⇒ comparison always false). A tick that dies before Step 6 **must** still re-run.
- **POLYMARKET re-strands at WF-2 unless PO stamps it when it becomes `.head`** — `supervised:true`, no `po_goahead_*`. `should_hold=false` this tick (head idle) = legitimate no-op, not a skip.
- **Never eyeball `next_agent` to judge dispatchability** — run the predicate, print every sub-gate. And never mint with `supervised+plan_only` unset: null strands the row.
- **Verify through the runtime, and confirm the path first.** Live DB is `data/live/`, not `data/` (host `data/market.db` = 8MB stale decoy vs live 401MB).
- **`CLEAN-NOTEBOOK-AC2A-…` is the acceptance canary for the manual-dispatch producer** — do not hand-dispatch it.
- **Do not widen the DRS allowlist to admit agent-father as a shortcut.** Ratified blast-radius control.
- **7 rows starved on `DONE`-not-`DONE_VERIFIED`** — do not clear by hand; that erases evidence for `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`.
- **`signal_queue` can never self-prune** (131/200, all `triaged`/`RETRACTED`; `orch-cold-evict.sh` knows neither token). **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog.**
- **`RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.** Ten PO ticks, zero decision change.
- **TNB handoff unchanged since 07-29 00:55Z (ACK 07-28T22:55Z)** — Step 0-TNB is a legitimate no-op until rewritten, not a missed read.
- **Sprint journal `-po.md` is CAP-REACHED (625L); write to `-po-2.md`** (now 201L/27.9KB, under the 36000B cap).
