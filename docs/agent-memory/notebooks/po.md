# PO Notebook

_Last: 2026-07-30T18:09Z (sprint-goal-entries-overage, router-dispatched from claude-manager-helper context-janitor Pass 5) · `.sprint_goal.entries[]` 18→14 under cap 15, 4 SHIPPED visions closed to canonical terminal status and cold-evicted with evidence. Prior cycle (07-30T09:44Z drain-tick) superseded; its live carry-over is retained below._

## This cycle

- **Age is not completion, so I gated on orphan-safety + independent evidence instead.** Joining all 18 visions against every `task_board` lane left 5 with zero referencing rows. The 3-oldest shortcut would have closed `VN-MACRO-TOOLING` (06-14) — still `ACTIVE` in `task_board.active_sprints` with a live backlog row. Zero-row is necessary (no orphan risk) but never sufficient: empty is not evidence of shipped.
- **I closed 4 of the 5 and deliberately held `PREDICTION-CLAIMS-DAILY-CADENCE` OPEN.** Its mechanism DID ship — `daily-predict.md` exists and `digest-predict/flow/main.md:127` routes it at the `digest-daily` `30 17 * * *` slot, exactly the cadence the vision's `cron_spec` asked for. But its success_metric is LIVE consecutive-day `prediction_claims` rows, which nobody has measured. Retiring a goal on mechanism-exists is how passive health masks dead data.
- **Re-verified every candidate on a plane independent of the board before closing.** A `done_verified` commit proves a gate ran on some past tree, not that the artifact survives at HEAD. RAW this tick: `QueName.tsx:79-95` renders coreMeaning+stateInterpretation+favorable+warning (the vision's exact `product_decision`); live `curl :3001/dashboard/money-radar` → **302** → `/dashboard/momentum` **200** + `TopNav.tsx:98` single entry; `get_money_radar_composite` in tool-registry + 4 source sites; `doclang_serializer.py`+usecase+unit test wired via `main.py`/`handlers.py`, with its Phase-2 gate resolved **NO-GO** (`a50f6c982`) so no residual scope.
- **I broke with both prior PO precedents on HOW to close, on purpose.** 07-11 and 07-20 deleted the entries in the jq transform, leaving evidence only as `_note` prose. `orchStateSchema.ts` §11 states plainly that the drift growing this array past cap IS non-canonical terminal tokens the cold-evict predicate can never match. So: canonical `status:"DONE_VERIFIED"` + `closure_evidence` via `orch-apply.sh`, then `orch-cold-evict.sh` → `archive/2026-07.json .closed_sprint_goals[]`. Deleting would have paid the cap while leaving that machinery unexercised.
- **Baseline dry-run BEFORE the flip is what makes the blast radius provable.** cold-evict has no age gate and sweeps done/done_verified/signals/decision_journal in the same pass — a known orphaning class. Pre-flip preview read **0 in every category**; post-flip read **exactly 4 sprint_goal entries, 0 everywhere else**. Without the baseline I could only have asserted that.
- **Corroborates the prior cycle's dangling-sprint finding from a new angle.** All 4 closed visions had NO `active_sprints[]` key, so `po/flow/main.md:129`'s MUST-set-`DONE` sign-off path was structurally impossible for every one of them. The sprint_goal-layer close (canonical status → cold-evict) is the path that actually works today — it does not need `active_sprints[]` to exist.

## Carry-over

- **A one-slot margin is not headroom.** 14/15. The next PO mint re-breaches; the janitor will re-flag. The durable fix is the `active_sprints[]`-reconcile work already on the board, not another cap-triage tick.
- **`.claude/skills/decision-journal/SKILL.md:20` still resolves my sprint via `select(.status=="active") | tail -1` → `COWORK-GUARANTEED-SLOT-CATCHUP`, which is dangling.** Unchanged by this tick (I closed 4 entries; none were the tail). I wrote to the `triage-<TS>-po.md` convention instead. Sprint journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po.md` remains `CAP-REACHED`, rolled to `-po-2.md`.
- **Still live from 07-30T09:44Z, unsuperseded:** 34 of 40 referenced sprint ids dangle (in neither `active_sprints[]` nor `closed_sprints[]`) so `decision-journal-archive.sh` skips them forever — 513 journals / 5.5MB. `orch-validate.mjs` referential check MUST stay warn-mode until that reconcile lands, or it rejects every board write. `FIX-DECISION-JOURNAL-BYTECAP-NO-ACTUATOR` (P1) keeps only the trigger half — do not merge it with the registry row. `FIX-ALERT-COMMANDER-NO-BASH-GRANT-…` is stranded (BOUNDED1=false, SLS=false, next=agent-father), moves only by deliberate dispatch. `review[]` holds 5 supervised+plan_only rows; 41 rows are `supervised:true` + `plan_only!=true` (4 P0) — a SEPARATE class, do not conflate. The two 09:21Z mints are ungated on purpose. `RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.
- **I did not touch `.head`, any task lane, or WIP this tick.** Scope was `.sprint_goal` only; board flood (382 backlog / 172 review) is untouched and still the dominant constraint.
