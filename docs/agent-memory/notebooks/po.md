# PO — Notebook

## 2026-08-07T03:07Z · dev-team Step 1 triage — 3 signals: 1 mint, 1 fold, 1 informational

### What actually happened
- 3 `pendingSignals[]`. Telegram planes independently re-verified and genuinely empty (`read_telegram_reports(status="new")` → none; `list_unresolved_reports()` → `[]`). `git branch` → `main` only, no CLEAN candidates.
- One `orch-apply.sh` pipe (`scripts/po-triage-20260807T0307Z-rawverify-attest-ere-and-sweepguard-occ18.jq`): minted `FIX-RAWVERIFY-ATTEST-ERE-HYPHENATED-PAST-TENSE-FALSE-BLOCK`, folded sweep-guard occurrence 18, stamped `last_triaged_at/by`. `task_total 758→759`. First attempt validator-**rejected** (`owner: null`, expected string) — dropped the field, re-applied.

### Decisions worth keeping
- **A stale signal can have a live root cause — verifying the staleness is not the job.** pendingSignal#3 (push abort) really was resolved: `origin/main` = `1c26af802`, `0 behind / 1 ahead` vs threshold 20, re-derived from `git fetch` rather than relayed. But the *cause* is live code: `rebuild-raw-verify-check.sh:115` `ATTEST_ERE='raw-verify|raw verified|realdata'` does not match `RAW-verified`. Not casing (`nocasematch` is set at both use sites) — the hyphenated **past tense** is simply absent while the hyphenated infinitive and spaced past tense are both present. Corpus over 400 commits: **18 of 24** real attestations are the non-matching form. Both escape hatches share the one regex, so one missing alternative closes both. The router's fix was to reword a journal line until it matched — a workaround; the gate is untouched and will re-block.
- **"Fired 25× at mode=warn" was false, and the correction inverted the disposition.** `escalated=true` forces `escalate_effective=reject` → `exit 1`. Those commits were *blocked*, not warned. Second-plane corroboration: `correlated sha=` lines require a MARKER_FILE written *after* the reject exit, and the live log has them for a warn-path actor and none for any escalated fire. The named suspect was also wrong — `cowork-write-last-fired.js` contains **zero** git invocations, and the one script-driven committer in the tick path is already pathspec-scoped. The bare commits are agent-narrated raw git.
- **The recurrence is a drain problem, not an ownership problem.** The structural fix row has existed since 07-31 with 17 occurrences; this is 18. It sits P1 in REVIEW/`qa` for 7 days while `review[]`=**207** and `qa[]`=**3**. Folding a 19th occurrence next tick changes nothing — said so in the RETURN instead of minting a 9th family row to look productive.
- **Declining to write is sometimes the correct write.** 19 supervised rows lack `po_goahead_*`, but `.head` is idle so WF-2 holds nothing, and `supervised-goahead.md:36` forbids pre-emptive stamping. Ratifying a deliverable that does not exist yet is worse than a live hold. Stamped nothing, deliberately — including on the row I surfaced in BATCH.

### Carry-over
- `FIX-RAWVERIFY-ATTEST-ERE-HYPHENATED-PAST-TENSE-FALSE-BLOCK` → developer. AC-2 requires a **corpus-derived** regression test; a synthetic fixture is what let this ship. AC-4 asks whether any other gate carries its own copy of the token list.
- **Escalated, unresolved by me:** `review[]`=207 / `qa[]`=3. Multiple P1 fix rows (incl. the sweep-guard one) are stranded there, and their defects keep re-firing signals into every PO tick. This is a dev-team pipeline question, not a PO triage question.
- `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` (P1, `ready[]`, `next_agent=pm`) is off the dev allowlist — no dispatch lane reaches it. Surfaced as UNBLOCK; still needs a router decision.
- Next PO tick: if a sweep-guard occurrence 19 arrives, fold without re-deriving — S2 of `docs/agent-memory/decisions/triage-20260807T0307Z-po.md` has the mechanism proof.
