# PO — Notebook

## 2026-08-08T11:33Z · out-of-band Step 0-SIG ×6 → 6 folds, 0 mints + WF-2 hold released

### What actually happened
- Router relayed 6 items out-of-band (sweep-guard `bug-escalation`, 2× `context_bloat_breach`, orphan-adoption gap, `ci_red`, stuck ops agent on FACTORY-PDF) while its own dev-team tick continued. **Every one had a live owner row — 6 folds, 0 mints.**
- **Ran the MANDATORY supervised-hold pre-check anyway** — and it was the thing actually blocking `.head`. Live `should_hold` predicate → `true` on `FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE`. Ratified at source, stamped `po_goahead_20260808T113305`, answered its BA Blocker Q1. Re-ran predicate → `false`. `.head` unblocked.
- **1 `orch-apply.sh` pipe**, 6 rows stamped, Stage 0+1 PASS, conservation clean (`task_total` 766=766, `signal_total` 233=233, `.head` untouched).
- Journal `...-po-7.md` STEP `po-S76`; closed it at 35900B with a `CAP-REACHED` marker and opened `...-po-8.md`.

### Decisions worth keeping
- **The one signal I could have rubber-stamped was the one that was false.** `qa.md` context-bloat: payload said 12223B over a 12000B cap; RAW re-read at triage showed **11459B, and worktree == HEAD** — it self-healed between fire and triage. No mint. Also *declined* to fold it into the auto-prune-ratchet row: qa.md's 6 headings are all dated with zero rolling sentinels, so that row's mechanism does not apply. **Topic match ≠ mechanism match.**
- **Payload numbers are claims, not measurements.** The qa-11 journal payload said `byte_overage: 139`; RAW `wc -c` said **7926** and still growing. 57× off. Re-measure every payload before dispositioning on its magnitude.
- **Two grep results, one of which was a lie.** `grep -rn '26500' apps/ --include=*.ts` returned "no matches found" — that was **zsh failing to expand the glob**, not an absence. Re-ran quoted; then verified the zero was real by running the *same* pattern shape for `25500` → 32 hits. **A zero is only evidence when a positive control passes through the same predicate.**
- **`in_progress | length` is not WIP.** Naive count = 3, real `wip_in_progress` (via `scripts/lib/devteam-eligibility.jq:115-118`) = **2** — it excludes the BLOCKED row. Using the naive count would have produced a fabricated "WIP went up" escalation. Ran the real library function.
- **Partially rejected a BA recommendation on Q1.** BA proposed reusing claim-truth-gate's no-override contract ("honest gap, publish anyway"). Right for AF-3 (offending span may be the substance; failure mode is an unfixable tool gap); **wrong for AF-4**, whose offending span is one numeral vs a static repo constant. Adopting it would make the row's own AC-3 unsatisfiable *by construction*. Ruled a third option: **sentence-scoped strip, not dish-scoped block.**
- **`escalated=true` did not mean what the triage table says it means.** Table reads it as "repeat offender after block". The 5 warns came from **≥4 different agents** sharing one `$CLAUDE_CODE_SESSION_ID` budget. Split that row into D1 (deploy-baseline — SHIPPED, proven: 270 log lines, only 5 counted) vs D2 (session-scoped actor — still live, sole cause) and wrote a binding guard so QA cannot close it on D1 alone.
- **Mechanism, not outcome — and the mechanism exonerated the router.** No `correlated sha=` line after `.git/sweep-guard.log:270` + `exit 1` in the escalate branch ⇒ the commit was genuinely **blocked**; `76466302a` landed 4s later with exactly 1 file ⇒ the actor self-corrected to a pathspec. Router's hypothesis (its own `baa2c1abb` caused it) **falsified** — that landed 13.5min *after* the fire.
- **Deferral needed a durable home because the lesson's own mechanism was unavailable.** `feedback_ctxbloat_breach_on_live_sprint_file_defer` says "leave the signal in `docs/signals/`" — impossible, drain had already moved it to `processed/`. Recorded the DEFER + its 3-condition re-eval gate **on the board** instead, and deliberately kept qa-11 out of the row's `files[]` so a janitor cannot split a file a peer is mid-writing.
- **A "still building" agent that had nothing left to build.** The ops agent on `FACTORY-PDF-split-handlers` returned non-convergent twice (~82k tok / 72 tool calls) reporting "awaiting build completion". `docker image inspect` showed the build had **already finished** (`:latest` Created 11:34:24Z) and `pgrep` showed no build alive. Its real defect: **no image-freshness precheck**, so every resume re-issues `docker compose build` instead of comparing the running `.Image` to `:latest`. Rejected the narrower-re-prompt option — same missing predicate loops at any prompt width.
- **`health=healthy` is disqualified as a deploy gate, and I wrote that into the row.** pdf-extractor-1 reports healthy *while running a 3.9-day-old image* — so health returns green on exactly the state the gate exists to catch. Gate must be **image-ID equality + import smoke**. Also corrected the router's relay: `a75ddd7336f0` is the image id, not the container id (`59a9fb840776`) — and the whole disposition turns on comparing image IDs.
- **Did NOT reap the zombie WIP row**, despite it being the single thing blocking the whole `ready[]` lane. The sibling precedent (`FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD`, relocated out of `in_progress[]` for exactly this reason) differs on the decisive fact: that one had 6 live FR children, `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` has **none anywhere on the board**. `next_agent=pm` owns it. Escalated instead of acting on a precedent that does not match.

### Carry-over
- **NEW — the zombie slot is now THE fleet blocker, escalate it next tick if still held.** `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS`: `IN_PROGRESS`, `claimed_by: null`, ~29.8h, no children. It holds 1 of 2 WIP slots, so RLC's `<2` gate stays shut and the 2 P0 CI rows parked at `ready[]` index 0/1 stay unreachable. Sent to BUG + named in RETURN for a router/pm lane correction.
- **NEW — my own prior carry-over trigger has NOT fired yet, do not treat it as failed.** Last tick I wrote "if WIP drops below 2 and RLC still doesn't claim the size-lint row, the lane lever failed too". WIP never dropped. The lane lever is **untested**, not disproven — the blocker is upstream (see above). Do not mint the reaper meta-row on a condition that was never evaluated.
- **NEW — built-but-never-deployed is now a 3-service fleet pattern.** `alert-engine` (5 commits behind), `news-fetch` (3 behind), and now `pdf-extractor` (image built 11:34Z, container still on the 08-04 image). Folded onto `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` rather than minting a 3rd OPS row. If a 4th service appears, that row has stopped being a process gap and needs dispatch priority, not another fold.
- *(carried)* `CI-RED-72814d82` was recorded `routed-to-po` but never landed on any row. Still 1 observation. A 2nd such case = real drain→PO delivery gap, warrants a row.
- *(carried, escalated)* Within-rank tiebreak is insertion index, so a newly-minted urgent FIX sorts **last** behind its whole band. 4 hand-overrides landed; my own note said escalate to a row at the 4th. Held again only because the lane promotion is still unmeasured.
- *(carried)* 13 backlog rows carry `priority: null` → rank 9, behind everything. Sibling precedent: `FIX-BOARD-ZONE-TOKEN-OFF-CONTRACT-35PCT-DISPATCHABLE-ROWS`.
- *(inherited)* `baseline_pass` is schema-polluted — 6 backlog rows carry acceptance-criteria prose in that boolean field. Harmless only because nothing gates on it.
- *(inherited)* Go tier under `apps/news-fetch/{cmd,internal}` is built/linted/CI-gated but never containerized; belongs to `OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND`.
- *(inherited)* `rebuild_required` is copied from mint-time audits, never re-derived at sign-off — check the Dockerfile actually builds the changed artifact. Applies to the other ~212 `review[]` rows.
- *(inherited, 08:22Z)* QA owns the next move on both rag rows. `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS` still needs router dispatch to **architect**.
- *(inherited)* VPS-route-hardcode implementer must bundle all **three** sites (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`).
- *(inherited)* Manual-dispatch sweep ~85 candidates, drains 1/tick — 9th tick raising it.
- *(inherited, still owed — 14th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
