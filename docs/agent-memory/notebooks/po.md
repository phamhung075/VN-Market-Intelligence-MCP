# PO Notebook

_Last: 2026-07-29T12:33Z (Tier-1 probe PLANE divergence → 3rd false-pass) · 0 minted, 2 escalated P1→P0, 3 annotated, 8-cycle carry discharged. Merged over a peer-PO cycle committed 6 min earlier at `116fd8429` — its full text is there, live items compressed below._

## This cycle

- **Two probe artifacts, and every fix went to the one nobody's verdict comes from.** Tier-1 runs `scripts/agents-flow/auditor-tier1-probe.sh` (872L, per-container, `MEM_FLOOR_MIB=40`) as a cron pre-gate, and `docs/agents/system-auditor/probe.sh` (128L, `grep 'mcp-server'`, untouched since 07-23) as what `flow/main.md:174` orders the **agent** to run. Neither references the other; `rag`/`MEM_FLOOR`/`acked`/`HEADROOM` grep to **0** in the second.
- **The 07-25 fix did not fail — it landed in the other plane.** Minted off occurrence #1, shipped at `663fc451b`; #2 (qa, 07-28) and #3 (12:06Z today) reproduced the identical shape anyway. **"Recurring failed fix" and "fix that reached the wrong artifact" look the same from the board and want opposite remedies.**
- **I minted nothing.** qa found this **07-28T15:20:59Z** and wrote the remaining-gap spec down to `probe.sh:57-71`/`:74-95`/`tier1-probe.md:144-178`, routed `agent-father`. It sat 21h. A new row would have been the **4th** copy of this predicate — the exact mechanism `FIX-A21-PREDBOUND-2` exists to stop. **Check whether the row already exists before believing the board is blind.**
- **The stranding is documented, not a bug I found.** QA-Drain (`dev-team/flow/main.md:723`) claims **only** `next_agent=='qa'`; the non-qa subset is an explicit *"not-yet-covered owner-triage class"* routed to **the PO queue**. The consumer that never ran was me. `scripts/audits/devteam-review-lane-drain-report.sh` lists it — **run it every triage tick.**
- **Same class as the peer's finding, and that is the real lesson.** Peer found 61 backlog rows in **no sweep lane at all**; I found review[]-non-qa in no lane either. **Rows do not fail by being wrong, they fail by being unenumerated — and priority does not reach a row no sweeper lists.** Two independent instances in one hour.
- **Reading the verdict logic instead of assuming it changed the disposition.** `tier1-probe.md` clause 4 (`vmhwm_kb > vmrss_kb → PASS`) sits **between** the evidence (cl.3) and the severity map (cl.5). VmHWM is a monotone high-water mark ⇒ true for any process not at its lifetime peak ⇒ pre-empts the map *including the OOMKilled CRITICAL branch*. **Widening scope against it ships a detector that inspects 13 containers and still only returns PASS** — occurrence #4, delivered *by* the fix. → `blocked_by` + P1→P0.
- **The loop is mute, and mute is worse than noisy.** Pre-gate FAILURE → `:697` gates `_write_heartbeat` on an *empty* failures list → heartbeat frozen 11:11:55Z → cron spawns on **both** arms → agent runs the blind plane → HEALTHY, `signals_posted=0 | telegram_sent=0 | dashboard_rows=0`. ~48×/day, zero state change.
- **Refused the quiet fix.** Floor-exempt for rag / raise `MEM_FLOOR` / re-widen the ACK — all rejected; that recreates the blindness the floor row was minted to close, and would be the *second* silencing in this family. Cap raise is user-gated → `FU-RAG-DEPLOY-MEMORY`.
- **Held `FIX-A21-PREDBOUND-2` at P1 though its thesis was vindicated.** Corroboration raises confidence in the *argument*, not this row's live consequence (`crashRestarts:0` either way until a user-gated deploy). Sequenced after the P0 — same owner, same file, would collide.

## Carry-over

- **A-30 veto carry DISCHARGED after 8 cycles.** `FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE` → **P0** + `blocks[]`, BUG msg 4396. It kept losing to rows with a visible symptom **because a detector that silently returns PASS has no symptom by construction** — failure mode and invisibility share one cause. A telegram alone would have made it a 9th carry; the board dependency is what makes it unavoidable.
- **Both P0s are agent-plane work on `tier1-probe.md`** — veto (architect, design) first or with, scope port (agent-father) second, `FIX-A21-PREDBOUND-2` third. **Do not run concurrently**; three agents editing one flow doc is how this family got three copies.
- **Live numbers, do not re-derive from stale text:** rag-service **99.44% / 4.3 MiB free** (12:20:30Z, was 11.2 at 12:06Z). mcp-server **82.00%** — 3 points under `probe.sh:87`'s ≥85 deep-probe gate, so today even the one container PLANE B watches is waved through by clause 1. **Two points is not a rate** (`auditor-tier1-probe.sh:75-77`) — no trend claimed.
- **`docker exec` against rag-service is off the table** (~4 MiB headroom; an exec allocates in-cgroup and SIGKILLed it at 10:12Z today). `probe.sh:117-118` already execs *pdf-extractor* for A-20 — pre-existing, out of scope, **do not copy that onto rag**.
- **Zone fields on this family are stale.** Corrected `cross-service/`→`docs/agents/` on the scope row (qa had established the routing on the record); **declined** on the veto row (no such finding) and flagged in-row. Correcting a zone without a routing finding is guessing.
- **From the peer cycle, still live, unsuperseded** (detail in `116fd8429`): mcp-server deploy lane **stalled 3 days** — `container_vm_headroom_mb` absent from the running image, runtime-verified, ⇒ cowork permanently `max_parallel=2`; acceptance had been demonstrated by `docker cp`-ing a replica, **served path never changed**. `LAYER2` P0 sits in the 61-row unreachable hole. `alert-commander.md` has **no Bash grant** ⇒ cannot run commit-mutex, notebook dirty 3 cycles; covering row lists only news-scout + market-watcher. **Verify each deploy claim at the runtime plane — do not inherit date arithmetic as proof.**
- Also open: `FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` reproduced 3× in REVIEW — qa must check observed claim/release **order**. `FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY` cannot pass QA from source (probes the stale container). Premature signal eviction: **3rd occurrence on a NEW row** triggers the rule.
- **Authorship carry closed as structural — stop carrying it.** All agents commit as `report-analyzer <daihung.pham@gmail.com>`; that is the session identity, not a PO attribution bug. 16 cycles of carrying an unfixable item is its own waste.
- **Peer-PO concurrency is real and unguarded.** Two PO instances ran inside 6 minutes; the OVERWRITE contract assumes "single-session state" and that assumption was false. I merged rather than clobbered. **Re-read the notebook immediately before writing it.**
