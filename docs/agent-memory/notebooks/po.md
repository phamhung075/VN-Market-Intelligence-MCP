# PO Notebook

_Last: 2026-08-06T14:20Z (dev-team S3 triage — 0 mints, 6 rows annotated, 2 promoted P1, 1 stale title corrected). Prior PO cycle 13:55Z preserved at `HEAD~`._

## 2026-08-06T14:20Z · Every one of the four escalations pointed somewhere other than where it said

- **I recorded 30 lines of destroyed audit data, then refuted myself.** Extracting notebook sections "heading → next heading" shows `c58` with a 0-line body — the same shape as the c41 gutting. Reading the file top-to-bottom: nothing was lost. The new `c59` heading was inserted **between c58's heading and c58's body**, orphaning the heading and re-parenting the body. Section order at HEAD is now `c58, c59, c57, c56`. Both versions are in the row note so the wrong one cannot be inherited from a cache.
- **The defect's new cost is fabricated evidence, not lost evidence.** With c58's body under c59's heading, one section says both "awaiting 768m→1g cap raise" and "post-cap-raise to 1GiB". They belong to different cycles. I spent part of this tick adjudicating an auditor self-contradiction that does not exist. **A body-hash AC would be green on this commit** — so the mechanical check needs heading/body adjacency, not just bytes.
- **I measured each named section instead of accepting or dismissing the warn.** `c56` byte-identical across all 3 commits = false positive. `c58` fired twice for two *different* true positives: a one-field retroactive edit of its own `[OUTPUT-CONTRACT] signal_queue_rows_written 0→1` after the commit closed the cycle, and the mis-parenting. One 3-signal batch, 1 FP + 2 TPs of different sub-classes — that is the corpus-clean fence earning its keep. Did not mint: the owning row's MINT-THRESHOLD NOTE pre-answers this exact case.
- **I refused to rule on rag-service last tick for want of a `docker inspect`; the deciding number was already in the notebook.** The container is **using 992.6 MiB — which cannot fit under a 768 MiB cap.** Branch B: the cap raise is live and did not fix anything; rag re-saturated the larger cap within ~17 min of a clean start. The arithmetic beats the label, and the label ("post-cap-raise to 1GiB") is exactly what I'd refused to trust. **What was missing was reading the evidence, not collecting it.**
- **That turned a sequencing plan into a deadlock.** `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` waits for the cap raise because "against a saturated cap the before/after is unreadable". Cap raised → re-saturated in 17 min. The deferral condition is "wait until the service isn't saturated" and the service saturates whatever it is given. **Waiting cannot clear it.** Promoted P1. Surfaced only because orch-apply's Stage-1g report printed the rationale verbatim.
- **"4 occurrences today, recurring-bug policy → escalate" was inverted.** `E-3 rc=3 check_id=scheduler_locks-FAIL` — `scheduler_locks`+`FAIL` is a hard-coded *test fixture*; three fires landed in 4m01s (test cadence, not cron); rc=3 is the script's own documented **usage error**, not a write failure; and `_send_orphan_bug_telegram()` has no test-sink gate. Minting would have sent a developer after a stuck lock that probably isn't there. I did **not** prove the suite ran then, so the AC I added is the discriminator: a silenced-but-ambiguous channel satisfies the row's title and leaves the triage defect intact.
- **A stale phrase inside a TITLE outlives the state it described.** `FIX-SBV-FETCHER-ZERO-VALUE-EMIT` sat in REVIEW/qa titled "...fix NOT yet applied". I nearly escalated it as a false-green trap. Its own review note documents the fix landing 2026-07-28 — the parenthetical froze at zone-correction time. **No status-token check can reach a status claim living in a title.**
- **The validator caught my jq before it corrupted 5 rows.** `def patch($id; $f)` evaluates `$f` in the *caller's* context, so `. + {...}` applied to the document root and stripped `id`/`status`. Non-`$` param = lazily-evaluated filter. `orch-apply.sh` aborted with "live file untouched" — the wrapper is not ceremony.

### Carry-over
- **`FU-RAG-DEPLOY-MEMORY` sign-off guidance is now stricter, not looser:** sign off only the deploy codification + compose edit. Post-raise saturation is **measured**, not suspected — do not sign it off as "memory resolved", and do not authorise a second cap raise.
- **`FIX-AUDITOR-SBVFX-SLA-POSTMARKET-TOLERANCE` carries a scope fence:** 1934 min = 32h14m breaches even a correct once-daily threshold. Widening the tolerance would turn a genuinely day-stale feed green.
- **Unchanged from 13:55Z:** `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` (P1) and `AF-SEMBLE-INIT-DEF` (P3) still need MANUAL dispatch (`next_agent=agent-father` is off the DRS allowlist); `FIX-AGENT-GUIDE-SSOT-NO-AUTHORIZED-WRITER` (P2, architect) still blocks all doctrine work.
- **Board after this tick:** `task_total` 811 → 811 (0 mints — every input folded onto an existing owner). 6 rows annotated, 2 promoted to P1, 1 title corrected.
