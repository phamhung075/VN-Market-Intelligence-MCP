# PO Notebook

_Last: 2026-08-06T15:50Z (single-row sign-off via dev-team review-lane SECONDARY-drain — 1 DONE_VERIFIED, 1 residual mint, board 805→806). Prior PO cycle 14:53Z (dev-team S1 triage) preserved at `HEAD~`._

## 2026-08-06T15:50Z · A 16-day-old green is not a green

- **The row's own residual is what made re-running mandatory.** `FIX-DRAIN-TEST-HARNESS-ORCH-HELPER-COPY-LIST` arrived carrying an unusually strong 2026-07-21 evidence trail: developer's derived-helper fix (`2e5b325d2`) *plus* router's own independent 28/28 re-run. The temptation was to sign that off as-is. Wrong, and the row itself says why — `router_residual_partial_drift_not_caught` records that the fix is **regex-coupled to one invocation idiom** and its staleness guard throws only on a ZERO match. That bound is *time-dependent*: any later `orch-apply.sh` helper added via `$SCRIPT_DIR/`, a bare path, or a variable makes the derivation return N-1 of N and re-breaks the sandbox silently. A green measured before 16 days of drift is not evidence of a green today.
- **Re-running said more than the note could.** `node scripts/agents-flow/drain-signals.test.js` → **36/36 PASS, exit 0** (measured un-`&&`-chained). All 13 previously-dark assertions observed passing **by name** — 3 FIX-DRAIN-PAYLOADREF, 4 ENOBUFS, 6 unparseable-loud — not inferred from a total, which is the precise failure mode this row exists to prevent. The count grew 28→36 because two *later* commits (`980937380`, `56be7b77e`) stacked 8 new assertions on the un-darkened harness. So the fix has been carrying real, growing coverage for 16 days — a stronger claim than router could make on day one, and it is only available to a late verifier.
- **Drift check: the residual has not materialised.** `git log scripts/orch-apply.sh` → **zero commits** since `a5d079663` (which predates the fix). Fresh ground-truth scan: exactly 3 live helper invocations (`orch-validate.mjs` L136, `orch-stamp-updated-at.mjs` L159, `orch-conservation-check.mjs` L179); all 10 other `scripts/` mentions are comment lines. Derivation still exhaustive.
- **Router left the disposition open; I split it rather than choosing one wrong trade.** "ROUTER DID NOT MINT" was a deferral to PO, and both obvious answers were bad: holding a verified P0 hostage to a latent hypothetical, or letting the bound ride into a terminal lane nobody reads. Closed the row **and** minted `FIX-DRAIN-HARNESS-DERIVED-HELPERLIST-PARTIAL-DRIFT-BLIND` (P3/XS, `developer`, cross-service) whose AC-1 is a *second, deliberately dumber* comment-stripped scan cross-checked against the derived set, AC-2 requires proving both polarities against a fixture — explicitly not another guard nobody has seen fire, same class as `FIX-MARKETDB-JOURNALMODE-GUARD-SHIPPED-BUT-NEVER-ARMED`. Sign-off does not depend on it landing.
- **Schema taught me the terminal-row convention the hard way.** First `orch-apply.sh` attempt aborted at the validator: `done_verified[9].next_agent: expected string, received null`. The task row shape is `z.string().optional()` on that path — nullable is *not* accepted there. Correct terminal form is the **absent key** (13 of 32 done/done_verified/archive rows carry none), so `del(.next_agent)`, not `null`. Leaving the old `dev-team` value would have kept a closed row dispatch-shaped.
- **The board moved under me mid-write and that is fine.** `done_verified` read 9 at 15:46Z and 11 after my apply, not 10 — a peer landed `FIX-EXECTIER-HEADSYNC-BRANCHNULL-REVIEW-IDLE` at 15:49:45Z between my survey and my `jq` read. Conservation confirmed the shape (`task_total 805→806`, +1 mint, lane-move nets zero). Counted, not assumed.

### Carry-over
- **The new P3 becomes P1 the instant `orch-apply.sh` grows a 4th helper by any means.** Recorded on the row as `priority_note`. Anyone touching `scripts/orch-apply.sh` should read it first — that file has been frozen 16 days, which is exactly the condition that makes the next edit dangerous.
- **Still withheld from BATCH, still not forgotten** (from 14:53Z, unchanged this tick — this was a single-row dispatch, no triage run): `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` (P1) and `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0/READY since 08-05T16:51Z). Both auto-eligible for BOUNDED-1. **If either is still un-dispatched at the next full triage, the inaction is the finding** — escalate the loop, not the rows.
- **The WAL hold is spent.** Do not re-apply. Acceptance evidence for both WAL P0s = a fresh `verify-market-db-journal-mode.sh` run, never my verdict.
- **Unchanged debt:** `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` (P1/ready) still has `root_cause`+`ac` null — needs architect before it is real work. `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` (P1) and `AF-SEMBLE-INIT-DEF` (P3) still need MANUAL dispatch. 4 backlog rows still carry prose-in-`depends_on` (BCTC-CTG, VPS-SYSTEMD, VERIFY-VPS-SSH, RAG-COMPACTION) = permanent Stage-1g noise, still unswept.
- **The review lane is still 253 rows deep.** This sign-off was reachable only because `FIX-DEVTEAM-REVIEW-LANE-SECONDARY-DRAIN` finally shipped. One row per drain against 253 is not convergence — if the sweeper's throughput is one-at-a-time, that is the next thing to measure.
