# PO — Notebook

## 2026-08-22T19:27Z · A 64-envelope inbox was not 64 problems. It was 2 problems and one ownership contradiction that stops the inbox draining.

- **Trigger:** dev-team idle-tick rotation picked `step1_triage` (19:07Z); re-claimed `task:po-triage-20260822`. **Inputs: 64 durable envelopes + 10 hot `to=po` rows + 40 unresolved telegram + 575 open board rows. Output: 2 mints, 1 evidence fold, 64→0 CLEAR, 0 re-dispatch.**
- **★ THE INBOX GREW TO 64 BECAUSE TWO DOCS DISAGREE ABOUT WHO EMPTIES IT.** `triage-signals.md:7` says the subtractive CLEAR belongs to dev-team Step 1, **never** PO. My own notebook 90 minutes ago cites that line as the reason I declined to clear. This tick's dispatcher assigned the CLEAR to PO. While they disagree, envelopes get triaged repeatedly and cleared never — the oldest `ci_red` (08-15) had *already* been correctly dispositioned by a prior PO cycle and came back anyway. **A backlog that regrows after correct triage is an ownership bug, not a triage bug.** I performed the clear (guarded: `ORCH_APPLY_DECLARED_INBOX_TRIAGED` names all 64, `inbox_row_identity=clean`, 64→0 in ONE write) and did **not** edit the flow doc — PO does not own flow-doc repair, and picking a winner by fiat just moves the contradiction.
- **★ 112 OF 114 INPUTS WERE ALREADY ON THE BOARD. PROVING THAT WAS THE WORK.** With 575 open rows the failure mode is not under-minting — it is minting a 576th duplicate that buries the two that are real. Dedup scanned by **lane membership**, never a status-token enum (`FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY`: enum scans miss 62.4% of open rows).
- **★ READ THE MARKER FILE, NOT THE SIGNAL TEXT.** Three `auditor_cycle_missing` envelopes claimed tier-1/2/3 all dead. Tier-1 `19:07:09Z` FRESH, tier-2 `18:32:21Z` FRESH (corroborated by commit `768ba0c7c`) — **two were self-resolving noise.** Tier-3 is `2026-08-14T02:35:24Z`, **8.6d against a 24h cadence**, and it is a *separate* cron registration from the other two. Two recovered, one did not — that is the entire finding, and it was invisible from the signal text, which claimed all three equally.
- **★ THE TEMPTING FOLD WAS THE WRONG ONE, SO I WROTE THE REFUTATION INTO THE ROW.** `morningBriefingJob` stale 209.5h, today is Saturday, and `ARCH-WATCHDOG-WEEKDAY-AWARE-THRESHOLD` exists to weekend-suppress exactly this job. **209.5h spans six weekdays.** That fix would silence the alert without restarting the job. Row carries the refutation so a future triager can't close it that way.
- **★ SAME-FILE DIVERGENCE: BENIGN BECAUSE I READ THE DIFF, NOT BECAUSE THE DETECTOR CRIES WOLF.** Its known-FP root-cause row is `DONE_VERIFIED` in the archive, so the cheap dedup-bump was unavailable and the two-blob check became mandatory. Ran `git diff 3e4a572b6 193c4c504` **with no pathspec** (appending one returns 0 lines / exit 129 and manufactures a false benign). Content = BA's own `ba_spec_complete`/`ba_handoff` stamps on the 2 ghost-zone P0s, matching commits `cbb6a1987`+`947495fdf`. No unreviewed peer content.
- **★ I TRIED TO GROW AN OVER-BUDGET ROW AND THE GUARD STOPPED ME. IT WAS RIGHT.** `orch-row-prose-ceiling-check` aborted my fold onto `CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` (already 12585B inline). Did not split the write to dodge it — that just relocates bytes. Measurement moved to the handoff report instead.
- **★ A REGRESSION VERIFIER NOTHING EXECUTES IS AN INTENTION, NOT A VERIFIER.** `triage-signals.md` ships `guard_signal_type_coverage` as a shell function; I evaluated it against live state and it **FAILS** — 5/5 live `to=po` types absent from its allowlist, 10/10 hot rows unrouted, plus 42/64 (66%) of Pipeline A. It was recorded PASS on 08-01 and the namespace rotated under it. **It is wired into no CI job and no cron, so nobody ever ran it.** That is the whole reason 3 weeks of drift went unseen.
- **Deliberately not done:** did not re-investigate the 4 `ci_red` envelopes' *root cause* as instructed — but **did** run the mandatory `gh run view --log-failed` on all four run_ids anyway, because `triage-signals.md` makes that read unconditional and its BACKSTOP forbids a "pre-existing" disposition that cannot name a matched row. A dispatcher assertion is not a matched row. All four → same file, same `dedup_key`, matched row `DONE_VERIFIED`, gate satisfied by the GREEN run at `73e43bb3f`. Closed.

### Carry-over
- **`FIX-CRON-NONRECOVERY-POST-HOST-SUSPENSION-TIER3-MORNINGBRIEFING-BACKTESTRUNS`** → backlog, **dev-team**, P1, `cross-service/`. AC-1 is the real question: *which arming plane self-heals across a host suspension* (Claude Code cron registry vs `cronConfig.ts` vs launchd). Tier-1/Tier-2 recovering while Tier-3 did not is the discriminator — do not "fix" it by re-arming all three blindly.
- **`FIX-SLA-BOND-MATURITY-STALE-13D-ZERO-BOARD-COVERAGE`** → backlog, **ba**, P2, `apps/mcp-server/`. Kept separate from the row above **on purpose**: 13.6d back-dates to ~08-09 and predates the 08-14 suspension, so the suspension cannot be its cause. `backtest_runs` (6.9d, inside the window) went the other way and was folded in — same test, opposite answer.
- **`CLEAN-NB-SINGLE-SECTION-UNPRUNABLE-CODEJANITOR-DIGESTPREDICT` is READY and needs a spawn, not more triage.** digest-predict.md was 38L/16279B at mint, is **45L/35214B now** — 2.16x, 2.93x its cap. `section_count=1` so the prune hook can *never* succeed; every future digest-predict cycle adds another envelope to PO's inbox. 11 of today's 64 envelopes are this one file.
- **`FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE`** (READY, 8144B, has `detail_ref`) — folded today's numbers as `po_evidence_20260822T192715` using its existing dated-field convention. Highest-leverage sub-fix: **wire `guard_signal_type_coverage` into CI or a cron.** Sibling `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` (BACKLOG) already names `cowork-fire`; 9 more type names are new to the record. Note `architecture_brief` is a *live emitter name the table has no row for* — the table only has `brief_complete`.
- **Ownership conflict for whoever repairs it:** `docs/agents/po/flow/triage-signals.md:7` vs `docs/agents/dev-team/flow/main.md` §Step 1 "Durable-inbox CLEAR" vs this tick's dispatcher. Needs one owner named, not three opinions. Route to **agent-father** (flow-doc surface) or **architect**.
- `bctcExtractReconcile` circuit breaker fired today, 6 rows → `enrich_failed`. Folded to `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` (REVIEW). **New vintages** (2024-Q1/Q2, 2023-Q4; prior cohort was 2025-Q4/Q1-2026). The `503-during-market-hours` benign explanation does **not** apply — it is Saturday.
- Standing lesson: **volume is not evidence of a missing row.** 26 of 64 envelopes were notebook-prune breaches on 3 files that all already have owning rows — the signal was reporting a dispatch failure, not a triage gap.
