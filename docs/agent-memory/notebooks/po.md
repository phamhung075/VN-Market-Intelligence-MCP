# PO Notebook

_Last: 2026-08-06T13:55Z (agent-father keep-sweep triage — 4 mints, 5 rows annotated, 1 of my own mints withdrawn). Prior PO cycle 12:45Z preserved at `HEAD~`._

## 2026-08-06T13:55Z · A check that passes against the wrong document is worth less than one that fails

- **The escalation's own fix would have left the alarm ringing.** agent-father proposed one edit to `microservice-main.md` to close 8 agents. Correct edit, incomplete fix: Check #2 greps only `<id>/flow/main.md`, so a shared-file edit still reports 8 FAILs tomorrow. **The probe has to learn the delegation hop, or the fix lands in the pre-gate and not in the thing that actually runs.** Added as AC-2 — it also mechanizes the 9 cowork false positives agent-father re-verifies BY HAND every single cycle.
- **The 2 agents that PASSED were the more interesting finding.** dev-mcp-server and dev-frontend satisfy the grep by pointing at `cowork-error-boundary/SKILL.md` — which omits the `.head` idle-reset entirely and prescribes `call_tool(server="vn-market", ...)` + `get_recent_fixes` dedup. **All 11 dev-\* agents hold `Read, Edit, Write, Glob, Grep, Bash` and no gateway grant.** A green check against a contract the agent cannot execute is a false green wearing a clean shirt.
- **Same shape, three times, one day → widen an existing row, don't mint a fourth.** `microservice-main.md` L90-94 orders a `task_heartbeat` call after every Python TDD loop (unexecutable, so its stolen-lock branch is dead prose); the two cowork-skill pointers; agent-father's own commit-mutex gap. Measured the class: **27 agents lack the gateway grant, 25 of them own docs prescribing MCP calls, 40 candidate docs** — recorded as an upper bound with a classification rule, never as a defect count. `FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK` had the right method written on it in a PO note from 2026-07-15 and nobody ran it outside cowork. All dev-\* are partition (a) — they have Bash, so the existing bypass works; that is the cheap half.
- **Read the script before accepting a diagnosis about the script.** Relay said the DASHBOARD "Telegram sent" text was copy-pasted fixed text. It is not — `emit-dashboard-row.sh:207` already branches on `$telegram_sent`. The defect is **a fail-OPEN default (`="yes"`, L130) plus zero callers passing `--telegram-sent` anywhere in the repo**, against a flow that mandates a row *even on SKIP-dedup*. The relayed fix would have sent a developer to an already-correct layer. Every dedup-skipped row since 2026-07-29 is contaminated.
- **That same grep refuted a guard-rail already sitting on another row.** `FIX-AUDITOR-B12-DOUBLE-INVOKE-EMIT-MARKER-LOSS` tells its future implementer the flag "is fed by invocation 1's OK outcome" and **DO NOT treat the DASHBOARD row as the lying artifact**. Two hits repo-wide, both inside the script itself. Corrected in place — a wrong premise parked in a row's guard-rails aims every future reader at the wrong layer, and it outlives whoever wrote it.
- **I did the exact thing I had just flagged, twice in one tick.** My prior-art probe was truncated by `head -20`; I missed a four-row rag-service cluster and minted a duplicate. Then I repeated a relayed premise ("1GiB cap raise already applied, still insufficient") without checking git — **the raise committed at 13:00:35Z (`2f835ec63`), AFTER two of the three WARNs.** Withdrew the row and both annotations, left stubs so the retracted claims cannot be inherited from a cache. **A truncated probe is not a probe, and a premise arriving inside an escalation is not evidence.**
- **Refused to resolve rag-service either way, deliberately.** Two WARNs post-date the commit; whether the container was ever recreated is undetermined and decides everything — 97.06% of 768MiB is 745MiB, of 1024MiB is 994MiB. One `docker inspect` (HostConfig.Memory + StartedAt) splits deploy-gap from escalation. **Infra probing is outside PO's boundary, so I wrote the branch table and the command instead of guessing.** Also: the A-30 discriminator is now on record erring in BOTH directions on this one container — over-counting dips (08-05 crash-cliff) and missing a real 109MiB no-restart reclamation (08-06 12:52Z). Corroborating evidence at best, never a sole basis.

### Carry-over
- **`FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` (P1) and `AF-SEMBLE-INIT-DEF` (P3) both need MANUAL dispatch** — `next_agent=agent-father` is deliberately OFF the DRS allowlist (fleet blast radius). They join ~12 TE-T\* rows already queued on that lane; that queue is itself a starvation signal worth a look.
- **`FU-RAG-DEPLOY-MEMORY` must NOT be signed off as "memory resolved."** Sign off only what it delivered (deploy codification + the compose edit). Run the `docker inspect` branch check first; if branch B, `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` becomes the primary remedy rather than a follow-up, and a second cap raise is the wrong move.
- **`FIX-AGENT-GUIDE-SSOT-NO-AUTHORIZED-WRITER` (P2, architect) blocks all doctrine work.** Until it lands, every guide carve-out gets parked in enforcement files and doctrine keeps drifting from the checks.
- **Not re-triaged (already handed off):** the 3 CRITICAL Bash-grant-vs-description mismatches (alert-commander/market-watcher/news-scout, origin `610110e16`) — `docs/signals/2026-08-06-chore-team-tool-recheck-livescope-handoff.json`.
- **Board after this tick:** `task_total` 808→811 (4 minted, 1 withdrawn); 5 rows annotated; 3 previously-stranded rows given a `next_agent`.
