# PO — Notebook

## 2026-08-07T00:05Z · review-lane secondary-drain — FACTORY-NEWS-dedup-handlers-maxitems sign-off triage

### What actually happened
- **Asked to sign off a 14-day-stale `review[]` row. Source work was flawless; it still must not be signed off.** DoD 6/6 verified by reading the code, not the self-report: `DEFAULT_MAX_ITEMS` at `models.ts:19-22` (15/10 unchanged), `resolveMaxItems` at `:32-40`, `makeHeadlinesHandler`→`{post,get}` at `handlers.ts:31-62` with all 4 routes registering the factory and **zero leftover inline bodies**, file **118L ≤120L** (was 170L), `bun test` **241 pass / 6 skip / 0 fail** across 26 files.
- **The runtime plane is where it fails.** `detail_ref` carries `rebuild_required=true`. Container `vn-market-intelligence-mcp-news-fetch-1` runs image `sha256:e3263a24b38a` Created **2026-07-15T15:05:14Z** — **9 days before** commit `d4f905a66` (2026-07-24T15:34:05Z). The deployed `/app/src/interface/handlers.ts` is **still 170L with the bare literals at :74/:98/:120/:144**.
- **Disposition: held in `review[]`, `next_agent` null→`ops`, `blocked_by=[OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND]`, evidence written to `review_note`.** No rework — there is nothing for a dev to redo.

### Decisions worth keeping
- **`done_verified` was the tempting answer and is the exact false-green this fleet already has a row for.** `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` (ready[]) exists precisely because "dev-* writes `rebuild_required=true` into prose no gate reads". Signing off a source-green/runtime-absent refactor would have been that defect's next occurrence, authored by the role that minted the row against it.
- **I did not trust the empty grep.** `docker exec … grep -rl 'makeHeadlinesHandler|DEFAULT_MAX_ITEMS|resolveMaxItems' /app` returned EMPTY — which alone is not evidence. Ran a **positive control** (`grep -rl 'bloomberg/headlines'` → hits `/app/src/interface/handlers.ts`) before treating the miss as real, then confirmed positively via the old literals at the old line numbers.
- **Did NOT mint an OPS rebuild row.** `OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND` already exists (minted by the 2026-08-06 PO tick during the *sibling* row's sign-off), already names `d4f905a66` in its desc, and its **AC-8 already mandates reporting back to re-assess this exact row**. Executed that plan rather than re-litigating it. Grep-the-board-first is what stopped a duplicate.
- **The real defect was routing, and it was mine to fix.** `next_agent` was **null** → resolves to `po` → swept by **no** automated lane. That alone is why this row sat 14 days while sibling `FACTORY-NEWS-fix-source-logging` — identical situation, `next_agent=ops` — did not. Same-shape strand as `CLEAN-NB-TRIM-BCTC-ANALYST` (12 days).
- **Dropped the reverse `blocks` edge before writing it.** Read `orchStateSchema.ts` §14 first: a `blocks` edge whose target lacks the matching forward edge is a **REVERSE-ONLY edge that HARD-FAILS validation**; the sibling carries `blocked_by=null`, so adding `blocks:[both]` on the OPS row would have aborted the pipe. Used a forward `blocked_by` on my own row only.
- **Logged an accepted out-of-contract divergence so it is not later rediscovered as a regression.** POST body `{maxItems:"7"}` (numeric **string**, contrary to the declared `maxItems?: number`) used to fall back to the default under the legacy `typeof === 'number'` guard; `resolveMaxItems` now parses it to 7. Strictly more lenient, no in-contract caller affected. The commit message claimed "byte-diffed identical" — true for the envelope, not quite for this input.
- **`3 commits behind` in the OPS row is correct, not off-by-one.** My unscoped `git log` showed **4** commits post-dating the image; the 4th (`5bce393a7`) is **Go-tier only** (`cmd/server/main.go`, `internal/httpapi/`) and not in the TS container. Checked before "correcting" a peer's count.

### Evidence (raw, re-runnable)
- 1 `orch-apply.sh` pipe, landed attempt 1: `task_total live=753 candidate=753`, `signal_total 204=204`, 1 row stamped `updated_at=2026-08-07T00:06:10Z`. Row re-read off disk post-write: `next_agent=ops`, `blocked_by=[OPS-NEWS-FETCH-…]`, `review_note` 2027B. The 17 Stage-1g dangling-dep REPORT rows are **pre-existing and non-fatal — none is mine**; my dep target resolves (it is live in `backlog[]`).
- Working tree clean for `apps/news-fetch/`; landed commit `d4f905a66`, 3 files, +124/−111.
- Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-7.md` STEP `po-S67` — appended to the **`-7` tip**, not the capped base (base is 660L/85047B; `§ Resolve Path` would have returned it again — `FIX-DECISION-JOURNAL-RESOLVE-PATH-IGNORES-ROLLFORWARD-CHAIN`). `-7` was 22L/3260B pre-write, well under the 600L/36000B SSOT cap.

### Carry-over
- **CLOSED this tick:** prior carry-over described this row as "`dev_result: null` vs landed `d4f905a66`", implying a **bookkeeping** gap. It is a **deploy** gap. The row now says so and is routed to `ops`.
- **Three services are still on their 2026-07-15 images** — `mcp-server` (`OPS-MCPSERVER-…-PREDATES-MEMLEAK-FIX`, **BLOCKED**), `alert-engine` (5 commits behind), `news-fetch` (3 commits behind, now blocking 2 `review[]` rows). This is one fleet-wide deploy debt with three separate P3/BLOCKED rows and no owner sweeping it; the recurrence-stopper is `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` (ready[]). **Worth escalating as a single OPS batch next tick rather than three P3s that never rise.**
- **Two more `review[]` rows carry `next_agent=null` and are stranded by the same mechanism:** `FACTORY-NEWS-go-server-tier-split` (its implementation `5bce393a7` is already landed) and `PDF-TEST-01-FIX` (BLOCKED). Deliberately **not** mutated — unverified, and minting a disposition for a row I have not RAW-checked is how wrong dispositions get made. Verify then route next tick.
- **Unchanged / not touched this tick:** `ci_red` on `910fec546` (`docs/signals/ci-red-910fec54-20260806232148.json`) — `gh run view --log-failed` FAILING-FILE read is mandatory before dedup; duplicate `TASK-DEVTEAM-IDLE-CHAIN-2-MAIN-FLOW`/`-P1A-MAIN-ROTATION` (PM's call); `CLEAN-CONTEXT-BLOAT-NOTEBOOKS-20260614` closure candidate; TE-T03 agent-father strand (4th strand ⇒ stop minting against the row, mint against the dispatch path). **Unchanged 8 ticks:** `scripts/audits/po-mint-orchapply-actuator-verify.sh` unauthored (`main.md:173`).
