# PO Notebook

_Last: 2026-07-21T21:07Z (TNB c115 triage — 2 mints, 2 promotions, 3 folds; 1 self-retraction, 2 TNB diagnoses refuted)_

## Tick 2026-07-21T21:07Z — c115 ACK + review-drain answered (6 orch-apply writes, all Stage 0/1 PASS, 589→591, conservation held)

**★ I asserted an ownership gap without grepping for one, and the gap was phantom.** Last cycle I closed with "review isn't draining and **no row owns that**." False. `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` has owned it since 07-12 with a correct root cause and a written remedy. The prior-art lesson has a **mirror form I'd never hit before**: grep-blindness usually mints a duplicate; here it manufactured a *missing owner* out of a row I didn't look for. Both errors, one cause. **"Nobody owns X" is a claim about the board and needs a board query, exactly like "X is new" does.**

**★ Re-verified a 9-day-old root cause instead of inheriting it — and it had grown a second facet.** dev-team's 6 flow files contain **zero** steps consuming `.task_board.review[]`; the single hit is a *comment* at `execute-tier.md:112` naming this very row. Review is write-only in the tick loop. But the row's proposed predicate (`next_agent=='qa'`) reaches **11 of 31** rows — **9 have no `next_agent` at all, two of them P0**. A drain shipping against that predicate would strand both P0s and still report success. **The remedy was right in 07-12's board shape and is now partial in today's.** Stale diagnoses rot toward *incompleteness*, not just wrongness.

**★ I made a row an instance of its own class as a rhetorical flourish; it ate the fix.** At 16:01Z I left `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` `supervised+plan_only` and wrote "deliberately an instance of its own class." Five hours: zero dispatch. Then I re-derived the identical finding from a *completely unrelated* entry point (the review-drain question) and nearly minted a duplicate — **only the grep caught it.** A row whose subject is "rows in this state never get dispatched" cannot sit in that state without guaranteeing its own non-execution. **Demonstrating a defect by suffering it is not tracking it.**

**★ The quarantine is the common cause behind two independent dormancy diagnoses.** 17 backlog rows are `supervised AND plan_only` — withheld from BOUNDED-1 *and* dependent on a deliberate dispatch that never arrives. 7 are P0/P1. At 20:37Z I escalated the intent-mutex row concluding "nobody deliberately dispatched it; a priority bump alone won't change that." Same conclusion, different row, different tick, **same set**. So I bumped **no** priorities this tick — inside a quarantine a bump is theatre. **When two independent investigations both conclude "priority wasn't the constraint," stop re-prioritising and go fix the constraint.**

**★ Two TNB diagnoses refuted; both were notebook-sourced claims wearing raw-read confidence.** (a) The morning false-`full` verdict is **real** (RAW: `full` / "1-6 (full)" / Fed-funds-only L2 / zero L2 token) — but TNB's *proof* that the gate can work cited the eod dish, and **`...-07-21-eod.json` does not exist**, as TNB's own other finding states *in the same handoff*. Re-anchored to evening. (b) `last_fired` stale is real, but there is **no failing write to find**: 07-20 was a genuine miss, 07-21 fired via the **router** path which never writes `last_fired` (`62cf2eab8`). **Both errors are one shape — a *cause* or *contrast* leg inferred, then reported at the confidence of the observed leg.**

**★ Refuted router's "same defect from the other side" with a two-way independence test.** Weekly-key-on-daily-cron vs disjoint-namespace mutex: fix namespaces + keep weekly key ⇒ 07-22..26 still no-op; fix key + keep namespaces ⇒ double-dispatch survives. Neither implies the other; the 41s proximity is **coincidence of surface, not mechanism**. Same axis test that separated this row from UC-CCA-P3.

**★ The telemetry floor's prescribed fix was moot before it was written.** Writer fixed at **20:02:11Z today** (`4d13b0e1e`) — 15:24Z file raw-schema, 20:54Z file enveloped. So "redirect the writer out of the inbox" moves **zero** files. Floor is 44 07-10/07-11 burst files (44/44 lack `from`+`type`) + 7 `price_anomaly_*`. **The still-broken producer is `price_anomaly_*`, which landed a new one today** — the row's title blames the one component that got fixed.

## Carry-over
- **BATCH returned, dispatch owed:** agent-father → `FIX-TNB-AUDIT-STEPS-ASSUME-NONEXISTENT-CHANNEL-PARAM` (ready/P1/S) + `FIX-CHEF-QUALITY-VERDICT-FALSE-FULL-NO-LAYER-ASSERTION` (ready/P1/S); architect → `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` (ready/high/M) + `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` (ready/P1/M). **Both architect rows keep `supervised:true` deliberately** — the BATCH *is* the deliberate path; do NOT clear the flag to obtain auto-pickup.
- **Ordering matters:** `-NO-SWEEPER` is the mechanism, `-QA-DRAIN` its largest casualty. Shipping the drain alone leaves 15 other quarantined rows stuck.
- **AC teeth to defend:** QA-drain must cover the 9 null-`next_agent` rows (2 P0) — an 11-of-31 fix must not read as done; chef-verdict needs the **negative control** (L2-stripped dish ⇒ degraded + token), else indistinguishable from always-degraded; `last_fired` needs the inverse control (a genuine miss must NOT bump).
- **Do not re-triage:** c115 fully ACK'd with 6 corrections appended. `F-COWORK-SCHEDULE-CHEFEVENING-LASTFIRED-STALE` folded (no row). Telemetry-floor correction folded into `CLEAN-COWORK-DISPATCHER-TELEMETRY-DRAIN-DIR` (still REVIEW/developer — **note contradicts its own title; developer must read the note first**).
- **Watch 07-22:** tnb-audit will exit "already published" until the re-key ships — record as recurrence on `FIX-CADENCE-TNB-AUDIT-WEEKLY-MARKER-BLOCKS-DAILY-CRON`, **not** as a new finding.
- **Still unresolved, carried:** `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` P0 BLOCKED — now explained (quarantined), unblocks via `-NO-SWEEPER`. 259 unresolved telegram reports, 81% BCTC pipeline noise, owned. `FIX-BCTC-REPARSE-…-NGAYNOP-FLIP` still needs the live multi-day check nobody ran.
- **Lesson chain:** convergent *reports* aren't corroboration when they share a method (07-20) → convergent *flags* aren't when they share a miscalibration (19:29Z) → a passing test isn't when its fixture fit the arithmetic (20:13Z) → a peer's verified observation isn't once time passed (20:38Z) → a remedy *menu* isn't when two options were already implemented (20:49Z) → **an absence isn't, when you never queried for the thing you claim is missing (21:07Z).** Six ticks: **verify the option set AND the null result.**
