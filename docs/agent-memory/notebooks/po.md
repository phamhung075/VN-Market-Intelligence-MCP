# PO — Notebook

## 2026-08-11T16:56Z · A 93-deep BUG channel was 2 defects and 2 unknowns; the noise was the finding

- dev-team Step 1 triage (`step1_triage` rotation). Inputs: 18 pendingSignals, 5 Pipeline-B `to=po` rows, **93 unresolved telegram reports (4591-4683)**. Output: **BATCH of 4**. 6 `orch-apply.sh` pipes all PASS, `task_total` 771→774 (3 mints), conservation clean. Journal: `docs/agent-memory/decisions/triage-20260811T1656Z-po.md`.
- **Telegram plane cleared 93 → 0**, verified by re-calling `list_unresolved_reports()` → `[]`. 87 `duplicate`, 5 `monitoring`, 1 `fixed`. Pipeline-B 5/5 closed to `triaged` with dispositions; NEW/READ now 0.
- Pre-checks: WF-2 `should_hold` not evaluated (`.head` idle, not `in_progress`). Sweep: 82 candidates (44 DRS, 37 backlog-XOR, 1 ready-XOR, 4 re-admitted). TNB unchanged since the 14:36Z ACK — no new findings.

### Decisions worth keeping
- **★ 91 OF 93 REPORTS WERE TWO ALREADY-TRACKED DEFECTS. THE DEDUP RATIO *IS* THE HEADLINE — 97.8%.** ~39 reports (42% of the channel) are one row: `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`. I proved a **third** consecutive identical daily replay (08-09, 08-10, 08-11) — same 12 tickers, byte-identical boundary-date counts every day (FRT 46v10, VND 115v26, DIG 143v28…). Identical counts across days means the acquisition layer re-resolves the *same* document for the same key on every pass. A second ~25% is RECONCILE EXHAUSTED, whose **own circuit breaker (id 4674) explicitly says "do NOT triage these rows one-by-one"** — I obeyed the pipeline's own escalation instead of minting 25 rows.
- **★ THE ALERT NAMED THREE CAUSES AND ALL THREE WERE FALSE — I ONLY FOUND THE REAL ONE BY PROBING.** `[sbv-omo-liquidity-cron] macro-indicators /liquidity-state unreachable — Check macro-indicators service / VPS`, 2 consecutive days at the *same wall-clock second*. Live: container **Up 12 days (healthy)**; `/health` **200 in 2.6ms**; `GET /liquidity-state` **405** — route registered. So not down, not VPS, not missing. Real cause: `POST /liquidity-state` **hangs 18.4s** vs `deadlineMs = 15_000` (`sbvOmoLiquidityCronJob.ts:63`). Every fire deadlines out. **`sbv_omo_daily` has silently accrued nothing for ≥2 days behind green health checks.** Minted `FIX-MACRO-LIQUIDITY-STATE-HANDLER-EXCEEDS-CRON-15S-DEADLINE`, and wrote **do NOT just raise the deadline** onto the row — that hides an unbounded handler behind a bigger number.
- **★ "ENOENT" ON A FILE THAT IS PRESENT, TRACKED AND UNIGNORED — AND A NEGATIVE CONTROL IN THE SAME BATCH.** news-scout KLFL-aborted on `.claude/skills/step-0-cowork/SKILL.md — ENOENT`. File is 6364B, `git ls-files` tracked, `check-ignore` clean. Cost is real: newest notebook entry is c249 12:07Z, **there is no c250** — a whole cycle lost. What makes it a defect and not an outage: the 16:09:37Z fire dispatched 4 slots and **market-watcher loads the same path and completed at 16:11**. Filed as SPIKE, not FIX — I do not know the mechanism. Wrote the three competing hypotheses on the row and required one be ruled out: **news-scout is the only `haiku` agent in that batch**, so "the agent mis-narrated a different failure as ENOENT" must be actively excluded before anyone touches the filesystem path. Grants are *not* the answer — `Read` is explicitly granted, so this is not the `limitation=structural` class.
- **★ REFUTED A LIVE CRITICAL RATHER THAN INHERITING IT.** Three reports + a signal row said rag-service **99.53%, 5MiB free, imminent OOM**. `docker stats` this tick: **76.91%** — recovered unaided. And `RestartCount=9` is cumulative: `StartedAt=01:00:43Z` means **no restart in ~16h**; the "9 restarts over 15h" framing reads a counter as a rate. Meanwhile **pdf-extractor is the real one** — 94.90% → **95.31%** eight minutes later, sustained and rising. Recorded rag-service as the **positive** reclamation control and pdf-extractor as the **negative** on `FIX-AUDITOR-A30-SUSTAINED-WINDOW-…`, because that row's whole purpose is telling those two apart and the A-30 direction has been misscored before.
- **★ TWO ci_red SIGNALS, ZERO NEW ROWS — BECAUSE I READ THE RUNS, NOT THE JOB NAMES.** `failing_jobs` said `bun test, size-lint` on both, which carries no information. `gh run view --log-failed` on **both** runs gave byte-identical `=== FAILED FILES (2) ===`, and all three files already carry exact `dedup_key` rows. Appended the observed-SHA ledger instead. Caught a drift worth more than the dedup: `FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-**237L**` — transport.ts is now **265L** against unchanged baseline=126L/upper=138L. **The title figure is stale; the overage nearly doubled while the row sat in BACKLOG.**
- **★ THE SWEEP-GUARD ESCALATION IS ACCELERATING, NOT STEADY.** 4 `bug-escalation`, all `escalated=true` = repeat-offender-after-block. Row minted at **13** strikes; worst counter now **31**, threshold still 3, still `mode=warn`. Two distinct actors offending concurrently — one of them **this session**. Occurrence 28→32. P0 is understated.
- **Guard drift, folded not minted:** `guard_signal_type_coverage` FAIL rc=1, unrouted grew **3 → 4** (`bctc_image_fetch_degraded` is new and emitted by live mcp-server code, so it recurs). Also noted the guard scans *every* `to=po` row regardless of status — my 5 rows are all `triaged` with NEW/READ at 0 and it is still RED, so "add one type" will not turn it green.

### Post-commit: the first GENUINE same-file divergence (10/10 priors were FPs)
- My own commit tripped `[sweep-guard] SAME-FILE DIVERGENCE` on `orch-state.json`, and **this one was real**. Inspected per brief §2.7 two-blob form (no pathspec): 43 lines of a peer's *uncommitted* work — qa/architect's `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` closeout + a `next_agent` pm→developer flip — written between my `git add` and my `git commit`, absorbed by pathspec `--only` working-tree semantics.
- **No data lost in either direction**, verified against HEAD: my 3 mints all present, the peer's `completed_at` and `next_agent=developer` also present, worktree clean. Cost is attribution only — the peer's work sits under my commit message.
- **Disposition: log, no mint.** All three `FIX-SWEEPGUARD-SAMEFILE-*` rows are `DONE_VERIFIED`/archived, and `…-HUNK-PATHSPEC-ONLY-SEMANTICS-NONGOAL-AND-DETECTOR` already root-caused exactly this as a permanent NON-GOAL (git has no per-hunk attribution). Minting here would re-open a closed non-goal.
- **★ Two shipped fixes confirmed working live, incidentally:** the hook printed the corrected inspect command *with* its "do not append `-- <path>`" warning, and it ran clean (43 lines, exit 0) — that is `FIX-SWEEPGUARD-SAMEFILE-INSPECT-CMD-GIT-USAGE-ERROR-MANUFACTURES-FALSE-BENIGN` doing its job. Previously this command was a git usage error that returned 0 lines and manufactured a false "benign".

### Carry-over / NEXT
- **A peer replaced that P0 row's long two-plane evidence `status_note` with a 4-line COMPLETED summary.** Not my sweep — their orch-apply write. Recoverable from git (parent of `7bb97d8b8`) if the outage forensics are ever needed again.
- **Do not re-triage the BCTC period-mismatch or RECONCILE clusters report-by-report.** Both have a single owning row and the reconcile pipeline emits its own circuit breaker forbidding per-row work. If the channel refills overnight with the same 12 tickers, that is *confirmation* of the tracked P0, not new signal.
- **`SPIKE-NEWSSCOUT-KLFL-…` must not be closed by re-creating the file.** The file exists. Closing it that way would fake a fix.
- **Watch pdf-extractor, not rag-service.** rag-service recovered on its own; pdf-extractor is the one drifting up (94.90→95.31% within this tick).

## 2026-08-11T17:36Z · The "dead pipeline" was 99 healthy report_ids and one class of shell that can never extract

- Review-lane SECONDARY-drain sign-off on `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (the lane PRIMARY QA-Drain never picks up — it only takes `next_agent=="qa"`). 1 `orch-apply.sh` pipe, 2 rows stamped, `task_total` 771→771 conserved, `.head` untouched. Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-8.md` STEP po-S82.
- **★ THE HANDOFF ASKED "CLOSE ON THIS EVIDENCE OR SEND TO QA" — BOTH OPTIONS WERE UNSAFE AS POSED.** The evidence was dev-pdf-extractor's 2026-07-30 live verification, **12 days stale**, and a peer PO had ruled 99 minutes earlier (`po_proof_refuted_20260811T1554`) that this exact pipeline was a **live systemic incident**. Closing on the relay = textbook false-green; routing to qa = re-running one query with no diff to regress. I took neither and **re-probed at source before ruling**. Generalisable: when a dispatch brief hands you a verdict and a peer ruling that contradict each other, the answer is never to pick one — it is that neither has been checked this tick.
- **★ THE PROBE INVERTED THE PICTURE.** `bctc_layout_units` **1245 → 5855 rows** across 7 distinct production days since 07-30, `MAX(extracted_at)=2026-08-11 11:52:30`, and 2 queue rows in `pek_triggered` at 17:05:42Z. That is **12 days of unattended production** — strictly stronger than the single hand-fired DPM row the close was offered on. All three ACs (semaphore / reachability / firewall) retired **by outcome**: an unreachable host cannot produce 4610 landed rows.
- **★ THE DISCRIMINATING QUERY, AND WHY THE 15:54Z "FLEET-WIDE DORMANCY" READ WAS WRONG.** `SELECT COUNT(DISTINCT report_id) … WHERE report_id LIKE 'fallback-%'` → **0, across all history**. Non-fallback since 08-01 → **99**. The producer is healthy; the RECONCILE-EXHAUSTED flood is confined to structurally-unextractable **shell** report_ids, exactly as `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` claims and as the same row's own 12:37Z fold suspected (14 of 16 were shells). **Per-report_id zero-counts cannot be summed into "the producer is dead"** — same trap class as a composite score masking a pruned-table detector, which the 15:54Z note itself had invoked.
- **★ THE BULK-RECOVERY QUESTION HAD A DEAD PREMISE — I REJECTED BOTH OFFERED OWNERS.** `enrich_failed` fell **128 → 56 with no intervention**, so terminal rows *do* auto-recover. That refutes the 2026-07-17 `close_caveat` AND dev-pdf-extractor's residual (2) outright; Part B needs no owner, it is self-satisfying and must be re-scored against the live count, not the 07-17 snapshot. The real residual is the **opposite** defect and I confirmed it live: **14 rows above their own cap of 8** (HUT 2024-Q2 at 11 passes) — unbounded retry burning pdf-extractor capacity on rows that can never succeed. That is `bctcExtractReconcileJob.ts` = mcp-server zone, so `next_agent` ops→**dev-mcp-server**.
- **Deliberately NOT done:** SPIKE left `REVIEW` (cap defect is a genuine unmet residual, not a sign-off), **no `po_goahead` stamped** (row is `supervised:true` and its remediation plan does not exist yet — `supervised-goahead.md` Step 1-2 forbid ratifying an absent deliverable), memory plane left on `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM`.
- **Two schema/proc traps worth keeping:** `next_agent: null` is **rejected** by the validator on a terminal row — the "null" precedents in `done_verified[]` are rows *missing the key*, so `del(.next_agent)`, never `= null`. And `bun:sqlite` readonly **exits 0 with every query failed** (`no such column`) — I only caught it because each query printed its own error; a bare `bun -e` here would have manufactured a clean-looking empty result.

### Carry-over / NEXT
- **Do not re-open the BCTC layout pipeline as an outage.** It is producing. If RECONCILE EXHAUSTED refills the BUG channel overnight, check the report_id prefix first: `fallback-*` = the shell row, already owned, **not** a producer regression.
- **The cap-not-enforced defect is now double-confirmed** (12:37Z reports plane + my 17:30Z DB probe, 14 rows over cap). If `dev-mcp-server` proposes only "raise the cap", that is the same anti-pattern as the liquidity-deadline row — the fix is to stop retrying shells at all.
