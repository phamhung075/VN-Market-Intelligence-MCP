# PO — Notebook

## 2026-08-12T13:10Z · the relay said "new CRITICAL"; the hook's own sentinel made the ONLY dated section look oldest

- Router-relay triage of the agent-father registry-cron finding (notebook-auto-prune dropped a live 53L section). **0 new FIX rows, 1 fold + escalation, 1 containment mint.** `orch-apply.sh` clean (task_total 753→754, signal_total 98→98, row-identity clean).
- **★ I REPRODUCED THE SELECTION BEFORE ACCEPTING THE NARRATIVE, AND IT IS WORSE THAN "UNFAIR ACCOUNTING".** Sourced `lib/notebook-section-direction.sh` against the live committed `agent-father.md`: keys = `{3:SENTINEL, 58:SENTINEL, 120:20260812032352000}`. `nso_ts_key` needs a `YYYY-MM-DD` token, so `## Keep (maintenance) 12:58` parses as **untimestamped → MAX sentinel**. The one dated `## EDIT` heading is therefore the **only non-sentinel key**, hence `MIN_KEY`, hence the drop candidate — **and it was the newest content in the file.** `TIE_COUNT=1`, so the direction-aware tie-break at L463-509 never even runs. **Deterministic, not probabilistic.**
- **★ THE SENTINEL IS A CORRECT RULE THAT INVERTS AT SAMPLE SIZE ONE.** "Untimestamped rolling headings must never look oldest" is right in isolation. But **one real timestamp is not an ordering** — ranked against MAX-sentinels it manufactures a false "oldest". Any notebook mixing dated + undated `## ` headings is armed. Wrote that as AC-5 in the general form (`|non-sentinel keys| == 1` AND ≥1 sentinel ⇒ that section MUST NOT be the victim), not as "agent-father.md".
- **★ NOT A NEW TICKET — AND THREE PLAUSIBLE PARENTS WERE THE WRONG ONES.** `FIX-NOTEBOOK-AUTOPRUNE-REGEX-HEADING-MISMATCH` is literally titled "drops NEWEST not oldest" and `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH` already names agent-father.md — **both ruled out by the repro**: every heading is a well-formed `## ` and parses fine. Real owner is `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE`, whose `root_cause` already names `NSO_SENTINEL_KEY` and whose `.files` already lists both scripts. The script header tracks **6 prior FIX-***; a 7th sibling fragments a defect that has one fix site.
- **★ THE SILENCE IS A SEPARATE DEFECT AND I GAVE IT ITS OWN AC.** The hook wrote no signal and exited 0 believing it acted correctly. **All 5 occurrences were caught by an agent happening to notice**, and every recovery ran `git show HEAD:` on *uncommitted* work — that escape hatch **fails closed the moment the losing write is a cycle's first write.** AC-6: dropping the section holding the MAX non-sentinel key MUST emit a signal before the atomic write, independently of whether the selection fix lands.
- **★ P0 ON A NOTEBOOK HOOK, DELIBERATELY.** 5 drops / 3 notebooks / 6 days, and `agent-father.md` is committed at **172L / 15376B against a 12000B cap** — armed to repeat on its next write (post-drop state 119L/10164B is *under* cap, so the loop stops after destroying exactly the one section that mattered). Silent deterministic loss on the fleet's own memory substrate is the P0 bar.
- **★ THE LANE MOVE IS LATENCY-NEGATIVE ON ITS OWN — I MEASURED MY OWN ROW'S INDEX.** Moved backlog→ready, and it landed at **index 83 of 85, behind 29 older P0s**; `ready[]` is append-only and the consumer tiebreaks by array index (po-S2 @10:02Z). So the **BATCH is the actuator**, the lane move is durable reachability. Did not hand-reorder `ready[]` — full-doc mutation class, and `FIX-READYLANE-NO-SEVERITY-EXPEDITE-FIFO-BURIES-INCIDENT-P0` already owns it.
- Containment split by **commit zone, not by convenience**: `scripts/agents-flow/` is outside agent-father's zone (why it correctly refused to patch), but `docs/agent-memory/` is its zone → `CLEAN-NB-AGENT-FATHER-MIXED-HEADING-OVERCAP-DISARM` (ready[], P1, next_agent=agent-father). Verified the containment is *safe to land*: the drop loop recounts both axes and `break`s **before** any prune (L390-395), so one under-cap Write never trips the hook.

### Carry-over (13:10Z)
- **A trim-only split will REGRESS — AC-2 is the load-bearing half.** Dating every `## ` heading (`grep -c '^## '` must equal `grep -cE '^## .*[0-9]{4}-[0-9]{2}-[0-9]{2}'`) is what actually empties the sentinel population. My own 2026-08-07T05:13Z scope-correction already proved trim-only regresses on code-janitor/digest-predict.
- **Do not let the hook fix be graded on the existing 8/8 suite.** AC-5's fixture must FAIL against HEAD first. And `nso_ts_key`/`NSO_SENTINEL_KEY` are shared with `scripts/notebook-compose.sh` (AC-7) — changing sentinel semantics has a second caller.
- **Disabling the hook fleet-wide was considered and rejected**: it is the only actuator for the 200L/12000B caps; removing it trades silent over-drop for silent unbounded growth. If anyone proposes it as the "fast fix", that is the counter-argument.
- Board pressure after this tick: `backlog[]` **392**, `ready[]` **85** (30 P0), `review[]` **134**, `qa[]` 7, `in_progress[]` 1. `review[]` at 134 flat for 5 ticks.

## 2026-08-12T13:05Z · the router said "same change 3x"; I read the three scripts and one of them has no choke point at all

- Directed sprint kickoff from a user "yes" (instrument the cron preflight scripts so silent-tick cost is measured, not estimated). Minted 1 `sprint_goal` entry + 1 BA row `BA-TICK-PREFLIGHT-USAGE-INSTRUMENTATION` (SPRINT-M, P2, `cross-service/`, next_agent `ba`), two separate `orch-apply.sh` pipes. Conservation clean (task_total 752→753, signal_total 98→98). Umbrella lock `task:TICK-PREFLIGHT-USAGE-INSTRUMENTATION` claimed:true.
- **★ THE FRAMING I WAS HANDED WAS FALSE, AND ONLY READING SOURCE SHOWED IT.** Router offered 1 WU vs 3, calling 3 "the same change 3x". `cowork-tick-preflight.sh:76-82` and `dev-team-tick-preflight.sh:200-203` each funnel every verdict through one `_emit_verdict()` — mechanical. `auditor-tier1-probe.sh` has **no choke point**: inline `jq -n` at 757/767/772/858/894/921, and it is **dual-mode** — `run_tiered_probe()` calls `run_probe()` then re-emits a wrapper with a *different field set*. A per-call-site hook **double-logs every tier-2/3 tick in two incompatible shapes** — it would corrupt the exact dataset the sprint exists to produce. Answer: **4 rows** (shared lib → cowork → dev-team → auditor gated), not 1 and not 3.
- **★ THE DENOMINATOR WAS WRONG TOO: THE FAMILY IS 6.** `code-janitor-tick-preflight.sh`, `db-integrity-probe.sh`, `orch-sentinel-lite-probe.sh` share the identical verdict-JSON-on-stdout shape and the same silent-tick economics. Did **not** widen — tripling exposure before the shared lib survives one live cron is how a measurement sprint becomes an outage. Written into `scope_out` (d) as the named follow-up so it can't quietly vanish.
- **★ "CHECK BEFORE INVENTING A RATIO" — IT ALREADY EXISTS.** 4 chars/token, `2026-05-21-token-toolcall-economy.md` §W-1, applied in `2026-07-12-token-economy-lazyload-audit.md`. Also killed the brief's `python3 json.dumps()` suggestion: all four scripts already hard-depend on `jq` and use `jq -nc --arg` as their quote-safe emitter. Adding python3 to four cron-critical paths = new failure mode, zero gain.
- **★ THE SCRIPT'S OWN HEADER VETOED A FIELD I WAS ABOUT TO SPEC.** cowork lines 56-57: `CLAUDE_CODE_SESSION_ID` is "never echoed/logged … beyond its use as a bound `task_claim` arg". Session correlation would have been genuinely useful and I dropped it anyway — a stated contract in the file you are editing outranks a nice-to-have.
- **★ THE NUMBER THIS SPRINT PRODUCES IS A LOWER BOUND, AND THAT IS AN AC (AC-11), NOT A FOOTNOTE.** `verdict_bytes` measures only the tool_result; real tick cost also includes the cron prompt text + flow-doc lines loaded. **Shipping a precise-looking number with an unstated denominator is worse than the estimate it replaces** — that is how ~250-500 tok/tick becomes fleet lore.

### Carry-over (13:05Z)
- **`.head` left `idle` on purpose.** Router dispatches `ba` straight from this RETURN; pinning head to a row about to be picked up re-creates the stale-pinned-head class the router itself cleaned at 12:52Z today. The mint *did* reach the board — conservation 752→753 is the proof, not my say-so.
- **Do not let anyone accept the quoted suite counts.** Brief says cowork 20/20, dev-team 37/37, auditor 32/32. Carried into AC-10 as **UNVERIFIED** — QA must run them pre-edit. Inheriting a quoted baseline is how a regression gets graded green.
- **Architect owes three answers, I did not decide them:** Q1 log destination + git-tracked vs ignored (PO preference: `docs/data/telemetry/tick-usage.jsonl` **git-ignored**). Q2 one shared JSONL vs per-script (rotation fairness). Q3 WU-3's choke-point extraction shape.
