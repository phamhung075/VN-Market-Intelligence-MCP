# PO Notebook

_Last: 2026-07-31T14:35Z (dev-team triage tick). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-3.md` STEP po-S88, po-S89, po-S90._

## This cycle

- **2 mints, 1 fold, 2 dedups.** Board 359→361 backlog, review 224, WIP 0.
- **The cowork session-id gap is real and I verified it at source, not off the relay.** `spawn-fanout.md:288` `ENTRY_PROMPT = IDENTITY_PREAMBLE + slot.trigger_prompt` and `:292` (legacy) both compose zero session identifier; `IDENTITY_PREAMBLE` (`:240-253`) has none either. The consumer, `refine_bctc_md/flow/main.md:23-34`, hard-requires it as a spawn-prompt literal and EXITs before claiming. **Producer-side, one composition line.**
- **The fix is already precedented — this is not a design question.** `CLAUDE.md` step 3 mandates "pass `$CLAUDE_CODE_SESSION_ID` in spawn prompt as coordination parameter" for router-dispatched agents; this very PO spawn carries `coordination_session=`. spawn-fanout just never adopted it. So: FIX, not SPIKE.
- **Two-plane corroboration is what cleared the recurring bar.** `signal_queue` row `cow-20260731T140900` (HIGH) and telegram 4238 fired in the same minute from different writers. Hit 1 (07-30 slot-4) threw a raw schema error; hit 2 (07-31 slot-2) EXITed cleanly via the guard added after hit 1 — **the symptom changed, the root cause did not.** Slot-2 is unrecoverable (`cowork-write-last-fired.js` is monotonic-forward-only); next fire 2026-08-01T14:00Z.
- **`cow-20260731T140900` was NOT a lost write.** It is absent from hot `.signal_queue.rows[]` because the 14:18Z drain marked it READ and evicted it to `docs/data/orch/archive/2026-07.json` — found it there. Empty read ≠ evidence.
- **PO's own signal routing table matches 0 of its 16 live types.** 128 hot rows to=po: `signal_feedback` 51, `microservice_degraded` 29, `data_stale` 20, `db_integrity_breach` 6, `system-issue` 4, `routing-gap` 4, +10 more. `triage-signals.md` keys on a disjoint set. **100% fall-through to "unknown type → log and skip"** — today's HIGH escalation only survived because the router also relayed a telegram.
- **Reports 4236/4237 are NOT a resolver stall.** mcp-server image `sha256:95ad7ae7ef39` built **2026-07-30T22:06:34Z**; fix commit `314e70718` authored **2026-07-31T00:34:15Z** — 2h28m *after*. Pre-fix code is what is running. Two firings 1h apart = the documented module-level `_lastStalledAlertDate` re-arming on a container restart, already an accepted tradeoff on the owning row.

## Carry-over

- **`rebuild_required` is now 11 rows (was 9).** `FIX-MCP-SERVER-DEPLOY-LANE-STALL-REBUILD-REQUIRED-INERT` is the highest-leverage row on the board: every "fix landed, still firing" report resolves into it. Flagged 00:29Z, still BACKLOG 14h later — folded into this tick's BATCH. **Rebuild mcp-server ONLY, then verify at the RUNTIME plane, never image metadata.**
- **manual-dispatch-sweep has the defect it was built to cure.** Step 1's `po_manual_dispatch_flagged_at` guard excludes flagged rows from the candidate list, but Step 3 only folds the row Step 2 just stamped — so **a flagged row whose BATCH never dispatched can never be re-surfaced**. 2 rows are stranded this way now (deploy-lane, TE-T12). Deliberately did NOT stamp a 3rd (TE-T14) this tick: flag rate is not the binding constraint, dispatch is. 39 unflagged candidates remain.
- **Cold-evict pruned a 9-minute-old READ row.** `cow-20260731T140900` written 14:09Z, evicted 14:18Z, while the documented PRUNE criterion is "READ/RESOLVED/SUPERSEDED **AND ts older than 24h**". Known lesson (`feedback_coldevict_no_age_gate_orphans_unread_po_escalation`). **No mint** — no loss occurred and review[] already holds ~10 cold-evict rows; adding an 11th is churn. Revisit if a row is ever evicted while still NEW.
- **`FIX-TASKCLAIM-OWNER-CLIENT-SESSION-MISSING-FLEET-FLOW-DOCS` sits in review[] with `next_agent: po`.** Its AC5/AC6 are open (no MCP grant to live-test). Its deferred residual is now owned by the new spawn-fanout row — when that ships, AC6's refine_bctc_md half can close.
- **`FIX-NOTEBOOK-AUTOPRUNE-...-ZERO-TS-NOTEBOOKS` still gates two REVIEW rows** (`...LINE-ONLY-SETPOINT-BYTE-CAP-NEVER-CONVERGES`, `...LINECAP-SWEEP-BYTE-BLIND-BACKSTOP`). **Do not let QA merge the three.** `po.md` is one of the 11.
- **`daily_ohlcv.updated_at` is a MUTATION timestamp, not arrival** — nightly backfill rewrites ~97% of rows. Never diagnose coverage off it.
- **`gh run view <run_id> --log-failed` remains the only disposition-grade read for a ci_red.** `failing_jobs[].name` carries zero file identity. **NEVER `--update`.**
- **Not run this tick:** channel-audit (router relayed `read_telegram_reports`/`list_unresolved_reports` — do NOT re-run, it burns the `new` status). TNB: `tnb-audit-latest.md` already ACKed 2026-07-28T22:55Z, no newer handoff. supervised-goahead: `.head` idle, nothing held.
