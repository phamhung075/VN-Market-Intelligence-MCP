# PO — Notebook

## 2026-08-24T01:12Z · Two "missing audit cycle" alarms were the detector going blind, not the auditor stopping

- **Trigger:** dev-team tick `2026-08-24T00:37Z`, Step 1 PO Triage. Inbox **9 envelopes**, 5 fresh. **Output: 2 orch-apply writes + 1 hand-edit — 1 mint, 4 folds, 1 in-place correction, 6 sign-offs, 2 Pipeline-B closures, CLEAR of 5 of 9.** `task_total` 806→807. head idle throughout, never written.
- **★ Refuted the instruction I was given, on measurement.** The brief said the tier-1 `auditor_cycle_missing` was "a real coverage hole, not noise". The auditor ran anyway: c116 completed **00:30Z** (`system-auditor.md:3/:5`, commit `3dc3ee791`) — **11 min before** the signal fired; tier-2 completed 00:49Z (`4145766ac`), **8 min after** its own alarm; unbroken tier-1 chain 21:30/22:30/23:16/23:30/00:30Z. Neither cycle was missing.
- **★ Root cause is order-dependence, and I replayed the function to prove it.** `_t1_latest_notebook_ts()` (`auditor-durability-sweep.sh:217-238`) keeps whichever tier-1 section came **last in file order** — it never takes a max. Compose writes newest-first (`1e34634d9` stamps `direction=newest_first`), so "last" = **oldest**. Replayed verbatim on the live notebook: returns `2026-08-23T21:03Z` (c114, line 242) while c116/00:41Z sits at **line 3**. Reported staleness *grows* as history is retained. Separately, the tier-2/3 arm (`:304-325`) is heartbeat-only with **no notebook fallback at all**, and `_write_heartbeat` is gated on zero failures — so it freezes for the whole duration of any real degradation. **The detector is anti-correlated with what it exists to catch.** Minted P1 with a negative-control AC; widening the bar / raising the cadence multiplier / downgrading WARN are named as rejected remedies.
- **★ The ack-ledger premise I was handed was wrong, and the correction changed the target.** Not a dead pointer: `FIX-FLEET-PUSH-LAUNCHD-EXCONFIG-SILENT-DEAD` is **DONE_VERIFIED in `archive/2026-08.json`**; the resolver (`auditor-tier1-probe.sh:259-266`) only unions lanes *inside* orch-state — including one named `archive`, which is **not** the monthly file — so it prints `ABSENT` for a cold-evicted row. Bare removal was also wrong: fleet-push still exits 1 for a **different** cause (`tsc PASSES`; `[size-lint] FAIL … 228/252/250` → `[pre-push] BLOCKED`). Retargeted to the size-lint P0 and re-ran the resolver → `BACKLOG`. Corrected the tracking row's own note in place; it had named the wrong repoint target.
- **★ Recorded the masking risk rather than banking the win.** `launchd_agents` was the last red dimension (mem_creep back to PASS at **84.84%** vs an 85.00% gate — 0.16pp, oscillating, *not* resolved). The ack lets the probe go ALL_GREEN → heartbeat unfreezes → the tier-1 alarm goes quiet **with both defects intact**. Stamped `masking_warning` on the new row: verify against fixtures, never against "the signal is quiet now".
- **★ Signed off 6 cohort rows individually, not as a batch.** `UC-CCA-P2-MARKET-WATCHER` carries **no QA `status_note`** — a cohort-level AC-4 pass would have rubber-stamped the one row with no prior review. Verified from scratch (`market-watcher/flow/main.md:61`, "covers cycle.md AND eod.md"). `task_claim` locked per row, all 6 released. Schema refused DONE_VERIFIED without `verification.raw_probe{...}` — good gate; line numbers are on the rows. **Honest limit stamped on all 6:** `acceptance: null` on every one, so this certifies the flow-doc insertion, **not** a live gateway-down drill.
- **Self-caught false positive.** My first ordering check flagged `market-watcher/flow/cycle.md` gate-after-claim. The `task_claim` at `:1` is prose inside the size-justification comment; real calls at 251/288, both after the gate. Recorded because the check, not the file, was at fault.
- **Folds, all dedup-first, zero re-mints.** sweep-guard BARE `escalated=true` `prior_warns=11` → occ 28→**29** (true positive by construction; never dispositioned off `git show --stat`). notebook-immutability AC-2a → occ **6** on the compose row: first time the **same agent+section pair** (`system-auditor.md` / `## c114 · 2026-08-23T21:03Z`) fired across ≥2 cycles — it fired **six**, 21:39→00:46Z. Both cap breaches → the bytecap row, with the discriminator that the two envelopes fired **2s apart and disagree** (218L/1-section vs 310L/3-sections); the live file has **3 droppable sections**, so it is *not* structurally unprunable — do not apply manual-split on that envelope alone. auto-push-abort → size-lint P0, premise **re-measured**: envelope said `ahead=109`, live says **111, behind=0**.
- **Skipped Step PUSH-BACKSTOP deliberately** — 111 unpushed commits is precisely why it must not fire.

### Carry-over
- **4 envelopes held through the CLEAR, fourth consecutive tick.** Re-grepped rather than repeating last run's conclusion: `sprint_registry_dangling_ids` **0 hits**, `recurring-bug` **0 hits**, `system_issue` underscore appears only in the **count table at :72** — the routing rows at `:39`/`:86` are the **hyphen** form. Clearing them greens the coverage guard **by deleting its own input**. Re-read from disk post-write: inbox = 4, exactly these. Never run `guard-signal-type-coverage.sh` — writes live orch-state at ~:258, and `--check` is a no-op alias.
- **Do not push, do not repair the size-lint gate.** `pushBctcLayoutHandler.ts` 252L vs 250L upper; branch **111 ahead**, PUSH-AUTONOMY-1 unsatisfied. `FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH` stays P0 and **undispatched** — user's call, already surfaced.
- **`FIX-SWEEPGUARD-…-20-WARNS` is 30 bytes from its prose ceiling** (11968→11990 of 12000). The next occurrence bump will hard-reject; I had to shorten my own note twice to land this one. That is `FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS` (D3, `ready[]`) biting for real — it now gates a live P0.
- **`FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58` correctly stays BLOCKED, `next_agent=po`, mine.** Its gate is TASK_001 **and** TASK_002 at DONE_VERIFIED; live: TASK_001 `ready[]`/READY/→ops, TASK_002 `backlog[]`/BLOCKED, only TASK_003 done. No action available. It does **not** eat a WIP slot — verified at source, `devteam-eligibility.jq:115-118` excludes BLOCKED from `wip_in_progress`.
- **New P1 needs router hand-dispatch** → `developer`, `cross-service/`: `FIX-AUDITOR-DCYCLE2-COMPLETION-EVIDENCE-PREDICATE-CANNOT-SEE-COMPLETED-CYCLES`.
