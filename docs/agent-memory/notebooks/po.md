# PO — Notebook

## 2026-08-23T12:42Z · The request named the stack, the stack was wrong, and the filter it asked for was already sitting in the payload

- **Trigger:** router-direct scope request (session `669e1d9f`), not a dev-team tick. "Add quarter + ticker dropdowns to `/dashboard/bctc-inspect`", pre-framed by the caller as a Remix + Tailwind + shadcn frontend feature for `dev-frontend`. **Output: 1 orch-apply write, 1 mint (`FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER`, SPRINT-S/P1/`apps/mcp-server/`/`next_agent=ba`), 0 duplicates, task_total 762→763.** Journal: `docs/agent-memory/decisions/scope-20260823T1241Z-po.md`.
- **★ The caller specified the zone AND the stack, and both were wrong — the only thing that caught it was opening the route file.** `apps/frontend/app/routes/dashboard.bctc-inspect.tsx` looks like a page by its path and by every naming convention in the repo; it is a **resource route** that returns a raw `Response` and renders no JSX. The actual UI is `apps/mcp-server/src/interface/bctc-inspector.html` — 2692L of self-contained vanilla JS/CSS with `const BASE=""`. A shadcn `<Select>` **cannot be mounted into it at all.** Had I honoured the stated framing, `dev-frontend` would have been dispatched to a file outside its zone, in a stack the page does not use. **Generalises: a `dashboard.*.tsx` path under `apps/frontend/app/routes/` is not evidence the surface is React — grep for `export default` before assigning the zone.** Two more such proxy pairs exist beside it (`api.bctc-inspect.$.tsx`, `api.bctc-eval.$.tsx`).
- **★ "Add a filter" turned out to be a zero-backend task, and only the live payload could prove it.** The reflex spec is a new query param on `/api/bctc-inspect/docs`. But `DocListItem` (`bctcInspectHandler.ts:141-165`) already ships `action_code`, `period_type`, `period_year`, `period_quarter` per item, and `loadDocList()` already pulls **all** of them in one 112669B call. The whole feature is a client-side facet over an array that is already in memory. **Reading the response, not the handler signature, is what collapsed the scope** — a handler-only read would have shown the endpoint takes no params and invited me to add one.
- **★ Probing the live payload for cardinality also surfaced a defect nobody filed.** `period_quarter` is `number` on 255 items and **`string` on 2** (both HUT), so `buildLabel()`'s `` `Q${period_quarter}` `` renders **`HUT Q1 QQ1 2024`** in the dropdown today. Unnormalised it also inflates the new quarter facet 11→13 options. The type drift is invisible from the schema, invisible from the handler, and visible only in a `jq 'group_by(type)'` over the real response. **Folded the ~3L display coercion into the row (same file, same developer) and explicitly named the DB write path OUT of scope** — one row, root cause of the *rendering* fixed, root cause of the *data* named so it cannot be silently absorbed.
- **`period_type` does not mean what its name means.** It holds `'Q1'..'Q4'` (81/65/33/78), not `QUARTERLY`/`ANNUAL`, and there are zero annual rows. An AC written from the field name alone would have built the quarter facet from the wrong column.
- **Declined the flow's own step 3.** `sprint-kickoff.md` prescribes a `.sprint_goal.entries[]` vision entry; `entries=16` against a documented cap of 15, breached last night for a user-originated P0. A two-dropdown facet filter does not earn a 17th. Board row only — **the mint is what makes work dispatchable, the vision entry is what makes it governed, and this needs the first without the second.**
- **Skipped the triage pre-flight deliberately.** PO ran the full chain at 11:57Z (45 min prior), `signal_queue` `to:po` `NEW`=0. Re-running tnb/channel/signal audits for a single scoped feature request would have duplicated a fresh tick at real token cost.

### Carry-over
- **`FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER` is in `backlog[]`, `next_agent=ba`, P1 — it is dispatchable but NOT dispatched.** It sits among ~471 backlog rows against an RLC that moves ~1/tick. If it matters on a user timescale it needs a manual first hop to `ba`, not the queue.
- **Still open from 11:57Z:** BATCH of 3 rows, all `agent-father`, all off the DRS allowlist ⇒ router manual dispatch or they sit. Two (`FIX-SIGNAL-TYPE-ROUTING-GAP-cowork-fire`, `FIX-PO-TRIAGE-INBOX-CLEAR-ECHO-PIPE-…`) touch the same file, different sections — dispatch in ONE hop. Third is `FIX-QA-VC-…`.
- **Sweep-guard still says this session is a repeat offender (`prior_warns=6 threshold=3 outcome=blocked`)** — explicit pathspecs on every commit, never bare `git commit -a`.
- **`orch-state.json` is 3.28MB** against the ~600KB HOT CEILING that `RC-ORCHMONO` exists to enforce; 23 rows are already grandfathered over the 12KB per-row prose ceiling. Every mint pays this tax.
