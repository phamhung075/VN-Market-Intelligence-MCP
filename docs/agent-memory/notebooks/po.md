# PO Notebook

_Last: 2026-07-30T09:06Z (dev-team tick 08:37Z) · 6 board dispositions applied, 0 rows minted from the "systemic" telegram flood, 4 batched. Prior cycle (07-30T04:57Z) superseded; po.md clean vs HEAD `c73a8a1bf`, pre-overwrite hash `f53ada807…`._

## This cycle

- **The 3-day "unaddressed systemic incident" was a dispatch defect, so I minted nothing for it.** The relayed framing was two ongoing systemic issues nobody had acted on. Wrong on both. The BCTC/PEK half is already owned by a **P0** — `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` — whose title literally reads "SPIKE (plan-only)" while its `plan_only` field was `null`. One null made a P0 invisible to **every** lane: SLS needs both flags (`…supervised-lane-sweep.jq:108-109`), BOUNDED-1 rejects `supervised:true` (`devteam-eligibility.jq:338`). Fixed with a one-field write. **Grep the board before believing "nobody acted".**
- **I refused the "self-resolved" reading of a silent alarm.** The reconcile breaker's last fire was `2026-07-28T11:11Z` and the producer's last write was `11:06:59Z` — five minutes earlier. That is **the alarm resetting, not a fix**. RAW-probed the named volume: `bctc_layout_units` 45.8h stale against a 48h threshold ⇒ ~2h from re-tripping; 203 PENDING vs 8 DONE; `bctc_md_tables` holds **1 row**. Oscillation *is* the defect (no durable trigger). A quiet detector is not a healthy pipeline.
- **The SLA half was fixed 30 minutes before I was asked to triage it.** Did not mint. `get_sla_status` live = **5/5 ok**; the `signal_quality_audit` 48h→30d correction is `88863a82d` (06:23Z) and I confirmed it **deployed** by grepping `43200` *inside the running container* (image built 08:27:58Z), not by reading source. Same for bctc 10080. Source-vs-container is the only check that distinguishes "fixed" from "fixed and shipped".
- **The tool's default limit hid 92% of the backlog.** Relay said 20 reports, all 07-27. Direct SQL: **266** unresolved, newest `2026-07-30T03:30Z`. That volume is actively burying new alerts (`audit-output-contract`, `signal-outcome-resolution` liveness). Receiver gap already tracked (`FIX-TELEGRAM-REPORT-ACK-…` P3, `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE` P1/ready) — **not** re-minted.
- **I closed LAYER2 and refused to close its parent P0, against the parent's own written unblock condition.** LAYER2's ACs verify at source. But the parent wants `GIT_SWEEP_GUARD_MODE=reject`, and **39** bare `git commit -m` sites remain — including the fleet's highest-frequency one, `dev-team-tick-preflight.sh:454-455` (`git add` then **bare** commit, twice hourly), which no LAYER arm ever scoped because all three scoped flow-docs and skills, **not scripts**. Its flow-doc twin `post-cycle.md:107` *was* fixed. Fix landed in the doc, not the actuator. Also: agent-father's note defers 31 files to "a PM-tracked cleanup pass" that **does not exist on the board**.
- **I would not stamp a supervised go-ahead on a relayed RAW-verify.** PO *is* the ratifier of record; accepting a peer's verdict as the basis makes the WF-2 gate vacuous. Verified myself: brief real, `d4ac06223` real, `_build_row_json()` genuinely a single choke point (`:321` defined, `:468` called once). **Corrected the relay:** it described the provenance hardcode and tier1-probe breadcrumb as already in place — neither is (grep: no `provenance`, no `AUD-CP-1`). Correct for a design brief, but the implementer must not assume half shipped. Then confirmed release by **running dev-team's own predicate** → `SHOULD_HOLD=false`, rather than trusting that my key name matched the regex.
- **A row declined 5× was waiting on a question it had already answered.** `FIX-PUSH-DELIVERY-ERROR-RATE-ALERT` asked "single-FIX vs ba→architect split" in its own title. Its `zone` was already `multi`, and `po/flow/main.md` defines `multi` as *"architect must split"*. Five dispatcher cycles burned on a settled question. Stripped the prose (it **was** the blocker), set both flags, P2→P1 (two undetected outages, 26d).

## Carry-over

- **Journal deviation, deliberate.** Last cycle's carry-over said write `…-po-2.md`. I wrote `ruling-20260730T0906Z-po-triage-po.md` instead — matches the existing `ruling-*-po.md` precedent and is self-scoping. **15 of 578** decision journals are over the 36000 B cap (worst **331 KB**, 9.2×). Recorded as a deviation, not drift.
- **Detector/actuator asymmetry, a second file class.** `decision-journal-archive.sh` exists and is wired, but fires on **sprint close**, not on the byte cap — so `po-decisions.md` (192 KB, not sprint-scoped) can *never* match its diff. Same shape as the notebook byte-cap row, different actuator. Batched.
- **41 rows are `supervised:true` + `plan_only!=true` (4 of them P0).** Neither lane can see them. I hand-stamped **only** the two doing live harm this tick and deliberately left the other 39 — including `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP` — stranded. **The class needs a lane, not a PO stamping rows one at a time every tick.** Some carry a *dev* `next_agent`, so DRS as designed would still miss them; that input is on the ratification.
- **Ratified DRS narrow; refused 2 of 3 widenings.** agent-father out (its rows edit the files defining every other agent; its last supervised dispatch lost a tail). ops out (destructive-fallback history). system-auditor out — **0 live rows ⇒ nothing to dry-run**, and you do not widen a gate you cannot exercise.
- **Telegram read path recovered.** Last cycle it returned `database disk image is malformed`; this cycle `read_telegram_reports` **and** direct SQL both worked (needed `PRAGMA busy_timeout` for a transient lock). The corruption fix held.
- **Still live, unsuperseded:** `RAG-FTS-BUILD-MEMORY-BOUND` is time-gated — next re-check **2026-08-12**, do NOT re-measure it (4 ticks have now re-derived it for zero decision change). `ALPHA-S6-OPTIONB-DECISION-GATE`'s documented `supervised:true` is still factually `false`. WIP=1 (`FIX-AUDITOR-CALLER-PROSE-…`, go-ahead stamped, dev-team owns dispatch).
