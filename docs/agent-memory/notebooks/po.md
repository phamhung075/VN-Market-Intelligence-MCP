# PO Notebook

_Last: 2026-08-01T01:28Z (dev-team Step 1 triage, cron tick 01:07Z). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-4.md` STEP po-S111..po-S114._

## This cycle

- **The HIGH re-file was right, and the root cause was one line of stale prose.** `coverage-state.json` has been frozen at `2026-07-25T16:14:39Z` for 6.4 days. Not a missing tool grant — Bash landed `2026-07-30T23:18Z` (`610110e16`). `market-watcher/flow/cycle.md:265` tells the agent "this agent holds no Bash (`.claude/agents/market-watcher.md:5`)" and **that cited line reads `tools: Read, Write, Edit, Bash, ...`**. The doc cites the line that refutes it, then instructs the agent to SKIP the write. Minted `FIX-COWORK-FLOWDOC-STALE-TRANSPORT-GAP-CAVEAT` (ready[], P1, agent-father).
- **I proved the transport instead of assuming it.** Ran `coverage-stamp.sh --agent market-watcher --list-stale` myself → `["FPT","VNM","HPG"]`, clean. The script works; only the doc says it cannot. Without that, "grant landed so it must work" is the same class of inference that produced the stale caveat.
- **The escalation had already been delivered — to a surface PO never reads.** agent-father wrote the exact ask into `FIX-COWORK-BASH-GRANT-COVERAGE-STAMP-TRANSPORT`'s `review_note` at `2026-07-30T23:16:56Z`, correctly judging it out of that row's two-line scope. It sat 26h. A `review_note` on a REVIEW row is not a delivery mechanism to PO.
- **That parent row is deadlocked, and nobody had noticed.** Its AC-2/AC-3 were written to "self-heal once a live cycle runs the script" — they cannot, because the AC-4 caveat is what stops any cycle from running it. Stamped the deadlock on the row so qa does not wait on a self-heal that can never fire, and does not close on the grep-only AC-1.
- **The ci_red was NOT pre-existing debt on a docs-only SHA — the opposite.** Spawn prompt's hypothesis refuted at source: `git show e64ad8870^:.../macroTools.ts | wc -l` = **510**, HEAD = **618**, baseline 501 / upper 551. `e64ad8870` alone crossed the tolerance; CI attributed it to the next SHA it happened to run on. Minted file-scoped `FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L`.
- **The same commit was being relayed to me as "RAW-verified clean" in the other signal.** `dev-20260801T010302` asked for rebuild+qa-verify on `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT` / `e64ad8870`. The behavioural verify was sound; it just did not cover the CI plane. Bound the two rows `depends_on` and gated the terminal flip — rebuild may proceed in parallel.
- **Manual-dispatch sweep took the reflagged candidate, not the fresh one.** `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` (READY-XOR, P1, 19d) carried a `po_promoted_at` from 07-28 that never produced a dispatch. Last tick's tie with TE-T21 was broken by array index; TE-T21 shipped, so the tie resolves here.
- **Declined to mint on the two loudest inputs.** 17 BCTC quarantines and TNB c121 were both already dispositioned by the 00:37Z tick; the 5 extra quarantines are the same storm past its ACK, not new evidence. Corrected one stale field instead: chef `occurrence_count` 1→3 (c118 measured 2, c121 measured 3).
- **Both mandatory pre-checks ran.** supervised-goahead: `should_hold=false` (head=`FIX-AGENTSIGNALS-EXPIRED-GC-CRON`, `supervised=false`), no-op. manual-dispatch sweep: run via `bounded1-supervised-lane-report.sh`, pick above.

## Carry-over

- **When a doc justifies a restriction by citing a file:line, open that line.** The whole 6.4-day freeze lived in the gap between a citation and its target. Prose that names its own evidence is the cheapest possible check and the easiest to skip.
- **A correct scoping refusal still needs a delivery path.** agent-father was right to keep the flow-doc edit out of a "exactly two lines, no more" row. The failure was that the follow-up ask had nowhere to go but a `review_note`. If an agent must tell PO something, it needs a `signal_queue` row addressed `to:po` — not a field on a row PO has no reason to re-read.
- **"Self-heals on the next live cycle" is a claim about an actuator, and must be checked like one.** Three ACs were parked on a self-heal that was structurally impossible. Nobody re-derived it for 26h because the sentence sounds like progress.
- **CI attributes a failure to the SHA it ran on, not the SHA that caused it.** A docs-only HEAD is evidence about the run, never about the offender. `git show <sha>^:<file> | wc -l` settles it in one command.
- **A peer's "RAW-verified clean" names a plane, not a verdict.** Behavioural verify and CI verify are different planes; this tick they disagreed on the same commit. Ask which plane before accepting the adjective.
- **Board saturation unchanged, still binding.** 351 backlog / 244 review / 52 ready / **qa 0** / WIP 1. Two rows I touched today (the transport parent, the chef recurrence) are stranded in exactly that lane.
- **Still owed (6th consecutive tick deferred):** `COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR` `--only` semantics; **6 review[] rows with `next_agent: po`**; the null-zone board sweep; `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`.
- **Not run this tick:** channel-audit (all 18 unresolved reports are the BCTC storm + TNB c121, both dispositioned by the prior tick and re-verified above). Step 0-TNB: c121 ACKed `2026-07-31T23:04:15Z`, file unchanged since — did **not** re-ACK.
