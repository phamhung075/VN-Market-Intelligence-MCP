# PO Notebook

_Last: 2026-08-06T12:30Z (P0 market.db WAL re-arm triage — incident declared resolved, mechanism still live). Prior PO cycle 11:40Z preserved at `HEAD~`._

## 2026-08-06T12:30Z · "Incident resolved" was true about the data and false about the mechanism

- **The DB is in WAL mode right now, ~10 minutes after recovery closeout.** `bash scripts/audits/verify-market-db-journal-mode.sh` → `verdict=FAIL journal_mode=wal wal_present=true shm_present=true`, exit 2. Host `data/live/market.db-wal` grew **140,112 → 737,512 → 1,545,032 bytes across ~3 minutes** of observation. Corruption #6 is incubating on the same bind-mounted virt layer that produced the other five. `get_alerts` returning 20 real rows is true and is not health — it is `feedback_passive_health_masks_dead_data`. **Reads working is not the predicate; journal_mode is.**
- **The ticket I was told to block had already merged, and I am the reason it was wrong.** `e370f5f51` (07-31T00:45Z) is in HEAD. It did not "risk taking the fleet from 2 re-armers to 4" — it *did*, 6 days ago, and the 2 it added (`Tier3CacheFetcher.FetchPrice`, `SQLitePriceHistoryRepository.GetHistory`) sit on the kinh-dich + frontend price-chart path, i.e. **far hotter than the two the incident doc names**. That is the missing explanation for why the 07-30 "permanent fix" survived exactly 14 hours.
- **Root-cause lesson, generalisable: "matches the sibling file" is a consistency argument, not a correctness one.** I minted that row at 07-31T00:07Z describing `foreign_flow_repository.go`/`room_event_repository.go` as "the correction already applied to 2 of 4 call sites". The architecture brief published **14h earlier** names those exact two files as "a live landmine … the single most actionable finding in this brief" and prescribes the **opposite** fix shape. I read a landmine as prior art and told a developer to propagate it across two more call sites. Prior-art greps must ask "has anything condemned this pattern", not just "does anything else do it".
- **The stale comment was the carrier.** All four sites repeat `// For production: market.db is expected to have WAL already enabled.` That was true 2026-04-25 → 2026-07-30T10:26Z, and false since. Nobody updates a comment; everybody copies one. AC-1 now requires deleting it at all four sites, not just editing the DSN.
- **`mode=ro` is the structural fix, not a stylistic one.** It makes a journal_mode write *fail* instead of silently mutating the file header — violation becomes impossible rather than merely discouraged. Paired with un-swallowing `//nolint:nilerr` (AC-2, never implemented), a future re-arm attempt becomes a loud error. The ticket's own evidence item (5) already proved `mode=ro` without the WAL pragma returns full rows, so this costs nothing.
- **A working guard sat unarmed for 6 days while the thing it detects was live.** `scripts/audits/verify-market-db-journal-mode.sh` is genuinely good — asserts through the running container (its header explains why source-grepping would have missed the defect it was built for), checks the `-wal`/`-shm` pair, `--self-test` proves both branches. Repo-wide grep for its name: **the script itself plus two decision journals. Zero call sites.** It worked first try when I ran it by hand. **Shipping a detector is not arming one** — this is the highest-leverage row of the tick, above any individual re-armer patch.
- **The deferred "DELETE-vs-WAL policy" question was already answered a week ago.** Architect resolved AC-A..AC-D in §4 of the 07-30 brief; market.db = DELETE, sole owner `schema.ts`. Only the *coordination.db* half of AC-B is open (verdict: do NOT blanket-convert, measure lock-hold in a fixture — `synchronous=FULL` on every `task_claim`/heartbeat lengthens the fleet mutex window, worse blast radius than the corruption). I refused to open a fresh spike. **The decision was never missing; enforcement and a discoverable home were** — which is precisely how a developer re-derived the opposite conclusion 14h later.
- **Proved the `immutable=1` observer defect instead of arbitrating two contradictory memories.** Same instant, same DB, -wal at 1.5MB pending: `immutable=1` → `daily_ohlcv=775891`, `journal_mode='delete'`; `mode=ro` → `775912`, `journal_mode='wal'`. Silent 21-row staleness, and — the serious half — **the integrity probe reports the SAFE journal mode while the DB sits in the DANGEROUS one.** The observer built to catch DB drift would certify as clean the exact condition that has corrupted this DB five times. Unifying rule: `immutable=1` is safe **iff `-wal` is 0 bytes at read time**, silently wrong otherwise; the 06-25 "it's correct" reading was simply taken during a checkpointed moment. Corroboration that it is a live disagreement, not theory: the journal-mode guard (normal readonly open) says `wal` at the same instant the probe says `delete`.
- **Sequencing matters and I recorded it as an AC, not prose.** Fix Go + redeploy stock-price **first**, *then* checkpoint+flip to DELETE. Flipping first is wasted — the next fetch re-arms. And a guard PASS taken before the four endpoints are exercised proves nothing: **dormant is not fixed.**
- **Rejected stopping the stock-price container as interim mitigation.** It would restore the exact outage (kinh-dich 503 every ticker, empty price chart) that the reopened row exists to fix — trading a probabilistic risk for a certain product outage, when the fix is four one-line DSN edits.

### Carry-over
- **Sequencing is load-bearing — do not let anyone "just flip journal_mode back" as a quick win.** Without the Go fix deployed it re-arms on the next stock-price fetch. Order: land row → redeploy stock-price → exercise all 4 paths → checkpoint+DELETE → guard must read PASS.
- **Incident-doc timestamps are +2h wrong.** `docs/incidents/2026-08-06-sqlite-db-corruption-wai-rearm-vector.md` labels Paris local time as `Z` ("14:14Z restart", "12:10Z–14:25Z window") — real UTC was 12:14Z / ~12:2xZ, confirmed against `date -u` and container mtimes. Same shape as `feedback_hand_typed_iso_timestamps_drift_into_the_future`. Anyone reasoning about the re-arm window from that doc will be 2h off.
- **Do NOT conflate with `FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT`** (alert-commander already did once). The link is one-directional and mechanical: the corruption row *creates* the WAL windows in which the sidecar row's `immutable=1` defect goes blind. Fixing the first shrinks the second's exposure but does not fix it.
- **`/app/data` holds ~14 `market.db.*` forensic artifacts, >1.5GB, on the same bind mount as the live DB.** Given `project_enospc_blocker`, worth a janitor sweep with a retention policy. Noted on the P2 row; deliberately not absorbed into it.
- **Board after this tick:** `task_total` 811→813; 4 rows touched — 2 reopened out of `review[]` (both were heading for a sign-off they had not earned), 2 minted. `review[]` saturation (~270) still the top-line constraint, and this tick is evidence the lane is not merely slow but **passing things through**: one row reached REVIEW with 2 of 3 ACs unimplemented.
- Deferred: dev-team signal `dev-20260806T115859` (LOW, stranded-state sweep, 5 unknown paths) ACKed + marked triaged, no row minted — re-surface next ambient tick.
- Still live from 11:40Z: `FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE` (treat `[emit-signal] BUG: E-3 write/read-back failure` as presumed test noise); architect owes the 4-lane S2 lock adjudication; `guard_signal_type_coverage` still rc=1.
