# PO Notebook

_Last: 2026-07-29T10:58Z (Tier-2 10:30Z audit triage — 6 handed findings) · 3 rows minted, 2 rows amended, 3 findings declined._

## This cycle

- **Four of six handed findings named a mechanism the artifacts contradict, and the check that settled it was one read-only tool call.** The pattern: the router reasoned from timestamps and narration; the defects were only visible in the emitting code's call structure. Timestamps are compatible with several mechanisms — which one requires reading how the artifact is *produced*.
- **The decisive test was "how many times can one invocation write this field?"** Two `sbv_fx` signal rows, distinct ids, 1s apart, were handed to me as a CAS retry that re-applied a successful write. But `_gen_row_id`/`_build_row_json` run **once** per `run_emit_signal`, *before* `_e3_write_row` — a retry re-applies a fixed `row_json`, so its signature is duplicate-**identical**-id. Distinct ids falsify it. `get_agent_signals` then showed **two E-1 rows** (#9885/#9886, expiries 12:33:36/12:33:38), and `_run_e1` fires once per invocation. The script ran **twice**. Fixing the CAS loop would have fixed nothing.
- **"A WARN paged nobody" was backwards — the paging worked and the *report* lied.** Premise was that the ledger stamp (10:33:36Z) predates the signals it dedups, so the agent read its own write. But E-2 runs at line 462 and the row ts is computed at 466: ledger-older-than-row is **correct ordering**. And `_check_dedup_and_maybe_send` upserts the ledger *only* inside branches that call `_send_bug_telegram()` first — no path stamps without sending. So DASHBOARD's "Telegram sent" is **true**; `telegram_sent=0` is the false artifact, because invocation 1's `OK` marker never reached `$MARKERS_FILE`. I was asked to fix the artifact that was telling the truth.
- **The contract script caught this itself and the finding was dropped on the floor.** `signals_posted=1` + `dedup_skipped=1` means exactly one marker parsed; `signal_queue_rows_written=3` can only have come from the V1 guard's independent re-read overriding it — which prints a `VIOLATION` line and BUG-Telegrams it. `main.md:875` requires that line in the RETURN. It is nowhere in the notebook. **The detector worked; the report deleted its output.**
- **A three-cycle "compliance" failure was an unsatisfiable instruction.** "All 7 routes must return ok" — `get_vps_proxy_health` returns **4** push-services, system-map `routes[]` has **8**. The literal 7 has no referent. Three cycles of briefing harder could not have worked, and an agent that "complied" would have had to fabricate three route results. Root-caused to the doc, not the agent — and it violates our own never-hardcode rule.
- **Chasing that number surfaced a live miss nobody reported:** `get_vps_service_health` shows **vn-sbv-fetch UNHEALTHY** (uptime 44m). The B-06/B-07 PASS was read off the freshness plane only — so the audit passed the check that would have explained its *own* B-12 WARN.
- **A timestamp-matching artifact was read as the wrong guard.** "The immutability guard detected it, enforcement is off" cited a signal file whose payload is `[sweep-guard] BARE commit about to absorb 1 staged file(s)`. That is the **pathspec** guard, not the immutability guard. The immutability detector never fired — so flipping it to `reject` would not have caught this commit, and the remedy on offer was for a different defect.
- **Two findings dissolved into reading the spec.** The tier-2 heartbeat on a DEGRADED cycle is documented at `main.md:828` as meaning "a cycle completed", explicitly *not* "was healthy", with the rename considered and rejected. A-29 has no broken-spec marker and `get_cron_health` returns fine. I was warned that carried-forward claim might be stale; it was.

## Carry-over

- **The instruction "don't touch those two rows" is why I found the worst defect of the cycle.** Re-checking that they were still there showed they were **gone** — absent from `rows[]` *and* `archive[]`, so no legitimate triage path explains it. A peer serialized a stale 131-row full doc over a 133-row tree. `orch-conservation-check.mjs` has `FLOOR_RATIO=0.5`, so losing 2 of 133 is invisible: it is a bulk-eviction breaker, and **single-row loss on the SSOT is unowned**. Minted P1. Verifying a negative constraint is a real check, not a formality.
- **Say which artifact is authoritative when two disagree, not just that they disagree.** Both new rows carry a fence naming the artifact that is *correct*, because in both cases the plausible-looking one was wrong. A row that says only "these conflict" invites the fixer to repair the honest one.
- **`FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` must NOT pass QA on current evidence.** Its two fix commits landed 08:43:01Z and 09:23:52Z; the violating commit landed **10:35:52Z** — recurrence *after* the fix, with no cap pressure (146L against a 200L cap). AC-4, the mechanical retained-bytes check, is the load-bearing one and is unbuilt. Recurring **failed** fix, not recurring bug.
- **That data loss has a downstream cost now on the record:** the deleted RAW-PROBE blocks were the evidence needed to pin the double-emit path, so the P1 emit row may need a reproduction run. Notebook data loss stopped being a hygiene issue this cycle.
- **`FIX-AUDITOR-COMMIT-NONEXPLICIT-PATHSPEC` is a one-line fix being carried at `medium`** — `scripts/auditor-notebook-commit.sh:202` already does the pathspec commit; the auditor just bypassed it. 28 sweep-guard signals fired today across 8 notebooks. Recommended bump; did not self-approve it.
- **`supervised = never dispatched` watch, 6th tick.** Both new rows are `supervised:true` + `plan_only:true`, adding two more to the pile. If the prior two are still untouched next tick, that is the routing defect confirmed — **not** more rows.
- **A-30 row 5d+, `recurring_bug_count=6`** (7th consecutive carry). Still BACKLOG with no architect activity next tick → **escalate to user**. Do not re-attach evidence again.
- Standing, 14th carry: verify my own commits carry my authorship (`git log --author`).
