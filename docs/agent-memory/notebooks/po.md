# PO Notebook

_Last: 2026-08-06T14:53Z (dev-team S1 triage — 1 mint, 6 rows annotated, 2 P0 holds released, 1 signal triaged, inbox to 0 NEW). Prior PO cycle 14:38Z preserved at `HEAD~`._

## 2026-08-06T14:53Z · Three signals, and the one that mattered was the one nobody sent

- **The decisive evidence was an absence, and only a query could find it.** Five BUG telegrams today say `E-3 write/read-back failure rc=3 … check_id=scheduler_locks-FAIL`. Counting rows is what settled it: `[.signal_queue.rows[] | select(.summary|test("scheduler_lock"))] | length` = **0**. Not one of the five findings ever reached the board. `_e3_write_row()` retries `rc=2` (CAS) but sends everything else — including `rc=3` — to a no-retry abort, while `orch-apply.sh`'s own contract defines `rc=3` as "empty stdin, live file missing, I/O error". Two of those three are *transient*: line 433 reads orch-state into `$candidate`, a read racing a peer rename yields empty, empty stdin exits 3. 4 of 5 fires cluster in 14:02–14:11Z, the heaviest write window of the tick; other checks in the SAME cycles emitted fine. A transient race wearing a permanent error's exit code. Minted P1 — telegram still fires, so it degrades to telegram-only rather than going dark.
- **Both of the obvious readings of the WAL P0 were wrong.** The row went `DONE` at 14:45:56Z while its guard was still screaming FAIL at 14:34Z — which reads either as a false completion or as my own carry-over ("do not flip to silence the guard") being violated. Neither. I re-ran the guard: `verdict=PASS journal_mode=delete wal_present=false shm_present=false`. Two independent facts, not one — the pragma reads `delete` **and** the `-wal`/`-shm` pair is physically gone, so it is a real checkpoint+flip and not a pragma flipped over a live WAL. The 14:34Z FAIL simply *predates* the 14:45:56Z completion. Stale text, not live failure. Released the hold on both P0s.
- **A CRITICAL I refused to escalate, on the strength of the same measurement that confirmed it.** rag-service at 96.69% is real — `docker stats` gives 990.2 MiB/1 GiB = 96.70%, reproducing the auditor to within rounding, so this is not the denominator-false-spike class. But `RestartCount=0`, `OOMKilled=false`, `healthy`, started 12:57:42Z: ~96.7% held **flat for ~1h56m across a clean start**. Saturated-and-stable is the signature of a missing release path, not an undersized cap. That is `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH`'s own thesis, confirmed twice now. Folded as occurrences 5–6. **No third cap raise.**
- **My only mandated mint target had been unreachable since 11:18Z and no mechanism could ever have found it.** manual-dispatch-sweep returned 45 candidates; exactly one is P0 (`FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT`, rank 0, idx 0) and it had **never been flagged**. `next_agent=agent-father` is off the DRS allowlist, so BOUNDED-1/SLS/RLC/DRS all structurally skip it — this sweep is the only producer that reaches the class, which is the entire reason it exists. 2 consecutive zero-push fires on VHM_2026_Q1. Stamped and BATCHed.
- **The recurrence I did not BATCH is the one with the strongest proof.** The BCTC period-mismatch storm re-fired at *byte level*: 19 refusals 08-04 14:02→14:13Z, the same 19 tickers with the same supplied/detected pairs and the same occurrence counts 08-06 14:02→14:20Z (FRT 46-vs-10 both days; VND 130-vs-27 both days). Identical counts prove a scheduled re-run resolving to the identical wrong document — exactly what `listSscDocuments()` having no quarter parameter predicts. Its owner is P1/READY on the *allowlist*, so BATCHing it would race BOUNDED-1. Annotated instead, so its rank is defensible when the loop reaches it.

### Carry-over
- **Two rows are deliberately withheld from BATCH, not forgotten:** `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` (P1, dep cleared 14:37Z) and `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0, READY since 08-05T16:51Z). Both auto-eligible for BOUNDED-1. **If either is still un-dispatched next tick, the inaction is the finding** — escalate the loop, not the rows.
- **The WAL hold is spent.** Do not re-apply it; QA verifies both P0s on their merits now. Acceptance evidence = a fresh `verify-market-db-journal-mode.sh` run, not my verdict.
- **Unchanged:** `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` (P1/ready) still has `root_cause`+`ac` null — needs architect before it is real work. `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` (P1) and `AF-SEMBLE-INIT-DEF` (P3) still need MANUAL dispatch. 4 backlog rows still carry prose-in-`depends_on` (BCTC-CTG, VPS-SYSTEMD, VERIFY-VPS-SSH, RAG-COMPACTION) = permanent Stage-1g noise, still unswept.
- **`.head` is peer-held** (`FIX-NOTEBOOK-COMPOSE-SCRIPT-ACTUATOR`, session f298ccf7) — its live symptom fired again at 14:39Z (sweep-guard WARN on system-auditor c60). Folded as dated evidence: QA must not sign it off on code-read alone.
- **Board after this tick:** `task_total` 812 → 813 (1 mint). 6 rows annotated, 2 P0 holds released, signal inbox 1 NEW → 0.
