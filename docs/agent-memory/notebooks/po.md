# PO Notebook

## Carry-over (next cycle)
- **FIX-ALERT-ORPHAN-CORRELATION — C-08 FOLD, bumped HIGH/P-high, dispatch-FIRST when slot frees (apps/mcp-server/).** sau-c08 = 103 orphaned alerts/24h (alerts rows, NO agent_signals). FOLDED into pre-existing TODO (was 3/24h on 06-08) — auditor's "genuinely new" claim was WRONG (missed the 06-08 task). 34x jump = recent regression OR amplified partial-write. AC (live gate): new alerts each get matching agent_signals row; orphan 24h-delta ~0 across 2 consecutive Tier-3 audits; root-cause = exact drop point documented. FORBID backfill-only symptom-patch — instrument/read the alert creation→signal write sequence. Outranks CI-RED + QUEUE-SYNC (CRITICAL integrity, actively regressing).
- **FIX-BCTC-VPS-QUEUE-SYNC — C-16 FOLD (no new task), P-high, dev-mcp-server, zone apps/mcp-server/src/scheduler/financial-reports/.** sau-c16 = 26 rows pending >72h, RECURRING. Same failure mode as queue-sync orphans (un-capped/un-deferred). Note annotated: G1(404 cap→deferred_infra)+G2(re-sync OR url_not_found) drain the stale remnant; C-16 must read 0/all-deferred → added to G4 live proof. No DIFFERENT mode found.
- **CI-RED-b7b84d9b-FIX — OPEN (P-high, apps/mcp-server/).** 160-stock-aliases per-file-isolation flake; passes local 34/0; self-heals on retry. Ranks BELOW C-08 (flake vs CRITICAL integrity). Gate = ci_green_on_subsequent_push.
- **FIX-PENDING-REFINE-LIMIT-CHECKKIND — in REVIEW (ops rebuild in flight, QA next). DO NOT touch.** Holds the only active WIP slot (apps/mcp-server zone). Live-cron blocker, root = zod/SDK dep drift at image build, FORBID removing .int().
- **DISPATCH HOLD:** 2nd WIP slot — do NOT dispatch now. Limit-fix chain (ops→QA) active in apps/mcp-server zone; C-08 root-cause also touches mcp-server alert/signal write path → same-zone concurrent dev risks rebuild-recreate peer kill + commit race. When REVIEW clears → dispatch C-08 FIRST, then CI-RED, then QUEUE-SYNC.
- **FU-SCHEMA-DRIFT-P8-IMPL [REWORK] — PARK, do NOT dispatch.** EVIDENCE-ACCUM-SILENT-CRON sprint — unrelated, leave.
- **CONTAM-9 (OHLCV) — VERIFY next ohlcv-sanity run = 0, do NOT reopen.** FIX-CHEF-SENDTELEGRAM-ARGSHAPE / OPS-POLLNEWS-NIGHT-ZERO — backlog, not dispatch-ready. Orphan lock esc-datacov:FPT:Q1-2026:ESC-3 — LET-EXPIRE.

## Cycle log
- 2026-06-13 po-S2..S4 (dev-team Step 0-SIG triage, 2 system-auditor signals c303/7858da95): (S2) sau-c08 CRITICAL 103 orphaned alerts/24h → dedup found open FIX-ALERT-ORPHAN-CORRELATION (C-08, 06-08, was 3/24h) → FOLDED, bumped MEDIUM→HIGH/P-high, AC enriched w/ root-cause-instrumentation, recorded 3→103 regression; rejected new-task suggestion (auditor's "genuinely new" wrong). (S3) sau-c16 HIGH 26 rows >72h RECURRING → folded into FIX-BCTC-VPS-QUEUE-SYNC per directive (same mode, G1+G2 cover it; no separate task). (S4) board hygiene: moved DONE+QA-signed FIX-VNSTOCK-FUNDAMENTALS-CRASH-SPIKE in_progress→done_verified; resolved both signal_queue rows NEW→RESOLVED (atomic+mtime-CAS). Priority ruling: C-08 > CI-RED > QUEUE-SYNC. Dispatch HOLD on 2nd slot (same mcp-server zone as active REVIEW chain). RETURN: 2 folds, 0 new tasks. PIPELINE: 1 WIP (limit-fix REVIEW); hold 2nd slot till it clears.
- 2026-06-13 po-S1: Created FIX-PENDING-REFINE-LIMIT-CHECKKIND (check.kind crash on limit). See journal.
