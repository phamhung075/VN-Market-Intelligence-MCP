# PO — Notebook

## 2026-08-22T20:44Z · Predict-engine accuracy — the loop was never closed, and arithmetic proved it

- **Trigger:** user-pasted "Dự báo AI & Kết quả" dashboard (49 predictions / 25 resolved / 24% hit / avg Brier 0.4743) + *"improve and make it more recurrent."* Router supplied 6 hypotheses, flagged verify-before-acting. **Output: 1 vision + 1 SPRINT-L mint + 1 37-day-old SPIKE escalated & folded, 2 hypotheses reclassified, 1 duplicate refused.**
- **★ BRIER > 0.25 IS A DIFFERENT DIAGNOSIS THAN "LOW ACCURACY."** 0.4743 beats-by-losing a constant 50% forecast, so the engine is *actively miscalibrated*, not imprecise — that reframing is what turned a "tune it" request into a structural sprint. Live `get_calibration_report` (itself 13d stale, the 08-16 Sunday slot produced nothing) was worse than the paste: Brier **0.5319**, trend degrading +0.244 WoW, bearish **0.7045** vs neutral 0.3309. Decisive artifact was the curve, not the headline: 65%→50%, 75%→33%, **85%→0%, 95%→0%** — monotonically anti-calibrated, most wrong where most confident.
- **★ THE FIX-DEFINING EVIDENCE WAS AN EXACT ARITHMETIC IDENTITY, NOT A CODE READING.** `get_evidence_summary("ACB")` live: two fragments scoring 0.4224 and 0.1800, carrying **LR=0.54 (n=91)** and **LR=0.86 (n=195)**, both TRUSTED, both *below 1.0* = empirically anti-predictive → reported `Bullish: 0.3012` = (0.4224+0.1800)/2 **exactly**. The likelihood ratio is arithmetically absent. Confirmed at source: `evidenceAccumulatorJob.ts` is `score = sum(magnitude*confidence)/count` and never imports `likelihoodRatioStore`. The system had already measured on n=91/n=195 that its own bullish BCTC evidence doesn't work, and kept weighting it full. **That identity is now the sprint's primary success metric — it must stop holding.** A prose "LR isn't applied" claim would have been arguable; the identity is not.
- **★ "MORE RECURRENT" WAS LITERAL, NOT VAGUE USER PHRASING.** `calibrationReportJob.ts` has **zero** `likelihood` references (grep -c) and ends at `insertCalibrationSnapshot` + `sendCalibrationDigest`. Two loops both dead-end: `baseRateComputationJob` (daily) fits LRs nobody reads; `calibrationReportJob` (weekly) scores real predictions and discards the result. No path from *"this was wrong"* → *"weight it less."* The user named the actual defect better than the dashboard did.
- **★ THE "3 CONSECUTIVE INDEPENDENT MISSES" WERE ONE ROW READ THREE TIMES.** Router hypothesis #1 wanted a `foreign_flow_institutional` sign flip off 3× VPB + 3× ACB repeats. `getLatestEvidenceScore` is `ORDER BY score_date DESC LIMIT 1` with **no recency bound**, while the accumulator only writes for stocks with fragments in 30d and purges expired ones first — so an expired-out stock serves its last score forever. Live VPB: `Score date 2026-08-09` (13d stale), `Bearish 0.9500`, `Fragment count 2`, `(no fragments found)`; ACB fresh 08-22 ⇒ per-ticker cache rot, **not** a dead cron. So that evidence is nearer n=1 than n=3 → **refused the refit**, wrote the prohibition into `scope_out` and a no-backtest-certification clause into AC-5.
- **Dedup held twice.** VPB staleness = existing `SPIKE-EVIDENCE-SCORE-CACHE-FRAGMENT-DECOUPLE` (created 2026-07-16, BACKLOG, **unrouted 37 days**) → escalated to `high` + `parent_sprint` + measured-cost note, not re-minted; its cost is now known (VPB is the worst ticker in the engine at Brier 0.9025 and all 3 of its 95% misses came off that one row). BCTC `total_assets=0` = existing `FIX-BCTC-VALIDATION-GATE-NONBANK-ZERO-SCALE` → refused, written into `scope_out`.
- **Deliberately not done:** did not re-run channel-audit / TNB-audit / manual-dispatch-sweep — a peer PO tick drained the durable inbox to 0 at 19:27Z and `signal_queue` shows `new_for_po: 0`; re-running races fresh state. Did not fix anything myself (router-only + PO boundary).

### Carry-over
- **`SPRINT-PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP`** → backlog, **next_agent=ba**, high, SPRINT-L, `apps/mcp-server/`, 9 files. Chain `ba → architect → pm → dev-mcp-server → qa`. Router must dispatch **ba** next — nothing else in the chain is armed.
- **n IS TOO SMALL TO CERTIFY ANYTHING.** 17 resolved in-window, 25 all-time, n=3 in each extreme bucket. AC-5 explicitly forbids QA signing off on a backtest over the same 17 claims; the durable metric is the LIVE forward curve. Structural ACs (D1/D2/D3) are provable by code + arithmetic and do **not** need sample size — that split is the whole defensibility argument, don't let architect collapse it.
- **`SPIKE-EVIDENCE-SCORE-CACHE-FRAGMENT-DECOUPLE`** is now this sprint's D4/AC-4, `parent_sprint` set. It must NOT be independently re-triaged — a second owner would re-derive the same root cause a third time.
- **sprint_goal cap 15 → 16, breached knowingly** (recorded on `sprint_goal._note` + the entry's `_note`). Next triage pass owes a closure audit. `PREDICTION-CLAIMS-DAILY-CADENCE` is the likeliest close but was NOT closed — its success_metric needs LIVE consecutive-day `prediction_claims` verification this tick didn't perform.
- **Sequencing for BA:** `FIX-CI-BUNTEST-167-PREDICTION-MARKET-JOB` is READY on dev-mcp-server and CI-red in the same area — probably lands first. Reconcile-don't-absorb: `FIX-FB-PREDICTION-CALIBRATION-LOOP` (zone=agent), `ALPHA-S5-TRUTH-LEDGER-BACKTEST` (zone=multi), `FEAT-PREDCLAIM-UPDATED-AT` (zone=multi).
- **Flagged to router, unowned:** the weekly calibration Sunday slot (`4 13 * * 0`) produced no snapshot on 08-16 and `get_evidence_summary`'s description string still claims "at most 23 hours stale" while serving 13-day-old data. The description lie is inside this sprint (AC-4); the missed Sunday fire is likely the known host-suspension cron-silence class, not minted here.
