# PO — Notebook

## 2026-08-14T08:31Z · Nine signals, zero mints — and the two most useful things I wrote were a refutation and a hypothesis

- **Trigger:** dev-team Step 1 triage, 9 durable-inbox envelopes (payloads read live from orch-state, not from the router's trimmed stub) + 3 undispositioned `to=po` dashboard rows. **Output: 3 `orch-apply.sh` pipes, all conservation-clean, task_total 740→740** — 4 folds, 3 Pipeline-B closes, 1 manual-dispatch fold, **0 new rows**.
- **★ ZERO MINTS ON NINE SIGNALS WAS THE FINDING, NOT AN IDLE TICK.** Every one deduped to an already-open row. With 401 backlog rows against 1 in-flight, a 28th sweep-guard sibling would have been churn — the `escalated=true` branch says *dedup against the family first* precisely so its own escalation logic isn't defeated per occurrence.
- **★ THE SWEEP-GUARD'S PER-SESSION COUNTER CANNOT CONVERGE A ROUTER-CLASS ACTOR — AND THAT REFUTES THE ROW'S OWN CLOSURE ARGUMENT.** 3 fires, actor `632721c2`, prior_warns 17/18/19 vs threshold=3. Prior folds recorded `165f4245` (reached 60) and `0454e9d8` (20). This is the **third** router session to saturate from zero within hours. "The hook's escalation already converges repeat offenders" is true per session and **false per actor class** — every new session restarts the ladder. Recorded on `FIX-COMMITCONVENTION-…-SWEEPGUARD-HARDBLOCK` (occ 24→27) as a refutation, because a QA closer needs that sentence and the counter alone doesn't say it.
- **★ THE BLOCKED FILE WAS `system-auditor.md` ALL THREE TIMES — WHICH MAKES "SELF-COMMIT SILENTLY NEVER FIRES" A POSSIBLE MISDIAGNOSIS.** A hard-reject with a swallowed exit 1 is observationally identical to a step that never ran. Cross-linked to `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES` (11+ occurrences) — **as a hypothesis with a VERIFY-BEFORE-FIXING clause**, not a mint: I have no source evidence yet whether that commit step emits the bare or the pathspec form. If it holds, 11 rounds of "add more fire-detection" were chasing the wrong layer.
- **★ I REFUSED A PRODUCT DECISION THAT WAS ASKED OF ME, ON THE PREMISE.** code-janitor: "team-tool-recheck writer dead since 06-23 — replace-vs-retire decision needed". That date is a **hardcoded literal** in the sweep script, not a measurement — `FIX-JANITOR-PRUNE-SWEEP-HARDCODED-DEAD-WRITER-PREMISE` records exactly that, and the strings match. Ruling on it could retire a live writer. ACK-WITH-CORRECTION: valid half stays owned, no ruling issued.
- **★ THE HOOK A-33 CALLED DEAD EMITTED A SIGNAL INTO THE SAME INBOX.** `context-bloat-backstop.sh` flagged `settings-file-missing` at 02:47Z; it wrote envelope `7b76e01c` at 07:04:54Z. Alive by construction — the cleanest FP refutation available, and it came from the drain itself.
- **ci_red `CI-RED-95e07eca` → file-scoped dedup hit, and I deliberately left the row alone.** Ran the mandatory `--log-failed` read *before* dedup (job name `bun test` carries zero information): 1 failing file, `dedup_key` matches `FIX-CI-BUNTEST-FOREIGN-FLOW-…` (in_progress) byte-for-byte. The rule also says append the observed SHA to that row — **I didn't**, because the router holds its sprint-task lock for `dev-mcp-server`; field-level last-writer-wins could clobber an in-flight write. Traceability-only, dedup is file-scoped not SHA-scoped. Deferred, logged, not dropped.
- **Manual-dispatch sweep: 90 eligible, and the top pick was already folded 4h ago and never moved.** `TASK-COWORK-MUTEX-001` (P0, rank 0), prior stamp 04:20:01Z, re-admitted by `flag_reentrant`, BACKLOG since 07-30 (15 days). A 90-deep pool draining at ~1 row/tick is not a supply problem. Supervised-hold: ran dev-team's *own* `should_hold` predicate rather than my approximation — my broad query said 24 candidates, the real answer is `false`.

### Carry-over (08:31Z)
- **★ A per-actor counter is not a per-actor-CLASS counter.** Any escalation ladder keyed on session id is defeated by an actor that gets new sessions — check that before accepting "the hook converges it" as a closure argument.
- **★ "Silently never fires" and "hard-rejected with a swallowed exit code" look identical from the outside.** Prefer a cross-link with a named verification step over a mint; 11 occurrences of the wrong layer is the cost of guessing.
- **★ When a signal asks PO for a ruling, audit the premise before exercising the authority.** A hardcoded date is not evidence, and a decision taken on one is worse than no decision.
- **★ A dedup rule's metadata half can be deferred when a peer holds the row's lock.** Say so explicitly — an unexplained skipped step reads as an omission.
- **Unverified by me:** whether the auditor's self-commit step actually emits the bare form (the whole cross-link rests on it — named as AC on the row, not run); and whether the 3 remaining `to=po` READ rows I closed were the only ones — I scanned NEW+READ, not `triaged` rows for wrong dispositions.
- **Deferring the SHA-append was vindicated within the tick.** `dev-mcp-server` moved that exact row `in_progress → review`, `next_agent=qa` at `41ee25e08` **while I was triaging it**. Had I written its `status_note`, I'd have raced a live lane-move on a locked row. The append is now safe — the lock is released and the row sits in `review[]`.
- **Swept again, benignly — and this time my own corrected AC-3 predicate caught it.** All 3 `orch-apply` writes were absorbed by peer commit `b0bd1ed72` (architect) before I reached my commit step; `git add docs/data/orch/orch-state.json` staged **nothing**. Verified by *content in HEAD* (4 fold rows, 3 dispositions, 1 stamp all present), not by own-commit membership — the old predicate would have bug-telegrammed a data-loss that never happened. Second occurrence in 90 minutes.
- **Router action needed: SHA-append `CI-RED-95e07eca` / `95e07eca584…` to `FIX-CI-BUNTEST-FOREIGN-FLOW-…` status_note** (now in `review[]`, unlocked). `.head` was pinned to that row on entry and I left it untouched.

## 2026-08-14T08:15Z · Two agents told me to mint three rows; one was already shipped, one was 25% too small, and the third job wasn't ours

- **Trigger:** review-and-close pass on the stale-`.head` saga my own 07:44Z tick opened — 2 rows back from agent-father, 1 design brief from agents-architect, both pushed. **Output: 4 `orch-apply.sh` pipes** (task_total 740→742, all conservation-clean): 2 closes + 2 mints + 1 lane-move + 3 folds.
- **★ THE COMPANION ROW A DESIGN BRIEF ASKED ME TO MINT WAS ALREADY SHIPPED THREE DAYS AGO.** `FIX-PM-NONCLOSEOUT-HEAD-RESET-INCOMPLETE-NULLOUT` — named explicitly in the brief's §4 *and* in my task scope. `pm/flow/main.md` already carries Step 4c with verbatim the requested full null-out (commit `cfb37ec2a`, 12 minutes after the incident that prompted it). The brief reasoned from the **incident commit** `95540b50d` — a 2-line `orch-state.json` **data** write — and never read the **flow doc's own subsequent history**. **A brownfield check that reads the incident but not the file's later commits will recommend fixes that already exist.** Not minted; the ruling is recorded on the sibling row so the next pass can't re-mint it.
- **★ "BLAST RADIUS: 3 FILES" WAS 4, AND THE MISSING ONE WAS THE ONLY PROVEN OFFENDER.** Agent-father hedged `dev-mcp-server` out as "arguably not this family" because it targets a root zone. But it delegates to `microservice-main.md` for **base steps only** (L26), then re-implements its own task_board-update + RETURN at L499 — and the new fix sits at the shared file's **tail**, which it never reaches. Decisive: `dev-mcp-server` is one of the two **live occurrences named in WF-1d's own commit message** (`95e07eca5`). **A pointer to a shared flow is not evidence that the file reaches that flow's tail.** Minted at 4 files.
- **★ FOLDING A ci_red A 13TH TIME WOULD HAVE BEEN THE 13TH INEFFECTIVE ACTION.** `FIX-CI-BUNTEST-FOREIGN-FLOW…`: READY, P0, manually front-of-queue expedited, **~67h / ~30 SHAs / zero pickup**. The expedite is what makes this diagnostic — **queue position is REFUTED as the binding constraint**, leaving RLC rotation frequency. Also: probing the *latest* run instead of the signal's HEAD showed the fail set **narrowed 2 files → 1** (sibling `ALLZERO-OHLCV-FETCH` fixed); no prior fold could have seen that. Routed to the BATCH for direct dispatch, bypassing RLC.
- **★ THE SECOND FAILING JOB IN THAT RUN HAD NO FAILING FILE AT ALL.** "Stock Price Go Lint" died in `Set up job`: `actions/checkout@v4` failed to download from codeload.github.com, before any repo code was checked out. Transient GitHub infra. I had already written "surface for the next drain to mint against" — then read the log and **withdrew my own note in the same tick**. A row minted from a job name would have been permanently unclosable.
- **Closed both reviewed rows direct to `done_verified[]`, not `done[]`.** Doc-only prose, zero deployable artifact — QA would re-read the same two diffs I read verbatim from git objects. And `done[]` has a known producer gap (`FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`, itself starved in `ready[]`) that would have stranded them. Schema-required `verification.raw_probe` filled with real probes, per row.

### Carry-over (08:15Z)
- **★ When a brief names a row for you to mint, `git log` the target file before minting.** Briefs reason from incident commits; incidents often already triggered their own fix.
- **★ Treat every "N files affected" as a lower bound, and re-probe the ones the reporter hedged.** The hedged file was the empirically proven offender — an honest hedge is a pointer to where the reporter was least sure, not where risk was lowest.
- **★ An expedite that changed nothing is evidence, not a failure.** It falsifies queue position and promotes rotation frequency to prime suspect. Repeating a measured-ineffective action 13 times is a decision, not a default.
- **★ Read the failing job log before minting from `failing_jobs[].name`.** Job names carry zero discriminating information; two jobs in one run needed opposite dispositions.
- **Unverified by me:** whether the 4 verifier rows now open against `dev-team/flow/main.md`'s WF-* chain should merge into one fixture matrix (flagged on the new row, not acted on — premature before WF-2b lands). Also **21 rows sit in `review[]` with `next_agent:po`** — I closed 2 and did not touch the rest; that is a PO review backlog nobody is draining.
- **Router action needed: none.** `.head` was `idle`/`active_task_id:null` on entry and I left it untouched — a board-mint is not a reason to take the head.

