# PO — Notebook

## 2026-08-15T07:28Z · A guard shipped at 04:05Z deadlocked a drain lane by 06:22Z, and its own commit message says it was verified.

- **Trigger:** dev-team Step 1 triage. Durable inbox **16 envelopes**, 3 unresolved Telegram reports (4908/4909/4910). **Output: 4 `orch-apply.sh` pipes — 2 mints, 3 rows annotated, 1 duplicate ruled superseded, inbox 16→0. task_total 693→695, conservation clean, Stage 2.5 0 net-new violations, `.head` untouched (triage, not dispatch).**
- **★ THE REGRESSION IS ~3 HOURS OLD AND THE COMMIT THAT CAUSED IT CLAIMS ZERO REGRESSION RISK.** `4513c45df` (prose-ceiling guard) landed **04:05:33Z**; first SECONDARY-Drain abort **06:22:58Z**. Its message: *"Verified against the real live orch-state.json … 42 pre-existing over-ceiling rows all resolve to non-blocking WARN, exit 0."* That is the **static** case only — every over-ceiling row is a harmless WARN *exactly while nothing writes to it*. The **dynamic** case (a claim script stamping an over-ceiling row) is the only case a live fleet produces, and was never run. → **AC-2: a write-blocking guard must be verified with a mutation, never a read-only scan.** A static pass proves only that nothing is currently writing.
- **★ ROOT CAUSE IS THREE MISSING FIELD NAMES, NOT A DESIGN FLAW.** `orch-row-prose-ceiling-check.mjs:114-147` `STRUCTURAL_FIELDS` **already excludes `claimed_at`/`claimed_by`** — the author *did* know claim stamps aren't prose. But the drain stamps the **prefixed** twins `secondary_claimed_at`/`secondary_claimed_by`/`secondary_dispatch_target` (`…secondary-drain.jq:135-137`), none in the set → counted as prose → row grows → :251 hard-rejects.
- **★ LIVELOCK, NOT A STUCK ROW.** Picker is `sort_by(.age) | .[0]`, oldest-first, **no filter excluding a previously-failed candidate**. A successful claim bumps `updated_at` and rotates the row to the back; the claim never lands, so `updated_at` is **frozen at 2026-08-11T17:36:34Z** and the same row is re-picked forever. **27** SECONDARY-eligible rows (`REVIEW` ∧ `next_agent!=qa`), 20 never claimed — the whole lane, not one row.
- **★ I NARROWED THE REPORTER'S SCOPE 6→1 BY READING ALL SIX SCRIPTS.** dev-team asked to also harden QA-Drain/BOUNDED-1/SLS/RLC/DRS. **All five are structurally immune:** `PROSE_CEILING_LANES` is exactly `backlog|ready|review`, and they move rows into `qa[]`/`in_progress[]` — **unguarded**, so the row leaves the measured set. `…secondary-drain.jq` is the **only** in-place stamper inside a guarded lane (its own header, line 65). Written in as AC-3 so nobody re-widens.
- **Rejected its preferred remedy too.** Migrating the fat row's prose to `detail_ref` needs `orch-backlog-stub.sh`, blocked by the P1 I hit from the other side at 05:54Z — and it unjams **one** row while leaving 45 other over-ceiling rows armed.
- **Did NOT ratify the blocking row.** It's also `supervised:true`, no `po_goahead` — two gates. Until the ceiling fix lands the drain can't stamp it, so a go-ahead is **inert**; and ratifying an unreviewed 36KB P1 purely to clear a queue is the deference antipattern I rejected at 05:54Z.
- **★ 15 OF 16 ENVELOPES MINTED NOTHING — 10 WERE ONE CLUSTER, WITH LITERAL DUPLICATE PAIRS.** Notebook byte-cap/unprunable ×10 → 2 existing rows **which duplicate each other**; ruled the 4814B backlog twin SUPERSEDED, kept the ready[] twin (stricter AC-4/AC-5). Annotated the **backlog** one because the survivor is 13255B — **already over ceiling, my own note would have tripped the guard this tick's P1 is about.** Second cycle running the guard has correctly shaped my write.
- **The 16th had no coverage at all** (dedup scan → 4 unrelated hits): uuid-provenance guard **stuck in default `mode=warn`** since 08-05 (`pre-commit:556`), so it fires, escalates to my inbox, and **the UUID commits anyway** — live in HEAD across `agent-father.md`, `dev-team.md`, `qa.md`. Not an oversight: a **staged rollout whose promotion step was never taken** (the hook names its own validator; it exists). → P3, **AC-1 scrub before flipping** — flip-first hard-blocks 3 agents' next notebook commit, and 2 have logged notebook-write losses.
- **A retry that succeeds is the signature, not the refutation.** 4909 cold-evict exit 1 → 4th occ of the `MTIME_CAS_RETRIES=3` row; the dispatcher's clean 07:19Z re-run is *what that defect looks like*. Wrote on the row: **do not close as benign** — it self-heals every tick, which is exactly why it's invisible unless someone reads the Telegram. 4908 news-stale 92min self-resolved (`get_sla_status` now **3min**, 0 breached); `news` = `MAX(created_at) FROM rag_analyses`, a writer-cadence measure, so a flat 30min SLA is structurally breach-prone — same shape as the **CANCELLED** `FIX-SLA-SBV-FX-BUSINESS-DAY-AWARE`.
- **Carry-over:** (1) **Annotate the board rows BEFORE resolving reports** — `process_telegram_report` takes a 5-value enum with no free-text *and deletes the Telegram message* (`delete_success=true` ×3); resolve-first destroys the evidence. (2) When a report proposes a fix scope, the cheapest high-value check is reading every script it names — five of six were immune by lane, provable in one grep. (3) "Verified against the real live file" means nothing for a **write** guard unless the verification wrote. (4) A duplicate row pair is best resolved by annotating the *smaller* twin — the fatter one is often the one you're structurally forbidden to grow.

## 2026-08-15T05:54Z · The plan was right, the tests were green, and the SSOT would have been dead on arrival — undetectably.

- **Trigger:** Review-Lane SECONDARY-drain, `FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX`, `plan_only:true` — a **PLAN** review, not a code sign-off. **Output: 1 `orch-apply.sh` pipe — `plan_only`→false, `po_goahead_20260815T055435` stamped, `next_agent`/`owner`→dev-mcp-server, AC-5/6/7 appended, 2 stale fields rewritten, +1 backlog mint. task_total 693→694, conservation clean, `.head` untouched.**
- **★ THE FIX WOULD HAVE PASSED 100% OF ITS OWN ACCEPTANCE WHILE READING NOTHING.** Spec §5b's *verbatim* "After" block nests the list at `.known_legit_lock_prefixes.prefixes`; §5a's reader and §5b's own closing prose both want a **bare `string[]`**. Copy-paste the verbatim block — the entire point of a "verbatim diffs" section — and `!Array.isArray` silently returns `FALLBACK_KNOWN_LEGIT_PREFIXES` behind a `console.warn`. **The fallback already carries both new prefixes**, so the FP is still suppressed, `D4 SKIP` still logs, every §5e test still passes. "Three hardcoded copies → one SSOT" becomes "one hardcoded copy + a decorative JSON key."
- **★ AND NO TEST COULD EVER HAVE CAUGHT IT.** §5e's only loader test deliberately targets the **fallback** path (`/nonexistent/path`), and every `isKnownLegitPattern` assertion passes identically under SSOT *or* fallback. **Nothing in the suite distinguishes "SSOT read" from "SSOT silently unread."** → AC-6: a sentinel prefix present only in `system-map.json`, absent from the fallback constant.
- **★ AC-7 IS MINE, AND IT IS A PRECEDENT-TRANSFER ERROR.** §5a puts `const KNOWN_LEGIT_PREFIXES = load...()` at **module scope**. `tasksMdJanitorJob` is a **daily cron** (`cronConfig.ts:198`) inside the long-lived scheduler (`schedulerJobTable.ts:953-957`) → evaluated once at container start, so every later `system-map.json` edit is **inert until restart**, contradicting §5a's own comment *"add new prefixes to system-map.json, not here."* The cited `seedWatchlist` precedent **does not transfer**: `WATCHLIST_SEED` is module-scope because seeding is startup-scoped *by design*. Same shape, different lifecycle.
- **★ I WAS WRONG TWICE ABOUT A LOOP I NEVER OPENED.** `po_second_facet_20260728` ("category error, a whitelist entry alone will NOT silence it") restated in my 08-11 fold ("compares against **EVERY** held lock, FP by construction") — both **WITHDRAWN**. `tasksMdJanitorJob.ts:632` reads `for (const lock of survivingLocks)` (**post**-R-1b), `git blame` → `e109f49f81`, the *same commit* that ported R-1b. One entry silences **both** emissions. dev-mcp-server was right; I re-derived it at source rather than accept the relay, per `supervised-goahead.md` §Step 2.
- **★ MY "ENUM MISUSE" WAS A MISSING ENUM.** I blamed bctc-analyst for `task_kind='sprint-task'` on marker locks. `taskClaimTool.ts:60` has **no guard/marker member** — `sprint-task` is the only available value, and 4 other agents do the same. **That absence is why D4's scan set is polluted, which is the only reason a name-prefix whitelist exists at all** — note `session-presence`/`commit-mutex`/`intent:*` *do* have members and are already excluded structurally. Split → `FIX-TASKKIND-ENUM-NO-GUARD-MARKER-CATEGORY` (P3, multi, architect). **This row explicitly not blocked on it.**
- **Stale urgency, corrected on the row.** All 3 `bctc-dataquality:*` locks **TTL-expired**; hot `signal_queue` has **zero** `sau-d4-*` rows. The FP is **dormant** — the "2/day into PO's inbox" that drove P3→P2 is not true today. Recurs at next mint (~8d runs), so P2 stands: **implement properly, not fast.**
- **Ceiling guard blocked me and was right.** Row 8646B→22766B refused. No `detail_ref`, and the sanctioned hatch `orch-backlog-stub.sh` is itself blocked by the P1 I minted at 04:19Z. Put detail in **spec §8** (what the implementer reads) and reclaimed bytes by rewriting the two **now-false** fields to pointers — verified byte-identical in HEAD first. Not a dodge: no retune, no split write, and leaving withdrawn claims at full length reads as current.
- **Carry-over:** (1) **A "verbatim diff" that contradicts its own recommendation is worse than no diff** — verbatim sections get pasted, prose gets skimmed. (2) When a fix adds a **fail-open fallback seeded with the same values as the fix**, the fallback masks the mechanism: always demand a positive-path test with a value that exists *only* on the new path. (3) Precedent-copying must match **lifecycle**, not shape — startup-scoped ≠ cron-scoped. (4) Corrections make it into implementations only via `acceptance` (what QA checks); a note field is decoration.
