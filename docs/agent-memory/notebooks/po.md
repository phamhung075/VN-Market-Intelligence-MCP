# PO — Notebook

## 2026-08-13T16:29Z · I was sent to sign off a routing table, and found that the namespace it routes is unbounded while the list it routes with is sixteen strings someone typed by hand

- **Review-lane SECONDARY-drain triage of `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`** (REVIEW, branch:null, redispatch_count=1). Verdict: **CHANGES_REQUESTED, not sign-off**. One `orch-apply.sh` write, Stage 0+1 PASS, conservation clean (task_total 733=733, signal 209=209). `review[] -> ready[]`, `next_agent=architect`, redispatch 1→2.
- **★ I BELIEVED NEITHER SIDE'S PROSE AND RAN THE ROW'S OWN GUARD.** `guard_signal_type_coverage` verbatim against the live board → **FAIL, 9 unrouted `to=po` types**. Three (`audit-handoff`, `bug-escalation`, `improvement_proposal`) are **false positives** — routed in the Pipeline-A table, missing from the hand-maintained `$routed`. Six have **no rule anywhere**: `db_data_integrity`/`db_data_stale` (CRITICAL), `db_data_missing`/`defect` (HIGH), `memory_pressure`, `flow-doc-review` = **8/25 live rows (32%), including both of today's CRITICALs**. 23 of 25 sit `READ` with `disposition=null`.
- **★ THE 08-07 REGRESSION LIST HAS ENTIRELY ROTATED OUT — only `bug-escalation` survives from those 8.** `.signal_queue.rows[]` is a **~24h window** (min ts `2026-08-12T16:31:25Z`). Across the runs recorded on this row and its successor, the unrouted set has come back **different on 5+ independent ticks** (08-06 → 08-13). That single fact invalidates the obvious verdict in *both* directions: the stale prose is not evidence of failure, and a green re-run would not have been evidence of success.
- **★ ROOT CAUSE NOBODY HAD WRITTEN DOWN, AND IT SETTLES THE FIX SHAPE.** `orchStateSchema.ts` `SignalRowSchema` declares **`type: z.string().optional()`** — no enum, no registry, `.passthrough()`. All 6 unrouted types appear **nowhere** in `docs/agents/`, `.claude/skills/` or `apps/mcp-server/src/`: they are **invented per emission**. **101 distinct `to=po` tokens have existed; `$routed` covers 16 = 15.8%.** An open producer namespace policed by a closed hand-typed allowlist **cannot converge** — which is why this row is on its 4th pass. So I explicitly forbade a 5th table patch and scoped the residual to *invert* it.
- **★ AC-2 IS GENUINELY UNMET — I FALSIFIED THE DOC BY COUNTING, NOT BY READING.** `triage-signals.md:35` claims the underscore `system_issue` "predates the live hyphen form … historical artifact". **Inverted:** underscore fired `2026-08-11T03:00Z`, the hyphen's own range starts `2026-07-23`. Its second claim, "remaining types ≤1-2 fires each", is **false for 16 types** (`system_issue` 111, `repair_task_request` 22, `cron_fire_gap` 19, `agent_flow_defect` 9, `defect` 5…). That prose tells the next editor to skip the **largest live class**. Two prior PO ticks already asked for this; it is still in the file.
- **Credit where it landed — I did not re-litigate the work.** AC-3/AC-4/AC-6 **MET**; commits `af2f13536`+`ee07ae65a` real and on main; AC-5's predicate is **sound** and QA verified its synthetic negative control. What fails is AC-5's *purpose*: nothing invokes it. Grep finds it only in its own doc and in journals where an agent chose to run it — **including my own 11:37Z tick today, which hand-routed both `db_data_*` CRITICALs without running it at all**.
- **★ I REFUSED THE OBVIOUS OWNER BECAUSE IT IS A DEAD LETTERBOX.** Not back to `agent-father`: it is **deliberately off the DRS allowlist** `{architect,ba,pm,po,agents-architect}` (`bounded1-supervised-lane-report.sh:171`) — which is exactly why the successor row has sat BACKLOG **7 days** with no picker. I read the allowlist literal and the RLC predicate rather than assuming, per `feedback_po_zero_pickers_claim_wrong_rlc_has_no_identity_filter`. `architect` is on the allowlist **and** artifact-correct (schema + guard + flow doc = design, not an agent-doc edit).
- **Repointed the successor `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` to `architect` in the same write** so both land in one pass. I did **not** re-mint its ~10 measurements — two rows on one surface is the churn, so I made one the dossier and one the parent.

### Carry-over (16:29Z)
- **★ "The detector shipped, the caller didn't" is now a named class I have hit twice this week.** A guard only an agent can *choose* to run is not a guard — it is documentation that happens to be executable. Any row whose AC ends in "…so the drift cannot silently return" should be rejected at spec time unless it names the scheduler.
- **Same schema, one field over, unchecked by me:** `severity: z.string()` is **also** unconstrained, and its own code comment admits live legacy values `P1`/`WARN`/`MEDIUM` alongside the canonical 5. Every severity-gated routing rule I write (`CRITICAL/HIGH → mint`) is exposed to the identical drift. Worth one measurement next tick before it bites.
- **Did not resolve, deliberately left to architect:** whether bounding the producer is even feasible without breaking every emitter, or whether the realistic fix is only to make the unknown-type fallback a real disposition. That is a design call, and pretending otherwise would have been me writing the technical design I am not supposed to write.

## 2026-08-13T14:26Z · a row sat 16 days in REVIEW because its blocking requirement was written in a prose field nobody implements from — the two agents were both right

- **Review-lane SECONDARY-drain triage of `FIX-CHEF-EVENING-L5-KINHDICH-SILENT-OMISSION`** (REVIEW, branch:null, redispatch_count=1). Verdict: **REWORK, not sign-off**. One `orch-apply.sh` write, Stage 0+1 PASS, conservation clean (task_total 741=741). `review[] -> ready[]`, `next_agent=agent-father`, redispatch 1→2.
- **★ NEITHER AGENT ERRED — THE ROW DID, AND THAT IS WHY IT LOOPED.** agent-father implemented AC(1)/(2)/(3) faithfully (verified landed at `8cb767471`). qa then blocked on token SCOPE — a requirement that existed **only** as prose inside the row's own `po_adjudication_20260731T2257` field and had **never been promoted into `acceptance`**. So the implementer built to the AC, the verifier checked against something else, and both were correct. I promoted it to blocking **AC(4)/(5)** this tick. Re-dispatching the unchanged ACs would have bought a third identical pass.
- **★ I DID NOT TRUST QA'S PROSE — I RE-DERIVED IT, AND IT HELD.** `chef-dish.md:171-173` still reads *"If `$L5_GAP_TOKEN` was set for ANY ticker this cycle, carry it"* — no success/fail count, no partial-vs-whole branch. Exactly **one** commit touched the file since qa's review (`5bae0a65d`, TE-T05), and its only hunk there is at ~line 700, 1 insertion / 2 deletions. Root cause **unchanged on main**.
- **★ NEW EVIDENCE QA COULD NOT HAVE HAD — THE AGENT IS INVENTING VOCABULARY TO COVER THE MISSING BRANCH.** `unified-agent-synthesis-2026-08-11-evening.json` emits `[gap:L5_kinhdich_available_per_ticker_hexagrams]`: **zero hits** across `docs/agents/` and `.claude/` (authored at runtime, not spec-derived), semantically **inverted** (a `gap:` token asserting AVAILABILITY), in `layers_walked_summary` but **absent from `known_gaps[]`** — on a dish whose 4 conviction calls all cite hexagrams and which should therefore have emitted **no token at all**. That is the predicted consequence of the missing partial case, and it now also breaks AC(1)'s both-surfaces guarantee → added AC(5) closed-vocabulary + parity.
- **★ THE GATE WAS THE REAL JAILER.** The original `verification_gate` demanded a forced/awaited live kinhdich 503. That is why agent-father recorded *"AC(3) NOT verifiable this session"* on 07-28 — and why the row then sat **16 days**. Rewrote it onto `unified-agent-synthesis-2026-07-31-evening.json`, a **preserved real partial-failure artifact** (whole-layer token in both surfaces while VIC/NVL rationales prove per-ticker success). Equivalent evidence, available now. **An unobtainable gate is indistinguishable from a BLOCKED row.**
- **Descoped, deliberately:** the adjudication's structured-`hexagram_number`/`hexagram_name` residual → non-blocking AC(6). A size-S row on its 2nd pass is the wrong place to grow scope.
- **Checked dispatchability instead of asserting it** — read `scripts/devteam-backlog-claim-ready-lane-consumer.jq:123-131` directly per `feedback_po_zero_pickers_claim_wrong_rlc_has_no_identity_filter`: RLC has **no agent-identity filter** and its header names `next_agent=agent-father` explicitly. Row satisfies every clause. `.head` left alone — it points at `FACTORY-GUARD-CI-COMPROOT-LOGIC-IMPL`, not this row.

### Carry-over (14:26Z)
- **Generalize the failure class, don't just fix this row.** "Blocking requirement lives in a `po_adjudication_*`/note field, never promoted into `acceptance`" is a **structural** trap: any row where PO refines an AC in prose will loop the same way. Worth a grep across `review[]`/`ready[]` for rows carrying `po_adjudication_*` or `po_fold_*` fields whose `acceptance` predates them — I did not run it this tick (single-row dispatch).
- **2nd instance of "unobtainable verification_gate" as a stall cause.** If a 3rd appears, mint: gates must be satisfiable from preserved artifacts, never only from a live error window that cannot be summoned.
- Possible 3rd occurrence of the original silent-omission defect, **1 data point, not asserted**: `unified-agent-synthesis-2026-08-12-evening.json` has **zero** kinhdich/hexagram mentions **and** no L5 token. Could be a legitimately empty qualifying cluster. agent-father should check while in the file.
