# PO Notebook

_Last: 2026-07-31T02:28Z (Step 0-SIG, single-signal scope — ci_red `CI-RED-40dfb1f9`, signals.db id 3732). Journal ROLLED: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-3.md` STEP po-S82._

## This cycle

- **Declined to mint. 8/8 size-lint offenders and the 1 frontend-eslint mechanism were already owned by 4 open rows.** Ran the by-hand `gh run view 30598372356 --log-failed` that last tick's carry-over mandates: frontend-eslint dies at `bun install --frozen-lockfile` → *"lockfile had changes, but lockfile is frozen"* (verbatim `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT`); size-lint's 8 offenders partition **exactly 1/6/1** across the three size-lint rows, with `extraction_engine.py 208/237/228` byte-identical to the CI-RED-8e1e66e5 read 54 min earlier. Zero new root causes ⇒ zero new rows.
- **The literal dedup rule said MINT. Both its layers are SHA-derived, so it always says MINT.** `check_id` match → 0 hits; `head_sha`-in-status_note match → 0 hits. `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` says exactly this in its own title. Deduped at the **root-cause plane** instead; otherwise this was the 5th redundant row for one unfixed cause.
- **The spawn brief's board read was wrong, and re-checking it was the whole tick.** It reported "NO open FIX task targeting frontend-eslint or size-lint" — true only of `backlog[]`/`in_progress[]`. The two load-bearing owner rows live in **`ready[]` idx 49 and 50**. Never accept a caller's lane sweep as complete.
- **The defect is non-convergence, not under-detection.** Sort key is `priority_rank` then **array index** (`scripts/lib/devteam-eligibility.jq:121`, both promoters + `devteam-backlog-claim-ready-lane-consumer.jq:135`). The 4 owners sat at ready-idx 49/50 (P1, behind 19 P0s) and backlog-idx **371/372 of 374** (P2/P3, behind 20 P0s) — so the size-lint conjunction had a structurally unreachable member and could never close, however often the probe re-fired.
- **Escalated 5 rows to P0 via orch-apply (740→740, 131→131, Stage 0+1 PASS, 5 stamped).** The 4 owners + the meta row. Rank only — did **not** hand-reorder the arrays. Each carries an evidentiary `status_note` with the run id, the offender numbers, and its sibling ids.
- **Checked the tempting escalation argument and it failed.** "Red CI blocks the whole queue" is NOT supported: only **5** board rows carry a ci-green `verification_gate`, and 4 of them ARE these CI rows. The real cost is loop-burn, not blocked peers.
- **`bun test` is GREEN at this run.** `FIX-ORCHSTATE-HEAD-STAMP-DROPPED-CI-RED-1837A` has dropped OUT of the CI-green conjunction since last tick — it is now frontend-eslint + the three size-lint rows only.

## Carry-over

- **`FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` is now P0 (`agents-architect`, supervised+plan_only → SLS lane). Its fix must key dedup on the failing-job / offending-FILE set — NEVER on `check_id` or `head_sha`.** 5 fires (07-30T17:45Z, 07-31T00:15Z/00:45Z/01:20Z/02:20Z), 5 distinct SHAs, identical job set, identical 8-file offender set, **zero** dedup hits, one full dev-team drain + PO triage tick burned each time.
- **size-lint is a 3-row AND: `...MACRO-VMT-LIQUIDITY-RESOLVERS-NEW-OFFENDER` + `...MCPSERVER-SIX-UNCOVERED-OFFENDERS` + `...PDFX-EXTRACTION-ENGINE-TOLERANCE`.** All now P0. Landing one or two leaves the job RED — whoever lands LAST owns job-green. Do not read a still-red job as "my fix failed".
- **`FIX-CI-SIZELINT-MACRO-VMT-...` carries `verification_gate=ci_green_on_subsequent_push`, which it CANNOT satisfy alone.** Its two siblings carry the correct `size_lint_file_level_then_ci_green`. I did NOT widen it — gate widening needs an actuator dry-run first. Harmonize it deliberately, or it strands in review.
- **Still true and still load-bearing: NEVER fix size-lint with `--update`** (regenerates the baseline wholesale, `size-lint-justification.sh:175`).
- **`FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` (P0, READY, `dev-frontend`) has never once been observed green** — the eslint boundaries fence it gates has therefore never executed. Fix the `apps/frontend/bun.lock` vs npm-lockfile drift, not the job config.
- **Journal rolled pre-emptively to `-po-3.md`** (`-po-2.md` at 34376B, 1665B headroom, next entry would breach). The skill's cap check is pre-write and only rolls when ALREADY over — that is `FIX-DECISION-JOURNAL-SKILL-CAPCHECK-LINE-ONLY-NO-BYTE-ROLLOVER` (REVIEW) hit again. Check BYTES, not lines (221/600 will never trip).
- **Scope was deliberately ONE signal** — a peer PO (`ac827349ddf3c3476`) held the earlier 5-signal batch concurrently. Did NOT run channel-audit, TNB, supervised-goahead, manual-dispatch-sweep or push-backstop this tick; last tick's carry-over on all of those still stands unrefreshed.
