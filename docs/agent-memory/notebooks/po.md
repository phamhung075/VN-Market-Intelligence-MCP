# PO — Notebook

## 2026-08-08T08:59Z · review-lane SECONDARY-drain — FACTORY-NEWS-go-server-tier-split DONE_VERIFIED; its rebuild gate disproved, not lifted

### What actually happened
- One `orch-apply.sh` pipe, PASS, conservation clean (`task_total` 778→778, `signal_total` 229→229, `.head` untouched — it still points at `FIX-ORPHAN-FR1-FR2-INFRA-HEARTBEAT-LADDER` under the qa-drain). Row moved `review[]`/REVIEW → `done_verified[]`/DONE_VERIFIED after 15 days stale. First pipe rejected: `next_agent:null` fails Zod (`expected string`) — on a terminal row the field must be **absent**, not null.
- Ruling: `docs/agent-memory/decisions/ruling-20260808T0859Z-newsfetch-go-tier-split-po.md`. 0 mints.

### Decisions worth keeping
- **The row's `rebuild_required:true` is a false premise, not a pending action — and that was the whole ruling.** `apps/news-fetch` ships **two programs**. The Dockerfile is Bun+Playwright and its runtime stage copies only `node_modules`/`src`/`composition-root.ts`/`package.json`/`tsconfig.json`; it never runs `go build`. Container `Cmd=["bun","run","src/index.ts"]`; `docker exec … ls /app` shows **zero Go artifacts** — no `cmd/`, no `internal/`. `ci.yml:316` states the design outright ("alongside the TS/Bun HTTP service"). So `docker compose build news-fetch` would ship **0 bytes** of this task while carrying peer-recreate risk. The 2026-06-15 audit that minted the row assumed the Go server *is* the runtime.
- **The near-miss: live `/health` returns `{"port":5008}` — which looks exactly like the un-fixed hardcoded literal this task removed.** It is served by `src/index.ts`, a *different program* from the Go binary that was edited. Reading it as the same plane would have produced a false rework verdict. Wrong-plane corroboration, caught only by asking which binary answers the port.
- **"PENDING-USER-GATED" was moot on two independent grounds, and the stronger one is not the one the brief expected.** PO has full rebuild autonomy — but the gated action was never *applicable*. A gate can be stale because it was lifted, or because it guards nothing; only the second also invalidates the DoD clause that depends on it.
- **DoD clause "RAW-verify … after rebuild" is unsatisfiable-as-written for an uncontainerized tier.** Developer's local-binary run (`PORT=15008` → `/health` echoed 15008, live vneconomy/vnexpress RSS → real rows in `rag_analyses`, sqlite3-inspected) is the correct *and only available* served-route parity check. Holding a green row BLOCKED on an impossible clause is how it rotted 15 days.
- **Re-ran every gate rather than banking the note.** `go build`/`go vet` rc=0, `go test` 2/2 ok, `golangci-lint` **0 issues** (verbatim the CI job `news-fetch-go-lint`), `composition-root-logic-gate` **0 violations**. Zero drift in 15 days: the only post-landing commit touching `apps/news-fetch` is `66ab9deb3`, TS/eslint-only. `gofmt -l` does flag 2 files — both last touched by `261a5d182` (2026-06-10 GFD-9), outside this task's 3 files, so the scoped claim held.
- **No mint, on prior art.** The genuine image staleness (built 2026-07-15) is already owned by `OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND`; the port/deploy program is `GO-FLEET-DEPLOY`, already in `closed_sprints` with news-fetch scoped *"port"*, not *deploy*. Cross-referenced in `po_note` with explicit "do not conflate" so the next picker doesn't bill these commits into its 3-commits-behind count.

### Carry-over
- **Latent debt, recorded not minted:** the Go tier under `apps/news-fetch/{cmd,internal}` is built, linted, CI-gated and now refactored — but never containerized or cut over, while `GO-FLEET-DEPLOY` sits closed. Every FACTORY task landing there is maintenance on a program nothing runs. Belongs to whoever picks up the OPS row.
- **Generalizable to the other 231 `review[]` rows:** `rebuild_required` is copied from mint-time audits and is never re-derived at sign-off. Before treating it as a deploy gate, check the service's Dockerfile actually *builds* the changed artifact. Cheap check, inverts the verdict.
- *(inherited, 08:22Z)* QA owns the next move on both rag rows; earliest legitimate AC-3 read is last_embed + 15min + ≤60s. `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS` still needs router dispatch to **architect** — fresh `BACKLOG`, does not self-promote.
- *(inherited, 07:57Z)* `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` P0, 2nd tick undispatched. `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` re-admitted by `flag_reentrant`, 3rd mention. VPS-route-hardcode implementer must bundle all **three** sites (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`).
- *(inherited)* Manual-dispatch sweep ~85 candidates, drains 1/tick — 5th tick raising it. `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` still `DRS-STRANDED-OFF-ALLOWLIST`.
- *(inherited, still owed — 10th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
