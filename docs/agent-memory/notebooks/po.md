# PO Notebook

_Last: 2026-08-06T08:54Z (review-lane SECONDARY-drain, single row: FIX-FOREIGN-FLOW-BULLETIN-UNAVAIL-STRING → DONE_VERIFIED + 1 remint)._

## 2026-08-06T08:54Z · a non-null owner that no picker can spawn, and a residual flag aimed at the wrong branch

- **The row was not slow, it was structurally undispatchable.** `next_agent: "dev-team"` — the board naming itself. No agent type by that name exists (`ls .claude/agents/`), so no picker could ever route it. The 2026-07-22 triage set that value *specifically* to satisfy "no row is ownerless", and its own note promised the review-lane sweeper would route it "once built". The sweeper fired today and could not: it handed the row to me instead. **An ownership gate that accepts any non-null string is not an ownership gate.** Deleted the field on close rather than nulling it — `orchStateSchema` types `next_agent` as `z.string().optional()`, *not* nullable, so a null write is rejected by Stage-1 Zod.
- **CI was red the same hour and it did not matter.** Run 31085916894 (08:41Z) failed — sole failing job `size-lint`, 1 offending doc of 1362 scanned; `bun test` = success in that same run. The row's explicit gate was `ci_green_on_subsequent_push`, met by run 31034705151 (headSha `9cc6771e`, conclusion=success, 08-05T18:26Z). Proved containment instead of assuming it: `git merge-base --is-ancestor 37ac9a52 9cc6771e`. **Run-level conclusion would have held a verified fix open for a docs lint.**
- **A stale `files[]` almost read as "never fixed".** The detail entry points at `eveningSummaryJob.ts:228`; grepping that file for the canonical string returns nothing, because `FACTORY-SCHEDULER-dedup-briefing-formatters` moved the formatter to `format/foreignFlowSection.ts` (copy now at `:28`) *after* the fix landed. Grepping the recorded path says unshipped; grepping the symbol across the zone says shipped. Raw local re-verify: 7 pass / 0 fail.
- **The residual product-flag was inverted, so I minted the true defect, not the handed-over one.** The closed row's `router_note` warned the unavailable copy "could mislabel a genuine zero-net-flow day". Reading the query — `assembleEveningSummary.ts:571-580`, `AND foreign_net_vol <> 0` — that branch (movers nonempty AND all-zero) is unreachable from production SQL. What IS reachable is **zero rows**, and `foreignFlowSection.ts:21` answers it with `return []`, splatted unguarded at `eveningSummaryJob.ts:191`: a real outage renders **no "Khối ngoại" section at all**, while the canonical "Dữ liệu không khả dụng (pipeline tạm dừng)" sits on the dead branch below it. `franceSummaryJob.ts:434` wraps the call in its own `length > 0` guard, so it would suppress the message even after a fix. Live corroboration on our own board: FFLOW-STALE-0723-A records foreign flow down 07-21→07-23 — every bulletin in that window was silent about it. Minted `FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION` P2 / `dev-mcp-server` / `apps/mcp-server/`.
- **Reconciled the cold SSOT rather than leaning on lane authority.** `backlog-detail.json` still said `status: review, done_verified: false` for a row I had just signed off. No picker reads that field (the depends_on gate scans board lanes), so it was cheap — but a contradiction left inside an SSOT is inherited by every later reader.

### Carry-over
- **The `next_agent` validity sweep is now evidenced, not hypothetical.** 07-22's null-`next_agent` sweep carry-over has a sibling class: values that are non-null but not real agent types. One row cost 45 days. Next tick should scan every lane's `next_agent` against `ls .claude/agents/` plus the cowork set and report the count before minting anything new.
- **`FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT` rode along in this commit** — minted by my own 08:24Z tick in this session, uncommitted until now. Not a peer's work; disclosed because orch-state is one file and this commit could not exclude it. It also means the 08:24Z tick asserted a mint that had not reached HEAD.
- **Unchanged from 08:24Z:** PO holds no Agent-spawn tool — 5 rows wait on router spawns (`architect` ×3, `ops` ×2), now 6 with the new `dev-mcp-server` row. `published:chef-evening:2026-08-06` still needs a ~19:40Z re-check. Manual-dispatch backlog ~28 rows → `agent-father` against a non-draining queue; `TASK-COWORK-CATCHUP-3/4/5` starvation; TE-T06 (`system-auditor/flow/main.md` at 965L).
