# PO — Notebook

## 2026-08-24T07:16Z · The ruling was never the missing piece — `.head` was. Same refusal, plus the routing fix.

- **Trigger:** router re-opened the SAME decision I ruled at 04:0xZ (ack pdf-extractor / raise its cap / other), framing the loop as undecided. It was not undecided. **Output: 1 orch-apply write (5 rows mutated, 0 mints, 0 deletes — `task_total` 802→802, `signal_total` 118→118), 1 ack-ledger re-base, 1 registry pointer.** Ledger got **no** pdf-extractor entry, again.
- **★ RULING: (c), UNCHANGED — and the (c) artifact already existed.** `FIX-AUDITOR-TIER1-FOLD-VERDICT-NOT-DURABLE-RESPAWNS-AUDITOR-EVERY-TICK` was minted 04:01:33Z with AC-1..AC-7 written. Nothing was undecided; **nothing had routed it**. Re-deciding would have burned the tick and left the loop running.
- **★ ROOT CAUSE OF THE PERSISTENCE — `.head`, not priority and not eligibility.** The row is **DRS-ELIGIBLE** (verified live: `bounded1-supervised-lane-report.sh` DRS-ELIGIBLE list, 102 rows, `next_agent=architect` is on the ratified allowlist). It never got a turn because `.head` froze **non-idle at 04:27:01Z — 26 minutes after the row was minted** — pinned to `FIX-OCRGATEWAY-INFLIGHT-BOOKKEEPING-DIVERGES-OS-TRUTH` with `next_agent=dev-pdf-extractor`, while that row had already moved to `qa[]` at **04:43:36Z** with `next_agent=qa` and **no lock**. **BOUNDED-1 / SLS / RLC / DRS / QA-Drain / Step-1 PO triage ALL run only on the head-idle fall-through**, so one stale pointer held the entire dispatch chain shut for ~2h45m. Head reset to idle in the same write; row P1→**P0** (DRS ranks `priority_rank` then backlog index — at P1/index 502 it sat behind 55 P1s).
- **★ The router's brief carried a premise that is wrong on the plane that decides it.** "headroom far above `MEM_FLOOR_MIB=40`" is a **MemPerc artifact**: `docker stats` subtracts `inactive_file` and `_mem_headroom_mib()` = `cap*(100-MemPerc)/100` ⇒ **~335 MiB free** where this cgroup's `memory.current` measured **99.85% of `memory.max`** (true free **1.5–3.7 MiB**, `memory.peak == memory.max` exactly). **The floor guard can therefore never fire for this container** — an ack here is unbounded in exactly the way `FIX-AUDITOR-MEMACK-HEADROOM-FLOOR-AND-DEAD-TRACKEDBY` exists to prevent. A per-entry `floor_mib` is unimplementable: the ack list is a **two-field TSV**.
- **(b) refused on host arithmetic, not preference.** Cap is **already 2.5 GiB** (2,684,354,560 B), 2nd largest on the host. Live caps sum to **13.5 GiB against 7.75 GiB of host RAM** = **1.74x overcommit**, with a documented host-memory-panic history. Raising it also moves `FIX-PDFX-…-HEADROOM`'s **AC-4** bar (a % **of cap**) — converting a failed acceptance criterion into a passing one by changing the denominator. That is metric-masking.
- **★ rag-service ack re-based to the live 2 GiB cap — and it is about to become the SECOND loop.** `headroom_mib_at_ack_review` **11.2 → 174.1** (cap 2048 MiB; live 91.50%, corroborated by the 06:39Z A-30 probe: 6 samples 90.72%, VmHWM 1602.6 MiB, **RestartCount=0, 9 days uptime** — killing this entry's own "1.6 restarts/day" pull-trigger). **Code behaviour unchanged** — the probe computes headroom live; that field is read by nothing. `floor_enforcement_20260729`'s prediction ("FAILURE every tick, BELOW-FLOOR") has been **false since the cap raise**, and the live 07:00Z verdict proves it: rag-service is in the *suppressed* clause, pdf-extractor is the sole breach. **Hazard recorded on the entry:** `tracked_by=FU-RAG-DEPLOY-MEMORY` **is the cap-raise row itself**, now in `qa[]`/P0/`next_agent=qa` — one sign-off from DONE_VERIFIED. The instant it lands, STALE-ACK fires and rag-service (91.5% vs 85%) starts a second permanent 30-min respawn loop **with no further human error required**. Written into the ledger: the answer then is the debounce row, **not** a new ack and **not** a retarget to a convenient open row.
- **Triaged the concurrent auditor's signal row `sys-20260824T0130-pdf-mem-loop-8f4a` → `triaged`.** `ts` **de-backdated 01:30:00Z → 07:10:00Z** (5h40m; the exact `:30:00` boundary is a rounded value, not a clock read) with `ts_original_backdated` + a note that the **id still embeds the wrong value deliberately** so references resolve. Two payload claims corrected: `root_cause` "sole cause of breach" is the sole cause of the **verdict**, not the breach; and `evidence`'s **"no backlog row" is false** — two rows have tracked this since 04:01Z. Its `action_required` offered only the two options PO has now refused twice.
- **★ Destructive-before-salvage hazard on the orphaned worktree, closed machine-readably.** Both worktree rows already existed and **contradict each other**: `CLEAN-STALE-WORKTREE-AGENT-AE9ED2CD-FULLY-MERGED-3D-IDLE` (08-15, would `git worktree remove`) vs `CLEAN-SALVAGE-…-231-INSERTIONS` (08-23). The delete row's title is **true about the branch, false by omission about the tree** — re-verified live: 6 modified files + 1 untracked test (`FIX-BCTC-NONBANK-…-HPG.test.ts`) that exists **nowhere on main**, 231 insertions. `git worktree prune` won't reap it (directory exists) and every merge probe says "done" (branch IS an ancestor of main). Set `depends`/`depends_on` delete→salvage, salvage P1→**P0**. Also deps-gated `FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG` (`ready[]`, `next_agent=ba`) on the salvage — otherwise the next picker re-implements code that already exists but is invisible from the main tree. **2nd occurrence in 12 days.**

- **★★ ADDENDUM 07:22Z — I ran the discriminator instead of asserting the FOLD, and it came back RED. The Tier-1 FAILURE is a TRUE POSITIVE.** Router surfaced qa[] prose (`memory.peak == memory.max`, 97.0–99.25% of cap) plus the documented `OOMKilled=false` false-negative class. So I read **kernel dmesg** via the read-only nsenter recipe on `FU-RAG-DEPLOY-MEMORY`'s own status_note rather than trusting `docker inspect`. **Two real `CONSTRAINT_MEMCG` OOM kills of pdf-extractor `python3` workers**, 2026-08-23 **14:27:11Z** (`anon-rss 2,484,996 kB` = 2.37 GiB) and **14:58:52Z** (2,468,832 kB) against the 2.5 GiB cap, cgroup `6ec27ac85c8e…` — the container instance **preceding** the current `2edf0c9c9905` (StartedAt 08-23T15:44:17Z). Negative control non-vacuous (942 `docker0` hits from the same pipeline); VM clock confirmed UTC in the same call.
- **★ That closes another team's open question and refutes their leading suspect.** `FIX-OCRGATEWAY-INFLIGHT-BOOKKEEPING-DIVERGES-OS-TRUTH`'s `desc` names "two silent ExitCode=0/OOMKilled=false exits (**14:27:10Z, 14:58:52Z**)" and calls ocr_gateway inflight accounting their "leading suspect". **The dmesg timestamps match to the second.** The kernel killed those extractions; the bookkeeping bug did not. The 21 stranded reconcile-queue rows are OOM collateral. Mechanism: Docker sets `.State.OOMKilled=true` only for the container's **MAIN** pid — a killed **child** leaves `OOMKilled=false / ExitCode=0 / RestartCount=0`, so **every docker-plane probe read clean while the kernel was killing workers**. Same false-negative class already on `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED`.
- **★ It also delivers a number the probe's own source says does not exist.** `_mem_container_acked`'s header: pdf-extractor's burst is "100s of MiB to ~1 GiB" but "**no settled single-allocation number exists yet**". It does now — **2.37 GiB terminal anon-rss, from the kernel's own OOM report**. Recorded on `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM`; `UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT` raised **P1→P0** and its burst-measurement half is now partially discharged for free (ops still owns the *sustained* profile). **A-30's FOLD was not wrong about its inputs — its inputs cannot see this event class.**
- **★ Net effect on the ruling: unchanged in direction, stronger in force.** An ack would now silence a **live crash source**, so (a) moves from "wrong" to "actively harmful" (3rd refusal, recorded in the ledger). (b) is still not the primary remedy — a container that legitimately wants 2.4 GiB for one OCR job needs the **job** bounded; 13.5 GiB of caps already sit on 7.75 GiB of host RAM. And `FIX-AUDITOR-TIER1-…-EVERY-TICK`'s **AC-3 ("the verdict itself must stay RED") is now the row's load-bearing clause**, not boilerplate — annotated on the row so the implementer cannot read it as permission to green the verdict.
- **★ I withdraw a figure of my own.** My 01:35:50Z note on the OCRGATEWAY row claimed "**22 HTTP 429s** against 418 /extract posts in the last 10h". dev-pdf-extractor re-measured the same window against the **server access log** and found **418 posts / 20 mismatch lines (both matching me) and ZERO true HTTP 429 status lines**. That is a ground-truth read against the artifact that actually records response status; mine was not. **The 22 is withdrawn.** The P0 hold does **not** move — it never rested on the 429 count, only on the 20 mismatch lines both measurements agree on. Scoring severity off a number I could not verify was the error; the disposition was not.

### Carry-over
- **`.head` staleness is the highest-leverage recurring defect on this board and it has no detector.** Two independent agents (dev-team bounded-1 pickup 04:27Z, then nothing) left it pinned; the row it named had already lane-moved. Every dispatch picker is downstream of it. A watchdog — "head non-idle AND its `active_task_id` is not in `in_progress[]` AND no lock held ⇒ reset" — is a real missing row. **MINTED at 07:26Z** as `FIX-ORCHSTATE-STALE-HEAD-NO-LIVENESS-DETECTOR-HALTS-EVERY-DISPATCH-PICKER` (P1, architect, `multi`, AC-1..AC-8, dedup-checked 0 hits) — with an **age floor** (AC-4) so it can never race a live in-flight dispatch, and an explicit `non_goals` ban on the per-writer approach that has now failed four times (pm mid-sprint, architect handoff, qa/ops close-out, dev-team bounded-1 pickup). The fix belongs in the **reader**, not in a fifth discipline rule.
- **The auditor skipped its own flow-mandated notebook write** (router reported `system-auditor.md` unchanged since `f4b9740b2`) **and** left `orch-state.json` uncommitted — 3rd distinct agent type to actuate-then-not-commit (ops, qa, system-auditor). Its row is folded into this commit. `_updated_by: null` on it is the known `signal_queue` producer-auth gap, not a new defect.
- **Did not push.** PUSH-AUTONOMY-1 unsatisfied and pre-push independently red (`pushBctcLayoutHandler.ts` 252L vs 250L, `FIX-SIZELINT-…` P0 held undispatched as a router call). Unchanged from 04:0xZ.
- **Steps run:** scoped ruling only. Channel audit, Step 0-TNB, Step 0-SIG, supervised-goahead and manual-dispatch-sweep **deliberately not run** — router scope was one decision plus two triage items. Reporting the skips rather than folding them into a pass.


## 2026-08-24T04:0xZ · Refused the ack: MemPerc said 85%, the cgroup was at 99.85% of its hard limit

- **Trigger:** router hand-dispatch, single scoped ruling — Tier-1 auditor loop non-convergent, ~48 spawns/day on a condition its own A-30 deep probe correctly FOLDs. **Output: 1 orch-apply write (4 rows: 2 mutated, 1 lane-moved, 2 minted), 0 ack-ledger edits.** head idle throughout, never written. `task_total` 800→802.
- **★ RULING: FIX_ONLY. No `acked_memory[]` entry for pdf-extractor.** The brief framed the reading as "a benign idle-memory reading". It is not. `docker stats` MemPerc **subtracts `inactive_file`**, so it read 85.00–85.43% while the cgroup itself sat at **99.85% of `memory.max`** — `memory.current` 2,681,094,144–2,682,740,736 B vs max 2,684,354,560 B, **true free 1.5–3.7 MiB**, `memory.peak == memory.max` **exactly** (the hard limit was already reached in this 12h life), `anon` 1.41→1.45 GB (**+37.8 MiB in ~7 min**). 6 samples, read-only. The A-30 FOLD read MemPerc/VmHWM/OOMKilled and **never read `memory.current` vs `memory.max`** — "zero volatility" is a wrong-plane artifact, not a wrong verdict.
- **★ The required floor is unsatisfiable in code — this is why the ack had to be refused, not merely deferred.** `_check_mem_creep` builds its ack list as `.acked_memory[]? | [(.container//""),(.tracked_by//"")] | @tsv` — **two fields**. A per-entry `floor_mib` would be **read by nothing**. The only floor is global `MEM_FLOOR_MIB=40`, and `_mem_headroom_mib()` computes `cap*(100-MemPerc)/100` = **~384 MiB free** where the cgroup has **3.73 MiB** — a ~100x overstatement, so the floor guard could **never fire** for this container. The probe's own `_mem_container_acked` header **pre-refuses this exact ack** ("pdf-extractor's burst profile is 100s of MiB to ~1 GiB… a per-container floor may need re-litigating **once pdf-extractor is ever ACK'd here**").
- **★ Acking would have hidden a failed acceptance criterion.** The container runs the **post-fix** image — `sha256:5d7d36db`, Created 08-23T15:44:11Z, `malloc_trim` present in `/app/interface/pek_run_helper.py` + `routes_extract.py` (verified at source). `FIX-PDFX-…-HEADROOM`'s **AC-4 bar is ≤80% of cap**; it idles at 85–87%. The mem_creep FAILURE is the **only live signal** saying the shipped remedy missed its own bar.
- **★ Fact #9 in the brief was wrong — ops DID act.** `UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT` carries `ops_ac1_ac2_status=PASS`, `ops_deployment_verdict=SUCCESS`, `ops_new_image_sha=sha256:4ee7f1c3`, `updated_by=ops` 08-15T09:49:23Z. **Deploy half discharged; only AC-4/AC-5/AC-6 outstanding.** Ruled **LIVE, not retired**, re-scoped to the measurement half, promoted `backlog[]`→`ready[]` (it had **no picker** in backlog). Its `evidence` sha is now **doubly stale** — the container was rebuilt *again* 08-23 after ops.
- **★ Fact #6(a) rests on an indicator that cannot move.** "unlikely, VmHWM is stable" — VmHWM is a **high-water mark**; it cannot fall, so it can never evidence that usage will drop. The driver is `memory.current`, which fell 158 MiB between 03:30Z and 03:49Z.
- **Un-stranded row #8.** `FIX-PDFX-…-HEADROOM`: `next_agent=null`→**`po`** (its own AC-6 says PO, not ops, closes it), `owner=po`, `blocked_reason` **set (639B)**, `depends=[UNBLOCK-PDFX-…]` so the block is machine-readable instead of prose-only, `status_note` records the AC-4-violated-at-trough finding. Row is 4,117B — the `non_goals` "33,898B against a 12,000B ceiling" warning refers to the **detail store** (`backlog-detail.json .items[]`, which is an **array**, not an object), not the board row.
- **Minted 2 rows — the honest alternative to a suppression ledger.** `FIX-AUDITOR-TIER1-MEMACK-HEADROOM-COMPUTED-FROM-MEMPERC-NETS-OUT-PAGE-CACHE` (P1, developer, `cross-service/`) and `FIX-AUDITOR-TIER1-FOLD-VERDICT-NOT-DURABLE-RESPAWNS-AUDITOR-EVERY-TICK` (P1, architect, `multi`). Debounce hides the **duplicate spawn**; an ack hides the **signal**. Both dedup-checked against all lanes (0 hits).
- **Serial-offender shape, recorded.** Tier-1 was ALL_GREEN at 01:17:09Z; pdf-extractor first breached at the **02:00Z** tick. The immediately preceding FAILUREs (08-23 21:00/21:30/23:00Z) were a **different** cause — launchd fleet-push STALE-ACK — hand-retargeted by po at 01:09:11Z. Each cause gets hand-fixed while the no-debounce structure re-arms on the next. That is the root cause; the breaches are symptoms.

### Carry-over
- **★ The `acked_memory[]` precedent is weaker than it looks — rag-service does not breach at all today.** It reads **75.62% of a now-2 GiB cap** (was 768m), so its entry is currently **inert** and every number in its ~4KB prose body (11.2 MiB headroom, 94–98%, the anon/file split) is against a **dead denominator**. Do not cite it as a live suppression precedent without re-measuring. Worth a CLEAN row to re-base or retire it.
- **Churn is NOT a locked 48/day** — the breach oscillates across the `>=85` boundary (85.00 / 85.11 / 85.43 / 86.97 / 87.06), so ALL_GREEN ticks will interleave and the heartbeat trigger will disarm on each. Do not re-rate the loop's cost off the worst-case figure without sampling.
- **`review[]` PRIMARY selector is exhausted: 0 rows `next_agent=="qa"`, 13 `next_agent: null`** (of 25). I fixed **one** (the pdf-extractor row) on its merits and deliberately did **not** mass-mutate the other 12 — assigning `next_agent` blind is how contraindicated rows get auto-claimed. Owner is dev-team/DRS, not a PO batch edit. Needs its own row.
- **Did not push, did not repair size-lint.** PUSH-AUTONOMY-1 unsatisfied; pre-push independently red (`pushBctcLayoutHandler.ts` 252L vs 250L). `FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-BLOCKS-ENTIRE-FLEET-PUSH` P0, still held undispatched as a router coordination call. **Recommendation stands: highest-value dispatch on the board.**
- **Inbox untouched, as instructed — 43 envelopes held.** `guard-signal-type-coverage.sh` derives its live-type set from that array, so clearing it greens the gate by deleting the guard's input. Also **not invoked** — it writes live orch-state via `ORCH_APPLY_LIVE_FILE_OVERRIDE` and its `--check` flag is a no-op alias, not a dry-run.
- **`FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58`** still BLOCKED, `next_agent=po`, still mine — not re-checked this tick (out of scope).
- **Steps run:** scoped single ruling only. Pre-check supervised-goahead, manual-dispatch-sweep, Step 0-TNB, Step 0-SIG, Step 0 Channel Audit **all deliberately NOT run** — router scope was one decision, not a general sweep. Reporting the skips rather than folding them into a pass.
