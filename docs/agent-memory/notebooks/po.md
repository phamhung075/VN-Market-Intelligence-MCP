# PO Notebook

_Last: 2026-08-01T04:38Z (dev-team Step 1 triage, cron tick 04:07Z — 8 routed signals + 2 dashboard rows). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-5.md` STEP po-S125._

## This cycle

- **The hand-off's premise on item 3 was false, and checking it found the tick's real defect.** It said dashboard row `dev-20260801T035943` was "already marked READ" and awaiting triage. It was not on the board at all. A row-presence walk — PRESENT(NEW) at `3ed27359f`…`b148abe1f`, ABSENT at `b56eb5647`, rows 135→134, id-set diff exactly one REMOVED / zero ADDED — showed it was **cold-evicted at ~22 minutes old against a 24h gate**.
- **Root cause: the age gate does not bind for near-miss ISO-8601.** Its `ts` is `"2026-08-01T03:59Z"` — minute precision. `jq`'s `fromdateiso8601` accepts only `%Y-%m-%dT%H:%M:%SZ`; `orch-cold-evict.sh:470`'s sibling `catch 0` maps the throw to epoch 0, and `0 < cutoff` is unconditionally true. Measured both variants live: minute-precision **throws**, fractional-second (`…01.546Z`) **throws**. The `:466` comment calls this "unparseable == oldest" and means it for genuinely unknown-age rows — it also fires on rows whose age is perfectly well known.
- **Verified it is not a pre-fix regression, which is what makes it actionable.** `git merge-base --is-ancestor bd9219ccb b56eb5647` = **true** — the age-gate fix was compiled into the very prune that evicted the row. So `FIX-COLDEVICT-SIGNALQUEUE-NO-AGE-GATE-ORPHANS-READ-ROWS` (review[]/qa) is an **incomplete fix about to be signed off**. Minted `FIX-COLDEVICT-MALFORMED-TS-CATCH0-EVICTS-FRESH-SIGNAL-ROWS` (P1) + wrote a QA sequencing hold on the parent.
- **Blast radius measured on both planes before sizing it.** Hot board **clean** (134 rows, 0 malformed, 0 null) — no mass repair needed. Cold archive: ~100 malformed across 2026-06/07, two recurring writer shapes (minute: whole `sau-d4-*` family + several `dev-*`; fractional: `wal-escalation-*`). Harmless historically because eviction was status-ONLY until today — the malformed population only became a correctness problem the moment the age gate landed.
- **Sweep-guard: same verdict as the hand-off, but on an admissible basis.** It justified "false positive" via `real_blob == parent HEAD blob` — which `triage-signals.md` names as an **INVALID** basis (outcome, not mechanism). Re-verified independently: the blobs form a closed chain (each signal's "staged" blob == the previous signal's "about to land" blob), and every "staged" blob maps back to a real prior commit of that path (`569ef942`=`477ed883`, `94b5635a`=`546fb50b`, `199340f2`=`6f122ecb`, `da2eddaa`=`f0cdcaec`, `87e99137`=`952f0c53`). The "staged" side is just the index entry git leaves after a commit. **10/10 fires, 10/10 FP.** Count was **7 new, not 6** — `…T042154Z-85927.json` fired after the drain, still unprocessed in `docs/signals/`.
- **push-abort: stale, closed, no row.** Signal claimed ahead=24 vs threshold 20; live `git rev-list` = **0 behind / 7 ahead** at read time. Confirmed resolved, as the hand-off said.
- **`cow-20260801T041122` named a mechanism gap worth acting on.** mw + news-scout both failed the coverage stamp same tick with *different* errors (TTL-err vs mutex-timeout) and **"no-Bash NOT cited"** — that explicitly rules out the transport explanation `FIX-COWORK-BASH-GRANT-COVERAGE-STAMP-TRANSPORT` (review[]/qa) is built on. Ground truth: `coverage-state.json ._updated_at` = **2026-07-25T16:14:39Z, frozen 6.5 days**. Attached as an occurrence to `FIX-COVERAGE-STATE-CROSS-AGENT-LOST-UPDATE` (**P2→P1**, scope widened lost-update → also mutual-failure) + QA hold on the transport row. No 4th row — three were already open on one file.
- **Unblocked a P0 stalled 4+ days on a condition already met.** `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` sat BLOCKED/`next_agent:po` since 07-30T21:58Z; its own unblock condition was "LAYER2 reaches DONE" and LAYER2 is `DONE_VERIFIED`. Moved backlog→review, `next_agent:qa`. **Did not self-close it** — closing a P0 off three sibling status *fields* is the status-lags-ground-truth trap; QA verifies the layers. Noted explicitly that the AC-3 FP must not re-block it (documented NON-GOAL, tracked separately).
- **Both mandatory pre-checks ran.** supervised-goahead: `should_hold=false` → no-op. manual-dispatch: **38 candidates** (36 DRS, 2 XOR, 0 re-flagged); selected `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` over strict-top `TE-T06` — both rank 1, so the tie-break is array index; TE-T06 has neither `files[]` nor `baseline_pass` (Step 3 builds the BATCH entry from exactly those), and I hit the selected row's defect head-on this tick.

## Carry-over

- **A hand-off's factual premises are claims, not context — the one labelled "already handled" is the one to check.** Seven of ten items were correctly pre-triaged, which is exactly why the eighth slipped through: a dense, accurate briefing buys trust that the one wrong line then spends. The row was described as sitting READ on the dashboard; it had been deleted 11 minutes before I was spawned. **Cheap rule: for every artifact a hand-off says exists, read it from its own store before dispositioning it — the read that returns nothing is the finding.**
- **"Unparseable == oldest" is a safe default only where age is genuinely unknown.** It was written to stop poison rows becoming permanently un-evictable — a real concern — but it silently reclassifies *well-formed-but-slightly-off* timestamps as infinitely old. Near-miss formats are not unknown-age. **Any `try … catch <sentinel>` around a parser needs to distinguish "no value" from "value I failed to parse"**; here the difference is a signal reaching PO or being deleted 22 minutes after it was written. Do not fix it by flipping the fallback to "newest" — that just restores the poison-row hole (AC-2 of the new row says so).
- **A correct conclusion reached by a forbidden method still has to be redone.** The sweep-guard verdict was right, and I could have shipped it on the relayed reasoning in one line. But `triage-signals.md` forbids that basis *by name*, and a precedent of accepting outcome-evidence-for-mechanism is how the next, wrong one gets through. The independent re-derivation cost ~2 minutes and produced strictly better evidence (a closed 5-blob chain vs a single comparison). **Grade the basis, not just the answer.**
- **Two fixes in `review[]` this tick were about to be signed off over live holes, found the same way.** Both `FIX-COLDEVICT-SIGNALQUEUE-…` and `FIX-COWORK-BASH-GRANT-…` are correct on their own mechanism and incomplete against the observable anyone will read the verdict as covering. **When holding a row, say which observable the verdict may NOT claim** — "coverage-state freshness restored" belongs to a different row than "transport restored".
- **Honoured a 4-tick-old carry-over instead of re-noting it a 5th time.** Minted `FIX-ZONE-ENUM-SSOT-CONTRADICTED-BY-259-OF-659-OPEN-ROWS` (P2, architect) **with a measurement**: 256/659 open rows (38.8%) violate the enum `main.md:30` calls mandatory. Three classes — (A) near-miss punctuation, incl. `apps/mcp-server` (14) coexisting with `apps/mcp-server/` (141), the dangerous one; (B) bare service names; (C) `docs/agents/`-shaped pseudo-zones (36+14+10+…), which is a genuine missing enum member, not a typo sweep. **The measurement is what made it mintable — four ticks of re-noting produced nothing.**
- **Board saturation unchanged and binding.** backlog **356** / review **252** / ready 51 / in_progress 1 / qa 0. task_total **769** (+2 mints, conservation OK each write). ahead **12**, behind 0.
- **Still owed (ageing):** 5 `review[]` rows with `next_agent: po`; 3 null-zone open rows (now covered by AC-5 of the new zone row); the 7th sweep-guard signal still unprocessed in `docs/signals/` (dev-team's drain owns it, not mine).
- **Not run this tick:** channel-audit (`read_telegram_reports(status="new")` empty, `list_unresolved_reports()` `[]`). Step 0-TNB: `tnb-audit-latest.md` last written 03:02:17Z — my own correction addendum from last tick; no c122 yet.
