# PO Notebook

_Last: 2026-06-28T12:13Z_

## This cycle — dev-team triage 20260628T120946Z: board+signal hygiene (live terminal owns CROSS-SESSION)

A live parallel terminal is actively driving the board (manual-drive, no sprint lock; last commit f3c09500 @14:03, ~6min pre-tick) on CROSS-SESSION-MULTI-TEAM-ORCH / TASK_1989 enum-drift. DEFERRED all that to the live leader — touched NONE of it. My scope = signal backlog + board hygiene the live terminal isn't handling. Actions (all RAW-verified, orch-apply.sh): (1) FIX-VNM-BCTC-ROWS-DATA-LOSS-RECOVER ready[](status REVIEW)→done_verified[] — work landed 68c7b99a + qa APPROVED f3c09500 (git show: 94 rows BS46/IS22/CF26), pure stale status-lag reconcile, CAS-guarded. (2) pm.md 329L context-bloat: REAL breach but pm ALREADY APPEND-class (file-size-caps.json + SKILL AC-6) → did NOT re-prune (treadmill) / did NOT mint membership; corroborated anchor HARDEN-NOTEBOOK-WRITE-GATE-AC5-BLOCKING (recurrence_count 6→7 + breach), signal→processed. (3) qa.md context-bloat NOW STALE (committed qa.md 201→45L, AC-3 self-cap worked) → signal→processed. LEFT 3 cross-session/po signals (all to:pm, live-pm territory). Telegram 3338/3339/3340 expected re-surfacers (not re-filed); 3341/3342 = TASK_1989 Migration-3 WAL-checkpoint = live-terminal, untouched. Returned NOTHING (no dispatch — WIP cap respected).

LESSON: when a live terminal manual-drives a sprint, PO triage stays strictly on the orthogonal hygiene surface (stale status-lag rows + signal drain) and reconciles ground-truth to the board WITHOUT re-dispatching already-done+approved work; a `to:pm` cross-session signal stays the live leader's to drain.

## Prev cycle — FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT final acceptance: SIGN-OFF + VNM carve-out

QA returned CHANGES_REQUESTED: VCB(bank,31f2a9a9)+HPG(918a7abd)+FPT(e71f845d non-reg) Stage4/Stage6 GREEN; VNM(4316f6d1)=0 rows FAIL. **Ruled: sprint ACCEPTED on VCB+HPG+FPT (de-overfit root fixed, generalization proven on bank+non-bank = orig DoD); VNM carved OUT** (distinct root: column-separated OCR no row-parser matches + VNM's 94 prod rows came from REFINE path not /extract-tables → QA verified wrong pipeline). Data-loss (VNM 94→0, QA /extract-tables overwrote live) is RECOVERABLE: /extract-tables writes bctc_table_rows only, never bctc_refined_units → re-run finalizeBctcRefineTool restores from surviving refined truth (no backup exists). Dispositions via scripts/po-s130-*.jq: FR330/331/332+sprint→done_verified; FIX-VNM-BCTC-ROWS-DATA-LOSS-RECOVER→ready P1 dev-mcp-server (head); SPIKE-BCTC-COLUMN-SEPARATED-LAYOUT (recon-first, build only if real gap), FU-EXTRACT-VERIFY-SHADOW-NOT-LIVE (guard, architect), FU-FR4-RISK4-30LINE→backlog. TASK_332=DONE. Handoff: docs/handoffs/TASK_332.md [PO] Ruling.

## Prev cycle — DECISION: P1 router-gate enum-drift defect → bless 7-kind taxonomy, ONE corrective

Router dogfooded the shipped P1 gate and found it NON-FUNCTIONAL: every dispatch's step-2.5 PRE-CLAIM `task_claim(task_kind="intent")` returns -32602. Deployed enum = 4 kinds; 'intent' absent. RAW-verified all 3 in-code enum sites carry only the 4. DECISION: bless COMPLETE 7-kind taxonomy in ONE Migration-3 (4 existing + intent + orphan-signal + session-presence); ONE corrective FIX-COORD-TASKKIND-ENUM-INTENT-GATE supersedes TASK_1982, re-gates TASK_1983-1988; QA adds LIVE integration check (router PRE-CLAIM returns claimed:true vs deployed schema). LESSON: a contract shipped across two substrates is only "shipped" when an INTEGRATION test exercises the real caller against the LIVE deployed schema; recurring enum-drift → bless the complete forward taxonomy in one migration.
