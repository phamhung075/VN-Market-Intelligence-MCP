# PO — Notebook

## 2026-08-14T18:01Z · What the spec verified held. Everything it inherited was wrong. That is now a rule, not an observation.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain, single row `TASK-COWORK-SIGNAL-CHEF-INTRADAY` (child 3 of 4, `plan_only`+`supervised`, idle in `review[]` since 08-08). **Output: 1 `orch-apply.sh` pipe, conservation OK (703=703), row `review[] → done_verified[]` in one write, spec doc §11 appended.** Disposition: **DONE_VERIFIED + binding Amendment 6 (7 parts, incl. a new AC-5)** — third consecutive sign-off-with-amendment on this chain.
- **★ THE SPEC'S OWN EDIT INSTRUCTION IS NOW DESTRUCTIVE.** §1.2 says "Replace lines 567-574" of `chef-dish.md`. Those lines today are Step 7.5's **quality-gate verdict block** — `GAP_CATALOGUE_OK` / `# Verdict` / `if L2_OK AND L3_OK AND …` / `QUALITY_VERDICT="full"`. An implementer following it literally deletes the quality verdict and pastes a `FILEPATH` derivation over it. `chef-dish.md` drifted **+67L**, `chef.md` **+27L**, **non-uniformly** — agent-father's UC-CCA-P2 edit added 15L to `chef.md` on **08-14, while this row sat in review**. So no offset repairs it, and re-stating today's numbers is not durable either: **the amendment's real product is the rule "every line number here is advisory, anchor on quoted text," not the table.**
- **★ THIRD FALSE ABSENCE CLAIM OUT OF FOUR DEFECT CLASSES ON ONE CHAIN.** §7 repeats task 1→2's "PM minted no per-task handoff stubs" verbatim. All four exist; **this task's own — `TASK-003-chef-intraday-filename.md` — carries the AC set, including the one line I had to override.** Absence is the cheapest assertion to re-check and the only one that has failed *every single time* here.
- **★ THE BOUNDEDNESS ARGUMENT WAS GROUNDED IN A CRON STRING; THE ARTIFACTS REFUTE IT.** §3 argues EC-2 is bounded because the cron is `13 2-8 * * 1-5`. Live: a 2026-08-12 fire at **15:13Z — outside the declared window entirely** (and its mtime says that `Z` is a mislabelled VN-local stamp, which independently re-justifies §4's `cycle_id` non-promotion), plus run-start drift of **+7..+22 min** across 11 artifacts against a **47-min margin** before `VN_HOUR` flips. **A cron declares intent; only artifacts record what fired.** The conclusion survives, its evidence did not — and the tripwire had to widen from "cron changed" to "cron changed OR a fire seen outside the window," because that schedule row is now `_superseded_by: cowork-dispatcher`: the cron is still the discriminator SSOT, no longer the actuator.
- **★ FOUND THE DEFEAT CONDITION FOR THE TASK'S OWN ACCEPTANCE TEST.** The filename family already carries **two slot-component conventions** (`chef-<slot>` ×4 vs bare `dish_type` ×64) — and on 2026-08-13 **both shapes exist for intraday on the same day, with different metadata schemas.** So AC-1's "two same-day fires → two distinct paths" **passes today, with no fix at all**, and AC-4's auditability stays unreachable. Folded as new binding **AC-5** rather than a new row: a defect that makes this task's own test vacuous is in-scope by definition, and a separate row would let someone ship against a green test.
- **Upheld the developer's flagged deviation.** `IS_MULTI_FIRE` over a hardcoded `"intraday"` match — verified at source it is pinned unconditionally (`chef.md:118`) from the slot's own cron read live (`:112-114`), and `chef.md:75` already ratifies "no slot name is hardcoded." The string match would have been the regression. **Recorded that this overrides stub `TASK-003` AC-1**, which mandates it — the stub only became binding because of Amendment 6(b), so 6(b) and 6(c) had to ship together.
- **Recorded a disagreement instead of enforcing it.** `VN_HOUR` is literally the "leaf-side `date`/run-start read" the parent's binding CAUTION forbids, and post-fix the filename (`CYCLE_DATE_UTC`) and the mutex (`WORK_DATE`, VN) **disagree on the date basis** — they coincide only because UTC 02-08 sits inside one VN day, i.e. *contingently*, not "by construction" as the stub claims. Blocking Phase 1 on it would invert the sequencing PO itself ratified. Widened the Phase-2 row's AC-1 to migrate the **date** component too — otherwise Phase 2 closes half and certifies the whole.
- **Carry-over:** (1) **PO owes 1 last spec review** — `TASK-COWORK-SIGNAL-NAMING-CONTRACT` (`review[]`/`next_agent=po`); only then can the parent gate be adjudicated. It is the naming-**contract** task, so **AC-5's determinism rule is its natural owner** — check it against that. (2) Re-check every absence claim in task 4 *first*. (3) `orch-state` schema gotcha for future PO writes: `next_agent` is optional-**not**-nullable — a literal `null` fails Zod; `del(.next_agent)`. And `DONE_VERIFIED` is refused without `verification.raw_probe{tool,args,live_value_observed,observed_at}`. (4) Prior ticks' carry-overs (supersede-mutex actuator, duplicate notebook-bytecap CLEAN pair, ba.md's dropped section, `live_mcp_fetched_at` unresolved dependency) untouched by this single-row tick and still stand.

## 2026-08-14T17:34Z · The spec's four function arguments do not exist anywhere in the agent that calls it. Nothing inside the document could have revealed that.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain, single row `TASK-COWORK-SIGNAL-BCTC-REKEY` (child 2 of 4, `plan_only`+`supervised`, idle in `review[]` since 08-08). **Output: 1 `orch-apply.sh` pipe, conservation OK (712=712), row `review[] → done_verified[]` in one write, spec doc §11 appended.** Disposition: **DONE_VERIFIED + binding Amendment 5** — same shape as po-1/po-2 on this chain.
- **★ THE DEFECT WAS CALLER-SIDE AND STRUCTURALLY INVISIBLE FROM INSIDE THE SPEC.** Task 1's contract says all four `derive_window_key()` inputs arrive "already in session state"; yesterday's Amendment 4(a) made *this* row the sole consumer. So this document's §2 is the only place in the whole 4-task decomposition where those arguments ever bind to real values — **and it binds none.** Live counts in `docs/agents/bctc-analyst/flow/`: `prompt_text` 0, `cowork_schedule_json` 0, `live_mcp_fetched_at` 0, `slot_id` 1 — and that one occurrence is *itself* an undefined placeholder. `stage-bootstrap.md` is 6 lines and its skill's declared outputs contain none of the four. **An implementer follows §2 verbatim and WINDOW_KEY comes back empty, so the filename collapses to a constant across all 4 daily slots — silently re-shipping the exact collision the parent row exists to close.** Amendment 4(b)'s fail-loud guard cannot catch it: the bad value arrives *before* the parse.
- **★ THE ONLY WAY TO SEE IT WAS TO CHECK ONE SPEC'S "ALREADY AVAILABLE" AGAINST ANOTHER FILE'S ACTUAL DEFINITIONS.** Both documents are internally consistent and each is correct about its own half. The gap lives precisely in the seam a per-task review is structured not to look at. **Decomposition into atomic tasks moves the risk from inside tasks to between them** — so on a decomposed set, reviewing the interface contract is not optional diligence, it is the review.
- **★ A CLAIM OF ABSENCE WAS INHERITED THREE TIMES WITHOUT ANYONE CHECKING.** §10 asserts PM "did not mint the per-task handoff stubs". They exist — all four, `docs/handoffs/TASK-00{1,2,3,4}-*.md`, dated 08-07 07:59. `TASK-002-bctc-analyst-rekey.md` (8.9 KB, *this* task's own) carries **both** things the 305-line spec is missing: the argument sourcing (its AC-2) and NFR-2 no-Bash (its Implementation Note 2). The `ba_handoff` field points at a wrong *filename*, not a missing artifact. Chain: architect brief §2 over-claim → task 1 §7 inherited → task 1 §10 → task 2 §10 inherited verbatim. **Two of the three defects on this chain were false absence claims, and absence is the one assertion that costs nothing to re-check and everything to repeat.** Recorded as a standing rule on the parent for the remaining 2 reviews.
- **Signed off instead of bouncing, and the calibration mattered.** Everything substantive verified at source: `stage-analyze.md:114`, the real FR-7 gap, the §5d-1 token, the genuinely-stale `stage-consolidate.md:64` xref, and NFR-4's no-op at `drain-signals.js:251-252` (`sha256(from+type+payload+createdAt)` — basename absent, so the rekey is fingerprint-neutral by construction). **Only `cycle.md` drifted (+9 lines); the other three files' anchors were still exact after 6 days** — so the spec's live-verification discipline was sound and a rework round-trip would have bought ~15 lines of amendment text for a full dispatch cycle.
- **Checked "superseded" before reviewing anything.** `grep -rn 'WINDOW_KEY|derive_window_key' docs/agents/bctc-analyst/` → zero matches. Nothing shipped during the 6 idle days; the row was still live work, not a stale artifact to close out.
- **Did NOT lift the `plan_only` gate.** The parent's `supervised_note` conditions shipping on parent re-adjudication, and that cannot be honestly discharged on a partial child set — 2 of 4 remain. Parent held at `BLOCKED`/`next_agent=po` deliberately, which also keeps Step 4.4's `all_children_terminal` guard correctly closed.
- **Carry-over:** (1) **PO owes 2 more spec reviews on this chain** — `TASK-COWORK-SIGNAL-CHEF-INTRADAY` and `TASK-COWORK-SIGNAL-NAMING-CONTRACT`, both `review[]`/`next_agent=po`; only after both can the parent gate be adjudicated. (2) **`live_mcp_fetched_at` is a genuine unresolved dependency**, not just a doc gap — it exists nowhere in bctc-analyst's flow *or* in `step-0-cowork`'s outputs, and bctc-analyst is a Bash-less class so a wall-clock fallback is not available; it may need a bootstrap-side change, which the implementer must raise rather than paper over. (3) The PM stubs `docs/handoffs/TASK-00{1,2,3,4}-*.md` are unreferenced by any child row's `ba_handoff` — the eventual implementer must read **both** the `-spec.md` and the `TASK-00N-*.md` for each task. (4) Prior tick's carry-over items (supersede-mutex actuator, the duplicate notebook-bytecap CLEAN pair, ba.md's dropped section) are untouched by this single-row tick and still stand.
