# PO Notebook

_Last: 2026-07-29T11:53Z (rag ACK scope mismatch → allocation mechanism → page-cache retraction) · 2 minted, 2 priorities raised, 15 rows annotated, ledger corrected twice, 3 self-corrections._

## This cycle

- **The scope mismatch was real, and the thing under it was worse.** The router asked whether the ACK's `tracked_by` was too narrow. It was — but `tracked_by` is read by **nothing**. `auditor-tier1-probe.sh:275` extracts `.acked_memory[].container` and stops. The STALENESS RULE both the ledger and the probe promise is enforced by no code, no cron, no test. A suppression whose only safety property is documentation is not narrowed by editing the documentation.
- **Binding the ACK to a time-gated row made it un-expirable by construction** — `RAG-FTS-BUILD-MEMORY-BOUND` is blocked on corpus growth projected to ~2026-08-16, so even a *live* `tracked_by` could not expire for ≥18 days. And **the falsification was already inside the row cited as the cause**: its own `qa_result.idle_baseline_flag` (2026-07-15) reads "idle mem already ~92% of 768m with ZERO FTS activity", ten days before the ledger named it root cause.
- **"Narrow the ACK" was not an available operation.** `:302-307` is one `pct >= WARN_PCT` test — suppressed identically at 85.01% and 99.99%. Only suppress-everything or re-churn the auditor 48×/day exist. Minted the row that makes the middle expressible.
- **I built a hypothesis against a measurement I had already read.** I argued the 98% plateau was reclaimable page cache from a 786 MiB corpus filling a 768 MiB cap. The cgroup split says **anon ≈ 726 MiB (~95%), file ≤ ~40 MiB** — process-resident, no reclaim, no mitigating branch. The disproof was in the row's own `root_cause` the whole time: **cold RSS 50.7 → 749.9 MiB on a *single* embed**. A 699 MiB step from one embed cannot be page cache; model weights are anonymous by construction.
- **What actually seduced me was a numeric coincidence.** 786 MiB on disk vs a 768 MiB cap are close numbers with no causal link — the corpus is not resident in that cgroup at all. I let magnitude similarity stand in for mechanism, and it overrode a direct measurement I had in hand.
- **The decisive test I called "un-run" had been run 90 minutes earlier, destructively** — a peer's `docker exec ... import lancedb` into 1.56 MiB of headroom SIGKILLed the container (exit 137, RestartCount 21→22) and *that kill* produced the reading. The measurement existed; it was a byproduct of the incident, so nothing in the board pointed at it.
- **Where I was right and it mattered:** the brief's window was undercounted — the 10.8 MiB step lands on a 2-second burst holding 2 `/index` **and 2 `/search`**, from a 6-line log. Request-proportional leak stays refuted by asymmetry (~190 `/index` → −4.1 MiB; 2+2 → +10.8 MiB), and the ~0.51 MiB/min extrapolation stays refuted by 10 flat sampler reads.
- **Three self-corrections, all caught by read-back, none by re-reasoning.** "Flat" on 2 samples; a P3→P2 note that never mutated the field; the page-cache hypothesis. **A transform that adds a key is not evidence it changed a sibling key.**

## Carry-over

- **The ledger was the artifact that mattered and I nearly left it stale.** `auditor-launchd-ack.json` still asserted 745.8 MiB — encoding **22.2 MiB of headroom when the real figure is 11.2**, a 2× overstatement on the one file whose whole job is justifying *not* alerting. Now carries the anon/file split, `headroom_mib_at_ack_review`, and an explicit `suppression_still_warranted` with pull-triggers (headroom <5 MiB, or >5 restarts/day). **Correcting the notebook is not correcting the system.**
- **Cap-sizing gate DISCHARGED, not blocked.** I wrote "no cap sizing before the split is measured"; it is measured and says process-resident, so a raise buys real headroom. Sizing arithmetic on the row: ~726 MiB resident + ~20 MiB burst + slack, against 768 MiB. Cap change and rebuild remain user-gated and unauthorised.
- **Disk and memory un-coupled on all three rows.** `FIX-RAG-COMPACTION-DISK-AMPLIFICATION` stays P2 **on disk grounds alone** (3137 fragments, 44 MiB version metadata, ENOSPC history); fixing it will reclaim disk and cut fan-out and will **not** move the 98%. A future reader must not inherit the conflation I introduced.
- **Premature signal eviction, 2nd today — recorded, not minted.** Both rows I ACK'd went to `archive/2026-07.json` (evicted, not destroyed) already triaged, so harmless. I did NOT identify the evicting process or read its predicate — a bypass for already-READ rows would make it *correct*. Third occurrence, specifically a NEW row, triggers the rule.
- `LAYER2` **P1→P0** ("4 call sites" is a floor — 8 agent paths swept in one day). Do NOT bump the superseded `FIX-AUDITOR-COMMIT-NONEXPLICIT-PATHSPEC`. `FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` reproduced 3× while in REVIEW — qa must check observed claim/release **order**. `FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY` cannot pass QA from source.
- **A-30 row 5d+, `recurring_bug_count=6`, 8th carry** → **escalate to user next tick**. Standing, 15th carry: verify my own commits carry my authorship.
