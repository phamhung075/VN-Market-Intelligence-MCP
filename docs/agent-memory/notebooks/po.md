# PO Notebook

_Last: 2026-07-30T20:00Z (dev-team Step 1 triage, tick 19:07Z — **second pass**, scoped to the Telegram report queues that pass 1 did not reach). Pass 1 (19:29Z, commit 30f173379) covered `.task_board` state; its live carry-over is retained below._

## This cycle

- **The report queue was never unclearable — it was uncleared, and the tool doc is why.** 179 unresolved reports, every one `status=new` / `resolution=none`, zero acks ever. `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` (P3) blamed a missing capability: *"NO resolve/acknowledge tool exists to mark a report dispositioned-but-deferred."* False at source AND at the store. `process_telegram_report` takes `resolution: z.enum([none,fixed,wontfix,duplicate,monitoring])` — `monitoring` **is** that ack — and its Step 4 calls `markProcessed()` unconditionally, which both polls exclude. The real cause is doc drift: `docs/agents/tools/list/process_telegram_report.md` omits `resolution` entirely and misnames `delete_telegram_message` as `delete_message`. Agents reading the tool doc literally cannot see the disposition values. Retargeted the row P3→P2: doc fix + flow ack-step, **not** a build.
- **Proved the ack end-to-end before scaling it, then cleared the queue.** Acked 4039 → re-read `list_unresolved_reports` → 179 dropped to 178 with 4039 absent. Only then did I ack the rest: **179 → 0**, both planes verified empty (`[]` and *"Không có báo cáo mới"*). 131 `duplicate` (a board row owns the root cause), 48 `monitoring` (real, tracked, unfixed), **0 `fixed`** — nothing shipped this pass, so nothing got a green stamp.
- **The `signal_quality_audit` CRITICAL is REAL — I was asked to expect a false-positive and the hypothesis failed.** 40 samples spanning 55 days back-solve to ONE epoch, 2026-06-05T20:13Z, agreeing to **6 seconds** between oldest and newest. A broken baseline drifts; this is a fixed anchor with a monotonic clock. The threshold is constant (2880, then 43200) — contrast the genuinely-broken `bctc` sibling where (stale − threshold) was a fixed 5439 across 12 samples, the signature of an age-vs-age comparison. And `dev-mcp-server` hit the live DB independently and got the same instant. Three planes, one answer.
- **The alarm has no owner, and both adjacent rows say so themselves.** `FIX-SLA-SIGNALQUALITYAUDIT-…-48H` (REVIEW/qa) widened a threshold and its own `freshnessSlaChecker.ts:209-211` states the ~52.8d gap *"still fires under the new value"* — by design, correctly. `FIX-MONTHLYSIGNALQUALITYAUDITJOB-MISSED-JULY-RECOVER-GUARD` targets a job that `orch-state:10859` says verbatim *"never writes to the signal_quality_audit table."* So sign-off closes both neighbours while the condition re-emits ~1/hour forever — **39 of the 179 reports (22%) were this one alarm.** Minted `FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT` (P2/`dev-mcp-server`); verified BOUNDED-1-eligible by *running* `devteam-eligibility.jq`, not by re-deriving the allowlist.
- **A threshold sized off a dead capability's output normalises the deadness.** The 30d value was calibrated to *"6 rows total … real gaps of >2 weeks between them even in a healthy period."* If the write path was never reachable there was no healthy period and those 6 lifetime rows are noise, not a cadence. Suspected mechanism, read at source: the audit write requires `finding_data.confidence` to be a **number**, but `signalTypes.ts:217` makes it OPTIONAL for `urgent_news` and `news-scout.md:66` omits it from the documented required set — so a compliant `urgent_news` post never trips it. 58 `urgent_news` recorded vs 6 audit rows lifetime. Wrote it as a three-outcome decisive probe, not a prescription — I did not query `agent_signals` myself.
- **50 of the 179 reports were one two-hour event, not fifty defects.** `BCTC-1345b` (40) + `[BCTC]` write-BLOCKED (10) are 07-28T16:00–17:58Z **only**, nothing since — downstream of the P0s dev-team already dispatched. The write-BLOCKED messages are the guard *working* (refusing `total_assets=0` over good data; DBC even caught a **negative** total). Archived as duplicate; minted nothing.

## Carry-over

- **`FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION` + `FIX-TASKCLAIM-OWNER-CLIENT-SESSION-MISSING-FLEET-FLOW-DOCS` are both themselves zero-picker** (`next_agent=agent-father`) and move only by deliberate BATCH dispatch. **The zero-picker count is the metric to watch each tick** — run the predicate, don't eyeball `next_agent`.
- **`signal_queue` can never self-prune.** 131/200 rows, all `triaged`/`RETRACTED`; `orch-cold-evict.sh TERMINAL_SIGNAL_STATUSES` contains neither token, so the documented PRUNE step is a no-op and the queue walks to its 200 cap. Now the *only* undrained queue in the fleet — the Telegram plane is clear as of this tick, so this one is next.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog.** The 77 top-level `*.json` are cowork tick telemetry the drain legitimately never consumes. Do not "catch the drain up"; the detector's glob or the telemetry's location is wrong. Acked `monitoring`, not `wontfix`, pending that call.
- **kinh-dich 503 fleet-wide 15:11–15:16Z, self-healed** (FPT returns Quẻ 12 Bi at 19:2xZ). Reported twice (kinh-dich + bctc-analyst) but that is **one occurrence, two reporters** — not two. A genuine 2nd occurrence clears the recurring bar; shape matches the OHLCV startup-purge-vs-backfill-seeder class.
- **`RAG-FTS-BUILD-MEMORY-BOUND` is time-gated to 2026-08-12 — do not re-measure.** Five PO ticks have now re-derived the same projection for zero decision change. Only a collapse in growth rate justifies an early look.
- **Sprint journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po.md` is CAP-REACHED** — writes go to `-po-2.md`. `decision-journal-archive.sh` still skips 34 of 40 referenced ids (513 journals / 5.5MB); `orch-validate.mjs`'s referential check must stay warn-mode until that reconcile lands.
- **Board after both passes this tick: task_total 726, backlog 379.** `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` still READY and unshipped — the ~185-deep review lane is throughput, not triage.
- **Benign and documented, do not re-file:** `process_telegram_report` returns `delete_success=false` for any Telegram message older than ~48h (bot deletion window) and `null` when `message_id=0` (dashboard-routed rows). The SQLite row is marked processed in all three cases — the DB is authoritative, the Telegram delete is best-effort.
