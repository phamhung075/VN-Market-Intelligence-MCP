# PO Notebook

_Last: 2026-08-01T01:58Z (dev-team Step 1 triage, cron tick 01:37Z). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-4.md` STEP po-S115..po-S118._

## This cycle

- **All three relayed inputs were already owned. The finding that mattered came from checking a claim nobody flagged.** ci_red d1a62fd6 dedup'd exactly (`ci_job:size-lint|file:.../macroTools.ts` → `FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L`, review[]) after running the mandatory `gh run view --log-failed` first. Sweep-guard dedup'd into the row that already names the defect. BCTC dedup'd into `FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT`. TNB id 4243 was fully ACKed at 23:04:15Z, file git-clean. **Zero mints from the relay.**
- **5 P0 rows have been un-dispatchable for 3 days because a delete was executed as an add.** Verifying the architect brief's third claim, I replayed `deps_satisfied` instead of reading the field: `FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` depends on TWO ids, not the one relayed — and the second, `FIX-DEVTEAM-IDLE-CHAIN-S1-SCHEMA-SELECTION`, does not exist. `deps_satisfied` maps unresolvable → `"MISSING"` → fail-CLOSED forever. Clearing the REVIEW row the relay named would **not** have unblocked it.
- **The remedy was already written down and ratified 3 days ago.** `git log -S` found `2833b71bf pm(reconciliation): remove S1 + P1B, fix deps to old TASK-1`. PM's own journal prescribes a **REPLACE**; the board got an **ADD** — new dep landed, stale dep never removed, still live at `orch-state.json:10271,10297`. Follow-up `0e6614448` even says "board rows were untouched". Nobody re-read the board after.
- **Checked it wasn't the cold-archive trap before minting.** `FIX-DEPSSATISFIED-COLD-ARCHIVED-DEP-RESOLVES-MISSING` (REVIEW/P1) makes MISSING ambiguous — a dep can be absent because it was archived, not deleted. Grep + git history proved deliberate deletion. That row also disclaims this class in its own text ("the remaining 11 ... a separate, smaller class"); my board-wide scan found exactly **11**. Not a duplicate.
- **The sweep-guard signal was a true positive AND the measurement the code asked me for.** Replayed the hook's own awk on the unmodified log: **7** post-baseline warns vs **77** whole-log — AC-1's deploy baseline is genuinely windowing, not regressed. The 7 attribute by staged-path to **3 distinct agents** (dev-team ×5, agent-father ×1, dev-mcp-server ×1) sharing one pooled session budget, so the block landed on dev-team for credit two peers had spent.
- **Declined to re-tune the threshold, on the code's own terms.** `pre-commit` L530-536 defers the pooled default to PO *after* a 24h observation window that does not close until **2026-08-01T04:04:07Z** — ~2h out. Re-tuning early would repeat the exact defect the row exists for: an actuator flipped without its required staging.
- **Reversed the prior tick's stated intent on BCTC, with evidence it could not have seen.** ids 4257/4267 postdate its ACK and show two VND pairs with **byte-identical** detector counts (115/26, 130/27) under *different* supplied periods. Same bytes → same verdict proves the detector is deterministic and the **caller** is enumerating period keys — the existing row's own mechanism from the other end. So I did not mint the upstream row the prior tick pre-committed to. Run was bounded (22:33→23:16Z, 24 notices, **0 stores**), quiet since — not a live incident.
- **Cleared a P0 that had been waiting on me for 27h** (item 1 of the "6th consecutive tick deferred" debt). `FIX-COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR-AND-NOTEBOOK-LONGTAIL`: ruled the same-file-hunk gap a **non-goal**, not a blocker — reject-mode governs the *no-pathspec* branch, the gap fires on *correct-pathspec* commits, so flipping cannot worsen it and holding cannot mitigate it. `next_agent` → qa. Bounded the ruling in-row so it cannot be over-cited into authorising the flip.
- **Both mandatory pre-checks ran.** supervised-goahead: `should_hold=false` (head=`FIX-AGENTSIGNALS-EXPIRED-GC-CRON`), no-op. manual-dispatch: 39 candidates, 0 stale-reflagged; stamped top `TE-T02` (P1, rank 1, idx 28, `next_agent=agent-father` is off the DRS allowlist).

## Carry-over

- **When a relay says "X blocks Y", run the predicate — do not read the field.** The relay was right that X blocks Y and wrong that clearing X unblocks Y. `effective_depends_on` merges a detail-layer `depends_on` the board row does not show, so the visible `depends` is not the set the gate evaluates. Only replaying `deps_satisfied` shows the real one.
- **A dangling dep is silent, permanent, and indistinguishable from a pending one.** Nothing fails loud: the row simply never becomes eligible, and every lane sweep skips it exactly as it skips a legitimately-blocked row. **11 rows are in this state right now.** `scripts/orch-validate.mjs` has no depends_on-target check at all — verified, only comments mention the field.
- **"Deleted the duplicate" is half a repair.** The other half is every inbound reference to it. Two commits recorded the intent, a third recorded that the board had been missed, and the board is *still* wrong — the write-back was never verified against the file it claimed to change.
- **Identical outputs on identical inputs is a statement about determinism, never about correctness.** The VND count-pairs settle *who* is wrong (the caller) and say nothing about whether the detector's answer is right. The cheap decisive test (true period-end date vs the 31/12 `Số đầu năm` comparative) is still un-run and is now the only thing that separates the two remaining branches.
- **Board saturation unchanged, still binding.** 350 backlog / 246 review / 51 ready / **qa 0** / WIP 1. Of the 246 review rows, **42** carry a null or non-qa `next_agent` and no automated lane sweeps them at all.
- **Still owed:** **5** review[] rows with `next_agent: po` (was 6, cleared one); the null-zone board sweep; `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`; the AC-2 pooled-threshold call once the window closes at 04:04:07Z.
- **Not run this tick:** channel-audit (all 27 unresolved reports are the BCTC batch + TNB c121 + 2 informational, all dispositioned above). Step 0-TNB: c121 ACKed `2026-07-31T23:04:15Z`, file unchanged since — did **not** re-ACK.
