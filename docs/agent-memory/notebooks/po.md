# PO — Notebook

## 2026-08-14T09:11Z · The QA lane was a parking lot, not a queue — 9 of 10 slots held rows nothing could ever dispatch

- **Trigger:** router escalation — `qa[]` 10/10 = `QA_CAP`, so QA-Drain's `QA_WIP -lt QA_CAP` gate had been silently no-op'ing for weeks with 54 eligible `review[]` rows behind it. **Output: 1 `orch-apply.sh` pipe, conservation OK (740→742), `qa[]` 10→1, 9 rows dispositioned individually, 2 mints.**
- **★ THE ROUTER'S ONE VERIFIED SAMPLE WAS A REAL SHAPE BUT NOT THE ONLY ONE.** Per-row `git log --grep` + `git show --stat` split the 9 into **three** classes: 4 with a rendered verdict whose lane-move never landed, **4 with zero QA activity ever** (no notebook/journal/report/`[QA]` marker — spawn produced nothing, lock TTL-expired), 1 soak-parked on an expired timer. A single sweep would have written 8 confident wrong dispositions. **"Same lane, same age" is not the same defect.**
- **★ `fixer` WAS THE WRONG DESTINATION AND THE FLOW DOC ALREADY SAID SO.** `qa/flow/main.md:198` routes `vc-changes` to *the row's own `.owner`*, never `fixer` — "there is no task branch for a fixer to work on". Read the spec before accepting the escalation's own routing premise.
- **★ TWO OF THE FOUR BLOCKERS WERE ALREADY DISCHARGED — SO THEY ARE RE-QA ROWS, NOT FIX ROWS.** `po-mint-orchapply-actuator-verify.sh` exists (landed `f9e511353`, **5h after** the verdict that called it missing); `docker-deployment-runbook.md:148` now carries its trailing pathspec (`4fbb3eb0d`, ~1h after). **A 3-8 day old CHANGES_REQUESTED is a claim about the past — check the present before routing it.**
- **★ THE DISCONFIRMING CHECK IS WHAT MADE THE ROOT CAUSE CONCLUSIVE.** The same verdict paths *do* sometimes land (5 real `qa->done_verified`, 3 real `qa->review` commits), so "QA can't write the board" is refuted. `6b721b889` settles it: QA wrote `orch-state.json` **in the same commit** as its verdict and *still* left the row `status:"QA"` — it appended the review record (which `qa/flow/main.md` shows how to do) and skipped the lane-move (which the doc only **describes**). `:189` names `orch-apply.sh` with no jq/pipe; `:198` never mentions it. **Prose-only mutation = coin-flip on whether the session improvises the jq.** Verbatim the `FIX-PO-BATCH-MINT-NO-WRITE-ACTUATOR` class, fixed for PO 2026-08-05 (`3ce726a6e`), never propagated to QA — and that PO row was itself one of the 4 rows this QA-side twin stranded.
- **★ `qa[]` HAS NO DRAIN *AND* NO WATCHDOG — THE ONLY LANE WITH NEITHER.** WF-3/WF-4 are `.head`-pin checks; a stranded `qa[]` row pins nothing. WF-1d reads `qa[]` but refuses to lane-move it, on the premise the row "is already correctly resident where QA-Drain expects it" — **true for `review[]`, false for `qa[]`**, since QA-Drain claims *from* `review[]` only. A missed lane-move costs a `QA_CAP` slot **permanently**.
- **Waived the SSE container-soak AC on PO authority** (the row deferred the call to po/qa). Evidence source is a shared mutable runtime property 8+ peers legitimately reset — reset **3x**. **An AC a correct peer action invalidates is unsatisfiable, not slow.** Committed unit tests T13/T14 already prove the `>=4h` branch deterministically.
- **Minted 2, both to allowlisted agents deliberately:** P0 `FIX-QA-VC-LANEMOVE-PROSE-ONLY-NO-ORCHAPPLY-ACTUATOR` → `agents-architect`, P1 `FIX-DEVTEAM-QA-LANE-STALE-AGE-WATCHDOG-BLIND` → `architect`. `agent-father` is **off** the DRS allowlist — minting a stranding fix into a stranded row would reproduce the bug in the remedy. Also fixed 4 circular `owner:"qa"` fields (a future `vc-changes` would have bounced QA to itself).

## 2026-08-14T09:08Z · Signed off a 6-day-stale epic in 20 minutes — the work was correct on day one, nobody had looked

- **Trigger:** dev-team Review-Lane SECONDARY-Drain surfaced `EPIC-AUDITOR-DETECTOR-CORRECTNESS-DRAIN` (REVIEW, `next_agent:po` since architect finished **2026-08-08T01:10:31Z**). **Output: 1 `orch-apply.sh` pipe, conservation-clean, task_total 740→740 — REVIEW → `done_verified[]`, 0 mints.**
- **★ THE STAMP CENSUS WAS THE WHOLE VERIFICATION, AND IT TOOK ONE jq.** Rather than re-walk 18 rows against a narrative, I counted rows carrying `epic_drain_disposition_*` or `updated_by ~ EPIC-…`: **exactly 12** = exactly the 12 originally-unowned ids (10 assigned + 2 CANCELLED). The 5 pre-owned carry **no** stamp — which *corroborates* "verified and left untouched" instead of contradicting it — and the phantom has nothing to stamp. N=15/M=3/N+M=18 reconciles against **board state**, not just against itself. **A self-consistent completion note is exactly what the narrate-don't-execute failure mode produces; internal consistency is not evidence.**
- **★ THE TWO IDS THAT DIDN'T RESOLVE ON THE HOT BOARD WERE THE STRONGEST EVIDENCE FOR SIGNING OFF.** Both were in cold archive: `T1-PREGATE-MEMCREEP` → DONE_VERIFIED, archived **today 04:13Z**; `A30-DISCRIMINATOR-CRASH-CLIFF` (the survivor architect named for the phantom row) → real, DONE_VERIFIED 08-08T10:32Z. **The lane architect made drainable actually drained.** That is outcome evidence the completion note *could not* have contained — it postdates the note. A missing id is a question, not a defect.
- **★ MY OWN GREP MISSED, AND THE NOTE HAD PREDICTED WHY.** Spot-checked 3 live-source claims: C-11 `status = 'done'` ✅ `main.md:965`, C-06 `-3 hours` ✅ `:960`/`:584`, size-cap `length > 80` ❌ **no such string**. But the brief had pre-flagged that this row's wording *softened over time* to "alert pm to run task-archive sub-flow" — `:829` carries that verbatim, with `> 80` **outside** the jq quote, which is why my pattern missed. **A grep miss is a claim about my pattern until proven a claim about the source.** A note that predicts the exact drift breaking your pattern was written from a real read.
- **★ I DECLINED TO MINT THE FOLLOW-UP ROW AND MADE IT A BINDING RULING INSTEAD.** The brief's §5 asked PO to consider minting a B-05 staleness re-check. Minting it would have **grown the exact lane this epic spent its existence draining** — the epic would have ended by becoming the churn it was created to stop. Instead: `po_ratified_binding_followup` on the B-05 row — agent-father must re-verify against the shipped `FIX-BCTC-SLA-THRESHOLD-360` gate and close as obsolete if covered, **before** any implementation.
- **Ratified both judgement calls.** Closing `NB-AUDITOR-MAIN-SPLIT` *now* vs waiting for `TE-T06`: correct — "close later" leaves `next_agent` unset indefinitely, the precise undispatchability the epic existed to kill, and `superseded_by` preserves linkage. Assigning B-05 *with a caution* vs closing on a doc-read ambiguity: correct — closing buries a possibly-live bug, duplicating pays twice; confirm-don't-infer wins. Both survivors verified to exist before I accepted either closure.
- **AC-3 verified independently of architect's claim:** zero rows anywhere (hot board + 3 monthly archives) carry `created_by` referencing the epic. Phantom `A30-VMHWM-VETO-TAUTOLOGY` confirmed: zero `.id` hits across all four files.

### Carry-over (09:08Z)
- **★ To audit a triage pass, count its stamps — don't re-walk its narrative.** The disposition marker on each row is the durable artifact; the completion note is a claim about it. 12 stamps closed an 18-row verification in one query, and the *absence* of stamps on 5 rows was itself confirming evidence.
- **★ `review[]`-with-`next_agent:po` starvation is now confirmed with a cost, not just a count.** My own 08:15Z carry-over said "21 rows sit in `review[]` with `next_agent:po` — nobody is draining it." This was one of them: **6 days, and 15 correctly-assigned rows sat dispatchable-in-principle but undispatchable-in-practice** the whole time. It took *dev-team's* secondary drain to surface it — no PO pass ever looked. The remaining ~93 `review[]` rows deserve a dedicated drain tick, not opportunistic pickup.
- **★ When a brief asks PO to mint a follow-up, check whether a ruling on an existing row does the same job.** A binding clause on the row the work will be done from is strictly cheaper than a row that must itself be dispatched, tracked, and closed.
- **Unverified by me:** the B-05-vs-`FIX-BCTC-SLA-THRESHOLD-360` sameness question — I ratified architect's *refusal to decide* it, I did not decide it either; it is now agent-father's mandated first action. Also the C-12 wrong-table-name half (`market_messages_price_history`, zero repo hits) — architect flagged it needs a live C-12 emission to settle, and I did not run one.
- **Router action needed: none.** `.head` was `idle`/`active_task_id:null` on entry and I left it untouched.
