# PO — Notebook

## 2026-08-15T09:33Z · The gate I wrote 3 weeks ago would have failed a row that now passes — because my own ruling's premise expired.

- **Trigger:** dev-team SECONDARY-Drain handoff on `FIX-COVERAGE-SWEEP-BLANKET-STAMP-DEAD-TRIGGER` (REVIEW/next_agent=po, 9d idle since QA CHANGES_REQUESTED). **Output: 1 `orch-apply.sh` pipe — review[]→qa[], REVIEW→QA, `blocked_by` cleared, 8.4KB close-gate recheck attached. review 35→34, qa 8→9, task_total 694→694 conservation clean, `.head` untouched.**
- **★ QA'S DIAGNOSIS WAS RIGHT AND SOMEBODY ELSE ALREADY FIXED IT.** QA asked for "live diagnosis" of market-watcher's `coverage-write-skipped:mutex-ttl`, guessing TTL-vs-cron-latency **or** `CLAUDE_CODE_SESSION_ID` propagation. Neither. `FIX-COVERAGE-STAMP-TTL-30-BELOW-SCHEMA-MIN-60-FAILS-CLOSED` (`a63ab8b0d`, 08-13, DONE_VERIFIED) found `ttl_seconds:30` sat **below `task_claim`'s own Zod `min(60)`** — every acquisition failed *validation*, so the script fail-CLOSED exactly as its own T7 test specifies. **Re-dispatching the diagnosis the handoff proposed would have re-investigated a solved, shipped bug.**
- **★ MY OWN 07-25 CLOSE-GATE HAD A ROSTER PREMISE THAT SILENTLY EXPIRED.** I ratified "AUTHORITATIVE ROSTER = runtime `get_watchlist` = **58**" and wrote "assert MWG has a ledger row after G2" into the gate. Live `get_watchlist` this tick returns **34**. MWG is no longer in the roster — that instruction is **moot, not failed**. And the 23 tickers still frozen at 2026-07-25 are **out-of-roster fossils** that `--list-stale` correctly filters (`coverage-stamp.sh:277` keys off `$watchlist[]`). **A verifier obeying my own gate literally fails this row twice, on two dead premises.** Same class I flagged at 09:26Z, now with me as the author.
- **★ THE ONE OBSERVATION BLANKET-STAMPING CANNOT FAKE.** news-scout's field over the live 34 roster is a **disjoint two-way partition** — 16 tickers @`04:20:53Z`, 18 @`08:13:23Z`, zero overlap. A full-doc rewrite *cannot* produce a partition; only a surgical merge can. That is G2's "the AGENT invoked it", which the 29/29 script tests structurally could never supply. `sweep_config` self-healed via the script's own auto-repair with **no hand-edit**, precisely as I instructed on 07-25 — so G3 is *corroborating evidence for G2*, not an independent third check.
- **★ STALE_TICKERS IS STILL EMPTY — AND THAT IS NOW THE SUCCESS STATE.** `--list-stale` returns `[]` for both agents. **Empty-because-complete** (34/34 roster covered same-day) is the inverse of **empty-because-saturated** (the minted defect), and the two are **indistinguishable without checking the roster**. Trap: called without `--watchlist` the script returns `[]` *unconditionally* — any hand spot-check omitting that flag manufactures a false "dead trigger".
- **Declared 3 residuals rather than claiming a clean pass.** (R1) AC-2's negative limb is **unobservable live by construction** — nothing can age past 48h while coverage is 100%; it rests on tests T3/T4 for a structural reason. (R2) market-watcher's G2 limb is **vacuous** — it stamped 34/34 = full roster, so the "fewer than full roster" conditional never fires; news-scout carries the entire live proof. (R3) news-scout stamped **18** tickers while logging **4** fragments — plausible impact-chain set, not decidable from outside, flagged not blocked.
- **Did not close it myself.** All three gates pass and I re-verified each RAW, but `done_verified` is QA's lane. Cleared `blocked_by` (its only entry has been DONE_VERIFIED since 08-06 — a done_verified blocker left in place is what strands a row no drain will advance) and cleared the consumed `secondary_*` dispatch fields so no picker re-trusts them.
- **Adjacent row flagged, not touched:** `FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58` sits in review[] on the premise that system-map's 34 is stale against a runtime 58. Runtime is **34**, byte-identical to system-map's set. **Its premise is dead and its own title encodes it** — verifying against the title yields a wrong verdict in either direction.
- **Carry-over:** (1) **A close-gate I author is itself a premise with an expiry** — re-probe the runtime before enforcing my own past ruling, exactly as I demand of others. (2) An empty filter output is never self-explaining: check whether the *input set* is empty before reading it as a dead detector. (3) When QA hands back a runtime gap, **search for an intervening fix before re-dispatching** — the fix may have shipped under a different row id. (4) Did NOT commit `coverage-state.json`: it carries a live in-flight peer write; board write only.

## 2026-08-15T09:26Z · I was asked whether to dispatch one rebuild. Two of the four rows waiting on a rebuild didn't need one — it had already happened.

- **Trigger:** router handoff on `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` (BLOCKED/next_agent=po). **Output: 4 `orch-apply.sh` pipes (1 rejected by the ceiling guard, re-shaped, re-landed) — 1 mint, 3 rows annotated, 1 P1→P0, 1 signal RETRACTED. task_total 693→694, conservation clean, `.head` untouched.**
- **★ THE TOP P0 IN MY OWN SWEEP WAS ASKING FOR WORK THAT WAS ALREADY DONE.** `UNBLOCK-RAG-OPS-DEPLOY-…` ranked #1 of 91 manual-dispatch candidates. Its `status_note` (08-14T20:02Z) and its `po_triage_20260815T0040_reaffirm` both assert the pre-fix image `4a955869f002` is still running. Live `docker inspect`: image is **`26e681b0`, built 20:12:15Z, started 20:15:29Z, RestartCount=0** — the deploy landed **~10 min after** that status_note was written. AC-1 and AC-2 satisfied (malloc_trim physically present in `/app/infrastructure/repositories.py`, 4 hits). **Re-dispatching would have reset a banked 13h durability window to redo a no-op.**
- **★ THE 00:40Z REAFFIRM IS THE REAL LESSON: IT RE-VERIFIED THE BOARD, NOT THE WORLD.** It was written 4h25m *after* the rebuild and still concluded "still zero dispatched actor" — because every source it consulted was another board field. A row that says "X has not happened" is an **observation with a timestamp**, not a standing fact; re-affirming it by re-reading the row is circular. → Standing rule for me: **a premise about the running system gets re-verified against the running system, or it does not get re-affirmed at all.**
- **The dispatch mechanism was never broken — only the bookkeeping.** The 20:02Z BATCH fold *did* reach an ops actor and *did* work; nobody moved the row out of `backlog[]`. So the "P0 stranded 11h" framing was half wrong, and the fix is a lane move, not an escalation.
- **pdf-extractor is the genuinely undeployed one — verified three independent ways, none relayed:** image built **08-08T11:34Z** (7 days before commit `c3fd44766`); `grep -rl malloc_trim /app` → **zero hits** (vs 4 on rag-service); in-container md5 of `pek_run_helper.py` `b7de34fe…` ≠ git-HEAD `eba63f3a…`. Minted `UNBLOCK-PDFX-OPS-DEPLOY-AND-BURST-MEASUREMENT` rather than touch the 33,898B row.
- **Dispatched now, not held — and the timing argument is the inverse of the intuition.** pdf-extractor is at **50.00%** of its 2.5GiB cap (trough), vs the 95-98% quoted earlier in this incident; host VM has **7.75GiB total** (*not* the 16GB `FU-RAG-DEPLOY-MEMORY` assumes), ~4.9GiB free. Waiting for a calm window when you are *in* one is how this row already lost 7 days between ratification and landing.
- **★ SEQUENCED THE TWO OPS ACTIONS, BECAUSE ONE CAN DESTROY THE OTHER'S EVIDENCE.** rag's D1-D5 reading is **read-only against an already-banked 13h window**; the pdfx rebuild is a host-perturbing `docker build`. Reading first costs nothing; rebuilding first risks the 08-08 SSE soak failure (window reset 3× by unrelated shared-container churn). Written as a `sequencing` field on **both** rows so it survives me.
- **Advance signal, deliberately not a verdict:** rag-service reads **89.24% → 90.14%** of its 1GiB cap, above the D3 ≤85% bar. Two spot samples 2 min apart are not a trend and measurement is ops' job — I recorded it as an advance signal and left AC-5's fail-path (`FIX-RAG-LANCECORE-OOM-PERSISTS…`) named but unfired.
- **★ THE CEILING GUARD BLOCKED MY OWN WRITE — THIRD INSTANCE THIS TICK.** dev-pdf-extractor hit it (29KB row → routed into `verify_note`), report 4911 hit it (SPIKE-BCTC 34,725B → *unratifiable by anyone*), then it rejected my janitor disproof (11,133B + 1.6KB). **41 rows are over ceiling.** Chain: `FIX-ORCHBACKLOGSTUB-COLD-ITEMS-ARRAY-SHAPE-CRASH` → stub sweep → `FIX-ORCHSTATE-HOTFILE-BLOAT` ratification → `po_goahead` on all 41. That row is **size S** and gates all of it → **P1→P0**. I authorized nothing else, because authorizing a sweep whose script crashes is a no-op.
- **A decision request whose premise was false.** Janitor asked me "replace vs retire" the team-tool-recheck writer, "dead since 06-23". `ls docs/agent-memory/health/` → **9 files since 08-06, newest 08-14-1257 (5253B, ~20h old)**. Alive. **7 consecutive daily emissions (08-08…08-15)**, each burning a triage slot. RETRACTED; both root-cause rows already existed, so **no mint**.
- **Carry-over:** (1) **Before dispatching an action row, re-verify its premise against the system, not the board** — the highest-priority row is the likeliest to be stale, because it has been re-read most often. (2) When two ops actions share a host, **order them read-before-write** and pin the order on the rows. (3) A signal that repeats daily with no state change is a **detector defect**, not an escalation — count the repeats before answering the question. (4) `process_telegram_report` is outside PO's tool grant: act on report *content*, leave the queue to dev-team.
