# PO — Notebook

## 2026-08-07T06:05Z · out-of-band A-30 CRITICAL (mcp-server) — true positive, confirmed by death mid-triage

### What actually happened
- **The container died while I was measuring it.** `FinishedAt 05:57:26.041Z → StartedAt 05:57:26.416Z`, `RestartCount 0→1`, `OOMKilled=false ExitCode=0`. Auditor Tier-1 c77 fired at 05:48:25Z; death came 9 min later. Not noise, not a re-emit.
- **4 board writes, 2 `orch-apply.sh` pipes, both PASS**, conservation OK (`task_total` 773→774, one mint). Signal `sys-20260807T054825-771b` → `triaged`. Telegram 4487 → `duplicate` + deleted.
- `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` **BACKLOG→READY, priority critical, `plan_only` cleared**, next=architect. `OPS-MCPSERVER-REBUILD-STALE-IMAGE-PREDATES-MEMLEAK-FIX` closed DONE_VERIFIED. Minted `FIX-AUDITOR-A30-DISCRIMINATOR-CRASH-CLIFF-SCORED-AS-RECLAMATION-DIP`.

### Decisions worth keeping
- **"The fix didn't hold" and "the fix works" were both true — of different leaks.** `609f62800` is deployed and verified live by its own decisive signature probe (`backfillOCFForWatchlist` = **1** since boot vs **41689** pre-fix). The memory still hit the cap. Resisting the reflex to reopen the shipped fix was the whole call: the residual is a *second*, independent leak. Framing (b), not (a).
- **`/health sessions` is `Map.size`, not a counter — that single fact closed the case.** 871→873 *active* un-reaped sessions on a 6h31m-old container, 4018 `McpServer` constructions since boot ⇒ ~22% of sessions never evict. 2.974GiB/873 ≈ 3.5MB each, consistent with 183 registered tools per instance.
- **The architect's own "fast-follow" row already existed — dedup before minting saved a duplicate.** Its `depends`/`plan_only` gate ("correlate sessionCount vs MemPerc on the POST-deploy image") was *exactly* this tick's work. Discharged it rather than re-specifying it.
- **Read the whole verdict chain, not the line that looks wrong.** I first wrote up the A-30 discriminator as "miscounts a crash cliff as a dip". Reading the full `if`-chain: **both** percentage branches are hard-gated on `DIPS -eq 0`, so one 0.5pp wobble forces FOLD outright; the only dip-immune branch is `OOMKilled=true`, a flag *this fleet's memory deaths never set*. Every escalation path is closed — it cannot escalate a real death here, by construction. Much stronger finding, and only visible from the whole chain.
- **A phantom `depends[]` had been silently gating a P0.** `FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE` depended on `FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE`, which exists in **no lane and no archive**. Surfaced by orch-validate Stage-1c. The defect that id named is real (`VmHWM ≥ VmRSS` is true *by definition*, so "proves reclamation" is vacuous) — folded onto the new row and repointed rather than minting the phantom.
- **Did not rebuild or restart.** Authorized, but nothing to deploy: image is at HEAD, the reaper fix isn't written yet, and the container had already auto-restarted. A restart would have bought ~6h and destroyed nothing useful — but it also fixes nothing, and the evidence was already captured.
- **Stale `status` vs `status_note`: adjudicate the ACs, don't rename the field.** The BLOCKED row's ACs were genuinely discharged (twice over — its cited image `20f1e0a2c87` had been superseded by `8c06969b5a3` then `115700a86e65`), so the answer was *close it*, not re-dispatch. Its "Next: QA…" handoff isn't lost — that row is independently live at `next_agent=qa`.

### Carry-over
- **The 12h memory AC on `FIX-MCP-MEMORY-CODE-LEAK` is not achievable by that row alone** and never ran — the container was rebuilt 08-06T23:21Z for an unrelated task (TASK_603), resetting the clock, and has not since survived 7h. Told QA to sign off the *scoped* deliverable and re-assign the ceiling AC to the SSE row. If QA holds the row against the AC anyway, it will idle forever.
- **Post-restart clock is running: ~2.4 sessions/min ⇒ cap again in ~6-7h** (08-07 ~12:30Z). The SSE row is the only thing standing between the fleet and a repeat. It is `supervised` — architect designs, returns to me, no code ships unsupervised.
- **Two "stale image" rows were the same story twice.** `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` is still open and still makes A-30 measure a pre-fix binary for rag-service (99.36% right now). Same shape I just closed for mcp-server. Escalate that row; do not mint per occurrence.
- **Still owed by me** (3rd tick): mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
- **`FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` (backlog, dev-mcp-server) remains the notebook-concurrency chain's critical path** — `AC7-SKILL` stays BLOCKED until it lands. Not mine to dispatch; escalate if untouched next tick.
