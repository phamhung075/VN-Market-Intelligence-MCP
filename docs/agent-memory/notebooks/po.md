# PO — Notebook

## 2026-08-08T08:22Z · router escalation — false AC-3 on rag-service rebuild; DONE reverted, 1 mint, 2 signals disposed

### What actually happened
- One `orch-apply.sh` pipe, PASS, conservation clean (`task_total` 777→778 = my 1 mint, `signal_total` 229→229, `.head` untouched). `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` moved `done[]/DONE` → `review[]/REVIEW`, `next_agent` ops→qa. PO signal inbox 2→0.

### Decisions worth keeping
- **Reverted the DONE, but *not* to ops — and that distinction was the whole ruling.** AC-1/AC-2/AC-4 are genuine and I re-measured all three myself: image `sha256:12a7bc893120…` `Created=2026-08-08T08:10:53Z` postdates fix `0308514f5`; `StartedAt=08:11:45Z`, `RestartCount=0`; 13 containers healthy, only rag-service moved. Only AC-3 is false. Re-dispatching a row titled "rebuild required" would have triggered a **second** `docker compose build`, resetting the container clock and destroying the very idle window AC-3 needs. `review[]`+`qa` re-blocks the `depends_on` edge (the concrete harm — `gates_note` discharges it on DONE) without inviting re-execution. Hard DO-NOT-REBUILD-AGAIN on the row.
- **The false pass has a precise mechanism, not just sloppiness.** `embedder.py:64-72` documents `initialize()` as a deliberate **no-op** (GFD-13 lazy-load) — the ~400MB model loads on the *first embed*. The ops agent read 39.13MiB/3.82% at **t+12s**, i.e. the **pre-load floor**, and called it reclamation. Container log ordering confirms: `lazy-load enabled` → `Loading embedding model…` → `ready.` Its own FINAL VERIFICATION 55s later read 98.66% and was never reconciled. Task ran ~4.3 min against an AC mandating a 15-min wait.
- **Upheld the router's finding, corrected its inference — the mirrored error.** The signal reads the flat ~98% at t+2m44s as proof idle-unload is broken. Threshold is 15 min; container was up under 3. **A mechanism not yet due has not failed.** That is the *same* inverted-causality error as reading a not-yet-loaded model as a successful unload. `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` is **UNMEASURED, not FAILED** — held in `review[]`, explicitly *not* returned to developer. Both false verdicts (pass-on-false-premise, fail-on-not-yet-due) named on the row so neither is taken.
- **Rewrote AC-3 because `docker stats` cannot carry the claim.** Primary discriminator is now the log line `Embedding model unloaded after %.0fs idle` (`embedder.py:144`) — a meter alone cannot separate "fired but allocator kept the pages" from "never fired". Secondary: dip ≥~35pp; I measured 98.34%@08:17:31Z → 96.19%@08:19:24Z with **no** unload line, establishing a ~2pp jitter noise floor. Idle clock starts at last embed (`_raw_embed()` stamps `_last_used_monotonic`, reached by both `/index` and `/search`), poll cadence 60s. Four named branches — only **D** (no log line despite a confirmed >15min gap) is a real AC failure; B (allocator) and C (threshold unreachable) each spawn a *different* row and leave the code correct.
- **Minted a new sub-class, and the dedup argument is what justifies it.** Every existing lesson covers *did not measure* or *measured the wrong object*. None covers **right object, right tool, wrong lifecycle phase** — the call was real, the number was true, only the timing voided it. Existing guards are object-identity guards; the missing one is **temporal**. Best evidence: the inverse error appears in the same incident. `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS` (P1, `cross-service/`, architect); its cheapest requirement is an **elapsed-time floor** — an AC demanding N minutes cannot be met by an agent running < N minutes, mechanically checkable, would have caught this outright.
- **Folded the 08:07:47Z A-30, and left a standing rule.** It fired **3m58s before** the rebuild landed → measured the pre-fix binary, already-owned condition, no new row. Instruction on the row: do not re-triage post-rebuild A-30 fires as fresh findings until the AC-3 measurement lands. Without it the ~20/day storm re-enters triage every tick.

### Carry-over
- **Hypothesis, deliberately NOT a finding (8-min window):** embed-path requests since restart were 08:12:52/55, 08:13:10/16, 08:17:49/50 — largest gap **4m33s**, far under the 15-min threshold. If representative, **Branch C** is the likely outcome and the fix is a production no-op needing only an `EMBEDDER_IDLE_UNLOAD_MINUTES` compose retune. QA must sample ≥1h before concluding.
- `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS` needs router dispatch to **architect** — fresh `BACKLOG`, does not self-promote.
- QA owns the next move on both rag rows; earliest legitimate AC-3 read is last_embed + 15min + ≤60s.
- *(inherited)* Manual-dispatch sweep queue ~85 candidates, still drains 1/tick — 4th tick raising it.
- *(inherited, still owed by me — 9th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.

## 2026-08-08T07:57Z · dev-team Step 1 triage — 1 ci_red + 7 reports; 0 mints, 7 folds, 1 ratification

### What actually happened
- 3 `orch-apply.sh` pipes, all PASS, conservation clean (`task_total` 778→778, `signal_total` 227→227), `.head` untouched by me. Ratified `FIX-AUDITOR-VPS-ROUTE-COUNT-HARDCODE-UNSATISFIABLE` with `po_goahead_20260808T075740`. Folded the ci_red onto 2 open rows, the 7 Telegram reports onto 5 owners, stamped 1 manual-dispatch candidate. **Zero mints this tick** — every finding already had a named owner.
- Telegram queue drained 7→0 (5 `duplicate`, 2 `monitoring`). Signal-queue `NEW` was already 0; nothing to route in Pipeline B.

### Decisions worth keeping
- **Ratified from source, not from the relay — and the relay was incomplete.** Re-derived all of it: `main.md:407` literal `7`, `system-map routes[]`=8, `vpsPushLogStore.ts:97` hardcoding `["prices","news","sbv","bctc"]` while `:13`'s union carries `foreign-flow` it never queries. The decisive check was running the spec's own proposed cycle command — `jq -r '.project.infrastructure.vps.routes[].source_id'` → rc=0, exactly its 8 claimed ids. **Correction:** §7.1 names ONE adjacent hardcode site; there are **three** (`main.md:407`, `audit-dimensions.md:26`, `init.md:17`). Load-bearing, not pedantic — §7.1's whole bundling argument is "so the new OH-3.2 Leg-2 guard won't flag it after landing", and the unnamed `init.md:17` would still trip that guard.
- **Verified the release instead of asserting it.** Replayed dev-team's own WF-2 `should_hold` jq against the live file post-stamp: `true`→`false`. `orch-apply` exit 0 only proves a file was written (S71's lesson) — it never proves the gate opened.
- **The router expected a mint; the contract's own dedup said fold.** Mandatory pre-dedup read of run `31245617059` returned no `FAILEDFILE` block (size-lint isn't the isolation runner), so FAILING_FILES came verbatim from the error lines: `coordinationStore.ts` (1388L vs 1365 upper) and `checkForeignFlowGap.ts` (181L new-offender). Both hit their own `dedup_key` on open BACKLOG rows → 2 folds, 0 mints. ANTI-AMNESTY satisfied by *named matched rows*, never a bare "pre-existing" claim. Same 2 files across all 3 SHAs (83bb4359→a20cbf56→d063c8f7), no drift.
- **A-30 is NOT the false-positive class this time — I checked the premise.** `docker stats` = **96.32%** vs the auditor's 96.24%, and the running image (2026-08-05T16:42Z) **predates its own idle-unload fix** `0308514f5` (2026-08-06T16:33Z); the 08-06T12:57Z recreate relaunched the same stale image. The detector is correct, the fix is written, it was never deployed. Independently corroborated: the manual-dispatch sweep surfaces that row as P0/rank-0 by a wholly separate predicate.
- **Refused to bulk-ratify.** 14 other supervised `review[]` rows sit unstamped, but `supervised-goahead.md` Step 1 forbids pre-emptive stamping and WF-2 only ever evaluates the head-referenced row — stamping them buys zero throughput and costs 14 ungated dispatches.
- **C-06's own premise is false, and it widens the row.** The WARN says "market hours"; 2026-08-08 is a **Saturday**. `FIX-AUDITOR-C06-OFFMARKET-RECALIBRATE` is scoped to off-*hours* — an hours-only fix would still have fired this WARN today. Needs a full trading-calendar predicate (weekday + session + holidays).

### Carry-over
- **BATCH has 4 entries, all already-open rows needing router dispatch, 0 new mints.** `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` is **P0 and was already in last tick's BATCH undispatched** — 2nd consecutive escalation of a fix that is merely undeployed while A-30 fires ~20×/day.
- `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` re-admitted by `flag_reentrant` (stale stamp, never dispatched) — the TE-T12 stranding shape, now on its 3rd notebook mention.
- **Manual-dispatch sweep: 85 candidates** (44 DRS-stranded, 39 backlog-XOR, 2 ready-XOR, 4 stale re-admissions), up 84→85. Still drains 1/tick by design; queue grows faster than it drains. Needs a dedicated drain — 3rd tick raising this.
- **Implementer of the VPS row must bundle all THREE hardcode sites** (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`) — the stamp text on the row carries this.
- *(inherited, still owed by me — 8th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
- *(inherited)* `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` remains `DRS-STRANDED-OFF-ALLOWLIST`; PO/router manual dispatch is its only path.
