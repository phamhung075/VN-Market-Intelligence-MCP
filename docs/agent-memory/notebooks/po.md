# PO Notebook

_Last: 2026-07-13T17:28Z (dev-team triage: FR-FUNC-02-FIX disposition + 2 signal intakes; coordination_session 69b0312e)_

## Tick 2026-07-13T17:28Z — dev-team triage: FR-FUNC-02-FIX→DONE_VERIFIED + 2 new backlog rows (BATCH)
Three inputs from dispatcher (session 69b0312e, holds dedup lock task:po-triage-20260713). ONE atomic orch-apply.sh write (validate Stage0+1 PASS, conservation task_total 509→511 net +2, CAS clean). NO Agent tool → returned BATCH; dispatcher drives chain.
- **FR-FUNC-02-FIX → DONE_VERIFIED (no code):** BOUNDED-1 picked it (rank-1 eligible, priority normal); pick-time pre-verify WITHHELD. RAW-verify (read-only): gateway call_tool(get_bctc_full, HPG) → fully-structured financials (net_rev 52,900.8 tỷ, total_assets 259,327.5 tỷ, ROE 6.5%, structured_data JSON, tier 2). CHECK PASSES. Original WARN = CLUSTER-G auditor probe-error (2026-06-10 probed VCB which had NO ingested BCTC → correct graceful-empty; expected degrade, not a defect). Corroborated by phase4-verdict ("FR-FUNC-02→PASS") + burndown-strategy (CLUSTER-G, RECLASSIFY, "No code", owner qa/system-auditor). Row was mis-scoped as dev code-fix. NOT a data-ingest gap. No depends_on referenced it (safe move). backlog→done_verified with full router_verify_note.
- **+FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT (FIX, high, apps/mcp-server/, deploy-gated):** durable fix for recurring SILENT-FAILURE class — PO call = NOT a bare timeout bump (480s direct call still failed, tesseract 17+ min). Two-part AC: (1) fail-loud — push-bctc-pdf must NOT return ok:true / mark bctc_vps_queue row done on OCR timeout; keep row retryable + surface staleness; (2) route large scanned PDFs via async /api/trigger-pek-extract. Architect design touch: async-reroute vs sync-bump + threshold. Post-deploy: reset queue row 223 (HPG Q4-2025) done→pending. Closes HPG consolidated coverage-gap (followup_of HPG-DISCOVER-CONSOLIDATED-PDF). Deploy = dispatch OPS to attempt rebuild once through front door + capture classifier denial verbatim (2026-07-13 ruling), NOT user-wait.
- **+DEBT-SCRIPTS-MIGRATIONS-TSC-COVERAGE (CLEAN, low, cross-service/, plan_only):** scripts/migrations/ outside all tsconfig include globs → tsc-clean claims false-clean-by-omission. Add tsc coverage (tsconfig include OR standalone `tsc --noEmit` in CI), then fix 3 QA-surfaced errors (discover-consolidated-bctc-pdf.ts:395 Blob/Uint8Array + reparse-bctc-reports.ts x2). Zero runtime impact.
- **SYSTEMIC FLAG (BATCH recommendation, NOT actioned — first pick, churn ≤2):** FR-FUNC-02-FIX is 1 of a ~17-row CLUSTER-G `*-FIX` cohort still BACKLOG at priority normal (FR-DEGRADE-01/SEC-FUNC-01/03/SEC-TEST-01/MD-FRESH-01-03/NEWS-OBS-01/ALT-PERF-01/ALT-FUNC-03/KD-FRESH-01/ANA-TEST-01/CI-FRESH-01/FW-FRESH-01/FW-OBS-03/CO-OBS-01/PA-CONSIST-01-FIX). All re-probed PASS/INFO in phase-4 but the parallel `*-FIX` rows never reconciled → each churns through BOUNDED-1 one-by-one. RECOMMEND dispatcher batch-reclassify the cohort (owner→qa/system-auditor, no code) to converge. Did NOT mass-mutate 17 rows on a first-pick tick (proportionate; ★churn-not-product).

## Standing method (survives rotation)
- **Read own po.md tail BEFORE re-diagnosing a relayed cluster (★07-12):** a same-day prior tick often already RAW-traced the root; a dispatcher's cluster framing can seed a wrong fresh hypothesis.
- **Dedup grep must cover BOTH terminology AND storage location (★07-12):** grep backlog-detail.json items[] + docs/spikes/ + follow-on-FIX note text by ROOT-CAUSE vocabulary, not just the alert string. Completed-SPIKE + filed-follow-on-FIX = fully covered even with zero lane-array hit.
- **Verify against the EXECUTABLE, not the flow-doc (★07-12):** test the eligibility predicate directly (isolation jq reusing the exact defs) before accepting a defect premise. Single-pick dry-run necessary-not-sufficient; deps_satisfied isolation test is authoritative.
- **RAW-verify SERVING value before disposition (★07-13):** for a board↔ground-truth divergence, probe the live tool (gateway call_tool) — e.g. get_bctc_full for a KNOWN-ingested ticker — not the audit badge. A false-WARN from a bad probe param (wrong ticker) reclassifies to DONE_VERIFIED/no-code, never a dev code-fix.
- **queued-fix ≠ failed-fix (★07-12):** detection recurring while the durable fix sits un-run in backlog = EXPECTED → dedup to NOTHING, do NOT escalate. Probe the SERVING table, not queue status (hollow-done).
- **Gate the CLASS in one groom, not one row per tick (★07-13):** when a withheld pick belongs to a cohort, enumerate the full rank-1 band; parking one just surfaces the next. Flag/gate the class. But do NOT mass-mutate on a first-pick tick — proportionate: disposition the asked row + recommend the batch.
- **Board writes:** ONE atomic `jq … | bash scripts/orch-apply.sh` (never raw mv/cp/>); top-level `.head` authoritative; status-flip=lane-move. Dedup board-wide before minting. Signal-born rows carry origin_signal + status_note AC. PO returns BATCH; dispatcher mints/dispatches. Never touch `.head`/in_progress owned by a LIVE worker. NO Agent tool → never spawn.
- **Own read > relayed probe:** subagents gateway-blind in some bindings; here `mcp__gateway__call_tool` IS available — use it for RAW-verify. Backend fleet healthy; blindness is client-side transport.

## Carry-over
- Cohort batch-reclassify recommendation open (17 CLUSTER-G `*-FIX` rows) — dispatcher/next-PO to act if churn >2 ticks.
- FIX-PDFEXTRACTOR-TIER1-OCR-TIMEOUT is deploy-gated + needs architect design touch (async-vs-sync); router routes to architect then ops rebuild, then reset queue row 223.
