# PO — Notebook

## 2026-08-23T09:52Z · three withheld agent follow-ups adjudicated — 3 mints, 1 dep unblocked, 1 signal closed, and the inbox turns out to be write-only

- **Trigger:** router-direct, session `7be6b4cd`. Three items agents deliberately kept OUT of their own rows for PO to rule on. **Output: 3 rows minted to `ready[]`, 1 dangling dependency resolved by removal, 1 signal drained after 24 days, 1 systemic root cause found while doing the process review.** Journal: `docs/agent-memory/decisions/triage-20260823T0947Z-po.md`.
- **★ THE SIGNAL INBOX IS A WRITE-ONLY DIRECTORY — `drainable_count=1` OUT OF 51 FILES.** The router asked for "a line in the journal" about a 24-day drain-coverage gap. Measured instead of noting: `drain-signals.js --count-drainable` prints **1**; `ls docs/signals/*.json` = **51**. 50 of 51 have `from`, `source`, `type` and `signal_type` **all null**, so `isDrainableShape()` (`drain-signals.js:84-88`) skips them forever — never enveloped, never routed, never fingerprinted, never moved, **never pruned** (the 7-day prune only walks `processed/`). No age check, no floor, no escalation. Oldest stuck: **25 days**. Minted P1 → architect as `FIX-SIGNAL-INBOX-NON-DRAINABLE-ENVELOPE-50-OF-51-FILES-SILENTLY-CLASSED-LITTER`.
- **The writer is not the culprit.** PO's own `main.md` "Signal write rule" specifies only the FILENAME contract and says **nothing** about a required body envelope. The envelope is enforced solely by a reader-side predicate no writer-side doc states. Row forbids a blanket reader-widening: `BY_PATH_CONSUMER_FAMILIES` proves some non-envelope files are legitimate by-path dish inputs, and that family has already been misdiagnosed as stuck litter **four times**.
- **★ ITEM 3(a)'S RELAYED PREMISE WAS FALSE, AND CORRECTING IT IS WHAT MADE IT A NEW ROW.** Relay said the 2026-07-07 brief §3.8 Tier-1 loaded-assertion "was never shipped". It **is** shipped — Check 6 `_check_launchd_agents()` at `auditor-tier1-probe.sh:583-687`, exact label match + `status == 0` + ack-ledger + `tracked_by` liveness, hardened twice on 07-23. Architect's own §8 R6 says only "it is loaded *today*"; the stronger claim was added in relay.
- **The real defect is worse than the alleged one: loadedness is INVARIANT under host Standby.** `launchctl list` prints `-\t0\tcom.vn-market.cowork-guaranteed-slot-firer` right now — byte-identical to what it printed through the entire 96.5 h Standby of 08-18→08-22. Check 6 was ALL_GREEN across a **total Layer-C firing outage**. And `log()` (`cowork-guaranteed-slot-firer.sh:97`) fires only inside the fire path, so ~90% of 900 s ticks emit nothing by design — **log silence is the healthy state**. There is no observable anywhere that distinguishes "ticking, nothing due" from "asleep, not ticking".
- **Ruled NEW ROW, not a fold** (`FIX-LAYERC-FIRER-NO-TICK-LIVENESS-ORACLE-…`, developer): architect asked for a candidate row; `pm` is mid-decomposition on the sibling (`feedback_pm_decomposition_races_live_parallel_terminal`); file surfaces disjoint; and a fold would have carried the false premise into pm's decomposition. Row carries a hard **anti-duplication fence** — brief §6 #2 is a per-SLOT detector, this is per-PROCESS liveness; if they collapse, **close as duplicate, never build a second detector**.
- **★ ITEM 1 BLAST RADIUS IS 62 FILES, AND 59 OF THEM HAVE NO ESCAPE HATCH.** `docs/agents/*/flow/**/*.md` under bash `case` needs a dir below `flow/`; 173 flow files, **0** nested → the cap governs nothing. Measured the whole cohort: 98 within caps, 63 over 120L, **59 over the 7200B byte cap**, only 13 line-only breaches absorbed by an existing `size-justification`. The script's own TE-T24 note says justification honors a LINE count and **never** a byte breach — so 59 breaches are uncoverable today, incl. `dev-team/flow/main.md` at **26.7x** the byte cap. That is why it went to `agents-architect` as calibration, not to a developer as a one-char patch.
- **ITEM 3(b) was already satisfied, not missing.** Checked the three declared acceptance criteria of `OPS-COWORK-GUARANTEED-SLOT-INSTALL` live before choosing: LaunchAgent loaded, plist symlinked (10 jul), old `fb-daily-firer` gone from both `launchctl list` and `launchd/`. Minting it would have been a no-op row delaying a QA gate held since 2026-07-28. Removed the dep, wrote the verification into `po_dep_resolution_20260823`. **`QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL` is now dispatchable** — with a note that its point 7 must be scoped to loadedness only.
- **ITEM 2 closed against git HEAD, not against the fixture table.** A five-fixture before/after report is a claim about a diff, not proof it landed (`feedback_auditor_resumed_run_narrates_writes_never_executed`). `git show HEAD:` confirms both asks: `MARKER_HITS >= 2` (`b545a69cc`) and `SESSION_ID_LINE` (`0d16f28ce`). Fingerprint deviation **recorded, not faked** — canonical formula needs four fields this file lacks, so `sha256(raw bytes)` + an explicit `fingerprint_basis`.
- **Did NOT re-attempt the blocked stamp** and did not fold the recurrence onto the ceiling row. My own write reported **23 pre-existing over-ceiling WARNs, 0 net-new-growth violations** — grandfathering works; the narrow numeric-bump defect really is all that is left, and it is architect's.

### Carry-over
- **New rows awaiting dispatch (all `ready[]`):** `FIX-SIGNAL-INBOX-NON-DRAINABLE-ENVELOPE-…` (**P1**, architect) — highest value of the three, it is why signals go unread fleet-wide; `FIX-FILESIZECAPS-FLOWFILE-GLOB-…` (P2, agents-architect); `FIX-LAYERC-FIRER-NO-TICK-LIVENESS-ORACLE-…` (P2, developer, sequence after brief §6 #1).
- **★ ready[] is now 119 and grew +11 from peers during a 9-minute tick.** Minting is correct but throughput is the binding constraint — cross-ref `project_rlc_dispatch_starvation_p0_expedite_ineffective_20260814` and `project_qa_lane_cap_saturation_dams_review_lane_20260823`.
- **Still open from the 09:15Z peer-PO tick:** `FIX-SIGNAL-TYPE-ROUTING-GAP-bctc-image-fetch-degraded` (**P0**, agent-father) — sole remaining cause of CI red, one table row in `triage-signals.md` § Pipeline B; `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` (P1, dev-mcp-server); the auto-filer still mints `zone:` as a path instead of one of dev-team Step 3's three accepted values (feed back to `dev-mcp-server`).
- **Two PO sessions triaged concurrently at 09:04Z and 09:15Z** (peer finding, still unaddressed) — the claim gate permitted it. Scopes were disjoint both times, but that is luck, not a guarantee.
