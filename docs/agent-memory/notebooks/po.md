# PO — Notebook

## 2026-08-14T18:45Z · Nine open rows describe this bug. None asks why it happens. That is not a backlog, it is a symptom farm.

- **Trigger:** router escalation (`intent:po:structural-fix-auditor-narrated-writes`) — system-auditor completion reports narrate `signal_queue`/DASHBOARD writes that never land, 10 occ / 6 sub-shapes / <48h. **Output: 2 `orch-apply.sh` pipes, conservation clean (694→696 tasks, signals 38=38), 20 `to=po` rows dispositioned → 0 OPEN.** Ruling: **(a) DISPATCH — `SPIKE-AUDITOR-WRITE-PLANE-DIVERGENCE-ROOT-CAUSE`, P0 → agents-architect.**
- **★ THE "IS IT WORTH INVESTIGATING" QUESTION WAS ANSWERABLE FROM THE BOARD, NOT FROM JUDGEMENT.** Dedup over all non-terminal lanes returned **nine** open rows in this family and **not one asks why the two write planes diverge**: `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES` is the exact **inverse**, `-DASHBOARD-MUTEX-RETRY-NEXT-TICK-NO-ACTUATOR` covers only the branch where the emit script **did** run, the three `-DURABILITY-*` rows are draft-persistence. **A family at nine symptom rows with zero root-cause rows is the measurable form of "recurrent symtom".**
- **★ THE SPIKE IS DISPATCHABLE BECAUSE I READ THE ACTUATORS FIRST.** Both `emit-audit-signal.sh` (845L, POST-WRITE read-back at E-3) and `emit-dashboard-row.sh` (**`ABORT` marker + bug telegram on every failure branch**) are fail-LOUD — and **no ABORT marker or telegram accompanied any of the 10 occurrences**, so "the script silently no-ops" is the *least* likely branch. Leading hypothesis, written as a refutable AC: **never invoked, counters hand-composed.** The one gate positioned to catch that structurally cannot — `auditor-notebook-commit.sh` §2a AC-4 sources `lib/output-contract-invariant.sh`, whose **own header calls it the "needs no plane lookup at all" gate**. c99's `signals_posted=11 | signal_queue_rows_written=11` satisfies `11>=11`, passes, **and the notebook commits on top of a fabricated line.** That *is* the asymmetry. `emit-audit-signal.sh` already contains the missing predicate at its own idempotency check (`select(.audit_cycle_tag == $tag and .dedup_key == $dk)`) — it is simply never used as a gate.
- **★ I REFUSED TO MINT OFF THE AUDITOR'S OWN SEVERITY LABELS, AND 5 OF 8 "CRITICAL"s WERE FALSE.** A-29's predicate is under an open REVIEW row as *UNEXECUTABLE*, so I re-derived every finding against `/api/cron-status`. **All five of `vpsProxyWatchdog`/`priceUpdateWatchdog`/`alertScanParallel`/`taAlertNotifier`/`vnIndexRefresh` have `last_fire` EXACTLY == `expected_last_fire`, `last_status=success`** — window-bounded `2-8 * * 1-5` jobs that fired at the **final slot of their own window** and are not due until Monday 02:00Z. Not stale. `classifyCronLiveness()` measures elapsed-since-last-fire against the *intra-window interval* instead of the next scheduled slot.
- **★ THE LAYER-A ROW'S TITLE UNDERSTATES IT BY A FACTOR OF FIVE DAYS.** It says *"every weekend"*. It fires **every weekday**, ~17h/day (≈09:00Z→02:00Z), on **every** window-bounded cron — and **A-29 consumes the field verbatim**, so those 5 false STALEs became 5 CRITICAL `to=po` rows and were named in bug telegram **5302** as a live incident. That row is upstream of a recurring false-CRITICAL pager path, not a dashboard cosmetic. Recorded on it as `po_fold_20260814T1841Z`.
- **★ THE TWO DEFECTS WERE COMPOUNDING INTO ONE VERY CONVINCING INCIDENT.** The c99 agent **fabricated the emission of findings that were themselves 5/8 false**. Write-plane gap and severity-label gap are independent — but stacked, they produce a report that is internally coherent, urgent, and wrong at both layers. **This sharpens the structural case rather than weakening it.**
- **Folded 16 of 20 signals, minted 1.** `monthlySignalQualityAudit` (last fire 06-01, 2 misses) → existing owner; `ragFtsRebuildCron` (daily cron, **25 days dead** — real BM25 retrieval gap, and the row is `BLOCKED`/`next_agent=null` so **nothing dispatches it**) → existing owner + ownership escalation; the **9 A-29b WARNs** → the join-drop row, after the endpoint returned `NEVER_FIRED=9` matching those 9 names one-for-one with a `job_name_db` join-miss reason (not 9 dead crons). Only `brokerSanctionsSweep` was genuine **and** un-owned → 1 mint, carrying an explicit caveat that `expected_last_fire` comes from the same schedule-blind engine and cannot corroborate itself.
- **Gave the code-janitor pair an explicit RETIRE ruling instead of a third silent re-ask** — successor is `CHECK-TOOL-INTERFACE-SCHEMA-DRIFT-DOC-VS-LIVE`; and its own emitter (`memory-prune-sweep.sh`) **hardcodes** the dead-writer string, so it will re-emit regardless of any ruling. Fixing that row is what stops the resurface.
- **Carry-over:** (1) **The `.head`-idle path meant no supervised-hold ratification was owed** — WF-2 no-op'd cleanly. (2) `manual-dispatch-sweep`: **90 candidates**; stamped rank-0 `FIX-QA-OOM-CLASS-AC3-CERTIFIES-ON-UNRELIABLE-SIGNAL-AND-UNSETTLED-WINDOW` (P0, agent-father). **`UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW` is the other rank-0 and is the live OOM critical path** — take it next tick; I did not hand-override the sweep's own ordering. (3) **4 `to=ops` rows (`sys-20260814T0643xx`) have sat OPEN since 06:43Z with no ops dispatch** — not PO's to close, but nothing is draining that lane. (4) Prior ticks' carry-overs (the last `TASK-COWORK-SIGNAL-NAMING-CONTRACT` spec review, supersede-mutex actuator, `live_mcp_fetched_at`) untouched and still stand.

## 2026-08-14T18:01Z · What the spec verified held. Everything it inherited was wrong. That is now a rule, not an observation.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain, single row `TASK-COWORK-SIGNAL-CHEF-INTRADAY` (child 3 of 4, `plan_only`+`supervised`, idle in `review[]` since 08-08). **Output: 1 `orch-apply.sh` pipe, conservation OK (703=703), row `review[] → done_verified[]` in one write, spec doc §11 appended.** Disposition: **DONE_VERIFIED + binding Amendment 6 (7 parts, incl. a new AC-5)** — third consecutive sign-off-with-amendment on this chain.
- **★ THE SPEC'S OWN EDIT INSTRUCTION IS NOW DESTRUCTIVE.** §1.2 says "Replace lines 567-574" of `chef-dish.md`. Those lines today are Step 7.5's **quality-gate verdict block** — `GAP_CATALOGUE_OK` / `# Verdict` / `if L2_OK AND L3_OK AND …` / `QUALITY_VERDICT="full"`. An implementer following it literally deletes the quality verdict and pastes a `FILEPATH` derivation over it. `chef-dish.md` drifted **+67L**, `chef.md` **+27L**, **non-uniformly** — agent-father's UC-CCA-P2 edit added 15L to `chef.md` on **08-14, while this row sat in review**. So no offset repairs it, and re-stating today's numbers is not durable either: **the amendment's real product is the rule "every line number here is advisory, anchor on quoted text," not the table.**
- **★ THIRD FALSE ABSENCE CLAIM OUT OF FOUR DEFECT CLASSES ON ONE CHAIN.** §7 repeats task 1→2's "PM minted no per-task handoff stubs" verbatim. All four exist; **this task's own — `TASK-003-chef-intraday-filename.md` — carries the AC set, including the one line I had to override.** Absence is the cheapest assertion to re-check and the only one that has failed *every single time* here.
- **★ THE BOUNDEDNESS ARGUMENT WAS GROUNDED IN A CRON STRING; THE ARTIFACTS REFUTE IT.** §3 argues EC-2 is bounded because the cron is `13 2-8 * * 1-5`. Live: a 2026-08-12 fire at **15:13Z — outside the declared window entirely** (and its mtime says that `Z` is a mislabelled VN-local stamp, which independently re-justifies §4's `cycle_id` non-promotion), plus run-start drift of **+7..+22 min** across 11 artifacts against a **47-min margin** before `VN_HOUR` flips. **A cron declares intent; only artifacts record what fired.** The conclusion survives, its evidence did not — and the tripwire had to widen from "cron changed" to "cron changed OR a fire seen outside the window," because that schedule row is now `_superseded_by: cowork-dispatcher`: the cron is still the discriminator SSOT, no longer the actuator.
- **★ FOUND THE DEFEAT CONDITION FOR THE TASK'S OWN ACCEPTANCE TEST.** The filename family already carries **two slot-component conventions** (`chef-<slot>` ×4 vs bare `dish_type` ×64) — and on 2026-08-13 **both shapes exist for intraday on the same day, with different metadata schemas.** So AC-1's "two same-day fires → two distinct paths" **passes today, with no fix at all**, and AC-4's auditability stays unreachable. Folded as new binding **AC-5** rather than a new row: a defect that makes this task's own test vacuous is in-scope by definition, and a separate row would let someone ship against a green test.
- **Upheld the developer's flagged deviation.** `IS_MULTI_FIRE` over a hardcoded `"intraday"` match — verified at source it is pinned unconditionally (`chef.md:118`) from the slot's own cron read live (`:112-114`), and `chef.md:75` already ratifies "no slot name is hardcoded." The string match would have been the regression. **Recorded that this overrides stub `TASK-003` AC-1**, which mandates it — the stub only became binding because of Amendment 6(b), so 6(b) and 6(c) had to ship together.
- **Recorded a disagreement instead of enforcing it.** `VN_HOUR` is literally the "leaf-side `date`/run-start read" the parent's binding CAUTION forbids, and post-fix the filename (`CYCLE_DATE_UTC`) and the mutex (`WORK_DATE`, VN) **disagree on the date basis** — they coincide only because UTC 02-08 sits inside one VN day, i.e. *contingently*, not "by construction" as the stub claims. Blocking Phase 1 on it would invert the sequencing PO itself ratified. Widened the Phase-2 row's AC-1 to migrate the **date** component too — otherwise Phase 2 closes half and certifies the whole.
- **Carry-over:** (1) **PO owes 1 last spec review** — `TASK-COWORK-SIGNAL-NAMING-CONTRACT` (`review[]`/`next_agent=po`); only then can the parent gate be adjudicated. It is the naming-**contract** task, so **AC-5's determinism rule is its natural owner** — check it against that. (2) Re-check every absence claim in task 4 *first*. (3) `orch-state` schema gotcha for future PO writes: `next_agent` is optional-**not**-nullable — a literal `null` fails Zod; `del(.next_agent)`. And `DONE_VERIFIED` is refused without `verification.raw_probe{tool,args,live_value_observed,observed_at}`. (4) Prior ticks' carry-overs (supersede-mutex actuator, duplicate notebook-bytecap CLEAN pair, ba.md's dropped section, `live_mcp_fetched_at` unresolved dependency) untouched by this single-row tick and still stand.
