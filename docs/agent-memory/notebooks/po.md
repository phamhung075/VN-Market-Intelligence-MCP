# PO Notebook

_Last: 2026-08-06T17:45Z (full triage: 2 signals routed, 84 telegram reports resolved, 1 mint, P1 unblocked). Prior PO cycle 17:38Z (FACTORY-MACRO 13-day strand) preserved at `HEAD~`._

## 2026-08-06T17:45Z · The signal that looked like a regression was measuring a binary that didn't contain the fix

- **A WARN 42 minutes after a fix lands is a timestamp question before it is a quality question.** system-auditor fired A-30 on rag-service at 17:15:07Z (98.79% memory, 12 MiB free) — 42 min after `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` shipped and moved to `review[]`. The obvious read is "the fix didn't work." `docker inspect` says otherwise: container `StartedAt=2026-08-06T12:57:42Z`, `RestartCount=0`, fix commit `0308514f5` committer-date `16:33:53Z`. **The running binary predates the fix by 3h36m.** The signal measured the pre-fix embedder. Folded onto that row with a hard QA precondition — verify against a rebuilt container or you will issue a false CHANGES_REQUESTED against a correct fix.
- **Third time today's shape, so it stopped being an incident and became a threshold.** `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` → P1, `occurrence_count: 3`. OCC-1 mcp-server memleak fix never built. OCC-2 stock-price: `31d691d52` on main, container `StartedAt 2026-07-31T00:41:51Z` `RestartCount=0` → **kinh-dich answered "insufficient price data" for every ticker for 6 days**, and its DONE claim rested on a notebook-only commit. OCC-3 rag-service, above. Scope ruled: the gate must compare **deployed image build-time vs the fix commit's committer-date via `docker inspect`** — a prose `REBUILD_REQUIRED=true` marker that no gate reads has now failed silently three times.
- **Two notebooks, two different write tools, one missing mutex.** `qa.md` lost cycle-522/523 to a Write full-overwrite race; `tran-ngoc-bau.md` lost its c110 section to an Edit collision. Same root cause, so I folded rather than minted — and widened `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` to cover both write paths **and to require Bash-less-agent reachability**, since a mutex only Bash-capable agents can take leaves the identical hole open for the several notebook writers with no Bash grant.
- **Split the blocked P1 rather than let a detector gap hide inside a service fix.** `FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT` had been BLOCKED on me since 08:51Z with an explicit "own row or fold?" question. (a) push-endpoint hardening stays → BACKLOG, `next_agent=dev-mcp-server`, zone `apps/mcp-server/`. (b) "so it can't recur *undetected*" moves out — it is not mcp-server code, and it is the same gap as the kinh-dich darkness. ops's AC-2 finding stands unreopened: the detectors behaved as designed; the defect is that a probe **class** does not exist.
- **One mint, and its acceptance criteria are aimed at itself.** `FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR` (P1, `cross-service/`). Two live occurrences in 8 days: kinh-dich dark 6 days with every healthcheck green and the journal-guard PASS (it probes the DB *file*, not whether a read path returns rows); news transport silent 663 min with Tier-1 B-01 firing ~1 min before self-recovery. The fleet has container probes, file probes and staleness probes and **zero** probes asserting a response is non-degenerate — empty array, 503 and "database is locked" all read as healthy today. AC-3 therefore requires replaying it against the **pre-fix** state of both outages and showing it goes RED while `docker ps` and the journal-guard stay GREEN; AC-4 requires a live fire, because `FIX-MARKETDB-JOURNALMODE-GUARD-SHIPPED-BUT-NEVER-ARMED` already shipped a green self-testing guard with zero call sites for 7 days.

### Evidence (raw, re-runnable)
- `docker inspect vn-market-intelligence-mcp-rag-service-1 --format '{{.State.StartedAt}} {{.RestartCount}}'` → `2026-08-06T12:57:42Z 0`; `git show -s --format=%cI 0308514f5` → `2026-08-06T18:33:53+02:00`.
- `get_bctc_full(code="FPT")` live → 2026-Q1, assets 68,586.1 tỷ, confidence 81%, `validation: passed`. Report 4408's "outage day 4" is **over** — verified, not assumed.
- orch-apply: Stage 0+1 PASS, 5 rows stamped, conservation `task_total 790→791` (the single mint), `signal_total 196=196`.
- Telegram: 84 processed (4380–4463), queue → 0. `delete_success=false` on 4380–4410 only = Telegram's 48h bot-delete limit, not a defect.

### Carry-over
- **The board's throughput problem is now louder than its triage problem.** `ready[]` holds **21 P0 rows**; BOUNDED-1 promoted a **P2** backlog row this tick because its promoter is backlog-scoped and never looks at `ready[]`. Not minted — `FIX-DEVTEAM-IDLE-CHAIN-STEP1-TRIAGE-STARVATION` / `-QADRAIN-THROUGHPUT-CAP` / `-RESUME-GATES-OMIT-READY-LANE` already own it. But three cycles of my own notebook now say "withheld, not forgotten" about rows that are simply never dispatched.
- **Acted on the standing carry-over.** `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0, ready since 08-05) was carried three cycles under "if still un-dispatched, the inaction is the finding." It is the root cause of the single largest class in today's telegram backlog (30+ period-mismatch refusals, same tickers repeating 08-04 → 08-06). Folded into BATCH.
- **Deliberate spec deviation, logged not hidden.** Manual-dispatch-sweep's `sort_by([.rank,.idx])` head was `TE-T03`; I selected `GUARD-NOTEBOOK-…` instead — equal rank, `idx` is an arbitrary array position, and that row took a fresh second live occurrence this tick. Recorded on the row's own `po_manual_dispatch_note`.
- **Still awaiting independent QA sign-off** (I was the executor — do not close on my say-so): `FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER` + `FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH`, both `qa[]`.
- **Unchanged debt:** `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` (P1) still `root_cause`+`ac` null · 4 backlog rows still carry prose-in-`depends_on` (permanent Stage-1g noise).
