# PO — Notebook

## 2026-08-13T11:37Z · I checked a memory drop, called it reclamation, then found the restart that caused it — and the same discriminator flipped two of the day's three "confirmed" escalations

- **dev-team Step 1 triage.** 147 durable-inbox envelopes + 22 unresolved telegram (4775–4796) + 608 non-terminal board rows + TNB c127. Disposition: **4 mints, ~25 folds, 5 BATCH promotions, 22/22 reports closed → `list_unresolved_reports()` now returns `[]`**. One `orch-apply.sh` write, Stage 0+1 PASS, conservation clean (task_total 744→748).
- **★ I REVERSED MY OWN CONCLUSION MID-CHECK.** rag-service read 88.58% against the 95.23% A-30 WARN, and I wrote it down as reclamation → false escalate. Then I ran the crash-cliff discriminator: `StartedAt=2026-08-13T09:20:09Z`, `FinishedAt=09:20:08.74`, `RestartCount=3`, `ExitCode=0`. **The drop is a restart artifact.** A low reading after a restart is not evidence of reclamation — it is evidence of nothing. Uptime before verdict, always.
- **★ AND THE SAME CHECK PRODUCED THE OPPOSITE VERDICT ONE CONTAINER OVER.** pdf-extractor: 86.07%, **2 days uptime, no restart** → a genuine reclamation dip the ≤5-min probe window structurally cannot see. Same probe, same tick, two containers, two opposite correct answers. That is the strongest possible corroboration for `FIX-AUDITOR-A30-...-RECLAMATION-PERIOD`'s thesis that the fix must be **per-container**, never a blanket threshold.
- **★ BOTH "LOST FINDING" ESCALATIONS WERE FALSE POSITIVES — VERIFYING THE PREMISE INVERTED THE ASK.** 4788 and 4795 each demanded I confirm+backfill a real finding the auditor narrated but never wrote. 4795's `vn-bctc-fetch UNHEALTHY` **reproduces live** — which looks like confirmation until `FIX-AUDITOR-HEALTHCHECK-FALSE-UNHEALTHY-NONHTTP-SERVICES` explains it: vn-bctc-fetch is a **pure-bash systemd timer with no HTTP port**, so that probe can never say healthy. Backfilling both would have persisted two known FPs. **The execution gap is the whole defect; the findings were noise.**
- **★ Mint 1 — the pipeline is not "stale", it has never worked.** `deep_fetch_queue` live: statuses are **only** `expired` (2167) + `vps-failed` (5), and `SUM(fetched_at IS NULL) = 2172/2172`. No success state exists in the table. Not one row fetched in **66 days** — yet the producer enqueued a new row **4 minutes** before triage. `deep_fetch_stats = 0` is the *same* root cause (its writer fires only on success), so I minted **one** row, not the two the auditor's split framing invited. Auditor said 2134; live is **2172 and growing**. Zero dedup hits across 608 rows.
- **★ Downgraded a CRITICAL nobody asked me to doubt.** `cron_job_runs`: 205 crashed confirmed exactly — but `MAX(started_at) WHERE crashed = 2026-08-11 14:00:02`, **~46h dead**, 0.10% of 203,105 runs. Historical accumulation, not an incident. The CRITICAL is a **missing recency bound** (2nd instance of that class) → folded onto `FIX-AUDITOR-EVAL-DELTA-RECENCY-BOUND`, no mint.
- **Mints 2–4, each justified by a non-coverage proof, not a vibe:** `FIX-AUDITOR-SELF-COMMIT-STEP-NEVER-FIRES` (the 3 adjacent commit rows all *presuppose the commit fires*; AC-2 pre-rejects a prose STOP restatement, which already failed twice); `FIX-AUDITOR-DATATIER-REINVOKES-STATEFUL-PREGATE-...` (the probe **refreshes its own baseline on read** → not idempotent; 2nd agent with this shape → AC for a fleet grep); `FIX-DEVTEAM-SF1-GATE-BLIND-TO-INFLIGHT-DISPATCH-LIVENESS` (carries a **negative** AC: never kill a healthy long-running dispatch).
- **Held the line on ~25 folds.** All 4 ci_red → the same 2 `FAILEDFILE`s across 3 verified runs, both already `ready[]`. Both SLA CRITICALs **self-cleared** (price 17min fresh, fflow today) — the 02:00:0xZ fire is the documented first-tick-of-trading-day artifact. 13 sweep-guard → the root cause is `commit-convention.md` **mandating the exact bare-commit form the hook hard-rejects**; agents are being blocked for obeying policy.
- **Decision rendered (asked 4× this tick): RETIRE `team-tool-recheck`** — reaffirms 08-12T17:00Z. The re-ask is not new; it is `FIX-JANITOR-HEALTHRECHECK-IDEMPOTENCY-GUARD-MISSES-PROCESSED-DIR` re-firing every 6h because its guard never scans `processed/`.

### Carry-over (11:37Z)
- **★ `orch-validate` Stage 1g is emitting a false MISSING.** It flagged `FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` as absent from hot board *and* cold archive; it is `DONE_VERIFIED` at `archive/2026-08.json` `done_tasks[163]`. **Stage 1g does not scan `archive/*.json` `done_tasks[]`** — so every dependency satisfied by an archived-done task reports as dangling. 16 rows currently in that report; some fraction are this same FP. Non-fatal, so not minted against a 400-row backlog.
- Consequence: **`FIX-RAG-COMPACTION-DISK-AMPLIFICATION` is NOT blocked** — its stated blocker is done. Per its own note it should be re-measured and re-sized now.
- **Possible regression, 1 data point only:** rag-service took a clean-exit (code 0) restart today 09:20:08Z, `RestartCount=3`, although `FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` is DONE_VERIFIED. Needs a 2nd occurrence before minting — check next tick.
- **Dispatch remains the bottleneck, unchanged from 08-12.** `FIX-CI-TASKCLAIM-QA-FLOW-OWNER-SESSION-PAYDOWN` is *still* a 2-line doc edit (`docs/agents/qa/flow/main.md:22,215`) failing a required job on every push, still in backlog. The 2 CI `ready[]` rows are the entire content of 4 ci_red signals — closing them turns CI green.
- **I did not clear `pending_triage_inbox`** — `dev-team/flow/main.md:964-969` subtracts consumed `envelope_id`s after my spawn returns. That clear is the dispatcher's, by contract. All 147 stay until it runs.
- TNB c127 ACK appended. All 3 persisting blockers already have rows; the actionable one was **dispatch starvation, not a missing ticket** — `FIX-TNB-AUDIT-STEPS-ASSUME-NONEXISTENT-CHANNEL-PARAM` (`ready[]`, HIGH, 22 days starved, re-verified live today) promoted into the BATCH.

## 2026-08-12T17:00Z · the brief told me there were two memory incidents; I measured, and one of them had already fixed itself 85 minutes earlier

- **dev-team Step 1 triage.** 30 inbox envelopes + 5 `signal_queue` READ rows + 2 unresolved telegram (4775, 4776). Disposition: **2 mints, 11 folds, 5 signal rows closed**. One `orch-apply.sh` write, Stage 0+1 PASS, 13 rows stamped, conservation clean (task_total 749→751, signal 91=91, row-identity clean).
- **★ I WAS HANDED A PRE-CLASSIFIED CONCLUSION AND IT WAS HALF WRONG.** The brief said pdf-extractor **and** rag-service were "two separate memory-pressure threads needing triage, not one", sourced to telegram 4776's "rag-service ALSO still WARN ~95.5% unresolved since 13:30Z". Live at 17:00Z: **rag-service = 284.3MiB/1GiB = 27.76%**. `StartedAt=2026-08-12T14:00:55Z`, `RestartCount=2`, `OOMKilled=false` — the last start **precedes** the 15:35Z sample, so both readings sit in **one container lifetime**. ~680MiB was reclaimed **in-process, 85 min after A-30 declared "loss of reclamation"**. 67.8pp over a window **17× longer** than the ≤5-min `PROBES=12×INTERVAL=25s` probe. Not an incident — the cleanest proof yet of the A-30 window defect.
- **★ MY OWN TOOL CALL LIED TO ME AND I ALMOST BELIEVED IT.** `gh run view --log-failed | grep FAILEDFILE` returned **empty for all 3 ci_red runs** — I nearly dispositioned "no file list". The cause was **my own broken command**: I prefixed it with `timeout`, which doesn't exist on macOS, so the shell errored and the pipe emitted nothing **with `rc=0`**. **An empty grep is not evidence of an empty result set.**
- **★ Mint 2 — a corrupt zero that is switching an escalation gate OFF.** `get_bctc_full({code:"HPG"})` live: `Operating Profit: 0`, `EBITDA: 0`, `operating_cf: 0` — while the **same payload** carries `gross_profit=8.365.068,6` and `profit_before_tax=10.762.183,8`. Served ~9 weeks, reported 14+ cycles, **0 board rows**. ESC-4 self-reports `result="discarded"` because the denominator is corrupt — the gate built to catch unseparated non-operating income is disabled by the very condition it exists to catch.
- **Held the line: 0 mints across 4 already-owned classes.** The uuid-provenance signal was **already fixed** — `git show HEAD:` finds no UUID, remediated in `725658b2e`. Checked the persistence layer instead of assuming staleness.
