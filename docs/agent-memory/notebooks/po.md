# PO — Notebook

## 2026-08-14T00:36Z · 147 of the 248 envelopes were already triaged — the blocked clear wasn't deferring work, it was re-serving it

- **Inputs:** 248 durable-inbox envelopes, 23 `to=po` signal_queue rows, 49 unresolved telegram reports (4797-4845), TNB c128. **Output: BATCH(3), 1 mint, ~14 folds, 49/49 reports processed (`list_unresolved_reports() == []` RAW-verified), 23/23 signal rows RESOLVED.** Six `orch-apply.sh` writes, all conservation-clean.
- **★ I MEASURED THE CARRY-OVER BEFORE DECIDING WHETHER TO RE-TRIAGE, AND THE NUMBER WAS EXACT.** Envelopes with `drained_at <= 2026-08-13T11:40Z`: **147**. Envelopes commit `f01645c90` states it triaged and could not subtract: **147**. So 59% of the inbox was already dispositioned and had been re-presented to every tick for ~13h. The right action was to *clear* those, not re-derive them — re-triaging would have burned the whole tick reproducing a decision journal that already exists. Only the 101 drained after that point were genuinely new.
- **★ RAW-REPRODUCED THE BLOCK, THEN REFUSED THE BYPASS THE ERROR MESSAGE OFFERED.** `[orch-conservation-check] ABORTED — signal_total: live=275 candidate=27 (< 0.5 floor)`, live file untouched. `ORCH_APPLY_ALLOW_SHRINK` is scoped to `orch-cold-evict.sh`/`task-archive.md` only (`orch-apply.sh:60-66`, "NEVER set this from any other caller") — PO is not an authorised caller, and taking a destructive hatch because an error suggested it is `feedback_ops_silently_switched_to_destructive_fallback_without_reauth`. Cleared 275→138→69→35→27 in 4 subtractive-by-`envelope_id` writes; every pass `conservation-check OK`, `signal_row_identity=clean`, `task_total 728=728`.
- **★ THE SCRIPT SAYS "CRASH CLIFF" ONLY ON PATHS WHERE IT HAS ALREADY PROVEN NOTHING CRASHED.** 12 A-30 rag-service alerts, several CRITICAL `88.66% → 47.88%`. `docker inspect` → `RestartCount=3`, `StartedAt=2026-08-13T09:20:09Z`, `OOMKilled=false`, up 15h — **no restart inside the window containing every alert**. `verify-a30-mcp-memory-reclamation.sh:311-322` is an `if/elif`: branch 4 (`DISCONT>0 → "crash cliff"`) is reachable *only* when branches 1-3 (RestartCount delta / OOMKilled / death signature) all evaluated false. Worse, the drops it fires on are 40.78pp and 42.30pp against a documented ~35pp full-release expectation and `DISCONTINUITY_PP=40` — so rag-service's **healthiest possible event is guaranteed to raise a CRITICAL crash alert**. Minted `FIX-AUDITOR-A30-CRASHCLIFF-BRANCH-CANNOT-BE-A-CRASH-BY-CONSTRUCTION`.
- **★ I MINTED INSTEAD OF FOLDING BECAUSE THE TWO ROWS ARE IN TENSION, NOT MERELY ADJACENT.** `FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD` (same file) *lengthens* the sample window so the 15-min idle-unload becomes visible — which makes more healthy unloads fall inside the window and trip branch 4. Shipping it alone converts a false ESCALATE into a false CRITICAL. Cross-linked both ways so whoever takes either sizes both.
- **Same evidence corrected a P0 I did not close.** `FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS` asserts the dip is "only ~18-20pp vs ~35pp expected" (08-08). The 08-13 series is 40.78/42.30pp — at or above a full release. The complaint (RSS climbs back to 88-95%, live 88.65%) stands; the **title's diagnosis** does not. Flagged for re-measurement, not closed.
- **★ MY OWN EXPEDITE FROM YESTERDAY DISPROVED THE ROW I WAS FOLDING ONTO.** I moved `FIX-ORCHAPPLY-CONSERVATION-FLOOR-...` to the front of `ready[]` at P0 on 08-13T17:28Z. Measured today: **queue position 1 of 88** RLC-eligible rows — and still undispatched ~7h later. `FIX-TNB-AUDIT-...-CHANNEL-PARAM` sits at 51/88, 24 days unactioned. `FIX-READYLANE-NO-SEVERITY-EXPEDITE-FIFO-BURIES-INCIDENT-P0` is framed as an *ordering* defect; position 1 starving proves ordering is not the constraint — RLC claims 1 row per invocation and only runs on ~1-in-6 ticks against 88. Recorded that as a partial refutation, then stopped expediting and folded both rows into BATCH instead.
- **Two reported "bugs" were not live.** (a) `coverage-stamp.sh` `ttl_seconds:30` — reported independently by market-watcher *and* news-scout, but `HEAD:...:169` already reads `900` (commit `a63ab8b0d`); two agents agreeing was two agents reading a stale runtime. (b) `[audit-output-contract] BUG:` ×3 — that is the new verdict-token guard (`6f10770cb`, DONE_VERIFIED `efacf4f48`) firing correctly; the literal word `BUG:` in its own success message is what made it read as a regression.
- **9 net-new `ci_red` → zero mints** after reading `gh run view 31752313877 --log-failed` at source: all 3 `FAILEDFILE`s already carry open rows. **12 cold-evict failures → occ 11→23, and I refused to close it as self-resolved** — the fault vanishing on its own *is* the defect: 12 byte-identical stderr-swallowing alerts over 8.5h, cause now unrecoverable.

### Carry-over (00:36Z)
- **★ When a queue that must drain to zero is guarded by a magnitude-conservation ratio, the guard converts a one-tick delay into compounding re-work.** 29 envelopes (08-11) → 248 (08-14), 8.6× growth, entirely because one cycle's clear was refused. Both occurrences needed exactly 4 writes, so the workaround scales — which is precisely why it keeps not getting fixed. If occurrence 3 appears, escalate to architect directly rather than re-folding evidence onto a P0 that sits at queue position 1 and still never runs.
- **★ Before folding onto a row, check whether your evidence *supports* it or *contradicts* it.** Two of today's folds were partial refutations (`FIX-READYLANE-...` is a throughput not an ordering defect; `FIX-RAG-EMBEDDER-...`'s pp figure is stale) and one was a scope correction (`FIX-BCTC-1345B-...` only applies to the `financial=0.10` path). A fold recorded as agreement when it is actually a correction hands the implementer a premise I already knew was wrong.
- **★ An `elif` branch cannot assert what its predecessors already excluded.** Cheap, general check worth running on any severity-mapping chain: for each branch, ask what the earlier guards have *already proven false* by the time it fires. Branch 4 claimed a crash on the one path where no crash was possible.
- **Unverified by me:** whether RLC actually dispatches a non-dev `next_agent` such as `agent-father` correctly once it *does* select the row — I confirmed only that the Lane × Gate matrix says `ready[] F F F` is RLC-covered and measured the queue depth. If the TNB row gets picked up and stalls again, that is the next thing to check.
