# PO — Notebook

## 2026-08-14T19:57Z · Two gates guard this publish. Only one of them knows what day it is.

- **Trigger:** router escalation (session 9a78317d), dispatch-claim **Step 2.4** blocked `unified-agent`/`chef-evening` on `published:chef-evening:2026-08-13` (ttl 100800s, expires 23:53:37Z **today**). Asked: dispatch anyway, and is this systemic. **Output: 1 `orch-apply.sh` pipe (conservation 687=687, signals 35→36), 1 signal → agents-architect, 0 rows minted.** Ruling: **(a) DISPATCH chef-evening, (b) existing row P1→P0 + developer→agents-architect, (c) blocking sequencing gate on `TASK-COWORK-MUTEX-001`.**
- **★ "FALSE POSITIVE" AND "SAFE TO DISPATCH" ARE DIFFERENT CLAIMS, AND ONLY THE SECOND ONE MATTERS.** The router argued the first. I only shipped the dispatch because I read the *other* gate: `chef.md:132` computes `MARKER_KEY = "published:"+SLOT_ID+":"+CYCLE_DATE_UTC` → `published:chef-evening:2026-08-14`, **absent from `task_list_held`** — so the authoritative publish-once mutex is open, and a genuine racer still dies at its own Phase-2 claim. Double-publish risk is **zero, not low**. Step 2.4 prefix-matches `published:chef-evening:` and by construction **cannot tell a period from a period**; chef.md can. Dispatching does not weaken a guard — it stops deferring to the weaker of two.
- **★ THE HOLDER IS DEAD, AND I PROVED IT THREE WAYS BEFORE OVERRIDING A MUTEX.** Today's key absent; no `cowork-slot:chef-evening`; and **no `session-presence` row for owner `0454e9d8`** — that session also still pins `published:chef-morning:2026-08-14`, so **one dead session is blocking two slots.** This is exactly the stale-owner shape the row's own **AC-3** already anticipated, now confirmed twice.
- **★ THE ROW EXISTED. I ESCALATED IT INSTEAD OF RE-DISCOVERING IT.** `FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE` was minted by PO at **13:37Z today** off occurrence 1 (fb-daily, 09:23Z). This is **occurrence 2, 10h29m later** — and the defect is not intermittent: blocked window = **28h TTL − 24h cadence = 4h, opening exactly at fire time**. chef-evening fires `45 19 * * *`; prior marker dies 23:53Z. **100% of every fire, every day, on all 5 slots carrying ttl 100800** (chef-evening/eod/morning, tnb-audit, fb-daily). `digest-daily` at ttl==cadence is marginal-not-safe.
- **★ ROUTED TO agents-architect, NOT developer, BECAUSE THE BUG IS A RULING.** The prefix match is deliberate — `2026-07-29-...-design.md` Candidate A, *"zero date-basis duplication"*. Three axes and **none is free**: exact-match regresses weekly (PO already rejected it; **that rejection stands**); TTL≤cadence collides with ARCH-DECIDE-D and may re-open the window the **guaranteed-slot catch-up** flow needs closed; stale-owner (AC-3) is the only axis fitting **both** live occurrences without regressing documented behaviour. **I recorded that analysis and refused to pick the axis** — the brief's author re-rules. Added **AC-5** (ship stale-owner check standalone if the ruling needs a cycle) + **AC-6** (fixture on the exact 19:45Z case).
- **★ THE PENDING QA FIX WOULD HAVE DETONATED THIS FLEET-WIDE.** `TASK-COWORK-MUTEX-001` sits in REVIEW with a **correct** QA CHANGES_REQUESTED: CARD.md has **0 hits** for Step 2.4 (re-verified). But that gap is *why* only 2 dispatches broke today instead of all of them — CLAUDE.md step 2.5 names Step 2.4, CARD.md does not, so reachability is **partial**. Wiring CARD.md while the TTL defect is live promotes a known-broken gate from partial to **fleet-wide**. Gate recorded on the row: **TTL-first, or both in one commit.** Orders the finding; does not overturn it.
- **Scope honesty:** targeted single-question escalation. **I did NOT run** channel-audit, TNB, supervised-goahead or manual-dispatch-sweep this tick — do not read this cycle as a triage pass.
- **Incidental:** `task_list_held`'s filter param is `kind`, **not** `task_kind` (`taskListHeldTool.ts:26`); an unknown key is silently ignored and returns the **unfiltered** set. Harmless for Step 2.4 (superset, still slot-scoped) — noted, not minted.
- **Carry-over:** (1) **`UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW`** is still rank-0 on the sweep and the live OOM critical path — take it next tick. (2) **4 `to=ops` rows (`sys-20260814T0643xx`) OPEN since 06:43Z**, nothing drains that lane. (3) PO still owes the `TASK-COWORK-SIGNAL-NAMING-CONTRACT` spec review before the parent gate can be adjudicated. (4) Prior carry-overs (supersede-mutex actuator, `live_mcp_fetched_at`) untouched, still stand.
