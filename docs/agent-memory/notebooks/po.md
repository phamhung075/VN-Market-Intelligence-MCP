# PO — Notebook

## 2026-08-07T05:15Z · dev-team 04:37Z tick, 2nd pass — 9 signals + 3 reports, 1 mint

### What actually happened
- **12 inputs, 11 folds, 1 mint.** Every signal and report already had an owner sitting in `ready[]` or `review[]`. Two `orch-apply.sh` pipes, both PASS, conservation OK (`task_total` 765→766, `signal_total` 216 unchanged).
- Minted `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` (P1, ops). Both live `signal_queue` rows dispositioned; telegram 4483/4484/4485 resolved and deleted.
- Pre-flight: `should_hold=false` (already ratified 04:41Z). TNB c123 already ACKed 08-06T22:05Z. Manual-dispatch sweep: 43 candidates, top `TE-T03` stamped + folded.

### Decisions worth keeping
- **"The fix is failing" and "the fix is not running" look identical from the detector.** A-30 fired ~20× on rag-service since 08-06. The row's own notes had already inferred the container predated the fix; I got the image-level proof: image `sha256:dd6381f9c637` Created=**2026-08-05T16:42:17Z**, fix commit `0308514f5` at **2026-08-06T16:33:53Z** — the image is ~24h *older* than the code it should contain, RestartCount=0. Not a sizing defect: `2f835ec63` granted +256MiB and it was gone in 15.6h.
- **A precondition nobody owns is a closed loop.** The FIX row's `qa_precondition` demanded a rebuild before any QA verdict, and no row owned that rebuild — so QA couldn't pass, the row couldn't leave `review[]`, and the detector kept re-firing. Folding alone would have left that intact. 4th instance of code-lands-but-container-never-rebuilt (mcp-server, alert-engine, news-fetch, now rag-service); `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` is the generic row and still has no consumer.
- **Re-added a `depends_on` I deliberately cleared yesterday — and that is not drift.** Yesterday's clearance killed an *unresolvable prose blob* whose condition could never be met by waiting. This edge names a real id, gates one discrete action, and is dispatched in the same tick. Rationale recorded on the row so the next tick doesn't "fix" it back.
- **`blocks` is decorative — the validator proved it.** Stage 1e rejected my reverse-only edge: `effective_depends_on` never traverses `blocks`. Correct encoding is the forward `depends_on` on the row being gated.
- **Read the file, not the signal's framing.** The 4 notebook signals say `section_count=1`. They don't say the one section is a *rolling* heading — `## State summary` / `## Known patterns / preferences`, no date token, so both take the sentinel MAX key: undroppable while supplying 100% of the bytes. Both agents are APPEND-class per AC-6 with **zero** dated sections. That is the absorbing state `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE` predicted verbatim, so a manual split alone regresses. Scope correction written onto the CLEAN row.
- **A decision re-asked is not a decision pending.** The janitor signal asks for a replace-vs-retire ruling; `CHORE-TEAM-TOOL-RECHECK-LOCAL-CRON` already carries REPLACE. It re-asks every 6h because of a separate guard bug — live-confirmed: **4/4** emitted files sit in `docs/signals/processed/`, **0** in `docs/signals/`, and the guard only scans the latter. ACK-WITH-CORRECTION, no re-decision.
- **4484/4485 are one emission, not two incidents** — byte-identical, 11s apart. The duplication itself is the new datum (emitter has no dedup on that path) and is now on the owning row.

### Carry-over
- **`OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` is `next_agent=ops` ⇒ DRS-stranded off-allowlist.** Dispatched via this tick's BATCH. **If it is still open at the next tick, escalate the ops row — do NOT clear the `depends_on` edge to unblock QA.** A QA verdict against the current container is a guaranteed false negative.
- **Still owed by me** (unchanged): mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships. Filename keying stops revisions clobbering each other; it does not make fields parseable.
- **Still unadjudicated, now directly relevant:** `FIX-AUDITOR-TIER1-A30-MEM-SINGLE-CONTAINER-SCOPE` (review/P0) and `FIX-AUDITOR-A30-PROBE-SH-MISSES-RAG-SERVICE-CONTAINER` (backlog/P1) name one defect. `probe.sh:123` still hardcodes `grep 'mcp-server'`. The A-30 storm this tick came from a *manual* rag sweep — the Tier-1 probe still cannot see that container.
- **Escalated for the 2nd consecutive tick, still not mine to fix:** `review[]`=211, of which **172 (81.5%) are `next_agent=qa`** against a one-row-per-tick QA-Drain — a ~172-tick horizon with non-zero arrivals. Measurement folded onto `FIX-DEVTEAM-QADRAIN-THROUGHPUT-CAP`. This tick's entire signal volume was symptom of that, not new coverage gap.
