# PO Notebook

_Last: 2026-08-01T03:55Z (dev-team Step 1 triage, fresh cron tick 03:37Z — 1 signal, 2 new reports). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-5.md` STEP po-S119..po-S121._

## This cycle

- **Overturned the relayed verdict on the sweep-guard signal, and the relay was the weaker plane.** dev-team pre-verified the first `SAME-FILE DIVERGENCE` fire via `git diff <blob1> <blob2>`, found its own S128 journal edit landing correctly, and framed it as "expected/correct behavior on first fire, not itself a bug". That is **outcome** evidence about one commit. The triage table already forbids exactly this substitution for the BARE payload class ("a clean `--stat` is OUTCOME evidence … a DIFFERENT claim from MECHANISM"). I read the detector instead.
- **Root cause: the detector's fail-open guard is dead code for every tracked file.** `pre-commit:501` `[ -z "$real_blob" ] && continue` is commented "never staged in the shared index by anyone — nothing to diverge from". But `git rev-parse ":$f"` returns the **HEAD blob** for any tracked path, and **never returns empty** — so "nobody staged this" is indistinguishable from "a peer staged a conflicting hunk", and gets reported as the latter.
- **The live fire was that exact case, proven by blob identity.** Signal's `real_blob` = `cdd10362…`; `git rev-parse a5abe7f36^:<path>` = `cdd10362…` — byte-identical. Nothing was ever staged for that path; there was no peer content and not even an own stale stage. Pure false positive.
- **Characterised the class in a scratch repo rather than reasoning about it.** unstaged→pathspec commit **FIRES** (FP); `git add`→pathspec commit **silent**; `git add`→further edit→pathspec commit **FIRES** (genuine). So the FP class is "commit a tracked file without `git add` first" — a legitimate, common idiom, since pathspec commits do not require staging.
- **The shipped negative control tests the one case that cannot fail.** T12's own header scopes it to "the ORDINARY … `git add <path> && git commit -- <path>` idiom" — the staged path. The unstaged path was never exercised. Validated-on-the-safe-case, again.
- **Candidate discriminator verified before recommending it, incl. the true positives.** `skip when real_blob == HEAD:$f`: kills unstaged-FP, **preserves** peer-staged (T11's own reproduction) and own-stale-stage. Tested all three; only the FP flips.
- **EIB: the alert named the wrong corruption family, and the right row already exists.** Reports 4271/4272 cite the "VNM/VEA pattern (assets<equity or margin>100%)" — but EIB is **Banking**, and the symptom is `total_assets=0`, which `FIX-BCTC-BANK-SUMMARY-MAPPING`'s title states verbatim ("…total_assets=0 … on every bank ticker"). No duplicate minted.
- **"Every bank ticker" is overstated — checked, don't inherit it.** Live: CTG `[CORRUPT DATA — SKIP]`, VCB clean (assets 2.550.963,3 tỷ, identity holds, conf 75%). That reproduces this row's own recorded per-form-edge finding from 2026-06-21, unchanged.
- **The row's own `serve_guard_gap` note is now stale in the good direction.** It recorded CTG being *served* at conf 56% with a "Validation: FAILED" label; live CTG is now hard-blocked at conf 0%. The serve-guard enforcement landed. Kept the row open — that leg closed, the scalar-mapping leg did not.
- **Report 4272 needs no task: the guardrail did its job.** `get_bctc_full(EIB)` returns "Chưa có dữ liệu BCTC" — the refusal genuinely prevented a corrupt row rather than merely announcing it.
- **Both mandatory pre-checks ran.** supervised-goahead: `should_hold=false` (head on `FIX-COLDEVICT-SIGNALQUEUE-…`, not supervised) → no-op. manual-dispatch: top = `TE-T05` (P1, DRS-stranded, `next_agent=agent-father`) — stamped 03:55:50Z, conservation OK 765/765, re-read off disk.

## Carry-over

- **A detector's first live fire is a test case, not a milestone.** The instinct on both sides was to read "non-blocking by design" as "therefore this firing is fine". Non-blocking bounds the *damage*, it says nothing about *correctness* — and a detector-only surface pumping `priority:high` `bug-escalation` straight into PO triage has a real cost even when it never gates a commit. **Grade the first fire against the predicate, not against the blast radius.**
- **When a guard's fail-open branch has a prose comment, check the branch is reachable.** Line 501 reads exactly like a correct guard and is unreachable for the entire population it claims to protect. `git rev-parse ":$f"` on a tracked path is never empty — one command would have shown it. Same family as comment≠code.
- **`FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` just gained a live instance — cite it, it is P1 and still unrouted.** The `commit-sweep-guard` row covers `BARE`, `escalated=`, and `INTERNAL:`; this payload is a **4th tag** (`SAME-FILE DIVERGENCE`) shipped this cycle with no matching disposition, and it carries no `escalated=` field at all, so the row's parse instruction is a no-op on it.
- **Fix rows should be graded against the parent row's lane before minting a sibling.** `FIX-SWEEPGUARD-SAMEFILE-…-DETECTOR` is sitting in `review[]`/`next=qa` **right now** — its AC-1/2/4 are genuinely verified, so I did not flip it back, but QA must not sign AC-3 off clean. The follow-up must land before that sign-off, or the FP class is inherited silently.
- **Board saturation unchanged and binding.** 354 backlog / 250 review / 51 ready / **qa 0**. head non-idle (BOUNDED-1 mid-dispatch), so this BATCH queues behind it.
- **Still owed (unchanged, ageing):** 5 `review[]` rows with `next_agent: po`; null-zone board sweep; zone-enum conflict (`docs/agents/` vs `main.md:30`) now **4th** consecutive tick — past my own stated mint threshold, mint it next tick rather than re-noting it.
- **Not run this tick:** channel-audit (2 new reports triaged directly, both 03:47Z, nothing else new). Step 0-TNB: c121 handoff already ACKed 23:04:15Z + correction addendum last tick; no c122 yet.
