# PO Notebook

_Last: 2026-08-05T17:30Z (dev-team Step 1 triage — CI RED on HEAD f080fbc08). Prior 2026-08-05T16:30Z (16:07Z drain)._

## This cycle

- **Main has been RED for 30 consecutive CI runs / 4+ days, and nobody's model of the system contained that fact.** Last green = run `30699139566`, 2026-08-01T12:09, `70cc91af4`. The tick arrived framed as "3 findings"; the finding is that **three independent gates broke on three different days from three unrelated commits and every one of them survived** — size-lint at `7ac55adc8` (08-01, code), bun test at `9af50bb26` (08-04, CLAUDE.md), task-claim lint at `3ce726a6e` (08-05, docs/agents/po/flow/).
- **The mechanism, read off `.git/hooks/pre-push` (115L, read in full): it runs tsc + rebuild-raw-verify and nothing else, both behind a `CODE_TOUCHING_REGEX` that EXPLICITLY excludes `docs/`.** Two of the three breakages arrived on exactly the paths that filter excludes. Minted `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER` (P1, architect) with an AC that **measures before widening** — UC-GCP-P4 recorded tsc at ~94s against a 90s commit-mutex TTL, which is *why* the filter exists. Widening without a dry-run re-opens the bug the filter fixed.
- **All three relayed root causes were wrong, and each was wrong in a way that would have produced a wrong fix.** Re-verified every one at source rather than minting on the relay:
  - *size-lint* — relayed as "this session's BCTC work pushed them over". `git log` on both files: last touch is `7ac55adc8`, **2026-08-01**, four days before this session. `git show --stat` = +116L / +153L across **exactly** those two files. One commit, one root cause → **one row, not two**.
  - *bun test* — relayed as "cronJobCount may actually agree with 4". It does not: **cronJobCount=88**. Read `findCronCountLiterals` (`tool-registry-parity.test.ts:223-235`) before ruling: exemption is `content[start-1]==="(" && content[end]===")"` — the *whole match* paren-wrapped. `CLAUDE.md:47` `(4 crons)` passes; `:48` `the 4 standalone crons` does not. **The detector is correct; the doc broke the convention.**
  - *task-claim* — relayed as "the fix never covered this file, or the file reintroduced it". Neither. `git blame -L 42,53` = `dc430566c2`, **2026-05-27**, untouched; and the baseline *does* grandfather it, as `sprint-kickoff.md:42`. `3ce726a6e` added 2 lines above, shifting it to **:44** and de-grandfathering it from a `(file,line,snippet)`-keyed baseline.
- **That third one is not lint cosmetics — it is a live defect in my own flow.** `coordinationTools.ts` declares `owner_client_session` as `z.string()` with **no `.optional()`** on `task_claim` (:104-110) *and* `task_release` (:199-205). A doc-faithful PO never engages the sprint umbrella mutex and can never release `task:<sprint_id>` — it strands for the full 3600s TTL. Carried since 2026-05-27, grandfathered rather than fixed. Row scope therefore covers all 3 PO-owned sites (kickoff `task_claim` + signoff `task_release` ×2), so no future line-shift can re-trigger it.
- **Declined to widen two guards, on purpose.** Not the size-lint baseline (bump without justification), not the cron regex exemption (un-parenthesised prose would punch a hole real global drift walks through), not the task-claim baseline key. All three ratchets are *working*; the only failure is that a 1-line debt went unpaid for 4 days. Gate-widening needs an actuator dry-run first.
- **The 7 prior `FIX-CI-SIZELINT-*` rows are NOT a non-converging pattern — I checked before assuming.** The lint now reports only 2 files, so all 7 prior targets are green. The family shape works. Those rows are parked at `next_agent=qa`; that is a **lane-drain backlog, not a failed fix**, and it must not be used to argue against minting in the same shape.
- **Disposed `FIX-TASKCLAIM-OWNER-CLIENT-SESSION-MISSING-FLEET-FLOW-DOCS`** (parked at `next_agent=po` since 07-30). AC5/AC6 verified satisfied at source (commit-mutex SKILL.md:36/:69, refine_bctc_md:24-36 incl. the spawn-prompt session channel, pm/flow/main.md). **AC4 is now positively proven rather than merely shipped: the guard it delivered caught a real regression today.** → qa. Fenced so the `:44` hit cannot reopen it.

Pre-checks: signal_queue 0 NEW `to=po` (all 15 recent po-addressed rows already READ/TRIAGED) · `read_telegram_reports(status="new")` = 20, all automated `bctcExtractReconcile`/`sla-monitor`, all folding into existing rows · `list_unresolved_reports()` 128, again overflowed the relay — read grouped myself, no untracked root · stale `ci_red` for `a6b695338` ignored per brief.

### Carry-over
- **Do not let the three symptom rows going green close `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER`.** Stated in the row. Green CI is the symptom clearing; the gate divergence is the disease.
- **Recurrence count on the pre-push/CI divergence is 3-in-5-days** — clears the standing recurring-bug escalation bar. If a 4th gate breaks on a docs-only push before that row lands, escalate rather than mint symptom row #4.
- The `(N crons)` convention has no written home — it exists only as a regex in a test file. That is why `9af50bb26` broke it innocently. If a 3rd skill bullet is ever added, expect the same break unless AC2 lands.
- Prior carry-over still live: rag-service deploy predicate (no memory-% drop as success), never `docker exec` rag-service, SSC quarter-blindness re-check, `RAG-FTS-BUILD-MEMORY-BOUND` time-gated ~2026-08-12 (stop re-deriving), `TE-T02` ~4.6d re-flagged-never-dispatched.
