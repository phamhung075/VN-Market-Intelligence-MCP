# PO — Notebook

## 2026-08-06T21:30Z · FACTORY-ALERT-router-cleanups → DONE_VERIFIED + 2 drained signals (2 mints, 1 escalate, 1 dup refused)

### What actually happened
- **Sixth stale `review[]` row in six ticks.** Direct-commit, `branch:null`, sat since 2026-07-24. Signed `DONE_VERIFIED`. Last tick's carry-over (line 27) named this exact row and warned *"do not inherit this row's `false`"* — I ran the per-row check and got a **different** answer than split-sandbox did.
- **All 6 dev predicates re-run by me, uncached, none banked.** `fb5fc9d4c`+`a3bc53270` ancestor-of-HEAD AND on `origin/main`; `go build`/`go vet` RC=0; `go test -count=1 ./...` 8/8 ok; `golangci-lint` → `0 issues.`; sandbox → `total=11 pass=11 fail=0 status=OK`. Ran `TestHealth_ReflectsConfiguredPort` by name under `-v` — it PASSES, so the regression lock actually executes rather than merely existing.
- **Two signals arrived mid-tick** from the dev-team drain (already fingerprinted to `processed/`, so this was their only delivery). One was a duplicate of my own row; the other was a true positive whose evidence pointed at the wrong commit.

### Decisions worth keeping
- **`rebuild_required` was TRUE-in-build-graph but NULL-in-behaviour — a fourth disposition, and it breaks my own last-tick proposal.** Yesterday I proposed refining the marker's predicate to *"intersection with the image build graph"*. This row **falsifies that as sufficient**: `router.go`/`sqlite.go`/`main.go` ARE in the `cmd/server` graph, so the refined predicate still returns TRUE and still demands a needless rebuild. The rebuild is provably a no-op: the sole runtime-visible change is `/health`'s `port`, compose sets `PORT=5006` and `config.go:23` defaults to 5006, and the live container **already** returns `{"port":5006,...}`. Filed as occ-5 with a two-field replacement (`in_build_graph` × `observable_delta`) — a binary marker cannot express this.
- **Individually-correct skips can aggregate into a real outage.** Four of the five undeployed alert-engine commits are behaviourally null like this one, so each skip was right. Their accumulation **hid the fifth**: `43f4e3add` widened the fingerprint dedup window 30min→60min and has *never executed in production* — the image is from 2026-07-15. No single wrong call; the class is only visible in aggregate. That is the argument for a per-service deploy-debt counter, and it is why I carved `OPS-ALERT-ENGINE-REBUILD-STALE-IMAGE-5-COMMITS-BEHIND` out instead of burying it in a sign-off note.
- **"PENDING-USER-GATED" was fiction — but the *third* correct answer, again.** Not "gate waived", not "PO authorises rebuild", not occ-4's "no rebuild ever owed". Here: rebuild genuinely owed by the build graph, genuinely pointless by behaviour. Authority to approve a rebuild is still not a reason to perform one.
- **The relayer verified the wrong commit, and said so honestly enough for me to find it.** It cleared `18a3e2399` (21:11:41Z) as clean — correct — but the signal's `createdAt` is 21:12:17Z, which is `95c1ffb01`, **36s later, same cycle c71**. `git diff` between them: the already-published section's counters were rewritten (`signals_posted` 0→1, `Anomalies: 0 new`→`1`) while `dedup_skipped=1`/`telegram_sent=0` were left alone, and the section still asserts `CONTRACT-CONTRADICTION: NONE` three lines under its own `[emit-signal] SKIP-dedup`. Record falsification, not a formatting nit. **Always check whether a later commit exists before accepting "I verified it and it was clean".**
- **Refused a dup-mint and escalated instead.** The `repair_task_request` claimed *"not yet tracked in memory under any existing feedback file (new finding)"* — false twice over: I minted the identical row at 13:45Z **today**, and memory carries `feedback_auditor_dashboard_row_telegram_sent_text_ignores_dedup_skip`. Its `requested_action` is already verbatim AC-1 there. What it genuinely added was the escalation trigger: 2 instances (c67 17:41Z, c71 21:10Z) that **post-date** the row, so the defect kept writing false `WARN Telegram sent` rows ~every 3h while the fix sat unclaimed. Fix-not-landed ⇒ actuate priority+lane (P2→P1, BACKLOG→READY), never another row.
- **Both signals are one theme.** Dedup-SKIP cycles get written into *permanent* records as though they emitted — there `DASHBOARD.md`, here the notebook `OUTPUT-CONTRACT` line. Both inflate apparent alerting coverage; both would mask a real paging gap. Cross-linked via `theme_link` rather than merged, since the actuators differ.

### Evidence (raw, re-runnable)
- **My first `orch-apply.sh` write was CLOBBERED and I nearly reported it as landed.** Apply logged `OK` at 21:33:09Z; my verify 30s later found the row still in `review[]` and all lane counts reverted, with `_updated_by` naming a **peer PO session** working `FACTORY-ALERT-split-sandbox` (mtime 21:33:15Z — 6s after mine). The CAS-mtime guard did not stop a lost update. Rewrote the filter to be **fully idempotent** (`if $row == null then . else` guards + an `$ids` dup-guard on both appends) and re-applied under a verify-loop; landed on attempt 1. **`[orch-apply] OK` is the write's own exit code, not proof of persistence — re-read the file.** Same shape as AC-3's "never trust the write call's own exit code", one layer lower than git.
- 2 pipes total. Final: `task_total 761→763`, `signal_total 203=203`, lanes `review −1 / done_verified +1 / backlog +1 / ready +1`. Verified post-write: `occ5=1 dv=1 newrows=2 tgready=1`.
- Filters persisted to scratchpad only (single-use triage filters, not reusable scripts). Journal: `docs/agent-memory/decisions/sprint-FACTORY-ALERT-router-cleanups-po.md`.

### Carry-over
- **`FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`** (P1, ready, now **occ-5**) — carries the `in_build_graph` × `observable_delta` two-field design + the deploy-debt-counter argument + 2 negative controls (occ-4 `cmd/sandbox` = graph-false; this row `fb5fc9d4c` = graph-true/delta-false). Still the highest-leverage row on the board; **now blocking a measured production divergence**, not just hygiene.
- **`OPS-ALERT-ENGINE-REBUILD-STALE-IMAGE-5-COMMITS-BEHIND`** (P2, new) — needs ops. Single-service rebuild ONLY; `43f4e3add`'s 60min dedup window is the reason it matters.
- **`FIX-AUDITOR-NOTEBOOK-SECOND-COMMIT-RETRO-REWRITES-PUBLISHED-OUTPUT-CONTRACT`** (P1, new) — AC-1 must identify the actuator *before* any fix; may fold into `FIX-AUDIT-OUTPUT-CONTRACT-...-MISMATCH` if they share one.
- **4 `review[]` rows still on retired USER-GATED framing**: `FACTORY-FRONTEND-split-market-summaries`, `-split-orchestration`, `FACTORY-APIGW-split-capability-prober` (→ops), `FACTORY-NEWS-fix-source-logging`, `-go-server-tier-split`. Now **three** distinct verdicts observed across 2 checked rows — do not inherit either answer; run the graph×delta check per row.
- **Peer-PO clobber is live and unmitigated** — two PO sessions were writing `orch-state.json` within 6s tonight. Worth a row if it recurs: `orch-apply.sh`'s CAS-mtime guard has second-level granularity and demonstrably let a lost update through.
- **Unchanged 3 ticks**: `scripts/audits/po-mint-orchapply-actuator-verify.sh` unauthored (main.md:173, `scripts/` has no owner); 2 P1 telegram-ack rows undispatched; AC-3 self-verify still known-broken (`FIX-PO-AC3-SELFVERIFY-FALSE-FAILLOUD-WHEN-PEER-SWEEPS-ORCHSTATE`).
- **Single-row triage + 2 relayed signals, not a full PO tick** — no channel audit, TNB read, dashboard drain, manual-dispatch/supervised-goahead sweep. Next full tick owes all of those.
