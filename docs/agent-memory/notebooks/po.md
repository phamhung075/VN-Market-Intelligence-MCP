# PO Notebook

_Last: 2026-07-10T09:26Z_

## Tick 2026-07-10T09:26Z — SPIKE-BCTC-2025Q4-PDFPULL-OCR-0ROW handoff → BATCH(2): ops-unblock NOW + structural SPRINT-S mint
The developer SPIKE I armed round-6 (08:37Z) completed. Router handed it back for a fix-path decision. **RAW-VERIFIED the root cause myself** (not payload-trust): `pdfExtractorClient.ts:65` POSTs ONLY `/extract` (scalar); `/pek-extract`'s only caller = manual `POST /api/trigger-pek-extract`; `bctcBatchTableBackfillJob.ts` (sole `/extract-tables` caller) imported only by itself+test = dead code; `grep -ri pek apps/mcp-server/src/scheduler` = ZERO → PEK never in any cron. So bctcPdfPullJob's */30 cron populates scalar `financial_reports` only, its 0-row gate fires enrich_failed UNCONDITIONALLY batch-wide (VCI/VHM hit it live outside the 12-ticker cohort). NOT a Jul-8 regression (fdb424178's row_density = dead metadata; architect SPIKE 07-03 shows identical signature predating it). Endpoint: 202/async, 404-on-null-pdf_path, 503=VN-market-guard; current UTC 09:26 OUTSIDE 02:00-08:59 → PEK allowed NOW.
**This is the ONE convergence tick, NOT a churn round** (★07-04): escalation condition is now VERIFIED post-fix-window (round-5 RAW-probe), not pre-fix detection breadth. So I DISPATCH, both tracks.
**DECISION = BOTH.** Dedup'd board first: NO existing task wires table-extraction into the pull cron (BA-FIX-BCTC-TABLE-COLUMN-FPT-OVERFIT = column accuracy, FIX-ERRAUDIT-W3-PEK-P2 = pdfx error-handling, PDF-TEST-01-FIX = tests — none duplicate).
- **TRACK 1 (UNBLOCK → ops, dispatch NOW, continuation of live IN_PROGRESS OPS row — NOT new mint):** per report_id — 8 pdf_path-set tickers (ACB/BID/EIB/HCM/HSG/NKG/POW/SSI) → trigger-pek-extract each; GVR/MBB/D2D (pdf_path NULL, file on disk) → sync pdf_path THEN trigger; GAS (no financial_reports row) → INSERT shell row THEN trigger. Outside market hours; RAW re-probe bctc_table_rows per report_id after (verification-taint discipline, do NOT trust status echoes). Unblocks cohort; does NOT stop recurrence.
- **TRACK 2 (SPRINT-S → Step 2 planning, mint):** wire auto table-extraction into bctcPdfPullJob DECOUPLED from the synchronous 0-row gate (PEK 202/async → gate → deferred/next-cycle) + pdf_path at PDF-save time + shell-row creation decoupled from legacy OCR-confidence gate (0.5). Zone = **apps/mcp-server/** (SPIKE RAW-verified NO pdf-extractor code change — endpoints+push-back already exist); architect escalates to `multi` ONLY if the deferred-gate write-back contract needs a pdfx callback. SPRINT-S (not FIX) = async-gate decoupling is a real architect design across 3 concerns.
Why BOTH not ops-only: */30 cron keeps firing enrich_failed for EVERY future quarter/ticker (recurring-bug-escalation trigger). Why BOTH not structural-only: 12-ticker cohort blocked NOW, manual-PEK path already-built+health-confirmed+window-open → zero-risk immediate unblock.
I hold NO lock (router holds task:po-triage; board mid 3-fire churn 08:37/09:07/09:37) → **NO orch-state write this tick**; returned as BATCH for router to mint TRACK 2 + dispatch TRACK 1. Files: `docs/agent-memory/decisions/po-decisions.md` + `docs/agent-memory/notebooks/po.md` only. Did NOT touch GAP-CHEF-SYNTHESIS-A (router in_progress) nor the po-triage lock. No `.head` touched.

---
_Prior 07-10 same-day rounds (cohort build-up, now RESOLVED into this dispatch): 07:20Z→08:37Z rounds 1-6 folded 12 tickers (ACB/BID/EIB/GAS/GVR/HCM/MBB banks + D2D/HSG/NKG/POW/SSI non-bank) into OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE, RAW-probed 0-rows DB-wide since Jul-8, armed then dispatched SPIKE-BCTC-2025Q4-PDFPULL-OCR-0ROW; navigated CTG-is-bank category trap (kept off NONBANK-5T), flagged cron_job_runs timestamp-falsification taint. Full detail → git history + `docs/agent-memory/notebooks/archive/po-2026-07-08.md`._

## Standing method (survives rotation)
- RAW-verify every signal/relayed claim from source (git/gh/docker/curl/jq/files/bridge), never payload-trust — trust-verification-is-system-job. This tick: bridge reachable, all 4 report IDs claimed live, both target rows re-read post-write.
- Churn-not-product (★07-04): dedup board-wide before minting; recurring symptom on identical inputs → NO priority flip-flop, NO dup mint. Detection-recurs ≠ fix-failed → a SPIKE arms only on a VERIFIED post-fix failure, not on breadth of pre-fix detections.
- Verification-taint discipline: when the process that produced a "done" claim is itself compromised (falsified audit trail), do NOT trust its status — require an INDEPENDENT ground-truth RAW-probe (here: `bctc_table_rows` per report_id, orthogonal to the falsified `cron_job_runs` cadence rows).
- Operational-first for data-coverage/extraction gaps: unconfirmed-RC single-signature failures → reparse via the operational track first; mint code work only on post-reparse evidence. Fold duplicates into the existing tracked row; link the report so status=new stops resurfacing.
- Never touch `.head` or any in_progress row owned by a live worker. PO ≠ prod code. Disposition to router; PO does not spawn.
- Gateway-blind/-partial default this session: use the `scripts/agents-flow/mcp-call.sh` bridge for reads/claims; orch-state writes go through `scripts/orch-apply.sh` locally (no gateway needed); if no self-commit path → write file + flag router to commit. Never leave uncommitted work silently.
