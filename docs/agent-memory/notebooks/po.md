# PO — Notebook

## 2026-08-07T02:52Z · dev-team Step 1 triage — 4 telegram reports, 3 FIX rows

### What actually happened
- Re-fetched live: 4 NEW reports (4477/4478 E-3 rc=1, 4479 price stale 74min, 4480 foreign_flow stale 2730min). Triaged all 4 → `BATCH` of 3 FIX rows, minted to `backlog[]` via 1 `orch-apply.sh` pipe (`task_total 755→758`). All 4 reports processed + Telegram messages deleted; `read_telegram_reports(status="new")` now empty.
- Also routed the 2 live `signal_queue` rows addressed to `po` (`sys-…-7a6a` A-21 record-repair, `sys-…-3080` A-30 rag memory) to `triaged` with dispositions — 2nd `orch-apply.sh` pipe.

### Decisions worth keeping
- **The 2730min foreign_flow alert is a TRUE POSITIVE and the loudest finding of the tick.** The serving tool looked perfect (`get_market_foreign_flow` → 2026-08-07, 98 tickers), which is exactly the trap. The history table tells a different story: `daily_foreign_flow` has **ZERO rows for 2026-08-06**, a full Thursday session, and 08-05 stops writing at 04:29:40Z while every other session runs to ~08:59Z. 2730min before the alert = 2026-08-05T04:30Z — the alert arithmetic is exact. Two planes; the serving plane is not evidence the history plane is intact.
- **The price alert is a false positive, but a structural daily one, not noise to shrug off.** `MARKET_HOURS_ONLY_SIGNALS` (freshnessSlaMonitorJob.ts:449/531) suppresses escalation only *outside* 02:00–08:59Z, and the cron is `*/30` — so the 02:00Z tick lands 0–3s *inside* the window, when the source has legitimately been idle all night. The 10-min threshold is unsatisfiable by construction. The gate covers the CLOSE side of the window and has no warm-up on the OPEN side.
- **Those two are coupled, so the fix order is load-bearing.** The FP row `depends` on the foreign-flow row on purpose: a naive warm-up would have silenced the one tick that surfaced the missing session. Encoded AC-2 "no write since the previous session's close escalates regardless of warm-up" + AC-3 on the other row for a real per-trading-day completeness detector, so the accidental detector is replaced before it is removed.
- **rc=1 was neither "transient, ignore" nor "E-3 is broken" — it is a misclassification.** `_e3_write_row()` retries rc=2 and (post the rc=3 fix) the empty-candidate case, but its final `else` treats rc=1 as fatal-no-retry — while `orch-apply.sh` exits 1 from three unrelated branches (validator :136-141, stamping :159-164, conservation :179-184). Split it: the validator arm is racy (candidate is built from a live read that can catch a peer mid-write), the conservation arm is not (candidate = live + 1 row, totals can only grow).
- **The diagnosability half is the cheaper and more valuable half.** `_orch_apply_invoke() { bash "$ORCH_APPLY_SH"; }` throws away orch-apply's stderr, so the BUG telegram carries `rc=1` and nothing else. I could only separate "racing peer" from "broken board" by re-running the validator by hand. That is the fix that makes the *next* occurrence self-triaging.

### Evidence (raw, re-runnable)
- `docker exec …mcp-server-1` readonly probe of `/app/data/market.db`: `daily_foreign_flow` GROUP BY date DESC → `08-07 n=98 mu=02:47:43Z | 08-05 n=99 mu=04:29:40Z | 08-04 n=99 mu=08:59:25Z | 08-03 n=99 mu=08:59:44Z | 07-31 n=104 | 07-30 n=103`. 08-06 absent. 07-31→08-03 is the weekend, correct.
- Same probe: `market_prices MAX(updated_at)=2026-08-07T02:47:41Z` (117 rows); `get_market_snapshot` VN-Index 1761.88, breadth sessionTime 02:47:10 → price plane healthy, 74min was a 00:46Z pre-open write measured 3s after open.
- E-3 transience proven, not assumed: `-6afe`/`-7023` absent from `.signal_queue.rows[]`; the same actuator succeeded 72s later (`sys-20260807T015218-7a6a` is live); `bun scripts/orch-validate.mjs` exits **0** on the live file *and* on 792769d2f / 0426a1910 / 70fa2b2a6 — so the failing state existed only in the uncommitted working tree.
- Grepped board before every mint: 0 id collisions; named the 4 adjacent foreign-flow rows and the 5 adjacent SLA rows in each note as explicit do-not-fold.

### Carry-over
- **Report 4477 carries `resolution="fixed"` and that is wrong** — I over-specified the enum on the first call before reading the sub-flow, which documents `process_telegram_report(id=…, delete_telegram_message=true)` with no resolution. 4478/4479/4480 use the honest `"none"`. Nothing is fixed; the disposition of record is the board row. Read 4477 as "triaged → row minted".
- Pre-checks I did **not** run this tick (dispatch was scoped to the 4 reports): TNB audit read, channel audit, full `supervised-goahead.md` and `manual-dispatch-sweep.md` sub-flows, push-backstop. Do not read this cycle as a full PO tick.
- Observed while scanning and worth a look by whoever runs the next full tick: **19+ `supervised:true` rows carry `po_goahead=NONE`**, most of them sitting in `review[]`. If WF-2 SUPERVISED-HOLD gates on that stamp, that is a large stalled set.
- Unchanged from 01:43Z: `FIX-CI-SIZELINT-SCHEMA-TS-DEFLAKE-REGRESSION-372L` is the 6th file of a 6-file CI RED (now `review[]`/qa); `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER` (ready[], P1) remains the root cause and is dispatchable.
