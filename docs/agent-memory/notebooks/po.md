# PO — Notebook

## 2026-08-23T14:11Z · QA handed back two rows; its refusals were right and both of its supporting facts were wrong — and the prior tick's "DISPATCH IT" had minted nothing

- **Trigger:** declared NEXT from the qa review-lane drain (`8199e6023`). **Output: 1 orch-apply write, 6 board mutations, 3 mints, 0 certifications.** `task_total` 780→783. Journal: `decisions/triage-20260823T1410Z-po.md`, STEPs `po-Q1`–`po-Q7`.
- **★ A HOLD verdict can be correct while every fact under it is false — verify the refusal's premises, not just its conclusion.** qa refused to verify `FIX-SYSTEM-MAP-WATCHLIST-STALE-34-OF-58` (right: it is BLOCKED/plan_only, nothing to verify) but attached two claims I inherited by default. Both measured false: (1) *"defect still live, 34 of 58, re-measured this cycle"* — `get_watchlist` via gateway = **34**, `system-map .project.watchlist` = **34**, sets **identical, zero diff either direction**. There is no 58-ticker roster; qa re-measured only the file and took 58 from the row's own **title**, which architect (08-14) and po (08-22) had each already refuted by independent methods. (2) *"consuming a WIP slot"* — `scripts/lib/devteam-eligibility.jq:115-118` excludes BLOCKED from `wip_in_progress` **by construction**; measured `wip_in_progress = 1`. **A stale title is a prose trap exactly like a stale `status_note`, and it survives longer because nobody edits titles.**
- **★ Last tick's own decision never actuated, and I only caught it by scanning for the artifact instead of reading the note.** The 08:20Z drain on `RAG-FTS-BUILD-MEMORY-BOUND` wrote "AC#2 … DISPATCH IT" into the row. Full-board scan: **zero** AC#2 rows on any lane, `next_agent` still `po`. Six hours of prose that moved nothing — `feedback_po_notebook_mint_never_reaches_orchstate_board` firing on **my own** prior output. Minted `RAG-FTS-AC2-PEAKMEM-WALLCLOCK-MEASURE` as a real row rather than restating the intent.
- **★ But that drain was also *wrong* to dispatch it, so the new row ships BLOCKED not READY.** AC#2 requires a live `/admin/rebuild-fts` = a deliberate memory stress, while two siblings (`FU-RAG-DEPLOY-MEMORY`, `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED`) hold **uncertified** durability windows on that same container. po had recorded that constraint on 08-15 and the 08:20Z pass silently dropped it. **`blocked_by` encodes both facts; prose encoded neither.** qa's floor on those siblings (>=24h after 2026-08-22T12:40Z) **matured at ~12:40Z today** — it was "too early" at 08:4xZ, so this unblocks soon.
- **★ Third staleness of one AC in five weeks → de-hardcoded instead of re-pinned.** AC#1 said `768m` at mint, corrected to `1g` on 08-15, and live `HostConfig.Memory` is now **2147483648 = 2 GiB** (idle 1.461GiB/2GiB = 73.03%, ~552MiB headroom vs ~165MiB on 08-15). Writing "2g" guarantees a fourth. The AC now instructs a **live `docker inspect` read at verification time**. A qa run against a ceiling that no longer exists measures nothing.
- **Held `done_verified` withheld** on the parent (its own record: one false green already certified at a **116-row** corpus; live corpus 32184 vs ~56k). **Did not close** `FIX-SYSTEM-MAP-WATCHLIST-*` either — AC-1 passes today but **AC-3, the negative control, has no mechanism at all**; today's zero-diff is a side effect of the 2026-07-31 bulk reseed, not a guard.

### Carry-over
- **No lane move for the watchlist parent, and it is a guard interaction not inertia:** `orch-row-prose-ceiling-check.mjs:105` scopes `PROSE_CEILING_LANES` to `[backlog, ready, review]`, so an `in_progress[]` source is invisible to the live-side lookup (`liveBytes=0`) and its ~17.9kB lands as **net-new** growth past 12000B → hard reject until **D3** (`FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS`, `ready[]`) ships. BLOCKED status alone already buys the outcome a move would have.
- **`TASK_003` pointed at `generic-developer` — an agent that does not exist** (no `docs/agents/` dir, absent from the dispatch table, one occurrence board-wide). READY + unspawnable. Corrected to `developer`. Worth a lint: `next_agent` is unvalidated free text, same class as the null-`dispatch_lane` strand.
- **Two mints are `scripts/`-zone and therefore need a *developer*, not agent-father** — `FIX-QA-VC-ACTUATOR-NO-REGRESSION-FIXTURES` (BLOCKED on the actuator agent-father is landing now) and `FEAT-COWORK-WRITE-LAST-FIRED-DESTAMP-MODE`. The first is the stronger case: qa found **2 blockers pre-ship** and a fixture feeding the actuator's output to `orch-validate.mjs` would have caught **both**.
- **Deliberately untouched:** `FIX-QA-VC-LANEMOVE-*` (→agent-father), `FIX-PM-DECOMPOSE-*` (→agents-architect), `FIX-PEK-EXTRACT-SEMAPHORE-*` (→ops) — all three spawning in parallel; re-routing would race live peers. Peer session `007e33e4` mutated `in_progress[]`/`review[]` mid-tick; orch-apply CAS held, no retry needed.

## 2026-08-23T13:43Z · A flow doc asserted its own bug was the requirement, three times, and the archive proved it was never approved

- **Trigger:** router escalation — `slot=fb-weekend` dispatch BLOCKED at dispatch-claim Step 2.4; asked whether Sunday `WEEKLY_PREDICTION` is a real deliverable or the weekend is intentionally one post. **Output: 1 mint, 0 dispatches.** `task_total` 784→785. Journal: `decisions/triage-20260823T1340Z-po.md`, STEPs `po-U1`–`po-U5`. Row: `FIX-FB-WEEKEND-MARKER-KEY-SATURDAY-ANCHOR-SUPPRESSES-SUNDAY-PREDICTION` (backlog, agent-father, P1).
- **★ Do not litigate a "documented intent" — go find the row that authorized it.** Three flow docs (`weekly-recap.md:44`, `weekly-prediction.md:47`, `daily.md:81` — line numbers live-checked) assert the shared Sat-anchored dedup key is deliberate. The router framed this as prose-vs-machinery and asked me to overturn one. Neither: `FIX-FB-WEEKEND-DEDUP-GATE` (archive/backlog-detail.json, 2026-06-30) approved `published:fb-weekend:'+**VN_DATE**` with gate "a second firing IN THE SAME VN-DATE no-ops". Commit `11e10fd20` shipped `PERIOD_SAT` instead — dedup period widened 1 day → 2 — **and wrote its own justification into the docs as if it were the spec.** Nothing to overturn, only a deviation to revert. **New reflex: when flow prose defends a behaviour that costs a deliverable, the originating task row is the SSOT, not the doc.**
- **★ Result: 6 consecutive Sundays of total public-deliverable loss, and every log reads healthy.** 07-19 → 08-23, zero posts, while Saturdays published on 5 of 6. The agent exits `"DONE: duplicate-weekend-fb-post blocked ... PIPELINE: no-op"` — **a success string.** No error, no signal, no notebook entry. Detected only because a human tried to dispatch it by hand.
- **★ Two config facts killed the "Sunday is just Saturday's catch-up" reading before it could survive.** `_dish_type_catchup_config.fb_weekly_post.catchup_max_lateness_minutes = 120` — a Sunday fire is ~24h late, structurally outside catch-up. And `weekly-recap.md:8` **forbids** the Sunday content ("EXPLICITLY NO prediction ... NO 'Du doan' section"), so Saturday cannot absorb it: suppression deletes the product, it does not merge it. Third site nobody had found: `cowork-schedule.json .slots[fb-weekend].publish_date_basis = "vn_date_saturday_anchor"` → AC-5.
- **★ Rejected the obvious fix before an implementer could reach for it.** Per-mode keys (`published:fb-weekend-recap:`) were the router's first candidate and they **fail open silently**: Step 2.4 prefix-matches `"published:" + slot_id + ":"` *with the trailing colon*, so `...-recap:` matches nothing and BOTH weekend markers vanish from the collision guard. VN_DATE preserves it exactly. Mode granularity, if ever wanted, must be a **suffix**. Wrote this into AC-6 so it is not rediscovered the hard way.
- **Declined the fold into the P0 sibling** (`FIX-COWORK-PUBLISHED-MARKER-TTL-28H-EXCEEDS-24H-DAILY-CADENCE`). Its own text excludes this case — "bounded to router-MANUAL dispatch ... the cron path is unaffected" — and this defect is the agent's own exact-match self-gate killing the **cron** path. None of its 3 ratified axes repairs it. Cross-linked instead; the P0 becomes the **successor** for fb-weekend manual dispatch once VN_DATE keys land, not a prerequisite.

### Carry-over
- **The fix needs no architect ruling** — it is a revert to an already-approved spec, S-sized, `docs/agents/` zone. Row is BACKLOG/`next_agent=agent-father`, so it reaches nothing until DRS promotes it or the **router spawns `agent-father` directly**; the latter is the fast path and lands the next Sunday post.
- **Concurrency, live:** a peer PO committed `po.md` at 13:43:18Z, one minute before this write — I **prepended instead of overwriting** to avoid destroying its cycle. My board write was swept into that peer's commit `addb8a9a5`, not my own (verified present in HEAD). Same `feedback_phaseb_intent_key_is_freeform...` class seen earlier today.
- **This was a SCOPED tick, not a full triage** — no TNB audit, no channel audit, no signal sweep. Recorded in the minted row's `status_note` so the next tick does not read it as a completed pass.

## 2026-08-23T13:41Z · The chain that built this feature has no deploy step, so "green tests + next_agent=qa" was one hop away from stamping DONE_VERIFIED on software no user can reach

- **Trigger:** router handoff on `TASK-BCTC-INSPECT-UI-FILTERS` (session `669e1d9f`) — dev-mcp-server done, 18 new tests green, tsc clean, row in `review[]`/`next_agent=qa`, developer's own handoff flagging `REBUILD_REQUIRED: true`. **Output: 1 orch-apply write, 2 rows gated (child + parent), 0 mints.** `task_total` 784→784. Journal: STEP `po-S187`.
- **★ I verified the developer's blocker claim instead of forwarding it, and it was true in a worse way than reported.** `curl :3000/api/bctc-inspect` and `:3001/dashboard/bctc-inspect` → both HTTP 200, both **exactly 103876 B**, both **zero** `id="quarter-filter"`; working tree has 3. But the running image `sha256:c8322d2` was built **2026-08-15T08:44Z** and is **8 `apps/mcp-server/src` commits behind main**, not 1. The rebuild is therefore not a one-feature deploy — it ships a week of accumulated mcp-server work, so live verify has to re-assert `/health` toolCount=183 + scheduler=88 alongside the dropdowns. A blocker reported by the agent that is blocked by it is still a hypothesis; the byte-identical 103876 B on both origins is what made it a fact.
- **★ The gate had to be a structured field, because prose gates demonstrably do not hold.** Wrote the reasoning into `status_note`, but the thing that actually stops a premature claim is `next_agent: qa → ops`. Same-day precedent is unambiguous: `feedback_dispatch_gates_blind_to_prose_status_note_autoclaim_contraindicated_row` — a picker auto-claimed a row whose prose said do-NOT-build. Also set `rebuild_required: true` and `po_verify_chain: [ops:rebuild-mcp-server, qa:live-dual-origin-AC1-AC10, close:done_verified]` so the sequence survives a session boundary.
- **★ A qa pass right now would have been structurally false-green, not merely premature.** AC10 names `http://localhost:3001/dashboard/bctc-inspect` — the user's actual entry point. Against a stale image, the only thing qa could verify is source + unit tests, which is exactly the `feedback_janitor_false_green_verify` / `feedback_fence_false_green` shape. **The acceptance criterion was unfalsifiable, and an unfalsifiable AC verified anyway always returns PASS.**
- **★ Root cause is fleet-wide and already filed: `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`, P1, `ready[]`, unclaimed since 2026-08-01 — 22 days.** `REBUILD_REQUIRED: true` is a marker a developer writes that **no lane reads**. The pm-authored chain here (`ba → architect → pm → dev-mcp-server → qa`) contains no deploy node at all. This task is not an exception to that gap; it is the gap firing again, and every image-baked service change in the last 22 days landed the same way. Did not append evidence to that row — it sits at 14205 B against the 12000 B growth-only prose ceiling.
- **The archived sibling was never deployed either, and its done_verified stamp records a source-only check.** `TASK-BCTC-INSPECT-LABEL-FIX` (parent AC9, `buildLabel()` double-quarter) is already `done_verified` → archived (`dde17428d`, pm closeout `57d221baa`) on "direct-commit verify". Its fix has never run in the live container. **One rebuild is the first real deployment for both children** — so AC9's `HUT Q1 2024` label is in qa's live scope too, even though the row that owns it is closed.
- **Held the parent open rather than let it drift to DONE_VERIFIED.** `FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER` sits in `done[]`/`next_agent=pm` with both children nominally finished — a pm tick could reasonably close it. Wrote a `verify_note` barring the advance until the child clears ops→qa. Ironically the thing currently protecting it is another defect: `done[]` has no `done_verified` producer, which is what `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION` (in progress this tick) exists to fix.

### Carry-over
- **Dispatch is NOT self-executing — I cannot spawn.** The chain is minted on the board but reaches nothing until the router spawns `ops`, then `qa`. `next_agent=ops` on a `review[]` row is off the DRS ratified allowlist ⇒ **router manual dispatch or it sits**, same standing condition as the 11:57Z and 12:48Z BATCHes still open.
- **Exact rebuild command is load-bearing:** `docker compose up -d --build mcp-server` — single service. **Never `down && up`** (`feedback_rebuild_recreate_destroys_peers` — kills the 12 healthy peers, one up 4 weeks). Verify the new image ID **differs from `c8322d2`** before declaring success (`feedback_docker_rebuild_race_pgrep_deadlock`: `pgrep`/health alone false-passes on the old container).
- **PO has full autonomy on this rebuild — no user gate** (`feedback_po_deploy_rebuild_full_autonomy_no_user_gate`). Authorization is given; only execution is delegated.
- **qa's live brief, beyond AC1–AC8:** dual-origin (`:3000` direct + `:3001` proxy), quarter facet = **11** options not 13 (the HUT string-quarter landmine), ticker = **50**, AND-compose, zero-match disabled+selected option, selection-preservation with no refetch — plus the 8-commit blast radius: `/health` toolCount=**183**, scheduler=**88**, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200.
- **Recurrence risk if only this instance is fixed:** re-dispatch `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` (P1, `ready[]`, `next_agent=agent-father`) in the same wave, or the next image-baked change repeats this exactly.
- **★ PUSH-BACKSTOP fired at tick exit and found the fleet stranded — minted `FIX-TASKCLAIM-LINT-LINEMOVED-GRANDFATHER-MISS-REDS-PREPUSH-STRANDS-FLEET` (P1, backlog, agent-father).** `ahead=42 behind=0`, tsc GREEN, push REJECTED by the pre-push `task-claim-lint` gate on 4 sites in `ba/init.md` + `pm/init.md`, each reporting **LINE-MOVED** (same file, same snippet, baseline records a line 4 lower). **This is not the known SIGABRT class** — it is deterministic, full-output, and reproduced on two consecutive runs (abort signals 13:35:47Z, 13:44:51Z). Root cause is that the baseline grandfathers by **file+line**, so any unrelated edit *above* a grandfathered block silently un-grandfathers it and reds the gate for the entire fleet. AC1 pays the 4-line debt; AC2 rekeys the baseline to file+snippet so it cannot recur. `task_total` 785→786.
- **The BCTC chain does not depend on that push** — rebuild and dual-origin verify are entirely local. Push repair is parallel, not a prerequisite.
- **Sweep-guard still flags this session** — explicit pathspecs on every commit, never bare `git commit -a`.
