# PO — Notebook

## 2026-08-06T21:48Z · FACTORY-NEWS-fix-source-logging → code DONE_VERIFIED, rebuild OWED (first TRUE×TRUE) + 1 ops mint

### What actually happened
- **Seventh stale `review[]` row in seven ticks.** Direct-commit, `branch:null`, sat since 2026-07-24. Last tick's carry-over (old line 27) named this exact row and said *"three distinct verdicts across 2 checked rows — do not inherit either answer; run the graph×delta check per row."* I ran it and got a **fourth** answer: the first genuine **rebuild-owed**.
- **All dev predicates re-run by me, uncached.** `fe264aa43`+`e87ae392c` both `merge-base --is-ancestor … main` = YES. `bun test` → **241 pass / 0 fail / 6 skip** (dev claimed 235/0/6 — delta is later unrelated work; 0 fail is the load-bearing number). `bun tsc --noEmit` → exit 0. Live tree still carries the fix 13 days on.
- **Checked the one thing the diff cannot show.** The fix is a two-branch ternary, so it is correct *only if* `NewsSource` has exactly two members. `domain/models.ts:9-12` → `export enum NewsSource { REUTERS, BLOOMBERG }`, a **real runtime enum**, so the `import type`→value-import switch is legal and the ternary is exhaustive. A third member would have silently inherited the bloomberg tag.

### Decisions worth keeping
- **First TRUE × TRUE — occ-5's two-field marker now has a control on both sides.** occ-4 was `in_build_graph=false`; `FACTORY-ALERT-router-cleanups` was graph-true/**delta-false**; this row is graph-true/**delta-true**. Two negatives could have been explained by a marker that just never fires. A genuine positive is what makes `in_build_graph × observable_delta` *discriminating* rather than merely conservative. Strongest evidence yet for `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`.
- **Proof, not inference, that the shipped fix has never executed.** `Dockerfile:34` `COPY --from=bun-builder /app/src ./src` + `:44` `CMD ["bun","run","src/index.ts"]`; `docker inspect` → only mounts are `./data/live` and `mcp.config.json`, **no `src` bind-mount**, so served code is image-baked. Decisive: `docker exec … grep -n console.warn /app/src/module/news_ingest/index.ts` still returns `_source` at `:102` and **all six** buggy warn lines. Image `Created=2026-07-15`, nine days *before* the fix commit.
- **Did NOT inherit the sibling's `false`, and did NOT copy the sibling's severity either.** Graded P3, not the alert-engine row's P2: all three unshipped news-fetch commits are behaviour-neutral at the data plane. The live cost is **diagnostic integrity** — prod still prints BOTH source tags on every fallback warn, so log-based triage attributes a Bloomberg fallback to Reuters, poisoning the news-freshness fetch-vs-analysis two-layer diagnosis. Real, but not data corruption. Severity gets measured per row too, not just disposition.
- **Held in `review[]` with `next_agent=ops` rather than flipping `done_verified`.** Flipping terminal hands qa a live-verify against an image I have *proven* stale — the `NO-CONSUMER` defect itself. Mirrors `FACTORY-APIGW-split-capability-prober` so both rebuild-owed rows behave identically. `PENDING-USER-GATED` retired per the 2026-08-06T19:00Z greenlight + standing PO-deploy-rebuild-full-autonomy.
- **The dispatch brief told me I had no gateway grant. It was wrong — I checked.** `task_claim` returned `{"claimed":true}`. Per `feedback_agent_reported_limitation_may_be_structural_check_the_tool_grant`, believing it would have meant committing to shared `main` with **no mutex** while peer PO sessions are demonstrably writing concurrently. **A dispatcher's claim about my own tool grants is a claim, not a fact.**

### Evidence (raw, re-runnable)
- 1 `orch-apply.sh` pipe, landed attempt 1: `task_total 754→755`, `signal_total 203=203`, 2 rows stamped. Post-write re-read (guarding last tick's peer-clobber) → `rebuild_authorization=PO-AUTHORIZED`, ops row present, file mtime matches my apply. **No clobber this tick.**
- `_updated_by`/`_updated_at` still name the 21:22Z peer session — `orch-apply.sh` stamps row-level `updated_at` but leaves those top-level fields (`head=unchanged`). They are **not** a reliable "who wrote last" signal; verify by row presence.
- Filter persisted to scratchpad only (single-use). Journal: `docs/agent-memory/decisions/sprint-FACTORY-NEWS-fix-source-logging-po.md`.

### Carry-over
- **`OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND`** (P3, new, backlog) — needs ops. Single-service rebuild ONLY. AC-4 is the decisive post-check (`docker exec` grep must show `source` + exactly THREE warns); AC-2 matters because the current container's `vn.market.git_sha` label reads literally `unknown`. **One rebuild clears TWO review rows** — also unblocks `FACTORY-NEWS-dedup-handlers-maxitems`.
- **Three services now stuck on their 2026-07-15 images**: news-fetch (new), alert-engine, mcp-server. That is a fleet pattern, not three coincidences — the per-service deploy-debt counter argued last tick has its third data point.
- **`FACTORY-NEWS-dedup-handlers-maxitems`** (review) has `dev_result: null` yet its commit `d4f905a66` is landed on `main`. Row state contradicts git. Not my row this tick; **triage it next**.
- **`FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`** (P1, ready, occ-5) — still highest-leverage; now carries a positive control, so AC-1 can be written against real both-sided data.
- **4 `review[]` rows still on retired USER-GATED framing**: `FACTORY-FRONTEND-split-market-summaries`, `-split-orchestration`, `FACTORY-ALERT-router-cleanups`(done), `FACTORY-NEWS-go-server-tier-split`. Four verdicts observed across three checked rows — inherit nothing, run graph×delta per row.
- **Unchanged 4 ticks**: `scripts/audits/po-mint-orchapply-actuator-verify.sh` unauthored (main.md:173); 2 P1 telegram-ack rows undispatched.
- **Single-row triage, not a full PO tick** — no channel audit, TNB read, dashboard drain, manual-dispatch/supervised-goahead sweep. Next full tick owes all of those.

## 2026-08-06T22:05Z · dev-team Step 1 triage — 5 reports + 1 signal → BATCH(6), 1 mint, 1 refutation

### What actually happened
- **5 unresolved reports, and only ONE deserved a new row.** 4/5 already had owners on the board; the escalating agents' own prior-art checks missed all four. The pattern in every miss: they searched the **symptom** string, not the **owner** row. `orphan-signal stale terminal` → searched "stale terminal", never found `FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD`, which not only exists but was **PM-decomposed into 6 `ready[]` children on 2026-07-22 and none have moved in 15 days**. FR-4 *is* the terminal-status precheck the signal asked someone to build.
- **Report 4474 was refuted, not fixed.** The auditor's "0 anomalies but signals_posted=1" is the *checker* misfiring. `audit-output-contract.sh` counts `[emit-signal] SKIP-dedup` as `signals_posted++`; `system-auditor/flow/main.md:835` defines the headline `N` as NEW-only ("dedup-skipped … do NOT count"). V4/V5 straddle that boundary ⇒ **every dedup-only cycle self-reports a false VIOLATION**, deterministically. V2/V3 are safe (their counters also increment on SKIP-dedup, so they're denominator-symmetric). Minted `FIX-AUDIT-OUTPUT-CONTRACT-V4-V5-DEDUPSKIP-DENOMINATOR-FALSE-VIOLATION` (P1).
- **TNB's self-audit fork adjudicated from git, not from narration.** It asked: (a) confabulated read-backs or (b) writes lost pre-commit? **(a)**. `git show 1f670c381^:docs/handoffs/tnb-audit-latest.md` still reads *Cycle 121*, so c122's claimed overwrite-plus-read-back never happened; and no c122 signal exists in `docs/signals/` **or** `processed/`. (b) is refuted, not just unsupported — a reverted write can't be absent from both dirs.

### Decisions worth keeping
- **The control is what makes the TNB ruling stick.** c123's own writes, on the *identical* Bash-less grant, DID land (handoff in `1f670c381`; signal at `processed/tnb-20260806T2029Z.json`). Same agent, same grant, opposite outcome ⇒ **the tool grant is not the variable; whether the call was issued is.** This retires the 2026-08-01 c120 diagnosis as half-wrong (it ruled out the grant correctly, but assumed a write existed to lose) and makes this **occurrence 5 — the first on a non-auditor agent**, which is exactly what settles that row's AC-1.
- **Also fixed a false-positive inside the reporter's own detector.** TNB checks `docs/signals/` for its dropped signal, but dev-team drains to `processed/` (its own moved in 11 min). Absence from the live dir is the *expected* steady state after a **successful** drop — so TNB's read-back will keep scoring its healthy cycles as failures. Written into the ACK.
- **Probed before escalating on a worsening trend — and it was right not to escalate.** rag-service A-30 read 96.42% → 98.08% → 98.20% across three signals. Live: **93.89%**, `RestartCount=0`, `OOMKilled=false`. Receded, no crash cliff; chronic pressure already owned by `RAG-FTS-BUILD-MEMORY-BOUND`. Checked deliberately against `feedback_a30_discriminator_crash_cliff_misscored_as_reclamation_dip` — the discriminator is Restart/OOM, never the percentage. The dedup suppression was *correct*; it just tripped the V4 bug above.
- **chef-morning miss ruled structural, with a falsification tripwire attached.** news-scout-sentiment (01:30Z) + bctc-analyst-slot-4 (00:00Z) also stuck while chef-intraday (07:23Z) fired ⇒ the failure is bounded to a **UTC window**, not a slot. 00:00–05:15Z = 02:00–07:15 local (host is UTC+2) = the no-live-CLI-session window. Kept OUT of the BATCH — the remedy is an infra/product call, not dev work. **Tripwire on the row: a miss inside 07:15–22:00 local refutes this and escalates immediately.** A "known cause" ruling without a falsifier is just a dismissal.
- **Overrode TNB's own 3-cycle recurrence gate on F-CHEF-BIZCTX-JOIN-MISS.** That threshold guards against minting on thin evidence; `FIX-CHEF-BIZCTX-GATHER-TO-CONVICTION-WIRING` has existed since 07-18 with an independently-derived root cause, so N=1 is *corroboration of a standing diagnosis*, not the basis for a new one. First concrete ticker-level evidence in 19 days.

### Carry-over → next tick
- **`FIX-ORPHAN-FR4-FR5` passed every documented eligibility gate and still sat 15 days.** Not DRS-stranded (`next_agent=developer` is a dev role), not ready-XOR (`supervised=false`, `plan_only` unset) — but `zone: "flow-docs/"` is not in the enum `po/flow/main.md:30` declares and `zone-detect/SKILL.md` cannot resolve it. **That is a coverage hole in BOTH BOUNDED-1 idle pickup and my own manual-dispatch-sweep.** I annotated rather than rewrote the zone (`FIX-ZONE-ENUM-SSOT-CONTRADICTED-BY-259-OF-659-OPEN-ROWS` owns it, 259/659 rows). **If it is still unpicked next tick, mint the coverage-hole row — do not re-fold it a third time.**
- **`TE-T06` folded for the SECOND time today** (first at 07:52:24Z, still BACKLOG 14h later). The 4h `flag_reentrant` re-admission works; what does not work is that agent-father is occupied. Stamp carries an explicit **DEFER-if-saturated** instruction. If it returns unpicked a third time, the defect is not the sweep — it is that 44 of 46 candidates route to a single serialized agent.
- **`FIX-AUDITOR-DASHBOARD-MUTEX-…` raised P2→P1**: minted 08:24Z, recurred 21:10Z — 2 instances in 13h. Note for the implementer: the dropped row isn't merely lost, V3 turns every mutex-drop into a *false* contract VIOLATION next cycle. Fixing the actuator retires both.
- Router `dispatch-race-observation` (`router-20260806T1852-dupdispatch`) triaged, not minted: PRE-CLAIM's namespace is `intent:<agent>:<intent-key>` and does not encode the task_id, so two peer routers can manually-dispatch the SAME task under DIFFERENT intent-keys without colliding. Remedy belongs with the orphan/coordination FR set already in `ready[]`. **Re-raise as its own row if a 2nd occurrence lands before that set ships.**
