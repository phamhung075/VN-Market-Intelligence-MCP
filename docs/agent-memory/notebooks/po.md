# PO Notebook

_Last: 2026-07-29T10:31Z (rag-service restart-loop diagnosis rehomed to the board; DIAGNOSE spike closed) · 1 row minted, 3 rows amended, 1 spike closed._

## This cycle

- **The handed diagnosis was right about the ceiling and wrong about the mechanism, and only re-reading the code it cited showed that.** I was given "no application leak, fixed model baseline eats the cap, compaction constant is stale" — all true. But `repositories.py` `compact()` puts `self._insert_count = 0` **inside the `try`, after `await table.optimize(...)`**; the `except` logs a WARNING and returns normally without resetting. `insert()` then wraps `await self.compact()` in its own try/except whose handler *does* reset — commented `# reset to avoid tight retry loop` — but that handler is **unreachable**, because `compact()` already swallowed the exception. Any compaction failure pins the counter at ≥100 and every subsequent insert re-fires a full-table `optimize()`. The code's own comment names the exact failure it fails to prevent. That is a defect, not a tuning constant, and nothing in the brief pointed at it.
- **The logs held the proof, and the brief had read the same logs.** 6h window: 770 `POST /index`, 14 compaction attempts = **55 inserts/attempt against a nominal 100**. At 09:02:36–09:02:44, **six `optimize()` calls in 8.6s** — four failures, one success, one more failure. The brief reported "zero errors in a 30-minute window." The window was too short and too recent to contain a burst. A quiet 30 minutes is not the absence of a loop.
- **One `docker stats` read beat the whole static analysis.** The brief inferred "78–91% of cap consumed by the model" from a docstring. I caught the container mid-warm: 50.7 MiB cold, then **749.9 MiB / 768 MiB = 97.65%** once the model loaded, having served *one* insert. **18.1 MiB of headroom.** The docstring's "~600–700 MiB" understates live. Why an `exec` kills it, why a ~20 MiB fragment write kills it — all of it falls out of that number, and it cost one command.
- **`find -mtime +2` turned the brief's weakest inference into a measurement.** It inferred `_indices` orphaning from "735 dirs exist." Against the same `_COMPACT_RETENTION=2d`: `_versions` has **zero** files older than the window, `data/` has 33 of 2927, `_indices` has **352 of 735, oldest 11 days**. Same parameter, opposite outcomes — a differential, not a count. Also 540 of the 735 dirs are **empty husks**, so "735 orphans = 136 MB" conflated two things.
- **The same command falsified the brief's headline item.** "Compaction is not achieving its documented guarantee" was read off 2927 files. But 38 are >1 MB (five largest 19.4–20.0 MB, near-hourly) — those *are* the compacted outputs, and version pruning provably works. Compaction converges. The real issue is the growth trajectory, a different row. File **count** and file **bytes** told opposite stories; only the count got read.
- **Both restart-count premises were stale, not just the one I was warned about.** The brief flagged `DIAGNOSE`'s "RestartCount=135". `FIX-…-CLEAN-EXIT`'s own "226 (~10/day)" is stale for the identical reason — the container was destroyed and recreated 2026-07-15 in the docker recovery. Live: 22 restarts / 13.8 days = **1.6/day**. But that same recovery reset the corpus to 116 rows, and compaction is O(corpus); at ~11.2k rows and +103/h the rate should climb back. **The improvement is a small-corpus artifact, not a fix** — recorded on the row so nobody reads 1.6/day as recovery.
- **The spike's `owner=ops` was right; the correction handed to me was wrong.** I was told the field is null. The *board row* omits it; the archived detail record carries `owner: ops` / `next_agent: ops`. The spike read the authoritative record. With thin-row-plus-`detail_ref`, "absent on the row" and "absent" are different facts, and I nearly propagated the correction. (Moved it to `developer` regardless — it is a one-line Python fix, not an ops action.)
- **Held it at P2 deliberately and wrote down why.** The pull was P1: 97.65%, an actively-firing loop, a trivial fix. Against: `ready` holds 49 rows at P0/P1 with `qa=0`, so a 50th P1 queues; and the fix is **necessary but not sufficient** — with 18 MiB of headroom the container stays killable however well the counter behaves. P1 would have promised a closure it cannot deliver. Escalation trigger recorded (restarts/day > ~5).
- **No findings doc.** The diagnosis went into the carrier FIX row, where the implementer actually starts. A findings doc is where correct analysis goes to not be read at dispatch time.

## Carry-over

- **The AC that guards against my own priority call:** AC6 on the fix row forbids claiming the restart loop is closed. If a task report cites a dropped RestartCount as proof, reject it — corpus size confounds that number completely right now.
- **`FIX-RAG-COMPACTION-DISK-AMPLIFICATION` must not be sized before the counter fix lands.** Retuning cadence on top of the unreset counter measures the defect, not the tuning. Encoded as `depends_on`, but it is exactly the kind of dependency a picker skips.
- **`supervised = never dispatched` watch, 5th tick.** Last cycle's SPIKE went READY + `supervised:true` and I predicted that sitting untouched confirms a routing defect. I have now added a *second* `supervised:true` row to `ready`. If both sit next tick, that is the routing defect confirmed — **not** two new rows.
- **A-30 row now 5d+, `recurring_bug_count=6`, missed three times** (6th consecutive carry). Threshold was reached two cycles ago: still BACKLOG with no architect activity next tick → **escalate to user**. Do not re-attach evidence again.
- **Half B of the A-12 fix still has no owner** (2nd carry). The api-gateway fan-out fix is outside the SPIKE's detector charter. If the SPIKE returns a detector-only remediation, reject it — same partial-delivery shape that closed the parent with A-12 undone.
- **Sequencing is load-bearing:** counters row before gate row. Reversed, the artifact-derived counter disagrees with the OUTPUT-CONTRACT line on every dedup-suppressed cycle and surfaces as hook noise rather than the upstream fix it indicates.
- Standing, 13th carry: verify my own commits carry my authorship (`git log --author`).
