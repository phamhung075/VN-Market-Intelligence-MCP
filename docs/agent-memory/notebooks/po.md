# PO — Notebook

## 2026-08-14T21:29Z · A `plan_only` row's review note reads exactly like finished work. The only thing that tells you it isn't is grepping the file it claims to have fixed.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain, `FIX-AUDITOR-DOCAUDIT-MEMORY-PATH-PREDICATE` (stale `review[]`, `branch:null`, minted 2026-07-22, sat 23 days). **Output: 1 `orch-apply.sh` pipe (row `review[]→backlog[]` + 1 row minted; task_total 689→690, conservation clean), 1 `po_goahead_*` stamp, 2 journal STEPs. No DONE_VERIFIED.**
- **★ REFUSED THE SIGN-OFF, AND THE REFUTATION TOOK ONE GREP.** `architect_review_note` is 1.6KB of confident past-tense prose — "verbatim diff proposed", "sibling sweep … zero other phantom paths found" — and its own first token is `PLAN-ONLY (… main.md NOT edited)`. Live: `docs/agents/system-auditor/flow/main.md:841` **still** reads ``### 1. Memory integrity — `memory/MEMORY.md` ``. Nothing shipped. **Standing rule for every supervised+`plan_only` row reaching me: the disposition is never read off the review note — it is read off the target file.** A note that describes a diff is evidence of *design*, never of *landing*.
- **★ LINE ANCHOR DRIFTED AGAIN (:720-722 → :841-843, +121L), BUT THE DIFF SURVIVED — AND THE REASON GENERALISES.** 5th anchor drift I've logged in 2 ticks. This one was **non-destructive** where the `drain-signals.md` case (prior section) was destructive, and the discriminator is *not* luck: this spec pinned its target by quoting the **3-line BEFORE block verbatim**, so I could re-locate it byte-for-byte after 121 lines of upstream churn. The `drain-signals.md` spec pinned by **line number only** and its anchor migrated inside a jq code fence. **Text-anchored diffs survive drift; line-anchored ones become hazards.** Told agent-father to apply at the *current* line numbers and not trust `:720`.
- **★ THE ARCHITECT OFFERED ME THE WRONG TWO IMPLEMENTERS.** Its handoff says "developer or a future architect direct-edit cycle, PO's call". Both are out-of-zone: `docs/agents/*/flow/*.md` is **agent-father's** scope (`agent-father/flow/keep.md:33`, dispatch table `create/edit/review/maintain agent`), and my own constraint forbids me editing agent files. Dispatch is `intent=edit`, `agent_name=system-auditor`. **Consequence I wrote onto the row rather than discovering later:** agent-father is *deliberately* off the DRS allowlist (fleet-wide blast radius — it edits the files defining every other agent), so `next_agent=agent-father` = `DRS-STRANDED-OFF-ALLOWLIST` **by construction**. Correct routing here *guarantees* a stranded row unless PO hand-dispatches it. Kept `supervised:true`, cleared `plan_only` true→false, folded into this tick's BATCH.
- **★ THE ADJACENT FINDING IS LOAD-BEARING EVIDENCE, NOT A LOOSE THREAD — AND THAT INVERTS THE OBVIOUS RULING.** Architect flagged `docs/agent-memory/INDEX.md`'s 5 session pointers all dead since `ace28b78d` (2026-04-27) and asked for a PO ruling. Re-derived independently (`cat INDEX.md` + `ls sessions/` — `LATEST.md` and all 4 `2026-04-26-*.md` absent; dir is now dated files + `archive/` + `*.log`). It is a P3 one-file cleanup anyone could ship in a minute — **and shipping it early destroys the parent's acceptance criterion.** That broken state IS the natural negative control (spec §3): the only proof the repointed predicate is *live* rather than defanged into a trivially-true file-existence check. So: minted `FIX-AGENTMEMORY-INDEX-DEAD-SESSION-POINTERS` with `depends_on` the parent **plus** a hard note — do not start until the fix lands AND one post-fix Tier-3 DOC-AUDIT cycle has emitted the real broken-pointer WARN. **Generalisable: a bug that a broken gate has been masking is often the cheapest available proof that the repaired gate works. Fix the gate first, harvest the evidence, then clean.**
- **★ THE PHANTOM PATH WAS NEVER IN CODE — IT WAS MANUFACTURED BY THE READER.** Spec §0: repo-wide grep for `docs/agent-memory/MEMORY.md` = **zero** source hits. The prose said `memory/MEMORY.md`; the auditor LLM resolved it against this repo's `docs/agent-memory/` and reported a MISSING file that never existed, every Tier-3 cycle for ~4 months. **No hardcoded constant to fix — an ambiguous relative path in a flow doc is itself the defect surface.** The real `memory/MEMORY.md` (Claude's per-session auto-memory) exists but is host-keyed and outside git; the spec correctly declines to assert it, and the fix name-drops it once so the next reader cannot re-derive the same confusion.
- **Stale-stamp hygiene (did it, worth stating):** cleared `dispatch_lane` + `claimed_at/by` + `secondary_claimed_*` on the moved row. The DRS claim script resolves its target off the row's own `dispatch_lane`; leaving an `architect` lane on a row now bound for agent-father is exactly the shape that got caught pre-spawn on 08-14.
- **Carry-over:** (1) **NEW** — `FIX-AUDITOR-DOCAUDIT-MEMORY-PATH-PREDICATE` is `backlog[]`/`next=agent-father`, `po_goahead_20260814T212941` stamped, off-allowlist ⇒ **will strand unless PO/router dispatches it**; verify closure by grepping main.md for `memory/MEMORY.md`, never off a review note. (2) **NEW** — `FIX-AGENTMEMORY-INDEX-DEAD-SESSION-POINTERS` is sequence-gated; do not let a doc-hygiene sweep close it early. (3) `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING-PHASE1-IMPL` `backlog[]`/`next=developer`, unsupervised, ready. (4) `FIX-CHEF-BIZCTX-…-WIRING` REVIEW/next=agent-father, `po_verification_verdict=FAILED` — verify against the dish JSON. (5) Manual-dispatch sweep folds **1/tick** vs ~88 candidates — **5th tick reporting**, needs a ruling not more sweeping. (6) 3 rag-service rows BLOCKED/next=po pending an unelapsed 24h dmesg window. (7) 4 `to=ops` rows open since 06:43Z, **5th tick**; nothing drains that lane.
