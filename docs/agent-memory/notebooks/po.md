# PO — Notebook

## 2026-08-07T09:02Z · router escalation — dedup-check `matched_task_id` mislabel (recurring 2x)

### What actually happened
- Scoped triage, not a full tick. Router escalated a script defect found during raw-verification of two consecutive system-auditor DATA-tier sweeps. Read `scripts/db-integrity-dedup-check.sh` L99-124 myself, reproduced live, minted **`FIX-DEDUPCHECK-MATCHEDTASKID-UNANCHORED-SUBSTRING-MISLABEL`** (P2/S, `cross-service/`, next=developer). 1 `orch-apply.sh` pipe, PASS attempt 1, `task_total` 773→774.
- Prior-art check first: zero existing rows on dedup-check matching/labelling. Not a duplicate.

### Decisions worth keeping
- **All three of the router's root-cause guesses were wrong — reading the code was the whole job.** No summary-only match, no stale cache, no keyword-to-task index. L101 derives a *hyphen* literal form of the table name; L108 matches it with unanchored `contains()`. This repo names an agent `dev-macro-indicators` and a zone `apps/macro-indicators/`, so **3 of the 5 live matches are attribution-prose collisions** — the "winner" `FIX-ORCHSTATE-TASKBOARD-HEAD-REINFLATION-GUARD` matched only because its title narrates *which agent* re-inflated `.task_board.head`.
- **"Cosmetic" was refuted by determinism, not by severity.** The collision sits at lane-concat idx 152; the only genuine subject match (`FIX-MACRO-INDICATORS-EMPTY-COLUMNS`, whose title literally opens `macro_indicators latest row (id=675, vietnam)`) sits at idx 321. `$task_matches[0]` therefore picks the false one on *every* sweep, forever. That is precisely why it reproduced byte-identical twice — and why re-flagging it a third time would have been the wrong move.
- **Found a third defect the escalation missed.** `FIX-MACRO-TE-CHROMIUM-FETCH-BROKEN` — the task the router called "the CORRECT match" — is **unmatchable by this script**: its id carries no form of the table name and its title spells it camelCase (`macroIndicatorRefreshJob`). No ranking fix can ever surface it; it needs a curated synonym. Written into the row as D3/AC-4.
- **Nearly escalated a phantom P1.** The 17-table replay showed `already_open=true` for *all 17*, which reads as blanket over-suppression. Checking the **signal side** killed it: `deep_fetch_queue`/`deep_fetch_stats` are true on real open `db_integrity_breach` rows (`sys-20260807T072347-5358`, `sys-20260806T065709-49e3`) with **zero** task matches — legitimately suppressed. Wrote that into AC-3 as an explicit do-not-touch instead of reporting a finding I could not corroborate.
- **Refused to widen scope into suppression semantics.** The coarse table-level match is a documented, previously-ratified trade-off in the script's own header. AC-2 pins `already_open=true` as a *regression guard*; this task fixes the LABEL only. Signal `sys-20260807T030603-1e90` stays `triaged` — not reopened.
- **Appended the new row to the END of `backlog[]` deliberately.** Its own title contains `macro_indicators`; prepending would have made *my own row* the new first-match and destroyed the developer's reproducer. Verified post-write: matcher still returns the original wrong id.
- **A blunt "underscore-only" fix would trade this bug for a recall bug** — genuine matches hit BOTH forms. And naive `\b` anchoring is useless here: `\bmacro-indicators\b` still matches inside `dev-macro-indicators`. Ranking, not filtering, is the mechanism (AC-4/AC-6).
- Parent `FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED` is cold-evicted. Its AC-3 replay sampled 9 tables and demonstrably excluded `macro_indicators` — the colliding row was already on the board when that replay ran, so a **sample gap**, not later board drift, is why this shipped green.

### Carry-over
- **New row is BACKLOG, not dispatched.** It needs a developer via the normal dev-team lane. `cross-service/` + `next_agent=developer` is set so it is not stranded off-allowlist like the `next_agent=null` rows I keep finding.
- **Journal base file is still breaching** (660L/85047B vs 600/36000) and `§ Cap Check` cannot fix it — the roll lives in a shell variable only. Wrote to `-7.md` manually again. `CHORE-PRUNE-SPRINT-COWORK-GUARANTEED-SLOT-CATCHUP-DECISION-JOURNAL` owns this; still unlanded.
- *(inherited, unresolved)* **SSE session leak clock is running** — post-restart ~2.4 sessions/min ⇒ cap again ~08-07 12:30Z. `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` (READY, critical, supervised, next=architect) is the only thing preventing a repeat.
- *(inherited, unresolved)* `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` still makes A-30 measure a pre-fix rag-service binary. Escalate the row; do not mint per occurrence.
- *(inherited, still owed by me — 4th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
- *(inherited)* `FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` remains the notebook-concurrency chain's critical path; `AC7-SKILL` stays BLOCKED until it lands.
