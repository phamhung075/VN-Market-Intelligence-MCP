# PO — Notebook

## 2026-08-14T09:32Z · Closed an epic wrapper whose only deliverable was the split — and the schema was right to refuse my first write

- **Trigger:** dev-team Review-Lane SECONDARY-Drain handed me `FIX-PUSH-DELIVERY-ERROR-RATE-ALERT` (`review[]`, `next_agent:po`, so QA-Drain never sweeps it). **Output: 1 `orch-apply.sh` pipe, conservation OK (741→741), `review[]` 95→94, `done_verified[]` 25→26, 0 mints.**
- **★ I RATIFIED AT SOURCE, NOT ON THE SPLIT NOTE'S PROSE.** The row is `supervised:true`, and `supervised-goahead.md` Step 2 exists precisely so PO is the ratifier of record — accepting a relayed verdict makes the gate vacuous. 4/4 architect claims re-derived: no `/metrics` route and no `prom-client` (both greps empty), `/api/vps-proxy-health` at `server.ts:1023`, and `vpsPushLogStore.ts:97` hardcoding `["prices","news","sbv","bctc"]` while the `VpsService` type at `:13` **does** include `foreign-flow`. The specced gap is real.
- **★ THE LOAD-BEARING CLAIM WAS THE SAFETY ONE.** `classifier.go:51` maps `high|critical → ChannelMarket`, which `alert-policy.md` reserves for alert-commander stock alerts. The child's "hardcode ChannelWork" mandate is therefore **necessary, not stylistic** — routing this through `Classify()` would push infra noise into the MARKET channel.
- **★ I RAN DEV-TEAM'S OWN ELIGIBILITY LIBRARY INSTEAD OF ASSERTING DISPATCHABILITY.** `is_bounded1_eligible` against the live board: child 1 `true`, lane `dev-mcp-server`; child 2 `false` **solely** via `deps_ok:false` on its `depends_on`. So it self-releases when child 1 lands — machine-encoded, not prose (`has_unbacked_sequencing_prose` fires on neither). **A prior PO's "zero pickers" claim on this class was wrong; measuring beats asserting.**
- **★ `orch-apply.sh` REJECTED MY FIRST WRITE AND WAS RIGHT.** I wrote `next_agent:null` to mean "nobody next" — `TaskSchema` declares `z.string().optional()`, optional but **not nullable**. Validator exit 2, live file untouched. Correct encoding is deleting the key, not nulling it.
- **Stamped `po_goahead` on a TERMINAL row deliberately:** WF-2 SUPERVISED-HOLD scans `done_verified[]`, and I kept `supervised:true` as the historical record of why SLS routed this to architect. Unstamped it could hard-hold the pipeline if `.head` ever named it. Replayed WF-2's own predicate post-write: `SHOULD_HOLD=false`.
- **Added the missing forward `children[]` edge** — the children pointed back via `split_from`, but `is_epic_wrapper` (`devteam-eligibility.jq:167`) reads `children`, so this row never registered as an epic wrapper to any sweep.

### Carry-over (09:32Z)
- **★ Closing a wrapper ≠ shipping the capability.** The 2026-07-04 CF-tunnel gap (2 outages, 0 detection) stays OPEN until `FIX-PUSH-DELIVERY-ALERT-ENGINE-POLLER` ships. Both children are P1 in `backlog[]` and inherit fleet RLC/BOUNDED-1 throughput limits — this closure bought traceability, not detection.
- **Still live, unrelated to this closeout:** `resolved_dispatch_lane` has no `zone=="multi"` special-case, so it defaults multi-zone rows to `developer` instead of `architect`. Only ever row-level patched (`router_dispatch_correction_20260808T0223Z`), never fixed systemically.
- **Notebook hazard hit live:** the `09:29Z` section below is a **concurrent peer PO's** work, not mine. po's declared OVERWRITE contract (preamble+1 section) would have destroyed it, so I inserted instead and left both prior sections byte-identical (41L, under the 50L cap). The AC-6 contract and the known peer-overwrite data-loss class are in direct tension for this file.
- **Router action needed: none.** `.head` (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`, `next_agent:qa`) was not this row; left untouched.

## 2026-08-14T12:38Z · The fix for occurrence 2 was already in the file — sitting below the RETURN that ends the flow

- **Trigger:** router triage, 3rd occurrence of pm decomposition leaving `.head` + parent row stale (UC-CCA-P2, router repair `090bc6a14`); escalation threshold declared hit after occ 2. **Output: 1 `orch-apply.sh` pipe, conservation OK (741→742), 1 P0 `ready[]` mint to `agents-architect`, 0 re-touches of UC-CCA-P2.**
- **★ THE REMEDIATION SHIPPED, THEN NEVER RAN — IT LANDED BELOW THE EXIT.** `pm/flow/main.md` **already contains** occ-2's fix as Step 4c (`FIX-PM-HEAD-RESET-SHAPE`, full `.head` null-out matching dev-team WF-1c byte-for-byte). But the flow inlines its own `## RETURN … PIPELINE: continue` template **at Step 3c**, so a decomposition tick emits RETURN and terminates; 3d/4/4b/4c are monitor-phase steps it never reaches. The board row proves the exit point: pm wrote 7 children + `pm_decomposed_at` + `decomposition_note` (Steps 3/3b/3c) and nothing from 4c. **The router's brief read occ 3 as "pm made zero attempt" — the instruction exists and is unreachable. Placement defect, not a missing step.** That inverts the fix class: appending another step below the same RETURN is exactly what failed between occ 2 and occ 3.
- **★ I REJECTED THE DEV-TEAM-GATE OPTION ON EVIDENCE, NOT TASTE.** A WF-1e keyed on `.head.next_agent` vs the row's own `next_agent` **provably cannot fire on occ 3** — both read `"pm"`, perfectly coherent — and `FIX-DEVTEAM-HEAD-NEXTAGENT-RESYNC-ON-REASSIGN` already owns that predicate. Any parenthood-keyed variant is blocked behind the `.children` drift already tracked by `FIX-DEVTEAM-EPICWRAPPER-PARENTHOOD-FIELD-DRIFT-AUTOCLOSE-BLIND`. **Option (b) is downstream of (a), never a substitute.**
- **★ THE MISSING `next_agent` IS A MIS-DISPATCH TRAP, NOT A STRANDING ONE.** `devteam-backlog-claim-ready-lane-consumer.jq:129-134` admits candidates on `(effective_next_agent OR effective_owner)` with **no** non-dev-owner gate, and `resolved_dispatch_lane` falls back to `effective_owner`. pm set `owner` to the **subject agent family being edited**, so RLC would have spawned six cowork MARKET agents — incl. `fb-market-poster`, which publishes externally — to edit their own flow docs, instead of `agent-father` as architect's own `review_note` specified. **The router's hand-repair pre-empted that by timing, not by design.**
- **★ AND THE OMISSION IS DOC-COMPLIANT.** `docs/standards/task-schema.md` has **zero** occurrences of `next_agent` in either its Mandatory or Optional tables; pm Step 3 copies that table verbatim. `orchStateSchema.ts:175` types it `z.string().optional()` — explicit null fatal, **omission silently legal**. pm did what its SSOT told it, so the fix must reach the schema doc, not just pm's flow.
- **Escalated P0, not P1, on one live fact:** `IVC-PM-DECOMPOSE` is in `ready[]` **right now** (`next_agent:pm`, "mint IVC-C1..C6 + IVC-A1 + IVC-A2"), RLC-eligible today. Occurrence 4 is pre-loaded.
- **Routed to `agents-architect`, not `agent-father`:** the same actuator applied occ-2's fix and it landed unreachable. This needs a reachability **invariant** + a fleet sweep for the same "RETURN inlined mid-flow with live steps below it" shape, then a zone-split implementation — architect's call, not another append.

### Carry-over (12:38Z)
- **★ When a fix for a recurrence recurs worse, read the file before believing the report.** Occ 3 was described as a regression in agent behaviour; it was a regression in nothing — the doc changed, the control flow did not. **Verify the remediation is REACHABLE, not merely present.** No gate anywhere checks that.
- **Open, unowned, flagged in the mint's Q2:** is "RETURN template inlined mid-flow with live steps below it" present in **other** agent flow docs? I did not sweep — do not let this be validated on `pm/flow/main.md` alone.
- **Dedup done, do not re-mint:** 4 adjacent rows checked and cross-referenced in the new row's `note` (`HEAD-NEXTAGENT-RESYNC-ON-REASSIGN`, `EPICWRAPPER-PARENTHOOD-FIELD-DRIFT`, `MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER`, `PM-DECOMPOSE-NO-PRIOR-ART-PROBE`). The epic-wrapper row owns the **sweep** half of the stale-parent facet; the new row owns the **pm-mint** half.
- **Router action needed: dispatch `agents-architect` on `FIX-PM-DECOMPOSE-CLOSEOUT-STEP-UNREACHABLE-PAST-RETURN-AND-MINT-OMITS-NEXTAGENT`** (`ready[]`/P0/rank-0, RLC-eligible). `.head` left `idle` — a triage ruling must not claim a dispatch lane.
