# PO Notebook

_Last: 2026-08-05T19:30Z (router hand-off — 3 items dev-team's cron tick found but had no Agent/spawn tool to dispatch)._

## This cycle

- **Item 1 — commit-sweep-guard bug-escalation, occurrence 9.** Payload parsed first (mandatory rule): `escalated=true prior_warns=9 threshold=3`, actor `f298ccf7-...`. Dedup-checked against the FIX-COMMIT-SWEEP-GUARD-* family (5 rows, all read) — matched `FIX-COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR-AND-NOTEBOOK-LONGTAIL` (P0, review, next=qa since 07-30, 6d). Appended as occurrence 9, **not** a new row — standing ruling (`po_live_evidence_20260801T0158`) already holds these are true positives / forcing-function-working-as-designed. This one is qualitatively new: unlike today's prior 8/8 self-corrected warns, dev-team's own staged write actually landed inside peer commit `be9b90953` (attribution corruption, no data loss). Sent a WORK telegram nudge — the row's own fix is implemented+verified but unshipped, and this is the first live sweep since it went to review.
- **Item 2 — ci_red CI-RED-71969033 (size-lint, HEAD 71969033).** Ran the mandatory pre-dedup failing-file read (`gh run view 31037767910 --log-failed`): single file, `apps/mcp-server/src/infrastructure/db/schema.ts` baseline=336L actual=377L upper=369L. Traced to `FIX-MCP-MEMORY-CODE-LEAK` (review/next=qa) — its own commit `609f62800` (WeakSet init-guard) caused the regression, and the router had already flagged it inline on that row minutes earlier. Folded (stamped `dedup_key` for future auto-fold), no new FIX row — would duplicate an already-tracked, already-QA-routed disposition. Drained the raw signal file via `node scripts/agents-flow/drain-signals.js` (routed-to-po, moved to processed/).
- **Item 3 — FIX-BCTC-FPT-BT5-BALANCE-GATE review-lane row.** dev-pdf-extractor's fix is complete and well-evidenced: OCR trailing-separator misread root-caused + fixed in `vn_number_normalize/primitive.py`, balance identity delta 43.7T VND → 0.0 EXACT, targeted suite 213/213, full non-regression suite clean (7 pre-existing unrelated fails, git-stash A/B confirmed), decision journal present. `next_agent` was a stale self-target (`dev-team`) — flipped to `qa` for merge-review (no lane move, status stays REVIEW). Minted the recommended follow-up `FIX-BCTC-FPT-PAGE8-EPS-BLEED-ROWCOUNT` (backlog, P2, `apps/pdf-extractor/`) for the out-of-scope page-8 EPS-bleed gap rather than leaving it as an unowned recommendation.

### Carry-over
- Watch `FIX-COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR-AND-NOTEBOOK-LONGTAIL` — 9 occurrences, 6 days in review, rate accelerating, now a genuine sweep not just a warn. If QA doesn't pick it up next tick, escalate harder.
- `FIX-MCP-MEMORY-CODE-LEAK` still carries 2 open items for QA: the size-lint extraction-vs-justification judgment (now dedup-anchored) + the SseSessionManager /sse leak fast-follow named in its `architect_review_note`.
- Did not run a full generic PO sweep this tick (10 other `next_agent=po` rows exist across lanes) — router scoped this dispatch to exactly these 3 items; next unattended PO tick should still pick those up.
