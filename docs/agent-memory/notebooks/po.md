# PO — Notebook

## 2026-08-12T13:05Z · the router said "same change 3x"; I read the three scripts and one of them has no choke point at all

- Directed sprint kickoff from a user "yes" (instrument the cron preflight scripts so silent-tick cost is measured, not estimated). Minted 1 `sprint_goal` entry + 1 BA row `BA-TICK-PREFLIGHT-USAGE-INSTRUMENTATION` (SPRINT-M, P2, `cross-service/`, next_agent `ba`), two separate `orch-apply.sh` pipes. Conservation clean (task_total 752→753, signal_total 98→98). Umbrella lock `task:TICK-PREFLIGHT-USAGE-INSTRUMENTATION` claimed:true.
- **★ THE FRAMING I WAS HANDED WAS FALSE, AND ONLY READING SOURCE SHOWED IT.** Router offered 1 WU vs 3, calling 3 "the same change 3x". `cowork-tick-preflight.sh:76-82` and `dev-team-tick-preflight.sh:200-203` each funnel every verdict through one `_emit_verdict()` — mechanical. `auditor-tier1-probe.sh` has **no choke point**: inline `jq -n` at 757/767/772/858/894/921, and it is **dual-mode** — `run_tiered_probe()` calls `run_probe()` then re-emits a wrapper with a *different field set*. A per-call-site hook **double-logs every tier-2/3 tick in two incompatible shapes** — it would corrupt the exact dataset the sprint exists to produce. Answer: **4 rows** (shared lib → cowork → dev-team → auditor gated), not 1 and not 3.
- **★ THE DENOMINATOR WAS WRONG TOO: THE FAMILY IS 6.** `code-janitor-tick-preflight.sh`, `db-integrity-probe.sh`, `orch-sentinel-lite-probe.sh` share the identical verdict-JSON-on-stdout shape and the same silent-tick economics. Did **not** widen — tripling exposure before the shared lib survives one live cron is how a measurement sprint becomes an outage. Written into `scope_out` (d) as the named follow-up so it can't quietly vanish.
- **★ "CHECK BEFORE INVENTING A RATIO" — IT ALREADY EXISTS.** 4 chars/token, `2026-05-21-token-toolcall-economy.md` §W-1, applied in `2026-07-12-token-economy-lazyload-audit.md`. Also killed the brief's `python3 json.dumps()` suggestion: all four scripts already hard-depend on `jq` and use `jq -nc --arg` as their quote-safe emitter. Adding python3 to four cron-critical paths = new failure mode, zero gain.
- **★ THE SCRIPT'S OWN HEADER VETOED A FIELD I WAS ABOUT TO SPEC.** cowork lines 56-57: `CLAUDE_CODE_SESSION_ID` is "never echoed/logged … beyond its use as a bound `task_claim` arg". Session correlation would have been genuinely useful and I dropped it anyway — a stated contract in the file you are editing outranks a nice-to-have.
- **★ THE NUMBER THIS SPRINT PRODUCES IS A LOWER BOUND, AND THAT IS AN AC (AC-11), NOT A FOOTNOTE.** `verdict_bytes` measures only the tool_result; real tick cost also includes the cron prompt text + flow-doc lines loaded. **Shipping a precise-looking number with an unstated denominator is worse than the estimate it replaces** — that is how ~250-500 tok/tick becomes fleet lore.
- Three ways this breaks a live cron, each given a real fault-injection AC, not a happy-path assertion: AC-4 logger must not alter return status (`_emit_verdict` is repeatedly the last statement before an explicit `return`; cowork's `_step8_silent_release` propagates via `return $?`), AC-5 unwritable dest must not fail the tick, AC-6 stdout purity (`feedback_tick_preflight_verdict_is_first_json_key_tail_always_drops_it`).

### Carry-over (13:05Z)
- **`.head` left `idle` on purpose.** Router dispatches `ba` straight from this RETURN; pinning head to a row about to be picked up re-creates the stale-pinned-head class the router itself cleaned at 12:52Z today. The mint *did* reach the board — conservation 752→753 is the proof, not my say-so.
- **Do not let anyone accept the quoted suite counts.** Brief says cowork 20/20, dev-team 37/37, auditor 32/32. Carried into AC-10 as **UNVERIFIED** — QA must run them pre-edit. Inheriting a quoted baseline is how a regression gets graded green.
- **Architect owes three answers, I did not decide them:** Q1 log destination + git-tracked vs ignored (PO preference: `docs/data/telemetry/tick-usage.jsonl` **git-ignored** — ~500+ lines/day of churn on a tracked file walks straight into the commit-sweep hazards; the tracked `*-last-verdict.json` precedents are single-row *snapshots*, not append-only, so they are not precedent). Q2 one shared JSONL vs per-script (rotation fairness — a chatty script can rotate away a quiet one's history). Q3 WU-3's choke-point extraction shape.
- Board pressure unchanged from 13:02Z: `backlog[]` **392**, `ready[]` 83, `review[]` **134**, `in_progress[]` 1. `review[]` at 134 is now 4 ticks flat.

## 2026-08-12T13:02Z · one ci_red signal was simultaneously stale AND novel — job-level decomposition was the only thing that saw it

- dev-team Step-1 triage: 35 envelopes, 3 `to=po` dashboard classes, board 391/83/134/7. **1 mint, 2 folds.** Conservation clean (task_total 751→752, signal_total 98→98, row-identity clean).
- **★ BOTH 10:02Z PREDICTIONS RESOLVED — IN FAVOUR OF THE MECHANISM I WAS ABOUT TO ESCALATE AGAINST.** I wrote: *"if the 11:xx tick still finds OPS-RAG in ready[] after being BATCHed, then PO's BATCH is also advisory… that would mean PO has no dispatch mechanism at all, and the escalation target is the router."* **It shipped** — `OPS-RAG-SERVICE-REBUILD` DONE_VERIFIED (`ca6d86869`), and the idx-74 91h-RED `FIX-CI-SIZELINT-TRANSPORT-TS` also shipped (`6cbc304e1`; size-lint now **PASSES live**, transport.ts 241L). **Lane move + BATCH did work; latency was ~3h, not infinity.** I had a fully-drafted "PO has no dispatch mechanism" escalation ready. Two ticks of patience beat it. **Do not re-open that escalation.**
- **★ ONE SIGNAL, TWO OPPOSITE DISPOSITIONS — AND THE SIGNAL CANNOT TELL YOU.** `CI-RED-ffb8f8cc` names 2 failing jobs. `size-lint` → `transport.ts`, **already fixed, stale**. `task-claim-owner-session-lint` → `docs/agents/qa/flow/main.md`, **untracked, still RED at HEAD**. A whole-signal verdict ("already tracked" / "new") is wrong *either way*. My flow mandates per-FILE decomposition **only inside the bun-test isolation runner**; the other jobs emit no `FAILEDFILE` block, so each needs its own error-line read. **Decompose per JOB first, then per FILE.** 3 bun-test files deduped 3/3 onto existing rows — the mint came from the job my flow's hot path doesn't decompose.
- **★ THE "NEW" BUG IS A LINE-SHIFT, NOT A REGRESSION.** Baseline grandfathers `qa/flow/main.md` at lines **21/212**; the byte-identical call sites now sit at **22/215**. The lint keys on `file+line`, so edits *above* a call site manufacture a CI RED with zero behaviour change. Exact sibling of DONE_VERIFIED `FIX-CI-TASKCLAIM-PO-FLOW-OWNER-SESSION-PAYDOWN` (21→22 there too). Its AC5 built the `[LINE-MOVED: …]` message that let me diagnose this in one read — **a prior fix's ergonomics AC paid off on its own recurrence.** Minted `FIX-CI-TASKCLAIM-QA-FLOW-OWNER-SESSION-PAYDOWN`; fix = pay the debt + delete the baseline entries, **not** `--update` re-grandfather (lint says so itself). Not lint-appeasement: owner-mismatched `task_release` orphans locks.
- **★ 32 of 35 ENVELOPES FOLDED, AND THE VOLUME LEADER WAS UNROUTED.** `notebook_single_section_overage_breach` fired **7x** — the single highest-volume type this tick — and it is **not in the routed set at all**. Coverage guard `guard_signal_type_coverage`, recorded PASS on 2026-08-01, now **FAILS**: 7 unrouted live `to=po` types. Already owned by 3 open rows (`FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED`, `…TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`, `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE`) → folded, **no 4th row**. The guard drifts back to FAIL faster than the rows close.
- **★ RAG A-30 IS CONVERGING, NOT DEGRADING — THE TREND IS THE DISCRIMINATOR.** 4 signals read as a fresh crisis; ordered by time they read as a fix landing: **95.32% → 90.81% → 89.18%** across the deploy. Two are STALE-ACK on a `DONE_VERIFIED` row, which the ledger's `floor_enforcement_20260729` documents as the **intended** outcome. Folded as corroboration onto `FIX-RAG-EMBEDDER-IDLE-UNLOAD-…`. A severity snapshot alone would have re-opened a solved incident.
- **★ 5 sweep-guard hard-blocks, 4 of them MY OWN session** (`165f4245`, `prior_warns` 52→55 vs `threshold=3`, `outcome=blocked`). Folded occ 13→18 onto the QA-lane row. So I committed **pathspec-scoped** below.

### Carry-over (13:02Z)
- **Routing flag for dev-team:** minted row is `zone: cross-service/` + `next_agent: agent-father` (edits `docs/agents/qa/flow/main.md`, agent-father's commit zone). If Step 3 dispatches on `zone` alone it will route to generic `developer` — re-route or accept, but don't let the mismatch strand it like the 18 no-picker rows.
- **`microservice_memory_degraded` is a NAME-DRIFT twin** of routed `microservice_degraded` — same producer, same subject, one char class apart. Cheapest real fix in the routing-gap family: alias it, don't author a row.
- Unresolved-report backlog is **~37 rows spanning 12.5h**; ~15 are the settled rag saga. It is now mostly an *archive* of solved incidents, and its size is being read as pressure. Needs a bulk-resolve pass, not triage.
- Board pressure: `backlog[]` **392**, `ready[]` 83, `review[]` **134**, `qa[]` 7, `in_progress[]` **1**. `review[]` at 134 remains the bottleneck, unchanged for 3 ticks.
