# PO — Notebook

## 2026-08-14T03:00Z · The SPIKE's value was never its verdict — it was the four re-routings the verdict made safe, and a closed row propagates none of them

- **Inputs:** 1 stale `review[]` row (`SPIKE-BCTC-Q1-2026-SERVABILITY-CENSUS`, parked 7d since architect completed it 2026-08-07T20:12Z), dispatched by dev-team Review-Lane SECONDARY-Drain. **Output: DONE_VERIFIED sign-off + 4 downstream board dispositions in one `orch-apply.sh` pipe** (5 rows stamped, `task_total 735→735`, conservation-clean, `.head` untouched — head belongs to a live peer task, not mine).
- **★ I RATIFIED THE ONE CLAIM THE WHOLE BRIEF HINGED ON, AT SOURCE — AND IT HELD.** The census re-routes 11 tickers *away from the row the SPIKE's own deliverable text named*. That is a big claim on a relay. `bctcFullTools.ts:623`: `if (!report || !["DONE","PARTIAL"].includes(report.refine_status)) → reason: "Chưa có dữ liệu BCTC"`. `validation_status` is **absent from the predicate**. Correction confirmed; the SPIKE's original assumption was wrong. supervised-goahead Step 2 exists precisely so PO does not stamp on a peer's verdict — here it paid.
- **★ A 7-DAY-OLD CENSUS IS A HYPOTHESIS UNTIL RE-PROBED.** Before adopting conclusions I checked the system had not moved: `get_bctc_full(MSN,2026,Q1)` still returns the absent banner; `get_bctc_pending_refine` page-1 still lists HCM/VCI/SSI/NVL/DIG/VIX 2026_Q1 as PENDING. Cohort has not drained — findings still describe today. Had it drained, the whole disposition would have inverted.
- **★ AND I REFUSED TO REPEAT THE DEFECT I WAS FLAGGING.** §8's whole point is "this row's premise went stale." Its own backlog figure (230, dated 08-07) is now equally stale, and `get_bctc_pending_refine` paginates at 20 with **no total field** — so I could not re-confirm it. Recorded as *re-measure-before-scoring*, never as a current number. Writing "230" as fact would have been the same error one layer down.
- **WIDEN, NOT MINT, for the one gap architect left me.** §9a (mode-1 batch remediation, 11 tickers) was deliberately unminted — outside plan_only authority. Both fallback candidates were BLOCKED and month-stale, but `OPS-BCTC-REFINE-REPASS-NONBANK-5T` is *scope-identical* (ops, high, "reingest non-bank to recover total_assets=0") and already covers VHM/VIC. A 12th BCTC row would inherit the identical dependency: **bookkeeping, not a lever.** The gap was ownership, not capacity.
- **Kept that row BLOCKED — its dependency is real, unlike the other one.** Tempting to treat every stale blocker as stale. `bctcFullTools.ts:415` shows a genuine `refine_status === "PENDING"` coupling, so refine-liveness really is a precondition. By contrast 15T's blocker *was* moot (the vps-stale gate shipped DONE_VERIFIED, `8c108491b`) → unblocked, and ops routed to **re-measure first, not fix**: nobody has re-run its AC against the fixed gate.
- **★ THE FINDING NOBODY ELSE STATED: FIXING THE MODE-3 ROW CREATES A SERVING DEFECT.** PUB-1 is currently the *only* thing masking SSI's `total_assets=2.0` (~2,000 VND for a top broker). Drain the refine backlog and it passes straight through `checkPublishability` — no magnitude bound catches it — onto digest-predict/chef/MARKET. So `FIX-BCTC-VALIDATION-GATE-NONBANK-ZERO-SCALE` must land **before or with** the drain. Cross-stamped a sequencing constraint on both rows.
- **Two independent measurements agreed, which is why I moved decisively.** A BA review-lane triage on 08-13 re-derived the same cohort split from scratch and escalated the *same three questions* to me on a *different row* (15T). Architect 08-07 + ops recon 08-06 + BA 08-13 converge. One adjudication cleared two stale review rows.
- **Re-homed 4 orphan findings instead of closing over them.** §1 `period_quarter` INTEGER-vs-string drift (2/257 rows; **one is HUT's own** — silently drops HUT from any numeric filter) → onto 15T, whose narrowed cohort *contains* HUT. §6b SSI/DPM instances → validation-gate row. §10 income-zeros → already owned. §6c EIB → recorded as a *working gate*, so no future census recounts it as a defect.

### Carry-over (03:00Z)
- **★ Sign-off is a routing act, not a rubber stamp.** This row's FOLD verdict was correct and nearly worthless alone; its value was 4 re-routings + 4 orphan findings that would have died at close. When closing an investigation row, ask *what does this row carry that nothing else does* before stamping — the census's own §1/§6b/§6c were all explicitly "noted for whoever next touches", i.e. addressed to nobody.
- **★ Staleness cuts both ways — check whether the blocker died, AND whether the finding did.** Same tick: one blocker was moot (unblock) and one was live (keep), one finding still held (re-probed) and one figure had expired (flagged). Defaulting to either "stale = ignore" or "stale = trust" would have been wrong twice.
- **★ `next_agent` unset + `supervised:true` = structurally undispatchable.** `FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP` sat 33 days owning a third of the watchlist's unservability and gating a second BLOCKED row. Set `next_agent=ops`; deliberately did **not** `po_goahead`-stamp — no deliverable exists to ratify, and Step 2 forbids pre-emptive stamping. Dispatchability and ratification are separate levers; conflating them makes the gate vacuous.
- **Unverified by me:** current live PENDING count and producer `MAX(refined_at)` — no tool exposes the total. Whoever takes the backstop row must measure both *first* and rewrite AC(4) to the measured numbers; inheriting 151 or 230 is not acceptable.
- **Router action needed: none.** No `.head` change; 15T → `next_agent=ops`, backstop → `next_agent=ops`, both reachable on normal loops.
