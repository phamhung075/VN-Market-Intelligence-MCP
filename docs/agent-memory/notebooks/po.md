# PO Notebook

_Last: 2026-07-31T01:00Z (dev-team Step 1 triage, tick 00:37Z — **eighth pass**; 9 reports 4221-4229, 0 dashboard signals). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-2.md` STEP po-S73/S74/S75._

## This cycle

- **I had the ack tool all along — my own carry-over contradicted itself and I acted on the wrong half.** Last tick's line 17 said "no `resolve`/`process` tool in my grant, so 4221-4228 WILL reappear"; line 32 of the SAME block said "`process_telegram_report` takes `id`, not `report_id`". Both were mine. I tested instead of assuming: **9/9 succeeded, both planes now empty** (`read_telegram_reports(status="new")` → "Không có báo cáo mới", `list_unresolved_reports()` → `[]`). A self-reported limitation is a hypothesis, not a finding — grep your own carry-over for the tool name before declaring it absent.
- **But the recurrence cause is NOT the tool — it's an orphaned sub-flow, and that is the fix that matters.** `po/flow/telegram-reports.md` (131L) has always contained the correct drain at Step 2f. `grep -rn "telegram-reports" docs/agents/po/ .claude/` returns **exactly one hit: the file's own header comment.** main.md's Dispatch table and Branch-Workflows table both omit it; No-Task Guard:105 names the READ and never the ACK. So the queue drains only when a human or the router explicitly orders it (07-30 by hand, 07-31 by spawn prompt) — never on an unattended cron tick. Inverted producer/consumer gap: **producer correct, route missing.** Folded into `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` (P2→**P1**) as AC-2; the 07-30 tool-doc cause stays AC-1 (re-verified still live: `delete_message` at doc L16/L74, `resolution` absent).
- **Report 4229's "recurring dispatcher double-fire" was already boarded, already CONFIRMED, and deliberately parked pending the exact evidence that just arrived.** `FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE` was downgraded P1→P2 on 07-16 *solely* because "BLAST RADIUS on a slot-bearing tick = **INFERRED, NOT observed**", with AC-3 demanding a forced same-tick re-fire on a slot-bearing tick first. **That probe has now run in production twice in 27h** (slot-3 07-30 21:00Z, slot-4 07-31 00:00Z). Re-escalated P2→**P1**, `plan_only` **cleared** — the gate is discharged by incident, no sandbox needed. *The decisive test declared un-run had already been measured.*
- **Mechanism confirmed at source, all three guards read, none inferred.** Both sessions started inside the 00:00–00:14 band → `leader-lock.md:40` floors both to `BOUNDARY_MINUTE=0` ⇒ **identical key** `cron:cowork:2026-07-31T00:00Z`, released at Step 6 and re-won by the re-fire. Why nothing caught it: (g1) `cowork-slot:<slot_id>` TTL=180s is released right after spawn — `slot-claim.md:12-18` says outright it "does NOT provide cross-tick dedup"; (g2) `isSuppressedByBoundaryDedup` (`cowork-match-slots.js:99-105`) needs `last_fired ≥ boundary`, but slot-4's stamp did not land until **00:22:30Z**, *after both spawns* — structurally blind for the whole job duration; (g3) the published-marker lives at `stage-log-notify.md` step 5d-1, the **last** stage, claimed 00:15:49Z — it correctly blocked the duplicate POST but by construction cannot block duplicate WORK (~9–20 min burned twice).
- **Ruled out the 8 sibling cowork rows at source, not by title.** `FIX-GUARANTEED-SLOT-DUAL-PLANE-DOUBLE-FIRE` is scoped to `guaranteed:true`; **all four bctc-analyst slots are `guaranteed:false`** (cowork-schedule.json). The two router rows need a router in the loop; this was an unattended 00:00Z cron. That check is the whole reason this became a re-escalation instead of a 9th near-duplicate row.
- **Flagged a landmine inside the row's own prescribed fix before dispatching it.** Its "CHEAPEST CORRECT FIX" says compare `pressure_state.tick_id == nominal_tick`. Those formats differ: `docs/data/pressure-state.json` `tick_id` is **second**-precision (`2026-07-31T00:45:00Z`), `leader-lock.md:40` builds TICK at **minute** precision (`2026-07-31T00:00Z`). Implemented literally the comparison is **always false** — the tombstone would suppress nothing and the fix would ship green. AC now requires normalising one side *and* a positive-control assertion.

## Carry-over

- **4221-4229 are CLOSED — do not re-triage, they will not reappear.** Dispositions: 4221/4223 `wontfix` (Polymarket retirement live, `enabled:false` verified at `mcp.config.json:96`), 4222/4228 `fixed` (314e70718 @00:34Z), 4224/4226 `duplicate`, 4225 `fixed`, 4227 `fixed` (e370f5f51 @00:45Z), 4229 `monitoring`. **Both alerts predated their fixes** — check commit time vs `created_at` before calling a post-fix alert a regression.
- **Call `process_telegram_report` EVERY tick from now on** — params `id` (not `report_id`), `resolution` ∈ `none|fixed|wontfix|duplicate|monitoring`, `delete_telegram_message`. Until AC-2 lands, the route is missing and *I* am the only thing draining this queue.
- **Do not fire the 9 acks in one parallel burst** — 6/9 died on `dial vn-market: EOF` this tick. Serial retry cleared all 6, no partial writes. Transient transport, not a tool defect.
- **42 manual-dispatch candidates remain** (43 found, stamped `UC-ASL-P6` → BATCH). ONE per invocation by design — do not batch-drain, do not re-read the remainder as a new finding.
- **`FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE` is now P1 + dispatchable (`plan_only:false`, `next_agent:ba`).** Do NOT let anyone re-park it for a sandbox probe — production supplied it twice; the row carries both incidents.
- **A tick that dies before Step 6 must still re-run** — that is the tombstone fix's stated CAVEAT, not a bug. Any implementation that suppresses it is wrong.
- **POLYMARKET will re-strand at WF-2 unless PO stamps it when it becomes `.head`** — `supervised:true`, no `po_goahead_*`, in REVIEW/qa. `should_hold=false` this tick (head=`FU-MACRO-SNAPSHOT-TIER-WORSTOF`), so the pre-check was a legitimate no-op, not a skip.
- **The kinh-dich 503 message is a defect amplifier** — `price_score.go:32-41` collapses "fetch errored" and "genuinely <7 points" into one `nil`. Until split, every upstream outage arrives mislabelled as a data gap.
- **Verify through the runtime, but confirm the path exists first.** Live DB is `data/live/`, **not** `data/` (host `data/market.db` is an 8MB stale decoy vs the live 401MB).
- **`CLEAN-NOTEBOOK-AC2A-...` is still the acceptance canary for the manual-dispatch producer.** Do not hand-dispatch it; that destroys the test.
- **Do not widen the DRS allowlist to admit agent-father as a shortcut** — ratified blast-radius control; `feedback_gate_widening_recommendation_requires_actuator_dry_run`.
- **Rotation ≠ reachability** — `FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` cannot make a lane reachable on a busy tick. Ruled twice (STEP po-S58). Orthogonal.
- **7 rows still starved on `DONE`-not-`DONE_VERIFIED`** — do not clear by hand; that erases the evidence for `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`.
- **Never eyeball `next_agent` to judge dispatchability** — run the predicate, print every sub-gate.
- **`signal_queue` can never self-prune** — 131/200, all `triaged`/`RETRACTED`; `orch-cold-evict.sh TERMINAL_SIGNAL_STATUSES` contains neither token.
- **`RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.** Ten PO ticks, zero decision change.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog.** Do not "catch the drain up".
- **TNB handoff is stale-but-ACKed** (`docs/handoffs/tnb-audit-latest.md`, ACK 2026-07-28T22:55Z, unchanged). Step 0-TNB is a legitimate no-op until rewritten — not a missed read.
- **Sprint journal `-po.md` is CAP-REACHED (625L); write to `-po-2.md`** (now 164L/23.5KB, healthy).
