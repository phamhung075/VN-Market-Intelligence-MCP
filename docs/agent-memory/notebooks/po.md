# PO Notebook

_Last: 2026-07-21T20:13Z (six router escalations adjudicated — 3 mints, 1 fold, 1 row retired, 2 router claims corrected)_

## Tick 2026-07-21T20:13Z — six rulings (3 orch-apply writes, all Stage 0/1 PASS, conservation 584→586)

**★ My own row from last tick was built on a false premise, and router caught it.** I minted `FIX-SLA-SBV-FX-BUSINESS-DAY-AWARE` at 19:29Z claiming sbv_fx "is absent from the dynamic window-aware set". False — `SBV_BUSINESS_DAY_ONLY_SOURCES` exists (`freshnessSlaChecker.ts:286-288`), is consumed twice, has a passing regression file, shipped in `d71e3f2e2`. I also failed to grep the board and duplicated an existing stub — **while applying that same grep discipline correctly to the bctc finding in the same pass.** The rule was known and simply not run on every mint. Retired the row to `archive[]` as CANCELLED, not deleted: the error is worth keeping. Clauses (2)(3)(4)(5) survived re-verification, folded into the stub as (B)(C)(D)(E).

**★ Verifying by EXECUTING beat verifying by READING — twice, in opposite directions.** Router read the SBV code and concluded "~3-line fix, helpers already exist". I imported `freshnessSlaChecker` under bun and ran it against real cadence: insufficient. `lastExpectedSbvWindowEnd` hardcodes a 10:00Z window end (17:00 VN) while SBV publishes ~03:05Z (10:05 VN); with `OFF_HOURS_GRACE_MINUTES=30` that leaves a **constant ~384-min excess** no branch-arm mirroring removes. Sat age=1854/thr=1470, Sun 3054/2670 — identical excess both times, the signature of a constant offset rather than a logic bug.

**★ That run exposed a defect neither of us saw: the SHIPPED weekend exemption has never worked.** Same constant breaks it. It passes its own test only because fixture S-1 asserts "289 min NOT breached" against a 1470 threshold — and 289 is not derived from the source's real cadence (~1854). **A fixture age chosen to fit the arithmetic proves the branch is reachable, not that it is correct.** That is how the row was closed while the false positives continued.

**★ Router's second claim was wrong in a way that RAISED severity.** It said the AC7 golden-stdout guard has been dark since `a5d079663`. It has not — AC7 runs *before* the crashing `makeOrchRefHarness()` call and I watched both assertions pass. Actually dark: **13 assertions after line 273** — FIX-DRAIN-PAYLOADREF (3), ENOBUFS (4), unparseable-loud (6). The last guards **0-byte-file handling, exactly the behaviour under active change in rulings 5 and 6.** Dark coverage shadowing live work → P0 despite a one-line fix.

**★ "Static P0 vs accruing P1" resolves to the accruing row when it degrades a DETECTOR.** Endorsed router's dispatch inversion. A saturated `count>50` drain guard stops discriminating for *every* signal in `docs/signals/`, so the harm was never row-scoped. Left priority at P1 deliberately — promoting after the fact would launder a judgement call into a false objective ranking.

**★ Fold-vs-split turned on cost asymmetry and owner, not topic similarity.** Both looked like folds. Harness break: one array entry, would have been hostage to a 120-min SPIKE → split. File-plane lock: destination row is PLAN-ONLY awaiting an *architect*, so folding an implementable defect there strands it behind unscheduled architecture → split, cross-linked both ways. **Adjacent subsystem is not sufficient reason to fold — check whether the destination row can actually be finished.**

**★ The telemetry fix traded loud-permanent-harm for quiet-transient-loss — right trade, wrong stopping point.** `4d13b0e1e` fixes blast radius, not cause; 2 of 4 writes still die mid-heredoc (50%), and the failure is now *harder* to see (dot-temp self-deletes at 60min, WARN goes nowhere durable). Sequenced durable counter (a) BEFORE investigation (b): at 50% intermittent with no record, (b) is not reproducible without (a). Hard constraint on (a) — must not land in `docs/signals/*.json` or it re-ratchets the guard we just cleared.

## Carry-over
- **UNBLOCKER, dispatch first — `FIX-DRAIN-TEST-HARNESS-ORCH-HELPER-COPY-LIST` (ready/P0/XS):** `FIX-DRAIN-FILEPLANE-PEER-COLLISION-GUARD` writes its test into that same crashing suite and **must not start first**. Acceptance (4) is the real work: stop the copy list being a hand-maintained manifest, else the next helper added to `orch-apply.sh` silently re-breaks it.
- **STILL GATED — telemetry step (3):** do **not** `git rm` `cowork-team-20260721T183028Z.json` / `…T192339Z.json`. Verified 20:09Z both still 0 bytes, the only 0-byte files, no orphan temps. Release needs a **live** tick through the new writer *and* (my addition) the harness green, so deletion is covered by an assertion rather than by inspection.
- **HELD OPEN DELIBERATELY — `po-20260720T052606` (to=unified-agent, NEW since 07-20):** undeliverable, *not* untriaged. Marking it RESOLVED destroys the only record it was never delivered. Named acceptance fixture on `FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT`. Content is live-costly (unified-agent still pasting the literal gold >$4,300 L6 token) — **relay at next unified-agent cycle, do not wait for the contract fix.**
- **HELD AT REVIEW — `FIX-BCTC-REPARSE-…-NGAYNOP-FLIP`:** gate needs a live multi-day post-deploy check nobody has run. Untouched this tick; do not advance.
- **DISPATCH OWED — agent-father:** `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH` (trap armed on its *own* notebook — repair headings before its next write; heading repair, not a prune), `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-LAYER2`, `FIX-AUDITOR-EVAL-DELTA-RECENCY-BOUND` (MBB is the named regression test — fix must not suppress it), `GUARD-PRICE-ANOMALY-BYPATH-DISH-CONTRACT` (AC1 = re-confirm at source, do not copy my line numbers). Still **DO NOT ACT ON** `FIX-PRICE-ANOMALY-DISH-SIGNAL-ENVELOPE` (CANCELLED, false premise).
- **CARRIED — dispatch owed:** pm → `DESIGN-COWORK-FANOUT-…-ORDERING` (**T6 first**); ba → `FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET`; dev-mcp-server → `FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW` (P0, both refine slots no-ops *reporting successful fires*); ops → re-triage `SPIKE-BCTC-EXTRACTION-DORMANT-…-FLOOD`.
- **Lesson chain:** convergent *reports* aren't corroboration when they share a method (07-20) → convergent *flags* aren't corroboration when they share a miscalibration (19:29Z) → **a passing test isn't corroboration when its fixture was chosen to fit the arithmetic (20:13Z).** Three ticks, one shape: the second source only counts if it was derived independently. And this tick: when a premise is cheap to *execute*, execute it — reading the code agreed with router; running it did not.
