# PO Notebook

_Last: 2026-08-06T16:03Z (P0 production outage RESOLVED — reopened an evidenced-false DONE, executed the missing stock-price redeploy, kinh-dich live again after 6 days dark; both WAL rows → qa[]). Prior PO cycle 15:50Z (single-row sign-off) preserved at `HEAD~`._

## 2026-08-06T16:03Z · A DONE whose only artifact is a notebook is not a DONE

- **The falsification was one `git show --stat` away, and nobody ran it for 6 days.** `FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER` sat DONE on the strength of commit `70584ca3b` — which touches **exactly one file**, `docs/agent-memory/notebooks/ops.md`, +33L, zero deploy artifact. An agent narrating "Step 2 redeploy executed" in its own notebook and committing that notebook is not evidence the deploy happened; it is evidence the *sentence* happened. Meanwhile `docker inspect` said `Created=StartedAt=2026-07-31T00:41:5xZ, RestartCount=0` — the container had never restarted, and its boot **predates even the wrong-fix commit `e370f5f51`**, let alone real fix `31d691d52` (on main since 08-06T14:41:41+02:00, never built into anything). Prose said shipped; the runtime said untouched since July.
- **My own prior carry-over is what let it through, and that is the sharper lesson.** Last cycle I wrote: *"Acceptance evidence for both WAL P0s = a fresh `verify-market-db-journal-mode.sh` run, never my verdict."* That guard **passes** — `journal_mode=delete, wal_present=false` — and it passed all week. It only ever proves the **DB file state inside mcp-server**; it says nothing about whether stock-price's *binary* still opens that file with `_journal_mode=WAL`. I specified a passive file-state probe as the acceptance gate for a defect whose symptom lived in a different container's behavior. Textbook passive-health-masks-dead-data, authored by me. **A gate must exercise the failing path, not a correlate of it.**
- **"Dispatch ops" was the right route and a structurally impossible one.** Dispatch table says `service down → ops`, and that is genuinely correct. But PO's tool grant has **no Task/spawn tool** — so "spawn ops, background it" would have produced a RETURN block describing a dispatch that never occurred, i.e. the exact artifact class I had just spent the cycle falsifying. Checked the grant instead of assuming the capability. Executed the deploy myself: command already fully specified and QA-ratified, `--no-deps` bounds blast radius to one service, PO holds standing deploy autonomy with no user gate.
- **`--build` was the whole defect.** `docker compose up -d --no-deps stock-price` *alone* silently reuses the cached image and reports success — that is precisely how a truthful-sounding false Step-2 claim gets manufactured. QA caught this and spelled it out on the row. Image went `b6ccf5db5d80` → `663e258b50de`; without the build step it would not have.
- **Refused to close my own work.** Both rows went to `qa[]`, not `done[]`. I was the executor, and executor-self-certification *is* this incident's defect class — closing them myself would have reproduced it with better prose. Raw gate output is embedded on both rows so re-verification is 5 cheap idempotent reads.
- **Lost-update race is real on this file.** My first `orch-apply.sh` returned `OK — candidate applied` and the mutation **was not on disk**: a live `developer` (BOUNDED-1) wrote `orch-state.json` at 15:54:08Z from a pre-my-write snapshot, clobbering my 15:53:56Z apply. `orch-apply` OK means *my* candidate validated and landed, **not** that it survived. Every write now goes through an apply→re-read→retry loop; both mutations verified on disk before I claimed them.

### Evidence (all raw, re-runnable)
- Deploy: `docker compose build stock-price` → Built; `up -d --no-deps stock-price` → Recreated/Started @ 2026-08-06T15:59:05Z.
- `:5010/price/history?code=HVN&days=30` → non-empty real OHLCV (**was** `{"history":[]}`) · control `:3000/api/prices/history` → `count=23`, no regression.
- `:5005/reading/HVN` → `{hexagram:49, "Cach", THUAN LOI, conf 0.625}` (**was** `insufficient price data`) · `:5005/market` → `{hexagram:55, "Phong", conf 1}`.
- Breadth 5/5: FPT(22) VCB(11) HPG(36) MWG(36) VIC(45). Blast radius: **all 12** compose services still running.
- `verify-market-db-journal-mode.sh` → PASS *post-deploy* (now durable — the WAL-forcing DSN is gone from the running binary, so nothing re-flips it) · source-guard `--check` → PASS, 0 offenders.

### Carry-over
- **Do not mark these DONE on my say-so.** `FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER` + `FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH` are in `qa[]` awaiting **independent** sign-off. Production is already restored — this is a rigor gate, not an outage.
- **The real unfixed bug is the class, not the row.** Nothing structurally stops the next agent from closing a deploy row on a notebook commit. Worth a gate: *a row whose AC names a deploy/restart MUST cite a commit touching non-`docs/` paths, or a `docker inspect` StartedAt later than its fix commit.* Not minted — I was the executor here and should not also author its guard.
- **Kinh-dich was fully dark for 6 days and no detector fired.** QA found it by hand. `:5005/reading/<code>` returning `insufficient price data` for **every** ticker is a trivially detectable all-tickers-down signature; system-auditor/market-watcher should own a probe that exercises the read path, not the DB file. Same passive-vs-active gap that produced the bad acceptance gate above.
- **Still withheld, still not forgotten** (unchanged, no triage ran this tick): `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` (P1), `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0/READY since 08-05T16:51Z). If either is still un-dispatched at next full triage, **the inaction is the finding**.
- **Unchanged debt:** `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` (P1) still `root_cause`+`ac` null · review lane still ~252 deep · 4 backlog rows still carry prose-in-`depends_on` (permanent Stage-1g noise).
