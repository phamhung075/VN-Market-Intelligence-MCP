# PO Notebook

_Last: 2026-08-01T06:23Z (batched auditor findings `auditor-findings-20260801` — 3 findings, 0 mints, 4 rows updated). Journal: `triage-20260801T0608Z-po.md` STEP po-S126…S130._

## This cycle

- **All three findings resolved by attaching to existing rows. Zero mints.** The brief invited a mint on finding 1; declined. The attach that mattered was an *amendment*, not an addition.
- **pdf-extractor: the row's own `scope_out` was the blocker, not missing evidence.** `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` (backlog/P1/architect) ordered the architect to discard the accumulation framing — "REFUTED by direct sawtooth observation … must not be carried forward". A 47-page job completed cleanly at `06:09:53.913Z`; across **54 samples / 12min25s afterwards, tesseract=0, PID1 `VmRSS` byte-identical at 2521008 kB** (96.2% of cap) — the sawtooth's down-stroke never happened. Withdrew the prohibition, kept the 07-28 evidence, added AC5–AC7.
- **The brief's own control column was wrong, and it inverted the diagnosis.** It reported the 53%→82% climb at "0 extracts/30m" (idle leak). `docker logs` since restart: **52 `POST /extract`, 51 of them inside 03:27:22–03:35:38Z**. Not idle. So it is neither an idle leak nor a released sawtooth — memory is acquired per job and **retained across job boundaries**, raising the floor.
- **Failure mode is `ExitCode=0` / `OOMKilled=false`.** Nothing in the OOM plane will ever witness this. AC7 explicitly forbids `OOMKilled` as a success signal — an AC keyed on it passes while the service dies.
- **Finding 2's mechanism was falsified by the table it rests on.** The claim: clean exits wrote clean-shutdown sentinels, so `crashRestarts` stayed 0 and A-21 "passed correctly by its own definition". `cron_job_runs`: **12 `mcpServerStartup` rows 11:36–12:40 on 07-31, ZERO `mcpServerCleanShutdown` in 11:00–13:59** (only sentinel that day = 14:44:23). By A-21's own algorithm all 11 gaps have `cs.n==0` → ~11 crashes vs threshold 2 → it **should have WARNed**. A-21's logic was never the cause. Attached to `OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN` (P1/dev-mcp-server), which owns unclean churn.
- **The real A-21 gap is the transport, and it belongs on the row about to rebuild it.** `tier1-probe.md:115-116` runs `docker exec` **inside mcp-server** to measure mcp-server's restarts, and `:148-149` maps probe failure to *"skip, NOT an infra finding"* — failure is positively correlated with the condition being detected. `FIX-A21-PREDBOUND-2` prescribes keeping `docker exec`, so the caveat went there with the verified host-side alternative. Flagged the TOOL-UNAVAILABLE branch as **inferred, not observed** (auditor notebook is overwrite-per-cycle; the 07-31 midday entry is gone).
- **Finding 3 was a denominator error and an inverted direction.** Measured: space-bound-only = **2**, T-bound-only = **0** — false INCLUSION, never exclusion, exactly as the row's own title already says. The cited "12 of 116 / 15 of 119 / 20 of 124" divides by the whole partially-pruned table back to **2026-05-18**. `julianday` ground truth = 27; the live space-separated 24h predicate also returns **27** — currently **exact**. Recorded the falsification; **left P1 unchanged** — the original 60-minute +61% evidence is untouched.
- **Checked my own strand hypothesis before reporting it, and it was wrong.** `FIX-MARKET-MESSAGES-TIMESTAMP-FORMAT` shows `next_agent=null` on the board — the classic undispatchable-P1 shape. `effective_next_agent` falls back to `backlog-detail.json`, which supplies `architect`: `drs_candidate=true, drs_allowed=true`. Routable. Would have shipped a false finding on pattern-match alone.
- **Declined to re-stamp the sweep's top candidate.** 38 manual-dispatch candidates; top is `TE-T02`, already flagged `01:58:29Z` and re-admitted only because that stamp aged out undispatched. Re-stamping from an invocation with no dispatch authority hides it another 4h — the exact failure the staleness window cures.

## Carry-over

- **When a brief supplies a control variable, verify the control, not just the measurement.** The pdf-extractor trajectory was numerically accurate at every sample; the single wrong cell was the "0 extracts/30m" column — and that cell selected the wrong mechanism (idle leak vs cross-boundary retention), which would have sent the architect after a leak in an idle event loop. **The control is where a brief's conclusion is manufactured; read the log the control claims to summarise.**
- **A "known false-positive fix that created a false negative" story is seductive and testable — test it.** Finding 2 arrived pre-explained: A-21 re-modelled crash-only, clean exits write sentinels, so it correctly passed. Coherent, cites the right file, matches a real prior fix. One query killed it: **there were no sentinels**. The trade-off narrative was retro-fitted onto an outcome nobody had checked. **When a finding explains why a detector was right to stay silent, query the detector's input table before accepting it** — otherwise the coverage gap gets filed in the wrong plane and the real one stays open.
- **Rows carry instructions that outlive their evidence.** `scope_out` is written to stop the *last* wrong turn and becomes a standing order forbidding the *next* right one. When attaching evidence that contradicts a row, amending its prohibitions is part of the attach — otherwise the evidence lands in a row that tells its owner to ignore it.
- **A denominator is a claim too.** Three tiers independently reported "N of TOTAL" and all three read a shrinking ratio as a defect; none checked what TOTAL was. Agreement across tiers replicated the arithmetic, not the premise. **Independent confirmation of a ratio is worthless if every tier inherits the same denominator.**
- **Board saturation unchanged and binding.** backlog **352** / review **257** / ready 51 / done 11 / in_progress 0 / qa **0**. 88 P1 in backlog; 214 review rows carry `next_agent: qa` against an empty qa lane — the drain, not priority, is the live constraint.
- **Still owed (ageing):** 38 manual-dispatch candidates unswept this tick; 25 P1 backlog rows resolve to an empty `next_agent` even after detail fallback (unverified whether the generic `developer` fallback makes them pickable — worth one query next tick, do not assume either way).
