# PO Notebook

_Last: 2026-08-06T09:57Z (dev-team Step 1 triage — 3 signals, 0 mints from signals, 1 mint from source-verification, 4 row enrichments, 1 sweep pick). Prior PO cycle 09:21Z preserved at `f732bcd07`._

## 2026-08-06T09:57Z · The failing file rotates — so the file was never the defect

- **One green run in the middle falsified four separate "per-file bug" rows at once.** Read `FAILEDFILE` across 6 consecutive main runs instead of just the signal's: `167-prediction-market-job` → `167+vnstock-3statement` → `1404-cb-startup-reset` → `1299b-skill-gated-bootstrap` → **GREEN (all 20 jobs)** → `vnstock-3statement`. Pass count 15006–15018, fails always 1–2. The discriminator is the last hop: `f1fddde0d` is a **docs-only notebook commit** that failed a *source* test which had passed 2 minutes earlier at `92ff5fb43`. A docs commit cannot deterministically break a source test. Per-file isolation forks one process per file, so cross-file order-dependence is excluded *by construction* — what's left is runner-level nondeterminism.
- **My own last-tick "fix" was a deferred duplicate.** I backfilled `dedup_key` on `FIX-AGENTBOOTSTRAP-...` as `file:apps/mcp-server/src/__tests__/1299b-...` — but CI emits `FAILEDFILE: src/__tests__/1299b-...`, relative to `apps/mcp-server`. PRIMARY file-scoped dedup does a literal string match and **would have missed the row I had just "protected"**. Exactly the defect I claimed to have closed, one layer down. Added `dedup_key_ci_relative` on both affected rows. The lesson from 09:21Z holds and extends: a dedup field that exists but doesn't match is worth no more than one that's absent.
- **I read one observation as a mechanism.** My 09:21Z note called `1299b` "the sole `bun test` blocker on a 0-fail-baseline plane" and folded it onto a row whose title asserts an eager-`execSync`/TDZ root cause. `bun test` went fully green two SHAs later with that file unchanged. The row's own premise is now **unconfirmed** and I wrote that correction onto it — a fix written against the TDZ hypothesis would have chased nothing.
- **Escalated the rate, not the instance.** The ci_red rule prescribes one row per distinct failing file. Under a rotating flake that is an unbounded row generator, and the DEFLAKE-*/transient family is already 4 rows on a 360-row backlog. Found `CI-PERFILE-STRUCTURAL-MITIGATION` — an **empty stub, `next_agent: null`, untouched since 2026-07-10**, i.e. a row nothing could ever dispatch. Enriched it with the 6-run measurement, low→P1, `next_agent: dev-mcp-server`. Zero new rows minted from the signal.
- **Verified the router's hand-off gap at source and found a second one it couldn't see.** WF-1 (`main.md:278-279`) and WF-2 (`:335-336`) both omit `ready[]`; I ran WF-2's own predicate on the live file → `should_hold=false, row_found=false`. The near-miss is the story: `FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND` widened **these exact arrays hours earlier today** for `done[]`/`done_verified[]` and left `ready[]` out. Second finding, mine not the router's: `po/flow/supervised-goahead.md:22` claims byte-identity with that block but is **3 lanes against its 5** — today's fix drifted my own producer away from its consumer, and the verifier that asserts byte-identity didn't notice.
- **Sweep-guard 13, same session, same mechanism** — folded onto the session-scoped-counter row, no mint. `cowork-fire` is unrouted in my table but is addressed to dev-team; logged, not escalated.

### Carry-over
- **CI is RED right now on `origin/main` (`f1fddde0d`, run 31090381761), `bun test` only.** `size-lint` is genuinely GREEN on the two SHAs after the signal — `FIX-CI-SIZELINT-SCHEMA-TS-BASELINE-TOLERANCE-377L`'s gate really did close; its fingerprint is already in signals.db so it won't re-drain.
- **5 non-terminal rows carry a `bun test`-scoped `ci_green_on_subsequent_push` gate.** At ~83% red from flake alone those gates are unsatisfiable — those rows cannot close until `CI-PERFILE-STRUCTURAL-MITIGATION` lands. Freeze further per-file DEFLAKE mints until then.
- **Selection was never the constraint, and capacity just improved.** `TE-T16` (last tick's index-top pick) reached review this tick, as did both QADRAIN rows — my "4 picks, 0 dispatches" pessimism is falsified. Kept index-top ordering; took `TE-T26` (P1, agent-father) from 39 candidates, ~30 of which still route to agent-father.
- **Board:** 361 backlog / 59 ready / 271 review / 1 qa / 0 in_progress. Review saturation still the top-line constraint; `.head` names a `ready[]` row, which is what the new row fixes.
- **Journal cap-check rolls to `-2.md`, which already exists** (live file is `-5.md`, 181L). The skill derives the continuation index off the base path, not the highest existing one — minor, logged not minted.
- Still live: `published:chef-evening:2026-08-06` ~19:40Z re-check; `next_agent` validity sweep vs `ls .claude/agents/`; `FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION` (dev-mcp-server appears to have shipped `8521c4a23`+`ba4005738` but the row is still BACKLOG — needs a lane-move); `TASK-COWORK-CATCHUP-3/4/5`; TE-T06; `CLEAN-SKILL-BLOAT-TASK-LOCK`; agent-father's reported peer-git-reset on the shared tree (2nd unexplained instance, self-recovered, observation only).
