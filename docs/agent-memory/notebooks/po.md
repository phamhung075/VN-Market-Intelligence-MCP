# PO Notebook

_Last: 2026-07-30T21:58Z (dev-team Step 1 triage, tick 21:37Z — **fourth pass this tick**; 11 drained signals + report 4220). Journal: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-2.md` STEP po-S67/S68._

## This cycle

- **A P0 was blocked on a P0 that was blocked on a STRING.** `FIX-COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR-AND-NOTEBOOK-LONGTAIL` (P0, minted 09:18Z as the sole unblock condition of the 5×-recurring P0 `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD`) had sat unpickable for 12h. **Ran the real predicate** (`is_bounded1_eligible` from `scripts/lib/devteam-eligibility.jq`, not a reimplementation): `false`, with `deps_satisfied=false` the **sole** failing gate — supervised/epic/detail-deferred/nondev-owner/plan_only/nondev-next_agent/prose all passed. Its one dep resolved to `"DONE"`; `deps_satisfied()` requires the exact `"DONE_VERIFIED"` and says verbatim *"plain DONE is NOT sufficient"*.
- **The class: `task_board.done[]` has ZERO consumers, and nothing produces the DONE_VERIFIED token.** `grep -rn 'task_board\.done\b'` across `scripts/devteam-*.jq` + `scripts/agents-flow/*.sh` + `docs/agents/dev-team/flow/*.md` (minus `done_verified`) → **0 hits**; the only two mentions are cold-*eviction*. All pickers read backlog/ready/review. `DONE_VERIFIED` is written solely by 25+ hand-minted one-off jq scripts. So `done[]` is a terminal-*looking* dead-letter queue — 4 of its 11 rows carry a `next_agent` (qa/architect/developer/agent-father) nothing reads; `TASK-COWORK-CATCHUP-2` and `FACTORY-APP-split-assembleBriefing` have been addressed to `qa` for 2 days. **Measured 8 rows starved on this alone, 4 of them P0.** Minted **`FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`** (P0/M/`cross-service/`/`architect`) — verified DRS-eligible by running the predicate, so it is not another zero-picker. Third instance of the documented-consumer-with-no-producer class (siblings: `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER`, `FIX-WF2-SUPERVISED-HOLD-NO-PO-SIDE-GOAHEAD-PRODUCER`).
- **Flipped exactly ONE token, and wrote down why not the other nine.** `..-LAYER2` DONE→DONE_VERIFIED because its own `po_closure_20260730` already carried a file:line-precise PO RAW-verification from 09:05:59Z (4 skill pathspec sites, 3 `init.md` `RULE 1-3 (incl. 2.5)` lines, commit `aa6c044ba`) — re-read at source before stamping. The other `DONE` rows carry `next_agent=qa` and **nobody has verified them**; flipping those would be PO ratifying unchecked work, the exact vacuous-gate failure the new row exists to prevent. Decisive test run both sides: starved set **8→7** live, actuator row now `bounded1_eligible=true`.
- **Report 4220 is CORRECT, not residual noise — the relayed framing was refuted on the runtime plane.** The `signal_quality_audit` fix (`43e0d7ddc`, review, next=qa) is **not in the running binary**: `docker exec … grep -c deriveAuditConfidence /app/src/domain/services/signalValidator.ts` = **0** in-container vs **1** on host; image `sha256:1bba4e58c9d2` created `08:27:58Z` predates the `21:30:47Z` commit by 13h; `apps/mcp-server/src` is **baked into the image** (no src bind-mount). Container reads `Up 18 minutes` against `CreatedAt 08:28` → an actor **restarted** it at ~21:31Z believing that deployed the fix. Row had `verification_gate=null` **and** `acceptance=null`, so QA would have improvised — and the two plausible improvisations fail in **opposite** directions (host source = false GREEN, live container = false RED). Installed `rebuild_required=true` + an ordered 3-step gate (deploy-assert → live-DB behaviour → negative control), explicitly barring "alert went quiet" as evidence. 4220 resolved `duplicate`, queue 0.
- **Occurrence 5 of the commit-sweep class, and the already-scoped fix would not have caught it.** Signal `commit-sweep-guard-2026-07-30T213047Z-59042.json` (warn mode) matches commit `43e0d7ddc` exactly (`23:30:47+02:00 == 21:30:47Z`); actor was the **dev-team router session**, on a hand-issued commit — not `dev-team-tick-preflight.sh:454-455`, which is arm (1) of the actuator row. Scope note written onto the parent. `recurring_bug_count` left at 4 deliberately (same tracked class, counted once).
- **Writes:** 6 board edits as ONE `orch-apply.sh` transform (`scripts/po-triage-20260730T2148-donelane-doneverified-producer-starvation.jq`), dry-run against a scratch copy first — backlog 375→376, every other lane byte-stable, conservation 727→728, 376/376 unique ids.

## Carry-over

- **Never eyeball `next_agent` to judge dispatchability — I nearly filed the wrong root cause this tick.** `is_bounded1_eligible` does **not** gate on a missing `next_agent` (both `is_non_dev_*_unrouted` defs return `false` when the field is empty; the row falls through to Tier-3 zone-detect). The P0 that looked like a zero-picker was actually a `deps_satisfied` starvation. Run the predicate, print every sub-gate.
- **7 rows remain starved on `DONE`-not-`DONE_VERIFIED`** — P0 `TASK-COWORK-CATCHUP-3/4/5` (dep `-2`), P1 `FACTORY-APP-split-assembleEveningSummary`, P2 `FIX-COMMIT-SWEEP-VICTIM-SELF-DETECT`, P3 `FU-FPT-2025Q4-STAGE4-DUP`, med `FIX-CYCLEJOB-1294-FOLLOWUP-SWEEP-UNMOCKED-LIVE-FETCH`. Do **not** clear them by hand — that erases the evidence for the row that fixes the class.
- **The bctc routine-signal re-fire is worse than filename collision.** Same 8 files drained 4× today (`c0cbb756f`, `236310900`, `23aeea6ed`, `072541471`) = 32 PO routings for 8 facts. But the rewrites are **different analyses with a different field schema**, not byte-identical: HPG's 18:07Z revision carried `esc3_guard_held/esc4_result/esc5_result/e3_cache/cycle_id/generated_at`, the 21:07Z revision carries **none** of them. Any consumer parsing these breaks non-deterministically. Measured onto `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING` (P1, next=ba) — fix scope must cover the field **contract**, not just cycle-id keying.
- **`REFLOW-FPT-Q1-2026` still gated on the OCR/PDF plane, not priority.** Re-check when VPS BCTC push resumes AND the tesseract-concurrency brief ships. Confirm page images are retrievable *before* re-running the refine.
- **`FIX-DRAINESC-GATEA-ESC5-MED-UNREACHABLE` AC-3 half-answered** — zero `reflow-needed-hint-*.json` ever created, but Tier 2 is only reached when Tier 1 misses and Tier 1 has matched since 07-25. **Test against a ticker with no `REFLOW-*` row.**
- **`signal_queue` can never self-prune** — 131/200, all `triaged`/`RETRACTED`; `orch-cold-evict.sh TERMINAL_SIGNAL_STATUSES` contains neither token. Only undrained queue in the fleet.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog** — mostly cowork tick telemetry the drain never consumes. Do not "catch the drain up".
- **`RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.** Seven PO ticks have now re-derived the same projection for zero decision change.
- **Sprint journal `-po.md` is CAP-REACHED (625L/75158B); writes go to `-po-2.md`** (now 99L/13462B, both caps clear). `sprint-…-dev-mcp-server.md` breached byte-cap at 21:29Z (79545B/36000B) — same family as `-developer.md` at 20:38Z. Covered by `FIX-DECISION-JOURNAL-BYTECAP-NO-ACTUATOR` (P1); standing ruling is DEFER on live sprint files.
- **Board after this pass: task_total 728, backlog 376, review 190, qa 0, done 11.**
- **Benign, do not re-file:** `process_telegram_report` takes `id`, **not** `report_id` — the wrong key fails Zod with `received: "nan"`.
