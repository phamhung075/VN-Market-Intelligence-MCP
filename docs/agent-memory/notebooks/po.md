# PO Notebook

_Last: 2026-07-10T17:05Z_

## Tick 2026-07-10T16:37Z — dev-team triage: 21 telegram reports (all dedup'd, NO new data task) + promote the verify-prune SWEEP (SHG convergence-blocker)
pendingSignals[] EMPTY; CI GREEN; WIP=0/idle. Router surfaced 21 unresolved reports + the SHG-2..SHG-5 stale-pickup finding for adjudication.
**Report triage — 3 buckets, ALL already tracked → resolved, NO new mint (★07-04 churn-avoid; dedup board-wide first):**
- **A) 17× [bctcPdfPull] ENRICH 0-rows FAIL-LOUD 2025-Q4** (ACB/BID/D2D/EIB/GAS/GVR/HCM/HSG/MBB/NKG/POW/SSI + new VCI/VHM/VIC/VPB/VRE) — the SAME cohort I folded this AM into `OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE` (now REVIEW, progressed 06:50→12:15Z) + drain sprint `BCTC-REFINE-STALL-RETRIGGER` ACTIVE; structural root `FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION` done_verified (decouples async table-extract from the sync 0-row gate). Per-ticker emission of a converging known cohort — convergence is gated on the reparse REVIEW row, NOT the telegram noise.
- **B) 1× [OHLCV-DEPTH] stall** (thin codes BDI:1/DLC:41/JSH:0/SIS:0/VDC:0) — honest per-code depth gap (VPS lacks history), NOT systemic backfill failure. Tracked `FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS`.
- **C) 3× [BCTC-1345b] low financial confidence** (D2D/CTG 2026-Q1, MBB 2025-Q4, composite 0.00-0.10) — conviction signal correctly SKIPPED by design; OCR/data-quality tracked (`OPS-BCTC-REFINE-REPASS-NONBANK-5T` in_progress, `FIX-BCTC-CTG-BALANCE-SHEET-REFINE`, report-spam meta `FIX-BCTC-1345B-REPORT-BATCH`).
Resolved ALL 21 via bridge `process_telegram_report resolution=monitoring` (status→processed; honest "tracked, convergence pending" — NOT "fixed", reparse not QA-signed). Verified unresolved 21→0. `resolution` is an ENUM {none,fixed,wontfix,duplicate,monitoring} — free-text -32602s (learned this tick).
**BATCH(1) = promote supervised `BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` backlog→ready (next_agent=architect, cross-service, M, P1).** WIP=0 idle capacity = the sanctioned window for an adjudicated-dispatch of a supervised row. **ROOT found (RAW):** `scripts/orch-cold-evict.sh` only evicts from .done/.done_verified/.active_sprints — NEVER scans backlog[], so status-flip-without-lane-move strands terminal rows there: 55 (34 DONE + 12 CANCELLED + 9 DONE_VERIFIED) + 12 mislaned active + ~62 DEFERRED/TODO/HELD = the 129 coherence warns that block the SHG-2/4 hard-fail flip AND the 385-bloat feeding recurring stale BOUNDED-1 picks. Sharpened D0-D5 on the row (D0 RAW-verify each terminal row, D1 evict, D2 relane, D3 enum-normalize, D4 fix cold-evict to scan all lanes, D5 flip orch-validate.mjs:375 warn→hard-fail then close SHG-2..5). **Folded SHG-2/3/4/5** in-place (`folded_into` marker) — did NOT close them: genuine open work behind the landed commits (129 coherence + the gated hard-fail flip). Promote via `scripts/orch-apply.sh` (conservation OK 538=538, backlog 385→384, ready 0→1). **Did NOT commit** — router re-verifies + commits.

## Standing method (survives rotation)
- RAW-verify every signal/relayed claim from source, never payload-trust. Churn-not-product (★07-04): dedup board-wide before minting; recurring symptom on identical inputs → NO dup mint. Detection-recurs ≠ fix-failed.
- Data-coverage/extraction reports → resolve pointing to the tracked reparse/refine row (status→processed stops resurfacing); convergence is gated on that row's REVIEW/QA, not the telegram count. Mint code work only on post-reparse evidence.
- Supervised backlog rows = router/PO/architect-adjudicated dispatch; promote to ready[] only under WIP≤2 with next_agent set. PO ≠ prod code, PO does not spawn — disposition to router.
- Bridge `scripts/agents-flow/mcp-call.sh <tool> <json>` for gateway reads/report-resolution when direct gateway is blind; orch-state writes via `scripts/orch-apply.sh` (never raw). No self-commit path → write file + flag router to commit; never leave work silently uncommitted.
- Never touch `.head`/in_progress owned by a live worker. Report `resolution` is an ENUM (not free text) — any valid call sets status=processed which clears both new + unresolved sets.
