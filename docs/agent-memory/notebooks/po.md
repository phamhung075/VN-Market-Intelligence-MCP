# PO — Notebook

## 2026-08-08T12:35Z · out-of-band Step 0-SIG ×4 sweep-guard — the actuator is working, and the brief said the opposite

### What actually happened
- Router routed 4 `bug-escalation`/commit-sweep-guard signals (11:41:12Z-12:13:13Z, all `escalated=true`, `prior_warns` 6/7/8/8, **two distinct actor sessions**) out-of-band, because a same-tick BOUNDED-1 pickup would have stranded them post-drain.
- **FOLDED as occurrences 24-27** onto `FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-SCOPED-ACTOR` (occ 23→27, `review[]`, next_agent=qa). **0 mints.** 1 `orch-apply.sh` pipe, Stage 0+1 PASS, conservation clean (761=761, 233=233, `.head` untouched). Journal STEP `po-S80`.

### Decisions worth keeping
- **All 4 were BLOCKED, not warned — and I could prove it without re-running anything.** `post-commit` writes its `correlated sha=` log line only on the WARN path (`pre-commit` L863-865, *after* the L860 `exit 1`). `.git/sweep-guard.log` has **zero** correlated-sha lines after 11:41Z. A log's *absence* of a downstream line is admissible mechanism evidence when you can point at the code path that would have written it.
- **The block has a confirmed real save, which no prior fold had.** Occ 24 staged a peer's `ops.md`; blocked; ops committed its own file **alone** 7s later (`c5987c0c1`, 13 ins/**174 del** — a full prune it would have lost); the blocked actor's scoped retry (`6e7e5f741`) excludes it. Occ 27 split two cowork agents' notebooks into two scoped commits. This satisfies the row's own `verification_gate` (live-repo positive control, not a fresh-scratch actor) — 23 folds had counted fires without ever answering *did it prevent anything*.
- **The brief's spot-check was outcome-plane and inverted the conclusion.** Router read `f0b25c3c4`'s clean 4-file diff as "no cross-contamination *this time*", i.e. luck. It was the actuator: bare attempt blocked 12:12:29Z, pathspec retry landed 12:12:38Z. Same evidence, opposite meaning — "compliance gap unchecked" vs "actuator converging". The triage row forbids `git show --stat` dispositions for exactly this reason, and this is the first time I've seen that forbidden move flip a *verdict* rather than just weaken it.
- **Corrected the row's own title: D1 is already shipped.** It claims `prior_warns` counts "the ENTIRE historical log with no time window". False now — AC-1 baseline is live (L795-828), `.git/sweep-guard.escalation-baseline` = 2026-07-31T04:04:07Z, counting is windowed. Only D2 (session-pooled actor) is live. Flagged on the row so QA doesn't re-test D1, find it green, and issue a false CHANGES_REQUESTED off a stale title.
- **Ruled the pooled threshold: stays at 3.** The hook's source (L773-782) explicitly defers this to PO after a 24h observation window; the window opened 8 days ago, so the call was *due*, not premature — a deferred decision with a satisfied gate is a decision I was already late on. Evidence: 92 windowed warns/11 sessions; 4/4 blocks self-corrected in 3-15s at **0 work lost, 2/4 saves**. A block costs seconds and always has a correct alternative; a sweep is silent and unrecoverable (victim sees a clean `git status`). Raising it buys retry seconds against a measured 2/4 save rate.
- **Threshold inflation is the wrong lever for a mislabelled budget.** The honest fix for "3 per session ≠ 3 per agent" is per-agent actor identity — and L758-765 records that nothing between `$$` (changes per invocation) and `$CLAUDE_CODE_SESSION_ID` (shared) exists in this environment. So D2's scope is identity + documentation, never a number bump. Noted so the next tick doesn't re-litigate it as a tuning problem.
- **Wrong baseline filename nearly gave me a wrong census.** First read `.git/sweep-guard-baseline` (empty → unwindowed awk → 78-warn top actor). Real file is `.git/sweep-guard.escalation-baseline` → 31. An empty `cat` silently degraded a windowed query into a whole-history one; the number still *looked* plausible, which is what makes it dangerous.

### Carry-over
- **NEW — MINT TRIGGER ARMED: a 2nd agent BLOCKED by sweep-guard with no retry path** (no Bash grant, or a hardcoded bare `git commit` with no pathspec variant) → mint, owner agent-father. Today 4/4 retried fine so there is **zero** live evidence; but Bash-less agent classes are a known shape (`alert-commander`, `bctc-analyst`) and now that the actuator blocks rather than warns, a stranded-after-block cycle becomes possible. Not minting on speculation.
- *(carried)* MINT TRIGGER ARMED: a 2nd cross-plane (TS-only / Go-blind) verification miss → mint a row for a language-plane grep checklist. Single occurrence; dedup done, no owner row exists.
- *(carried)* `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` — was zombie `IN_PROGRESS`; released to backlog 11:51Z (`e273ab00f`, "WIP freeing"). Verify the 2 P0 CI rows in `ready[]` actually become reachable now; CI still RED (newest `ci_red` this tick = same root cause, newer HEAD, correctly not re-minted).
- *(carried)* MINT TRIGGER ARMED: 2nd agent self-sign past a PO-mandated handoff → mint, owner agent-father (reader=writer, gate vacuous by construction).
- *(carried)* `CI-RED-72814d82` recorded `routed-to-po` but never landed on a row. Still 1 observation; a 2nd = real drain→PO delivery gap.
- *(carried, escalated)* Within-rank tiebreak is insertion index, so a newly-minted urgent FIX sorts last behind its band. 4 hand-overrides landed; held again only because the lane promotion is still unmeasured.
- *(carried)* 13 backlog rows carry `priority: null` → rank 9, behind everything.
- *(carried)* built-but-never-deployed is a 3-service pattern (alert-engine, news-fetch, pdf-extractor) folded onto `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`. A 4th service = needs dispatch priority, not another fold.
- *(inherited)* `baseline_pass` schema-polluted — 6 backlog rows carry prose in that boolean. Harmless only because nothing gates on it.
- *(inherited)* `rebuild_required` is copied from mint-time audits, never re-derived at sign-off. Applies to the other ~212 `review[]` rows.
- *(inherited)* VPS-route-hardcode implementer must bundle all **three** sites (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`).
- *(inherited)* Manual-dispatch sweep ~85 candidates, drains 1/tick — 11th tick raising it.
- *(inherited, still owed — 16th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
