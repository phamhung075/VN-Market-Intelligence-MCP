# PO — Notebook

## 2026-08-23T13:41Z · The chain that built this feature has no deploy step, so "green tests + next_agent=qa" was one hop away from stamping DONE_VERIFIED on software no user can reach

- **Trigger:** router handoff on `TASK-BCTC-INSPECT-UI-FILTERS` (session `669e1d9f`) — dev-mcp-server done, 18 new tests green, tsc clean, row in `review[]`/`next_agent=qa`, developer's own handoff flagging `REBUILD_REQUIRED: true`. **Output: 1 orch-apply write, 2 rows gated (child + parent), 0 mints.** `task_total` 784→784. Journal: STEP `po-S187`.
- **★ I verified the developer's blocker claim instead of forwarding it, and it was true in a worse way than reported.** `curl :3000/api/bctc-inspect` and `:3001/dashboard/bctc-inspect` → both HTTP 200, both **exactly 103876 B**, both **zero** `id="quarter-filter"`; working tree has 3. But the running image `sha256:c8322d2` was built **2026-08-15T08:44Z** and is **8 `apps/mcp-server/src` commits behind main**, not 1. The rebuild is therefore not a one-feature deploy — it ships a week of accumulated mcp-server work, so live verify has to re-assert `/health` toolCount=183 + scheduler=88 alongside the dropdowns. A blocker reported by the agent that is blocked by it is still a hypothesis; the byte-identical 103876 B on both origins is what made it a fact.
- **★ The gate had to be a structured field, because prose gates demonstrably do not hold.** Wrote the reasoning into `status_note`, but the thing that actually stops a premature claim is `next_agent: qa → ops`. Same-day precedent is unambiguous: `feedback_dispatch_gates_blind_to_prose_status_note_autoclaim_contraindicated_row` — a picker auto-claimed a row whose prose said do-NOT-build. Also set `rebuild_required: true` and `po_verify_chain: [ops:rebuild-mcp-server, qa:live-dual-origin-AC1-AC10, close:done_verified]` so the sequence survives a session boundary.
- **★ A qa pass right now would have been structurally false-green, not merely premature.** AC10 names `http://localhost:3001/dashboard/bctc-inspect` — the user's actual entry point. Against a stale image, the only thing qa could verify is source + unit tests, which is exactly the `feedback_janitor_false_green_verify` / `feedback_fence_false_green` shape. **The acceptance criterion was unfalsifiable, and an unfalsifiable AC verified anyway always returns PASS.**
- **★ Root cause is fleet-wide and already filed: `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`, P1, `ready[]`, unclaimed since 2026-08-01 — 22 days.** `REBUILD_REQUIRED: true` is a marker a developer writes that **no lane reads**. The pm-authored chain here (`ba → architect → pm → dev-mcp-server → qa`) contains no deploy node at all. This task is not an exception to that gap; it is the gap firing again, and every image-baked service change in the last 22 days landed the same way. Did not append evidence to that row — it sits at 14205 B against the 12000 B growth-only prose ceiling.
- **The archived sibling was never deployed either, and its done_verified stamp records a source-only check.** `TASK-BCTC-INSPECT-LABEL-FIX` (parent AC9, `buildLabel()` double-quarter) is already `done_verified` → archived (`dde17428d`, pm closeout `57d221baa`) on "direct-commit verify". Its fix has never run in the live container. **One rebuild is the first real deployment for both children** — so AC9's `HUT Q1 2024` label is in qa's live scope too, even though the row that owns it is closed.
- **Held the parent open rather than let it drift to DONE_VERIFIED.** `FEAT-BCTC-INSPECT-QUARTER-TICKER-FILTER` sits in `done[]`/`next_agent=pm` with both children nominally finished — a pm tick could reasonably close it. Wrote a `verify_note` barring the advance until the child clears ops→qa. Ironically the thing currently protecting it is another defect: `done[]` has no `done_verified` producer, which is what `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION` (in progress this tick) exists to fix.

### Carry-over
- **Dispatch is NOT self-executing — I cannot spawn.** The chain is minted on the board but reaches nothing until the router spawns `ops`, then `qa`. `next_agent=ops` on a `review[]` row is off the DRS ratified allowlist ⇒ **router manual dispatch or it sits**, same standing condition as the 11:57Z and 12:48Z BATCHes still open.
- **Exact rebuild command is load-bearing:** `docker compose up -d --build mcp-server` — single service. **Never `down && up`** (`feedback_rebuild_recreate_destroys_peers` — kills the 12 healthy peers, one up 4 weeks). Verify the new image ID **differs from `c8322d2`** before declaring success (`feedback_docker_rebuild_race_pgrep_deadlock`: `pgrep`/health alone false-passes on the old container).
- **PO has full autonomy on this rebuild — no user gate** (`feedback_po_deploy_rebuild_full_autonomy_no_user_gate`). Authorization is given; only execution is delegated.
- **qa's live brief, beyond AC1–AC8:** dual-origin (`:3000` direct + `:3001` proxy), quarter facet = **11** options not 13 (the HUT string-quarter landmine), ticker = **50**, AND-compose, zero-match disabled+selected option, selection-preservation with no refetch — plus the 8-commit blast radius: `/health` toolCount=**183**, scheduler=**88**, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200.
- **Recurrence risk if only this instance is fixed:** re-dispatch `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` (P1, `ready[]`, `next_agent=agent-father`) in the same wave, or the next image-baked change repeats this exactly.
- **Sweep-guard still flags this session** — explicit pathspecs on every commit, never bare `git commit -a`.
