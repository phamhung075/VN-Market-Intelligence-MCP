# PO Notebook

_Last: 2026-07-31T05:00Z (Step 0-SIG, 4 signals). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-3.md` STEP po-S83._

## This cycle

- **Ran the unshipped brief's rule BY HAND and it changed the board.** `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY`'s FILE-scoped `dedup_key` produced **2 dedup hits + 1 new mint** on live data. The shipped SHA-keyed rule would have produced **0 hits** — i.e. either 2 duplicate rows or a blanket amnesty. That is the positive control the brief §7 asks for, except a human still had to remember to run it.
- **Brief ratified at source, not on the relay.** `po_goahead_20260731T045857` stamped on the AMNESTY row, `plan_only` cleared, `next_agent` po→**agent-father**. Row deliberately LEFT in `review[]`: agent-father is **off** the DRS allowlist `[architect,ba,pm,po,agents-architect]`, so `ready[]`+`supervised=true` would manufacture a READY-XOR/DRS-stranded row. Dispatch = PO **BATCH type=UNBLOCK**.
- **The "size-lint 3-way conjunction" note was STALE and was telling devs the wrong thing.** Offender-set diff across 3 runs: 30598372356 (02:12Z) = **8** offenders → 30603458514 (04:10Z) = **1** → 30604839507 (04:42Z) = **2**. At 04:10Z the single offender was `MACRO-VMT`'s own file — that row **could** have turned size-lint green alone while its own `status_note` said it could not. Corrected in-place.
- **Minted 1 row for a brand-new regression the dev-team's own fix caused this tick.** `af272fe1d` (FDA-5, 04:32Z) grew `energyTools.ts` 152→215L with no `size-justification:` header and no baseline entry → `FIX-CI-SIZELINT-MCPSERVER-ENERGYTOOLS-NEW-OFFENDER` (P0, `dev-mcp-server`). Cross-referenced on FDA-5 (review→qa) rather than folded into it.
- **The 6 mcp-server + 1 pdfx size-lint rows are genuinely fixed on the CI plane.** Absent from the offender set at 30603458514/30604839507; host `wc -l` corroborates. `orchStateSchema.ts` clears via a **`size-justification: 839L` header**, not a shrink (839 > its 730 baseline-upper). Baseline file untouched since `22cd084d4` — nobody laundered anything with `--update`. Evidence attached to both rows for qa.
- **`context_bloat_breach` has no row in `triage-signals.md`** → hits the catch-all. Both instances are live-sprint decision journals → **DEFERRED** per standing policy; the `-dev-team-3.md` one was already resolved by dev-team's own rollover. Folded as evidence into `FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE` (ready, P1, agent-father) — no new row.
- **orch-apply: Stage 0+1 PASS, 7 rows stamped, 740→741 tasks, signals 131→131.**

## Carry-over

- **Triage is now converging; DISPATCH is not.** `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` has been **READY + dispatchable since 2026-07-30T04:57Z (~24h), P0 since 02:27Z, zero dispatch attempts**, at `ready[]` idx 49 behind ~19 other P0s, while main CI stayed continuously RED. Its job has **never once been observed green** (0/8+ runs), so the eslint boundaries fence has never executed. Next tick: hand-dispatch it ahead of the P0 queue.
- **size-lint green now needs exactly TWO rows, not three:** `...MACRO-VMT-LIQUIDITY-RESOLVERS-NEW-OFFENDER` (ready, P0) **+** `...MCPSERVER-ENERGYTOOLS-NEW-OFFENDER` (backlog, P0, new). The MCPSERVER-SIX and PDFX rows are out of the offender set — do NOT re-cite the old 3-way AND.
- **Remedies for a size-lint offender are exactly two** (`scripts/audits/size-lint-justification.sh` `cmd_check` + `is_justified_now`): shrink under the baseline-upper, **or** put a literal `size-justification: <N>L` token in the **first 10 lines** with N within ±10% of actual. A package doc comment is not enough. **NEVER `--update`** — it rewrites the whole baseline and launders every live offender.
- **`FIX-CI-SIZELINT-MACRO-VMT-...` still carries `verification_gate=ci_green_on_subsequent_push`** while its (now-cleared) siblings carried `size_lint_file_level_then_ci_green`. Still not widened — gate widening needs an actuator dry-run. With the conjunction down to 2, this matters less, but it will strand in review if the OTHER row lands last.
- **The undrained `docs/signals/ci-red-22bdf63b-20260731044716.json` MUST dedup, not mint.** Both its failing jobs are now owned: `bun.lock` → FRONTEND-ESLINT row, `usecases_vmt_liquidity_resolvers.go` → MACRO-VMT row, `energyTools.ts` → the row minted this tick. Match on `dedup_key`, never on `check_id`/`head_sha`.
- **`gh run view <id> --log-failed` is the only disposition-grade read for a ci_red.** `failing_jobs[].name` carries zero file identity. A ci_red disposition with no file list attached is invalid — this is the 7th consecutive red where the job names were identical and the files were not.
- **Not run this tick** (scope = Step 0-SIG): channel-audit, TNB, push-backstop. `supervised-goahead` WAS run — `should_hold=false` (head=FDA-6). `manual-dispatch-sweep` not run as a sweep, but its §Step 2 rule (additive stamp, never a lane move) governed the AMNESTY routing decision.
