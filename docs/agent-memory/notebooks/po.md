# PO Notebook

_Last: 2026-08-05T18:55Z (Report-Channel queue triage, 2nd pass of dev-team tick 18:07Z — 131 reports → 0, 1 row minted). Prior 2026-08-05T18:30Z (same tick, signal_queue pass — 8 signals, 0 rows)._

## This cycle

- **131 reports, 1 mint. The ratio is the finding, not the throughput.** 130 of 131 mapped onto a board row that already owned both mechanism and remedy. The Report Channel is not a backlog of undone work — it is a **notification stream that nobody was acking**, and every "urgent" prefix in it (`[BCTC]`, `[bctcExtractReconcile]`, `[audit-output-contract]`, `[system-auditor]`) is a detector re-firing against an already-tracked cause.
- **Read the circuit-breaker's own instruction instead of overriding it.** The 3 `RECONCILE CIRCUIT BREAKER TRIPPED` messages say verbatim *"systemic incident, do NOT triage these rows one-by-one"* — and were **already escalated** (`SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD`, REVIEW). The 87-row BCTC block was one cause with 87 notifications, each row already terminal (`enrich_failed`) at the data layer.
- **The sla-monitor CRITICALs were never a live incident — and the tell was in the data, not the age.** Both days emit **byte-identical values at byte-identical wall-clock**: `59min`/`1020min` at 02:00Z on 08-04 *and* 08-05. Real incidents don't reproduce to the minute. 02:00 UTC **is** VN market open, so price = one pre-open tick, foreign_flow = prior session's close. Second tell: they quote `threshold 10min` while the live table says **621min** — the detector's constant no longer matches served config. Verified current (`get_sla_status` = **5 ok / 0 breached**) rather than reasoning from the 40h-old snapshot. Dispositioned `wontfix`, not `fixed` — nothing was repaired.
- **The one mint, verified at source rather than believed.** Report 4411 claimed dev-team "cannot Agent()-spawn". Instead of trusting a self-report, read `.claude/agents/*.md` frontmatter directly: po/developer/architect/ba/pm/qa/agent-father are **all** `Read, Edit, Write, Glob, Grep, Bash` — **zero agents carry `Task`.** `cron-dev-team.md` fires a bare flow-path with no context pin, so the dispatcher promotes + claims a row, cannot spawn, releases the lock, leaves `orch-state.json` uncommitted. **Structural missing grant, not laziness.** Board sweep + `backlog-detail.json` (457 rows) → zero owning rows. Minted `FIX-DEVTEAM-CRON-TICK-NO-SPAWN-CAPABILITY-NO-ROSTER-AGENT-CARRIES-TASK-TOOL` (P1, `cross-service/`, `next_agent=architect`) — and explicitly **forbade** granting `Task` via frontmatter without sign-off, since that widens every agent's spawn surface at once.
- **Emptying the queue is NOT the fix, and I refused to let 131→0 read as one.** Two rows already own the cause and **neither has ever been dispatched**: `FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE` (AC-1, tool doc) and `FIX-PO-MAINFLOW-ORPHANS-TELEGRAM-REPORTS-RESOLVER-SUBFLOW` (P1, BACKLOG since **08-01T04:12Z**, `next_agent=agent-father`, `updated_by` still null). Re-verified both ACs unshipped live: `process_telegram_report.md:16` still says `delete_message` (real: `delete_telegram_message`) and still never mentions `resolution`; `telegram-reports.md` appears in **neither** main.md's Dispatch table nor Branch Workflows — only an incidental prose mention at `main.md:170`. Recurrence **3/3** (179→0 07-30, 9→0 07-31, 131→0 today), each time only because a human/router said so. **Recurring failed fix, not recurring detection** — folded evidence onto both rows, did not mint a third.
- **Asserted on BOTH planes, because single-plane assertion is exactly how the 07-30 fix looked complete while the route stayed dead.** `read_telegram_reports(status="new")` → *"Không có báo cáo mới"* AND `list_unresolved_reports()` → `[]`.
- `delete_success=false` on 45 of 131 is the **documented benign** ~48h Telegram bot-deletion window. DB is authoritative; all 131 are `status=processed`.

### Carry-over
- **The queue WILL refill.** Nothing shipped this tick that stops it — only an operator-triggered drain. Next unattended tick re-accumulates. If a 4th recurrence lands with `FIX-PO-MAINFLOW-ORPHANS-TELEGRAM-REPORTS-RESOLVER-SUBFLOW` still `BACKLOG`/never-updated, **escalate the non-dispatch**, do not re-drain and do not re-file.
- 9 rows left `monitoring` (real + tracked + unfixed), the live ones being: **rag-service still below memory floor and fully unmitigated** (an ops deploy self-report was proven falsified — future timestamp, nonexistent image hash, unchanged container), and **`get_bctc_full` outage day 5** (`FIX-BCTC-FULL-SERVING-EMPTY-NEWEST-PERIOD-HEAD-OF-LINE`, REVIEW). `monitoring` rows auto-resurface via `expire_monitoring_reports` — that is intended, not a leak.
- `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (READY) says "19/19 period-mismatch refusals"; live count is now **39/39, same LATER skew**. Evidence strengthened, hypothesis unchanged — pick it up before the number grows again.
- Newly minted row targets `architect`, which may be **off the DRS ratified allowlist** → zero eligible pickers (known class: `FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION`). Fallback is written into the row itself: retarget `agent-father` for the doc/cron-prompt portion only. Watch that it does not strand — it is the row that unblocks the dispatcher that dispatches everything else.
- Prior carry-over still live: `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` 3rd gutting = failed fix not open fix · `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER` (green CI is symptom, not disease) · 14 `to=po` `signal_queue` rows stuck at `READ` · `.active_sprints` empty while 265 rows sit in `review` — does the review lane have a drain owner at all? · rag-service deploy predicate (no memory-% drop ⇒ not success), never `docker exec` rag-service · `RAG-FTS-BUILD-MEMORY-BOUND` time-gated ~2026-08-12 · `TE-T02` re-flagged-never-dispatched.
