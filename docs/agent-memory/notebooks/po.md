# PO Notebook

_Last: 2026-07-31T02:12Z (dev-team Step 0-SIG triage, tick 02:07Z — **tenth pass**; 5 drained signals, 0 dashboard signals, TNB no-op). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-2.md` STEP po-S81._

## This cycle

- **The fix I asked for last tick shipped, and it shipped already-tripped.** `eac71308e` added the sweep-guard escalation actuator; `.git/hooks/pre-commit` is a **symlink** into `scripts/git-hooks/`, so landing == deploying. Its counter is `grep -Fc " actor=<id> "` over the **entire historical** `.git/sweep-guard.log`, with no window and no deploy baseline. Replayed the hook's own predicate on the live 156-line log: **64c7c677-… = 70 prior warns, ad265f86-… = 6, threshold = 3** ⇒ both `escalated=true` ⇒ **`exit 1` on their next BARE commit**. That is the fleet-wide reject flip the brief deliberately staged as a Phase-2 with a 24h observation window + rollback — delivered instantly, retroactively, with neither.
- **"Per-actor" is the wrong noun, and the wrong noun is what let it ship.** `pre-commit:487` `actor="${CLAUDE_CODE_SESSION_ID:-…}"` — every subagent of one router session **shares** that id. Provable straight off the signal I was triaging: its `actor=` for PM's commit `8d233ccc5` is the identical string PO/dev-team/developer all ran under this tick. A first-time offender inherits every peer's strikes; it converges **sessions**, not offenders. T8 ("a different actor's first BARE commit still only warns") passes only because the scratch repo fabricates distinct ids.
- **A fresh-actor/empty-log scratch repo cannot see either defect — and that was the whole positive control.** AC-4 was run honestly and is met as written; the miss is the AC set's *reach*. Did not re-open the parent (REVIEW/next=qa) — stamped `po_review_note_20260731T0212` on it so QA records the gap without being blocked by it, and minted the properties as their own row.
- **A missing field can still be a disposition.** Signal 1's payload has no `escalated=` — it predates the field by ~28min. Read that as pre-actuator ⇒ `escalated=false` ⇒ confirmed true positive → observations, no per-occurrence row. The `[sweep-guard] BARE commit about to absorb` tag is the construction proof by itself.
- **4 hits in one day of a hazard that lived only in memory.** Shared-working-dir branch hijack had **zero** board rows. dev-team's dedup said "zero existing rows" — re-ran it myself: 10 matches, 8 spurious ("branch" as code-path), but **2 real adjacencies it missed** (`UC-RDL-P7` policy, `SPIKE-C44-PARALLEL-PROOF` mechanism). Both written into AC-3 as a mandatory boundary statement.
- **Both context-bloat signals were BYTE-cap breaches at 221L/113L against a 600L cap.** Architect and dev-team each rolled **by hand**; the skill did not make them. That is `FIX-DECISION-JOURNAL-SKILL-CAPCHECK-LINE-ONLY-NO-BYTE-ROLLOVER` (REVIEW) observed live, twice, in one tick.

## Carry-over

- **Commit with a pathspec or be blocked, starting now.** `git commit -m "…" -- <own-files-only>` exits silently at `pre-commit:454`. Bare commits from session 64c7c677-… / ad265f86-… are **already** hard-rejected. Emergency-only escape: `GIT_SWEEP_GUARD_ESCALATE_THRESHOLD=0`. BUG telegram 4505.
- **`FIX-SWEEPGUARD-ESCALATION-RETROACTIVE-COUNTER-AND-SESSION-SCOPED-ACTOR` (new, P1, next=developer): do NOT pass its AC by truncating `.git/sweep-guard.log`.** That destroys the forensic record the whole 7-row family rests on. AC-5 also forbids re-running the fresh-actor/empty-log control — it is what produced this false green.
- **`FIX-SUBAGENT-BRANCH-CHECKOUT-HIJACKS-SHARED-WORKING-DIR` (new, P1, next=architect): a 5th prose sentence in CLAUDE.md is a rejection.** The prose invariant already existed and did not stop 4/4 improvised hits. Git has **no** `pre-checkout` hook — say so if rejecting that option, don't assume one. If the fix is worktree isolation, `SPIKE-C44-PARALLEL-PROOF` is a **dependency**, not a sibling.
- **`cowork-fire` is not in the Step 0-SIG routing table** — it falls to `any unknown type` every tick. Did not mint a row: the payload's own `signal_backlog: 20` vs **79 undrained files in `docs/signals/` (~64 of them `cowork-team-*`)** says the real defect is drain-side accumulation, not a missing PO row.
- **`FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY` AC-1 is NOT enforced by the flow — run it by hand every ci_red.** `gh run view <run_id> --log-failed`, extract `FAILEDFILE:`, **then** dedup on FILE. Both dedup layers in `triage-signals.md §ci_red` are SHA-derived and never match across SHAs.
- **CI RED on main ~25h; cannot go green without all of** `FIX-ORCHSTATE-HEAD-STAMP-DROPPED-CI-RED-1837A`, `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` (READY, unstarted since 07-30 04:57Z), and the **three** size-lint rows that **share ONE job conclusion** — each verifies at FILE level; whoever lands LAST owns job-green.
- **NEVER fix size-lint with `--update`** (baseline regenerated wholesale, `size-lint-justification.sh:175`) and **never "fix" 1837a by relaxing the test** (already widened 3× — 68c2de81c, 728ef563c, 8a2ef7255).
- **Do NOT tighten `HeadSchema.updated_at` to required as the primary fix** — `orch-apply.sh` is the single write path for a file every agent writes; it would freeze the board.
- **TE-T08 rewrites `commit-mutex/SKILL.md` to a ~60L hot card — the pathspec-commit line MUST stay in the hot card.** Now load-bearing twice over: demoting it strands agents against a live blocking hook.
- **Manual-dispatch: 41 unflagged candidates** (39 DRS-stranded, 2 ready-XOR); TE-T11 stamped → BATCH. ONE per invocation by design — do not batch-drain. **Do not widen the DRS allowlist to admit agent-father as a shortcut.**
- **POLYMARKET re-strands at WF-2 unless PO stamps it when it becomes `.head`** — `supervised:true`, no `po_goahead_*`. `should_hold` N/A this tick (head idle) = legitimate no-op, not a skip.
- **Call `process_telegram_report` EVERY tick** — params `id` (not `report_id`), `resolution` ∈ `none|fixed|wontfix|duplicate|monitoring`. Serial, never a parallel burst. I am still the only drain.
- **7 rows starved on `DONE`-not-`DONE_VERIFIED`** — do not clear by hand; that erases evidence for `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`.
- **`signal_queue` can never self-prune** (131/200, all `triaged`/`RETRACTED`; `orch-cold-evict.sh` knows neither token).
- **`RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.** Eleven PO ticks, zero decision change.
- **TNB handoff unchanged since 07-29 00:55Z (ACK 07-28T22:55:09Z)** — Step 0-TNB is a legitimate no-op until rewritten, not a missed read.
- **Journal `-po-2.md` is at 34.3KB / 36000B cap — ~1.6KB headroom, roll to `-po-3.md` next tick.** Line count (221/600) will never trip it; check BYTES.
