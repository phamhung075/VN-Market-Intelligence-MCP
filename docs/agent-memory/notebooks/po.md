# PO Notebook

_Last: 2026-07-31T00:30Z (dev-team Step 1 triage, tick 00:07Z — **seventh pass**; 8 reports 4221-4228, 0 dashboard signals). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-2.md` STEP po-S72._

## This cycle

- **The "[kinh-dich] HVN 503 insufficient price data" alert was a TOTAL outage wearing a single-ticker costume. Minted P1 `FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH`.** The alert text invites the "thin/new-listing ticker, not actionable" read — that read is wrong. Live DB: **775 HVN rows** (2023-06-27..2026-07-30); the handler's own 30d window returns **22**. `/stock/price/history` returns `{history:[]}` for HVN **and FPT/VNM/VCB**, at days=30/365/3650 — universal and parameter-independent. `/reading/HVN` **and** `/market` (VNINDEX) both 503 → 100% of kinh-dich readings are dead, plus the frontend chart (`client.ts:245`).
- **Root cause reproduced in isolation, not inferred.** `sqlite3 'file:data/live/market.db?mode=ro' 'PRAGMA journal_mode=WAL;'` → `attempt to write a readonly database (8)`, and the SELECT after it never runs; the same SELECT **without** the WAL pragma returns 22. Live `PRAGMA journal_mode` = `delete`. `fetchers.go:303` (+`:240` Tier3) still build `mode=ro&_journal_mode=WAL`, while the two sibling repos in the same package (`foreign_flow_repository.go:33`, `room_event_repository.go:32`) already dropped `mode=ro` **with a comment naming this exact conflict** — a 2-of-4-call-sites partial fix. `//nolint:nilerr` at `:318-320` converts the error into HTTP 200 + `[]`, which is the whole reason a total outage presented as a per-ticker 503.
- **Not a deploy problem — I checked before blaming the stale image.** stock-price's image is 16 days old, but only 3 commits touched `apps/stock-price` since and **none** touch the read path; the defect is in current source, so a rebuild will NOT fix it. Recorded in the row so nobody burns a cycle on ops.
- **I nearly minted a phantom "broken 24h cooldown".** POLYMARKET fired 22:00:04Z and 22:30:03Z — 30min apart against a declared 24h in-module cooldown. The container was **recreated at 22:06:41Z** (`RestartCount 0`), resetting `_lastStalenessAlertAt`. Two observations straddling a restart boundary are not a broken mechanism (`feedback_single_observation_degenerate_case_read_as_broken_mechanism`). No mint.
- **Polymarket 4221/4223 need no action — retirement is LIVE and verified by absence, not by relay.** `8766bedc9` landed 23:06:55Z (both alerts predate it); `mcp.config.json` is **bind-mounted**, so `predictionMarkets.enabled:false` took effect with no rebuild; the guard returns at `predictionMarketJob.ts:443`, before `checkStaleness` at `:547`. **4 poll cycles since (23:00/23:30/00:00/00:30) emitted zero alerts.** Row stays QA's.
- **Deploy-lane row's "RUNTIME-VERIFIED" proof is FALSIFIED — stamped a correction instead of dispatching it.** `FIX-MCP-SERVER-DEPLOY-LANE-STALL-...` grepped `/app/apps/mcp-server/src/...`, a path that **does not exist** in the container; 0 matches came from a missing file (`feedback_empty_read_is_not_evidence_confirm_tool_targets_store`). Real path `/app/src/...` shows **9** occurrences → the pressure-state fix IS deployed, and the image was rebuilt **2026-07-30T22:06:34Z**, not 07-25. `/app/src` is baked in (only docs/data/signals/reports are bind-mounted), so the grep is valid evidence. Its general premise survives via stock-price's 16-day image — re-scope, don't dispatch as written.
- **4224/4226 (`signal_quality_audit` 79,367→79,427min vs 43,200min) are covered — the ~1/hour re-emission is a known, owned consequence.** `FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT` (REVIEW, P2) already states the threshold was widened to 30d and that nothing owns the firing condition, so it "re-emits ~1/hour forever". No mint. 4222/4228 in-flight, 4225 informational.

## Carry-over

- **Do NOT re-triage 4221-4228 as new next tick** — every one is dispositioned above; only 4227 produced a mint. There is no `resolve`/`process` tool in my grant to mark them read, so they WILL reappear as `status=new`. Re-deriving them is the churn risk, not the reports.
- **The kinh-dich 503 message is itself a defect amplifier** — `price_score.go:32-41` collapses "fetch errored" and "genuinely <7 points" into one `nil`. Until that is split, every upstream outage will keep arriving mislabelled as a data gap. Captured as `misleading_alert_note` on the new row; worth its own follow-up if the dev scopes it out.
- **Verify through the runtime, but confirm the path exists first.** Both of this tick's biggest findings turned on it: the deploy-lane row died on a nonexistent container path, and the live DB is `data/live/`, **not** `data/` (host `data/market.db` is an 8MB stale decoy vs the live 401MB — my first HVN query hit the wrong file and the wrong column name).
- **44 manual-dispatch candidates remain** (was 45; stamped the deploy-lane row this tick). ONE per invocation by design — do not batch-drain, do not re-read the remainder as a new finding.
- **POLYMARKET will re-strand at WF-2 unless PO stamps it when it becomes `.head`** — `supervised:true`, no `po_goahead_*`, now in REVIEW/qa. `should_hold=false` this tick (head was the signal-outcomes row), so the pre-check was a legitimate no-op, not a skip.
- **`CLEAN-NOTEBOOK-AC2A-...` is still the acceptance canary for the manual-dispatch producer.** Do not hand-dispatch it; that destroys the test.
- **Do not widen the DRS allowlist to admit agent-father as a shortcut** — ratified blast-radius control; `feedback_gate_widening_recommendation_requires_actuator_dry_run`.
- **Rotation ≠ reachability** — `FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` cannot make a lane reachable on a busy tick. Ruled twice (STEP po-S58). Orthogonal.
- **7 rows still starved on `DONE`-not-`DONE_VERIFIED`** — do not clear by hand; that erases the evidence for `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`.
- **Never eyeball `next_agent` to judge dispatchability** — run the predicate, print every sub-gate.
- **`signal_queue` can never self-prune** — 131/200, all `triaged`/`RETRACTED`; `orch-cold-evict.sh TERMINAL_SIGNAL_STATUSES` contains neither token.
- **`RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.** Ten PO ticks, zero decision change.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog.** Do not "catch the drain up".
- **TNB handoff is stale-but-ACKed** (`docs/handoffs/tnb-audit-latest.md`, ACK 2026-07-28T22:55Z, unchanged since). Step 0-TNB is a legitimate no-op until rewritten — not a missed read.
- **Sprint journal `-po.md` is CAP-REACHED (625L); write to `-po-2.md`** (now 137L/19.7KB, healthy).
- **Benign, do not re-file:** `process_telegram_report` takes `id`, not `report_id`.
