# PO — Notebook

## 2026-08-15T13:06Z · The relay described the brief; the brief said something else.

- **Trigger:** router-dispatched triage of agent-father's 2026-08-15 Keep-cycle (`baf52fb7e`) — 2 escalations it cannot act on itself (no Agent/spawn grant). **Output: 3 `orch-apply.sh` pipes — `AF-SEMBLE-INIT-DEF` occurrence_3 + P3→P2, 1 row minted (`FIX-COWORK-WRITE-BOUNDARY-ENFORCEMENT-BRIEF-ORPHANED-NEVER-IMPLEMENTED` P1, backlog 426→427), `TE-T06` manual-dispatch re-stamp. `.head` idle throughout, never named either row.**
- **★ I WAS HANDED A SUMMARY OF A DOCUMENT AND THE DOCUMENT CONTRADICTED IT ON THREE AXES.** The escalation said the write-boundary brief "§7 names only 3 agents" and that scope must widen 3→6. The brief's actual enforcement scope was **always 7** — §1 names 7, §4.1 authors 7 `write_boundary` blocks, §5 lists 7 frontmatter files. The "3" lives **only** in §7's residual Bash-vector flag, a different set. `bctc-analyst` is not "the sole clean holdout" of scope; §4.1 gives it the **richest** allow-list — it is the sole holdout of the *Bash grant*. And the 3 "newly-granted" agents were scope-in from day one. **A relay that names a section number still has not read the section for you.**
- **★ THE FRAMING I WAS ASKED TO ACT ON WOULD HAVE PRODUCED A BETTER-SCOPED ORPHAN.** Before editing §7 I probed all ten §5 paths. **Zero shipped.** No `write_boundary` key in `system-map.json`, no `agent-write-boundary-guard.mjs`, no `settings.json` registration, no `docs/signals/doc-fix-proposals/`, no `doc-self-heal` Step 0. This was never scope drift on a live spec — it is a **total orphan**, 9 days old. **When asked to refine a spec, check it was implemented before improving its wording.**
- **★ THE ORPHANING MECHANISM IS STRUCTURAL AND WILL RECUR.** Origin row `GUARD-COWORK-NOTEBOOK-AGENTS-SELF-EDIT-FLOW-DOC` carried `plan_only: true`, so delivering the brief **correctly** satisfied it and it closed `DONE` into `archive/2026-08.json`. The brief's own RETURN said *"NEXT: agent-father | implement §4.1-4.4"* — that produced no successor row. Closing a plan-only row on plan delivery is right; letting it close with **no successor** is the defect. A P1 risk was retired on paper while remaining live in fact.
- **★ THE BRIEF WAS UNDELIVERABLE BY THE OWNER IT NAMED.** §5 requires `scripts/agents-flow/*.mjs` and `.claude/settings.json`; `scripts/` is outside agent-father's commit zone. So even had a successor row been minted at agent-father, it would have stalled — a **second, independent** cause. Routed to `pm` to split on commit-zone lines, with §8's atomicity as a hard cross-agent gate: channel first, mechanism never first.
- **★ A RISK DOWNGRADE CAN EXPIRE WITHOUT ANYONE TOUCHING IT.** §7 called the Bash vector "a residual, not the load-bearing gap" partly because it hit a 3-of-7 minority. Live re-derivation from `tools:` lines: **6 of 7** now hold Bash, and `FIX-BCTC-ANALYST-STAGELOG-NOTIFY-NO-BASH` would take it to 7-of-7. The specced guard is `PreToolUse` on `Write|Edit` — `sed -i`/`cp`/heredoc walk straight past it. The minority argument inverted; I promoted the backstop from optional residual to in-scope AC-4.
- **★ THE THIRD RELAY OF A ROW THAT WAS ALREADY DECIDED.** `AF-SEMBLE-INIT-DEF` came back posing "formalize as agent vs document as exempt skill" — a binary **answered 9 days ago**, and the answer was *neither pole* (a named tool-style exception **with** a minimal floor). Re-verified AC-1/AC-2 still unlanded. The one new fact — roster:45 says "N/A skill, not agent" while `.claude/agents/semble-search.md` is a real spawnable haiku stub — **does not reopen it**; the exception class already classifies it, so the roster line is just stale text → AC-4, not a new row. **Bumped P3→P2 on relay economics, not severity:** 3 relays / 9 days prove P3 is functionally "never" under a one-row-per-tick sweep, while the fix is 2 lines in a 19-line file.
- **Carry-over:** (1) When a dispatch quotes a document, open the document — the quote is a claim. (2) Before improving a spec, verify it shipped. (3) `plan_only` rows need a successor-mint gate; closing-on-plan-delivery silently retires live risk. (4) Check the named owner's commit zone covers the file list before routing. (5) A recurring relay is a priority signal even when severity is genuinely low — cost of relay can exceed cost of fix.

## 2026-08-15T12:58Z · The brief's load-bearing link was the one it never showed.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain — `SPIKE-BCTC-RECONCILE-EXHAUSTED-REAL-UUID-SUBSET`, architect's root-cause brief returning for ratification. **Output: 1 `orch-apply.sh` pipe — review[]→done_verified[] (30→29 / 26→27) + 2 rows minted (`FIX-PEK-EXTRACT-SEMAPHORE-CONTENTION-BOUNDED-QUEUE` P0, `FIX-BCTC-VPSINGEST-REQUEUE-NO-RECONCILE-COUNTER-RESET` P1; backlog 424→426). `.head` already idle, never named this row.**
- **★ THE ONE LINK THE BRIEF ASSERTED INSTEAD OF SHOWING WAS THE ONE THE WHOLE CAUSAL CHAIN HANGS ON.** Architect said a 20-wide batch fired with "zero pacing" creates a burst against `Semaphore(1)`. But the reconcile loop **`await`s each trigger** — read literally, that *serializes* the batch and there is no burst at all. The claim only survives because `pekExtractTrigger.ts` scores **HTTP 202 as success** and `routes_pek.py` is `status_code=HTTP_202_ACCEPTED` + `BackgroundTasks`: each await returns in milliseconds while the real work detaches, so **N sequential awaits launch N concurrent extractions.** Neither file is cited in the brief. **Verify the step the author found too obvious to write down — that is where an unsound chain hides.**
- **★ THE EVIDENCE WINDOW WAS GONE AND IT DID NOT MATTER — BECAUSE I HAD STOPPED DEPENDING ON IT.** Architect's proof was 129/183 `SemaphoreContendedError` traces from 08-08..08-13. The container was recreated at 09:47Z today (`RestartCount=0`); 1068 retained lines carry **zero** of them, zero `_run_pek_extract: FAILED`, zero `pek-extract` requests. Third occurrence of this retention gap in one task family. Had my verification rested on re-counting logs I would have had to choose between rubber-stamping and falsely rejecting a correct finding. **Prefer the proof that survives log rotation: mechanism from source beats counts from a mutable window.**
- **★ I MANUFACTURED A FALSE ZERO AND ALMOST BELIEVED IT.** My first count was `timeout 90 docker logs … | grep -c` → `0`. `timeout` **does not exist on this host** — zsh printed `command not found`, grep got empty stdin, and the pipeline reported a confident `0` with exit 0. That number is indistinguishable from a real refutation of the architect. **A pipeline whose head silently dies still returns a well-formed answer; check the wrapper exists before reading the count.** I wrote the trap onto the row so the next reader does not re-derive it.
- **★ ARCHITECT'S "MINOR, PO/PM VISIBILITY ONLY" NOTE WAS THE THING THAT WOULD HAVE MADE THE P0 FIX LOOK BROKEN.** He flagged one row at `reconcile_attempts=11 > cap 8` as "likely a manual trigger". Live DB: **14 rows over cap** ({7:5, 8:64, 9:10, 10:1, 11:3}), and the mechanism is exactly locatable — of the only two writers of `status='pek_triggered'`, `bctcPdfPullJob.ts:445` resets the counter and `bctcVpsIngestHandler.ts:99` **does not**. Arithmetic proves it: the reconcile loop tops out at 8, so >8 is unreachable without a stale-counter requeue. **Impact:** the RECONCILE EXHAUSTED alert's *own* recommended remediation re-drives an at-cap row that then re-exhausts on its **first** pass — so shipping the semaphore fix alone recovers **none** of the ~58 `enrich_failed` rows and reads as "the fix didn't work". Promoted to its own P1 row. **A secondary finding is only minor until you ask whether it blocks the primary fix's recovery path.**
- **★ THE SAME TWO SCHEMA REJECTIONS, A THIRD DAY RUNNING.** `next_agent: null` (Zod `.optional()` ≠ nullable) then missing `verification.raw_probe`. Both of my last two cycles logged this identical pair as a lesson; logging it has now demonstrably **not** prevented it. That is a signal about the *notebook* as a mechanism, not about my memory: a lesson that recurs after being written down needs a **gate**, not a third entry. (The gate did its job again — it forced the DB histogram and the 5 source probes onto the row as replayable evidence instead of leaving them here.)
- **Carry-over:** (1) In any review, find the link the brief **asserts** rather than shows, and prove that one first. (2) Prefer source-derived mechanism over log counts — containers rotate. (3) Never trust a `0` from a pipeline without confirming every command in it exists. (4) Ask of every "minor, out of scope" note whether it blocks the primary fix's **recovery** path. (5) `next_agent` string-not-null + `verification.raw_probe` — 3rd repeat; propose a pre-flight template rather than re-logging it.

