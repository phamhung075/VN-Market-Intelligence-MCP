# PO Notebook

_Last: 2026-07-31T16:05Z (dev-team triage tick). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-3.md` STEP po-S94..po-S97._

## This cycle

- **11 signals + 4 reports → 1 new row.** Not because the inputs were noise: three of them were *already-correct dedups I had to prove*, and the single mint came from **verifying a false alarm**, not from the alarm itself.
- **The false alarm was the finding.** #4236/#4237 ("2 signal_outcomes unresolved >72h, resolver may be stalled") were **stale pre-deploy**: fix `314e70718` landed in git 00:34Z but the image was only rebuilt **14:44Z** — both alerts (12:17Z, 13:17Z) came from a container still running pre-GLOB code. Confirmed at the RUNTIME plane, not the host: deployed bundle *does* carry the filter (`/app/src/scheduler/alerts/signalOutcomeResolutionJob.ts:92`), live GLOB query = 0.
- **…and the same query showed the pipeline is dead.** `MAX(checked_at)=2026-07-26`, 105 rows, **zero created in 5 days**. The guard's predicate is **monotone in row existence** — it can only fire if rows are being made. So post-fix it returns `0 = healthy` **precisely when the producer is deadest**. Minted `FIX-SIGNAL-OUTCOMES-LIVENESS-BLIND-TO-ZERO-PRODUCTION` (P1). Passive-health shape: *excluding the false-positive removed the last thing making noise.*
- **A correct fix can destroy the only signal.** Before `314e70718`, MACRO/MULTI kept the count at 2 and the subsystem stayed *visible by accident*. Excluding them was right — and it blinded the detector. **When you silence a noisy guard, ask what the noise was incidentally proving.**
- **ci_red = clean dedup, and I proved it in the mandated order.** FAILEDFILE read FIRST (`gh run view 30639708394 --log-failed` → 1 file), *then* dedup. `dedup_key` matched the review[] row exactly — and that row's own `origin_signal_id` **is this signal's id**: same signal re-drained. Fix `d1bb1140f` already landed after the red SHA. Nothing to append.
- **sweep-guard `escalated=true` was the actuator WORKING, not a new defect.** Parsed the payload (never `git show --stat`), then read `scripts/git-hooks/pre-commit` at source: L574-577 installs the deploy baseline, L579-582 windows the count to it, L583-588 forces reject. Hand-replayed `.git/sweep-guard.log`: **exactly 3 post-baseline warns** for this actor → true-positive block, offender converged on its first retry. Appended evidence to the review row; **did not mint a 7th sibling** into a family that already has 3 awaiting QA.

## Carry-over

- **Told "escalated=true → open a FIX row"; appended instead.** The table says "**open/append**". With 3 family rows already in review, a 7th sibling is churn. **Read the whole verb before obeying half of it.**
- **QA must be told what shipped as WONTFIX.** The `SESSION-SCOPED-ACTOR` arm was resolved by **rename + docs**, not by making `actor` agent-scoped (`pre-commit:491` is still `$CLAUDE_CODE_SESSION_ID`, pooled across every agent in a session; L612 now says so). Flagged on the row — QA must sign that off deliberately, not assume granularity was delivered.
- **Stamped a manual-dispatch candidate again (TE-T14).** Last tick I *deliberately* skipped, because stamping under a saturated WIP cap manufactures the next stranded row. The strand fix (`05e5823d0`) shipped, so the 4h staleness window now re-admits undispatched rows — the reason to abstain is gone. **39 candidates remain.**
- **Row-supplied fields are not guaranteed.** TE-T14 carried no `files`; I derived it from source (`docs/agents/system-auditor/flow/main.md:51`). Check `files`/`desc` on any swept row before folding it into BATCH — cold-storage detail lookups came back empty for both rows I tried.
- **A `zone: null` row is invisible to dispatch, not merely untagged.** `FIX-ALERT-CASCADE-OUTCOME-DEAD` sat P3 with null zone — dev-team Step 3 rejects a batch entry with no zone, so it could never be picked up regardless of priority. Repaired to `apps/mcp-server/` + P1 on live evidence. **Sweep the board for other null-zone rows next tick.**
- **6 review[] rows still carry `next_agent: po`** (RAG-FTS-BUILD-MEMORY-BOUND, FLEET-PUSH-LAUNCHD-EXCONFIG, TASKCLAIM-OWNER-CLIENT-SESSION-FLEET-DOCS, BCTC-LAYOUT-PUSH-DEADLOCK, COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR, PO-NO-PRODUCER-MANUAL-DISPATCH). Oldest since 07-29. **This is a real PO queue, not residue** — the last one is confirmed residual (`po_blocking_gate: cleared`), the other five are not. Work it next idle tick.
- **`COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR` asks me a live question:** its review_note reproduced (2 scratch-repo tests) that `git commit -- <path>` uses **`--only` semantics = working-tree content of that path, not the staged subset**. So the fleet-mandated pathspec commit does *not* protect against same-file hunk sweep. It asks whether that blocks fleet-wide `GIT_SWEEP_GUARD_MODE=reject`. **Unanswered — I owe this decision.**
- **`gh run view <run_id> --log-failed` remains the only disposition-grade read for a ci_red.** `failing_jobs[].name` carries zero file identity. **NEVER `--update`.**
- **`daily_ohlcv.updated_at` is a MUTATION timestamp, not arrival** — nightly backfill rewrites ~97%. Never diagnose coverage off it.
- **PO's signal routing table still matches 0 of its 16 live types** (`FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`, backlog, `next_agent: agent-father`). Still 100% fall-through to "unknown → skip".
- **Not run this tick:** channel-audit (router relayed `list_unresolved_reports` — re-running burns `new` status). TNB `c119` ACKed 2026-07-28, unchanged, no re-ACK. supervised-goahead: `should_hold=false`.
