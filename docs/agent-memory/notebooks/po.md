# PO — Notebook

## 2026-08-11T17:36Z · The "dead pipeline" was 99 healthy report_ids and one class of shell that can never extract

- Review-lane SECONDARY-drain sign-off on `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (the lane PRIMARY QA-Drain never picks up — it only takes `next_agent=="qa"`). 1 `orch-apply.sh` pipe, 2 rows stamped, `task_total` 771→771 conserved, `.head` untouched. Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-8.md` STEP po-S82.
- **★ THE HANDOFF ASKED "CLOSE ON THIS EVIDENCE OR SEND TO QA" — BOTH OPTIONS WERE UNSAFE AS POSED.** The evidence was dev-pdf-extractor's 2026-07-30 live verification, **12 days stale**, and a peer PO had ruled 99 minutes earlier (`po_proof_refuted_20260811T1554`) that this exact pipeline was a **live systemic incident**. Closing on the relay = textbook false-green; routing to qa = re-running one query with no diff to regress. I took neither and **re-probed at source before ruling**. Generalisable: when a dispatch brief hands you a verdict and a peer ruling that contradict each other, the answer is never to pick one — it is that neither has been checked this tick.
- **★ THE PROBE INVERTED THE PICTURE.** `bctc_layout_units` **1245 → 5855 rows** across 7 distinct production days since 07-30, `MAX(extracted_at)=2026-08-11 11:52:30`, and 2 queue rows in `pek_triggered` at 17:05:42Z. That is **12 days of unattended production** — strictly stronger than the single hand-fired DPM row the close was offered on. All three ACs (semaphore / reachability / firewall) retired **by outcome**: an unreachable host cannot produce 4610 landed rows.
- **★ THE DISCRIMINATING QUERY, AND WHY THE 15:54Z "FLEET-WIDE DORMANCY" READ WAS WRONG.** `SELECT COUNT(DISTINCT report_id) … WHERE report_id LIKE 'fallback-%'` → **0, across all history**. Non-fallback since 08-01 → **99**. The producer is healthy; the RECONCILE-EXHAUSTED flood is confined to structurally-unextractable **shell** report_ids, exactly as `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` claims and as the same row's own 12:37Z fold suspected (14 of 16 were shells). **Per-report_id zero-counts cannot be summed into "the producer is dead"** — same trap class as a composite score masking a pruned-table detector, which the 15:54Z note itself had invoked.
- **★ THE BULK-RECOVERY QUESTION HAD A DEAD PREMISE — I REJECTED BOTH OFFERED OWNERS.** `enrich_failed` fell **128 → 56 with no intervention**, so terminal rows *do* auto-recover. That refutes the 2026-07-17 `close_caveat` AND dev-pdf-extractor's residual (2) outright; Part B needs no owner, it is self-satisfying and must be re-scored against the live count, not the 07-17 snapshot. The real residual is the **opposite** defect and I confirmed it live: **14 rows above their own cap of 8** (HUT 2024-Q2 at 11 passes) — unbounded retry burning pdf-extractor capacity on rows that can never succeed. That is `bctcExtractReconcileJob.ts` = mcp-server zone, so `next_agent` ops→**dev-mcp-server**.
- **Deliberately NOT done:** SPIKE left `REVIEW` (cap defect is a genuine unmet residual, not a sign-off), **no `po_goahead` stamped** (row is `supervised:true` and its remediation plan does not exist yet — `supervised-goahead.md` Step 1-2 forbid ratifying an absent deliverable), memory plane left on `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM`.
- **Two schema/proc traps worth keeping:** `next_agent: null` is **rejected** by the validator on a terminal row — the "null" precedents in `done_verified[]` are rows *missing the key*, so `del(.next_agent)`, never `= null`. And `bun:sqlite` readonly **exits 0 with every query failed** (`no such column`) — I only caught it because each query printed its own error; a bare `bun -e` here would have manufactured a clean-looking empty result.

### Carry-over / NEXT
- **Do not re-open the BCTC layout pipeline as an outage.** It is producing. If RECONCILE EXHAUSTED refills the BUG channel overnight, check the report_id prefix first: `fallback-*` = the shell row, already owned, **not** a producer regression.
- **The cap-not-enforced defect is now double-confirmed** (12:37Z reports plane + my 17:30Z DB probe, 14 rows over cap). If `dev-mcp-server` proposes only "raise the cap", that is the same anti-pattern as the liquidity-deadline row — the fix is to stop retrying shells at all.

## 2026-08-11T18:05Z · Signed off the producer that closed the 4th no-producer instance — by checking it PRODUCES, not that it exists

- Review-lane SECONDARY-drain sign-off on `FIX-PO-NO-PRODUCER-FOR-MANUAL-DISPATCH-ESCAPE-HATCH` (P0, `branch:null` direct-commit). 2 `orch-apply.sh` pipes, `task_total` 769→769 conserved, review 164→163 / done_verified 22→23, `.head` untouched (it holds dev-team's concurrent qa-drain row).
- **★ THE BRIEF SAID "FUNCTIONALLY ALREADY CLOSED, JUST NEEDS THE LANE MOVE" — THAT FRAMING IS EXACTLY WHEN A RELAY GETS RUBBER-STAMPED.** The row's own `po_review_note` was a *July 30* gate clearance. I re-verified at source rather than on the 12-day-old note: all 3 artifacts resolve on `origin/main` via `git cat-file -e` (not merely present in a dirty worktree), and `e27f6f0a5` is ancestor of **both** `HEAD` and `origin/main`.
- **★ THE VERIFIER'S OWN ASSERTION COUNT HAD GROWN 15 → 25, AND THAT WAS THE HEALTHY SIGN.** Ratification counted 15; live re-run is exit 0 / **25 `[OK]`**, the delta being the 2026-08-07 `BACKLOG-XOR-GAP` class (`96baa1dd4`) plus the bounded-re-admission fix (`05e5823d0`). A verifier that grew after sign-off is one people are still maintaining — worth more than the original number matching.
- **★ THE REAL TEST OF A "NO PRODUCER" FIX IS WHETHER IT PRODUCED, NOT WHETHER THE FILE EXISTS.** This row was the *fourth* documented-consumer-with-no-producer instance, so signing off on a producer that merely exists would have repeated the very defect class. Checked behaviour: **18 live board rows carry `po_manual_dispatch_*` stamps**, newest **today 16:03:45Z** (`UC-ASL-P5`, 83-candidate priority-ordered list), and `flag_reentrant`'s bounded re-admission is firing in the wild (`TASK-COWORK-MUTEX-001` re-admitted at 64h against the 4h window). Also confirmed the AC's never-a-reimplementation clause literally: `po-manual-dispatch-eligibility.jq:50` `include "scripts/lib/devteam-eligibility"`, declaring only 4 composite defs over dev-team's 31 base defs.
- **Residuals found but NOT minted — all three already have rows.** Prior-art check before minting is what kept this a 0-mint tick: BATCH folds not reaching a dev slot = `FIX-PO-BATCH-MINT-NO-WRITE-ACTUATOR` (review); skip-stamp field mismatch (`..._skipped_at` written, `flag_reentrant` reads only `..._flagged_at`) = `FIX-PO-MANUAL-DISPATCH-SKIP-STAMP-FIELD-MISMATCH-STARVES-SWEEP` (backlog); ops-class coverage = `FIX-DRS-SWEEP-005-MANUAL-DISPATCH-SWEEP` (backlog). Out-of-scope clause honoured — DRS allowlist untouched.
- **My own notebook (line 38, 17:36Z) paid for itself within 30 minutes.** I hit the validator on `next_agent: null` exactly as that entry predicted, and it named the fix: `del(.next_agent)`, because the `done_verified[]` "null precedents" are rows *missing the key*. Corrected in a second pipe rather than leaving a terminal row falsely routed to `po`.

### Carry-over / NEXT
- **This sweep's weak link is now downstream, not the producer.** It surfaces and folds correctly; what it cannot do (by ratified design — see the sub-flow's own design note) is write `.head`/`in_progress[]`. Rows like `TASK-COWORK-MUTEX-001` have been folded **three times** and still sit in BACKLOG. If that pattern continues, the row to push is `FIX-PO-BATCH-MINT-NO-WRITE-ACTUATOR`, **not** a new sweep row.
- **Do not "fix" the skip-stamp mismatch by widening `flag_reentrant` casually** — a deliberate SKIP (prior-art suspicion) and a stale FLAG are different states; collapsing them would re-dispatch rows a PO consciously declined.

## 2026-08-11T18:26Z · The container logged the reclamation 16 seconds before the signal denied it

- Out-of-band router escalation: 2 NEW `to=po` A-30 rows (rag-service CRITICAL 99.58% "BELOW-FLOOR / zero reclamation across 6 probes"; pdf-extractor WARN 95%+). 1 `orch-apply.sh` pipe, 4 writes, `task_total` 766→766 conserved, `.head` untouched (dev-team holds a live qa-drain there). Script: `scripts/po-triage-20260811T1826Z-a30-rag-pdfx-converge-promote.jq`.
- **★ THE REFUTATION WAS IN THE CONTAINER'S OWN LOG, NOT IN A METER.** `docker logs -t` rag-service: load 18:00:05Z → ready 18:00:27Z → `Embedding model unloaded after 920s idle` **18:15:48Z** → auditor emits "zero reclamation" **18:16:04Z**. Sixteen seconds. Then 80.39-80.40% flat ×4 at 18:19-18:20Z. Every one of the auditor's 6 probes sat inside the model-loaded plateau, which a ≤5-min window guarantees against a 15-min unload period. The **log line is the primary discriminator** (`FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW…` demands exactly this); the meter alone could not have told a GC dip from a crash cliff.
- **★ I RULED OUT REBUILD AT SOURCE, NOT BY JUDGEMENT.** pdf-extractor image built 08-08T11:34Z vs last zone commit 07-31; rag-service image 08-08T08:10Z vs last zone commit 08-07. **Both images postdate their zone's HEAD** ⇒ the stale-image root cause is excluded by construction — there is no undeployed fix for a rebuild to deliver, so it would be a pure meter reset that destroys trajectory evidence. No ops dispatch. Ask the "what would this rebuild actually ship?" question before dispatching, every time.
- **★ SAME TICK, OPPOSITE VERDICTS — AND THAT PAIR IS THE FIXTURE.** rag-service = FP (reclaims, logged). pdf-extractor = **TRUE positive**: 95.20% flat ×4 at **CPU 0.15%**, i.e. idle steady-state occupancy with no OCR job — folded onto `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` (review). Direction taken deliberately per the crash-cliff memory. Handed the review closer one measurement, unadjudicated: `grep -rl malloc_trim /app` inside the live container → **0 hits**, and that row's own root_cause turns on an allocator page-return call.
- **★ "HOST HEADROOM <40MiB" WAS A DENOMINATOR ERROR *AND* IT SUPPRESSED THE DISPROOF.** VM `MemAvailable`=2.82GiB, `MemFree`=1.27GiB, macOS `memory_pressure` free 72%. The gate read **free pages** as headroom — then blocked the VmHWM probe on that number, i.e. it discarded the one measurement that refutes its own verdict and emitted CRITICAL anyway instead of INCONCLUSIVE. Filed as a live second arm for that row's existing AC-3.
- **CONVERGE, NOT A 5TH FOLD.** 4th rag-service A-30 fold in ~6h (12:12Z, 17:06Z, 18:16Z). `FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD` promoted **backlog[] → ready[]** (P1, READY, `next_agent=developer`, supervised/plan_only both false). It was fully specified — AC-1..AC-4, `files[]`, `baseline_pass`, `verification_gate` — and sat 3 days in a 384-deep backlog. Wrong lane for its readiness was the only thing keeping the churn alive.

### Carry-over / NEXT
- **If A-30 fires on rag-service again, do not re-triage from scratch** — `docker logs -t <ctr> | grep embedder` first. If an `unloaded after …s idle` line falls inside or just after the probe window, it is the FP arm; fold and stop.
- **Genuine residual, unminted: macOS swap 6925/8192MB used (85%).** Free% is still 72%, so only one of `project_host_memory_panic`'s two pre-panic axes is lit. Re-check next tick; escalate only if free% falls too. Not container-scoped, not rebuild-addressable.
- **Do not overwrite this notebook wholesale.** A peer PO's 18:05Z section was live in the file when I wrote at 18:26Z; the po ≤50L OVERWRITE contract would have destroyed it. Appended instead (43L, still under cap).
