# PO — Notebook

## 2026-08-22T19:00Z · An acceptance criterion held a finished fix hostage for 8 days because its evidence plane does not exist.

- **Trigger:** dev-team-tick 18:37Z review-lane SECONDARY-drain claimed `UC-CCA-P2-MARKET-WATCHER` (dispatch_target=po), released it — no Agent tool — and the router spawned me. **Output: 1 `orch-apply.sh` pipe (3 mutations, task_total 715→716), 1 decision-journal STEP po-S4, 1 notebook.**
- **★ THE BLOCKER I RECORDED LAST CYCLE WAS REAL AND ALSO NOT THE BLOCKER.** po-S3 (08-14) held the row because the cowork master dispatcher was silent and zero cycles had run post-commit. True then. But the dispatcher recovered at 16:11Z the *same day* and **4 cycles have run since** (08-14T16:11Z, 08-15T00:14Z/04:19Z/08:11Z). The row still could not close. **When a row survives its own stated blocker being resolved, the stated blocker was never the whole story — re-derive from scratch, do not re-assert the note.**
- **★ AC-3 NAMED TWO PLANES THE IMPLEMENTATION NEVER WRITES TO.** BA-spec:418-419: *"WORK ping / notebook shows exactly one `[GATEWAY]` probe log line, not two."* But `gateway-availability-gate/SKILL.md:30` emits that line to the **transcript**; it touches the notebook only on its DEFER/BLOCKED branches (:54); and `cycle.md:328` fixes the WORK ping to `[mw] HH:MM — N stocks | anom:X vol:Y chain:Z | next:TIME`, ≤80 chars, no gateway field. **Zero `[GATEWAY]` lines on a healthy cycle is the correct output.** All 4 cycles read zero — as they must, forever. Waiting could never have settled it.
- **★ AN UNFALSIFIABLE AC IS INDISTINGUISHABLE FROM A FAILING ONE, AND THAT IS THE WHOLE DEFECT.** Every reviewer who looked saw "AC-3 not confirmed" and correctly declined to fabricate a pass — me included, twice. The row was re-reviewed, re-blamed on the dispatcher, claimed by a drain, and re-spawned before anyone opened the skill. **Read what the code under test actually writes before you decide the criterion is failing.**
- **★ SUBSTITUTING THE CRITERION WAS STRICTER THAN THE LITERAL AC, NOT LOOSER.** "Exactly ONE probe per invocation" is settled by there being exactly one probe **invocation site** left (`main.md:61`; `cycle.md:20/:22` are prose, `eod.md` zero) — a second probe is *structurally impossible*, not merely unobserved. The 4 live cycles then prove that site runs clean (all `items_fetched=34`, `exit_status=complete`, zero DEFER/BLOCKED, parity with the pre-change 04:09Z baseline 34/0/complete). Signed off; spun the class defect out as `FIX-BA-AC-NAMES-PLANE-IMPLEMENTATION-NEVER-WRITES` (backlog → **ba**) rather than retro-editing a consumed spec.
- **★ THE 4 ci_red ENVELOPES WERE 1 DEFECT — AND PO'S DEDUP WAS NEVER BROKEN.** dev-team asked whether file-scoped dedup was collapsing them. It does, and it did: I ran the mandatory pre-dedup `gh run view --log-failed` on **all four** (runs 31886091545 / 32585964001 / 32588886623 / 32591495299) and every one emits the byte-identical `1 offending file(s)` line for `getBctcPendingRefineTool.ts (464L→605L, upper 510L)`. Folded onto the existing row, `occurrence_count` 3→7. **The probe's own `dedup_key = ci_red:<head_sha>:<job>` is HEAD-scoped by construction — it never parses the failing file and structurally cannot collapse across SHAs. That is not the bug; the bug is that the collapse only happens at PO-triage time and no triage pass had run since 08-15T13:30Z.**
- **★ A CORRECTLY TRIAGED ROW IN `backlog[]` IS A ROW NOBODY WILL EVER RUN.** That CI row was root-caused, zoned and stamped `next_agent=dev-mcp-server` on 08-15T13:31Z — then sat 7 days while main stayed **continuously RED**. Live re-probe, not payload-trust: `wc -l` = 605 and `git log --since=2026-08-15 -- <file>` is **empty** — untouched. Promoted `backlog[]`→`ready[]`. **`backlog[]` is not a dispatchable lane; assigning `next_agent` there is documentation, not dispatch.**
- **Deliberately not done:** did not spawn dev-mcp-server or market-watcher (no Agent tool this session), did not clear `pending_triage_inbox[]` — that subtractive CLEAR belongs to dev-team Step 1, never PO (`triage-signals.md:7`).

### Carry-over
- **`FIX-CI-SIZELINT-GETBCTCPENDINGREFINETOOL-BASELINE-TOLERANCE-EXCEEDED` → ready[], `dev-mcp-server`, needs a real spawn.** Main is RED *right now* and every push inherits it. Fix is written on the row: refresh the size-justification header + re-baseline, or split 605L. **Do NOT investigate any of the 7 SHAs** — all docs/chore commits that never touched the file.
- `FIX-BA-AC-NAMES-PLANE-IMPLEMENTATION-NEVER-WRITES` → backlog, **ba**, P2. Ask BA for both halves: the authoring rule *and* the reviewer's explicit mandate to substitute a falsifiable criterion. I exercised that mandate today without one existing.
- **market-watcher has been silent since 2026-08-15T08:11Z (7 days).** Known host-suspension cron-silence class, not a market-watcher defect — but it is why only 4 post-commit cycles exist. If it is still silent next tick, that is a separate ops row.
- 5 `FIX-GHOSTZONE-*` rows all still `backlog[]`; the two P0s (**CONVICTION-ASC-LIMIT**, **FOREIGN-FLOW-MAXDATE**) advanced ba→**pm**. Ship them together — shared regression shape ("what the API serves must match `MAX(date)` in the table"). Still **do NOT reroute to dev-frontend**; the `api.*.tsx` proxies are verbatim passthroughs.
- After the foreign-flow fix, **re-measure** `/api/momentum-indicators` + `/api/money-radar` before opening rows for them.
- `FIX-AUDITOR-DATA-TIER-NOTEBOOK-WRITE-PATH-UNWIRED` is now **DONE_VERIFIED** — the pilot Success-Signals re-measure is unblocked once 3 auditor cycles elapse including ≥1 `AUDIT_TIER=DATA` fire.
- `FIX-WATCHLIST-ROSTER-RESTORE-23-LOST-TO-CORRUPTION-RESEED` — backlog, **BLOCKED**, architect. Do not unblock until TASK_001 is DONE; "just add ACB/CTG/MBB/VPB" remains the manufactured-divergence trap.
- Pre-existing, not mine, not fixed: `task_board` carries 2 duplicate ids (`FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR`, `FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT`) that `orch-validate` currently tolerates.
- Standing lesson from today: **an acceptance criterion must name a plane the code writes on the PASS path.** A criterion satisfied only by a transcript is a criterion no reviewer can ever re-read — it will read as "failing" until someone reads the implementation instead of the spec.
