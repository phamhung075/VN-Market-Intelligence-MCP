# PO Notebook

_Last: 2026-08-01T00:54Z (dev-team Step 1 triage, cron tick 00:37Z). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-4.md` STEP po-S108..po-S110._

## This cycle

- **I refuted my own prior tick's hypothesis using the data it had already collected.** Last tick offered, as a hypothesis, that the BCTC period detector mistakes the balance-sheet `Số đầu năm` opening-balance column for the report's period. That column is **31/12 of the PRIOR year** → it predicts a **prior-year** Q4. Five instances (FRT/DXG/DIG/GEX/DBC, all `2024-Q1`) detect **`2024-Q4` — same year**. Falsified on the existing evidence; no probe, no dev cycle spent. The real signature is **detected period never earlier than supplied** = fetch-side substitution, which is the residual `po_corroboration_20260728` already logged as UNVERIFIED on that same row.
- **The BCTC storm's ticker set gave away what it actually was.** 11 of 13 storm tickers are members of the 15-ticker cohort named in `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP`. So tonight was very likely the **remediation sweep of the 07-19 corruption**, refused wholesale by the 07-28 period guard. Two rows, one incident — verifying either alone yields a misleading verdict. Cross-linked both.
- **Escalated QA-drain starvation because it is now load-bearing on live corruption, not because it is slow.** `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP` ("ACTIVE + SPREADING") has sat in review **10.3 days**; its generator still emitted **three `total_assets=0` writes tonight**, one aimed at a row holding a good `total_assets=127684178.83214`. Only an unrelated guard stopped it — and that guard is the 0%-pass-rate one, also stranded in the same lane.
- **The starvation is circular, and I dispatched the narrowest point.** 202 review rows carry `next_agent:qa`; the remedy is *inside that set*. Part 1 (`FIX-DEVTEAM-QADRAIN-HEAD-WRITE-CONDITIONAL`, commit `9fe706fa2` verified real, script-only, S, no depends) has sat ~26h. PO BATCH → dev-team Step 3 is the only dispatch path that doesn't traverse the starved picker.
- **Declined to mint anything.** 23 quarantines + 12 unresolved reports + 40 sweep candidates produced **0 new rows**. Producer-side prior art already exists three ways; a 4th row fragments one defect across four.
- **Both mandatory pre-checks ran.** supervised-goahead: `should_hold=false`, no-op, head left untouched. manual-dispatch sweep: 40 candidates, stamped `TE-T21` — first live exercise of the 07-31 stale-reflag re-admission branch.

## Carry-over

- **Rank ties in the manual-dispatch sweep are decided by array index, which is not a priority signal.** `TE-T21` and `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` were both P1/rank-1; only `idx` 27 vs 42 separated them. Sort order is arbitrary board position — treat a rank tie as a genuine PO judgement call, not a resolved ordering.
- **A `reflag:true` candidate is the sweep's most fragile case, not its least urgent.** It means a prior tick stamped it and dispatch never happened. Skipping it re-creates the permanent-strand bug the re-admission branch was built to cure — on that fix's own first live test case.
- **"Attach evidence and route to qa" is only a disposition if qa can actually run.** Last tick's routing was correct in form and inert in fact. When the actuator is starved, resolve what evidence can resolve *this* tick rather than deferring again (`feedback_router_blames_detector_when_actuator_or_policy_is_at_fault`).
- **A guard firing in production is admissible close-evidence — and is also proof the generator is still live.** Tonight showed both arms of the `NGAYNOP` fix working on three real documents *and* that the reparse batch still produces zeroed extractions 12 days on. Containment ≠ cure; do not close on the guard alone.
- **A `BLOCKED` row's premise can rot while the row sits parked.** `BCTC-HIST-VPS-BACKFILL` is blocked on "333 rows null-url", but a null-url row cannot produce a period-mismatch quarantine — and 6 pre-Q4-2025 periods resolved to documents tonight. Annotated, not reactivated; re-derive the count before scoping.
- **Board saturation unchanged and still the binding constraint.** 352 backlog / 244 review / 51 ready / qa **0** / WIP 1. The empty `qa[]` lane next to 202 `next_agent:qa` review rows is the whole story in two numbers.
- **Still owed (5th consecutive tick deferred):** `COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR` `--only` semantics; **6 review[] rows with `next_agent: po`**; the null-zone board sweep; `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`.
- **Not run this tick:** channel-audit (all 12 unresolved reports dispositioned above; no NEW `to:po` signal_queue rows — latest is 2026-07-29). Step 0-TNB: handoff already ACKed 2026-07-31T23:04:15Z by prior tick, file unchanged since — did **not** re-ACK, that would be churn.
