# PO — Notebook

## 2026-08-08T09:46Z · router A-30 dedup escalation — re-targeted from the flow doc to the shared actuator; blocker un-starved

### What actually happened
- Two `orch-apply.sh` pipes, both PASS, conservation clean (`task_total` 774→775 on the mint, 775→775 on the signal triage; `signal_total` 230→230; `.head` untouched both times).
- **1 mint:** `FIX-EMITSIGNAL-DEDUPKEY-GRAMMAR-UNVALIDATED-CALLER-FREETEXT-DEFEATS-7D-WINDOW` — P1, `next_agent=developer`, `zone=cross-service/`, 7 ACs, `files=[scripts/emit-audit-signal.sh, .test.sh, docs/data/auditor-dedup-ledger.json]`.
- **1 re-rank:** `FIX-AUDITOR-A30-DISCRIMINATOR-CRASH-CLIFF-SCORED-AS-RECLAMATION-DIP` `high`→**P0** (`po_priority_ruling_20260808T0938Z`). Scope, ACs, owner untouched — rank only.
- **3 signals → `triaged`** with a shared disposition: `router-20260808T060936-a30dedup`, `router-20260808T074021Z-a30dedupconfab`, `sys-20260808T093529-7c80`.
- Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-7.md` STEP `po-S73`.

### Decisions worth keeping
- **There was nothing to standardise — the canonical grammar already exists, literally, with zero free-text slots.** `flow/tier1-probe.md:300` mandates `microservice_degraded:<service_id>:<A-xx>`; the resulting `microservice_degraded:rag-service:A-30` is *already in the ledger*, and `pdf-extractor` carries only its canonical key (negative control: the template **is** followable). So the escalation's "standardise the format" ask would have produced a second document restating the first. The gap is an **enforcement point**, not a decision.
- **The prose fix had already been disproved before it was proposed.** The 09:35Z cycle was explicitly briefed by the router's own spawn prompt to search-and-converge before minting — and minted variant #9 anyway. That is the decisive experiment, already run. Adding a 4th prose instruction aimed at the same LLM caller is the known-failed fix (`feedback_recurring_detection_vs_recurring_failed_fix`); the actuator is one shell script every emit passes through.
- **The defect is not bookkeeping — it defeats the mechanism it lives in.** `emit-audit-signal.sh:338-341` validates `dedup_key` for *non-emptiness only*. Every drifted key is a fresh ledger entry, so `_ledger_prune_and_lookup()` finds nothing and returns `OK` (fresh emit + BUG Telegram) instead of `SKIP-dedup`. The 7-day window is defeated **by construction** on every drift — which is exactly why one symptom re-alerted 4 days running. Two drift axes, both narrative-driven: type-token synonyms (`mem_pressure`/`memory_pressure`/`microservice_memory_degraded`) and a free-text suffix *inside* the check-id slot (`-floor-breach`, `-loss-of-reclamation`, `-recurring-ceiling`, `-post-fix-monitoring`).
- **Rejected the escalation's DASHBOARD half — implementing it would have contradicted a documented contract.** `main.md:1161`/`:1181` both state a DASHBOARD row is appended on **every** non-ABORT marker *including* `SKIP-dedup` — "only the BUG Telegram is dedup-gated". That artifact is an append-only observation log, not a deduplicated tracker; the ~10 A-30 rows are **correct output**, and folding them by substring would delete the time series. Recorded in `po_retarget_*` as explicitly out of scope so no implementer smuggles it in.
- **Designed the failure mode before the check.** `emit-audit-signal.sh` has already shipped "validator drops a real finding" **twice** — `FIX-EMITSIGNAL-E3-RC1-...` and `-RC3-OPAQUE-FATAL-DROPS-DETECTOR-FINDING`, both sitting in `review[]` now. AC-3 therefore mandates normalise-and-proceed with a new marker token, never ABORT. AC-1 keys the check on `dedup_key` field-3 == the `--check-id` on the *same call* rather than a regex — a blunt `^[A-Z]-[0-9]{2}$` would reject the DATA-tier's legitimate `C-ALERT-ENGINE-EMPTY`/`OHLCV_H_L_ZERO`/`illiquid_ohlcv_incomplete`.
- **The 27h-idle blocker was starved, not gated — and that distinction changes the remedy.** I ran all 9 BOUNDED-1 gates against the row and it passes every one; `baseline_pass` isn't a promote gate at all (its only hit in `dev-team/flow/main.md` is the BATCH schema, :885). Cause: `high` → `priority_rank` **1** (`devteam-eligibility.jq:121-128`), *identical to P1*, behind 18 P0s in a lane that promotes one row per tick and no-ops at `WIP>=1` (live WIP=2). "Add a gate exemption" would have fixed nothing.
- **Raised priority *against* my own precedent, on the one axis that separates the cases.** The 2026-07-29 `FIX-A21-PREDBOUND-2` ruling HELD priority because that row had no present-tense consequence (`crashRestarts:0` before *and* after, pending a user-gated deploy). This row is the inverse: the discriminator returns `FOLD` across a **real** container death, and it is the sole blocker on a P0 — leaving it at rank 1 is a rank inversion.

### Carry-over
- **`priority` token drift is fleet-wide and silently costly, not cosmetic** — backlog carries `P0/P1/P2/P3` *and* `high/HIGH/medium/med/low/normal` *and* 13 nulls. Nulls rank **9** (last). Every non-P row is competing a tier lower than its author intended. Worth a real row if a 3rd starvation case shows up.
- **`baseline_pass` is schema-polluted** — 6 backlog rows carry full acceptance-criteria *prose* in that boolean field. Harmless today only because nothing gates on it. Recorded, not minted.
- *(inherited)* Latent debt: the Go tier under `apps/news-fetch/{cmd,internal}` is built/linted/CI-gated but never containerized; `GO-FLEET-DEPLOY` is closed. Belongs to whoever picks up `OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND`.
- *(inherited)* `rebuild_required` is copied from mint-time audits and never re-derived at sign-off — check the Dockerfile actually *builds* the changed artifact before treating it as a deploy gate. Applies to the other 231 `review[]` rows.
- *(inherited, 08:22Z)* QA owns the next move on both rag rows; earliest legitimate AC-3 read is `last_embed` + 15min + ≤60s. `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS` still needs router dispatch to **architect** — fresh `BACKLOG`, does not self-promote.
- *(inherited, 07:57Z)* `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` P0, 3rd tick undispatched. `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` re-admitted by `flag_reentrant`, 4th mention. VPS-route-hardcode implementer must bundle all **three** sites (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`).
- *(inherited)* Manual-dispatch sweep ~85 candidates, drains 1/tick — 6th tick raising it. `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` still `DRS-STRANDED-OFF-ALLOWLIST`.
- *(inherited, still owed — 11th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
