# PO Notebook

_Last: 2026-07-10T19:47Z_

## Tick 2026-07-10T19:47Z — dev-team triage: cowork-team tick-report (digest-daily guaranteed-slot miss, 2nd consecutive) → NOTHING
1 signal drained (source=file, type=tick-report → Step 0-SIG "unknown type"). GUARANTEED SLOT MISSED digest-daily (cron 30 17 * * *), 2nd consecutive day (07-09 + 07-10T17:30Z), session guaranteed-miss=10. Telegram clean (0 new / 0 unresolved). WIP=5 (2 ready/3 in_progress) → promoted NOTHING. **Disposition = NOTHING (no new mint): the durable backstop config fix is already TRIPLE-covered in flight, so recurring-2+ threshold is satisfied by existing coverage, not a fresh FIX:**
- `OPS-COWORK-GUARANTEED-SLOT-INSTALL` [REVIEW] — generalized firer LaunchAgent fires ALL guaranteed slots from `docs/data/cowork-schedule.json (.slots[]|select(.guaranteed))`; digest-daily explicitly at plist L43 (`30 17 * * *`) + firer test T4. Installed 07-10T11:09Z, awaiting first-live-fire QA (first fire 07-11T05:15Z). RAW-confirmed digest-daily IS in scope — not a gap.
- `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` [BACKLOG] — dispatcher-wide backstop/supersede root cause; digest-daily = same class as its 3 named slots ("dispatcher-wide").
- `FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK` [BACKLOG] — retrofit affected flows w/ mcp-call.sh curl bypass; names digest-daily explicitly.
Gateway-blind ROOT = user-action handoff `GATEWAY-BLIND-USER-ACTION-2026-06-18.md` (not dev-mintable). A digest-daily-specific FIX would fragment the dispatcher-wide fix (★07-04 churn-avoid). Signal's recommended "durable backstop config fix" = OPS-COWORK-GUARANTEED-SLOT-INSTALL, whose 07-11 QA first-fire is the proof-point.

## Tick 2026-07-10T18:41Z — RATIFY D2.5 (BLOCKED lane) + re-scope D1 residual (SWEEP convergence)
Router requested PO adjudication: sprint nearly converged (133->16 coherence warns; D0/D0B/D3/D4/D1-a all DONE_VERIFIED, RAW-verified). Two decisions, board groomed via `jq -f | orch-apply.sh` (conservation 473=473, zero data mutation). Full rationale → `docs/agent-memory/decisions/sprint-BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP-po.md`.
- **D2.5 = RATIFY** (architect brief §6). Extend `LANE_ALLOWED_STATUSES`: backlog/review/in_progress each += BLOCKED (orchStateSchema.ts:412-420 + doc-comment 392-399, ~3L). BLOCKED = orthogonal sub-state, NOT a lane (rejected new `blocked[]` lane = blast radius + loses in-progress framing). Precedent ADD-1 `ready+=TODO` (live). Zero data mutation → 7 BLOCKED warns coherent (4 backlog + 3 review). qa/done/done_verified/ready left unchanged (never observed; "done-but-blocked" contradictory); qa-BLOCKED revisit-if-observed. Moved backlog->ready[] +po_signoff, dispatchable now, dev-mcp-server.
- **D1 residual = reuse D1 row** (not fresh D1b — its title already charters this; keeps D5.depends valid). Did NOT fold into D2.5 dispatch (diff zone/owner: schema-code vs data-mutation → avoids "dev silently picks" hazard). Scope: (b) close FACTORY-INTERFACE-split-server-ts (4/4 commits verified) via devteam-close-task-done-verified.jq; (c) 8 lane-moves per D0B triage_result RELABEL (5 REVIEW->review[], 3 IN_PROGRESS->in_progress[]); (d) **PO-ADDED** FIX-BCTC-BANK-SUMMARY-MAPPING DONE->BLOCKED (EXCLUDE-from-evict but DONE label wrong; genuinely open, blocked-on-ops-reingest, corroborated by BLOCKED review sub-task — accurate+coherent, NOT a fake close). depends=[D2.5], size S, moved backlog->ready[].
- **Math 16->0:** D2.5=7, (c)=8, (d)=1, (b)=correctness/0-delta → unblocks D5 (validator hard-fail flip + close SHG-2/3/4/5). PO closed no task (dev/qa execute). Router to commit.

## Tick 2026-07-10T16:37Z — dev-team triage: 21 telegram reports (all dedup'd, NO new data task) + promote the verify-prune SWEEP (SHG convergence-blocker)
pendingSignals[] EMPTY; CI GREEN; WIP=0/idle. Router surfaced 21 unresolved reports + the SHG-2..SHG-5 stale-pickup finding for adjudication.
**Report triage — 3 buckets, ALL already tracked → resolved, NO new mint (★07-04 churn-avoid; dedup board-wide first):**
- **A) 17× [bctcPdfPull] ENRICH 0-rows FAIL-LOUD 2025-Q4** (ACB/BID/D2D/EIB/GAS/GVR/HCM/HSG/MBB/NKG/POW/SSI + new VCI/VHM/VIC/VPB/VRE) — the SAME cohort I folded this AM into `OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE` (now REVIEW, progressed 06:50→12:15Z) + drain sprint `BCTC-REFINE-STALL-RETRIGGER` ACTIVE; structural root `FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION` done_verified (decouples async table-extract from the sync 0-row gate). Per-ticker emission of a converging known cohort — convergence is gated on the reparse REVIEW row, NOT the telegram noise.
- **B) 1× [OHLCV-DEPTH] stall** (thin codes BDI:1/DLC:41/JSH:0/SIS:0/VDC:0) — honest per-code depth gap (VPS lacks history), NOT systemic backfill failure. Tracked `FIX-OHLCV-DEPTH-ALERT-HONEST-GAP-SUPPRESS`.
- **C) 3× [BCTC-1345b] low financial confidence** (D2D/CTG 2026-Q1, MBB 2025-Q4, composite 0.00-0.10) — conviction signal correctly SKIPPED by design; OCR/data-quality tracked (`OPS-BCTC-REFINE-REPASS-NONBANK-5T` in_progress, `FIX-BCTC-CTG-BALANCE-SHEET-REFINE`, report-spam meta `FIX-BCTC-1345B-REPORT-BATCH`).
Resolved ALL 21 via bridge `process_telegram_report resolution=monitoring` (status→processed; honest "tracked, convergence pending" — NOT "fixed", reparse not QA-signed). Verified unresolved 21→0. `resolution` is an ENUM {none,fixed,wontfix,duplicate,monitoring} — free-text -32602s (learned this tick).
**BATCH(1) = promote supervised `BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` backlog→ready (next_agent=architect, cross-service, M, P1).** WIP=0 idle capacity = the sanctioned window for an adjudicated-dispatch of a supervised row. **ROOT found (RAW):** `scripts/orch-cold-evict.sh` only evicts from .done/.done_verified/.active_sprints — NEVER scans backlog[], so status-flip-without-lane-move strands terminal rows there: 55 (34 DONE + 12 CANCELLED + 9 DONE_VERIFIED) + 12 mislaned active + ~62 DEFERRED/TODO/HELD = the 129 coherence warns that block the SHG-2/4 hard-fail flip AND the 385-bloat feeding recurring stale BOUNDED-1 picks. Sharpened D0-D5 on the row (D0 RAW-verify each terminal row, D1 evict, D2 relane, D3 enum-normalize, D4 fix cold-evict to scan all lanes, D5 flip orch-validate.mjs:375 warn→hard-fail then close SHG-2..5). **Folded SHG-2/3/4/5** in-place (`folded_into` marker) — did NOT close them: genuine open work behind the landed commits (129 coherence + the gated hard-fail flip). Promote via `scripts/orch-apply.sh` (conservation OK 538=538, backlog 385→384, ready 0→1). **Did NOT commit** — router re-verifies + commits.

## Tick 2026-07-10T20:19Z — dev-team triage: 3 drained signals (1 ci_red MINT + 2 cowork NOTHING)
Gateway native tool absent all session; self-diag OK → bash bridge `mcp-call.sh` functional (read_telegram_reports=`Không có báo cáo mới`, list_unresolved_reports=`[]` → no user requests). WIP: in_progress=3, no conflict (only backlog append). HEAD=31d4e2701 (signal SHA 1a8c1bffb is ancestor; only chore commits between → source unchanged, failures persist).
- **Signal 1 `ci-red-1a8c1bff` → MINT FIX** `CI-RED-1a8c1bff-FIX` (backlog[], type FIX, P1, zone `apps/mcp-server/`, origin_signal_id set). Dedup: no dupe on check_id or head_sha (3 existing REVIEW CI-RED rows are unrelated macro/foreign-flow). **PO-reproduced all 3 files 20:21Z, deterministic not-flaky:** (1) 1190 schedulerFileCount 64→65 count-drift; (2) FACTORY-SCHEDULER job-table 3× +1 (57→58 x2, 79→80) count-drift from server.ts split-refactor (8c228ffa6 et al); (3) 1352a async-extraction-race status `done`→`pek_triggered` — recon: pek_triggered is a DOCUMENTED intended intermediate status (bctcExtractReconcileJob.ts exists solely to resolve it from bctcPdfPullJob's fire-and-forget /pek-extract) → flagged INVESTIGATE-not-blind-patch (likely stale test asserting superseded sync model; dev confirms before editing either side). Gate: CI green on subsequent push. Router routes → dev-mcp-server Step 3 direct-FIX.
- **Signals 2+3 cowork (tnb-audit 07-10T20:15Z 3rd-consec-day miss#12; chef-evening 07-10T19:45Z 2nd-consec miss#11) → NOTHING.** Verified in-scope of 3 GENERALIZED/dispatcher-wide fixes (not chef-specific): `OPS-COWORK-GUARANTEED-SLOT-INSTALL` (REVIEW — LaunchAgent installed 07-10T11:09Z, awaiting first live fire 07-11T05:15Z chef-morning), `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` (dispatcher-wide supersede-wiring root cause), `FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK` (retrofit ALL gateway-blind cowork flows). tnb-audit's superseded-despite-trigger-id / gateway-blind pattern squarely covered → no new mint (★07-04 churn-avoid; durable fix = user gateway reconnect, tracked). Same disposition as prior-tick chef-evening.
- **Doc-drift observed (not fixed this tick):** `triage-signals.md` ci_red/repair_task_request/zone_missing_tier3 templates still say `status:"TODO"` for backlog[] entries, which now violates the D5 lane-coherence hard-fail (backlog allows only {BACKLOG,BLOCKED}). Used `BACKLOG` (live schema authoritative). Worth a follow-up doc-fix.

## Standing method (survives rotation)
- RAW-verify every signal/relayed claim from source, never payload-trust. Churn-not-product (★07-04): dedup board-wide before minting; recurring symptom on identical inputs → NO dup mint. Detection-recurs ≠ fix-failed.
- Data-coverage/extraction reports → resolve pointing to the tracked reparse/refine row (status→processed stops resurfacing); convergence is gated on that row's REVIEW/QA, not the telegram count. Mint code work only on post-reparse evidence.
- Supervised backlog rows = router/PO/architect-adjudicated dispatch; promote to ready[] only under WIP≤2 with next_agent set. PO ≠ prod code, PO does not spawn — disposition to router.
- Bridge `scripts/agents-flow/mcp-call.sh <tool> <json>` for gateway reads/report-resolution when direct gateway is blind; orch-state writes via `scripts/orch-apply.sh` (never raw). No self-commit path → write file + flag router to commit; never leave work silently uncommitted.
- Never touch `.head`/in_progress owned by a live worker. Report `resolution` is an ENUM (not free text) — any valid call sets status=processed which clears both new + unresolved sets.
