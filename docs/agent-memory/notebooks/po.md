# PO Notebook

_Last: 2026-07-30T23:02Z (dev-team Step 1 triage, tick 22:37Z — **sixth pass this tick**; 1 signal + reports 4221/4222/4223). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-2.md` STEP po-S69..S71._

## This cycle

- **Closed my own last-pass carry-over: I DID measure `signal_outcomes` this time.** Last pass I flagged that `query_database` will not resolve through the gateway wrapper from my grant, so my 65→2 claim was board-record vs report-text. Route found: `docker exec … bun` + `bun:sqlite` readonly on `/app/data/market.db` — the in-container plane, not the host copy. Do not inherit the old caveat; do inherit the route.
- **Report 4222 is a false positive of the guard its own fix shipped.** Live: exactly 2 unresolved rows — id 49 `stock_code=MACRO` (1,306.9h old) and id 74 `stock_code=MULTI` (850.9h), both `price_at_signal=null`, both `chain_catalyst`/news-scout. **103/105 resolved**; `MAX(checked_at)=2026-07-26`. `MACRO`/`MULTI` are not tickers, so no price series exists and `checked_at` can never advance. `checkStalledResolutionLiveness()` (added BY `FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED`) counts them forever → a daily BUG alert in perpetuity, worded "price lookup failing", which is false. Its own review_note predicted these exact 2 rows as legit-unresolvable and shipped the guard anyway. Batched a narrow FIX; the parent row stays QA's.
- **The context-bloat recurrence is the journal WRITER's rollover, and a natural experiment proves it.** `.claude/skills/decision-journal/SKILL.md` § Cap Check rolls to `-2.md` only on `LINES -gt 600` — **no byte branch**. In this sprint the ONLY journal that rolled is `po.md`, the ONLY one that crossed the LINE cap (625L). architect 211L/51,945B, dev-mcp-server 312L/79,545B, developer 488L/156,774B all sit under 600 lines → rollover never fires while the byte detector fires every cycle. Fleet-wide **10** journals are in this blind spot; worst `sprint-SYSTEMIC-REMAKE-P1-qa.md` **225,553 B / 504 L**.
- **Did NOT force-split the live file, and did NOT re-mint the archiver row.** The developer journal grew 154,793→156,774 B *during* this triage — `feedback_ctxbloat_breach_on_live_sprint_file_defer` holds. `FIX-DECISION-JOURNAL-BYTECAP-NO-ACTUATOR` (P1) already owns the ARCHIVER half and is blocked behind the dangling-ids row. Third actuator in the same family; only this one is unblocked and prevents regrowth at the write site.
- **Ratified architect's `FIX-PO-NO-PRODUCER-...` at source; gate opened, then cleared mid-tick.** Ran the verifier myself: PASS, **15 assertions = 12 predicate branches + 3 existence checks** (dev-team's "12/12" counts branches only; I first wrote "13/13" and that was wrong — corrected on the board at 23:04Z rather than left to propagate). Load-bearing negatives: D/J (supervised&&plan_only excluded — the producer does not poach SLS's lane) and G (idempotency guard is real code). Live Step 1: **46** unflagged candidates (43 DRS, 3 XOR). I withheld `done_verified` on untracked artifacts; `e27f6f0a5` then landed all 3 + the main.md pointer and pushed, so the gate is **cleared** — no PO objection.
- **Polymarket 4221/4223 = in-flight, verified not by relay but by `git status`:** `polymarket.ts`, `predictionMarketJob.ts`, `predictionTools.ts` modified, `flow/prediction.md` + `get_prediction_markets.md` + 2 tests deleted — matches architect's RETIRE design item-for-item. No action.

## Carry-over

- **RESOLVED mid-tick, do not re-raise:** architect's 3 deliverables were UNTRACKED when I ratified (live `feedback_dead_worker_uncommitted_live_file_revert` exposure, 4 unpushed commits, ~79 dirty paths). `e27f6f0a5` committed all 3 + the `main.md` pointer and origin/main now has them. **Lesson kept:** I re-read `git status` for those exact paths at tick exit instead of trusting my own 10-minute-old snapshot — the tree moves under you when peers are live.
- **dev-mcp-server's Polymarket retirement is still UNCOMMITTED (~70 dirty paths incl. deletions).** Any commit this session must stay **pathspec-scoped**; a bare `git commit -a` sweeps it. Deletions are involved, so `git add` + bare commit is doubly wrong here (`feedback_pathspec_commit_drops_rename_deletion`).
- **The manual-dispatch producer is LIVE and I used it — it is no longer a theory.** Stamped `FIX-ALERT-COMMANDER-NO-BASH-GRANT-NOTEBOOK-UNCOMMITTABLE` and folded it into BATCH. Raw-verified its premise first (13h-old status_note): `.claude/agents/alert-commander.md:5` still reads `tools: Read, Write, Edit, mcp__gateway__call_tool`, untouched since mint. Co-dispatch with `FIX-COWORK-BASH-GRANT-COVERAGE-STAMP-TRANSPORT` (ready, P1, same agent-father, same file class).
- **45 candidates remain after this tick's one.** The sweep takes exactly ONE per invocation by design — do not batch-drain it, and do not read the remaining 45 as a new finding each pass.
- **`CLEAN-NOTEBOOK-AC2A-...` is still the acceptance canary for the producer.** Do not hand-dispatch it; that destroys the test.
- **POLYMARKET will re-strand at WF-2 unless PO stamps it when it becomes `.head`.** `supervised:true`, no `po_goahead_*`. `.head` was idle this tick so the pre-check was a legitimate no-op — not a skip.
- **Do not widen the DRS allowlist to admit agent-father as a shortcut** — ratified blast-radius control; `feedback_gate_widening_recommendation_requires_actuator_dry_run` applies.
- **Rotation ≠ reachability** — `FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` cannot make a lane reachable on a busy tick. Ruled twice now (STEP po-S58). Orthogonal.
- **7 rows still starved on `DONE`-not-`DONE_VERIFIED`** — do not clear by hand; that erases the evidence for `FIX-DONELANE-NO-DONEVERIFIED-PRODUCER-DEP-STARVATION`.
- **Never eyeball `next_agent` to judge dispatchability** — run the predicate, print every sub-gate.
- **`signal_queue` can never self-prune** — 131/200, all `triaged`/`RETRACTED`; `orch-cold-evict.sh TERMINAL_SIGNAL_STATUSES` contains neither token. Dashboard inbox read 0 NEW this tick.
- **`RAG-FTS-BUILD-MEMORY-BOUND` time-gated to 2026-08-12 — do not re-measure.** Nine PO ticks, zero decision change.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog.** Do not "catch the drain up".
- **TNB handoff is stale-but-ACKed** (`docs/handoffs/tnb-audit-latest.md`, ACK 2026-07-28T22:55Z, file unchanged since). Step 0-TNB is a legitimate no-op until it is rewritten — not a missed read.
- **Sprint journal `-po.md` is CAP-REACHED (625L); write to `-po-2.md`** (127L/17.5KB, healthy).
- **Benign, do not re-file:** `process_telegram_report` takes `id`, not `report_id`.
