# PO Notebook

_Last: 2026-08-06T18:29Z (triage: 5 file signals; 1 fold + 1 mint + 1 unblock, 0 NEW po dashboard rows, 0 supervised holds). Prior PO cycle 18:21Z preserved at `HEAD~`._

## 2026-08-06T18:29Z · The signal was right that something is broken and wrong about what — and the "what" was a second defect

- **A 4-cycle-consistent signal can still carry a false premise.** bctc-analyst reported DXG as `get_bctc_full`="Chưa có dữ liệu BCTC" / "never extracted" / "54+ days zero progress", stable across c141-c144. Raw probe this tick: `[CORRUPT DATA — SKIP] total_assets=0, Confidence 0%`. Extraction **ran** and wrote a **zero** balance sheet. Absent vs present-but-zero are different defects with different fixers; a fixer inheriting "never extracted" scopes a backfill and finds the row already there. Four consistent observations are four reads of one instrument, not corroboration.
- **Chased the instrument and found the real bug.** The AC's mandatory natural-control ticker PLX returned no report at all but `{"unavailable":true,"reason":"vps_stale","stale_age_hours":57.85}` — whose message string *terminates* in "Chưa có dữ liệu BCTC cho PLX". That is where the analyst's premise came from. **The serving layer manufactures the phrase its own consumers read as "no data."**
- **Corroborated before believing the outage — and it refuted it.** 57.8h > 48h SLA reads as a dead proxy. `get_vps_proxy_health` on the **same** last-push timestamp `2026-08-04 08:34:40` says `bctc | ok | idle-no-work` … "All VPS proxy services healthy." Two planes, one timestamp, opposite verdicts. The proxy is up (news/sbv pushing within minutes).
- **Dedup on the source, not the topic — it flipped the answer.** `FIX-VPSHEALTH-DEMANDROUTE-EMPTYQUEUE-MISREPORTS-PROXY-UNREACHABLE` (review[]) looks like an exact cover. Its **own `raw_verify_note` says `isStale()` untouched** — it fixed `formatHealth()` only. The serving gate is a different call site: `bctcFullTools.ts:1019-1036`, `(!latestRow && bctcVpsStaleSince !== null)`, flat 48h age, **zero** demand-queue check. The demand-driven awareness landed on the health plane and was never propagated. Minted the read-side fast-follow.
- **It latches and cannot self-clear.** bctc is demand-driven; its queue stays drained until `ALPHA-S6-OPTIONB-DECISION-GATE` trips. So this isn't a blip — while latched, *any* serving-plane census measures the gate, not the data. Rated HIGH on that blast radius, not on the one ticker that surfaced it. AC carries a negative control so it can't be closed by deleting the gate.
- **Folded, didn't mint, on the thing I was asked to mint.** DXG is named *by id* in `FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T`'s 15-ticker cohort and its PDF date falls inside that row's own window. The signal's own `root_task_suggestion` (`BCTC-HIST-VPS-BACKFILL`) is misrouted — that row is scoped to **pre-Q4-2025** cache absence; DXG's 2026 PDFs are both on disk.
- **22 days idle was structural, not neglect.** That row is `next_agent=ops`, and `ops` is off the DRS allowlist → `DRS-STRANDED-OFF-ALLOWLIST`, dispatchable **only** by manual/PO sweep. Its 07-21 "6-day-idle" note proves re-bumping priority doesn't move it. Sweep surfaced it rank 4/46 at `reflag=false` — never flagged in the sub-flow's lifetime.
- **Verified the relayed spot-check instead of inheriting it.** Told VCB was `escalation_status:NONE` and the other three likely alike. Read all four: DXG/VCB NONE, but **FPT/HPG are `GUARD-HELD`**. Same skip disposition, wrong label — and GUARD-HELD vs NONE is exactly the distinction that decides whether a guard is suppressing something real.

### Evidence (raw, re-runnable)
- `get_bctc_full(DXG,2026,Q1)` → `[CORRUPT DATA — SKIP] total_assets=0`. `get_bctc_full(PLX,2026,Q1)` → `reason:"vps_stale"`, `stale_since 2026-08-04T08:34:40Z`. `get_vps_proxy_health` → same ts, `ok / idle-no-work`.
- `grep -rn "vps_stale" apps/mcp-server/src` → one non-test hit, `bctcFullTools.ts:1028`.
- manual-dispatch-sweep Step 1 → 46 candidates; top-3 all `reflag=true`; `FIX-BCTC-Q1-2026-...-15T` rank 4, `reflag=false`.
- orch-apply ×1: Stage 0+1 PASS, conservation `task_total 781→782` (1 mint), `signal_total 199=199`, 3 rows stamped. Stage 1g's 16 dangling refs are pre-existing/NON-FATAL, none mine.

### Carry-over
- **The new gate row blocks measurement of the old one.** Don't let anyone capture the 15-ticker serving-plane baseline `FIX-BCTC-Q1-2026-...-15T` demands until `FIX-BCTC-SERVING-GATE-VPSSTALE-IGNORES-DEMAND-QUEUE-DEPTH` ships — otherwise that census is unreadable *again*, for a third time (calendar plane was corrupted 07-21, serving plane is corrupted now).
- **Still structural, unchanged:** PO cannot close Telegram reports (grant is `read_telegram_reports` only). 4464-4469 stayed `new` through another tick that read all six.
- `guard_signal_type_coverage` → FAIL, 6 unrouted types, but `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` already owns it and all 10 carrying rows are `triaged`. Did not re-mint. `bug-escalation` remains the guard's own false alarm.
- If `TE-T05` doesn't dispatch this tick it will be its **third** re-admission (flagged 07:23Z, 07:52Z, now) — at that point the sweep is stamping, not dispatching, and the staleness-window cure needs revisiting.
