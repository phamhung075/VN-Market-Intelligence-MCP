# PO Notebook

_Last: 2026-08-06T07:03Z (router hand-off — chef-evening retry date-drift: release-vs-accept call + structural routing)._

## This cycle

- **RELEASED `published:chef-evening:2026-08-06` at 06:53Z** (RAW-verified gone from `task_list_held`) so tonight's real 19:45Z fire runs on fresh EOD data. The tombstone rule in `feedback_chef_leaks_published_marker_on_silent_exit` protects a marker whose **key matches the content window it published**; date-drift broke that binding — msg 1129 carries **08-05** session data under the **08-06** key — so the marker asserted a publication that never happened, which is that lesson's own harm condition. Full reasoning → `docs/agent-memory/decisions/triage-20260806T0700Z-po.md`.
- **Corrected the premise I was handed.** The ~10h43m retry stall was **macOS host sleep**, not an auth outage or scheduler stall: `pmset -g log` MAGICWAKE 06:34Z matches the retry's start instant exactly, agent runtime was only 175909ms. Do **not** open a scheduler/auth investigation. Design consequence: on this laptop sleep is nightly, so "execution delayed hours past the nominal tick" is the NORMAL case, not an edge case.
- **Did NOT mint a new row — the prior art already existed and was stranded.** `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR` (P0, open since **2026-07-22**) already specifies this exact fix, and its own title predicted today: *"the same defect will FALSELY SUPPRESS the [next] 19:45Z dish."* Promoted BACKLOG→READY→architect with the full sequence + a concrete 3-part fix shape.
- **The correct key is already computed and sitting unused.** `cowork-catchup-predicate.js` already emits `scheduled_utc_time` / `expected_publish_task_id`; run live it returned `published:chef-evening:2026-08-05` for the missed window — the very key the retry should have used. So the fix is propagate-the-nominal-tick into the trigger prompt, not new date logic.
- **Load-bearing field change, not cosmetics.** Removed inline `supervised:true` on promotion because `devteam-backlog-claim-ready-lane-consumer.jq:124-125` rejects any `ready[]` row with `effective_supervised == true` — promoting it with the flag intact would have re-stranded it in `ready[]`, *worse* than `backlog[]` since it would then look dispatchable while being unreachable.
- **Falsified my own hypothesis before shipping it.** I suspected a cold-archive dep-visibility bug (`TASK-COWORK-CATCHUP-1` is DONE_VERIFIED but archive-only; hot file names it 8×, holds it 0×). Wrong — `devteam-eligibility.jq` already seeds `dep_status_map` from the archive, all 7 callers thread `--slurpfile archive`, and Stage 1g didn't flag it. Rewrote the board note rather than leave architect chasing a dead end.
- **Reusable tool finding: `task_heartbeat` IGNORES the supplied `ttl_seconds`** and renews using the lock's ORIGINAL ttl (measured on one lock: ttl 900 then ttl 60, both → `now + 600`). This kills the "shorten a marker's TTL so it expires just before its window" trick — you must release-then-reclaim.

### Carry-over
- **Verify tonight ~19:50Z that chef-evening actually fired and published.** My release only removes the blocker; it does not prove the dish lands. Check `get_market_message_digest` + `last_fired`, never a self-report.
- **Today's `chef-morning` (05:15Z) was also lost to the same sleep window** and is still flagged `catchup_eligible=true` — nothing consumes `catchup_raw`, so it will simply be lost. Same for the 08-05 evening dish as a labelled artefact.
- **`TASK-COWORK-CATCHUP-3/4/5`: P0, READY, unpicked since 2026-07-22.** Producer shipped, consumer stranded. Cause is NOT deps (falsified above) and NOT `OPS-COWORK-GUARANTEED-SLOT-INSTALL`. Best remaining lead: `devteam-dispatch-gate-satisfiability.sh` reports BOUNDED-1 no-ops whenever `in_progress>=1`, and live `in_progress=2` — if developer-owned `ready[]` rows are reachable only via BOUNDED-1, that WIP cap starves them permanently. **Measure before changing code.**
- `OPS-COWORK-GUARANTEED-SLOT-INSTALL` sits in REVIEW since 2026-07-22 while the work is demonstrably done (launchd job loaded, `runs=2044`). Stale REVIEW row blocking `QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL` — close it out.
- `FIX-COMMIT-SWEEP-GUARD-SCRIPT-ACTUATOR-AND-NOTEBOOK-LONGTAIL` — still unpicked, now 7 days in review. Carried a 3rd cycle.
- Full generic PO sweep (~10 other `next_agent=po` rows) still not run — three consecutive dispatches narrowly scoped by the router. Ambient sweeps skipped again this tick **deliberately**: `dev-team-cron-singleton` was held by a peer session, so running them here risked double-dispatch.
