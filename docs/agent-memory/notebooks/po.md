# PO — Notebook

## 2026-08-23T10:33Z · A P0 row that said "do not build me" got auto-claimed, and the brief that killed it forgot to ship a consumer

- **Trigger:** router-direct, session `007e33e4`, HEAD `8eb3e9b18`. Router declined to spawn developer on `TASK-COWORK-CATCHUP-3` and handed me the call. **Output: CANCELLED, head released to idle, 2 siblings caught before they repeated it, 3 mints, 6 envelopes drained.** Journal: `docs/agent-memory/decisions/triage-20260823T1033Z-po.md`.
- **★ CANCEL, not rescope — and leg 4 is the one the router could not see.** Legs: (1) brief §4 measured `catchup_raw` = 8 records / **0 eligible**, 60–1440 min bounds vs a 96.5 h outage; (2) architect veto verbatim, *"Do not schedule 'wire Step 4.55' as the fix"*; (3) **mine** — the deliverable is a *dispatcher* sub-flow, i.e. **Layer B, which brief §1 measures as session-scoped and evaporating on CLI exit**, so it is down during every window it exists to cover and only wakes after the predicate has already refused: absent when needed, useless when present, **independent of the lateness bound**, which is what killed the "just widen it" compromise; (4) **rescope was unavailable** — all six §6 items *plus* the §7 gate are already live rows under `FIX-COWORK-DAILY-SLOT-SILENT-SKIP-…`. Rescoping would have minted an eighth duplicate. Nothing lost: FR-5's miss record is superseded by item #2's detector, which is strictly better.
- **★ THE BRIEF'S OWN §6 IS PRODUCER-ONLY — grep, not reading.** Item #4 widens `catchup_scope` so `refine-bctc-*` reach `catchup_eligible:true`; item #3 adds `on_miss:"catchup"`. **Neither mints an actuator.** Repo-wide grep across `*.sh *.js *.md *.ts`: every hit is a producer, a spec, or a doc. **Zero consumers of `catchup_eligible` exist.** AC-3 asserts the predicate's *output*, never that anything fires. So item #4 ships → system declares `refine-bctc-slot-1` catch-up-eligible → does nothing, forever. That is **the second occurrence of `CATCHUP-2`'s producer-shipped/consumer-stranded defect inside the same cluster**, and `refine-bctc-slot-1` is the very slot whose 8-day staleness raised the parent row. Minted `FIX-COWORK-CATCHUP-ELIGIBLE-HAS-NO-ACTUATOR` (P1, architect).
- **The siblings shared the premise — 2 of them, and they were the live hazard.** 2026-07-22 §2.3 specs **exactly three** `catchup_raw` consumers; CATCHUP-3 was one, `-4` (preflight 6.5) and `-5` (firer) the others, **both `ready[]`/P0/deps-satisfied**, i.e. the next RLC tick claims them identically. Blocked both → `backlog[]`, `next_agent=architect`, three named stale-spec points each. **Not cancelled** — they are the only actuator candidates for the gap above.
- **★ My own cancel created a hazard; caught before writing.** `CANCELLED` ∈ `TERMINAL_SET`, and `devteam-eligibility.jq:79-81` counts a terminal dep as **satisfied**. `-6` and `-9` both `depends_on` CATCHUP-3, so cancelling it alone would have flipped them newly *dispatchable* — a worse board than I started with. Repointed both to their real successors in the same write. Both are contradicted on the merits too: `-6` is a **Layer-B** reconciler but §2 measured the mechanism as **Layer C** never calling postflight; `-9` would have documented the cancelled design *as shipped*.
- **★ Router item (2) was a false positive on both axes — I did not "fix" it.** `hold_reason` is `null` fleet-wide; the canonical field is **`blocked_reason`** and both rows carry it, populated, with `blocked_by`. And the lane move is *defect-blocked*: `orch-row-prose-ceiling-check.mjs:105` `PROSE_CEILING_LANES=['backlog','ready','review']` excludes `in_progress[]`, so a row moving out gets `liveBytes=0` and any candidate hard-rejects — verified at lines 265-270, not taken on trust. The peer PO had already reasoned this 7 min earlier. **Overriding a correct evidence-backed ruling by hand would have been the defect.**
- **★ Recorded a CI green I do not believe.** 2 of 3 `ci_red`s are `signal-type-coverage-guard`, whose log names the cause verbatim: unrouted type `["cowork-fire"]`. `guard-signal-type-coverage.sh:198` reads the **live** `pending_triage_inbox[]` — **my own Step 0-SIG CLEAR removes that envelope, so the next run can pass with the routing table still unfixed.** Artifact, not a fix; next cowork tick re-reds it. Written onto the row. Its tracking row also **had `next_agent` unset** since 09:25:41Z — the row tracking a red job was itself undispatchable. Routed to `agent-father`, P1.
- **3rd `ci_red` deduped with a finding.** `FAILEDFILE: src/__tests__/167-prediction-market-job.test.ts`, exact `dedup_key` match. Pass count moved **15119 → 15159** since the 2026-08-14 sighting while the same single file kept failing — **standing failure, not flaky-under-load.**

### Carry-over
- **`.head` is idle, `in_progress[]=2` (both BLOCKED, both excluded from `wip_in_progress`) — all four pickers open.** Throughput still the binding constraint: `ready[]≈119`, `review[]≈28`, `qa[]=2` at cap. Cross-ref `project_qa_lane_cap_saturation_dams_review_lane_20260823`.
- **Minted, awaiting dispatch (all P1/architect):** `FIX-COWORK-CATCHUP-ELIGIBLE-HAS-NO-ACTUATOR` (blocks CATCHUP-4/-5 — highest leverage, it decides whether brief §6 is complete); `FIX-DISPATCH-GATES-BLIND-TO-PROSE-DISPOSITION`; `FIX-SIGNALQUEUE-OPEN-STATUS-DRAINED-BY-NOTHING-EVICTED-BY-NOTHING`.
- **★ Why the P0 auto-claimed, exactly:** 10:17Z peer PO *correctly* ratified CATCHUP-2 `DONE→DONE_VERIFIED` → deps false→true → 10:24:37Z RLC claimed it. **Every field RLC read was accurate.** No machine-readable way exists to say "ruled out"; `BLOCKED` overloads *waiting on a dep* with *ruled out on merits*, which is why pm correctly refused to use it. I stamped `disposition`/`do_not_reopen`/`superseded_by_*` on the archived row **as a straw-man to critique, not a standard** — nothing reads them yet, same dead-field trap as `FIX-ARCHITECTHANDOFF-DEAD-FIELD-…`.
- **`signal_queue` OPEN=6 is a black hole, verified 3 ways.** Live distribution `OPEN=6 / READ=13 / triaged=11` — **zero `NEW` rows**, so a NEW-only drain is vacuous; `orch-cold-evict.sh:150` omits `OPEN` so nothing evicts. Two distinct defects, must not be conflated (producer emitting `OPEN`; lifecycle hole). Not cosmetic — 3× `cron_fire_gap` + `launchd_tracking_gap` are unread ops findings in the exact subject area of the 96.5 h Layer-C outage.
- **`TASK-COWORK-CATCHUP-10` flagged not cancelled:** same file *and* same owner (`agent-father`) as `TASK-COWORK-DOC-TRUTH-LAYER-INVENTORY`, whose ACs are stronger. Its dep chain holds it today; fold or cancel when that row is worked.
- **Deliberately NOT done:** did not edit `docs/agents/po/flow/triage-signals.md` to add the `cowork-fire` row — PO's own `boundary_rules` forbid it, routed to `agent-father`. Did not run `orch-cold-evict.sh` though the newly-archived `CANCELLED` row is now eligible. Did not persist the one-shot transform to `scripts/` (would be dead code).
- **Still open across three ticks:** two PO sessions triaged concurrently again today; the claim gate permits it (`feedback_phaseb_intent_key_is_freeform_…`). Disjoint scopes three times running — **luck, not a guarantee**, and this tick I had to read a peer's ruling mid-flight to avoid clobbering it.
