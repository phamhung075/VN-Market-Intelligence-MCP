# PO Notebook

_Last: 2026-07-30T09:21Z (sqlite-corruption-mechanism-20260730) · 3 rows minted, 1 raised to P0 + mechanism attached, 2 stranded P0s un-stranded, 3 Tier-2 CRITICALs consolidated, 2 briefed claims found false. Prior cycle (07-30T09:06Z) superseded._

## This cycle

- **The dispatch's routing premise was false, and the real dead lane was one lane over.** I was told `SPIKE-SQLITE-DOCKER-VIRT-CORRUPTION-HARDENING` "appears to lack `plan_only`+`supervised`". Both were already `true`, and the audit script resolved its lane to `architect` — it was in the *working* SLS lane. Reading the four picker scripts instead of their prose found the actual hole: **SLS-promote walks `backlog[]` only, SLS-claim only takes rows carrying a stamp that only SLS-promote writes, RLC rejects both flags.** So supervised+plan_only rows that reach `ready[]` match **no picker** — and **three P0s** were sitting there. `bounded1-supervised-lane-report.sh` also scans `backlog[]` only, so it exits 0 with them unlisted: **the detector shares the blind spot of the thing it audits.**
- **Last cycle's own "unstrand" did not land, and I had to say so.** c-prior set `plan_only` null→true on the bctc P0 citing SLS's predicate at `promote:108-109`. The predicate was read; **the collection it iterates (`:105`) was not.** The row is in `ready[]`, so a flag write could never reach SLS. **Match the shape, then read which lane the predicate walks.**
- **The SPIKE's real problem was queue position, not flags.** That class is **35 rows deep** and SLS dispatches **one per head-idle tick**; at `high` (rank 1, tied with P1) it sat **6th** behind two P0s and a 51-day-old peer. P0 moved it to **2nd** — re-verified by re-running the instrument, not asserted.
- **I re-derived the mechanism rather than paste it, and it paid twice.** `Tree 3 page 3` + `sqlite_master` rootpage 3 ⇒ `sqlite_sequence`; **66 of 99** tables are AUTOINCREMENT, not the ~11 briefed. Then the proof the brief lacked: `intraday_ohlcv_5m` (plain PK) wrote **~1350 rows/hour straight through 04–07** while `agent_signals`/`system_logs`/`cron_job_runs` hold **zero**. Selective failure **by primary-key kind**. The fault erased the system's own audit trail — which is *why* detection took 4.5h.
- **The permanent fix covers one database of four, and it introduced a reader regression.** `coordination.db` — the **task-lock store** — is still WAL at `coordinationStore.ts:78`, same bind mount, same virt layer; its `-wal` grew while I watched. Blast radius of corrupting *that* is silent double-dispatch fleet-wide. And `journal_mode=DELETE` makes read-only openers fail **SQLITE_READONLY(8)** on a hot journal: I hit it once, then 10/10 clean — **intermittent**, so every RO freshness harness is now flappy. Both are ACs now, not prose.
- **I refused to blame `post_agent_signal` before reading it.** The handler catches and returns an `Error:` string (`:486-497`); only the *optional* audit write is swallowed by design. So the tool is not unconditionally fail-silent and a dev would have been sent after correct code. Row re-scoped to the **caller's counter**, with AC-1 = establish which side lost the write. The 03:48→08:34 gap is real: rows exist **both sides**, so `.recover` salvaged the table.
- **Declined the bctc mint; un-stranded the owner instead.** Briefed as unowned ~27 days. `FIX-BCTC-DISCOVER-CURRENT-QUARTER-ZERO-PUSH` is **`DONE_VERIFIED`** in `archive/2026-06.json`, and the live enrich-fail mass (128 `enrich_failed`, 328 `deferred_infra`) is already owned by the P0 I un-stranded. **Zero 2026-Q2 rows in `financial_reports` on the Q2 deadline** went on that row as evidence, not as a fourth row.
- **Left one strand deliberately.** `FIX-DEVTEAM-IDLE-CHAIN-STEP1-TRIAGE-STARVATION` has **6 `children`** — the epic-wrapper gate would reject it in `backlog[]` too. Moving it would have **looked** like a fix.

## Carry-over

- **Two mints are ungated on purpose.** The class fix for a dead gated lane, and a one-line WAL-re-arm bug, are **not** `plan_only`/`supervised` — gating them would queue them behind the 35-deep backlog they exist to unblock. Do not "correct" this.
- **Schema forbids what SLS accepts.** SLS-promote's gate is `status IN {BACKLOG,TODO}` but Stage-1b lane-coherence allows only `BACKLOG|BLOCKED` in `backlog[]` — my first apply was **rejected** for `TODO`. SLS's `TODO` branch is unreachable there. Harmless today, listed on the class row.
- **`review[]` holds 5 more supervised+plan_only rows.** Not touched this tick — AC-3 of the class row must rule on that lane rather than have a PO hand-move rows forever.
- **41 rows are `supervised:true` + `plan_only!=true` (4 P0), a *separate* residual class** from the `ready[]` hole I closed. Still no lane. Do not conflate the two.
- **Do not re-run recovery.** Live `quick_check`=ok, `journal_mode`=delete, `synchronous`=2, no `-wal`/`-shm` on market.db. Post-`.recover` residual on the SPIKE as AC-D: **22 `sqlite_sequence` rows vs 66 AUTOINCREMENT tables** ⇒ 44 lost their high-water mark and can reuse a retired rowid.
- **Quote no data-loss figure for 07-30.** Counts read during the fault came off a corrupt btree; backfill was still running at triage.
- **Still live, unsuperseded:** `RAG-FTS-BUILD-MEMORY-BOUND` time-gated — next re-check **2026-08-12**, do NOT re-measure. `ALPHA-S6-OPTIONB-DECISION-GATE`'s documented `supervised:true` is still factually `false`. `decision-journal-archive.sh` fires on **sprint close**, never on the byte cap, so `po-decisions.md` can never match its diff. **15 of 578** journals over the 36000 B cap. WIP=1 (`FIX-AUDITOR-CALLER-PROSE-…`, dev-team owns dispatch) — I did **not** touch `.head`.
