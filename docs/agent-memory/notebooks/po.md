# PO Notebook

_Last: 2026-07-21T19:29Z (triage 9 signals + supervised sign-off — 5 mints, 1 approve, 1 orphan held open)_

## Tick 2026-07-21T19:29Z — 9-signal triage + FIX-COMMIT-PATH sign-off (1 orch-apply write, Stage 0/1 PASS, conservation 579→584)

**★ Six of six system-auditor signals were false or stale. The channel is now mostly noise.** sbv_fx CRITICAL, both B-07 WARNs, and 2 of 3 `bctc_eval_delta` HIGHs did not survive a source check. Only MBB (eval row 1.4d old, `code_coverage` 0.21 vs 0.8) was real *and* current. Each FP costs a full human-attention triage cycle, so I re-priced the already-tracked B-07 row `low → P2`: a defect whose per-occurrence cost is a triage cycle is mispriced at "low".

**★ I mistook a second miscalibrated flag for corroboration — recorded because it nearly shipped a wrong verdict.** I first read `get_vps_proxy_health` (bctc 0 pushes/24h, last push 42h) as independent confirmation that vn-bctc-fetch was genuinely down, and wrote "corroborated on 2 planes". Wrong. bctc is event-driven with a 168h SSOT threshold, and `get_sla_status` independently reports it **ok** (1769/9830min). Prior art then showed vn-bctc-fetch is a **pure-bash systemd timer with no HTTP port**, so its unhealthy badge is structurally permanent. **Two miscalibrated flags agreeing is not a second plane.** The board row existed the whole time; grepping prior art before concluding is what caught it.

**★ sbv_fx is a category error, not market-hours blindness.** The dispatcher asked me to check the market-hours FP class. Broader: `freshnessSlaChecker.ts:145-148` gives sbv_fx a **flat 30-min threshold** for a **once-per-business-day** source — guaranteed breach ~47.5h of every 48. system-map says 1440min (48x divergence, defeating the never-hardcode mandate). And `slaStatusTools.ts:165` *prints* "sbv_fx: VN business days" as an off-hours window that `:99-100` never applies — **a user-facing string advertising a guarantee the code does not implement, 65 lines apart in one file.** Real last push was 03:05Z = 10:05 VN Tuesday, exactly as designed.

**★ The notebook signal under-reported its own severity by a category.** Reported as "211L over cap, auto-prune can't act". Actually `notebook-write/SKILL.md:62-66` routes a zero-`## `-count file to a **blank-state full-file Write** — so agent-father's next notebook write **destroys 211 lines / 6 sections**. The file uses `### ` only; the predicate counts `^## `. Armed right now. Swept all notebooks: agent-father.md is the **only** at-risk file, so this stays S-sized. *This is why I wrote this file with `## `.*

**★ Marked terminal only where disposition is durably captured — and found a second failure mode the escalation missed.** Verified `drain-signals.md` 0a-D collects only `status=NEW`, so a blanket mark-READ really would sever dev-team's inbox. But leaving triaged rows NEW guarantees infinite re-drain. Rule applied: terminal **iff** a board row or journal verdict holds the disposition. Then the census: **73 live rows carry `triaged`, which is nowhere in the skill's NEW→READ→RESOLVED vocabulary** — and PRUNE only evicts READ/RESOLVED/SUPERSEDED, so all 73 are **structurally unprunable forever**. Used canonical `RESOLVED`; widened the contract row to cover the migration.

**★ Approved the supervised row on substance and refused to launder an unverifiable claim into evidence.** agent-father claims it wrote acceptance before editing; the text first appears in git *after* the code commit, because its commit zone excludes orch-state.json and a peer cold-evict collapsed both writes into one blob. **Unverifiable ≠ disproven, and no fault attaches to the agent** — the instruction was unsatisfiable as written. Decisive: **no AC (1)-(6) requires acceptance-first**, so approval rests only on checkable artifacts. `acceptance_written_at` is self-reported by the agent whose ordering it attests — that is narration, not evidence.

**★ An agent whose commit zone excludes the board can never evidence an ordering claim about it.** So "write acceptance first" is unenforceable across the whole maintenance lane. Minted the ruling as a SPIKE: acceptance authorship moves to the **dispatcher**, whose zone already includes the board — ordering then becomes provable across two disjoint zones by construction. Rejected letting maintenance agents commit the board themselves (re-introduces the peer-sweep class this very sprint just fixed).

**★ Honoured both "DO NOT MINT" requests.** Both cowork escalations asked to be graded, not minted. Graded: `FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT` lands **first** (a terminal status is unsafe until a receiver exists to consume it), `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT` second and now `depends` on it. Both P1.

## Carry-over
- **HELD OPEN DELIBERATELY — `po-20260720T052606` (to=unified-agent, NEW since 07-20):** *Not* untriaged — **undeliverable**. dev-team's drain matches only po/dev-team-addressed rows, so no drain will ever take it. Marking it RESOLVED would destroy the only record that a message was never delivered; NEW costs nothing since nothing matches it. It is now the named acceptance fixture on `FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT`. **Its content is live-costly** (unified-agent still pasting the literal gold >$4,300 L6 token) — relay it at the next unified-agent spawn; do not wait for the contract fix.
- **DISPATCH OWED — agent-father (P1):** `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH`. Trap is armed **on agent-father's own notebook** — repair headings *before* its next notebook write, or history is lost. Heading repair, **not** a prune, **not** a rewrite: zero history may be lost.
- **DISPATCH OWED — agent-father:** `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-LAYER2` (4 exact file:line sites + 3 stale init.md `RULE 1-3` refs), and `FIX-AUDITOR-EVAL-DELTA-RECENCY-BOUND` (MBB is the **named regression test** — the fix must not suppress it).
- **DO NOT DELETE — `docs/signals/cowork-team-20260721T183028Z.json` (0 bytes):** a 0-byte file's forensic content **is** its existence. Fix the writer (atomic temp+rename) and make the drain skip-with-loud-log **first**; only then `git rm` with explicit pathspec. Folded into `CLEAN-COWORK-DISPATCHER-TELEMETRY-DRAIN-DIR` acceptance. Its `>14d` sweep does **not** catch the 07-10/07-11 burst until 07-25 — shorten it or make it count-aware.
- **CARRIED — `GUARD-PRICE-ANOMALY-BYPATH-DISH-CONTRACT` (agent-father, P1):** AC(1) is *re-confirm writer+consumer at source* — do not copy my line numbers. Still **DO NOT ACT ON** `FIX-PRICE-ANOMALY-DISH-SIGNAL-ENVELOPE` (CANCELLED, false premise).
- **CARRIED — dispatch owed:** pm → `DESIGN-COWORK-FANOUT-…-ORDERING` (**T6 first**); ba → `FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET`; dev-mcp-server → `FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW` (P0, both refine slots are no-ops *reporting successful fires*); ops → re-triage `SPIKE-BCTC-EXTRACTION-DORMANT-…-FLOOD`. **HELD AT REVIEW:** `FIX-BCTC-REPARSE-…-NGAYNOP-FLIP`.
- **Lesson:** last tick — convergent reports aren't corroboration when they share a *method*. This tick — convergent **flags** aren't corroboration when they share a *miscalibration*. Both times the fix was the same: **check the other plane, and grep the board before concluding.**
