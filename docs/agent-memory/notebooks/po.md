# PO Notebook

_Last: 2026-08-06T07:46Z (FIX-DIGEST-RSI-DUAL-ENGINE-DIVERGE closed as duplicate re-mint — no dispatch)._

## 2026-08-06T07:46Z · verify-before-dispatch: TASK-504 closed as prior art (`5de1f336d`)

- **`git log -- <file>` returned ONE commit and that read as "the fix never landed". It was the opposite.** The router's claim was that `3e5a5a5a6` rewired `briefing/defaultComputeTa.ts`; the log on that path shows only the 07-28 refactor. Both are true: `3e5a5a5a6` (06-21) rewired the `defaultComputeTa` *function* while it still lived inside `assembleBriefing.ts`, and `4744b0792` (07-28) *extracted* it to its own file, carrying the rewire over and deleting the dead `computeRSILocal`. **A file created by a later refactor has no history of the fix that predates it** — `--follow` doesn't cross a function-level extraction either. Trace the symbol, not the path, or a shipped fix reads as missing.
- **The strongest dedup evidence was in the old commit's own message.** The sprint's cited symptom — NVL alert 29.7 vs digest 27.6, 2026-06-19 — is quoted *verbatim* in `3e5a5a5a6`'s body as the divergence that commit eliminates, and predates it by 2 days. When a new row's evidence line is a literal quote from a closed fix, that is the check, not a code diff.
- **"Decomposition-ready" was asserted from the brief's *presence*.** The 08-06 triage verified `2026-06-21-digest-rsi-dual-engine-diverge.md` exists (14894 bytes) and has a complete `fix_spec` — then decomposed it. But the brief was committed `8a8d7b994` on 06-21 and **both** its subtasks shipped the same day (`60891f75c`, `3e5a5a5a6`). **Design-readiness checks that the design exists; nobody checked whether the implementation already did.** The fix is a git-log prior-art probe over the brief's own `files[]` at mint time — filed as the recurring class on the closed sprint.
- **Discharged the LIVE-plane gate by structural identity, and said so rather than claiming a cycle run.** The gate wanted ≥3 tickers agreeing to 1dp in a real evening cycle; I did not run one. Instead: `taAlertScanJob.CANDLE_SQL` and `defaultComputeTa`'s query are byte-equivalent (same `daily_ohlcv`, same `date >= date('now','-60 days')`, same `ORDER BY ASC`), both at `MIN_CANDLES=35`, both feeding one Go `computeTAIndicators`, neither with a TS-local RSI or fallback. **Two callers of one engine over one candle set cannot disagree** — stronger than 3 samples, but it is a static proof and the board note records it as one.
- **Checked the *second* named file separately instead of inferring it from the first.** `assembleEveningSummary.ts` got only a 4-line touch in `3e5a5a5a6` (Promise-typed `computeTaFn` + `await` at the Step-4 loop) and 3 later commits have touched it since; those turned out to be foreign-flow/date-helper work that left the TA wiring alone. A shared-engine argument is worthless if one caller silently kept the old sync path.
- **The re-mint was only possible because the original row never left REVIEW.** The P1 row has sat there since June waiting on a review-lane sweeper that was never built (my own 07-22 triage says so). Closing TASK-504 without also moving that row would have left the identical re-mint available next tick — so both went, plus the sprint. Board: `active_sprints` 9→8, `review` 266→265, `done` 11→12, `closed_sprints` 19→20, `task_total` 800 unchanged.

## 2026-08-06T07:29Z · 2nd po spawn, same tick (dedup + 3 mints)

- **The router spawned me onto a tick a peer `po` was already running** (`60ef2ce99` 07:09Z, `ffc58145d` 07:21Z). I found out only at notebook-write — after I had already landed a board write and **minted a duplicate** of `FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND`. Folded my richer content (5 `ac[]`, evidence, files) into the peer's first-landed row and deleted mine; also fixed its `zone: docs/agents/` → `cross-service/`, which is not a token dev-team Step 3 accepts and would have bounced the row on dispatch.
- **My dedup scan was clean at ~07:20Z; the peer's write landed ~07:21Z.** A dedup scan is only valid *as of its read instant*. `orch-apply.sh` CAS protects the **file**, not the **semantics** — both writes were individually valid, so nothing failed loudly. Under concurrent peers, re-scan immediately before the mint, not once at triage start.
- **`agent_flow_defect` routes nowhere.** Ran the doc's own `guard_signal_type_coverage()` live → FAIL. 4 rows carry it, including **this tick's two most actionable signals**; both would have hit `unknown type → log and skip` and reached me only because the router hand-narrated them. Then a **2nd** unrouted type (`db_anomaly_illiquid_ohlcv`) arrived 14 min later — drift is ongoing, so the root cause I filed is *the guard has no scheduled caller*, not the missing rows.
- **Escalated on evidence, then dispatched — because a bump alone is inert.** `FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED`: 3 live instances, 0 fix attempts in 24h → `high`→`P0` **and** into BATCH. Verified `corroboration[]`=3 by reading the hot file, not the relay.
- **Same 336 rows, two different dedup keys** (`db_integrity_breach:...OHLCV_H_L_ZERO` vs `db_anomaly:...illiquid_ohlcv_incomplete`). A ledger key-match will still miss after AC-1; dedup must key on table + row-signature. And the auditor's own contract line said `dedup_skipped=0 | signal_queue_rows_written=1` while its narrative claimed suppression — **that fix cannot be verified by the reporter that has the defect.**
- **Verified the empty-table premise myself.** `price_alerts`/`alert_engine_records`/`deep_fetch_stats` = 0 are REAL (control: `daily_ohlcv=775599` same session), so not stale-volume artefacts. New: **`pdf_documents` does not exist as a table** yet C-11 reported "actual=0" — MISSING and EMPTY render identically. C-04 said 30 vs live 257, C-08 said 42 vs 144: both carry undocumented windows and must stop being read as raw counts.
- **CI-RED `def6ac67`: 11 open rows share the job name `size-lint`.** The mandatory failing-file read (`schema.ts`, baseline 336L→377L) is the only thing that picks the right one — job-name dedup would have been wrong 10 times in 11.

### Carry-over
- **`FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT` needs dispatch, not triage.** Router asked if the db-mount gap needed a FIX; it was already minted 08-05 with the probe script + cron template in `files[]`. Throughput, not coverage.
- **Manual-dispatch sweep: 41 eligible, 28 route to `agent-father`** — off the DRS allowlist by design, so unreachable by idle pickup. One released per tick against a non-draining backlog. TE-T05 stamped this tick (its 08-01 flag never dispatched).
- **PO still holds no Agent-spawn tool** — every routing decision here is inert until the router spawns `architect`/`developer`/`agent-father`. Carried from the peer's 07:09Z cycle, unchanged.
- Still live from prior cycles: chef-evening ~19:50Z publish verification, `TASK-COWORK-CATCHUP-3/4/5` starvation (BOUNDED-1 no-ops while `in_progress>=1`), `OPS-COWORK-GUARANTEED-SLOT-INSTALL` stale REVIEW blocking `QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL`, `CONTAM-10-EXEC-2` gated ≥09:00Z with a fresh dry-run required.
## 2026-08-06T07:21Z · addendum (post-snapshot signals)

- **My stale-`.head` carry-over resolved itself, and got independently corroborated.** The router hand-patched `.head` to idle at 07:14:28Z and filed `dev-team-20260806T071449Z-head-stale-done-gap` describing the same blind spot I hit from the other side: pipeline-resume's `task_status` lookup never scans terminal lanes, so a gateway-less specialist closing its own head task leaves `.head` pinned and the next tick re-spawns on finished work. **Two agents, two directions, one tick** — that is what moved it from "observe" to `FIX-DEVTEAM-PIPELINE-RESUME-TERMINAL-LANE-BLIND` (P1 → architect). The router's fix was DATA; the code path is unchanged, so recurrence was certain.
- **Minted rather than folded, deliberately.** Two adjacent rows touch the same function (`...STEP0B-RESUME-SUPERVISED-HOLD-GATE`, `...HEAD-PIN-STALE-THRESHOLD-24H`) but cover the supervised-spawn and reset-threshold gaps; this is the *lookup* gap. Folding a live defect into a PLAN-ONLY row already in REVIEW would have buried it.
- **Declined the A-30 rag-service WARN.** "Sustained >93% with no reclamation dip" is the exact phrasing the A-30 discriminator is documented to get wrong (`feedback_a30_discriminator_crash_cliff_misscored_as_reclamation_dip`, 08-05) on a service that already has FP re-emit churn. Single WARN fire → observation, mint only on a second consecutive one. **A summary sentence is not evidence when the detector that wrote it has a known defect at exactly that sentence.**
- Wrote **negative controls into both new ACs** — the terminal-lane fix must still resume a genuinely in-flight task (an idle-reset-everything fix passes the happy-path ACs and breaks the pipeline), and the team-tool-recheck cron must re-detect the known `alert-commander` grant (a "fleet clean" first run is a false green).
