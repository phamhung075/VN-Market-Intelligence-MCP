# PO Notebook

## 2026-08-24T10:50-11:03Z · Two independent emitters shipped the same bug the same day: a summary template that never reads its own payload

- **Trigger:** router hand-dispatch — 7 `to: po` `signal_queue` rows held at `NEW` across six cowork ticks (09:30Z→10:45Z), because **`po` has no cron-armed dispatch path in any of the three session-armed cron families and zero `cowork-schedule.json` slots**. Not a missed tick; there was never a tick. **Output: 4 orch-apply writes · 7 signal rows dispositioned · 4 mints · 5 folds · inbox 82→27.** `task_total` 808→812, `.head` idle throughout, never written.
- **★ THE HEADLINE — same defect shape, two unrelated emitters, found within one tick.** `bctcImageFetchDegradedSignalWriter.ts` hardcodes `"capped <=0.6 confidence"` into every summary and **never reads a confidence value at all**; `narrative-truth-gate.sh:421-423` hardcodes `"returned non-null data"` and **never reads `returned_summary`**. Both produce rows that contradict their own payloads. One is TypeScript in mcp-server, one is bash in scripts/, no shared code — this is a *class*, not a coincidence, and it is worth a fleetwide sweep for "summary template that asserts a conclusion the payload does not support".
- **★ Verified fact #1 at source and it is worse than the router reported.** Live `get_bctc_refined(1f53ef33…)`: **THREE** units flagged `image_unavailable` (0004/0.75, 0005/0.65, 0006/0.70), not 2 — and **none at or below 0.6**. Mechanism pinned: `shouldSignalImageFetchDegradation()` fires on strict `count===2` (rising edge, by design) and nothing ever amends the row; unit-0006 was refined at 09:04:24, **10s after** the signal's own ts 09:04:14. **The real damage is downstream:** the `<=0.6` cap is asserted in three places (writer header + 2 flow docs) and enforced in **none** — no server-side clamp on the `push_bctc_refined_unit` path — so any consumer filtering `confidence<=0.6` to find degraded units gets **zero** for a report that has three. Silent false-green.
- **★ REFUTED fact #2 on 5 of 6 rows.** Router said all six `narrative_contradiction` rows carry `returned_value: "not found in database"`. **Exactly one does** (`…-62a037`). The other five carry `62.1/62.1/61/61/60` — genuine FAIL-shaped rows. Folding all six identically would have retracted five true findings and preserved one **false accusation against chef**. Split: **1 RETRACTED, 5 triaged/FOLD.**
- **★ CCATO emitter is STILL FIRING — 13th batch, 78 rows.** Git-traced: 72 at `22429c27e` (08:53:31Z) → 78 at `c637f9e69` (08:59:31Z); set-diff returns exactly this tick's 6 ids. That is **8h45m after** the P0 was minted and **7h20m after** it was expedited. All 78 still carry ts `2026-08-24T00:00:00Z` **to the second across 10h27m of wall clock** — the not-a-clock-read proof is now 13 batches, not 3. Still 4 findings, still zero `dedup_key`: **19.5x amplification**. Rewrote `po_expedite` in place (968B replacing 913B) because the row is prose-frozen at 12372B.
- **★ NEW STRUCTURAL FINDING — the manual-dispatch sweep cannot stamp its own #1 candidate.** Step 1 returns **106** candidates; Step 2 mandates stamping the top one; the top one is prose-frozen at 34589B and the four `po_manual_dispatch_*` fields are **absent from `STRUCTURAL_FIELDS`**, so the stamp is net-new prose growth and `orch-apply` hard-aborts. **Probed live, not inferred** — ran Step 2's jq verbatim, got `ABORTED … live file untouched`, `flagged_at` still null on re-read. `flag_reentrant()` only excludes *freshly*-stamped rows, so the row stays at rank 1 forever and **105 candidates (two more P0s) never get a turn**. Second family to hit this gap — `secondary_claimed_*` was fixed for the identical reason on 08-15. Stamped rank-2 instead and recorded the skip on the row.
- **★ Inbox CLEAR: 55 cleared, 27 held back, CI left honestly red.** The guard derives its Pipeline-A live-type set from the array this step clears, so clearing an unrouted envelope is **green-because-the-input-was-deleted**. Unrouted set has grown **2 → 9 in 17h**, and five of the seven new ones (`narrative_contradiction`, `cron_fire_gap`, `db_integrity_breach`, `auditor_cycle_missing`, `system_issue`) **have perfectly good Pipeline-B rows** — they read unrouted only because of the deliberate per-pipeline scoping added 08-22. Correct detector, but it turns every cross-pipeline type into a hand-written bridge row. Recorded on the tactical row; structural answer left with architect/registry.
- **★ Wrote the CLEAR to the FIXED shape, not the documented one.** `FIX-PO-TRIAGE-INBOX-CLEAR-ECHO-PIPE-MANGLES-JSON-UNDER-ZSH-SILENT-NOOP` (P0) says the doc's `echo "$json" | jq` block silently no-ops under zsh behind `|| true`. Read ids straight from the file with `jq`, `printf` not `echo`, dropped `|| true`, post-write re-read confirmed **82→27, zero residual routed types**.
- **Mints (4).** `FIX-BCTC-IMGDEG-SIGNAL-SUMMARY-CONTRADICTS-ITS-OWN-LIVE-CONFIDENCES` (P1, `backlog.509`) · `FIX-SIZELINT-TASKSMDJANITORJOB-1012L-SECOND-CONCURRENT-OFFENDER-BLOCKS-FLEET-PUSH` (**P0**, `backlog.510`) · `FIX-PO-MANUAL-DISPATCH-SWEEP-STAMP-REJECTED-BY-PROSE-CEILING-ON-ITS-OWN-TOP-CANDIDATE` (P1, `backlog.511`) · `FIX-CYCLE-SNAPSHOT-PRODUCER-NAMES-BY-WALLCLOCK-CONSUMER-LOOKS-UP-BY-NOMINAL-TICK` (P1, `backlog.512`). All four paths resolved by jq PATH under `task_board.`, never grep.
- **★ Second size-lint offender is REAL, COMMITTED, and was untracked.** Premise re-verified before minting rather than trusted: `git show HEAD:…/tasksMdJanitorJob.ts | wc -l` = **1012** AND worktree = **1012** (cap 996) — HEAD and worktree **agree**, so this is *not* another working-tree-only phantom. The 02:13Z reading was 906/1012 and was correctly filed as a phantom; that diagnosis is now void. `ahead=213, behind=0`, re-measured (envelope said 144, TNB's 01:37Z ACK said 114).
- **★★ THEN THE GATE CORRECTED ME 15 MINUTES LATER — I almost skipped the push as "known red".** I ran `git push origin main` anyway and the pre-push hook **enumerated its own complete offender set in one line**: `[size-lint] FAIL — 2 offending file(s) (scanned 1422): tasksMdJanitorJob.ts (baseline=906 actual=1012 upper=996); apps/pdf-extractor/infrastructure/ocr_gateway.py (baseline=527 actual=594 upper=579)`. **`pushBctcLayoutHandler.ts` is NOT in it** — it now measures **85L** in HEAD and worktree, down from the 252L its P0 is named after. So the live blocking pair is **{tasksMdJanitorJob.ts, ocr_gateway.py}**, not the pair asserted by the cowork envelope, the 01:37Z TNB ACK, two of my own carry-overs, and the row I had minted 15 minutes earlier. Four artifacts repeating one unmeasured inheritance for three days. Minted `FIX-SIZELINT-OCRGATEWAY-594L-IS-THE-REAL-SECOND-FLEET-PUSH-BLOCKER` (**P0**, `backlog.513`, dev-pdf-extractor — *different zone, different owner*, so one dev-mcp-server hop cannot clear the gate), corrected my own row in place, and stamped the stale P0 without closing it (its lane is `in_progress` and owned by dev-mcp-server; the close-out is theirs, and I cannot tell whether 252→85 is their shipped work or a side effect).
- **★ METHOD, worth more than the finding: when a gate can enumerate its own failures, run the gate.** File-by-file inspection had me at 2-of-2 files with 1 wrong. One real push produced the complete, authoritative set. It also confirmed the serial-offender pattern a **third** time — pushBctcLayoutHandler got fixed and the gate immediately surfaced two more, which is why the new row's AC-5 asks whether baseline-tolerance is a permanent treadmill rather than just fixing file #4.

### Carry-over
- **★ 27 envelopes are deliberately parked and will keep CI red until agent-father lands the routing rows.** That is intended. Anyone who "fixes" CI by draining them has broken the detector, not the bug. `FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-…` (P0, agent-father) carries the full 9-type list and the two-sub-class split.
- **★ Highest-value dispatch is a CROSS-ZONE PAIR — and it is NOT the pair previously carried here.** `FIX-SIZELINT-TASKSMDJANITORJOB-1012L-…` (P0, `dev-mcp-server`, `apps/mcp-server/`) **+** `FIX-SIZELINT-OCRGATEWAY-594L-…` (P0, `dev-pdf-extractor`, `apps/pdf-extractor/`). Two zones, two owners, **neither can clear the gate alone** — this needs a coordinated two-hop dispatch, not a single sprint. **Stop citing `FIX-SIZELINT-PUSHBCTCLAYOUTHANDLER-252L-…`**: its file is at 85L and the gate does not name it. **213** commits exist only on this host.
- **PUSH-BACKSTOP deliberately skipped.** `ahead=212 > 20` so the threshold fires, but the gate is red on two committed files and every attempt writes another `auto-push-abort` (~48/day). Step is documented SECONDARY best-effort and the launchd timer already retries every 30 min — pushing from here adds noise with zero chance of success.
- **`po-decision-bug5468-…` left at `NEW` on purpose.** Addressed `to: ops`, not `to: po`; its own ruling holds it open because the MITIGATION clause is still unactuated. Also still true: **ops has no `signal_queue` selector anywhere in the fleet**, so nothing will ever deliver it.
- **Pre-checks:** signal dashboard ✓ · supervised-goahead ✓ (`.head` idle → no-op, `should_hold` not evaluable) · manual-dispatch-sweep ✓ (106 candidates, see above) · Step 0-TNB ✓ (latest handoff already ACKed 01:37Z, no new findings) · Step 0-SIG ✓ · channel audit **not run** (router scope was the stranded queue + inbox).
- **Did NOT run `scripts/audits/guard-signal-type-coverage.sh`** — it is not read-only (writes live orch-state via `ORCH_APPLY_LIVE_FILE_OVERRIDE`) and `--check` is a no-op alias. Hand-replayed its two extractors instead, same as the 08-23 tick.
