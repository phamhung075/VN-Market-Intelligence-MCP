# PO — Notebook

## 2026-08-23T15:07Z · The guard the last cycle deliberately held CI red for is now green — and the day's loudest incident had the wrong root cause

- **Trigger:** router-direct triage. Inbox 10 (4 bug-escalation, 3 auto-push-abort, 1 brief_complete, 1 ci_red, 1 context_bloat_breach). **Output: 2 orch-apply writes, 5 mints, CLEAR 10→0, 0 duplicate rows.** `task_total` 787→792. Commit `e1d209373`, AC-3 PASS against git HEAD.
- **★ Released the 3 envelopes my predecessor held — but only after proving the fix, not the queue, is what turned the guard green.** Last cycle left CI red on purpose because `guard-signal-type-coverage.sh` reads `pending_triage_inbox[]` as its input, so clearing it games the gate. Commit `3cef7c30e` has since written the `auto-push-abort` row. I verified in the order the trap demands: **grepped the Pipeline-A table first** (present, line 43), **then** ran the guard **while all 3 envelopes were still in the inbox** → PASS. Only then cleared. Guard-green-after-clear would have proven nothing; guard-green-before-clear proves the row.
- **★ CI stays red on origin/main for a reason no board row can fix: the fix is committed and unpushed.** `signal-type-coverage-guard` passes locally at HEAD. `ahead=5, behind=0` — and `fleet-worktree-push.sh` hard-skips below `PUSH_THRESHOLD=20`. So a P0 CI-red fix sits 15 commits short of its own delivery mechanism. **Neither CI-red half needed a new row** — both dedup to open P0 READY rows (`FIX-SIGNALTYPE-…`/`FIX-BASHGRANT-GATE-NEGATED-…`, the latter naming `refine_bctc_md/init.md:80` verbatim). Minting a third would have been the duplicate the file-scoped dedup exists to prevent. The actionable item is a **push**, not a task.
- **★ The day's headline hypothesis was falsified at source, and the real defect was underneath it.** Handed a plausible single root cause: `pdf-extractor` at **99.28 % of 2.5 GiB**, memory-starved, producing zero-extractions. Measured instead — **5 samples over 20 s: 377–428 MiB = 14.7–16.7 %**, `OOMKilled=false`, `ExitCode=0`. Never OOM-killed. The 99.28 % was transient and not reproducible. **But the CPU was pinned at 119–206 %**, and that is the real tell: `parseBctcReport.ts:373-396` blocks a `total_assets<=0` write and `return`s with **no durable failure record of any kind** — the message says *"flagged for manual review"* and nothing is flagged anywhere. So the same pair is re-fetched and re-OCR'd forever. **8 of 8 (ticker,quarter) pairs blocked on 08-22 reappear on 08-23**; GEX 2025-Q4 fired 4× in 36 min. The extractor was not starved — it was **doing the same work over and over**. Correlation was real, causation inverted.
- **★ An 11-day-old worktree is building a board row that is still queued to be planned from scratch.** `.claude/worktrees/agent-ae9ed2cd6f04b3686`: merged branch, `commits_ahead=0` — every main-tree probe calls it prunable. It holds **+231 insertions** and an untracked `FIX-BCTC-NONBANK-OPERATING-PROFIT-EBITDA-SCALAR-ZERO-HPG.test.ts`. **That row is live in `ready[]` right now, `next_agent=ba`.** SALVAGE, and exclude two paths: the ` D apps/mcp-server/data` deletion is a tracked **symlink** replaced by a local dir, and `tool-usage-stats.json` is shared rolling telemetry 11 days stale.
- **Read the worktree's orch-state, not the main tree's, for two whole tool calls.** `cd`-ing into a worktree silently repoints every relative path at an 11-day-old snapshot; it reported the row `IN_PROGRESS` when live is `READY`. **Absolute paths, or verify which tree answered.**
- **Resolver defect confirmed in code, and it is not the row that looks like it.** `devteam-eligibility.jq` `effective_next_agent`/`effective_owner` return the `backlog-detail.json` copy whenever non-empty and **never compare the two `updated_at` stamps both planes carry**. Not `FIX-DRS-CLAIM-TRUSTS-CACHED-DISPATCH-LANE` — the claim script is correct; the resolver is wrong. The PEK instance has since re-converged (both planes `qa`), so I minted on the **mechanism**, not the symptom.

### Carry-over
- **★ Dedup paid for itself: 5 of the 10 inbox entries needed no row at all.** `context_bloat_breach` → open `CLEAN-CTXBLOAT-CRON-COWORK-TEAM-SKILL-*` (file grew 242L→293L, fold). `[sweep-guard] BARE escalated=true` → open `FIX-SWEEPGUARD-BARE-COMMIT-REPEAT-AFTER-BLOCK-*`. `brief_complete` → its §6 rows were **already minted at 14:45Z** by a peer. Both sla-monitor CRITICALs covered. **Reading the board first is cheaper than minting and archiving.**
- **`SAME-FILE DIVERGENCE` is still undispositionable by construction** — its dedup target `FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE` is DONE_VERIFIED and cold-archived, so the rule's "still open → bump" branch is unreachable and the fallback demands an index diff that no longer exists by triage time. Same conclusion as 2026-08-14. **A triage rule whose dedup target can be cold-evicted has a silent expiry date.**
- **117 unresolved telegram reports, not 20** — `read_telegram_reports(status="new")` pages; `list_unresolved_reports()` is the real number (2026-08-22T12:40Z→now). 41 are BCTC-class, all one defect. `FIX-PO-TOOLS-PACKAGE-MISSING-TELEGRAM-REPORT-RESOLVE-TOOLS` already owns why they never clear.
- **`docs/signals/` is 47, not the 76 the 08-22 report claims** — drains caught up. 3 `stranded-state-sweep-*` files are a permanent +3 floor no drain can ever clear.
- **Did not touch:** `qa` (2 RAG rows), `ops`, `agent-father`, the two BLOCKED `in_progress[]` rows (both carry populated `blocked_reason`; BLOCKED is excluded from `wip_in_progress` by construction — prior ruling re-verified, not re-decided).
- **Handoff:** `FIX-CTXBLOAT-SIZEJUSTIFICATION-BYTE-DIMENSION` is READY/developer with no deps — the brief's R1, and R2/R3 are BLOCKED behind it.

## 2026-08-23T14:52Z · CI was red for two unrelated reasons, and my own mandated inbox CLEAR would have hidden one of them

- **Trigger:** router, PRIORITY 1 undrained `ci_red` + two agents-architect briefs routed to po. **Output: 5 orch-apply writes, 8 mints, 4 archived, 1 signal drained.** `task_total` 778→787. Journal: `decisions/triage-20260823T1440Z-po.md`, STEPs `po-C1`–`po-C9`.
- **★ "main has moved past that sha" is not the same as "the tree is green" — re-measure, do not infer.** Signal named `83ab26dc`; origin/main is `429dcf8fa`. Run **32645643303 on 429dcf8fa is ALSO failure**, same two jobs. Then ruled OUT the known self-red divergence by running **both guards locally**: both fail here too, so CI and local agree RED. Only after that did I mint. Fingerprint `ef76ba2e…` recorded in the drained payload — a `ci_red` closed without it re-drains as fresh.
- **★ The inbox CLEAR I am mandated to run every cycle would have turned the guard green while the gap it reports stayed real.** `guard-signal-type-coverage.sh` reads `pending_triage_inbox[]` **as its input**, and Step 0-SIG empties that inbox as its last action. So doing my job gaming the gate is the default path. This is the **inverse** of the documented CI-green/local-red mode and is strictly worse — nothing reports it. The CLEAR contract only licenses clearing **routed** entries, and a type with no routing rule is unrouted by definition. **Cleared 18, held 3, left CI red on purpose.** A coverage guard must measure a doc against a registry, never against a transient queue.
- **★ The bash-grant failure is a pure false positive, and both obvious fixes are traps.** `DEMAND_ERE` substring-matches `git commit` inside the **negated** phrase *"no per-line git commit"* — boilerplate that commit `baa91292c` rolled out fleet-wide. Verified `refine_bctc_md`'s entire corpus has **exactly one** DEMAND_ERE hit and that is it. Granting Bash would launder a detector bug into a real privilege grant on a deliberately Bash-less agent; baselining is barred by the script's own doc (needs a non-null `owning_task`; there is no debt to own). Blast radius: every future Bash-less agent reds on arrival.
- **★ Patching the stale `detail_ref` hit the exact ceiling defect the brief it points to describes.** Row measured **12332 B against a 12000 B growth-only ceiling** — brief §4.7's own failure mode (21 live rows) firing on the row that fixes it. Only way to land a 2-byte ref correction was to **shrink the row**: dropped ~10.5 kB of `evidence`/`question`/`note`/`deliverable` that the 29 kB brief supersedes verbatim → 4241 B.
- **★ Wired a dependency neither brief could wire itself.** filesizecaps §7.2 warns `pm/flow/main.md` is a baseline entry about to be rewritten by the pm-decompose row — snapshot first and the baseline is stale on arrival. Two independently-written briefs; only the board can express the edge. R2 now `depends_on` the pm flow-doc row.
- **Verified the peer PO's 14:10Z ruling instead of re-deciding it.** Wanted to free a WIP slot by moving the watchlist parent out of `in_progress[]`. Re-checked `devteam-eligibility.jq:115-118`: BLOCKED is excluded from `wip_in_progress` **by construction**, and the move hard-rejects anyway (`in_progress[]` is outside `PROSE_CEILING_LANES` ⇒ all ~25 kB reads as net-new). Prior ruling correct. **Untouched.**

### Carry-over
- **★ `TASK_002` would have sat BLOCKED forever.** Its block existed **only as prose** (`blocked_reason: "Depends on TASK_001 landing first"`), `depends_on: null`, no `blocked_by` — `deps_satisfied` had nothing to evaluate, so TASK_001 reaching DONE_VERIFIED would have unblocked nothing. Wired `depends_on`. Inverse of `feedback_pickers_blind_to_prose_status_note`: there the gates could not see a prose STOP, here a prose GO. **Prose-only state is invisible in both directions.**
- **4 of 5 `FIX-SIGNAL-TYPE-ROUTING-GAP-*` rows were already satisfied** — their table rows exist; only `auto-push-abort` is live. Archived the 4. Safe: the guard's dedup scans backlog/ready/in_progress/review/qa only (`:226-228`), never `archive[]`, so a regression re-mints. Root cause filed one level up (`FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE`, architect, plan_only): open producer-side type namespace vs hand-maintained closed allowlist = one CI red per new producer, 5 so far.
- **Second "brief landed, row never re-pointed at it" in ONE cycle** — pm-decompose had a stale ref, `FIX-COWORK-PUBLISHED-MARKER-TTL-…` had `detail_ref: null` despite a 14:04Z `brief_complete`. Both fixed. **A third occurrence should mint a systemic row against the brief→row link step in agents-architect's flow.**
- **★ 5 of 8 minted rows are NOT auto-dispatchable and will sit until the router spawns them by hand.** `agent-father` fails `is_dev_role` (`^dev(-|$)|^developer$`). Ready now: `FIX-SIGNAL-TYPE-ROUTING-GAP-auto-push-abort` (P0, unreds CI) and `FIX-PM-3E-FAILLOUD-HOTFIX` (XS, stops silent board corruption). Developer rows dispatch normally.
- **Verify the auto-push-abort fix by GREPPING the Pipeline-A table for the type, never by the guard's exit code** — an emptied inbox produces a false green. Written into the row's `status_note` and the drained signal's `verification_note`.
- **Did not touch:** `agent-father`, `ops` (PEK + `.head`), `qa` (RAG pair + 4 review rows), `developer` — all live. pdf-extractor incident already ops-owned, not duplicated.
- **This was a scoped triage** — no TNB audit, no channel audit. Peer session `007e33e4` on dev-team; all 5 writes CAS-clean, no retry.
