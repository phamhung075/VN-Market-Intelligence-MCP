# PO — Notebook

## 2026-08-06T21:22Z · triage: FACTORY-ALERT-split-sandbox → DONE_VERIFIED (+occ-4 on existing P1, 0 mints)

### What actually happened
- **Fifth stale `review[]` row in five ticks.** Direct-commit, `branch:null`, sat since 2026-07-24 (13 days). Signed `DONE_VERIFIED`. A pure mechanical file split: `cmd/sandbox/main.go` 571L → `discovery.go` 96 + `exec_primitive.go` 207 + `exec_module.go` 200 + `main.go` 116.
- **All 11 `dev_result` claims re-derived BY EXECUTION, none banked.** `0a961e255` ancestor-confirmed; `wc -l` exact 4/4; `go build`/`go vet` RC=0; `go test -count=1` 7 ok + 2 no-test-files; `golangci-lint` `0 issues.`; `gofmt -l` clean; NBSP 0/0/0/0; sandbox re-run by me → `total=11 pass=11 fail=0 status=OK`.
- **Last tick's carry-over predicted this tick's finding.** Line 29 of the 20:59Z entry: "`rebuild_required` is *asserted* by template, never *derived* from the Dockerfile build target." Confirmed today on live evidence. Carry-over notes are worth re-reading as hypotheses, not just as a TODO list.

### Decisions worth keeping
- **Beat the dev's own equivalence proof instead of accepting it.** The note claimed per-block `diff` = 0. I did something strictly stronger: stripped comment/import/package/blank from `0a961e255^:main.go` and from the concat of all 4 new files, then diffed the **sorted multisets** → **374 ≡ 374, IDENTICAL**. Whole-file and order-insensitive, so unlike a per-block diff it cannot be satisfied by compensating edits in two different files. That is the cleanest refactor evidence class available; nothing was left for QA to add.
- **"Size-justified" is a vacuous claim until you prove the gate saw the file.** A justification header only matters if the scanner scopes `.go`. Checked `INCLUDE_PATHSPECS` has `apps/**/*.go` at `THRESHOLD=120` and that `cmd/sandbox/*.go` escapes `EXCLUDE_PATTERN`, then forced a **positive** scoped run — `PASS — 0 unjustified offenders (scanned 4 files)`. `scanned 4` is the load-bearing token, not `PASS`. (`fleet gate validated on 1 file`, `arg=silent`.)
- **`rebuild_required=true` was FALSE ON THE FACTS — a third, distinct disposition.** Not "user gate waived", not "PO authorises the rebuild". `Dockerfile:21` builds exactly one target, `./cmd/server/`; stage 2 copies exactly `/out/server`; `cmd/sandbox` is its own `package main` so `cmd/server` cannot import it even transitively. The repo asserts it unprompted: `go-analytics-plane.md:182` — *"cmd/sandbox is NOT a server … don't deploy it or expect a port."*
- **Autonomy to authorise a rebuild is not a reason to perform a needless one.** The 08-01 directive retires the user gate, so I did not block — but the right answer was to *disprove the premise*, not to exercise the power. Recycling a healthy 3-week container for a functionally identical binary buys nothing and risks `rebuild_recreate_destroys_peers`.
- **Appended occurrence-4 rather than minting.** Prior-art check first found `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` (P1, ready, `agent-father`, occ 3) already owns the class. Killed my own new-row instinct.
- **But occ-4 is the INVERSE shape of occ 1–3, and that changes the fix.** 1–3: marker true-but-unread → real rebuild skipped → false-green QA. Here: unread **and false** → no rebuild ever owed. So AC-1's predicate ("diff touches `apps/<svc>/` non-test source") is **over-broad** and would have fired `true` on this very row. A gate that routinely demands needless rebuilds trains agents to wave it through — reintroducing the exact false-green class 1–3 exist to close. Refinement recorded: derive the marker from **intersection with the image build graph**, not the path prefix; `cmd/sandbox` (5 services ship one) is the standing AC-3 negative control.
- **Refused to batch-approve the 6 sibling `USER-GATED` rows.** Same retired framing, but a *frontend*/*server-tier* split plausibly **is** in the build graph — the inverse of this row. Generalising a `false` verdict off one verified sibling is the rubber-stamp failure I'd just spent the tick avoiding. Surfaced with the per-row discriminator instead.

### Evidence (raw, re-runnable)
- 1 `orch-apply.sh` pipe — Stage 0+1 PASS, conservation `task_total 761=761`, `signal_total 203=203`, 2 rows stamped, head untouched. Lane diff: `review[]` −1, `done_verified[]` +1, target `in_review:0 / in_dv:1`.
- Filter persisted per convention: `scripts/po-triage-20260806-2122-alert-split-sandbox-signoff.jq`. Ruling: `docs/agent-memory/decisions/ruling-20260806T2122Z-alert-split-sandbox-po.md`.
- **Nearly misread a concurrent peer write as my own bug** — dry-run showed `done_verified` 2→7. Live file had moved 2→6 between my 21:17 read and the 21:24 dry-run. Re-read live before concluding; 6+1=7 was correct. Never diff a hot file against a stale snapshot of itself.
- Title says "565L", `git show 0a961e255^` says 571L — mint-vs-execution drift (`1c45abb1e` grew it in-window). Cosmetic, noted not actioned.

### Carry-over
- **`FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`** (P1, ready, `agent-father`, now occ-4) — carries the build-graph AC-1 refinement + negative-control fixture. This is the fix for the class stranding 6 more rows; it is the highest-leverage row on the board.
- **6 `review[]` rows still on retired USER-GATED framing**: `FACTORY-FRONTEND-split-market-summaries`, `-split-orchestration`, `FACTORY-APIGW-split-capability-prober` (→ops), `FACTORY-ALERT-router-cleanups`, `FACTORY-NEWS-fix-source-logging`, `-go-server-tier-split`. Per-row Dockerfile build-graph check required; do **not** inherit this row's `false`.
- **`FIX-STOCK-PRICE-VNDIRECT-DSI-INV-1-NULL-CHANGE-SERVED-AS-ZERO`** + **`FACTORY-STOCK-extract-vndirect-mapper`** (both P1) — one rewire closes both; nil-safety audit across Go + TS consumers is the risky part.
- **`scripts/audits/po-mint-orchapply-actuator-verify.sh` still unauthored** (main.md:173) — `scripts/` outside agent-father's commit zone, no owner. Unchanged 2 ticks.
- **Two P1 telegram-ack rows still undispatched** (`FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE`, `FIX-PO-MAINFLOW-ORPHANS-TELEGRAM-REPORTS-RESOLVER-SUBFLOW`).
- **AC-3 self-verify still known-broken** (`FIX-PO-AC3-SELFVERIFY-FALSE-FAILLOUD-WHEN-PEER-SWEEPS-ORCHSTATE`) — greps own commit stat, not HEAD tree; asserted against HEAD tree again this cycle.
- **Single-row triage, not a full PO tick** — no channel audit, TNB read, signal-dashboard drain, manual-dispatch/supervised-goahead sweep. Next full tick owes all of those.
- **No `mcp__gateway__call_tool` binding this sub-session** (INV-GATEWAY-1) — calling dev-team dispatcher must release `task:FACTORY-ALERT-split-sandbox`.
