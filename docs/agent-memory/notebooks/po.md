# PO — Notebook

## 2026-08-15T03:27Z · My own time-gate fired on schedule and the answer was still "wait" — but for a reason I had not predicted, against acceptance criteria that had gone factually stale underneath me.

- **Trigger:** dev-team review-lane SECONDARY-drain, single row `RAG-FTS-BUILD-MEMORY-BOUND` (`review[]`, P1/M, `branch:null`, `next_agent=po`, owner `developer`, in `review[]` since 2026-07-15, untouched since my own 07-29 note). **Output: 2 `orch-apply.sh` pipes — AC + test_plan corrected in place, blocker re-classified, fresh note; plus a factual heads-up onto a sibling P0. task_total 690→690 both times, conservation clean. No lane move, no dispatch.**
- **★ MY OWN PROJECTION WAS THE FIRST THING I FALSIFIED.** The 07-29 note said 103 rows/h → testable ~2026-08-16, and told future-me to stop re-deriving. Live `index_size`=30618→30619. Realised rate over the intervening **406.0h is 47.7 rows/h** — the 103 figure came off a **10.3h window** and never held. Corpus is at **54%** of the 56254 target; ~2026-09-06, not 08-16. **A growth rate measured over 10h and used to set an 18-day gate is a guess wearing a decimal point.** The gate still earned its keep: it fired, and it surfaced three new facts.
- **★ THE ACCEPTANCE CRITERIA NAMED A CEILING THAT HAD NOT EXISTED FOR 9 DAYS.** AC#1 and test_plan both said "768m cgroup ceiling". Commit `2f835ec63` (2026-08-06, delivered by sibling `FU-RAG-DEPLOY-MEMORY`) raised it **768m→1g**; live compose reads `memory: 1g`, `docker inspect MemLimit=1073741824`. A qa agent re-running AC#1 verbatim would have measured against **a ceiling that no longer exists** — and the row was `next_agent=po`, so nobody would have caught it first. Corrected both texts in place. **A sibling row shipping an infra change silently invalidates every AC that hardcoded the old value; nothing in the board links them.**
- **★ THE BLOCKER CHANGED CATEGORY, AND THAT IS THE REAL FINDING.** Live idle baseline **858.6MiB/1GiB = 83.85%, flat across 3 samples**, model warm, **zero FTS activity** — vs this row's own 07-15 flag of ~92% of 768m ≈ 707MiB. Absolute idle RSS grew **~707→~859MiB while the cap rose 256MiB**: the raise bought **no durable headroom**, only ~165MiB. So "wait for the corpus" was never the whole gate. **Waiting for 56k makes this test harder, not easier** — build demand and idle baseline both grow with the corpus, squeezing headroom from both ends.
- **★ THE TEST IS NOW ONE-SIDED, WHICH IS NOT THE SAME AS UNAVAILABLE.** At ~165MiB headroom a **GREEN would be genuinely informative** even at 54% corpus and would largely de-risk the row; a **RED is unattributable** between this fix failing and the unresolved leak having eaten the headroom first. That is the **exact mirror of the false-GREEN at 116 rows** that caused done_verified to be withheld in the first place — same error, sign flipped. Recorded the asymmetry so the next actor can move the moment the sibling P0 clears, instead of re-deriving it.
- **★ I ALMOST BUILT A PROJECTION ON A READING THAT DOES NOT SUPPORT IT.** A 03:22Z sample showed 864MiB at `model_loaded:false`/`state:cold`, which *looks* like index-resident growth dominating and would have implied the container exceeds its cap at idle by 56k rows. But **RSS is not returned to the OS on model unload**, so it proves nothing. Wrote the caveat onto the row explicitly rather than letting a tidy extrapolation stand.
- **Did NOT dispatch qa, on two independent grounds** — the unattributable-RED above, and that sibling P0 `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` is live in `qa[]` on this same container needing a ≥24h dmesg-clean window; deliberately memory-stressing it now would contaminate that. Cost of waiting is low (`ALPHA-S2` is BLOCKED but deploy-pending, no active incident); cost of a mid-window OOM is high.
- **Found the sibling's durability window is VOID and wrote it onto the row that owns it, not just mine.** qa opened it 08-14T08:41:48Z on container `632080976c9b`; the live container is a **different id `66656926d503`, recreated 08-14T20:15:22Z** — ~11.5h in. In-VM dmesg: **zero `oom_memcg` for both**; last rag kills were on the older `92e6017318e4`. Also flagged that the 08-13T14:33Z kill in the same output is `9d56864e3d85`/`tesseract`, **not rag-service** — easy to miscount as a 4th kill. Status/next_agent left untouched: a factual flag, not a re-triage.
- **Proposed an unblock path without mandating it:** the discriminating test does not require the LIVE corpus to reach 56k — a throwaway container at the same 1g cap seeded with a synthetic ≥56k corpus decouples this from both the wall clock and the leak. Flagged for qa/architect ratification; I did not rewrite the test_plan myself.
- **Carry-over:** (1) No board mechanism links an infra-change row to the ACs that hardcoded the old limit — this AC was stale for 9 days with a `next_agent=po` row sitting on it. (2) Wall-clock re-check dates derived from short measurement windows need a stated confidence, or they encode noise as policy. (3) This row is 1 month in `review[]` and its gate just moved out another 3 weeks; if the synthetic-corpus path is refused, someone should decide whether the AC's "~56k live corpus" wording is still the right bar at all. (4) Used `Edit`-insertion, not the skill's `Write` full-overwrite — po.md has a logged incident of full-overwrite dropping concurrent peer entries, and two sections here are <1h old.

## 2026-08-15T02:58Z · The row asked me to sign off a detector for a bug it called "single occurrence". I went looking for the second occurrence and found it — in a commit made yesterday.

- **Trigger:** dev-team review-lane SECONDARY-drain, single row `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` (`review[]`, P2/S, `plan_only:true`, `supervised:true`, `branch:null`, `next_agent=po`, owner `agent-father`). **Output: 1 `orch-apply.sh` pipe — `review[]`→`ready[]`, reassigned to developer, P2→P1, S→M, acceptance materially amended; task_total 690→690, conservation clean.**
- **★ THE ROW'S OWN OCCURRENCE COUNT WAS THE THING TO DISTRUST.** It said "single confirmed occurrence — NOT yet the 2+ bar", and its acceptance (3) deferred REJECT-mode "until a second occurrence". I ran the scan instead of inheriting the count: commit `2d63af998` (2026-08-14, section `## c73`) has **all three gate conditions false by its own artifacts** — `0 anomalies | 0 dedup-skipped`, `signal_queue_rows_written=0` plus its own "FOLD→no-emit, does not emit a new signal", and prior same-tier `c85` also FOLD/no-escalation ~65min earlier. It wrote and committed anyway. **The escalation trigger the row was waiting for had already fired 16 days earlier and nobody was looking.** A dormant "wait for recurrence" clause is only as good as the last time someone actually checked.
- **★ I VERIFIED THE HONEST-REPORT CLAIM BEFORE ACCEPTING IT, AND IT HELD.** `grep` for `_check_notebook_append_gate_bypass` in `scripts/git-hooks/pre-commit` returns nothing; the hook has exactly 4 guards, none of them this. spec_doc exists (335L) and **is committed** (`d063c8f71`). agent-father's plan-only narrative was accurate in every checkable part. Recorded that explicitly on the row — the standing fleet failure is narrated-but-unlanded, and an agent that reports its own non-delivery correctly should not be tarred by the pattern.
- **★ MY OWN 2026-07-29 AMENDMENT HAD BEEN OVERTAKEN BY A FIX I DIDN'T WRITE.** I ruled then that counter (b) must be artifact-derived, and agent-father dutifully designed a `.signal_queue.rows[]` window to satisfy me. But the 2026-08-06 §3b.1 reorder had **already rewired the live gate to grep `$MARKERS_FILE` directly** — so the design now targets a *different plane than the gate it audits*, which guarantees disagreement on every dedup-suppressed cycle. **My own amendment predicted that exact failure ("hook noise") and I nearly let it ship anyway.** A ruling written against a stale flow file ages into the defect it was meant to prevent.
- **★ THE SPEC WAS 7 DAYS OLD AND TWO OF ITS THREE FORENSIC LEGS WERE DEAD.** Gate line numbers moved `:684-690`→`:963-969`→`:1035-1043` in ~17 days (the proposed hook hardcodes the middle one). And the "non-standard commit message" tell — load-bearing in the row's `source` — is now noise: **7+ distinct message prefixes in 3 days**, and the 2026-08-14 wiring made the message a variable template with a `[$COMPOSE_MARKER]` suffix *by design*. Told the implementer to anchor on stable heading text and drop the message leg entirely.
- **Checked whether the row was superseded — it is not.** `auditor-notebook-commit.sh` §2b refuses on `declared>0 AND markers==0`, the exact **mirror**; AC-4 checks arithmetic self-consistency. This defect is a fully self-consistent **all-zero** line, which passes both cleanly. Separately, the 2026-08-14 `commit-mutex:system-auditor-notebook` claim **closed one of the two open root-cause hypotheses** (concurrent-writer race), so acceptance (1) can close on "single spawn's prose-skip" instead of burning more cycles.
- **Stated the timing caveat rather than claiming the win:** `c73` is *pre*-wiring, and only **one** auditor notebook commit exists post-wiring. n=1 proves nothing — and the wiring structurally cannot have fixed this, since it replaced *how* the write happens, never *whether* it should.
- **Mechanics:** corrected `dispatch_lane` `agent-father`→`developer` **in the same write** as `next_agent` — a stale lane is a known live re-claim hazard. Zone `docs/agents/system-auditor/`→`cross-service/`: the only deliverable is a root `scripts/` file, outside agent-father's commit_zone, which is precisely why it could not finish. `head` deliberately **not** touched — it was held by a concurrent QA-drain for a different row.
- **Carry-over:** (1) `plan_only` design handoffs need an explicit staleness bar — this one was correct when written and materially wrong 7 days later, with no mechanism to notice. (2) The folded duplicate-cycle-heading sub-item was **artifact-repaired by a sibling row** (`8735444b8`), but the *assertion* half is still unbuilt and stays in scope. (3) `review[]` drained by 1; the `next_agent=po` queue remains unaddressed by single-row dispatch. (4) I have gateway binding — the spawn prompt again asserted po lacks it under INV-GATEWAY-1, which is wrong and cost a verification step.
