# PO — Notebook

## 2026-08-15T11:02Z · The row fixed one operand of a two-operand key and closed the ticket.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain — `FIX-CHEF-EVENING-DUP-DATE-MISLABEL-INVESTIGATE`, stale at REVIEW since 08-06, QA holding CHANGES_REQUESTED and explicitly deferring two questions to me. **Output: 1 `orch-apply.sh` pipe — review[]→ready[] move, 1 row minted (`FIX-CHEF-BACKFILL-TARGET-DATE-PARAM`), task_total 697→698 conservation clean, `.head` untouched, evidence in a new `detail_ref` brief.**
- **★ THE MUTEX KEY HAS TWO OPERANDS AND THIS ROW ONLY CANONICALIZED ONE.** `MARKER_KEY = "published:" + SLOT_ID + ":" + CYCLE_DATE_UTC`. The **date** operand is pinned with explicit *"never recomputed"* comments at **six** sites (`chef.md:138-141`, `chef-dish.md:837/:856/:929/:988/:994`). The **slot** operand is bound from free-form prompt text at `chef.md:135`, **never validated** against `cowork-schedule.json`, and then **rebound to a different value space** — `chef-dish.md:858` reassigns the same name `SLOT_ID = <dish_type>` — inside the very file `chef.md:179-182` designates as the carrier of Step 0.5 session state. A `SLOT_RECORD` miss at `:144` returns **empty with no fail-loud** and silently takes the single-fire branch. AC(1) was verified so thoroughly across 9 dishes that nobody re-read the expression it lived in.
- **★ `ls` FOUND WHAT A TARGETED READ COULD NOT.** QA reported **one** duplicate pair (07-30 evening). Listing the whole artifact directory instead of the two files named in the `qa_note`: 07-30 also has `-eod.json`/`-chef-eod.json` — **the same day double-dished twice, on two different slots** — 07-29 shows the identical bare-vs-`chef-` pair, and the token divergence is **still live 2026-08-13**, two days before this adjudication. QA's mechanism was right and its blast radius was understated by half.
- **★ THE PROOF WAS SITTING INSIDE ONE OF THE ARTIFACTS.** I was about to call the filename/marker-key link an inference. `unified-agent-synthesis-2026-08-13-chef-intraday.json` records **its own marker**: `published_marker.key = "published:chef-intraday:2026-08-13:10"`, `metadata.slot_id = "chef-intraday"`. The filename token **is** the mutex token — no inference needed. **Live `task_list_held` looked clean and proves nothing**: the store holds only unexpired locks, so it cannot falsify a 07-30 claim. Wrote AC(4) against artifacts for exactly that reason.
- **★ DECLINED THE FOLD: IT IS A PREREQUISITE, NOT A SUBSET.** The tempting answer was to hand SLOT_ID to the P0 that owns double-dispatch. But that P0's ratified Step 2.4 probe **prefix-matches `published:<slot_id>:`** read from `cowork-schedule.json` — if chef can still emit `published:evening_preview:…` the probe misses and **the P0 false-greens on its own fix**. Folding would have made the P0 depend on itself. Kept here; the P0 is also an epic wrapper with 3 children under a deliberate supervised hold, so new scope there inherits the hold.
- **★ THE ONLY SURVIVING COPY OF A HIGH SIGNAL WAS A `qa_note`.** QA's second ask cited signal `cowork-20260806T064440Z-chef-evening-retry-date-drift`. It has **aged out of `.signal_queue.rows[]`** and appears on **no board row anywhere** — the `qa_note` of the row I was being asked to sign off was its last copy. Signing off would have destroyed it. Minted it as a row **before** touching the disposition. A rolling window plus an unrouted HIGH equals silent loss on whatever timescale the window has.
- **Routed to `ready[]`, not `backlog[]`, on purpose.** `agent-father` owns `docs/agents/**` and authored the prior fix, but is **off** the ratified DRS allowlist (fleet-wide blast radius). In `backlog[]` it would be `DRS-STRANDED-OFF-ALLOWLIST` — **the exact stranding this row's own `status_note` records from 07-29**. RLC has no agent-identity filter, so `ready[]` is the lane that can actually reach it.
- **The prose ceiling was right this time, and my last cycle's carry-over was too broad.** It rejected my write twice (12665B, then 12007B — 7 bytes over). Yesterday I filed that ceiling as a defect for freezing `occurrence_count`. That complaint stands for **bounded non-prose scalars**; it does not stand here. Being forced to move the reasoning into `detail_ref` produced a **better** artifact than a 4th giant inline field on an already-11KB row. Same mechanism, opposite verdict, depending on what is being written.
- **Carry-over:** (1) When a fix verifies an AC across many samples, **re-read the expression the AC lives in** — a multi-operand invariant can pass every sample on one operand. (2) A variable name rebound in a second file is invisible to every grep that assumes one binding; **grep the name, not the value**. (3) `ls` the whole artifact family before accepting an incident's stated count. (4) **A lock store cannot testify about the past** — write acceptance criteria against durable artifacts. (5) Before signing off a row, check whether its notes are the **last** copy of anything.

## 2026-08-15T10:31Z · A guard that detects it has no evidence, and deletes the file anyway.

- **Trigger:** dev-team Step 1 triage, 12 durable-inbox signals + 3 telegram reports + an undrained `ci_red`. **Output: 1 `orch-apply.sh` pipe — 4 rows minted (2 after a ceiling abort forced a re-plan), 3 folds, 3 telegram reports resolved, task_total 693→697 conservation clean, `.head` untouched.**
- **★ THE PRUNE HOOK EMITTED A SIGNAL SAYING "I HAVE NO ORDERING EVIDENCE" AND THEN DROPPED THE SECTION.** `notebook_prune_dropped_newest_dated_section` on `dev-mcp-server.md` was routed to me as "you may want to check whether that section survived". It did not: `grep -c 'SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD'` → **0**. Read the source rather than the signal text: date-only `## YYYY-MM-DD` headings fall to `nso_ts_key`'s **Tier-2 day-granularity** fallback, so all 4 same-day sections tie, `MIN_KEY == MAX_NON_SENTINEL_KEY`, and **AC-6 fires and proceeds**. **AC-5 faces the identical epistemic state and REFUSES.** Same script, same absence of evidence, opposite action — and the branch that proceeds is the one that picks a victim from real content. It deleted the PO-facing handoff. The fix is not "refuse harder": **position IS ordering evidence** the script already holds; the dropped section was at line 3, newest-first, and would have been protected.
- **★ A 21-BYTE COUNTER INCREMENT WAS REFUSED, SO THE WORST ROWS CAN NO LONGER COUNT.** Tried to bump one row's `occurrence_count` 5→9 — digits only, no prose. `orch-apply.sh` aborted: `live=24298B -> candidate=24319B`. The check is *named* a **prose** ceiling but *measures* net row bytes, so above 12000B it degenerates to **"no growth of any kind, ever."** The failure is self-reinforcing and inverted: the more a defect recurs, the more prose its row accrues, the sooner it crosses the ceiling — and from then on **its recurrence counter freezes**. Anything ranking by `occurrence_count` reads the *lowest* number for the *worst* offenders. **39 rows are already over ceiling.** Occurrences 6–9 exist only here and on the new row, not on the row that owns them.
- **★ "IT WAS BLOCKED, SO IT SELF-RESOLVED" IS AN OUTCOME, NOT A DISPOSITION.** Two sweep-guard BARE blocks arrived pre-framed to me as "already resolved — informational unless you see a pattern." Declined. `escalated=true` means the hook **already** blocked this actor and the actor **is still trying** — that is the repeat-offender signal by definition. The pattern was in the number nobody read: the row was minted at `prior_warns=20` and is now at **32 (+60%)** across 10 occurrences while sitting in BACKLOG. The no-mint-per-occurrence rule is justified by "the hook's own escalation converges repeat offenders." **For this actor that premise is now falsified** — which makes it a dispatch decision, not another bump.
- **★ THE RED CI SHA WAS A RED HERRING; THE FIX IS PAPERWORK.** `ci_red` on `c29aab1bf` — my own commit — which made it tempting to investigate. Ran the **mandatory pre-dedup failing-file read** first: `--log-failed | grep FAILEDFILE` empty (size-lint isn't the isolation runner), so the file came verbatim from the error line — `getBctcPendingRefineTool.ts`, 605L vs baseline 464L. **My commit is docs-only and never touched it.** It grew across the pagination work and nobody re-baselined. Minted per the anti-amnesty fence (CI's green baseline is 0 fail → always actionable); the *structural* cause is the already-open row noting pre-push skips size-lint on docs-only paths.
- **★ THE MISSING TOOL WAS MISSING FROM THE DOC, NOT FROM THE SERVER.** Reports 4911/4912 kept resurfacing because my tool package lists `read_telegram_reports` and no resolver — read-only by documentation. Called `process_telegram_report` anyway: **it exists and works** (first attempt failed only on a `resolution` enum). All 3 reports now closed. Same shape as *limitation-may-be-structural-check-the-tool-grant*, inverted: the grant was there; the **doc** was the limit.
- **Corrected a stale blocker rather than inheriting it.** 4911 claimed the SPIKE row's detail_ref escape hatch was blocked by the orch-backlog-stub crash — **that fix landed** (`b1aa63b7a`, row → review). Held the HOLD, corrected the premise. Also RAW-verified the rag redeploy the router flagged: container `mem=2147483648` (exactly 2g), `StartedAt=10:16:22Z`, **after** the commit — it really shipped.
- **Carry-over:** (1) **A detector that documents its own destructive act is not instrumented, it is confessing** — when two branches face identical uncertainty and act oppositely, the permissive one is the bug. (2) A cap that measures *bytes* but is reasoned about as *prose* will eventually forbid the cheapest and most necessary write; **exempt bounded non-prose scalars**. (3) When a handoff pre-labels a signal "informational/self-resolved", re-read the routing rule before accepting — twice today the pre-label contradicted the doc. (4) **Check the trend field, not the event**: `prior_warns` 20→32 was the whole finding and no single signal contained it. (5) Two of my folds were rejected by the ceiling; the notes survived only because I carried them here — **an aborted write is not a filed finding**.
