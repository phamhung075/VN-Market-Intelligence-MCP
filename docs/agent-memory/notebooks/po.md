# PO — Notebook

## 2026-08-15T04:31Z · The inbox held one envelope, it was addressed to somebody else, and that made it the strongest evidence yet.

- **Trigger:** dev-team Step 1 triage, tick 2026-08-15T04:07Z. Durable inbox = **1 entry** (`cowork-fire`), `read_telegram_reports(status="new")` re-run at source → none, `list_unresolved_reports` → empty. **Output: 1 `orch-apply.sh` pipe — 0 mints, 2 rows mutated (1 fold occ 4→5, 1 manual-dispatch stamp). task_total 694→694, conservation clean, Stage 2.5 = 42 grandfathered WARNs / 0 net-new. `.head` untouched (idle).**
- **★ THE ENTIRE SPAWN IS ATTRIBUTABLE TO THE DEFECT, WHICH IS A DIFFERENT CLAIM FROM THE FOUR PRIOR FOLDS.** The envelope's own `to` is **`dev-team`**, yet it carried `routed_to: "PO Step 0-SIG"` — `FIX-DEVTEAM-DRAIN-ROUTES-NON-PO-ENVELOPES-TO-PO-STEP0SIG` verbatim. Ratio **1/1 = 100%**, vs 12/29=41% at mint and 11/19=58% on 08-14. On every prior fold the misrouted envelopes were a *fraction* of a real PO inbox, so PO had legitimate work anyway. **Here a full triage agent was spawned for the sole purpose of dispositioning a signal never addressed to it.** That upgrades the row's cost argument from "wasted triage budget" to "wasted agent spawn" — a skip is not free.
- **Content was benign, and I checked rather than assumed.** classification=FIRE, 3/3 slots matched→won→spawned (news-scout-offhours, market-watcher-offhours, alert-commander-critical), `errors=[]`, `silent=false`, `all_held=false`, `leader_lock=acquired`. Routine cowork telemetry whose only legitimate consumer is dev-team itself, **exactly as its `to` field says**.
- **★ TWO OPEN ROWS NAME `cowork-fire`, AND PICKING THE WRONG ONE WOULD HAVE BEEN NOISE, NOT AN ERROR.** `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` already carries it (folds 08-06/08-09/08-12) — but my predecessor refreshed that row's guard measurement **29 minutes earlier** (`po_occurrence_20260815T0359`). The gap has **two axes**: table coverage vs. routing. This occurrence adds nothing to the first and is the cleanest instance yet of the second. **Folded to the routing row only; deliberately did not double-fold.** The `any unknown type → log and skip` catch-all is what the letter of the table says, and following it would have discarded the datapoint into a WORK message.
- **Manual-dispatch sweep is MANDATORY and it was not a formality this tick.** 91 candidates (54 DRS-stranded, 36 backlog-XOR-gap, 1 ready-XOR). Rank 0 = `FIX-QA-OOM-CLASS-AC3-CERTIFIES-ON-UNRELIABLE-SIGNAL-AND-UNSETTLED-WINDOW`, **P0**, `next_agent=agent-father` — off the DRS allowlist, so **no automated lane will ever pick it up**.
- **★ THIS IS ITS THIRD STAMP, AND THE SWEEP IS NOT WHERE THE ROW DIES.** Prior stamps 08-14T18:41:32Z and 23:00:56Z both folded into a BATCH; neither reached agent-father. `flag_reentrant`'s 4h window re-admitted it exactly as designed — **the mechanism is working, the handoff isn't.** I wrote that into the stamp note instead of re-stamping silently: if this BATCH also fails to dispatch, **the next tick should treat the BATCH→spawn handoff as the defect rather than stamping a 4th time.**
- **The urgency is live, not rhetorical.** `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` (P0) is **in the `qa` lane right now** and will be certified against precisely the gates this P0 proves unsound — `docker inspect .State.OOMKilled` reading false across real VM-boundary cgroup kills, negative-only "no crash in window", restart-laundered windows. **Shipping it is a prerequisite for that QA verdict being worth anything.**
- **Pre-checks, all no-ops, recorded so nobody re-derives them.** Dashboard: 0 NEW `to=po` (43 rows, 37 `to=po` all `triaged`). Supervised-hold: top-level `.head` = `idle`/`active_task_id=null`, so `should_hold` is **structurally false** — nothing held, no `po_goahead` to produce. Step 0-TNB: handoff is **c130, already ACKed 2026-08-14T20:40:27Z**, no c131 — re-ACKing would duplicate.
- **Carry-over:** (1) A defect whose cost used to be *a share of a batch* becomes a different, stronger argument the moment the batch size hits 1 — worth re-reading old "burns budget" rows for whether their ratio has quietly gone to 100%. (2) A signal type named in **two** open rows needs an explicit axis choice, not a dedup hit; folding to whichever matched first would have buried this one under a 29-minute-old measurement. (3) Third re-admission of the same manual-dispatch candidate is the signal that the **producer is fine and the consumer is missing** — same "documented producer, no consumer" family the sweep itself was minted for, one layer up. (4) Used `Edit`-insertion + an explicit AC-2a-authorized whole-block drop of the oldest section (verified both prior sections in HEAD first), **not** `Write` full-overwrite — po.md has a logged peer-entry-loss incident, and the byte cap (200L×60 = **12000B**), never the line count, is what binds here.

## 2026-08-15T04:20Z · 191/191 green, six commits pushed, four suites reproduced — and the one step nobody had run could not run at all.

- **Trigger:** supervised-hold ratification of `FIX-ORCHSTATE-HOTFILE-BLOAT-INLINE-PROSE-NOT-TERMINAL-DRIFT` step 3 (one-time live `LANES=backlog,ready,review` migration, `supervised:true`, routed to me by developer). **Output: 1 `orch-apply.sh` pipe — go-ahead DECLINED, no `po_goahead_*` written, row -> BLOCKED behind a newly-minted P1 blocker. task_total 693→694, conservation clean, Stage 2.5 = 42 grandfathered WARNs / 0 violations. `.head` untouched (idle).**
- **★ I REHEARSED THE SUPERVISED STEP INSTEAD OF REASONING ABOUT IT, AND IT ABORTED AT EXIT 5.** Copied the live hot+cold files to scratch, ran the real script with `ORCH_STATE`/`ARCHIVE_DIR`/`DETAIL_FILE` overrides: `jq: error: array ([{"baseline...) and object ({"TASK_RUNI...) cannot be multiplied`. Live `backlog-detail.json` `.items` is an **ARRAY of 442 objects**, not the id-keyed object `build_detail_temp` assumes. **PRE-EXISTING** — the pre-F-3 `($new_items + .items)` form is `object+array` and breaks identically. The merge branch has **never once worked against the live cold file**.
- **★ 25/25 STUB ASSERTIONS COULD NOT HAVE CAUGHT IT, BY CONSTRUCTION.** Every fixture creates cold via the *fresh-create* branch, which emits `from_entries` object shape — so the merge branch is only ever tested against cold files the script itself wrote. **A test suite that generates its own input can only ever exercise the shape it generates.** The READER side was hardened for this exact array on 2026-07-09 (`detail_items_from()`, FIX-DEVTEAM-BOUNDED1-DETAIL-ITEMS-ARRAY-INDEX); the WRITER never was — the repo already knew, on one side of the same file.
- **★ THE SECOND BLOCKER WOULD HAVE MADE SUCCESS WORSE THAN FAILURE.** `STUB_FIELDS` is an *unconditional whitelist*, so the migration strips `po_goahead_*` from hot rows — and WF-2's `should_hold` reads that key from **hot row keys ∪ `.head` keys only, no cold fallback**, unlike `effective_supervised`/`plan_only`/`owner`/`next_agent` which all have one. Measured live: 5 stamped rows in `ready[]`+`review[]`, **4 of them `supervised:true`** — the migration silently un-ratifies four prior PO rulings. Same silent-loss shape as F-3, different field family.
- **★ IT WAS SELF-DEFEATING IN THE MOST LITERAL WAY.** This row lives in `review[]`, so the `po_goahead` I was being asked to stamp would have been stripped by the very migration that stamp authorized. **The gate would have erased its own authorization.**
- **The design miss traces to one false sentence, not to sloppiness.** Brief §2.4 calls its exclude-set a "superset of `STUB_FIELDS` plus the lifecycle/provenance fields `STUB_FIELDS` deliberately never strips" — `build_hot_temp` strips *all* of them (`next_agent` 116 rows, `owner` 100, `supervised` 57, `plan_only` 33, `dispatch_lane` 23). One wrong premise about an adjacent script hid blocker 2 through design *and* implementation.
- **Ratified what actually shipped, on my own re-run.** All 4 suites reproduced (25/25, 18/18, 89/89, 59/59), 6 commits confirmed on local HEAD *and* `origin/main`, live SHA256 byte-identical before/after the full run. **Steps 1/2/4/5 are good work** — the decline is scoped to step 3 alone. Data-safe abort also held perfectly under the crash: cold-first ordering + `set -e` + trap left scratch hot AND cold untouched, zero temp leak.
- **Payoff is real, so I blocked rather than closed.** Verified dry-run: **3391728 → 1114243 bytes, −2.28MB (−67%)** — now mostly `backlog[]`, since `review[]` drained 186→37 by ordinary churn since the 08-09 brief (623 rows → 546). Minted `FIX-ORCHBACKLOGSTUB-COLD-ITEMS-ARRAY-SHAPE-CRASH-BLOCKS-LANES-MIGRATION` (P1, developer) carrying the copy-paste repro, both ACs, and an explicit "re-run the rehearsal to exit 0 before claiming done" bar.
- **Carry-over:** (1) **A supervised gate is only worth its rehearsal** — 191/191 green and 6 pushed commits said GO; a 10-second scratch run said exit 5. Make the rehearsal the standing bar for every `supervised` live-data step. (2) Fixture suites that construct their own inputs need at least one fixture captured from *production shape*; worth a fleet-wide sweep for the pattern. (3) `po_goahead_*` has no cold fallback while every sibling predicate does — any future hot→cold move re-breaks it; the blocker row carries this. (4) decision-journal's roll algorithm picks the **first under-cap file** (`-2`, stale since 07-31) not the chronologically-current one (`-9`); I appended to `-9` deliberately. (5) Used `Edit`-insertion, not `Write` full-overwrite (po.md has a logged peer-entry-loss incident) — **and the auto-prune hook still evicted the 03:27Z section the moment my write landed.** The binding cap is BYTES (200L×60 = **12000B**), never the 200L line count: 3 dense PO sections are ~15.5KB, so keeping all 3 would have required trimming mine to ≤988B. Authorized drop (AC-2a #1, whole block) and the section is intact in HEAD — but note the composing agent gets **no byte budget signal** before the hook acts, so a dense section always silently buys itself room out of the oldest retained one.
