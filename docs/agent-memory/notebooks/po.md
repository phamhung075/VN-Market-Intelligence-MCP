# PO Notebook

_Last: 2026-07-29T08:15Z (A-21 predecessor-bound: architect brief in, implementation split minted) · 2 rows minted, parent re-dispositioned to umbrella, 3 corrections written against the brief and my own 07:21Z ruling._

## This cycle

- **I checked the recurrence count instead of spending it, and it said something different.** The row carried `recurring_bug_count: 3` and I was handed "three prior fix cycles, this would be the fourth". Git says otherwise: `babe54c6c` is a **FEAT that introduced** the 2× lookback; `2d494f77e` and `ff874c821` are **same-day commits sharing one task id**, and **neither touched the bound**. That is one feature plus one two-commit cycle — not three failed fixes. The defect was never diagnosed once, survived two adjacent edits, then **multiplied** by hand-copy into the 2026-07-23 brief and from there into `tier1-probe.md`. The multiplier is **replication, not re-breakage**.
- **That distinction picked the fix.** "Three failed fixes" would have justified re-architecting the crash predicate — scope inflation, since the predicate is fine and `LAG` corrects the window mechanics completely. The evidence-supported structural move is **de-duplication**. So child 2 is deliberately **not** a port-the-corrected-SQL row: it **deletes** the second copy and calls the exported function. Shipping corrected SQL into a second hand-maintained copy would fix today's arithmetic and leave the replication mechanism **re-armed** — the third hand-port of this predicate, chosen with full knowledge of what hand-porting already cost.
- **The brief asserted a verified premise that is false, and it would have caused the same bug in the test plane.** §6: "No test file for `restartCadenceAlertJob.ts` exists today (confirmed by search)" → create a new `2028-` file. It exists: `FIX-MCP-CRASH-LOOP-A-restart-cadence.test.ts`, **332L, 10 cases**, importing that exact function, already carrying the `makeDb`/`insertStartup`/`nowMinus` convention the brief went hunting for elsewhere. A second test file for one module is the *same duplication pathology*, one plane over. Overrode it: extend the existing file.
- **Read the existing suite rather than assuming it pins the bug.** It does not — `:320` "no predecessor in history" hits the bootstrap guard and stays green under `LAG`. The real hole: **no fixture places an in-window candidate whose predecessor lies beyond the 8h bound**. And `:253` has a `nowMinus(9)` predecessor that is currently *never fetched*, so it passes **partly for the wrong reason** — flagged as must-confirm-not-assume, and must not be edited to pass.
- **I was wrong at 07:21Z about the word "new".** I called the sentinel interaction a *new* false-positive class and blocked on it. The row's own evidence falsifies me: at HEAD the effective rule is "3 restarts in 8h", so the FP path is **already reachable**. The fix lowers an accidental bar back to the documented one — it changes false-positive **likelihood, not existence**. Withdrew the blocking framing; adopted ship-together.
- **But the brief left the hole my block was covering.** It rejected option (c) ship-unconditionally, adopted (b) ship-with-a-guard, then made the guard "a recommendation the implementer may decline". Declinable guard ⇒ (b) silently collapses into the (c) it just rejected. My block is lifted **conditional on the guard existing**, so I elevated it to a required AC. Wording is the implementer's; **presence is not**.
- **Found a trap the brief did not have: the fix can look shipped while the running image still under-counts.** The probe imports from *inside the container*, the mcp-server deploy is user-gated and market-hours-fenced, so after the SQL is deleted a reader greps a clean import and concludes A-21 is fixed. Added a deploy-lag honesty marker, and a hard AC that **import failure maps to `TOOL-UNAVAILABLE`, never `crashRestarts:0`** — a catch degrading to zero is a silent false negative of the exact class this task exists to remove.
- **Two rows, not three; every one carries `next_agent`.** Parent is not a new mint, so keeping it as a dependency-gated umbrella costs no lane growth and preserves AC(2) "must not diverge", which is checkable across both diffs and inside **neither child**. Stranded `FIX-AUDITOR-*` count unchanged at **11** — this split did not add a twelfth.

## Carry-over

- **Verify the brief-override actually reaches the implementer.** `dev-mcp-server` may open §6, read "no test file exists", and mint `2028-`. The correction lives in `po_correction_20260729T0812` **on the row**, not in the brief. Same failure shape as the AC(5) amendment and the `next_agent`-unset finding: **the correction existed and was never reached.** If it is missed, that is a routing defect in how row-level overrides are surfaced — not a fresh row.
- **Watch for the quiet hand-port.** If child 2 returns with the `LAG` SQL pasted into the heredoc "because the import was fiddly", reject it — the fallback was removed from implementer discretion on purpose and returning to PO is the required move.
- **Standing check earned last cycle held up twice today.** Re-derive handed arithmetic *and* handed history from the artifact before acting. `recurring_bug_count`, "confirmed by search", and "three prior fix cycles" were all assertions; two of the three did not survive a `git log` and an `ls`.
- **A-30 row now 4d+ old, `recurring_bug_count=6`, missed twice** (4th consecutive carry). Threshold reached: still BACKLOG with no architect activity next tick → **escalate to user**; do not re-attach evidence a fourth time.
- **`supervised = never dispatched` watch, 3rd tick.** Both children are `supervised:true` and neither is `plan_only`. If they sit untouched alongside the notebook-skill row, that is the routing defect escalating — not another row.
- Standing, 11th carry: verify my own commits carry my authorship (`git log --author`).
