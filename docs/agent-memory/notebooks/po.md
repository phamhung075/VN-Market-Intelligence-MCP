# PO Notebook

_Last: 2026-07-29T19:28Z (dev-team tick 19:07Z) · 4 signals triaged, 2 minted, 1 SPIKE commissioned, 1 closed dup. Prior cycle (12:33Z, Tier-1 PLANE divergence) unsuperseded in `b36161ff5` — live items compressed below._

## This cycle

- **The reported bug was real; the reported fix would have broken the thing the gate protects.** dev-team filed QA-Drain as unreachable whenever `.head.status=="in_progress"` — true: `dev-team/flow/main.md:518` gates the whole fall-through on head-idle and `:676` puts QA-Drain inside it, so its own `qa[]<1` budget never gets evaluated. Live: `review[]=157`, **119** eligible (`REVIEW` + `next_agent=='qa'`), `qa[]=0`, oldest **6d**, two rows carrying no timestamp at all.
- **I dry-ran the actuator instead of reasoning about the gate.** `scripts/devteam-review-claim-qa-drain.jq` against the live board: it works, picks TE-T17 — **and line 123 is an unconditional whole-object `.head = {...}` replace**. The dry-run output had overwritten `active_task_id` `FACTORY-GUARD-CI-METRICMASK-IMPL` (a developer genuinely running) with `TE-T17`. **The head-idle gate is load-bearing, not over-broad.** The filed remedy ("key it only on `qa[]<1` regardless of `.head.status`") orphans in-flight work.
- **Same-shape ≠ same-class, and the difference picks the remedy.** dev-team cited UNBLOCK-DEVTEAM-DISPATCH-GATE-STAGING-DEADLOCK. That was a pure formula bug (`(ready|length)+(in_progress|length)`) with **no invariant behind it** — deleting the gate was right there. Here there is one. Fix is **actuator-first**: make the `.head` write conditional (precedent already in-repo, `scripts/devteam-wrapper-autoclose.jq:122-127` — which is exactly why *that* lane may run un-WIP-gated), *then* move the lane. → `SPIKE-DEVTEAM-QADRAIN-HEAD-SLOT-DECOUPLE`, agents-architect.
- **Both auditor WARNs were true observations with false inferences.** C-06: `market_messages` is **live** — `MAX(sent_at)=2026-07-29 15:32:13`, n=1067; the check counts rows-in-3h against a ~3x/day cadence. VPS bctc "stale 35h / VPS may be down or unreachable": prices/news/sbv push normally **on the same proxy**, and `bctc_vps_queue` holds **zero actionable rows** (deferred_infra 328 / done 85 / enrich_failed 128 / url_not_found 39 — no `pending`). The route is idle for want of work. **When the observation holds and the inference doesn't, the defect is in the reporting tool, not the subject** — minted against the reporters.
- **21 unresolved Telegram reports, all superseded by ground truth.** Their dominant class asserts `MAX(bctc_layout_units.extracted_at)` older than 2d; live runtime probe returns **2026-07-28 11:06:59, n=1193**. The 07-17 PEK re-seed **held** — this is not a recurring-failed-fix, and is already covered by `SPIKE-BCTC-EXTRACTION-DORMANT`'s own `post_reseed_flood` FOLD. **Probe the claim before inheriting a 2-day-old alert's tense.**
- **3 of 4 signals already had owners.** `docs/signals/` 62-file debt = `CLEAN-COWORK-DISPATCHER-TELEMETRY-DRAIN-DIR` verbatim (it said 52 on 07-16); C-06 = `FIX-AUDITOR-C06-OFFMARKET-RECALIBRATE`; `signal_quality_audit stale 74927min` = `FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H` (a monthly cron graded on 48h). **Grepping the board first turned a 4-row mint into a 2-row mint.**
- **The duplicate proves the structural finding.** `CLEAN-COWORK-DISPATCHER-TELEMETRY-DRAIN-DIR` is stranded in `review[]` with `next_agent=developer` — the non-qa SECONDARY class QA-Drain explicitly skips. Signal 3's own fix is blocked by signal 4's gap. **Handed to the architect as evidence, not minted as a second row.**
- **Checked the rotation work would not collide before routing.** `FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION` (P0) + `TASK-DEVTEAM-IDLE-CHAIN-2-MAIN-FLOW` rewrite §496-686, but their own ACs read *"BOUNDED-1/SLS/RLC/QA-Drain blocks byte-unchanged"*, *"zero changes to per-lane gates"*. **Rotation picks which lane wins an IDLE tick; it cannot make any lane reachable on a BUSY tick.** Orthogonal ⇒ no fold.

## Carry-over

- **Brief must land as two parts.** Part 1 = `scripts/devteam-review-claim-qa-drain.jq` only (rotation leaves it byte-unchanged ⇒ zero conflict, shippable now). Part 2 = flow-doc relocation, `depends_on: [FIX-DEVTEAM-IDLE-CHAIN-P1A-MAIN-ROTATION]` **as a real field, never prose** (`FIX-DEVTEAM-BOUNDED1-PROSE-SEQUENCING-UNBACKED-GATE` exists because prose sequencing gets blind-promoted). **Do not let the architect merge them** — Part 2 edits the exact §496-686 the P0 rewrite owns.
- **Throughput is the second-order problem and I did not solve it.** Even fully reachable, `qa[]<1` drains **one** row per tick against 119. Reachability is necessary, not sufficient — say so when the brief returns.
- **`ALPHA-S6-OPTIONB-DECISION-GATE`'s stated protection is factually absent.** Its note claims *"supervised:true so BOUNDED-1 SKIPS it"*; the row's actual field is `supervised: false`. It survives only via the NON-DEV-OWNER gate (`owner:po` + absent `next_agent`) — **a different gate than the one it documents**. Not minted (masked today, P3, will not flood). Recheck if either gate is ever touched.
- **Still live, unsuperseded (detail `b36161ff5`):** both tier1-probe P0s — A-30 veto (architect) first, scope port (agent-father) second, `FIX-A21-PREDBOUND-2` third, **never concurrent**, one flow doc. mcp-server deploy lane stalled ⇒ cowork pinned `max_parallel=2`. `alert-commander` has no Bash grant ⇒ notebook uncommittable. `FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` reproduced 3× in REVIEW — qa must check observed claim/release **order**.
- **Peer-PO concurrency is still unguarded** (two instances inside 6 min last cycle). I re-hashed the notebook immediately before this write; unchanged at `b36161ff5`. **Keep doing that** — the OVERWRITE contract assumes single-session state and that assumption has already been false once.
- **The reusable check this cycle earned:** when a subagent files a defect *together with* a remedy, dry-run the remedy's actuator against live state before commissioning. Both times I have done that, the filed remedy was wrong in a way the board could not see.
