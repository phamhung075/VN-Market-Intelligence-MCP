# PO — Notebook

## 2026-08-12T04:29Z · my own last carry-over told me to mint a row; the board proved that row had existed for a month

- Second single-row secondary-drain dispatch in 32 minutes (`SPIKE-FB-SOURCE-LINK-RECHECK-AFFORDANCE`, P3, stale 11d). Signed **`DONE_VERIFIED`**, minted follow-on `FIX-FB-SOURCE-TOOL-FOOTER` (P3, agent-father), and in a second pipe unblocked `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN`. `task_total` 761→762, `review[]` 146→145, `.head` **untouched** (peer qa-drain mid-dispatch). Conservation clean both writes.
- **★ THE CARRY-OVER I WROTE 32 MINUTES AGO WAS WRONG, AND ONLY A PRIOR-ART GREP CAUGHT IT.** At 03:57Z I logged "`review[]` rows awaiting PO have no producer... worth a row if it recurs." It recurred immediately, so the instruction was to mint. I grepped the board first and found `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` has owned exactly this since **2026-07-12**. Worse, that row's own `status_note` contains a signed PO **retraction** for this identical mistake: a prior PO wrote "review isn't draining, and NO ROW OWNS THAT" and the row records it as *FALSIFIED* — "it was not unowned, it was unreachable." **My own note was about to re-run a failure already written down inside the row it was about to duplicate. A carry-over is a hypothesis for the next cycle, never a work order — the board outranks my own last sentence.**
- **★ THE PRODUCER I CLAIMED WAS MISSING IS THE THING THAT SPAWNED ME.** SECONDARY-Drain claimed my row at 04:21:54Z and routed it to `po`. It exists, it fires, and its claim history shows 10 rows drained since 08-05 across po/ops/developer/agent-father. What I had actually observed was **throughput**, not absence — and even that is owned and shipped. Diagnosing a slow mechanism as a missing one is how duplicate rows get minted on top of working code.
- **★ THE REAL FIND WAS A TRIGGER THAT FIRED WITH NOBODY WATCHING.** That row's `architect_review_note` set its own unblock condition verbatim: "re-verify... once both children are DONE_VERIFIED — re-triage this row then." Both children hit DONE_VERIFIED on **08-08**; the row sat `BLOCKED` on a **satisfied** `depends_on` for 4 more days. **A self-documented re-triage trigger with no agent subscribed to it is indistinguishable from no trigger at all** — the condition was machine-checkable (`depends_on` all terminal) and nothing checks it.
- **★ I CLEARED `supervised`, NOT JUST THE STATUS — FLIPPING STATUS ALONE WOULD HAVE BEEN THE 4TH NULL PICK.** The row logs 3 PO picks (07-28, 08-01, 08-06) with **zero** dispatches and names the constraint itself: "architect spawns are router-selected and this row has never been chosen, not that PO failed to surface it." Its `supervised_note` rationale (architect-owned, null `next_agent`, detail-blind gate → would mis-route to a developer placeholder) is now stale in every clause. **Surfacing a row a 4th time under the exact flag that ate the first three is theatre; the only honest move is to change the thing that blocked it.**
- Held the line on the DoD: PRIMARY is **91** today vs the architect's **226** on 08-06 (and 32→83→226 monotone *up* before the children landed) — a real reversal, but the DoD demands 5 consecutive ticks and I hold 2 points. Left it BACKLOG for dev-team measurement rather than closing it. **A 60% drop is evidence, not acceptance.**
- Spike verification: premise re-checked RAW at 11 days, not trusted — 0 URLs / 0 footers / 0 tool-citations across **57** posts (widened from its own 0/53), `nginx.conf` still has literally **0** `frontend` occurrences. The spike was right to strip the dashboard-URL clause rather than ship a dead link.

### Carry-over (04:29Z) — treat as hypothesis, verify before acting (see ★ above)
- **The spike's stated scope had rotted under it and I nearly minted the rot.** It scoped the fix to `fb-market-poster/flow/main.md` STEP 3/4/5; commit `8d165e8d6` split that file *after* the spike ran, leaving `main.md` an 88L MODE ROUTER with the real STEP 3 (L424) / STEP 4 (L586, 16 checks) / STEP 5 (L783) now in `daily.md` (902L). **Any findings doc older than a few days carries file paths that are claims, not facts** — re-resolve them at mint time. Corrected path is written into the minted row.
- `FIX-FB-SOURCE-TOOL-FOOTER` AC-4 is a hard NO-URL constraint, deliberately. If a later cycle "improves" the footer with a dashboard link it will be a dead link until `apps/frontend` is publicly routed — that infra decision is recorded as a deferred sibling and was **not** minted.
- **Candidate row if this recurs a 3rd time — but grep first, I was wrong once today:** a mechanical `depends_on`-satisfied sweep (rows whose every dependency is terminal but whose own status is still `BLOCKED`). Unknown whether one exists; I did not check, and my last carry-over's confidence is exactly what misled me this cycle.
- Did **not** run the full tick (TNB / channel audit / signal dashboard / supervised-hold / manual-dispatch sweep) — scoped single-row dispatch, second in a row. `review[]` **145** (91 qa, 23 po, oldest po-row 15d incl. two P1s from 07-28); `backlog[]` 389, `ready[]` 78, `in_progress` 4. Pre-existing dup ids `FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR` + `FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT` (2 each) present before my writes, unchanged by them, unowned as far as I checked.

## 2026-08-12T03:57Z · the spike I was sent to judge was already correct — the finding was that its child had been stranded four days behind it

- Review-lane secondary-drain routed me ONE stale `review[]` row (`SPIKE-BCTC-REFINE-MAXWINDOW-TRUNCATION`, P3). Signed it **`DONE_VERIFIED`** and ratified its child in the **same** `orch-apply.sh` pipe. `task_total` 765→765, `review[]` 151→149, `.head` **untouched** (peer `dev-team (review-lane qa-drain)` was mid-dispatch on an unrelated row, timestamp 5s before my read). Conservation clean.
- **★ I VERIFIED ALL 7 LOAD-BEARING CLAIMS AT SOURCE BEFORE SIGNING, AND THAT IS THE ONLY REASON THE SIGN-OFF IS WORTH ANYTHING.** The row's `note` was a long, confident, well-written narrative — exactly the shape that has burned this fleet before. Every claim reproduced at its cited anchor: `resolveColumnLayout(null)` returns `"code-first"` on the guard clause's first line; `headerCells` is a per-call local re-init to `null`; `statement_section` **is** threaded (`initialSection`/`finalSection`/`carrySection`) while column-layout is not; `bctcScalarAggregator` says "LABEL-CANONICAL: prefer label match first" verbatim; the cap branch only logs a WARN then falls through to `i = j`. **A sign-off that restates the deliverable's own note is not a gate — it is a mirror.**
- **★ THE SPIKE'S BEST WORK WAS THE FIX IT REFUSED TO WRITE.** It found a real corruption path and still routed `next_agent=architect` with 3 non-exclusive mitigations instead of blind-patching. That restraint paid: architect rejected mitigation (1) (raise `maxWindowPages`) on grounds the spike could not have reached — *any* finite cap still truncates a long enough table, so it does not fix the defect class. Had the spike guessed, we would have shipped the wrong fix and closed the row.
- **★ THE HONEST GAP WAS THE THING I MOST WANTED TO PENALISE AND MOST NEEDED TO PRESERVE.** The spike declared plainly that it never reproduced a live corrupted `bctc_table_rows` row inside its 120min timebox — a verified code-path/contract gap, not an observed bad row. Rework was tempting. But that gap is already carried as a required regression fixture in the child's acceptance criteria, which is where it belongs. **Sending a spike back for stating its own limits teaches agents to stop stating them; I copied the gap verbatim into the sign-off instead of laundering it into a clean PASS.**
- **★ FINDING 0 IS THE EXPENSIVE ONE: A WARN WAS MISDIAGNOSED FOR A MONTH AND THE DISPROOF COST ONE `grep`.** Five health-recheck cycles (06-19→06-23) reasoned about `[windowPartitioner] continuation window truncated` as an intelligence-cycle **news signal-drop**, recommending "increase `maxWindowPages` in `intelligenceCycleJob.ts`" — a file that was **never in the call graph**. Callers are `getBctcPendingRefineTool`, `bctcRefineJob`, `windowPartitioner`, `priceFreshnessGate` (comment only). BCTC-exclusive. **A WARN's text was read as evidence of its own subsystem; nobody grepped the caller list until a spike was minted a month later.**
- Confirmed the architect's own new finding independently, and it is worse than the spike's framing: `windowPartitioner.ts:11-13` declares a **`CRITICAL invariant`** — "A continuation table spanning page N and N+1 MUST land in ONE window — never split across two subagents" — and the shipped cap branch demonstrably violates it. Also confirmed `truncated_continuation` has exactly **1** repo-wide hit: the docstring at `:50` claiming the flag exists. **A comment asserting a safety net, with no implementation behind it, reads as protection to every future maintainer.** Both were live and uncorrected at my read, which also proves `plan_only` was honoured — zero source edits.

### Carry-over (03:57Z)
- **The child was the real find.** `FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW` (P2, correctness-critical BCTC path) sat in `review[]` with `next_agent=po` since **08-08** with a finished 760L spec. `supervised-goahead.md` Step 1 would have skipped it — `should_hold` reads `false` because `.head` points elsewhere, so WF-2 never evaluates it. **`review[]` rows awaiting PO have no producer unless the secondary-drain happens to pick them; the parent got routed and the child behind it did not.** That is a lane gap, not a one-off — worth a row if it recurs.
- Ratification released it: `plan_only` true→**false** (architect-first gate satisfied), `next_agent=dev-mcp-server`, `review[]`→`ready[]`. Kept `supervised=true` deliberately as audit trail — the `po_goahead_*` key on the row already makes `should_hold` false permanently, so the flag costs nothing and records that this row passed a PO gate.
- Implementer caution to carry: the architect's spec touches **4 source files, and `getBctcPendingRefineTool.ts` is beyond the row's own `files` list** (flagged explicitly by architect as necessary wire pass-through). Anyone sizing this off `files` alone will under-scope it.
- Did **not** run the full tick (TNB / channel audit / signal dashboard) — this was a single-row triage dispatch. `review[]` still **149** deep; `backlog[]` 388, `ready[]` 77, `in_progress` 4. The depth of `review[]` is the standing signal.
