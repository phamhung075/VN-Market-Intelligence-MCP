# PO Notebook

_Last: 2026-08-06T12:45Z (rag-service near-OOM ruling — the fix was live, the remedy was designed, nothing was dispatched). Prior PO cycle 12:30Z preserved at `HEAD~`._

## 2026-08-06T12:45Z · A lane name is a claim about process, never about deployed state

- **The whole triage turned on one `docker exec`.** Router offered two branches — undeployed-fix-pending, or deployed-fix-doesn't-work — and asked which. Neither. `docker inspect` Created=2026-08-05T16:49:10Z (5h33m after commit `22232ad2b`) and the fixed code is physically in the running container: `_compact_lock` L138, `finally:` L293, `_insert_count = 0` L297. **REVIEW told me nothing about deploy state, and there is no board field that could have.** Resolve deploy questions against the container, always.
- **The third branch was written on the row a week before the symptom appeared.** FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP's AC6: *"Does NOT claim to close the restart loop... the residual belongs to FU-RAG-DEPLOY-MEMORY."* The developer disclaimed this exact symptom while landing the fix. Reading the near-OOM as that fix failing would have re-opened a working change and left the real remedy untouched another cycle. **When a symptom persists after a fix, read the fix's own AC list before reaching for recurring-bug-escalation** — "same service, same symptom" is a topic match, not a mechanism match (`feedback_selfreport_conflates_two_dbadjacent_defects_by_topic`).
- **Detection fired 8+ times, design completed, and the file was never touched.** c47–c55 plus three A-30 signals today (08:16Z CRITICAL, 09:07Z WARN, 12:42Z WARN), one confirmed OOM kill at 08:17:28Z, architect brief decided at 09:04Z: limits 768m→1g, reservations 256m→512m. **3h40m later `docker-compose.yml` was byte-identical to its 2026-07-28 commit and `in_progress[]` held no RAG row.** Everything upstream of dispatch worked perfectly. That is the finding.
- **So I aimed recurring-bug-escalation at the dispatch path, not the code** — a use the rule had not been put to. Not a recurring bug, not a recurring *failed* fix: a recurring **undispatched** fix. And it already has an owner, so I minted nothing: FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE (P1, REVIEW since 10:16Z). **The fix for the dispatch gap is itself stuck in the lane it fixes**, so it cannot dispatch this row — manual dispatch this tick, or the 3h40m simply extends.
- **Refused the interim restart, and wrote it on the row as standing, because the question recurs every time memory crosses 99%.** `restart: unless-stopped` already auto-recovers (observed: start 1.6s after the die at 08:17:30.93Z), so a manual restart duplicates an armed mechanism. It buys ~25 min — measured twice, not estimated. And it costs a ~400MB cold model reload with degraded service. **The cap-raise recreate is itself a restart; it just comes back with ~270-300MiB of headroom instead of 0.7MiB.** Never spend a restart *instead of* the one that carries the fix.
- **Re-derived the headroom myself rather than inheriting the brief's numbers.** `docker info` MemTotal = 7939 MiB, `docker stats` sums 2299 MiB actual across 13 containers → +256MiB ceiling lands ~2555/7939 even if fully consumed. Architect quoted 8192 nominal / ~2.74 GiB; mine are **more conservative and agree**. Authorising a memory raise on an agent's self-reported free-space figure is exactly `feedback_agent_selfreport_metalayer_confabulation` waiting to happen.
- **§3 of the brief had no row, which is the same defect one level down.** The embedder idle-unload was designed at 09:03Z and lived only inside `architect_review_note` — a note field is not dispatchable. Minted FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH (P2, `apps/rag-service/`, sequenced after the cap raise so its effect is measurable against a non-saturated cap). **The cap raise buys time, not closure** — corpus grows ~103 rows/h against a fixed, unreleasable ~726MiB baseline.

### Carry-over
- **FU-RAG-DEPLOY-MEMORY needs MANUAL dispatch to developer — it will not self-dispatch.** It is `ready[]`, P1, `next_agent=developer`, fully authorised, no gate left. Two-line compose edit (`docker-compose.yml` L165-172) + `docker compose up -d --no-deps rag-service`. Never down/stop/kill/rm/--force-recreate.
- **Container is at 99.91% with RestartCount=6 right now.** Expect further A-30 fires and possible OOM kills until the recreate lands; those are the known residual, not new defects. Signal `sys-20260806T124200-5a4b` folded onto the row, not re-minted.
- **Give FIX-DEVTEAM-RESUME-GATES-OMIT-READY-LANE's QA this evidence** — it is no longer a doc-level finding, it has a production casualty with a timestamped 3h40m gap.
- **FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP is 20h in REVIEW and blocks FIX-RAG-COMPACTION-DISK-AMPLIFICATION (`depends_on`).** Not expedited on OOM grounds — judge it on AC1/AC2/AC5 only, against the live container, and do not read the memory figure as it failing.
- **Board after this tick:** `task_total` 813→814; 3 rows annotated, 1 minted, 1 signal triaged. Still live from 12:30Z: the market.db WAL sequencing hold, the +2h incident-doc timestamps, `FIX-EMITSIGNAL-BUGTELEGRAM-NO-TEST-SINK-GATE`, architect's 4-lane S2 lock adjudication, `guard_signal_type_coverage` rc=1.
