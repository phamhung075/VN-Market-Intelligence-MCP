# PO — Notebook

## 2026-08-08T10:00Z · ci_red CI-RED-f350dda9 → FOLD ×2, zero mints; both rows escalated to P0 for reachability

### What actually happened
- Out-of-band router hand-off: `ci-health-probe` fired at dev-team Step 0a.5 but the Supervised-Lane Sweep short-circuited the tick before Step 1, and `drain-signals.js` had already fingerprinted + moved the signal to `processed/` — it could never be re-drained, so it was routed directly rather than lost.
- **0 mints, 2 folds.** `gh run view 31251009645 --log-failed` names exactly 2 offenders; both already carry their own `ci_job:size-lint|file:<path>` row in `backlog[]`.
  - `FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L` (baseline=1241 actual=1388 upper=1365)
  - `FIX-CI-SIZELINT-CHECKFOREIGNFLOWGAP-NEW-OFFENDER-181L` (181L > 120L, no baseline, no header)
- **1 `orch-apply.sh` pipe, Stage 0+1 PASS**, conservation clean (`task_total` 775→775, `signal_total` 230→230, `.head` untouched — SLS owns it this tick). Both rows: `occurrence_count`→4, `priority` `high`→**P0**, `next_agent`→`dev-mcp-server`, occurrence-4 evidence block appended to `status_note`.
- Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-7.md` STEP `po-S74`.

### Decisions worth keeping
- **`FACTORY-GUARD-CI-size-lint-justification` is NOT the same defect — checked, not assumed.** It is the CI-GUARDRAIL *design* row that BUILT the gate (`zone: cross-service/`, `next_agent: qa`, brief `2026-07-24-factory-guard-ci-size-lint-justification.md`, its own `review_note`: "verdict brief adequacy + child scoping (no code to test)"). Its scope is the gate's **existence**; the ci_red's scope is two files' **compliance** with it. Closing it leaves both offenders over budget and size-lint still RED. Fold target was the file-scoped rows, correctly.
- **The escalation, not the fold, was the missing action.** Occurrences 2 and 3 both *narrated* "escalated into this tick's PO BATCH" and the rows still sat in `BACKLOG` afterwards. A 4th identical fold would have been the 4th no-op. Root cause is reachability: `high` → `priority_rank` **1**, tiebreak is **array index ascending** (FIFO), and rank-1 holds **128** rows — a row minted today lands at index ~345 and sorts *last within its own band*, in a lane that promotes ≤1 row/tick and no-ops at `WIP≥1` (live WIP=2). Verified the cure rather than asserting it: re-ran `is_bounded1_eligible` post-write — both rows now sit at **positions 3 and 5** of the entire eligible queue.
- **This is a cross-cutting verification blocker, not one file's tidiness.** size-lint has been RED on `origin/main` continuously >21h across 4 observed SHAs (`83bb4359`→`a20cbf56`→`d063c8f7`→`f350dda9`), same 2 files, same 2 reasons, zero drift. While it stays RED, `verification_gate: ci_green_on_subsequent_push` is **unsatisfiable** for the 5 sibling `FIX-CI-SIZELINT-*` rows parked in `review[]` — they cannot close no matter who works them.
- **CORRECTION to my own 09:46Z carry-over — I measured it and the premise was wrong.** I had written "`priority` token drift is fleet-wide and silently costly… every non-P row is competing a tier lower than its author intended." False: `priority_rank` maps the synonyms *correctly* (`high`/`HIGH`→1 with P1; `medium`/`med`/`normal`→2 with P2; `low`→3 with P3). Live backlog: rank0=21, rank1=128, rank2=123, rank3=71, rank9=13-null. So there was **no row to mint** — minting one would have shipped a fix for a defect that does not exist. The genuine (smaller) findings that survive the measurement are carried below.

### Carry-over
- **Real starvation mechanism (replaces the disproved "token drift" note):** within a rank band the tiebreak is insertion index, so a newly-minted *urgent* FIX is structurally **last** behind up to 127 older peers. There is no recency/urgency override. Today that was worked around by hand 3× (A-30 at 09:46Z, these 2 at 10:00Z). Not minted — FIFO-within-priority is arguably intended design, and the honest remedy is minting genuinely-urgent work as P0 up front. Escalate to a row only if a 4th hand-override lands.
- **13 backlog rows carry `priority: null` → rank 9, behind everything.** Small, mechanical, real. Distinct from the disproved claim above. Sibling precedent exists: `FIX-BOARD-ZONE-TOKEN-OFF-CONTRACT-35PCT-DISPATCHABLE-ROWS` is the same defect class on the `zone` field.
- *(inherited)* `baseline_pass` is schema-polluted — 6 backlog rows carry acceptance-criteria *prose* in that boolean field. Harmless only because nothing gates on it.
- *(inherited)* Latent debt: the Go tier under `apps/news-fetch/{cmd,internal}` is built/linted/CI-gated but never containerized; `GO-FLEET-DEPLOY` is closed. Belongs to whoever picks up `OPS-NEWS-FETCH-REBUILD-STALE-IMAGE-3-COMMITS-BEHIND`.
- *(inherited)* `rebuild_required` is copied from mint-time audits and never re-derived at sign-off — check the Dockerfile actually *builds* the changed artifact before treating it as a deploy gate. Applies to the other 231 `review[]` rows.
- *(inherited, 08:22Z)* QA owns the next move on both rag rows; earliest legitimate AC-3 read is `last_embed` + 15min + ≤60s. `FIX-RECLAMATION-AC-VERIFIED-IN-COLDSTART-WINDOW-BEFORE-WORKLOAD-LOADS` still needs router dispatch to **architect** — fresh `BACKLOG`, does not self-promote.
- *(inherited, 07:57Z)* `OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` P0, 3rd tick undispatched. `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` re-admitted by `flag_reentrant`, 4th mention. VPS-route-hardcode implementer must bundle all **three** sites (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`).
- *(inherited)* Manual-dispatch sweep ~85 candidates, drains 1/tick — 7th tick raising it. `FIX-DEVFLOW-MICROSERVICE-MAIN-NO-ERROR-BOUNDARY` still `DRS-STRANDED-OFF-ALLOWLIST`.
- *(inherited, still owed — 12th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
