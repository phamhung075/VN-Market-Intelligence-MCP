# PO — Notebook

## 2026-08-23T23:46Z · The lane that dispatched me is dead, and it dispatched me by accident

- **Trigger:** dev-team tick 2026-08-23T23:07Z, Review-Lane SECONDARY-Drain, claimed row `FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT`. Inbox **36 envelopes / 13 types**. **Output: 3 orch-apply writes — 4 net mints, 4 folds, 1 lane-move, 1 partial CLEAR (32 of 36), 1 self-correcting dedup repair.** `task_total` 805→809. head untouched (never written by this lane, by design).
- **★ Refused the sign-off the lane asked for.** All 4 decomposition children resolve LIVE to `ready[]`/`TODO`, 0/4 complete, untouched 10 days. `DONE_VERIFIED` had a concrete victim: `FIX-COWORK-STEP0A-TOPO-DRAIN-STATUS-CONTRACT` `depends` on this id and `deps_satisfied()` accepts only DONE_VERIFIED — signing off unblocks it on a 0/4-implemented contract. The row's real defect was **no reader**, so I gave it one: populated `children[]` + moved `done[] → ready[]`, because `devteam-wrapper-autoclose.jq`'s candidate set is `ready[] ∪ in_progress[]`, **never `backlog[]`**. It now auto-closes when the children land, and BOUNDED-1's EPIC-WRAPPER GATE simultaneously stops it being auto-claimed as atomic work. Same write stamped `next_agent=agent-father` on all 4 children — they had **none**, and their `zone` values are bare labels (`cowork-team`/`docs`/…), not `apps/<service>/` paths, so zone-detect Tier-3 would have mis-routed them to the generic `developer`.
- **★ SECONDARY-Drain is silently dead for `review[]` and has been since 2026-08-08.** The claim script picks from `review[] ∪ done[]`; its only caller reads back `[.task_board.review[] | select(...)] | first // empty` — **`review[]` only**. A `done[]`-origin pick stamps the board, returns `picked` empty, logs nothing, spawns nothing: byte-identical to "nothing eligible". Live: **36 eligible candidates, 0 of 16 `review[]` rows ever stamped**, all 3 stamps `done[]`-origin, the 8 oldest candidates all `done[]`-origin. Self-perpetuating — a `done[]` row never leaves DONE precisely because the reader it needs is the thing never spawned. **I only got dispatched because the router hand-carried the row out-of-band.** Filed → agent-father (`docs/agents/**` is its exclusive zone), NOT fixed here.
- **★ Did not fold it into the row that looks like its dedup target.** `FIX-DONELANE-DEVTEAM-FLOWDOC-PROSE-SYNC` (agent-father, same file, same section) *excludes* this defect in its own words — "zero bash/call-site changes in main.md, so the mechanism is fully live the instant that row lands." That sentence is the bug. Folding a P1 liveness defect into a P2 prose-sync would have shipped as prose.
- **★ Minted a duplicate, caught it, repaired it net-zero — id-token dedup is not dedup.** My routing-gap mint collided with a live P0, `FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS`. My sweep missed it because the regex keyed on `SIGNAL-TYPE-ROUTING-GAP`/`routing-gap` and that id contains **neither token**. Found it only by accident, reading a prior journal for a filename convention. Deleted + re-minted scoped to the one genuinely uncovered type (`system_issue` underscore) in ONE write, so `task_total` delta = 0.
- **★ Acted on the CORRECTION envelope, not on the 3 qa notebooks it corrects.** Three qa sessions independently recommended a QA-Drain actuation row; their test ("no commit moves these ids into qa[]") **cannot distinguish "never actuated" from "actuated then reverted"**. I minted no such row. Verified both real causes at source instead: `orch-apply.sh:116` captures `MTIME_BEFORE` at *its own* start, i.e. **after** the caller's jq already read the file, so a stale full-doc candidate applies with no mismatch and conservation is blind (a lane revert preserves every count); and the qa-drain pipe's `|| true` discards a genuine CAS exit-2 that `orch-apply.sh:68` explicitly says to retry.
- **Cleared 32, held 4.** `auto-push-abort` is now **safe** — its routing row landed at line 43, so the earlier hold retires. Still held: `recurring-bug` ×2, `sprint_registry_dangling_ids` ×1, and `system_issue` **underscore** ×1 — the underscore's only appearance is a stats-table row at line 72 inside the `**CORRECTION` block the guard's parser **skips by construction**. Never ran the guard (not read-only; `--check` is a no-op alias); hand-replayed its two extractors. Re-measured push premise rather than trusting envelopes: **ahead=95 behind=0**, same size-lint gate on all 4 recent attempts — backlog grew 46→95 since that P0 was minted.
- **Prose ceiling bit mid-write.** My first apply ABORTED: the sweep-guard row is 11968B against a 12000B ceiling, so a fold *note* was impossible. Bumped `occurrence_count` 27→28 **only** — "28" and "27" are the same byte length, so net growth was exactly zero. Detail lives in the journal, not on the row.

### Carry-over
- **4 envelopes parked on purpose.** Do not clear them for a green guard — green-because-the-input-was-deleted is indistinguishable from green-because-fixed. They drain once agent-father lands the table rows.
- **Two `done[]` rows still carry undispatched SECONDARY stamps** — `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES` (11 days) and `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-SKILLS`. Left deliberately as live evidence; AC-5 of the new row owns re-driving them. Neither can take `DONE_VERIFIED` anyway — both have `verification: absent` and `checkVerificationGate` requires `raw_probe`.
- 4 new rows all need **router hand-dispatch**: 3 are agent-father (off the DRS allowlist), 1 is developer/P0 (`scripts/orch-apply.sh`). No automated lane picks up the agent-father three.

## 2026-08-23T21:12Z · Three "facts" handed to me were wrong: a routed type called unrouted, a live agent called orphaned, a dangling id that resolves three times

- **Trigger:** dev-team tick 2026-08-23T20:37Z, rotation `step1_triage`. Inbox **30 envelopes / 12 types**. **Output: 3 orch-apply writes — 3 mints, 6 folds, 1 manual-dispatch stamp, 1 partial CLEAR (27 of 30).** `task_total` 803→806. head untouched (idle throughout), WIP lanes unchanged.
- **★ Cleared 27, not the 23 I was told to.** The spawn prompt named `bctc_signal` (×4) a FOURTH unrouted type needing a new routing-gap row. **False.** It has a Pipeline-A row — the multi-alias cell `` `news_impact` / `price_anomaly` / `bctc_signal` / `fundamental_*` `` — and the guard's `extract_type_column()` pulls *every* backtick token from the first cell, with a header comment naming that exact row as why. Holding those 4 back would be a **no-op for guard honesty** (already green on that type) while guaranteeing re-delivery every tick forever. Held only the genuinely-unrouted 3: `recurring-bug` ×2, `sprint_registry_dangling_ids` ×1. Hand-replayed the guard's two read-only extractors (never ran the script — not read-only, `--check` is a no-op alias): `UNROUTED_A=["recurring-bug","sprint_registry_dangling_ids"]`, `UNROUTED_B=[]`. Verified after: inbox=3, exactly the held set.
- **★ tnb-audit's HIGH "qa-responder orphaned 89d" is falsified — it is demand-driven with an intact path.** `cronConfig.ts:39` has the `*/12` cron, `schedulerJobTable.ts:1150` registers it, `askQueueCheckJob.ts` calls `spawnQaResponder()` on pending>0, `qaResponderSpawner.ts:61` early-returns `no_pending` on an empty queue — and live `get_pending_ask_questions()` → `[]`. 88 days of silence is *correct behaviour with zero demand*, not an outage; "any /ask went unanswered" is false. The obvious fix (add a cowork slot) would have been **actively harmful**: brief `2026-05-18-cowork-team-command.md:184` exempts it by AC-7, so a slot re-add silently reverses an architectural decision *while looking correct*. Residual = 5 docs giving 5 different cadences (that contradiction is what misdirected the audit) → P2 doc debt. ACK-WITH-CORRECTION written into the handoff so c134 downgrades instead of re-escalating.
- **★ Fleet push: 4 signals, 1 proven cause, 1 P0 mint.** auto-push-abort ×2 + alert-commander bug-escalation + tnb c133 all converge on `pushBctcLayoutHandler.ts` (baseline 228 / actual 252 / upper 250). Re-measured rather than trusting envelopes — both carried stale `ahead` (24, 43); live **ahead=46 behind=0**, `wc -l`=252. Did **not** fold into `FIX-SIZELINT-PREPUSH-SCANS-WORKING-TREE-NOT-PUSH-RANGE`: different defect, and tonight's offender is already committed, therefore already inside `@{u}..HEAD` — that fix would move nothing. Annotated the falsified log-rotation row in place so nobody dispatches it as the fix, but left its own scope/priority intact (the log genuinely has no rotation; it just is not tonight's blocker).
- **★ `sprint_registry_dangling_ids` names an id that resolves three times** — `FIX-BCTC-BANK-SUMMARY-MAPPING` is live in `active_sprints[]`, `backlog[]` *and* `.sprint_goal`. Nothing dangling. Minting a reconciliation CHORE against a satisfied premise would repeat the exact failure that row's own history documents (a prior fix CANCELLED on a grep that passed against a string that was never the problem). Verified, no mint.
- **6 folds, 0 duplicate mints.** `recurring-bug` bd05e417 carried `action_required: mint_fix_task`, but its symptom (bare `0,` at byte 581 right after `"sentinel_section_count": 0`) is the exact `grep -c … || echo` double-emit signature already owned by an open P1 — **an emitter's own `action_required` does not override a resolving dedup.** ci_red `CI-RED-ab508729` = same run 32655888564 already cited on the P0 routing row → recorded the SHA. sweep-guard `escalated=true` ×2 → occurrence 25→27, **integer only**, no prose (row frozen at 12469B).
- **Deliberately did NOT mint 4 notebook/ctxbloat breaches.** ba.md + dev-rag-service.md symptoms are verbatim the scope of `FIX-NOTEBOOK-AUTOPRUNE-ROLLING-SECTIONS-BYTE-COUNTED-BUT-UNDROPPABLE`, already READY at P0 — a 5th corroboration on an already-dispatchable P0 adds zero dispatch value, and the row is frozen at 24298B so it could not receive one. The 2 sprint-journal breaches took DEFER (sprint is `active`).
- **CLEAR actuator: ignored the documented `echo "$var" | jq` idiom** per the open P0 proving zsh corrupts `\n`-bearing payloads and `|| true` then swallows the abort. Read ids straight from the file, dropped `|| true`, re-read the file to confirm. Same defect would have silently no-op'ed this whole tick.

### Carry-over
- 3 envelopes still parked in `pending_triage_inbox`. **Do not clear them to make CI green** — they are the guard's only remaining evidence. Drain naturally once agent-father lands the 2 table rows.
- BATCH returned 4 rows, 3 of them agent-father-owned and **off the DRS allowlist** — they need router hand-dispatch, no automated lane picks them up. Manual-dispatch sweep found **102 eligible candidates**; flow stamps 1/tick, so that queue is ~102 ticks deep at current rate.
- Broke a rank-0 tie on *value* not array index: took the CI-unblocking row over the two arbitrary-index ties. All 3 are the same file + owner → one hop.

