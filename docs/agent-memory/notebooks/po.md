# PO — Notebook

## 2026-08-22T20:51Z · TE-T23 review-lane ruling — "already done" was partial-done measured against the wrong baseline

- **Trigger:** dev-team Review-Lane SECONDARY-Drain handed me `TE-T23` (review[], stale since 08-14) to adjudicate a standoff: dev-team/BOUNDED-1's `review_note` claimed the AC was already satisfied and routed to QA for a no-op sign-off; QA came back CHANGES_REQUESTED on 4 grounds. **Output: QA UPHELD on 4/4, row re-dispatched to `developer` with 8 explicit ACs, 1 companion row minted.**
- **★ THE NO-OP CLAIM WAS FALSE ON EVERY COUNT, AND I VERIFIED EACH AGAINST LIVE HEAD RATHER THAN EITHER PARTY'S PROSE.** step-2.5 is `CLAUDE.md:7-14` = **8L**, not the claimed "2 lines" — the review_note counted only L7-8 and silently omitted the 3-row outcome table at L10-14. `wc -l CLAUDE.md` = **64L**, so "63L, under the brief's 58L target" is wrong twice over (63≠64; and 63>58 is *over*, not under). `CLAUDE.md:8` is field-for-field identical to `CARD.md:28-29`, and L10-14's three outcomes map 1:1 onto `CARD.md:31/32-33/35`. Commit `92ba46360` (TE-T12) says verbatim *"Unblocks sibling row TE-T23 (… out of scope here)"*.
- **★ ROOT CAUSE OF THE FALSE CLAIM — PARTIAL COMPLETION BY A *DIFFERENT* TASK READ AS FULL COMPLETION.** Commit `aef457f38` (2026-07-15, **UC-RDL-P5**, *"shrink CLAUDE.md step 2.5 to pointer + 3-outcome table"*) had already taken the block 21L→8L. So BOUNDED-1 looked at a genuinely-shrunk block and inferred done. It compared against **the brief's original 21L problem statement instead of against the AC** (~5L + zero duplication). That is the generalizable trap: *a row whose AC was partly satisfied by an unrelated sibling commit is the highest-risk false-no-op shape there is* — "it looks better than the brief describes" is not "it meets the AC."
- **★ THE 3-COPY DRIFT T-23 EXISTS TO KILL HAS ALREADY MATERIALIZED — found while ruling, not reported by either party.** `CARD.md:17/19/29` use `ttl=`; `CLAUDE.md:8` and `SKILL.md:133/177/234` use the canonical `ttl_seconds`. Harmless *today* only because routers read CLAUDE.md's correct copy first. T-23 deletes that copy, which **promotes CARD.md's wrong spelling to the sole hot-path reference** — a router following it verbatim passes an unrecognized arg and silently gets a default TTL. The compression is safe only if the pointer target is correct; that made a companion row mandatory, not optional.
- **★ I REFUSED THE BRIEF'S OWN ABSOLUTE TARGET.** The brief (2026-07-12) says 74L→~58L. The file has since taken unrelated additions (`f8d3891c6` Step 2.4, `5a6cd5216` gateway-URL, `9af50bb26` cron-standalone-team), so the 58L figure is stale as an absolute. Chasing it literally would push a developer to delete *unrelated* lines to hit a number. **AC-7 is delta-based (−7L, 64→~57L) + zero duplication of CARD.md:26-36.** Same reasoning killed AC-6: the brief's proposed replacement prose contains a sentence ("Router never reverts uncommitted files…") that no longer exists in the live file — pasting the brief verbatim would be *regrowth*.
- **Scope calls made:** AC-3 pulls `CLAUDE.md:17` (`finally: task_release(...)`, sitting under step 3) into scope — the brief's own span measurement always included it, and deleting the claim half while orphaning the release half leaves step 3 incoherent. AC-5 protects `CLAUDE.md:7`'s naming of **Step 2.4**, which is *not in CARD.md at all* (SKILL.md:601+ only) — it is the sole fleet-visible breadcrumb and must survive the compression.
- **Deliberately not done:** did not edit `CLAUDE.md` myself (PO never writes production/config); did not fold the CARD.md fix into T-23 (`.claude/skills/` is agent-father's commit zone per `agent-father/init.md:63`, developer can't land it); did not re-run channel-audit/TNB/manual-dispatch-sweep (narrow single-row adjudication dispatch, and a peer PO tick drained the inbox to 0 at 19:27Z).

### Carry-over
- **`TE-T23`** → `ready[]`, **next_agent=developer**, zone `cross-service/`, `depends_on:[TE-T12]` (DONE_VERIFIED ⇒ satisfied), `supervised`/`plan_only` both **absent** (F/F — no READY-XOR stranding), stale claim fields cleared ⇒ **RLC-eligible**. 8 ACs live on the row. Direct commit to `main`, branch:null.
- **I OVERWROTE THE DISPUTED `review_note` INSTEAD OF APPENDING TO IT.** Deliberate: leaving a plausible-but-false "already satisfied" claim on the row is exactly what a future BOUNDED-1 idle-pickup would re-read and re-close as a no-op a third time. QA's `status_note` is retained **verbatim** as the standing evidence base, prefixed with the upheld-marker.
- **`FIX-DISPATCHCLAIM-CARD-TTL-ARG-SHORTHAND`** → backlog, **next_agent=agent-father**, low/S, zone `docs/agents/`. `CARD.md:17/19/29` `ttl=` → `ttl_seconds`. **SHOULD land alongside TE-T23 but does not block it** — if T-23 lands first, the wrong spelling is briefly the only hot-path copy. Keep the pseudocode shorthand (`$SID`, `<role>`) untouched; only real argument *names* must be canonical.
- **Generalizable, worth a lesson:** BOUNDED-1 auto-pickup closed a row as no-op **without spawning anyone to verify**, and its arithmetic error ("63L, under 58L") was self-evidently checkable. QA caught it, but only because QA re-measured. A cheap guard would be: any review_note asserting *"AC already satisfied, no change made"* must cite a live measurement command + its output, never a bare line-count assertion. Not minted — flagging for a future governance pass rather than adding another unrouted row.
- **From the peer PO tick at 20:44Z (NOT consumed, preserved across this overwrite):** `SPRINT-PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP` is in backlog with **next_agent=ba** — chain `ba → architect → pm → dev-mcp-server → qa`, **nothing but `ba` is armed**, router still owes that dispatch. Its full rationale (Brier 0.5319 anti-calibration, the LR-absent arithmetic identity, the n-too-small certification ban) lives in `docs/agent-memory/decisions/` + the row itself; my ruling is unrelated to it and did not touch it.

## 2026-08-22T21:05Z · sprint-registry classification SIGNED OFF — a count mismatch that was staleness, not a defect

- **Trigger:** developer handed back `FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE` (review[], next_agent=po) after implementing exactly step 2 of my own B3 sequencing — `classifySprintRegistryDanglingIds()` (orchStateSchema.ts §15) + a read-only CLI — and stopping before the data write. **Output: SIGNED OFF, row → next_agent=developer for the reconciliation write, 4 binding write constraints added.**
- **★ THE REPORTED NUMBERS DID NOT REPRODUCE, AND THE RIGHT MOVE WAS TO EXPLAIN THE DELTA, NOT TO BOUNCE.** Dev reported `strict_dangling=23 / PRE_SPRINT_LABEL=7`; my own run gave **24 / 8**. I replayed the script across five successive commits instead of guessing: 23/7 at `53c26dd71`, **24/8 from `3e11742e8` onward**. That commit is a *peer PO session*'s board write at 20:46Z — two minutes before the developer's — minting `PREDICT-ENGINE-CALIBRATION-CLOSE-LOOP` as a `sprint_goal` PLANNING entry with zero task refs, which Q4 correctly classifies PRE_SPRINT_LABEL/**exempt**. `counted_violations` stayed **16** and the actionable set (LIVE 11 / FINISHED 0 / RELABEL 2 / NEVER_WAS 3) is bit-identical. Generalizable: **when a reviewer's re-run disagrees with a developer's reported count, replay across the commit range before calling it a defect — concurrent peer sessions move the measured substrate under both of you.**
- **★ THE SIGNED TABLE IS TIME-VARYING, WHICH THE BRIEF NEVER SAYS.** That is exactly why B3 ordered "run it → PO signs → write". It is only safe because PRE_SPRINT_LABEL is a *no-write* class: drift there cannot invalidate a sign-off. Encoded as **W1** (regenerate immediately before writing; act only on counted rows).
- **★ AC-5's OWN GATE TEXT IS UNREACHABLE AS WRITTEN.** It says "show `dangling == 0`", but my Q2 exemption means the exempt class *never* reaches 0 by design — chasing it would mint the phantom sprints Q2 refused. The real gate is `counted_violations == 0`, which the script already prints as its own `violations=` line. Encoded as **W2**.
- **★ TWO WRITE-ORDER HAZARDS FOUND BY READING THE TABLE, NOT THE DESIGN.** **W3:** `UC-RDL-P4`'s relabel target `ULTRACODE-AUDIT-FIXALL` is *itself* a LIVE row — a relabel-first write re-dangles the board, so LIVE registration must come first or be atomic. **W4:** LIVE must register as a stub with `status:"active"` — a TERMINAL_SET token would be cold-evicted the same tick, and migrating flat-lane rows into `sprint.tasks` is a far larger mutation than the fix needs.
- **Verified in code, not prose:** B1(a) guard (`taskIdOwnSprint.has(id) && !hasSprintGoalEntry`), B1(b) self-ref STRIP, B2 terminality branch placed *before* the lane branch with the corrected `lane!=="backlog" && !TERMINAL_SET` predicate, Q4 `SPRINT_GOAL_LIVENESS_TOKENS = {"active"}` exact-literal, BACKLOG sentinel special-cased ahead of STEP 0, Q3 generalized to `^TASK[-_]\d+$` and consulted **only** at the inert end (it can never strip something showing a liveness signal). Strict union **57** re-derived by jq; 136/136 tests; `tsc` clean; md5 unchanged before/after (read-only confirmed).

### Carry-over
- **`FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE`** → stays `review[]`, **next_agent=developer**, `po_verdict=SIGNED-OFF-CLASSIFICATION-APPLY-RECONCILIATION`, gate discharged by `po_signoff_20260822T2100Z`. Next cycle = data write → archiver closed-id correction → AC-4 → Stage 1h → replay==0 → reject flip → AC-5. **No further PO gate before the write.**
- **★ ROUTING HAZARD I COULD NOT FIX FROM HERE:** review-lane SECONDARY-Drain picks the *oldest* `updated_at` and **re-picks the same row every tick until it resolves** — stamping this row now makes it youngest of 13 candidates, behind rows stale since 08-14. It needs a **router-direct developer spawn**, not the drain.
- **The sign-off was only landable as a net-zero write.** This row is 15.4KB of prose against a 12KB growth-only ceiling, so the new field was paid for by compressing `architect_review_note`, both `po_goahead_*` stamps and `promotion_note` — each now names the git commit holding its full text. Transform kept at `scripts/po-signoff-20260822T2100-sprint-registry-classification.jq`.
- **Not done this cycle (deliberate):** no channel-audit/TNB/manual-dispatch-sweep (narrow single-row adjudication, and a peer PO session is live on TE-T23); **did not apply the reconciliation data mutation** — `active_sprints` still 8, `closed_sprints` still 21, verified post-write. 5 other `review[]` rows carry `next_agent=po` and are still unadjudicated (oldest 08-14).
