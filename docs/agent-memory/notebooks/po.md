# PO — Notebook

## 2026-08-14T06:27Z · I retracted five of my own folds — the "benign recurrence" I kept filing was a crash loop, and dmesg said so

- **Inputs:** 12 durable-inbox envelopes + Telegram 4856/4857 (system-auditor Tier-1 c93, rag-service memory oscillation). **Output: 1 `orch-apply.sh` pipe** (conservation-clean, task_total 734→734) — 1 severity escalation P0 + 3.1KB OOM evidence note, 4 folds, 2 BATCH mints.
- **★ I HAD DISPOSITIONED THIS FIVE TIMES AS "RECURRENCE EVIDENCE ONLY, NO FRESH INVESTIGATION WARRANTED." ALL FIVE WERE WRONG.** `po_fold_20260811T1237Z`, `…08-11T1826Z`, `…08-12T1505Z`, `po_expedite_20260812T0542Z`, `po_triage_20260814T0525_fold` — every one folded an A-30 signal onto a REVIEW row as passive corroboration. `dmesg` inside the Docker VM shows **three kernel memcg OOM-kills of the current post-fix container**: 08-12T13:46:51Z, 08-12T14:00:57Z (**invoker `lancedb-tokio-w`** — the exact thread the fix pins), 08-13T09:20:09Z (matches `StartedAt` exactly). **A "sustained-high-but-stable" reading and an active crash loop look identical from `docker stats`.** Fold is a disposition for corroboration, not a substitute for a probe.
- **★ THE ROW'S OWN COMMIT MESSAGE TOLD ME `docker inspect` LIES HERE, AND I'D BEEN TRUSTING IT ANYWAY.** `ca6d86869` documents that `.State.OOMKilled` reads false/ExitCode=0 for real memcg kills in Docker Desktop because the VM-boundary cgroup signal doesn't propagate to dockerd. Live now: `OOMKilled=false ExitCode=0 RestartCount=3` — and 3 confirmed kills. **The evidence that refuted my dispositions was written into the artifact I was dispositioning.**
- **★ I ALMOST SHIPPED A STALE-IMAGE FINDING ON A TIMESTAMP INFERENCE.** Image built 10:40:10Z, fix committed 11:08:24Z → "container predates the fix", a known recurring class here. **md5 of `/app/infrastructure/repositories.py` in the container == git HEAD exactly.** Built from a dirty tree, committed 28min later. Timestamps order *events*, not *content*; hash the artifact.
- **★ THE ROUTER'S LEAD WAS THE WRONG ROW, AND THE AUDITOR'S WAS TOO.** Both said revisit `FU-RAG-DEPLOY-MEMORY`'s DONE_VERIFIED. Its QA record explicitly approves **only** the compose deploy-codification, disclaims "memory resolved" verbatim, and pre-routes the residual to the idle-unload row. **Correctly scoped closure — reopening it would relitigate a row that never made the claim.** ACK-WITH-CORRECTION.
- **★ FIVE NO-OP FOLDS HAD ONE STRUCTURAL CAUSE: THE GATE HAD NO ACTOR.** The row's AC needs a ≥2h **ops-supervised** measurement. `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` — cited in `ca6d86869`'s message *and* `repositories.py:5,116` — has **zero rows in any lane**. Evidence was accumulating on a gate nobody was ever dispatched to open.
- **Refuted two of my own hypotheses before they reached the batch.** `jemalloc_bg_thd` thread suggested `malloc_trim` was a no-op — but the container is Ubuntu 22.04 glibc and `hasattr(lib,"malloc_trim")` is True. Kept the one that survived: replicating the pin exactly still spawns 2 `lancedb-tokio-w`, not 1.

### Carry-over (06:27Z)
- **★ A fold is a claim that something is already-owned AND already-progressing. Verify the second half.** Five folds onto a REVIEW row that no actor could advance produced a 3-day silent crash loop. Before folding, ask which agent is dispatched to close the gate — if the answer is nobody, the fold is a drop.
- **★ When an artifact documents that a tool lies, stop reading that tool.** I had `docker inspect OOMKilled=false` in three prior dispositions, from a row whose own commit says that field is unreliable in this environment.
- **★ Hash the artifact, don't date it.** Build-time-vs-commit-time inference was about to mint a false stale-image row.
- **Unverified by me:** whether `TOKIO_WORKER_THREADS=1` is *ineffective* or merely *insufficient* — 2 threads with pin=1 is suggestive, not conclusive; the post-pin OOM invoked by `lancedb-tokio-w` is the finding that stands on its own. Also unverified: whether the other 3 microservices carry the same `docker inspect` OOM false-negative blind spot in their health probes.
- **Router action needed: none.** `.head` untouched — idle at entry, idle at exit. Minting is not a reason to take the head.

## 2026-08-14T05:25Z · The inbox handed me one defect wearing five costumes, and my own last carry-over was the wrong lead

- **Inputs:** 44 durable-inbox envelopes + 10 unclaimed Telegram bug reports (4846–4855). **Output: 2 `orch-apply.sh` pipes** (task_total 733→735, both conservation-clean) — 15 folds, 2 mints, 1 LANE-A proposal approved with 4 binding modifications, 10 reports processed.
- **★ MY OWN PRIOR CARRY-OVER WAS A VERIFIED-WRONG PREMISE, AND THE REFUTATION WAS SITTING IN THE SAME INBOX.** 04:15Z me wrote: "`context-bloat-backstop.sh` absent from **all three** settings files (verified: 0 matches each), so the hook is dead — next tick mints on recurrence." This tick's inbox contained a `context_bloat_breach` signal **emitted by that hook** at 04:31:10Z. A hook that emits cannot be missing. Re-grepped: `.claude/settings.local.json` has **1 match**. So the real defect is A-33's own `$PROJECT_ROOT` misresolving (`tier1-probe.md:295-301` fires `settings-file-missing` only when `[ -f ]` fails on a file that exists). **Had I obeyed my own carry-over I'd have shipped a fix for a hook that was never broken.** Minted the corrected row instead.
- **★ FIVE IMMUTABILITY FIRES, THREE AGENT TYPES — THE COUNT WASN'T THE FINDING, THE SPREAD WAS.** `system-auditor` (c5/c6/c89) + **`news-scout` (c266)** + **`alert-commander` (c179)** in one 3h window. Every prior occurrence on `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS` was single-agent, which kept the "it's system-auditor's local compose copy" reading alive. The other two don't share that copy. **That refutes the local-copy theory and confirms the shared skill path** — so the LANE-A proposal I approved is correctly a *pilot* and provably not the whole fix. P1→P0 on both rows.
- **★ THE ROW THAT OWNS THE FLEET DEFECT HAS NO DRAINING LANE, AND HASN'T FOR 15 DAYS.** REVIEW + `supervised` + `plan_only` + `next_agent=agent-father`, `qa_routed=false` — `bounded1-supervised-lane-report.sh` confirms zero pickers. QA returned CHANGES_REQUESTED on 08-08 and it has sat since, while its defect produced 5 more silent data-loss events today. **This is the manual-dispatch-sweep pick; a P0 with no lane is indistinguishable from a closed row.**
- **Replaced a Success Signal in a proposal I was approving.** SS-3 was `grep -c 'notebook-compose.sh' main.md > 0` — satisfied by a commented-out line, by prose, and (the case that matters) by a correct wiring whose Bash allowlist grant was missed, which is *this proposal's own failure mode one layer down*. Swapped for a runtime-execution proof. Also closed AC-3 and the section-cap by ruling, and **severed item 7** (a governance question) — three open questions on a rewire that has already stalled twice is three places to stall again.
- **Folded the QA `nso_ts_key` bug instead of minting a 5th sibling — after reproducing it myself.** `## cycle-705 · …T01:33:11Z · (commit \`761988143\`)` → key `76198814000000000` vs a sibling's `20260814010000000`; the optional `T…Z` group + `tail -1` keys off the commit SHA. Real, live, in `qa.md`. But same file, same function, same suite, same owner as an already-READY/P0 row whose own `lane_note` argues folding beats minting *at this site*. **A distinct mechanism earns a distinct AC, not a distinct row, when the fix site is identical.**
- **Refused one mint on an unverified premise.** `vn_index_cache` "only 1 row, expected 2+ index types" — MED, single fire, and "expected 2+" is the auditor's *hypothesis*, not a read contract. Re-verify next tick.

### Carry-over (05:25Z)
- **★ Treat your own carry-over as a lead, not a finding — re-verify it before acting.** I wrote "verified true" last tick and it was false. The carry-over convention exists to save re-derivation; it silently converts a wrong verification into a durable premise.
- **★ When one guard fires on N agents, the spread is the finding.** Volume within one agent reads as churn; the same defect on agents that don't share code is a root-cause refutation you can't get any other way.
- **★ A P0 in a lane with no picker is not tracked, it's parked.** Check the *draining* lane, not the status token.
- **Unverified by me:** whether the `emit-dashboard` mutex loss (4853) has a standing rate — I have 2 occurrences 8 days apart, not a series. Also unverified: whether A-33's other 3 hook checks are equally blind (they share the one `$SETTINGS_LOCAL`; AC-4 asks, I did not measure).
- **Router action needed: none.** `.head` untouched — idle at entry, idle at exit. Minting is not a reason to take the head.
