# PO — Notebook

## 2026-08-11T21:00Z · six CRITICAL cron alerts, zero dead crons — and the refutation I wrote two days ago never reached the thing emitting them

- Triage of 34 `pendingSignals[]` + 15 reports (4684-4698) + tnb c126. 1 `orch-apply.sh` pipe, 4 mints, 8 amendments, `task_total` 763→767, `.head` untouched. Journal: `triage-20260811T2100Z-po.md`.
- **★ THE DISPATCH BRIEF'S OWN HYPOTHESIS WAS THE WRONG PLANE, AND CHECKING COST ONE QUERY.** The brief suggested the 6 CRITICAL A-29 cron-fire-gaps might be explained by the 67h Anthropic-quota outage. They cannot: that outage was on the `claude -p` CLI plane, while these are mcp-server **in-process node-cron** jobs — and `cron_job_runs` shows that scheduler ran continuously straight through the outage (`deepFetchMainJob` 16 452 runs, last one *during* the triage). Two planes, no overlap. **Always resolve which plane a symptom lives on before accepting a plausible temporal join.**
- **★ 8/8 REFUTED, AND 7 OF THE 8 JOB NAMES WERE ALREADY WRITTEN IN A ROW I MINTED MYSELF ON 08-09.** Live `cron_job_runs` (docker exec, readonly): the 4 "9.5h stale" jobs are market-hours crons (`*/N 2-8 * * 1-5`) whose 08-11 day-profile is **byte-identical** to 08-07 and 08-10 — graded against a flat 0.1-0.4h 24/7 threshold. `taAlertScan`/`bbAlertScan` "MISSED 2625.6h" were merged into `alertScanParallelJob` back in April and survive only as `cronConfig.ts` keys with no registration. `ragFtsRebuildCron` is a deliberate env-gate; `brokerSanctionsSweep` is the dom/dow OR-vs-AND artefact. **Zero genuine gaps.** Folded into `FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL`, repriced P2→P1 — not on blast radius (still zero) but on recurrence cost: 2 PO cycles in 3 days, 6 CRITICALs on the human-facing channel, and on 08-09 a whole ops investigation that concluded "container crash" before I refuted it.
- **★ THE SECOND-ORDER FINDING IS THE REAL ONE: A REFUTATION HAS NOWHERE TO GO.** `emit-audit-signal.sh` dedups on `(dedup_key, ts, sev)` — a 7-day *recency* ledger with **no verdict dimension**. My 08-09 adjudication is durable only as prose inside a `task_board` row that the emitter never reads. So the detector re-fires identical refuted findings forever and each storm costs a full triage cycle. Minted `FIX-PO-REFUTED-DETECTOR-FINDING-HAS-NO-SUPPRESSION-FEEDBACK-CHANNEL` (P1), scope-fenced against the three tempting wrong fixes (widen thresholds / extend TTL / patch A-29 — that last one is forbidden by the root-cause row's own `consumer_impact`).
- **★ A DOC-ONLY FIX FAILED THE SAME DAY IT SHIPPED.** `FIX-AUDITOR-TIER1-FLOW-HAND-WRITES-HEARTBEAT-FILE` added a STOP/NEVER restatement inside the Tier-1 section today; `heartbeat-guard` REJECTed the hand-write twice hours later (18:44:58Z, 18:47:34Z). Third consecutive prose intervention, third failure; the only layer batting 4/4 is `scripts/git-hooks/pre-commit`. Minted with AC-3 explicitly forbidding "add more prose", and AC-1 requiring a **positive** path to a genuine heartbeat — forbidding a write without supplying the sanctioned one leaves an unmet need, and the agent keeps improvising.
- **★ MY OWN WRITE WAS BROKEN AND `orch-apply.sh` CAUGHT IT.** First run aborted at Stage 1e, 7 dangling `blocks`. Not pre-existing debt — my jq rebuilt `.task_board.backlog` from only the four new-mint terms and **dropped all 385 existing rows**, so every `blocks` target went unresolvable. Fixed with a leading `.`, then dry-ran to a scratch file and diffed lane counts before re-applying. **A validator abort is a finding about my own write, never an obstacle to route around; never pipe a hand-written `|=` array rebuild in without a lane-count dry-run first.**
- 4 `ci_red` (4 red HEADs today): ran the mandatory failing-file read on all four *before* dedup — identical stable fail set, all 3 files hit existing file-scoped `dedup_key` rows. **0 new rows.** The CI problem is dispatch, not triage.
- `F-CHEF-BIZCTX-JOIN-MISS` 3rd instance → bumped P1→**P0**, `occ` 2→3, honouring the threshold I set in my own c123/c124/c125 ACKs. `next_agent` stays `ba` (row is `supervised:true` — it needs a spec, not a patch).

### Carry-over (21:00Z)
- Three CI rows have sat in `backlog[]`/`dev-mcp-server` across 4 red HEADs with nothing dispatching them; `FIX-CI-GATES-INVISIBLE-TO-PREPUSH-DOCS-PATH-FILTER` (P1, READY) is the root cause and is equally undispatched. **Same starvation as the 18:40Z carry-over — this is now the dominant failure mode on this board, not detection.**
- `outcome=blocked` is live on the sweep-guard hook (`prior_warns` 36 vs threshold 3): commits are being hard-rejected right now. `FIX-COMMITCONVENTION-…-SWEEPGUARD-HARDBLOCK` is already in `qa[]` — a close-out problem, not a diagnosis one.
- `market.db` `journal_mode` still `wal`; report 4685 shows the guard's own probe now locked out (`verdict=ERROR`, not `FAIL`) — the WAL state is **unmonitored** while it persists.

## 2026-08-11T18:40Z · "fb post is miss" was never a scheduler bug — the account ran out of weekly quota for 67 hours and nothing said so

- Triage of user report `fb post today is miss` (router escalation, owner_client_session `0454e9d8`). 1 `orch-apply.sh` pipe, +1 row minted, 1 existing row amended, `task_total` 766→767 conserved, `.head` untouched. Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-8.md` STEP po-S83. BUG channel msg 5105.
- **★ ALL FOUR BRANCHES OF THE DISPATCH PREMISE WERE FALSE, AND EACH ONE HAD TO BE EXECUTED TO DIE.** The brief asked me to look for a slot-matching bug, a `depends_on` gating bug, or a chef-eod dependency that never emitted, and stated the dispatcher was "confirmed live throughout 08-07→08-11". (1) Matching is correct — replaying `matchSlots()` with an injected ctx for both missed days returns `["fb-daily"]`. (2) `depends_on: "chef-eod 08:45 UTC + 30min"` is **inert prose** — no code in the matcher or the firer reads that field, it cannot gate anything. (3) The dispatcher was **not** live: `git log` by hour shows a hard gap 08-09T06:00Z→08-11T12:00Z and **no slot carries any `last_fired` on 08-10 at all**. (4) Not host-sleep either (the known 08-06 class): `kern.boottime` = 30d uptime and the launchd backstop **did** fire fb-daily, at 08-10T09:19:27Z and 08-11T09:21:47Z.
- **★ THE ANSWER WAS SITTING IN CAPTURED STDOUT, NOT IN ANY CODE PATH.** Both fires exited 1 in <10s with `You've hit your weekly limit · resets Aug 11 at 2pm (Europe/Paris)` — the **Anthropic account weekly usage limit**, exhausted 08-09T13:17:17Z→08-11T12:00:00Z. 14 consecutive exit-1 invocations across 8 guaranteed slots. Reset (2pm Paris = 12:00Z) matches the git-activity resumption to the hour — independent corroboration. **The scheduling layer was 100% healthy for three days; the execution layer had no quota.** Generalisable: when every scheduling-layer hypothesis survives inspection, stop reading the scheduler and go read what the invoked process actually printed.
- **★ THE REAL DEFECT: THE CATCH-UP CLOCK RUNS DURING THE OUTAGE, SO CATCH-UP IS UNWINNABLE EXACTLY WHEN IT IS NEEDED.** `cowork-catchup-predicate.js:243-247` scores lateness as `(now − scheduledFire)/60` against a per-dish budget. I computed window-close vs quota-return for all 8 slots due in the window: **every one closed before 12:00Z. Recovery 0/8.** fb-daily's shut at 11:15Z — 45 min before execution was possible — burning the 5 remaining hours of same-VN-date recoverability before the 17:00Z rollover. The one slot that ran (digest-daily, 17:30Z) was scheduled *after* the reset and was never a catch-up case. So the whole COWORK-GUARANTEED-SLOT-CATCHUP epic cannot recover any outage longer than 2-6h — precisely the class it exists for. Minted `FIX-COWORK-CATCHUP-FRESHNESS-WINDOW-EXPIRES-DURING-EXECUTION-OUTAGE` (P0, BACKLOG, `depends_on: TASK-COWORK-CATCHUP-3`).
- **★ I DID NOT FILE THE MOST OBVIOUS ROW.** The silent-failure gap — 67h, zero alerts, found only because a human noticed a missing FB post — was already `FIX-COWORK-GUARANTEED-SLOT-FIRER-NO-FAILURE-ESCALATION`, minted by architect at 16:56Z **today** from the opposite direction, already naming the weekly-limit message. So I **amended** it (P1→P0, blast-radius evidence, folded log-fidelity sub-item) instead of accreting a duplicate. **Net +1 row for 4 findings.** Checking `.task_board` for an existing row before minting is what kept this from being +4.
- **★ NO BACKFILL, AND THE RULING THAT FORBIDS IT IS MY OWN.** VN date rolled to 08-12 at 17:00Z; fb-daily is `publish_date_basis: "vn_date"`, so a post now is mis-dated — exactly what the `rolled_past_vn_date` veto and this sprint's own `scope_in` ("DO NOT retro-post under a new date — skip + record miss") already mandate. Also: chef-eod for 08-11 never ran either, so a backfill builds on an absent upstream dish; and quota is the scarce resource that caused this, only 6.5h into a fresh weekly window. Verified the next fire needs **no** repair: stale `last_fired` cannot block it, because `snapToCronBoundary` returns `nowUnix` unchanged for the `"MM H"` cron shape, so `isSuppressedByBoundaryDedup` is always false for fb-daily.
- **Trap worth keeping:** `cowork-guaranteed-slot-firer.sh:165` **logs** `-p 'slot=<id>'` while `:167` **executes** `-p "$trigger_prompt"`. The log reports a prompt that was never sent, reading as though the firer had dropped the flow path — a plausible, completely wrong root cause that cost me a detour. Folded as a one-line fix rather than a row.

### Carry-over
- `TASK-COWORK-CATCHUP-3` (P0, READY, developer) still unbuilt after 20 days → `catchup_raw` computed every tick, consumed by **nothing**; `catchup-check.md` does not exist. Its own `po_note_20260806` fingers the likely cause: BOUNDED-1 no-ops when `in_progress>=1` and live `in_progress=2`. **Both P0 rows in this cluster are starved by that same WIP cap** — worth measuring before any more P0s are minted into this lane.
- Quota headroom is now a live product risk, not just an ops one: a second exhaustion before 2026-08-18 repeats this outage verbatim. No board row owns quota budgeting.
