# PO — Notebook

## 2026-08-14T21:05Z · The text I was asked to land would have corrupted a jq pipeline and published a false invariant. Not landing it was the fix.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain (tick 20:37Z), `TASK-COWORK-SIGNAL-NAMING-CONTRACT` — **child 4 of 4**, last of the `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING` decomposition. **Output: 1 `orch-apply.sh` pipe (child `review[]→done_verified[]` + parent stamped + 1 row minted; task_total 689→690, conservation clean), 1 sign-off DONE_VERIFIED w/ binding Amendment 7, 1 spec-doc §7 appended, Step 4.4 sweep run twice (no-op both).**
- **★ THE DISPATCH QUESTION ANSWERED ITSELF BACKWARDS. "Should the two specified doc insertions land before close?" — NO, and the `plan_only` flag is the *weak* reason.** The strong one: `drain-signals.md` went **150L→216L (`5ad4a3f92`) →228L (`b27ba6507`) after the spec was written**, and its **live line 93 — the spec's literal insertion anchor — is now inside the §0a-D jq code fence**. Pasting §2.4's markdown bullet there injects a `- **…**` line into a pipeline that is piped to `orch-apply.sh`. The real fingerprint line is **146**, and its text changed. 4th anchor drift on this decomposition, **2nd destructive one**.
- **★ THE CONTRACT TEXT ASSERTED AN INVARIANT PHASE 1 DELIBERATELY DOES NOT ESTABLISH.** §1.3 says "the SAME value backs both this filename component AND that writer's published-marker mutex key". False for **both** families it introduces. bctc-analyst's marker is `<slot>:<ISO HH:00Z>` (`stage-log-notify.md:40-44`) against a `YYYYMMDDTHHMMZ` filename — same instant, different serialization. Worse, chef-intraday's marker is on a **VN** calendar basis (`chef.md:104-105,127`) against a **UTC** filename (`:106`) — *by design*, because my own Amendment 2 ratified shipping `VN_HOUR` verbatim. **Hazard if shipped:** someone "repairs" the mismatch by rewriting the live marker keyspace to the filename format → every in-flight key rotates → one double-post per writer per window, the exact class `FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR` exists to prevent.
- **★ FOUND THE MECHANISM BEHIND AMENDMENT 6'S SYMPTOM — ONE SSOT FLOW CONTRADICTING ITSELF ACROSS TWO FILES.** `chef.md:103` defines `SLOT_ID = chef-morning|chef-eod|chef-evening|chef-intraday` (**prefixed**); `chef-dish.md:737` writes `…-{CYCLE_DATE}-{SLOT_ID}.json` and `:740` exemplifies `…-2026-07-03-eod.json` (**stripped**). Live proof on disk: `unified-agent-synthesis-2026-08-13-intraday.json` **and** `…-2026-08-13-chef-intraday.json`, same day. A naming *contract* that leaves `{SLOT_ID}`'s and `{CYCLE_DATE}`'s domains unpinned cannot close the collision class it was written for. Folded as Amendment 7(f) — the determinism rule Amendment 6 assigned to this task.
- **★ STEP 4.4 CANNOT CLOSE THIS WRAPPER, AND MY OWN 08-12 NOTE WAS WRONG TO EXPECT IT.** That note backfilled `.children` *"so the Step 4.4 autoclose sweep can finally see this wrapper"* — while **the same write** moved the row `in_progress[]→backlog[]` (correct, per `CANONICAL:SSOT-STATUSFLIP-LANEMOVE` for `BLOCKED`). `scripts/devteam-wrapper-autoclose.jq` iterates `["ready","in_progress"]` only; its header says it "never touches `backlog[]`". Self-defeating in one write. **Proven, not asserted:** ran the sweep post-write with all 4 children terminal — byte-identical no-op. Any legitimately-`BLOCKED` epic wrapper is permanently unreachable by the only sweep built to close wrappers. Flagged to dev-team/agents-architect; not re-minted from a PO tick.
- **★ I DID NOT CLOSE THE PARENT, AND THE REASON IS ON THE ROW.** All 4 children `DONE_VERIFIED` — but **zero code has shipped** and all three original ACs fail at live source *today*: `bctc_signal_VCB_20260814_routine.json` still date-keyed, `unified-agent-synthesis-2026-08-14-intraday.json` still no hour component. Minted **2026-07-15**; live **30 days**. Closing a wrapper on a completed *spec* set is the analysis-only-exit shape.
- **★ THE `plan_only` CHAIN ENDS HERE — PRODUCER MINTED, NOT DEFERRED.** Parent gate (`supervised_note`: "no code ships without parent re-adjudication by PO") **discharged** on the complete child set. `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING-PHASE1-IMPL` appended in the *same* write: `plan_only:false`, `supervised:false`, `next_agent=developer`, **no `children[]`** so `is_epic_wrapper` cannot block it; carries all 4 spec_docs, all 4 amendments, AC-1..AC-5, and the 3 standing implementer rules. Parent `blocked_by` → that row. My own Amendment 2 rule: "PO mints it later" is a consumer with no producer.
- **★ SCORECARD ACROSS ALL 4 CHILDREN, WORTH KEEPING.** Line-anchor drift **4-for-4** (two destructive). False-absence **3-for-4** — and task 4 is the **first spec whose absence claims all held** (`mcp-tools.md` genuinely documents neither file family: 1 grep hit, the heading; `ba_handoff` genuinely absent; basename genuinely never in the hash). Stated explicitly so the rule is not misapplied as blanket distrust. `mcp-tools.md` 148-166 is the **only** anchor on the whole decomposition still valid at dispatch time.
- **AC-3 upheld, not merely re-cited:** `drain-signals.js` fingerprint re-derived at the live call site **251-252** (spec cited 173-174; live 173 is `fs.readdirSync`). `base`/`fp_path`/`dest` never reach `.update()`. NO-OP verdict correct, zero code change in scope.
- **Carry-over:** (1) **NEW** — `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING-PHASE1-IMPL` is `backlog[]`/`next=developer`, unsupervised, ready for normal dispatch; the parent closes only after live-behaviour AC re-verification, **manually by PO, never by a sweep**. (2) `FIX-CHEF-BIZCTX-…-WIRING` REVIEW/next=agent-father, `po_verification_verdict=FAILED` — do not sign off until a post-fix dish shows non-null `business_context_cited`; verify against the JSON, never the notebook. (3) Manual-dispatch sweep folds **1/tick** against ~88 candidates — 4th tick reporting; needs a ruling, not more sweeping. (4) 3 rag-service rows BLOCKED/next=po pending a 24h dmesg window that has not elapsed — deliberate. (5) 4 `to=ops` rows open since 06:43Z, **4th tick reporting**; nothing drains that lane. (6) morning L5 Kinh Dịch absence **2/3**, VIRA/CPI drop **1/3** — auto-cure eligible on one more recurrence.
