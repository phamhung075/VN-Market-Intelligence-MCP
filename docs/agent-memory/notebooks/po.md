# PO — Notebook

## 2026-08-14T09:31Z · Our own stopgap destroyed the certification window — the durability bar was gameable by the mitigation

- **Trigger:** router escalation — rag-service "memory-creep fixed" (82216e291) confirmed not fixed; monotonic climb 84.85%→86.11%→89.18% on the post-fix image. Asked to reopen `FU-RAG-DEPLOY-MEMORY`, route root cause, judge the closing bar. **Output: 1 `orch-apply.sh` pipe, conservation OK (741→741), 1 de-certification + lane move, 3 rows amended, 3 signals triaged, 0 mints.**
- **★ THE MITIGATION AND THE CERTIFICATION WINDOW ARE MUTUALLY EXCLUSIVE — AND NOBODY HAD NOTICED.** The ops preemptive restart the router dispatched *in parallel with me* landed at 09:23:21Z, **42 minutes into a 24h window** opened at 08:41:48Z. RAW-verified: `.State.StartedAt` moved, memory 34.54MiB = 3.37% (cold). **Restarting a leaking process at 89% then reporting "zero OOM in 24h" is a false green of the same family as the `docker inspect .State.OOMKilled` false-negative the bar was written to replace** — it measures the mitigation, not the fix. Declared the window **VOID, not reset**: if the service needs a restart every ~40–80min to survive, it *cannot* produce a valid window, and that impossibility **is** the finding.
- **★ A PURELY NEGATIVE AC CANNOT ESTABLISH BOUNDEDNESS, AND HAS NOW FAILED 3× ON ONE INCIDENT.** thread-pin → certified → OOM at +1h00m/+1h14m/+20h34m. cap-raise → certified → 3 kernel OOMs on the raised cap. skip-rebuild → STALE-ACK'd benign on one 84.85% reading → accelerating climb. Every time: "no crash observed yet", over a window shorter than the failure's own period, on a signal that cannot see the failure. **BAR v2 adds a POSITIVE plateau (≤0.02 pp/min over final 12h AND ≤85% of cap) — a window ending at 95% with zero OOM is now a FAIL.** Plus D2 one-continuous-lifetime (unchanged `.State.StartedAt` at open *and* close) and D4 window-invalid-if-mitigated.
- **★ THE ROUTER'S PREMISE WAS PARTLY WRONG AND QA DESERVED CREDIT, NOT CORRECTION.** Reported as "marked DONE_VERIFIED again". `FU-RAG-DEPLOY-MEMORY.updated_at` = **2026-08-08T10:59:52Z**, untouched since; and the actual 08-14 row was **correctly left at `QA`** with `qa_durability_certified:false` and an explicit "cannot certify this cycle" note. The harm is real but the mechanism differs: **a terminal row read as evidence**, not a fresh bad closure. Worth stating, or the next actor hunts a QA defect that does not exist.
- **★ DE-CERTIFIED WITHOUT REVERTING — THE DELIVERY STANDS, ONLY THE DURABILITY CLAIM FALLS.** 2f835ec63 (cap 768m→1g) is live *right now* (RAW: `HostConfig.Memory=1073741824`). Reverting to IN_PROGRESS would assert an un-delivered change and re-open a decision the architect made correctly. Moved `done_verified[]`→`review[]`@`BLOCKED` instead — terminal rows are **invisible to every lane-scoped dedup scan, cold-evictable, yet still readable as "closed"**, which is exactly what produced the 08:57Z STALE-ACK. Same treatment/reasoning as `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` 2.5h earlier — applying the convention, not inventing one.
- **★ FOUND THE COMPOUNDING GAP: THE ROW WAS EXEMPT FROM THE VERIFICATION GATE ENTIRELY.** `orchStateSchema.ts:~536` lists it in `RC_VERIF_GRANDFATHERED_IDS`, so `checkVerificationGate()` never demanded a `verification.raw_probe{}`. It reached DONE_VERIFIED with **no live re-probe at all**, and the exemption **survived the falsification**. Filed as AC-ext (5) on the gate row (P1→P0); schema is not my zone.
- **Zero mints, deliberately.** Both live rows carry standing "do not open a 3rd tracking row" instructions, and parallel measurement of this one metric already produced **5 consecutive no-op folds**. Routed instead to the row that already predicted today's data on 08-12 ("bounded retention is flat after first unload; this is monotonic growth from cold → a second, unbounded source"). Today **confirms** it on the post-fix image.

### Carry-over (09:31Z)
- **★ When a stopgap and a certification run in parallel, the stopgap wins and the certification is silently voided.** Nobody wrote that down; the window note still pointed at 2026-08-15T08:41:48Z on a container lifetime that had already ended. **Check whether a peer mitigation landed inside your own measurement window before trusting it.**
- **★ "Necessary but insufficient" is now the verdict on three consecutive rag-service fixes.** All three attacked the *environment around* the process (cap, thread pool, restart-time rebuild). **Ruled: no more restart-timing or deploy-config tweaks — the next change must come from measuring where the bytes go inside the process.**
- **Unverified by me:** did not read dmesg in-VM this tick (no new OOM expected — container restarted cold at 09:23Z, measured 3.37%). The three historical OOM timestamps are carried from QA's in-VM read, not independently re-confirmed.
- **Retention deliberately left to the auto-pruner:** 3 peer PO sections were written to this file within the 20min of my cycle (09:11Z/09:29Z/09:32Z). Hand-pruning to "last 3" during that churn is how concurrent peer entries get dropped — left all 4 for `scripts/agents-flow/notebook-auto-prune.sh`.
- **Router action needed: dispatch `dev-rag-service` on `FIX-RAG-EMBEDDER-IDLE-UNLOAD-ALLOCATOR-PAGES-NOT-RETURNED-TO-OS`** (`REVIEW`/P0/`next_agent:dev-rag-service`, dispatch-ready). `.head` deliberately left `idle` — a triage ruling must not reset a dispatch lane.

## 2026-08-14T09:32Z · Closed an epic wrapper whose only deliverable was the split — and the schema was right to refuse my first write

- **Trigger:** dev-team Review-Lane SECONDARY-Drain handed me `FIX-PUSH-DELIVERY-ERROR-RATE-ALERT` (`review[]`, `next_agent:po`, so QA-Drain never sweeps it). **Output: 1 `orch-apply.sh` pipe, conservation OK (741→741), `review[]` 95→94, `done_verified[]` 25→26, 0 mints.**
- **★ I RATIFIED AT SOURCE, NOT ON THE SPLIT NOTE'S PROSE.** The row is `supervised:true`, and `supervised-goahead.md` Step 2 exists precisely so PO is the ratifier of record — accepting a relayed verdict makes the gate vacuous. 4/4 architect claims re-derived: no `/metrics` route and no `prom-client` (both greps empty), `/api/vps-proxy-health` at `server.ts:1023`, and `vpsPushLogStore.ts:97` hardcoding `["prices","news","sbv","bctc"]` while the `VpsService` type at `:13` **does** include `foreign-flow`. The specced gap is real.
- **★ THE LOAD-BEARING CLAIM WAS THE SAFETY ONE.** `classifier.go:51` maps `high|critical → ChannelMarket`, which `alert-policy.md` reserves for alert-commander stock alerts. The child's "hardcode ChannelWork" mandate is therefore **necessary, not stylistic** — routing this through `Classify()` would push infra noise into the MARKET channel.
- **★ I RAN DEV-TEAM'S OWN ELIGIBILITY LIBRARY INSTEAD OF ASSERTING DISPATCHABILITY.** `is_bounded1_eligible` against the live board: child 1 `true`, lane `dev-mcp-server`; child 2 `false` **solely** via `deps_ok:false` on its `depends_on`. So it self-releases when child 1 lands — machine-encoded, not prose (`has_unbacked_sequencing_prose` fires on neither). **A prior PO's "zero pickers" claim on this class was wrong; measuring beats asserting.**
- **★ `orch-apply.sh` REJECTED MY FIRST WRITE AND WAS RIGHT.** I wrote `next_agent:null` to mean "nobody next" — `TaskSchema` declares `z.string().optional()`, optional but **not nullable**. Validator exit 2, live file untouched. Correct encoding is deleting the key, not nulling it.
- **Stamped `po_goahead` on a TERMINAL row deliberately:** WF-2 SUPERVISED-HOLD scans `done_verified[]`, and I kept `supervised:true` as the historical record of why SLS routed this to architect. Unstamped it could hard-hold the pipeline if `.head` ever named it. Replayed WF-2's own predicate post-write: `SHOULD_HOLD=false`.
- **Added the missing forward `children[]` edge** — the children pointed back via `split_from`, but `is_epic_wrapper` (`devteam-eligibility.jq:167`) reads `children`, so this row never registered as an epic wrapper to any sweep.

### Carry-over (09:32Z)
- **★ Closing a wrapper ≠ shipping the capability.** The 2026-07-04 CF-tunnel gap (2 outages, 0 detection) stays OPEN until `FIX-PUSH-DELIVERY-ALERT-ENGINE-POLLER` ships. Both children are P1 in `backlog[]` and inherit fleet RLC/BOUNDED-1 throughput limits — this closure bought traceability, not detection.
- **Still live, unrelated to this closeout:** `resolved_dispatch_lane` has no `zone=="multi"` special-case, so it defaults multi-zone rows to `developer` instead of `architect`. Only ever row-level patched (`router_dispatch_correction_20260808T0223Z`), never fixed systemically.
- **Notebook hazard hit live:** the `09:29Z` section below is a **concurrent peer PO's** work, not mine. po's declared OVERWRITE contract (preamble+1 section) would have destroyed it, so I inserted instead and left both prior sections byte-identical (41L, under the 50L cap). The AC-6 contract and the known peer-overwrite data-loss class are in direct tension for this file.
- **Router action needed: none.** `.head` (`FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`, `next_agent:qa`) was not this row; left untouched.
