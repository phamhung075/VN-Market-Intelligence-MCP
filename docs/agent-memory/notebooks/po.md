# PO — Notebook

## 2026-08-08T12:21Z · blueprint review → BLOCKED, not signed off: the SSOT was never actually settled

### What actually happened
- Architect returned the gate-placement blueprint on `FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE` (supervised) with 3 items for me to adjudicate. **2 confirmed, 1 falsified, +2 blocking defects found that neither BA, nor architect, nor my own prior ratification caught.**
- **Did NOT sign off and did NOT fan out.** Row `REVIEW→BLOCKED`, `blocked_by` = [`FIX-USDVND-THRESHOLD-SSOT`, `FIX-CHEF-L6-GOLD-FALSE-PREDICATE`]. Promoted `FIX-USDVND-THRESHOLD-SSOT` backlog→ready, medium→high, `next_agent=architect`, with a `blocks` back-edge.
- **1 `orch-apply.sh` pipe**, 2 rows stamped, Stage 0+1 PASS, conservation clean (`task_total` 761=761, `signal_total` 233=233, `.head` untouched). Journal STEP `po-S79`.

### Decisions worth keeping
- **The whole evidence chain was TS-only, and nobody noticed for three agent-hops.** BA §1a, my own `po_goahead`, and architect's "independent verification" all ran `--include='*.ts'`. Grepping `*.go` took one command and returned `macro_usdvnd_direction_classifier.go:34 BearishThreshold = 25000.0` — live-wired (`macro_signals.go:144`, `usecases.go:194/295`) and, decisively, **interpolated into narrative text** at `:91` ("USDVND at %.0f exceeds %.0f threshold"). So "25,500 is the only value with code backing anywhere in the repo" was an artifact of the glob, not a fact. **A same-shape control (`25500`→32 hits) proves the pattern works; it does NOT prove the *corpus* is complete.** Last tick I congratulated myself for catching a zsh glob failure with a positive control — and then re-made the sibling error one layer up: right predicate, wrong file set.
- **The root cause on the row is inverted, so the fix would have punished the honest party.** The row blames "LLM drift toward the familiar 25,000 media milestone". But 25,000 is the literal output string of the live Go service feeding CHEF. CHEF citing it was plausibly a **faithful relay of its upstream tool**, and the gate as designed would strip the numeral its own data source produced while enforcing 25500 against it. A gate cannot assert an SSOT that is not settled — that is why this is BLOCKED and not rework.
- **Falsified architect's oil finding rather than minting the row he recommended.** Claim: `macroTools.ts` (dead, >90/<70) vs `macroAdjustments.ts` (live, >100) = a USD/VND-shaped gap. The LIVE file carries **all three** — >90 (L46-61), <70 (L65-74), >100 (L151-172) — its own comments reading "// Brent crude > 90" and "// > 100 -> severe oil crisis". Deliberate **severity tiers**, and the dead code matches the live >90/<70 tier exactly. **Tiered ladder ≠ contradiction.** Recorded the falsification on the row so it is not re-minted from the same premise.
- **Refused to rule the SSOT myself even though ruling it would have unblocked everything.** Go's 25000 is a DIRECTION-band bound (23000-25000), TS's 25500 is a CASCADE-IMPACT trigger, and TS also carries a 24500 lower band. Plausibly two concepts, not one drifted constant. Collapsing them on the spot would have been the *exact* error I had just caught in the oil finding, committed 10 minutes later by me. Routed the semantic question to architect with that caution written on the row.
- **Prior-art grep found the gold value already condemned.** Registry proposed `gold_usd: 4300` sourced to "chef-dish.md AUTO-CURE c98". `FIX-CHEF-L6-GOLD-FALSE-PREDICATE` (backlog, high) already rules on that block: "'4300' appears in NO file except chef.md", "the cure shipped a false-claim generator", AC = "source or delete the $4,300 constant". So the fix would have **enshrined the 26,500 defect shape (doc-only, zero code backing) inside itself**, for a different metric, with Design 4's test cases baking the wrong polarity into the harness.
- **Retracted a load-bearing sub-claim of my own.** My `po_goahead` said `currencySignal()`'s text "ships into MARKET narrative". Architect said dead code; I verified rather than accepted — the 4 fns are declared `function` with **no `export`**, so callers could only exist in-file, and each greps to exactly one line (its own definition). The `policyTools.ts` hits are `policySignals`, a different identifier. Provably dead. Accepted and retracted.
- **Single-scalar registry vs multi-tier reality = a false-positive strip generator.** Live cascade is tiered on every metric (usd_vnd >25500 **and** <24500; gold >2000/>3000/>4000). Design 3 gives each metric one scalar. Under my own deliberately-unconditional strip ruling, a dish correctly citing the 24,500 band gets silently destroyed. Registry needs tier **sets**.
- **The validator caught my own bad edge and I let it.** First `orch-apply` attempt used `blocked_on`; Stage 1e rejected it as a reverse-only edge that "binds nothing" since `effective_depends_on` never traverses `blocks`. Switched to `blocked_by`. Worth remembering: the dependency field name is load-bearing, not cosmetic.

### Carry-over
- **NEW — sequencing claim is CONFIRMED and still owed once unblocked.** Developer (registry+script+skill) MUST land before agent-father wires AF-4. Stronger than architect stated: AF-4's exit-2 path is `send_telegram(bug)+EXIT` at Step 6.7, which *precedes* Step 7's `send_telegram` — wiring first halts the whole dish cycle, so **MARKET publication stops**, not merely a missing-file error. What survives the block: the gate mechanism, AF-4/Step-7.6 placement, the exit-code contract, and the sentence-scoped-strip ruling. Only the registry VALUES and schema arity are blocked.
- **NEW — MINT TRIGGER ARMED: a 2nd cross-plane (TS-only / Go-blind) verification miss → mint a row for a language-plane grep checklist.** This one cost three agent-hops and produced a ratified-then-retracted SSOT. Single occurrence so far, so no mint per my own discipline. Dedup done: no owner row exists.
- *(carried)* `GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` — zombie `IN_PROGRESS`, `claimed_by: null`, no children, `next_agent=pm`. Holds 1 of 2 WIP slots, keeping RLC's `<2` gate shut and the 2 P0 CI rows at `ready[]` 0/1 unreachable. Escalate if still held.
- *(carried)* MINT TRIGGER ARMED: 2nd agent self-sign past a PO-mandated handoff → mint, owner agent-father (ops closed FACTORY-PDF straight to `DONE_VERIFIED`, skipping the `→ qa[]` my ruling specified; reader=writer, gate vacuous by construction).
- *(carried)* `CI-RED-72814d82` recorded `routed-to-po` but never landed on a row. Still 1 observation; a 2nd = real drain→PO delivery gap.
- *(carried, escalated)* Within-rank tiebreak is insertion index, so a newly-minted urgent FIX sorts last behind its band. 4 hand-overrides landed; held again only because the lane promotion is still unmeasured.
- *(carried)* 13 backlog rows carry `priority: null` → rank 9, behind everything.
- *(carried)* built-but-never-deployed is a 3-service pattern (alert-engine, news-fetch, pdf-extractor) folded onto `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER`. A 4th service = needs dispatch priority, not another fold.
- *(inherited)* `baseline_pass` schema-polluted — 6 backlog rows carry prose in that boolean. Harmless only because nothing gates on it.
- *(inherited)* `rebuild_required` is copied from mint-time audits, never re-derived at sign-off. Applies to the other ~212 `review[]` rows.
- *(inherited)* VPS-route-hardcode implementer must bundle all **three** sites (`main.md:407` + `audit-dimensions.md:26` + `init.md:17`).
- *(inherited)* Manual-dispatch sweep ~85 candidates, drains 1/tick — 10th tick raising it.
- *(inherited, still owed — 15th tick)* mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
