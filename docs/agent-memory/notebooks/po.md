# PO Notebook

_Last: 2026-07-29T11:33Z (rag ACK scope mismatch + 4 carried cowork signals) · 2 rows minted, 1 priority raised, 8 rows annotated, 1 ledger corrected._

## This cycle

- **The scope mismatch was real, and the thing under it was worse.** The router asked whether the ACK's `tracked_by` was too narrow. It was — but `tracked_by` is read by **nothing**. `auditor-tier1-probe.sh:275` extracts `.acked_memory[].container` and stops. The STALENESS RULE that both the ledger comment and the probe comment promise ("remove once tracked_by reaches DONE_VERIFIED") is enforced by no code, no cron, no test. A suppression whose only safety property is documentation is not narrowed by editing the documentation.
- **Binding the ACK to a time-gated row made it un-expirable by construction.** `RAG-FTS-BUILD-MEMORY-BOUND` is `blocked_on` corpus repopulation to ~56k rows, projected ~2026-08-16. Even if `tracked_by` *were* live, the ACK could not expire for ≥18 days. Re-pointed to `FU-RAG-DEPLOY-MEMORY` — the row whose DONE_VERIFIED would actually mean rag-service no longer sits at 97% of its cap.
- **The falsification was already inside the row that was cited as the cause.** That row's own `qa_result.idle_baseline_flag`, written 2026-07-15, says "idle mem already ~92% of 768m with ZERO FTS activity". The ledger was seeded 2026-07-25 naming it as root cause. Ten days, same file, opposite claim. Prior art is not just a dedup check — read what the cited row *says* before citing it.
- **Neither widen nor mint was right for rag.** An earlier tick today (10:20–10:31Z) already root-caused this to the embedder singleton + the compaction counter defect, across `FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP` and `FU-RAG-DEPLOY-MEMORY`. Answering the router's question literally would have minted a third.
- **"Narrow the ACK" was not an available operation.** `:302-307` is one `pct >= WARN_PCT` test, so an ACK'd container is suppressed identically at 85.01% and 99.99%. The only two states are suppress-everything or re-churn the auditor 48×/day. Minted the row that makes the middle expressible — an absolute-headroom floor the ACK may not swallow. Not a new idea: the parent row deferred exactly this, *"if the loop proves the percentage view insufficient."* It has.
- **The 199 `POST /index` were not an agent burning headroom.** Bucketing 24h of logs by source IP settled it: 51–258 calls/hour, **every hour including overnight**, corroborated by corpus growth of ~103 rows/h. An improvising agent makes a burst, not a diurnal-invariant stream. `192.168.65.1` decodes to the Docker Desktop port-forwarder — it means "arrived via the published port", not "someone typed curl". Recorded on the row so the next auditor does not re-litigate it.
- **I overclaimed "flat" on two samples and had to correct myself 9 minutes later.** 749.9 @10:25Z → 745.8 @11:19Z read as *down* 4.1 MiB; the 11:34Z sample was 756.6 MiB. Five samples show an ~11 MiB oscillation band — **22.2 down to 11.4 MiB of headroom** — which is the optimize-burst shape the CLEAN-EXIT row already predicts. No leak is demonstrated and the fixed baseline claim survives; the word *flat* did not. I made the exact 2-sample error I had just flagged the router for.
- **The row the router asked me to prioritise was the wrong row.** `FIX-AUDITOR-COMMIT-NONEXPLICIT-PATHSPEC` is SUPERSEDED and says "DO NOT WORK THIS ROW". Bumping it re-creates the duplicate the supersede prevented. The 28-signal evidence went to `...-LAYER2` (**P1→P0**) — sole remaining dependency of a P0 parent, and Layer-0 stays WARN-only until it lands, so 28 signals/day are hitting an actuator that cannot act.

## Carry-over

- **`FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-LAYER2`'s "4 remaining call sites" is a floor, not the spec.** 8 distinct agent commit paths swept in one day, and system-auditor **bypassed a mandated pathspec script it already has** (`scripts/auditor-notebook-commit.sh:202`). The work is "make the existing actuator the only reachable path", not "edit 4 sites". Re-scope before implementing.
- **`FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` reproduced 3× today while sitting in REVIEW.** qa must verify against an observed dispatch's **claim/release order**, not flow-doc prose — reading the doc cannot distinguish "fix not applied" from "fix applied to a doc that does not bind the executor".
- **`FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY` cannot pass QA from source.** Source has `container_vm_headroom_mb`; the running image emits `host_headroom_mb`, so cowork fan-out has been pinned at MAX_PARALLEL=2 since 07-28. Gate must assert the **deployed** emit. No rebuild dispatched — user-gated.
- **rag-service is invisible to the fleet's only memory detector until the headroom-floor row ships.** 22.2 MiB free on a 768 MiB cap, ACK'd, ALL_GREEN. That is the accepted cost of not re-churning the auditor, and it is now on the record rather than implicit.
- **A-30 row 5d+, `recurring_bug_count=6`, 8th consecutive carry.** Still BACKLOG, no architect activity → **escalate to user next tick**. Do not re-attach evidence again.
- **`supervised = never dispatched` watch, 7th tick.** Both rows minted this cycle are `supervised:false` deliberately, to stop feeding that pile while it is unresolved.
- Standing, 15th carry: verify my own commits carry my authorship (`git log --author`).
