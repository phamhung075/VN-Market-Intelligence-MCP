# PO — Notebook

## 2026-08-22T18:42Z · Four "ghost zones" the user saw by eye, four non-empty backing tables. Not one was a frontend bug.

- **Trigger:** router-relayed user report, verbatim — *"zone not working on frontend, actual i see frontend many zone is like ghost"*. **Output: 1 `orch-apply.sh` pipe (5 rows minted to `backlog[]`, 438→443), 1 decision-journal doc. task_total +5.** All 5 → `zone: apps/mcp-server/`, `next_agent: ba`.
- **★ "ZONE" WAS LITERAL, NOT OUR INTERNAL VOCABULARY.** First instinct was to read "zone" as `apps/<service>/`. Wrong: `apps/frontend/app/components/analysis/*Zone.tsx` are six real components (Technical/CorporateEvents/Financials/Reputation/NewsBuzz/ConvictionHistory) on `/dashboard/analysis`. **When a user's word collides with a project term, check whether the product literally ships that word before reaching for the internal meaning.**
- **★ I ALMOST FILED A FABRICATED P0 OFF MY OWN BAD JQ.** My conviction probe used `(.history//.points//.rows//.entries//[])|length` and returned `series: 0` for **all 8 tickers** — I was one step from filing "`?symbol=` filter broken for every ticker". The field is `series` and it is an **object map**; my alternation never matched it, so `[]` was the default. Caught only because :3000 direct and the :3001 proxy disagreed — impossible for a verbatim passthrough. **A jq alternation that misses the real key is indistinguishable from empty data. "Broken for 100% of inputs" should indict the probe before the system.**
- **★ COUNT THE BACKING TABLE BEFORE BLAMING ANY RENDERER — IT REVERSED THE ROUTING TWICE.** The report arrived as a frontend bug and the coverage map still marks these LIVE. But `conviction_history`=3942, `cascade_rule_hits`=10976, `daily_foreign_flow`=6302, `vnstock_trading_stats`=7449 rows. Zero frontend faults, and zero plain "no data yet" gaps either — the answer I expected and did not get. **2 server-side query defects, 2 writer defects.** The zone components render their empty states *correctly*; they are behaving right while showing nothing.
- **★ A `LIMIT` THAT WAS SAFE WHEN WRITTEN BECAME SILENT NEWEST-DATA TRUNCATION.** `convictionHistoryStore.ts`: `ORDER BY date ASC … LIMIT 2000`. Proof, not inference — `SELECT MAX(date) FROM (…ORDER BY date ASC LIMIT 2000)` = `2026-06-17` = **exactly** the `tradingDate` the API reports, `staleByDays` 64 agrees. 1907 rows (48%, all recent, writer healthy at ~33/day through 2026-08-21) invisible. Docstring said *"766 rows … well within the default cap"* — true on 2026-06-11. **The table outgrew the cap and the cap silently ate the newest data; it staled one more day every day and could never self-heal.**
- **★ THE FIX WAS ALREADY WRITTEN IN THE COMMENT ABOVE THE BUG.** `foreignFlowHandler.ts` line 8 states the contract — *"Latest trading day = MAX(date) with foreign_volume IS NOT NULL"* — and the SQL subquery omits that guard. It grabs `MAX(date)`=2026-08-22 (41 rows, **all 41 NULL**), then the outer non-null predicate wipes the set. 2026-08-18 with 99 good rows sits right there. **No design question to escalate; read the docstring before writing a spec.** Intermittent because it only fires when a partial/NULL batch lands on top (also 08-16, 08-15) — which is why the user said *sometimes* ghost.
- **★ REFUSED TO MINT THE CASCADE SYMPTOMS.** `momentum-indicators.foreign_accum` (null, *"Insufficient tickers ≥5 days flow data"*) and `money-radar` (`coverage_pct` 0.83) both consume foreign flow. Named them **inside** the foreign-flow row as expected-to-clear + re-measure, rather than 3 board rows for 1 defect inviting two devs to chase symptoms. Same call excluded the WARMUP set (`indicator-gauges` sentiment 18-of-21, breadth `sessions_below_21`): by-design, self-healing. **Filing warmup as breakage teaches the fleet that warmup is breakage.**
- **★ REFUSED A MERGE THAT WOULD HAVE LEFT THE PAGE STILL GHOST.** My `market_prices` row looks like existing BLOCKED `FIX-DEPTHTHIN-A-PRICE-HISTORY-RETENTION-10D` — same endpoint, so merging was tempting. Different tables: `market_prices` (current: **2 rows, BRENT+GOLD, zero equities**, docstring recorded 121) vs `market_prices_history` (depth). Merge → one lands, `/api/sector-rotation` stays empty. Cross-referenced both instead.
- **Durability over restart:** restarting the `cascade_rule_hits` writer (dead since 2026-08-15 08:12, inside the known host-suspension window) clears today's symptom but re-ghosts next outage — a hard 7-day window renders "writer dead" and "quiet market" identically. That row carries a **second** deliverable (surface `last event <date>`) and states (a)-alone is insufficient.
- **The detection gap is the real finding:** `frontend-data-coverage-map.json` is 53 days stale (2026-06-30) and still claims 40 LIVE / 1 GAP; its own `update_mechanic.L4` says system-auditor should read it as freshness SSOT and alarm. It flagged none of these four. **The user was the monitor.** P2 row opened for a *scripted* sweep — a hand-regenerated JSON rots again by definition.

### Carry-over
- 5 new `backlog[]` rows, all `apps/mcp-server/` → **ba**: `FIX-GHOSTZONE-CONVICTION-ASC-LIMIT-TRUNCATES-NEWEST` (P0), `-FOREIGN-FLOW-MAXDATE-MISSING-NONNULL-GUARD` (P0), `-MARKET-PRICES-DRAINED-TO-2-COMMODITY-ROWS` (P1), `-CASCADE-RULE-HITS-WRITER-STALLED` (P1), `-COVERAGE-MAP-STALE-CONTRADICTS-LIVE` (P2).
- **Do NOT reroute any of these to dev-frontend.** The `api.*.tsx` proxies are verbatim passthroughs; every root cause is in mcp-server. That misroute costs a full cycle proving the frontend innocent.
- Both P0s are one-line-class query fixes with exact reproductions in the row `note` — ship them together, they share a regression-test shape ("what the API serves must match `MAX(date)` in the table").
- After the foreign-flow fix, **re-measure** `/api/momentum-indicators` + `/api/money-radar` before opening rows for them.
- `FIX-WATCHLIST-ROSTER-RESTORE-23-LOST-TO-CORRUPTION-RESEED` — backlog, **BLOCKED**, architect, P1. Still do not unblock until TASK_001 is DONE; "just add ACB/CTG/MBB/VPB" remains the manufactured-divergence trap.
- `FIX-AUDITOR-DATA-TIER-NOTEBOOK-WRITE-PATH-UNWIRED` — ready[], developer, P0. Do not re-measure pilot Success Signals until it ships **and** 3 auditor cycles elapse including ≥1 `AUDIT_TIER=DATA` fire.
- `docs/agent-memory/notebooks/system-auditor.md` still live-corrupt (1 section/179L, duplicate `c104`); repaired from `git show f25dc3d27^:` by AC-3 of that P0 row.
- Standing lesson: a stale freshness-SSOT file is worse than none — it answers "is this zone healthy?" with a confident 53-day-old *yes*, which is why nobody looked until a human did.
