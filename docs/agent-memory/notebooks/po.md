# PO — Notebook

## 2026-08-23T09:11Z · cron re-arm guard cluster — 6 rows minted, and verifying the brief corrected it twice

- **Trigger:** router-direct triage, intent `intent:po:cron-rearm-guard-defects`, user verbatim *"send all problem to dev team for fix"*. 3 defects in the brief, +2 in addendum 1 (post-recovery 09:00Z cowork tick), +1 in addendum 2. **Output: 6 rows, 0 folds, 0 dupes.** Journal: `docs/agent-memory/decisions/triage-20260823T0904Z-po.md`.
- **★ I VERIFIED EVERY CLAIM AT SOURCE AND TWO SCOPING PREMISES CAME BACK WRONG.** The brief was detailed, internally consistent, and carried measured timestamps — exactly the shape that invites minting straight off it. Re-reading the three SKILL.md files, `cowork-schedule.json`, and `spawn-fanout.md` changed the routing of two rows. **A detailed brief is not evidence** (`feedback_flow_doc_veto_manufactures_pass_read_spec_before_blaming_script`).
- **★ CORRECTION 1 — the three cron families do NOT share one threshold, and they fail in OPPOSITE directions.** cowork + detect-loop pass `orphan_threshold_seconds: 7200`; standalone passes `120` (tool minimum, deliberate). Byte-identical branch text, both naming session-presence as PRIMARY oracle. So the shape produces **false-LIVE** at T=7200 (the 8h10m outage) and **false-DEAD → double-arm** at T=120 (already confirmed 2x in memory). Two families failing in opposite directions from one spec is the actual proof it is a spec bug, not operator error — that became the row's load-bearing argument instead of the brief's single-blind-window framing.
- **★ CORRECTION 2 — the already-specced catch-up fix cannot close the symptom it was proposed for.** `match-slots.md:40` defines `catchup_raw` verbatim as "one record per considered **`guaranteed:true`** slot". `refine-bctc-slot-1`, the slot cited as evidence, is `guaranteed=false`. So are **6 of the 11** badly-stale slots. Finishing Step 4.55 as specced misses the reported case entirely. Worse: **5 of 8** `guaranteed=true` slots are ≥9d stale (`digest-sunday` **34.7d**, `fb-weekend` 14.8d, `chef-eod` 10.0d, `fb-daily` 9.9d, `chef-morning` 9.1d) — so the brief's premise that RemoteTriggers cover them is contradicted by live data and the guaranteed/non-guaranteed split may not be load-bearing at all. Routed to **architect**, not developer; a "wiring-only" dispatch would have shipped a provable miss.
- **★ CORRECTION 3 — the off-flow detector greps for strings its own file injects.** Addendum 2 blamed a `.output` transcript symlink for carrying all six `OFFFLOW_MARKERS`. Measured: project CLAUDE.md has **3 of 6**, global has **0**, and `Coordination Results`/`Dispatch Routing`/`Expected Behavior` are **0 in both** — they live at `spawn-fanout.md:200-201`, inside `IDENTITY_PREAMBLE`, prepended to **every** spawn prompt. The detector is self-defeating by construction; the symlink merely exposes it. `:376`'s own provenance comment is measurably false. Fixing only the surface, as asked, would have left the real defect.
- **★ REFUSED TO ASSERT WHAT I COULD NOT REPRODUCE.** No `*.output` exists under `~/.claude` at triage time. That artifact shape is recorded on the row as **ROUTER-REPORTED, not PO-verified**, with a re-confirm instruction. The row still stands because the self-injection half is PO-measured and independent of it.
- **★ REJECTED THE FULL BA→architect→PM→dev CHAIN THE ROUTER ASKED FOR.** These are FIX/SPIKE, not sprints. A BA spec for a 3-file doc-truth correction is process for its own sake, and BA authoring an AC for a plane it has not measured is the live defect `FIX-BA-AC-NAMES-PLANE-IMPLEMENTATION-NEVER-WRITES`. Entered the chain at the depth each row needs.
- **Kept Defect 2 a SPIKE.** ttl-1800-with-no-renewal-hook is the most plausible of three candidate planes — which is exactly why I did not mint it as a FIX. Row names a *countable* test per plane and forbids shipping on a hypothesis. Deliberately **not** a hard `depends_on` of the P0, so the architect may conclude "remove presence from the cron decision path" without waiting on it.
- **Verified by execution, not by reading the row** (`zone-routing.md` § A2): all six predicates run live. Five picker-reachable; `FIX-CRONCREATE-CONTRACT-DIVERGENCE` is `deps_ok=false` **by design** — same three SKILL.md files as the P0, dep-gated to serialise, not stranded.

### Carry-over
- **BATCH → router (dispatch now, do not wait for RLC):** `FIX-CRON-REARM-STEP1B1-LIVENESS-ORACLE-BLIND-WINDOW-FALSE-LIVE` (**P0**, `cross-service/`, architect) · `FIX-COWORK-DAILY-SLOT-SILENT-SKIP-NO-CATCHUP-CATCHUPRAW-SCOPED-TO-GUARANTEED-ONLY` (P1, `cross-service/`, architect). The two P3 agent-father doc rows can ship as one dispatch.
- **★ THE P0 AND THE P1 MUST BE READ AS A PAIR.** Guard = trigger; missing catch-up = amplifier. With no backstop, any downtime — this guard **or** host suspension (`project_host_suspension_causes_multiday_cron_silence_backlog_flush`, confirmed 2x) — becomes permanent silent loss, not a delayed fire. Cross-reference written into both `status_note`s so whichever row a reader lands on tells them about the other.
- **Timezone trap promoted from anecdote to shipping requirement** on the P0: `stat -f '%Sm'` prints LOCAL, markers carry epoch/UTC, host is CEST while memory `user_location` says GMT+7. Hit independently twice (`cycle-snapshot-09:03.json` mtime prints "23 aoû 11:03"). An anecdote in a handoff gets lost; a MUST on the row does not.
- **★ BLOCKED AGAIN BY A DEFECT ALREADY ON THE BOARD — 2nd consecutive tick.** manual-dispatch-sweep Step 2 stamp on `FIX-AUDITOR-NOTEBOOK-APPEND-GATE-BYPASSED-ALL-GREEN-WRITE` aborted: 26552B live → 26858B candidate against a growth-only 12000B ceiling. Did **not** split the write (the check forbids exactly that) and did **not** bulk-run `orch-backlog-stub.sh` (outside a triage tick's blast radius). Recurrence recorded in the journal, **not** folded onto `FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-ON-OVER-CEILING-ROWS` (P1, `ready[]`, architect) — folding it would grow that row and hit the same ceiling. The escape hatch from last tick (`detail_ref` migration) works and is still the answer for the other 23.
- **supervised-goahead:** ran dev-team's own `should_hold`; `.head` idle, nothing held, no-op.
- **Not run (deliberate):** channel-audit, TNB — single-intent router dispatch with two addenda arriving mid-task; widening scope would have delayed the P0.
</content>
