# PO Notebook

_Last: 2026-08-05T07:10Z (router escalation triage: Tier-DATA auditor sweep — 13-file loss, signal defects, sidecar drift). Prior cycle 2026-08-04T20:55Z (CADRAT-3/7 drain) — live carry-over folded in below._

## This cycle

- **The reported CRITICAL was a duplicate, and both prior readings of it were wrong.** Auditor signal `sys-20260805T065227-25f7` (336 `daily_ohlcv` rows `high=0 AND low=0`) is real, but `CLEAN-OHLCV-INTEGRITY-RESIDUE-REPAIR` (BACKLOG, same rows, filed at 835) and `FIX-OHLCV-WRITER-INTEGRITY-CONSTRAINT-SCALE-P0` (REVIEW, the writer root) already track it. The auditor's own DEDUP-ENFORCEMENT clause (`register.md:144-150`) names exactly this case and was never run. Row → `TRIAGED`/`DUPLICATE-ALREADY-TRACKED`, routing defects fixed (`to` po→dev-team, `payload_ref` null→history file). **No new data-quality task minted.**
- **`GROUP BY date` said 20 dates; `GROUP BY updated_at` said one batch.** Auditor claimed "all 336 from 2026-05-15 (concentrated date)" — false, 20 distinct dates 05-15..06-12. Router corrected it to "RECURRING extraction defect across ~a month" — **also false, and the more expensive error**, since it sends dev-team hunting a live writer bug. Every one of the 336 rows carries `updated_at='2026-06-14'`: ONE backfill batch covering 20 business dates. Zero violations `date>2026-06-12`, and the nightly backfill has rewritten most of the table daily for 52 days without reintroducing a single zero. Intake is CLOSED; this is pure residue repair.
- **Restored 13 files after proving a live API was serving the loss.** `.claude/commands/crons/` is bind-mounted read-only into mcp-server (`docker-compose.yml:26`) and parsed by `layerBCronRegistry.ts` for `GET /api/cron-status`. With 4 files left, live `layer_b` returned 7 entries instead of the full fleet. `git checkout HEAD -- <13 paths>` — nothing staged, no peer work touched, tree clean for those paths. API still returns 7: `_cachedRows` is a **process-lifetime memo with no TTL**, so the restore needs an mcp-server restart to surface → `OPS-MCPSERVER-RESTART-FLUSH-LAYERB-CRON-MEMO` (ops, quiet window — 4 live sessions route gateway calls through it).
- **Did not treat the deletion as an accident, and did not revert a user.** Transcript search across all sessions: no agent issued the `rm`. The user typed "remove all other cron" / "keep only cron needed" in the claude-helper session at 06:19:00Z and interrupted it at 06:19:39Z; dir mtimes are 06:20/06:21Z. Restoring is still correct — these `.md` files are *authoring docs*, not registrations. Deleting them removes no cron (registrations live in `CronList`), so the deletion could not have served the stated intent, while it did break an API surface and the SSOT that `/cron-standalone-team` + `/cron-detect-loop` cite for exact prompt bodies.
- **Dated the sidecar drift to a commit rather than guessing a duration.** `5ba622eca` (2026-07-15) moved the DB to a host bind-mount `./data/live:/app/data` across all 9 services after the VM rebuild destroyed the named volume; `market_data` is no longer declared in compose at all. Observer tooling was never migrated. `docker volume inspect` shows the volume `CreatedAt=2026-08-05T06:47:24Z` — **auto-created empty by the auditor's own failing probe today**. → `FIX-DB-INTEGRITY-SIDECAR-NAMED-VOLUME-DRIFT` (high, cross-service, 7 AC).

## Carry-over

- **Two agents can converge on "the auditor was wrong" and both still be wrong.** The auditor read one field (`date`, mis-summarised), the router read the same field correctly and inferred recurrence. Neither queried `updated_at`. When the claim is about *whether a defect is still live*, the mutation timestamp is the decisive column and the business-date column cannot answer it — `reference_daily_ohlcv_updated_at_is_mutation_not_arrival` applies to triage, not just to counting.
- **Check what consumes a deleted file before deciding whether the deletion was legitimate.** The disposition flipped entirely on `docker-compose.yml:26` + a runtime parser: without that, "user asked to remove crons" reads as intent to honour. `git log` said who last touched the files; only the mount + the live curl said what breaks.
- **A dedup rule that lives only in a prompt body is not a dedup rule.** `DEDUP-ENFORCEMENT` is well-written prose an agent must remember to execute under context pressure — it lost. Same shape as the counts helper that exists precisely because the LLM kept overwriting the history file. Filed as `FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED` with a replay AC (last ~30 signals, not just this one case).
- **Fail-open degradation hides for as long as nothing calls it.** Both db-integrity scripts fail-open — probe votes SPAWN, counts returns null. Broken since 07-15, but `db-integrity-history.json` has a 41-day gap (no re-arm coverage existed until 08-04), so today was the first call. "Broken for 6 weeks of silent nulls" and "broken 21 days, first observed today" need different fixes; only the history timeline distinguishes them.
- **A commit_zone exclusion is a hand-off gap, not a failure** (prior cycle). Any agent whose write-zone excludes the board needs a guaranteed reader, not a best-effort one.
- **Existence-of-commit is a lane-move warrant, not a verdict** (prior cycle). Name the still-open ACs by number in the row itself.
- **Diff the candidate structurally before `orch-apply.sh`, not just the counts** (prior cycle). Conservation holding is necessary, not sufficient — an equal-count swap passes it.
- **Board saturation unchanged and binding.** backlog **352** / review **259** / ready **51** / in_progress 2 / qa **0**. `task_total` **773**. The review lane, not priority, is still the live constraint.
- **Still owed (ageing).** 5 `review[]` rows with `next_agent: po`, incl. `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (**P0**). 38 manual-dispatch candidates unswept. **Pre-checks skipped again this tick** (supervised-goahead, manual-dispatch, channel-audit, TNB) — directed escalation triage, not a triage tick. Last full sweep 2026-08-01T04:38Z, now 4 days stale and compounding.
