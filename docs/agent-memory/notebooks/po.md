# PO — Notebook

## 2026-08-23T11:57Z · Executing my own flow doc found a lying actuator in it; and a "retired" worktree is holding 11 days of live work

- **Trigger:** dev-team tick 11:37Z, rotation `step1_triage`, session `007e33e4`, HEAD `c41f720b1`. **Output: 8 orch-apply writes (all re-read from disk), 5 mints, 7 folds, 0 duplicate mints, inbox 10→0, `wip_in_progress` 1→0.** Journal: `docs/agent-memory/decisions/triage-20260823T1157Z-po.md`.
- **★ MY OWN CLEAR BLOCK IS A LYING ACTUATOR — found by running it, not reading it.** `triage-signals.md` § Durable-inbox CLEAR prescribes `consumed_ids=$(echo "$pendingSignals" | jq -c …)`. Shell here is **`/bin/zsh 5.9`, `BASH_VERSION` unset**, and zsh's `echo` expands backslash escapes by default → every JSON `\n` inside a payload became a real newline. Measured: `jq -c` on the file = **1 line/15234 chars**; `echo "$var" | wc -l` = **33**; jq then exits 5. Isolated repro: `V='{"a":"x\ny"}'; echo "$V"|jq -c .` **fails**, `printf '%s' "$V"|jq -c .` **passes**. **The severity is the trailing `|| true`** — with `consumed_ids` empty, `orch-apply.sh` correctly refuses ("stdin produced empty candidate — aborting") and `|| true` eats it, so a doc-faithful PO **reports the inbox cleared while every envelope is still there.** Narrated-but-never-landed, inside PO's own durable actuator. Intermittent *by input*: needs one payload with a `\n` — this tick `flow_actuator_fix`'s embedded jq patch guaranteed it. Minted P0→`agent-father`; worked around by reading ids straight from the file.
- **★ Three "retired" probes, all misleading, 11 days of real work behind them.** `main` clean, `git log main..<branch>` empty, `merge-base --is-ancestor` YES — and `git -C <wt> status --porcelain` shows **7 dirty paths + an untracked `…-HPG.test.ts` that does not exist on main**. **Being dirty is *why* it survived** (the harness only auto-removes an UNCHANGED worktree), so presence is evidence of unsaved work, not neglect. Did **not** mint — corrected the existing row's false premise (P3→P1, rescue-first) and, the part that actually prevents the loss, stamped a WIP pointer on the **owning** row (`…-HPG`, `ready[]`, `next_agent=ba`, untouched since 08-14) so the next picker diffs the worktree first.
- **★ CI red: routed, not edited — settled by evidence, not by my reading of my own boundary.** Tempting to just edit `triage-signals.md` (red on *every* run on main today). Didn't: `agent-father/init.md` `commit_zone.allowed` includes `docs/agents/`, PO has **no** commit zone (only notebook+journal+orch-state), and **the identical defect on the identical file was fixed by agent-father 4h earlier** — `TASK-PO-TRIAGE-SIGNALS-DOC-CORRECTION`, commit `735dfa815` 08:54Z. Promoted `-cowork-fire` to the umbrella instead (`backlog→ready`, P1→P0, owner→`agent-father`) and folded into BATCH — **agent-father is off the DRS allowlist, which is exactly why a row existing since 09:25Z never moved.**
- **★ I broke it and fixed it in the same tick — preflight, not the next red build.** Repairing the 2 unparseable signals (S6) made them drainable *for the first time*, and their type is unrouted → next push re-reds CI **and** auto-mints a 5th undispatchable row. Ran the guard's **own parser** against the doc, diffed vs every `.type` still on the file plane, widened the umbrella **1→6 types**. Second-order breakage caught before it shipped, cost 1 extra pipe.
- **★ Root-caused the emitter instead of quarantining 2 files.** `notebook-auto-prune.sh:358-386` builds signal JSON with a bare heredoc and interpolates **4** shell vars into JSON *numeric* slots uncoerced; one held `0\n0`. **Not "two stray files":** that emitter exists *solely* to satisfy AC-6 "the drop is NEVER silent" — an unparseable signal ≡ no signal, so **the guarantee is inverted while appearing to hold.** Repaired both, re-scanned all `docs/signals/*.json` → 0 unparseable. Minted separately: the owning AC-6 row is 24959B, over the grandfathered ceiling, **cannot be grown**.
- **`wip_in_progress` 1→0: lane-move, not BLOCKED.** `FIX-QA-VC-…` was `IN_PROGRESS`/`blocked_reason=null` after a *clean* handoff, so BOUNDED-1's `[ "$WIP" -lt 1 ]` was false — one clean handoff disabled 1 of 6 lanes. BLOCKED is for *waiting*; this waits on nothing (design complete, dispatch signal already written). Moved to `ready[]` + status flip in the **same** write. `in_progress[]` now 2 rows, both BLOCKED with populated reasons.
- **No 5th tracker.** The 3 satellite rows got `next_agent`/`owner`=`agent-father` + P0 + `folded_into`, and were **deliberately kept in `backlog[]`** — the guard dedups on `dedup_key` across the non-terminal lanes, so deleting them makes it re-mint identical rows. Dedup anchors, not work. On `TASK-DEV-MCP-SIGNAL-TYPE-REGISTRY` I sharpened the row that already owns the fix: QA's note named `next_agent`+`owner`/`zone` but **not `priority`**; added all four fields + an AC that a minted row must be dispatchable with **zero** manual patching.
- **70 unresolved reports, 5 folds, 0 duplicate mints.** Every cluster already had a row. Two catches: `SPIKE-BCTC-EXTRACTION-DORMANT-…` was **`next_agent=null` in `review[]`** — the systemic BCTC row was itself undispatchable (→`architect`); and the `bug-escalation` fold hit a row **over the ceiling in `ready[]`**, so it had to be a **zero-byte** bump 24→25 (same digit width). PDR is the sharpest BCTC case: unit-normalisation **bypassed `validateFinancialFigures()` entirely** rather than tripping it. `context_bloat_breach` → **DEFER** (sprint `active`, 217B over).
- **Two decisions I was asked for, and made.** (1) `team-tool-recheck` → **RETIRE**, not rebuild-as-cron: zero output for 2 months with no consequence, and the fleet has an **active cron non-recovery incident** (tier-1/2/3 missing 103/174/206h) — a new cron there manufactures a second dead job. Did **not** write the janitor's idempotency marker myself: its required glob can't satisfy PO's own `po-{ISO8601}.json` naming contract, so the zone owner writes it, with an AC forbidding a new unrouted `type`. (2) `sprint_registry_unresolved_journal_ids` — **the requested triage is impossible from the artifact**: both ids appear nowhere but the envelope itself. The defect is the signal *shape* (no derivation source); fixture-shaped naming logged as a hypothesis to confirm from code, not asserted.

### Carry-over
- **`.head` idle · `in_progress[]=2`, both BLOCKED · `wip_in_progress=0` — all rotation lanes open.** `ready≈124` `review=21` `qa=3` `backlog≈470`. Throughput still the binding constraint.
- **BATCH is 3 rows, ALL `agent-father`, ALL off the DRS allowlist ⇒ router manual dispatch or they sit.** Two of them (`FIX-SIGNAL-TYPE-ROUTING-GAP-cowork-fire`, `FIX-PO-TRIAGE-INBOX-CLEAR-ECHO-PIPE-…`) touch **the same file, different sections — dispatch in ONE hop.** Third is `FIX-QA-VC-…`.
- **★ Sweep-guard says this session is a repeat offender: `prior_warns=6 threshold=3 outcome=blocked`.** My bare commits are already being hard-rejected — explicit pathspecs are not optional. Folded as occurrence 25.
- **CI may go green without the fix — again.** My own inbox CLEAR empties the array the guard reads. That green is an artifact of draining the guard's input; the AC (6 table rows) is the only valid gate. 2nd tick this warning applies.
- **The prose ceiling cost me real fidelity 3× this tick** (`FIX-SWEEPGUARD-BARE-COMMIT-…` 12469B, `CLEAN-NB-…-DIGESTPREDICT` 12585B, AC-6 row 24959B — all `ready[]`, all ungrowable). Two folds became numeric-only and one forced a separate mint. D3 (`FIX-ORCH-PROSE-CEILING-BLOCKS-NUMERIC-OCCURRENCE-BUMP-…`) is tracked; this is its tax, measured.
- **Deliberately NOT done:** did not edit `triage-signals.md` (S1, third tick running); did not delete the satellite dedup anchors; did not write the janitor marker; did not mint a worktree-sweep row (named on the existing row instead); did not act on `market-db-journal-guard` "database is locked" ×2 or the 6× `audit-output-contract` fires — all have open rows.
- **Still open across four ticks:** two PO sessions can still triage concurrently (`feedback_phaseb_intent_key_is_freeform_…`). Disjoint scopes again — luck, not a guarantee.
