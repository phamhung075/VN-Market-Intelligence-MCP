# PO — Notebook

## 2026-08-22T16:41Z · An audit asked me to delete 13 working tools and 619 live scripts. I deleted nothing.

- **Trigger:** router-dispatched triage of code-janitor's user-requested audit "optimize workflow tools actual agent use" (janitor returned NEXT: po, deliberately deleted nothing). **Output: 1 `orch-apply.sh` pipe — 2 folds, 3 rows minted (`FIX-JANITOR-DEADCODE-AUDIT-CALLER-SURFACE-TOO-NARROW-MASS-FALSE-POSITIVES` P1, `DEBT-SCRIPTS-ONESHOT-JQ-TRANSFORM-ACCUMULATION-NO-RETIREMENT-POLICY` P2→ba, `CLEAN-SCRIPTS-54-UNREFERENCED-QUARANTINE-FIRST` P3; backlog 429→432, task_total 699→702). All 3 janitor recommendations rejected as specified.**
- **★ I TESTED THE ORACLE BEFORE TESTING ANYTHING IT SAID — AND THE ORACLE WAS A ROLLING WINDOW.** `tool-usage-stats.json` (real path `docs/agent-memory/modules/`, not the `docs/data/` janitor cited — it does not exist there) is 975 bytes with **no window metadata**. Its own git history refutes it: `uniqueTools` goes 17→19→**0 across five revisions**→15→68→30, and `get_market_snapshot` goes 700→789→**48**→1994→951. **Non-monotonic counts are not a usage ledger.** Absence means "not called this window", never "dead". Had I spot-checked individual tools first I would have inherited the defect.
- **★ 8 OF THE 13 "DEAD" TOOLS HAD REAL CALL COUNTS TEN DAYS EARLIER.** From the 2026-08-12 snapshot: `get_foreign_room`=39, `get_bctc_full`=12, `get_bctc_report_id`=9, `get_insider_sentiment`=10, `get_market_sentiment_index`=10, plus three more. The other 5 dissolved too: `get_agent`/`get_deficit`/`get_files` have **zero exact-word matches** anywhere (janitor's grep was unanchored — it matched `get_agent` inside `get_agent_signals`), so "documented but unused" was false on *both* halves. **Zero tools were deletion candidates.**
- **★ THE ONE REAL DRIFT WAS INVERTED FROM THE FRAMING I WAS GIVEN.** `get_current_date` is not a documented-tool-nobody-calls; it is called in `digest-predict/flow/main.md:50` and **does not exist on the server** (probed live: `-32602 Tool not found`). Benign only because the call site reads "if that tool is available". **The finding was real but pointed the opposite direction** — folded onto `CHECK-TOOL-INTERFACE-SCHEMA-DRIFT-DOC-VS-LIVE` as its fixture rather than minted fresh.
- **★ ONE GREP METHOD PRODUCED BOTH FINDINGS, SO BOTH FELL TOGETHER.** Janitor resolved callers from `docs/agents/*/flow/main.md` alone — excluding every sub-flow doc, `.claude/skills/**`, `.claude/commands/crons/*.md`, `apps/*/src/__tests__/**`, and `dev-standards.md` pointers. Measured on 268 non-jq scripts: **61 (janitor) vs 168 (wide surface) vs 908−54 (full repo)**. Only **54 of 908** basenames are referenced nowhere — 5.9%, not 84%. **`.sh` specifically: 174 of 177 live.**
- **★ THE "MEDIUM-RISK" DELETION LIST WOULD HAVE TAKEN DOWN PRODUCTION.** `cowork-match-slots.js` is invoked by `cron-cowork-team/SKILL.md` + `cron-fb-market-poster.md`; `cadence-policy.js` is under live test by `DWF-phase1-cadence.test.ts` (deleting it turns CI red); `cowork-tick-postflight.sh` is a CANONICAL pointer in `dev-standards.md`. And **`cowork-tick-preflight.sh` is invoked by `cron-code-janitor.md` — janitor proposed deleting a script its own cron runs.** Risk tiers assigned by a defective method carry no information; I acted on none of them, including the "low-risk" one.
- **★ MY OWN FIRST SCAN WAS WRONG IN THE MIRROR-IMAGE DIRECTION.** An early pass reported 10 non-jq orphans and 545 "referenced" `.jq` — because `apps/mcp-server/scripts` is a **symlink to `../../scripts`**, so `grep -r` followed it and every file matched itself. Re-ran on git-tracked regular files with symlinks excluded. **Self-match contamination and a too-narrow corpus are the same bug wearing opposite signs; a reference scan must declare its corpus or it is unfalsifiable.**
- **★ THE MASS WAS REAL EVEN THOUGH THE ORPHAN FRAMING WAS NOT.** 626 of 908 scripts are one-shot *dated* jq transforms (`po-s108-*`, `router-d41-*-20260615`, `po-triage-20260808T1248Z-*`) — 69% of the directory, **516 older than 30 days, zero touched in 7**. Root cause is a policy gap, not neglect: the orch-state write contract *requires* a jq transform per mutation, and Script Persistence says save anything useful to `scripts/` — a SAVE rule with **no retirement rule and no reusable-vs-one-shot distinction**. That tension needs a ruling, so it went to `ba`, not to a patch.
- **★ I REFUSED THE "CANONICALIZE THE 41 ACTIVE SCRIPTS" ASK EVEN THOUGH IT WAS THE SAFE-LOOKING ONE.** 41 came from the broken surface, and a hand-maintained list of live scripts drifts within days. **If an active-script SSOT is wanted it must be generated from a declared corpus, or not built.** Wrote this tick's own jq to scratchpad, not `scripts/` — minting a 627th dated one-shot while opening a row about their accumulation would have been self-refuting.
- **Carry-over:** (1) Test the measuring instrument before acting on any measurement it produced — check for reset/window semantics first. (2) A file with no window metadata will eventually be read as cumulative; make it self-describing. (3) Word-anchor every name grep; unanchored substrings manufacture whole findings. (4) A reference scan must declare its corpus, and must exclude symlinks or it self-matches. (5) When one method produced N findings, refuting the method refutes all N — re-derive, do not spot-check. (6) Never delete on a doc-surface result alone; cron files, skills and tests are call sites too. (7) Risk labels from a defective method are not risk labels.

## 2026-08-15T13:31Z · Fifteen envelopes, six incidents, and a guard that blocked my own mandated write.

- **Trigger:** dev-team Step 1 triage, durable inbox 15 envelopes + 2 Telegram BUGs + manual-dispatch sweep. **Output: 1 `orch-apply.sh` pipe — 4 folds, 2 rows minted (`FIX-REENTRANT-BRANCH-SHARED-SESSION-UUID-DOUBLE-DISPATCH` P1, `FIX-PROSECEILING-PO-MANUAL-DISPATCH-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET` P1; backlog 427→429, task_total 699→701). BATCH of 6. `.head` idle throughout.**
- **★ THE INBOX WAS 15 ENVELOPES AND 6 INCIDENTS; THE DUPLICATION IS STRUCTURAL, NOT NOISE.** Eight notebook-breach envelopes collapsed to four files — **each file fired exactly twice, ~60s apart** (10:30 and 10:31). Two hooks observing one incident, not two incidents. Three `ci_red` envelopes across three different HEAD SHAs all resolved to **one** failing file. Counting envelopes would have inflated a 6-item triage into a 15-item one. **Dedupe on the underlying artifact (file path), never on the envelope or its SHA.**
- **★ RAW RE-VERIFY SAVED ME FROM WORKING A FILE THAT HAD ALREADY HEALED.** `code-janitor.md` arrived as a breach at 194L/15662B. Live `wc -lc` at triage: **58L/4688B — under both caps.** It self-healed in the ~3h between hook and triage. Had I trusted the payload I would have dispatched a manual archive-split against a compliant file. Dropped it from the row's `files[]`. The mirror-image error was equally available: `digest-predict.md`'s payload said 16279B, live is **32387B**. **Signal payloads are point-in-time snapshots and drift in BOTH directions; re-measure at HEAD before dispositioning.**
- **★ THE GROWTH RATE WAS THE FINDING; THE OVERAGE WAS A DISTRACTION.** `dev-team.md` 12837B→22536B **in under 3 hours** while the auto-prune hook was actively firing and self-reporting it could not act. `digest-predict.md` +16KB in 5 days at ~736 bytes/line. A notebook cannot reach that shape through cycle appends — a producer is inlining multi-KB single lines. **A split relocates the bytes and re-breaches; the port from the superseded twin ("producer defect, not overage") was the load-bearing insight and it had never been carried across.** Ported into the surviving row's dispatch.
- **★ A GUARD BLOCKED THE WRITE MY OWN MANDATORY SUB-FLOW ORDERED ME TO MAKE — AND THE GUARD WAS RIGHT.** `manual-dispatch-sweep.md` Step 2 stamps `po_manual_dispatch_*` on its selected candidate. `orch-apply.sh` ABORTED the entire pipe: the stamp counts as prose growth on an already-over-ceiling row (26552B→27043B). This is the **second same-day instance** of the exact class `FIX-PROSECEILING-SECONDARY-CLAIM-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET` fixed for the secondary-drain family — coordination metadata mis-counted as author prose, deterministic livelock. **I did not bypass and did not split the write to sneak past** (the check forbids both by name). Skipped the stamp — fail-safe, since an unstamped row stays eligible — folded via Step 3 anyway, and minted the fix. **When a guard blocks a mandated write, the guard has usually found a second bug, not an obstacle.**
- **★ BOTH TELEGRAM CRITICALS WERE FALSE, AND THE DISCRIMINATOR WAS CO-OCCURRENCE.** A-29 "alertScanParallel stale 25.8h": the job is `success`, rate 1.00, 140 runs. **2026-08-15 is Saturday.** Eight market-hours-gated crons share the identical Friday-close last_run window. *A genuine per-job stall does not stop eight unrelated jobs at the same minute.* D-CYCLE-2 "tier-2 missing 11h": the heartbeat landed **92 seconds after** the WARN fired. **Neither refutation reaches a suppression layer** (`FIX-PO-REFUTED-DETECTOR-FINDING-HAS-NO-SUPPRESSION-FEEDBACK-CHANNEL`), so A-29 re-fires every weekend regardless.
- **★ A DECISION RECORDED ONLY IN BOARD PROSE IS NOT A DECISION.** code-janitor asked for the same team-tool-recheck ruling a **third** time. PO ruled RETIRE on 08-14 and wrote it onto the row — but the marker file that would silence the emitter was never dropped, so the emitter is *correct* to keep asking. Same shape as the prose-ceiling finding: **the state that stops a mechanism must live in the mechanism, not in a note about it.**
- **★ MAIN WAS RED FOR OVER TWO HOURS BECAUSE THE ROW HAD NO `next_agent`.** The size-lint row was minted at 10:31 with full root cause and a written fix path — and no assignee, so no picker could see it. Three probe cycles later the file is still 605L at HEAD. Set `next_agent`, batched rank-1. **A perfectly-diagnosed row with no routing field is indistinguishable from no row at all.**
- **Carry-over:** (1) Dedupe on artifact identity, never envelope or SHA — 15→6 this tick. (2) Re-measure at HEAD; payloads drift both ways. (3) Read growth *rate*, not just breach magnitude — it names the producer. (4) A guard that blocks a mandated write is a second finding; never bypass, never split-to-dodge. (5) Co-occurrence across unrelated jobs refutes a per-job staleness alarm. (6) If a ruling must stop an emitter, land the emitter's own idempotency key — prose does not silence anything. (7) Always set `next_agent` at mint; unrouted rows are invisible.
