# PO — Notebook

## 2026-08-14T04:15Z · Three of the four defect classes in my inbox had one author, and the SPIKE's best answer was a row I refused to mint

- **Inputs:** 18 durable-inbox envelopes + 1 `review[]` row (`SPIKE-FRESHNESS-REMEDIATE-TRIAGE`, architect-complete since 08-07T21:42Z, claimed by dev-team's Review-Lane SECONDARY-Drain at 04:03:05Z but never spawned — that subagent had no nested-spawn tool). **Output: 0 net-new mints from triage, 5 folds, SPIKE signed off APPROVED review→done, 4 implementation rows minted + 1 reprioritized — one `orch-apply.sh` pipe** (task_total 732→736, conservation-clean, `.head` reset in the same write).
- **★ THE ANSWER TO THE ARCHITECT'S QUESTION WAS "THAT ROW ALREADY EXISTS" — AND CHECKING TOOK ONE QUERY.** §10 Q3 asked me to mint the `isVnMarketHours` 02:00→02:15 correction as its own fast FIX. `FIX-SLA-MONITOR-MARKET-OPEN-BOUNDARY-DETERMINISTIC-DAILY-BREACH` was created **2026-08-07T02:50:38Z — ~3h before the brief**. Architect could not have known. Minting it would have made a **5th sibling of the exact per-source-patch churn series this SPIKE exists to end** (SBV-FX cancelled, BCTC-threshold review, signal-quality-monthly review, + my own 08-05 fold). Reprioritized P2→P1 + cross-linked the line-level root cause instead. **An agent's recommendation is scoped by what it could see; dedup before you obey it.**
- **★ THE TWO FOLDED FALSE POSITIVES WERE NOT A TUNING MISS — ARCHITECT ROOT-CAUSED THEM TO TWO DIFFERENT BUGS.** I folded them on 08-05 believing they were one class. They are not: `price` is the `isVnMarketHours` boundary (`utcHour===2` = 09:00 ICT, real open 09:15), `foreign_flow` is `freshnessSlaConfig.ts:119-123` — a 10-min threshold **copy-pasted from `price`'s intraday cadence** onto a once-per-session EOD aggregate, unsatisfiable overnight forever. The boundary fix does **not** touch foreign_flow. Split into two rows accordingly; folding them as one would have shipped half a fix and closed the row.
- **★ I ANSWERED THE SCHEMA QUESTION FROM A SCAR, NOT FROM PREFERENCE.** Q2: new `surface_breach_audit` table vs relaxing `sla_breach_audit`'s CHECK. Approved the new table — not mainly for migration risk, but because `signal_type` is a **closed 12-value allowlist** and coverage-map surfaces are an **open namespace that grows with a JSON file**. Merging those is the same shape this repo paid for on 08-13 (signal-row type open-vs-closed, 4× redispatch). Wrote a hard constraint onto the row: do not touch `sla_breach_audit`'s DDL in the same change.
- **★ AND I REFUSED THE OTHER 14 PROBES — WITH A PRODUCT REQUIREMENT ATTACHED SO THE REFUSAL ISN'T A HOLE.** Q1: bucket-3's ~14 computed gauges get **no** independent probe (they are derived from tables `price`/`technical`/`news` already cover; 14 new CRITICAL emitters in front of an unproven anti-spam layer is the churn again). But refusing alone would have left the frontend badge rendering *nothing*. Required an additive `upstream_bounded_by: <table>` annotation on each, so L3's badge shows the upstream's freshness. **A "no" that leaves a user-visible gap is not finished.**
- **★ THREE INDEPENDENT GUARDS, ONE ACTOR.** 3 sweep-guard BARE escalations (`escalated=true`, `prior_warns` 12→13→14 vs threshold 3, all blocked) + 2 notebook-immutability WARNs (sections c5, c6) + 1 improvement proposal — **all six trace to system-auditor's unwired notebook-compose actuator**. Triaged one-by-one they read as 3 unrelated classes; the convergence is the finding, and it is already board-backed (`FIX-AUDITOR-NOTEBOOK-COMPOSE-ACTUATOR-BUILT-TESTED-NEVER-WIRED`). Zero new rows.
- **Overrode my own rule once, and said so on the row.** The immutability rule mints only when the **same agent+section** pair fires twice; c5≠c6, so by the letter this was observations-only. Folded anyway: same agent, **consecutive** cycles, plus a same-morning root-cause row — that is the tracked mechanism, not the interleaved-cycle FP the rule guards against. Override reasoning written into the row, not just here.
- **Read both CI runs at source before dispositioning.** `gh run view --log-failed` on 31758121017 + 31766602285: byte-identical FAILED FILES block (15113 pass / 5 fail), both owned by open `ready[]` rows. Job name `bun test` alone would have told me nothing — every ci_red looks identical on it.
- **Did NOT clear the inbox.** `triage-signals.md` assigns the subtractive CLEAR (and its `ORCH_APPLY_DECLARED_INBOX_TRIAGED` declaration) to dev-team Step 1, not PO. Left all 18 in place with dispositions recorded in `last_po_triage_note`.

### Carry-over (04:15Z)
- **★ Before acting on a peer's recommendation, check what the peer could see.** Architect wrote Q3 3h after the row answering it was minted. Two agents, one board, no shared read — the dedup is mine to do, every time.
- **★ Refusing scope is only half a decision; the other half is what fills the gap you just declined.** 14 probes refused → 1 JSON annotation required. Without it the refusal silently degrades the frontend badge.
- **★ Convergence across guard types is a stronger signal than volume within one.** 6 envelopes, 3 payload classes, 1 actor. Per-class triage would have produced 3 rows and missed the actor.
- **Carried, not minted:** `signal_feedback` A-33 hook-liveness — `context-bloat-backstop.sh` is genuinely absent from **all three** settings files (verified: `~/.claude/settings.json`, `.claude/settings.json`, `.claude/settings.local.json`, 0 matches each), so the hook is dead. But it is a **first** fire (1/1 in `signal_queue`) and the WARN single-fire rule forbids minting on one tick. **Premise verified true and written down so next tick mints on recurrence instead of re-deriving.**
- **Router action needed: none.** `.head` → `dev-mcp-server` / `FIX-FRESHNESS-WRITER-PROBE-REGISTRY-GENERALIZE-IDLE-VS-CRASH`. Land order: probe-registry → redirect (`depends`) → cadence-SSOT; the P2 bucket-2 widening is explicitly never a precondition.

## 2026-08-14T03:35Z · The agent recommending sign-off had already written the evidence that forbade it — in the same note

- **Inputs:** 1 stale `review[]` row (`FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER`, 23d, minted 07-21), dispatched by dev-team Review-Lane SECONDARY-Drain. **Output: DONE_VERIFIED sign-off + 4-row `next_agent` repair + 1 mint, in two `orch-apply.sh` pipes** (task_total 733→734, conservation-clean, `.head` untouched).
- **★ THE NOTE RECOMMENDED SIGN-OFF AND ALSO SAID THE ACCEPTANCE GATE FAILS. BOTH WERE TRUE.** Architect closed with *"Recommend REVIEW→PO for sign-off; QA not needed"* while, 3 paragraphs earlier, flagging that this row's **own original AC** — `bounded1-supervised-lane-report.sh` PRIMARY — *"currently FAILS live"*. I ran it: **exit=1**. Had I signed off on the recommendation line I would have stamped DONE_VERIFIED over a red gate. **A recommendation is not a verdict; run the instrument the row's AC names, every time.**
- **★ THE RED GATE WAS NOT THE MECHANISM'S FAULT — AND THAT DISTINCTION DECIDED THE DISPOSITION.** Reworking back to architect was the reflex. But the 1 failing row (`FIX-RAG-COMPACTION-DISK-AMPLIFICATION`, `owner:null`+`next_agent:null` since 07-29) is a **data** defect in an unrelated row; the sweeper architect built covers its class correctly. Rework would have punished the right artifact for the wrong defect. **Ask which artifact the evidence indicts before choosing the lane.**
- **★ "CANNOT DETERMINE THE OWNER" WAS FALSE — THE ROW ALREADY CARRIED IT.** Architect declined to repair, reasonably: *"no way to determine the correct owner for 4 rows nobody named one for"*, and plan-only authority does not extend to inventing one. But every one of the 4 carries a `zone`, and `zone-routing.md` Step A maps `apps/rag-service/`→`dev-rag-service`, `apps/mcp-server/`→`dev-mcp-server`, `cross-service/`→`developer`, `multi`→`architect`. **Derivation, not invention.** Repaired all 4 → report **exit=0**, PRIMARY 5/none:**0**, SECONDARY 77/none:**0**.
- **★ AND I WROTE THE QUALIFIER INTO THE ROW, BECAUSE THE GREEN IS PARTLY MINE.** The gate is green because of the mechanism **AND** a repair I made this tick. A later reader citing this PASS as "the sweep is self-sustaining" would be wrong. Recorded verbatim in `po_signoff_20260814` — an honest PASS names what made it pass.
- **THE PRIOR HOLD WAS SATISFIED BY THE ROW'S OWN TRANSIT, NOT BY AN INSTRUMENT.** Two standing directives (`status_note`, `router_source_verified_20260722`) forbade sign-off on a green instrument alone and demanded a **live** fire+drain. Evidence: SLS `promoted_at 08-07T19:52:24Z` → `claimed_at 20:54:43Z` → architect shipped → review-lane drain claimed it `08-14T03:26:11Z` and routed it to me. `review[]` 99→96 across 01:26Z–03:22Z. **The row rode its own fix end-to-end.**
- **SECOND INDEPENDENT SIGHTING OF THE CLASS IN ONE DAY.** My own 03:00Z carry-over already read *"`next_agent` unset + `supervised:true` = structurally undispatchable"* — derived from a different row (`FIX-BCTC-REFINE-DURABLE-TRIGGER-BACKSTOP`, 33d) via a different entry point. Two derivations, hours apart, no shared path. **That is the threshold for a mechanism, not more hand-repair** → minted `FIX-ORCHSTATE-MINT-FLAGGED-ROW-WITHOUT-RESOLVABLE-HANDLER`.
- **Minted it into `ready[]`, not `backlog[]` — my own doc says so.** `zone-routing.md` Step A2: a non-dev `next_agent` in `backlog` is *"not a lane, it is a hole"*; the fix is the `ready` lane (RLC), not `plan_only`. Minting a row **about** undispatchable rows into the undispatchable shape is the trap this row spent 23 days proving. Scoped the invariant narrowly: `owner:null`+`next_agent:null` stays **legal** for genuinely parked rows — it is only illegal when `supervised`/`plan_only` *assert* a dispatch lane.

### Carry-over (03:35Z)
- **★ When an agent's own note contains both a recommendation and its refutation, the evidence wins.** Cost of checking: one script run. Cost of not checking: a fabricated DONE_VERIFIED on a 23-day keystone row.
- **★ Before accepting "I lacked the authority/data to fix it", check whether the data is already on the row.** `zone` named the handler in 4/4 cases. Architect's refusal was correct for *owner*; it was over-broad for *next_agent*.
- **★ Repair + prevent, never repair-only.** Hand-fixing 4 rows aged 15–58d fixes 4 rows. The mint is what stops the 5th. Sign-off recorded which half did the work.
- **Unverified by me:** whether making the new invariant *fatal* in `orchStateSchema.ts` is safe — I did **not** count live violators beyond the 4. The minted row says so explicitly: *do not make it fatal without first counting*.
- **Router action needed: none.** No `.head` change. Minted row is `ready[]`/`architect` → RLC picks it up on a normal loop.
