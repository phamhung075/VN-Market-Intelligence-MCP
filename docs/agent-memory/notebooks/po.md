# PO Notebook

_Last: 2026-08-01T03:0xZ (dev-team Step 1 triage, 27 unresolved telegram reports). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-5.md` STEP po-S115..po-S118 (po-4.md rolled at 37332B/36000B — carry-over from last tick executed)._

## This cycle

- **The "routine BCTC noise" was not routine, and the tell was in the direction.** 24 of 27 reports were BCTC pipeline notices that look like the guard healthily refusing bad writes. It *is* healthy — but I extracted the (supplied → detected) pair from all 19 period-mismatches and the detected period is **strictly LATER in 19 of 19, zero counter-examples** (offsets +3q ×10, +4q ×7, +8q ×2). Random mislabelling cannot produce a one-sided 19/19 split (~2⁻¹⁹). Refusal was masking a **total coverage outage**: 43 minutes of backfill, zero stores.
- **Three prior ticks had this evidence and stopped one step short — at the source file.** `po_live_evidence_*` at 23:00Z / 00:47Z / 01:58Z already logged the storm, refuted their own opening-balance hypothesis, and even found the byte-identical VND fingerprints. All three reasoned **only from Telegram message text** and landed on "fetch/resolution signature" as a *hypothesis*. I read the code. `listSscDocuments` (`ssc.ts:85-90`) has **no quarter parameter in its signature at all**; `fetchParseAndStoreBctc.ts:68` takes `docs[0]` **unconditionally**. Every quarter of a given (ticker, year) resolves to the same document.
- **Minted `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P1) — overturning 3 prior non-mint decisions, deliberately.** Their reasoning ("don't fragment one defect across four rows") was sound for an *unlocated* defect. It does not survive a pinned call site: board grep for `listSscDocuments`/`docs[0]`/quarter-blind returned **zero** rows. The rows they cited own the detector (review/qa), the reparse-corruption cohort, and two backfill *consumers* — **none owns document selection, and none can route a developer to `ssc.ts`.** Textbook detector-right/actuator-blind: the guard kept accruing evidence while the thing that must change had no owner.
- **Wrote the provenance in, so the next reader doesn't double-count my "discovery".** New row carries `po_provenance_*` crediting all three prior ticks by field name and stating exactly what this tick added (source read) vs what it inherited (the observational case).
- **Falsified my own best hypothesis before minting.** I first read the VJC cluster (4262/4263 at 23:09:34 vs 4264 at 23:09:46) as a gate-ordering bug — alerts firing 12s *before* the period check. Read `parseBctcReport.ts`: the check throws at :623, Step 1 extraction is at :627, and lines 276/361 live inside `storeReport`, downstream. **Order is correct; those were different documents.** Would have been a fabricated row.
- **Also ruled out the detector-false-positive reading at source, not by assumption.** A genuine Q4 filing states `01/10→31/12`, boundary pairs (1,10)+(31,12), *both* Q4, with the `01/01` comparative giving one Q1 hit — predicting exactly the observed 42:1, 68:2, 64:10 shapes. A real Q1 filing could not score 42-to-1 for Q4. The documents genuinely are the later period.
- **Caught a non-sequitur in my own predecessor's ACK.** The 23:04:15Z ACK answered TNB's "confirm c120's Telegram landed" by citing report id 4243 — **but 4243 *is* c121's report.** A message's own arrival cannot evidence a different, earlier message's arrival. Corrected in a handoff addendum; c120's Telegram delivery is **re-opened as unknown** (the 50-row page caps at 4207, so 4208-4241 was unreadable).
- **Verified TNB's c120 self-audit at the plane TNB couldn't reach.** `git log` on the handoff: **no commit on 2026-07-30** at all (last is `cdd5fa5ad`, 07-29). The c120 signal file **never existed in any commit**. TNB was right on both counts. Also corrected the *attribution*: the prior ACK filed this under the Bash-grant class, but c119 and c121 both persisted files via **Write** (no Bash needed) — so it's a **Write-without-read-back** gap, not a missing tool grant.
- **Both mandatory pre-checks ran.** supervised-goahead: `should_hold=false`, no-op. manual-dispatch: 38 candidates, top = `FIX-PO-BATCH-MINT-NO-WRITE-ACTUATOR` (**P0**) — stamped and folded into BATCH.
- **Found the 5th and 6th instances of my own P0 while working, not by looking for them.** (5th) The 01:25Z tick recorded that it "picked" `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` for dispatch — live read: `po_manual_dispatch_flagged_at` is **null**, and it resurfaced unflagged in this tick's sweep. Discriminating control: the *same* tick's TE-T02 stamp **did** land (01:58:29Z), so not a global write outage — the same selective narrate-vs-persist split, one tick later. (6th) TNB's c120 is the same class **cross-agent**, which is why AC-3 should be written as a reusable rule.
- **Verified persistence before returning, per my own carry-over.** Two `orch-apply.sh` runs, both exit 0, conservation OK (`task_total 764→765`); re-read all 5 mutations off disk by key name.

## Carry-over

- **A 100%-refusal validator is two different findings, and the second one is the expensive one.** "Is the guard wrong?" is the cheap question and was answered No three ticks ago. "Then what is feeding it 19 bad documents, and who owns *that*?" went unasked for four ticks because each tick appended evidence to the row it already had open. **When evidence keeps landing on a row that cannot act on it, that is the signal to mint the actuator — not to write a better note.**
- **Reading the Telegram text is not reading the system.** Three ticks of careful inference from message strings; one file read settled it. Escalate from log text to source the moment a hypothesis survives two ticks.
- **Directionality is a cheap, strong test — use it earlier.** Extracting (supplied → detected) into a two-column table took one `awk` and converted "noisy storm" into a one-sided 19/19 result no random process explains. Do this before writing prose about any batch of similar alerts.
- **Journal cap is a *dual*-axis check; the byte axis fires first here.** po-4.md rolled at 279/600 lines but 37332/36000 bytes. My entries are dense, so line count will never be the binding constraint — check bytes.
- **Board saturation unchanged and still binding.** 356 backlog / 249 review / 51 ready / **qa 0**. head went **idle at 02:57:29Z** (developer finished `FIX-SWEEPGUARD-…`), so WIP is 0 and this tick's BATCH is immediately dispatchable.
- **Still owed:** 5 review[] rows with `next_agent: po`; the null-zone board sweep; `FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES`; zone-enum conflict (`docs/agents/` vs `main.md:30`) now on its **3rd** consecutive tick — per my own last-tick note, that threshold means it needs a row next tick.
- **Not run this tick:** channel-audit (all 27 reports triaged directly; nothing newer than 2026-08-01T01:02Z). Step 0-TNB: c121 was already ACKed 23:04:15Z — appended a **correction addendum** rather than a fresh ACK.
