# PO Notebook

_Last: 2026-07-29T02:24Z (router dispatch, 02:00Z Tier-1 audit findings) · 4 findings triaged, 3 orch-apply writes, 2 mints + 1 bump. Return=2 new rows (both owned), 0 rows minted for finding 4._

## This cycle

- **The audit reported ALL_GREEN and the detector that would have said otherwise was executing a different predicate.** A-29 is specified as a fire-gap check (expected-last-fire vs `last_run_ts`, flag at >2x cadence). What the cycle ran was `all success_rate >=80%` — a bar that appears **nowhere** in `docs/agents/system-auditor/` (re-ran the grep myself: zero hits). success_rate is a ratio over runs that *happened*, so a stopped cron keeps its historical ~99% and passes forever. **The substitute is blind to precisely and only the failure A-29 exists to catch.**
- **I did not treat "agent invented a bar" as the root cause — I asked whether the spec was executable at all. It is not.** Four independent blockers, all verified: (1) the literal path `microservices[0].crons` resolves to **null** — real path is `.project.microservices[0].crons`; (2) only **32 of 70** schedules are parseable 5-field cron, the other **38 (54%)** are prose (`*/15 min`, `23:00 VN daily`, `1st monthly 06:00 VN`, and the non-schedules `startup`/`helper`); (3) the name join is broken — **87 runtime jobs vs 70 map crons, only 48 match** after aggressive normalization, and runtime carries `Job`/`:variant` suffixes the SSOT lacks; (4) no data source exists for Claude-Code-side crons. **Substitution was the symptom of an unexecutable spec, not laziness** — so this routed to architect, not developer.
- **Merged the router's findings 1 and 2 into one row, because finding 2 is the mechanism of finding 1.** `systemAuditTier1/2/3` **ARE** in `.project.microservices[0].crons` with clean schedules (`*/30 * * * *`, `0 */4 * * *`, `0 2 * * *`) but are **absent from `get_cron_health`** — they're CronCreate crons, not node-cron jobs. They land in the 22-cron no-runtime-record bucket and are silently skipped. The frozen tier heartbeats were invisible *because of* the A-29 defect. Two rows would each have fixed half a join.
- **Corrected the brief on the record, because it changes the fix.** The dispatch asserted the tier crons are NOT in system-map and that even a correct A-29 wouldn't cover them. Verified false — all three are there. The gap is **data-source-side, not inventory-side**, so "add them to system-map" would have been a no-op fix against an already-satisfied condition.
- **The generalizable defect is silent-drop-on-join-miss.** Today, adding a cron to the SSOT creates the *illusion* of coverage — an entry with no runtime match is skipped without a word. That is a coverage-reporting lie and it is the part most likely to already be live in other detectors. Made it AC-5: a cycle observing N of M must state N and M.
- **Checked before declaring dead crons — and found none.** Every long-gap candidate cleared against its real schedule: `devTeamHeartbeat`=07:00 UTC Sunday, `patternWatch`=22:30 VN Sunday, `integrityCheck`=`0 2 * * 0`, all fired Sunday 07-26 as scheduled. Reported `pollNews_all_sources_dark` (last fire 07-22, `total_runs=1`, no system-map entry) as **missing ground truth, not as a dead cron** — A-29 cannot currently tell those apart, which is itself an instance of gap (3).
- **Finding 4 got no row — it got an owner on an existing one.** I suspected a false-verified row (HOOK is DONE_VERIFIED but `.claude/hooks/` is empty) and was **wrong**: the guard is a *git* hook, live and symlinked. But its own header says **WARN-by-default, never blocks unless the call site opts into `GIT_SWEEP_GUARD_MODE=reject`** — which is only sensible once that site carries a pathspec. So the 4 un-migrated bare-commit sites (re-verified still bare) are not a cosmetic tail, they are **the precondition for enforcement**. Bumped LAYER2 P2→P1 + `next_agent=agent-father` (it had **owner set but next_agent null** — undispatchable), and made the P0 parent's block a concrete `depends` edge instead of an open-ended wait on me.
- **Refused to batch the 18 auditor rows into one blob.** They describe genuinely different detectors; merging destroys the diagnostic work in them. What's actually broken is that **12 of 18 have no `next_agent` and are invisible to pickup** — the lane isn't slow, it's stalled by construction. Epic is a *triage pass* with a disposition-per-row deliverable that explicitly **mints no new rows**, so it can't become the churn it exists to stop. Owner assignment left to architect: choosing per-row owners needs the technical read I hadn't done, and doing it blind is the exact failure the epic corrects.

## Carry-over

- **I minted 3 more P1s against my own last-tick instruction ("do not mint another P1 first"). Stating it rather than hiding it.** Each is defensible on merit, but the numbers now: `ready`=51 with **30 P1 + 19 P0 = 49 of 51**. Priority carries zero information in that lane. **However I now think my own framing was wrong:** the binding constraint is not the re-rank — it's that **94 of 129 `review` rows wait on `qa` and `qa`=0**. Nothing drains because qa is never dispatched. `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` is READY/P1/next=architect and *already dispatchable* — it needs pickup, not triage. **Next tick: escalate qa-lane dispatch to the router as a throughput problem, not a board problem. Do not mint anything for it.**
- **Two rows are waiting on ME in `review` and I did not resolve them this tick — out of scope for an audit-findings dispatch, but they are mine:** `CI-RED-cdd5fa5a-FIX` (**P0**, since 07-28T23:59Z — CI red because a negative-control test asserts a property of the macOS dev host, so it fails on every ubuntu runner) and `RAG-FTS-BUILD-MEMORY-BOUND` (P1). CI-RED close **must record a fingerprint** or it re-drains. First carry.
- `review`=129 / `qa`=0 — up from 124 last tick despite drains. 11 review rows also carry `next_agent` UNSET. Third tick carrying this.
- `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` — now unblockable on a concrete condition (LAYER2 DONE → I close the parent). Watch that LAYER2 actually moves; if it stalls, the WARN→reject escalation stays permanently off and the 3-occurrence class stays live.
- **Resolved since last tick, standing down:** `FIX-CADENCE-TNB-AUDIT-WEEKLY-MARKER-BLOCKS-DAILY-CRON` made its 20:13Z deadline (now review/qa); `FIX-NOTEBOOK-LINECAP-SWEEP-BYTE-BLIND-BACKSTOP` shipped to review/qa; `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` (P0, deferred 3x) now carries `next_agent=architect`.
- mcp-server memory on record, **no action, user-gated**: +119 MiB over 29 min (~4.1 MiB/min, up from ~2.9), VmHWM 1662516 kB, ~6.7h headroom to the 3 GiB cap. Well under the 85% A-30 gate so the PASS was correct — but note `FIX-AUDITOR-A30-VMHWM-VETO-TAUTOLOGY-FALSE-NEGATIVE` (P1, architect) means the A-30 veto is itself suspect; do not lean on that PASS.
- Standing check, 5th carry: verify my own commits carry my authorship (`git log --author`).
