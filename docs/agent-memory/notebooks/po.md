# PO Notebook

## c · 2026-06-05T02:11Z — EXECUTE FU-RUNTIME-SET-TRUTH-RECONCILE P3a: bless pdf-extractor into host_runtime_set (doc-only, aab19951)

**Tick.** dev-team cron 02:09Z. 0 signals, 0 reports, head idle, WIP 0. No NEW input — drove the ONE decision-ready deferred item to DONE (de-risked last tick by d83189c8 enrichment), refusing to re-defer indefinitely.

**Router raw-verify (not relayed):** `docker ps` → pdf-extractor Up 33h (healthy), rag-service Up 9h (healthy). pdf-extractor unambiguously deployed+working (BCTC depends on it, capability_manifest.pdf=live, CPU-only 8GB-cap per PEK). LATENT MONITORING GAP: not_deployed services are auditor INFO/grey never CRITICAL → a pdf-extractor crash today would stay SILENT despite BCTC breaking. Fixing the classification makes its downtime correctly CRITICAL.

**Edit (hardened atomic, [[feedback_jq_empty_guard_clobbers_ssot]]):** jq -f in same dir → guards (rc, non-empty -s, sentinel `services` array non-empty, top-key count 7→7 unchanged = zero key loss, assert pdf moved + rag untouched) → mv -f. system-map.json: services += pdf-extractor; not_deployed_by_design -= pdf-extractor; not_deployed_short_keys -= pdf; _last_verified→2026-06-05. capability_manifest left as-is (pdf already live). 101 diff lines = jq reflow; only 4 semantic changes (asserts prove it). python json.load + jq empty both VALID.

**rag-service deliberately UNTOUCHED (P3b):** its deploy-vs-16GB-host-memory tradeoff is an OPEN architect call (FU-RAG-DEPLOY-MEMORY, touches kernel-panic host-safety [[project_host_memory_panic]]). Blessing it pre-empts that. FU-RUNTIME-SET-TRUTH-RECONCILE retitled (P3a DONE / P3b rag remains), size S→XS, scope_remaining=rag, progress logged w/ SHA — status STAYS BACKLOG, depends_on FU-RAG-DEPLOY-MEMORY intact.

**Commit aab19951** (main, explicit-path staging, NO -A/--force; "docs/data ignorés" warning SPURIOUS — both files verified landed via `git show --stat`: system-map.json + orch-state.json). NO rebuild (system-map read live by auditor/dashboard, not compiled). Other backlog (CNYVND-DEAD-FIELD/SBV-EFFECTIVE-DATE/MACRO-SNAPSHOT-WORSTOF) left gated. WIP 0→0.

**LESSON:** a "not-deployed-by-design" runtime-set list is also a MONITORING POLICY — a service silently demoted there suppresses its own outage alerts. When a service graduates to load-bearing+stable (router-verified Up + a real dependent), reconciling it INTO services[] isn't cosmetic config hygiene, it re-arms the auditor's CRITICAL path for it. Split a mixed-confidence reconcile: ship the unambiguous half (pdf), leave the architect-gated half (rag) under its owning task rather than flipping the whole flag.

## c · 2026-06-04T20:05Z — DSI QA-gate SIGN-OFF: SIGNED-OFF-PARTIAL (serve fixed live, operator FB symptom HALF-closed) + 3 follow-ups triaged

**Verdict (honest, NOT a clean win).** Read QA report TASK_REPORT_DSI-QA.md full + arch brief + BA spec. Router raw-verified LIVE myself via gateway get_macro_snapshot (not relayed): carry.regime=UNKNOWN, carrySpread=null, is_estimate=true, source_tier=4, fetched_at_source=2026-05-28 (NOT now), dataSource=estimate. NO FII_OUTFLOW_RISK from fixture arithmetic. PART A (operator core complaint) + PART B (provenance labels credit/energy/bond/BCTC + conf??0 + roe??null) all live-PASS.

**Honest scope (what's genuinely on the live path vs off-path/latent):** (1) Live macro serve path = the **Go macro-indicators :5004 service** (ALREADY in host_runtime_set.services — system-map was CORRECT; the stale belief is in the arch brief R-4 + [[project_dashboard_two_data_planes]]). REAL fix = Go commit 09e93d76 (DSI-MACRO-INDICATORS-LATENT, already DONE-LIVE-VERIFIED). The TS DSI-S1-MACRO (macroTools.ts) is correct DEFENSIVE code but OFF the live path. (2) DSI-S2-PRICE Go stock-price = LATENT (container not running, not in runtime set); live prices served by mcp-server yahooFinance.ts which STILL has cnyVndRate=0/no-staleness/no per-symbol is_estimate — UNFIXED on live path. (3) Real durable wins ON live path: SLA staleness guard resurrection (dead 18d→alive, a6b86ed0) + Part-B labels.

**Operator symptom only HALF-closed — refused to mark clean win.** Serve layer suppresses carrySpread→null, BUT the response STILL exposes raw fedFundsRate=5.33+vndDepositRate=5, and consumers recompute. Raw-read fb-post-2026-06-02 L11/L23/L27 + fb-post-2026-06-04 L19/L31 → literal "Mỹ 5,33% vs VN 5,00% chênh lệch −0,33 rủi ro dòng ngoại". chef.md L188/L193 hardcode FII_OUTFLOW_RISK/−33bp as example template (leak risk). This is why the serve fix does NOT close the loop.

**3 follow-ups triaged → backlog (atomic jq -f, hardened [[feedback_jq_empty_guard_clobbers_ssot]]):** DSI-CONSUMER-HONORS-ISESTIMATE (P1, HIGHEST operator value, owner agent-father flow-edits + dev-mcp-server — consume served carry verbatim, omit narrative when is_estimate/null, scrub chef.md template prose, consider null-ing raw rates serve-side as defence) | DSI-S2-PRICE-TS-GAP (P2, dev-mcp-server — yahooFinance.ts provenance) | FU-RUNTIME-SET-TRUTH-RECONCILE (P3 DOC, ops — pdf/rag UP but marked not-deployed; macro already correct). Macro-indicators runtime question RESOLVED-BY-FACT: BLESSED (already in host_runtime_set, no change). Existing FU-MACRO-INDICATORS-MISSCOPE + FU-FRED-EFFR-STALE backlog items kept (cover arch-brief correction + the WHY).

**Disposition:** sprint_goal + active_sprints DSI → SIGNED-OFF-PARTIAL. head idle, next_agent=po. NOT starting a follow-up sprint this tick — queued; next cron triage picks up P1 CONSUMER fix.

**LESSON:** a serve-layer integrity fix is only HALF the loop when the served object still exposes the raw inputs AND consumers recompute downstream — the operator's actual symptom (wrong FB macro) lives in the CONSUMER, not just the producer. Verify the operator's real artifact (the FB post text), not just the tool response. Also: "latent/not-deployed" claims in an architect brief can be stale vs both docker ps AND the system-map runtime set — check BOTH; here the brief was wrong (Go service running + already blessed) and the real fix was the "latent backlog" item, not the on-path TS task. Sign honestly: SIGNED-OFF-PARTIAL beats a green DONE that leaves the operator's complaint open.

## c · 2026-06-04T18:23Z — EXPAND FIX-MACRO-FEDFUNDS-REGRESS → architect-led cross-zone sprint DATA-SERVE-INTEGRITY (router-verified systemic audit)

**Input:** operator-flagged VERY CRITICAL, router-verified audit (3 Explore agents + router raw sed/grep). Systemic anti-pattern across the data-serve layer: code fails-soft to a hardcoded fixture/zero/last-value, STAMPS fetched_at=now, DROPS/never-wires the staleness/estimate flag → fabricated value served as live. Regression of DPI (DPI-2b introduced safe-degrade fixtures; DPI-FU-A ff9a64ce added FRED-live+fail-loud, system reverted to fixture). Single FIX-MACRO-FEDFUNDS-REGRESS was the tip of the iceberg — same pattern in stock-price + sector + financial tools.

**PO raw-confirmed (not relayed)** the two ordering-critical findings before planning: macroIndicatorSla.ts:36,73 `.get("VN")` vs macroIndicatorRefreshJob.ts:242 writes `"vietnam"` → the freshness guard never matches a row, never fires → THE REGRESSION WAS SILENT. This is why AC4 (fix detection net) is sequenced FIRST (DSI-S1-SLA, do_first:true). Did NOT re-discover the rest — operator audit is the evidence; planned + routed only.

**Disposition → SPRINT-L DATA-SERVE-INTEGRITY, lead=architect.** Did NOT just point-fix: recurring-bug policy (≥2 cycles same module: DPI-2b→DPI-FU-A→this) mandates architect root-cause rethink BEFORE point fixes. Created sprint_goal + 7 backlog tasks (atomic jq -f, hardened: jq rc + non-empty + sentinel-key guard per [[feedback_jq_empty_guard_clobbers_ssot]] — first attempt aborted clean on a `def`+`;` compile error, SSOT untouched, exactly the lesson). ABSORBED FIX-MACRO-FEDFUNDS-REGRESS (status ABSORBED-INTO-SPRINT, sprint tag set so it clusters under DSI; NOT a 2nd task — dedup honored).

**Task DAG:** DSI-ARCH (P0, architect, multi — formal fleet-wide invariant brief + per-zone split) → BA-DSI (per-zone specs). Impl: DSI-S1-SLA (P0, do_first — detection net) → DSI-S1-MACRO (P0, fedFunds+SBV live fail-loud, absorbs the regress) → DSI-S1-FE-TYPE (P1, frontend TS types must not drop dataSource/isEstimate/failedSymbols). DSI-S2-PRICE (P1, dev-stock-price — Staleness through FetchPriceResponse, Tier3 no re-stamp). DSI-S3-SECTOR-FIN (P2, credit/energy/bond-seed/bctc??0/conf??1). head.next_agent=architect.

**Zones:** apps/mcp-server→dev-mcp-server (S1*, S3), apps/stock-price→dev-stock-price (S2). CORRECTION carried from prior cycle: macro-indicators Go plane is NOT deployed (separate microservice plane, host-safety) → its code is fixed for correctness-when-deployed but the LIVE serve path that corrupts the product is apps/mcp-server. Architect leads the cross-zone structural rule. Out-of-scope: query-knob defaults, algo params, deterministic lookup tables, honestly-tier-3-labeled values; Math.random() in macro-indicators _deprecated/services.ts:73 → SEPARATE ticket.

**LESSON:** when a recurring point-fix turns out to be one instance of a NAMED anti-pattern spanning multiple zones + a type-boundary flag-drop, the PO move is NOT a bigger FIX — it's an architect-led sprint with the structural invariant formalized FIRST, the point-fix ABSORBED (tagged, not duplicated), and the broken DETECTION NET sequenced before the value fixes (a guard that never fires is why the regression was invisible — fix the net first so the next regression surfaces). Raw-confirm the ordering-critical finding (the SLA key mismatch) yourself even in a pre-verified audit, because it changes the task sequence.

## c · 2026-06-04T17:44Z — triage CONFIRMED macro fedFundsRate REGRESSION → FIX-MACRO-FEDFUNDS-REGRESS (P0, recurring → architect)

**Input:** router-verified raw `get_macro_snapshot` 17:40Z vs ground truth. carry.fedFundsRate=5.33 (real Fed 3.50–3.75 / EFFR ~3.58) → carrySpread −0.33pp → regime FII_OUTFLOW_RISK; real ~3.58 → +1.42pp carry-favorable. SIGN FLIPPED. oil 95.06 −2.11% (real 96.97 −0.86%, wrong dir); gold 4509.6 +0.97% BULLISH (real ~4475 falling, wrong dir, ≈7d-avg 4505=stale tick). dataSource:"live" dishonest (no per-field ts on oil/gold/usdVnd).

**Key finding — RECURRING, not new:** this is the SAME defect DPI-2/DPI-FU-A fixed 2026-05-30 (ff9a64ce: 5.33 fixture→3.62 FRED-live, regime→NEUTRAL). It REGRESSED back to the 5.33 fixture exactly as FU-A's forward-dependency warned (needs macroIndicatorRefreshJob 19:13Z + FRED reachable; 96h checkAndAlertEffrStaleness guard didn't fire/wasn't acted on). Per recurring-bug-escalation (≥2 cycles same module) → set `escalate_architect:true`, next_agent=architect for root-cause rethink BEFORE another point-fix. Do NOT just re-run the fetch.

**Zone CORRECTION (brief was wrong):** request said route dev-macro-indicators — but that agent is zone-locked to `apps/macro-indicators/` (separate not-deployed Go microservice plane). The ENTIRE serving stack (FRED fetch fredApi.ts, fixture fallback macroTools.ts L55-58 '5.33 fallback'+fedFundsRateIsEstimate, carry/regime carryTradeSignal.ts, staleness guard macroIndicatorRefreshJob.ts) lives in `apps/mcp-server/` → correct zone = **dev-mcp-server**. Verified by grep, not guessed.

**Disposition:** 1 FIX task `FIX-MACRO-FEDFUNDS-REGRESS` (P0, size M, zone apps/mcp-server/, sprint DATA-PIPELINE-INTEGRITY) → orch-state .task_board.backlog (atomic jq -f, hardened [[feedback_jq_empty_guard_clobbers_ssot]]). DEDUPED: grep confirmed no pre-existing macro/fedfunds task (count=1 after insert); supersedes processed signal po-20260530T081716Z.json. head.next_agent=architect. Signal po-20260604T174505Z.json emitted. AC encodes: live rate+freshness (no live-masking fixture), regime recompute positive, oil/gold dir match source, per-field ts, dataSource=live only when ALL fresh, then ops REBUILD mcp-server → QA raw-verify.

**LESSON:** a "DONE+PO-signed-off" fix whose freshness depends on an external cron+egress (FRED) is a regression-prone class — the fix shipped a fail-loud ALERT but kept a fixture FALLBACK that masquerades as live (top-level dataSource stayed "live" even though fedFundsRateIsEstimate flipped). Durable fix must kill the masquerade (per-field freshness gate), not re-pull. Router raw-verify of LIVE serving tool (not badges) caught the regime sign-flip that corrupted 4 days of fb-poster theses. Always verify the brief's proposed zone against the agent's zone-lock + where the code actually lives — the obvious-named zone (macro-indicators) was the wrong plane.

## c · 2026-06-04T07:45Z — triage architect HIGH signal → sequence RAPID-DATA-LAYER sprint (10 fixes)

**Signal:** `docs/signals/architect-20260604T060000Z.json` (design_complete, HIGH). Brief SSOT `docs/architecture-briefs/2026-06-04-rapid-analysis-data-layer-gaps.md` (READY-FOR-PM-SEQUENCING). 6 rapid-analysis cowork skills (SKILL-1..6, the pre-TNB gate) ALL BLOCKED: their data inputs aren't on the 156-tool surface. 37 fields assessed (COVERED 3 / PARTIAL 12 / GAP 22), 4 root-cause clusters, 10 remediations.

**Pre-flight:** TNB c86 handoff = all findings pre-existing methodology/pipeline persisting-blockers (F8 COWORK-LEADER-SELFLOCK, F2/F3/F4/F5 structural data-gaps) — none new-HIGH for THIS sprint; ACK'd, no new tasks. Channels not re-audited (clear signal-driven kickoff). FU-RAPID-SKILLS-RESIDUAL in backlog is only size-justification tidy (out-of-dev-scope) — NOT these data-layer gaps; no dedup collision. RECON-AGM-1/FIX-* confirmed ABSENT (grep count 0) before insert.

**Disposition → SPRINT-M RAPID-DATA-LAYER** (signal-routing table: brief targeting apps/ code → SPRINT). Architect already pre-atomized tasks w/ file:line + RC mapping → I sequenced, did not re-decompose. 10 tasks into `.task_board.backlog` + `.sprint_goal.entries` (atomic jq -f, hardened: non-empty + sentinel-key guard per [[feedback_jq_empty_guard_clobbers_ssot]]). WIP_max=2.

**Dependency edges (architect order):** RECON-AGM-1(P0,ops-vps-fetch,IN-PROGRESS router-launched NOW, blocks FIX-G) | FIX-B(P1,gate-first,no-dep — SKILL-1 mandatory size gate, NO skill lives without it) → then FIX-A/FIX-D/FIX-H(P1,no-dep,parallel) | FIX-C(P2,dep FIX-B,BLOCKED)/FIX-E(P2,no-dep) | FIX-F(P3,VAS-audit prereq,bank-form guard) | FIX-G(P4,dep RECON-AGM-1,BLOCKED) | FIX-I(P5,dep FIX-A,BLOCKED). Zones: all apps/mcp-server EXCEPT RECON-AGM-1=ops-vps-fetch, FIX-G=dev-vps-crawls+mcp-server.

**In flight (WIP=1, room for 1 more P1):** head.active_task_id=FIX-B, next_agent=pm. Emitted po→pm `sprint_sequenced` signal. FIX-G HARD-GATED on RECON-AGM-1 (status BLOCKED, deps:[RECON-AGM-1] — no dev work until recon DONE w/ positive fetch recipe; Risk-4 PDF-vs-HTML format decides effort).

**LESSON:** when architect ships a READY-FOR-PM brief with per-zone atomic tasks already specified (file:line, classification, RC), PO sequences-by-dependency + sets WIP/blocks — does NOT re-decompose (that's done). The leverage rule = ship the mandatory entry gate (FIX-B) FIRST since every downstream skill inherits it; a parallel no-dep P1 fills the 2nd WIP slot. Gate a new-data-source fix (FIX-G) behind its recon spike so dev never starts on an unfetchable source. Verify a "residual" backlog entry isn't the same work before creating a dup.

## c · 2026-06-04T12:13Z — dev-team triage tick: route LanceDB to ops; split FIX-G into G-1→G-2

**State:** head.status=idle, wip=0. ALL P1 RAPID-DATA-LAYER DONE-LIVE-VERIFIED (A/B/C/D/E/F/H; FIX-B + FU-FIXB-COLUMNKEY-VERIFY settled ~4min ago, get_market_cap(FPT)=130318.29bn confirmed real). 159→toolCount confirmed. Channels CLEAN (read_telegram_reports new=0, list_unresolved_reports=[]). 4 cowork heartbeats drained non-actionable. No new bug.

**LanceDB infra signal → ops (PLAN-ONLY).** signal_queue NEW row ops-lancedb-offline-20260604T1204Z (LanceDB offline ~25h/12 news-scout cycles, search_similar_context errors; NON-FATAL — gatherers degrade, signals still emit). INFRA/RAG, NOT a dev FIX. No existing lancedb/rag task to fold into → emitted repair_task_request_ops_lancedb_offline_20260604T121309Z.json (check_id OPS-LANCEDB-OFFLINE, ops lane). Tied to [[project_disk_full_lancedb_bloat]] (could be store bloat recurrence). Told ops VERIFY RAW first (docker ps -a / df / open store) per [[feedback_auditor_false_positive_destructive]] + [[project_dashboard_two_data_planes]] — no mass-start (16GB host panic). Marked row READ+triaged. 0 NEW remaining.

**FIX-G → SPLIT into FIX-G-1 → FIX-G-2 (NOT one L feature).** Crosses VPS↔mcp-server boundary; fetcher must land + be confirmed fetching live before tool can serve real (not mock) data — same class as FPT-mock-data lesson. So 2 sequenced atomic M tasks each in ONE zone (cleaner pm→dev, no "multi" for architect to re-split): G-1 [BACKLOG, vps-scripts/] VPS fetcher (HTTP-only, no Chromium — recon confirms plain JSON API + CSRF warm-up, no CF) → G-2 [BLOCKED, depends_on G-1, apps/mcp-server/] agm_revenue_plans table + get_agm_plans tool. WIP≤2 → only G-1 starts now. Replaced the null-id stub in backlog; verified G-1/G-2 landed, old stub gone.

**Recon VERIFIED concrete before greenlight:** docs/vps-sources/vietstock-agm-plan/recon.md verdict FETCHABLE, live multi-ticker data (FPT/VIC/ACB 5yr), working Python CookieJar recipe, PTID drift noted (rev 5/7, LNTT 8, LNST 9; banks no rev) → filter by PTName. PDF fallback NOT needed (structured API gives approved figures). Precedent paths raw-verified: vps-scripts/ has vn-*.service+fetch-*-loop.sh pattern; mcp-server migrations dir + interface/mcp/tools/market-data/ + registry.ts + RAPID-B* test precedent all exist.

**LESSON:** an infra_anomaly in signal_queue is an OPS repair_task_request, never a dev FIX — and the PO must inject the raw-verify-first + no-mass-start guard into the ops handoff (detector/auditor false-positive history). A new external-source feature that crosses VPS↔mcp-server should be SPLIT by zone with a hard depends_on edge so the tool task can't start (and ship mock data) before the fetcher is confirmed live — keeps each task single-zone and lets WIP gate the boundary.
