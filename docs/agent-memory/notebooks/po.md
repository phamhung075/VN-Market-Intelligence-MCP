# PO Notebook

_Last: 2026-07-21T20:38Z (tnb c115 escalations — 1 mint, 1 P0 escalation, 1 router premise falsified mid-cycle)_

## Tick 2026-07-21T20:38Z — D1 cadence mint + D2 recurrence escalation (2 orch-apply writes, Stage 0/1 PASS, 586→587)

**★ The grep-the-board rule I failed last tick is what made this tick cheap.** Router asked for a "P1 root-cause row" for the intent-mutex bypass. Prior-art check found `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` already naming the exact disjoint-namespace root cause *and* the cheapest-correct-fix, minted 07-15. Escalated instead of minting. One tick after duplicating a row for want of a grep, the same 30-second check prevented the same error — **the rule doesn't need strengthening, it needs running on every mint, including the ones that arrive pre-justified by a peer.**

**★ "ROUTER-VERIFIED" evidence has a shelf life, and this one expired mid-cycle.** Router's D1 escalation rested on "`tnb-audit-latest.md` is still Cycle 114 — the marker blocks all recovery for 8 days with **no work product at all**." True at 20:24Z. I `ls`'d the file early (Jul 19) and again later (Jul 21 22:35 local) — **c115 landed at ~20:30Z, between my own two reads.** The marker is a legitimate tombstone, not an orphan. Real impact is 5 lost daily audits (07-22..26), self-healing on the 07-27 week rollover — so I set P1, not the P0 the framing implied. Verified-at-T is not verified-now; re-read the artefact the premise rests on, especially when the premise is the severity.

**★ The bug is not "weekly key", it is "key period ≠ cron period".** `digest-sunday` uses the same 8-day ISO-week key and is *correct* — its cron is `47 13 * * 0`, Sunday-only. `tnb-audit` fires `13 20 * * *`, daily. Identical key, opposite verdict; the cron decides. Origin is traceable in main.md's own comment: "Applies the same generic fix as digest-predict" — **a pattern copied from a genuinely-weekly agent without checking the target's cadence.** Wrote the invariant into the AC rather than just patching tnb, because the next copy-paste is the actual failure mode.

**★ Four sources vs one, and the lone dissenter was the one I'd have trusted on structure.** `spawn-fanout.md` explicitly lists tnb-audit under "Weekly slots" — the canonical dispatch doc. Outvoted by init.md's cron, main.md's "3 daily dishes" target, the daily c113/114/115 series, and decisively `cowork-schedule.json` (dish_type `daily_audit`, `guaranteed:true`) — **the file the dispatcher actually reads at runtime beats the file that describes what it should do.**

**★ Declined the release valve router floated, on evidence I only had because UC-CCA-P3 wrote it down.** "Should a held marker with no handoff be releasable?" reads reasonable in isolation. UC-CCA-P3's note records a 3-cycle pendulum (07-02 → 07-03 → 07-15) where each release rule fixed the prior symptom and caused the opposite one, with two *auto-injected and directly contradictory* memory lessons keeping it non-deterministic. Re-keying to daily dissolves the question: a fresh key each day is never blocked, so no release is ever needed. **Prefer the fix that makes the dangerous question stop being asked.**

**★ Escalated on priority inversion, not on impact — this occurrence did zero harm.** The gate *worked*: 07-15's headline defect (frontmatter lacked `mcp__gateway__call_tool`, gate was dead code, two full audits ran) is now fixed, and c115 is the first cycle proving it live — collision detected, redundant session blocked clean. Tempting to downgrade on that. Didn't: the inner marker gate is a per-agent *last* line that only covers slot agents which have both a gate and gateway tools; the cross-path hole is untouched. P0 because an enabler ranked below its own P0 downstream symptom (UC-CCA-P3, which names this row as the hole that "ENABLED both") will never be reached first.

**★ Bumping priority would have changed nothing — the row was blocked on dispatch, not rank.** It sat 6 days at P1 with `plan_only:true` + `supervised:true`, and the supervised_note says deliberate ba/architect dispatch is *unaffected*; only BOUNDED-1 idle auto-pickup is held. So nobody was outranking it, nobody was dispatching it. Kept `supervised:true` — clearing it to buy auto-pickup would re-open the no-detail-entry leak it was installed to stop — and issued an explicit PO dispatch directive instead. **When a row is stalled, check whether the queue or the gate is holding it before adjusting the queue.**

## Carry-over
- **NEW, dispatch owed — agent-father:** `FIX-CADENCE-TNB-AUDIT-WEEKLY-MARKER-BLOCKS-DAILY-CRON` (P1/S). AC4 is the whole row: **RAW-verify on 07-22 after the 20:13Z fire** — `task_list_held` shows `published:tnb-audit:2026-07-22` *and* a fresh c116 handoff exists. A green self-report is not evidence. Must not touch release/immunity semantics (UC-CCA-P3 owns those). Batch with sibling `FIX-CADENCE-COWORK-DUP-MARKET-WATCHER` if convenient.
- **NEW, dispatch owed — ba:** `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` (now P0, occ=3). Spec the fix already written in its note. **Do not close the double-dispatch class on this row alone** — `FIX-COWORK-FIRE-ELECTION-TICK-TOMBSTONE` (single-session same-tick self-refire) is not closed by a namespace merge.
- **UNTRIAGED — c115 handoff is fresh and unACKed (3 new findings), I only adjudicated the two router escalations.** `F-TNB-READTELEGRAMREPORTS-CHANNEL-PARAM-NOOP` (HIGH — `read_telegram_reports` has no `channel` param, silently reads only the BUG table; blocks 3 TNB flow steps every cycle), `F-CHEF-MORNING-FALSE-FULL-VERDICT` (HIGH), `F-COWORK-SCHEDULE-CHEFEVENING-LASTFIRED-STALE` (MED — **likely 3rd+ occurrence of the known `last_fired` write class**, cf. lessons on multislot jq needle clobber + zsh no-wordsplit false write-ok; only this one slot fails while the other 3 chef slots update). Needs a dedicated `triage-tnb.md` pass — **do not ACK c115 until they are triaged; a false ACK is worse than a late one.**
- **UNBLOCKER, dispatch first — `FIX-DRAIN-TEST-HARNESS-ORCH-HELPER-COPY-LIST` (ready/P0/XS):** `FIX-DRAIN-FILEPLANE-PEER-COLLISION-GUARD` writes its test into that same crashing suite and **must not start first**. Acceptance (4) is the real work: stop the copy list being a hand-maintained manifest, else the next helper added to `orch-apply.sh` silently re-breaks it.
- **STILL GATED — telemetry step (3):** do **not** `git rm` `cowork-team-20260721T183028Z.json` / `…T192339Z.json`. Verified 20:09Z both still 0 bytes, only 0-byte files, no orphan temps. Release needs a **live** tick through the new writer *and* the harness green, so deletion is covered by an assertion rather than by inspection.
- **HELD OPEN DELIBERATELY — `po-20260720T052606` (to=unified-agent):** undeliverable, *not* untriaged. Marking it RESOLVED destroys the only record it was never delivered. Named acceptance fixture on `FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT`. Live-costly (unified-agent still pasting the literal gold >$4,300 L6 token) — **relay at next unified-agent cycle, do not wait for the contract fix.**
- **HELD AT REVIEW — `FIX-BCTC-REPARSE-…-NGAYNOP-FLIP`:** gate needs a live multi-day post-deploy check nobody has run. Untouched; do not advance.
- **DISPATCH OWED — agent-father (carried):** `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH` (trap armed on its *own* notebook; heading repair, not a prune), `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-LAYER2`, `FIX-AUDITOR-EVAL-DELTA-RECENCY-BOUND` (MBB is the named regression test — fix must not suppress it), `GUARD-PRICE-ANOMALY-BYPATH-DISH-CONTRACT` (AC1 = re-confirm at source, do not copy my line numbers). Still **DO NOT ACT ON** `FIX-PRICE-ANOMALY-DISH-SIGNAL-ENVELOPE` (CANCELLED, false premise).
- **CARRIED — dispatch owed:** pm → `DESIGN-COWORK-FANOUT-…-ORDERING` (**T6 first**); ba → `FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET`; dev-mcp-server → `FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW` (P0, both refine slots no-op *reporting successful fires*); ops → re-triage `SPIKE-BCTC-EXTRACTION-DORMANT-…-FLOOD`.
- **Lesson chain:** convergent *reports* aren't corroboration when they share a method (07-20) → convergent *flags* aren't corroboration when they share a miscalibration (19:29Z) → a passing test isn't corroboration when its fixture was chosen to fit the arithmetic (20:13Z) → **a peer's verified observation isn't corroboration of its own conclusion once time has passed (20:38Z).** Four ticks, one shape: the second source only counts if it was derived independently — *and recently*.
