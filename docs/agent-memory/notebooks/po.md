# PO Notebook

_Last: 2026-07-29T07:21Z (router-dispatched triage: A-21 crash-window predecessor bound) · 1 row minted, 2 prior rulings amended — one of them my own._

## This cycle

- **I was wrong at 06:25Z, and the correction is the cycle.** I retracted the A-21 signal partly because "`crashRestarts=1` is below threshold, so A-21 stays **armed** — a second crash fires on its own." It does not. The same query returned **0 at 07:08Z on unchanged ground truth**: the `2026-07-28 22:58:42` predecessor aged out of the fixed 8h fetch window at **06:58:42Z** while the 05:43:21 crash was still **2h20m inside its own 4h alert window**. The count does not hold and wait — it **decays**. So the preserved observation was, in the detector plane, the only surviving record of that crash. Amended the disposition rather than leaving the false reassurance to govern.
- **Reframed the defect so the obvious fix gets rejected.** Reported as "loop starts at `i=1`, first row never examined". Structurally it is: a **fixed 2× lookback cannot bound an unbounded inter-startup gap**. For in-window startup `T`, predecessor `P` is fetched only if `P >= now-8h`; if the gap exceeds 8h, `P` is **always** dropped. So A-21 can only classify crashes following **<8h uptime** — it works inside an established crash loop and is blind to the first crash after a stable period. The `i=1` framing invites "widen 8h to 24h", which just moves the boundary. AC(1) **rejects** widening the constant; the fix is an unbounded predecessor lookup with candidates still window-bounded.
- **The guarantee is documented *and* false — that is the strongest evidence, not the loop bound.** `restartCadenceAlertJob.ts:171-173` says the 2× lookback exists "to capture the startup that preceded the oldest in-window startup (needed to classify the first row)", and `:207` skips `i===0` as "conservative: not a crash". The author knew a predecessor was required and reached for a constant to guarantee it. The skip is not conservative — it is a **silent false negative on the highest-signal event**.
- **Same defect in three artifacts, because the query is hand-copied.** Flow doc `:112-131`, the production Telegram job it claims to port "1:1", and the design brief `:178-181` that both descend from. So this is a **spec** defect → architect, `zone=multi`. AC(6) asks whether the flow doc should keep hand-porting this predicate at all.
- **Two standing artifacts said "do not open a row against A-21". Both were right about the *threshold* and silent on the *count*.** `ALERT_THRESHOLD=2` is deliberate and stays. But `FIX-AUDITOR-CALLER-PROSE-...`'s AC(5) — "DO NOT change ... the A-21 predicate" — reads as a veto on today's fix, so I **scoped it in place** to the verdict mapping at `:133-142`. A mint that leaves a live veto standing is not dispatchable work; two P1 rows with contradictory instructions on one file strand both.
- **"The detector was CORRECT" was inferred from a single instant.** The 06:09Z reading of 1 was accurate only because the predecessor had not yet aged out. Same query, same truth, different answer 59 minutes later. Consistency at one instant is not correctness — this is the second time this week I have caught that shape.
- **The documented threshold is unreachable as documented.** `>=2 in 4h` actually requires **three** startups inside 8h, since the earliest fetched row can only ever be a predecessor, never a candidate. The spec and the artifact have never agreed.
- **Found the blocker the fix would have hit in production.** `lastCleanShutdown` is frozen at `2026-07-22 17:48:42` (I read it while verifying, it was not in the report). Absence-of-sentinel is this detector's *only* crash evidence — so an unbounded predecessor makes **every** restart classify as a crash, converting the false negative into a new false-positive class on the WORK-channel alert path. Hence `plan_only`; the architect must state an ordering position vs `OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN`.
- **Did not create a 13th stranded row.** `next_agent=architect`, `owner=architect`, `plan_only`+`supervised` — on the board *and* pickable. Stranded `FIX-AUDITOR-*` count unchanged at 11.

## Carry-over

- **Verify the AC(5) scope amendment is honoured, not just present.** If the architect picks up `FIX-AUDITOR-CALLER-PROSE-...` and still reads "do not change the A-21 predicate" as blocking, that is a **routing defect in how amendments are surfaced**, not a fresh row. Same failure shape as the `next_agent`-unset-for-26-days finding: the correction existed and was never reached.
- **The ordering question in `cross_row_interaction` is the real deliverable.** If the architect returns a window fix without a position on the sentinel-writer dependency, reject it — that is the half that pages during market hours.
- **A-30 row is now 4d old, `recurring_bug_count=6`, missed twice** (unchanged, 3rd consecutive carry). Threshold holds: still BACKLOG with no architect activity next tick → **escalate to user**; do not re-attach evidence a fourth time.
- **`supervised = never dispatched` watch, 2nd tick.** Did `agent-father` get dispatched for the notebook-skill row? Today's mint is also `supervised:true`. If both sit untouched, that is the routing defect escalating — not another row.
- **New standing check, earned today:** when I retract or dispose of something on the grounds that "the detector stays armed / this costs nothing", **verify the detector is actually armed** by re-running it. A safety argument about a mechanism is a claim about that mechanism, and I did not test it.
- Standing, 2nd tick: when a report hands me arithmetic, re-derive it from the artifact before minting.
- Standing, 10th carry: verify my own commits carry my authorship (`git log --author`).
