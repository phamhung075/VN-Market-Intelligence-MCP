# PO — Notebook

## 2026-08-11T14:38Z · The product has been dark for 3 days and the watchdog was inside the outage

### What actually happened
- dev-team Step 1 triage, tick `2026-08-11T14:31Z` (orphan-adoption resume of `task:po-triage-20260811`, no checkpoint → fresh cycle). **2 mints, 4 escalations/folds, 1 sweep stamp, 2 self-corrections.** 5 `orch-apply.sh` pipes, all PASS, conservation OK (`task_total` 768→770).
- Pre-checks: dashboard inbox empty (0 `to=po` NEW). WF-2 `should_hold=false`. Sweep: 85 candidates (46 DRS, 38 backlog-XOR, 1 ready-XOR, 4 re-admitted). Journal: `docs/agent-memory/decisions/triage-20260811T1431Z-po.md`.

### Decisions worth keeping
- **★ ALL 8 `guaranteed:true` COWORK SLOTS HAVE BEEN DEAD ~66h — AND THE DISPATCHER IS FINE.** Newest fire anywhere in the class is `2026-08-08T20:23:36Z`. chef-morning/eod/evening (MARKET dishes), fb-daily/weekend, digest-daily/sunday, tnb-audit — the entire user-facing output plane. `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` raised **high → P0**, BATCH #1.
- **★ THE THIRD PLANE IS THE FINDING; THE FIRST TWO WOULD HAVE MADE ME FILE THE WRONG BUG.** `.last_fired` (8/8 stale) + the agents' own notebooks agree, but both are *equally consistent with "the session is down"*. What decides it: **non-guaranteed slots on the SAME dispatcher ran today** — market-watcher 12:07Z, alert-commander `c97` 12:08:47Z, news-scout 08-11. Failure is **class-selective**. Without that read I'd have filed a session-outage report and been wrong.
- **★ "COINCIDENT WITH THE API OUTAGE" IS WHAT KEPT THIS INVISIBLE FOR 3 DAYS.** Two PO ticks today saw the stale TNB handoff and stopped: 12:37Z wrote "coincident with the 08-09→08-11 weekly-API-limit outage", 13:22Z logged a no-op. Both locally reasonable. **An ambient explanation makes a stale artifact look expected and nobody reads the plane that would falsify it.** One `jq` over `cowork-schedule.json` falsified it.
- **★ IT IS FAILURE TO RESUME, NOT MISSED FIRES — WHICH CHANGES THE FIX.** Today is Tuesday; chef-morning 05:15Z, chef-eod 08:45Z, fb-daily 09:15Z all passed with no fire *while the dispatcher was demonstrably running*. An outage that **ended** cannot explain a class that stayed dead after it ended. So catch-up/look-back tuning (`BA-COWORK-GUARANTEED-SLOT-CATCHUP`) cannot fix it — **no window fixes a path that never fires again.** Named the self-latching shape: an eligibility predicate keyed off `.last_fired` requiring a recent previous fire would latch exactly like this, and `.last_fired` is the stale field on all 8.
- **★ THE WATCHDOG WAS IN THE DEAD CLASS.** `tnb-audit` is the audit that exists to catch this, and it is slot #8. Zero alerts in 66h across the 50 newest BUG reports. `guaranteed:true` is a silent false promise — measured, not predicted.
- **★ IDENTICAL REFUSALS 24h APART = ZERO CONVERGENCE.** BCTC period-mismatch sets for 08-09 and 08-10 are **byte-identical** — FRT `2024-Q1`vs`2024-Q4` at `46 vs 10` both days, KDC `51 vs 6` both days, VND `115 vs 26` both days, same 14:02–14:20Z window. Not a backfill pass: a daily zero-yield retry loop that also manufactures ~19 CRITICAL-looking BUG messages/day. `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` is `P0`/`READY`/`dev-mcp-server`, no flags, no deps, **10 days untouched** — dispatch starvation, not spec gap. BATCH #2.
- **★ I RETRACTED MY OWN TIE-BREAK BEFORE IT JUSTIFIED A DISPATCH.** Picked `OPS-FFLOW-VPS-CLOCKDRIFT-PREVENTIVE-RESIDUALS` over the `idx`-head on "live `foreign_flow stale 3900min` corroboration". Then: the 12:37Z peer tick had **already** cross-linked that alert elsewhere (a boundary artifact can't manufacture 65h), and live `get_system_status` shows HOSE 44min / SBV 18min — **both fetchers healthy now**, the exact condition residual (3) was written against. Kept the stamp (Step 2 = one row per invocation, additive, no lane move) and **corrected the rationale on the row**: real basis is 19 days stranded off the DRS allowlist + residual (2) is a live latent-outage bug (`deploy-vinahost.sh` ships unsubstituted `__MCP_BASE__`/`__API_KEY__`).
- **★ MY OWN 5 PIPES VALIDATED PASS OVER DUPLICATE BOARD IDS.** `orch-apply.sh` advertises "dup-key" validation but doesn't check task ids in lane arrays. `FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT` sits **twice inside `ready[]`** and the copies **disagree** (`P2`/no next_agent vs `P1`/`dev-mcp-server`). Every consumer uses `map(select(.id==$tid))|first`, so **array position decides which row a gate sees** — a `po_goahead`/`supervised` stamp on one copy is invisible to a reader of the other. Minted P1, guard **before** de-dup (fixing data before the writer just re-acquires it).
- **★ A `[STALE]` LABEL DOES NOT DISCLOSE A UNIT ERROR.** Indicator plane `vnindex = 170468` (= `1704.68 × 100`) vs served `vnIndex = 1773.41` tier-1, 43s apart, same container. Same payload: `interest_rate_pct = 1` against the serving plane's own `vndDepositRate = 5`. Minted P2, explicitly **not** a dup of the CANCELLED delta-gate row — that gate exists and fired twice today; this is the stored value's *unit*.
- **★ ROOT CAUSE BEAT PRIORITY DEBATE.** `[te-chromium-news] Browser was not found at /usr/bin/chromium-browser` + SOURCE HEALTH `Chưa bao giờ` (last success = **never**). Prior Dockerfile fix failed → recurring bar met, and the fix is now one line. `P2 → P1`. Bar written on the row: **do not close on a green build** — a wrong path fails at scrape time, never at build time.
- **Checked a peer had already run my own discriminator.** Attached the weekend-FP correlation for `ops-diagnostics` 23/90 to `FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL` (count 23, label MISSED, weekend window all match), prescribed a weekday re-read — then found `po.md:44` already ran it ("self-recovered to 8 stale/1 missed/72 on-time"). Upgraded to **SUPPORTED**, report 4561 closable. Residual is the 9-cron gap (81≠90), already routed to `FIX-AUDITOR-A29`.
- **No mint** (recorded so nobody re-derives): enricher zero-URL, A-30 rag memory, cold-evict ×3 (self-healed), sbv-omo (fresh now), news SLA. BCTC OCR-corruption alerts (HPG assets<equity, VJC margin 45970432%) are the **write gate working** — refused, prior rows preserved.

### NEXT
- **#1 `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` (P0).** First question is cheap and decides everything: is the firer not being *invoked*, or invoked and no-op-ing? No log lines after 2026-08-08T20:23Z → launchd/plist problem. Lines but no fires → supersede/eligibility wiring.
- **Coverage hole, unpatched by design:** that P0 is `supervised:true` + `next_agent:pm`. `pm` is **on** the DRS allowlist so `is_drs_stranded_off_allowlist` excludes it; `is_backlog_xor_gap` needs a dev-role next_agent so that excludes it too. **Invisible to both sweep classes AND needs a supervised gate.** Did not pre-stamp `po_goahead` (forbidden on a non-`.head` row). Worth its own row if it recurs.
- **`Dự đoán (Poly)` 42 days stale, no owner row, deliberately NOT minted** — could not establish in-budget whether Polymarket is retired or silently dead. Settle that before minting; do not mint on the unverified premise.
- Standing from 13:30Z, still true and now stronger: **before promoting any row, check whether its deliverable already shipped** — and before attaching a "someone should check X" instruction, check whether a peer tick already checked X. Cost me two corrections this tick; both caught pre-dispatch.
- Still not cleared (4 ticks old): 4 `TASK-COWORK-SIGNAL-*` rows in `review[]`, `supervised=true`, zero `po_goahead`.

## 2026-08-11T13:30Z · Zero mints. Five dispatches. Re-folding onto rows nobody picks up is a detection loop.
- **★ ZERO MINTS IS NOT ZERO WORK.** All 4 folds landed on rows minted days ago and never dispatched; one row's own `status_note` says *"PROMOTED INTO PO BATCH 2026-08-08T20:54Z"* and it is still `backlog[]`. Returning `NOTHING` would have been the 4th consecutive tick converting evidence into notes instead of dispatches.
- **★ ONE SIGNAL, TWO JOBS, TWO READ PROCEDURES.** The `FAILEDFILE:` grep returns empty for `size-lint` **by construction**. If a `ci_red` names N jobs, you owe N reads, not one grep.
- **★ I WAS WRONG AND CAUGHT IT BEFORE DISPATCH.** Claimed `triage-signals.md` was false about the hook already blocking; **neither tick had read the hook.** `pre-commit:850-878` — `escalated=true` overrides mode and `exit 1`s, landed 8 days *before* the P0 was minted. The doc was right; I was the defect. Also: the runaway counter counts **blocks, not sweeps** (log at :866, `exit 1` at :877), so "28 vs threshold 3" is equally consistent with 28 attempts / 28 blocks / 0 sweeps.
- **★ A SIGNAL'S `mode=` FIELD IS CONFIGURATION, NOT OUTCOME.** The outcome lives in the code path and nowhere else.
- Journal: `docs/agent-memory/decisions/triage-20260811T1322Z-po.md`. Earlier ticks today: `…T1237Z-po.md` (UC-RDL-P4 ratified at source, `po_goahead_20260811T130245`), `…T1300Z-po.md`.
