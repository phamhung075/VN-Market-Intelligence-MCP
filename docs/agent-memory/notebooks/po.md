# PO — Notebook

## 2026-08-22T20:25Z · Dashboard-sweep triage — real evidence beats title-string dedup, twice

- **Trigger:** router-direct dispatch, `/dashboard/orchestration` full sweep, 6 candidate problems. **Output: 1 mint, 1 widen, 5 already-closed confirmed, 0 duplicates.**
- **★ TWO BUGS SHARE A DASHBOARD TABLE AND ARE NOT THE SAME BUG.** Router's "server-cron overdue cluster" item split into three things on inspection: an already-owned classifier-blindness row (`FIX-CRON-STATUS-LAYERA-SCHEDULE-BLIND-FALSE-CRITICAL`, untouched, correctly diagnosed), a NEW job-name-resolution gap (minted `FIX-CRONSTATUS-STATICMAP-MISSING-9-PAIRS` — 9 CRONS keys whose DB `job_name` doesn't survive tier-2 normalize, live-confirmed via `docker exec ... bun:sqlite` against `cron_job_runs`), and a real multi-day scheduler outage (widened the peer-minted `FIX-CRON-NONRECOVERY-POST-HOST-SUSPENSION-...` row rather than re-mint). Would have folded all three into one if I'd trusted the dashboard's single grouping.
- **★ AN UNCONDITIONAL 24/7 JOB IS THE CLEAN DISCRIMINANT.** `schedulerWatchdogJob` fires every 10min with no market-hours restriction, so the classifier-blindness bug cannot explain it. Its own timestamp series shows a genuine escalating-then-total blackout: rock-solid → 6h50m gap → 33h gap → 2 isolated single ticks → 16h gap → 5-run burst → **99.7h/4d3.7h dead stop spanning 3 ordinary weekdays** → self-recovery at `2026-08-22 12:40:04` with **no process/container restart** in between. That timestamp-shape evidence, not the dashboard's STALE label, is what earned the widen.
- **★ "CHECK STATUS FIRST" ON 5 NAMED HIGH-PRIORITY ITEMS — ALL FIVE WERE ALREADY CLOSED.** Verified at source (archive files, sprint_goal.entries absence, DONE_VERIFIED status), not by trusting the router's framing that they might still be open: `DRAIN-INJECTION-SAFE`, `AUD-ND-1`, `FLEET-HOST-SAFETY`, `FIX-DAILY-FF-VIEW-JOIN-ANCHOR`, BCTC `PUBLISH-INTEGRITY GAP` (BAL-0). Zero re-work.
- **Deliberately not done:** did not re-run channel-audit/manual-dispatch-sweep/TNB-audit — a peer PO tick (`triage-20260822T1927Z-po.md`, committed `69b3b30d2`/`cb83b20bf`) drained the durable inbox 64→0 fifty minutes before this session started; re-running would race already-fresh state. Also did not touch the session-cron "never fired" table item — read `layerBCronRegistry.ts` at source, confirmed `SESSION_SCOPED`/`last_fire:null` is a deliberate, documented (R2) architecture decision, not a defect.

### Carry-over
- **`FIX-CRONSTATUS-STATICMAP-MISSING-9-PAIRS`** → backlog, **dev-mcp-server**, medium, `apps/mcp-server/`. Adding the 9 pairs WILL break `cronStatusCompute.test.ts`'s "exactly 26 entries = CANONICAL_WATCHDOG_JOB_NAMES" invariant by design — task's own AC-2 requires rescoping that assertion (superset, not exact-equals), not deleting it.
- **`FIX-CRON-NONRECOVERY-POST-HOST-SUSPENSION-TIER3-...`** → backlog, **dev-team**, P1, widened this tick (field `po_blast_radius_widen_20260822T2020Z`, peer's original note untouched). Real scope is ~40 Layer-A jobs, not the 3 originally named; AC-1's arming-plane diagnosis should now also explain why `schedulerWatchdogJob` self-recovered with zero restart while the market-hours-window group has not (as of this tick).
- **`FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE`** — a peer PO tick ratified this 4min before my own started; not re-touched, out of this tick's scope.
