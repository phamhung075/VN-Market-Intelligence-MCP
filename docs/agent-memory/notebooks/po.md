# PO Notebook

_Last: 2026-08-06T18:57Z (triage: 4 signals → 4 folds, **0 mints**; 44 manual-dispatch candidates; 0 supervised holds; head idle). Prior PO cycle 18:29Z preserved at `HEAD~`._

## 2026-08-06T18:57Z · Three bloat signals, zero new rows — the recurrence was a dispatch defect wearing a pruning defect's clothes

- **The tempting read was "prune the file again." The measured read was "this row has no lane."** `CLEAN-SKILL-BLOAT-CRON-STANDALONE-DB-INTEGRITY-SPLIT-CHILDREN-BYTE-CAP` is on occurrence 3. It never shipped because `next_agent: claude-manager-helper` is off every allowlist → DRS-stranded, reachable only by PO manual dispatch, where it ranked **15th of 44**. claude-manager-helper ran at 18:06Z and didn't touch it — empirical, not inferred. Re-pointed to `developer`/`cross-service/`, the lane that *demonstrably* edits those files per `git log` on the files themselves.
- **Verified the cure in both directions rather than asserting it.** `is_bounded1_eligible` → `true`, `is_drs_stranded_off_allowlist` matches → `0`. A repoint that only satisfies the first predicate would have left the row in the manual queue too.
- **Found the growth mechanism instead of re-measuring the growth.** Four post-split commits added +146/+32/+26/+12 lines to the pair, every one a developer-lane cron fix — the ordinary cost of maintaining the cron, not misbehaviour. And the split commit's *own message* records offhours as "same prompt as Job 1": the children are a duplicated pair, so **every fix lands twice**. That is why a line-only re-prune re-breached, and why the revised spec is "extract the one shared prompt body", not "prune harder".
- **A byte-cap breach with zero line overage is a fingerprint, not a nuisance.** `sprint-COWORK-...-agent-father.md` at 532L/36224B — under the 600L rollover trigger, over the 36000B cap. That is verbatim the natural experiment `FIX-DECISION-JOURNAL-SKILL-CAPCHECK-LINE-ONLY-NO-BYTE-ROLLOVER` already documents, in the **same sprint** it cites, with a new file joining the trio. Evidence, not a defect → folded. That row has sat in `review[]` since 07-30 while the defect keeps firing: **written, unverified — not unwritten.**
- **84% concentration turned a vague backlog complaint into one answerable question.** Grouped the 44 stranded rows by `effective_next_agent`: **37 are `agent-father`**. The sweep stamps exactly one row per tick by design, so against a still-growing 44-row queue, drain ≤ mint and the queue is non-decreasing *regardless of priority*. Live proof: the set's only P0 re-admitted at 14464s, having never dispatched — a capacity defect, not a prioritisation one.
- **Prior art beat the mint, again.** `FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-NO-DISPATCH-DESTINATION` already owned the class — but had `desc: ""` since 07-30. An owned-but-stub row is indistinguishable from an unowned one at dispatch time; filled it rather than duplicating it.
- **Dry-ran the competitor before claiming no race.** Before folding the BCTC gate row I executed `devteam-backlog-promote-bounded1.jq` against the live board: its next pick is `FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR` (earlier index, equal rank), then WIP=1 no-ops it. "Probably won't collide" was available; running it cost one command.

### Evidence (raw, re-runnable)
- `wc` now: offhours 255L/21159B, weekday 257L/21361B — vs 09:29Z title numbers 15967B/16158B = +32.5%/+32.2% in 9.4h.
- `is_drs_stranded_off_allowlist` grouped by next_agent → `agent-father 37, ops 3, ops-mainserver-fetch 2, code-janitor 1, ops-vps-fetch 1` (44).
- P0 `FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT`: prior stamp `14:53:40Z`, age **14464s** vs window 14400s — re-admitted by 64s. Re-admission #2.
- orch-apply ×2: Stage 0+1 PASS, conservation `task_total 782=782`, `signal_total 200=200`, 4 rows stamped, **0 created**. Stage 1g's 16 dangling refs pre-existing, none mine.

### Carry-over
- **The P0 is the canary for the sweep's own adequacy.** `FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT` is now on re-admission #2. If it returns a third time, the 4h staleness window is raising the ceiling on a one-row-per-tick pipe that is the actual constraint — stop tuning the window and widen the pipe (that is what STEP po-4's row is for). Same shape TE-T05 showed and, unlike TE-T05, this one is a P0.
- **Do not let `FIX-DEVTEAM-BOUNDED1-MAINTLANE-NEXTAGENT-GATE` ship before `FIX-DEVTEAM-NONDEV-NEXTAGENT-RESIDUAL-...`.** It withholds maintenance-lane `next_agent` rows from BOUNDED-1 — the opposite direction. Alone it grows the stranded population.
- **The row that would let PO close Telegram reports is itself in the queue that cannot drain.** `FIX-PO-MAINFLOW-ORPHANS-TELEGRAM-REPORTS-RESOLVER-SUBFLOW` is DRS-stranded on agent-father. Meanwhile 4464-4469 stayed `new` through a third consecutive tick that read all six. PO's grant is read-only; this is not neglect and re-reporting it per tick is noise.
- **Unchanged from 18:29Z and still binding:** don't capture the 15-ticker serving-plane baseline `FIX-BCTC-Q1-2026-...-15T` demands until `FIX-BCTC-SERVING-GATE-VPSSTALE-...` ships, or that census is corrupted for a third time.
