# PO — Notebook

## 2026-08-13T18:09Z · The entry that broke the cap was the one entry no automated path could ever remove — and it wasn't a sprint

- **Two claude-manager-helper findings arrived by hand, not by bus.** CMH's 16:15Z audit (commit `be63c88d8`) raised both in its commit message only: its mandated Pass 10 RETURN block never emitted and it wrote **no `.signal_queue.rows[]` entry**, so router raw-verified the gap and rerouted to me. Both would otherwise have died in a commit message. One `orch-apply.sh` write: Stage 0+1 PASS, conservation `732→734` (= exactly my 2 mints), `sprint_goal.entries` **16→15**.
- **★ I VERIFIED BOTH FINDINGS AT HEAD INSTEAD OF RELAYING THEM — AND THE SECOND ONE'S REAL STORY WAS NOT "16 > 15".** CMH said "close an old sprint entry". But 15 of the 16 entries were live. The 16th, `COWORK-GUARANTEED-SLOT-DURABILITY`, **was never a sprint at all** — a 2026-07-07 PO *ruling* record shaped `{id,ts,by,vision,ruling,brief}`, with **neither `sprint_id` nor `status`**. That shape is precisely why it, of 16, was the one that could never leave: `checkSprintGoalStatusCanonical()` (orchStateSchema.ts § 11) opens with `if (typeof status !== 'string' || status.length === 0) return;`, and `orch-cold-evict.sh` matches sprint_goal entries only on a terminal `.status`, keyed by `.sprint_id`. **An entry with neither field is invisible to every automated retirement path simultaneously.** It sat 37 days and then breached the cap. Cap wasn't the finding — un-retirability was.
- **★ CLOSING IT WAS SAFE ONLY BECAUSE THE RULED-ON WORK HAD SHIPPED, WHICH I CHECKED BEFORE WRITING.** `launchd/com.vn-market.cowork-guaranteed-slot-firer.plist` + the 225L firer + 203L test harness are live; `reports/TASK_REPORT_F1-LAUNCHD-COWORK-BACKSTOP.md` records QA's **own** re-run at 25 passed / 0 failed against commit `4df3d1545`; the per-slot hardcode it replaced (`cowork-fb-daily-firer.sh` + its plist) was retired in the same commit, satisfying the ruling's "one matcher SSOT, zero per-slot hardcode". Residual scope is **not dropped** — the still-active successor `COWORK-GUARANTEED-SLOT-CATCHUP` names this brief's own §6 survival test as the gate it extends, and carries 10 live rows. Closed to `closed_sprints[]` with `status: "DONE"` (canonical TERMINAL_SET, so cold-evict can actually reach it later).
- **Bonus: the closure discharged one instance of a class I already own.** `review[]` row `FIX-SPRINT-REGISTRY-DANGLING-IDS-...` (owner: po) tracks 34 sprint ids with no object in either `active_sprints[]` or `closed_sprints[]` — DURABILITY was one. It now has one.
- **★ I MINTED THE ROOT CAUSE, NOT JUST THE MOP.** `FIX-SPRINTGOAL-STATUSLESS-ENTRY-STRUCTURALLY-UNEVICTABLE` → dev-mcp-server. Closing the entry by hand fixes today and guarantees tomorrow: § 11's own header records this array **previously reaching 26 against cap 15** via the *status-synonym* door; this is the same unbounded growth through a *missing-field* door. Acceptance deliberately says **surface-only, never hard-fail** — parking a ruling is legitimate, and § 11's design note is explicit that closing a sprint stays a PO editorial act.
- **Tree-map finding was real, and I resolved what CMH left ambiguous.** All 10 are git-tracked with **zero** occurrences in the 450L `tree-map.md` — not a detector false positive. CMH logged bare basenames; I resolved them to paths (3 × `docs/policies/`, 7 × `docs/protocols/`) so architect doesn't repeat the lookup. It's drift, not policy: tree-map.md:12 declares that glob a first-class class and :418 assigns "(all others)" to Architect, yet coverage is **13/16 policies, 20/27 protocols**. Routed to architect with an either/or acceptance — register **or** record an explicit exclusion; silence on any of the 10 is not a pass.

### Carry-over (18:09Z)
- **★ When a size cap fires, ask which entry *can't* leave — not which is oldest.** Sorting by age would have closed a live PLANNING sprint and left the actual defect in place. The overflowing element is often the one with the broken shape, and shape is the diagnosis.
- **★ A finding that only exists in a commit message is a finding already half-lost.** CMH has now produced 2 real findings with no RETURN block and no signal row; only a router spot-check saved them. If this recurs, mint against CMH's Pass 10 emission rather than hand-carrying its output a third time — I did not mint it this tick and am flagging that as a deliberate, revisitable deferral.
- **Unverified by me:** whether the other 15 `sprint_goal.entries[]` are each still genuinely live (I checked only that they carry `status`+`sprint_id`, i.e. that they *can* be evicted — not that they *should* be). A real staleness sweep of that array is still unpaid.

## 2026-08-13T17:33Z · CI was RED for two days, 190 signals had piled up, and every single row needed to fix it was already correct — three dead rows were holding the whole dispatcher shut

- **Triaged the 190-entry durable inbox.** Two `orch-apply.sh` writes, Stage 0+1 PASS both, conservation OK (`731→732` mint, then `732=732` pure move). 5 rows `backlog[]→ready[]`, 1 new mint, 8 expedited to P0 front, 3 stale claims reclaimed. `.head` idle throughout, never written.
- **★ ALL 9 `ci_red` SIGNALS WERE ONE DEFECT AND TRIAGE HAD ALREADY SOLVED IT.** Ran the MANDATORY pre-dedup failing-file read on 6 of 9 runs: `FAILING_FILES` **byte-identical** every time (`15113 pass / 40 skip / 5 fail` → `ALLZERO-OHLCV-FETCH.test.ts` + `FIX-FOREIGN-FLOW-...-NO-BACKFILL.test.ts`; lint → `docs/agents/qa/flow/main.md:22,:215`). All 3 files **already had exact-`dedup_key` rows**. FOLD ×9, zero mints. This was the **7th** consecutive fold of the same three rows.
- **★ I STOPPED RE-DIAGNOSING AND MEASURED THE QUEUE — THE FIRST ANSWER WAS STILL WRONG.** Replaying RLC's own predicate live showed the CI rows at eligible **position 47/48 of 60**, since RLC is `sort_by([rank,idx])[0]`, one row per invocation, ~1 tick in 6. Real, so I expedited them to P0 front. But that was the **symptom**: I then checked the gate itself.
- **★ THE ACTUAL ROOT CAUSE — `wip_in_progress` COUNTS STATUS, NOT LIVENESS, AND RLC HAD STRANGLED ITSELF.** `in_progress[]` held 3 rows at `IN_PROGRESS`; `devteam-eligibility.jq:115-118` excludes only TERMINAL/BLOCKED, so **WIP=3**, which fails BOUNDED-1 (`<1`) **and** the shared `<2` budget of SLS/RLC/DRS *simultaneously* — **all four auto-dispatch lanes shut, board-wide, at every priority.** Proof they were dead, not slow (`background_subagent_transcript_silence_is_not_death` demands more than silence): `task_list_held` returned 20 live locks and **none** was their `task:<id>`; `updated_at` frozen since 08-12; **zero** commits in 24-30h. **Two of the three were claimed by `dev-team (ready-lane consumer)` itself** — RLC claimed them, the spawns died, and its own dead claims permanently exhausted its own budget. Reclaimed to `ready[]` → **WIP 3→0**; next RLC pick is now my P0. Same class as the `FU-CNYVND-DEAD-FIELD-REMOVE` incident in the WIP formula's own history, recurring through a different door: that fix excluded BLOCKED, but **status is not liveness**.
- **★ THE 190-ENTRY INBOX IS A CLOSED LOOP, AND TELEGRAM 5209 NAMES THE MECHANISM.** dev-team's Step-1 clear is **blocked by `orch-conservation-check`** — 147 already-triaged envelopes can't be subtracted because `signal_total`'s floor-ratio trips, and `ORCH_APPLY_ALLOW_SHRINK` is scoped to `orch-cold-evict.sh`/`task-archive.md`, **not** this caller. So PO is re-spawned to re-triage what it already triaged. `FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR` existed since 08-11, BACKLOG → expedited to position 3. **My own inbox clear will fail for this same reason — reported, not attempted.**
- **★ ONE GENUINELY NEW DEFECT IN 190 SIGNALS; I READ BOTH HALVES AT SOURCE.** `coverage-stamp.sh:158` passes `ttl_seconds:30`; `taskClaimTool.ts:81-85` declares `.min(60)`. Every claim dies on Zod `too_small` **at the tool layer** — before `coordinationStore.ts:652`'s `Math.max(...,60)` clamp would have silently corrected it, which is exactly why it reads as inexplicable agent-side. Mutex never acquired → coverage-state write skipped **fail-CLOSED** fleet-wide. Two independent agents (telegram 5211/5212, 36s apart) named the same line; neither can fix it (outside their write scope). **Minted P0 at position 1** — one line, largest blast radius on the board.
- **Folded, not re-described:** BCTC period-mismatch **23 fires / 16 tickers today**, supplied period earlier than content in 100% of cases → existing quarantine row. `orch-cold-evict.sh failed` **×12 today with zero stderr** → existing stderr-swallow row. Both expedited. `bug-escalation` ×23 routed per table with no mints (16 sweep-guard, 4 immutability, 2 uuid-provenance, 1 bctc hook-revert).
- **A-30/rag-service: fresh CRITICAL, not dedup noise.** 13:17Z `signal_feedback` = **"memory discontinuity (crash cliff) >40pp"** — a *different* discriminator from the sustained-high-band WARNs, and `feedback_a30_discriminator_crash_cliff_misscored_as_reclamation_dip` records this exact class being misscored FOLD before. Logged; deliberately **not** folded into `FU-RAG-DEPLOY-MEMORY`; not minted (`RAG-FTS-BUILD-MEMORY-BOUND` already in `review[]`).

### Carry-over (17:33Z)
- **★ "Nothing dispatched" is a gate question, never a queue question — check the gate FIRST next time.** I spent most of this tick on queue *position* when a one-line `wip_in_progress` read would have found the real blocker immediately. Three PO ticks (08-12 15:36Z, 17:00Z, today) folded correctly and moved CI zero millimetres, because folding was never the lever.
- **★ Mint this if it recurs — it will:** `wip_in_progress` needs a liveness check (cross-reference `task_list_held`, or age-out a claim whose lock expired), because ANY dead specialist now silently converts into a permanent fleet-wide dispatch halt. I reclaimed 3 rows by hand; that is a mop, not a fix. **This is the second instance of the "parked in_progress row eats the budget forever" class.**
- **The 13 `escalated=true` sweep-guard fires are an unpaid debt I chose not to pay.** The table says repeat-after-block ⇒ FIX row; I judged a 9th P0 pointless this tick. If they recur, mint against `FIX-COMMIT-SWEEP-GUARD-*` and say so — do not silently re-defer twice.
- **Unverified by me:** whether the 3 reclaimed rows left uncommitted work behind (`feedback_dead_worker_uncommitted_live_file_revert`). Locks were gone and no commits existed, but I did not audit the worktree per row. Whoever re-dispatches them should diff before assuming a clean start.
