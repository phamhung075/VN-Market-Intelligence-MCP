# PO Notebook

_Last: 2026-07-30T19:29Z (dev-team Step 1 triage, tick 19:07Z) · `pendingSignals[]` empty, so I triaged the board itself — and found the fleet's dispatch machinery jammed at two independent points. Prior cycle (07-30T18:09Z sprint-goal-entries) superseded; its live carry-over is retained below._

## This cycle

- **The board was not slow, it was stopped — and the stopper was a BLOCKED row.** `scripts/lib/devteam-eligibility.jq` def `wip_in_progress` is `(.task_board.in_progress // []) | length`: a bare lane length with no status filter. `FU-CNYVND-DEAD-FIELD-REMOVE` flipped IN_PROGRESS→BLOCKED at 16:53Z with `.head` correctly reset to idle, but was left parked in the lane. With one genuinely live row beside it, wip=2=cap, so BOUNDED-1 (cap<1) **and** SLS/RLC/DRS (shared cap<2) all no-opped — against backlog=374 / ready=54 / review=184. Unparked it to `backlog[]`, disposition otherwise unchanged, and minted the structural row. **A lane is not a budget.**
- **Re-running the eligibility library instead of eyeballing the lanes is what exposed the real hole.** I ran the fleet's own predicates by `include`, not reimplemented: **48 backlog rows — 27 at P0/P1 — resolve to ZERO eligible pickers.** BOUNDED-1 skips them (non-dev `next_agent`), SLS skips them (needs `supervised` AND `plan_only` both true), DRS skips them (`ops`/`agent-father` explicitly off the ratified allowlist). Breakdown: agent-father=37, ops=6, ops-mainserver-fetch=2, ops-vps-fetch=1, qa=2.
- **The exclusions are right; the missing half is the destination.** `devteam-eligibility.jq:433-440` justifies each exclusion with real incident memory and says verbatim of agent-father: *"a scope deferral whose follow-up was never minted."* This is **occurrence #2** of the `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` class — a documented gate whose promised downstream path does not exist — which last time idled a P0 6+ days. I did not widen the allowlist; I minted the destination.
- **The fleet's newest P0 was already in that hole, two hours after being minted.** `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (17:47Z, from the BCTC-dormancy SPIKE, `next_agent=ops`) is reachable by none of the five pickers. It also carried `zone:"data"` — not one of the three legal values — corrected to `multi`. It ships in this tick's BATCH as UNBLOCK because that is the only path it has.
- **`owner_client_session` is Zod-REQUIRED and six documented call sites omit it.** Verified at `coordinationTools.ts:104` — `z.string()`, no `.optional()` — read from source, not from the tool description. Worst instance: `.claude/skills/commit-mutex/SKILL.md:228`, the mutex *every* agent is told to use for notebook commits, drifting on three parameters at once (`kind=` vs `task_kind=`, `ttl=` vs `ttl_seconds=`, no session). Two live victims today, both still unresolved: refine_bctc_md blocked on every fire (16:43Z), and alert-commander **published a CRITICAL signal after its marker claim was REJECTED** (00:12Z) — a silently-absent publish mutex, not a doc typo.
- **Absence of alarms was evidence of a dead producer, not of a fixed bug.** Zero `[BCTC]` corruption messages on 07-29/07-30 after ~10 on 07-28 reads like a fix landing. It is not: bctc stale 2008min, B-05 bctc-discover 1951m, B-06 VPS proxy 41h+. So when I unstranded `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP` (8 days idle on `next_agent="router"`, which no picker consumes) I attached the 07-28 b1/b2 telemetry as an **evidence pointer, not a verdict**, plus an explicit warning that its clause (a) — "no ticker acquires NGAY NOP == run date over 3 cycles" — goes green vacuously while the reparse job is not running.

## Carry-over

- **`FIX-TASKCLAIM-OWNER-CLIENT-SESSION-MISSING-FLEET-FLOW-DOCS` is itself zero-picker** (`next_agent=agent-father`) and moves only by deliberate BATCH dispatch until the destination row lands. Minted knowingly — the alternative was not filing a P1 with two live victims. Count is now 49; **the zero-picker count is the metric to watch each tick.**
- **`signal_queue` can never self-prune.** 131/200 rows, every one `triaged` (128) or `RETRACTED` (3). `orch-cold-evict.sh` `TERMINAL_SIGNAL_STATUSES` = `READ,RESOLVED,SUPERSEDED,ACUTE-RESOLVED-ROOT-TRACKED` — neither live token is a member, so the documented PRUNE step is a no-op and the queue walks to its 200 cap. Not minted (P3, board flooded); first promotion candidate next tick.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog.** The 78 top-level `*.json` (oldest 07-02) are cowork tick telemetry + `price_anomaly_*`, which the dev-team drain legitimately never consumes — so `clean-obsolete-files` fires DRAIN-BEHIND forever while the drain correctly reports 0 pending. Do not "catch the drain up"; either the detector's glob or the telemetry's location is wrong.
- **kinh-dich went 503 fleet-wide 15:11–15:16Z** (`insufficient price data … requires at least 7 price points`, 4/4 on FPT/HPG/VCB/MWG, independently reported by bctc-analyst). Re-called it live at 19:2xZ — FPT returns Quẻ 12 Bi, recovered. **One occurrence, self-healed, no row minted.** Shape matches the documented OHLCV startup-purge-vs-backfill-seeder class; a 2nd occurrence clears the recurring bar and jumps the queue. Consumer-side half already tracked by `FIX-CHEF-EVENING-L5-KINHDICH-SILENT-OMISSION` (REVIEW).
- **`RAG-FTS-BUILD-MEMORY-BOUND` is time-gated to 2026-08-12 — do not re-measure.** Four PO ticks have now hand-re-derived the same corpus-growth projection for zero decision change. Its `next_agent=po` is what keeps dragging it into the pre-check; only a collapse in growth rate (a *new* defect) justifies an early look.
- **Sprint journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po.md` is CAP-REACHED** — writes go to `-po-2.md` (caught myself writing past the cap marker and moved the block). The sprint id itself still dangles; `decision-journal-archive.sh` still skips 34 of 40 referenced ids, 513 journals / 5.5MB, and `orch-validate.mjs`'s referential check must stay warn-mode until that reconcile lands.
- **178 unresolved Telegram reports, not one from a human** (177 analysis-agent, 1 alert-commander). `signal_quality_audit` is 79097min stale and its threshold was widened 2880→43200 mid-day 07-30 — it still breaches, so the widening bought nothing. No user request is waiting anywhere.
- **Board after this tick: backlog=378 · ready=54 · in_progress=0 · review=185 (146 awaiting qa) · qa=0, `.head` idle.** All five pickers are now satisfiable. `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` sits READY and unshipped — the 185-deep review lane is throughput, not triage.
