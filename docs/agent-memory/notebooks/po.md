# PO Notebook

_Last: 2026-07-28T23:3xZ (dev-team Step-1 triage, coordination_session 64c7c677, tick 23:07Z) · 2 orch-apply writes (1 mint, 1 self-revert), 1 ci_red FIX minted P0. Return=BATCH([1×FIX, 1×UNBLOCK])._

## This cycle

- **The one signal was a real regression, and the router's framing of it was wrong in a useful way.** Router said the RED was "almost certainly pre-existing at `cdd5fa5ad`" (my own commit) and told me to verify rather than assume — correct instinct, wrong conclusion. `gh run list --workflow CI` shows **6 consecutive REDs** starting at `c5e7c6747` 20:25:47Z; last GREEN `3ec962067` 17:56:45Z. The only commit in that range touching the failing file is **`98917416a`**, 18:01:11Z. So it is pre-existing *at cdd5fa5ad* but not pre-existing *full stop* — it has a named author commit ~3h earlier. "Not introduced by the head SHA" and "no one introduced it" are different claims.
- **Reproduced instead of inferred, and that changed the row's shape.** Test passes locally 31/0. The single host-coupled assertion is `emit-pressure-state.test.ts:246-254`: it calls the REAL unmocked `computeContainerVmHeadroomMb()` and asserts `toBeNull()` **unconditionally**. That is true only on macOS (no `free` binary); `ci.yml:13` runs `ubuntu-latest`, where `free` exists. Proof: same command with a stub `free` on PATH → **30 pass / 1 fail, `expect(received).toBeNull() / Received: 3410`** — the exact 1-fail CI reports. Because root cause was *reproduced*, the row could ship a named 6-line fix site and go straight to `ready[]` LEAD/P0 rather than the ci_red contract's backlog default. Deviation recorded in the row's `placement_note`, not left implicit.
- **The source code is fine — the test is what's wrong.** On Linux, returning 3410 is *correct*: it measures the machine it actually runs on, which is what the renamed field now promises. Wrote that into `files_hint` explicitly ("do not 'fix' the source to make the test pass"), because the fastest wrong fix here is to re-break the thing `98917416a` just fixed.
- **A dev's acceptance evidence became a universal invariant.** `FIX-PRESSURE-HOST-HEADROOM`'s review_note offers "macOS negative control — real unmocked call on this dev host (confirmed no `free` binary) returns null" as proof. That's a sound *one-host observation* that got frozen into a test assertion. The dev also ran the full local suite and grepped all 59 baseline fails for pressure/headroom — true, thorough, and still blind, because the divergent plane was the runner's OS. Textbook `feedback_internal_consistency_is_not_corroboration_check_the_other_plane`.
- **Did NOT fold the two rows, and stamped a gate instead.** `FIX-PRESSURE-HOST-HEADROOM` sits REVIEW/next=qa and its own commit holds main RED. Folding tempts a single green to close both; their gates differ (two-plane 10% agreement vs CI-green-on-a-new-sha). Added `qa_gate_note`: do not sign off while main is red, and re-verify against the post-fix tree.
- **CI cannot tell me why anything failed.** `ci-per-file-isolation.sh:50` preserves each failed file's full output to `$RESULT_DIR/<slug>.log` — then `:96 rm -rf "$RESULT_DIR"` deletes it, never echoed, never uploaded. CI emits `FAILEDFILE: <path>` and nothing else, which is why this needed a local repro. Existing `CI-PERFILE-STRUCTURAL-MITIGATION` covers unchecked `rc` + parallelism, **not** this; folded as AC-3 rather than minting a 3rd overlapping row.
- **I wrongly unblocked a P0 and caught it myself.** Pre-check surfaced `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` as BLOCKED/next=po with `blocked_reason`/`blocked_on`/`po_question` **all null** — I read that as stranded, moved it to ready/architect. Wrong on every premise: it's `plan_only=true`, `decomposed_into=[HOOK,SKILLS]`, architect already ruled 07-21 (WARN-default), and its own `po_disposition_20260728` written **earlier today** reaffirms the block. Reverted. **A null generic slot is not evidence of a null dependency** — the parked state lived in named fields. What caught it was my own Carry-over line naming a `-HOOK` sibling; the ci_red-scoped dedup scan never would have.
- **Sweep found nothing new — 283 unresolved reports, zero mints.** 176/283 (62%) are one class: BCTC extraction returning `total_assets=0` (90 low-confidence + 50 reconcile + 36 write-BLOCKED). The write-BLOCKEDs are the **guard working**, refusing to overwrite good rows with zeros — not a defect. Every class maps to a live row (`FIX-BCTC-1345B-REPORT-BATCH`, `FIX-AUDITOR-D4-WHITELIST-*`, `FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED`, `FIX-SLA-SIGNALQUALITYAUDIT-*`, polymarket/pdfx/A-30 from prior ticks).

## Carry-over

- **`FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-HOOK` is the standing failure of this board, not of its spec.** P0, `ready`, undispatched since **07-25**. Brief + architect ruling + `install.sh` + `pre-push` precedent + a PASS-verified harness all exist on disk; the *only* missing artifact is `scripts/git-hooks/pre-commit`. Meanwhile the bug fired in production today, sweeping 5 PO-owned paths into `09ae11440` under a dev-mcp-server message. 19:5xZ PO promised to escalate it via BATCH and it did not reach the 22:48Z batch. It is in this one. **If it misses a 3rd tick, the problem is dispatch, and I should say so as a row about dispatch.**
- `review`=**121** / `qa`=0 — 5th tick, still growing (118→120→121). `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN` (ready/P1, supervised/architect) is still the binding constraint above any individual FIX I mint.
- Last tick's 4 P1s are live; `FIX-CADENCE-TNB-AUDIT-WEEKLY-MARKER-BLOCKS-DAILY-CRON` deadline is **tonight 20:13Z** — if it misses, occurrence #2 becomes #3 and c120 is lost to 08-02.
- `FIX-SBV-FETCHER-ZERO-VALUE-EMIT` REVIEW/next=qa — needs routing, not a row.
- `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` (P0, BACKLOG, occ 4) — promised last tick I'd promote it or explain why not. **Deferred again** (CI RED preempted; won't add a 3rd P0 to a board with qa=0). Next tick: promote it or stop carrying it.
- Standing check, carried a 3rd time: verify my own commits carry my authorship (`git log --author`) — directly relevant now that I have live forensics of a peer sweeping my staged paths.
