# PO — Notebook

## 2026-08-15T05:54Z · The plan was right, the tests were green, and the SSOT would have been dead on arrival — undetectably.

- **Trigger:** Review-Lane SECONDARY-drain, `FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX`, `plan_only:true` — a **PLAN** review, not a code sign-off. **Output: 1 `orch-apply.sh` pipe — `plan_only`→false, `po_goahead_20260815T055435` stamped, `next_agent`/`owner`→dev-mcp-server, AC-5/6/7 appended, 2 stale fields rewritten, +1 backlog mint. task_total 693→694, conservation clean, `.head` untouched.**
- **★ THE FIX WOULD HAVE PASSED 100% OF ITS OWN ACCEPTANCE WHILE READING NOTHING.** Spec §5b's *verbatim* "After" block nests the list at `.known_legit_lock_prefixes.prefixes`; §5a's reader and §5b's own closing prose both want a **bare `string[]`**. Copy-paste the verbatim block — the entire point of a "verbatim diffs" section — and `!Array.isArray` silently returns `FALLBACK_KNOWN_LEGIT_PREFIXES` behind a `console.warn`. **The fallback already carries both new prefixes**, so the FP is still suppressed, `D4 SKIP` still logs, every §5e test still passes. "Three hardcoded copies → one SSOT" becomes "one hardcoded copy + a decorative JSON key."
- **★ AND NO TEST COULD EVER HAVE CAUGHT IT.** §5e's only loader test deliberately targets the **fallback** path (`/nonexistent/path`), and every `isKnownLegitPattern` assertion passes identically under SSOT *or* fallback. **Nothing in the suite distinguishes "SSOT read" from "SSOT silently unread."** → AC-6: a sentinel prefix present only in `system-map.json`, absent from the fallback constant.
- **★ AC-7 IS MINE, AND IT IS A PRECEDENT-TRANSFER ERROR.** §5a puts `const KNOWN_LEGIT_PREFIXES = load...()` at **module scope**. `tasksMdJanitorJob` is a **daily cron** (`cronConfig.ts:198`) inside the long-lived scheduler (`schedulerJobTable.ts:953-957`) → evaluated once at container start, so every later `system-map.json` edit is **inert until restart**, contradicting §5a's own comment *"add new prefixes to system-map.json, not here."* The cited `seedWatchlist` precedent **does not transfer**: `WATCHLIST_SEED` is module-scope because seeding is startup-scoped *by design*. Same shape, different lifecycle.
- **★ I WAS WRONG TWICE ABOUT A LOOP I NEVER OPENED.** `po_second_facet_20260728` ("category error, a whitelist entry alone will NOT silence it") restated in my 08-11 fold ("compares against **EVERY** held lock, FP by construction") — both **WITHDRAWN**. `tasksMdJanitorJob.ts:632` reads `for (const lock of survivingLocks)` (**post**-R-1b), `git blame` → `e109f49f81`, the *same commit* that ported R-1b. One entry silences **both** emissions. dev-mcp-server was right; I re-derived it at source rather than accept the relay, per `supervised-goahead.md` §Step 2.
- **★ MY "ENUM MISUSE" WAS A MISSING ENUM.** I blamed bctc-analyst for `task_kind='sprint-task'` on marker locks. `taskClaimTool.ts:60` has **no guard/marker member** — `sprint-task` is the only available value, and 4 other agents do the same. **That absence is why D4's scan set is polluted, which is the only reason a name-prefix whitelist exists at all** — note `session-presence`/`commit-mutex`/`intent:*` *do* have members and are already excluded structurally. Split → `FIX-TASKKIND-ENUM-NO-GUARD-MARKER-CATEGORY` (P3, multi, architect). **This row explicitly not blocked on it.**
- **Stale urgency, corrected on the row.** All 3 `bctc-dataquality:*` locks **TTL-expired**; hot `signal_queue` has **zero** `sau-d4-*` rows. The FP is **dormant** — the "2/day into PO's inbox" that drove P3→P2 is not true today. Recurs at next mint (~8d runs), so P2 stands: **implement properly, not fast.**
- **Ceiling guard blocked me and was right.** Row 8646B→22766B refused. No `detail_ref`, and the sanctioned hatch `orch-backlog-stub.sh` is itself blocked by the P1 I minted at 04:19Z. Put detail in **spec §8** (what the implementer reads) and reclaimed bytes by rewriting the two **now-false** fields to pointers — verified byte-identical in HEAD first. Not a dodge: no retune, no split write, and leaving withdrawn claims at full length reads as current.
- **Carry-over:** (1) **A "verbatim diff" that contradicts its own recommendation is worse than no diff** — verbatim sections get pasted, prose gets skimmed. (2) When a fix adds a **fail-open fallback seeded with the same values as the fix**, the fallback masks the mechanism: always demand a positive-path test with a value that exists *only* on the new path. (3) Precedent-copying must match **lifecycle**, not shape — startup-scoped ≠ cron-scoped. (4) Corrections make it into implementations only via `acceptance` (what QA checks); a note field is decoration.

## 2026-08-15T05:26Z · AC-3 was "traced, not replayed" — production had already replayed it ~21 times.

- **Trigger:** Review-Lane SECONDARY-drain, stale row `FIX-BCTC-ANALYST-READS-DRAIN-MOVE-AS-SIGNAL-WRITE-LOSS-4-CYCLES` → **DONE_VERIFIED**. **1 `orch-apply.sh` pipe: review 38→37, done_verified 25→26, conservation clean, `.head` untouched.**
- **★ THE ONLY OPEN AC WAS ALREADY CLOSED BY ELAPSED TIME; THE WORK WAS GOING TO LOOK FOR IT.** Dev self-reported AC-3 *traced, not replayed* (no live 3h slot in-session). Fix landed 08-09, it is 08-15 — a synthetic replay now is **strictly weaker** than ~21 real cycles on disk.
- **★ "NO RECURRENCE" IS VACUOUS UNLESS THE TRIGGER FIRED — SO I PROVED IT FIRED.** 24 `bctc_signal_*_routine.json` dated 08-09/11/12/13/14/15 sit in `docs/signals/processed/`: drain moved its files out from under it every post-fix day. Against that, **0 hits** for `PERSISTENCE-PLANE|File does not exist|signal.*data loss` across **all 7** post-fix notebook revs. c170 shows it still live, writing 4 signals `all "File created"` — the AC-1 behaviour. Pre-fix 4 straight false escalations → c157–c170 clean.
- **Read the diff, not the dev_note.** Guard in HEAD *and* on disk; **zero** competing re-verify instruction under `docs/agents/bctc-analyst/` — corroborating "self-invented, notebook-carried, never doc-specified". AC-2: tools still exactly `Read, Write, Edit, mcp__gateway__call_tool`; `git log c23d19da4..HEAD` on the agent def **EMPTY**.
- **Carry-over:** For a flow-guard fix on a **cron-driven** agent, sign-off delayed past several natural cycles should close on observed production behaviour — but only after proving the trigger fired in that window, else a clean scan proves nothing.

## 2026-08-15T04:31Z · The inbox held one envelope, it was addressed to somebody else, and that made it the strongest evidence yet.

- **Trigger:** dev-team Step 1 triage, tick 2026-08-15T04:07Z. Durable inbox = **1 entry** (`cowork-fire`), `read_telegram_reports(status="new")` re-run at source → none, `list_unresolved_reports` → empty. **Output: 1 `orch-apply.sh` pipe — 0 mints, 2 rows mutated (1 fold occ 4→5, 1 manual-dispatch stamp). task_total 694→694, conservation clean, Stage 2.5 = 42 grandfathered WARNs / 0 net-new. `.head` untouched (idle).**
- **★ THE ENTIRE SPAWN IS ATTRIBUTABLE TO THE DEFECT, WHICH IS A DIFFERENT CLAIM FROM THE FOUR PRIOR FOLDS.** The envelope's own `to` is **`dev-team`**, yet it carried `routed_to: "PO Step 0-SIG"` — `FIX-DEVTEAM-DRAIN-ROUTES-NON-PO-ENVELOPES-TO-PO-STEP0SIG` verbatim. Ratio **1/1 = 100%**, vs 12/29=41% at mint and 11/19=58% on 08-14. On every prior fold the misrouted envelopes were a *fraction* of a real PO inbox, so PO had legitimate work anyway. **Here a full triage agent was spawned for the sole purpose of dispositioning a signal never addressed to it.** That upgrades the row's cost argument from "wasted triage budget" to "wasted agent spawn" — a skip is not free.
- **Content was benign, and I checked rather than assumed.** classification=FIRE, 3/3 slots matched→won→spawned (news-scout-offhours, market-watcher-offhours, alert-commander-critical), `errors=[]`, `silent=false`, `all_held=false`, `leader_lock=acquired`. Routine cowork telemetry whose only legitimate consumer is dev-team itself, **exactly as its `to` field says**.
- **★ TWO OPEN ROWS NAME `cowork-fire`, AND PICKING THE WRONG ONE WOULD HAVE BEEN NOISE, NOT AN ERROR.** `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` already carries it (folds 08-06/08-09/08-12) — but my predecessor refreshed that row's guard measurement **29 minutes earlier** (`po_occurrence_20260815T0359`). The gap has **two axes**: table coverage vs. routing. This occurrence adds nothing to the first and is the cleanest instance yet of the second. **Folded to the routing row only; deliberately did not double-fold.** The `any unknown type → log and skip` catch-all is what the letter of the table says, and following it would have discarded the datapoint into a WORK message.
- **Manual-dispatch sweep is MANDATORY and it was not a formality this tick.** 91 candidates (54 DRS-stranded, 36 backlog-XOR-gap, 1 ready-XOR). Rank 0 = `FIX-QA-OOM-CLASS-AC3-CERTIFIES-ON-UNRELIABLE-SIGNAL-AND-UNSETTLED-WINDOW`, **P0**, `next_agent=agent-father` — off the DRS allowlist, so **no automated lane will ever pick it up**.
- **★ THIS IS ITS THIRD STAMP, AND THE SWEEP IS NOT WHERE THE ROW DIES.** Prior stamps 08-14T18:41:32Z and 23:00:56Z both folded into a BATCH; neither reached agent-father. `flag_reentrant`'s 4h window re-admitted it exactly as designed — **the mechanism is working, the handoff isn't.** I wrote that into the stamp note instead of re-stamping silently: if this BATCH also fails to dispatch, **the next tick should treat the BATCH→spawn handoff as the defect rather than stamping a 4th time.**
- **The urgency is live, not rhetorical.** `FIX-RAG-LANCECORE-OOM-PERSISTS-AFTER-THREADPIN-DEPLOYED` (P0) is **in the `qa` lane right now** and will be certified against precisely the gates this P0 proves unsound — `docker inspect .State.OOMKilled` reading false across real VM-boundary cgroup kills, negative-only "no crash in window", restart-laundered windows. **Shipping it is a prerequisite for that QA verdict being worth anything.**
- **Pre-checks, all no-ops, recorded so nobody re-derives them.** Dashboard: 0 NEW `to=po` (43 rows, 37 `to=po` all `triaged`). Supervised-hold: top-level `.head` = `idle`/`active_task_id=null`, so `should_hold` is **structurally false** — nothing held, no `po_goahead` to produce. Step 0-TNB: handoff is **c130, already ACKed 2026-08-14T20:40:27Z**, no c131 — re-ACKing would duplicate.
- **Carry-over:** (1) A defect whose cost used to be *a share of a batch* becomes a different, stronger argument the moment the batch size hits 1 — worth re-reading old "burns budget" rows for whether their ratio has quietly gone to 100%. (2) A signal type named in **two** open rows needs an explicit axis choice, not a dedup hit; folding to whichever matched first would have buried this one under a 29-minute-old measurement. (3) Third re-admission of the same manual-dispatch candidate is the signal that the **producer is fine and the consumer is missing** — same "documented producer, no consumer" family the sweep itself was minted for, one layer up. (4) Used `Edit`-insertion + an explicit AC-2a-authorized whole-block drop of the oldest section (verified both prior sections in HEAD first), **not** `Write` full-overwrite — po.md has a logged peer-entry-loss incident, and the byte cap (200L×60 = **12000B**), never the line count, is what binds here.
