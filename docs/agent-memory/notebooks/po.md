# PO — Notebook

## 2026-08-23T18:09:26Z · Held 3 envelopes back from the inbox CLEAR so CI stays honestly red — the guard's input IS the array PO clears

- **Trigger:** dev-team tick 2026-08-23T17:37Z, rotation lane `step1_triage`. Inbox held **44 envelopes / 13 types** (carried over several ticks). **Output: 3 orch-apply writes — 6 folds + 1 undispatchable-row repair, 3 mints, 1 AC split, 1 partial CLEAR (41 of 44).** `task_total` 805→808.
- **★ Cleared 41, HELD BACK 3 deliberately.** `scripts/audits/guard-signal-type-coverage.sh` derives its Pipeline-A live-type set from the *same* `pending_triage_inbox[]` array Step 0-SIG clears. Clearing an unrouted envelope turns the guard green **because its input was deleted** — indistinguishable from a real fix. Held all 3 envelopes of the 2 unrouted types (`recurring-bug` x2, `sprint_registry_dangling_ids` x1). Verified after the write: inbox=3, ROUTED_A=28, still-unrouted = exactly those 2. CI stays red on `signal-type-coverage-guard` until agent-father lands the table rows. Same call PO made earlier today for `auto-push-abort`.
- **★ Never ran the guard — it is not read-only and `--check` is a no-op alias, not a dry-run.** Hand-replayed its two read-only extractors instead; got `["recurring-bug","sprint_registry_dangling_ids"]`, which the failing CI log (run 32655888564, head ab5087296) then confirmed verbatim. The CI log also *proves* the write path: after printing FAIL the guard attempted two auto-mints, aborting only on `bun: command not found`. On a dev host with bun those mints land — that auto-filer is the source of today's undispatchable rows.
- **★ The prior fix row for `sprint_registry_dangling_ids` was closed on a false green.** Emitter `scripts/orch-validate.mjs:862` emits `sprint_registry_dangling_ids`; the table has `sprint_registry_unresolved_journal_ids` — a *different type from a different emitter*. `FIX-SIGNAL-TYPE-ROUTING-GAP-sprint-registry-unresolved-journal-ids` was CANCELLED at 14:40Z "confirmed by grep on the live doc" — that grep used the **row's** name, not the **emitter's** literal (0 hits, re-verified). Minted `FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS` (P0, agent-father) carrying both literals character-exact.
- **`recurring-bug` is a closed loop:** dev-team's own `flow/main.md:162` HEAD.lock-recurrence template hands authors a type its own routing table cannot route. dev-team self-reported it was absent at `0c9380859` and present after its 15:37Z tick — half this CI red was created by the tick reporting it.
- **★ PEK AC-8: split, did NOT certify durability on an open window.** QA offered accept-on-1h51m or hold-24h; took neither. AC-8a (silent instant drop eliminated) is SATISFIED on live evidence (`bctc_layout_units` +237, `bctc_table_rows` +240, image `sha256:5d7d36db65f4`). AC-8b (contention → ~zero over ≥24h) moved **intact and still unproven** to `FOLLOWUP-PEK-EXTRACT-CONTENTION-PACING-CAPACITY-24H-WINDOW` (P2) with a stamped window: opens 2026-08-23T15:44Z, earliest honest re-measure 2026-08-24T15:44Z. Basis is the row's *own* pre-existing `out_of_scope(a)`, so it is a split not a goalpost move: a raise after a full 1800s bounded wait is the shipped design under overload — a capacity fact, not a regression. Ruling written to `verify_note` (structural field ⇒ no prose-ceiling cost on an 11765B row); `redispatch_count` deliberately not incremented.
- **Folded 6, minted 0 duplicates.** 20 notebook/ctxbloat envelopes → one fold on `CLEAN-NOTEBOOK-BYTECAP-3-FILES-UNPRUNABLE-SINGLE-SECTION` (ba.md ×5 and digest-predict.md ×4 are the hottest subjects). Both `recurring-bug` payloads carried their own `action_required`/`row_to_amend` — honoured both (drain-litter row: re-measured 49 top-level files, residue ≥7, incl. 3 *parseable-but-shapeless* `price_anomaly_*` artifacts that drain silently, the worse mode). Also folded: DRS null-`dispatch_lane`, SLS stale-stamp claim, pek `fallback-*` report_id UUID rejection (producer half now measured — 11 jobs × ~15-24min CPU, 100% discarded), team-tool-recheck retirement.
- **Every Telegram-report item was already on the board** — bond_maturity, backtest_runs, BCTC zero-extraction, pollNews all have open rows; the DRAIN-BEHIND report is **stale** (claims 76 top-level signal files, actual 49). Zero mints from that channel.
- **Repaired 1 undispatchable row** (`FIX-NOTEBOOK-AUTOPRUNE-AC6-SAMEDAY-TIE-...`, `next_agent` absent → `developer`) rather than filing a report about it. Re-pointed the 17-day-old umbrella `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED` to agent-father + P0 (structural fields only — its prose is frozen at 34589B, any append hard-aborts).

### Carry-over
- 3 envelopes deliberately parked in `pending_triage_inbox`. **Do not clear them to make CI green** — they are the guard's only remaining evidence. They drain naturally once agent-father adds the 2 table rows.
- `FIX-PM-3E-FAILLOUD-HOTFIX` (review, agent-father): QA's finding independently reproduced — `bash -n` errors, odd single-quote count (5) in the jq block; apostrophe in `no-op'd` at `docs/agents/pm/flow/main.md:209` closes the quote opened at :199. One-character fix, jq guard design itself sound. Needs dispatch, not a decision.
- Still open from prior round: 5 non-CCATO cold-evict victims unrestored; 13 of 17 null-`next_agent` `ready[]` rows untriaged (needs `manual-dispatch-sweep.md`).
## 2026-08-23T15:16Z · Phantom CCATO-MCP prerequisites were never phantom — a cold-evict commit narrated an archive write it never made

- **Trigger:** router triage bundle, 3 sources (router CCATO forensics + qa items + developer items), disposition-only round. **Output: 5 orch-apply writes, 5 mints, 1 major restoration, 1 dependency repair, 2 dedup removals, 2 reverted (safety gates caught them).** `task_total` 787→797.
- **★ CCATO-MCP-T1/T2/T4 were fully built, QA-verified DONE_VERIFIED (commits 4d20a76a2/f79019f9b/154180ebd, files confirmed live on disk) — then vaporized by commit 38c013342e, whose own message claims an archive write it never made.** `git show --stat`: "1 file changed, 1 insertion(+), 290 deletions(-)" — orch-state.json only, `archive/2026-08.json` untouched. Restored all 3 verbatim from `git show 38c013342e^`, honest raw_probe reconstructed from each row's own historical qa status_note. Unblocks CCATO-MCP-T3 now; T5-T8 correctly still gated on real in-flight work, not phantoms. 9 rows total died in that one commit — 4 restored (T1/T2/T4 + OPS-COWORK-GUARANTEED-SLOT-INSTALL, the last one needed live by qa's SESSION-DOWN row), 5 left for the systemic fix (`FIX-ORCHCOLDEVICT-NARRATED-ARCHIVE-WRITE-NEVER-EXECUTED-DATA-LOSS`, P0, minted).
- **★ The dependency gate was never the bug — the dispatch path around it was.** `devteam-eligibility.jq` `deps_satisfied()` correctly refuses a MISSING dependency; the auto-picker would have refused all 5 CCATO rows. Manual/router dispatch runs no such check at all. Minted `FIX-DEVTEAM-MANUAL-DISPATCH-BYPASSES-DEPS-SATISFIED-GATE` (P1). Confirmed a real dual-field divergence in the wild: `IVC-PM-DECOMPOSE` has `depends:[]` but `depends_on:[IVC-A1]`.
- **qa's own "4 of 5 hide it" was half right.** Field-verified all 5 flagged `ready[]`/`next_agent=po` rows: 2 already carry the real prerequisite in `.depends` (genuinely gated, not hidden from the real picker — qa's check apparently reads `.depends_on` only). Repaired the ONE truly-empty row (`QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL`) with its 2 real prerequisites; mirrored `.depends_on` onto the 2 already-correct rows for defense-in-depth; left the 2 qa called "correctly declared" untouched.
- **★ FU-RAG-DEPLOY-MEMORY's grandfather exemption is a live, proven vulnerability, not yet an executed false-cert** — qa's own control repro (on its row) shows identical `del(.verification)`+DONE_VERIFIED PASSES for this grandfathered id, ABORTS for a non-grandfathered control. Row is still `status=QA`. Minted `FIX-RCVERIF-GRANDFATHER-EXEMPTION-IGNORES-RETRACTION-VOID-MARKERS` (P0), both of qa's options offered.
- **Two safety gates caught my own mistakes before they landed.** Removing an `active_sprints[]` stub for `FIX-BCTC-BANK-SUMMARY-MAPPING` looked like dedup but tripped Stage 1h referential-integrity (sprint_goal legitimately needs it) — reverted, flagged instead (7-week stale un-decomposed sprint, real finding, not touched). Assigning `next_agent` to P0 `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` tripped the row-prose-ceiling hard-abort (already 16061B, grandfathered — ANY growth blocks) — reverted; row is NOT patchable via a normal field-write, flagged for the router to hand-dispatch directly or for detail_ref extraction first.
- **system-map.json telegram env-var names are wrong SSOT, low current blast radius** — verified production `send_telegram` reads `.env` directly (correct names), so nothing is broken today, but the explicit no-hardcode convention makes this a real latent trap. Minted P1. Also minted P1 for `guard-signal-type-coverage.sh`'s `--check` flag being a documented no-op (writes live board regardless) — read only, never executed, per fence.
- **Did not touch:** the 3 held `auto-push-abort` envelopes — already correctly drained by a prior cycle after the routing fix landed (confirmed 0 in inbox). Ghost-row dedup: removed 2 confirmed `created_at=00:00:00Z` placeholder duplicates (`FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT`, `FIX-ACTIVE-READPATH-LIVENESS-PROBE-NO-DETECTOR`).

- **Wave 2 (router mid-task queue, 3 agents returned):** 8 more FIX rows minted (ocr_gateway inflight bookkeeping, pdf-extractor Dockerfile label mismatch, bctcPdfPullJob done-without-table-leg-await, size-lint pre-push scans working-tree-not-push-range, task-claim-lint baseline rekey, cowork runbook doc drift, chef evening SLOT_ID spelling conflict, bctc-analyst missing tool grant). Unblocked `FIX-QA-VC-ACTUATOR-NO-REGRESSION-FIXTURES` (parent DONE_VERIFIED). `TASK-COWORK-PMSET-WAKE-ADJUNCT` moved to `BLOCKED` with its AC-1/AC-2 conflict resolved (single-earliest-slot wake, not union-of-8 — sudo/human-action blocker documented, not agent-closeable). **BUG 5468 decision: do NOT pause `bctcPdfPullJob`/`bctcExtractReconcileJob`** — no safe fast toggle exists; recorded as a signal row (target row is over the 12000B ceiling) with a targeted-reconcile_attempts-reset mitigation if the 21-row cohort nears the cap-8 terminal-fail before dev-pdf-extractor's restart-loop fix lands.

### Carry-over
- 5 non-CCATO victims of the same cold-evict commit not yet restored (folded into the systemic fix's AC-3). 13 of 17 null-`next_agent` `ready[]` rows not triaged — needs `manual-dispatch-sweep.md`, out of this round's scope.
