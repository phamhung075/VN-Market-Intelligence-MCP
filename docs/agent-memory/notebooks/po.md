# PO — Notebook

## 2026-08-06T20:33Z · sign-off: FACTORY-APIGW-split-sandbox → DONE_VERIFIED (2nd stale review[] row, same shape)

### What actually happened
- **Second instance of the identical failure shape signed off two ticks apart.** Like `FACTORY-KINHDICH-split-sandbox`, this row carried NO `status_note`, NO `review_note`, NO `developer_review_note`. `dev-api-gateway` shipped `251cda70e` on 2026-07-24 with a 92L decision journal and a rich commit body — and left the board row bare. 6 weeks of Review-Lane sweeps found nothing resolvable. Re-derived all 8 DoD clauses RAW; banked nothing from the self-report.
- **All 8 DoD clauses pass.** 744L `cmd/sandbox/main.go` → 119/188/53/193/240L across 5 `package main` files. `go build`/`go vet`/`gofmt -l` clean; `go test ./...` 11 pkgs ok 0 fail; `golangci-lint run ./...` "0 issues."; sandbox 14/14 primitive + 1/1 module + 15/15 all, status=OK. Zone-scoped `size-lint-justification.sh --check` → PASS, 0 unjustified.
- **The two decisive checks were the symbol diff and the trace diff — not the test run.** Sorted top-level decls pre vs post: 23 → 25, diff EXACTLY +2, and both are the DoD-mandated additions (`buildPrimitiveTrace`, `type sandboxService`). Zero symbols dropped/renamed. Then diffed all 15 `sandbox/traces/*.json` from a live HEAD run against the PRE-SPLIT blobs via `git show 251cda70e^:<path>` — `jq -S 'del(.run_at)'` normalised, **15 compared, 0 diffs**. Empirical behaviour-equivalence without ever checking out old code (shared working dir stays untouched — `feedback_subagent_branch_checkout_hijacks_shared_working_dir`).
- **Same phantom deploy gate, now confirmed as a CLASS.** `rebuild_required:true` + commit-body "PENDING-USER-GATED". `apps/api-gateway/Dockerfile:12` builds `-o gateway ./cmd/server/` only — `cmd/sandbox` never enters the image. Two-for-two: the split-sandbox FACTORY row template sets `rebuild_required:true` on work that is categorically undeployable. Dispositioned on the row, not inherited.

### Decisions worth keeping
- **Whitespace-sensitive grep nearly produced a wrong conclusion.** `grep "targetURL string"` on the PRE-split blob returned 0 — the field was gofmt-aligned `targetURL    string`. Had I trusted that single grep I would have "disproved" the 3x anonymous struct the DoD names. Re-ran with `grep -E 'targetURL[[:space:]]+string'`: 3x pre, 1x post (inside the single named type). **On a dedup-DoD, count the construct with a whitespace-tolerant pattern or the negative result is an artefact of gofmt, not of the code.**
- **Read the gate's own tolerance arithmetic instead of quoting a peer's prose about it.** `discover.go` declares `~165L` against actual 188L; tolerance is `declared/10` min 5 → upper 181L, so it is OUTSIDE its own header tolerance and passes only via its grandfather entry in `size-lint-baseline.json` (added wholesale at `22cd084d4`, the guard's implementation commit — not a targeted rescue). Gate is GREEN either way. Recorded on the row as a caveat rather than absorbed, and NOT a rework request. Also surfaced a latent trip-wire: `exec_module.go` sits 2L under its header upper bound with no baseline entry, so a future +3L edit flips it to a new-offender FAIL.
- **Prior-art check killed a dup-mint.** Was about to mint a header-refresh row; grepped board + `backlog-detail.json` first and found `FACTORY-XZONE-size-justification-sweep` (BACKLOG, P2) whose DoD already owns exactly this ("honest size-justification header" for every remaining over-cap file). Minted nothing.
- **Declined again to mint for the missing-`review_note` class** — now 2 instances, which makes it a fleet-wide flow-compliance pattern, not an api-gateway defect. A zone-scoped row off one instance is still the wrong shape. Belongs in a flow-level pass together with the `rebuild_required` template defect.

### Evidence (raw, re-runnable)
- 1 `orch-apply.sh` pipe (`--rawfile` args + `scratchpad/signoff.jq`): Stage 0+1 PASS, conservation `task_total 781=781` / `signal_total 200=200`, 1 row stamped, head untouched. `review 237→236`, `done 11→12`. All 17 Stage-1g dangling refs pre-existing, none mine.
- Post-apply re-read from disk: 0 rows matching the id in `review[]`, 1 in `done[]` at `DONE_VERIFIED`.
- `git status --porcelain -- apps/api-gateway/` empty before AND after the sandbox run (trace `run_at` noise reverted via `git checkout -- apps/api-gateway/sandbox/traces/`).
- Timestamps all from `date -u`. Noted in passing: the prior cycle's `po_signoff_at` on the KINHDICH row is `20:37:00Z` — round `:00` seconds, ~5min ahead of the real clock at the time. Hand-typed, not measured (`feedback_hand_typed_iso_timestamps_drift_into_the_future`). Mine are measured.

### Carry-over
- **Flow-level pass owed on TWO now-confirmed classes** (both 2/2 on split-sandbox rows): implementer ships code + journal but leaves the board row noteless; and `rebuild_required:true` on `cmd/sandbox` work that no image ever builds. Neither is zone-scoped. Not yet minted anywhere.
- **`scripts/audits/po-mint-orchapply-actuator-verify.sh` still unauthored** (main.md:173) — `scripts/` is outside agent-father's commit zone, no owner.
- **The two P1 telegram-ack rows remain undispatched** (`FIX-TELEGRAM-REPORT-ACK-STATUS-STOP-RESURFACE`, `FIX-PO-MAINFLOW-ORPHANS-TELEGRAM-REPORTS-RESOLVER-SUBFLOW`). Untouched this cycle.
- **AC-3 self-verification still known-broken** (`FIX-PO-AC3-SELFVERIFY-FALSE-FAILLOUD-WHEN-PEER-SWEEPS-ORCHSTATE`) — greps own commit stat instead of HEAD tree. Asserted against HEAD tree content this cycle, per that row's guidance.
- **Single-row triage, not a full PO tick** — no channel audit, no TNB read, no signal-dashboard drain, no manual-dispatch/supervised-goahead sweep. Next full tick still owes all of those.
