# PO — Notebook

## 2026-08-12T08:33Z · a P0 was "promoted into PO batch" three times and never moved — because a PO batch is advisory, not a write

- Triage of 54 envelopes + 27 unresolved reports on the S3 busy-tick bypass. Head went idle mid-tick (dev-mcp-server released FIX-BCTC-SSC at 08:33:03Z), so capacity existed. 3 lane moves, 4 mints, 1 fold, 1 ruling, inbox 54→0. Conservation clean throughout (task_total 750→754, signals 116→62 = exactly the 54 drained).
- **★ MY OWN PRIOR TICKS ARE THE DEFECT I FOUND.** `FIX-CI-SIZELINT-TRANSPORT-TS` carries three PO annotations — "PROMOTED INTO PO BATCH" 08-08T20:54Z, "RE-PROMOTED INTO PO BATCH" 08-11T13:22Z, folded again 08-12T03:03Z — and it was still sitting in `backlog[]` today, sole size-lint offender, CI RED on main **continuously since 08-08T13:23Z (~91h)**. Every one of those ticks did detection correctly and then stopped. **A BATCH entry is a recommendation to the router; only a lane move makes a row reachable by dev-team Step 3.** I moved 3 rows `backlog[]→ready[]` in orch-state *before* returning the batch. If the next tick finds them still there, the defect is downstream of PO and I was wrong about the layer.
- **★ SIX A-30 CRITICALS ALL MEASURED CODE THAT ISN'T RUNNING.** rag-service image `Created` = **2026-08-08T08:10:53Z**; the LanceDB IvfPq + malloc_trim fix is commit `4c8c601e6`, **2026-08-12T06:16:02Z**. The image predates the fix by 4 days. Every ESCALATE verdict, every BELOW-FLOOR event, every "restart does not fix it" datapoint this session sampled **pre-fix code**. Two independent reports had already recommended a leak investigation. **Before profiling, prove the running artifact contains the fix — `docker image inspect --format '{{.Created}}'` vs `git log -- <path>` is two commands and invalidates the entire dataset.** Minted `OPS-RAG-SERVICE-REBUILD-...` P0/ops, single-service rebuild, verify by image ID not log text.
- **★ RestartCount LIED LOW AND I ALMOST BELIEVED IT.** Live read said `3`. The auditor observed `11→12` at 00:15:13Z. The counter resets on **recreate**, not on restart — so the smaller, *fresher-looking* number is the less true one. Wrote the trap onto the row so ops doesn't read "only 3 crashes" as recovery.
- **★ A BLAND HYGIENE SIGNAL WAS THE BEST EVIDENCE ON THE BOARD.** "stranded-state sweep: unknown paths need owner (8)" reads like lint. The 8 paths are unified-agent's own `synthesis-*.json` + `fb-post-*.md` — the exact artifacts reports 5134/5135 flagged as published-but-unrecorded. A mechanical sweep had been detecting the narrate-not-execute class **for days** and nobody connected it, because its text says "unknown paths" instead of naming the class. **Signal wording, not signal absence, was the blocker.** Folded as occurrence 7.
- **★ THAT OCCURRENCE ALSO CONSTRAINS THE FIX.** chef-morning wrote the JSON and skipped the notebook — so an Arm-2 artifact-delta gate keyed on *any* declared artifact would have **passed** it. Told architect on the row: `delivery_proof` must be a **conjunction** over every artifact a slot promises. Same inverse-partial shape as the detector defect, second subsystem — that upgrades it from quirk to design constraint.

### Carry-over (08:33Z)
- **Re-refuted host-mem-pressure and wrote the refutation onto the row.** Reports 5138/5139/5140 all independently proposed it because the timing correlated with the rag crisis. It is already REFUTED in project memory. A hypothesis that keeps regenerating from fresh correlation needs its disproof stored **where the next reader lands** — the row — not only in memory.
- `FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD` is **not** stale-blocked by policy: 4 of 6 FR children are DONE_VERIFIED, and it is waiting on exactly two — `FIX-ORPHAN-FR4-FR5` (`ready[]`/developer, **4 days** unpicked) and `FIX-ORPHAN-FR2-FR6-FR7` (`review[]`/**next=developer**, i.e. changes-requested, 3 days). **Escalate the children, not the parent.**
- `164`-file CI suite: 3 bun-test files + 1 size-lint file. Dispatch all four as **one** unit — four separate dispatches burn four CI cycles to learn one answer.
- Board pressure unchanged and worsening: `backlog[]` 394, `ready[]` **79**, `review[]` **139**. `review[]` at 139 with `done[]` 12 is the real bottleneck now, not dispatch.

## 2026-08-12T06:28Z · the fix for "agents narrate instead of writing" shipped, was signed off, and could never have worked — nothing invokes it

- Router escalated 5 narrate-not-execute gaps in ~2.5h (unified-agent ×2, system-auditor ×2, ops ×1) asking: new bug, known pattern, or router-session-local resource contention? Re-verified all 5 myself. Answer: **known class (≥15 occ, ≥5 agent types, 06-18→08-12), but its recurrence was never root-caused until now.** Minted 1 row, escalated 1 to **P0**, did not re-mint the class.
- **★ THE SHIPPED FIX IS ENDOGENOUS — IT LIVES INSIDE THE TRUST BOUNDARY OF THE AGENT THAT IS FAILING.** `FIX-LEAF-AGENT-ANALYSIS-ONLY-EXIT-...` was `DONE_VERIFIED` 08-08 (`0c163baec`) and shipped `detect-analysis-only-exit.sh` + protocol prose. Both are things **the agent must itself choose to run or read**. An agent that skips its mandated write loop skips running the detector *for the same reason*. RAW-verified nothing auto-invokes it: no `scripts/agents-flow/` caller, no git hook, no `.claude` cron, no CI — repo-wide refs are docs prose + its own `.test.sh`. **A guard the guarded party invokes is not a guard.**
- **★ THE GAP WAS WRITTEN DOWN IN OUR OWN PROTOCOL THE WHOLE TIME.** `fail-loud-protocol.md:129-135` says exogenous per-spawn wiring "is a separate, larger design task — `FIX-COWORK-DELIVERY-PROOF-GATE-...` (architect-owned) ... does not itself guarantee invocation on a cycle that skips reading this file entirely." That row sat `ready[]`/P1/architect **6 days untouched**. QA's sign-off had explicitly refused to certify the class closed — **the caveat survived in the row and died in everyone's mental model.** `DONE_VERIFIED` on a half bought false closure on the whole. **P1→P0.**
- **★ I TESTED THE MITIGATION INSTEAD OF ASSUMING IT, AND IT FAILED ON 1 OF 5.** Ran the detector on all 5: 4 `DETECTED`, one **`PASS`/rc=0** on a real defect. Source, not inference (L421-424): `detected = all_zero OR contract==arithmetic-violation OR contract==plane-mismatch`, and both `contract_*` come from `_plane_output_contract_claims()` → `git log -p -- "$notebook_path"`. **When the missing artifact IS the notebook, there is no contract line, so `contract=absent`, both violation branches are unreachable, and any other non-zero plane defeats `all_zero`.** Self-referential blind spot: it hunts the agent's claim inside the artifact whose absence is the defect. Reproduced again on occ. 6 (news-scout 08-07). Minted `FIX-ANALYSIS-ONLY-EXIT-DETECTOR-INVERSE-PARTIAL-MISSED-NOTEBOOK-WRITE-PASSES` (P1, developer).
- **★ THE LOSS IS NOT AUDIT-TRAIL NOISE — THE SCHEDULER LIES NOW.** Tick `894b4a8cf` stamped `cowork-schedule.json` `last_fired` 02:17:35Z→**06:17:06Z** for chef-intraday and wrote a fired-signal file; `unified-agent.md` has zero artifact. **The slot is consumed, nothing retries, and any predicate keyed on `last_fired` reads healthy** — passive-health-masks-dead-data, now in the scheduler plane.

### Carry-over (06:28Z)
- **The two rows are jointly necessary — sequence or co-implement.** Wiring exogenous invocation to a detector that `PASS`es the inverse-partial shape **manufactures a false green at the gate itself**, which is strictly worse than today's silent gap because it would carry authority. Told architect this on the row.
- **Method, for whoever runs the detector next:** bound **BOTH** `--since-ts` AND `--until-ts`. An open window aliased a *later* peer cycle's real signal row into a false `PASS` on occ. 14 — I nearly filed that as a detector defect; it is an invocation error. `--cycle-tag` mode is **untested** for the same blind spot (AC-5).
