# PO Notebook

_Last: 2026-07-29T08:48Z (A-12 CURL_ERR root cause resolved from files; signal folded as occurrence #13, SPIKE commissioned) · 0 rows minted, 4 rows amended, 1 signal closed._

## This cycle

- **The handed mechanism cite pointed at the wrong file, and that was the whole puzzle.** I was told the CURL_ERR mechanism was open because `--max-time 6` cannot explain requests that all finished under 3.5s. That 6s lives in `scripts/agents-flow/auditor-tier1-probe.sh:202` — the shell pre-gate, which probes **only :3000 and :3001** (`:479`/`:482`) and **never :4000**. A-12's api-gateway probe is a different file: `docs/agents/system-auditor/probe.sh:48`, `--max-time 3`. One of the four observed requests took **3483ms**. The mechanism was never open; it was measured against the wrong constant. Third consecutive cycle where re-deriving a handed fact from the artifact changed the answer.
- **The disproof and the failure live on different planes.** Every "it's healthy" reading — docker healthcheck `{"status":"ok"}`, nine downstreams at 0–2ms, host curl ~5ms — is a **cache hit**. `capability_prober.go:61` caches on a 60s TTL, so those probes never execute the fan-out that produces CURL_ERR. Both sides were true simultaneously. "Responds in 5ms now" is not evidence about a route that only costs anything when cold.
- **Two independently-authored 3000ms constants collide.** Auditor client budget = 3000ms (`probe.sh:48`). Gateway **per-downstream** probe budget = 3000ms (`capability_prober.go:62`). `ProbeAll` (`:96-113`) is a plain sequential `for` loop over nine downstreams — no goroutines. So the auditor's whole-request budget equals the timeout of **one of nine sequential sub-probes**, and worst case /health is ~27s. That is not a tight threshold to tune; it is a health check that cannot pass whenever anything is cold. And `:137` runs the probe outside the lock with no single-flight, so three independent pollers stampede every TTL boundary — which is exactly the shape of the evidence: two tight pairs 51ms and 839ms apart, latency **rising** across them as they contend.
- **Not a false positive, and closing it as one would have deleted a worsening regression.** Cold /health went **1.424s (07-25) → 3.483s (07-29)** — 2.4x in four days, during market hours. The detector fired truthfully; only its *naming* was wrong. The debounce that scope item 2 prescribes is still correct for the churn, but shipping it alone would have silenced the measurement.
- **"Unreachable" is a template phrase, not a claim.** It appears in **8 of the 14** historical A-12 rows, across four different services. I was triaging one bad word; it is a standing gloss the agent reproduces every time it sees CURL_ERR, and CURL_ERR is emitted for a client-side timeout on a slow-but-200 route. Fixing the row's text fixes nothing.
- **Zero mints. Every one of the four defects already had a home** — and the A-12 home was hidden by a *missing back-pointer*, not by absence. `FIX-AUDITOR-A12A20A30-FP-REEMIT-CONVERGE` is DONE and correctly signed off, but delivered 2 of 3 scope items; the brief deferred A-12 at `:227-229` and the row closed anyway. The SPIKE pointed **at** the closed row; nothing pointed back. A reader arriving from the signal sees a DONE row and concludes A-12 was fixed. Added the reverse pointer — that asymmetry, not the deferral, is what manufactures duplicate mints.
- **One misread marker produced six wrong outputs and defeated two guards.** `SKIP-dedup` gates the Telegram only; E-3 still wrote the row. The agent read it as "nothing emitted" and zeroed all four counters *plus* the anomalies headline *plus* `NEXT: clean`. `main.md:669`'s cross-check is `rows_written==0 AND signals_posted>0` — both narrated by the same agent from the same misreading, so it yields 0 and 0 and **passes**. A guard whose two inputs share one point of failure cannot see that failure. The correct count was in the marker the whole time: every success branch of `emit-audit-signal.sh` (`:51-60`) carries `id=`.
- **Ruled the gate defect a split, not a single owner.** Counter correctness → the emit path (developer, upstream, lands first). Choice of predicate *source* → the auditor flow (agent-father), because a gate reading a number its own agent authored stays one bug from the same silence even after the number is fixed. The pre-commit detector that row prescribes parses that same self-reported prose, so it had inherited the defect and now reads `.signal_queue.rows[]` instead.
- **Did not bump recurrence on the gate row.** Today is a first occurrence of the *false-input* mode, not a second of the *bypass* mode. Summing two different failures of one gate to reach the 2+ bar would be miscounting to license a stronger fix.
- **Stranded `FIX-AUDITOR-*` count unchanged at 11.** I touched four rows; three carry `next_agent` (architect / developer / agent-father). The fourth is DONE — left `next_agent` null deliberately, since giving a signed-off row a dispatch target is how BOUNDED-1 picks up closed work.

## Carry-over

- **`supervised = never dispatched` watch, 4th tick.** The SPIKE is now READY + `supervised:true` + `plan_only:true` — dispatchable only by explicit router route. It absorbed 13 occurrences over 9 days while undispatchable in BACKLOG. If it sits READY untouched next tick alongside the two A-21 children and the notebook-skill row, that is the routing defect confirmed, **not** another row.
- **Half B has no owner yet and that is the real product risk.** The api-gateway fan-out fix (`apps/api-gateway/`, specialist `dev-api-gateway`) is outside the SPIKE's original detector charter. Architect must split the zones on the way out. If the SPIKE returns a detector-only remediation, reject it — that is the same partial-delivery shape that closed the parent with A-12 undone.
- **Sequencing is load-bearing:** counters row before gate row. Reversed, the artifact-derived counter will disagree with the OUTPUT-CONTRACT line on every dedup-suppressed cycle and surface as hook noise rather than as the upstream fix it indicates.
- **A-30 row now 5d+, `recurring_bug_count=6`, missed three times** (5th consecutive carry). Threshold was reached last cycle: still BACKLOG with no architect activity next tick → **escalate to user**. Do not re-attach evidence again.
- **Verify the brief-override reaches the implementer** (2nd carry). Corrections for the A-21 split live in `po_correction_20260729T0812` **on the row**, not in the brief. Same shape as today's missing back-pointer: the correction existed and was not reachable from where the reader starts.
- Standing, 12th carry: verify my own commits carry my authorship (`git log --author`).
