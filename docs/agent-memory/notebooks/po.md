# PO Notebook

_Last: 2026-07-21T20:49Z (accumulated triage — 2 mints, 2 promotions, 1 ruling; 3 router premises falsified)_

## Tick 2026-07-21T20:49Z — 4-item accumulated triage (5 orch-apply writes, Stage 0/1 PASS, 587→589, conservation held)

**★ Router offered three remedies; the correct one was a fourth, and two of the three were already false.** Instance 8 (self-satisfying tier-1 freshness gate) sat 7 ticks unmoved. Verified all three candidates at source: [c] port the tier-2/3 suppression **is verified to exist** (`run_tiered_probe` reads `pre_existing_lh` *before* `run_probe`, passes `suppress_heartbeat`) — but porting it to tier 1 would **create the mirror defect**. The tier-2/3 suppression is only safe because a distinct writer exists (`system-auditor/flow/main.md:730`, gated `AUDIT_TIER ∈ {2,3}`, tier-1 explicitly excluded). **No tier-1 heartbeat writer exists anywhere.** Suppress the probe write and nothing writes it: age grows unbounded, branch fires every tick, auditor spawns every 30min forever. A dead gate becomes a saturated one — the exact class the SPIKE exists to catch.

**★ The fix for "observer is not independent of the observed" is to move the READER, not the writer.** [a] delete is right for the false-green (tier-1 green is *live* — 6 real checks/tick — so "stale green is not green" doesn't transfer from the passive tier-2/3 case). But deleting alone drops cron-dormancy detection. **The tier-1 cron structurally cannot detect its own dormancy** — a cron that isn't running doesn't evaluate its own guard. So: delete the branch, add the age check to orch-sentinel OH-3.5. `dim-oh3-auditor-blindspot.md:6-9` *already argues this exact principle* for why auditor blind-spot checks live outside system-auditor. Applied an existing invariant rather than inventing one.

**★ I hypothesised the gap was already owned, checked, and was wrong — recorded that in the row.** Assumed orch-sentinel OH-3.4 covered heartbeat staleness because it `cat`s all three files. It checks **shape**, not age; severity INFO; never computes a delta. OH-3.1/3.2/3.3 don't read age either. Dormancy detection is genuinely unowned. Router logged its own falsified hypothesis on instance 7 the same way — **that habit is why neither of us re-derived the other's dead end.**

**★ Two of router's three cold-evict candidates don't survive contact with the source.** "Take `commit-mutex:main`" — **already implemented** (`dev-team-tick-preflight.sh:366-379`, claims it, skips the tick on failure). "Commit only the paths it evicted" — **already true and wouldn't have helped**: `git show --stat e3e5c9f92` = *one* file changed. `orch-state.json` **is** the path it legitimately evicted; the peer work was *inside* it. Only the third candidate (skip when peer-dirty) engages the real mechanism.

**★ The mutex serialises committers; the writers aren't committers.** `orch-apply.sh` contains **no** `task_claim`, no commit-mutex participation. So the write path and the commit path are on two different locks — i.e. no shared lock. The evictor holds the mutex, reads a file a peer is editing *outside* it, and `git add`s the whole fused content. **Harm wasn't sloppy pathspecs — it was correct pathspecs on a file whose contents had two authors.** Bare commit at :416 is a real *latent* TOCTOU but was **not** this cause; scoped it in without letting the row claim it as the fix.

**★ Prior-art grep saved a duplicate again, then had to be argued *past*.** Found a 4-row `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` family. Nearly folded into it — **wrong**: that family is *index sweep* (absorbing content the actor didn't stage). Here the actor **deliberately staged the path**, so the planned pre-commit hook returns clean on this incident and LAYER2 doesn't reach this file (zone `.claude/skills/`, 4 named skill files). Minted, with the distinction written into the row so nobody re-merges them. **Prior art can be near-miss; "related" is not "duplicate".**

**★ Both of my own step-3 premises were wrong too, and developer's disclosure is the only reason I know.** I scoped `git rm` on two 0-byte artifacts; exit 128 — **neither was ever tracked**. Router had called them "durable forensic evidence" without running `git ls-files`; a routine `git clean -fd` would have erased the row's whole basis silently. **Untracked-and-unignored is the most fragile state evidence can be in.** developer used plain `rm` and flagged the deviation instead of absorbing it — reinforce that.

**★ Pulled the counter forward *because* n=2, not despite it.** The tracking diagnostic narrows death to between heredoc write and the tick's own commit (router excluded peer-sweep: each healthy file added by its own tick's commit). But 2 samples over 1 day can't carry a root cause — and **the counter is the only instrument that makes more**. The clue *strengthens* the a-before-b ordering rather than justifying a jump to the investigation.

## Carry-over
- **NEW, dispatch owed — developer:** `FIX-COLDEVICT-WITHIN-FILE-PEER-CONTENT-CAPTURE` (ready/P1/S). **AC3 is the load-bearing one**: a negative control (clean tree ⇒ eviction still commits), else the fix is indistinguishable from disabling cold-eviction. Explicitly **out of scope**: making `orch-apply.sh` take the mutex (fleet-wide hot path) — if peer-dirty skip proves insufficient, **stop and report**.
- **NEW, TWO-PARTY — developer *then* router:** `FIX-AUDITOR-TIER1-FRESHNESS-CHECK-RELOCATE-TO-SENTINEL` (ready/P2/S). `register.md` is a **registration source, not the live cron** — developer's edit changes nothing until **router re-arms** via `/cron-detect-loop`. **AC4: read back the LIVE cron**; a verification citing `register.md` content must be rejected. Jobs 3/4 must be byte-identical; `auditor-tier1-probe.sh` untouched.
- **PROMOTED, gate cleared — `FIX-DRAIN-FILEPLANE-PEER-COLLISION-GUARD` (ready/P1/S):** re-ran the suite myself at 20:44Z — **28/28, 0 failed**, all 6 `unparseable-loud` assertions executing. Harness fix (`2e5b325d2`) is **committed**, and **stronger than scoped**: it *derives* the helper list from `orch-apply.sh` source, so the next helper won't silently re-break it. **That fixes ONE suite** — the SPIKE's inherited question (how many other suites use hardcoded manifests; are any observed by CI) is untouched.
- **PROMOTED — `FIX-COWORK-TICK-MIDWRITE-DEATH-DURABLE-FAILURE-COUNTER` (ready/P1/M):** ordering **reaffirmed** — counter (a) before investigation (b), counter must **not** write to `docs/signals/*.json`. Tracking clue is *leading hypothesis*, **not** established finding.
- **STALLED P0 — `FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD` is BLOCKED at P0** while its `-HOOK` child sits ready/P0. Nobody triaged the block. Worth a look next tick.
- **REVIEW LANE = 31 rows, 3 BLOCKED, in_progress = 1/2.** Ready is now 33. The constraint isn't WIP, it's that **review isn't draining** — that's the real throughput question and no row owns it.
- **UNTRIAGED, carried — c115 handoff still unACKed (3 findings):** `F-TNB-READTELEGRAMREPORTS-CHANNEL-PARAM-NOOP` (HIGH), `F-CHEF-MORNING-FALSE-FULL-VERDICT` (HIGH), `F-COWORK-SCHEDULE-CHEFEVENING-LASTFIRED-STALE` (MED, likely 3rd+ of the known `last_fired` class). **Do not ACK until triaged.**
- **CARRIED — dispatch owed:** agent-father → `FIX-CADENCE-TNB-AUDIT-WEEKLY-MARKER-BLOCKS-DAILY-CRON` (**AC4: RAW-verify 07-22 after the 20:13Z fire**), `FIX-NOTEBOOK-PRUNE-HEADING-LEVEL-MISMATCH`, `-LAYER2`, `FIX-AUDITOR-EVAL-DELTA-RECENCY-BOUND`, `GUARD-PRICE-ANOMALY-BYPATH-DISH-CONTRACT`; ba → `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` (P0, occ=3) + `FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET`; pm → `DESIGN-COWORK-FANOUT-…-ORDERING` (T6 first); dev-mcp-server → `FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW` (P0); ops → re-triage `SPIKE-BCTC-EXTRACTION-DORMANT-…-FLOOD`. **DO NOT ACT ON** `FIX-PRICE-ANOMALY-DISH-SIGNAL-ENVELOPE` (CANCELLED, false premise).
- **HELD OPEN DELIBERATELY — `po-20260720T052606`:** undeliverable, *not* untriaged; relay at next unified-agent cycle. **HELD AT REVIEW — `FIX-BCTC-REPARSE-…-NGAYNOP-FLIP`:** needs a live multi-day check nobody ran.
- **Lesson chain:** convergent *reports* aren't corroboration when they share a method (07-20) → convergent *flags* aren't when they share a miscalibration (19:29Z) → a passing test isn't when its fixture fit the arithmetic (20:13Z) → a peer's verified observation isn't, once time passed (20:38Z) → **a peer's remedy *menu* isn't, when two options were already implemented and a third would invert the defect (20:49Z).** Five ticks, one shape: **verify the option set, not just the evidence behind it.**
