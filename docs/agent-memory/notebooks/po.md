# PO Notebook

_Last: 2026-08-06T17:38Z (review-lane secondary-drain: FACTORY-MACRO signed off DONE_VERIFIED after a 13-day strand; the row that caused the strand lane-fixed out of a no-lane hole). Prior PO cycle 16:03Z (P0 stock-price/kinh-dich outage resolved) preserved at `HEAD~`._

## 2026-08-06T17:38Z · A row held on "user-gated rebuild" was waiting for something that had already happened

- **Both halves of the blocker were false, and each was cheap to falsify.** `FACTORY-MACRO-split-or-justify-over-cap` sat in `review[]` from 2026-07-24 until today on a `review_note` reading *"rebuild-verify PENDING-USER-GATED"*. (a) There is no user gate — `feedback_po_deploy_rebuild_full_autonomy_no_user_gate` (user directive, PERMANENT) retires that exact phrase from board rows; single-service rebuild is ops-executed, no gate. (b) There was nothing left to wait for: the macro-indicators image was rebuilt **2026-07-29T23:58:16Z**, five days *after* commit `a87079574` (07-24T11:07:04Z), incidentally. Thirteen days of strand for a gate that self-satisfied on day five and a permission nobody ever needed.
- **Build-timestamp > commit-timestamp is necessary and not sufficient — check the binary.** A newer image only proves *a* build ran, not that it built *this* source. `grep -a` on the running `/app/server` returns both post-split markers `parsers_vmt_sbv_policy_rates.go` and `cmd/server/adapters.go` — files that exist only in `a87079574`. That is what turned "probably deployed" into evidence; the timestamp alone would have been the same shape of inference the review_note was itself built on.
- **Re-ran the DoD instead of reading it.** The `review_note` asserted build/vet/test green — as of 07-24. Re-run at HEAD today: `go build` rc=0, `go vet` rc=0, `go test ./...` 12/12 packages ok, and `POST :5004/snapshot` → 200 in 0.12s with **both** split halves visibly serving (gold 4300.10 tier-1 from `SJCGoldFXAdapter`; carry `vndDepositRate=5.00% source_tier=2` from the SBV path that was split out). A 13-day-old green claim is a claim, not a result.
- **The row that caused this strand was itself unreachable.** Prior-art grep found `FIX-DEVTEAM-REBUILD-REQUIRED-MARKER-NO-CONSUMER` already owns the mechanism exactly (`rebuild_required` marker that no gate reads) — so I did **not** mint a sibling. But it had sat 5 days in `backlog[]` with `next_agent=agent-father` and no `plan_only`: predicates run live returned `non_dev_unrouted=true, supervised=false, plan_only=false` — BOUNDED-1 gates it out, SLS needs both flags true. That is the literal NO-LANE trap its own `po/flow/zone-routing.md` Step A2 names. Moved to `ready[]`/`READY`, P2→P1 on second occurrence. Post-move RLC predicates: `resolved_lane=agent-father, priority_rank=1, deps=true, epic_wrapper=false`.
- **Occurrence 2, same shape as PDF-AVAIL-02-FIX.** That one cost 7 days of A-20 signal spam re-detecting an already-fixed bug; this one cost 13 days of a review row. Both are a *fix committed but not known-deployed*, held by a marker no gate consumes. Folded into that row's AC-2: the ops rebuild-satisfied stamp must also detect the **reverse** case — a rebuild that already landed incidentally — so no row is ever held on a gate that is de-facto satisfied.

### Evidence (all raw, re-runnable)
- Image `sha256:9c9721b596c2` created `2026-07-29T23:58:16Z`; container `macro-indicators-1` runs that exact image ID, `healthy` 7d; `/health` → `{status:ok,service:macro-indicators,port:5004}`.
- `git merge-base --is-ancestor a87079574 HEAD` → true. Only 2 later macro-indicators commits (`e02e20192` header, `9431a7cdd` CI gate) — both non-behavioral, so no rebuild is owed by this row.
- Size DoD: `adapters_vmt_sjc_fx.go` 504→174L, new `parsers_vmt_sbv_policy_rates.go` 372L, `main.go` 394→230L + `adapters.go` 291L; size-justification headers present in all 7 files checked.
- Board: `review[]` 242→241, `done_verified[]` +1, `backlog[]` 360→359, `ready[]` 59→60; conservation check `task_total 790=790`.

### Carry-over
- **Verify-by-execution paid twice this cycle.** Both dispositions were decided by running predicates/probes, never by reading the row's own prose. The `review_note` is the *claimant*, not the evidence — second cycle running where the row's own narrative was the thing that was wrong.
- **The review lane is still ~241 deep and this is what is in it.** A 13-day-old row whose blocker had evaporated on day 5 stayed invisible until a secondary-drain sweep happened to route it here. Rows held on *external preconditions* need a re-check trigger; nothing ever re-reads a `PENDING-*` marker.
- **Still withheld, still not forgotten** (no full triage ran this tick either): `FIX-RAG-EMBEDDER-IDLE-UNLOAD-PATH` (P1), `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST` (P0/READY since 08-05T16:51Z). Third cycle carried. If still un-dispatched at next full triage, **the inaction is the finding**.
- **Awaiting independent QA sign-off** (do not close on my say-so — I was the executor): `FIX-MARKETDB-WAL-SEQUENCE-STEPS-2-4-NO-OWNER` + `FIX-STOCKPRICE-PRICEHISTORY-RO-WAL-DSN-SWALLOWED-EMPTY-KILLS-KINHDICH`, both in `qa[]`.
- **Unminted class from last cycle, sharpened by today:** nothing structurally stops a deploy row closing on a notebook-only commit — and nothing stops a row *staying open* after its deploy silently landed. Same missing artifact, both directions.
- **Unchanged debt:** `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` (P1) still `root_cause`+`ac` null · 4 backlog rows still carry prose-in-`depends_on` (permanent Stage-1g noise).
