# PO Notebook

_Last: 2026-08-04T19:53Z (cadence-rationalization greenlight → 7-row batch mint). Journal: `sprint-CADENCE-RATIONALIZATION-20260804-po.md` STEP po-S1…S4. Prior cycle 2026-08-01T06:32Z (BCTC alert-hint triage, `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-5.md` po-S131) — its live carry-over folded in below._

## This cycle

- **Routed a user-greenlit brief into 7 `ready[]` rows, not 9.** `docs/architecture-briefs/2026-08-04-cadence-rationalization.md` §8 items 1–7 + §9 rows 13/4. Folded §8-item-2 with §9-row-13 (both edit `.claude/commands/crons/cron-db-data-integrity.md` — two rows would race on one file for zero traceability gain) and §8-item-3 with §8-item-5 (identical diff-gate pattern; the brief itself says batch them). IDs `CADRAT-1..7`, shared `sprint: CADENCE-RATIONALIZATION-20260804`, greppable as one unit. `ready[]` 51→58, `task_total` 770→777.
- **The prior-art grep paid for itself immediately.** Live backlog row `FIX-COWORK-CADENCE-DANGLING-POLICY-ID` already owns the *same file* (`docs/data/cadence-policy.json`) with a **contradictory** spec: it demands `alert-commander-market: interval_minutes 15` / `critical: 240`; the greenlit brief demands all 10 rows `null + _cron_fallback: true`. Whichever landed second would have silently overwritten the first. Attached a supersede note pinning which spec governs, *and* enumerating what still survives in the old row (fail-loud policy_id resolution, the standing dangling-ref check, the 2026-07-25 value-axis domain check) so nobody closes it as fully covered.
- **Chose behavior-preservation over the "better" number, because the fleet is unobservable.** `15/240` isn't wrong on its face — it's an unmeasured 16x cadence change shipped into a fully dormant cron fleet with zero armed jobs. `null + _cron_fallback: true` closes the config hole and changes nothing live. The adaptive rate can be a deliberate row later, with a measurement.
- **Resolved the brief's one open sub-decision rather than passing it down.** §8 item 4 left extend-`cron-detect-loop` vs. new-skill to the implementer. Picked **new `cron-standalone-team` skill**: `cron-detect-loop/SKILL.md:14-17` carries an explicit size-justification that its hot path (~46–48 of 48 dev-team ticks/day) must never load `register.md`; going 4→8 `CronList` conditions taxes that path 48x/day for 4 crons outside the loop. That's a documented-rationale regression on top of the naming smell the brief already flagged.
- **Corrected the brief's own location gap on §9 row 4.** The brief named the schedule but not the file. `news-scout-sentiment` is a **cowork slot**, not a `CronCreate` cron — it lives in `docs/data/cowork-schedule.json:271` plus a mirror at `docs/agents/news-scout/init.md:94`. Two consequences written into the row: (a) editing it arms nothing, and (b) `cowork-schedule.json` is `_maintained_by: agent-father (via architect brief only)` — the gate is satisfied *because* this brief is the architect brief, so `next_agent: agent-father` with the citation.
- **Made NO-RE-ARM a gradeable AC, not a sprint-level aside.** Verbatim constraint block in all 7 `note` fields, plus hard ACs: CADRAT-2 AC-10 (grep-proof zero `CronCreate` in the diff) and CADRAT-4 AC-6/7 (authoring the CronCreate calls into `register.md` is the deliverable; *executing* them is the forbidden side effect). CADRAT-4's whole output is a file full of `CronCreate` calls — precedent `feedback_chef_dryrun_publishes` is an agent that executed a publish path it was authoring "to verify".

## Carry-over

- **A row-scoped implementer does not read sprint-scoped prohibitions.** If a constraint must hold, it has to be an AC on the row whose deliverable most tempts violating it. Ambient notes are read by nobody under load.
- **Two board rows editing one file with different values is a silent-overwrite bug, and only the prior-art grep finds it.** Neither row is wrong in isolation; the defect only exists in the pair. Extends `feedback_file_prior_art_check_before_minting_row` — the check is not just "does a row exist", it's "does it specify a *different value* for the same field".
- **A dormant system is the worst place to ship an unmeasured improvement.** No observer, no feedback, no rollback signal. Prefer the strict no-op that closes the hole; defer the behavior change to when someone is watching.
- **A brief can be implementation-ready and still have a location gap.** Every §8/§9 file+line in this one verified clean against the live tree except §9 row 4, which named no file at all. Verify anchors before minting — the row inherits whatever the brief got wrong.
- **Repo convention beats brief spec on scaffolding.** Every probe in `scripts/agents-flow/` ships a paired `.test.sh` (tier1, cowork-tick, dev-team-tick). The brief never mentioned one for `db-integrity-probe.sh`; the row makes it mandatory (AC-7), which also happens to be exactly the QA bar the user set.
- **When an alert ships a remediation hint, the hint is code and needs the same proof as the code** (prior cycle). Cheap check: can the rule family the message names actually *return* the number the message carries? Related: **a unique magic constant is a free fingerprint** — but one sharing a literal with a documented-but-dead default is worse than no sentinel.
- **A tool answering "no data" can mean "data exists and I am refusing to serve it"** (prior cycle). Serving-path silence is not a storage fact. Extends `feedback_empty_read_is_not_evidence…`.
- **Board saturation unchanged and binding.** backlog **350** / review **259** / ready **58** / in_progress 1 / qa **0**. `task_total` **777**. 214 review rows carry `next_agent: qa` against an empty qa lane — the drain, not priority, is the live constraint. These 7 new rows enter a board that already cannot drain; expect them to need explicit dispatch rather than BOUNDED-1 pickup.
- **Still owed (ageing).** 5 `review[]` rows with `next_agent: po`, incl. `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (**P0**). 38 manual-dispatch candidates unswept. 25 P1 backlog rows resolve to an empty `next_agent` even after detail fallback. **Pre-checks skipped this tick** (supervised-goahead, manual-dispatch, channel-audit, TNB) — narrow directed spawn, not a triage tick; last full sweep 2026-08-01T04:38Z and now 3 days stale.
- **Open, unowned:** §8 item 9 (re-arm the fleet) is deliberately deferred pending user greenlight after CADRAT-1..7 land and verify. §9's two flagged-but-unverified rows (`chef-evening` weekend fires, `market-watcher-eod` 8h-post-close) remain open questions with no row — informational only, per user scope.
