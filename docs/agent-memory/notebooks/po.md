# PO — Notebook

## 2026-08-07T05:45Z · dev-team 05:07Z tick — architect-brief sign-off + 6 signals, 3 mints

### What actually happened
- **`FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING`: GO with 3 binding amendments.** Advanced `architect → pm`, `po_goahead_20260807T054500`, `supervised`/`plan_only` preserved. `.head` was stale at `next_agent=architect` (04:33:33Z) — resynced. Two `orch-apply.sh` pipes, both PASS, conservation OK (`task_total` 766→769).
- **6 signals + 1 telegram report.** 3 mints, 2 folds, 3 informational skips. Telegram 4486 resolved `duplicate` + deleted.
- Mints: `FIX-CHEF-INTRADAY-MARKER-KEY-UTC-HOUR-BASIS-MIGRATION` (BLOCKED), `FIX-CI-SIZELINT-CHECKFOREIGNFLOWGAP-NEW-OFFENDER-181L`, `FIX-CHEFDISH-STEP8E-OWNPATHS-EXCLUDES-SYNTHESIS-JSON`.

### Decisions worth keeping
- **Both options an escalation offers you can be wrong — check whether the thing is already owned.** Architect asked me to pick "monitor FR-4" vs "commission a `task_claim` lock for FR-4". Neither: a three-row chain already owns it (`GUARD-NOTEBOOK-CONCURRENT-EDIT-COLLISION-DATA-LOSS` → `FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` → `FIX-NOTEBOOK-WRITE-AC7-SKILL`), and AC7-SKILL already specifies the exact `task_claim(task_kind='notebook-write', ttl=120s)` mechanism option 2 proposed. Descoped and folded.
- **A per-file null result is not a per-mechanism null result.** Architect's RAW-verify (3 tnb cycles, zero repeat `c<NNN>`) was scoped to `tran-ngoc-bau.md`. The mechanism recurred *inside that window* on `qa.md` (08-06, cycle-522/523, occurrence 2). Option 1's premise was false on a plane nobody looked at.
- **"Monitor" needs a named monitor that can actually see the failure.** Checked `pre-commit:310-382`: the AC-2a guard is fleet-scoped and does cover tnb — but an authorized whole-section DROP is legal to it, which is the exact shape this race produces. Would have monitored nothing.
- **"Mint it later once X ships" is a consumer with no producer.** Architect's EC-2 Phase-2 deferral. Same class that idled `FIX-WF2-SUPERVISED-HOLD-NO-PO-SIDE-GOAHEAD-PRODUCER` and `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` (P0, 6+ days). Confirmed the split, overrode the timing: minted now, `status=BLOCKED`, `depends:["FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR"]` — Stage 1g resolved it, no new dangle.
- **The fix reintroduced the bug at the day boundary.** FR-1's fallback (the brief's own ACTIVE day-one path) is `WINDOW_KEY = <UTC date of run> + CRON_HOUR`. `bctc-analyst-slot-4` is `0 0 * * *` — a fire minutes early keys to *yesterday's* slot-4 window and clobbers a 24h-old file; a slot-3 run delayed or re-run past midnight keys to a *future* window. Amendment: snap to the NEAREST `CRON_HOUR:00Z` occurrence. "Nearest", not "most recent at-or-before" — only nearest is correct in both the early and late directions. +2 unit cases.
- **Amend, don't bounce, when the correction fits in a sentence.** The FR-1 gap is real but fully specifiable; a return-to-architect costs a cycle and changes nothing else. Same handling the BA spec got.
- **ci_red: read the failing file before dedup, and check provenance separately.** `gh run view 31150664913 --log-failed` named `checkForeignFlowGap.ts` (181L, new-offender) at source. The relay said the offender predates HEAD `83bb4359e` — confirmed independently: sole commit `147ce3a68` @ 03:50:06Z; CI red on 4 consecutive runs since 04:56Z. No file-scoped row existed (5 sibling `FIX-CI-SIZELINT-*` rows are all other paths) ⇒ mint, per the anti-amnesty backstop.

### Carry-over
- **`FIX-NOTEBOOK-WRITE-TASK-KIND-ENUM-EXTENSION` (backlog, dev-mcp-server) is the whole notebook-concurrency chain's critical path** — additive Zod enum value + AC-12 concurrent-writer harness, cheapest of the three, and `AC7-SKILL` stays BLOCKED until it lands. Now carrying FR-4's ask too. Not mine to dispatch; escalate if still untouched next tick.
- **Tripwire recorded on the new EC-2 row:** any change to `chef-intraday`'s cron in `cowork-schedule.json` must unblock and dispatch `FIX-CHEF-INTRADAY-MARKER-KEY-UTC-HOUR-BASIS-MIGRATION` first — Phase 1's `VN_HOUR` basis is safe only because `13 2-8 * * 1-5` stays clear of 17:00 UTC.
- **Still owed by me** (unchanged, 2nd tick): mint a FIX for `bctc_signal_*` / `unified-agent-synthesis-*` field-schema instability once the filename fix ships.
- **`OPS-RAG-SERVICE-REBUILD-STALE-IMAGE-PREDATES-IDLE-UNLOAD-FIX` still open** ⇒ A-30 keeps measuring the pre-fix binary. Two more re-fires this tick (signal row + telegram 4486), both closed as duplicates. Do not mint per occurrence; escalate the ops row instead.
- **New, from the chef-dish mint:** an uncommitted synthesis JSON is unrecoverable once the next cycle clobbers the path — `FIX-CHEFDISH-STEP8E-OWNPATHS-EXCLUDES-SYNTHESIS-JSON` and the filename-keying row compound. Whoever ships either should read both.
