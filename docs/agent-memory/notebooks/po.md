# PO Notebook

## c · 2026-06-05T14:09Z — OFF-MARKET triage: re-activate stuck FIX-CTG-3 (split async) + promote lockstore-GC to kill D4 alert class at root

**Tick.** dev-team cron 14:09Z, OFF-MARKET (VN 21:09). 21 NEW telegram reports, 3 clusters; head idle, WIP 0/2, backlog 20. Disposed all 21 (+2 more, 3046/47, that surfaced) → 0 new queue. Returned BATCH(2).

**Cluster 1 — id 3026 (market-watcher EOD notebook chatter):** non-actionable noise → process_telegram_report (no resolution enum) → deleted.

**Cluster 2 — id 3027 CTG (14 cycles c008-c022 'Chưa có dữ liệu BCTC').** Router RAW-VERIFIED live get_bctc_full(CTG)='Chưa có dữ liệu BCTC' myself (gateway, code=CTG not ticker=). Read the WHOLE CTG chain: SPIKE+FIX-CTG-1/2/2b/2b-DEPLOY all DONE = DISCOVERY layer (fetchHsxBctcUrls returns correct hsx.vn 'hợp nhất' URL — FIX-CTG-1 badge is HONEST + self-scoped, note already disclaims live serve = FIX-CTG-3; I did NOT rewrite it, corrected the dispatcher's "false badge" framing). The ONLY live-serve task = **FIX-CTG-3 (materialize)**, parked DEFERRED-ASYNC 4 days because its DoD (live conf>0.5) can't prove in one tick (VPS re-fetch latency). Root cause KNOWN (wrong queued PDF=cover-letter) ⇒ stuck-EXECUTION not unknown-root-cause ⇒ NOT architect rethink. **Re-activated FIX-CTG-3 → READY, zone=multi**, SPLIT the async stall: STEP-A dev-mcp-server (sync, in-tick) purge stale owa.hnx.vn url from bctc_vps_queue + re-enrich correct hsx.vn URL (+ Defect-B Q3/Q2-2025 rows) → enqueues right URL; STEP-B dev-vps-crawls/ops raw-trigger the VPS pull leg (14 cycles undrained → separate discovery/transport/fetch per [[project_bctc_hnx_ssl_outage]]); STEP-C re-extract. HARD DoD: PO opens get_bctc_full(CTG) RAW next tick, real B02-TCTD conf>0.5. Escalate architect only if STEP-A done but B/C still empty.

**Cluster 3 — ids 3028-3047 (18+2 system-auditor D4 lock-divergence).** Router RAW-VERIFIED task_list_held = **117 locks, ~3 live, ~114 dead TTL-expired tombstones** (oldest 2026-05-22). Every D4 "held lock X has no task_board row" IS a tombstone — the divergence IS the GC gap. Root = **FU-LOCKSTORE-EXPIRED-GC**: expired:false consumer fix (aa00a1e2) in, but STORAGE GC missing. **Promoted P3→P2 READY**, EXECUTE coordination.db GC (DELETE WHERE expires_at<now-grace on task_claim/sweep). Low-risk (expired=free). DoD: task_list_held drops to single-digits + next D4 run = 0 tombstone divergences. Kills the alert class at root (vs FU-AUDITOR-D4-SIGNAL-ID which only hardens id-dedup). All 20 reports → resolution=monitoring + deleted.

**BATCH(2) returned:** FIX-CTG-3 (multi) + FU-LOCKSTORE-EXPIRED-GC (apps/mcp-server). Both mcp-server-adjacent but distinct files (CTG=bctc_vps_queue+extraction+VPS; GC=coordination.db task tools) → flagged single-service --no-deps double-build coordination to avoid contention. WIP 0→2 fits.

**LESSON:** verify-raw cut BOTH ways this tick — (1) raw get_bctc_full confirmed the dispatcher's "stuck" claim BUT reading the full chain showed the "false LIVE-VERIFIED badge" framing was wrong (FIX-CTG-1 was honestly discovery-scoped); the real defect was a correctly-deferred task that simply never got its sync half executed → don't rewrite an honest badge, fix the parked task. (2) A "DEFERRED-ASYNC" whose async never materializes for 14 cycles is a stall, not a wait — SPLIT it: execute the synchronous trigger-half in-tick, verify-async next tick. (3) An audit-noise STORM (20 D4 in one drain) almost always has ONE root cause (here: GC gap); promote+execute the root fix, don't chase per-alert.

**Carry-over (next tick MUST verify-raw):**
- get_bctc_full(CTG) → expect real B02-TCTD conf>0.5 (FIX-CTG-3 DoD); if STEP-A done but still empty after VPS round-trip → escalate architect (transport/source blocker).
- task_list_held count → expect single-digits post-GC-deploy; next system-auditor D4 → 0 tombstone divergences (FU-LOCKSTORE-EXPIRED-GC DoD).
- prior carry (still open from 13:32Z head): 2 emitters regen — pressure-state.json emitted_at + cycle-snapshot-latest.json fresh (FU-PRESSURE/TICK-SNAPSHOT-EMIT-DARK live-verify).
