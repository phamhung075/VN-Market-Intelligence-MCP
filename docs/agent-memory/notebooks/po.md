# PO Notebook

_Last: 2026-07-29T11:42Z (rag ACK scope mismatch → allocation mechanism · 4 carried cowork signals) · 2 minted, 2 priorities raised, 11 rows annotated, 1 ledger corrected, 2 self-corrections._

## This cycle

- **The scope mismatch was real, and the thing under it was worse.** The router asked whether the ACK's `tracked_by` was too narrow. It was — but `tracked_by` is read by **nothing**. `auditor-tier1-probe.sh:275` extracts `.acked_memory[].container` and stops. The STALENESS RULE both the ledger and the probe promise ("remove once `tracked_by` reaches DONE_VERIFIED") is enforced by no code, no cron, no test. A suppression whose only safety property is documentation is not narrowed by editing the documentation.
- **Binding the ACK to a time-gated row made it un-expirable by construction.** `RAG-FTS-BUILD-MEMORY-BOUND` is blocked on corpus growth to ~56k rows, projected ~2026-08-16 — so even a *live* `tracked_by` could not expire for ≥18 days. Re-pointed to `FU-RAG-DEPLOY-MEMORY`.
- **The falsification was already inside the row cited as the cause.** That row's own `qa_result.idle_baseline_flag` (2026-07-15): "idle mem already ~92% of 768m with ZERO FTS activity". The ledger was seeded 2026-07-25 naming it as root cause. Same file, opposite claim, ten days apart. Prior-art checking is not just dedup — read what the cited row *says*.
- **"Narrow the ACK" was not an available operation.** `:302-307` is one `pct >= WARN_PCT` test; an ACK'd container is suppressed identically at 85.01% and 99.99%. Only two states exist: suppress-everything, or remove and re-churn the auditor 48×/day. Minted the row that makes the middle expressible — the parent had already deferred exactly this *"if the loop proves the percentage view insufficient."*
- **I made the 2-sample error I had just flagged the router for, then made a second one.** Called memory "flat" on two samples; it was 756.6 MiB fifteen minutes later. Then wrote a note *describing* a P3→P2 bump without mutating the field — narration without actuation, the same shape as the dead `tracked_by`. Caught only by reading the field back after apply. **A jq transform that adds a key is not evidence it changed a sibling key.**
- **The allocation question had a measurable answer, and the brief's window was undercounted.** I was told 10.8 MiB rose on 2 `/index` calls. The full log for that window has **6 lines**, and the rise lands on a 2-second burst at 11:29:50–52Z containing 2 `/index` **and 2 `/search`**. The read path was omitted, and it is the likelier allocator.
- **Request-proportional leak is refuted by asymmetry.** ~190 `/index` over 31 min → **−4.1 MiB**. 2 index + 2 search over 15 min → **+10.8 MiB**. A per-request leak would have killed the container during the 190-call burst.
- **The structural fact nobody had measured: the dataset is bigger than the cap.** `data/live/lancedb` = **785 MiB against a 768 MiB limit** — 3137 fragments / 596 MiB of data, 145 MiB of indices, 44 MiB of version+txn metadata. A cgroup whose working set exceeds its limit fills to the limit by construction. That, not a leak, is the parsimonious reading of a 94–98% plateau that has held since 2026-07-15.

## Carry-over

- **The decisive test is un-run and I did not run it:** the cgroup `memory.stat` **anon vs file** split. If file-backed dominates, the memory is reclaimable, there is no death clock, and the ~0.51 MiB/min extrapolation is invalid; if `anon` climbs, the CLEAN-EXIT row is under-scoped. `docker exec` into rag allocates in the target cgroup and is how RestartCount reached 22 today — I have no non-exec grant. **Named on the row as step one for whoever picks it up.** Do not size a cap change before it.
- **Extrapolation is not supported by the data I do have:** 6 sampler reads flat at 756.6 MiB across 11:38–11:41Z, RestartCount unmoved at 22 for 90 minutes. Step-driven by read traffic, not a ramp. Sampler persisted at `scripts/audits/container-memory-sampler.sh` so the next agent stops eyeballing 3 samples.
- **`FIX-RAG-COMPACTION-DISK-AMPLIFICATION` P3→P2** — 3137 fragments for ~11.2k rows is not tidy-up: fragment count multiplies read cost, which is what pins the cgroup. It and the CLEAN-EXIT row are one failure seen from both ends (the counter defect is *why* the fragments accumulated); neither row said so.
- **`FIX-COMMIT-PATH-PEER-INDEX-SWEEP-GUARD-LAYER2` P1→P0; its "4 call sites" is a floor, not the spec.** 8 agent commit paths swept in one day and system-auditor bypassed a mandated pathspec script it already has. Do NOT bump the superseded `FIX-AUDITOR-COMMIT-NONEXPLICIT-PATHSPEC` — disposition recorded on it.
- **`FIX-DEVTEAM-BACKGROUND-SPAWN-LOCK-RELEASED-AT-SPAWN-NOT-COMPLETION` reproduced 3× today while in REVIEW.** qa must check an observed dispatch's **claim/release order**, not flow-doc prose.
- **`FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY` cannot pass QA from source** — running image still emits `host_headroom_mb`, pinning cowork fan-out at MAX_PARALLEL=2 since 07-28. Gate on the deployed emit. Rebuild user-gated, not dispatched.
- **rag-service stays invisible to the fleet's only memory detector until the headroom-floor row ships.** 11.4 MiB free, ACK'd, ALL_GREEN — the accepted cost of not re-churning the auditor, now on the record rather than implicit.
- **A-30 row 5d+, `recurring_bug_count=6`, 8th carry.** Still BACKLOG, no architect activity → **escalate to user next tick**. Do not re-attach evidence again.
- Standing, 15th carry: verify my own commits carry my authorship (`git log --author`).
