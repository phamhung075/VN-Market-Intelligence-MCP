# PO — Notebook

## 2026-08-22T17:22Z · I re-ran both suspect commits through the real actuator. The script was never the problem.

- **Trigger:** router-dispatched triage of agent-father's drain-pickup (commit `46aa66161`) on `FIX-NOTEBOOK-COMPOSE-REWRITES-RETAINED-PRIOR-SECTIONS`, with an explicit instruction to independently verify its finding #3 rather than trust the summary. **Output: 3 `orch-apply.sh` pipes + 2 cold-store writes — occurrence 7 recorded, sibling pilot's Success Signals re-measured FAIL, 1 P0 row minted to `ready[]` (`FIX-AUDITOR-DATA-TIER-NOTEBOOK-WRITE-PATH-UNWIRED`, next_agent=developer). task_total 709→710.**
- **★ COUNTERFACTUAL REPLAY IS THE ONLY TEST THAT SETTLES "DID THE ACTUATOR RUN?".** Reading a diff tells you what landed, never what *should* have. I checked out each commit's true parent blob and its new section, then ran the real `scripts/notebook-compose.sh` with `main.md:1099`'s own arguments (`3 150`). `22039783e` → script yields **3 sections/73L**, commit landed **5 sections/212L**. `f25dc3d27` → script yields **3 sections/172L, explicitly retaining `## c103` and `## d4-auto`**, commit landed **1 section/179L**. The actuator does the right thing on both failing inputs. **Finding #3 CONFIRMED: 2 sections the script provably keeps from byte-identical inputs were destroyed.** Adopt this shape for any "was the deterministic path used?" question.
- **★ AGENT-FATHER'S ROOT CAUSE WAS RIGHT ABOUT THE FILE AND WRONG ABOUT THE REASON — WHICH WOULD HAVE COST THE FIX.** It reported `cron-db-data-integrity.md` as "a STANDALONE cron prompt" in "a file that dispatcher never reaches", and on that basis **declined the one in-zone edit that mattered**. That cron's body L59 reads verbatim `Launch subagent (subagent_type=system-auditor). Read and execute docs/agents/system-auditor/flow/main.md` + `AUDIT_TIER=DATA`. **The dispatcher IS reached on every DATA fire.** The hole is inside it: `main.md` L138-143 lists TIER=1/2/3/4/5, each ending `→ notebook (gated) → RETURN`, and **has no TIER=DATA row**; L130 parks DATA as "not yet a real branch". One unbound tier is enough to keep the whole pilot red.
- **★ SS-2 PASSED AND THAT IS EXACTLY THE TRAP.** The pilot gate reads `wc -l ≤ 200` → 179L → PASS. The file is under cap **because** 4 sections were destroyed to pay for a 177L section that is itself 27L over its own 150L cap. **A cap check that a data-loss event makes greener is not a health signal.** The load-bearing signal was SS-4: `git log --grep=notebook-compose` on that notebook returns **zero commits in its entire history** — the actuator has never executed once in the 8 days since it was wired.
- **★ THE GUARDS DIDN'T MISS THIS; THEY CANNOT SEE IT.** `_check_notebook_immutability` only diffs bodies of headings *retained across* the boundary — a pure append mutates none, and a whole-section drop is AC-2a **authorized** mutation (1). `notebook-auto-prune.sh` gates only on line/byte cap and early-exits under it, never counting sections. Nothing checks retention COUNT at commit time. **So AC(4) must be a section-COUNT + per-section-cap commit gate, not another byte-hash check.** Recorded on the row so the next implementer doesn't build the wrong guard.
- **Routing call:** sent to `developer`, not agent-father. agent-father was **correct** that `.claude/commands/crons/` is outside its zone — but `docs/agents/system-auditor/flow/main.md` (the *primary* fix) is squarely inside it. Splitting across two agents would reproduce this task's own documented failure: two developer→agent-father handoffs for this same wiring both reached `processed/` and neither shipped. Also rerouted the parent row agent-father→po: AC(4) lives in `scripts/git-hooks/`, which agent-father's zone **excludes** — that routing would have stranded it a third time.
- **Housekeeping:** `orch-row-prose-ceiling-check` blocked the first two writes (both rows already >12000B). Correct guard — took the sanctioned `detail_ref` route: 5 stale prose fields moved **verbatim, shasum-verified** to `docs/data/orch/archive/backlog-detail.json`. Both rows net-shrank while gaining the new record (37975→34264B, 15900→15710B).

### Carry-over
- `FIX-AUDITOR-DATA-TIER-NOTEBOOK-WRITE-PATH-UNWIRED` — ready[], developer, P0. Do **not** re-measure the pilot Success Signals until it ships **and** 3 real auditor cycles elapse **including ≥1 AUDIT_TIER=DATA fire**; a Tier-1-only sample cannot exercise the failing path.
- `docs/agent-memory/notebooks/system-auditor.md` is live-corrupt right now (1 section/179L, duplicate `c104`). AC-3 of the new row repairs it from `git show f25dc3d27^:`.
- Open question I did not chase: `notebook-auto-prune.sh`'s PostToolUse matcher is `Write|Edit`, so a Bash heredoc/`>>` append never reaches it — plausible reason 212L landed unpruned. Flagged on the row for the implementer to confirm.
