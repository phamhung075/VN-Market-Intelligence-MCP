# PO Notebook

_Last: 2026-08-05T09:11Z (dev-team cron triage tick 08:37Z — full sweep, first in 4 days). Prior 2026-08-05T08:28Z (router escalation: auditor C-08) — carry-over folded below._

## This cycle

- **The relay undercounted the inbox 6x, and two of four mints came from what it omitted.** dev-team passed "19 new"; `list_unresolved_reports()` returned **113, all `status=new`** (`[bctcExtractReconcile]` 45, `[BCTC]` 43, `[sla-monitor]` 6, `[BCTC-1345b]` 4, `[audit-output-contract]` 4, rest singletons). The day-4 serving outage and the audit-output-contract bug were **not in the relay at all**. Read the source list, never the summary count.
- **A supervised hold was blocking `.head` and no relay mentioned it — the mandatory pre-check is what caught it.** `should_hold=true` on `UC-CRITIC-HOOKS-ENFORCEMENT`. Ratified at source, not on relay: brief exists (382391 B, anchor at L1973) and the load-bearing claim re-derived **from live config** — exactly 6 hook commands ending `2>/dev/null || true` in `.claude/settings.local.json` + 1 in `settings.json` = 7 of 8 swallow failure. Still true 24 days on. Stamped `po_goahead_20260805T090611`; re-ran dev-team's own predicate → `should_hold=false`.
- **Carried a correction into the stamp instead of releasing silently.** The brief names the *orch-state prewrite Zod gate* among the swallowing hooks; live config shows `orch-state-hook-prewrite.mjs` is the **one fail-closed hook**. The swallower is `orch-state-hook-bash-backstop.sh`. BA would have "fixed" the only correct gate.
- **"Pipeline dormant" was 3 weeks stale, and folding into it would have buried a live root cause.** Probed before folding: `bctc_layout_units` n=5134, MAX 2026-08-04 22:12:12, **1865 rows written on 08-03** — the same day 4 reports claimed "0 rows"; `bctc_vps_queue` done=171 spanning 2023-Q4→2026-Q2. `FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION` (closed 07-10) shipped and **works**. Dropped that P0 to P1.
- **The real cause is a perfect 1:1 partition nobody had queried.** `financial_reports` = 191 UUID rows (`data_env=NULL`) + **66 `fallback-%` rows (`data_env='production'`)**, zero cross-over. Rows in `bctc_layout_units` **or** `bctc_table_rows` with a `fallback-%` id: **0 of 66**, while UUID reports hold all 5134. So "0 rows after 8 passes" is a **tautology** for that cohort, and each shell re-emits a fail-loud BUG forever. 63 of the 66 have a real PDF on disk. → `FIX-BCTC-FALLBACK-SHELL-REPORTS-STRUCTURALLY-UNEXTRACTABLE` (P0).
- **Found a second, untracked live defect by reading two MAXes side by side.** `bctc_layout_units` last wrote 08-04 22:12; `bctc_table_rows` last wrote **08-01 16:39** (per-day 07-29:451, 07-30:391, 07-31:109, 08-01:652, then zero). Two consecutive stages, one alive and one silent 4 days. Re-scoped the dormancy SPIKE onto exactly this.
- **Answered a stranded row's own standing question rather than re-asking it.** `OPS-BCTC-BANK-2025Q4-ENRICH-0ROW-REPARSE` wanted a per-`report_id` re-probe: **11/12 still exactly 0/0; GAS still has no FR row; no progress in 26 days.** 6 are `fallback-` ids (explained); the other 5 are UUID-keyed and still 0 → mechanism is **necessary but not sufficient**, recorded so nobody closes it early.
- **Byte-identical batches 24h apart mean one loop, not 44 incidents.** 08-03 ≡ 08-04 period-mismatch reports — same tickers, same supplied→detected pairs, same match/runner-up counts (the document fingerprint): 4317≡4381, 4318≡4382, 4320≡4384, 4331≡4395. ~21 BUG/day forever; **44 of the 113** unresolved reports are this one loop. Folded onto `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST`, occ 19→63, **P1→P0**. No new mint.
- **Corrected that row's own 2^-19 argument before anyone designs on it.** It claims content-period is *always* later than supplied (19/19). Report 4407 (DPM, supplied 2025-Q4 → content **2025-Q1**) is earlier — an always-latest actuator cannot produce that. The `docs[0]`/no-quarter-param findings still stand, but a fix that *reverses a bias* instead of *selecting by quarter* leaves DPM broken.
- **Reproduced the day-4 outage instead of trusting the reporting agent — no board row existed.** `get_bctc_full(code="FPT")` → `"Chưa có dữ liệu BCTC"`, live. Data is intact: FPT/HPG/VCB newest period 2026-Q2 is an empty `pending_extraction` shell while 2026-Q1 and 2025-Q4 are COMPLETE and passed. Newest-period selection head-of-line-blocks the whole ticker → takes out the watchlist at every quarter start, invisible to passive health. → `FIX-BCTC-FULL-SERVING-EMPTY-NEWEST-PERIOD-HEAD-OF-LINE` (P0). Param is `code`, **not** `ticker`.
- **Verified the SLA breaches against the harness, not the alert text — both are cadence FPs.** `check-foreign-flow-freshness.sh` → `PASS latest_date=2026-08-05`, rc=0 (a daily-EOD feed under a 10-min SLA; the 1020min figure repeating exactly two days running is a publication boundary). All three price breaches fired at 02:00Z = 09:00 ICT, **before** the 09:15 open. Folded onto `SPIKE-FRESHNESS-REMEDIATE-TRIAGE` — a 5th per-source patch after `SBV-FX`(CANCELLED)/`BCTC-THRESHOLD`/`SIGNALQUALITYAUDIT` is the churn pattern.
- **Minted the resolver gap at the root and refused to count the hand-repoint as the fix.** `devteam-review-claim-secondary-drain.jq:114` has exactly one defaulting case (null→`po`); `"dev-team"` passes through verbatim. **The script's own header (L20-21) already enumerates `dev-team=5`** — five known rows, no case for them. → `FIX-DEVTEAM-SECONDARY-DRAIN-NO-SELF-TARGET-RESOLVER-CASE` (P1). Anti-recursion refusal stays correct; do not relax it.
- **Closed a routing gap in my own flow doc rather than dispositioning a 4th time by hand.** `[notebook-immutability-guard]` had no rule in `triage-signals.md` (the 3 documented classes all parse `escalated=`; this one carries no such key). Added the row — including that a clean `git status` **after** the fact is outcome evidence and cannot refute an index-vs-HEAD finding, and a ≥2-cycle same-agent+section threshold before minting.

## Carry-over

- **Inconclusive is a disposition; "benign" is a claim.** The sweep-guard signal's index state at 07:42:36Z is unreconstructable post-commit. I could confirm `## c18` is byte-identical HEAD↔worktree and that mode=warn never blocks — that bounds the *risk*, it does not clear the *finding*. Logged as inconclusive.
- **Pre-checks ran for the first time in 4 ticks, and the first one fired.** Prior 3 cycles were directed escalations that skipped supervised-goahead/manual-dispatch/channel-audit/TNB. This tick's supervised-goahead caught a live `.head` block. The skipped-precheck debt was real.
- **Grep `docs/agent-memory/decisions/` before declaring no prior art** — board-only greps miss archived rulings (prior cycle, still binding).
- **Check whether a fix landed in the probe the agent actually runs** — `DONE` can be accurate while the symptom persists in a different plane (prior cycle).
- **Board saturation unchanged and binding.** backlog **357** (+4) / review 258 / ready 51 / in_progress 2 / qa 1; `task_total` **778**. The review lane, not priority, is still the constraint. All 4 mints route `next_agent: architect` and will queue behind it.
- **Still owed (ageing).** 7 `review[]` rows with `next_agent: po`, incl. `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (**P0**). ~38 manual-dispatch candidates unswept; manual-dispatch + TNB + channel-audit still not run this tick either — TNB last ACKed 2026-08-01T03:02Z, now 4 days stale.
- **113 reports remain `status=new`.** Triaged and dispositioned above, but not marked resolved in the reports store — the class is disposed, the rows are not. Next tick should close them by id or the same 113 re-enter every triage.
