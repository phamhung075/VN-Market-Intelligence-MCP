# PO — Notebook

## 2026-08-14T13:41Z · Reading payloads beat trusting types: two P0s arrived under signal types my own routing table does not have

- **Trigger:** dev-team Step-1 triage, 29 durable-inbox entries + 6 telegram reports. **Output: 1 `orch-apply.sh` pipe, conservation OK (728→736), 8 `backlog[]` mints, 9 existing rows enriched/rescoped/retargeted, 0 duplicates minted.**
- **Router's manifest was wrong on 3 of 11 counts** and I only caught it because I re-derived from the jq instead of triaging the summary: notebook-auto-prune was 11 not 8, context-bloat 4 not 5, and the "1x commit-sweep-guard remainder" did not exist (double-count). The router *told* me it was best-effort and unverified. Believe that warning literally — the manifest is a hint, never an input.
- **The single highest-value finding came from a signal type that would have been dropped.** `type=architecture_brief` and `type=notebook_prune_wrong_section_dropped_ts_key_bug` are both absent from `triage-signals.md`; by the letter of the table both fall to "unknown type → log and skip". One carried a 487L P0 design brief addressed to me with an explicit §9 to-mint list; the other carried a live data-loss bug. **Lesson: route by reading the payload, then fix the table. Never the reverse.** Folded both into `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED`.
- **A "suspected false positive" was a confirmed true defect, and the reporter's fix was wrong.** Report 4861 asked to switch FR-3 to exact period-key match. I read `dispatch-claim/SKILL.md:647-648` — the prefix match is *deliberate*, commented, and exists to cover weekly slots. Real root cause is the **TTL: 100800s (28h) against a 24h cadence**, which guarantees a 4h collision window *every day*. Recommended fix would have regressed weekly. **Lesson: when a report names both a symptom and a fix, verify the fix against the source separately from the symptom — they fail independently.**
- **Chased a claim into its own refutation.** Report 4858 (my own prior correction) said `OPS-RAG-SERVICE-REBUILD-DEPLOY-LANCEDB-FIX` had zero rows in any lane. It is live in `review[]/BLOCKED`, restored from cold archive at 06:58:58Z — **16 minutes after that report was written**. The gap was closed by the same corrective action that produced the report. Re-verify every structural claim against the board even when *you* wrote it.
- **The real RAG blocker was an absence, not a defect.** Four P0 rows, all `BLOCKED`, all `next_agent: po`, all waiting on one action — rebuild+deploy then the D1-D5 window — and **not one of them dispatches it**. Minted `UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW` (ops) as the missing *actor*, adding zero new diagnosis. Live `docker stats` read 82.17%, well under the 93% the reports claimed — a calm window, so deploy now rather than treat as emergency.
- **Root-caused 3 inbox signals to 1 bug by grepping for the missing evidence.** The 3 `notebook-immutability-guard` AC-2a WARNs looked like 3 separate agents misbehaving. They are downstream of QA's `nso_ts_key()` report: `tail -1` parses a trailing numeric git SHA as an 8-digit date, so auto-prune drops the wrong section mid-cycle. **I confirmed a second, worse loss QA had not found:** `grep -c COWORK-MUTEX-001 qa.md` == 0 while an inbox signal quotes that exact section verbatim — the `CHANGES_REQUESTED` verdict for a live P0 row in `review[]` is gone, and that row's `status_note` is empty. Minted P0 with a data-recovery AC.
- **The sweep-guard offender is this very session:** `actor=632721c2… prior_warns=20 threshold=3 outcome=blocked`. The hook's escalation converged to a hard block without anyone ever finding the offending call-site — escalation working, remediation loop absent. Wrote AC-4 asking the payload to name the *call-site*, not just the actor. **I committed pathspec-scoped for exactly this reason.**
- **Carry-over:** `in_progress[]` = 0 against `ready[]` = 100 / `review[]` = 91 — nothing is executing; consistent with tracked RLC starvation. `FIX-CI-BUNTEST-167-PREDICTION-MARKET-JOB` is READY/`dev-mcp-server` and dispatchable now. Durable-inbox CLEAR is dev-team's write, not mine — I did not touch it.

## 2026-08-14T13:02Z · The 44-hour-stale heartbeat was the whole story — and it proved the opposite of what it looked like

- **Trigger:** router asked me to triage a 6h cowork master-dispatcher silence (last tick commit `a10c8e989` 06:45Z → re-armed 12:52Z) it had already hand-fixed, and to mint only if the underlying gap was genuinely un-owned. **Output: 1 `orch-apply.sh` pipe, conservation OK (740→741), 1 P1 `backlog[]` mint to `developer`, 1 existing row enriched + re-prioritised, 0 duplicates.**
- **★ THE ROUTER'S HEADLINE NUMBER WAS THE EVIDENCE FOR A DIFFERENT DEFECT THAN THE ONE IT NAMED.** `task_force_release_orphan` reported `heartbeat_age=158976` (44.2h) on `cron-registration:cowork-team`. But that session was demonstrably firing `*/15` ticks until 06:45Z — **6h** before the read. A 44h age on a 6h-dead session is only possible if `heartbeat_at` froze at `claimed_at` and never moved. **The stale marker was not evidence the peer had been dead 44h; it was evidence the renewal never runs at all.**
- **★ THE RENEWAL EXISTS, IS CORRECTLY WRITTEN, AND SITS ON A BRANCH REACHED ONLY ON `verdict=ERROR`.** Repo-wide grep yields exactly ONE executable call site: `cowork-team/flow/preflight-error-fallback.md:57-63`, whose own header says *"reached only on Step 0 preflight verdict=ERROR"*. `scripts/agents-flow/cowork-tick-preflight.sh` — the only thing the CronCreate prompt runs every tick — contains **zero** occurrences of `cron-registration`. The brief (`2026-08-06-cron-rearm-cross-session-dedup.md` §1.4) targeted `main.md`'s Step 0a *"already fires every */15 tick"* — a premise **already falsified** by WU-2 (2026-07-13) three weeks before the brief was written; TE-T03 (08-11) then moved it further onto the exception branch.
- **★ I MEASURED THE TWO SIBLINGS INSTEAD OF GENERALISING FROM ONE.** Same `task_list_held` read: `detect-loop` `claimed_at=1786601070 heartbeat_at=1786711724` → renewing, last beat ~6min old. `standalone-team` `claimed_at==heartbeat_at` → never renews, **by design** (brief gives it `orphan_threshold_seconds=120` precisely because it has no per-tick hook). **cowork-team is the only family specced to renew that doesn't.** A one-line "all three markers are broken" claim would have been wrong and would have widened the fix 3×.
- **★ THE ROUTER'S PROPOSED DETECTOR WOULD HAVE BEEN A FALSE-POSITIVE MACHINE.** It suggested alarming on `cowork-schedule.json .slots[].last_fired` staleness. Two firing planes exist and **only one writes that field**: `chef-eod`/`fb-daily` both read `last_fired=2026-08-13` while `cowork-guaranteed-slot-firer.log` shows both invoked and completed **exit 0 today** (08:59:14Z, 09:18:34Z) with real published output. `last_fired` writeback from the launchd plane is still an open proposal (`UC-CDC-UNVERIFIED-BATCH` P12). Recorded as constraint C1 on the watchdog row so it isn't rediscovered.
- **★ DID NOT MINT THE DETECTION HALF — IT IS ALREADY OWNED, AND SAYING SO IS THE POINT.** `ARCH-SESSION-CRON-PLANE-LIVENESS-WATCHDOG` (`backlog`, created 2026-07-22, `next_agent:ba`) already reads *"build the launchd-hosted liveness watchdog that turns 'a session died and its whole loop vanished' from silent into a BUG-channel alert"*. Its `po-S3` decision-journal entry scopes it to *"launchd hosts re-arm + liveness only"*. Scanned all 8 lanes (740 rows) for `cron|dispatcher|liveness|marker|registration|silen|heartbeat|session-presence`; `UC-CDC-P5` owns the session-start self-arm half, `FIX-COWORK-PRESENCE-CLAIM-PAYLOAD-OBJECT-VS-STRING` is a **different lock**. **Enriched + `depends_on`-linked it; did not duplicate it.**
- **★ THE SKILL'S RECOVERY LOGIC IS NOT BROKEN — Step 1b.1 WOULD HAVE HEALED THIS.** `cron-cowork-team/SKILL.md` Step 1b.1 cross-checks `task_list_held(kind=session-presence)` and force-releases a dead holder. It is correct, and it is what the router effectively re-ran by hand. **The defect is that nothing periodic invokes it** — so the runbook's §4 recovery is adequate while its §2 detection signatures (all three output-side: MARKET age, signal-file age) have no registration-side row at all. Folded that as C3 into the watchdog row rather than minting a doc row.
- **★ CORRECTED THE BLAST RADIUS DOWNWARD, ON PURPOSE.** No guaranteed slot was lost — the launchd plane covered every one. Real damage is confined to slots the firer deliberately filters out (`guaranteed===true` only): `alert-commander-market` ~8 missed fires (`last_fired 06:51:20Z`, cadence `*/15 2-8 * * 1-5`), `chef-intraday` ~2, and the three `0 */4` offhours slots each missing 08:00Z. **~13 lost dispatches — including position-danger alerting silent through the afternoon of a −2.07% / −36.55pt VN-Index day.** That is what makes it P1 and not P0.
- **Routed `developer`, not `architect`, and wrote down why in `po_rescope_note`:** the design is already fully specified in the brief; only the *file* is wrong, and the correct file/function/line-range are named from source. Deliberately NOT folded into the ARCH row — that row has sat 23 days, and letting a 3-line unblock inherit an epic's queue position is how the outage recurs.

### Carry-over (13:02Z)
- **★ A frozen field reads as "very stale", never as "never written".** Both look like a large number. The discriminator is cross-referencing the age against an *independent* liveness fact (the tick-commit clock) — `heartbeat_age` alone cannot distinguish a long-dead session from a never-renewed marker, and I nearly triaged the wrong one.
- **★ Every liveness detector built on this marker inherits the frozen field.** `AC-5` on the new row therefore demands a **live** two-tick re-read of `task_list_held`, not a fixture-green test — the entire defect is a call site that is plausible on inspection and never executes.
- **Latent, not yet observed, and the reason this is P1 not P2:** brief §1.3 sets `orphan_threshold_seconds=7200` explicitly to stop a momentarily-stale `session-presence` read from stealing a **live** marker, and states it *"Requires the marker's `heartbeat_at` to be periodically refreshed"*. That precondition is false, so every cowork marker is always force-releasable → **two `*/15` dispatchers** is reachable, which is exactly what the dedup fix was built to prevent.
- **Open, unowned, deliberately not swept by me:** `AC-6` asks which of `detect-loop`'s two call sites actually fires, since both are documented as exception-only (`dev-team/flow/preflight-fallback.md` = ERROR-verdict only; `system-auditor/flow/main.md` Step 0d = only when the subagent spawns, which `cron-detect-loop/register.md` Job 2/3/4 suppress on `SKIP-SPAWN`). It **is** renewing, so one of them fires — **if it turns out to depend on something being broken, that is a separate row.**
- **Router action needed: dispatch `developer` on `FIX-COWORK-CRONREG-MARKER-RENEWAL-HEARTBEAT-STRANDED-ON-ERROR-ONLY-BRANCH`** (`backlog[]`/P1/`cross-service/`). `ARCH-SESSION-CRON-PLANE-LIVENESS-WATCHDOG` now `depends_on` it — do not dispatch the watchdog first, it would be built on the frozen field. `.head` left untouched: a triage ruling must not claim a dispatch lane.
