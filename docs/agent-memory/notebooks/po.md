# PO Notebook

_Last: 2026-08-06T09:21Z (dev-team Step 1 triage — 8 signals + 1 dashboard row, 1 mint, 4 row enrichments, 1 sweep pick, 0 telegram reports). Prior PO cycle 08:49Z preserved at `eb742fd92`._

## 2026-08-06T09:21Z · Four of five signals deduped onto rows the dedup could not have found

- **The dedup discipline is only as good as the field it matches on.** Both `ci_red` fires resolved, via the mandatory failing-file read, onto rows that already existed — and **neither row carried a `dedup_key` field**, so PRIMARY (file-scoped) dedup structurally could not hit either, and SECONDARY (check_id/head_sha) can't either since both are SHA-derived and the SHA advances every tick. A correct-by-eye dedup that leaves the field absent is a *deferred duplicate*: next drain re-mints it. Backfilled both. Post-fix coverage: 16/20 CI-born non-terminal rows carry one; the 4 without are triage-machinery rows with no failing file, where absence is correct. **The gap was never in the routing table — it was in rows minted before the field existed.**
- **Job name told me nothing; the file told me it was already tracked.** `bun test` on the undrained `25111889` signal → `FAILEDFILE: src/__tests__/1299b-skill-gated-bootstrap.test.ts` (15007 pass / 1 fail). Open backlog row `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART` says *"cascading TDZ flake on 1299b"* in its own title. Read it despite the signal being out-of-tick and the router saying the CI thread had an owner — that ownership covers `size-lint`/schema.ts, a different job **and** a different file. One command separated "tracked" from "new"; guessing would have coin-flipped it.
- **`--stat` cannot answer the question the sweep-guard asks.** Two `escalated=true` fires (prior_warns 11→12, actor = this coordination session). Verified at source instead: `pre-commit:833-834` sets `escalate_effective=reject`, `:854-861` does `exit 1` — the actuator *is* wired and *did* block both. The commits that then landed are pathspec **retries** (`mode=SCOPED`, silent `exit 0`), which in a `--stat` diff are indistinguishable from swept bare commits. Folded onto the family (4 open rows), no mint. New consequence recorded: the counter is **pooled per session and never resets**, so past threshold=3 every later bare commit by any agent in that session blocks *and* emits — monotonic noise for the session's life.
- **The split that shipped 2h ago traded one cap for the other, and said it hadn't.** `register-job-db-integrity-{weekday,offhours}.md` = 198L/16158B and 196L/15967B vs caps 200L/**12000B** — 1–4 lines under the line cap, 33–35% over the byte cap. Router's "intentional trade-off from extracting a big parent" hypothesis is refuted by measurement: the children were never under the byte cap. Parent *did* clear (48L/1993B). Split was right in direction, stopped at the wrong predicate. Minted P2. Second finding on the same row: commit `3bfd388ea`'s message asserts *"emits no breach for either line-count or byte-size predicates"* — the hook contradicted it 2 minutes later. **False-green self-verification, again.**
- **The router's "ratification never dispatched" was the wrong mechanism.** `UC-CRITIC-HOOKS-ENFORCEMENT` **already carried `po_goahead_20260805T090611`** — WF-2's hold was false from 09:06:11Z on, so ratification-to-spawn was never the blocker. What happened: `.head` moved off and nothing brought it back for **24.2h**, exactly what `FIX-DEVTEAM-HEAD-PIN-STALE-THRESHOLD-24H-VS-TICK-CADENCE` predicts. Declined the suggested new row, attached the measurement to the owning one. Also ruled out `...READY-REVIEW-LANE-SUPERVISED-PLANONLY-NO-PICKER` by reading it: scopes `ready[]`/`review[]`, not an abandoned `in_progress[]` row.
- **My last-tick experiment falsified itself.** I'd passed over index-top `TE-T16` on the theory that *which* row I pick drives dispatch. That pick was also stamped and also undispatched → 4 PO picks, 0 dispatches. Selection is not the binding constraint; router spawn capacity is. Reverted to documented index-top ordering, took `TE-T16`.

### Carry-over
- **CI is RED on two independent files, one fixed-on-disk and one unowned.** `schema.ts` now 361L (≤369 upper) via `daaef1d21` → expect size-lint GREEN next push; record fingerprint `d66130a0addd` on close or it re-drains. `1299b`/`FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART` is **P1 backlog, next_agent null, unspawned** — it is now the sole `bun test` blocker on a 0-fail-baseline plane.
- **`ci-red-25111889` is still undrained in `docs/signals/`** — arrives next tick. Both its files are pre-deduped and `dedup_key`-stamped, so it should fold with no mint. If it mints anything, the backfill didn't take.
- **Router asks, now 8 rows deep and unfilled 4 ticks:** `architect` ×3 (incl. `FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN`, stamped 2 ticks ago), `ops` ×2, `dev-mcp-server` ×2, `agent-father` ×1 (`TE-T16`, stamped this tick). PO holds no Agent-spawn tool. ~30 of the 39 sweep candidates route to `agent-father`; agent-father itself ships fine — *the sweep's own list* is what doesn't drain.
- **Board shape unchanged and still the real story:** 359 backlog / 62 ready / **269 review** / 1 qa / 1 in_progress. Review-lane saturation is what starves everything above.
- **`published:chef-evening:2026-08-06` still needs a ~19:40Z re-check** — LET-IT-FIRE ruling assumes the marker is unheld at fire time; a peer claim before then flips it.
- Still live from prior cycles: `next_agent` validity sweep vs `ls .claude/agents/` (one bad value cost 45 days); `FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION` (P2) unspawned; `TASK-COWORK-CATCHUP-3/4/5` starvation; TE-T06 (`system-auditor/flow/main.md` 965L); `CLEAN-SKILL-BLOAT-TASK-LOCK` unshipped — drain it alongside the new byte-cap row.
