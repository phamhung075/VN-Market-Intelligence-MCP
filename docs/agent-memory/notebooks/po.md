# PO Notebook

_Last: 2026-07-21T22:00Z (dev-team board-deadlock triage — 1 UNBLOCK minted, 4 signals DEDUP, keystone dispatch-type ruling)_

## Tick 2026-07-21T22:00Z — the dead gate is the common cause of the whole board; ruled BOTH + UNBLOCK

**★ The deadlock I kept re-deriving from the outside is a two-line arithmetic fact I can now read at source.** Verified in main.md: BOUNDED-1 gate `(ready+in_progress)<1` (L501), SLS `(ready+in_progress)<2` (L534). ready=36+in_progress=1=37 ⇒ both `37<threshold`=false ⇒ **neither auto-pickup lane can ever fire while ready is saturated.** Every prior tick I diagnosed a *symptom* of this (review not draining, quarantine dormant, "dispatch owed"). They are one gate. **When N independent dormancy findings all trace to "the consumer never runs," stop cataloguing casualties and read the consumer's own predicate.**

**★ Option-(a)-alone was a trap I nearly fell into again — the gate fix does not drain what is already stuck.** in_progress-only gating unfreezes NEW backlog pickup, but the 25 stranded ready rows (CCATO-MCP-T*, SYSREMAKE-P2-T*, DESIGN-COWORK-FANOUT-T*) are PM epic children with a real next_agent and **no claimer stamps them** — BOUNDED-1/SLS only claim rows they minted. So the answer is genuinely BOTH: gate=in_progress + a ready-lane consumer that dispatches promoted-but-unclaimed rows. **"Fix the gate" and "drain the queue" are different repairs; the saturated queue needs its own consumer, not just a satisfiable gate.**

**★ Dispatch TYPE was the load-bearing decision, not the fix content.** Last tick's architect BATCH rows are all sitting in ready[] ("dispatch owed") — a SPRINT-S/FIX entry gets *promoted to ready by the router*, where, until this fix lands, nothing consumes it. So minting the deadlock fix as SPRINT-S would strand it **inside the deadlock it repairs.** Only UNBLOCK's S4 block does immediate task_claim+direct spawn with no ready-promotion. **The vicious circle is broken by the dispatch primitive, not the task text — pick the type whose router-handling doesn't route through the broken machinery.**

**★ Four of five signals were already on the board; the churn was real and I refused to add to it.** #4 A-30 → FIX-AUDITOR-A12A20A30-FP-REEMIT-CONVERGE; #5 gold split across FIX-SIGNALQUEUE-RECEIVER-DELIVERY-CONTRACT (unified-agent has no inbox READ — exact match, and the 1 NEW signal_queue row po-20260720T052606 is dark for precisely that reason) + FIX-CHEF-L6-GOLD-FALSE-PREDICATE; both TNB c115 HIGHs already in ready. **Instance-9 with today's point-fix already inert = the definition of churn — one root-cause UNBLOCK that SUBSUMES the cluster, zero new duplicates.**

**★ The green instrument is the tell, not the proof.** FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER passes bounded1-supervised-lane-report.sh because that script checks *lane resolution* (dispatch-lane≠none), never *gate satisfiability*. It is green and inert simultaneously. New DoD must build a saturated live-shaped fixture (ready≈36,in_progress=1) and assert the gate FIRES. **An acceptance test that measures a label instead of the behavior certifies the exact bug it was written to catch.**

## Carry-over
- **DISPATCH OWED (guaranteed path):** UNBLOCK-DEVTEAM-DISPATCH-GATE-STAGING-DEADLOCK → **architect**, via S4 direct-spawn (NOT ready-promotion). Ruling = BOTH: gate=in_progress-only + ready-lane consumer + review QA-drain + satisfiability-testing instrument. Subsumes FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER (REVIEW, inert), FIX-DEVTEAM-REVIEW-LANE-QA-DRAIN, SPIKE-SATURATED-COUNT-THRESHOLD-GATES-SWEEP, SPIKE-BOUNDED1-ELIGIBILITY-CONTRACT-REVIEW.
- **DO NOT sign off** FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER on its lane-resolution instrument — DoD unmet until the gate is satisfiable. Router flagged this; hold it in REVIEW / reopen via the UNBLOCK.
- **After the keystone lands, the board drains itself:** the 4 dedup'd rows (#4/#5 pair, both TNB HIGHs) + 36 ready + 32 review all get consumers. Do NOT re-mint any of them — verify they drain.
- **AC teeth to defend (carried):** satisfiability fixture (ready≈36) not lane-label; ready-consumer must cover null-next_agent rows too or it strands the review-drain casualties again; A-30 retune needs the OOMKilled/93%/97% predicate not a threshold nudge.
- **259 unresolved telegram reports, 81% BCTC OCR/enrich noise** — owned, benign, not a user request. No self-initiation from channels this tick.
- **Lesson chain (extended):** an absence isn't corroboration till you query for it (21:07) → **a deadlock's cause is its consumer's own predicate, not the pile of casualties (22:00)**; and the dispatch TYPE, not the fix text, is what decides whether a repair reaches the broken machine.
