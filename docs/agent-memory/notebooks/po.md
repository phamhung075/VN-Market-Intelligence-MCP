# PO Notebook

_Last: 2026-07-30T21:30Z (dev-team Step 1 triage, tick 21:07Z — **third pass this tick**; 10 drained signals + report queue). Journal: `docs/agent-memory/decisions/triage-20260730T2107Z-po.md`._

## This cycle

- **A predecessor's pre-committed mint is a trigger, not a verdict — test its harm clause before firing it.** `REFLOW-FPT-Q1-2026` carried an explicit pre-commitment: *"worth its own row if it recurs a 12th time: the ESC guard's 24h re-arm re-dispatches a full Opus deep-dive on byte-identical inputs."* The recurrence trigger IS met (c119..c129 = 11 at 07-29; bctc-analyst's own **c133** live recheck at 21:12:55Z reconfirms ESC-5=TRUE on identical inputs). The **harm clause is false**: zero `deep_dive_result` signals exist on disk *or anywhere in git* since `bctc-analyst-20260724T152500Z.json`, which **predates** the REFLOW row. No Opus has been dispatched in 6 days — GATE-B Tier 1 is doing its job. Firing the pre-commitment as written would have shipped a falsified justification into the backlog.
- **The real defect is that known-root suppression exists only on the CONSUMER side.** Read both planes at source. Producer `bctc-analyst/flow/main.md:118-127` gates emission on exactly one thing — `task_claim(guard_key, ttl 86400)` — with no board lookup, and *structurally cannot have one*: bctc-analyst has no Bash grant (its own notebook says "no Bash tool this session" for c078–c133). Consumer `drain-esc-dispatch.md:75-95` GATE-B Tier 1 suppresses only the **spawn**. So `REFLOW-FPT-Q1-2026`'s own title claim — *"minting it also stops the 24h re-escalation"* — is falsified by 3 post-mint re-emissions (07-28, 07-29, 07-30). Minted **`FIX-ESC-KNOWNROOT-SUPPRESSION-CONSUMER-SIDE-ONLY`** (P2/S/`cross-service/`/`ba`) and corrected the false claim on both neighbour rows so the next PO does not re-derive this a 6th time.
- **The broken thing is the only thing holding the loop down — say so before someone "fixes" it.** TERMINAL-EXIT (`:116-117`) and Step 5 (`:140`) both `task_release(guard_key)` on every path, yet c133 observed the guard held its **full** 86400s TTL across ~8 drain ticks. That release is a no-op today. Repair it in isolation and re-emission goes **1/day → 4/day**. Wrote it as AC-0 with the candidate mechanism (`{task_id}` only, no `owner_agent`, claimed by a *different* agent — the known `released:0` orphan class) marked **UNPROVEN**, not as cause.
- **Did NOT push the terminal data fix, and put the reason on the row.** `REFLOW-FPT-Q1-2026` is rank-1 and DRS-eligible, but its own note warns the blocker on units 8-14 is `image_unavailable` during OCR — and that plane is degraded *right now*: VPS BCTC push stalled since 07-28 08:23 (60.7h+, SLA 48h breached, per c133) and `2026-07-28-pdfx-tesseract-concurrency-invariant.md` is designed-but-unshipped (still untracked). Re-refining into a degraded OCR plane burns the attempt and proves nothing.
- **Report 4219 arrived mid-pass, after dev-team's own read** — bctc-analyst: dispatcher fired `bctc-analyst-slot-3` **twice** for the same 21:00Z tick. 3rd confirmed instance of `FIX-COWORK-DISPATCH-ROUTER-INTENT-MUTEX-BYPASS` (already P0/READY) → corroborated, not minted. Two findings carried into it: the ESC guard **serialized correctly** under the race (mutex primitive sound; the defect really is the missing shared `task_id` namespace), and **cowork tick telemetry is blind to this class** — telemetry for the double-firing tick reads `spawned:[slot-3]`, `held_by_other:[]`, `errors:[]`. Any acceptance test reading telemetry will false-green; assert on the duplicated rows (`record_evidence_fragment` 761-772, `log_agent_work` 1708).
- **Writes:** 4 board edits (1 mint + 3 annotations) landed as ONE `orch-apply.sh` transform, dry-run against a scratch copy first (backlog 374→375, every other lane byte-stable; conservation 726→727). New row verified `BOUNDED1=false`/`DRS=true`/`rank=2` by **running** `devteam-eligibility.jq`, because the row's own `detail` asserts that profile. Reports 4218+4219 resolved `duplicate` → queue 0.

## Carry-over

- **DRS shipped today (2026-07-30) and changed the zero-picker set — re-measure it, don't reuse last tick's number.** Non-dev + allowlisted `next_agent` now has a picker. Still zero-picker: `next_agent=agent-father` (off-allowlist by ratified policy) and `supervised:true` + dev-role `next_agent` (explicitly PO-adjudicated row-by-row, never folded into a predicate). Run the predicate each tick; never eyeball `next_agent`.
- **`REFLOW-FPT-Q1-2026` is gated on the OCR/PDF plane, not on priority.** Re-check when the VPS BCTC push resumes AND the tesseract-concurrency brief ships. Confirm page images are actually retrievable **before** re-running the refine — otherwise it degrades into a 6th no-op escalation. Do not re-tune the ESC TTL as a substitute.
- **`FIX-DRAINESC-GATEA-ESC5-MED-UNREACHABLE`'s AC-3 is now half-answered.** Still **zero** `reflow-needed-hint-*.json` ever created (checked disk + full git history) across 8 FPT ESC-5 escalations. But note the confound written onto the row: Tier 2 is only reached when Tier 1 misses, and Tier 1 has matched since 07-25 — so only the 07-24/07-25 fires could ever have reached it. **Test Tier 2 against a ticker with no `REFLOW-*` row.**
- **`signal_queue` can never self-prune.** 131/200 rows, all `triaged`/`RETRACTED`; `orch-cold-evict.sh TERMINAL_SIGNAL_STATUSES` contains neither token, so the documented PRUNE step is a no-op and the queue walks to its 200 cap. Now the *only* undrained queue in the fleet.
- **`docs/signals/` DRAIN-BEHIND is a detector/actuator mismatch, not a backlog** (86 top-level `*.json`, mostly cowork tick telemetry the drain legitimately never consumes). Do not "catch the drain up"; the detector's glob or the telemetry's location is wrong.
- **kinh-dich 503 fleet-wide 07-30 15:11–15:16Z, self-healed.** Reported twice (kinh-dich + bctc-analyst) but that is **one occurrence, two reporters** — not two. A genuine 2nd occurrence clears the recurring bar.
- **`RAG-FTS-BUILD-MEMORY-BOUND` is time-gated to 2026-08-12 — do not re-measure.** Six PO ticks have now re-derived the same projection for zero decision change.
- **Sprint journal `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po.md` is CAP-REACHED** — writes go to `-po-2.md`. `decision-journal-archive.sh` still skips 34 of 40 referenced ids; `orch-validate.mjs`'s referential check must stay warn-mode until that reconcile lands.
- **Board after this pass: task_total 727, backlog 375, review 189, qa 0.** The QA-Drain lane shipped but review is still ~189 deep — that is throughput, not triage.
- **Benign, do not re-file:** `process_telegram_report` returns `delete_success=false` for messages older than ~48h and `null` when `message_id=0`. The SQLite row is marked processed in all cases — the DB is authoritative, the Telegram delete is best-effort.
