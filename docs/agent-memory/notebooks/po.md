# PO Notebook

_Last: 2026-08-01T06:32Z (standalone triage — telegram_reports id=4273, routed from dev-team Step 4.1). Journal: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-po-5.md` STEP po-S131. Prior cycle 06:23Z (auditor-findings batch, `triage-20260801T0608Z-po.md` po-S126…S130) committed at `06963d25c` — its carry-over is folded in below._

## This cycle

- **The routed question was unanswerable as posed, and proving that was the finding.** The brief asked whether NVL 2026-Q2 shows the VNM/VEA signature (`assets<equity` or `margin>100%`). It does not — and *no* alert carrying `financial=0.10` ever can. `confidence_financial` is a ternary at `parseBctcReport.ts:737-741`: when `detectUnitMismatch` (cross-statement >1000x) or `detectBsIntraStmtUnitMismatch` (intra-BS >100x) fires it short-circuits to a literal `0.1` and **`validateFinancialFigures()` is never called** — which is exactly where BCTC-VAL-01 (the VNM rule) and BCTC-VAL-03 (the VEA rule) live. The alert's hint names the one rule family its own value rules out.
- **Brute-forced it rather than arguing it from reading.** 160,000 input combinations through `validateFinancialFigures`: reachable outputs are exactly **{0, 0.4, 0.6, 0.8, 1.0}** — `0.1` appears **zero** times. Documented VNM figures (assets 957 << equity 18829, rev 63645) → **0.8**; same without the >30x-revenue escape → **0.0**; VEA margin 3.3 → **0.8**. Second defect fell out of the same run: the `Math.max(confidence, 0.1)` floor at `financialFiguresValidator.ts:306`, and its `:28`/`:177` docstrings, describe a floor that cannot be reached — VAL-01 requires `equity>0` while VAL-06 requires `equity<0`, so max stacked penalty is 0.6 → min 0.4. That dead `0.1` in the docs is what hides the collision with the unit-mismatch sentinel.
- **Live data corroborated on an independent plane.** 221 non-null `confidence_financial` rows: `1.0`→121, **`0.1`→65**, `0.8`→18, `0.0`→17. `0.4` and `0.6` have **zero rows** — the soft-penalty band the alert implies is empirically unpopulated, while the sentinel is the 2nd most common value at 29.4%. 23 tickers; split 44 cross-statement / 11 intra-BS / 7 both.
- **Triggering datum is real corruption, just not the named kind.** NVL 2026-Q2 (parsed 05:52:14.418Z): `total_assets=420322071420`, `liabilities=0`, `equity=0`, `net_revenue=31` → assets/revenue = 1.4e10. Raw VND in a triệu-VND column. `assets > equity`, so the VNM signature is flatly absent.
- **Containment is incidental, not designed — worth knowing before anyone relaxes it.** `get_bctc_full` hides the row via **PUB-1** (`refine_status=PENDING`), not via any corruption guard. `checkBctcIdentityGuard` (`total_assets<=0 OR total_assets<equity_total`) **passes** this row, because `equity_total=0` makes `assets>=equity` trivially true.
- **Minted one row, not two.** `FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY-THAT-CANNOT-PRODUCE-ITS-OWN-VALUE` (P2/S, `apps/mcp-server/`, `dev-mcp-server`), AC-5 an explicit NON-GOAL fencing off the underlying unit normalisation. 48 open BCTC rows already exist; a fifth overlapping normalisation row is churn, so that half went into the report's `monitoring` rationale instead. Scope-hold onto `FIX-BCTC-1345B-REPORT-BATCH` (review/qa) — it edits the same emission block but changes **volume only**; its verdict must not be read as making the alert text accurate. Report 4273 resolved `monitoring`; `read_telegram_reports(status="new")` now empty.

## Carry-over

- **When an alert ships a remediation hint, the hint is code and needs the same proof as the code.** This one has been sending triagers to the wrong rule family for months and reads perfectly plausible — two real patterns, two real prior incidents, right domain. What breaks it is that the branch emitting the value *skips the function implementing the hint*. **Cheap check: can the rule family the message names actually return the number the message is carrying?**
- **A unique magic constant is a free fingerprint — read it before investigating anything else.** `0.10` has exactly one producer in the whole path; knowing that closed an open-ended "check for OCR corruption" in one grep. Conversely it collides with a *documented* floor that turns out unreachable, and that collision is what made it look ambiguous. **A sentinel sharing a literal with a documented-but-dead default is worse than no sentinel.**
- **A tool answering "no data" can mean "data exists and I am refusing to serve it".** `get_bctc_full`/`get_bctc_series` both reported NVL absent; I briefly built a missing-data story on it. The store had all three rows. The tell was in the string — bare `Chưa có dữ liệu BCTC` is the PUB-gate reason, while genuine absence appends `cho <TICKER>. Kiểm tra bằng list_stored_pdfs`. **Serving-path silence is not a storage fact.** (Extends `feedback_empty_read_is_not_evidence…`.)
- **Both fork-arms in a brief can be wrong at once.** "Mint if it reproduces, else resolve" had no slot for *reproduces, but as a different class than the one named* — where the mintable defect is the alert, not the datum.
- **When a brief supplies a control variable, verify the control, not just the measurement** (prior cycle). The pdf-extractor trajectory was accurate at every sample; the one wrong cell was the "0 extracts/30m" control, and it selected the wrong mechanism. Related: **a denominator is a claim too** — three tiers agreed on "N of TOTAL" and all inherited the same unchecked TOTAL.
- **A finding that explains why a detector was right to stay silent must be tested against the detector's input table** (prior cycle). A-21's "clean exits wrote sentinels" story died on one query: there were no sentinels. Also: **rows carry `scope_out` instructions that outlive their evidence** — amending the prohibition is part of attaching contradicting evidence.
- **Leading-edge timing, unclaimed.** 2026-Q2 has 19 rows: 18 `pending_extraction` + this 1 `low_confidence`. NVL is the **first** Q2-2026 extraction attempted and it failed on unit corruption — the wave is just opening. No open row owns the normalisation (commit `06c65978c`, 07-06-07, added the **detector** only). If the next few Q2 extractions repeat it, that becomes mintable with a real denominator.
- **Board saturation unchanged and binding.** backlog **352** / review **258** / ready 51 / in_progress 0 / qa **0**. task_total **770**. 48 open BCTC rows; 214 review rows carry `next_agent: qa` against an empty qa lane — the drain, not priority, is the live constraint.
- **Still owed (ageing):** 5 `review[]` rows with `next_agent: po`, incl. `FIX-BCTC-LAYOUT-PUSH-FAILURE-NETWORK-DEADLOCK` (**P0**). 38 manual-dispatch candidates unswept. 25 P1 backlog rows resolve to an empty `next_agent` even after detail fallback. Pre-checks not run this tick (supervised-goahead, manual-dispatch) — narrow spawn scope; last full sweep 04:38Z.
