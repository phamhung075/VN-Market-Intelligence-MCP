# PO — Notebook

## 2026-08-15T01:03Z · I ratified a supervised row and stamped the go-ahead the flow told me to stamp. That stamp would have left the row exactly as undispatchable as before I touched it.

- **Trigger:** dev-team Review-Lane SECONDARY-Drain, single-row dispatch, `FIX-AUDITOR-C04-PARSEDAT-RECENCY-PREDICATE` (`review[]`, `supervised:true`, `plan_only:true`, `next_agent=po`, architect's PLAN-ONLY design complete). **Output: 1 `orch-apply.sh` pipe — 1 row ratified + re-minted `review[]`→`ready[]`, 2 dependent rows minted, task_total 687→689, conservation clean.**
- **★ `po_goahead` RELEASES ONE GATE AND EXACTLY ONE. THE OTHER FOUR NEVER LOOK AT IT.** `supervised-goahead.md` is unambiguous: stamp `^po_goahead`, WF-2's `should_hold` goes false, "the very next dev-team resume tick proceeds automatically." True — and irrelevant here. `is_bounded1_eligible` (`scripts/lib/devteam-eligibility.jq:429`) opens with `effective_supervised($detail_items) != true` and **has no `po_goahead` escape hatch at all**; RLC's claim predicate excludes `supervised`/`plan_only` the same way. A stamped-but-still-`supervised:true` row in `ready[]` resolves to `READY-XOR-SUP-OR-PLANONLY` — manual PO dispatch only. **I would have performed a complete, correct, fully-verified ratification and produced a row no picker on the board can take.** That is the `FIX-BOUNDED1-SUPERVISED-LANE-NO-SWEEPER` shape that idled a P0 six days. Ratifying means **clearing the flags**; the stamp is the audit trail, never the actuator.
- **★ I VERIFIED DISPATCHABILITY BEFORE APPLYING, NOT AFTER — AND THAT IS THE ONLY REASON I KNOW IT WORKS.** Ran the real `effective_supervised`/`effective_plan_only`/`is_epic_wrapper`/`deps_satisfied`/`resolved_dispatch_lane`/`is_bounded1_eligible` defs against the **candidate JSON** before it ever reached `orch-apply.sh`. Got `sup=false po=false wrap=false deps_ok=true lane=developer b1=true` on the impl row and — the part I actually wanted to see — **`deps_ok=false` on the flow-doc follow-up**, proving the ordering gate really bites. **Predicting "this will be dispatchable" is not the same as running the predicate that decides it.** Cheap, pre-write, no cleanup if wrong.
- **★ A `plan_only` ROW WITH A PERFECT SPEC IS THE EASIEST FALSE DONE ON THE BOARD.** 395 lines, verbatim script, verbatim diff, fixture procedure, rejected alternatives, explicit out-of-scope list. `scripts/auditor-db-checks.sh` **does not exist** and `main.md:988` still runs the defective inline SQL. Signing this `DONE_VERIFIED` would have closed a row that changed nothing. The spec's own quality is what makes it tempting.
- **★ VERIFIED AT SOURCE, AND THE SOURCE HAD THE ANSWER WRITTEN IN A COMMENT.** Did not stamp on the architect's relay. `parseBctcReport.ts` `ON CONFLICT(action_code,sort_key) DO UPDATE SET` carries `parsed_at = excluded.parsed_at` and, four lines up, a verbatim comment that `published_at` is **deliberately** excluded from that SET clause. `:834` `callerPublishedAt ?? parsedAt`. `ensureFinancialReportShellRow.ts:119` binds `'pending_extraction', 0`. Claim held exactly. **Re-deriving a correct claim cost minutes; accepting a wrong one costs a shipped predicate.**
- **★ "PO TO CONSIDER A FOLLOW-UP ROW" IS A DEFECT WITH NO ROW.** The spec's final footnote flagged `db-integrity-counts.sh`'s own `low_confidence_reports_count` as carrying the identical population defect. RAW-probed it: **47 counted, of which 34 `pending_extraction` + 2 `pending` = 77% never-extracted shell rows.** Real, live, currently wrong, in a different script feeding `cron-db-data-integrity`. Minted. A defect that lives only in another task's footnote is a defect nobody owns.
- **Zone forced a 3-row split, not a preference:** `scripts/` (developer) and `docs/agents/system-auditor/flow/` (agent-father, exclusive) cannot be one row without a cross-zone edit. The doc flip is gated by a **real `depends_on`**, never prose — if it landed first, C-04 would reference a script that does not exist. Cleared the stale `dispatch_lane:"architect"` and the 2026-08-08 `claimed_at/by` in the same write; `resolved_dispatch_lane()` recomputes from `next_agent`, and a stale denormalised lane already caused one near-miss.
- **Line numbers in this row's own paper trail were stale twice:** original `:572` → architect's 08-08 correction `:862` → actual today `:988`. Told the implementer to grep `| C-04 |`. **Any line number written into a task row is wrong by the time it is read.**
- **Carry-over:** (1) `ready[]` is now **90 rows against RLC's ~1/tick** — this P2 is realistically unreachable; I did **not** inflate its priority, because gaming a starved scheduler hides the ceiling instead of measuring it. The 3-consecutive-failed-expedite trigger from last tick still stands and this is more evidence for it. (2) `FIX-AUDITOR-C04-FLOWDOC-REPOINT` resolves to `agent-father`, **off the DRS allowlist by policy** — reachable only via `manual-dispatch-sweep.md`; correct, documented, but it means a future PO tick must actually run that sweep or the C-04 fix never wires up. (3) Did not re-audit channels or the `next_agent=po` queue — single-row dispatch, out of scope; last tick's **17 rows at `next_agent=po`, 6 P0** is unaddressed by this cycle.

## 2026-08-15T00:51Z · I minted the duplicate my own predecessor had explicitly refused to mint 90 minutes earlier — and the only thing that caught it was reading my own notebook.

- **Trigger:** dev-team Step 1 triage (28 durable-inbox entries, 41 `status=new` telegram reports 4856-4896, `head=idle`, `.signal_queue` `to=po` all already `triaged`). **Output: 2 `orch-apply.sh` pipes — 9 rows folded/promoted, 1 row minted AND retracted in the same tick (task_total 689→690→689, conservation clean), 1 manual-dispatch stamp, 41 telegram reports resolved to zero. BATCH of 5 (1 UNBLOCK, 4 FIX).**
- **★ THE DEDUP SCAN SAW THE OWNING ROW AND I MISREAD IT ANYWAY.** Reports 4889/4890 asked for a structural fix to "auditor narrates signal_queue writes that never land". I grepped five lanes, saw `FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` in the results, read its **title** (cowork Step 5.3 off-flow detector…), classified it cowork-scoped, and minted a new P0. It was already the ratified owner: `docs/protocols/fail-loud-protocol.md:151-154` names it **by id** as owner of "wiring this detector to run automatically, exogenously". **Ownership lived in a protocol doc, not in the row title — a title-and-id grep can never find that.** Retracted in-tick.
- **★ AND MY PREDECESSOR HAD ALREADY DONE THIS EXACT ANALYSIS AT 23:03Z.** The notebook I overwrite every cycle contained, in bold, the finding that no SPIKE existed, the ruling not to mint, the protocol citation, and the measured reason (19 P0s in `review[]`, RLC ~1/tick). I read it only when I opened the file to write. **Reading my own notebook is not a closing formality — it is a dedup surface, and the cheapest one available.** Order it before the mint, not after.
- **★ TWO IDENTICAL EXPEDITES, ZERO LANE MOVEMENT — THE EXPEDITE IS NOW THE THING UNDER TEST.** 23:00Z folded that P0 into BATCH as UNBLOCK. One full dev-team tick later: still `review[]`/P0/`next=pm`/`plan_only=true`, unmoved. I re-folded it and wrote the measurement onto the row with an explicit stop condition: **if it does not move a third time, the correct action is a ruling on the dispatch ceiling, not a third stamp.** Three identical expedites with no movement would make the expedite mechanism the defect.
- **★ 18/18 BCTC QUARANTINES POINT ONE DIRECTION — THE CORPUS WAS THE DELIVERABLE, NOT A ROW.** Every one of reports 4862-4880 has detected-quarter strictly LATER than supplied, never earlier. Independent bad documents would err both ways. Folded onto the already-open read-side row as its **post-fix regression corpus** (zero quarantines for these ticker/period pairs must follow). A "working as designed, no action" disposition would have thrown away the only test set that can prove the fix.
- **★ A ROW MINTED WITH NO `next_agent` IS INVISIBLE TO EVERY PICKER ON THE BOARD.** Two rows this tick: `FIX-DECISION-JOURNAL-BYTECAP-NO-ACTUATOR` (16 days, `next_agent` null) and `FIX-CADENCE-COWORK-DUP-MARKET-WATCHER` (`next_agent` null AND no `supervised`/`plan_only`, so excluded from BOUNDED-1, SLS, DRS, READY-XOR *and* BACKLOG-XOR-GAP simultaneously). Both assigned handlers. **The journal row's live numbers also refuted its own premise:** a freshly-rotated journal went 0 → 51365B against a 36000B cap in under 4 hours. **Rotation happens; capping does not.**
- **Premise corrections issued (3):** report 4858's "OPS-RAG-…-LANCEDB-FIX has ZERO rows in ANY lane" — still refuted, it is `review[]`/P0/`next=po`. Reports 4856/4857 ("FU-RAG-DEPLOY-MEMORY incomplete") — already corrected by 4858 itself; that row's DONE_VERIFIED is correctly scoped to compose deploy-codification and disclaims "memory resolved"; do **not** reopen. Report 4892 (chef DXG gate reversal) — **closed by FR-8**, shipped 21:16:01Z, not new scope for an in-flight spec; unverifiable until the next chef dish fires.
- **Hygiene:** resolved all 41 telegram reports (30 `duplicate`, 11 `monitoring`, no deletions) — `status=new` is now empty. The resolver exists in `telegram-reports.md` §2f; `main.md` just never calls it.
- **Carry-over:** (1) **17 rows sit at `next_agent=po`, 6 of them P0** — that queue, not the inbox, is the PO bottleneck, unchanged from last tick. (2) The RLC 1-row/tick ceiling now has **two consecutive measured failures** on the same P0; next occurrence is the trigger for a ruling. (3) `UNBLOCK-RAG-OPS-DEPLOY-AND-DURABILITY-MEASUREMENT-WINDOW` undispatched **11h05m**, gating four P0 rag rows. (4) 13 `to=ops` rows open, nothing drains that lane — **7th tick reporting it**. (5) TNB c130 ACKed 20:40:27Z — do **not** re-ACK; next TNB check is `business_context_cited` non-null on a post-FR-8 dish, RAW against the synthesis JSON. (6) sweep-guard `prior_warns=27 outcome=blocked` on the live dispatcher session — its commits are being hard-blocked now, not warned.
