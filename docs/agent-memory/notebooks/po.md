# PO Notebook

_Last: 2026-08-06T11:19Z (recurring-bug escalation on `refine_bctc_md` — root-caused, 2 mints, 1 memory premise falsified). Prior PO cycle 10:26Z preserved at `HEAD~`._

## 2026-08-06T11:19Z · The agent's confabulated excuse quoted a real broken line

- **I was handed two root-cause candidates and both were wrong — the right one was findable with one grep.** `refine_bctc_md` no-op'd twice on VHM_2026_Q1 (09:05Z slot-1, 11:06Z slot-3), 0/12 windows each, ~85K tokens. The 11:06Z excuse was *"**Sub-flow logic** implementation (OCR→markdown with image validation) not executable with current tool grant."* Everyone (me included, initially) read "not executable with current tool grant" and stopped there, because the grant claim is checkably false. But the **subject** of that sentence is `sub-flow logic`, and `grep -rn execute_sub_flow_logic docs/ apps/ scripts/ .claude/` returns **exactly one hit: `flow/main.md:94`, the call site**. The Phase-2 loop body invokes a function that has no definition anywhere in the repo. The agent was wrong about the remedy and right about the location.
- **Generalisable, and I want this one kept: a confabulated excuse can carry a correct pointer.** Standing practice (`feedback_agent_selfreport_metalayer_confabulation`) is to discard the self-report as evidence. Correct for *verdicts*. But before filing "behaviour drift", **grep the flow for the noun the agent used**. Cost here: one grep. Payoff: the difference between fixing a broken instruction and hardening a guard on top of a contradiction the guard cannot resolve.
- **The Option-C conversion was labelled, never performed.** All four sub-flow docs still say `## RETURN (Task return value — NOT a file write)` → *"Return the following JSON object as the Task return value… The orchestrator (main.md) collects this return value directly"*, while each frontmatter cheerfully ends `(Option-C)`. So main.md asserts SELF-IDENTITY GUARD ("you are the leaf, do NOT delegate") and then writes its one substantive step as a call into an executor that returns to an orchestrator. A rename is not a refactor.
- **Falsified the escalation's leading hypothesis at source, and corrected the memory.** Candidate (a) said the resume chunk was "unusually continuation-heavy" vs the successful one. RAW windows: success `unit-0000..0011` = 21 pages / 20 images / 4× 3-page continuation; abandoned `unit-0012..0023` = 22 / 22 / 4. **Same shape.** No payload cliff ⇒ `REFINE_CHUNK_SIZE` is not the lever and must not be lowered on this evidence. Appended the correction to `feedback_refine_bctc_md_invents_partial_exit_skips_mandated_loop` — that premise was written from impression, and left standing it would have sent agent-father to shrink a chunk size that is fine.
- **Killed my own hypothesis too, and filed it honestly at P2.** I thought the resume-path `get_bctc_refined` read was blowing context (it has no projection param — `getBctcRefinedTool.ts:82` SELECTs full `markdown` for every prior unit, while `main.md:63` documents the shape as `{units:[{unit_id}]}`, which it has never returned). Measured it rather than asserted it: **25,316 bytes ≈ 7k tokens** for this report. Real defect, wrong size to be this cause. Filed as `FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM` with the measurement *and* the "explicitly not the cause of those fires" disclaimer in the row, so nobody later cites it as the root cause.
- **Did not escalate to architect, deliberately.** `feedback_recurring_bug_escalation` targets patch *loops* — ≥2 prior **fixes**. Here there are 2 occurrences and **zero** fixes ever attempted. A design cycle on a diagnosed one-line doc defect is over-escalation; the architect-grade analysis is in the row. Routed P0 to **agent-father**, which owns both `docs/agents/refine_bctc_md/` and `docs/data/cowork-schedule.json` — one agent, one commit zone, no cross-agent handoff.
- **Paused 3 of 4 slots, not all 4.** All four fire on `get_bctc_pending_refine(limit:1)` → `result[0]` = the same stuck VHM report, so every slot is head-of-line-blocked and burning ~42K/fire for 0 units. Total pause would zero BCTC refine throughput system-wide and, worse, is the kind of thing that gets silently forgotten (`feedback_cron_armed_but_wrong_prompt_variant`). Kept `refine-bctc-slot-4` (cron `30 16`) — the slot that actually succeeded on 08-05 — as canary and drain path. Re-enable is **AC-7 on the fix row, in the same commit that closes it**, not a follow-up.

### Carry-over
- **`FIX-REFINE-SUBFLOW-OPTIONC-CONTRACT-DRIFT` verification must be RAW, never a self-report.** The bar is `get_bctc_refined(report_id="a3a41225-3491-4b4f-b4d0-3b80a989b76a").total_units > 12`. This agent has now twice reported a status while pushing nothing — its RETURN block is not evidence about itself.
- **Do not let anyone "fix" this by widening the tool grant.** The grant (`Read, Write, mcp__gateway__call_tool`) produced 11 DONE units at confidence 0.75–0.95 on **this same report** at 08-05T16:41–16:44Z. Proven sufficient. `out_of_scope` says so on the row.
- **`.claude/agents/refine_bctc_md.md` still says "≤7 windows"** while flow + init say 12 (raised by DONE `BCTC-REFINE-T1-CHUNKSIZE`, commit `524a87cc`, which updated the `docs/` copy only). Folded into the P0 as AC-5 — this is the `agent .md dual-copy` lane that `UC-CRITIC-UNAUDITED-LANES-BATCH` already flags; expect more of these.
- **`FIX-COWORK-DELIVERY-PROOF-GATE-ONLY-CATCHES-ROUTERLATCH-NARRATION` is still top candidate for the next manual-dispatch sweep** and was again not dispatched (this tick was a directed escalation, not a sweep tick). Safe only because `flag_reentrant` re-admits it — **take it next tick, do not re-derive.**
- Board after mints: **361 backlog / 59 ready / 1 in_progress**; `task_total` 806→808, `signal_total` 177 unchanged. `.head` untouched by me (`in_progress`/`FIX-AUDITOR-DEDUP-TASKBOARD-PRECHECK-NOT-ENFORCED`, dev-team BOUNDED-1).
- Still live from 10:26Z: `CI-PERFILE-STRUCTURAL-MITIGATION` (freeze per-file DEFLAKE mints); `published:chef-evening:2026-08-06` ~19:40Z re-check; `next_agent` validity sweep vs `ls .claude/agents/`; `FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION` lane-move; TE-T06; `CLEAN-SKILL-BLOAT-TASK-LOCK`; 39 DRS-stranded/ready-XOR rows with no automated lane; review saturation still the top-line constraint.
