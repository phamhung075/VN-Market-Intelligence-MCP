# PO — Notebook

## 2026-08-22T20:12Z · An amendment that fixes a contradiction in one branch and reproduces it in the next branch is not ratifiable on its summary. Re-run the resolver.

- **Trigger:** architect returned `FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE` (review[], `next_agent:po`) after the AMEND-THEN-RESUBMIT pass my 08-14 verdict ordered. **Output: RATIFIED-IMPLEMENT-WITH-BINDING-CORRECTIONS (B1/B2/B3), Q4 ruled, AC-0 severed to its own P0 row, 1 orch-apply write.**
- **★ THE AMENDMENT'S CORE WAS RIGHT AND I ONLY KNOW THAT BECAUSE I RE-RAN IT.** A1's numbers reproduced exactly on my own resolver — strict known-union **57** vs broad **100**, strict-dangling **23** vs broad **11**. That is what earned the ratification. Everything I *rejected* came from the same re-run, not from reading the prose harder.
- **★ THE FIX FOR A CONTRADICTION LANDED NEXT TO A FRESH COPY OF THE SAME CONTRADICTION.** My 08-14 A3 said two branches must not both match one id with opposite verdicts. The amendment fixed that instance (branch 1, `PLANNING`) — and left branch 2 firing on any non-backlog **lane** *including* `done`/`done_verified`, while branch 3 (FINISHED) tests terminal **status**. Both match a `DONE_VERIFIED` row; precedence hands it to branch 2 and **registers a finished sprint as active**. Verified at the executable why that is strictly worse than the bug we started with: `decision-journal-archive.sh:135` is `PROCESS_IDS = CLOSED_IDS - ACTIVE_IDS` and `:183` pins any `active_sprints[]` id as `active_stay` — a wrong active registration makes journal archival **permanently** unreachable. Ruling: terminality is evaluated **before** lane.
- **★ "TWO CONFIRMED LIVE INSTANCES" WAS FOUR.** The new STEP-0 task-id-collision check is written as an unguarded universal first branch, so I ran it as written: it fires on `BA-IND-P1-MOMENTUM-FRONTEND`, `UC-RDL-P4` (the two named) **plus** `FIX-BCTC-BANK-SUMMARY-MAPPING` and `FIX-BCTC-CTG-BALANCE-SHEET-REFINE`. Both unnamed ones get a §11.3 disposition that **contradicts §11.7's own table for the same id**, today. One is simultaneously a backlog task, an `active` sprint_goal entry, and the `.sprint` of 2 review rows; the other's `.sprint` equals **its own id** — a live 1-hop cycle under a design that claims "none observed live today".
- **★ A NEW VOCABULARY WORD IS NOT AUTOMATICALLY DRIFT — CHECK WHO ALREADY OWNS THE FIELD.** Q4 asked me to rule on `sprint_goal.status: "OPEN"`, framed as a third word "neither this brief nor PO's ruling anticipated". `orchStateSchema.ts` §11 already names it verbatim: *"Non-terminal tokens (OPEN, ACTIVE, PLANNING, ...) are untouched by design"*. So I **refused both offered options** — normalizing my own 2026-06-24 editorial field would have papered over a predicate defect with a data edit. Ruled the polarity inversion instead: closed set of liveness-**asserting** tokens (`active` + TERMINAL_SET), everything unknown falls to the **inert** side where it can only under-classify, never fabricate a sprint.
- **★ THE PROCESS GATE WAS PROTECTING PROSE WHILE THE ACTUAL HAZARD GREW.** The row is 23 days old and `plan_only` the whole time, but AC-0's danger is **armed and rising**: my own read-only `--all --dry-run` reads `archived(would)=356`, of which **147 journals belong to 10 ids with open work, six `sprint_goal:active`** — trajectory 299 (08-08) → 346 (08-14) → **356**. `dev-standards.md` still documents the command as a sanctioned backfill with no warning. AC-0 has zero open questions and §11.5's `DJA_ALLOW_ALL_UNGATED=1` leg makes it shippable **without** the not-yet-written replay script, so holding it behind a design loop bought nothing. **Severed to its own P0 row.**
- **★ SUPERVISION HAD TO MOVE FROM THE FLAG TO A ROW BOUNDARY.** `supervised:true` is now inert here — WF-2's gate is `$supervised and ($goahead|not)`, and my own 08-14 stamp permanently satisfied `$goahead`. So I did not pretend the flag still gates: the real gate is now written into the row — **the orch-state data write may not be applied until PO signs the freshly regenerated classification output**, `next_agent=po` before that write.
- **Deliberately not done:** did **not** bounce to architect a 4th time despite B1 being a live in-brief contradiction. Every residual item is a one-line predicate correction on questions PO owns outright (sprint-goal vocabulary, sprint-kickoff authority), and the brief already mandates regenerating its own table at implementation time — so §11.7 is advisory by its own terms. Also did **not** mint a row for the stranded `PREDICTION-CLAIMS-DAILY-CADENCE` goal, to keep this write single-purpose.

### Carry-over
- **`FIX-DJA-ALL-SAFETY-VALVE-ARMED-HAZARD`** → backlog, **developer**, P0, `cross-service/`. No `supervised`/`plan_only`, dev-role `next_agent` → BOUNDED-1 eligible, should auto-promote. **Ship AC-1 leg (a) only** (`DJA_ALLOW_ALL_UNGATED=1`); leg (b) waits on the parent row's replay script and must not block.
- **`FIX-SPRINT-REGISTRY-DANGLING-IDS-BREAK-SIGNOFF-AND-JOURNAL-ARCHIVE`** → review[], **developer**, `plan_only` cleared, `supervised` retained as documentation of the PO gate. Implementer reads brief §11 **plus** `po_review_note` B1/B2/B3 + `po_ruling_q4`, which supersede §11.3/§11.4/§11.7/§11.8 on conflict. It is a **dev-role `next_agent` + `supervised:true`** row — the documented DRS/BOUNDED-1/SLS blind spot — so it needs **explicit router dispatch**, it will not self-promote.
- **Count trap for the implementer:** strict-dangling is **23, not 22**. §11.7's table omits the `"BACKLOG"` sentinel (22 refs, 5 of them non-backlog = a *counted* violation). If it is not a row in the regenerated table, the AC-5 `violations==0` gate can never close.
- **Row prose ceiling forced the shape of this write.** The row was 17186B against a 12000B growth-only ceiling, so adding a ratification required *shrinking* first: replaced my superseded 08-14 note, compressed the folded Q1/Q2/Q3 to operative one-liners, and compressed architect's `architect_review_note` (pure duplication of §11, which `detail_ref` already points at). Net −755B. **Detail belongs in `detail_ref`, not inline** — this is the second cycle running that this guard shaped a PO write.
- Standing lesson, and this is the second time it has paid this month: **when an agent reports "N confirmed live instances", re-derive N.** It was 4, not 2 — and the two it missed were the two that broke the design.
