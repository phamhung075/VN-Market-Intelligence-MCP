# PO — Notebook

## 2026-08-11T15:54Z · The "coverage hole" was the dispatcher working; the real gap was a rescinded ruling

- dev-team Step 1 triage, orphan-adoption resume of `task:po-triage-20260811` (redispatch_count=1, no checkpoint → full flow). **0 mints, 4 folds/corrections, 4 signal dispositions, 1 sweep stamp.** 5 `orch-apply.sh` pipes, all PASS, `task_total` 768→768 (conservation clean — nothing minted by design). Journal: `docs/agent-memory/decisions/triage-20260811T1554Z-po.md`.
- Pre-checks: dashboard **0 NEW but 4 rows stuck at `READ`** — ACK'd by a prior tick, never dispositioned. WF-2 `should_hold=false`. Sweep: 83 candidates (44 DRS, 38 backlog-XOR, 1 ready-XOR, 4 re-admitted). TNB unchanged since this session's 14:36Z ACK.

### Decisions worth keeping
- **★ I FALSIFIED MY OWN PREDECESSOR'S HEADLINE FINDING BY RUNNING THE PREDICATE INSTEAD OF READING ITS PROSE.** The 14:31Z tick wrote that the P0 `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` is "invisible to BOTH manual-dispatch-sweep classes — a genuine coverage hole", and yesterday's `NEXT` told the next tick to consider minting a row for it. Executing `is_design_router_eligible` live: **all 7 clauses true, DRS_ELIGIBLE=true**, and it is **#1 of 44** DRS-eligible rows (rank 0; the entire rest of the field is rank 1). **Exclusion from `is_drs_stranded_off_allowlist` means `next_agent=pm` is ON the allowlist — i.e. DRS itself is the picker.** Those two sweep classes are *recovery nets for rows DRS won't take*; being outside them is the healthy case, not the broken one. **A new escape hatch would have "fixed" a defect that does not exist.**
- **★ THE 14:31Z P0 BUMP BOUGHT POLE POSITION AND THE TICK THAT MADE IT DIDN'T KNOW.** At `high` the row sat behind ~20 rank-1 peers; at P0 it is rank 0 and sorts first. So the escalation was already the effective act. **Only remaining gate is latency, not reachability:** DRS fires only on a head-idle tick where the 1-of-6 idle rotation selects `drs`, and `.head` has been on the QA-drain batch since 15:25Z. `wip_in_progress=1`, so the WIP<2 gate is open too (the BLOCKED `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING` row does **not** count against WIP — checked, because "2 rows in `in_progress[]`" looked like saturation and isn't).
- **★ THE ACTUAL BLOCKER WAS A RULING I WROTE MYSELF, 14 DAYS AGO.** `po_consolidation_ruling_20260728` says *"Do NOT route these until the epic closes"* — and its own stated reason (2) is *"it buys zero throughput: all 5 of these rows are ALREADY inert"*. **That premise died when the row became P0 owner of a live 67h outage.** Rescinded for this row only; left AC-9 (close all 6 together) binding at closeout, and left the original field byte-intact for audit. A rescission is not a closure. Without this, `pm` could correctly bounce the dispatch citing my own stamp.
- **★ `READ` IS AN ACK, NOT A DISPOSITION — AND 4 SIGNALS DIED IN THAT GAP.** Prior tick logged the dashboard per the "0 NEW" test and moved on. Four `to=po` rows sat ACK'd-but-unrouted. Closed all 4 → `triaged`, inbox now 0 stuck. **"0 NEW" is not "inbox clean".**
- **★ TWO INDEPENDENT PLANES BEAT A KNOWN-FP PRIOR.** A-30 pdf-extractor memory WARNs are a documented FP-churn class I'd normally hold at observation. But the standing benign-rule for the *other* plane (`reconcile-exhausted 02:00–08:00Z is expected`) names **one** escalation trigger — persistence past the ~09:05Z proof point — and it fired: 10 reports 09:05→14:05Z, then a **15:05:04Z circuit-breaker trip** declaring systemic dormancy. Meanwhile A-30 ran 94.07%→97.81% sustained, loss of reclamation. **Memory exhaustion and fleet-wide zero-row layout extraction are one story on two instruments.** Marked `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD`'s review-lane proof **REFUTED** — it must not sign off.
- **★ A GREEN TOP-LEVEL METRIC DOES NOT CLEAR A DEAD SUB-PLANE.** `get_system_status` shows `BCTC 1.7h Bình thường` — that tracks the *reports* table, not the three layout tables (`bctc_layout_units`/`bctc_table_rows`/`bctc_md_tables`) the reconcile job counts at **0**. Wrote that on the row so the next reader can't use the healthy number to close the sick one.
- **★ CHECKED THE ROW'S PREMISES BEFORE SPENDING A DEV CYCLE ON IT.** Sweep top `UC-ASL-P5` argues `triaged` is not cold-evictable and the residual "keeps growing". Both dead: `orch-cold-evict.sh:150` has included `triaged/TRIAGED/RETRACTED` since 2026-08-08, and the queue went **154 → 20 rows**. Its part-(5) enum-tighten would now **wedge orch writes**. Stamped + folded, but as **DISPATCH-WITH-RESCOPE**, not as spec.
- **Fold, not mint:** guard `FAIL` on `[auditor_cycle_loss, cron_fire_gap, system_issue]` — **10/18 (56%)** of the hot inbox, `system_issue` alone 6/18 — onto `FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED`. Its AC-1 names a type that has since aged out of the rolling window, so shipping it literally leaves the guard RED (same trap as 15:05Z). AC-4 "nothing schedules the guard" now confirmed on 2 ticks 5 days apart.
- **Correlation, not a mint:** both `auditor_cycle_loss` ticks (14:00Z, 14:36Z) and this session's own reaped marker (14:06Z) sit in one window — `get_system_status` uptime 1h41m at 16:01Z ⇒ mcp-server started **~14:20Z**. Flagged as hypothesis needing a restart-log read, not asserted.

### Carry-over / NEXT
- **#1 `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` (P0), BATCH entry #1, 3rd tick running.** It is dispatchable and top-ranked — **do not mint a hatch, do not widen a predicate.** If it is still BACKLOG next tick, the question is why the idle rotation never selected `drs`, not whether it's reachable. First diagnostic is still cheap: firer not *invoked* (launchd/plist) vs invoked-and-no-op (supersede wiring).
- **Retract line 45 of the 14:38Z entry** ("Coverage hole, unpatched by design... Worth its own row if it recurs") — **wrong, superseded by this tick.** Left the entry below intact so the correction is legible.
- `SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD` is in `review[]` with `next_agent=ops` and **must not pass QA** — re-work, not sign-off. Sibling rows: `FIX-PDFX-PARENT-PROCESS-MEMORY-BURST-HEADROOM` (P1, review), `FIX-AUDITOR-A30-SUSTAINED-WINDOW-SHORTER-THAN-TARGET-RECLAMATION-PERIOD` (P1, backlog).
- Standing, and it decided three calls this tick: **when a prior tick hands you a conclusion, re-run the predicate it was derived from.** Prose conclusions invert; predicates don't. Cost ~3 jq runs, overturned the tick's headline finding.
- Still not cleared (6 ticks): 4 `TASK-COWORK-SIGNAL-*` in `review[]`, `supervised=true`, zero `po_goahead`.

## 2026-08-11T14:38Z · The product has been dark for 3 days and the watchdog was inside the outage

- **★ ALL 8 `guaranteed:true` COWORK SLOTS DEAD ~66h — AND THE DISPATCHER IS FINE.** Newest fire `2026-08-08T20:23:36Z`. chef-morning/eod/evening, fb-daily/weekend, digest-daily/sunday, tnb-audit — the entire user-facing output plane. Raised **high → P0**.
- **★ THE THIRD PLANE IS THE FINDING.** `.last_fired` + agents' notebooks are both equally consistent with "session is down". What decides it: **non-guaranteed slots on the SAME dispatcher ran today** (market-watcher 12:07Z, alert-commander `c97`, news-scout). Failure is **class-selective**.
- **★ "COINCIDENT WITH THE API OUTAGE" KEPT THIS INVISIBLE FOR 3 DAYS.** Two earlier PO ticks saw the stale TNB handoff and stopped at an ambient explanation. One `jq` over `cowork-schedule.json` falsified it.
- **★ IT IS FAILURE TO RESUME, NOT MISSED FIRES.** Tuesday's 05:15/08:45/09:15Z slots all passed with no fire *while the dispatcher was demonstrably running*. **No look-back window fixes a path that never fires again.** Named the self-latching shape: an eligibility predicate keyed off `.last_fired` requiring a recent previous fire.
- **★ THE WATCHDOG WAS IN THE DEAD CLASS.** `tnb-audit` is slot #8. Zero alerts in 66h. `guaranteed:true` is a measured false promise.
- **★ IDENTICAL REFUSALS 24h APART = ZERO CONVERGENCE.** BCTC period-mismatch sets for 08-09/08-10 byte-identical (FRT `46 vs 10`, KDC `51 vs 6`, VND `115 vs 26`). A daily zero-yield retry loop manufacturing ~19 CRITICAL-looking BUG messages/day.
- **★ A `[STALE]` LABEL DOES NOT DISCLOSE A UNIT ERROR.** `vnindex = 170468` (= `1704.68 × 100`) vs served `vnIndex = 1773.41`, 43s apart, same container.
- **★ MY OWN PIPES VALIDATED PASS OVER DUPLICATE BOARD IDS.** `FIX-NEWSVPS-OVERNIGHT-PUSH-OUTAGE-663M-SILENT` sits **twice in `ready[]`** and the copies disagree (`P2` vs `P1`/`dev-mcp-server`). Consumers use `first`, so **array position decides which row a gate sees**.
