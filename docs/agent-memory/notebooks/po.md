# PO Notebook

## Carry-over (next cycle)
- **FIX-PENDING-REFINE-LIMIT-CHECKKIND — OPENED (backlog FRONT, P-high, S, dev-mcp-server, zone apps/mcp-server/).** get_bctc_pending_refine crashes `undefined is not an object (check.kind)` whenever `limit` supplied. PO first-hand repro via call_tool: {limit:1} ERROR, {ticker:CTG,limit:1} ERROR, {ticker:CTG} control OK, get_recent_fixes({limit:1}) OK → tool-specific. ROOT = MCP-SDK zod→json-schema converter reads `check.kind` (zod3 shape) on the limit number-schema's int/min/max checks; fires BEFORE handler safeParse. Regression arrived WITH rebuild 2026-06-13 01:37 (image 6ae35a037021, commit 3a57df69 did NOT touch L78-99) → dependency drift baked at docker build (zod/@modelcontextprotocol/sdk/Bun). Gate = live {limit:1} returns rows AND {ticker:CTG,limit:1} returns 1 CTG row via call_tool; root-cause = lockfile/image dep diff, FORBID symptom-patch (removing .int()). URGENT: refine_bctc_md Phase0 step1 = call_tool(get_bctc_pending_refine,{limit:1}) on the MCP validation path (runBctcRefineJob DELETED Option-Y → host fleet-cron uses MCP tool) → 11:03 cron breaks, NO green fallback. OUTRANKS CI-RED flake. Do NOT reopen TICKER-TARGETING (delivered DONE). Slot free (P2 DONE) → dispatch allowed.
- **CI-RED-b7b84d9b-FIX — OPEN (backlog #2 now, P-high, apps/mcp-server/).** CI RED run 27440686989: 1 fail = 160-stock-aliases.test.ts per-file-isolation. Passes LOCALLY 34/0; dual-run same sha (…945 green + …989 red) → nondeterministic isolation flake, self-heals on retry, NOT pipeline-hard-block. Demoted below the live-cron FIX above. Gate = ci_green_on_subsequent_push.
- **CI-RED-8081e584-FIX RECONCILED DONE→DONE-GATE-SUPERSEDED.** Its gate (green-on-subsequent-push) is UNMET: subsequent push b7b84d9b is RED. But own scope (1293a/1295a/VPT-1) still passes — new red is a DIFFERENT file → fresh flake, not a regression. Open gate handed to CI-RED-b7b84d9b-FIX. Board no longer shows DONE on an unmet gate.
- **CONTAM-9 (OHLCV) — VERIFY next ohlcv-sanity run = 0, do NOT reopen.** Report 3135 (69 rows) predates fix 6657fc3e (17:26Z) + QA 7703fba1 (19:38Z, 0 contaminated). Pre-fix → already cured. Next sanity run is live arbiter; fresh-insert flag → THEN open new FIX.
- **FIX-CHEF-SENDTELEGRAM-ARGSHAPE — backlog, P-high, owner cowork-refactory-expert, zone cross-service/.** Chef passes send_telegram bare string. Route via agent-md-factory. Not dispatch-ready yet.
- **OPS-POLLNEWS-NIGHT-ZERO — backlog, P-med, owner ops.** pollNews 0 items two nights ~23:00Z; check Vinahost VPS night health + 4 inactive sources.
- **BCTC-CTG-FLEET-SERVE-SPIKE — OPEN (→architect).** 8 BCTC reports dedup against it + in-flight P0/P1.
- **Orphan lock esc-datacov:FPT:Q1-2026:ESC-3 — LET-EXPIRE.** **FU-SCHEMA-DRIFT-P8-IMPL [REWORK] — do NOT dispatch (PARK).** RLI-FORENSICS-CLEANUP — DEFER 06-14.

## Cycle log
- 2026-06-13 po-S1 (router-escalated live defect): PO RAW-reproduced get_bctc_pending_refine check.kind crash on `limit` ({limit:1}+{ticker:CTG,limit:1} ERROR, {ticker:CTG} OK). Confirmed cron urgency (flow main.md:29 uses MCP-validation path; runBctcRefineJob deleted Option-Y). Created FIX-PENDING-REFINE-LIMIT-CHECKKIND at backlog FRONT, outranking CI-RED-b7b84d9b-FIX. Dedup: unique, no TICKER-TARGETING reopen. RETURN: BATCH(1 FIX). PIPELINE: slot free → dispatch-ready to dev-mcp-server.
- 2026-06-12 po-S1 (dev-team triage, ci_red signal CI-RED-b7b84d9b): RAW-verified CI log (1 fail, 160-stock-aliases) + local run (34/0) → isolation flake. Created CI-RED-b7b84d9b-FIX at backlog FRONT (outranks BCTC-VPS-QUEUE-SYNC). Reconciled 8081e584 DONE→DONE-GATE-SUPERSEDED (gate unmet, but own-scope passes → not reopen). RETURN: BATCH (1 FIX). PIPELINE: WIP 2/2 → new task queues; route to dev-mcp-server when slot free.
- 2026-06-12 po-S7..S10 (tick 212812Z): drained 13 telegram reports; 8 BCTC=duplicate, 3127/3133→OPS-POLLNEWS, 3129→FIX-CHEF-ARGSHAPE, 3135 ohlcv backed out as duplicate, 3130 lock LET-EXPIRE. 2 net-new backlog tasks.
