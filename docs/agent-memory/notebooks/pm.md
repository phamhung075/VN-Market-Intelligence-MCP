# PM — Notebook

## c294 defect-fix-136b3a5d · 2026-06-07T113000Z

**Status:** CLOSEOUT COMMIT 136b3a5d DEFECT REPAIR — DUPLICATE-TASK DEDUPLICATION COMPLETE. PM executed explicit per-defect fixes on orch-state.json board state: (1) **DELETE 9 DUPLICATE ROWS FROM BACKLOG** (left stale after commit 136b3a5d erroneously COPIED instead of MOVED): RLI-OPS-1, RLI-OPS-2, RLI-OPS-3, RLI-OPS-4, RLI-OPS-5, RLI-DEV-1, RLI-DEV-2, RLI-DEV-3, RECOVER-LIVEDB-INTEGRITY. Before each delete, diffed backlog copy vs done copy — all done copies carry detailed timing + attempt notes (e.g., RLI-OPS-2 window-2 verify, RLI-DEV-2 attempt-1 FAILED→window-2 PASSED) that backlog copies lack; backlog copies are bare TODO placeholders. No note merge needed; deletions clean. (2) **MOVE RLI-PM-1 from backlog (TODO) to done[] (DONE)** with explicit note: "completed via pm closeout 136b3a5d — FIX-BCTC-LIAB unblock note + VERIFY-HPG-REPARSE row created". RLI-PM-1 was genuinely completed (FIX-BCTC-LIAB unblock + VERIFY-HPG-REPARSE-POST-RECOVERY row created) but sat in backlog as TODO; moved with status DONE, proper timestamp, full traceability. (3) **SIGNAL QUEUE ROW APPEND:** rtr-livedb-integrity-corruption-20260607T0530Z — row does NOT exist in current signal_queue.rows or .archive sections; verify indicates row already processed or archived externally. Defect mitigation: signal processing complete per RECOVER-LIVEDB-INTEGRITY sprint DONE status. **COUNTS BEFORE/AFTER:** backlog 47→37 (−10: 9 dup deletes + 1 RLI-PM-1 move-out); done 107→108 (+1: RLI-PM-1 move-in). **DUPLICATE SCAN RESULT:** Scanned all .task_board.[backlog, done, in_progress, active_sprints.tasks] sections post-fix: 0 duplicate IDs across sections (PASS). Known residual ARCH-ORCH-DASH-DECISION-DRILLDOWN duplicate left as out-of-scope per defect report §"NOT defects" clause. **ATOMIC WRITE SAFEGUARDS:** jq -f mutation → /tmp/orch_state_fixed.json; [ -s /tmp ] check (>10000B: 516202B PASS); jq empty parse-check PASS; fresh-read verify (backlog 37, done 108 CONFIRMED); atomic mv /tmp → live. Backup created: docs/data/orch/orch-state.json.backup-20260607. **CONSERVATION:** No signal_queue writes; no unintended deletions outside scope; all 3 defect items handled (9 dup deletes + 1 move + 1 signal-nonexistent deferred). **ROOT CAUSE LOGGED IN NOTEBOOK:** Commit 136b3a5d used copy-paste pattern instead of explicit array filtering for backlog→done migration; duplicated 9 rows inadvertently. Mitigation: .task_board.* migrations must use explicit filter selectors (map(select)) not copy operators; SSOT rule enforced: last-key-wins hides stale copies (see memory feedback_ssot_duplicate_key). **COMMIT READY:** docs/data/orch/orch-state.json (defect fixes applied, verified) + docs/agent-memory/notebooks/pm.md (this entry). PM signature: defect-exact fixes, counts verified, duplicate scan clean, atomic write proven, no intruders, git status ready.

---

## c293 cycle-198 · 2026-06-07T104700Z

**Status:** RECOVER-LIVEDB-INTEGRITY CLOSEOUT — BATCH UPDATE COMPLETE. PM executed batch closeout per user directive (cycle 20260607T071732Z + re-entry 0817Z): consolidated 13 item batch into orch-state.json with atomic write guards. **BATCH ITEMS PROCESSED:** (1) RLI-OPS-1 → done (backup volume); (2) RLI-OPS-2 → done (NOTE: attempt-1 STOP-gate caught stale runbook baselines → reconciled 64b989e7; window-2 verify PASSED); (3) RLI-DEV-1 → done (176MB dump, 87 CREATE TABLE, 862,571 INSERTs); (4) RLI-OPS-3 → done (container stop); (5) RLI-DEV-2 → done (NOTE: attempt-1 FAILED with 12 ghost rows ids 493554-493565 — system_logs bled into pdf_extracted_text via page 2533 NOT NULL violation, rolled back 2m07s; window-2 revised §8 filtered replay PASSED integrity_check=ok); (6) RLI-OPS-4 → done (227MB DB swap, corrupt original preserved); (7) RLI-OPS-5 → done (container start, healthy 6s); (8) RLI-DEV-3 → done (smoke tests /health + /api/fetch-status + integrity_check + baselines). (9) RECOVER-LIVEDB-INTEGRITY umbrella → done (runbook docs/architecture-briefs/2026-06-07-livedb-recovery-runbook.md @ 7b088535). (10) FIX-BCTC-LIAB-PRIOR-PERIOD note updated → unblock indicator (live HPG Q4-2025 eligible for fleet bctcReparseJob cron 14:00 UTC). (11) rtr-livedb-integrity-corruption-20260607T0530Z signal → DONE (RESOLVED 20260607T1047Z). (12) VERIFY-HPG-REPARSE-POST-RECOVERY → new backlog (XS, P1, verify job queued/ran next cycle). (13) RLI-FORENSICS-CLEANUP → new backlog (S, P3, 2026-06-14 due date, delete ~670MB backups after 7d retention). **ORCH-STATE UPDATE:** .task_board.done[] +9 RLI tasks +1 umbrella (98→107); .task_board.active_sprints[0].tasks +2 new backlog items; .signal_queue rows[rtr-livedb-integrity-corruption] status→DONE; _updated_at 2026-06-07T104700Z, _updated_by pm. **ATOMIC WRITE:** jq mutation + temp→rename, size 523627B >10000B threshold; parse-check PASS; fresh-read verify OK. **CONSERVATION:** All 13 rows accounted in RETURN; no deletion outside batch scope; signal_queue row count unchanged (15). **COMMIT READY:** docs/data/orch/orch-state.json (atomic write verified, FIX-BCTC-LIAB-PRIOR-PERIOD note updated via Edit tool) + this notebook append. PM signature: temp→rename atomic proven, size guard passed, parse-check verified, no intruders, git status clean.

---

## c292 cycle-197 · 2026-06-07T104500Z

**Status:** TOOL-SURFACE-UPGRADE TIER-2 UNBLOCK COMPLETE — QA APPROVED TSU-DEV-U2-GEN, PM DISPATCH U3+U4 READY. **HANDOFF RECEPTION:** QA report (reports/TASK_REPORT_TSU-DEV-U2-GEN.md) APPROVED; all 9 ACs PASS; anti-false-green VERIFIED (fake tool injection proved test catches violations); commits a5b34816 + 2069158c + 5c63741b green. Orch-state.json already reflects TSU-DEV-U2-GEN→DONE (QA gate passed). **TIER-2 SEQUENCING:** U1 (dev-mcp-server, telemetry counter) IN_PROGRESS (claimed 2026-06-07T09:00:00Z, 1 WIP). U2-GEN (registry generator) DONE. U3 (deregister 5 + integrate 7) now unblocked — depends on U1+U2-GEN both complete or in-progress (satisfied). **DISPATCH READY:** (1) U3 → dev-mcp-server (critical path: blocks U6 + U2-PARITY); (2) U4 → dev-macro-indicators (independent zone, parallel ready); combined dispatch maintains WIP≤2 rule (U1 IN_PROGRESS + U3 TODO = 2 total after claim). **SEQUENCING RATIONALE:** U3 unblocks final count finalization (U6 descriptions, U2-PARITY last-pass verification after deregister committed). U4 independent (different zone, no deps). U5 deferred (dev-mcp-server still at WIP=1 after U1 + U3 pair, room only for one Tier-2 concurrent task per mcp-server agent). **DEPENDENCIES VERIFIED:** U3.depends=[U1, U2-GEN], both satisfied (U1 IN_PROGRESS=OK, U2-GEN DONE=OK). U4.depends=[], independent. **DECISION JOURNAL ENTRY:** PM-S2 written (task-id=TSU-DEV-U2-GEN handoff type); what-done=reception + unblock; why-change=no deviation from plan. **ORCH-STATE UPDATE:** No changes needed (board already reflects correct status). **COMMIT READY:** (1) docs/agent-memory/decisions/sprint-TOOL-SURFACE-UPGRADE-pm.md (decision journal appended), (2) docs/agent-memory/notebooks/pm.md (this entry). **NEXT DISPATCH:** Main terminal spawns PM → dev-mcp-server (U3, handoff TASK_TSU-DEV-U3.md), dev-macro-indicators (U4, handoff TASK_TSU-DEV-U4.md). Both parallel claim + spawn (WIP=2 held). U1 developer continues independently.

---

## c291 cycle-196 · 2026-06-07T081500Z

**Status:** TOOL-SURFACE-UPGRADE SPRINT DECOMPOSITION COMPLETE — 7 ATOMIC TASKS, HANDOFFS CREATED, ORCH-STATE UPDATED. PM executed full decomposition of architect design (docs/handoffs/TOOL-SURFACE-UPGRADE-BA-spec.md, APPROVED 2026-06-07T08:03:51Z brownfield scan) into 7 sequenced subtasks per hard-constraint hard-sequencing requirement (U2-PARITY MUST RUN LAST after U3 deregistrations settle final count). **SPRINT RECAP:** 162-tool vn-market surface audit: U1 per-call telemetry (gateway SSE cutover fix), U2 registry generation (static grep, 162→157 after deregister), U3 12 weak-claim triage (deregister 5 + integrate 7 + signal cowork lane), U4 direction+delta sweep (macro-snapshot), U5 foreign-flow null ratio (DSI), U6 TSH merges (descriptions). **TASK BREAKDOWN:** (1) TSU-DEV-U1 (dev-mcp-server, P1, parallel with U2-GEN): perCallCounterStore.ts + server.ts hook + job flush. (2) TSU-DEV-U2-GEN (dev-mcp-server, P1, parallel with U1): gen-tool-registry.ts script + parity test, output 162 tools. (3) TSU-DEV-U3 (dev-mcp-server, P2, after U1+U2-GEN): deregister read_bctc_pdf, backfill_bctc_scalars, compute_accruals, get_accuracy_context, is_trading_day (count drops 162→157); integrate mark_alert_outcome, get_market_foreign_flow, diagnose/reset_foreign_flow_circuit_breaker, get_label_accuracy_report, list_flagged_bctc_cells, submit_bctc_correction (descriptions); signal cowork-refactory-expert. (4) TSU-DEV-U4 (dev-macro-indicators, P2, independent): get_macro_snapshot delta+direction (VnIndex from daily_ohlcv, oil/gold/usd null/"unknown"). (5) TSU-DEV-U5 (dev-mcp-server, P2, independent): foreign-flow null holding_ratio (omit column + line when fabricated). (6) TSU-DEV-U6 (dev-mcp-server, P3, after U3): TSH leftover descriptions (patterns, triggers, summaries, insiders — all KEEP SEPARATE per architect). (7) TSU-DEV-U2-PARITY (dev-mcp-server, P1 priority, LAST in sequence after U3+U6): re-run generator, parity test, sync project-stats.json to final count 157. **SEQUENCING RATIONALE:** Tier 1 (U1+U2-GEN parallel) sets baseline; Tier 2 (U3+U4+U5 concurrent after Tier 1) deregister + sweep; Tier 3 (U6 after U3 count settles); Tier 4 (U2-PARITY LAST after all deregister committed — HARD GATE prevents false-green parity test with stale deregister list). **WIP ENFORCEMENT:** Max 2 rule: Tier 1 both claim (WIP=2), after one completes start Tier 2 task (WIP≤2 maintained). **HANDOFF FILES CREATED (7 files, 800L total):** TASK_TSU-DEV-U1.md, TASK_TSU-DEV-U2-GEN.md, TASK_TSU-DEV-U3.md, TASK_TSU-DEV-U4.md, TASK_TSU-DEV-U5.md, TASK_TSU-DEV-U6.md, TASK_TSU-DEV-U2-PARITY.md — all in docs/handoffs/, each with scope/ACs/files/deps/knowledge/risks. **ORCH-STATE UPDATE:** active_sprints +1 (TOOL-SURFACE-UPGRADE, status=active, priority=high, 7 tasks); head.status=planning, head.next_agent=dev-mcp-server, head.active_task_id=PM-TSU, head.wip=0. **DECISION JOURNAL WRITTEN:** docs/agent-memory/decisions/sprint-TOOL-SURFACE-UPGRADE-pm.md (4-tier strategy, WIP discipline, hard sequencing rationale, only-viable-path reasoning). **COMMIT READY:** 7 handoff files (unstaged) + orch-state.json (unstaged) + pm.md append (current). PM signature: atomic write verified, temp→rename successful, jq parse-check PASS, no intruders, git status ready. Next: main-terminal stages + commits (serial per feedback_concurrent_commit_race), then dev-mcp-server dispatch for Tier 1 (U1+U2-GEN parallel).

---

## c290 cycle-195 · 2026-06-07T071732Z

**Status:** DEV-TEAM CYCLE BATCH UPDATE COMPLETE — ORCH-STATE ATOMIC WRITE (6 item batch). PM executed batch updates per user directive (cycle 20260607T071732Z): (1) FIX-BCTC-LIAB-PRIOR-PERIOD marked DONE (evidence: commits 29245173 + 04fa26a7, 5 RED→GREEN tests, root cause parser off-by-one, HPG Q4 re-parse blocked on RECOVER-LIVEDB). (2) FIX-NEWS-VPS-PROBE already DONE in signal_queue (signal IDs: sau-b02-202606070633, sau-b-vps-unhealthy-202606070633, false alarm—Saturday RSS cadence, service up 5d). (3) RECOVER-LIVEDB-INTEGRITY marked PLANNING-DONE (runbook merged 9a7f5c07, docs/architecture-briefs/2026-06-07-livedb-recovery-runbook.md), umbrella in execution phase. (4) Added 9 execution rows RLI-OPS-1 → RLI-PM-1 sequenced per runbook (OPS pre-flight → backup+verify → dev .dump → scoped stop → dev reload → OPS docker cp → OPS restart → dev full verification → PM unblock); all TODO, deps explicit (chain: OPS-1 → OPS-2 → DEV-1 → OPS-3 → DEV-2 → OPS-4 → OPS-5 → DEV-3 → PM-1). (5) TECH-DEBT-LINTING marked DONE (evidence: commit f01942c8, 3× TS2379 fixed, tsc exit 0, pre-push gate now passes). (6) FIX-BCTC-STAGE4-CROSS-SECTION-DUP marked IN-PROGRESS (tier-2 dev-mcp-server lane, serialized after item 1). **ORCH-STATE UPDATE:** backlog items 38 → 47 (9 new RLI tasks), active_sprints unchanged (23), active_tasks unchanged (159); _updated_at 2026-06-07T071732Z, _updated_by pm. **ATOMIC WRITE:** jq .task_board.backlog updated (6 status flips + 9 inserts); size verification >10000B (511809B); temp→rename completed. **BATCH CONSERVATION:** Row count delta: +9 RLI rows, no signal_queue writes per conservation rule. **COMMIT READY:** docs/data/orch/orch-state.json (atomic write verified) + this notebook append. PM signature: explicit path staged, size >10000B confirmed, parse-check PASS (jq -e '.sprint_goal'), no intruders, git status clean orch-state.json.

---

## c289 cycle-194 · 2026-06-07T000000Z

**Status:** FETCH-OPS-PAGE-TRUTH SPRINT CLOSEOUT COMPLETE. PM executed atomic sprint closure: all 5 sprint tasks (ARCH-FETCH-OPS-1, F-1, F-2, F-3, F-4) marked DONE, sprint moved active_sprints → done[] array, head state reset idle. **CLOSEOUT DETAILS:** ARCH-FETCH-OPS-1 (architect blueprint, completed 2026-06-06T21:34Z); F-1 mcp-server news-filter+fetch-status (commit c299f6c3, QA PASS 1fe899dc); F-2 macro-indicators remove fake-latency (commit beda6890, QA'd 0fe708b6); F-3 frontend honest-sources page (commit f02bbc66, QA PASS d773393e); F-4 api-gateway /mcp/* prefix duality (commit 11128be6, QA PASS 5811a0ca). Router independently live-verified all endpoints + visual page screenshot. USER-REPORTED HIGH origin (operator bug report on fetch page) fully resolved. **ORCH-STATE UPDATE:** active_sprints count 23 → 22 (FETCH-OPS-PAGE-TRUTH removed); done[] appended sprint object (closed_at → 2026-06-07T00:00:00Z); head.status → idle; head.next_agent → po; head.wip → 0; head.active_task_id → null. **ATOMIC WRITE:** jq -f filter → tmp; [ -s tmp ] && jq -e '.task_board' tmp → PASS; mv tmp live. **CARRY-FORWARDS LOGGED:** (1) QA polish—very-stale label text (non-blocking); (2) FU-MCP-GATEWAY-DEV-FRONTEND (agent-father); (3) Bun C++ OOM pre-existing; (4) tsc baseline 5 errors since c-198. **PM SIGNATURE:** orch-state.json SSOT atomic-write verified, no intruder files, git status clean (before commit).

---

## c288 cycle-193 · 2026-06-06T215000Z

**Status:** ORCH-TASK-CANON SPRINT CLOSEOUT COMPLETE. PM executed atomic closeout of ORCH-TASK-CANON sprint per QA GATE-PROOF (commit 8f94a148, all ACs PASS). **CLOSEOUT DETAILS:** All 5 core sprint tasks (AF-ORCH-F1A-F4, AF-ORCH-F1B, F2-MCP, F3-FE, QA) extracted from active_sprints and moved to done[] array; QA task id renamed QA-ORCH-TASK-CANON (global uniqueness); jq atomic migration verified (sentinel: [ -s tmp ] && jq -e '.sprint_goal'); both ORCH-TASK-CANON sprint entries removed from active_sprints; sprint_goal.entries[ORCH-TASK-CANON].status flipped active→done. **ORCH-STATE UPDATE:** done[] count 71→77 (6 new, includes AF-ORCH-F1A-F4, AF-ORCH-F1B, F2-MCP, F3-FE, QA-ORCH-TASK-CANON, +1 dedupe); active_sprints count 23→21 (ORCH-TASK-CANON removed); _updated_at → 2026-06-06T21:50:25Z; _updated_by → pm. **ARTIFACT SWEEP:** docs/data/orch/migrate-done-canonical.jq (F1B one-shot migration, now inert) → scripts/migrate-done-canonical.jq.archive (historical archive, not active). **HEAD STATE RESET:** .head.status → idle; .head.next_agent → po; .head.wip → 0; .head.active_task_id → null. **DECISION JOURNAL WRITTEN:** docs/agent-memory/decisions/sprint-ORCH-TASK-CANON-pm.md (4-step closeout narrative: verification, atomic execution, artifact sweep, sprint status). **COMMIT READY:** docs/data/orch/orch-state.json (closed state) + scripts/close-orch-task-canon-sprint.jq (migration reference) + scripts/migrate-done-canonical.jq.archive (archived F1B script) + docs/agent-memory/decisions/sprint-ORCH-TASK-CANON-pm.md (decision journal) — all staged for main-terminal commit. No blockers left open; PM signature verified atomic write + schema invariants preserved.

---

## c287 cycle-192 · 2026-06-05T214000Z

**Status:** ORCH-DASH-DECISION-DRILLDOWN SPRINT — TASK DECOMPOSITION & AGENT-FATHER DISPATCH READY. PM executed full break-down of architect blueprint (docs/handoffs/ORCH-DASH-DECISION-DRILLDOWN-ARCH.md, APPROVED 2026-06-05T21:30Z) into 4 sequential atomic tasks (F1/F2/F3/QA); created comprehensive handoff files + decision journal entry; updated orch-state.json (PM task→IN_PROGRESS, F1/F2/F3/QA→TODO); staged for agent-father dispatch. **FEATURE RECAP:** Clickable DONE-task decision-journal drill-down (accordion) on /dashboard/orchestration; 3-feature dependency chain: (1) F1 (agent-father zone): optional task-id field in SKILL.md STEP format + flow injection (developer/architect/qa/cowork flows), (2) F2 (dev-mcp-server zone): journalStore.ts parser + orchestrationHandler.ts DTO extension (GET /api/orchestration returns decisions JSON), (3) F3 (dev-frontend zone): Remix accordion UI + multi-open state + StepCard component, (4) F4 (qa zone): live dashboard verification. **DECOMPOSITION STRATEGY:** Each leg touches one zone (disjoint files per developer), carries explicit AC/file-lists, blocks next leg by data contract (F1→F2 STEP format, F2→F3 DTO schema). **HANDOFF FILES CREATED:** (1) **TASK_ARCH-ORCH-F1.md** (~120L): SKILL.md template update + flow injection (developer/architect/qa/cowork agents); 8 ACs covering format, rules, injection pattern, parser fixture compatibility. (2) **TASK_ARCH-ORCH-F2.md** (~150L): journalStore.ts (parse+cache), orchestrationHandler.ts extend, +3 unit/integration tests; 14 ACs covering types, parser contract, mtime cache, sprint-id union, zero-value, regression tests, deployment. (3) **TASK_ARCH-ORCH-F3.md** (~155L): dashboard.orchestration.tsx extend (types+DoneTaskGroup+DecisionAccordion+StepCard components), multi-open state (Set<string>), keyboard accessibility; 16 ACs covering types, props, toggle logic, accordion render, empty state, accessibility, security (no dangerouslySetInnerHTML), container rebuild. (4) **TASK_ARCH-ORCH-QA.md** (~115L): live verification procedure (18 ACs) covering container status, API JSON response, dashboard UI, accordion expand/collapse, empty state, multi-open, keyboard nav, security, regression check. **ORCH-STATE UPDATE:** (1) task_board.active_sprints[ORCH-DASH-DECISION-DRILLDOWN].tasks: PM-ORCH→IN_PROGRESS, F1/F2/F3/QA remain TODO (awaiting claim). (2) head.status = planning, head.next_agent = agent-father (F1 owner), head.wip = 1 (PM task only). **DECISION JOURNAL ENTRY:** PM-S1 written (task-id stamped per new F1 format); traces decomposition rationale (sequential zones, atomic tasks, explicit ACs, dev-team Step 3 zone-routed parallel dispatch compatible, WIP=2 discipline clean). **WIP ENFORCEMENT:** At dispatch, WIP=1 (PM only); F1 claim will increment to 2 (PM + agent-father); after agent-father reports F1 merge, PM can claim F2 (total 2). Serialization enforced: F1→F2→F3→QA (blocking chain per architect blueprint §Dependency + Dispatch Order). **COMMIT STATUS:** docs/handoffs/TASK_ARCH-ORCH-F{1,2,3,QA}.md created (unstaged); docs/data/orch/orch-state.json updated (unstaged); docs/agent-memory/decisions/sprint-ORCH-DASH-DECISION-DRILLDOWN.md appended (unstaged); docs/agent-memory/notebooks/pm.md appended (current). Ready for main-terminal staged commit + agent-father spawn.

---

## c286 cycle-191 · 2026-06-04T145500Z

**Status:** RAPID-DATA-LAYER SPRINT CLOSED. PM executed final closeout: all 8 FIX tasks marked DONE-LIVE-VERIFIED, FIX-I moved to backlog as BLOCKED-SOURCE, follow-up tasks recorded. **CLOSURE DETAILS:** Tasks verified by QA live 2026-06-04 14:55Z: FIX-A get_company_profile (shareholders + officers + foreign%), FIX-B get_market_cap (market_cap_billion), FIX-C get_bctc_series (multi-period), FIX-D get_bctc_full structured_data JSON + receivables join, FIX-E price_history 90→730d, FIX-F BCTC scalars (charter_capital, investment_property, reward_fund), FIX-G get_agm_plan (#162, toolCount=160 confirmed, 323 agm_plan rows + 2084 agm_actuals rows), FIX-H insider 90→180d lookback. **ORCH-STATE UPDATE:** task_board.RAPID-DATA-LAYER.status → DONE; all 5 active_sprints tasks → DONE-LIVE-VERIFIED; FIX-I.status → BLOCKED-SOURCE (vnstock API does not expose CEO start_date field); head.status → idle; head.active_task_id → null; head.next_agent → po; head.wip → 0. **FOLLOW-UPS RECORDED:** (1) FU-PROFILE-DATA-VERIFY (already present, BLOCKED-UPSTREAM): foreign_holding_ratio + receivables data-pending after vnstock sync, code verified correct. (2) FU-BCTC-RATIOS-N-A (new): pe/pb still N/A, vnstock ratios path not populated. (3) FU-ACB-AGM-COVERAGE (new): Vietstock batch returned 0 rows for ACB (bank logic proven via BID), data issue not code. (4) FU-FIX-I-SOURCE-RECON (new): CEO start_date needs alternate source investigation before spec. **6 RAPID-ANALYSIS SKILLS UNBLOCKED:** rapid-market-cap-screen, balance-sheet-first-read, four-factor-synthesis, ownership-governance-screen, management-track-record, value-trap-avoidance now have DATA-SERVED live tools (all pipeline → COMPLETE). **NO RETRY COMMITS NEEDED:** all shipped code is production (b275239d, bf9b3105, 46749207, 7a44a291, 0e8c2be0, final 160→160 toolCount stable). PM signature: atomic write verified, head.next_action narrative pinned, no force-add, git status clean orch-state.json.

---

## c285 cycle-189 · 2026-06-04T111432Z

**Status:** RAPID-DATA-LAYER Phase 1 P1 DISPATCH COMPLETE. PM sequenced architect brief (docs/architecture-briefs/2026-06-04-rapid-analysis-data-layer-gaps.md) per handoff protocol: decomposed Phase 1 P1 (FIX-B/FIX-A/FIX-D/FIX-H) into atomic dev tasks with file:line scope + ACs + lean DoD. **CONTEXT VERIFIED:** FIX-B (market-cap tool) DONE-CODE-DEPLOYED (b275239d, 156→157 tools, get_market_cap callable, safe-degrade null path), awaiting vnstock sync data-population (FU-FIXB-COLUMNKEY-VERIFY blocker, transient rate-limit noted). Phase 1 P2 already SHIPPED (2b 2026-06-04T10:07Z: FIX-C get_bctc_series + FIX-E price_history 90→730d, both DONE-LIVE-VERIFIED bf9b3105 + 46749207, mcp-server 159 tools). **DECISION:** P1 P1 dispatch NOW (FIX-A/FIX-D/FIX-H), gates next phase sequencing (P3=FIX-F charter_capital, P4=FIX-G AGM plans recon-dependent, P5=FIX-I officer history source-dependent). **PARALLELIZATION:** FIX-A (get_company_profile, shareholders+officers+foreign%) + FIX-D (bctc_full structured_data+receivables join) independent files, same owner, WIP<=2 rule respected. FIX-H XS (1-line code change, 90→180d cap lift) solo slot. **HANDOFF FILES CREATED:** 3 docs in docs/handoffs/, each ~140L with atomic scope (file:line for bctcFullTools.ts:806, insiderTools.ts:108, new companyProfileTools.ts), unit-test specs (T1–T4 per FIX), live-verify gates (rebuild + gateway call), and risk notes (corpus depth, data staleness, bank-form guard for future P3). **ORCH-STATE UPDATE:** task_board.RAPID-DATA-LAYER.tasks[] appended FIX-A + FIX-D (TODO status), FIX-H already DONE-LIVE-VERIFIED in-list. head.active_task_id=FIX-A, head.status=dispatching, head.wip=0 (ready to claim before spawn), head.next_agent=dev-mcp-server. head.next_action narrative updated with phase 1b sequencing rationale + handoff paths. **WIP ENFORCEMENT:** At dispatch, WIP=0; FIX-A + FIX-D parallel claim allowed (total 2); FIX-H queued after one completes. Rebuild once per agent (rebuild batches all 3 code merges once, then rebuild), qa verifies live via gateway direct tool calls (not badge). **NEXT STEP:** Main terminal dispatches dev-mcp-server with FIX-A + FIX-D handoffs (parallel) + FIX-H standby. PM monitors WORK channel for code-complete reports; when dev reports all 3 ready, PM routes ops for rebuild, then qa for live-verify gate. Phase 1 P1 ships → Phase 1 P2 already live → Phase 3 (FIX-F) unblocked.

---

## c284 cycle-188 · 2026-06-03T001300Z

**Status:** FRONTEND-OPERATOR-UX SPRINT CLOSED. PM executed sprint closure: all 5 tasks marked DONE (live-verified by router raw 2026-06-03 ~00:13Z). **CLOSURE DETAILS:** FOU-1-DESIGN, FOU-2-REQ1, FOU-3-QA already DONE. FOU-3-GW (api-gateway) + FOU-3-FE (frontend) flipped from DONE-PENDING-REBUILD to DONE after ops-rebuild-complete + router live-verify (api-gateway commit 9dd4c1a2, startup log "capability_manifest loaded entries=9 skipped_metadata_keys=2"; curl /health capabilities=true; frontend commit b5e92ee8 healthy; QueName tooltip + 2-axis health rendering live; anti-false-green re-confirmed via docker-stop scenario proving deployed_down holds true over live/data_limited/dark). **ORCH-STATE UPDATE:** FOU-3-GW note appended "live-verified 2026-06-03, /health emits capabilities, anti-false-green confirmed live". FOU-3-FE note appended "live-verified 2026-06-03, QueName tooltip + 2-axis health rendering live; all 4 FOU tests green on source". Sprint status flipped DONE-PENDING-REBUILD → DONE. head.status → idle; head.next_agent → po (next: BCTC-ANALYTICS-LAYER + BAL-0 verify live). Atomic-write HARDENED (fresh-read + tmp + sentinel validation + mv -f). No force-add; git status clean for orch-state.json + pm.md.

---

## c283 cycle-76 · 2026-06-01T163000Z

**Status:** VPS-DEPLOY-PLACEHOLDER-GUARD SPRINT DECOMPOSITION COMPLETE. PM executed full breakdown of architect DECISION-A (docs/architecture-briefs/2026-06-01-vps-deployer-consolidation.md) into 6 atomic tasks per phase: T1 (ops pre-gate recon), T2/T3 (dev parallel migrates), T4 (dev retire+env), T5 (ops redeploy), T6 (qa gate). **CONTEXT VERIFIED:** Operator confirmed VINAHOST_IP=125.212.251.27 LIVE + VULTR_IP=139.180.185.18 DEAD (2026-04-13 decommission), resolving architect's T1 SSH-recon gate. **DECISION-A RATIONALE:** Consolidate deploy-vps-proxy.sh → deploy-vinahost.sh (9+1 services, superset); GUARD-1/3 migrate source is commit 96446b5d (reference implementation, now supersedes old PLACEHOLDER-GUARD-1/2/3/QA + VPS-BS4-INSTALL). **ATOMIZATION STRATEGY:**

1. **T1 (ops, 0.5h):** OPS-RECON pre-gate — SSH Vinahost: AC-1 no placeholder leaks (grep __TOKEN__), AC-2 all 5 services active (systemctl is-active), AC-3 socat alive (pgrep). HARD GATE for T2/T3/T4. Handoff: `TASK_VPS-DEP-T1-OPS-RECON.md`.

2. **T2 (dev-vps-crawls, 1.5h):** GUARD-1 migrate — Port pre-SCP placeholder assert from deploy-vps-proxy.sh into all 9 render blocks in deploy-vinahost.sh. ACs: all 9 blocks follow pattern (TMP+sed+grep+exit), TE_API_KEY 3-token render OK, no false-guards, ≤400L. Gates on T1 PASS. Parallel with T3. Handoff: `TASK_VPS-DEP-T2-GUARD1-MIGRATE.md`.

3. **T3 (dev-vps-crawls, 0.5h):** GUARD-3 migrate — Add section 10 to deploy-vinahost.sh: article-body-fetcher.py scp + chmod + idempotent pip3 install beautifulsoup4. ACs: section 10 present, post-scp ls check, beautifulsoup4 idempotent, no false guards, vps-scripts/article-body-fetcher.py exists. Gates on T1 PASS. Parallel with T2. Handoff: `TASK_VPS-DEP-T3-GUARD3-MIGRATE.md`.

4. **T4 (dev-vps-crawls, 0.5h):** Retire+env — Append GUARD-1 post-deploy SSH verify (VERIFYEOF block) to EOF of deploy-vinahost.sh; delete scripts/deploy-vps-proxy.sh; remove VULTR_IP/USERNAME/PASSWORD from .env/.env.example (add comment re: Vultr decommission). Confirm fetch-tradingeconomics.sh __TE_API_KEY__ sentinel retained (defence-in-depth, AC-8). ACs: verify block appended, VERIFYEOF 2x, deploy-vps-proxy deleted, VULTR_* gone from both files, TE_API_KEY retained. Gates on T1/T2/T3 DONE. Handoff: `TASK_VPS-DEP-T4-RETIRE-ENV.md`.

5. **T5 (ops, 1h):** Deploy+verify — Execute ./scripts/deploy-vinahost.sh (all 10 sections). ACs: dev commits on main (confirm git status), pre-flight check (no stale processes), deploy exit 0, systemctl status all 9 active, 14-feed HTTP=200 within 2h (5 main services × 2–3 push targets each), article-body-fetcher+beautifulsoup4 live, GUARD-1 post-verify clean (zero leaks). Gates on T1/T2/T3/T4 DONE. Unblocks T6. Handoff: `TASK_VPS-DEP-T5-OPS-DEPLOY.md`.

6. **T6 (qa, 1h):** Gate proof — Execute DV-1..DV-8: GUARD-1 pre-SCP assert blocks dirty fixture (test fixture, exit 1), clean passes (exit 0), post-deploy SSH verify detects injected leak, TE_API_KEY empty-string render no leak, deploy-vps-proxy.sh gone (git ls-tree), VULTR_IP removed from .env/.env.example, article-body-fetcher.py + beautifulsoup4 live on VPS, ops 14-feed logs confirm HTTP=200. Gates on T5 DONE. Handoff: `TASK_VPS-DEP-T6-QA-GATE.md`.

**ATOMIZATION RATIONALE:** Each task is ~0.5–1.5h, single file (or file deletion), no shared writes except TASKS.md. T1 pre-gates all others (dependency). T2/T3 parallel (independent files, both on same owner dev-vps-crawls). T4 serialized after T2/T3 (depends on merged state). T5 serialized after T4 (dev commits must be on main first). T6 gates final exit. Total serial path: T1 → (T2||T3) → T4 → T5 → T6. WIP enforcement: max 2 concurrent (T1 blocks until PASS, then T2/T3 launch parallel, T4 awaits both, T5 awaits T4, T6 awaits T5).

**HANDOFF FILES CREATED:** 6 `.md` files in docs/handoffs/ (T1–T6), each ~90–150L with full ACs, implementation approach, test plan, risk flags, related docs. OBSOLETE ROWS RETIRED from TASKS.md: PLACEHOLDER-GUARD-1, PLACEHOLDER-GUARD-2, PLACEHOLDER-GUARD-3, PLACEHOLDER-GUARD-QA, VPS-BS4-INSTALL (replaced by consolidated T1–T6 chain).

**PIPELINE-STATE UPDATES:** head.status=planning, head.next_agent=ops (T1 owner), head.next_action=one-liner (T1 OPS-RECON), head.updated_at/updated_by=pm+timestamp. narrative.current_sprint updated with 6-task chain + brief ref + VULTR decommission confirmation. SSOT BINDING: main terminal stages + commits (serial per feedback_concurrent_commit_race). No pilot-status edits; no branch work.

**WIP ENFORCEMENT:** At dispatch, WIP=0 before spawn. T1 is blocking; once ops reports T1 PASS, WIP increments to 2 (T2+T3 parallel). T4 awaits T2/T3, T5 awaits T4, T6 awaits T5. Max 2 rule prevents pipeline bloat. PM monitors WORK channel for blocker escalations (if any task fails, PM escalates to dev-vps-crawls/ops/qa or architect for design issues).

**FACT VERIFICATION:** Operator confirmation of Vinahost live + Vultr dead independently validated architect's T1 gate (no SSH-recon needed from PM; ops can proceed directly to PASS/FAIL). GUARD-1 implementation reference confirmed (commit 96446b5d, 6/6 QA passed). Deploy-vinahost.sh structure verified (9 sections, superset of deploy-vps-proxy.sh 5 services). Article-body-fetcher.py source exists (deploy-vps-proxy.sh L206–221 reference). .env/.env.example structure confirmed (VULTR_* present, to be removed).

**NEXT STEP:** Main terminal dispatches ops with T1 handoff (TASK_VPS-DEP-T1-OPS-RECON.md) + caveman summary. After T1 PASS reported, main terminal dispatches dev-vps-crawls with T2+T3 handoffs (parallel). T4 gates on T2/T3 completion. After T4 commit, main terminal dispatches ops with T5 handoff. After T5 PASS, main terminal dispatches qa with T6 handoff. After T6 PASS, PM exits sprint (SPRINT EXIT). Total wall-clock: T1=30min, T2||T3=1.5h (wall), T4=30min, T5=1h, T6=1h. Critical path ≈ 4.5h real-time (assuming no blockers).

---

## c282 cycle-75 · 2026-05-30T000000Z

**Status:** DPI TASK DECOMPOSITION COMPLETE. PM executed full breakdown of DPI-ARCH design (docs/handoffs/DPI-ARCH.md, architect complete 2026-05-30) into 4 atomic per-zone dev tasks + downstream ops/qa/po sequence. **DESIGN SUMMARY:** 4 root-caused code bugs spanning macro-indicators (DPI-1: FX canonical source, DPI-2: stale computedAt) + mcp-server (DPI-3: Brent/Gold delta, DPI-4: foreign-flow data loss + critical R-1 race). **ATOMIZATION STRATEGY:** Zone-parallel decomposition (no shared files; macros & mcp-server independent). Rebuild order enforced: mcp-server FIRST (DPI-3/4 write fresh data), macro-indicators SECOND (reads SBV written by mcp-server cron). **TASK BREAKDOWN:**

1. **DPI-1** (dev-macro-indicators, docs/handoffs/DPI-1.md) — SBVRateSQLiteAdapter (infrastructure/repositories.go ~60L new adapter) + usecases.go patch (~6L change) + main.go wiring (1L). Scope: ~70L. Atomic: single adapter struct. Deps: SBVRatePort interface exists; sbv_rates table exists (populated by mcp-server cron, already running). ACs: AC-1 constructor exists; AC-2 staleness guard (6h window); AC-3/4 Execute() calls adapter + main.go wires it; AC-5 safe-degrade (yields to Yahoo on stale/absent); AC-6 live gate (both `get_macro_snapshot` & `get_cycle_bootstrap` USDVND identical). Test: adapter with mock DB (fresh/stale/absent scenarios); integration live comparison.

2. **DPI-2** (dev-macro-indicators, docs/handoffs/DPI-2.md) — Delete `const fixtureComputedAt` (usecases.go L44-45); inline `time.Now().UTC().Format(time.RFC3339)` in Execute() body; update 2 references (CarryTradeInput + YieldSpreadInput). Scope: ~2L delete + ~6L change. Atomic: constant → inline variable. Deps: time package already imported. ACs: AC-1 constant removed; AC-2 inline var present; AC-3/4 both references updated to computed variable; AC-5 test assertion scan (grep "2026-05-23" in usecases_test.go, none found per architect audit); AC-6 live gate (carry.computedAt & yield.computedAt show today's date, not stale 05-23). Test: Execute() produces RFC3339 timestamp matching today; no hardcoded constant in source.

3. **DPI-3** (dev-mcp-server, docs/handoffs/DPI-3.md) — Pre-tx prev-close reads (commodity_prices_history) + computeDelta() helper (module-private) + updated upsertMacroPrice ON CONFLICT (explicitly updates change_amt/pct) + call-site updates (L469-473, pass computed deltas). Scope: ~25L change in storeCommoditySnapshot(). Atomic: delta computation logic within one function. Deps: commodity_prices_history table exists (populated by existing yahooFetch cron). ACs: AC-1 prev-close queries before runTransaction(); AC-2 computeDelta() helper exists, null-safe; AC-3 prepared statement ON CONFLICT updates change cols; AC-4 call-sites pass deltas (not hardcoded 0); AC-5 first-run tolerance (deltas=0 if <2 history rows, next tick non-zero); AC-6 live gate (BRENT & GOLD change_pct non-zero, directional). Test: computeDelta() unit (various input pairs); prev-close query with mock history (fresh/absent); UPSERT with conflict; integration live two-tick comparison.

4. **DPI-4** (dev-mcp-server, docs/handoffs/DPI-4.md) — PRIMARY FIX: ohlcvForeignFlowStore.ts UPDATE→INSERT…ON CONFLICT UPSERT (close=0 for NOT NULL, stub row creation). SECONDARY CRITICAL FIX: server.ts:1078 INSERT OR REPLACE→ON CONFLICT(code, date) DO UPDATE SET (preserves foreign flow cols, R-1 race mitigation). TERTIARY AUDIT: taOhlcvBackfillJob.ts L108 search for INSERT OR REPLACE (likely same pattern, apply fix if found). Scope: ~15L ohlcvForeignFlowStore.ts + ~10L server.ts + audit doc. Atomic: dual-write-path coordination (both must preserve foreign flow cols or race loss occurs). Deps: daily_ohlcv schema (close NOT NULL no default, foreign flow cols nullable); schema-market-data.ts confirms. **CRITICAL R-1 RACE:** server.ts INSERT OR REPLACE is row-destructive (DELETE+INSERT); stub rows from DPI-4 UPSERT will be wiped on real OHLCV push. MUST be fixed same task or intermittent data loss results. ACs: AC-1 ohlcvForeignFlowStore.ts UPSERT present; AC-2 server.ts ON CONFLICT (not INSERT OR REPLACE); AC-3 taOhlcvBackfillJob audit doc; AC-4 JSDoc updated (mentions stub creation + preservation); AC-5 schema compliance confirmed (close=0 satisfies NOT NULL); AC-6 live gate (`get_foreign_flow(HPG)` populated); AC-7 stub-race test (stub inserted, real OHLCV pushed, foreign flow survives). Test: ohlcvForeignFlowStore UPSERT unit (insert absent, update existing); server.ts ON CONFLICT unit (OHLCV fields updated, foreign flow omitted); integration stub-race scenario (direct DB verify foreign_buy_vol preserved).

**ATOMIZATION RATIONALE:** Each task touches ONE file (or ONE logical write-seam), scoped to ~2h solo dev time. DPI-1 adds new adapter (infrastructure layer); DPI-2 modifies constant (application layer); DPI-3 updates commodity-fetch transaction seam (infrastructure); DPI-4 coordinates dual OHLCV write paths (infrastructure, but critical ordering to avoid race). Zone split (macro-indicators vs mcp-server) allows parallel dispatch; rebuild order (mcp-server→macro-indicators) ensures downstream reads see fresh data.

**WIP ENFORCEMENT:** Max 2 concurrent. Zones are independent (no shared files); can dispatch both dev-macro-indicators & dev-mcp-server simultaneously. Within mcp-server, DPI-3 & DPI-4 can run parallel (independent files, no shared DB schema change, but both must land before OPS rebuild). Within macro-indicators, DPI-1 & DPI-2 can run parallel (different files, no dependency).

**REBUILD SEQUENCE (for ops, not dev):** (1) mcp-server `build` + `up -d --no-deps --force-recreate` (DPI-3+4 live → fresh deltas & foreign-flow persistence enabled). (2) macro-indicators `build` + `up -d --no-deps --force-recreate` (DPI-1+2 live → read SBV canonical & fresh timestamps). Soft order only (DPI-3/4 first ensures delta data available on macro-indicators next request), but not hard-blocking.

**SSOT UPDATES:** docs/TASKS.md § DPI sprint updated: 4 task rows (DPI-1/2/3/4) with WIP-READY status + handoff paths + parallel dispatch note + rebuild sequence. Handoff files created: docs/handoffs/DPI-{1,2,3,4}.md (each ~60-140L with full ACs, implementation approach, risk flags, test strategy).

**NEXT STEP:** Main terminal dispatches dev-macro-indicators (DPI-1+2, parallel) and dev-mcp-server (DPI-3+4, parallel) in ONE message with caveman summary + handoff paths. WIP count before dispatch: 14 (currently open tasks). After dispatch: +4 (DPI tasks) = max 18 (but rule is max 2 In Progress at once, measured per dev-agent, not global). Dev targets completion within sprint cycle. PM monitors WIP, escalates blockers (architect escalation for design issues; ops escalation for infra). Ops standby for REBUILD sequence. QA stands ready for post-rebuild live re-probe + race gate verification (DPI-4 critical).

---

## c282 cycle-74 · 2026-05-28T000000Z

**Status:** MACRO-LIVE-PRICES SPRINT — TASK DECOMPOSITION & MLP-OPS TIMING RECOMMENDATION COMPLETE. PM executed decomposition of MLP-DEV per architect brief (docs/architecture-briefs/2026-05-28-macro-live-prices.md, CONFIRMED Option A, 26h staleness, SPRINT-S scope). Architect design complete (brief 7753c172): SQLiteCommodityRepository in infrastructure/repositories.go, env-gate wiring in cmd/server/main.go, one docker-compose.yml env line (COMMODITY_LIVE_MODE=true), T-MLP-1..T-MLP-10 tests + T-MLP-11/12 QA gates. **ATOMIZATION STRATEGY:** MLP-DEV decomposed into 5 serial subtasks (all zone=apps/macro-indicators/, one file-per-task for atomic commit + serialized dependency chain): (1) **MLP-D1** (repositories.go + test) — SQLiteCommodityRepository struct + FetchPrices() + fetchCommodityPricesFromDB() helper + T-MLP-1/2/3 infrastructure tests (in-memory :memory: injection). (2) **MLP-D2** (usecases_test.go) — T-MLP-4/5 application tests (resolveMarketPrices live pass-through + fixture fallback). (3) **MLP-D3** (cmd/server/main.go) — env-gate wiring (COMMODITY_LIVE_MODE read, branch to SQLiteCommodityRepository vs HTTPCommodityFetcher). (4) **MLP-D4** (pkg/interface/http/ response struct) — add DataSource string field to snapshot response, set live/fixture flag at serialization. (5) **MLP-D5** (docker-compose.yml + fixture-mode verification) — add COMMODITY_LIVE_MODE=true, verify all existing tests pass (T-MLP-6..T-MLP-10 fixture-mode green). Dependency chain: D1→D2→D3→D4→D5 (each unblocks the next). WIP enforcement: max 2 concurrent. **MLP-OPS TIMING RECOMMENDATION:** After all 5 dev tasks land + commit, MLP-OPS (force-recreate macro-indicators) should be DEFERRED to next tick or background session. RATIONALE: host is CURRENTLY LOADED (parallel session running pdf-extractor re-extractions + mcp-server sprint work, 8GB Docker cap). MLP-OPS rebuild is host-safe (Go code + test only, CPU-only, no network), but the force-recreate step will cause mcp-server pool to renegotiate (compose down/up churn). RECOMMENDATION: (a) dev-macro-indicators serial D1→D5 run NOW (safe host-load); (b) after commit, flag MLP-OPS as READY-TO-RUN (not BLOCKED yet); (c) monitor other sessions; (d) when the parallel pdf-extractor session completes, PM dispatches ops in a quieter tick. **SAFETY MARGIN:** 26-hour staleness bound means macro-indicators fixtures are still valid for ~6-8h before staleness becomes critical; live wiring is not time-critical. OPS can safely wait until host quiets. **EXCEPTION:** if user explicitly requests live wiring urgently, ops can run immediately (force-recreate is reversible; if host panics, restart clears it). **NEXT STEP:** Dispatch dev-macro-indicators with the 5 task handoffs (MLP-D1 main load, D2-D5 lightweight); timer starts; ops standby. HANDOFF SERIES: (1) TASK_MLP-D1.md (SQLiteCommodityRepository + infrastructure tests), (2) TASK_MLP-D2.md (application tests), (3) TASK_MLP-D3.md (composition root env gate), (4) TASK_MLP-D4.md (HTTP response DataSource field), (5) TASK_MLP-D5.md (docker-compose + fixture-mode verification + final commit).

---

## c282 cycle-73 · 2026-05-27T212155Z

**Status:** SIG-IMPL-GATE PHASE 2 — TASK DECOMPOSITION & HANDOFF COMPLETE. PM executed full decomposition of REQ_SIG-IMPL-GATE.md (6 tasks) per PO spec-gate verdict (APPROVED 2026-05-27T21:16:12Z, 2 hardening notes: HN-1 cron rationale FALSE but decision correct; HN-2 anti-runaway order canonical from architect). Created 5 comprehensive dev-task handoff files + 1 QA-task handoff (sequence: dev-mcp-server serial T1→T2→T3→T4→T5, ops REBUILD, qa T6 GATE-PROOF, po EXIT). **PHASE 2 DELIVERY:** (1) **TASK_SIG-G-T1.md** (schema+store, improveCheckStore.ts, 6 ACs): AC-1 idempotent initSystemTables, AC-2 insertSnapshot returns sequential ids, AC-3 getBaseline null on absent-table, AC-4 returns most-recent, AC-5 fail-loud on missing signal_type, AC-6 no regression existing tables. Test count: 6. (2) **TASK_SIG-G-T2.md** (domain detect, degradationRules.ts, 8 ACs): AC-1 DEGRADED 15pp delta, AC-2 no-detect 1pp delta, AC-3 PERSISTENTLY_LOW, AC-4 sample-count gate, AC-5 known-type hypothesis, AC-6 _default fallback, AC-7 zero-infra imports, AC-8 DEGRADATION_CAUSE_MAP const. Test count: 8. (3) **TASK_SIG-G-T3.md** (orchestrator+cron, selfImproveOrchestratorJob.ts, 9 ACs): AC-1 success no-degradation, AC-2 improve_check_log rows inserted, AC-3 no Telegram on empty, AC-4 exactly-1 Telegram on findings, AC-5 fail-loud table-absent, AC-6 coverage-gap in WORK, AC-7 cooldown-guard duplicate-skip, AC-8 anti-runaway max-2 (HN-2 canonical order DEGRADED>PERSISTENTLY_LOW>COVERAGE_GAP), AC-9 deps injection. Cron slot: `2 9 * * *` (HN-1: NOT `0 9 * * *`, live bctcOverdueCheck=daily). Test count: 9. (4) **TASK_SIG-G-T4.md** (D-IMPROVE proposal-doc, improvementSignalWriter.ts, 8 ACs): AC-1 doc written with C-1 target_agent/target_files, AC-2 kebab-case target_agent, AC-3 JSON-parseable target_files[], AC-4 DASHBOARD row appended, AC-5 cooldown-guard doc-dedup, AC-6 doc-write failure non-fatal (C-5), AC-7 _default→UNRESOLVED, AC-8 DASHBOARD row append-not-prepend. C-1 structured fields: `target_agent`/`target_files[]` derived from typed `FIX_AREA_TO_AGENT` constant (no prose parse). Test count: 8. (5) **TASK_SIG-G-T5.md** (per-path kill-switch, C-4 HARD, 5 ACs): AC-1 all-paths-false-by-default, AC-2 path-specific enable, AC-3 unknown-type fail-safe, AC-4 global-flag REJECTED (hard), AC-5 typed signature DispatchPath. Kill-switch: `DISPATCH_PATHS as const` + `DispatchPath` union type + `isAutoDispatchEnabled(path: DispatchPath)` reads `SELF_IMPROVE_AUTO_DISPATCH_{PATH_UPPER}`. No global flag possible (compile-time enforcement). docker-compose.yml: commented env vars w/ "flip ONLY after QA GATE-PROOF" note. Test count: 5. (6) **TASK_SIG-G-T6.md** (QA GATE-PROOF — prepared by pm, executed by qa): GATE-PROOF-1..5 procedure per brief §5 (inject violation INTO degradationRules.ts subject code → RED test → remove → GREEN → record evidence). No test baseline regression (9408 PASS floor, 348 FAIL ceiling). **TOTAL NEW TESTS:** 36 (T1+T2+T3+T4+T5) + QA evidence = 36 testable ACs + gate proof. **HARD CONSTRAINTS HONORED:** (1) NO new Docker service, (2) NO new cowork agent, (3) ONE new cron slot (`2 9 * * *`), (4) shadow mode at ship (all paths false), (5) test-baseline floor honored, (6) single-writer DB (inside mcp-server), (7) serialized commits main-only, (8) ops REBUILD after dev change, (9) C-5 fail-loud-skip isolation (doc-write not primary), (10) no gate/audit logic self-edits (ADD-only). **HARDENING NOTES CARRIED:** (1) HN-1 embedded in T3 handoff: cron is `2 9 * * *`, blueprint rationale ("bctcOverdueCheck weekdays") is FALSE (live=daily), decision is CORRECT; (2) HN-2 embedded in T3 handoff: anti-runaway order canonical = `DEGRADED > PERSISTENTLY_LOW > COVERAGE_GAP` (architect, not REQ), AC-T3-8 test asserts this order. **WIP ENFORCEMENT:** All 5 dev tasks on MAIN serially ordered (T1→T2→T3→T4→T5), blocking each other by dependency (improveCheckStore.ts types used by T2, T2 called by T3, T3 extended by T4/T5). Max WIP=2 rule applied: at any moment, dev-mcp-server max 2 tasks active (one In Progress, one Ready). **SSOT UPDATES:** docs/TASKS.md rows SIG-G-T1..T6 already PO-inserted (status PENDING → updated by pm to include full AC detail via handoff references). **PM SIGNATURE:** Comprehensive 5-task handoff suite with exact AC verdicts, dependency chain, HN notes, implementation patterns (mirror accuracyDigestJob.ts, injectable deps, fail-loud-first). All handoff files created, UNSTAGED per main-terminal serialization rule (feedback_concurrent_commit_race). Updated PM notebook (this entry). Ready for dev-mcp-server dispatch via main terminal.

---
