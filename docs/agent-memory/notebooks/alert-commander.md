# Alert Commander — Notebook

**Last updated:** 2026-08-26 00:08 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c257 · 2026-08-25T16:08:08Z (slot=alert-commander-critical, tick=16:00)
- Signals: no fresh tick-snapshot (no cycle-snapshot-16:06.json exact-tick match; 16:04.json ignored per exact-match rule) → direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price) + `get_agent_signals`(agent,2h,status=all) → "Không có tín hiệu mới" (aggregate empty, all 6 consumed types zero). No candidate ticker. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 0 candidates (no ticker-named signal this cycle) → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no stopLossHit/singleDayDrop>5% candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean ("Không có tín hiệu khủng hoảng nào"); corp-reputation sub-50: BSR24.0(danger)/PLX34.0(warning)/VCB46.5(warning) deteriorating, unchanged from c256, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.4036, gk_vol_20d_pct=16.01, drawdown_252d=16.38%) | `get_vn_liquidity_state` OMO+interbank blocked (honest-NULL, source unreachable) | `get_foreign_room` market_saturation_pct=24.25% (top-10 tickers; VCB room_utilization=66.93% highest, outflow_z_5d=-1.48) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1791.41(+2.63), VN market CLOSED (outside 02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2142.

## c258 · 2026-08-25T20:08:27Z (slot=alert-commander-critical, tick=20:00)
- Signals: no exact tick-snapshot match for 20:06/20:08 (cycle-snapshot-20:05.json ignored per exact-match rule) → direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_alerts`(price) + `get_agent_signals`(agent,2h,status=all). Only bus hit: freshness-sla-monitor `urgent_news` id11424 (sbv_fx source stale 36min, infra SLA-breach noise, not real market news, no ticker) → suppressed per standing rule. No candidate ticker. Fired: 0 | Suppressed: 1 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 0 candidates (no ticker-named signal this cycle) → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no stopLossHit/singleDayDrop>5% candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean ("Không có tín hiệu rủi ro pháp lý"); bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean ("Không có tín hiệu khủng hoảng nào"); corp-reputation sub-50: BSR24.0(danger)/PLX34.0(warning)/VCB46.5(warning) deteriorating, unchanged from c257, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.4036, gk_vol_20d_pct=16.01, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored (macro-indicators service unavailable, honest-SKIP) | `get_foreign_room` market_saturation_pct=24.25pct (top-10 tickers; VCB room_utilization=66.93% highest, outflow_z_5d=-1.48) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1791.41(+2.63), VN market CLOSED (outside 02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2146.

## c259 · 2026-08-26T00:08:07Z (slot=alert-commander-critical, tick=00:00)
- Signals: no exact tick-snapshot match (cycle-snapshot-00:04.json 1min-fresh but TICK=00:05/00:08 mismatch; cycle-snapshot-00:05.json stale 24h — exact-match rule) → direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price) + `get_agent_signals`(agent,2h,status=all) + per-type explicit filters (urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity — all "Không có tín hiệu mới"). Only bus item: alert-engine `verified_decision` id11427 (BID, produced-type echo, status=read, not a consumed type). No candidate ticker. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 0 candidates (no ticker-named signal this cycle) → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no stopLossHit/singleDayDrop>5% candidate (largest watchlist move DBC+6.63%/BSR-2.55%, stale as-of 2026-08-25 08:59, not fresh).
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean ("Không có tín hiệu khủng hoảng nào"); corp-reputation sub-50: BSR24.0(danger)/PLX34.0(warning)/VCB46.5(warning) deteriorating, unchanged from c258, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.4036, gk_vol_20d_pct=16.01, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored (macro-indicators service unavailable, honest-SKIP) | `get_foreign_room` market_saturation_pct=24.25% (top-10 tickers; VCB room_utilization=66.93% highest, outflow_z_5d=-1.48) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1791.41(+2.63), VN market CLOSED (outside 02:00-08:59 UTC). Note: `get_system_status` DB section listed unresolved "disk I/O error"/"database disk image is malformed" (push-news pipeline) — did not affect this cycle's own tool calls (all returned valid data); infra diagnosis is ops/developer's job (not_my_job), not escalated. Silent exit — no MARKET/WORK send. `log_agent_work` id=2149.
