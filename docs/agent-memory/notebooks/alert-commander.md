# Alert Commander — Notebook

**Last updated:** 2026-08-24 08:07 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c224 · 2026-08-24T07:50:05Z (slot=alert-commander-market, tick=07:45)
- Signals: no exact-minute tick-snapshot (07:47 absent) — direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`+`get_crisis_early_warning`+`get_agent_signals`(alert-commander,2h)+`get_kinhdich_reading`(VIC,VHM,KDH,VCB)+`get_positions`+`get_market_snapshot`. Bus (2h) → 4: `VERIFIED_DECISION` id11256(KDH)/11257(VCB)/11258(VHM)/11259(VIC), all alert-engine correlation stubs, not consumed types. No `urgent_news`/`price_anomaly`/`verified_chain`/`chain_catalyst`/`legal_risk`/`crisis_velocity` this cycle. Fired: 0 | Suppressed: 4 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: none in bus this cycle.
- Watchlist-opp: same VIC/VHM fund-inflow story ("VIC và VHM có thể được các quỹ mua vào gần 23.500 tỷ đồng") + KDH BB-breakout — VIC +4.63%, VHM +2.37%, KDH +3.06%, VCB +0.17% intraday. Checked kinhDich for all 4: none returns BUY (VIC=BAN 100%, VHM=GIU 25%, KDH=GIU 100%, VCB=GIU 63%) — cond2 fails for every candidate, gate closed before newsSentiment/majority checked.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 71,400, -11.1% total P/L). FPT -0.83% intraday per market_context — no single-day drop >5%; `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"). Gate fails at cond1+cond2.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR (44.0, improving) + PLX (48.0, improving), no CRITICAL. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=15.99, drawdown_252d=16.38%) | `get_vn_liquidity_state`: OMO+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.887 | `get_foreign_room` market_saturation_pct=5.85% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index +1.17%, breadth 199up/115down/59flat, Kinh Dich market-wide Quan(20) TRUNG TÍNH conf 38%. Silent exit — no MARKET/WORK send. `log_agent_work` id=2097.

## c225 · 2026-08-24T08:07:00Z (slot=alert-commander-market, tick=08:00)
- Signals: no exact-minute tick-snapshot (08:04 absent, nearest 08:02 stale) — direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`+`get_crisis_early_warning`+`get_agent_signals`(alert-commander,2h + 6 per-type)+`get_kinhdich_reading`(VIC,VHM,PDR,KDH)+`get_positions`+`get_market_snapshot`. Bus (2h) → 4: `VERIFIED_DECISION` id11256(KDH)/11257(VCB)/11258(VHM)/11259(VIC), all alert-engine correlation stubs, not consumed types. All 6 per-type bus queries clean — no `urgent_news`/`price_anomaly`/`verified_chain`/`chain_catalyst`/`legal_risk`/`crisis_velocity` this cycle. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: none in bus this cycle.
- Watchlist-opp: same VIC/VHM fund-inflow story continues — VIC +4.63%, PDR +4.47%, KDH +3.06%, VHM +2.37% intraday. Checked kinhDich for VIC/VHM/PDR/KDH: none returns BUY (VIC=BAN 100%, VHM=GIU 25%, PDR=GIU 63%, KDH=GIU 100%) — cond2 fails for every candidate, gate closed before newsSentiment/majority checked.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 71,400, -11.1% total P/L, unchanged). FPT -0.83% intraday per market_context — no single-day drop >5%; `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"). Gate fails at cond1+cond2.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR (44.0, improving) + PLX (48.0, improving), no CRITICAL. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=15.99, drawdown_252d=16.38%) | `get_vn_liquidity_state`: OMO+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.969 | `get_foreign_room` market_saturation_pct=5.85% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index +1.17%, breadth 199up/115down/59flat, Kinh Dich market-wide Quan(20) TRUNG TÍNH conf 38%. Silent exit — no MARKET/WORK send. `log_agent_work` id=2100.

## c226 · 2026-08-24T08:07:04Z (slot=alert-commander-critical, tick=08:00)
- Signals: no exact-minute tick-snapshot (08:04 absent) — direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`(days=1,hours_back=6)+`get_crisis_early_warning`+`get_agent_signals`(bulk 2h + 6 per-type)+`get_kinhdich_reading`(VIC,VHM,KDH,PDR,HPG,DXG,BSR,FRT)+`get_positions`+`get_macro_calendar`+`get_market_snapshot`. Bus (2h) → 4: `VERIFIED_DECISION` id11256(KDH)/11257(VCB)/11258(VHM)/11259(VIC), alert-engine correlation stubs, not consumed types. No `urgent_news`/`price_anomaly`/`verified_chain`/`chain_catalyst`/`legal_risk`/`crisis_velocity` this cycle. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: none in bus this cycle.
- Watchlist-opp: same VIC/VHM fund-inflow rally continues (VIC+4.63%,VHM+2.37%,KDH+3.06%,PDR+4.47%,HPG+2.53%,DXG+2.16%,BSR+2.05%,FRT+2.79%) — checked kinhDich for all 8: none returns BUY (VIC=BAN 100%, VHM=GIU 25%, KDH=GIU 100%, PDR=GIU 63%, HPG=GIU 100%, DXG=THAN TRONG 13%, BSR=GIU 38%, FRT=THAN TRONG 50%) — cond2 fails for every candidate, gate closed before newsSentiment/majority checked.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 71,400, -11.1% total P/L, unchanged). FPT -0.83% intraday per market_context — no single-day drop >5%; `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"). Gate fails at cond1+cond2.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR (44.0, improving) + PLX (48.0, improving), no CRITICAL. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=15.99, drawdown_252d=16.38%) | `get_vn_liquidity_state`: OMO+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.969 | `get_foreign_room` market_saturation_pct=5.85% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index +1.17%, breadth 199up/115down/59flat, Kinh Dich market-wide Quan(20) TRUNG TÍNH conf 38%. Silent exit — no MARKET/WORK send. `log_agent_work` id=2098.
