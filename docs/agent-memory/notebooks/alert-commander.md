# Alert Commander — Notebook

**Last updated:** 2026-08-26 05:53 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c274 · 2026-08-26T05:25:26Z (slot=alert-commander-market, tick=05:15)
- Signals: no tick-snapshot exact match for current tick (`cycle-snapshot-05:22.json` absent) → treated absent per protocol, direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price, none active) + `get_volatility_indicators` + `get_vn_liquidity_state` (OMO+interbank honest-NULL, source unreachable) + `get_foreign_room` + `get_legal_risk_signals(days=1,hours_back=6)` (clean) + `get_crisis_early_warning` (primary indicator clean) + `get_agent_signals`(agent,2h,status=all). Bus carried 2 alert-engine `verified_decision` echoes (FPT volume_spike, VCB news_mention — produced-type, not consumed) + same 3 `chain_catalyst` ids as c271-273 (news-scout, still in 2h window). Fired: 0 | Suppressed: 3 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 4h49m).
- ChainCatalyst: 0 fired | 3 suppressed (record_signal_outcome re-applied ids 11344/11345/11346, unchanged disposition) | event_types: gold safe-haven inflow (no ticker, no named external catalyst, conf 0.80), VN-Index breadth-divergence (domestic recap, conf 0.80), banking credit-differentiation (conf 0.40 < 0.75 threshold).
- Watchlist-opp: 2 candidates via `get_kinhdich_reading` — FPT GIU/38% (Que Co 18), VCB GIU/38% (Que Tinh 48) — none BUY, gate fails.
- Position-danger: `get_positions()` FPT only (avg 80.3k, current 72.3k, -10.0% P/L, -40.0M VND); `get_portfolio_risk()` VaR(95%) -0.1%, max drawdown -0.6% — no singleDayDrop>5%, no stopLossHit → gate fails.
- CRITICAL-always: `get_legal_risk_signals` clean; bus carries no legal_risk/verified_chain/crisis_velocity. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged from c273 (BSR20.0 danger; FPT/KDH/PLX/SSI/VCB/VIC warning), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.48, drawdown_252d=16.38%) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.48) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1795.63(-11.71 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2144.

## c275 · 2026-08-26T05:38:26Z (slot=alert-commander-market, tick=05:30)
- Signals: no exact-tick cycle-snapshot match (nearest `cycle-snapshot-05:34.json` off-tick by write time) → treated absent per protocol, direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present, no Global-Liquidity line) + `get_macro_calendar` (pivot_window_active=true) + `get_market_context`(6h) + `get_alerts`(price, none active) + `get_volatility_indicators` (vol_regime=NORMAL) + `get_vn_liquidity_state` (OMO+interbank honest-NULL, source unreachable) + `get_foreign_room` (market_saturation 5.76%) + `get_legal_risk_signals(days=1,hours_back=6)` (clean) + `get_crisis_early_warning` (primary indicator clean) + `get_agent_signals`(agent,2h,status=all). Bus carried 2 alert-engine `verified_decision` echoes (FPT volume_spike, VCB news_mention — produced-type, not consumed) + same 3 `chain_catalyst` ids as c272-274 (news-scout, still in 2h window). Fired: 0 | Suppressed: 3 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 5h1m).
- ChainCatalyst: 0 fired | 3 suppressed (record_signal_outcome re-applied ids 11344/11345/11346, unchanged disposition) | event_types: gold safe-haven inflow (no ticker, no named external catalyst, conf 0.80), VN-Index breadth-divergence (domestic recap, conf 0.80), banking credit-differentiation (conf 0.40 < 0.75 threshold).
- Watchlist-opp: 2 candidates via `get_kinhdich_reading` — FPT GIU/38% (Que Co 18), VCB GIU/38% (Que Tinh 48) — none BUY, gate fails.
- Position-danger: `get_positions()` FPT only (avg 80.3k, current 72.3k, -10.0% P/L, -40.0M VND); `get_portfolio_risk()` VaR(95%) -0.1%, max drawdown -0.6% — FPT +2.26% today, no singleDayDrop>5%, no stopLossHit → gate fails.
- CRITICAL-always: `get_legal_risk_signals` clean; bus carries no legal_risk/verified_chain/crisis_velocity. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged from c274 (BSR20.0 danger; FPT/KDH/PLX/SSI/VCB/VIC warning), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.48, drawdown_252d=16.38%) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.48) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1795.63(-11.71 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2145.

## c276 · 2026-08-26T05:53:33Z (slot=alert-commander-market, tick=05:45)
- Signals: no exact-tick cycle-snapshot match (`cycle-snapshot-05:45.json` absent, nearest `-05:50.json` off-tick) → treated absent per protocol, direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present, no Global-Liquidity line) + `get_macro_calendar` (pivot_window_active=true) + `get_market_context`(6h) + `get_alerts`(price, none active) + `get_volatility_indicators` (vol_regime=NORMAL) + `get_vn_liquidity_state` (OMO+interbank honest-NULL, source unreachable) + `get_foreign_room` (market_saturation 5.76%) + `get_legal_risk_signals(days=1,hours_back=6)` (clean) + `get_crisis_early_warning` (primary indicator clean) + `get_agent_signals`(agent,2h,status=all). Bus carried 2 alert-engine `verified_decision` echoes (FPT volume_spike, VCB news_mention — produced-type, not consumed) + same 3 `chain_catalyst` ids as c273-275 (news-scout, still in 2h window). Fired: 0 | Suppressed: 3 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 5h17m).
- ChainCatalyst: 0 fired | 3 suppressed (record_signal_outcome re-applied ids 11344/11345/11346, unchanged disposition) | event_types: gold safe-haven inflow (no ticker, no named external catalyst, conf 0.80), VN-Index breadth-divergence (domestic recap, conf 0.80), banking credit-differentiation (conf 0.40 < 0.75 threshold).
- Watchlist-opp: 2 candidates via `get_kinhdich_reading` — FPT GIU/38% (Que Co 18), VCB GIU/38% (Que Tinh 48) — none BUY, gate fails.
- Position-danger: `get_positions()` FPT only (avg 80.3k, current 72.3k, -10.0% P/L, -40.0M VND); `get_portfolio_risk()` VaR(95%) -0.1%, max drawdown -0.6% — FPT +2.26% today, no singleDayDrop>5%, no stopLossHit → gate fails.
- CRITICAL-always: `get_legal_risk_signals` clean; bus carries no legal_risk/verified_chain/crisis_velocity. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged from c275 (BSR20.0 danger; FPT/KDH/PLX/SSI/VCB/VIC warning), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.48, drawdown_252d=16.38%) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.48) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1795.63(-11.71 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2146.
