# Alert Commander — Notebook

**Last updated:** 2026-08-25 02:09 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c232 · 2026-08-24T20:07:44Z (slot=alert-commander-critical, tick=20:00)
- Signals: CYCLE_SNAPSHOT hit tick=20:03 (2min fresh) → macro from snapshot, skip `get_macro_snapshot`. Direct `get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`(error "TA Service volatility: 500 internal error", SKIP)+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`(days=1,hours_back=6)+`get_crisis_early_warning`+`get_agent_signals` x5 (bulk 2h + verified_chain + urgent_news + chain_catalyst + price_anomaly). Bus (2h) → 0: ALL 5 calls "Không có tín hiệu mới" — fully clean, same rolling-2h-TTL pattern as c230/c231. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 signals in bus this cycle (n/a — none to evaluate).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (bus empty) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (session recap bullish: VN-Index +20.66, breadth green — VIC+4.63%, PDR+4.47%, KDH+3.06% top movers, no red >1.5%). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity` type. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | `get_volatility_indicators` errored (TA Service 500), standard thresholds applied | `get_vn_liquidity_state`: omo+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=99.02 | `get_foreign_room` market_saturation_pct=24.59% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index 1788.78 (+20.66). Silent exit — no MARKET/WORK send. `log_agent_work` id=2110.

## c233 · 2026-08-25T00:08:48Z (slot=alert-commander-critical, tick=00:00)
- Signals: CYCLE_SNAPSHOT hit tick=00:05 (2min fresh) → macro from snapshot, skip `get_macro_snapshot`. Direct `get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`(days=1,hours_back=6)+`get_crisis_early_warning`+`get_agent_signals`(bulk 2h). Bus (2h) → 2: `urgent_news` (freshness-sla-monitor SLA-breach, infra noise — suppressed per standing rule) + `verified_decision` (alert-engine gold macro-deviation recap, out-of-scope consumed-type, no action). Fired: 0 | Suppressed: 1 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 signals in bus this cycle (n/a — none to evaluate).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (bus carries only infra-noise urgent_news + out-of-scope verified_decision) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (last close: VIC+4.63%, PDR+4.47%, KDH+3.06% top movers, worst SAB-1.50% — no red beyond that). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity` type. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=15.99, drawdown_252d=16.38%) | `get_vn_liquidity_state`: omo+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.985 | `get_foreign_room` market_saturation_pct=24.59% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN market CLOSED (outside 02:00–08:59 UTC) — vnIndex unavailable this cycle. Silent exit — no MARKET/WORK send. `log_agent_work` id=2113.

## c234 · 2026-08-25T02:09:49Z (slot=alert-commander-market, tick=02:00)
- Signals: no tick-snapshot hit for exact minute (02:06) → direct `get_cycle_bootstrap`. Bus (bootstrap 2h) → 15 signals: 4 `chain_catalyst`→all (no stockCode, regime_adj_score 0.4-0.6, all <0.75 NEUTRAL threshold → suppressed, `record_signal_outcome` called each), 4 `urgent_news`→alert-commander targeted (KDH/VIC/HPG/FPT, no conviction field, evaluated direct vs Firing Gate), 2 `urgent_news` freshness-sla-monitor SLA-breach infra noise (suppressed per standing rule), 3 `verified_decision`+2 `verified_decision`(ta_oversold HUT/VHM) from alert-engine (out-of-scope produced-type, not consumed, no action). Fired: 0 | Suppressed: 4 (chain_catalyst, recorded) + 2 (infra noise) | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 4 suppressed (ids 11301/11302/11309/11310, regime_adj_score/10=0.5/0.5/0.4/0.6, all <0.75) | event_types: domestic VN-Index recap x2, US tech/Iran spillover x1 (no ticker), Iran sanctions trade_war x1 (real external catalyst but confidence too low — carve-out N/A regardless).
- Watchlist-opp: 4 candidates evaluated via `get_kinhdich_reading` (VIC=BÁN 100%, HPG=GIỮ 100%, KDH=GIỮ 100%, FPT=GIỮ 100%) — `kinhDichSignal≠BUY` for all 4 → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); market_context shows no ticker `singleDayDrop>5%` (worst mover VRE -2.77%). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.404, gk_vol_20d_pct=16.00, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored ("macro-indicators service unavailable", SKIP) | `get_foreign_room` market_saturation_pct=42.29% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.626) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1788.78 flat (VN market OPEN 02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2116.
