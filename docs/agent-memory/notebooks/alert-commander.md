# Alert Commander — Notebook

**Last updated:** 2026-08-25 06:40 UTC | **Sprint:** TASK-DEV-MCP-SIGNAL-TYPE-REGISTRY

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c246 · 2026-08-25T04:53:24Z (slot=alert-commander-market, tick=04:45)
- Signals: no exact tick-snapshot hit (no cycle-snapshot-04:45.json) → direct `get_cycle_bootstrap` + `get_macro_snapshot` + `get_macro_calendar` + `get_agent_signals`(agent, 2h) + explicit signal_type queries. Bus (2h) → 11 total: 6 `verified_decision` (alert-engine, out-of-scope) + 2 `urgent_news` (HPG Shinhan LNST+56% impact8, VIC PNV wealth-news impact7) + 2 `chain_catalyst` (gold safe-haven regime_adj_score=7→conf0.70; S&P/Nasdaq tech selloff amid US-Iran tensions regime_adj_score=7→conf0.70) — both below NEUTRAL 0.75 threshold → suppressed via `record_signal_outcome` (11332, 11333). Evaluated 4 tickers via `get_kinhdich_reading` (HPG/VIC/DBC/PDR) — HPG=GIU 25%, VIC=GIU 81%, DBC=BAN 100%, PDR=GIU 81% — no BUY signal, watchlist-opp fails all (urgent_news HPG/VIC fail direct-gate check per kinhDich≠BUY). Fired: 0 | Suppressed: 2 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 2 suppressed (conf 0.70 < 0.75 NEUTRAL threshold) | event_types: gold-safehaven(market-wide, no ticker), US-Iran/tech-selloff(FPT-mentioned in prose only, no top-level stock_code) — same pair re-evaluated as c244/c245 (still within 2h window), unchanged verdict.
- Watchlist-opp: 4 candidates evaluated via `get_kinhdich_reading` (HPG/VIC/DBC/PDR) — 3 `GIU` + 1 `BAN`(DBC), none `BUY` → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (worst movers BSR -1.64%, KDH -1.62%; DBC/VIC moves were rallies +6.93%/+4.71%, not drops). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.4429, gk_vol_20d_pct=15.76, drawdown_252d=16.38%) | `get_vn_liquidity_state` OMO+interbank blocked (honest-NULL, source unreachable) | `get_foreign_room` market_saturation_pct=5.77% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.69) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1807.11 (+18.33), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2129.

## c247 · 2026-08-25T05:08:06Z (slot=alert-commander-market, tick=05:00)
- Signals: no exact tick-snapshot hit (no cycle-snapshot-05:0x.json) → direct `get_cycle_bootstrap` + `get_macro_snapshot` + `get_macro_calendar` + `get_agent_signals`(agent, 2h). Bus (2h) → 10 total: 8 `verified_decision` (alert-engine, out-of-scope produced-type: DBC×3/VIC×3/KDC×2) + 2 `urgent_news` (HPG Shinhan LNST+56% impact8, VIC PNV wealth-news impact7) + 2 `chain_catalyst` (gold safe-haven regime_adj_score=7→conf0.70; S&P/Nasdaq tech selloff amid US-Iran tensions regime_adj_score=7→conf0.70) — both below NEUTRAL 0.75 threshold → suppressed via `record_signal_outcome` (11332, 11333). Evaluated 2 tickers via `get_kinhdich_reading` (HPG=GIU 25%, VIC=GIU 81%) — no BUY signal, watchlist-opp fails; urgent_news HPG/VIC fail direct-gate check (kinhDich≠BUY). Fired: 0 | Suppressed: 2 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 2 suppressed (conf 0.70 < 0.75 NEUTRAL threshold) | event_types: gold-safehaven(market-wide, no ticker), US-Iran/tech-selloff(FPT-mentioned in prose only, no top-level stock_code) — same pair re-evaluated as c244–c246 (still within 2h window), unchanged verdict.
- Watchlist-opp: 2 candidates evaluated via `get_kinhdich_reading` (HPG/VIC) — both `GIU`, none `BUY` → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (DBC/VIC moves were rallies +6.93%/+4.71%, not drops). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.4429, gk_vol_20d_pct=15.76, drawdown_252d=16.38%) | `get_vn_liquidity_state` OMO+interbank blocked (honest-NULL, source unreachable) | `get_foreign_room` market_saturation_pct=5.77% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.69) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1807.11 (+18.33), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2130.

## c248 · 2026-08-25T06:40:36Z (slot=alert-commander-market, tick=06:38)
- Signals: no exact tick-snapshot hit (no cycle-snapshot-06:38.json, nearest 06:36 not exact-match) → direct `get_cycle_bootstrap` + `get_macro_snapshot` + `get_macro_calendar` + `get_agent_signals`(agent, 2h) + explicit signal_type queries. Bus (2h) → 1 total: 1 `verified_decision` (alert-engine, KDC, out-of-scope produced-type). All 6 explicit signal_type queries (urgent_news/chain_catalyst/price_anomaly/verified_chain/legal_risk/crisis_velocity) returned empty — prior cycles' urgent_news(HPG/VIC)+chain_catalyst(gold/US-Iran) pair aged out of rolling 2h TTL window, no replacement bus signal arrived. No candidate ticker to evaluate — watchlist-opp and position-danger gates both fail on lack of input. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window (prior gold-safehaven/US-Iran pair expired).
- Watchlist-opp: 0 candidates (no urgent_news/chain_catalyst named a ticker this cycle) → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); DBC +6.93% is a rally not a drop, no ticker singleDayDrop>5% flagged. Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.4286, gk_vol_20d_pct=15.86, drawdown_252d=16.38%) | `get_vn_liquidity_state` OMO+interbank blocked (honest-NULL, source unreachable) | `get_foreign_room` market_saturation_pct=5.77% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.62) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1799.52 (+10.74), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2131.
