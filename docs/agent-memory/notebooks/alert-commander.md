# Alert Commander — Notebook

**Last updated:** 2026-08-26 04:09 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c267 · 2026-08-26T03:52:28Z (slot=alert-commander-market, tick=03:49)
- Signals: tick-snapshot hit `cycle-snapshot-03:49.json` (13s old, <7min) → skipped direct `get_cycle_bootstrap`/`get_macro_snapshot`, used `$CYCLE_SNAPSHOT.macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price, none active) + `get_volatility_indicators` + `get_vn_liquidity_state` + `get_foreign_room` + `get_legal_risk_signals(days=1,hours_back=6)` (clean) + `get_crisis_early_warning` (primary indicator clean) + `get_agent_signals`(agent,2h,status=all) + per-type explicit filters (urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity — all "Không có tín hiệu mới"). Bus carried 2 alert-engine `verified_decision` echoes (VHM ta_oversold RSI28.4, DXG ta_bb_breakout_up — produced-type, not consumed) evaluated as due-diligence candidates. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 3h16m).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 2 candidates via `get_kinhdich_reading` — VHM GIU/25%, DXG BAN/37-38% — none BUY, gate fails.
- Position-danger: `get_positions()` FPT only (avg 80.3k, current 72.1k, -10.2% P/L, -41.0M VND); `get_portfolio_risk()` VaR(95%) -0.1%, max drawdown none (price rising in period) — no singleDayDrop>5% → gate fails.
- CRITICAL-always: `get_legal_risk_signals` clean; bus carries no legal_risk/verified_chain/crisis_velocity. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged from c266 (BSR20.0 danger; FPT/KDH/PLX/SSI/VCB/VIC warning), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.49, drawdown_252d=16.38%) | `get_vn_liquidity_state` OMO+interbank blocked (honest-NULL, source unreachable) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.51) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1794.03(-13.31 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2136.

## c268 · 2026-08-26T04:08:39Z (slot=alert-commander-market, tick=04:05)
- Signals: tick-snapshot hit `cycle-snapshot-04:05.json` (~1min30s old, <7min) → skipped direct `get_cycle_bootstrap`/`get_macro_snapshot`, used `$CYCLE_SNAPSHOT.macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_alerts`(price, none active) + `get_volatility_indicators` + `get_vn_liquidity_state` (error: macro-indicators service unavailable, skipped) + `get_foreign_room` + `get_legal_risk_signals(days=1,hours_back=6)` (clean) + `get_crisis_early_warning` (primary indicator clean) + `get_agent_signals`(agent,2h,status=all) + per-type explicit filters (urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity — all "Không có tín hiệu mới"). Bus carried 1 alert-engine `verified_decision` echo (DXG ta_bb_breakout_up, status=read — produced-type, not consumed) evaluated as due-diligence candidate. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 3h32m).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 2 candidates via `get_kinhdich_reading` — VHM GIU/25%, DXG BAN/37-38% — none BUY, gate fails.
- Position-danger: `get_positions()` FPT only (avg 80.3k, current 72.1k, -10.2% P/L, -41.0M VND); `get_portfolio_risk()` VaR(95%) -0.1%, max drawdown none (price steady in period) — no singleDayDrop>5% → gate fails.
- CRITICAL-always: `get_legal_risk_signals` clean; bus carries no legal_risk/verified_chain/crisis_velocity. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged from c267 (BSR20.0 danger; FPT/KDH/PLX/SSI/VCB/VIC warning), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.49, drawdown_252d=16.38%) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.50) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1792.75(-14.59 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2137.

## c269 · 2026-08-26T04:09:09Z (slot=alert-commander-critical, tick=04:00)
- NNN-collision note: peer `alert-commander-market` slot claimed `c268` (tick=04:05) concurrently — renumbered this cycle to next-free `c269` per stage-dispatch-log.md concurrent-slot self-correct (no content dropped, c268 kept as-is).
- Signals: tick-snapshot hit `cycle-snapshot-04:05.json` (~4min old, <7min) → skipped direct `get_cycle_bootstrap`/`get_macro_snapshot`, used `$CYCLE_SNAPSHOT.macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price, none active) + `get_volatility_indicators` + `get_vn_liquidity_state` (honest-NULL, source unreachable) + `get_foreign_room` + `get_legal_risk_signals(days=1,hours_back=6)` (clean) + `get_crisis_early_warning` (primary indicator clean) + `get_agent_signals`(agent,2h,status=all) + per-type explicit filters (urgent_news/price_anomaly/verified_chain/chain_catalyst — all "Không có tín hiệu mới"). Bus carried 1 alert-engine `verified_decision` echo (DXG ta_bb_breakout_up, status=read — produced-type, not consumed) evaluated as due-diligence candidate. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 3h32m).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 2 candidates via `get_kinhdich_reading` — VHM GIU/25%, DXG BAN/37-38% — none BUY, gate fails.
- Position-danger: `get_positions()` FPT only (avg 80.3k, current 72.1k, -10.2% P/L, -41.0M VND); `get_portfolio_risk()` VaR(95%) -0.1%, max drawdown none (price rising in period) — no singleDayDrop>5% → gate fails.
- CRITICAL-always: `get_legal_risk_signals` clean; bus carries no legal_risk/verified_chain/crisis_velocity. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged from c268 (BSR20.0 danger; FPT/KDH/PLX/SSI/VCB/VIC warning), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.49, drawdown_252d=16.38%) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.50) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1792.75(-14.59 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2138.
