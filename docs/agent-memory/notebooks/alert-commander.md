# Alert Commander — Notebook

**Last updated:** 2026-08-26 02:38 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c260 · 2026-08-26T02:07:36Z (slot=alert-commander-market, tick=02:00)
- Signals: exact-tick file `cycle-snapshot-02:05.json` present but stale (dated 2026-08-25, wrong day) → treated as absent per exact-match rule → direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price) + `get_agent_signals`(agent,2h,status=all) + per-type explicit filters (urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity — all "Không có tín hiệu mới" except urgent_news). Bus carried 2 freshness-sla-monitor `urgent_news` (price source stale 75min, foreign_flow source stale 1290min) — infra SLA-breach noise, no ticker → suppressed per standing rule; plus 7 alert-engine `verified_decision` echoes (produced-type, not consumed). No candidate ticker. Fired: 0 | Suppressed: 2 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 1h31m).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 0 candidates (no ticker-named urgent_news signal this cycle) → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no stopLossHit/singleDayDrop>5% candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean ("Không có tín hiệu khủng hoảng nào"); corp-reputation sub-50: BSR20.0(danger)/FPT42.5/KDH37.0/PLX30.0/SSI45.7/VCB42.5/VIC49.0 (warning, deteriorating), not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.77, drawdown_252d=16.38%) | `get_vn_liquidity_state` OMO+interbank blocked (honest-NULL, source unreachable) | `get_foreign_room` market_saturation_pct=6.67% (top-10 tickers; outflow_z_5d=-0.79) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1791.41(-15.93 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2129.

## c261 · 2026-08-26T02:24:16Z (slot=alert-commander-market, tick=02:15)
- Signals: exact-tick file `cycle-snapshot-02:20.json` present but stale (created_at 2026-08-25, wrong day) → treated as absent per exact-match rule → direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price) + `get_agent_signals`(agent,2h,status=all). Bus carried 2 freshness-sla-monitor `urgent_news` (price source stale 75min, foreign_flow source stale 1290min) — infra SLA-breach noise, no ticker → suppressed per standing rule; plus 7 alert-engine `verified_decision` echoes (VIC/VHM x2/VCI/macro-Brent/BID/FPT, produced-type, not consumed). Candidate check: VCI (insider-buying news) + VHM (RSI oversold + negative news) both `get_kinhdich_reading` = GIU, confidence 25% → fail watchlist-opp kinhDichConfidence≥70 gate. Fired: 0 | Suppressed: 2 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 1h47m).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 2 candidates checked (VCI, VHM) — both GIU/25% confidence, gate fails, no fire.
- Position-danger: `get_positions()` FPT (avg 80.3k, current 71.1k, P/L -11.5% since entry); `get_portfolio_risk()` max drawdown -0.4%, today intraday +0.57% — no singleDayDrop>5% → gate fails.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50: BSR20.0(danger)/FPT42.5/KDH37.0/PLX30.0/SSI45.7/VCB42.5/VIC49.0 (warning, deteriorating), unchanged from c260, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.39, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored (macro-indicators service unavailable, honest-SKIP) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.61) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1796.12(-11.22 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2130.

## c262 · 2026-08-26T02:38:14Z (slot=alert-commander-market, tick=02:30)
- Signals: TICK=02:35 (wall-clock at Step 0b) — exact-tick file `cycle-snapshot-02:35.json` present but stale (dated 2026-08-25, wrong day) → treated as absent per exact-match rule → direct `get_cycle_bootstrap` + `get_macro_snapshot` (JSON shape, text field present) + `get_macro_calendar` + `get_market_context`(6h) + `get_alerts`(price) + `get_agent_signals`(agent,2h,status=all) + per-type explicit filters (urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity — all "Không có tín hiệu mới"). Bus carried 7 alert-engine `verified_decision` echoes (VIC/VHM x2/VCI/macro-Brent/BID/FPT/VHM-RSI, produced-type, not consumed); no urgent_news this cycle. Candidate check: VCI (insider-buying), VHM (RSI oversold+bearish news), BID (Siemens divest news) all `get_kinhdich_reading` = GIU (VCI 25%, VHM 25%, BID 100%-but-GIU-not-BUY) → fail watchlist-opp kinhDichSignal=BUY gate. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt — VN market OPEN, uptime 2h1m).
- ChainCatalyst: 0 fired | 0 suppressed | no chain_catalyst signal in window.
- Watchlist-opp: 3 candidates checked (VCI, VHM, BID) — all GIU signal (not BUY), gate fails, no fire.
- Position-danger: `get_positions()` FPT (avg 80.3k, current 71.5k, -11.0% since entry); `get_portfolio_risk()` max drawdown none (price rising in period), VaR(95%) -0.1% — no singleDayDrop>5% → gate fails.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`/`chain_catalyst` (explicit per-type filters all clean). `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50: BSR20.0(danger)/FPT42.5/KDH37.0/PLX30.0/SSI45.7/VCB42.5/VIC49.0 (warning, deteriorating), unchanged from c261, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.40, gk_vol_20d_pct=15.39, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored (macro-indicators service unavailable, honest-SKIP) | `get_foreign_room` market_saturation_pct=5.76% (top-10 tickers; foreign_outflow_z_5d=-0.59) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 6 days"). VN-Index 1797.16(-10.18 vs prior), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2131.
