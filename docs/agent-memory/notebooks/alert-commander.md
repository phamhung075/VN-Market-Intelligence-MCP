# Alert Commander — Notebook

**Last updated:** 2026-08-25 02:40 UTC | **Sprint:** FIX-REAPER-ORPHAN-MINT-KEYS-ON-TTL-ONLY-NO-SESSION-LIVENESS-CHECK

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c234 · 2026-08-25T02:09:49Z (slot=alert-commander-market, tick=02:00)
- Signals: no tick-snapshot hit for exact minute (02:06) → direct `get_cycle_bootstrap`. Bus (bootstrap 2h) → 15 signals: 4 `chain_catalyst`→all (no stockCode, regime_adj_score 0.4-0.6, all <0.75 NEUTRAL threshold → suppressed, `record_signal_outcome` called each), 4 `urgent_news`→alert-commander targeted (KDH/VIC/HPG/FPT, no conviction field, evaluated direct vs Firing Gate), 2 `urgent_news` freshness-sla-monitor SLA-breach infra noise (suppressed per standing rule), 3 `verified_decision`+2 `verified_decision`(ta_oversold HUT/VHM) from alert-engine (out-of-scope produced-type, not consumed, no action). Fired: 0 | Suppressed: 4 (chain_catalyst, recorded) + 2 (infra noise) | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 4 suppressed (ids 11301/11302/11309/11310, regime_adj_score/10=0.5/0.5/0.4/0.6, all <0.75) | event_types: domestic VN-Index recap x2, US tech/Iran spillover x1 (no ticker), Iran sanctions trade_war x1 (real external catalyst but confidence too low — carve-out N/A regardless).
- Watchlist-opp: 4 candidates evaluated via `get_kinhdich_reading` (VIC=BÁN 100%, HPG=GIỮ 100%, KDH=GIỮ 100%, FPT=GIỮ 100%) — `kinhDichSignal≠BUY` for all 4 → gate fails, no fire.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); market_context shows no ticker `singleDayDrop>5%` (worst mover VRE -2.77%). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.404, gk_vol_20d_pct=16.00, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored ("macro-indicators service unavailable", SKIP) | `get_foreign_room` market_saturation_pct=42.29% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.626) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1788.78 flat (VN market OPEN 02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2116.

## c235 · 2026-08-25T02:23:55Z (slot=alert-commander-market, tick=02:15)
- Signals: no tick-snapshot hit for exact minute (02:22) → direct `get_cycle_bootstrap` + `get_macro_snapshot` + `get_macro_calendar` + `get_agent_signals`(agent, 2h). Bus (2h) → 10 signals: 2 `chain_catalyst` (Iran sanctions no-ticker, VN-Index-1800 no-ticker) both below NEUTRAL 0.75 threshold → suppressed, `record_signal_outcome` called each. 1 `urgent_news` (FPT, real content, impact 6/10) evaluated direct vs Firing Gate — bearish/headwind, fails both gates. 2 `urgent_news` freshness-sla-monitor infra noise (suppressed per standing rule). 5 `verified_decision` (HPG/FPT/KDC news_mention + HUT/VHM ta_oversold, alert-engine) out-of-scope produced-type, no action. Fired: 0 | Suppressed: 4 (2 chain_catalyst recorded + 2 infra noise) | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 2 suppressed (ids 11309/11310, regime_adj_score/10=0.4/0.6, both <0.75) | event_types: Iran sanctions/trade_war (real external catalyst, confidence too low), domestic VN-Index-1800 breakout recap (no ticker).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (only real urgent_news this cycle, FPT, is bearish/headwind) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (FPT +0.42%, worst mover BSR -1.09%). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.407, gk_vol_20d_pct=15.60, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored ("macro-indicators service unavailable", SKIP) | `get_foreign_room` market_saturation_pct=5.77% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.866) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1794.60 (+5.82), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2117.

## c236 · 2026-08-25T02:40:14Z (slot=alert-commander-market, tick=02:40)
- Signals: no tick-snapshot hit for exact minute (02:36) → direct `get_cycle_bootstrap` + `get_macro_snapshot` + `get_macro_calendar` + `get_agent_signals`(agent, 2h). Bus (2h) → 9 signals: 2 `chain_catalyst` (Iran sanctions no-ticker, VN-Index-1800 no-ticker) both below NEUTRAL 0.75 threshold → suppressed, `record_signal_outcome` called each. 1 `urgent_news` (FPT, real content, impact 6/10, Iran/tech spillover) evaluated direct vs Firing Gate — bearish/headwind, fails both gates. 2 `urgent_news` freshness-sla-monitor infra noise (suppressed per standing rule). 4 `verified_decision` (FPT/KDC news_mention + HUT/VHM ta_oversold, alert-engine) out-of-scope produced-type, no action. Fired: 0 | Suppressed: 2 (chain_catalyst, recorded) + 2 (infra noise) | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 fired | 2 suppressed (ids 11309/11310, regime_adj_score/10=0.4/0.6, both <0.75) | event_types: Iran sanctions/trade_war (real external catalyst, confidence too low), domestic VN-Index-1800 breakout recap (no ticker).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (only real urgent_news this cycle, FPT, is bearish/headwind) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (FPT +0.42%, worst mover BSR -0.73%). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity`. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.429, gk_vol_20d_pct=15.66, drawdown_252d=16.38%) | `get_vn_liquidity_state` errored ("macro-indicators service unavailable", SKIP) | `get_foreign_room` market_saturation_pct=5.77% (top-10 tickers, room_utilization_pct null across all — data-quality gap; outflow_z_5d=-1.857) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 7 days"). VN-Index 1799.76 (+10.98), VN market OPEN (02:00-08:59 UTC). Silent exit — no MARKET/WORK send. `log_agent_work` id=2118.
