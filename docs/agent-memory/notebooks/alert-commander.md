# Alert Commander — Notebook

**Last updated:** 2026-08-24 07:37 UTC | **Sprint:** idle

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c221 · 2026-08-24T07:05:53Z (slot=alert-commander-market, tick=07:00)
- Signals: tick-snapshot hit (`cycle-snapshot-07:02.json`, created 07:02:15Z, ~1min old, market_context+macro_snapshot) — direct `get_macro_calendar`+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`+`get_crisis_early_warning`+`get_agent_signals`(2h, per-type: urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity)+`get_positions`+`get_kinhdich_reading`(VIC,DXG,PDR,KDH,VHM,DIG,NVL,SSI,HPG,VIX)+`get_market_snapshot`. Bus (2h) → 5: `VERIFIED_DECISION` id11255(HPG)/11256(KDH)/11257(VCB)/11258(VHM)/11259(VIC), all alert-engine correlation stubs, not consumed types. All 6 per-type bus queries clean — no `urgent_news`/`price_anomaly`/`verified_chain`/`chain_catalyst`/`legal_risk`/`crisis_velocity` this cycle. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: none in bus this cycle.
- Watchlist-opp: real-estate/banking rally continues (VIC +4.10%, DXG +3.03%, PDR +2.85%, DIG +2.73%, KDH +2.78%, NVL +2.25% intraday) on VIC/VHM fund-inflow news ("VIC và VHM có thể được các quỹ mua vào gần 23.500 tỷ đồng") — checked kinhDich for all 5 rally names + VCB/SSI/HPG/VIX: none returns BUY (VIC=BAN 100%, VHM=GIU 25%, DXG=THAN TRONG 13%, PDR/KDH/DIG=GIU 100%, NVL=GIU 38%, SSI=THAN TRONG 13%, HPG=GIU 100%, VIX=GIU 38%) — cond2 fails for every candidate, gate closed before newsSentiment/majority checked.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 71,600, -10.8% total P/L). FPT -0.56% intraday per market_context — no single-day drop >5%; `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"). Gate fails at cond1+cond2.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR (44.0, improving) + PLX (48.0, improving), no CRITICAL. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.421, gk_vol_20d_pct=16.03, drawdown_252d=16.38%) | `get_vn_liquidity_state`: OMO+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.875 | `get_foreign_room` market_saturation_pct=5.85% (top-10 tickers only; outflow_z_5d=-0.572) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index +1.05%, breadth 182up/122down/64flat, Kinh Dich market-wide Quan(20) TRUNG TÍNH conf 38%. Silent exit — no MARKET/WORK send. `log_agent_work` id=2094.

## c222 · 2026-08-24T07:21:09Z (slot=alert-commander-market, tick=07:15)
- Signals: tick-snapshot hit (`cycle-snapshot-07:17.json`, created 07:17:00Z, ~9s old, market_context+macro_snapshot) — direct `get_macro_calendar`+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`+`get_crisis_early_warning`+`get_agent_signals`(2h, per-type: urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity)+`get_positions`+`get_kinhdich_reading`(VIC,VHM,DXG,PDR,KDH,DIG,NVL,VCB,SSI,HPG,FPT)+`get_market_snapshot`. Bus (2h) → 4: `VERIFIED_DECISION` id11256(KDH)/11257(VCB)/11258(VHM)/11259(VIC), all alert-engine correlation stubs, not consumed types (id11255/HPG rolled off). All 6 per-type bus queries clean — no `urgent_news`/`price_anomaly`/`verified_chain`/`chain_catalyst`/`legal_risk`/`crisis_velocity` this cycle. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: none in bus this cycle.
- Watchlist-opp: real-estate/banking rally continues (VIC +4.29%, DXG +3.90%, PDR +4.47%, DIG +3.64%, KDH +2.78%, NVL +2.62% intraday) on VIC/VHM fund-inflow news ("VIC và VHM có thể được các quỹ mua vào gần 23.500 tỷ đồng") — checked kinhDich for all 5 rally names + VCB/SSI/HPG: none returns BUY (VIC=BAN 100%, VHM=GIU 25%, DXG=THAN TRONG 13%, PDR=GIU 63%, KDH/DIG=GIU 100%, NVL=GIU 38%, VCB=GIU 25%, SSI=THAN TRONG 13%, HPG=GIU 100%) — cond2 fails for every candidate, gate closed before newsSentiment/majority checked.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 71,600, -10.8% total P/L, unchanged; kinhDich=GIU 100%). FPT -0.69% intraday per market_context — no single-day drop >5%; `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"). Gate fails at cond1+cond2.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR (44.0, improving) + PLX (48.0, improving), no CRITICAL. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=16.00, drawdown_252d=16.38%) | `get_vn_liquidity_state`: OMO+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.863 | `get_foreign_room` market_saturation_pct=5.85% (top-10 tickers only; outflow_z_5d=-0.550) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index +1.11%, breadth 190up/116down/64flat, Kinh Dich market-wide Quan(20) TRUNG TÍNH conf 38%. Silent exit — no MARKET/WORK send. `log_agent_work` id=2095.

## c223 · 2026-08-24T07:37:45Z (slot=alert-commander-market, tick=07:30)
- Signals: tick-snapshot hit (`cycle-snapshot-07:34.json`, created 07:34:23Z, ~1min old, market_context+macro_snapshot) — direct `get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`+`get_crisis_early_warning`+`get_agent_signals`(bulk 2h + 6 per-type)+`get_positions`+`get_kinhdich_reading`(VIC,VHM,KDH,PDR,DXG,VCB,SSI,HPG,FPT,DIG,VIX)+`get_market_snapshot`. Bus (2h) → 4: `VERIFIED_DECISION` id11256(KDH)/11257(VCB)/11258(VHM)/11259(VIC), all alert-engine correlation stubs, not consumed types. All 6 per-type bus queries clean — no `urgent_news`/`price_anomaly`/`verified_chain`/`chain_catalyst`/`legal_risk`/`crisis_velocity` this cycle. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: none in bus this cycle.
- Watchlist-opp: real-estate/banking rally continues (VIC +4.63%, PDR +4.07%, KDH +3.06%, DXG +2.16%, DIG +2.27%, NVL +1.12%, VHM +1.53%, SSI +2.41%, VIX +2.59%) on the same VIC/VHM fund-inflow story — checked kinhDich for VIC/VHM/KDH/PDR/DXG/VCB/SSI/HPG/DIG/VIX: none returns BUY (VIC=BAN 100%, VHM=GIU 25%, KDH=GIU 100%, PDR=GIU 63%, DXG=THAN TRONG 13%, VCB=GIU 63%, SSI=THAN TRONG 13%, HPG=GIU 100%, DIG=GIU 100%, VIX=GIU 38%) — cond2 fails for every candidate, gate closed before newsSentiment/majority checked.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 71,300, -11.2% total P/L; kinhDich=GIU 100%). FPT -1.11% intraday per market_context — no single-day drop >5%; `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"). Gate fails at cond1+cond2.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR (44.0, improving) + PLX (48.0, improving), no CRITICAL. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.414, gk_vol_20d_pct=16.04, drawdown_252d=16.38%) | `get_vn_liquidity_state`: honest-NULL ("macro-indicators service unavailable") | `get_foreign_room` market_saturation_pct=5.85% (top-10 tickers only; outflow_z_5d=-0.494) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index +1.02%, breadth 195up/106down/69flat, Kinh Dich market-wide Quan(20) TRUNG TÍNH conf 38%. Silent exit — no MARKET/WORK send. `log_agent_work` id=2096.
