# Alert Commander — Notebook

**Last updated:** 2026-08-24 20:07 UTC | **Sprint:** CCATO-MCP-T8-DOD-HARNESS

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c230 · 2026-08-24T12:06:40Z (slot=alert-commander-critical, tick=12:00)
- Signals: no exact-minute tick-snapshot (12:04, freshest file 12:03) → direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`(days=1,hours_back=6)+`get_crisis_early_warning`+`get_agent_signals` x5 (bulk 2h + verified_chain + urgent_news + chain_catalyst + price_anomaly). Bus (2h) → 0: ALL types "Không có tín hiệu mới" — fully rolled off since c229 (08:56Z), consistent w/ rolling 2h TTL and the ~3h08m gap to this 4h critical-sweep slot. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 signals in bus this cycle (n/a — none to evaluate).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (bus empty) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% in market_context (max down SAB, not in 6h ctx — no drop flagged). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity` type. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=15.99, drawdown_252d=16.38%) | `get_vn_liquidity_state`: omo+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=99.017 | `get_foreign_room` market_saturation_pct=24.59% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index 1788.78 (+20.66). Silent exit — no MARKET/WORK send. `log_agent_work` id=2105.

## c231 · 2026-08-24T16:06:00Z (slot=alert-commander-critical, tick=16:00)
- Signals: CYCLE_SNAPSHOT hit tick=16:02 (3min fresh) → macro from snapshot, skip `get_macro_snapshot`. Direct `get_macro_calendar`+`get_alerts(price)`+`get_volatility_indicators`+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`(days=1,hours_back=6)+`get_crisis_early_warning`+`get_agent_signals` x5 (bulk 2h + verified_chain + urgent_news + chain_catalyst + price_anomaly). Bus (2h) → 0: ALL types "Không có tín hiệu mới" — fully rolled off since c229 (08:56Z), consistent w/ c230's rolling 2h TTL pattern. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 signals in bus this cycle (n/a — none to evaluate).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (bus empty) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged. Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity` type. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | vol NORMAL (rv_20d_pctile=0.432, gk_vol_20d_pct=15.99, drawdown_252d=16.38%) | `get_vn_liquidity_state`: omo+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=98.957 | `get_foreign_room` market_saturation_pct=24.59% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index 1788.78 (+20.66). Silent exit — no MARKET/WORK send. `log_agent_work` id=2107.

## c232 · 2026-08-24T20:07:44Z (slot=alert-commander-critical, tick=20:00)
- Signals: CYCLE_SNAPSHOT hit tick=20:03 (2min fresh) → macro from snapshot, skip `get_macro_snapshot`. Direct `get_macro_calendar`+`get_market_context`(6h)+`get_alerts(price)`+`get_volatility_indicators`(error "TA Service volatility: 500 internal error", SKIP)+`get_vn_liquidity_state`+`get_foreign_room`+`get_legal_risk_signals`(days=1,hours_back=6)+`get_crisis_early_warning`+`get_agent_signals` x5 (bulk 2h + verified_chain + urgent_news + chain_catalyst + price_anomaly). Bus (2h) → 0: ALL 5 calls "Không có tín hiệu mới" — fully clean, same rolling-2h-TTL pattern as c230/c231. Fired: 0 | Suppressed: 0 | MARKET: none. Gateway healthy (Step 0-GW probe OK, single attempt).
- ChainCatalyst: 0 signals in bus this cycle (n/a — none to evaluate).
- Watchlist-opp: no BUY-signal candidate ticker surfaced (bus empty) — gate not evaluated, no candidate.
- Position-danger: `get_alerts(type=price)` clean ("Không có cảnh báo nào đang hoạt động"); no ticker singleDayDrop>5% flagged (session recap bullish: VN-Index +20.66, breadth green — VIC+4.63%, PDR+4.47%, KDH+3.06% top movers, no red >1.5%). Gate fails, no candidate.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean; bus carries no `legal_risk`/`verified_chain`/`crisis_velocity` type. `get_crisis_early_warning` primary indicator clean; corp-reputation sub-50 unchanged BSR39.0/FPT42.5/KDH41.0/PLX37.0 deteriorating, not itself CRITICAL.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2) | `get_volatility_indicators` errored (TA Service 500), standard thresholds applied | `get_vn_liquidity_state`: omo+interbank honest-NULL (HTML parse fail / VPS unreachable), fx dxy=99.02 | `get_foreign_room` market_saturation_pct=24.59% (top-10 tickers only; outflow_z_5d=-0.473) | Pivot window: TRUE (pivotWindowWarning: "Entering pivot window September 2026 in 8 days"). VN-Index 1788.78 (+20.66). Silent exit — no MARKET/WORK send. `log_agent_work` id=2110.
