# Alert Commander — Notebook

**Last updated:** 2026-08-15 08:13 UTC | **Sprint:** FIX-PROSECEILING-SECONDARY-CLAIM-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET

**Role:** Event-only alert dispatch (position-danger / watchlist-opportunity / CRITICAL-always) | **Cadence:** `*/15min` market hours (02:00–08:30 UTC) + 4h critical sweep | **Contract:** APPEND class, one `## c<NNN>` section per cycle per `.claude/skills/notebook-write/SKILL.md` AC-6.

> **Structural reset (FIX-ALERT-COMMANDER-NOTEBOOK-SINGLE-BLOB-UNPRUNABLE, 2026-07-29):** prior history archived → `docs/agent-memory/notebooks/archive/alert-commander-archive-20260729.md`. One top-level `## c<NNN> · <ISO-timestamp>` section per cycle now.

## c191 · 2026-08-15T00:16:25Z (slot=alert-commander-critical, tick=00:00)
- Signals: bus pull `get_agent_signals(agent=alert-commander, status=all, hours_back=2)` → 1 signal: `verified_decision` VJC id11020 (news_mention, alert-engine, conf60, status=read) — out-of-scope (not urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity). Fired: 0 | Suppressed: 0 | MARKET: none. Off-hours critical-sweep (market CLOSED); no exact-tick snapshot match (TICK=00:13→nearest-slot 00:10/00:15 both absent, nearest file cycle-snapshot-00:11.json not an exact match) — fell through to direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_macro_calendar`+`get_agent_signals`. Gateway healthy (Step 0-GW probe OK).
- ChainCatalyst: 0 fired | 0 suppressed — no chain_catalyst signal in bus this cycle.
- Watchlist-opp: checked DXG (recent HIGH price_surge/volume_spike stack + bullish "tăng cận trần" news 08-14) directly via `get_kinhdich_reading` — MUA but confidence 37-38% (<70% NEUTRAL threshold) → fails at condition 1, not evaluated further. FPT kinhDich = GIU (not BUY) → fails.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ avg 80,300, current 68,300, -14.9% P/L). `get_alerts(type=price)` clean (no active alerts). singleDayDrop for FPT = -1.30% (last close 08-14) — well under 5% threshold → gate fails regardless of stop-loss breach (price already far below avg_cost×0.93 hard floor, but condition 2 kills the AND-gate).
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary crisis-indicator section clean (no mention-velocity crisis); corp-reputation sub-50 list same 7 tickers (BSR/FRT/HUT/PLX/VCB/VJC/VNM) — BSR+PLX still DANGER tier (score 24.0, deteriorating), continued from c190, no further escalation. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot JSON shape has no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2, fetched_at_source=2026-08-12) | vol ELEVATED (rv_20d_pctile=0.796, gk_vol_20d_pct=18.62, drawdown_252d=16.38%) | `get_vn_liquidity_state` **errored** again ("macro-indicators service unavailable") — standard thresholds used, `[SKIP]` logged | `get_foreign_room` market_saturation_pct=24.56% (outflow_z_5d=+0.058) — same as c190, same-day (08-14 close) EOD figure unchanged since market still closed, not a new jump | Pivot window: false (pivotWindowWarning=null, next Sept 2026 PMI/CPI/FOMC/SBV). Silent exit — no MARKET/WORK send. `log_agent_work` id=2042.

## c192 · 2026-08-15T04:21:14Z (slot=alert-commander-critical, tick=04:15)
- Signals: bus pull `get_agent_signals(agent=alert-commander, status=all, hours_back=2)` → 2 signals: `verified_decision` BSR id11040 + PLX id11041 (alert-engine news_mention conf75, shared oil-tailwind story "Chứng khoán Mỹ tăng 3 tuần liên tiếp, giá dầu tăng 5%") — both out-of-scope (not urgent_news/price_anomaly/verified_chain/chain_catalyst/legal_risk/crisis_velocity). Fired: 0 | Suppressed: 0 | MARKET: none. Off-hours critical-sweep (market CLOSED, Saturday); no exact-tick snapshot for 04:1x (nearest on-disk 00:11.json stale) — direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_macro_calendar`+`get_agent_signals`. Gateway healthy (Step 0-GW probe OK).
- ChainCatalyst: 0 fired | 0 suppressed — no chain_catalyst signal in bus this cycle.
- Watchlist-opp: driving oil-tailwind story evaluated against both named tickers directly (`get_kinhdich_reading`). PLX = MUA conf100% (clears cond1+2) but its RECENT ANALYSIS line carries `score:10.0 down` — per field-derivation convention (score direction over label) treated as newsSentiment≤-0.5, contradicting the line's own `[bullish]` tag → cond3 fails, suppressed (not fired — label/score mismatch flagged for review, not resolved in agent's favor). BSR = THAN TRONG conf87% (not BUY) → fails cond1.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 68,300, -14.9% P/L). `get_alerts(type=price)` clean. singleDayDrop FPT = -1.30% (08-14 close, unchanged from c191) — gate fails.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean (no mention-velocity crisis); corp-reputation sub-50 same 7 tickers (BSR/FRT/HUT/PLX/VCB/VJC/VNM) — BSR+PLX still DANGER tier (score 24.0, deteriorating), continued c190-191, no further escalation. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot text carries Investment-Clock/oil/gold/usdvnd/carry/yield block, no Global-Liquidity line; carry.regime=NEUTRAL, carrySpread=1.37pp, source_tier=2, fetched_at_source=2026-08-12) | vol ELEVATED (rv_20d_pctile=0.796, gk_vol_20d_pct=18.62, drawdown_252d=16.38%) | `get_vn_liquidity_state` ok this cycle (omo/interbank both honest-NULL, blocked_reason parse-fail/unreachable — standard thresholds used) | `get_foreign_room` market_saturation_pct=24.75% (outflow_z_5d=+0.058, ~flat vs c190/191) | Pivot window: false (pivotWindowWarning=null, next Sept 2026 PMI/CPI/FOMC/SBV). Silent exit — no MARKET/WORK send. `log_agent_work` id=2045.

## c193 · 2026-08-15T08:13:15Z (slot=alert-commander-critical, tick=08:10)
- Signals: bus pull `get_agent_signals(agent=alert-commander, status=all, hours_back=2)` → empty ("Không có tín hiệu mới"); `get_cycle_bootstrap`'s own `agent_signals[]` also empty. Fired: 0 | Suppressed: 0 | MARKET: none. Off-hours critical-sweep (market CLOSED, Saturday); no exact-tick snapshot for 08:1x (only on-disk file `cycle-snapshot-08:09.json.tmp` is a 0-byte in-progress write) — direct `get_cycle_bootstrap`+`get_macro_snapshot`+`get_macro_calendar`+`get_agent_signals`. Gateway healthy (Step 0-GW probe OK).
- ChainCatalyst: 0 fired | 0 suppressed — no chain_catalyst signal in bus this cycle.
- Watchlist-opp: no new ticker-driving story in the 2h lookback (BSR/PLX oil-tailwind + DXG rally stories already evaluated/suppressed c191-192, no update this cycle) — nothing new to evaluate against the kinhDich gate.
- Position-danger: only held position FPT (`get_positions`: 5,000sh @ 80,300, current 68,300, -14.9% P/L). `get_alerts(type=price)` clean. singleDayDrop FPT = -1.30% (unchanged, market closed since 08-14 close) — gate fails.
- CRITICAL-always: `get_legal_risk_signals(days=1,hours_back=6)` clean. `get_crisis_early_warning` primary indicator clean (no mention-velocity crisis); corp-reputation sub-50 same 7 tickers (BSR/FRT/HUT/PLX/VCB/VJC/VNM) — BSR+PLX still DANGER tier (score 24.0, deteriorating), continued c190-192, no further escalation. No verified_chain in bus.
- Regime: NEUTRAL (fallback — macro_snapshot text has Investment-Clock/oil/gold/usdvnd/carry/yield block, no Global-Liquidity line; carry.regime flipped to UNKNOWN this cycle — carrySpread unavailable, source_tier=4, "regime suppressed per DSI-INV-1" — vs c190-192's consistent NEUTRAL/1.37pp/source_tier=2 read; flagged as an anomaly, not investigated further, not itself a firing trigger) | vol ELEVATED (rv_20d_pctile=0.796, gk_vol_20d_pct=18.62, drawdown_252d=16.38%, unchanged) | `get_vn_liquidity_state` **errored** again ("macro-indicators service unavailable") — standard thresholds used, `[SKIP]` logged | `get_foreign_room` market_saturation_pct=24.75% (outflow_z_5d=+0.0575, flat vs c190-192) | Pivot window: false (pivotWindowWarning=null, next Sept 2026 PMI/CPI/FOMC/SBV). Silent exit — no MARKET/WORK send. `log_agent_work` id=2046.
