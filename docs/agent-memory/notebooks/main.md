# Dev Team — Sprint Boundary Notebook

**Written:** 2026-07-08T00:37Z (dev-team tick 2026-07-07T23:37Z)

## cycle-20260708T0037Z — 2 BOUNDED-1 picks (1 stale-close, 1 real recurring-bug fix via cowork-refactory-expert), cold-evict

- **Preflight:** RUN verdict — SF-1 + fire-election locks acquired cleanly. GCC-PREFLIGHT read, no HEAD.lock, no stale worktree locks.
- **Step 0a drain:** same 4 stale non-signal-shape inbox files as every prior tick (1 cowork-team snapshot + 3 price_anomaly_v1 dumps) — confirmed non-signal-shape via `jq keys`, left untouched. `signal_queue` NEW=0, orphan-signal probe 0 held. Neither persist-guard threshold hit (4 files ≤50, signals.db mtime 1h < 24h) — no drain commit needed.
- **Step 0a.5 CI probe:** GREEN on HEAD `be18a25f8` (run 28907008819).
- **Step 0b:** `.head` idle, WIP=0 → BOUNDED-1 engaged.
- **BOUNDED-1 pick #1 — `FIX-EVIDENCE-PIPELINE-STARVED`** (P-high): promoted via the REAL promote script (not manual peek, per prior tick's lesson). Task's own `status_note` already flagged it superseded: "Root reframed 2026-07-01 (PO)... do NOT dispatch directly." Cross-referenced `.task_board.head` (deprecated stub) note confirming sprint `PREDICTION-EVIDENCE-REVIVAL` shipped + QA-approved 2026-07-01T07:45-52Z (commits `24d1a4b5`/`20264221`, TASK-EVIDENCE-HOP1-MCP + HOP2-AGENTS). Live-verified via `get_evidence_summary(FPT)`: 11 fragments, 3+ distinct producer types (news_sentiment_stock, foreign_flow_institutional, bctc_roe_strong), TRUSTED rows (n=16/n=18) with correct direction resolution — confirms the monoculture + direction-hardcode bugs were already fixed. Closed via `scripts/devteam-close-task-done-verified.jq` (generic dispatcher-side close, no worker spawned). Commit `b0b7bdb63`.
- **BOUNDED-1 pick #2 — `FIX-CHEF-SENDTELEGRAM-ARGSHAPE`** (P-high, `zone:"cross-service/"` — NOT a dev-* zone, `owner:"cowork-refactory-expert"` explicit in backlog-detail): a genuine, real, PO-tracked recurring-bug task (3rd occurrence, opened 2026-06-12 per `sprint-2026-06-12-po.md` STEP po-S8) that had sat undispatched for ~4 weeks — BOUNDED-1's gap-closing purpose working as designed. Pre-verified live: grepped all `send_telegram(` call sites in `docs/agents/unified-agent/**` — all already used correct named-param shape at the pseudocode level, so this was NOT a stale-close case; genuine investigation needed. Zone-detect Tier-1 doesn't apply (`cross-service/` isn't `apps/<service>/`) — routed by the task's own explicit `owner` field instead of the dev-* specialist table (correct override, mirrors last tick's Tier-2 override precedent in the opposite direction).
  - Claimed `task:FIX-CHEF-SENDTELEGRAM-ARGSHAPE` (Phase 3.5 dispatcher-wrap), dispatched `cowork-refactory-expert` in background with full pre-verify context + explicit DoD.
  - **Agent completed cleanly this time** (unlike last tick's dropped dev-mcp-server): root-caused to ambiguous natural-language `on_error` retry-path instructions in `docs/agents/unified-agent/init.md:56` and `.claude/skills/cowork-boundary/SKILL.md:11` (`send_telegram(work) one-line error` — an LLM could misread this as a bare-string call), not the visible Step-7 pseudocode. Added defensive guardrail blocks at every call site (chef.md, chef-telemetry.md) plus fixed both ambiguous on_error lines to explicit `channel=`/`message=` form. Self-committed (`52538c2ac` fix + `aed32e900` journal+board), self-wrote DJ-GATE-1 journal entry, self-flipped task_board `in_progress`→`done_verified`, pushed.
  - **Trust-but-verify pass** (still mandatory even on a clean-looking completion): confirmed both commits landed, diffs are minimal/sound/match the DoD, journal entry well-structured, task_board correctly `done_verified`. Found ONE gap: agent updated `task_board` but left top-level `.head` still pointing `in_progress` at the now-done task — reset it myself (commit `520b5aa3a`). Ran Merge Gate Controls (index-check, tree-verify on final commit) — both PASS. Released `task:FIX-CHEF-SENDTELEGRAM-ARGSHAPE` lock (was still correctly held by dev-team, not the agent — Phase 3.5's outer lock is dev-team's to release after the tier batch returns).
  - Net: 3 commits this sub-cycle (`a3fe0762a` claim, `52538c2ac`+`aed32e900` from the agent, `520b5aa3a` head-reset by me).
- **Step 4.0.5 mock-guard HARD-FAIL** — same 2 Go `*_test.go` stub-URL FPs as every prior tick (6th+ consecutive occurrence). Still tracked as `FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO` (still open, undispatched) — skipped re-filing, informational WORK log only.
- **Step 4.2 Cold-evict:** `done=26>10, done_verified=2>0` → evicted → `archive/2026-07.json` (hot 727078B→714974B). Post-eviction Stage-2 validate PASS (same 118 pre-existing non-blocking SHG-migration warnings — one fewer than last tick's 119, since FIX-EVIDENCE-PIPELINE-STARVED's own coherence warning cleared on close). Commit `2833661bd`.
- Commits this span: `b0b7bdb63`(stale-close FIX-EVIDENCE-PIPELINE-STARVED)→`a3fe0762a`(claim FIX-CHEF-SENDTELEGRAM-ARGSHAPE)→`52538c2ac`(agent fix)→`aed32e900`(agent journal+board)→`520b5aa3a`(head-reset)→`2833661bd`(cold-evict). Push-backstop clean, tsc OK all 6 pushes.

### Queue watch for next cycle
- **`FIX-NEWS-CB-FALSE-CLOSED`** (P1, backlog, NOT `supervised`) — carried forward from last tick, NOT touched this tick either (BOUNDED-1 picked 2 different rows). Confirmed LIVE+WORSE as of last tick (66 consecutive errors vs 14 at filing). Worth a direct PO look — still eligible for an unpredictable future BOUNDED-1 pick since it's not supervised.
- 4 stale non-signal-shape inbox files (unchanged, still benign) — same as noted last 3+ ticks.
- `FIX-PDF-EXTRACTOR-TEST-SYS-MODULES-LEAK` (backlog, dev-pdf-extractor) — still open, not urgent.
- `BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` (P1, supervised) — still undispatched. This tick: 1 stale-close (FIX-EVIDENCE-PIPELINE-STARVED) + 1 genuine hit (FIX-CHEF-SENDTELEGRAM-ARGSHAPE) — BOUNDED-1's hit rate stays mixed-but-real, not pure churn.
- `FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO` — 6th+ consecutive tick, still undispatched.
- **New lesson this tick:** a task's own `status_note`/`.task_board.head` deprecated-stub notes can carry MORE CURRENT ground truth than the task's own title — always cross-check both before trusting a backlog row's literal description, especially for rows dated weeks/months old.
- **New lesson this tick:** non-`apps/<service>/` zones (e.g. `cross-service/`) are legitimate and route via the task's own explicit `owner` field, NOT through zone-detect's dev-* Tier-1/2/3 table (which is scoped to dev-* specialist resolution only). `cowork-refactory-expert` is the correct owner for agent/cowork `.md` file fixes per `feedback_agent_md_factory`.
- **New lesson this tick:** even a specialist agent that self-commits/self-journals/self-flips-board cleanly (no dropped-worker gap this time) can still leave a smaller gap (stale `.head` pointer) — trust-but-verify remains mandatory on every completion, not just suspicious-looking ones.

### Carry-forward (unchanged lanes)
- 8 P0 sprint-scale structural splits — still frozen `supervised:true`, awaiting architect briefs.
- worktree.baseRef=head still set — verify before worktree-parallel dispatch.
- Review lane: `F1-LAUNCHD-COWORK-BACKSTOP` + `FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED` (REVIEW, since prior-prior tick), `PDF-TEST-01-FIX` (BLOCKED), 2 pre-existing BLOCKED BCTC-refine rows, 1 old DEFERRED (`ARCH-SHIP-WAVE-REAUDIT`, since 2026-06-11 — long-stale, candidate for a future PO look).
