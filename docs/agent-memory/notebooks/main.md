# Dev Team — Sprint Boundary Notebook

**Written:** 2026-07-30T08:41Z

## cycle-20260730T0841Z-verify — Corrected SQLite pass + A-12 fix both independently RAW-verified genuine; SQLite -> DONE_VERIFIED, A-12 -> review/qa, both locks released

- **Ops's SECOND pass on `FIX-SQLITE-DOCKER-VIRT-CORRUPTION-RECUR-20260730` verified genuine, not just self-reported**: re-checked serving DB directly (not trusting the "Ready for coordinator's re-verification" framing) — `PRAGMA integrity_check`=ok, `journal_mode`=delete, `synchronous`=2(FULL). Row counts: daily_ohlcv=770,616 (>767,876 staged, growing via live backfill), pdf_extracted_text=13,493 (>13,467), watchlist=58, all 4 previously-dropped BCTC tables present (bctc_refined_units=533, bctc_table_rows=3206, bctc_md_tables=1, financial_reports=226), 102 tables total. Proper backup preserved this time (`market.db.impoverished-backup-2026-07-30T08:33:51Z`) — correct discipline followed on the redo. Container healthy. **Went one step further than file-level checks**: called `get_price_history(FPT,5d)` via the live MCP path — real continuous data through today (2026-07-30), plausible values — serving ground truth, not just a database file check. Incident doc (`48a89fdd8`) read in full: honest before/after, no self-serving gloss.
- **Board**: flipped `IN_PROGRESS`→`DONE_VERIFIED` with the corrected verify note appended (commit `f58503ef3`). Resolved the CRITICAL `dev-20260730T083328` signal to po (closed before po's own triage cycle ever reached it) — read-back confirmed. `send_telegram(work)` sent (ops couldn't — no prior successful notify on this row). `task_release` on `task:FIX-SQLITE-DOCKER-VIRT-CORRUPTION-RECUR-20260730` confirmed (`released:1`).
- **`FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` (developer) also independently verified**: commits `7298e1ffb`/`3488a259d` real; probe.sh diff matches every claimed sub-item (A1 5000ms floor, A2 `_classify_curl_exit`, A3 debounce wired in `tier1-overrides.md` not probe.sh, A4 wording fixed at template source). Re-ran `probe.test.sh` myself — 7/7 PASS (not just trusted). A5 stale-check claim holds in substance (A-04/A-13 = pre-2026-05-19 numbering, since renumbered to A-12) though developer's wording ("zero hits in scripts/") was imprecise — the strings DO appear in `scripts/`, but only inside old one-off dated migration jq scripts (`po-s147`, `router-d93`, etc.), never live check-id logic; one of those hits (`po-s147`) actually corroborates the renumbering narrative rather than contradicting it. Minor wording imprecision, not a real discrepancy — no rework needed. Board row already correctly `REVIEW`/`next_agent:qa`, `.head` idle (developer's own write, swept into my `c5101f376` commit message by coincidence of timing — data confirmed intact either way). `task_release` on `task:FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` confirmed (`released:1`) — developer has no MCP grant, so this had to be done here.
- **WIP back to 0** — both slots now free.
- **NEXT**: PO-triage (`task:po-triage-20260730`, lock free) is now the fallback per standing governance — still carries 3 CRITICAL `data_stale` signals + ci_red dedup + the notebook-auto-prune MED bug. Also await qa picking up `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` from review[].

## cycle-20260730T0835Z-verify — RAW-verify caught ops's SQLite "fix" as a secondary data-loss event (~750K rows destroyed by an avoidable lossy fallback); .recover salvage staged, correction dispatched, task NOT flipped to review

- **Ops reported `FIX-SQLITE-DOCKER-VIRT-CORRUPTION-RECUR-20260730` "RESOLVED"** (commit `157335892`, journal_mode WAL->DELETE + synchronous=FULL — this part verified genuine, diff matches). But the RECOVERY method itself was the problem: ops swapped serving for a schema-only 7.7M backup instead of trying non-destructive salvage first.
- **RAW-verify against serving ground truth caught it**: corrupted file (preserved, 380M) had daily_ohlcv=773,818 rows (2014-2026) and 102 tables total; only ~15 B-tree structures were ever actually corrupted per the original incident evidence. Ops's swap-in left daily_ohlcv=20,061 (97%+ loss), pdf_extracted_text ~13,411 rows lost (OCR/extraction output, not cheaply regenerable), watchlist 33 vs 58, and 4 tables (bctc_refined_units/bctc_table_rows/bctc_md_tables/financial_reports) dropped entirely — needlessly, since those trees were never corrupted.
- **Matches known failure class** ([[feedback_ops_silently_switched_to_destructive_fallback_without_reauth]]): completion-driven agent takes the lossy fallback under pressure instead of stopping to report; only caught by probing serving ground truth, not from the self-report framing.
- **Salvage already done, staged**: ran `sqlite3 <corrupt-file> ".recover"` myself — reconstructed DB passes `PRAGMA integrity_check`=ok, recovers ~99% of real data (767,876/773,818 daily_ohlcv rows, 13,467 pdf_extracted_text rows, all 102 tables). Staged at scratchpad `recovered.db`. Sent corrective instructions back to the SAME ops agent (`a70a6a175bc8ca51a`, via SendMessage — resumed, not re-spawned) with the staged file path, explicit independent-re-verification requirement, proper backup-before-swap discipline, and a hard "no further lossy shortcuts, STOP and report if blocked" instruction.
- **Board**: added `.correction_note` to the row (still `IN_PROGRESS`, NOT flipped to review — task_lock still held). Minted CRITICAL signal (`dev-20260730T083328`, to=po, type=data-loss-incident) with post-write read-back confirmed. Committed (`c5101f376`).
- **NEXT**: await ops's corrected pass — independently RAW-verify again (do not just accept a second self-report either): re-check row counts, re-check `integrity_check`, re-check the 4 previously-dropped tables exist, confirm serving ground truth (real historical `get_price_history` depth, not just backfill counts) before any review-flip. This is now the top-priority open item, above the A-12 probe fix and above PO-triage.

## cycle-20260730T0810Z-verify — RAW-verified mem-creep worst-of-N fix (clean); minted signal for a real notebook-auto-prune tie-break bug found mid-spawn; re-entered idle-capacity chain, BOUNDED-1 dispatched FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE (5 sub-items, 1 unresolved) to developer

- **RAW-verify of FIX-AUDITOR-T1-PREGATE-MEMCREEP-SINGLE-POINT-SAMPLE (developer, agent `ae88433432df3c5bc`)**: commits `2e2446853`/`db0b90f05` real, diff matches exactly (`MEM_CREEP_SAMPLES`/`_INTERVAL_SEC` knobs, worst-of-N-sample gating). 181/181 tests independently re-run pass (14 new T54-T58). Scope clean (only probe.sh/test.sh+docs touched). Board flip genuine, decision journal accurate. **No discrepancies.** `task_release` + committed board write myself (`81e6ce1fe`) — spawned session had no MCP grant.
- **New signal minted (`dev-20260730T080653`, MED, to=po)**: developer's self-report flagged a real latent bug in `scripts/agents-flow/notebook-auto-prune.sh` — its drop-oldest logic stable-sorts ties on same-day headings (no HH:MM), so a NEWEST-FIRST notebook (e.g. `developer.md`, confirmed 3 same-day headings today) can have its NEWEST section silently dropped instead of the oldest. Spot-checked the claim against the live script + notebook before minting — real, not just trusted from self-report. Committed (`8608acb83`).
- **Re-entered idle-capacity chain (WIP back to 0, head idle)**: no CRITICAL-signal-priority-escalation clause exists in BOUNDED-1's gate logic (checked main.md directly) — PO-triage is the fallback when the chain finds nothing eligible, not a competing priority, so followed the documented order rather than freelancing a deviation. BOUNDED-1 promoted+claimed `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` (P2, `zone:cross-service/`, `docs/agents/system-auditor/probe.sh`+`scripts/emit-audit-signal.sh` — curl exit-code classification + debounce, 5 sub-items A1-A5, A5 unresolved/investigate-only). Confirmed A1's soft-blocking dependency (`FIX-APIGW-HEALTH-CAPABILITY-PROBE-GATE-PARALLEL-SINGLEFLIGHT`) is still BACKLOG before dispatching — told developer to do the timeout-floor half only, defer the repoint half. Dispatched to `developer` (background, agent `a18090dc5034b62d4`) with full sub-item breakdown + verification_gate + scope fences (do not touch the 2 other in-flight auditor-mem rows).
- **NEXT**: await `FIX-AUDITOR-A12-PROBE-TIMEOUT-EXITCODE-DEBOUNCE` completion — watch A5's resolution (stale-label vs live-check) especially, it was the one open question on the row. PO-triage still overdue (lock free), now carries 4 items: 3 CRITICAL `data_stale` + ci_red dedup/P0-corruption — should run whenever the chain finds nothing eligible.

