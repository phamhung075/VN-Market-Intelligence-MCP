# Dev Team — Sprint Boundary Notebook

**Written:** 2026-07-07T23:56Z (dev-team tick 2026-07-07T23:07Z, spanning 23:16Z→23:56Z)

## cycle-20260707T2307Z — BOUNDED-1 found + fixed a genuine live CI-red (rare positive outcome), dropped-worker adoption, cold-evict

- **Preflight:** RUN verdict — SF-1 (`dev-team-cron-singleton`) + fire-election (`cron:dev-team:2026-07-07T23:07Z`) locks acquired cleanly. GCC-PREFLIGHT read, no HEAD.lock, worktree clean.
- **Step 0a drain:** same 4 stale non-signal-shape inbox files as prior tick (1 cowork-team snapshot + 3 price_anomaly_v1 dumps, 07-02/03/04) — correctly left untouched. `.signal_queue` empty, orphan-signal probe 0 held.
- **Step 0a.5 CI probe:** GREEN on HEAD `f9157aab0` at probe time.
- **Step 0b:** `.head` idle, WIP=0 → BOUNDED-1 engaged.
- **BOUNDED-1 pick #1 — `FIX-TA-INDICATORS-TIER3-ROUTING`** (P1, stale-close): live-probed `get_technical_indicators(FPT)`/`(VCB)` — both return full non-N/A indicators (bug claimed ALL N/A). Code-verify: `technicalIndicatorTools.ts:614/648/669` hardcodes `source_tier:3 as const` unconditionally by design ("derived — no live external source" per doc comment) on BOTH the Go-primary and local-DB-fallback paths; repo-wide `grep ta_ohlcv` = 0 matches — no such pre-computed table exists anywhere in this codebase for the bug to have bypassed. Bug premise misdiagnosed hardcoded-by-design architecture as a routing defect. Closed done_verified. Commit `b078abe1c`.
- **BOUNDED-1 pick #2 — `FIX-NEWS-CB-FALSE-CLOSED`** (P1, top-rank on manual peek, SKIPPED — not via the real promote script, see below): live-probed `get_system_status` — confirmed the underlying two-layer CB-vs-source-health split is STILL LIVE and WORSE than filed (66 consecutive errors now vs 14 at filing time; internal CB counters report `[OK] failures:0` while the separate source-health ledger correctly shows `Ngưng`/stopped with `66 ⚠`). Explicitly annotated in its own title "PLAN-ONLY at PO layer — dev-team chain implements" — initially treated as a soft PO-planning gate and manually skipped in favor of peeking the next candidate. **This manual skip turned out to be moot**: the real `devteam-backlog-promote-bounded1.jq` script (run immediately after, per the documented main.md bash snippet) independently picked a DIFFERENT top row (`CI-TEST-04-FIX`, priority `"high"` — a string my manual peek's rank function didn't recognize, ranking it below P1 in my mirror but correctly above P1 in the real script). Net effect: FIX-NEWS-CB-FALSE-CLOSED was never touched this tick either way — still open in backlog, still real, still worse than filed. Carried forward below.
- **BOUNDED-1 pick #3 (via real promote+claim script) — `CI-TEST-04-FIX`** (priority `high`, zone stored as `qa` but actual code is `apps/mcp-server/`): claimed via the documented `devteam-backlog-promote-bounded1.jq`/`-claim-bounded1.jq` pair (not manual override). Pre-verify via its own `verification_gate=ci_green_on_subsequent_push` turned up a **genuinely live CI-red on HEAD** (commit `b078abe1c`, run 28905477363, job "bun test" — FAILED, unrelated to the JSON-only commit that triggered it). Root-caused via `gh run view --log-failed` + local repro: `src/__tests__/183-alert-accuracy.test.ts` AC-2b failing (`expected hits=1, got 0`), deterministic, not flaky-on-rerun.
  - PRE-CLAIM (`intent:dev-mcp-server:CI-TEST-04-FIX`) + Phase 3.5 dispatcher-wrap claim (`task:CI-TEST-04-FIX`) + zone-resolved `apps/mcp-server/` → `dev-mcp-server` via system-map (stored zone field `"qa"` was a role label, not a code path — Tier-2 file-based inference correctly overrode it).
  - Dispatched `dev-mcp-server` in background with full root-cause context (the failing assertion, the SQL query under `alertAccuracy.ts`, the specific hypothesis: ISO-8601-vs-`datetime('now')` SQLite string-compare bug, matching known memory `feedback_sqlite_iso8601_datetime_strcompare_bypass`).
  - **Agent stalled**: ran ~13.3 min, produced a correct, complete, well-commented diff (bound `nowIso = new Date().toISOString()` for ISO-to-ISO price-window compares instead of mixing with SQLite `datetime('now')`) but never committed/pushed/updated task_board/released its intent lock before returning — its own completion message referenced an unrelated phantom task id ("standing by for task b4kpmr9dl"), suggesting internal confusion, not a real blocker. No orphaned lock found (`intent:` TTL 600s had already self-expired by the time I checked; `task:CI-TEST-04-FIX` was still correctly held by dev-team, not the agent).
  - **Dead-worker adoption** (per `feedback_dead_worker_uncommitted_live_file_revert` convention — probe tree before reverting): inspected the diff directly — sound, well-reasoned, cross-checked both `market_prices_history.fetched_at` writers (`pushPricesHandler.ts`, `hose.ts`) bind ISO strings, not raw SQL `datetime()`. Verified empirically: target test 20/20 pass in isolation (was 19/20); full local `bun test` suite showed 49 fails across 15 unrelated files (news-poll/OCR/SSC, 2 native "Illegal instruction" crashes) — spot-checked one file in isolation (8/8 clean) confirming this was parallel-run environment noise (missing local chromium, resource contention), not a regression from the fix.
  - Landed the fix myself: `index-check.sh` PASS → commit `e170624d3` (explicit-path, no `-a`) → `tree-verify.sh` PASS → push (CI triggered) → DJ-GATE-1 decision-journal entry written (`docs/agent-memory/decisions/sprint-SYSTEMIC-REMAKE-P1-dev-team.md`, new file, STEP dev-team-S1) → task_board `in_progress`→`review` with full root-cause note, `task:CI-TEST-04-FIX` released, commit `c55e41dc3`.
  - Stopped the redundant duplicate agent instance (it resumed post-"completion" and started re-running its own full-suite check — `TaskStop` on `a92025a0a690c9953`, clean, no orphaned locks/dirty state left).
  - **CI confirmed GREEN on `e170624d3`** (`gh run list` → `conclusion:success`) — verification_gate satisfied. Promoted `review`→`done_verified`, commit `a3ad813d7`.
  - Net: 5 commits this sub-cycle (`b078abe1c`, `65c07e518`, `e170624d3`, `c55e41dc3`, `a3ad813d7`), all pre-push tsc-verified, all pushed individually with commit-mutex discipline throughout (claim-before/release-after on every write, zero lapses this tick).
- **Step 1 PO Triage — SKIPPED entirely this tick** (not gate-checked, structurally bypassed): main.md's BOUNDED-1 block jumps straight to `execute` on a successful claim, bypassing Step 1 by design — Step 1 is only the fallback for when nothing gets claimed. Not applicable this tick.
- **Step 4.0.5 mock-guard HARD-FAIL** — same 2 Go `*_test.go` stub-URL FPs as every prior tick (5th+ consecutive occurrence). Still tracked as backlog TODO `FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO` (confirmed still open, undispatched) — skipped re-filing, informational WORK log only.
- **Step 4.2 Cold-evict:** `done=28>10, done_verified=2>0` → evicted → `archive/2026-07.json` (hot 717559B / cold 570383B post-evict, both jq-validated). Post-eviction Stage-2 validate PASS (same 119 pre-existing non-blocking SHG-migration warnings). Commit `22c0af164`.
- Commits this span: `b078abe1c`(stale-close FIX-TA-INDICATORS-TIER3-ROUTING)→`65c07e518`(claim CI-TEST-04-FIX)→`e170624d3`(fix landed)→`c55e41dc3`(→review)→`a3ad813d7`(→done_verified)→`22c0af164`(cold-evict). Push-backstop clean, tsc OK all 6 pushes.

### Queue watch for next cycle
- **`FIX-NEWS-CB-FALSE-CLOSED`** (P1, backlog, `supervised` NOT set) — confirmed LIVE and WORSE than filed this tick (66 consecutive errors on Reuters RSS + Trading Economics since 2026-06-13T23:18Z restart, internal CB counters still `[OK] failures:0` while source-health ledger shows `Ngưng`/stopped — the two-layer split IS the bug). Annotated "PLAN-ONLY at PO layer — dev-team chain implements" in its own title. Two fix directions already spelled out (lower CB threshold and/or add never-succeeded-since-restart detector). Since it's not `supervised:true`, the NEXT BOUNDED-1 promote run may legitimately pick it (the real script's rank function may or may not rank it above other P1/high rows — untested this tick since `CI-TEST-04-FIX` won the actual pick). Worth a direct PO look given severity has grown since filing, rather than waiting for another BOUNDED-1 roll.
- 4 stale non-signal-shape inbox files (unchanged, still benign) — same as noted last 2 ticks.
- `FIX-PDF-EXTRACTOR-TEST-SYS-MODULES-LEAK` (backlog, dev-pdf-extractor) — still open, not urgent.
- `BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP` (P1, supervised) — still undispatched; this tick's #1 pick (FIX-TA-INDICATORS-TIER3-ROUTING) was ANOTHER stale-misdiagnosed row, reinforcing the case, though this tick also produced a genuine hit (CI-TEST-04-FIX) — so BOUNDED-1's hit rate isn't purely churn, just noisy.
- `FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO` — 5th+ consecutive tick, still undispatched.
- **Manual-peek rank-function caveat for future ticks**: my ad-hoc jq mirror of the promote script's priority ranking does NOT recognize string values like `"high"` (only `P0/P1/P2/P3/critical/null`) — it silently mis-ranked `CI-TEST-04-FIX` (actual winner) below P1 rows in my own peek. Do not trust a manual peek's ordering for anything beyond "is there an eligible non-stale-looking candidate at all" — always let the real promote script make the final pick.

### Carry-forward (unchanged lanes)
- 8 P0 sprint-scale structural splits — still frozen `supervised:true`, awaiting architect briefs.
- worktree.baseRef=head still set — verify before worktree-parallel dispatch.
- Review lane unchanged from prior tick's carry-forward except CI-TEST-04-FIX now fully closed (passed through review→done_verified same tick): `F1-LAUNCHD-COWORK-BACKSTOP` + `FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED` (REVIEW, entered 21:00:37Z prior tick), `PDF-TEST-01-FIX` (BLOCKED), 2 pre-existing BLOCKED BCTC-refine rows, 1 old DEFERRED (`ARCH-SHIP-WAVE-REAUDIT`, since 2026-06-11 — long-stale, candidate for a future PO look).
