# Dev Team — Sprint Boundary Notebook

**Written:** 2026-07-07T21:50Z (dev-team tick 20260707T194651Z, spanning 19:46Z→21:50Z, 2 re-entrant fires)

## cycle-20260707T1946Z — kinh-dich error-notify wired, cowork gateway-blind bootstrap guard shipped, PO out-of-band signal triage, cold-evict

- **KD-OBS-01-FIX ✅ DONE_VERIFIED** — BOUNDED-1 idle-capacity pickup (pre-verified genuine via semble search, not stale). Wired kinh-dich error paths (`kinhDichReadingHandler.ts`, `kinhDichSignalsHandler.ts`, `kinhDichMarketHandler.ts`, `kinhdich/index.ts`) to a new fire-and-forget `notifyKinhDichError` → BUG Telegram channel; 8 catch-block sites, 11 new tests/43 expect. dev-mcp-server implemented (3 commits), qa independently re-ran tests/tsc/3× isolated suite and approved (3 more commits). Merged `6c1cd6aa9`.
- **Process gap found + worked around: BOUNDED-1's `JUMP TO execute` shortcut skips Step 1 PO Triage entirely**, silently orphaning that tick's `pendingSignals[]` drained in Step 0a (their fingerprints are marked `routed-to-po` in `signals_processed` and never resurface). Dispatched `po` out-of-band with all 5 deferred signals rather than let a genuine one (bctc-analyst bug-escalation) get lost. Not yet filed as a formal backlog FIX row — durable fix (BOUNDED-1 should still forward `pendingSignals[]` to PO, or defer JUMP-TO-execute until Step 1 has consumed them) is a candidate for a future row.
- **FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP ✅ DONE_VERIFIED — the real headline from PO's out-of-band triage.** PO minted this from a live bctc-analyst signal: local cowork-spawned subagents bootstrapping with Read/Write/Edit ONLY (zero `mcp__gateway__call_tool`, zero Bash), 3× consecutive in one session — live-corroborated a 2nd, independent way when PO's own out-of-band triage subagent (a different session) hit the identical gap. agent-father root-caused it as a session-level MCP transport gap (config-layer, fixed 2026-06-23, recurs after outages until `/mcp` reconnect) — NOT a missing tool-grant (fleet grep confirmed every cowork agent already declares the gateway tool). Traced the REAL live execution path (bctc-analyst: `main.md→cycle.md→stage-bootstrap.md→cycle-bootstrap/SKILL.md`, not the AC-named `step-0-cowork/SKILL.md`, which is only reached via `always_load`) and fixed BOTH files with a CONFIRMED-BLIND vs TRANSIENT guard: on confirmed-blind, skips `send_telegram` (itself a gateway call that fails identically — the exact flaw that produced 4 divergent ad-hoc raw-Write escalations on 2026-07-07) and instead writes the canonical `fail-loud-protocol.md` §5 signal schema directly, DEFERs cleanly, no lock held. QA independently re-verified all 5 points (re-grepped the fleet, re-traced routing itself, cross-checked the CONFIRMED-BLIND string patterns against 3 real historical incident docs) — hit the identical gateway-blind gap mid-review and correctly used the sanctioned `mcp-call.sh` Bash fallback rather than stalling. Approved. Merged `caba878b7`+`83bca6c04`.
- **PO out-of-band signal triage** — 5 signals: 1 actionable (→ FIX above), 2 stale-closed (ci-red already downstream+green; ops-recon already DONE_VERIFIED/archived), 2 informational (cowork-fire events, folded into the FIX's evidence). Committed `92133678a`.
- **Recurring `.head` clobber-adjacent drift — 2nd occurrence this session (ops, then qa on KD-OBS-01-FIX close) — dispatcher-side guarded reset applied both times.** 3rd occurrence did NOT recur on this tick's qa close (FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP) — qa correctly reset `.head` to idle itself this time. Not escalated as a formal ticket; watch for a 3rd true recurrence.
- **mock-guard.sh HARD-FAIL** — same 2 Go `*_test.go` stub-URL hits as last tick, confirmed known false-positive (`feedback_mockguard_go_test_false_positive.md`), tracked `FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO` (TODO) — not re-filed.
- **Cold-evict (Step 4.2):** done=33>10, done_verified=2>0 → evicted → `docs/data/orch/archive/2026-07.json`. Commit `512105247`.
- Commits this span: `ecae1bf5e`(signal drain)→`a60bb69bd`(BOUNDED-1 claim)→3×dev-mcp-server→3×qa(KD-OBS-01-FIX)→`92133678a`(po triage)→`20cd753a1`(head-reset fix)→`caba878b7`(agent-father)→`83bca6c04`(qa, FIX-COWORK)→`512105247`(cold-evict). Push-backstop clean, tsc OK. Orch-state coherence warnings steady at 119 (pre-existing SHG-migration drift, non-blocking).

### Queue watch for next cycle
- `FIX-COWORK-SUBAGENT-GATEWAY-BLIND-BOOTSTRAP`'s guard is a graceful DEFER, not a permanent cure — the underlying session-level MCP transport gap still needs a `/mcp` reconnect after outages. If cowork subagents keep going gateway-blind repeatedly, that's an infra/ops signal, not a repeat of this FIX.
- BOUNDED-1 PO-triage-bypass process gap (see above) — still not filed as a formal row. Consider filing if it recurs on a future BOUNDED-1 tick with a genuinely actionable orphaned signal.
- BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP (P1, supervised) — still not dispatched.

### Carry-forward (unchanged lanes)
- 8 P0 sprint-scale structural splits — still frozen `supervised:true`, awaiting architect briefs.
- worktree.baseRef=head still set — verify before worktree-parallel dispatch.
