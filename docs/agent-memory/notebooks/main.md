# Dev Team — Sprint Boundary Notebook

**Written:** 2026-07-07T19:20Z (dev-team tick 20260707T164729Z, spanning 16:47Z→19:20Z re-entrant fires)

## cycle-20260707T1647Z — CI-red fix merged, Monday price gap backfilled, 2.5mo VPS OHLCV auto-recovery bug found+fixed, 13 telegram reports triaged, cold-evict

- **CI-RED-c5b5f885-FIX ✅ DONE_VERIFIED** — bun-test fix, QA independently re-verified and approved. Merged `b40310b5e`.
- **DATA-BACKFILL-PRICES-20260706-MONDAY-GAP ✅ DONE_VERIFIED** — user-reported stale Monday/today prices ("burn out token"). dev-mcp-server backfilled 585 rows via targeted VNDirect fetch (`scripts/migrations/backfill-ohlcv-gap-2026-07-06.ts`, reused `writeOhlcvBatch`), spot-checked against VCB/FPT/HPG/VNM/VIC/VNINDEX, zero fabricated data. Committed `8fc69cd12`.
- **OPS-OHLCV-VPS-BACKFILL-STALL-NONWATCHLIST ✅ DONE_VERIFIED — the real headline.** During the Monday-gap backfill, dev-mcp-server surfaced that the VPS backfill-queue poller marks rows "done" without ever pushing data. Upgraded this pre-existing low-priority (filed 2026-07-01, re-emitted twice unresolved) row to P1 pipeline-wide. `ops-vps-fetch` SSH recon confirmed root cause live: `scripts/deploy-vinahost.sh` §6 has NEVER deployed `/root/fetch-ohlcv-backfill.sh` correctly since commit `68263eb6b` (2026-04-17) — Phase A (Apr17→Jun30) script absent entirely; Phase B (Jun30→present) manually `scp`'d outside the sed-substitution pipeline, leaving literal `__MCP_BASE__`/`__API_KEY__` unsubstituted. Zero successful `push-ohlcv-history` calls across the poller's ENTIRE 2.5-month history, unrelated to (predates by 2.5mo) the Cloudflare Tunnel outage. `ops` fixed `deploy-vinahost.sh` (commit `e89f09ac1`), redeployed to VPS, and got REAL fresh end-to-end confirmation: 109 tickers, 77,434 bars pushed, exit 0 (2026-07-07T19:10:36Z). Dispatcher caught+fixed a `.head` clobber drift the closing agent left behind (`status:"done_verified"` instead of `idle` — would have silently blocked next auto-dispatch).
- **Telegram triage (13 reports, ids 3505-3517) — 0 escalated, all resolved.** `po` live-verified each: Docker-daemon incident already fixed (gateway healthy); pollNews all-dark was tail of the already-closed Cloudflare Tunnel outage (fired 1min before recovery); 9× BCTC-1345b low-confidence skips all mapped to already-ticketed rows (`W5-FU-CTG-REFINE-96e36139`, `REFLOW-MBB-Q1-2026`, `FIX-BCTC-BANK-SCALAR-MAPPING`, `SPIKE-BCTC-CONVICTION-COMPOSITE-DRAG-PERFECT-FINANCIAL`, `OPS-BCTC-REFINE-REPASS-NONBANK-5T`) — designed low-confidence gate working correctly, not a regression; VCB no-baseline-price was a Saturday off-market date + transient Docker-restart-window null, marked monitoring; OHLCV-DEPTH shallow-codes (BDI/DLC/JSH/SIS/VDC) all confirmed suspended/delisted non-watchlist tickers (VNDirect genuinely has 0 bars) — stale residue landing 37s after the fix's good run, not the bug.
- **mock-guard.sh HARD-FAIL** — 2 Go `*_test.go` stub-URL hits, confirmed known false-positive (memory `feedback_mockguard_go_test_false_positive.md`); fix already tracked `FIX-MOCKGUARD-SCOPE-EXCLUDE-TESTGO` (TODO) — not re-filed, not re-surfaced.
- **Cold-evict (Step 4.2):** done=60>10, done_verified=29>0 → evicted → `docs/data/orch/archive/2026-07.json`. Commit `aae651f5e`.
- Commits this span: `860b74f2c`→`aae5dc0bbaaf27a8b`(agent, no commit)→`cfc79eec5`→`aae651f5e`. Orch-state coherence warnings steady at 119-120 (pre-existing SHG-migration drift, non-blocking).

### Queue watch for next cycle
- Confirm `vn-ohlcv-backfill.timer` keeps firing clean on its normal ~30min cadence (no regression) — spot-check `/var/log/ohlcv-backfill-poll.log` on next VPS touch.
- Two latent observations from po's triage, deliberately NOT ticketed this tick (would be churn, not this-tick regressions): (1) `verdictResolutionJob` conflates "undeterminable" with "false_positive" on no-baseline verdicts; (2) OHLCV-DEPTH detector will keep re-firing forever on permanently-delisted tickers (BDI/DLC/JSH/SIS/VDC) — suppression candidate akin to `FIX-BCTC-1345B-REPORT-BATCH`. Surface to detector-tuning work if the systemic-review follow-through picks it up.
- BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP (P1, supervised) — still not dispatched; BOUNDED-1 stale-pickup debt persists (see memory `project_bounded1_first_pickup_stale_backlog_hygiene_debt`).

### Carry-forward (unchanged lanes)
- 8 P0 sprint-scale structural splits — still frozen `supervised:true`, awaiting architect briefs (per `project_bounded1_first_pickup_stale_backlog_hygiene_debt` 2nd failure mode).
- worktree.baseRef=head still set — verify before worktree-parallel dispatch.
