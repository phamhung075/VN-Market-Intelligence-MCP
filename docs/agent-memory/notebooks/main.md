# Dev Team — Sprint Boundary Notebook

**Written:** 2026-07-29T13:55Z

## cycle-20260729T1355Z-verify — RAW-verified pm's 7-task decomposition; caught a DUPLICATE-DECOMPOSITION bug (parent row dispatched to pm twice — ~05:46Z tick and 13:37Z tick — producing 2 overlapping child-task sets)

- **BGFAN-1 RAW-verification**: commit `400a3761f` real, on HEAD. 7 handoff files + decision journal genuinely exist on disk, matching the self-report.
- **Not a clean completion — found a genuine duplicate-dispatch bug**: `git log` shows an EARLIER commit `6617edbd4` (this session, ~05:46Z tick) already decomposed this SAME parent row into 5 tasks (`TASK-DEVTEAM-IDLE-CHAIN-1..5`). Task 1 (`SCHEMA-UTILITIES`) isn't just backlogged — it's already **REVIEW**, with real shipped code confirmed on disk: `dev_team_idle_chain` on `OrchStateSchema`, `rotation_selected()` in `scripts/lib/devteam-eligibility.jq`, `scripts/devteam-idle-chain-stamp.jq`. Exact same scope as pm's brand-new `FIX-DEVTEAM-IDLE-CHAIN-S1-SCHEMA-SELECTION` (fresh in backlog). The other 4 old tasks (`2-MAIN-FLOW`/`3-DRAIN-DURABILITY`/`4-TESTS`/`5-CONSERVATION-DOCS`) also substantially overlap the 6 remaining new tasks (`P1A/P2A/P1B/MAIN-COMPLETION/TEST-FAIRNESS/TEST-DURABLE`) — a live duplicate-work landmine if BOUNDED-1/RLC picks either set.
- **Root cause**: parent row's `plan_only:true/supervised:true/next_agent:pm` were never flipped after the first pm decomposition completed, so SLS's eligibility check saw the row as fresh again this tick and re-claimed+re-dispatched pm blind to the earlier decomposition. Matches [[feedback_pipeline_resume_stale_placeholder_duplicate_spawn_risk]].
- **Declined to release the lock or report clean**: 12 total child tasks now exist across 2 overlapping sets — reconciling which supersedes which needs pm's own judgment (it authored both sets), not a mechanical board fix. Re-spawned `pm` (background) with the full finding to merge into ONE canonical task set and stamp the parent row so a 3rd SLS pick can't recur. Sprint-task lock held pending verified reconciliation.

## cycle-20260729T1348Z — Tick 13:37Z: drained 1 context-bloat signal to po, declined an unsound BOUNDED-1 pick (PO-confirm-needed prose, no override), SLS auto-picked FIX-DEVTEAM-IDLE-CHAIN-STEP1-TRIAGE-STARVATION (P0, the idle-chain fairness fix itself), dispatched to pm

- **Preflight/GCC clean**: RUN tick `2026-07-29T13:37Z`. No HEAD.lock, no stray worktrees, no dirty-tree blockers.
- **Drain 13:37Z**: 1 routed to po (context-bloat notice, architect decision-journal). Committed `e6c5253f3`. CI probe deduped (same HEAD `aa6c044b` already signaled last tick, no new event). Dashboard-plane (`signal_queue`) empty.
- **BOUNDED-1 offered `FIX-PUSH-DELIVERY-ERROR-RATE-ALERT` (zone=multi) — declined to apply**: bare backlog stub, no `.note`/no `backlog-detail.json` entry, title itself poses an unresolved "PO confirm priority + single-FIX vs ba->architect split" question with no prior PO authorization anywhere on the row (unlike `ALPHA-S3` last tick, which carried an explicit prior USER GO override). None of BOUNDED-1's structured gates (supervised/plan_only/next_agent/owner/prose-sequencing) catch this class — a genuine coverage gap distinct from the existing PROSE-SEQUENCING gate (that one only covers ordering prose, not scope-confirmation prose). Left unpromoted in `backlog[]`, flagging for Step 1 PO Triage rather than risk repeating the exact recon-then-bounce cycle just seen on ALPHA-S3.
- **SLS fired instead (WIP2=0)**: promoted+claimed `FIX-DEVTEAM-IDLE-CHAIN-STEP1-TRIAGE-STARVATION` (P0, supervised+plan_only) — this row IS the fix for the fixed-priority idle-chain starvation this session has repeatedly observed (BOUNDED-1 wins nearly every tick; SLS/RLC/QA-Drain rarely reached). PO already ruled 2026-07-25 (aged round-robin + durable signal inbox, both parts mandatory, BOUNDED-1's cap explicitly untouched); architect brief + journal already exist. `dispatch_lane` resolved to `pm` by the promote script itself. Committed `d8cc9aa13`. Spawned `pm` (background `a4e4b40d6578d5c49`) with the full PO ruling + architect brief pointers + acceptance criteria threaded into the prompt; lock held per LOCK-LIFETIME, pending verified completion.
- SLS dispatch consumed the tick per its own JUMP-TO-end contract — did not fall through to RLC/QA-Drain/Step 1.

## cycle-20260729T1332Z-verify — RAW-verified developer's ALPHA-S3-DIVERGENCE-SCREEN-V1 recon-and-decline (legit, not implemented); caught+fixed a CANONICAL:SSOT-STATUSFLIP-LANEMOVE violation stranding WIP at 1

- **BGFAN-1 RAW-verification, all claims confirmed real**: commits `2d45e1fcc`/`022174059` real, on HEAD, in order. Developer declined to self-design the divergence/ranking/honest-null logic and routed the row to `architect` instead of implementing — spot-checked the cited precedent directly, not just trusted the prose: `getMoneyRadarComposite.ts` exists, both `computeForeignAccumRank`/`computeRelativeStrength` HTTP clients exist in `clients.ts`, and `sprint-FLOW-PRICE-ALPHA-LOOP-architect.md` genuinely shows 3+ prior zone=multi→single-zone corrections this sprint (same reasoning pattern). Judgment call is sound — a new composite detector feeding a live external Telegram digest is architect-brief territory, not a lean auto-fix.
- **Caught a lane-move bug the completion itself introduced**: developer's write set `.status:BLOCKED, next_agent:architect` but left the row physically in `.task_board.in_progress[]` — a direct violation of `execute-tier.md` CANONICAL:SSOT-STATUSFLIP-LANEMOVE (BLOCKED is explicitly named in the MUST clause's terminal-token list). WIP is computed by array membership not `.status`, so this would have falsely pinned WIP=1 and starved BOUNDED-1 on every subsequent tick until someone noticed.
- **Fixed directly**: moved row `in_progress[]` → `backlog[]`, preserved `status:BLOCKED`/`next_agent:architect`/all fields verbatim via dry-run-checked jq + `orch-apply.sh` (conservation check 695=695, no data loss). WIP confirmed back to 0. Committed `b79a73f44`.
- **Residual gap flagged, not fixed**: row is now BLOCKED+next_agent=architect+not-plan_only in `backlog[]` — per main.md's own NON-DEV-NEXT_AGENT gate documentation this is a *documented* "tracked residual gap (no dedicated sweep lane yet)", correctly withheld from BOUNDED-1 (non-dev next_agent) and not covered by Supervised-Lane Sweep (not plan_only). Needs Step 1 PO Triage or a manual `architect` dispatch to actually progress — flagging for the next PO Triage pass (5 signals already queued from this tick).
- Sprint-task lock `task:ALPHA-S3-DIVERGENCE-SCREEN-V1` released.
