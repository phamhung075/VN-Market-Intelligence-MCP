# dev-mcp-server -- Notebook

## 2026-07-30 — FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT (BOUNDED-1 idle-pickup, P2) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. AC-1 answer (read source BEFORE changing anything): the "5439" constant PO measured was never a duration — `getSlaThreshold("bctc")`'s active off-hours/weekend branch returned `minutesSinceLast{WindowEnd,EarningsWindowEnd}(now) + grace`, i.e. `now - T_anchor` for a FIXED past anchor (last VN market close or last earnings-window close). `ageMinutes` itself is `now - T_data` for the FIXED last `financial_reports` parse. Both are AGES growing 1:1 with wall-clock `now`, so `ageMinutes - thresholdMinutes = (T_anchor - T_data) - grace` is a time-invariant constant for as long as no new bctc data arrives and the day/hours regime doesn't flip — confirms PO's hypothesis exactly (not refuted): a breach recorded in that regime could never self-clear by time alone. Backed out `T_data ≈ 2026-07-20T13:50Z` from the sample data — internally consistent with the reported `stale=5950min` at the first sample.

**Fix:** `getSlaThreshold("bctc")` now returns one of exactly two FIXED, config-sourced constants gated only by `isBctcEarningsWindowActive(now)` (a boolean, not a growing quantity) — **1440 min (24h)** while an earnings-filing window is active, **10080 min (168h/7d)** otherwise (AC-5 answer). SSOT: `docs/data/system-map.json` `.project.data_sources["bctc-discover"].sla` — `earnings_window.stale_threshold_hours: 24` / `default_stale_threshold_hours: 168` — these values already existed there but were never wired into `DEFAULT_SLA_CONFIG` (which instead carried invented 120/360-min values plus the broken dynamic formula). Removed the `minutesSinceLastWindowEnd`/`minutesSinceLastEarningsWindowEnd` calls entirely from the bctc branch (those functions remain exported/used elsewhere — price/foreign_flow's own off-hours branch, and pure calendar-arithmetic tests — unaffected). The false-CRITICAL-during-quiet-period concern the removed formula was chasing is now the sole job of the already-shipped queue-depth+service-state idle/crash gate (`checkSignalSla`, FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH) — strictly more accurate (real pipeline state vs calendar guesswork) and already short-circuits before the threshold is ever consulted.

**Bidirectional proof (AC-3):** new dedicated `FIX-SLA-BCTC-THRESHOLD-TRACKS-STALENESS-NOT-CONSTANT.test.ts` proves the gate CLEARS on fresh data and FIRES on stale data in both regimes (1439/1441min around the 1440 threshold; 10079/10081min around the 10080 threshold), plus a recovery test (stale→fresh flips breach→recovery on the same `now`) and asserts the threshold is byte-identical across 3 emissions spanning market-hours/off-hours/weekend and even 90 days apart within the same window-state (AC-2) — the exact property bond_maturity/signal_quality_audit/news/sbv_fx already had. AC-4: `DEFAULT_SLA_CONFIG`'s bctc entry pinned to `{defaultThresholdMinutes:10080, earningsWindowThresholdMinutes:1440}`; the legacy `marketHoursThresholdMinutes`/`offHoursThresholdMinutes` fields were removed from `SignalSlaConfig` entirely (grep-confirmed unused elsewhere) so they cannot silently reappear.

**Test-fixture ripple (6 files):** the old 120/360-min and dynamic-formula values were baked into fixtures across `FIX-BCTC-SLA-WEEKEND.test.ts`, `FIX-BCTC-SLA-THRESHOLD-360.test.ts`, `FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts`, `1920i-freshness-sla-extension.test.ts`, `234-vps-health-sla.test.ts`, `1407b-sla-market-hours-gate.test.ts` — all updated to the new fixed values with explicit supersession notes in their docstrings (kept as historical record, not deleted); pure calendar-arithmetic tests (`isBctcEarningsWindowActive`, `lastExpectedEarningsWindowEnd`, `minutesSinceLastEarningsWindowEnd`) left untouched since those functions didn't change.

**Evidence:** tsc clean. Targeted SLA/bctc suite (8 files) 107/107 pass. Full `bun test`: 14870 pass/40 skip/60 fail/1240 files (609s) — zero of the 60 fails reference `freshnessSlaChecker`/`freshnessSlaMonitorJob` or any bctc/sla test name (grep-confirmed); same pre-existing unrelated-infra fail set (network/5000ms-timeout MCP-client tests, vps_push_log/vnstock-store schema issues) observed in both a before-my-final-fix run (56 fail) and after (60 fail) — count variance is the fail set's own known flakiness, not something I introduced. `toolCount=184`/`cronJobCount=88` unchanged (`gen-project-stats --dry-run`). Fresh local boot (`PORT=3099 DB_PATH=:memory:`, own process not the pre-existing live container): health 200 `toolCount=184`, `sessions=0`/`uptime≈6s` proving it's a genuinely fresh boot, zero sla-monitor errors in the boot log. Live-container `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200. Commit `78f945fb2`.

Zone health: tsc clean, dedicated regression suite 107/107 (was 5 failing on FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts before fixtures were updated), bidirectional clear+fire proof (not just "no longer fires"), tool/cron counts unchanged, fresh-process boot clean, full-suite fail-set keyword-scanned clean of my 8 changed files across 2 consecutive runs | HEALTHY.

## 2026-07-30 — FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H (BOUNDED-1 idle-pickup, P2) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. **Row's own hypothesis was WRONG — corrected at source, not blindly implemented.** The row assumed `signal_quality_audit.created_at` tracks the same-named `monthlySignalQualityAuditJob` cron (`0 0 1 * *`) and that 48h graded the wrong cadence class. Traced the table's writer: `insertSignalQualityAudit()` (`infrastructure/db/signalQualityAuditStore.ts`) has exactly ONE call site — `post_agent_signal`'s handler (`agentSignalTools.ts`), fired ONLY when `signal_type` ∈ {`price_confirmation`,`urgent_news`} AND `finding_data.confidence` is a number. `runMonthlySignalQualityJob()` (the actual cron) queries `signal_rejections` and sends a Telegram WORK report — it **never writes `signal_quality_audit`**. The table genuinely is event-driven, matching the ORIGINAL pre-fix comment; the real defect is that 48h was too tight for how rarely that specific event fires — live DB history: 6 rows ever, real gaps >2 weeks apart even in a healthy period, zero since 2026-06-05T20:12Z (the 76067min/52.8d staleness figure backs out to exactly that timestamp, not any monthly-cron boundary).

**Fix:** `DEFAULT_SLA_CONFIG` signal_quality_audit `defaultThresholdMinutes` 2880 (48h) → 43200 (30d) — sized off the OBSERVED real writer cadence, not the wrong monthly-cron premise. Satisfies AC-1's fixture (20d=28800min → ok; 40d=57600min → breached) and still fires CRITICAL on the confirmed production gap (76067 > 43200×1.5=64800) so the genuine dead-spot signal is not silenced. Corrected all 3 misleading "event-driven ... tracks the monthly job" comments across `freshnessSlaChecker.ts` (×2) and `freshnessSlaMonitorJob.ts` (×1) to state the real writer path so a future reader doesn't re-litigate the same wrong hypothesis.

**AC-2 (confirm/refute at source, do not guess):** queried the LIVE named-volume `market.db` `cron_job_runs` table directly (`docker exec` + `bun:sqlite`). A naive `SELECT * ... LIKE ... ORDER BY` hit the table's known page corruption (`database disk image is malformed`, tracked separately as `FIX-SQLITE-DOCKER-VIRT-CORRUPTION-RECUR-20260730`, same DB, unrelated defect) — worked around it with an exact `job_name=` match against the live `idx_cron_job_runs_job_started` index, which bypasses the corrupted scan path. Result: `monthlySignalQualityAuditJob` has exactly 2 rows ever — `2026-05-01T00:00:00` and `2026-06-01T00:00:00`, both `status=success` — **NO row for 2026-07-01**. CONFIRMED: the cron genuinely missed its July fire. This is real but does NOT explain the SLA-breach defect (the cron never wrote `signal_quality_audit` in the first place).

**AC-3 (backfill or file follow-up):** filed `FIX-MONTHLYSIGNALQUALITYAUDITJOB-MISSED-JULY-RECOVER-GUARD` (BACKLOG, P3, owner dev-mcp-server) via `orch-apply.sh` rather than live-backfilling — a live Telegram-sending production side-effect is outside this zone's TDD/code-change scope for a defect that (per the trace above) doesn't touch this row's actual SLA breach; row documents the confirmed missed run, the T4-dedup-guard prerequisite before `recoverMissedExecutions` can safely flip to `true`, and the closing-2026-08-01 window if a manual backfill of June's audit is instead preferred.

**Evidence:** new dedicated `FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H.test.ts` (8 tests) — AC-1 bidirectional (20d ok / 40d breach, both via `checkSignalSla` and the full `checkDataFreshnessSla` pipeline), config pin, fixed-not-calendar-derived threshold check, boundary tests at exactly 30d/30d+1min, and an explicit anti-false-green test asserting the real production 76067min figure still breaches CRITICAL post-fix. Updated `1920i-freshness-sla-extension.test.ts` TC-6f to the new value. tsc clean. Targeted SLA suite (12 files, 251 tests) 251/251 pass. Full `bun test` run TWICE post-fix: 14897 pass/40 skip/**52** fail (546s) and 14894 pass/40 skip/**55** fail (562s) — both within the standing pre-existing-flake band (54-59, per recent sibling-fix baselines this sprint); grep-confirmed zero of either fail list references `freshnessSlaChecker`/`freshnessSlaMonitorJob`/`signal_quality_audit`/this task ID — same recurring theme (5000ms MCP-client timeouts, vnstock/vps_push_log store schema issues) both runs. `1408-tool-diacritics.test.ts` (the sibling-row unblock this same tick) independently re-confirmed GREEN (8/8) — CI's `bun test` job is also green on the latest push (only `frontend-eslint` lockfile-drift and `size-lint` on unrelated files are red).

Zone health: tsc clean, dedicated bidirectional regression suite green, root-cause hypothesis actively corrected (not rubber-stamped) with the correction documented in code/tests/journal, both confirmed-at-source sub-defects (SLA threshold + missed cron) resolved via distinct closing actions (fix vs filed follow-up), anti-silencing test asserts the real production gap still fires post-fix, full-suite fail-set keyword-scanned clean across 2 consecutive runs | HEALTHY.
