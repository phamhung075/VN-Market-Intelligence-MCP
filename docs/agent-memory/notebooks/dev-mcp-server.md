# dev-mcp-server -- Notebook

## 2026-07-31 — FIX-SIGNAL-OUTCOMES-LIVENESS-GUARD-COUNTS-STRUCTURALLY-UNRESOLVABLE-ROWS (BOUNDED-1 auto-pickup, P1) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause was RAW-verified by dev-team/po before dispatch (not re-derived): `checkStalledResolutionLiveness()`'s WHERE clause had no `stock_code` exclusion, so `signal_outcomes` id 49 (`MACRO`, 2026-06-06) and id 74 (`MULTI`, 2026-06-25) — non-ticker pseudo-codes with no derivable price series, so `checked_at` can never advance — count as "stuck" forever, firing a false daily BUG alert while the resolver is actually healthy (103/105 resolved).

**Fix:** added `AND stock_code GLOB '[A-Z][A-Z][A-Z]'` to the stuck-count query. Checked for an existing "is this a real ticker" utility first (AC-1's own instruction): `isValidTicker` (commandHandlers.ts) is a bare `/^[A-Za-z]{2,10}$/` — MACRO/MULTI both pass it, no discrimination. `KNOWN_VN_TICKERS` (vnRelevanceFilter.ts) is explicitly doc-commented "kept minimal," ~45 tickers only — using it as an allowlist would incorrectly blind the guard on any real ticker outside that short list, failing AC-2's own positive-control requirement. Verified live (docker exec readonly bun:sqlite against prod `market.db`): every distinct `stock_code` across `watchlist`/`agent_signals`/`signal_outcomes`/`vnstock_financials` is exactly 3 uppercase letters — MACRO/MULTI (5 chars) are the only anomalies — so the GLOB shape rule is empirically grounded, not invented, and generalizes to any future non-3-letter pseudo-code without a re-edit.

**Tests:** extended `FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED.test.ts` (natural home per dispatch note) — 2 new cases: (1) MACRO+MULTI-only stuck rows → `stuckCount=0`, zero alerts (matches the 2 live prod rows exactly); (2) real ticker (VNM) stuck >72h mixed WITH MACRO/MULTI noise rows → `stuckCount=1`, one alert (positive control — guard not blinded by pseudo-code presence). Pre-existing "sends exactly one BUG alert..." test (real VCB ticker stuck) also still green, doubling as a second positive control.

**Secondary note (module-level `_lastStalledAlertDate` re-arms on container restart):** left untouched — its own doc comment already documents this as an accepted tradeoff mirroring `freshnessSlaMonitorJob.ts`'s `_lastSummaryDate` sibling pattern ("worst case one extra alert same day, never silence"); persisting across restarts needs a settings/kv table + migration, disproportionate to a P1/S task per the dispatch note's own "do not let it block the main AC" guidance.

**Evidence:** `bun tsc --noEmit` clean. Target file: 11/11 pass (was 9/9 pre-change + 2 new). Related-module sweep (`FIX-CRON-WATCHDOG-COVERAGE-2026-07-22.test.ts` + `cronStatusCompute.test.ts`, both reference this job): 34/34 pass. `gen-project-stats.ts --dry-run`: `toolCount=183 cronJobCount=88`, unchanged (no tool/scheduler surface touched). Full `bun test`, baseline (clean main, before touching anything) 14941 pass/56 fail/1 error/40 skip (571s) vs. post-fix 14947 pass/52 fail/40 skip (558s) — diffed the two named-fail lists directly: `vnstock-3statement.test.ts`'s 8 failures (the CI-red the task flagged) present byte-identical in BOTH runs; the 4 tests that differ between runs (Task 1227/1288 pollNews-Reuters-mock timing, FIX-1267 PDF-timeout circuit-breaker, push-news-9-sources) are unrelated to `signal_outcomes`/this file and match this sprint's own documented pre-existing-flake band (see 2026-07-30 entries above) — zero named fails reference `signalOutcome`/`checkStalledResolutionLiveness`/MACRO/MULTI in either run.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full-suite delta isolated to the sprint's known flake band + the task's own pre-flagged vnstock-3statement CI-red (byte-identical both sides) — not this diff. Housekeeping note (non-blocking, deferred per standing "breach on live sprint file → defer" rule): `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md` was already over the 36000-byte decision-journal cap (81437B) before this cycle's S34 entry — pre-existing, not caused here; flagging for a future housekeeping pass, not actioned solo mid-shared-file. | HEALTHY.

## 2026-07-31 — FU-MACRO-SNAPSHOT-TIER-WORSTOF (BOUNDED-1 auto-pickup via developer zone-route, P3/XS) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. `get_macro_snapshot` (`macroTools.ts` handler, ~L469-489) computed `sourceTier = signals.carry.source_tier ?? 2` only — silently ignored `yield` (or any other present component)'s own `source_tier`, under-reporting staleness whenever a non-carry component was worse-tier.

**Fix:** `sourceTier` is now `max()` over `source_tier` of every `data.signals.*` component actually PRESENT (generic `Object.values(...).filter(isSourceTier)` reduce — not a carry/yield-only pair, so any future signal component is picked up automatically). An ABSENT component never enters the max (not defaulted to tier 0/best-case); empty-fallback (no present tier at all, e.g. older Go build) unchanged at 2. Tool description string corrected too (was a hardcoded "Source tier: 2").

**Tests (`1881a-source-tier.test.ts`):** the shared `beforeAll` fixture (carry=2/yield=4) made its own `get_macro_snapshot` assertions false-green the bug pre-fix — updated AC-3 case 2→4 with an explanatory comment; AC-8 firstKey-only test strengthened with the same value assertion (would otherwise have stayed silently blind to the regression). New AC(b) case with its own local fetch-mock fixture that omits `signals.yield` entirely, proving worst-of degenerates to carry's own tier (2) when yield is absent — not a shared-fixture reuse. File: 21/21 pass (was 19).

**Evidence:** `bun tsc --noEmit` clean. Related smoke suites sharing the handler (089-tool-macro, 1423d, 1423f, 1570c, 1903a, 1918a — all use fixtures with carry==yield tier, so value-unaffected): 68/68 pass. Server boot on a free port: `/health` toolCount=183 (baseline match), no import errors. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88, both unchanged (no tool/scheduler surface touched). Full `bun test` (repo-wide, informational per `dev-standards.md:1135` CANONICAL merge-gate reading — targeted suite is the actual gate, `apps/mcp-server` carries a standing tracked order-dependent full-suite red): 14944 pass/40 skip/56 fail/596.81s, inside this cluster's already-documented pre-existing flake band (52-59, see prior entries above); the targeted+related-suite 89/89 pass is the decisive evidence for this diff.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), no file outside `macroTools.ts` + its own test touched | HEALTHY.

## 2026-07-31 — FIX-CI-SIZELINT-MCPSERVER-SIX-UNCOVERED-OFFENDERS (BOUNDED-1 auto-pickup, P0) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. AC-1 file-level gate: `size-lint-justification.sh --check` no longer lists any of the 6 owned files (only the 2 sibling-owned offenders remain — job stays RED pending those, per design). AC-2 landmine avoided: zero `--update` runs, zero `docs/data/size-lint-baseline.json` edits.

**5 of 6 split into ≤120L sibling files** (barrel-reexported where in-zone consumers already imported by name, zero consumer-file churn): `claimCandidateScanner.ts` 190L(NEW)→116L + `claimToolMapTypes.ts`(51L) + `tickerExtraction.ts`(46L); `vpsPushLogStore.ts` 212→160L (demand-queue split to `vpsDemandQueue.ts`, 72L); `signalValidator.ts` 259→177L (audit helpers to `signalQualityAudit.ts`, 112L); `vpsProxyTools.ts` 269→87L (staleness+formatting split to `vpsProxyStaleness.ts`(111L)+`vpsProxyHealthFormat.ts`(115L)); `polymarket.ts` 500→462L (SQLite persistence to `polymarketStore.ts`, 56L, plus removed a now-unused `getDb` import).

**orchStateSchema.ts (870→839L) got trim+justification, NOT a split** (AC-3's own explicit alternative): its §9-12 guard functions are imported BY NAME from this exact path by `scripts/orch-validate.mjs`, AND raw-copied whole (not dependency-aware) by `scripts/agents-flow/drain-signals.test.js`'s `makeOrchRefHarness()` to exercise the real validator end-to-end — any physical split leaves that copied file's own import dangling in the isolated sandbox. Fixing the harness's copy-list is a 1-line change but lives in `scripts/`, outside this agent's zone. Did a genuine ~46L comment-debt trim (duplicated `.passthrough()→.strict()` promotion-trigger blocks condensed, §9/§11/§12 verbose narrative headers tightened — zero logic/type/export changes) then added a substantive `size-justification: 839L` header (first 10 lines) explaining the cohesion + the harness constraint + a pointer for a future scripts/-owning agent to land a coordinated split.

**Evidence:** `bun tsc --noEmit` clean. Targeted suites for every touched file + every consumer (CCATO/orchStateSchema/sprint-goal/blocks-decorative/polymarket/prediction-job/vps-health×4/signal-quality-audit/push-log fixes, ~24 files): 650+ pass/0 fail. Fresh server boot (`DB_PATH=:memory:`, free port): `/health` toolCount=183, zero import errors. `gen-project-stats.ts --dry-run`: toolCount=183 cronJobCount=88 (baseline match, no tool/scheduler surface touched). Dashboard circular-dep probes (`/api/bctc-inspect`, `/dashboards/news-fetch/`) both clean. Full `bun test`: 14945 pass/40 skip/55 fail (556.87s) — inside this sprint's documented pre-existing flake band (52-59, see prior 2026-07-30/31 entries); fail-list not individually re-grepped (captured via `tail -80`), but the count sitting in-band plus the 650+/0 targeted result is decisive for this diff. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md S35.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 6/6 files cleared from size-lint (job stays RED pending 2 sibling rows, by design — AC-1 is file-level) | HEALTHY.
