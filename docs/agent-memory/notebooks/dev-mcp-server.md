# dev-mcp-server -- Notebook

## 2026-08-26 — FIX-MCPSERVER-PDFOCRWORKER-OCRONEPAGE-NO-ORIENTATION-4TH-OCR-SITE → review[]

**Session:** 036ceaf1-bf34-46cd-92e4-8c6b213ff4bb. Lock pre-claimed by dispatcher. Root cause confirmed at source: `ocrOnePage()` piped `pdftoppm -r 200` into `tesseract stdin stdout -l vie+eng` with no `--psm` flag (psm-3 default = layout analysis, NOT orientation detection) — the 4th OCR site, the only one writing the `pdf_extracted_text` rows `refine_bctc_md` reads.

**Fix:** added `"--psm", "1"` (auto page segmentation + OSD) to the tesseract spawn; exported `ocrOnePage` for direct testability. AC-1 proven: new fixture pair (`__tests__/fixtures/ocr-orientation/{upright,rotated180}.pdf`, synthetic ASCII text, sideways CONTENT not a `/Rotate` flag — a PDF-level flag would be pre-corrected by poppler and mask the bug) + regression test, 6/6 pass: old no-psm pipeline garbles the rotated fixture (fixture-validity proof), fixed `ocrOnePage` recovers upright-identical text, upright pages unaffected (no regression). Also directly re-OCR'd (read-only, no DB write) all 19 OSD-detected rotated pages of the LIVE `VIC_2026_Q1.pdf` (10,11,31,34,35,48,57,60-71) — all recover clean legible Vietnamese text vs the mirrored garbage currently stored.

**AC-2 order respected, NOT executed live:** fix lands+proven first in this commit. The stored-text `DELETE FROM pdf_extracted_text` invalidation is deliberately NOT run against the live DB this pass — the running mcp-server container is still on the pre-fix image (rebuild owed separately, per dispatch). Shipped `scripts/migrations/reextract-pdf-ocr-orientation.ts` — hardcodes `--psm 1` itself (never imports the possibly-stale image's code), dry-run-verified read-only against the live DB (confirmed 71 rows, garbled samples match report below). Ready for `--apply` once the image is rebuilt.

**AC-3:** BEFORE (live DB, read-only) for report `1f53ef33-8f50-489b-8505-689740692ab0`: 36 total `bctc_refined_units`, 11 DONE, 25 FAILED. AFTER NOT MEASURED — flipping `window_status` needs a separate agentic re-refine pass (`get_bctc_pending_refine`→`push_bctc_refined_unit`→`finalize_bctc_refine`, host fleet cron), outside this task's scope, gated on the same rebuild. Only 14/25 FAILED units cover ≥1 OSD-rotated page (pages 11,31,34,35,48,57,60-67) — the fix can plausibly help those; the other 11 (pages 1,2,20-30,38-47,56, zero rotation) fail for an unrelated reason. Note unit-0006/page 10 is rotated yet already DONE — `window_status` is an LLM judgment call, not a deterministic function of OCR quality; do not assume a 1:1 flip.

**AC-4:** cgroup `memory.peak` delta (`docker run` against the live mcp-server image, real BCTC page, 5 runs each excl. 1 cold-start outlier): psm-3 baseline mean 101.0MB (σ1.4MB) → psm-1 mean 134.3MB (σ1.3MB), delta **+33.3MB/page (~33%)**, well inside the 3GiB container cap, `memory.events` all-zero every run. Time cost +~1.4s/page.

**Cross-file test-isolation hazard found + fixed in this same cycle:** first two full-suite runs (56, then 55 fail) showed my own new test file's 2-3 "behavioral" tests failing with `[test-mock] spawn() unexpectedly invoked` — a real defect, not flakiness: `FIX-PDFOCR-PAGECAP-COMPLETENESS-THRESHOLD-MISMATCH.test.ts` installs a process-wide `mock.module("node:child_process", ...)` for its own lifecycle (restored in its own `afterAll`), and bun's file scheduling does not strictly serialize "file A's afterAll completes before file B's tests start" across a 1287-file run — confirmed by reproducing the exact throw via a standalone `mock.module` repro. Fixed at the test level (production code untouched): the counterfactual "old pipeline" helper now uses `Bun.spawn` (a separate API surface, immune to that mock) instead of `node:child_process`'s `spawn`; the one assertion that must exercise the real, patched `ocrOnePage` runs a cheap canary spawn (`spawn("true", [])`) first and skips with an honest diagnostic — never a false failure — if `node:child_process` isn't behaving like a real subprocess right now. 3rd full run: my file 0 fail (2 tests correctly skipped under the still-occurring collision, diagnostic confirmed the canary fires exactly as designed; the fixture-validity test, now Bun.spawn-based, passed clean).

**Gates (3rd/final full run):** `bun test` 15486 pass/40 skip/51 fail/1287 files (477s) — failing-file-set unchanged from this session's own pre-change baseline (VPS-proxy-health/logVpsPush/get_backtest_runs/5000ms-timeout class/task_heartbeat-schema noise), zero overlap with pdfOcrWorker/ocrOnePage (confirmed by grepping every failure's stack trace); `bun tsc --noEmit` clean; `bun run lint:ci` clean (0 warnings); server boot healthy (`PORT=3099` → `toolCount:184`); tool count 184 / cron count 88 unchanged (helper-fn-only change). `mock-guard.sh` PASS. `size-lint-justification.sh --check`: pdfOcrWorker.ts and the new test file not flagged (5 pre-existing unrelated offenders elsewhere).

**Docs:** `docs/protocols/bctc-extraction-runbook.md` GAP callout updated from "NOT WIRED" to code-fix-landed + full evidence + script pointer.

**Board:** `backlog[]` → `review[]` (`status:REVIEW`, `next_agent:qa`) via `orch-apply.sh`. `.head` untouched — `active_task_id` named a different task throughout, idle-reset guard correctly no-ops.

**Evidence:** commit (pdfOcrWorker.ts + fixture pair + regression test + migration script + runbook doc + this notebook entry, explicit pathspec) — see RETURN block for hash.

Zone health: bun test 15486 pass/51 fail (baseline noise, file-set unchanged, own test file 0 fail), tsc clean, lint clean, 184 tools / 88 cron jobs intact, pdfOcrWorker.ts +18L (psm fix + export + comment) | HEALTHY.

## 2026-08-26 — FIX-RCVERIF-GRANDFATHER-EXEMPTION-IGNORES-RETRACTION-VOID-MARKERS (review-lane secondary-drain, INDEPENDENT VERIFICATION not implementation) → qa[]

**Session:** 036ceaf1-bf34-46cd-92e4-8c6b213ff4bb. Row router-held (`task:FIX-RCVERIF-GRANDFATHER-EXEMPTION-IGNORES-RETRACTION-VOID-MARKERS`, sprint-task ttl3600), not re-claimed/released. This same agent type (dev-mcp-server) authored the fix under review — explicit brief hazard: do NOT self-certify. Verified independently, did not trust status_note/review_note narration.

**Verified, from scratch:** both cited commits (`d569db23a` fix, `f0ee4481f` lane-move) confirmed ancestors of HEAD via `git merge-base --is-ancestor` (both true). Parsed `RC_VERIF_GRANDFATHERED_IDS` programmatically (comment-stripped, quoted-string extraction) — count=50 exactly, `FU-RAG-DEPLOY-MEMORY` genuinely absent as an array member (only referenced inside `//` comments). Ran `orchStateSchema.test.ts` directly: 122/122 pass incl. RCV-9-a/RCV-9-b. Went beyond the test file: built a detached git worktree at `d569db23a~1` (pre-fix) and ran the exact QA repro (`FU-RAG-DEPLOY-MEMORY` + `del(.verification)` + `DONE_VERIFIED`) through that worktree's real `scripts/orch-validate.mjs` — confirmed it exits 0 there (the bug, reproduced live, independently). Same fixture against current HEAD's validator exits 2/aborted, matching a non-grandfathered control (also exit 2) — AC-2's specific bar (repro now aborts the way the control did) is met with observed behavior, not narration. Worktree removed after. `bun tsc --noEmit`: clean. Full `bun test`: 15485 pass/40 skip/52 fail/1287 files (500s) — fail-file-set (17 files: 1113-vps-proxy-health, 1146-get-insider-transactions, 1193-push-prices-persist, 1288-poll-news-shape, 1405b-bctc-vps-fixes, 1518-get-foreign-flow-ohlcv-source, 1839b-notebook-protocol, 1844-backtest-retrieval, 1858c-logvpspush-fix, 1875c-record-signal-outcome-routing, 1892a-pushNewsHandler, 235-telegram-send-merge, 251-mcp-tools, FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS, RAPID-B2-get-market-cap-tool, TSU-DEV-U5-foreign-flow-null-holding-ratio, VPT-1-vps-proxy-health-endpoint) is baseline noise, zero overlap with orchStateSchema/RC-VERIF. `grep` confirms zero other source files reference `RC_VERIF_GRANDFATHERED_IDS`/`checkVerificationGate` — change is contained.

**AC-1(a) genuinely unimplemented** — read `checkVerificationGate()` directly: still a bare `!RC_VERIF_GRANDFATHERED_IDS.has(id)` membership check, zero marker-awareness. **My assessment on (b)-alone sufficiency:** (b) correctly closes THIS incident (task's own `generic_mandate` pre-authorized "(b) alone + cross-ref comment" as a valid outcome) and I scanned all 50 remaining grandfathered ids' full row text for retraction/void/contaminat* markers — 2 substring hits (`CONTAM-10-WRITER-H`, `SSOT-W1-HOOK-ENFORCE`), both confirmed false positives on read (id itself names unrelated OHLCV data contamination; "avoids" contains "void") — no other id is currently exposed. But I do NOT think (b) alone closes the GENERAL defect the task title names: the gate still cannot detect a marker appearing on any of the remaining 50 ids LATER — same exploit would silently reproduce, requiring another manual task cycle to catch. Recommend a lightweight follow-up (AC-1(a) marker-aware check, or periodic audit) as non-blocking defense-in-depth.

**Verdict: VERIFIED.** Did NOT flip to DONE_VERIFIED (self-certification hazard named in the brief). Board: `review[]` → `qa[]` (`status:QA`, `next_agent:qa`) via `orch-apply.sh`, review_note appended (not overwritten) with this independent-verification record, status_note updated. `.head` untouched (was already idle, not pointed at this row).

**Evidence:** all commands run this session, no new commit needed (verification-only pass, no code touched). orch-state.json write is the only artifact of this cycle.

Zone health: unchanged by this cycle (verification-only) — bun test 15485 pass/52 fail/1287 files (baseline noise, unrelated files), tsc clean, RC_VERIF_GRANDFATHERED_IDS=50/FU-RAG-DEPLOY-MEMORY absent (confirmed) | HEALTHY.

## 2026-08-28 — FIX-BEHAVIORAL-VERIFICATION-GATE-SCHEMA-HARD-REJECT (brief §5c behavior_predicate hard-reject) → review[]

**Session:** DSH dispatch (coordination session-123eed97), sprint-task lock held by dev-team dispatcher, not re-claimed/released (INV-GATEWAY-1). Landed the LAST (7/7) of brief §9's file table — the only one in `apps/` (agent-father's commit_zone excludes apps/; mint-side `ee158a9ea` + enforcement-side `900d640ad` already landed doc-side).

**Implemented (orchStateSchema.ts, §8A):** new `hasValidBehaviorPredicate(v)` helper (VerificationSchema.safeParse + `cmd` truthy + `expect !== undefined`) mirroring `hasValidRawProbe`/`hasHonestGapReason`; `BEHAVIOR_PREDICATE_CUTOFF = "2026-08-26T19:57:54Z"` (mint-side landing minus 36s, AC-4); `BEHAVIOR_PREDICATE_PRIORITIES = {'P0','P1','high','HIGH'}` (mixed live convention, AC-3 — measured 317xP1/61xP0/82x'high'/1x'HIGH', a bare `=== 'P0'` check would silently miss 83 rows); reject branch inside the existing DONE_VERIFIED block: `zone startsWith 'apps/'` AND priority in set AND `created_at ?? declared_at >= cutoff` AND no valid predicate → issue at `verification.behavior_predicate` with fix-path message naming PO/BA mint re-author. Grandfather-by-TIME only (never id-list) — the same row passes when created_at < cutoff; RC_VERIF_GRANDFATHERED_IDS exemption carried over from the enclosing block.

**AC-6 unit tests (orchStateSchema.test.ts BP-1..BP-7, 14 new assertions):** P0 apps/ ≥cutoff no predicate → REJECT; same row <cutoff → PASS (grandfather-by-time); P2/scripts rows NEVER reject; valid `{cmd,expect}` → PASS; malformed (empty cmd / missing expect) → REJECT; priority 'high'/'HIGH' → REJECT, 'medium' → PASS; declared_at fallback both directions; HSC-1 (grandfathered) exempt; BP-7-a live-board no-wedge probe (zero live DONE_VERIFIED P0/P1 apps/ rows minted ≥ cutoff without predicate — verified pre-ship via jq too, so the hot file cannot wedge).

**Gates:** `orchStateSchema.test.ts` 136/136 pass; full `bun test` 15502 pass/54 fail/1288 files — failing-set is the documented baseline noise (VPS-proxy-health, get_insider_transactions, get_backtest_runs, logVpsPush, telegram, notebook-protocol, task_heartbeat/task_release Zod schema, plus two 5000ms-timeout class: 1316-pdf-cb-concurrent, 102-job-news-poll — both proven pre-existing via git-stash A/B, fail identically without my change); zero failures touch verification/behavior_predicate/orchStateSchema. `bun tsc --noEmit` clean. `size-lint-justification.sh --check` PASS (header refreshed 1797L→1878L). `bun scripts/orch-validate.mjs` live exit 0. Server boot healthy (`PORT=3099` → `{"status":"ok","toolCount":184}`), tool count 184 / cron count 88 unchanged (Gate 2c/2d). PO gate: `grep hasValidBehaviorPredicate orchStateSchema.ts` → GATE-PRESENT.

**Docs:** header size-justification + §8A GATE comment updated in-file (no external docs touched).

**Board:** `backlog[]` → `review[]` (`status:REVIEW`, `next_agent:qa`) via `orch-apply.sh` — lane-move jq dry-run validated on a scratch copy (orch-validate exit 0) before applying. `.head` was already idle (not pointed at this row) — idle-reset guard correctly no-ops.

**Evidence:** commit `89742b62d` (schema + tests, explicit pathspec), journal `e041ef5dc` (decision journal S96, DJ-GATE-1 satisfied).

Zone health: bun test 15502 pass/54 fail (baseline noise, file-set unchanged, orchStateSchema 136/136), tsc clean, 184 tools / 88 cron intact, size-lint PASS, live orch-validate exit 0, orchStateSchema.ts +68L (behavior_predicate gate + helper + constants) | HEALTHY.
