# dev-mcp-server -- Notebook

## 2026-07-23 — FIX-MCP-DOCKERFILE-ENTRYPOINT-KNOWNHOSTS-REGRESSION (BOUNDED-1 direct-execute FIX, router dispatch) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Root cause (PO-verified board row): `6165aa3b4` ("rebase Dockerfiles from Debian to Ubuntu") silently dropped ALL of task-1779c's already-QA-approved SSH infra from `apps/mcp-server/Dockerfile` — both `openssh-client` and `ENTRYPOINT ["/entrypoint.sh"]` — no mention in the commit message. `entrypoint.sh` stayed tracked+clean at HEAD but COPY'd/referenced by NOTHING (`git log -S entrypoint.sh -- Dockerfile` empty since the rebase) — dead code ~2.5 months, no alarm. `openssh-client` was already re-added by yesterday's `FIX-VPS-SSH-TRIGGER-FAIL-LOUD`; only the COPY/chmod/ENTRYPOINT trio was still missing.

Fix: restored the 3-line entrypoint block to Dockerfile (after HEALTHCHECK, matching original `fd3b10ac4` placement). Did NOT restore `entrypoint.sh` verbatim — mandatory constraint forbade it: original was `set -e` + hard `exit 1` on empty `ssh-keyscan` output, which would turn any VPS outage into total mcp-server boot failure (88 crons + full tool surface). Rewrote non-fatal: no `set -e`, WARN (not ERROR) + continue on empty keyscan, `exec "$@"` always reached. Parameterized `KNOWN_HOSTS_DIR`/`KNOWN_HOSTS_FILE` (default unchanged: `/root/.ssh/known_hosts`) so the regression test drives the real script end-to-end without Docker/root.

`sshExec.ts` `StrictHostKeyChecking` NOT reverted to `yes` — PO ruling gates that on seeding being "live and proven"; VPS 125.212.251.27 confirmed unreachable from this sandbox (`nc -z` timeout), so kept the `accept-new` stopgap per explicit instruction, with a new code comment explaining why + what unblocks the revert.

New test `FIX-MCP-DOCKERFILE-ENTRYPOINT-KNOWNHOSTS-REGRESSION.test.ts` (11 cases): 4 static Dockerfile-wiring checks (COPY/chmod/ENTRYPOINT/openssh-client), 3 entrypoint.sh source guards (no `set -e`, no nonzero `exit`, ends `exec "$@"`), 4 live behavioral checks running the real script via `sh` against a tmp dir + fake `ssh-keyscan` on PATH (VPS_HOST empty, keyscan-empty, keyscan-real, wrapped exit-code passthrough) — proves the non-fatal contract without Docker or real network. `bun tsc --noEmit` exit 0. Tool count 184 / cronJobCount 88 unaffected (gen-project-stats probe).

Could not live-verify the VPS-reachable path (container boot + real `StrictHostKeyChecking=yes` SSH success) — sandbox has no route to the VPS; ships code + regression test per task's own fallback instruction, REVIEW status_note documents the gap for QA/ops.

Zone health: tsc clean, 11/11 new tests + all pre-existing SSH/docker tests green, tool count 184 unaffected | HEALTHY.

## 2026-07-23 — FIX-CRON-SSCCHECKERJOB-DEAD-87D (BOUNDED-1 direct-execute FIX, dev-team auto-pickup) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Item 1 root cause: `docker exec` + `git log` pinned it — task 1281-fix (2026-04-25) added an `ENABLE_LOCAL_BCTC_FETCH` guard that returned BEFORE `recordJobRun()` in `sscCheckerJob.ts`; flag defaults `false` and IS unset live (confirmed) → daily cron fired but wrote ZERO `cron_job_runs` rows for ~88 days, indistinguishable from crashed. Guard itself is CORRECT (avoids the France server hitting geo-blocked SSC directly) and `checkSscReports()`'s discovery+alert role is fully superseded by the queue-based VPS pipeline (`GET /api/bctc-fetch-queue` + `bctcQueueEnricherJob` + `POST /api/push-bctc-pdf`) + `signalDetector.ts`'s generic `report_new` signal — did NOT flip the flag (would reintroduce the timeout regression). Fix: moved the guard inside the `recordJobRun` callback so the cron always records honest `success`/`rowsWritten:0` telemetry when it correctly no-ops; call-count spy proves `checkSscReports()` still never runs.

Item 2: direct live-DB query (not just the audit summary) on `dataAuditJob:weekly` — 8 rows, all Saturday 18:00 UTC (=Sunday 01:00 ICT, correct per its `{timezone:'Asia/Ho_Chi_Minh'}` registration), last success 2026-06-27 — 26+ days stale, a genuinely SECOND dead job (same restart-timing mechanism as the 07-22 "4 dead Sunday jobs" fix, but outside that audit's Sunday-UTC-morning scan scope). Added a 5th `startScheduler.ts` startup-catchup block (`requiredUtcDay=6`, `runWeeklyAuditWithDb(db)` direct call — bespoke job, not in `jobTable`, can't share the 4-job loop).

Item 3 class fix: three hand-maintained registries (`CANONICAL_WATCHDOG_JOB_NAMES`, `WATCHDOG_MANIFEST` keys, `cronStatusCompute.ts`'s `STATIC_JOB_NAME_MAP`) had already drifted once (07-22 ripple). New `_staticJobNameMapKeysForTests()` accessor + a new registry-invariant test reads the REAL map keys (no 4th hand-copied fixture) and cross-checks against a source-derived call-site scan (WD-11's mechanism, duplicated self-contained per this codebase's no-shared-test-helpers convention) + `CANONICAL_WATCHDOG_JOB_NAMES`. Proved fail-loud via a live injected-typo experiment (corrupted then reverted `STATIC_JOB_NAME_MAP.sscCheck`).

3 new test files (12 cases): `FIX-CRON-SSCCHECKERJOB-DEAD-87D` (3 — telemetry honesty), `-dataauditweekly-catchup` (5 — Saturday window + source-contract), `-classfix-registry-invariant` (4 — registry drift guard). `bun tsc --noEmit` clean. Live boot probe (PORT=3998, 33s wait): `[startup-catchup] dataAuditJob:weekly: not the required UTC day (got 4, need 6) — skipping` — wiring runs cleanly, clean SIGTERM. Full suite: **14680 pass / 40 skip / 41 fail / 46627 expect(), 1214 files [472-482s]** — fail count identical to 07-22's documented baseline (41), zero name-overlap with touched files (`_deprecated`, `logVpsPush`/`vps_push_log`, 5000ms-timeout class — same 3 categories). Tool count 184 / cronJobCount 88 both unaffected (gen-project-stats probe). One ripple caught+fixed: a project-wide `mock-module-afterall-guard.test.ts` lint failed on my new mock.module() until an `afterAll(() => mock.restore())` was added.

Docs updated: `news-analysis.md` (sscCheckerJob table footnote), `docs/standards/cron-jobs.md` (5th startup-catchup job + class-fix section). Residual for QA/ops: sscCheckerJob's daily fire is now honestly visible but STILL a permanent no-op on the local server by design — nothing to rebuild/deploy-verify beyond the standard image rebuild; `alert_report_new` watchlist column is now vestigial (only consumer was `checkSscReports()`) — flagged, not removed (out of this task's scope).

Zone health: tsc clean, 21/21 new+adjacent touched-file tests green, tool/cron counts unaffected, 41 pre-existing fails zero-overlap-verified | HEALTHY.

## 2026-07-23 — MD-FUNC-01-FIX (BOUNDED-1 idle-capacity auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: verify `get_market_snapshot` returns VN-Index `price`/`change_pct`/`direction`, fix root cause if any field absent/mis-mapped/hardcoded. Finding: NO fix needed — the `vn_index` struct was already shipped in `marketTools.ts` under commit 815ccaedd/ddc36452e (2026-06-16), which predates the currently-running mcp-server image (built 2026-07-22), so the fix is live. `vn_index.price`/`.change_pct` are read straight off `fetchVnIndex("VNINDEX")` (real VNDirect `vnmarket_prices` fetch, `infrastructure/fetchers/hose.ts`); `direction` is dynamically derived from the live `changePct` sign (`>0?"up":<0?"down":"flat"`) — not hardcoded.

Live verification (per task instruction: live payload authoritative, not schema/test comment) via `scripts/agents-flow/mcp-call.sh` → `get_market_snapshot {}` returned `vn_index:{price:1699.38, change_pct:1.85, direction:"up"}` — all 3 fields non-null, plausible against the same payload's breadth block (189 adv / 122 dec, net-positive matches +1.85%). Targeted test `1982-quality-burndown-CHIJ.test.ts` (MD-FUNC-01 section) 18/18 pass; market-tool-adjacent files (084-tool-market, 1881a-source-tier, TASK-unblock-cowork-mcp-tools, 123-integration-mcp, tool-registry-parity) 83/84 pass (1 pre-existing skip). `bun tsc --noEmit` clean. Full suite: 14679 pass / 40 skip / 42 fail / 1 error, 46627 expect() calls, 1214 files [473s] — fail count consistent with the documented 41-42 baseline (`_deprecated`/`logVpsPush`/timeout classes, unrelated); zero source files touched this task so this is exactly the pre-existing baseline, not a regression. Tool count 184 / cronJobCount 88 unaffected.

Zone health: tsc clean, MD-FUNC-01 targeted suite 18/18 + adjacent 83/84 green, verification-only (no code change), tool/cron counts unaffected | HEALTHY.
