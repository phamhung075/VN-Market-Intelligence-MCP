# dev-mcp-server -- Notebook

## 2026-07-10 — D2.5-BACKLOG-HYGIENE-SCHEMA-BLOCKED-LANE → (left in ready[], router flips)

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (router direct-dispatch, gateway MCP unavailable this session — no `mcp__gateway__call_tool` used. PO-ratified 2026-07-10T18:41Z, `po_signoff` field on the board row + decision journal `docs/agent-memory/decisions/sprint-BACKLOG-HYGIENE-VERIFY-PRUNE-SWEEP-po.md`, commit `aa1901c72` — verified both exist and match before touching code, per standing read-handoff-first discipline; design was NOT re-litigated)

3-line functional diff in `apps/mcp-server/src/infrastructure/orchStateSchema.ts`'s `LANE_ALLOWED_STATUSES` map (§9 LANE-STATUS COHERENCE, ADD-2): `backlog`/`review`/`in_progress` each gained `"BLOCKED"` alongside their existing allowed status. `qa`/`done`/`done_verified`/`ready` untouched, exactly as PO scoped. Updated the doc-comment table directly above (lines ~392-399) to match, plus a short "Extended: D2.5..." note citing the ratification timestamp + `po_signoff` commit so a future reader doesn't have to archaeology the decision journal.

Verification chain: (1) `orchStateSchema.test.ts` 103/103 pass unchanged (its live-data C3-a test is intentionally non-asserting on count — just logs — and now logs 9 instead of 16, confirming the fixture-level `LANE_ALLOWED_STATUSES` change is live); (2) `bun scripts/orch-validate.mjs` against the real `docs/data/orch/orch-state.json` dropped from 16 → 9 coherence warnings — the exact 7 BLOCKED-in-{backlog×4, review×3} rows PO's arithmetic named, confirmed via `jq` count before touching anything; the remaining 9 are the separate D1-residual lane-move/relabel warnings, left untouched as instructed; (3) `bun tsc --noEmit` clean; (4) full-suite via `scripts/ci-per-file-isolation.sh` (bare `bun test` hangs in this sandbox — same documented pre-existing issue as prior D3C/R-HIGH-2 cycles, confirmed live again: killed after `Illegal instruction` background-hang, switched to the isolation runner) — 14315 pass / 40 skip / 70 fail across 19 pre-existing-failing files (news-poll/SSC-circuit-breaker/OCR/PDF-extractor categories, incl. the already-known `1294b-bctc-fallback.test.ts`), zero import overlap with `orchStateSchema.ts`/`LANE_ALLOWED_STATUSES` confirmed by grep across all 19 — pre-existing, not a regression from this diff.

**Self-correction disclosed:** attempted a `git stash`/re-run-baseline A-B comparison for extra rigor on the 19 failed files; the stash+re-run got interrupted mid-flight (external file-change system reminder fired on the stash pop) before producing a clean second data point. Recovered immediately via `git stash pop`, diffed the restored file byte-for-byte against the pre-stash diff (identical), and re-ran the schema test + tsc + live validator fresh — all three reconfirmed clean. Did not repeat the stash experiment; the zero-import-overlap grep + the already-passing isolation run were treated as sufficient regression evidence instead.

Commit: this agent's own commit (small in-zone schema-only change, task explicitly authorized dev-side commit — unlike the D3C/R-HIGH-2/conservation-guard precedents above where router commits). Board: task `D2.5-BACKLOG-HYGIENE-SCHEMA-BLOCKED-LANE` deliberately left in `ready[]` per dispatch instructions — router RAW-verifies (diff + live validator re-run + test suite) and flips the board itself.

Zone health: tsc clean, schema test 103/103 pass, live validator 16→9 (7 BLOCKED warnings resolved, 9 D1-residual warnings correctly untouched), full-suite isolation run 14315 pass/70 fail (pre-existing, zero overlap with touched file), zero data mutation (schema-only), toolCount/cron-count unaffected (no tool/cron file touched) | HEALTHY.

## 2026-07-10 — FIX-BCTC-D3C-RECONCILE-JOB → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (D3c — closes the chain: D1/D2/D3A/D3B all landed DONE_VERIFIED; this is the FINAL leaf, architecturally urgent since D3B ships `pek_triggered` with no exit path)

New `bctcExtractReconcileJob.ts` (mirrors `bctcPdfPullJob.ts`'s shape: injectable deps, `makeProductionDeps()`, `runXJob(opts)`, `_isRunning` overlap guard). Each tick: selects `pek_triggered` rows past a 5-min grace window, re-derives `report_id` via `(action_code, sort_key)` (the queue row carries NO `report_id` column — this lookup is mandatory, not defensive), checks `(bctc_layout_units WHERE quarantined=0 count>0) OR (bctc_table_rows count>0) OR (bctc_md_tables count>0)`. Success→`done`. Exhausted `MAX_RECONCILE_ATTEMPTS` (new const=8, ~4h budget)→`enrich_failed` + fail-loud `sendTelegramBug` (same mechanism the pre-D3B gate used, reused not reinvented). Under cap→stays `pek_triggered`, `reconcile_attempts++` (NEW dedicated column, idempotent migration in `schema-financial-reports.ts` — kept separate from the pre-existing `attempts` column, which already means something else: pre-download fetch-failure count).

**Re-fire-vs-passive decision (mine to resolve, QA-flagged at D3B):** adopted uniform always-re-fire-on-still-zero (bounded by the same attempt cap). `bctc_vps_queue` has no column recording which of D3B's 4 folded outcomes fired, so a 503/502 row (PEK never started) and a 202 row (genuinely in-flight) are indistinguishable at this layer — passive-only re-check would leave the whole 503/502 class stuck until it silently exhausts into `enrich_failed` with zero real retries. Re-firing an already-in-flight 202 is cheap+safe (`pushBctcLayoutHandler.ts`'s DELETE-before-INSERT makes repeat `/pek-extract` idempotent).

New cron `CRONS.bctcExtractReconcile = '5,35 * * * *'` (design doc's own suggested offset), registered in `schedulerJobTable.ts`'s `JOB_TABLE` array (58 entries now, was 57 — generic `scheduleCron`/`wrapRun` wiring, confirmed by reading the consumption loop).

New `bctc-extract-reconcile-job.test.ts` (14 tests, 56 expect() calls): empty queue, grace-window skip, 3 tables tested independently for the OR-success-check, quarantined-only-does-NOT-count, under-cap re-fire (reportId/pdfPath asserted), re-fire skipped-but-attempt-still-advances when no report_id resolvable, exhausted→enrich_failed+bug-fired-once (incl. an unresolved-report_id variant + a sendBugFn-throws-non-fatal variant), report_id re-derivation independent of insertion order, overlap guard. tsc clean. mock-guard PASS on all 5 touched/new files. Full BCTC-scoped per-file sweep (105 files, incl. this new file) 1153 pass/8 fail/5 skip — all 8 fails are the same pre-existing `1294b-bctc-fallback.test.ts` standalone timeout already root-caused at S30/S31/S33/S35 (grepped for any `bctc_vps_queue`/`pek_triggered`/`reconcile_attempts` overlap — zero). toolCount=183 unchanged. Gate-2d's `cron.schedule` literal-grep probe is (still, pre-existing) stale post-refactor (only 1 file matches now regardless of job count, since a `scheduleCron()` wrapper centralizes registration) — reported the real `JOB_TABLE` entry delta instead. Live server-boot: module load + new migration + full scheduler registration clean, zero import errors; final `listen()` hit the same pre-existing `EADDRINUSE` (:3000 already held by a live Docker container) as every S31-S35 probe — that live instance's `/health` + `/api/bctc-inspect` still respond clean post-diff.

Docs: `docs/architecture/microservice/mcp-server/financial-reports.md` (+2 Scheduler-Jobs rows, +invariant #8 state-machine note — did NOT rewrite the file's pre-existing stale push-based invariant #5, out of scope), `docs/data/system-map.json` (SSOT crons 65→66) + `docs/data/cron-registry.json` (mirror, 64→65) both updated + re-validated as well-formed JSON. `docs/data/project-stats.json#cronJobCount` deliberately NOT regenerated — its generator shares the same stale literal-grep source of truth, would just write a differently-wrong number.

Commit: NOT committed by this agent — router RAW-verifies and commits per the D1/D2/D3A/D3B dispatch discipline. Board: `READY`→`REVIEW` via `orch-apply.sh` (new dev-side guard script `scripts/dev-fix-bctc-d3c-reconcile-job-ready-to-review.jq` — no pre-existing dev-side ready→review script found for D1-D3B, written following the QA-side scripts' guard-refuse convention), `next_agent=qa`.

Zone health: tsc clean, new test 14/14 pass (56 expect()), full BCTC-scoped sweep 1153 pass/8 fail (1 pre-existing unrelated file, zero overlap), mock-guard PASS, toolCount=183 unchanged, this is D3C — the pek_triggered→done/enrich_failed path now exists end-to-end | HEALTHY.

## 2026-07-10 — FIX-BCTC-R-HIGH-2-MARKET-HOURS-GUARD → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (small follow-on to D3B/D3C, both landed — closes the design doc's own flagged R-HIGH-2 gap: the automatic post-pull trigger fired unconditionally even during VN market hours, when PEK's own Layer-2 guard always 503s it)

Added a client-side `isVnMarketHours(now)` pre-check in `bctcPdfPullJob.ts` immediately before the `deps.triggerPekExtraction` call (Step 6, `if(shellRow)` branch) — reused the EXISTING helper from `domain/services/freshnessSlaChecker.ts` verbatim, did not reinvent (confirmed: it's the same helper `freshnessSlaMonitorJob.ts`/`coverageMapFreshnessChecker.ts`/`vpsProxyHealthHandler.ts` already reuse; the dispatch prompt's claim that `vpsProxyWatchdogJob.ts`/`priceUpdateWatchdogJob.ts`/`vpsHealthPoller.ts` also import it was checked and found FALSE — those 3 each define their own separate `isVnMarketHoursUtc`/`isVnMarketHoursAt`, flagged in the board status_note, does not change which helper was correct to reuse here). PM option A implemented as recommended, no override. Market open → skip the call (clear log line: ticker+reportId), market closed → call exactly as before. The unconditional `updatePekTriggered.run(row.id)` write right after the if/else block is untouched — row still advances to `pek_triggered` either way; D3C's reconciliation job re-fires once market hours pass. Added `opts.now?: Date` (default `new Date()`) to `runBctcPdfPullJob` for deterministic testing — production caller passes no opts, zero behavior change live.

Extended `bctc-pdf-pull-job.test.ts` in place (731L, judged extendable not "large/hard to extend"): TC-18 (market open → NOT called, row still `pek_triggered`), TC-19 (market closed → called, baseline), TC-20 (shellRow-null branch unaffected by the guard during market hours — same technique as pre-existing TC-17). Also pinned an explicit off-hours `now` into the 3 pre-existing D3B tests (TC-14/15/16) that assert the call fires — they didn't pass `now` before and would have gone wall-clock-flaky (silently start failing during a 02:00-08:59 UTC CI run) the moment this guard shipped; direct in-scope consequence of the new guard, not scope creep.

Verification: `bun tsc --noEmit` clean, `mock-guard.sh --files` PASS (both files). `bctc-pdf-pull-job.test.ts` standalone: 26/26 pass, 80 expect() (was 23/71 pre-task). 4 closest siblings (D3A helper, D3C reconcile, ENRICH-SILENT-0ROWS, OVERLAP-GUARD) standalone: 26/26 pass, zero regression — flagged residual (not this task's scope): OVERLAP-GUARD's own PEK-call assertions don't pin `now` either, same latent wall-clock risk, future task. Bare `bun test` hung again (same documented sandbox issue, confirmed live) — used `scripts/ci-per-file-isolation.sh` host-safety runner for the full sweep (results appended by router/QA). toolCount/scheduler-count unaffected (no tool/cron file touched). Diff genuinely minimal: 1 import, +1 opts field, +1 local var, if/else-if restructure at the exact call site (net +11L incl. comments), zero surrounding refactor.

Commit: NOT committed by this agent — router RAW-verifies and commits per the D1/D2/D3A/D3B/D3C dispatch discipline. Board: `READY`→`REVIEW` via `orch-apply.sh` (new dev-side guard script `scripts/dev-fix-bctc-r-high-2-market-hours-guard-ready-to-review.jq`, mirrors the D3C precedent script), `next_agent=qa`.

Zone health: tsc clean, new test 3/3 pass (9 expect()), 26/26 in the extended file, 4 sibling files 26/26 pass zero regression, mock-guard PASS, toolCount/scheduler-count unchanged, guard-the-call-not-the-state-machine contract verified (updatePekTriggered.run unconditional either branch) | HEALTHY.

## 2026-07-10 — FIX-ORCHSTATE-CONSERVATION-GUARD-CIRCUIT-BREAKER → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (router-adjudicated DIRECT dispatch, supervised:true, bypassed BOUNDED-1 FIFO — closes the content-safety half of FIX-AUDITOR-ORCHSTATE-FULLDOC-OVERWRITE-CLOBBERS-SSOT)

New `scripts/orch-conservation-check.mjs` (bun) — whole-board magnitude-ratio guard (task_total = 6 flat lanes + active/closed sprint tasks per brief formula; signal_total = signal_queue.rows.length; FLOOR_RATIO=0.5, MIN_BASELINE=10, `ORCH_APPLY_ALLOW_SHRINK` bypass mirroring `ORCH_APPLY_LIVE_FILE_OVERRIDE`). Wired as `orch-apply.sh` Stage 2 (after schema validation, before CAS-mtime rename) + `orch-state-hook-prewrite.mjs` PreToolUse parity (same fail-open-on-infra-missing contract as the existing validator check). Bypass wired ONLY into `orch-cold-evict.sh` (its internal `orch-apply.sh` call) + `pm/flow/task-archive.md` Sprint Eviction step — the 2 legitimate bulk-eviction writers per the brief; audited `orch-backlog-stub.sh` — confirmed no bypass needed (field-strip only, backlog length unchanged).

Extended (not forked) `scripts/test/orch-apply-wrapper-tests.sh` with COLLAPSE/APPEND-HAPPY/SHRINK-ALLOWED (320-backlog/340-done_verified/100-signal populated fixture). Proved RED-before (git-reverted `orch-apply.sh`, reran same extended harness: COLLAPSE exit 0, fixture destroyed) → GREEN-after (restored fix: exit 1, byte-unchanged) — both runs shown, not just asserted. Full harness 31/31. Extended pre-existing `orch-state-hook.test.mjs`: 2 tests needed `ORCH_HOOK_CONSERVATION_CHECK=/nonexistent` isolation (they compare minimal synthetic Write-tool content against the REAL live file's much larger totals — correctly blocking that IS the guard working as designed, not a bug) + 1 new fail-open parity test. 19/19.

**Incident (disclosed, recovered same tool-call):** an ad-hoc `orch-cold-evict.sh` smoke test against a throwaway `ORCH_STATE`-overridden fixture briefly overwrote the REAL `docs/data/orch/orch-state.json` — its internal `orch-apply.sh` call was not propagating `ORCH_STATE` into `ORCH_APPLY_LIVE_FILE_OVERRIDE` (pre-existing latent gap, not introduced by this task). Caught immediately (count mismatch vs the just-run `--dry-run` preview), recovered via `git checkout` to last commit (10a9a934d), hash-verified clean. Fixed as in-scope safety hardening: both `orch-cold-evict.sh` and `orch-backlog-stub.sh` now explicitly propagate `ORCH_APPLY_LIVE_FILE_OVERRIDE="${ORCH_STATE}"` (no-op in production — both already default to the identical canonical path); re-verified end-to-end against a throwaway fixture with real-file-hash-unchanged proof.

Live-confirmed in production: this row's own backlog→in_progress move passed the guard with zero friction (task_total live=538 candidate=538), proving normal lane moves net to zero as designed. Zero `apps/mcp-server/src` changes (verified). CANONICAL pointers added to `docs/policies/dev-standards.md`.

Commit: NOT committed by this agent — router RAW-verifies (re-run both test suites) and commits. Board: `BACKLOG`→`IN_PROGRESS`→`REVIEW` via `orch-apply.sh` (2 new dev-side jq scripts), `next_agent=router`.

Zone health: harness 31/31 PASS (RED-before/GREEN-after shown), hook tests 19/19 PASS, tsc N/A (no .ts touched), zero apps/mcp-server/src changes, real orch-state.json hash-verified unchanged post-incident-recovery | HEALTHY.

## 2026-07-10 — CI-RED-1a8c1bff-FIX → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (direct Step-3 FIX dispatch, no PM decomposition — single-zone, PO-reproduced 3 CI-red files)

Three stale/superseded test-literal fixes, all root-caused before editing (not blind-patched):

1. `1190-pipeline-watchdog.test.ts` — `schedulerFileCount` 64→65. `docs/data/cron-registry.json`'s live value was already 65 (bumped by commit `43f4c8a22`); the test literal alone was stale.
2. `FACTORY-SCHEDULER-job-table-registry.test.ts` — Group A/B 57→58, Group D total 79→80. Same root cause as (1): `43f4c8a22` (D3C, `bctcExtractReconcileJob.ts`) added one entry to `schedulerJobTable.ts`'s `buildJobTable()` generic wrapRun table (`grep` confirmed single registration, no duplicate; `system-map.json`'s crons mirror already lists it too). **Not** caused by this session's `server.ts` split-refactor commits (`8c228ffa6`/`56f922c93`/`821bbbeea`/`bce8be44b`) as the dispatch prompt hypothesized — traced to the actual commit via `git log -p -- docs/data/cron-registry.json` before bumping either count.
3. `1352a-async-extraction-race.test.ts` A-1/A-2 — terminal-status expectation `'done'` → `'pek_triggered'`. Read `bctcPdfPullJob.ts`'s own header comment (`FIX-BCTC-D3B-GATE-PEK-TRIGGERED-STATUS`, 2026-07-10) and `bctcExtractReconcileJob.ts`'s header before touching anything: D3B deliberately removed the old synchronous 0-row done/enrich_failed gate (a sync check right after a fire-and-forget 202 `/pek-extract` call is structurally guaranteed to read 0 rows) — the row now lands at `pek_triggered` by design, and D3C's `bctcExtractReconcileJob.ts` is the documented sole successor that reconciles it to `done`/`enrich_failed` out-of-band later. Confirmed via `git log` that this test file (last touched `e3501171f`) predates D3C (`43f4c8a22`). This is scenario (a) from the dispatch prompt — a stale test asserting a superseded synchronous model, not a real regression. Reconcile-to-`done` transition coverage already exists in `bctc-extract-reconcile-job.test.ts` (14 cases) — not duplicated here. Updated assertions + `describe`/`it` titles + inline comments; the actual 1352a race-condition contract (extraction genuinely awaited before any status write happens) is unchanged and still exercised.

Verification: all 3 files standalone + together — 39 pass/0 fail, 320 expect(). `bun tsc --noEmit` clean. Full raw `bun test` (single process) is a known-unreliable local repro in this sandbox — 81 fail/6 errors + a Bun engine crash at the tail, but ALL failures were in `src/_deprecated/` (ESM cross-file contamination on files CI doesn't even run — `scripts/ci-per-file-isolation.sh` only scans `src/__tests__/*.test.ts`), zero overlap with the 3 touched files or any BCTC/scheduler production code (none touched — test-file-only diff). Pushed `0d9226a91`; fresh GitHub Actions run `29122457852` — `bun test` job `conclusion=success`, all 8 jobs green, `headSha=0d9226a9115561ef3d29ff880d5e08c0d94704c2` differs from the original failing `1a8c1bffb13ddfc096338c4f346e6e6d139d7947` — verification_gate satisfied via live `gh run view`, not local-only claim.

Commit: this agent's own direct commit (RUN-SOLO, explicit-path staging, 3 files only — `git diff --cached --name-only` verified before commit). Board: `BACKLOG`→`REVIEW` via `orch-apply.sh` (lane-move `task_board.backlog[]`→`task_board.review[]`, not an in-place status-flip — per the standing status-flip≠lane-move lesson), `next_agent=qa`.

Zone health: tsc clean, 3 touched files 39/39 pass, toolCount/scheduler-count unaffected (test-only diff, zero production code touched), fresh CI run green on pushed HEAD | HEALTHY.
