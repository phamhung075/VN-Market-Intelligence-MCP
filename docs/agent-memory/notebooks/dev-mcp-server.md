# dev-mcp-server -- Notebook

## 2026-08-07 — FIX-CI-SIZELINT-SCHEMA-TS-DEFLAKE-REGRESSION-372L — schema.ts extraction (2nd size-lint RED on this file in 2 days) → REVIEW, next_agent=qa

**Session:** f298ccf7-8cf4-452d-9a5a-57dcb47e65ac. Root-cause extraction (not another comment-trim, per PO's explicit AC1 reject-criteria): moved the always-run post-init migration block (HUT/1869b/VCB/BACKFILL_079/1489/1490/foreign-flow-migration-calls) into new `schema-post-init-migrations.ts`, and the 2 standalone foreign-flow helpers (`migrateForeignFlowColumns`, `backfillDailyForeignFlow`) into new `schema-foreign-flow-migrations.ts`. `schema.ts` 372L→261L — 75L real headroom under the 336L baseline. Both new files stayed <=120L (79L/69L) to avoid needing a size-justification header on day 1. `schema.ts` re-exports both foreign-flow functions for the 15+ existing direct test importers (byte-identical logic, zero behavior change) — confirmed at source that the PRAGMA-bootstrap and WeakSet-guard blocks (the other 2 PO-suggested candidate seams) are tightly coupled to live deflake/memory-leak fixes and were left untouched.

**Evidence:** `size-lint --check` — schema.ts absent from offenders (AC3). Scoped 91 pass/0 fail (1527-schema-slices-test.ts + both foreign-flow test files, backward-compat contract intact). `tsc`/`pnpm check` blocked in this worktree — no `node_modules` (pnpm-lock.yaml drift, pre-existing, out of scope). Full-suite AC4: this worktree's missing `node_modules` (no `zod`) inflates the fail count env-wide, so ran a controlled A/B in the SAME broken environment — WITHOUT fix: 10476 pass/637 fail/350 errors; WITH fix: 10477 pass/636 fail/349 errors (net -1 fail, the documented order-dependent flaky-floor delta, zero regression attributable to the extraction). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S78 (renumbered — third independent collision on this shared journal file this Merge Gate). Board `backlog[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`.

Zone health: schema.ts clear of size-lint, foreign-flow/schema-slice backward-compat 91/91 pass, tsc unverifiable this worktree (infra gap, flagged not fixed) | HEALTHY.

## 2026-08-07 — FIX-CI-SIZELINT-PUSHBCTCREFINEDUNITTOOL-283L size-lint RED on pushBctcRefinedUnitTool.ts (P1, XS, self-inflicted by 73e65eafc this session) → REVIEW, next_agent=qa

**Session:** f298ccf7-8cf4-452d-9a5a-57dcb47e65ac. `73e65eafc` (this same session's FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE, AC2) wired the image-fetch-degraded signal fire into `pushBctcRefinedUnitTool.ts`, growing it 227L→283L (baseline=227, upper=249) without a size-justification header — that task's own DoD only re-checked its new test file + existing bctc suites, never re-ran size-lint on the file it grew. Fixed via header (not extraction): `size-justification: 292L` (exact post-edit count) explaining the growth is the AC2 signal-fire branch, sharing the handler's existing `db`/`report_id`/`effectiveFlags` closure — matches the live `getBctcRefinedTool.ts`/`composition-root.ts`/`orchStateSchema.ts` precedent (header alone clears `--check` regardless of a stale baseline entry).

**Also fixed in-session (infra, not this task's regression):** this worktree's `apps/mcp-server` had zero `node_modules` (2nd time this notebook documents it, see FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE's own entry above) — ran `bun install` from INSIDE `apps/mcp-server` (correct cwd, not repo root), restored all 424 packages cleanly. This unblocked a real full-suite run instead of an inflated missing-`zod` fail count.

**Evidence:** `bash scripts/audits/size-lint-justification.sh --check` — RC=0, this file no longer listed. `bun tsc --noEmit` clean. Targeted `TRUST-RED-sanity-gate`/`AR-refined-units-idempotency`/`FIX-REFINE-LOCK-TTL-RECLAIM`/`FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE` test files: 46/46 pass, 159 expect() calls — comment-only diff, zero behavior change. Full suite (with `node_modules` fixed): 15174 pass/45 fail/1 error/48087 expect() (499s) — matches this notebook's documented flaky floor (S71:53, S75:44-45), zero bctc-named failures. Commit pathspec-scoped to `pushBctcRefinedUnitTool.ts` + this notebook + decision journal only (excluded pre-existing unrelated dirty state: `apps/mcp-server/data` symlink→dir drift, `tool-usage-stats.json`). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S79. Board minted fresh directly into `review[]`/`next_agent=qa` via `orch-apply.sh` — no pre-existing `in_progress[]` row existed for this task_id (router claimed the task-lock but never minted a board row; mirrored the sibling `FIX-CI-SIZELINT-BCTCREFINED-PROJECTION-BASELINE` row's exact field shape).

Zone health: pushBctcRefinedUnitTool.ts clear of size-lint, tsc clean, targeted 46/46 + full-suite within flaky floor, 1 prod file touched (comment-only header, zero behavior change), node_modules infra gap resolved for the rest of this session | HEALTHY.

## 2026-08-08 — FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX (P2, S, SLS/supervised+plan_only) → REVIEW, next_agent=po, spec-only

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router). `supervised:true`/`plan_only:true` — no code or doc-of-record file touched; deliverable is a spec doc only. Re-verified the row live rather than trusting its snapshot: `data-quality-anomaly:DGC:Q1-2026` (the row's cited FP source) has TTL-expired (2026-07-30) and is no longer held (`task_list_held` confirmed, 12 held sprint-task locks, none matching). bctc-analyst has since renamed this escalation lock-kind to `bctc-dataquality:*` — 3 locks held today, and TODAY's own D4 cycle (`docs/agent-memory/notebooks/system-auditor.md` `## d4-auto · 2026-08-08T03:00:02.589Z`) already shows 6 first-occurrence candidates for it (both `not_found`/`pipeline_mismatch` facets × 3 locks) — will emit at the next daily tick if unaddressed. Spec covers both prefixes.

**Category-error refutation:** `po_second_facet_20260728` claimed `pipeline_mismatch` needs a SEPARATE whitelist consultation from `not_found` (a "category error" — comparing `.head.active_task_id` against a different-plane lock). `git blame -L 628,645 tasksMdJanitorJob.ts`: the R-2 per-lock mismatch loop has iterated `survivingLocks` (post-R1b-filter, same set R-3 reads) since `e109f49f81` (2026-07-08) — the same commit that ported R-1b itself. One whitelist entry already silences both emissions; claim refuted against live code.

**Design decision (deliverable point 2):** promoted `KNOWN_LEGIT_PREFIXES` to `docs/data/system-map.json` `.project.coordination.known_legit_lock_prefixes` (option 2b) over a structural `<kind>:<TICKER>:<PERIOD>` pattern-match (option 2a). Refuting evidence for 2a: the live `bctc-dataquality:vnindex-crosstool-mismatch` lock is NOT ticker/period-shaped — a shape-regex would have missed this real, currently-forming FP. 2b mirrors this codebase's own proven `loadWatchlistSeedFromSystemMap()` precedent (`seedWatchlist.ts`) with a non-empty fail-open `FALLBACK_KNOWN_LEGIT_PREFIXES` (an empty fallback would un-suppress every currently-known pattern on SSOT-read failure — worse than the bug this fix exists to close).

**Stale-doc-drift correction (deliverable point 4):** `handlers.md:19` / `audit-dimensions.md:88-90` claim the code was "NOT yet updated" — confirmed FALSE, code shipped same-day (`e109f49f8`, 2026-07-08) and was extended again 2026-08-07 (`86b31eccd`, `cron-registration:*`) — that commit touched code+test only, doc-sync happened via a SEPARATE follow-up commit, live proof of exactly the "re-drops on next port" risk deliverable point 3 warns about.

**Evidence:** Spec doc `docs/handoffs/FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX-spec.md` (verbatim before/after diffs for all 4 files + test-table extension + negative control, e.g. `bogus-escalation:XYZ:Q1-2026:ESC-1` must still trip D4). Commit `2dd62630d` (spec, pathspec-scoped). Board `task_board.in_progress[2]` → `task_board.review[]` via `orch-apply.sh` (Stage 0/1 PASS, conservation check OK) — `status:REVIEW`, `owner:po`, `next_agent:po`, `supervised`/`plan_only` preserved, `spec_doc` + `dev_mcp_server_review_note` added; `.head` reset idle same write. Commit `96ea6be74` (board-state, pathspec-scoped — first bare `-m` attempt hit sweep-guard escalate-threshold=3, retried with explicit pathspec). `git status` confirms zero diff on `tasksMdJanitorJob.ts`/`handlers.md`/`audit-dimensions.md`/`system-map.json` this cycle. DJ: `sprint-FIX-AUDITOR-D4-WHITELIST-DATA-QUALITY-ANOMALY-PREFIX-dev-mcp-server.md` S1-S4.

Zone health: spec-only cycle, zero prod files touched, zero test run required (no code shipped) | HEALTHY.

## 2026-08-08 — FIX-AGENTSIGNALS-FROMAGENT-SCHEMA (P2, stale review[]-lane row) → DONE_VERIFIED, next_agent=pm

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router, Review-Lane SECONDARY-Drain). detail_ref resolved owner/next_agent=dev-mcp-server (own-zone code-verification), not the stale top-level `next_agent:qa`. Did not trust the row's own review_note — independently re-verified every claim: both cited commits real and match description (8a6b798ce made `agent` optional 2026-06-19; 440e924f7 diff-confirmed pure const-extraction of `GetAgentSignalsShape`, zero other line changed); new `FIX-AGENTSIGNALS-FROMAGENT-SCHEMA.test.ts` re-run myself 8/8 pass; 4 precedent/consumer suites re-run myself 28/28 pass (note's "30/30" was an arithmetic slip, immaterial — 0 fail either way); `bun tsc --noEmit` clean.

**Closed the note's own flagged REMAINING GAP myself:** mcp-server image rebuilt 2026-08-07 (postdates both fix commits) — running container already had the fix. Native `mcp__gateway__*` tool binding absent this session (session-scoped gap, gateway-call-contract.md §6 Degraded Mode); used the documented Bash bridge (`scripts/agents-flow/mcp-call.sh` `mcp_call`) for a genuine LIVE RAW-verify against the running container: `from_agent='news-scout'` (sender-history) → valid response, no -32602; neither `agent` nor `from_agent` (inbox mode) → controlled readable error, not a protocol crash; `from_agent=null` (all-producers) → real rows returned. All 3 branches confirmed live — no ops/rebuild gate needed.

**Evidence:** commits `git show --stat` on both hashes; scoped `bun test` re-runs (8/8 + 28/28); `bun tsc --noEmit` clean; `docker image inspect` Created timestamp vs commit dates; live `mcp_call get_agent_signals` × 3 arg shapes. Board `review[]`→`done_verified[]` via `orch-apply.sh` (Stage 0/1 PASS, conservation check OK: 775/775). Commit `5a23565e0`, pathspec-scoped to `orch-state.json` only.

Zone health: no code touched this cycle (verification-only), fix confirmed correct + live + already deployed | HEALTHY.
