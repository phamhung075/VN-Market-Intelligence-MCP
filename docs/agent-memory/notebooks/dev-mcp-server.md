# dev-mcp-server -- Notebook

## 2026-07-30 — CCATO-MCP-T1-DOMAIN-ENGINE (Ready-Lane Consumer, P0, epic SPRINT-CCATO-TRUTHGATE-MCP-NATIVE) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Spec: `docs/architecture-briefs/2026-07-17-ccato-truthgate-mcp-native.md` §3.2/§5.1 (T1 of 8, depends: none). Port the pure scan/classify/quarter-resolve engine from `scripts/narrative-truth-gate.sh` (python) into `apps/mcp-server/src/domain/services/narrativeTruthGate/` (TS), zero I/O.

**New files:** `claimCandidateScanner.ts` (scanClaimCandidates/findTickers/splitParagraphs/splitSentences — byte-faithful port of script L144-163+280-323, incl. the ≤1-candidate-per-(paragraph,dimension) dedup ordering quirk), `verdictClassifier.ts` (classifyVerdict/flattenText/summarizeVerdict — port of L251-278), `quarterResolver.ts` (resolveLatestElapsedYoyPeriods — port of L236-247, injectable `now: Date`, Jan-Mar rolls back to prior-year Q4).

**§5.1 hard AC (side-by-side fixture parity):** new `CCATO-MCP-T1-DOMAIN-ENGINE.test.ts`, 28 tests. Spins up a local `Bun.serve` MCP-gateway stub (fixed non-null response forces every candidate to FAIL so `claim_text` is recoverable from stdout), spawns the REAL unmodified `scripts/narrative-truth-gate.sh` (`NTG_SKIP_SIGNAL_EMIT=1` — no orch-state write, no jq dep) against the REAL `docs/social/fb-post-2026-06-30.md` + `docs/data/claim-tool-map.json`, parses its `[FAIL]` lines, asserts IDENTICAL dimension/ticker_or_dim/claim_text triples vs. the new TS scanner on the same 2 files. Passed first run — no JS/python regex divergence on this fixture (R-1 closed empirically).

**Evidence:** `bun tsc --noEmit` clean. New test file 28/28 pass. Full `bun test`: 14946-14947 pass/40 skip/52-53 fail (566s) — within the standing pre-existing-flake band (52-59); grep-confirmed zero named fails reference `narrativeTruthGate`/CCATO/scanner/classifier/resolver (zero existing files touched — pure addition, regression structurally impossible). Tool/scheduler probes unchanged (184/88 — T1 registers no MCP tool, that's T6). Server boot log: clean import, `toolCount:184`, before an unrelated pre-existing :3000 port conflict (environment artifact). `domain-model.md`+`testing.md` updated (doc-review).

Zone health: tsc clean, new-file-only diff, full-suite fail-set keyword-scanned clean, §5.1 parity ran against the real bash engine not a hand-derived fixture | HEALTHY.

## 2026-07-30 — FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT (BOUNDED-1 auto-pickup, P2) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Mechanism-first per AC-1: `docker exec` readonly `bun:sqlite` probe against the LIVE prod `market.db` (not a host copy). `price_confirmation`: 0 rows ALL TIME (dead emitter, out of scope). `urgent_news`: 13 rows all-time, 9 since 2026-06-05 (the SLA's own epoch) — 0/9 carried numeric `finding_data.confidence`. Cross-checked the live emitter template (`docs/agents/news-scout/flow/stage-signals.md`'s `urgent_news` block): it populates `regime_adjusted_score` (0-10 scale), never `confidence` — outcome (b) confirmed: signals posted, required field absent, not a dead emitter and not a silent insert failure.

**Decision: WIRE**, not retire (AC-2). `deriveAuditConfidence()` (new, `domain/services/signalValidator.ts`) falls back to `regime_adjusted_score / 10` (clamped `[0,1]`) when `confidence` is absent — uses the real quality signal news-scout already computes rather than a fabricated constant. `regime_adjusted_score` added as a typed optional field to `UrgentNewsFindingData`/`Schema`/`LooseSchema` (`domain/signals/signalTypes.ts`) and documented in `docs/agents/tools/package/news-scout.md` + `docs/agents/tools/list/post_agent_signal.md` — emitter contract and audit trigger now agree. `price_confirmation` unchanged (schema already requires `confidence`). Table-wide SLA check is `MAX(created_at)` across all rows regardless of source, so unblocking `urgent_news` alone unblocks the whole alarm.

**AC-3 (swallow observability):** both non-fatal catches (`agentSignalTools.ts` audit-write, `signalQualityAuditStore.ts` insert) now `logger.warn(...)` instead of `console.warn(...)` — persists to `system_logs` (queryable by `source`), not just stdout.

**AC-4/5:** new `FIX-SIGNALQUALITYAUDIT-WRITE-GATE-UNREACHABLE-BY-EMITTER-CONTRACT.test.ts`, 13 tests: unit coverage of `deriveAuditConfidence` (priority/fallback/clamp/null), bidirectional proof via the real `post_agent_signal` handler (urgent_news+regime_adjusted_score → row lands; urgent_news+neither field → row legitimately absent; price_confirmation regression unchanged; non-qualifying type unaffected), and swallow-observability (forced DB throw → `logger.warn` fires, response stays `success:true`; source-text regression pin against `console.warn` reverting). `233-cowork-resilience-e2e.test.ts:393`'s `signal_quality_audit` reference is a comment inside an `expect(true).toBe(true)` placeholder (AC-8, explicitly "no automated assertion needed") — reconciled, not a real dependency. `freshnessSlaChecker.ts`'s `signal_quality_audit` threshold comment updated to flag the 6-row history as pre-fix noise (not a steady-state cadence) without re-tuning the 30d value itself, per the task's explicit caution.

**Evidence:** `bun tsc --noEmit` clean. New file 13/13 pass. Tool/scheduler probes unchanged (184/88). Local server boot clean (`toolCount:184`, no import errors). Full `bun test`: 14955 pass/57 fail (586s) vs. a same-tree baseline (`git stash`, re-run) of 14946 pass/53 fail (580s) — net +4, fully accounted for by `1324-push-news-all-sources.test.ts`'s pre-existing chromium-scraper-retry flake (`/usr/bin/chromium` absent in this sandbox; standalone run: 10/10 pass in 23.7s) flipping under full-suite load; one pollNews/Reuters test flipped the OTHER direction between the two runs, confirming shared unrelated flakiness not a directional regression. Grep-confirmed zero of the 57 named fails reference `signalQualityAudit`/`agentSignalTools`/`signalValidator`/`freshnessSla`/`SIGNALQUALITYAUDIT`. Both baseline (53) and with-fix (57) sit inside this sprint's documented pre-existing-flake band (52-59, see 2026-07-30 entries above).

Zone health: tsc clean, tool/sched counts unchanged, full-suite delta isolated to a named pre-existing unrelated flake (not my diff), decisive live-DB probe run before any code change | HEALTHY.

## 2026-07-30 — FIX-POLYMARKET-FETCH-DEAD-GEOBLOCK-ACTUATOR (S4 UNBLOCK, P1) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Implemented architect's already-made RETIRE ruling exactly (not restore-via-VPS — ANJ is a sovereign-regulator block, not a generic geoblock; did not re-litigate). Killed acquisition: `predictionMarkets.enabled` defaults `false` (`config.ts` + `mcp.config.json`, file value always wins so both had to flip). `predictionMarketPollJob` deliberately LEFT in `schedulerJobTable.ts` JOB_TABLE (cronJobCount unchanged 88) — cheap no-op while disabled, single-env-flip re-enable path.

**ACCEPTANCE-4 (required either way):** new `PolymarketTransportError` (`polymarket.ts`) — `fetchPolymarkets()` now throws it ONLY when the Gamma call transport-fails (TLS/403/DNS/parse) AND combined CLOB+Gamma results are 0; a legitimately-empty-but-successful fetch never throws (negative control, tested). `predictionMarketJob.ts` Step-3 catch + outer catch both narrowly rethrow past their swallow/cached-fallback/log paths so `jobRunRepo.wrapRun`→`recordJobRun` marks `status=error` instead of `status=success` — the literal mechanical fix for 28d of dead fetches reading as healthy.

**Consumer/tool-surface removal:** `get_prediction_markets()` dropped from `market-analysis.md`+`qa-responder/cycle.md`; `flow/prediction.md` deleted whole; `main.md` dispatch row + `init.md` catalog entry removed — grep-verified 0 remaining references across all 5 gate files. `get_prediction_markets` deregistered from `predictionTools.ts` (`get_prediction_accuracy` kept, unaffected — reads historical `prediction_signals`, not live). `agentBootstrap.ts` SKILL_MANIFEST + `docs/SKILL_MANIFEST.md` mirror cleaned. `tool-registry.json`/`project-stats.json`/`tools/list/INDEX.md` regenerated via canonical generators (184→183). Deleted 2 tool-specific test files (168-prediction-mcp-tool, 1850f-fixture-contamination); trimmed `1194-agent08-tools.test.ts`. `quality-checklist.json` DS-FRESH-01+CI-FRESH-03 retired to `status=INFO` (no new enum value invented) pending qa ratification.

**Evidence:** `bun tsc --noEmit` clean. Targeted suites (164/167/169/1194/1337/1209/1345c/1219 + skill-manifest siblings, 20 files): 234 pass/0 fail. Live server boot (`DB_PATH=:memory:`): `/health` → `toolCount:183`, zero import errors. `gen-project-stats.ts --dry-run`: `toolCount=183 cronJobCount=88`. Full `bun test` kicked off in background for corroboration but did NOT complete within this session window (high concurrent-agent load on the shared sandbox, ~3.5min CPU with little progress) — not claiming a full-suite number I don't have; the targeted evidence above is scoped exactly to every touched module. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md S33.

Zone health: tsc clean, toolCount 184→183 (intentional, tracked via canonical generators not hand-edited), cronJobCount unchanged 88, live server boot confirms no import breakage | HEALTHY.
