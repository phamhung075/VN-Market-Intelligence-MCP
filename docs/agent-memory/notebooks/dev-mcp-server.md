# dev-mcp-server -- Notebook

## 2026-08-08 — FIX-CI-SIZELINT-CHECKFOREIGNFLOWGAP-NEW-OFFENDER-181L (P0, XS, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa, split not header

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router RLC dispatch). Row fired CI RED 7 consecutive times (SHAs 83bb4359→76466302) since 2026-08-07T05:27Z; `checkForeignFlowGap.ts` was 181L, no baseline entry, no header (`bash scripts/audits/size-lint-justification.sh --check` confirmed new-offender). Read the file first per the task's own instruction to not default to a header: the D-NEW4 forensic root-cause header (~59L) is genuinely load-bearing (mirrors why `docs/architecture-briefs/2026-07-24-...` itself chose not to split), but `findForeignFlowGapDays` (pure calendar-math gap-day finder, 44L incl. JSDoc) is a clean, cohesive unit with zero coupling to the escalation/dedup logic in `checkForeignFlowGap` — a clean split was cheaper than a header.

**Split:** extracted `findForeignFlowGapDays` + its `MAX_LOOKBACK_DAYS`/`MAX_GAP_DAYS_PER_RUN` caps + the calendar-usage rationale paragraph into new `foreignFlowGapDetection.ts` (81L). `checkForeignFlowGap.ts` re-exports `findForeignFlowGapDays` (`import {...}; export {...};`) so the public import path is unchanged for the existing test file (`FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` imports both symbols from `checkForeignFlowGap.js` — zero test-file edits needed). Trimmed the remaining root-cause header's prose (tighter wrapping, no facts dropped) to land `checkForeignFlowGap.ts` at exactly 120L (was 125L after the mechanical split alone).

**Verified:** `bash scripts/audits/size-lint-justification.sh --check` now reports only the separate, out-of-scope `coordinationStore.ts` offender (1 file, not 2) — confirms `checkForeignFlowGap.ts` cleared. `bun tsc --noEmit` clean. `bun test` on 3 relevant suites (dedicated D-NEW4 test, `157-data-audit-job.test.ts`, `FACTORY-SCHEDULER-split-dataAuditJob.test.ts`) — 40/40 pass. `eslint --max-warnings 0` on both touched files — clean.

**Notebook-write process note:** first Edit attempt (prepended at file top) landed with tool-reported success but `git diff` showed zero change afterward — worked around per the documented Edit-tool-hook-silent-strip pattern via raw bash append + explicit-pathspec commit; also corrected placement — this file's own git-log-verified convention is append-at-bottom (chronological), not prepend.

**Evidence:** commit `638df5da0` (pathspec-scoped to `checkForeignFlowGap.ts` + new `foreignFlowGapDetection.ts`). Board `task_board.in_progress[]` → `task_board.review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`, `commit_sha` stamped. Did not touch `coordinationStore.ts` (separate row/owner) or the `FACTORY-GUARD-CI-size-lint-justification` design row/brief, per task scope.

Zone health: split verified locally green (tsc+tests+eslint+size-lint script), CI confirmation pending next push | HEALTHY.

## 2026-08-08 — FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER (P0-critical, M, router-dispatched) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router). Claimed READY→IN_PROGRESS myself via `orch-apply.sh` per the router's own dispatch instruction (Status-Flip=Lane-Move). Implemented architect's already-source-verified design brief (`docs/architecture-briefs/2026-08-07-fix-mcp-sse-session-manager-reaper.md`) — no design deviation. Root cause: `transport.ts:95` `mcpServer` was a bare local, never `.close()`d by either existing eviction path (`res.on("close")`, heartbeat-write-fail — the latter effectively dead code since Node's `res.write()` on a broken pipe buffers rather than throwing). Live measurement on the board row (po_rawverify) had already correlated this to a real container death (OOM, 6h36m from cold start).

**Fix:** collapsed 2 parallel Maps (`sessions`, `heartbeatIntervals`) + 2 duplicated inline cleanup blocks into 1 `SessionRecord` map (`transport`+`mcpServer`+`heartbeatInterval`+`createdAt`+`lastActivityAt`) + 1 `evictSession()` called from every trigger (close/heartbeat-fail/idle-reap/max-age-reap/DELETE). New idle(15min)/max-age(4h) reaper, `lastActivityAt` bumped in `handleMessage()`. New `DELETE /sse|/messages?sessionId=` route (`server.ts`) — defense-in-depth, not primary (the dominant gateway caller is out-of-repo and can't be made to send it). `stopReaper()` wired into `BunServerInstance.close()`.

**Test evidence:** `1862c-transport-session-eviction.test.ts` extended 5→12 cases (T6-T12 assert every eviction trigger now calls `mcpServer.close()` — the actual root-cause gap; T9 is a negative control proving `handleMessage()`'s activity-bump genuinely resets the idle clock, not just exists) — 12/12 pass. `081-bun-mcp-server.test.ts` +2 integration cases (DELETE 200→404 round-trip, DELETE-no-sessionId 400) — 10/10 pass.

**Full-suite regression rigor (not just scoped-file green):** ran the complete 1268-file/15290-test `bun test` 4× total, including one `git stash`-isolated CONTROL run against the untouched pre-fix code. The control run's 44 failures reproduce byte-identical in every fix-run (same 5000ms-class timeout family under full-suite CPU/socket contention — Task 1146, TSU-DEV-U5, FIX-B-2, Task 251, none in this zone); the extra 0-2 failures beyond that baseline SHIFT IDENTITY on every single rerun of either control or fix (Task 102 → FIX-POLLNEWS-COUNTER-CONSERVATION → Task 1345a → FIX-1267, all unrelated files) — decisive, diffed proof this is pre-existing environmental flakiness at full-suite scale, not a regression from this change. One genuinely reproducible issue was in MY own new DELETE test (raced an internal 5s abort against system-wide contention) — fixed by adopting the exact graceful-degrade pattern the file's own pre-existing "GET /sse" test already used, not inventing a new convention.

**Live verification beyond unit tests:** port 3000 was occupied by the live Docker container (not my build) — probed my locally-built code on `PORT=3099` instead (clean boot, no import errors). `/health` toolCount=183 sessions=0; `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200 (dashboard circular-dep check). Live GET+DELETE `/sse` round-trip with a real sessionId confirmed `{closed:true}` then `{closed:false}` on retry. Bonus real-world finding: a `curl --max-time`-aborted connection on this exact build never fired `res.on("close")` — live confirmation the leak shape this row exists to fix is real and reproducible right now; that session is now bounded by the new idle reaper (unit-proven via T8/T9) instead of leaking forever as it did pre-fix.

**Doc:** new "SSE Session Manager" subsection in `docs/architecture/microservice/mcp-server/infrastructure.md` per the brief's own file list. Doc self-heal: Gate 2b's `bun run src/index.ts &` / `curl localhost:3000` probe in `docs/agents/dev-mcp-server/flow/main.md` assumed port 3000 free — false on a dev host with the live container running; added the `PORT=3099` override as the documented fallback for next cycle.

**Evidence:** `tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88, both match pre-task baseline. Commits: `apps/mcp-server/src/interface/mcp/transport.ts`+`server.ts`+2 test files (code), `docs/architecture/microservice/mcp-server/infrastructure.md`+`docs/agents/dev-mcp-server/flow/main.md` (docs), board row IN_PROGRESS→REVIEW via `orch-apply.sh`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S81. Graphify: skipped — no Skill-invocation tool available to this subagent session (infra doc updated; recommend router run `/graphify docs --update --no-viz`).

Zone health: transport.ts session-leak root cause closed (every eviction path now calls mcpServer.close()), reaper backstop live-confirmed against a real leaked session on this build, full-suite regression rigorously diffed clean against a stashed control run | HEALTHY.

## 2026-08-08 — FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L (P0, S, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa, split not header

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router RLC dispatch). Row fired CI RED 7+ consecutive times since ~2026-08-07T12:14Z; `coordinationStore.ts` was 1388L vs baseline=1241L/upper=1365L (23L overage). Sibling offender named at mint time (`checkForeignFlowGap.ts`, see entry above) was already closed; a NEW sibling (`transport.ts`, own row `FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L`) appeared later — neither is this row's scope.

**Split, not header:** the file's own ST-3 comment already flagged the `scheduled_tasks` helpers as a self-contained internal-helper block. All 10 functions (`claimDueScheduledTasks`, `completeScheduledTask`, `expireScheduledTask`, `failScheduledTask`, `cancelScheduledTaskById`, `getScheduledTaskByDedupKey`, `getScheduledTaskById`, `insertScheduledTask`, `listScheduledTasksDb` + `ScheduledTaskRow` type) take an already-open `Database` handle and never touch `coordinationStore`'s private `task_locks` singleton — cohesive, structurally independent, just historically co-located. Extracted verbatim into new `scheduledTaskStore.ts` (259L, own size-justification header). `coordinationStore.ts` re-exports via `export * from "./scheduledTaskStore.js"` — all 3 existing call sites (`scheduledTaskTools.ts`, `scheduledTasks.test.ts`, `coordinationStore.test.ts`) needed zero edits. Table DDL stayed in `ensureCoordinationTable()` (one shared startup migration pass). Result: `coordinationStore.ts` 1388L → 1173L, well under baseline — the `docs/data/size-lint-baseline.json:263` entry (1241) was NOT touched/raised, per the row's own explicit anti-laundering mandate.

**Verified live in CI, not just locally:** pushed commit `763ef6822`; CI run `31277793877` job `size-lint` now reports exactly 1 offending file — `apps/mcp-server/src/interface/mcp/transport.ts` (separate sibling row, not mine). `coordinationStore.ts`/`scheduledTaskStore.ts` both absent from the failing-file list (confirmed via `gh api .../jobs/93154116153/logs`, not just the badge). Same run's `bun test` job (CI's real per-file isolation runner) shows exactly ONE unrelated `FAILEDFILE`: `167-prediction-market-job.test.ts` — zero relation to this change, matches the documented rotating-flake class (`SPIKE_CI-PERFILE-ISOLATION-FLAKE.md`). Locally: `bash scripts/audits/size-lint-justification.sh --check` matches CI. `bun tsc --noEmit` clean. Focused 14-file coordination/scheduled-task suite: 246/246 pass. Full monolithic `bun test` (ran 2×): 44/46 fail both times, identical unrelated-test set (Task 1146, TSU-DEV-U5, FIX-B-2, Task 251, etc — same class as the entry above), zero overlap with touched files.

**Evidence:** commits `763ef6822` (code: `coordinationStore.ts`+new `scheduledTaskStore.ts`+`docs/architecture/microservice/mcp-server/system.md`) and `ebcdc1225` (board `task_board.in_progress[]` → `task_board.review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`, `commit_sha` stamped, live-CI evidence recorded in `dev_implementation_note`/`qa_verification_hint`). Overall CI run conclusion is still `failure` — expected and job-scoped, per the row's own mint note: size-lint only goes fully green once the separate `transport.ts` sibling row also lands; this row's own file-scoped compliance is independently closed. Did not touch that sibling row or the `FACTORY-GUARD-CI-size-lint-justification` design row/brief.

Zone health: `coordinationStore.ts` split cleanly, verified both locally (tsc+focused-tests+size-lint script+server boot) AND live in CI (size-lint failing-file list + per-file bun-test isolation runner) | HEALTHY.

## 2026-08-09 — FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L (P1, S, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa, split not header

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (dev-team Step 3 direct-execute, branch:null). Zone `apps/mcp-server/` routed directly. NOT a duplicate of the already-DONE_VERIFIED `FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L` sibling entry above — that row was `coordinationStore.ts` (infrastructure/db/); this row is `coordinationTools.ts` (interface/mcp/tools/system/), a different file, 457L vs baseline=388L/upper=426L (31L overage), CI-RED on `size-lint` (run 31285673936, head 39e1e161).

**Split, not header:** all 6 registered tools (`task_claim`, `task_heartbeat`, `task_release`, `task_list_held`, `task_force_release_orphan`, `get_week_period`) were each already a self-contained `server.tool(...)` call — zero cross-tool coupling except `SERVER_SESSION_ID` (used only by `task_claim`). Extracted each verbatim (zero logic change) into its own file under `interface/mcp/tools/system/coordination/` (`taskClaimTool.ts` 119L, `taskHeartbeatTool.ts` 100L, `taskReleaseTool.ts` 77L, `taskListHeldTool.ts` 75L, `taskForceReleaseOrphanTool.ts` 80L, `weekPeriodTool.ts` 72L) — every one comfortably under the 120L size-lint threshold, so none needs a `size-justification` header. `coordinationTools.ts` is now a 54L thin entry point that still exports `registerCoordinationTools(server)` unchanged (calls all 6 sub-registrars), so `registry.ts`, the `coordination/index.ts` barrel (`export { registerCoordinationTools } from "../coordinationTools.js"`), and every direct-importer test needed zero edits. Also dropped the dead `buildWeeklyPublishMarkerKey` import (unused in the file body — confirmed via grep before deleting, not assumed) and surgically pruned the now-stale 388L `docs/data/size-lint-baseline.json` entry via a scoped `jq del` (NOT a wholesale `--update`, which would have also silently rebaselined the separate, out-of-scope, already-failing `transport.ts` offender — not this row's file, left untouched).

**Verified locally (CI confirmation is next-tick QA's job per `verification_gate: ci_green_on_subsequent_push` — not observable from this session):** `bash scripts/audits/size-lint-justification.sh --check` no longer lists `coordinationTools.ts` (sole remaining offender: pre-existing `transport.ts`, untouched, different row). `bun tsc --noEmit` clean. `npx eslint` (fence config) clean on all 7 touched/new files. Targeted suite — the 4 files that `import { registerCoordinationTools }` directly (`task-lock-coordination-tools.test.ts`, `1980-p1-final-required-flip.test.ts`, `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts`, `1982-quality-burndown-CHIJ.test.ts`) plus `FIX-DIGEST-PREDICT-ISO-WEEK-DEDUP.test.ts` (covers `get_week_period`'s domain dependency) = 96/96 pass, 0 fail.

**Evidence:** commit `1653cea0aa56e43b4a84463a3dc2772bbde15705` (code + baseline prune, pushed, pre-push hook `pnpm --filter vn-market check`/`tsc` green, no `--no-verify`). Board `task_board.backlog[]` → `task_board.review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`, `commit_sha` stamped in `dev_implementation_note`/`qa_verification_hint`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S83 (new continuation file — `-4.md` was at 35824/36000 bytes, no headroom for this entry, rolled per the dual-axis cap-check precedent). Did not touch `transport.ts` or any row/scope outside this task.

**Next for QA:** `gh run view <databaseId with headSha AFTER 1653cea0aa56e43b4a84463a3dc2772bbde15705> --json jobs -q '.jobs[]|select(.name=="size-lint")|.conclusion' == success`.

Zone health: `coordinationTools.ts` split cleanly into 6 single-tool files, verified locally (tsc+targeted-tests+eslint+size-lint script), live CI confirmation pending next tick's QA check | HEALTHY.
