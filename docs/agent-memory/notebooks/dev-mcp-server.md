# dev-mcp-server -- Notebook

## 2026-08-22 — DDD document-as-deviation annotations (router-dispatched follow-up on agents-architect brief; agent-father ratified, routed writes to me) → comment-only, no board row

**Session:** 88555d2e-8fbc-4591-9164-20f6b54d475f (router-held). agent-father ratified (STEP agent-father-S53) "document-as-deviation, not relocate" for two files flagged by `docs/architecture-briefs/2026-08-22-agent-fabric-ddd-debug-logger-tool-optimization.md` § Part 1: `orchStateSchema.ts` (StatusEnum/TERMINAL_SET/checkLaneCoherence et al — pure domain invariants with zero `domain/` counterpart) and `coordinationStore.ts` (`ORPHAN_EMIT_ALLOW_LIST` — a real business decision embedded in `infrastructure/db/`, the exact class `composition-root-logic-gate.go` catches on the Go side with no TS equivalent). agent-father has no `apps/mcp-server/` zone grant, so routed the actual annotation writes back through the router to me.

**What shipped:** two `ddd-deviation-allow:` comment blocks (comment-only, zero behavior change) — one before `orchStateSchema.ts`'s § 1 STATUS ENUM banner, one before `coordinationStore.ts`'s `ORPHAN_EMIT_ALLOW_LIST` doc comment — mirroring the existing `size-justification:`/`composition-root-logic-allow:` exemption convention (no repo precedent for a DDD-drift marker existed, so minted a new one consistent with that naming family rather than overloading the Go-specific `composition-root-logic-allow:`). Each block cross-references the brief's Finding 1/2 and the S53 ratification.

**Verified:** `bun tsc --noEmit` clean. `bun scripts/gen-project-stats.ts --dry-run`: toolCount=183/cronJobCount=88, both unchanged (comment-only edit, no barrel touched). Full non-isolated `bun test`: 15303 pass/40 skip/52 fail (577.65s) — within the documented noise band for this session (51-57 fail range, full-suite-under-contention flakes unrelated to this change; comments cannot affect runtime behavior).

**Item 2 (fast-follow, non-blocking per agent-father's RETURN):** TS-side guardrail equivalent to `composition-root-logic-gate.go` (catch "business rule in `infrastructure/` with zero `domain/` counterpart" — invisible to import-graph-only `eslint-plugin-boundaries`). Not attempted this cycle — a rushed AST heuristic risks false positives (the Go gate needed live-scan threshold tuning to hit zero FPs before CI-wiring); minted backlog row `FIX-COMPROOT-LOGIC-GATE-TS-EQUIVALENT` (priority low, owner dev-mcp-server) via `scripts/orch-apply.sh` instead of leaving it silently dropped.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S9. No task_board row (ambient, comment-only — same as agent-father's own S53 cycle); backlog row `FIX-COMPROOT-LOGIC-GATE-TS-EQUIVALENT` is the one board write this cycle made.

Zone health: comment-only DDD-deviation annotation landed with full G12 evidence (tsc clean, tool/cron counts unchanged, full-suite fail count within documented noise band), fast-follow guardrail honestly deferred to a tracked backlog row instead of skipped or rushed | HEALTHY.

## 2026-08-22 — FIX-CI-SIZELINT-GETBCTCPENDINGREFINETOOL-BASELINE-TOLERANCE-EXCEEDED (P-high, S, PO-mint CI-red, main RED 7 occurrences/7d) → done_verified[]

**Session:** 02594cce-7946-4d62-8d2f-586b9b883695 (router-held). PO had root-caused and stamped `next_agent=dev-mcp-server` on 2026-08-15 but the row sat in `backlog[]` (not dispatchable) for 7 days with main continuously RED on the `size-lint` CI job; promoted to `ready[]` 2026-08-22T19:00Z and dispatched.

**Root cause:** `getBctcPendingRefineTool.ts` grew 464L → 605L across 3 tested/documented commits (`51f162829` pagination, `8b4f977fd` ColumnLayout cross-window pass-through, `532fc71a0` response-size guard rework, all landed 2026-08-08→08-15) but nobody re-ran `scripts/audits/size-lint-justification.sh --update` afterward, so `docs/data/size-lint-baseline.json`'s stale 464L entry kept failing `--check`'s tolerance test (upper=510L) on every push, independent of the actual commit's own content (each failing CI SHA was an unrelated docs/chore commit — the sequencing trap the row's own notes already flagged).

**Investigated bloat-vs-legit before fixing (per the row's own instruction not to paper over unreviewed bloat):** all 3 growth commits are real, tested, documented feature work — `532fc71a0` in particular was RAW-verified live against the running container's real production data at the time (limit=100 measured 565,285→524 chars post-guard). Confirmed convention precedent: sibling files in the same `financial-reports/` directory (`bctcFullTools.ts` 1467L, `cashFlowTool.ts` 784L, `reports.ts` 672L) are baseline-grandfathered the same way with no inline `size-justification:` header — re-baselining matches established zone convention; splitting the file would deviate from it for no functional gain.

**Fix:** ran `scripts/audits/size-lint-justification.sh --update` (the CANONICAL, documented mechanism) — wholesale-regenerated `docs/data/size-lint-baseline.json`. Diff scoped to exactly 9 files' line-count snapshots (my target 464→605, 2 already-shrunk unrelated files absorbed for free, 6 small in-tolerance drifts) + `generated_at` — no code touched. `--check` now PASS (0 unjustified offenders, 1409 scanned); `size-lint-justification.test.sh` 6/6 PASS. `tsc --noEmit` clean, toolCount=183/cronJobCount=88 unchanged (data-file-only change, G12 full-suite bun test not re-run — no code surface touched).

**Landed + verified:** commit `5944172e0` pushed (`dc0f90334..5944172e0`). RAW-verified CI green on that exact new head SHA per the row's own stated `verification_gate` (`ci_green_on_subsequent_push`): `gh run view 32593075632` — `size-lint` job conclusion=success, overall run conclusion=success. Closed the row `ready[]` → `done_verified[]` myself (commit `73e43bb3f`) with `verification.raw_probe` attached (RC-VERIF gate requires it — this id is not grandfathered) since the row's AC is a single mechanical gh-CLI check I had already satisfied; no PM/QA pipeline was wired for this direct PO-mint CI-red row.

**Evidence:** commits `5944172e0` (baseline fix), `73e43bb3f` (board close); `gh run 32593075632`.

Zone health: CI RED on main closed (size-lint job green on new head), root cause confirmed legitimate reviewed growth not bloat (matches zone's own baseline-grandfather convention for financial-reports/ tool files), zero code touched, tool/scheduler counts unchanged | HEALTHY.

## 2026-08-22 — FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY-THAT-CANNOT-PRODUCE-ITS-OWN-VALUE (Review-Lane SECONDARY-Drain, QA CHANGES_REQUESTED redispatch, direct-commit row) → review[]

**Session:** 02594cce-7946-4d62-8d2f-586b9b883695 (router-held). Direct-commit row (branch:null) — QA ran a Direct-Commit Verify on commit 7ac55adc8 and returned CHANGES_REQUESTED (AC-1 met, AC-2 not met). No task branch existed, so not routed through fixer.

**Root cause of the AC-2 gap:** `matchesVnmVeaSignature` in `confidenceFinancialReasonBuilder.ts` gated the VNM/VEA OCR-corruption hint on rule-membership alone (`violations.some(rule in {VAL-01,VAL-03,VAL-10})`). BCTC-VAL-01/VAL-10 hard-fail and always return exactly 1 violation (confidence always lands on 0.0), but BCTC-VAL-03 is a stacking SOFT violation — it can co-fire with BCTC-VAL-05/VAL-06/VAL-01-SCALE/VAL-01-POSITION, landing confidence on 0.4 or 0.6, values the VNM/VEA rule family (0.0/0.8 only) can never actually produce. QA reproduced live: `{totalAssets:1000, totalEquity:500, totalLiabilities:400, operatingMargin:2.0, netRevenue:-1}` → VAL-03+VAL-05 stack → confidence=0.6, hint still attached.

**Fix:** re-gated `matchesVnmVeaSignature` on `violations.length===1` (equivalently `confidence===0.0||confidence===0.8`) AND rule-membership — the rule must fire ALONE, not merely be present among stacked violations. Corrected the misleading module docstring (previously claimed "a lone VAL-03 soft violation yields exactly 0.8 ... falls out of the function's own structure" as if no separate gate were needed — false, per the task's own AC-3 test which already stacks 3 soft violations to reach 0.4).

**Tests added (2, matching QA's exact ask):** (1) QA's own live repro verbatim — VAL-03+VAL-05 stack, confidence=0.6, hint absent; (2) a second stack — VAL-03+VAL-01-SCALE, confidence=0.4, hint absent. Both assert the hint text is NOT present while the individual rule names still appear (AC-1 unaffected).

**Verified:** targeted suite `FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY.test.ts` 11/11 pass (9 pre-existing + 2 new), 40 expect() calls. `tsc --noEmit` clean. Server boot (`PORT=3099`) healthy, `/health` returned `toolCount=183` matching baseline. `gen-project-stats.ts --dry-run`: toolCount=183/cronJobCount=88 unchanged (domain-logic-only change, no barrel touched). Full non-isolated `bun test`: 15307 pass/40 skip/50-51 fail across 2 runs/48331 expect() (~514s) — zero fail-list matches for bctc/financial/1345b/VAL-01/VAL-03/confidenceFinancial; every failure is a pre-existing unrelated class (task_heartbeat Zod schema, VPS proxy health, get_insider_transactions, get_market_cap, get_backtest_runs, etc.) already present before this change.

**Doc-review:** skipped — no `docs/architecture/microservice/mcp-server/` doc currently references `confidenceFinancialReasonBuilder`/`matchesVnmVeaSignature`/BCTC-VAL-NN (checked via grep before touching anything); the original 7ac55adc8 commit that introduced the function also shipped without a doc entry. No new domain concept introduced by this narrow gating fix — nothing to update incrementally.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S10. Commit: `1854156a6`. Board: `orch-state.json` `.task_board.review[]` row updated (status stays REVIEW pending fresh QA pass, `redispatch_count` context preserved, `updated_by`/`updated_at`/`status_note` refreshed to record the fix).

Zone health: QA's exact AC-2 gap closed with the smallest correct fix (length-check, no new abstraction), 2 regression tests reproduce QA's own live finding plus a second stack shape, zero regression across targeted + full-suite runs, tsc clean, tool/scheduler counts unchanged | HEALTHY.
