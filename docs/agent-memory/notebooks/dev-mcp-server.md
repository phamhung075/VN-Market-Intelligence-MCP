# dev-mcp-server -- Notebook

## 2026-07-28 — FIX-POLLNEWS-COUNTER-CONSERVATION (router incident dispatch, news-ingestion-stall) → diagnosis + safe fix, no board row

**Session:** e3a3f331-f086-4c43-bd81-f40de22ef599. Router brief claimed a 6.5h news-ingestion stall (last row 07:15:02Z) plus an unreconciled fetched/duplicates/inserted arithmetic gap. Investigated both; neither is what it looked like.

**Arithmetic gap — root-caused, fixed:** the VN-relevance pre-filter (Task 1247, `isVnRelevant`) runs before the dedup loop in `pollNews.ts`; `fetched=allItems.length` but `inserted`/`duplicates` only sum over post-filter `relevantItems`. The discarded count was `logger.debug`-only, never in `PollNewsResult` or the info-level "cycle complete" line — not a data-loss bug (loop invariant: every relevant item is inserted XOR duplicate, verified by code read), but the counters genuinely didn't conserve visibly. Added `irrelevant` field (optional-typed to avoid a 14-file test-mock blast radius) to `PollNewsResult`, wired into both `[pollNews] cycle complete` and `[push-news] pipeline complete` logs.

**"6.5h stall" — FALSE POSITIVE, not fixed here (out of zone):** direct docker-exec `bun:sqlite` readonly probe against the live container proved `rag_analyses` (the real news-ingestion table) had 126 rows in the trailing 3h and a live MAX(created_at) ~2 min before the signal fired — news never stopped. Traced the "07:15:02Z" figure to an EXACT literal match against `market_messages.sent_at` (`2026-07-28 07:15:02`) — the outbound MARKET-briefing-post log, unrelated to RSS ingestion — which is system-auditor's own documented C-06 probe (`docs/agents/system-auditor/flow/main.md:589`, `SELECT count(*) FROM market_messages...`), here mislabeled "news". Corroborates 3 existing BACKLOG rows already tracking market_messages-freshness false positives (`FIX-AUDITOR-C06-OFFMARKET-RECALIBRATE`, `FIX-AUDITOR-C12-READONLY-BLINDED-AND-TABLENAME`, `FIX-MARKET-MESSAGES-TIMESTAMP-FORMAT`) — grepped the board first, did not mint a 4th row (rediscovery = corroboration). The dead reuters/bloomberg/heartbeat re-push duplicate churn is separately covered by `FIX-NEWS-REUTERS-DEAD-URL-DECOMMISSION` (not conflated).

Evidence: new `FIX-POLLNEWS-COUNTER-CONSERVATION.test.ts` (3 tests/16 expect, RED→GREEN). tsc clean. Targeted: every file constructing/consuming `PollNewsResult` (16 files) 130/130 pass. Full `bun test`: 14825 pass/40 skip/55 fail/1 error — fail count matches this repo's own 3-day-old documented baseline (55/50 fail across two runs, 2026-07-25), zero net-new. `gen-project-stats --dry-run`: toolCount 184/cronJobCount 88 unchanged. Commit `2945d4fcb`. No board row existed for this (grepped first) — router dispatch was direct, not board-tracked; `.head` left idle throughout per dispatch instruction. `rebuild_required:true` — additive/log-only change, needs mcp-server rebuild+restart to take live effect (user-gated, not performed).

Zone health: tsc clean, 130/130 targeted green, full-suite zero net-new fail, tool/cron counts unaffected, root-cause + false-positive both verified live (not self-report), no fabricated fix, wholly in-zone | HEALTHY.

## 2026-07-28 — FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT (BOUNDED-1 auto-pickup) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. PO settled the direction (compute period server-side at the ingest boundary, gate the caller override — UC-CDC-P1 class parallel), left ONE open choice among validate-only/derive-from-content/both. Chose **both**: new pure domain module `periodContentExtractor.ts` derives (year,quarter) from repeated VAS quarter-boundary date statements in `rawText` (diacritic/OCR-corruption tolerant — verified against the REAL report `5b0dad71` PDF's mangled pdf-parse text, "Tqi ngity 31 thing 03 nbn 2026" still resolves to Q1-2026); conservative (MIN_SIGNAL_COUNT=3 + strict plurality, else `null`="cannot determine"). `parseBctcReport()` Step 0 runs `checkPeriodContentConsistency()`; a confident mismatch throws `BctcPeriodContentMismatchError` (names both periods) BEFORE any extraction/write — propagates through the EXISTING fail-loud paths (fetchParseAndStoreBctc's catch→null, pushBctcExtraction's catch→`failed`+Telegram) with zero plumbing changes, plus a dedicated debounced Telegram bug fired directly.

**AC-1 DATA (time-ordered FIRST, executed before code):** live-probed `get_bctc_report_id` state first — report `5b0dad71` (DPM/2025-Q4) `refine_status=PENDING` (finalize NOT yet armed, confirmed via 2 independent fresh DB reads). Found it's a byte-identical duplicate (md5 confirmed) of `3e2a26d9` (correctly-labelled DPM/2026-Q1) — `bctc_vps_queue` proved root cause: VPS-side (DPM,2025,Q4) backfill target fetched the wrong (newest, actually Q1-2026) SSC listing URL. New CANONICAL script `scripts/migrations/dedupe-mislabeled-bctc-period.ts` (decision-table gated: refuses on source-already-DONE/target-CONFIRMED/action_code-mismatch/same-sort_key/target-has-units/pdf-hash-mismatch) re-parented 23 `bctc_refined_units` windows onto the correct row, deleted the duplicate, reset the freed queue row to pending. Independently RAW-verified post-apply via a fresh connection (not the writer's own).

Tests: `FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT.test.ts` 16/16 (domain unit + integration: AC-2 reject, AC-3 both negative controls, AC-4 slot-recovery, AC-5 regression) + `dedupe-mislabeled-bctc-period.test.ts` 14/14. tsc clean (0 errors). Server startup healthy, toolCount=184/cronJobCount=88 both match baseline exactly. Base-vs-head A/B on the full pre-existing 70-file/670-test BCTC corpus: 635 pass/34 fail/15 errors on BOTH — zero net-new failures (the 34/15 are pre-existing, unrelated `scanDiskForStrandedPdfs`/disk-scan failures, confirmed via git-stash A/B, not touched by this change). Docs: `usecases.md` + `financial-reports.md` (new invariant #10) + flow-doc CANONICAL pointer updated.

Zone health: tsc clean, 30/30 new tests green, zero net-new regression (base-vs-head A/B verified), tool/cron counts unchanged, AC-1 DATA action live-applied + independently re-verified, time_gate respected (applied while still PENDING) | HEALTHY.

## 2026-07-28 — FIX-BDI-SHIPPING-STALE-404-GUARD (UNBLOCK dispatch, PO ci_red root-cause) → QA, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Router UNBLOCK: PO root-caused (`po_ci_red_rootcause_20260725T1736`, re-triaged 07-28) a 3+ day CI RED (fingerprint bda56d1c/1a4cbfb0, 16+ consecutive fails) to a stale assertion in `1408-tool-diacritics.test.ts` case 5a — it still asserted the literal `"ổn định"` fake-conclusion string this same row's own already-landed guard fix (`646960658`) deliberately removed from `supplyChainTools.ts` when `indices=[]`.

Verified live source first (not blind-swap, per PO instruction): `buildSupplyChainExposureOutput([],[],null)` reaches the `indices.length===0` branch and produces exactly PO's prescribed string (`"TỔNG KẾT: Không đủ dữ liệu vận tải biển hiện thời để kết luận..."`). Swapped the assertion + renamed the `it()` title; zero production-code touch — guard behaviour re-verified unchanged (stale/absent shipping input never reads as `ổn định`).

Evidence: 1408 standalone 8/8 pass (was 7/1). Targeted/merge-gate suite (1408 + the 10 precedent-listed affected supply-chain/commodity/BDI suites) 115/115 pass, 0 fail. `bun tsc --noEmit` exit 0. `toolCount=184`/`cronJobCount=88` unchanged (test-only diff; confirmed via `gen-project-stats.ts` + running container `/health`). Repo-wide plain `bun test`: 14840 pass/40 skip/56 fail/1 error — standing `FIX-MCP-SUITE-HEALTH-BASELINE` red (dev-standards.md:590 pinned reading), confirmed unrelated (diff = 1 test file, 5 insertions/4 deletions, assertion-only; unrelated failures live in `src/_deprecated/`). Commit `9374e65e0` (explicit-path, test-only). Decision journal: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md STEP dev-mcp-server-S12. Board row `task_board.review[]`→`task_board.qa[]` via `orch-apply.sh` in the same write (status QA, next_agent=qa); `.head` left untouched (already pointing at a different peer task, not this one).

Zone health: tsc clean, 115/115 targeted-suite green, zero touch to production code, root-cause independently re-verified against live source (not PO's prose alone), full-suite red confirmed pre-existing/unrelated | HEALTHY.
