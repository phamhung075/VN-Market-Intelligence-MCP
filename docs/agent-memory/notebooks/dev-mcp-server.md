# dev-mcp-server -- Notebook

## 2026-07-31 — INVESTIGATE-EMPTY-DATA-TABLES (BOUNDED-1 auto-pickup, SPIKE/low) → investigation complete, no code changed

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. SPIKE (feature-spike.md mode, no TDD, no branch — investigation-only per its own AC). Live-verified all 5 zero-row tables (`credit_data`, `insider_transactions`, `public_contracts`, `pharma_events`, `broker_sanctions`) against the real bind-mounted `market.db` (`./data/live:/app/data`, same file all 13 services — not a stale mount) via `sqlite3 -readonly` + `cron_job_runs` history, and REPRODUCED each writer's live runtime behavior with `curl` against the real VPS proxy / origin endpoints (not just a code read — the "never throws" fetcher contract makes "genuinely empty" and "silently broken" produce identical code-level symptoms).

**Verdicts:** `credit_data`=empty-by-design/orphan (Sprint 1922c already fully diagnosed+guarded, zero writer exists, re-confirmed unchanged). The other 4 are **dead-pipeline**, each root-caused: `insider_transactions` — SSC portal (`congbothongtin.ssc.gov.vn`) is Oracle ADF/WebCenter, pure JS-rendered shell (live curl today: HTTP 200, zero `<table>` markup) — regex HTML parsing structurally can't work even though upstream is no longer 503ing (contradicts the 07-29 vps-proxy-server.js comment's outage diagnosis, which is now stale). `public_contracts` — `MUASAMCONG_VPS_PROXY_URL` env var used verbatim by `getMuasamcongUrl()`, drops the `?path=` param needed to reach the results page; proxy forwards to the bare portal homepage instead (live curl confirmed: `<title>Hệ thống mạng đấu thầu quốc gia...</title>`, 0 `table-result` matches). `pharma_events` — 2 stacked defects: (a) `davPharmacyCheckJob` has fired successfully exactly once in 3 months, zero startup-catchup guard (same node-cron `Scheduler.start()` re-seed class already fixed for 6 sibling jobs this sprint — FIX-CRON-SUNDAY-STARTUP-CATCHUP×4/FIX-CRON-SSCCHECKERJOB-DEAD-87D/FIX-BASE-RATE-COMPUTATION-CRON-DEAD — just never swept for this monthly one); (b) `dav.gov.vn` hard-times-out from France (25s, live-tested) with zero VPS-proxy fallback wired, unlike its Sprint-1922-series siblings. `broker_sanctions` — `defaultFetchSanctions()` is a literal explicit `TODO` stub (`return []`, Sprint 1920d scaffolding shipped intentionally without the real scraper); target enforcement page also JS-only ADF shell (shares insider_transactions' obstacle).

**Findings doc:** `docs/spikes/SPIKE-INVESTIGATE-EMPTY-DATA-TABLES.md` — full per-table evidence + verdict table + 4 proposed follow-up FIX tasks (`FIX-PUBLIC-CONTRACTS-MUASAMCONG-PROXY-PATH`, `FIX-DAV-PHARMACY-CATCHUP-AND-VPS-PROXY`, `FIX-SSC-ADF-JS-RENDERING-INSIDER-TRANSACTIONS`, `FIX-BROKER-SANCTIONS-SSC-FETCHER-STUB`), scoped and root-caused for PO/dev-team to mint directly without re-investigating. No code changed — per SPIKE AC, dead-pipeline findings become their own follow-up tasks, not fixed inline. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S47.

Zone health: no code touched, tsc/tool-count/cron-count unaffected | N/A (investigation-only cycle).

## 2026-07-31 — FACTORY-INFRA-delete-deprecated-rag-kinhdich (BOUNDED-1 auto-pickup, P3/M) → BLOCKED, next_agent=po

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Investigated before coding (mandatory): task's DoD (barrel re-exports removed, test anchors re-pointed/deleted, zero remaining importers, 4 `_deprecated` files gone) is ALREADY fully satisfied — the source row is a stale 2026-06-15 audit-brief entry never closed out after 2 unrelated later tasks independently covered the identical files. `456851797` (2026-07-16, CI-RED-da847805-FIX) deleted `rag/_deprecated/{vectorstore,retriever,embeddings}.ts`, its 5 test anchors (011/012/013-rag-*/135-rag-temporal-decay/security-sql-injection), and the `rag/index.ts` + `infrastructure/index.ts` barrel re-exports. `448b41b0a` (2026-07-30, FACTORY-GUARD-CI-DEADCODE-IMPL) deleted `infrastructure/_deprecated/kinhDichWrapper.ts`, `1077-kinh-dich-wrapper.test.ts`, and surgically trimmed `1081-sprint-054-smoke.test.ts`.

**Verification:** repo-wide grep confirms zero real importers of `_deprecated`/`kinhDichWrapper` (only historical comments referencing the deletion commits); `find` confirms all 4 target files + the `rag/_deprecated/` dir absent from disk. No code change made — nothing to implement. Flipped BLOCKED per dispatch brief's explicit "files already gone" trigger, routed to `po` to close/retire the stale audit-brief backlog row rather than re-litigate via QA. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S48.

Zone health: no code touched, zero drift found — stale backlog-row hygiene issue only | N/A (blocked-triage cycle).

## 2026-07-31 — FACTORY-DOMAIN-extract-sla-config (BOUNDED-1 auto-pickup, P3/M) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Moved `SignalType`/`SignalSlaConfig`/`DEFAULT_SLA_CONFIG` out of `freshnessSlaChecker.ts` (966L) into a new sibling pure-data file `domain/services/freshnessSlaConfig.ts` (194L, `size-justification: ~180L` header) — mirrors the existing split-file precedent (`alertThresholds.ts`, `macroThresholds.ts`, `newsNormalizerTables.ts`). `freshnessSlaChecker.ts` (now 814L) imports the 3 names for local use and re-exports them unchanged so every existing import path (domain barrel, SLA MCP tools, the freshness monitor scheduler job, all 14 SLA test files) keeps working with zero call-site changes; behavior byte-identical (config table entries untouched, same order/values).

**Deviation from the backlog approach note:** did NOT use `packages/shared-config/slaConfig.ts` (the note's primary suggestion) — that package currently has 0 real importers repo-wide and is tracked separately for a wire-or-prune decision (`FACTORY-SHARED-wire-or-prune-shared-packages`, `docs/data/shared-package-import-baseline.json`); it also sits outside `apps/mcp-server/`, this agent's zone boundary. Used the note's own explicitly-sanctioned alternative: "(or a freshness/config.ts)".

**Fixed 1 brittle test:** `234-vps-health-sla.test.ts` AC-9 checked the exact literal substring `"export type SignalType"` — broke on the (equally valid) re-export syntax. Updated the assertion to check the new re-export line; AC-9's real intent (zero infrastructure imports, SignalType still exported) is unaffected.

**Evidence:** `bun tsc --noEmit` clean. Targeted SLA suite (14 files): 212/212 pass. `size-lint-justification.sh --check` PASS on both touched files. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged. Fresh server boot (PORT=3099): `/health` toolCount=183, 90 cron keys registered, zero import errors. Full `bun test`, 2 independent runs: 14988/14995 pass, 59/52 fail (1/0 error) — grep-confirmed ZERO SLA/freshness/SignalType/config matches in either fail list (pre-existing FIX-MCP-SUITE-HEALTH-BASELINE order-dependent red). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S49.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 3 files touched (1 new + 2 modified), size-lint clean on both | HEALTHY.
