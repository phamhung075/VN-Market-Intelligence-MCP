# dev-mcp-server -- Notebook

## 2026-07-24 — FACTORY-APP-pollNews-layering-fix (DDD pure move) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: move the `globalSourceTracker` source-health singleton out of the interface layer down into infrastructure. `eslint.config.mjs` Fence-B already named this exact violation by file ("pollNews.ts imports globalSourceTracker from interface/" — FENCE-LEGACY). Investigated at source first.

Moved the stateful singleton block (`globalSourceTracker` + seeding + `_resetGlobalSourceTracker`, globalThis-stashed for `bun --hot` reload survival) verbatim from `interface/mcp/tools/news-analysis/sourceHealthTools.ts` to new `infrastructure/observability/sourceHealthRegistry.ts` — mirrors the existing stateful-tracker precedent there (`jobMetrics.ts`, `circuitBreakerLogger.ts`). Left `formatSourceHealthTable`/`registerSourceHealthTools` (pure rendering, no state) in the interface file — genuinely an interface concern, not part of the violation. Rewired 3 production consumers (`application/usecases/pollNews.ts` — the Fence-B violation itself; `interface/mcp/tools/system/systemTools.ts`; `infrastructure/fetchers/tradingEconomicsChromium.ts`'s 2 dynamic imports, which was *also* wrongly reaching into interface/) + 6 test files (1332/fix-fetch-source-issue2/FIX-NEWS-CB-FALSE-CLOSED/1832b/1227/1898b). Removed the now-obsolete `eslint-disable` FENCE-LEGACY comment from pollNews.ts and updated the stale eslint.config.mjs comment + a stale precedent citation in getMoneyRadarComposite.ts (both named the moved file directly).

Evidence: byte-diffed the singleton block old-vs-new — identical. `bun tsc --noEmit` clean (no dangling imports to the old path — grep-confirmed zero remaining code references). Targeted 10-file source-health/pollNews suite: 239-240 pass/4 skip, one known singleton-state cross-file flake (passes standalone, fails intermittently only under batch order) reproduced identically before AND after the move — same signature, same file, proving no new regression, singleton semantics genuinely preserved (still one shared object reference across all consumers). Server boot probe (PORT=3099, port 3000 held by live docker): health 200, toolCount=184 = baseline. `gen-project-stats.ts --dry-run`: toolCount 184 / cronJobCount 88, both unaffected (pure infra relocation, zero tool/cron surface change). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-APP-pollNews-layering-fix-dev-mcp-server.md`.

Zone health: tsc clean, targeted suite flake signature identical before/after (singleton semantics preserved), tool/cron counts unaffected, Fence-B violation resolved | HEALTHY.

## 2026-07-24 — FACTORY-INFRA-agentSignal-sql-binding (BOUNDED-1 auto-pickup, security fix) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: replace string-interpolated SQL with bound placeholders in `agentSignalStore.ts` READ paths. Investigated at source first: 5 caller-supplied-value interpolation sites across `getSignals()` (hoursBack minutes, signalType equality, mark-as-read `UPDATE...IN(ids)`) and `getSignalEffectiveness()` (days offset, fromAgent/signalType equality) — all fixed. `getChainFindings`/`getChainFromRoot`/`getPriceAnomalySignals`/`getSignalsGroupedByCausalRoot`/`getOpenChainFindings` were already fully bound, no change. No structural (dynamic column/table) interpolation existed; the only structural-shape site was the id-list `IN(...)`, made safe via generated placeholders (count from array length, values bound) — precedent: coordinationStore.ts/seedWatchlist.ts. `datetime('now', ? || ' days'/' minutes')` idiom copied from cronJobRunStore.ts/agentWorkLogStore.ts.

Empirically verified (git-stash A/B probe, not assumed) which sites were genuinely exploitable pre-fix vs. already-safe: `fromAgent`/`signalType` had correct manual `.replace(/'/g,"''")` escaping (not actually broken by quotes); `hoursBack`'s `Math.ceil(x*60)` arithmetic coerced any non-numeric attacker string to `NaN` (also not exploitable). The ONE genuinely unescaped site was `getSignalEffectiveness()`'s `days` field: a crafted `days` value (`1'); DROP TABLE agent_signals;--`) corrupted the WHERE clause pre-fix (silently returned a malformed single-null-row instead of throwing or matching correctly); post-fix it's bound as an opaque parameter → clean empty result, table always intact both ways (bun:sqlite's `prepare()` only compiles the first statement, so the stacked DROP itself never ran either way — but the WHERE-clause corruption was the live footprint).

Evidence: `bun tsc --noEmit` clean. New file `src/__tests__/FACTORY-INFRA-agentSignal-sql-binding.test.ts` (9 tests, incl. the days-field differential proof): 9/9 pass. Full 26-file agentSignalStore-dependent regression (277 tests): 277/277 pass, identical to pre-fix baseline. Full `bun test`: 14733 pass/40 skip/44 fail/2 errors — grep-confirmed zero `(fail)` lines mention agentSignalStore/agentSignal (all 44 are pre-existing VPS-push/insider-transactions/market-cap/foreign-flow 5000ms-timeout flake, same category as prior sessions' documented pre-existing classes; fail-count fluctuated 42→44 across 2 runs, confirming flaky not deterministic). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-INFRA-agentSignal-sql-binding-dev-mcp-server.md`.

Zone health: tsc clean, targeted 277/277 + 9/9 new security tests pass 0 fail, full-suite pre-existing flake confirmed zero overlap via grep across 2 runs, no tool/cron surface touched (pure infra hardening) | HEALTHY.

## 2026-07-24 — FACTORY-INFRA-split-ssc-fetchers (BOUNDED-1 auto-pickup, pure structural split) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: split `ssc.ts` (1029L — SSC/HOSE/HNX/UPCOM disclosure fetchers bundled together) into one module per portal. Explicit no-dedup-delete scope note honored.

Split: `sscCommon.ts` (SscDocument/HttpClient types, makeDefaultHttpClient, titleMatchesReportType — shared plumbing), `sscPortal.ts` (SSC congbothongtin.ssc.gov.vn: buildSscSearchUrl/parseSscHtml/withBrowserLock/`_runSscPath`), `hoseDisclosure.ts`, `hnxDisclosure.ts`, `upcomDisclosure.ts` (named `*Disclosure.ts`, not `hose.ts`/`hnx.ts`, to avoid colliding with the existing unrelated market-price fetchers of those names already in `infrastructure/fetchers/`). `ssc.ts` (1029L→186L) kept to `listSscDocuments`/`listSscDocumentsWithFlag` orchestration, re-exporting the full pre-split public surface (types + all per-portal fetch/parse functions) so every existing `from "ssc.js"` call site (7 production + 16 test files) is unaffected — mirrors this session's newsNormalizer split precedent. Note: the canonical backlog ticket for this task specifies a `disclosures/` subfolder + `<=120L`-per-file cap; followed the dispatcher's explicit newsNormalizer-precedent instruction instead (flat siblings, no cap — `sscPortal.ts` lands at 388L) — flagged in the decision journal for QA visibility.

Evidence: code-only diff (comments/imports/type-exports stripped, statement sets sorted) shows the ONLY delta vs. original is 3 previously-private functions (`_runSscPath`, `makeDefaultHttpClient`, `titleMatchesReportType`) gaining `export` (needed cross-module) + 5 re-export barrel lines — zero logic drift. `bun tsc --noEmit` clean. Targeted 9-file ssc/disclosure suite: 85/85 pass, identical before/after. Full `bun test`: 14734 pass/40 skip/43 fail/46780 expect() calls — grep-confirmed zero fail-line overlap with ssc/sscPortal/sscCommon/hoseDisclosure/hnxDisclosure/upcomDisclosure; all 43 match today's already-documented pre-existing flaky classes (vps_push_log schema-drift, 5000ms MCP-client-timeout, deprecated 1302-technical-indicators), within the 41-44 range logged twice already today as flaky-not-deterministic. `gen-project-stats.ts --dry-run`: toolCount 184/cronJobCount 88 unaffected. Server boot probe (PORT=3099): health 200 toolCount=184, both dashboards 200. Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-INFRA-split-ssc-fetchers-dev-mcp-server.md`.

Zone health: tsc clean, targeted 85/85 identical before/after, full-suite pre-existing flake confirmed zero overlap (43 fails, grep-verified), tool/cron counts unaffected, server boot clean | HEALTHY.
