# dev-mcp-server -- Notebook

## 2026-07-23 — FIX-PREDICTION-SIGNALS-EMPTY (router dispatch, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: `prediction_signals` frozen at 6 rows since 2026-04-27 (87d) despite `predictionMarketPollJob` reporting status=success every ~30min cycle. Finding: REAL fix needed. RAW-verified live (docker exec against production `market.db` + container logs, not the checklist): ruled out DS-DEGRADE-01-class table-gap explicitly (`schema.ts` imports+calls `initMacroTables()` from `schema-macro.ts` inside `initDatabase()`; live DB has both tables, correct columns) and read/write table mismatch (both target `prediction_signals`/`prediction_markets` consistently). Actual cause: `fetchPolymarkets()` is a documented "never throws" fetcher — swallows CLOB (403 geo-block, known) AND Gamma failures internally, resolving `[]`. Live probe (`curl -v` from inside `vn-market-intelligence-mcp-mcp-server-1`) proved `gamma-api.polymarket.com` resolves to 145.239.225.117 serving cert `CN=*.anj.fr` (network-level block, `ERR_TLS_CERT_ALTNAME_INVALID` every cycle since ≥2026-06-30). Because the fetch never throws, `runPredictionMarketPoll()`'s Step 3 catch-fallback never fires, and the OLD Step 5 early-return (`currentMarkets.length===0`) sat BEFORE Step 5b's staleness/Telegram-alert guard — making that guard structurally unreachable for the whole outage (`cron_job_runs` confirms status=success/rows_written=null every cycle). Same DS-DEGRADE-01/DS-OBS-01 "success path disconnected from output" class.

Fix: reordered `runPredictionMarketPoll()` (`predictionMarketJob.ts`) so the staleness guard runs unconditionally BEFORE the empty-markets early return — no change to the TLS/geo-block network condition itself (external, outside apps/mcp-server ownership). New test `FIX-PREDICTION-SIGNALS-EMPTY-staleness-guard-unreachable.test.ts` (3 TCs, run through REAL `initDatabase()`/`getDb()`, DS-DEGRADE-01-FIX precedent, not an isolated fixture): stale+empty-fetch→alert fires (was silent), fresh+empty-fetch→no regression, never-populated+empty-fetch→alert fires immediately. 3/3 green. Full prediction-suite regression (167/1209/1345c/172/248/169/1354/1318): 83/83 pass. Full `bun test`: 14698 pass/42 fail — all 42 pre-existing confirmed via `git stash` A/B, zero touch prediction-market code. `bun tsc --noEmit` clean. Server boot probe clean (toolCount=184, 90 CRONS keys). Tool count 184 / cronJobCount 88 unaffected. Secondary finding (not fixed, out of scope): 3/6 `prediction_signals` rows are `t163-*` test fixtures leaked into production DB — flagged for follow-up, not this task's mandate. Decision journal: `docs/agent-memory/decisions/sprint-FIX-PREDICTION-SIGNALS-EMPTY-dev-mcp-server.md`.

Zone health: tsc clean, 3/3 new + 83/83 adjacent sibling tests green, full-suite baseline unaffected (42 pre-existing fails confirmed via git-stash A/B), tool/cron counts unaffected | HEALTHY.

## 2026-07-23 — FIX-COMMODITY-WTI-DELTA-CORRUPT (BOUNDED-1 auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: I10 `wti_crude_usd` stuck at $95.5 (reported vs live Brent $87.33), I8 `get_macro_snapshot` oil/gold/usdVnd deltas null, I4 BDI 67d+ stale. Finding: I10 REAL fix needed, I8 NO_CHANGE_NEEDED (already fixed elsewhere), I4 confirmed out-of-scope. RAW-verified live named-volume `market.db`: `wti_crude_usd` has 79 rows, ALL value=95.5, latest `extracted_at`=2026-04-12 (102d+ stale) — it has NO live fetcher (`commodityTracker.ts` news-regex only, comment confirms), so once TE stopped mentioning WTI the value froze forever. Two consumers read it with zero staleness gate: `kinhDichTools.computeMacroScore()` (Hao 6 oil-regime z-score) and `assembleBriefing.ts` step 9 → morning Telegram briefing. Smoking gun: `DSI-MACRO-PHANTOM-STALE-GUARD.test.ts`'s own header comment already claimed "listTrackedIndicators() adds isStale flag so consumers (assembleBriefing step 9) can surface honest staleness" — but the actual `assembleBriefing.ts` code still called the UNGUARDED `listTrackedIndicators()` (no isStale) the whole time; the promised wiring was never delivered. I8: live HTTP probe of `apps/macro-indicators` port 5004 `/snapshot` (Go service, separate zone/app — out of my zone regardless) showed `oilUsdDelta:3.55`/`goldUsdDelta:-75.1` as real signed floats (commit `e55805aa3`, deployed 2026-07-15); `usdVndDelta:null` is the documented Q2 SBV-override same-source-only honesty suppression, not a bug — NO_CHANGE_NEEDED, zero Go edits. I4: live BDI last row 2026-04-07 (107d stale, `^BDI` Yahoo still 404) — already tracked as its own backlog row `FIX-BDI-SHIPPING-STALE-404-GUARD` (owner dev-mcp-server, status BACKLOG); confirmed genuinely out of scope, flagged not fixed.

Fix (I10 only, reused existing `TRACKED_INDICATOR_STALE_MS` 4h guard + `listTrackedIndicatorsFromDb`, no new threshold invented): (1) `kinhDichTools.computeMacroScore()` — SELECT now includes `extracted_at`; an indicator whose latest row is ≥4h stale is excluded entirely from the composite z-score (degrades to zero-influence, never fabricates a z-score against a frozen number). (2) `assembleBriefing.ts` step 9 — switched `listTrackedIndicators()` → `listTrackedIndicatorsFromDb(db)`, threading `isStale` into `trackedCommodities[]`. (3) `morningBriefingJob.ts` `formatCommoditiesSection` — appends `" [STALE]"` when `isStale:true` (reuses `buildWatchlistSection`'s existing `[STALE]` convention) — never silently serves a frozen value as current, never silently drops it either.

New/extended tests, all through the REAL `getDb()`/`initDatabase()` path (DS-DEGRADE-01-FIX precedent, not an isolated fixture): `298-macro-score-fix.test.ts` +2 (stale extreme wti contributes ZERO influence vs WTI-absent baseline; fresh falling wti DOES influence — proves guard is staleness-specific not a blanket WTI blackout) — 13/13 green. `DSI-MACRO-PHANTOM-STALE-GUARD.test.ts` +4 (GUARD-7/7b: real `assembleBriefing()` end-to-end threads isStale true/false; GUARD-8/8b: formatter renders/omits `[STALE]`) — 10/10 green. Full touched-module regression (43 files incl. all kinhdich/briefing/commodity suites): 438/438 pass, 0 fail. `bun tsc --noEmit` clean. Full `bun test`: 14705 pass/41 fail — confirmed via `git stash` A/B on all 5 changed files (identical 1-fail count before/after, an unrelated pre-existing cron-expression test-order flake) that none of the 41 touch my scope; targeted 43-file batch already proved 0/438 fail directly. Server boot probe clean (toolCount=184, 88 cron keys, both dashboards 200). Tool/cron counts unaffected (job-internals-only change). Decision journal: `docs/agent-memory/decisions/sprint-FIX-COMMODITY-WTI-DELTA-CORRUPT-dev-mcp-server.md`.

Zone health: tsc clean, 6/6 new + 438/438 touched-module regression green, full-suite baseline unaffected (41 pre-existing fails confirmed via git-stash A/B), tool/cron counts unaffected | HEALTHY.

## 2026-07-23 — FIX-BASE-RATE-COMPUTATION-CRON-DEAD (BOUNDED-1 auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: `baseRateComputationJob` reported ~20d stale (T3-ARCH-CRON-WATCHDOG live alert, created 06-14 when the job was still weekly). Finding: PARTIAL — 2 real fixes + 1 dependency flag. RAW-verified live (docker exec, production `market.db`): the job IS registered (daily `7 19 * * *` since the 07-01 TASK-EVIDENCE-HOP1-MCP cadence fix), fires daily, 100% success (18/18 runs in 30d) — but `evidence_likelihood_ratios` (its only output table) hasn't gotten a real content update since **2026-06-28** (25d), because `triplesUpserted=0` every run since — HONEST zero, not fabricated: its sole input `evidence_fragments` shows a real (rows_written=2) success on 06-28, then ZERO rows of any kind until 07-19 (a ~21d silent gap), matching the documented ~19-day container restart-storm already called out in `startScheduler.ts`'s FIX-CRON-SUNDAY-STARTUP-CATCHUP comment (`mcpServerStartup` recorded 28 boots across 06-28..07-19). `evidence_fragments` has since recovered (239 rows, earliest 07-19, fresh through 07-23) but isn't yet ≥5d old, so today's 0-output is still correct/expected — will self-heal ~07-24/25. Root cause of the upstream gap itself: already tracked under still-open `FIX-EVIDENCE-PIPELINE-STARVED`, flagged not chased (out of this S-task's scope).

Found 2 GENUINE reachable bugs during this dig: (1) `runBaseRateComputationWithDb` (`startupHelpers.ts`) wrapped `recordJobRun` around a default `fn` that called the ALREADY-self-recording `runBaseRateComputationJob()` — the exact double-wrap anti-pattern already diagnosed+fixed for `runEvidenceAccumulatorWithDb` but never applied here. Live proof: 2-3 `cron_job_runs` rows per single daily tick every day sampled (137899-151743, 07-19..23) — 1-2 noisy `rows_written=NULL` rows alongside the 1 real row (correctness wasn't broken — the T4 dedup guard already no-op'd the 2nd physical node-cron tick — but observability was). (2) `baseRateComputationJob` had NO startup-catchup probe, unlike its 5 siblings (morningBriefing/eveningSummary/franceSummary/alertDigest/summaryJob:daily/dataAuditJob:weekly) already hardened against exactly this restart-timing class — its own history (real success 06-28, then total silence to 07-19) is a textbook instance.

Fix: (1) `runBaseRateComputationWithDb` default fn now calls the core `runBaseRateComputation(db)` directly (single recordJobRun), with the T4 `shouldSkipRecoveryReplay` dedup guard replicated INTO the wrapper (was previously invisible to a 2nd physical tick since schedulerJobTable.ts calls the wrapper, not the inner Job()). (2) Added a 6th startup-catchup probe in `startScheduler.ts` (`shouldRunCatchup(db,'baseRateComputationJob',19,7,new Date(),false)` → `runBaseRateComputationWithDb(db)` directly, NOT double-wrapped via `jobRunRepo.wrapRun`). New test `FIX-BASE-RATE-COMPUTATION-CRON-DEAD.test.ts` (11 cases, T1/T2 single-row proof, T3/T4 dedup, T5 cross-window boundary, T6 single-wrap end-to-end through real core logic, T7-T10 `shouldRunCatchup` window semantics, T11 source-contract) — all green. Mid-task discovery: `FACTORY-SCHEDULER-job-table-registry.test.ts`'s `mock.module("../scheduler/startupHelpers.js",...)` never restores (no `mock.restore()`), silently stubbing `shouldRunCatchup`/`run*WithDb` for any file sharing the same `bun test` process afterward — fixed my OWN test's exposure using the ALREADY-established in-repo workaround (034-telegram-notifier.test.ts / 1298b-imf-infra.test.ts precedent: `?isolate=` query-busted dynamic `import()` bypasses Bun's module-mock cache) rather than editing the other task's shipped test file. RED→GREEN proven via `git stash` A/B on the 2 touched source files (T3/T4/T6/T11 flip fail→pass). Targeted regression (25 scheduler/catchup/cron-health files incl. FACTORY-SCHEDULER + EVIDENCE-ACCUM-SILENT-CRON + both prior catchup precedents): 255/255 pass. `bun tsc --noEmit` clean. Full `bun test`: 14715 pass/40 skip/42 fail — `git stash` A/B on both touched files confirms all 42 pre-existing (vps_push_log/logVpsPush schema-drift + 5000ms MCP-client-timeout class, zero overlap). Server boot probe clean (toolCount=184, health 200, both dashboards `/api/bctc-inspect` + `/dashboards/news-fetch/` 200). Tool count 184 / cronJobCount 88 unaffected (job-internals + 1 new startup-catchup entry inside the existing composition root, no new tool/cron surface). Decision journal: `docs/agent-memory/decisions/sprint-FIX-BASE-RATE-COMPUTATION-CRON-DEAD-dev-mcp-server.md`.

Zone health: tsc clean, 11/11 new + 255/255 targeted regression green, full-suite baseline unaffected (42 pre-existing fails confirmed via git-stash A/B), tool/cron counts unaffected; also surfaced+worked around a latent process-global test-isolation defect in FACTORY-SCHEDULER-job-table-registry.test.ts (unrestored mock.module) — flagged here, not fixed at source (out of zone-task scope) | HEALTHY.
