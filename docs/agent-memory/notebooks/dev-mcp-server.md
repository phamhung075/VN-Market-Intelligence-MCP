# dev-mcp-server -- Notebook

## 2026-07-29 — FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION (BOUNDED-1 idle-pickup, P2) → REVIEW, next_agent=ops (rebuild_required)

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. RAW-verified LIVE (docker exec bun:sqlite, named-volume market.db) BEFORE any code change: the 2026-06-25 finding (102 all-time / 43 active-24h identical dup-groups) no longer holds — total=202 rows, 0 dup-groups both all-time and active-24h. Not a false alarm: the rows behind the finding had simply expired via TTL + `cleanExpired()` GC in the intervening month. Confirmed the underlying MECHANISM is still live via `cron_job_runs`: 41 distinct job names multi-fire within the same scheduled minute over 7d (2702 job/minute pairs) — far broader than the sibling `FIX-SCHEDULER-DOUBLE-REGISTRATION`'s originally-scoped 2 jobs — including `freshnessSlaMonitorJob` (10x) and `intelligenceCycleJob` (35x), both `agent_signals` writers.

**Layer 1 (data-layer backstop, mandatory per PO regardless of current 0-count):** partial `CREATE UNIQUE INDEX idx_agent_signals_dedup_identical` (`schema-news.ts`) keyed on `(from_agent, signal_type, COALESCE(stock_code,''), payload, minute-bucket)` `WHERE payload != '{}'` + `INSERT OR IGNORE` on all 10 INSERT variants in `postSignal()`/`_postSignalInner` (`agentSignalStore.ts`), returning -1 (existing suppression sentinel) via new `resolveInsertId()`. The `WHERE payload != '{}'` exclusion is a deliberate, RAW-evidence-backed deviation from the literal mandate text: `payload='{}'` is used EXCLUSIVELY (RAW-confirmed live) by `alertStore.ts`'s `verified_decision` correlation stubs (`from_agent='alert-engine'`), which already have a precise `alert_id`-scoped dedup guard — applying the coarser minute+payload key there too would risk silently dropping a legitimate 2nd correlation stub when 2 genuinely-different alerts (e.g. TA + BB) fire on the same stock in the same minute.

**Layer 2 (per-emitter root cause, in-zone only):** `freshness-sla-monitor` is the ONLY of the 5 named emitters that's a genuine mcp-server-scheduler job (the other 4 — system-auditor/news-scout/bctc-analyst/alert-engine-as-external-caller — post via the generic `post_agent_signal` MCP tool from cowork-agent sessions, out of zone). Its payload always embeds a fresh millisecond timestamp (present since file creation) — structurally cannot byte-collide even under re-entrancy; does not reproduce this defect class. `alert-engine` is NOT one cowork agent — it's the `from_agent` tag written by 8 different mcp-server scheduler call sites via `alertStore.ts`'s co-write. 7/8 already use deterministic alert ids; `scanMarket.ts` (via `alertGenerator.ts`'s `chooseAlertId` "otherwise" fallback) used a random id with NO fingerprint — `computeAlertFingerprint` existed in `alertDedup.ts` since Task 131 with ZERO production callers (dead/unwired). This is the most plausible root cause of the historical DPM 0.9s-apart genuine-duplicate sample cited in the task. Fixed: new `computeGenericAlertFingerprint()` (minute-bucket, content-keyed, deliberately narrower than `computeScanAlertFingerprint`'s day-bucket so a genuinely-recurring later event is never suppressed) wired into `generateAlerts()` unconditionally, feeding the existing `alerts.fingerprint` UNIQUE gate.

**VERIFY-DON'T-CLAIM:** traced `verified_decision` (from_agent=alert-engine) consumers — write-only correlation stub, payload always `{}`, consumed only by the C-08 orphan-audit check. The real Telegram/MARKET dispatch is driven directly from the `alerts` table by `alert-commander` (a separate `verified_decision` emission path per its own init.md, broadcast AFTER firing/suppressing). No double-Telegram-alert risk confirmed — held at P2, not escalated.

**Evidence:** new `FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION.test.ts` 10/10 pass (data-layer AC-1..6 + alertGenerator root-cause AC-7..10, incl. end-to-end `storeAlerts()` round-trip proving a simulated re-entrant double-fire collapses to exactly 1 `alerts` row + 1 correlation stub). Targeted suites (agentSignalStore/alertGenerator/alertStore/alertDedup + adjacent — 10 files) 114/114 pass. tsc clean. `gen-project-stats --dry-run`: toolCount=184/cronJobCount=88 unchanged (no new tool/cron). Live boot (fresh `:memory:`, PORT=3098): health 200 toolCount=184, `/api/bctc-inspect`+`/dashboards/news-fetch/` 200. Full `bun test`: 14886 pass/40 skip/54 fail/1238 files (570s) — matches the standing `FIX-MCP-SUITE-HEALTH-BASELINE` band exactly (identical 54-fail count to today's `FIX-CONVICTION-HISTORY-EOD-BACKFILL` sibling); zero failures reference agent_signals/agentSignalStore/alertGenerator/alertDedup/schema-news/verified_decision/dedup_identical. New CANONICAL replay script `scripts/audits/check-agent-signals-dup.ts` (RAW-verified live post-write: 211 rows, still 0 dup-groups). Doc-review: `alerts.md` (2 new invariants) + `infrastructure.md` (new `agent_signals` section + `alerts.fingerprint` column correction).

**Docker Microservice Code-Change Close Gate applies** — held REVIEW/next_agent=ops/rebuild_required=true, NOT self-closed (matches this session's `FIX-SCHEDULER-DOUBLE-REGISTRATION` precedent). Chain: ops (rebuild+swap) → qa (LIVE gate: `bun scripts/audits/check-agent-signals-dup.ts` against the named-volume DB returns 0 dup-groups across 2 fetch cycles post-deploy).

Zone health: tsc clean, 114/114 targeted-suite + 10/10 new-file green, tool/cron counts unchanged, full-suite fail-set keyword-scanned clean, RAW-verified the historical finding no longer holds AND confirmed the underlying mechanism (scheduler re-entrancy) is still live so the backstop is not moot, per-emitter root cause fixed at the one in-zone path that lacked protection | HEALTHY.

## 2026-07-29 — TASK-DEVTEAM-IDLE-CHAIN-1-SCHEMA-UTILITIES (BOUNDED-1 idle-pickup, P-high) → REVIEW

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Task 1 of 5 (pm decomposition, commit `6617edbd4`) of architect brief `2026-07-25-devteam-idle-chain-rotation-durable-inbox.md` — redesigns dev-team's idle-tick dispatch chain from fixed-priority (BOUNDED-1 always wins if eligible) to a 5-consumer aged round-robin. This task ships only the schema+utility layer; tasks 2-5 (main.md rotation-dispatch wiring, drain-signals.md durable-inbox ordering) are NOT dispatched yet, and per the row's own PLAN-ONLY constraint I did not touch either flow doc.

**Shipped (all 3 tightly-coupled per row note):** `dev_team_idle_chain: z.record(z.unknown()).optional()` on `OrchStateSchema` root (`orchStateSchema.ts`, alongside `narrative`/`dashboard_section_cache`/`session_handoff_status` precedent); `rotation_selected($doc)` in `scripts/lib/devteam-eligibility.jq` (verbatim architect-brief §2.2 algorithm — oldest `last_served_tick` wins, missing/null defaults to epoch, fixed-order tiebreak); new `scripts/devteam-idle-chain-stamp.jq` per-tick stamp writer (self-healing on bootstrap via jq's auto-create-intermediate-object `=` semantics; guards `$c` against the fixed 5-id set, refuses to write a garbage rotation key on an unrecognized id).

**Evidence:** tsc clean. Direct Zod `safeParse` probes (with/without/loose-shape `dev_team_idle_chain`) all PASS; `bun scripts/orch-validate.mjs` Stage 0+1 PASS on live orch-state.json post-schema-change. `orchStateSchema.test.ts` 104/104 pass. jq scenario probes: bootstrap-absent-key defaults to fixed order, all-null tie-break, mid-rotation next-oldest-wins, and a simulated 5-tick cycle selects each of the 5 ids exactly once. Stamp-writer probes: bootstrap self-heal, existing-doc partial update (other consumers untouched), unrecognized-id no-op, standalone `jq -n -f` valid-JSON output. `toolCount=184`/`cronJobCount=88` unchanged (schema-only, no new tool/cron). Live boot (`DB_PATH=:memory:`): health 200, `/api/bctc-inspect`+`/dashboards/news-fetch/` 200. Full `bun test`: 14885 pass/40 skip/55 fail/1238 files (564s) — within the standing `FIX-MCP-SUITE-HEALTH-BASELINE` band (dev-standards.md:610); confirmed zero overlap with orchStateSchema/dev_team_idle_chain/devteam-eligibility (fails are vnstock stores/VPS-proxy/news-polling/insider-tx/backtest-runs/deprecated-1302, all pre-existing). Doc-review: NONE (infrastructure.md does not separately enumerate OrchStateSchema root keys — matches the narrative/dashboard_section_cache/session_handoff_status precedent).

Zone health: tsc clean, orchStateSchema.test.ts 104/104, jq utilities verified via jq -n scenario probes (bootstrap/tie-break/rotation/full-cycle), tool/cron counts unchanged, full-suite fail-set confirmed disjoint from this diff's 3-file scope | HEALTHY.

## 2026-07-29 — FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE (BOUNDED-1 idle-pickup, P2) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. RAW-verified LIVE (docker exec bun, market.db): 52/52 current `verified_decision`/`alert-engine` rows `payload='{}'` (historical dbsweep 559/559 over 12+ days) — sole writer confirmed `alertStore.ts`, which hardcoded `payload='{}'` as a bare SQL LITERAL (never bound) in the `agent_signals` co-write inside `storeAlerts()`/`storeAlertsFromCommander()`. `stock_code` was already correct (only 1/52 null, `MACRO`-scoped by design, unchanged since the 06-13 first commit) — the historical "null stock_code" sub-finding did not reproduce, downgraded per VERIFY-DON'T-CLAIM rather than "fixed" with a no-op change.

**Fix:** new `buildVerifiedDecisionPayload()` builds real content (`alert_id`/`alert_type`/`title`/`severity`/`confidence`/`detected_at`) from the `Alert`'s own required fields instead of the literal. `alert_id` deliberately embedded: discovered the SAME-DAY sibling `idx_agent_signals_dedup_identical` partial UNIQUE index (`FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION`, `WHERE payload != '{}'`) explicitly relied on alert-engine rows staying `'{}'` to avoid silently dropping a 2nd correlation stub when 2 different alert types fire on the same ticker/minute — populating payload without a per-alert-unique key would have reintroduced exactly that class the moment payload stopped being `'{}'`. Emit-time guard (`logger.warn` + skip-this-row, never blocks the base `alerts` row) refuses empty-content writes.

**Evidence:** new `FIX-ALERT-ENGINE-VERIFIED-DECISION-EMPTY-PAYLOAD-NULL-STOCKCODE.test.ts` 6/6 pass. Targeted alert-suite (25 files incl. FIX-ALERT-ORPHAN-CORRELATION/FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID/FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION/FIX-ALERT-ENGINE-VERIFIED-DECISION-ALERTID-UUID-MISMATCH/ta+bb scan jobs) 283/283 pass. tsc clean. `toolCount=184`/`cronJobCount=88` unchanged. Full `bun test`: 14891 pass/40 skip/55 fail/1239 files — none touch alert/signal/payload/stock_code; `git stash`-verified (4 files reverted) + isolated re-run of 3 representative fail-list files all PASS standalone — confirmed pre-existing full-suite cross-test-pollution, not a regression; stash popped back immediately. Live boot (`DB_PATH=:memory:`): health 200, toolCount=184, zero import errors. Doc-review: `infrastructure.md` updated (payload construction + stale `WHERE payload != '{}'` comment corrected, same in `schema-news.ts` + its test).

Zone health: tsc clean, 283/283 targeted-suite + 6/6 new-file green, tool/cron counts unchanged, full-suite fail-set keyword-scanned + isolation-verified clean, root cause fixed at the single write-time construction site coordinated with the same-day sibling dedup-index fix | HEALTHY.
