# dev-mcp-server -- Notebook

## 2026-08-09 — FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L (P1, S, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa, split not header

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (dev-team Step 3 direct-execute, branch:null). Zone `apps/mcp-server/` routed directly. NOT a duplicate of the already-DONE_VERIFIED `FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L` sibling entry above — that row was `coordinationStore.ts` (infrastructure/db/); this row is `coordinationTools.ts` (interface/mcp/tools/system/), a different file, 457L vs baseline=388L/upper=426L (31L overage), CI-RED on `size-lint` (run 31285673936, head 39e1e161).

**Split, not header:** all 6 registered tools (`task_claim`, `task_heartbeat`, `task_release`, `task_list_held`, `task_force_release_orphan`, `get_week_period`) were each already a self-contained `server.tool(...)` call — zero cross-tool coupling except `SERVER_SESSION_ID` (used only by `task_claim`). Extracted each verbatim (zero logic change) into its own file under `interface/mcp/tools/system/coordination/` (`taskClaimTool.ts` 119L, `taskHeartbeatTool.ts` 100L, `taskReleaseTool.ts` 77L, `taskListHeldTool.ts` 75L, `taskForceReleaseOrphanTool.ts` 80L, `weekPeriodTool.ts` 72L) — every one comfortably under the 120L size-lint threshold, so none needs a `size-justification` header. `coordinationTools.ts` is now a 54L thin entry point that still exports `registerCoordinationTools(server)` unchanged (calls all 6 sub-registrars), so `registry.ts`, the `coordination/index.ts` barrel (`export { registerCoordinationTools } from "../coordinationTools.js"`), and every direct-importer test needed zero edits. Also dropped the dead `buildWeeklyPublishMarkerKey` import (unused in the file body — confirmed via grep before deleting, not assumed) and surgically pruned the now-stale 388L `docs/data/size-lint-baseline.json` entry via a scoped `jq del` (NOT a wholesale `--update`, which would have also silently rebaselined the separate, out-of-scope, already-failing `transport.ts` offender — not this row's file, left untouched).

**Verified locally (CI confirmation is next-tick QA's job per `verification_gate: ci_green_on_subsequent_push` — not observable from this session):** `bash scripts/audits/size-lint-justification.sh --check` no longer lists `coordinationTools.ts` (sole remaining offender: pre-existing `transport.ts`, untouched, different row). `bun tsc --noEmit` clean. `npx eslint` (fence config) clean on all 7 touched/new files. Targeted suite — the 4 files that `import { registerCoordinationTools }` directly (`task-lock-coordination-tools.test.ts`, `1980-p1-final-required-flip.test.ts`, `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts`, `1982-quality-burndown-CHIJ.test.ts`) plus `FIX-DIGEST-PREDICT-ISO-WEEK-DEDUP.test.ts` (covers `get_week_period`'s domain dependency) = 96/96 pass, 0 fail.

**Evidence:** commit `1653cea0aa56e43b4a84463a3dc2772bbde15705` (code + baseline prune, pushed, pre-push hook `pnpm --filter vn-market check`/`tsc` green, no `--no-verify`). Board `task_board.backlog[]` → `task_board.review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`, `commit_sha` stamped in `dev_implementation_note`/`qa_verification_hint`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S83 (new continuation file — `-4.md` was at 35824/36000 bytes, no headroom for this entry, rolled per the dual-axis cap-check precedent). Did not touch `transport.ts` or any row/scope outside this task.

**Next for QA:** `gh run view <databaseId with headSha AFTER 1653cea0aa56e43b4a84463a3dc2772bbde15705> --json jobs -q '.jobs[]|select(.name=="size-lint")|.conclusion' == success`.

Zone health: `coordinationTools.ts` split cleanly into 6 single-tool files, verified locally (tsc+targeted-tests+eslint+size-lint script), live CI confirmation pending next tick's QA check | HEALTHY.

## 2026-08-09 — FIX-SSE-SOAK-VERIFY-DEPENDS-ON-SHARED-CONTAINER-UPTIME-3RD-RESET (P1, S, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (dev-team Step 3 direct-execute, branch:null). The already-shipped `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` fix (own entry above, 2026-08-08) had an unverifiable-by-construction soak AC: its evidence was the shared `dev-mcp-server` container's `StartedAt` (>=4h uninterrupted uptime), which ANY of 8+ peers' legitimate rebuilds resets. Reset 3x in ~24h (most recently by `550fda673`, `FIX-AGENT-SIGNALS-IDENTICAL-DUP-EMISSION`) — the row sat in `qa[]` with an empty `status_note` through all 3 resets, no durable record on the row itself.

**Fix (AC-1):** added an injectable `_now: () => number = Date.now` 8th constructor param to `SseSessionManager` (`transport.ts`), same override idiom as the pre-existing `_heartbeatIntervalMs`/`_idleTimeoutMs`/`_maxAgeMs`/`_reaperIntervalMs` — threaded through all 3 `Date.now()` call sites (`handleSse` createdAt/lastActivityAt, `handleMessage` lastActivityAt bump, `reapStaleSessions` now). New T13/T14 tests in `1862c-transport-session-eviction.test.ts` construct the manager with every real shipped default intact (max-age left `undefined` ⇒ the REAL 4h, not a shrunk override) and inject a fake clock: T13 advances it in <4h steps (bumping activity each step, proving independence from the idle clock, mirroring T10's real-timer version) then crosses the real 4h mark and asserts `mcpServer.close()` fires; T14 is a boundary negative control (4h-1s survives). Bun 1.3.13 has no built-in fake-timer/`setSystemTime` support (checked bun-types) — the injectable-clock pattern is the only mechanism available, and it sidesteps the real-timer CPU-contention race class entirely (unlike T10's shrunk-threshold approach, which still uses real `setTimeout`).

**Verified under contention, not just locally:** 1862c file 14/14 pass (45 expect), 5 clean reruns. Under manufactured 48× `yes` CPU contention (same methodology as `SPIKE_CI-PERFILE-ISOLATION-FLAKE.md`): T13/T14 clean 8/8 runs — the only failures observed were the PRE-EXISTING T9 real-timer race (already triaged as a test-harness artifact on the sibling row's `qa_step5_checkpoint_2` note, tracked separately as `FIX-CI-BUNTEST-1862C-TRANSPORT-SESSION-EVICTION`, NOT touched here — out of this row's scope). `tsc --noEmit` clean. Full suite: 15230 pass/40 skip/51 fail/48042 expect across 1269 files — consistent with the pre-existing flaky-churn baseline the sibling row already documented (~44-46 pre-existing, shifting identity per rerun); none of the 51 in files this commit touches. Gate 2: `PORT=3099` clean boot, `/health {toolCount:183,sessions:0}`; `gen-project-stats.ts --dry-run` toolCount=183/cronJobCount=88, both match baseline.

**AC-2:** stamped `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER`'s `qa[]` row `status_note` (absent through all 3 resets) with the full incident history — the 2 documented checkpoint resets (16:59:50Z→19:06:16Z by `550fda673`) plus the router's `process_gap` signal (`sys-20260808T200919-a7c2`) confirming it as the 3rd. That row's own lane/status was deliberately left UNCHANGED — this fix replaces the verification METHOD going forward; it does not itself discharge that row's live QA Step 5/6 sign-off (qa's/po's call whether the new unit-test evidence supersedes the container-soak requirement).

**Doc:** `docs/architecture/microservice/mcp-server/infrastructure.md` SSE Session Manager section gained an "Injectable clock" bullet.

**Evidence:** commits `a3d7ff35f` (code+test+doc) and `50a0fce44` (board: `FIX-SSE-SOAK-VERIFY-...` `backlog[]→review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`, `.head` untouched since not `.head.active_task_id`; `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER` `status_note` stamped). Both pushes clean, pre-push hook green, no `--no-verify`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S84. Generic lesson recorded on the row per its own instruction: an acceptance criterion whose evidence source is a SHARED mutable runtime property is unsatisfiable under a multi-writer fleet — watch for this pattern elsewhere.

Zone health: reaper's max-age branch now re-provable in milliseconds via an injected clock, immune to both container-uptime resets and the T9-class real-timer contention flake, full-suite regression consistent with pre-existing baseline | HEALTHY.
