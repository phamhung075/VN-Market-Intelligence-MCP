# dev-mcp-server -- Notebook

## 2026-07-28 — CI-RED-cdd5fa5a-FIX (PO-triage board row, P0 blocking) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause (PO already reproduced locally, re-confirmed): `emit-pressure-state.test.ts`'s "[macOS path, REAL — negative control]" test called the REAL unmocked `computeContainerVmHeadroomMb()` and unconditionally asserted `toBeNull()` — only true on a host with no `free` binary. CI runs `bun test` on `ubuntu-latest`, which HAS `free`, so the real exec returns a real number and the assertion always failed there (6 consecutive red runs, last green 3ec962067). The SOURCE (from the immediately-prior task FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY, commit 98917416a) is NOT defective — a real number on Linux is correct; only the test's expectation was host-specific.

**Fix — entirely in the test, zero production code touched.** Replaced the unconditional `toBeNull()` with a runtime presence-probe (`command -v free` via `node:child_process` execSync) that gates the strict leg: `free` present → assert not-null/finite/>=0; `free` absent → assert null. Still the REAL unmocked default execFn (no stub), so it keeps exercising the true production path. Also asserts an unconditional platform-independent invariant (never throws; result is null-or-non-negative-finite-number) ahead of the gated leg. Updated the stale file-header comment block that stated the same host assumption.

**Secondary fix (board row's own AC-3, small+safe, in-scope):** `scripts/ci-per-file-isolation.sh` captured each failed file's output to `$RESULT_DIR/${slug}.log` but `rm -rf $RESULT_DIR` destroyed it before CI ever surfaced it — CI only ever emitted `FAILEDFILE: <path>` with zero detail (why this required local reproduction). Added a block that cats each `.log` into `$GITHUB_STEP_SUMMARY` (collapsible, tail -100L) before the `rm -rf`. `bash -n` syntax-checked; a synthetic RESULT_DIR+log fixture confirmed the block emits correctly.

**Evidence:** targeted file `emit-pressure-state.test.ts` 31/31 pass on BOTH legs — real macOS host (no `free`) AND a stub `free` shim on PATH reproducing PO's exact CI-repro harness (the same canned Linux `free -m` table). tsc clean. `gen-project-stats --dry-run`: toolCount=184/cronJobCount=88 unchanged (test + CI script only, no tool/cron file touched). CI-equivalent evidence — ran the just-fixed `ci-per-file-isolation.sh` itself end-to-end: 14734 pass/40 skip/6 fail across 1230 files; `emit-pressure-state.test.ts` NOT among the 6 fails. The 5 failing files (083-tool-analysis, 1324-push-news-all-sources, 1398/1793-pollnews-cooldown, FIX-1267-ssc-circuit-breaker) grepped for zero reference to either changed file — pre-existing unrelated drift, not a regression.

**NOT pushed by this agent.** Per `dev-standards.md` PUSH-AUTONOMY-1 ("the session holding the chain mutex pushes") and this task's own explicit VERIFICATION GATE ("do not claim DONE_VERIFIED yourself; flip to REVIEW, let qa/router confirm the green run"), board flipped ready→review only. `verification_gate: ci_green_on_subsequent_push` stays open until a fresh CI run lands conclusion=success on a HEAD SHA != cdd5fa5ad344406298fdf70b59afd71050c30289 after this fix is pushed.

Zone health: tsc clean, 31/31 targeted-file green on both platform legs (entire blast radius — zero production code touched), 14734/40/6 CI-equivalent run clean of target file, tool/cron counts unchanged | HEALTHY.

## 2026-07-29 — ALPHA-S2-SUB5-WATCHDOG-STRETCH (BOUNDED-1 idle-pickup, P1, optional stretch) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae, commit `17a906143`. Added `intraday5mCompactorJob` to `schedulerWatchdogJob.ts`'s `WATCHDOG_MANIFEST` (`cadenceMs=300_000`, `thresholdMultiplier=3`, `action='self-heal'`), mirroring `ohlcv-daily-aggregator`/`reputationComputeJob`/`ta-ohlcv-backfill`. `selfHealFn` wired in `schedulerJobTable.ts`'s `liveManifest` (same composition-root DDD pattern — `jobRunRepo.wrapRun('intraday5mCompactorJob', runIntraday5mCompactor)`). `CANONICAL_WATCHDOG_JOB_NAMES` updated in lockstep for the WD-10 integrity guard.

**Follow-on I found and fixed, not asked in the AC:** `cronStatusCompute.ts`'s CN-1 tier-1 `STATIC_JOB_NAME_MAP` also needed the new pair (`intraday5mCompactor` → `intraday5mCompactorJob`) — without it the `/api/cron-status` dashboard endpoint would have permanently cached the cache-unsafe tier-2/3 fallback for this job (per the file's own R1 memoization-contract comment). Live-verified end-to-end (`PORT=3097`, cwd=project root matching prod): job row present, `job_name_db=intraday5mCompactorJob`, correctly classified.

3 pre-existing tests hardcoded a manifest-size invariant (25) that my addition legitimately bumps to 26 — confirmed RED before the fix (`FIX-CRON-WATCHDOG-COVERAGE-2026-07-22.test.ts` AC-4), fixed to 26 with an explanatory note preserving each prior task's own historical delta claim rather than rewriting it. New dedicated test file `ALPHA-S2-SUB5-WATCHDOG-STRETCH.test.ts` (4 tests: manifest shape, canonical registration, stale/healthy alert behavior at 3x/5min).

**Evidence:** targeted suites (8 files incl. `ARCH-CRON-watchdog`, `cronStatusHandler`, `cronStatusCompute`, `FIX-CRON-SSCCHECKERJOB-DEAD-87D-classfix-registry-invariant`, `ALPHA-S2-intraday-5m-compactor`) 90/90 pass. `tsc` clean. `gen-project-stats --dry-run`: toolCount=184/cronJobCount=88 unchanged (manifest-only, no new tool/cron). Full `bun test`: 14855 pass/40 skip/57 fail/1 error (608s, 1235 files) — within the standing `FIX-MCP-SUITE-HEALTH-BASELINE` band; the 2 fail details visible in the captured tail are both from unrelated `src/_deprecated/1302-technical-indicators.test.ts`. Live boot: `/health` toolCount=184, `/api/bctc-inspect`+`/dashboards/news-fetch/` 200, `/api/cron-status` 200. Doc-review: `usecases.md`+`testing.md` job/pair/test counts corrected to current live values.

Zone health: tsc clean, 90/90 targeted-suite green, tool/cron counts unchanged, cron-status dashboard resolution live-verified for the new job, no CI-blast-radius change | HEALTHY.

## 2026-07-29 — FIX-SCHEDULER-DOUBLE-REGISTRATION (BOUNDED-1 idle-pickup, P2) → REVIEW, next_agent=ops (rebuild_required)

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. RAW-verified LIVE (docker exec bun:sqlite, named-volume market.db) BEFORE any code change: the ticket's hypothesis (duplicate `registerJob`/re-entrant `startScheduler`) did NOT hold — registration was already singular + guarded (`__vnMarketSchedulerStarted`). Precisely traced the real defect through `node_modules/node-cron@3.0.3`'s `scheduler.js`: `recoverMissedExecutions:true` (Lever B, the sibling ARCH-CRON-SCHEDULER-RELIABILITY missed-fire fix — kept, not reverted) makes its `i===0||autorecover` gate always true, and its "already fired" guard compares full-ms `.getTime()` not whole-second — under ordinary ~1s timer-loop jitter the SAME scheduled second gets independently re-detected (and re-executed) by the next poll. Confirmed shared across the fleet, not the 2 named jobs: `vnIndexRefreshJob` 13.04%/7d dup-minute rate, `vpsServiceHealthJob` 10.33%/7d, `walCheckpointJob` 3.74%/7d — same pattern. `pollNewsJob` isn't a cron registration at all: its 2x/30min is `newsHeadlinesRefreshJob`'s by-design double push (Bloomberg+Reuters) to `/api/push-news`, already URL/title-deduped downstream — left untouched (deduping it would drop real news).

**Fix:** new `dedupeCronTick()` in `startupHelpers.ts` wraps every `scheduleCron()` callback (the single funnel for all ~84 registrations — grep-confirmed zero direct `cron.schedule()` bypass) with a per-registration whole-second last-fired guard; a repeat detection of an already-fired second is skipped, a genuinely new second still fires. `recoverMissedExecutions` stays enabled.

Tests: new `FIX-SCHEDULER-DOUBLE-REGISTRATION.test.ts` (8 TCs, RED→GREEN: same-second dedup incl. exact observed sub-second-offset scenario, cross-second no-suppression, manual/init passthrough, per-registration isolation, real `scheduleCron`+node-cron wiring via `task.now()`). Found + worked around (in my own file only) a pre-existing cross-file `mock.module()` leak in `FACTORY-SCHEDULER-job-table-registry.test.ts` (same documented class as 034/1298b/084/FIX-BASE-RATE-COMPUTATION-CRON-DEAD) using the established `?isolate=` dynamic-import pattern. 12 scheduler suites 160/160 pass. tsc clean. `toolCount=184`/`cronJobCount=88` unchanged. Live boot: `/health` toolCount=184, `/api/bctc-inspect`+`/dashboards/news-fetch/` 200, zero scheduler errors. Full `bun test` run TWICE post-fix: 14865/55fail then 14864/56fail/1err — both within the standing baseline band, zero failure references `startupHelpers`/`dedupeCronTick`/`scheduleCron`. Doc-review: `system.md` § Scheduler Jobs — new subsection with the corrected root-cause mechanism.

**Docker Microservice Code-Change Close Gate applies** (`apps/mcp-server/src/` baked via `COPY` in Dockerfile, confirmed by direct read) — held REVIEW/next_agent=ops/rebuild_required=true, NOT self-closed. Chain: ops (rebuild+swap) → qa (LIVE gate: exactly 1 success row/job/scheduled-minute across 2 fetch cycles for vnIndexRefreshJob+vpsServiceHealthJob; pollNewsJob's 2x/30min is EXPECTED, not a gate failure) → po sign-off.

Zone health: tsc clean, 160/160 targeted-suite green (12 files incl. the mock-leak precedent files), tool/cron counts unchanged, full-suite fail-set diffed clean of my change, root cause corrected from the ticket's stated hypothesis and documented | HEALTHY.
