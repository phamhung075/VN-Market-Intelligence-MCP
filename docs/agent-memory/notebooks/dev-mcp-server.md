# dev-mcp-server -- Notebook

## 2026-07-15 — ALPHA-S2-OMO-LIQUIDITY-CRON (dev-team dispatch, atomic task) → returned to dev-team

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (dev-team umbrella chain-mutex `task:ALPHA-S2-OMO-LIQUIDITY-CRON`; architect brief `docs/architecture-briefs/2026-07-15-alpha-s2-omo-liquidity-cron.md`, PM retasked with DoD embedded — did NOT claim/release the umbrella lock, did NOT touch `.task_board`/`.head` per explicit dispatch instruction)

New file `apps/mcp-server/src/scheduler/macro/sbvOmoLiquidityCronJob.ts` exports `runSbvOmoLiquidityCron(deps?)` — pure trigger+observe cron: `POST /liquidity-state` on macro-indicators via `macroFetch<T>()`+`getMacroBaseUrl()`+`LiquidityStateResponseSchema` (all reused verbatim from the existing `get_vn_liquidity_state` tool, zero new schema). The Go service already persists `sbv_omo_daily` server-side on every call (idempotent, `ParseOK`-gated, P0-3-OMO-CURVE); this job writes NOTHING locally — no `getDb` import anywhere in the module. Asymmetric fail-loud: `macroFetch` `ok:false` → `sendTelegramBug()`+`logger.error` every time; `omo.is_estimate===true` (single-day soft-fail) → `logger.warn` only, no BUG alert; success → `logger.info`. Registered `sbvOmoLiquidityCron: '9 9 * * *'` in `cronConfig.ts` + a `buildJobTable()` entry in `schedulerJobTable.ts` (same pattern as sibling `ALPHA-S2-FF-SUB3`/`SUB4`). No startup one-shot (today's row either exists or doesn't).

New test file `ALPHA-S2-OMO-LIQUIDITY-CRON.test.ts` (5 tests): HARD-fail/SOFT-fail/success/schema-mismatch paths via mocked `globalThis.fetch`+injected `notifyBug`, plus a source-inspection assertion (reads the job file, asserts no `getDb`/`schema.js`/`.prepare(`/`bun:sqlite` substrings) proving zero local DB coupling — chosen over a `mock.module()` DB trap after a full-suite run caught `mock-module-afterall-guard.test.ts` RED from my own file's unrestored module-scope mock (fixed before commit; source-inspection is leak-free and arguably a stronger structural guarantee). Docs/registry sync (schedulerFileCount 67→68 everywhere): `docs/data/cron-registry.json`, `docs/data/system-map.json` (mcp-server `crons[]`), new `docs/standards/cron-jobs.md` section. Bumped `1190-pipeline-watchdog.test.ts` (67→68) and `FACTORY-SCHEDULER-job-table-registry.test.ts` (Group A/B 60→61, Group D 82→83) guard counts, both dated BUMP comments.

Verified: `bun test src/__tests__/ALPHA-S2-OMO-LIQUIDITY-CRON.test.ts` → **5 pass / 0 fail**. Combined guard-suite run (own test + `mock-module-afterall-guard` + `1190-pipeline-watchdog` + `FACTORY-SCHEDULER-job-table-registry`) → **37 pass / 0 fail / 321 expect() calls**. `bun tsc --noEmit` → **exit 0**. `bun scripts/gen-project-stats.ts --dry-run` → toolCount=183/cronJobCount=2 unchanged (no hand-edit). `mock-guard.sh --files` → PASS. Two full-suite RAW runs (1206 files): run 1 caught the `mock-module-afterall-guard` regression (fixed), run 2 post-fix — 14604 pass/65 fail/6 err, that guard test absent from the fail list, remaining fails pre-existing/unrelated (`src/_deprecated/`), both runs end in the documented Bun v1.3.13 crash-on-exit (env-level, pre-existing).

Did NOT flip `.task_board`/`.head` or touch `task:ALPHA-S2-OMO-LIQUIDITY-CRON` — dev-team (router) holds the umbrella chain-mutex and owns board advancement per this dispatch's explicit instruction. Committed on `main` (explicit pathspec: job file + `cronConfig.ts` + `schedulerJobTable.ts` + new test file + 3 docs files + `1190-pipeline-watchdog.test.ts` + `FACTORY-SCHEDULER-job-table-registry.test.ts`), separate notebook+decision-journal commit, pushed on green (PUSH-AUTONOMY-1).

Zone health: tsc clean, all targeted guard suites green post-fix, full-suite fail-count did not increase from this change (one self-caught-and-fixed regression, zero residual) | HEALTHY.

## 2026-07-15 — ALPHA-S2-RAG-FTS-REBUILD-CRON (dev-team relay hop 3/4) → returned to router

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (router-driven relay; architect brief `docs/architecture-briefs/2026-07-15-alpha-s2-rag-fts-rebuild-cron.md`; mirrored sibling `ALPHA-S2-OMO-LIQUIDITY-CRON` `ae45fd0e7`'s file-touch shape exactly per dispatch instruction; did NOT claim/release the umbrella lock or touch `.head`/`.task_board`)

Added `ragRebuildFts()` (`ragHttpClient.ts` — `POST /admin/rebuild-fts`, 90s `AbortSignal.timeout`, NOT the file's existing 8s constant: DFR-P3 documents ~30-60s FTS build time, a tighter deadline would false-alert nightly), exported from the `rag/` barrel. New `apps/mcp-server/src/scheduler/rag/ragFtsRebuildCronJob.ts` (first cron in a new `rag/` scheduler domain dir) exports `runRagFtsRebuildCron(deps)` — pure trigger+observe, single-branch HARD-fail contract (no SOFT branch unlike OMO — the endpoint has no ambiguous partial-success state): reject → `sendTelegramBug`+`logger.error`, `rebuilt=false`; resolve → `logger.info` only, `rebuilt=true`, zero local DB writes (confirmed rag-service's `POST /admin/rebuild-fts` already exists + is unit-tested, DFR-P3 AC-P3R-5, before wiring). Registered `ragFtsRebuildCron: '15 20 * * *'` in `cronConfig.ts` (off-market slot, verified free) + `buildJobTable()` entry in `schedulerJobTable.ts`, right after `sbvOmoLiquidityCronJob`. Did NOT touch `scheduler/index.ts`/`jobs.ts` — OMO's own reference commit (the one I was told to mirror exactly) never added its job function to any barrel there either; `buildJobTable()` picks up new entries automatically.

New test file `ALPHA-S2-RAG-FTS-REBUILD-CRON.test.ts` (4 tests): success (`notifyBug` NOT called), 2 HARD-fail paths (HTTP-error + timeout rejects, `notifyBug` called with endpoint detail), source-inspection DB-import proof — DI-injects `rebuildFn` directly (per brief) rather than mocking `globalThis.fetch`, since `ragRebuildFts()` throws (Error-based) not a discriminated envelope. Docs/registry sync (schedulerFileCount 68→69 everywhere): `cron-registry.json`, `system-map.json` (mcp-server `crons[]`), new `cron-jobs.md` section. Bumped `1190-pipeline-watchdog.test.ts` (68→69) and `FACTORY-SCHEDULER-job-table-registry.test.ts` (Group A/B 61→62, Group D 83→84).

Verified: `bun tsc --noEmit` exit 0. `mock-guard.sh --files` on all 8 new/changed files → PASS. Targeted run (new test + both bumped guard suites + OMO sibling test) → **40 pass / 0 fail / 340 expect() calls**. Full `bun test` (1207 files): **14608 pass / 40 skip / 65 fail / 5 errors**, then the documented Bun v1.3.13 crash-at-teardown. Exhaustive grep of the untruncated 22762-line log: zero of the 65 fails or anywhere else reference `ragFtsRebuildCron`/`ragRebuildFts`/`rebuild-fts`/`ragHttpClient`/`schedulerJobTable`/`cronConfig.ts` — fail-count exactly matches OMO's own prior post-fix full-suite run (`ae45fd0e7`: 14604 pass/65 fail/6 err), +4 pass delta = exactly this task's new tests. Same long-documented pre-existing fail class (pollNews/VPS-push-log/insider-tx/`_deprecated/1302-technical-indicators`/5000ms-timeout-under-load).

Committed `35cc8cd56` (11 files: 2 new, 9 modified — job file + test file + `ragHttpClient.ts`/`index.ts`/`cronConfig.ts`/`schedulerJobTable.ts` + 2 bumped test files + 3 docs), pushed on green (PUSH-AUTONOMY-1) — `origin/main == HEAD`, pre-push `tsc OK`.

Zone health: tsc clean, targeted 40/40 pass, full-suite fail-count unchanged from pre-existing baseline (grepped, zero overlap) | HEALTHY.

## 2026-07-15 — ALPHA-S2-RAG-FTS-CRON-SAFETY-GATE (dev-team dispatch, atomic FIX) → returned to router

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (dev-team dispatch; MCP gateway degraded/502 all task — no `send_telegram`/MCP calls made, pure TS + mocked-unit-test task; did NOT claim/release the umbrella lock or touch `.head`/`.task_board` per explicit dispatch instruction)

The live mcp-server container predates `ragFtsRebuildCron` (`35cc8cd56`) — the moment ops redeploys for any reason, the cron arms and OOMs rag-service's 768m cgroup nightly at 20:15 UTC (FTS rebuild can't complete at ~56k rows, `RAG-FTS-BUILD-MEMORY-BOUND`, parked BLOCKED). `buildJobTable()` (`schedulerJobTable.ts`) now reads a new boolean `CRON_RAG_FTS_REBUILD_ENABLED` (default unset/false) and OMITS the `ragFtsRebuildCronJob` entry from the returned table entirely (registration-time defusal via conditional array-spread `...(enabled ? [{...}] : [])`, read fresh inside `buildJobTable()` per-call so tests can flip the env var without re-importing) — a stray redeploy cannot arm it. Flag=`'true'` reproduces prior behavior exactly (cron `'15 20 * * *'`, `{timezone:'UTC'}`). Deliberately did NOT gate via empty `CRON_RAG_FTS_REBUILD=''` — `cronConfig.ts` reads it with `??`, which does not treat `''` as absent, and an empty cron expression crashes croner at boot.

`FACTORY-SCHEDULER-job-table-registry.test.ts`: added Group A2 (4 new tests — flag unset/`'false'` omits the entry + asserts 61-length table; flag=`'true'` registers it with cron `'15 20 * * *'`+`{timezone:'UTC'}` + asserts 62-length; `registerJobTable()` actually schedules it) and rebaselined the pre-existing count guards for the new default (Group A/B 62→61, Group D 84→83 when unset) plus a dedicated flag=`'true'` Group D test reproducing the old 84-count exactly. `schedulerFileCount` stays 69 (no file added/removed — registration-only gate). Docs (`cron-jobs.md`, `cron-registry.json`, `system-map.json`) note the flag and that it stays OFF until `RAG-FTS-BUILD-MEMORY-BOUND` is verified fixed.

Verified: `bun tsc --noEmit` exit 0. `mock-guard.sh --files` on both changed src files → PASS. `bun test src/__tests__/FACTORY-SCHEDULER-job-table-registry.test.ts` → **20 pass / 0 fail / 284 expect() calls**. `bun test` on the sibling `ALPHA-S2-RAG-FTS-REBUILD-CRON.test.ts` + `1190-pipeline-watchdog.test.ts` → 20 pass / 0 fail (unaffected, `schedulerFileCount` assertion untouched by this task). Full `bun test` (1207 files): **14629 pass / 40 skip / 49 fail**, then the documented Bun v1.3.13 crash-at-teardown — grepped the full 49-line fail list: zero reference `schedulerJobTable`/`ragFts`/`CRON_RAG_FTS`/`FACTORY-SCHEDULER`; same long-documented pre-existing fail class (`vps_push_log`/insider-tx/5000ms-timeout-under-load/`_deprecated/1302-technical-indicators`).

Did NOT flip `.task_board`/`.head` — router owns board flip + QA gate per explicit dispatch instruction. Committed `db921c52d` on `main` (explicit pathspec: `schedulerJobTable.ts` + `FACTORY-SCHEDULER-job-table-registry.test.ts` + `cron-jobs.md` + `cron-registry.json` + `system-map.json`), separate notebook commit, pushed on green (PUSH-AUTONOMY-1).

Zone health: tsc clean, 20/20 targeted pass, full-suite fail-count did not increase from this change (grepped, zero overlap) | HEALTHY.
