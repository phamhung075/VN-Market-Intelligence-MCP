# dev-mcp-server -- Notebook

## 2026-07-28 — FACTORY-APP-split-assembleBriefing (BOUNDED-1 auto-pickup, P0) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. `_assembleBriefingImpl` (1327L file, ~620L of inline-SQL orchestration inside one function) split into `usecases/briefing/` — 24 single-owner modules (types.ts + 22 step/helper files) covering all 19 numbered steps (poll-news, VN-Index, top-stories, alerts, watchlist, new-reports, macro, sensitive-dates, tracked-commodities, auto-resolve-stale, unresolved-alerts, top-conviction, prediction-signals, portfolio-PnL, insider-recent, foreign-flow, evidence-scores, TA-summary, upcoming-deadlines, global-snapshot) plus persist + freshness-gate. Sequencer now 72L (was inline). Two step modules (`computeTaSummary`, `queryUpcomingDeadlines`) needed one further split each (`defaultComputeTa.ts`, `queryFiledAt.ts`) to clear the <=120L step budget — all 22 non-types files land 23–107L.

**Design choice — decentralized try/catch:** moved per-step error handling FROM the sequencer INTO each owning step module (same catch scope/log-ctx-string/fallback-default as the pre-split code) rather than centralizing 19 try/catches in the "thin sequencer" — this is what actually makes the sequencer thin (72L, not ~250L) while genuinely preserving "a single failing step never aborts the briefing."

**Backward-compat facade:** all public exports (`DailyBriefing`, `AssembleBriefingOptions`, `defaultComputeTa`, `queryForeignFlowSummary_TEST`, `BEARISH_WARNING_THRESHOLD`, `TaSignal`, `GlobalSnapshot`, `BctcDeadlineRow`, `VnIndexSnapshot`, `BriefingAlert`, `TopStory`, `NewReport`, `WatchlistEntry`) re-exported verbatim from `assembleBriefing.ts` — zero call-site edits needed in the 27 dependent test files or 5 consumer modules (`assembleEveningSummary.ts`, `morningBriefingJob.ts`, `franceSummaryJob.ts`, `eveningSummaryJob.ts`, `usecases/index.ts`). This directly de-risks the queued P1 follow-on `FACTORY-APP-split-assembleEveningSummary` (depends_on this task) — it can import the same shared step modules (`defaultComputeTa`, `queryGlobalSnapshot`, etc.) instead of its own duplicated copies once picked up.

Dropped `computeRSILocal` (dead code — RSIFIX-2 already rewired `defaultComputeTa` to the Go TA engine; zero remaining call sites, verified repo-wide grep before deletion) while relocating `defaultComputeTa`/`computeMALocal`.

**Verification:** `bun tsc --noEmit` clean. Targeted 27-file briefing/TA suite: 251 pass / 1 fail both before and after (same pre-existing unrelated cron-expression test) — zero net-new. Full `bun test`: 14844 pass/40 skip/57 fail/1 error across 1233 files — grepped all 57 `(fail)` lines to 17 unique files, none touching briefing/watchlist/TA/conviction/foreignFlow/insiderRecent; re-ran all 17 in isolation (no full-suite contention) → 55/55 pass, confirming standing `FIX-MCP-SUITE-HEALTH-BASELINE` order-dependent flakiness (dev-standards.md:590), not a regression. `gen-project-stats --dry-run`: toolCount=184/cronJobCount=88 unchanged. Live server startup (`PORT=3099`, alongside the running production container on :3000 — did not disturb it) healthy, `/health` toolCount=184, scheduler registered 90 cron keys; `/api/bctc-inspect` + `/dashboards/news-fetch/` both returned HTML (no 500/empty) — dashboard circular-dep check clean.

Zone health: tsc clean, zero net-new test failures (targeted + isolated full-suite delta both verified), tool/cron counts unchanged, server+dashboard live-probed healthy, backward-compat facade verified via 0 downstream call-site edits | HEALTHY.

## 2026-07-28 — FIX-SBV-FETCHER-ZERO-VALUE-EMIT (re-zoned from dev-macro-indicators, P1) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause (already live-traced by dev-macro-indicators, re-confirmed against current code): `pushSbvRatesHandler.ts` defaulted the 6 optional SBV rate fields to synthetic `0` whenever the VPS payload omitted them — which it always does (`vps-scripts/fetch-sbv.sh` only ever sends `usdVndOfficial`+`fetchedAt`). Those synthetic zeros tripped `storeSbvSnapshot`'s own `SENTINEL_ZERO_COLUMNS` guard and rejected the WHOLE snapshot, including the valid non-zero FX rate; the handler never checked the `{skipped,zeroColumns}` return, logging a false-positive "stored" line regardless.

**Fix — merge, don't default, and check the return value.** New export `getLatestSbvRatesRow()` (`sbv.ts:384`) reads the last-persisted `sbv_rates` row; every field the VPS payload OMITS (`undefined`) is merged from it instead of zero-defaulted — an EXPLICIT `0` in the payload still passes through untouched (storeSbvSnapshot's guard remains the correct place to reject that). Handler now branches on `storeSbvSnapshot`'s return: skip → `log.error` + honest `{ok:false,skipped:true,zeroColumns}`. `sbv_rates` is `INSERT OR REPLACE` full-row (schema-macro.ts:141) — no partial-update path exists, so merge-then-full-write was the less-invasive option vs adding one.

**intelligenceCycleJob.ts step A2** (secondary contributor — missing the guard `sbvRatesJob.ts` already had): swapped the raw `fetchSbvRates()+storeSbvSnapshot()` call for `runSbvRatesRefreshJob()` — reuses the dedicated cron's pre-flight sentinel guard wholesale (no 3rd copy of the sentinel-field list) and checks `zeroRateSkipped`. Tradeoff accepted, not mitigated: A2 fires every cycle (far more often than the 4h dedicated cron), so a persistent VCB outage now alerts WORK more frequently than before (previously silent) — flagged in the decision journal, not addressed (task mandate was explicit re: guard-wiring).

Tests: new `FIX-SBV-FETCHER-ZERO-VALUE-EMIT.test.ts` (7 TCs: carry-forward merge, first-write-allows-zero, explicit-zero still rejected, prior-row-untouched-on-reject, `getLatestSbvRatesRow` null/mapped, source-inspection of the A2 wiring) + all 8 pre-existing sbv-related suites — 100/100 pass. tsc clean. `toolCount=184`/`cronJobCount=88` unchanged (no tool/cron touched). Live boot (`PORT=3098`) healthy: `/health` toolCount=184, `/api/bctc-inspect`+`/dashboards/news-fetch/` both 200. Full `bun test`: 14852 pass/56 fail (634s, 1234 files) — within the standing `FIX-MCP-SUITE-HEALTH-BASELINE` band this sprint (56/59/57 across S12-S14), no sbv/intelligenceCycle overlap visible. `apps/macro-indicators/` untouched (confirmed zero write-path, out of scope). Doc-review: `infrastructure.md` Macro table + new sbv.ts subsection (previously undocumented). Board: `in_progress`→`review` (router had already moved backlog→in_progress before dispatch).

Zone health: tsc clean, 100/100 targeted-suite green (new + pre-existing sbv coverage), tool/cron counts unchanged, zero storeSbvSnapshot rejected-zero events reproduced in the fixed merge path, live boot+dashboard probes clean | HEALTHY.

## 2026-07-28 — CI-RED-cdd5fa5a-FIX (PO-triage board row, P0 blocking) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause (PO already reproduced locally, re-confirmed): `emit-pressure-state.test.ts`'s "[macOS path, REAL — negative control]" test called the REAL unmocked `computeContainerVmHeadroomMb()` and unconditionally asserted `toBeNull()` — only true on a host with no `free` binary. CI runs `bun test` on `ubuntu-latest`, which HAS `free`, so the real exec returns a real number and the assertion always failed there (6 consecutive red runs, last green 3ec962067). The SOURCE (from the immediately-prior task FIX-PRESSURE-HOST-HEADROOM-WRONG-MACHINE-WRONG-QUANTITY, commit 98917416a) is NOT defective — a real number on Linux is correct; only the test's expectation was host-specific.

**Fix — entirely in the test, zero production code touched.** Replaced the unconditional `toBeNull()` with a runtime presence-probe (`command -v free` via `node:child_process` execSync) that gates the strict leg: `free` present → assert not-null/finite/>=0; `free` absent → assert null. Still the REAL unmocked default execFn (no stub), so it keeps exercising the true production path. Also asserts an unconditional platform-independent invariant (never throws; result is null-or-non-negative-finite-number) ahead of the gated leg. Updated the stale file-header comment block that stated the same host assumption.

**Secondary fix (board row's own AC-3, small+safe, in-scope):** `scripts/ci-per-file-isolation.sh` captured each failed file's output to `$RESULT_DIR/${slug}.log` but `rm -rf $RESULT_DIR` destroyed it before CI ever surfaced it — CI only ever emitted `FAILEDFILE: <path>` with zero detail (why this required local reproduction). Added a block that cats each `.log` into `$GITHUB_STEP_SUMMARY` (collapsible, tail -100L) before the `rm -rf`. `bash -n` syntax-checked; a synthetic RESULT_DIR+log fixture confirmed the block emits correctly.

**Evidence:** targeted file `emit-pressure-state.test.ts` 31/31 pass on BOTH legs — real macOS host (no `free`) AND a stub `free` shim on PATH reproducing PO's exact CI-repro harness (the same canned Linux `free -m` table). tsc clean. `gen-project-stats --dry-run`: toolCount=184/cronJobCount=88 unchanged (test + CI script only, no tool/cron file touched). CI-equivalent evidence — ran the just-fixed `ci-per-file-isolation.sh` itself end-to-end: 14734 pass/40 skip/6 fail across 1230 files; `emit-pressure-state.test.ts` NOT among the 6 fails. The 5 failing files (083-tool-analysis, 1324-push-news-all-sources, 1398/1793-pollnews-cooldown, FIX-1267-ssc-circuit-breaker) grepped for zero reference to either changed file — pre-existing unrelated drift, not a regression.

**NOT pushed by this agent.** Per `dev-standards.md` PUSH-AUTONOMY-1 ("the session holding the chain mutex pushes") and this task's own explicit VERIFICATION GATE ("do not claim DONE_VERIFIED yourself; flip to REVIEW, let qa/router confirm the green run"), board flipped ready→review only. `verification_gate: ci_green_on_subsequent_push` stays open until a fresh CI run lands conclusion=success on a HEAD SHA != cdd5fa5ad344406298fdf70b59afd71050c30289 after this fix is pushed.

Zone health: tsc clean, 31/31 targeted-file green on both platform legs (entire blast radius — zero production code touched), 14734/40/6 CI-equivalent run clean of target file, tool/cron counts unchanged | HEALTHY.
