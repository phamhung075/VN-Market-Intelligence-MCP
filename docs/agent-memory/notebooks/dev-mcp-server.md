# dev-mcp-server -- Notebook

## 2026-07-31 — FU-BACKFILL-MULTIPLE-COVER-LETTERS (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Residual weakness #2 from FIX-CTG-PDF-MISLINK (77092007): backlog hypothesised "first-match-only" cover-letter exclusion, but code already used `.filter()` (excludes ALL literal cover letters). Real gap: the "consolidated" bucket was NEGATIVE-match (`!isCoverLetterFilename`) — "not a recognised cover letter" ≠ "is the real report". A non-report file (e.g. a P&L-variance explanatory note) that merely failed the cover-letter pattern could be wrongly selected as the sole "consolidated" candidate, or inflate false ambiguity when a real consolidated file was also present.

**Fix (`backfillBctcPdfPaths.ts`):** new `isConsolidatedReportFilename()` — POSITIVE match on Vietnamese "hop nhat" / English "consolidated" marker (same signal `hsxBctcFetcher.ts::rankItem()` already uses). Both the NULL-pass and heal-pass now require this positive match before selecting a PDF among 2+ candidates; a lone candidate still always links (generalised from `coverLetters.length===1` to `matches.length===1`, unconditional on classification). Zero positive matches among 2+ candidates → loud `console.warn`, row stays PENDING (never a silent wrong pick). Heal-pass re-check guard broadened from `isCoverLetterFilename(current)` to `!isConsolidatedReportFilename(current)` — previously a row mislinked to an unrecognised non-report file (not literally cover-letter-patterned) was never eligible for healing at all.

**Tests (new file `FU-BACKFILL-MULTIPLE-COVER-LETTERS.test.ts`, 12 cases):** granular 2-layer RED confirmed — (1) `isConsolidatedReportFilename` added standalone first (no selection-logic change yet): AC-1/AC-3/AC-4/AC-2-GREEN genuinely failed against the unmodified selection logic (AC-3 concretely reproduced the live mislink: old code returned `updated=1`, silently linking the non-report file instead of leaving PENDING); (2) `legacyNegativeMatchSelect()` — a frozen, non-imported snapshot of the exact pre-fix 77092007 decision branches — independently proves `{kind:"ambiguous"}` (fails to heal) on the identical AC-1 fixture. Post-fix: 12/12 pass. One unrelated pre-existing gap found+sidestepped (out of scope, not fixed): `extractTicker()`'s `KEYWORD_SET` is missing `"CV"`, so a `CV_CBTT_TICKER_...`-ordered filename mis-parses ticker as `"CV"` — fixtures use the already-proven `TICKER_..._CV_CBTT_...` ordering instead.

**Evidence:** `bun tsc --noEmit` clean. Targeted suite (new file + `FIX-CTG-PDF-MISLINK.test.ts` + `PI3-bctc-inspect-reopen2.test.ts`): 56/56 pass. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged. Fresh server boot (`DB_PATH=:memory:`, free port 3099): `/health` toolCount=183; `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200. Full `bun test` (RAW-verified, full-capture log not tail-truncated): 14976 pass/40 skip/52 fail, 548.75s — inside the documented pre-existing flake band (52-59); see follow-up entry/journal for the fail-list grep confirming zero bctc/pdf/backfill matches. Docs: `docs/architecture/microservice/mcp-server/usecases.md` — new `### backfillBctcPdfPaths.ts` section (was previously undocumented as its own use case). Simplicity gate: PASS (Q1 no excess scope — positive matcher is the AC's own explicit requirement; Q2 `isConsolidatedReportFilename` has 2 production call-sites; Q3 no added indirection; Q4 overhead consistent with file's existing doc-comment density). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S42.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 3 files touched (backfillBctcPdfPaths.ts + new regression test + usecases.md) | HEALTHY.

## 2026-07-31 — FIX-LANCEDB-INSERT-SEGFAULT (BOUNDED-1 auto-pickup, P3/XS) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause RE-VERIFIED, does NOT hold: backlog claimed "Bun v1.3.13 segfault (addr 0x20) in fetchParseAndStoreBctc step-4 LanceDB insert" (2026-06-07T12:33:46Z repro). Commit d29da3a8d (2026-05-24) had already rewired step-4 to HTTP `ragIndex` (rag-service, port 5002) — 2 weeks BEFORE the repro. Commit 456851797 (2026-07-16, CI-RED-da847805-FIX) independently deleted the last `@lancedb/lancedb` import repo-wide. Live grep today: zero import statements of `@lancedb/lancedb` anywhere in `apps/mcp-server/src` — a native segfault on this path is categorically impossible now.

**Real live gap found (same defect class):** `insertBctcAnalysis.ts`'s `const inserter = insertAnalysisFn ?? (await getDefaultInsertAnalysis())` sat OUTSIDE its own try/catch. A throw during resolution (missing module, or historically a native-addon-import crash — the exact mechanism CI-RED-da847805-FIX fixed) propagated uncaught out of `insertBctcAnalysis()` AND `fetchParseAndStoreBctc()`'s unguarded Step-4 call, breaking the file's own documented "non-fatal" contract.

**Fix:** moved the resolution line inside the try block (1-line move). Also added a deterministic `insertAnalysisResolverFn` injection seam (both files already document "everything injectable") after an earlier `mock.module()`-based test proved flaky under the full suite — Bun hot-swaps (eagerly re-invokes) a throwing factory at registration time whenever the target specifier is already cached, which is true almost always in full-suite order since `ragHttpClient.js` is transitively imported by dozens of files.

**Tests (new file `FIX-LANCEDB-INSERT-SEGFAULT-step4-guard.test.ts`, 2 cases — unit + integration):** RED confirmed genuinely (temp-reverted the try/catch placement, re-ran same test file): both fail, full stack trace `insertBctcAnalysis.ts:79` → `fetchParseAndStoreBctc.ts:121`, exactly the propagation path claimed. GREEN post-fix: both pass, insert failure now logged non-fatally, SQLite Step-3 persistence unaffected.

**Evidence:** `bun tsc --noEmit` clean. AC3 regression subset (new file + 1181-financial-reports-persist + 1352a-async-extraction-race + 1352b-pdf-extractor-wiring + p2-f-rag-http-rewire): 33/33 pass. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged (no tool/scheduler file touched). Full `bun test`: 14974 pass/40 skip/56 fail — inside the SAME pre-existing flake band already documented in this notebook's prior cycle (52-59, FU-BACKFILL entry above); new test file itself 0-fail in full run (was flaky under the earlier mock.module design, fixed); sole `fetchParseAndStoreBctc`-touching failure in the fail list (`FIX-1267` AC-7, 17.7s) independently re-run standalone → 9/9 pass, confirming full-suite-only resource-contention flakiness unrelated to this diff. Port 3000 occupied by the live production Docker container — skipped a conflicting local server boot (Gate 2b); code change requires an ops rebuild before it is live (not deployed by this task). Boundary: dev-rag-service NOT touched — fix is 100% mcp-server-side. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S43.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 3 files touched (insertBctcAnalysis.ts, bctc/types.ts, fetchParseAndStoreBctc.ts) + 1 new regression test | HEALTHY.

## 2026-07-31 — FIX-BCTC-1345B-REPORT-BATCH (P3/XS) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause RE-VERIFIED before coding (mandatory per dispatch brief): 1792's `bctc_signal_debounce` only blocks same-(ticker,period) re-fires within a 1h window — retry-storm case, already fixed. The real live gap is `bctcReparseJob.ts` (runs DAILY at 09:30 GMT+7): it retries the SAME still-broken low-confidence backlog every day, 24h apart — well outside 1792's window — so each daily cycle re-fires the `[BCTC-1345b]` alert for an issue that was already known/tracked the day before. That daily re-fire (not same-minute storms) is the actual source of the backlog note's "6 reports today / 11 more the next day, all same low-confidence-guard class."

**Fix (`parseBctcReport.ts` `storeReport()`):** before the `[BCTC-1345b]` alert, query `financial_reports.validation_status` for the same (action_code, sort_key). If already `'low_confidence'` from a PRIOR parse, suppress the send (row still upserts as normal — audit trail intact). Reuses the existing column (no new table/migration); self-closing — the moment a re-parse produces real confidence the status flips off `'low_confidence'` and a future regression can alert again. Rejected: (a) batch-per-cycle digest — no existing cycle-boundary primitive, XS size doesn't justify new infra; (b) tagging the message to piggyback Task-1215's generic 4h category dedup in `telegram.ts` — its window is still shorter than the 24h reparse cadence AND it would over-suppress genuinely-new-ticker findings sharing the category.

**Tests (new file `FIX-BCTC-1345B-REPORT-BATCH.test.ts`, 3 cases):** regression test ages the 1792 debounce row back 2h (simulating the next daily reparse cycle) then re-parses the SAME already-low-confidence ticker+period — RED confirmed via `git stash` of the fix alone (2 sends, expected 1); GREEN after restore (1 send). Second test: 3 genuinely-different tickers each still alert once (not over-suppressed). Third test: after validation_status flips off `'low_confidence'` (simulated fix), a LATER regression alerts again (self-closing proven).

**Evidence:** `bun tsc --noEmit` clean. Targeted suites: new file 3/3, `1792-conviction-debounce` 5/5, `1345e-integration-pipeline`+`1424a-bctc-unit-scale-mismatch`+`1810c-vnm-unit-mismatch` 19/19 — all pass. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged (no tool/scheduler file touched). Full `bun test`: 14977 pass/40 skip/56 fail — same pre-existing full-suite-only flake band already documented in this notebook's S43 entry above (VPS Proxy Health, get_market_cap, insider_transactions, balance/cashflow stores, MCP SSE, 5000ms-timeout/DB-contention class); re-ran every one of those exact failing files standalone → 100% pass, confirming not caused by this diff. Boundary: the file's OTHER `isBctcSignalDebounced` call site (period-content-mismatch, same file) intentionally untouched — that branch rejects the parse before any row is written, so there is no persistent row to gate against; out of the note's `[BCTC-1345b]` scope. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S44.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 2 files touched (parseBctcReport.ts + new regression test) | HEALTHY.
