# dev-mcp-server -- Notebook

## 2026-08-12 — FACTORY-APP-split-pollNews (P0, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f. BOUNDED-1 auto-pickup (WIP was 0), title-only spec, no detail_ref. `apps/mcp-server/src/application/usecases/pollNews.ts` was 1444L (baselined at 1444L in `size-lint-baseline.json`, no header) — 1 giant `pollNews()` orchestrator + 5 unrelated helper clusters bolted on.

**Split (staged, Stage 1):** extracted 5 single-responsibility siblings under new `pollNews/`: `types.ts` (5 public interfaces), `insiderDetectors.ts` (2 pure title classifiers, Task 1260/1308a), `signalDedup.ts` (1 internal helper), `defaultFetchers.ts` (6 lazy network fetchers — dropped the grep-confirmed-dead `defaultTradingEconomicsFetcher`, not relocated), `dbHelpers.ts` (4 SQLite helpers). `pollNews.ts` is now a 923L orchestrator (36% smaller) that imports + re-exports everything — zero external call-site changes (grep-verified: only `PollNewsResult`/`SourceFetchers`/`detectInsiderFamilyBuying`/`detectInsiderSelling`/`_resetAllDarkAlert`/`pollNews` are imported elsewhere, all still resolve). Decomposing the remaining ~790L orchestrator body itself is flagged as explicit follow-up, not done this pass (tightly-threaded local state — db/watchlist/macroContext/nmCfg closures — across every stage).

**Verified:** `tsc --noEmit` clean. `size-lint-justification.sh --check`: only pre-existing untouched `transport.ts` fails; scoped `jq` update of `pollNews.ts`'s baseline entry 1444→923 (not a wholesale `--update`, which would silently rebaseline `transport.ts` too). New files carry in-file `size-justification:` headers. All 13 pollNews-referencing test files pass in isolation (109/109). Full suite under contention showed 4 pollNews-touching timeouts at the ~5000ms bun-test-timeout mark; reproduced the IDENTICAL signature swapping the pre-split HEAD version back in, then re-ran `FIX-POLLNEWS-COUNTER-CONSERVATION.test.ts` alone post-suite on the split version (3/3 pass) — confirms pre-existing load-dependent network-timeout flake, not a split regression. Gate 2b/2c/2d: clean boot, toolCount=183/cronJobCount=88 both match baseline. Dashboard circular-dep check clean.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S86. Doc: `usecases.md` `pollNews.ts` entry expanded with the module map. Graphify: SKIPPED — no Skill-tool binding from this subagent context (noted honestly, not claimed).

Zone health: pollNews.ts god-file split into 5 verified single-responsibility modules (1444L→923L orchestrator), tsc clean, size-lint clean, toolCount/cronJobCount unchanged, isolated pollNews test suite 100% green, full-suite drift consistent with documented pre-existing flaky baseline | HEALTHY.

## 2026-08-12 — FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW (P2, M, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f. Supervised-Lane Sweep dispatch; router corrected a stale `dispatch_lane=architect` (stamped 2026-08-08 plan-only phase) to dev-mcp-server per PO's `po_goahead_20260812T035700` ratification — independently re-verified live in orch-state (spec_doc present, `po_ratified_at:2026-08-12T03:57:00Z`, `plan_only:false`). Implemented architect's fully-ratified spec (`docs/handoffs/FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW-spec.md`) verbatim.

**Root cause:** `windowPartitioner.ts`'s `maxWindowPages` cap splits a long continuation table into a genuine head window (with the real header) plus one or more headless truncation-tail windows. `resolveColumnLayout()` always fell back to hardcoded `"code-first"` for a tail window's missing header, silently swapping code/label on every 4-column row of a label-first (bank Mẫu B02a/TCTDHN) continuation table split by the cap — `statement_section` was already threaded cross-window (`carrySection`), but column layout never was.

**Fix:** threaded `ColumnLayout` cross-window (`refinedMarkdownParser.ts`: new exported `ColumnLayout` type, `resolveColumnLayout(headerCells, inheritedLayout)`, `parseRefinedMarkdown`'s `initialColumnLayout` param + `ParseResult.finalColumnLayout` — exact structural mirror of `carrySection`/`statement_section`; `finalizeBctcRefine.ts`: `parseDoneUnitsToRows`'s new `carryColumnLayout` loop var). Made `Window.truncated_continuation`/`RefineWindow.truncated_continuation` real (`windowPartitioner.ts` tracks `pendingTruncationTail`; `getBctcPendingRefineTool.ts` wire pass-through — the necessary file architect flagged beyond the task's own list). Corrected 2 stale docstrings claiming behavior the code never implemented. Updated `continuation-stitch.md` (new "Truncation-Tail Windows" section: don't fabricate a header, server already recovers layout) + `main.md` (window shape lists the new field).

**Verified:** RED-first — wrote the spec's full hand-traced regression file (4 parts: partitioner cap-hit flagging, label-first bank tail-window thread-vs-no-thread contrast, code-first negative control, full `finalize_bctc_refine` DB-integration), 5/7 failed pre-fix, 7/7 GREEN post-fix, every assertion matched exactly (`rows_parsed=5`, tail row `code="3"` not swapped). `tsc --noEmit` clean. Targeted 412/412 pass across all 32 test files referencing the touched functions/tools. Full `bun test` x2: run1 15234 pass/57 fail/1 error (770s); run2's fail-name list grep-confirmed ZERO overlap with the changed files — consistent with this zone's documented pre-existing flaky-churn baseline. Gate 2: clean boot toolCount=183/cronJobCount=88 both match baseline, dashboard circular-dep check clean. `windowPartitioner.ts` crossed the 120L size-lint threshold (115L→149L) — added its own `size-justification:` header; only pre-existing untouched `transport.ts` still fails.

**Deliberately NOT touched:** `bctcRefineJob.ts`'s own `parseRefinedMarkdown` call site (same carrySection-only pattern, no columnLayout thread) — its module header states the production spawn path is DELETED (Option-Y ruling), `refineOneReport()` is test-only. `bctcSectionCompleteness.ts`/BEQ-7 unchanged (real gap, out of scope per architect §8). No `docs/architecture/microservice/mcp-server/*.md` update — zero existing section documents this agentic-refine pipeline at all (pre-existing gap, not introduced here).

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S87. Commits: `8b4f977fd` (code+test+docs) + `2d7c6a7e4` (board: `in_progress[]`→`review[]`, `.head` idle-reset same write, per CANONICAL:SSOT-STATUSFLIP-LANEMOVE). Both pushed clean, pre-push tsc hook green.

Zone health: cross-window ColumnLayout threading closes the code/label-swap defect class for ANY maxWindowPages-truncated continuation table regardless of length or cap value; truncated_continuation flag now real, no more comment≠code drift; tsc clean, toolCount/cronJobCount unchanged, targeted BCTC-pipeline suite 100% green | HEALTHY.

## 2026-08-12 — FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST (P0, M, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f. RLC dispatch (dispatch-starved 7 days since 2026-08-05T16:51Z, ~19-20/day BUG-channel refusals). `listSscDocuments()` (`ssc.ts:85-90`) has no quarter parameter — every quarter request for a (ticker, year) lists the SAME document array — and `fetchParseAndStoreBctc.ts:68` took `docs[0]` unconditionally, silently returning whichever document the portal listed first. The already-shipped detector (`FIX-BCTC-INGEST-PERIOD-IDENTITY-UNVALIDATED-VS-CONTENT`) correctly refuses the mislabelled result — that refusal was the daily flood; this row is the actuator fix behind it.

**Fix (architect brief `2026-08-05-fix-bctc-ssc-doc-selection-quarter-blind.md`, implemented verbatim):** new `domain/services/financial-reports/documentTitlePeriodExtractor.ts` (`extractPeriodFromTitle`, pure — parses "quý N/I..IV năm YYYY" + "QN YYYY" title forms) + `application/usecases/bctc/selectSscDocument.ts` (`selectSscDocumentForPeriod`, title-first with `deriveQuarterFromPublishedAt` fallback, named `SscDocumentPeriodNotFoundError`). `fetchParseAndStoreBctc.ts` Step 1 now selects by period instead of `docs[0]`; no-match converts to the function's existing `return null` contract with a debounce-gated Telegram bug (mirrors `parseBctcReport.ts:685-701`'s idiom, distinct key `doc-selection-not-found`). `listSscDocuments()` itself untouched — `checkSscReports.ts` still needs all quarters back.

**Verified:** RED-first new test `FIX-BCTC-SSC-DOC-SELECTION-QUARTER-BLIND-ALWAYS-LATEST.test.ts`: 15/15 pass (35 expect) — domain title-parser unit tests (every live fixture-title shape), selector unit tests (AC-1 match, AC-2 named-error-no-match incl. empty-publishedAt-never-trusted), AC-3 family-A regression (Q1 request vs Q4-first listing, Q4 not selected), 3 `fetchParseAndStoreBctc` integration tests (real `listSscDocuments()` path selects correctly; no-match returns null with exactly 1 debounced Telegram bug across 2 calls; docs.length===0 stays Telegram-silent). `tsc --noEmit` clean. Targeted 376/376 pass across 39 files referencing the touched functions. Full `bun test`: 15247 pass/40 skip/59 fail/1 error/48101 expect (546s) — zero overlap between fail names and bctc/ssc/selectSscDocument/documentTitlePeriod files (one superficially-alarming hit, `1405b-bctc-vps-fixes.test.ts`, fails on unrelated `vps_push_log` news-service assertions, imports none of this task's files) — consistent with today's documented flaky-churn band (S85/86/87 siblings 60/63/57 fail). Gate 2: clean boot toolCount=183/cronJobCount=88 both match baseline, dashboard circular-dep check clean (both HTTP 200). `fetchParseAndStoreBctc.ts` grew 125L→168L (baselined file, touched) — added accurate `size-justification:` header; only pre-existing untouched `transport.ts` still fails.

**Test-fixture repair (not anticipated by the brief):** 3 pre-existing `293-ocr-fallback-pipeline.test.ts` tests requested Q2/Q3/Q4 against a mock SSC listing containing only a Q1-titled document — previously masked by Step 1 being quarter-blind. Corrected their `quarter` param to `Q1` (matches the fixture; OCR-cache filename key derives from the doc URL, independent of requested quarter — pure fixture-honesty fix, no behavior change).

**Scope:** Family B (year-shifted mismatches, DPM 4407) is a different acquisition path (`bctcVpsIngestHandler.ts`'s receive-only `/api/push-bctc-pdf`, never calls `listSscDocuments`) — architect-confirmed out of scope (brief §5). AC-5 (backfill-replay) explicitly BLOCKED on `BCTC-ENRICHER-OLD-QUARTERS` (still BACKLOG) per brief §7 — noted, not skipped; AC-1..AC-4 + AC-3 regression all shipped this cycle.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S88. Doc: `usecases.md` + `financial-reports.md` (new invariant 12) updated.

Zone health: quarter-blind document-selection defect closed at the one call site that mattered (`fetchParseAndStoreBctc.ts` Step 1), fail-loud named error replaces silent `docs[0]`, tsc clean, toolCount/cronJobCount unchanged, new+targeted suites 100% green, full-suite drift consistent with today's documented flaky baseline | HEALTHY.
