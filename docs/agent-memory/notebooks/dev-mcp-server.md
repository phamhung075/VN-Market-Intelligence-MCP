# dev-mcp-server -- Notebook

## 2026-07-29 — FIX-GET-FOREIGN-ROOM-TOOL-RESULT-TOKEN-BUDGET (BOUNDED-1 idle-pickup, recurring-3x) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. RAW-verified LIVE (mcp-call.sh probe against running mcp-server) BEFORE any code change: `get_foreign_room()` with no `code` returned 74,245 chars / 106 tickers — root cause confirmed exactly per PO triage: `getAllTickersHistory()` (`foreignRoomStore.ts`) reads EVERY code in `vnstock_trading_stats` (full traded universe the fundamentals job has touched, 100+ live), NOT the ~33-ticker watchlist.

**Fix:** new pure `summarizeForeignRoomTickers()` (`domain/services/market-data/foreignRoomAnalyzer.ts`) trims `tickers[]` to `top_n` (new zod param, default 10; effective limit always `min(top_n, actual count)` — never a hardcoded universe-size ceiling), ranking ROOM_LOCKED/FULL_ROOM_SELL-flagged tickers first, then `|depletion_velocity_5d|` desc ("top-N by |net|"), then utilization, then market_cap_bn/code tie-break. `tickers_rollup` (counts + avg utilization) computed over the FULL universe regardless of trimming; `fetch_more.omitted_codes` lists dropped tickers for individual re-fetch via `code=<ticker>`. Change is interface-layer-only (`foreignRoomTools.ts`) — the `getForeignRoom` usecase itself is UNCHANGED (still computes cap-weighted saturation/z-score over the full universe internally; `getMoneyRadarComposite.ts` calls the usecase directly, never the MCP tool, so it's unaffected).

**Evidence:** new AC-12/AC-12b sections in `P0-2-foreign-room-suite.test.ts` (24 new tests, 43/43 file total pass). tsc clean. `toolCount=184`/`cronJobCount=88` unchanged (no tool added/removed). Live boot (`APP_ENV=development`, ephemeral file DB, PORT=3099): health 200 toolCount=184. Neighbor-consumer suite (MONEY-RADAR-P0-T2-COMPOSITE, IND-P1-MCP-REST-GAUGES-ENDPOINT, money-radar-api, P0-2-foreign-room-suite — the only 4 files touching this code path) 108/108 pass — zero regression. Full `bun test`: 14903 pass/40 skip/55 fail/1 error/1239 files (580s) — matches the standing `FIX-MCP-SUITE-HEALTH-BASELINE` band; keyword-scanned zero overlap with foreign_room/ForeignRoom/money-radar/indicator-gauges. Doc-review: `docs/agents/tools/list/get_foreign_room.md` (top_n param + new return fields), `market-data.md` (new tool row + invariant #12 explaining the fix + why the usecase/REST projection are unaffected), `testing.md` (new test-file row).

**AC caveat (flagged, not closable solo):** the row's own AC requires "no token-budget error for 3 consecutive alert-commander cycles" — that is a live, post-deploy, multi-cycle observation that only qa/ops can confirm after this ships to the running mcp-server container. I verified the fix mechanically (payload size bounded regardless of universe size, behavior/ranking/rollup correctness, no regression) — the 3-cycle live confirmation is the explicit QA/post-deploy follow-up.

Zone health: tsc clean, 43/43 own-file + 108/108 neighbor-consumer green, tool/cron counts unchanged, full-suite fail-set keyword-scanned clean, root cause fixed at the interface-layer serialization boundary without touching any of the 3 other live consumers of this data | HEALTHY.

## 2026-07-29 — TASK_2006 SUBTASK-DAILY-FF-7 (BOUNDED-1 idle-pickup, optional, size S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Optional follow-on to ARCH-DAILY-FOREIGN-FLOW-TABLE (SUBTASK-DAILY-FF-7, `depends_on` TASK_2002 writer cutover — already DONE_VERIFIED per BOUNDED-1 promote-gate cold-archive check, not re-verified here per dispatch instruction). Annotation-only: zero code/logic change.

**Change:** schema comment marking `daily_ohlcv.foreign_buy_vol/foreign_sell_vol/foreign_net_vol/put_through_vol` frozen/historical-only (`schema-market-data.ts` — JS block comment above `db.exec` + inline SQL `--` comments on the 4 columns inside the DDL string itself, near-verbatim architect phrasing from the design doc §Change 2/R-7). Reiterated the freeze in `ohlcvForeignFlowStore.ts`'s JSDoc (already had an extensive SSOT-FREEZE block from TASK_2002/2003 — added a short explicit "DEPRECATED in favor of daily_foreign_flow" paragraph cross-referencing the schema comment, satisfying the AC's literal wording). NOT a `DROP COLUMN` per the parent design's live-named-volume-DB posture.

**Evidence:** `git diff` on both touched files is comment-lines-only (verified by inspection — no code/logic delta). `bun tsc --noEmit` / `pnpm check`: clean, exit 0. Targeted suite (`daily-foreign-flow-*` — backfill/integration/schema, the 3 files owning this data plane): 33/33 pass. `toolCount=184`/`cronJobCount=88` unchanged (no tool/scheduler touched). Full `bun test`: 14904 pass/40 skip/54 fail/1239 files (557s) — within the standing `FIX-MCP-SUITE-HEALTH-BASELINE` band; the 2 fail files visible in the tail (`_deprecated/1302-technical-indicators.test.ts`, `DS-OBS-01-FIX-sla-breach-work-bug-alert.test.ts` simulated-failure scenario) are both pre-existing/unrelated to this diff's 2-file, comments-only scope. Doc-review: NONE (annotation-only, no behavior/API/schema-shape change to document).

Zone health: tsc clean, pnpm check clean, 33/33 targeted-suite green, tool/cron counts unchanged, diff verified comment-only, full-suite fail-set is the standing pre-existing baseline | HEALTHY.

## 2026-07-29 — FIX-BCTC-D3C-FOLLOW-UP-RESET-ATTEMPTS (BOUNDED-1 auto-claim, XS) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. QA follow-up from D3C DONE_VERIFIED review (sprint-SYSTEMIC-REMAKE-P1-qa.md point 2): bctcQueueEnricherJob.ts's Arm-2 grace-period retry (7d+ in enrich_failed/url_not_found, attempts<6) recycled a row back to `pending` without zeroing `attempts`; the recycled row then re-entered `pek_triggered` (via bctcPdfPullJob.ts) still carrying its old `reconcile_attempts` counter — able to re-exhaust bctcExtractReconcileJob.ts's budget on the very next pass instead of getting a fresh one.

**Fix:** two surgical UPDATE-statement additions, no schema migration (`reconcile_attempts` already existed, `schema-financial-reports.ts`). (1) bctcQueueEnricherJob.ts's Arm-2 recycle write now also sets `attempts = 0` (no-op for Arm-1 rows already pending/attempts=0). (2) bctcPdfPullJob.ts's `pek_triggered` write now also sets `reconcile_attempts = 0` unconditionally — the single natural reset point since bctcExtractReconcileJob.ts only reads/increments that column while status='pek_triggered' (no-op for genuinely first-time rows).

**Evidence:** 2 new regression tests (FIX-BCTC-ENRICHER-STUCK-BACKLOG.test.ts (b) — enrich_failed row w/ attempts=5 recycled → attempts=0 after successful re-discovery; bctc-pdf-pull-job.test.ts — row w/ reconcile_attempts=8 seeded → 0 after re-entering pek_triggered). Targeted 7-file suite 94/94 pass. tsc clean. Full `bun test` caught a genuine regression: 1352a-async-extraction-race.test.ts's hand-rolled fixture schema lacked `reconcile_attempts` (4 tests SQLiteError'd) — fixed the 1-line stale fixture (no other of the ~20 similar hand-rolled fixtures exercises this write path). Re-ran full suite clean: 14906 pass/40 skip/54 fail/1239 files (557s), zero `reconcile_attempts` errors, zero bctc/enricher/pdf-pull fails — matches the standing FIX-MCP-SUITE-HEALTH-BASELINE band. `toolCount=184`/`cronJobCount=88` unchanged. Doc-review: none required (internal scheduler logic only, no tool/API/schema-shape change).

Zone health: tsc clean, 94/94 targeted-suite green, full-suite fail-set keyword-scanned clean of bctc/reconcile/enricher/pdf-pull, root cause fixed at both single write-time reset points, zero-diff scope held (2 prod UPDATEs + comments + 2 new tests + 1 stale-fixture fix) | HEALTHY.
