# dev-mcp-server -- Notebook

## 2026-08-01 — FIX-ERRAUDIT-W2-DEADLINE-UNITTEST (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Test-hygiene gap qa flagged at W2 sign-off (parent FIX-ERRAUDIT-W2-MCP-FETCH-DEADLINE, epic ERROR-AUDIT-2026-06-15 wave 2): `fetchDeadline.ts`'s `withDeadline`/`macroFetch` had zero unit test coverage despite a 6-check code-verification audit at T-1. Added `apps/mcp-server/src/infrastructure/fetchers/__tests__/fetchDeadline.test.ts` (12 tests, colocated sibling `__tests__/` — matches the task's own wording + the already-live `infrastructure/**/__tests__/*.test.ts` pattern used by `orchStateSchema`/`alertVerdictStore`/`FIX-SIGNAL-CONFIDENCE-DEFAULT-50`, `dev-standards.md` 7th-guardrail deviation note 2026-07-30): (a) deadline fires -> `DeadlineError` w/ correct label/deadlineMs/name + FR-6 attribution log, (b) fast response resolves + `clearTimeout` fires exactly once (EC-1), (c) non-abort error rethrown as the exact same instance (never wrapped), (d) `macroFetch` `ok:false` for all 3 `DegradeEnvelope.reason` branches (deadline/http-error/network), (e) `macroFetch` `ok:true` only when `response.ok`, never on a non-2xx body. Mocked `globalThis.fetch`/`spyOn(clearTimeout)`/`spyOn(console.error)` — zero live network. Also added a "Colocated Infrastructure Tests" section to `testing.md` documenting all 4 sibling-`__tests__` files (3 pre-existing ones were undocumented).

**Evidence:** standalone file 12/12 pass, 32 expect() calls. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Fresh boot `PORT=3199`: `/health` toolCount=183. Full `bun test`, 2 independent runs: 15005/15007 pass, 54/52 fail — both inside the documented `14988-14995 pass, 52-59 fail` FIX-MCP-SUITE-HEALTH-BASELINE band (+12 net new passing tests each run); grep-confirmed ZERO `(fail)` lines matching `fetchDeadline|withDeadline|macroFetch` in either run's full log. Commit `b3a5c5145`. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S53.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 2 files touched (1 new test + 1 doc) | HEALTHY.

## 2026-08-01 — FIX-FOREIGN-FLOW-DEAD-ENDPOINT (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Epic AUDIT-FETCH-COMPLETE / parent_recon AUDIT-FC-FOREIGN-FLOW. `fetchForeignFlowWithFallback`'s "Strategy 1: primary VPS endpoint" GETs `http://${VINAHOST_IP}/foreign-flow` — `vps-scripts/vps-proxy-server.js` has never exposed that route (push-only architecture, `POST /api/push-foreign-flow` / `pushForeignFlowHandler.ts` is the sole live write path) — so the GET 404'd silently every market-minute via the production cron (`runForeignFlowFetcherJobCron -> runForeignFlowFetcherJob()`). Fixed by gating Strategy 1 behind `overrides?.fetchFn` being explicitly injected (the function's own pre-existing DI seam, already used for `now`); zero production caller ever supplies `fetchFn`, so production is now a true no-op — no network call, no per-minute 404. Deliberately did NOT delete `fetchPrimaryVpsEndpoint`/Strategy 1 outright (the backlog's literal first fix option, and the verification_gate's literal grep wording) after confirming live that 5 test files ­— including `1392-foreign-flow-cb-probe-regression.test.ts` and `1288-foreign-flow-fallback.test.ts` §4 — explicitly inject `fetchFn` and assert the primary path IS reached, as regression coverage for the historical Task 1392 CB-stuck-open incident; deletion would have discarded that coverage and orphaned the out-of-scope 424-line `foreignFlowValidator.ts`. Updated `infrastructure.md`'s fetcher table + added a footnote (doc-review).

**Evidence:** New `FIX-foreign-flow-dead-endpoint.test.ts` (4 tests, RED confirmed pre-fix, GREEN after): production-shaped call never touches `globalThis.fetch`, no `/foreign-flow` URL constructed, DI seam still reaches `source:"primary"` when `fetchFn` is injected. `bun tsc --noEmit` clean. Targeted 9-file foreign-flow suite: 58/58 pass. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Full `bun test`: 15008 pass/40 skip/55 fail — inside the documented FIX-MCP-SUITE-HEALTH-BASELINE band; zero fail-list matches for any of the 9 foreign-flow-fetcher test files; the 2 fail-list lines containing "foreign" (`TSU-DEV-U5`, `Task 1518`) re-ran standalone 15/15 pass — full-suite-only timeout flakiness, neither file imports `foreignFlowFetcher.ts`. Commit `9ec88cd4e`. REBUILD_REQUIRED=true — live post-rebuild RAW row-count check deferred to qa/ops. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S54.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 3 files touched (1 code + 1 new test + 1 doc). Observed but out of this task's scope: Strategy 3 (SSE fallback) in the same fetcher is equally DI-only/dead in production (`overrides.sseMessageBus` also never supplied by any production caller) — same defect shape as the one just fixed, candidate for a future BACKLOG row if it matters | HEALTHY.

## 2026-08-01 — FIX-BCTC-SLA-FRESHNESS-EXCLUDE-TERMINAL (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Residual of the FIX-BCTC-DISCOVER-CURRENT-QUARTER-ZERO-PUSH reroute. `get_sla_status` (`slaStatusTools.ts`, `MAX(parsed_at) FROM financial_reports`) already structurally excludes terminal rows — a `url_not_found` ticker never gets a `financial_reports` row. The REAL live defect: `queryBctcPipelineRuntimeState()` in `freshnessSlaMonitorJob.ts` counted `url_not_found`/`enrich_failed` (terminal/parked, bounded 7-day grace-retry only) toward the `queueDepth` feeding `checkSignalSla`'s CRASH/IDLE gate in `freshnessSlaChecker.ts` — a queue holding ONLY terminal rows never reported `queueDepth=0`, permanently defeating IDLE and letting a genuinely-absent ticker make an otherwise-idle period misclassify as CRASH once push-age exceeded threshold. Live DB probe confirmed this defeats the gate right now: `vn-bctc-fetch` `vps_service_health` reads `unhealthy` since 2026-07-28 with 0 pending/pek_triggered rows (39 url_not_found + 128 enrich_failed only) — a genuinely idle queue. Narrowed the SQL to `status IN ('pending','pek_triggered')`; documented the reader contract on `PipelineRuntimeState` in the domain file. Found the identical pattern duplicated in `vpsHealthPoller.ts`'s bctc `queueGuardSql` (same live symptom) — deliberately NOT touched: separate tool (`get_vps_health`), own QA-approved test file explicitly asserting the old behavior — flagging as a follow-on candidate, not silently folding into this P3 row.

**Evidence:** RED confirmed (4 failing: stale `C-2` updated + new `C-2c`/`C-2d`/`C-10`, `C-10` mirrors live prod exactly). GREEN: `FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH.test.ts` 23/23 pass. Targeted 25-file SLA/bctc-queue suite: 338/338 pass. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Full `bun test`, 2 independent runs: 15007/15009 pass, 59/57 fail — both inside the documented `14988-14995 pass, 52-59 fail` FIX-MCP-SUITE-HEALTH-BASELINE band; grep-confirmed ZERO fail-list matches for `freshnessSla|FIX-HEALTH-RECHECK-BCTC|FIX-BCTC-SLA-FRESHNESS|queryBctcPipelineRuntimeState|vpsHealthPoller` in the full (non-truncated) 2nd-run log. Commit `f0831e6be`. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S55.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 3 files touched (2 code + 1 test). `vpsHealthPoller.ts` carries the identical terminal-state-counted-as-active bug for `vn-bctc-fetch` VPS health (currently live-manifesting false "unhealthy" since 2026-07-28) — candidate for a dedicated follow-on task | HEALTHY.
