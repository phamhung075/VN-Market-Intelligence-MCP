# dev-mcp-server -- Notebook

## 2026-08-01 — SPIKE-BCTC-REFINE-MAXWINDOW-TRUNCATION (BOUNDED-1 auto-pickup, P3, recon) → REVIEW, next_agent=po

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Recon-only, no code change. Health-recheck (2026-06-21 ISSUE-8) framed `[windowPartitioner] continuation window truncated at maxWindowPages` as an intelligence-cycle news signal-drop — grep-CONFIRMED WRONG (`partitionIntoWindows` has zero news/intelligence-cycle callers; BCTC-refine-exclusive, and the misattribution persisted across 9 health-recheck cycles 06-19→06-23). Traced the cap through the LIVE agentic-refine path: `windowPartitioner.ts`'s outer loop never skips a page (no wholesale row loss), but when the cap truncates a >3-page continuation table, the excess pages start a brand-new "headless" tail window (no header of its own) that `docs/agents/refine_bctc_md/flow/continuation-stitch.md`'s contract never anticipated (assumes the window's first page always carries the real header). `refinedMarkdownParser.ts::resolveColumnLayout()`'s `headerCells` state resets per-window-call — unlike `statement_section`, which IS threaded (`carrySection`) — so a headless tail window silently defaults to code-first layout, swapping `code`/`label` on label-first (bank Mẫu B02a/TCTDHN) continuation tables. `bctcScalarAggregator.ts` resolves headline scalars (total_assets/equity_total/net_revenue) via label-preferred matching, so a swap can silently degrade them — with no gate catching it (BEQ-7/`checkSectionCompleteness` only checks section presence, never code/label correctness). Live-confirmed trigger frequency: 5 truncation WARNs in the same second, one health-recheck cycle — not theoretical. A corrupted live row was NOT independently reproduced within the 120-min timebox (code-path finding, corroborated not directly observed).

**Disposition:** kept WARN severity (real corruption path, not benign already-flagged behavior); minted `FIX-BCTC-REFINE-WINDOWTRUNCATION-COLUMNLAYOUT-CROSSWINDOW` (P2, backlog, next_agent=architect — 3 non-exclusive candidate mitigations need a design call, not a blind implementation) instead of a blind fix either direction, per the task's own either/or framing. Findings doc: `docs/spikes/SPIKE-BCTC-REFINE-MAXWINDOW-TRUNCATION.md`. Board: SPIKE row IN_PROGRESS→REVIEW, `.head`→idle (CANONICAL:SSOT-STATUSFLIP-LANEMOVE, branch:null direct-execute), applied via `orch-apply.sh` (validated, conservation-checked). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S59.

Zone health: tsc/tests untouched (recon-only, zero code/test files modified) | HEALTHY.

## 2026-08-01 — FIX-AGENTSIGNALS-ALLPRODUCERS-NULLSTRIP (BOUNDED-1 auto-pickup, P3/S) → CLOSED NOT-A-DEFECT

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Row carried a PO correction (`po_correction_20260721T1724`) instructing me to re-establish the "MCP transport strips from_agent:null→undefined" premise myself before implementing, and close NOT-A-DEFECT if it doesn't reproduce. Grepped every `from_agent:null` caller of `get_agent_signals`: both real callers — market-watcher Step 0-GW corroboration (`docs/agents/market-watcher/flow/main.md:78-82`) and news-scout SIBLING_WINDOW_CACHE (`docs/agents/news-scout/flow/stage-bootstrap.md:56-60`) — use the identical `call_tool(server="vn-market",...)`/`mcp__gateway__call_tool` mechanism PO already live-verified returns a correct all-producers result (94 rows, `cow-20260721T162324`); no distinct transport exists for either. Grepped every bash-transport caller (`scripts/agents-flow/mcp-call.sh`, which bypasses the gateway and POSTs directly to vn-market's own `/mcp` endpoint): zero call sites pass `from_agent:null` to `get_agent_signals` anywhere in the repo — the one bash caller (`scripts/emit-audit-signal.sh:288`) uses a concrete agent-name string, unaffected. `getCycleBootstrap.ts`/`agentBootstrap.ts` never cross the MCP/Zod boundary for signals at all (call `getSignals()`/`getBroadcastSignals()` domain functions directly). Read the MCP TypeScript SDK's own request path (`@modelcontextprotocol/sdk` `dist/esm/server/mcp.js:430`, `safeParseAsync(argsObj, request.params.arguments)`): a straight JSON→Zod pass-through, no null-stripping logic anywhere. `agentSignalTools.ts`'s `if (args.from_agent === null)` all-producers branch has been checked FIRST (before the inbox undefined-guard) since it was introduced (`51c727254`, 2026-06-18) — unchanged today, no regression window. Independent corroboration: sibling residue row `FIX-COWORK-SIBLING-WINDOW-CACHE-FAIL-OPEN` already carries a PO note (`po_provenance_20260721T1724`) reaching the identical NOT-A-DEFECT conclusion for a near-identical 2026-07-21 false report, attributing it to "a transport failure misread as a tool-contract rejection" during an mcp-server restart window — the same failure signature most plausibly explains this row's own source (health-recheck 2026-06-21T20:07Z BUG-6 probe).

**Disposition:** NOT-A-DEFECT — no remaining null-stripping path found anywhere in the codebase for `get_agent_signals`. Did NOT implement the `fix_spec`'s defensive `all_producers:true` flag / `get_recent_signals` re-routing — per PO's explicit instruction, no speculative code against a non-reproducing premise; re-open with the concrete caller/transport if a future RAW-observed strip surfaces. Board: row IN_PROGRESS→archive/CANCELLED, `.head`→idle (CANONICAL:SSOT-STATUSFLIP-LANEMOVE, branch:null direct-execute), applied via `orch-apply.sh` (validated, conservation-checked, exit 0). Full findings recorded inline on the archived row (`dev_mcp_server_closure_20260801`). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S60.

Zone health: no code/test files touched (investigation-only closure) | HEALTHY.

## 2026-08-01 — FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY-THAT-CANNOT-PRODUCE-ITS-OWN-VALUE (P2/S, dev-team pipeline-resume) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause (measured by PO/analysis-agent, telegram_reports id=4273): parseBctcReport.ts's unit-mismatch short-circuit sets confidenceFinancial=0.1 WITHOUT ever calling validateFinancialFigures() — but the [BCTC-1345b] alert text unconditionally told the reader to check the VNM/VEA (BCTC-VAL-01/03) patterns, which live INSIDE the skipped call; validateFinancialFigures() can never actually return 0.1 (only {0,0.4,0.6,0.8,1.0}). Fix: split the inlined unit-mismatch ternary into named `crossStmtMismatch`/`bsIntraStmtMismatch` booleans, added exported `describeConfidenceFinancialReason()` that builds the alert's tail sentence from whichever detector actually fired (unit-mismatch branch: cross-stmt/intra-BS ratio+figures, never VNM/VEA; validator branch: new `validateFinancialFiguresDetailed()`, a thin-wrapper refactor of `validateFinancialFigures()` — same scoring, now also returns `{rule, detail}[]`, single source of truth so existing callers/tests keep their plain-number contract). AC-2's "0.0/0.8-only" VNM/VEA-hint gate falls out structurally (VAL-01 always hard-fails to exactly 0.0, a lone VAL-03 always yields exactly 0.8) rather than a manual check. AC-3: deleted the unreachable `Math.max(confidence, 0.1)` floor in financialFiguresValidator.ts (max stacked soft penalty is 0.6 — VAL-01-SCALE/POSITION requires totalEquity>0, VAL-06 requires totalEquity<0, cannot co-fire — true floor is 0.4, not 0.1) and corrected the docstrings.

**Evidence:** new file `FIX-BCTC-1345B-ALERT-NAMES-A-RULE-FAMILY.test.ts` (9 tests, 31 expect(): unit-mismatch alert omits VNM/VEA + names the real detector w/ figures, both direct-unit and end-to-end via parseBctcReport; genuine BCTC-VAL-01/03 alerts DO carry the rule+VNM/VEA signature; a BCTC-VAL-05-only violation does NOT; AC-3 sweep asserts confidence never equals 0.1). Targeted 10-file BCTC suite: 72 pass/0 fail, 146 expect(). `bun tsc --noEmit` clean. Full `bun test`: 15050 pass/40 skip/60 fail/2 errors, 47734 expect(), 494.76s — grep-confirmed ZERO `(fail)` lines match bctc/financial/1345b/VAL-01/VAL-03/unit-mismatch; every failure is a pre-existing unrelated class (VPS proxy health, vnstock stores, push-news, insider-transactions/1146, get_backtest_runs/1844a, foreign-flow/1518, TSU-DEV-U5, get_market_cap/FIX-B-2, MCP SSE/Task 251, telegram routing/Task 235) — fail-count crept 52→60 since the last session's baseline read, all within the documented flaky-5000ms-timeout class, none newly introduced by this change. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged (no tool/scheduler surface touched). Fresh boot `PORT=3198`: `/health` toolCount=183, `/api/bctc-inspect` returns valid HTML (dashboard circular-dep check clean). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S61. Aside (out-of-zone, not actioned): pdf-extractor's Python mirror (`docs/architecture/microservice/pdf-extractor/domain-model.md`/`testing.md`) documents the identical dead-0.1-floor pattern for its own `validate_financial_figures()` — flagging for PO/dev-pdf-extractor, zone-restricted from touching it here.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 3 files touched (2 code + 1 new test) | HEALTHY.
