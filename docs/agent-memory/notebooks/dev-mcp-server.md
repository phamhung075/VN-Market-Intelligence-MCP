# dev-mcp-server -- Notebook

## 2026-07-24 — FACTORY-INTERFACE-move-kinhdich-ta-scoring-down (BOUNDED-1 auto-pickup, DDD pure move) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: DDD layering — move Kinh Dich + TA scoring math out of `interface/mcp/tools/` down into domain/application, pure move (byte-identical output). Investigated at source first, per no-fabrication constraint.

Kinh Dich half: `interface/mcp/tools/kinhdich/kinhDichTools.ts` had 9 score-computation functions (`computeSentimentScore`, `computeFundamentalsScore`, `computePriceScore`, `computeForeignFlowScore`, `computeSectorScore`, `computeMacroScore`, `tickerJitter`, `computeHaoScores`, `computeMacroIndicatorScore`) defined inline in the interface layer, despite already accepting an injected `IKinhDichScoreRepository` (task 1838b) — genuine layering debt. Moved verbatim to new `application/services/kinhDich/kinhDichScoring.ts` — **not** `domain/services/kinhDich/` (that folder's own header says "Pure domain — NO I/O imports"; these functions call `getDb()`/repo directly, so application is the correct, source-verified landing spot, mirroring `application/services/imfConvictionBridge.ts` + `application/usecases/getForeignRoom.ts`). Interface file now imports + calls them; kept `formatKinhDichTradingContext` (response shaping) and `registerKinhDichTools` (parsing/registration) in place. Updated 1 scheduler consumer (`defaultComputeHexagrams.ts`'s dynamic `import()`, previously an interface→domain-ward dependency-direction smell, now fixed) + 6 test files' import paths. Byte-diffed all 9 moved function bodies old-vs-new: **identical**, zero drift.

TA half: investigated `interface/mcp/tools/market-data/technicalIndicatorTools.ts`'s local pure-math helpers (localEma/localComputeRSI/localComputeMACD/localComputeBB/maSignal/rsiLabel/etc) — did **NOT** move. Source check (`docs/handoffs/TASK_P2-B2.md` + `docs/architecture-briefs/2026-05-22-refactor/p2-b-caller-inventory.md`) shows this is already-shipped, deliberate G5 architecture: the real domain TA service was quarantined to `src/_deprecated/technicalIndicators.ts` on purpose (TA business logic's permanent home is the Go microservice, port 5003); the interface-layer duplicate is a documented, intentional DB-fallback, not oversight debt. Moving it to mcp-server's domain layer would resurrect explicitly-retired code and fight a shipped decision — same category as Kinh Dich's own AC-8 "not migrated to microservice," just for TA. Recorded as PARTIAL-by-design in the decision journal, not silently dropped.

Evidence: `bun tsc --noEmit` clean. Targeted kinhdich/macro/diacritics regression (19 files, 457 tests incl. 285/298/299/297/302/278/1292/1007/1408/1410/1414/1416/1982/KD-OBS-01-FIX/1047/TASK17-PAGE11/1501/312/1077): 453 pass/4 skip/0 fail, identical before vs after (git-stash A/B confirmed — stashed run flaked under batch-timing contention on 1501's real network calls, but the SAME flake reproduced on the untouched original code too, and passed clean in isolation both ways, proving it's pre-existing environmental flakiness, not a regression). Full `bun test`: 14727 pass/40 skip/41 fail (one run) and 14725 pass/43 fail (repeat run) — count fluctuates 41-43 run-to-run (confirms flaky/timeout class, not deterministic); grepped every `(fail)` line across the repeat full run — zero mentions of kinhdich/hexagram/macro-score/sector-score/foreign-flow-score; all failures match today's already-documented pre-existing classes (vps_push_log schema-drift, 5000ms MCP-client-timeout, the pre-quarantined `_deprecated/1302-technical-indicators.test.ts` flake). Local server boot probe (port 3999, isolated from the live docker container): health 200, toolCount=184, both `/api/bctc-inspect` + `/dashboards/news-fetch/` dashboards 200. Tool count 184 / cronJobCount 88 unaffected (pure relocation, zero tool/cron surface change). Doc updated: `docs/architecture/microservice/mcp-server/kinhdich.md` (new "Application services" line). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-INTERFACE-move-kinhdich-ta-scoring-down-dev-mcp-server.md`.

Zone health: tsc clean, targeted 453/453 pass 0 fail (byte-identical to pre-change baseline), full-suite fail-count flux (41-43) confirmed pre-existing/unrelated via grep + isolation, tool/cron counts unaffected, 9/9 moved functions byte-diff-verified identical | HEALTHY.

## 2026-07-23 — FIX-BASE-RATE-COMPUTATION-CRON-DEAD (BOUNDED-1 auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: `baseRateComputationJob` reported ~20d stale (T3-ARCH-CRON-WATCHDOG live alert, created 06-14 when the job was still weekly). Finding: PARTIAL — 2 real fixes + 1 dependency flag. RAW-verified live (docker exec, production `market.db`): the job IS registered (daily `7 19 * * *` since the 07-01 TASK-EVIDENCE-HOP1-MCP cadence fix), fires daily, 100% success (18/18 runs in 30d) — but `evidence_likelihood_ratios` (its only output table) hasn't gotten a real content update since **2026-06-28** (25d), because `triplesUpserted=0` every run since — HONEST zero, not fabricated: its sole input `evidence_fragments` shows a real (rows_written=2) success on 06-28, then ZERO rows of any kind until 07-19 (a ~21d silent gap), matching the documented ~19-day container restart-storm already called out in `startScheduler.ts`'s FIX-CRON-SUNDAY-STARTUP-CATCHUP comment (`mcpServerStartup` recorded 28 boots across 06-28..07-19). `evidence_fragments` has since recovered (239 rows, earliest 07-19, fresh through 07-23) but isn't yet ≥5d old, so today's 0-output is still correct/expected — will self-heal ~07-24/25. Root cause of the upstream gap itself: already tracked under still-open `FIX-EVIDENCE-PIPELINE-STARVED`, flagged not chased (out of this S-task's scope).

Found 2 GENUINE reachable bugs during this dig: (1) `runBaseRateComputationWithDb` (`startupHelpers.ts`) wrapped `recordJobRun` around a default `fn` that called the ALREADY-self-recording `runBaseRateComputationJob()` — the exact double-wrap anti-pattern already diagnosed+fixed for `runEvidenceAccumulatorWithDb` but never applied here. Live proof: 2-3 `cron_job_runs` rows per single daily tick every day sampled (137899-151743, 07-19..23) — 1-2 noisy `rows_written=NULL` rows alongside the 1 real row (correctness wasn't broken — the T4 dedup guard already no-op'd the 2nd physical node-cron tick — but observability was). (2) `baseRateComputationJob` had NO startup-catchup probe, unlike its 5 siblings (morningBriefing/eveningSummary/franceSummary/alertDigest/summaryJob:daily/dataAuditJob:weekly) already hardened against exactly this restart-timing class — its own history (real success 06-28, then total silence to 07-19) is a textbook instance.

Fix: (1) `runBaseRateComputationWithDb` default fn now calls the core `runBaseRateComputation(db)` directly (single recordJobRun), with the T4 `shouldSkipRecoveryReplay` dedup guard replicated INTO the wrapper (was previously invisible to a 2nd physical tick since schedulerJobTable.ts calls the wrapper, not the inner Job()). (2) Added a 6th startup-catchup probe in `startScheduler.ts` (`shouldRunCatchup(db,'baseRateComputationJob',19,7,new Date(),false)` → `runBaseRateComputationWithDb(db)` directly, NOT double-wrapped via `jobRunRepo.wrapRun`). New test `FIX-BASE-RATE-COMPUTATION-CRON-DEAD.test.ts` (11 cases, T1/T2 single-row proof, T3/T4 dedup, T5 cross-window boundary, T6 single-wrap end-to-end through real core logic, T7-T10 `shouldRunCatchup` window semantics, T11 source-contract) — all green. Mid-task discovery: `FACTORY-SCHEDULER-job-table-registry.test.ts`'s `mock.module("../scheduler/startupHelpers.js",...)` never restores (no `mock.restore()`), silently stubbing `shouldRunCatchup`/`run*WithDb` for any file sharing the same `bun test` process afterward — fixed my OWN test's exposure using the ALREADY-established in-repo workaround (034-telegram-notifier.test.ts / 1298b-imf-infra.test.ts precedent: `?isolate=` query-busted dynamic `import()` bypasses Bun's module-mock cache) rather than editing the other task's shipped test file. RED→GREEN proven via `git stash` A/B on the 2 touched source files (T3/T4/T6/T11 flip fail→pass). Targeted regression (25 scheduler/catchup/cron-health files incl. FACTORY-SCHEDULER + EVIDENCE-ACCUM-SILENT-CRON + both prior catchup precedents): 255/255 pass. `bun tsc --noEmit` clean. Full `bun test`: 14715 pass/40 skip/42 fail — `git stash` A/B on both touched files confirms all 42 pre-existing (vps_push_log/logVpsPush schema-drift + 5000ms MCP-client-timeout class, zero overlap). Server boot probe clean (toolCount=184, health 200, both dashboards `/api/bctc-inspect` + `/dashboards/news-fetch/` 200). Tool count 184 / cronJobCount 88 unaffected (job-internals + 1 new startup-catchup entry inside the existing composition root, no new tool/cron surface). Decision journal: `docs/agent-memory/decisions/sprint-FIX-BASE-RATE-COMPUTATION-CRON-DEAD-dev-mcp-server.md`.

Zone health: tsc clean, 11/11 new + 255/255 targeted regression green, full-suite baseline unaffected (42 pre-existing fails confirmed via git-stash A/B), tool/cron counts unaffected; also surfaced+worked around a latent process-global test-isolation defect in FACTORY-SCHEDULER-job-table-registry.test.ts (unrestored mock.module) — flagged here, not fixed at source (out of zone-task scope) | HEALTHY.

## 2026-07-23 — FACTORY-INTERFACE-delete-bak-files (FACTORY, P2, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: janitorial removal of committed `.bak` dead-code files. Dispatcher named 2 mcp-server files + "repo-root .bak" (via `ls *.bak`, matched `docker-compose.yml.bak`). Mandated safety grep surfaced the canonical backlog ticket text (2026-06-15 maintainability audit + 2026-07-04 server.ts extraction brief + `docs/data/orch/archive/backlog-detail.json`, same ticket-id) specifying 4 files total (+`docs/TASKS.md.bak`, confirmed dead — the live `docs/TASKS.md` no longer exists) plus a `.gitignore` `*.bak` entry as the DoD. Honored the canonical scope over the narrower dispatcher paraphrase.

Fix: `git rm` all 4 tracked `.bak` files (`server.ts.bak`, `telegramReportTools.ts.bak`, `docker-compose.yml.bak`, `docs/TASKS.md.bak`) + added `*.bak` to `.gitignore`. Zero-reference grep confirmed clean before deletion (only doc-mentions of this same ticket). `bun tsc --noEmit` clean (0 errors — `.bak` was never compiled, no behavior change). No test/tool/cron surface touched. Commit `2a146ecdd`. Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-INTERFACE-delete-bak-files-dev-mcp-server.md`.

Zone health: tsc clean, git ls-files shows zero `.bak` tracked, `.gitignore` blocks future `*.bak`, zero live references pre-deletion, tool/cron counts unaffected (no runtime change) | HEALTHY.
