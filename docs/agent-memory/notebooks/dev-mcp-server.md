# dev-mcp-server -- Notebook

## 2026-08-15 — SPIKE-BCTC-EXTRACTION-DORMANT-MASS-ENRICHFAIL-FLOOD supervised-hold triage (P1, M, review-lane SECONDARY-Drain unstuck, no code work) → HELD, deferred to PO

**Session:** 632721c2-41e4-4aff-8d06-a47cf80dc0d7. Row unfroze via dev-team's Review-Lane SECONDARY-Drain (`secondary_claimed_by="dev-team (review-lane secondary-drain)"`, updated_at 2026-08-15T07:40:55Z) after being pinned since 2026-08-11T17:36:34Z behind the same-day prose-ceiling bug (`FIX-PROSECEILING-SECONDARY-CLAIM-STAMP-FIELDS-MISSING-FROM-STRUCTURAL-EXCLUDE-SET`, dispatched elsewhere — not touched here).

**Gate confirmed live:** row carries `supervised:true` + `plan_only:true` + NO `po_goahead_*` field. PO's own same-day note `docs/agent-memory/decisions/triage-20260815T0728Z-po.md` explicitly declined to ratify: "until the ceiling fix lands the drain cannot stamp it, so a ratification would have been inert ... Flagged on the row as PO's own follow-up." Held — did NOT start AC-1/AC-2/cap-enforcement investigative work; did NOT reassign `next_agent`; did NOT mark DONE_VERIFIED.

**Compounding-gate finding (new):** attempted to write a hold-note back onto the row via the canonical `scripts/orch-apply.sh` path; `scripts/orch-row-prose-ceiling-check.mjs` Stage 2.5 hard-rejects it (probe-verified on a scratch candidate, live file untouched: row=34725B vs 12000B ceiling, ANY net-new field is an unbypassable exit-1 — no bypass env var by design). The row's sanctioned escape hatch (`scripts/orch-backlog-stub.sh` detail_ref migration) is itself blocked by `FIX-ORCHBACKLOGSTUB-COLD-ITEMS-ARRAY-SHAPE-CRASH-BLOCKS-LANES-MIGRATION` — same wall PO hit this morning. Net effect: this row cannot receive ANY inline board write right now, including PO's own eventual `po_goahead_*` stamp, until either the backlog-stub crash is fixed or the ceiling exclude-set is widened again.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S6.

Zone health: no code touched this cycle (supervised-hold triage only); hold correctly recorded in the decision journal (row-level write blocked by an independently-verified 2nd gate, not fabricated); escalated the write-blocker finding back to dispatcher | HELD.

## 2026-08-15 — FIX-PENDING-REFINE-OUTPUT-235K-OVERFLOW rework (P-medium, S, Review-Lane SECONDARY-Drain, QA CHANGES_REQUESTED) → qa[] for fresh QA pass

**Session:** 632721c2-41e4-4aff-8d06-a47cf80dc0d7. Rework of prior fix (51f162829+e0ed0369e). QA RAW-verified the pagination/offset-walk mechanics (no data loss) but live-tested the deployed container against REAL production data and found MAX_LIMIT=100 reproduces/worsens the original 235K overflow (limit=100 → 565,285 chars, 2.4x the original bug).

**Root cause of the gap:** the prior fix bounded ROW COUNT, not PAYLOAD SIZE — real `bctc_refined_units` window weight averages ~6-7KB/row and drifts as the pending queue ages. Live-reverified myself: the SAME `limit=20` query QA measured at 125,211 chars on 2026-08-12 already read 153,138 chars on 2026-08-15 (zero code change, +22% in 3 days) — a static row-count-derived cap would eventually re-break the same way.

**Fix:** response-size guard on `getBctcPendingRefineTool.ts` checks the ACTUAL assembled JSON payload on every call (not `limit`) — when it would exceed `SAFE_INLINE_CHAR_LIMIT` (150,000 chars, dep-injectable via new `guardOverride` param mirroring the existing `dbOverride`/`fetchPageTextsDeps` DI seam), writes the full lossless payload to `data/exports/bctc-pending-refine-oversized-<ts>.json` (mirrors `exportPortfolioSnapshot.ts` convention) and returns a small structured `{error:"response_too_large", file_path, char_count, row_count, suggested_limit}` object instead of the oversized blob. `suggested_limit` is computed from the ACTUAL avg row size observed in that call. Guard also protects the `report_id` single-fetch branch (no `suggested_limit` there — not paginable).

**Verified:** new AC-d/e/f/g tests (4 cases, tiny injected `safeInlineCharLimit` to trip deterministically) — RED confirmed (3/4 failed pre-impl, missing 3rd handler param + guard logic), GREEN after. Full file 9/9 pass; 5 sibling consumer test files reusing this tool (`FIX-REFINE-PENDING-SCHEMA`, `FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW`, `AR-refined-units-idempotency`, `HC-human-confirm`, `UNBLOCK-CTG-REFINE-DRAIN`): 87/87 pass. `tsc --noEmit` clean. Gate 2b/2c/2d: `PORT=3099` boot clean (`/health` 200, toolCount=183), toolCount/cronJobCount=183/88 (183 is a pre-existing drift from unrelated work since 2026-08-12, confirmed via git-stash A/B, not caused by this fix). **Live production verify (in the running container, real data, not just the synthetic fixture):** limit=1 (fleet-cron's real usage per `refine_bctc_md/flow/main.md`) → 7,562 chars, guard silent; limit=20/50/100 → guard fires, returns 520-524 char structured responses (vs. 153,138/373,767/695,225 chars unguarded), file confirmed to contain the full lossless row set (100/100 ids verified for the limit=100 case). Full non-isolated `bun test`: 15303 pass/40 skip/52 fail/1 error (508.75s) — within the documented noise band (prior cycles this session: 51/57 fail); grepped the ENTIRE log for `pending.refine|PendingRefine|235K|getBctcPendingRefineTool` — zero matches, confirming no attribution to this change.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S7. Docs: `docs/agents/tools/list/get_bctc_pending_refine.md` (guard behavior + example payload documented), tool docstring + MCP description string updated. Commit: `532fc71a0` (code+test+doc).

Zone health: real-data size-bound gap closed with a dynamic per-call guard (not a point-in-time-average recalibration that would drift stale again — proven via a live 3-day re-measurement showing the same query already grew 22%), fleet-cron's real limit=1 usage pattern completely unaffected, zero regression across targeted/consumer/full-suite runs, tsc clean, toolCount/cronJobCount unchanged | HEALTHY.

## 2026-08-15 — FACTORY-APP-split-pollNews stage-1 (fetch/health) rework (P0, XL, Review-Lane SECONDARY-Drain, QA CHANGES_REQUESTED redispatch) → review[] for fresh QA pass

**Session:** 632721c2-41e4-4aff-8d06-a47cf80dc0d7. QA CHANGES_REQUESTED on commit 0f23a703f (redispatch_count=1): only 5 peripheral helper clusters were extracted (types/insiderDetectors/signalDedup/defaultFetchers/dbHelpers); pollNews()'s own ~790L pipeline body (fetch/health → dedup/insert → cascade/alert-generation) was untouched, pollNews.ts stayed 923L against the task's own DoD (each module <=120L, thin ~120L orchestrator).

**What shipped this cycle (stage 1 of the "one extraction per commit" ladder the task's own approach field names):** 5 new files under `usecases/pollNews/`, each independently <=120L (no size-justification header needed — unlike 4/5 files QA flagged in the prior commit): `resolveFetchers.ts` (per-cycle fetcher-set resolution incl. Task 1345a Reuters-stale newsapi fallback), `teChromiumRetry.ts` (Task 1821a 0-item cold-start retry wrapper), `sourceHealth.ts` (`SOURCE_DISPLAY_NAMES`/`STUB_CAPABLE_KEYS`/`isNewsapiConfigured`), `fetchAndRecordHealth.ts` (`Promise.allSettled` fetch + `globalSourceTracker` health recording), `allSourcesDarkAlert.ts` (DB-backed cooldown + Telegram alert, caller-owned `DarkAlertState` box so `_resetAllDarkAlert()` still works). `pollNews.ts` 923L → 671L (27% smaller this pass).

**Scope decision:** did NOT attempt the full remaining decomposition in one commit (unlike sibling FACTORY-APP-split-assembleBriefing, which did all 19 steps in one pass) — the ~790L pollNews() body threads shared mutable local state (db/watchlist/entries/signals) across all 3 stages far more tightly than assembleBriefing's 19 independent best-effort steps, and the task's own approach field explicitly names "one extraction per commit." Remaining stages (dedup/insert: Step 2-3; cascade/alert-generation: Step 4-5) are flagged as follow-up in the file header + usecases.md, not silently left implicit — this redispatch continues the ladder rather than re-requesting review with no code change.

**Verified:** pure code motion, zero behavior change. All 209 pollNews-touching test assertions across 32 pre-existing isolated test files pass unchanged (zero internal-symbol imports found — every test exercises `pollNews()` black-box via injected options, confirmed by grep before touching anything). `tsc --noEmit` clean. Gate 2b/2c/2d: `PORT=3099` boot healthy (`/health` 200), toolCount=183/cronJobCount=88 unchanged (no barrel edit). `size-lint-justification.sh --check`: 1 pre-existing failure (`getBctcPendingRefineTool.ts`, untouched by me, confirmed via `git status`/`git log` — unrelated to this task, carried over from a different recent dev-mcp-server fix). Full non-isolated `bun test`: 15302 pass/40 skip/53 fail/48325 expect() (494.10s) — within the documented noise band (prior cycles this session: 51-52 fail); the sole pollNews-titled failure (`push-news: ... accepts tuoitre items via fetcher injection`, 5003.70ms timeout) matches the exact full-suite-under-contention flake class the prior commit (0f23a703f) already documented for this same test file — passed cleanly in the isolated targeted run.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S8. Docs: `usecases.md` (pollNews.ts section rewritten with the 5 new file rows + explicit "remaining stages" note). Commit: `72bcaf940`.

Zone health: real pipeline-body decomposition landed (not just a re-request-review), stage-1 fetch/health cleanly separated with zero regression across targeted/full-suite runs, tsc clean, toolCount/cronJobCount unchanged, remaining 2 stages honestly flagged as follow-up rather than claimed done | HEALTHY.
