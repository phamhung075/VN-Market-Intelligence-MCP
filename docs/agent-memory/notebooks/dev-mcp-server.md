# dev-mcp-server -- Notebook

## 2026-07-10 — FIX-BCTC-D3A-PEK-TRIGGER-HELPER → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (D3a of the FIX-BCTC-PDFPULL-WIRE-TABLE-EXTRACTION chain — D1/D2 landed DONE_VERIFIED; D3b auto-trigger wiring still BACKLOG, blocked on this task)

Pure DRY refactor, no new logic (per the board row's own note). Read `handleTriggerPekExtract` (`bctcVpsIngestHandler.ts:194-280`) first, then extracted its fetch + market-hours-handling block into a new `triggerPekExtractionForReport(reportId, pdfPath, log?)` (`infrastructure/fetchers/pekExtractTrigger.ts`, co-located with `pdfExtractorClient.ts` per the design doc's DDD-layer table). Returns a discriminated union (`queued|market_hours|pdf_extractor_error|unreachable`) — the handler now just translates each outcome into the exact same `res.writeHead`/`res.end` shapes it always produced (verified via `git diff`: response bodies/status codes byte-for-byte unchanged). `log` defaults to the infra singleton `logger` (matches `bctcPdfPullJob.ts`'s own convention — the future D3b caller has no injected per-request logger); the HTTP handler passes its injected `log` through explicitly so its own log call-sites stay identical.

New `FIX-BCTC-D3A-PEK-TRIGGER-HELPER.test.ts` (6 tests, 25 expect() calls, mocked `globalThis.fetch`): 202→queued, 503→market_hours (body forwarded verbatim), non-OK→pdf_extractor_error, thrown fetch error→unreachable, POST body forwards `{report_id, pdf_path}` verbatim, default-param (no log arg) path used by the future D3b caller. Targeted regression (8 files incl. `1112-bctc-vps-proxy`, `bctc-pdf-pull-job`, D1/D2 tests, `pek-render-seam`) 82/82 pass, 287 expect() calls. tsc clean. mock-guard PASS on all 3 changed/new files.

Bare `bun test` hung again (documented S29/S30/S31 pattern) — used `scripts/ci-per-file-isolation.sh 12`: 14295 pass/40 skip/58 fail/1185 files. 3 files SIGILL pass-then-crash (known Bun 1.3.13 teardown crash, summary printed before crash so not counted as fails); 1 file (`FIX-1267-ssc-circuit-breaker.test.ts`) hung near-zero-CPU 5.5min, killed — independently pre-documented flaky (`sprint-FIX-MACRO-THRESHOLD-FXFLOOR-OVERCLAMP-architect.md`: same file flaky on an unrelated diff's unchanged baseline). Grepped all 16 genuinely-failed files for `pekExtractTrigger`/`triggerPekExtractionForReport`/`bctcVpsIngestHandler`/`handleTriggerPekExtract`: zero matches. toolCount=183 unchanged, no scheduler/cron file touched (D3b out of scope). `git status --porcelain -- apps/mcp-server/` confirms exactly 3 files (1 modified, 2 new) — matches the pure-refactor scope claim.

Commit: NOT committed by this agent — router RAW-verifies and commits per explicit dispatch instruction. Board: `ready`→`in_progress`→`review` via orch-apply.sh, `next_agent=qa`.

Zone health: tsc clean, new test 6/6 pass + targeted regression 82/82 pass, full-suite sweep 14295 pass/58 fail (16 files, zero overlap with this diff's touched files), mock-guard PASS, toolCount=183 unchanged | HEALTHY.

## 2026-07-10 — FIX-BCTC-SERVE-GATE-FINANCIAL-REPORTS → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (router-dispatched, chain-continuation of D2's QA finding — D2 landed DONE_VERIFIED, this closes the gap it left)

Architect (STEP architect-S5) already triaged the field-choice trap: `get_bctc_full`'s PUB-1 gate keys on `refine_status`, but that column only transitions via the separate agentic-refine pipeline (`finalizeBctcRefine.ts`) — legacy scalar-pipeline rows (`fetchParseAndStoreBctc`/`storeReport`) never touch it and sit at the DB default forever despite holding real data. Mirroring PUB-1 verbatim onto `get_financial_summary`/`compare_financials` would have hidden legitimate legacy reports — a regression, not a fix. Gated on `validation_status==='pending_extraction'` instead — D2's additive-only enum value, zero collision with legacy values.

Added `validation_status: string` to `ReportRow` (SELECT * already returned it). `get_financial_summary`: new check after the `!row` 404 branch, before `checkBctcIdentityGuard` — returns a plain-text "PDF downloaded but not yet extracted" message (not thrown, not isError). `compare_financials`: same gate after `!row1||!row2`, before the dual identity-guard block — checks both periods individually (mirrors the guard1/guard2 idiom right below it), reports which period(s) are pending.

New `FIX-BCTC-SERVE-GATE-FINANCIAL-REPORTS.test.ts` (6 tests, 20 assertions): pending-extraction row → pending message not 404/real-data; compare_financials one-pending and both-pending; **regression proof** (the exact case architect flagged) — a legacy row at the DB DEFAULT `validation_status='pending'` (`bctc-schema.ts:818`) plus explicit `'passed'`/`'failed'` rows still serve real data unaffected, incl. a legacy-vs-legacy `compare_financials` case. Pending-extraction fixtures built via the real `ensureFinancialReportShellRow` usecase, not hand-rolled SQL.

Targeted regression: 085-tool-reports + D2-shell-row + 1181 + 1086 (16 pass/2 skip/0 fail) plus every test file importing `registerReportTools`/`reports.js` — incl. `fix-bctc-identity-serve-guard.test.ts`'s all 15 `DV-BCTC-GUARD-*` identity-guard cases — 93/93 pass. tsc clean. mock-guard PASS. Bare `bun test` hung (documented S29-S33 sandbox issue, host-safety canonical runner exists precisely to avoid it) — killed, used `scripts/ci-per-file-isolation.sh` P=12: 14303 pass/40 skip/65 fail/16 files, grepped all 16 for this diff's touched symbols — zero matches, same pre-existing pollNews/Chromium/SSC-breaker/pdf-extractor/RAG realm already root-caused in S30/S31/S33. toolCount=183 unchanged (dry-run + live `:3000/health`). No scheduler file touched. Live server-boot probe: module load clean; final `listen()` hit a pre-existing process already bound to :3000 (EADDRINUSE, same S31/S32 precedent) — that live instance's health/bctc-inspect/news-fetch dashboard probes all clean.

Doc-review (interface layer touched): updated `docs/agents/tools/list/get_financial_summary.md`'s Error/No-Data table with the new pending-extraction row (+ the pre-existing but previously-undocumented identity-guard CORRUPT-DATA row, same table, same gap class). FLAGGED not fixed (out of this S-size task's scope): `docs/agents/tools/list/compare_financials.md` documents a DIFFERENT/stale tool entirely — module path `news-analysis/compareTools.ts`, params `stocks[]/metric/periods` — that doesn't match the real live `compare_financials` tool I edited (`financial-reports/reports.ts`, params `actionCode/period1/period2`). Zero accurate per-tool doc exists for the real tool; a full doc rewrite is a separate task, not absorbed here.

Commit: NOT committed by this agent — router RAW-verifies and commits per this session's established discipline. Board: `READY`→`REVIEW` via orch-apply.sh, `next_agent=qa`.

Zone health: tsc clean, new test 6/6 pass, targeted regression 93/93 pass (incl. full identity-guard suite), full-suite sweep 14303 pass/65 fail (16 files, zero overlap with this diff), mock-guard PASS, toolCount=183 unchanged, pre-existing `compare_financials.md` doc-drift flagged (describes a different/stale tool, not fixed — out of scope) | HEALTHY.

## 2026-07-10 — FIX-BCTC-D3B-GATE-PEK-TRIGGERED-STATUS → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (router-dispatched, D3b of the chain — D1/D2/D3A landed DONE_VERIFIED; D3c reconciliation job still BACKLOG, must be dispatched next)

Removed the synchronous 0-row gate (bctc_table_rows/bctc_md_tables count → done/enrich_failed decided right after triggerExtraction) from `bctcPdfPullJob.ts` — `/pek-extract` is 202 fire-and-forget so the check was structurally guaranteed to read 0. After D2's shell-row upsert, now calls D3A's `triggerPekExtractionForReport(shellRow.id, filePath)` (new injectable dep, optional only for pre-D3B test ergonomics, always real in `makeProductionDeps()`) and unconditionally writes `bctc_vps_queue.status='pek_triggered'` — ALL 4 outcomes (202 queued, 503 market-hours, 502 pdf_extractor_error, 502 unreachable) fold into `pek_triggered` (generalized the design doc's explicit 503-folding call to the other 2 undocumented outcomes: no outcome is a proven synchronous verdict at this layer, only D3C's future row-count check is). Legacy scalar `triggerExtraction` (Step 5) UNCHANGED — separate pipeline/tables. Removed the now-dead `enrichFailed` result field + `sendBugFn` option (BUG-notify responsibility moves to D3C).

R-HIGH-1 (folded in): `vpsHealthPoller.ts` + `freshnessSlaMonitorJob.ts` active_count queries and `bctcQueueEnricherJob.ts`'s `isEarningsWindowActive()` now include `pek_triggered` as active/in-progress. Deliberately did NOT add it to bctcQueueEnricherJob's Arm-2 grace-period sweep (would wrongly reset in-flight PEK rows) — documented inline. Repo-wide grep confirmed exactly these 3 files have `bctc_vps_queue.status IN(...)` lists; `fetchStatusHandler.ts`'s dashboard SUM(CASE) counters are a separate display-only pattern, flagged not touched.

Tests: `bctc-pdf-pull-job.test.ts` +4 (TC-14..17: call-contract, 503/error-folding, shell-row-failure-skip), 5 pre-existing status asserts updated. `FIX-BCTC-ENRICH-SILENT-0ROWS.test.ts` REWRITTEN — its whole premise (the removed gate) is obsolete; explicit SUPERSEDED header maps every old AC, 3 tests now prove row-count no longer branches the outcome. `FIX-BCTC-PDFPULL-JOB-OVERLAP-GUARD` + `FIX-BCTC-VPS-QUEUE-SYNC` (1 genuine miss caught by first full sweep, fixed): done→pek_triggered. `FIX-BCTC-FRESHNESS-GATE` +1, `FIX-HEALTH-RECHECK-BCTC-IDLE-VS-CRASH` +1. Full BCTC per-file sweep (~105 files) 0 fail after the fix above; `1294b-bctc-fallback.test.ts` times out standalone — pre-existing/unrelated (imports only `fetchParseAndStoreBctc`, zero overlap; matches prior root-cause note). tsc clean. mock-guard PASS. toolCount=183 + scheduler count unaffected (no tool/cron file touched).

**D3C dependency flag:** any `pek_triggered` row created after this lands has NO path to done/enrich_failed until `bctcExtractReconcileJob.ts` (FIX-BCTC-D3C-RECONCILE-JOB) lands — expected/acceptable for this review window, but D3C must dispatch immediately after DONE_VERIFIED.

Commit: NOT committed by this agent — router RAW-verifies and commits. Board: READY→REVIEW via orch-apply.sh, `next_agent=qa`.

Zone health: tsc clean, new+rewritten tests pass, full BCTC-scoped sweep clean (1 pre-existing unrelated flaky file), mock-guard PASS, toolCount=183 unchanged | HEALTHY.

## 2026-07-10 — FIX-BCTC-D3C-RECONCILE-JOB → REVIEW

**Session:** 5a45feda-431e-46c8-941d-a6539a0eca77 (D3c — closes the chain: D1/D2/D3A/D3B all landed DONE_VERIFIED; this is the FINAL leaf, architecturally urgent since D3B ships `pek_triggered` with no exit path)

New `bctcExtractReconcileJob.ts` (mirrors `bctcPdfPullJob.ts`'s shape: injectable deps, `makeProductionDeps()`, `runXJob(opts)`, `_isRunning` overlap guard). Each tick: selects `pek_triggered` rows past a 5-min grace window, re-derives `report_id` via `(action_code, sort_key)` (the queue row carries NO `report_id` column — this lookup is mandatory, not defensive), checks `(bctc_layout_units WHERE quarantined=0 count>0) OR (bctc_table_rows count>0) OR (bctc_md_tables count>0)`. Success→`done`. Exhausted `MAX_RECONCILE_ATTEMPTS` (new const=8, ~4h budget)→`enrich_failed` + fail-loud `sendTelegramBug` (same mechanism the pre-D3B gate used, reused not reinvented). Under cap→stays `pek_triggered`, `reconcile_attempts++` (NEW dedicated column, idempotent migration in `schema-financial-reports.ts` — kept separate from the pre-existing `attempts` column, which already means something else: pre-download fetch-failure count).

**Re-fire-vs-passive decision (mine to resolve, QA-flagged at D3B):** adopted uniform always-re-fire-on-still-zero (bounded by the same attempt cap). `bctc_vps_queue` has no column recording which of D3B's 4 folded outcomes fired, so a 503/502 row (PEK never started) and a 202 row (genuinely in-flight) are indistinguishable at this layer — passive-only re-check would leave the whole 503/502 class stuck until it silently exhausts into `enrich_failed` with zero real retries. Re-firing an already-in-flight 202 is cheap+safe (`pushBctcLayoutHandler.ts`'s DELETE-before-INSERT makes repeat `/pek-extract` idempotent).

New cron `CRONS.bctcExtractReconcile = '5,35 * * * *'` (design doc's own suggested offset), registered in `schedulerJobTable.ts`'s `JOB_TABLE` array (58 entries now, was 57 — generic `scheduleCron`/`wrapRun` wiring, confirmed by reading the consumption loop).

New `bctc-extract-reconcile-job.test.ts` (14 tests, 56 expect() calls): empty queue, grace-window skip, 3 tables tested independently for the OR-success-check, quarantined-only-does-NOT-count, under-cap re-fire (reportId/pdfPath asserted), re-fire skipped-but-attempt-still-advances when no report_id resolvable, exhausted→enrich_failed+bug-fired-once (incl. an unresolved-report_id variant + a sendBugFn-throws-non-fatal variant), report_id re-derivation independent of insertion order, overlap guard. tsc clean. mock-guard PASS on all 5 touched/new files. Full BCTC-scoped per-file sweep (105 files, incl. this new file) 1153 pass/8 fail/5 skip — all 8 fails are the same pre-existing `1294b-bctc-fallback.test.ts` standalone timeout already root-caused at S30/S31/S33/S35 (grepped for any `bctc_vps_queue`/`pek_triggered`/`reconcile_attempts` overlap — zero). toolCount=183 unchanged. Gate-2d's `cron.schedule` literal-grep probe is (still, pre-existing) stale post-refactor (only 1 file matches now regardless of job count, since a `scheduleCron()` wrapper centralizes registration) — reported the real `JOB_TABLE` entry delta instead. Live server-boot: module load + new migration + full scheduler registration clean, zero import errors; final `listen()` hit the same pre-existing `EADDRINUSE` (:3000 already held by a live Docker container) as every S31-S35 probe — that live instance's `/health` + `/api/bctc-inspect` still respond clean post-diff.

Docs: `docs/architecture/microservice/mcp-server/financial-reports.md` (+2 Scheduler-Jobs rows, +invariant #8 state-machine note — did NOT rewrite the file's pre-existing stale push-based invariant #5, out of scope), `docs/data/system-map.json` (SSOT crons 65→66) + `docs/data/cron-registry.json` (mirror, 64→65) both updated + re-validated as well-formed JSON. `docs/data/project-stats.json#cronJobCount` deliberately NOT regenerated — its generator shares the same stale literal-grep source of truth, would just write a differently-wrong number.

Commit: NOT committed by this agent — router RAW-verifies and commits per the D1/D2/D3A/D3B dispatch discipline. Board: `READY`→`REVIEW` via `orch-apply.sh` (new dev-side guard script `scripts/dev-fix-bctc-d3c-reconcile-job-ready-to-review.jq` — no pre-existing dev-side ready→review script found for D1-D3B, written following the QA-side scripts' guard-refuse convention), `next_agent=qa`.

Zone health: tsc clean, new test 14/14 pass (56 expect()), full BCTC-scoped sweep 1153 pass/8 fail (1 pre-existing unrelated file, zero overlap), mock-guard PASS, toolCount=183 unchanged, this is D3C — the pek_triggered→done/enrich_failed path now exists end-to-end | HEALTHY.
