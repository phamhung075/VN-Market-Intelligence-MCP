# dev-mcp-server -- Notebook

## 2026-07-13 — ALPHA-S1-OHLCV-BACKFILL-DONE-BUG (wave-1, P1) → REVIEW

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (dev-team dispatch; architect design `docs/handoffs/ALPHA-S1-architect-design.md` §3, fully implemented as specified)

RAW-probed the live named-volume DB first (docker exec, readonly bun:sqlite): `ohlcv_backfill_queue` had 650/650 rows `done=1`, 0 pending, no `bars_inserted` history ever existed (the bug's own footprint — nothing to retroactively reconstruct). Corroborated the defect independently via 7-day container logs: 68 real `push-ohlcv-history` zero-insert events (`"count":0`) silently returned `ok:true`. Fix: added nullable `ohlcv_backfill_queue.bars_inserted` column (`schema-market-data.ts`, same idempotent-ALTER idiom as `retry_count`) + inserted-count verification in `handleOhlcvBackfillDone` (`ohlcvBackfillHandler.ts`) — `barsPushedTotal===null` (no authoritative report landed) or `===0` on the row THIS call actually closed now re-queues via a new `done=0` row (reusing the existing R-5 retry/escalate ladder), mutually exclusive with the pre-existing depth probe to avoid a double-fire. `done` still flips unconditionally — preserves the VPS poller's documented "regardless of exit code" unblock contract.

Added BT-6/7/8 to `ohlcv-backfill-done-subtask-b.test.ts` per architect spec (zero-insert re-queue + bars_inserted persisted; no-report re-queue even with healthy watchlist depth; retry-cap escalation). Unplanned fallout: pre-existing `1360-ohlcv-backfill-queue.test.ts` TC-6 asserted the OLD buggy assumption (empty-body close + healthy depth = no re-queue) — updated to the corrected behavior since it was directly exercising the exact scenario this fix corrects.

Verified: targeted 109/109 pass (5 files incl. both directly-touched suites), `bun tsc --noEmit` clean, tool count 183 unchanged (gen-project-stats dry-run). Bounded full-suite run completed this cycle (14549 pass/40 skip/67 fail/5 errors across 1199 files, then the documented Bun-native tail-crash) — grepped clean for `ohlcv-backfill`/`bars_inserted`/`ALPHA-S1`/`1360-ohlcv`/`subtask-b`, zero hits among the 67 pre-existing fails (pollNews/insider/VPS-proxy-health/foreign-flow-read/deprecated-TA, unrelated). Gate 2b (local server boot on :3000) skipped — port already bound by the LIVE market-hours container (DEPLOY GATE: VN market open, no container touch); in-process `createBunServer` boots inside the test runs themselves as the substitute proof (toolCount=183 logged each run).

**DEPLOY-REQUIRED:** code committed only, not live — the running container still executes pre-fix logic. Rebuild/verify command (user/ops-gated, off-market window): `docker compose up -d --build mcp-server` then re-run the live verification in the architect design's Shared verification gate (§ post-implementation, confirm `ohlcv_backfill_queue.bars_inserted` populates non-NULL after the next real backfill cycle; live-trigger a `bars_pushed_total:0` test call, confirm exactly one Telegram BUG).

**Remediation proposal for the 457 historical done-with-zero-bar rows (NOT executed — flagging only):** no ground truth exists to retroactively identify which of the 650 historical closures actually inserted zero (the missing column IS the bug), so no historical UPDATE is proposed. Forward-looking, non-destructive option: after deploy, either let the next `vn-ohlcv-backfill.timer` cycle (30-min systemd oneshot, already running) exercise the new fail-loud path naturally, or manually force one cycle via the existing canonical `scripts/migrations/trigger-ohlcv-backfill-queue.ts --apply` (dedup-guarded INSERT, no new script needed) — but only AFTER deploy, since triggering it against the still-unfixed live container would just reproduce the bug once more.

Zone health: tsc clean, 109/109 targeted pass (incl. 8+9 in the 2 directly-touched files), toolCount=183 unchanged, full-suite 67 pre-existing fails unrelated (grepped) | HEALTHY.

## 2026-07-13 — ALPHA-S1-STARTUP-CANDLE-GUARD (wave-1, P1) → REVIEW

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (dev-team dispatch; architect design `docs/handoffs/ALPHA-S1-architect-design.md` §2)

Sibling `ALPHA-S1-CANDLE-RECOVER` closed as WEEKEND-BLIND FALSE-POSITIVE and shipped NO code, so its `recoverMissingOhlcvSession()` primitive (originally §1 of the same design) was built here instead — net-new, this row's own scope. Added: `vnTradingCalendar.ts` additive exports `shiftDateDays()` + `mostRecentTradingDayOnOrBefore()` (bounded 14-day backward walk over `isVnTradingDay`); `application/usecases/recoverMissingOhlcvSession.ts` (idempotent no-op → tick-survival probe → re-aggregate via the existing aggregator's injectable `nowMsFn` OR trigger the VPS-relay queue); `scheduler/market-data/ohlcvCandleGuard.ts` (`runOhlcvCandlePresenceGuard` — resolves the expected VN trading session via the calendar walk-back, checks watchlist+VNINDEX presence, one WORK alert + recovery call on a genuine gap, fail-loud via `sendTelegramBug` + re-throw on a recovery exception). Wired at 2 call sites: `startScheduler.ts` (static import, same phase as `runOhlcvStartupProbe`) and a trailing step inside `runOhlcvDailyAggregator` itself (checks the PREVIOUS trading day via `nowMs - 24h` — daily self-heal, no restart dependency); the trailing site uses a **dynamic** import specifically to break a 3-node static circular import (`ohlcvCandleGuard → recoverMissingOhlcvSession → ohlcvDailyAggregatorJob`), same deferred-load idiom `ohlcvStartupProbe.ts` already uses.

**Bug found + fixed in the architect's own illustrative snippet** (not a re-architecture — the orchestration/branch structure is unchanged): the re-aggregation branch's `nowMsFn` must resolve to the END of the VN session window (`vnNextMidnightUtcMs - 1`), not "VN midnight + 5h" as literally written — the aggregator's window is `[vnMidnight(nowMs), nowMs)`, so an early `nowMs` truncates capture to the pre-market hours and silently drops the actual afternoon-session close, corrupting the recovered candle. Verified via a direct unit test (REC-2) with 3 ticks spanning the full session.

**Fail-loud design note:** the guard does NOT invent a new retry-cap ladder. A genuine exception from `recoverMissingOhlcvSession` escalates immediately via `sendTelegramBug` + re-throw (never swallowed). A *persistent* trading-day gap that routes through the VPS-relay branch is escalated to exactly one Telegram BUG at retry-cap by the ALREADY-EXISTING R-5 ladder inside `handleOhlcvBackfillDone` (sibling commit `599f4aee0`) once the VPS poller reports back — this guard only needs to correctly trigger that existing pipeline via the `ohlcv_backfill_queue` row.

New test file `ALPHA-S1-STARTUP-CANDLE-GUARD.test.ts` (14 cases, own in-memory `bun:sqlite` DB, no `createBunServer` needed — neither function is an HTTP route): domain helper sanity (weekend walk incl. a real VN holiday block 04-30/05-01→04-29), `recoverMissingOhlcvSession`'s 4 branches direct, and the guard's calendar-skip (weekend + holiday), real-gap catch-up, VNINDEX-only gap, and fail-loud-not-swallowed paths — 14/14 pass. `tsc --noEmit` clean. Regression 93/93 pass across 7 related suites (aggregator×4, is-trading-day, backfill-done-subtask-b, contam/pred/guard-checks/volume-unit/diacritics). A combined 12-13-file single-invocation run showed 1 pre-existing unrelated flake (`getVpsProxyHealth` export SyntaxError, HMR/module-cache artifact) — confirmed via `git stash` against the clean baseline that the identical failure reproduces with my changes fully removed, so it predates this task. `mock-guard.sh` PASS. DDD grep: `domain/` zero infra imports (golden rule intact); `recoverMissingOhlcvSession.ts` (application) importing `runOhlcvDailyAggregator` (scheduler) matches the architect's design verbatim (disclosed, not a new pattern) — the only cross-cycle static edge is type-only (erased at compile time), the value-level edge is broken via the dynamic import above.

Disjoint file set from sibling `ALPHA-S1-OHLCV-BACKFILL-DONE-BUG` (`ohlcvBackfillHandler.ts`, untouched). Commit `1bbc8cead` (6 files: 3 new, 3 edited). Board: moved in_progress→review, `deploy_pending:true`, top-level `.head.next_agent="qa"` synced (not the decorative `.task_board.head`, per standing lesson). **DEPLOY-REQUIRED:** code only, not live — batch with `599f4aee0` into one off-market `docker compose up -d --build mcp-server` (VN market OPEN at task time, deploy is user/ops-gated).

Zone health: tsc clean, 14/14 new + 93/93 regression pass, toolCount unaffected (no MCP tool surface touched), mock-guard PASS | HEALTHY, REVIEW pending QA + deploy.

## 2026-07-13 — VCB-MISSING-PDFS (BOUNDED-1 auto-pickup, P2/XS) → REVIEW

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (dev-team BOUNDED-1 idle-capacity auto-pickup)

Pick-time pre-verify (row is 5 weeks old) found the board's claim was half-stale: docker exec RAW-probe of the live named-volume market.db confirmed `VCB_2025_Q4.pdf` is genuinely gone from `data/pdfs/` (feedback id=323, 271 reparse_attempts, live log confirms "file disappeared before reparse" as recently as 2026-07-11T11:48Z) but `VCB_2025_Q1.pdf` is actually PRESENT on disk right now (0 attempts, no spin — board desc wrong for this half). `financial_reports` already holds valid VCB Q4-2025 AND Q1-2025 rows parsed under their real canonical filenames — re-sourcing the duplicate `VCB_2025_Q4.pdf` name would add nothing, no data is actually missing.

Root-cause fix: added `DEAD_AT_ATTEMPTS(10)` guard to `bctcReparseJob.ts`'s feedback-row retry loop — a row failing past 10 attempts AND confirmed file-missing now flips to `status='dead'` (never `'resolved'` — honest, it was never reparsed) instead of spinning forever. Threshold-gated so it doesn't fire before the existing escalate(3)/alert(5) budget, and doesn't break the 6 existing test files whose synthetic `/tmp/*` seed paths never exist (their `reparseFn` stubs bypass real fs). Confirmed this is a GENERIC recurring class, not VCB-only: 6 other tickers (NVL/HCM/VHM/HSG/CTG/KBC/REE) sit at 99-100 attempts too, but their files are still present — a different, still-open failure mode, correctly left untouched. New generic script `scripts/migrations/reap-dead-stranded-bctc-rows.ts` (no ticker literals; dry-run default) ran `--apply` live to retire id=323 immediately, independent of any rebuild.

Verified (scoped, deterministic): `1019-bctc-reparse-job.test.ts` 21/21 pass (17 pre-existing + 4 new dead-row cases, 53 expect() calls); new script's own test file 9/9 pass; `tsc --noEmit` clean; toolCount=183 unchanged (scheduler-internal change, no MCP tool touched). One full-suite run (bls39jc60) completed clean before a coordinator note flagged the full local suite as unreliable in this env (2nd run died mid-run on an unrelated chromium/pollNews crash-loop, killed) — the completed run showed 14573 pass/65 fail/4 err, grepped for bctcReparseJob/VCB/stranded_bctc_pdf/reap-dead: 0 hits among all fails; scoped runs above are the authoritative gate per dispatcher guidance.

**DEPLOY-REQUIRED:** `DEAD_AT_ATTEMPTS` code guard needs an mcp-server rebuild to self-heal FUTURE occurrences live (user-gated, not self-deployed). Live-DB cleanup for id=323 is already in effect right now via the migration script, independent of the rebuild.

Zone health: tsc clean, 21/21+9/9 scoped pass, toolCount=183 unchanged, full-suite fails unrelated (grepped) | HEALTHY, REVIEW pending QA + deploy.

## 2026-07-13 — HPG-DISCOVER-CONSOLIDATED-PDF (BOUNDED-1 auto-pickup, P2/S) → partial, still IN_PROGRESS

**Session:** 69b0312e-df43-43a9-9e0b-bddf66d374e3 (dev-team BOUNDED-1 idle-capacity auto-pickup)

RAW-verified the board's own note: HPG 2025-Q4's `financial_reports` row (id `918a7abd-...`) and `bctc_vps_queue` row (id 223) both pointed at `...Bao-cao-tai-chinh-rieng-Cong-ty-me...` (standalone parent-company filing), enriched 2026-04-27 — BEFORE `FIX-CTG-1` (2026-06-03) added "hop nhat" (consolidated) ranking preference to `hsxBctcFetcher.ts`. Confirmed via a live call to hsx.vn's mediafiles API that the real consolidated sibling (`...Bao cao tai chinh hop nhat va giai trinh Q4.2025.pdf`) has always existed in the same folder (id 2435680). Wrote generic `scripts/migrations/discover-consolidated-bctc-pdf.ts` (ticker/year/quarter args, no hardcoding) that re-runs discovery with the current ranking logic, detects the mismatch, and on `--apply` downloads + re-drives the correct PDF through the live push-bctc-pdf pipeline.

Ran `--apply` live: downloaded the real consolidated PDF (33p, 7,135,524 bytes — byte-exact match to hsx.vn's Content-Length), reset `bctc_vps_queue` row 223 `done→pending`, POSTed it through `/api/push-bctc-pdf` — queue row's `source_url` is now correctly the consolidated URL and file sits in `data/pdfs/` (bind-mounted, live in container). Full scalar reflow into `financial_reports` (pdf_path/net_revenue/etc.) did NOT complete: `pdfExtractorClient.ts`'s Tier-1 call has a hardcoded 120s timeout, but OCR of this scanned 33-page doc genuinely exceeds it (confirmed via a direct 480s call to pdf-extractor that still didn't return; `ps aux` showed one single tesseract page running continuously for 17+ minutes). Did not blind-bump the timeout — could not verify a fix works within a bounded session, and a code change here needs a container rebuild (deploy-gated, not self-deployed). Killed the stray tesseract processes I'd stacked from retries to stop wasting shared pdf-extractor capacity.

Mid-task the mcp-server crash-looped (SQLite corruption, 15:37–15:50Z) and was restored from a 04:30Z backup — my first in-flight queue-reset got wiped; re-verified post-recovery state matched pre-crash reads (data predates 04:30Z) and redid the apply cleanly against the healthy container.

Zone health: tsc clean, scoped `BCTC-3b-hsx-fetcher.test.ts` 9/9 pass (logic mirrored, not modified), toolCount=183 / schedulerFileCount=64 unchanged (no src touched) | HEALTHY. Left status as-is (not self-flipped to REVIEW) — router's dispatch explicitly said it will RAW-verify + flip; deeper OCR-timeout gap flagged as a separate follow-up defect, not absorbed into this task's claim.
