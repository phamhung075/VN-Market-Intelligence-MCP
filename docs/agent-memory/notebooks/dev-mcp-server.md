# dev-mcp-server -- Notebook

## 2026-07-24 — FACTORY-INFRA-split-ssc-fetchers (BOUNDED-1 auto-pickup, pure structural split) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: split `ssc.ts` (1029L — SSC/HOSE/HNX/UPCOM disclosure fetchers bundled together) into one module per portal. Explicit no-dedup-delete scope note honored.

Split: `sscCommon.ts` (SscDocument/HttpClient types, makeDefaultHttpClient, titleMatchesReportType — shared plumbing), `sscPortal.ts` (SSC congbothongtin.ssc.gov.vn: buildSscSearchUrl/parseSscHtml/withBrowserLock/`_runSscPath`), `hoseDisclosure.ts`, `hnxDisclosure.ts`, `upcomDisclosure.ts` (named `*Disclosure.ts`, not `hose.ts`/`hnx.ts`, to avoid colliding with the existing unrelated market-price fetchers of those names already in `infrastructure/fetchers/`). `ssc.ts` (1029L→186L) kept to `listSscDocuments`/`listSscDocumentsWithFlag` orchestration, re-exporting the full pre-split public surface (types + all per-portal fetch/parse functions) so every existing `from "ssc.js"` call site (7 production + 16 test files) is unaffected — mirrors this session's newsNormalizer split precedent. Note: the canonical backlog ticket for this task specifies a `disclosures/` subfolder + `<=120L`-per-file cap; followed the dispatcher's explicit newsNormalizer-precedent instruction instead (flat siblings, no cap — `sscPortal.ts` lands at 388L) — flagged in the decision journal for QA visibility.

Evidence: code-only diff (comments/imports/type-exports stripped, statement sets sorted) shows the ONLY delta vs. original is 3 previously-private functions (`_runSscPath`, `makeDefaultHttpClient`, `titleMatchesReportType`) gaining `export` (needed cross-module) + 5 re-export barrel lines — zero logic drift. `bun tsc --noEmit` clean. Targeted 9-file ssc/disclosure suite: 85/85 pass, identical before/after. Full `bun test`: 14734 pass/40 skip/43 fail/46780 expect() calls — grep-confirmed zero fail-line overlap with ssc/sscPortal/sscCommon/hoseDisclosure/hnxDisclosure/upcomDisclosure; all 43 match today's already-documented pre-existing flaky classes (vps_push_log schema-drift, 5000ms MCP-client-timeout, deprecated 1302-technical-indicators), within the 41-44 range logged twice already today as flaky-not-deterministic. `gen-project-stats.ts --dry-run`: toolCount 184/cronJobCount 88 unaffected. Server boot probe (PORT=3099): health 200 toolCount=184, both dashboards 200. Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-INFRA-split-ssc-fetchers-dev-mcp-server.md`.

Zone health: tsc clean, targeted 85/85 identical before/after, full-suite pre-existing flake confirmed zero overlap (43 fails, grep-verified), tool/cron counts unaffected, server boot clean | HEALTHY.

## 2026-07-24 — FACTORY-INFRA-split-stores-and-migrations (BOUNDED-1 auto-pickup, CODE-ONLY, rebuild PENDING) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: split vnstockStore.ts (937L, 8 entities + migrations) into `infrastructure/db/vnstock/` per-entity modules + make schema-financial-reports.ts's ~22 inline `if(!colNames.has)ALTER TABLE` migrations declarative. **CRITICAL SCOPE BOUND honored: CODE-ONLY, no rebuild, no live-DB touch** — rebuild + named-volume-DB RAW-verify are user-gated and explicitly PENDING.

Split into `vnstock/{financialsStore,tradingStatsStore,officersStore,shareholdersStore,eventsStore,balanceSheetStore,cashFlowStore,foreignFlowStore}.ts` (one per entity) + `fetchLog.ts` (shared isStale/markFetched, not entity-specific) + `migrations.ts` (runVnstockMigrations verbatim). `vnstockStore.ts` is now a 37L pure re-export barrel. `foreignFlowStore.ts` (240L) and `migrations.ts` (256L) carry size-justification comments — dual-schema upsert logic and pure-DDL migration sequence respectively, both load-bearing on the hottest write path, fragmenting further would raise risk not lower it. schema-financial-reports.ts's 22 financial_reports-table column migrations (confirmed exact count across 7 formerly-separate try/catch blocks) collapsed into ONE `FINANCIAL_REPORTS_COLUMN_MIGRATIONS: {column,ddl}[]` array + ONE `runColumnMigrations()` loop; `initFinancialReportsTables` stays a thin orchestrator. Left 4 OTHER-table migrations (bctc_vps_queue/vnstock_trading_stats/vnstock_officers/bctc_table_rows) untouched — out of the ticket's financial_reports-scoped count.

Equivalence proof built as an ACTUAL A/B diff, not hand-waved: copied the pre-refactor schema-financial-reports.ts verbatim from `git show HEAD:...` into a sibling temp file (deleted after use, never committed), ran old-vs-new `initFinancialReportsTables` against separate fresh in-memory DBs in the SAME process, diffed `PRAGMA table_info` — 5/5 pass, byte-identical column set (name/type/notnull/default) + index + idempotent + 6 unrelated tables untouched. Barrel re-export verified by strict reference equality (`barrel.fn === subfolder.fn`) across all 19 exports — proves pure delegation, not a drifted copy. Both proofs are now a permanent test: `FACTORY-INFRA-split-stores-and-migrations.test.ts` (23 assertions).

Evidence: `bun tsc --noEmit` clean. Targeted suites: 189 vnstock/foreign-flow (19 files) + 233 financial-reports/schema (12 files) + 23 new equivalence — all green, matching pre-change baseline exactly. Full `bun test` (~1220 files, 3 runs for determinism check): settled at 14743 pass/40 skip/57 fail vs. a true pre-change baseline of 14729 pass/40 skip/48 fail/2 errors captured before touching any file. Set-difference (not string-diff) of failure names: exactly 8 new failures, ALL in `vnstock-3statement.test.ts` (a file I did NOT touch) — root-caused to a PRE-EXISTING, already-documented-in-that-file's-own-comments Bun `mock.module` cross-file cache-pollution hazard from `1466-sync-db-corruption-bail.test.ts`; confirmed via isolation (standalone 10/10 pass, paired-with-1466 13/13 pass) that this is a shared-process-only, order-sensitive flake unrelated to this refactor's logic. My own test file's first draft had a functional-round-trip section relying on the same shared-singleton pattern used by ~15 pre-existing vnstock test files; it inherited the SAME pre-existing hazard in the full run, so it was removed (zero net coverage loss — those ~15 files already exercise the same round-trips and were re-verified green in targeted runs) rather than shipping a second flaky witness to an already-known issue.

Decision journal (detailed A/B transcript + full root-cause writeup): `docs/agent-memory/decisions/sprint-FACTORY-INFRA-split-stores-and-migrations-dev-mcp-server.md`. Commits: `76c73b4c1` (code, 13 files) + `abaf44fd1` (journal).

Zone health: tsc clean, all directly-relevant suites green (189+233+23), full-suite delta traced to a documented pre-existing test-infra hazard unrelated to this change, rebuild+RAW-DB-verify explicitly PENDING (user-gated, not run) | HEALTHY.

## 2026-07-24 — FACTORY-SCHEDULER-dedup-briefing-formatters (BOUNDED-1 auto-pickup, CODE-ONLY, rebuild PENDING) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: extract shared briefing formatters + break the franceSummaryJob↔morningBriefingJob↔eveningSummaryJob job→job cross-import cycle. **CRITICAL SCOPE BOUND honored: CODE-ONLY, no rebuild, no live-container verify** — user just rebuilt, this batches onto a future one.

Created `scheduler/briefings/format/{globalSnapshotSection,foreignFlowSection,moversSection,alertLines,vnIndexFreshness}.ts` (42L/45L/114L/98L/27L — all ≤120L) — verbatim moves of the 5 formatters the ticket named, including their private leaf helpers (`deltaArrow` duplicated into globalSnapshotSection.ts — also still needed privately by morning's `formatCommoditiesSection`; `fmtVolume`/`fmtMoverLine`/`MoverEntry` into moversSection.ts; `severityLabel`/`AlertDisplayRow` into alertLines.ts). Only 3 of the 5 were actually cross-imported (france→morning `formatGlobalSnapshotSection`, france→evening `formatForeignFlowSection`+`isVnIndexFresh`, evening→morning `formatGlobalSnapshotSection`) — extracted all 5 per the ticket's explicit list anyway, consolidating the full formatter family in one pass. All 3 job files now import from `./format/*.js`; `grep 'from "./morningBriefingJob.js"\|eveningSummaryJob.js"\|franceSummaryJob.js"' scheduler/briefings/*.ts` returns zero — cycle confirmed gone. Removed now-dead `sectorPeers.js` import from eveningSummaryJob.ts and `TelegramMessageFactory` import from franceSummaryJob.ts (both orphaned once their sole consumer moved out). Test-import handling: kept thin `export { X } from "./format/x.js"` re-exports on all 3 job files (not test-path rewrites) — 9 test files import these formatters directly from the job files; re-export barrel matches this session's own ssc.ts/vnstockStore.ts split precedent.

Equivalence: byte-diffed all 9 moved function bodies (`git show HEAD:<old> | sed -n '/^function X/,/^}/p'` vs new file) — every diff empty (identical). Targeted 9-file suite (1783-foreign-flow-bulletin, 1503-ohlcv-foreign-flow, 1784-sector-alert-format, 1552-evening-vnindex-freshness, 1511/1512-*global-snapshot, 1424-evening-sector-aggregation, 1794-eod-vol-rsi, FIX-DIGEST-FOREIGN-FLOW-ZERO-PAD-TOPN): 89 pass/0 fail/188 expect() calls before AND after — identical.

Evidence: `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount 184/cronJobCount 88 unaffected. Local server boot probe (PORT=3099, not a container rebuild): no import errors, "90 cron keys" registered, health 200 toolCount=184, both `/api/bctc-inspect` + `/dashboards/news-fetch/` 200. Full `bun test` run twice: 14748 pass/40 skip/52 fail (run1) and 14747 pass/40 skip/53 fail/1 error (run2) — grep-confirmed zero `(fail)` lines or file-section headers reference `briefings/`, `franceSummaryJob`, `eveningSummaryJob`, `morningBriefingJob`, or `format/`; the 1-2 test swing between runs matches today's already-documented flaky-not-deterministic pattern (5000ms MCP-client-timeout class). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-SCHEDULER-dedup-briefing-formatters-dev-mcp-server.md`.

Zone health: tsc clean, targeted 89/89 byte-identical before/after, full-suite flake confirmed zero overlap via grep (2 runs), tool/cron counts unaffected, local server boot clean, rebuild+live-container-verify explicitly PENDING (user-gated, not run) | HEALTHY.
