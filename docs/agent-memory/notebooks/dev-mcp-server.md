# dev-mcp-server -- Notebook

## 2026-07-23 — ALT-FUNC-02-FIX (BOUNDED-1 idle-capacity auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: `get_alert_accuracy` must return `accuracy_rate` in [0,1]. Observed signal `qc-ALT-FUNC-02-4016`: `accuracy_rate=MISSING`, `insufficientSample=true`, `scored_pct=35%`, 616 alerts all unknown. Finding: NO fix needed — `alertAccuracy.ts` already returns top-level `accuracy_rate:number|null` (null only when `hits+misses===0`), shipped in commit `815ccaedd` (2026-06-10T17:24Z, comment "ALT-FUNC-02 FIX"). Backlog row was created 2026-06-10T09:27Z, ~8h *before* that fix landed — a stale pre-fix snapshot BOUNDED-1 auto-picked 6 weeks later. `docs/data/quality-checklist.json` already carries `ALT-FUNC-02: status=PASS, last_verified=2026-06-10T18:00Z` citing the same commit.

Live verification via `scripts/agents-flow/mcp-call.sh` → `get_alert_accuracy {}` returned `accuracy_rate:1, insufficientSample:false, scored_pct:19, total:628, hits:110, misses:0, unknowns:518` — a genuine 0..1 rate, contract satisfied live (data has since accumulated 110 scored alerts, unlike the stale-signal snapshot). Targeted tests `1982-quality-burndown-CHIJ.test.ts` "FIX 3 — ALT-FUNC-02" (3 cases: emptyReport null, insufficientSample null, in-range number) + `183-alert-accuracy.test.ts` + `1847d-D-alert-tools.test.ts`: 46/46 pass. `bun tsc --noEmit` clean. Tool count 184 / cronJobCount 88 unaffected (gen-project-stats probe). `git status apps/mcp-server/` clean — zero source files touched.

Zone health: tsc clean, ALT-FUNC-02 targeted suite 46/46 green, verification-only (no code change, stale orphan backlog row), tool/cron counts unaffected | HEALTHY.

## 2026-07-23 — BCT-OBS-01-FIX (BOUNDED-1 idle-capacity auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: does `dataAuditDaily` produce a data-integrity report sent to WORK channel? Signal `qc-BCT-OBS-01-4033`. Finding: NO fix needed — `runDailyAudit` (`dataAuditJob.ts`) unconditionally writes `system_logs('data-auditor')` + `audit_state`, then `maybeSendTelegram` calls `sendTelegramWork` whenever `hasIssues` (any `auto_cleaned>0` or `flagged`/`escalated` finding). Live-probed via direct DB read (not self-report): `cron_job_runs` shows 4 consecutive real fires 07-19..07-22 (16:00 UTC = 23:00 GMT+7, matches `CRONS.dataAuditDaily`), all `status=success`; each wrote a `system_logs` row with nonzero cleaned+warnings (07-22: 6763 cleaned/63 warn; 07-21: 18516/63; 07-20: 3931/149; 07-19: 199747/227) — `hasIssues=true` every single run, so the WORK-channel send-gate fires daily.

Live gateway call (`mcp-call.sh get_system_status {}`, fresh this session) confirms `TELEGRAM_BOT_TOKEN`/`TELEGRAM_INFO_WORK_CHANNEL_ID` SET and `TELEGRAM_ENABLED=true` in the running container — the exact preconditions `sendTelegramWork`/`coreSend` require. Source-read of `infrastructure/notifiers/telegram.ts` confirms `sendTelegramWork` has ZERO DB persistence by design (only `sendTelegramBug`->`insertReport` persists to `telegram_reports`, and `sendTelegramMarket` to `market_messages`) — so the checklist row's documented `recheck_how` (`read_telegram_reports {channel:'work'}`) can never observe a WORK send; independently confirms the checklist's own `"original FAIL was wrong recheck_how"` annotation is correct, not stale. `docs/data/quality-checklist.json` BCT-OBS-01 already `status=PASS` since 2026-06-10T18:00Z citing WORK-channel msg IDs 2743-2766 (a genuine historical live-Telegram observation, not DB-derivable). Unit coverage `157-data-audit-job.test.ts` AC-6 (silent on clean DB) + AC-7 (sends when issues exist): 18/18 pass — contract matches live DB evidence. `bun tsc --noEmit` clean. Tool count 184 / cronJobCount 88 unaffected (gen-project-stats probe). `git status apps/mcp-server/` clean — zero source files touched.

Zone health: tsc clean, 157-data-audit-job targeted suite 18/18 green, verification-only (no code change), tool/cron counts unaffected | HEALTHY.

## 2026-07-23 — BCT-OBS-02-FIX (BOUNDED-1 auto-pickup, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: does `sscCheck` (20:00 VN nightly BCTC check) send its result to WORK? Finding: REAL fix needed (opposite of the two sibling OBS checks above) — RAW-verified at source, not the checklist self-report: zero `sendTelegram*`/`telegram` references in `sscCheckerJob.ts` or `checkSscReports.ts`, across their FULL git history (`f3eeb9b7d..791b0e4dc`). `docs/data/quality-checklist.json`'s BCT-OBS-02="PASS" (evidence "WORK-channel msg IDs 2743-2766") is a false-positive: live signal msg 3628 proves `read_telegram_reports`'s `channel` param is a zod-schema no-op, so that recheck could never have actually confirmed WORK. Independently corroborated by this session's own BCT-OBS-01-FIX entry above: `sendTelegramWork` has zero DB persistence by design — `read_telegram_reports` can structurally never show a WORK sscCheck message either way.

Fix: `runSscCheck()` now posts exactly one WORK summary message per executed nightly cycle (VPS-only no-op skip / full run / unhandled error) via an injectable `sendWorkAlertFn` defaulting to `sendTelegramWork` — mirrors the `bctcOverdueCheckJob.ts` FR-OBS-01-FIX precedent (commit `7ce61568e`). Concurrency-guard and T4 recovery-dedup early-returns stay silent (nothing new happened). Added `checkSscReportsFn` + `enableLocalBctcFetchOverride` test-only DI seams (mcpConfig is a module-load-time singleton, cannot be flipped by an env-var mid-test-run) so both branches are behaviorally testable without network/DB.

New test `FIX-BCT-OBS-02-SSCCHECK-WORK-ALERT.test.ts` (5 cases: skip-message, full-run-message, concurrency-silence, send-failure-non-fatal, T4-dedup-silence) — all GREEN. `bun tsc --noEmit` clean. Full suite: 14684 pass / 42 fail / 1 error — RAW-confirmed pre-existing via git-stash A/B (identical 2 failures reproduced with the fix stashed out): RSS-degradation timing test + deprecated technical-indicators golden-output test, zero "ssc"/"BCT-OBS" hits anywhere in the failure log. Docs updated: `docs/standards/cron-jobs.md`, `docs/architecture/microservice/mcp-server/news-analysis.md`. Tool count 184 / cronJobCount 88 unaffected (no new MCP tool or cron; existing cron unchanged).

Zone health: tsc clean, 5/5 new + 9/9 adjacent sibling tests green, docs updated, tool/cron counts unaffected | HEALTHY.

## 2026-07-23 — DS-OBS-01-FIX (dev-team dispatcher assign, direct FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: does `freshnessSlaMonitor` send a WORK/BUG telegram alert when any of its 12 signal sources exceeds `stale_threshold`? Structurally identical audit-then-fix to the BCT-OBS-02-FIX sibling above. Finding: REAL fix needed. RAW-verified at source: `escalateToCommander()` (the default escalation callback fired on breach, post cooldown/market-hours gate) only writes an `agent_signals` row (`urgent_news`, `toAgent:"alert-commander"`) — never calls `sendTelegramWork`/`sendTelegramBug`. `docs/agents/alert-commander/flow/stage-signals.md` (live, 2026-07-12 finding) confirms alert-commander correctly SUPPRESSES these synthetic `freshness-sla-monitor` `urgent_news` signals every cycle as infra noise — they never satisfy the position-danger/watchlist-opportunity/CRITICAL-override MARKET firing gate — so the signal-bus escalation is a genuine dead end for observability. The job's only `sendTelegramWork` call was the unconditional once-per-UTC-day `buildDailySummary()` coverage snapshot, not breach-triggered. `docs/data/quality-checklist.json` DS-OBS-01="PASS" evidence ("WORK-channel msg IDs 2743-2766") is the identical copy-pasted string found byte-for-byte across 4 unrelated checklist rows — same self-report-confab false-positive class BCT-OBS-02-FIX already flagged this session, not corroborating.

Fix: at the existing 60-min-cooldown-gated escalation site in `runFreshnessSlaMonitor()`, added a direct channel send — CRITICAL breach (`age > threshold × 1.5`) → BUG via new injectable `sendBugFn` (defaults `sendTelegramBug`), HIGH breach → WORK via the pre-existing injectable `sendWorkFn` (reused). Independent try/catch, non-fatal, does not touch `escalations`/`markEscalationSent` bookkeeping. The pre-existing signal-bus `escalateToCommander` call is left unchanged (alert-commander's suppression of it is documented as correct behavior for MARKET purposes — this fix adds the missing WORK/BUG visibility, it doesn't touch that path).

New test `DS-OBS-01-FIX-sla-breach-work-bug-alert.test.ts` (6 cases: HIGH→WORK, CRITICAL→BUG, off-hours-skip, cooldown-active-skip, no-breach-silence, BUG-send-failure-non-fatal) — 6/6 green. Sibling freshness-SLA suite (`234-vps-health-sla`, `1354b`, `1407b`, `1920i`, `1352c`, `freshness-coverage-map-checker`, `1313-channel-routing-enforcement`): 95/95 pass, no regressions. `bun tsc --noEmit` clean. Full suite A/B via `git stash` on the one modified file: AFTER 14690 pass/40 skip/42 fail vs BEFORE 14684 pass/40 skip/42 fail — diffed the `(fail)` sets directly, only delta is one flaky news-polling timing pair swap (fully unrelated files); `235-telegram-send-merge.test.ts`'s BUG-channel test failure is present identically in both logs (pre-existing `mock.module` cross-file pollution under full-suite load, unrelated to this fix's new `sendTelegramBug` import). Docs updated: `docs/standards/cron-jobs.md` (new DS-OBS-01-FIX observability note). Decision journal: `docs/agent-memory/decisions/sprint-ds-obs-01-fix-dev-mcp-server.md`. Tool count 184 / cronJobCount 88 unaffected (no new tool/cron, existing cron behavior extended in-place).

Zone health: tsc clean, 6/6 new + 95/95 adjacent sibling tests green, full-suite A/B confirms zero regressions, docs updated, tool/cron counts unaffected | HEALTHY.
