# dev-mcp-server -- Notebook

## 2026-08-05 — FIX-BCTC-FULL-SERVING-EMPTY-NEWEST-PERIOD-HEAD-OF-LINE (P0/S, PO board-mint, architect-scoped) → REVIEW, next_agent=qa

**Session:** f298ccf7-8cf4-452d-9a5a-57dcb47e65ac. Live P0 outage (day 4): `get_bctc_full` served "Chưa có dữ liệu BCTC" for FPT/HPG/VCB even though 2026-Q1/2025-Q4 sit fully DONE one/two quarters back — root cause was the unconditional `ORDER BY sort_key DESC LIMIT 1` row-selection query (`bctcFullTools.ts:954-958`, no predicate on `refine_status`), which let an empty `ensureFinancialReportShellRow.ts` PENDING shell at the newest period head-of-line-block the whole ticker. Architect brief (`docs/architecture-briefs/2026-08-05-fix-bctc-full-serving-empty-newest-period-fallback.md`) fully scoped the fix — implemented verbatim: for no-explicit-`{year,quarter}` calls only, replaced the single fetch with a bounded `CANDIDATE_WINDOW=6` scan reusing the existing exported `checkPublishability` gate (PUB-1..8, no duplicated logic) to pick the newest candidate that actually passes, falling through to today's unchanged rejection when nothing in the window qualifies. Explicit `{year,quarter}` calls stay byte-identical (separate branch). Added an honest fallback note (`textOutput` + `content[1].fallback_from_newest_sort_key`) reusing the file's existing FR-DEGRADE-01 transparency pattern.

**Evidence:** extended `240-bctc-full.test.ts` with the brief's 5 cases — RED confirmed pre-fix (2/5 failed on the correct symptom; test fixtures corrected mid-cycle to mirror production's `total_assets=NULL` shell shape — NOT overriding `total_assets` on PENDING-shell fixtures, since `checkBctcIdentityGuard` fails-open on null and PUB-1's `refine_status` check is the gate that must actually fire, matching the live DB probe in the board row's `status_note`). GREEN post-fix: 25/25 pass, 81 expect(). `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Fresh boot `PORT=3199`: `/health` toolCount=183, `/api/bctc-inspect` + `/dashboards/news-fetch/` both serve valid HTML (dashboard circular-dep check clean). Full `bun test`: 15070 pass/40 skip/55 fail/1 error, 47788 expect(), 482.81s — zero `(fail)` lines match bctc/financial/240; every failure is the same pre-existing flaky-5000ms-timeout class already documented in the prior session's baseline (VPS proxy health, vnstock stores, insider-transactions/1146, get_backtest_runs/1844a, foreign-flow/1518, TSU-DEV-U5, get_market_cap/FIX-B-2, MCP SSE/Task 251, telegram routing/Task 235, push-news) — fail/error counts actually IMPROVED (60→55 fail, 2→1 error) since 2026-08-01's baseline read. Doc updated: `docs/architecture/microservice/mcp-server/financial-reports.md` Invariant #11. Commit `70257dfc0` (pathspec-scoped — a concurrent peer session had an unrelated staged file `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-developer-3.md` in the shared index; explicit-pathspec commit left it untouched, not swept). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S62. Note for QA: same user-gated-rebuild caveat as the sibling row (`FIX-BCTC-PENDING-REFINE-HEAD-OF-LINE-FAILED-ROW`) — do NOT rebuild autonomously; the live gateway verification gate (`get_bctc_full(code:"FPT"|"HPG"|"VCB")` returning structured data) needs a user-authorized `docker compose up -d --build mcp-server` first.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite improved (60→55 fail, 2→1 error vs 2026-08-01 baseline), 3 files touched (1 code + 1 test + 1 doc) | HEALTHY.

## 2026-08-05 — CCATO-MCP-T2-CLAIM-MAP-LOADER (P0, PM decomposition of SPRINT-CCATO-TRUTHGATE-MCP-NATIVE) → REVIEW, next_agent=qa

**Session:** f298ccf7-8cf4-452d-9a5a-57dcb47e65ac. Board row's own `depends:[]` was thin but correct — spec is in `docs/architecture-briefs/2026-07-17-ccato-truthgate-mcp-native.md` §3.2/§7 (the brief's own decomposition table also lists T2 with no deps); pre-checked sibling `CCATO-MCP-T1-DOMAIN-ENGINE` anyway per instruction and confirmed it's already landed+committed (`4d20a76a2`, status REVIEW) so `ClaimToolMap`/`ClaimToolMapDimension` types (`domain/services/narrativeTruthGate/claimToolMapTypes.ts`) exist to import — no build-order gap. New `infrastructure/fileStore/claimToolMapLoader.ts` (`loadClaimToolMap(filePath?)`, `CLAIM_TOOL_MAP_PATH` constant) + `claimToolMapValidator.ts` (shape assertion, split out — size-lint, <=120L each, matching T1's own precedent for a brand-new file over a justification header): reads+parses `docs/data/claim-tool-map.json`, throws `ClaimToolMapLoadError` fail-loud on missing/unreadable/unparsable/shape-mismatched input, tolerates additive SSOT fields (`tool_null_markers`/`_meta`/`version`). Deviation from the brief's literal spec text (flagged, not silent): the brief said to copy `improvementSignalWriter.ts`'s `resolve(__dirname, "../../../../../../")` REPO_ROOT pattern verbatim — verified empirically first, found it resolves ONE LEVEL ABOVE the real repo root locally and above `/` in the deployed container (Dockerfile copies `apps/mcp-server/src/` to a shallower `/app/src/`); used the already-correct `getProjectRoot()` helper instead for the new path constant, did not touch `improvementSignalWriter.ts` (out of zone/scope for this task), documented the discrepancy in `infrastructure.md`.

**Evidence:** New `CCATO-MCP-T2-CLAIM-MAP-LOADER.test.ts`, 14 tests/25 expect(): real-SSOT load + shape assertions (5 dimension ids), output directly usable by `scanClaimCandidates` with zero transformation, fail-loud coverage (missing file, bad JSON, non-object, missing/mistyped `negation_lexicon`/`non_ticker_tokens`/`dimensions[]`, empty-dimensions, bad dimension entry), additive-fields-tolerated case. `bun tsc --noEmit` clean. `bash scripts/audits/size-lint-justification.sh --check`: 2 pre-existing offenders only, neither touched here. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged (T2 registers no MCP tool/cron). Fresh boot `PORT=3197`: `/health` toolCount=183, both dashboard routes serve valid HTML. Full `bun test`: 15085 pass/40 skip/54 fail, 47837 expect(), 498.79s — IMPROVED vs the same-day S62 baseline (15070 pass/55 fail/1 error); structural proof independent of the count: `grep -rl claimToolMapLoader\|claimToolMapValidator src` shows the only consumer is this task's own new test file (no production code imports either module yet — T5-USECASE not landed), so this change structurally cannot have caused any pre-existing failure. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S63. Docs updated: `docs/architecture/microservice/mcp-server/{infrastructure,testing,domain-model}.md`.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite improved (55→54 fail vs same-day S62 baseline), 3 new files + 3 docs touched | HEALTHY.

## 2026-08-05 — FIX-MCP-MEMORY-CODE-LEAK (P-high/M, dev-team Supervised-Lane Sweep, architect-designed) → REVIEW, next_agent=qa

**Session:** f298ccf7-8cf4-452d-9a5a-57dcb47e65ac. Architect brief (`docs/architecture-briefs/2026-08-05-fix-mcp-memory-code-leak-initdatabase-guard.md`) re-verified `initDatabase()` (schema.ts:156, call-site count grown 68→117) had no already-initialized guard — every bare call re-ran the ~3300-line domain-slice DDL sweep + 3 unconditional bctc backfill calls (backfillBctcQ4/Q1_2026/Historical, seedWatchlist.ts). Implemented the prescribed `WeakSet<Database>` identity guard (not a bare boolean, per brief), but deviated from the brief's literal code block in one load-bearing way — found by running the full suite before committing, not by inspection alone: the brief's guard wrapped initDatabase()'s ENTIRE body, which broke 2 real tests (`1489-tracked-indicators-dedup.test.ts`, `daily-foreign-flow-backfill.test.ts` "wired into boot sequence" case) that explicitly re-run `initDatabase()` against the SAME db after seeding new rows and assert the cleanup/backfill re-fires. Root cause: those tests exercise the function's "Post-init migrations" tail (HUT migration, BACKFILL_079, Task 1489 purge, `migrateForeignFlowColumns`/`backfillDailyForeignFlow`) — never identified by the brief's own Finding 1 as part of the cost (only the DDL slices + the 3 named backfill calls were). Rescoped the guard to wrap only the identified-expensive section, leaving the tail unguarded/always-run — closes the brief's actual target with zero regression.

**Evidence:** extended `002-db-schema.test.ts` per the brief's test plan (rewritten once — the first draft's marker, a BACKFILL_079 row, turned out to live in the unguarded tail, not the guarded section): drop `backtest_runs` (guarded-section-only table) after first init, confirm it stays dropped across a 2nd/3rd call on the same db; two different fresh `:memory:` Databases both get fully initialized. 26/26 pass. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Fresh boot `PORT=3196`: `/health` toolCount=183, `/api/bctc-inspect` + `/dashboards/news-fetch/` valid HTML, clean SIGTERM shutdown. Full `bun test` (3 runs — 1 pre-fix baseline reproducing the 2-test regression, 2 post-restructure): final 15088 pass/53 fail/0 errors, 47844 expect(), 523.90s — grep-confirmed zero fail lines match 1489/TASK_2001/schema/initDatabase; isolated re-run of the 9 fast (<200ms) failing names against the reverted pre-fix code showed 91/91 pass, confirming those are the pre-existing full-suite-order-dependent flaky class already documented in the 08-01/08-05 baselines (52-60 fail band), not caused by this change. Docs: `infrastructure.md` (Key Infrastructure Patterns), `testing.md` (002-db-schema row). Commit `609f62800` (pathspec-scoped — left an unrelated peer-session dirty BCTC set (`parseBctcReport.ts`, `financialFiguresValidator.ts`, 2 new untracked files) untouched). Second finding from the brief (SseSessionManager /sse per-connection McpServer+heartbeat leak) is explicitly out of scope here — confirmed still flagged separately on the board, not duplicate-fixed. Graphify: skipped — no Skill-tool path for this spawned agent (same disposition as dev-rag-service/dev-frontend).

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite 53 fail (within the documented 52-60 flaky band, 0 regressions vs isolated pre-fix baseline), 4 files touched (1 code + 1 test + 2 docs) | HEALTHY.
