# dev-mcp-server -- Notebook

## 2026-07-24 — FIX-AGENTSIGNALS-FROMAGENT-SCHEMA (BOUNDED-1 auto-pickup, P2, stale-backlog closure) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Ticket claimed `get_agent_signals({from_agent})` still 32602s on `agent` Required. Traced: the identical root cause (agentSignalTools.ts) was independently minted as sibling ticket `FIX-AGENT-SIGNALS-AGENT-PARAM-CONTRACT` (po-s107, 2026-06-19) and SHIPPED that same day (`8a6b798ce`: `agent` schema-optional + runtime "at least one of agent/from_agent" guard) — 2 days after THIS ticket minted, never cross-reconciled. `agent` was already `.optional()` on disk; no behavior change needed.

Closed the traceability gap instead: extracted the inline zod shape to exported `GetAgentSignalsShape` (agentSignalTools.ts) so tests assert the REAL runtime schema (no hand-mirrored-copy drift risk). New `FIX-AGENTSIGNALS-FROMAGENT-SCHEMA.test.ts` (8 tests: 4 schema-level + 4 end-to-end via real `McpServer`+`registerAgentSignalTools()`+`:memory:` DB) proves the ticket's literal repro `get_agent_signals({from_agent:'news-scout'})` returns rows/empty array, never `-32602`, and neither-present still gets a controlled readable error not a protocol crash.

Evidence: `bun tsc --noEmit` clean. New suite 8/8 pass. Precedent/consumer suites (FIX-AGENT-SIGNALS-AGENT-PARAM-CONTRACT + 1194-agent08-tools + FACTORY-INFRA-agentSignal-sql-binding + 1968b1-hours-back): 30/30 pass. Full `bun test` run twice: 14772 pass/40 skip/49 fail (530s) and 14771 pass/40 skip/50 fail (535s, full log grepped) — the complete run2 fail list matches the SAME pre-existing flaky/resource-contention class cited across 5+ prior QA journals (CI-RED-RECONCILE, FIX-FUNDAMENTALS-REFRESH-CRON-DEAD, FIX-PRED-CLAIMS-EXCLUDED-SERVE-DISPLAY, PREDICTION-EVIDENCE-REVIVAL, SYSTEMIC-REMAKE-P1) — zero mention of `agentSignalTools`/`get_agent_signals`/`GetAgentSignalsShape`. `1875c-record-signal-outcome-routing.test.ts` (same source file) IS in the fail-set under full-suite load but re-ran 5/5 clean standalone — isolation-confirmed pre-existing cross-file contention, matching its own multi-cycle documented history, not this change. Simplicity gate: PASS (Q1 pure extraction; Q2 shape has 2 real call-sites [prod+test]; Q3 clean; Q4 <50% overhead). Decision journal: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md` STEP dev-mcp-server-S6.

Zone health: tsc clean, new+precedent suites 38/38 green, full-suite fail-set isolation-confirmed 100% pre-existing/unrelated across 2 runs + 5 historical QA cross-refs | HEALTHY.

## 2026-07-25 — FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED (BOUNDED-1 auto-pickup, P2, rebuild REQUIRED) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Live-verified 102/105 `signal_outcomes` rows stuck 'pending' with `checked_at` NULL despite the hourly `signalOutcomeResolutionJob` cron reporting `status='success'` every run (5+ weeks) — a successful *run* was never successful *progress*. Root cause = 2 compounding bugs in `signalOutcomeStore.ts`: (1) `fetchPriceFromStockService` parsed the Go `/price/history` response as a flat `{date,close}[]` array — real shape is `{code,history}` (same defect class as already-shipped 1945a fix in `verdictResolutionJob.ts`, never propagated here) — `Array.isArray()` was always false, Option B permanently dead code; also hardcoded `days=3` only returns the 3 most-recent trading days, useless for old dates. (2) baseline `price_at_signal` capture had NO Option-B fallback at all against `market_prices_history`, which is an intraday cache with a rolling ~24h purge (`pushPricesHandler.ts`) — by the time the scan first looks at a row (already ≥windowHours old), Option A's window has typically already rolled off.

Fixed both via the canonical `getPriceHistory` client (clients.ts, zero duplicated parsing logic) + dynamic `days` sized to the actual gap-to-target, and added the missing baseline Option-B fallback (`resolveSignalOutcomes` now takes an injectable `fetchHistoryFn` dep, DI pattern matching `verdictResolutionJob`). Live-copy-DB proof (real stock-price service via host port 5010, NOT the live container): 102 pending → 7 (98/105 checked_at populated); remaining 7 are legit (2 non-ticker MACRO/MULTI signals with no price series, 5 too-recent for the 48h window to have elapsed yet) — self-heals fully via the SAME already-scheduled hourly cron once deployed, no separate backfill script needed.

Found+fixed a SEPARATE live production bug while investigating: `cleanExpired()` (agentSignalStore.ts, TTL prune) and `checkStaleAgentSignals()` (30-day stale-null-outcome prune) both do a bulk `DELETE FROM agent_signals` — with `PRAGMA foreign_keys=ON` already set on the shared connection, deleting a row still referenced by a `signal_outcomes` child throws "FOREIGN KEY constraint failed", and being a single bulk statement SQLite rolls back the WHOLE delete (0 rows pruned) — silently swallowed by both callers' generic catch. Live-verified 104/5983 currently-expired `agent_signals` rows are blocking `cleanExpired()` on every call. Guarded both queries with `id NOT IN (SELECT signal_id FROM signal_outcomes)`; empirically confirmed (failing test first) that SQLite blocks on ANY referencing child regardless of resolved/pending status (no CASCADE) — settled on "any-reference blocks deletion" rather than CASCADE (would destroy accuracy history) or a pending-only guard (impossible without a schema migration). `getAccuracyStats` changed INNER→LEFT JOIN against `agent_signals` so an orphaned row (parent already gone) still contributes to `sample_count`/`accuracy_rate`, not silently dropped. This durably satisfies `folded_scope_orphan_fk`: the 1 existing orphan (id=66, signal_id=6218) needed NO special-case script — the fixed generic resolver resolved it normally (live-copy-verified: outcome_24h/48h='neutral', checked_at populated) since resolution never depended on the parent row existing.

Added `checkStalledResolutionLiveness()` (signalOutcomeResolutionJob.ts) — counts rows unresolved >72h past creation after each run and sends ≤1 BUG alert/UTC-day; this is the guard that would have caught the original 5-week stall immediately instead of a `status='success'` cron masking zero progress.

**Scope note:** verification_gate (b) references `get_calibration_report`, which is powered by a DIFFERENT subsystem (`prediction_claims`/`calibrationReportJob.ts`, unrelated to `signal_outcomes`) — live DB already shows `computed_at=2026-07-19`/`resolved=6` (independently self-recovered since task-mint-time 2026-06-14), so this gate already passes without touching that code; did not expand scope into it.

Evidence: `bun tsc --noEmit` clean. New suite `FIX-SIGNAL-OUTCOMES-RESOLUTION-STALLED.test.ts` 9/9 pass (31 expect() calls) — baseline Option-B fallback, real-envelope-shape production path (mocked global fetch), `getAccuracyStats` LEFT JOIN orphan preservation, `cleanExpired` FK-safe guard (2 cases), `checkStaleAgentSignals` fallback, liveness self-alert dedup. Affected+precedent suites (signal-outcome-store, 1382-signal-outcome-job, 242-agent-signals, 1945a-verdict-resolution-envelope, 1945b-accuracy-digest-handler, accuracy-context-tool, cronStatusCompute, FIX-CRON-WATCHDOG-COVERAGE-2026-07-22, FACTORY-SCHEDULER-split-dataAuditJob, 157-data-audit-job, 314-dataaudit-market-prices-wipe, 1420-cron-health-coverage, 1086-financial-reports-trend): 147/147 pass, 452 expect() calls (caught+fixed one real regression: 157-data-audit-job's `1863h` test needs `signal_outcomes` probed before guarding, not string-matched). `gen-project-stats.ts --dry-run`: toolCount 184/cronJobCount 88 unaffected (no new tool/cron — existing job logic only). Local server boot probe (PORT=3099): clean, `90 cron keys` registered, health 200 toolCount=184. rebuild_required=true (LIVE named-vol verify is QA's downstream gate per this task's own contract).

## 2026-07-25 — FIX-HNX-OFFHOURS-FORCE-FETCH-ERROR-DOWNGRADE (BOUNDED-1 auto-pickup, P2, stale-duplicate closure) → DONE, no code shipped

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Ticket claimed off-hours `force:true` HNX/UPCOM fetches (`sectorRotationTools.ts:181/188`, `portfolioTools.ts:160/163`, `marketTools.ts` forceOpts:244/253) still log ERROR-level spam on expected closed-session rejection. Traced: the identical fix already SHIPPED under sibling ticket `FIX-HNX-OFFHOURS-ERROR-DOWNGRADE` (commit `93e9dbeb8`, 2026-06-21T01:56:45Z), QA-APPROVED (`a73bf035d`), and **rebuild CONFIRMED live** (`f0ac29998`, "RAW marker hit both sites", `rebuild_required:false`) — all BEFORE this row was even minted (minted_at 2026-06-21T03:14:47Z, 78 min after the fix landed). The row's own note admits `"gateway-blind triage: file-confirmed force:true sites, live log-severity NOT probed"` — filed off a file-grep of the 3 call sites without checking whether `hnx.ts`'s log severity was already fixed.

Confirmed at HEAD: `fetchHnxPrices`/`fetchUpcomPrices` (`infrastructure/fetchers/hnx.ts` L354/L436) already branch on `isVnTradingWindow(options?.now)` — the shared SSOT (`domain/services/tradingWindow.ts`, reused across server-startup/pushPricesHandler/foreignFlowFetcherJob/dataFreshnessTools) — in-hours keeps `logger.error`, off-hours downgrades to `logger.debug`. All 3 named call sites route through this shared fetcher with no additional error-log wrapper of their own (portfolioTools/marketTools catch-paths log `warn`, not `error`, and only fire on thrown rejections which these fetchers never produce). Repo-wide grep sweep for other force:true HNX/UPCOM sites: none found beyond the 3 named + 1 non-force scheduler call (already session-gated, out of scope). Pre-existing test `FIX-HNX-OFFHOURS-ERROR-DOWNGRADE.test.ts` (6 tests, not authored by this task) already satisfies the ticket's baseline_pass (off-hours expected-rejection → non-ERROR; in-hours genuine failure → ERROR, clock-injected). No second duplicate test file added (`feedback_file_prior_art_check_before_minting_row`).

Evidence: `bunx tsc --noEmit` exit 0. `bun test` on the pre-existing fix+regression files: 50 pass/0 fail/6 skip (101 expect calls) — the 2 captured `level:error` lines are the market-OPEN cases, confirming discrimination still holds today. Sector/portfolio/market tool suites (186/1059/TASK17-PAGE9/283/182/084): 91 pass/0 fail (245 expect calls), zero regression. `git show 93e9dbeb8` diff pasted as RED-baseline evidence (parent commit had unconditional `logger.error` at both sites). No code/test/doc commit — only the decision journal ships. `rebuild_required: false` — nothing changed; fix has been live since 2026-06-21. Decision journal: `docs/agent-memory/decisions/sprint-FIX-HNX-OFFHOURS-FORCE-FETCH-ERROR-DOWNGRADE-dev-mcp-server.md`.

Zone health: tsc clean, precedent+affected suites green, duplicate confirmed via timeline+source-read, no fabricated fix | HEALTHY.

## 2026-07-25 — FIX-BDI-SHIPPING-STALE-404-GUARD (P2, no-fake-data/staleness) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. `get_supply_chain_exposure` served a ~2.5mo-stale BDI (^BDI Yahoo permanently 404 since 2026-04) as current "+0.0%" concluding false "ổn định" — same class as `DSI-MACRO-PHANTOM-STALE-GUARD`. Verified at source: guard genuinely didn't exist (L160 still `?? Date.now()/1000`). Reused the ALREADY-SHIPPED shared helper (`TRACKED_INDICATOR_STALE_MS`/`listTrackedIndicatorsFromDb`, DSI precedent, epoch-ms compare) instead of inventing a new threshold: `readShippingIndicesFromDb` (now exported) filters stale `shipping_*` rows out; `buildSupplyChainExposureOutput` no longer defaults to "ổn định" when `indices=[]`. Root cause fix at `shippingIndex.ts` fetchSymbolData: removed the `?? Date.now()/1000` fallback — missing `regularMarketTime` now returns null (no-data) instead of fabricating "now"; this seam is shared by both the serve path and `commodityTrackerRefreshJob`, so no direct edit to that job file was needed.

RED confirmed via `git stash` A/B (new test failed pre-fix with missing-export error) → GREEN after restore. New test 7/7 pass. Precedent+affected suites (252/253/254/256/1920c/DSI-MACRO/FIX-DOWJONES/1039/1248-bdi-vps-proxy/255-signal-integration): 107/107 pass, zero regression. `bunx tsc --noEmit` exit 0. `rebuild_required:true`. Part (b) source-replacement (^BDI dead, needs a live feed) stays OPEN for ops-mainserver-fetch — did not fabricate a replacement URL. Journal: `docs/agent-memory/decisions/sprint-FIX-BDI-SHIPPING-STALE-404-GUARD-dev-mcp-server.md`.

## 2026-07-25 — FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE (P2 FIX) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. `get_macro_calendar` (`carryTools.ts`) HTTP-GET'd the macro-indicators Go `/macro-calendar` endpoint — verified at source that handler (`handlers_calendar.go`) is a PERMANENT FDA-4 honest-unavailable STUB, always `{events:[],status:"unavailable",source_tier:4}`; no live source was ever wired there (by-design honest-degraded, not a regression). Meanwhile `domain/services/macro/macroCalendar.ts#getMacroCalendar()` already existed, fully built and unwired: a pure function (no fetch) returning ~36 FOMC/GSO-CPI/GSO-GDP/Vietnam-PMI/SBV 2026 events with `isPivotWindow`/`currentMonthIsPivotWindow`/`nextPivotWindow`/`pivotWindowWarning`.

Chose option (a) per task guidance: repointed the tool at the local domain service directly, dropping the HTTP hop entirely — data was already in-repo, no upstream fetch needed, keeps the fix wholly inside apps/mcp-server/ (zone-restricted). Removed the now-dead `withDeadline`/fetch/try-catch block; new payload: `{source_tier:3, is_estimate:true, status:"ok", daysRequested, events, currentMonthIsPivotWindow, nextPivotWindow, pivotWindowWarning}`. `days=N` stays a generic window filter applied to `new Date()` via the existing domain service (no per-date literal, no single-month special case). `pivotWindowWarning` is the exact field alert-commander (`stage-bootstrap.md`) and digest-predict (`weekly.md`) already derive `pivot_window_active` from client-side (`pivotWindowWarning != null`) — no downstream doc/consumer change needed.

RED confirmed: new `FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE.test.ts` (7 tests, real `McpServer`+`registerCarryTools()`+`_registeredTools[name].handler(args)` harness, precedent: FIX-AGENTSIGNALS-FROMAGENT-SCHEMA/1194-agent08) run against unmodified pre-fix code — 0/7 pass (fetch called, source_tier=4, status="unavailable", events=[], `pivotWindowWarning` missing). Applied fix → GREEN 7/7 (31 expect calls): no HTTP fetch made, events.length>0 with real 2026 schedule, status≠"unavailable", source_tier=3, isPivotWindow boolean on every event, pivotWindowWarning/currentMonthIsPivotWindow/nextPivotWindow present and non-crashing, days=N generically honored (days=1 returns only today-dated events, days=365>days=20 event count).

Evidence: `bunx tsc --noEmit` exit 0. New suite 7/7 pass. Precedent/affected suites (1423c, 1423e-macro-calendar, 1881a-source-tier) + new suite: 70/70 pass, 0 fail, 231 expect() calls, zero regression. `gen-project-stats.ts --dry-run`: toolCount 184 unchanged (repoint of an existing tool, not a new registration). `rebuild_required:true` — MCP-server code change; LIVE gateway RAW-verify (events>0/source_tier=3/status≠unavailable/days honored + digest-predict/alert-commander non-null pivot_window signal) is QA's post-rebuild gate, not claimed pre-rebuild. Journal: `docs/agent-memory/decisions/sprint-FIX-MACRO-CALENDAR-WIRE-STATIC-SOURCE-dev-mcp-server.md`.

Zone health: tsc clean, new+precedent suites 70/70 green, toolCount unaffected, root-cause verified at source (not self-report), no fabricated fix, wholly in-zone | HEALTHY.

## 2026-07-25 — FIX-PREDCLAIM-CREATIONPRICE-UNGATE-ZOD-CONTRACT (BOUNDED-1 auto-pickup, P1) → REVIEW, next_agent=qa

**Session:** e3a3f331-f086-4c43-bd81-f40de22ef599. Dispatcher's condensed brief covered only "add a Zod gate at the store boundary"; reading the FULL `orch-state.json` `task_board.in_progress[]` entry mid-flight surfaced an 8-item deliverable (a-h) the condensed brief omitted — went back and implemented the missing pieces rather than shipping the narrower interpretation (SSOT is the board row, not the dispatcher's paraphrase).

**(a+f) UNGATE the producer:** `evidenceTools.ts`'s `create_prediction_claim` looked up `creation_price` from `daily_ohlcv` ONLY when the optional `direction`+`expected_move_pct` pair was BOTH supplied — every claim that omitted them (100% since 2026-06-14) silently persisted `creation_price=NULL`, permanently unscoreable. Made the price lookup unconditional; `direction`/`expected_move_pct` now only gate the derived `target_price`. Fixed the dormant sibling (`intelligenceCycleJob.ts` chain-synthesizer hardcoded `creation_price: null` — zero live rows carry that agent_id yet, but would have become the next source once the primary path closed).

**(b+d) ZOD WRITE-DOOR at the store:** `predictionClaimStore.ts::insertPredictionClaim` now runs a `.strict()` Zod contract (creation_price required finite number; stock/agent_id/claim_text/direction/resolution_date/confidence also required per the board's exact required-field list) and THROWS a §2-blueprint-aligned descriptive rejection (`docs/architecture-briefs/2026-07-25-input-validation-coverage-blueprint.md` §2 field/problem/expected/got shape) — plus a fire-and-forget `sendTelegramBug(...)` alert (fail-loud per `docs/protocols/fail-loud-protocol.md`, not awaited: ~30 call sites incl. tests are synchronous, and `sendTelegramBug` already "never throws" by its own contract, so this doesn't need a breaking async signature change).

**(c) Update-path:** NOT a second Zod layer — `resolveClaim`/`excludeClaim`'s UPDATE statements structurally never touch stock/direction/creation_price/resolution_date/confidence (SET clause only ever writes resolution_outcome/actual_price/brier_score/resolved_at or is_excluded/resolved_at), so the acceptance criterion ("cannot blank" those fields) already holds by construction.

**(e) Grandfathering:** verified by construction — the gate lives only in `insertPredictionClaim` (write path); `mapRow`/`getAllClaimsForTracker`/the serve handler were untouched, so all pre-fix NULL-creation_price rows keep reading correctly (confirmed by the existing TASK17-PRED/FIX-PRED-CLAIMS-EXCLUDED suites, updated to seed those legacy rows via raw SQL — the only way to simulate them post-gate — and still 100% green).

**(g) Docs that caused the outage:** `docs/agents/tools/list/create_prediction_claim.md` (added direction/expected_move_pct + creation_price contract) and `docs/agents/digest-predict/flow/daily-predict.md:110` (the exact 5-arg caller-doc line the board's root_cause traced as L1 of the 4-layer chain) — both synced.

**(h) False comments:** `predictionClaimsHandler.ts`'s docstring called excluded rows "legacy neutral claims" (live data refuted this — newest excluded rows postdate the oldest scoreable ones) and hardcoded a stale 2026-06-11 worked example (9 total/3-3-0-3/hitRate 0.50) against the board-cited live 2026-07-25T07:11:04Z probe (17 total/4-2-5-6/hitRate 0.6667) — corrected both.

**NOT done (by design):** backfilling the 11 existing NULL rows — separate row `FIX-PREDCLAIM-BACKFILL-NULL-CREATIONPRICE` (BACKLOG, owner dev-mcp-server, depends on this row) already exists and is **TIME-CRITICAL**: 5 pending claims (ids 13-17) auto-convert to permanently-excluded starting the 2026-07-28T16:35Z resolution tick, 3 days from this task's mint time — flagging prominently for next dispatch, not mine to silently do per this row's own BACKFILL SCOPE note.

Evidence: `bun tsc --noEmit` clean (both before and after the deliverable-gap fixes). Targeted suite (every file that calls `insertPredictionClaim`/`create_prediction_claim`, 8 files incl. new `FIX-PREDCLAIM-CREATIONPRICE-UNGATE-ZOD-CONTRACT.test.ts`): 114/114 pass, 356 expect() calls — re-ran clean after the telegram-alert addition too (log confirms `sendTelegramBug` fires on rejection, no-ops cleanly on missing env config in test). Full `bun test` run twice (pre-deliverable-gap-fixes): 14822 pass/40 skip/55 fail/2 errors/47023 expect (509.37s) and 14827 pass/40 skip/50 fail/47029 expect (464.99s) — zero `(fail)` lines matching predict/evidence/creation in either full log; visible failures (SLA-monitor, OHLCV-aggregator-seed, `_deprecated/1302-technical-indicators`, insider-transactions/foreign-holding-ratio 5000ms timeouts) are the SAME pre-existing documented flaky class cited in prior cycles' journals. Board's own regression floor (`>= 9408 pass / <= 348 fail`) cleared with large margin both runs. `gen-project-stats.ts --dry-run`: toolCount 184/cronJobCount 88 unaffected (no new tool/cron). Simplicity gate: PASS (Q1 — evidenceTools.ts/intelligenceCycleJob.ts changes are the board's own deliverable, not scope creep; Q2 — `formatWriteRejection`/`PredictionClaimInsertSchema` are single-call-site today but implement the architecture-brief's own prescribed §1.4/§2 contracts, interface-contract exception; Q3 clean; Q4 <50% overhead, test files exempted). Decision journal: `docs/agent-memory/decisions/sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md` STEP dev-mcp-server-S9. **PENDING-REBUILD:** live gateway acceptance (the board's own AC-1 "re-read through the LIVE served endpoint") NOT claimed — running container predates this commit; rebuild is USER-GATED and not authorized this session.

Zone health: tsc clean, targeted 114/114 green (incl. new store-gate + tool-ungate tests), 2 full-suite runs clean of predict-related failures + comfortably clear the board's own regression floor, tool/cron counts unaffected, docs synced at both layers that caused the outage, backfill urgency flagged not silently done | HEALTHY.
