# dev-mcp-server -- Notebook

## 2026-08-01 — FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Spun from VERIFY-COWORK-MACRO-SNAPSHOT-ENVELOPE (router-verified NO active leak at the time). `get_macro_snapshot.text` was `JSON.stringify(data)` — raw JSON — unlike `get_market_snapshot.text` (human-readable prose), a latent MARKET-channel leak-trap for any future naive prose-echo. Added `buildMacroSnapshotText()` in `macroTools.ts`: a GENERIC recursive renderer (`humanizeKey`/`formatScalar`/`renderSection`) that walks the raw macro-indicators response and emits a bracketed `[Section]`/indented `Key: Value` prose block — no per-field/per-ticker hardcode (generic_mandate), any future Go-service field auto-renders. Added `data` (raw upstream object, verbatim) as a new envelope field alongside `source_tier`/`text`/`fetchedAt` so synthesizing agents keep typed access without parsing prose. New test file `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT.test.ts` (16 tests). Rewired the `parseInner()` helper (duplicated in `089`/`1423d`/`1423f`/`1570c` + 1 assertion in `1903a`) from `JSON.parse(env.text)` to `env.data` — those 5 files' helper WAS the leak-trap behavior itself, asserted correct pre-fix. Updated `macro.md` (invariant 1a) + `get_macro_snapshot.md` (contract refresh).

**Evidence:** New file 16/16 pass. Targeted 8-file macro suite: 106/106 pass, 215 expect(). `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Fresh boot `PORT=3198`: `/health` toolCount=183. Full `bun test`, 2 independent runs: 15027/15026 pass, 52/53 fail — both inside the documented FIX-MCP-SUITE-HEALTH-BASELINE band; grep-confirmed ZERO fail-list matches for `macro|089-tool-macro|1423d|1423f|1570c|1903a|1881a|1918a|FIX-MACRO-SNAPSHOT` in either full log. Commit `e64ad8870`. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S56 (rolled — `-2.md` hit BYTE_CAP 41819/36000, CAP-REACHED marker written there).

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 9 files touched (1 impl + 6 test + 2 docs). **Tool-grant gap observed this cycle:** this session had no bound MCP/gateway tool (only Read/Edit/Write/Bash) so the decision-journal skill's mandatory `send_telegram(channel="bug")` CAP-REACHED alert could not be sent — flagging for router relay, structural not agent-caused (matches `feedback_agent_reported_limitation_may_be_structural_check_the_tool_grant`) | HEALTHY.

## 2026-08-01 — FIX-CI-SIZELINT-MACROTOOLS-HUMANIZE-618L → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. CI RED size-lint new-offender: `e64ad8870` (this cycle's own FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT) grew `macroTools.ts` 510L→618L, past the file's baseline-501L/upper-tolerance-551L. Per PO's ruling (`po_ruling_20260801T0125`) this is confirmed NEW debt, not pre-existing, and gates `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT`'s terminal DONE flip. Fixed by extracting the self-contained generic renderer (`humanizeKey`/`formatScalar`/`renderSection`/`buildMacroSnapshotText`) into new sibling `macroSnapshotText.ts` (109L, under the 120L default cap) — same file-per-concern seam as the `FIX-CI-SIZELINT-TECHANALYSIS-ROUTER-NEW-OFFENDER-143L` precedent (`health_handler.go` out of `router.go`). `macroTools.ts` re-imports + re-exports `buildMacroSnapshotText` for test-import backward-compat. `macroTools.ts`: 618L→530L (within the 551L tolerance). Explicitly did NOT run `size-lint-justification.sh --update` or add a size-justification header (both would have grandfathered/disarmed the regression — the task's own landmine warning). **Mid-task hazard hit:** a concurrent peer session on this shared working tree transiently reverted the uncommitted edit (both files briefly reappeared at pre-fix HEAD state — `subagent branch hijacks shared dir` pattern); re-applied the identical edit and committed immediately (explicit-path stage, no `-A`) to close the race window, per that lesson's own recovery guidance.

**Evidence:** `size-lint-justification.sh --check` PASS (0 offenders, scanned 1356). `bun tsc --noEmit` clean. `FIX-MACRO-SNAPSHOT-HUMANIZE-TEXT.test.ts` + `089-tool-macro.test.ts`: 34/34 pass, 72 expect(). Byte-identical function bodies — pure move, zero logic change. Commit `f28c6c239` (2 files: `macroTools.ts`, new `macroSnapshotText.ts`). Pushed `origin/main`. CI-plane verified: run `30678397978`, headSha `f28c6c239ac8a4e8e8e97ad83dbf2d8b8e121113`, `size-lint` job conclusion=`success` (`gh run view --json jobs`). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S57.

Zone health: tsc clean, size-lint 0 offenders, 2 files touched (1 modified + 1 new, both <=120L/551L caps respectively) | HEALTHY.

## 2026-08-01 — FIX-AGENTSIGNALS-EXPIRED-GC-CRON (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. RAW-verify-on-pickup (mandatory per the task) found the AC's magnitudes STALE: live named-volume DB shows `agent_signals` total=124/expired=119 (104 FK-blocked by `signal_outcomes`, 15 GC-eligible) and `alerts` total=8/unresolved=8/unresolved>30d=0 — both far below the 2026-06-20/21 auditor snapshot (~3493 expired / 829 unresolved / 205 >30d). Root cause of the drop: BOTH tables already have working daily off-hours GC wired into the SAME `dataAuditJob:daily` cron (`0 23 * * *` Asia/Ho_Chi_Minh) — `agentSignalStore.cleanExpired()` for agent_signals, `checkStaleAlerts` D-3/D-4 for alerts — so the described "no cleanup" gap does not exist as stated; it has been running the whole time. Rather than ship a 3rd/4th duplicate GC mechanism (rejected — debt), hardened the two existing functions in place: wrapped both predicates as `datetime(col) < datetime('now', ...)` (not just the threshold) per `feedback_sqlite_iso8601_datetime_strcompare_bypass` — RAW-confirmed live that `alerts.triggered_at` stores a raw JS `.toISOString()` "T...Z" string while `agent_signals.expires_at` stores the SQLite-normalized space format; `datetime()` parses both correctly today (verified), but the explicit wrap removes reliance on date-field-dominant string-compare luck for any future format drift. Tightened D-4's threshold 60d→30d and its `resolution_notes` marker to `'auto-resolved by retention GC'` (was `'auto-expired by audit'`) per the task's literal spec text. Deliberately did NOT narrow `cleanExpired()` to literal `status IN ('read','expired')` — no `'expired'` status value is ever written in this codebase; narrowing would strand unread-but-expired rows forever, reintroducing the unbounded-growth defect this task exists to close (documented in both files' doc comments + decision journal). `autoResolveStaleAlerts.ts` (72h low/med-only, briefing-triggered) left untouched — different semantic concern, own coverage, out of scope.

**Evidence:** 8 new tests added across `242-agent-signals.test.ts`, `157-data-audit-job.test.ts`, `FACTORY-SCHEDULER-split-dataAuditJob.test.ts` (ISO-'T...Z'-format GC for both tables, 30d in/out-of-range boundary, already-resolved-row immunity, retention-GC marker text). Targeted 4-file suite: 58/58 pass, 207 expect(). `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged (no new cron — by design, folded into the existing `dataAuditJob:daily`). Fresh boot `PORT=3197`: `/health` toolCount=183. Full `bun test` captured to file: 15035 pass/40 skip/52 fail, 47667 expect(), 486.66s — inside the documented FIX-MCP-SUITE-HEALTH-BASELINE band; grep-confirmed ZERO fail-list matches for `agentSignal|checkStaleAlerts|stale.?alert|cleanExpired|retention|D-4|D-3` or any of the 3 touched test filenames — all 52 failures are pre-existing unrelated classes. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md S58.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 5 files touched (2 code + 3 test), zero new surface area (consolidated into an existing cron rather than adding one). Mid-task note: a concurrent peer session's uncommitted WIP on an unrelated file (`macroTools.ts`) briefly shared this working tree during my `tsc` isolation check (`git stash`/`pop`) — resolved cleanly, no data loss, peer's own DJ entry documents their side of the same event | HEALTHY.
