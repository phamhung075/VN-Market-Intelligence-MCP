# dev-mcp-server -- Notebook

## 2026-07-29 — SPIKE-BOOTSTRAP-BROADCAST-CATALYST-CONSUME (BOUNDED-1 idle-pickup) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. SPIKE->FIX. Root row (chain_catalyst id=8779) is now TTL/`cleanExpired`-purged from the live DB (min id currently 3383) and left no log/backup trace — a literal RAW re-probe of its `created_at` is genuinely impossible today. Instead: RED-reproduced the reported failure shape directly (git-stash A/B on my own fix) — a `to_agent='all'` broadcast posted BEFORE either bootstrap call still made `getCycleBootstrap('digest-predict')` return `agent_signals=[]` for it AFTER `getCycleBootstrap('alert-commander')` had already read it — this rules out the pure-timing-artifact alternative by construction (creation strictly precedes both calls) and reproduces the exact 07-21/07-22 recurrence shape.

**Root cause confirmed:** `agent_signals` has ONE global `status` column, no per-recipient read table. `getSignals()`'s unread-only UPDATE (agentSignalStore.ts, end of `getSignals`) flips a `to_agent='all'` row's status GLOBALLY on the first recipient's read; every other recipient's later unread-only bootstrap then silently excludes it.

**Fix direction (a) chosen over (b):** added `getBroadcastSignals(db)` — non-consuming, bounded only by `expires_at`, `WHERE to_agent='all'`, ignores `status` entirely (precedent: `getRecentSignals`/`getPriceAnomalySignals`'s existing non-consuming pattern). `getCycleBootstrap` now unions this with the normal `getSignals()` result (dedup by id) via a new `getInboxSignals()` helper. Rejected (b) per-recipient read-state table: would need a new junction table + touch every `getSignals`/`get_agent_signals` call site for a schema change; (a) is a single new read-only query + a 2-line call-site change, zero schema migration, zero behavior change for genuinely 1:1-addressed signals (regression-guarded by AC-4).

**Evidence:** new `SPIKE-BOOTSTRAP-BROADCAST-CATALYST-CONSUME.test.ts` (5 TCs: 2nd/3rd-recipient visibility, dedup guard, 1:1-addressed non-leak regression guard, `getBroadcastSignals` unit) — RED confirmed via stash (AC-1/AC-3 fail exactly as reported), GREEN after fix. Targeted suite (12 signal/bootstrap files) 87/87 pass. `tsc --noEmit` clean. `toolCount=184`/`cronJobCount=88` unchanged (no tool touched). Live boot (`DB_PATH=:memory:`): health 200 toolCount=184, `/api/bctc-inspect`+`/dashboards/news-fetch/` both 200. Full `bun test`: 14901 pass/40 skip/64 fail/1240 files (604s) — the 17 unique failing files (vps-proxy-health, insider-transactions, vnstock cash-flow/balance-sheet store, 1875c record-signal-outcome registration flake, FIX-1267 5000ms timeout, etc.) grepped for zero reference to agentSignalStore/getCycleBootstrap/agentSignalTools — pre-existing order/network flakiness, several already logged unrelated in prior same-day entries (S19/S21 journal). Doc: `docs/agents/tools/list/get_cycle_bootstrap.md` Notes bullet added.

Zone health: tsc clean, 87/87 targeted-suite green, RED->GREEN reproduction of the exact reported defect (not just a passing test), tool/cron counts unchanged, full-suite fail-set keyword-scanned clean of my 3 changed files | HEALTHY.

## 2026-07-29 — FIX-CI-RED-ALERTOUTCOME-CLOCK-SEAM (BOUNDED-1 auto-pickup, S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Confirmed RED locally first (3 pass/6 fail, byte-identical to CI's 3 consecutive red runs). Root cause: `readPendingOutcomeAlerts()` (alertStore.ts:523) computed its 90-day `since` window from raw `Date.now()`, ignoring `runAlertOutcomeJob`'s `deps.nowFn` seam entirely — the seam only reached classification/elapsed-days, not candidate selection — so the test's pinned-`NOW`+relative-`daysAgo` fixtures silently aged out of the REAL wall-clock window every week.

**Fix (AC-1):** added `now: Date = new Date()` as a 3rd param to `readPendingOutcomeAlerts()` (default preserves prod behaviour exactly); `alertOutcomeJob.ts:164` now passes the already-computed `now` (from `deps.nowFn`) through. Only 1 production call site existed (grep-confirmed), so no other caller needed touching.

**Fix (AC-2):** added TEST-10 — NOW shifted 200 REAL days into the past (anchored to `Date.now()` at run-time, never the fixed 2026-05-06 constant, so this control itself can never rot) with fixtures seeded relative to it; asserts `evaluated > 0`. Proved via git-stash A/B that TEST-10 fails on pre-fix code (0 vs `>0`) and passes post-fix — stash popped immediately. The existing pinned-NOW fixture is now also safe from re-drift: window and classification share the same threaded clock.

**Evidence:** target file 10/10 pass (was 3/9 RED). tsc clean. `toolCount=184`/`cronJobCount=88` unchanged. Full per-file-isolation run (`ci-per-file-isolation.sh 16`, the actual CI runner, not bare `bun test`): 14797 pass/40 skip/3 fail — none of the 3 (1324-push-news-all-sources, 1398/1793-pollnews-cooldown) import alertStore/alertOutcomeJob; re-ran individually GREEN (1398/1793 = P16 resource-contention flake; 1324 passed on retry = local missing-chromium-binary/network flake) — pre-existing, unrelated. Live server already running on :3000 health-checked 200/toolCount=184. Doc-review: none required (internal store/job signature change, no tool/API/schema-shape change, no doc referenced this function).

Zone health: tsc clean, target file 10/10 (was 3/9), full per-file-isolation suite 3 pre-existing unrelated flakes (all GREEN in isolation), tool/cron counts unchanged, RED->GREEN proof via git-stash on the new regression test itself | HEALTHY.

## 2026-07-30 — FIX-VNINDEX-CACHE-STARTUP-PURGE (BOUNDED-1 auto-pickup, P2) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. RAW-verified live BEFORE coding per task mandate — the board row's "purged on startup + market-hours-only refresh" hypothesis is WRONG: zero purge code anywhere (exhaustive grep + full git history). Real root cause: the LIVE `market.db` still carries the Sprint-1922b orphan `vn_index_cache` table (id/snapshot_json/source/fetched_at/expires_at/created_at — abandoned 1842a cache design, retired-but-never-dropped, commit `b50ef1777`). `FIX-VNINDEX-CACHE-EMPTY-REFRESH-PATH` (`ac8ad6c78`, 2026-06-20) reused the SAME table name via `CREATE TABLE IF NOT EXISTS`, which silently no-ops against it — every `upsertVnIndexCache()` write has failed with `table vn_index_cache has no column named code` on literally every single tick since 2026-06-20 (confirmed via `docker logs` grep + `sqlite_sequence` showing the orphan's AUTOINCREMENT was NEVER used, 0 rows ever). This is why the prior DONE fix recurred — its own test suite always boots a fresh `:memory:` DB where `CREATE TABLE IF NOT EXISTS` creates the correct schema from scratch, so it never had a chance to catch a name collision that only exists on the long-lived named-volume DB.

**Fix:** `initMarketDataTables()` now detects the legacy/orphan shape (no `code` column) and `DROP TABLE`s it before the `CREATE` — safe (0 rows ever, 0 current readers/writers, already deprecated by its own retirement commit). Runs at DB-init, i.e. every mcp-server startup — the writer/startup path the board note pointed at; refresh cron untouched. New `FIX-VNINDEX-CACHE-STARTUP-PURGE.test.ts` (4/4 pass) reproduces the live orphan shape.

**LIVE verification:** single-service rebuild (`docker compose build mcp-server && up -d --no-deps mcp-server`) against the real named-volume `market.db`. Pre-fix: 0 rows, legacy schema confirmed. Immediately post-restart (before any cron tick): schema already migrated to `code`-keyed shape. Next tick (04:00:03Z): first successful write in this table's ENTIRE history (VNINDEX 1726.38), zero "upsert failed" warnings since. Commit `998ad9b44`. tsc clean, targeted+neighbor suites (schema-slices, freshness-SLA×5, EMPTY-REFRESH-PATH) all green, `toolCount=184`/`cronJobCount=88` unchanged, local `:memory:` boot health 200 + bctc-inspect/news-fetch dashboards 200.

**⚠️ CRITICAL SIDE FINDING (P0, needs immediate ops escalation, NOT fixed by me — out of zone/role):** the restart required for the above verification surfaced an ACTIVE occurrence of the already-known recurring **FIX-SQLITE-DOCKER-VIRT-CORRUPTION** class (precedented 2026-04-25 + 2026-07-13, `docs/incidents/2026-07-13-sqlite-db-corruption-restart-loop.md` — macOS Docker-virtualization torn-SHM-on-container-stop). `PRAGMA quick_check` (+ independent confirm via the container's own `bun:sqlite` runtime, not just host CLI) shows REAL corruption (`database disk image is malformed`, errno 11) in **daily_ohlcv, cron_job_runs, system_logs, pdf_extracted_text** + their indexes — 282 malformed errors in the first 6 live minutes, hitting `push-ohlcv-history`, `prediction-market-job`, `taAlertScan`/`bbAlertScan` (dozens of tickers) every cycle. Container itself is stable (not crash-looping, healthy, `RestartCount=0`) so NOT the acute 07-13 pattern, but is silently degrading live production data right now — `data/live/market.db.corrupt-20260719-1125Z` + `.pre-salvage-20260719-202912Z` + `.salvage-attempt-failed-20260719T203907Z` on disk suggest this may trace back to an incompletely-recovered 07-19 event. `vn_index_cache` itself is NOT affected (independently confirmed healthy read/write). I deliberately did **NOT** attempt any DB repair/restore/2nd-restart myself — that's ops's established playbook (stop, backup corrupt, verify+swap clean backup, restart+verify — precedent doc above) and a 2nd deliberate restart right now risks compounding a live incident for a verification nice-to-have already covered by code-level proof (no purge path exists once migrated). **This needs PO/ops action now** — I have no MCP/gateway tool grant this session so could not `send_telegram` or mint the board row myself beyond what pure-bash `orch-apply.sh` allows.

Zone health: tsc clean, targeted+neighbor suites green, RED→GREEN proof via a test that reproduces the EXACT live production error, tool/cron counts unchanged, LIVE restart-cycle RAW-verify against the real named-volume DB (not just code-read-and-assert) | HEALTHY for my own change, but flagging a P0 unrelated live-DB corruption discovery — see above.
