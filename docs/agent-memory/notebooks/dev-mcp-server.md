# dev-mcp-server -- Notebook

## 2026-08-06 — DEFLAKE-VNSTOCK-3STATEMENT (P2/S, recurring-bug escalation, BOUNDED-1 auto-pickup) → REVIEW, next_agent=qa

**Session:** 24817246-8a3f-4511-95f7-1b4385797bee. 4th observation of a same-code green→red flake in `vnstock-3statement.test.ts` under CI per-file-isolation (obs 4: docs-only intervening commit, strongest evidence yet). Live-reproduced (not just theorized) TWO independent defects — the board row's own suspected candidates (DB_PATH env leak, mock.module poison) turned out to be a red herring for the actual CI-observed symptom once I read `docs/spikes/SPIKE_CI-PERFILE-ISOLATION-FLAKE.md` finding #1: `ci-per-file-isolation.sh` runs each file as a SEPARATE OS process (`bun test "$f"`), so cross-file env/module-cache leakage is structurally impossible there — only a plain shared-process `bun test` can exhibit that class. (1) `schema.ts` `getDb()` set `PRAGMA busy_timeout=5000` LAST, after `journal_mode`/`synchronous`/`foreign_keys` — any lock contention during those earlier statements threw `SQLITE_BUSY` immediately instead of retrying 5s. Reproduced: 20 concurrent `bun test` processes of this file alone hit the exact stack (`getDb→journal_mode→SQLITE_BUSY`) in 1/20 runs. (2) The test itself overrode `DB_PATH` to a real on-disk temp file keyed only by `Date.now()` (ms resolution, no PID) — contradicting its own "in-memory DB" comment; 20 concurrent copies produced 3 exact path collisions/20 samples. Fixed both at source: busy_timeout reordered first (schema.ts, benefits every `getDb()` caller, not just this test); test switched to a true `:memory:` DB (matches its stated intent, eliminates the on-disk-file class entirely — no retries/test.retry added, per AC-2's explicit prohibition). Separately, while stress-testing in combination with sibling zone files, found and fixed a 3rd, REAL (but per-file-isolation-inapplicable, shared-process-only) defect: `1466-sync-db-corruption-bail.test.ts`'s `afterAll` "restore" of `vnstockStore.js` re-registered its OWN no-op stub instead of the true original module — not a restoration. In a shared-process run this permanently poisoned any later import of `vnstockStore.js`, including vnstock-3statement's `?isolate=` query-bust workaround (mock.module rebinds the module's live export bindings in place — a bare captured namespace-object reference reflects whatever is CURRENTLY mocked, not the pristine snapshot; fix required spreading into a plain object at capture time). This makes AC-3's `?isolate=` workaround no longer load-bearing.

**Evidence:** 50x solo per-file-isolation runs under the exact self-contention pattern that reproduced the original failure = 0 fail. 15+15 interleaved with `167-prediction-market-job.test.ts` (obs-3's co-failing sibling) = 0/30 fail. Minimal 3-file repro for the mock.module defect (`1291-foreign-flow-duplicate-dedup-migration` + `1466` + `vnstock-3statement`) = 0/18 fail (was 8/18 pre-fix, confirmed via bisection of a 9-file prefix). Targeted 25-file vnstock/foreign-flow/schema zone sweep = 282/282 pass (was 274/282 pre-fix, same combined-process ordering). `bun tsc --noEmit` clean. `eslint src/` unaffected (`src/__tests__/**` is excluded by config; only `schema.ts` is a production file and lints clean standalone). Full `bun test`: 15144 pass/40 skip/48 fail/2 err (663s) — zero vnstock-3statement/1466-related failures (grep-confirmed on file+test names), within the documented pre-existing flaky floor (unrelated: vps_push_log/insider_transactions/foreign_flow/market_cap tool tests, mostly 5000ms-timeout pattern). Commits: `8931e47c7` (code+3 test/prod files), `951151d00` (board lane-move). Board `in_progress[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`, `.head` synced idle (branch:null self-closeout). **Tool-grant note:** this spawn's grant is Read/Edit/Write/Bash only (no gateway MCP tool) — could not call `task_release` myself; dispatching router session (INV-GATEWAY-1) RAW-verifies + releases.

Zone health: tsc clean, 3 files touched (1 prod pragma-reorder + 2 test files), full suite 48 fail/2 err (within/below the documented 52-61 flaky floor, 0 regressions — zone-sweep + full-suite grep confirmed) | HEALTHY.

## 2026-08-06 — FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART (P1/M, router-dispatched CI-red hotfix, PO batch #1) → REVIEW, next_agent=qa

**Session:** 24817246-8a3f-4511-95f7-1b4385797bee. Board's own `detail` (mint 2026-07-16) blamed `registerAgentMemoryTools -> getProjectRoot() -> execSync('git rev-parse')` for the recurring 1299b TDZ flake — but `getProjectRoot()` was ALREADY fixed to a pure fs walk-up on 2026-07-13 (`252f8ffd1`, predates this row's own creation). Verified against the actual live CI-red evidence cited (run 31087473563, HEAD 25111889a, `gh api .../jobs/92570249394/logs`): per-file isolation already bounds today's failure to exactly 1 file/1 assertion, not the 6/9 cascade the old spike described — the execSync mechanism is stale, but the STRUCTURAL defect the row's own AC names (`buildToolNameMap()` still ran as a bare top-level `const toolNameMap=...` statement — on every import, including server.ts's live boot path + ~30 transitive test-file importers) was still live. Fixed the structural half: converted the eager top-level probe into a lazy, memoized `getToolNameMap()` accessor (module-scope `let`, not `const=fn()`), called only from `resolveToolNames()`. A still-building `let` can never leave the module's ES-module Evaluate() step half-finished the way an aborted top-level `const` initializer can — importing agentBootstrap.js is now O(1)/side-effect-free regardless of how slow any of the ~107 probed registryFns are, present or future.

**Evidence:** New guard `FIX-AGENTBOOTSTRAP-EAGER-EXECSYNC-COLDSTART.test.ts` (5/5 pass): AC-1 no execSync/child_process in agentBootstrap.ts, AC-2 `buildToolNameMap()` only ever called from an indented (non-top-level) line, AC-3 no bare top-level `const X=buildToolNameMap()` regression, AC-4 importing the module resolves <500ms (cold-start non-regression), AC-5 lazy probe still resolves the full ~107-fn registry + stable across repeated calls. `1299b-skill-gated-bootstrap.test.ts` 9/9 pass (368ms). Blast-radius sweep (39 files: 6 direct importers + 31 files transitively importing via server.ts + the projectRoot execSync-removal guard): 329 pass/0 fail. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 unchanged. Fresh boot (`PORT=3099` — port 3000 held by a live pre-existing instance): `/health` toolCount=183, `/api/bctc-inspect` + `/dashboards/news-fetch/` both served valid HTML (dashboard circular-dep check clean). Full `bun test`: 15126 pass/40 skip/54 fail/1 err (545.59s) — within the documented 52-61 same-notebook flaky floor (ran concurrently with a peer dev-mcp-server session's own full-suite run); none of the 54+1 failures are agentBootstrap/1299b-related (targeted blast-radius sweep above is the corroborating evidence, not just floor-comparison). Commit `7a3db0616`. Board `backlog[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`; cleared the stale `plan_only:true` flag left over from a 2026-07-16 BOUNDED-1 mis-claim revert (this dispatch was an explicit router spawn per PO's fresh 2026-08-06T09:21Z triage, not an auto-pickup). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md` S68 (file then hit BYTE_CAP, rolled to `-4.md` for future writes).

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite 54 fail (within flaky floor, 0 regressions — blast-radius-sweep-confirmed), 2 files touched (1 prod refactor + 1 new guard test) | HEALTHY.

## 2026-08-06 — FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION (P2, PO-minted mirror of FIX-FOREIGN-FLOW-BULLETIN-UNAVAIL-STRING) → REVIEW, next_agent=qa

**Session:** 24817246-8a3f-4511-95f7-1b4385797bee. Both bulletins pushed nothing for `foreignFlowMovers=[]` (outage/DB-error/all-flat collapse to the same empty shape under the `<>0` SQL filter) — the whole "Khối ngoại" section vanished silently instead of showing the canonical unavailable copy that the just-shipped sibling fix wired in one branch lower (unreachable by the real query). New `formatForeignFlowSectionOrUnavailable()` wrapper renders the unavailable line on empty input; used at both `eveningSummaryJob.ts`/`franceSummaryJob.ts` call sites. `formatForeignFlowSection()` itself untouched (frozen by 1503/1783/ZERO-PAD-TOPN tests). **Flagged deviation:** did NOT touch `assembleEveningSummary.ts`'s SQL as PO's `files[]` expected — `FIX-DIGEST-FOREIGN-FLOW-ZERO-PAD-TOPN.test.ts` T-1/T-2 (frozen) lock that exact filter's current behaviour; fix_spec's own escape hatch ("ship neutral wording if not cheaply distinguishable") covers this. Updated 1516 AC-4's stale assertion (explicit empty array → silence was literally this bug).

**Evidence:** new `FIX-FFLOW-BULLETIN-OUTAGE-SILENT-OMISSION.test.ts` 11/11 pass. Targeted sweep (1783+ZERO-PAD-TOPN+1516+1503+new) 47/47 pass. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: tools=183/cron=88 unchanged. Full `bun test`: 15123 pass/40 skip/52 fail/0 err (483s) — floor of the documented 52-60 flaky band; grep-confirmed the 6 foreign-flow-named failures are in unrelated MCP tools (`get_foreign_flow`/`get_company_profile`, not the files touched here), timeout-pattern. Commits `8521c4a23` (code+tests) + `ba4005738` (briefings.md doc). Board `backlog[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-3.md` S67. **Concurrency hazard hit mid-task:** uncommitted edits to these 3 files were transiently wiped-then-restored by a peer session's shared-index git churn (`git reset`/broad add) — re-verified content fresh before each commit, used explicit-pathspec commits (2nd commit's staged index held ~10 unrelated peer files, correctly excluded).

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite 52 fail (floor of flaky band, 0 regressions), 4 code/test files + 1 doc touched | HEALTHY.

## 2026-08-06 — FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM (P2/XS, PO recurring-bug escalation, BOUNDED-1 auto-pickup) → REVIEW, next_agent=qa

**Session:** 24817246-8a3f-4511-95f7-1b4385797bee. `get_bctc_refined` had no projection param — SELECTed `markdown`+every column for every prior unit of a report on EVERY call, even though `refine_bctc_md`'s Phase 0 resume step only needs a skip-set of unit_ids; the read grew with every completed chunk (measured worst live case: report b061d92b, 77 units/103,606 markdown bytes ~30k tokens, for a caller that wanted a list of ids). Same overflow family as sibling `FIX-PENDING-REFINE-OUTPUT-235K-OVERFLOW` (`get_bctc_pending_refine`, different tool/caller) but explicitly NOT the root cause of the 2026-08-06 zero-push fires per the row's own honest sizing (measured 12-unit case = 25,316 bytes, material but too small). Added optional `fields:'ids'|'full'` (default `'full'` — bctc-analyst/ESC-5 gate unaffected, byte-identical). `fields:'ids'` SELECTs only `unit_id, window_status` (no markdown read from DB) and always returns `{report_id, total_units, units:[{unit_id, window_status}]}` — including empty `units:[]` on zero pushed units (a valid resume state on a fresh report), never the `'full'` path's `{error}` shape. Updated `refine_bctc_md/flow/main.md` Phase 0 step 5 to pass `fields:"ids"` and documented the actual returned shape (was `{units:[{unit_id}]}`, never produced).

**Evidence:** new `FIX-GET-BCTC-REFINED-NO-PROJECTION-PARAM.test.ts` 7/7 pass (served path, real McpServer+`_registeredTools`): AC-1a ids-shape/no-markdown-anywhere, AC-1b full path byte-identical to pre-fix + zero-unit error preserved, AC-1c ids empty-set structured shape, AC-3 12-unit ids payload <4KB, 77-unit ids payload >10x smaller than the equivalent full payload. `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: tools=183/cron=88 unchanged (no tool added/removed). Fresh boot `PORT=3099`: `/health` toolCount=183, `/api/bctc-inspect` + `/dashboards/news-fetch/` both served (dashboard circular-dep check clean). Full `bun test`: 15139 pass/40 skip/53 fail/1 error (470s) — within the documented 52-61 flaky floor; targeted bctc/financial-reports zone sweep 1380 pass/0 fail across 121 files, zero overlap with the 52 full-suite failure names (grep-confirmed). Commit `ff36391dc` (code+test+docs) + `11e139f15` (board lane-move). Board `in_progress[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`, `.head` synced idle (branch:null self-closeout). DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S71. **Tool-grant note:** this spawn's actual grant is Read/Edit/Write/Bash only (no gateway MCP tool) — could not call `task_release` myself; per this zone's own flow (INV-GATEWAY-1) that release is the dispatcher session's job anyway.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite 53 fail/1 err (within flaky floor, 0 regressions — zone-sweep-confirmed), 4 files touched (1 prod + 1 new test + 2 docs) | HEALTHY.
