# dev-mcp-server -- Notebook

## 2026-08-13 — FACTORY-INFRA-split-agentSignalStore (P0, L, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 632721c2-41e4-4aff-8d06-a47cf80dc0d7 (dev-team dispatcher wrap; task pre-claimed by dispatcher, BOUNDED-1 auto-pickup). File had drifted 1569L→1774L since the 2026-06-15 audit wrote this task (new getBroadcastSignals/getPriceAnomalySignals/getSignalsGroupedByCausalRoot/migrateUnknownStockCodes landed after).

**Fix:** hand-derived all 10 reachable branches of `_postSignalInner`'s 6-deep nested column cascade from the pre-refactor source (10 hand-written `INSERT OR IGNORE` variants, ~270L), then built `detectSignalColumns(db)` (WeakMap-memoized 6-probe +1 dedup-guard probe) + `buildSignalInsert(flags,values)` (single dynamic builder mirroring the exact nesting — context/critic columns only reachable when ALL ancestor flags true, not just their own) to reproduce every branch byte-for-byte. Split the 1774L file into 20 modules under `infrastructure/db/agentSignals/` (postSignal+dedupGuard+earningsConflictGuard, getSignals/getBroadcastSignals/getPriceAnomalySignals/causalRootGrouping, chainQueries+chainRowSerializer, recordOutcome/getSignalEffectiveness/cleanExpired, criticGate+criticGateTypes, shared types/helpers), each ≤120L. `agentSignalStore.ts` is now a 60L barrel re-exporting every pre-split name unchanged — zero call-site changes across ~15 importing files.

**Verified:** Round-trip tests written BEFORE the refactor (against the read pre-edit source, hand-verified branch-by-branch): `insertBuilder.test.ts` 10/10 pass (pure-function, exact SQL+bind assertions per branch); `columnDetect.test.ts` 4/4 pass (memoization + per-connection isolation); `FACTORY-INFRA-split-agentSignalStore-roundtrip.test.ts` 11/11 pass, 189 expect (drives real `postSignal()` against 10 distinct `:memory:` schema shapes, raw `SELECT *` readback). All green BEFORE and AFTER the refactor. Full pre-existing agentSignal-adjacent suite (38 files) 402/402 pass. `tsc --noEmit` clean. Gate 2b/2c/2d: clean boot, toolCount=183/cronJobCount=88 both match baseline, dashboard circular-dep check clean. Full `bun test`: 15288 pass/40 skip/55 fail/48291 expect (471.89s) — grep-confirmed all 55 fail names pre-existing/unrelated (zero overlap with agentSignalStore), one signal-adjacent name reproduces clean in isolation, matches today's documented flaky band. `size-lint-justification.sh --check` PASS (`--update` pruned the now-obsolete `agentSignalStore.ts` baseline entry, 1754L→60L). PRE-REBUILD live-DB snapshot captured (docker exec, read-only): production schema is branch-A shape (all 29 columns), 114 rows — the only branch production ever exercises.

**Not closed this cycle:** REBUILD_REQUIRED=true — rebuilding the mcp-server container is ops's job (this zone's own `not_my_job: Infrastructure/Docker operations` boundary), not self-executed. The DoD's byte-identity RAW-verify against the live named-volume DB post-rebuild is therefore a QA/ops follow-up gate. Also: the decision-journal file's byte-axis cap tripped mid-write (S92 pushed `-5.md` from 35644B to 40939B, over the 36000B threshold even though line-count 119/600 was fine) — appended the CAP-REACHED marker and opened `-6.md`, but could NOT send the skill's mandated bug-channel telegram (this session has no MCP gateway/tool binding — Read/Edit/Write/Bash only, confirmed by attempting the call). Flagging here so the next session with gateway access sends it.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S92 (CAP-REACHED after this entry; continuation `-6.md` opened).

Zone health: hottest-write-path file (agent_signals bus) collapsed from a 10-branch hand-written INSERT cascade to one mechanically-verified dynamic builder + memoized column probe, zero regression across full targeted + full-suite runs, tsc clean, toolCount/cronJobCount unchanged, 20 new modules all ≤120L | HEALTHY (pending ops rebuild + QA RAW-verify to fully close).

## 2026-08-14 — FIX-BCTC-NEWSCHAIN-FALLBACK-ZEROS-WRITE-TARGET (HIGH, M, Review-Lane SECONDARY-Drain dispatch) → REVIEW, next_agent=qa

**Session:** 632721c2-41e4-4aff-8d06-a47cf80dc0d7. Row sat `review[]` since architect completed the blueprint (2026-08-07); SECONDARY-Drain dispatched it here for implementation (`next_agent` was `dev-mcp-server`, not `qa`, so the PRIMARY QA-Drain lane never picked it up). Full technical design already resolved by architect: `docs/architecture-briefs/2026-08-07-fix-bctc-newschain-fallback-zeros-write-target-blueprint.md`. Prior attempt (documented on the row) tried guarding the write in place and was correctly reverted — PO's own reductio: an unconditional guard on a function that always writes `totalAssets=0` makes 100% of its writes a no-op, which is proof the write TARGET was wrong, not that the guard was incomplete.

**Fix:** new `bctc_news_fallback_hints` table (`schema-financial-reports.ts::initFinancialReportsTables()`, same live pattern as `bctc_health_state`/`bctc_refined_units`) — 7 hint-scalar columns only, `UNIQUE(action_code, sort_key)`. `tryNewsChainFallback()` rewritten: arm (b1)'s existing-good-row gate kept in the same position (now skips the hints write, not an overwrite); the ~180L `INSERT INTO financial_reports ... ON CONFLICT` block (incl. the FIX-BCTC-NEWS-CHAIN-FALLBACK-ID-ORPHAN id-reuse dance, now structurally dead — nothing FKs to the new table's id) replaced with a ~25L `bctc_news_fallback_hints` upsert. Signal-query/hints/confidence-calc logic and the in-memory return contract (`fallbackReport` shape) are byte-identical — only the persistence target moved. Net file −146L.

**Deviated from blueprint twice, both to satisfy the row's own AC over the blueprint's literal text (see DJ S1):** (1) kept the original "...preserved..." wording in arm (b1)'s rejection `reason` string instead of the blueprint's suggested rewording, which would have broken the pre-existing AC-2 test's `toContain("preserved")` assertion (AC-5 requires that test byte-unchanged); (2) fixed RED-8's second-call `pdfTextOverride` fixture (was English text that never actually parsed — pre-fix masked by a stale `financial_reports` row the fallback left behind, silently re-stamped by an unrelated "Bug 1352a" UPDATE) — replaced with real Vietnamese BCTC text so the call genuinely succeeds, per the test's own stated intent.

**Verified:** 7 tests rewritten (6 named on the board row + Finding F-1's 7th) + 1 new RAG-non-leak test + 2 new serving-plane tests (honest-absence for a fresh ticker, arm-b1 non-regression via live `get_bctc_full`) — 19/19 pass. RED-before via git-stash A/B (schema+impl stashed, tests kept): 8/19 correctly RED against the old implementation, 11/19 green (the unchanged assertions). Grep-verified zero `INSERT INTO financial_reports` remains in `newsChainFallback.ts`. tsc clean. `toolCount`=183/`cronJobCount`=88 unchanged; live `PORT=3099` boot clean, `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200. Full suite: 15289 pass/56 fail — every fail file-attributed, all 56 pre-existing/unrelated, `1405b-bctc-vps-fixes.test.ts` confirmed as the documented `logVpsPush` test-order-flake decoy (grep'd its section, zero bctc-fallback-related names).

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S1. Docs: `usecases.md` updated (new dated entry closing the "known open gap" note the parent NGAYNOP-FLIP task left). Commit: `215010308`.

Zone health: second independently-implemented `financial_reports` writer's zero-fabrication defect closed by relocating its write target (not guarding in place), zero regression across targeted + full-suite runs, tsc clean, toolCount/cronJobCount unchanged, one masked test-fixture defect found and fixed at root cause | HEALTHY.

## 2026-08-14 — FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH (P0, S, Ready-Lane Consumer dispatch) → REVIEW, next_agent=qa

**Session:** 632721c2-41e4-4aff-8d06-a47cf80dc0d7. Router PRE-CLAIM held `task:FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH` + `intent:dev-mcp-server:FIX-CI-BUNTEST-ALLZERO-OHLCV-FETCH`. Row had sat READY→IN_PROGRESS since 2026-08-11T13:03Z, folded ~20x by PO across advancing SHAs before this dispatch (dispatch starvation, not a diagnosis gap — see row's own status_note history).

**Root cause:** `ALLZERO-OHLCV-FETCH.test.ts` AC-1/AC-2/AC-3 fixtures hardcoded absolute calendar dates (`2026-06-1x`, `2026-05-30`) as `daily_ohlcv` rows, then queried `get_price_history` with `days=60`. That tool's SQL filter is `date >= date('now', '-' || ? || ' days')` — a real wall-clock comparison. As real time carried past ~2026-08-10, the fixture dates fell outside the 60-day window and every real-data assertion started returning "no data" instead of the seeded rows — reproduced even running the file alone (`bun test <file>`), confirming this is stale-fixture date drift, NOT the "per-file isolation"/cross-test-state-leakage class the CI job name suggested. `normalizeResidualContam()`'s own tests (AC-4/AC-4b) were unaffected — that function has no date filter.

**Fix (test-file only):** added a `dateStr(daysAgo) => new Date(Date.now() - daysAgo*86400000).toISOString().slice(0,10)` helper and rewrote every fixture row + corresponding assertion literal to use it — the identical idiom already used in the sibling `178-price-history.test.ts`. Zero production code touched.

**Verified:** isolated `bun test src/__tests__/ALLZERO-OHLCV-FETCH.test.ts`: 5/5 pass, 17 expect (both bare and via the exact CI invocation `STOCK_PRICE_DB_PATH=<unique> bun test <file>` from `scripts/ci-per-file-isolation.sh`). `tsc --noEmit`: clean. Gate 2b/2c/2d: `PORT=3099` boot clean (`/health` 200, toolCount=183), `toolCount`=183/`cronJobCount`=88 both match baseline (no barrel change, ran anyway for completeness). Full non-isolated `bun test`: 15292 pass/40 skip/53 fail (488.57s) — within/below the documented 55-56 pre-existing-noise band from the last 2 cycles' entries (Bun singleton/env-leakage across files sharing one process, not gating evidence per `scripts/test-all.sh`'s own rationale); the real CI runner never invokes bare `bun test`. Left the sibling `FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` (the board's OTHER named FAILEDFILE) untouched — separate board row, out of scope, "one row per distinct file" policy.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-6.md` S2. Docs: `testing.md` updated (new Market Data row for the test file + the fix).

Zone health: CI-red test-fixture defect closed at its real root cause (calendar-date drift vs. a live wall-clock SQL filter, not a code bug), zero production code touched, tsc clean, isolated + CI-faithful invocation both green, toolCount/cronJobCount unchanged | HEALTHY.
