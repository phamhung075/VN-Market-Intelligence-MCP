# dev-mcp-server -- Notebook

## 2026-07-31 — FIX-BCTC-1345B-REPORT-BATCH (P3/XS) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Root cause RE-VERIFIED before coding (mandatory per dispatch brief): 1792's `bctc_signal_debounce` only blocks same-(ticker,period) re-fires within a 1h window — retry-storm case, already fixed. The real live gap is `bctcReparseJob.ts` (runs DAILY at 09:30 GMT+7): it retries the SAME still-broken low-confidence backlog every day, 24h apart — well outside 1792's window — so each daily cycle re-fires the `[BCTC-1345b]` alert for an issue that was already known/tracked the day before. That daily re-fire (not same-minute storms) is the actual source of the backlog note's "6 reports today / 11 more the next day, all same low-confidence-guard class."

**Fix (`parseBctcReport.ts` `storeReport()`):** before the `[BCTC-1345b]` alert, query `financial_reports.validation_status` for the same (action_code, sort_key). If already `'low_confidence'` from a PRIOR parse, suppress the send (row still upserts as normal — audit trail intact). Reuses the existing column (no new table/migration); self-closing — the moment a re-parse produces real confidence the status flips off `'low_confidence'` and a future regression can alert again. Rejected: (a) batch-per-cycle digest — no existing cycle-boundary primitive, XS size doesn't justify new infra; (b) tagging the message to piggyback Task-1215's generic 4h category dedup in `telegram.ts` — its window is still shorter than the 24h reparse cadence AND it would over-suppress genuinely-new-ticker findings sharing the category.

**Tests (new file `FIX-BCTC-1345B-REPORT-BATCH.test.ts`, 3 cases):** regression test ages the 1792 debounce row back 2h (simulating the next daily reparse cycle) then re-parses the SAME already-low-confidence ticker+period — RED confirmed via `git stash` of the fix alone (2 sends, expected 1); GREEN after restore (1 send). Second test: 3 genuinely-different tickers each still alert once (not over-suppressed). Third test: after validation_status flips off `'low_confidence'` (simulated fix), a LATER regression alerts again (self-closing proven).

**Evidence:** `bun tsc --noEmit` clean. Targeted suites: new file 3/3, `1792-conviction-debounce` 5/5, `1345e-integration-pipeline`+`1424a-bctc-unit-scale-mismatch`+`1810c-vnm-unit-mismatch` 19/19 — all pass. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged (no tool/scheduler file touched). Full `bun test`: 14977 pass/40 skip/56 fail — same pre-existing full-suite-only flake band already documented in this notebook's S43 entry above (VPS Proxy Health, get_market_cap, insider_transactions, balance/cashflow stores, MCP SSE, 5000ms-timeout/DB-contention class); re-ran every one of those exact failing files standalone → 100% pass, confirming not caused by this diff. Boundary: the file's OTHER `isBctcSignalDebounced` call site (period-content-mismatch, same file) intentionally untouched — that branch rejects the parse before any row is written, so there is no persistent row to gate against; out of the note's `[BCTC-1345b]` scope. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S44.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 2 files touched (parseBctcReport.ts + new regression test) | HEALTHY.

## 2026-07-31 — FIX-CRON-JOB-RUNS-DOUBLE-LOG (BOUNDED-1 auto-pickup, P3/low) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Router DATA-FRESHNESS AUDIT (2026-06-11) finding: `cron_job_runs` writes two rows per (job, started_at) — one blank `rows_written`, one with the integer. RAW-verified live (docker exec, real market.db, 2026-07-31): `predictionResolutionJob` (daily 16:35 UTC) produced exactly 2 rows on every single sampled day (07-21..07-30, 10 consecutive pairs) — the SAME "double-wrap" class already fixed for `runEvidenceAccumulatorWithDb` / `runBaseRateComputationWithDb` / `runBctcReparseWithDb`, never applied to `predictionResolutionJob`. Also live-scanned for OTHER double-logging jobs (14d dup-group query): `pollNewsJob` (275 groups) is a DIFFERENT defect — two independently-completed rows (own `duration_ms`/`finished_at` each), the separate still-open `FIX-SCHEDULER-DOUBLE-REGISTRATION` class — correctly left untouched.

**Fix (`startupHelpers.ts` only — `predictionResolutionJob.ts` untouched, matching the sibling fixes' file-touch footprint):** `runPredictionResolutionWithDb`'s default fn previously called the self-recording `runPredictionResolutionJob()` wrapper, which ALSO calls `recordJobRun` internally. Default fn now calls the core `runPredictionResolution(db)` directly (single `recordJobRun` call) and maps `resolved+unresolvable+excluded` to `rowsWritten`. T4 `shouldSkipRecoveryReplay` dedup guard replicated into the wrapper itself so a genuine same-window double-fire (`recoverMissedExecutions`) still no-ops with zero extra rows.

**Tests (new file `FIX-CRON-JOB-RUNS-DOUBLE-LOG.test.ts`, 10 cases, mirrors FIX-BASE-RATE/FIX-BCTC-REPARSE structure):** RED confirmed via `git stash` of the fix — T3/T4/T4b/T6/T6b genuinely failed against unmodified code (T4: expected 1 row, got 2). GREEN after `git stash pop` restore: 10/10 pass.

**Evidence:** `bun tsc --noEmit` clean. Related-file regression (1420-cron-health-coverage, 1125/PRED-RESOLVER-GAP-FIX/1154-prediction-resolution-loop, FIX-BASE-RATE-COMPUTATION-CRON-DEAD, FIX-BCTC-REPARSE-DOUBLE-WRAP-DEDUP-GUARD, ARCH-CRON-recover-jitter, FACTORY-SCHEDULER-job-table-registry): 130/130 pass. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — unchanged. Fresh server boot (`DB_PATH=:memory:`, free port 3099): `/health` toolCount=183; `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200. Full `bun test` (RAW-verified, full-capture log not tail-truncated): 14987 pass/40 skip/56 fail, 565.38s — inside the documented pre-existing flake band (52-59); grep confirmed ZERO prediction/cron/scheduler/startupHelpers matches in the fail list. Docs: NONE (macro.md's `predictionResolutionJob` schedule-table row is unchanged — internal observability fix only, no behavior/schedule change). Simplicity gate: PASS (Q1 no excess scope — guard mirrors 3 already-proven sibling fixes; Q2 zero new abstractions, reuses pre-existing `runPredictionResolution`/`shouldSkipRecoveryReplay`; Q3 no added indirection; Q4 overhead consistent with file's existing doc-comment density). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S45.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 2 files touched (startupHelpers.ts + new regression test); `predictionResolutionJob.ts`'s own `runPredictionResolutionJob()` export now unused-in-production, deliberately left in place matching precedent | HEALTHY.

## 2026-07-31 — INVESTIGATE-EMPTY-DATA-TABLES (BOUNDED-1 auto-pickup, SPIKE/low) → investigation complete, no code changed

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. SPIKE (feature-spike.md mode, no TDD, no branch — investigation-only per its own AC). Live-verified all 5 zero-row tables (`credit_data`, `insider_transactions`, `public_contracts`, `pharma_events`, `broker_sanctions`) against the real bind-mounted `market.db` (`./data/live:/app/data`, same file all 13 services — not a stale mount) via `sqlite3 -readonly` + `cron_job_runs` history, and REPRODUCED each writer's live runtime behavior with `curl` against the real VPS proxy / origin endpoints (not just a code read — the "never throws" fetcher contract makes "genuinely empty" and "silently broken" produce identical code-level symptoms).

**Verdicts:** `credit_data`=empty-by-design/orphan (Sprint 1922c already fully diagnosed+guarded, zero writer exists, re-confirmed unchanged). The other 4 are **dead-pipeline**, each root-caused: `insider_transactions` — SSC portal (`congbothongtin.ssc.gov.vn`) is Oracle ADF/WebCenter, pure JS-rendered shell (live curl today: HTTP 200, zero `<table>` markup) — regex HTML parsing structurally can't work even though upstream is no longer 503ing (contradicts the 07-29 vps-proxy-server.js comment's outage diagnosis, which is now stale). `public_contracts` — `MUASAMCONG_VPS_PROXY_URL` env var used verbatim by `getMuasamcongUrl()`, drops the `?path=` param needed to reach the results page; proxy forwards to the bare portal homepage instead (live curl confirmed: `<title>Hệ thống mạng đấu thầu quốc gia...</title>`, 0 `table-result` matches). `pharma_events` — 2 stacked defects: (a) `davPharmacyCheckJob` has fired successfully exactly once in 3 months, zero startup-catchup guard (same node-cron `Scheduler.start()` re-seed class already fixed for 6 sibling jobs this sprint — FIX-CRON-SUNDAY-STARTUP-CATCHUP×4/FIX-CRON-SSCCHECKERJOB-DEAD-87D/FIX-BASE-RATE-COMPUTATION-CRON-DEAD — just never swept for this monthly one); (b) `dav.gov.vn` hard-times-out from France (25s, live-tested) with zero VPS-proxy fallback wired, unlike its Sprint-1922-series siblings. `broker_sanctions` — `defaultFetchSanctions()` is a literal explicit `TODO` stub (`return []`, Sprint 1920d scaffolding shipped intentionally without the real scraper); target enforcement page also JS-only ADF shell (shares insider_transactions' obstacle).

**Findings doc:** `docs/spikes/SPIKE-INVESTIGATE-EMPTY-DATA-TABLES.md` — full per-table evidence + verdict table + 4 proposed follow-up FIX tasks (`FIX-PUBLIC-CONTRACTS-MUASAMCONG-PROXY-PATH`, `FIX-DAV-PHARMACY-CATCHUP-AND-VPS-PROXY`, `FIX-SSC-ADF-JS-RENDERING-INSIDER-TRANSACTIONS`, `FIX-BROKER-SANCTIONS-SSC-FETCHER-STUB`), scoped and root-caused for PO/dev-team to mint directly without re-investigating. No code changed — per SPIKE AC, dead-pipeline findings become their own follow-up tasks, not fixed inline. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-2.md S47.

Zone health: no code touched, tsc/tool-count/cron-count unaffected | N/A (investigation-only cycle).
