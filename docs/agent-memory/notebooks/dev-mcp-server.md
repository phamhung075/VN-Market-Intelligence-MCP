# dev-mcp-server -- Notebook

## 2026-07-24 — FACTORY-INTERFACE-move-kinhdich-ta-scoring-down (BOUNDED-1 auto-pickup, DDD pure move) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: DDD layering — move Kinh Dich + TA scoring math out of `interface/mcp/tools/` down into domain/application, pure move (byte-identical output). Investigated at source first, per no-fabrication constraint.

Kinh Dich half: `interface/mcp/tools/kinhdich/kinhDichTools.ts` had 9 score-computation functions (`computeSentimentScore`, `computeFundamentalsScore`, `computePriceScore`, `computeForeignFlowScore`, `computeSectorScore`, `computeMacroScore`, `tickerJitter`, `computeHaoScores`, `computeMacroIndicatorScore`) defined inline in the interface layer, despite already accepting an injected `IKinhDichScoreRepository` (task 1838b) — genuine layering debt. Moved verbatim to new `application/services/kinhDich/kinhDichScoring.ts` — **not** `domain/services/kinhDich/` (that folder's own header says "Pure domain — NO I/O imports"; these functions call `getDb()`/repo directly, so application is the correct, source-verified landing spot, mirroring `application/services/imfConvictionBridge.ts` + `application/usecases/getForeignRoom.ts`). Interface file now imports + calls them; kept `formatKinhDichTradingContext` (response shaping) and `registerKinhDichTools` (parsing/registration) in place. Updated 1 scheduler consumer (`defaultComputeHexagrams.ts`'s dynamic `import()`, previously an interface→domain-ward dependency-direction smell, now fixed) + 6 test files' import paths. Byte-diffed all 9 moved function bodies old-vs-new: **identical**, zero drift.

TA half: investigated `interface/mcp/tools/market-data/technicalIndicatorTools.ts`'s local pure-math helpers (localEma/localComputeRSI/localComputeMACD/localComputeBB/maSignal/rsiLabel/etc) — did **NOT** move. Source check (`docs/handoffs/TASK_P2-B2.md` + `docs/architecture-briefs/2026-05-22-refactor/p2-b-caller-inventory.md`) shows this is already-shipped, deliberate G5 architecture: the real domain TA service was quarantined to `src/_deprecated/technicalIndicators.ts` on purpose (TA business logic's permanent home is the Go microservice, port 5003); the interface-layer duplicate is a documented, intentional DB-fallback, not oversight debt. Moving it to mcp-server's domain layer would resurrect explicitly-retired code and fight a shipped decision — same category as Kinh Dich's own AC-8 "not migrated to microservice," just for TA. Recorded as PARTIAL-by-design in the decision journal, not silently dropped.

Evidence: `bun tsc --noEmit` clean. Targeted kinhdich/macro/diacritics regression (19 files, 457 tests incl. 285/298/299/297/302/278/1292/1007/1408/1410/1414/1416/1982/KD-OBS-01-FIX/1047/TASK17-PAGE11/1501/312/1077): 453 pass/4 skip/0 fail, identical before vs after (git-stash A/B confirmed — stashed run flaked under batch-timing contention on 1501's real network calls, but the SAME flake reproduced on the untouched original code too, and passed clean in isolation both ways, proving it's pre-existing environmental flakiness, not a regression). Full `bun test`: 14727 pass/40 skip/41 fail (one run) and 14725 pass/43 fail (repeat run) — count fluctuates 41-43 run-to-run (confirms flaky/timeout class, not deterministic); grepped every `(fail)` line across the repeat full run — zero mentions of kinhdich/hexagram/macro-score/sector-score/foreign-flow-score; all failures match today's already-documented pre-existing classes (vps_push_log schema-drift, 5000ms MCP-client-timeout, the pre-quarantined `_deprecated/1302-technical-indicators.test.ts` flake). Local server boot probe (port 3999, isolated from the live docker container): health 200, toolCount=184, both `/api/bctc-inspect` + `/dashboards/news-fetch/` dashboards 200. Tool count 184 / cronJobCount 88 unaffected (pure relocation, zero tool/cron surface change). Doc updated: `docs/architecture/microservice/mcp-server/kinhdich.md` (new "Application services" line). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-INTERFACE-move-kinhdich-ta-scoring-down-dev-mcp-server.md`.

Zone health: tsc clean, targeted 453/453 pass 0 fail (byte-identical to pre-change baseline), full-suite fail-count flux (41-43) confirmed pre-existing/unrelated via grep + isolation, tool/cron counts unaffected, 9/9 moved functions byte-diff-verified identical | HEALTHY.

## 2026-07-24 — FACTORY-DOMAIN-name-bctc-cascade-magic-numbers (BOUNDED-1 auto-pickup, pure readability refactor) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: name 2 magic-number families — values byte-identical, presentation only.

VAS row-code guard: `domain/services/financial-reports/lib/lineScan.ts` had inline `val >= 10 && val <= 990 && val % 10 === 0` repeated 3× (findValue's isGuarded + findValueByCode Form A + Form B) — item-code artifact skip for the shared BCTC extractor family (balanceSheetExtractor/cashFlowExtractor/incomeStatementExtractor). Named `VAS_ROW_CODE_MIN=10`, `VAS_ROW_CODE_MAX=990`, `VAS_ROW_CODE_STEP=10` — MIN and STEP kept as separate constants even though both equal 10 (range floor vs. numbering increment are distinct domain facts).

Cascade broadcast-floor (Task 162 market-wide broadcast gate): inline default `6` for min impactScore duplicated 3×/5-occurrences across `cascadeEngine.ts` (1) + application-layer config-load fallbacks in `runImpactChain.ts` (2) and `pollNews.ts` (2, used by BCTC-overdue's `runImpactChain` call chain too via `bctcOverdueCheckJob.ts`). Consolidated into 1 exported SSOT `DEFAULT_BROADCAST_MIN_IMPACT=6` in cascadeEngine.ts (domain layer), imported at both application-layer duplicate sites — same concept repeated verbatim, not distinct concepts sharing a coincidental value.

Evidence: `bun tsc --noEmit` clean. Targeted regression (9 BCTC-extractor files + 7 cascade/broadcast files, 193 tests): identical pass counts before/after (105+88). Full `bun test`: 14727 pass/40 skip/41 fail — grep-verified zero overlap with touched files/concepts (lineScan/cascadeEngine/runImpactChain/pollNews/broadcast/VAS/rowCode/bctcOverdue all absent from the fail list); all 41 match today's documented pre-existing classes (vps_push_log schema-drift, 5000ms MCP-client-timeout, deprecated 1302-technical-indicators flake). Server boot probe (port 3999): health 200, toolCount=184, both dashboards 200. Tool count 184 / cronJobCount 88 unaffected (pure domain/application refactor, no tool/cron surface touched). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-DOMAIN-name-bctc-cascade-magic-numbers-dev-mcp-server.md`.

Zone health: tsc clean, targeted 193/193 pass identical before/after, full-suite baseline unaffected (41 pre-existing fails, grep-confirmed zero overlap), tool/cron counts unaffected | HEALTHY.

## 2026-07-24 — FACTORY-APP-pollNews-layering-fix (DDD pure move) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Task: move the `globalSourceTracker` source-health singleton out of the interface layer down into infrastructure. `eslint.config.mjs` Fence-B already named this exact violation by file ("pollNews.ts imports globalSourceTracker from interface/" — FENCE-LEGACY). Investigated at source first.

Moved the stateful singleton block (`globalSourceTracker` + seeding + `_resetGlobalSourceTracker`, globalThis-stashed for `bun --hot` reload survival) verbatim from `interface/mcp/tools/news-analysis/sourceHealthTools.ts` to new `infrastructure/observability/sourceHealthRegistry.ts` — mirrors the existing stateful-tracker precedent there (`jobMetrics.ts`, `circuitBreakerLogger.ts`). Left `formatSourceHealthTable`/`registerSourceHealthTools` (pure rendering, no state) in the interface file — genuinely an interface concern, not part of the violation. Rewired 3 production consumers (`application/usecases/pollNews.ts` — the Fence-B violation itself; `interface/mcp/tools/system/systemTools.ts`; `infrastructure/fetchers/tradingEconomicsChromium.ts`'s 2 dynamic imports, which was *also* wrongly reaching into interface/) + 6 test files (1332/fix-fetch-source-issue2/FIX-NEWS-CB-FALSE-CLOSED/1832b/1227/1898b). Removed the now-obsolete `eslint-disable` FENCE-LEGACY comment from pollNews.ts and updated the stale eslint.config.mjs comment + a stale precedent citation in getMoneyRadarComposite.ts (both named the moved file directly).

Evidence: byte-diffed the singleton block old-vs-new — identical. `bun tsc --noEmit` clean (no dangling imports to the old path — grep-confirmed zero remaining code references). Targeted 10-file source-health/pollNews suite: 239-240 pass/4 skip, one known singleton-state cross-file flake (passes standalone, fails intermittently only under batch order) reproduced identically before AND after the move — same signature, same file, proving no new regression, singleton semantics genuinely preserved (still one shared object reference across all consumers). Server boot probe (PORT=3099, port 3000 held by live docker): health 200, toolCount=184 = baseline. `gen-project-stats.ts --dry-run`: toolCount 184 / cronJobCount 88, both unaffected (pure infra relocation, zero tool/cron surface change). Decision journal: `docs/agent-memory/decisions/sprint-FACTORY-APP-pollNews-layering-fix-dev-mcp-server.md`.

Zone health: tsc clean, targeted suite flake signature identical before/after (singleton semantics preserved), tool/cron counts unaffected, Fence-B violation resolved | HEALTHY.
