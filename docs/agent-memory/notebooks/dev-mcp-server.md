# dev-mcp-server -- Notebook

## 2026-08-12 — FIX-CI-SIZELINT-TRANSPORT-TS-SSE-REAPER-237L (P0, S, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f. PO BATCH dispatch (row stranded in ready[] since 2026-08-08, FIFO-buried P0, main CI RED on size-lint continuously for ~4 days / 20+ folded ci_red signals — see `FIX-READYLANE-NO-SEVERITY-EXPEDITE-FIFO-BURIES-INCIDENT-P0`). Confirmed genuinely RED at current main HEAD before starting (`gh run view 31583395162` on head `4424dd410`, ancestor of HEAD with zero later commits touching `transport.ts`): size-lint FAILs, 1 offender of 1383 scanned — `apps/mcp-server/src/interface/mcp/transport.ts` at 265L (baseline=126L upper=138L; the row's own `237L` id/title was stale, real growth came from the SSE-reaper consolidation on `FIX-MCP-SSE-SESSION-MANAGER-PERCONN-LEAK-NO-REAPER`).

**Judgment call (per PO's explicit steer — extraction vs. size-justification header):** every method on `SseSessionManager` (`evictSession`/`reapStaleSessions`/`handleSse`/`handleMessage`/`closeSession`) shares the same `sessions` Map + injectable clock; the file's whole reason for existing is that a prior version had 2 parallel Maps + 2 duplicated inline cleanup blocks, which a naive multi-file split would reintroduce (>=2 call sites mutating shared session state again — the exact missed-eviction-path bug this file closes). Closing the ~127L gap to the 138L upper bound would require fragmenting nearly half the file for no cohesion benefit. Chose: trim the file's 57-line historical narrative header (pure prose, retrievable via `git log` + the linked design brief — zero code/behavior touched) down to ~22 lines, then add an accurate `size-justification: 241L — <reason>` header (matches repo precedent: `financialFiguresRules.ts` 300L, `stockAliases.ts` 267L, `sectorRules.ts` ~2040L all use this pattern for cohesive, non-fragmentable files).

**Verified:** `bash scripts/audits/size-lint-justification.sh --check` → PASS (0 unjustified offenders, 1383 files scanned) — was the sole failing file before. `tsc --noEmit` clean (comment-only diff). Targeted `1862c-transport-session-eviction.test.ts` 14/14 pass; `251-mcp-tools.test.ts` (incl. `GET /sse returns 200`) 16/16 pass in isolation. Full `bun test` clean run (killed a leftover `PORT=3099` Gate-2b server that had inflated a 1st contended run 68 fail → this clean run 57 fail): 15249 pass/40 skip/57 fail/48113 expect (502s) — the one transport/SSE-adjacent fail name in the list (`Task 251 > GET /sse returns 200`, 5002ms) reproduces 0-fail in isolation, i.e. a load-induced timeout not a regression; 57 matches today's documented pre-existing flaky-churn band (S85-S88 siblings: 60/63/57/59 fail). Gate 2b/2c/2d: clean boot, `/health {toolCount:183,sessions:0}`, `gen-project-stats.ts` toolCount=183/cronJobCount=88 both match baseline. Dashboard circular-dep check clean (both 200). No architecture-doc update needed — `infrastructure.md`'s `SseSessionManager` section documents behavior/structure, unchanged; only comment prose moved.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S89. Board: `ready[]`→`in_progress[]` via `orch-apply.sh` before starting; `in_progress[]`→`review[]` in the closing commit.

Zone health: main CI size-lint job green again after ~4 days RED (sole offending file across the whole repo scan); zero functional/behavior change (comment-only), tsc clean, toolCount/cronJobCount unchanged, targeted SSE suite 100% green, full-suite drift consistent with today's documented flaky baseline | HEALTHY.

## 2026-08-12 — UC-CCA-P3-FR5-CODE-GATE (P0, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f. Router direct dispatch (2 prior nested-spawn attempts failed structurally — Task-tool-spawned sessions carry no Agent/Task-tool grant). Dependency `FIX-CI-SIZELINT-COORDINATIONSTORE-BASELINE-1388L` confirmed DONE_VERIFIED; live-reverified all 4 line anchors from the row's note unchanged (`coordinationStore.ts` 1173L pre-change: `releaseTask` :888, `releaseOrphanTask` :1000, `ReleaseResult` :391-393, `OrphanReleaseResult` :395-400).

**Fix (architecture brief `2026-08-08-uc-cca-p3-published-marker-gate-skill.md` §6, PO B1 Path-A ruling, implemented verbatim):** new `domain/services/publishedMarkerImmunity.ts` (`isPublishedMarkerTaskId`, pure prefix predicate on `"published:"`). Both `coordinationStore.ts`'s `releaseTask()` and `releaseOrphanTask()` now refuse UNCONDITIONALLY as their literal first statement — before any owner-match or heartbeat-staleness check — returning `{ok:true, released:0, reason:'published_marker_immune'}` / `{released:false, reason:'published_marker_immune'}`, never `released:1`/`true`, for ANY caller. `ReleaseResult` got an additive optional `reason?: string`; `OrphanReleaseResult` needed zero change (already had `reason`).

**Spec-verification finding (not a spec-invalidator):** the row's note cited `coordinationTools.ts:208`/`:320` for the "zero call-site changes" claim — live-verified those line numbers are stale: an UNRELATED prior sprint (`FIX-CI-SIZELINT-COORDINATIONTOOLS-TS-457L`, 2026-08-09) had already split that 457L file into a 54L barrel + 6 per-tool files under `./coordination/`. Read both successor files directly (`taskReleaseTool.ts`, `taskForceReleaseOrphanTool.ts`) — both `JSON.stringify(result)` the store's return value unmodified, so the substantive "zero changes needed" claim holds true; only the cited line numbers were dead references. Documented the correction in `system.md` rather than trusting the stale numbers blind.

**Verified:** RED-first `publishedMarkerImmunity.test.ts` (8 cases: prefix match/non-match, mid-string non-match, case-sensitivity) → GREEN. `coordinationStore.test.ts` extended +4 cases per brief §6 (a: release refused released:0 not released:1; b: orphan-sweep on a genuinely stale published:* lock still refused, proving the guard fires BEFORE the staleness check; c×2: negative control cron:*/sprint-task-shaped ids with identical staleness still release normally) → 20/20 pass. All 20 test files referencing `coordinationStore`/`releaseTask`/`releaseOrphanTask` run together: 335/335 pass. `tsc --noEmit` clean. Full `bun test`: 15263 pass/40 skip/55 fail/48122 expect (488s) — grep-confirmed only 2 fail names touch coordination code at all (`task_release Zod schema` S5/S6 inside `FIX-ORPHAN-FR2-FR6-FR7-INTERFACE-COORDINATION-TOOLS.test.ts`), that exact file reproduces 13/13 clean in isolation — load-induced flakiness, not a regression (consistent with today's documented flaky-churn band, S85-S89 siblings 60/63/57/59/57 fail). Gate 2: clean boot toolCount=183/cronJobCount=88 both match baseline, dashboard circular-dep check clean (both HTTP 200). `size-lint-justification.sh --check`: PASS — `coordinationStore.ts` grew 1173L→1192L, well inside the 1365L upper bound (baseline 1241L + 10% tolerance); no helper-module factoring needed.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S90. Docs: `domain-model.md` + `system.md` (new Invariant 8) updated. Commits: `47f987f50` (code+tests), `068a62d37` (docs).

Zone health: `published:*` release-refusal is now a code-enforced choke-point invariant (ends 3x prose-gate oscillation since 2026-07-02), zero regression across every coordination-lock test file + full suite, tsc clean, toolCount/cronJobCount unchanged, size-lint clean well under the upper bound | HEALTHY.

## 2026-08-12 — FIX-CI-BUNTEST-1108-AGENT-WORK-LOG-STORE (P2, S, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa

**Session:** 165f4245-6173-4054-87fd-c55bb626265f. PO triage (dev-team Step 1, tick 17:00Z) minted this via the ci_red ANTI-AMNESTY BACKSTOP — sole FAILEDFILE across run `31613681006` (head `0b3d494d`) with zero dedup hit anywhere on the board; PASSED on both bracketing runs (`b701e913`, `f954d591`). Brief explicitly forbade bisecting-as-regression until flaky-vs-real was determined.

**Root cause:** `purgeOldAgentWorkLogs keeps rows exactly at the boundary` in `1108-agent-work-log-store.test.ts` is a real-clock race, not a code defect. The fixture INSERTs a row at `datetime('now','-30 days')` and then calls `purgeOldAgentWorkLogs(db, 30)`, whose own `DELETE ... WHERE started_at < datetime('now', '-30 days')` independently recomputes `'now'`. If the wall clock ticks over a second boundary between the two statements, the row flips from kept to deleted. Reproduced deterministically with a standalone `bun:sqlite` script (insert boundary row → inject a 1.1s delay → run the same DELETE): `deleted=1`, matching the CI failure shape. Both bracketing commits are unrelated notebook/orch-state writes — confirms deflake, not regression.

**Fix (test-file only, per dispatch scope):** widened the boundary row to `datetime('now','-30 days','+5 seconds')` — still exercises "row just inside the retention window must be kept" (the `<` not `<=` semantics the test exists to prove), but now needs >5s of real jitter between two back-to-back synchronous SQLite statements to flip, which is orders of magnitude beyond realistic CI scheduling variance. Re-ran the same 1.1s-delay repro against the new value: `deleted=0`. Left `agentWorkLogStore.ts` (production code) untouched — its `datetime('now')` usage is internally consistent (DEFAULT column + all filters use SQLite's own format, no ISO8601 string-vs-space-separator strcompare bypass).

**Verified:** isolated `bun test src/__tests__/1108-agent-work-log-store.test.ts`: 17/17 pass, 41 expect (stable, re-run clean). `bun tsc --noEmit`: clean (worktree had no `node_modules` — ran `bun install` first, 424 packages, before this gate was runnable). Full non-isolated `bun test`: 15262 pass/56 fail/1 error/1272 files (497s) — per `scripts/test-all.sh`'s own documented rationale (Bun singleton/env-leakage across files sharing one process) this is expected pre-existing noise, not gating evidence for a single-file fix; the actual CI runner `scripts/ci-per-file-isolation.sh` never invokes bare `bun test`. No production code touched — Gate 2b/2c/2d (tool/scheduler count, server boot) not triggered.

**Evidence:** DJ `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-5.md` S91.

Zone health: single flaky-fixture defect closed at its real-clock-race root cause (not point-patched by bisection or margin-padding a symptom), zero production code touched, tsc clean, isolated target file stable across repeated runs | HEALTHY.
