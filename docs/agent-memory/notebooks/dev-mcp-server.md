# dev-mcp-server -- Notebook

## 2026-08-08 — FIX-AGENTSIGNALS-FROMAGENT-SCHEMA (P2, stale review[]-lane row) → DONE_VERIFIED, next_agent=pm

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router, Review-Lane SECONDARY-Drain). detail_ref resolved owner/next_agent=dev-mcp-server (own-zone code-verification), not the stale top-level `next_agent:qa`. Did not trust the row's own review_note — independently re-verified every claim: both cited commits real and match description (8a6b798ce made `agent` optional 2026-06-19; 440e924f7 diff-confirmed pure const-extraction of `GetAgentSignalsShape`, zero other line changed); new `FIX-AGENTSIGNALS-FROMAGENT-SCHEMA.test.ts` re-run myself 8/8 pass; 4 precedent/consumer suites re-run myself 28/28 pass (note's "30/30" was an arithmetic slip, immaterial — 0 fail either way); `bun tsc --noEmit` clean.

**Closed the note's own flagged REMAINING GAP myself:** mcp-server image rebuilt 2026-08-07 (postdates both fix commits) — running container already had the fix. Native `mcp__gateway__*` tool binding absent this session (session-scoped gap, gateway-call-contract.md §6 Degraded Mode); used the documented Bash bridge (`scripts/agents-flow/mcp-call.sh` `mcp_call`) for a genuine LIVE RAW-verify against the running container: `from_agent='news-scout'` (sender-history) → valid response, no -32602; neither `agent` nor `from_agent` (inbox mode) → controlled readable error, not a protocol crash; `from_agent=null` (all-producers) → real rows returned. All 3 branches confirmed live — no ops/rebuild gate needed.

**Evidence:** commits `git show --stat` on both hashes; scoped `bun test` re-runs (8/8 + 28/28); `bun tsc --noEmit` clean; `docker image inspect` Created timestamp vs commit dates; live `mcp_call get_agent_signals` × 3 arg shapes. Board `review[]`→`done_verified[]` via `orch-apply.sh` (Stage 0/1 PASS, conservation check OK: 775/775). Commit `5a23565e0`, pathspec-scoped to `orch-state.json` only.

Zone health: no code touched this cycle (verification-only), fix confirmed correct + live + already deployed | HEALTHY.

## 2026-08-08 — OPS-MCP-RESTART-CHURN-UNCLEAN-SHUTDOWN (P1, SLS FALLBACK claim, supervised+plan_only, recurrence #3) → REVIEW, next_agent=po, diagnosis-only

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router). `plan_only:true`/`supervised:true` honored throughout — zero code changed, zero infra command executed (no restart/stop/rebuild/recreate); every command run was read-only (`docker inspect`, `docker logs`, `docker stats --no-stream`, a read-only `docker run --rm --pid=host --privileged busybox dmesg` kernel probe).

**Root cause found (resolves the row's own discriminator):** mcp-server's `bun` process is killed by an UNCATCHABLE kernel SIGKILL from the per-container cgroup (memcg) OOM-killer when RSS hits the container's own configured `3g` cap (`docker-compose.yml` `deploy.resources.limits.memory`, live-confirmed `HostConfig.Memory=3221225472`) — not a graceful SIGTERM. This is why neither shutdown handler ever fires: `composition-root.ts:242` sentinel writer, AND a second, independently-discovered, uncoordinated shutdown handler nobody knew about (`checkpoint.ts:335` `registerShutdownHook()`, wired via `startScheduler.ts:373`, registered on the SAME `SIGTERM`/`SIGINT` before composition-root's own handlers). SIGKILL cannot be intercepted by any userspace code — this is a structural impossibility, not a bug in the sentinel path.

**Evidence (multi-source, cross-referenced to the second, 3/3 independent restarts):** kernel `dmesg` memcg OOM-kill events (`task_memcg=/docker/758e5f874d170599b19cfdd52af3cb5ba241a15b954a701d7b5b49c4324cf108`, task name `bun`, anon-rss 3.02-3.13GB) byte-exact-match the LIVE container's own ID and land within 0.2-2.7s of each of the 3 `docker logs` restart boundaries in this container's life (2026-08-07T05:57:27.6Z / 15:38:18.0Z / 2026-08-08T01:15:03.4Z). Ruled out `news-fetch` (the only other bun-based service) via memory-cap arithmetic (1g cap, would show ~1GB not ~3GB victims). `docker logs` at all 3 boundaries shows ZERO shutdown-handler log lines (not even the first synchronous line either handler logs before any `await`) — ruling out a two-handler-race explanation (a race would still show ≥1 opening line) in favor of "neither ever runs." Live 4th-cycle corroboration captured same session: `docker stats` shows the CURRENT instance already at 2.312GiB/3GiB (77.05%) ~12h16m into its life — same trajectory, in progress, not just historical.

**Corrects a prior-cycle conclusion on this exact row:** `po_evidence_refresh_20260805T1037` used `docker inspect` `ExitCode=0`/`OOMKilled=false` to call the host-OOM re-route branch negative. Those Docker-summary fields are a **demonstrated false negative** for this kill shape (kernel dmesg contradicts them directly, 3/3) — but the underlying host-level-headroom conclusion still stands (this is container-cgroup-scoped, not host-wide; different plane, re-route condition still does not trigger — checked explicitly, see brief §6).

**Secondary finding (flagged, not the dominant cause — do not conflate):** the two competing shutdown handlers (§ above) would also race on a genuinely graceful SIGTERM (real `docker stop`/redeploy) — no live instance of that was observed this pass, only OOM-SIGKILL deaths, but it's a second, independent contributor worth fixing alongside the primary lever.

**Evidence:** `docs/architecture-briefs/2026-08-08-ops-mcp-restart-churn-cgroup-oom-diagnosis.md` (full evidence chain, timestamp tables, re-route-condition checklist, recommended-not-decided fix directions). Board `task_board.in_progress[]` → `task_board.review[]` via `orch-apply.sh` (Stage 0/1 PASS, conservation check OK: 762/762) — `status:REVIEW`, `next_agent:po`, `owner` unchanged (`dev-mcp-server`), `dev_diagnosis_20260808T1345` field added, `supervised`/`plan_only` preserved verbatim; `.head` reset to `idle` same write (WF-1 STOP-RELEASE idle-reset convention).

Zone health: diagnosis-only cycle, zero prod files touched, zero test run required (no code shipped) | HEALTHY.

## 2026-08-08 — FIX-CI-SIZELINT-CHECKFOREIGNFLOWGAP-NEW-OFFENDER-181L (P0, XS, dispatch_lane=dev-mcp-server) → REVIEW, next_agent=qa, split not header

**Session:** 165f4245-6173-4054-87fd-c55bb626265f (router RLC dispatch). Row fired CI RED 7 consecutive times (SHAs 83bb4359→76466302) since 2026-08-07T05:27Z; `checkForeignFlowGap.ts` was 181L, no baseline entry, no header (`bash scripts/audits/size-lint-justification.sh --check` confirmed new-offender). Read the file first per the task's own instruction to not default to a header: the D-NEW4 forensic root-cause header (~59L) is genuinely load-bearing (mirrors why `docs/architecture-briefs/2026-07-24-...` itself chose not to split), but `findForeignFlowGapDays` (pure calendar-math gap-day finder, 44L incl. JSDoc) is a clean, cohesive unit with zero coupling to the escalation/dedup logic in `checkForeignFlowGap` — a clean split was cheaper than a header.

**Split:** extracted `findForeignFlowGapDays` + its `MAX_LOOKBACK_DAYS`/`MAX_GAP_DAYS_PER_RUN` caps + the calendar-usage rationale paragraph into new `foreignFlowGapDetection.ts` (81L). `checkForeignFlowGap.ts` re-exports `findForeignFlowGapDays` (`import {...}; export {...};`) so the public import path is unchanged for the existing test file (`FIX-FOREIGN-FLOW-MISSING-TRADING-DAY-2026-08-06-NO-BACKFILL.test.ts` imports both symbols from `checkForeignFlowGap.js` — zero test-file edits needed). Trimmed the remaining root-cause header's prose (tighter wrapping, no facts dropped) to land `checkForeignFlowGap.ts` at exactly 120L (was 125L after the mechanical split alone).

**Verified:** `bash scripts/audits/size-lint-justification.sh --check` now reports only the separate, out-of-scope `coordinationStore.ts` offender (1 file, not 2) — confirms `checkForeignFlowGap.ts` cleared. `bun tsc --noEmit` clean. `bun test` on 3 relevant suites (dedicated D-NEW4 test, `157-data-audit-job.test.ts`, `FACTORY-SCHEDULER-split-dataAuditJob.test.ts`) — 40/40 pass. `eslint --max-warnings 0` on both touched files — clean.

**Notebook-write process note:** first Edit attempt (prepended at file top) landed with tool-reported success but `git diff` showed zero change afterward — worked around per the documented Edit-tool-hook-silent-strip pattern via raw bash append + explicit-pathspec commit; also corrected placement — this file's own git-log-verified convention is append-at-bottom (chronological), not prepend.

**Evidence:** commit `638df5da0` (pathspec-scoped to `checkForeignFlowGap.ts` + new `foreignFlowGapDetection.ts`). Board `task_board.in_progress[]` → `task_board.review[]` via `orch-apply.sh`, `status:REVIEW`, `next_agent:qa`, `commit_sha` stamped. Did not touch `coordinationStore.ts` (separate row/owner) or the `FACTORY-GUARD-CI-size-lint-justification` design row/brief, per task scope.

Zone health: split verified locally green (tsc+tests+eslint+size-lint script), CI confirmation pending next push | HEALTHY.
