# dev-mcp-server -- Notebook

## 2026-07-30 — FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H (BOUNDED-1 idle-pickup, P2) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. **Row's own hypothesis was WRONG — corrected at source, not blindly implemented.** The row assumed `signal_quality_audit.created_at` tracks the same-named `monthlySignalQualityAuditJob` cron (`0 0 1 * *`) and that 48h graded the wrong cadence class. Traced the table's writer: `insertSignalQualityAudit()` (`infrastructure/db/signalQualityAuditStore.ts`) has exactly ONE call site — `post_agent_signal`'s handler (`agentSignalTools.ts`), fired ONLY when `signal_type` ∈ {`price_confirmation`,`urgent_news`} AND `finding_data.confidence` is a number. `runMonthlySignalQualityJob()` (the actual cron) queries `signal_rejections` and sends a Telegram WORK report — it **never writes `signal_quality_audit`**. The table genuinely is event-driven, matching the ORIGINAL pre-fix comment; the real defect is that 48h was too tight for how rarely that specific event fires — live DB history: 6 rows ever, real gaps >2 weeks apart even in a healthy period, zero since 2026-06-05T20:12Z (the 76067min/52.8d staleness figure backs out to exactly that timestamp, not any monthly-cron boundary).

**Fix:** `DEFAULT_SLA_CONFIG` signal_quality_audit `defaultThresholdMinutes` 2880 (48h) → 43200 (30d) — sized off the OBSERVED real writer cadence, not the wrong monthly-cron premise. Satisfies AC-1's fixture (20d=28800min → ok; 40d=57600min → breached) and still fires CRITICAL on the confirmed production gap (76067 > 43200×1.5=64800) so the genuine dead-spot signal is not silenced. Corrected all 3 misleading "event-driven ... tracks the monthly job" comments across `freshnessSlaChecker.ts` (×2) and `freshnessSlaMonitorJob.ts` (×1) to state the real writer path so a future reader doesn't re-litigate the same wrong hypothesis.

**AC-2 (confirm/refute at source, do not guess):** queried the LIVE named-volume `market.db` `cron_job_runs` table directly (`docker exec` + `bun:sqlite`). A naive `SELECT * ... LIKE ... ORDER BY` hit the table's known page corruption (`database disk image is malformed`, tracked separately as `FIX-SQLITE-DOCKER-VIRT-CORRUPTION-RECUR-20260730`, same DB, unrelated defect) — worked around it with an exact `job_name=` match against the live `idx_cron_job_runs_job_started` index, which bypasses the corrupted scan path. Result: `monthlySignalQualityAuditJob` has exactly 2 rows ever — `2026-05-01T00:00:00` and `2026-06-01T00:00:00`, both `status=success` — **NO row for 2026-07-01**. CONFIRMED: the cron genuinely missed its July fire. This is real but does NOT explain the SLA-breach defect (the cron never wrote `signal_quality_audit` in the first place).

**AC-3 (backfill or file follow-up):** filed `FIX-MONTHLYSIGNALQUALITYAUDITJOB-MISSED-JULY-RECOVER-GUARD` (BACKLOG, P3, owner dev-mcp-server) via `orch-apply.sh` rather than live-backfilling — a live Telegram-sending production side-effect is outside this zone's TDD/code-change scope for a defect that (per the trace above) doesn't touch this row's actual SLA breach; row documents the confirmed missed run, the T4-dedup-guard prerequisite before `recoverMissedExecutions` can safely flip to `true`, and the closing-2026-08-01 window if a manual backfill of June's audit is instead preferred.

**Evidence:** new dedicated `FIX-SLA-SIGNALQUALITYAUDIT-MONTHLY-CADENCE-MISCLASSIFIED-48H.test.ts` (8 tests) — AC-1 bidirectional (20d ok / 40d breach, both via `checkSignalSla` and the full `checkDataFreshnessSla` pipeline), config pin, fixed-not-calendar-derived threshold check, boundary tests at exactly 30d/30d+1min, and an explicit anti-false-green test asserting the real production 76067min figure still breaches CRITICAL post-fix. Updated `1920i-freshness-sla-extension.test.ts` TC-6f to the new value. tsc clean. Targeted SLA suite (12 files, 251 tests) 251/251 pass. Full `bun test` run TWICE post-fix: 14897 pass/40 skip/**52** fail (546s) and 14894 pass/40 skip/**55** fail (562s) — both within the standing pre-existing-flake band (54-59, per recent sibling-fix baselines this sprint); grep-confirmed zero of either fail list references `freshnessSlaChecker`/`freshnessSlaMonitorJob`/`signal_quality_audit`/this task ID — same recurring theme (5000ms MCP-client timeouts, vnstock/vps_push_log store schema issues) both runs. `1408-tool-diacritics.test.ts` (the sibling-row unblock this same tick) independently re-confirmed GREEN (8/8) — CI's `bun test` job is also green on the latest push (only `frontend-eslint` lockfile-drift and `size-lint` on unrelated files are red).

Zone health: tsc clean, dedicated bidirectional regression suite green, root-cause hypothesis actively corrected (not rubber-stamped) with the correction documented in code/tests/journal, both confirmed-at-source sub-defects (SLA threshold + missed cron) resolved via distinct closing actions (fix vs filed follow-up), anti-silencing test asserts the real production gap still fires post-fix, full-suite fail-set keyword-scanned clean across 2 consecutive runs | HEALTHY.

## 2026-07-30 — FIX-SQLITE-JOURNALMODE-WAL-REARM-DEFEATS-DELETE-MITIGATION (BOUNDED-1 auto-pickup, P1) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. Live safety check FIRST (before any code change): `docker exec` into `vn-market-intelligence-mcp-mcp-server-1`, read-only PRAGMA against `/app/data/market.db` — `journal_mode=delete`, no live `-wal`/`-shm` pair (only historically-suffixed `.corrupt-*`/`.backup*` snapshots sit alongside it). Not an active-breach window today; the defect is a transient re-arm during any backfill/migration run.

**AC-1:** `bctcEvalBackfillRunner.ts:44-49` opened its own `new Database(dbPath)` + `PRAGMA journal_mode=WAL` — replaced with the shared `getDb()`/`closeDb()` singleton from `schema.ts` (sole owner of `journal_mode`, DELETE since `157335892`). Dead `Database` import removed.

**AC-2 sweep (fresh grep, not the stale triage list):** `coordinationStore.ts:78` — different file (`coordination.db`), out of scope (SPIKE-SQLITE-DOCKER-VIRT-CORRUPTION-HARDENING AC-B), untouched. ~90 test files + all 4 `smoke-task-lock*.ts` — `:memory:`/tmp fixtures only, zero prod-DB risk, untouched. `run-bt7-backfill.ts` — real `market.db` but only *reads* `journal_mode` (no `=` assignment), untouched. **Found the SAME real-market.db WAL-setter defect in 4 files** the triage list mislabeled: `scripts/migrations/{run-finalize-bctc-refine.ts:36, dedupe-mislabeled-bctc-period.ts:368, resync-watchlist-sysmap-2026-07-11.ts:266, carry-forward-bctc-orphaned-rows.ts:361}`. NOT fixed by me — outside my `zone_restricted: apps/mcp-server/`; flagged in RETURN for the coordinating session to route. Live vector still open after this task closes.

**AC-3/4:** new `scripts/audits/verify-market-db-journal-mode.sh` — asserts through the RUNNING container: read-only `docker exec bun -e` PRAGMA + exact-filename `-wal`/`-shm` check (excludes suffixed backup copies). `--self-test` builds two in-container fixture DBs (forced WAL / forced DELETE) and proves the guard FAILs (exit 2) and PASSes (exit 0) respectively — both branches verified live. Exit contract 0=PASS/2=FAIL/3=ERROR, verdict always stdout line 1.

**Evidence:** `bun tsc --noEmit` clean. Live guard: `verdict=PASS journal_mode=delete wal_present=false shm_present=false`. `--self-test`: `SELF_TEST verdict=PASS` (both branches correct). No pre-existing test exercised `bctcEvalBackfillRunner.ts` (zero importers, grep-confirmed). Full `bun test` run for regression-check.

Zone health: tsc clean, AC-1 diff minimal (import swap + pragma removal only, git-diff confirmed), AC-3/4 guard proven bidirectionally via live self-test (not source-only), live prod DB confirmed healthy before+after, AC-2 sweep found 4 real defect instances the triage list missed with an explicit non-silent zone-boundary call on them | scope note: NOT fully closed system-wide — see RETURN.
