# dev-mcp-server -- Notebook

## 2026-08-07 — TASK_601 coordinationStore.ts gcExpiredLocks cron-registration:* exclusion (P2, S, pm-decomposed from FIX-CRON-REGISTRATION-PREFIX-NOT-EXCLUDED-ORPHANEMIT-AND-D4-R1B, 1 of 3 — 602 parallel, 603 gated on both) → REVIEW, next_agent=qa

**Session:** 085954f2-75fe-44ef-8fbe-c550db2da453. Phase-1 pre-GC scan in `gcExpiredLocks` (`coordinationStore.ts`) excludes adoptable-kind rows from orphan-signal emission by `task_kind` allow-list + a `task_id` prefix guard (`published:%`/`cron:%`/`dev-team-cron-singleton`) for markers that carry `task_kind=sprint-task` but aren't real work. A new `cron-registration:<family>` marker namespace (separate, not-yet-shipped work) needed the same treatment — added `AND task_id NOT LIKE 'cron-registration:%'` to the same WHERE clause, 1 line. Updated both doc-comment blocks (`ORPHAN_EMIT_ALLOW_LIST` header + inline Phase-1 comment) with the new marker's rationale (8-day backstop TTL designed to outlive the <=30min session-presence expiry — expected steady-state, not an orphan). **Critical constraint honored:** Phase-2 DELETE (line ~587) independently re-verified to have zero task_id filter at all — untouched, so expired `cron-registration:*` rows still GC normally, just silently (no signal).

**Evidence:** `bun tsc --noEmit` clean. Scoped `bun test FU-LOCKSTORE-EXPIRED-GC.test.ts task-lock-coordination-store.test.ts` — 54 pass/0 fail/163 expect() calls, 0 regressions. `gen-project-stats.ts --dry-run`: tools=183/cron=88 unchanged (no tool/cron touched). Full suite intentionally not run — TASK_603 (depends_on both 601+602) owns AC-4 full-suite + AC-6 deploy per dispatch instructions. Commit `951ddfdba`. Handoff `docs/agents/dev-team/handoff/TASK_601.md` Implementation Record appended. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S73. Board `ready[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), scoped sweep 54/54 pass, 1 prod file touched (11 lines, comment+1 WHERE-clause line) | HEALTHY.

## 2026-08-07 — TASK_602 tasksMdJanitorJob.ts KNOWN_LEGIT_PREFIXES += cron-registration:* (P2, S, pm-decomposed, 2 of 3 — 601 parallel independent file, 603 gated on both) → REVIEW, next_agent=qa

**Session:** 085954f2-75fe-44ef-8fbe-c550db2da453. `isKnownLegitPattern`'s `KNOWN_LEGIT_PREFIXES` array (`tasksMdJanitorJob.ts`, D4 audit dimension) gains `"cron-registration:"` — verified by construction it lands in the prefix array, not the `-singleton` suffix branch (`cron-registration:*` has no such suffix). Without this, D4's daily 03:00Z held-lock-no-board-row scan would flag every `cron-registration:<family>` marker the sibling not-yet-shipped producer work mints as an orphan — a daily false-positive generator, same class already solved for `esc-datacov:`/`intent:`/etc. Extended `FIX-D4-HELD-LOCK-NO-BOARD-ROW-RECONCILE.test.ts` (already exercises `isKnownLegitPattern` directly) with a new `describe` block: 3 positive families (`cowork-team`/`detect-loop`/`standalone-team`) + 1 negative control (`some-random-id`).

**Evidence:** Non-vacuousness per AC-3b mandate — `git stash push --keep-index -- tasksMdJanitorJob.ts` (source-only, test kept) → scoped test run 27 pass/3 fail (RED) → `git stash pop` → re-run 30 pass/0 fail (GREEN). `bun tsc --noEmit` clean. Full suite intentionally not run — TASK_603 (depends_on both 601+602) owns AC-4 full-suite + AC-6 deploy per dispatch instructions. Commit `86b31eccd` (code+test) + `a5fa7bf7c` (orch-state REVIEW + handoff record). Handoff `docs/agents/dev-team/handoff/TASK_602.md` Implementation Record appended. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S74. Board `in_progress[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`. TASK_601 (`coordinationStore.ts`, sibling in this same file above) landed independently, no conflict — both now sit REVIEW, unblocking TASK_603.

Zone health: tsc clean, scoped test file 30/30 pass, 2 files touched (1 prod: 1-line array entry, 1 test: 18-line describe block) | HEALTHY.

## 2026-08-07 — TASK_603 full suite + gcExpiredLocks AC-3a negative control + deploy (P2, S, pm-decomposed, 3 of 3, depends_on TASK_601+TASK_602 both DONE_VERIFIED) → REVIEW, next_agent=qa

**Session:** 085954f2-75fe-44ef-8fbe-c550db2da453. TASK_601's `coordinationStore.ts` fix was already complete/correct — only the deferred AC-3a regression coverage was missing. Extended `task-lock-coordination-store.test.ts`'s existing AC-11 describe block with 2 tests matching the sibling `cron:*`/`dev-team-cron-singleton` guard-test pattern (real code convention, not the handoff's pseudo-ORM `orphan_signals` table — no such table exists; signals are `task_locks` rows with `task_id='orphan-signal:<orig>'`): negative control (expired `cron-registration:cowork-team` emits NO signal AND is still deleted) + positive control (ordinary expired sprint-task still emits). Non-vacuousness proven manually: reverted the WHERE-clause line → 45 pass/1 fail RED → restored (byte-identical to HEAD) → 46 pass/0 fail GREEN.

**Evidence:** `pnpm --filter vn-market check` (tsc, the pre-push gate) clean. AC-4 full suite run twice, not a subset: 15157 pass/44 fail (467.87s) and 15156 pass/45 fail (466.32s, full capture) — the 1-test delta is this repo's documented order-dependent flaky floor (`FIX-MCP-SUITE-HEALTH-BASELINE`); all 45 named failures inspected, zero overlap with coordinationStore/task_locks/cron-registration/D4. `task-lock-coordination-store.test.ts`'s full 46-test block (incl. both new tests) has zero `(fail)` lines inside the full-suite log. Tool=183/sched=88 unchanged. AC-6 deploy: `docker compose up -d --build mcp-server` (single-service only) — image ID RAW-verified changed `824eeacf77bb`→`115700a86e65` (not cache-reused), container healthy, `/health` fresh (uptime 19.7s, toolCount 183), `/api/bctc-inspect` + `/dashboards/news-fetch/` both 200, zero error-level logs. AC-5 confirmed: `git diff HEAD~1 HEAD -- docs/agents/system-auditor/{handlers,audit-dimensions}.md` empty both before and after commit — agent-father's zone untouched. Commits `814182608` (test) + `03af0f983` (handoff+board). Handoff `docs/agents/dev-team/handoff/TASK_603.md` Implementation Record appended. Board `ready[]`→`review[]`/`next_agent=qa` via `orch-apply.sh`.

**Sequencing gate:** MET at TASK_603 developer-level (both AC-4 net-new-zero-failures and AC-6 image-diff evidence attached). Parent row `FIX-CRON-REGISTRATION-PREFIX-NOT-EXCLUDED-ORPHANEMIT-AND-D4-R1B` still needs QA to verify + flip to `done_verified` before the router's held agent-father Lane-1 dispatch unblocks — NOT yet done_verified as of this write.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), full suite 44-45 fail (documented flaky floor, 0 net-new regressions), real deploy confirmed via image-ID diff, 3 files touched across this task (1 test file + handoff + orch-state) | HEALTHY.

## 2026-08-07 — FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE (P1, S, po-originated) → REVIEW, next_agent=qa

**Session:** f298ccf7-8cf4-452d-9a5a-57dcb47e65ac. AC1 investigation (root cause, not code guess): compared pdf-extractor's own container logs across both windows on report `76129128-947c-422b-a591-e1d2b95cbeb8` — the 16:37Z batch shows pdf-extractor healthy throughout (zero ERROR/WARN, `/health` 200 OK). mcp-server's `data/logs/tool-get_bctc_page_image.log` (a per-tool file log, separate from stdout) is the decisive evidence: unit-0014/page28's on-demand rasterize completed cleanly (PNG confirmed present on the shared `bctc-page-images` volume, no "page failed" WARN) yet the pushed unit STILL carries `image_unavailable` — and units 0015/0019 (pages 29/37) have ZERO log entry at all, meaning `get_bctc_page_image` was never invoked for them. Neither hypothesis in the task's FILES list (`getBctcPageImageTool.ts`/`pdfExtractorClient.ts`) reproduces this — every traced call that DID execute succeeded end-to-end. Concluded: root cause is the gap between a successful server response and the calling `refine_bctc_md` subagent under Option-C's shared-execution-context architecture (large base64 image payloads accumulating across up to 12 windows in one continuous turn) — agent-flow/architecture territory, not a TS bug in this zone. Left the verified-correct 0.55 confidence / <=0.6 cap (`bctcSanityValidator.ts`) untouched per the task's explicit instruction.

**AC2 shipped:** new `bctcImageFetchDegradedSignalWriter.ts` (same precedent as `improvementSignalWriter.ts`/`narrativeContradictionSignalWriter.ts` — thin wrapper over `appendSignalQueueRow`) + wiring in `pushBctcRefinedUnitTool.ts`: on any push whose flags carry `image_unavailable*`, counts occurrences for that `report_id` and fires ONE HIGH-severity `bctc_image_fetch_degraded` signal_queue row on the 2nd occurrence (rising-edge — never re-fires on the 3rd+, avoiding noise on a single flaky page while still catching genuine batch degradation).

**AC3/AC4:** AC3 explicitly NOT claimed — no fetch-plane code changed, so `next_refine_batch_has_zero_image_unavailable_flags` cannot be proven by this commit alone (reported honestly rather than forcing a false PASS). AC4 (contiguous page 30-41 `ocr_corruption` tail) intentionally left open, flagged for PO to split per the task's own suggestion.

**Evidence:** New `FIX-BCTC-REFINE-PAGE-IMAGE-UNAVAILABLE-CAPS-CONFIDENCE.test.ts` 18/18 pass (flag matcher incl. false-positive guard, rising-edge threshold incl. 3rd-occurrence non-refire, row shape vs `SignalRowSchema`, real-deps end-to-end wiring). Existing bctc push/finalize/idempotency + CCATO/improvement-signal-writer suites unmodified, all pass (44/44). `bun tsc --noEmit` clean. `gen-project-stats.ts --dry-run`: tools=183 unchanged (no new MCP tool registered). Full suite: 15174 pass/45 fail/1 error (467s) — within this notebook's own documented flaky floor (S71:53, S75:44-45), zero bctc-named failures (grep-confirmed). Commit `73e65eafc`. DJ: `sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server-4.md` S76. Board `backlog[]`→`review[]`/`next_agent=qa` via `orch-apply.sh` (bundled a 1-field repair: nulled a pre-existing dangling `signal_queue.rows[134].payload_ref` pointing at a since-deleted `docs/data/db-integrity-history.json` — that Stage-1c dangle was blocking the write path for ANY agent, not caused by this task, row already fully triaged/dispositioned by PO as duplicate so the ref was non-load-bearing).

**Environment note (not this task's defect, fixed in-session):** this worktree's `apps/mcp-server` had zero `node_modules` at session start; an initial `bun install` run from the repo ROOT (wrong cwd — this monorepo uses per-app `bun.lock` files, not a hoisted root install) clobbered the ROOT `bun.lock`/`node_modules` down to 4 packages. Reverted `bun.lock` via `git checkout --`, then ran `bun install --frozen-lockfile` from INSIDE `apps/mcp-server` (matching `test_command`) — restored all 424 packages, zero lockfile diff. Flagging so a future agent doesn't repeat the root-level `bun install` mistake in this repo shape.

Zone health: tsc clean, tool count unchanged (183), scoped new-test 18/18 pass + existing bctc suites 44/44 pass, full suite within documented flaky floor, 3 files touched (2 prod-ish: 1 new infra module + 1 tool wiring, 1 new test file) | HEALTHY.
