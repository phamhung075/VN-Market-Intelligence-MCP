# dev-mcp-server -- Notebook

## 2026-06-28 · TASK_1973 P1-MCP-1 — owner_client_session column migration (REVIEW)
Sprint CROSS-SESSION-MULTI-TEAM-ORCH. Added `owner_client_session TEXT` (nullable, NOT UNIQUE) to `task_locks` as Migration 2 in `migrateCoordinationTable`. Changed early-return pattern to conditional block so Migration 1 (commit-mutex enum widen) and Migration 2 (ADD COLUMN) both run in sequence on every startup call. PRAGMA table_info guard makes Migration 2 idempotent. Live verification: rebuilt mcp-server container, triggered migration via `listHeldTasks` call inside container — log line confirmed + PRAGMA table_info shows cid:9 `owner_client_session TEXT notnull:0`. All 5 pre-migration rows survived with NULL. NOT UNIQUE confirmed (no unique index, two rows with same session UUID both insert). Tests: 10 new (P1-MCP-1-owner-client-session-migration.test.ts, 6 AC groups) + 63 regression (5 coordination test files) all GREEN. tsc clean. Change is strictly additive — no tool schema changes, no matching-ladder changes (those are TASK_1974 + TASK_1975). Handoff [Developer] section written.
Zone health: 10 new GREEN + 63 regression GREEN, tsc 0 errors, migration LIVE in named-volume DB | HEALTHY

## 2026-06-27 · FIX-BCTC-Q1-2026-INGEST-DISCOVERY-GAP (IN_PROGRESS → REVIEW)
ROOT CAUSE: PO hypothesis (DISCOVER-layer defect) disproved by recon. HPG+ACV Q1-2026 filings were discovered and pulled; gap is at REFINE layer. Both tickers: bctc_vps_queue.status='done', pdf_extracted_text has 37 pages (HPG) / 50 pages (ACV), but financial_reports.refine_status='PENDING' (fleet-cron stalled post 2026-06-07). Scale: 47 reports total stuck at REFINE (full June 7+ batch — GVR, HPG, HVN, MBB, POW, DPM, VPB, VRE, KBC, HSG, VHM, and 36+ others). FALSE-GREEN source: queryBctcCounts() only read bctc_vps_queue (both HPG+ACV = 'done' → pending=0 appeared healthy); bctcOverdueCheckJob used published_at IS NOT NULL (set at PULL time, not REFINE time) → no overdue alerts. FIX: Added refine_pending field to BctcQueueCounts interface and queryBctcCounts() in fetchStatusHandler.ts — second SELECT COUNT(*) FROM financial_reports WHERE text_status='COMPLETE' AND refine_status='PENDING' exposes the true stall count at /api/fetch-status. Tests: F-1-fetch-ops-page-truth.test.ts extended (financial_reports DDL added to setupDb, 3 new tests: AC-3 key check, refine_pending counts, refine_pending=0 clean state), 23/23 GREEN. tsc clean. 166 tools / 3 cron.schedule unchanged.
Zone health: bun test 23/23 GREEN (F-1), tsc 0 errors, 166 tools intact, scheduler 3 unchanged | HEALTHY

## 2026-06-27 · TASK-FFT-L2 — L2 data_asof contract for 5 handlers (REVIEW)
Sprint FRONTEND-FRESHNESS-TRANSPARENCY. Added top-level `data_asof` (ISO 8601 UTC) to all 5 L2 handlers. Spec had 3 column errors (market_summaries.generated_at does not exist; vps_push_log.created_at does not exist; alerts.updated_at does not exist) — applied contract-from-live-payload rule, used real schema columns: market_messages.sent_at, alerts.triggered_at, daily_ohlcv.updated_at, vps_push_log.pushed_at. qualityChecklistHandler = computed-on-read, no store (JSDoc comment added per RISK-2). daily_ohlcv.updated_at has DEFAULT '' sentinel — queryPriceHistoryAsof() guards with `val && val.trim()` before returning. 20 new tests (freshness-dataasof-handlers.test.ts): 2 per handler (with-data + empty-table fallback) + helper unit tests. Coverage map: rows_no_asof 8→2, L2 4→0, LIVE 26→30. Commits: a384497a (handlers+tests), 56c0e4ac (docs).
Zone health: bun test 20/20 GREEN (freshness-dataasof-handlers), tsc 0 errors, 166 tools intact | HEALTHY

## 2026-06-27 · TASK-FFT-L4 — Coverage-map-aware SLA self-policing (REVIEW)
Sprint FRONTEND-FRESHNESS-TRANSPARENCY. New domain service coverageMapFreshnessChecker.ts: pure, zero I/O, ARCH-RATIFY-FFT-3 injectable (injectedRows?: CoverageMapRow[], NOT path). Maps 5 endpoints (market-digest/alerts/quality-checklist/price-history/vps-proxy-health) to DB MAX() queries. STALE_RISK off-hours suppression in domain via isVnMarketHours(). EC-7: null MAX = not-seeded sentinel, skip. Extended freshnessSlaMonitorJob.ts: injectedCoverageMapRows? param (backward-compat); additive L4 second pass after existing 12-signal path; lazy-import postSignal per breach. 25 tests: CM-1..CM-10 (pure domain, minimal in-memory DB) + SC-1..SC-4 (scheduler integration, initDatabase). tsc clean. 166 tools / 79 scheduleCron (no new cron added). Commits: 1dd3c6d1 (impl), 718aa01c (handoff), 1bdf0511 (orch).
Zone health: bun test 25/25 GREEN (new file), 76/76 GREEN (4 freshness files), full suite exit 0 (13678 tests), tsc clean, 166 tools, 79 scheduleCron | HEALTHY
