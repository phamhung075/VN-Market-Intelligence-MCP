# dev-mcp-server -- Notebook

## 2026-07-31 — FIX-CI-SIZELINT-MCPSERVER-SIX-UNCOVERED-OFFENDERS (BOUNDED-1 auto-pickup, P0) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. AC-1 file-level gate: `size-lint-justification.sh --check` no longer lists any of the 6 owned files (only the 2 sibling-owned offenders remain — job stays RED pending those, per design). AC-2 landmine avoided: zero `--update` runs, zero `docs/data/size-lint-baseline.json` edits.

**5 of 6 split into ≤120L sibling files** (barrel-reexported where in-zone consumers already imported by name, zero consumer-file churn): `claimCandidateScanner.ts` 190L(NEW)→116L + `claimToolMapTypes.ts`(51L) + `tickerExtraction.ts`(46L); `vpsPushLogStore.ts` 212→160L (demand-queue split to `vpsDemandQueue.ts`, 72L); `signalValidator.ts` 259→177L (audit helpers to `signalQualityAudit.ts`, 112L); `vpsProxyTools.ts` 269→87L (staleness+formatting split to `vpsProxyStaleness.ts`(111L)+`vpsProxyHealthFormat.ts`(115L)); `polymarket.ts` 500→462L (SQLite persistence to `polymarketStore.ts`, 56L, plus removed a now-unused `getDb` import).

**orchStateSchema.ts (870→839L) got trim+justification, NOT a split** (AC-3's own explicit alternative): its §9-12 guard functions are imported BY NAME from this exact path by `scripts/orch-validate.mjs`, AND raw-copied whole (not dependency-aware) by `scripts/agents-flow/drain-signals.test.js`'s `makeOrchRefHarness()` to exercise the real validator end-to-end — any physical split leaves that copied file's own import dangling in the isolated sandbox. Fixing the harness's copy-list is a 1-line change but lives in `scripts/`, outside this agent's zone. Did a genuine ~46L comment-debt trim (duplicated `.passthrough()→.strict()` promotion-trigger blocks condensed, §9/§11/§12 verbose narrative headers tightened — zero logic/type/export changes) then added a substantive `size-justification: 839L` header (first 10 lines) explaining the cohesion + the harness constraint + a pointer for a future scripts/-owning agent to land a coordinated split.

**Evidence:** `bun tsc --noEmit` clean. Targeted suites for every touched file + every consumer (CCATO/orchStateSchema/sprint-goal/blocks-decorative/polymarket/prediction-job/vps-health×4/signal-quality-audit/push-log fixes, ~24 files): 650+ pass/0 fail. Fresh server boot (`DB_PATH=:memory:`, free port): `/health` toolCount=183, zero import errors. `gen-project-stats.ts --dry-run`: toolCount=183 cronJobCount=88 (baseline match, no tool/scheduler surface touched). Dashboard circular-dep probes (`/api/bctc-inspect`, `/dashboards/news-fetch/`) both clean. Full `bun test`: 14945 pass/40 skip/55 fail (556.87s) — inside this sprint's documented pre-existing flake band (52-59, see prior 2026-07-30/31 entries); fail-list not individually re-grepped (captured via `tail -80`), but the count sitting in-band plus the 650+/0 targeted result is decisive for this diff. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md S35.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 6/6 files cleared from size-lint (job stays RED pending 2 sibling rows, by design — AC-1 is file-level) | HEALTHY.

## 2026-07-31 — FDA-5 (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. `get_energy_grid_signals` hardcodes 4 grid figures (thermalDispatchPct=40/renewableDispatchPct=22/peakDemandGW=45/installedCapacityGW=85 — no live EVN API). Provenance was already disclosed (is_estimate:true/source_tier:4 on internal signal objects, VN "[ƯỚC TÍNH]"/EVN-API banner text) but ONLY inside the unstructured `content[0].text` string — a downstream consumer parsing the payload structurally had no reliable way to see the estimate flag.

**Fix:** added `structuredContent` (real MCP CallToolResult field, `z.record(z.string(), z.unknown()).optional()`, present server-side regardless of whether an `outputSchema` is registered) carrying `is_estimate: true`, `source_tier: 4`, `estimated_fields` (the 4 field names), `grid_figures` (the actual numbers, plus live/fallback `hydroCapacityPct`), and `hydro_data_source`. Chose `structuredContent` over creditFlowTools.ts's precedent (bespoke top-level fields like `is_estimate`/`direction`) because that file's own comment + CallToolResultSchema (zod, non-passthrough) confirm bespoke top-level keys get silently stripped client-side — only in-process callers ever see them, not real wire consumers, which is exactly the risk FDA-5 flagged. No live EVN API integration built (explicitly out of scope) — VN prose banner/text output byte-identical to before.

**Tests (`DSI-S3-sector-fin.test.ts`, new "FDA-5" describe block):** 5 new cases — structuredContent present/object, is_estimate=true + source_tier=4 read structurally (no text parsing), grid_figures carries the 4 literal hardcoded values, estimated_fields lists exactly the 4 field names, and `getEnergyGridSignals` (the fn the registered tool actually calls) also carries structuredContent. File: 28/28 pass (was 23).

**Evidence:** `bun tsc --noEmit` clean. Targeted suites (DSI-S3-sector-fin + 262-mcp-tools-042 + 1410-diacritics-sweep + 1472-diacritics-batch2): 75/75 pass. Fresh server boot (`DB_PATH=:memory:`, free port): `/health` toolCount=183, zero import errors; dashboard probes (`/api/bctc-inspect`, `/dashboards/news-fetch/`) both 200. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — both unchanged. DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md S36.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 2 files touched (energyTools.ts + its own test) | HEALTHY.

## 2026-07-31 — FDA-6 (BOUNDED-1 auto-pickup, P3/S) → REVIEW, next_agent=qa

**Session:** 64c7c677-0f0f-4cee-a3ce-dba79d70b7ae. `get_credit_flow_signal` fabricates mortgage rate (10.5/11.0%), YoY credit growth (±15%), RE-credit ratio (20/19%, always-static), and total credit (reCredit×5, always-derived) when live SBV/NHNN data is absent — already DSI-hardened (mortgageIsEstimate/yoyIsEstimate flags + VN "[ƯỚC TÍNH] is_estimate=true source_tier=4"/"[static_seed]" prose). Two residuals: `date: new Date().toISOString()` stamped the current/previous CreditData snapshot as "today" even when its values were hardcoded fallbacks (same class as the frozen-usdVnd-labelled-tier1 lesson); the existing is_estimate/direction fields were bespoke top-level keys, not real MCP wire fields.

**Fix:** `CreditData.date` widened `string → string | null` (`domain/services/creditFlowAnalyzer.ts`, unused inside the chain-rule computation, metadata only); `computeCreditFlowSignal.ts` now stamps `null` whenever `mortgageIsEstimate || yoyIsEstimate`, a real ISO date only when both legs are live. Added `structuredContent` (same FDA-5 pattern) to `getCreditFlowSignalHandler` carrying is_estimate/source_tier/estimated_fields/mortgage_is_estimate/yoy_is_estimate/fully_estimated/current_date/previous_date. Did NOT implement the dispatch note's "omit dish when BOTH estimated" suggestion — that's the tool's DEFAULT no-params call shape, so suppressing output there is an unreviewed breaking change for a P3/S ticket; added `fully_estimated` as a machine-readable hook instead so a future consumer can choose to fail-loud on its own. VN `content` text byte-identical to before.

**Tests (`DSI-S3-sector-fin.test.ts`, new "FDA-6" describe block):** 5 new cases — structuredContent present, default-path is_estimate=true/source_tier=4/fully_estimated=true, estimated_fields lists all 4 field names (2 always-static + 2 conditional), current_date/previous_date null on the estimate path, and real ISO dates + source_tier=2 + fully_estimated=false + estimated_fields=[2 static only] on the all-params-live path. File: 27/27 pass (was 22).

**Evidence:** `bun tsc --noEmit` clean. Targeted cluster (DSI-S3-sector-fin + 246-credit-flow + 1254-credit-flow-db-fallback + VMT-6-credit-flow-survey-distribution + MONEY-RADAR-P0-T2-COMPOSITE + 251-mcp-tools + 1410-diacritics-sweep + 1472-diacritics-batch2): 136/136 pass. Fresh server boot (`DB_PATH=:memory:`, free port): `/health` toolCount=183, zero import errors; dashboard probes (`/api/bctc-inspect`, `/dashboards/news-fetch/`) both 200. `gen-project-stats.ts --dry-run`: toolCount=183, cronJobCount=88 — both unchanged. Full `bun test`: 14954 pass/40 skip/56 fail/2 errors (489.64s) — inside the documented pre-existing flake band (52-59); zero credit/energy-related failures in the fail-list grep. Doc updated: `docs/agents/tools/list/get_credit_flow_signal.md` (structuredContent example, matches FDA-5's per-tool-doc precedent). DJ: sprint-COWORK-GUARANTEED-SLOT-CATCHUP-dev-mcp-server.md S37.

Zone health: tsc clean, tool/scheduler counts unchanged (183/88), 4 source files touched (domain+application+interface+test) + 1 tool doc | HEALTHY.
