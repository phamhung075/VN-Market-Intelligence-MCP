# dev-mcp-server -- Notebook

## 2026-07-21 — FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP part 2 (router dispatch, orch-board FIX) → REVIEW, returned to router

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. PO's `bctcReparseJob.ts` attribution was explicitly UNVERIFIED — traced instead. Found TWO independent writers to `financial_reports` (`parseBctcReport.ts::storeReport()` + `bctc/newsChainFallback.ts::tryNewsChainFallback()`), both with the identical two defects: `published_at = excluded.published_at` unconditional on every `ON CONFLICT` re-parse (the NGAY NOP flip), and no guard against a worse re-extraction (`total_assets<=0`) overwriting a previously-good row (`total_assets>0`) — `tryNewsChainFallback` hardcodes `total_assets=0` on EVERY call by design, so every invocation was, structurally, the exact corruption fingerprint. Every known caller (`bctcReparseJob.ts` recovery, VPS push handler chain, `composition-root.ts` bootstrap hook) funnels through these same two SQLite UPSERTs — fixing there is upstream-agnostic.

Fix: `published_at` removed from both `ON CONFLICT DO UPDATE SET` clauses (immutable post-insert, same treatment as `id`); `parseBctcReport()` gained a `publishedAt` param threaded from `fetchParseAndStoreBctc.ts`'s `doc.publishedAt` BEFORE the write (the old in-memory post-hoc patch never reached SQLite); new pre-write guard in both functions skips the write entirely (+ `sendTelegramBug`) when an existing row is servable (`total_assets>0`) and the new write would corrupt it (`total_assets<=0`). `composition-root.ts` also gained `DISABLE_BOOTSTRAP_OCREPARSE=1` kill switch (operational lever, not the fix).

New test `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP.test.ts` (6 cases) — RED confirmed via `git stash` A/B before the fix existed, GREEN after. Live runtime DB probe (docker exec, not host file) classified all 58 watchlist × 2026-Q1 into 4 modes: 9 servable, 16 `total_assets<=0` (this row's exact incident signature — 8 from the 07-19/20 window, 8 pre-existing June), 21 refine_status-PENDING-gated (separate pre-existing PUB-1 backlog, 205/226 of ALL rows — NOT this fix's scope), 12 row-absent. Confirmed via `cron_job_runs` sentinels + direct query that the 2026-07-21T01:34Z container restart touched zero rows (bleed was already stopped before this session, independent of the fix). Full suite: targeted BCTC files 78/78 pass; 13 pre-existing timeout failures in `1294b`/`293`/`FIX-1267` fallback-network tests reproduced byte-identical with/without the fix (git-stash A/B) — confirmed unrelated, real-network/circuit-breaker test-infra flake, not a regression. `bun tsc --noEmit` exit 0. Tool count 183 unaffected.

**Not fixed here (explicitly out of scope, PO's call):** the refine_status-PENDING servability gate (21/58, pre-existing, unrelated mechanism) and row-absent tickers (12/58, no PDF ever ingested) are separate bugs this write-back fix cannot and does not touch — flagged, not folded in.

Committed `25ae59a36` (code: `parseBctcReport.ts` + `fetchParseAndStoreBctc.ts` + `bctc/newsChainFallback.ts` + `composition-root.ts` + new test + `usecases.md`). **Deploy note: requires an mcp-server image rebuild — not live yet.** Flipped orch-state row IN_PROGRESS→REVIEW, `.head`→idle/next_agent=router.

Zone health: tsc clean, 78/78 targeted BCTC tests pass (incl. 6 new RED→GREEN), full-suite pre-existing-flake class only (confirmed via stash A/B), tool count 183 unaffected | HEALTHY.

## 2026-07-21 — FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP arm (b2) (router dispatch, revised gate) → REVIEW, returned to router

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Gate revised 16:49Z (po_acceptance_reconciliation_20260721T1649): PO audited all 16 tickers and found ZERO confirmed good→corrupt transitions — the real transition is ABSENT→manufactured zero-row, which the prior round's guard misses because it only fires when `existingGoodRow.total_assets>0`. Dispatched narrowly on this arm (b2) only; explicitly forbidden from touching the ingest-stall remediation (13/16 tickers owned by FIX-BCTC-Q1-2026-STORED-PDF-INGEST-STALL-15T, CTG by W5-FU-CTG-REFINE-96e36139, D2D by OPS-BCTC-REFINE-REPASS-NONBANK-5T).

Fix: `storeReport()` (`parseBctcReport.ts`) guard now evaluates `report.balanceSheet.totalAssets<=0` UNCONDITIONALLY (was `existingGoodRow && ... && <=0`) — refuses the write whether it would clobber a good row (b1, unchanged) or CREATE a fresh corrupt row where none existed (b2, the new fix). RED-before proven via `git stash push` on just the 2 source files (test changes kept) — both new (b2) assertions failed against pre-fix code exactly as predicted (manufactured `total_assets=0` row for a no-prior-row ticker); `git stash pop` restored the fix, GREEN after (7/7 pass).

**Scope-drift caught and reverted:** first attempt also extended the same unconditional guard to `tryNewsChainFallback()` (bctc/newsChainFallback.ts) since it shares the identical `existingGoodRow`-gated pattern and always hardcodes `total_assets=0`. That broke 6 pre-existing tests (`1294b-bctc-fallback.test.ts` ×4, `FIX-BCTC-NEWS-CHAIN-FALLBACK-ID-ORPHAN.test.ts` ×2) that legitimately exercise a first-ever fallback write succeeding. Reverted that half cleanly (`git checkout --`) — confirmed that writer is currently unreachable from any live caller (`enableBctcFallback` defaults `false`, never set `true` outside tests, grep-verified) and is not part of the 16-ticker incident's actual write-back path. Left as a documented, reported gap (`usecases.md`, both call sites) rather than folded in — this is exactly the scope-drift pattern the dispatch warned against.

Not scored on `get_earnings_calendar` QUA HAN per explicit router/PO instruction (corrupted discriminator, 13/15 already green from the ingest-stall cohort regardless of this fix).

Verified: targeted BCTC suite (`FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP`, `FIX-BCTC-NEWS-CHAIN-FALLBACK-ID-ORPHAN`, `1294b-bctc-fallback`, `FIX-BCTC-D1-STABILIZE-REPORT-ID`) 20/20 pass. `bun tsc --noEmit` exit 0. Full suite: 14601 pass / 41 fail / 46300 expect — all 41 pre-existing, unrelated (telegram/vps-push/insider-tx/foreign-flow/technical-indicators, mostly 5000ms-timeout network flake), zero BCTC/financial_reports hits (name-checked against the fail list). Tool count 183 / cronJobCount 2 unaffected (zero scheduler/tool files touched).

Committed (explicit pathspecs, verified via `git show --name-status`): code `parseBctcReport.ts` + test `FIX-BCTC-REPARSE-BATCH-CORRUPTION-NGAYNOP-FLIP.test.ts` + doc `usecases.md`. Flipped orch-state row IN_PROGRESS→REVIEW, `.head`→idle/next_agent=router.

Zone health: tsc clean, 20/20 targeted BCTC tests pass (incl. 2 new RED→GREEN for arm b2), full-suite pre-existing-flake class only (zero BCTC overlap), tool count 183 unaffected | HEALTHY.

## 2026-07-22 — cron-audit-server-fixes (router direct dispatch, 3 items) → all 3 fixed, no orch-board row (ad-hoc audit task)

**Session:** 88e6b035-9e4e-40df-a659-0fb4c39eca39. Item 1 (4 dead Sunday jobs): RAW-verified live `cron_job_runs`+`mcpServerStartup` — pinned root mechanism as `recoverMissedExecutions` (scheduleCron default) never bridging a process RESTART (only in-process event-loop lag; `Scheduler.start()` re-seeds `lastCheck`/`lastExecution` from `new Date()` every boot). A weekly job gets ONE window/week; if the container is down at that exact moment the whole week silently drops. Sibling Sunday jobs (patternWatchJob 15:30 UTC, weeklyPortfolioReportJob 16:00 UTC) kept firing purely because the 07-19 restart (12:31Z) landed after the 4 dead jobs' early-morning windows but before theirs — not a timezone/DOW bug. Fix: `shouldRunCatchup()` gained `requiredUtcDay` param; `startScheduler.ts` runs a 30s-post-boot Sunday probe for all 4, looking up each runner by name from `jobTable` (schedulerJobTable.ts, single source of truth) and firing via `jobRunRepo.wrapRun`.

Item 2 (VPS SSH tools fake success): found the 5 `trigger_*_vps_fetch` tools never called `sshExec()`/`Bun.spawn` AT ALL — only string-built a command + false "fire-and-forget" prose, `attempted/success/failed` hardcoded `[]` always. New shared `vpsDebugSshTrigger.ts` (ticker charset sanitization + real `sshExec()` call, injectable for tests) wired into all 5 handlers; removed the now-redundant duplicate log-building from the 5 tool files + `debugTriggerRoutes.ts`. Second layer found verifying live: `/root/.ssh/known_hosts` absent in the container + `StrictHostKeyChecking=yes` + `BatchMode=yes` = every connection refused regardless of client — `restart_vps_service` had the same latent defect. Fixed to `accept-new` (TOFU) in `sshExec.ts` rather than a build-time `ssh-keyscan` (rejected: hardcodes VPS IP + makes image build depend on VPS reachability). Dockerfile: added `openssh-client`. **Requires mcp-server image rebuild (ops) — not live yet; mandatory post-deploy REAL-DATA verification per PUSH-AUTONOMY-1 (live probe of one trigger tool against the real VPS).**

Item 3 (watchdog 81% blind): RAW-verified the audit's "5 confirmed bespoke sites" — 2 (sscCheckerJob, dataAuditJob:weekly) already had telemetry under a DIFFERENT job_name than the CRONS key the audit used; only 3 (signalOutcomeJob, alertOutcomeJob, signalOutcomeResolution) were genuinely blind. Wired those 3 through `jobRunRepo.wrapRun`; added all 9 (4 Sunday + 2 misnamed-but-real + 3 newly-wired) to `WATCHDOG_MANIFEST`/`CANONICAL_WATCHDOG_JOB_NAMES` (WD-10/WD-11 still green — zero guessed names). Discovered `sscCheckerJob` live-stale ~87 days (pre-existing, unrelated incident) — added to manifest (closes the exact detection gap) but did NOT chase root cause (out of scope, flagged to PO). Full fleet expansion (~65 more uncovered jobs) explicitly NOT attempted — proportionate scope only, flagged as follow-up.

**Ripple found via full-suite run (not targeted files alone):** the 16→25 manifest widening broke 5 pre-existing tests coupled to a SEPARATE dashboard SSOT — `cronStatusCompute.ts`'s `STATIC_JOB_NAME_MAP` (CN-1 "hybrid 3-tier" CRONS-key→job_name resolver) had its own hardcoded 16-pair map + a "1:1 exactly CANONICAL_WATCHDOG_JOB_NAMES" test. First attempt (tier-1 entries for only the 2 jobs tier-2's normalizer can't match) still failed 3 tests — root cause: `job_name_db` is memoized "per CRONS key for the lifetime of the process"; tier-2 resolution depends on a live DB snapshot at first-computation time, and if that happens before the job has ever recorded a row (fresh boot, or an earlier empty-DB test sharing the process), the WRONG tier-3 fallback gets cached FOREVER — a real production risk, not just a test artifact. Added tier-1 entries for all 9 new jobs (cache-safe, unconditional); the 1:1 invariant is naturally restored (25=25).

LOW (project-stats SSOT): `gen-project-stats.ts`'s cron counter grepped literal `cron.schedule(` — correct pre-refactor, silently collapsed to ~2 after FACTORY-SCHEDULER-job-table-registry moved 61 table-driven registrations behind one generic loop call site. Rewrote to sum buildJobTable `name:` entries (61) + registerBespokeJobs call sites (22) + summaryJobs.ts (5) = 88, with a `MIN_PLAUSIBLE_CRON_COUNT=50` floor guard against silent re-drift. Regenerated `project-stats.json` (cronJobCount 2→88).

New tests: `FIX-CRON-SUNDAY-STARTUP-CATCHUP` (8), `FIX-CRON-WATCHDOG-COVERAGE-2026-07-22` (18), `FIX-VPS-SSH-TRIGGER-FAIL-LOUD` (20) + `-known-hosts` (5) — all new GREEN; updated pre-existing artifacts encoding old (buggy) assumptions: `1779a-ssh-exec.test.ts` TC-5 (StrictHostKeyChecking yes→accept-new), `ARCH-CRON-watchdog.test.ts` (sscCheckerJob.ts added to WD-11 scan list), `cronStatusHandler.test.ts` + `cronStatusCompute.test.ts` (16→25 ripple above, 7 tests), `TSU-DEV-U6-tsh-leftover-descriptions.test.ts` (sbv/foreign-flow tool descriptions now name their VPS script — the T-U6 test relied on the removed fake-SSH-log code as its only match, so the fix adds a genuinely more complete description instead of just appeasing the test, 2 tests). `bun tsc --noEmit` exit 0 throughout. Server startup probed PORT=3999 (3000 = live container, left untouched) — clean boot, toolCount=183, "90 cron keys" self-reported, clean SIGTERM. **Full suite, final authoritative run: 14652 pass / 40 skip / 41 fail / 46538 expect(), 1209 files [474s].** First run was 48 fail (7 of those = my own ripple regression from the manifest widening, all now fixed and verified GREEN in a clean re-run without git-stash contamination); remaining 41 name-checked against every touched file — zero overlap, all pre-existing (`_deprecated`, `logVpsPush`/`vps_push_log` schema drift, 5000ms network-timeout class).

No orch-board row exists for this task (ad-hoc audit dispatch, not a TASK_NNN) — nothing to flip in orch-state. Decision journal: `docs/agent-memory/decisions/sprint-cron-audit-server-fixes-dev-mcp-server.md`.

Zone health: tsc clean, all new+updated tests green, tool count 183 unaffected, cronJobCount SSOT corrected 2→88, server boots clean | HEALTHY.

## 2026-07-23 — BCTC-REPORT-ID-LOOKUP-TOOL (BOUNDED-1 direct-execute FIX, router dispatch) → REVIEW, next_agent=qa

**Session:** 4ae45b71-6dbf-4623-ab62-f388d14d2c85. Root cause: RECURRING dark-escalation (30 cycles) — bctc-analyst ESC-5 (`docs/agents/bctc-analyst/flow/main.md`) calls `get_bctc_refined(report_id)` but no tool ever surfaced `report_id` for a ticker+period; confirmed by reading `bctcFullTools.ts` end-to-end — `get_bctc_full`'s own JSON `structured_data` block omits `report_id` entirely (only appears in internal `logger.warn` calls, never in the returned payload). Every ESC-5 cycle therefore silently degraded to the graceful "no rows returned → FALSE" branch regardless of the report's real refine state.

Fix: new tool `get_bctc_report_id` (`getBctcReportIdTool.ts`) — `code[+year][+quarter]` → `financial_reports.id`, `WHERE refine_status='DONE'` only (the status the 4 downstream report_id-consuming tools — `get_bctc_page_text`/`get_bctc_page_image`/`get_bctc_refined`/`list_flagged_bctc_cells` — need). Zero matches → typed-absent (`report_id:null`, NOT an `{error}` shape), with a diagnostic `existing_refine_status` field (most recent report's actual status, or null if none filed) so callers can tell "not yet refined" from "no report at all" — mirrors ESC-5's own graceful-degrade precedent. Wired into `registry.ts` (import + array entry, #186); regenerated `docs/data/tool-registry.json` + `docs/data/project-stats.json#toolCount` (183→184) via the existing `bun scripts/gen-tool-registry.ts` + `gen-project-stats.ts` generators (never hand-edited — both are `_maintained_by: generator`).

Closed ESC-5's dark-escalation at the flow-doc level too (task AC-3, gating-logic-only, flow structure untouched): `main.md` ESC-5 gained a "Step 5d — resolve report_id first" block that calls `get_bctc_report_id(code=ticker, quarter=quarter)` before the existing `get_bctc_refined(report_id)` call, with the null/non-null branches spelled out. Also added `docs/agents/tools/list/get_bctc_report_id.md` + one-line `INDEX.md` Financial-category entry (19→20; left the pre-existing top-level "157 tools" header drift alone — unrelated, much larger doc-debt not caused by this task).

New test `FIX-BCTC-REPORT-ID-LOOKUP-TOOL.test.ts` (5 cases, in-memory SQLite, minimal `financial_reports`-only schema): exact-match DONE lookup, typed-null for PENDING-same-period (with `existing_refine_status` diagnostic), typed-null for no-report-at-all, DONE/non-DONE exclusion across two different periods for one ticker, no-filter multi-match ordered by `sort_key DESC`. `bun tsc --noEmit` exit 0 (one `matches[0]` possibly-undefined narrowing fixed with `!`). Targeted run green: `FIX-BCTC-REPORT-ID-LOOKUP-TOOL` 5/5, `308-tool-registry` + `251-mcp-tools` 14/14 (live boot self-reports `toolCount:184`), `BEQ-4b`/`240-bctc-full` 28/28 unaffected.

Rebuild note: tool is code-committed; a running mcp-server container needs a rebuild to actually load it live (single-service rebuild path, ops-executed per OVERRIDE 2026-07-03 — noted in REVIEW status_note for qa/ops, not run here).

Zone health: tsc clean, 47/47 across all touched+adjacent test files green, tool count 184 (was 183, +1 by design) | HEALTHY.
