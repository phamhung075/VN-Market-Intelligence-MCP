# TASKS — VN Market Intelligence MCP

> **Active:** Current sprint only. Historical: `docs/TASKS_ARCHIVE.md` | WIP limit: max 2 In Progress | Workflow: Backlog → Todo → In Progress → Review → Done | Branch: `task/NNN-kebab-name` | Report: `reports/TASK_REPORT_NNN.md` | **Archived Done tasks:** See `docs/TASKS_ARCHIVE.md` for complete history (1777–1896+)

---

## Sprint MACRO-LIVE-PRICES — Live oil/gold/usdvnd wiring for get_macro_snapshot

**Status:** OPEN — OPENED 2026-05-28 by BA (PO-approved batch, dev-team tick). **Priority: HIGH — macro regime accuracy, 4-cycle escalation confirmed.** Zone: `apps/macro-indicators/` ONLY. Spec: `docs/REQ_MACRO-LIVE-PRICES.md`. Sprint size: SPRINT-S (conditioned on architect choosing Option A — DB read; see §7 of spec for re-size flag). NOT a scale pilot — does NOT consume WIP fleet cap; does NOT touch `pilot-status-*.json`.

**Goal:** `get_macro_snapshot` must serve LIVE oil/gold/usdvnd, not the fixture seed (82.5/2350/24500). Root cause confirmed: `HTTPCommodityFetcher.FetchPrices()` returns hardcoded fixture map permanently. Existing live data is already written to `commodity_prices` table by mcp-server's `commodityTrackerRefreshJob` (Yahoo Finance, daily 06:00 UTC) — macro-indicators must read it.

**Day-0 constraints:** explicit-file staging only; no `--force`/`--no-verify`; no `git push`; all on `main`; do NOT touch `pilot-status-*.json`; zone `apps/macro-indicators/` + `docker-compose.yml` only.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| MLP-BA | **[ba] Requirement spec for live oil/gold/usdvnd wiring.** Spec written: `docs/REQ_MACRO-LIVE-PRICES.md`. Root cause confirmed against live code. Data-source options A/B/C laid out for architect. Env gate contract, false-green QA gate, and full test matrix (T-MLP-1..T-MLP-12) specified. Sprint sizing SPRINT-S with conditional re-size flag for Option B. | HIGH | TASK | ba | — | **DONE/REVIEW** — spec at PO spec-review gate | — |
| MLP-ARCH | **[architect] Technical design.** Read `docs/REQ_MACRO-LIVE-PRICES.md`. Select Option A/B/C for data source. If Option B: assess geo-block risk from within Docker container, flag SPRINT-M re-size if VPS routing needed. Design `SQLiteCommodityRepository` (or equivalent) adapter, env gate wiring in `cmd/server/main.go`, recency-bound enforcement. Confirm usdVnd port (CommodityFetcherPort vs SBVRatePort). Produce handoff for dev. | HIGH | TASK | architect | docs/handoffs/TASK_MLP-ARCH.md | BLOCKED | MLP-BA |
| MLP-DEV | **[dev-macro-indicators] Implement live commodity wiring.** Per architect design. Zone `apps/macro-indicators/` + `docker-compose.yml` env only. All T-MLP-1..T-MLP-10 tests green. Existing `go test ./...` passes unchanged (fixture-mode green). Commit explicit-file staged. | HIGH | TASK | dev | docs/handoffs/TASK_MLP-DEV.md | BLOCKED | MLP-ARCH |
| MLP-OPS | **[ops] Rebuild macro-indicators container.** After dev commit: force-recreate macro-indicators container (not restart — stale image). Verify `COMMODITY_LIVE_MODE=true` is in env. | HIGH | TASK | ops | — | BLOCKED | MLP-DEV |
| MLP-QA | **[qa] QA gate.** Invoke `get_macro_snapshot` via `call_tool(server="vn-market", tool="get_macro_snapshot")`. Verify `oilUsd > 90`, `goldUsd > 3000`, `usdVnd > 25000` (T-MLP-11). Confirm `go test ./...` still passes in fixture mode (T-MLP-6..T-MLP-8). No direct curl to :5004 — MCP tool path only. | HIGH | GATE | qa | — | BLOCKED | MLP-OPS |
| MLP-EXIT | **[po] Sprint sign-off.** After QA gate: confirm live values in `get_macro_snapshot`. Mark DONE. | HIGH | GATE | po | — | BLOCKED | MLP-QA |

---

## Sprint NEWS-FULLDAY — Refine `/news` to a COMPLETE deduped importance-ranked day digest (+ strip HTML)

**Status:** OPEN — OPENED 2026-05-27T21:41:17Z by PO (self-initiated from an EXPLICIT user feature request; full autonomy to scope). **Priority: HIGH — UX/coverage tier, user-facing.** User (non-technical, French-based, verbatim intent): `/news` *"only shows the newest items, just new info"* → wants `/news` to return **ALL important news of the DAY** (full-day coverage, deduped, ranked by importance — not only the latest delta). Goal SSOT: `docs/SPRINT_GOAL_NEWS-FULLDAY.md`. **Single zone: `apps/mcp-server`** — owner `dev-mcp-server`. Refines the just-shipped `/news` (`handleNews`) IN PLACE; reuses `chunkStories` + the `texts[]` webhook send-loop. NOT a scale pilot — does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`.

**PO autonomous product decisions at kickoff (settled scope — BA/architect must NOT re-litigate):**
- **REVISING the RECAP-CMD "`/news` stays as-is" ruling (recorded, not silent).** At RECAP-CMD kickoff I ruled the day-SYNTHESIS gap is closed by the NEW `/recap`, not by changing `/news` — that still holds (`/recap` owns index move + movers + positions). But the user has now sent a SEPARATE explicit signal about `/news`'s OWN job: it shows "just new info," not the complete day. So `/news` stays a news LIST but becomes a COMPLETE deduped importance-ranked LIST of the day. `/news` and `/recap` remain single-purpose and distinct. **No overlap with RECAP-CMD** — that sprint owns the 3 recap commands; this sprint touches ONLY `handleNews`.
- **The gap is NOT the date window** (PO traced the live code): `handleNews` already queries `created_at >= midnight-VN ORDER BY impact_score DESC, created_at DESC LIMIT 20`. The "incomplete" perception comes from THREE concrete defects, each now in scope: (1) **silent cap at 20** (no signal more exist) → full-day coverage; (2) **no dedup** (same story from multiple feeds repeats, padding the list + pushing distinct stories off the cap) → dedup on title/url, keep highest-impact copy; (3) **raw HTML in `summary`** (`<a href=…>`, `<img>`) → strip/escape at render (this FOLDS IN the backlogged `NEWS-CMD-HTML-STRIP`; same display path).
- **Single-zone render refinement.** Render-time HTML strip inside `handleNews` (NOT upstream `apps/news-fetch/` ingestion — that would split the zone). Dedup is a pure in-handler transform (no schema/query change beyond selecting needed columns). No new DB table / MCP tool / cron / microservice / compose change. No LLM. Synchronous read-only pull, ~1s reply, same contract as `/news` today.

**DoD (do NOT close early):** (1) `handleNews` refined: full-day coverage (no silent 20-slice; `/news N` still an explicit override), dedup (each distinct story once, highest-impact copy kept), importance order preserved, HTML stripped/escaped. (2) ALL user-facing text plain comprehensible Vietnamese — no jargon, no `impact_score` number, no raw HTML tags (`feedback_market_report_plain_vietnamese`). (3) `chunkStories` reused — no story split mid-item across 4096 chars; no silent truncation. (4) `HELP_TEXT` `/news` line updated only if its wording changes. (5) Empty-state fallbacks ("Chưa có tin hôm nay." / "Tin tức gần đây") intact. (6) T-NEWS test suite extended (injected in-mem DB / fakes, zero creds/network): dedup proof + HTML-strip proof + full-day-coverage proof; no test-baseline regression (floor 9408 PASS / ceiling 348 FAIL). (7) ops REBUILD + FORCE-RECREATE mcp-server (not restart — stale image; `feedback_rebuild_after_dev_change`). (8) QA verifies LIVE on `zenmidi.com/vn-market/webhook` (NOT bare `/webhook` — fixed today, commit 3ddeb820): full deduped day, no raw HTML. (9) Commit safety: serialized, no `-A`, main terminal commits (`feedback_concurrent_commit_race`).

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| NEWS-FD-BA | **[ba] Requirement spec for the `/news` refinement.** Read `docs/SPRINT_GOAL_NEWS-FULLDAY.md` (the three defects + single-zone render-time fix + no-overlap-with-RECAP-CMD are LOCKED — do not re-open). Write `docs/REQ_NEWS-FULLDAY.md` decomposing into atomic dev + QA tasks with testable ACs: (a) the **full-day-coverage** mechanism — exact default behavior so a normal trading day's distinct important stories ALL appear (raise default + chunk as the length safety-valve, and/or an honest "hiển thị N tin quan trọng nhất hôm nay" line when a sane upper bound is hit); `/news N` stays an explicit override. (b) the **dedup** key + tie-break — normalize on `source_title` (and/or `source_url`), keep the highest-impact / most-complete copy; specify the exact comparison so duplicates from multiple feeds collapse to one. (c) the **HTML-strip** rule — strip/escape tags in `source_title` + `summary`, preserve human-readable link text, drop markup; dependency-free local helper (flag for architect if a dep is wanted). (d) importance order preserved (no `impact_score` number shown). (e) the unit-test matrix extending the existing T-NEWS suite (dedup proof, HTML-strip proof, full-day-coverage proof — injected fakes, mirror `handleNews` tests). (f) `HELP_TEXT` line wording. Hand to architect to confirm the coverage mechanism + that dedup/strip are pure in-handler transforms, then to pm/dev-mcp-server. Spec → `docs/REQ_NEWS-FULLDAY.md`. Return to PO spec-review gate. | HIGH | TASK | ba | docs/handoffs/TASK_NEWS-FULLDAY.md | **APPROVED** — PO spec-review gate PASSED 2026-05-28 (critique-before-approve, 5/5 axes; code-claims verified live: `stripHtml` greenfield/absent, `handleNews` L510 + `chunkStories` L480 + cap L511-513 + HELP L77 confirmed). B1 = architect engineering call; B2 product half ANSWERED by PO (fallback IS capped, number→architect; REQ §5). No conflict w/ RECAP-CMD (shared `stripHtml` defined here). NEXT: architect | — |
| NEWS-FD-ARCH | **[architect] Technical design — confirm B1 (no-arg cap: remove SQL LIMIT vs large ceiling constant) + B2 (fallback-cap NUMBER; PO already ruled the fallback IS capped per REQ §5 — architect picks only the value/constant) + that dedup + HTML-strip are pure in-handler transforms (no schema/index/GROUP-BY change). Confirm `MAX_LIMIT_EXPLICIT` for `/news N`. Produce design note in `docs/architecture-briefs/`. **ONE architect pass covers BOTH sibling sprints (this + RECAP-ARCH)** — same zone `apps/mcp-server`, same file `telegramCommands.ts`, shared `stripHtml`. Hand to pm → dev-mcp-server (single dev pass, single ops rebuild). | HIGH | TASK | architect | docs/REQ_NEWS-FULLDAY.md | PENDING — spec APPROVED, ready. Sibling-batched with RECAP-ARCH (one pass). | NEWS-FD-BA |
| NEWS-FD-EXIT | **[po] Sprint sign-off vs Scope + Success Metric.** After ops rebuild + QA live verification: confirm `/news` returns the complete deduped importance-ranked day in plain VN with zero raw HTML, correctly chunked, fallback intact. Goal stays ARMED until QA attests live on `zenmidi.com/vn-market/webhook` + user verbal G9 on comprehensibility (lane-c). | HIGH | GATE | po | docs/handoffs/TASK_NEWS-FULLDAY.md | BLOCKED | NEWS-FD-BA + dev + ops + qa |

**Notes:**
- **NEWS-FULLDAY — PO KICKOFF 2026-05-27T21:41:17Z:** Self-scoped from the user's explicit `/news` refinement signal (item #1 of a four-item request; items 2/3/4 = `/recap` `/recapw` `/recapm` are ALREADY owned by the sibling Sprint RECAP-CMD opened 21:34Z — NO duplication). I REVISED my earlier "`/news` stays as-is" ruling on the record: `/news` stays a news LIST but must become COMPLETE + deduped + importance-ranked (the day-SYNTHESIS gap is still `/recap`'s, unchanged). PO traced the live `handleNews` — the date window is already correct (today-since-VN-midnight, impact-ranked); the real defects are the silent 20-cap, missing dedup, and raw HTML in `summary` (the latter folds in the backlogged NEWS-CMD-HTML-STRIP). All three fix inside ONE handler, single zone `apps/mcp-server`, no new infra. Channel-audit (MARKET/WORK/BUG live read) NOT run this cycle — the gateway `call_tool` wrapper is unavailable in the PO subagent toolset; flagged for main terminal on the next cron tick (does not block a user-initiated sprint). NEXT: ba. PIPELINE: continue.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push`; all on `main` (NO branches); do NOT touch any `pilot-status-*.json`; never ask user to run/deploy — spawn ops/dev; after dev change to mcp-server, ops MUST force-recreate (not restart); main terminal commits.

---

## Sprint RECAP-CMD — On-demand `/recap` `/recapw` `/recapm` Telegram pull-commands

**Status:** OPEN — OPENED 2026-05-27T21:34:53Z by PO (self-initiated from an EXPLICIT user feature request; full autonomy to scope). **Priority: HIGH — UX/coverage tier, user-facing.** User (non-technical, French-based, verbatim): *"/recap for day", "/recapw for week", "/recapm for month"*; and on the just-shipped `/news`: *"it reply but i dont see complete recap of day"*. Goal SSOT: `docs/SPRINT_GOAL_RECAP-CMD.md`. **Single zone: `apps/mcp-server`** — owner `dev-mcp-server`. Builds DIRECTLY on the just-shipped NEWS-CMD pattern (`handleNews` + `chunkStories` + webhook `texts[]` send-loop) — reuse, do not reinvent. NOT a scale pilot — does NOT consume the WIP=2 fleet cap, does NOT touch any `pilot-status-*.json`.

**PO autonomous decisions at kickoff (settled scope — BA/architect must NOT re-litigate):**
- **`/news` AMBIGUITY RESOLVED:** `/news` STAYS AS-IS (pure news LIST from `rag_analyses` — title + gist + sentiment). `/recap` IS the fuller day SYNTHESIS the user wants. The gap the user feels ("dont see complete recap of day") is closed by a NEW command, NOT by changing `/news`. Each command stays single-purpose.
- **#1 ARCHITECTURAL BLOCKER (data source per command) — RESOLVED by PO before any dev starts.** PO verified the container's reach: `docs/recaps/*.md` are NOT mounted into the mcp-server container (Dockerfile COPYs only `apps/mcp-server/src/`; compose mounts only `market_data`, `mcp.config.json`, `reports`, `docs/agent-memory`, three `docs/data/*.json`). Reading recap `.md` files (option a) is **REJECTED** — would force a compose remount + add a stale-file failure mode. All three commands use IN-CONTAINER, ALL-DB assembly functions that ALREADY EXIST:
  - `/recap` (day) → `assembleEveningSummary({ db })` (`apps/mcp-server/src/application/usecases/assembleEveningSummary.ts`) — returns typed `EveningSummary`. Run LIVE, render from typed fields. NOT the persisted `reports/*.json`.
  - `/recapw` (week) → `generatePeriodicSummary("weekly", undefined, db)` (`.../generatePeriodicSummary.ts`) — returns typed `PeriodicSummary`.
  - `/recapm` (month) → `generatePeriodicSummary("monthly", undefined, db)` — same use-case.
- **RENDER FROM STRUCTURED OBJECTS, NOT PROSE:** the existing `buildSummaryText()` / `PeriodicSummary.summaryText` produce ENGLISH + jargon ("RECOMMENDATIONS", "confidence: 70%", "[UP]", outlook="bullish") and MUST NOT be piped to Telegram. New handlers render their OWN plain-Vietnamese view from the TYPED fields only. Both assembly fns are `async` → handlers `async`; router already `async`.
- **NO new DB table / MCP tool / cron / microservice / compose change.** Synchronous read-only pulls, ~1s reply, same contract as `/news`. NOT a workflow trigger, NOT a background job. NO LLM in the render path.

**DoD (do NOT close early):** (1) `handleRecap`/`handleRecapWeek`/`handleRecapMonth` added to `telegramCommands.ts`, returning `{ texts: string[] }`, wired into `handleTelegramCommand` (`await`ed, same shape as the `/news` branch). (2) ALL user-facing text plain comprehensible Vietnamese — no jargon, direction + delta % on moves, `vi-VN` number format (`feedback_market_report_plain_vietnamese`, `feedback_market_data_direction`). (3) `chunkStories()` reused — no section split mid-item across 4096 chars. (4) `HELP_TEXT` lists the 3 new commands. (5) Plain-VN empty-state fallbacks per command. (6) Unit tests (injected in-mem DB / fakes, zero creds/network) — happy path + empty-state + chunk-boundary. (7) `/news` behavior UNCHANGED. (8) ops rebuild + FORCE-RECREATE mcp-server (not restart — stale image; `feedback_rebuild_after_dev_change`). (9) QA verifies LIVE on `zenmidi.com/vn-market/webhook` (NOT bare `/webhook` — fixed today, commit 3ddeb820). (10) Commit safety: serialized, no `-A`, main terminal commits (`feedback_concurrent_commit_race`).

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| RECAP-BA | **[ba] Requirement spec for `/recap` `/recapw` `/recapm`.** Read `docs/SPRINT_GOAL_RECAP-CMD.md` (data-source decision + render-from-structured-object caveat are LOCKED — do not re-open). Write `docs/REQ_RECAP-CMD.md` decomposing into atomic dev + QA tasks: (a) for EACH command, the exact plain-Vietnamese SECTION layout rendered from the named typed object's fields (`/recap` from `EveningSummary`: VN-Index move direction+points+%, watchlist movers direction+%, top news titles, notable alerts, open positions P/L; `/recapw`+`/recapm` from `PeriodicSummary`: period range, news/alert/report totals, key events, per-stock moves, alert breakdown — NO `summaryText`, NO English field names, NO numeric confidence); (b) the per-command empty-state Vietnamese strings; (c) the handler signatures (`async`, return `{ texts: string[] }`) + router wiring matching the `/news` branch; (d) chunk-boundary acceptance (a recap exceeding 4096 chars splits without breaking a section); (e) the unit-test matrix (happy / empty / chunk-boundary, injected fakes — mirror the `handleNews` test suite); (f) `HELP_TEXT` lines. Hand to architect to confirm the VN render contract + lock section labels, then to pm/dev-mcp-server. Spec → `docs/REQ_RECAP-CMD.md`. Return to PO spec-review gate. | HIGH | TASK | ba | docs/handoffs/TASK_RECAP-CMD.md | **APPROVED** — PO spec-review gate PASSED 2026-05-28 (critique-before-approve, 5/5 axes; typed-field claims verified live: every `EveningSummary` L97 + `PeriodicSummary` L53 field rendered actually exists; banned `summaryText`/`recommendation.confidence` genuinely present so the ban is enforceable; `stripHtml` reused from NEWS-FD, no dup). B1 (block>4096 split) + B2 (test-injection mechanism given `writeFileSync` side-effect) BOTH genuine architect calls — neither a PO product call. No conflict w/ NEWS-FULLDAY. NEXT: architect | — |
| RECAP-ARCH | **[architect] Technical design — confirm the plain-VN render contract + LOCK the Vietnamese section labels (REQ §3) + B1 (single section-block >4096 split strategy: newline-split + `(tiếp theo…)` marker per BA, or alternative) + B2 (test injection: `assembleFn` wrapper vs real assembly fns with in-memory DB given the `assembleEveningSummary` `writeFileSync`/`reportsDir` side-effect L831-833). Produce design note in `docs/architecture-briefs/`. **Sibling-batched with NEWS-FD-ARCH — ONE architect pass covers both** (same zone, same `telegramCommands.ts`, shared `stripHtml`). Hand to pm → dev-mcp-server (single dev pass, single ops rebuild). | HIGH | TASK | architect | docs/REQ_RECAP-CMD.md | PENDING — spec APPROVED, ready. Sibling-batched with NEWS-FD-ARCH (one pass). | RECAP-BA |
| RECAP-EXIT | **[po] Sprint sign-off vs Scope + Success Metric.** After ops rebuild + QA live verification: confirm all 3 commands reply in ~1s with complete plain-VN recaps, correctly chunked, sensible empty-states, `/news` unchanged. Goal stays ARMED until QA attests live on `zenmidi.com/vn-market/webhook`. | HIGH | GATE | po | docs/handoffs/TASK_RECAP-CMD.md | BLOCKED | RECAP-BA + dev + ops + qa |

**Notes:**
- **RECAP-CMD — PO KICKOFF 2026-05-27T21:34:53Z:** Self-scoped from the user's verbatim feature request. Two tangled asks split cleanly: `/news` (headlines, unchanged) vs `/recap` (whole-day synthesis, NEW). The #1 blocker the user flagged (where does recap content live + can the container reach it) is RESOLVED in-flow: `docs/recaps/*.md` are NOT mounted (verified Dockerfile + compose) so .md-reading is rejected; all 3 commands reuse the EXISTING all-DB in-container assembly fns (`assembleEveningSummary` for day, `generatePeriodicSummary` weekly/monthly for week/month) — zero filesystem dependency, zero compose change, ~1s sync reply. Critical caveat carried: render plain-VN from the TYPED objects, NEVER pipe the English `buildSummaryText`/`summaryText`. Single zone `apps/mcp-server`. Channel-audit (MARKET/WORK/BUG live read) NOT run this cycle — the gateway `call_tool` wrapper is unavailable in the PO subagent toolset; flagged for main terminal to run the routine audit on the next tick (it does not block this user-initiated sprint). NEXT: ba. PIPELINE: continue.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push`; all on `main` (NO branches); do NOT touch any `pilot-status-*.json`; never ask user to run/deploy — spawn ops/dev; after dev change to mcp-server, ops MUST force-recreate (not restart).

---

## Sprint SELF-IMPROVE-GATE — Gated Self-Improvement Loop (auto-detect → PO deliberation gate → implement behind proof)

**Status:** OPEN — OPENED 2026-05-27T22:30:00Z by PO (kickoff from EXPLICIT, USER-APPROVED DIRECTION — full autonomy). **Priority: HIGH — architecture/governance tier.** User (non-technical, verbatim): *"po approve, all automate, need thinking before approve anything."* Goal SSOT: `docs/SPRINT_GOAL.md § Sprint SELF-IMPROVE-GATE`. **agents-architect-led DESIGN** (this is a multi-agent governance design, the agents-architect's domain — `project_agents_architect`). **Zone: meta** — phase 1 is a DESIGN BRIEF only (`docs/architecture-briefs/`, zero code). Implementation phases split by lane: lane-(a)/(c) flow+agent `.md` → agent-father; lane-(b) microservice code → dev-team chain with QA gate (zone set per the brief). NOT a scale pilot — does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`.

**PO autonomous decisions at kickoff (settled scope — agents-architect must NOT re-litigate):**
- **DESIGN FIRST, no code in phase 1.** The user's FIRST ACTION is explicit: agents-architect writes the brief. Implementation is sequenced AFTER the brief returns to the PO gate. No `.md` edits, no microservice edits until the brief is PO-approved.
- **Reuse existing agents ONLY.** system-auditor = detect, agents-architect = propose, agent-father = `.md` impl, dev-team (po→ba→architect→pm→dev-*→qa) = code/tool impl. The brief may NOT introduce a brand-new orchestrator agent or a new always-on cron daemon without an explicit, line-itemed host-load budget (RAM + disk + tick cost) that the PO signs off — host kernel-panics under full fleet load (`project_host_memory_panic`), Docker capped 8GB, prior ENOSPC (`project_enospc_blocker`).
- **Three lanes are LOCKED, not a design choice.** (a) AUTO-PROPOSE + PO-APPROVE for flow/agent `.md`; (b) AUTO-IMPLEMENT only behind a PROVEN gate for hard/ungameable signals; (c) NEVER AUTO-CLOSE → escalate to human for subjective comprehensibility, irreversible actions, AND any change to the gate/audit logic itself. The brief formalizes the boundaries + routing; it does NOT get to drop a lane.
- **PO critique-before-approve is a HARD gate, not advisory.** Every proposal gets a written red-team critique (break-risk / false-green-risk / gameability / host-load) recorded BEFORE the verdict. The brief specifies WHERE the critique + proposal live (proposal doc format + PO notebook), so the gate is auditable and unskippable.
- **Anchor case study is binding:** today's plain-VN MARKET fix passed the internal tran-ngoc-bau audit yet failed the user ("too complicated"). Self-audit cannot detect subjective comprehensibility — that axis is lane-c FOREVER. The brief MUST name this blind spot and route it to the human; a design that lets the loop self-certify comprehensibility is REJECTED.

**DoD (do NOT close early):** (1) architect brief exists at `docs/architecture-briefs/2026-05-27-gated-self-improvement-loop.md` formalizing the three-lane model + PO deliberation gate + proven-gate proof procedure + host-load budget + escalation boundary. (2) The brief specifies the EXACT artifacts: proposal doc format/location, the PO critique fields, the proposal→lane routing table, the proven-gate proof procedure (inject-a-violation-confirm-red), and which existing flow `.md` files each phase touches. (3) PO reviews the brief at the deliberation gate (writes a critique of the DESIGN itself before approving). (4) Implementation phases sequenced by lane after PO approval — `.md` via agent-father, code via dev-team+QA. (5) The proven-gate proof is DEMONSTRATED red on a deliberate violation for any lane-b mechanism before that mechanism is trusted (`feedback_fence_false_green`). (6) NO new always-on cron/agent unless the brief carries an explicit PO-approved host-load budget. (7) Commit safety: serialized commits, no `-A`, main terminal commits (`feedback_concurrent_commit_race`).

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| SIG-DESIGN | **[agents-architect] Architecture brief — Gated Self-Improvement Loop.** Write `docs/architecture-briefs/2026-05-27-gated-self-improvement-loop.md` formalizing: (1) the THREE-LANE model — (a) AUTO-PROPOSE + PO-APPROVE for flow/agent `.md`, (b) AUTO-IMPLEMENT behind a PROVEN gate ONLY for hard/ungameable signals (tests/lint/dedup), (c) NEVER AUTO-CLOSE → escalate to human for subjective comprehensibility + irreversible actions + ANY change to the gate/audit logic itself — with a crisp classification rule + worked examples of each lane drawn from real system memory (e.g. stale-doc=lane-a, missing-test-with-proven-gate=lane-b, plain-VN-comprehensibility=lane-c). (2) The DETECT→PROPOSE→DELIBERATE→IMPLEMENT pipeline mapped onto EXISTING agents ONLY (system-auditor detect via DASHBOARD; agents-architect emit a structured proposal doc; PO deliberation gate; agent-father for `.md` / dev-team po→ba→architect→pm→dev-*→qa for code). (3) The PROPOSAL artifact: exact file format + location (e.g. `docs/improvement-proposals/<id>.md`) with required fields (weakness, evidence, proposed change, lane, success-signal, rollback). (4) The PO CRITIQUE gate: the mandatory red-team fields (what could break / false-green + silent-swallow risk / is the success signal gameable / host-load impact), recorded before the verdict, auditable. (5) The PROVEN-GATE proof procedure for lane-b: inject a deliberate violation → confirm the gate goes red → only then trust it (`feedback_fence_false_green`, `feedback_silent_swallow_serial_bugs`). (6) The HOST-LOAD budget rule: no new always-on agent/cron without a line-itemed RAM+disk+tick budget; prefer riding existing cron ticks (`project_host_memory_panic`, `project_enospc_blocker`). (7) The ESCALATION boundary: how lane-c reaches the human + why the loop can never self-promote a lane-c item. (8) COMMIT-SAFETY note (serialized commits, `feedback_concurrent_commit_race`). (9) An IMPLEMENTATION SEQUENCE proposal listing which flow/agent `.md` each phase touches + which (if any) needs code. Signal agent-father per the doc-ownership rule for any `.md` it proposes to edit. Brief → `docs/architecture-briefs/`. DESIGN ONLY — zero code, zero `.md` edits outside the brief. Return to PO deliberation gate. | HIGH | TASK | agents-architect | docs/handoffs/TASK_SELF-IMPROVE-GATE.md | **DONE** — brief delivered `docs/architecture-briefs/2026-05-27-gated-self-improvement-loop.md` (Architect Design Record e1557bc7); 9 required sections + EDIT-1..5 flow map + §9 Phase-2 code map | — |
| SIG-PO-GATE | **[po] Deliberation gate on the brief.** PO red-teams the DESIGN itself (not rubber-stamp): does the three-lane boundary actually hold? can the loop self-promote a lane-c item? is the proven-gate proof real or asserted? does any proposed cron blow the host budget? is the comprehensibility blind spot truly routed to the human? Write the critique (break/false-green/gameability/host-load) BEFORE the verdict. Approve / reject-with-feedback / escalate. | HIGH | GATE | po | docs/handoffs/TASK_SELF-IMPROVE-GATE.md | **DONE** — VERDICT APPROVE-WITH-CONDITIONS (df65d6ca); 5 conditions C-1..C-5 recorded in handoff BEFORE verdict; 4/5 red-team axes PASS clean (host-load within 1948 budget, blind-spot named lane-C-forever, gameability-barrier holds, lane-B gate shape correct); 2 false-green seams + 1 EDIT-4 input-contract mismatch attached as hardening conditions (none lane-C, none re-opens design) | SIG-DESIGN |
| SIG-IMPL-MD | **[agent-father] Codify lane-(a)/(c) into flow + agent `.md`.** After PO approval: implement the brief's `.md` changes — the proposal-emit step (system-auditor/agents-architect), the PO review-proposal deliberation gate (PO flow), the lane-routing + escalation boundary. Per AGENT_CREATION_GUIDE.md, SSOT/DRY/lazy-load. Serialized commit, no `-A`, main terminal commits. | HIGH | TASK | agent-father | docs/handoffs/TASK_SELF-IMPROVE-GATE.md | **DONE** — committed 062a6569. EDIT-1..5 wired into 5 flow files (166 insertions): system-auditor D-IMPROVE proposal emit (C-5 skip-not-abort), agents-architect IP-review handler, po triage `improvement_proposal` row w/ 5 mandatory critique fields (C-3 lane-C-in-disguise), agent-father `improvement_approved_md` route (C-1 structured target_agent/target_files + C-2 IMPLEMENTING→DONE lifecycle), dev-team `improvement_proposal_lane_b` route (C-4 per-path gate deferred to Phase-2 code) | SIG-PO-GATE |
| SIG-IMPL-GATE | **[dev-team via po→ba→architect→pm→dev-mcp-server→qa] Lane-(b) proven-gate CODE mechanism.** Build the Phase-2 lane-b substrate (brief §9 Phase 2 + SPIKE_1947 §6 Phase 1) — these files do NOT exist yet (grep-confirmed absent): ship the never-shipped Sprint-1948 SHADOW-MODE code (`improve_check_log` table in `schema-system.ts`, `improveCheckStore.ts`, `degradationRules.ts` DEGRADATION_CAUSE_MAP, `selfImproveOrchestratorJob.ts` cron entry, `cronConfig.ts`+`startScheduler.ts` wiring) EXTENDED to emit `docs/improvement-proposals/<IMP-YYYYMMDD-slug>.md` DRAFT docs (the D-IMPROVE bridge from brief §9 Phase 2). Do NOT reinvent the detection layer — it is already designed in SPIKE_1947 §4/§5/§8; this builds the designed-but-unshipped code + the governance bridge. Zone: `apps/mcp-server/` ONLY. **C-4 (HARD, QA-owned):** `SELF_IMPROVE_AUTO_DISPATCH` MUST be PER-DISPATCH-PATH, default `false` per path, flipped `true` only AFTER QA records that path's GATE-PROOF — ONE GLOBAL FLAG IS REJECTED. The proven-gate proof (inject a deliberate violation INTO THE SUBJECT CODE not the gate config → confirm gate goes RED → remove → confirm GREEN; brief §5 GATE-PROOF-1..5) is QA's ACCEPTANCE CRITERION, NOT an assertion (`feedback_fence_false_green`, `feedback_silent_swallow_serial_bugs`). Shadow mode is the ship target — NO live auto-dispatch in this task (Phase-3 global enable is a separate human-gated step). Host budget: NO new always-on agent, NO new Docker service, NO new cron beyond the already-budgeted 1948 `selfImproveOrchestratorJob` 0 9 * * * inside the existing mcp-server process (`project_host_memory_panic`, 8GB cap). FULL chain WITH QA gate — never a shortcut. **NEXT: ba** decomposes into atomic tasks reading brief §9 Phase 2 + SPIKE_1947 §6/§8/§12 + this handoff + condition C-4. | HIGH | TASK | po→ba | docs/handoffs/TASK_SELF-IMPROVE-GATE.md | **SPEC-GATE APPROVED 2026-05-27T21:16:12Z** — ba spec `docs/REQ_SIG-IMPL-GATE.md` + architect blueprint (handoff `[Architect] SIG-IMPL-GATE Blueprint`) both PASS the PO spec gate on all 7 axes (handoff `[PO] SIG-IMPL-GATE Spec-Gate Verdict`). C-1..C-5 honored; GATE-PROOF QA-owned subject-code-injected; greenfield = no lane-C leak; shadow-mode inert, one cron slot, host budget OK. 2 hardening notes recorded: HN-1 (cron `2 9 * * *` correct; "bctcOverdueCheck is weekdays" premise FALSE — live is daily), HN-2 (anti-runaway order = `DEGRADED > PERSISTENTLY_LOW > COVERAGE_GAP`; reconcile REQ TASK-3 step 8). **NEXT: pm** — decompose REQ TASK-1..6 into TASK_NNN dev tasks (rows below), sequence dev-mcp-server serial → ops REBUILD → qa GATE-PROOF, honor WIP=2. | SIG-PO-GATE (done) | --- **CLOSED 2026-05-27T22:04:44Z** — all 6 child tasks DONE; shipped commit ef109a76 (in main ancestry, PO-verified); deployed LIVE (mcp-server force-recreated, healthy, shadow-mode, cron `2 9 * * *` registered); QA GATE-PROOF PROVEN-RED. C-1..C-5 all PO-verified in shipped code (not asserted). |
| SIG-G-T1 | **[dev-mcp-server] TASK-1 schema + store.** Add `improve_check_log` table (REQ §TASK-1 schema verbatim) to `initSystemTables()` in `apps/mcp-server/src/infrastructure/db/schema-system.ts`; create NEW `apps/mcp-server/src/infrastructure/db/improveCheckStore.ts` with the 4 injectable-`db` functions + `ImproveCheckRow`/`ImproveCheckInsert`/`DispatchStatus` types. 6 tests (AC-T1-1..6). Fail-loud invalid `dispatch_status` at insert (app-layer throw, no silent coerce). | HIGH | TASK | dev-mcp-server | docs/REQ_SIG-IMPL-GATE.md | **DONE** — committed ef109a76; `improve_check_log` table live in container DB (PO-verified `sqlite_master` query); store + types shipped | SIG-IMPL-GATE |
| SIG-G-T2 | **[dev-mcp-server] TASK-2 domain detect.** NEW `apps/mcp-server/src/domain/services/degradationRules.ts` — `DEGRADATION_CAUSE_MAP` (SPIKE §5 verbatim, ADD-only, no modify), `detectDegradedSignalTypes()` pure (zero infra imports — local `SignalAccuracyStatsByType` re-declaration per blueprint §2). Two-window delta policy SPIKE §4 settled. 8 tests (AC-T2-1..8, incl. AC-T2-7 zero-infra-import static check). | HIGH | TASK | dev-mcp-server | docs/REQ_SIG-IMPL-GATE.md | **DONE** — committed ef109a76; degradationRules.ts DDD-pure (PO grep-confirmed zero infra/app imports); this is the lane-B subject code QA proved RED (SIG-G-T6) | SIG-G-T1 |
| SIG-G-T3 | **[dev-mcp-server] TASK-3 orchestrator + cron wiring.** NEW `selfImproveOrchestratorJob.ts` (Option C, inside mcp-server; `_running` + `wrapRun` mirror `accuracyDigestJob.ts`); `cronConfig.ts` add `selfImproveOrchestrator` = **`2 9 * * *`** (HN-1: the `0 9 * * *` premise vs `marketOpen 0 9 * * 1-5`/`bctcOverdueCheck 0 9 * * *` collision → offset is correct; ONE new slot only); wire `startScheduler.ts` via `jobRunRepo.wrapRun()`. 9 tests (AC-T3-1..9). **HN-2: anti-runaway top-2 order = `DEGRADED > PERSISTENTLY_LOW > COVERAGE_GAP`** (architect canonical; reconcile REQ TASK-3 step 8 wording before writing AC-T3-8). | HIGH | TASK | dev-mcp-server | docs/REQ_SIG-IMPL-GATE.md | **DONE** — committed ef109a76; cron `2 9 * * *` registered via `jobRunRepo.wrapRun('selfImproveOrchestratorJob', ...)` (PO-verified in startScheduler.ts:941 + live cronConfig.ts:172); HN-2 order honored in code (docstring step 8); outer catch re-throws so wrapRun records status='error' | SIG-G-T2 |
| SIG-G-T4 | **[dev-mcp-server] TASK-4 D-IMPROVE proposal-doc bridge.** Extend orchestrator to write `docs/improvement-proposals/IMP-<YYYYMMDD>-<slug>.md` DRAFT (status DRAFT only — C-2 no DONE flip) with C-1 structured `target_agent`/`target_files[]` via typed `FIX_AREA_TO_AGENT` (no prose parse; `_default`→UNRESOLVED/[]) + append DASHBOARD.md `## po` row. **C-5: doc-write isolated in try/catch — must NOT abort improve_check_log insert or WORK Telegram** (AC-T4-6). Extract `improvementSignalWriter.ts` per blueprint §1e. mkdir-if-absent for the new dirs (R-3/R-4). 8 tests (AC-T4-1..8). | HIGH | TASK | dev-mcp-server | docs/REQ_SIG-IMPL-GATE.md | **DONE** — committed ef109a76; `improvementSignalWriter.ts` shipped with typed `FIX_AREA_TO_AGENT` (C-1, no prose parse, UNRESOLVED fail-safe — PO-verified); orchestrator step-12 doc-write wrapped in isolated try/catch "non-fatal" (C-5, PO-verified selfImproveOrchestratorJob.ts:391/423-432); `docs/improvement-proposals/` dir created (empty — no live degraded data yet). **NOTE: emit path proven by unit mocks (AC-T4-1..8); NOT yet proven end-to-end against a real run — see SIG-EXIT condition X-1.** | SIG-G-T3 |
| SIG-G-T5 | **[dev-mcp-server] TASK-5 per-path kill-switch (C-4 HARD).** `DISPATCH_PATHS as const` + `type DispatchPath`; `isAutoDispatchEnabled(path: DispatchPath)` reads `SELF_IMPROVE_AUTO_DISPATCH_{PATH_UPPER}`; **default-false per path enforced in code, NO global flag** (AC-T5-4 asserts a bare `SELF_IMPROVE_AUTO_DISPATCH=true` enables nothing). `docker-compose.yml` commented-out per-path env vars w/ "flip ONLY after QA GATE-PROOF" note. 5 tests (AC-T5-1..5). | HIGH | TASK | dev-mcp-server | docs/REQ_SIG-IMPL-GATE.md | **DONE** — committed ef109a76; `DISPATCH_PATHS as const` + typed `DispatchPath`, `isAutoDispatchEnabled` reads `SELF_IMPROVE_AUTO_DISPATCH_{PATH_UPPER}` (no global flag can match — C-4 enforced at COMPILE time, PO-verified selfImproveOrchestratorJob.ts:62-89); docker-compose.yml lines 52-55 all 4 per-path vars commented-out (default-false); call-site type-guards `DISPATCH_PATHS.includes(...)` before lookup | SIG-G-T4 |
| SIG-G-REBUILD | **[ops] Force-recreate mcp-server (REBUILD, not restart).** After SIG-G-T1..T5 land + commit, `docker-compose` force-recreate the mcp-server container so the new cron/code is live (`feedback_rebuild_after_dev_change` — restart relaunches the stale image). Confirm `selfImproveOrchestrator` cron registered + healthy. Required before QA can observe a live proposal doc. | HIGH | TASK | ops | docs/REQ_SIG-IMPL-GATE.md | **DONE** — mcp-server force-recreated LIVE; PO independently verified: container healthy + 146 tools (`/health` 200), `improve_check_log` table present in live `/app/data/market.db`, ZERO `SELF_IMPROVE_*` env vars in container (SHADOW MODE confirmed), no selfImprove boot crash in logs | SIG-G-T5 |
| SIG-G-T6 | **[qa] TASK-6 GATE-PROOF (acceptance, not assertion).** Against the running orchestrator's first proposal doc: inject a deliberate violation INTO `degradationRules.ts` SUBJECT CODE (threshold const, not test/gate config) → confirm AC-T2-1 goes RED → remove → confirm GREEN → record "Gate proven red on [date] by [method]. Evidence:" into the proposal doc `## Gate Proof` (AC-T6-1..4). **AC-T6-5: if gate does NOT go red → record `GATE-PROOF-FAILED`, demote lane-B→lane-A; never silent-pass** (`feedback_fence_false_green`). Verify no path is `true` without a recorded GATE-PROOF (C-4 enforcement). No test-baseline regression (floor 9408 PASS / ceiling 348 FAIL). | HIGH | GATE | qa | docs/REQ_SIG-IMPL-GATE.md | **DONE — GATE-PROOF: PROVEN-RED** (qa notebook cycle-135, 2026-05-28). Injected `>=0.50` into degradationRules.ts DEGRADED threshold (subject code, NOT gate config) → 5 AC tests RED; reverted → 15/15 GREEN; clean git diff (byte-identical to ef109a76). AC-T6-1 PASS (docker-compose all 4 commented), AC-T6-2/3 PASS (RED-then-GREEN), AC-T6-5 N/A (gate DID go red, no lane-A demotion). **AC-T6-4 DEFERRED** — no live proposal doc emitted (shadow-mode, no real degraded `signal_outcomes` data); gate-proof evidence recorded in qa notebook. Sharded suite (8GB cap blocks full 9408 run): 305 pass / 0 NEW failures / 33 files; tsc exit 0. C-4 enforced: no path `true`. **PO ruling at SIG-EXIT: AC-T6-4 is the one real open seam → SIG-EXIT condition X-1 (synthetic dry-run to prove emit path end-to-end).** | SIG-G-REBUILD |
| SIG-EXIT | **PO sign-off vs Scope + Success Metric.** Verify the loop is live + the PO critique gate is unskippable + three lanes hold + lane-b proven red on injected violation + lane-c escalates + zero un-budgeted new cron. Goal stays ARMED until the loop is demonstrated end-to-end. | HIGH | GATE | po | docs/handoffs/TASK_SELF-IMPROVE-GATE.md | **APPROVE-WITH-CONDITIONS 2026-05-27T22:04:44Z** (critique + verdict in handoff `[PO] SIG-EXIT critique`). All 7 SIG-EXIT checklist axes PASS, PO-verified against live container + git + shipped code (not QA word alone): flow-resident loop live end-to-end (5 EDIT flows 062a6569 + code gate ef109a76 connect: system-auditor D-IMPROVE → po 5-field critique gate → agent-father lane-A / dev-team lane-B → proven gate); PO critique gate unskippable (5 mandatory fields incl. lane-C-in-disguise, auto-reject empty); three lanes hold; lane-B PROVEN-RED (qa cycle-135); lane-C escalates + STOPS; C-1/C-4/C-5 honored in shipped code; one new cron slot, shadow-mode, no new Docker/agent. **2 CONDITIONS (routable to dev/qa, NOT lane-C):** X-1 = forced synthetic-data dry-run proving the D-IMPROVE emit path end-to-end (`writeImprovementProposal` → real file in `docs/improvement-proposals/` + DASHBOARD.md append with valid structured `target_agent`/`target_files`) — closes AC-T6-4; X-2 = log a follow-up for the 1941c batch-isolation false-green (SIG-FOLLOWUP-1941C below). Open-item rulings + human-reserved boundary in handoff. | SIG-IMPL-GATE |
| SIG-FOLLOWUP-DRYRUN (X-1) | **[dev-mcp-server → qa] Synthetic-data dry-run of the D-IMPROVE emit path.** Seed a synthetic degraded `signal_outcomes` fixture (or inject `detectFn`/`coverageGapFn` deps) so `runSelfImproveOrchestrator` reaches step-12 and actually calls `writeImprovementProposal` → produces ONE real `docs/improvement-proposals/IMP-*.md` DRAFT file + appends a `DASHBOARD.md ## po` row. Verify the file carries valid structured `target_agent` (kebab-case, from `FIX_AREA_TO_AGENT`, or `UNRESOLVED`) + `target_files[]` (C-1), `status: DRAFT` (C-2, no DONE flip), and `lane: LANE-A|LANE-B` correctly classified. This proves the emit seam end-to-end (closes AC-T6-4) — unit mocks alone (AC-T4-1..8) do not prove the orchestrator wires the real writer. Reversible, machine-checkable, zero live auto-dispatch (paths stay default-false). Do NOT flip any path. | MEDIUM | TASK | dev-mcp-server | docs/handoffs/TASK_SELF-IMPROVE-GATE.md | OPEN — non-blocking; closes the one SIG-EXIT condition | SIG-EXIT |
| SIG-FOLLOWUP-1941C (X-2) | **[dev-mcp-server] Fix the 1941c accuracyDigest batch-isolation false-green.** `1941c-accuracy-digest.test.ts` passes 7/7 standalone but FAILS in batch (shared `signal_outcomes` state across concurrent test workers; committed 6a690c24, predates SIG). Out of SIG scope but a latent test-isolation false-green in the broader suite (`feedback_fence_false_green`). Isolate the test's DB/state (per-test `:memory:` or unique signal_type namespace) so batch == standalone. No production-code change expected. | LOW | TASK | dev-mcp-server | docs/TASKS.md | OPEN — logged at SIG-EXIT; accepted out-of-SIG-scope, not a blocker | SIG-EXIT |

**Notes:**
- **SELF-IMPROVE-GATE — PO SIG-IMPL-GATE SPEC-GATE VERDICT 2026-05-27T21:16:12Z (Phase 2):** APPROVED. I red-teamed the architect's blueprint (handoff `[Architect] SIG-IMPL-GATE Blueprint`) against the live repo — not a rubber-stamp (user mandate "need thinking before approve anything"). Grep-verified GREENFIELD (4 target files ABSENT, `improve_check_log`=0, `SELF_IMPROVE_AUTO_DISPATCH`/`DEGRADATION_CAUSE_MAP`=zero in `apps/mcp-server/src`), confirmed reused infra actually exists (`getAccuracyStats`/`SignalAccuracyStats` in signalOutcomeStore.ts, `jobRunRepo.wrapRun()` + `accuracyDigestJob.ts` `_running`/`AccuracyDigestDeps` reference pattern), and read the live cronConfig 09:xx block. All 7 mandated axes PASS: C-1 typed `FIX_AREA_TO_AGENT` (no prose parse, total map, UNRESOLVED fail-safe); C-2 DRAFT-only no DONE flip; C-3 lane-C-in-disguise field shipped Phase-1; **C-4 strongest — per-path default-false enforced at COMPILE time (`DispatchPath` union) + AC-T5-4 global-flag REJECT, structurally not by convention**; C-5 doc-write isolated (AC-T4-6); GATE-PROOF QA-owned, subject-code-injected, RED-then-GREEN + lane-A demotion on false-green (AC-T6-5); greenfield = lane-C-in-disguise structurally impossible (no constant to mutate yet); shadow-mode inert, ONE cron slot, within Sprint-1948 host budget, no regression-floor breach. **2 hardening notes (NOT blockers, no return to architect):** HN-1 the blueprint's "bctcOverdueCheck = `0 9 * * 1-5`" is FACTUALLY WRONG — live is `0 9 * * *` DAILY (the `1-5` slot is `marketOpen`); the offset DECISION to `2 9 * * *` is nonetheless CORRECT (avoids both the daily bctc and the weekday marketOpen pileup); pm must not propagate the false premise. HN-2 anti-runaway severity ordering diverges silently: REQ TASK-3 step 8 says `DEGRADED > COVERAGE_GAP > PERSISTENTLY_LOW`, blueprint says `DEGRADED > PERSISTENTLY_LOW > COVERAGE_GAP`; architect's order is canonical, reconcile REQ before AC-T3-8 is written. Routed SIG-IMPL-GATE into the dev chain — decomposed into SIG-G-T1..T5 (dev-mcp-server, serial by dependency) → SIG-G-REBUILD (ops force-recreate) → SIG-G-T6 (qa GATE-PROOF). NEXT: pm. PIPELINE: continue.
- **SELF-IMPROVE-GATE — PO SIG-IMPL-GATE KICKOFF 2026-05-27 (Phase 2):** Phase 1 (SIG-IMPL-MD) is DONE+committed 062a6569 — agent-father wired the gated loop into 5 flows honoring C-1/C-2/C-3/C-5. SIG-PO-GATE verdict was APPROVE-WITH-CONDITIONS (df65d6ca, PO's own). I now drive Phase 2 (SIG-IMPL-GATE, lane-b proven-gate CODE) through the dev-team chain po→ba→architect→pm→dev-mcp-server→qa. **Substrate reconciliation (grep-confirmed):** the Sprint-1948 files (`selfImproveOrchestratorJob.ts`, `degradationRules.ts`, `improveCheckStore.ts`, `improve_check_log`, `SELF_IMPROVE_AUTO_DISPATCH`) do NOT exist in the codebase yet — 1948 was QUEUED-and-PAUSED, never shipped. So SIG-IMPL-GATE BUILDS the designed-but-unshipped code from SPIKE_1947 §6 Phase-1 (do NOT reinvent the detection design — §4 two-window delta, §5 DEGRADATION_CAUSE_MAP, §8 schema are settled), EXTENDED with the brief §9 Phase-2 D-IMPROVE bridge (emit `docs/improvement-proposals/<id>.md` DRAFT docs, not just a WORK Telegram). **C-4 carried forward as the HARD QA-owned gate:** `SELF_IMPROVE_AUTO_DISPATCH` per-dispatch-path, default false per path, flipped true ONLY after QA records that path's GATE-PROOF (inject violation INTO subject code → RED → remove → GREEN, §5 GATE-PROOF-1..5). One global flag REJECTED. SHADOW MODE is the ship target — this task ships detection+log+proposal-emit with auto-dispatch OFF. Host budget honored: zero new always-on agent/Docker-service/cron beyond the budgeted 1948 job inside mcp-server (~0MB incremental). **Lane-C boundary check (per the kickoff constraint to STOP if a human product call is required):** the BUILD itself is NOT lane-C — it is reversible, the success signal is machine-checkable (gate proven red), and it IMPLEMENTS the gate logic rather than editing it. No lane-C boundary is crossed by shipping shadow-mode code behind a default-false per-path kill-switch. The ONE forward-looking item that stays human-reserved (named here, NOT authorized by this kickoff): the eventual GLOBAL/fleet-wide flip of auto-dispatch to live (Phase-3 across all paths at once) and ANY change to the gate/audit logic itself = lane-C — those need an explicit user product call when/if they ever arise. They are NOT part of SIG-IMPL-GATE. NEXT: ba. PIPELINE: continue.
- **SELF-IMPROVE-GATE — PO KICKOFF 2026-05-27T22:30:00Z:** Self-scoped from the user-approved direction "po approve, all automate, need thinking before approve anything." This is a META-governance sprint — it builds the loop by which the system improves its OWN flows + tools, with the PO as the deliberating approval authority (NOT the user for routine changes) and the human reserved for the lane-c blind spot (subjective comprehensibility, irreversibility, gate-self-edit). Reuses existing agents only; phase 1 is a design brief (zero code). Builds on the PAUSED Sprint 1948 closed-loop auto-improvement Phase-1 shadow-mode work (`project-stats.json#previousSprint`) — agents-architect SHOULD reconcile with that prior shadow-mode design rather than reinvent it. Channel-audit step NOT run this cycle — the gateway `call_tool` wrapper is unavailable in the PO subagent toolset; for an explicit user-approved direction the direction IS the backlog. Flagged for main terminal to run the routine MARKET/WORK/BUG audit on the next cron tick.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push`; all on `main` (NO branches); do NOT touch any `pilot-status-*.json`; never ask user to run/deploy — spawn ops/dev; commit-mutex uncallable by subagents → leave files UNSTAGED, main terminal commits.

---

## Sprint NEWS-CMD — On-demand `/news` Telegram pull command (full news digest)

**Status:** DONE (build) — PO SIGNED OFF 2026-05-27T20:52:50Z. OPENED 2026-05-27T19:50:01Z by PO (kickoff from EXPLICIT USER FEATURE REQUEST — full autonomy). **Priority: MEDIUM — UX/coverage tier.** User (non-technical, verbatim): *"make /news on telegram command for get all content if user need"*. Goal SSOT: `docs/SPRINT_GOAL.md § Sprint NEWS-CMD`. **Zone: `apps/mcp-server/`** (dev-mcp-server = sole code owner). NOT a scale pilot — does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`. **Sprint goal stays ARMED on the USER-comprehensibility axis only (verbal G9) — every machine-checkable AC is MET.** One LOW backlog follow-up opened: NEWS-CMD-HTML-STRIP.

**PO autonomous product decisions (do NOT re-litigate — these are settled scope, not BA/architect choices):**
- **SYNCHRONOUS command pattern** (like `/watchlist`/`/price`/`/health`), NOT the async `/ask` queue. Data already exists in `rag_analyses`; no agent reasoning needed. A spawned QA-responder-style agent would be wasteful overhead. REJECTED the async-queue approach.
- **Source = `rag_analyses` table** (the live news+impact SSOT), reusing the exact query shape already proven in `newsFetchLiveHandler.ts` (`GET /api/news-fetch/live`). NOT the `docs/daily/` blackboard (that's a future-migration target post-cowork-redesign-Phase-3; must not block on it).
- **Pull-only reply** via the existing webhook request/response path (`sendTelegramMarket` with the user's `chatId`). Does NOT collide with the cron-only MARKET-GROUP push rule — confirmed: that rule (`docs/architecture-briefs/2026-05-27-cowork-team-daily-document-redesign.md § C`) governs unsolicited GROUP pushes; a direct reply to a user command is the established in-production `/ask`/`/watchlist` lane.

**Verified codebase facts (do NOT re-derive — main terminal + PO traced these at kickoff):**
- Inbound command router: `apps/mcp-server/src/infrastructure/notifiers/telegramCommands.ts` — `handleTelegramCommand` switch (extend it; add `/news` to the switch + `HELP_TEXT`). `/news` confirmed NOT already implemented (grep-clean).
- Webhook reply path: `apps/mcp-server/src/interface/mcp/routes/webhookHandler.ts` — already sends sync command results back via `sendTelegramMarket(result.text, {chatId})`. NOTE: currently sends ONE message per command result — see NEWS-CMD-DESIGN for the chunking implication.
- News source + reusable query: `apps/mcp-server/src/interface/mcp/routes/newsFetchLiveHandler.ts` reads `rag_analyses` (source_title, source_url, published_at, sentiment, impact_direction, impact_score, created_at). The evening-summary digest (`apps/mcp-server/src/application/usecases/assembleEveningSummary.ts` L449-460) shows the "stories since midnight GMT+7, ORDER BY impact_score DESC" pattern to mirror.

**DoD:** (1) `/news` live in the running mcp-server container, returns a plain-Vietnamese digest of today's `rag_analyses` content on demand (headline + one-line gist + plain-VN sentiment label); `/news N` caps count. (2) Over-4096-char output is CHUNKED into sequential messages — NO silent truncation. (3) Empty-table → friendly "chưa có tin hôm nay" fallback (not N/A/null/undefined). (4) Plain comprehensible Vietnamese ONLY — no impact_score numbers, no jargon (`feedback_market_report_plain_vietnamese`). (5) Unit tests: formatted digest from seeded rows + empty-DB fallback + chunking proof; no test-baseline regression (floor 9408 PASS / ceiling 348 FAIL per `project-stats.json`). (6) ops REBUILD (not restart) mcp-server after the change (`feedback_rebuild_after_dev_change`). (7) QA verifies live with real content (not stub/NOT-RUN); USER confirms it reads usefully.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| NEWS-CMD-BA | **[BA] Requirement spec.** Decompose `docs/SPRINT_GOAL.md § Sprint NEWS-CMD` into `docs/REQ_NEWS-CMD.md`: testable acceptance criteria for the `/news` sync handler (digest formatting, plain-VN sentiment labels, `/news N` arg parsing, empty-DB fallback wording, Telegram chunking over 4096 chars, never-throws contract). Specify the `rag_analyses` query (today-since-midnight-GMT+7 ordered by impact, fallback to most-recent N if today empty) reusing the `newsFetchLiveHandler.ts` shape. Flag for the architect ONLY two design points: (i) the chunking mechanism (multi-message send vs single-message hard-cap + "thêm" affordance), (ii) the today-vs-recent fallback window. Do NOT add async-queue, do NOT add a new push, do NOT read `docs/daily/` — those are PO-settled OUT-of-scope. Return spec to PO approval gate. | MEDIUM | TASK | ba | docs/handoffs/TASK_NEWS-CMD.md | **DONE** — spec APPROVED at PO gate 2026-05-27T20:01:02Z (`docs/REQ_NEWS-CMD.md` § 11 PO RULING) | — |
| NEWS-CMD-DESIGN | **[architect] Lightweight design note.** Spec APPROVED (`docs/REQ_NEWS-CMD.md` § 11). **PO has PRE-SETTLED both prior deferrals — read § 11 first:** (B1) chunking MUST deliver ALL content — Option B (single-message cap + "thêm") is REJECTED; the architect's ONLY remaining decision is the exact single-zone Option-A implementation contract (CommandResult `texts?: string[]` + webhook send loop, OR handler-driven sequential `sendTelegramMarket`, OR a small chunker helper) with AC-FR6-1/2/3 provably met. (B2) fallback window is SETTLED = most-recent N (no date window) + header switches to "Tin tức gần đây" — do NOT re-decide. Confirm sync-handler placement in the switch + the `rag_analyses` query. Confirm zero collision with the cron-only push rule + zero change to delivery cron / alert-commander + single-zone (`apps/mcp-server/`). Brief → `docs/architecture-briefs/`. Small scope — single-command feature, not an architecture overhaul. | MEDIUM | TASK | architect | docs/handoffs/TASK_NEWS-CMD.md | **DONE** — design complete 2026-05-27. Brief: `docs/architecture-briefs/2026-05-27-news-cmd-design.md`. Handoff: `docs/handoffs/TASK_NEWS-CMD.md`. Chosen contract: `CommandResult.texts?: string[]` + sequential send-loop in `webhookHandler.ts`. B2 confirmed. | NEWS-CMD-BA (done) |
| NEWS-CMD-IMPL | **[dev-mcp-server] Implement `/news`.** Per architect brief `docs/architecture-briefs/2026-05-27-news-cmd-design.md` and handoff `docs/handoffs/TASK_NEWS-CMD.md`. Four files: (1) `telegramCommands.ts` — add `VN_OFFSET_MS` import from `timeConstants.js`, extend `CommandResult` with `texts?: string[]`, add `handleNews(db, args)` implementing today-query + fallback + plain-VN formatter + story-boundary chunker (each chunk <= 4096 chars), add `case "/news":` branch, add `/news` line to `HELP_TEXT`. (2) `webhookHandler.ts` — replace single `sendTelegramMarket(result.text,...)` with `const chunks = result.texts ?? [result.text]; for (const chunk of chunks) { await sendTelegramMarket(chunk,...) }`. (3) `src/__tests__/214-telegram-commands.test.ts` — add T-NEWS-1..8 test cases (seeded rag_analyses, sentiment map, empty-DB fallback, chunking proof). (4) `docs/architecture/microservice/mcp-server/news-analysis.md` — append `/news` command reference. Zero changes outside `apps/mcp-server/`. Never throw. No new push. ops REBUILD after. | MEDIUM | TASK | dev-mcp-server | docs/handoffs/TASK_NEWS-CMD.md | **DONE** — commit 25a92ca6 | NEWS-CMD-DESIGN (done) |
| NEWS-CMD-DEPLOY | **[ops] REBUILD + force-recreate mcp-server.** `docker compose build mcp-server` then `up -d --no-deps --force-recreate` (restart relaunches stale image). Confirm container healthy + webhook responsive. | MEDIUM | TASK | ops | docs/handoffs/TASK_NEWS-CMD.md | **DONE** — container healthy 2026-05-27T22:29Z, 146 tools | NEWS-CMD-IMPL |
| NEWS-CMD-QA | **[qa] Live verify.** Confirm `/news` returns real `rag_analyses` content (not stub/N/A) in the running container — exercise the handler against live/seeded DB; verify plain-VN formatting (no jargon/impact numbers), `/news N` cap, chunking for over-limit input, empty-DB fallback wording. No test-baseline regression. Emit `qa-news-cmd-<UTC>.json`. NOT-RUN / stub-green FORBIDDEN. | MEDIUM | TASK | qa | docs/handoffs/TASK_NEWS-CMD.md | **DONE** — APPROVED 2026-05-27T20:50Z. T-NEWS-1..8 31/31 pass, 1406c 3/3, full suite 9873/0, tsc clean, live `/news` confirmed on 174 today-rows | NEWS-CMD-DEPLOY |
| NEWS-CMD-FIX | fixer cycle (only if QA CHANGES_REQUESTED — route to dev-mcp-server zone). | LOW | TASK | fixer | docs/handoffs/TASK_NEWS-CMD.md | N/A — pending QA verdict | NEWS-CMD-QA |
| NEWS-CMD-EXIT | **PO sign-off vs Scope + Success Metric.** Verify `/news` live + plain-VN + chunking + fallback. Goal stays ARMED until USER confirms it reads usefully. | MEDIUM | GATE | po | docs/handoffs/TASK_NEWS-CMD.md | **DONE** — PO SIGNED OFF 2026-05-27T20:52:50Z. Build deliverable complete; every machine-checkable Success-Metric AC MET (PO independently re-verified live in-container, not QA word alone). Sprint goal's USER-comprehensibility axis stays ARMED for verbal G9 (lane-c, human-judged). | NEWS-CMD-QA |
| NEWS-CMD-HTML-STRIP | **[backlog/LOW] Strip or escape HTML in `/news` story summaries.** Live `rag_analyses.summary` contains raw HTML fragments (`<a href=...>`, `<img ...>`) from the news ingestion pipeline (RSS/scraper preserves HTML). The `/news` handler renders `summary` as-is per `docs/REQ_NEWS-CMD.md` § 8 (spec-conformant, NOT a handler defect), so a non-technical user sees raw tags in the plain-text Telegram message. Fix: strip/escape HTML before display (render-time strip in `telegramCommands.ts handleNews`, OR upstream sanitise at ingestion in `apps/news-fetch/`). Owner + zone decided at triage. Non-blocking — did NOT gate NEWS-CMD-EXIT. | LOW | TASK | dev-mcp-server | docs/handoffs/TASK_NEWS-FULLDAY.md | **FOLDED INTO Sprint NEWS-FULLDAY 2026-05-27T21:41:17Z** — same display path as the `/news` refinement; HTML-strip is now DoD item (3)/scope of NEWS-FD-BA. Tracked there, not separately. | — |

**Notes:**
- **NEWS-CMD — PO SIGN-OFF (NEWS-CMD-EXIT) 2026-05-27T20:52:50Z:** APPROVED. Build deliverable complete. PO independently re-verified LIVE in-container (not QA word alone, per false-green discipline): mcp-server healthy, built 2026-05-27T22:29Z, 146 tools; invoked the real `handleTelegramCommand` router against live `market.db` (4458 rows, 8 today since VN-midnight): `/news` → `Tin tức hôm nay (8 bài):`, sentiment rendered as words (`trung tính` etc.), zero forbidden jargon (no impact_score / 0.NN / σ / bp / Layer # / hexagram), each chunk ≤4096. `/news N` cap works. HELP_TEXT carries `/news [N]` (L77); empty-fallback `Chưa có tin hôm nay.` (L563/568) + `Tin tức gần đây` fallback header (L572) present in live source — both QA-attested (T-NEWS-3, T-NEWS-8). Maps to SPRINT_GOAL.md § NEWS-CMD Success Metric: MET on every machine-checkable axis. **Goal stays ARMED only on the subjective USER-comprehensibility axis (verbal G9)** — that axis is human-judged forever (`feedback_market_report_plain_vietnamese`, SELF-IMPROVE-GATE lane-c). **HTML-in-summary decision:** I independently reproduced the QA note — live output DOES contain raw `<a href=...>` tags from ingested news. This is spec-conformant (REQ § 8 renders `summary` as-is) and a pre-existing ingestion data-quality issue, NOT a handler defect, so it did NOT block sign-off. Decision = **(b) backlog** a LOW follow-up (NEWS-CMD-HTML-STRIP) to strip/escape HTML before display, because a non-technical user seeing raw tags hits exactly the comprehensibility axis the product cares about. Owner/zone decided at triage.
- **NEWS-CMD — PO SPEC-GATE 2026-05-27T20:01:02Z:** `docs/REQ_NEWS-CMD.md` APPROVED. BA confirmed all 4 kickoff handoff claims against live code + correctly added the `summary` column to the `/news` query (the "one-line gist" the Vision promises; absent from `newsFetchLiveHandler.ts`, present in `schema-news.ts`). PO made two product calls at the gate (§ 11 PO RULING): **B1 chunking — Option B REJECTED** (single-message cap violates the binding goal "get all content" + Hard Constraint 3 "no silent truncation"); architect must use the Option-A family (deliver all content via sequential multi-message), with only the exact single-zone implementation contract left to design. **B2 fallback — SETTLED** = most-recent N, no date window (a date window can return empty on a quiet weekend, defeating the purpose), header switches to "Tin tức gần đây" so the user knows the data isn't today. Architect's NEWS-CMD-DESIGN scope is now effectively one decision (the chunking contract). NEXT: architect.
- **NEWS-CMD — PO KICKOFF 2026-05-27T19:50:01Z:** Self-scoped from the verbatim user feature request. Three product decisions settled by PO (sync-not-async, rag_analyses-not-daily-folder, pull-reply-not-push) — BA/architect must NOT re-open these; they are scope, not design. Architect has exactly two deferred design points (chunking mechanism + fallback window). Channel-audit step (MARKET/WORK/BUG read via MCP gateway) NOT run this cycle — the gateway `call_tool` wrapper is unavailable in the PO subagent toolset; for an EXPLICIT user feature request the request IS the backlog, so the audit (whose purpose is self-initiated bug/UX discovery) is not a precondition. Flagged for main terminal to run the routine audit on the next cron tick.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push`; all on `main` (NO branches); do NOT touch any `pilot-status-*.json`; never ask user to run/deploy — spawn ops/dev; commit-mutex uncallable by subagents → leave files UNSTAGED, main terminal commits.

---

## Sprint PEK-INTEGRATE — Re-engine apps/pdf-extractor on PDF-Extract-Kit (CPU-only, 8GB-safe)

**Status:** Round 6 RENDER-SEAM — **PO DELIBERATION GATE PASSED 2026-05-27T21:00:06Z: APPROVE-WITH-CONDITIONS** (C-1 visible stale banner, C-2 all-12 has_pek check, C-3 window-bounded re-extract). PEK-RENDER-MCP + PEK-RENDER-PDFX READY; dev chain dispatched. PEK-EXIT/PEK-MULTIPAGE-EXIT/PEK-RENDER-EXIT close is **BLOCKED** — do NOT sign off until FPT renders FRESH data + all-12 check + USER verbal G9. **NEW ROOT CAUSE (CODE-PROVEN by main terminal, do NOT re-litigate):** the user's bug ("OCR Text render always old data FPT page 3 and 5, no change after all fix") is a **DUAL-PATH RENDER DRIFT** — the bctc-inspector OCR Text panel (`GET /api/bctc-inspect/ocr/{doc_id}` → `bctcInspectHandler.ts:19,380`) reads the OLD-pipeline table `pdf_extracted_text` (filename-keyed); the structured-table panel reads `bctc_table_rows`; ONLY the ZONES panel reads the NEW PEK table `bctc_page_zones`. PEK writes ONLY `bctc_layout_units` + `bctc_page_zones` (via `pushBctcLayoutHandler.ts`) — NOTHING the OCR Text / table panels read. **So even a perfect PEK extraction can NEVER change the OCR Text render — this is the seam to fix.** COMPOUNDING: the re-extract trigger returned HTTP 422 — `PekExtractRequestSchema` (`apps/pdf-extractor/interface/handlers.py:142-155`) requires BOTH `report_id` AND `pdf_path` (both mandatory, no Optional); the backfill driver POSTed `{"report_id":...}` only → PEK never re-ran on FPT or 9 others. CORPUS: only 2/12 have PEK units (DGC 6, DIG 11); FPT `e71f845d` (SENTINEL) + 9 others = 0 PEK units; FPT old-path data dated 2026-05-26. The prior PEK-MULTIPAGE backend grouping fix (`2e228f0d`+`ed347661`) was adjudicated CORRECT (content present in `bctc_layout_units`) but addressed only the BACKEND half — it can never surface to the user through the wrong panels. — OPENED 2026-05-26T20:37Z by PO (kickoff from EXPLICIT USER DIRECTIVE — full autonomy). REQ-PEK-12 (OCR-backend pluggability) FORMALIZED + MET. **Priority: HIGH — reliability tier; candidate fix for the KNOWN-BROKEN BCTC table path (BCTC-TABLE-3: live 94 junk + 44 orphan of 150 rows).** Goal SSOT: `docs/SPRINT_GOAL.md § Sprint PEK-INTEGRATE`. **Architect-led** integration (user picked "Architect designs it" over direct in-place rewrite). **Zone: now `multi`** — render-seam repoint = `apps/mcp-server/` (dev-mcp-server); trigger/endpoint + PEK SSOT-table-population = `apps/pdf-extractor/` (dev-pdf-extractor); architect writes ONLY `docs/architecture-briefs/` and decides the exact split. NOT a scale pilot — does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`.

**Clone state (DONE by router — DO NOT re-clone):** pristine upstream at `apps/pdf-extractor/PDF-Extract-Kit` (89MB, depth-1, own `.git`). **PRISTINE — DO NOT MODIFY A SINGLE LINE** (published upstream; library/engine only).

**SUPERSESSION (binding):** SUPERSEDES the ENGINE layer of Sprint BCTC-LAYOUT-FIRST (`LF-EXTRACT`); PRESERVES + absorbs its UX layer (`LF-OVERLAY`, engine-agnostic). `LF-EXTRACT`/`LF-DEPLOY`/`LF-QA` PAUSED pending PEK architect brief. `text_table_extractor.py` structured `bctc_table_rows` path stays 0-byte-diff/unregressed.

**HARD CONSTRAINTS (gate the architecture):** (1) CPU-only — `requirements-cpu.txt` start point but TRIM further: drop `unimernet` (~1.4GB, OUT), choose table model (TableMaster light vs StructEqTable 1B-VLM heavy — `configs/table_parsing.yaml` defaults to StructEqTable = biggest RAM risk). (2) 8GB Docker cap HARD ceiling — kernel-panic history (`project_host_memory_panic`); architect produces explicit per-option RAM budget. (3) Keep FastAPI `/api` PULL contract intact (mcp-server pulls extractions).

**ARCHITECT MUST DECIDE (each w/ RAM number):** (a) trimmed task set = layout_detection + table_parsing + ocr ONLY, + table-model pick; (b) in-process vs on-demand torn-down worker container; (c) clone embedding (submodule/vendored-gitignored/pip-from-path) + Docker hygiene (current Dockerfile `COPY . .` + `.dockerignore` does NOT exclude `PDF-Extract-Kit/` → WILL bloat image; + model-weight cache location); (d) lazy model load + per-process RSS caps.

**DoD (scale-pilot bar, `feedback_scale_pilot_done_bar` — do NOT close early):** (1) live BCTC rows clean across multi-doc corpus via DIRECT market.db query (endpoint stale-capable, never arbiter; NOT-RUN ≠ green); (2) fleet within 8GB / NO kernel panic under load (ops verifies resident + peak RSS); (3) `/api` PULL unbroken + `bctc_table_rows` unregressed; (4) ZERO lines of `PDF-Extract-Kit/` modified (git-diff proof); (5) ops REBUILD (not restart) after dev change (`feedback_rebuild_after_dev_change`); (6) USER verbal G9.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| PEK-BA | **[BA] Requirement spec.** Decompose `docs/SPRINT_GOAL.md § Sprint PEK-INTEGRATE` into a `docs/REQ_PEK-INTEGRATE.md` spec: testable acceptance criteria for the engine wiring + the `/api` contract-preservation + the pristine-repo-untouched invariant + the CPU-only / 8GB-RAM-budget / no-kernel-panic constraints + the trimmed-task-set + the LF-OVERLAY-reuse note + the structured-`bctc_table_rows`-unregressed clause. Flag the 4 architect-open decisions (a)-(d) as architect-deferred (do NOT pre-decide RAM topology — that's the architect's call). Return spec to PO for the approval gate. | HIGH | TASK | ba | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (committed `22bc3d54`; PO-APPROVED 2026-05-26T20:49:25Z) | — |
| PEK-DESIGN | **[architect] Integration blueprint (DESIGN ONLY).** Resolve decisions (a)-(d) WITH a per-option RAM budget (model RSS + inference peak + FastAPI base + fleet headroom, vs the 8GB cap); pick the table-recognition model + the trimmed task set; decide in-process vs on-demand worker topology; decide clone-embedding + Docker `.dockerignore`/`.gitignore` hygiene + model-weight cache location; specify lazy-load + per-process RSS caps; reconcile with the preserved LF-OVERLAY zone-render contract (reuse `2026-05-26-bctc-layout-first-pipeline.md` §3 for PEK layout bboxes); confirm `/api` contract preserved + `text_table_extractor.py` `bctc_table_rows` zero-collision; specify per-task dev ACs; decide whether engine REPLACES or WRAPS the hand-built tiers. Brief → `docs/architecture-briefs/`. | HIGH | TASK | architect | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (brief `docs/architecture-briefs/2026-05-26-pek-integrate-design.md`, 2026-05-26T21:00Z) | — |
| PEK-IMPL | **[dev-pdf-extractor] Wire PDF-Extract-Kit as the table-extraction engine.** Per architect blueprint: integrate the engine (pristine, NEVER edited — zero-diff, source present for editable install) behind the existing FastAPI `/api`; trimmed task set (layout+ocr, PaddleOCR PP-StructureV2 table mode directly — NO TableMaster/StructEqTable, R-CRIT-2/3); CPU-only deps; lazy model load + per-process RSS cap; market-hours guard (HTTP 503 during 02:00–08:59 UTC Mon–Fri + cron `CRON_BCTC_REPARSE_JOB=0 21 * * *`, REQ-PEK-11/AC-PEK-NEW-1/2); Docker hygiene (`.dockerignore` excludes `PDF-Extract-Kit/.git/`+`models/`, `.gitignore` `pek_models/` + weight cache named volume, image < 2GB); keep `bctc_table_rows` path unregressed; import guard NEVER `TableParsingTask`/`FormulaDetectionTask`; unit/sandbox tests green; update `docs/architecture/microservice/pdf-extractor/`. Do NOT touch `PDF-Extract-Kit/` or frozen surfaces (`text_table_extractor.py`, sandbox/runner.py, dashboard trust-contract). | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (commit `efd23447`; PEK-DEP-RECONCILE escalation triggered by QA fail) | — |
| PEK-DEP-RECONCILE | **[dev-pdf-extractor] Numpy-2 ABI coherence fix.** Architect rethink (≥2 crashes on same module). Three edits only: (1) `requirements-pek.txt` — pin `numpy>=2.0.0,<2.3.0` + `opencv-python==4.12.0.88` + `ultralytics>=8.3.10`; (2) `Dockerfile` — pin `torch==2.5.1 torchvision==0.20.1` in CPU build step + add smoke gate `RUN python3 -c "import numpy, cv2, paddleocr; from doclayout_yolo import YOLOv10; print('pek-native-imports: ALL OK')"` immediately before EXPOSE; (3) `scenarios/pek_single_doc_extraction.py` line ~534 — change `patch("infrastructure.pek_engine_adapter.convert_from_path")` to `patch("pdf2image.convert_from_path")`. Brief: `docs/architecture-briefs/2026-05-27-pek-dependency-reconcile.md`. Do NOT touch PDF-Extract-Kit/ or frozen surfaces. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (commit `9ab93889`; numpy ABI fixed — new crash unimernet triggers PEK-IMPORT-CHAIN escalation) | — |
| PEK-IMPORT-CHAIN | **[dev-pdf-extractor] Bypass `pdf_extract_kit.tasks` entirely — fix unimernet eager-import crash.** Round 3 escalation (architect mandatory). Two file edits only, PEK subtree untouched. (1) `infrastructure/pek_engine_adapter.py` — add `_PekLayoutModel` class using `doclayout_yolo.YOLOv10` + `fitz` directly (bypasses `tasks/__init__.py` eager import chain); replace `LayoutDetectionTask`/`OCRTask` imports with `from doclayout_yolo import YOLOv10`; remove dead `OCRTask` block (never invoked); update return dict + `_run_extraction` + module docstring. (2) `Dockerfile` — replace smoke gate with corrected body that imports `pek_engine_adapter` module itself (not proxy symbols). Brief: `docs/architecture-briefs/2026-05-27-pek-import-chain.md`. Do NOT touch PDF-Extract-Kit/ or frozen surfaces. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PEK-INTEGRATE.md | **SUPERSEDED by PEK-OCR-ROOTCAUSE** (committed `6c124745`; _PekLayoutModel done; next defect discovered = _to_pil + lang=en; PEK-OCR-ROOTCAUSE replaces this in the ready queue) | — |
| PEK-OCR-ROOTCAUSE | **[dev-pdf-extractor] One-pass OCR fail-loud + _to_pil + lang=vi fix. Round 4 escalation (architect mandatory).** Three file edits only, PEK subtree untouched. (1) `infrastructure/ocr_backends.py` — add `_to_pil` module-level helper (numpy ndarray→PIL passthrough); remove bare `except Exception` swallows at lines 134 + 237 (replace with raises); fix `ImportError` catch to raise instead of return empty. (2) `infrastructure/pek_engine_adapter.py` — change `lang="en"` to `lang="vi"` at line 316 (PaddleOCR model for Vietnamese BCTC); annotate per-crop isolation catches at lines 1006+1019. (3) `__tests__/test_ocr_backends.py` — add 5 tests that exercise real numpy ndarray input shape through `recognize_text()` (Tests A-E per brief §7). One-pass audit grep FIRST (§8 in brief) before implementing. Brief: `docs/architecture-briefs/2026-05-27-pek-ocr-rootcause.md`. Do NOT touch PDF-Extract-Kit/ or frozen surfaces. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (commit 8535b175 — QA verified GREEN) | — |
| PEK-DEPLOY | **[ops] REBUILD + force-recreate + host-safe single-doc re-extract.** `docker compose build pdf-extractor` then `up -d --no-deps --force-recreate` (restart relaunches stale image); re-extract STRICTLY SEQUENTIAL single-doc (NEVER batch sweep — kernel panic); capture resident + peak RSS during a real extraction with the fleet running; confirm under 8GB / no panic. | HIGH | TASK | ops | docs/handoffs/TASK_PEK-INTEGRATE.md | BLOCKED | PEK-IMPL |
| PEK-QA | **[qa] Live clean-rows + RAM/no-panic gate.** Verify via DIRECT market.db query (`docker compose exec -T mcp-server bun -e` + `bun:sqlite`; endpoint CAN be stale — never arbiter): BCTC rows clean across the multi-doc corpus (measured pass-rate, not one doc); `/api` PULL end-to-end unbroken; `bctc_table_rows` unregressed; ZERO `PDF-Extract-Kit/` lines modified (git-diff proof); no test-baseline regression; RAM within 8GB + no kernel panic (cross-check ops RSS capture). Emit `qa-pek-integrate-<UTC>.json`. NOT-RUN panels + fixture-green alone FORBIDDEN. | HIGH | TASK | qa | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (GREEN — 12/12 PASS, commit 8535b175, image 439d42948589; report `reports/TASK_REPORT_PEK-QA.md`) | PEK-DEPLOY |
| PEK-FIX | fixer cycle (only if QA CHANGES_REQUESTED — route to dev-pdf-extractor zone). | MEDIUM | TASK | fixer | docs/handoffs/TASK_PEK-INTEGRATE.md | N/A — QA APPROVED, no fixes needed | PEK-QA |
| PEK-EXIT | **PO sign-off vs Scope + Success Metric.** Main terminal independently re-verifies LIVE (multi-doc clean rows via direct DB + RAM under 8GB + pristine repo untouched + `/api` intact); main terminal commits in-tree work. Goal stays ARMED until USER verbal G9. | HIGH | GATE | po | docs/handoffs/TASK_PEK-INTEGRATE.md | **BLOCKED — REOPENED 2026-05-27T20:46Z** (G9 REJECTED AGAIN: render seam — OCR/table panels read OLD tables PEK never writes; prior sign-off VOIDED; do NOT close until PEK-RENDER-EXIT) | PEK-RENDER-QA |
| PEK-MULTIPAGE | **[dev-pdf-extractor] Rewrite `_group_bboxes_into_units` — multi-page financial statement grouping.** G9 REJECTED — page-coverage defect. Round 5 escalation (architect mandatory). ONE file, ONE function: rewrite `_group_bboxes_into_units` body in `apps/pdf-extractor/infrastructure/pek_engine_adapter.py`. New algorithm: consecutive table pages → single unit; prose page = unit boundary; cap at 8 consecutive table pages; eliminate double-finalize ghost-unit bug. Signature unchanged. 5 new unit tests in `__tests__/test_pek_engine_adapter.py`. Brief: `docs/architecture-briefs/2026-05-27-pek-multipage.md`. FROZEN: text_table_extractor.py, sandbox/runner.py, pilot-status.json, generic_md_table_extractor.py, PDF-Extract-Kit/ subtree. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** (commits `2e228f0d` grouping + `ed347661` row_count; backend grouping ADJUDICATED CORRECT — `bctc_layout_units` content present, FPT pages 7/8/9 → ONE unit md_len=2903; but backend-only, never surfaces through OCR/table panels → Round 6 PEK-RENDER) | PEK-EXIT |

---

### Round 6 — RENDER-SEAM (the user-visible bug — ARCHITECT-FIRST, `feedback_recurring_bug_escalation`)

**Why Round 6 exists (CODE-PROVEN, do NOT re-litigate):** 6 fix commits on the PEK pipeline (`9ab93889`→`ed347661`) drove the BACKEND extraction green (PEK writes clean multi-page units to `bctc_layout_units`). But the user STILL sees "old data FPT page 3 and 5" because the inspector's OCR Text + structured-table panels NEVER read the PEK tables — they read OLD-pipeline `pdf_extracted_text` + `bctc_table_rows`. Plus the re-extract trigger 422'd (mandatory `pdf_path` missing) so PEK never re-ran on FPT. This is a NEW class (render seam + trigger), not another OCR-quality patch — architect designs the SSOT-unification root-cause fix BEFORE any new code.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| PEK-RENDER-DESIGN | **[architect] Root-cause brief — dual-path render seam + 422 trigger.** Decide the SSOT for the bctc-inspector's OCR Text + structured-table render — ONE unified path, fail-loud, NO dual-path: either (a) repoint `bctcInspectHandler.ts` OCR/table panels to read PEK `bctc_layout_units.stitched_markdown` (+ `bctc_page_zones`), OR (b) have the PEK pipeline ALSO populate the tables the inspector reads (`pdf_extracted_text` / `bctc_table_rows`). Justify the pick (freshness, fail-loud, least dead-data). ALSO spec the 422 re-extract-trigger fix: the driver/endpoint contract must carry `pdf_path` (look up from `financial_reports.pdf_path` server-side, or make the backfill driver send it) so PEK actually re-runs on all 12. Set the exact zone split (render = `apps/mcp-server/`; trigger/endpoint + any PEK SSOT-table write = `apps/pdf-extractor/`). Confirm zero collision with `text_table_extractor.py` `bctc_table_rows` write path + LF-OVERLAY zones contract. Brief → `docs/architecture-briefs/`. DESIGN ONLY — zero code. Return to PO. | HIGH | TASK | architect | docs/handoffs/TASK_PEK-INTEGRATE.md | **DONE** — brief `docs/architecture-briefs/2026-05-27-pek-render-seam.md`. Decision: Option A (repoint inspector to PEK tables). Trigger fix: new `POST /api/trigger-pek-extract` on mcp-server does pdf_path lookup then calls /pek-extract. dev-pdf-extractor has ZERO code changes for this. | PEK-MULTIPAGE (done) |
| PEK-RENDER-MCP | **[dev-mcp-server] Repoint inspector render to the SSOT per brief.** Option A: extend `handleBctcInspectOcr` + `handleBctcInspectTable` in `bctcInspectHandler.ts` to prioritize `bctc_layout_units` for PEK-processed reports (emit `has_pek: true/false`, `pek_coverage_gap`; retain old-path fallback behind `has_pek: false`). Add `POST /api/trigger-pek-extract` route to `server.ts` (looks up `financial_reports.pdf_path`, calls pdf-extractor `/pek-extract`). Update `bctc-inspector.html` panels to branch on `has_pek`. New tests for all 3 changes. **C-1 (PO gate, HARD):** `bctc-inspector.html` MUST render a VISIBLE banner (not a hidden JSON field) when `has_pek:false` ("Dữ liệu cũ — chưa qua PEK") AND a distinct visible marker on `pek_coverage_gap:true` — mirror the existing `ocr-sync-note` pattern (`bctc-inspector.html:770-778`). A non-technical user must never mistake old for fresh by looking at the panel. Brief: `docs/architecture-briefs/2026-05-27-pek-render-seam.md`. Zone: `apps/mcp-server/` only. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_PEK-INTEGRATE.md | **READY — PO-APPROVED-WITH-CONDITIONS 2026-05-27T21:00:06Z (C-1)** | PEK-RENDER-DESIGN (done) |
| PEK-RENDER-PDFX | **[dev-pdf-extractor] No code change required.** The 422 trigger fix is owned by mcp-server (new trigger endpoint). `PekExtractRequestSchema` stays unchanged (pdf_path mandatory is correct). PEK subtree pristine; frozen surfaces 0-diff. Zone: `apps/pdf-extractor/` — NO FILES CHANGED. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PEK-INTEGRATE.md | **READY — PO-APPROVED 2026-05-27T21:00:06Z (zero-change task — verify frozen surfaces only)** | PEK-RENDER-DESIGN (done) |
| PEK-RENDER-DEPLOY | **[ops] REBUILD mcp-server + re-extract the corpus.** `docker compose build mcp-server` then `up -d --no-deps --force-recreate mcp-server` (NOT restart; do NOT force-recreate mid-extraction). Then re-extract the 10 non-VCB corpus reports SEQUENTIALLY via `POST /api/trigger-pek-extract` {report_id}. **C-3 (PO gate, HARD):** STRICTLY within an off-hours window (between 09:00 UTC market-close and the next 02:00 UTC open); if corpus doesn't finish in one window, STOP before 02:00 UTC and RESUME the next off-hours window — the 503 is a backstop, NOT the scheduler. Capture resident + peak RSS during a real extraction with the fleet running; confirm < 8 GB / no panic. Verify each report landed via DIRECT in-container market.db COUNT (`docker exec ...mcp-server-1 bun -e "...readonly..."`), NOT push-handler echo (false-success). | HIGH | TASK | ops | docs/handoffs/TASK_PEK-INTEGRATE.md | BLOCKED | PEK-RENDER-MCP, PEK-RENDER-PDFX |
| PEK-RENDER-QA | **[qa] THE acceptance test for THIS bug.** Open `/api/bctc-inspect` for FPT `e71f845d`; confirm the OCR Text + table panels for the financial-statement pages render the FRESH multi-page PEK extraction (NOT 2026-05-26 data) with `has_pek:true`. **C-1 verify (HARD):** force a `has_pek:false` response (a non-PEK report) and confirm the VISIBLE stale banner renders — JSON-flag-only green is REJECTED. **C-2 verify (HARD):** check `has_pek:true` + fresh `extracted_at` (≥2026-05-27) for ALL 12 corpus reports via DIRECT market.db COUNT (or documented `pdf_path IS NULL` 404 for the 2 VCB rows) — FPT-only pass REJECTED; per-report PASS/FAIL table. PLUS carry-over: clean rows, code 100/270 present, balance passes, diacritics intact, FPT multi-page grouped into ONE unit. Run the PEK-MULTIPAGE 4-gate (md_len-based Gate B per QA-ADJUDICATE) across the corpus via DIRECT market.db COUNT. NOT-RUN / stale-data / push-echo green FORBIDDEN. Emit `qa-pek-render-<UTC>.json`. | HIGH | TASK | qa | docs/handoffs/TASK_PEK-INTEGRATE.md | BLOCKED | PEK-RENDER-DEPLOY |
| PEK-RENDER-FIX | fixer cycle (only if QA CHANGES_REQUESTED — route to the failing zone: dev-mcp-server or dev-pdf-extractor). | MEDIUM | TASK | fixer | docs/handoffs/TASK_PEK-INTEGRATE.md | N/A — pending QA verdict | PEK-RENDER-QA |
| PEK-RENDER-EXIT | **PO sign-off vs render-seam acceptance.** NOT done until FPT OCR Text render shows FRESH data AND user signs verbal G9. Then re-seek user G9. | HIGH | GATE | po | docs/handoffs/TASK_PEK-INTEGRATE.md | BLOCKED | PEK-RENDER-QA |

**Notes — PEK-INTEGRATE Round 6 (PO DELIBERATION GATE 2026-05-27T21:00:06Z):** APPROVE-WITH-CONDITIONS on the PEK-RENDER-DESIGN brief (`docs/architecture-briefs/2026-05-27-pek-render-seam.md`). Option A (repoint inspector readers to the PEK SSOT tables) is the correct root-cause fix — Option B would perpetuate the dual-write drift that IS the bug; write paths untouched (`bctc_table_rows` non-regression is structural); `/api/trigger-pek-extract` grep-confirmed absent; 503 market-hours guard live at `handlers.py:403`; `CRON_BCTC_REPARSE_JOB=0 21 * * *` intact. Architecture half does NOT re-spin. Three HARD conditions attached (full critique + verdict in `docs/handoffs/TASK_PEK-INTEGRATE.md` PO deliberation-gate record, written BEFORE the verdict per `feedback_fence_false_green`): **C-1** (dev-mcp-server+qa) — `bctc-inspector.html` MUST show a VISIBLE stale banner on `has_pek:false`, not just the JSON flag (the false-green seam: the original "old data" bug recurs the moment a re-extract is incomplete and the panel silently renders stale `text_content`); **C-2** (qa) — verify `has_pek:true`+fresh `extracted_at` for ALL 12 corpus reports (or documented `pdf_path IS NULL` 404 for the 2 VCB rows), NOT FPT alone (gameability: a partial re-extract greens FPT while others serve stale data); **C-3** (ops) — window-bounded SEQUENTIAL re-extract of the 10 non-VCB reports inside the off-hours window (09:00 UTC close → next 02:00 UTC open), STOP+RESUME across windows, 503 is a backstop not the scheduler, RSS captured < 8GB. NEXT: dev-mcp-server (PEK-RENDER-MCP) + dev-pdf-extractor (PEK-RENDER-PDFX verify-only) in parallel.

**Notes — PEK-INTEGRATE Round 6 (PO BLOCK 2026-05-27T20:46:28Z):** G9 REJECTED a second time. Per `feedback_recurring_bug_escalation` (6 fix commits on the same module + "fix didn't take"), NO new patch authorized before the architect root-causes the render seam. Main terminal CODE-PROVED both defects read-only (render seam in `bctcInspectHandler.ts:19,380` reading `pdf_extracted_text`; 422 from `PekExtractRequestSchema` mandatory `pdf_path` at `handlers.py:142-155`) — architect designs the fix, does NOT re-derive. Zone flips to `multi` (render=mcp-server, trigger=pdf-extractor). HARD CONSTRAINTS unchanged: PDF-Extract-Kit/ pristine (`git -C apps/pdf-extractor/PDF-Extract-Kit diff` EMPTY); scoped per-file `git add`, never `-A`; CPU-only / 8GB cap; FROZEN unless architect explicitly reopens: text_table_extractor.py, sandbox/runner.py, pilot-status-pdf-extractor.json, generic_md_table_extractor.py; all on main, no branches; re-extract STRICTLY outside HOSE hours; DB verify = direct in-container `bun -e` readonly COUNT, never push-handler echo.

---

## Sprint CHEF-ATTN — Bootstrap attention diversity cap (pre-redesign targeted fix)

**Status:** READY — OPENED 2026-05-27T19:25:01Z by PO (self-initiated scoping verdict; full autonomy). **Priority: MEDIUM — UX/coverage tier.** Diagnosis-driven: main terminal traced + verified the code path (`getCycleBootstrap → marketContextBuilder`). NOT a scale pilot. **Zone: `apps/mcp-server/`** (dev-mcp-server = sole code owner). Does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`.

**Why this exists (scope verdict):** The cowork daily-document redesign (`docs/architecture-briefs/2026-05-27-cowork-team-daily-document-redesign.md` Design Point A + F25) STRUCTURALLY fixes CHEF attention-starvation by rewiring CHEF's GATHER to read the full daily folder instead of the 20-row alert window. BUT that rewire lands near the END of a multi-week 3-phase migration (5-day + 10-day QA gates). Meanwhile `get_cycle_bootstrap(unified-agent)` — observed live by the owner today — keeps surfacing only ~7 news-heavy large-caps because `buildAlertsSection`'s recency-only `LIMIT 20` window is monopolized by high-frequency `news_mention` alerts on big-caps (aggravated when the VN market is CLOSED: price/technical scanners are market-hours only; news flows 24/7). A small per-stock diversity cap improves the live product immediately for every consumer of the bootstrap (CHEF AND the direct user view) across the entire migration window. **The signal-bus `'all'`-routing half of the original proposal is REJECTED** — it would inject churn into the signal-bus model the redesign is deprecating in Phase 3.

**Verified code locations (do NOT re-derive):**
- `apps/mcp-server/src/domain/services/marketContextBuilder.ts` `buildAlertsSection` (lines 269-305): `WHERE triggered_at >= ? AND read = 0 ORDER BY triggered_at DESC LIMIT 20` — pure recency, no diversity cap. This is the fix site.
- `buildWatchlistSection` (lines 149-195): full coverage, no LIMIT — NOT the problem, do NOT touch.
- `buildAnalysisSection` (lines 307-350): `ORDER BY impact_score DESC LIMIT 10` — secondary clustering; in-scope for the same diversity treatment if cheap, otherwise leave (alerts section is the primary offender).
- Consumed by `apps/mcp-server/src/application/usecases/getCycleBootstrap.ts` via `buildMarketContextText`.

**DoD:** (1) `buildAlertsSection` applies a per-stock (and/or per-signal-type) diversity cap so no single ticker / `news_mention` cluster can occupy all 20 rows — e.g. cap N rows per `affected_actions_json` code, fill remaining slots with the most-recent distinct tickers; total still ≤20, still recency-ordered within the diversified set. Exact cap value is the architect/dev call — default suggestion: max 3 rows per ticker. (2) Existing unit tests for `marketContextBuilder` stay green + a new test proves the cap (inject 20 `news_mention` alerts on 2 tickers → output shows ≤cap each + other watchlist tickers' alerts surface). (3) No test-baseline regression (floor 9408 PASS / ceiling 348 FAIL per `project-stats.json`). (4) ops REBUILD (not restart) mcp-server after the change (`feedback_rebuild_after_dev_change`). (5) The pre-redesign fix is harmless-on-top-of redesign: it only diversifies the bootstrap string; when F25 lands and CHEF stops reading this path, the cap simply continues to benefit the direct-user bootstrap view.

| Task ID | Title | Priority | Type | Owner | Status | Blocked by |
|---------|-------|----------|------|-------|--------|-----------|
| CHEF-ATTN-BA | **[BA] Spec the diversity cap.** Decompose this section into `docs/REQ_CHEF-ATTN.md`: testable ACs for the per-stock (and optional per-signal-type) cap on `buildAlertsSection`, the new unit test, the no-regression clause, the REBUILD-not-restart clause, and the explicit out-of-scope list (watchlist section untouched; NO signal-bus `'all'` routing change). Return to PO approval gate. | MEDIUM | TASK | ba | READY | — |
| CHEF-ATTN-IMPL | **[dev-mcp-server] Implement the diversity cap** in `buildAlertsSection` (+ `buildAnalysisSection` if cheap) per approved spec. Add the cap unit test. Keep watchlist + macro sections untouched. Update `docs/architecture/microservice/mcp-server/` if the section-builder contract is documented there. | MEDIUM | TASK | dev-mcp-server | BLOCKED | CHEF-ATTN-BA |
| CHEF-ATTN-DEPLOY | **[ops] REBUILD + force-recreate mcp-server** after dev change (`docker compose build mcp-server` then `up -d --no-deps --force-recreate`); confirm `get_cycle_bootstrap(unified-agent)` OPEN ALERTS section shows diversified tickers. | MEDIUM | TASK | ops | BLOCKED | CHEF-ATTN-IMPL |
| CHEF-ATTN-QA | **[qa] Verify** the live bootstrap diversifies (no single ticker monopolizes OPEN ALERTS), watchlist coverage unregressed, test baseline held. Emit verdict. | MEDIUM | TASK | qa | BLOCKED | CHEF-ATTN-DEPLOY |
| CHEF-ATTN-EXIT | **PO sign-off** vs DoD. | MEDIUM | GATE | po | BLOCKED | CHEF-ATTN-QA |

**Notes:**
- **CHEF-ATTN — PO SCOPE VERDICT 2026-05-27T19:25:01Z:** BOTH actions taken from the routed finding. (A) Recorded as motivation/validation in the redesign brief backlog (`docs/architecture-briefs/2026-05-27-cowork-team-daily-document-redesign.md § Backlog — Validating Findings`). (B) Greenlit this small targeted pre-redesign fix — alerts-section diversity cap ONLY. Rejected the signal-bus `'all'`-routing half (would churn a soon-deprecated model). Code path independently re-verified by main terminal at routing time (`buildAlertsSection` LIMIT 20 recency-only; signals route to `alert-commander` not `'all'` at `market-watcher/flow/cycle.md:64` + `news-scout/flow/stage-signals.md:74,104`). NEXT: BA spec (`CHEF-ATTN-BA`) → PO approval gate.

---

**PEK Notes:**
- **PEK-EXIT — PO SIGN-OFF 2026-05-27T14:04:39Z (done-pending-G9):** QA GREEN accepted (12/12 corpus PASS, 0 FAIL; FPT Q4 2025 sentinel `e71f845d` = 23/23 non-empty, Vietnamese diacritics confirmed, code 270 current + prior period non-null, all 5 BCTC-TABLE-3 mode checks PASS; TS 9810/9810, Python 26/26; report `reports/TASK_REPORT_PEK-QA.md`). **REQ-PEK-12 (OCR-backend pluggability) FORMALIZED + recorded MET** in `docs/REQ_PEK-INTEGRATE.md` — cell/line TEXT recognition pluggable via `OcrBackendPort` (`domain/repositories.py:163`), env-driven via `OCR_TEXT_BACKEND` ∈ {tesseract-vie (default), paddleocr, auto} (`select_ocr_backend()` `infrastructure/ocr_backends.py:387`), injected at composition root (`main.py:120`); LAYOUT (DocLayout-YOLO) + TABLE-GRID (PP-StructureV2 table mode) NON-selectable per the port contract (`:184-186`). **Done-when criteria confirmed:** (1) OCR pluggable committed (`8535b175`) ✓; (2) container REBUILT not restarted — image `439d42948589` built 2026-05-27 10:26 UTC, weights runtime-only on named volume `pek_model_cache`, fleet RAM 3.4 GiB < 8 GB ✓; (3) qa clean BCTC rows via direct market.db + FPT sentinel ✓; (4) PEK subtree pristine (`git -C apps/pdf-extractor/PDF-Extract-Kit diff` EMPTY, re-verified at exit) + 503 market-hours guard holds (`handlers.py:403`, `CRON_BCTC_REPARSE_JOB=0 21 * * *`) ✓. **ONLY criterion (5) USER verbal G9 outstanding** — main terminal obtains it; PO does NOT block. PEK subtree left UNSTAGED/pristine; PO commits only doc changes with scoped per-file `git add`.
- **REQ AMENDED 2026-05-26T21:03:47Z (PO — resolves architect brief §8 R-CRIT-1 + §10 new ACs; PEK-IMPL was BLOCKED on these two PO-owned amendments, now UNBLOCKED):** `docs/REQ_PEK-INTEGRATE.md` re-stamped `status: APPROVED (AMENDED 2026-05-26T21:03:47Z)`. (1) **AC-PEK-3a REWRITTEN (R-CRIT-1):** prior literal "`ls /app/PDF-Extract-Kit` = No such file" CONTRADICTED the chosen `pip install -e ./PDF-Extract-Kit` editable install (source MUST be present in container). Pristine invariant now = **zero-diff, NOT absence**: (a) `.git/` excluded from image, (b) weights NEVER baked (image < 2GB via `docker image inspect`), (c) `git -C apps/pdf-extractor/PDF-Extract-Kit diff` EMPTY. User constraint "repo publish, dont touch" honored as zero-diff. (2) **REQ-PEK-11 APPENDED (AC-PEK-NEW-1 + AC-PEK-NEW-2, verbatim brief §10):** market-hours isolation — NEW-1 runtime HTTP 503 + no RSS rise at simulated VN market-open instant (Mon 03:00 UTC); NEW-2 cron not 02:00–08:59 UTC weekdays (`CRON_BCTC_REPARSE_JOB=0 21 * * *`). Done-Bar now SEVEN conditions (#6 = market-hours). Spec now 11 reqs, 37 ACs. (3) **R-CRIT-2 + R-CRIT-3 confirmed NOTED for dev-pdf-extractor (architect resolved in-brief, no REQ change):** R-CRIT-2 = no TableMaster in clone → use PaddleOCR PP-StructureV2 table mode directly; R-CRIT-3 = StructEqTable hard-asserts CUDA → import guard, NEVER `TableParsingTask`/`FormulaDetectionTask`, unit test asserts no CUDA import. **NEXT: dev-pdf-extractor (PEK-IMPL) against the amended spec.**
- **SPEC-APPROVAL GATE CLEARED 2026-05-26T20:49:25Z (PO):** `docs/REQ_PEK-INTEGRATE.md` (10 reqs, 35 ACs, commit `22bc3d54`) APPROVED — `status: APPROVED` set in the spec header. Verified: (1) all 4 architect-deferred decisions (a)-(d) remain OPEN — BA did NOT pre-answer RAM topology; decision (b) in-process vs on-demand worker is genuinely deferred with per-option RAM budget required (the biggest kernel-panic-risk variable); (2) hard constraints encoded as ACs — CPU-only (AC-PEK-2a/2d), 8GB RSS-under-load (AC-PEK-2b/2c/4c), `/api` PULL unbroken (AC-PEK-5a–5d), ZERO `PDF-Extract-Kit/` edits with git-diff proof (AC-PEK-0a–0c; live subtree diff confirmed empty); (3) scale-pilot done-bar held — direct market.db is arbiter (AC-PEK-7d, endpoint never the gate), FPT Q4 2025 sentinel anchor (AC-PEK-7e, report_id `e71f845d-...`, 4 sentinels cross-verified vs closed BCTC-TABLE record), ops REBUILD-not-restart (REQ-PEK-10), USER verbal G9; (4) REQ-PEK-8 reuses LF-OVERLAY §3 contract (`bctc_page_zones`/`bctc_layout_units` + `POST /api/push-bctc-layout` confirmed present in `2026-05-26-bctc-layout-first-pipeline.md` §3.1/§3.2), no duplicate-table reinvention (AC-PEK-8d). **PEK-DESIGN UNBLOCKED → architect.**
- **PO-verified ground truth (not assumed):** clone present (89MB, own `.git`, `requirements-cpu.txt` + 8 configs); `requirements-cpu.txt` STILL pulls `unimernet==0.2.1` + `struct-eqtable` (so the stock CPU file is NOT 8GB-safe as-is — architect trims further); `configs/table_parsing.yaml` defaults to `StructEqTable` (InternVL2-1B VLM — biggest RAM risk); current `apps/pdf-extractor/.dockerignore` does NOT exclude `PDF-Extract-Kit/` and Dockerfile does `COPY . .` → naive build copies the 89MB pristine repo + its `.git` into the image (the (c) decision must fix this).
- **Recurring-bug discipline:** the broken column-guessing path carries 9 MD-EXTRACT + 7 BT fix commits — PEK-DESIGN IS the architect root-cause rethink (`feedback_recurring_bug_escalation.md`); no more blind dev patches to the column-guesser.
- **Privacy guardrail preserved:** LOCAL-ONLY (PDF-Extract-Kit + models run fully on-host; no page-image off-machine, no cloud VLM). Compatible with the BCTC-LAYOUT-FIRST privacy clause.
- **Host/hardware guardrail:** SEQUENTIAL single-doc only; NEVER `run_bctc_batch_sweep`; lazy-load + RSS caps mandatory.
- **No false-greens:** 5 prior on this surface. Tier-3 verified by DIRECT market.db, not the endpoint; NOT-RUN panels not green; measured corpus pass-rate.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push`; all on `main` (NO branches); `git show --stat HEAD` zero foreign files; do NOT touch any `pilot-status-*.json`, any `PDF-Extract-Kit/` line, or the frozen pdf-extractor surfaces; never ask user to run/deploy — spawn ops/dev; commit-mutex uncallable by subagents → leave files UNSTAGED, main terminal commits.

---

## Sprint BCTC-LAYOUT-FIRST — Document-Structure-First Extraction + Geometric Zone Review Overlay

> **SUPERSESSION NOTICE (2026-05-26T20:37Z, PO):** ENGINE layer (`LF-EXTRACT`) SUPERSEDED by Sprint PEK-INTEGRATE (above). UX layer (`LF-OVERLAY`) PRESERVED + absorbed by PEK-INTEGRATE. `LF-EXTRACT`/`LF-DEPLOY`/`LF-QA` PAUSED pending PEK architect brief reconciliation. `bctc_table_rows` path untouched under both. Parallel-session owner: read PEK-INTEGRATE § "RELATIONSHIP TO Sprint BCTC-LAYOUT-FIRST" before resuming.

**Status:** OPEN 2026-05-26T18:07Z by PO (kickoff from a multi-round design brainstorm CO-AUTHORED WITH THE USER; user gave the go to build — full autonomy). **Priority: HIGH — reliability + UX tier; this is the architect-grade ROOT-CAUSE RETHINK mandated by the recurring-bug guard (`generic_md_table_extractor.py` = 9 MD-EXTRACT commits; `text_table_extractor.py` = 7 BT commits).** Goal SSOT: `docs/SPRINT_GOAL.md § Sprint BCTC-LAYOUT-FIRST`. Ladder + per-task ACs + constraints: `docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md`. **Zone: `multi` — architect MUST split at the pdf-extractor↔mcp-server boundary.** extraction + zone-geometry JSON = `apps/pdf-extractor/` (dev-pdf-extractor, sole owner; redesign target `apps/pdf-extractor/infrastructure/generic_md_table_extractor.py`) + `docs/architecture/microservice/pdf-extractor/`; inspector zone-overlay render = `apps/mcp-server/` (dev-mcp-server, sole market.db write-owner; target `apps/mcp-server/src/interface/mcp/routes/bctcInspectHandler.ts`); architect writes only `docs/architecture-briefs/`. NOT a scale pilot — does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`. SUPERSEDES Sprint BCTC-MD-TABLE (the bbox-clustering engine that passed FPT Q4 but does not generalize). Sprint BCTC-TABLE structured `bctc_table_rows` path stays CLOSED + UNTOUCHED (augment, not replace).

**Binding session goal (verbatim, ARMED + UNMET):** *"table on pdf on all bctc need correct extract text and convert to md style"* — GENERIC PDF table detection → markdown across ALL ~18 BCTC docs in the DB, not one test doc.

**TWO converged deliverables:** (1) **LAYOUT-FIRST / DOCUMENT-STRUCTURE-FIRST extraction** — Tier 0 document map (group consecutive pages into logical units by GEOMETRIC column-fingerprint; anchors hint-only; tag table|prose|blank; tolerate page gaps; cheap — reuse `pdf_extracted_text` + low-DPI projection profile) → Tier 1 per-page layout zoning with continuation pages INHERITING the schema-page column grid (the direct fix for the missing-header scramble) → Tier 2 OCR into the known grid + cross-page STITCH into ONE markdown table per unit → Tier 3 per-unit invariant gate (balance identity reconciles across the stitch, codes monotonic across page boundary, every data row has label + ≥1 value). (2) **GEOMETRIC ZONE REVIEW OVERLAY** — an ON/OFF toggle on `/api/bctc-inspect` rendering detected zones (column gutters, row bands, header/footer bands, logical-unit boundaries) over the page image; pdf-extractor emits zone-geometry JSON, mcp-server renders the toggle.

**ROOT CAUSE (binding):** current engine OCRs page-by-page then GUESSES columns by clustering number-token x-positions; on a continuation page with NO column header (FPT Q1 2026 page 5 / NGUỒN VỐN — pages 3,4,6 have "Mã số / Thuyết minh / Số cuối / Số đầu", page 5 + page-10 cash-flow do not) it has nothing to anchor → scramble. Tier-0/Tier-1 SCHEMA INHERITANCE is the direct fix. Anchors can't be the spine: in notes (p41) CÂN ĐỐI+KẾT QUẢ+LƯU CHUYỂN+THUYẾT MINH all matched → geometry must be the spine.

**PO-RESOLVED DECISIONS (binding, user co-authored):** **A — GENERIC by construction (AC-0):** geometry is the spine, ZERO hardcoded BCTC semantics in table/zone logic, anchors hint-only, grep-proof. **B — REPLACE the column-guessing engine inside `generic_md_table_extractor.py`; AUGMENT (do NOT touch) the structured `bctc_table_rows` path (`text_table_extractor.py` 0-byte-diff, SSOT for analyzable figures).** **C — Tier 0 stays CHEAP** (existing OCR text + low-DPI geometry; heavy OCR = one pass/page in Tier 2). **D — schema inheritance is the named fix.** **E — geometry-before-text review surface** (pdf-extractor emits JSON, mcp-server renders toggle; store-vs-compute = architect call). **F — DONE-BAR (G9):** Tier-3 invariants PASS across the multi-doc corpus verified by DIRECT market.db query (endpoint can be stale, NEVER the arbiter) AND user verbal sign-off; NOT-RUN panels not green; 5 prior false-greens.

**PRIVACY GUARDRAIL (non-negotiable):** self-hosted LOCAL ONLY (PIL/OpenCV/Tesseract). NO PDF/page-image off-machine, NO cloud VLM/OCR. External-API VLM deferred/opt-in, explicit consent. Heavy local CV (PP-Structure/Table-Transformer/img2table) = fallback only, must be fully local.

**HARDWARE/HOST GUARDRAIL:** 2018 Intel Mac, 16GB, no GPU, kernel-panics under swap, Docker 8GB cap. SEQUENTIAL single-doc OCR ONLY; NEVER `run_bctc_batch_sweep` / any batch backfill. Tier 0 cheap (no new heavy OCR).

**VALIDATION CORPUS (in market.db `pdf_extracted_text`, already OCR'd, 18 docs):** VCB/ACB/EIB/SHB (banks); FPT/HPG/DGC/GAS/BSR (industrials); DHG (pharma); VNM (consumer); DIG (real-estate); VEA. Only FPT Q4 2025 (row id=11) ever ran the bbox engine — generalization UNPROVEN. Root-cause anchor: FPT Q1 2026 (`20260424-FPT-BCTC-hop-nhat-Quy-1-nam-2026.pdf`, report_id `e8ea3df5-3f32-413d-a3eb-c71634c0438d`).

**DEPLOY:** pdf-extractor builds from BUILD-CONTEXT → ops `docker compose build pdf-extractor` then `up -d --no-deps --force-recreate` (restart relaunches stale image). mcp-server = SOLE market.db write-owner. sqlite3 NOT in containers → query via `docker compose exec -T mcp-server bun -e` + `require("bun:sqlite")`.

**DONE CONDITION:** Tier-3 invariant gate PASSES across the multi-doc corpus (DIRECT market.db query — endpoint can be stale, never the arbiter); zone overlay renders ON/OFF on LIVE `/api/bctc-inspect` from the SAME generic pipeline (AC-0 grep-clean); continuation-page scramble FIXED via schema inheritance; structured `bctc_table_rows` unregressed; zero off-machine data flow (grep/audit proof); main terminal independently re-verifies LIVE; USER verbal G9.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| LF-BA | **[BA] Requirement spec.** Decompose `docs/SPRINT_GOAL.md § Sprint BCTC-LAYOUT-FIRST` into a `docs/REQ_*.md` spec: testable acceptance criteria for both deliverables, the 4-tier behavior contract, the AC-0 generic/grep-proof clause, the privacy + host + multi-doc-corpus + DIRECT-DB-arbiter constraints, the service-boundary split note for architect. Return spec to PO for approval gate. | HIGH | TASK | ba | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | **DONE — PO APPROVED 2026-05-26T18:20Z** (`docs/REQ_BCTC-LAYOUT-FIRST.md`; faithful to Decisions A–F + privacy/host; 3 architect-open Qs correctly deferred) | — |
| LF-DESIGN | **[architect] Root-cause rethink + 4-tier blueprint (DESIGN ONLY).** Recurring-bug RCA (9+7 fix commits) → design the Tier 0-3 layout-first pipeline (document map by geometric fingerprint, schema inheritance, cross-page stitch, per-unit invariant gate); design zone-geometry JSON contract + the pdf-extractor↔mcp-server SERVICE-BOUNDARY SPLIT for the overlay (store-vs-compute call); confirm zero-collision with structured `bctc_table_rows` + 1954c write chain; resolve the 3 architect-open Qs (zone+md schema vs `bctc_table_rows`; exact boundary JSON contract; quarantined-unit storage for QA direct-DB count); specify per-task dev ACs incl. AC-0 grep-proof generality; keep Tier 0 cheap; brief → `docs/architecture-briefs/`. | HIGH | TASK | architect | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | **DONE 2026-05-26T19:30Z** — brief: `docs/architecture-briefs/2026-05-26-bctc-layout-first-pipeline.md`; 3 open Qs resolved; ACs written; LF-EXTRACT + LF-OVERLAY ready in parallel | LF-BA (PO-approved 18:20Z) |
| LF-EXTRACT | **[dev-pdf-extractor] Tier 0-3 layout-first engine + zone-geometry JSON (redesign `generic_md_table_extractor.py`).** Implement document map + page-layout zoning + schema inheritance + cross-page stitch + invariant gate per architect blueprint (`docs/architecture-briefs/2026-05-26-bctc-layout-first-pipeline.md` §2 + §4.1); emit zone-geometry JSON for the overlay; ZERO hardcoded BCTC semantics (geometry only, anchors hint-only); unit tests + sandbox green; local Tesseract/PIL/OpenCV only. Do NOT touch `text_table_extractor.py` (0-byte-diff) or other frozen surfaces. ACs: AC-LFE-0 through AC-LFE-11 in brief §4.1. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | **READY** (parallel with LF-OVERLAY — contract fully specified in brief §3.2) | LF-DESIGN |
| LF-OVERLAY | **[dev-mcp-server] Geometric-zone overlay toggle on /api/bctc-inspect.** Consume the zone-geometry JSON per contract in brief §3.2; add `bctc_layout_units` + `bctc_page_zones` DDL (brief §3.1); new `POST /api/push-bctc-layout` handler; render ON/OFF toggle layer drawing column gutters / row bands / header-footer bands / logical-unit boundaries over the page image; route 200/400 tests; do NOT touch structured `bctc_table_rows` read path (Decision B). Target `bctcInspectHandler.ts`. ACs: AC-LFO-0 through AC-LFO-7 in brief §4.2. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | **READY** (parallel with LF-EXTRACT — contract fully specified in brief §3.2) | LF-DESIGN |
| LF-DEPLOY | **[ops] Single-doc host-safe re-extract + rebuild/recreate.** `docker compose build pdf-extractor` + `up -d --no-deps --force-recreate` (+ mcp-server if overlay route changed); re-extract docs STRICTLY SEQUENTIALLY single-doc (NEVER the batch sweep — host kernel-panic); prove engine + overlay live. | HIGH | TASK | ops | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | BLOCKED | LF-EXTRACT, LF-OVERLAY |
| LF-QA | **[qa] Tier-3 invariant gate across the multi-doc corpus.** Verify via DIRECT market.db query (`docker compose exec -T mcp-server bun -e` + `bun:sqlite`; endpoint CAN be stale — never the arbiter): balance identity reconciles across cross-page stitch, codes monotonic across page boundary, every data row has label + ≥1 value — measured pass-rate over the 18-doc corpus, not one doc; continuation-page scramble (FPT Q1 p5) fixed; zone overlay renders ON/OFF live; AC-0 grep-proof (zero BCTC semantics in zone/grid logic); structured `bctc_table_rows` unregressed; no test-baseline regression; privacy audit. Emit `qa-bctc-layout-first-<UTC>.json`. NOT-RUN panels + fixture-green alone FORBIDDEN. | HIGH | TASK | qa | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | BLOCKED | LF-DEPLOY |
| LF-FIX | fixer cycle (only if QA CHANGES_REQUESTED — route per zone). | MEDIUM | TASK | fixer | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | BLOCKED | LF-QA |
| LF-EXIT | **PO sign-off vs Decisions A-F + Success Metric.** Main terminal independently re-verifies LIVE (multi-doc Tier-3 pass via direct DB + zone overlay ON/OFF + scramble fixed); main terminal commits in-tree work. Goal stays ARMED until USER verbal G9. | HIGH | GATE | po | docs/handoffs/TASK_BCTC-LAYOUT-FIRST.md | BLOCKED | LF-QA |

**Notes:**
- **[PM] Task Decomposition (LF-PM, 2026-05-26T19:45Z):** LF-EXTRACT (dev-pdf-extractor, ~2h, Tiers 0–3 + 3 test files) + LF-OVERLAY (dev-mcp-server, ~2h, schema + handler + overlay toggle) DISPATCHED PARALLEL (full JSON contract in brief §3.2 enables independent implementation; zero inter-agent code dependency). **Recommendation:** PARALLEL is host-safe (repo-source dev, no runtime container until LF-DEPLOY re-extract). LF-DEPLOY gates on BOTH done (sequential single-doc, never batch). See handoff § [PM] LF-PM for full decomposition + atomic task breakdown + WIP enforcement.
- **Recurring-bug discipline (binding):** `generic_md_table_extractor.py` carries 9 MD-EXTRACT commits + `text_table_extractor.py` 7 BT commits — LF-DESIGN IS the architect root-cause rethink per `feedback_recurring_bug_escalation.md`. No more blind dev patches to the column-guessing logic; the redesign replaces it.
- **Augment, not replace (Decision B):** the structured `bctc_table_rows` path (`text_table_extractor.py`) is the user-confirmed working surface feeding financial analysis — 0-byte-diff, must not regress. Layout-first markdown is the human-recheck layer.
- **Multi-zone split is mandatory:** zone = `multi`; architect splits the contract at the pdf-extractor↔mcp-server service boundary before any dev dispatch.
- **No false-greens:** 5 prior false-greens on this surface. Tier-3 gate verified by DIRECT market.db query, NOT the (possibly stale) endpoint; NOT-RUN panels are not green; measured corpus pass-rate, not one doc.
- **Separate open sprints stay separate:** MCPZONE-HARDEN-1 + DEPLOY-DRIFT (-1/-2/-3) are NOT folded in.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push` (user owns); all on `main` (NO branches); `git show --stat HEAD` zero foreign files; do NOT touch any `pilot-status-*.json` or the frozen pdf-extractor surfaces (sandbox/runner.py, dashboard trust-contract, text_table_extractor.py); the /api/bctc-inspect overlay IS in scope this sprint; never ask user to run/deploy — spawn ops/dev. Commit-mutex uncallable by subagents → leave files UNSTAGED; main terminal commits.

---

## BUG DEPLOY-DRIFT — Deployed container images lag latest commit (MCP tools 404) — INCIDENT 2026-05-25

**Status:** OPEN 2026-05-25T07:08Z by PO (filed from user incident report via main terminal). **Incident origin:** 2026-05-25 macro + kinh-dich outage — both microservice containers were unreachable for MCP tool access. **The connectivity ROOT CAUSE is already FIXED + committed** (`a5b6203d` mcp-server Docker-network hostname env vars + `3bd9e6ae` corrected `MACRO_INDICATORS_URL`). These tasks are the **LEFTOVER deployment drift**, not the outage itself: with connectivity restored, two MCP tools still 404 because the deployed images are stale (older builds that predate routes which exist in the repo HEAD). **Priority: HIGH — reliability tier, top of PO order (reliability → coverage → UX → architecture). Dead/partially-dead MCP tools = production reliability.** NOT a SCALE pilot — does NOT consume the WIP=2 fleet cap; does NOT touch any `pilot-status-*.json`. Single-zone dev + ops deploy; the systemic guard may route to architect.

**Zones:** DRIFT-1 = `apps/macro-indicators/` (dev-macro-indicators) + ops (deploy); DRIFT-2 = `apps/kinh-dich-service/` rebuild only (ops; code already at HEAD); DRIFT-3 = `cross-service/` CI/CD (architect design → owner-zone or cross-service dev).

**DONE CONDITION:** (1) `get_macro_calendar` returns 200 with real calendar data end-to-end through mcp-server (not a direct-service bypass — the 3bd9e6ae lesson: direct `/snapshot` test gave a false green); (2) all 4 newer kinh-dich endpoints (`/readings/{code}/history`, `/hexagram/{number}/transitions`, `/backtest/{code}`, `/hexagram/{number}/explain`) return 200 end-to-end + basic tools still work (non-regression); (3) a CI/CD step verifies deployed image SHA matches latest commit before a deploy is declared complete, with a deliberate-drift proof that it catches a stale image.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| DRIFT-1 | **get_macro_calendar 404 — macro image drift. PATH DECISION RESOLVED → (a) deploy Go image; collapses to OPS-REBUILD.** `/macro-calendar` route EXISTS in the Go service at `apps/macro-indicators/pkg/interface/http/handlers_calendar.go` (verified on disk 2026-05-25T11:19Z) AND the Dockerfile was switched from stale Bun/TS to Go multi-stage build in `f85ad1d9` (2026-05-25T08:35Z, AFTER this row was filed). The dev path-decision (deploy Go, NOT backport TS) is therefore MADE + the code is at HEAD — no dev code change remains. Env-var connectivity already fixed (`3bd9e6ae`). **REMAINING = ops `docker compose up -d --build macro-indicators` in the SEPARATE docker session (16GB host cap; NOT triggered by a dev agent).** | HIGH | TASK | ops (rebuild + redeploy; dev-macro-indicators on standby only if rebuild surfaces a code defect) | docs/handoffs/TASK_DEPLOY-DRIFT.md | OPS-REBUILD-READY (docker session; routed to WORK + DASHBOARD ## ops — NOT dev-team-dispatchable) | — |
| DRIFT-2 | **kinh-dich-service running stale May-20 TS image.** Predates the 2026-05-24 TS→Go reboot + P2-KD-G. Basic tools (`get_market_hexagram`, `get_kinhdich_reading`) work, but 4 newer endpoints 404. **FIX = rebuild + redeploy the container to repo HEAD** — the Go code is ALREADY on disk (`apps/kinh-dich-service/Dockerfile` = Go reboot, `cmd/server/main.go`, latest `746dee48`). This is a redeploy, NOT a code change. | HIGH | TASK | ops (rebuild + redeploy; dev-kinh-dich on standby only if rebuild surfaces a code defect) | docs/handoffs/TASK_DEPLOY-DRIFT.md | READY (dispatch NOW — parallel w/ DRIFT-1) | — |
| DRIFT-3 | **Systemic guard (dev-kinh-dich recommendation).** Add a CI/CD step that verifies the deployed container image matches the latest commit BEFORE declaring a deployment complete — this image-drift class caused BOTH DRIFT-1 and DRIFT-2. **Architect designs first** (cross-service concern: is this one verify-step per service, or a shared deploy-gate?), then routes to the owning zone or cross-service dev. Include a deliberate-stale-image proof (drift detected → deploy fails). | MEDIUM | TASK | architect (design) → owner-zone/cross-service dev (impl) | docs/handoffs/TASK_DEPLOY-DRIFT.md | **Phase A COMPLETE + merged (A1 236dfc8b, A2 1e3b3803, A3 09998906). Deferred: ops live-E2E + rebuilds w/ --build-arg GIT_SHA; Phase B (pdf Dockerfile) deferred.** | — |
| DRIFT-3-A1 | **Add `ARG GIT_SHA` + `LABEL` to all 10 non-pdf Dockerfiles** (final 2 lines of runtime stage, after all COPYs). Parallel per-zone edits: dev-mcp-server, dev-api-gateway, dev-stock-price, dev-technical-analysis, dev-macro-indicators, dev-kinh-dich, dev-alert-engine, dev-rag-service, dev-news-fetch, dev-frontend. AC: 10 services tagged + pdf-extractor untouched. Handoff: docs/handoffs/DRIFT-3-A1.md. Rebuild DEFERRED. | MEDIUM | TASK | dev-cross-service (per-zone owners) | docs/handoffs/DRIFT-3-A1.md | **DONE** (`236dfc8b`) | — |
| DRIFT-3-A2 | **Create SHA verification scripts** (3 new files): `scripts/verify-deploy-sha.sh` (main gate), `scripts/test-sha-drift-guard.sh` (deliberate-stale proof), `scripts/test-sha-comparison-unit.sh` (unit tests, no Docker). All follow preflight-disk.sh pattern (set -euo pipefail, exit codes). Handoff: docs/handoffs/DRIFT-3-A2.md. Rebuild DEFERRED. | MEDIUM | TASK | dev-cross-service | docs/handoffs/DRIFT-3-A2.md | **DONE** (`1e3b3803`) | — |
| DRIFT-3-A3 | **Update runbook Step 4** in `docs/protocols/docker-deployment-runbook.md` — replace timestamp check with `scripts/verify-deploy-sha.sh <svc>` call. Must exit 0 to proceed. Handoff: docs/handoffs/DRIFT-3-A3.md. Rebuild DEFERRED. | MEDIUM | TASK | dev-cross-service | docs/handoffs/DRIFT-3-A3.md | **DONE** (`09998906`) | — |
| DRIFT-QA | **QA gate (covers -1 + -2; -3 has its own deliberate-drift proof).** End-to-end through mcp-server: `get_macro_calendar` 200 + real calendar data; all 4 kinh-dich endpoints 200; basic kinh-dich tools non-regression; no test-baseline regression (9277/34). Emit `qa-deploy-drift-<UTC>.json`. | HIGH | TASK | qa | docs/handoffs/TASK_DEPLOY-DRIFT.md | BLOCKED | DRIFT-1, DRIFT-2 |
| DRIFT-CLOSE | PO sign-off vs DONE CONDITION; main terminal commits in-tree work. | HIGH | GATE | po | docs/handoffs/TASK_DEPLOY-DRIFT.md | BLOCKED | DRIFT-QA (DRIFT-3 may close on its own architect+QA proof) |

**Notes:**
- **Priority rationale (PO):** reliability tier outranks ALL active structural work (Phase-0 frontend pilot, P2-TA pilot, BCTC-TABLE, KD-QREF-LANG) in my order, but these are SMALL targeted redeploys/route-fixes, not pilots — they run on the general dev/ops lane and do NOT consume the WIP=2 fleet cap. DRIFT-1 + DRIFT-2 dispatch NOW in parallel; both are isolated single zones with zero collision against active sprints.
- **False-green guard (from incident `3bd9e6ae`):** the prior macro fix gave a false green on a DIRECT `/snapshot` test that bypassed mcp-server; the real path read `MACRO_INDICATORS_URL` (undefined → localhost fallback → refused). DONE CONDITION mandates END-TO-END-through-mcp-server verification for DRIFT-1, not a direct-service curl.
- **DRIFT-2 is a redeploy, not a rebuild-from-new-code:** repo HEAD already carries the Go reboot. If the rebuild surfaces a genuine code defect, escalate to dev-kinh-dich; otherwise ops owns end to end.
- **Recurring-bug guard (binding):** the deploy-drift CLASS now has 2 instances (macro + kinh-dich) from the SAME root cause (image lags commit). DRIFT-3 IS the structural-rethink response per `feedback_recurring_bug_escalation.md` — architect-designed cross-service guard, not another one-off patch. A 3rd drift incident before DRIFT-3 lands → PO blocks all further deploys until the guard ships.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push` (user owns); all on `main` (NO branches); `git show --stat HEAD` zero foreign files; do NOT touch any `pilot-status-*.json`; never ask user to run/deploy — spawn ops/dev. HONEST counts only — verify deployed image SHA, not "should be deployed."

---

## MAINT — Residual non-PDF maintenance batch (PO dev-team triage 2026-05-26T19:40Z)

**Status:** OPEN 2026-05-26T19:40Z by PO during dev-team triage. **NOT a sprint, NOT a pilot — does NOT consume the WIP=2 fleet cap.** These are the residual non-BCTC backlog items triaged while a PARALLEL session owns the active BCTC-LAYOUT-FIRST sprint (LF-FIX→LF-DEPLOY→LF-QA→LF-EXIT). **HARD EXCLUSIONS honored:** zero `apps/pdf-extractor/` touch, zero `LF-*` task, zero task requiring a Docker rebuild RIGHT NOW (any rebuild is DEFERRED to a later serialized ops hop — the other session's LF-DEPLOY rebuilds pdf-extractor, and concurrent rebuilds risk the 16GB host kernel-panic). All authoring work is repo-source code/test/data/doc only; `baseline_pass` until the deferred rebuild lands.

**Priority order (PO standard: reliability → coverage → UX → architecture).** DRIFT-3 (above) + MACRO-CONTRACT are the dispatched top-2 (WIP ≤ 2). Remainder are queued/parked.

| Task ID | Title | Priority | Type | Owner | Zone | Status | Notes |
|---------|-------|----------|------|-------|------|--------|-------|
| MACRO-CONTRACT | **[dev-macro-indicators] Body-contract test for `/macro/snapshot` (R-HIGH risk flag, frontend close).** Author a body-contract test asserting the keyed-object shape (6 keys: investment-clock/oil/gold/usdvnd/carry/yield) the frontend consumes via `Object.entries()`. This is the exact contract that broke `/dashboard/analysis` live (500) at frontend close (caught by the verification gate, not a unit test). Target: `apps/macro-indicators/pkg/interface/http/handlers_snapshot.go` + test file. Go `httptest`, zero creds. NO rebuild to author — rebuild DEFERRED to a later serialized ops hop. | HIGH | TASK | dev-macro-indicators | `apps/macro-indicators/` | **DONE (80c776de)** | Body-contract locked. |
| CLIENTS-TYPE | **[dev-mcp-server] Fix `clients.ts` signals type mismatch (R-MED risk flag).** Correct the signals type in `apps/mcp-server/src/infrastructure/microservices/clients.ts` to match the macro keyed-object body-contract (Record<string,MacroSignalEntry>, not array). TS-only type fix + unit/type-check. Does NOT touch the BCTC-LAYOUT-FIRST overlay surface (`bctcInspectHandler.ts`); the other session's LF-DEPLOY rebuilds pdf-extractor ONLY (mcp-server unchanged per pipeline-state), so no rebuild collision. NO rebuild to author — DEFERRED. | MEDIUM | TASK | dev-mcp-server | `apps/mcp-server/` | **DONE (5d49e3be)** | Macro keyed-object contract end-to-end complete. |
| SSOT-REFRESH | **[dev-mcp-server] Refresh stale SSOT stat — `system-map.json` MCP toolCount.** THREE divergent numbers: `system-map.json` tools array = 125 (verified), `project-stats.json` toolCount = 146, this board's prior note said live=148. Owner MUST first COUNT the real registered tool count from the live mcp-server tool registry, then write that SINGLE true value to `docs/data/system-map.json` (and reconcile `project-stats.json` toolCount + `infrastructureStatus.toolCount` to match). Pure data-file refresh. No docker, no pdf, no rebuild. mcp-server zone contention cleared (LF-OVERLAY lock expired 21:01Z; LF-FIX touches pdf-extractor only). | LOW | CLEAN | dev-mcp-server | `apps/mcp-server/` | **DONE (`862c41cf`)** | SSOT 3-number divergence CLOSED → all 146. system-map.json tools array reconciled 125→146 (145 `server.tool()` + 1 `server.registerTool()` = sequential_market_analysis); 21 missing tools added; lastUpdated 2026-05-19→2026-05-26. project-stats.json toolCount + infrastructureStatus.toolCount were ALREADY 146 (no change needed). |
| ARCH-DOC-DRIFT | **[cross-service dev] ARCHITECTURE.md macro Go/TS doc drift (R-LOW risk flag).** Reconcile `docs/ARCHITECTURE.md` macro service language (Go, not TS). Doc-only. | LOW | CLEAN | cross-service dev | `cross-service/` | PARKED (lowest) | Doc-only. |
| CLEAN-1972 | **[qa] Stale branch `task/1972-vndirect-ohlcv-null-coercion`.** 1 unmerged commit `0d918f08` (verified `git log main..<branch>` = 1 commit; PM-cycle chore that ALSO carries `ohlcvBackfill.ts` + a 159-line test). Route to QA to decide merge-or-discard (do NOT auto-discard — it carries real code + a test). NO `git push`, NO `--force`; all on `main`. | LOW | CLEAN | qa | `cross-service/` | **DONE — DISCARDED, ZERO coverage lost** | QA verdict = DISCARD. vndirect OHLCV null-coercion fix already fully on main at `0a51a5a0`; branch carried ZERO unique coverage — `ohlcvBackfill.ts` byte-identical (0 diff), branch test file (159L, `660ab575`) was an OLDER subset of main's (163L, `558ffbfe` — main added a preconnect TS-strictness shim). Verified `git merge-base --is-ancestor 0a51a5a0 main` = YES before discard. Branch deleted; lock released. |

**Parked (NOT in this batch — pipeline-state explicitly defers / cosmetic):**
- **MACRO-VNINDEX-DATA-GAP** (dev-macro-indicators) — pipeline-state: "defer under host load." Not dispatched.
- **NEWS-INGEST-2c** — dashboard `<option>` selector in `apps/news-fetch/dashboard/index.html` (cosmetic; API already serves VN via `source=all`). Developer cosmetic lane, low priority. Not dispatched.
- **DRIFT-2 residual** — kinh-dich hexagram endpoints (501) owned by the dev-kinh-dich Go pilot, NOT this batch.

**Notes:**
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`; NO `git push` (user owns); all on `main` (NO branches); do NOT touch `apps/pdf-extractor/`, any `LF-*` task, any `pilot-status-*.json`, or `docs/pipeline-state.json` BCTC-LAYOUT-FIRST fields (parallel session owns them). Main terminal commits; leave files UNSTAGED. Never ask user to run/deploy.
- **Rebuild-deferral rationale (binding):** the parallel LF-DEPLOY rebuilds the pdf-extractor container; two concurrent Docker rebuilds risk the 16GB host kernel-panic (MEMORY: project_host_memory_panic). Every MAINT task authors source/test/data/doc only and FLAGS its rebuild as deferred to a later serialized ops hop.

---

## Deep-Module Refactor Rollout — COMPLETE 11/11 + Frontend → ARCHIVED 2026-05-26T14:30Z (PO dev-team triage)

**Status: CLOSED. The 2026-05-22 three-tier deep-module+DDD rollout end-state ("complete the 2 last refactor services, rebuild the container(s), verify") is ACHIEVED.** Both remaining pre-0 pilots terminally closed verdict=scale:
- **Frontend SCALE pilot — DONE (10/12 YES + G3/G5 N/A-with-justification, verdict=scale).** PO terminal close `2f33d871` (QA P2-Z close-gate APPROVED `723ef803`); pipeline-state corrected `c7f184ad`. G9 graded via ops live-recheck (Playwright 4/4 vs live :3001, macro keyed-object fix `a0364390`), NOT user verbal sign-off (awaitingUserG9Signoff gate RETIRED). SSOT: `docs/data/pilot-status-frontend.json` (status=DONE, decisionMatrix 3×YES). Container rebuilt ops FE-REBUILD 2026-05-25T19:31Z (sha256:605035cf) + P2-H rebuild 13fe4167.
- **mcp-server SCALE pilot (FINAL/11th) — DONE 12/12, verdict=scale.** PO terminal close `8972a155` (2026-05-26T07:40Z). SSOT: `docs/data/pilot-status-mcp-server.json` (status=DONE, goalsEarned=12).
- **Backend fleet 11/11 + frontend = deep-module rollout COMPLETE.** BUILD-WAVE SEQUENCING governance (A frontend → B mcp-server-SOLO → C ops rebuild → D QA) fully executed; no open wave remains.

Full per-task ledgers (frontend P0-FE-1..5/EXIT + P1 WAVE-A + P2-A..Z; mcp-server P0-MCP-1..5 + P1-A..H + P1-QA/EXIT) and the BUILD-WAVE SEQUENCING concurrency governance are archived → `docs/TASKS_ARCHIVE.md § Archive — Added 2026-05-26 (deep-module rollout 11/11 + frontend close)`. The authoritative state is the two pilot-status SSOT files above — NOT this board.

---

## BUG NEWS-INGEST — News ingest silently rejected entire feed as duplicates → CLOSED 2026-05-26T18:50Z (PO NEWS-INGEST-CLOSE = SIGN-OFF)

**Status: CLOSED / DONE.** Reliability fix SHIPPED. Root cause (c): VPS `fetch-vn-news.sh` re-pushed the same ~160 already-stored VN articles every ~15m cycle (no since-cursor) → mcp-server correctly dropped all 160 → `inserted:0` forever. Fix = persistent since-cursor in `vps-scripts/fetch-vn-news.sh` (NEWS-INGEST-2 `9711ca72`, strict `>` cursor, advances only on HTTP-200) + live-panel VN surface in `newsFetchLiveHandler.ts` (NEWS-INGEST-2b `e1e08a29`, `source=all` now returns all providers). mcp-server dedup (title-dedup + partial UNIQUE index) untouched + intact. **qa APPROVED (`d729e4d1`, signal `docs/signals/qa-news-ingest-2026-05-26T1846Z.json`):** AC-1 cursor PASS (DIRECT-DB 2127 distinct VN urls == total, 0 dup; 140 new post-fix, 70 today), AC-2 VN panel PASS (NF-LD-2 19/19, reuters/bloomberg non-regression), AC-3 dedup PASS (UNIQUE index present, 0 dup), AC-4 no regression PASS (9433 pass / 362 fail under baseline, tsc 0). Commit chain `9711ca72`→`e1e08a29`→`d729e4d1`. Full ledger + worklog: `docs/handoffs/TASK_NEWS-INGEST.md`; close signal `docs/signals/po-news-ingest-close-20260526T185009Z.json` + notebook. NOT a pilot — never consumed the WIP=2 fleet cap. **Live-proof folded into close:** qa DIRECT-DB evidence (70 genuinely-new VN rows today, latest created_at 2026-05-26T17:58:57Z) proves the live cycle inserts NEW distinct VN rows — re-push eliminated in production (no separate ops NEWS-INGEST-LIVE hop needed). **Deferred non-blocking follow-up (NOT dispatched):** NEWS-INGEST-2c — dashboard `<option>` selector in `apps/news-fetch/dashboard/index.html` still lists only reuters/bloomberg/all; API already returns VN via `source=all`. Cosmetic/UX, developer cosmetic lane, low priority.

---

## Sprint BCTC-TABLE — Correct Result-Table Extraction for BCTC Analysis (NEW BUILD)

**Status:** **REOPENED 2026-05-25T21:10Z as BCTC-TABLE-3 — BT-EXIT FINAL=DONE was a FALSE-GREEN, REVOKED.** PO re-verified LIVE (`curl GET /api/bctc-inspect/table/e71f845d-ffa5-48f9-8f09-30ac2cd09c65`): of 150 rows only 11 well-formed; 94 junk (code=null+value=null, company name/address/headers as rows); 44 orphans (value but blank label); code 100 MISSING; 222 DUPLICATED; value_prior null on 118/150. Balance badge real, row table garbage. ROOT CAUSE: dual-path drift — prod `text_table_extractor.py` block-column positional-zip diverged from the proven spike `lines_to_rows()` line-parser. FIX: re-parse stored OCR with spike line-join (host-safe, zero re-OCR). **Reopen section + AC-1..AC-7 + ladder: `docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3` + table below.** — _Prior (REVOKED) CLOSED 2026-05-25T20:51Z record:_ BT-EXIT FINAL claimed DONE on FPT Q4 150 rows / period 31/12/2025 / balanced — but only checked badge + count, not row composition. Final record: `docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT-EXIT FINAL`. — Originally OPEN 2026-05-24T21:24Z (PO from explicit user `/goal`: *"bctc can extract correct result table for analyze"*, via main terminal). Goal: `docs/SPRINT_GOAL.md` (Sprint BCTC-TABLE). Research SSOT (DONE, no new research): `docs/architecture-briefs/2026-05-24-bctc-table-extraction-research.md`. Spec + per-task ACs: `docs/handoffs/TASK_BCTC-TABLE.md` (architect appends blueprint). **Zone:** primary `apps/pdf-extractor/` (sole BCTC extraction owner); model hosting = main-server infra (ops / dev-mainserver-crawls). **WIP=2 fleet cap.** Pilot stays DONE 12/12 FROZEN — `pilot-status-pdf-extractor.json` NOT edited (post-pilot correctness build, not a reopen).

**PRIVACY GUARDRAIL (binding, non-negotiable):** NO task in this sprint sends a financial PDF or page-image to a third-party API. The external-API VLM cross-check is DEFERRED + opt-in (Open Question 1). Phase-0 + the self-hosted track have ZERO external data flow. Any task proposing an off-infra data send is REJECTED back to PO.

**FREEZE COORDINATION:** the 1954c BCTC write-chain consolidation (recurring-bug-escalation freeze) has LANDED (`372fbc91` deprecate pdfOcrWorker; service = sole extraction owner). This sprint builds table-structure extraction ON TOP of the consolidated path. Architect (BT-2) must confirm no collision with frozen write paths before dev touches shared code.

**DONE CONDITION:** (1) parse fix lands + VNM/DHG decimal-shift flips red→green or is caught as `"shift"`; (2) Phase-0 scoreboard covers all 14 gold-set docs × candidates on figure-accuracy + TEDS, PO records measured production pick; (3) winning extractor integrated as adapter, regex column-picking replaced, regression gate meets the agreed figure-accuracy bar, model deployed + ops PROVES live; (4) `reconcile_figures` cross-check gate blocks >10× divergence + WORK alert; (5) audit proves zero off-infra data send.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| BT-1 | **Vietnamese number-format fix (parse-half).** Pure primitives: `vn_number_normalize` (adapter-fed clean string, `.`=thousands `,`=decimal), `reconcile_figures` (generalize >10× `isDecimalShiftAnomaly` → "agree"/"shift"/"low"), `select_period_column` (pick consolidated-current-quarter col). Unit tests w/ VNM `0.000051` + DHG `0.000009` as red→green anchors. Primitives stay PURE (zero infra import). | CRITICAL | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md | READY (dispatch in parallel w/ BT-0; no model needed; ships immediate correctness) | — |
| BT-0 | **Phase-0 SPIKE.** Eval harness + gold-set on real FPT BCTC. TEXT (Tesseract vie+eng) vs IMAGE (PP-StructureV3), CPU, Intel Mac. | HIGH | SPIKE | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md | **DONE** (`f6dd2e83` + eval results on disk; TEXT 100%@4s/page, IMAGE 0/6@45s/page skipped) | — |
| BT-0-PICK | PO records production pick + measured pass-bar verdict. | HIGH | GATE | po | docs/po-decisions/2026-05-25-bctc-table-bt0-pick-text-path.md | **DONE** (PICK=TEXT path Tesseract+BT-1; IMAGE deferred cross-check only; ≥95%±0.5% MET on FPT; Q1/Q2/Q3 resolved PO-default) | BT-0 |
| BT-2 | **Architect blueprint (DESIGN ONLY) — close produce→store→render gap.** Design: (a) TEXT extractor emits structured code→value table + balance check; (b) NEW `bctc_table_rows` schema stores rows per doc+page (additive on 1954c path); (c) `/api/bctc-inspect` read contract for table-next-to-text + balance PASS/FAIL badge. Plus: `ExtractTablesUseCase`, multi-page stitch→store, main-server CPU hosting (no GPU — Q2 resolved), 1954c collision check, one-shot re-extraction plan, Security Clause. | HIGH | TASK | architect | docs/handoffs/TASK_BCTC-TABLE.md | **READY** (BT-0-PICK DONE — dispatch NEXT) | — |
| BT-3-A | **[dev-pdf-extractor] Infra adapters + ports (NEW).** `infrastructure/text_table_extractor.py` (Tesseract+BT-1 TEXT-path adapter), `infrastructure/table_push_client.py` (aiohttp POST to mcp-server), `domain/modules/financial_reports/ports.py` (add `TableAssemblerPort`+`TablePushClientPort`). Zero infra imports in domain. Fence-A/B pass. Unit tests for adapter w/ fixture text (code/header/None-value rows). **Duration: ~2h.** | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § BT-3-A | BLOCKED | BT-2, BT-1 |
| BT-3-B | **[dev-pdf-extractor] Usecase + routes.** `application/extract_tables_usecase.py` (DI, orchestrates extractor→balance-check→push), `interface/handlers.py` (NEW `POST /extract-tables` route), `main.py` (wire composition root). Zero infrastructure imports in application. **Duration: ~2h.** | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § BT-3-B | BLOCKED | BT-3-A |
| BT-3-C | **[dev-pdf-extractor] Integration: process_report() → structured table output.** Wire `ExtractTablesUseCase` + stitching into the module-tier `process_report()` pipeline. Returns NEW keys `structured_table_rows` + `balance_check`. Test FPT red→green: AC-1/AC-2/AC-3 (≥70 rows, balance_pass=true, idempotent). Sandbox green. **Duration: ~2h.** | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § BT-3-C | BLOCKED | BT-3-B |
| BT-3i-A | **[dev-mcp-server] Schema migration + GET route (mcp-server).** `schema-financial-reports.ts`: add `bctc_table_rows` + `bctc_balance_checks` DDL (CREATE TABLE IF NOT EXISTS, migration guard). `pushBctcTableHandler.ts` (NEW): `POST /api/push-bctc-table` handler (DELETE+INSERT idempotent, bulk). Register route in `server.ts`. Test: bulk-insert w/ in-memory DB, duplicate-call idempotency. **Duration: ~2h.** | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_BCTC-TABLE.md § BT-3i-A | BLOCKED | BT-2, BT-3-C |
| BT-3i-B | **[dev-mcp-server] Inspector GET route + HTML render (mcp-server).** `bctcInspectHandler.ts`: add `handleBctcInspectTable()` + `GET /api/bctc-inspect/table/{doc_id}` route (response schema w/ rows + balance_check). `bctc-inspector.html`: add `#table-section` div + `renderTable()` JS (bold summary-rows, balance PASS/FAIL badge). Test: route 200/400 cases, balance badge colors, summary-row bold. **Duration: ~2h.** | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_BCTC-TABLE.md § BT-3i-B | BLOCKED | BT-3i-A |
| BT-4 | **[ops + dev-mainserver-crawls] Deploy extractor to main server + CPU-baseline confirm.** Ops: confirms main-server CPU sizing ≥4s/page (no GPU needed — Q2 resolved). Docker: add `MCP_SERVER_URL=http://mcp-server:3000` env var. Infra: verify `POST /extract-tables` reachable from mcp-server container. NO Mac model in prod (D6). **Duration: ~1h.** | HIGH | TASK | ops + dev-mainserver-crawls | docs/handoffs/TASK_BCTC-TABLE.md § BT-4 | BLOCKED | BT-2 |
| BT-4b | **[dev-pdf-extractor + ops] One-shot re-extraction of 14 stranded docs.** `bctcBatchTableBackfillJob.ts` (mcp-server): NEW one-shot function (NOT cron) iterates `financial_reports WHERE pdf_path IS NOT NULL`, calls `POST /extract-tables` per row. Runs ONCE after BT-3+BT-3i+BT-5 land, BEFORE BT-6. Idempotent: DELETE+INSERT per report_id. Test: row count = 14 (or fewer if unreadable; log, no crash). **Duration: ~1.5h.** | MEDIUM | TASK | dev-pdf-extractor + ops | docs/handoffs/TASK_BCTC-TABLE.md § BT-4b | BLOCKED | BT-3-C, BT-3i-B, BT-5 |
| BT-5 | **[dev-pdf-extractor] Cross-check confidence gate (self-hosted).** Wire `reconcile_figures` into `ExtractTablesUseCase`: for each summary-row code (100/270/300/400/440), call against `financial_reports` scalar. If ratio >10×: BLOCK push + WORK alert + `blocked_reason` in `/api/bctc-inspect/table`. Image-track VLM = SELF-HOSTED only, DEFERRED. Test: FPT 6 sentinels pass reconcile (agree); inject 1000× shift, verify block fires. **Duration: ~2h.** | MEDIUM | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § BT-5 | BLOCKED | BT-3-C, BT-4 |
| BT-6 | **[qa] QA regression gate.** Re-run BT-0 harness across full 14-doc gold-set (not just FPT): figure-accuracy ≥95%±0.5% per doc; VNM/DHG green; 14 docs extracted to `bctc_table_rows`; `/api/bctc-inspect/table/{doc_id}` returns `has_table:true` for each; balance badge rendered (for BS docs); cross-check detects >10× shift + fires WORK alert; sandbox exit-0+zero creds; Fence-A/B intact; `pilot-status-pdf-extractor.json` diff empty; zero off-infra HTTP send. Close QA-on-BT1 (BT-1 green signal verified). Emit `qa-bctc-table-<UTC>.json`. **Duration: ~3h.** | HIGH | TASK | qa | docs/handoffs/TASK_BCTC-TABLE.md § BT-6 | **DONE** (`acd0d61e` APPROVED; 276/276 pdf-extractor, 38/38 mcp-server BCTC, fence genuine, FPT Q4 balance delta=0 live; 4-zero + noise gaps flagged for PO) | BT-4b, BT-5 |
| BT-EXIT | **[po] PO sign-off** vs DONE CONDITION on REAL gold-set + live `/api/bctc-inspect` detected table + balance badge + privacy audit. | CRITICAL | GATE | po | docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT-EXIT FINAL | **DONE** (2026-05-25T20:51Z: FPT consolidated-BS goal SIGNED OFF — FPT Q4 = 150 clean rows (was 2170 noise), `period_current=31/12/2025` (signature-date leak FIXED), balanced=true delta=0, anchors 270/300/400/440 EXACT; HPG Q4 clean 2nd proof; privacy PASS. No QA re-gate required (BT-6 APPROVED + BT-7 281 tests + independent live re-backfill). Residual coverage gaps (a)-(d) → BCTC-TABLE-2, NOT blockers. **Sprint CLOSED.**) | BT-6, BT-7 |
| BT-7 | **[dev-pdf-extractor (+dev-mcp-server/ops)] Clean balance-sheet section filter on pre-supply path + idempotent re-backfill.** Filter `pre_supplied_pages` to the BS page range (same VN markers as BT-3-D auto-locate, on supplied text — host-safe zero Tesseract); unify Path A + Path B; derive `period_current` from the BS section header only. | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § BT-7 | **DONE** (`210a0a62` fix + `29efb93c` deploy/re-backfill: FPT Q4 2170→150 rows, period 26/01/2026→31/12/2025, anchors exact, balanced=true; HPG Q4 clean; +281 tests; fence LIVE. FPT Q1 still 0 rows = quarterly-format gate-block, folded into BCTC-TABLE-2.) | BT-6 |

**Notes:**
- **Dispatch order (UPDATED 2026-05-25T17:17Z + PM SPLIT 2026-05-25T22:00Z):** BT-1 DONE, BT-0 DONE, BT-0-PICK DONE (PICK=TEXT path). **NEXT = BT-2 (architect blueprint)** — gates all integration. Then **PARALLEL dispatch BT-3-A (dev-pdf-extractor infra) + BT-4 (ops deploy prep)** (no WIP contention: BT-4 is ops, BT-3-A is dev; BT-4 gates on BT-2 only, can proceed independently). Then **SERIAL BT-3-B → BT-3-C** (same zone, blocked-chain). In parallel: **BT-3i-A (mcp-server schema)** gates on BT-3-C (both must land before BT-3i-B render). Then **BT-3i-B** unblocks BT-5. WIP snapshot: (1) BT-3-A in-progress + (2) BT-4 ops-session = max-2. BT-3-B starts when BT-3-A closes. BT-3i-A starts when BT-3-C done (BT-3i-A is a schema+route, depends on extractor working). Flow: **BT-3-A || BT-4** → **BT-3-B** → **BT-3-C + BT-3i-A (parallel, same doc_id won't collide — different tables)** → **BT-3i-B** → **BT-5** → **BT-4b** → **BT-6** → **BT-EXIT**. **UPDATE 2026-05-25T20:18Z (PO BT-EXIT live verification):** BT-3-D landed (OCR wiring + auto-locate). BT-4b-2 backfilled 12 docs via Path A (pre-supply stored OCR). BT-6 QA APPROVED (`acd0d61e`). PO held BT-EXIT at PARTIAL — pre-supply path stores all-pages noise (FPT Q4 = 2170 rows / 44 pages, wrong period). **NEW tail: BT-7 (clean section filter + re-backfill) → QA re-verify → PO final BT-EXIT.**
- **Core gap reframed (user finding 2026-05-25):** `/api/bctc-inspect` shows ONLY OCR text + 4 summary figures because production has NO structured code→value table storage. Fix = produce table (BT-3) → store new schema (BT-3) → render in inspector + balance badge (BT-3i). BT-3i is SI-2 boundary → dev-mcp-server, NOT dev-pdf-extractor.
- **Why architect hop (D5):** the adapter boundary + new-primitive contracts + main-server hosting are designed ONCE to avoid a rename-churn cycle. Spike findings (BT-0) feed the blueprint (BT-2). PO → Architect → dev.
- **Security Clause (binding, carried from pilot):** OCR/model calls + PDF I/O are impure → infrastructure adapters; `domain/primitives/*` stay PURE (import-linter fence: primitives must not import infrastructure); `sandbox/runner.py` holds ZERO credentials. API keys (if a self-hosted model needs none) live only in the adapter runtime env, never in sandbox, never in a primitive.
- **Vietnamese-format fix placement (D4):** normalization is an ADAPTER step feeding clean strings to the pure `decimal_normalizer` — do NOT make `decimal_normalizer` locale-aware (it must stay deterministic on clean input).
- **Gold-set (do NOT fetch new):** 14 docs already on disk — `data/pdfs-local/` (VCB, FPT, HPG, DHG, DIG, BSR, DGC, SHB, VEA, VNM) + `data/pdfs/` (VNM, VEA). VNM/DHG are mandatory red→green regression anchors.
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`; NO `git push` (user owns); all on `main` (NO branches); `git show --stat HEAD` zero foreign files; never ask user to run/deploy — spawn ops/dev.
- **Open questions — RESOLVED (PO defaults, full autonomy):** Q1 self-hosted ONLY (external-API VLM deferred opt-in); Q2 GPU NOT required for TEXT figures path (CPU 4s/page; ops confirms CPU sizing at BT-4; GPU only if deferred image cross-check activated); Q3 ≥95%±0.5% adopted + MET on FPT (BT-6 validates wider gold-set). Decision: `docs/po-decisions/2026-05-25-bctc-table-bt0-pick-text-path.md`.
- **Deferred / honestly-flagged:** FPT p5 95.8% + p7 86.7% sub-bar rows + low cell-F1 (0.07-0.12) → PP-StructureV3 IMAGE deferred remedy (self-hosted only); QA-on-BT1 pending (folded into BT-6); 14 stranded docs hold old-parser figures (re-extract ONCE at BT-4b); pass-bar proven on FPT only (BT-6 proves wider).
- **PO BT-EXIT verdict 2026-05-25T20:18Z — PARTIAL, BT-7 required:** gap functionally closed (FPT Q4 + VEA + HPG render a balance table with delta=0 to the dong, anchors exact, privacy PASS self-hosted Tesseract) but NOT clean — the pre-supply backfill path (Path A) feeds ALL stored OCR pages (FPT Q4 = 44 pages → 2170 rows, only 96 coded) with no BS-section filter, and `period_current` is the signature date (26/01/2026), not 31/12/2025. The 74→2170 is all-pages noise, NOT accumulation (live counts idempotent + stable, match backfill table). BT-3-D's auto-locate runs only on Path B (in-process OCR), not Path A. BT-7 unifies the section filter across both paths + scopes period detection + re-backfills idempotently; folds in the 4-zero-row gap (FPT Q1's BS is on stored page 3). Final BT-EXIT sign-off gates on BT-7 + QA re-verify. Full record: `docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT-EXIT`.
- **PO BT-EXIT FINAL verdict 2026-05-25T20:51Z — ⚠ REVOKED as FALSE-GREEN 21:10Z (see BCTC-TABLE-3 below):** BT-7 (`210a0a62`) + deploy/re-backfill (`29efb93c`) closed BOTH PARTIAL blockers on the user's explicit target. Live (read-only `GET /api/bctc-inspect/table/{FPT Q4 e71f845d}`): FPT Q4 = 150 rows (was 2170 noise), `period_current=31/12/2025` (was signature-date "26/01/2026"), balanced=true delta=0, anchors 270=88,089,621,779,862 / 300=44,338,155,487,272 / 400=43,751,466,292,590 / 440=88,089,621,779,862 all EXACT; HPG Q4 = 117 rows, 31/12/2025, balanced=true (2nd clean proof). **WHY REVOKED: I checked row COUNT + balance badge + period, never row COMPOSITION. Re-curl this cycle shows of those 150 rows only 11 are well-formed; 94 are junk text rows; 44 are orphan value-rows with blank labels; code 100 missing; 222 duplicated. The badge was real, the table was garbage.** Residuals (a)-(d) → Sprint BCTC-TABLE-2 (below) stay open. Full (now-revoked) record: `docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT-EXIT FINAL`.

---

### Sprint BCTC-TABLE-3 — REOPEN: row table is garbage (false-green correction) → BT3-RETHINK → ✅ CLOSED (BT3-EXIT2)

**Status:** **✅ DONE / SPRINT CLOSED 2026-05-26T00:12Z (BT3-EXIT2).** The user's `/goal` is MET and the user has been shown the corrected live table. BT3-FIX5 (commit `81970243`, dev-pdf-extractor — 7th attempt, the one that held) resolved the live BCTC table scramble + the BT3-FIX4 false-green. Deployed by ops BT3-DEPLOY2 (image rebuilt + container recreated, 3 markers verified live). QA BT3-QA2 = **PASS/APPROVED** (commit `9f829289`, report `reports/TASK_REPORT_BT3-QA2.md`) — all 11 ACs honest-green. Main terminal independently re-verified the LIVE endpoint `GET /api/bctc-inspect/table/e71f845d-...` + a direct in-container DB count. FINAL STATE (FPT Q4 2025 balance sheet): **79 rows, 0 orphans, 0 dup codes, 0 NULL value_prior** (except code 418, accounting-valid); sentinels exact + label-aligned (100/270/300/400/440); 6 embedded codes recovered & code-split (222/223/226/131/319/421b); balance_delta=0 (44338155487272 + 43751466292590 = 88089621779862); two labels (134, 317) OCR-garbled TEXT but correct code+values (local-Tesseract blemish, NOT structural). **ROOT CAUSE of the 6 prior false-greens eliminated:** test fixture now uses LIVE poppler OCR substrate (architect Ruling D / AC-0), not the spike's PyMuPDF OCR. **Durable lesson:** positive-keep + positional-cutoff + diacritic-insensitive + embedded-code recovery beat the literal-skip-list; AC-0 live-substrate fixture is mandatory. Closure record: `docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-EXIT2 — SPRINT CLOSED`. Decision: `docs/po-decisions/2026-05-26-bctc-table-bt3-exit2-done.md`. Two deferred mcp-server-zone hardening items scheduled as new follow-up Sprint MCPZONE-HARDEN-1 (non-blocking, post-goal, below). — _Prior (REVOKED) status:_ **BT3-FIX4 REVOKED 2026-05-25T23:29Z — FALSE-GREEN #6 — RECURRING-BUG ESCALATION → ARCHITECT (BT3-RETHINK).** The BT3-DESIGN→BT3-FIX→…→BT3-FIX4-PARSE sub-chain SHIPPED (`1ab1f7a6`→`3e47ccf3`→`8dbb19e3`→`c66a7ff7`; psm-6 root-cause `3b722462`; pdf2image dep `4dabeb55`) and DID improve the table (orphans 44→23, dup codes gone, "Test Row" gone, balance_delta=0, ~72/95 rows clean) — but it is STILL broken on LIVE. dev's BT3-FIX4-PARSE "DONE" claimed *"Zero orphan rows, value_prior 78/78=100%"* against the SPIKE PyMuPDF OCR **fixture** (`fpt_q4_2025_pages_4-7.txt`, 79 rows). PO re-verified the **LIVE poppler-OCR** endpoint independently: **95 rows, 23 ORPHANS** (code=null), value_prior null on those 23, codes 222/223/226/131/319/421b ABSENT-or-orphan, header/date/signature junk rows survive. `text_table_extractor.py` = **5 `fix(` commits in 30d** AND the architect's OWN FIX4 ruling false-greened → **per `feedback_recurring_bug_escalation.md`, NO MORE BLIND DEV PATCHES.** Escalated to **architect (BT3-RETHINK)** for a FILTER-STRATEGY root-cause ruling (negative-skip vs POSITIVE-keep vs POSITIONAL-cutoff + embedded-code split). **Acceptance moves to the LIVE endpoint row-by-row; the fixture is FORBIDDEN as the gate + MUST be regenerated from live poppler OCR.** Full record + revised ladder + HARD CONSTRAINTS: `docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-FIX4 REVOKED → BT3-RETHINK`. Decision: `docs/po-decisions/2026-05-26-bctc-table-bt3-fix4-false-green-rethink.md`.

<details><summary>SUPERSEDED — REOPENED 2026-05-25T21:10Z (BCTC-TABLE-3 original, superseded by BT3-RETHINK)</summary>

**Status:** REOPENED 2026-05-25T21:10Z by PO (self-initiated; supersedes the revoked BT-EXIT FINAL). **Severity:** CRITICAL. **User `/goal`:** *"http://localhost:3000/api/bctc-inspect need present correct data table for recheck."* **Zone:** primary `apps/pdf-extractor/` (`text_table_extractor.py` row-assembler); dev-mcp-server only if row contract must change (it should not). **Privacy:** self-hosted Tesseract only, zero external API. **Re-backfill:** host-safe pre-supplied-stored-OCR path (`bctcBatchTableBackfillJob`), sequential, zero Tesseract ([[project_host_memory_panic]]). Full spec + AC-1..AC-7 + live evidence + root cause: `docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3`.

**ROOT CAUSE (PO-diagnosed, confirmed by reading both parsers):** dual-path drift round 2. Stored OCR is already one-line-per-row; the spike `fpt_balance_sheet_eval.py::lines_to_rows()` built ~80 perfectly-joined rows from it (gold = `spike/eval/results/FPT_balance_sheet_4-7.md`). Production `text_table_extractor.py` added a fabricated `_detect_block_column_layout`→`_extract_block_columns` positional-zip state machine that hardcodes `label=""` (44 orphans), misaligns code↔value (drops 100, dups 222, nulls value_prior), and dumps non-code lines as junk rows (94 junk). FIX: align prod assembler to the spike line-join parser (PREFERRED — re-parse stored OCR, host-safe zero re-OCR; zone-OCR only if architect proves line-parsing can't recover the join).

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| BT3-DESIGN | **Architect ruling (DESIGN ONLY):** re-parse stored OCR with spike `lines_to_rows` line-join (preferred, host-safe) vs zone/coordinate OCR. Pin the exact change in `text_table_extractor.py` (kill/repair block-column positional-zip), confirm row CONTRACT unchanged (code,label,value_current,value_prior), sequence the host-safe re-backfill. Privacy: self-hosted only. | CRITICAL | TASK | architect | docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3 | **READY (dispatch FIRST)** | — |
| BT3-FIX | **[dev-pdf-extractor]** Fix `text_table_extractor.py` row-assembler to one-line-per-row join (label+code+current+prior kept together). Drop/repair block-column path. Integration test drives REAL adapter on REAL stored FPT OCR text — NO subclass/preloaded bypass (false-green lesson) — asserts AC-1..AC-5 vs gold. Fence-A/B intact, sandbox exit-0. | CRITICAL | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3 | BLOCKED | BT3-DESIGN |
| BT3-DEPLOY | **[ops]** docker-compose redeploy pdf-extractor (+ mcp-server iff contract touched) + idempotent host-safe re-backfill of FPT Q4 via `bctcBatchTableBackfillJob` (sequential, zero Tesseract). | HIGH | TASK | ops | docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3 | BLOCKED | BT3-FIX |
| BT3-QA | **[qa]** LIVE curl verify AC-1..AC-7 on `GET /api/bctc-inspect/table/e71f845d-ffa5-48f9-8f09-30ac2cd09c65` against spike gold (NO mock). Emit `qa-bctc-table-3-<UTC>.json`. | HIGH | GATE | qa | docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3 | BLOCKED | BT3-DEPLOY |
| BT3-EXIT | **[po]** Sign-off vs AC-1..AC-7 on LIVE curl evidence + privacy audit. This time inspect ROW COMPOSITION (zero junk, zero orphan, code 100 present, no dups, value_prior filled), not just the badge. | CRITICAL | GATE | po | docs/handoffs/TASK_BCTC-TABLE.md § SPRINT BCTC-TABLE-3 | BLOCKED | BT3-QA |

**Dispatch order:** BT3-DESIGN (architect) → BT3-FIX (dev-pdf-extractor) → BT3-DEPLOY (ops) → BT3-QA (qa) → BT3-EXIT (po). Strictly serial — single zone, single doc, correctness-critical.

</details>

**BT3-RETHINK ladder (✅ COMPLETE — all rows DONE; supersedes the BT3-DESIGN..BT3-EXIT rows above):**

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| BT3-RETHINK | **Architect FILTER-STRATEGY root-cause ruling (DESIGN ONLY — recurring-bug escalation).** DO NOT propose another regex patch. Rule on: (A) filter strategy — negative-skip-list (proven inadequate) vs POSITIVE-keep (emit row only if valid code OR recognized BCTC section label) vs POSITIONAL-cutoff (drop after last summary code 440/270) vs combination; (B) embedded-code split — replace layout-specific regexes with a structural code-finder (find 2-3-digit[a-z]? token anywhere → split label-left/values-right)? Must justify against the 3 LIVE poppler-OCR root-cause classes, NOT the spike fixture. Output: brief under `docs/architecture-briefs/` + revised ACs appended to handoff. First instruction to dev: regenerate fixture from LIVE poppler OCR. | CRITICAL | TASK | architect | docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-FIX4 REVOKED → BT3-RETHINK | **DONE** (ruling: POSITIVE-keep + positional-cutoff + diacritic-insensitive `_norm()` + Layout-5 embedded-code scan; AC-0 live-substrate fixture mandate. Brief `docs/architecture-briefs/2026-05-26-bctc-table-bt3-rethink-filter-strategy.md`) | — |
| BT3-FIX5 | **[dev-pdf-extractor]** Implement EXACTLY the architect's BT3-RETHINK strategy (no freelance regex). **HARD AC: REGENERATE the test fixture from LIVE poppler OCR** (re-OCR e71f845d via the production poppler path / pull stored poppler text) — NOT the spike PyMuPDF text. Fence-A/B intact, sandbox exit-0, FROZEN surfaces untouched. Leave files UNSTAGED, list exact paths in RETURN. | CRITICAL | TASK | dev-pdf-extractor | docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-FIX4 REVOKED → BT3-RETHINK | **DONE** (`81970243` — Rulings A/B/C/D; fixture regenerated from live poppler OCR via PdfOcrAdapter; 285 unit tests pass incl. 32 new diacritics tests; Fence-A/B kept, exit 0) | BT3-RETHINK |
| BT3-DEPLOY2 | **[ops]** `docker compose up -d --build pdf-extractor` (separate docker session, 16GB cap) + re-extract **ONLY e71f845d** via `POST http://localhost:5001/extract-tables`, single-doc sequential. **NEVER run `bctcBatchTableBackfillJob`** (host kernel-panic risk). | HIGH | TASK | ops | docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-FIX4 REVOKED → BT3-RETHINK | **DONE** (image rebuilt + container recreated; single-doc re-extract of e71f845d only; 3 markers verified live) | BT3-FIX5 |
| BT3-QA2 | **[qa] LIVE endpoint row-by-row gate (NOT the fixture; `balance_pass` alone FORBIDDEN as gate).** `GET /api/bctc-inspect/table/e71f845d-...`: orphans ≤2; ZERO header/date/signature junk; codes 222/223/226/131/319/421b present + code-split with values; 4 sentinels exact (270/100/300/400); value_prior populated; no dup codes; balance_delta=0. Emit `qa-bctc-table-<UTC>.json`. | CRITICAL | GATE | qa | docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-FIX4 REVOKED → BT3-RETHINK | **DONE — APPROVED** (`9f829289`; report `reports/TASK_REPORT_BT3-QA2.md`; all 11 ACs PASS; 79 rows / 0 orphans / 0 junk / 0 dup; in-container DB count 79 verified) | BT3-DEPLOY2 |
| BT3-EXIT2 | **[po]** Independently re-verify the LIVE endpoint row-by-row (do NOT trust QA claim or fixture) vs the BT3-QA2 bar. Main terminal commits in-tree work after sign-off. | CRITICAL | GATE | po | docs/handoffs/TASK_BCTC-TABLE.md § [PO] BT3-EXIT2 — SPRINT CLOSED | **DONE — SIGNED OFF** (2026-05-26T00:12Z; main terminal independently re-verified live endpoint + in-container DB count; user shown the corrected table; SPRINT CLOSED. Decision: `docs/po-decisions/2026-05-26-bctc-table-bt3-exit2-done.md`) | BT3-QA2 |

**Dispatch order:** BT3-RETHINK (architect) → BT3-FIX5 (dev-pdf-extractor) → BT3-DEPLOY2 (ops) → BT3-QA2 (qa) → BT3-EXIT2 (po). Strictly serial — single zone, single doc, correctness-critical. **✅ ALL COMPLETE.**

**HARD CONSTRAINTS (all agents):** QA acceptance = LIVE endpoint row-by-row (fixture forbidden as gate; fixture regenerated from live poppler OCR). HOST KERNEL-PANIC: single-doc OCR only, NEVER batch backfill. PRIVACY: self-hosted local OCR only, zero third-party API. FROZEN: `apps/pdf-extractor/dashboard/{index.html,traces.js,trust-contract.spec.js}` + `sandbox/runner.py` + `pilot-status-pdf-extractor.json`. COMMITS: subagents leave files UNSTAGED + list paths; main terminal commits explicitly on `main`, no push, `git show --stat HEAD` zero foreign.

**DEFERRED — NOW SCHEDULED as Sprint MCPZONE-HARDEN-1 (below, non-blocking, post-goal):** (a) `pushBctcTableHandler.ts` `rows_stored: rows.length` is an input echo not a DB count (false-success that masked the write-wedge); (b) a test wrote to the LIVE `/app/data/market.db` + seeded the "Test Row" (test-isolation breach). Both routed to dev-mcp-server in a future sprint.

---

## Sprint BCTC-TABLE-2 — Multi-ticker / Quarterly BCTC Coverage (FOLLOW-UP) → QUEUED

**Status:** QUEUED (PO follow-up, NOT active; carries BCTC-TABLE-3 verified FPT goal forward to wider multi-ticker + quarterly coverage). Does NOT consume WIP=2 fleet cap. Zone: `apps/pdf-extractor/` (sole BCTC owner). Goals: B2-1 (quarterly code-map for "Báo cáo tình hình tài chính" / FPT Q1) + B2-2 (period-detection hardening on VEA/SHB layouts) + B2-3 (empty-period docs ACB/DGC/DHG/EIB) + B2-4 (low-row investigation VNM/EIB). Bindings: self-hosted Tesseract ONLY; frozen surfaces (`pilot-status-pdf-extractor.json`, sandbox, dashboards) untouched; all work on `main`, explicit-file staging. **NEXT:** dispatch AFTER LF-EXTRACT + LF-OVERLAY close (prioritize active pipeline stability).

---

## Sprint MCPZONE-HARDEN-1 → CLOSED 2026-05-26T18:04Z

**Status: ✅ DONE / CLOSED.** MZH-1 (DB-verified `rows_stored`) + MZH-2 (prod-db test guard) shipped `2d4f71d9` → ops MCPZONE-DEPLOY-1 PASS (live: handler `rows_stored:1` matched direct DB COUNT; write-wedge gone; health 200, 146 tools, 2.6GB/8GB; +7 baseline passes). Close signal `docs/signals/po-20260526T180433Z.json`.

---

## Sprint PDF-INSPECT — Side-by-Side PDF / Extracted-Text Inspector (NEW FEATURE)

**Status:** DONE + CLOSED (RE-SIGNED) 2026-05-24T19:34Z — verified on REAL `market.db` data after TWO reopens (honest trail below). Premature first close 2026-05-24T17:47Z reopened TWICE on real-data defects. Opened 2026-05-24T17:19Z (PO self-initiated from explicit user feature request via main terminal). Goal: `docs/SPRINT_GOAL.md` (Sprint PDF-INSPECT). Spec + ACs + full reopen trail: `docs/handoffs/TASK_PDF-INSPECT.md`. **Zone migrated** `apps/pdf-extractor/` → `apps/mcp-server/` (REOPEN-1: real BCTC data lives in mcp-server's `market.db`, not pdf-extractor's `pdf_extractor.db`). **Impl owner migrated** dev-pdf-extractor → dev-mcp-server. **User-facing URL changed** `localhost:15001/inspect` (fixture-only, premature) → `http://localhost:3000/api/bctc-inspect` (REAL data, LIVE NOW). **WIP=1 strictly sequential.** POST-PILOT feature — pdf-extractor SCALE pilot stays DONE 12/12 + frozen; `pilot-status-pdf-extractor.json` NOT touched (PO-only, inspector is a dev tool not a pilot task); sandbox dashboard surface UNTOUCHED.

**CORRECTED DONE CONDITION (supersedes the premature first close):** "Sprint DONE" = the served viewer renders REAL `financial_reports`/`pdf_extracted_text` rows from the deployed container's `market.db` (verified row counts + non-null-rate of the columns relied upon), NOT fixtures and NOT schema-existence. The premature 17:47Z close was proven on a local uvicorn with seeded fixtures (`localhost:15001`) — at deploy time the viewer was EMPTY on real data → REOPEN.

**Delivery model (FORK RESOLVED, R1):** SERVED FastAPI viewer (port 5001), NOT `file://` — container PDFs + extraction store live in the `market_data:/app/data` named volume a `file://` page cannot reach. **Acceptance (user's real path, L9):** user opens served viewer in browser → list of PDFs → select one → LEFT = rendered PDF, RIGHT = extracted text/fields, side-by-side.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| PI-1 | Design served viewer (pdf-extractor). DESIGN ONLY. | HIGH | TASK | architect | docs/handoffs/TASK_PDF-INSPECT.md | DONE (superseded by REOPEN-1 — read wrong DB) | — |
| PI-2 | Implement served viewer in pdf-extractor (fixture-verified only). | HIGH | TASK | dev-pdf-extractor | docs/handoffs/TASK_PDF-INSPECT.md | DONE-then-DEPRECATED (`4651c080`; reads junk `pdf_extractor.db` — dead on real data; kept w/ DEPRECATED comment) | — |
| PI-3 | Verify under served URL — FIXTURE data only (premature). | HIGH | TASK | qa | docs/handoffs/TASK_PDF-INSPECT.md | DONE-on-fixtures (`0d10f310`; not real-data path) | — |
| PI-EXIT | PO sign-off (PREMATURE — fixture-only, not real data). | CRITICAL | GATE | po | docs/handoffs/TASK_PDF-INSPECT.md | SIGNED-then-REOPENED ×2 (`97cd5763`) | — |
| PI-RO1-DESIGN | REOPEN-1: architect re-ground — inspector → mcp-server, real `market.db` wiring (4 routes). | HIGH | TASK | architect | docs/handoffs/TASK_PDF-INSPECT.md | DONE (`8f7b54fa`) | — |
| PI-RO1-DEV | REOPEN-1: dev-mcp-server build `GET /api/bctc-inspect` over real `market.db`. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_PDF-INSPECT.md | DONE (`1b5799fb`) | PI-RO1-DESIGN |
| PI-RO1-QA | REOPEN-1: QA real-data verify → found all 14 rows `pdf_path=NULL`, list count:0. | HIGH | TASK | qa | docs/handoffs/TASK_PDF-INSPECT.md | CHANGES_REQUESTED (`127cb347`) | PI-RO1-DEV |
| PI-RO2-DESIGN | REOPEN-2: architect re-root-cause — backfill `pdf_path` + serve-time safety net + secondary OCR join. | HIGH | TASK | architect | docs/handoffs/TASK_PDF-INSPECT.md | DONE (`0245ff4c`) | PI-RO1-QA |
| PI-RO2-DEV | REOPEN-2: dev-mcp-server `backfillBctcPdfPaths` + all-rows LIST + secondary OCR join. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_PDF-INSPECT.md | DONE (`69da9d01`) | PI-RO2-DESIGN |
| PI-RO2-QA | REOPEN-2: QA re-verify on REAL `market.db` — count=14, 12 has_pdf, 14 has_ocr, 7 anomaly, VNM PDF+OCR rendered. PASS. | HIGH | TASK | qa | docs/handoffs/TASK_PDF-INSPECT.md | DONE — PASS (`3098c69d`) | PI-RO2-DEV |
| PI-RO2-EXIT | PO final re-sign vs user acceptance on REAL data + record meta-lesson. | CRITICAL | GATE | po | docs/handoffs/TASK_PDF-INSPECT.md | DONE — RATIFIED 2026-05-24T19:34Z | PI-RO2-QA |

**PI-EXIT sign-off (2026-05-24T17:47Z) — PREMATURE (kept on record, NOT erased):** RATIFIED at the time against FIXTURE data. QA had verified under a local uvicorn with seeded fixtures (`http://localhost:15001/inspect`): list→select→LEFT pdf.js render, RIGHT text/tables, VNM `net_profit 0.000051` visible. PI-3 ACs 1-6 PASS, 186/186 pytest, Fence-A/B KEPT. Commit chain PI-1/PI-2 `4651c080` + PI-3 `0d10f310` + PM record `97cd5763`. **WHY THIS WAS WRONG:** acceptance was proven on FIXTURES, never on the deployed real-data path. At DEPLOY time the viewer was EMPTY on real data → REOPEN. The served viewer read the WRONG DB (pdf-extractor's `pdf_extractor.db` = 15,570 junk `status=failed`/`example.com` test rows + a non-existent extractions path). Real BCTC data lives in mcp-server's `market.db`. → meta-lesson recorded below.

**REOPEN-1 (architect re-ground `8f7b54fa` → dev-mcp-server `1b5799fb` → QA CHANGES_REQUESTED `127cb347`):** Inspector MOVED out of pdf-extractor INTO mcp-server (impl owner now dev-mcp-server). Built `GET /api/bctc-inspect` (HTML + `/docs` list + `/pdf/{id}` + `/ocr/{id}`) reading real `financial_reports` + `pdf_extracted_text` from `market.db`. 39 tests, tsc clean, zero-foreign. **BUT** QA on REAL data found `GET /api/bctc-inspect/docs` returned `count:0`: all 14 real `financial_reports` rows had `pdf_path=NULL` (every row entered via the news-inference fallback path `fetchParseAndStoreBctc.ts:645 tryNewsChainFallback`, which hardcodes `pdfPath:null`); the architect's `WHERE pdf_path IS NOT NULL` filter returned zero rows → empty list. Recurring-bug-escalation: 2nd straight assume-the-data-shape defect.

**REOPEN-2 (architect re-root-cause `0245ff4c` → dev-mcp-server `69da9d01` → QA PASS `3098c69d`):** Architect re-spec — backfill + serve-time safety net. dev-mcp-server added idempotent `backfillBctcPdfPaths(db, pdfDir)` (two-pass filename-token matcher links the 14 rows to 17 on-disk PDFs by `action_code+year+quarter`, zero-guess on ambiguity), removed the `WHERE pdf_path IS NOT NULL` filter so the LIST shows ALL 14 real rows with per-row `has_pdf`/`has_ocr` flags, and added a secondary OCR join (`parsePdfFilenameTokens` → `pdf_extracted_text.filename`) for NULL-path rows. 5 files all `apps/mcp-server/`, zero-foreign, 64 tests, tsc clean, write-safety = `UPDATE pdf_path` only (idempotent guard, no INSERT/DELETE).

**REOPEN-2 RE-SIGN (PO, 2026-05-24T19:34Z): RATIFIED on REAL data.** QA REOPEN-2 re-verify against the REAL deployed container (mcp-server rebuilt from `69da9d01`, running on port 3000): `GET /api/bctc-inspect/docs` → `count=14` real docs (NOT 0, NOT 15,552 junk); `has_pdf:true`=12 (architect bar ≥10 MET; 2 VCB rows left NULL by zero-guess ambiguity, still shown w/ OCR), `has_ocr:true`=14, `anomaly_decimal_shift`=7. Playwright headless on `http://localhost:3000/api/bctc-inspect`: select `VNM Q4 2025` → **LEFT** = real 4.1MB VNM PDF rendered by pdf.js (CÔNG TY CỔ PHẦN SỮA VIỆT NAM cover, signed stamp visible); **RIGHT** = DECIMAL-SHIFT ANOMALY banner (OCR `net_profit 0.0001 M VND` vs API `2,840,370 M VND`) + real Vietnamese OCR text. Safety (UUID-400/traversal-404/doc_not_found-404), regression (frozen pdf-extractor dashboard + `pilot-status-pdf-extractor.json` empty diff; 0 PI3 regressions), and write-safety all PASS. Screenshot `/tmp/qa-reopen2-verified.png`. **User acceptance condition — GENUINELY MET on real data:** the VNM decimal-shift bug is visible BY EYE beside the rendered real PDF — the literal user intent. PO independent spot-check (disk+git, pre-trust): `69da9d01`/`0245ff4c`/`3098c69d` all zero-foreign; deliverable files on disk. **PILOT UNTOUCHED** — pdf-extractor SCALE pilot stays DONE 12/12 frozen; `pilot-status-pdf-extractor.json` not edited. Sign-off signal `docs/signals/po-pdf-inspect-reopen2-signoff-20260524T193400Z.json` (supersedes the premature first close's done-condition). **PIPELINE: complete.**

**Out-of-scope follow-ups surfaced (do NOT block this sign-off):**
- **(i) PIPELINE DEFECT — dev-mcp-server task:** `fetchParseAndStoreBctc.ts:645 tryNewsChainFallback()` hardcodes `pdfPath:null` even when a matching PDF exists on disk. The REOPEN-2 startup backfill links them retroactively, but new news-inference rows will keep entering with `pdf_path=NULL` until the fallback path is enhanced to scan for an existing on-disk PDF at insert time (`findExistingPdf(action_code, year, quarter, pdfDir)`). Data-quality fix, NOT inspector scope.
- **(ii) PROD-DATA POLLUTION — ops/dev cleanup:** pdf-extractor's `pdf_extractor.db` holds 15,570 `status=failed` / `example.com` test rows leaked into the production `market_data` volume during SCALE-pilot factory runs. Zero impact on the inspector (it reads `market.db`, never `pdf_extractor.db`) but the prod volume should be truncated/cleaned.

**Notes:**
- **Binding (Day-0, every agent):** explicit-file staging (`git add <path>`, never `-A`/`.`); no `--force`/`--no-verify`/`--no-gpg-sign`; NO `git push` (user owns); all on `main` (NO branches); `git show --stat HEAD` shows zero foreign files (heavy fleet commit-race). Never ask user to run/deploy — spawn agents.
- **Frozen (R6):** `pilot-status-pdf-extractor.json`, sandbox runner, `dashboard/traces.js`, the 3 sandbox panels, `trust-contract.spec.js` — NOT touched by this sprint.
- **Security-Clause distinction:** the viewer is a REAL served surface that legitimately reads `/app/data` PDFs + extraction store via the app process — by design, NOT a sandbox zero-credential violation.
- **R4 zone-gate:** default single zone. One read-only SELECT-only mcp-server route allowed ONLY if architect proves the user-meaningful parsed fields live exclusively in mcp-server's BCTC DB; if added, zone=`multi`, dev unstages any other mcp-server file.

---

## Follow-On Enhancement — Kinh-Dich 64-Quẻ Trading Reference (KD-QREF)

**Status:** Opened 2026-05-24 (PO dispatch signal `po-kinh-dich-que-reference-20260524T170814Z.json`). POST-PILOT ENHANCEMENT — kinh-dich pilot stays DONE 12/12 + frozen; does NOT reopen any goal. User request: add a browsable 64-Quẻ reference with market-trading descriptions (translated from `kinhdich_logic/que_convert/`) to `apps/kinh-dich-service/dashboard/index.html`. Decision: `docs/po-decisions/2026-05-24-kinh-dich-que-reference-dashboard.md`. Spec + ACs: `docs/handoffs/TASK_KD-QREF.md`. Zone: `apps/kinh-dich-service/` (single). Binding trust gate: `dash-check.mjs` must stay exit-0 (no red dots / JS errors / category-chip leaks).

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| KD-QREF-1 | Design data asset (Go SSOT) + dashboard integration + render contract (trust-gate safe) | HIGH | TASK | architect | docs/handoffs/TASK_KD-QREF.md | DONE | — |
| KD-QREF-2 | Implement: populate 64-que `queReference` Go asset (translate/reframe) + emit `que-reference.js` + dashboard section | HIGH | TASK | dev-kinh-dich | docs/handoffs/TASK_KD-QREF.md | DONE | — |
| KD-QREF-3 | Verify: 64 que render + detail views + `dash-check.mjs` exit-0 + source spot-check + diff-scope | HIGH | TASK | qa | docs/handoffs/TASK_KD-QREF.md | DONE | — |
| KD-QREF-EXIT | PO sign-off vs spec; main terminal commits (commit-mutex enum defect — dev agents can't acquire) | HIGH | GATE | po | docs/handoffs/TASK_KD-QREF.md | DONE | — |

**Notes:**
- **CLOSED 2026-05-24T17:39Z** — PO signed off (DoD met, all 5 ACs + 8 QA checks PASS). Pilot stays DONE 12/12 frozen (no goal reopened). Main terminal commits the in-tree work. Sign-off record: TASK_KD-QREF.md `[PO]` section.
- WIP=2 fleet cap still applies (stock-price + kinh-dich pilots). This enhancement is a SINGLE-ZONE chain, not a pilot; dispatch KD-QREF-1 when capacity allows.
- Ambiguities resolved by PO: (A1) bilingual English-primary, VN name/glyph verbatim; (A2) one fixed shape for all 64 = summary + detail w/ 6-phase; (A3) Go data asset SSOT, emitted to dashboard (never hand-typed HTML); (A4) additive panel, honest-green preserved.

---

## Follow-On Enhancement #2 — Kinh-Dich 64-Quẻ Trading Reference EN/VI Language Switch (KD-QREF-LANG)

**Status:** OPEN 2026-05-24T18:51Z (PO self-initiated from user feature request, routed by main terminal; dispatch signal `po-kd-qref-lang-20260524T185115Z.json`). FOLLOW-ON to KD-QREF (bilingual EN-primary panel shipped `0b401124`, data regenerated `e9608167`). POST-PILOT enhancement #2 on the same `.qref-*` panel — kinh-dich pilot stays DONE 12/12 FROZEN; `pilot-status-kinh-dich.json` NOT edited, no goal reopened. User request: full EN view + full VI view, user-toggled. Decision: `docs/po-decisions/2026-05-24-kinh-dich-que-reference-language-switch.md`. Spec + ACs: `docs/handoffs/TASK_KD-QREF-LANG.md`. Zone: `apps/kinh-dich-service/` (single). Binding gate: `dash-check.mjs` exit-0 with toggle present; 3 trust panels + 17 sandbox dots + modal + `sandbox-traces.js` UNCHANGED.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| KD-QREF-LANG-1 | Design i18n shape (Go struct `{en,vi}` + emit + render/toggle/localStorage contract, trust-gate safe). DESIGN ONLY. | HIGH | TASK | architect | docs/handoffs/TASK_KD-QREF-LANG.md | OPEN | — |
| KD-QREF-LANG-2 | Implement: 64 × `{en,vi}` every textual field from `que_convert/*.md`; re-emit `que-reference.js`; wire EN\|VI toggle + localStorage + localized labels in `index.html`. | HIGH | TASK | dev-kinh-dich | docs/handoffs/TASK_KD-QREF-LANG.md | BLOCKED | KD-QREF-LANG-1 |
| KD-QREF-LANG-3 | Verify: 64 × both langs no-gap, toggle swaps whole panel both ways, persistence, `dash-check.mjs` exit-0, frozen-surface diff-scope, source spot-check. Emit `qa-kd-qref-lang-<UTC>.json`. | HIGH | TASK | qa | docs/handoffs/TASK_KD-QREF-LANG.md | BLOCKED | KD-QREF-LANG-2 |
| KD-QREF-LANG-EXIT | PO sign-off vs ACs; main terminal commits in-tree work (commit-mutex enum defect). | HIGH | GATE | po | docs/handoffs/TASK_KD-QREF-LANG.md | BLOCKED | KD-QREF-LANG-3 |

**Notes:**
- **Architect hop REQUIRED** (PO decision): the i18n shape (`localized{en,vi}` nested vs paired `*En`/`*Vi`) + emitted-key migration (breaking-rename vs additive) + outcome-gloss location (data vs closed-enum JS map) affects both the Go struct AND the JS render — getting it right once avoids a rename-churn cycle. Architect appends design notes + per-task ACs to the handoff, then dev-kinh-dich implements.
- PO decisions D1–D5: (D1) default EN; (D2) `localStorage["kd-qref-lang"]`, file:// safe, try/catch → EN fallback; (D3) `.qref-*` panel ONLY, trust panels stay frozen English; (D4) both langs in Go SSOT → emitted `que-reference.js`, never fetched/hand-typed; (D5) EN\|VI control in `.qref-header`, static labels/headers/legend also localize, no `dot-*`/`.category-chip`/"not wired"/fetch/CDN.
- VI content reused/lightly-trimmed from `que_convert/*.md` (schema verified consistent across files 01/29/SCHEMA.md) — NOT machine-retranslated from English. Field→source map in handoff.
- Commit reality: dev-team agents cannot acquire commit-mutex (gateway absent + `task_claim` enum lacks the kind). Work stays IN-TREE; commit manifest at EXIT; MAIN TERMINAL commits (as KD-QREF `0b401124`).

---

## Phase 0 Backlog (Stock-Price Fleet Pilot 3)

**Status:** Opened 2026-05-23 (PO dispatch signal po-pilot3-stock-price-chartered-20260523T220944Z.json). Phase 0 scope: 6 deliverables (brownfield inventory, R-CGO confirmation, bug-inventory entry, agent-flow + G12 DoD baking, anchor commit, phase-1 task plan). WIP limit enforced: max 2 In Progress. Sprint deadline: 1 sprint (2026-05-24 delivery expected). Exit gate: all 6 deliverables + architect verification signal before PO approval of Phase 0→Phase 1 transition.

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| P0-SP-1 | Brownfield inventory of apps/stock-price (architecture audit + R-CGO feasibility) | HIGH | TASK | architect + system-auditor | docs/handoffs/TASK_P0-SP-1-brownfield-inventory.md | READY | — |
| P0-SP-2 | Bug-inventory entry: stock_price_baseline (G10 metric) | HIGH | TASK | system-auditor | docs/handoffs/TASK_P0-SP-2-bug-inventory-entry.md | READY | — |
| P0-SP-3 | Agent-father: confirm dev-stock-price.md + bake dev-stock-price flow with G12 DoD Gate + CGO/Fence rules | HIGH | TASK | agent-father | docs/handoffs/TASK_P0-SP-3-agent-flow-baking.md | READY | — |
| P0-SP-4 | Set anchor commit + update pilot-status SSOT | MEDIUM | TASK | pm | docs/handoffs/TASK_P0-SP-4-anchor-commit.md | READY | P0-SP-1, P0-SP-2, P0-SP-3 (all deliverables before anchor) |
| P0-SP-5 | R-CGO Confirmation: verify primitives + module + sandbox build CGO_ENABLED=0 (binding risk gate) | CRITICAL | TASK | dev-stock-price | docs/handoffs/TASK_P0-SP-5-r-cgo-confirmation.md | READY | P0-SP-1 (brownfield R-CGO feasibility) |
| P0-SP-6 | Phase-1 task plan authoring (architect) | HIGH | TASK | architect | docs/handoffs/TASK_P0-SP-6-phase1-task-plan.md | READY | P0-SP-1, P0-SP-2 (brownfield + bug-inventory inputs) |
| P0-EXIT-GATE | Phase 0 exit gate verification (architect signal) | CRITICAL | GATE | architect | — | READY | P0-SP-1..6 all DONE |

**Notes:**
- **WIP=2 cap (fleet pilot):** max 2 READY→IN-PROGRESS at once; stock-price + kinh-dich (pilot-4) capped together at WIP=2
- **Parallel dispatch eligible:** P0-SP-1 + P0-SP-2 + P0-SP-3 + P0-SP-5 + P0-SP-6 are independent; P0-SP-4 depends on all others (sequential last)
- **R-CGO critical:** P0-SP-5 is a BINDING risk gate (HIGH severity); if BLOCKED, Phase 1 cannot proceed without architect re-cut
- **Architect sign-off required:** P0-EXIT-GATE requires architect verification signal before PO approves Phase 0→Phase 1
- **Charter reference:** docs/architecture-briefs/2026-05-23-stock-price-factory/pilot-charter.md §Phase 0 + §CGO Boundary Clause

---

## Phase 2 Backlog (Technical-Analysis Pilot)

**Status:** Expanded 2026-05-23 by architect. WIP limit enforced: max 2 In Progress. Sprint deadline: 2026-07-03 (6 sprints from kickoff 2026-05-23).

| Task ID | Title | Priority | Type | Owner | Handoff | Status | Blocked by |
|---------|-------|----------|------|-------|---------|--------|-----------|
| P2-F2 | agent-father inserts dashboard-green DoD step in dev-technical-analysis flow | HIGH | TASK | agent-father | docs/handoffs/TASK_P2-F2.md | RECONCILED-STALE → READY (marker was IN-PROGRESS but NO live lock backs it 2026-05-26T21:13Z PO triage; not re-dispatched this tick) | — |
| P2-A1 | Author `.golangci.yml` with Fence-A/B/C depguard rules | HIGH | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-A1.md | RECONCILED-STALE → READY (marker was IN-PROGRESS but NO live lock backs it 2026-05-26T21:13Z PO triage; not re-dispatched this tick — parallel LF-FIX active, deferred) | — |
| P2-B0 | Brownfield inventory scan: all TS TA callers in mcp-server | MEDIUM | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-B0.md | DONE 2026-05-23 (c175f745) | — |
| P2-B1 | Rewire TA callers to HTTP (assembleBriefing + tool handler + type fixes) — SCOPE EXPANDED per B0 audit | HIGH | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-B1.md | READY (P2-B0 done — next-up after A1/F2 land per WIP=2 rule) | P2-B0 (done) |
| P2-A2 | Add `go-lint` CI job to `.github/workflows/ci.yml` | HIGH | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-A2.md | PENDING | P2-A1 |
| P2-A3 | Verify CI green on clean codebase (no violations) | HIGH | TASK | qa | docs/handoffs/TASK_P2-A3.md | PENDING | P2-A2 |
| P2-B2 | Move `technicalIndicators.ts` domain service to `_deprecated/` | HIGH | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-B2.md | PENDING | P2-B1 |
| P2-D0 | Preflight: verify bug-inventory.json has ≥1 TA candidate | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-D0.md | PENDING | — |
| P2-A4 | Deliberate-violation artifact: prove CI red/green cycle | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-A4.md | PENDING | P2-A3 |
| P2-B3 | Remove all "TODO: migrate" comments from mcp-server + technical-analysis | MEDIUM | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-B3.md | PENDING | P2-B2 |
| P2-D1 | Design and document bug-injection spec | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-D1.md | PENDING | P2-D0, P2-F1 |
| P2-B4 | Integration test: TA MCP tool end-to-end via Go service | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-B4.md | PENDING | P2-B3 |
| P2-D2 | QA injects bug; dispatches dev-technical-analysis with dashboard scenario only | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-D2.md | PENDING | P2-D1 |
| P2-E1 | QA designs scenario pair A + B (shared input shape, regression canary) | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-E1.md | PENDING | P2-F1 |
| P2-D3 | dev-technical-analysis fixes bug (≤2 cycles); dashboard GREEN | MEDIUM | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-D3.md | PENDING | P2-D2 |
| P2-E2 | QA injects bug A; dispatches dev-technical-analysis | MEDIUM | TASK | qa | docs/handoffs/TASK_P2-E2.md | PENDING | P2-E1 |
| P2-C | G9 async user verification gate (PO-owned) | LOW | TASK | po | docs/handoffs/TASK_P2-C.md | PENDING | — |
| P2-E3 | dev-technical-analysis fixes A (triggers B red); fixes B in same cycle; both GREEN | MEDIUM | TASK | dev-technical-analysis | docs/handoffs/TASK_P2-E3.md | PENDING | P2-E2 |
| P2-F3 | QA reads flow file, confirms DoD step, counts 3-streak tasks | LOW | TASK | qa | docs/handoffs/TASK_P2-F3.md | PENDING | P2-D3, P2-E3 |

**Notes:**
- P2-F1 (architect brief) completed 2026-05-23 — not included in dispatch queue (architect-owned, non-delegable)
- P2-F2 (agent-father flow edit) IN-PROGRESS — critical path; must complete before P2-D2 + P2-E2 dispatch so streak tasks accrue under the rule
- P2-A1 IN-PROGRESS (dev-technical-analysis); P2-B0 DONE
- **PO next-dispatch gates (updated 2026-05-23 cycle 2):**
  - After P2-F2 lands → dispatch P2-D1 + P2-E1 to qa
  - After P2-A1 lands → dispatch P2-A2 to dev-technical-analysis (sequential)
  - After P2-A3 green → dispatch P2-B2 chain (deletion can proceed once fence proven)
  - **P2-B1 is READY now (P2-B0 done) but PO is holding it back** to keep dev-technical-analysis WIP ≤ 2; will dispatch when P2-A1 lands
  - **P2-B1 SCOPE EXPANDED (PM 2026-05-23T22:35Z):** Based on P2-B0 audit finding (signal file main-router-P2-B0-finding-20260523T223500Z.json), B1 now includes assembleBriefing.ts rewire (SEV-2 gap) + DailyCandle type fixes (SEV-3). Handoff updated: docs/handoffs/TASK_P2-B1.md. AC count 6→10, estimate 45min→1h. Pre-step: git tag p2-b-pre-delete before P2-B2 deletion commit.
  - After P2-D3 lands → dispatch P2-E1/E2 (regression pair needs G10 fix pattern visible)
  - After P2-D3 + P2-E3 → dispatch P2-F3 to qa (streak verification)
- G4 (fence): P2-A1 → P2-A2 → P2-A3 → P2-A4 (sequential, same owner, ~45 min)
- G5 (deletion): P2-B0 ✓ → P2-B1 → P2-B2 → P2-B3 → P2-B4 (sequential, same owner + QA, ~2.5 hours)
- G10 (AI-fix): P2-D0 → P2-D1 → P2-D2 → P2-D3 (sequential, ~2 hours + 1h agent fix)
- G11 (regression): P2-E1 → P2-E2 → P2-E3 (sequential, ~2 hours + 1h agent fix)
- G12 (flow rule): P2-F2 → (gates P2-D2/E2) → P2-F3 after P2-D3+E3 complete
- G9 (async): P2-C independent, no blocker on dev work — send DEFERRED-CYCLE-2 (ops blocker; signal po-20260522T225100Z.json)

---

## News-Fetch SCALE Pilot (Phase 0/1/2) + NF-LD follow-ons — ARCHIVED 2026-05-26 (PO dev-team :07 triage)

**Status: CLOSED.** News-Fetch SCALE Pilot DONE 12/12 verdict=scale (6th pilot to scale, closed 2026-05-24T09:45Z `41e4b2ce`). NF-LD live-data view DONE+CLOSED. NF-LD-4 served-dashboard + NF-LD-5 refresh-button PO-signed-off (terminal gate = ops rebuild/prove). Full per-task ledgers archived → `docs/TASKS_ARCHIVE.md § Archive — Added 2026-05-26 (net-reduce closed news-fetch + NF-LD ledgers)`. Authoritative state: `docs/data/pilot-status-news-fetch.json` (status=DONE, goalsEarned=12) + `docs/handoffs/TASK_NF-LD.md`. **Outstanding (non-blocking):** NF-LD-4-OPS + NF-LD-5-OPS ops-deploy proofs — the FETCH-ANALYZE ops rebuild this tick will also pick these up if the running mcp-server image still predates `15d9b034`.


---

## Phase 1 Backlog (pdf-extractor SCALE Pilot) — OPEN 2026-05-24

**Status:** OPEN 2026-05-24T09:30Z (PO Phase-0 exit gate close; pilot-status phase=1 ACTIVE). Plan: `docs/architecture-briefs/2026-05-24-pdf-extractor-factory/phase-1-task-plan-python.md` (10 tasks, est 9.3h). **WIP=1 strictly sequential** (pilot-status `phase1.wip_limit`). Owner = `dev-pdf-extractor`, zone `apps/pdf-extractor/` ONLY. Language Python (locked Day 0). G12 DoD gate effective for every streak task. BCTC freeze: all 10 tasks CLEAR.

**HARD GATE — P1-B1 (and all primitives) MUST NOT dispatch until:**
- AC-5 PASS: `env | grep -iE "DB_PATH|VPS_|VINAHOST|STORAGE_DIR|OCR|TESSERACT|TOKEN|SECRET|API_KEY|PASSWORD"` returns EMPTY in sandbox process env.
- AC-6 PASS: `grep -rniE "db_path|vps|vinahost|storage_dir|token|secret|api_key|password" apps/pdf-extractor/sandbox/` returns 0 matches.
- dev-pdf-extractor pastes both AC-5 + AC-6 terminal output (literal) into `docs/handoffs/TASK_P1-A1.md` before any P1-B1 dispatch.

| Task ID | Title | Goals | Owner | Status | Blocked by |
|---------|-------|-------|-------|--------|-----------|
| **P1-A1** | Sandbox runner `apps/pdf-extractor/sandbox/runner.py` (JSON-in → trace-JSON-out, zero credentials, zero pdfplumber/pytesseract) + scenario dir layout. FastAPI composition root ≤80 logical lines. 7 ACs. **ZERO-CREDS gate before P1-B1.** | G7, G12 | dev-pdf-extractor | **Todo** | — |
| P1-A2 | Scenario directory layout: `scenarios/{primitives,modules,service}/` + README | G1, G7 | dev-pdf-extractor | Blocked | P1-A1 |
| P1-A3 | `main.py` shrink ≤80 logical lines (extract `os.makedirs` → `infrastructure/startup.py`, lifespan → `infrastructure/lifespan.py`) | G3 | dev-pdf-extractor | Blocked | P1-A2 |
| P1-B1 | First primitive `validate-financial-figures` — move from `domain/services.py`, 3 scenario JSONs. G12 streak #1. **Blocked until P1-A1 zero-creds gate (AC-5+AC-6) confirmed PASS.** | G1, G12 | dev-pdf-extractor | Blocked | P1-A3 + P1-A1-zero-creds-gate |
| P1-B2 | Second primitive `decimal-normalizer` — READ-ONLY mcp-server archaeology for fixture values, ZERO mcp-server write. 3 scenario JSONs. | G1, G12 | dev-pdf-extractor | Blocked | P1-B1 |
| P1-C | Module stub `financial-reports` — Protocol ports + `FinancialReportsModule` + mock ports in tests. G12 streak #2. | G2, G12 | dev-pdf-extractor | Blocked | P1-B2 |
| P1-D | Module scenario JSON `scenarios/modules/financial_reports/` — ≥1 multi-primitive story. | G2, G7, G12 | dev-pdf-extractor | Blocked | P1-C |
| P1-E1 | Dashboard stub `apps/pdf-extractor/dashboard/index.html` — 3 panels NOT-RUN, SI-2 boundary comment. G12 streak #3. | G6, G8, G9, G12 | dev-pdf-extractor | Blocked | P1-C, P1-D |
| P1-E2 | Edit-rerun handler + G7 all-4 sub-gates (env audit + scenario grep + zero-infra import + edit→rerun cycle). Evidence in handoff. | G7, G8, G12 | dev-pdf-extractor | Blocked | P1-E1 |
| P1-G | QA close-gate: sandbox all-green, zero-creds audit PASS, dashboard 3-panel PASS, G12 streak-3 confirmed. Emits `qa-pdf-extractor-phase1-gate-<UTC>.json`. | closes Phase 1 | qa | Blocked | P1-E2 |

**Notes:**
- **WIP=1 sequential:** dispatch one task at a time. Each unblocks only when predecessor is DONE with evidence in handoff.
- **G12 streak tasks:** P1-B1 (streak#1) + P1-C (streak#2) + P1-E1 (streak#3). None marked DONE without sandbox-green evidence pasted in handoff.
- **P1-B2 READ-ONLY rule:** if any `apps/mcp-server/` file appears in `git diff --cached`, task is BLOCKED — dev must unstage before commit.
- **Handoff:** `docs/handoffs/TASK_P1-A1.md` (created this cycle — full ACs + Architect design notes).
- **Charter ref:** `docs/architecture-briefs/2026-05-24-pdf-extractor-factory/pilot-charter.md`. Plan: `phase-1-task-plan-python.md`.
