# TASKS — VN Market Intelligence MCP

> **Active sprints only.** Historical: `docs/TASKS_ARCHIVE.md` | WIP≤2 | Decisions: `docs/po-decisions/` | Goals: `docs/SPRINT_GOAL.md`

## Closed sprints (live follow-ups only — full records in briefs/archive)

- BCTC-TRUST-RED ✅ e0c900d0 → FU-TRUST-REFRESH ✅ EXIT-WITH-CAVEAT 2026-05-31 → BANK-AWARE-BCTC ✅ PO-EXIT 2026-05-31 (941bf552 HYBRID; QA 040409f9; live img 7f413304; ACB B02-TCTD serves raw + FPT B01-DN 0-regression, both router-raw-verified; 🔄 FU-BANK-CODECOL below) · BCTC-AI-INPUT-TAB ✅ b4ed9266 · BCTC-HUMAN-CONFIRM ✅ 441f8e18 · BCTC-AGENTIC-REFINE ✅ 🔄 AR-FU-DETERMINISM DEFERRED · DATA-PIPELINE-INTEGRITY ✅ 🔄 FU-C DEFERRED ⏳ FU-MON (Monday) · BCTC-TABLE-BOUNDARY ✅ FU-BTB-OCR registered
- DYN-WF-FOUNDATION ✅ DWF-EXIT 2026-05-31 (84643927…eee22112) · DWF-PHASE1 ✅ P1-PO-EXIT 2026-05-31 (5a19485e…38d241c5) ⛔ Phase 3+ DEFERRED · MACRO-CMDTY-DELTA ✅ PO-EXIT 2026-05-31 (e510e5df…fdc17265) 🔄 FU-MON · FF-DEAD ✅ fixed 0cbce0b4 ⏳ FU-MON · PEK-INTEGRATE ✅ DONE-PENDING-G9 2026-05-28 (12 corpus has_pek:true; awaits USER verbal G9)
- VPS-NEWS-CAFEF-VNECO ✅ 2026-06-01 (814088b0+91bdb305, QA APPROVED; P1 is_blocked() fix restored cafef+vneconomy feed; P2 /proxy/article-body on VPS:8765, 5k/8k body extract). 🔄 FU-OPS-CAFEF-1/2 (spot-check ≥2 cycles cafef-market/biz>0 zero PERMANENTLY_BLOCKED; verify beautifulsoup4 + extraction cap) · 🔄 FU-DEV-CAFEF-1 (wire /proxy/article-body into /api/push-news = next sprint candidate) · **RESUME-ECONOMY** ✅ 2026-06-01 (b38ac812; brief `…/2026-06-01-context-resume-economy.md`): DASHBOARD delta-read + mandatory PRUNE + pipeline-state v2 head/narrative + main.md Step 0b head-only — ALL 3 phases shipped same session (router raw-verified). 🔄 RE-CAP-1 below = sole residual; stale cache linecount self-heals next write (brief §5).
- PROSE-TEXT-LOSS (Task #18) ✅ PO-EXIT 2026-06-01 — DISPLAY-ONLY (no data lost): bctc-inspect blanked OCR on prose pages (pek_coverage_gap=true) — handleBctcInspectOcr never fell back to pdf_extracted_text (holds every page). Brief fe9683c5 (Layer C only) · fix a10448b0 (gap-branch SELECT text_content/confidence FROM pdf_extracted_text; html renders it; +5 DV DV-1 RED→GREEN) · OPS rebuild img 33e4386c≠4446a6e9 healthy. QA APPROVED `reports/TASK_REPORT_PROSE-DEV-1.md` (cef5fca5): tsc 0-new · DV 5/5 genuine-RED · neighbors green · LIVE FPT e8ea3df5 p1=2081ch/p2=134ch + pek_coverage_gap:true · SHA differs. ROUTER RAW-VERIFY: read live /ocr — FPT p1=2081ch real, p2=134ch, table pp.22/25 unaffected. 🔄 OUT-OF-SCOPE (pre-existing): pp.22/25 identical (PEK segment-window) · VCB refine_status=PENDING · FU-BANK-CODECOL · FPT YoY 2025-Q4 GM-100% → FU-TRUST-REFRESH.
- VPS-DEPLOY-PLACEHOLDER-GUARD ✅ 2026-06-01 (DECISION-A consolidate; T2/T3/T4 ed967839, T5 ops live-deploy 5ba761c6 — 9 services active on Vinahost + article-body-fetcher/bs4, GUARD-1 clean; T1 recon folded into T5). T6-QA APPROVED 7/8 raw (71cce86b); AC-6 closed by `.env` tombstone reword (working-tree, .env stays gitignored). ⚠️ **SECURITY (contained):** fixer `git add -f`'d the gitignored `.env` (ALL secrets incl. Vinahost root pw) into UNPUSHED commit d77f4294 — router caught it via raw diff (`new file mode`), `reset --soft` + untracked, `.env` purged from all history, never reached origin (was 139-ahead) → **no exposure, no rotation**. 🔄 FU-FIXER-NO-FORCE (HIGH, agent-father): harden fixer + all dev agents against `git add -f`/force-add of gitignored paths — the no-`--force` binding must be enforced in agent flows, not just documented.
- FRONTEND-BCTC-TAB ✅ PO-EXIT 2026-06-01 — operator "move this page to new tab on :3001": mcp-server BCTC Inspect viewer (`:3000/api/bctc-inspect`) now a "BCTC Inspect" dashboard tab on :3001 via server-side proxy (Approach A2), ZERO mcp-server edits. Brief `…/2026-06-01-frontend-bctc-inspect-tab.md` (A2 locked); ROUTER raw-verify caught 2 brief defects pre-dev (upstream path keeps /api prefix; 2nd proxy /api/bctc-eval/* needed for eval tab) — both in shipped code. FBT-DEV 80f2911b (4 files apps/frontend/ only, +205L: dashboard.bctc-inspect.tsx raw-Response HTML proxy · api.bctc-inspect.$.tsx binary-safe splat · api.bctc-eval.$.tsx splat · dashboard.tsx 1 NAV entry; tsc clean). FBT-OPS frontend rebuilt clean (img f768593059b2 healthy :3001, mcp-server untouched). FBT-QA 5046a7da APPROVED 5/5 gates (PDF 16.6MB + PNG 273KB MD5-identical :3001 vs :3000 · 7 data sub-paths parity · eval 200 parity · human-confirm POST round-trip proven via bun:sqlite DB read+reset · 0 mcp-server regression, distinct img SHA, :3000 still 200). 🔄 OUT-OF-SCOPE (pre-existing data-coverage, NOT a proxy bug): 13 docs lack rasterized PNG → page-image 404 on those (only FPT/ACB Q1 have PNGs). NOTE: tab label "BCTC Inspect" (English, casing consistency).
- TOOL-SURFACE-HYGIENE ✅ 2026-06-01 — surface cleaned; live toolCount=154, `get_market_hexagram` ABSENT (qa raw `tools/list`, not /health echo). BA/ARCH ✅ (FR-1=1b deregister · FR-2/3/4 description-clarify · FR-5 WONTFIX). TSH-1 ✅ c29f36cf · TSH-2/3/4 ✅ f4da532f · TSH-5 ✅ project-stats 154 consistent (643d4619) · TSH-6 ✅ 9b895ffd (kinh-dich honest-omit: service-down→OMIT block + `logger.warn` real cause; 200-data-short→honest VN line preserved; mcp-server rebuilt `--no-cache`; router+qa live-verified `get_market_snapshot` has NO trailing fallback while :5005 down) — **also resolves MSG-2** (per-code N+1 fallback lines eliminated). 🔄 TSH-7 (BACKLOG, UX, optional, needs BA): watchlist default for no-codes `get_market_snapshot`.

---

## Sprint ORCH-STATE-CONSOLIDATE — single JSON SSOT, delete 4 markdown files

**Status:** OPEN 2026-06-01 (operator-greenlit). **Pri: HIGH.** **Zone: multi** (architect-split done in brief). BA/ARCH ✅ DONE — authoritative blueprint `docs/architecture-briefs/2026-06-01-orch-state-consolidate.md` (v2, 46632eb0). **Open decision DEFAULTED to (A)** by PO: D4-R4 concurrent-commit alarm stays 30s on `orch-state.json` (more sensitivity, slightly more noise). Subsumes the `2026-06-01-dashboard-state-sync.md` pipeline endpoint — the `/api/orchestration` endpoint reads `orch-state.json` directly; JSON-twin concept OBSOLETE (the JSON IS the SSOT now). **Goal:** one SSOT file `docs/data/orch/orch-state.json` (sections head/task_board/signal_queue/narrative); delete TASKS.md + TASKS_ARCHIVE.md + DASHBOARD.md + DASHBOARD_ARCHIVE.md; humans read state via localhost:3001 dashboard / raw JSON.

> **SELF-REFERENTIAL SURGERY** — the dev-team executes THROUGH these very files. TASKS.md remains the board until OSC-2 flips it. Sequencing constraints below are NON-NEGOTIABLE.

**Hard constraints (enforce on every task):** NO branches (main only) · explicit-file staging, NO `git add -A` / NO `--force` (FU-FIXER-NO-FORCE; .env never staged) · frontend → api-gateway:4000 ONLY (never microservice ports, never bind-mount docs/ into frontend) · signal_queue stores `summary`≤120 + `payload_ref` only (never raw payloads) · NEVER parse markdown in an HTTP handler · detector/qa agents PLAN-ONLY (AUD-ND-1; ops is the only remediation agent) · ops REBUILD not restart mcp-server (`up -d --build mcp-server`) · single-service ops, never mass-start (16GB host-memory-panic) · atomic temp-file-then-rename write protocol (brief §2.3) mandatory for every `orch-state.json` writer.

- 🔄 **OSC-1 — Create `orch-state.json`, migrate pipeline-state + TASKS + DASHBOARD content.** `zone: docs/` (agent-father). `mkdir -p docs/data/orch/`; write `orch-state.json` (`_schema:"v3"`) merging current `docs/pipeline-state.json` head/dashboard_section_cache/narrative/session_handoff_status verbatim + populate `task_board` from current open TASKS.md sprints + `signal_queue` from current DASHBOARD.md NEW rows (summary≤120 + payload_ref, NO raw payloads). DO NOT delete any old file (OSC-2 does that atomically). AC: OSC-1-AC1 `jq . orch-state.json` exits 0 · OSC-1-AC2 all 4 top-level sections present (head/task_board/signal_queue/narrative) · OSC-1-AC3 head fields match current pipeline-state.json head EXACTLY. **Sequencing: FIRST.**
- 🔄 **OSC-2 — ATOMIC code + agent-file + deletion migration (ONE COMMIT).** `zone: apps/mcp-server/ + docs/` coordinated, single committer. (a) dev-mcp-server: update all §4.1 code readers — `1837a-pipeline-state.test.ts` path → `docs/data/orch/orch-state.json`; `tasksMdJanitorJob.ts` L308–310 path+parser (`jq .task_board.tasks[]`), L421 git-log path → `orch-state.json` (30s window kept = DECISION-A), DASHBOARD write → JSON append helper w/ atomic write; `dailyDashboardJob.ts` L495 path+count-from-JSON; `1854a-daily-dashboard-job.test.ts` L47–113 fixture→JSON; `improvementSignalWriter.ts` L30 const + L252–261 writer→`appendSignalQueueRow()` atomic; `1948d-improvement-signal-writer.test.ts` L187/L324 fixture→JSON. (b) agent-father: re-point EVERY §1.1+§2.1 reader site (40+ flows/skills/protocols) — `docs/pipeline-state.json`→`orch-state.json`, `docs/TASKS.md`→`orch-state.json .task_board`, `docs/signals/DASHBOARD.md`→`orch-state.json .signal_queue`; PRESERVE the :07 RETURN write contract (agent-chaining-protocol §PIPELINE_STATE_WRITE — schema fields UNCHANGED, only path+section change). (c) `git rm` the 5 files: pipeline-state.json, TASKS.md, TASKS_ARCHIVE.md, DASHBOARD.md, DASHBOARD_ARCHIVE.md. AC: OSC-2-AC1/2/3 `bun test 1837a-pipeline-state` / `1854a-daily-dashboard` / `1948d-improvement-signal-writer` all exit 0 · AC4 `grep -r "docs/pipeline-state.json" .` → 0 functional · AC5 `grep -r '"docs/TASKS.md"' .` → 0 functional · AC6 `grep -r '"docs/signals/DASHBOARD.md"' .` → 0 functional · AC7 all 5 old files ENOENT. **Sequencing: AFTER OSC-1. HIGHEST RISK — split commit leaves test suite red + pipeline write-wedged. Single committer assembles dev + agent-father changesets, run ALL tests BEFORE commit. Markdown deletion ONLY after every reader re-pointed.**
- 🔄 **OSC-3 — Rewrite signal-dashboard SKILL to target `orch-state.json .signal_queue`.** `zone: .claude/skills/` (agent-father). Rewrite SKILL.md §WRITE (append to `.signal_queue.rows[]`, atomic) / §READ (delta on `.signal_queue._updated_at`+rows[]) / §PRUNE (max 200 rows; archive RESOLVED|READ >7d → `.signal_queue.archive[]`); rewrite `dashboard-protocol.md`. AC: OSC-3-AC1 after §WRITE, `.signal_queue.rows` has the new row, `jq` exits 0 · OSC-3-AC2 §READ returns only NEW rows for requesting agent. **Sequencing: SAME COMMIT WINDOW as OSC-2** (else an active session §WRITE hits deleted DASHBOARD.md — RISK-3).
- 🔄 **OSC-4 — Read-only `GET /api/orchestration` endpoint (dev-mcp-server) + docker mount (ops) + frontend route (dev-frontend).** (a) dev-mcp-server: add read-only handler(s) that serialize `orch-state.json` sections to JSON (DDD per dashboard-state-sync §9: port/usecase/infra-reader/route + server.ts wiring); MUST include `last_updated_iso` for stale badge; expose summary/severity/status only from signal_queue (HC-2, never raw payloads); NO markdown parse. (b) ops: docker-compose mount `./docs/data/orch/orch-state.json:/app/docs/data/orch/orch-state.json:rw` (rw — cron writes signal_queue) + `up -d --build mcp-server` (REBUILD not restart). (c) dev-frontend: `/dashboard/orchestration` (or pipeline/tasks/signals) route + nav entry, SSR loader via api-gateway:4000 ONLY, stale amber banner when `last_updated_iso` > threshold. AC: gateway round-trip `GET /api/orchestration/*` returns live orch-state JSON · frontend renders it on :3001 via api-gateway (never direct :3000) · stale badge fires honestly · 0 raw payload in response. **Sequencing: AFTER OSC-2 lands.**
- 🔄 **OSC-5 — QA: atomic-write PROVEN-RED + reader-migration regression + endpoint contract.** `zone: qa` (PLAN-ONLY, no destructive docker/host ops — ops is sole remediation). PROVE the §2.3 atomic temp-file-then-rename protocol (simulated 2-agent RETURN: no clobber, no sibling-section overwrite, no partial-write corruption — inject the failure to prove it's caught) · reader-migration regression (the 3 bun tests + grep ACs from OSC-2) · endpoint contract (HC-1 api-gateway-only, HC-2 no raw payload) · router-raw-verify-able evidence (read raw VALUES, not badges). AC: atomic-write PROVEN-RED demonstrated · all OSC-2 ACs re-verified live · endpoint contract green. **Sequencing: LAST, after OSC-4.**

**Sequencing diagram:** OSC-1 → OSC-2 (+OSC-3 same window) → OSC-4 → OSC-5. No agent cycles between OSC-1 and OSC-2 (RISK-2 phantom dual-write).

---

## Sprint FLEET-HOST-SAFETY — on 16GB host, fleet must NEVER be `up`

**Status:** OPEN 2026-06-01 (was BACKLOG). **AUD-ND-1 (the destructive-RESPONSE fix) IMPLEMENTED+QA-PASS 2026-06-01 :07 (d30f9221)** — next-priority = **A-01-EXPECTED-SET** (the false-positive SOURCE, MEDIUM) + **DRAIN-INJECTION-SAFE** (HIGH). Zone: agents-architect → agent-father. Root: 4 false-positive auditor triggers, now with permanent data loss (host-panic + data-integrity risk).
- 🟢 **AUD-ND-1 — CRITICAL, IMPLEMENTED 2026-06-01 :07 (d30f9221), QA PASS static+flow-walk.** Chain po→agents-architect (brief `…/2026-06-01-detector-plan-only-safety.md`, 131f0731)→agent-father→qa. PLAN-ONLY invariant (forbidden docker stop/kill/rm/restart, compose, kill/pkill, rm -rf) + emit-signal-not-act contract + incident anchor inserted in system-auditor flow/main.md + init.md + tools-package Bash read-only allowlist. QA flow-walk (line-quoted) confirmed ZERO destructive authored step in any tier + all CRITICAL/WARN paths route to signal+DASHBOARD+BUG+EXIT + invariant positioned before every decision point + allowlist breaks no diagnostic; registration line-1 intact. ⏳ **AUD-ND-1-LIVE-PROVEN-RED (DEFERRED, ops-supervised off-hours):** synthetic false-positive injection → assert auditor emits signal + docker-ps mcp-server start-ts UNCHANGED (NOT run in-gate — would risk re-triggering the docker-stop it guards). ⏳ **SA-FLOW-CAP (follow-up, hygiene):** system-auditor/flow/main.md 494L vs 120L cap (PRE-EXISTING, was 469L before this edit; stale 175L justification) — refactor to linked files preserving the in-flow invariant. Siblings still OPEN: A-01-EXPECTED-SET (false-positive SOURCE), DRAIN-INJECTION-SAFE. **ESCALATION 2026-06-01:** a false-positive system-auditor `docker stop`-ped the mcp-server backend during Monday 2026-06-01 trading hours (~09:00→15:19 UTC). Price pipeline is **live-only, no backfill** → Monday's INTRADAY dataset was PERMANENTLY DESTROYED (only the close survived). This is the **3rd fleet-danger manifestation** and the severity has graduated from "downtime" (recoverable) to "irreversible data loss." The PLAN-ONLY / no-destructive-ops invariant is no longer a nice-to-have — every auditor cycle is now a live data-loss hazard. Bump ahead of all MEDIUM TOOL-SURFACE / MCP-SURFACE work. Original trigger (2026-05-31 21:08Z ENOSPC) + AC unchanged in `docs/SPRINT_GOAL.md`; this row only re-prioritizes. Route po→architect→agent-father.
- 🔄 Other items: DRAIN-INJECTION-SAFE (no shell interpolation of payloads, HIGH) · A-01-EXPECTED-SET (check intended-runtime-set only, MEDIUM) · AUDITOR-SLA-CADENCE (per-source staleness SLA, LOW) · ✅ VPS-SOCAT-PERSIST — CLOSED 2026-06-01: ops replaced hand-PID socat with launchd plist `com.vn-market.socat-bridge` (RunAtLoad+KeepAlive, survives reboot; gateway :4000/health 200) + restarted macro-indicators (Up healthy, get_macro_snapshot live vnIndex 1844.54). Router raw: get_market_snapshot tier-2 OK. **C-TIER1-CEILING (doc-only, now sole residual):** `get_market_snapshot` always returns source_tier:2 (VPS aggregator relay), never tier-1 (direct HOSE/HNX). Decide-and-document, NOT "fix": is tier-1 achievable on this minimal-stack host or is tier-2 the permanent ceiling? Write the answer into a brief; NO separate sprint. (Router live-verified tier-2 2026-06-01 19:12Z.) Full items in `docs/SPRINT_GOAL.md` §FLEET-HOST-SAFETY.

---

## Sprint MCP-SURFACE-GAPS — Router-found MCP defects (intake 2026-06-01)

**Status:** OPEN 2026-06-01. **Zone:** `apps/mcp-server/` (dev-mcp-server) unless noted. Source: router direct-gateway-verify session 2026-06-01 (raw evidence — re-confirmed live by PO). NEEDS BA spec before impl. (TOOL-SURFACE-HYGIENE TSH-1/TSH-6 now landed — MSG-1 rebuild-collision gate cleared.)
- 🔄 **MSG-1 (dev-mcp-server) — FEATURE, HIGH** — no market-wide foreign-flow aggregate. PO raw-verify: `get_foreign_flow {}` → InputValidationError `code` Required (strictly per-ticker); no tool returns the market total net buy/sell (e.g. "khối ngoại bán ròng 633 tỷ, phiên thứ N"). Today every consumer must scrape the web for the headline number the FB/MARKET dish needs daily. **Approach (BA to scope):** either extend `get_foreign_flow` to make `code` optional → market aggregate when omitted, OR add `get_foreign_flow_market` (cleaner, no overloaded contract). Data source: confirm the VPS foreign-flow feed exposes a market total or whether it must be summed server-side from per-ticker rows — may pull in dev-stock-price / VPS foreign-flow source. AC: a gateway call with no `code` returns market net value (tỷ VND) + direction + consecutive-session count; raw-verified via gateway; documented in mcp-tools standard. Pri HIGH (unblocks daily product headline; highest product value of this batch). **Cross-ref A.**
- ✅ **MSG-2 — RESOLVED by TSH-6 (9b895ffd) 2026-06-01** — the per-code N+1 `"Kinh Dịch: Chưa đủ dữ liệu…"` lines were the same bare-catch :5005-down defect; TSH-6 honest-omit eliminates ALL of them (router live-verified `get_market_snapshot` market + stock paths emit 0 fallback lines). No separate fix. (Re-open only if a genuine 200-with-data ever duplicates per-code — footer-scope bug in `appendStockHexagram` loop.)
- 🔄 **MSG-3 (dev-mcp-server) — SURFACE-HYGIENE / DOC, LOW** — phantom-tool discoverability gap, NOT a gateway-registration inconsistency. PO root-caused defect D: `get_market_breadth` and `get_top_movers` return "Tool not found" via the gateway because **they do not exist in the mcp-server source at all** (zero `server.tool` match) — they are NOT in the 154 count, so there is no count-vs-resolve mismatch. The prior agent invented/expected tool names that were never built. `get_foreign_flow` (and breadth-adjacent data) resolve fine. **RESOLUTION:** (a) record in `docs/standards/mcp-tools.md` that there is no market-breadth / top-movers tool (so future agents don't expect them); (b) if market breadth/top-movers is genuinely wanted as product, that is a NEW FEATURE request → fold into MSG-1's BA scoping as a related "market-aggregate tools" family (foreign-flow-market + breadth + movers) rather than a defect. Pri LOW (doc/expectation fix; feature portion defers to product decision). **Cross-ref D — reframed: discoverability gap, not registration bug.**

---

## Sprint ENV-ISOLATION — Fleet-wide test/prod data isolation (split P1/P2)

**Status:** P1 ✅ PO-EXIT 2026-05-31 (9eab754f·89e9b5b8·0c9bed2a, QA cycle-164 APPROVED `reports/TASK_REPORT_EI-P1.md`); P2 ⛔ GATED. **Pri: MEDIUM.** Zone: multi. Brief `…/2026-05-31-fleet-env-isolation-architecture.md` (6e8f3d23). Full shape → `docs/SPRINT_GOAL.md` ENV-ISOLATION §.
- ✅ **EI-P1-1/2/3** — PO raw-verified (not badge): rendered `docker compose config` = 9 `APP_ENV: production` (mcp/pdf/rag/ta/macro/kinhdich/news/stock/alert) + `COORDINATION_DB_PATH` on mcp-server, none on api-gateway/frontend/flaresolverr; both maintenance scripts carry live guard logic (resolved-path print before write + `--force-dev`); `dev-environment.md` (241L) covers start/seed/promote-FK/LanceDB/restore/RISK-5. HCM-DISAMBIG 0-diff, PEK pristine, 3 commits scoped per-file on main.
- 🟢 **P2 (GATE RELEASED 2026-05-31 — FU-TRUST-REFRESH FU-4 data-trust satisfied at FU-EXIT; now schedulable):** EI-P2-1 startup assertion → EI-P2-2 `data_env` ×5 tables → EI-P2-3 `docker-compose.dev.yml` → EI-P2-QA ENV-GUARD-1. Serialize EI-P2-2 mcp-server rebuild vs BANK-AWARE-BCTC BANK-OPS (same zone).
- 🔄 **FU-EI-COMPOSE (backlog, NOT gated):** 2 pre-existing non-P1 items (alert-engine DB_PATH, run-bt7-backfill import path). Compose/scripts only.
---

## Sprint SELF-IMPROVE-GATE — Gated Self-Improvement Loop

**Status:** OPEN — Phase 2 (lane-B code gate) live 2026-05-28. PO APPROVE-WITH-CONDITIONS (062a6569 + ef109a76). **Pri: HIGH.** Zone: `apps/mcp-server/`. ✅ Phase1 (brief `…/2026-05-27-gated-self-improvement-loop.md`) + Phase2 code (selfImproveOrchestratorJob + degradationRules + improveCheckStore, GATE-PROOF PROVEN-RED). 🔄 SIG-FOLLOWUP-DRYRUN (X-1): synthetic dry-run, D-IMPROVE emit path e2e.

---

## Sprint NB-PRUNE-FIX — Notebook prune anchor-format mismatch (fleet bloat)

**Status:** OPEN 2026-05-31 (recurring `context_bloat_breach`; manual prune 41c9ac73). **Pri: HIGH.** Zone: `.claude/skills/` + agent flow `.md` (disjoint from peer `apps/mcp-server/`). Fresh first-fix — normal chain.
- **Defect (call-site/contract mismatch, skill internally correct):** `.claude/skills/notebook-write/SKILL.md` prune+guard (AC-2/3/5) greps ONLY `^## c[0-9]`, but agents emit `## <ISO-ts>`/`## Session:` headings → prune Edits no-op, retention never fires → unbounded growth (ops 5871L, dev-alert-engine 389L, unified-agent 377L, agents-architect 316L; ALL 0 `## c` sections). Notes also inconsistent: po/main.md L126 "OVERWRITE ≤50L" vs developer L125 "append c<NNN>".
- ✅ **NB-PRUNE-1 (developer)** — DONE 2026-05-31 (7166db01). APPROVED by QA. Anchor widened to `^## ` in SKILL.md (104L ≤120L). QA repro (deliberate-violation, 3 fixtures): Session-style 5871L→344L (3 sections retained, AC-5 guard fires correctly); ISO-ts 316L→27L; c-format 166L→58L. Preamble preserved in ISO+c-format fixtures. Exactly-3 no-prune edge case confirmed. Fenced-code-block `## ` over-count risk is theoretical only (0 live occurrences). TODO po/developer invocation-note contradiction: non-blocking (po.md=26L, deferred reconciliation acceptable).
- 🔄 **NB-BLOAT-FLOW-OVERWRITE (developer, ACTIVE FIX 2026-06-01)** — PROMOTED from backlog. system-auditor.md re-breached 26L→**249L** within hours of prune 1013a624 (≥10 prepend notebook commits in one night) → emits `context_bloat_breach` every 30-min Tier-1 audit = highest-frequency drain noise. NB-PRUNE-1 fixed the SKILL anchor but system-auditor STILL PREPENDS per agent behavioral drift vs `docs/agents/system-auditor/flow/main.md` L427 (full-overwrite). Durable FIX = make overwrite instruction unambiguous in the flow (e.g. 'DELETE all prior content; write ONLY current cycle block ≤50L; never prepend') via agent-md-factory discipline. Zone: agent flow `.md` (cross-service). AC: system-auditor.md ≤50L after one audit cycle + no prepend recurrence 14 days + no `context_bloat_breach` for that file 14 days. NOTE: requires agent-md-factory skill (touches agent-system flow) → architect/agent-father chain, but the actual one-file edit is developer-scoped per backlog routing — route via Step 2 planning. · **2026-06-01 :07 EVIDENCE+MANUAL-SWEEP** — `context_bloat_breach` fired for news-scout.md (221L); dispatcher swept all 38 notebooks via claude-manager-helper, manually pruned 5 over-cap (news-scout 221→98 [trigger], bctc-analyst 218→124, qa 243→63, agents-architect 326→37, ops 5914→275); ALL git-safe (dropped sections in HEAD history). PROVES NB-PRUNE-1 anchor-fix did NOT propagate to news-scout (still grew to 6 sections → its flow likely never invokes notebook-write prune). RESIDUAL — 3 files CANNOT reach cap via last-3 prune: ops.md 275 (3-section floor), dev-alert-engine 389L & dev-rag-service 223L (single verbose section). WIDEN this item's AC to ALSO mandate within-section trimming / max-section-size, not only prepend→overwrite.
- 🔄 **RE-CAP-1 (agents-architect → agent-father, MEDIUM)** — sibling cap-compliance item (same skill/flow-.md theme). PROMOTED from RESUME-ECONOMY residual; resolves persistent `context_bloat_breach` ×2. `.claude/skills/signal-dashboard/SKILL.md` = **192L vs 120 cap (overage 72)**. ROOT: b38ac812 added the now-load-bearing delta-read/PRUNE/cache contract → over cap. NOT a naive prune (signal #2 correctly flagged: deleting content breaks the resume-economy contract every fleet agent depends on). FIX = agent-md-factory lazy-load: extract verbatim blocks (PRUNE thresholds, payload-pointer rules, signal-type + per-type-doc tables) to a pointered sibling `.md`; keep §READ/WRITE/PRUNE *contracts* in SKILL ≤120L. AC: SKILL ≤120L; the 3 contracts present-or-pointered (not deleted); drain-signals.md PRUNE call still resolves; 0 breach 14d. Zone: `.claude/skills/`. Route po→architect→agent-father.

## Sprint BCTC-LAYOUT-FIRST · CHEF-ATTN · OTHER READY

**BCTC-LAYOUT-FIRST:** Phase 0 READY. Zone: multi. Brief `…/2026-05-26-bctc-layout-first-pipeline.md`. 🔄 LF-EXTRACT · LF-OVERLAY · LF-DEPLOY/QA.  
**CHEF-ATTN:** Per-stock diversity cap. Status READY (2026-05-27). Pri: MEDIUM. Zone: `apps/mcp-server/`. 🔄 BA → IMPL → DEPLOY → QA → EXIT.

---

## Backlogs
- 🔄 **BCTC-CTG-ATTACHMENT-FETCH (dev-vps-crawls, MED, fresh — needs BA)** — report 3024: CTG 2026-Q1 conviction skip (composite=0.00) is NOT OCR-corruption / NOT a bank-misparse regression (FU-BANK-CODECOL not implicated). Router raw-verify: stored `CTG_2026_Q1.pdf` is only **0.5 MB** and `read_bctc_pdf` shows it is the Thông tư 96/2020 disclosure cover-letter (Mẫu 01-A, "Tài liệu đính kèm: BCTC Quý 1/2026") — ZERO balance-sheet/income numbers. The crawler fetched the announcement stub, not the attached full BCTC. Low-confidence skip fired CORRECTLY (per reference_low_confidence_handling); root cause = discovery/fetch grabbed cover-letter not attachment. Kin of BCTC-HNX-SSL fetch lessons. AC: CTG (and other cover-letter-with-attachment filers) fetch the attached BCTC PDF (size/page sanity gate: reject <1MB "BCTC" stubs, follow `Tài liệu đính kèm`); CTG_2026_Q1 re-fetch yields a real B02-TCTD with non-zero Tổng tài sản. Not acute (skip is honest). Report 3024 → resolved with this finding.
- TNB-GATEWAY-PROBE / CW-STEP47-HYGIENE (agent-father, LOW) · NB-BLOAT-FLOW-OVERWRITE → PROMOTED (Sprint NB-PRUNE-FIX) · FU-BANK-CODECOL / BCTC-TABLE-2 / KD-QREF-LANG · VPS-PROXY-RESTORE ✅ 2026-06-01 (B-02/B-06/B-03 cleared) · SSC-IBOARD-MIGRATE (dev-vps-crawls, MED) · Phase 0/1 pilot frozen → `docs/TASKS_ARCHIVE.md`

---

**Binding:** explicit-file staging; no `-A`/`--force`; all on `main`; no `pilot-status-*.json` edits; main terminal commits.
