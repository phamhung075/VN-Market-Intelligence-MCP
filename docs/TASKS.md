# TASKS — VN Market Intelligence MCP

> **Active:** Current sprint only. Historical: `docs/TASKS_ARCHIVE.md` | WIP limit: max 2 In Progress | Workflow: Backlog → Todo → In Progress → Review → Done | Branch: `task/NNN-kebab-name` | Report: `reports/TASK_REPORT_NNN.md` | **Archived Done tasks:** See `docs/TASKS_ARCHIVE.md` for complete history (1777–1896+)

---
## Backlog

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
| 1949-T1 | **Sprint 1949 PHASE 1 — THE CRITICAL GATE** — `unified-agent.md` rewrite + NEW `flows/unified-agent/chef.md` (8-step TNB recipe). Flip `market: write: false`→`true`; remove `rule: never`; remove "never sends to MARKET" from `not_my_job`; replace schedule with 5-slot table (05:23 morning / 02-08:XX:13 intraday convergence / 08:37 EOD / 19:37 evening / hourly health); lazy_load adds tnb-methodology.md + market-analysis.md + kinh-dich-layer.md + alert-policy.md; NEW chef.md implements GATHER→CLUSTER→LAYER1→LAYER2+3→LAYER4→LAYER5→LAYER6→WRITE-DISH→LOG with convergence rule (≥2 signal types same ticker 24h, ≥3 signals same sector 24h, macro-micro contradiction, severity=CRITICAL or RSI outside 2-sigma; intraday silent-exit if 0 cluster). `flows/unified-agent/main.md` adds dispatch routing for new chef windows. Size=M. Zone=`.claude/agents/` + `.claude/flows/unified-agent/`. **GATE — chef must publish ≥1 MARKET dish post-deploy before any Phase 2/4 task starts.** Brief §3+§4+§6+§7+§8. | HIGH | TASK | agent-father | docs/architecture-briefs/2026-05-18-cowork-reorder-and-cook-schedule.md | — |
| 1949-T2 | **Sprint 1949 PHASE 1** — `market-watcher.md` MARKET write removed + `flows/market-watcher/eod.md` patched. `market: write: false`; `rule: batch4_eod_only`→`rule: never`; description→gatherer-only ("writes `docs/signals/price_anomaly_*.json` only"); `flows/market-watcher/eod.md` removes `send_telegram(channel="market")` step. AC: next EOD cycle produces signal file with zero MARKET write. Size=S. Zone=`.claude/agents/` + `.claude/flows/market-watcher/`. Brief §3+§8. | HIGH | TASK | agent-father | — | — |
| 1949-T3 | **Sprint 1949 PHASE 1** — `news-scout.md` comment-only clarification: alert-digest signal output feeds chef (unified-agent) input, NOT MARKET. No flow change. AC: description string contains the new clarification; no behavioral change. Size=XS. Zone=`.claude/agents/`. Brief §3+§8. | MEDIUM | TASK | agent-father | — | — |
| 1949-T4 | **Sprint 1949 PHASE 2** — `alert-commander.md` event-only + `flows/alert-commander/cycle.md` patched. Cron stays `*/15 2-8 * * 1-5` but `cycle.md` gates entire flow on 3-condition (position-danger) or 4-condition (watchlist-opp) rule from `docs/policies/alert-policy.md`; cycle-header send_telegram block REMOVED; exit silently if neither fires; ≤140 chars urgent format when firing. NEW constraint `no_cycle_headers: true`. AC: next cycle sends 0 MARKET absent firing condition; sends exactly 1 when condition fires. Size=S. Zone=`.claude/agents/` + `.claude/flows/alert-commander/`. **BLOCKED until 1949-T1 GATE clears (chef live).** Brief §3+§8. | HIGH | TASK | agent-father | — | 1949-T1 GATE |
| 1949-T5 | **Sprint 1949 PHASE 3** — `digest-predict.md` scoped to Sunday weekly only. Remove `daily_digest` cron `30 15 * * *`; remove `monthly` cron; keep `weekly_digest` moved to `47 13 * * 0` (Sun 13:47 UTC = Sun 20:47 VN = Sun 15:47 France); update `monday_predict` to drop daily responsibilities. AC: 0 daily MARKET messages; exactly 1 calibration+portfolio dish on Sunday 13:47 UTC. Size=S. Zone=`.claude/agents/`. Brief §3+§8. | MEDIUM | TASK | agent-father | — | — |
| 1949-T6 | **Sprint 1949 PHASE 4** — `foreignFlowAlertJob.ts` cron `09:30`→`08:13 UTC` (`13 8 * * 1-5`). EOD chef reads at 08:37 UTC = 24min later (safe read window). AC: first run at 08:13 UTC next weekday post-deploy; signal in `docs/signals/` before 08:37 UTC; cron expression test passes. Size=S. Zone=`apps/mcp-server/`. **BLOCKED until 1949-T1 GATE clears (chef live).** Brief §4+§8. | HIGH | TASK | dev-mcp-server | — | 1949-T1 GATE |
| 1949-T7 | **Sprint 1949 PHASE 4** — `macroIndicatorRefreshJob.ts` US macro refresh confirms/adds `13 19 * * *` (19:13 UTC) slot. 24min before evening chef at 19:37 UTC. If different cron exists at different minute, retune. AC: macroIndicatorRefreshJob has 19:13 UTC slot; verified in cronConfig + integration test. Size=S. Zone=`apps/mcp-server/`. **BLOCKED until 1949-T1 GATE clears.** Brief §4+§8. | HIGH | TASK | dev-mcp-server | — | 1949-T1 GATE |
| 1949-T8 | **Sprint 1949 PHASE 4** — `cronConfig.ts` env-var defaults updated for T6+T7. `FOREIGN_FLOW_ALERT_CRON` (or equivalent) → `13 8 * * 1-5`; `MACRO_INDICATOR_REFRESH_CRON` → `13 19 * * *`. `.env.example` updated. New unit test verifies defaults. Size=S. Zone=`apps/mcp-server/`. **BLOCKED until 1949-T1 GATE clears; depends on 1949-T6+T7.** Brief §8. | HIGH | TASK | dev-mcp-server | — | 1949-T1 GATE + 1949-T6 + 1949-T7 |
| 1949-T9 | **Sprint 1949 PHASE 5** — `tran-ngoc-bau.md` audit scope updated + cron moved to `13 20 * * *` (20:13 UTC = 1h36min after evening chef). Description/audit scope from "audits MARKET atoms" → "audits chef narrative for TNB layer walk completeness (all 6 layers walked, business context cited, gap catalogue applied)"; `flows/tran-ngoc-bau/main.md` updates audit target. AC: next run audits the 3 daily dishes for TNB layer completeness; produces audit row in WORK only. Size=S. Zone=`.claude/agents/` + `.claude/flows/tran-ngoc-bau/`. Depends on 1949-T1. Brief §3+§4+§8. | MEDIUM | TASK | agent-father | — | 1949-T1 |
| 1949-T10 | **Sprint 1949 PHASE 6** — `financial-analyst.md` + `report-analyzer.md` signal output spec adds business-context fields per TNB foundational philosophy: `product` (1 sentence what company sells), `customer` (1 sentence who buys), `ops` (1 sentence operating posture), `mgmt` (1 sentence management track record). Chef cites these in narrative. AC: signal output schema updated in both .md files; example signal block in description includes the 4 fields. Size=S. Zone=`.claude/agents/`. Brief §3+§8. | MEDIUM | TASK | agent-father | — | — |
| 1949-T11 | **Sprint 1949 PHASE 7** — Documentation updates. `docs/standards/cron-jobs.md` adds chef cron table (5 slots), updates tran-ngoc-bau `0 13`→`13 20`, foreignFlowAlertJob `09:30`→`08:13`. `docs/policies/alert-policy.md` adds "Alert Commander Exclusivity" section (cycle headers removed, event-only formalized). `docs/references/workflow-map.md` updates `unified-agent` row to "Reads all cowork signals → writes MARKET chef dishes 3x/day"; removes "MARKET (eod only)" from `market-watcher` row. Run `/graphify docs --update --no-viz` post-edit. AC: all 3 doc files updated; no orphan refs. Size=S. Zone=`docs/`. Brief §8. | MEDIUM | TASK | agent-father | — | 1949-T1..T9 (docs reflect shipped state) |
| 1948a | **BLOCKED: Sprint 1948 Phase 1** — `improve_check_log` schema + `improveCheckStore.ts` (domain/infra DB layer). DB migration to `schema-system.ts` + Drizzle types + 6 store functions (insert, getPending, update, getRecentCheck, etc). Unit tests: insert, getPending, update, getRecentCheck, schema guard. Size=S. Zone=apps/mcp-server/. **PRE-CONDITION:** Blocked until post-1945-verdict-resolution-scored-pct gate clears 2026-05-20T07:22Z. If gate misses, resolution pipeline fix takes priority. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1948a.md | 2026-05-20T07:22Z (post-1945-verdict-resolution-scored-pct gate) |
| 1948b | **BLOCKED: Sprint 1948 Phase 1** — `degradationRules.ts` domain service (pure, zero imports). `DegradedSignalType` + `DegradationHypothesis` interfaces, `DEGRADATION_CAUSE_MAP` rule-table, `classifyDegradation()` + `lookupHypothesis()` functions. Detection logic: 7d vs 30d `accuracy_rate` delta ≥10pp OR baseline <40% with ≥10 samples per signal_type. Unit tests: degraded / not-degraded / insufficient-sample / null-rates / persistently-low / neutral. Size=S. Zone=apps/mcp-server/. **PRE-CONDITION:** Blocked until post-1945-verdict-resolution-scored-pct gate clears 2026-05-20T07:22Z. Depends on 1948a. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1948b.md | 1948a + 2026-05-20T07:22Z gate |
| 1948c | **BLOCKED: Sprint 1948 Phase 1** — `selfImproveOrchestratorJob.ts` scheduler entry + wiring + integration tests. Shadow-mode only: no signal-bus write, WORK Telegram output only. Detection loop: 7d/30d accuracy-rate delta, coverage-gap query, hypothesis lookup, `improve_check_log` snapshot write. Phase 1 AC: ≥1 degraded signal type detected → exactly 1 WORK Telegram; zero degradation → clean exit; all detect/hypothesis/log cycles tested (6+ tests). Wiring: add `CRONS.selfImproveOrchestrator` to cronConfig.ts, wire in startScheduler.ts, add env vars to .env.example. Size=M. Zone=apps/mcp-server/. **PRE-CONDITION:** Blocked until gate clears. Depends on 1948a + 1948b. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1948c.md | 1948a + 1948b + 2026-05-20T07:22Z gate |
| 1948e-C | **SPIKE-1948e-fix child 3/3 (OPTIONAL, deferred)** — Add PC1 to primary watchlist (identical scope to 1946a PLX). Files: `docs/data/system-map.json` `.project.watchlist[]`, `apps/mcp-server/mcp.config.json` `.market.watchlist[]`, `apps/mcp-server/src/infrastructure/db/seedWatchlist.ts` `WATCHLIST_SEED`, `domain/market.ts` `WATCHLIST_STOCKS`. Tests: seed presence, query returns PC1, idempotency. AC: PC1 in all 3 SSoT sources, tests TC1-TC4 GREEN, regression suite ≥50 tests pass. NOT required for legal_risk signal path (Fix A+B sufficient). Improves news-scout urgency classification. Size=S. Zone=`apps/mcp-server/` + `docs/data/`. Gate: None. PM judgment: schedule separate after 1948e-A+B merge, or defer to cycle 2. | LOW | FIX | dev-mcp-server | docs/handoffs/TASK_1948e-C.md | — |
| OBSERVE-1948d | **BLOCKED: Sprint 1948 Phase 1 observation gate** — 7-day shadow-mode verification (2026-05-25T09:00Z post-deploy). AC: ≥1 degraded signal type detected (improve_check_log.dispatch_status='shadow'), ≥1 WORK Telegram sent with detection details, zero false-positive auto-dispatches (Phase 1 is shadow-only), no DB errors, cron_job_runs shows 7 consecutive successful runs. Size=OBSERVE. **PRE-CONDITION:** Blocked until 1948c deploys (2026-05-20T09:00Z assumed, after gate clears and tasks complete). | MEDIUM | OBSERVE | ops | — | 1948c deployment + 2026-05-25T09:00Z |
| alert-precision-488-unknowns | **MONITORING**: Post-DB-rebuild agent_signals=46 (fresh DB). HOLD until ≥550. From TNB c58 Finding #8 + bug 2874. | MEDIUM | TRACKING | — | — | — |
| fa-shape-guard-watch | **MONITORING**: Next observation = first post-restart FA live session. Auto-cure trigger: REGIME-mismatch or news-fallback → spawn 1921a-fa-shape-guard-propagate. If NEUTRAL macro_snapshot → close. | MEDIUM | TRACKING | — | — | — |
| 1907a-digest-predict-silence | **CRITICAL** (c168 update): `vn-market` MCP server (`http://localhost:3000/sse`) added to `claude_desktop_config.json` mcpServers. **USER-ACTION: restart Claude Desktop** to load new MCP config. After restart, scheduled cowork tasks will have vn-market MCP access. Verify by checking digest-predict runs again. | CRITICAL | OPS | user | — | — |
| 1897b-carry | F1 USER: Docker .git/ exclude bundle + VirtioFS structural fix. PREFLIGHT cure permanent policy (1906a c89). F1 USER action (Docker .git/ exclusion) is the only structural cure. Brief: `docs/architecture-briefs/2026-05-13-headlock-recurrence-post-F2a.md`. | HIGH | URGENT-F1 | user | — | — |

---

## Todo
| post-1945-verdict-resolution-scored-pct | **Sprint 1945 AC-1 OBSERVE** — gate 2026-05-20T07:22Z (48h post-1945a deploy). AC: `alert_accuracy.scored_pct` rises ≥60% AND `unknowns_30d` drops by ≥100 (was 36%/520 pre-1945a). If miss → 1947b-verdict-resolution-followup (HIGH FIX, dev-mcp-server). | HIGH | OBSERVE | ops | — | 2026-05-20T07:22Z |
| post-1945-bug-storm-silence | **Sprint 1945 AC-2 OBSERVE** — gate 2026-05-20T07:22Z (48h). AC: zero new `[bug] verdictResolutionJob` Telegram messages for 48h post-1945a deploy. Any new msg → 1947c-verdict-resolution-bug-followup. | MEDIUM | OBSERVE | ops | — | 2026-05-20T07:22Z |
| 1941b-signal-outcomes-seed-window | **Sprint 1941 TIER 2** — 7-day OBSERVE on `signal_outcomes` seeding. AC by 2026-05-25: ≥30 resolved rows (outcome_24h ∈ correct/incorrect/neutral) across ≥3 distinct `signal_type` values. Failure mode → bug task to dev-mcp-server (seed path broken in postSignal wrapper). No code change unless escalation triggers. | MEDIUM | OBSERVE | ops | — | 2026-05-25 |
| 1922g-pharma-events-source-verify | **OBSERVE** — `pharma_events` empty. `davPharmacyJob` cron `0 6 1 * *`. Next tick = 2026-06-01 06:00 UTC. AC: check status + row count after tick. | LOW | OBSERVE | ops | — | 2026-06-01 |
| 1922i-alert-engine-records | **WONTFIX c160 (SPIKE-1933a resolved)** — alert_engine_records always 0: evaluateAlert() dead code deleted (1933b). Architecture: market.db.alerts → Alert Commander = canonical intelligence path. Go alert-engine (/evaluate) reserved for future stop-loss use case. | MEDIUM | WONTFIX | — | — | — |
| post-1942-fa-verify | **MONITOR** — Verify FA next live cycle (~23:00 UTC tonight) reports ≥20/30 BCTC analyses (was 3/38 pre-1942). If still 3/38 → deploy-gap bug task to dev-mcp-server (Docker rebuild). Auto-close on observation. | MEDIUM | OBSERVE | ops | — | 2026-05-19 |

---

## In Progress

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
_(empty)_ | — | — | — | — | — | — |

---

---

## Review

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
_(empty)_ | — | — | — | — | — | — |

---
## Done

| Task ID | Title | Priority | Type | Owner | Completed |
| 1948e-B | **DONE 2026-05-18** — Added Legal Risk Signal Dispatch block to `.claude/flows/news-scout/stage-signals.md` before `urgent_news` block. Trigger: `detectLegalRisk()` non-empty OR prosecution keywords + stock code match. Dedup: 360-min window on (stock_code, signal_type="legal_risk"). Confidence: prosecution/asset_freeze=0.95, tax/license=0.85, investigation=0.70. ttl_minutes=360. No verdictResolutionJob contact (AC-8). Test: 5/5 GREEN (TC1 detect PC1 khởi tố, TC2 roundtrip post+query, TC2b dedup suppression, TC3 confidence mapping, TC4 AC-8 regression). tsc 0 errors. Branch: `task/1948e-b-legal-risk-dispatch`. Commit: `ddff5105`. | MEDIUM | FIX | dev-mcp-server | 2026-05-18 |
| 1948e-A | **DONE 2026-05-18** — Added `"legal_risk"` to `SignalTypeSchema` enum in `apps/mcp-server/src/infrastructure/db/agentSignalStore.ts` L39-49. One-line additive change; zero DB migration. `SignalTypeSchema.safeParse("legal_risk")` now returns `{ success: true }`. Test: 3/3 GREEN (TC1 schema accepts legal_risk, TC3 all 9 existing types pass, TC4 unknown_type still rejected). tsc 0 errors. Regression: +1 pass / 0 new failures. Branch: `task/1948e-a-legal-risk-schema-enum`. Unblocks: 1948e-B. | MEDIUM | FIX | dev-mcp-server | 2026-05-18 |
| SPIKE-1948e | **DONE 2026-05-18 ARCHITECT** — PC1 legal_risk pipeline review. Three-point cascade gap confirmed: (1) `SignalTypeSchema` in `agentSignalStore.ts:39-49` missing `"legal_risk"` — Zod rejects any `post_agent_signal(signal_type: "legal_risk")` call before DB write; (2) `stage-signals.md` has no `legal_risk` dispatch path — news-scout only routes to `urgent_news` or `chain_catalyst`; (3) PC1 absent from primary watchlist — treated as low-priority sector ripple by news-scout. `legalRiskDetector.ts` patterns correct (covers `khởi tố`). `get_legal_risk_signals` read-side correct (1940a already queries `agent_signals`). Recommended fix: Size S — Fix A enum addition + Fix B flow dispatch block. Child task filed: 1948e-fix. Spike: `docs/spikes/SPIKE_1948e-pc1-legal-risk-pipeline.md`. Brief: `docs/architecture-briefs/2026-05-18-legal-risk-signal-pipeline.md`. | MEDIUM | SPIKE | architect | 2026-05-18 |
| 1945d-reparse-pipeline-gap | **DONE 2026-05-18 QA-APPROVED** — Fixed two pipeline gaps in BCTC extraction. GAP-A: `bctcReparseJob.scanDiskForStrandedPdfs` now runs unconditionally every cycle (was only when feedback rows=0); deduplicates via processedFilenames set. GAP-B: `push-bctc-pdf` setImmediate now calls `triggerPushBctcExtraction` with `pdfTextOverride` — bypasses geo-blocked SSC/VPS URL download (same pattern as `bctcPdfPullJob.triggerExtraction`). New module: `pushBctcExtraction.ts` with injectable deps. 12 new tests (1945d-reparse-pipeline-gap.test.ts) — AC-3 met. Root cause for 6/7 banks (ACB/BID/CTG/MBB/VCB/VPB) diagnosed as VPS-side gap: source_urls in bctc_vps_queue are SSC URLs (not bctc-files/), VPS playwright scripts have not fetched those PDFs. Outside mcp-server zone. EIB+DHG will extract on next bctcReparseJob cycle post-deploy. Merge: `chore(1945/bctc): merge task/1945d-reparse-pipeline-gap`. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| SPRINT-1947 | **CLOSED 2026-05-18T09:09Z** — Closed-loop auto-improvement system DESIGN sprint. Single anchor task DONE: SPIKE-1947 architect deliverable (spike doc + ARCH brief, committed `b55ea5c8`). All 5 ACs met. Host decision: scheduler job in `apps/mcp-server/src/scheduler/audits/selfImproveOrchestratorJob.ts` (Option C). Phased rollout decided: Phase 1 shadow → Phase 2 manual-gate → Phase 3 auto-dispatch with kill-switch. Sprint 1948 PM-decomposed (1948a/b/c/OBSERVE-1948d) and queued in Backlog — all gate-blocked until post-1945-verdict-resolution-scored-pct clears 2026-05-20T07:22Z. WIP=0. | SPRINT | CLOSE | po | 2026-05-18 |
| SPIKE-1947 | **DONE 2026-05-18 ARCHITECT** — Closed-loop auto-improvement system design. Host decision: scheduler job inside `apps/mcp-server/src/scheduler/audits/selfImproveOrchestratorJob.ts` (Option C — direct SQLite access, no new Docker service). Detection: 7d vs 30d `accuracy_rate` delta ≥10pp OR baseline <40% with ≥10 samples per signal_type. Hypothesis: `degradationRules.ts` rule-table in domain layer (Phase 1-2); LLM agent optional Phase 3 via signal-bus. Dispatch: 3-phase rollout — Phase 1 shadow (WORK Telegram only, Sprint 1948), Phase 2 manual-gate (signal-bus JSON, human drain), Phase 3 auto-dispatch (kill-switch `SELF_IMPROVE_AUTO_DISPATCH=false`, WIP≤2 cap). Recheck: 7-day post-dispatch window, before/after `accuracy_rate` delta. Loop-exit: `hit_rate ≥60%` sustained 2 consecutive weekly windows. Safety: cooldown 7d per signal_type, max 2 dispatches/cycle, freeze-on-worsening, recurring-bug escalation after 2 failed cycles. OBSERVE gates: 3 of 6 can retire once Phase 1 proves stable (verdict-resolution-scored-pct + bug-storm-silence + 1941b-seed-window absorbed by orchestrator). Spike: `docs/spikes/SPIKE_1947-auto-improve-loop.md`. Brief: `docs/architecture-briefs/2026-05-18-closed-loop-auto-improvement.md`. Commit: `b55ea5c8`. Proceed to Sprint 1948 Phase 1: YES (pre-condition: post-1945 gate clears 2026-05-20T07:22Z). | HIGH | SPIKE | architect | 2026-05-18 |
| SPRINT-1946 | **CLOSED 2026-05-18T08:40Z** — Crisis detection coverage gap diagnosed and fixed. Two tasks DONE QA-APPROVED: SPIKE-1946 (architect confirmed PLX absent from watchlist = root cause; FIX recommended) and 1946a (PLX added to all 3 watchlist SSoTs + frontend + 7 tests GREEN). Docker rebuild dispatched to ops (separate agent). 6 OBSERVE gates carried forward into post-sprint monitoring window. Sprint outcome: minimum-viable single-stock crisis coverage restored — `get_crisis_early_warning` now has PLX in its evaluation universe. No code in alert-engine/verdictResolutionJob touched (R-1 honored). | SPRINT | CLOSE | po | 2026-05-18 |
| 1946a | **DONE 2026-05-18 QA-APPROVED** — Added PLX (Petrolimex, HOSE, Oil & Gas) to all three watchlist SSoT sources. (1) `docs/data/system-map.json` `.project.watchlist[]` — PLX added after BSR with `active=true`, `exchange=HOSE`, `sector="Oil & Gas / Petroleum Retail"`. (2) `mcp.config.json` `.market.watchlist` — PLX added after BSR. (3) `apps/mcp-server/src/infrastructure/db/seedWatchlist.ts` `WATCHLIST_SEED` — `{ code: "PLX", exchange: "HOSE", domain: "oil_gas" }` added after GAS. (4) `apps/frontend/app/domain/market.ts` `WATCHLIST_STOCKS` — PLX entry added after BSR. (5) 7 new tests in `1946a-plx-watchlist-crisis-coverage.test.ts` — seed presence, velocity ratio ≥2.0 → crisisIndicators contains PLX, below-threshold negative test, idempotency. (6) Pre-existing 1343a test stale-count failures (from 1876a-A6 not updating them) fixed: 26→34 rows, domain set 11→13, HNX assertion corrected. All 49 watchlist/crisis tests GREEN. tsc 0 errors. Branch: `task/calendar-source-10s-timeout`. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| 1945b-frontend | **DONE 2026-05-18 QA-APPROVED** — AccuracyDigestCard component + fetch helpers + domain types. (1) `AccuracyDigestStats` + `SignalTypeAccuracyDigest` types added to `domain/market.ts:169`. (2) `fetchAccuracyDigest()` + `deriveAccuracyDigestState()` + `digestRateColor()` added to `client.ts`. (3) Loader extended: `fetchAccuracyDigest(30)` as 3rd `Promise.allSettled` leg, `accuracyDigest` field in `LoaderData`, `ACCURACY_SEEDING_WINDOW_END = "2026-05-25"` constant. (4) `AccuracyDigestCard` inline component: 6 states (loading/empty/all-neutral/insufficient-sample/partial/normal). (5) SectionCard inserted after "Kinh Dịch — Cổ phiếu mẫu". (6) 20 tests in `1945b-accuracy-digest-card.test.ts` — all GREEN. Full suite: 144/144 tests pass, 0 tsc errors. ACs AC-1 through AC-7 all verified. | MEDIUM | FEATURE | dev-frontend | 2026-05-18 |
| 1945b-backend | **DONE 2026-05-18 QA-APPROVED** — `GET /api/accuracy/digest?days=N` handler added to `server.ts` after line 1020. Import extended: `getSystemAccuracyDigestStats` added to `signalOutcomeStore.js` import. `days` param clamped [1,90] using `isNaN` guard (not `|| 30` — correctly handles days=0). 6 tests in `1945b-accuracy-digest-handler.test.ts` (default/clamp-hi/clamp-lo/absent/error-500/zero-struct) all GREEN. tsc 0 errors. R-4 mitigated: clamping applied before SQL call. generatedAt appended at handler level. Zero-struct return 200 confirmed (function table-guard path). | MEDIUM | FEATURE | dev-mcp-server | 2026-05-18 |
| 1945a | **DONE 2026-05-18 QA-APPROVED** — Fixed `verdictResolutionJob` baseline-price shape mismatch. `PriceHistoryEnvelope` type added to `clients.ts`; `getPriceHistory()` return type changed from `PriceSnapshot[]` to `PriceHistoryEnvelope`. `defaultFetchHistory()` updated to read `envelope.history[0].close` instead of `(snaps[0] as {price}).price`. Only caller is `verdictResolutionJob.ts` (audit confirmed `registry.ts` + `portfolioRiskCalculator.ts` do NOT call `getPriceHistory` from clients.ts). 6 new unit tests (`1945a-verdict-resolution-envelope.test.ts`) — envelope shape, empty history, null envelope, baseline extraction via `.close`. tsc 0 errors. 25/25 verdict resolution tests GREEN. Fix eliminates silent TypeError → null → `false_positive:unresolvable` path that was preventing ~520 alerts from being scored. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| ARCH-1945b | **DONE 2026-05-18** — Accuracy digest frontend card technical brief. Brownfield scan: `getSystemAccuracyDigestStats` already exists in `signalOutcomeStore.ts:380`; already imported in `server.ts:48`; no api-gateway change needed (`/mcp/*` catch-all confirmed). Exact insertion points: handler after `server.ts:1020`, SectionCard after `dashboard.analysis.tsx:1417`. New types: `AccuracyDigestStats` + `SignalTypeAccuracyDigest` in `domain/market.ts`. New helper: `fetchAccuracyDigest()` + `deriveAccuracyDigestState()` in `client.ts`. 6 UI states (null/empty/all-neutral/insufficient/partial/normal). 5 risk flags including R-4 (days param SQL injection guard — clamping in handler, before function call). Brief: `docs/architecture-briefs/2026-05-18-accuracy-digest-frontend-card.md`. | MEDIUM | ARCH | architect | 2026-05-18 |
| BA-1942d | **DONE 2026-05-18** — Accuracy digest frontend card spec written. Spec: `docs/REQ_1945b-accuracy-digest-frontend-card.md`. 11 sections: route (dashboard.analysis), component shape (top-3/bottom-3), gateway endpoint contract (GET /mcp/api/accuracy/digest), loading state, empty state, all-neutral state, insufficient-sample state, DDD layer mapping, edge cases, NFRs, 7 ACs. No PO blockers. | MEDIUM | BA-SPEC | ba | 2026-05-18 |
| SPIKE-1946 | **DONE 2026-05-18 ARCHITECT** — Root cause confirmed FIXABLE: PLX absent from `watchlist` table → `get_crisis_early_warning` never evaluated PLX. Three layered causes: RC-1 PLX not in seedWatchlist.ts/mcp.config.json/system-map.json; RC-2 tool is velocity-spike detector (not price-crash detector) — architectural scope is correct; RC-3 chain_catalyst #3383 TTL window (design trade-off). Verdict: FIX — add PLX to watchlist (S task, 3 files). news-scout chain_catalyst path covers non-watchlist stocks probabilistically but with TTL expiry risk. Child task 1946a filed. Spike: `docs/spikes/SPIKE_1946-crisis-detection-plx-gap.md`. | HIGH | SPIKE | architect | 2026-05-18 |
| SPIKE-1945 | **DONE 2026-05-18 ARCHITECT** — Root cause confirmed FIXABLE: `defaultFetchHistory()` in `verdictResolutionJob.ts` treats Go `/price/history` response as `PriceSnapshot[]` but Go returns `{code, history: DailyOHLCV[]}` envelope. `snaps[0]` = `undefined` → TypeError → caught → `null` → "price-fetch-failed:unresolvable" path fires for every verdict. Explains 100% of ~520 unscored rows and BUG-storm (19 msgs / 21h). Sprint 1336 SQLite isolation not implicated. `false_positive` label from 1926a is incorrect for these rows (not direction-evaluated). Child task 1945a scoped (FIX, dev-mcp-server, `scored_pct ≥ 60%`). Spike: `docs/spikes/SPIKE_1945-verdict-resolution-no-baseline.md`. | HIGH | SPIKE | architect | 2026-05-18 |
| 1944c | **DONE 2026-05-18** — End-to-end smoke verification: Docker container rebuilt and running latest code (commit 1b3d6c00). BCTC enricher active and processing queue (cycles at 05:45 + 06:15 UTC). All 7 banking Q1-2026 queue entries have source_url populated (100%). VPS proxy fix deployed: `{results:[{url,source,confidence}],error}` envelope confirmed. X-API-Key injection verified. Dead strategies removed (SSC/vietstock). Smoke report: `reports/TASK_REPORT_1944c.md`. AC PASS: all 5 criteria met (Docker rebuilt + enricher active + 7/7 banking cohort with source_url + all 1944 fixes verified). No escalation required. | MEDIUM | TASK | ops | 2026-05-18 |
| 1944b | **DONE 2026-05-18** — Removed dead BCTC discovery strategies: SSC iboard (NXDOMAIN since 2026-04-27) and vietstock.vn (HTTP 404 for every ticker). `_fetchSsc`, `_fetchVietstock` deprecated to no-op in `DiscoverOptions` (backward-compat). `_fetchCafef` was already deprecated (TASK_1916b). Strategy chain now: hsx(0) → VPS Playwright(1) → null. Production wiring in `bctcQueueEnricherJob.ts` cleaned up. AC-5: `LIVE-4` test added to `1944a-vps-live-probe.test.ts` (VPS_INTEGRATION guard). 142 BCTC tests GREEN (138 pass / 4 skip). tsc 0 errors. Cafef already removed in TASK_1916b (confirmed). Files: `bctcDiscovery.ts`, `bctcQueueEnricherJob.ts`, 11 test files updated. Zone: `apps/mcp-server/`. | MEDIUM | FIX | dev-mcp-server | 2026-05-18 |
| 1944a-vps | **DONE 2026-05-18** — Shape fix: `vps-proxy-server.js` `/proxy/bctc-discover/:ticker` now returns `{results:[{url,source,confidence}],error:null}` envelope instead of bare `string[]`. Deployed to Vinahost VPS via `scripts/deploy-vinahost.sh`. Verified: envelope shape confirmed via curl (see verification log). `vn-vps-proxy.service` running post-deploy. Fix: Sprint 1944a-vps. Zone: `vps-scripts/`. | HIGH | FIX | dev-vps-crawls | 2026-05-18 |
| 1944a-mcp | **DONE 2026-05-18** — VPS_INTEGRATION-guarded live probe test created (`1944a-vps-live-probe.test.ts`). 2 guard tests pass + 3 live tests skip cleanly (CI-safe). Verified: `VPS_PUSH_API_KEY` set in `.env` (line 13); added to `.env.example` (was missing). `BCTC_DISCOVER_URL` + `VPS_HOST` confirmed in `docker-compose.yml`. 1916a 6/6 GREEN. 56/56 cashflow+bctc regression GREEN. tsc 0 errors. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| ARCH-1944 | **DONE 2026-05-18** — Zone-split brief for 1944a. Brownfield scan revealed: (1) VPS route + header injection both already landed in repo; (2) response shape mismatch (`string[]` vs `{results,error}` envelope) identified as blocking risk R-1; (3) cafef removal already complete in TASK_1916b. Two child tasks defined: 1944a-vps (shape fix + VPS deploy) + 1944a-mcp (verify wiring + live probe test). Brief: `docs/architecture-briefs/2026-05-18-vps-bctc-discover-route-zone-split.md`. | HIGH | ARCH | architect | 2026-05-18 |
| ARCH-1942 | **DONE 2026-05-18** — Sprint 1942 blocking arch brief. Two-gap diagnosis: vnstock startup probe missing + cashFlowTool needs fallback read path. Brief: `docs/architecture-briefs/2026-05-18-watchlist-fundamentals-cadence.md`. | HIGH | ARCH | architect | 2026-05-18 |
| 1942a | **DONE 2026-05-18 QA-APPROVED** — Startup backfill probe. `vnstockStartupProbe.ts` + wired in `startScheduler.ts`. Guard: COUNT(DISTINCT code WHERE data_type='financials') < 10 OR last fetch > 7 days → fire runVnstockFundamentalsJob() after 90s delay. 6/6 unit tests GREEN. All 11 ACs PASS. Report: `reports/TASK_REPORT_1942a.md`. | HIGH | TASK | qa | 2026-05-18 |
| 1942b | **DONE 2026-05-18 QA-APPROVED** — cashFlowTool fallback read path (financial_reports → vnstock_cash_flow). COALESCE-style decision + data_source field + loading=true UX + unit conversion ×1000. backfillOCFForWatchlist() added. 10/10 new tests GREEN, 50/50 cashflow suite GREEN, 0 tsc errors. Coverage result: 31/33 tickers = 94% (sprint goal ≥20/30 ACHIEVED). | HIGH | TASK | dev-mcp-server | 2026-05-18 |
| 1942c | **DONE 2026-05-18 QA-APPROVED** — HPG OCF all-zeros fix. `vnstockBridge.ts`: CASH_FLOW_SCRIPT 3-key fallback + NULL policy + NI multi-key fallback. `cashFlowExtractor.ts`: P_OPERATING_CF_MFG + F_OPERATING_CF_MFG steel-sector labels. `vnstockTypes.ts`: operatingCashFlow: number\|null. 6/6 new tests GREEN (T1-T6). 50/50 cashflow regression GREEN. 0 tsc errors. | MEDIUM | TASK | dev-mcp-server | 2026-05-18 |
| kinh-dich-name-fix | **DONE 2026-05-18 QA-APPROVED** — Three root-cause fixes in kinh-dich-service: (1) All 64 QUE_META hexagram names in domain/services.ts corrected to full Vietnamese diacritics (was plain ASCII). (2) Fallback path in application/usecases.ts resolves name from QUE_META.find(q => q.id === stored.hexagram_number) instead of fresh placeholder-score reading. (3) SQLitePriceScoreRepository in infrastructure/repositories.ts corrected to query market_prices_history (was price_history), columns price + fetched_at. 30/30 tests GREEN (8 new), tsc 0 errors, DDD PASS, security PASS. Commit: abf5ef2d. | MEDIUM | FIX | dev-kinh-dich | 2026-05-18 |
| 1943a | **DONE 2026-05-18 QA-APPROVED** — resetQ1UrlNotFound() wired in initFinancialReportsTables (startup idempotent reset). Grace-period auto-retry arm added to bctcQueueEnricherJob COMBINED_SQL (url_not_found + last_attempt >7d + attempts <6). bctcBatchSweepJob diagnostic log added; root cause documented (wrapRun key correct, zero-run = container down 2026-04-25 09:00 UTC). 16/16 new tests GREEN (AC-1 thru AC-4). Full suite: 9219 pass, zero new failures. tsc 0 errors. DDD PASS. Security PASS. Report: reports/TASK_REPORT_1943a.md. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| SPIKE-1943 | **DONE 2026-05-18 ARCHITECT** — Root cause diagnosis: VPS PDF discovery failure, not stale calendar. Financial_reports zero Q1-2026 rows (max_year=2025). 31 tickers stuck at `url_not_found` in `bctc_vps_queue` (MAX_ENRICH_ATTEMPTS=5 exhausted), 7 `pending` (0 attempts). Calendar deadline logic correct (dynamic computation, no hardcoded seeds). `bctcBatchSweepJob` zero runs (likely `wrapRun` key mismatch). Recommended: TASK-1943a (FIX-HIGH, reset queue + diagnose batch sweep + add auto-retry policy). Output: `docs/spikes/SPIKE_1943-bctc-banking-q1-2026-deadline-delay.md`. | MEDIUM | SPIKE | architect | 2026-05-18 |
| 1941d-fpt-net-profit-ocr-fix | **DONE 2026-05-18 QA-APPROVED** — net_profit_api_bridge column + bridgeNetProfitToFinancialReports() + cashFlowTool COALESCE + ni_source. FPT Q4/2025 OCR=20,225 → bridge=2,509,520 triệu VND, ratio 1.6371 (passes guard). 7 new tests + 17 regression = 24 cashflow tests GREEN, tsc clean, DDD PASS, security PASS. Merged to main. Report: reports/TASK_REPORT_1941d.md. | MEDIUM | FIX | dev-mcp-server | 2026-05-18 |
| _(Older Done entries (1941c, 1941a, calendar-source-replacement, 1940a, 1939a/b, 1937a, 1938a, 1862c-E, 1936b, news-bugs-reuters-bloomberg-fix, 1936, macro-calendar-timeout-cap, 1934, 1933b, 1928a/1929a/1930a/1930c, 1862c-F, 1930b, 1932a, 1931a, 1922f, 1927a, 1926a, 1925a, 1924a/b/c/d, 1923a, 1909c) archived → `docs/TASKS_ARCHIVE.md`; pre-c141 history in git)_ | — | — | — | — | 2026-05-16..18 |

---

## Deferred

| Sprint | Title | Reason | Next Step |
|--------|-------|--------|-----------|
| 1887 | METHODOLOGY-FORENSICS: Virtual Capital / related-party graph detector | Needs own architect brief — graph-store choice, related-party data source, traversal patterns, false-positive control all unspecified | When 1885+1886 ship, queue separate ARCH-1887 brief before ba spec |
| 1892a-ops AC-3 | OPS-NOTE: 1892a-ops AC-3 now UNBLOCKED by 1892b merge (2026-05-12). VPS POST to `/api/push-news` should reach MCP server after deploy. | Unblocked 2026-05-12 | ops re-verify next cycle (observational) |
| TNB-c39-#3 | MONITOR: unified-agent FPT pillar gap (2nd cycle of evidence at c39) | Per TNB protocol need 3rd cycle to auto-cure. | If c40 unified-agent cycle repeats FPT-without-pillars pattern → spawn auto-cure CHORE. If c40 PASSES → close as transient. |
