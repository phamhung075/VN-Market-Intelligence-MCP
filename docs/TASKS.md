# TASKS — VN Market Intelligence MCP

> **Active:** Current sprint only. Historical: `docs/TASKS_ARCHIVE.md` | WIP limit: max 2 In Progress | Workflow: Backlog → Todo → In Progress → Review → Done | Branch: `task/NNN-kebab-name` | Report: `reports/TASK_REPORT_NNN.md` | **Archived Done tasks:** See `docs/TASKS_ARCHIVE.md` for complete history (1777–1896+)

---
## Backlog

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
| 1958-recovery | **[done 2026-05-20T20:06:31Z ops signal ops-1958a-stack-recovered.json]** Sprint 1958 — Stack recovery complete. 10 of 11 microservices DOWN (19:59:48Z audit) → 11/11 UP (20:06:31Z). Action: `docker compose up -d` + rag-service restart. AC: (1) `docker ps` 11/11 Up ✓; (2) health endpoints A-12→A-20 return 200 ✓; (3) pre-recovery evidence captured; (4) recovery time 4 min. | HIGH | INCIDENT | ops | docs/signals/ops-1958a-stack-recovered.json | — |
| 1958-rca | **Sprint 1958 — Root-cause analysis for 10-service docker-compose outage (recovery-hang + outage-window).** Phase 1 recovery-hang RCA: disk pressure (97% full) + RAG lifespan handler blocked on sentence-transformers model init + LanceDB 29GB cold-load I/O contention → hang at 20:05:22Z→20:06:31Z. **PHASE 1 COMPLETE** ✓ (signal `docs/signals/ops-1958-rca.json` published). **GAP:** RCA timeline begins at recovery 20:05:22Z. Does NOT explain original 04:32Z → 19:59Z drop window when 9 services were already DOWN. Investigating: manual stop / docker daemon restart / failed update / OOM / restart-policy differential. Output: `docs/architecture-briefs/2026-05-20-stack-outage-rca.md` (recovery root cause published). Outage-window investigation deferred to 1958-rca-2. Estimate: 2h Phase 1 (DONE), 2h Phase 2 (1958-rca-2 TODO). Zone=ops investigation. | HIGH | TASK | ops | docs/handoffs/TASK_1958-rca.md | — |
| 1958-rca-2 | **Sprint 1958 — Investigation gap: docker events + macOS journal 04:32Z–19:59Z.** Why were 9 services already DOWN before 20:05:22Z recovery attempt? Enumerate hypothesis: (A) macOS sleep / Docker Desktop pause + selective `restart:` policy (mcp-server + frontend have `unless-stopped`, others have defaults); (B) manual `docker compose down` / partial stop (check shell history); (C) Docker daemon restart event (check dockerd.log); (D) OOM/resource pressure (docker events --since 16h | grep -iE oom); (E) VirtualMachine SHM tear. Output: brief append to `docs/architecture-briefs/2026-05-20-stack-outage-rca.md` §outage-window, or signed-off "logs inaccessible, defer to 1954a pattern analysis". AC: (1) hypothesis checklist (5 items) verified with commands; (2) single root cause identified + evidence cited, OR explicit "unavailable" verdict; (3) brief appended; (4) logs fresh (buffer rotation window <24h). Estimate: 1h. Zone=ops investigation. **PRIORITY:** run soon — docker events + journal buffers rotate. | MEDIUM | DONE | ops | docs/handoffs/TASK_1958-rca-2.md | — |
| 1958-disk-relief | **Sprint 1958 — IMMEDIATE disk-space recovery: vacuum LanceDB, rotate logs, prune docker images.** Restore ≥15 GB free on Docker VM (currently 97% full, 30GB app data total). Work: (1) `docker image prune -a --force` (remove orphaned images); (2) compress + archive old logs in `/app/data/logs/` (keep ≤30MB running); (3) run LanceDB vacuuming job (if available) to compact 29GB vector DB (may reduce 10–15%); (4) verify free space ≥15GB via `df /app/data`. AC: (1) `df /app/data` free ≥15GB (or at minimum ≥12GB to unblock 1958-watchdog-3 image pre-bake); (2) no service restarts triggered; (3) next `docker compose up -d` succeeds without disk-pressure symptoms. Estimate: 30m (mostly automated; may need manual LanceDB compaction design). Zone=ops. **STATUS: DONE** — COMPLETE 2026-05-20T20:31:26Z — disk recovered 26.5GB (docker buildx 33GB -> 0GB), system now 85% full with 32GB free for 1958-watchdog-3. | HIGH | TASK | ops | docs/handoffs/TASK_1958-disk-relief.md | — |
| 1958-watchdog-1 | **Sprint 1958 hardening — Pre-flight disk check before `docker compose up -d`.** Fail-fast if free disk < 15 GB. Work: bash script `scripts/preflight-disk.sh` (or equiv) checks `df /app/data | tail -1 | awk '{print $4}'` (free blocks), errors if < 15GB with STDERR message "ERROR: Docker disk <15GB free, cannot start. Run disk-relief task." Wire into deployment docs / restart procedures. AC: (1) script exists + executable; (2) manual test: script exits 1 on low disk, 0 on healthy disk; (3) documented in `docs/protocols/docker-deployment-runbook.md` as pre-flight step; (4) wired into helm/ops procedures if applicable. Estimate: 1h. Zone=ops/scripts. | HIGH | TASK | ops | docs/handoffs/TASK_1958-watchdog-1.md | 1958-disk-relief |
| 1958-watchdog-2 | **Sprint 1958 hardening — Bump RAG service healthcheck `start_period`.** Increase `docker-compose.yml` `services.rag-service.healthcheck.start_period` from 30s → 60s to account for disk I/O contention + sentence-transformers model loading latency. AC: (1) `docker-compose.yml` updated; (2) deploy + verify healthcheck succeeds on first restart cycle (no premature timeout); (3) rag-service Up + health 200 within 60s. Estimate: 15m (trivial edit + deployment). Zone=ops/apps/mcp-server. Owner=dev-mcp-server (owns compose). | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1958-watchdog-2.md | — |
| 1958-watchdog-3 | **Sprint 1958 hardening — Pre-bake sentence-transformers model in RAG Dockerfile.** Add RUN step to `apps/rag-service/Dockerfile` to eagerly download + cache sentence-transformers embedding model during image build, avoiding first-run HuggingFace network call + ~400MB disk I/O on startup. Work: (1) add RUN command (e.g., `RUN python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('all-MiniLM-L6-v2')"`) to Dockerfile after base deps; (2) rebuild image locally + verify layer includes model (~400MB); (3) deploy + verify cold-start rag-service healthcheck latency reduced. AC: (1) Dockerfile RUN step present; (2) rebuilt image layer includes model; (3) image size grows ~400MB (acceptable); (4) cold-start latency <30s (was >30s due to model download blocking health). Estimate: 1h. Zone=apps/rag-service/. Owner=dev-rag-service. **DEPENDS:** 1958-disk-relief (need headroom before adding 400MB layer). | MEDIUM | TASK | dev-rag-service | docs/handoffs/TASK_1958-watchdog-3.md | 1958-disk-relief |
| 1958-watchdog-4 | **Sprint 1958 hardening — LanceDB compaction / archival cron.** Implement weekly (or daily) job to compact/archive LanceDB vector store, reclaim disk space as PDFs age. LanceDB currently 29GB; target <25GB baseline with cron maintaining that ceiling. Work: (1) design retention policy (e.g., keep embeddings ≤180d old, archive older); (2) write `lanceDbCompactionJob.ts` scheduler entry calling LanceDB compaction API or equivalent; (3) register in `cronConfig.ts` (weekly Mon 02:00Z or daily cadence); (4) unit test: verify compaction call succeeds, disk metric decreases by ≥5%. AC: (1) cron registered + fires; (2) unit test GREEN; (3) `cron_job_runs` shows ≥1 successful run within 7 days of merge; (4) LanceDB disk usage trend <25GB (baseline acceptable, growth capped). Estimate: 3h (design + cron + test). Zone=apps/mcp-server/. Owner=dev-rag-service. | MEDIUM | TASK | dev-rag-service | docs/handoffs/TASK_1958-watchdog-4.md | — |
| 1958-watchdog-5 | **Sprint 1958 hardening — Disk-usage alert cron.** Monitor `/app/data/lancedb` size; emit BUG Telegram when > 20GB (threshold lower than current 29GB to give headroom). Work: (1) write `diskUsageAlertJob.ts` cron (5-min or 1h cadence); (2) query `du -sh /app/data/lancedb` or equivalent; (3) if size > 20GB, send BUG Telegram with message "ALERT: LanceDB disk usage X GB (threshold 20GB) — run disk-relief task"; (4) register in `cronConfig.ts`; (5) unit test + integration test on real data. AC: (1) cron fires every 5 min / 1h; (2) BUG Telegram sent when threshold breached; (3) clean exit (no Telegram) when < 20GB; (4) `cron_job_runs` shows 12+ successful runs without false positives. Estimate: 2h. Zone=apps/mcp-server/. Owner=dev-mcp-server. | MEDIUM | TASK | dev-mcp-server | docs/handoffs/TASK_1958-watchdog-5.md | — |
| 1958-watchdog-6 | **Sprint 1958 hardening — Async-ify RAG lifespan handler.** Move sentence-transformers model loading to thread pool / async context to avoid blocking FastAPI startup. Current: model load happens synchronously in lifespan handler, blocks health checks. Work: (1) refactor RAG lifespan handler to offload model loading to `asyncio.to_thread()` or equivalent thread pool; (2) fast-path: return healthy immediately, model load completes in background; (3) verify cold-start API responds <5s (vs current 30s+); (4) unit test: lifespan completion verified; integration test: API responds during model load. AC: (1) RAG lifespan refactored; (2) unit test GREEN; (3) cold-start API latency <5s; (4) health endpoint returns 200 before model fully loaded (graceful degradation). Estimate: 3h. Zone=apps/rag-service/. Owner=dev-rag-service. | LOW | TASK | dev-rag-service | docs/handoffs/TASK_1958-watchdog-6.md | — |
| 1955a | **[done 2026-05-20 agent-father commit 8b23795a] Sprint 1955 Phase 2 — Master dispatcher slot-locking (Step 4.6)** — Wire `.claude/flows/cowork-team/main.md` Step 4.6 per architecture brief §7. Insertion point: between Step 4b (collision-detection guard) and Step 5 (parallel fan-out). For each slot in MATCHES, compute `task_id = "cowork-slot:" + slot.slot_id + ":" + nominal_tick` (rounded-down UTC minute). Call `task_claim(task_id, task_kind="cowork-slot", owner_agent="cowork-team", ttl_seconds=900, payload=JSON)`. On claim=false, send WORK Telegram and SKIP that slot. On claim=true, proceed to Step 5 to spawn. After spawn returns (success or failure), call `task_release` for that task_id. Edge cases: all slots held → silent exit w/ telemetry `all_held=true`; partial holds → spawn only won slots; spawn failure after claim → release immediately. Model 1 (selected per brief rationale): master holds lock 900s, agent doesn't heartbeat, TTL covers one 15-min cycle. AC: (1) Step 4.6 inserted and compiled; (2) cowork-team.md flow runs without syntax errors; (3) 3 test cases: all slots won, partial win, all held — all matched behavior verified; (4) after cowork-team runs once post-deploy, `coordination.db` task_locks table shows ≥1 row with task_kind='cowork-slot'; (5) decision comment block added explaining Model 1 choice. Estimate: 2h. Zone=`.claude/flows/cowork-team/`. Owner=agent-father. | HIGH | TASK | agent-father | docs/handoffs/TASK_1955a.md | — |
| 1955b | **[done 2026-05-20 qa commit aaa4a06d] Sprint 1955 Phase 2 — Multi-session collision smoke test** — Write integration test simulating two parallel cowork-team master dispatchers firing at the same nominal_tick. Test harness: (A) two async/parallel invocations or test processes of cowork-team dispatcher logic (reuse Step 2–5 from main.md, bypass cron layer); (B) both fire at same nominal_tick; (C) verify only one wins each slot's task_claim, other skips silently; (D) verify Agent tool spawn count = number of slots (not 2× slots); (E) verify after completion, next nominal_tick can claim cleanly (no stale locks). File: `apps/mcp-server/tests/integration/cowork-team-multi-session-smoke.test.ts` or equivalent integration test location. AC: (1) test file exists; (2) test compiles, tsc 0 errors; (3) 5 test cases pass (slots won, skipped, cleanup); (4) Agent spawn count matches expected; (5) no dangling locks after test exits. Size=M (test harness + 5 test cases + spy/mock on task_claim). Estimate: 3h. Zone=`apps/mcp-server/tests/` (or test coordinator). Owner=qa or dev-mcp-server (whichever runs integration tests). Depends: 1955a merged. | HIGH | TASK | qa | docs/handoffs/TASK_1955b.md | 1955a-merged |
| 1955c | **[done 2026-05-20 agent-father commit d10112a3] Sprint 1955 Phase 2 — Verify 3 cowork agents have task-lock tools in packages** — Audit `.claude/tools/package/{report-analyzer,qa-responder,market-analyst}.md` and confirm each lists `task_claim`, `task_heartbeat`, `task_release` (Phase 2 requires only claim+release; heartbeat deferred to Phase 3). If missing, add them to the Tools Provided section. Cross-check: all 10 scheduled cowork agents (alert-commander, market-watcher, news-scout, financial-analyst, unified-agent, digest-predict, tran-ngoc-bau, report-analyzer, qa-responder, market-analyst) must have the three tools listed. AC: (1) all 10 agents' tool packages verified; (2) report-analyzer, qa-responder, market-analyst tool lists updated if needed (should be 3 tools each for Phase 2, 4th tool task_list_held optional); (3) grep confirms all 10 list at least task_claim. Estimate: 0.5h. Zone=`.claude/tools/package/`. Owner=agent-father (or pm as a small SSOT check task). | MEDIUM | TASK | agent-father | docs/handoffs/TASK_1955c.md | — |
| 1955d | **[done 2026-05-20 agent-father commit f38608f5] Sprint 1955 Phase 2 — Documentation: cron-jobs.md + MEMORY.md** — Update two files per brief rationale: (A) `docs/standards/cron-jobs.md` add a row in the "Claude Code Agent Crons" section under "cowork-team (master dispatcher)" entry, note the new collision-safety property via task-lock (`cowork-slot` kind, Model 1, 15-min TTL, no agent heartbeat). ~2 lines. (B) `docs/agent-memory/MEMORY.md` append a project memory index entry: "Phase 2 cowork-slot locking — 2026-05-20 Sprint 1955, brief `docs/architecture-briefs/2026-05-20-task-lock-system.md`, Phase 2 tasks 1955a–1955d landed 2026-05-20, commit hash." AC: (1) cron-jobs.md updated with collision-safety note under master dispatcher; (2) MEMORY.md index entry created; (3) both files readable and consistent with Phase 2 completion. Estimate: 0.5h. Zone=`docs/`. Owner=agent-father or pm. | MEDIUM | TASK | agent-father | docs/handoffs/TASK_1955d.md | — |
| 1954b | **BLOCKED — Sprint 1954 design phase: OCR-completion ACK token + writer contract** — Define canonical OCR-completion acknowledgment pattern between Stage 2 (OCR write to `pdf_extracted_text`) and Stage 3 (cache-read). Per brief §2 Failure B and §4 architectural rot, Stage 3 must synchronously validate that Stage 2 rows exist BEFORE calling pipeline. Architectural decision: (1) add `ocr_completion_ack` table with `(filename, completed_at, row_count)` OR (2) extend `pdf_extracted_text` with `batch_id` + transactional barrier. Document in `docs/architecture-briefs/2026-05-19-bctc-write-chain-rca.md` as decision record (append §8). Also define writer contract: Stage 2 MUST insert completion-ack ONLY after ALL page rows committed; Stage 3 cache-read MUST check ack before returning null. AC: (1) decision doc appended to brief; (2) next 2 tasks (1954c, 1954d) reference this contract in their code reviews. Estimate: 3h (design + doc + code-review setup). Zone=`apps/mcp-server/`. Owner route: dev-mcp-server + architect-oversight (design review, not write). **PO GATE (2026-05-19):** Allowed to KICK OFF 2026-05-20 in parallel with 1954a sign-off (design-only, no code). | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1954b.md | 1954a-verified (kick off 2026-05-20) |
| 1954c | **BLOCKED — Consolidate 4 queue-entry paths to single owner** — Per brief §4, collapse `backfillBctcQ12026` (A), `bctcQueueEnricher.discoverHosePdfUrls()` (B), push-endpoint `server.ts:703` (C), and `bctcReparseJob.scanDiskForStrandedPdfs()` (D) into a single canonical entry: pull job `bctcPdfPullJob.runBctcPdfPullJob()` owns all writes. Work: (1) redirect backfill output to standard queue insert format (AC from 1954a already validates schema); (2) push-endpoint queues rows instead of calling extraction inline; (3) reparse-disk-scan adds found PDFs to queue instead of triggering extraction directly. Write contract from 1954b applied. Verify: all entry paths write to `bctc_vps_queue` with identical row shape; no extraction triggered outside pull job. AC: (1) 4 paths tested to produce identical queue row format; (2) integration test: push endpoint + enricher + backfill all load queue, pull job processes all; (3) zero duplicate queue writes per filename per 24h window. Estimate: 4h (refactor + integration tests). Zone=`apps/mcp-server/`. **PO GATE: recurring-bug-escalation freeze — 1954c is the consolidation design; no other mcp-server/PDF patches until 1954c approved.** | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1954c.md | 1954b-contract-defined |
| 1954d | **BLOCKED — Per-page OCR timeout + DPI escalation for scanned PDFs** — Per brief §3, under-extraction on scanned PDFs (EIB 3/40, DHG 3/36) caused by: (A) 45s per-page timeout too aggressive for scanned PDFs + tesseract confidence thresholds; (B) 2s inter-page yield accumulates 60s+ idle for long PDFs. Work: (1) reduce inter-page yield to 200ms (line 284 pdfOcrWorker.ts); (2) add adaptive timeout: if page ≤5 chars extracted, re-run with DPI 600 (doubled from 300) in second pass; (3) add explicit page-type detection (vector vs scanned bitmap heuristic: if first 5 pages <10 chars avg, treat rest as scanned, use DPI 600 baseline). Test against EIB/DHG Q1-2026 samples. AC: (1) pdfOcrWorker compiles, tsc 0 errors; (2) EIB/DHG re-extraction yields ≥15/40 and ≥15/36 pages respectively (≥40% vs baseline 8%); (3) GAS/FPT extraction time reduced by ≥30% (proof: OCR wall-clock time in logs); (4) confidence distribution plot shows ≥50% of high-confidence pages. Estimate: 4h (timeout tuning + DPI escalation + test harness). Zone=`apps/mcp-server/`. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1954d.md | 1954c-single-owner-complete |
| 1954e | **BLOCKED — Backfill + reparse stranded PDFs with new writer chain** — One-shot execution (not an automated cron addition): call `bctcReparseJob.runBctcReparseJob()` with production DB against all stranded PDFs found in `data/pdfs/`. This unblocks Q1-2026 financial_reports rows for FPT/GAS/EIB/DHG once 1954c consolidation + 1954b ACK token in place. Work: script at `apps/mcp-server/scripts/backfill-bctc-q1-2026-finalize.ts` (similar to 1953b backfill but now uses consolidated queue entry via 1954c + ACK token via 1954b). Run manually post-1954d deploy, monitor logs, verify ≥1 financial_reports row per ticker. AC: (1) script executes idempotently (INSERT OR REPLACE semantics); (2) bctc_vps_queue rows processed; (3) `SELECT COUNT(DISTINCT stock_code) FROM financial_reports WHERE period_year=2026 AND period_quarter='Q1'` ≥ 26 distinct tickers. Estimate: 1h (script + manual run + log verification). Zone=`apps/mcp-server/`. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1954e.md | 1954d-dpi-escalation-complete |
| 1954f | **BLOCKED — QA VERIFY — Q1-2026 financial_reports coverage** — Observation gate + acceptance verification. Gate: 48h post-1954e manual backfill. AC: (1) `SELECT COUNT(DISTINCT stock_code) FROM financial_reports WHERE period_year=2026 AND period_quarter='Q1'` ≥ 26 of 34 watchlist tickers; (2) zero extraction errors in bctcReparseJob cron logs (daily 02:30Z UTC run post-deploy); (3) per-ticker validation_status distribution: ≥60% 'validated', ≤15% 'low_confidence'. If PASS → Sprint 1954 COMPLETE, unblock downstream financial-analyst + fusion pipelines. If FAIL → escalate 1954g (diagnostic + potential 1953e fallback SSC/VPS URL rewrite). Estimate: OBSERVE (automated SQL + log check). Zone=`ops` + `qa`. | MEDIUM | OBSERVE | qa | — | 1954e-backfill-complete + 48h |
| 1953-FixB | **Signal transport materialization** — news-scout flow writes `docs/signals/news_impact_<ts>.json` + financial-analyst flow writes `docs/signals/bctc_signal_<ts>.json` after each batch. Schema: `{ schema, ticker, signal_type, source_tier, key_fields: {...}, generated_at }`. Closes signal-transport gap identified in `docs/architecture-briefs/2026-05-19-data-fusion-gap.md §4 Fix B`. AC: both file patterns present in `docs/signals/` within 30min of each agent cycle; chef.md Step 0 file-read loop finds ≥1 file per type in 24h window. Owner route: cowork agent .md edits (news-scout cycle.md + financial-analyst cycle.md) via dev-mcp-server or agent-father. | MEDIUM | TASK | dev-mcp-server | — | 1951b-complete |
| 1953-FixC | **Signal fusion rules standard** — Create `docs/standards/signal-fusion-rules.md` (~60 lines) defining: canonical join key (ticker + 24h window), conflict-arbitration rule (price_anomaly direction contradicts fundamental_validation → label "divergence", set conviction LOW), multi-source conviction formula (base on weakest-confidence signal, adjust up only when all sources agree on direction). Reference from chef.md Step 0 lazy-load and SYNTHESIZE step (6.5). AC: file exists + chef.md Step 0 knowledge block lists it + TNB can cite §rule-id in audit verdicts. Ref brief: `docs/architecture-briefs/2026-05-19-data-fusion-gap.md §4 Fix C`. Owner: architect (design) + agent-father (file creation + chef.md lazy-load hook). | MEDIUM | TASK | architect | — | — |
| 1948a | **BLOCKED: Sprint 1948 Phase 1** — `improve_check_log` schema + `improveCheckStore.ts` (domain/infra DB layer). DB migration to `schema-system.ts` + Drizzle types + 6 store functions (insert, getPending, update, getRecentCheck, etc). Unit tests: insert, getPending, update, getRecentCheck, schema guard. Size=S. Zone=apps/mcp-server/. **PRE-CONDITION:** Blocked until post-1945-verdict-resolution-scored-pct gate clears 2026-05-20T07:22Z. If gate misses, resolution pipeline fix takes priority. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1948a.md | 2026-05-20T07:22Z (post-1945-verdict-resolution-scored-pct gate) |
| 1948b | **BLOCKED: Sprint 1948 Phase 1** — `degradationRules.ts` domain service (pure, zero imports). `DegradedSignalType` + `DegradationHypothesis` interfaces, `DEGRADATION_CAUSE_MAP` rule-table, `classifyDegradation()` + `lookupHypothesis()` functions. Detection logic: 7d vs 30d `accuracy_rate` delta ≥10pp OR baseline <40% with ≥10 samples per signal_type. Unit tests: degraded / not-degraded / insufficient-sample / null-rates / persistently-low / neutral. Size=S. Zone=apps/mcp-server/. **PRE-CONDITION:** Blocked until post-1945-verdict-resolution-scored-pct gate clears 2026-05-20T07:22Z. Depends on 1948a. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1948b.md | 1948a + 2026-05-20T07:22Z gate |
| 1948c | **BLOCKED: Sprint 1948 Phase 1** — `selfImproveOrchestratorJob.ts` scheduler entry + wiring + integration tests. Shadow-mode only: no signal-bus write, WORK Telegram output only. Detection loop: 7d/30d accuracy-rate delta, coverage-gap query, hypothesis lookup, `improve_check_log` snapshot write. Phase 1 AC: ≥1 degraded signal type detected → exactly 1 WORK Telegram; zero degradation → clean exit; all detect/hypothesis/log cycles tested (6+ tests). Wiring: add `CRONS.selfImproveOrchestrator` to cronConfig.ts, wire in startScheduler.ts, add env vars to .env.example. Size=M. Zone=apps/mcp-server/. **PRE-CONDITION:** Blocked until gate clears. Depends on 1948a + 1948b. | HIGH | TASK | dev-mcp-server | docs/handoffs/TASK_1948c.md | 1948a + 1948b + 2026-05-20T07:22Z gate |
| 1948e-C | **SPIKE-1948e-fix child 3/3 (OPTIONAL, deferred)** — Add PC1 to primary watchlist (identical scope to 1946a PLX). Files: `docs/data/system-map.json` `.project.watchlist[]`, `apps/mcp-server/mcp.config.json` `.market.watchlist[]`, `apps/mcp-server/src/infrastructure/db/seedWatchlist.ts` `WATCHLIST_SEED`, `domain/market.ts` `WATCHLIST_STOCKS`. Tests: seed presence, query returns PC1, idempotency. AC: PC1 in all 3 SSoT sources, tests TC1-TC4 GREEN, regression suite ≥50 tests pass. NOT required for legal_risk signal path (Fix A+B sufficient). Improves news-scout urgency classification. Size=S. Zone=`apps/mcp-server/` + `docs/data/`. Gate: None. PM judgment: schedule separate after 1948e-A+B merge, or defer to cycle 2. | LOW | FIX | dev-mcp-server | docs/handoffs/TASK_1948e-C.md | — |
| OBSERVE-1948d | **BLOCKED: Sprint 1948 Phase 1 observation gate** — 7-day shadow-mode verification (2026-05-25T09:00Z post-deploy). AC: ≥1 degraded signal type detected (improve_check_log.dispatch_status='shadow'), ≥1 WORK Telegram sent with detection details, zero false-positive auto-dispatches (Phase 1 is shadow-only), no DB errors, cron_job_runs shows 7 consecutive successful runs. Size=OBSERVE. **PRE-CONDITION:** Blocked until 1948c deploys (2026-05-20T09:00Z assumed, after gate clears and tasks complete). | MEDIUM | OBSERVE | ops | — | 1948c deployment + 2026-05-25T09:00Z |
| alert-precision-488-unknowns | **MONITORING**: Post-DB-rebuild agent_signals=46 (fresh DB). HOLD until ≥550. From TNB c58 Finding #8 + bug 2874. | MEDIUM | TRACKING | — | — | — |
| fa-shape-guard-watch | **MONITORING**: Next observation = first post-restart FA live session. Auto-cure trigger: REGIME-mismatch or news-fallback → spawn 1921a-fa-shape-guard-propagate. If NEUTRAL macro_snapshot → close. | MEDIUM | TRACKING | — | — | — |
| 1897b-carry | F1 USER: Docker .git/ exclude bundle + VirtioFS structural fix. PREFLIGHT cure permanent policy (1906a c89). F1 USER action (Docker .git/ exclusion) is the only structural cure. Brief: `docs/architecture-briefs/2026-05-13-headlock-recurrence-post-F2a.md`. | HIGH | URGENT-F1 | user | — | — |
| SPIKE-1952a-tnb-layer-rubric | **Analysis-quality #2** — Formal 6-layer scoring rubric for TNB chef-dish audit (data discipline / US stack / VN stack / 4-pillar / 6-step / gap catalogue) → numeric coverage % per dish. Auto-flag dishes <70% back to unified-agent for revision. ARCH must design: rubric schema (per-layer 0/0.5/1 or 0–1), aggregation (mean/min), threshold (<70% retry), audit_score_log table (or extend improve_check_log), retry mechanism (signal back to chef), false-positive guard. Output: brief + AC for BA. Time-box 3h. Zone=`apps/mcp-server/` + `agents-architect`. Gate: none. | HIGH | SPIKE | architect | — | — |
| SPIKE-1952b-convergence-detector | **Analysis-quality #3** — Cross-source triangulation: same stock with ≥3 source agreements over 24h = HIGH-confidence signal; conflicts = WORK alert. Sources: financial-analyst + news-scout + report-analyzer + foreignFlow. ARCH must design: convergence query (window 24h, group by stock_code+direction, count distinct from_agent ≥3), conflict detection (≥2 BULLISH AND ≥2 BEARISH same stock), output channel (new signal_type='convergence' OR WORK msg only), dedup (per stock per direction per 24h), interplay with critic-gate. Zone=`apps/alert-engine/` reading `agent_signals` (cross-zone). Time-box 3h. | HIGH | SPIKE | architect | — | — |
| 1952c-business-context-mandatory | **Analysis-quality #4** — Make business-context fields (product/customer/ops/mgmt) MANDATORY for any agent signal naming a ticker. Reject signals missing fields at `postSignalWithCriticGate()` wrapper. Extends 1939a critic-gate with new check (`business_context_complete`). Zone=`apps/mcp-server/`. Gate: 7-day observation that 1939 critic-gate is stable (zero false-rejection escalations). Pre-block until 2026-05-25 (1939 stability window). Size=S. | MEDIUM | FIX | dev-mcp-server | — | post-1939-critic-gate-stable (2026-05-25) |
| SPIKE-1952d-macro-regime-classifier | **Analysis-quality #5** — Macro regime classifier on FRED+SBV data → risk-on/off, USD-strength, commodity-cycle labels. All alerts gated by regime (suppress mismatched signals). Existing `get_macro_snapshot` returns TIGHTENING/NEUTRAL/EASING — this is a finer 3-axis overlay. ARCH must design: 3-axis classifier rules (deterministic thresholds vs LLM), label schema (extend macro_snapshot vs new table), gate semantics (HARD-suppress vs flag-only), backward-compat with cowork agents that already read macro_snapshot. Zone=`apps/macro-indicators/`. Time-box 3h. | MEDIUM | SPIKE | architect | — | — |
| 1952e-brier-per-agent-weighting | **Analysis-quality #1 (Phase 2)** — Per-agent Brier score on `signal_outcomes` (T+24h verified) → weight forward signals by historical accuracy. Extends SPIKE-1947 closed-loop brief (Phase 1 = degradation detect only). New: `agent_accuracy_history` view computing Brier per (from_agent, signal_type), inject score into critic-gate or post_agent_signal payload. Pre-condition: Sprint 1948 Phase 1 stable + ≥30 resolved signal_outcomes rows. ARCH brief needed before BA spec. Zone=`apps/mcp-server/`. | MEDIUM | SPIKE | architect | — | Sprint-1948-stable + 1941b-signal-outcomes-seed-window |
| 1953e | **Sprint 1953 FALLBACK (conditional) — widen bctcPdfPullJob URL filter OR enricher SSC→VPS rewrite** — Trigger ONLY if at 48h post-1953b-2 + 1953a deploy >5 idle SSC-URL queue rows (attempts=0). Option A (risky): widen `bctcPdfPullJob.ts:L238` SQL filter to handle both VPS and SSC URLs — but downloads SSC host-side, geo-block risk per `project_bctc_vps_proxy.md`. Option B (preferred): fix `bctcQueueEnricher` to rewrite SSC URLs into VPS-format URLs once VPS has cached the file. Verification: 0 SSC-URL queue rows with attempts=0 after 24h. Size=M. Zone=`apps/mcp-server/`. Trigger condition: 48h observation + residual SSC-URL backlog. | LOW | FIX | dev-pdf-extractor | — | 48h-observation |
| OBSERVE-1907a-verify | **Verify digest-predict natural Sunday fire post-1957a reactivation.** Gate: 2026-05-24T14:30Z (45min after expected 13:47Z fire). AC: (1) `docs/agent-memory/notebooks/digest-predict.md` shows new cycle entry timestamped 2026-05-24T13:47Z±5min; (2) MARKET channel receives weekly digest msg; (3) `cowork-schedule.json` `digest-sunday.last_fired` rises from null to non-null. If MISS → escalate Sprint 1958 (dev-mcp-server diagnostic: RemoteTrigger fire telemetry vs claude.ai dispatcher). If PASS → close 1907a permanently. | MEDIUM | OBSERVE | ops | — | 2026-05-24T14:30Z |
| OBSERVE-1957d | **Sprint 1957 OBSERVE — BCTC VPS push cadence 72h tracker.** Triggered 2026-05-20T04:23Z (po c215 triage of system-auditor Tier-2 B-10/B-05a + Tier-3 B-06). Last BCTC push 2026-05-19T07:05:07Z. Gate: 2026-05-23T07:05:07Z (72h window). AC-PASS: (1) BCTC VPS push count rises ≥3 during window (proves discovery alive), OR (2) Q1-2026 financial_reports coverage ≥26 lands at OBSERVE-1953g (2026-05-21T02:30Z) — both indicate ecosystem healthy. AC-ESCALATE: BOTH miss AND no new BCTC pushes by 2026-05-23T07:05Z → spin Sprint 1958 with dev-vps-crawls diagnostic (Vinahost VPS BCTC scraper status + SSC source page reachability). Earnings window context: late Q2 (Apr–May) quiet period is normal per system-auditor Tier-3 notes; this gate exists to prove "quiet ≠ broken". Zone=`apps/vps-crawls/` (only if escalates). | LOW | OBSERVE | ops | — | 2026-05-23T07:05:07Z |
| OBSERVE-1955c | **Sprint 1955 OBSERVE — vnstockFundamentalsRefresh next scheduled fire.** Verify weekly Mon 01:00Z tick fires correctly post-1955b deploy. Gate: 2026-05-25T01:30Z. AC: `cron_job_runs` has new row `started_at='2026-05-25 01:00'` with `status IN ('success','error')` AND `finished_at IS NOT NULL`. If status='running' with NULL finished_at after 02:00Z → genuine hang, escalate to 1955e diagnostic (timeout / API lock investigation). | MEDIUM | OBSERVE | ops | — | 2026-05-25T01:30Z |
| OBSERVE-1955d | **Sprint 1955 OBSERVE — vnstockTradingStatsRefresh next scheduled fire.** Verify weekday 08:30Z tick fires correctly post-1955b deploy. Gate: 2026-05-20T09:00Z (next Tue 2026-05-20). AC: `cron_job_runs` has new row `started_at='2026-05-20 08:30'` with `status IN ('success','error')` AND `finished_at IS NOT NULL`. If status='running' with NULL finished_at after 09:30Z → genuine hang, escalate to 1955e diagnostic. | MEDIUM | OBSERVE | ops | — | 2026-05-20T09:00Z |

---

## Todo

| 1962b | **[PLAN READY] Sprint 1962 Phase 3.5 — Task-lock dispatcher-wrap row-level plan** — PM decompose architect brief 1962a into one atomic task per spawn site (S1–S7, 7 total). Each spawn site = one commit per c47 policy. Grouped S2+S3+S4 (all in main.md) into one commit per split policy. DELIVERED: `docs/handoffs/TASK_1962-dispatcher-wrap.md` with row-level decomposition: S1 execute-tier (1962c-1), S2/S3/S4 main.md (1962c-2+3+4 grouped), S5 developer (1962c-5), S6 ba (1962c-6), S7 pm (1962c-7); each row includes: file, lines, edit summary, owner (agent-father), commit subject, verification steps (claim/release pair, dispatcher identity, TTL). AC all met. Output signal: `docs/signals/pm-1962b-plan-done.json` type=plan_complete. Zone=`.claude/`. No code, no MCP, pure flow edits. Depends: 1962a DONE. | HIGH | TASK | pm | docs/handoffs/TASK_1962-dispatcher-wrap.md | — |

| 1962c-1 | **Sprint 1962 Phase 3.5 — S1 execute-tier.md dispatcher-wrap** — Agent-father wire outer task_claim/task_release around parallel Agent() spawns in execute-tier.md lines 33–38 (tier-parallel batch). Model 1 pattern: for-loop over task_ids, claim each, filter wins, spawn wins, release wins. Owner-agent="dev-team". TTL=3600s. AC: (1) claim/release pair present; (2) dispatcher identity correct; (3) TTL=3600s; (4) syntax valid, .md parses. Commit subject: `chore(pm/1962c-1): dispatcher-wrap S1 execute-tier parallel fan-out`. Size=S. Estimate: 0.5h. Depends: 1962b-done. | HIGH | TASK | agent-father | docs/handoffs/TASK_1962-dispatcher-wrap.md (§S1) | 1962b-done |

| 1962c-2+3+4 | **Sprint 1962 Phase 3.5 — S2/S3/S4 main.md dispatcher-wrap** — Agent-father wire outer task_claim/task_release around three spawn sites in dev-team/main.md: S2 (line 122 pipeline-resume, activeTaskId), S3 (line 134 po-triage, synthetic task:po-triage-YYYYMMDD TTL=1800s), S4 (lines 150–151 UNBLOCK/CLEAN, batch_id). All owner-agent="dev-team". S2/S4 TTL=3600s, S3 TTL=1800s. Single commit (one file, three hunks per c47 policy). AC: (1) claim/release pairs for S2, S3, S4; (2) dispatcher identity="dev-team"; (3) TTL correct (3600s S2/S4, 1800s S3); (4) S3 synthetic key generation correct (date format); (5) S2 uses activeTaskId from pipeline-state.json; (6) syntax valid. Commit subject: `chore(pm/1962c-2+3+4): dispatcher-wrap S2/S3/S4 three main.md hunks`. Size=S. Estimate: 1h. Depends: 1962b-done. | HIGH | TASK | agent-father | docs/handoffs/TASK_1962-dispatcher-wrap.md (§S2,§S3,§S4) | 1962b-done |

| 1962c-5 | **Sprint 1962 Phase 3.5 — S5 developer.md dispatcher-wrap** — Agent-father wire outer task_claim/task_release around multi-zone developer fan-out in developer.md line 37 (parallel Agent(dev-*, taskX) spawns). Model 1 pattern: for-loop, claim each task_id with owner-agent="developer", collect wins, spawn wins, release wins. TTL=3600s. AC: (1) claim/release surrounding spawn_pattern; (2) dispatcher identity="developer"; (3) TTL=3600s; (4) no inner self-claim touched; (5) syntax valid. Commit subject: `chore(pm/1962c-5): dispatcher-wrap S5 developer multi-zone fan-out`. Size=S. Estimate: 0.5h. Depends: 1962b-done. | HIGH | TASK | agent-father | docs/handoffs/TASK_1962-dispatcher-wrap.md (§S5) | 1962b-done |

| 1962c-6 | **Sprint 1962 Phase 3.5 — S6 ba.md dispatcher-wrap** — Agent-father wire outer task_claim/task_release around ba architect fan-out in ba.md line 118 (parallel Agent(architect, REQ_NNN) spawns). Model 1 pattern. Owner-agent="ba". TTL=3600s. AC: (1) claim/release surrounding architect spawns in spawn_pattern; (2) dispatcher identity="ba"; (3) TTL=3600s; (4) syntax valid. Commit subject: `chore(pm/1962c-6): dispatcher-wrap S6 ba architect fan-out`. Size=S. Estimate: 0.5h. Depends: 1962b-done. | MEDIUM | TASK | agent-father | docs/handoffs/TASK_1962-dispatcher-wrap.md (§S6) | 1962b-done |

| 1962c-7 | **Sprint 1962 Phase 3.5 — S7 pm.md dispatcher-wrap (HIGHEST priority)** — Agent-father wire outer task_claim/task_release around pm dev-specialist fan-out in pm.md line 124 (parallel Agent(dev-*, TASK_NNN) spawns). Model 1 pattern: for-loop, claim task_id with owner-agent="pm", collect wins, spawn wins, release wins. TTL=3600s. This is the primary planner site; gap report c221 specifically flags dev-alert-engine FIX + dev-mcp-server FIX double-dispatch from here. AC: (1) claim/release surrounding dev-* spawns in spawn_pattern; (2) dispatcher identity="pm"; (3) TTL=3600s; (4) claim-fail path skips spawn, continues; (5) no cross-zone WIP regression; (6) syntax valid. Commit subject: `chore(pm/1962c-7): dispatcher-wrap S7 pm dev-specialist fan-out`. Size=S. Estimate: 0.5h. Depends: 1962b-done. | HIGHEST | TASK | agent-father | docs/handoffs/TASK_1962-dispatcher-wrap.md (§S7) | 1962b-done |

| 1962d | **Sprint 1962 Phase 3.5 — QA smoke: multi-router collision prevention** — QA validate dispatcher-wrap collision-prevention logic. Test: two parallel Agent(dev-stock-price, task:test-1962d) dispatches with same task_id → expect one claimed=true (first router spawns agent), one claimed=false (second router skips, WORK Telegram). Verify Agent spawn count=1 (not 2). Verify stale locks don't interfere (after task_release, next claim succeeds). Test scenarios: T1 double-dispatch same dispatcher, T2 outer claim → release → inner claim sequence, T3 cross-owner-agent collision. AC: (1) T1 PASS (one claimed=true, one claimed=false); (2) T2 PASS (inner claim fires after outer release); (3) T3 PASS (collision on task_id regardless of owner_agent); (4) SKIP Telegram emitted on claimed=false; (5) post-dispatch cleanup verified (no dangling locks). Size=S. Estimate: 1h. Depends: 1962c-1 through 1962c-7 merged. | HIGH | TASK | qa | docs/handoffs/TASK_1962d.md (create per brief §8) | 1962c-all-merged |

| 1962e | **Sprint 1962 Phase 3.5 — Docs: task-lock SKILL + MEMORY.md** — Agent-father update `.claude/skills/task-lock/SKILL.md` Phase Status table to note dispatcher-wrap (Phase 3.5) shipped. Append `docs/agent-memory/MEMORY.md` project index entry: "Phase 3.5 dispatcher-wrap outer-claim pattern — 2026-05-21 Sprint 1962, brief `docs/architecture-briefs/2026-05-21-task-lock-dispatcher-wrap.md`, 1962b plan + 1962c-1 through 1962c-7 commits + 1962d smoke PASS, phase_3_5 shipped 2026-05-21." AC: (1) SKILL.md Phase Status row added; (2) MEMORY.md entry created; (3) both files readable and consistent. Size=XS. Estimate: 0.5h. Depends: 1962d PASS. | MEDIUM | TASK | agent-father | docs/handoffs/TASK_1962e.md (or combined with 1962d) | 1962d-pass |

| OBSERVE-1951d-verify | **Sprint 1951 Phase 1 AC-3 — 24h cowork cycle post-merge verification.** Gate: 2026-05-21T08:30Z (next market open after merge window 2026-05-20 20:00Z–2026-05-21 08:30Z VN time). AC: (1) ≥1 cowork cycle fires (cowork-team-*.json signal in docs/signals/); (2) MARKET channel receives ≥1 agent message; (3) zero "trigger not found" errors in logs; (4) cowork-schedule.json dispatcher executes without collision errors (task-lock Phase 2 gates already in place from 1955a); (5) master CronCreate */15 tick appears in cron_job_runs. If ALL PASS → 1951d closure complete, master dispatcher operational. If ANY FAIL → escalate to architect (cowork-dispatcher stability concern, may need revert to hybrid RemoteTrigger fallback). | MEDIUM | OBSERVE | qa | — | 2026-05-21T08:30Z |
| post-1945-verdict-resolution-scored-pct | **Sprint 1945 AC-1 OBSERVE** — gate 2026-05-20T07:22Z (48h post-1945a deploy). AC: `alert_accuracy.scored_pct` rises ≥60% AND `unknowns_30d` drops by ≥100 (was 36%/520 pre-1945a). If miss → 1947b-verdict-resolution-followup (HIGH FIX, dev-mcp-server). | HIGH | OBSERVE | ops | — | 2026-05-20T07:22Z |
| post-1945-bug-storm-silence | **Sprint 1945 AC-2 OBSERVE** — gate 2026-05-20T07:22Z (48h). AC: zero new `[bug] verdictResolutionJob` Telegram messages for 48h post-1945a deploy. Any new msg → 1947c-verdict-resolution-bug-followup. | MEDIUM | OBSERVE | ops | — | 2026-05-20T07:22Z |
| 1941b-signal-outcomes-seed-window | **Sprint 1941 TIER 2** — 7-day OBSERVE on `signal_outcomes` seeding. AC by 2026-05-25: ≥30 resolved rows (outcome_24h ∈ correct/incorrect/neutral) across ≥3 distinct `signal_type` values. Failure mode → bug task to dev-mcp-server (seed path broken in postSignal wrapper). No code change unless escalation triggers. | MEDIUM | OBSERVE | ops | — | 2026-05-25 |
| 1922g-pharma-events-source-verify | **OBSERVE** — `pharma_events` empty. `davPharmacyJob` cron `0 6 1 * *`. Next tick = 2026-06-01 06:00 UTC. AC: check status + row count after tick. | LOW | OBSERVE | ops | — | 2026-06-01 |
| 1922i-alert-engine-records | **WONTFIX c160 (SPIKE-1933a resolved)** — alert_engine_records always 0: evaluateAlert() dead code deleted (1933b). Architecture: market.db.alerts → Alert Commander = canonical intelligence path. Go alert-engine (/evaluate) reserved for future stop-loss use case. | MEDIUM | WONTFIX | — | — | — |
| post-1942-fa-verify | **MONITOR** — Verify FA next live cycle (~23:00 UTC tonight) reports ≥20/30 BCTC analyses (was 3/38 pre-1942). If still 3/38 → deploy-gap bug task to dev-mcp-server (Docker rebuild). Auto-close on observation. | MEDIUM | OBSERVE | ops | — | 2026-05-19 |
| OBSERVE-1953g | **Sprint 1953 AC-3 — 48h financial_reports coverage check.** Gate: 2026-05-21T02:30Z (after 2nd daily bctcReparseJob tick post-deploy). Deploy verified 2026-05-19T19:14Z (EPIPE fix working, OCR no crashes, bctc_vps_queue_pending=103). EIB/DHG under-extracted (3/40 and 3/36 pages — likely image-heavy scanned PDFs). AC: `SELECT COUNT(DISTINCT stock_code) FROM financial_reports WHERE period_year=2026 AND period_quarter='Q1'` ≥ 26. If pass → Sprint 1953 COMPLETE. If fail → open 1953e (SSC/VPS URL fix) AND 1953h (EIB/DHG low-extraction diagnostic). Note: bctcReparseJob runs daily 02:30Z UTC; first post-deploy run is 2026-05-20T02:30Z, second is 2026-05-21T02:30Z. | HIGH | OBSERVE | ops | — | 2026-05-21T02:30Z |

---


| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|

---

## Review

---
## Done

| 1962a | **[done 2026-05-21 architect]** Sprint 1962 — Task-lock dispatcher-wrap architecture brief. Verified all 7 PO-identified spawn sites present. Designed Model 1 outer-claim pattern (dispatcher holds claim around Agent() spawn, releases immediately after spawn returns). Key decisions: inner self-claim (Phase 3 Model 2) KEPT AS-IS — outer wrap is additive; remove only after 1962d observation window. owner_agent = dispatcher identity for outer claim (dev-team/developer/ba/pm), spawned-agent identity for inner. PO triage uses synthetic `task:po-triage-<YYYYMMDD>` key. No Docker rebuild, no protocol amendment, no new MCP tools. Brief: `docs/architecture-briefs/2026-05-21-task-lock-dispatcher-wrap.md`. Signal: `docs/signals/architect-1962a-brief-done.json`. Zone=`.claude/`. | HIGH | TASK | architect | 2026-05-21 |

| FIX-listunresolvedreports | **[done 2026-05-20 dev-mcp-server]** FIX-listunresolvedreports-tool-registration — Case (a): tool `list_unresolved_reports` IS implemented in `telegramReportTools.ts` and registered in `registry.ts`. Callers in `dev-team/main.md` (lines 18+134), `dev-team/post-cycle.md` (line 18), and `po/main.md` (line 20) used wrong camelCase pseudocode `listUnresolvedReports()` instead of MCP tool name `list_unresolved_reports`. Fixed all 4 occurrences. 12/12 existing tests GREEN. tsc 0 errors. toolCount=146 confirmed on live server. No container rebuild needed. | HIGH | FIX | dev-mcp-server | 2026-05-20 |
| c220 | **[done 2026-05-20 dev-alert-engine]** FIX-alertsource-legal-risk-enum — Added `'legal_risk'` to `WRITE_ALERT_VERDICT_SCHEMA` Zod enum in `alertVerdictTools.ts`. alert-commander Telegram report 2954 (2026-05-20T04:39Z): VPB legal_risk payload rejected, forced fallback to `position_danger`. Fix: one-line enum extension + `c220-legal-risk-alert-source.test.ts` (5 tests: schema accepts, write+readback round-trip, regression guard). `tsc` 0 errors. Zone deviation noted: actual fix in `apps/mcp-server/` (PO spec referenced stale TS paths from pre-Go-migration; Go alert-engine already accepted free-form signalTypes). | HIGH | FIX | dev-alert-engine | — |
| Sprint-1961 | **DONE 2026-05-20T19:45Z (po close signal `docs/signals/po-1961-close.json`)** — Task-lock Phase 1 deployment-gap HOTFIX shipped end-to-end. 1961a (ops: `docker compose up -d --build mcp-server` — container Up healthy, image digest `sha256:598b94c7…`, 146 tools registered), 1961b (ops: 4 Phase 1 tools live-smoked on `vn-market` gateway — claim/heartbeat/list/release round-trip PASS), 1961c (qa: re-ran Phase 2 + Phase 3 smoke against LIVE MCP — 19/19 PASS, signal `docs/signals/qa-1961c-approved.json`, commit `3bb94db3`), 1961d (po: protocol patched — 5-col Phase Availability table + Deployment-verified Ritual, signal `docs/signals/po-1961d-protocol-patched.json`). Closes the "code committed ≠ live availability" class of failure: any future task-lock or MCP-tool sprint MUST rebuild container + live-probe gateway before phase-shipped. | CRITICAL | SPRINT-CLOSE | po | 2026-05-20 |
| 1961c | **[done 2026-05-20T19:45Z qa commit `3bb94db3`]** Sprint 1961 — Live smoke re-validation of Phase 2 + Phase 3 lock semantics on live `vn-market` MCP. Phase 2 9/9 PASS (`scripts/smoke-task-lock-phase2.ts`) + Phase 3 22/22 PASS (10 smoke + 9 grep + 2 pipeline + 1 MCP schema) against post-1961a container (Up 2min healthy). Signal: `docs/signals/qa-1961c-approved.json`. All 4 task-lock tools confirmed callable on gateway. | HIGH | TASK | qa | 2026-05-20 |
| 1961d | **[done 2026-05-20T19:38Z po sibling c220]** Sprint 1961 — Patched `docs/protocols/task-lock-protocol.md`: Phase Availability table extended 3→5 cols (added Container-rebuild SHA + Gateway-tools-callable date), added `## Deployment-verified Ritual` section citing Sprint 1961 gap. c221 backfilled the `_pending_` cells with concrete SHAs (`b144f560` rebuild SHA, `2026-05-20` callable date) at sprint-close. Signal: `docs/signals/po-1961d-protocol-patched.json`. | HIGH | TASK | po | 2026-05-20 |
| Sprint-1960 | **DONE 2026-05-20 (po close signal `docs/signals/po-1960-close.json`)** — Phase 3 task-lock system shipped end-to-end. 1960a (architect brief `docs/architecture-briefs/2026-05-21-task-lock-phase3-devteam.md`), 1960b (pm plan `docs/signals/pm-1960-sprint-plan.json`), 1960c (10 commits: 448eb7f3→31c47ea5, 8 flows + tool packages wired, Model 2 agent-self heartbeat), 1960d (smoke 335dda54, 10/10 PASS), 1960e (docs + MEMORY.md consolidation). All 3 phases of task-lock system now SHIPPED. **Addendum 2026-05-20T19:45Z (1961c):** Phase 3 lock semantics re-verified against LIVE MCP post-1961a container rebuild — original 1960d smoke was against stale image (INVALID). See `docs/protocols/task-lock-protocol.md` Phase Availability table. | HIGH | SPRINT-CLOSE | po | 2026-05-20 |
| 1951d | **[done 2026-05-20 ops commit 093f8905]** Sprint 1951 Phase 1 parallel-run cutover — 12 legacy RemoteTriggers marked deleted in SSOT (cowork-schedule.json trigger_status='deleted' + trigger_id=null). AC-1 PASS (SSOT audit: 12/12 slots marked). AC-2 PASS with platform caveat (RemoteTrigger MCP does not expose delete action — platform UI removal deferred; logical retirement in SSOT sufficient for dispatcher skip). AC-3 PENDING_VERIFY (24h observation window 2026-05-20 20:00Z–2026-05-21 08:30Z). AC-4 PASS (signal ops-1951d-cutover-complete.json emitted). Next: OBSERVE-1951d-verify task queued for QA. Runbook: `docs/protocols/cowork-master-cron-runbook.md`. | MEDIUM | TASK | ops | 2026-05-20 |
| 1959a | **[done 2026-05-20 dev-mcp-server commit b144f560, QA-APPROVED]** coordinationStore.ts + coordinationTools.ts exactOptionalPropertyTypes compliance fix. Three violations: coordinationStore.ts:272 ternary split; coordinationTools.ts:108+204 spread guards. Test files: 4x non-null assertions + 1x cast via unknown. Type-only, tsc 0 errors, 9330/283 suite, remote push unblocked. All 1958a commits now on remote. Report: `reports/TASK_REPORT_1959a.md`. | CRITICAL | FIX | dev-mcp-server | 2026-05-20 |
| 1958a | **[done 2026-05-20 qa commit 84c2b375] Sprint 1958 — 5 MARKET-summary cron jobs not firing.** RCA: event-loop starvation (5h OHLCV backfill + bctcReparseJob zombies) caused node-cron to miss alertDigestJob (14:00 UTC) and summaryJob:daily (15:30 UTC) windows; recoverMissedExecutions=false default + missing startup catchup probes. Fix: recoverMissedExecutions:true on alertDigestJob+summaryJob:daily; startup catchups added for all 5 MARKET-summary jobs. 16 tests GREEN (9287 full suite), zero regression. AC-3 ops observation gate 2026-05-21T09:00Z: verify ≥1 new cron_job_runs success row per job after deploy. Report: `reports/TASK_REPORT_1958a.md`. Architect-followup signal (`docs/signals/qa-1958a-architect-followup.json`, advisory): OHLCV 5h startup backfill structural concern — routed to PO triage, NOT blocking. Remote push BLOCKED by pre-existing tsc errors in coordinationStore.ts/coordinationTools.ts (commit 79ac45e9, exactOptionalPropertyTypes compliance) — fixer task 1959a created. | HIGH | FIX | qa | 2026-05-20 |
| Sprint-1955-Phase2 | **DONE 2026-05-20 (po close signal `docs/signals/po-1955-close.json`)** — Phase 2 cowork-slot locking shipped end-to-end. 1955a (`8b23795a` Step 4.6 slot-lock claim), 1955b (`aaa4a06d`+`e4f3abdb` smoke 9/9 PASS), 1955c (`d10112a3` 10 cowork tool packages verified), 1955d (`f38608f5`+`0d518efe` cron-jobs + MEMORY.md). Multi-session collision risk fully resolved. Next phase 3 (dev-team dashboard-row + sprint-task locks) deferred to demand. **Addendum 2026-05-20T19:45Z (1961c):** Phase 2 lock semantics re-verified against LIVE MCP post-1961a container rebuild — original 1955b smoke ran against stale 11h container that lacked the registered tools. See `docs/protocols/task-lock-protocol.md` Phase Availability table. | HIGH | SPRINT-CLOSE | po | 2026-05-20 |
| 1955b | **[done 2026-05-20 dev-mcp-server]** zombie cron_job_runs reap on startup. CHECK extended to include 'crashed', idempotent migration guard in schema-system.ts, reapZombieJobRuns() called in startScheduler.ts before cron registration. 4/4 tests GREEN (AC-1 happy path, AC-2 idempotence×2, AC-3 migration), tsc 0 errors, full suite 9284+/284 (no regression — 284 failures are pre-existing BCTC PDF parsing, not 1955b). | MEDIUM | FIX | dev-mcp-server | — |
| 1955a | **[done 2026-05-20 dev-mcp-server]** `dailyDashboardJob.ts:459` projectRoot() `..` count reduced 6→3; resolves to /app inside container. 5/5 tests GREEN, tsc 0 errors, full suite 9281/283 (no regression). AC-4 deferred to ops: verify 16:30Z cron tick logs `status=success`. | HIGH | FIX | dev-mcp-server | — |
| 1957a | **DONE 2026-05-20T00:00Z (ops)** — 12 legacy cowork RemoteTriggers reactivated from `pending_delete` → `active`. SSOT cowork-schedule.json updated. Cowork pipeline restored ahead of 1951d cutover. Signal: `docs/signals/processed/ops-1957a-triggers-reactivated.json`. | CRITICAL | HOTFIX | ops | 2026-05-20 |
| 1957b | **DONE 2026-05-20 (agent-father)** — cron-cowork-team skill + cowork-master-cron-runbook + CLAUDE.md pointer. All 7 ACs met. Gate 1957c cleared. Signal: `docs/signals/processed/agent-father-1957b-cowork-skill-built.json`. | HIGH | TASK | agent-father | 2026-05-20 |
| 1907a-digest-predict-silence | **RESOLVED-PREMISE-WRONG 2026-05-20 (PO c216)** — Original c168 description false: claude_desktop_config.json does not gate cowork MCP (claude.ai server-side gateway). Real root cause = digest-sunday RemoteTrigger `pending_delete` → missed Sun 2026-05-10 + 2026-05-17 fires. Already fixed by Sprint 1957a reactivation. Next natural fire Sun 2026-05-24T13:47Z. Verification = OBSERVE-1907a-verify. Signal: `docs/signals/po-1907a-premise-rewrite.json`. | CRITICAL | OPS | po | 2026-05-20 |
| 1957c | **DONE 2026-05-20 (ops)** — Re-block 1951d cutover by documenting 1957b-done gate in TASKS.md Blocked-by. Prerequisite: 1957b (skill + runbook) must be deployed before 1951d cutover can safely delete 12 legacy RemoteTriggers. Signal: `docs/signals/ops-1957c-1951d-gated.json`. | MEDIUM | TASK | ops | 2026-05-20 |
| Task ID | Title | Priority | Type | Owner | Completed |
| Sprint-1956 | **DONE 2026-05-19 (dev-frontend + dev-rag-service + ops)** — Multi-service recovery: (A) dev-frontend commit `d4fa8648`: renamed `dashboard.server.tsx → dashboard.services.tsx` (Remix v7 server-only module violation); (B) dev-rag-service commit `af0d798b`: CPU-only torch wheel eliminates nvidia-cudnn-cu13 hash mismatch; (C) ops: `docker compose up -d --build` → 11/11 services operational. Duration 65s. Signals: `ops-1956d-final-recovery.json`. | CRITICAL | INCIDENT | dev-frontend+dev-rag-service+ops | 2026-05-19 |
| 1961a | **[done 2026-05-20 ops]** Sprint 1961 HOTFIX — Rebuild + restart `mcp-server-1` container — `docker compose up -d --build mcp-server` executed from repo root. Container restarted, healthcheck passing, Up <2min. AC PASS. | CRITICAL | HOTFIX | ops | 2026-05-20 |
| 1961b | **[done 2026-05-20 ops]** Sprint 1961 — Verify 4 Phase 1 tools live on `vn-market` MCP — Smoke-test `task_claim`, `task_heartbeat`, `task_list_held`, `task_release` via MCP gateway. All 4 tools responded correctly: claim→heartbeat→list→release round-trip successful, array-based list working. AC PASS. | CRITICAL | TASK | ops | 2026-05-20 |
| OBSERVE-1951b | **DONE 2026-05-19T20:34Z — 24h Parallel-Run Validation COMPLETE.** AC-6 PASS: zero MARKET double-published dishes during window. Double-fire detected (22:38Z) was benign — outside prepost-eligible window, no MARKET write. Unblocks 1951d cutover (dispatched to ops). | HIGH | OBSERVE | ops | 2026-05-19 |
| 1954a | **DONE 2026-05-19 (dev-mcp-server commit `2a5cc2a7`, QA-APPROVED + ops AC-3 PASS)** — `backfillBctcQ12026.ts` column names fixed. AC-1 tsc PASS. AC-2 no regressions PASS. AC-3: ops inserted 19/33 rows (14 skipped via OR IGNORE), 53 pending Q1-2026 rows verified. Unblocks 1955a + 1955b. | CRITICAL | HOTFIX | dev-mcp-server | 2026-05-19 |
| CLEAN-1953b-2-ocr-epipe-fix | **[done 2026-05-20 qa]** CLEAN-1953b-2-ocr-epipe-fix — stale local branch `task/1953b-2-ocr-epipe-fix` deleted (0 commits ahead of main, no remote tracking, no worktree pinned). Pre-flight: 3/3 checks passed. | LOW | CLEAN | qa | 2026-05-20 |
| _(1951e-chef-synthesize + 1953a + 1953b-2 + 1953b+1953c + 1951c + 1951h+1951b + 1951j + Sprint-1949/1950/1951a-d + 1952f/g + 1948e-A/B + older archived → `docs/TASKS_ARCHIVE.md`)_ | — | — | — | — | 2026-05-18–19 |

---


| Sprint | Title | Reason | Next Step |
|--------|-------|--------|-----------|
| 1887 | METHODOLOGY-FORENSICS: Virtual Capital / related-party graph detector | Needs own architect brief — graph-store choice, related-party data source, traversal patterns, false-positive control all unspecified | When 1885+1886 ship, queue separate ARCH-1887 brief before ba spec |
| 1892a-ops AC-3 | OPS-NOTE: 1892a-ops AC-3 now UNBLOCKED by 1892b merge (2026-05-12). VPS POST to `/api/push-news` should reach MCP server after deploy. | Unblocked 2026-05-12 | ops re-verify next cycle (observational) |
| TNB-c39-#3 | MONITOR: unified-agent FPT pillar gap (2nd cycle of evidence at c39) | Per TNB protocol need 3rd cycle to auto-cure. | If c40 unified-agent cycle repeats FPT-without-pillars pattern → spawn auto-cure CHORE. If c40 PASSES → close as transient. |
