# TASKS — VN Market Intelligence MCP

> **Active:** Current sprint only. Historical: `docs/TASKS_ARCHIVE.md` | WIP limit: max 2 In Progress | Workflow: Backlog → Todo → In Progress → Review → Done | Branch: `task/NNN-kebab-name` | Report: `reports/TASK_REPORT_NNN.md` | **Archived Done tasks:** See `docs/TASKS_ARCHIVE.md` for complete history (1777–1896+)

---
## Backlog

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
| alert-precision-488-unknowns | **MONITORING**: Post-DB-rebuild agent_signals=46 (fresh DB). HOLD until ≥550. From TNB c58 Finding #8 + bug 2874. | MEDIUM | TRACKING | — | — | — |
| fa-shape-guard-watch | **MONITORING**: Next observation = first post-restart FA live session. Auto-cure trigger: REGIME-mismatch or news-fallback → spawn 1921a-fa-shape-guard-propagate. If NEUTRAL macro_snapshot → close. | MEDIUM | TRACKING | — | — | — |
| 1907a-digest-predict-silence | **CRITICAL** (c168 update): `vn-market` MCP server (`http://localhost:3000/sse`) added to `claude_desktop_config.json` mcpServers. **USER-ACTION: restart Claude Desktop** to load new MCP config. After restart, scheduled cowork tasks will have vn-market MCP access. Verify by checking digest-predict runs again. | CRITICAL | OPS | user | — | — |
| 1897b-carry | F1 USER: Docker .git/ exclude bundle + VirtioFS structural fix. PREFLIGHT cure permanent policy (1906a c89). F1 USER action (Docker .git/ exclusion) is the only structural cure. Brief: `docs/architecture-briefs/2026-05-13-headlock-recurrence-post-F2a.md`. | HIGH | URGENT-F1 | user | — | — |

---

## Todo
| 1945b-backend | **Sprint 1945 TIER 2a** — HTTP handler for accuracy digest endpoint. Deliverables: (1) `GET /api/accuracy/digest?days=N` handler in `server.ts:1021` (~20 lines); (2) import extension for `getSystemAccuracyDigestStats` from signalOutcomeStore; (3) `days` param clamped [1,90] before SQL call (R-4 risk mitigation); (4) 6 test cases in `1945b-accuracy-digest-handler.test.ts` (default/clamp-hi/clamp-lo/absent/error/zero-struct). ACs: AC-6 (clamp validation). Zone: `apps/mcp-server/`. No dependency on frontend. Arch brief: `docs/architecture-briefs/2026-05-18-accuracy-digest-frontend-card.md` §4,8b. Handoff: `docs/TASK_1945b-backend.md`. | MEDIUM | FEATURE | dev-mcp-server | `docs/TASK_1945b-backend.md` | — |
| 1945b-frontend | **Sprint 1945 TIER 2b** — AccuracyDigestCard component + fetch helpers + domain types. Deliverables: (1) `AccuracyDigestStats` + `SignalTypeAccuracyDigest` types in `domain/market.ts:169` (~25 lines); (2) `fetchAccuracyDigest()` + `deriveAccuracyDigestState()` in `client.ts:521` (~30 lines); (3) component + SectionCard in `dashboard.analysis.tsx` (inline, ~100 lines); (4) loader integration (allSettled + LoaderData); (5) 16 test cases in `1945b-accuracy-digest-card.test.ts` (fetch, states, colours, footer, EC checks). ACs: AC-1/2/3/4/5/7 (component states, colours, non-fatal). Zone: `apps/frontend/`. Arch brief: `docs/architecture-briefs/2026-05-18-accuracy-digest-frontend-card.md` §3,5,6,8a. Handoff: `docs/TASK_1945b-frontend.md`. | MEDIUM | FEATURE | dev-frontend | `docs/TASK_1945b-frontend.md` | 1945b-backend |
| 1941b-signal-outcomes-seed-window | **Sprint 1941 TIER 2** — 7-day OBSERVE on `signal_outcomes` seeding. AC by 2026-05-25: ≥30 resolved rows (outcome_24h ∈ correct/incorrect/neutral) across ≥3 distinct `signal_type` values. Failure mode → bug task to dev-mcp-server (seed path broken in postSignal wrapper). No code change unless escalation triggers. | MEDIUM | OBSERVE | ops | — | 2026-05-25 |
| 1922g-pharma-events-source-verify | **OBSERVE** — `pharma_events` empty. `davPharmacyJob` cron `0 6 1 * *`. Next tick = 2026-06-01 06:00 UTC. AC: check status + row count after tick. | LOW | OBSERVE | ops | — | 2026-06-01 |
| 1922i-alert-engine-records | **WONTFIX c160 (SPIKE-1933a resolved)** — alert_engine_records always 0: evaluateAlert() dead code deleted (1933b). Architecture: market.db.alerts → Alert Commander = canonical intelligence path. Go alert-engine (/evaluate) reserved for future stop-loss use case. | MEDIUM | WONTFIX | — | — | — |
| post-1942-fa-verify | **MONITOR** — Verify FA next live cycle (~23:00 UTC tonight) reports ≥20/30 BCTC analyses (was 3/38 pre-1942). If still 3/38 → deploy-gap bug task to dev-mcp-server (Docker rebuild). Auto-close on observation. | MEDIUM | OBSERVE | ops | — | 2026-05-19 |
| post-1944-financial-reports-q1-2026 | **MONITOR** — 1944c smoke shows banking 7/7 source_url populated but financial_reports Q1-2026 = 0 rows. Reparse pipeline expected to populate within 1-3 enricher+sweep cycles (30-90 min from 07:52 UTC sign-off). AC: ≥3 banks (ACB/BID/CTG/EIB/MBB/VCB/VPB) have Q1-2026 row by 12:00 UTC 2026-05-18. If 0 rows after 3 cycles → spawn 1945d-reparse-pipeline-gap bug task to dev-mcp-server. | MEDIUM | OBSERVE | ops | — | 2026-05-18T12:00Z |

---

## In Progress

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
| _(empty)_ | — | — | — | — | — | — |

---

---

## Review

| Task ID | Title | Priority | Type | Owner | Handoff | Blocked by |
|---------|-------|----------|------|-------|---------|------------|
_(empty)_ | — | — | — | — | — | — |

---
## Done

| Task ID | Title | Priority | Type | Owner | Completed |
| 1945a | **DONE 2026-05-18 QA-APPROVED** — Fixed `verdictResolutionJob` baseline-price shape mismatch. `PriceHistoryEnvelope` type added to `clients.ts`; `getPriceHistory()` return type changed from `PriceSnapshot[]` to `PriceHistoryEnvelope`. `defaultFetchHistory()` updated to read `envelope.history[0].close` instead of `(snaps[0] as {price}).price`. Only caller is `verdictResolutionJob.ts` (audit confirmed `registry.ts` + `portfolioRiskCalculator.ts` do NOT call `getPriceHistory` from clients.ts). 6 new unit tests (`1945a-verdict-resolution-envelope.test.ts`) — envelope shape, empty history, null envelope, baseline extraction via `.close`. tsc 0 errors. 25/25 verdict resolution tests GREEN. Fix eliminates silent TypeError → null → `false_positive:unresolvable` path that was preventing ~520 alerts from being scored. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| ARCH-1945b | **DONE 2026-05-18** — Accuracy digest frontend card technical brief. Brownfield scan: `getSystemAccuracyDigestStats` already exists in `signalOutcomeStore.ts:380`; already imported in `server.ts:48`; no api-gateway change needed (`/mcp/*` catch-all confirmed). Exact insertion points: handler after `server.ts:1020`, SectionCard after `dashboard.analysis.tsx:1417`. New types: `AccuracyDigestStats` + `SignalTypeAccuracyDigest` in `domain/market.ts`. New helper: `fetchAccuracyDigest()` + `deriveAccuracyDigestState()` in `client.ts`. 6 UI states (null/empty/all-neutral/insufficient/partial/normal). 5 risk flags including R-4 (days param SQL injection guard — clamping in handler, before function call). Brief: `docs/architecture-briefs/2026-05-18-accuracy-digest-frontend-card.md`. | MEDIUM | ARCH | architect | 2026-05-18 |
| BA-1942d | **DONE 2026-05-18** — Accuracy digest frontend card spec written. Spec: `docs/REQ_1945b-accuracy-digest-frontend-card.md`. 11 sections: route (dashboard.analysis), component shape (top-3/bottom-3), gateway endpoint contract (GET /mcp/api/accuracy/digest), loading state, empty state, all-neutral state, insufficient-sample state, DDD layer mapping, edge cases, NFRs, 7 ACs. No PO blockers. | MEDIUM | BA-SPEC | ba | 2026-05-18 |
| SPIKE-1945 | **DONE 2026-05-18 ARCHITECT** — Root cause confirmed FIXABLE: `defaultFetchHistory()` in `verdictResolutionJob.ts` treats Go `/price/history` response as `PriceSnapshot[]` but Go returns `{code, history: DailyOHLCV[]}` envelope. `snaps[0]` = `undefined` → TypeError → caught → `null` → "price-fetch-failed:unresolvable" path fires for every verdict. Explains 100% of ~520 unscored rows and BUG-storm (19 msgs / 21h). Sprint 1336 SQLite isolation not implicated. `false_positive` label from 1926a is incorrect for these rows (not direction-evaluated). Child task 1945a scoped (FIX, dev-mcp-server, `scored_pct ≥ 60%`). Spike: `docs/spikes/SPIKE_1945-verdict-resolution-no-baseline.md`. | HIGH | SPIKE | architect | 2026-05-18 |
| 1944c | **DONE 2026-05-18** — End-to-end smoke verification: Docker container rebuilt and running latest code (commit 1b3d6c00). BCTC enricher active and processing queue (cycles at 05:45 + 06:15 UTC). All 7 banking Q1-2026 queue entries have source_url populated (100%). VPS proxy fix deployed: `{results:[{url,source,confidence}],error}` envelope confirmed. X-API-Key injection verified. Dead strategies removed (SSC/vietstock). Smoke report: `reports/TASK_REPORT_1944c.md`. AC PASS: all 5 criteria met (Docker rebuilt + enricher active + 7/7 banking cohort with source_url + all 1944 fixes verified). No escalation required. | MEDIUM | TASK | ops | 2026-05-18 |
| 1944b | **DONE 2026-05-18** — Removed dead BCTC discovery strategies: SSC iboard (NXDOMAIN since 2026-04-27) and vietstock.vn (HTTP 404 for every ticker). `_fetchSsc`, `_fetchVietstock` deprecated to no-op in `DiscoverOptions` (backward-compat). `_fetchCafef` was already deprecated (TASK_1916b). Strategy chain now: hsx(0) → VPS Playwright(1) → null. Production wiring in `bctcQueueEnricherJob.ts` cleaned up. AC-5: `LIVE-4` test added to `1944a-vps-live-probe.test.ts` (VPS_INTEGRATION guard). 142 BCTC tests GREEN (138 pass / 4 skip). tsc 0 errors. Cafef already removed in TASK_1916b (confirmed). Files: `bctcDiscovery.ts`, `bctcQueueEnricherJob.ts`, 11 test files updated. Zone: `apps/mcp-server/`. | MEDIUM | FIX | dev-mcp-server | 2026-05-18 |
| 1944a-vps | **DONE 2026-05-18** — Shape fix: `vps-proxy-server.js` `/proxy/bctc-discover/:ticker` now returns `{results:[{url,source,confidence}],error:null}` envelope instead of bare `string[]`. Deployed to Vinahost VPS via `scripts/deploy-vinahost.sh`. Verified: envelope shape confirmed via curl (see verification log). `vn-vps-proxy.service` running post-deploy. Fix: Sprint 1944a-vps. Zone: `vps-scripts/`. | HIGH | FIX | dev-vps-crawls | 2026-05-18 |
| 1944a-mcp | **DONE 2026-05-18** — VPS_INTEGRATION-guarded live probe test created (`1944a-vps-live-probe.test.ts`). 2 guard tests pass + 3 live tests skip cleanly (CI-safe). Verified: `VPS_PUSH_API_KEY` set in `.env` (line 13); added to `.env.example` (was missing). `BCTC_DISCOVER_URL` + `VPS_HOST` confirmed in `docker-compose.yml`. 1916a 6/6 GREEN. 56/56 cashflow+bctc regression GREEN. tsc 0 errors. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| ARCH-1944 | **DONE 2026-05-18** — Zone-split brief for 1944a. Brownfield scan revealed: (1) VPS route + header injection both already landed in repo; (2) response shape mismatch (`string[]` vs `{results,error}` envelope) identified as blocking risk R-1; (3) cafef removal already complete in TASK_1916b. Two child tasks defined: 1944a-vps (shape fix + VPS deploy) + 1944a-mcp (verify wiring + live probe test). Brief: `docs/architecture-briefs/2026-05-18-vps-bctc-discover-route-zone-split.md`. | HIGH | ARCH | architect | 2026-05-18 |
| ARCH-1942 | **DONE 2026-05-18** — Sprint 1942 blocking arch brief. Two-gap diagnosis: vnstock startup probe missing + cashFlowTool needs fallback read path. Brief: `docs/architecture-briefs/2026-05-18-watchlist-fundamentals-cadence.md`. | HIGH | ARCH | architect | 2026-05-18 |
| 1942a | **DONE 2026-05-18 QA-APPROVED** — Startup backfill probe. `vnstockStartupProbe.ts` + wired in `startScheduler.ts`. Guard: COUNT(DISTINCT code WHERE data_type='financials') < 10 OR last fetch > 7 days → fire runVnstockFundamentalsJob() after 90s delay. 6/6 unit tests GREEN. All 11 ACs PASS. Report: `reports/TASK_REPORT_1942a.md`. | HIGH | TASK | qa | 2026-05-18 |
| 1942b | **DONE 2026-05-18 QA-APPROVED** — cashFlowTool fallback read path (financial_reports → vnstock_cash_flow). COALESCE-style decision + data_source field + loading=true UX + unit conversion ×1000. backfillOCFForWatchlist() added. 10/10 new tests GREEN, 50/50 cashflow suite GREEN, 0 tsc errors. Coverage result: 31/33 tickers = 94% (sprint goal ≥20/30 ACHIEVED). | HIGH | TASK | dev-mcp-server | 2026-05-18 |
| 1942c | **DONE 2026-05-18 QA-APPROVED** — HPG OCF all-zeros fix. `vnstockBridge.ts`: CASH_FLOW_SCRIPT 3-key fallback + NULL policy + NI multi-key fallback. `cashFlowExtractor.ts`: P_OPERATING_CF_MFG + F_OPERATING_CF_MFG steel-sector labels. `vnstockTypes.ts`: operatingCashFlow: number\|null. 6/6 new tests GREEN (T1-T6). 50/50 cashflow regression GREEN. 0 tsc errors. | MEDIUM | TASK | dev-mcp-server | 2026-05-18 |
| kinh-dich-name-fix | **DONE 2026-05-18 QA-APPROVED** — Three root-cause fixes in kinh-dich-service: (1) All 64 QUE_META hexagram names in domain/services.ts corrected to full Vietnamese diacritics (was plain ASCII). (2) Fallback path in application/usecases.ts resolves name from QUE_META.find(q => q.id === stored.hexagram_number) instead of fresh placeholder-score reading. (3) SQLitePriceScoreRepository in infrastructure/repositories.ts corrected to query market_prices_history (was price_history), columns price + fetched_at. 30/30 tests GREEN (8 new), tsc 0 errors, DDD PASS, security PASS. Commit: abf5ef2d. | MEDIUM | FIX | dev-kinh-dich | 2026-05-18 |
| 1943a | **DONE 2026-05-18 QA-APPROVED** — resetQ1UrlNotFound() wired in initFinancialReportsTables (startup idempotent reset). Grace-period auto-retry arm added to bctcQueueEnricherJob COMBINED_SQL (url_not_found + last_attempt >7d + attempts <6). bctcBatchSweepJob diagnostic log added; root cause documented (wrapRun key correct, zero-run = container down 2026-04-25 09:00 UTC). 16/16 new tests GREEN (AC-1 thru AC-4). Full suite: 9219 pass, zero new failures. tsc 0 errors. DDD PASS. Security PASS. Report: reports/TASK_REPORT_1943a.md. | HIGH | FIX | dev-mcp-server | 2026-05-18 |
| SPIKE-1943 | **DONE 2026-05-18 ARCHITECT** — Root cause diagnosis: VPS PDF discovery failure, not stale calendar. Financial_reports zero Q1-2026 rows (max_year=2025). 31 tickers stuck at `url_not_found` in `bctc_vps_queue` (MAX_ENRICH_ATTEMPTS=5 exhausted), 7 `pending` (0 attempts). Calendar deadline logic correct (dynamic computation, no hardcoded seeds). `bctcBatchSweepJob` zero runs (likely `wrapRun` key mismatch). Recommended: TASK-1943a (FIX-HIGH, reset queue + diagnose batch sweep + add auto-retry policy). Output: `docs/spikes/SPIKE_1943-bctc-banking-q1-2026-deadline-delay.md`. | MEDIUM | SPIKE | architect | 2026-05-18 |
| 1941d-fpt-net-profit-ocr-fix | **DONE 2026-05-18 QA-APPROVED** — net_profit_api_bridge column + bridgeNetProfitToFinancialReports() + cashFlowTool COALESCE + ni_source. FPT Q4/2025 OCR=20,225 → bridge=2,509,520 triệu VND, ratio 1.6371 (passes guard). 7 new tests + 17 regression = 24 cashflow tests GREEN, tsc clean, DDD PASS, security PASS. Merged to main. Report: reports/TASK_REPORT_1941d.md. | MEDIUM | FIX | dev-mcp-server | 2026-05-18 |
| _(Older Done entries (1941c, 1941a, calendar-source-replacement, 1940a, 1939a/b, 1937a, 1938a, 1862c-E, 1936b, news-bugs-reuters-bloomberg-fix, 1936, macro-calendar-timeout-cap, 1934, 1933b, 1928a/1929a/1930a/1930c, 1862c-F, 1930b, 1932a, 1931a, 1922f, 1927a, 1926a, 1925a, 1924a/b/c/d, 1923a, 1909c) archived → `docs/TASKS_ARCHIVE.md`; pre-c141 history in git)_ | — | — | — | — | 2026-05-16..18 |

---

## Deferred

| Sprint | Title | Reason | Next Step |
|--------|-------|--------|-----------|
| 1887 | METHODOLOGY-FORENSICS: Virtual Capital / related-party graph detector | Needs own architect brief — graph-store choice, related-party data source, traversal patterns, false-positive control all unspecified | When 1885+1886 ship, queue separate ARCH-1887 brief before ba spec |
| 1892a-ops AC-3 | OPS-NOTE: 1892a-ops AC-3 now UNBLOCKED by 1892b merge (2026-05-12). VPS POST to `/api/push-news` should reach MCP server after deploy. | Unblocked 2026-05-12 | ops re-verify next cycle (observational) |
| TNB-c39-#3 | MONITOR: unified-agent FPT pillar gap (2nd cycle of evidence at c39) | Per TNB protocol need 3rd cycle to auto-cure. | If c40 unified-agent cycle repeats FPT-without-pillars pattern → spawn auto-cure CHORE. If c40 PASSES → close as transient. |
