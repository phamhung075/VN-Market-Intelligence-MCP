# REQ-1412: fix(diacritics-wave3) — Scheduler, Domain, Application Layer

status: READY_FOR_ARCHITECT
sprint: 146
po_vision: Fix all remaining unaccented Vietnamese output strings in scheduler, domain, and application layers so users never see broken diacritics in Telegram messages or MCP tool responses.

## User Story

As a Vietnamese stock investor using Claude,
I want all Telegram alerts and MCP tool responses to display proper Vietnamese diacritics,
So that I can read market signals and portfolio recommendations without deciphering garbled text.

## Scope Boundary

**IN scope:** `src/scheduler/`, `src/domain/`, `src/application/` layers only.
**OUT of scope:** `src/interface/mcp/tools/` — fully fixed in Sprint 145 (tasks 1410+1411). Do NOT re-scan or re-fix those files.

---

## String Inventory — Exact Per-File Catalogue

Rules applied:
- INCLUDE: user-facing output strings (Telegram messages, MCP content text, formatted reports)
- EXCLUDE: DB column keys, `.describe()` text, internal enum values (e.g. `NguHanhDynamic`), logger messages, code comments

---

### FILE 1: `src/scheduler/predictionMarketJob.ts`

Function: `buildTelegramMessage()` (lines 235–256) — output goes to Telegram MARKET channel.

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 238 | `"NGHIEM TRONG"` | `"NGHIÊM TRỌNG"` |
| 239 | `"QUAN TRONG"` | `"QUAN TRỌNG"` |
| 241 | `"LUU Y"` | `"LƯU Ý"` |
| 244 | `` `dich chuyen ${...}%` `` | `` `dịch chuyển ${...}%` `` |
| 249 | `` `Thi truong: ${...}` `` | `` `Thị trường: ${...}` `` |
| 251 | `` `Loai tin hieu: ${...}` `` | `` `Loại tín hiệu: ${...}` `` |
| 252 | `` `Xac suat YES: ${...}` `` | `` `Xác suất YES: ${...}` `` |
| 253 | `` `Khoi luong 24h: ${...}` `` | `` `Khối lượng 24h: ${...}` `` |
| 254 | `` `Do tin cay: ${...}` `` | `` `Độ tin cậy: ${...}` `` |
| 255 | `` `Phan tich: ${...}` `` | `` `Phân tích: ${...}` `` |

**Note:** `"[POLYMARKET]"` on line 249 — keep as-is (product name, not Vietnamese).

---

### FILE 2: `src/scheduler/calibrationReportJob.ts`

Function: `sendCalibrationDigest()` — MARKET channel output block (lines 342–370).

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 348 | `"Brier Score trung binh:"` | `"Brier Score trung bình:"` |
| 353 | `` `Xu huong: ${trend}` `` | `` `Xu hướng: ${trend}` `` |

**Note:** Lines 301–338 are WORK channel (internal dev digest). Keep as-is — developer channel content is English/mixed and not user-facing.
Lines 343–344 already accented (`"BÁO CÁO CALIBRATION TUẦN"`, `"Dự đoán đã giải quyết"`) — do not touch.
Lines 357–367 already accented — do not touch.

---

### FILE 3: `src/application/usecases/getCrisisEarlyWarning.ts`

Function: `formatCrisisWarningOutput()` (lines 121–165) — output becomes MCP content `text` field.

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 126 | `"=== RADAR KHUNG HOANG — Crisis Early Warning ==="` | `"=== RADAR KHỦNG HOẢNG — Crisis Early Warning ==="` |
| 127 | `` `Thoi gian: ${...}` `` | `` `Thời gian: ${...}` `` |
| 131 | `"--- CHI SO CANH BAO KHUNG HOANG ---"` | `"--- CHỈ SỐ CẢNH BÁO KHỦNG HOẢNG ---"` |
| 133 | `"Khong co tin hieu khung hoang nao duoc phat hien."` | `"Không có tín hiệu khủng hoảng nào được phát hiện."` |
| 137 | `` `[CANH BAO] ${...}` `` | `` `[CẢNH BÁO] ${...}` `` |
| 139 | `` `${...} nguon tin \| ${...}` `` | `` `${...} nguồn tin \| ${...}` `` |
| 147 | `"--- UY TIN DOANH NGHIEP (score < 50) ---"` | `"--- UY TÍN DOANH NGHIỆP (score < 50) ---"` |
| 149 | `"Tat ca co phieu co diem uy tin an toan."` | `"Tất cả cổ phiếu có điểm uy tín an toàn."` |
| 154 | `` `xu huong=${rep.trend}` `` | `` `xu hướng=${rep.trend}` `` |

---

### FILE 4: `src/domain/services/sentimentTrend.ts`

Function: `buildVietnameseSummary()` (lines 183–211) — `summary` field on `SentimentTrend` object, rendered in MCP tool responses.

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 195 | `` `${code} khong co du lieu cam tinh trong ${windowDays} ngay qua.` `` | `` `${code} không có dữ liệu cảm tính trong ${windowDays} ngày qua.` `` |
| 200 | `"co xu huong TICH CUC"` | `"có xu hướng TÍCH CỰC"` |
| 202 | `"co xu huong TIEU CUC"` | `"có xu hướng TIÊU CỰC"` |
| 203 | `"ON DINH"` | `"ỔN ĐỊNH"` |
| 208–209 | `` `${code} ${dirLabel} trong ${windowDays} ngay qua (...)` `` | `` `${code} ${dirLabel} trong ${windowDays} ngày qua (...)` `` |

**Note:** `dirLabel` variable is already fixed by the three label corrections above. The surrounding template only needs `"ngay qua"` → `"ngày qua"` (line 208).

---

### FILE 5: `src/domain/services/kinhDich/kinhDichFormatter.ts`

Output via `formatReading()` — rendered in Telegram and MCP tool text. Every string below is user-visible.

**HAO_STATE_LABELS (lines 11–14):**

| Key | Current (unaccented) | Correct (accented) |
|-----|---------------------|-------------------|
| `LAO_DUONG` | `"Lao Duong ⚡ (qua mua, sap dao chieu)"` | `"Lão Dương ⚡ (quá mua, sắp đảo chiều)"` |
| `THIEU_DUONG` | `"Thieu Duong (tang on dinh)"` | `"Thiếu Dương (tăng ổn định)"` |
| `THIEU_AM` | `"Thieu Am (giam on dinh)"` | `"Thiếu Âm (giảm ổn định)"` |
| `LAO_AM` | `"Lao Am ⚡ (qua ban, sap hoi phuc)"` | `"Lão Âm ⚡ (quá bán, sắp hồi phục)"` |

**HAO_POSITION_LABELS (lines 20–25):**

| Key | Current (unaccented) | Correct (accented) |
|-----|---------------------|-------------------|
| `1` | `"So hao   (Tam ly)"` | `"Sơ hào   (Tâm lý)"` |
| `2` | `"Nhi hao  (Co ban)"` | `"Nhị hào  (Cơ bản)"` |
| `3` | `"Tam hao  (Gia)   "` | `"Tam hào  (Giá)   "` |
| `4` | `"Tu hao   (Ngoai) "` | `"Tứ hào   (Ngoại) "` |
| `5` | `"Ngu hao  (Nganh) "` | `"Ngũ hào  (Ngành) "` |
| `6` | `"Thuong hao (Vi mo)"` | `"Thượng hào (Vĩ mô)"` |

**`formatReading()` body strings (lines 49–107):**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 49 | `` `Que chinh: ${...}` `` | `` `Quẻ chính: ${...}` `` |
| 50 | `` `  Xu huong: ${...}` `` | `` `  Xu hướng: ${...}` `` |
| 51 | `` `  Tin hieu: ${...} \| Do tin cay: ${...}` `` | `` `  Tín hiệu: ${...} \| Độ tin cậy: ${...}` `` |
| 55 | `"6 Hao:"` | `"6 Hào:"` |
| 64–70 | `` `Hao dong: ${posStr} — bien chuyen, su thay doi dang den (→ ${bienQue.name})` `` | `` `Hào động: ${posStr} — biến chuyển, sự thay đổi đang đến (→ ${bienQue.name})` `` |
| 70 | `"Hao dong: Khong co hao dong — que on dinh, xu huong hien tai ben vung"` | `"Hào động: Không có hào động — quẻ ổn định, xu hướng hiện tại bền vững"` |
| 76–79 | `` `Ho que (an sau): ${...}` `` | `` `Hộ quẻ (ẩn sâu): ${...}` `` |
| 82–84 | `` `Bien que (tuong lai): ${...}` `` | `` `Biến quẻ (tương lai): ${...}` `` |
| 91 | `` `Ngu Hanh: ${...} — ${nguHanh.dynamic}` `` | `` `Ngũ Hành: ${...} — ${nguHanh.dynamic}` `` |
| 94 | `` `  Diem ngu hanh: ${...}` `` | `` `  Điểm ngũ hành: ${...}` `` |
| 100–103 | `` `Markov: Xac suat chuyen sang ${...} (${...}): ${pct}%` `` | `` `Markov: Xác suất chuyển sang ${...} (${...}): ${pct}%` `` |

---

### FILE 6: `src/domain/services/kinhDich/kinhDichReading.ts`

Function: `computeReading()` — builds `tradingSignalDisplay`, `actionNote`, `overallReading` fields of `KinhDichReading`.

**KEEP as-is (lookup keys, not display):**
- `OUTCOME_SCORES_ASCII` keys: `"CAT"`, `"HUNG"`, `"VO CUU"`, `"HOI"`, `"LAN"`, `"LE"` — intentional ASCII for robust matching
- `TREND_SCORE_MAP` ASCII variants: `"THUAN LOI"`, `"BAT LOI"`, `"TRUNG TINH"` — matching guards
- `TIEN_KEYWORDS`, `LUI_KEYWORDS`, `GIU_KEYWORDS`, `CHO_KEYWORDS`, `THAN_KEYWORDS` — matching guards
- `extractAction()` return values `"MUA"`, `"BAN"`, `"GIU"`, `"CHO"`, `"THAN TRONG"` — canonical action codes used as keys downstream

**FIX (user-facing display strings):**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 257 | `` `${tradingSignal} (tich cuc)` `` | `` `${tradingSignal} (tích cực)` `` |
| 257 | `` `${tradingSignal} (tieu cuc)` `` | `` `${tradingSignal} (tiêu cực)` `` |
| 281 | `"tich cuc"` (scoreSign) | `"tích cực"` |
| 281 | `"tieu cuc"` (scoreSign) | `"tiêu cực"` |
| 283 | `` `KHUYEN NGHI: ${actionVerb} — Que ${...} (${...}) ${scoreSign}` `` | `` `KHUYẾN NGHỊ: ${actionVerb} — Quẻ ${...} (${...}) ${scoreSign}` `` |
| 284 | `` `, xu huong: ${...}` `` | `` `, xu hướng: ${...}` `` |
| 285 | `` `. Do tin cay: ${...}%.` `` | `` `. Độ tin cậy: ${...}%.` `` |
| 290 | `` `Hao dong: ${changingLines.join(", ")} — bien chuyen sang ${bienQue.name}.` `` | `` `Hào động: ${changingLines.join(", ")} — biến chuyển sang ${bienQue.name}.` `` |
| 291 | `` `Khong co hao dong — que on dinh.` `` | `` `Không có hào động — quẻ ổn định.` `` |
| 295 | `` `Xu huong: ${trendRaw}. ` `` | `` `Xu hướng: ${trendRaw}. ` `` |
| 296 | `` `Ho que (an sau): ${hoQue.name}` `` | `` `Hộ quẻ (ẩn sâu): ${hoQue.name}` `` |
| 298 | `` `Ngu Hanh: ${nguHanh.interpretation}. ` `` | `` `Ngũ Hành: ${nguHanh.interpretation}. ` `` |

---

### FILE 7: `src/domain/services/kinhDich/nguHanhClassifier.ts`

Function: `classifyNguHanh()` — `interpretation` field on `NguHanhResult`, rendered in Telegram and MCP text via `kinhDichFormatter.ts`.

**KEEP as-is (internal type identifiers):**
- `NguHanhDynamic` values: `"TUONG_SINH"`, `"TUONG_KHAC"`, `"SAME"`, `"NEUTRAL"` — enum-style keys, not displayed to user
- `NguHanh` type values: `"Kim"`, `"Moc"`, `"Thuy"`, `"Hoa"`, `"Tho"` — domain keys

**FIX (user-facing `interpretation` strings, lines 81–101):**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 81 | `"Cung hanh — tang cuong xu huong hien tai"` | `"Cùng hành — tăng cường xu hướng hiện tại"` |
| 85 | `"Noi luc sinh ngoai luc — xu huong ben vung"` | `"Nội lực sinh ngoại lực — xu hướng bền vững"` |
| 88 | `"Ngoai luc ho tro noi luc — moi truong thuan loi"` | `"Ngoại lực hỗ trợ nội lực — môi trường thuận lợi"` |
| 92 | `"Noi luc khac ngoai luc — mau thuan noi tai"` | `"Nội lực khắc ngoại lực — mâu thuẫn nội tại"` |
| 95 | `"Ngoai luc triet tieu noi luc — ap luc ben ngoai"` | `"Ngoại lực triệt tiêu nội lực — áp lực bên ngoài"` |
| 100 | `"Trung tinh — khong tuong tac dac biet"` | `"Trung tính — không tương tác đặc biệt"` |

---

### FILE 8: `src/domain/services/decisionNoteSynthesizer.ts`

Functions: `buildPositionLine()` and `buildActionNote()` — output is portfolio dashboard text in MCP tool responses.

**`buildPositionLine()` (lines 61–75):**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 63 | `"CHUA CO VI THE"` | `"CHƯA CÓ VỊ THẾ"` |
| 74 | `` `Vi the: ${sharesStr} co phieu @ ${avgStr} \| Gia hien tai: ${curStr} \| Lai/Lo: ${pnlStr}` `` | `` `Vị thế: ${sharesStr} cổ phiếu @ ${avgStr} \| Giá hiện tại: ${curStr} \| Lãi/Lỗ: ${pnlStr}` `` |

**`buildActionNote()` (lines 97–131):**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 102 | `"Khuyen nghi: CHUA CO VI THE"` | `"Khuyến nghị: CHƯA CÓ VỊ THẾ"` |
| 107 | `"Khuyen nghi: GIAM BOT"` | `"Khuyến nghị: GIẢM BỚT"` |
| 112 | `"Khuyen nghi: GIAM BOT"` | `"Khuyến nghị: GIẢM BỚT"` |
| 117 | `"Khuyen nghi: THEM VAO"` | `"Khuyến nghị: THÊM VÀO"` |
| 122 | `"Khuyen nghi: XEM XET GIAM"` | `"Khuyến nghị: XEM XÉT GIẢM"` |
| 130 | `"Khuyen nghi: GIU NGUYEN"` | `"Khuyến nghị: GIỮ NGUYÊN"` |

**Note:** JSDoc comment line 74 example string `"Vi the: 1,000 co phieu..."` — fix the comment to match the corrected output string.

---

## Functional Requirements

### FR-1: Fix unaccented strings in scheduler layer

- Description: Replace all unaccented Vietnamese in `predictionMarketJob.ts` and `calibrationReportJob.ts` Telegram message builders
- Input: existing function logic unchanged
- Output: Telegram messages with proper diacritics
- Business rule: only strings in message-building functions; do not alter logic, parameters, or log messages
- DDD layer: interface/scheduler

### FR-2: Fix unaccented strings in application layer

- Description: Replace all unaccented Vietnamese in `getCrisisEarlyWarning.ts` output formatter
- Input: `CrisisEarlyWarningResult` struct
- Output: MCP `McpContentItem[]` with properly accented Vietnamese text
- DDD layer: application

### FR-3: Fix unaccented strings in domain layer

- Description: Replace all unaccented display strings in `sentimentTrend.ts`, `kinhDichFormatter.ts`, `kinhDichReading.ts`, `nguHanhClassifier.ts`, `decisionNoteSynthesizer.ts`
- Input: domain objects (unchanged types)
- Output: summary/interpretation/actionNote/overallReading fields with proper diacritics
- Business rule: do NOT change lookup keys (ASCII variants in `kinhDichReading.ts` lines 67–83, action return values lines 124–129) — those are matching guards, not display text
- DDD layer: domain

### FR-4: TDD RED test first

- Description: `src/__tests__/1412-diacritics-wave3.test.ts` — assert accented output from representative samples of all 8 files
- Must be RED before fix task 1413 starts
- Minimum 8 test cases (one per file), recommended 20+
- DDD layer: test

## Non-Functional Requirements

- Performance: no change — pure string replacement
- Type safety: `bun tsc --noEmit` clean after fix
- Test baseline: full suite 5090+ pass, 0 fail after fix
- Language: all user-facing strings Vietnamese with correct diacritics; keep English technical terms (`Brier Score`, `Markov`, `Polymarket`) as-is

## Edge Cases & Data Quality

- [ ] `kinhDichReading.ts` ASCII lookup keys (lines 67–83) must NOT be diacriticised — they are input matching guards, not output
- [ ] `decisionNoteSynthesizer.ts` action keyword returns (`"GIU"`, `"MUA"`, `"BAN"`, `"CHO"`, `"THAN TRONG"`) used as downstream keys — keep as-is
- [ ] `kinhDichFormatter.ts` HAO_POSITION_LABELS trailing spaces must be preserved for column alignment

## Blockers

None. All questions resolved by code inspection.

## Acceptance Criteria

### AC-1: TDD RED test

**Given** `src/__tests__/1412-diacritics-wave3.test.ts` written, fix task 1413 not yet applied
**When** `bun test src/__tests__/1412-diacritics-wave3.test.ts` runs
**Then**
- At least 8 cases FAIL with string assertion mismatches (RED)
- `bun tsc --noEmit` clean

### AC-2: All 8 files fixed

**Given** task 1413 applied all string replacements per inventory above
**When** `bun test src/__tests__/1412-diacritics-wave3.test.ts` runs
**Then**
- All cases GREEN
- `bun tsc --noEmit` 0 errors
- `bun test` full suite 5090+ pass, 0 fail

### AC-3: No regression on Sprint 144+145

**Given** Sprint 144 (`src/__tests__/1408-tool-diacritics.test.ts`) and Sprint 145 (`src/__tests__/1410-tool-diacritics-sweep.test.ts`) test files exist
**When** full `bun test` runs
**Then**
- All Sprint 144 and 145 cases remain GREEN
- No new failures in `src/interface/mcp/tools/` files

### AC-4: Logic integrity

**Given** `kinhDichReading.ts` ASCII lookup guards preserved
**When** `computeReading()` called with hexagram data containing ASCII trend keywords (`"THUAN LOI"`, `"BAT LOI"`, `"TRUNG TINH"`)
**Then**
- `extractTrendScore()` returns the correct numeric score
- `tradingSignal` computation is unaffected

## DDD Layer Map

| Requirement | Layer | File Path |
|-------------|-------|-----------|
| FR-1a | interface/scheduler | `src/scheduler/predictionMarketJob.ts` |
| FR-1b | interface/scheduler | `src/scheduler/calibrationReportJob.ts` |
| FR-2 | application | `src/application/usecases/getCrisisEarlyWarning.ts` |
| FR-3a | domain | `src/domain/services/sentimentTrend.ts` |
| FR-3b | domain | `src/domain/services/kinhDich/kinhDichFormatter.ts` |
| FR-3c | domain | `src/domain/services/kinhDich/kinhDichReading.ts` |
| FR-3d | domain | `src/domain/services/kinhDich/nguHanhClassifier.ts` |
| FR-3e | domain | `src/domain/services/decisionNoteSynthesizer.ts` |
| FR-4 | test | `src/__tests__/1412-diacritics-wave3.test.ts` |

_Paths are exact — verified by BA file read on 2026-04-18._
