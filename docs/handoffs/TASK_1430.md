# TASK_1430 — TDD RED: shouldRunCatchup + probe integration assertions

sprint: 155
branch: task/1430-startup-catchup-red
layer: interface/scheduler (test only)
depends_on: []
tech: docs/TECH_1430.md

## Goal

Write a failing test suite for `shouldRunCatchup` and the startup probe integration.
All assertions must be RED before task 1431 begins (no implementation exists yet).

## Test File

`src/__tests__/1430-startup-catchup.test.ts`

## DB Setup Pattern

Use in-memory SQLite with `cron_job_runs` DDL only — same pattern as task 1420:

```typescript
import { describe, it, expect, beforeEach } from "bun:test";
import { Database } from "bun:sqlite";
import { shouldRunCatchup } from "../scheduler/jobs.js";

function createCronTable(db: Database): void {
  db.exec(`
    CREATE TABLE IF NOT EXISTS cron_job_runs (
      id           INTEGER PRIMARY KEY AUTOINCREMENT,
      job_name     TEXT NOT NULL,
      started_at   TEXT NOT NULL,
      finished_at  TEXT,
      status       TEXT NOT NULL CHECK(status IN ('running','success','error')),
      rows_written INTEGER,
      error_msg    TEXT,
      duration_ms  INTEGER
    )
  `);
}

function insertRunToday(db: Database, jobName: string): void {
  db.prepare(
    `INSERT INTO cron_job_runs (job_name, started_at, status)
     VALUES (?, datetime('now'), 'success')`
  ).run(jobName);
}

let db: Database;
beforeEach(() => {
  db = new Database(":memory:");
  createCronTable(db);
});
```

## Assertions Required

### AC-1: morning window passed, no row → true

```typescript
it("AC-1: morning window passed, no row — returns true", () => {
  const now = new Date("2026-04-18T03:00:00Z"); // 03:00 UTC > 01:00 threshold
  expect(shouldRunCatchup(db, "morningBriefingJob", 1, 0, now)).toBe(true);
});
```

### AC-2: morning window passed, row exists today → false

```typescript
it("AC-2: morning window passed, row exists — returns false", () => {
  insertRunToday(db, "morningBriefingJob");
  const now = new Date("2026-04-18T03:00:00Z");
  expect(shouldRunCatchup(db, "morningBriefingJob", 1, 0, now)).toBe(false);
});
```

### AC-3: morning window not reached (00:30 UTC) → false regardless of DB

```typescript
it("AC-3: morning window not yet reached — returns false", () => {
  const now = new Date("2026-04-18T00:30:00Z"); // 00:30 UTC < 01:00
  expect(shouldRunCatchup(db, "morningBriefingJob", 1, 0, now)).toBe(false);
});
```

### AC-4: evening window passed, no row → true

```typescript
it("AC-4: evening window passed, no row — returns true", () => {
  const now = new Date("2026-04-18T16:00:00Z"); // 16:00 UTC > 15:30 threshold
  expect(shouldRunCatchup(db, "eveningSummaryJob", 15, 30, now)).toBe(true);
});
```

### AC-5: evening window not reached (15:29 UTC) → false

```typescript
it("AC-5: evening window not yet reached (15:29) — returns false", () => {
  const now = new Date("2026-04-18T15:29:00Z");
  expect(shouldRunCatchup(db, "eveningSummaryJob", 15, 30, now)).toBe(false);
});
```

### AC-6: evening window exactly at 15:30 UTC → true

```typescript
it("AC-6: evening window exactly 15:30 — returns true", () => {
  const now = new Date("2026-04-18T15:30:00Z");
  expect(shouldRunCatchup(db, "eveningSummaryJob", 15, 30, now)).toBe(true);
});
```

### AC-7: DB error → false (fail-safe)

```typescript
it("AC-7: DB error — returns false without throwing", () => {
  // Drop table to force query error
  db.exec("DROP TABLE cron_job_runs");
  const now = new Date("2026-04-18T03:00:00Z");
  expect(() =>
    shouldRunCatchup(db, "morningBriefingJob", 1, 0, now)
  ).not.toThrow();
  expect(shouldRunCatchup(db, "morningBriefingJob", 1, 0, now)).toBe(false);
});
```

### AC-8: row from a different job does not satisfy dedup

```typescript
it("AC-8: row for different job — does not block morningBriefingJob", () => {
  insertRunToday(db, "eveningSummaryJob");
  const now = new Date("2026-04-18T03:00:00Z");
  expect(shouldRunCatchup(db, "morningBriefingJob", 1, 0, now)).toBe(true);
});
```

## RED Acceptance

```bash
bun test src/__tests__/1430-startup-catchup.test.ts
# Expected: 8 FAIL (shouldRunCatchup not yet exported from jobs.ts)
# If GREEN: stop — implementation already exists, reassign to 1431
```

---

## [Architect] Brownfield Findings

interfaces_found:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/infrastructure/db/cronJobRunStore.ts`   # REUSE — `recordJobRun` wraps probe calls; `insertCronJobRunStart` uses `datetime('now')` confirming `date('now')` dedup is valid
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/scheduler/jobs.ts`   # REUSE — existing `log()`, `getDb()`, `runMorningBriefing`, `runEveningSummary`, `recordJobRun` imports all present
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/infrastructure/db/schema.ts`   # REUSE — `cron_job_runs` DDL confirmed: `started_at TEXT NOT NULL` written as `datetime('now')` (UTC)

interfaces_to_create:
- export `shouldRunCatchup` from `src/scheduler/jobs.ts`   # NEW — pure query fn, injectable nowUtc

decisions:
- "Probe wraps calls with `recordJobRun(db, jobName, ...)` — not bare `runMorningBriefing()` — so dedup row is written for second-restart protection."
- "`date('now')` dedup valid: SQLite stores `started_at` as ISO `datetime('now')` (UTC); `date('now')` returns UTC `YYYY-MM-DD`; TEXT lexicographic `>=` comparison is correct."
- "No new file — all changes in jobs.ts matching BA DDD layer specification."
- "bctcReparseJob setTimeout block (jobs.ts:323-332) is the exact pattern to replicate: 30s delay, try/catch, fire-and-forget."

brownfield_scan_clean: true

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1430-startup-catchup.test.ts   # created: 8 test assertions covering all ACs

tests_written:
- src/__tests__/1430-startup-catchup.test.ts   # 8 assertions, RED confirmed (SyntaxError: export not found)

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # 5429 pass, 0 fail (Bun OOM panic after run is known Bun 1.3.11 issue, not test failure)
