# Handoff: Task 1354 — Scheduler Gap Tests Phase 3

**Sprint:** 1354
**Created:** 2026-04-28
**Status:** Ready for BA

---

## Context

Scheduler test coverage gap-fill program:
- Phase 1 (Sprint 1351): `vpsProxyWatchdogJob` + `weatherCheckJob` — 16 tests
- Phase 2 (Sprint 1353): `imfIndicatorPollerJob` + `priceUpdateWatchdogJob` — 16 tests
- Phase 3 (Sprint 1354): `parallelServiceDispatcherJob` + `freshnessSlaMonitorJob` helpers — 16 tests

**Baseline entering sprint:** 7673 pass / 0 fail

---

## Task 1354a — parallelServiceDispatcherJob gap tests

**File:** `apps/mcp-server/src/scheduler/system/parallelServiceDispatcherJob.ts`

**Current coverage:** ZERO dedicated tests. No 135x-series test file exists.

**Why critical:** Runs every 15 minutes. Orchestrates all 4 microservices (TA/Macro/Gateway/KinhDich) via `Promise.allSettled`. Any silent regression here degrades the entire intelligence cycle without triggering CI failures.

**Export to test:** `runParallelServiceDispatcher()` — currently no DI overload. Developer must add DI overload (similar to ImfPollerOptions pattern from 1353a) to allow injectable `computeTAIndicators`, `getMacroSnapshot`, `getGatewayHealth`, `getMarketHexagram`, `sendTelegramWork`, `getDb` dependencies.

**8 gap test cases:**
1. PSD-1: all services OK → `allOk=true`, no Telegram alert, correct duration shape
2. PSD-2: one service fails (TA throws) → `allOk=false`, failed service recorded, others still ok
3. PSD-3: all services fail → `allOk=false`, Telegram alert fired with all failed names
4. PSD-4: macro service fails → macro `status='failed'`, correct error message captured
5. PSD-5: weekday morning UTC 01:xx → Telegram heartbeat sent when allOk=true
6. PSD-6: weekend → no Telegram heartbeat even if allOk=true
7. PSD-7: empty watchlist (0 tickers) → TA loop runs with empty array, no throw
8. PSD-8: getDb() throws → function absorbs, still returns DispatcherResult shape or rethrows cleanly

**DI overload interface:**
```typescript
export interface DispatcherDeps {
  computeTAFn?: typeof computeTAIndicators;
  getMacroFn?: typeof getMacroSnapshot;
  getGatewayFn?: typeof getGatewayHealth;
  getKinhDichFn?: typeof getMarketHexagram;
  sendTelegramFn?: typeof sendTelegramWork;
  getDbFn?: () => Database;
}
```

---

## Task 1354b — freshnessSlaMonitorJob helper unit tests

**File:** `apps/mcp-server/src/scheduler/system/freshnessSlaMonitorJob.ts`

**Current coverage:** 1352c covers `runFreshnessSlaMonitor` orchestration + `querySignalAges`. Gap: the 5 DB-write helper functions have NO unit tests.

**Why critical:** These functions write to the `sla_breach_audit` table — the audit trail for SLA breaches and recovery. Bugs here silently corrupt the breach history, causing missed alerts or phantom escalations.

**Functions to test (all exported):**
- `getPriorBreaches(db)` — queries `sla_breach_audit` for open breaches
- `isEscalationCooldownActive(db, signalType)` — cooldown check (60-min window)
- `recordSlaBreach(db, signalType, ageMinutes, thresholdMinutes, severity)` — INSERT into audit table
- `recordSlaRecovery(db, signalType)` — UPDATE status to 'recovered'
- `markEscalationSent(db, signalType)` — UPDATE escalation_callback_sent=1

**8 gap test cases:**
1. SLA-1: `getPriorBreaches` returns empty array when no open breaches exist
2. SLA-2: `getPriorBreaches` returns open breaches, ignores 'recovered' rows
3. SLA-3: `isEscalationCooldownActive` returns false when no recent escalation
4. SLA-4: `isEscalationCooldownActive` returns true within 60-min window after `markEscalationSent`
5. SLA-5: `recordSlaBreach` inserts correct row (signalType, ageMinutes, thresholdMinutes, severity, status='breach_open')
6. SLA-6: `recordSlaRecovery` updates status to 'recovered' + sets recovered_at
7. SLA-7: `markEscalationSent` sets escalation_callback_sent=1 on most recent open breach
8. SLA-8: `recordSlaRecovery` is idempotent — second call on already-recovered row does not error

**No DI change needed** — all functions accept `db: Database` already.

---

## Acceptance Criteria

- [ ] 1354a: New file `src/__tests__/1354a-parallel-service-dispatcher-gaps.test.ts` — 8 tests pass
- [ ] 1354a: `runParallelServiceDispatcher` accepts optional `DispatcherDeps` DI overload
- [ ] 1354b: New file `src/__tests__/1354b-freshness-sla-monitor-helpers.test.ts` — 8 tests pass
- [ ] Full suite: ≥7689 pass (7673 + 16 new), 0 fail
- [ ] No changes to existing test files
- [ ] TypeScript strict: no `any`, no suppressed errors

---

## Sprint Close Criteria

- Both test files merged to main
- `project-stats.json` updated: `testBaseline: 7689`, `currentSprint: 1354`
- `SPRINT_GOAL.md` updated to Sprint 1354
- `TASKS.md` updated: both tasks moved to Done
