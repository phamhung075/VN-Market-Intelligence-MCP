---
sprint: CROSS-SESSION-MULTI-TEAM-ORCH
branch: task/1991-router-roster-read
size: S
zone: .claude/skills/ + CLAUDE.md
depends_on: [TASK_1990]
blocks: [TASK_1992]
---

## TLDR

Wire roster READ into router Step 2.5 (CLAUDE.md + dispatch-claim SKILL): before PRE-CLAIM, read `task_list_held(kind="session-presence")` so the router sees who's working cross-team. Enables next step (TASK_1992) to verify presence registration actually works.

## [PM] Planning Context

- **Zone:** .claude/skills/ + CLAUDE.md (main terminal zone)
- **Acceptance Criteria:**
  - [ ] CLAUDE.md Step 2.5 new → documented: read presence roster via `task_list_held(kind="session-presence")` before PRE-CLAIM
  - [ ] dispatch-claim SKILL: Step 1 new → call roster read, log who's online (agent_id + host + current_task)
  - [ ] Roster read result structure documented: [{ task_id, owner_agent, owner_client_session, payload:{agent_id,host,started_at,current_task}, ... }]
  - [ ] Router example output captured: "session-presence roster: [af/host-1/TASK_1990, dev-frontend/host-2/TASK_500, ...]"
  - [ ] Cross-session collision detection verified: duplicate agent_id across sessions → log warning (non-blocking; allows parallel cowork/dev-team)

- **Files to read first:**
  - `CLAUDE.md` — main terminal dispatch section
  - `.claude/skills/dispatch-claim/SKILL.md` (from TASK_1990)
  - `apps/mcp-server/src/interface/mcp/tools/coordinationTools.ts:226-233` (tool filters)
  - `docs/data/system-map.json` (for agent roster context)

- **Files to create:**
  - (None — edits only)

- **Files to modify:**
  - `CLAUDE.md` — add Step 2.5 roster read
  - `.claude/skills/dispatch-claim/SKILL.md` — Step 1 router integration (call via dispatch-claim)

- **Dependencies:** TASK_1990 (dispatch-claim SKILL must exist first)

- **Knowledge needed:**
  - `docs/references/agent-roster.md`
  - `docs/policies/commit-convention.md`
  - Coordinator tool schema: coordinationTools.ts listHeldTasks

- **Technical notes:**
  - No new MCP tool needed — listHeldTasks already supports kind + owner_agent filters
  - Roster read is READ-ONLY, non-blocking
  - Expected: 2-4 presence rows active (cowork-team + dev-team + auditor + 0-2 child agents)
  - Router collects this data for TASK_1993 design context (multi-team comms breadth)

- **Estimated effort:** ~1.5h (read integration + test harness)
