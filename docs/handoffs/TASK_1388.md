# Handoff: TASK_1388 — fix: omit filler sections in formatBriefingMessage when data absent

sprint: 137
status: Todo
branch: task/1388-morning-briefing-filler-fix
depends_on: [1387 merged]
layer: scheduler

## Context

After 1387 TDD RED tests are merged, apply the minimal fix to `formatBriefingMessage` in `src/scheduler/morningBriefingJob.ts`.

## Target state (3 changes)

### Change 1 — VN-Index section (lines ~39-43)

```ts
// BEFORE
if (briefing.vnIndex) {
  ...
} else {
  lines.push("📈 VN-Index: chưa có dữ liệu");
}

// AFTER
if (briefing.vnIndex) {
  ...
}
// omit section entirely when null
```

### Change 2 — Watchlist section (lines ~45-56)

```ts
// BEFORE
if (briefing.watchlistSummary.length > 0) {
  for (const w of briefing.watchlistSummary) {
    const price = w.price ? `${w.price.toLocaleString("en-US")}` : "N/A";
    ...
    lines.push(`  ${w.code}: ${price}${chg}`);
  }
} else {
  lines.push("  Chưa có dữ liệu giá");
}

// AFTER
if (briefing.watchlistSummary.length > 0) {
  const entries = briefing.watchlistSummary.filter((w) => w.price != null);
  if (entries.length > 0) {
    lines.push("");
    lines.push("📊 Giá cổ phiếu:");
    for (const w of entries) {
      const price = w.price!.toLocaleString("en-US");
      const chg = w.changePct != null ? ` (${w.changePct >= 0 ? "+" : ""}${w.changePct.toFixed(2)}%)` : "";
      lines.push(`  ${w.code}: ${price}${chg}`);
    }
  }
}
// omit header+entries entirely when empty or all null prices
```

Note: Move the `lines.push(""); lines.push("📊 Giá cổ phiếu:");` header inside the guard so it only emits when there are entries.

## Acceptance criteria

| Criterion | Target |
|-----------|--------|
| T1–T4 all GREEN | PASS (4/4) |
| Full suite | 5021+ pass, 0 fail, 21+ skip |
| tsc --noEmit | 0 errors |
| grep "chưa có dữ liệu\|Chưa có dữ liệu giá" morningBriefingJob.ts | 0 matches |

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/scheduler/morningBriefingJob.ts   # Change 1: removed else branch for vnIndex (omit section when null); Change 2: filter null-price entries, moved header inside guard; Change 3: removed "Chưa có dữ liệu giá" fallback

tests_written:
- src/__tests__/1387-morning-briefing-filler.test.ts   # 4 tests (T1-T4), 21 assertions, all GREEN (merged from 1387 branch)

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # 5026 pass, 0 fail, 21 skip
