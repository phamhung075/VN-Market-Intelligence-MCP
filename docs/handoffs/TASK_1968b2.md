# TASK 1968b2 — L-6 Cron Stagger + L-7 Notebook Commit Batching (agent-father pure)

**Sprint:** 1968 | **Phase:** 2b | **Owner:** agent-father | **Released:** 2026-05-21T19:35:13Z po c237
**Brief:** `docs/architecture-briefs/2026-05-21-token-toolcall-economy.md` §L-6 + §L-7
**Time-box:** 2h

## Goal
Stagger cowork agent cron schedules to prevent concurrent `get_cycle_bootstrap` collision, AND batch notebook commits to end-of-market-session to reduce git lock contention.

## L-6 Subset (agent-father-pure portion only)
Original brief L-6 proposes a `docs/data/cycle-snapshot-<TICK>.json` shared file written by cowork-team dispatcher. That full implementation requires cowork-team.md edit + `.gitignore` update — deferred to a future dev task.

**1968b2 L-6 scope (agent-father-only):**
1. Edit cron schedules in:
   - `.claude/agents/news-scout.md` `schedule.cron`
   - `.claude/agents/market-watcher.md` `schedule.cron`
   - `.claude/agents/alert-commander.md` `schedule.cron`
2. Stagger so no two fire at the same minute. Suggested:
   - news-scout: `0,15,30,45 2-8 * * 1-5`
   - market-watcher: `5,20,35,50 2-8 * * 1-5`
   - alert-commander: `10,25,40,55 2-8 * * 1-5`
3. Edit `.claude/skills/cycle-bootstrap/SKILL.md`:
   - Add **Step -1**: `If docs/data/cycle-snapshot-<HH:MM>.json exists and is <7min old, READ it and skip get_cycle_bootstrap. Else fall through to current Step 0.` (Snapshot file is optional — fallback to direct MCP call is the always-safe path.)
4. No behavior regression: snapshot file MAY or MAY NOT be present. Fallback is the canonical path.

## L-7 Notebook Commit Batching
1. Edit `.claude/flows/market-watcher/cycle.md` Step 5:
   - Keep notebook WRITE.
   - Remove `git commit` step (leave only `git add` if needed for staging; commit deferred to eod.md).
2. Edit `.claude/flows/market-watcher/eod.md`:
   - Add commit covering BOTH `docs/agent-memory/notebooks/market-watcher.md` AND `docs/agent-memory/notebooks/news-scout.md`.
   - Commit message format: `chore(memory/market-session-eod): notebook YYYY-MM-DD cycles N`.
3. Edit `.claude/flows/news-scout/stage-log-notify.md`:
   - Remove per-cycle git commit step.
   - Notebook write remains per-cycle.
4. Add pointer to `docs/protocols/head-lock-self-cure.md` in eod.md in case commit fails mid-session.
5. Off-hours cycles (cowork cron every 4h outside market hours) retain their own per-cycle commit (acceptable since rare).

## Acceptance Criteria
- AC-1: 3 agent .md files have non-overlapping cron schedule minutes.
- AC-2: cycle-bootstrap SKILL has Step -1 with snapshot-lookup + explicit fallback.
- AC-3: no regression in cowork cycle behavior (snapshot optional).
- AC-4: market-watcher/cycle.md Step 5 = write only, no commit.
- AC-5: market-watcher/eod.md commits both market-watcher.md + news-scout.md notebooks.
- AC-6: news-scout/stage-log-notify.md no longer commits per-cycle.
- AC-7: head-lock-self-cure runbook pointer present in eod.md.
- AC-8: caveman ULTRA done signal `docs/signals/agent-father-1968b2-done.json` emitted.

## Out of Scope
- L-4 get_agent_signals consolidation (1968b1).
- L-8 composite skill, L-9 server-side filter (Phase 3).
- cowork-team.md edit / snapshot writer (future dev task).
- BCTC paths (freeze).

## Constraints
- WIP: 1968b1 + 1968b2 = 2 parallel; no further dev tasks.
- BCTC freeze active.
- No branches.
- NFR-5: zero overlap with 1967b. If 1967b architect brief lands and proposes alternative cron, agent-father merges before commit.

## Coordination
- 1968b1 (L-4) runs in parallel; both lanes are agent-father-touch-pure after their phase1 unblock.
- **1967b canonical brief LANDED 2026-05-21T19:29Z** (`docs/architecture-briefs/2026-05-21-orchestration-bug-conflict-audit.md`, 22 findings). PO audit:
  - **ITEM-12** (alert-commander mcp-tools.md trigger:startup) — DEFER-TO-1968a per NFR-5. **Closed** by 1968a L-1 (mcp-tools.md promoted to always_load with inline justification). No 1968b action required.
  - **ITEM-05** (market-watcher/cycle.md Step 5 APPEND/OVERWRITE drift) — owner agent-father, surface 2e. **COLLISION WITH L-7**: both edits touch `.claude/flows/market-watcher/cycle.md` Step 5. **REQUIRED**: agent-father MUST merge both fixes in a single touch — (a) remove APPEND instruction, change to OVERWRITE per `.claude/skills/notebook-write/SKILL.md` (ITEM-05 fix), AND (b) remove per-cycle git commit (L-7 fix). Single commit, single touch.
  - **ITEM-10** (cowork fire-drift drift_min=5) — owner dev-mcp-server, surface 5d. Different surface than L-6 (which staggers agent cron). No collision.
  - **ITEM-11** (API_MIN_INTERVAL dead slots in cowork-schedule.json) — owner agent-father, surface 5b. Different file than L-6 (which edits `.claude/agents/*.md` schedule.cron). No collision. PM will likely sprint this in 1967c slate.
  - All other 1967b items: no L-6/L-7 surface overlap.
- ITEM-05+L-7 merge cite: include "Fixes ITEM-05 + L-7 in single touch (1967b/1968b2 collision merge)" in commit message.
