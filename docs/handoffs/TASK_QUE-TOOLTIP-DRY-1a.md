---
sprint: QUE-TOOLTIP-DRY
branch: task/QUE-TOOLTIP-DRY-1a-codegen-pipeline
size: M
zone: scripts/ + apps/frontend/
depends_on: []
blocks: ["QUE-TOOLTIP-DRY-1b"]
---

## TLDR
Rewrite gen-que-descriptions.ts to read que-reference.js directly (bypassing hexagramLibrary.ts), regenerate the TypeScript interface to 2 fields (coreMeaning, marketTrendLabel), and update QueName.tsx tooltip to use the new fields. Fixes SSOT misalignment: generator now reads kinh-dich-service source, not mcp-server copy.

## [PM] Planning Context
- **Zone:** scripts/ (codegen) + apps/frontend/ (interface + generated file)
- **Acceptance Criteria:**
  - [ ] gen-que-descriptions.ts reads apps/kinh-dich-service/dashboard/que-reference.js directly (no HTTP call, parse JSON via string strip)
  - [ ] bun run gen:que produces apps/frontend/app/lib/que-descriptions.generated.ts with header comment citing que-reference.js
  - [ ] Generated file has exactly 64 entries (smoke test via Object.keys())
  - [ ] QueDescription interface reduced to 2 fields: coreMeaning (string), marketTrendLabel (string)
  - [ ] QueName.tsx imports updated QueDescription interface (no breaking signature changes)
  - [ ] QueName.tsx tooltip secondary field changed from {desc.state_trend} to {desc.marketTrendLabel}
  - [ ] Remove italic class from secondary tooltip line (marketTrendLabel is a label, not prose)
  - [ ] Grep verify: `grep "Source:.*que-reference" apps/frontend/app/lib/que-descriptions.generated.ts` returns 1 match
  - [ ] Text content matches que-reference.js .vi output (spot-check quẻ 1: coreMeaning and marketTrendLabel against que-reference.js)

- **Files to read first:** 
  - scripts/gen-que-descriptions.ts (current implementation)
  - apps/frontend/app/lib/que-descriptions.generated.ts (output format)
  - apps/frontend/app/components/QueName.tsx (consumer interface)
  - apps/kinh-dich-service/dashboard/que-reference.js (SSOT source)

- **Files to modify:** 
  - scripts/gen-que-descriptions.ts — rewrite to readFileSync + JSON.parse que-reference.js; emit 2-field interface
  - apps/frontend/app/lib/que-descriptions.generated.ts — regenerated by script (do not hand-edit)
  - apps/frontend/app/components/QueName.tsx — update QueDescription interface + tooltip secondary field + remove italic

- **Files to create:** 
  - None (all modifications)

- **Dependencies:** 
  - None (does not depend on other QUE-TOOLTIP tasks, but BLOCKS Subtask 1b)

- **Knowledge needed:** 
  - docs/architecture-briefs/2026-06-12-que-tooltip-dry.md (design context + BLOCKER-1 Option B decision)
  - docs/handoffs/QUE-TOOLTIP-DRY-BA-spec.md (PO-Q3 ruling on tooltip fields)

---

## Implementation Notes

### gen-que-descriptions.ts Rewrite

Replace the current hexagramLibrary import with a direct file read:

```typescript
// BEFORE
import { QUE_DATA, QUE_META } from '../apps/mcp-server/src/domain/services/kinhDich/hexagramLibrary'

// AFTER
import { readFileSync } from 'fs'
import { resolve } from 'path'

const queRefPath = resolve(__dirname, '../apps/kinh-dich-service/dashboard/que-reference.js')
const queRefContent = readFileSync(queRefPath, 'utf-8')
const stripped = queRefContent.replace('window.__QUE_REFERENCE__ = ', '').replace(/;\s*$/, '')
const queReference = JSON.parse(stripped)
```

Field mapping:
- `queReference[i].coreMeaning.vi` → `QueDescription.coreMeaning`
- `queReference[i].marketTrendLabel.vi` → `QueDescription.marketTrendLabel`

Drop these fields (not used in tooltip):
- `judgment_interpretation` (was from hexagramLibrary.ts `judgment.interpretation`)
- `image_action` (was from hexagramLibrary.ts `image.action`)
- `state_trend` (was raw ASCII prefix; replaced by marketTrendLabel)

### Smoke Test

After script rewrite, run:
```bash
bun run gen:que
node -e "const d = require('./apps/frontend/app/lib/que-descriptions.generated.ts'); console.log('entries:', Object.keys(d.QUE_DESCRIPTIONS).length)"
# Should output: entries: 64
```

### QueName.tsx Changes

Update the QueDescription interface in the component file:
```typescript
export interface QueDescription {
  coreMeaning: string
  marketTrendLabel: string
}
```

Update tooltip secondary field:
```tsx
// BEFORE
<span className="text-xs italic text-gray-500">{desc.state_trend}</span>

// AFTER
<span className="text-xs text-gray-500">{desc.marketTrendLabel}</span>
```

Note: `className` removes `italic`.

---

---

## [Developer] Implementation Record

- **Service:** frontend + scripts
- **Zone:** scripts/ + apps/frontend/
- **Build tier:** 4 (component interface + codegen pipeline)
- **Files modified:**
  - `scripts/gen-que-descriptions.ts` — rewritten to readFileSync que-reference.js, strip JS wrapper, JSON.parse, emit 2-field interface
  - `apps/frontend/app/lib/que-descriptions.generated.ts` — regenerated (64 entries, 2 fields: coreMeaning + marketTrendLabel, header cites que-reference.js)
  - `apps/frontend/app/components/QueName.tsx` — `desc.state_trend` → `desc.marketTrendLabel`, removed `italic` class from secondary tooltip line
  - `docs/data/orch/orch-state.json` — task status TODO → IN_PROGRESS → REVIEW
- **Tests written:**
  - `apps/frontend/app/__tests__/QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.ts` — 14 assertions GREEN (64 entries count, 2-field interface, field rename verification, quẻ 1 spot-check, NFR-3 graceful no-op)
- **Git commits:** `8f1fcdb0 feat(frontend/QUE-TOOLTIP-DRY-1a): codegen pipeline rewrite + QueDescription 2-field interface`
- **Type check:** clean (tsc --noEmit exit 0)
- **Service tests:** 14 pass / 0 fail (new tests); 21 pre-existing TopNav count failures unrelated to this task
- **Playwright (G12):** 4/4 GREEN
- **Grep AC:** `grep "Source:.*que-reference" apps/frontend/app/lib/que-descriptions.generated.ts` → 1 match
- **Quẻ 1 spot-check:** coreMeaning = "Sức sáng tạo nguyên thủy, năng lượng dương cương kiện không ngừng vận hành" (1 clause, matches que-reference.js); marketTrendLabel = "Thuận lợi (THUẬN LỢI)"
- **RF-2 verified:** `grep -rn "state_trend\|judgment_interpretation\|image_action" apps/frontend/app/` returns only test assertion strings, zero runtime consumers
- **Docs updated:** handoff + notebook dev-frontend
- **Graphify:** skipped (no architecture doc changed)

**Vitest summary:** Tests 14 passed (14) — `app/__tests__/QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.ts`
**Playwright summary:** 4 passed (4.4s) — `tests/e2e/render-check.spec.ts`

---

## Risk Flags

- **RF-1 (LOW):** que-reference.js file format must be verified (whitespace + semicolon variants). Mitigation: smoke test confirms 64 entries.
- **RF-2 (LOW):** Field rename (`state_trend` → `marketTrendLabel`) may break other consumers. Mitigation: this subtask verifies grep shows no other imports before Subtask 1b runs.

---

## Signal to QA
Once this task reaches REVIEW, Subtask 1b (SnapshotRow migration + NFR gates) is unblocked and may proceed in parallel.

---

## [QA] Review Record

**QA date:** 2026-06-12T12:00Z
**Verdict:** APPROVED

### Checks performed

| Check | Result |
|---|---|
| Sprint tests (14 — QUE-TOOLTIP-DRY-1a-codegen-pipeline.test.ts) | 14 pass / 0 fail |
| tsc --noEmit (frontend) | 0 errors |
| DDD scan (domain→infrastructure imports) | PASS — 0 matches |
| Security scan (process.env, hardcoded secrets) | PASS — pre-existing server-side origin pattern, not sprint-introduced |
| mock-guard | PASS (exit 0) |
| Generated file header cites que-reference.js | CONFIRMED (line 3) |
| 64 entries in QUE_DESCRIPTIONS | CONFIRMED (node count) |
| old fields absent (state_trend, judgment_interpretation, image_action) | CONFIRMED (grep exit=1) |
| italic removed from secondary tooltip line | CONFIRMED (grep exit=1) |
| Quẻ 1 spot-check: coreMeaning = "Sức sáng tạo nguyên thủy..." | MATCHES que-reference.js exactly |
| Quẻ 1 spot-check: marketTrendLabel = "Thuận lợi (THUẬN LỢI)" | MATCHES que-reference.js exactly |

**Status: DONE**
