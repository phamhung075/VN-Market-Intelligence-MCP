# TASK_1799 Handoff — Trading Economics Chromium News Fetcher

## Status
IMPL COMPLETE — branch `fix/te-chromium-news`, commit `a0afee05`

## What was built

### 1. `tradingEconomicsChromium.ts` — new exports

| Symbol | Kind | Purpose |
|--------|------|---------|
| `TENewsItem` | interface | Single news item scraped from TE Vietnam news page |
| `TeNewsCacheEntry` | interface | Persisted JSON cache record for news |
| `TeNewsDeps` | interface | DI interface: `scrape(url): Promise<string>` + optional `cachePath` |
| `extractTeNewsItems(html, limit)` | pure fn | Cheerio parser: `ul#stream li.te-stream-item` → `TENewsItem[]` |
| `parseRelativeDate(raw)` | pure fn | "2 hours ago" / "3 days ago" / "1 week ago" / "30 minutes ago" → ISO 8601 |
| `fetchTradingEconomicsNews(limit?, deps?)` | async fn | 30-min cache, 2h stale fallback, returns `[]` on total failure |
| `playwrightScrapeNews(url)` | async fn | Production Puppeteer scraper (never called in tests) |

Cache file: `/app/data/te-news-cache.json` (TTL 30 min, stale window 2h).

### 2. `pollNews.ts` — additions

- `SourceFetchers.teChromiumNews` — new injectable key
- `defaultTeChromiumNewsFetcher()` — maps `TENewsItem[]` → `RssItem[]` via `parseRelativeDate`
- Registered in `knownKeys`, `resolvedFetchers`, `SOURCE_DISPLAY_NAMES` ("Trading Economics News")
- `TENewsItem.source` → `"tradingeconomics"` so the existing `scoreMacroIndicator` tiered scoring applies

### 3. Tests — `1799-te-chromium-news.test.ts`

19 tests, 16 acceptance criteria:
- AC-1 to AC-6: HTML extraction (title, URL, summary, category, date, limit)
- AC-7 to AC-10: cache lifecycle (fresh hit, stale re-scrape, failure fallback, no-cache returns [])
- AC-11 to AC-15: `parseRelativeDate` (hours, days, weeks, minutes, unknown)
- AC-16: `source` is always `'trading_economics'`

## Files changed

- `apps/mcp-server/src/infrastructure/fetchers/tradingEconomicsChromium.ts` — appended ~280 lines
- `apps/mcp-server/src/application/usecases/pollNews.ts` — added fetcher + registration (~40 lines)
- `apps/mcp-server/src/__tests__/1799-te-chromium-news.test.ts` — new, 19 tests

## Test results

```
19 pass  0 fail  (1799-te-chromium-news.test.ts)
8453 pass  39 fail  (full suite — 39 failures are pre-existing, none in Task 1799 scope)
```

## QA validation checklist

- [ ] `extractTeNewsItems` correctly maps all 5 fields from the real TE HTML structure
- [ ] `parseRelativeDate` handles all documented relative-time formats
- [ ] Cache write/read round-trip works (30-min TTL, 2h stale window)
- [ ] `pollNews` with `fetchers.teChromiumNews` injected returns articles through full pipeline
- [ ] On dev machine (no Chromium), `teChromiumNews` returns `[]` gracefully, `errors` counter stays 0
- [ ] `SOURCE_DISPLAY_NAMES["teChromiumNews"]` renders as "Trading Economics News" in health table
