# TASK 1805b-1 — alert-commander cycle.md: chain_catalyst firing matrix

## Sprint
1805b — chain_catalyst firing matrix

## Wave
Wave 1 (parallel with 1805b-2 and 1805b-3)

## File
`.claude/flows/alert-commander/cycle.md`

## Context
Sprint 1804d introduced `chain_catalyst` as a signal produced by news-scout.
The alert-commander cycle.md has no awareness of this signal type.
It must learn: thresholds, signal matrix row, processing block, Step 4a caveat, and Step 5 log template.

## Changes Required

### 1. Signal Matrix (Step 3) — add `chain_catalyst` row

In the table under **3. Signal Matrix**, after the `verified_chain` row, insert:

```
| `chain_catalyst` | confidence ≥ regime threshold | CRITICAL or MARKET |
```

Regime thresholds for `chain_catalyst`:
- `TIGHTENING`: confidence ≥ 0.85
- `EASING`:     confidence ≥ 0.70
- `NEUTRAL`:    confidence ≥ 0.75

Add these thresholds to the "Base thresholds" paragraph just above the table:
```
`chain_catalyst` NEUTRAL conviction ≥ 0.75
```

### 2. Step 3 processing block — chain_catalyst handling

After the existing **3b. Price-validation override** block, add a new block **3c. chain_catalyst processing**:

```markdown
**3c. chain_catalyst processing**
For each `chain_catalyst` signal from signal bus:
  1. Read `finding_data.confidence`, `finding_data.direction`, `finding_data.event_type`, `finding_data.affected_stocks`
  2. Apply regime threshold (see Step 3 matrix): confidence ≥ threshold → proceed
  3. Direction routing:
     - `bearish` → CRITICAL alert (position-danger)
     - `bullish` + earnings event_type → MARKET alert (watchlist-opportunity)
     - `bullish` + macro/trade_war event_type → MARKET alert with regime caveat
     - `neutral` → WORK channel only, do not fire MARKET
  4. Conflict check: call `get_agent_signals(signal_type="chain_catalyst", stock_code=affected_stock)` — if two signals for same ticker have conflicting `direction` → append conflict warning from `payload.detail` (earningsConflictDetector sets this)
  5. Log: `"[ChainCatalyst] [TICKER] event={event_type} dir={direction} conf={confidence:.2f} → {CRITICAL|MARKET|suppressed}"`
  6. Call `record_signal_outcome(signal_id, "fired"|"suppressed", reason)`
```

### 3. Step 4a — regime caveat for chain_catalyst

In **4a. MARKET channel**, after the existing three regime caveat bullets, add:

```markdown
- `chain_catalyst` + `TIGHTENING` + `bullish`:
  `"Lưu ý: Xúc tác chuỗi trong môi trường thắt chặt — xác nhận thêm trước khi hành động."`
- `chain_catalyst` + `bearish` (any regime):
  `"Cảnh báo: Xúc tác tiêu cực được phát hiện — kiểm tra danh mục ngay."`
```

### 4. Step 5 session log — add chain_catalyst line

In **5. Session log**, extend the template to include:
```
- ChainCatalyst: N fired | M suppressed | event_types: [list]
```

## Acceptance Criteria
- [ ] Signal matrix table contains `chain_catalyst` row with correct action column
- [ ] Base thresholds paragraph mentions `chain_catalyst` NEUTRAL = 0.75
- [ ] Step 3c block present with all 6 sub-steps
- [ ] Step 4a contains both chain_catalyst caveat bullets
- [ ] Step 5 template includes ChainCatalyst log line
- [ ] No existing content removed or reformatted

## Dependencies
None (Wave 1 — independent file)

## Owner
developer

## Estimated effort
~1h
