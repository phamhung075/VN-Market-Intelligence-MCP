---
sprint: IVC-C5
branch: none   # CLAUDE.md: NO branches — all work stays on main (flow Step 3b template is stale)
size: S
zone: scripts/
owner: dev-mcp-server
next_agent: dev-mcp-server
depends_on: ["IVC-C1"]
blocks: []
---

# IVC-C5 — Class-C audit sink docs/data/validation-rejections.jsonl + cold-archive-sweep.sh rotation + bug-channel telegram per BLOCK

## TLDR
Class-C audit sink docs/data/validation-rejections.jsonl + cold-archive-sweep.sh rotation + bug-channel telegram per BLOCK. Zone `scripts/`, owner `dev-mcp-server`, size S. BLOCKED until IVC-C1 reach DONE_VERIFIED — `deps_satisfied()` enforces this mechanically at dispatch, do not start early.

## [PM] Planning Context
- **Zone:** `scripts/`
- **Priority:** high
- **Dependencies:** `IVC-C1`
- **Files in scope:**
  - `docs/data/validation-rejections.jsonl`
  - `scripts/cold-archive-sweep.sh`
- **Acceptance criteria / design payload** (verbatim from the board row `note` — the architect brief it cites is the SSOT, this is the pointer):

  > Parent IVC-PM-DECOMPOSE. Append-only, one ValidationRejection record per BLOCK. Reuse cold-archive-sweep.sh's existing monthly rotation — do NOT build a bespoke second rotation mechanism. NFR-5: this jsonl is the Class-C sink; the write_rejections DB table (IVC-A2) is the Class-A sink — same logical shape, deliberately distinct physical stores, never unified.

- **Knowledge needed:** `docs/policies/dev-standards.md`, `docs/ARCHITECTURE.md`, `docs/policies/commit-convention.md` (AC trailer), `.claude/skills/commit-boundary/SKILL.md`

## Definition of Done
- [ ] Every acceptance criterion above is satisfied AND demonstrated by a test or a live run — narration is a rejection.
- [ ] Commit carries the `AC:` trailer per `docs/policies/commit-convention.md`.
- [ ] Decision-journal entry written, stamped with task-id `IVC-C5` (DJ-GATE-1 blocks the DONE flip without it).
- [ ] Board row moved to `review[]` with `next_agent: qa`.

---
_Generated by `scripts/pm-emit-handoff-from-row.sh` from the live `IVC-C5` board row. Re-run after any row edit — do not hand-edit this file._

