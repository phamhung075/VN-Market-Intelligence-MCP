---
sprint: TASK-BRANCHGUARD-POSTCHECKOUT-HOOK
branch: none   # CLAUDE.md: NO branches — all work stays on main (flow Step 3b template is stale)
size: M
zone: scripts/git-hooks/
owner: developer
next_agent: developer
depends_on: []
blocks: []
---

# TASK-BRANCHGUARD-POSTCHECKOUT-HOOK — Author scripts/git-hooks/post-checkout branch-hijack guard (MODE=warn default at install) + install.sh wiring + tests

## TLDR
Author scripts/git-hooks/post-checkout branch-hijack guard (MODE=warn default at install) + install.sh wiring + tests. Zone `scripts/git-hooks/`, owner `developer`, size M. No dependencies — dispatchable now.

## [PM] Planning Context
- **Zone:** `scripts/git-hooks/`
- **Priority:** P1
- **Dependencies:** none
- **Files in scope:**
  - `scripts/git-hooks/post-checkout`
  - `scripts/git-hooks/install.sh`
- **Acceptance criteria / design payload** (verbatim from the board row `note` — the architect brief it cites is the SSOT, this is the pointer):

  > Parent FIX-SUBAGENT-BRANCH-CHECKOUT-HIJACKS-SHARED-WORKING-DIR. Design: docs/architecture-briefs/2026-07-31-fix-subagent-branch-checkout-hijacks-shared-working-dir.md, AC-1(a) chosen. git has NO pre-checkout hook (verified, git 2.49.0) — post-checkout hard-reverts a non-main checkout in the PRIMARY working dir back to main and fail-louds. MUST exempt LINKED worktrees by construction (git-dir vs git-common-dir), so SPIKE-C44 is NOT foreclosed and NOT a dependency. AC-2: reachable by agents with no MCP grant — this is why it is a git hook, not a skill or flow-doc rule (4/4 incidents were improvised behaviour, not doc-following). Non-destructive edge: if uncommitted work would be discarded, REFUSE the revert and fail loud rather than discard. PM DECISION (this is the timing call architect explicitly deferred to pm): ship at MODE=warn default here, do NOT enable enforce yet — see TASK-BRANCHGUARD-ENFORCE-FLIP.

- **Knowledge needed:** `docs/policies/dev-standards.md`, `docs/ARCHITECTURE.md`, `docs/policies/commit-convention.md` (AC trailer), `.claude/skills/commit-boundary/SKILL.md`

## Definition of Done
- [ ] Every acceptance criterion above is satisfied AND demonstrated by a test or a live run — narration is a rejection.
- [ ] Commit carries the `AC:` trailer per `docs/policies/commit-convention.md`.
- [ ] Decision-journal entry written, stamped with task-id `TASK-BRANCHGUARD-POSTCHECKOUT-HOOK` (DJ-GATE-1 blocks the DONE flip without it).
- [ ] Board row moved to `review[]` with `next_agent: qa`.

---
_Generated by `scripts/pm-emit-handoff-from-row.sh` from the live `TASK-BRANCHGUARD-POSTCHECKOUT-HOOK` board row. Re-run after any row edit — do not hand-edit this file._

