# TASK_1402 — Resolve Stranded agent_feedback Rows (IDs 73, 76, 77)

## Status
Todo

## Priority
MEDIUM

## Type
bug / data

## Owner
developer

## Context

Three `agent_feedback` rows are stuck in `status = 'stranded_bctc_pdf'`:

| ID | Status                | Created     |
|----|----------------------|-------------|
| 73 | stranded_bctc_pdf    | ~2026-04-27 |
| 76 | stranded_bctc_pdf    | ~2026-04-27 |
| 77 | stranded_bctc_pdf    | ~2026-04-27 |

These rows block the disk-scan fallback path in the BCTC PDF extraction pipeline. When
`bctcReparseJob` checks for unprocessed feedback, these stranded rows prevent it from
advancing the fallback queue.

## Acceptance Criteria

1. Rows 73, 76, 77 are resolved — either:
   - Updated: `status = 'resolved'`, `resolved_at = NOW()`, `resolution_note = 'Cleared by TASK_1402: stranded_bctc_pdf cleanup'`
   - OR deleted if the schema has no FK dependencies
2. Verification query confirms none of the three IDs retain `status = 'stranded_bctc_pdf'`
3. No other `agent_feedback` rows are modified (only IDs 73, 76, 77)
4. A one-shot SQL script committed as `scripts/fix-stranded-feedback-rows.sql`
5. Task report documents: which option was chosen (update vs delete) and why

## Implementation Notes

- Preferred: UPDATE (preserves audit trail) — set status to 'resolved'
- Check FK deps first: `PRAGMA foreign_key_list('agent_feedback')` or equivalent
- After fix, confirm disk-scan fallback path is unblocked by checking bctcReparseJob next cycle log

## Dependencies
Can run in parallel with TASK_1401.

## Estimated Size
XS (15–20 min)

## Task Report
`apps/mcp-server/reports/TASK_REPORT_1402.md`
