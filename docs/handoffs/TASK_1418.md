# Handoff: TASK-1418 — RED test: diacritics wave 6

sprint: 149
status: Todo
agent: Dev
depends_on: —
tech_ref: docs/TECH_1418.md

## Objective

Write `src/__tests__/1418-diacritics-wave6.test.ts`.
All assertions must FAIL before task 1419 fixes the source files.
After task 1419, all assertions must be GREEN.

## Deliverable

Single file: `src/__tests__/1418-diacritics-wave6.test.ts`

## Test Structure

Two groups, same pattern as waves 1–5 (reference: `src/__tests__/1416-diacritics-wave5.test.ts`).

### Group A — direct-call (exportPortfolioSnapshot)

```
import { exportPortfolioSnapshot } from "../application/usecases/exportPortfolioSnapshot.js";
```

Setup: use `Database(":memory:")` as injected db. No fixtures needed (write error triggers on bad dir).

| it() description | call | assertion |
|------------------|------|-----------|
| "error sentinel contains Vietnamese diacritics" | `exportPortfolioSnapshot({ db, exportDir: "/root/no-perm-xyz" })` | `result.error` contains `"(không thể ghi file):"` |
| "old broken sentinel absent" | same | `result.error` does NOT contain `"khong the ghi file"` |

### Group B — source-scan

```
import { readFileSync } from "fs";
import { join } from "path";
const TOOLS = join(import.meta.dir, "../interface/mcp/tools");
const APP   = join(import.meta.dir, "../application/usecases");
```

#### customAlertTools.ts — 3 strings

```
const src = readFileSync(join(TOOLS, "customAlertTools.ts"), "utf8");
```

| Positive | Negative guard |
|----------|----------------|
| contains `"Chưa có quy tắc cảnh báo tùy chỉnh nào."` | not contains `"Chua co quy tac canh bao tuy chinh nao."` |
| contains `"Quy tắc cảnh báo tùy chỉnh"` and `"đang hoạt động"` and `"tổng cộng"` | not contains `"Quy tac canh bao tuy chinh"` |
| contains `"Lỗi khi lấy danh sách quy tắc:"` | not contains `"Loi khi lay danh sach quy tac:"` |

#### changelogTools.ts — 4 strings

```
const src = readFileSync(join(TOOLS, "changelogTools.ts"), "utf8");
```

| Positive | Negative guard |
|----------|----------------|
| contains `"Lỗi khi ghi sửa lỗi:"` | not contains `"Loi khi ghi sua loi:"` |
| contains `"Chưa có sửa lỗi nào được ghi lại."` | not contains `"Chua co sua loi nao duoc ghi lai."` |
| contains `"sửa lỗi gần nhất (mới nhất trước)"` | not contains `"sua loi gan nhat (moi nhat truoc)"` |
| contains `"Lỗi khi đọc lịch sử sửa lỗi:"` | not contains `"Loi khi doc lich su sua loi:"` |

#### targetAllocationTools.ts — 12 strings

```
const src = readFileSync(join(TOOLS, "targetAllocationTools.ts"), "utf8");
```

| Positive | Negative guard |
|----------|----------------|
| contains `"Chưa có mục tiêu phân bổ."` | not contains `"Chua co muc tieu phan bo."` |
| contains `"Dùng set_target_allocation để thiết lập mục tiêu."` | not contains `"Dung set_target_allocation de thiet lap muc tieu."` |
| contains `"Phân bổ mục tiêu"` | not contains `"Phan bo muc tieu"` |
| contains `pad("Mã",` | not contains `pad("Ma",` |
| contains `pad("Mục tiêu",` | not contains `pad("Muc tieu",` |
| contains `pad("Hiện tại",` | not contains `pad("Hien tai",` |
| contains `"(đúng)"` | not contains `"(dung)"` |
| contains `"(thừa)"` | not contains `"(thua)"` |
| contains `"(thiếu)"` | not contains `"(thieu)"` |
| contains `"(không có mục tiêu)"` | not contains `"(khong co muc tieu)"` |
| contains `"Tổng danh mục:"` | not contains `"Tong danh muc:"` |
| contains `"Lưu ý: Giá ước tính (giá vốn) cho:"` | not contains `"Luu y: Gia uoc tinh"` |
| contains `"Lưu ý: Chưa có vị trí nào trong danh mục"` | not contains `"Luu y: Chua co vi tri nao"` |
| contains `"Lỗi khi đọc phân bổ mục tiêu:"` | not contains `"Loi khi doc phan bo muc tieu:"` |

#### exportPortfolioSnapshot.ts (application layer) — 2 strings

```
const src = readFileSync(join(APP, "exportPortfolioSnapshot.ts"), "utf8");
```

| Positive | Negative guard |
|----------|----------------|
| contains `"(không thể ghi file)"` | not contains `"(khong the ghi file)"` |

## Acceptance

- `bun test src/__tests__/1418-diacritics-wave6.test.ts` → all assertions FAIL (RED) before fix
- No TypeScript compilation error (`bun tsc --noEmit`)
- No imports from infrastructure layer (no DB init needed — Group A uses injected db)

## Branch

`task/1418-red-test-diacritics-wave6`

---

## [Architect] Brownfield Findings

interfaces_found:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1416-diacritics-wave5.test.ts`   # REUSE pattern — Group A + Group B structure, readFileSync scan
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/190-export-snapshot.test.ts`   # EXISTING test with conflicting sentinel on line 254 — update in task 1419

interfaces_to_create:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1418-diacritics-wave6.test.ts`   # NEW — RED test

decisions:
- "customAlertTools, changelogTools, targetAllocationTools embed diacritics strings in handler closures not exported — source-scan is correct strategy"
- "exportPortfolioSnapshot exports the function directly — direct-call is possible for Group A"
- "test-190 line 254 asserts old broken sentinel — must be updated atomically in task 1419 with the source fix"

brownfield_scan_clean: true

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1418-diacritics-wave6.test.ts   # NEW — RED test, 46 assertions

tests_written:
- src/__tests__/1418-diacritics-wave6.test.ts   # 46 assertions, all RED before task 1419

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # 46/46 RED confirmed pre-fix; branch merged to main
