# TASK_1361 Handoff — 48h telegram_reports purge in daily audit

## What was done

Added a 48-hour purge step for `telegram_reports` to the daily audit job.

### File 1 — `apps/mcp-server/src/infrastructure/db/telegramReportStore.ts`

Added `deleteOldReports(db, olderThanHours = 48): number` at the bottom of the file (after `listNewReportsUnclaimed`). Uses unix epoch cutoff against `created_at INTEGER` column. Returns `result.changes`.

### File 2 — `apps/mcp-server/src/scheduler/news-analysis/dataAuditJob.ts`

- Added import: `import { deleteOldReports } from "../../infrastructure/db/telegramReportStore.js";`
- Inserted **D-10** block (telegram_reports purge) between old D-9 (system_logs purge) and old D-10 (row count snapshot)
- Old D-10 row count snapshot renamed to **D-11** in comments (D-11b for the drop detection inner block)
- JSDoc updated: "D-1 through D-11 checks"

### File 3 — `apps/mcp-server/src/__tests__/226-telegram-report-store.test.ts`

Added 6 new tests in section 6 (Task 1361):
1. Deletes rows older than 48h and returns correct count
2. Does NOT delete rows newer than 48h
3. Deletes old rows but preserves recent ones (mixed scenario)
4. Returns 0 when no rows exist
5. Returns 0 when all rows are within the cutoff window
6. Respects a custom `olderThanHours` threshold (1h test)

## Test results

- Targeted: 29 pass, 0 fail (226-telegram-report-store.test.ts)
- Full suite: 7857 pass, 0 fail (7878 ran; Bun GC crash at teardown is a pre-existing Bun 1.3.11 issue, not a test failure)

## Commit

`ba123db9` — feat(1361): add 48h purge for telegram_reports to daily audit job (D-10)
