# TASK_1403 — VPS: Restart vn-foreign-flow.service (Circuit Breaker OPEN)

## Status
Done — commit e5d7f498 on task/1403-cb-idle-bugs

## Priority
HIGH

## Type
ops / infra

## Owner
ops

## Context

`vn-foreign-flow.service` on the Vinahost VPS has its circuit breaker in OPEN state. The service
was last seen active ~9 hours ago (as of 2026-04-28 ops health check). Foreign flow data has not
been pushed to the MCP server since then, meaning the foreignFlow circuit breaker server-side is
also likely in a degraded state.

The server-side CB auto-reset (halfOpenMaxAttempts:1) was fixed in Sprint 1388, so once the VPS
service is back up and pushes data, the server-side CB should close automatically.

## Acceptance Criteria

1. SSH to Vinahost VPS and check service status: `systemctl status vn-foreign-flow`
2. Review last 50 lines of journal for root cause: `journalctl -u vn-foreign-flow -n 50`
3. Restart the service: `systemctl restart vn-foreign-flow`
4. Confirm service is active (running): `systemctl is-active vn-foreign-flow`
5. Confirm data push resumed: watch MCP server logs for `foreignFlow` push accepted
6. If service fails to start, diagnose and fix the underlying cause before marking done
7. Document root cause + fix in task report

## Implementation Notes

- VPS connection details: see `docs/handoffs/VPS_DEPLOYMENT_2026-04-25.md` or `.claude/knowledge/`
- If OOM: check `free -h` and restart with `systemctl restart vn-foreign-flow` (watchdog should prevent recurrence)
- If config issue: check `/etc/systemd/system/vn-foreign-flow.service` for stale paths
- If network issue: check `curl -s http://localhost:<port>/health` from VPS
- After VPS restart, server-side CB will auto-probe on next foreignFlow push (Sprint 1388 fix)

## Dependencies
None. Standalone ops task.

## Estimated Size
S (20–40 min depending on root cause)

## QA Acceptance Criteria

1. `bun test src/__tests__/1403-cb-idle-bugs.test.ts` — 9/9 pass
2. `bun test src/__tests__/1312b-migration-guard.test.ts src/__tests__/FIX-foreign-flow-unique-constraint.test.ts src/__tests__/1131-upsert-foreign-flow.test.ts src/__tests__/234-vps-health-sla.test.ts` — all pass
3. After docker restart: VPS foreign-flow pushes should be accepted (no CB errors in server logs)
4. `vps_service_health` table accepts `health_status='idle'` without constraint error

## Files Changed

- `apps/mcp-server/src/infrastructure/db/vnstockStore.ts` — Bug 1 fix
- `apps/mcp-server/src/infrastructure/db/schema-system.ts` — Bug 2 fix
- `apps/mcp-server/src/__tests__/1403-cb-idle-bugs.test.ts` — 9 new tests

## Task Report
`apps/mcp-server/reports/TASK_REPORT_1403.md`
