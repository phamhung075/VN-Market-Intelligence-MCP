# TASK 1968b1 — L-4 get_agent_signals Consolidation (dev-mcp-server -> agent-father)

**Sprint:** 1968 | **Phase:** 2a | **Owners:** dev-mcp-server (phase1) -> agent-father (phase2) | **Released:** 2026-05-21T19:35:13Z po c237
**Brief:** `docs/architecture-briefs/2026-05-21-token-toolcall-economy.md` §L-4
**Time-box:** dev-mcp-server 1h + agent-father 1h (sequential)

## Goal
Consolidate news-scout's 3 separate `get_agent_signals` calls per cycle into a single cached call. Saves ~2 MCP calls/cycle = ~56 calls/trading-day.

## Phase 1 — dev-mcp-server param check
1. Read `apps/mcp-server/src/...` (tool: `get_agent_signals`).
2. Check whether tool supports `hours_back` parameter (or equivalent lookback param ≥360 min for legal_risk dedup window).
3. If supported: confirm default behavior; document in `docs/standards/mcp-tools.md`.
4. If NOT supported: add `hours_back: number` parameter (default = 60 min preserves backward compat; news-scout will pass `hours_back: 360`).
5. Run unit + integration test(s) covering the new param.
6. Emit `docs/signals/dev-mcp-server-1968b1-param-ready.json` (caveman ULTRA) with status + param name.

## Phase 2 — agent-father flow edits (gated on phase1 signal)
1. Edit `.claude/flows/news-scout/stage-bootstrap.md` Step 0c:
   - Replace 3 separate `get_agent_signals` calls (feedback + dedup + legal_dedup) with a single call: `get_agent_signals(from_agent="news-scout", hours_back=360)`.
   - Store result in flow-scoped `SELF_SIGNALS_CACHE`.
2. Edit `.claude/flows/news-scout/stage-signals.md`:
   - Replace dedup MCP call with client-side filter on `SELF_SIGNALS_CACHE` (filter by `signal_type` in dedup-eligible set).
   - Replace legal_risk dedup MCP call with client-side filter on same cache (filter by `signal_type IN ('legal_risk', 'crisis_early_warning')`).
3. Ensure cache is scoped per-cycle (not persisted between cycles).
4. Caveman ULTRA done signal `docs/signals/agent-father-1968b1-done.json`.

## Acceptance Criteria
- AC-1: get_agent_signals supports hours_back (or equivalent) ≥360 min lookback.
- AC-2: news-scout consolidates 3 calls → 1.
- AC-3: cache lookup logic in stage-signals.md uses client-side filter, no MCP round-trip.
- AC-4: smoke shows 2 fewer MCP calls/cycle for news-scout (verify in log_agent_work or session log).
- AC-5: signal `docs/signals/agent-father-1968b1-done.json` emitted (caveman ULTRA).

## Out of Scope
- L-6 cron stagger / snapshot file (1968b2).
- L-7 notebook commit batching (1968b2).
- L-8 composite skill, L-9 server-side filter (Phase 3).
- BCTC paths (freeze).

## Constraints
- WIP: 1968b1 + 1968b2 = 2 parallel; no further dev tasks.
- BCTC freeze active.
- No branches.
- NFR-5 honored: zero overlap with 1967b architect brief surfaces.

## Coordination
- 1968b2 (agent-father L-6+L-7) may run in parallel after dev-mcp-server starts phase1.
- 1967b architect re-run still in flight (~ETA 4h from 19:01Z). If 1967b lands a finding touching news-scout stage-bootstrap/stage-signals, agent-father merges before commit.

---

## [QA] Review Record
**Date:** 2026-05-21 | **Reviewer:** qa | **Round:** 1

**Verdict: APPROVED**

| Check | Result |
|-------|--------|
| 1968b1 unit tests 7/7 | PASS |
| mcp-server regression (9314 pass / 283 fail) | PASS — 283 pre-existing baseline, unchanged |
| tsc --noEmit | 0 errors |
| DDD (domain → infra import) | PASS |
| Security (process.env, hardcoded secrets) | PASS |
| SQL parameterization | PASS (hoursBack injected via Math.ceil on Zod-validated positive number) |
| AC-1 hours_back Zod schema | PASS |
| AC-2 3 calls → 1 | PASS |
| AC-3 client-side cache filter | PASS |
| AC-4 2 fewer MCP calls/cycle | PASS (design-verified: 360min covers all 3 dedup windows) |
| AC-5 agent-father-1968b1-done.json | PASS |
| BCTC freeze NFR-3 | PASS — 0 BCTC files touched |
| Tool doc get_agent_signals.md updated | PASS |
| Caveman compression preserved | PASS |

Report: `reports/TASK_REPORT_1968b1.md`
