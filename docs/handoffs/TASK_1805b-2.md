# TASK 1805b-2 — alert-commander.md: add chain_catalyst to signals + inter_agent

## Sprint
1805b — chain_catalyst firing matrix

## Wave
Wave 1 (parallel with 1805b-1 and 1805b-3)

## File
`.claude/agents/alert-commander.md`

## Context
The agent metadata file drives Cowork agent bootstrap. `signals.consumes` and
`inter_agent.receives_from` do not list `chain_catalyst`. This means the agent
file is inconsistent with the cycle flow (post 1805b-1) and may cause confusion
during Cowork refresh.

## Changes Required

### 1. signals.consumes — add chain_catalyst

Current list:
```yaml
signals:
  consumes:
    - urgent_news
    - price_anomaly
    - verified_chain
    - legal_risk
    - crisis_velocity
```

Add `chain_catalyst` after `verified_chain`:
```yaml
signals:
  consumes:
    - urgent_news
    - price_anomaly
    - verified_chain
    - chain_catalyst
    - legal_risk
    - crisis_velocity
```

### 2. inter_agent.receives_from — add chain_catalyst entry

Current `receives_from` list ends with the `market-watcher` entry.
Append a new entry after it:

```yaml
      - agent: news-scout
        mechanism: signal_bus
        signal_type: chain_catalyst
        trigger: crisis_or_macro_catalyst
```

Note: news-scout already appears once (for `news_impact, crisis_velocity`).
This is a separate entry for `chain_catalyst` — keep both, do not merge.

### 3. tools frontmatter — verify get_agent_signals present

The YAML frontmatter `tools:` line must include `mcp__vn-market__get_agent_signals`.
Check current line 5. If missing, add it after `mcp__vn-market__get_crisis_early_warning`.
(It is already in `permissions.tools` at line 30 — frontmatter must match.)

## Acceptance Criteria
- [ ] `signals.consumes` contains `chain_catalyst` between `verified_chain` and `legal_risk`
- [ ] `inter_agent.receives_from` has a news-scout entry with `signal_type: chain_catalyst`
- [ ] YAML frontmatter `tools:` includes `mcp__vn-market__get_agent_signals`
- [ ] No other content modified

## Dependencies
None (Wave 1 — independent file)

## Owner
developer

## Estimated effort
~30min
