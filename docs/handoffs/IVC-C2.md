---
sprint: IVC-C2
branch: none   # CLAUDE.md: NO branches — all work stays on main (flow Step 3b template is stale)
size: M
zone: apps/mcp-server/
owner: dev-mcp-server
next_agent: dev-mcp-server
depends_on: ["IVC-C1"]
blocks: []
---

# IVC-C2 — Schema registry scaffold storeSchemaRegistry.ts + classify docs/data/*.json into shape families + generated class-c-coverage.json

## TLDR
Schema registry scaffold storeSchemaRegistry.ts + classify docs/data/*.json into shape families + generated class-c-coverage.json. Zone `apps/mcp-server/`, owner `dev-mcp-server`, size M. BLOCKED until IVC-C1 reach DONE_VERIFIED — `deps_satisfied()` enforces this mechanically at dispatch, do not start early.

## [PM] Planning Context
- **Zone:** `apps/mcp-server/`
- **Priority:** high
- **Dependencies:** `IVC-C1`
- **Files in scope:**
  - `apps/mcp-server/src/infrastructure/schemas/storeSchemaRegistry.ts`
  - `docs/data/coverage/class-c-coverage.json`
- **Acceptance criteria / design payload** (verbatim from the board row `note` — the architect brief it cites is the SSOT, this is the pointer):

  > Parent IVC-PM-DECOMPOSE. NFR-3: registry is a TS module — Zod schemas are TS values, NEVER moved to docs/data/*.json. NFR-4: class-c-coverage.json is script output, never hand-edited. EC-2: family classification must FLAG which families are multi-writer (do not assume none are) — those need orch-apply.sh's CAS-mtime atomic-rename reused verbatim as an opt-in wrapper. Live re-glob must reproduce the same family boundaries (no silent drift).

- **Knowledge needed:** `docs/policies/dev-standards.md`, `docs/ARCHITECTURE.md`, `docs/policies/commit-convention.md` (AC trailer), `.claude/skills/commit-boundary/SKILL.md`

## Definition of Done
- [ ] Every acceptance criterion above is satisfied AND demonstrated by a test or a live run — narration is a rejection.
- [ ] Commit carries the `AC:` trailer per `docs/policies/commit-convention.md`.
- [ ] Decision-journal entry written, stamped with task-id `IVC-C2` (DJ-GATE-1 blocks the DONE flip without it).
- [ ] Board row moved to `review[]` with `next_agent: qa`.

---
_Generated by `scripts/pm-emit-handoff-from-row.sh` from the live `IVC-C2` board row. Re-run after any row edit — do not hand-edit this file._

