# TASK 1550 — fix(watchdog): alert MARKET channel when VPS data pipeline goes stale

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/scheduler/vpsProxyWatchdogJob.ts
  - added `sendTelegramMarket` to import
  - added `notifyUser?` to options type
  - wired `notifyUser` default (`sendTelegramMarket`); called after WORK alert succeeds; user message names stale services, no SSH commands

tests_written:
- src/__tests__/1550-watchdog-market-alert.test.ts — 3 assertions, all GREEN
  1. MARKET alert fires when stale (names service, no ssh/systemctl)
  2. MARKET alert skipped during cooldown (shared 30-min window)
  3. WORK alert still fires independently

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # 5937 pass, 0 fail (Bun runtime crash pre-existing, unrelated)
