# Handoff: TASK-1424 — test(evening-summary): TDD RED — sector aggregation assertions

sprint: 152
role: Dev
status: Todo
depends_on: none

---

## Mission

Create `src/__tests__/1424-evening-sector-aggregation.test.ts` with assertions that import `formatMoversSection` from `eveningSummaryJob.ts`. Tests MUST fail (RED) because `formatMoversSection` does not exist yet. No implementation in this task.

---

## What to build

File: `src/__tests__/1424-evening-sector-aggregation.test.ts`

Import target (will fail until Task 1425):
```typescript
import { formatMoversSection } from "../scheduler/eveningSummaryJob.js";
```

Write 6 tests matching AC-1 through AC-6 from TECH_1424.md §Test Specification.

Each test calls `formatMoversSection(movers)` and asserts on the returned `string[]` (join with `\n` to use `.toContain()` or check array members directly).

### Test data for AC-1 (banking VCB+TCB, avg = (2.10+1.60)/2 = 1.85)
```typescript
const movers = [
  { code: "FPT",  changePct: 3.20 },
  { code: "VCB",  changePct: 2.10 },
  { code: "TCB",  changePct: 1.60 },
];
```
VCB and TCB must be in SECTOR_PEERS banking list — confirmed in `sectorPeers.ts`. FPT must be in tech list — also confirmed.

### Test data for AC-5 (sector order by |avgPct| DESC)
```typescript
// banking avg +1.00%, steel avg -3.00% → steel first
const movers = [
  { code: "VCB",  changePct: 1.00 },  // banking
  { code: "TCB",  changePct: 1.00 },  // banking
  { code: "HPG",  changePct: -3.00 }, // steel
  { code: "HSG",  changePct: -3.00 }, // steel
];
```
Confirm HPG + HSG exist in SECTOR_PEERS steel list before coding.

---

## Acceptance

```bash
bun test src/__tests__/1424-evening-sector-aggregation.test.ts
# → ALL tests FAIL with import error or assertion error (RED confirmed)
bun tsc --noEmit
# → no type errors in test file itself
```

---

## [Architect] Brownfield Findings

interfaces_found:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/domain/services/sectorPeers.ts`  # REUSE — getStockProfile, SECTOR_NAME_VI, computeSectorAverage all exported
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/scheduler/eveningSummaryJob.ts`  # MODIFY — extract formatMoversSection from lines 163-169

interfaces_to_create:
- none — formatMoversSection is a new export in existing file, not a new interface

decisions:
- "formatMoversSection extracted from runEveningSummary body so tests never need DB/Telegram stub"
- "computeSectorAverage(WatchlistMover[]) fits exactly — WatchlistMover already has {code, changePct}"
- "domain layer imports direction correct: eveningSummaryJob (interface/scheduler) → sectorPeers (domain/services)"

brownfield_scan_clean: true

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1424-evening-sector-aggregation.test.ts   # created: 6 describe blocks, 16 test cases, RED import error confirmed

tests_written:
- src/__tests__/1424-evening-sector-aggregation.test.ts   # 16 assertions, all RED before Task 1425

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # pre-existing Bun OOM crash on full bun test; 1000-1499 range 864 pass 0 fail confirmed
