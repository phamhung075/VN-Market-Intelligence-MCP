# TASK 1399 — Evening Report Empty: Fixes Applied

## Status
FIXED — Awaiting ops verification

## Changes Made

### Fix 1: Zombie process killed
- `kill 95755` executed — bun host process confirmed gone (`ps aux | grep index.ts` returned empty)
- Verified: no orphan schedulers running on host

### Fix 2: reports bind mount added to docker-compose.yml
```yaml
- ./apps/mcp-server/reports:/app/reports
```
- Container was **recreated** (not just restarted) via `docker compose up -d --no-build mcp-server`
- Mount confirmed via `docker inspect`: `bind /host_mnt/.../apps/mcp-server/reports -> /app/reports`
- Container status: **Up (healthy)**

### Fix 3: launchctl plist disabled
- `com.vn-market.mcp` plist found in `~/Library/LaunchAgents/`
- `launchctl disable gui/<uid>/com.vn-market.mcp` — exit 0 (success)
- Plist file kept in place but disabled; zombie will NOT respawn on reboot

## Test Baseline
- `Ran 7964 tests across 696 files` — no regressions (docker-compose.yml is infra config only)
- Baseline was 7926 pass / 15 fail; test count increase is from unrelated additions

## Commit
`80120495` — fix(1399): kill zombie host bun process + add reports bind mount to docker-compose

## Expected After Fix
Once eveningSummaryJob next runs (15:30 UTC / 22:30 VN):
1. `apps/mcp-server/reports/YYYY-MM-DD-evening.json` size > 541 bytes
2. `topAlerts`, `watchlistMovers`, `newsCount > 0` populated from Docker DB
3. VN-Index `fetchedAt` within last 25h

## For Ops Verification
1. `docker ps --filter "name=mcp-server"` → Up (healthy)
2. `docker inspect ... --format '{{range .Mounts}}...'` → reports bind mount present
3. Wait for next eveningSummaryJob cycle or trigger manually
4. Check `ls -la apps/mcp-server/reports/` → new file > 541 bytes
