---
sprint: CROSS-SESSION-MULTI-TEAM-ORCH
branch: task/1992-qa-presence-registry
size: M
zone: docs/protocols/ + .claude/skills/
depends_on: [TASK_1990, TASK_1991]
blocks: []
---

## TLDR

QA gate: LIVE-vs-DEPLOYED-server check + 53-fail baseline-diff. Verify:
1. Real tick registers a presence row (owner_client_session + payload fields)
2. Cross-session read surfaces it correctly
3. NEGATIVE assertion — presence TTL-expiry emits ZERO orphan-signals (proves reaper allow-list separation)

Conditional ops rebuild ONLY if live-probe finds listHeldTasks fields undeployed (not expected — likely live in HEAD).

## [PM] Planning Context

- **Zone:** docs/protocols/ + .claude/skills/ (test/verification)
- **Acceptance Criteria:**
  - [ ] Test Harness 1 (LIVE): Spawn two parallel router sessions; each claims session-presence; verify both rows appear in live db via task_list_held()
  - [ ] Test Harness 2 (CROSS-SESSION READ): Session B reads session-presence rows from Session A; verify owner_client_session ≠ Session B's UUID; payload contains all fields (agent_id, host, started_at, current_task)
  - [ ] Test Harness 3 (TTL EXPIRY): Let session-presence row TTL expire naturally (1800s → simulate via mcp-server clock or direct db UPDATE closed_at); call background reaper task; verify NO new rows in agent_signals nor orphan-FK entries
  - [ ] Negative Path: Verify reaper IGNORES presence rows (allow-list check: kind != 'session-presence')
  - [ ] 53-fail baseline: Run full baseline before + after; diff only EXPECTED flows (cowork Step 0, dev-team Step 0, presence heartbeats); no regression
  - [ ] Rebuild Gate: Live-probe coordinationStore.ts listHeldTasks for fields {owner_client_session, payload, redispatch_count}; if missing → flag rebuild_required. Otherwise SKIP rebuild.

- **Files to read first:**
  - `docs/protocols/fail-loud-protocol.md` (error boundary for test harness)
  - `apps/mcp-server/src/infrastructure/coordinationStore.ts:758-759` (listHeldTasks query)
  - `docs/agents/qa/flow/main.md` (QA test methodology)
  - `docs/data/orch/orch-state.json` (53-fail baseline state)

- **Files to create:**
  - `docs/agent-memory/decisions/sprint-CROSS-SESSION-MULTI-TEAM-ORCH-qa.md` — QA decision journal (all test results)

- **Files to modify:**
  - `.claude/skills/task-lock/SKILL.md` — test helper for TTL simulation (if needed)

- **Dependencies:** TASK_1990, TASK_1991 must be live before QA starts

- **Knowledge needed:**
  - `docs/data/system-map.json` (for session coordination)
  - MCP server lifecycle: live database + connection lifecycle
  - Reaper allow-list logic: `apps/mcp-server/src/scheduler/maintenance/reaperCleanupJob.ts`

- **Technical notes:**
  - P2 MCP foundation already deployed — no rebuild expected
  - QA is verification-only; no code changes expected
  - TTL simulation: either (a) mock clock on mcp-server, or (b) direct db UPDATE orphaned_at
  - Orphan-signals check: query `docs/signals/` .json files; verify NO new rows with agent_signals match
  - Baseline diff scope: ONLY newly added presence rows; do not diff other signal types

- **DoD (Definition of Done):**
  - All 3 test harnesses GREEN
  - Negative path GREEN (reaper ignore-list honored)
  - Baseline diff analyzed (53 expected, 0 regressions)
  - Rebuild gate decision recorded in decision journal
  - Zero P0 or P1 alerts from live deployment

- **Estimated effort:** ~3h (test harness + live-probe + analysis)
