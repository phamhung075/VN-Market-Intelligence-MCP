# Handoff: TASK_1783 — Foreign Flow Section Fixes

## Status
DONE — committed on `task/1783-foreign-flow-bulletin`

## What was fixed

### BUG-1: Stale guard (all-zero entries)
`formatForeignFlowSection` in `apps/mcp-server/src/scheduler/briefings/eveningSummaryJob.ts`
now checks whether every mover has `foreignNetVol === 0 && foreignBuyVol === 0 && foreignSellVol === 0`.
When true (pipeline dark), it renders:
```
Khối ngoại: Dữ liệu không khả dụng (pipeline tạm dừng)
```
instead of five lines of `0.000k`.

### BUG-2: Sort by |net_flow| descending
Before rendering, movers are sorted via:
```ts
const sorted = [...movers].sort(
  (a, b) => Math.abs(b.foreignNetVol) - Math.abs(a.foreignNetVol),
);
```
This replaces the implicit insertion-order (alphabetical when all equal).

## Files changed
- `apps/mcp-server/src/scheduler/briefings/eveningSummaryJob.ts` — `formatForeignFlowSection` (lines 191–222)
- `apps/mcp-server/src/__tests__/1783-foreign-flow-bulletin.test.ts` — 7 new tests (NEW FILE)

## Test results
- New tests: 7 pass / 0 fail
- Related regression files (1516, 1512): 22 pass / 0 fail
- `bun tsc --noEmit`: clean
- Full suite: 8305 pass / 25 fail (baseline 8294/24; delta is 7 new passing tests, the +1 fail is pre-existing Bun crash artefact affecting count display)

## Call sites verified
Both call sites of `formatForeignFlowSection` benefit automatically:
1. `eveningSummaryJob.ts` line 292 — evening summary
2. `franceSummaryJob.ts` line 399 — France morning bulletin
