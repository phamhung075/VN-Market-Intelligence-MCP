# TASK 1434 + 1435 — fix(morning-briefing): show commodity values instead of count-only

## Context

`formatBriefingMessage()` rendered trackedCommodities as a count-only summary line
("N chỉ số hàng hóa đang theo dõi"). Users saw no actual data. Fix shows
per-commodity lines capped at 5, section omitted when empty.

## Acceptance Criteria

- T1: commodity with name+value+unit → line contains all three fields
- T2: >5 commodities → only first 5 shown
- T3: empty list → section omitted entirely
- T4: count-only string "N chỉ số hàng hóa đang theo dõi" never appears

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/scheduler/morningBriefingJob.ts
  - Added exported `formatCommoditiesSection()` before `formatBriefingMessage()`
  - Replaced count-only line with per-commodity lines via `formatCommoditiesSection()`

tests_written:
- src/__tests__/1434-morning-briefing-commodity-values.test.ts — 4 assertions, all GREEN

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # 5437 pass, 0 fail, 21 skipped (5458 tests across 414 files)
