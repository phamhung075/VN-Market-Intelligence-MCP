# Handoff: TASK_1409 — fix(tool-diacritics): replace unaccented strings

sprint: 144
tech_ref: docs/TECH_1408.md
req_ref: docs/REQ_1408.md
branch: task/1409-tool-diacritics-fix
depends_on: 1408 (must be RED and merged first)
blocks: nothing

---

## Dev Instructions

Verify task 1408 is RED before starting. Then apply three independent edits.

---

## Edit 1 — kinhDichTools.ts

**File:** `src/interface/mcp/tools/kinhDichTools.ts`

### Step A: Add exported function before `registerKinhDichTools` (currently at line 516)

Insert the following block at line 515 (blank line before `registerKinhDichTools`):

```typescript
/**
 * Formats the trading-context sentence for a Kinh Dich reading output.
 * Exported for testability (task 1408).
 * @param trend - the trend label from data.state.trend (any case)
 */
export function formatKinhDichTradingContext(trend: string): string {
  const t = trend.toUpperCase();
  if (t.includes("THUAN LOI") || t.includes("THUẬN LỢI")) {
    return "Nhận định giao dịch: Thuận lợi cho giao dịch — xu hướng tích cực";
  } else if (t.includes("BAT LOI") || t.includes("BẤT LỢI")) {
    return "Nhận định giao dịch: Bất lợi cho giao dịch — cẩn thận";
  } else {
    return "Nhận định giao dịch: Trung tính — cần xem thêm tín hiệu khác";
  }
}
```

### Step B: Replace inline block at lines 1030-1040 (after inserting above, line numbers shift +10)

Find this exact block (currently lines 1030-1040, will be ~1040-1050 after insertion):

```typescript
        // Trading summary
        const trend = data.state.trend.toUpperCase();
        let tradingContext = "";
        if (trend.includes("THUAN LOI") || trend.includes("THUẬN LỢI")) {
          tradingContext = "THUAN LOI cho giao dich — xu huong tich cuc";
        } else if (trend.includes("BAT LOI") || trend.includes("BẤT LỢI")) {
          tradingContext = "BAT LOI cho giao dich — can than trong";
        } else {
          tradingContext = "TRUNG TINH — can xem them tin hieu khac";
        }

        lines.push(`Nhan dinh giao dich: ${tradingContext}`);
```

Replace with:

```typescript
        // Trading summary
        lines.push(formatKinhDichTradingContext(data.state.trend));
```

---

## Edit 2 — technicalIndicatorTools.ts

**File:** `src/interface/mcp/tools/technicalIndicatorTools.ts`

### Step A: Four string fixes inside `formatReport`

| Exact old string | Exact new string |
|---|---|
| `"MA50=N/A (can 50 nen)"` | `"MA50=N/A (cần 50 nến)"` |
| `"sap xep hon hop"` | `"sắp xếp hỗn hợp"` |
| `` `MA:   ${ma5Str}  ${ma20Str}  ${ma50Str}  → Xu huong ${maSig} (${maDesc})` `` | `` `MA:   ${ma5Str}  ${ma20Str}  ${ma50Str}  → Xu hướng ${maSig} (${maDesc})` `` |
| `` `RSI(14): N/A (can toi thieu 15 nen)` `` | `` `RSI(14): N/A (cần tối thiểu 15 nến)` `` |

Note: do NOT touch lines 231-238 (conclusion block — fixed in Sprint 142 task 1405).

### Step B: Add export alias after closing `}` of `formatReport` (currently line 245)

Insert immediately after line 245:

```typescript
/** Exported alias for testability (task 1408). Same function as formatReport. */
export const formatTaIndicatorReport = formatReport;
```

---

## Edit 3 — supplyChainTools.ts

**File:** `src/interface/mcp/tools/supplyChainTools.ts`

Four string replacements inside `buildSupplyChainExposureOutput`. No structural change.

| Line | Exact old string | Exact new string |
|---|---|---|
| 106 | `"\nTin hieu co phieu: Khong co tin hieu dang ke (chi so vận tai trong ngưỡng binh thuong)"` | `"\nTín hiệu cổ phiếu: Không có tín hiệu đáng kể (chỉ số vận tải trong ngưỡng bình thường)"` |
| 115 | `"TONG KET: Co tin hieu QUAN TRONG - theo doi chat chat HPG/GMD/VNM"` | `"TỔNG KẾT: Có tín hiệu QUAN TRỌNG — theo dõi chặt chẽ HPG/GMD/VNM"` |
| 117 | `"TONG KET: Phat hien tin hieu nhe - theo doi them"` | `"TỔNG KẾT: Phát hiện tín hiệu nhẹ — theo dõi thêm"` |
| 119 | `"TONG KET: Chuoi cung ung on dinh - khong co tin hieu bat thuong"` | `"TỔNG KẾT: Chuỗi cung ứng ổn định — không có tín hiệu bất thường"` |

---

## Scope boundary — do NOT touch

| File | Lines | Reason |
|---|---|---|
| `kinhDichReading.ts` | any | "BAT LOI" lookup key must stay unchanged |
| `technicalIndicatorTools.ts` | 231-238 | conclusion block fixed Sprint 142 |
| `technicalIndicatorTools.ts` | 131-135 | insufficient-data guard strings — separate sprint |
| `technicalIndicatorTools.ts` | 91-93 | RSI labels — separate sprint |
| `technicalIndicatorTools.ts` | 172-179 | MACD block — separate sprint |
| `supplyChainTools.ts` | 55,60,68,79,82-84,88,93 | section headers — separate sprint |

---

## Verification steps

```bash
# 1. TypeScript clean
bun tsc --noEmit

# 2. Task 1408 cases GREEN
bun test src/__tests__/1408-tool-diacritics.test.ts

# 3. Confirm kinhDichReading.ts BAT LOI key unchanged
grep "BAT LOI" src/domain/services/kinhDichReading.ts

# 4. Full suite regression
bun test
# Expected: 5055+ pass, 0 fail
```

---

## Branch + commit

```bash
git checkout -b task/1409-tool-diacritics-fix
# apply edits to three files
git add src/interface/mcp/tools/kinhDichTools.ts
git add src/interface/mcp/tools/technicalIndicatorTools.ts
git add src/interface/mcp/tools/supplyChainTools.ts
git commit -m "fix(tool-diacritics): replace unaccented strings in kinhDichTools + technicalIndicatorTools + supplyChainTools"
# merge to main, delete branch
```

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/kinhDichTools.ts   # added exportedformatKinhDichTradingContext(); replaced 10-line inline block with one-line call
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/technicalIndicatorTools.ts   # fixed 4 strings; added formatTaIndicatorReport export alias
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/supplyChainTools.ts   # fixed 4 strings (line 106 + lines 115/117/119)
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1408-tool-diacritics.test.ts   # removed @ts-expect-error directives (exports now exist)
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/285-kinhdich-tools.test.ts   # updated assertion from "Nhan dinh giao dich" to "Nhận định giao dịch"

tests_written:
- src/__tests__/1408-tool-diacritics.test.ts   # 8 assertions, all GREEN (was RED before this task)

tests_skipped: []

tsc_clean: true
full_suite_pass: true   # 5063 pass, 0 fail, 21 skip
