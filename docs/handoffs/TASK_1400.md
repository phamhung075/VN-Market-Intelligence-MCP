# Handoff: TASK 1400 — fix(db-isolation): Bun.env namespace + purge phantom rows

req_ref: REQ-1400
tech_ref: TECH-1400
status: READY_FOR_DEV

---

## [Architect] Brownfield Findings

interfaces_found:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/infrastructure/db/schema.ts:64`  # CORRECT — reads `Bun.env["DB_PATH"]`, no change needed
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/infrastructure/db/schema.ts:84`  # REUSE — `closeDb()` already exists for test teardown
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/setup.ts:12`           # BUG — `process.env` write, must change to `Bun.env`

interfaces_to_create:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1400-db-isolation.test.ts`  # NEW — TDD isolation proof
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/scripts/purge-phantom-reports.ts`         # NEW — one-shot purge op

decisions:
- "Purge delivered as standalone script (not schema.ts migration) — avoids executing DELETE on every server boot"
- "Dynamic import in test avoids singleton caching before env check"
- "No new interfaces needed — fix is purely a namespace mismatch correction"

brownfield_scan_clean: true

---

## Dev Instructions (task 1400)

### Step 1 — RED (write failing test first)

Create `src/__tests__/1400-db-isolation.test.ts` — see TECH-1400 Interface Contracts section. Run `bun test src/__tests__/1400-db-isolation.test.ts` — first assertion FAILS (Bun.env is undefined, not ":memory:").

### Step 2 — GREEN (fix setup.ts)

```
File: src/__tests__/setup.ts line 12
Before: process.env["DB_PATH"] = ":memory:";
After:  Bun.env["DB_PATH"] = ":memory:";
```

Re-run test — both assertions PASS.

### Step 3 — Fix dev-standards.md template

```
File: .claude/knowledge/dev-standards.md line 47
Before: process.env["DB_PATH"] = ":memory:";
After:  Bun.env["DB_PATH"] = ":memory:";
```

### Step 4 — Verify no remaining broken pattern

```bash
grep -r 'process\.env\["DB_PATH"\]' /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/
# Must return 0 matches
```

### Step 5 — Full suite regression

```bash
bun test
# Pass count >= 5061, 0 new failures
```

---

## Dev Instructions (task 1401 — purge)

Create `scripts/purge-phantom-reports.ts` — see TECH-1400. Run once:

```bash
bun scripts/purge-phantom-reports.ts
# Expected output: [purge] deleted NNN phantom rows (created_at < 1000000)
```

Verify:
```bash
# In production shell or via bun repl:
SELECT COUNT(*) FROM telegram_reports WHERE created_at < 1000000;
# Must return 0
```

---

## Acceptance Gate (QA checklist)

- [ ] AC-1: `Bun.env["DB_PATH"]` equals `":memory:"` during `bun test`
- [ ] AC-2: `SELECT COUNT(*) FROM telegram_reports WHERE created_at < 1000000` = 0 on production DB
- [ ] AC-3: `src/__tests__/1400-db-isolation.test.ts` both tests GREEN
- [ ] AC-4: `dev-standards.md:47` shows `Bun.env["DB_PATH"]`
- [ ] AC-5: full suite >= 5061 pass, 0 new failures
- [ ] `bun tsc --noEmit` clean
- [ ] `data/market.db` mtime unchanged after `bun test` run

---

## [QA] Review Record

verdict: APPROVED
blocking_issues: []
non_blocking: []

files_confirmed_clean:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/setup.ts
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1400-db-isolation.test.ts
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/scripts/purge-phantom-reports.ts
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/.claude/knowledge/dev-standards.md

merge_commit: 98ec2a0
