# Handoff: TASK-1415 — fix(diacritics-wave4): replace unaccented Vietnamese in 5 files

tech_ref: docs/TECH_1414.md
branch: task/1415-diacritics-wave4-fix
layer: interface
depends_on: 1414 RED merged to main
blocks: nothing

---

## Pre-condition

Task 1414 branch merged. `bun test src/__tests__/1414-diacritics-wave4.test.ts` is RED on main.

---

## [Architect] Brownfield Findings

(Inherited from TASK_1414.md — no re-scan needed.)

brownfield_scan_clean: true

decisions:
- "Fix only the 13 strings listed. Do NOT fix other unaccented strings in compareTools.ts (e.g. 'Chi tieu', 'Gia', 'Thay doi', 'Xac tin', 'Cap nhat') — out of scope per REQ-1414."
- "supplyChainTools.ts: fix all 7 strings in the event block (lines 79-88), including the 5 adjacent lines the BA flagged as out-of-scope. Architect decision in TECH-1414 expands this."
- "Do NOT touch template variable names, function signatures, or the `buildRow` function body."

---

## Exact string replacements — per file

Apply each replacement exactly. Use editor search (not regex) to avoid accidental collisions.

### FILE 1: `src/interface/mcp/tools/kinhDichTools.ts`

7 replacements:

| Old (unaccented) | New (accented) |
|-----------------|----------------|
| `` `${ts} \| Que ${r.hexagramNumber} ${queName} ${chinese} \| Tin hieu: ${r.tradingSignal} \| Do tin cay: ${confPct}%` `` | `` `${ts} \| Quẻ ${r.hexagramNumber} ${queName} ${chinese} \| Tín hiệu: ${r.tradingSignal} \| Độ tin cậy: ${confPct}%` `` |
| `` `Co phieu: ${code}` `` | `` `Cổ phiếu: ${code}` `` |
| `` `Loi: Khong co du lieu giai thich cho Que ${number} (${meta.name}).` `` | `` `Lỗi: Không có dữ liệu giải thích cho Quẻ ${number} (${meta.name}).` `` |
| `` `Tinh trang que:` `` | `` `Tình trạng quẻ:` `` |
| `` `  Xu huong: ${data.state.trend}` `` | `` `  Xu hướng: ${data.state.trend}` `` |
| `` `  Canh bao: ${data.state.warning}` `` | `` `  Cảnh báo: ${data.state.warning}` `` |
| `"6 Hao (tung duong):"` | `"6 Hào (từng đường):"` |

**Guard:** line 729 `` `Que ${r.hexagramNumber}` `` (in `queName` fallback) is NOT in scope — do not touch.
**Guard:** line 740 (`Que pho bien nhat:`) is NOT in scope — out of scope per REQ-1414.

### FILE 2: `src/interface/mcp/tools/supplyChainTools.ts`

7 replacements (full event block, lines 79-88):

| Old (unaccented) | New (accented) |
|-----------------|----------------|
| `` `\nSU KIEN GIAN DOAN [${sevMap[event.severity] ?? event.severity.toUpperCase()}]:` `` | `` `\nSỰ KIỆN GIÁN ĐOẠN [${sevMap[event.severity] ?? event.severity.toUpperCase()}]:` `` |
| `` `  Loai: ${event.eventType}` `` | `` `  Loại: ${event.eventType}` `` |
| `` `  Mo ta: ${event.description}` `` | `` `  Mô tả: ${event.description}` `` |
| `` `  Tuyen duong bi anh huong: ${event.affectedRoutes.join(", ")}` `` | `` `  Tuyến đường bị ảnh hưởng: ${event.affectedRoutes.join(", ")}` `` |
| `` `  Co phieu bi anh huong: ${event.affectedStocks.join(", ")}` `` | `` `  Cổ phiếu bị ảnh hưởng: ${event.affectedStocks.join(", ")}` `` |
| `` `  Do tin cay: ${Math.round(event.confidence * 100)}%` `` | `` `  Độ tin cậy: ${Math.round(event.confidence * 100)}%` `` |
| `"\nSu kien gian doan: Khong phat hien su kien bat thuong"` | `"\nSự kiện gián đoạn: Không phát hiện sự kiện bất thường"` |

### FILE 3: `src/interface/mcp/tools/alertMuteTools.ts`

1 replacement (line 142):

| Old (unaccented) | New (accented) |
|-----------------|----------------|
| `` `${code}: lenh tat tieng da het han (${formatViDate(wasUntil)}). Canh bao da duoc bat lai.` `` | `` `${code}: lệnh tắt tiếng đã hết hạn (${formatViDate(wasUntil)}). Cảnh báo đã được bật lại.` `` |

**Guard:** line 146 (`` `${code} da duoc bat lai canh bao...` ``) is NOT in scope — do not touch.

### FILE 4: `src/interface/mcp/tools/watchlist.ts`

2 replacements (lines 286-287):

| Old (unaccented) | New (accented) |
|-----------------|----------------|
| `` `Da them ${actionCode} (${exchange}) vao danh sach theo doi.\n` `` | `` `Đã thêm ${actionCode} (${exchange}) vào danh sách theo dõi.\n` `` |
| `` `Canh bao: giam ${dropPct}% \| tang +${risePct}% \| impact >= ${impactScore}/10` `` | `` `Cảnh báo: giảm ${dropPct}% \| tăng +${risePct}% \| impact >= ${impactScore}/10` `` |

**Guard:** the `+` string concatenation operator between lines 286 and 287 must not change.
**Guard:** `\n` at end of line 286 must be preserved exactly.
**Guard:** `| impact >= ${impactScore}/10` tail of line 287 is already correct — do not change.

### FILE 5: `src/interface/mcp/tools/compareTools.ts`

1 replacement (line 386):

| Old (unaccented) | New (accented) |
|-----------------|----------------|
| `buildRow("Canh bao (7d)", alertVals)` | `buildRow("Cảnh báo (7d)", alertVals)` |

**Guard:** only this one `buildRow` call changes. Do NOT touch `buildRow("Chi tieu", codes)`, `buildRow("Gia", ...)`, `buildRow("Thay doi", ...)`, `buildRow("Xac tin", ...)`, or `buildRow("Cap nhat:", ...)` — those are out of scope.

---

## Verification steps (ordered)

```bash
# 1. Type-check
bun tsc --noEmit

# 2. Wave 4 test — must go GREEN
bun test src/__tests__/1414-diacritics-wave4.test.ts

# 3. Regression — prior waves must stay GREEN
bun test src/__tests__/1408-tool-diacritics.test.ts
bun test src/__tests__/1410-tool-diacritics-sweep.test.ts
bun test src/__tests__/1412-diacritics-wave3.test.ts

# 4. Full suite
bun test
```

Expected outcome:
- `bun tsc --noEmit` → 0 errors
- wave 4 test → all GREEN (13+ pass, 0 fail)
- prior wave tests → unchanged GREEN
- full suite → 0 fail, count >= 5090

---

## Acceptance Criteria

- AC-1: `bun test src/__tests__/1414-diacritics-wave4.test.ts` → all GREEN
- AC-2: `bun tsc --noEmit` → 0 errors
- AC-3: 1408 + 1410 + 1412 test files → no new failures
- AC-4: No logic changes — template variable names, function signatures, buildRow body all unchanged

---

## Branch hygiene

```bash
git checkout -b task/1415-diacritics-wave4-fix
# apply 13 string replacements across 5 files
bun tsc --noEmit
bun test src/__tests__/1414-diacritics-wave4.test.ts
bun test src/__tests__/1408-tool-diacritics.test.ts
bun test src/__tests__/1410-tool-diacritics-sweep.test.ts
bun test src/__tests__/1412-diacritics-wave3.test.ts
bun test
git add src/interface/mcp/tools/kinhDichTools.ts \
        src/interface/mcp/tools/supplyChainTools.ts \
        src/interface/mcp/tools/alertMuteTools.ts \
        src/interface/mcp/tools/watchlist.ts \
        src/interface/mcp/tools/compareTools.ts
git commit -m "fix(diacritics-wave4): replace unaccented Vietnamese in 5 interface/mcp/tools files"
# open PR → QA review → merge → checkout main → delete branch
```

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/kinhDichTools.ts   # 7 strings: Quẻ, Tín hiệu, Độ tin cậy, Cổ phiếu, Lỗi/Không có/Quẻ, Tình trạng quẻ, Xu hướng, Cảnh báo, 6 Hào
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/supplyChainTools.ts   # 7 strings: SỰ KIỆN GIÁN ĐOẠN, Loại, Mô tả, Tuyến đường bị ảnh hưởng, Cổ phiếu bị ảnh hưởng, Độ tin cậy, Sự kiện gián đoạn null path
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/alertMuteTools.ts   # 1 string: lệnh tắt tiếng đã hết hạn + Cảnh báo đã được bật lại
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/watchlist.ts   # 2 strings: Đã thêm/vào danh sách theo dõi + Cảnh báo: giảm/tăng
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/compareTools.ts   # 1 string: Cảnh báo (7d)
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/217-compare-stocks.test.ts   # updated 3 regex assertions from /Canh bao/i to /C[aả]nh b[aá]o/i to accept accented output

tests_written:
- (inherited from 1414)

tests_skipped: []

tsc_clean: true
full_suite_pass: true (5149 pass, 0 fail)
