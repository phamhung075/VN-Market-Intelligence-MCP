# TASK_1360 — Morning Briefing Bug Fixes (title ellipsis + delta arrows)

## Status: IMPL COMPLETE — ready for QA

## Commit
`7ba2a624` — chore(1360): sprint complete — 40 domain unit tests merged, baseline 7865

All four source changes landed in this commit on `main`.

---

## Bug 1 — News titles cut mid-word (FIXED)

**File**: `apps/mcp-server/src/scheduler/briefings/morningBriefingJob.ts` line 124

**Before**: `s.title.slice(0, 70)` — cut at 70 chars, no ellipsis

**After**: `TelegramMessageFactory.formatStoryTitle(s.title)` — smart word-boundary truncation at 100 chars with `…`

---

## Bug 2 — Delta arrows for global market values and commodities (FIXED)

### Step A — GlobalSnapshot interface (`assembleBriefing.ts` line ~49)

Added optional fields: `prevVix`, `prevDxy`, `prevSp500`, `prevHangSeng` to `GlobalSnapshot` interface.

### Step B — Query previous commodity_prices row (`assembleBriefing.ts` Step 19)

Added `OFFSET 1` query for `cpRowPrev`. Spread into `globalSnapshot` when present.

### Step C — `previousValue` on commodity items (`assembleBriefing.ts` Step 9)

Imported `getIndicatorHistory` from `commodityTracker.ts`. For each tracked indicator, fetches last 2 values (DESC). `history[1].value` = previousValue when ≥2 rows exist.

Updated `DailyBriefing.trackedCommodities` type to include `previousValue?: number`.

### Step D — Formatters with delta arrows (`morningBriefingJob.ts`)

Added private helper `deltaArrow(current, prev)` → `" ↑"` / `" ↓"` / `""`.

Updated `formatGlobalSnapshotSection` signature to accept `prev*` fields; all 4 metrics now append arrow.

Updated `formatCommoditiesSection` signature to accept `previousValue?`; commodity lines append arrow.

---

## Tests

### `1511-morning-briefing-global-snapshot.test.ts`

Added: AC-6 (↑ arrows), AC-7 (↓ arrows), AC-8 (no arrow when prev absent), AC-9 (assembleBriefing populates prev* with 2 rows in DB).

### `1434-morning-briefing-commodity-values.test.ts`

Added: T5 (↑ arrow), T6 (↓ arrow), T7 (no arrow when previousValue absent). Also added `formatCommoditiesSection` import.

**Result**: 16 tests / 2 files — 16 pass, 0 fail.

---

## QA Checklist

- [ ] Run `bun test src/__tests__/1511-morning-briefing-global-snapshot.test.ts src/__tests__/1434-morning-briefing-commodity-values.test.ts` — expect 16 pass
- [ ] Run full `bun test` — expect no regressions vs baseline 7865
- [ ] Verify `formatStoryTitle` is used (grep confirms line 124 in morningBriefingJob.ts)
- [ ] Verify `deltaArrow` is present (grep confirms function defined at line 35)
- [ ] Verify `prevVix` etc. in GlobalSnapshot interface (assembleBriefing.ts line ~56)

---

## Files Changed

- `apps/mcp-server/src/scheduler/briefings/morningBriefingJob.ts`
- `apps/mcp-server/src/application/usecases/assembleBriefing.ts`
- `apps/mcp-server/src/__tests__/1511-morning-briefing-global-snapshot.test.ts`
- `apps/mcp-server/src/__tests__/1434-morning-briefing-commodity-values.test.ts`
