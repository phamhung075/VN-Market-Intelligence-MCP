# TASK_1877e-3 — C2-Exempt Commit Categories Table

**Sprint:** 1877e | **Type:** SPRINT-M | **Estimate:** ~0.5h

---

## Brief

Add a new **C2-Exempt Commit Categories** section to `.claude/knowledge/commit-convention.md` after the existing C3-Exempt table. Document the 4 commit patterns that are excluded from C2 denominator:
1. `chore(cycle-NN)` — cycle artifact persist, not sprint task
2. `chore(pm/cNN)` — cycle reference bookkeeping
3. `chore(pm/NNNN*)` — sprint-scoped PM housekeeping (decompose, move-to-Done)
4. Sprint-scoped chore subject containing `merge task/` — AC lives on the feat/fix commit

This table serves as the **SSOT for all C2 exemption logic** — audit script (1877e-1) and flow documentation (1877e-2) both reference it.

---

## Files

- **Target:** `.claude/knowledge/commit-convention.md` (after line 112, end of C3-Exempt table)
- **Reference:** docs/architecture-briefs/2026-05-17-c2-task-trailer-gap.md § 3 (exact literal patterns)
- **Architecture:** `.claude/knowledge/` is shared knowledge tree per tree-map.md

---

## Acceptance Criteria

- **AC-1** — `.claude/knowledge/commit-convention.md` contains new **## C2-Exempt Commit Categories** heading (level 2, after C3 section).
- **AC-2** — Section includes a markdown table with 3 columns: Pattern | Example | Reason.
- **AC-3** — 4 rows in table:
  - Row 1: `chore(cycle-NN): ...` | `chore(cycle-28): persist 1872a artifacts` | Digit is cycle number, not sprint ID
  - Row 2: `chore(pm/cNN): ...` | `chore(pm/c26): add Done rows from TNB c36` | Digit is cycle reference
  - Row 3: `chore(pm/NNNN*): ...` | `chore(pm/1862c): decompose RCA brief` | PM sprint bookkeeping, no code delivery
  - Row 4: Sprint-scoped chore containing `merge task/` | `chore(1869/mcp-server): merge task/1869b-...` | AC lives on the feat/fix commit
- **AC-4** — No other sections modified; file remains logically coherent (C1/C2/C3/C4 criteria explained, then exemptions below).
- **AC-5** — Markdown syntax clean: `markdown-lint` passes, no broken links.

---

## Implementation

**Patch site:** After line 112 (end of C3-Exempt table). This is the end of the "## C3-Exempt Commit Categories" section.

**Insert:**

```markdown
## C2-Exempt Commit Categories

These commit types are excluded from the C2 denominator — they contain a digit in scope but do not deliver sprint tasks:

| Pattern | Example | Reason |
|---|---|---|
| `chore(cycle-NN): ...` | `chore(cycle-28): persist 1872a artifacts` | Digit is cycle number, not sprint ID |
| `chore(pm/cNN): ...` | `chore(pm/c26): add Done rows from TNB c36` | Digit is cycle reference |
| `chore(pm/NNNN*): ...` | `chore(pm/1862c): decompose RCA brief` | PM sprint bookkeeping, no code delivery |
| Sprint-scoped chore containing `merge task/` | `chore(1869/mcp-server): merge task/1869b-...` | AC lives on the feat/fix commit |
```

**Delta:** +12 LOC (heading + blank line + intro paragraph + table header + 4 rows).

---

## Commit Convention

This task is a **knowledge/documentation update** (no code delivery). Knowledge files document process, not product features. Commit style: `chore(docs)` or `chore(.claude/knowledge)` with no Task trailer (documentation-only exemption).

**Example:**

```bash
git add .claude/knowledge/commit-convention.md
git commit -m "docs(.claude/knowledge): add C2-Exempt Commit Categories table

4 patterns excluded from C2 denominator: cycle-NN, pm/cNN, 
pm/NNNN* (sprint), and merge-task subject. SSOT for audit script 
(1877e-1) and flow documentation (1877e-2).

AC: AC-1, AC-2, AC-3, AC-4, AC-5"
```

**Note:** No `Sprint:` or `Task:` trailers — knowledge updates are inherently C2-exempt (different category from chore(pm/NNNN)).

**Path-restricted commit** (cycle 32 race lesson):
```bash
git commit -- .claude/knowledge/commit-convention.md
```

---

## Testing

1. Verify file syntax: `cat .claude/knowledge/commit-convention.md | grep -A 20 "C2-Exempt"` — table should be visible.
2. Check markdown: `markdown-lint .claude/knowledge/commit-convention.md` (if available) or manual review for broken links.
3. Spot-check patterns in table:
   - Row 1: `cycle-28` example matches brief SHAs 234a69b3 ✓
   - Row 2: `pm/c26` example matches brief SHAs 6f02aed1 ✓
   - Row 3: `pm/1862c` example matches brief SHAs e6d37aa7 ✓
   - Row 4: `merge task/` pattern matches brief SHAs c7545b9a (subject: "merge task/1869b-...") ✓
4. Verify heading structure: ## C2-Exempt should appear after ## C3-Exempt (section ordering preserved).

---

## Risk

**R1 — Over-documentation:** The C2 logic is already baked into audit script (1877e-1) via case statements. This table is reference docs, not code. If table and code diverge, code is SSOT. Mitigation: update both (1877e-1 and 1877e-3) in same sprint; link from each to the other.

**R2 — Table not enforced:** Developers may ignore the table in knowledge file. Mitigation: PM reviews each commit; flow reminders (1877e-2) reinforce the categories. Table is SSOT discovery, not replacement for code logic.

---

## Blockers

**Depends on:** None — documentation only.

**Enables:** 1877e-1 (audit script) and 1877e-2 (flow tightening) both reference this table. Can be written in parallel, but SSOT should be created first (1877e-3 before shipping 1877e-1/2).

---

## Session Notes

(Cycle 34: PM decomposition) Knowledge file replaces the informal exemption patterns scattered across commits into a single reference table. Brief §3 provided exact literal patterns; brief §5 provided AC-3 row content. All 4 rows are taken verbatim from brief and confirmed against live commit SHAs. Cross-links between 1877e-1 audit script, 1877e-2 flow docs, and 1877e-3 knowledge SSOT complete the "exemptions + flow tightening + knowledge doc" strategy.
