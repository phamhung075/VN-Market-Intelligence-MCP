# TASK 1805b-4 — QA: verify chain_catalyst signal fires alert-commander correctly

## Sprint
1805b — chain_catalyst firing matrix

## Wave
Wave 2 (after 1805b-1 and 1805b-2 are merged)

## Context
Tasks 1805b-1 and 1805b-2 updated the alert-commander cycle flow and agent
metadata to handle `chain_catalyst` signals. This QA task verifies the
end-to-end firing logic is correct by reading all edited files and tracing
a synthetic signal through the documented rules.

## Verification Steps

### A. Schema consistency check
1. Read `.claude/flows/alert-commander/cycle.md`
2. Read `.claude/agents/alert-commander.md`
3. Read `.claude/flows/news-scout/cycle.md`
4. Confirm:
   - alert-commander cycle Step 3 matrix has `chain_catalyst` row
   - alert-commander cycle Step 3c block is present and complete
   - alert-commander.md `signals.consumes` includes `chain_catalyst`
   - alert-commander.md `inter_agent.receives_from` has news-scout chain_catalyst entry
   - news-scout chain_catalyst `finding_data` has no `regime` or `regime_adjusted_score` keys

### B. Synthetic signal trace — BEARISH (NEUTRAL regime)

Inject (mentally trace, no real tool call required):
```json
{
  "signal_type": "chain_catalyst",
  "from_agent": "news-scout",
  "finding_data": {
    "event_type": "trade_war",
    "direction": "bearish",
    "confidence": 0.82,
    "affected_stocks": ["VNM"],
    "affected_sectors": ["consumer_goods"]
  },
  "payload": { "impact_score": 8 }
}
```

Expected trace per Step 3c:
- confidence 0.82 ≥ NEUTRAL threshold 0.75 → proceed
- direction=bearish → CRITICAL alert (position-danger)
- Step 4a caveat: `"Cảnh báo: Xúc tác tiêu cực..."` appended
- Log: `"[ChainCatalyst] VNM event=trade_war dir=bearish conf=0.82 → CRITICAL"`

### C. Synthetic signal trace — BULLISH earnings (TIGHTENING regime)

```json
{
  "signal_type": "chain_catalyst",
  "from_agent": "news-scout",
  "finding_data": {
    "event_type": "earnings",
    "direction": "bullish",
    "confidence": 0.84,
    "affected_stocks": ["FPT"]
  }
}
```

Expected trace:
- TIGHTENING threshold = 0.85, confidence 0.84 < 0.85 → suppressed
- Log: `"[ChainCatalyst] FPT event=earnings dir=bullish conf=0.84 → suppressed"`
- Step 4a TIGHTENING caveat would have applied if fired

### D. Synthetic signal trace — BULLISH earnings at threshold (TIGHTENING)

Same as C but confidence = 0.86:
- 0.86 ≥ 0.85 → MARKET alert
- Regime caveat appended: `"Lưu ý: Xúc tác chuỗi trong môi trường thắt chặt..."`

### E. news-scout chain_catalyst payload.detail check

Verify Step 3 in news-scout cycle.md: the `payload.detail` template now
includes `regime=<REGIME> regime_adj_score=<N>` inline, and `finding_data`
does NOT contain `regime`/`regime_adjusted_score`.

## Acceptance Criteria
- [ ] All 5 schema checks in Step A pass
- [ ] Trace B: bearish 0.82 → CRITICAL confirmed
- [ ] Trace C: bullish 0.84 TIGHTENING → suppressed confirmed
- [ ] Trace D: bullish 0.86 TIGHTENING → MARKET + caveat confirmed
- [ ] Trace E: news-scout finding_data clean, regime in payload.detail
- [ ] No issues found → post WORK: `"[QA-1805b] chain_catalyst matrix verified — all traces pass"`
- [ ] Issues found → post BUG: `"[QA-1805b] FAIL: <specific mismatch>"` and mark task blocked

## Dependencies
- 1805b-1 merged (alert-commander cycle.md updated)
- 1805b-2 merged (alert-commander.md updated)
- 1805b-3 merged (news-scout cycle.md updated)

## Owner
qa

## Estimated effort
~1h
