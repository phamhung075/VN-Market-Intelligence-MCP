# Handoff: Tasks 1383 + 1384 — France Summary Message Quality

sprint: 135
date: 2026-04-17
status: ready-for-dev

## Context

`franceSummaryJob.ts` → `formatFranceSummaryVI()` sends filler lines to the MARKET channel when a data section is empty:
- "Khong co du lieu gia." (movers empty)
- "Khong co canh bao." (alerts empty)
- "Khong co tin hieu ky thuat" (TA signals empty)

Per message-quality audit rule: empty-section filler = spam. Omit the section entirely.

Also: all Vietnamese text uses unaccented ASCII ("qua mua", "gia tren MA20") — poor UX for Vietnamese-language product.

## Task 1383 — TDD test (RED first)

file: `src/__tests__/1383-france-summary-message-quality.test.ts`
branch: `task/1383-france-msg-quality-tdd`

Test cases (minimum 6):
1. movers=0, alerts=2, ta=0 → message has alerts section, NO "Khong co" anywhere
2. movers=3, alerts=0, ta=0 → message has movers section, no alerts/ta section lines
3. movers=0, alerts=0, ta=2 → message has TA section only
4. movers=2, alerts=1, ta=1 → all 3 sections present
5. Vietnamese diacritics present: "quá mua", "quá bán", "giá trên MA20", "giá dưới MA20", "Bản tin", "Pháp"
6. `bun tsc --noEmit` clean

Run: `bun test src/__tests__/1383-france-summary-message-quality.test.ts` → all FAIL (RED)

## Task 1384 — Fix (GREEN)

file: `src/scheduler/franceSummaryJob.ts`
branch: `task/1384-france-msg-quality-fix` (branch off 1383 after merge)

Changes to `formatFranceSummaryVI`:

**Section 1 (movers):** remove the `else { lines.push("Khong co du lieu gia.") }` branch. If `movers.length === 0`, skip entire section (no header, no lines).

**Section 2 (alerts):** remove the `else { lines.push("Khong co canh bao.") }` branch. If `alerts.length === 0`, skip entire section.

**Section 3 (TA signals):** remove the `else { lines.push("Khong co tin hieu ky thuat") }` branch. If `signals.length === 0`, skip entire section.

**Diacritics — replace these strings:**
| Old | New |
|-----|-----|
| `"Ban tin sang Phap"` | `"Bản tin sáng Pháp"` |
| `"Top bien dong gia"` | `"Top biến động giá"` |
| `"Canh bao gan nhat"` | `"Cảnh báo gần nhất"` |
| `"Tin hieu ky thuat"` | `"Tín hiệu kỹ thuật"` |
| `"qua mua"` | `"quá mua"` |
| `"qua ban"` | `"quá bán"` |
| `"gia tren MA20"` | `"giá trên MA20"` |
| `"gia duoi MA20"` | `"giá dưới MA20"` |
| `"Khong co du lieu gia."` | (deleted) |
| `"Khong co canh bao."` | (deleted) |
| `"Khong co tin hieu ky thuat"` | (deleted) |

Severity labels in `severityLabel()`:
| Old | New |
|-----|-----|
| `"NGHIEM TRONG"` | `"NGHIÊM TRỌNG"` |
| `"CANH BAO"` | `"CẢNH BÁO"` |
| `"THONG TIN"` | `"THÔNG TIN"` |

acceptance_criteria:
- `bun test src/__tests__/1383-france-summary-message-quality.test.ts` → all PASS
- `bun test` full suite → 5010+ pass, 0 fail, 21+ skip
- `bun tsc --noEmit` → 0 errors
- grep "Khong co" src/scheduler/franceSummaryJob.ts → 0 matches
- Message with movers=[]+alerts=[2]+ta=[] has NO blank/filler section lines

## Files to modify

| File | Action |
|------|--------|
| `src/__tests__/1383-france-summary-message-quality.test.ts` | CREATE (task 1383) |
| `src/scheduler/franceSummaryJob.ts` | EDIT `formatFranceSummaryVI` only (task 1384) |

## DDD layer

`franceSummaryJob.ts` = interface/scheduler layer. Formatting function is pure (no DB calls) — safe to edit without side effects on query logic.

---

## [Architect] Brownfield Findings

interfaces_found:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/scheduler/franceSummaryJob.ts`   # REUSE — formatFranceSummaryVI, severityLabel, rsiLabel, ma20Label already exist; modify in-place only

interfaces_to_create:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1383-france-summary-message-quality.test.ts`   # NEW — TDD RED test

decisions:
- "No new interfaces or domain objects needed — all changes are string-literal and control-flow edits inside formatFranceSummaryVI and its three private helpers."
- "Section-omit logic: build blocks[] per non-empty section, join with \\n\\n — eliminates trailing blank line and any filler."
- "Legacy number overload guard Array.isArray(taSignals) preserved — no call-site breakage."

brownfield_scan_clean: true
tech_design: docs/TECH_135.md

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1383-france-summary-message-quality.test.ts   # CREATED — 8 RED tests covering filler omission + diacritics

tests_written:
- src/__tests__/1383-france-summary-message-quality.test.ts   # 8 tests, 7 FAIL (RED) + 1 pass (T6 callable check)

tests_skipped: []

tsc_clean: true
full_suite_pass: N/A   # RED phase — task tests intentionally fail; full suite not run (no code changes)

notes:
- T1–T4: filler-omission assertions (no "Khong co" in any output)
- T5: all diacritics checked — headers, signal labels, severity labels
- T6: callable/return-type guard (passes since fn exists, just returns wrong strings)
- T7: all-empty case — title present, no filler
- T8: old unaccented ASCII strings absent from full output
