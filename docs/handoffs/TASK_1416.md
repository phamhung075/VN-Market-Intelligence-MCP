# TASK-1416 — test(diacritics-wave5): RED test

tech_ref: TECH-1416
sprint: 148
role: Dev
depends_on: TECH-1416 approved

## Objective

Write `src/__tests__/1416-diacritics-wave5.test.ts`. All assertions must be RED (failing) before task 1417 fixes source files.

## Test file contract

File: `src/__tests__/1416-diacritics-wave5.test.ts`
Header line 1: `process.env["DB_PATH"] = ":memory:";`

### Group A — direct-call tests (pure functions, no DB)

Import and call these exported functions; assert accented return values.

**dataFreshnessTools.ts** — `classifyFreshness`, `formatAge`

```typescript
import { classifyFreshness, formatAge } from "../interface/mcp/tools/dataFreshnessTools.js";
```

| it() description | call | assertion |
|---|---|---|
| `classifyFreshness(0) returns "Tốt"` | `classifyFreshness(0)` | `toBe("Tốt")` |
| `classifyFreshness(0.99) returns "Tốt"` | `classifyFreshness(0.99)` | `toBe("Tốt")` |
| `classifyFreshness(1) returns "Bình thường"` | `classifyFreshness(1)` | `toBe("Bình thường")` |
| `classifyFreshness(6) returns "Cũ"` | `classifyFreshness(6)` | `toBe("Cũ")` |
| `classifyFreshness(24) returns "Rất cũ"` | `classifyFreshness(24)` | `toBe("Rất cũ")` |
| `classifyFreshness(null) returns "Chưa có dữ liệu"` | `classifyFreshness(null)` | `toBe("Chưa có dữ liệu")` |
| `formatAge(0.25) contains "phút"` | `formatAge(0.25)` | `toContain("phút")` |
| `formatAge(48) contains "ngày"` | `formatAge(48)` | `toContain("ngày")` |

**sourceHealthTools.ts** — `formatSourceHealthTable`

```typescript
import { formatSourceHealthTable } from "../interface/mcp/tools/sourceHealthTools.js";
```

| it() description | call | assertion |
|---|---|---|
| `formatSourceHealthTable([]) contains "Chưa có dữ liệu"` | `formatSourceHealthTable([])` | `toContain("Chưa có dữ liệu")` |

**stockSearch.ts** — `formatSearchResults`

```typescript
import { formatSearchResults } from "../domain/services/stockSearch.js";
```

| it() description | call | assertion |
|---|---|---|
| `formatSearchResults empty → "Không tìm thấy kết quả nào"` | `formatSearchResults("xyz", [])` | `toContain("Không tìm thấy kết quả nào")` |

**technicalIndicatorTools.ts** — `formatTaIndicatorReport`

```typescript
import { formatTaIndicatorReport } from "../interface/mcp/tools/technicalIndicatorTools.js";
// helper:
function flatCandles(n: number) {
  return Array.from({ length: n }, (_, i) => ({ day: `2026-01-${String(i+1).padStart(2,"0")}`, close: 100 }));
}
```

| it() description | call | assertion |
|---|---|---|
| `formatTaIndicatorReport <35 candles → "Không đủ dữ liệu kỹ thuật"` | `(formatTaIndicatorReport as any)("VCB", flatCandles(10), 60)` | `toContain("Không đủ dữ liệu kỹ thuật")` |
| `formatTaIndicatorReport <35 candles → "cần tối thiểu 35 cho MACD"` | same call | `toContain("cần tối thiểu 35 cho MACD")` |

### Group B — source-scan tests (readFileSync)

```typescript
import { readFileSync } from "fs";
import { join } from "path";
const TOOLS_DIR = join(import.meta.dir, "../interface/mcp/tools");
function readTool(f: string) { return readFileSync(join(TOOLS_DIR, f), "utf8"); }
const DOMAIN_DIR = join(import.meta.dir, "../domain/services");
function readDomain(f: string) { return readFileSync(join(DOMAIN_DIR, f), "utf8"); }
```

**alertMuteTools.ts** (6 strings)

```typescript
const src = readTool("alertMuteTools.ts");
expect(src).toContain("đã được tắt tiếng trong");
expect(src).toContain("Lý do:");
expect(src).toContain("Để bật lại cảnh báo");
expect(src).toContain("hiện không bị tắt tiếng cảnh báo");
expect(src).toContain("đã được bật lại cảnh báo");
expect(src).toContain("Lỗi khi tắt tiếng");
// negative guards
expect(src).not.toContain("da duoc tat tieng");
expect(src).not.toContain("Ly do:");
expect(src).not.toContain("De bat lai canh bao");
```

**climateTools.ts** (1 string)

```typescript
const src = readTool("climateTools.ts");
expect(src).toContain("Lọc: ${opts.stock}");
expect(src).not.toContain("Loc: ${opts.stock}");
```

**compareTools.ts** (9 strings)

```typescript
const src = readTool("compareTools.ts");
expect(src).toContain("Lỗi: Cần ít nhất 2 mã cổ phiếu");
expect(src).toContain("Lỗi: Tối đa 5 mã cổ phiếu");
expect(src).toContain("So sánh cổ phiếu (");
expect(src).toContain('"Chỉ tiêu"');
expect(src).toContain('"Giá"');
expect(src).toContain('"Thay đổi"');
expect(src).toContain('"Xác tín"');
expect(src).toContain("Cập nhật:");
expect(src).toContain("Lỗi khi so sánh cổ phiếu:");
// negative guards
expect(src).not.toContain("Can it nhat 2 ma co phieu");
expect(src).not.toContain("So sanh co phieu");
expect(src).not.toContain('"Chi tieu"');
```

**dataFreshnessTools.ts** — source-scan supplement (labels + header columns)

```typescript
const src = readTool("dataFreshnessTools.ts");
expect(src).toContain('"Tin tức (RSS)"');
expect(src).toContain('"Giá cổ phiếu"');
expect(src).toContain('"Hàng hóa"');
expect(src).toContain('"Tỷ giá SBV"');
expect(src).toContain('"Dự đoán (Poly)"');
expect(src).toContain('"Độ tuổi dữ liệu"');
expect(src).toContain('"Nguồn"');
expect(src).toContain('"Cập nhật cuối"');
expect(src).toContain('"Tuổi"');
expect(src).toContain('"Trạng thái"');
expect(src).toContain('"v Tốt"');
expect(src).toContain('"~ Bình thường"');
expect(src).toContain('"! Cũ"');
expect(src).toContain('"!! Rất cũ"');
expect(src).toContain('"- Chưa có dữ liệu"');
expect(src).toContain("Kiểm tra lúc:");
// negative guards
expect(src).not.toContain('"Tin tuc (RSS)"');
expect(src).not.toContain('"Gia co phieu"');
expect(src).not.toContain('"Do tuoi du lieu"');
expect(src).not.toContain('"Kiem tra luc');
```

**earningsCalendarTools.ts** (10 strings)

```typescript
const src = readTool("earningsCalendarTools.ts");
expect(src).toContain('"ĐÃ NỘP"');
expect(src).toContain('"QUÁ HẠN"');
expect(src).toContain('"SẮP ĐẾN"');
expect(src).toContain('"(ước tính)"');
expect(src).toContain("Danh sách theo dõi trống");
expect(src).toContain("LỊCH NỘP BCTC");
expect(src).toContain("Ngày tham chiếu:");
expect(src).toContain("CỔ PHIẾU");
expect(src).toContain("cổ phiếu`");
expect(src).toContain("Lỗi khi lấy lịch nộp BCTC");
// negative guards
expect(src).not.toContain('"DA NOP"');
expect(src).not.toContain('"QUA HAN"');
expect(src).not.toContain('"SAP DEN"');
expect(src).not.toContain('"(uoc tinh)"');
```

**kinhDichTools.ts** (28 strings — representative subset, positive+negative)

```typescript
const src = readTool("kinhDichTools.ts");
// Error strings
expect(src).toContain("không có trong watchlist. Thêm cổ phiếu trước khi đọc Kinh Dịch");
expect(src).toContain("Lỗi khi đọc Kinh Dịch cho");
expect(src).toContain("Lỗi khi tính quẻ thị trường");
// History
expect(src).toContain("LỊCH SỬ KINH DỊCH:");
expect(src).toContain("Tổng số lần đọc:");
expect(src).toContain("Quẻ phổ biến nhất:");
// Transition
expect(src).toContain("XÁC SUẤT CHUYỂN QUẺ:");
expect(src).toContain("Xác suất chuyển sang (top 10):");
expect(src).toContain("Thay đổi TB:");
expect(src).toContain("Tỷ lệ thắng:");
// Backtest
expect(src).toContain("BACKTEST KINH DỊCH:");
expect(src).toContain("Độ chính xác (BUY/SELL):");
expect(src).toContain("Lợi nhuận TB 5 phiên:");
expect(src).toContain("Quẻ tốt nhất:");
expect(src).toContain("Quẻ xấu nhất:");
expect(src).toContain("Lưu ý: Backtest chỉ mang tính tham khảo");
// Hexagram detail
expect(src).toContain("Thượng quán (trên):");
expect(src).toContain("Ý nghĩa chính:");
expect(src).toContain("Hào từ (Phán quyết):");
expect(src).toContain("Tượng truyện (Hình tượng):");
expect(src).toContain("Hành động:");
expect(src).toContain("Sự nghiệp:");
expect(src).toContain("Kết quả:");
// negative guards
expect(src).not.toContain("khong co trong watchlist");
expect(src).not.toContain("Loi khi doc Kinh Dich");
expect(src).not.toContain("LICH SU KINH DICH");
expect(src).not.toContain("XAC SUAT CHUYEN QUE");
expect(src).not.toContain("BACKTEST KINH DICH");
expect(src).not.toContain("Do chinh xac");
expect(src).not.toContain("Loi nhuan TB");
expect(src).not.toContain("Ty le thang");
// Wave4-already-fixed lines must still be present (do-not-break guard)
expect(src).toContain("| Quẻ ${r.hexagramNumber}");
expect(src).toContain("`Cổ phiếu: ${code}`");
```

**pharmaTools.ts** (1 string)

```typescript
const src = readTool("pharmaTools.ts");
expect(src).toContain("Lọc theo cổ phiếu:");
expect(src).not.toContain("Loc theo co phieu:");
```

**portfolioTools.ts** (2 strings)

```typescript
const src = readTool("portfolioTools.ts");
expect(src).toContain("Watchlist trống");
expect(src).toContain("Ngành ${r.sectorName}:");
expect(src).not.toContain("Watchlist trong");
expect(src).not.toContain("Nganh ${r.sectorName}:");
```

**sourceHealthTools.ts** — source-scan supplement

```typescript
const src = readTool("sourceHealthTools.ts");
expect(src).toContain('"Suy giảm"');
expect(src).toContain('"Ngưng"');
expect(src).toContain("Tình trạng nguồn dữ liệu");
expect(src).toContain("Trạng thái");
expect(src).toContain("Lần cuối thành công");
expect(src).toContain("Lỗi liên tiếp");
expect(src).toContain("Cập nhật lúc:");
expect(src).toContain('"Chưa bao giờ"');
expect(src).toContain("giây trước");
expect(src).toContain("phút trước");
expect(src).toContain("giờ trước");
expect(src).toContain("ngày trước");
// negative guards
expect(src).not.toContain('"Suy giam"');
expect(src).not.toContain('"Ngung"');
expect(src).not.toContain("Tinh trang nguon du lieu");
expect(src).not.toContain("Chua bao gio");
expect(src).not.toContain("giay truoc");
expect(src).not.toContain("phut truoc");
```

**supplyChainTools.ts** (10 strings)

```typescript
const src = readTool("supplyChainTools.ts");
expect(src).toContain("PHÂN TÍCH CHUỖI CUNG ỨNG");
expect(src).toContain("CHỈ SỐ VẬN TẢI BIỂN:");
expect(src).toContain("Chỉ số vận tải: Chưa có dữ liệu");
expect(src).toContain('"NGHIÊM TRỌNG"');
expect(src).toContain('"QUAN TRỌNG"');
expect(src).toContain('"LƯU Ý"');
expect(src).toContain('"THẤP"');
expect(src).toContain("TÍN HIỆU TÁC ĐỘNG CỔ PHIẾU:");
expect(src).toContain("Sai lệch:");
expect(src).toContain('"TĂNG"');
expect(src).toContain('"GIẢM"');
expect(src).toContain('"TRUNG LẬP"');
// negative guards
expect(src).not.toContain("PHAN TICH CHUOI CUNG UNG");
expect(src).not.toContain("CHI SO VAN TAI BIEN");
expect(src).not.toContain('"NGHIEM TRONG"');
expect(src).not.toContain('"TANG"');
```

**technicalIndicatorTools.ts** — source-scan supplement

```typescript
const src = readTool("technicalIndicatorTools.ts");
expect(src).toContain("Quá mua (overbought)");
expect(src).toContain("Quá bán (oversold)");
expect(src).toContain("Trung tính (vùng 40-70");
expect(src).toContain("histogram dương và tăng");
expect(src).toContain("histogram âm và giảm");
expect(src).toContain("cần tối thiểu 34 nến");
expect(src).toContain("cần tối thiểu 20 nến");
expect(src).toContain("Lỗi tính chỉ báo kỹ thuật");
expect(src).toContain("giá > MA5 > MA20 > MA50");
expect(src).toContain("giá < MA5 < MA20 < MA50");
expect(src).toContain("trung bình-thấp");
expect(src).toContain("thấp (cảnh oversold)");
expect(src).toContain("cao (cảnh overbought)");
// negative guards
expect(src).not.toContain("Qua mua (overbought)");
expect(src).not.toContain("Trung tinh");
expect(src).not.toContain("histogram duong");
expect(src).not.toContain("Loi tinh chi bao");
```

**stockSearch.ts** (3 strings)

```typescript
const src = readDomain("stockSearch.ts");
expect(src).toContain("Không tìm thấy kết quả nào");
expect(src).toContain("Kết quả tìm kiếm");
expect(src).toContain("Tên công ty");
expect(src).toContain("Ngành");
expect(src).toContain("Theo dõi");
// negative guards
expect(src).not.toContain("Khong tim thay ket qua");
expect(src).not.toContain("Ket qua tim kiem");
expect(src).not.toContain("Ten cong ty");
```

## Acceptance criteria

All tests in `1416-diacritics-wave5.test.ts` must FAIL before task 1417 runs.
After task 1417 runs, all tests must PASS with zero regressions.

---

## [Architect] Brownfield Findings

interfaces_found:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/dataFreshnessTools.ts`   # MODIFY — exports classifyFreshness, formatAge (consumed by systemTools)
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/sourceHealthTools.ts`     # MODIFY — exports formatSourceHealthTable, STATUS_LABEL
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/interface/mcp/tools/technicalIndicatorTools.ts` # MODIFY — exports formatTaIndicatorReport
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/domain/services/stockSearch.ts`               # MODIFY — exports formatSearchResults

legacy_tests_that_break_after_fix:
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/185-data-freshness.test.ts`           # 12+ assertions on unaccented return values
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1208-price-freshness-vps-push.test.ts` # 3 assertions
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1293-data-freshness-label.test.ts`    # 4 assertions
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/210-source-health.test.ts`            # 4 assertions
- `/Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1302-technical-indicators.test.ts`    # 4 assertions

decisions:
- "source-scan strategy for 9 files with no isolated pure formatter; direct-call for 4 files with exported pure functions"
- "leadershipTools.ts confirmed clean — skip entirely"
- "kinhDichTools wave4-fixed lines (524-529, 734, 808-809) guarded with do-not-break assertions in RED test"

brownfield_scan_clean: true

---

## [Developer] Implementation Record

files_actually_modified:
- /Users/admin/Documents/Hung/__works__/__PROJET/__labo/VN-Market-Intelligence-MCP/src/__tests__/1416-diacritics-wave5.test.ts   # created: RED test suite, 167 assertions across Group A (direct-call) + Group B (source-scan) + wave4 do-not-break guards

tests_written:
- src/__tests__/1416-diacritics-wave5.test.ts   # 167 assertions, 164 RED (fail before 1417), 3 pass (wave4 guards + negative guards on already-clean content)

tests_skipped: []

tsc_clean: true
full_suite_pass: false   # expected — 164 assertions RED by design
