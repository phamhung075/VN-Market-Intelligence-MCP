# REQ-1414: fix(diacritics-wave4) — Interface MCP Tools Layer (5 files, 10 strings)

status: READY_FOR_ARCHITECT
sprint: 147
po_vision: Fix the last 10 unaccented Vietnamese output strings in 5 interface/mcp/tools files so every MCP tool response renders correct diacritics.

## User Story

As a Vietnamese stock investor using Claude,
I want all MCP tool responses to display proper Vietnamese diacritics,
So that I can read Kinh Dịch signals, supply chain events, alert mute status, watchlist confirmations, and stock comparison tables without deciphering garbled text.

## Scope Boundary

**IN scope:** 5 files in `src/interface/mcp/tools/` — exact lines listed below.
**OUT of scope:** All other layers (scheduler, domain, application) — fixed in Sprints 144–146. Do NOT re-scan those files.

---

## String Inventory — Exact Per-File Catalogue

Rules applied:
- INCLUDE: user-facing output strings (MCP `content[].text` fields, formatted report rows)
- EXCLUDE: DB column keys, `.describe()` text, internal enum values, logger messages, code comments

---

### FILE 1: `src/interface/mcp/tools/kinhDichTools.ts`

**5 strings across 4 lines.**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 734 | `` `${ts} \| Que ${r.hexagramNumber} ${queName} ${chinese} \| Tin hieu: ${r.tradingSignal} \| Do tin cay: ${confPct}%` `` | `` `${ts} \| Quẻ ${r.hexagramNumber} ${queName} ${chinese} \| Tín hiệu: ${r.tradingSignal} \| Độ tin cậy: ${confPct}%` `` |
| 808 | `` `Co phieu: ${code}` `` | `` `Cổ phiếu: ${code}` `` |
| 1008 | `` `Loi: Khong co du lieu giai thich cho Que ${number} (${meta.name}).` `` | `` `Lỗi: Không có dữ liệu giải thích cho Quẻ ${number} (${meta.name}).` `` |
| 1033 | `` `  Xu huong: ${data.state.trend}` `` | `` `  Xu hướng: ${data.state.trend}` `` |
| 1035 | `` `  Canh bao: ${data.state.warning}` `` | `` `  Cảnh báo: ${data.state.warning}` `` |

**Note:** Line 1032 `"Tinh trang que:"` — also unaccented; verified in read. Fix to `"Tình trạng quẻ:"`.
**Note:** Line 1038 `"6 Hao (tung duong):"` — also unaccented; verified in read. Fix to `"6 Hào (từng đường):"`.

> Both line 1032 and 1038 are within the same output block and must be fixed alongside the 5 listed strings. Total strings to fix in this file: **7**.

---

### FILE 2: `src/interface/mcp/tools/supplyChainTools.ts`

**2 strings on lines 84 and 86.**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 84 | `` `  Co phieu bi anh huong: ${event.affectedStocks.join(", ")}` `` | `` `  Cổ phiếu bị ảnh hưởng: ${event.affectedStocks.join(", ")}` `` |
| 86 | `` `  Do tin cay: ${Math.round(event.confidence * 100)}%` `` | `` `  Độ tin cậy: ${Math.round(event.confidence * 100)}%` `` |

**Note:** Surrounding lines 80–82 and 88 also contain unaccented strings (`"Loai:"`, `"Mo ta:"`, `"Tuyen duong bi anh huong:"`, `"Su kien gian doan:"`). These were NOT listed in the sprint goal but are in the same output block. Dev must NOT fix them unless the Architect's TECH doc explicitly adds them. Flag in TECH for Architect decision.

---

### FILE 3: `src/interface/mcp/tools/alertMuteTools.ts`

**1 string on line 142.**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 142 | `` `${code}: lenh tat tieng da het han (${formatViDate(wasUntil)}). Canh bao da duoc bat lai.` `` | `` `${code}: lệnh tắt tiếng đã hết hạn (${formatViDate(wasUntil)}). Cảnh báo đã được bật lại.` `` |

---

### FILE 4: `src/interface/mcp/tools/watchlist.ts`

**1 string on line 287 (spans lines 286–287 as template literal).**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 286–287 | `` `Da them ${actionCode} (${exchange}) vao danh sach theo doi.\n` `` | `` `Đã thêm ${actionCode} (${exchange}) vào danh sách theo dõi.\n` `` |

**Note:** Line 287 continuation `` `Canh bao: giam ${dropPct}% \| tang +${risePct}% \| impact >= ${impactScore}/10` `` — fix `"Canh bao:"` → `"Cảnh báo:"`, `"giam"` → `"giảm"`, `"tang"` → `"tăng"`.

---

### FILE 5: `src/interface/mcp/tools/compareTools.ts`

**1 string on line 386.**

| Line | Current (unaccented) | Correct (accented) |
|------|---------------------|-------------------|
| 386 | `` `lines.push(buildRow("Canh bao (7d)", alertVals))` `` | `buildRow` argument: `"Canh bao (7d)"` → `"Cảnh báo (7d)"` |

---

## Functional Requirements

### FR-1: Fix 7 unaccented strings in kinhDichTools.ts

- Description: Replace unaccented Vietnamese in trading history rows, hexagram transition header, error message, and hexagram state block
- Input: existing handler logic unchanged
- Output: MCP `content[].text` with proper diacritics
- Business rule: do not alter template variable names (`r.tradingSignal`, `data.state.trend`, etc.) — only fix the surrounding literal text
- DDD layer: interface

### FR-2: Fix 2 unaccented strings in supplyChainTools.ts

- Description: Replace `"Co phieu bi anh huong:"` and `"Do tin cay:"` in supply chain event formatter
- Input: `event.affectedStocks[]`, `event.confidence` — unchanged
- Output: properly accented supply chain disruption message
- Business rule: fix only lines 84 and 86 as scoped; Architect decides on adjacent unaccented lines 80–82, 88
- DDD layer: interface

### FR-3: Fix 1 unaccented string in alertMuteTools.ts

- Description: Replace expired-mute notification string on line 142
- Input: `code`, `wasUntil` — unchanged
- Output: properly accented mute-expiry notification
- DDD layer: interface

### FR-4: Fix 2 unaccented strings in watchlist.ts

- Description: Replace add-to-watchlist confirmation string on lines 286–287
- Input: `actionCode`, `exchange`, `dropPct`, `risePct`, `impactScore` — unchanged
- Output: properly accented watchlist confirmation text
- DDD layer: interface

### FR-5: Fix 1 unaccented string in compareTools.ts

- Description: Replace `buildRow` label `"Canh bao (7d)"` → `"Cảnh báo (7d)"` on line 386
- Input: `alertCounts` map — unchanged
- Output: comparison table row with accented label
- DDD layer: interface

### FR-6: TDD RED test first (task 1414)

- Description: `src/__tests__/1414-diacritics-wave4.test.ts` — assert accented output for all 5 files
- Must be RED (failing) before fix task 1415 starts
- Minimum 5 test cases (one per file), recommended 10+
- Regression: prior wave test files (1408, 1410, 1412) must remain GREEN
- DDD layer: test

---

## Non-Functional Requirements

- Performance: no change — pure string replacement
- Type safety: `bun tsc --noEmit` clean after fix
- Test baseline: full suite passes after fix (baseline from Sprint 146: 5090+)
- Language: all user-facing strings Vietnamese with correct diacritics; keep English/technical terms (`confidence`, `exchange`, `impact`) as-is in English where they appear

---

## Edge Cases & Data Quality

- [ ] kinhDichTools.ts line 734 — template has 3 unaccented tokens (`Que`, `Tin hieu`, `Do tin cay`) — all 3 must be fixed in a single string
- [ ] watchlist.ts lines 286–287 — fix both lines of the same template literal concatenation; do not break the `\n` join
- [ ] compareTools.ts line 386 — only the string argument to `buildRow()` changes; do not alter `buildRow` signature or `alertVals` logic
- [ ] supplyChainTools.ts adjacent unaccented lines (80–82, 88) — OUT of scope for task 1415; do not fix unless Architect adds them to TECH doc

---

## Blockers

None. All strings confirmed by BA file read on 2026-04-18.

---

## Acceptance Criteria

### AC-1: TDD RED test (task 1414)

**Given** `src/__tests__/1414-diacritics-wave4.test.ts` written, fix task 1415 not yet applied
**When** `bun test src/__tests__/1414-diacritics-wave4.test.ts` runs
**Then**
- At least 5 cases FAIL with string assertion mismatches (RED)
- `bun tsc --noEmit` clean
- No regressions in prior wave test files (1408, 1410, 1412)

### AC-2: All 5 files fixed (task 1415)

**Given** task 1415 applies all string replacements per inventory above
**When** `bun test src/__tests__/1414-diacritics-wave4.test.ts` runs
**Then**
- All cases GREEN
- `bun tsc --noEmit` 0 errors
- `bun test` full suite passes, 0 fail

### AC-3: Regression guard — prior waves

**Given** Sprint 144 (`1408-tool-diacritics.test.ts`), Sprint 145 (`1410-tool-diacritics-sweep.test.ts`), Sprint 146 (`1412-diacritics-wave3.test.ts`) test files exist
**When** full `bun test` runs after task 1415
**Then**
- All prior wave cases remain GREEN
- No new failures in any `src/interface/mcp/tools/` file

### AC-4: Logic integrity

**Given** only string literals changed (no logic, no variable names, no function signatures)
**When** each affected tool handler is called with valid inputs
**Then**
- Template variable substitution (`${code}`, `${r.tradingSignal}`, `${event.affectedStocks}`, etc.) produces correct output
- No TypeScript type errors introduced

---

## DDD Layer Map

| Requirement | Layer     | File Path |
|-------------|-----------|-----------|
| FR-1        | interface | `src/interface/mcp/tools/kinhDichTools.ts` |
| FR-2        | interface | `src/interface/mcp/tools/supplyChainTools.ts` |
| FR-3        | interface | `src/interface/mcp/tools/alertMuteTools.ts` |
| FR-4        | interface | `src/interface/mcp/tools/watchlist.ts` |
| FR-5        | interface | `src/interface/mcp/tools/compareTools.ts` |
| FR-6        | test      | `src/__tests__/1414-diacritics-wave4.test.ts` |

_Paths exact — verified by BA file read on 2026-04-18._
