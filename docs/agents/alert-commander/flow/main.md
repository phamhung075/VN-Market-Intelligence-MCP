# Alert Commander — Main Dispatcher

Universal entry. Alert Commander has a single sub-flow (`cycle.md`); this dispatcher keeps the entry uniform with the rest of the team.

## Dispatch

Always → `docs/agents/alert-commander/flow/cycle.md`

## Steps

1. Read and execute `docs/agents/alert-commander/flow/cycle.md` end-to-end.
2. Return that sub-flow's RETURN block verbatim.

Extend the table here if new sub-flows are added.
