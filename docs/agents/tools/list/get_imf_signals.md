# get_imf_signals

**Purpose:** Get IMF macro signals and policy indicators

**Parameters:**
| Name | Type | Description |
|------|------|-------------|
| — | — | No parameters |

**Returns:** IMF macro signals with confidence scores

**Example:**
```javascript
call_tool(server="vn-market", tool="get_imf_signals", arguments={
  
})
```

**See also:** `docs/standards/mcp-tools.md` — MCP Gateway pattern
