# get_market_hexagram

**Purpose:** Get the market-wide I Ching hexagram derived from VN-Index momentum, USD/VND, oil, gold, foreign flow, and macro signals.

**Parameters:**
| Name | Type | Description |
|------|------|-------------|
| — | — | No parameters |

**Returns:** Not itemized separately in the live tool description — call the tool to see the returned shape.

**Example:**
```javascript
call_tool(server="vn-market", tool="get_market_hexagram", arguments={})
```

**See also:** `docs/standards/mcp-tools.md` — MCP Gateway pattern
