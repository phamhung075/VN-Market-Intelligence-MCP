# get_policy_signals

**Purpose:** Get macro policy event signals

**Parameters:**
| Name | Type | Description |
|------|------|-------------|
| — | — | No parameters |

**Returns:** Policy events with market impact estimates

**Example:**
```javascript
call_tool(server="vn-market", tool="get_policy_signals", arguments={
  
})
```

**See also:** `docs/standards/mcp-tools.md` — MCP Gateway pattern
