# get_vn_liquidity_state

**Purpose:** Returns Vietnam liquidity state snapshot from the State Bank of Vietnam (SBV) and market.db.

**Parameters:**
| Name | Type | Description |
|------|------|-------------|
| — | — | No parameters |

**Returns:** Not itemized separately in the live tool description — call the tool to see the returned shape.

**Example:**
```javascript
call_tool(server="vn-market", tool="get_vn_liquidity_state", arguments={})
```

**See also:** `docs/standards/mcp-tools.md` — MCP Gateway pattern
