# PO — Reusable Triage Scripts Registry

**Parent flow:** `docs/agents/po/flow/main.md` (§ Reusable triage scripts) — lazy-load only when authoring a NEW triage script; check for an existing reusable pattern here first.
**Relocated:** TE-T09 (TOKEN-ECONOMY-AUDIT), verbatim/byte-identical extraction of the registry section formerly inlined in `main.md` (line 225 → EOF).

---

**Reusable triage scripts** (idempotent backlog appends — ALL writes: `jq ... docs/data/orch/orch-state.json | bash "$PROJECT_ROOT/scripts/orch-apply.sh"` — NEVER raw temp→rename; per-script harness descriptions are superseded by this rule):
- `scripts/po-s50-origin-lag-triage.jq` — append a PLAN-ONLY task to `.task_board.backlog`, skipping if `id` already present.
- `scripts/po-s51-cowork-guaranteed-backstop-groom.jq` — append a READY task to `.task_board.ready` (idempotent across all board arrays) + flip a tnb signal row NEW→RESOLVED in one atomic pass. Pattern reusable for any "groom one task + resolve its source signal" single-signal triage (`--arg now`, CAS-guard the rename).
- `scripts/po-s52-chart-range-triage.jq` — dual-mutation triage: promotes `ALLZERO-OHLCV-FETCH` backlog→ready (scope + regression note) and idempotently appends `FIX-FE-CHART-PRICE-DOMAIN` to ready[]. Originated from 2026-06-14 user BUG report (zero-candle / sliver chart). Usage: `jq --arg now "$NOW" -f scripts/po-s52-chart-range-triage.jq docs/data/orch/orch-state.json`.
- `scripts/po-s50-cron-umbrella-hold-backlog-triage.jq` — multi-mutation cycle triage: HOLD-OPEN an umbrella sprint on a future LIVE re-verification gate (G-gate), mark superseded child dups, append id-guarded ready/backlog tasks, and flip NEW signal_queue rows → RESOLVED in one atomic pass. Reusable pattern for "umbrella mechanism-complete but outcome-gate pending market-day + drain signal backlog". Usage: `jq --arg now "$NOW" -f scripts/po-s50-cron-umbrella-hold-backlog-triage.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s54-rsi-pair-dispatch-ready-prune.jq` — single-pass tick triage: PRUNE stale `ready[]` entries that already carry a terminal status (relocate each to `done_verified[]`/`done[]` by status, de-dup guarded — never drop) + DISPATCH a chosen id-set `ready`→`in_progress` with dispatch stamps + annotate held in_progress/parked-review dispositions, all atomically. Reusable pattern for "the ready[] array drifted (false ready[N]) — reconcile to status-correct arrays AND dispatch this tick's real backlog in one pass". Harness adds a CONSERVATION guard (total entries across the 5 board arrays must be unchanged — pure relocation, catches an append/move that loses or duplicates an entry). Usage: `jq --arg now "$NOW" -f scripts/po-s54-rsi-pair-dispatch-ready-prune.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→array-shape→conservation→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s55-health-recheck-wave2-triage.jq` — single-pass stale-backlog drain triage: enrich a bare backlog row (owner/zone/scope), id-guarded MINT of N new FIX tasks (one HELD with a `depends`+`hold_reason` on a future behavioral gate), and PROMOTE a chosen id-set backlog→ready (`status=READY`), all atomically. Originated 2026-06-15 draining 36 never-drained health-recheck reports (3142..3180) where most dedup'd to done_verified/live-recovered. Reusable pattern for "drain a stale detect→fix backlog: dedup against done_verified + live, enrich+mint only the still-broken-NOW, hold the gate-overlapping one". Harness adds a CONSERVATION guard (post-backlog == pre-backlog + mints − promotions; post-ready == pre-ready + promotions). Usage: `jq --arg now "$NOW" -f scripts/po-s55-health-recheck-wave2-triage.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s60-bctc-outage-doublefire-triage.jq` — single-pass dev-team :07 multi-mutation triage: id-guarded MINT of N tasks → `ready[]` (one P0 recon-first NOT-a-coding-lane + several deferred-because-owner-BUSY roots), ANNOTATE an existing backlog recurrence (recurrence_count bump, no dup of the new outage report), and ACK a `signal_queue.rows` row NEW→READ (READ not RESOLVED until fixes ship), all atomically. Originated 2026-06-15 (BCTC pipeline 2nd-recurrence outage + offhours-gatherer double-fire). Reusable pattern for "a real recurring outage + a multi-root cowork signal land in the same tick — recon-first the outage, dedup against the existing handoff, mint+defer the roots by owner-busy state". Harness: CONSERVATION guard (ready += exactly N mints, backlog length unchanged, total += N) + HARD-CONSTRAINT guard (a named review[] row stays untouched) + signal-ack guard. Usage: `jq --arg now "$NOW" -f scripts/po-s60-bctc-outage-doublefire-triage.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→guards→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s53-builder-prune-codify-triage.jq` — single-task weekend (market-independent / infra) triage: id-guarded PROMOTE of a FIX task into `.task_board.ready[]` with full spec (root_cause/fix_spec/generic_mandate/verification_gate/size), skipped if id present in ANY board array. Originated from the 3rd-recurrence (2026-06-14) host disk-full ENOSPC from Docker build-cache; codifies `docker builder prune -f` into the shared ops rebuild flow. Reusable pattern for "promote one fully-specced infra FIX to ready, idempotent". Usage: `jq --arg now "$NOW" -f scripts/po-s53-builder-prune-codify-triage.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s62-factory-maintainability-mint.jq` — bulk epic-mint triage: id-guarded MINT of an entire zone-routed audit backlog into `.task_board.backlog[]` under a named epic, pulled from the audit `.output` via `--slurpfile` (carries task_id/title/priority/dev_agent/zone/effort/risk/rebuild_required/dod/depends_on — never retyped), PLUS a CI-guardrail cluster (one row per guardrail + an architect spike umbrella listing its children), tagging a named fast-track id-set (`fast_track:true`) and attaching a per-task NOT-NULL caution where a fix could relocate a mask. All rows `status:BACKLOG` (never promoted to ready[]); fully idempotent (re-run mints 0). Originated 2026-06-15 minting the 94-task maintainability factory-audit (Workflow wl2ia1tk8) + 7 CI guardrails. Reusable pattern for "a read-only factory/audit Workflow produced an N-task backlog + guardrail set — mint the whole program under one epic without promoting, dedup against the live board, surface the fast-track wave and any mask-relocation caution". Harness: CONSERVATION guard (backlog += N tasks + M guardrails + 1 spike; ready/in_progress/review/done/done_verified byte-unchanged) + CAS mtime-retry + idempotency re-run (delta 0). Usage: `AUDIT=<.output>; NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" --slurpfile audit "$AUDIT" -f scripts/po-s62-factory-maintainability-mint.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→CAS→rename; commit orch-state by EXPLICIT PATH; PUSH held — PO's deferred call).
- `scripts/po-s66-head-ssot-collapse-reconcile.jq` — single-head-SSOT collapse + reconcile: RECONCILES top-level `.head` to the most-recent real dispatch, COLLAPSES the deprecated `.task_board.head` to a non-routing redirect stub, and flips the originating signal row NEW→RESOLVED — all atomic, idempotent. Origin 2026-06-15 signal `head-drift-po-s64-vs-task-board-head` (RECURRING ROOT: po-s65/po-s54 scripts wrote `.task_board.head` while dev-team flow Step 0b + orch-state-access.md §2 + router-d1-claim read TOP-LEVEL `.head`, so the dispatch went invisible to flow-resume). Durable fix shipped alongside: the 3 offending writer scripts retargeted to top-level `.head`; canonical-head rule + write-guard documented in `docs/standards/orch-state-access.md` §4. Reusable pattern for "two divergent-writer pointer fields drifted — collapse to the one the consumers read, redirect-stub the other, retarget every writer". Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s66-head-ssot-collapse-reconcile.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→CAS-mtime→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s71-erraudit-w2-done-verified-w3-fold.jq` — single-pass dual-mutation SIGN-OFF triage: RELOCATE a named review[] row → done_verified[] with RAW-verify provenance stamps (idempotent — no-op if already in done_verified), PLUS FOLD a concrete site-list into an EXISTING backlog row in-place via a `.folded_sites` marker (idempotent, guarded by marker presence — NO SSOT duplicate-key when the wave-N+1 task already exists). Originated 2026-06-16 signing off FIX-ERRAUDIT-W2-MCP-DATALAYER + folding qa's 15 out-of-scope surviving-bare-catch sites into the pre-existing W3-MCP-P2. Reusable pattern for "QA approved task N; flip it done_verified AND fold the out-of-scope same-anti-pattern sites qa found into the already-existing next-wave backlog row (never mint a dup)". Harness: CONSERVATION (review −1, done_verified +1, backlog UNCHANGED, total unchanged) + row-move/fold-once invariant. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s71-erraudit-w2-done-verified-w3-fold.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→invariant→rename; commit orch-state by EXPLICIT PATH; PUSH held).
- `scripts/po-s72-w1-pek-p0-promote.jq` — single-task groom+promote: PROMOTE a named READY-FOR-GROOMING backlog row → ready[] with full grooming/promotion stamps + `next_agent` for the router lock-claim+spawn chain. Idempotent: skipped entirely if the id is already in ANY non-backlog lane (re-run promotes 0). Originated 2026-06-16 promoting FIX-ERRAUDIT-W1-PEK-P0 into a freed coding slot once its SAME-ZONE dependency (RASTERIZE) reached done_verified. Reusable pattern for "a dependency cleared + the zone is free + coding WIP has a slot — promote one fully-specced unblocked backlog task to ready for the router, po does NOT spawn". Harness: CONSERVATION (backlog −1, ready +1, others unchanged) + promote-once invariant. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s72-w1-pek-p0-promote.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→rename; commit orch-state by EXPLICIT PATH; PUSH held).
- `scripts/po-s73-w1-pek-p0-signoff-w2-frontend-dispatch.jq` — single-pass SIGN-OFF + NEXT-WAVE-DISPATCH triage: RELOCATE a named coding task in_progress[] → done_verified[] with full RAW-verify provenance stamps (idempotent — no-op if already in done_verified), PLUS PROMOTE the next wave's first unblocked backlog row → ready[] (status=READY, `next_agent`) with grooming/promotion stamps, PLUS SET top-level `.head` to dispatch that task's first agent — all atomic. Idempotent: relocate guarded by done_verified membership, promote skipped if id already in ANY non-backlog lane. Originated 2026-06-16 signing off FIX-ERRAUDIT-W1-PEK-P0 (last of Wave-1, RAW-verified live DB+pytest) and kicking off the Wave-2 frontend hop (FIX-ERRAUDIT-W2-FRONTEND-SAFEFETCH, ba) — its sequence_after dep (W2-MCP-FETCH-DEADLINE) already done_verified so the inner-first gate is satisfied. Reusable pattern for "QA approved coding task N (last of a wave) — flip it done_verified AND promote the next wave's first unblocked backlog task to ready + point the canonical head at its first agent, in one pass; po does NOT spawn". Harness: CONSERVATION (in_progress −1, done_verified +1, backlog −1, ready +1, review/done byte-unchanged, total unchanged) + relocate-once/promote-once + head-points-to-promoted invariants. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s73-w1-pek-p0-signoff-w2-frontend-dispatch.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→invariant→rename; commit orch-state by EXPLICIT PATH; PUSH = PO's call).
- `scripts/po-s74-sla-test-ts2367-unblock-push-promote.jq` — single-task PUSH-UNBLOCK promote+escalate: PROMOTE a named backlog tsc-red FIX → ready[] AND ESCALATE its priority (P3→P2) + set `blocking:true` + `next_agent`, PLUS REPOINT top-level `.head` at it as the immediate next dispatch — because a red pre-push hook (`pnpm --filter vn-market check`) blocks EVERY push on a red tree, so the sole tsc-red now strands the whole fleet's push including already-RAW-verified done_verified sign-off commits (red-prepush-strands-fleet lesson). Idempotent: skipped if the id is already in ANY non-backlog lane. Originated 2026-06-16 when a PO push-now decision hit a SELF-INTRODUCED red (FIX-SIGNAL-CONFIDENCE-DEFAULT-50.test.ts:270 TS2367, HIGH-vs-CRITICAL no-overlap, in the unpushed chain commit 4f5192c5 — NOT benign cloud-chore weather). Reusable pattern for "push-now blocked by a real red pre-push hook — verify it's the SOLE error via `bun tsc --noEmit`, confirm self-introduced (in unpushed commits, touches a changed file), escalate the already-tracked lint task to a blocking push-unblocker, promote+repoint head; po does NOT write the code fix and does NOT push around the red". Harness: CONSERVATION (backlog −1, ready +1, others byte-unchanged) + promote-once + head-points-to-promoted invariants. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s74-sla-test-ts2367-unblock-push-promote.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→invariant→rename; commit orch-state by EXPLICIT PATH; PUSH HELD until tsc green).
- `scripts/po-s76-ohlcv-p0-code-complete-review-promote-sau-d4-reconcile.jq` — RECOVERY-TRIAGE single-pass triple-mutation reconcile: RELOCATE a CODE-COMPLETE-but-stranded task `ready[]`→`review[]` (next_agent=qa) with full commit-set + RAW-re-run regression provenance + a LIVE post-rebuild qa_gate carried verbatim (code complete ≠ done_verified when the verification_gate is rebuild+live-repair+RAW), STAMP a `.head` reconcile note WITHOUT moving the head (head deliberately stays on the SOLE-tsc-red push-unblocker), and FLIP a paired NEW signal_queue id (both rows) → RESOLVED with ground-truth notes (one STALE false-positive held-lock-on-expired-lock, one reconciled active-vs-held concurrency). Originated 2026-06-16 recovery triage after a dev-team worker shipped all 7 OHLCV-P0 SUBTASKs to HEAD then went quiet mid-lane (lock expired 04:03:05Z, loop quiet 83 min) without a dispatcher tick advancing `ready→review`. Reusable pattern for "a stranded code-complete task whose gate is LIVE+rebuild → promote to review for qa (NOT done_verified), reconcile the head-drift signals it spawned, keep the head on its real blocker". Harness: CONSERVATION (ready −1, review +1, in_progress/done/done_verified byte-unchanged, total unchanged, signal NEW count −2) + CAS-mtime + idempotency (review-membership + status!=RESOLVED guards → re-run mutates 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s76-ohlcv-p0-code-complete-review-promote-sau-d4-reconcile.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→CAS→rename; commit orch-state by EXPLICIT PATH; PUSH HELD until tsc green).
- `scripts/po-s81-ohlcv-gate-red-foreign-recon-triage.jq` — single-pass dual-finding tick triage: RELOCATE a recon SPIKE `ready[]`→`done[]` (done_verified:false — recon=findings, the spun-out FIXES are the verified work) with verdict stamp, PLUS id-guarded MINT of N follow-on FIX tasks split by lane/priority (one P0 coding-lane → `ready[]` sequenced ahead of lower-pri ready items; the rest → `backlog[]`), while ASSERTING (placement guard, never mutating) that a parent done_verified row stays done_verified, a review[] row stays in review (NOT done_verified — non-generic behavioral gate), and a backlog HELD row stays HELD. Originated 2026-06-16 from two router-RAW-verified findings in one tick: OHLCV behavioral-gate RED (4 residual root classes → 1 P0 aggregator-seed-unmigrated + 1 P2 corp-action-continuity) + foreign-flow recon complete (verdict real-count-no-truncation → 1 P2 coverage + 1 P3 dead-endpoint). Reusable pattern for "a recon completes + a done_verified fix's behavioral gate goes RED in the SAME tick — close the recon done-but-unverified, mint its spun-out FIXES, mint the gate-RED follow-ons split P0-to-ready vs P2/P3-to-backlog, and assert the held/review/done_verified rows are untouched". Harness: CONSERVATION (ready net +0 [−1 relocate, +1 P0 mint], done +1, backlog += N backlog-mints, in_progress/review/done_verified byte-unchanged, total += total-mints) + PLACEMENT guard (each held row in its asserted lane) + idempotency re-run (delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s81-ohlcv-gate-red-foreign-recon-triage.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→placement→rename; commit orch-state by EXPLICIT PATH; PUSH HELD until tsc green).
- `scripts/po-s83-ohlcv-startup-seeder-mint-aggregator-p0-overturn.jq` — TARGETED-MINT single-pass + gate-overturn triage: id-guarded MINT of one P0 coding-lane FIX → `ready[]` (leads), an IN-PLACE gate-state OVERTURN of a `done[]` QA-approved-but-now-RED task (status DONE→`SUPERSEDED_PARTIAL`, `done_verified:false`, stamp `behavioral_gate:RED` + `superseded_by` — partial work NOT reverted), and IN-PLACE annotations on two held dependents (`review[]` row keeps status + gets a `live_source_note`; `backlog` HELD row gets `live_source_note` + the new P0 appended to its `depends[]`) — all idempotent (mint id-guard, overturn status-guard, annotations marker-guarded on `live_source_note`), and `.head` deliberately untouched (it was on an unrelated active coding wave). Originated 2026-06-16 when the router RAW-found a same-boot startup seeder re-writing 636 flat zero-vol O=H=L=C bars ~2.5min AFTER `purgeStrandedSeedRows()`, defeating the purge and overturning the prior "done" aggregator-migration P0 (its behavioral gate is RED — the startup-seeder path was never migrated). Reusable pattern for "a router LIVE finding overturns a prior done[] gate — mint the real-root P0 to ready, overturn the superseded task in-place (keep it in done[], don't revert good partial code), and re-annotate any dependents whose live source is still poisoned, without minting dups or moving the head". Harness: CONSERVATION (ready +1; backlog/review/done/done_verified/in_progress LENGTHS byte-stable — M2/M3 are in-place field edits) + PLACEMENT guard (mint in ready, overturn in done with SUPERSEDED_PARTIAL+RED, both alert tasks annotated-in-place + held) + IDEMPOTENCY re-run (byte-identical, delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s83-ohlcv-startup-seeder-mint-aggregator-p0-overturn.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→placement→idempotency→metadata-bump→rename; commit orch-state by EXPLICIT PATH; PUSH HELD until tsc green).
- `scripts/po-s85-ohlcv-seeder-green-board-closeout.jq` — BOARD CLOSE-OUT single-pass multi-mutation reconcile-to-ground-truth (NO new tasks): RELOCATE N QA-approved `done[]` rows whose `verification_gate` was WITHHELD_PENDING_LIVE_PROBE → `done_verified[]` once the router RAW-probes the gate GREEN (full live_probe provenance stamp), INCLUDING a prior SUPERSEDED_PARTIAL/behavioral_gate:RED row whose superseder is now live-GREEN → relocate it too with `behavioral_gate:GREEN_VIA_SUPERSEDER` + `resolution:completed-by-chain` (its partial code KEPT, NOT reverted) so it no longer falsely reads RED, PLUS ANNOTATE-IN-PLACE (never promote) each dependent gate row in `review[]`/`backlog[]` with a `data_source_fixed`/`giá0_source_eliminated` marker (the shared DATA SOURCE is fixed but each owes its OWN behavioral gate — do NOT auto-flip to done_verified), PLUS RESET a stale top-level `.head` (pointing at an already-done_verified task) → idle so no dispatcher re-spawns it. All idempotent (relocate guarded by done_verified membership, annotate by marker presence, head by active_task_id==null). Originated 2026-06-16 closing the OHLCV-seeder fix chain after router RAW-probed flat-seed=0 stable / DAG honest-gap / majors intact at 09:36Z (3 rows done→done_verified incl the GREEN_VIA_SUPERSEDER aggregator P0; 2 alert-gate dependents annotated+held; head reset off stale FIX-ERRAUDIT-W2-FRONTEND-SAFEFETCH). Reusable pattern for "a multi-task fix chain's behavioral gate is RAW-verified GREEN — promote every WITHHELD-gate done row + the now-GREEN superseded partial to done_verified, annotate (not promote) the dependent alert gates, and idle-reset a stale head". Harness: CONSERVATION (done −N, done_verified +N, review/backlog lengths byte-stable [in-place edits], total unchanged) + PLACEMENT guard (each row in correct lane, 0 RED left in done[]) + IDEMPOTENCY re-run (lane delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s85-ohlcv-seeder-green-board-closeout.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→placement→idempotency→rename; commit orch-state by EXPLICIT PATH; PUSH HELD — PO out-of-band).
- `scripts/po-s88-bctc-vps-stale5d-recon-closeout-ci-red-promote-auditor-reconcile.jq` — single-pass multi-mutation :07-tick triage combining FOUR independent dispositions: M1 RECON-CLOSEOUT (relocate a HANDOFF P0 backlog row → done[] with `done_verified:true` + `resolution:"non-bug"` when a recon RAW-verdict proves the framed outage is source-side/functional-not-broken — the recon doc IS the live verification, no rebuild), M2 CODE-LANDED RECONCILE (relocate a BACKLOG row whose fix already COMMITTED → review[] with `next_agent=qa` + a LIVE verification gate — code-complete ≠ done_verified, stale health-rechecks that predate the commit don't re-probe), M3 STANDING-CI-RED PROMOTE (relocate an already-fully-specced `blocking:true` BACKLOG row → ready[] LEAD via unshift — per /goal#1 recheck-shipped-code, deduped against the live tracked task, NOT a new mint), and M4 id-guarded MINT of N residual-risk children split by lane (P2 coding→ready, P2 deferred→backlog, recon→backlog SPIKE) carrying the parent recon's §Residual-Risks verbatim + a `generic_mandate`/`verification_gate` each, with explicit DISTINCT-FROM notes where a child could be mistaken for an already-shipped sibling fix (e.g. queue-row-lifecycle drop ≠ the shipped health-status freshness-gate). Originated 2026-06-16 closing FIX-BCTC-VPS-PIPELINE-STALE-5D (recon verdict pipeline_functional_source_non_filing; the whole 4-contract durability program already done_verified) + promoting the standing FIX-CI-RED-STANDING-1837A-1352A + reconciling FIX-AUDITOR-EMIT-SCHEMA-DRIFT-BUSDARK (landed 220b48c5). Reusable pattern for "a recon closes a framed-outage as non-bug + a committed-but-ungated fix needs QA + a standing blocking red needs promoting + the recon spun out residual children — do all four in ONE atomic pass, dedup every promotion against the live board, mint only the genuinely-new gaps". Harness: CONSERVATION (ready +2, review +1, backlog −1, done +1, in_progress/done_verified byte-stable, total += N-mints) + PLACEMENT guard (each row in its target lane with the right status/flags) + HARD-CONSTRAINT (named review/in_progress/done_verified rows untouched) + IDEMPOTENCY re-run (lane delta 0, byte-identical modulo updated_at). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s88-bctc-vps-stale5d-recon-closeout-ci-red-promote-auditor-reconcile.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→placement→idempotency→rename; commit orch-state by EXPLICIT PATH; PUSH HELD — PO out-of-band).
- `scripts/po-s98-fleet-push-2red-unblock-autopush-backstop-mint.jq` — POST-PUSH single-pass triple-mutation triage: M1 id-guarded MINT of one P1 blocking FIX → `ready[]` LEAD (the push-gate unblocker for the ci_green-gated cluster), M2 id-guarded MINT of one architect DESIGN task → `backlog[]` that EXTENDS an already-done_verified discipline task (durable root-fix, not a dup), M3 ANNOTATE-IN-PLACE a `done[]` row to reassign its withheld verification gate to the new blocker (done_verified stays null). Originated 2026-06-17 after the worktree fleet push (882ab789→701923bc) landed the CI-RED-STANDING fix but the full per-file-isolation CI suite stayed RED on 2 GENUINE reds DISJOINT from that fix (084-tool-market stale toBe(2)→3 tool-count + FIX-VPS-HEALTH-FRESHN behavioral) — both fail LOCALLY too (NOT host-weather), so the 5 ci_green_on_subsequent_push-gated tasks stay WITHHELD and the gate-blocker is reassigned. Durable backstop minted = the deferred option-2 (threshold-triggered worktree-push) of FU-ORIGIN-LAG-PUSH-DISCIPLINE. Reusable pattern for "a fleet push landed the targeted fix but full-suite CI is still red on disjoint genuine reds — mint the unblocker FIX, reassign the gate, withhold the gated cluster, and mint the durable automation that stops the manual-nudge recurrence". Harness: CONSERVATION (ready +1, backlog +1, done/done_verified/in_progress/review LENGTHS byte-stable [M3 in-place]) + CAS-mtime + IDEMPOTENCY re-run (delta 0, each mint once, annotation marker-guarded). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s98-fleet-push-2red-unblock-autopush-backstop-mint.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→CAS→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s100-ohlcv-p0ab-review-to-done-code-reconcile.jq` — single-pass BOARD-RECONCILIATION (NO new tasks): RELOCATE a QA-APPROVED P0 `review[]`→`done[]` as CODE-COMPLETE (`status:DONE`, `done_verified:false`, `rebuild_shipped:true`, full behavioral-gate stamp) when QA verdict is APPROVE-CODE + router RAW-verified the ops rebuild LIVE but the behavioral gate (market-open) is HELD, PLUS COLLAPSE a sibling P0 represented by N bare SUBTASK strings in `review[]` INTO its existing design-parent object in `done[]` (fold the strings into a `.subtasks` field + `.subtask_commits`, flip the parent DESIGN_COMPLETE→DONE/done_verified:false) so the design-parent + code-subtasks become ONE done-code SSOT record with zero orphan bare-string entries, PLUS ASSERT-ONLY (placement guard, no mutation) that the non-P0 follow-ons already exist in `backlog[]` (no dup mint). Idempotent: relocate guarded by done[] membership, collapse guarded by parent status + absence of the subtask strings in review[]. Originated 2026-06-17 reconciling the two QA-cycle-287-APPROVED OHLCV writer-SSOT P0s (P0-A ARCH-OHLCV-WRITER-SSOT-DURABLE design-parent + 3 SUBTASK strings; P0-B FIX-ALERT-SCAN-REJECT-STUB-BAR-P0) after ops rebuilt LIVE but done_verified is HELD to the 2026-06-18 02:15Z market-open behavioral gate. Reusable pattern for "two QA-approved rebuilt-LIVE P0s land in review[] — one as a clean object, one as bare subtask strings hanging off a design-parent — move BOTH to done-code (NOT done_verified, gate is market-open) and collapse the strings into the parent without losing the work or minting a dup". Harness: CONSERVATION (review −[1 object + N strings], done +1, done_verified/backlog byte-stable, total −N) + PLACEMENT (both P0s in done[] with status DONE/done_verified:false, 0 subtask strings left in review[], both follow-ons present + BACKLOG in backlog[]) + IDEMPOTENCY re-run (delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s100-ohlcv-p0ab-review-to-done-code-reconcile.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→placement→rename; commit orch-state by EXPLICIT PATH; PUSH HELD — done_verified gated to market-open).
- `scripts/po-s102-auto-push-backstop-promote-dispatch.jq` — single-task PROMOTE+DISPATCH (idempotent): M1 PROMOTE a fully-recon'd design task `backlog[]`→`ready[]` (status=READY + promotion stamps + next_agent), skipped if the id is already in ANY non-backlog lane; M2 REPOINT canonical top-level `.head` at the task's first agent (architect) ONLY if head is idle (active_task_id==null) AND the promote happened. Originated 2026-06-18 (po-s102) promoting `ARCH-AUTO-PUSH-THRESHOLD-BACKSTOP` after the 3rd manual fleet worktree-push of one session — the deferred option-2 backstop (po-s98) that sat ~9 passes unstarted because it gates nothing visible. Reusable pattern for "a fully-specced backlog design task whose absence keeps recurring as manual router cost — promote it to ready + dispatch its first agent via the canonical head, idempotent, without touching coding-WIP lanes". Harness: CONSERVATION (ready +1, backlog −1, in_progress/review/done/done_verified byte-stable) + CAS-mtime + IDEMPOTENCY (promote guarded by non-backlog membership, head guarded by active_task_id==null → re-run mutates 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s102-auto-push-backstop-promote-dispatch.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→CAS→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s105-auto-push-trigger-not-firing-mint-annotate.jq` — single-pass dual-mutation BOARD-CORRECTION (honesty over a green badge): M1 id-guarded MINT of `FIX-AUTO-PUSH-TRIGGER-NOT-FIRING` → `done[]` as DONE/`done_verified:false` carrying the REAL qa gate (AUTONOMOUS-FIRE: origin advances on a launchd-timer tick with NO human/router invoking it — NOT the isolated-script-green gate that produced the false-done), M2 ANNOTATE-IN-PLACE the 4 false-done flow-step rows (`TASK-AUTO-PUSH-B-PO`/`B-DT`/`C` + `ARCH-AUTO-PUSH-THRESHOLD-BACKSTOP`) with a `trigger_corrected` marker (their tick-exit placement NEVER fired autonomously — failure mode (a): PO is a bg triage sub-agent, sub-flows RETURN to the router, Step PUSH-BACKSTOP is never reached; script/doc work retained, only the TRIGGER was wrong — superseded by launchd timer `com.vn-market.fleet-push`). Idempotent: M1 id-guarded, M2 marker-guarded → re-run mutates 0. Harness: CONSERVATION (done +1, all other lanes byte-stable) + idempotency re-run. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s105-auto-push-trigger-not-firing-mint-annotate.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s107-insider-agentsignals-schema-contract-promote.jq` — single-pass dual-PROMOTE (WIP=0 → ≤2): backlog→ready for two SAME-CLASS schema-vs-caller contract FIX tasks where a live Zod schema REQUIRES a param the SSOT tool-doc marks optional/auto, so every-cycle callers omit it → -32602 reject + a data feed disabled. M1 promotes `FIX-INSIDER-OUTSTANDINGSHARES-SCHEMA-DOC` as a direct dev-mcp-server FIX, CORRECTING the board row's wrong zone (cross-service/→apps/mcp-server/) + wrong files (the flow .md → the real schema leadershipTools.ts/leadershipSignal.ts + doc) + priority (P3→P2), with a recon-first desc carrying the PO-read ground truth (code+outstandingShares both REQUIRED, no derive/fallback, auto-fetch UNWIRED, callers mis-use the pure classifier with no transactions[]). M2 promotes `FIX-AGENT-SIGNALS-AGENT-PARAM-CONTRACT` as-is (SPRINT-S→architect — agent z.string() REQUIRED even in the sender-history mode where the description+doc L-4 example omit it → doc internally contradictory → genuine direction ambiguity). Both idempotent (skipped if id already in ANY non-backlog lane). Originated 2026-06-18 dev-team tick 20260619T003100Z (WIP=0, health-recheck 3244 P1×2). Reusable pattern for "WIP freed up + two backlog schema-vs-doc/caller drift FIXes confirmed live — promote both backlog→ready, correcting any stale zone/files/priority on the row, routing the unambiguous one direct-to-dev and the doc-internally-contradictory one to architect". Harness: CONSERVATION (backlog −2, ready +2, in_progress/review/done/done_verified byte-stable, total unchanged) + promote-once + lane/status check. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s107-insider-agentsignals-schema-contract-promote.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s109-verify-cowork-macro-resolve-deadroute-bpe-arch-dispatch.jq` — CLOSE+UNSTICK single-pass triple-mutation: M1 RESOLVE a router-RAW-verified VERIFY-class task `ready[]`→`done_verified[]` (verified-no-active-leak provenance + a `dead_route_note`), M2 UNSTICK by REPOINTING top-level `.head` OFF a DEAD route ONTO a real dev-chain executor (`status=in_progress` + `next_agent=architect` so dev-team Step-0b head-resume FIRES), M3 id-guarded defensive MINT of a P3 latent-trap closer → `backlog[]`. Originated 2026-06-19 (po-s109): VERIFY-COWORK-MACRO carried `next_agent=cowork-team` (the cowork CYCLE dispatcher, NOT a board-task executor — dev-team spawn-guard FORBIDS spawning it = DEAD route) AND `head.status="dispatching"` (a non-routing status Step-0b doesn't handle), so BOTH promoted tasks stranded ~2h; resolving VERIFY freed the head, repointed at BPE-ARCH-1→architect. **ROUTING RULE: VERIFY-class / live-probe board tasks route to a REAL executor (router-probe / qa), NEVER cowork-team; promoted `ready[]` tasks only dispatch when the canonical `.head` carries `status=in_progress` + a spawnable `next_agent`.** Harness: CONSERVATION (ready −1, done_verified +1, backlog +1, in_progress/review/done byte-stable) + PLACEMENT (head on BPE-ARCH-1/in_progress/architect, VERIFY in done_verified, humanize P3 minted) + IDEMPOTENCY re-run (delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s109-verify-cowork-macro-resolve-deadroute-bpe-arch-dispatch.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→placement→rename; commit orch-state by EXPLICIT PATH).
- `scripts/po-s122-ssot-perimeter-dod-harden.jq` — single-pass DoD-harden (idempotent, scoped to `active_sprints[SSOT-INTEGRITY-PERIMETER]` only → flat lanes never touched, conservation trivial): ADD `.verification_gate` (4-gap `wave1_done_when` + 3-tier `rule_parity` decision), MINT `SSOT-W1-OPS-REBUILD-ENFORCE` (ops, Gap-1 REBUILD-TO-LIVE) + `SSOT-W1-DOC-SYNC-WRITE-CONTRACT` (pm, Gap-3) into `tasks[]`, SET `acceptance="0 direct hot-file writers remain"` on `SSOT-W1-ORCH-APPLY-WRAPPER` (Gap-2), APPEND 3 `ranked_scope` rows incl `SSOT-W2-RULE-PARITY-PROMOTE` (Gap-4). Reusable pattern for "router RAW-verified a half-live deploy surface — harden the sprint DoD so SIGN-OFF can't certify done while enforcement is source-only / writers bypass the wrapper / docs unsynced / the two enforcement points carry divergent blocking rule-sets". Idempotent: vgate add-if-absent, mints id-guarded, acceptance/ranked_scope marker-guarded → re-run mutates 0. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); EPOCH=$(date +%s); jq --arg now "$NOW" --argjson epoch "$EPOCH" -f scripts/po-s122-ssot-perimeter-dod-harden.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→`scripts/orch-state-validate.sh`→CAS-mtime→rename; commit orch-state by EXPLICIT PATH; PUSH HELD — fleet-push timer pushes).
- `scripts/po-s132-ind-p1-momentum-ba-mint-frontend-promote.jq` — single-pass NEXT-WAVE promote triple-mutation (idempotent): M1 id-guarded MINT of one BA spec task → `ready[]` (next_agent=ba, zone=multi) covering an N-tool sub-wave whose gates just cleared; M2 ANNOTATE-IN-PLACE the N placeholder roadmap rows in `backlog[]` with `specced_under=<BA-id>` (they STAY BACKLOG — pm decomposition under the BA spec mints the real per-tool dev tasks; placeholders must NOT dispatch directly); M3 PROMOTE one fully-specced disjoint-zone frontend row `backlog[]`→`ready[]` (next_agent=dev-frontend, `parallel_eligible:true`). All type-guarded against string lane entries (`if type=="object" then .id else ...`). Head DELIBERATELY UNTOUCHED (router continues from the PO RETURN NEXT, not `.head`). Originated 2026-06-30 promoting the IND-P1 momentum/relative-strength sub-wave (4 backend tools: ROC-Momentum/Relative-Strength/52W-High/Foreign-Accum-Rank) + the frontend gauge-cards once IND-P1-CONSUMER-WIRING-AUDIT reached done_verified (3fd6e151 — 5 P0 tools LIVE-consumed by 6 helper flows; consumption pattern proved). Reusable pattern for "a gate cleared + a multi-tool sub-wave was held PLAN-ONLY → route the whole family via ONE BA spec (architect SPLITs multi-zone), annotate the placeholders so they don't double-dispatch, and promote any disjoint-zone parallel-eligible companion task". Harness: CONSERVATION (ready +2, backlog −1, in_progress/review/done/done_verified byte-stable, total +1) + PLACEMENT (BA spec + frontend in ready, N placeholders annotated+held in backlog) + idempotency re-run (delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s132-ind-p1-momentum-ba-mint-frontend-promote.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).
- `scripts/po-s133-ind-p1-momentum-consumer-wiring-mint-nextwave-rank.jq` — single-pass DUAL-mutation PLAN-ONLY triage (idempotent): M1 id-guarded MINT of `IND-P1-MOMENTUM-CONSUMER-WIRING` → `backlog[]` (status=BACKLOG, owner+next_agent=`cowork-refactory-expert`, zone=`cross-service`) carrying a per-flow `wiring_map` + `generic_mandate` + 6 ACs + `verification_gate` — closes the AC6 P1 consumer-wiring gap left OPEN by the P0-only `IND-P1-CONSUMER-WIRING-AUDIT` (these are cowork agent `.md` FLOW changes, NOT service code → cowork-refactory path, NOT a dev-* zone); M2 ANNOTATE-IN-PLACE the `IND-ROADMAP-LEDGER` backlog row with `next_wave_ranking` (16 remaining IND-P1/P2 rows ranked by analytical value-per-effort + `next_wave_to_sprint` top-3). Originated 2026-06-30 (po-s133) at the IND-P1 momentum wave-completion boundary. Reusable pattern for "a wave shipped tools but left an AC6 consumer-wiring gap untracked on the board + the next indicator wave needs a recorded priority order — mint the wiring task BACKLOG and stamp the ranked sequence onto the roadmap-ledger SSOT, plan-only (PO does NOT promote or spawn)". Harness: CONSERVATION (backlog +1, all other lanes byte-stable) + IDEMPOTENCY (M1 id-guard across all lanes, M2 marker-guard `has("next_wave_ranking")` → re-run delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s133-ind-p1-momentum-consumer-wiring-mint-nextwave-rank.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).
- `scripts/po-s134-ind-p1-momentum-frontend-ba-mint.jq` — single-mutation cascade-kickoff MINT (idempotent): id-guarded MINT of `BA-IND-P1-MOMENTUM-FRONTEND` → `ready[]` (status=READY, next_agent=ba, zone=multi, priority=high, `user_prioritized:true`, type=SPRINT-M) covering FRONTEND surfacing of the 4 P1 momentum indicators — deliverable-1 `GET /api/momentum-indicators` REST aggregator (dev-mcp-server, mirror indicator-gauges, REUSE clients.ts NOT the MCP tool layer, Promise.allSettled 4-section isolation, always HTTP 200, honest-NULL passthrough); deliverable-2 `api.momentum-indicators.tsx` proxy + `dashboard.momentum.tsx` cards + TopNav + tests (dev-frontend). Architect SPLITs multi-zone; pm decomposes. Carries the standing freshness("Cập nhật lúc" SSOT=coverage-map) / all-info(source-link+dropdown) / no-fake-data(honest-NULL+null_reason) / coverage-map-GAP-plan-only ACs. Originated 2026-06-30 (po-s134) folding the router addendum after the user asked to "add to frontend new implement" (PO RAW-grep confirmed 0 P1 momentum surface in apps/frontend/app while P0 gauges are LIVE). USER-PRIORITIZED → ready[] leads the next-wave NEW-indicator backlog rows. Reusable pattern for "user asked to surface already-shipped backend tools on the dashboard → mint ONE BA-spec cascade-kickoff to ready[] (next_agent=ba, zone=multi) carrying the REST-aggregator + frontend-cards split; PO does NOT spawn — dev-team cron adopts". Harness: CONSERVATION (ready +1, all other lanes byte-stable) + IDEMPOTENCY (id-guard across all lanes → re-run delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s134-ind-p1-momentum-frontend-ba-mint.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).
- `scripts/po-s135-dash-cron-recheck-table-kickoff.jq` — single-pass DUAL-mutation self-initiated SPRINT KICKOFF (idempotent): M1 append `sprint_goal.entries[]` with the `DASH-CRON-RECHECK-TABLE` vision (id-guarded); M2 MINT the BA-spec cascade-kickoff `BA-DASH-CRON-RECHECK-TABLE` → `ready[]` (next_agent=ba, zone=multi, type=SPRINT-M, user_prioritized) — id-guarded across ALL lanes. Head deliberately untouched (router continues from the PO RETURN NEXT). Originated 2026-07-01 (po-s135) from the USER feature request "add a CRON TABLE to /dashboard/orchestration to RECHECK every scheduled cron vs its expected fire time (on-time/late/missed/stale/never)". Reuse mandate baked in: CRONS map (SSOT for ~80 Layer-A exprs) + cronJobRunStore MAX(started_at) (double-log-immune last-fire) + schedulerWatchdogJob WATCHDOG_MANIFEST (cadence×threshold classifier to GENERALIZE from 16→all) + get_cron_health (extend, not duplicate) + orchestrationHandler.ts/api.orchestration.tsx (mirror the REST+proxy pattern). Two-layer honesty: Layer-A node-cron (trackable) vs Layer-B CLI-session crons (SESSION_SCOPED, NEVER MISSED). Same class as po-s134 (user asks to surface a capability on the dashboard → ONE BA-spec cascade-kickoff to ready[]; PO does NOT spawn). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s135-dash-cron-recheck-table-kickoff.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).
- `scripts/po-s61-bctc-recon-done-enrich-hardening-triage.jq` — single-pass recon-completion + durable-hardening-sprint triage: RELOCATE a COMPLETED recon `in_progress`→`done` (with `done_verified:false` + a `done_verified_deferred_note` gating on a future infra cycle), id-guarded MINT of N follow-on tasks to `ready[]` (incl. an architect-brief umbrella SPIKE that lists its child FIX ids), and ANNOTATE the recurrence-anchor backlog row (recurrence_count held, `architect_briefed:true`, no dup) — ALL idempotent (relocate guarded by `done` membership; annotation guarded by `architect_briefed`; mints by id-presence). Originated 2026-06-15 closing OPS-BCTC-PIPELINE-RECON (2nd recurrence BCTC-VPS-PIPELINE-STALE; deployed discovery fixes did NOT resolve the user-facing P0 whose TRUE root = enrich-silent-0-rows). Reusable pattern for "a recon lane completes + deploys partial fixes + spawns a multi-owner durable hardening sprint whose true user-facing root differs from the deployed fix — close recon done-but-unverified, mint the children incl. an architect brief per recurring-bug-escalation, annotate the recurrence anchor". Harness: CONSERVATION (ready += N mints, in_progress −1, done +1, backlog/review/done_verified unchanged, total += N) + HARD-CONSTRAINT (other in_progress + all review rows untouched, recurrence_count held, each mint present exactly once). Usage: `jq --arg now "$NOW" -f scripts/po-s61-bctc-recon-done-enrich-hardening-triage.jq docs/data/orch/orch-state.json` (atomic temp→`[ -s ]`→`jq empty`→conservation→hard-constraint→rename; commit orch-state by EXPLICIT PATH).

- `scripts/po-s137-repair-request-3signal-backlog-mint-resolve.jq` — single-pass DUAL-mutation repair_task_request drain triage (idempotent): id-guarded MINT of N `repair_task_request` signals → `.task_board.backlog[]` as PLAN-ONLY FIX rows (skip any id already in ANY board lane) + RESOLVE their source `.signal_queue.rows[]` (status-guard NEW/READ→RESOLVED — the cold-evictable terminal, NOT TRIAGED). Reusable pattern for "dev-team drained N repair_task_request signals → mint each as a backlog FIX (po→ba→pm→dev chain) + resolve its source row, atomic". Originated 2026-07-02 (dev-team tick T02:37Z) minting FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE (agent-father) + FIX-BCTC-ANALYST-STAGELOG-NOTIFY-NO-BASH (agent-father) + FIX-RAG-SERVICE-CLEAN-EXIT-RESTART-LOOP (ops). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s137-repair-request-3signal-backlog-mint-resolve.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s138-b05-ssc-failfast-respec-promote.jq` — single-task RE-SPEC + PROMOTE (idempotent): CORRECT an INVERTED backlog-row spec in place (rewrite `status_note`/`files_hint`/`acceptance`, stamp `scope_corrected`+`prior_spec`) then PROMOTE backlog→ready. No-op if the id already sits in ANY non-backlog lane. Originated 2026-07-03 (po-s138) dev-team :07 tick (WIP=0, main free): the `B-05-FU-SSC-503-RETRY` row's spec ("add 1 retry + 60s backoff in `_ssc_curl_search()`") was semantically INVERTED vs the B-05 RECON RAW-verified root cause — the ~60s blocking retry EXCEEDS mcp-server's ~5s discovery timeout → silent `[]` → ~328 queue items frozen in `deferred_infra`; the fix is the OPPOSITE (bound the SSC curl to fail-fast STRICTLY UNDER the caller timeout, remove the 60s loop). Reusable pattern for "a backlog row's spec is semantically inverted vs the RAW-verified root cause — rewrite the spec in place and promote to ready so dev implements the RIGHT thing, not a dup mint". Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s138-b05-ssc-failfast-respec-promote.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s139-bctc-nonbank-total-assets-spike-remediation-mint.jq` — single-pass id-guarded MINT of 3 PLAN-ONLY rows → `.task_board.backlog[]` (skip any id already present in ANY task_board array lane → re-run mints 0), the consolidated remediation of a done_verified read-only SPIKE. Originated 2026-07-03 (po-s139) on SPIKE-BCTC-NONBANK-TOTAL-ASSETS-ZERO (findings doc 43453950b, verdict MIXED: 7/8 "never refined" shared bucket + POW genuine residual). Mints: `OPS-BCTC-REFINE-REPASS-NONBANK-5T` (operational agentic-refine repass + reingest for VHM/VIC/VRE/HSG/MWG, ZERO new code, proven CTG runbook — SPIKE item 1), `SPIKE-BCTC-REFINE-TOTAL-ROW-TRANSCRIPTION-DROP` (architect-first, POW grand-total-row transcription-drop — item 3), `FIX-DEVTEAM-PREFLIGHT-SF1-REENTRANT` (router latent candidate). SPIKE item 2 (REE/VNM keep own tickets) + item 4 (REJECT per-ticker regex branch = FPT-OVERFIT anti-pattern, baked as `generic_mandate` on the 2 BCTC rows) are no-mint decisions. All rows `status:BACKLOG` — NOT promoted, head untouched (dev-team cron drains). Reusable pattern for "a done_verified consolidated-remediation SPIKE — mint the accepted items as PLAN-ONLY backlog rows (operational repass + follow-up SPIKE + latent FIX), decline the no-mint items, bake the standing guardrail as a generic_mandate field, dedup against the whole board". Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s139-bctc-nonbank-total-assets-spike-remediation-mint.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s140-b05-bctc-freshness-layer-split-mint-hnx-ssl-ops-verify.jq` — single-pass DUAL-mutation router-dispatched DECISION+DEDUP triage (idempotent): M1 id-guarded MINT of `FIX-AUDITOR-B05-BCTC-FRESHNESS-LAYER-SPLIT` → `.task_board.backlog[]` (BACKLOG, PLAN-ONLY, skip if id in ANY lane) — the bctc sibling of `FIX-AUDITOR-B11-NEWS-FRESHNESS-LAYER-SPLIT`: B-05 applies a 24h-continuous SLA to the RAW quarterly-PDF-push layer (`get_vps_proxy_health` raw last-push) instead of the authoritative analysis-layer gate (`get_sla_status`), firing ~6x/day CRITICAL + Telegram BUG for an event-driven quarterly source; fix = read the analysis-layer SLA gate + exempt when `trigger_bctc_vps_fetch(dry_run)` pending==0 AND `get_vps_service_health(vn-bctc-fetch)` HEALTHY. M2 marker-guarded UPDATE-IN-PLACE of `BCTC-HNX-SSL-HARDEN` in review[] (`.po_recovery_disposition` + `next_agent`=ops) — records that the analysis layer recovered via a DIFFERENT fix chain (the SSC-503 enricher drain), so this DECOUPLED security-debt task's OWN post-deploy AC stays unverified → routed to ops for a live deploy+fetch-with-verification-ON probe (OVERRIDE 07-03: delegate gated deploys to ops); STAYS in review[] (no promotion, no resolution). Reusable pattern for "an auditor two-layer-freshness FP fires recurring CRITICAL+Telegram-BUG while a linked deploy task shows decoupled recovery — mint the auditor-layer-split FIX (dedup vs the sibling family) and route the deploy task to ops for its own post-deploy live verification without resolving it on the unrelated recovery". Harness: CONSERVATION (backlog +1, review length byte-stable [M2 in-place], all other lanes byte-stable) + IDEMPOTENCY (M1 id-guard across all lanes, M2 marker-guard on `po_recovery_disposition` → re-run delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s140-b05-bctc-freshness-layer-split-mint-hnx-ssl-ops-verify.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s141-cowork-guaranteed-slot-durability-brief-signoff-dispatch.jq` — single-pass ARCHITECTURE-BRIEF SIGN-OFF triage (idempotent): DEDUP-PROMOTE the pre-existing board rows a brief supersedes (re-spec in place → `ready[]`, keep id/lineage, stamp `scope_corrected`+`prior_spec`/`prior_title`+`architect_brief`), MINT only the genuinely-new owner tasks (dependent ones → `backlog[]` HELD with `depends[]`+`hold_reason`), GATE the source `signal_queue.rows[]` row on the QA DoD task (add `resolution_gated_on`+`po_signoff`, keep status READ — do NOT resolve), APPEND the ruling to `sprint_goal.entries[]`. Head deliberately untouched; PO does NOT spawn (gateway-blind — router does per-agent PRE-CLAIM+spawn). Originated 2026-07-07 (po-s141) signing off the cowork guaranteed-slot durability brief (Option A generalized): re-spec F1-LAUNCHD-COWORK-BACKSTOP (F1.4) + FIX-AUDITOR-T1-PEER-FIRER-HEALTH-DEGRADED to the brief's design, mint OPS-COWORK-GUARANTEED-SLOT-INSTALL + DOC-COWORK-CRON-RUNBOOK-FRESHEN + QA-COWORK-SLOT-SESSION-DOWN-SURVIVAL (the DoD gate). Reusable pattern for "PO signs off an architecture brief that owner-maps implementation tasks — dedup-promote what the board already tracks, mint only the new-owner work held on deps, gate the signal on the QA DoD, record the ruling; never resolve the signal before the QA gate passes". Harness: dry-run conservation (backlog net 0, ready +N, others byte-stable) + idempotency re-run (delta 0, marker/id-guarded) BEFORE apply. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s141-cowork-guaranteed-slot-durability-brief-signoff-dispatch.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+dup-key+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s142-clean-stale-inprogress-reconcile.jq` — single-pass CLEAN board-hygiene reconcile (idempotent): relocate N stale `in_progress[]` rows whose implementation closed OUT-OF-BAND to their RAW-verified ground-truth lane (shipped-but-never-flipped → `done_verified[]` DONE_VERIFIED; direction-abandoned → `done[]` DONE + `done_verified:false` + resolution note — no SUPERSEDED/CANCELLED enum, so abandonment is modelled as DONE+note per LANE_ALLOWED_STATUSES) + close the tracking CLEAN row `backlog[]`→`done[]`. Originated 2026-07-10T23:07Z dev-team tick executing `CLEAN-STALE-INPROGRESS-P5SELFHEAL-L2FRESHNESS`: FIX-L2-FRESHNESS-DATAASOF-FIELDS (shipped a384497a3, RAW-verified 5-handler data_asof + 20 tests + tsc + rows_no_asof 8→2, sat in_progress 13d) → done_verified; FIX-SCHEMA-DRIFT-P5-SELFHEAL (abandoned per 755c761a8 REVIEW→REWORK, P8 superseded efbab47b6, sat >1mo) → done. Freed 2 dead WIP slots starving BOUNDED-1. Reusable pattern for "stale in_progress rows closed out-of-band silently consume WIP — relocate each by RAW-verified ground-truth status, terminal rows null detail_ref, close the CLEAN tracker". Harness: dry-run conservation (in_progress −2, backlog −1, done +2, done_verified +1, task_total unchanged) + placement + idempotency re-run (byte-identical) BEFORE apply. Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s142-clean-stale-inprogress-reconcile.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s144-tnb-c109-triage.jq` — single-pass PLAN-ONLY tnb-audit HANDOFF triage (idempotent, all mutations confined to `.task_board.backlog[]`): M1 ANNOTATE-IN-PLACE the already-tracked `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` with a `tnb_recurrence` marker (a recurrence datum, NOT a dup mint), M2 ESCALATE-IN-PLACE the stale P1 `FIX-COWORK-FLOWS-GATEWAY-BLIND-BRIDGE-FALLBACK` with a `tnb_escalation` marker folding N sibling findings as in-scope manifestations + a design-vs-defect ruling + fix-script-exists-unwired note (recurring-bug escalation, NOT a self-promote), M3 id-guarded MINT of only the genuinely-new untracked finding → `backlog[]` PLAN-ONLY (skip if id in ANY lane). All guards make re-run a no-op (marker-guarded `has(...)` + id-guard across all lanes). Originated 2026-07-14 (po-s144) triaging tran-ngoc-bau c109 (verdict GAPS): folded F-MCP-SUBAGENT-SYSTEMIC + F-TNB-DUAL-DISPATCH into the gateway-blind umbrella, recorded F-CHEF-EOD-DORMANT recurrence on the supersede-wiring SPIKE, minted FIX-CHEF-EVENING-DUP-DATE-MISLABEL-INVESTIGATE. Reusable pattern for "triage a supervisor audit handoff PLAN-ONLY — annotate the umbrella/recurrence rows the board already tracks, escalate a stale recurring-bug umbrella with folded manifestations + a design-vs-defect correction, and mint ONLY the genuinely-new finding; PO does NOT promote or spawn". Harness: orch-apply conservation (backlog +1 = the sole mint; M1/M2 in-place field edits) + idempotency re-run (delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s144-tnb-c109-triage.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s145-cowork-3signal-guard-enrich-filename-fireelection-mint.jq` — single-pass 3-signal cowork-team dev-team-:07 triage (idempotent): M1 DEDUP-ENRICH a signal INTO an existing GUARD row instead of re-minting (marker-guarded `has("guard_2nd_instance_folded")|not`): bump P2→P1 on recurring-class confirmation + add a `false_premise_correction`/`enrichment` field that CORRECTS the row's own proposed fix (a boundary-enforcement row whose option-1 "sole legit write is a notebook" was FACTUALLY WRONG — ~207 committed docs/signals/*.json across 7 agents) + a 3rd scope item, WITHOUT falsifying the row (a self-report-FP guard: the "content correct" claim is RAW-probe evidence in a named commit, not agent say-so); M2/M3 id-guarded MINT of 2 DISTINCT PLAN-ONLY FIX rows the router's suggested dedup-targets did NOT cover (a date-keyed-filename intra-day-clobber class row + a fire-election tick-tombstone row that the peer-namespace P1 row explicitly cannot close because it is single-session self-refire); M4 marker-guarded SIBLING-LINK on the P1 row so its ba does not assume the namespace fix closes the refire path; M5 FLIP the 3 source `signal_queue.rows[]` (dedup-folded→RESOLVED, minted-and-tracked→READ per origin_signal_id auto-RESOLVE); M6 last_triaged stamps. Originated 2026-07-15 (po-s145) triaging cow-20260715T212837 (self-edit 2nd GUARD instance, content VERIFIED CORRECT) + 212838 (date-keyed signal filenames clobber, 3rd instance, FPT 9240→8120) + 214005 (cron:cowork:<TICK> released at Step 6 → same-tick self-refire re-elects+re-runs). Reusable pattern for "a batch of cowork signals where one SHARPENS-not-falsifies an existing row (fold+enrich+correct-its-fix-premise) and the others are DISTINCT from the router's suggested dedup-targets (mint PLAN-ONLY + link the sibling that would otherwise be assumed-covered), flipping each source signal READ-vs-RESOLVED by whether it minted trackable work". Harness: dry-run JSON-valid + backlog +2 + GUARD priority=P1/marker + signal flips + idempotency re-run (delta 0, no dup id, no double-enrich) BEFORE apply; orch-apply conservation (task_total +2, signal_total unchanged). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s145-cowork-3signal-guard-enrich-filename-fireelection-mint.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-sprint-band-priority-bump.jq` — GENERIC idempotent priority bump for a named sprint's BACKLOG band (`--arg sprint`, `--arg now`, `--arg by`). Rule (target computed from CURRENT priority + `plan_only`, never incremental → re-run is a no-op for rows already at target): `plan_only==true` → P3→P2, keep P2/P1/P0 (plan-verify/recon/UNVERIFIED-umbrella rows CAP at P2, never inflated to P1); `plan_only!=true` → P2→P1, P3→P2, keep P0/P1 (concrete fix rows bumped one tier, CAP at P1, never P0). Scope: only `.sprint==$sprint` AND `.status in {BACKLOG,TODO}`; stamps `priority_bumped_from/at/by` only on rows whose priority actually changes (untouched rows byte-identical, so `bumped_from` always reflects the TRUE pre-bump value). Does NOT touch next_agent/owner/supervised/deps/lanes or convert plan_only→fix. Originated 2026-07-17 (user-approved "priority bump on the ULTRACODE-AUDIT-FIXALL band" so the BOUNDED-1 dev-team loop drains it ahead of lower-urgency backlog): 22 non-plan_only P2→P1 + 4 plan_only P3→P2 = 26 rows. Reusable pattern for "user wants sprint X's band to drain first — raise the band one priority tier, plan-verify rows capped one tier below the concrete fixes". Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg sprint "<SPRINT_ID>" --arg now "$NOW" --arg by "po/<intent>" -f scripts/po-sprint-band-priority-bump.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-sprint-band-to-front.jq` — GENERIC idempotent PARTITION: move a named sprint's backlog rows to the FRONT of `.task_board.backlog` (`--arg sprint`), preserving each partition's internal order. WHY: `scripts/devteam-backlog-promote-bounded1.jq` picks the eligible row with the lowest `[priority_rank, backlog-array-index]`; array index is the ONLY intra-tier tiebreak, so when a band shares its priority tier with a co-equal competitor sprint (e.g. ULTRACODE-AUDIT-FIXALL vs the live TOKEN-ECONOMY-AUDIT P1 band, 2026-07-17) the competitor's lower index wins every tiebreak and a same-tier priority bump alone never changes the next pick — moving the band to the front is the ONLY lever short of P0 over-inflation. SAFETY: reorders the TIEBREAK ONLY — never beats a genuinely higher-priority row (rank sorts before idx, so any eligible P0 still outranks this band's P1 regardless of position); row count/lane-membership/fields byte-identical (conservation trivially holds); fully reversible. Idempotent (band already at front → identical output); survives concurrent peer drains (tail shrinks, band stays front) and appends (new rows land at tail). Compose AFTER po-sprint-band-priority-bump.jq when you want both the bump AND the intra-tier lead. Originated 2026-07-17 (made the BOUNDED-1 next pick flip TE-T08 → UC-SDF-P3). Reusable pattern for "a bumped band still loses the FIFO tiebreak to a co-equal live sprint — front-partition it to win the intra-tier pick without P0 inflation". Usage: `jq --arg sprint "<SPRINT_ID>" -f scripts/po-sprint-band-to-front.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh`.

- `scripts/po-s146-tnb-c112-triage.jq` — single-pass PLAN-ONLY tnb-audit c112 HANDOFF triage (idempotent, all mutations in `.task_board.backlog[]`): M1 ANNOTATE-IN-PLACE `SPIKE-COWORK-GUARANTEED-SLOT-SUPERSEDE-WIRING` with a `tnb_recurrence_c112` marker (DISTINCT key so the prior-cycle `tnb_recurrence` from c109 is NOT clobbered — a per-cycle recurrence datum: 4th consecutive unhealthy-EOD business day + the 2-failure-mode reconciliation that reassigns 07-17 to the mid-flow-BAIL class, keeping the SPIKE = true-non-dispatch root only), M2 FOLD-IN-PLACE `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING` with a `tnb_intraday_symptom` marker (F-INTRADAY-SYNTHESIS-STALE-MULTIFIRE is a NEW first-write-wins symptom of the SAME date-keyed synthesis-collision root — the INVERSE of the row's documented last-write-clobber — so it folds + carries an `ac_refinement` requiring both same-day intraday cycles to land + stay queryable regardless of write-order, NOT a re-mint), M3 id-guarded MINT of the one genuinely-new finding `FIX-CHEF-USDVND-THRESHOLD-NUMERIC-DRIFT-GATE` → `backlog[]` (PLAN-ONLY, P3, plan_only, next_agent=ba, supervised — LLM-narrative-drift FX-threshold citation needs a deterministic numeric-literal gate + a canonical-value reconciliation first, NOT prose). Re-run is a no-op (M1/M2 marker-guarded `has(...)`, M3 id-guarded across all lanes). Originated 2026-07-17 (po-s146) triaging tran-ngoc-bau c112 (NEEDS_ATTENTION): held F-CHEF-EOD-DORMANT / F-MCP-SUBAGENT / F-CHEF-STEP0-BCTC(auto-cured) / F-L6-COVERAGE(WATCH,below-threshold) as dedup/no-mint. Reusable pattern for "triage a supervisor audit handoff PLAN-ONLY — record a per-cycle recurrence datum on the umbrella SPIKE without clobbering the prior cycle's, FOLD a new symptom-variant into the existing root row with an AC refinement, and mint ONLY the genuinely-new finding; hold every below-threshold WATCH and every already-max-escalated recurrence; PO does NOT promote or spawn". Harness: orch-apply conservation (backlog +1 = the sole mint; M1/M2 in-place field edits, other lanes byte-stable) + idempotency re-run (delta 0). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s146-tnb-c112-triage.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — router carries it on the tick push).

- `scripts/po-s143-ultracode-audit-fixall-mint.jq` — BULK EPIC-MINT of the user "fix all" directive on the 2026-07-12 ultracode workflow audit (verification FINAL 2026-07-13: 17 CONFIRMED / 23 RESCOPE / 8 REJECTED / 58 UNVERIFIED). Single-pass id-guarded MINT of 49 rows → `.task_board.backlog[]` under sprint `ULTRACODE-AUDIT-FIXALL` (skip any id already in ANY board lane → re-run mints 0): 16 CONFIRMED FIX/CLEAN/SPRINT rows (`UC-<DOMAIN>-Pn`, UC-RDL-P1 lock-prefix-align P0 sequenced lead), 22 RESCOPE rows each carrying a `note` = the verifier Rescope-note summary + `detail_ref` to the brief anchor (NOT the original proposal), 8 per-domain UNVERIFIED PLAN-ONLY batch umbrellas (`UC-<DOMAIN>-UNVERIFIED-BATCH`, `plan_only:true`, pick-time pre-verify MANDATORY), 3 completeness-critic PLAN-ONLY rows (`UC-CRITIC-HOOKS-ENFORCEMENT` + `UC-CRITIC-GATEWAY-CONTRACT-DRIFT` high-risk + `UC-CRITIC-UNAUDITED-LANES-BATCH`). PLUS marker-guarded (`has("audit_ref")|not`) ANNOTATION of 8 existing backlog rows so audit proposals that FOLD into filed work are linked/superseded not double-minted: FIX-ORPHAN-ADOPTION-BOARD-STATE-GUARD (router-P3 folds — do NOT dup/close, AC2 residual), FIX-DEVTEAM-EPIC-WRAPPER-AUTOCLOSE-SWEEP (dev-team-loop-P3 ship vehicle), TE-T05/TE-T11/TE-T33 (P6-Piece2/P2 · P1-rider · P4-coord), FIX-AUDITOR-C11/C06 (P3 repoint), SYSREMAKE-P2 (git-P2/P8 RC-GITSTATE coord). REJECTED (8) minted 0; auditor-signal-loop-P4 (D4 janitor FP) verified already-landed (commit e109f49f8, all 3 markers live in tasksMdJanitorJob.ts) with no open tracking row → no close-out needed. Reusable pattern for "a verified multi-domain audit with FINAL verdicts — mint CONFIRMED as fix rows (S-effort/critical first), RESCOPE per the verifier note not the proposal, UNVERIFIED as plan-only per-domain umbrellas, critic-gaps as investigation rows, and link/annotate every proposal that duplicates or folds into an already-filed row". Harness: dry-run JSON-valid + backlog delta = expected + every row zoned + idempotency re-run (delta 0) BEFORE apply; orch-apply conservation (task_total +49). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s143-ultracode-audit-fixall-mint.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).

- `scripts/po-s140-chef-marker-key-window-anchor-mint-ac-extend.jq` — single-pass TRIPLE-mutation RECURRING-CLUSTER-WITH-NEW-ROOT triage (idempotent): M1 id-guarded MINT of 2 PLAN-ONLY rows → `.task_board.backlog[]` (`FIX-CHEF-MARKER-KEY-WINDOW-ANCHOR` P0 root cause + `FIX-CHEF-LOG-AGENT-WORK-MISSING` P2 observability), M2 ANNOTATE-IN-PLACE the P0 umbrella `UC-CCA-P3` with a marker-guarded `key_agreement_20260722T2010Z` AC extension + `related[]` append (umbrella NOT re-scoped, NOT re-prioritised, NOT promoted), M3 ANNOTATE-IN-PLACE the adjacent `FIX-COWORK-SIGNAL-FILENAME-CYCLEID-KEYING` with a marker-guarded caution so it does not re-ship the same divergence in a new field. Originated 2026-07-22 (po-s140) triaging the router-verified `chef-evening` double-publish: two runs 4.5min apart derived DIFFERENT marker keys (`published:chef-evening:2026-07-22` vs `:2026-07-23`) from ONE wall-clock instant, because the slot's cron `45 19 * * *` = 19:45 UTC = 02:45 VN of D+1 makes chef.md Step 0.5's `TZ=Asia/Ho_Chi_Minh date +%Y-%m-%d` key straddle VN midnight. **Reusable pattern for "a tracked recurring cluster recurred with a NEW root cause that is ORTHOGONAL to the existing umbrella — mint the new root as a SIBLING (never fold-and-close, or the umbrella signs off green while the defect lives), extend the umbrella's ACs in place so it CANNOT close without the new precondition, and inoculate the adjacent row that would otherwise re-introduce the same bug in a different field".** Key PO invariant recorded on the row: a dedup/mutex key MUST be window-anchored (scheduled cron fire-window in UTC) and MUST NOT be derived from any `date` call made by the executing agent; the mutex key and the human-readable display label are two separate values and must never share one derivation. Harness: dry-run JSON-valid + lane deltas (backlog +2, all other lanes byte-stable) + dup-key check + both markers set + idempotency re-run (byte-identical modulo timestamps) BEFORE apply; orch-apply conservation (task_total 615→617, signal_total unchanged). Usage: `NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); jq --arg now "$NOW" -f scripts/po-s140-chef-marker-key-window-anchor-mint-ac-extend.jq docs/data/orch/orch-state.json | bash scripts/orch-apply.sh` (orch-apply does Zod+coherence+dup-key+conservation+CAS+atomic rename; PUSH HELD — fleet-push timer pushes).
