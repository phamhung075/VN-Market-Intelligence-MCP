# PO — Step 0-SIG: Triage pendingSignals[]

**Parent flow:** `docs/agents/po/flow/main.md` (Step 0-SIG dispatcher)

**Two sanctioned invocation paths, ONE contract (FIX-TRIAGE-INBOX-CLEAR-OWNERSHIP-PO-SELF-READ, 2026-08-22 — see decision journal `docs/agent-memory/decisions/triage-20260822T1927Z-po.md`):** (1) `docs/agents/dev-team/flow/main.md` § Step 1 spawns PO in-tick and passes `pendingSignals[]` as a same-tick convenience argument; (2) the router spawns PO directly, per `.claude/skills/dispatch/SKILL.md`'s "queue / triage" dispatch-table row — an ad hoc invocation that never runs dev-team's Step 1 body at all (confirmed live 2026-08-22: this is exactly how a 64-envelope backlog accumulated — dev-team's own Step 1 CLEAR code is physically unreachable on path (2), so ownership can never live there alone). Because either path can be how PO gets here, **PO does not trust a caller-supplied array as its source of truth on EITHER path** — Step 0-SIG's first action, always, is its own fresh read:
```bash
pendingSignals=$(jq -c '.dev_team_idle_chain.pending_triage_inbox // []' docs/data/orch/orch-state.json)
```
Each signal has shape `{from, to, type, payload, priority, createdAt, envelope_id}` where `payload` is usually a file path. PO must read each payload and route accordingly — otherwise the signal is silently lost.

**Durable-inbox CLEAR ownership (FIX-TRIAGE-INBOX-CLEAR-OWNERSHIP-PO-SELF-READ, 2026-08-22 — supersedes FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR's "dev-team Step 1 owns it, PO never does" assignment, which a same-session router-direct dispatch contradicted the same day):** PO now owns the subtractive CLEAR outright, unconditionally, on every invocation — it is the only party guaranteed present on BOTH paths above, so it is the only place this can live without going stale the moment a signal is triaged via a route dev-team's own flow never executes. `docs/agents/dev-team/flow/main.md` § Step 1 performs NO write here any more (cross-reference only, its own local read is now used solely to gate whether spawning PO is worth it this tick — see that section). This is the SOLE sanctioned remover of `.dev_team_idle_chain.pending_triage_inbox[]` entries; the executable CLEAR block is at the end of the Pipeline-A section below, run once after every entry read above has been routed.

**TWO distinct pipelines land on PO, both routed by this doc (FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES, 2026-08-01 — measured 0/16 live `to=po` types matched before this fix):**
- **Pipeline A — dev-team drain** (`pendingSignals[]`, shape above). Table immediately below.
- **Pipeline B — `.signal_queue.rows[]` hot inbox**, read directly by `main.md`'s "Pre-check — Signal dashboard" step (`.claude/skills/signal-dashboard/SKILL.md` § READ), shape `{id, ts, from, to, type, summary, severity, status, payload_ref}`. Table at § Live `.signal_queue.rows[]` inbox below — DO NOT assume Pipeline A's table covers these; the type namespaces are disjoint.

For each signal in `pendingSignals[]` (Pipeline A):

| Signal `type` | From | Action | Routing in Step 1 BATCH |
|---|---|---|---|
| `audit-handoff` | `tran-ngoc-bau` | Already covered by Step 0-TNB (reads `docs/handoffs/tnb-audit-latest.md`) | findings → FIX / SPRINT-* |
| `brief_complete` / `architecture_brief` | `agents-architect` | Read `payload` (a `docs/architecture-briefs/*.md`). Brief specifies which files/agents change. **Naming note (FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE, 2026-08-22):** the live emitter sends `type=architecture_brief`; `brief_complete` was the only name this row recognised, so every live brief fell through to the catch-all. Both names route identically here — treat as aliases, same disposition. | If brief targets `.claude/agents/*` or `docs/agents/*` → UNBLOCK with `route_to: agent-father`. If brief targets `apps/flow/*` code → SPRINT-S/M/L depending on scope. Propagate target zone (e.g. `apps/stock-price/`) into the sprint so architect inherits zone hint. |
| `news_impact` / `price_anomaly` / `bctc_signal` / `fundamental_*` | cowork agents | Cowork signals are usually for `alert-commander`, not PO. If `to=po` was set explicitly, read payload and decide: investment-thesis update (skip — not dev work) or data-pipeline bug (FIX task with `zone: apps/<service>/`). | rarely actionable; default skip with notebook log |
| `zone_missing_tier3` | `dev-team` | Open a CHORE task pre-tagged `zone: <suggestedZone>` (BATCH type CLEAN). **Before inserting:** check `docs/data/orch/orch-state.json` `.task_board` for an existing open entry with `taskId` in title or task_id (dedup on `taskId` field — same guard used in drain-signals). If duplicate found → skip, log `"[po] zone_missing_tier3: task {taskId} — duplicate, skipped"`. If new: append to `.task_board.backlog[]` — canonical shape per `docs/standards/task-schema.md`: `{id: "fix-zone-{taskId}", title: "Fix missing zone on task {taskId} ({suggestedZone})", owner: "po", status: "BACKLOG", zone: "{suggestedZone}", created_at: "<ISO-8601 UTC now>"}`, mark signal processed. If `payload.suggestedZone` absent → emit SPIKE to architect for zone inference. Log `"[po] zone_missing_tier3: task {taskId} — CHORE created zone:{suggestedZone}"`. | CHORE/CLEAN with `zone: {suggestedZone}` OR SPIKE to architect |
| `zone_health_report` | `dev-<service>` | Read `payload.findings[]` and `payload.severity`. If `severity=critical` → open FIX task with `zone: {payload.zone}`. If `severity=warn` → add to `pendingObservations[]` for sprint planning. If `severity=ok` → log and skip. Batch all zone_health_report signals from the same week into one sprint review entry. | FIX (critical) OR pendingObservations (warn) OR skip (ok) |
| `implementation_complete` | `agent-father` | Acknowledgment that a brief track shipped. Log `"[po] track {payload.track} of {payload.brief} acknowledged — {N} files changed"`. No task creation. Append to notebook `pendingObservations[]` if `payload.summary` flags a follow-up (e.g. "needs verification"). Signal moved to processed/ by dev-team — no further action. | skip (acknowledgment-only) |
| `improvement_proposal` | `agents-architect` | Read the proposal doc at `payload` path. **MANDATORY PO critique — ALL FIVE fields must be filled before writing a verdict. An empty or placeholder value on any field auto-rejects with reason "critique incomplete".** Write the following into the `## PO Critique` section of the proposal doc: **(1) What could break:** answer required — which live agent/flow/cron is disrupted. **(2) False-green / silent-swallow risk:** answer required — can the Success Signal return green while the weakness persists. **(3) Is the success signal gameable:** answer required — can the proposed verification be manipulated by the fix itself. **(4) Host-load impact:** answer required — RAM/disk/tick estimate; reference Docker 8GB cap + `project_host_memory_panic`. **(5) Lane-C-in-disguise check (C-3):** answer required — explicitly judge whether this proposal, regardless of its labeled lane, actually edits the gate/audit logic, the loop's own success criteria, an irreversible action, or user-facing comprehensibility. If yes on any sub-criterion → auto-reject, reclassify to LANE-C. Cannot be empty or "N/A". Then write verdict + date + rationale. **SHADOW-PILOT BRANCH (brief 2026-06-01-agent-self-critique-detect-source.md §8 — REMOVE at Phase-2 promote decision):** Check the proposal doc's `Created by:` field. IF `Created by` value is an agent-id ∈ {news-scout, dev-team} → this is a shadow-pilot proposal: perform the FULL five-field MANDATORY critique and write verdict into `## PO Critique` AS NORMAL (required to measure "what fraction would have been approved" per §8 promote criteria). Do NOT emit the LANE-A agent-father dispatch signal. Do NOT create the LANE-B SPRINT batch entry. NO dispatch of any kind. Instead append one line to the proposal doc: `[SHADOW] would-have: <APPROVED-LANE-A | APPROVED-LANE-B | REJECTED | LANE-C> — <reason>`. Then `send_telegram(channel="work", message="[shadow-pilot] {id} verdict logged (no dispatch): {would-have}")`. Commit: `git add docs/improvement-proposals/{id}.md docs/agent-memory/notebooks/po.md` (commit-mutex, explicit paths). EXIT this row — do not fall through to routing-by-outcome. ELSE (Created by = agents-architect or any non-pilot author) → fall through to routing-by-outcome unchanged. Routing by outcome: LANE-A + APPROVED → emit signal `type=improvement_approved_md` to agent-father with `payload=proposal-path`; LANE-B + APPROVED → create SPRINT-S or SPRINT-M batch entry for dev-team with signal `type=improvement_proposal_lane_b`; LANE-C or any REJECTED → `send_telegram(channel="work", message="[loop] Lane-C or rejected: {id} — {reason}")`. Commit: `git add docs/improvement-proposals/{id}.md docs/agent-memory/notebooks/po.md` (commit-mutex, explicit paths). | SHADOW-PILOT (Created by ∈ {news-scout, dev-team}): critique+verdict written, no dispatch, shadow-log line + WORK Telegram. ELSE APPROVED→ agent-father signal (lane-a) or BATCH entry (lane-b); REJECTED/C → WORK Telegram |
| `repair_task_request` | `system-auditor` | Read signal JSON at `payload` path. Extract `check_id`, `anomaly_type`, `severity_class`, `zone_owner`, `summary`, `suggested_sprint_class`. **Dedup check:** scan the NON-TERMINAL LANES by name — `.task_board.backlog[]` + `.ready[]` + `.in_progress[]` + `.review[]` + `.qa[]` (lane membership is the open/closed discriminant on this board — NEVER a status-token enum like TODO/IN_PROGRESS/REVIEW/BLOCKED, which structurally misses every BACKLOG/READY row and was measured live 2026-07-31 to leave 395/633 = 62.4% of open rows invisible; see `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY`) — for any entry whose title or id contains `check_id`. If found → log `"[po] repair_task_request: {check_id} — duplicate task_board entry, skipped"`, mark signal DONE, skip. If no duplicate: append to `.task_board.backlog[]` — canonical shape per `docs/standards/task-schema.md`: `{id: "{check_id}-FIX", title: "{check_id}-FIX ({zone_owner}) — {summary ≤80 chars}", owner: "po", status: "BACKLOG", zone: "{zone_owner}", created_at: "<ISO-8601 UTC now>", status_note: "AC: {check_id} check passes Tier-N (no signal_queue row, no BUG alert 7d). Priority: {severity_class}.", origin_signal_id: "{signal_id}"}` — **additive field, back-reference only:** `{signal_id}` = the incoming signal's own `id` (`payload.id` if the signal JSON carries one; else the drained `signal_queue.rows[].id` for dashboard-sourced signals, per `drain-signals.md` §0a-D `row.id`); omit the field if neither is resolvable — never blocks task creation. Commit orch-state.json update (commit-mutex, atomic write, path `docs/data/orch/orch-state.json`). Log `"[po] repair_task_request: created BACKLOG task {check_id}-FIX zone:{zone_owner}"`. | `.task_board.backlog[]` entry — normal po→ba→pm→dev chain applies |
| `ci_red` | `ci-health-probe` | Read signal fields directly (not a payload path — shape is inline in signal JSON per `docs/agents/dev-team/flow/ci-health-probe.md` §Signal shape). Extract `payload.check_id`, `payload.failing_jobs`, `payload.head_sha`, `payload.run_id`, `payload.suggested_sprint_class`. **MANDATORY PRE-DEDUP FAILING-FILE READ (no exceptions — dedup MUST NOT be evaluated before this step completes):** `failing_jobs[].name` is a literal GitHub job name ("bun test", "size-lint") — it carries ZERO discriminating information about WHAT failed; every ci_red looks identical on job name alone. Run `gh run view {payload.run_id} --log-failed | grep -E 'pass / |FAILEDFILE'`. If the output contains one or more `FAILEDFILE: <path>` lines → the sorted, deduped set of those paths is FAILING_FILES (the block `scripts/ci-per-file-isolation.sh` emits at `=== FAILED FILES (N) ===`, one entry per test file that failed in per-file isolation). If the grep returns EMPTY (the failing job is not the `bun test` isolation runner — e.g. a dependency-install or lint-tool crash with no FAILEDFILE block) → FAILING_FILES is instead the single most specific path named in the failing step's own error line, read verbatim from `gh run view {payload.run_id} --log-failed` (e.g. `apps/frontend/bun.lock` for a frozen-lockfile error, or the offending source path for a lint/size-lint failure) — never the bare job name. Record FAILING_FILES verbatim on whatever row is created below, OR in the skip-log line if skipping — a disposition with no file list attached is INVALID and must be redone. **Dedup check (FILE-SCOPED — PRIMARY; check_id/head_sha are SECONDARY, both SHA-derived and proven to never match across advancing SHAs on this repo, do not dedup on them alone):** for EACH distinct file in FAILING_FILES, build `dedup_key = "ci_job:{failing job name for that file}|file:{file path}"` and (1) scan the NON-TERMINAL LANES by name — `.task_board.backlog[]` + `.ready[]` + `.in_progress[]` + `.review[]` + `.qa[]` (lane membership is the open/closed discriminant on this board — NEVER a status-token enum like TODO/IN_PROGRESS/REVIEW/BLOCKED, which structurally misses every BACKLOG/READY row and was measured live 2026-07-31 to leave 395/633 = 62.4% of open rows invisible; see `FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY`) — for any entry whose own `dedup_key` field matches — if found → log `"[po] ci_red: {check_id} — file-scoped dedup hit on {existing_id} ({dedup_key}), duplicate skipped"`, append the new `head_sha`/`check_id` as an additional observed SHA to that row's `status_note`, mark signal DONE, skip that file. (2) SECONDARY, only if (1) finds nothing for that file: scan the SAME NON-TERMINAL LANES for an open entry whose title or id contains `check_id`, or whose `status_note` contains `head_sha` — same skip on match. If NEITHER matches for that file: append to `.task_board.backlog[]`: `{id: "{check_id}-FIX" (or a descriptive FIX-<SLUG> id when the root cause is already clear — do not force the templated id when a more legible one is available; -2/-3 suffix if more than one file mints from the same check_id), title: "{check_id}-FIX — CI RED: {job name} — {file path}", owner: "po", status: "BACKLOG", zone: "apps/<zone inferred from the FILE path, never the job name>", check_id: "{check_id}", dedup_key: "ci_job:{job name}|file:{file path}", ci_fingerprint: "{payload.dedup_key sha256, if present — traceability only, NOT the dedup discriminant}", created_at: "<ISO-8601 UTC now>", verification_gate: "ci_green_on_subsequent_push", status_note: "AC: gh run view <databaseId with headSha AFTER {head_sha}> --json jobs -q '.jobs[]|select(.name==\"{job name}\")|.conclusion' == success (verification_gate=ci_green_on_subsequent_push). Priority: high. Failing job: {job name}. Failing file: {file path}."}`. One row PER DISTINCT FILE — do not collapse multiple unrelated failing files into one row (matches already-validated practice on `FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT` / `FIX-CI-SIZELINT-MACRO-VMT-LIQUIDITY-RESOLVERS-NEW-OFFENDER`, minted as two sibling rows from one signal). Commit orch-state.json (commit-mutex, atomic write). Log `"[po] ci_red: created BACKLOG task {check_id}-FIX — file: {file path}"`. **ANTI-AMNESTY FENCE (hard forbid, no override):** a ci_red MUST NEVER be deduped into `FIX-MCP-SUITE-HEALTH-BASELINE` or any other LOCAL-full-suite row. Reason: `.github/workflows/ci.yml` runs `scripts/ci-per-file-isolation.sh` per commit `3174de0ce`, forking one bun process per test file, so the CI plane's green baseline is 0 fail (proven run `30425110302`) and structurally excludes the full-suite order-dependent pollution that class of row characterises — the two planes never share a fail set and must never share a disposition. **BACKSTOP:** the CI plane's last known-green baseline is 0 fail — a ci_red is therefore ALWAYS actionable. "Pre-existing" / "same standing baseline" is NEVER a valid disposition unless it names a specific, already-open, FILE-scoped `.task_board` row matched via the dedup check above; an assertion of pre-existence with no matched row is a fabricated disposition and the row MUST be minted. | `.task_board.backlog[]` FIX entry (one per distinct failing file) — standard po→dev chain applies; DONE only when CI GREEN on subsequent push for that file's job |
| `bug-escalation` | `commit-sweep-guard` | **MANDATORY: parse the payload's own leading tag BEFORE any other check — never disposition off `git show --stat`.** A payload beginning `[sweep-guard] BARE commit about to absorb` is a TRUE POSITIVE BY CONSTRUCTION: pre-commit:453-454 `exit 0`s silently on `mode=SCOPED` before either the log write or this signal's own `write_signal` call is reached, so the signal existing at all IS the mechanism proof — re-verify against `bash scripts/audits/verify-commit-sweep-discriminator.sh` (VERDICT: PASS confirms the discriminator itself is sound on the current git version) if in doubt, never against the resulting commit's diff. **INVALID dispositions — never use to mark a signal benign/closed:** "hook is noisy", "cannot see the pathspec", "`git show --stat <sha>` is clean" — a clean `--stat` is OUTCOME evidence (no peer happened to collide in this particular race window) and is a DIFFERENT claim from MECHANISM (whether a pathspec was actually on the `git commit` line); it can never substitute for the mechanism check above. Correct disposition: parse `escalated=` from the payload (added by FIX-SWEEPGUARD-WARN-ONLY-NO-ACTUATOR-AND-TRIAGE-MISADJUDICATION's hook actuator). If `escalated=true` → the hook itself already blocked this attempt (actor was already over its own per-session threshold and is still trying) — this is a REPEAT-OFFENDER-AFTER-BLOCK signal: open/append a `bug-escalation` FIX-class `.task_board` row (`zone: cross-service/`, dedup-checked against the FIX-COMMIT-SWEEP-GUARD-* family first) citing the actor, priority high. If `escalated=false` (a normal first/second/third-strike warn) → log as a CONFIRMED true positive and append to `pendingObservations[]` for volume tracking; do NOT open a new FIX row per occurrence (the hook's own escalation already converges repeat offenders — see §2 architecture brief `2026-07-31-sweepguard-escalation-actuator-and-triage-mechanism-check.md`). A payload beginning `[sweep-guard] INTERNAL:` is the SEPARATE fail-open/mechanism-broken path (discriminator itself may be malfunctioning on a new git version) — always independently actionable, dedup into a FIX row against the discriminator, never conflated with a BARE finding. A payload beginning `[sweep-guard] SAME-FILE DIVERGENCE` is a THIRD, distinct payload class from the AC-3 detector (`_detect_samefile_pathspec_only_divergence`, architecture brief `2026-07-21-commit-path-peer-index-sweep-guard.md` §2.7) — its signal JSON carries NO `escalated` key at all (fields are exactly `from/to/type/priority/createdAt/payload`, `payload` a plain string); this is BY CONSTRUCTION, not a malformed signal, and parsing `escalated=` on it will always fail — never disposition it through the `escalated=`/`INTERNAL:` branches above. The brief's own §2.7 disposition is NON-GOAL / never-gating / informational-only (git has no per-hunk attribution, so this detector cannot be strengthened into a hard block). Correct disposition: dedup-check the NON-TERMINAL LANES for `FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE` (the tracked root-cause row for this class — as of 2026-08-01, 10/10 live fires of this payload class were confirmed false positives from that row's own dead fail-open branch, see its `occurrence_4_10` note). If that row is still open (any NON-TERMINAL LANE) → bump its `occurrence_count` (do not re-mint a new FIX row per occurrence), log `"[po] bug-escalation SAME-FILE DIVERGENCE — known FP root cause FIX-SWEEPGUARD-SAMEFILE-DETECTOR-UNSTAGED-PATH-FALSE-POSITIVE, occurrence bumped"`, append to `pendingObservations[]` for volume tracking. If that row is DONE/DONE_VERIFIED (the FP root cause already shipped) and a SAME-FILE DIVERGENCE still fires: it is now a candidate GENUINE divergence (peer-staged same-file hunk) — verify per §2.7 (`git diff <staged-blob> <landing-blob>` — the two-blob form takes NO pathspec; appending `-- <path>` is a git USAGE ERROR (0 lines, exit 129), not a scope filter — proven live 2026-08-09T01:35:08Z, see `FIX-SWEEPGUARD-SAMEFILE-INSPECT-CMD-GIT-USAGE-ERROR-MANUFACTURES-FALSE-BENIGN`); still never hard-block (detector is permanently NON-GOAL/informational by design, independent of the FP-bug fix) — log to `pendingObservations[]`, and only if the diff reveals genuinely unreviewed peer content, escalate via a fresh `bug-escalation`-class FIX row citing the actor. A payload beginning `[notebook-immutability-guard]` is a FOURTH, distinct payload class (added 2026-08-05 after a live fire on `docs/agent-memory/notebooks/system-auditor.md` reached this row with no matching rule) — it is emitted by the notebook-write AC-2a immutability check, NOT by the commit-sweep discriminator, so it carries NO `escalated=` key and NO `[sweep-guard]` prefix; parsing `escalated=` on it will always fail and the three branches above are all inapplicable. It is `mode=warn` and NEVER blocks, by construction. Correct disposition: the guard compares a retained section's bytes in the INDEX against HEAD, so the ONLY valid check is `git diff --cached` / `git show HEAD:<path>` scoped to the named section — a clean `git status` or a clean worktree diff read AFTER the fact is OUTCOME evidence and does NOT refute the finding (the index state at fire time is generally unreconstructable once the agent has committed, so absence of a reproducible diff is INCONCLUSIVE, never "benign"). Because notebook-writing agents auto-prune and rewrite their own retained sections mid-cycle, interleaved-cycle false positives are expected and the base rate is high; therefore do NOT mint per occurrence — append to `pendingObservations[]` for volume tracking, and only mint a FIX when the SAME agent+section pair fires on ≥2 separate cycles (genuine AC-2a violation = real notebook data loss, which is what AC-2a exists to prevent). | `escalated=true` → FIX row (repeat-after-block) / `escalated=false` → `pendingObservations[]` (never "benign") / `INTERNAL:` → separate FIX (detector malfunction) / `SAME-FILE DIVERGENCE` → dedup-bump known-FP row (open) OR informational log + conditional escalate (FP root cause already fixed) — never hard-block |
| `recurring-bug` | `dev-team` | A recurring-bug escalation — dev-team's OWN flow template (`docs/agents/dev-team/flow/main.md` § Step 0 preflight, HEAD.lock-recurrence example) hands authors this exact literal, and live envelopes carry `payload.module`, `payload.summary`, `payload.count`, often `payload.action_required` (`mint_fix_task` / `amend_existing_row`) and a `row_to_amend` id (FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS, 2026-08-28 — previously unrouted; the 2026-08-24 live fire, envelope 622c03db, had to ride the to-fallback). Dedup-check NON-TERMINAL LANES FIRST on `payload.row_to_amend` id if present, else `payload.module` (`scripts/po-board-dedup-search.sh`). If the named row/module resolves → FOLD (append this occurrence + `origin_signal_id`), mark signal DONE, do NOT re-mint. Honour `payload.action_required`: `amend_existing_row` → amend the cited row; `mint_fix_task` → mint `.task_board.backlog[]` FIX (shape per `repair_task_request` above) ONLY when no row/module resolves. | FOLD (dedup on `row_to_amend`/`module`) OR mint FIX (new, no row resolves) |
| `context_bloat_breach` | `context-bloat-backstop-hook` | Notebook/decision-journal byte-cap breach (`payload.file`, `payload.reason`, `action_required=prune_or_split`). **DEFER standing policy** (`feedback_ctxbloat_breach_on_live_sprint_file_defer`): if `payload.file` is a still-open sprint's own live decision-journal (`docs/agent-memory/decisions/sprint-*.md` for a sprint not yet `done_verified`) → log DEFER, `pendingObservations[]`, no mint. Else dedup-check NON-TERMINAL LANES on `payload.file` first — this hook fires ~90x/week on the same handful of files, so ONE open artifact per file, never one per fire; if found → fold (bump occurrence), no re-mint. If new → mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/notebooks/` (or `docs/agent-memory/decisions/` for a decision-journal file), `dedup_key: "context_bloat_breach:{payload.file}"`. | FOLD (dedup on `payload.file`) OR DEFER (open sprint journal) OR CHORE (new) |
| `notebook_unparseable_breach` | `notebook-auto-prune-hook` | Notebook failed to parse — no `## ` section boundaries found (`payload.file`, `action_required=manual_review`). Dedup-check NON-TERMINAL LANES on `payload.file` first — fold into an existing open repair CHORE. If new → mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/notebooks/`, `dedup_key: "notebook_unparseable_breach:{payload.file}"`. | FOLD (dedup on `payload.file`) OR CHORE (new) |
| `notebook_single_section_overage_breach` | `notebook-auto-prune-hook` | Notebook over its line/byte cap with only preamble+1 section left — the pruner cannot drop further without data loss (`payload.file`, `action_required=manual_split_to_archive`). Dedup-check NON-TERMINAL LANES on `payload.file` first — fold into an existing open split/archive CHORE. If new → mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/notebooks/`, `dedup_key: "notebook_single_section_overage_breach:{payload.file}"`. | FOLD (dedup on `payload.file`) OR CHORE (new) |
| `notebook_no_valid_drop_candidate_breach` | `notebook-auto-prune-hook` | Notebook has 2+ sections but every one is structurally ineligible as a drop victim (sentinel/rolling headings, or the lone dated section — `payload.file`, `action_required=manual_split_to_archive`). Same hand-off pattern as the two rows above — dedup-check on `payload.file`, fold if open, else mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/notebooks/`, `dedup_key: "notebook_no_valid_drop_candidate_breach:{payload.file}"`. | FOLD (dedup on `payload.file`) OR CHORE (new) |
| `notebook_undroppable_remainder_over_cap_breach` | `notebook-auto-prune-hook` | The preamble plus every currently-present sentinel/rolling `## ` section is, BY ITSELF, already over the line or byte cap (`payload.file`, `payload.undroppable_line_count`/`undroppable_byte_count` vs `payload.cap`/`payload.byte_cap`, `action_required=manual_split_to_archive`). **Same hand-off pattern as the three `*_breach` rows above, with ONE semantic difference that changes what "fixed" means:** this fires on the FIRST loop iteration it is ever reachable on, BEFORE any section has been dropped this invocation (`scripts/agents-flow/notebook-auto-prune.sh` § `emit_undroppable_remainder_over_cap_signal`, gated on `UNDROPPABLE_LINE_COUNT`/`UNDROPPABLE_BYTE_COUNT` exceeding cap), every dated section is retained untouched, and the whole shortfall sits in content the SELECTION plane is permanently forbidden to drop — so this file can NEVER converge by dropping dated sections. The only remediation is a manual split of the rolling/sentinel content to `docs/agent-memory/notebooks/archive/`; do NOT dispatch a "prune harder"/tune-the-heuristic fix, and do NOT read a re-fire as the pruner misbehaving. **Pipeline-A bridge** (same pattern as the `system-issue` row above): the emitter sets `to: claude-manager-helper`, never `po`, but this table's loop is unconditional over `pendingSignals[]`, so the envelope lands here regardless. Dedup-check NON-TERMINAL LANES on `payload.file` first — fold into an existing open split/archive CHORE covering that file (ONE row per file, never one per fire: this type re-fires on every write to an over-cap notebook, so per-fire minting floods the board). If new → mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/notebooks/`, `status: "BACKLOG"`, `dedup_key: "notebook_undroppable_remainder_over_cap_breach:{payload.file}"`. | FOLD (dedup on `payload.file`) OR CHORE (new) |
| `data-coverage-gap` | `dev-team` / `system-auditor` | A named data feed/table has a coverage HOLE — a missing range, distinct from `db_freshness`/`data_stale`'s stale-tail gap. Dedup-check NON-TERMINAL LANES on the named feed/table first. If found → fold. If new → mint `.task_board.backlog[]` FIX, `owner: ops`, `zone` = the owning service named in `payload`/`summary`. | FOLD (dedup) OR FIX (`owner: ops`, new) |
| `deep_dive_result` / `esc-deep-dive-request` | `agent-father` | A deep-dive investigation PO itself requested has returned (`deep_dive_result`), or an escalation is asking PO to dispatch a new one (`esc-deep-dive-request`). For a result: log `"[po] deep-dive {payload.subject} acknowledged"` to notebook, no task creation — the finding lands as its own board row via the deep-dive flow, this is an ack-only receipt. For a request: action per `docs/agents/po/flow/main.md`'s deep-dive dispatch step. | skip (ack, `deep_dive_result`) OR dispatch (`esc-deep-dive-request`) |
| `cowork-fire` | `cowork-team` (dispatcher heartbeat, usually `to=dev-team`) | Routine per-slot spawn telemetry (`classification=FIRE`, `errors: []`, `silent: false`) — NOT dev work by default; this fires ~10x/day across every cowork cadence. Dedup-check first (one open observation per recurring pattern, never one row per fire — matches `context_bloat_breach`'s discipline). Log-and-skip / fold into `pendingObservations[]` for volume tracking on the routine "spawned clean" case. Only escalate to a `.task_board.backlog[]` FIX (`zone: docs/agents/cowork-team/`) when the payload itself shows a genuine anomaly (non-empty `errors[]`, `silent: true`, a missed/failed slot, or a repeat degraded classification on the SAME slot across ≥2 fires). | FOLD (dedup) OR `pendingObservations[]` (routine) OR FIX (genuine anomaly, new) |
| `cowork_defect_confirmation` / `cowork_defect_correction` | `cowork-team` | Self-reported cowork-slot defect envelopes (live 2026-08-25, `to=po`; payload carries `subject`, `defect` (a named row id), `reproductions`, `preferred_fix`, `observed_cost` for confirmations; `headline`/`spec_source`/`true_mechanism`/`quantified` for corrections — a correction refutes or narrows a prior claim, it is NOT a new defect). **FOLD BY DEFAULT:** dedup-check NON-TERMINAL LANES on `payload.defect`/`subject` first (`scripts/po-board-dedup-search.sh`); confirmations attach evidence to the named row, corrections amend it — never re-mint the refuted sub-claims. Mint `.task_board.backlog[]` FIX (`zone` from the named agent/file, per `payload.preferred_fix` when present) ONLY for a genuinely new, uncovered actionable defect. | FOLD (dedup on `defect`/`subject`) OR FIX (new actionable defect) |
| `cowork_tick_closeout` / `cowork_tick_telemetry` | `cowork-team` | Cowork tick closeout / telemetry receipts (`to=po`; payload carries `tick`, `closeout` summary, `standing_counters`, `new_finding_*`, cycle/telemetry counters — live 2026-08-25). Mostly informational: read `new_finding_*`/`standing_counters` for anything genuinely actionable, fold each onto its cited/named row or `pendingObservations[]` (one open observation per recurring pattern, never one per tick). Mint only for a named defect with no covering row. | `pendingObservations[]` (routine) OR FOLD (named finding) OR FIX (new actionable defect) |
| `telemetry` | `cowork-team` / `system-auditor` (tick-telemetry emit family) | Generic tick telemetry envelope (`payload` = usage/run counters). Log-and-skip / `pendingObservations[]` — telemetry is not dev work by default. Only mint when the payload itself flags a genuine anomaly (non-empty `errors[]`, missed slot, degraded classification). | skip / `pendingObservations[]` (routine) OR FIX (genuine anomaly, new) |
| `flow_actuator_fix` | `agents-architect` | A design-complete "one-hop" dispatch signal (`no_new_brief: true` — PO's own prior scope-note on the named `task_id` already said the design is fully specified and a fresh architecture brief is not needed). The signal body itself — not an external doc — carries `payload.task_id`/`target_files[]`/`proposed_patch` (copy-executable jq+orch-apply.sh blocks) for `agent-father` to apply verbatim. Verify the named `task_id` row exists on `.task_board` and is reachable (owner/next_agent resolve to `agent-father`); since `agent-father` is OFF the DRS-ratified allowlist `[architect,ba,pm,po,agents-architect]`, PO cannot just next_agent-flip the row and walk away — fold it into PO's manual-dispatch BATCH (same `DRS-STRANDED-OFF-ALLOWLIST` class as `improvement_approved_md`'s LANE-A path) so the router actually picks it up. Any AC the signal itself flags as out-of-zone (`scripts/`, `apps/` — outside `agent-father`'s `commit_zone`) gets split to a `developer`-owned follow-up row per the signal's own `actions[]` list — never assign that slice to `agent-father`. | manual-dispatch BATCH fold (`agent-father`, in-zone ACs) + split-off `developer` row (out-of-zone ACs, if flagged) |
| `system-issue` / `system_issue` | any infra-facing agent | **Pipeline-A bridge** — **ALIAS NOTE (FIX-TRIAGESIGNALS-SYSTEMISSUE-UNDERSCORE-FORM-UNROUTED, 2026-08-28):** the underscore form `system_issue` is a live emitter literal (system-auditor `tasksMdJanitorSignalWriter.ts`; 112 fires 2026-06→08 vs 109 hyphen — both spellings live concurrently, no precedence). Both spellings route identically here; do not re-derive the emitter-vs-alias question — convergence is owned by FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE. (same tactical-bridge pattern as the `audit-handoff` row at the bottom of this doc) — this table's own loop is unconditional over `pendingSignals[]`, so a `system-issue` envelope can land here even though the type also has a Pipeline-B rule below; that Pipeline-B rule cannot fire for a Pipeline-A occurrence (disjoint namespaces, `feedback_devteam_drain_step_can_itself_turn_signal_type_guard_red`'s sibling gap). Apply the IDENTICAL disposition as the Pipeline-B `system-issue` row below: dedup-check first; verify the premise before acting (cross-check the cited commit/CI-run/service-state); if valid and untracked → mint FIX/CLEAN with `zone` from `payload`/named service, or route `owner: ops` if the fix is a live infra action; if the premise needs correcting → ACK-WITH-CORRECTION. `severity` HIGH/CRITICAL → also `send_telegram(channel="bug")`. | FIX/CLEAN (zone from payload) OR ops route (live infra action) OR ACK-WITH-CORRECTION (same as Pipeline-B rule) |
| `sprint_registry_unresolved_journal_ids` | `decision-journal-archive` (`scripts/agents-flow/decision-journal-archive.sh` AC-4 signal) | `payload.ids[]` names sprint ids a decision-journal file references that resolve in NEITHER `active_sprints[]` NOR any closed-sprint source (hot `closed_sprints[]`, cold archive `closed_sprints[]`/`closed_sprint_goals`/`done_tasks[].sprint`) — `action_required=triage_derived_ids_with_no_orch_record`. The emitting script already dedups its own signal file by a hash of the id-set (`sprint-registry-unresolved-ids-{hash}-*.json`) — a repeat file with the SAME hash is a stale re-read, not a new occurrence; skip. If new: for each id, check whether it is a known synthetic/test-fixture id (e.g. the script's own test harness placeholders) → log-and-skip, no mint. If genuinely orphaned → mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/decisions/`, citing the unresolved id(s) for manual sprint-registry reconciliation. | skip (stale hash / test-fixture ids) OR CHORE (genuine orphan ids, new) |
| `sprint_registry_dangling_ids` | `orch-validate-stage1h` (`scripts/orch-validate.mjs` Stage 1h, line ~862) | `payload.ids[]` names sprint/task ids referenced in ONE plane but unresolvable in ANOTHER (sprint-registry referential-integrity violations); `payload.action_required=reconcile_via_verify_sprint_registry_referential_integrity_script`. This is the emitter's EXACT literal, character-for-character — the earlier CANCELLED row (FIX-SIGNAL-TYPE-ROUTING-GAP-sprint-registry-unresolved-journal-ids) was closed on a false green because that grep matched the ROW's name (`sprint_registry_unresolved_journal_ids`), not the EMITTER's literal; do not repeat that mistake. Dedup on the id-set first (same discipline as the sibling `sprint_registry_unresolved_journal_ids` row directly above — the emitting script dedups its own signal file by a hash of the id-set; a repeat file with the SAME hash is a stale re-read, skip). If new: reconcile via the named `verify_sprint_registry_referential_integrity` script when the ids are legitimately re-resolvable, else mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/decisions/`, citing the dangling id(s) — same disposition as the sibling row. | skip (stale hash / resolvable ids) OR CHORE (genuine dangling ids, new) |
| `notebook_prune_dropped_newest_dated_section` | `notebook-auto-prune-hook` | Same family as `notebook_unparseable_breach`/`notebook_single_section_overage_breach`/`notebook_no_valid_drop_candidate_breach` above, HIGH-priority variant: the pruner's drop-oldest heuristic found every dated section tied at the SAME max timestamp key, so ranking one as "oldest" is unsupported by within-file evidence and may have dropped the NEWEST real content instead (`payload.file`, `payload.heading`, `payload.line`). Non-blocking — the prune already proceeded; this is a possible-data-loss notice, not a live block. Dedup-check NON-TERMINAL LANES on `payload.file` first — fold into an existing open repair CHORE. If new → mint `.task_board.backlog[]` CHORE, `owner: claude-manager-helper`, `zone: docs/agent-memory/notebooks/`, priority HIGH (content-loss risk, not routine hygiene), `dedup_key: "notebook_prune_dropped_newest_dated_section:{payload.file}"`. | FOLD (dedup on `payload.file`) OR CHORE (new, priority HIGH) |
| `notebook_tiebreak_direction_defaulted` | `notebook-auto-prune-hook` | Informational-only variant (`action_required=informational_no_action_needed`) confirming the documented tiebreak default (`newest_first`) fired correctly on a file where every retained section lacks a parseable timestamp — NOT a defect signal (`FIX-NOTEBOOK-AUTOPRUNE-DIRECTION-UNRESOLVABLE-ZERO-TS-NOTEBOOKS` already covers the underlying design). Log to `pendingObservations[]` for volume tracking, no mint. Escalate to a CHORE (dedup-check `payload.file` first, same shape as the row above) ONLY if the SAME file fires this repeatedly with genuinely DIFFERENT tied-section content each time — a possible sign the default is silently discarding distinct sections rather than one stable rolling set. | `pendingObservations[]` (routine) OR CHORE (same file, recurring with differing content) |
| `auto-push-abort` | `fleet-worktree-push` (`scripts/fleet-worktree-push.sh` `emit_abort_signal()`, line 93) | **DISCRIMINATE ON `payload.reason` — the six emit sites are not one failure.** `push-fail` / `behind-set-code`: FIRST re-measure the premise, do not trust the envelope's own `ahead` count — it is a snapshot from the aborted run and routinely stale by the time PO reads it (`git rev-list --count origin/main..HEAD` and `git fetch origin main && git rev-list --count HEAD..origin/main`). Premise resolved (backlog drained, `behind=0`) → ACK-and-drop, mint nothing, log in notebook. Still diverging → mint FIX `zone: scripts/`, `owner: developer`, naming the gate that is rejecting the push (read the abort's `detail`; a red pre-push gate strands the WHOLE fleet, `feedback_red_prepush_strands_fleet`). `tsc-red` / `merge-conflict` / `merge-continue-fail`: a real break on `main` — mint FIX P0 `owner: developer`, zone from the `detail`'s own file list. `tsc-gate-timeout-skipped`: informational, the push already proceeded and the commits were individually gated — ACK-and-drop unless it recurs 3x (then mint a CHORE against the worktree tsc gate). | Per `payload.reason` as above — ACK-and-drop for a resolved premise or a lone timeout-skip; otherwise FIX `zone: scripts/` (push/backstop mechanics) or FIX P0 (tsc/merge break), `owner: developer`. `dedup_key = "auto-push-abort:{payload.reason}"` — the backstop re-fires every 15min while the condition holds, so 3+ identical envelopes are ONE incident, never N tasks. |
| `flow-defect` / `flow_defect` | `router` | Structural/mechanism defect a router session self-detects in an agent flow doc during its own tick (e.g. an unwired commit pathspec, a live stale-lookup collision). **Both spellings observed live — treat as aliases, same disposition** (same duplicate-spelling class as the `system_issue`/`system-issue` pair documented in the Pipeline-B section below): the guard's own self-filed `FIX-SIGNAL-TYPE-ROUTING-GAP-flow-defect` row (2026-08-25T05:13:21Z) caught the underscore form `flow_defect` in this exact table's own inbox; the hyphen form `flow-defect` is what the live 2026-08-26 router envelopes actually carry (verified in `docs/signals/processed/analysis-briefs-no-owning-committer-20260826T014430Z.json` + `cyclesnapshot-priorday-decoy-observed-20260826T021500Z.json`, both literal `"type": "flow-defect"`). Read `payload.title`/`payload.summary`/`payload.evidence` (flat fields on the Pipeline-A envelope, not a separate file). Dedup-check NON-TERMINAL LANES on the named subject/file first (`scripts/po-board-dedup-search.sh`). If a covering row exists → FOLD (append occurrence/evidence, e.g. `action_requested: FOLD` payloads that already name the target row). If new → mint `.task_board.backlog[]` FIX per `repair_task_request`'s canonical shape above, `zone` inferred from the payload's named agent/file (e.g. `docs/agents/{agent}/flow/` or the named service), `owner`/`next_agent` per `payload.suggested_owner` if present, else `developer`, `priority` from `payload.priority` (router default `medium`). | FOLD (dedup, incl. explicit `action_requested: FOLD` payloads) OR FIX (new, zone from payload) |
| `detector_defect` / `audit_finding` / `preserved_bug_no_tracking_row` / `tooling_defect` | any (`router`/`system-auditor`/`qa`/`dev-team`) | **Pipeline-A bridge** (same tactical-bridge pattern as `system-issue` above and `audit-handoff` below) — each of these 4 types has a live Pipeline-B `to==po` disposition rule in § "4 more live types (added 2026-08-26...)" below; that rule structurally cannot fire for a Pipeline-A occurrence (disjoint namespaces). This table's own loop is unconditional over `pendingSignals[]`, so any of these 4 can land here regardless of the envelope's own `to` field. **Confirmed live 2026-08-26T02:34Z:** `detector_defect` from `router`, `to=architect` (`sha…d7a5e915…`, dev_team_idle_chain envelope), reached this table via dev-team's own drain because the type had no row in `drain-signals.md` § 0a-3 and fell to that table's own "any other → PO Step 0-SIG" annotation-only fallback — the envelope itself was never lost (§0a-3's routing column is informational-only per that doc's own text, "PO's triage-signals.md is the authoritative dispatch handler"), only unannotated. Apply the IDENTICAL disposition as the matching Pipeline-B row below, using the envelope's own `to`/payload to pick the owner of any resulting FIX/fold — do NOT hardcode `owner: po` when the envelope names a different owner (e.g. `architect` for the live example above). | Same disposition as the matching Pipeline-B row (dedup/fold/FIX per type), owner from the envelope's own `to`/payload, not hardcoded `po` |
| `auditor_cycle_missing` | `system-auditor` | **Pipeline-A bridge** — the Pipeline-B rule below ("5 more live types") cannot fire for a Pipeline-A occurrence (disjoint namespaces); apply the IDENTICAL disposition: dedup-check on `dedup_key` (`auditor-cycle-missing:{tier}:{window}`, already stamped) FIRST — multiple ticks on the SAME tier+window MUST fold into ONE row (consolidation, not per-tick mint); a new window/tier → mint FIX, `zone: apps/mcp-server/`, priority from `severity`. | FOLD (same tier+window) OR FIX (new tier/window) |
| `cron_fire_gap` | `system-auditor` | **Pipeline-A bridge** — Pipeline-B rule below; same disposition: dedup-check on `dedup_key` (`auditor-a29-fire-gap:{job}`, already stamped)/job name first; new job → mint FIX, `zone: apps/mcp-server/` (scheduler), `severity=CRITICAL` → priority high. | FOLD (`dedup_key`) OR FIX (new job) |
| `db_integrity_breach` | `system-auditor` | **Pipeline-A bridge** — Pipeline-B rule below; same disposition: FIRST check whether it is a CONSEQUENCE of an already-open infra incident in the same window (OOM/restart/outage) → fold-with-re-verify, note `"RE-VERIFY next tick — if still bad after recovery, escalate as genuine gap"`, no new mint yet; if no covering incident, or the re-verify tick still shows the anomaly → mint FIX (`severity=CRITICAL` → priority high), `zone: apps/mcp-server/` (or the DB owner named in `summary`). | FOLD+RE-VERIFY (known incident) OR FIX (uncovered / still bad on re-verify) |
| `narrative_contradiction` | `digest-predict` | **Pipeline-A bridge** — Pipeline-B rule below; same disposition: dedup-check on `(payload.ticker, payload.tool, payload.cycle)` FIRST, fold repeats; if `payload.returned_value` names a pipeline-lag reason (e.g. "not yet extracted", "check back shortly") → transient/benign, `pendingObservations[]`, no mint; if the claim has no such basis (genuine fabrication) → mint FIX targeting digest-predict's CCATO gate, `zone: apps/mcp-server/`. | FOLD (dedup) OR pendingObservations (transient/pipeline-lag) OR FIX (genuine gate violation) |
| `signal_feedback` | `system-auditor` (`scripts/emit-audit-signal.sh`, `post_agent_signal` `signal_type=signal_feedback`) | **Pipeline-A bridge** — Pipeline-B rule below; same disposition: dedup-check first; found → FOLD (append this occurrence as a disposition note, no new mint); not found AND `severity` CRITICAL/HIGH → mint `.task_board.backlog[]` FIX (shape per `repair_task_request` above), `zone` inferred from the service named in `summary`; WARN/MED/INFO with no corroborating prior fire → do NOT mint on a single tick, log + `pendingObservations[]`, re-check next tick for recurrence. | FOLD (dedup) OR FIX (CRITICAL/HIGH, new) OR pendingObservations (WARN, single-fire) |
| `auditor_probe_anomaly` | `system-auditor` | A detector-probe anomaly (live example: "Tier-1 heartbeat frozen: probe threshold wedge", WARN, `to=architect` — lands here anyway because this loop is unconditional over `pendingSignals[]`). Read `summary` + `audit_cycle_tag`; **verify the premise at source BEFORE acting** (the live example was correct-by-contract — the frozen heartbeat value was a deliberate threshold floor, not a bug, and the envelope was rehoused to existing rows). Dedup-check on `dedup_key` first; fold if covered. If a genuine probe defect → mint FIX targeting the auditor probe (`zone: apps/mcp-server/`), priority from `severity`. | FOLD (dedup) OR FIX (genuine probe defect, new) |
| `agent-output-contract-violation` | `cowork-team` (or any agent) | An agent's reported output contract was violated — it claimed success its own persistence/mutex layers cannot support (live example: chef-morning returned DONE/PIPELINE:complete/QUALITY:full after a 10-day dispatch gap; four reported outcomes falsified by direct filesystem + lock-store measurement — "the dish content itself may be fine — the persistence and mutex layers are not"). Read `payload.summary`/`evidence`; dedup-check NON-TERMINAL LANES on the named agent + failure mode first (`scripts/po-board-dedup-search.sh`); if covered → fold. If new → mint FIX targeting the reporting agent's flow/persistence layer (`zone: docs/agents/{agent}/` for a flow defect, else the owning service), priority from `severity`. | FOLD (dedup) OR FIX (new, targets the reporting agent's flow/persistence) |
| any unknown `type` | any | **ROUTE-BY-`to` FALLBACK** (FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE, 2026-08-22 — replaces the prior silent-drop catch-all, which measured 206/322 = 64% of live signals landing here with no durable artifact, no owner notification, nothing on the board): **DEDUP GUARD FIRST (mandatory, no exceptions):** build `dedup_key = "{type}:{payload.file, or another stable payload subject/path if present, or summary first 60 chars}"` and dedup-check the NON-TERMINAL LANES (`backlog[]`+`ready[]`+`in_progress[]`+`review[]`+`qa[]`) plus open `signal_queue.rows[]` for an existing entry whose `dedup_key`/title already contains it — a permanently-firing detector (e.g. `context_bloat_breach` ~90x/week) MUST yield ONE open artifact per subject, never one per fire. If found → fold (append this occurrence, bump timestamp/count), mark signal DONE, do NOT re-mint. If not found: read the signal's own `to` field. **`to==po` BRANCH (AC-3 of FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED, 2026-08-26 — closes a circular no-op):** the normal "emit a durable artifact addressed to `to`" step below is a SELF-LOOP when `to` is already `po` — it hands the unrouted signal back to the exact step that failed to route it, so it is silently re-delivered next tick forever with no owner ever assigned and no board trace. When `to=="po"`, do NOT create a `signal_queue.rows[]` row addressed back to `po`. Instead mint a `.task_board.backlog[]` CHORE/FIX row DIRECTLY, same shape as `repair_task_request` above, `owner`/`next_agent` inferred from the signal's OWN content (the target service/agent named in `summary`/`payload`), defaulting to `owner: po` only if no better owner is inferable, `origin_signal_id` stamped. This is the same dedup-then-mint mechanism the fallback already uses below, applied one step earlier so a `to=po` signal never round-trips through itself. **ELSE (`to` present, resolves to a known agent id, and is NOT `po`)** → emit a durable artifact addressed to `to` — a `signal_queue.rows[]` row (`.claude/skills/signal-dashboard/SKILL.md` § WRITE: `to: {to}`, `type: {type}`, `summary` from the signal, `dedup_key` as built above) for informational/low-volume types, or a `.task_board.backlog[]` CHORE/FIX row (`owner: {to}`, canonical shape per `repair_task_request` above, `dedup_key` field stamped) when the signal is clearly actionable work. Log `"[po] {type} from {from} — no table row, routed via to-fallback to {to}"`. **ONLY IF `to` is absent or does not resolve to a known agent id** → fall back to log-and-skip: `"[po] Unknown signal type {type} from {from} — payload retained, {to}=unresolvable"` + `send_telegram(channel="work")` so the unresolvable-owner gap gets fixed rather than silently repeated. Adding an explicit table row per newly-observed high-volume type is still preferred once volume/pattern is clear (see the 6 rows immediately above, all promoted out of this exact fallback the same way) — this fallback exists so a brand-new emitter type is never silently lost in the window before its own row gets written. | FOLD (dedup) OR `to==po` → direct backlog mint (no self-loop) OR durable artifact routed to declared `to` (signal_queue row or backlog CHORE/FIX, `to!=po`) OR log-and-skip (only if `to` unresolvable) |

**`origin_signal_id` (additive — generalizes across signal-born task rows):** any row above that mints a new `.task_board` entry directly from an incoming signal (`repair_task_request`, `zone_missing_tier3`, `ci_red`) SHOULD stamp the same `origin_signal_id` field on the created entry, using the resolution rule documented in the `repair_task_request` row. This is the back-reference `docs/agents/pm/flow/task-archive.md` reads on `DONE_VERIFIED` to flip the originating `signal_queue` row `READ→RESOLVED` (`.claude/skills/signal-dashboard/SKILL.md` § CLOSE). Does not change any existing routing/dedup logic — additive field only.

ACK each processed signal by writing the result into your notebook entry (no file ACK — the signal is already in `signals_processed` DB).

**Durable-inbox CLEAR (executable — FIX-TRIAGE-INBOX-CLEAR-OWNERSHIP-PO-SELF-READ, 2026-08-22):** run once, as the LAST action of Pipeline-A processing, after every entry in the `pendingSignals[]` read at the top of this step has been routed (including the "any unknown type" catch-all row — routed, not necessarily task-minted). Subtracts by `envelope_id`, never a blind `= []` — defensive against an entry landing between the read at the top of this step and this write. Skip entirely if `pendingSignals` was empty at the top of this step (nothing to clear). Duplicate-safe, not loss-safe (deliberate asymmetry, matches the prior dev-team-owned design this supersedes): if this write never lands (PO crashes mid-triage before reaching here), the SAME envelope_ids simply get re-delivered and re-triaged next invocation — a low-cost duplicate this doc's own per-signal dedup guards above (e.g. `zone_missing_tier3` dedup on `taskId`, `ci_red` file-scoped dedup) already absorb, never a silent loss.
```bash
consumed_ids=$(printf '%s' "$pendingSignals" | jq -c '[.[].envelope_id]')
consumed_ids_csv=$(printf '%s' "$consumed_ids" | jq -r 'join(",")')
NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ)
jq --argjson ids "$consumed_ids" --arg now "$NOW" \
  '.dev_team_idle_chain.pending_triage_inbox |= map(select(.envelope_id as $i | ($ids|index($i))|not))
   | .dev_team_idle_chain._updated_at = $now | .dev_team_idle_chain._updated_by = "po"' \
  docs/data/orch/orch-state.json | ORCH_APPLY_DECLARED_INBOX_TRIAGED="$consumed_ids_csv" bash "$PROJECT_ROOT/scripts/orch-apply.sh" || true
```
`ORCH_APPLY_DECLARED_INBOX_TRIAGED` (per `scripts/orch-conservation-check.mjs`'s inbox row-identity guard, `FIX-ORCHAPPLY-CONSERVATION-FLOOR-BLOCKS-SANCTIONED-PO-INBOX-DRAIN-CLEAR`) is unchanged by this ownership move — same declaration mechanism, same never-bypassable guard, only the caller identity changed (`dev-team` → `po`, reflected in `_updated_by` above).

## Live `.signal_queue.rows[]` inbox (Pipeline B, `to==po`)

Read by `main.md`'s "Pre-check — Signal dashboard" step. Row shape: `{id, ts, from, to, type, summary, severity, status, payload_ref, disposition?, triaged_at?, triaged_by?}` — `summary` is usually the full finding (≤120 chars), `payload_ref` is `null` unless the finding is too large to inline. Re-derived live 2026-08-01 from `.signal_queue.rows[]` (hot) — NOT copied from `.claude/skills/signal-dashboard/SKILL.md`'s § Signal types table, which only names 6 types and is known-incomplete. **Status enum admits `"triaged"` and `"RETRACTED"` in addition to the SKILL's documented `NEW/READ/RESOLVED/SUPERSEDED`** — see `.claude/skills/signal-dashboard/SKILL.md` for the definitions; write `triaged_at`/`triaged_by`/`disposition` on every row you close via this section (matches the live convention already in use on 127/132 hot `to=po` rows).

**Cold-archive cross-check (`docs/data/orch/archive/2026-06.json`/`2026-07.json`/`2026-08.json` `.signal_rows[]`, `to=="po"`):** every currently-recurring type in the two most recent cold months (`system_issue`/`system-issue`, `signal_feedback`, `data_stale`, `microservice_degraded`, `db_integrity_breach`, `repair_task_request`, `config_drift`, `orch-health-finding`, `methodology-flag`, `bug-escalation`, `brief_complete`) is already covered — either here or in the Pipeline-A table above.

**CORRECTION (2026-08-23 — architect brief `docs/architecture-briefs/2026-08-23-signal-type-registry-open-namespace-vs-closed-allowlist.md` §1, falsifying this row's own prior claim):** the text formerly here asserted `system_issue`/`system-issue` were "≤1-2 fires each" and that the underscore form "predates" the live hyphen form as a "historical artifact, not a live routing gap." Both claims are false — independently re-verified via `grep -c` across `docs/data/orch/archive/2026-0{6,7,8}.json` + the hot file:

| form | 2026-06 | 2026-07 | 2026-08 | total |
|---|---|---|---|---|
| `system_issue` (underscore) | 20 | 86 | 6 | 112 |
| `system-issue` (hyphen) | 0 | 51 | 57 | 109 |

Both spellings are concurrently live through August (underscore fired 6 times, hyphen 57 times, *this* month) — there is no precedence, and at 109-112 fires each these are the two largest type classes in the whole ~100+-type namespace, not stragglers. **Do not treat the table above as current** by the time you read this — a frozen "rare, skip it" claim is exactly the defect being corrected here, and hardcoding today's counts as a new frozen claim would be the same defect class one measurement later. For the live type distribution, run `bash scripts/audits/guard-signal-type-coverage.sh --check` against `docs/data/orch/orch-state.json` (extended by `TASK-DEV-MCP-SIGNAL-TYPE-REGISTRY` to derive a full cross-pipeline registry) rather than trusting any static count in this doc, including the one above.

**Dedup discipline for every row below:** unless stated otherwise, "dedup-check" means scan the NON-TERMINAL LANES — `.task_board.backlog[]` + `.ready[]` + `.in_progress[]` + `.review[]` + `.qa[]` — the same lane set used by `repair_task_request`/`ci_red` above (status-token enums structurally miss BACKLOG/READY rows — never dedup on a status enum).

**Dedup lookup tool (use this, not `grep`) → `scripts/po-board-dedup-search.sh <regex> [--all-lanes]`** (2026-08-24). `grep <id> docs/data/orch/orch-state.json` and `jq '.. | select(.id == …)'` BOTH false-positive on this file: telemetry/finding objects elsewhere in it share the task-row shape `{id,title,status,…}`, so a hit proves nothing about board membership — 4 recorded occurrences, 2 of them back-to-back on 2026-08-24, cost 2 suppressed escalations (`feedback_auditor_dedup_citation_resolves_to_nonexistent_row_id`). The script resolves each match's jq PATH, **requires** a `task_board.<lane>[i]` prefix, and prints lane + index + id + `dedup_key` + title so the second half of the check — *is this row ABOUT the subject, or is the filename mentioned incidentally* — is forced rather than assumed. Defaults to the NON-TERMINAL lane set defined immediately above.

The 5 highest-volume live types (covers 114/132 = 86% of the current hot `to=po` inbox):

| `type` | From | Action | Routing |
|---|---|---|---|
| `signal_feedback` | `system-auditor` | Per-check FAIL/WARN feedback from a Tier-N audit (`"{check_id} {FAIL/WARN}: {finding}"` in `summary`; `payload_ref` usually `null`). Dedup-check first. If found → FOLD: append this occurrence as a disposition note on the existing row, no new mint. If not found AND `severity` CRITICAL/HIGH → mint `.task_board.backlog[]` FIX (shape per `repair_task_request` above), `zone` inferred from the service named in `summary`. If WARN/MED/INFO with no corroborating prior fire → do NOT mint on a single tick (`feedback_auditor_mcpserver_a21_a30_memory_fp_reemit_churn`); log + `pendingObservations[]`, re-check next tick for recurrence before minting. | FOLD (dedup) OR FIX (CRITICAL/HIGH, new) OR pendingObservations (WARN, single-fire) |
| `microservice_degraded` | `system-auditor` | Named service failing its health probe (`summary` = `"{service} {symptom}"`). Dedup-check for an existing recurring-degradation FIX row for that service first (live naming convention: `*-AVAIL-*`/`*-HEALTH-*`). If found → FOLD, append corroboration to the row. If new AND (`severity=CRITICAL` OR the service fired on ≥2 consecutive ticks — persistent, not a single blip) → mint FIX, `zone: apps/{service}/`. Single WARN blip with no history → `pendingObservations[]`, no mint. | FOLD OR FIX (persistent/CRITICAL) OR pendingObservations (single WARN) |
| `data_stale` | `dev-team` | A named pipeline/service stopped ingesting (`summary` names the feed + last-good date). Dedup-check for an existing freshness-gap row on the SAME feed FIRST — live pattern is CONSOLIDATION (multiple stale-feed signals fold into ONE incident, never split; precedent: FFLOW-STALE-0723). If found → fold `origin_signal_id` onto the existing row, no new mint. If genuinely new: `severity` CRITICAL/HIGH → mint FIX `zone: apps/{service}/` (+ a companion ops-owned recon task if root cause looks like a live infra issue, not code); WARN → `pendingObservations[]`, re-verify next tick before minting. | FOLD OR FIX (+ ops companion) OR pendingObservations (WARN) |
| `system-issue` / `system_issue` | any infra-facing agent (code-janitor, cowork-team, dev-team, etc.) | **AC-3 first-class rule — this is the canonical infra-escalation type** (`.claude/skills/signal-dashboard/SKILL.md` § Signal types names `po` as receiver). **ALIAS NOTE (FIX-TRIAGESIGNALS-SYSTEMISSUE-UNDERSCORE-FORM-UNROUTED, 2026-08-28):** `system_issue` (underscore) is a live emitter literal with 112 fires 2026-06→08 vs 109 hyphen — both spellings route identically here; convergence is owned by FIX-SIGNALTYPE-OPEN-NAMESPACE-VS-CLOSED-ALLOWLIST-5TH-INSTANCE, do not re-derive. Read inline `summary` + `payload_ref` when present (`docs/signals/processed/*.json`). Dedup-check first. **Verify the premise before acting** — live example: a rebuild request assumed a commit was CI-clean when it had actually turned `size-lint` RED; cross-check the cited evidence (commit SHA / CI run / service state) before dispositioning. If the ask is valid and untracked → mint FIX/CLEAN with `zone` taken from `payload`/named service; if the fix is a live infra action (restart/redeploy/SSH) rather than a code change, route `owner: ops` instead of the `po` default. If valid but the premise needs correcting → ACK-WITH-CORRECTION (log the corrected premise, still action the valid part). `severity` HIGH/CRITICAL → also `send_telegram(channel="bug")`. | FIX/CLEAN (zone from payload) OR ops route (live infra action) OR ACK-WITH-CORRECTION |
| `db_integrity_breach` | `system-auditor` | DB row-count/consistency anomaly (e.g. `"market_messages count=0 in 3h"`). FIRST check whether it is a CONSEQUENCE of an already-open infra incident in the same window (OOM/restart/outage) — live pattern is fold-with-re-verify, not blind mint. If a covering incident exists and the window overlaps → fold, note `"RE-VERIFY next tick — if still bad after recovery, escalate as genuine gap"`, no new mint yet. If no covering incident, or the re-verify tick still shows the anomaly → mint FIX (`severity=CRITICAL` → priority high), `zone: apps/mcp-server/` (or the DB owner named in `summary`). | FOLD+RE-VERIFY (known incident) OR FIX (uncovered / still bad on re-verify) |

**Remaining 17 lower-traffic live types** (routing-gap, orch-health-finding, tool_contract_gap, bctc-data-quality-anomaly, config_drift, context_bloat_breach, methodology-flag, notebook_unparseable_breach, ops_followup_request, recurring_churn_escalation, triage-persist-request, bctc_image_fetch_degraded, container_capacity_pressure, launchd_tracking_gap, memory_loop_breach, memory_pressure_transient, observability_defect — the last five added 2026-08-28 by FIX-TRIAGESIGNALS-PIPELINEA-UNROUTED-RECURRINGBUG-AND-SPRINTREGISTRY-DANGLING-IDS, system-auditor WARN/capacity detectors, ≤1-4 fires each) → lazy-load sibling `docs/agents/po/flow/triage-signals-longtail.md` (load ONLY when a signal of one of these types is in the inbox — keeps this hot-path doc from absorbing 17 rows inline).

**5 more live types (added 2026-08-22, FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE — measured 10/10 = 100% of the hot `to=po` inbox on that date, none of the 5 in the guard's `$routed` allowlist below, from `system-auditor`/`digest-predict` detectors):**

| `type` | From | Action | Routing |
|---|---|---|---|
| `auditor_cycle_loss` | `system-auditor` | An audit-cycle tick died mid-run (orphaned marker, no clean completion) — `summary` names the tick, `dedup_key` = `auditor-cycle-loss:{tick}` (already stamped on the row by the detector). Dedup-check on `dedup_key` first — if an open row already cites the same tick → fold. If new → mint `.task_board.backlog[]` FIX, `zone: apps/mcp-server/` (auditor runner), priority from `severity`. | FOLD (`dedup_key`) OR FIX (new) |
| `auditor_cycle_missing` | `system-auditor` | A Tier-N audit cycle has no completion evidence within N× its own cadence — `dedup_key` = `auditor-cycle-missing:{tier}:{window}` (already stamped). Multiple ticks re-firing on the SAME tier+window MUST fold into one row (consolidation, not per-tick mint) — dedup-check on `dedup_key`. If a new window/tier → mint FIX, `zone: apps/mcp-server/`, priority from `severity`. | FOLD (same tier+window) OR FIX (new tier/window) |
| `cron_fire_gap` | `system-auditor` | A named scheduled job is stale beyond its cadence threshold (check A-29 family) — `dedup_key` = `auditor-a29-fire-gap:{job}` (already stamped). Dedup-check on `dedup_key`/job name first. If new → mint FIX, `zone: apps/mcp-server/` (scheduler), `severity=CRITICAL` → priority high. | FOLD (`dedup_key`) OR FIX (new job) |
| `db_freshness` | `system-auditor` | A named DB table/feed has no write activity within its freshness SLA (check C-06 family) — `dedup_key` = `db_freshness:{table}:{check_id}` (already stamped). Same consolidation discipline as the `data_stale` row above — dedup-check on the named feed FIRST, never mint per-tick. If new: `severity` CRITICAL/HIGH → mint FIX `zone: apps/mcp-server/` (+ ops companion if root cause looks like live infra, not code). | FOLD (`dedup_key`/same feed) OR FIX (+ ops companion) |
| `narrative_contradiction` | `digest-predict` | digest-predict's own CCATO gate (Claim-Contradicts-Actual-Tool-Output) caught it asserting an absence a live tool call actually contradicted (`payload.ticker`/`payload.tool`/`payload.cycle`). This class re-fires per-cycle on the SAME ticker while a dependent pipeline (e.g. BCTC extraction) is genuinely pending — dedup-check on `(payload.ticker, payload.tool, payload.cycle)` FIRST, fold repeats. If new: read `payload.returned_value` — if it names a pipeline-lag reason (e.g. "not yet extracted", "check back shortly") → transient/benign, `pendingObservations[]`, no mint. If the claim has no such basis (genuine fabrication) → mint FIX targeting digest-predict's CCATO gate, `zone: apps/mcp-server/`. | FOLD (dedup) OR pendingObservations (transient/pipeline-lag) OR FIX (genuine gate violation) |

**4 more live types (added 2026-08-26, FIX-PO-TRIAGE-SIGNALS-AGENT-FLOW-DEFECT-TYPE-UNROUTED — measured live 2026-08-26: 6 rows total, all `status=READ` i.e. already silently swallowed by the any-unknown-type fallback before this fix: `audit_finding` 1, `detector_defect` 2, `preserved_bug_no_tracking_row` 1, `tooling_defect` 2):**

| `type` | From | Action | Routing |
|---|---|---|---|
| `audit_finding` | `system-auditor` | A structural coverage/measurement finding ABOUT the system under audit (contrast `detector_defect` below, which is about a defect IN the detector itself) — e.g. "N data sources lack queryable last_fetch_ts". Dedup-check on `dedup_key` (already stamped by the detector) first. If found → fold. If new: `severity` CRITICAL/HIGH → mint FIX, `zone` inferred from the named service/table in `summary`; WARN/MED/INFO → `pendingObservations[]`, re-check next tick before minting (same single-fire discipline as `signal_feedback` above). | FOLD (dedup) OR FIX (CRITICAL/HIGH, new) OR pendingObservations (WARN, single-fire) |
| `detector_defect` | `system-auditor` / `dev-team` (forwarding a correction) | The detector/gate ITSELF has a logic defect (missing guard, false-positive class, wrong comparison) — NOT a finding about the target system. Dedup-check on `dedup_key` first. If found → fold. If new → mint FIX targeting the detector's own code/flow (`zone: apps/mcp-server/` for a system-auditor check, or the named script's owning zone for a dev-team-forwarded correction), priority from `severity`. A `dev-team`-forwarded correction additionally names which of the ORIGINAL finding's sub-claims still stand vs are refuted — action only the still-standing sub-claims, do not re-mint the refuted ones. | FOLD (dedup) OR FIX (new, targets the detector) |
| `preserved_bug_no_tracking_row` | `qa` | QA found a deliberate, already-disclosed pre-existing bug with ZERO board row tracking it (`payload_ref` names the sprint/decision-journal that disclosed it). Dedup-check NON-TERMINAL LANES on the bug's own description/file first. If found → fold. If new → mint `.task_board.backlog[]` BACKLOG row (shape per `repair_task_request` above), `zone` from the named file/service, priority from `severity`. | FOLD (dedup) OR BACKLOG (new) |
| `tooling_defect` | `dev-team` (router-observed) | A defect in shared fleet tooling itself (a script/wrapper used by multiple agents — e.g. `orch-apply.sh`, `dev-team-tick-preflight.sh`), NOT a defect in any one agent's flow. Dedup-check on `dedup_key` first (already stamped, names the tool + defect class). If found → fold. If new → mint FIX, `zone` from the named tool's own path in `summary` (usually `scripts/`), priority from `severity` (CRITICAL → P0). | FOLD (dedup) OR FIX (new, zone = tool's own path) |

**Tactical bridge — Pipeline-B `audit-handoff` (2026-08-23, FIX-PO-TRIAGE-SIGNALS-TABLE-MATCHES-ZERO-LIVE-SIGNAL-TYPES Part 3, architect brief `docs/architecture-briefs/2026-08-23-signal-type-registry-open-namespace-vs-closed-allowlist.md` §3):** Pipeline-A already routes `audit-handoff` (parent table above, keyed `from=tran-ngoc-bau`, dispositioned by Step 0-TNB) — but that rule structurally cannot fire for a Pipeline-B row, since it lives in a different table scoped to a different inbox. The row that tripped CI red, `tra-20260822T203234`, arrived on `.signal_queue.rows[]` (Pipeline B), so it fell through unrouted despite Pipeline-A's rule existing. This row is the missing Pipeline-B counterpart — a tactical bridge to unblock CI now, not a substitute for `TASK-DEV-MCP-SIGNAL-TYPE-REGISTRY`'s structural fix (a registry that checks each pipeline's live inbox against only its own pipeline's tagged rules, closing this class of split-table blind spot generally):

| `type` | From | Action | Routing |
|---|---|---|---|
| `audit-handoff` | `tran-ngoc-bau` | Same disposition as the Pipeline-A rule above: already covered by Step 0-TNB (reads `docs/handoffs/tnb-audit-latest.md`). Triage `tra-20260822T203234` (and any future open `audit-handoff` row landing on Pipeline B) through that step normally — do NOT close it as a side effect of this rule shipping. | findings → FIX / SPRINT-* (same as Pipeline-A) |

### Regression verifier — signal-type coverage guard

**Wired in, not just written down (FIX-SIGNAL-ROUTING-ROWS-COVERAGE-GAP-DEEPDIVE, 2026-08-22):** this guard previously existed ONLY as a bash function embedded here — wired into NO CI job and NO cron, so nothing ever executed it. Its last recorded PASS was 2026-08-01; by 2026-08-22 the live `to==po` type namespace had fully rotated underneath it (5 new detector types, none in the then-hardcoded `$routed` array this function embedded, 10/10 hot rows unrouted) and that went undetected for 3 weeks because the "guard" never actually ran. It is now the canonical script `scripts/audits/guard-signal-type-coverage.sh`, wired into `.github/workflows/ci.yml` (`signal-type-coverage-guard` job — runs on every push/PR, since `docs/data/orch/orch-state.json` is git-tracked). Run it manually any time:

```bash
bash scripts/audits/guard-signal-type-coverage.sh --check
```

The script does NOT hand-maintain a duplicate `$routed` array (that copy-paste-and-forget-to-sync array is exactly what let 5 new live types go unrouted for 3 weeks with a stale "PASS" still on record) — it PARSES the `type` column directly out of this table and its lazy-load sibling `triage-signals-longtail.md`. Adding a table row to either doc is now sufficient by itself to extend guard coverage; there is no second array to remember to edit.

Verified live 2026-08-22 (post-fix, BEFORE/AFTER both reproduced): running the script against the unmodified doc first reproduced the live FAIL (`unrouted to=po types: ["auditor_cycle_loss","auditor_cycle_missing","cron_fire_gap","db_freshness","narrative_contradiction"]`, matching PO's 10/10 measurement exactly); after the 5 rows above were added, the SAME script against the fixed doc → PASS, all live `to=po` types routed. Synthetic negative control unchanged in spirit: a fixture row `{"to":"po","type":"zz-synthetic-unrouted-test"}` still fails loudly, naming the offending type — confirms the guard detects drift rather than vacuously passing.

## Regression verifier — backlog[] mint status token

Every row above that mints into `.task_board.backlog[]` (`zone_missing_tier3`, `repair_task_request`, `ci_red`) MUST write `status: "BACKLOG"` — `apps/mcp-server/src/infrastructure/orchStateSchema.ts`'s `LANE_ALLOWED_STATUSES.backlog` only permits `{BACKLOG, BLOCKED}` (Stage 1b lane-coherence, hard fail in `scripts/orch-validate.mjs`). Minting the un-canonical TODO token there aborts at `scripts/orch-apply.sh` on first attempt — live file untouched, but the write wastes a round-trip (FIX-PO-TRIAGE-SIGNALS-CIRED-TEMPLATE-STATUS-TODO-REJECTED-BY-VALIDATOR). Same drift class was independently found and fixed in `docs/agents/po/flow/channel-audit.md`'s generic bug/correction template and `docs/agents/po/flow/sprint-kickoff.md`'s BA task template. `scripts/audits/po-triage-mint-backlog-status-lane-coherence-verify.sh` replays every fixed template's exact mint shape against the real `scripts/orch-apply.sh` on a throwaway fixture: BEFORE (the un-canonical TODO token) reproduces the live Stage 1b abort, AFTER (`status: "BACKLOG"`) passes and lands. Touches no live file.

If `pendingSignals[]` is empty, log `"[po] No pending signals"` and proceed to Step 0 Channel Audit.
