{
  "from": "cowork-team",
  "to": "po",
  "type": "coverage-gap-liveverify",
  "payload": {
    "subject": "Market-hours cowork INTELLIGENCE layer is DARK — dispatcher market slots disabled AND zero RemoteTriggers exist (documented stopgap falsified)",
    "severity": "HIGH (scoped — server-side speed/price alerts unaffected; gap is the cowork intelligence half of the Alert Split)",
    "detected_tick": "20260526T020342Z (nominal 02:00Z, Tuesday, market OPEN)",
    "finding": [
      "Dispatcher market-hours slots are enabled:false in docs/data/cowork-schedule.json: news-scout-market (*/15 2-8 1-5), market-watcher-market (*/15 2-8 1-5), alert-commander-market (*/15 2-8 1-5), market-watcher-prepost (*/30 * * 1-5). matchSlots() filters on (sl.enabled && !sl._disabled_by) so the dispatcher correctly skips them every tick.",
      "Their _disabled_by reason 'API_MIN_INTERVAL — cowork master dispatcher owns sub-hourly firing' is STALE and self-contradicted: (a) the whole point of the */15 CronCreate dispatcher was to OWN sub-hourly firing and resolve API_MIN_INTERVAL; (b) the schedule's own _notes OQ-1 ANSWERED states 'RemoteTrigger supports full cron range/step syntax (e.g. */15 2-8 * * 1-5)'.",
      "The schedule's phase2_residual note asserts '12 server-side RemoteTriggers from sprint 1957a stopgap still exist on claude.ai' as the live coverage for these slots. LIVE-FALSIFIED: RemoteTrigger action=list returns {\"data\":[],\"has_more\":false} — ZERO triggers exist. The stopgap is empty.",
      "ASYMMETRY: chef-intraday (unified-agent, 13 2-8 1-5) IS enabled and fires hourly during market hours, but its upstream gatherers are all disabled → the intraday chef synthesizes from STALE off-hours data (gatherers run only 0 */4 → 00:00/04:00/08:00Z)."
    ],
    "net_effect": "During market hours 02:00-08:30Z (weekdays) there is NO cron path firing news-scout/market-watcher/alert-commander intraday. No intraday news gathering, no market-watcher anomaly scans, no alert-commander verified-chain danger alerts via cowork. Server-side mcp-server scheduler crons (price/TA/BB stop-loss = the 'speed' half) are unaffected.",
    "relation_to_open_escalation": "This is the flip-side of HOLLOW-RUN-20260525T1206Z. Those slots were kept disabled under 'keep RemoteTriggers, do NOT certify CronCreate path'. But the RemoteTrigger fallback that decision relied on does not exist → the hold left market-hours intelligence with no coverage at all.",
    "evidence_mitigation_now_works": "This session's 00:00Z dispatcher fan-out spawned 3 cowork agents that ran GENUINELY (MCP task_claim probe + anti-hollow-run directive gated the spawn; dispatcher live-caught news-scout's false-CRITICAL — see NEWSFETCH-FALSECRIT-20260526T0012Z). The current flow's Step-4.6 MCP-reachability probe is the AC-6 mitigation the original block wanted.",
    "recommended_po_decision": "Choose one (PO/agents-architect/agent-father own the change — dispatcher will NOT self-edit the schedule): (A) Re-enable the 4 market slots in cowork-schedule.json (flip enabled:true, clear _disabled_by) now that the MCP-probe + anti-hollow-run gate is proven this session — dispatcher would then fire them within ±2min each */15 tick during 02-08Z; (B) recreate the RemoteTriggers if the dual-path parallel-run is still desired; (C) accept the gap explicitly and document it. NOTE: do NOT do BOTH A and B simultaneously (double-publish risk in MARKET channel — the original AC-6 concern).",
    "dispatcher_action_this_tick": "silent-exit (matcher [] is mechanically correct given enabled:false). No spawn. This is a one-time escalation, not per-tick spam."
  },
  "priority": "high",
  "createdAt": "2026-05-26T02:04:00Z"
}
