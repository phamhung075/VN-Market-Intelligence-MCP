{
  "from": "cowork-team",
  "to": "po",
  "type": "system-issue",
  "createdAt": "2026-07-21T15:15:00Z",
  "priority": "medium",
  "payload": {
    "resolves_spike": "SPIKE-TICK-SNAPSHOT-DEADCODE-OR-REGRESSED",
    "spike_question": "genuinely inert dead-code (remove) OR silently regressed (re-fix)?",
    "answer": "NEITHER dead nor regressed - the promotion path is live and correct, but writer and reader key the filename off two different clocks, so it can only ever succeed by coincidence. Re-fix, do not remove.",
    "root_cause": {
      "writer": "docs/agents/cowork-team/flow/tick-snapshot.md:40 — FILE_TICK=$(date -u +%H:%M) = WALL-CLOCK minute → writes docs/data/cycle-snapshot-15:08.json",
      "reader": "apps/mcp-server/src/interface/mcp/tools/system/emitPressureStateTool.ts:359-362 — tickHHMM parsed from tick_id = NOMINAL 15-min floor → looks for docs/data/cycle-snapshot-15:00.json",
      "miss_path": "emitPressureStateTool.ts:233 — if (!existsSync(snapPath)) return { promoted:false, stale:false } — silent, and stale:false so no staleness flag is raised",
      "success_condition": "only when the dispatcher completes Step 4.7 in the SAME minute as a :00/:15/:30/:45 boundary, i.e. drift_min rounds to 0"
    },
    "evidence_experimental": {
      "tick": "2026-07-21T15:00Z",
      "drift_min": 6,
      "snapshot_written": "docs/data/cycle-snapshot-15:08.json (14819 bytes, fresh)",
      "tick_id_passed": "2026-07-21T15:00:00Z",
      "emit_result": "{success:true, cycle_snapshot_promoted:false}",
      "discriminator": "no stale_refused field in the response → line-233 existsSync miss, NOT the line-260 freshness gate"
    },
    "evidence_historical": {
      "latest_json_created_at": "2026-07-07T19:47:26Z",
      "latest_json_tick_field": "19:45",
      "inference": "the last successful promotion carried tick=19:45 — exactly a nominal 15-min boundary, corroborating that promotion succeeds only on boundary-minute coincidence",
      "stale_days": 14
    },
    "blast_radius": "MODERATE, not critical. telemetry.md:19-20 reads latest.json for last_regime/last_volatility_level; both currently degrade to \"unknown\", which forces conservative legacy/adaptive defaults rather than wrong values. No agent is acting on 14-day-old prices — the ≤7min gate in cycle-bootstrap/step-0-cowork rejects stale reads. Cost is lost pressure adaptivity, not data corruption.",
    "prior_art": "docs/architecture-briefs/2026-07-12-ultracode-workflow-improvement-audit.md:1547-1549 correctly localised the failure to the server-side promotion and proposed a rescope; it did NOT identify the nominal-vs-wall-clock key mismatch. Line 776 sibling proposal (fixed filename cycle-snapshot-current.json) would close this at root cause.",
    "recommendation": "Sequence with the line-776 fixed-filename proposal rather than patching one side: a fixed cycle-snapshot-current.json removes the shared-clock coupling entirely. If patching in place instead, floor FILE_TICK to the 15-min boundary in tick-snapshot.md so both sides derive the same key. Router did NOT edit either file — dev-team owns both.",
    "filed_by": "cowork-team dispatcher, tick 2026-07-21T15:00Z",
    "no_new_board_row": "prior-art check found SPIKE-TICK-SNAPSHOT-DEADCODE-OR-REGRESSED already in BACKLOG; this payload answers it instead of duplicating it"
  }
}
