{
  "from": "cowork-team",
  "to": "po",
  "type": "cowork-fire",
  "payload": {
    "fire_time": "20260527T000315Z",
    "matched_slots": ["financial-analyst-morning", "news-scout-offhours", "market-watcher-offhours"],
    "won_slots": [],
    "held_by_other": [],
    "all_held": false,
    "spawned": [],
    "silent": false,
    "drift_min": 3,
    "errors": [
      {
        "step": "4.6",
        "error": "vn-market unreachable via gateway — all 3 slot-lock task_claim calls returned 'dial vn-market: Get http://host.docker.internal:3000/sse: dial tcp 192.168.65.254:3000: i/o timeout'. NO spawns issued: agents reach vn-market only through the same gateway wrapper, so all 3 would fail at bootstrap. Withheld spawns to avoid wasted runs + load amplification on a degraded Docker VM."
      }
    ],
    "infra_diagnosis": {
      "incident": "gateway-to-host-bridge-down",
      "server_health_loopback": "localhost:3000/health=HTTP200, /sse=200+streaming (mcp-server HEALTHY on host loopback)",
      "gateway_path": "claude_ai_gateway container -> host.docker.internal (192.168.65.254:3000) = i/o timeout",
      "docker_daemon": "`docker ps` HANGS (Docker VM networking degraded / daemon unresponsive)",
      "conclusion": "Not a server-down. mcp-server is healthy on host loopback; the Docker host-bridge from the gateway container to the host is failing, AND the Docker daemon is unresponsive. Likely needs Docker Desktop networking/daemon restart (host-level — see project_host_memory_panic: 16GB Mac under fleet load).",
      "blast_radius": "ALL cowork agents + this dispatcher are blind until the gateway path recovers. send_telegram also unavailable (routes through gateway).",
      "recommended_owner": "ops (docker restart gateway / Docker networking) or user (restart Docker Desktop if daemon stays unresponsive)"
    }
  },
  "priority": "high",
  "createdAt": "20260527T000315Z"
}
