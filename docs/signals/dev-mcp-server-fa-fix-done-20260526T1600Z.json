{
  "from": "dev-mcp-server",
  "to": ["po", "qa", "ops"],
  "type": "fix-done",
  "id": "dev-mcp-server-fa-fix-done-20260526T1600Z",
  "parent": "FA-FIX",
  "commit": "3c00c17a",
  "files_changed": [
    "apps/mcp-server/src/interface/mcp/tools/news-analysis/analysis.ts",
    "apps/mcp-server/src/infrastructure/rag/ragHttpClient.ts",
    "apps/mcp-server/src/__tests__/1973-fetch-analyze-timeout.test.ts"
  ],
  "test_results": {
    "bun": "9449 pass / 345 fail (within ≤348 pre-existing baseline — 0 new failures)",
    "tsc": "exit 0",
    "new_scenarios": "7/7"
  },
  "ops_rebuild_required": true,
  "ops_rebuild_note": "CODE change in apps/mcp-server/ runtime bundle → docker compose up -d --build mcp-server required; restart alone serves stale image",
  "qa_gate": "slow-mock injection (no Playwright); FA-OPS must prove fetch_and_analyze ≤25s wall-clock through mcp-server with a dead upstream",
  "summary": {
    "rec1_p0": "Per-source outer timeout budgets: cafef=10s, vnexpress=10s, vneconomy=12s, reuters=15s (from 30s). withSourceTimeout() resolves to [] on expiry.",
    "rec2_p0": "Promise.all(fetchPromises) → Promise.allSettled in Step-1 fan-out. Dead sources contribute [] and log; other sources continue.",
    "rec3_p1": "AbortSignal.timeout(8_000) added to ragSearch() and ragIndex() in ragHttpClient.ts. Step-4 fan-out also switched to Promise.allSettled for graceful rag degradation.",
    "rec4_deferred": "use_ingested SQLite read-path (REC-4/P2) deferred per PO ruling. Opens as FETCH-ANALYZE-2."
  }
}
