{
  "from": "cowork-team",
  "to": "po",
  "type": "infra-anomaly",
  "status": "NEW",
  "priority": "high",
  "createdAt": "2026-06-10T22:05:25Z",
  "payload": {
    "anomaly": "cowork-leader lease un-renewable (persistent)",
    "symptom": "task_heartbeat(task_id=cowork-leader, owner_agent=cowork-dispatcher) returns {ok:false, expires_at:0} on 4 consecutive ticks (2026-06-10 21:50, 21:50-retry, 21:45-grid, 22:00). Returned ok:true through 21:20. The 21:50 first heartbeat TIMED OUT (transport), and every heartbeat since is ok:false.",
    "diagnosis": "task_heartbeat is renew-only; the cowork-leader lease record was lost/zeroed at the 21:50 mcp-server event and there is NO reachable recreate path from the dispatcher (task_claim rejects task_kind=cowork-leader: enum allows only cowork-slot|sprint-task|dashboard-row|commit-mutex). emit_pressure_state + commit-mutex task_claim/release still succeed -> mcp-server process is UP but its lease store for cowork-leader is wedged. Signature matches F-OOM-MCP-SERVER (2GiB cap) restart invalidating lock owner_session (memory feedback_lock_orphaned_by_rebuild).",
    "impact": "SILENT ticks unaffected (leader lease not load-bearing; no fire/spawn/commit to serialize). NEXT FIRE at 00:00 UTC (gatherer pair news-scout-offhours + market-watcher-offhours + bctc-analyst-slot-3->c042) would proceed WITHOUT confirmed leadership. Single-dispatcher deployment => no split-brain risk in practice, but the Step 0b leadership guarantee is degraded.",
    "recommended_action": "ops: (1) verify mcp-server health + memory headroom (docker stats; F-OOM-MCP-SERVER 2GiB cap is the likely root); (2) reset/recreate the cowork-leader lease in the task store so Step 0b heartbeat returns ok:true again, OR (3) dev: add a leader-lease create path (allow task_kind=cowork-leader in task_claim enum, or make task_heartbeat create-or-renew) so the dispatcher can self-recover the lease after an mcp-server restart instead of requiring manual reset. Root-cause = F-OOM-MCP-SERVER; fix that to stop recurrence.",
    "dispatcher_posture": "LET-EXPIRE on SILENT (done); will retry Step 0b heartbeat at 00:00 FIRE. If still ok:false at FIRE, dispatcher proceeds single-dispatcher (no peer to conflict) but flags leadership-unconfirmed in that tick's telemetry and re-escalates.",
    "related_findings": ["F-OOM-MCP-SERVER", "feedback_lock_orphaned_by_rebuild", "project_mcp_server_write_wedge"]
  }
}
