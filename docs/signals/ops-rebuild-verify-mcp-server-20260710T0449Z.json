{
  "signal_type": "ops-rebuild-verify",
  "task_id": "FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID",
  "target_service": "mcp-server",
  "created_at": "2026-07-10T04:49:00Z",
  "created_by": "qa",
  "priority": "P1",
  "deferred_verification": {
    "description": "QA independently RAW-verified commit 33bb3078e (writer-level agent_signals.alert_id dangling-FK guard in alertStore.ts storeAlerts/storeAlertsFromCommander + D-NEW2 checkOrphanAgentSignalsAlertId audit tripwire) — code+tests APPROVED. Confirmed the RUNNING mcp-server container's image was built 2026-07-09T12:49:50Z, strictly BEFORE this fix's commit (2026-07-10T04:30:25Z) — the deployed container is CONFIRMED still running the pre-fix code. The current live 0-orphan DB snapshot (10/10 alert_id-tagged agent_signals rows resolve) is NOT evidence the fix is live — it only reflects that no fingerprint collision has occurred recently under the OLD code. The writer guard closes the defect class going forward regardless of caller correctness, but it cannot be observed as 'fixed' until the container ships the new code.",
    "check": "1) docker compose build mcp-server --build-arg GIT_SHA=$(git rev-parse HEAD) && docker compose up -d mcp-server (rebuild+swap, container-swap is user-gated per feedback_container_swaps_user_gated.md — NOT run by qa). 2) Confirm deployed image git_sha label >= 33bb3078e (bash scripts/verify-deploy-sha.sh mcp-server exit 0). 3) docker exec bun -e against /app/data/market.db (DB_PATH env, named volume vn-market-intelligence-mcp_market_data — NOT readonly, to avoid WAL-blindness): re-run the RAW orphan query `SELECT COUNT(*) FROM agent_signals s LEFT JOIN alerts a ON a.id=s.alert_id WHERE s.alert_id IS NOT NULL AND a.id IS NULL` immediately after swap (expect 0, same as pre-swap) AND again after >=1 alert-scan cycle has fired post-swap (ta/bb alert scan jobs run periodically — confirms the guard holds under live traffic, not just at swap instant). 4) Optional stronger proof: docker exec the new checkOrphanAgentSignalsAlertId (D-NEW2) audit finding via /api/data-audit or equivalent — expect rowsAffected=0, action='none' post-swap across multiple daily runs. 5) Server health 200, toolCount unchanged, no new boot errors.",
    "acceptance": "Deployed image git_sha >= 33bb3078e AND live RAW orphan query returns 0 both immediately post-swap and after >=1 subsequent alert-scan cycle (ideally observed across >=1 day to catch a fingerprint-collision re-fire, the exact scenario this fix targets).",
    "blocking": false,
    "deployment_verification": {
      "swap_timestamp": "2026-07-10T04:59:15Z",
      "deployed_image_sha": "3afc9b80dabe0ca208eafa08d5dc44e20b320407",
      "deployed_image_includes_fix_33bb3078e": true,
      "post_swap_orphan_count": 0,
      "server_health_status": "ok",
      "server_toolcount": 183,
      "verification_timestamp": "2026-07-10T04:59:15Z",
      "notes": "Build completed, image deployed successfully. Immediate post-swap orphan query returned 0 (no dangling FK violations in agent_signals.alert_id). Server health verified: status=ok, toolCount=183 unchanged. Deployment awaiting live alert-scan cycle confirmation to complete acceptance criterion."
    }
  },
  "status": "swapped-pending-live-cycle-confirm",
  "notes": "QA verdict: code+tests APPROVED (independently re-verified — diff read, 8/8 new test + 129/129 targeted alert-suite re-run, tsc clean, DDD/security/mock-guard clean, full bun test 14456 pass/40 skip/56 fail/5 errors with zero overlap on alert/signal/orphan/audit in the failing set, live-DB 0-orphan RAW-probe independently reproduced). Held task_board.review[] status=REVIEW (NOT done_verified) and next_agent=ops per standing repo precedent (qa journal S7/S9: docker compose swap is ops-gated, QA does not self-authorize a production-behavior claim before the code is actually deployed). This signal tracks the deferred rebuild+swap+post-deploy live re-verify. Full QA rationale: docs/agent-memory/decisions/sprint-SYSTEMIC-REMAKE-P1-qa.md (task-id FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID entry).",
  "status_note_of": "FIX-AGENT-SIGNALS-ORPHAN-ALERT-ID"
}
