{
  "event": "cowork-team main.md Step 0a instructs the dispatcher to drain rows where to ∈ {po, tran-ngoc-bau, unified-agent, alert-commander} and mark each processed NEW row READ directly (no lock) -- but dev-team's OWN drain (drain-signals.md §0a-D) is the claim-locked, ownership-clear consumer of to==po rows, and its filter is status==NEW only. If cowork-team's Step 0a marks a to==po row READ before dev-team's 0a-D ever claims/routes it, that row silently drops out of dev-team's NEW set -- it will look 'consumed' (status flips) but was never actually claimed, never loaded into pendingSignals[], never routed, and never had dev-team's own logic run against it. This is a signal-loss class bug (status advances without the true owner having acted), not merely a race that self-heals.",
  "discovered_during": "cowork-team 00:15Z tick (WORK verdict via new_signals=3, slots=[] and one_shots=[] both empty). All 3 NEW signal_queue rows this tick (cow-20260730T000800, dev-20260730T001827, cow-20260730T001827) are addressed to=po -- none to tran-ngoc-bau/unified-agent/alert-commander, so nothing was routable to a Step 5 spawn slot this tick anyway.",
  "evidence": {
    "cowork_step0a_text": "docs/agents/cowork-team/flow/main.md Step 0a (lines ~104-109): 'Find all cowork-addressed rows (to ∈ {po, tran-ngoc-bau, unified-agent, alert-commander}). Collect status=NEW rows -> load payload_ref -> route to matching agent slot at Step 5 or log for PO. Mark each processed row NEW -> READ (atomic write).' No task_claim / per-row lock of any kind in this body.",
    "devteam_0aD_text": "docs/agents/dev-team/flow/drain-signals.md §0a-D (lines 21-54): explicitly claims each row via task_claim(task_id='dash:signal_queue:'+row.id, task_kind='dashboard-row', owner_agent='dev-team', ...) BEFORE loading payload/marking READ; on claim failure it SKIPS ('Do NOT add to pendingSignals[], do NOT mark READ'); releases the per-row lock after consumption. This is the only claim-guarded consumer of to==po rows found in the flow tree.",
    "receivers_table": "signal-dashboard SKILL.md Receivers table lists 'po' receiving from tran-ngoc-bau/agents-architect/system-auditor (cowork-team by this session's own precedent) -- i.e. po's inbox is written BY cowork-team/observers and read BY dev-team. Nothing in the skill or either flow doc names cowork-team as a second legitimate reader of to==po rows."
  },
  "action_taken_by_dispatcher": "Declined to execute the literal mark-READ action against this tick's 3 to==po NEW rows (all pre-existing, none newly matched to a cowork agent slot). Left them status=NEW, untouched, for dev-team's own §0a-D claim-guarded drain to consume normally. No rows were routed to Step 5 this tick regardless (none addressed to a cowork-owned agent). This is the same observe-and-report posture applied earlier this session to the published-marker session-id gap and the Step 5.3 PRE-CLAIM false positive (both cow-20260730T001827) -- decline a mechanical action when raw evidence shows it would cause harm, document the flaw for the flow's owner instead of self-editing it.",
  "recommendation": "Route to po -> agents-architect/cowork-refactory-expert for a scoping decision on main.md Step 0a: (1) drop 'po' from the to-filter entirely (cowork-team should only drain rows addressed to agents it actually owns/can route to at Step 5: tran-ngoc-bau, unified-agent, alert-commander) and let dev-team's already-correct §0a-D remain the sole po-inbox consumer; OR (2) if early cross-team visibility into po-addressed rows is genuinely wanted from the cowork dispatcher, add the same task_claim('dashboard-row', ...) per-row lock dev-team uses so the two drains cannot race, and do NOT flip to READ on a claim failure. Option (1) is simpler and matches the existing ownership boundary (cowork-team already writes signal_queue.rows[] to po via a plain append with no read-side responsibility elsewhere in the codebase).",
  "ts": "2026-07-30T00:26:05Z"
}
