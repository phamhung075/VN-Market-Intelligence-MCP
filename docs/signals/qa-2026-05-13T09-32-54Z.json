{
  "type": "qa_signal",
  "from": "dev-mcp-server",
  "timestamp": "2026-05-13T09:32:54Z",
  "branch": "task/push-path-fix-vps-contract-tests",
  "pipeline": "vps-push-path-fix",
  "summary": "VPS push path failures investigated and root cause confirmed. mcp-server routes are correct. Cloudflare tunnel config fix was already applied. Contract tests added.",
  "failures_investigated": [
    {
      "id": "vps-prices-push",
      "symptom": "38 consecutive 'cannot reach MCP server' failures from fetch-prices.sh",
      "root_cause": "Cloudflare tunnel had no /api/* ingress rule — all /api/push-prices requests hit catch-all http_status:404",
      "mcp_server_status": "HEALTHY — route registered at server.ts:317, handler at routes/pushPricesHandler.ts",
      "contract_status": "CONFIRMED — VPS body shape matches handler schema exactly",
      "fix": "~/.cloudflared/config.yml: added path: ^/api/ ingress rule pointing to localhost:4000 (already applied, needs tunnel restart via ops)"
    },
    {
      "id": "vn-news-rss-push",
      "symptom": "/api/push-news http=404 from fetch-vn-news.sh (245 items per cycle not reaching mcp-server)",
      "root_cause": "Same Cloudflare tunnel /api/* routing gap",
      "mcp_server_status": "HEALTHY — route registered at server.ts:387, handler at routes/pushNewsHandler.ts",
      "contract_status": "CONFIRMED — VPS body shape matches handler schema, heartbeat sentinel accepted",
      "fix": "Same as vps-prices-push above"
    }
  ],
  "tests_added": {
    "file": "apps/mcp-server/src/__tests__/1892b-vps-contract-push.test.ts",
    "count": 10,
    "pass": 10,
    "fail": 0,
    "coverage": [
      "P1: VPS stock item exact body → 200 + price stored as full VND (× 1000 unit conversion)",
      "P2: VPS index item (type:index) → price stored as-is",
      "P3: VPS global_index item → price stored as-is",
      "P4: Malformed body (object not array) → 400",
      "P5: Empty array → 400",
      "P6: Missing auth → 401",
      "N1: VPS 14-source news merge → 200 + received count matches",
      "N2: Malformed body (string not array) → 400",
      "N3: Missing auth → 401",
      "N4: Heartbeat sentinel accepted → 200"
    ]
  },
  "recon_docs_updated": [
    "docs/vps-sources/vps-prices/recon.md",
    "docs/vps-sources/vn-news-rss/recon.md"
  ],
  "ops_action_required": {
    "action": "Restart cloudflared tunnel to activate /api/* ingress rule",
    "config_file": "~/.cloudflared/config.yml",
    "rule_added": "hostname: zenmidi.com, path: ^/api/, service: http://localhost:4000",
    "command": "brew services restart cloudflared",
    "verify": "curl https://zenmidi.com/api/watchlist -H 'X-API-Key: <key>' — expect 200"
  },
  "e2e_smoke_test_required": {
    "prices": "SSH to VPS, trigger fetch-prices.sh manually, check /var/log/vn-price-fetch.log for http=200",
    "news": "SSH to VPS, trigger fetch-vn-news.sh manually, check /var/log/vn-news-fetch.log for http=200",
    "db_verify": "SELECT COUNT(*), MAX(updated_at) FROM market_prices — should show fresh rows",
    "note": "E2E smoke test blocked on ops tunnel restart (cloudflared service restart)"
  }
}
