{
  "task_id": "P2-F",
  "pilot": "alert-engine",
  "phase": "2",
  "title": "G5a — git mv domain/services.go → _deprecated/ + evaluate.go rewire to alert_pipeline",
  "status": "SEQUENCED",
  "sequenced_at": "2026-05-24T093000Z",
  "sequenced_by": "pm",
  "owner": "dev-alert-engine",
  "blocked_by": "P2-E (alert-engine-pre-delete tag confirmed at ccef14fa)",
  "blocks": "P2-G (G5b/G5c MCP handler audit)",
  "handoff_reference": "docs/handoffs/TASK_P2-F-ae-g5a-deprecation.md",
  "acceptance_criteria": {
    "AC-1": {
      "criterion": "G5a file moved",
      "command": "test -f apps/alert-engine/pkg/domain/_deprecated/services_v1.go && echo FOUND",
      "expectation": "FOUND (original services.go path gone)"
    },
    "AC-2": {
      "criterion": "Application use case rewired (zero direct calls to deprecated functions)",
      "command": "grep -n 'domain\\.ComputeFingerprint|domain\\.IsDuplicate|domain\\.ShouldSuppressAlert' apps/alert-engine/pkg/application/evaluate.go",
      "expectation": "0 matches (uses alert_pipeline module instead)"
    },
    "AC-3": {
      "criterion": "Build clean",
      "command": "cd apps/alert-engine && go build ./...",
      "expectation": "exit 0"
    },
    "AC-4": {
      "criterion": "Fence-A/B clean post-move",
      "command": "cd apps/alert-engine && golangci-lint run",
      "expectation": "exit 0 (no new fence violations)"
    },
    "AC-5": {
      "criterion": "G12 DoD gate",
      "command": "cd apps/alert-engine && CGO_ENABLED=0 go run ./cmd/sandbox -tier=all -module=alert-engine -scenario=all",
      "expectation": "exit 0 (≥11 scenarios PASS)"
    },
    "AC-6": {
      "criterion": "_deprecated/ directory exists with moved file",
      "command": "find apps/alert-engine/pkg -path '*_deprecated*' -type f | sort",
      "expectation": "output includes services_v1.go + services_v1_test.go (if present) under _deprecated/"
    },
    "AC-7": {
      "criterion": "Fence-C still holds (infra not imported outside composition root)",
      "command": "grep -rn 'mattn/go-sqlite3|pkg/infrastructure' apps/alert-engine/pkg/domain/ apps/alert-engine/pkg/application/ apps/alert-engine/pkg/module/ apps/alert-engine/pkg/primitive/",
      "expectation": "0 matches (infra only in cmd/server/main.go)"
    }
  },
  "files_to_move": [
    "apps/alert-engine/pkg/domain/services.go → apps/alert-engine/pkg/domain/_deprecated/services_v1.go",
    "apps/alert-engine/pkg/domain/services_test.go → apps/alert-engine/pkg/domain/_deprecated/services_v1_test.go (if present)"
  ],
  "files_to_rewire": [
    "apps/alert-engine/pkg/application/evaluate.go (replace domain.ComputeFingerprint/IsDuplicate/ShouldSuppressAlert calls with alert_pipeline module calls)"
  ],
  "pre_condition": {
    "command": "git log --oneline alert-engine-pre-delete",
    "expected": "ccef14fa or later (P2-D/P2-E close-gate commit)",
    "verification": "alert-engine-pre-delete tag MUST exist before git mv; is rollback anchor"
  },
  "constraints": {
    "git_move": "Use git mv to preserve history — never delete + recreate",
    "cgo_build": "Keep CGO_ENABLED=0 go build ./... green",
    "sandbox": "Keep sandbox ≥11/11 (current: 11/11)",
    "fence": "Keep golangci-lint run exit 0 — must not reintroduce fence violations",
    "anchor": "alert-engine-pre-delete tag is rollback anchor if mv goes wrong",
    "staging": "L84 explicit paths only — never git add -A or git add ."
  },
  "goal_posture": {
    "goal_advanced": "G5a",
    "goal_flip": false,
    "note": "NO goal flips. G5a advances but does NOT flip to YES. All goal flips are PO-only at 12/12 terminal Phase-3 commit. goalsEarned stays 0. decisionMatrix stays all-TBD. §4.5 SSOT untouched."
  },
  "next_actor": "dev-alert-engine",
  "next_action": "Execute git mv (services.go + services_test.go to _deprecated/), rewire evaluate.go to call alert_pipeline module instead of deprecated domain functions, verify all 7 ACs PASS, commit with subject 'chore(alert-engine): P2-F — git mv domain/services.go → _deprecated/ + evaluate.go rewire to alert_pipeline (G5a)', emit signal dev-ae-P2-F-done-<UTC>.json"
}
