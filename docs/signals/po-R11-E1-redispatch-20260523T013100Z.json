{
  "kind": "dispatch",
  "from": "po",
  "to": "qa",
  "cycle": "c282 cycle-11",
  "timestamp": "2026-05-23T01:31:00Z",
  "task_id": "P2-E1",
  "anchor": "62edbf3d",
  "supersedes_prior_dispatch": "docs/signals/po-P2-E1-dispatch-20260523T003500Z.json (qa silent-stall ~56min in-flight at cycle-11 start, past 40min stall-watch trigger logged cycle-10)",
  "rationale": "Same as D1 re-dispatch — prior qa subagent on E1 was tied up on the A3 push-gate (qa is single-identity across the three tasks). Fresh qa subagent in parallel with D1 dispatch unblocks G11 critical path work which is pure-write spec.",
  "task_summary": "Read docs/handoffs/TASK_P2-E1.md (full file). Per AC-1..AC-4 in that handoff, design and write docs/architecture-briefs/2026-05-22-refactor/p2-e-regression-scenario-spec.md. Pair scenario A (primary primitive bug) + scenario B (canary). Spec must explain WHY natural A-fix could break B (shared code path / helper / constant). Confirm A+B scenario JSONs exist in pkg/primitive/.../scenarios/ from Phase 1 P1-D1, or add new ones if missing. >20 lines.",
  "constraints": [
    "WIP-1 (this qa subagent owns ONLY P2-E1; do NOT touch P2-D1 or P2-A3)",
    "NO dispatch chain (PO is the only dispatcher)",
    "Do NOT mutate TASK_P2-E1.md frontmatter",
    "Atomic commit per docs/policies/commit-convention.md, reference closure anchor 62edbf3d",
    "Coordinate-by-spec only with D1 — if you choose RSI off-by-one for scenario A, that's fine to share with D1 (both are spec-level descriptions of the same bug class); but the regression CANARY (scenario B) is unique to E1"
  ],
  "exit_condition": "Spec file landed in atomic commit on origin/main HEAD branch, then write completion signal docs/signals/qa-P2-E1-done-{timestamp}.json with commit hash + file path + AC-1..AC-4 confirmations. Exit.",
  "stall_watch_note_for_qa": "If you cannot finish within 30min wall clock, write a status signal at the 30min mark with what's done and what's blocking — do NOT silently stall.",
  "scope_shrink_option_if_blocked": "If scenario A or B JSONs are missing from Phase 1 suite, document the gap in spec + write signal — do NOT silently create scenarios without architect/dev-ta input."
}
