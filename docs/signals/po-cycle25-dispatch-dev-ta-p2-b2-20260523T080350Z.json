{
  "type": "po-dispatch",
  "from": "po",
  "to": "dev-technical-analysis",
  "cycle": "c282 cycle-25",
  "dispatched_at": "2026-05-23T08:03:50Z",
  "task_id": "P2-B2",
  "handoff_doc": "docs/handoffs/TASK_P2-B2.md",
  "phase": "2",
  "pilot": "technical-analysis",
  "goal": "G5",
  "priority": "P0 (G5 chain critical path — G4 just closed YES, brief at 12/12 minus G5)",
  "wip_constraints": {
    "wip_limit_per_subagent": 1,
    "no_dispatch_chain": "dev-ta does P2-B2 ONLY; PO will dispatch P2-B3 separately on B2 completion",
    "no_handoff_frontmatter_mutation": "TASK_P2-B2.md frontmatter must remain intact — append-only §Verification below frontmatter is allowed"
  },
  "background_context_for_dispatch": {
    "g4_just_closed": "PO cycle-25 atomic close — G4 status flipped IN-PROGRESS → YES with qa P2-A4 PASS evidence (commits f611d753 + c86c26f4). Brief now at 12/12 minus G5.",
    "g5_chain_unblocks": "P2-B0 DONE c175f745 (brownfield inventory) → P2-B1 DONE b9d0a82b (HTTP rewire + tag p2-b-pre-delete) → **P2-B2 (this dispatch — deletion)** → P2-B3 (TODO migrate cleanup) → P2-B4 (qa integration verify)",
    "pre_delete_rollback_tag": "p2-b-pre-delete EXISTS but at commit 943adc8e (a PO dispatch commit) NOT at expected b9d0a82b (P2-B1 landing). This is a TAG-MARKER DRIFT from cycle-4 poll-2 era. Resolution: as part of P2-B2 work, dev-ta MUST first re-create the tag at the correct anchor BEFORE the deletion commit, per the AC re-create guidance below. Use `git tag -f p2-b-pre-delete b9d0a82b` is FORBIDDEN here per PO no-force policy — INSTEAD delete the stale tag with `git tag -d p2-b-pre-delete` (local-only, no remote push since no branches) then re-create with `git tag p2-b-pre-delete b9d0a82b` BEFORE the deletion mv. This rolls forward cleanly.",
    "inventory_already_done": "docs/architecture-briefs/2026-05-22-refactor/p2-b-caller-inventory.md is the authoritative scan. P2-B0 ran and surfaced 4 callers BEYOND architect pre-scan; ALL of them resolved in P2-B1 commit b9d0a82b. P2-B2 deletion is therefore LOW-RISK — `bun tsc --noEmit` was already clean post-B1."
  },
  "task_body": {
    "title": "P2-B2 — Move technicalIndicators.ts domain service to _deprecated/",
    "files_touched": [
      "apps/mcp-server/src/domain/services/technicalIndicators.ts (MOVE via git mv to apps/mcp-server/src/_deprecated/technicalIndicators.ts)",
      "apps/mcp-server/src/__tests__/1302-technical-indicators.test.ts (MOVE via git mv to apps/mcp-server/src/_deprecated/1302-technical-indicators.test.ts)"
    ],
    "acceptance_criteria": [
      "AC-1: apps/mcp-server/src/domain/services/technicalIndicators.ts no longer exists at original path",
      "AC-2: File moved via `git mv` (preserves history as rename, not delete+create) to apps/mcp-server/src/_deprecated/technicalIndicators.ts",
      "AC-3: Test file moved via `git mv`: 1302-technical-indicators.test.ts → _deprecated/1302-technical-indicators.test.ts",
      "AC-4: `bun test` still passes — no broken imports referencing the old path (P2-B1 already cleared all SEV-1..SEV-3 callers per p2-b-caller-inventory.md)",
      "AC-5: `find apps/mcp-server/src -path '*technical*' -name '*.ts' -not -path '*_deprecated*'` returns 1 result ONLY — apps/mcp-server/src/interface/mcp/tools/market-data/technicalIndicatorTools.ts (the HTTP-rewired tool handler, A2 in inventory). Domain service A1 + test A3 must be moved to _deprecated/.",
      "AC-6: Header comment added to BOTH moved files at the very top (before all other content): `// DEPRECATED: G5 Phase 2. Moved from domain/services/. Delete after G5 verification passes.`",
      "AC-7 (added by PO for tag hygiene): Before the move, verify `git tag -l p2-b-pre-delete` exists; if it points anywhere OTHER than b9d0a82b, delete the local stale tag (`git tag -d p2-b-pre-delete`) and re-create at the correct anchor (`git tag p2-b-pre-delete b9d0a82b`). The deletion commit must be created AFTER this tag re-anchoring so rollback safety is preserved. Confirm with `git show-ref p2-b-pre-delete` shows b9d0a82b before proceeding to the mv."
    ],
    "smoke_check_command": "cd apps/mcp-server && bun test && find apps/mcp-server/src -path '*technical*' -name '*.ts' -not -path '*_deprecated*' | wc -l",
    "smoke_check_expected": "bun test passes (with the same pre-existing alertAccuracy module failure noted in b9d0a82b commit body — that one was pre-existing and unrelated); find count = 1 (only technicalIndicatorTools.ts the tool handler remains outside _deprecated/)",
    "atomic_commit_subject_template": "refactor(technical-analysis): P2-B2 — move technicalIndicators.ts to _deprecated/ (G5)"
  },
  "constraints": {
    "L84_explicit_file_staging": "git add ONLY the moved files + handoff §Verification + (if you re-anchored the tag) nothing — tags are not in git index. NO broad-glob `git add docs/` or `git add .`.",
    "no_force": "NO `git push --force`, NO `git tag -f`, NO `git push --no-verify`. The single allowed tag-rewrite is `git tag -d` + `git tag` on LOCAL ONLY (no remote tag push) per the AC-7 path.",
    "no_no_verify": "NO `git commit --no-verify`. Run all hooks.",
    "no_push": "NO `git push` this cycle. CI billing block still active per docs/signals/bug-ci-billing-block-20260523T013000Z.json (owner=user). Local landing only.",
    "no_decisionMatrix_touch": "NEVER touch docs/data/pilot-status.json `decisionMatrix.{speed,trust,scale}` — those are PO matrix-authorship per Charter §4.5, populated ONLY after all 12 G-goals terminal (G5 still TBD until B4 lands; matrix flip happens cycle-26+ on G5=YES atomic close).",
    "no_in_flight_handoff_mutation": "TASK_P2-B2.md frontmatter (lines 1-15) must remain intact. Append §Verification block AFTER the frontmatter with: cycle log row + sandbox/test output verbatim + tag-anchor evidence (git show-ref p2-b-pre-delete output).",
    "no_other_files": "Do NOT touch decisionMatrix, do NOT touch other goal status fields, do NOT touch pilot-status.json — that is exclusively PO surface this cycle. Your scope is the move + tag re-anchor + handoff §Verification + completion signal write."
  },
  "completion_signal_contract": {
    "path_pattern": "docs/signals/dev-ta-p2-b2-done-<UTC>.json",
    "required_fields": [
      "type=dev-completion",
      "from=dev-technical-analysis",
      "to=po",
      "task_id=P2-B2",
      "consumed_dispatch=docs/signals/po-cycle25-dispatch-dev-ta-p2-b2-20260523T080350Z.json",
      "commit_sha=<the move commit SHA>",
      "result=PASS|FAIL|BLOCKED",
      "ac_results.ac_1..ac_7 verdicts",
      "tag_anchor_evidence={pre_state: <old SHA>, post_state: 'b9d0a82b', git_show_ref_output: <verbatim>}",
      "bun_test_exit_code",
      "bun_test_pre_existing_failures (list — should be only the alertAccuracy one from b9d0a82b)",
      "find_command_output_verbatim",
      "completion_timestamp"
    ]
  },
  "stall_watch": {
    "self_report_at_minutes": 20,
    "self_report_format": "write docs/signals/dev-ta-p2-b2-stall-<UTC>.json with: progress_to_date, blocking_cause, eta_minutes",
    "R-11_trigger": "2026-05-23T09:03:50Z (60min from dispatch). If no completion signal AND no stall signal by then, PO will fire R-11 status-check per cycle-7/10/22 precedent."
  },
  "out_of_scope": [
    "DO NOT delete the moved files outright — they MUST be MOVED to _deprecated/ (git mv preserves history; deletion can happen post-G5 verification in a separate sprint).",
    "DO NOT touch P2-B3 (TODO migrate cleanup) or P2-B4 (qa integration verify) — those are downstream chain tasks, PO will dispatch separately on B2 completion.",
    "DO NOT push to remote — CI billing block.",
    "DO NOT modify decisionMatrix, goals[], or any other PO-authored field in pilot-status.json.",
    "DO NOT modify the HTTP tool handler technicalIndicatorTools.ts — it was rewired in B1 and is the live tool (path remains outside _deprecated/ per AC-5).",
    "DO NOT re-run the brownfield inventory — already done in B0 commit c175f745."
  ],
  "rationale_for_dispatch_target": "P2-B2 handoff frontmatter declares owner=dev-technical-analysis. The task is a Go-zone-adjacent TS file movement (mcp-server zone, but tightly coupled to G5 which is the TS-deletion completion for the TA refactor pilot). dev-technical-analysis has full context on the Go service that replaces this TS code. Alternative target consideration (generic developer) rejected because: (a) the work touches the TA pilot deletion chain whose context lives in dev-technical-analysis notebooks, and (b) handoff frontmatter is the SSOT for owner assignment.",
  "po_cycle25_atomic_context": "This dispatch is part of PO cycle-25 atomic close: same commit will (a) flip goals[G4].status YES, (b) close phase2.buckets.P2-A, (c) write this dispatch signal, (d) overwrite po notebook. One atomic commit per L84 + closure anchor 62edbf3d held."
}
