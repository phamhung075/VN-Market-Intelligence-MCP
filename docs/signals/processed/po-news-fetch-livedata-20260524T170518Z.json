{
  "from": "po",
  "to": "architect",
  "type": "task-dispatch",
  "priority": "high",
  "createdAt": "2026-05-24T17:05:18Z",
  "payload": {
    "task": "NF-LD-1",
    "feature": "news-fetch live-data inspection view (follow-on enhancement, NOT a pilot reopen)",
    "handoff": "docs/handoffs/TASK_NF-LD.md",
    "ledger": "docs/TASKS.md § Follow-On Enhancement (news-fetch live-data inspection view)",
    "chain": "architect(NF-LD-1) -> developer(NF-LD-2) -> qa(NF-LD-3) -> po(NF-LD-EXIT)",
    "key_findings": [
      "news-fetch (port 5008) is STATELESS — no DB, no repo in src/domain/repositories.ts (scraper ports only).",
      "Persisted rows live in mcp-server rag_analyses table (schema-news.ts); path = news-fetch -> newsHeadlinesRefreshJob -> /api/push-news -> pollNews INSERT OR IGNORE INTO rag_analyses.",
      "Live view MUST be a read-only endpoint on mcp-server (port 3000, /api/*), NOT on news-fetch. Precedent: G5 allowed exactly one mcp-server task for the HTTP boundary.",
      "Security Clause: sandbox process + 3 existing sandbox dashboard panels (data.js, file:// zero-CORS) stay zero-creds + UNTOUCHED. Live section is separate, http-fetch only, honest-degrade under file://."
    ],
    "product_shape": {
      "sources": ["reuters", "bloomberg"],
      "fields": ["source", "headline(source_title)", "url(source_url)", "published_at(parsed)", "verdict(sentiment+impact_direction/impact_score)", "fetched_at(created_at)"],
      "dedup_key": "computed in news-fetch, NOT stored in rag_analyses — architect surfaces derived hint OR omits; do not fabricate a column",
      "row_count": "last 20 per source, ORDER BY created_at DESC",
      "live_vs_cached": "live SELECT-only query on each section load; no cache; read-only (no INSERT/UPDATE/DELETE)"
    },
    "architect_deliverable": "endpoint contract + exact rag_analyses column->field mapping (confirm provider-value column) + dashboard live-section design + boundary/honesty rules + auth posture; append design notes + per-task ACs (NF-LD-2/NF-LD-3) to handoff; RETURN handing NF-LD-2 to developer",
    "anti_regression": "pilot-status-news-fetch.json FROZEN at 12/12; this enhancement does NOT touch it or flip any goal",
    "constraints": "L84 explicit-file staging; no --force/--no-verify/--no-gpg-sign; local-only no push; all on main; Bun.env not process.env; ESM .js suffixes"
  }
}
