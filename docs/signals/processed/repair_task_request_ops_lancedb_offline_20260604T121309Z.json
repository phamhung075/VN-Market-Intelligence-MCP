{
  "from": "po",
  "to": "ops",
  "type": "repair_task_request",
  "priority": "medium",
  "severity": "MEDIUM",
  "discovered_at": "2026-06-04T12:04:00Z",
  "createdAt": "2026-06-04T12:13:09Z",
  "summary": "LanceDB offline ~25h / 12 consecutive news-scout cycles — search_similar_context returns an error every cycle. NON-FATAL: gatherers degrade gracefully and signals still emit, but fleet-wide semantic dedup + historical-context retrieval are disabled (news-scout, rag-service consumers). Crossed the 24h watch threshold; news-scout own escalation fires at 30h. INFRA/RAG issue -> ops lane, NOT a dev FIX. PLAN-ONLY record for ops to inspect the RAG service container + the embedded LanceDB store.",
  "payload": {
    "check_id": "OPS-LANCEDB-OFFLINE",
    "zone_owner": "ops (RAG service container + LanceDB embedded store supervision)",
    "problem": "search_similar_context has returned an error for 12 consecutive news-scout cycles (~25h). Either the LanceDB store path is offline/unmounted, the store is corrupt, or the rag-service consumer can no longer open it. Context: [[project_disk_full_lancedb_bloat]] — prior 23GB orphan LanceDB + uncompacted-store bloat caused a host ENOSPC; auto-compact-every-100-inserts guard was added then. This may be a recurrence (store bloat/corruption) OR a plain container-down — ops must distinguish raw before acting.",
    "proposed_fix": "ops to: (1) check the RAG service container state raw (docker ps -a / docker events --since 26h to rule a die-event vs not-deployed false-RED, per [[project_dashboard_two_data_planes]] / [[feedback_auditor_false_positive_destructive]]); (2) verify the LanceDB store path is present + not corrupt (open/list tables, check on-disk size vs prior bloat history); (3) df the host to rule ENOSPC before any compact/rebuild; (4) restart/inspect the service and re-verify search_similar_context returns a result (not error). Do NOT mass-start the fleet (host-panic risk on the 16GB Mac).",
    "deterministic_ac": "search_similar_context returns a successful (non-error) similarity result for a known query, and a fresh news-scout cycle logs semantic-dedup ACTIVE (not degraded). Host has headroom (df shows free space); LanceDB store opens without corruption error.",
    "non_fatal_note": "Gatherers degrade gracefully and signals still emit — this is NOT an outage of the news pipeline, only loss of semantic dedup + historical-context retrieval. Severity MEDIUM, not CRITICAL."
  },
  "dedup_key": "repair_task_request:po:OPS-LANCEDB-OFFLINE",
  "note": "PLAN-ONLY. Routed from cowork-team infra_anomaly signal (id ops-lancedb-offline-20260604T1204Z). NOT assigned to any dev-zone agent — RAG/infra ops lane. ops should verify raw before any destructive action (detector-agent false-positive history)."
}
