{
  "from": "agents-architect",
  "to": "agent-father",
  "type": "architecture_brief",
  "payload": "docs/architecture-briefs/2026-08-15-cowork-cron-registration-sibling-process-defer.md",
  "priority": "high",
  "createdAt": "2026-08-15T11:21:35Z",
  "summary": "Router-dispatched: user hand-authored a defer note in the wrong file (.claude/skills/cron-detect-loop/register.md, inert there -- it governs cron-detect-loop's own 4 jobs, not the unrelated cowork-team */15 dispatcher). Root-caused the underlying registration-layer bug: cron-cowork-team/SKILL.md Step 1a's fast path keys exclusively on owner_client_session; two OS processes sharing one $CLAUDE_CODE_SESSION_ID (verified live this session -- 4 distinct claude processes currently running on this host, confirmed via ps) both pass hb.ok==true and each independently runs Step 1d's local CronList/CronCreate, invisible to each other -- producing duplicate */15 registrations. Corrected the router's own evidence en route: owner_session (pid-N-ts-M on every lock row) is the MCP SERVER's own process+boot diagnostic (taskClaimTool.ts:25-30/coordinationTools.ts:18, 'MUST NOT be used as the ownership key'), NOT a client-terminal discriminator -- it is identical for every caller regardless of which terminal calls, so it cannot and does not resolve this. session-presence's '1 live row' is similarly uninformative (per-session singleton by construction, dispatch-claim/SKILL.md:162-199). NEW finding, flagged not fixed here: the identical shared-UUID gap also defeats the P3 fire-time election's RE-ENTRANT branch (leader-lock.md + dev-team Step[3] + auditor tiers + the router's own dispatch-claim hot path) -- a correctness bug (double dispatch), not just registration waste; brief section 0.4/5 recommends PO scope this as a separate follow-up given 5+-file blast radius including the router's own hot path. FIX DESIGNED (this brief's implementation contract, all .md-only, agent-father's own zone): (1) remove the misplaced Block/Interdit paragraph from cron-detect-loop/register.md; (2) cron-cowork-team/SKILL.md Step 1a rewrite -- add a client-side $PPID+lstart($PPID) process fingerprint (empirically verified stable across Bash calls within one session, no MCP schema change needed, stored in the existing marker's free-form payload) compared on the hb.ok==true fast path; mismatch => defer (no local CronCreate, WORK telegram) with a heartbeat_at-based self-heal (age>1800s => presumed-dead sibling, steal+re-register) so a genuinely-dead sibling process doesn't permanently block re-arm (same guaranteed-slot-missed protection the 2026-08-06 cron-rearm-cross-session-dedup brief's own constraint requires); (3) Step 1c payload gains one field (registering_process); (4) new 'Sibling-Process Defer -- Fallback Only' subsection formalizes the user's original manual-defer intent as an explicitly-labeled FALLBACK (not primary), mirroring exactly how P3 itself kept the retired feedback_router_cowork_defer_to_live_leader convention as fallback until its own code gate was smoke-tested -- rejected making the manual convention primary again, since that would silently reverse P3's own rationale without re-arguing it.",
  "_note_to_agent_father": "Implementation contract is bounded to brief section 5's first list (2 files: cron-detect-loop/register.md removal + cron-cowork-team/SKILL.md Step 1a/1c/new-fallback-subsection). Do NOT implement section 0.4/5's fire-election RE-ENTRANT fix (leader-lock.md, dev-team main.md, auditor tiers, dispatch-claim CARD.md/SKILL.md) as part of this signal -- that is explicitly out of scope pending PO ratification given its blast radius (includes the router's own hot path). Flag it to PO/next architect cycle if not already tracked.",
  "_processed": {
    "fingerprint": "389eb1f6566b8c7344b51ed853d8af1a971913db54ddc653b3d0c01825b5d5f5",
    "processedAt": "2026-08-15T11:49:21Z",
    "processedBy": "dev-team",
    "result": "routed-to-po"
  }
}
