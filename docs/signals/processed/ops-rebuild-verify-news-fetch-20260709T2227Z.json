{
  "signal_type": "ops-rebuild-verify",
  "task_id": "FACTORY-NEWS-split-sandbox",
  "target_service": "news-fetch",
  "created_at": "2026-07-09T22:27:00Z",
  "created_by": "dev-news-fetch",
  "priority": "P2",
  "deferred_verification": {
    "description": "RAW-verify the news-fetch container still starts and serves correctly after this pure sandbox-tooling refactor. This task touched ONLY apps/news-fetch/src/sandbox/ (dev-only scenario-runner CLI, zero infrastructure imports, not shipped into any runtime request path) — no production route, module, or primitive code changed. The rebuild-verify here is a low-risk sanity check, not a behavior-change confirmation.",
    "check": "After rebuilding the news-fetch container, confirm the service boots cleanly (docker compose logs news-fetch shows no startup error) and a live headline fetch/ingest cycle runs as before. The sandbox runner itself (apps/news-fetch/src/sandbox/) does not run inside the container's request path, so this check is about confirming the rebuild picked up the new file layout without breaking the Bun build, not about runtime behavior change (there is none in the shipped service).",
    "acceptance": "news-fetch container starts clean post-rebuild; no new errors in logs attributable to the sandbox/ file split.",
    "blocking": false
  },
  "status": "pending",
  "notes": "Per DoD: split apps/news-fetch/src/sandbox/runner.ts (486L) into cli.ts, registry.ts, discover.ts, equal.ts, execute.ts, report.ts + runner.ts as a thin orchestrator (all files <=120L, execute.ts=120L exactly). registry.ts stores a per-entry argAdapter fn replacing the by-name if/else signature switch. import.meta.dir resolution preserved (all files stay in src/sandbox/). bun test 233 pass/6 skip/0 fail (unchanged baseline), bun tsc --noEmit clean, eslint lint:ci clean, scenario JSON valid, G12 sandbox gate 16 PASS/0 FAIL/0 ERROR (unchanged). Direct before/after trace-JSON diff (pre-split vs post-split runner.ts, runAt field excluded) = byte-identical. Container still running pre-change image — this signal tracks the deferred live-rebuild confirmation (feedback_user_gates_delegate_to_ops.md). Do not close task as DONE_VERIFIED until ops confirms rebuild + boot check.",
  "status_note_of": "FACTORY-NEWS-split-sandbox",
  "_processed": {
    "fingerprint": "aa7d96f0c5abff2ff22bd71c2a6f84b00b609550625c309390f9c19647ef31bb",
    "processedAt": "2026-07-09T22:46:46Z",
    "processedBy": "dev-team",
    "result": "routed-to-po"
  }
}
