{
  "from": "agents-architect",
  "to": "agent-father",
  "type": "architecture_brief",
  "payload": "docs/architecture-briefs/2026-08-22-cowork-detect-loop-flow-review.md",
  "priority": "normal",
  "createdAt": "2026-08-22T16:20:35Z",
  "summary": "User-requested observe-and-report review (not a fix cycle) of the cowork-team master dispatcher loop + the anomaly-detection to dev-team-planning loop (cron-detect-loop's 4 crons), with a mermaid diagram of both loops and their shared junction (docs/data/orch/orch-state.json .signal_queue.rows[]), per-agent mechanics annotations, and a live correctness check. VERIFIED (not assumed): the signal_queue junction hypothesis holds but is recipient-scoped -- rows to in {tran-ngoc-bau, unified-agent, alert-commander} stay entirely inside the cowork loop (drained by cowork-team's own Step 0a); only to==po rows (including every anomaly-task-bridge repair_task_request) cross into the dev-team loop. LIVE STATE FINDINGS: (F2) at review time .signal_queue.rows[] held 3 undrained NEW rows (2026-08-14/15/18) plus 4 undrained to=ops OPEN rows, .head 7 days stale, dev-team-idle-widen-state.json 4 days stale, no cowork-schedule.json slot fired after 2026-08-15 -- all four corroborate a real multi-day host-suspension outage (system-auditor's own c104 notebook entry today confirms 'host was dark 4 days'), a known/catalogued pattern, not a new defect. (F3) both cron-registration:cowork-team and cron-registration:detect-loop cross-session markers were freshly re-claimed ~9min before this review (this session's own re-arm) -- registration-layer freshness CONFIRMED; underlying CronList entries could not be independently verified (no tool access route from this review agent -- same structural limitation the codebase's own preflight scripts document). (F4) the guaranteed-slot launchd backstop (com.vn-market.cowork-guaranteed-slot-firer) is real, independent, and bridged part of the outage (confirmed live: fb-weekend fired+published today 13:29-13:35Z) but NEVER writes cowork-schedule.json's last_fired field (grep-confirmed zero references in the firer script) -- that field is stamped only by the primary dispatcher's own spawn-fanout/last-fired.md step, so it silently desyncs from reality whenever the backstop, not the primary dispatcher, does the work. Low severity (duplicate-publish is independently protected by the published-marker gate) but a real observability gap. (F5) a signal_queue row addressed to=agents-architect (po-20260814T195709, re: Step 2.4 cowork-probe TTL>cadence false positive) has sat status=NEW unread for 8 days -- likely already informally tracked via memory feedback_step24_cowork_collision_probe_ttl_exceeds_cadence_daily_false_positive, but the formal signal_queue row was never closed. (F6) .claude/skills/anomaly-task-bridge/SKILL.md ATB-4b still documents backlog[] task shape with status:TODO, but po's triage-signals.md (which ATB-4b cites as authoritative) mints status:BACKLOG -- TODO is rejected by orchStateSchema.ts's LANE_ALLOWED_STATUSES.backlog validator (confirmed by the adjacent FIX-PO-TRIAGE-SIGNALS-CIRED-TEMPLATE-STATUS-TODO-REJECTED-BY-VALIDATOR note). Not live-broken (PO follows the corrected template) but stale/misleading doc. (F7, open question, not a confirmed defect) drain-signals.md's 'dev-team-addressed agent' recipient set is never enumerated anywhere (unlike cowork-team's own jq-derived set) -- unclear if the 4 to=ops rows from F2 are even in scope for dev-team's drain. No wiring defects found; the documented mechanics of both loops are internally consistent and the anomaly-task-bridge -> repair_task_request -> PO -> task_board.backlog[] chain does close (origin_signal_id back-reference flips the source row on DONE_VERIFIED). Nothing was fixed -- this is observe-and-report only per the dispatch table.",
  "_note_to_agent_father": "No urgent implementation contract. Two small, low-priority, well-scoped follow-ups if picked up: (1) F6 -- one-line doc correction in .claude/skills/anomaly-task-bridge/SKILL.md ATB-4b (status:TODO -> status:BACKLOG, to match triage-signals.md's already-corrected template). (2) F4 -- have scripts/agents-flow/cowork-guaranteed-slot-firer.sh (or the spawned flow itself) also stamp cowork-schedule.json.slots[].last_fired on a successful guaranteed-slot fire, so the schedule's own bookkeeping doesn't silently desync whenever the launchd backstop (not the primary */15 dispatcher) is the one doing the work. F5 (unread signal to agents-architect) and F7 (undocumented drain-scope predicate) are flagged for PO/next architect cycle awareness, not agent-father action. F2/F3 are live-state observations tied to a known host-suspension pattern, not new wiring defects -- no action requested.",
  "_processed": {
    "fingerprint": "5231a5bf0f9834decd735b6a2766877a5c14ce9f402772ba965ee4ac9a977ada",
    "processedAt": "2026-08-22T16:48:03Z",
    "processedBy": "dev-team",
    "result": "routed-to-po"
  }
}
