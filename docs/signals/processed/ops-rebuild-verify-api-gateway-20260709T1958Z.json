{
  "signal_type": "ops-rebuild-verify",
  "task_id": "FACTORY-APIGW-split-handlers",
  "target_service": "api-gateway",
  "created_at": "2026-07-09T19:58:00Z",
  "created_by": "dev-api-gateway",
  "priority": "P1",
  "deferred_verification": {
    "description": "RAW-verify proxy + dashboard behavior unchanged after rebuild — hottest-path production HTTP handler split across 4 files within the same package",
    "check": "After rebuilding api-gateway container, hit GET /health-dashboard and confirm title/status badges/service cards/placeholder sections render identically to pre-split. Hit GET /health, GET /health/<service>, and ANY /<service>/* (both a deployed service and a NOT_DEPLOYED-rerouted service, e.g. /kinh-dich/... which reroutes to mcp-server) and confirm identical status codes/JSON error shapes/proxy pass-through behavior to pre-split baseline.",
    "acceptance": "curl :4000/health-dashboard 200 text/html with same visible content; curl :4000/health 200/503 JSON matching pre-split; curl :4000/<known-service>/<path> proxies through with same status; curl :4000/kinh-dich/... 503-reroutes-to-mcp-server path unchanged.",
    "blocking": false
  },
  "status": "pending",
  "notes": "Per DoD: rebuild_required=true (hottest live-serving HTTP path). handlers.go (421L) split into dashboard.go (119L)/middleware.go (44L)/proxy.go (117L, new private newReverseProxy+serveProxied helpers collapsing 2 duplicate reverse-proxy blocks)/handlers.go (85L), same package, all exported symbols/behavior unchanged, go test ./... 10/10 packages PASS pre-rebuild, golangci-lint 0 issues, G12 sandbox primitive 14/14 + module 1/1 PASS. This signal tracks the deferred post-rebuild behavior verification that was user-gated (feedback_user_gates_delegate_to_ops.md). Task closed as REVIEW (not self-asserted DONE_VERIFIED) pending this check given hottest-path risk.",
  "_processed": {
    "fingerprint": "f714730d9a66af1a21de89110b05e725ecd206a3bd16ab151c692f5cee0fc1cd",
    "processedAt": "2026-07-09T20:16:50Z",
    "processedBy": "dev-team",
    "result": "routed-to-po"
  }
}
