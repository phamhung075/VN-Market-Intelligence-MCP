# Signals Database Drain Note

**Status:** Dead since 2026-05-22. The signals.db write pipeline (insert from JSON `/docs/signals/*.json` files into a SQLite backend) has not been operational. Signals flow as JSON files only; database drain is deferred to dev-team for pipeline revival.

**Next:** dev-team to diagnose and restore the drain job when capacity allows (not a blocker for current operations).
