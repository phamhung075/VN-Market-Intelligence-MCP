{
  "from": "agents-architect",
  "to": "agent-father",
  "cc": ["po", "dev-team"],
  "type": "brief_complete",
  "payload": "docs/architecture-briefs/2026-07-31-cired-triage-failedfile-dedup.md",
  "priority": "high",
  "createdAt": "2026-07-31T04:29:55Z",
  "summary": "FIX-CIRED-TRIAGE-WRONG-PLANE-DEDUP-AMNESTY brief ready. Two files, zero production code. (1) docs/agents/po/flow/triage-signals.md ci_red row: adds MANDATORY pre-dedup `gh run view {run_id} --log-failed` FAILEDFILE read (AC-1), replaces SHA-only check_id/head_sha dedup with FILE-scoped `dedup_key: ci_job:<job>|file:<file>` as primary key (AC-2, already field-validated twice on FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT + FIX-CI-SIZELINT-MACRO-VMT-LIQUIDITY-RESOLVERS-NEW-OFFENDER + FIX-ORCHSTATE-HEAD-STAMP-DROPPED-CI-RED-1837A), adds explicit anti-amnesty fence naming FIX-MCP-SUITE-HEALTH-BASELINE with the CI-plane-vs-local-plane reason (AC-3), adds 0-fail-baseline backstop closing the 'pre-existing' escape hatch (AC-4). One row per distinct failing file, not collapsed. (2) docs/agents/dev-team/flow/ci-health-probe.md: doc-accuracy-only correction to Hard Constraint #2 (layer-c description was about to go stale) + one clarifying NOTE under Step CI-3's DEDUP_KEY so a future reader never mistakes the probe's own SHA+job-scoped key for the cross-SHA fix — CANON-SCRIPT (ci-health-probe.js) untouched, no probe behavior change, payload.run_id already threads through end to end. AC-5 retro-sweep executed and evidenced in the brief S5: 1408-tool-diacritics fixed by 9374e65e0, emit-pressure-state fixed by 98917416a+d19d6cdc5, both confirmed GREEN on current main run 30603458514 (0 fail, zero FAILEDFILE lines). Exact replacement table-row text for both files is in brief S3/S4, copy-paste ready. Scope fence: does NOT touch the two live CI-red content fixes (FIX-CI-FRONTEND-ESLINT-BUNLOCK-DUAL-LOCKFILE-DRIFT, FIX-CI-SIZELINT-MACRO-VMT-LIQUIDITY-RESOLVERS-NEW-OFFENDER, both ready[], unclaimed) — mechanism only. Row moved IN_PROGRESS->REVIEW, next_agent=po (po's own brief_complete routing row: target is docs/agents/* -> route_to agent-father). .head deliberately NOT touched — it points at a different in-flight task (FDA-5, dev-mcp-server) per this dispatch's own note; only this row's lane moved."
}
