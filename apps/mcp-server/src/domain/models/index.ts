/**
 * Domain Models — barrel export
 *
 * Core data entities for the VN Market Intelligence domain.
 * No I/O dependencies — pure TypeScript types and interfaces.
 *
 * Populated by future tasks:
 *   - Task 061: AnalysisEntry (RAG hierarchy)
 *   - Task 047: FinancialReport (BCTC)
 *   - Task 064: Alert
 *   - Task 082: WatchlistItem
 */

// Re-export all domain models as they are implemented
export type * from "./shared-types.js";
