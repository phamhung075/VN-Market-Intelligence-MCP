{
  "name": "@vn-market/stock-price",
  "version": "1.0.0",
  "description": "Stock Price microservice — 3-tier price fallback with concurrent fetching (DDD, port 5000)",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "check": "bun tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.4.0"
  }
}
