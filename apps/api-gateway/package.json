{
  "name": "@vn-market/api-gateway",
  "version": "1.0.0",
  "description": "API Gateway — health aggregation + reverse proxy for all microservices (DDD, port 4000)",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "check": "bun tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.4.0"
  }
}
