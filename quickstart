  Quick start

  # Start a sprint session
  bash scripts/agents-flow/tmux-agent.sh setup sprint

  # Attach to it
  tmux attach -t vn-market

  # Or open just one agent window
  bash scripts/agents-flow/tmux-agent.sh open developer

  # Send a command to a pane
  bash scripts/agents-flow/tmux-agent.sh send dev "bun test --watch"

  # Clean up
  bash scripts/agents-flow/tmux-agent.sh teardown

  The skill is now registered as /tmux-layout and the script is ready at scripts/agents-flow/tmux-agent.sh.