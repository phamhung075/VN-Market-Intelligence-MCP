.env
.env.local
.DS_Store
node_modules
~/
/data/
*.db
*.db-shm
*.db-wal
*.fresh-*
*.pre-repair-*
*.corrupt-*
dist/
.claude/worktrees
.claude/scheduled_tasks.lock
.claude/tmp/
.claire/
**/reports/*-evening.json
**/__pycache__/
docs/agent-memory/sessions/*.log
apps/*/server
# compiled sandbox runners (build artifacts — rebuilt from cmd/sandbox sources)
apps/technical-analysis/sandbox
apps/kinh-dich-service/sandbox
apps/stock-price/sandbox
apps/stock-price/bin/
apps/api-gateway/sandbox/sandbox-runner
# build output & ephemeral test runner state
apps/frontend/build/
**/test-results/
# vendored third-party clone (has its own .git) & local dep install
apps/pdf-extractor/PDF-Extract-Kit/
packages/primitives/
docs/data/cycle-snapshot-*.json
docs/data/.cycle-snapshot-*.stage
docs/data/cycle-snapshot-*.json.tmp
docs/daily/
docs/outbox/

# PDF single-source consolidation (PDF-SINGLE-SOURCE)
/data/pdfs/*.pdf
/data/pdfs-local/

# E3 idempotency cache — bctc-analyst ephemeral agent-layer metadata (OQ-5)
# Note: covered by top-level /data/ rule above; explicit entry for documentation clarity
/data/bctc-analysis-cache/

# ephemeral runtime/generated files in docs/data/ — not tracked, suppress from status
# (previously hidden by the unanchored data/ rule; now explicit after anchoring to /data/)
docs/data/alert-verdicts.json
docs/data/daily-dashboard.json
docs/data/md-dependency-graph.html
docs/data/pressure-state.json
docs/data/vn-market-tasks.json
docs/data/BATCH_S_1849_SUMMARY.md

# runtime commit serialization lock — operational state, never version-controlled
docs/data/commit-mutex.json

# Rolling operational telemetry (cap 200, helper recreates) — must survive fleet git reset --hard (FIX-DB-INTEGRITY-TRAIL-GITRESET-DATALOSS)
docs/data/db-integrity-history.json

# machine-state plane debris (UC-GCP-P2 / git-ci-publish-P2 2026-07-16) — test-run scratch state, never source
.test-notebook-prune-debug/
/scratchpad_*.txt

# obsolete-file cleanup quarantine (FIX-CMH-OBSOLETE-FILE-CLEANUP 2026-07-20) — Pass 0b
# --live target: scripts/audits/clean-obsolete-files.sh moves candidates here, never rm's directly.
docs/data/.trash/
