--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -260,13 +260,13 @@
   cloudflared:
     image: cloudflare/cloudflared:latest
-    command: tunnel run vn-market-mcp
+    command: ["tunnel", "run", "vn-market-mcp", "--config", "/etc/cloudflared/config.yml"]
     volumes:
     - ~/.cloudflared/config.yml:/etc/cloudflared/config.yml:ro
     - ~/.cloudflared/vn-market-mcp.json:/etc/cloudflared/vn-market-mcp.json:ro
     environment:
       TUNNEL_LOGFILE: /tmp/cloudflared.log
       TUNNEL_LOGLEVEL: debug
-      TUNNEL_CRED_FILE: /etc/cloudflared/vn-market-mcp.json
+      TUNNEL_CRED_FILE: /etc/cloudflared/vn-market-mcp.json
+      TUNNEL_ORIGIN_CERT: /etc/cloudflared/cert.pem
     restart: unless-stopped
     labels:
       app: vn-market-intelligence
